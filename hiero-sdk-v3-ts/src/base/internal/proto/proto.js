/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-mixed-operators, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars, default-case, jsdoc/require-param*/
import $protobuf from "protobufjs/minimal.js";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;
const $Object = $util.global.Object, $undefined = $util.global.undefined, $Error = $util.global.Error, $RangeError = $util.global.RangeError, $TypeError = $util.global.TypeError, $Number = $util.global.Number, $parseInt = $util.global.parseInt, $String = $util.global.String, $BigInt = $util.global.BigInt, $Array = $util.global.Array, $Boolean = $util.global.Boolean, $isFinite = $util.global.isFinite;

// Exported root namespace
const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const com = $root.com = (() => {

    /**
     * Namespace com.
     * @exports com
     * @namespace
     */
    const com = {};

    com.hedera = (function() {

        /**
         * Namespace hedera.
         * @memberof com
         * @namespace
         */
        const hedera = {};

        hedera.hapi = (function() {

            /**
             * Namespace hapi.
             * @memberof com.hedera
             * @namespace
             */
            const hapi = {};

            hapi.platform = (function() {

                /**
                 * Namespace platform.
                 * @memberof com.hedera.hapi
                 * @namespace
                 */
                const platform = {};

                platform.event = (function() {

                    /**
                     * Namespace event.
                     * @memberof com.hedera.hapi.platform
                     * @namespace
                     */
                    const event = {};

                    event.EventConsensusData = (function() {

                        /**
                         * Properties of an EventConsensusData.
                         * @typedef {Object} com.hedera.hapi.platform.event.EventConsensusData.$Properties
                         * @property {proto.Timestamp.$Properties|null} [consensusTimestamp] A consensus timestamp.<br/>
                         * The network's consensus agreement on a timestamp for this event.
                         * <p>
                         * This timestamp MUST be strictly greater than the `consensus_timestamp` of
                         * the previous consensus event.<br/>
                         * This is a consensus value and MAY NOT match real-world "wall clock" time.
                         * @property {number|Long|null} [consensusOrder] A consensus order sequence number.<br/>
                         * A non-negative sequence number that identifies an event's consensus order
                         * since genesis.
                         * <p>
                         * This SHALL be the unique for each consensus event.<br/>
                         * This SHALL always increase, and SHALL NOT decrease.<br/>
                         * This SHALL increment by one for each consensus event.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of an EventConsensusData.
                         * @memberof com.hedera.hapi.platform.event
                         * @interface IEventConsensusData
                         * @augments com.hedera.hapi.platform.event.EventConsensusData.$Properties
                         * @deprecated Use com.hedera.hapi.platform.event.EventConsensusData.$Properties instead.
                         */

                        /**
                         * Shape of an EventConsensusData.
                         * @typedef {com.hedera.hapi.platform.event.EventConsensusData.$Properties} com.hedera.hapi.platform.event.EventConsensusData.$Shape
                         */

                        /**
                         * Constructs a new EventConsensusData.
                         * @memberof com.hedera.hapi.platform.event
                         * @classdesc Event Consensus Data.<br/>
                         * This message records the critical values produced by consensus for an event.
                         * @constructor
                         * @param {com.hedera.hapi.platform.event.EventConsensusData.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const EventConsensusData = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A consensus timestamp.<br/>
                         * The network's consensus agreement on a timestamp for this event.
                         * <p>
                         * This timestamp MUST be strictly greater than the `consensus_timestamp` of
                         * the previous consensus event.<br/>
                         * This is a consensus value and MAY NOT match real-world "wall clock" time.
                         * @member {proto.Timestamp.$Properties|null|undefined} consensusTimestamp
                         * @memberof com.hedera.hapi.platform.event.EventConsensusData
                         * @instance
                         */
                        EventConsensusData.prototype.consensusTimestamp = null;

                        /**
                         * A consensus order sequence number.<br/>
                         * A non-negative sequence number that identifies an event's consensus order
                         * since genesis.
                         * <p>
                         * This SHALL be the unique for each consensus event.<br/>
                         * This SHALL always increase, and SHALL NOT decrease.<br/>
                         * This SHALL increment by one for each consensus event.
                         * @member {number|Long} consensusOrder
                         * @memberof com.hedera.hapi.platform.event.EventConsensusData
                         * @instance
                         */
                        EventConsensusData.prototype.consensusOrder = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * Creates a new EventConsensusData instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.event.EventConsensusData
                         * @static
                         * @param {com.hedera.hapi.platform.event.EventConsensusData.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.event.EventConsensusData} EventConsensusData instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.event.EventConsensusData.$Shape): com.hedera.hapi.platform.event.EventConsensusData & com.hedera.hapi.platform.event.EventConsensusData.$Shape;
                         *   (properties?: com.hedera.hapi.platform.event.EventConsensusData.$Properties): com.hedera.hapi.platform.event.EventConsensusData;
                         * }}
                         */
                        EventConsensusData.create = function(properties) {
                            return new EventConsensusData(properties);
                        };

                        /**
                         * Encodes the specified EventConsensusData message. Does not implicitly {@link com.hedera.hapi.platform.event.EventConsensusData.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.event.EventConsensusData
                         * @static
                         * @param {com.hedera.hapi.platform.event.EventConsensusData.$Properties} message EventConsensusData message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EventConsensusData.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp"))
                                $root.proto.Timestamp.encode(message.consensusTimestamp, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.consensusOrder != null && $Object.hasOwnProperty.call(message, "consensusOrder") && (typeof message.consensusOrder === "object" ? message.consensusOrder.low || message.consensusOrder.high : message.consensusOrder !== 0))
                                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.consensusOrder);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified EventConsensusData message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.event.EventConsensusData.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.event.EventConsensusData
                         * @static
                         * @param {com.hedera.hapi.platform.event.EventConsensusData.$Properties} message EventConsensusData message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EventConsensusData.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes an EventConsensusData message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.event.EventConsensusData
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.event.EventConsensusData & com.hedera.hapi.platform.event.EventConsensusData.$Shape} EventConsensusData
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EventConsensusData.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.event.EventConsensusData();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.consensusTimestamp = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusTimestamp);
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.consensusOrder = value;
                                        else
                                            delete message.consensusOrder;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes an EventConsensusData message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.event.EventConsensusData
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.event.EventConsensusData & com.hedera.hapi.platform.event.EventConsensusData.$Shape} EventConsensusData
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EventConsensusData.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies an EventConsensusData message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.event.EventConsensusData
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        EventConsensusData.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp")) {
                                let error = $root.proto.Timestamp.verify(message.consensusTimestamp, _depth + 1);
                                if (error)
                                    return "consensusTimestamp." + error;
                            }
                            if (message.consensusOrder != null && $Object.hasOwnProperty.call(message, "consensusOrder"))
                                if (!$util.isInteger(message.consensusOrder) && !(message.consensusOrder && $util.isInteger(message.consensusOrder.low) && $util.isInteger(message.consensusOrder.high)))
                                    return "consensusOrder: integer|Long expected";
                            return null;
                        };

                        /**
                         * Creates an EventConsensusData message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.event.EventConsensusData
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.event.EventConsensusData} EventConsensusData
                         */
                        EventConsensusData.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.event.EventConsensusData)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.event.EventConsensusData: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.event.EventConsensusData();
                            if (object.consensusTimestamp != null) {
                                if (!$util.isObject(object.consensusTimestamp))
                                    throw $TypeError(".com.hedera.hapi.platform.event.EventConsensusData.consensusTimestamp: object expected");
                                message.consensusTimestamp = $root.proto.Timestamp.fromObject(object.consensusTimestamp, _depth + 1);
                            }
                            if (object.consensusOrder != null)
                                if (typeof object.consensusOrder === "object" ? object.consensusOrder.low || object.consensusOrder.high : $Number(object.consensusOrder) !== 0)
                                    if ($util.Long)
                                        message.consensusOrder = $util.Long.fromValue(object.consensusOrder, true);
                                    else if (typeof object.consensusOrder === "string")
                                        message.consensusOrder = $parseInt(object.consensusOrder, 10);
                                    else if (typeof object.consensusOrder === "number")
                                        message.consensusOrder = object.consensusOrder;
                                    else if (typeof object.consensusOrder === "object")
                                        message.consensusOrder = new $util.LongBits(object.consensusOrder.low >>> 0, object.consensusOrder.high >>> 0).toNumber(true);
                            return message;
                        };

                        /**
                         * Creates a plain object from an EventConsensusData message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.event.EventConsensusData
                         * @static
                         * @param {com.hedera.hapi.platform.event.EventConsensusData} message EventConsensusData
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        EventConsensusData.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                object.consensusTimestamp = null;
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.consensusOrder = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.consensusOrder = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                            }
                            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp"))
                                object.consensusTimestamp = $root.proto.Timestamp.toObject(message.consensusTimestamp, options, _depth + 1);
                            if (message.consensusOrder != null && $Object.hasOwnProperty.call(message, "consensusOrder"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.consensusOrder = typeof message.consensusOrder === "number" ? $BigInt(message.consensusOrder) : $util.Long.fromBits(message.consensusOrder.low >>> 0, message.consensusOrder.high >>> 0, true).toBigInt();
                                else if (typeof message.consensusOrder === "number")
                                    object.consensusOrder = options.longs === $String ? $String(message.consensusOrder) : message.consensusOrder;
                                else
                                    object.consensusOrder = options.longs === $String ? $util.Long.prototype.toString.call(message.consensusOrder) : options.longs === $Number ? new $util.LongBits(message.consensusOrder.low >>> 0, message.consensusOrder.high >>> 0).toNumber(true) : message.consensusOrder;
                            return object;
                        };

                        /**
                         * Converts this EventConsensusData to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.event.EventConsensusData
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        EventConsensusData.prototype.toJSON = function() {
                            return EventConsensusData.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for EventConsensusData
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.event.EventConsensusData
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        EventConsensusData.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.event.EventConsensusData";
                        };

                        return EventConsensusData;
                    })();

                    event.EventDescriptor = (function() {

                        /**
                         * Properties of an EventDescriptor.
                         * @typedef {Object} com.hedera.hapi.platform.event.EventDescriptor.$Properties
                         * @property {Uint8Array|null} [hash] The hash of the event.<br/>
                         * The hash SHALL be a SHA-384 hash.<br/>
                         * The hash SHALL have the following inputs, in the specified order:<br/>
                         * 1. The bytes of the `EventCore` protobuf<br/>
                         * 2. The SHA-384 hash of each individual `EventTransaction`, in the order the transactions appear in the `event_transactions` field of the `GossipEvent` protobuf
                         * @property {number|Long|null} [creatorNodeId] The creator node identifier.<br/>
                         * This SHALL be the unique identifier for the node that created the event.<br/>
                         * This SHALL match the ID of the node as it appears in the address book.
                         * @property {number|Long|null} [birthRound] The birth round of the event.<br/>
                         * The birth round SHALL be the pending consensus round at the time the event is created.<br/>
                         * The pending consensus round SHALL be **one greater** than the latest round to reach consensus.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of an EventDescriptor.
                         * @memberof com.hedera.hapi.platform.event
                         * @interface IEventDescriptor
                         * @augments com.hedera.hapi.platform.event.EventDescriptor.$Properties
                         * @deprecated Use com.hedera.hapi.platform.event.EventDescriptor.$Properties instead.
                         */

                        /**
                         * Shape of an EventDescriptor.
                         * @typedef {com.hedera.hapi.platform.event.EventDescriptor.$Properties} com.hedera.hapi.platform.event.EventDescriptor.$Shape
                         */

                        /**
                         * Constructs a new EventDescriptor.
                         * @memberof com.hedera.hapi.platform.event
                         * @classdesc Unique identifier for an event.
                         * @constructor
                         * @param {com.hedera.hapi.platform.event.EventDescriptor.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const EventDescriptor = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The hash of the event.<br/>
                         * The hash SHALL be a SHA-384 hash.<br/>
                         * The hash SHALL have the following inputs, in the specified order:<br/>
                         * 1. The bytes of the `EventCore` protobuf<br/>
                         * 2. The SHA-384 hash of each individual `EventTransaction`, in the order the transactions appear in the `event_transactions` field of the `GossipEvent` protobuf
                         * @member {Uint8Array} hash
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @instance
                         */
                        EventDescriptor.prototype.hash = $util.newBuffer([]);

                        /**
                         * The creator node identifier.<br/>
                         * This SHALL be the unique identifier for the node that created the event.<br/>
                         * This SHALL match the ID of the node as it appears in the address book.
                         * @member {number|Long} creatorNodeId
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @instance
                         */
                        EventDescriptor.prototype.creatorNodeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                        /**
                         * The birth round of the event.<br/>
                         * The birth round SHALL be the pending consensus round at the time the event is created.<br/>
                         * The pending consensus round SHALL be **one greater** than the latest round to reach consensus.
                         * @member {number|Long} birthRound
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @instance
                         */
                        EventDescriptor.prototype.birthRound = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                        /**
                         * Creates a new EventDescriptor instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @static
                         * @param {com.hedera.hapi.platform.event.EventDescriptor.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.event.EventDescriptor} EventDescriptor instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.event.EventDescriptor.$Shape): com.hedera.hapi.platform.event.EventDescriptor & com.hedera.hapi.platform.event.EventDescriptor.$Shape;
                         *   (properties?: com.hedera.hapi.platform.event.EventDescriptor.$Properties): com.hedera.hapi.platform.event.EventDescriptor;
                         * }}
                         */
                        EventDescriptor.create = function(properties) {
                            return new EventDescriptor(properties);
                        };

                        /**
                         * Encodes the specified EventDescriptor message. Does not implicitly {@link com.hedera.hapi.platform.event.EventDescriptor.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @static
                         * @param {com.hedera.hapi.platform.event.EventDescriptor.$Properties} message EventDescriptor message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EventDescriptor.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash.length)
                                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.hash);
                            if (message.creatorNodeId != null && $Object.hasOwnProperty.call(message, "creatorNodeId") && (typeof message.creatorNodeId === "object" ? message.creatorNodeId.low || message.creatorNodeId.high : message.creatorNodeId !== 0))
                                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.creatorNodeId);
                            if (message.birthRound != null && $Object.hasOwnProperty.call(message, "birthRound") && (typeof message.birthRound === "object" ? message.birthRound.low || message.birthRound.high : message.birthRound !== 0))
                                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.birthRound);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified EventDescriptor message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.event.EventDescriptor.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @static
                         * @param {com.hedera.hapi.platform.event.EventDescriptor.$Properties} message EventDescriptor message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EventDescriptor.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes an EventDescriptor message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.event.EventDescriptor & com.hedera.hapi.platform.event.EventDescriptor.$Shape} EventDescriptor
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EventDescriptor.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.event.EventDescriptor();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.hash = value;
                                        else
                                            delete message.hash;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                            message.creatorNodeId = value;
                                        else
                                            delete message.creatorNodeId;
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                            message.birthRound = value;
                                        else
                                            delete message.birthRound;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes an EventDescriptor message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.event.EventDescriptor & com.hedera.hapi.platform.event.EventDescriptor.$Shape} EventDescriptor
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EventDescriptor.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies an EventDescriptor message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        EventDescriptor.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                                if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                                    return "hash: buffer expected";
                            if (message.creatorNodeId != null && $Object.hasOwnProperty.call(message, "creatorNodeId"))
                                if (!$util.isInteger(message.creatorNodeId) && !(message.creatorNodeId && $util.isInteger(message.creatorNodeId.low) && $util.isInteger(message.creatorNodeId.high)))
                                    return "creatorNodeId: integer|Long expected";
                            if (message.birthRound != null && $Object.hasOwnProperty.call(message, "birthRound"))
                                if (!$util.isInteger(message.birthRound) && !(message.birthRound && $util.isInteger(message.birthRound.low) && $util.isInteger(message.birthRound.high)))
                                    return "birthRound: integer|Long expected";
                            return null;
                        };

                        /**
                         * Creates an EventDescriptor message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.event.EventDescriptor} EventDescriptor
                         */
                        EventDescriptor.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.event.EventDescriptor)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.event.EventDescriptor: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.event.EventDescriptor();
                            if (object.hash != null)
                                if (object.hash.length)
                                    if (typeof object.hash === "string")
                                        $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                                    else if (object.hash.length >= 0)
                                        message.hash = object.hash;
                            if (object.creatorNodeId != null)
                                if (typeof object.creatorNodeId === "object" ? object.creatorNodeId.low || object.creatorNodeId.high : $Number(object.creatorNodeId) !== 0)
                                    if ($util.Long)
                                        message.creatorNodeId = $util.Long.fromValue(object.creatorNodeId, false);
                                    else if (typeof object.creatorNodeId === "string")
                                        message.creatorNodeId = $parseInt(object.creatorNodeId, 10);
                                    else if (typeof object.creatorNodeId === "number")
                                        message.creatorNodeId = object.creatorNodeId;
                                    else if (typeof object.creatorNodeId === "object")
                                        message.creatorNodeId = new $util.LongBits(object.creatorNodeId.low >>> 0, object.creatorNodeId.high >>> 0).toNumber();
                            if (object.birthRound != null)
                                if (typeof object.birthRound === "object" ? object.birthRound.low || object.birthRound.high : $Number(object.birthRound) !== 0)
                                    if ($util.Long)
                                        message.birthRound = $util.Long.fromValue(object.birthRound, false);
                                    else if (typeof object.birthRound === "string")
                                        message.birthRound = $parseInt(object.birthRound, 10);
                                    else if (typeof object.birthRound === "number")
                                        message.birthRound = object.birthRound;
                                    else if (typeof object.birthRound === "object")
                                        message.birthRound = new $util.LongBits(object.birthRound.low >>> 0, object.birthRound.high >>> 0).toNumber();
                            return message;
                        };

                        /**
                         * Creates a plain object from an EventDescriptor message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @static
                         * @param {com.hedera.hapi.platform.event.EventDescriptor} message EventDescriptor
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        EventDescriptor.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                if (options.bytes === $String)
                                    object.hash = "";
                                else {
                                    object.hash = [];
                                    if (options.bytes !== $Array)
                                        object.hash = $util.newBuffer(object.hash);
                                }
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, false);
                                    object.creatorNodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.creatorNodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, false);
                                    object.birthRound = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.birthRound = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                            }
                            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                                object.hash = options.bytes === $String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hash) : message.hash;
                            if (message.creatorNodeId != null && $Object.hasOwnProperty.call(message, "creatorNodeId"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.creatorNodeId = typeof message.creatorNodeId === "number" ? $BigInt(message.creatorNodeId) : $util.Long.fromBits(message.creatorNodeId.low >>> 0, message.creatorNodeId.high >>> 0, false).toBigInt();
                                else if (typeof message.creatorNodeId === "number")
                                    object.creatorNodeId = options.longs === $String ? $String(message.creatorNodeId) : message.creatorNodeId;
                                else
                                    object.creatorNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.creatorNodeId) : options.longs === $Number ? new $util.LongBits(message.creatorNodeId.low >>> 0, message.creatorNodeId.high >>> 0).toNumber() : message.creatorNodeId;
                            if (message.birthRound != null && $Object.hasOwnProperty.call(message, "birthRound"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.birthRound = typeof message.birthRound === "number" ? $BigInt(message.birthRound) : $util.Long.fromBits(message.birthRound.low >>> 0, message.birthRound.high >>> 0, false).toBigInt();
                                else if (typeof message.birthRound === "number")
                                    object.birthRound = options.longs === $String ? $String(message.birthRound) : message.birthRound;
                                else
                                    object.birthRound = options.longs === $String ? $util.Long.prototype.toString.call(message.birthRound) : options.longs === $Number ? new $util.LongBits(message.birthRound.low >>> 0, message.birthRound.high >>> 0).toNumber() : message.birthRound;
                            return object;
                        };

                        /**
                         * Converts this EventDescriptor to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        EventDescriptor.prototype.toJSON = function() {
                            return EventDescriptor.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for EventDescriptor
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.event.EventDescriptor
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        EventDescriptor.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.event.EventDescriptor";
                        };

                        return EventDescriptor;
                    })();

                    event.EventCore = (function() {

                        /**
                         * Properties of an EventCore.
                         * @typedef {Object} com.hedera.hapi.platform.event.EventCore.$Properties
                         * @property {number|Long|null} [creatorNodeId] The creator node identifier.<br/>
                         * This SHALL be the unique identifier for the node that created the event.<br/>
                         * This SHALL match the ID of the node as it appears in the address book.
                         * @property {number|Long|null} [birthRound] The birth round of the event.<br/>
                         * The birth round SHALL be the pending consensus round at the time the event is created.<br/>
                         * The pending consensus round SHALL be **one greater** than the latest round to reach consensus.
                         * @property {proto.Timestamp.$Properties|null} [timeCreated] The wall clock time at which the latest information affecting this event was received by this node.<br/>
                         * The information includes: parents, transactions and birth round<br/>
                         * If the event is a genesis event, and has no transactions, this value will just be the wall clock time when creating this event.<br/>
                         * If the event has a self parent, this timestamp MUST be strictly greater than the `time_created` of the self parent.
                         * @property {number|Long|null} [coin] A random number used in the unlikely case that the consensus algorithm gets to a coin vote.<br/>
                         * This number SHALL be used to break ties in the consensus algorithm.<br/>
                         * The number SHALL be between 0 and the size of the roster (inclusive) for the event's birth round.<br/>
                         * For example, if the roster size is 10, the minimum value is 0 and the maximum value is 10.<br/>
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of an EventCore.
                         * @memberof com.hedera.hapi.platform.event
                         * @interface IEventCore
                         * @augments com.hedera.hapi.platform.event.EventCore.$Properties
                         * @deprecated Use com.hedera.hapi.platform.event.EventCore.$Properties instead.
                         */

                        /**
                         * Shape of an EventCore.
                         * @typedef {com.hedera.hapi.platform.event.EventCore.$Properties} com.hedera.hapi.platform.event.EventCore.$Shape
                         */

                        /**
                         * Constructs a new EventCore.
                         * @memberof com.hedera.hapi.platform.event
                         * @classdesc Contains information about an event and its parents.
                         * @constructor
                         * @param {com.hedera.hapi.platform.event.EventCore.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const EventCore = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The creator node identifier.<br/>
                         * This SHALL be the unique identifier for the node that created the event.<br/>
                         * This SHALL match the ID of the node as it appears in the address book.
                         * @member {number|Long} creatorNodeId
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @instance
                         */
                        EventCore.prototype.creatorNodeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                        /**
                         * The birth round of the event.<br/>
                         * The birth round SHALL be the pending consensus round at the time the event is created.<br/>
                         * The pending consensus round SHALL be **one greater** than the latest round to reach consensus.
                         * @member {number|Long} birthRound
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @instance
                         */
                        EventCore.prototype.birthRound = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                        /**
                         * The wall clock time at which the latest information affecting this event was received by this node.<br/>
                         * The information includes: parents, transactions and birth round<br/>
                         * If the event is a genesis event, and has no transactions, this value will just be the wall clock time when creating this event.<br/>
                         * If the event has a self parent, this timestamp MUST be strictly greater than the `time_created` of the self parent.
                         * @member {proto.Timestamp.$Properties|null|undefined} timeCreated
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @instance
                         */
                        EventCore.prototype.timeCreated = null;

                        /**
                         * A random number used in the unlikely case that the consensus algorithm gets to a coin vote.<br/>
                         * This number SHALL be used to break ties in the consensus algorithm.<br/>
                         * The number SHALL be between 0 and the size of the roster (inclusive) for the event's birth round.<br/>
                         * For example, if the roster size is 10, the minimum value is 0 and the maximum value is 10.<br/>
                         * @member {number|Long} coin
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @instance
                         */
                        EventCore.prototype.coin = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                        /**
                         * Creates a new EventCore instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @static
                         * @param {com.hedera.hapi.platform.event.EventCore.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.event.EventCore} EventCore instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.event.EventCore.$Shape): com.hedera.hapi.platform.event.EventCore & com.hedera.hapi.platform.event.EventCore.$Shape;
                         *   (properties?: com.hedera.hapi.platform.event.EventCore.$Properties): com.hedera.hapi.platform.event.EventCore;
                         * }}
                         */
                        EventCore.create = function(properties) {
                            return new EventCore(properties);
                        };

                        /**
                         * Encodes the specified EventCore message. Does not implicitly {@link com.hedera.hapi.platform.event.EventCore.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @static
                         * @param {com.hedera.hapi.platform.event.EventCore.$Properties} message EventCore message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EventCore.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.creatorNodeId != null && $Object.hasOwnProperty.call(message, "creatorNodeId") && (typeof message.creatorNodeId === "object" ? message.creatorNodeId.low || message.creatorNodeId.high : message.creatorNodeId !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.creatorNodeId);
                            if (message.birthRound != null && $Object.hasOwnProperty.call(message, "birthRound") && (typeof message.birthRound === "object" ? message.birthRound.low || message.birthRound.high : message.birthRound !== 0))
                                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.birthRound);
                            if (message.timeCreated != null && $Object.hasOwnProperty.call(message, "timeCreated"))
                                $root.proto.Timestamp.encode(message.timeCreated, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.coin != null && $Object.hasOwnProperty.call(message, "coin") && (typeof message.coin === "object" ? message.coin.low || message.coin.high : message.coin !== 0))
                                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.coin);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified EventCore message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.event.EventCore.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @static
                         * @param {com.hedera.hapi.platform.event.EventCore.$Properties} message EventCore message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EventCore.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes an EventCore message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.event.EventCore & com.hedera.hapi.platform.event.EventCore.$Shape} EventCore
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EventCore.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.event.EventCore();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                            message.creatorNodeId = value;
                                        else
                                            delete message.creatorNodeId;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                            message.birthRound = value;
                                        else
                                            delete message.birthRound;
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        message.timeCreated = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.timeCreated);
                                        continue;
                                    }
                                case 5: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                            message.coin = value;
                                        else
                                            delete message.coin;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes an EventCore message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.event.EventCore & com.hedera.hapi.platform.event.EventCore.$Shape} EventCore
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EventCore.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies an EventCore message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        EventCore.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.creatorNodeId != null && $Object.hasOwnProperty.call(message, "creatorNodeId"))
                                if (!$util.isInteger(message.creatorNodeId) && !(message.creatorNodeId && $util.isInteger(message.creatorNodeId.low) && $util.isInteger(message.creatorNodeId.high)))
                                    return "creatorNodeId: integer|Long expected";
                            if (message.birthRound != null && $Object.hasOwnProperty.call(message, "birthRound"))
                                if (!$util.isInteger(message.birthRound) && !(message.birthRound && $util.isInteger(message.birthRound.low) && $util.isInteger(message.birthRound.high)))
                                    return "birthRound: integer|Long expected";
                            if (message.timeCreated != null && $Object.hasOwnProperty.call(message, "timeCreated")) {
                                let error = $root.proto.Timestamp.verify(message.timeCreated, _depth + 1);
                                if (error)
                                    return "timeCreated." + error;
                            }
                            if (message.coin != null && $Object.hasOwnProperty.call(message, "coin"))
                                if (!$util.isInteger(message.coin) && !(message.coin && $util.isInteger(message.coin.low) && $util.isInteger(message.coin.high)))
                                    return "coin: integer|Long expected";
                            return null;
                        };

                        /**
                         * Creates an EventCore message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.event.EventCore} EventCore
                         */
                        EventCore.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.event.EventCore)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.event.EventCore: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.event.EventCore();
                            if (object.creatorNodeId != null)
                                if (typeof object.creatorNodeId === "object" ? object.creatorNodeId.low || object.creatorNodeId.high : $Number(object.creatorNodeId) !== 0)
                                    if ($util.Long)
                                        message.creatorNodeId = $util.Long.fromValue(object.creatorNodeId, false);
                                    else if (typeof object.creatorNodeId === "string")
                                        message.creatorNodeId = $parseInt(object.creatorNodeId, 10);
                                    else if (typeof object.creatorNodeId === "number")
                                        message.creatorNodeId = object.creatorNodeId;
                                    else if (typeof object.creatorNodeId === "object")
                                        message.creatorNodeId = new $util.LongBits(object.creatorNodeId.low >>> 0, object.creatorNodeId.high >>> 0).toNumber();
                            if (object.birthRound != null)
                                if (typeof object.birthRound === "object" ? object.birthRound.low || object.birthRound.high : $Number(object.birthRound) !== 0)
                                    if ($util.Long)
                                        message.birthRound = $util.Long.fromValue(object.birthRound, false);
                                    else if (typeof object.birthRound === "string")
                                        message.birthRound = $parseInt(object.birthRound, 10);
                                    else if (typeof object.birthRound === "number")
                                        message.birthRound = object.birthRound;
                                    else if (typeof object.birthRound === "object")
                                        message.birthRound = new $util.LongBits(object.birthRound.low >>> 0, object.birthRound.high >>> 0).toNumber();
                            if (object.timeCreated != null) {
                                if (!$util.isObject(object.timeCreated))
                                    throw $TypeError(".com.hedera.hapi.platform.event.EventCore.timeCreated: object expected");
                                message.timeCreated = $root.proto.Timestamp.fromObject(object.timeCreated, _depth + 1);
                            }
                            if (object.coin != null)
                                if (typeof object.coin === "object" ? object.coin.low || object.coin.high : $Number(object.coin) !== 0)
                                    if ($util.Long)
                                        message.coin = $util.Long.fromValue(object.coin, false);
                                    else if (typeof object.coin === "string")
                                        message.coin = $parseInt(object.coin, 10);
                                    else if (typeof object.coin === "number")
                                        message.coin = object.coin;
                                    else if (typeof object.coin === "object")
                                        message.coin = new $util.LongBits(object.coin.low >>> 0, object.coin.high >>> 0).toNumber();
                            return message;
                        };

                        /**
                         * Creates a plain object from an EventCore message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @static
                         * @param {com.hedera.hapi.platform.event.EventCore} message EventCore
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        EventCore.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, false);
                                    object.creatorNodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.creatorNodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, false);
                                    object.birthRound = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.birthRound = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                object.timeCreated = null;
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, false);
                                    object.coin = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.coin = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                            }
                            if (message.creatorNodeId != null && $Object.hasOwnProperty.call(message, "creatorNodeId"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.creatorNodeId = typeof message.creatorNodeId === "number" ? $BigInt(message.creatorNodeId) : $util.Long.fromBits(message.creatorNodeId.low >>> 0, message.creatorNodeId.high >>> 0, false).toBigInt();
                                else if (typeof message.creatorNodeId === "number")
                                    object.creatorNodeId = options.longs === $String ? $String(message.creatorNodeId) : message.creatorNodeId;
                                else
                                    object.creatorNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.creatorNodeId) : options.longs === $Number ? new $util.LongBits(message.creatorNodeId.low >>> 0, message.creatorNodeId.high >>> 0).toNumber() : message.creatorNodeId;
                            if (message.birthRound != null && $Object.hasOwnProperty.call(message, "birthRound"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.birthRound = typeof message.birthRound === "number" ? $BigInt(message.birthRound) : $util.Long.fromBits(message.birthRound.low >>> 0, message.birthRound.high >>> 0, false).toBigInt();
                                else if (typeof message.birthRound === "number")
                                    object.birthRound = options.longs === $String ? $String(message.birthRound) : message.birthRound;
                                else
                                    object.birthRound = options.longs === $String ? $util.Long.prototype.toString.call(message.birthRound) : options.longs === $Number ? new $util.LongBits(message.birthRound.low >>> 0, message.birthRound.high >>> 0).toNumber() : message.birthRound;
                            if (message.timeCreated != null && $Object.hasOwnProperty.call(message, "timeCreated"))
                                object.timeCreated = $root.proto.Timestamp.toObject(message.timeCreated, options, _depth + 1);
                            if (message.coin != null && $Object.hasOwnProperty.call(message, "coin"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.coin = typeof message.coin === "number" ? $BigInt(message.coin) : $util.Long.fromBits(message.coin.low >>> 0, message.coin.high >>> 0, false).toBigInt();
                                else if (typeof message.coin === "number")
                                    object.coin = options.longs === $String ? $String(message.coin) : message.coin;
                                else
                                    object.coin = options.longs === $String ? $util.Long.prototype.toString.call(message.coin) : options.longs === $Number ? new $util.LongBits(message.coin.low >>> 0, message.coin.high >>> 0).toNumber() : message.coin;
                            return object;
                        };

                        /**
                         * Converts this EventCore to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        EventCore.prototype.toJSON = function() {
                            return EventCore.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for EventCore
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.event.EventCore
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        EventCore.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.event.EventCore";
                        };

                        return EventCore;
                    })();

                    event.GossipEvent = (function() {

                        /**
                         * Properties of a GossipEvent.
                         * @typedef {Object} com.hedera.hapi.platform.event.GossipEvent.$Properties
                         * @property {com.hedera.hapi.platform.event.EventCore.$Properties|null} [eventCore] The core event data
                         * @property {Uint8Array|null} [signature] A node signature on the event hash.<br/>
                         * The signature SHALL be created with the SHA384withRSA algorithm.<br/>
                         * The signature MUST verify using the public key belonging to the `event_creator`.<br/>
                         * The `event_creator` public key SHALL be read from the address book that corresponds to the event's birth round.<br/>
                         * The signed event hash SHALL be a SHA-384 hash.<br/>
                         * The signed event hash SHALL have the following inputs, in the specified order:<br/>
                         * 1. The bytes of the serialized `event_core` field<br/>
                         * 2. The bytes of serialized parent event descriptors, in order they appear in 'parents' field.
                         * 3. The concatenation of SHA-384 hashes of every individual transaction byte array, in the order the transactions appear in the `transactions` field
                         * @property {Array.<Uint8Array>|null} [transactions] A list of serialized transactions.
                         * <p>
                         * This field MAY contain zero transactions.<br/>
                         * Each transaction in this list SHALL be presented exactly as
                         * it was supplied to the consensus algorithm.<br/>
                         * This field MUST contain one entry for each transaction
                         * included in this gossip event.
                         * @property {Array.<com.hedera.hapi.platform.event.EventDescriptor.$Properties>|null} [parents] A list of EventDescriptors representing the parents of this event.<br/>
                         * The list of parents SHALL include zero or one self parents, and zero or more other parents.<br/>
                         * The first element of the list SHALL be the self parent, if one exists.<br/>
                         * The list of parents SHALL NOT include more than one parent from the same creator.
                         * <p>
                         * NOTE: This field is currently being migrated from EventCore to GossipEvent.
                         * Once the migration is complete, this field will be removed from EventCore.
                         * While migration is ongoing, the expectation is that only one of the two
                         * fields will be set, but not both.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a GossipEvent.
                         * @memberof com.hedera.hapi.platform.event
                         * @interface IGossipEvent
                         * @augments com.hedera.hapi.platform.event.GossipEvent.$Properties
                         * @deprecated Use com.hedera.hapi.platform.event.GossipEvent.$Properties instead.
                         */

                        /**
                         * Shape of a GossipEvent.
                         * @typedef {com.hedera.hapi.platform.event.GossipEvent.$Properties} com.hedera.hapi.platform.event.GossipEvent.$Shape
                         */

                        /**
                         * Constructs a new GossipEvent.
                         * @memberof com.hedera.hapi.platform.event
                         * @classdesc An event that is sent and received via gossip
                         * @constructor
                         * @param {com.hedera.hapi.platform.event.GossipEvent.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const GossipEvent = function (properties) {
                            this.transactions = [];
                            this.parents = [];
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The core event data
                         * @member {com.hedera.hapi.platform.event.EventCore.$Properties|null|undefined} eventCore
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @instance
                         */
                        GossipEvent.prototype.eventCore = null;

                        /**
                         * A node signature on the event hash.<br/>
                         * The signature SHALL be created with the SHA384withRSA algorithm.<br/>
                         * The signature MUST verify using the public key belonging to the `event_creator`.<br/>
                         * The `event_creator` public key SHALL be read from the address book that corresponds to the event's birth round.<br/>
                         * The signed event hash SHALL be a SHA-384 hash.<br/>
                         * The signed event hash SHALL have the following inputs, in the specified order:<br/>
                         * 1. The bytes of the serialized `event_core` field<br/>
                         * 2. The bytes of serialized parent event descriptors, in order they appear in 'parents' field.
                         * 3. The concatenation of SHA-384 hashes of every individual transaction byte array, in the order the transactions appear in the `transactions` field
                         * @member {Uint8Array} signature
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @instance
                         */
                        GossipEvent.prototype.signature = $util.newBuffer([]);

                        /**
                         * A list of serialized transactions.
                         * <p>
                         * This field MAY contain zero transactions.<br/>
                         * Each transaction in this list SHALL be presented exactly as
                         * it was supplied to the consensus algorithm.<br/>
                         * This field MUST contain one entry for each transaction
                         * included in this gossip event.
                         * @member {Array.<Uint8Array>} transactions
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @instance
                         */
                        GossipEvent.prototype.transactions = $util.emptyArray;

                        /**
                         * A list of EventDescriptors representing the parents of this event.<br/>
                         * The list of parents SHALL include zero or one self parents, and zero or more other parents.<br/>
                         * The first element of the list SHALL be the self parent, if one exists.<br/>
                         * The list of parents SHALL NOT include more than one parent from the same creator.
                         * <p>
                         * NOTE: This field is currently being migrated from EventCore to GossipEvent.
                         * Once the migration is complete, this field will be removed from EventCore.
                         * While migration is ongoing, the expectation is that only one of the two
                         * fields will be set, but not both.
                         * @member {Array.<com.hedera.hapi.platform.event.EventDescriptor.$Properties>} parents
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @instance
                         */
                        GossipEvent.prototype.parents = $util.emptyArray;

                        /**
                         * Creates a new GossipEvent instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @static
                         * @param {com.hedera.hapi.platform.event.GossipEvent.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.event.GossipEvent} GossipEvent instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.event.GossipEvent.$Shape): com.hedera.hapi.platform.event.GossipEvent & com.hedera.hapi.platform.event.GossipEvent.$Shape;
                         *   (properties?: com.hedera.hapi.platform.event.GossipEvent.$Properties): com.hedera.hapi.platform.event.GossipEvent;
                         * }}
                         */
                        GossipEvent.create = function(properties) {
                            return new GossipEvent(properties);
                        };

                        /**
                         * Encodes the specified GossipEvent message. Does not implicitly {@link com.hedera.hapi.platform.event.GossipEvent.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @static
                         * @param {com.hedera.hapi.platform.event.GossipEvent.$Properties} message GossipEvent message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        GossipEvent.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.eventCore != null && $Object.hasOwnProperty.call(message, "eventCore"))
                                $root.com.hedera.hapi.platform.event.EventCore.encode(message.eventCore, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature") && message.signature.length)
                                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.signature);
                            if (message.transactions != null && message.transactions.length)
                                for (let i = 0; i < message.transactions.length; ++i)
                                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.transactions[i]);
                            if (message.parents != null && message.parents.length)
                                for (let i = 0; i < message.parents.length; ++i)
                                    $root.com.hedera.hapi.platform.event.EventDescriptor.encode(message.parents[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified GossipEvent message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.event.GossipEvent.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @static
                         * @param {com.hedera.hapi.platform.event.GossipEvent.$Properties} message GossipEvent message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        GossipEvent.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a GossipEvent message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.event.GossipEvent & com.hedera.hapi.platform.event.GossipEvent.$Shape} GossipEvent
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        GossipEvent.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.event.GossipEvent();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.eventCore = $root.com.hedera.hapi.platform.event.EventCore.decode(reader, reader.uint32(), $undefined, _depth + 1, message.eventCore);
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.signature = value;
                                        else
                                            delete message.signature;
                                        continue;
                                    }
                                case 4: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.transactions && message.transactions.length))
                                            message.transactions = [];
                                        message.transactions.push(reader.bytes());
                                        continue;
                                    }
                                case 5: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.parents && message.parents.length))
                                            message.parents = [];
                                        message.parents.push($root.com.hedera.hapi.platform.event.EventDescriptor.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a GossipEvent message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.event.GossipEvent & com.hedera.hapi.platform.event.GossipEvent.$Shape} GossipEvent
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        GossipEvent.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a GossipEvent message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        GossipEvent.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.eventCore != null && $Object.hasOwnProperty.call(message, "eventCore")) {
                                let error = $root.com.hedera.hapi.platform.event.EventCore.verify(message.eventCore, _depth + 1);
                                if (error)
                                    return "eventCore." + error;
                            }
                            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                                    return "signature: buffer expected";
                            if (message.transactions != null && $Object.hasOwnProperty.call(message, "transactions")) {
                                if (!$Array.isArray(message.transactions))
                                    return "transactions: array expected";
                                for (let i = 0; i < message.transactions.length; ++i)
                                    if (!(message.transactions[i] && typeof message.transactions[i].length === "number" || $util.isString(message.transactions[i])))
                                        return "transactions: buffer[] expected";
                            }
                            if (message.parents != null && $Object.hasOwnProperty.call(message, "parents")) {
                                if (!$Array.isArray(message.parents))
                                    return "parents: array expected";
                                for (let i = 0; i < message.parents.length; ++i) {
                                    let error = $root.com.hedera.hapi.platform.event.EventDescriptor.verify(message.parents[i], _depth + 1);
                                    if (error)
                                        return "parents." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates a GossipEvent message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.event.GossipEvent} GossipEvent
                         */
                        GossipEvent.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.event.GossipEvent)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.event.GossipEvent: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.event.GossipEvent();
                            if (object.eventCore != null) {
                                if (!$util.isObject(object.eventCore))
                                    throw $TypeError(".com.hedera.hapi.platform.event.GossipEvent.eventCore: object expected");
                                message.eventCore = $root.com.hedera.hapi.platform.event.EventCore.fromObject(object.eventCore, _depth + 1);
                            }
                            if (object.signature != null)
                                if (object.signature.length)
                                    if (typeof object.signature === "string")
                                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                                    else if (object.signature.length >= 0)
                                        message.signature = object.signature;
                            if (object.transactions) {
                                if (!$Array.isArray(object.transactions))
                                    throw $TypeError(".com.hedera.hapi.platform.event.GossipEvent.transactions: array expected");
                                message.transactions = $Array(object.transactions.length);
                                for (let i = 0; i < object.transactions.length; ++i)
                                    if (typeof object.transactions[i] === "string")
                                        $util.base64.decode(object.transactions[i], message.transactions[i] = $util.newBuffer($util.base64.length(object.transactions[i])), 0);
                                    else if (object.transactions[i].length >= 0)
                                        message.transactions[i] = object.transactions[i];
                            }
                            if (object.parents) {
                                if (!$Array.isArray(object.parents))
                                    throw $TypeError(".com.hedera.hapi.platform.event.GossipEvent.parents: array expected");
                                message.parents = $Array(object.parents.length);
                                for (let i = 0; i < object.parents.length; ++i) {
                                    if (!$util.isObject(object.parents[i]))
                                        throw $TypeError(".com.hedera.hapi.platform.event.GossipEvent.parents: object expected");
                                    message.parents[i] = $root.com.hedera.hapi.platform.event.EventDescriptor.fromObject(object.parents[i], _depth + 1);
                                }
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a GossipEvent message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @static
                         * @param {com.hedera.hapi.platform.event.GossipEvent} message GossipEvent
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        GossipEvent.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.arrays || options.defaults) {
                                object.transactions = [];
                                object.parents = [];
                            }
                            if (options.defaults) {
                                object.eventCore = null;
                                if (options.bytes === $String)
                                    object.signature = "";
                                else {
                                    object.signature = [];
                                    if (options.bytes !== $Array)
                                        object.signature = $util.newBuffer(object.signature);
                                }
                            }
                            if (message.eventCore != null && $Object.hasOwnProperty.call(message, "eventCore"))
                                object.eventCore = $root.com.hedera.hapi.platform.event.EventCore.toObject(message.eventCore, options, _depth + 1);
                            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                                object.signature = options.bytes === $String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.signature) : message.signature;
                            if (message.transactions && message.transactions.length) {
                                object.transactions = $Array(message.transactions.length);
                                for (let j = 0; j < message.transactions.length; ++j)
                                    object.transactions[j] = options.bytes === $String ? $util.base64.encode(message.transactions[j], 0, message.transactions[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.transactions[j]) : message.transactions[j];
                            }
                            if (message.parents && message.parents.length) {
                                object.parents = $Array(message.parents.length);
                                for (let j = 0; j < message.parents.length; ++j)
                                    object.parents[j] = $root.com.hedera.hapi.platform.event.EventDescriptor.toObject(message.parents[j], options, _depth + 1);
                            }
                            return object;
                        };

                        /**
                         * Converts this GossipEvent to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        GossipEvent.prototype.toJSON = function() {
                            return GossipEvent.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for GossipEvent
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.event.GossipEvent
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        GossipEvent.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.event.GossipEvent";
                        };

                        return GossipEvent;
                    })();

                    event.StateSignatureTransaction = (function() {

                        /**
                         * Properties of a StateSignatureTransaction.
                         * @typedef {Object} com.hedera.hapi.platform.event.StateSignatureTransaction.$Properties
                         * @property {number|Long|null} [round] The round number corresponding to the round number of the state snapshot
                         * being signed.<br/>
                         * This number MUST be greater than 0.
                         * @property {Uint8Array|null} [signature] The signature of state snapshot hash.<br/>
                         * This signature MUST be a RSA signature with a maximum length of 384 bytes.<br/>
                         * The signature algorithm used MUST be RSASSA-PKCS1-v1_5 with SHA-384.
                         * @property {Uint8Array|null} [hash] The hash of the state snapshot being signed.<br/>
                         * This hash MUST be a SHA-384 hash.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a StateSignatureTransaction.
                         * @memberof com.hedera.hapi.platform.event
                         * @interface IStateSignatureTransaction
                         * @augments com.hedera.hapi.platform.event.StateSignatureTransaction.$Properties
                         * @deprecated Use com.hedera.hapi.platform.event.StateSignatureTransaction.$Properties instead.
                         */

                        /**
                         * Shape of a StateSignatureTransaction.
                         * @typedef {com.hedera.hapi.platform.event.StateSignatureTransaction.$Properties} com.hedera.hapi.platform.event.StateSignatureTransaction.$Shape
                         */

                        /**
                         * Constructs a new StateSignatureTransaction.
                         * @memberof com.hedera.hapi.platform.event
                         * @classdesc An signature of a state snapshot gossiped to other nodes.
                         * Each node SHALL hash the root of the merkle tree of a state snapshot every
                         * round. Once this hash is calculated, it SHOULD be signed with the nodes
                         * private signing key. This signature, together with the hash SHOULD be added
                         * to an event as a StateSignatureTransaction.
                         * @constructor
                         * @param {com.hedera.hapi.platform.event.StateSignatureTransaction.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const StateSignatureTransaction = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The round number corresponding to the round number of the state snapshot
                         * being signed.<br/>
                         * This number MUST be greater than 0.
                         * @member {number|Long} round
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @instance
                         */
                        StateSignatureTransaction.prototype.round = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                        /**
                         * The signature of state snapshot hash.<br/>
                         * This signature MUST be a RSA signature with a maximum length of 384 bytes.<br/>
                         * The signature algorithm used MUST be RSASSA-PKCS1-v1_5 with SHA-384.
                         * @member {Uint8Array} signature
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @instance
                         */
                        StateSignatureTransaction.prototype.signature = $util.newBuffer([]);

                        /**
                         * The hash of the state snapshot being signed.<br/>
                         * This hash MUST be a SHA-384 hash.
                         * @member {Uint8Array} hash
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @instance
                         */
                        StateSignatureTransaction.prototype.hash = $util.newBuffer([]);

                        /**
                         * Creates a new StateSignatureTransaction instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @static
                         * @param {com.hedera.hapi.platform.event.StateSignatureTransaction.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.event.StateSignatureTransaction} StateSignatureTransaction instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.event.StateSignatureTransaction.$Shape): com.hedera.hapi.platform.event.StateSignatureTransaction & com.hedera.hapi.platform.event.StateSignatureTransaction.$Shape;
                         *   (properties?: com.hedera.hapi.platform.event.StateSignatureTransaction.$Properties): com.hedera.hapi.platform.event.StateSignatureTransaction;
                         * }}
                         */
                        StateSignatureTransaction.create = function(properties) {
                            return new StateSignatureTransaction(properties);
                        };

                        /**
                         * Encodes the specified StateSignatureTransaction message. Does not implicitly {@link com.hedera.hapi.platform.event.StateSignatureTransaction.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @static
                         * @param {com.hedera.hapi.platform.event.StateSignatureTransaction.$Properties} message StateSignatureTransaction message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        StateSignatureTransaction.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.round != null && $Object.hasOwnProperty.call(message, "round") && (typeof message.round === "object" ? message.round.low || message.round.high : message.round !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.round);
                            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature") && message.signature.length)
                                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.signature);
                            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash.length)
                                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.hash);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified StateSignatureTransaction message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.event.StateSignatureTransaction.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @static
                         * @param {com.hedera.hapi.platform.event.StateSignatureTransaction.$Properties} message StateSignatureTransaction message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        StateSignatureTransaction.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a StateSignatureTransaction message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.event.StateSignatureTransaction & com.hedera.hapi.platform.event.StateSignatureTransaction.$Shape} StateSignatureTransaction
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        StateSignatureTransaction.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.event.StateSignatureTransaction();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                            message.round = value;
                                        else
                                            delete message.round;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.signature = value;
                                        else
                                            delete message.signature;
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.hash = value;
                                        else
                                            delete message.hash;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a StateSignatureTransaction message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.event.StateSignatureTransaction & com.hedera.hapi.platform.event.StateSignatureTransaction.$Shape} StateSignatureTransaction
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        StateSignatureTransaction.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a StateSignatureTransaction message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        StateSignatureTransaction.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.round != null && $Object.hasOwnProperty.call(message, "round"))
                                if (!$util.isInteger(message.round) && !(message.round && $util.isInteger(message.round.low) && $util.isInteger(message.round.high)))
                                    return "round: integer|Long expected";
                            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                                    return "signature: buffer expected";
                            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                                if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                                    return "hash: buffer expected";
                            return null;
                        };

                        /**
                         * Creates a StateSignatureTransaction message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.event.StateSignatureTransaction} StateSignatureTransaction
                         */
                        StateSignatureTransaction.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.event.StateSignatureTransaction)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.event.StateSignatureTransaction: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.event.StateSignatureTransaction();
                            if (object.round != null)
                                if (typeof object.round === "object" ? object.round.low || object.round.high : $Number(object.round) !== 0)
                                    if ($util.Long)
                                        message.round = $util.Long.fromValue(object.round, false);
                                    else if (typeof object.round === "string")
                                        message.round = $parseInt(object.round, 10);
                                    else if (typeof object.round === "number")
                                        message.round = object.round;
                                    else if (typeof object.round === "object")
                                        message.round = new $util.LongBits(object.round.low >>> 0, object.round.high >>> 0).toNumber();
                            if (object.signature != null)
                                if (object.signature.length)
                                    if (typeof object.signature === "string")
                                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                                    else if (object.signature.length >= 0)
                                        message.signature = object.signature;
                            if (object.hash != null)
                                if (object.hash.length)
                                    if (typeof object.hash === "string")
                                        $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                                    else if (object.hash.length >= 0)
                                        message.hash = object.hash;
                            return message;
                        };

                        /**
                         * Creates a plain object from a StateSignatureTransaction message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @static
                         * @param {com.hedera.hapi.platform.event.StateSignatureTransaction} message StateSignatureTransaction
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        StateSignatureTransaction.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, false);
                                    object.round = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.round = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                if (options.bytes === $String)
                                    object.signature = "";
                                else {
                                    object.signature = [];
                                    if (options.bytes !== $Array)
                                        object.signature = $util.newBuffer(object.signature);
                                }
                                if (options.bytes === $String)
                                    object.hash = "";
                                else {
                                    object.hash = [];
                                    if (options.bytes !== $Array)
                                        object.hash = $util.newBuffer(object.hash);
                                }
                            }
                            if (message.round != null && $Object.hasOwnProperty.call(message, "round"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.round = typeof message.round === "number" ? $BigInt(message.round) : $util.Long.fromBits(message.round.low >>> 0, message.round.high >>> 0, false).toBigInt();
                                else if (typeof message.round === "number")
                                    object.round = options.longs === $String ? $String(message.round) : message.round;
                                else
                                    object.round = options.longs === $String ? $util.Long.prototype.toString.call(message.round) : options.longs === $Number ? new $util.LongBits(message.round.low >>> 0, message.round.high >>> 0).toNumber() : message.round;
                            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                                object.signature = options.bytes === $String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.signature) : message.signature;
                            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                                object.hash = options.bytes === $String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hash) : message.hash;
                            return object;
                        };

                        /**
                         * Converts this StateSignatureTransaction to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        StateSignatureTransaction.prototype.toJSON = function() {
                            return StateSignatureTransaction.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for StateSignatureTransaction
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.event.StateSignatureTransaction
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        StateSignatureTransaction.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.event.StateSignatureTransaction";
                        };

                        return StateSignatureTransaction;
                    })();

                    return event;
                })();

                platform.state = (function() {

                    /**
                     * Namespace state.
                     * @memberof com.hedera.hapi.platform
                     * @namespace
                     */
                    const state = {};

                    state.PlatformState = (function() {

                        /**
                         * Properties of a PlatformState.
                         * @typedef {Object} com.hedera.hapi.platform.state.PlatformState.$Properties
                         * @property {proto.SemanticVersion.$Properties|null} [creationSoftwareVersion] A version describing the current version of application software.
                         * <p>
                         * This SHALL be the software version that created this state.
                         * @property {number|null} [roundsNonAncient] A number of non-ancient rounds.
                         * <p>
                         * This SHALL be the count of rounds considered non-ancient.
                         * @property {com.hedera.hapi.platform.state.ConsensusSnapshot.$Properties|null} [consensusSnapshot] A snapshot of the consensus state at the end of the round.
                         * <p>
                         * This SHALL be used for restart/reconnect.
                         * @property {proto.Timestamp.$Properties|null} [freezeTime] A timestamp for the next scheduled time when a freeze will start.
                         * <p>
                         * If a freeze is not scheduled, this SHALL NOT be set.<br/>
                         * If a freeze is currently scheduled, this MUST be set, and MUST
                         * match the timestamp requested for that freeze.
                         * @property {proto.Timestamp.$Properties|null} [lastFrozenTime] A timestamp for the last time a freeze was performed.<br/>
                         * If not set, there has never been a freeze.
                         * @property {number|Long|null} [latestFreezeRound] The number of the latest round that was a freeze round. If this round
                         * is a freeze round, then the value is the same as the current round. It
                         * is set after executing all transactions in this round. <br/>
                         * If there has never been a freeze round, this SHALL be zero.<br/>
                         * @property {Uint8Array|null} [legacyRunningEventHash] A running event hash.<br/>
                         * This is computed by the consensus event stream.
                         * <p>
                         * This will be _removed_ and the field number reserved once the consensus
                         * event stream is retired.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a PlatformState.
                         * @memberof com.hedera.hapi.platform.state
                         * @interface IPlatformState
                         * @augments com.hedera.hapi.platform.state.PlatformState.$Properties
                         * @deprecated Use com.hedera.hapi.platform.state.PlatformState.$Properties instead.
                         */

                        /**
                         * Shape of a PlatformState.
                         * @typedef {com.hedera.hapi.platform.state.PlatformState.$Properties} com.hedera.hapi.platform.state.PlatformState.$Shape
                         */

                        /**
                         * Constructs a new PlatformState.
                         * @memberof com.hedera.hapi.platform.state
                         * @classdesc The current state of platform consensus.<br/>
                         * This message stores the current consensus data for the platform
                         * in network state.
                         * 
                         * The platform state SHALL represent the latest round's consensus.<br/>
                         * This data SHALL be used to ensure consistency and provide critical data for
                         * restart and reconnect.
                         * @constructor
                         * @param {com.hedera.hapi.platform.state.PlatformState.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const PlatformState = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A version describing the current version of application software.
                         * <p>
                         * This SHALL be the software version that created this state.
                         * @member {proto.SemanticVersion.$Properties|null|undefined} creationSoftwareVersion
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @instance
                         */
                        PlatformState.prototype.creationSoftwareVersion = null;

                        /**
                         * A number of non-ancient rounds.
                         * <p>
                         * This SHALL be the count of rounds considered non-ancient.
                         * @member {number} roundsNonAncient
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @instance
                         */
                        PlatformState.prototype.roundsNonAncient = 0;

                        /**
                         * A snapshot of the consensus state at the end of the round.
                         * <p>
                         * This SHALL be used for restart/reconnect.
                         * @member {com.hedera.hapi.platform.state.ConsensusSnapshot.$Properties|null|undefined} consensusSnapshot
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @instance
                         */
                        PlatformState.prototype.consensusSnapshot = null;

                        /**
                         * A timestamp for the next scheduled time when a freeze will start.
                         * <p>
                         * If a freeze is not scheduled, this SHALL NOT be set.<br/>
                         * If a freeze is currently scheduled, this MUST be set, and MUST
                         * match the timestamp requested for that freeze.
                         * @member {proto.Timestamp.$Properties|null|undefined} freezeTime
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @instance
                         */
                        PlatformState.prototype.freezeTime = null;

                        /**
                         * A timestamp for the last time a freeze was performed.<br/>
                         * If not set, there has never been a freeze.
                         * @member {proto.Timestamp.$Properties|null|undefined} lastFrozenTime
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @instance
                         */
                        PlatformState.prototype.lastFrozenTime = null;

                        /**
                         * The number of the latest round that was a freeze round. If this round
                         * is a freeze round, then the value is the same as the current round. It
                         * is set after executing all transactions in this round. <br/>
                         * If there has never been a freeze round, this SHALL be zero.<br/>
                         * @member {number|Long} latestFreezeRound
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @instance
                         */
                        PlatformState.prototype.latestFreezeRound = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * A running event hash.<br/>
                         * This is computed by the consensus event stream.
                         * <p>
                         * This will be _removed_ and the field number reserved once the consensus
                         * event stream is retired.
                         * @member {Uint8Array} legacyRunningEventHash
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @instance
                         */
                        PlatformState.prototype.legacyRunningEventHash = $util.newBuffer([]);

                        /**
                         * Creates a new PlatformState instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @static
                         * @param {com.hedera.hapi.platform.state.PlatformState.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.state.PlatformState} PlatformState instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.state.PlatformState.$Shape): com.hedera.hapi.platform.state.PlatformState & com.hedera.hapi.platform.state.PlatformState.$Shape;
                         *   (properties?: com.hedera.hapi.platform.state.PlatformState.$Properties): com.hedera.hapi.platform.state.PlatformState;
                         * }}
                         */
                        PlatformState.create = function(properties) {
                            return new PlatformState(properties);
                        };

                        /**
                         * Encodes the specified PlatformState message. Does not implicitly {@link com.hedera.hapi.platform.state.PlatformState.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @static
                         * @param {com.hedera.hapi.platform.state.PlatformState.$Properties} message PlatformState message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        PlatformState.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.creationSoftwareVersion != null && $Object.hasOwnProperty.call(message, "creationSoftwareVersion"))
                                $root.proto.SemanticVersion.encode(message.creationSoftwareVersion, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.roundsNonAncient != null && $Object.hasOwnProperty.call(message, "roundsNonAncient") && message.roundsNonAncient !== 0)
                                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.roundsNonAncient);
                            if (message.consensusSnapshot != null && $Object.hasOwnProperty.call(message, "consensusSnapshot"))
                                $root.com.hedera.hapi.platform.state.ConsensusSnapshot.encode(message.consensusSnapshot, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.freezeTime != null && $Object.hasOwnProperty.call(message, "freezeTime"))
                                $root.proto.Timestamp.encode(message.freezeTime, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                            if (message.lastFrozenTime != null && $Object.hasOwnProperty.call(message, "lastFrozenTime"))
                                $root.proto.Timestamp.encode(message.lastFrozenTime, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                            if (message.latestFreezeRound != null && $Object.hasOwnProperty.call(message, "latestFreezeRound") && (typeof message.latestFreezeRound === "object" ? message.latestFreezeRound.low || message.latestFreezeRound.high : message.latestFreezeRound !== 0))
                                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.latestFreezeRound);
                            if (message.legacyRunningEventHash != null && $Object.hasOwnProperty.call(message, "legacyRunningEventHash") && message.legacyRunningEventHash.length)
                                writer.uint32(/* id 10000, wireType 2 =*/80002).bytes(message.legacyRunningEventHash);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified PlatformState message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.state.PlatformState.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @static
                         * @param {com.hedera.hapi.platform.state.PlatformState.$Properties} message PlatformState message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        PlatformState.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a PlatformState message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.state.PlatformState & com.hedera.hapi.platform.state.PlatformState.$Shape} PlatformState
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        PlatformState.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.state.PlatformState();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.creationSoftwareVersion = $root.proto.SemanticVersion.decode(reader, reader.uint32(), $undefined, _depth + 1, message.creationSoftwareVersion);
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 0)
                                            break;
                                        if (value = reader.uint32())
                                            message.roundsNonAncient = value;
                                        else
                                            delete message.roundsNonAncient;
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        message.consensusSnapshot = $root.com.hedera.hapi.platform.state.ConsensusSnapshot.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusSnapshot);
                                        continue;
                                    }
                                case 4: {
                                        if (wireType !== 2)
                                            break;
                                        message.freezeTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.freezeTime);
                                        continue;
                                    }
                                case 5: {
                                        if (wireType !== 2)
                                            break;
                                        message.lastFrozenTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.lastFrozenTime);
                                        continue;
                                    }
                                case 6: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.latestFreezeRound = value;
                                        else
                                            delete message.latestFreezeRound;
                                        continue;
                                    }
                                case 10000: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.legacyRunningEventHash = value;
                                        else
                                            delete message.legacyRunningEventHash;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a PlatformState message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.state.PlatformState & com.hedera.hapi.platform.state.PlatformState.$Shape} PlatformState
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        PlatformState.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a PlatformState message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        PlatformState.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.creationSoftwareVersion != null && $Object.hasOwnProperty.call(message, "creationSoftwareVersion")) {
                                let error = $root.proto.SemanticVersion.verify(message.creationSoftwareVersion, _depth + 1);
                                if (error)
                                    return "creationSoftwareVersion." + error;
                            }
                            if (message.roundsNonAncient != null && $Object.hasOwnProperty.call(message, "roundsNonAncient"))
                                if (!$util.isInteger(message.roundsNonAncient))
                                    return "roundsNonAncient: integer expected";
                            if (message.consensusSnapshot != null && $Object.hasOwnProperty.call(message, "consensusSnapshot")) {
                                let error = $root.com.hedera.hapi.platform.state.ConsensusSnapshot.verify(message.consensusSnapshot, _depth + 1);
                                if (error)
                                    return "consensusSnapshot." + error;
                            }
                            if (message.freezeTime != null && $Object.hasOwnProperty.call(message, "freezeTime")) {
                                let error = $root.proto.Timestamp.verify(message.freezeTime, _depth + 1);
                                if (error)
                                    return "freezeTime." + error;
                            }
                            if (message.lastFrozenTime != null && $Object.hasOwnProperty.call(message, "lastFrozenTime")) {
                                let error = $root.proto.Timestamp.verify(message.lastFrozenTime, _depth + 1);
                                if (error)
                                    return "lastFrozenTime." + error;
                            }
                            if (message.latestFreezeRound != null && $Object.hasOwnProperty.call(message, "latestFreezeRound"))
                                if (!$util.isInteger(message.latestFreezeRound) && !(message.latestFreezeRound && $util.isInteger(message.latestFreezeRound.low) && $util.isInteger(message.latestFreezeRound.high)))
                                    return "latestFreezeRound: integer|Long expected";
                            if (message.legacyRunningEventHash != null && $Object.hasOwnProperty.call(message, "legacyRunningEventHash"))
                                if (!(message.legacyRunningEventHash && typeof message.legacyRunningEventHash.length === "number" || $util.isString(message.legacyRunningEventHash)))
                                    return "legacyRunningEventHash: buffer expected";
                            return null;
                        };

                        /**
                         * Creates a PlatformState message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.state.PlatformState} PlatformState
                         */
                        PlatformState.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.state.PlatformState)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.state.PlatformState: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.state.PlatformState();
                            if (object.creationSoftwareVersion != null) {
                                if (!$util.isObject(object.creationSoftwareVersion))
                                    throw $TypeError(".com.hedera.hapi.platform.state.PlatformState.creationSoftwareVersion: object expected");
                                message.creationSoftwareVersion = $root.proto.SemanticVersion.fromObject(object.creationSoftwareVersion, _depth + 1);
                            }
                            if (object.roundsNonAncient != null)
                                if ($Number(object.roundsNonAncient) !== 0)
                                    message.roundsNonAncient = object.roundsNonAncient >>> 0;
                            if (object.consensusSnapshot != null) {
                                if (!$util.isObject(object.consensusSnapshot))
                                    throw $TypeError(".com.hedera.hapi.platform.state.PlatformState.consensusSnapshot: object expected");
                                message.consensusSnapshot = $root.com.hedera.hapi.platform.state.ConsensusSnapshot.fromObject(object.consensusSnapshot, _depth + 1);
                            }
                            if (object.freezeTime != null) {
                                if (!$util.isObject(object.freezeTime))
                                    throw $TypeError(".com.hedera.hapi.platform.state.PlatformState.freezeTime: object expected");
                                message.freezeTime = $root.proto.Timestamp.fromObject(object.freezeTime, _depth + 1);
                            }
                            if (object.lastFrozenTime != null) {
                                if (!$util.isObject(object.lastFrozenTime))
                                    throw $TypeError(".com.hedera.hapi.platform.state.PlatformState.lastFrozenTime: object expected");
                                message.lastFrozenTime = $root.proto.Timestamp.fromObject(object.lastFrozenTime, _depth + 1);
                            }
                            if (object.latestFreezeRound != null)
                                if (typeof object.latestFreezeRound === "object" ? object.latestFreezeRound.low || object.latestFreezeRound.high : $Number(object.latestFreezeRound) !== 0)
                                    if ($util.Long)
                                        message.latestFreezeRound = $util.Long.fromValue(object.latestFreezeRound, true);
                                    else if (typeof object.latestFreezeRound === "string")
                                        message.latestFreezeRound = $parseInt(object.latestFreezeRound, 10);
                                    else if (typeof object.latestFreezeRound === "number")
                                        message.latestFreezeRound = object.latestFreezeRound;
                                    else if (typeof object.latestFreezeRound === "object")
                                        message.latestFreezeRound = new $util.LongBits(object.latestFreezeRound.low >>> 0, object.latestFreezeRound.high >>> 0).toNumber(true);
                            if (object.legacyRunningEventHash != null)
                                if (object.legacyRunningEventHash.length)
                                    if (typeof object.legacyRunningEventHash === "string")
                                        $util.base64.decode(object.legacyRunningEventHash, message.legacyRunningEventHash = $util.newBuffer($util.base64.length(object.legacyRunningEventHash)), 0);
                                    else if (object.legacyRunningEventHash.length >= 0)
                                        message.legacyRunningEventHash = object.legacyRunningEventHash;
                            return message;
                        };

                        /**
                         * Creates a plain object from a PlatformState message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @static
                         * @param {com.hedera.hapi.platform.state.PlatformState} message PlatformState
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        PlatformState.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                object.creationSoftwareVersion = null;
                                object.roundsNonAncient = 0;
                                object.consensusSnapshot = null;
                                object.freezeTime = null;
                                object.lastFrozenTime = null;
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.latestFreezeRound = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.latestFreezeRound = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                if (options.bytes === $String)
                                    object.legacyRunningEventHash = "";
                                else {
                                    object.legacyRunningEventHash = [];
                                    if (options.bytes !== $Array)
                                        object.legacyRunningEventHash = $util.newBuffer(object.legacyRunningEventHash);
                                }
                            }
                            if (message.creationSoftwareVersion != null && $Object.hasOwnProperty.call(message, "creationSoftwareVersion"))
                                object.creationSoftwareVersion = $root.proto.SemanticVersion.toObject(message.creationSoftwareVersion, options, _depth + 1);
                            if (message.roundsNonAncient != null && $Object.hasOwnProperty.call(message, "roundsNonAncient"))
                                object.roundsNonAncient = message.roundsNonAncient;
                            if (message.consensusSnapshot != null && $Object.hasOwnProperty.call(message, "consensusSnapshot"))
                                object.consensusSnapshot = $root.com.hedera.hapi.platform.state.ConsensusSnapshot.toObject(message.consensusSnapshot, options, _depth + 1);
                            if (message.freezeTime != null && $Object.hasOwnProperty.call(message, "freezeTime"))
                                object.freezeTime = $root.proto.Timestamp.toObject(message.freezeTime, options, _depth + 1);
                            if (message.lastFrozenTime != null && $Object.hasOwnProperty.call(message, "lastFrozenTime"))
                                object.lastFrozenTime = $root.proto.Timestamp.toObject(message.lastFrozenTime, options, _depth + 1);
                            if (message.latestFreezeRound != null && $Object.hasOwnProperty.call(message, "latestFreezeRound"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.latestFreezeRound = typeof message.latestFreezeRound === "number" ? $BigInt(message.latestFreezeRound) : $util.Long.fromBits(message.latestFreezeRound.low >>> 0, message.latestFreezeRound.high >>> 0, true).toBigInt();
                                else if (typeof message.latestFreezeRound === "number")
                                    object.latestFreezeRound = options.longs === $String ? $String(message.latestFreezeRound) : message.latestFreezeRound;
                                else
                                    object.latestFreezeRound = options.longs === $String ? $util.Long.prototype.toString.call(message.latestFreezeRound) : options.longs === $Number ? new $util.LongBits(message.latestFreezeRound.low >>> 0, message.latestFreezeRound.high >>> 0).toNumber(true) : message.latestFreezeRound;
                            if (message.legacyRunningEventHash != null && $Object.hasOwnProperty.call(message, "legacyRunningEventHash"))
                                object.legacyRunningEventHash = options.bytes === $String ? $util.base64.encode(message.legacyRunningEventHash, 0, message.legacyRunningEventHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.legacyRunningEventHash) : message.legacyRunningEventHash;
                            return object;
                        };

                        /**
                         * Converts this PlatformState to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        PlatformState.prototype.toJSON = function() {
                            return PlatformState.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for PlatformState
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.state.PlatformState
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        PlatformState.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.state.PlatformState";
                        };

                        return PlatformState;
                    })();

                    state.ConsensusSnapshot = (function() {

                        /**
                         * Properties of a ConsensusSnapshot.
                         * @typedef {Object} com.hedera.hapi.platform.state.ConsensusSnapshot.$Properties
                         * @property {number|Long|null} [round] A consensus round.<br/>
                         * The round number of this snapshot.
                         * @property {Array.<com.hedera.hapi.platform.state.MinimumJudgeInfo.$Properties>|null} [minimumJudgeInfoList] A list of minimum judge information entries.<br/>
                         * These are "minimum ancient" entries for non-ancient rounds.
                         * @property {number|Long|null} [nextConsensusNumber] A single consensus number.<br/>
                         * The consensus order of the next event to reach consensus.
                         * @property {proto.Timestamp.$Properties|null} [consensusTimestamp] A "consensus" timestamp.<br/>
                         * The consensus timestamp of this snapshot.
                         * <p>
                         * Depending on the context this timestamp may have different meanings:
                         * <ul>
                         * <li>if there are transactions, the timestamp is equal to the timestamp of the last transaction</li>
                         * <li>if there are no transactions, the timestamp is equal to the timestamp of the last event</li>
                         * <li>if there are no events, the timestamp is equal to the timestamp of the previous round plus a small constant</li>
                         * </ul>
                         * <p>
                         * This SHALL be a consensus value and MAY NOT correspond to an actual
                         * "wall clock" timestamp.<br/>
                         * Consensus Timestamps SHALL always increase.
                         * @property {Array.<com.hedera.hapi.platform.state.JudgeId.$Properties>|null} [judgeIds] ConsensusSnapshot judgeIds
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a ConsensusSnapshot.
                         * @memberof com.hedera.hapi.platform.state
                         * @interface IConsensusSnapshot
                         * @augments com.hedera.hapi.platform.state.ConsensusSnapshot.$Properties
                         * @deprecated Use com.hedera.hapi.platform.state.ConsensusSnapshot.$Properties instead.
                         */

                        /**
                         * Shape of a ConsensusSnapshot.
                         * @typedef {com.hedera.hapi.platform.state.ConsensusSnapshot.$Properties} com.hedera.hapi.platform.state.ConsensusSnapshot.$Shape
                         */

                        /**
                         * Constructs a new ConsensusSnapshot.
                         * @memberof com.hedera.hapi.platform.state
                         * @classdesc A consensus snapshot.<br/>
                         * This is a snapshot of the consensus state for a particular round.
                         * 
                         * This message SHALL record consensus data necessary for restart
                         * and reconnect.
                         * @constructor
                         * @param {com.hedera.hapi.platform.state.ConsensusSnapshot.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const ConsensusSnapshot = function (properties) {
                            this.minimumJudgeInfoList = [];
                            this.judgeIds = [];
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A consensus round.<br/>
                         * The round number of this snapshot.
                         * @member {number|Long} round
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @instance
                         */
                        ConsensusSnapshot.prototype.round = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * A list of minimum judge information entries.<br/>
                         * These are "minimum ancient" entries for non-ancient rounds.
                         * @member {Array.<com.hedera.hapi.platform.state.MinimumJudgeInfo.$Properties>} minimumJudgeInfoList
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @instance
                         */
                        ConsensusSnapshot.prototype.minimumJudgeInfoList = $util.emptyArray;

                        /**
                         * A single consensus number.<br/>
                         * The consensus order of the next event to reach consensus.
                         * @member {number|Long} nextConsensusNumber
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @instance
                         */
                        ConsensusSnapshot.prototype.nextConsensusNumber = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * A "consensus" timestamp.<br/>
                         * The consensus timestamp of this snapshot.
                         * <p>
                         * Depending on the context this timestamp may have different meanings:
                         * <ul>
                         * <li>if there are transactions, the timestamp is equal to the timestamp of the last transaction</li>
                         * <li>if there are no transactions, the timestamp is equal to the timestamp of the last event</li>
                         * <li>if there are no events, the timestamp is equal to the timestamp of the previous round plus a small constant</li>
                         * </ul>
                         * <p>
                         * This SHALL be a consensus value and MAY NOT correspond to an actual
                         * "wall clock" timestamp.<br/>
                         * Consensus Timestamps SHALL always increase.
                         * @member {proto.Timestamp.$Properties|null|undefined} consensusTimestamp
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @instance
                         */
                        ConsensusSnapshot.prototype.consensusTimestamp = null;

                        /**
                         * ConsensusSnapshot judgeIds.
                         * @member {Array.<com.hedera.hapi.platform.state.JudgeId.$Properties>} judgeIds
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @instance
                         */
                        ConsensusSnapshot.prototype.judgeIds = $util.emptyArray;

                        /**
                         * Creates a new ConsensusSnapshot instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @static
                         * @param {com.hedera.hapi.platform.state.ConsensusSnapshot.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.state.ConsensusSnapshot} ConsensusSnapshot instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.state.ConsensusSnapshot.$Shape): com.hedera.hapi.platform.state.ConsensusSnapshot & com.hedera.hapi.platform.state.ConsensusSnapshot.$Shape;
                         *   (properties?: com.hedera.hapi.platform.state.ConsensusSnapshot.$Properties): com.hedera.hapi.platform.state.ConsensusSnapshot;
                         * }}
                         */
                        ConsensusSnapshot.create = function(properties) {
                            return new ConsensusSnapshot(properties);
                        };

                        /**
                         * Encodes the specified ConsensusSnapshot message. Does not implicitly {@link com.hedera.hapi.platform.state.ConsensusSnapshot.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @static
                         * @param {com.hedera.hapi.platform.state.ConsensusSnapshot.$Properties} message ConsensusSnapshot message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        ConsensusSnapshot.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.round != null && $Object.hasOwnProperty.call(message, "round") && (typeof message.round === "object" ? message.round.low || message.round.high : message.round !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.round);
                            if (message.minimumJudgeInfoList != null && message.minimumJudgeInfoList.length)
                                for (let i = 0; i < message.minimumJudgeInfoList.length; ++i)
                                    $root.com.hedera.hapi.platform.state.MinimumJudgeInfo.encode(message.minimumJudgeInfoList[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.nextConsensusNumber != null && $Object.hasOwnProperty.call(message, "nextConsensusNumber") && (typeof message.nextConsensusNumber === "object" ? message.nextConsensusNumber.low || message.nextConsensusNumber.high : message.nextConsensusNumber !== 0))
                                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.nextConsensusNumber);
                            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp"))
                                $root.proto.Timestamp.encode(message.consensusTimestamp, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                            if (message.judgeIds != null && message.judgeIds.length)
                                for (let i = 0; i < message.judgeIds.length; ++i)
                                    $root.com.hedera.hapi.platform.state.JudgeId.encode(message.judgeIds[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified ConsensusSnapshot message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.state.ConsensusSnapshot.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @static
                         * @param {com.hedera.hapi.platform.state.ConsensusSnapshot.$Properties} message ConsensusSnapshot message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        ConsensusSnapshot.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a ConsensusSnapshot message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.state.ConsensusSnapshot & com.hedera.hapi.platform.state.ConsensusSnapshot.$Shape} ConsensusSnapshot
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        ConsensusSnapshot.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.state.ConsensusSnapshot();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.round = value;
                                        else
                                            delete message.round;
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.minimumJudgeInfoList && message.minimumJudgeInfoList.length))
                                            message.minimumJudgeInfoList = [];
                                        message.minimumJudgeInfoList.push($root.com.hedera.hapi.platform.state.MinimumJudgeInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                case 4: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.nextConsensusNumber = value;
                                        else
                                            delete message.nextConsensusNumber;
                                        continue;
                                    }
                                case 5: {
                                        if (wireType !== 2)
                                            break;
                                        message.consensusTimestamp = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusTimestamp);
                                        continue;
                                    }
                                case 6: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.judgeIds && message.judgeIds.length))
                                            message.judgeIds = [];
                                        message.judgeIds.push($root.com.hedera.hapi.platform.state.JudgeId.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a ConsensusSnapshot message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.state.ConsensusSnapshot & com.hedera.hapi.platform.state.ConsensusSnapshot.$Shape} ConsensusSnapshot
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        ConsensusSnapshot.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a ConsensusSnapshot message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        ConsensusSnapshot.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.round != null && $Object.hasOwnProperty.call(message, "round"))
                                if (!$util.isInteger(message.round) && !(message.round && $util.isInteger(message.round.low) && $util.isInteger(message.round.high)))
                                    return "round: integer|Long expected";
                            if (message.minimumJudgeInfoList != null && $Object.hasOwnProperty.call(message, "minimumJudgeInfoList")) {
                                if (!$Array.isArray(message.minimumJudgeInfoList))
                                    return "minimumJudgeInfoList: array expected";
                                for (let i = 0; i < message.minimumJudgeInfoList.length; ++i) {
                                    let error = $root.com.hedera.hapi.platform.state.MinimumJudgeInfo.verify(message.minimumJudgeInfoList[i], _depth + 1);
                                    if (error)
                                        return "minimumJudgeInfoList." + error;
                                }
                            }
                            if (message.nextConsensusNumber != null && $Object.hasOwnProperty.call(message, "nextConsensusNumber"))
                                if (!$util.isInteger(message.nextConsensusNumber) && !(message.nextConsensusNumber && $util.isInteger(message.nextConsensusNumber.low) && $util.isInteger(message.nextConsensusNumber.high)))
                                    return "nextConsensusNumber: integer|Long expected";
                            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp")) {
                                let error = $root.proto.Timestamp.verify(message.consensusTimestamp, _depth + 1);
                                if (error)
                                    return "consensusTimestamp." + error;
                            }
                            if (message.judgeIds != null && $Object.hasOwnProperty.call(message, "judgeIds")) {
                                if (!$Array.isArray(message.judgeIds))
                                    return "judgeIds: array expected";
                                for (let i = 0; i < message.judgeIds.length; ++i) {
                                    let error = $root.com.hedera.hapi.platform.state.JudgeId.verify(message.judgeIds[i], _depth + 1);
                                    if (error)
                                        return "judgeIds." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates a ConsensusSnapshot message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.state.ConsensusSnapshot} ConsensusSnapshot
                         */
                        ConsensusSnapshot.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.state.ConsensusSnapshot)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.state.ConsensusSnapshot: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.state.ConsensusSnapshot();
                            if (object.round != null)
                                if (typeof object.round === "object" ? object.round.low || object.round.high : $Number(object.round) !== 0)
                                    if ($util.Long)
                                        message.round = $util.Long.fromValue(object.round, true);
                                    else if (typeof object.round === "string")
                                        message.round = $parseInt(object.round, 10);
                                    else if (typeof object.round === "number")
                                        message.round = object.round;
                                    else if (typeof object.round === "object")
                                        message.round = new $util.LongBits(object.round.low >>> 0, object.round.high >>> 0).toNumber(true);
                            if (object.minimumJudgeInfoList) {
                                if (!$Array.isArray(object.minimumJudgeInfoList))
                                    throw $TypeError(".com.hedera.hapi.platform.state.ConsensusSnapshot.minimumJudgeInfoList: array expected");
                                message.minimumJudgeInfoList = $Array(object.minimumJudgeInfoList.length);
                                for (let i = 0; i < object.minimumJudgeInfoList.length; ++i) {
                                    if (!$util.isObject(object.minimumJudgeInfoList[i]))
                                        throw $TypeError(".com.hedera.hapi.platform.state.ConsensusSnapshot.minimumJudgeInfoList: object expected");
                                    message.minimumJudgeInfoList[i] = $root.com.hedera.hapi.platform.state.MinimumJudgeInfo.fromObject(object.minimumJudgeInfoList[i], _depth + 1);
                                }
                            }
                            if (object.nextConsensusNumber != null)
                                if (typeof object.nextConsensusNumber === "object" ? object.nextConsensusNumber.low || object.nextConsensusNumber.high : $Number(object.nextConsensusNumber) !== 0)
                                    if ($util.Long)
                                        message.nextConsensusNumber = $util.Long.fromValue(object.nextConsensusNumber, true);
                                    else if (typeof object.nextConsensusNumber === "string")
                                        message.nextConsensusNumber = $parseInt(object.nextConsensusNumber, 10);
                                    else if (typeof object.nextConsensusNumber === "number")
                                        message.nextConsensusNumber = object.nextConsensusNumber;
                                    else if (typeof object.nextConsensusNumber === "object")
                                        message.nextConsensusNumber = new $util.LongBits(object.nextConsensusNumber.low >>> 0, object.nextConsensusNumber.high >>> 0).toNumber(true);
                            if (object.consensusTimestamp != null) {
                                if (!$util.isObject(object.consensusTimestamp))
                                    throw $TypeError(".com.hedera.hapi.platform.state.ConsensusSnapshot.consensusTimestamp: object expected");
                                message.consensusTimestamp = $root.proto.Timestamp.fromObject(object.consensusTimestamp, _depth + 1);
                            }
                            if (object.judgeIds) {
                                if (!$Array.isArray(object.judgeIds))
                                    throw $TypeError(".com.hedera.hapi.platform.state.ConsensusSnapshot.judgeIds: array expected");
                                message.judgeIds = $Array(object.judgeIds.length);
                                for (let i = 0; i < object.judgeIds.length; ++i) {
                                    if (!$util.isObject(object.judgeIds[i]))
                                        throw $TypeError(".com.hedera.hapi.platform.state.ConsensusSnapshot.judgeIds: object expected");
                                    message.judgeIds[i] = $root.com.hedera.hapi.platform.state.JudgeId.fromObject(object.judgeIds[i], _depth + 1);
                                }
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a ConsensusSnapshot message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @static
                         * @param {com.hedera.hapi.platform.state.ConsensusSnapshot} message ConsensusSnapshot
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        ConsensusSnapshot.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.arrays || options.defaults) {
                                object.minimumJudgeInfoList = [];
                                object.judgeIds = [];
                            }
                            if (options.defaults) {
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.round = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.round = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.nextConsensusNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.nextConsensusNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                object.consensusTimestamp = null;
                            }
                            if (message.round != null && $Object.hasOwnProperty.call(message, "round"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.round = typeof message.round === "number" ? $BigInt(message.round) : $util.Long.fromBits(message.round.low >>> 0, message.round.high >>> 0, true).toBigInt();
                                else if (typeof message.round === "number")
                                    object.round = options.longs === $String ? $String(message.round) : message.round;
                                else
                                    object.round = options.longs === $String ? $util.Long.prototype.toString.call(message.round) : options.longs === $Number ? new $util.LongBits(message.round.low >>> 0, message.round.high >>> 0).toNumber(true) : message.round;
                            if (message.minimumJudgeInfoList && message.minimumJudgeInfoList.length) {
                                object.minimumJudgeInfoList = $Array(message.minimumJudgeInfoList.length);
                                for (let j = 0; j < message.minimumJudgeInfoList.length; ++j)
                                    object.minimumJudgeInfoList[j] = $root.com.hedera.hapi.platform.state.MinimumJudgeInfo.toObject(message.minimumJudgeInfoList[j], options, _depth + 1);
                            }
                            if (message.nextConsensusNumber != null && $Object.hasOwnProperty.call(message, "nextConsensusNumber"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.nextConsensusNumber = typeof message.nextConsensusNumber === "number" ? $BigInt(message.nextConsensusNumber) : $util.Long.fromBits(message.nextConsensusNumber.low >>> 0, message.nextConsensusNumber.high >>> 0, true).toBigInt();
                                else if (typeof message.nextConsensusNumber === "number")
                                    object.nextConsensusNumber = options.longs === $String ? $String(message.nextConsensusNumber) : message.nextConsensusNumber;
                                else
                                    object.nextConsensusNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.nextConsensusNumber) : options.longs === $Number ? new $util.LongBits(message.nextConsensusNumber.low >>> 0, message.nextConsensusNumber.high >>> 0).toNumber(true) : message.nextConsensusNumber;
                            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp"))
                                object.consensusTimestamp = $root.proto.Timestamp.toObject(message.consensusTimestamp, options, _depth + 1);
                            if (message.judgeIds && message.judgeIds.length) {
                                object.judgeIds = $Array(message.judgeIds.length);
                                for (let j = 0; j < message.judgeIds.length; ++j)
                                    object.judgeIds[j] = $root.com.hedera.hapi.platform.state.JudgeId.toObject(message.judgeIds[j], options, _depth + 1);
                            }
                            return object;
                        };

                        /**
                         * Converts this ConsensusSnapshot to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        ConsensusSnapshot.prototype.toJSON = function() {
                            return ConsensusSnapshot.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for ConsensusSnapshot
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.state.ConsensusSnapshot
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        ConsensusSnapshot.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.state.ConsensusSnapshot";
                        };

                        return ConsensusSnapshot;
                    })();

                    state.JudgeId = (function() {

                        /**
                         * Properties of a JudgeId.
                         * @typedef {Object} com.hedera.hapi.platform.state.JudgeId.$Properties
                         * @property {number|Long|null} [creatorId] The creator node ID who created this judge.
                         * @property {Uint8Array|null} [judgeHash] SHA-384 hash value of this judge
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a JudgeId.
                         * @memberof com.hedera.hapi.platform.state
                         * @interface IJudgeId
                         * @augments com.hedera.hapi.platform.state.JudgeId.$Properties
                         * @deprecated Use com.hedera.hapi.platform.state.JudgeId.$Properties instead.
                         */

                        /**
                         * Shape of a JudgeId.
                         * @typedef {com.hedera.hapi.platform.state.JudgeId.$Properties} com.hedera.hapi.platform.state.JudgeId.$Shape
                         */

                        /**
                         * Constructs a new JudgeId.
                         * @memberof com.hedera.hapi.platform.state
                         * @classdesc A judge information that includes the creator node ID and the
                         * SHA-384 hash value of the judge.
                         * @constructor
                         * @param {com.hedera.hapi.platform.state.JudgeId.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const JudgeId = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The creator node ID who created this judge.
                         * @member {number|Long} creatorId
                         * @memberof com.hedera.hapi.platform.state.JudgeId
                         * @instance
                         */
                        JudgeId.prototype.creatorId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * SHA-384 hash value of this judge
                         * @member {Uint8Array} judgeHash
                         * @memberof com.hedera.hapi.platform.state.JudgeId
                         * @instance
                         */
                        JudgeId.prototype.judgeHash = $util.newBuffer([]);

                        /**
                         * Creates a new JudgeId instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.state.JudgeId
                         * @static
                         * @param {com.hedera.hapi.platform.state.JudgeId.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.state.JudgeId} JudgeId instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.state.JudgeId.$Shape): com.hedera.hapi.platform.state.JudgeId & com.hedera.hapi.platform.state.JudgeId.$Shape;
                         *   (properties?: com.hedera.hapi.platform.state.JudgeId.$Properties): com.hedera.hapi.platform.state.JudgeId;
                         * }}
                         */
                        JudgeId.create = function(properties) {
                            return new JudgeId(properties);
                        };

                        /**
                         * Encodes the specified JudgeId message. Does not implicitly {@link com.hedera.hapi.platform.state.JudgeId.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.state.JudgeId
                         * @static
                         * @param {com.hedera.hapi.platform.state.JudgeId.$Properties} message JudgeId message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        JudgeId.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.creatorId != null && $Object.hasOwnProperty.call(message, "creatorId") && (typeof message.creatorId === "object" ? message.creatorId.low || message.creatorId.high : message.creatorId !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.creatorId);
                            if (message.judgeHash != null && $Object.hasOwnProperty.call(message, "judgeHash") && message.judgeHash.length)
                                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.judgeHash);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified JudgeId message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.state.JudgeId.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.state.JudgeId
                         * @static
                         * @param {com.hedera.hapi.platform.state.JudgeId.$Properties} message JudgeId message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        JudgeId.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a JudgeId message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.state.JudgeId
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.state.JudgeId & com.hedera.hapi.platform.state.JudgeId.$Shape} JudgeId
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        JudgeId.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.state.JudgeId();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.creatorId = value;
                                        else
                                            delete message.creatorId;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.judgeHash = value;
                                        else
                                            delete message.judgeHash;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a JudgeId message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.state.JudgeId
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.state.JudgeId & com.hedera.hapi.platform.state.JudgeId.$Shape} JudgeId
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        JudgeId.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a JudgeId message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.state.JudgeId
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        JudgeId.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.creatorId != null && $Object.hasOwnProperty.call(message, "creatorId"))
                                if (!$util.isInteger(message.creatorId) && !(message.creatorId && $util.isInteger(message.creatorId.low) && $util.isInteger(message.creatorId.high)))
                                    return "creatorId: integer|Long expected";
                            if (message.judgeHash != null && $Object.hasOwnProperty.call(message, "judgeHash"))
                                if (!(message.judgeHash && typeof message.judgeHash.length === "number" || $util.isString(message.judgeHash)))
                                    return "judgeHash: buffer expected";
                            return null;
                        };

                        /**
                         * Creates a JudgeId message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.state.JudgeId
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.state.JudgeId} JudgeId
                         */
                        JudgeId.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.state.JudgeId)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.state.JudgeId: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.state.JudgeId();
                            if (object.creatorId != null)
                                if (typeof object.creatorId === "object" ? object.creatorId.low || object.creatorId.high : $Number(object.creatorId) !== 0)
                                    if ($util.Long)
                                        message.creatorId = $util.Long.fromValue(object.creatorId, true);
                                    else if (typeof object.creatorId === "string")
                                        message.creatorId = $parseInt(object.creatorId, 10);
                                    else if (typeof object.creatorId === "number")
                                        message.creatorId = object.creatorId;
                                    else if (typeof object.creatorId === "object")
                                        message.creatorId = new $util.LongBits(object.creatorId.low >>> 0, object.creatorId.high >>> 0).toNumber(true);
                            if (object.judgeHash != null)
                                if (object.judgeHash.length)
                                    if (typeof object.judgeHash === "string")
                                        $util.base64.decode(object.judgeHash, message.judgeHash = $util.newBuffer($util.base64.length(object.judgeHash)), 0);
                                    else if (object.judgeHash.length >= 0)
                                        message.judgeHash = object.judgeHash;
                            return message;
                        };

                        /**
                         * Creates a plain object from a JudgeId message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.state.JudgeId
                         * @static
                         * @param {com.hedera.hapi.platform.state.JudgeId} message JudgeId
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        JudgeId.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.creatorId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.creatorId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                if (options.bytes === $String)
                                    object.judgeHash = "";
                                else {
                                    object.judgeHash = [];
                                    if (options.bytes !== $Array)
                                        object.judgeHash = $util.newBuffer(object.judgeHash);
                                }
                            }
                            if (message.creatorId != null && $Object.hasOwnProperty.call(message, "creatorId"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.creatorId = typeof message.creatorId === "number" ? $BigInt(message.creatorId) : $util.Long.fromBits(message.creatorId.low >>> 0, message.creatorId.high >>> 0, true).toBigInt();
                                else if (typeof message.creatorId === "number")
                                    object.creatorId = options.longs === $String ? $String(message.creatorId) : message.creatorId;
                                else
                                    object.creatorId = options.longs === $String ? $util.Long.prototype.toString.call(message.creatorId) : options.longs === $Number ? new $util.LongBits(message.creatorId.low >>> 0, message.creatorId.high >>> 0).toNumber(true) : message.creatorId;
                            if (message.judgeHash != null && $Object.hasOwnProperty.call(message, "judgeHash"))
                                object.judgeHash = options.bytes === $String ? $util.base64.encode(message.judgeHash, 0, message.judgeHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.judgeHash) : message.judgeHash;
                            return object;
                        };

                        /**
                         * Converts this JudgeId to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.state.JudgeId
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        JudgeId.prototype.toJSON = function() {
                            return JudgeId.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for JudgeId
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.state.JudgeId
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        JudgeId.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.state.JudgeId";
                        };

                        return JudgeId;
                    })();

                    state.MinimumJudgeInfo = (function() {

                        /**
                         * Properties of a MinimumJudgeInfo.
                         * @typedef {Object} com.hedera.hapi.platform.state.MinimumJudgeInfo.$Properties
                         * @property {number|Long|null} [round] A consensus round.<br/>
                         * The round this judge information applies to.
                         * @property {number|Long|null} [minimumJudgeBirthRound] This is a minimum birth round for all judges for a given round.
                         * <p>
                         * This SHALL reflect the relevant minimum birth round
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a MinimumJudgeInfo.
                         * @memberof com.hedera.hapi.platform.state
                         * @interface IMinimumJudgeInfo
                         * @augments com.hedera.hapi.platform.state.MinimumJudgeInfo.$Properties
                         * @deprecated Use com.hedera.hapi.platform.state.MinimumJudgeInfo.$Properties instead.
                         */

                        /**
                         * Shape of a MinimumJudgeInfo.
                         * @typedef {com.hedera.hapi.platform.state.MinimumJudgeInfo.$Properties} com.hedera.hapi.platform.state.MinimumJudgeInfo.$Shape
                         */

                        /**
                         * Constructs a new MinimumJudgeInfo.
                         * @memberof com.hedera.hapi.platform.state
                         * @classdesc Records the minimum ancient indicator for all judges in a particular round.
                         * @constructor
                         * @param {com.hedera.hapi.platform.state.MinimumJudgeInfo.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const MinimumJudgeInfo = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A consensus round.<br/>
                         * The round this judge information applies to.
                         * @member {number|Long} round
                         * @memberof com.hedera.hapi.platform.state.MinimumJudgeInfo
                         * @instance
                         */
                        MinimumJudgeInfo.prototype.round = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * This is a minimum birth round for all judges for a given round.
                         * <p>
                         * This SHALL reflect the relevant minimum birth round
                         * @member {number|Long} minimumJudgeBirthRound
                         * @memberof com.hedera.hapi.platform.state.MinimumJudgeInfo
                         * @instance
                         */
                        MinimumJudgeInfo.prototype.minimumJudgeBirthRound = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * Creates a new MinimumJudgeInfo instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.state.MinimumJudgeInfo
                         * @static
                         * @param {com.hedera.hapi.platform.state.MinimumJudgeInfo.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.state.MinimumJudgeInfo} MinimumJudgeInfo instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.state.MinimumJudgeInfo.$Shape): com.hedera.hapi.platform.state.MinimumJudgeInfo & com.hedera.hapi.platform.state.MinimumJudgeInfo.$Shape;
                         *   (properties?: com.hedera.hapi.platform.state.MinimumJudgeInfo.$Properties): com.hedera.hapi.platform.state.MinimumJudgeInfo;
                         * }}
                         */
                        MinimumJudgeInfo.create = function(properties) {
                            return new MinimumJudgeInfo(properties);
                        };

                        /**
                         * Encodes the specified MinimumJudgeInfo message. Does not implicitly {@link com.hedera.hapi.platform.state.MinimumJudgeInfo.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.state.MinimumJudgeInfo
                         * @static
                         * @param {com.hedera.hapi.platform.state.MinimumJudgeInfo.$Properties} message MinimumJudgeInfo message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        MinimumJudgeInfo.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.round != null && $Object.hasOwnProperty.call(message, "round") && (typeof message.round === "object" ? message.round.low || message.round.high : message.round !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.round);
                            if (message.minimumJudgeBirthRound != null && $Object.hasOwnProperty.call(message, "minimumJudgeBirthRound") && (typeof message.minimumJudgeBirthRound === "object" ? message.minimumJudgeBirthRound.low || message.minimumJudgeBirthRound.high : message.minimumJudgeBirthRound !== 0))
                                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.minimumJudgeBirthRound);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified MinimumJudgeInfo message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.state.MinimumJudgeInfo.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.state.MinimumJudgeInfo
                         * @static
                         * @param {com.hedera.hapi.platform.state.MinimumJudgeInfo.$Properties} message MinimumJudgeInfo message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        MinimumJudgeInfo.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a MinimumJudgeInfo message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.state.MinimumJudgeInfo
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.state.MinimumJudgeInfo & com.hedera.hapi.platform.state.MinimumJudgeInfo.$Shape} MinimumJudgeInfo
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        MinimumJudgeInfo.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.state.MinimumJudgeInfo();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.round = value;
                                        else
                                            delete message.round;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.minimumJudgeBirthRound = value;
                                        else
                                            delete message.minimumJudgeBirthRound;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a MinimumJudgeInfo message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.state.MinimumJudgeInfo
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.state.MinimumJudgeInfo & com.hedera.hapi.platform.state.MinimumJudgeInfo.$Shape} MinimumJudgeInfo
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        MinimumJudgeInfo.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a MinimumJudgeInfo message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.state.MinimumJudgeInfo
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        MinimumJudgeInfo.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.round != null && $Object.hasOwnProperty.call(message, "round"))
                                if (!$util.isInteger(message.round) && !(message.round && $util.isInteger(message.round.low) && $util.isInteger(message.round.high)))
                                    return "round: integer|Long expected";
                            if (message.minimumJudgeBirthRound != null && $Object.hasOwnProperty.call(message, "minimumJudgeBirthRound"))
                                if (!$util.isInteger(message.minimumJudgeBirthRound) && !(message.minimumJudgeBirthRound && $util.isInteger(message.minimumJudgeBirthRound.low) && $util.isInteger(message.minimumJudgeBirthRound.high)))
                                    return "minimumJudgeBirthRound: integer|Long expected";
                            return null;
                        };

                        /**
                         * Creates a MinimumJudgeInfo message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.state.MinimumJudgeInfo
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.state.MinimumJudgeInfo} MinimumJudgeInfo
                         */
                        MinimumJudgeInfo.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.state.MinimumJudgeInfo)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.state.MinimumJudgeInfo: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.state.MinimumJudgeInfo();
                            if (object.round != null)
                                if (typeof object.round === "object" ? object.round.low || object.round.high : $Number(object.round) !== 0)
                                    if ($util.Long)
                                        message.round = $util.Long.fromValue(object.round, true);
                                    else if (typeof object.round === "string")
                                        message.round = $parseInt(object.round, 10);
                                    else if (typeof object.round === "number")
                                        message.round = object.round;
                                    else if (typeof object.round === "object")
                                        message.round = new $util.LongBits(object.round.low >>> 0, object.round.high >>> 0).toNumber(true);
                            if (object.minimumJudgeBirthRound != null)
                                if (typeof object.minimumJudgeBirthRound === "object" ? object.minimumJudgeBirthRound.low || object.minimumJudgeBirthRound.high : $Number(object.minimumJudgeBirthRound) !== 0)
                                    if ($util.Long)
                                        message.minimumJudgeBirthRound = $util.Long.fromValue(object.minimumJudgeBirthRound, true);
                                    else if (typeof object.minimumJudgeBirthRound === "string")
                                        message.minimumJudgeBirthRound = $parseInt(object.minimumJudgeBirthRound, 10);
                                    else if (typeof object.minimumJudgeBirthRound === "number")
                                        message.minimumJudgeBirthRound = object.minimumJudgeBirthRound;
                                    else if (typeof object.minimumJudgeBirthRound === "object")
                                        message.minimumJudgeBirthRound = new $util.LongBits(object.minimumJudgeBirthRound.low >>> 0, object.minimumJudgeBirthRound.high >>> 0).toNumber(true);
                            return message;
                        };

                        /**
                         * Creates a plain object from a MinimumJudgeInfo message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.state.MinimumJudgeInfo
                         * @static
                         * @param {com.hedera.hapi.platform.state.MinimumJudgeInfo} message MinimumJudgeInfo
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        MinimumJudgeInfo.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.round = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.round = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.minimumJudgeBirthRound = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.minimumJudgeBirthRound = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                            }
                            if (message.round != null && $Object.hasOwnProperty.call(message, "round"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.round = typeof message.round === "number" ? $BigInt(message.round) : $util.Long.fromBits(message.round.low >>> 0, message.round.high >>> 0, true).toBigInt();
                                else if (typeof message.round === "number")
                                    object.round = options.longs === $String ? $String(message.round) : message.round;
                                else
                                    object.round = options.longs === $String ? $util.Long.prototype.toString.call(message.round) : options.longs === $Number ? new $util.LongBits(message.round.low >>> 0, message.round.high >>> 0).toNumber(true) : message.round;
                            if (message.minimumJudgeBirthRound != null && $Object.hasOwnProperty.call(message, "minimumJudgeBirthRound"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.minimumJudgeBirthRound = typeof message.minimumJudgeBirthRound === "number" ? $BigInt(message.minimumJudgeBirthRound) : $util.Long.fromBits(message.minimumJudgeBirthRound.low >>> 0, message.minimumJudgeBirthRound.high >>> 0, true).toBigInt();
                                else if (typeof message.minimumJudgeBirthRound === "number")
                                    object.minimumJudgeBirthRound = options.longs === $String ? $String(message.minimumJudgeBirthRound) : message.minimumJudgeBirthRound;
                                else
                                    object.minimumJudgeBirthRound = options.longs === $String ? $util.Long.prototype.toString.call(message.minimumJudgeBirthRound) : options.longs === $Number ? new $util.LongBits(message.minimumJudgeBirthRound.low >>> 0, message.minimumJudgeBirthRound.high >>> 0).toNumber(true) : message.minimumJudgeBirthRound;
                            return object;
                        };

                        /**
                         * Converts this MinimumJudgeInfo to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.state.MinimumJudgeInfo
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        MinimumJudgeInfo.prototype.toJSON = function() {
                            return MinimumJudgeInfo.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for MinimumJudgeInfo
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.state.MinimumJudgeInfo
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        MinimumJudgeInfo.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.state.MinimumJudgeInfo";
                        };

                        return MinimumJudgeInfo;
                    })();

                    state.NodeId = (function() {

                        /**
                         * Properties of a NodeId.
                         * @typedef {Object} com.hedera.hapi.platform.state.NodeId.$Properties
                         * @property {number|Long|null} [id] A numeric identifier.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a NodeId.
                         * @memberof com.hedera.hapi.platform.state
                         * @interface INodeId
                         * @augments com.hedera.hapi.platform.state.NodeId.$Properties
                         * @deprecated Use com.hedera.hapi.platform.state.NodeId.$Properties instead.
                         */

                        /**
                         * Shape of a NodeId.
                         * @typedef {com.hedera.hapi.platform.state.NodeId.$Properties} com.hedera.hapi.platform.state.NodeId.$Shape
                         */

                        /**
                         * Constructs a new NodeId.
                         * @memberof com.hedera.hapi.platform.state
                         * @classdesc A consensus node identifier.<br/>
                         * This value uniquely identifies a single consensus node within the network.
                         * @constructor
                         * @param {com.hedera.hapi.platform.state.NodeId.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const NodeId = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A numeric identifier.
                         * @member {number|Long} id
                         * @memberof com.hedera.hapi.platform.state.NodeId
                         * @instance
                         */
                        NodeId.prototype.id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * Creates a new NodeId instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.state.NodeId
                         * @static
                         * @param {com.hedera.hapi.platform.state.NodeId.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.state.NodeId} NodeId instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.state.NodeId.$Shape): com.hedera.hapi.platform.state.NodeId & com.hedera.hapi.platform.state.NodeId.$Shape;
                         *   (properties?: com.hedera.hapi.platform.state.NodeId.$Properties): com.hedera.hapi.platform.state.NodeId;
                         * }}
                         */
                        NodeId.create = function(properties) {
                            return new NodeId(properties);
                        };

                        /**
                         * Encodes the specified NodeId message. Does not implicitly {@link com.hedera.hapi.platform.state.NodeId.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.state.NodeId
                         * @static
                         * @param {com.hedera.hapi.platform.state.NodeId.$Properties} message NodeId message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        NodeId.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && (typeof message.id === "object" ? message.id.low || message.id.high : message.id !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.id);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified NodeId message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.state.NodeId.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.state.NodeId
                         * @static
                         * @param {com.hedera.hapi.platform.state.NodeId.$Properties} message NodeId message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        NodeId.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a NodeId message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.state.NodeId
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.state.NodeId & com.hedera.hapi.platform.state.NodeId.$Shape} NodeId
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        NodeId.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.state.NodeId();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.id = value;
                                        else
                                            delete message.id;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a NodeId message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.state.NodeId
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.state.NodeId & com.hedera.hapi.platform.state.NodeId.$Shape} NodeId
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        NodeId.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a NodeId message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.state.NodeId
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        NodeId.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                                if (!$util.isInteger(message.id) && !(message.id && $util.isInteger(message.id.low) && $util.isInteger(message.id.high)))
                                    return "id: integer|Long expected";
                            return null;
                        };

                        /**
                         * Creates a NodeId message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.state.NodeId
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.state.NodeId} NodeId
                         */
                        NodeId.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.state.NodeId)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.state.NodeId: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.state.NodeId();
                            if (object.id != null)
                                if (typeof object.id === "object" ? object.id.low || object.id.high : $Number(object.id) !== 0)
                                    if ($util.Long)
                                        message.id = $util.Long.fromValue(object.id, true);
                                    else if (typeof object.id === "string")
                                        message.id = $parseInt(object.id, 10);
                                    else if (typeof object.id === "number")
                                        message.id = object.id;
                                    else if (typeof object.id === "object")
                                        message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber(true);
                            return message;
                        };

                        /**
                         * Creates a plain object from a NodeId message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.state.NodeId
                         * @static
                         * @param {com.hedera.hapi.platform.state.NodeId} message NodeId
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        NodeId.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults)
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.id = typeof message.id === "number" ? $BigInt(message.id) : $util.Long.fromBits(message.id.low >>> 0, message.id.high >>> 0, true).toBigInt();
                                else if (typeof message.id === "number")
                                    object.id = options.longs === $String ? $String(message.id) : message.id;
                                else
                                    object.id = options.longs === $String ? $util.Long.prototype.toString.call(message.id) : options.longs === $Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber(true) : message.id;
                            return object;
                        };

                        /**
                         * Converts this NodeId to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.state.NodeId
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        NodeId.prototype.toJSON = function() {
                            return NodeId.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for NodeId
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.state.NodeId
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        NodeId.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.state.NodeId";
                        };

                        return NodeId;
                    })();

                    state.SigSet = (function() {

                        /**
                         * Properties of a SigSet.
                         * @typedef {Object} com.hedera.hapi.platform.state.SigSet.$Properties
                         * @property {Array.<com.hedera.hapi.platform.state.NodeIdSignaturePair.$Properties>|null} [nodeIdSignaturePairs] A list of node ID and signature pairs.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a SigSet.
                         * @memberof com.hedera.hapi.platform.state
                         * @interface ISigSet
                         * @augments com.hedera.hapi.platform.state.SigSet.$Properties
                         * @deprecated Use com.hedera.hapi.platform.state.SigSet.$Properties instead.
                         */

                        /**
                         * Shape of a SigSet.
                         * @typedef {com.hedera.hapi.platform.state.SigSet.$Properties} com.hedera.hapi.platform.state.SigSet.$Shape
                         */

                        /**
                         * Constructs a new SigSet.
                         * @memberof com.hedera.hapi.platform.state
                         * @classdesc A set of signatures from consensus nodes.<br/>
                         * This message contains a list of node ID and signature pairs.
                         * @constructor
                         * @param {com.hedera.hapi.platform.state.SigSet.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const SigSet = function (properties) {
                            this.nodeIdSignaturePairs = [];
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A list of node ID and signature pairs.
                         * @member {Array.<com.hedera.hapi.platform.state.NodeIdSignaturePair.$Properties>} nodeIdSignaturePairs
                         * @memberof com.hedera.hapi.platform.state.SigSet
                         * @instance
                         */
                        SigSet.prototype.nodeIdSignaturePairs = $util.emptyArray;

                        /**
                         * Creates a new SigSet instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.state.SigSet
                         * @static
                         * @param {com.hedera.hapi.platform.state.SigSet.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.state.SigSet} SigSet instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.state.SigSet.$Shape): com.hedera.hapi.platform.state.SigSet & com.hedera.hapi.platform.state.SigSet.$Shape;
                         *   (properties?: com.hedera.hapi.platform.state.SigSet.$Properties): com.hedera.hapi.platform.state.SigSet;
                         * }}
                         */
                        SigSet.create = function(properties) {
                            return new SigSet(properties);
                        };

                        /**
                         * Encodes the specified SigSet message. Does not implicitly {@link com.hedera.hapi.platform.state.SigSet.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.state.SigSet
                         * @static
                         * @param {com.hedera.hapi.platform.state.SigSet.$Properties} message SigSet message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        SigSet.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.nodeIdSignaturePairs != null && message.nodeIdSignaturePairs.length)
                                for (let i = 0; i < message.nodeIdSignaturePairs.length; ++i)
                                    $root.com.hedera.hapi.platform.state.NodeIdSignaturePair.encode(message.nodeIdSignaturePairs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified SigSet message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.state.SigSet.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.state.SigSet
                         * @static
                         * @param {com.hedera.hapi.platform.state.SigSet.$Properties} message SigSet message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        SigSet.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a SigSet message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.state.SigSet
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.state.SigSet & com.hedera.hapi.platform.state.SigSet.$Shape} SigSet
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        SigSet.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.state.SigSet();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.nodeIdSignaturePairs && message.nodeIdSignaturePairs.length))
                                            message.nodeIdSignaturePairs = [];
                                        message.nodeIdSignaturePairs.push($root.com.hedera.hapi.platform.state.NodeIdSignaturePair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a SigSet message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.state.SigSet
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.state.SigSet & com.hedera.hapi.platform.state.SigSet.$Shape} SigSet
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        SigSet.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a SigSet message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.state.SigSet
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        SigSet.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.nodeIdSignaturePairs != null && $Object.hasOwnProperty.call(message, "nodeIdSignaturePairs")) {
                                if (!$Array.isArray(message.nodeIdSignaturePairs))
                                    return "nodeIdSignaturePairs: array expected";
                                for (let i = 0; i < message.nodeIdSignaturePairs.length; ++i) {
                                    let error = $root.com.hedera.hapi.platform.state.NodeIdSignaturePair.verify(message.nodeIdSignaturePairs[i], _depth + 1);
                                    if (error)
                                        return "nodeIdSignaturePairs." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates a SigSet message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.state.SigSet
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.state.SigSet} SigSet
                         */
                        SigSet.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.state.SigSet)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.state.SigSet: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.state.SigSet();
                            if (object.nodeIdSignaturePairs) {
                                if (!$Array.isArray(object.nodeIdSignaturePairs))
                                    throw $TypeError(".com.hedera.hapi.platform.state.SigSet.nodeIdSignaturePairs: array expected");
                                message.nodeIdSignaturePairs = $Array(object.nodeIdSignaturePairs.length);
                                for (let i = 0; i < object.nodeIdSignaturePairs.length; ++i) {
                                    if (!$util.isObject(object.nodeIdSignaturePairs[i]))
                                        throw $TypeError(".com.hedera.hapi.platform.state.SigSet.nodeIdSignaturePairs: object expected");
                                    message.nodeIdSignaturePairs[i] = $root.com.hedera.hapi.platform.state.NodeIdSignaturePair.fromObject(object.nodeIdSignaturePairs[i], _depth + 1);
                                }
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a SigSet message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.state.SigSet
                         * @static
                         * @param {com.hedera.hapi.platform.state.SigSet} message SigSet
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        SigSet.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.arrays || options.defaults)
                                object.nodeIdSignaturePairs = [];
                            if (message.nodeIdSignaturePairs && message.nodeIdSignaturePairs.length) {
                                object.nodeIdSignaturePairs = $Array(message.nodeIdSignaturePairs.length);
                                for (let j = 0; j < message.nodeIdSignaturePairs.length; ++j)
                                    object.nodeIdSignaturePairs[j] = $root.com.hedera.hapi.platform.state.NodeIdSignaturePair.toObject(message.nodeIdSignaturePairs[j], options, _depth + 1);
                            }
                            return object;
                        };

                        /**
                         * Converts this SigSet to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.state.SigSet
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        SigSet.prototype.toJSON = function() {
                            return SigSet.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for SigSet
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.state.SigSet
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        SigSet.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.state.SigSet";
                        };

                        return SigSet;
                    })();

                    state.NodeIdSignaturePair = (function() {

                        /**
                         * Properties of a NodeIdSignaturePair.
                         * @typedef {Object} com.hedera.hapi.platform.state.NodeIdSignaturePair.$Properties
                         * @property {number|Long|null} [nodeId] A node identifier.<br/>
                         * @property {number|null} [signatureType] A signature type.<br/>
                         * @property {Uint8Array|null} [signatureBytes] Signature bytes.<br/>
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a NodeIdSignaturePair.
                         * @memberof com.hedera.hapi.platform.state
                         * @interface INodeIdSignaturePair
                         * @augments com.hedera.hapi.platform.state.NodeIdSignaturePair.$Properties
                         * @deprecated Use com.hedera.hapi.platform.state.NodeIdSignaturePair.$Properties instead.
                         */

                        /**
                         * Shape of a NodeIdSignaturePair.
                         * @typedef {com.hedera.hapi.platform.state.NodeIdSignaturePair.$Properties} com.hedera.hapi.platform.state.NodeIdSignaturePair.$Shape
                         */

                        /**
                         * Constructs a new NodeIdSignaturePair.
                         * @memberof com.hedera.hapi.platform.state
                         * @classdesc A pair of node ID and signature bytes.<br/>
                         * This message contains a node ID, signature type, and signature bytes.
                         * @constructor
                         * @param {com.hedera.hapi.platform.state.NodeIdSignaturePair.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const NodeIdSignaturePair = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A node identifier.<br/>
                         * @member {number|Long} nodeId
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @instance
                         */
                        NodeIdSignaturePair.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                        /**
                         * A signature type.<br/>
                         * @member {number} signatureType
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @instance
                         */
                        NodeIdSignaturePair.prototype.signatureType = 0;

                        /**
                         * Signature bytes.<br/>
                         * @member {Uint8Array} signatureBytes
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @instance
                         */
                        NodeIdSignaturePair.prototype.signatureBytes = $util.newBuffer([]);

                        /**
                         * Creates a new NodeIdSignaturePair instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @static
                         * @param {com.hedera.hapi.platform.state.NodeIdSignaturePair.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.state.NodeIdSignaturePair} NodeIdSignaturePair instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.state.NodeIdSignaturePair.$Shape): com.hedera.hapi.platform.state.NodeIdSignaturePair & com.hedera.hapi.platform.state.NodeIdSignaturePair.$Shape;
                         *   (properties?: com.hedera.hapi.platform.state.NodeIdSignaturePair.$Properties): com.hedera.hapi.platform.state.NodeIdSignaturePair;
                         * }}
                         */
                        NodeIdSignaturePair.create = function(properties) {
                            return new NodeIdSignaturePair(properties);
                        };

                        /**
                         * Encodes the specified NodeIdSignaturePair message. Does not implicitly {@link com.hedera.hapi.platform.state.NodeIdSignaturePair.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @static
                         * @param {com.hedera.hapi.platform.state.NodeIdSignaturePair.$Properties} message NodeIdSignaturePair message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        NodeIdSignaturePair.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.nodeId);
                            if (message.signatureType != null && $Object.hasOwnProperty.call(message, "signatureType") && message.signatureType !== 0)
                                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.signatureType);
                            if (message.signatureBytes != null && $Object.hasOwnProperty.call(message, "signatureBytes") && message.signatureBytes.length)
                                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.signatureBytes);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified NodeIdSignaturePair message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.state.NodeIdSignaturePair.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @static
                         * @param {com.hedera.hapi.platform.state.NodeIdSignaturePair.$Properties} message NodeIdSignaturePair message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        NodeIdSignaturePair.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a NodeIdSignaturePair message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.state.NodeIdSignaturePair & com.hedera.hapi.platform.state.NodeIdSignaturePair.$Shape} NodeIdSignaturePair
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        NodeIdSignaturePair.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.state.NodeIdSignaturePair();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                            message.nodeId = value;
                                        else
                                            delete message.nodeId;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 0)
                                            break;
                                        if (value = reader.int32())
                                            message.signatureType = value;
                                        else
                                            delete message.signatureType;
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.signatureBytes = value;
                                        else
                                            delete message.signatureBytes;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a NodeIdSignaturePair message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.state.NodeIdSignaturePair & com.hedera.hapi.platform.state.NodeIdSignaturePair.$Shape} NodeIdSignaturePair
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        NodeIdSignaturePair.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a NodeIdSignaturePair message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        NodeIdSignaturePair.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                    return "nodeId: integer|Long expected";
                            if (message.signatureType != null && $Object.hasOwnProperty.call(message, "signatureType"))
                                if (!$util.isInteger(message.signatureType))
                                    return "signatureType: integer expected";
                            if (message.signatureBytes != null && $Object.hasOwnProperty.call(message, "signatureBytes"))
                                if (!(message.signatureBytes && typeof message.signatureBytes.length === "number" || $util.isString(message.signatureBytes)))
                                    return "signatureBytes: buffer expected";
                            return null;
                        };

                        /**
                         * Creates a NodeIdSignaturePair message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.state.NodeIdSignaturePair} NodeIdSignaturePair
                         */
                        NodeIdSignaturePair.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.state.NodeIdSignaturePair)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.state.NodeIdSignaturePair: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.state.NodeIdSignaturePair();
                            if (object.nodeId != null)
                                if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                    if ($util.Long)
                                        message.nodeId = $util.Long.fromValue(object.nodeId, false);
                                    else if (typeof object.nodeId === "string")
                                        message.nodeId = $parseInt(object.nodeId, 10);
                                    else if (typeof object.nodeId === "number")
                                        message.nodeId = object.nodeId;
                                    else if (typeof object.nodeId === "object")
                                        message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber();
                            if (object.signatureType != null)
                                if ($Number(object.signatureType) !== 0)
                                    message.signatureType = object.signatureType | 0;
                            if (object.signatureBytes != null)
                                if (object.signatureBytes.length)
                                    if (typeof object.signatureBytes === "string")
                                        $util.base64.decode(object.signatureBytes, message.signatureBytes = $util.newBuffer($util.base64.length(object.signatureBytes)), 0);
                                    else if (object.signatureBytes.length >= 0)
                                        message.signatureBytes = object.signatureBytes;
                            return message;
                        };

                        /**
                         * Creates a plain object from a NodeIdSignaturePair message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @static
                         * @param {com.hedera.hapi.platform.state.NodeIdSignaturePair} message NodeIdSignaturePair
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        NodeIdSignaturePair.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, false);
                                    object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                object.signatureType = 0;
                                if (options.bytes === $String)
                                    object.signatureBytes = "";
                                else {
                                    object.signatureBytes = [];
                                    if (options.bytes !== $Array)
                                        object.signatureBytes = $util.newBuffer(object.signatureBytes);
                                }
                            }
                            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, false).toBigInt();
                                else if (typeof message.nodeId === "number")
                                    object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                else
                                    object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber() : message.nodeId;
                            if (message.signatureType != null && $Object.hasOwnProperty.call(message, "signatureType"))
                                object.signatureType = message.signatureType;
                            if (message.signatureBytes != null && $Object.hasOwnProperty.call(message, "signatureBytes"))
                                object.signatureBytes = options.bytes === $String ? $util.base64.encode(message.signatureBytes, 0, message.signatureBytes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.signatureBytes) : message.signatureBytes;
                            return object;
                        };

                        /**
                         * Converts this NodeIdSignaturePair to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        NodeIdSignaturePair.prototype.toJSON = function() {
                            return NodeIdSignaturePair.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for NodeIdSignaturePair
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.state.NodeIdSignaturePair
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        NodeIdSignaturePair.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.state.NodeIdSignaturePair";
                        };

                        return NodeIdSignaturePair;
                    })();

                    state.StateItem = (function() {

                        /**
                         * Properties of a StateItem.
                         * @typedef {Object} com.hedera.hapi.platform.state.StateItem.$Properties
                         * @property {com.hedera.hapi.platform.state.StateKey.$Properties|null} [key] State Key.<br/>
                         * The key used to identify and locate this virtual map entry.
                         * @property {com.hedera.hapi.platform.state.StateValue.$Properties|null} [value] State Value.<br/>
                         * The actual data content stored for this virtual map entry.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a StateItem.
                         * @memberof com.hedera.hapi.platform.state
                         * @interface IStateItem
                         * @augments com.hedera.hapi.platform.state.StateItem.$Properties
                         * @deprecated Use com.hedera.hapi.platform.state.StateItem.$Properties instead.
                         */

                        /**
                         * Shape of a StateItem.
                         * @typedef {{
                         *   key?: com.hedera.hapi.platform.state.StateKey.$Shape|null;
                         *   value?: com.hedera.hapi.platform.state.StateValue.$Shape|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * }} com.hedera.hapi.platform.state.StateItem.$Shape
                         */

                        /**
                         * Constructs a new StateItem.
                         * @memberof com.hedera.hapi.platform.state
                         * @classdesc This message defines the State Item.
                         * @constructor
                         * @param {com.hedera.hapi.platform.state.StateItem.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const StateItem = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * State Key.<br/>
                         * The key used to identify and locate this virtual map entry.
                         * @member {com.hedera.hapi.platform.state.StateKey.$Properties|null|undefined} key
                         * @memberof com.hedera.hapi.platform.state.StateItem
                         * @instance
                         */
                        StateItem.prototype.key = null;

                        /**
                         * State Value.<br/>
                         * The actual data content stored for this virtual map entry.
                         * @member {com.hedera.hapi.platform.state.StateValue.$Properties|null|undefined} value
                         * @memberof com.hedera.hapi.platform.state.StateItem
                         * @instance
                         */
                        StateItem.prototype.value = null;

                        /**
                         * Creates a new StateItem instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.state.StateItem
                         * @static
                         * @param {com.hedera.hapi.platform.state.StateItem.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.state.StateItem} StateItem instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.state.StateItem.$Shape): com.hedera.hapi.platform.state.StateItem & com.hedera.hapi.platform.state.StateItem.$Shape;
                         *   (properties?: com.hedera.hapi.platform.state.StateItem.$Properties): com.hedera.hapi.platform.state.StateItem;
                         * }}
                         */
                        StateItem.create = function(properties) {
                            return new StateItem(properties);
                        };

                        /**
                         * Encodes the specified StateItem message. Does not implicitly {@link com.hedera.hapi.platform.state.StateItem.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.state.StateItem
                         * @static
                         * @param {com.hedera.hapi.platform.state.StateItem.$Properties} message StateItem message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        StateItem.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                $root.com.hedera.hapi.platform.state.StateKey.encode(message.key, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                                $root.com.hedera.hapi.platform.state.StateValue.encode(message.value, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified StateItem message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.state.StateItem.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.state.StateItem
                         * @static
                         * @param {com.hedera.hapi.platform.state.StateItem.$Properties} message StateItem message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        StateItem.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a StateItem message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.state.StateItem
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.state.StateItem & com.hedera.hapi.platform.state.StateItem.$Shape} StateItem
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        StateItem.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.state.StateItem();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        message.key = $root.com.hedera.hapi.platform.state.StateKey.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key);
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        message.value = $root.com.hedera.hapi.platform.state.StateValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.value);
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a StateItem message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.state.StateItem
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.state.StateItem & com.hedera.hapi.platform.state.StateItem.$Shape} StateItem
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        StateItem.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a StateItem message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.state.StateItem
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        StateItem.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.key != null && $Object.hasOwnProperty.call(message, "key")) {
                                let error = $root.com.hedera.hapi.platform.state.StateKey.verify(message.key, _depth + 1);
                                if (error)
                                    return "key." + error;
                            }
                            if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
                                let error = $root.com.hedera.hapi.platform.state.StateValue.verify(message.value, _depth + 1);
                                if (error)
                                    return "value." + error;
                            }
                            return null;
                        };

                        /**
                         * Creates a StateItem message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.state.StateItem
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.state.StateItem} StateItem
                         */
                        StateItem.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.state.StateItem)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.state.StateItem: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.state.StateItem();
                            if (object.key != null) {
                                if (!$util.isObject(object.key))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateItem.key: object expected");
                                message.key = $root.com.hedera.hapi.platform.state.StateKey.fromObject(object.key, _depth + 1);
                            }
                            if (object.value != null) {
                                if (!$util.isObject(object.value))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateItem.value: object expected");
                                message.value = $root.com.hedera.hapi.platform.state.StateValue.fromObject(object.value, _depth + 1);
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a StateItem message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.state.StateItem
                         * @static
                         * @param {com.hedera.hapi.platform.state.StateItem} message StateItem
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        StateItem.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                object.key = null;
                                object.value = null;
                            }
                            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                object.key = $root.com.hedera.hapi.platform.state.StateKey.toObject(message.key, options, _depth + 1);
                            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                                object.value = $root.com.hedera.hapi.platform.state.StateValue.toObject(message.value, options, _depth + 1);
                            return object;
                        };

                        /**
                         * Converts this StateItem to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.state.StateItem
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        StateItem.prototype.toJSON = function() {
                            return StateItem.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for StateItem
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.state.StateItem
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        StateItem.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.state.StateItem";
                        };

                        return StateItem;
                    })();

                    state.QueueState = (function() {

                        /**
                         * Properties of a QueueState.
                         * @typedef {Object} com.hedera.hapi.platform.state.QueueState.$Properties
                         * @property {number|Long|null} [head] A head of a queue.<br/>
                         * Indicate at what position a new element is removed from the queue.
                         * @property {number|Long|null} [tail] A tail of a queue.<br/>
                         * Indicate at what position a new element is put to the queue.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a QueueState.
                         * @memberof com.hedera.hapi.platform.state
                         * @interface IQueueState
                         * @augments com.hedera.hapi.platform.state.QueueState.$Properties
                         * @deprecated Use com.hedera.hapi.platform.state.QueueState.$Properties instead.
                         */

                        /**
                         * Shape of a QueueState.
                         * @typedef {com.hedera.hapi.platform.state.QueueState.$Properties} com.hedera.hapi.platform.state.QueueState.$Shape
                         */

                        /**
                         * Constructs a new QueueState.
                         * @memberof com.hedera.hapi.platform.state
                         * @classdesc This message defines the internal state of a queue state.
                         * Note that queue state is not a state of type Queue (versus Singleton or KV).
                         * This state contains two pointers (longs), which indicate at what position a new element is put to the queue (tail)
                         * or removed from the queue (head). These aren't indices in any array or whatsoever, they are just numbers,
                         * which are increased on every operation, used to maintain elements order in the queue.
                         * @constructor
                         * @param {com.hedera.hapi.platform.state.QueueState.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const QueueState = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A head of a queue.<br/>
                         * Indicate at what position a new element is removed from the queue.
                         * @member {number|Long} head
                         * @memberof com.hedera.hapi.platform.state.QueueState
                         * @instance
                         */
                        QueueState.prototype.head = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * A tail of a queue.<br/>
                         * Indicate at what position a new element is put to the queue.
                         * @member {number|Long} tail
                         * @memberof com.hedera.hapi.platform.state.QueueState
                         * @instance
                         */
                        QueueState.prototype.tail = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * Creates a new QueueState instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.state.QueueState
                         * @static
                         * @param {com.hedera.hapi.platform.state.QueueState.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.state.QueueState} QueueState instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.state.QueueState.$Shape): com.hedera.hapi.platform.state.QueueState & com.hedera.hapi.platform.state.QueueState.$Shape;
                         *   (properties?: com.hedera.hapi.platform.state.QueueState.$Properties): com.hedera.hapi.platform.state.QueueState;
                         * }}
                         */
                        QueueState.create = function(properties) {
                            return new QueueState(properties);
                        };

                        /**
                         * Encodes the specified QueueState message. Does not implicitly {@link com.hedera.hapi.platform.state.QueueState.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.state.QueueState
                         * @static
                         * @param {com.hedera.hapi.platform.state.QueueState.$Properties} message QueueState message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        QueueState.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.head != null && $Object.hasOwnProperty.call(message, "head") && (typeof message.head === "object" ? message.head.low || message.head.high : message.head !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.head);
                            if (message.tail != null && $Object.hasOwnProperty.call(message, "tail") && (typeof message.tail === "object" ? message.tail.low || message.tail.high : message.tail !== 0))
                                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.tail);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified QueueState message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.state.QueueState.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.state.QueueState
                         * @static
                         * @param {com.hedera.hapi.platform.state.QueueState.$Properties} message QueueState message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        QueueState.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a QueueState message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.state.QueueState
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.state.QueueState & com.hedera.hapi.platform.state.QueueState.$Shape} QueueState
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        QueueState.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.state.QueueState();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.head = value;
                                        else
                                            delete message.head;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.tail = value;
                                        else
                                            delete message.tail;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a QueueState message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.state.QueueState
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.state.QueueState & com.hedera.hapi.platform.state.QueueState.$Shape} QueueState
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        QueueState.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a QueueState message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.state.QueueState
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        QueueState.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.head != null && $Object.hasOwnProperty.call(message, "head"))
                                if (!$util.isInteger(message.head) && !(message.head && $util.isInteger(message.head.low) && $util.isInteger(message.head.high)))
                                    return "head: integer|Long expected";
                            if (message.tail != null && $Object.hasOwnProperty.call(message, "tail"))
                                if (!$util.isInteger(message.tail) && !(message.tail && $util.isInteger(message.tail.low) && $util.isInteger(message.tail.high)))
                                    return "tail: integer|Long expected";
                            return null;
                        };

                        /**
                         * Creates a QueueState message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.state.QueueState
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.state.QueueState} QueueState
                         */
                        QueueState.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.state.QueueState)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.state.QueueState: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.state.QueueState();
                            if (object.head != null)
                                if (typeof object.head === "object" ? object.head.low || object.head.high : $Number(object.head) !== 0)
                                    if ($util.Long)
                                        message.head = $util.Long.fromValue(object.head, true);
                                    else if (typeof object.head === "string")
                                        message.head = $parseInt(object.head, 10);
                                    else if (typeof object.head === "number")
                                        message.head = object.head;
                                    else if (typeof object.head === "object")
                                        message.head = new $util.LongBits(object.head.low >>> 0, object.head.high >>> 0).toNumber(true);
                            if (object.tail != null)
                                if (typeof object.tail === "object" ? object.tail.low || object.tail.high : $Number(object.tail) !== 0)
                                    if ($util.Long)
                                        message.tail = $util.Long.fromValue(object.tail, true);
                                    else if (typeof object.tail === "string")
                                        message.tail = $parseInt(object.tail, 10);
                                    else if (typeof object.tail === "number")
                                        message.tail = object.tail;
                                    else if (typeof object.tail === "object")
                                        message.tail = new $util.LongBits(object.tail.low >>> 0, object.tail.high >>> 0).toNumber(true);
                            return message;
                        };

                        /**
                         * Creates a plain object from a QueueState message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.state.QueueState
                         * @static
                         * @param {com.hedera.hapi.platform.state.QueueState} message QueueState
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        QueueState.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.head = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.head = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.tail = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.tail = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                            }
                            if (message.head != null && $Object.hasOwnProperty.call(message, "head"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.head = typeof message.head === "number" ? $BigInt(message.head) : $util.Long.fromBits(message.head.low >>> 0, message.head.high >>> 0, true).toBigInt();
                                else if (typeof message.head === "number")
                                    object.head = options.longs === $String ? $String(message.head) : message.head;
                                else
                                    object.head = options.longs === $String ? $util.Long.prototype.toString.call(message.head) : options.longs === $Number ? new $util.LongBits(message.head.low >>> 0, message.head.high >>> 0).toNumber(true) : message.head;
                            if (message.tail != null && $Object.hasOwnProperty.call(message, "tail"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.tail = typeof message.tail === "number" ? $BigInt(message.tail) : $util.Long.fromBits(message.tail.low >>> 0, message.tail.high >>> 0, true).toBigInt();
                                else if (typeof message.tail === "number")
                                    object.tail = options.longs === $String ? $String(message.tail) : message.tail;
                                else
                                    object.tail = options.longs === $String ? $util.Long.prototype.toString.call(message.tail) : options.longs === $Number ? new $util.LongBits(message.tail.low >>> 0, message.tail.high >>> 0).toNumber(true) : message.tail;
                            return object;
                        };

                        /**
                         * Converts this QueueState to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.state.QueueState
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        QueueState.prototype.toJSON = function() {
                            return QueueState.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for QueueState
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.state.QueueState
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        QueueState.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.state.QueueState";
                        };

                        return QueueState;
                    })();

                    state.StateKey = (function() {

                        /**
                         * Properties of a StateKey.
                         * @typedef {Object} com.hedera.hapi.platform.state.StateKey.$Properties
                         * @property {com.hedera.hapi.platform.state.SingletonType|null} [singleton] StateKey singleton
                         * @property {proto.AccountID.$Properties|null} [TokenService_I_ACCOUNTS] A state identifier for the Accounts state.
                         * @property {proto.ProtoBytes.$Properties|null} [TokenService_I_ALIASES] A state identifier for account aliases.
                         * @property {proto.SlotKey.$Properties|null} [ContractService_I_STORAGE] A state identifier for contract storage slots.
                         * @property {proto.ContractID.$Properties|null} [ContractService_I_BYTECODE] A state identifier for contract bytecode.
                         * @property {proto.FileID.$Properties|null} [FileService_I_FILES] A state identifier for Hedera File Service (HFS).
                         * @property {proto.TokenID.$Properties|null} [TokenService_I_TOKENS] A state identifier for Hedera Token Service (HTS).
                         * @property {proto.NftID.$Properties|null} [TokenService_I_NFTS] A state identifier for non-fungible/unique tokens.
                         * @property {proto.EntityIDPair.$Properties|null} [TokenService_I_TOKEN_RELS] A state identifier for token relationships.
                         * @property {proto.EntityNumber.$Properties|null} [TokenService_I_STAKING_INFOS] A state identifier for network staking information.
                         * @property {proto.ScheduleID.$Properties|null} [ScheduleService_I_SCHEDULES_BY_ID] A state identifier for scheduled transactions.
                         * @property {proto.ProtoLong.$Properties|null} [ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC] A state identifier for scheduled transaction expiration.
                         * @property {proto.ProtoBytes.$Properties|null} [ScheduleService_I_SCHEDULES_BY_EQUALITY] A state identifier for scheduled transaction deduplication.
                         * @property {proto.EntityNumber.$Properties|null} [AddressBookService_I_NODES] A state identifier for address book nodes.
                         * @property {proto.TopicID.$Properties|null} [ConsensusService_I_TOPICS] A state identifier for the Topics state.
                         * @property {proto.PendingAirdropId.$Properties|null} [TokenService_I_PENDING_AIRDROPS] A state identifier for pending airdrops.
                         * @property {proto.ProtoBytes.$Properties|null} [RosterService_I_ROSTERS] A state identifier for the rosters.
                         * @property {proto.TimestampSeconds.$Properties|null} [ScheduleService_I_SCHEDULED_COUNTS] A state identifier for counts of transactions scheduled and
                         * processed in a second.
                         * @property {proto.ProtoBytes.$Properties|null} [ScheduleService_I_SCHEDULE_ID_BY_EQUALITY] A state identifier for scheduled transaction deduplication.
                         * @property {com.hedera.hapi.node.state.tss.TssMessageMapKey.$Properties|null} [TssBaseService_I_TSS_MESSAGES] A state identifier for TSS messages.
                         * @property {com.hedera.hapi.node.state.tss.TssVoteMapKey.$Properties|null} [TssBaseService_I_TSS_VOTES] A state identifier for TSS votes.
                         * @property {proto.ScheduledOrder.$Properties|null} [ScheduleService_I_SCHEDULED_ORDERS] A state identifier for the ordering of scheduled transactions.
                         * @property {proto.TimestampSeconds.$Properties|null} [ScheduleService_I_SCHEDULED_USAGES] A state identifier for scheduled throttle usage snapshots.
                         * @property {proto.EntityNumber.$Properties|null} [TssBaseService_I_TSS_ENCRYPTION_KEYS] A state identifier for the TSS encryption keys.
                         * @property {com.hedera.hapi.node.state.hints.HintsPartyId.$Properties|null} [HintsService_I_HINTS_KEY_SETS] A state identifier for hinTS key sets.
                         * @property {com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Properties|null} [HintsService_I_PREPROCESSING_VOTES] A state identifier for hinTS preprocessing output votes.
                         * @property {com.hedera.hapi.platform.state.NodeId.$Properties|null} [HistoryService_I_PROOF_KEY_SETS] A state identifier for history proof key sets.
                         * @property {com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties|null} [HistoryService_I_HISTORY_SIGNATURES] A state identifier for signatures on roster transition histories.
                         * @property {com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties|null} [HistoryService_I_PROOF_VOTES] A state identifier for votes on history proofs.
                         * @property {com.hedera.hapi.platform.state.NodeId.$Properties|null} [HintsService_I_CRS_PUBLICATIONS] A state identifier for the CRS publications.
                         * @property {proto.HookId.$Properties|null} [ContractService_I_EVM_HOOK_STATES] A state identifier for EVM hook states.
                         * @property {com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Properties|null} [ContractService_I_EVM_HOOK_STORAGE] A state identifier for EVM hook storage.
                         * @property {proto.AccountID.$Properties|null} [AddressBookService_I_ACCOUNT_NODE_REL] A state identifier for account to node relationships.
                         * @property {com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties|null} [HistoryService_I_WRAPS_MESSAGE_HISTORIES] A state identifier for WRAPS message histories used in a roster transition proof.
                         * @property {proto.EntityNumber.$Properties|null} [AddressBookService_I_REGISTERED_NODES] A state identifier for registered nodes.
                         * @property {number|Long|null} [RecordCache_I_TRANSACTION_RECEIPTS] Queue index for the round receipts queue.
                         * @property {number|Long|null} [FileService_I_UPGRADE_DATA_150] Queue index for the `150` upgrade file data.
                         * @property {number|Long|null} [FileService_I_UPGRADE_DATA_151] Queue index for the `151` upgrade file data.
                         * @property {number|Long|null} [FileService_I_UPGRADE_DATA_152] Queue index for the `152` upgrade file data.
                         * @property {number|Long|null} [FileService_I_UPGRADE_DATA_153] Queue index for the `153` upgrade file data.
                         * @property {number|Long|null} [FileService_I_UPGRADE_DATA_154] Queue index for the `154` upgrade file data.
                         * @property {number|Long|null} [FileService_I_UPGRADE_DATA_155] Queue index for the `155` upgrade file data.
                         * @property {number|Long|null} [FileService_I_UPGRADE_DATA_156] Queue index for the `156` upgrade file data.
                         * @property {number|Long|null} [FileService_I_UPGRADE_DATA_157] Queue index for the `157` upgrade file data.
                         * @property {number|Long|null} [FileService_I_UPGRADE_DATA_158] Queue index for the `158` upgrade file data.
                         * @property {number|Long|null} [FileService_I_UPGRADE_DATA_159] Queue index for the `159` upgrade file data.
                         * @property {"singleton"|"TokenService_I_ACCOUNTS"|"TokenService_I_ALIASES"|"ContractService_I_STORAGE"|"ContractService_I_BYTECODE"|"FileService_I_FILES"|"TokenService_I_TOKENS"|"TokenService_I_NFTS"|"TokenService_I_TOKEN_RELS"|"TokenService_I_STAKING_INFOS"|"ScheduleService_I_SCHEDULES_BY_ID"|"ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC"|"ScheduleService_I_SCHEDULES_BY_EQUALITY"|"AddressBookService_I_NODES"|"ConsensusService_I_TOPICS"|"TokenService_I_PENDING_AIRDROPS"|"RosterService_I_ROSTERS"|"ScheduleService_I_SCHEDULED_COUNTS"|"ScheduleService_I_SCHEDULE_ID_BY_EQUALITY"|"TssBaseService_I_TSS_MESSAGES"|"TssBaseService_I_TSS_VOTES"|"ScheduleService_I_SCHEDULED_ORDERS"|"ScheduleService_I_SCHEDULED_USAGES"|"TssBaseService_I_TSS_ENCRYPTION_KEYS"|"HintsService_I_HINTS_KEY_SETS"|"HintsService_I_PREPROCESSING_VOTES"|"HistoryService_I_PROOF_KEY_SETS"|"HistoryService_I_HISTORY_SIGNATURES"|"HistoryService_I_PROOF_VOTES"|"HintsService_I_CRS_PUBLICATIONS"|"ContractService_I_EVM_HOOK_STATES"|"ContractService_I_EVM_HOOK_STORAGE"|"AddressBookService_I_ACCOUNT_NODE_REL"|"HistoryService_I_WRAPS_MESSAGE_HISTORIES"|"AddressBookService_I_REGISTERED_NODES"|"RecordCache_I_TRANSACTION_RECEIPTS"|"FileService_I_UPGRADE_DATA_150"|"FileService_I_UPGRADE_DATA_151"|"FileService_I_UPGRADE_DATA_152"|"FileService_I_UPGRADE_DATA_153"|"FileService_I_UPGRADE_DATA_154"|"FileService_I_UPGRADE_DATA_155"|"FileService_I_UPGRADE_DATA_156"|"FileService_I_UPGRADE_DATA_157"|"FileService_I_UPGRADE_DATA_158"|"FileService_I_UPGRADE_DATA_159"} [key] StateKey key
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a StateKey.
                         * @memberof com.hedera.hapi.platform.state
                         * @interface IStateKey
                         * @augments com.hedera.hapi.platform.state.StateKey.$Properties
                         * @deprecated Use com.hedera.hapi.platform.state.StateKey.$Properties instead.
                         */

                        /**
                         * Narrowed shape of a StateKey.
                         * @typedef {{
                         *   singleton?: com.hedera.hapi.platform.state.SingletonType|null;
                         *   TokenService_I_ACCOUNTS?: proto.AccountID.$Shape|null;
                         *   TokenService_I_ALIASES?: proto.ProtoBytes.$Shape|null;
                         *   ContractService_I_STORAGE?: proto.SlotKey.$Shape|null;
                         *   ContractService_I_BYTECODE?: proto.ContractID.$Shape|null;
                         *   FileService_I_FILES?: proto.FileID.$Shape|null;
                         *   TokenService_I_TOKENS?: proto.TokenID.$Shape|null;
                         *   TokenService_I_NFTS?: proto.NftID.$Shape|null;
                         *   TokenService_I_TOKEN_RELS?: proto.EntityIDPair.$Shape|null;
                         *   TokenService_I_STAKING_INFOS?: proto.EntityNumber.$Shape|null;
                         *   ScheduleService_I_SCHEDULES_BY_ID?: proto.ScheduleID.$Shape|null;
                         *   ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: proto.ProtoLong.$Shape|null;
                         *   ScheduleService_I_SCHEDULES_BY_EQUALITY?: proto.ProtoBytes.$Shape|null;
                         *   AddressBookService_I_NODES?: proto.EntityNumber.$Shape|null;
                         *   ConsensusService_I_TOPICS?: proto.TopicID.$Shape|null;
                         *   TokenService_I_PENDING_AIRDROPS?: proto.PendingAirdropId.$Shape|null;
                         *   RosterService_I_ROSTERS?: proto.ProtoBytes.$Shape|null;
                         *   ScheduleService_I_SCHEDULED_COUNTS?: proto.TimestampSeconds.$Shape|null;
                         *   ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: proto.ProtoBytes.$Shape|null;
                         *   TssBaseService_I_TSS_MESSAGES?: com.hedera.hapi.node.state.tss.TssMessageMapKey.$Shape|null;
                         *   TssBaseService_I_TSS_VOTES?: com.hedera.hapi.node.state.tss.TssVoteMapKey.$Shape|null;
                         *   ScheduleService_I_SCHEDULED_ORDERS?: proto.ScheduledOrder.$Shape|null;
                         *   ScheduleService_I_SCHEDULED_USAGES?: proto.TimestampSeconds.$Shape|null;
                         *   TssBaseService_I_TSS_ENCRYPTION_KEYS?: proto.EntityNumber.$Shape|null;
                         *   HintsService_I_HINTS_KEY_SETS?: com.hedera.hapi.node.state.hints.HintsPartyId.$Shape|null;
                         *   HintsService_I_PREPROCESSING_VOTES?: com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Shape|null;
                         *   HistoryService_I_PROOF_KEY_SETS?: com.hedera.hapi.platform.state.NodeId.$Shape|null;
                         *   HistoryService_I_HISTORY_SIGNATURES?: com.hedera.hapi.node.state.history.ConstructionNodeId.$Shape|null;
                         *   HistoryService_I_PROOF_VOTES?: com.hedera.hapi.node.state.history.ConstructionNodeId.$Shape|null;
                         *   HintsService_I_CRS_PUBLICATIONS?: com.hedera.hapi.platform.state.NodeId.$Shape|null;
                         *   ContractService_I_EVM_HOOK_STATES?: proto.HookId.$Shape|null;
                         *   ContractService_I_EVM_HOOK_STORAGE?: com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Shape|null;
                         *   AddressBookService_I_ACCOUNT_NODE_REL?: proto.AccountID.$Shape|null;
                         *   HistoryService_I_WRAPS_MESSAGE_HISTORIES?: com.hedera.hapi.node.state.history.ConstructionNodeId.$Shape|null;
                         *   AddressBookService_I_REGISTERED_NODES?: proto.EntityNumber.$Shape|null;
                         *   RecordCache_I_TRANSACTION_RECEIPTS?: number|Long|null;
                         *   FileService_I_UPGRADE_DATA_150?: number|Long|null;
                         *   FileService_I_UPGRADE_DATA_151?: number|Long|null;
                         *   FileService_I_UPGRADE_DATA_152?: number|Long|null;
                         *   FileService_I_UPGRADE_DATA_153?: number|Long|null;
                         *   FileService_I_UPGRADE_DATA_154?: number|Long|null;
                         *   FileService_I_UPGRADE_DATA_155?: number|Long|null;
                         *   FileService_I_UPGRADE_DATA_156?: number|Long|null;
                         *   FileService_I_UPGRADE_DATA_157?: number|Long|null;
                         *   FileService_I_UPGRADE_DATA_158?: number|Long|null;
                         *   FileService_I_UPGRADE_DATA_159?: number|Long|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * } & (
                         *   ({ key?: undefined; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "singleton"; singleton: com.hedera.hapi.platform.state.SingletonType; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "TokenService_I_ACCOUNTS"; singleton?: null; TokenService_I_ACCOUNTS: proto.AccountID.$Shape; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "TokenService_I_ALIASES"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES: proto.ProtoBytes.$Shape; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "ContractService_I_STORAGE"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE: proto.SlotKey.$Shape; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "ContractService_I_BYTECODE"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE: proto.ContractID.$Shape; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "FileService_I_FILES"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES: proto.FileID.$Shape; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "TokenService_I_TOKENS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS: proto.TokenID.$Shape; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "TokenService_I_NFTS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS: proto.NftID.$Shape; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "TokenService_I_TOKEN_RELS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS: proto.EntityIDPair.$Shape; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "TokenService_I_STAKING_INFOS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS: proto.EntityNumber.$Shape; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "ScheduleService_I_SCHEDULES_BY_ID"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID: proto.ScheduleID.$Shape; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC: proto.ProtoLong.$Shape; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "ScheduleService_I_SCHEDULES_BY_EQUALITY"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY: proto.ProtoBytes.$Shape; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "AddressBookService_I_NODES"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES: proto.EntityNumber.$Shape; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "ConsensusService_I_TOPICS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS: proto.TopicID.$Shape; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "TokenService_I_PENDING_AIRDROPS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS: proto.PendingAirdropId.$Shape; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "RosterService_I_ROSTERS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS: proto.ProtoBytes.$Shape; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "ScheduleService_I_SCHEDULED_COUNTS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS: proto.TimestampSeconds.$Shape; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY: proto.ProtoBytes.$Shape; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "TssBaseService_I_TSS_MESSAGES"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES: com.hedera.hapi.node.state.tss.TssMessageMapKey.$Shape; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "TssBaseService_I_TSS_VOTES"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES: com.hedera.hapi.node.state.tss.TssVoteMapKey.$Shape; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "ScheduleService_I_SCHEDULED_ORDERS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS: proto.ScheduledOrder.$Shape; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "ScheduleService_I_SCHEDULED_USAGES"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES: proto.TimestampSeconds.$Shape; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "TssBaseService_I_TSS_ENCRYPTION_KEYS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS: proto.EntityNumber.$Shape; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "HintsService_I_HINTS_KEY_SETS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS: com.hedera.hapi.node.state.hints.HintsPartyId.$Shape; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "HintsService_I_PREPROCESSING_VOTES"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES: com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Shape; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "HistoryService_I_PROOF_KEY_SETS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS: com.hedera.hapi.platform.state.NodeId.$Shape; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "HistoryService_I_HISTORY_SIGNATURES"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES: com.hedera.hapi.node.state.history.ConstructionNodeId.$Shape; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "HistoryService_I_PROOF_VOTES"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES: com.hedera.hapi.node.state.history.ConstructionNodeId.$Shape; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "HintsService_I_CRS_PUBLICATIONS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS: com.hedera.hapi.platform.state.NodeId.$Shape; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "ContractService_I_EVM_HOOK_STATES"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES: proto.HookId.$Shape; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "ContractService_I_EVM_HOOK_STORAGE"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE: com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Shape; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "AddressBookService_I_ACCOUNT_NODE_REL"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL: proto.AccountID.$Shape; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "HistoryService_I_WRAPS_MESSAGE_HISTORIES"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES: com.hedera.hapi.node.state.history.ConstructionNodeId.$Shape; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "AddressBookService_I_REGISTERED_NODES"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES: proto.EntityNumber.$Shape; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "RecordCache_I_TRANSACTION_RECEIPTS"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS: number|Long; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "FileService_I_UPGRADE_DATA_150"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150: number|Long; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "FileService_I_UPGRADE_DATA_151"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151: number|Long; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "FileService_I_UPGRADE_DATA_152"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152: number|Long; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "FileService_I_UPGRADE_DATA_153"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153: number|Long; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "FileService_I_UPGRADE_DATA_154"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154: number|Long; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "FileService_I_UPGRADE_DATA_155"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155: number|Long; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "FileService_I_UPGRADE_DATA_156"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156: number|Long; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "FileService_I_UPGRADE_DATA_157"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157: number|Long; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "FileService_I_UPGRADE_DATA_158"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158: number|Long; FileService_I_UPGRADE_DATA_159?: null }|{ key?: "FileService_I_UPGRADE_DATA_159"; singleton?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; TokenService_I_PENDING_AIRDROPS?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_PREPROCESSING_VOTES?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_PUBLICATIONS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; AddressBookService_I_REGISTERED_NODES?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159: number|Long })
                         * )} com.hedera.hapi.platform.state.StateKey.$Shape
                         */

                        /**
                         * Constructs a new StateKey.
                         * @memberof com.hedera.hapi.platform.state
                         * @classdesc This message defines the type of keys used in the Virtual Map State.
                         * Note that `_I_` plays a role of a reliable separator between the service name
                         * and the state name, which will be present in all types of code generation results as just underscores
                         * may be deleted and create unexpected collisions.
                         * @constructor
                         * @param {com.hedera.hapi.platform.state.StateKey.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const StateKey = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * StateKey singleton.
                         * @member {com.hedera.hapi.platform.state.SingletonType|null|undefined} singleton
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.singleton = null;

                        /**
                         * A state identifier for the Accounts state.
                         * @member {proto.AccountID.$Properties|null|undefined} TokenService_I_ACCOUNTS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.TokenService_I_ACCOUNTS = null;

                        /**
                         * A state identifier for account aliases.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} TokenService_I_ALIASES
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.TokenService_I_ALIASES = null;

                        /**
                         * A state identifier for contract storage slots.
                         * @member {proto.SlotKey.$Properties|null|undefined} ContractService_I_STORAGE
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.ContractService_I_STORAGE = null;

                        /**
                         * A state identifier for contract bytecode.
                         * @member {proto.ContractID.$Properties|null|undefined} ContractService_I_BYTECODE
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.ContractService_I_BYTECODE = null;

                        /**
                         * A state identifier for Hedera File Service (HFS).
                         * @member {proto.FileID.$Properties|null|undefined} FileService_I_FILES
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.FileService_I_FILES = null;

                        /**
                         * A state identifier for Hedera Token Service (HTS).
                         * @member {proto.TokenID.$Properties|null|undefined} TokenService_I_TOKENS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.TokenService_I_TOKENS = null;

                        /**
                         * A state identifier for non-fungible/unique tokens.
                         * @member {proto.NftID.$Properties|null|undefined} TokenService_I_NFTS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.TokenService_I_NFTS = null;

                        /**
                         * A state identifier for token relationships.
                         * @member {proto.EntityIDPair.$Properties|null|undefined} TokenService_I_TOKEN_RELS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.TokenService_I_TOKEN_RELS = null;

                        /**
                         * A state identifier for network staking information.
                         * @member {proto.EntityNumber.$Properties|null|undefined} TokenService_I_STAKING_INFOS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.TokenService_I_STAKING_INFOS = null;

                        /**
                         * A state identifier for scheduled transactions.
                         * @member {proto.ScheduleID.$Properties|null|undefined} ScheduleService_I_SCHEDULES_BY_ID
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.ScheduleService_I_SCHEDULES_BY_ID = null;

                        /**
                         * A state identifier for scheduled transaction expiration.
                         * @member {proto.ProtoLong.$Properties|null|undefined} ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC = null;

                        /**
                         * A state identifier for scheduled transaction deduplication.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} ScheduleService_I_SCHEDULES_BY_EQUALITY
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.ScheduleService_I_SCHEDULES_BY_EQUALITY = null;

                        /**
                         * A state identifier for address book nodes.
                         * @member {proto.EntityNumber.$Properties|null|undefined} AddressBookService_I_NODES
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.AddressBookService_I_NODES = null;

                        /**
                         * A state identifier for the Topics state.
                         * @member {proto.TopicID.$Properties|null|undefined} ConsensusService_I_TOPICS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.ConsensusService_I_TOPICS = null;

                        /**
                         * A state identifier for pending airdrops.
                         * @member {proto.PendingAirdropId.$Properties|null|undefined} TokenService_I_PENDING_AIRDROPS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.TokenService_I_PENDING_AIRDROPS = null;

                        /**
                         * A state identifier for the rosters.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} RosterService_I_ROSTERS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.RosterService_I_ROSTERS = null;

                        /**
                         * A state identifier for counts of transactions scheduled and
                         * processed in a second.
                         * @member {proto.TimestampSeconds.$Properties|null|undefined} ScheduleService_I_SCHEDULED_COUNTS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.ScheduleService_I_SCHEDULED_COUNTS = null;

                        /**
                         * A state identifier for scheduled transaction deduplication.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} ScheduleService_I_SCHEDULE_ID_BY_EQUALITY
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY = null;

                        /**
                         * A state identifier for TSS messages.
                         * @member {com.hedera.hapi.node.state.tss.TssMessageMapKey.$Properties|null|undefined} TssBaseService_I_TSS_MESSAGES
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.TssBaseService_I_TSS_MESSAGES = null;

                        /**
                         * A state identifier for TSS votes.
                         * @member {com.hedera.hapi.node.state.tss.TssVoteMapKey.$Properties|null|undefined} TssBaseService_I_TSS_VOTES
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.TssBaseService_I_TSS_VOTES = null;

                        /**
                         * A state identifier for the ordering of scheduled transactions.
                         * @member {proto.ScheduledOrder.$Properties|null|undefined} ScheduleService_I_SCHEDULED_ORDERS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.ScheduleService_I_SCHEDULED_ORDERS = null;

                        /**
                         * A state identifier for scheduled throttle usage snapshots.
                         * @member {proto.TimestampSeconds.$Properties|null|undefined} ScheduleService_I_SCHEDULED_USAGES
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.ScheduleService_I_SCHEDULED_USAGES = null;

                        /**
                         * A state identifier for the TSS encryption keys.
                         * @member {proto.EntityNumber.$Properties|null|undefined} TssBaseService_I_TSS_ENCRYPTION_KEYS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.TssBaseService_I_TSS_ENCRYPTION_KEYS = null;

                        /**
                         * A state identifier for hinTS key sets.
                         * @member {com.hedera.hapi.node.state.hints.HintsPartyId.$Properties|null|undefined} HintsService_I_HINTS_KEY_SETS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.HintsService_I_HINTS_KEY_SETS = null;

                        /**
                         * A state identifier for hinTS preprocessing output votes.
                         * @member {com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Properties|null|undefined} HintsService_I_PREPROCESSING_VOTES
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.HintsService_I_PREPROCESSING_VOTES = null;

                        /**
                         * A state identifier for history proof key sets.
                         * @member {com.hedera.hapi.platform.state.NodeId.$Properties|null|undefined} HistoryService_I_PROOF_KEY_SETS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.HistoryService_I_PROOF_KEY_SETS = null;

                        /**
                         * A state identifier for signatures on roster transition histories.
                         * @member {com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties|null|undefined} HistoryService_I_HISTORY_SIGNATURES
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.HistoryService_I_HISTORY_SIGNATURES = null;

                        /**
                         * A state identifier for votes on history proofs.
                         * @member {com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties|null|undefined} HistoryService_I_PROOF_VOTES
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.HistoryService_I_PROOF_VOTES = null;

                        /**
                         * A state identifier for the CRS publications.
                         * @member {com.hedera.hapi.platform.state.NodeId.$Properties|null|undefined} HintsService_I_CRS_PUBLICATIONS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.HintsService_I_CRS_PUBLICATIONS = null;

                        /**
                         * A state identifier for EVM hook states.
                         * @member {proto.HookId.$Properties|null|undefined} ContractService_I_EVM_HOOK_STATES
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.ContractService_I_EVM_HOOK_STATES = null;

                        /**
                         * A state identifier for EVM hook storage.
                         * @member {com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Properties|null|undefined} ContractService_I_EVM_HOOK_STORAGE
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.ContractService_I_EVM_HOOK_STORAGE = null;

                        /**
                         * A state identifier for account to node relationships.
                         * @member {proto.AccountID.$Properties|null|undefined} AddressBookService_I_ACCOUNT_NODE_REL
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.AddressBookService_I_ACCOUNT_NODE_REL = null;

                        /**
                         * A state identifier for WRAPS message histories used in a roster transition proof.
                         * @member {com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties|null|undefined} HistoryService_I_WRAPS_MESSAGE_HISTORIES
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.HistoryService_I_WRAPS_MESSAGE_HISTORIES = null;

                        /**
                         * A state identifier for registered nodes.
                         * @member {proto.EntityNumber.$Properties|null|undefined} AddressBookService_I_REGISTERED_NODES
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.AddressBookService_I_REGISTERED_NODES = null;

                        /**
                         * Queue index for the round receipts queue.
                         * @member {number|Long|null|undefined} RecordCache_I_TRANSACTION_RECEIPTS
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.RecordCache_I_TRANSACTION_RECEIPTS = null;

                        /**
                         * Queue index for the `150` upgrade file data.
                         * @member {number|Long|null|undefined} FileService_I_UPGRADE_DATA_150
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.FileService_I_UPGRADE_DATA_150 = null;

                        /**
                         * Queue index for the `151` upgrade file data.
                         * @member {number|Long|null|undefined} FileService_I_UPGRADE_DATA_151
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.FileService_I_UPGRADE_DATA_151 = null;

                        /**
                         * Queue index for the `152` upgrade file data.
                         * @member {number|Long|null|undefined} FileService_I_UPGRADE_DATA_152
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.FileService_I_UPGRADE_DATA_152 = null;

                        /**
                         * Queue index for the `153` upgrade file data.
                         * @member {number|Long|null|undefined} FileService_I_UPGRADE_DATA_153
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.FileService_I_UPGRADE_DATA_153 = null;

                        /**
                         * Queue index for the `154` upgrade file data.
                         * @member {number|Long|null|undefined} FileService_I_UPGRADE_DATA_154
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.FileService_I_UPGRADE_DATA_154 = null;

                        /**
                         * Queue index for the `155` upgrade file data.
                         * @member {number|Long|null|undefined} FileService_I_UPGRADE_DATA_155
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.FileService_I_UPGRADE_DATA_155 = null;

                        /**
                         * Queue index for the `156` upgrade file data.
                         * @member {number|Long|null|undefined} FileService_I_UPGRADE_DATA_156
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.FileService_I_UPGRADE_DATA_156 = null;

                        /**
                         * Queue index for the `157` upgrade file data.
                         * @member {number|Long|null|undefined} FileService_I_UPGRADE_DATA_157
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.FileService_I_UPGRADE_DATA_157 = null;

                        /**
                         * Queue index for the `158` upgrade file data.
                         * @member {number|Long|null|undefined} FileService_I_UPGRADE_DATA_158
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.FileService_I_UPGRADE_DATA_158 = null;

                        /**
                         * Queue index for the `159` upgrade file data.
                         * @member {number|Long|null|undefined} FileService_I_UPGRADE_DATA_159
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        StateKey.prototype.FileService_I_UPGRADE_DATA_159 = null;

                        // OneOf field names bound to virtual getters and setters
                        let $oneOfFields;

                        /**
                         * StateKey key.
                         * @member {"singleton"|"TokenService_I_ACCOUNTS"|"TokenService_I_ALIASES"|"ContractService_I_STORAGE"|"ContractService_I_BYTECODE"|"FileService_I_FILES"|"TokenService_I_TOKENS"|"TokenService_I_NFTS"|"TokenService_I_TOKEN_RELS"|"TokenService_I_STAKING_INFOS"|"ScheduleService_I_SCHEDULES_BY_ID"|"ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC"|"ScheduleService_I_SCHEDULES_BY_EQUALITY"|"AddressBookService_I_NODES"|"ConsensusService_I_TOPICS"|"TokenService_I_PENDING_AIRDROPS"|"RosterService_I_ROSTERS"|"ScheduleService_I_SCHEDULED_COUNTS"|"ScheduleService_I_SCHEDULE_ID_BY_EQUALITY"|"TssBaseService_I_TSS_MESSAGES"|"TssBaseService_I_TSS_VOTES"|"ScheduleService_I_SCHEDULED_ORDERS"|"ScheduleService_I_SCHEDULED_USAGES"|"TssBaseService_I_TSS_ENCRYPTION_KEYS"|"HintsService_I_HINTS_KEY_SETS"|"HintsService_I_PREPROCESSING_VOTES"|"HistoryService_I_PROOF_KEY_SETS"|"HistoryService_I_HISTORY_SIGNATURES"|"HistoryService_I_PROOF_VOTES"|"HintsService_I_CRS_PUBLICATIONS"|"ContractService_I_EVM_HOOK_STATES"|"ContractService_I_EVM_HOOK_STORAGE"|"AddressBookService_I_ACCOUNT_NODE_REL"|"HistoryService_I_WRAPS_MESSAGE_HISTORIES"|"AddressBookService_I_REGISTERED_NODES"|"RecordCache_I_TRANSACTION_RECEIPTS"|"FileService_I_UPGRADE_DATA_150"|"FileService_I_UPGRADE_DATA_151"|"FileService_I_UPGRADE_DATA_152"|"FileService_I_UPGRADE_DATA_153"|"FileService_I_UPGRADE_DATA_154"|"FileService_I_UPGRADE_DATA_155"|"FileService_I_UPGRADE_DATA_156"|"FileService_I_UPGRADE_DATA_157"|"FileService_I_UPGRADE_DATA_158"|"FileService_I_UPGRADE_DATA_159"|undefined} key
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         */
                        $Object.defineProperty(StateKey.prototype, "key", {
                            get: $util.oneOfGetter($oneOfFields = ["singleton", "TokenService_I_ACCOUNTS", "TokenService_I_ALIASES", "ContractService_I_STORAGE", "ContractService_I_BYTECODE", "FileService_I_FILES", "TokenService_I_TOKENS", "TokenService_I_NFTS", "TokenService_I_TOKEN_RELS", "TokenService_I_STAKING_INFOS", "ScheduleService_I_SCHEDULES_BY_ID", "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC", "ScheduleService_I_SCHEDULES_BY_EQUALITY", "AddressBookService_I_NODES", "ConsensusService_I_TOPICS", "TokenService_I_PENDING_AIRDROPS", "RosterService_I_ROSTERS", "ScheduleService_I_SCHEDULED_COUNTS", "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY", "TssBaseService_I_TSS_MESSAGES", "TssBaseService_I_TSS_VOTES", "ScheduleService_I_SCHEDULED_ORDERS", "ScheduleService_I_SCHEDULED_USAGES", "TssBaseService_I_TSS_ENCRYPTION_KEYS", "HintsService_I_HINTS_KEY_SETS", "HintsService_I_PREPROCESSING_VOTES", "HistoryService_I_PROOF_KEY_SETS", "HistoryService_I_HISTORY_SIGNATURES", "HistoryService_I_PROOF_VOTES", "HintsService_I_CRS_PUBLICATIONS", "ContractService_I_EVM_HOOK_STATES", "ContractService_I_EVM_HOOK_STORAGE", "AddressBookService_I_ACCOUNT_NODE_REL", "HistoryService_I_WRAPS_MESSAGE_HISTORIES", "AddressBookService_I_REGISTERED_NODES", "RecordCache_I_TRANSACTION_RECEIPTS", "FileService_I_UPGRADE_DATA_150", "FileService_I_UPGRADE_DATA_151", "FileService_I_UPGRADE_DATA_152", "FileService_I_UPGRADE_DATA_153", "FileService_I_UPGRADE_DATA_154", "FileService_I_UPGRADE_DATA_155", "FileService_I_UPGRADE_DATA_156", "FileService_I_UPGRADE_DATA_157", "FileService_I_UPGRADE_DATA_158", "FileService_I_UPGRADE_DATA_159"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * Creates a new StateKey instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @static
                         * @param {com.hedera.hapi.platform.state.StateKey.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.state.StateKey} StateKey instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.state.StateKey.$Shape): com.hedera.hapi.platform.state.StateKey & com.hedera.hapi.platform.state.StateKey.$Shape;
                         *   (properties?: com.hedera.hapi.platform.state.StateKey.$Properties): com.hedera.hapi.platform.state.StateKey;
                         * }}
                         */
                        StateKey.create = function(properties) {
                            return new StateKey(properties);
                        };

                        /**
                         * Encodes the specified StateKey message. Does not implicitly {@link com.hedera.hapi.platform.state.StateKey.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @static
                         * @param {com.hedera.hapi.platform.state.StateKey.$Properties} message StateKey message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        StateKey.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.singleton != null && $Object.hasOwnProperty.call(message, "singleton"))
                                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.singleton);
                            if (message.TokenService_I_ACCOUNTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_ACCOUNTS"))
                                $root.proto.AccountID.encode(message.TokenService_I_ACCOUNTS, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_ALIASES != null && $Object.hasOwnProperty.call(message, "TokenService_I_ALIASES"))
                                $root.proto.ProtoBytes.encode(message.TokenService_I_ALIASES, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.ContractService_I_STORAGE != null && $Object.hasOwnProperty.call(message, "ContractService_I_STORAGE"))
                                $root.proto.SlotKey.encode(message.ContractService_I_STORAGE, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                            if (message.ContractService_I_BYTECODE != null && $Object.hasOwnProperty.call(message, "ContractService_I_BYTECODE"))
                                $root.proto.ContractID.encode(message.ContractService_I_BYTECODE, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                            if (message.FileService_I_FILES != null && $Object.hasOwnProperty.call(message, "FileService_I_FILES"))
                                $root.proto.FileID.encode(message.FileService_I_FILES, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_TOKENS != null && $Object.hasOwnProperty.call(message, "TokenService_I_TOKENS"))
                                $root.proto.TokenID.encode(message.TokenService_I_TOKENS, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_NFTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_NFTS"))
                                $root.proto.NftID.encode(message.TokenService_I_NFTS, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_TOKEN_RELS != null && $Object.hasOwnProperty.call(message, "TokenService_I_TOKEN_RELS"))
                                $root.proto.EntityIDPair.encode(message.TokenService_I_TOKEN_RELS, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_STAKING_INFOS != null && $Object.hasOwnProperty.call(message, "TokenService_I_STAKING_INFOS"))
                                $root.proto.EntityNumber.encode(message.TokenService_I_STAKING_INFOS, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULES_BY_ID != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_ID"))
                                $root.proto.ScheduleID.encode(message.ScheduleService_I_SCHEDULES_BY_ID, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC"))
                                $root.proto.ProtoLong.encode(message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULES_BY_EQUALITY != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_EQUALITY"))
                                $root.proto.ProtoBytes.encode(message.ScheduleService_I_SCHEDULES_BY_EQUALITY, writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
                            if (message.AddressBookService_I_NODES != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_NODES"))
                                $root.proto.EntityNumber.encode(message.AddressBookService_I_NODES, writer.uint32(/* id 20, wireType 2 =*/162).fork(), _depth + 1).ldelim();
                            if (message.ConsensusService_I_TOPICS != null && $Object.hasOwnProperty.call(message, "ConsensusService_I_TOPICS"))
                                $root.proto.TopicID.encode(message.ConsensusService_I_TOPICS, writer.uint32(/* id 21, wireType 2 =*/170).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_PENDING_AIRDROPS != null && $Object.hasOwnProperty.call(message, "TokenService_I_PENDING_AIRDROPS"))
                                $root.proto.PendingAirdropId.encode(message.TokenService_I_PENDING_AIRDROPS, writer.uint32(/* id 25, wireType 2 =*/202).fork(), _depth + 1).ldelim();
                            if (message.RosterService_I_ROSTERS != null && $Object.hasOwnProperty.call(message, "RosterService_I_ROSTERS"))
                                $root.proto.ProtoBytes.encode(message.RosterService_I_ROSTERS, writer.uint32(/* id 28, wireType 2 =*/226).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULED_COUNTS != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_COUNTS"))
                                $root.proto.TimestampSeconds.encode(message.ScheduleService_I_SCHEDULED_COUNTS, writer.uint32(/* id 29, wireType 2 =*/234).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY"))
                                $root.proto.ProtoBytes.encode(message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY, writer.uint32(/* id 30, wireType 2 =*/242).fork(), _depth + 1).ldelim();
                            if (message.TssBaseService_I_TSS_MESSAGES != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_MESSAGES"))
                                $root.com.hedera.hapi.node.state.tss.TssMessageMapKey.encode(message.TssBaseService_I_TSS_MESSAGES, writer.uint32(/* id 31, wireType 2 =*/250).fork(), _depth + 1).ldelim();
                            if (message.TssBaseService_I_TSS_VOTES != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_VOTES"))
                                $root.com.hedera.hapi.node.state.tss.TssVoteMapKey.encode(message.TssBaseService_I_TSS_VOTES, writer.uint32(/* id 32, wireType 2 =*/258).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULED_ORDERS != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_ORDERS"))
                                $root.proto.ScheduledOrder.encode(message.ScheduleService_I_SCHEDULED_ORDERS, writer.uint32(/* id 33, wireType 2 =*/266).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULED_USAGES != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_USAGES"))
                                $root.proto.TimestampSeconds.encode(message.ScheduleService_I_SCHEDULED_USAGES, writer.uint32(/* id 34, wireType 2 =*/274).fork(), _depth + 1).ldelim();
                            if (message.TssBaseService_I_TSS_ENCRYPTION_KEYS != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_ENCRYPTION_KEYS"))
                                $root.proto.EntityNumber.encode(message.TssBaseService_I_TSS_ENCRYPTION_KEYS, writer.uint32(/* id 35, wireType 2 =*/282).fork(), _depth + 1).ldelim();
                            if (message.HintsService_I_HINTS_KEY_SETS != null && $Object.hasOwnProperty.call(message, "HintsService_I_HINTS_KEY_SETS"))
                                $root.com.hedera.hapi.node.state.hints.HintsPartyId.encode(message.HintsService_I_HINTS_KEY_SETS, writer.uint32(/* id 37, wireType 2 =*/298).fork(), _depth + 1).ldelim();
                            if (message.HintsService_I_PREPROCESSING_VOTES != null && $Object.hasOwnProperty.call(message, "HintsService_I_PREPROCESSING_VOTES"))
                                $root.com.hedera.hapi.node.state.hints.PreprocessingVoteId.encode(message.HintsService_I_PREPROCESSING_VOTES, writer.uint32(/* id 40, wireType 2 =*/322).fork(), _depth + 1).ldelim();
                            if (message.HistoryService_I_PROOF_KEY_SETS != null && $Object.hasOwnProperty.call(message, "HistoryService_I_PROOF_KEY_SETS"))
                                $root.com.hedera.hapi.platform.state.NodeId.encode(message.HistoryService_I_PROOF_KEY_SETS, writer.uint32(/* id 43, wireType 2 =*/346).fork(), _depth + 1).ldelim();
                            if (message.HistoryService_I_HISTORY_SIGNATURES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_HISTORY_SIGNATURES"))
                                $root.com.hedera.hapi.node.state.history.ConstructionNodeId.encode(message.HistoryService_I_HISTORY_SIGNATURES, writer.uint32(/* id 46, wireType 2 =*/370).fork(), _depth + 1).ldelim();
                            if (message.HistoryService_I_PROOF_VOTES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_PROOF_VOTES"))
                                $root.com.hedera.hapi.node.state.history.ConstructionNodeId.encode(message.HistoryService_I_PROOF_VOTES, writer.uint32(/* id 47, wireType 2 =*/378).fork(), _depth + 1).ldelim();
                            if (message.HintsService_I_CRS_PUBLICATIONS != null && $Object.hasOwnProperty.call(message, "HintsService_I_CRS_PUBLICATIONS"))
                                $root.com.hedera.hapi.platform.state.NodeId.encode(message.HintsService_I_CRS_PUBLICATIONS, writer.uint32(/* id 49, wireType 2 =*/394).fork(), _depth + 1).ldelim();
                            if (message.ContractService_I_EVM_HOOK_STATES != null && $Object.hasOwnProperty.call(message, "ContractService_I_EVM_HOOK_STATES"))
                                $root.proto.HookId.encode(message.ContractService_I_EVM_HOOK_STATES, writer.uint32(/* id 51, wireType 2 =*/410).fork(), _depth + 1).ldelim();
                            if (message.ContractService_I_EVM_HOOK_STORAGE != null && $Object.hasOwnProperty.call(message, "ContractService_I_EVM_HOOK_STORAGE"))
                                $root.com.hedera.hapi.node.state.hooks.EvmHookSlotKey.encode(message.ContractService_I_EVM_HOOK_STORAGE, writer.uint32(/* id 52, wireType 2 =*/418).fork(), _depth + 1).ldelim();
                            if (message.AddressBookService_I_ACCOUNT_NODE_REL != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_ACCOUNT_NODE_REL"))
                                $root.proto.AccountID.encode(message.AddressBookService_I_ACCOUNT_NODE_REL, writer.uint32(/* id 53, wireType 2 =*/426).fork(), _depth + 1).ldelim();
                            if (message.HistoryService_I_WRAPS_MESSAGE_HISTORIES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_WRAPS_MESSAGE_HISTORIES"))
                                $root.com.hedera.hapi.node.state.history.ConstructionNodeId.encode(message.HistoryService_I_WRAPS_MESSAGE_HISTORIES, writer.uint32(/* id 54, wireType 2 =*/434).fork(), _depth + 1).ldelim();
                            if (message.AddressBookService_I_REGISTERED_NODES != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_REGISTERED_NODES"))
                                $root.proto.EntityNumber.encode(message.AddressBookService_I_REGISTERED_NODES, writer.uint32(/* id 56, wireType 2 =*/450).fork(), _depth + 1).ldelim();
                            if (message.RecordCache_I_TRANSACTION_RECEIPTS != null && $Object.hasOwnProperty.call(message, "RecordCache_I_TRANSACTION_RECEIPTS"))
                                writer.uint32(/* id 126, wireType 0 =*/1008).uint64(message.RecordCache_I_TRANSACTION_RECEIPTS);
                            if (message.FileService_I_UPGRADE_DATA_150 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_150"))
                                writer.uint32(/* id 10001, wireType 0 =*/80008).uint64(message.FileService_I_UPGRADE_DATA_150);
                            if (message.FileService_I_UPGRADE_DATA_151 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_151"))
                                writer.uint32(/* id 10002, wireType 0 =*/80016).uint64(message.FileService_I_UPGRADE_DATA_151);
                            if (message.FileService_I_UPGRADE_DATA_152 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_152"))
                                writer.uint32(/* id 10003, wireType 0 =*/80024).uint64(message.FileService_I_UPGRADE_DATA_152);
                            if (message.FileService_I_UPGRADE_DATA_153 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_153"))
                                writer.uint32(/* id 10004, wireType 0 =*/80032).uint64(message.FileService_I_UPGRADE_DATA_153);
                            if (message.FileService_I_UPGRADE_DATA_154 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_154"))
                                writer.uint32(/* id 10005, wireType 0 =*/80040).uint64(message.FileService_I_UPGRADE_DATA_154);
                            if (message.FileService_I_UPGRADE_DATA_155 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_155"))
                                writer.uint32(/* id 10006, wireType 0 =*/80048).uint64(message.FileService_I_UPGRADE_DATA_155);
                            if (message.FileService_I_UPGRADE_DATA_156 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_156"))
                                writer.uint32(/* id 10007, wireType 0 =*/80056).uint64(message.FileService_I_UPGRADE_DATA_156);
                            if (message.FileService_I_UPGRADE_DATA_157 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_157"))
                                writer.uint32(/* id 10008, wireType 0 =*/80064).uint64(message.FileService_I_UPGRADE_DATA_157);
                            if (message.FileService_I_UPGRADE_DATA_158 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_158"))
                                writer.uint32(/* id 10009, wireType 0 =*/80072).uint64(message.FileService_I_UPGRADE_DATA_158);
                            if (message.FileService_I_UPGRADE_DATA_159 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_159"))
                                writer.uint32(/* id 10010, wireType 0 =*/80080).uint64(message.FileService_I_UPGRADE_DATA_159);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified StateKey message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.state.StateKey.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @static
                         * @param {com.hedera.hapi.platform.state.StateKey.$Properties} message StateKey message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        StateKey.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a StateKey message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.state.StateKey & com.hedera.hapi.platform.state.StateKey.$Shape} StateKey
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        StateKey.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.state.StateKey();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        message.singleton = reader.int32();
                                        message.key = "singleton";
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_ACCOUNTS = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_ACCOUNTS);
                                        message.key = "TokenService_I_ACCOUNTS";
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_ALIASES = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_ALIASES);
                                        message.key = "TokenService_I_ALIASES";
                                        continue;
                                    }
                                case 4: {
                                        if (wireType !== 2)
                                            break;
                                        message.ContractService_I_STORAGE = $root.proto.SlotKey.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ContractService_I_STORAGE);
                                        message.key = "ContractService_I_STORAGE";
                                        continue;
                                    }
                                case 5: {
                                        if (wireType !== 2)
                                            break;
                                        message.ContractService_I_BYTECODE = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ContractService_I_BYTECODE);
                                        message.key = "ContractService_I_BYTECODE";
                                        continue;
                                    }
                                case 6: {
                                        if (wireType !== 2)
                                            break;
                                        message.FileService_I_FILES = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FileService_I_FILES);
                                        message.key = "FileService_I_FILES";
                                        continue;
                                    }
                                case 7: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_TOKENS = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_TOKENS);
                                        message.key = "TokenService_I_TOKENS";
                                        continue;
                                    }
                                case 8: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_NFTS = $root.proto.NftID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_NFTS);
                                        message.key = "TokenService_I_NFTS";
                                        continue;
                                    }
                                case 9: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_TOKEN_RELS = $root.proto.EntityIDPair.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_TOKEN_RELS);
                                        message.key = "TokenService_I_TOKEN_RELS";
                                        continue;
                                    }
                                case 10: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_STAKING_INFOS = $root.proto.EntityNumber.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_STAKING_INFOS);
                                        message.key = "TokenService_I_STAKING_INFOS";
                                        continue;
                                    }
                                case 14: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULES_BY_ID = $root.proto.ScheduleID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULES_BY_ID);
                                        message.key = "ScheduleService_I_SCHEDULES_BY_ID";
                                        continue;
                                    }
                                case 15: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC = $root.proto.ProtoLong.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC);
                                        message.key = "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC";
                                        continue;
                                    }
                                case 16: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULES_BY_EQUALITY = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULES_BY_EQUALITY);
                                        message.key = "ScheduleService_I_SCHEDULES_BY_EQUALITY";
                                        continue;
                                    }
                                case 20: {
                                        if (wireType !== 2)
                                            break;
                                        message.AddressBookService_I_NODES = $root.proto.EntityNumber.decode(reader, reader.uint32(), $undefined, _depth + 1, message.AddressBookService_I_NODES);
                                        message.key = "AddressBookService_I_NODES";
                                        continue;
                                    }
                                case 21: {
                                        if (wireType !== 2)
                                            break;
                                        message.ConsensusService_I_TOPICS = $root.proto.TopicID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ConsensusService_I_TOPICS);
                                        message.key = "ConsensusService_I_TOPICS";
                                        continue;
                                    }
                                case 25: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_PENDING_AIRDROPS = $root.proto.PendingAirdropId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_PENDING_AIRDROPS);
                                        message.key = "TokenService_I_PENDING_AIRDROPS";
                                        continue;
                                    }
                                case 28: {
                                        if (wireType !== 2)
                                            break;
                                        message.RosterService_I_ROSTERS = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.RosterService_I_ROSTERS);
                                        message.key = "RosterService_I_ROSTERS";
                                        continue;
                                    }
                                case 29: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULED_COUNTS = $root.proto.TimestampSeconds.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULED_COUNTS);
                                        message.key = "ScheduleService_I_SCHEDULED_COUNTS";
                                        continue;
                                    }
                                case 30: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY);
                                        message.key = "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY";
                                        continue;
                                    }
                                case 31: {
                                        if (wireType !== 2)
                                            break;
                                        message.TssBaseService_I_TSS_MESSAGES = $root.com.hedera.hapi.node.state.tss.TssMessageMapKey.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TssBaseService_I_TSS_MESSAGES);
                                        message.key = "TssBaseService_I_TSS_MESSAGES";
                                        continue;
                                    }
                                case 32: {
                                        if (wireType !== 2)
                                            break;
                                        message.TssBaseService_I_TSS_VOTES = $root.com.hedera.hapi.node.state.tss.TssVoteMapKey.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TssBaseService_I_TSS_VOTES);
                                        message.key = "TssBaseService_I_TSS_VOTES";
                                        continue;
                                    }
                                case 33: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULED_ORDERS = $root.proto.ScheduledOrder.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULED_ORDERS);
                                        message.key = "ScheduleService_I_SCHEDULED_ORDERS";
                                        continue;
                                    }
                                case 34: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULED_USAGES = $root.proto.TimestampSeconds.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULED_USAGES);
                                        message.key = "ScheduleService_I_SCHEDULED_USAGES";
                                        continue;
                                    }
                                case 35: {
                                        if (wireType !== 2)
                                            break;
                                        message.TssBaseService_I_TSS_ENCRYPTION_KEYS = $root.proto.EntityNumber.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TssBaseService_I_TSS_ENCRYPTION_KEYS);
                                        message.key = "TssBaseService_I_TSS_ENCRYPTION_KEYS";
                                        continue;
                                    }
                                case 37: {
                                        if (wireType !== 2)
                                            break;
                                        message.HintsService_I_HINTS_KEY_SETS = $root.com.hedera.hapi.node.state.hints.HintsPartyId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HintsService_I_HINTS_KEY_SETS);
                                        message.key = "HintsService_I_HINTS_KEY_SETS";
                                        continue;
                                    }
                                case 40: {
                                        if (wireType !== 2)
                                            break;
                                        message.HintsService_I_PREPROCESSING_VOTES = $root.com.hedera.hapi.node.state.hints.PreprocessingVoteId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HintsService_I_PREPROCESSING_VOTES);
                                        message.key = "HintsService_I_PREPROCESSING_VOTES";
                                        continue;
                                    }
                                case 43: {
                                        if (wireType !== 2)
                                            break;
                                        message.HistoryService_I_PROOF_KEY_SETS = $root.com.hedera.hapi.platform.state.NodeId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HistoryService_I_PROOF_KEY_SETS);
                                        message.key = "HistoryService_I_PROOF_KEY_SETS";
                                        continue;
                                    }
                                case 46: {
                                        if (wireType !== 2)
                                            break;
                                        message.HistoryService_I_HISTORY_SIGNATURES = $root.com.hedera.hapi.node.state.history.ConstructionNodeId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HistoryService_I_HISTORY_SIGNATURES);
                                        message.key = "HistoryService_I_HISTORY_SIGNATURES";
                                        continue;
                                    }
                                case 47: {
                                        if (wireType !== 2)
                                            break;
                                        message.HistoryService_I_PROOF_VOTES = $root.com.hedera.hapi.node.state.history.ConstructionNodeId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HistoryService_I_PROOF_VOTES);
                                        message.key = "HistoryService_I_PROOF_VOTES";
                                        continue;
                                    }
                                case 49: {
                                        if (wireType !== 2)
                                            break;
                                        message.HintsService_I_CRS_PUBLICATIONS = $root.com.hedera.hapi.platform.state.NodeId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HintsService_I_CRS_PUBLICATIONS);
                                        message.key = "HintsService_I_CRS_PUBLICATIONS";
                                        continue;
                                    }
                                case 51: {
                                        if (wireType !== 2)
                                            break;
                                        message.ContractService_I_EVM_HOOK_STATES = $root.proto.HookId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ContractService_I_EVM_HOOK_STATES);
                                        message.key = "ContractService_I_EVM_HOOK_STATES";
                                        continue;
                                    }
                                case 52: {
                                        if (wireType !== 2)
                                            break;
                                        message.ContractService_I_EVM_HOOK_STORAGE = $root.com.hedera.hapi.node.state.hooks.EvmHookSlotKey.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ContractService_I_EVM_HOOK_STORAGE);
                                        message.key = "ContractService_I_EVM_HOOK_STORAGE";
                                        continue;
                                    }
                                case 53: {
                                        if (wireType !== 2)
                                            break;
                                        message.AddressBookService_I_ACCOUNT_NODE_REL = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.AddressBookService_I_ACCOUNT_NODE_REL);
                                        message.key = "AddressBookService_I_ACCOUNT_NODE_REL";
                                        continue;
                                    }
                                case 54: {
                                        if (wireType !== 2)
                                            break;
                                        message.HistoryService_I_WRAPS_MESSAGE_HISTORIES = $root.com.hedera.hapi.node.state.history.ConstructionNodeId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HistoryService_I_WRAPS_MESSAGE_HISTORIES);
                                        message.key = "HistoryService_I_WRAPS_MESSAGE_HISTORIES";
                                        continue;
                                    }
                                case 56: {
                                        if (wireType !== 2)
                                            break;
                                        message.AddressBookService_I_REGISTERED_NODES = $root.proto.EntityNumber.decode(reader, reader.uint32(), $undefined, _depth + 1, message.AddressBookService_I_REGISTERED_NODES);
                                        message.key = "AddressBookService_I_REGISTERED_NODES";
                                        continue;
                                    }
                                case 126: {
                                        if (wireType !== 0)
                                            break;
                                        message.RecordCache_I_TRANSACTION_RECEIPTS = reader.uint64();
                                        message.key = "RecordCache_I_TRANSACTION_RECEIPTS";
                                        continue;
                                    }
                                case 10001: {
                                        if (wireType !== 0)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_150 = reader.uint64();
                                        message.key = "FileService_I_UPGRADE_DATA_150";
                                        continue;
                                    }
                                case 10002: {
                                        if (wireType !== 0)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_151 = reader.uint64();
                                        message.key = "FileService_I_UPGRADE_DATA_151";
                                        continue;
                                    }
                                case 10003: {
                                        if (wireType !== 0)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_152 = reader.uint64();
                                        message.key = "FileService_I_UPGRADE_DATA_152";
                                        continue;
                                    }
                                case 10004: {
                                        if (wireType !== 0)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_153 = reader.uint64();
                                        message.key = "FileService_I_UPGRADE_DATA_153";
                                        continue;
                                    }
                                case 10005: {
                                        if (wireType !== 0)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_154 = reader.uint64();
                                        message.key = "FileService_I_UPGRADE_DATA_154";
                                        continue;
                                    }
                                case 10006: {
                                        if (wireType !== 0)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_155 = reader.uint64();
                                        message.key = "FileService_I_UPGRADE_DATA_155";
                                        continue;
                                    }
                                case 10007: {
                                        if (wireType !== 0)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_156 = reader.uint64();
                                        message.key = "FileService_I_UPGRADE_DATA_156";
                                        continue;
                                    }
                                case 10008: {
                                        if (wireType !== 0)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_157 = reader.uint64();
                                        message.key = "FileService_I_UPGRADE_DATA_157";
                                        continue;
                                    }
                                case 10009: {
                                        if (wireType !== 0)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_158 = reader.uint64();
                                        message.key = "FileService_I_UPGRADE_DATA_158";
                                        continue;
                                    }
                                case 10010: {
                                        if (wireType !== 0)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_159 = reader.uint64();
                                        message.key = "FileService_I_UPGRADE_DATA_159";
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a StateKey message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.state.StateKey & com.hedera.hapi.platform.state.StateKey.$Shape} StateKey
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        StateKey.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a StateKey message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        StateKey.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            let properties = {};
                            if (message.singleton != null && $Object.hasOwnProperty.call(message, "singleton")) {
                                properties.key = 1;
                                if (typeof message.singleton !== "number" || (message.singleton | 0) !== message.singleton)
                                    return "singleton: enum value expected";
                            }
                            if (message.TokenService_I_ACCOUNTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_ACCOUNTS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.AccountID.verify(message.TokenService_I_ACCOUNTS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_ACCOUNTS." + error;
                                }
                            }
                            if (message.TokenService_I_ALIASES != null && $Object.hasOwnProperty.call(message, "TokenService_I_ALIASES")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.TokenService_I_ALIASES, _depth + 1);
                                    if (error)
                                        return "TokenService_I_ALIASES." + error;
                                }
                            }
                            if (message.ContractService_I_STORAGE != null && $Object.hasOwnProperty.call(message, "ContractService_I_STORAGE")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.SlotKey.verify(message.ContractService_I_STORAGE, _depth + 1);
                                    if (error)
                                        return "ContractService_I_STORAGE." + error;
                                }
                            }
                            if (message.ContractService_I_BYTECODE != null && $Object.hasOwnProperty.call(message, "ContractService_I_BYTECODE")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.ContractID.verify(message.ContractService_I_BYTECODE, _depth + 1);
                                    if (error)
                                        return "ContractService_I_BYTECODE." + error;
                                }
                            }
                            if (message.FileService_I_FILES != null && $Object.hasOwnProperty.call(message, "FileService_I_FILES")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.FileID.verify(message.FileService_I_FILES, _depth + 1);
                                    if (error)
                                        return "FileService_I_FILES." + error;
                                }
                            }
                            if (message.TokenService_I_TOKENS != null && $Object.hasOwnProperty.call(message, "TokenService_I_TOKENS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.TokenID.verify(message.TokenService_I_TOKENS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_TOKENS." + error;
                                }
                            }
                            if (message.TokenService_I_NFTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_NFTS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.NftID.verify(message.TokenService_I_NFTS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_NFTS." + error;
                                }
                            }
                            if (message.TokenService_I_TOKEN_RELS != null && $Object.hasOwnProperty.call(message, "TokenService_I_TOKEN_RELS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.EntityIDPair.verify(message.TokenService_I_TOKEN_RELS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_TOKEN_RELS." + error;
                                }
                            }
                            if (message.TokenService_I_STAKING_INFOS != null && $Object.hasOwnProperty.call(message, "TokenService_I_STAKING_INFOS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.EntityNumber.verify(message.TokenService_I_STAKING_INFOS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_STAKING_INFOS." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULES_BY_ID != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_ID")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.ScheduleID.verify(message.ScheduleService_I_SCHEDULES_BY_ID, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULES_BY_ID." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.ProtoLong.verify(message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULES_BY_EQUALITY != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_EQUALITY")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.ScheduleService_I_SCHEDULES_BY_EQUALITY, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULES_BY_EQUALITY." + error;
                                }
                            }
                            if (message.AddressBookService_I_NODES != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_NODES")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.EntityNumber.verify(message.AddressBookService_I_NODES, _depth + 1);
                                    if (error)
                                        return "AddressBookService_I_NODES." + error;
                                }
                            }
                            if (message.ConsensusService_I_TOPICS != null && $Object.hasOwnProperty.call(message, "ConsensusService_I_TOPICS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.TopicID.verify(message.ConsensusService_I_TOPICS, _depth + 1);
                                    if (error)
                                        return "ConsensusService_I_TOPICS." + error;
                                }
                            }
                            if (message.TokenService_I_PENDING_AIRDROPS != null && $Object.hasOwnProperty.call(message, "TokenService_I_PENDING_AIRDROPS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.PendingAirdropId.verify(message.TokenService_I_PENDING_AIRDROPS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_PENDING_AIRDROPS." + error;
                                }
                            }
                            if (message.RosterService_I_ROSTERS != null && $Object.hasOwnProperty.call(message, "RosterService_I_ROSTERS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.RosterService_I_ROSTERS, _depth + 1);
                                    if (error)
                                        return "RosterService_I_ROSTERS." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULED_COUNTS != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_COUNTS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.TimestampSeconds.verify(message.ScheduleService_I_SCHEDULED_COUNTS, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULED_COUNTS." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY." + error;
                                }
                            }
                            if (message.TssBaseService_I_TSS_MESSAGES != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_MESSAGES")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.tss.TssMessageMapKey.verify(message.TssBaseService_I_TSS_MESSAGES, _depth + 1);
                                    if (error)
                                        return "TssBaseService_I_TSS_MESSAGES." + error;
                                }
                            }
                            if (message.TssBaseService_I_TSS_VOTES != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_VOTES")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.tss.TssVoteMapKey.verify(message.TssBaseService_I_TSS_VOTES, _depth + 1);
                                    if (error)
                                        return "TssBaseService_I_TSS_VOTES." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULED_ORDERS != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_ORDERS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.ScheduledOrder.verify(message.ScheduleService_I_SCHEDULED_ORDERS, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULED_ORDERS." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULED_USAGES != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_USAGES")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.TimestampSeconds.verify(message.ScheduleService_I_SCHEDULED_USAGES, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULED_USAGES." + error;
                                }
                            }
                            if (message.TssBaseService_I_TSS_ENCRYPTION_KEYS != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_ENCRYPTION_KEYS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.EntityNumber.verify(message.TssBaseService_I_TSS_ENCRYPTION_KEYS, _depth + 1);
                                    if (error)
                                        return "TssBaseService_I_TSS_ENCRYPTION_KEYS." + error;
                                }
                            }
                            if (message.HintsService_I_HINTS_KEY_SETS != null && $Object.hasOwnProperty.call(message, "HintsService_I_HINTS_KEY_SETS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.hints.HintsPartyId.verify(message.HintsService_I_HINTS_KEY_SETS, _depth + 1);
                                    if (error)
                                        return "HintsService_I_HINTS_KEY_SETS." + error;
                                }
                            }
                            if (message.HintsService_I_PREPROCESSING_VOTES != null && $Object.hasOwnProperty.call(message, "HintsService_I_PREPROCESSING_VOTES")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.hints.PreprocessingVoteId.verify(message.HintsService_I_PREPROCESSING_VOTES, _depth + 1);
                                    if (error)
                                        return "HintsService_I_PREPROCESSING_VOTES." + error;
                                }
                            }
                            if (message.HistoryService_I_PROOF_KEY_SETS != null && $Object.hasOwnProperty.call(message, "HistoryService_I_PROOF_KEY_SETS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.com.hedera.hapi.platform.state.NodeId.verify(message.HistoryService_I_PROOF_KEY_SETS, _depth + 1);
                                    if (error)
                                        return "HistoryService_I_PROOF_KEY_SETS." + error;
                                }
                            }
                            if (message.HistoryService_I_HISTORY_SIGNATURES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_HISTORY_SIGNATURES")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.history.ConstructionNodeId.verify(message.HistoryService_I_HISTORY_SIGNATURES, _depth + 1);
                                    if (error)
                                        return "HistoryService_I_HISTORY_SIGNATURES." + error;
                                }
                            }
                            if (message.HistoryService_I_PROOF_VOTES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_PROOF_VOTES")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.history.ConstructionNodeId.verify(message.HistoryService_I_PROOF_VOTES, _depth + 1);
                                    if (error)
                                        return "HistoryService_I_PROOF_VOTES." + error;
                                }
                            }
                            if (message.HintsService_I_CRS_PUBLICATIONS != null && $Object.hasOwnProperty.call(message, "HintsService_I_CRS_PUBLICATIONS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.com.hedera.hapi.platform.state.NodeId.verify(message.HintsService_I_CRS_PUBLICATIONS, _depth + 1);
                                    if (error)
                                        return "HintsService_I_CRS_PUBLICATIONS." + error;
                                }
                            }
                            if (message.ContractService_I_EVM_HOOK_STATES != null && $Object.hasOwnProperty.call(message, "ContractService_I_EVM_HOOK_STATES")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.HookId.verify(message.ContractService_I_EVM_HOOK_STATES, _depth + 1);
                                    if (error)
                                        return "ContractService_I_EVM_HOOK_STATES." + error;
                                }
                            }
                            if (message.ContractService_I_EVM_HOOK_STORAGE != null && $Object.hasOwnProperty.call(message, "ContractService_I_EVM_HOOK_STORAGE")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.hooks.EvmHookSlotKey.verify(message.ContractService_I_EVM_HOOK_STORAGE, _depth + 1);
                                    if (error)
                                        return "ContractService_I_EVM_HOOK_STORAGE." + error;
                                }
                            }
                            if (message.AddressBookService_I_ACCOUNT_NODE_REL != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_ACCOUNT_NODE_REL")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.AccountID.verify(message.AddressBookService_I_ACCOUNT_NODE_REL, _depth + 1);
                                    if (error)
                                        return "AddressBookService_I_ACCOUNT_NODE_REL." + error;
                                }
                            }
                            if (message.HistoryService_I_WRAPS_MESSAGE_HISTORIES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_WRAPS_MESSAGE_HISTORIES")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.history.ConstructionNodeId.verify(message.HistoryService_I_WRAPS_MESSAGE_HISTORIES, _depth + 1);
                                    if (error)
                                        return "HistoryService_I_WRAPS_MESSAGE_HISTORIES." + error;
                                }
                            }
                            if (message.AddressBookService_I_REGISTERED_NODES != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_REGISTERED_NODES")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                {
                                    let error = $root.proto.EntityNumber.verify(message.AddressBookService_I_REGISTERED_NODES, _depth + 1);
                                    if (error)
                                        return "AddressBookService_I_REGISTERED_NODES." + error;
                                }
                            }
                            if (message.RecordCache_I_TRANSACTION_RECEIPTS != null && $Object.hasOwnProperty.call(message, "RecordCache_I_TRANSACTION_RECEIPTS")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                if (!$util.isInteger(message.RecordCache_I_TRANSACTION_RECEIPTS) && !(message.RecordCache_I_TRANSACTION_RECEIPTS && $util.isInteger(message.RecordCache_I_TRANSACTION_RECEIPTS.low) && $util.isInteger(message.RecordCache_I_TRANSACTION_RECEIPTS.high)))
                                    return "RecordCache_I_TRANSACTION_RECEIPTS: integer|Long expected";
                            }
                            if (message.FileService_I_UPGRADE_DATA_150 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_150")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                if (!$util.isInteger(message.FileService_I_UPGRADE_DATA_150) && !(message.FileService_I_UPGRADE_DATA_150 && $util.isInteger(message.FileService_I_UPGRADE_DATA_150.low) && $util.isInteger(message.FileService_I_UPGRADE_DATA_150.high)))
                                    return "FileService_I_UPGRADE_DATA_150: integer|Long expected";
                            }
                            if (message.FileService_I_UPGRADE_DATA_151 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_151")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                if (!$util.isInteger(message.FileService_I_UPGRADE_DATA_151) && !(message.FileService_I_UPGRADE_DATA_151 && $util.isInteger(message.FileService_I_UPGRADE_DATA_151.low) && $util.isInteger(message.FileService_I_UPGRADE_DATA_151.high)))
                                    return "FileService_I_UPGRADE_DATA_151: integer|Long expected";
                            }
                            if (message.FileService_I_UPGRADE_DATA_152 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_152")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                if (!$util.isInteger(message.FileService_I_UPGRADE_DATA_152) && !(message.FileService_I_UPGRADE_DATA_152 && $util.isInteger(message.FileService_I_UPGRADE_DATA_152.low) && $util.isInteger(message.FileService_I_UPGRADE_DATA_152.high)))
                                    return "FileService_I_UPGRADE_DATA_152: integer|Long expected";
                            }
                            if (message.FileService_I_UPGRADE_DATA_153 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_153")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                if (!$util.isInteger(message.FileService_I_UPGRADE_DATA_153) && !(message.FileService_I_UPGRADE_DATA_153 && $util.isInteger(message.FileService_I_UPGRADE_DATA_153.low) && $util.isInteger(message.FileService_I_UPGRADE_DATA_153.high)))
                                    return "FileService_I_UPGRADE_DATA_153: integer|Long expected";
                            }
                            if (message.FileService_I_UPGRADE_DATA_154 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_154")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                if (!$util.isInteger(message.FileService_I_UPGRADE_DATA_154) && !(message.FileService_I_UPGRADE_DATA_154 && $util.isInteger(message.FileService_I_UPGRADE_DATA_154.low) && $util.isInteger(message.FileService_I_UPGRADE_DATA_154.high)))
                                    return "FileService_I_UPGRADE_DATA_154: integer|Long expected";
                            }
                            if (message.FileService_I_UPGRADE_DATA_155 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_155")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                if (!$util.isInteger(message.FileService_I_UPGRADE_DATA_155) && !(message.FileService_I_UPGRADE_DATA_155 && $util.isInteger(message.FileService_I_UPGRADE_DATA_155.low) && $util.isInteger(message.FileService_I_UPGRADE_DATA_155.high)))
                                    return "FileService_I_UPGRADE_DATA_155: integer|Long expected";
                            }
                            if (message.FileService_I_UPGRADE_DATA_156 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_156")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                if (!$util.isInteger(message.FileService_I_UPGRADE_DATA_156) && !(message.FileService_I_UPGRADE_DATA_156 && $util.isInteger(message.FileService_I_UPGRADE_DATA_156.low) && $util.isInteger(message.FileService_I_UPGRADE_DATA_156.high)))
                                    return "FileService_I_UPGRADE_DATA_156: integer|Long expected";
                            }
                            if (message.FileService_I_UPGRADE_DATA_157 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_157")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                if (!$util.isInteger(message.FileService_I_UPGRADE_DATA_157) && !(message.FileService_I_UPGRADE_DATA_157 && $util.isInteger(message.FileService_I_UPGRADE_DATA_157.low) && $util.isInteger(message.FileService_I_UPGRADE_DATA_157.high)))
                                    return "FileService_I_UPGRADE_DATA_157: integer|Long expected";
                            }
                            if (message.FileService_I_UPGRADE_DATA_158 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_158")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                if (!$util.isInteger(message.FileService_I_UPGRADE_DATA_158) && !(message.FileService_I_UPGRADE_DATA_158 && $util.isInteger(message.FileService_I_UPGRADE_DATA_158.low) && $util.isInteger(message.FileService_I_UPGRADE_DATA_158.high)))
                                    return "FileService_I_UPGRADE_DATA_158: integer|Long expected";
                            }
                            if (message.FileService_I_UPGRADE_DATA_159 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_159")) {
                                if (properties.key === 1)
                                    return "key: multiple values";
                                properties.key = 1;
                                if (!$util.isInteger(message.FileService_I_UPGRADE_DATA_159) && !(message.FileService_I_UPGRADE_DATA_159 && $util.isInteger(message.FileService_I_UPGRADE_DATA_159.low) && $util.isInteger(message.FileService_I_UPGRADE_DATA_159.high)))
                                    return "FileService_I_UPGRADE_DATA_159: integer|Long expected";
                            }
                            return null;
                        };

                        /**
                         * Creates a StateKey message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.state.StateKey} StateKey
                         */
                        StateKey.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.state.StateKey)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.state.StateKey: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.state.StateKey();
                            switch (object.singleton) {
                            case "UNKNOWN":
                            case 0:
                                message.singleton = 0;
                                break;
                            case "EntityIdService_I_ENTITY_ID":
                            case 1:
                                message.singleton = 1;
                                break;
                            case "TokenService_I_STAKING_NETWORK_REWARDS":
                            case 11:
                                message.singleton = 11;
                                break;
                            case "CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS":
                            case 12:
                                message.singleton = 12;
                                break;
                            case "CongestionThrottleService_I_CONGESTION_LEVEL_STARTS":
                            case 13:
                                message.singleton = 13;
                                break;
                            case "FeeService_I_MIDNIGHT_RATES":
                            case 17:
                                message.singleton = 17;
                                break;
                            case "BlockRecordService_I_RUNNING_HASHES":
                            case 18:
                                message.singleton = 18;
                                break;
                            case "BlockRecordService_I_BLOCKS":
                            case 19:
                                message.singleton = 19;
                                break;
                            case "FreezeService_I_UPGRADE_FILE_HASH":
                            case 22:
                                message.singleton = 22;
                                break;
                            case "FreezeService_I_FREEZE_TIME":
                            case 23:
                                message.singleton = 23;
                                break;
                            case "BlockStreamService_I_BLOCK_STREAM_INFO":
                            case 24:
                                message.singleton = 24;
                                break;
                            case "PlatformStateService_I_PLATFORM_STATE":
                            case 26:
                                message.singleton = 26;
                                break;
                            case "RosterService_I_ROSTER_STATE":
                            case 27:
                                message.singleton = 27;
                                break;
                            case "HintsService_I_ACTIVE_HINTS_CONSTRUCTION":
                            case 38:
                                message.singleton = 38;
                                break;
                            case "HintsService_I_NEXT_HINTS_CONSTRUCTION":
                            case 39:
                                message.singleton = 39;
                                break;
                            case "EntityIdService_I_ENTITY_COUNTS":
                            case 41:
                                message.singleton = 41;
                                break;
                            case "HistoryService_I_LEDGER_ID":
                            case 42:
                                message.singleton = 42;
                                break;
                            case "HistoryService_I_ACTIVE_PROOF_CONSTRUCTION":
                            case 44:
                                message.singleton = 44;
                                break;
                            case "HistoryService_I_NEXT_PROOF_CONSTRUCTION":
                            case 45:
                                message.singleton = 45;
                                break;
                            case "HintsService_I_CRS_STATE":
                            case 48:
                                message.singleton = 48;
                                break;
                            case "TokenService_I_NODE_REWARDS":
                            case 50:
                                message.singleton = 50;
                                break;
                            case "TokenService_I_NODE_PAYMENTS":
                            case 55:
                                message.singleton = 55;
                                break;
                            case "AddressBookService_I_REGISTERED_NODES":
                            case 56:
                                message.singleton = 56;
                                break;
                            case "EntityIdService_I_HIGHEST_NODE_ID":
                            case 57:
                                message.singleton = 57;
                                break;
                            case "HistoryService_I_WRAPS_PROVING_KEY_HASH":
                            case 58:
                                message.singleton = 58;
                                break;
                            case "RecordCache_I_TRANSACTION_RECEIPTS":
                            case 126:
                                message.singleton = 126;
                                break;
                            case "FileService_I_UPGRADE_DATA_150":
                            case 10001:
                                message.singleton = 10001;
                                break;
                            case "FileService_I_UPGRADE_DATA_151":
                            case 10002:
                                message.singleton = 10002;
                                break;
                            case "FileService_I_UPGRADE_DATA_152":
                            case 10003:
                                message.singleton = 10003;
                                break;
                            case "FileService_I_UPGRADE_DATA_153":
                            case 10004:
                                message.singleton = 10004;
                                break;
                            case "FileService_I_UPGRADE_DATA_154":
                            case 10005:
                                message.singleton = 10005;
                                break;
                            case "FileService_I_UPGRADE_DATA_155":
                            case 10006:
                                message.singleton = 10006;
                                break;
                            case "FileService_I_UPGRADE_DATA_156":
                            case 10007:
                                message.singleton = 10007;
                                break;
                            case "FileService_I_UPGRADE_DATA_157":
                            case 10008:
                                message.singleton = 10008;
                                break;
                            case "FileService_I_UPGRADE_DATA_158":
                            case 10009:
                                message.singleton = 10009;
                                break;
                            case "FileService_I_UPGRADE_DATA_159":
                            case 10010:
                                message.singleton = 10010;
                                break;
                            default:
                                if (typeof object.singleton === "number" && (object.singleton | 0) === object.singleton)
                                    message.singleton = object.singleton;
                            }
                            if (object.TokenService_I_ACCOUNTS != null) {
                                if (!$util.isObject(object.TokenService_I_ACCOUNTS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.TokenService_I_ACCOUNTS: object expected");
                                message.TokenService_I_ACCOUNTS = $root.proto.AccountID.fromObject(object.TokenService_I_ACCOUNTS, _depth + 1);
                            }
                            if (object.TokenService_I_ALIASES != null) {
                                if (!$util.isObject(object.TokenService_I_ALIASES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.TokenService_I_ALIASES: object expected");
                                message.TokenService_I_ALIASES = $root.proto.ProtoBytes.fromObject(object.TokenService_I_ALIASES, _depth + 1);
                            }
                            if (object.ContractService_I_STORAGE != null) {
                                if (!$util.isObject(object.ContractService_I_STORAGE))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.ContractService_I_STORAGE: object expected");
                                message.ContractService_I_STORAGE = $root.proto.SlotKey.fromObject(object.ContractService_I_STORAGE, _depth + 1);
                            }
                            if (object.ContractService_I_BYTECODE != null) {
                                if (!$util.isObject(object.ContractService_I_BYTECODE))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.ContractService_I_BYTECODE: object expected");
                                message.ContractService_I_BYTECODE = $root.proto.ContractID.fromObject(object.ContractService_I_BYTECODE, _depth + 1);
                            }
                            if (object.FileService_I_FILES != null) {
                                if (!$util.isObject(object.FileService_I_FILES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.FileService_I_FILES: object expected");
                                message.FileService_I_FILES = $root.proto.FileID.fromObject(object.FileService_I_FILES, _depth + 1);
                            }
                            if (object.TokenService_I_TOKENS != null) {
                                if (!$util.isObject(object.TokenService_I_TOKENS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.TokenService_I_TOKENS: object expected");
                                message.TokenService_I_TOKENS = $root.proto.TokenID.fromObject(object.TokenService_I_TOKENS, _depth + 1);
                            }
                            if (object.TokenService_I_NFTS != null) {
                                if (!$util.isObject(object.TokenService_I_NFTS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.TokenService_I_NFTS: object expected");
                                message.TokenService_I_NFTS = $root.proto.NftID.fromObject(object.TokenService_I_NFTS, _depth + 1);
                            }
                            if (object.TokenService_I_TOKEN_RELS != null) {
                                if (!$util.isObject(object.TokenService_I_TOKEN_RELS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.TokenService_I_TOKEN_RELS: object expected");
                                message.TokenService_I_TOKEN_RELS = $root.proto.EntityIDPair.fromObject(object.TokenService_I_TOKEN_RELS, _depth + 1);
                            }
                            if (object.TokenService_I_STAKING_INFOS != null) {
                                if (!$util.isObject(object.TokenService_I_STAKING_INFOS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.TokenService_I_STAKING_INFOS: object expected");
                                message.TokenService_I_STAKING_INFOS = $root.proto.EntityNumber.fromObject(object.TokenService_I_STAKING_INFOS, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULES_BY_ID != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULES_BY_ID))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.ScheduleService_I_SCHEDULES_BY_ID: object expected");
                                message.ScheduleService_I_SCHEDULES_BY_ID = $root.proto.ScheduleID.fromObject(object.ScheduleService_I_SCHEDULES_BY_ID, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC: object expected");
                                message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC = $root.proto.ProtoLong.fromObject(object.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULES_BY_EQUALITY != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULES_BY_EQUALITY))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.ScheduleService_I_SCHEDULES_BY_EQUALITY: object expected");
                                message.ScheduleService_I_SCHEDULES_BY_EQUALITY = $root.proto.ProtoBytes.fromObject(object.ScheduleService_I_SCHEDULES_BY_EQUALITY, _depth + 1);
                            }
                            if (object.AddressBookService_I_NODES != null) {
                                if (!$util.isObject(object.AddressBookService_I_NODES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.AddressBookService_I_NODES: object expected");
                                message.AddressBookService_I_NODES = $root.proto.EntityNumber.fromObject(object.AddressBookService_I_NODES, _depth + 1);
                            }
                            if (object.ConsensusService_I_TOPICS != null) {
                                if (!$util.isObject(object.ConsensusService_I_TOPICS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.ConsensusService_I_TOPICS: object expected");
                                message.ConsensusService_I_TOPICS = $root.proto.TopicID.fromObject(object.ConsensusService_I_TOPICS, _depth + 1);
                            }
                            if (object.TokenService_I_PENDING_AIRDROPS != null) {
                                if (!$util.isObject(object.TokenService_I_PENDING_AIRDROPS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.TokenService_I_PENDING_AIRDROPS: object expected");
                                message.TokenService_I_PENDING_AIRDROPS = $root.proto.PendingAirdropId.fromObject(object.TokenService_I_PENDING_AIRDROPS, _depth + 1);
                            }
                            if (object.RosterService_I_ROSTERS != null) {
                                if (!$util.isObject(object.RosterService_I_ROSTERS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.RosterService_I_ROSTERS: object expected");
                                message.RosterService_I_ROSTERS = $root.proto.ProtoBytes.fromObject(object.RosterService_I_ROSTERS, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULED_COUNTS != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULED_COUNTS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.ScheduleService_I_SCHEDULED_COUNTS: object expected");
                                message.ScheduleService_I_SCHEDULED_COUNTS = $root.proto.TimestampSeconds.fromObject(object.ScheduleService_I_SCHEDULED_COUNTS, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY: object expected");
                                message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY = $root.proto.ProtoBytes.fromObject(object.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY, _depth + 1);
                            }
                            if (object.TssBaseService_I_TSS_MESSAGES != null) {
                                if (!$util.isObject(object.TssBaseService_I_TSS_MESSAGES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.TssBaseService_I_TSS_MESSAGES: object expected");
                                message.TssBaseService_I_TSS_MESSAGES = $root.com.hedera.hapi.node.state.tss.TssMessageMapKey.fromObject(object.TssBaseService_I_TSS_MESSAGES, _depth + 1);
                            }
                            if (object.TssBaseService_I_TSS_VOTES != null) {
                                if (!$util.isObject(object.TssBaseService_I_TSS_VOTES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.TssBaseService_I_TSS_VOTES: object expected");
                                message.TssBaseService_I_TSS_VOTES = $root.com.hedera.hapi.node.state.tss.TssVoteMapKey.fromObject(object.TssBaseService_I_TSS_VOTES, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULED_ORDERS != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULED_ORDERS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.ScheduleService_I_SCHEDULED_ORDERS: object expected");
                                message.ScheduleService_I_SCHEDULED_ORDERS = $root.proto.ScheduledOrder.fromObject(object.ScheduleService_I_SCHEDULED_ORDERS, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULED_USAGES != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULED_USAGES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.ScheduleService_I_SCHEDULED_USAGES: object expected");
                                message.ScheduleService_I_SCHEDULED_USAGES = $root.proto.TimestampSeconds.fromObject(object.ScheduleService_I_SCHEDULED_USAGES, _depth + 1);
                            }
                            if (object.TssBaseService_I_TSS_ENCRYPTION_KEYS != null) {
                                if (!$util.isObject(object.TssBaseService_I_TSS_ENCRYPTION_KEYS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.TssBaseService_I_TSS_ENCRYPTION_KEYS: object expected");
                                message.TssBaseService_I_TSS_ENCRYPTION_KEYS = $root.proto.EntityNumber.fromObject(object.TssBaseService_I_TSS_ENCRYPTION_KEYS, _depth + 1);
                            }
                            if (object.HintsService_I_HINTS_KEY_SETS != null) {
                                if (!$util.isObject(object.HintsService_I_HINTS_KEY_SETS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.HintsService_I_HINTS_KEY_SETS: object expected");
                                message.HintsService_I_HINTS_KEY_SETS = $root.com.hedera.hapi.node.state.hints.HintsPartyId.fromObject(object.HintsService_I_HINTS_KEY_SETS, _depth + 1);
                            }
                            if (object.HintsService_I_PREPROCESSING_VOTES != null) {
                                if (!$util.isObject(object.HintsService_I_PREPROCESSING_VOTES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.HintsService_I_PREPROCESSING_VOTES: object expected");
                                message.HintsService_I_PREPROCESSING_VOTES = $root.com.hedera.hapi.node.state.hints.PreprocessingVoteId.fromObject(object.HintsService_I_PREPROCESSING_VOTES, _depth + 1);
                            }
                            if (object.HistoryService_I_PROOF_KEY_SETS != null) {
                                if (!$util.isObject(object.HistoryService_I_PROOF_KEY_SETS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.HistoryService_I_PROOF_KEY_SETS: object expected");
                                message.HistoryService_I_PROOF_KEY_SETS = $root.com.hedera.hapi.platform.state.NodeId.fromObject(object.HistoryService_I_PROOF_KEY_SETS, _depth + 1);
                            }
                            if (object.HistoryService_I_HISTORY_SIGNATURES != null) {
                                if (!$util.isObject(object.HistoryService_I_HISTORY_SIGNATURES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.HistoryService_I_HISTORY_SIGNATURES: object expected");
                                message.HistoryService_I_HISTORY_SIGNATURES = $root.com.hedera.hapi.node.state.history.ConstructionNodeId.fromObject(object.HistoryService_I_HISTORY_SIGNATURES, _depth + 1);
                            }
                            if (object.HistoryService_I_PROOF_VOTES != null) {
                                if (!$util.isObject(object.HistoryService_I_PROOF_VOTES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.HistoryService_I_PROOF_VOTES: object expected");
                                message.HistoryService_I_PROOF_VOTES = $root.com.hedera.hapi.node.state.history.ConstructionNodeId.fromObject(object.HistoryService_I_PROOF_VOTES, _depth + 1);
                            }
                            if (object.HintsService_I_CRS_PUBLICATIONS != null) {
                                if (!$util.isObject(object.HintsService_I_CRS_PUBLICATIONS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.HintsService_I_CRS_PUBLICATIONS: object expected");
                                message.HintsService_I_CRS_PUBLICATIONS = $root.com.hedera.hapi.platform.state.NodeId.fromObject(object.HintsService_I_CRS_PUBLICATIONS, _depth + 1);
                            }
                            if (object.ContractService_I_EVM_HOOK_STATES != null) {
                                if (!$util.isObject(object.ContractService_I_EVM_HOOK_STATES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.ContractService_I_EVM_HOOK_STATES: object expected");
                                message.ContractService_I_EVM_HOOK_STATES = $root.proto.HookId.fromObject(object.ContractService_I_EVM_HOOK_STATES, _depth + 1);
                            }
                            if (object.ContractService_I_EVM_HOOK_STORAGE != null) {
                                if (!$util.isObject(object.ContractService_I_EVM_HOOK_STORAGE))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.ContractService_I_EVM_HOOK_STORAGE: object expected");
                                message.ContractService_I_EVM_HOOK_STORAGE = $root.com.hedera.hapi.node.state.hooks.EvmHookSlotKey.fromObject(object.ContractService_I_EVM_HOOK_STORAGE, _depth + 1);
                            }
                            if (object.AddressBookService_I_ACCOUNT_NODE_REL != null) {
                                if (!$util.isObject(object.AddressBookService_I_ACCOUNT_NODE_REL))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.AddressBookService_I_ACCOUNT_NODE_REL: object expected");
                                message.AddressBookService_I_ACCOUNT_NODE_REL = $root.proto.AccountID.fromObject(object.AddressBookService_I_ACCOUNT_NODE_REL, _depth + 1);
                            }
                            if (object.HistoryService_I_WRAPS_MESSAGE_HISTORIES != null) {
                                if (!$util.isObject(object.HistoryService_I_WRAPS_MESSAGE_HISTORIES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.HistoryService_I_WRAPS_MESSAGE_HISTORIES: object expected");
                                message.HistoryService_I_WRAPS_MESSAGE_HISTORIES = $root.com.hedera.hapi.node.state.history.ConstructionNodeId.fromObject(object.HistoryService_I_WRAPS_MESSAGE_HISTORIES, _depth + 1);
                            }
                            if (object.AddressBookService_I_REGISTERED_NODES != null) {
                                if (!$util.isObject(object.AddressBookService_I_REGISTERED_NODES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateKey.AddressBookService_I_REGISTERED_NODES: object expected");
                                message.AddressBookService_I_REGISTERED_NODES = $root.proto.EntityNumber.fromObject(object.AddressBookService_I_REGISTERED_NODES, _depth + 1);
                            }
                            if (object.RecordCache_I_TRANSACTION_RECEIPTS != null)
                                if ($util.Long)
                                    message.RecordCache_I_TRANSACTION_RECEIPTS = $util.Long.fromValue(object.RecordCache_I_TRANSACTION_RECEIPTS, true);
                                else if (typeof object.RecordCache_I_TRANSACTION_RECEIPTS === "string")
                                    message.RecordCache_I_TRANSACTION_RECEIPTS = $parseInt(object.RecordCache_I_TRANSACTION_RECEIPTS, 10);
                                else if (typeof object.RecordCache_I_TRANSACTION_RECEIPTS === "number")
                                    message.RecordCache_I_TRANSACTION_RECEIPTS = object.RecordCache_I_TRANSACTION_RECEIPTS;
                                else if (typeof object.RecordCache_I_TRANSACTION_RECEIPTS === "object")
                                    message.RecordCache_I_TRANSACTION_RECEIPTS = new $util.LongBits(object.RecordCache_I_TRANSACTION_RECEIPTS.low >>> 0, object.RecordCache_I_TRANSACTION_RECEIPTS.high >>> 0).toNumber(true);
                            if (object.FileService_I_UPGRADE_DATA_150 != null)
                                if ($util.Long)
                                    message.FileService_I_UPGRADE_DATA_150 = $util.Long.fromValue(object.FileService_I_UPGRADE_DATA_150, true);
                                else if (typeof object.FileService_I_UPGRADE_DATA_150 === "string")
                                    message.FileService_I_UPGRADE_DATA_150 = $parseInt(object.FileService_I_UPGRADE_DATA_150, 10);
                                else if (typeof object.FileService_I_UPGRADE_DATA_150 === "number")
                                    message.FileService_I_UPGRADE_DATA_150 = object.FileService_I_UPGRADE_DATA_150;
                                else if (typeof object.FileService_I_UPGRADE_DATA_150 === "object")
                                    message.FileService_I_UPGRADE_DATA_150 = new $util.LongBits(object.FileService_I_UPGRADE_DATA_150.low >>> 0, object.FileService_I_UPGRADE_DATA_150.high >>> 0).toNumber(true);
                            if (object.FileService_I_UPGRADE_DATA_151 != null)
                                if ($util.Long)
                                    message.FileService_I_UPGRADE_DATA_151 = $util.Long.fromValue(object.FileService_I_UPGRADE_DATA_151, true);
                                else if (typeof object.FileService_I_UPGRADE_DATA_151 === "string")
                                    message.FileService_I_UPGRADE_DATA_151 = $parseInt(object.FileService_I_UPGRADE_DATA_151, 10);
                                else if (typeof object.FileService_I_UPGRADE_DATA_151 === "number")
                                    message.FileService_I_UPGRADE_DATA_151 = object.FileService_I_UPGRADE_DATA_151;
                                else if (typeof object.FileService_I_UPGRADE_DATA_151 === "object")
                                    message.FileService_I_UPGRADE_DATA_151 = new $util.LongBits(object.FileService_I_UPGRADE_DATA_151.low >>> 0, object.FileService_I_UPGRADE_DATA_151.high >>> 0).toNumber(true);
                            if (object.FileService_I_UPGRADE_DATA_152 != null)
                                if ($util.Long)
                                    message.FileService_I_UPGRADE_DATA_152 = $util.Long.fromValue(object.FileService_I_UPGRADE_DATA_152, true);
                                else if (typeof object.FileService_I_UPGRADE_DATA_152 === "string")
                                    message.FileService_I_UPGRADE_DATA_152 = $parseInt(object.FileService_I_UPGRADE_DATA_152, 10);
                                else if (typeof object.FileService_I_UPGRADE_DATA_152 === "number")
                                    message.FileService_I_UPGRADE_DATA_152 = object.FileService_I_UPGRADE_DATA_152;
                                else if (typeof object.FileService_I_UPGRADE_DATA_152 === "object")
                                    message.FileService_I_UPGRADE_DATA_152 = new $util.LongBits(object.FileService_I_UPGRADE_DATA_152.low >>> 0, object.FileService_I_UPGRADE_DATA_152.high >>> 0).toNumber(true);
                            if (object.FileService_I_UPGRADE_DATA_153 != null)
                                if ($util.Long)
                                    message.FileService_I_UPGRADE_DATA_153 = $util.Long.fromValue(object.FileService_I_UPGRADE_DATA_153, true);
                                else if (typeof object.FileService_I_UPGRADE_DATA_153 === "string")
                                    message.FileService_I_UPGRADE_DATA_153 = $parseInt(object.FileService_I_UPGRADE_DATA_153, 10);
                                else if (typeof object.FileService_I_UPGRADE_DATA_153 === "number")
                                    message.FileService_I_UPGRADE_DATA_153 = object.FileService_I_UPGRADE_DATA_153;
                                else if (typeof object.FileService_I_UPGRADE_DATA_153 === "object")
                                    message.FileService_I_UPGRADE_DATA_153 = new $util.LongBits(object.FileService_I_UPGRADE_DATA_153.low >>> 0, object.FileService_I_UPGRADE_DATA_153.high >>> 0).toNumber(true);
                            if (object.FileService_I_UPGRADE_DATA_154 != null)
                                if ($util.Long)
                                    message.FileService_I_UPGRADE_DATA_154 = $util.Long.fromValue(object.FileService_I_UPGRADE_DATA_154, true);
                                else if (typeof object.FileService_I_UPGRADE_DATA_154 === "string")
                                    message.FileService_I_UPGRADE_DATA_154 = $parseInt(object.FileService_I_UPGRADE_DATA_154, 10);
                                else if (typeof object.FileService_I_UPGRADE_DATA_154 === "number")
                                    message.FileService_I_UPGRADE_DATA_154 = object.FileService_I_UPGRADE_DATA_154;
                                else if (typeof object.FileService_I_UPGRADE_DATA_154 === "object")
                                    message.FileService_I_UPGRADE_DATA_154 = new $util.LongBits(object.FileService_I_UPGRADE_DATA_154.low >>> 0, object.FileService_I_UPGRADE_DATA_154.high >>> 0).toNumber(true);
                            if (object.FileService_I_UPGRADE_DATA_155 != null)
                                if ($util.Long)
                                    message.FileService_I_UPGRADE_DATA_155 = $util.Long.fromValue(object.FileService_I_UPGRADE_DATA_155, true);
                                else if (typeof object.FileService_I_UPGRADE_DATA_155 === "string")
                                    message.FileService_I_UPGRADE_DATA_155 = $parseInt(object.FileService_I_UPGRADE_DATA_155, 10);
                                else if (typeof object.FileService_I_UPGRADE_DATA_155 === "number")
                                    message.FileService_I_UPGRADE_DATA_155 = object.FileService_I_UPGRADE_DATA_155;
                                else if (typeof object.FileService_I_UPGRADE_DATA_155 === "object")
                                    message.FileService_I_UPGRADE_DATA_155 = new $util.LongBits(object.FileService_I_UPGRADE_DATA_155.low >>> 0, object.FileService_I_UPGRADE_DATA_155.high >>> 0).toNumber(true);
                            if (object.FileService_I_UPGRADE_DATA_156 != null)
                                if ($util.Long)
                                    message.FileService_I_UPGRADE_DATA_156 = $util.Long.fromValue(object.FileService_I_UPGRADE_DATA_156, true);
                                else if (typeof object.FileService_I_UPGRADE_DATA_156 === "string")
                                    message.FileService_I_UPGRADE_DATA_156 = $parseInt(object.FileService_I_UPGRADE_DATA_156, 10);
                                else if (typeof object.FileService_I_UPGRADE_DATA_156 === "number")
                                    message.FileService_I_UPGRADE_DATA_156 = object.FileService_I_UPGRADE_DATA_156;
                                else if (typeof object.FileService_I_UPGRADE_DATA_156 === "object")
                                    message.FileService_I_UPGRADE_DATA_156 = new $util.LongBits(object.FileService_I_UPGRADE_DATA_156.low >>> 0, object.FileService_I_UPGRADE_DATA_156.high >>> 0).toNumber(true);
                            if (object.FileService_I_UPGRADE_DATA_157 != null)
                                if ($util.Long)
                                    message.FileService_I_UPGRADE_DATA_157 = $util.Long.fromValue(object.FileService_I_UPGRADE_DATA_157, true);
                                else if (typeof object.FileService_I_UPGRADE_DATA_157 === "string")
                                    message.FileService_I_UPGRADE_DATA_157 = $parseInt(object.FileService_I_UPGRADE_DATA_157, 10);
                                else if (typeof object.FileService_I_UPGRADE_DATA_157 === "number")
                                    message.FileService_I_UPGRADE_DATA_157 = object.FileService_I_UPGRADE_DATA_157;
                                else if (typeof object.FileService_I_UPGRADE_DATA_157 === "object")
                                    message.FileService_I_UPGRADE_DATA_157 = new $util.LongBits(object.FileService_I_UPGRADE_DATA_157.low >>> 0, object.FileService_I_UPGRADE_DATA_157.high >>> 0).toNumber(true);
                            if (object.FileService_I_UPGRADE_DATA_158 != null)
                                if ($util.Long)
                                    message.FileService_I_UPGRADE_DATA_158 = $util.Long.fromValue(object.FileService_I_UPGRADE_DATA_158, true);
                                else if (typeof object.FileService_I_UPGRADE_DATA_158 === "string")
                                    message.FileService_I_UPGRADE_DATA_158 = $parseInt(object.FileService_I_UPGRADE_DATA_158, 10);
                                else if (typeof object.FileService_I_UPGRADE_DATA_158 === "number")
                                    message.FileService_I_UPGRADE_DATA_158 = object.FileService_I_UPGRADE_DATA_158;
                                else if (typeof object.FileService_I_UPGRADE_DATA_158 === "object")
                                    message.FileService_I_UPGRADE_DATA_158 = new $util.LongBits(object.FileService_I_UPGRADE_DATA_158.low >>> 0, object.FileService_I_UPGRADE_DATA_158.high >>> 0).toNumber(true);
                            if (object.FileService_I_UPGRADE_DATA_159 != null)
                                if ($util.Long)
                                    message.FileService_I_UPGRADE_DATA_159 = $util.Long.fromValue(object.FileService_I_UPGRADE_DATA_159, true);
                                else if (typeof object.FileService_I_UPGRADE_DATA_159 === "string")
                                    message.FileService_I_UPGRADE_DATA_159 = $parseInt(object.FileService_I_UPGRADE_DATA_159, 10);
                                else if (typeof object.FileService_I_UPGRADE_DATA_159 === "number")
                                    message.FileService_I_UPGRADE_DATA_159 = object.FileService_I_UPGRADE_DATA_159;
                                else if (typeof object.FileService_I_UPGRADE_DATA_159 === "object")
                                    message.FileService_I_UPGRADE_DATA_159 = new $util.LongBits(object.FileService_I_UPGRADE_DATA_159.low >>> 0, object.FileService_I_UPGRADE_DATA_159.high >>> 0).toNumber(true);
                            return message;
                        };

                        /**
                         * Creates a plain object from a StateKey message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @static
                         * @param {com.hedera.hapi.platform.state.StateKey} message StateKey
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        StateKey.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (message.singleton != null && $Object.hasOwnProperty.call(message, "singleton")) {
                                object.singleton = options.enums === $String ? $root.com.hedera.hapi.platform.state.SingletonType[message.singleton] === $undefined ? message.singleton : $root.com.hedera.hapi.platform.state.SingletonType[message.singleton] : message.singleton;
                                if (options.oneofs)
                                    object.key = "singleton";
                            }
                            if (message.TokenService_I_ACCOUNTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_ACCOUNTS")) {
                                object.TokenService_I_ACCOUNTS = $root.proto.AccountID.toObject(message.TokenService_I_ACCOUNTS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "TokenService_I_ACCOUNTS";
                            }
                            if (message.TokenService_I_ALIASES != null && $Object.hasOwnProperty.call(message, "TokenService_I_ALIASES")) {
                                object.TokenService_I_ALIASES = $root.proto.ProtoBytes.toObject(message.TokenService_I_ALIASES, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "TokenService_I_ALIASES";
                            }
                            if (message.ContractService_I_STORAGE != null && $Object.hasOwnProperty.call(message, "ContractService_I_STORAGE")) {
                                object.ContractService_I_STORAGE = $root.proto.SlotKey.toObject(message.ContractService_I_STORAGE, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "ContractService_I_STORAGE";
                            }
                            if (message.ContractService_I_BYTECODE != null && $Object.hasOwnProperty.call(message, "ContractService_I_BYTECODE")) {
                                object.ContractService_I_BYTECODE = $root.proto.ContractID.toObject(message.ContractService_I_BYTECODE, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "ContractService_I_BYTECODE";
                            }
                            if (message.FileService_I_FILES != null && $Object.hasOwnProperty.call(message, "FileService_I_FILES")) {
                                object.FileService_I_FILES = $root.proto.FileID.toObject(message.FileService_I_FILES, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "FileService_I_FILES";
                            }
                            if (message.TokenService_I_TOKENS != null && $Object.hasOwnProperty.call(message, "TokenService_I_TOKENS")) {
                                object.TokenService_I_TOKENS = $root.proto.TokenID.toObject(message.TokenService_I_TOKENS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "TokenService_I_TOKENS";
                            }
                            if (message.TokenService_I_NFTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_NFTS")) {
                                object.TokenService_I_NFTS = $root.proto.NftID.toObject(message.TokenService_I_NFTS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "TokenService_I_NFTS";
                            }
                            if (message.TokenService_I_TOKEN_RELS != null && $Object.hasOwnProperty.call(message, "TokenService_I_TOKEN_RELS")) {
                                object.TokenService_I_TOKEN_RELS = $root.proto.EntityIDPair.toObject(message.TokenService_I_TOKEN_RELS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "TokenService_I_TOKEN_RELS";
                            }
                            if (message.TokenService_I_STAKING_INFOS != null && $Object.hasOwnProperty.call(message, "TokenService_I_STAKING_INFOS")) {
                                object.TokenService_I_STAKING_INFOS = $root.proto.EntityNumber.toObject(message.TokenService_I_STAKING_INFOS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "TokenService_I_STAKING_INFOS";
                            }
                            if (message.ScheduleService_I_SCHEDULES_BY_ID != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_ID")) {
                                object.ScheduleService_I_SCHEDULES_BY_ID = $root.proto.ScheduleID.toObject(message.ScheduleService_I_SCHEDULES_BY_ID, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "ScheduleService_I_SCHEDULES_BY_ID";
                            }
                            if (message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC")) {
                                object.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC = $root.proto.ProtoLong.toObject(message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC";
                            }
                            if (message.ScheduleService_I_SCHEDULES_BY_EQUALITY != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_EQUALITY")) {
                                object.ScheduleService_I_SCHEDULES_BY_EQUALITY = $root.proto.ProtoBytes.toObject(message.ScheduleService_I_SCHEDULES_BY_EQUALITY, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "ScheduleService_I_SCHEDULES_BY_EQUALITY";
                            }
                            if (message.AddressBookService_I_NODES != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_NODES")) {
                                object.AddressBookService_I_NODES = $root.proto.EntityNumber.toObject(message.AddressBookService_I_NODES, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "AddressBookService_I_NODES";
                            }
                            if (message.ConsensusService_I_TOPICS != null && $Object.hasOwnProperty.call(message, "ConsensusService_I_TOPICS")) {
                                object.ConsensusService_I_TOPICS = $root.proto.TopicID.toObject(message.ConsensusService_I_TOPICS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "ConsensusService_I_TOPICS";
                            }
                            if (message.TokenService_I_PENDING_AIRDROPS != null && $Object.hasOwnProperty.call(message, "TokenService_I_PENDING_AIRDROPS")) {
                                object.TokenService_I_PENDING_AIRDROPS = $root.proto.PendingAirdropId.toObject(message.TokenService_I_PENDING_AIRDROPS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "TokenService_I_PENDING_AIRDROPS";
                            }
                            if (message.RosterService_I_ROSTERS != null && $Object.hasOwnProperty.call(message, "RosterService_I_ROSTERS")) {
                                object.RosterService_I_ROSTERS = $root.proto.ProtoBytes.toObject(message.RosterService_I_ROSTERS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "RosterService_I_ROSTERS";
                            }
                            if (message.ScheduleService_I_SCHEDULED_COUNTS != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_COUNTS")) {
                                object.ScheduleService_I_SCHEDULED_COUNTS = $root.proto.TimestampSeconds.toObject(message.ScheduleService_I_SCHEDULED_COUNTS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "ScheduleService_I_SCHEDULED_COUNTS";
                            }
                            if (message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY")) {
                                object.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY = $root.proto.ProtoBytes.toObject(message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY";
                            }
                            if (message.TssBaseService_I_TSS_MESSAGES != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_MESSAGES")) {
                                object.TssBaseService_I_TSS_MESSAGES = $root.com.hedera.hapi.node.state.tss.TssMessageMapKey.toObject(message.TssBaseService_I_TSS_MESSAGES, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "TssBaseService_I_TSS_MESSAGES";
                            }
                            if (message.TssBaseService_I_TSS_VOTES != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_VOTES")) {
                                object.TssBaseService_I_TSS_VOTES = $root.com.hedera.hapi.node.state.tss.TssVoteMapKey.toObject(message.TssBaseService_I_TSS_VOTES, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "TssBaseService_I_TSS_VOTES";
                            }
                            if (message.ScheduleService_I_SCHEDULED_ORDERS != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_ORDERS")) {
                                object.ScheduleService_I_SCHEDULED_ORDERS = $root.proto.ScheduledOrder.toObject(message.ScheduleService_I_SCHEDULED_ORDERS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "ScheduleService_I_SCHEDULED_ORDERS";
                            }
                            if (message.ScheduleService_I_SCHEDULED_USAGES != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_USAGES")) {
                                object.ScheduleService_I_SCHEDULED_USAGES = $root.proto.TimestampSeconds.toObject(message.ScheduleService_I_SCHEDULED_USAGES, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "ScheduleService_I_SCHEDULED_USAGES";
                            }
                            if (message.TssBaseService_I_TSS_ENCRYPTION_KEYS != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_ENCRYPTION_KEYS")) {
                                object.TssBaseService_I_TSS_ENCRYPTION_KEYS = $root.proto.EntityNumber.toObject(message.TssBaseService_I_TSS_ENCRYPTION_KEYS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "TssBaseService_I_TSS_ENCRYPTION_KEYS";
                            }
                            if (message.HintsService_I_HINTS_KEY_SETS != null && $Object.hasOwnProperty.call(message, "HintsService_I_HINTS_KEY_SETS")) {
                                object.HintsService_I_HINTS_KEY_SETS = $root.com.hedera.hapi.node.state.hints.HintsPartyId.toObject(message.HintsService_I_HINTS_KEY_SETS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "HintsService_I_HINTS_KEY_SETS";
                            }
                            if (message.HintsService_I_PREPROCESSING_VOTES != null && $Object.hasOwnProperty.call(message, "HintsService_I_PREPROCESSING_VOTES")) {
                                object.HintsService_I_PREPROCESSING_VOTES = $root.com.hedera.hapi.node.state.hints.PreprocessingVoteId.toObject(message.HintsService_I_PREPROCESSING_VOTES, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "HintsService_I_PREPROCESSING_VOTES";
                            }
                            if (message.HistoryService_I_PROOF_KEY_SETS != null && $Object.hasOwnProperty.call(message, "HistoryService_I_PROOF_KEY_SETS")) {
                                object.HistoryService_I_PROOF_KEY_SETS = $root.com.hedera.hapi.platform.state.NodeId.toObject(message.HistoryService_I_PROOF_KEY_SETS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "HistoryService_I_PROOF_KEY_SETS";
                            }
                            if (message.HistoryService_I_HISTORY_SIGNATURES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_HISTORY_SIGNATURES")) {
                                object.HistoryService_I_HISTORY_SIGNATURES = $root.com.hedera.hapi.node.state.history.ConstructionNodeId.toObject(message.HistoryService_I_HISTORY_SIGNATURES, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "HistoryService_I_HISTORY_SIGNATURES";
                            }
                            if (message.HistoryService_I_PROOF_VOTES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_PROOF_VOTES")) {
                                object.HistoryService_I_PROOF_VOTES = $root.com.hedera.hapi.node.state.history.ConstructionNodeId.toObject(message.HistoryService_I_PROOF_VOTES, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "HistoryService_I_PROOF_VOTES";
                            }
                            if (message.HintsService_I_CRS_PUBLICATIONS != null && $Object.hasOwnProperty.call(message, "HintsService_I_CRS_PUBLICATIONS")) {
                                object.HintsService_I_CRS_PUBLICATIONS = $root.com.hedera.hapi.platform.state.NodeId.toObject(message.HintsService_I_CRS_PUBLICATIONS, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "HintsService_I_CRS_PUBLICATIONS";
                            }
                            if (message.ContractService_I_EVM_HOOK_STATES != null && $Object.hasOwnProperty.call(message, "ContractService_I_EVM_HOOK_STATES")) {
                                object.ContractService_I_EVM_HOOK_STATES = $root.proto.HookId.toObject(message.ContractService_I_EVM_HOOK_STATES, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "ContractService_I_EVM_HOOK_STATES";
                            }
                            if (message.ContractService_I_EVM_HOOK_STORAGE != null && $Object.hasOwnProperty.call(message, "ContractService_I_EVM_HOOK_STORAGE")) {
                                object.ContractService_I_EVM_HOOK_STORAGE = $root.com.hedera.hapi.node.state.hooks.EvmHookSlotKey.toObject(message.ContractService_I_EVM_HOOK_STORAGE, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "ContractService_I_EVM_HOOK_STORAGE";
                            }
                            if (message.AddressBookService_I_ACCOUNT_NODE_REL != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_ACCOUNT_NODE_REL")) {
                                object.AddressBookService_I_ACCOUNT_NODE_REL = $root.proto.AccountID.toObject(message.AddressBookService_I_ACCOUNT_NODE_REL, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "AddressBookService_I_ACCOUNT_NODE_REL";
                            }
                            if (message.HistoryService_I_WRAPS_MESSAGE_HISTORIES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_WRAPS_MESSAGE_HISTORIES")) {
                                object.HistoryService_I_WRAPS_MESSAGE_HISTORIES = $root.com.hedera.hapi.node.state.history.ConstructionNodeId.toObject(message.HistoryService_I_WRAPS_MESSAGE_HISTORIES, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "HistoryService_I_WRAPS_MESSAGE_HISTORIES";
                            }
                            if (message.AddressBookService_I_REGISTERED_NODES != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_REGISTERED_NODES")) {
                                object.AddressBookService_I_REGISTERED_NODES = $root.proto.EntityNumber.toObject(message.AddressBookService_I_REGISTERED_NODES, options, _depth + 1);
                                if (options.oneofs)
                                    object.key = "AddressBookService_I_REGISTERED_NODES";
                            }
                            if (message.RecordCache_I_TRANSACTION_RECEIPTS != null && $Object.hasOwnProperty.call(message, "RecordCache_I_TRANSACTION_RECEIPTS")) {
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.RecordCache_I_TRANSACTION_RECEIPTS = typeof message.RecordCache_I_TRANSACTION_RECEIPTS === "number" ? $BigInt(message.RecordCache_I_TRANSACTION_RECEIPTS) : $util.Long.fromBits(message.RecordCache_I_TRANSACTION_RECEIPTS.low >>> 0, message.RecordCache_I_TRANSACTION_RECEIPTS.high >>> 0, true).toBigInt();
                                else if (typeof message.RecordCache_I_TRANSACTION_RECEIPTS === "number")
                                    object.RecordCache_I_TRANSACTION_RECEIPTS = options.longs === $String ? $String(message.RecordCache_I_TRANSACTION_RECEIPTS) : message.RecordCache_I_TRANSACTION_RECEIPTS;
                                else
                                    object.RecordCache_I_TRANSACTION_RECEIPTS = options.longs === $String ? $util.Long.prototype.toString.call(message.RecordCache_I_TRANSACTION_RECEIPTS) : options.longs === $Number ? new $util.LongBits(message.RecordCache_I_TRANSACTION_RECEIPTS.low >>> 0, message.RecordCache_I_TRANSACTION_RECEIPTS.high >>> 0).toNumber(true) : message.RecordCache_I_TRANSACTION_RECEIPTS;
                                if (options.oneofs)
                                    object.key = "RecordCache_I_TRANSACTION_RECEIPTS";
                            }
                            if (message.FileService_I_UPGRADE_DATA_150 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_150")) {
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.FileService_I_UPGRADE_DATA_150 = typeof message.FileService_I_UPGRADE_DATA_150 === "number" ? $BigInt(message.FileService_I_UPGRADE_DATA_150) : $util.Long.fromBits(message.FileService_I_UPGRADE_DATA_150.low >>> 0, message.FileService_I_UPGRADE_DATA_150.high >>> 0, true).toBigInt();
                                else if (typeof message.FileService_I_UPGRADE_DATA_150 === "number")
                                    object.FileService_I_UPGRADE_DATA_150 = options.longs === $String ? $String(message.FileService_I_UPGRADE_DATA_150) : message.FileService_I_UPGRADE_DATA_150;
                                else
                                    object.FileService_I_UPGRADE_DATA_150 = options.longs === $String ? $util.Long.prototype.toString.call(message.FileService_I_UPGRADE_DATA_150) : options.longs === $Number ? new $util.LongBits(message.FileService_I_UPGRADE_DATA_150.low >>> 0, message.FileService_I_UPGRADE_DATA_150.high >>> 0).toNumber(true) : message.FileService_I_UPGRADE_DATA_150;
                                if (options.oneofs)
                                    object.key = "FileService_I_UPGRADE_DATA_150";
                            }
                            if (message.FileService_I_UPGRADE_DATA_151 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_151")) {
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.FileService_I_UPGRADE_DATA_151 = typeof message.FileService_I_UPGRADE_DATA_151 === "number" ? $BigInt(message.FileService_I_UPGRADE_DATA_151) : $util.Long.fromBits(message.FileService_I_UPGRADE_DATA_151.low >>> 0, message.FileService_I_UPGRADE_DATA_151.high >>> 0, true).toBigInt();
                                else if (typeof message.FileService_I_UPGRADE_DATA_151 === "number")
                                    object.FileService_I_UPGRADE_DATA_151 = options.longs === $String ? $String(message.FileService_I_UPGRADE_DATA_151) : message.FileService_I_UPGRADE_DATA_151;
                                else
                                    object.FileService_I_UPGRADE_DATA_151 = options.longs === $String ? $util.Long.prototype.toString.call(message.FileService_I_UPGRADE_DATA_151) : options.longs === $Number ? new $util.LongBits(message.FileService_I_UPGRADE_DATA_151.low >>> 0, message.FileService_I_UPGRADE_DATA_151.high >>> 0).toNumber(true) : message.FileService_I_UPGRADE_DATA_151;
                                if (options.oneofs)
                                    object.key = "FileService_I_UPGRADE_DATA_151";
                            }
                            if (message.FileService_I_UPGRADE_DATA_152 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_152")) {
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.FileService_I_UPGRADE_DATA_152 = typeof message.FileService_I_UPGRADE_DATA_152 === "number" ? $BigInt(message.FileService_I_UPGRADE_DATA_152) : $util.Long.fromBits(message.FileService_I_UPGRADE_DATA_152.low >>> 0, message.FileService_I_UPGRADE_DATA_152.high >>> 0, true).toBigInt();
                                else if (typeof message.FileService_I_UPGRADE_DATA_152 === "number")
                                    object.FileService_I_UPGRADE_DATA_152 = options.longs === $String ? $String(message.FileService_I_UPGRADE_DATA_152) : message.FileService_I_UPGRADE_DATA_152;
                                else
                                    object.FileService_I_UPGRADE_DATA_152 = options.longs === $String ? $util.Long.prototype.toString.call(message.FileService_I_UPGRADE_DATA_152) : options.longs === $Number ? new $util.LongBits(message.FileService_I_UPGRADE_DATA_152.low >>> 0, message.FileService_I_UPGRADE_DATA_152.high >>> 0).toNumber(true) : message.FileService_I_UPGRADE_DATA_152;
                                if (options.oneofs)
                                    object.key = "FileService_I_UPGRADE_DATA_152";
                            }
                            if (message.FileService_I_UPGRADE_DATA_153 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_153")) {
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.FileService_I_UPGRADE_DATA_153 = typeof message.FileService_I_UPGRADE_DATA_153 === "number" ? $BigInt(message.FileService_I_UPGRADE_DATA_153) : $util.Long.fromBits(message.FileService_I_UPGRADE_DATA_153.low >>> 0, message.FileService_I_UPGRADE_DATA_153.high >>> 0, true).toBigInt();
                                else if (typeof message.FileService_I_UPGRADE_DATA_153 === "number")
                                    object.FileService_I_UPGRADE_DATA_153 = options.longs === $String ? $String(message.FileService_I_UPGRADE_DATA_153) : message.FileService_I_UPGRADE_DATA_153;
                                else
                                    object.FileService_I_UPGRADE_DATA_153 = options.longs === $String ? $util.Long.prototype.toString.call(message.FileService_I_UPGRADE_DATA_153) : options.longs === $Number ? new $util.LongBits(message.FileService_I_UPGRADE_DATA_153.low >>> 0, message.FileService_I_UPGRADE_DATA_153.high >>> 0).toNumber(true) : message.FileService_I_UPGRADE_DATA_153;
                                if (options.oneofs)
                                    object.key = "FileService_I_UPGRADE_DATA_153";
                            }
                            if (message.FileService_I_UPGRADE_DATA_154 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_154")) {
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.FileService_I_UPGRADE_DATA_154 = typeof message.FileService_I_UPGRADE_DATA_154 === "number" ? $BigInt(message.FileService_I_UPGRADE_DATA_154) : $util.Long.fromBits(message.FileService_I_UPGRADE_DATA_154.low >>> 0, message.FileService_I_UPGRADE_DATA_154.high >>> 0, true).toBigInt();
                                else if (typeof message.FileService_I_UPGRADE_DATA_154 === "number")
                                    object.FileService_I_UPGRADE_DATA_154 = options.longs === $String ? $String(message.FileService_I_UPGRADE_DATA_154) : message.FileService_I_UPGRADE_DATA_154;
                                else
                                    object.FileService_I_UPGRADE_DATA_154 = options.longs === $String ? $util.Long.prototype.toString.call(message.FileService_I_UPGRADE_DATA_154) : options.longs === $Number ? new $util.LongBits(message.FileService_I_UPGRADE_DATA_154.low >>> 0, message.FileService_I_UPGRADE_DATA_154.high >>> 0).toNumber(true) : message.FileService_I_UPGRADE_DATA_154;
                                if (options.oneofs)
                                    object.key = "FileService_I_UPGRADE_DATA_154";
                            }
                            if (message.FileService_I_UPGRADE_DATA_155 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_155")) {
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.FileService_I_UPGRADE_DATA_155 = typeof message.FileService_I_UPGRADE_DATA_155 === "number" ? $BigInt(message.FileService_I_UPGRADE_DATA_155) : $util.Long.fromBits(message.FileService_I_UPGRADE_DATA_155.low >>> 0, message.FileService_I_UPGRADE_DATA_155.high >>> 0, true).toBigInt();
                                else if (typeof message.FileService_I_UPGRADE_DATA_155 === "number")
                                    object.FileService_I_UPGRADE_DATA_155 = options.longs === $String ? $String(message.FileService_I_UPGRADE_DATA_155) : message.FileService_I_UPGRADE_DATA_155;
                                else
                                    object.FileService_I_UPGRADE_DATA_155 = options.longs === $String ? $util.Long.prototype.toString.call(message.FileService_I_UPGRADE_DATA_155) : options.longs === $Number ? new $util.LongBits(message.FileService_I_UPGRADE_DATA_155.low >>> 0, message.FileService_I_UPGRADE_DATA_155.high >>> 0).toNumber(true) : message.FileService_I_UPGRADE_DATA_155;
                                if (options.oneofs)
                                    object.key = "FileService_I_UPGRADE_DATA_155";
                            }
                            if (message.FileService_I_UPGRADE_DATA_156 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_156")) {
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.FileService_I_UPGRADE_DATA_156 = typeof message.FileService_I_UPGRADE_DATA_156 === "number" ? $BigInt(message.FileService_I_UPGRADE_DATA_156) : $util.Long.fromBits(message.FileService_I_UPGRADE_DATA_156.low >>> 0, message.FileService_I_UPGRADE_DATA_156.high >>> 0, true).toBigInt();
                                else if (typeof message.FileService_I_UPGRADE_DATA_156 === "number")
                                    object.FileService_I_UPGRADE_DATA_156 = options.longs === $String ? $String(message.FileService_I_UPGRADE_DATA_156) : message.FileService_I_UPGRADE_DATA_156;
                                else
                                    object.FileService_I_UPGRADE_DATA_156 = options.longs === $String ? $util.Long.prototype.toString.call(message.FileService_I_UPGRADE_DATA_156) : options.longs === $Number ? new $util.LongBits(message.FileService_I_UPGRADE_DATA_156.low >>> 0, message.FileService_I_UPGRADE_DATA_156.high >>> 0).toNumber(true) : message.FileService_I_UPGRADE_DATA_156;
                                if (options.oneofs)
                                    object.key = "FileService_I_UPGRADE_DATA_156";
                            }
                            if (message.FileService_I_UPGRADE_DATA_157 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_157")) {
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.FileService_I_UPGRADE_DATA_157 = typeof message.FileService_I_UPGRADE_DATA_157 === "number" ? $BigInt(message.FileService_I_UPGRADE_DATA_157) : $util.Long.fromBits(message.FileService_I_UPGRADE_DATA_157.low >>> 0, message.FileService_I_UPGRADE_DATA_157.high >>> 0, true).toBigInt();
                                else if (typeof message.FileService_I_UPGRADE_DATA_157 === "number")
                                    object.FileService_I_UPGRADE_DATA_157 = options.longs === $String ? $String(message.FileService_I_UPGRADE_DATA_157) : message.FileService_I_UPGRADE_DATA_157;
                                else
                                    object.FileService_I_UPGRADE_DATA_157 = options.longs === $String ? $util.Long.prototype.toString.call(message.FileService_I_UPGRADE_DATA_157) : options.longs === $Number ? new $util.LongBits(message.FileService_I_UPGRADE_DATA_157.low >>> 0, message.FileService_I_UPGRADE_DATA_157.high >>> 0).toNumber(true) : message.FileService_I_UPGRADE_DATA_157;
                                if (options.oneofs)
                                    object.key = "FileService_I_UPGRADE_DATA_157";
                            }
                            if (message.FileService_I_UPGRADE_DATA_158 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_158")) {
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.FileService_I_UPGRADE_DATA_158 = typeof message.FileService_I_UPGRADE_DATA_158 === "number" ? $BigInt(message.FileService_I_UPGRADE_DATA_158) : $util.Long.fromBits(message.FileService_I_UPGRADE_DATA_158.low >>> 0, message.FileService_I_UPGRADE_DATA_158.high >>> 0, true).toBigInt();
                                else if (typeof message.FileService_I_UPGRADE_DATA_158 === "number")
                                    object.FileService_I_UPGRADE_DATA_158 = options.longs === $String ? $String(message.FileService_I_UPGRADE_DATA_158) : message.FileService_I_UPGRADE_DATA_158;
                                else
                                    object.FileService_I_UPGRADE_DATA_158 = options.longs === $String ? $util.Long.prototype.toString.call(message.FileService_I_UPGRADE_DATA_158) : options.longs === $Number ? new $util.LongBits(message.FileService_I_UPGRADE_DATA_158.low >>> 0, message.FileService_I_UPGRADE_DATA_158.high >>> 0).toNumber(true) : message.FileService_I_UPGRADE_DATA_158;
                                if (options.oneofs)
                                    object.key = "FileService_I_UPGRADE_DATA_158";
                            }
                            if (message.FileService_I_UPGRADE_DATA_159 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_159")) {
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.FileService_I_UPGRADE_DATA_159 = typeof message.FileService_I_UPGRADE_DATA_159 === "number" ? $BigInt(message.FileService_I_UPGRADE_DATA_159) : $util.Long.fromBits(message.FileService_I_UPGRADE_DATA_159.low >>> 0, message.FileService_I_UPGRADE_DATA_159.high >>> 0, true).toBigInt();
                                else if (typeof message.FileService_I_UPGRADE_DATA_159 === "number")
                                    object.FileService_I_UPGRADE_DATA_159 = options.longs === $String ? $String(message.FileService_I_UPGRADE_DATA_159) : message.FileService_I_UPGRADE_DATA_159;
                                else
                                    object.FileService_I_UPGRADE_DATA_159 = options.longs === $String ? $util.Long.prototype.toString.call(message.FileService_I_UPGRADE_DATA_159) : options.longs === $Number ? new $util.LongBits(message.FileService_I_UPGRADE_DATA_159.low >>> 0, message.FileService_I_UPGRADE_DATA_159.high >>> 0).toNumber(true) : message.FileService_I_UPGRADE_DATA_159;
                                if (options.oneofs)
                                    object.key = "FileService_I_UPGRADE_DATA_159";
                            }
                            return object;
                        };

                        /**
                         * Converts this StateKey to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        StateKey.prototype.toJSON = function() {
                            return StateKey.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for StateKey
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.state.StateKey
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        StateKey.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.state.StateKey";
                        };

                        return StateKey;
                    })();

                    state.StateValue = (function() {

                        /**
                         * Properties of a StateValue.
                         * @typedef {Object} com.hedera.hapi.platform.state.StateValue.$Properties
                         * @property {proto.EntityNumber.$Properties|null} [EntityIdService_I_ENTITY_ID] A state identifier for the next entity Identifier.
                         * @property {proto.Account.$Properties|null} [TokenService_I_ACCOUNTS] A state identifier for the Accounts state.
                         * @property {proto.AccountID.$Properties|null} [TokenService_I_ALIASES] A state identifier for account aliases.
                         * @property {proto.SlotValue.$Properties|null} [ContractService_I_STORAGE] A state identifier for contract storage slots.
                         * @property {proto.Bytecode.$Properties|null} [ContractService_I_BYTECODE] A state identifier for contract bytecode.
                         * @property {proto.File.$Properties|null} [FileService_I_FILES] A state identifier for Hedera File Service (HFS).
                         * @property {proto.Token.$Properties|null} [TokenService_I_TOKENS] A state identifier for Hedera Token Service (HTS).
                         * @property {proto.Nft.$Properties|null} [TokenService_I_NFTS] A state identifier for non-fungible/unique tokens.
                         * @property {proto.TokenRelation.$Properties|null} [TokenService_I_TOKEN_RELS] A state identifier for token relationships.
                         * @property {proto.StakingNodeInfo.$Properties|null} [TokenService_I_STAKING_INFOS] A state identifier for network staking information.
                         * @property {proto.NetworkStakingRewards.$Properties|null} [TokenService_I_STAKING_NETWORK_REWARDS] A state identifier for network staking rewards.
                         * @property {proto.ThrottleUsageSnapshots.$Properties|null} [CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS] A state identifier for throttle usage.
                         * @property {proto.CongestionLevelStarts.$Properties|null} [CongestionThrottleService_I_CONGESTION_LEVEL_STARTS] A state identifier for network congestion start times.
                         * @property {proto.Schedule.$Properties|null} [ScheduleService_I_SCHEDULES_BY_ID] A state identifier for scheduled transactions.
                         * @property {proto.ScheduleList.$Properties|null} [ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC] A state identifier for scheduled transaction expiration.
                         * @property {proto.ScheduleList.$Properties|null} [ScheduleService_I_SCHEDULES_BY_EQUALITY] A state identifier for scheduled transaction deduplication.
                         * @property {proto.ExchangeRateSet.$Properties|null} [FeeService_I_MIDNIGHT_RATES] A state identifier for conversion rate updates.
                         * @property {proto.RunningHashes.$Properties|null} [BlockRecordService_I_RUNNING_HASHES] A state identifier for the network running hash(es).
                         * @property {proto.BlockInfo.$Properties|null} [BlockRecordService_I_BLOCKS] A state identifier for network block information.
                         * @property {com.hedera.hapi.node.state.addressbook.Node.$Properties|null} [AddressBookService_I_NODES] A state identifier for address book nodes.
                         * @property {proto.Topic.$Properties|null} [ConsensusService_I_TOPICS] A state identifier for the Topics state.
                         * @property {proto.ProtoBytes.$Properties|null} [FreezeService_I_UPGRADE_FILE_HASH] A state identifier for the hash of the next "upgrade" file.
                         * @property {proto.Timestamp.$Properties|null} [FreezeService_I_FREEZE_TIME] A state identifier for the next network freeze time. Singleton state.
                         * @property {com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Properties|null} [BlockStreamService_I_BLOCK_STREAM_INFO] A state identifier for the block stream status. Singleton state.
                         * @property {proto.AccountPendingAirdrop.$Properties|null} [TokenService_I_PENDING_AIRDROPS] A state identifier for pending airdrops.
                         * @property {com.hedera.hapi.platform.state.PlatformState.$Properties|null} [PlatformStateService_I_PLATFORM_STATE] A state identifier for the platform state. Singleton state.
                         * @property {com.hedera.hapi.node.state.roster.RosterState.$Properties|null} [RosterService_I_ROSTER_STATE] A state identifier for the roster state. Singleton state.
                         * @property {com.hedera.hapi.node.state.roster.Roster.$Properties|null} [RosterService_I_ROSTERS] A state identifier for the rosters.
                         * @property {proto.ScheduledCounts.$Properties|null} [ScheduleService_I_SCHEDULED_COUNTS] A state identifier for counts of transactions scheduled and
                         * processed in a second.
                         * @property {proto.ScheduleID.$Properties|null} [ScheduleService_I_SCHEDULE_ID_BY_EQUALITY] A state identifier for scheduled transaction deduplication.
                         * @property {com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Properties|null} [TssBaseService_I_TSS_MESSAGES] A state identifier for TSS messages.
                         * @property {com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Properties|null} [TssBaseService_I_TSS_VOTES] A state identifier for TSS votes.
                         * @property {proto.ScheduleID.$Properties|null} [ScheduleService_I_SCHEDULED_ORDERS] A state identifier for the ordering of scheduled transactions.
                         * @property {proto.ThrottleUsageSnapshots.$Properties|null} [ScheduleService_I_SCHEDULED_USAGES] A state identifier for scheduled throttle usage snapshots.
                         * @property {com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Properties|null} [TssBaseService_I_TSS_ENCRYPTION_KEYS] A state identifier for the TSS encryption keys.
                         * @property {com.hedera.hapi.node.state.hints.HintsKeySet.$Properties|null} [HintsService_I_HINTS_KEY_SETS] A state identifier for hinTS key sets.
                         * @property {com.hedera.hapi.node.state.hints.HintsConstruction.$Properties|null} [HintsService_I_ACTIVE_HINTS_CONSTRUCTION] A state identifier for the active hinTS construction. Singleton state.
                         * @property {com.hedera.hapi.node.state.hints.HintsConstruction.$Properties|null} [HintsService_I_NEXT_HINTS_CONSTRUCTION] A state identifier for the next hinTS construction. Singleton state.
                         * @property {com.hedera.hapi.node.state.hints.PreprocessingVote.$Properties|null} [HintsService_I_PREPROCESSING_VOTES] A state identifier for hinTS preprocessing output votes.
                         * @property {com.hedera.hapi.node.state.entity.EntityCounts.$Properties|null} [EntityIdService_I_ENTITY_COUNTS] A state identifier for the entity counts. Singleton state.
                         * @property {proto.ProtoBytes.$Properties|null} [HistoryService_I_LEDGER_ID] A state identifier for the ledger id. Singleton state.
                         * @property {com.hedera.hapi.node.state.history.ProofKeySet.$Properties|null} [HistoryService_I_PROOF_KEY_SETS] A state identifier for history proof key sets.
                         * @property {com.hedera.hapi.node.state.history.HistoryProofConstruction.$Properties|null} [HistoryService_I_ACTIVE_PROOF_CONSTRUCTION] A state identifier for the active proof construction. Singleton state.
                         * @property {com.hedera.hapi.node.state.history.HistoryProofConstruction.$Properties|null} [HistoryService_I_NEXT_PROOF_CONSTRUCTION] A state identifier for the next proof construction. Singleton state.
                         * @property {com.hedera.hapi.node.state.history.RecordedHistorySignature.$Properties|null} [HistoryService_I_HISTORY_SIGNATURES] A state identifier for signatures on roster transition histories.
                         * @property {com.hedera.hapi.node.state.history.HistoryProofVote.$Properties|null} [HistoryService_I_PROOF_VOTES] A state identifier for votes on history proofs.
                         * @property {com.hedera.hapi.node.state.hints.CRSState.$Properties|null} [HintsService_I_CRS_STATE] A state identifier for the CRS state. Singleton state.
                         * @property {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties|null} [HintsService_I_CRS_PUBLICATIONS] A state identifier for the CRS publications.
                         * @property {proto.NodeRewards.$Properties|null} [TokenService_I_NODE_REWARDS] A state identifier for the node rewards. Singleton state.
                         * @property {com.hedera.hapi.node.state.hooks.EvmHookState.$Properties|null} [ContractService_I_EVM_HOOK_STATES] A state identifier for EVM hook states.
                         * @property {proto.SlotValue.$Properties|null} [ContractService_I_EVM_HOOK_STORAGE] A state identifier for EVM hook storage.
                         * @property {com.hedera.hapi.platform.state.NodeId.$Properties|null} [AddressBookService_I_ACCOUNT_NODE_REL] A state identifier for account to node relationships.
                         * @property {com.hedera.hapi.node.state.history.WrapsMessageHistory.$Properties|null} [HistoryService_I_WRAPS_MESSAGE_HISTORIES] A state identifier for WRAPS message histories used in a roster transition proof.
                         * @property {com.hedera.hapi.node.state.token.NodePayments.$Properties|null} [TokenService_I_NODE_PAYMENTS] A state identifier for the node payments. Singleton state.
                         * @property {com.hedera.hapi.node.state.addressbook.RegisteredNode.$Properties|null} [AddressBookService_I_REGISTERED_NODES] A state identifier for registered nodes.
                         * @property {com.hedera.hapi.platform.state.NodeId.$Properties|null} [EntityIdService_I_HIGHEST_NODE_ID] A state identifier for the next node Identifier. Singleton state.
                         * @property {proto.ProtoBytes.$Properties|null} [HistoryService_I_WRAPS_PROVING_KEY_HASH] A state identifier for the expected WRAPS proving key hash. Singleton state.
                         * @property {proto.TransactionReceiptEntries.$Properties|null} [RecordCache_I_TRANSACTION_RECEIPTS] Metadata of the round receipts queue.
                         * @property {com.hedera.hapi.platform.state.QueueState.$Properties|null} [queueState] Metadata of the queue state.<br/>
                         * Reasons for the field number:
                         * <ul>
                         * <li>Intermediate state types are expected to be rare (field numbers 8000-9999 are allocated for that purpose).</li>
                         * <li>QueueState represents internal head/tail pointers of a queue, not a standalone Queue type (as with Singleton or KV).</li>
                         * </ul>
                         * @property {proto.ProtoBytes.$Properties|null} [FileService_I_UPGRADE_DATA_150] Metadata of the `150` upgrade file data queue.
                         * @property {proto.ProtoBytes.$Properties|null} [FileService_I_UPGRADE_DATA_151] Metadata of the`151` upgrade file data queue.
                         * @property {proto.ProtoBytes.$Properties|null} [FileService_I_UPGRADE_DATA_152] Metadata of the `152` upgrade file data queue.
                         * @property {proto.ProtoBytes.$Properties|null} [FileService_I_UPGRADE_DATA_153] Metadata of the `153` upgrade file data queue.
                         * @property {proto.ProtoBytes.$Properties|null} [FileService_I_UPGRADE_DATA_154] Metadata of the`154` upgrade file data queue.
                         * @property {proto.ProtoBytes.$Properties|null} [FileService_I_UPGRADE_DATA_155] Metadata of the `155` upgrade file data queue.
                         * @property {proto.ProtoBytes.$Properties|null} [FileService_I_UPGRADE_DATA_156] Metadata of the `156` upgrade file data queue.
                         * @property {proto.ProtoBytes.$Properties|null} [FileService_I_UPGRADE_DATA_157] Metadata of the `157` upgrade file data queue.
                         * @property {proto.ProtoBytes.$Properties|null} [FileService_I_UPGRADE_DATA_158] Metadata of the `158` upgrade file data queue.
                         * @property {proto.ProtoBytes.$Properties|null} [FileService_I_UPGRADE_DATA_159] Metadata of the `159` upgrade file data queue.
                         * @property {"EntityIdService_I_ENTITY_ID"|"TokenService_I_ACCOUNTS"|"TokenService_I_ALIASES"|"ContractService_I_STORAGE"|"ContractService_I_BYTECODE"|"FileService_I_FILES"|"TokenService_I_TOKENS"|"TokenService_I_NFTS"|"TokenService_I_TOKEN_RELS"|"TokenService_I_STAKING_INFOS"|"TokenService_I_STAKING_NETWORK_REWARDS"|"CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS"|"CongestionThrottleService_I_CONGESTION_LEVEL_STARTS"|"ScheduleService_I_SCHEDULES_BY_ID"|"ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC"|"ScheduleService_I_SCHEDULES_BY_EQUALITY"|"FeeService_I_MIDNIGHT_RATES"|"BlockRecordService_I_RUNNING_HASHES"|"BlockRecordService_I_BLOCKS"|"AddressBookService_I_NODES"|"ConsensusService_I_TOPICS"|"FreezeService_I_UPGRADE_FILE_HASH"|"FreezeService_I_FREEZE_TIME"|"BlockStreamService_I_BLOCK_STREAM_INFO"|"TokenService_I_PENDING_AIRDROPS"|"PlatformStateService_I_PLATFORM_STATE"|"RosterService_I_ROSTER_STATE"|"RosterService_I_ROSTERS"|"ScheduleService_I_SCHEDULED_COUNTS"|"ScheduleService_I_SCHEDULE_ID_BY_EQUALITY"|"TssBaseService_I_TSS_MESSAGES"|"TssBaseService_I_TSS_VOTES"|"ScheduleService_I_SCHEDULED_ORDERS"|"ScheduleService_I_SCHEDULED_USAGES"|"TssBaseService_I_TSS_ENCRYPTION_KEYS"|"HintsService_I_HINTS_KEY_SETS"|"HintsService_I_ACTIVE_HINTS_CONSTRUCTION"|"HintsService_I_NEXT_HINTS_CONSTRUCTION"|"HintsService_I_PREPROCESSING_VOTES"|"EntityIdService_I_ENTITY_COUNTS"|"HistoryService_I_LEDGER_ID"|"HistoryService_I_PROOF_KEY_SETS"|"HistoryService_I_ACTIVE_PROOF_CONSTRUCTION"|"HistoryService_I_NEXT_PROOF_CONSTRUCTION"|"HistoryService_I_HISTORY_SIGNATURES"|"HistoryService_I_PROOF_VOTES"|"HintsService_I_CRS_STATE"|"HintsService_I_CRS_PUBLICATIONS"|"TokenService_I_NODE_REWARDS"|"ContractService_I_EVM_HOOK_STATES"|"ContractService_I_EVM_HOOK_STORAGE"|"AddressBookService_I_ACCOUNT_NODE_REL"|"HistoryService_I_WRAPS_MESSAGE_HISTORIES"|"TokenService_I_NODE_PAYMENTS"|"AddressBookService_I_REGISTERED_NODES"|"EntityIdService_I_HIGHEST_NODE_ID"|"HistoryService_I_WRAPS_PROVING_KEY_HASH"|"RecordCache_I_TRANSACTION_RECEIPTS"|"queueState"|"FileService_I_UPGRADE_DATA_150"|"FileService_I_UPGRADE_DATA_151"|"FileService_I_UPGRADE_DATA_152"|"FileService_I_UPGRADE_DATA_153"|"FileService_I_UPGRADE_DATA_154"|"FileService_I_UPGRADE_DATA_155"|"FileService_I_UPGRADE_DATA_156"|"FileService_I_UPGRADE_DATA_157"|"FileService_I_UPGRADE_DATA_158"|"FileService_I_UPGRADE_DATA_159"} [value] StateValue value
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a StateValue.
                         * @memberof com.hedera.hapi.platform.state
                         * @interface IStateValue
                         * @augments com.hedera.hapi.platform.state.StateValue.$Properties
                         * @deprecated Use com.hedera.hapi.platform.state.StateValue.$Properties instead.
                         */

                        /**
                         * Narrowed shape of a StateValue.
                         * @typedef {{
                         *   EntityIdService_I_ENTITY_ID?: proto.EntityNumber.$Shape|null;
                         *   TokenService_I_ACCOUNTS?: proto.Account.$Shape|null;
                         *   TokenService_I_ALIASES?: proto.AccountID.$Shape|null;
                         *   ContractService_I_STORAGE?: proto.SlotValue.$Shape|null;
                         *   ContractService_I_BYTECODE?: proto.Bytecode.$Shape|null;
                         *   FileService_I_FILES?: proto.File.$Shape|null;
                         *   TokenService_I_TOKENS?: proto.Token.$Shape|null;
                         *   TokenService_I_NFTS?: proto.Nft.$Shape|null;
                         *   TokenService_I_TOKEN_RELS?: proto.TokenRelation.$Shape|null;
                         *   TokenService_I_STAKING_INFOS?: proto.StakingNodeInfo.$Shape|null;
                         *   TokenService_I_STAKING_NETWORK_REWARDS?: proto.NetworkStakingRewards.$Shape|null;
                         *   CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: proto.ThrottleUsageSnapshots.$Shape|null;
                         *   CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: proto.CongestionLevelStarts.$Shape|null;
                         *   ScheduleService_I_SCHEDULES_BY_ID?: proto.Schedule.$Shape|null;
                         *   ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: proto.ScheduleList.$Shape|null;
                         *   ScheduleService_I_SCHEDULES_BY_EQUALITY?: proto.ScheduleList.$Shape|null;
                         *   FeeService_I_MIDNIGHT_RATES?: proto.ExchangeRateSet.$Shape|null;
                         *   BlockRecordService_I_RUNNING_HASHES?: proto.RunningHashes.$Shape|null;
                         *   BlockRecordService_I_BLOCKS?: proto.BlockInfo.$Shape|null;
                         *   AddressBookService_I_NODES?: com.hedera.hapi.node.state.addressbook.Node.$Shape|null;
                         *   ConsensusService_I_TOPICS?: proto.Topic.$Shape|null;
                         *   FreezeService_I_UPGRADE_FILE_HASH?: proto.ProtoBytes.$Shape|null;
                         *   FreezeService_I_FREEZE_TIME?: proto.Timestamp.$Shape|null;
                         *   BlockStreamService_I_BLOCK_STREAM_INFO?: com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Shape|null;
                         *   TokenService_I_PENDING_AIRDROPS?: proto.AccountPendingAirdrop.$Shape|null;
                         *   PlatformStateService_I_PLATFORM_STATE?: com.hedera.hapi.platform.state.PlatformState.$Shape|null;
                         *   RosterService_I_ROSTER_STATE?: com.hedera.hapi.node.state.roster.RosterState.$Shape|null;
                         *   RosterService_I_ROSTERS?: com.hedera.hapi.node.state.roster.Roster.$Shape|null;
                         *   ScheduleService_I_SCHEDULED_COUNTS?: proto.ScheduledCounts.$Shape|null;
                         *   ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: proto.ScheduleID.$Shape|null;
                         *   TssBaseService_I_TSS_MESSAGES?: com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Shape|null;
                         *   TssBaseService_I_TSS_VOTES?: com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Shape|null;
                         *   ScheduleService_I_SCHEDULED_ORDERS?: proto.ScheduleID.$Shape|null;
                         *   ScheduleService_I_SCHEDULED_USAGES?: proto.ThrottleUsageSnapshots.$Shape|null;
                         *   TssBaseService_I_TSS_ENCRYPTION_KEYS?: com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Shape|null;
                         *   HintsService_I_HINTS_KEY_SETS?: com.hedera.hapi.node.state.hints.HintsKeySet.$Shape|null;
                         *   HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: com.hedera.hapi.node.state.hints.HintsConstruction.$Shape|null;
                         *   HintsService_I_NEXT_HINTS_CONSTRUCTION?: com.hedera.hapi.node.state.hints.HintsConstruction.$Shape|null;
                         *   HintsService_I_PREPROCESSING_VOTES?: com.hedera.hapi.node.state.hints.PreprocessingVote.$Shape|null;
                         *   EntityIdService_I_ENTITY_COUNTS?: com.hedera.hapi.node.state.entity.EntityCounts.$Shape|null;
                         *   HistoryService_I_LEDGER_ID?: proto.ProtoBytes.$Shape|null;
                         *   HistoryService_I_PROOF_KEY_SETS?: com.hedera.hapi.node.state.history.ProofKeySet.$Shape|null;
                         *   HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: com.hedera.hapi.node.state.history.HistoryProofConstruction.$Shape|null;
                         *   HistoryService_I_NEXT_PROOF_CONSTRUCTION?: com.hedera.hapi.node.state.history.HistoryProofConstruction.$Shape|null;
                         *   HistoryService_I_HISTORY_SIGNATURES?: com.hedera.hapi.node.state.history.RecordedHistorySignature.$Shape|null;
                         *   HistoryService_I_PROOF_VOTES?: com.hedera.hapi.node.state.history.HistoryProofVote.$Shape|null;
                         *   HintsService_I_CRS_STATE?: com.hedera.hapi.node.state.hints.CRSState.$Shape|null;
                         *   HintsService_I_CRS_PUBLICATIONS?: com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Shape|null;
                         *   TokenService_I_NODE_REWARDS?: proto.NodeRewards.$Shape|null;
                         *   ContractService_I_EVM_HOOK_STATES?: com.hedera.hapi.node.state.hooks.EvmHookState.$Shape|null;
                         *   ContractService_I_EVM_HOOK_STORAGE?: proto.SlotValue.$Shape|null;
                         *   AddressBookService_I_ACCOUNT_NODE_REL?: com.hedera.hapi.platform.state.NodeId.$Shape|null;
                         *   HistoryService_I_WRAPS_MESSAGE_HISTORIES?: com.hedera.hapi.node.state.history.WrapsMessageHistory.$Shape|null;
                         *   TokenService_I_NODE_PAYMENTS?: com.hedera.hapi.node.state.token.NodePayments.$Shape|null;
                         *   AddressBookService_I_REGISTERED_NODES?: com.hedera.hapi.node.state.addressbook.RegisteredNode.$Shape|null;
                         *   EntityIdService_I_HIGHEST_NODE_ID?: com.hedera.hapi.platform.state.NodeId.$Shape|null;
                         *   HistoryService_I_WRAPS_PROVING_KEY_HASH?: proto.ProtoBytes.$Shape|null;
                         *   RecordCache_I_TRANSACTION_RECEIPTS?: proto.TransactionReceiptEntries.$Shape|null;
                         *   queueState?: com.hedera.hapi.platform.state.QueueState.$Shape|null;
                         *   FileService_I_UPGRADE_DATA_150?: proto.ProtoBytes.$Shape|null;
                         *   FileService_I_UPGRADE_DATA_151?: proto.ProtoBytes.$Shape|null;
                         *   FileService_I_UPGRADE_DATA_152?: proto.ProtoBytes.$Shape|null;
                         *   FileService_I_UPGRADE_DATA_153?: proto.ProtoBytes.$Shape|null;
                         *   FileService_I_UPGRADE_DATA_154?: proto.ProtoBytes.$Shape|null;
                         *   FileService_I_UPGRADE_DATA_155?: proto.ProtoBytes.$Shape|null;
                         *   FileService_I_UPGRADE_DATA_156?: proto.ProtoBytes.$Shape|null;
                         *   FileService_I_UPGRADE_DATA_157?: proto.ProtoBytes.$Shape|null;
                         *   FileService_I_UPGRADE_DATA_158?: proto.ProtoBytes.$Shape|null;
                         *   FileService_I_UPGRADE_DATA_159?: proto.ProtoBytes.$Shape|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * } & (
                         *   ({ value?: undefined; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "EntityIdService_I_ENTITY_ID"; EntityIdService_I_ENTITY_ID: proto.EntityNumber.$Shape; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TokenService_I_ACCOUNTS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS: proto.Account.$Shape; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TokenService_I_ALIASES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES: proto.AccountID.$Shape; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "ContractService_I_STORAGE"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE: proto.SlotValue.$Shape; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "ContractService_I_BYTECODE"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE: proto.Bytecode.$Shape; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FileService_I_FILES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES: proto.File.$Shape; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TokenService_I_TOKENS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS: proto.Token.$Shape; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TokenService_I_NFTS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS: proto.Nft.$Shape; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TokenService_I_TOKEN_RELS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS: proto.TokenRelation.$Shape; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TokenService_I_STAKING_INFOS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS: proto.StakingNodeInfo.$Shape; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TokenService_I_STAKING_NETWORK_REWARDS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS: proto.NetworkStakingRewards.$Shape; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS: proto.ThrottleUsageSnapshots.$Shape; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "CongestionThrottleService_I_CONGESTION_LEVEL_STARTS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS: proto.CongestionLevelStarts.$Shape; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "ScheduleService_I_SCHEDULES_BY_ID"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID: proto.Schedule.$Shape; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC: proto.ScheduleList.$Shape; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "ScheduleService_I_SCHEDULES_BY_EQUALITY"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY: proto.ScheduleList.$Shape; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FeeService_I_MIDNIGHT_RATES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES: proto.ExchangeRateSet.$Shape; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "BlockRecordService_I_RUNNING_HASHES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES: proto.RunningHashes.$Shape; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "BlockRecordService_I_BLOCKS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS: proto.BlockInfo.$Shape; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "AddressBookService_I_NODES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES: com.hedera.hapi.node.state.addressbook.Node.$Shape; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "ConsensusService_I_TOPICS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS: proto.Topic.$Shape; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FreezeService_I_UPGRADE_FILE_HASH"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH: proto.ProtoBytes.$Shape; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FreezeService_I_FREEZE_TIME"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME: proto.Timestamp.$Shape; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "BlockStreamService_I_BLOCK_STREAM_INFO"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO: com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Shape; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TokenService_I_PENDING_AIRDROPS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS: proto.AccountPendingAirdrop.$Shape; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "PlatformStateService_I_PLATFORM_STATE"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE: com.hedera.hapi.platform.state.PlatformState.$Shape; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "RosterService_I_ROSTER_STATE"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE: com.hedera.hapi.node.state.roster.RosterState.$Shape; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "RosterService_I_ROSTERS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS: com.hedera.hapi.node.state.roster.Roster.$Shape; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "ScheduleService_I_SCHEDULED_COUNTS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS: proto.ScheduledCounts.$Shape; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY: proto.ScheduleID.$Shape; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TssBaseService_I_TSS_MESSAGES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES: com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Shape; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TssBaseService_I_TSS_VOTES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES: com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Shape; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "ScheduleService_I_SCHEDULED_ORDERS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS: proto.ScheduleID.$Shape; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "ScheduleService_I_SCHEDULED_USAGES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES: proto.ThrottleUsageSnapshots.$Shape; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TssBaseService_I_TSS_ENCRYPTION_KEYS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS: com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Shape; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HintsService_I_HINTS_KEY_SETS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS: com.hedera.hapi.node.state.hints.HintsKeySet.$Shape; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HintsService_I_ACTIVE_HINTS_CONSTRUCTION"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION: com.hedera.hapi.node.state.hints.HintsConstruction.$Shape; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HintsService_I_NEXT_HINTS_CONSTRUCTION"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION: com.hedera.hapi.node.state.hints.HintsConstruction.$Shape; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HintsService_I_PREPROCESSING_VOTES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES: com.hedera.hapi.node.state.hints.PreprocessingVote.$Shape; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "EntityIdService_I_ENTITY_COUNTS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS: com.hedera.hapi.node.state.entity.EntityCounts.$Shape; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HistoryService_I_LEDGER_ID"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID: proto.ProtoBytes.$Shape; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HistoryService_I_PROOF_KEY_SETS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS: com.hedera.hapi.node.state.history.ProofKeySet.$Shape; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HistoryService_I_ACTIVE_PROOF_CONSTRUCTION"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION: com.hedera.hapi.node.state.history.HistoryProofConstruction.$Shape; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HistoryService_I_NEXT_PROOF_CONSTRUCTION"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION: com.hedera.hapi.node.state.history.HistoryProofConstruction.$Shape; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HistoryService_I_HISTORY_SIGNATURES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES: com.hedera.hapi.node.state.history.RecordedHistorySignature.$Shape; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HistoryService_I_PROOF_VOTES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES: com.hedera.hapi.node.state.history.HistoryProofVote.$Shape; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HintsService_I_CRS_STATE"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE: com.hedera.hapi.node.state.hints.CRSState.$Shape; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HintsService_I_CRS_PUBLICATIONS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS: com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Shape; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TokenService_I_NODE_REWARDS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS: proto.NodeRewards.$Shape; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "ContractService_I_EVM_HOOK_STATES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES: com.hedera.hapi.node.state.hooks.EvmHookState.$Shape; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "ContractService_I_EVM_HOOK_STORAGE"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE: proto.SlotValue.$Shape; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "AddressBookService_I_ACCOUNT_NODE_REL"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL: com.hedera.hapi.platform.state.NodeId.$Shape; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HistoryService_I_WRAPS_MESSAGE_HISTORIES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES: com.hedera.hapi.node.state.history.WrapsMessageHistory.$Shape; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "TokenService_I_NODE_PAYMENTS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS: com.hedera.hapi.node.state.token.NodePayments.$Shape; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "AddressBookService_I_REGISTERED_NODES"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES: com.hedera.hapi.node.state.addressbook.RegisteredNode.$Shape; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "EntityIdService_I_HIGHEST_NODE_ID"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID: com.hedera.hapi.platform.state.NodeId.$Shape; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "HistoryService_I_WRAPS_PROVING_KEY_HASH"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH: proto.ProtoBytes.$Shape; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "RecordCache_I_TRANSACTION_RECEIPTS"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS: proto.TransactionReceiptEntries.$Shape; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "queueState"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState: com.hedera.hapi.platform.state.QueueState.$Shape; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FileService_I_UPGRADE_DATA_150"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150: proto.ProtoBytes.$Shape; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FileService_I_UPGRADE_DATA_151"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151: proto.ProtoBytes.$Shape; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FileService_I_UPGRADE_DATA_152"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152: proto.ProtoBytes.$Shape; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FileService_I_UPGRADE_DATA_153"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153: proto.ProtoBytes.$Shape; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FileService_I_UPGRADE_DATA_154"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154: proto.ProtoBytes.$Shape; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FileService_I_UPGRADE_DATA_155"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155: proto.ProtoBytes.$Shape; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FileService_I_UPGRADE_DATA_156"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156: proto.ProtoBytes.$Shape; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FileService_I_UPGRADE_DATA_157"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157: proto.ProtoBytes.$Shape; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FileService_I_UPGRADE_DATA_158"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158: proto.ProtoBytes.$Shape; FileService_I_UPGRADE_DATA_159?: null }|{ value?: "FileService_I_UPGRADE_DATA_159"; EntityIdService_I_ENTITY_ID?: null; TokenService_I_ACCOUNTS?: null; TokenService_I_ALIASES?: null; ContractService_I_STORAGE?: null; ContractService_I_BYTECODE?: null; FileService_I_FILES?: null; TokenService_I_TOKENS?: null; TokenService_I_NFTS?: null; TokenService_I_TOKEN_RELS?: null; TokenService_I_STAKING_INFOS?: null; TokenService_I_STAKING_NETWORK_REWARDS?: null; CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS?: null; CongestionThrottleService_I_CONGESTION_LEVEL_STARTS?: null; ScheduleService_I_SCHEDULES_BY_ID?: null; ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC?: null; ScheduleService_I_SCHEDULES_BY_EQUALITY?: null; FeeService_I_MIDNIGHT_RATES?: null; BlockRecordService_I_RUNNING_HASHES?: null; BlockRecordService_I_BLOCKS?: null; AddressBookService_I_NODES?: null; ConsensusService_I_TOPICS?: null; FreezeService_I_UPGRADE_FILE_HASH?: null; FreezeService_I_FREEZE_TIME?: null; BlockStreamService_I_BLOCK_STREAM_INFO?: null; TokenService_I_PENDING_AIRDROPS?: null; PlatformStateService_I_PLATFORM_STATE?: null; RosterService_I_ROSTER_STATE?: null; RosterService_I_ROSTERS?: null; ScheduleService_I_SCHEDULED_COUNTS?: null; ScheduleService_I_SCHEDULE_ID_BY_EQUALITY?: null; TssBaseService_I_TSS_MESSAGES?: null; TssBaseService_I_TSS_VOTES?: null; ScheduleService_I_SCHEDULED_ORDERS?: null; ScheduleService_I_SCHEDULED_USAGES?: null; TssBaseService_I_TSS_ENCRYPTION_KEYS?: null; HintsService_I_HINTS_KEY_SETS?: null; HintsService_I_ACTIVE_HINTS_CONSTRUCTION?: null; HintsService_I_NEXT_HINTS_CONSTRUCTION?: null; HintsService_I_PREPROCESSING_VOTES?: null; EntityIdService_I_ENTITY_COUNTS?: null; HistoryService_I_LEDGER_ID?: null; HistoryService_I_PROOF_KEY_SETS?: null; HistoryService_I_ACTIVE_PROOF_CONSTRUCTION?: null; HistoryService_I_NEXT_PROOF_CONSTRUCTION?: null; HistoryService_I_HISTORY_SIGNATURES?: null; HistoryService_I_PROOF_VOTES?: null; HintsService_I_CRS_STATE?: null; HintsService_I_CRS_PUBLICATIONS?: null; TokenService_I_NODE_REWARDS?: null; ContractService_I_EVM_HOOK_STATES?: null; ContractService_I_EVM_HOOK_STORAGE?: null; AddressBookService_I_ACCOUNT_NODE_REL?: null; HistoryService_I_WRAPS_MESSAGE_HISTORIES?: null; TokenService_I_NODE_PAYMENTS?: null; AddressBookService_I_REGISTERED_NODES?: null; EntityIdService_I_HIGHEST_NODE_ID?: null; HistoryService_I_WRAPS_PROVING_KEY_HASH?: null; RecordCache_I_TRANSACTION_RECEIPTS?: null; queueState?: null; FileService_I_UPGRADE_DATA_150?: null; FileService_I_UPGRADE_DATA_151?: null; FileService_I_UPGRADE_DATA_152?: null; FileService_I_UPGRADE_DATA_153?: null; FileService_I_UPGRADE_DATA_154?: null; FileService_I_UPGRADE_DATA_155?: null; FileService_I_UPGRADE_DATA_156?: null; FileService_I_UPGRADE_DATA_157?: null; FileService_I_UPGRADE_DATA_158?: null; FileService_I_UPGRADE_DATA_159: proto.ProtoBytes.$Shape })
                         * )} com.hedera.hapi.platform.state.StateValue.$Shape
                         */

                        /**
                         * Constructs a new StateValue.
                         * @memberof com.hedera.hapi.platform.state
                         * @classdesc This message defines the type of values used in the Virtual Map State.
                         * Note that `_I_` plays a role of a reliable separator between the service name
                         * and the state name, which will be present in all types of code generation results as just underscores
                         * may be deleted and create unexpected collisions.<br/>
                         * ID ranges for StateValue types:
                         * <ol>
                         * <li>0–7999: Core state entries. Reserved for all platform states
                         * (accounts, tokens, schedules, consensus topics, etc.).</li>
                         * <li>8000–9999: Auxiliary/helper entries. Reserved for internal or support
                         * messages that are not themselves states (e.g. `QueueState`).</li>
                         * <li>10000 and above: File/blob entries. Reserved for file service or any
                         * large-blob content stored via the virtual map (for example, file
                         * contents, etc.).</li>
                         * </ol>
                         * @constructor
                         * @param {com.hedera.hapi.platform.state.StateValue.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const StateValue = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A state identifier for the next entity Identifier.
                         * @member {proto.EntityNumber.$Properties|null|undefined} EntityIdService_I_ENTITY_ID
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.EntityIdService_I_ENTITY_ID = null;

                        /**
                         * A state identifier for the Accounts state.
                         * @member {proto.Account.$Properties|null|undefined} TokenService_I_ACCOUNTS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TokenService_I_ACCOUNTS = null;

                        /**
                         * A state identifier for account aliases.
                         * @member {proto.AccountID.$Properties|null|undefined} TokenService_I_ALIASES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TokenService_I_ALIASES = null;

                        /**
                         * A state identifier for contract storage slots.
                         * @member {proto.SlotValue.$Properties|null|undefined} ContractService_I_STORAGE
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.ContractService_I_STORAGE = null;

                        /**
                         * A state identifier for contract bytecode.
                         * @member {proto.Bytecode.$Properties|null|undefined} ContractService_I_BYTECODE
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.ContractService_I_BYTECODE = null;

                        /**
                         * A state identifier for Hedera File Service (HFS).
                         * @member {proto.File.$Properties|null|undefined} FileService_I_FILES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FileService_I_FILES = null;

                        /**
                         * A state identifier for Hedera Token Service (HTS).
                         * @member {proto.Token.$Properties|null|undefined} TokenService_I_TOKENS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TokenService_I_TOKENS = null;

                        /**
                         * A state identifier for non-fungible/unique tokens.
                         * @member {proto.Nft.$Properties|null|undefined} TokenService_I_NFTS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TokenService_I_NFTS = null;

                        /**
                         * A state identifier for token relationships.
                         * @member {proto.TokenRelation.$Properties|null|undefined} TokenService_I_TOKEN_RELS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TokenService_I_TOKEN_RELS = null;

                        /**
                         * A state identifier for network staking information.
                         * @member {proto.StakingNodeInfo.$Properties|null|undefined} TokenService_I_STAKING_INFOS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TokenService_I_STAKING_INFOS = null;

                        /**
                         * A state identifier for network staking rewards.
                         * @member {proto.NetworkStakingRewards.$Properties|null|undefined} TokenService_I_STAKING_NETWORK_REWARDS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TokenService_I_STAKING_NETWORK_REWARDS = null;

                        /**
                         * A state identifier for throttle usage.
                         * @member {proto.ThrottleUsageSnapshots.$Properties|null|undefined} CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS = null;

                        /**
                         * A state identifier for network congestion start times.
                         * @member {proto.CongestionLevelStarts.$Properties|null|undefined} CongestionThrottleService_I_CONGESTION_LEVEL_STARTS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS = null;

                        /**
                         * A state identifier for scheduled transactions.
                         * @member {proto.Schedule.$Properties|null|undefined} ScheduleService_I_SCHEDULES_BY_ID
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.ScheduleService_I_SCHEDULES_BY_ID = null;

                        /**
                         * A state identifier for scheduled transaction expiration.
                         * @member {proto.ScheduleList.$Properties|null|undefined} ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC = null;

                        /**
                         * A state identifier for scheduled transaction deduplication.
                         * @member {proto.ScheduleList.$Properties|null|undefined} ScheduleService_I_SCHEDULES_BY_EQUALITY
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.ScheduleService_I_SCHEDULES_BY_EQUALITY = null;

                        /**
                         * A state identifier for conversion rate updates.
                         * @member {proto.ExchangeRateSet.$Properties|null|undefined} FeeService_I_MIDNIGHT_RATES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FeeService_I_MIDNIGHT_RATES = null;

                        /**
                         * A state identifier for the network running hash(es).
                         * @member {proto.RunningHashes.$Properties|null|undefined} BlockRecordService_I_RUNNING_HASHES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.BlockRecordService_I_RUNNING_HASHES = null;

                        /**
                         * A state identifier for network block information.
                         * @member {proto.BlockInfo.$Properties|null|undefined} BlockRecordService_I_BLOCKS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.BlockRecordService_I_BLOCKS = null;

                        /**
                         * A state identifier for address book nodes.
                         * @member {com.hedera.hapi.node.state.addressbook.Node.$Properties|null|undefined} AddressBookService_I_NODES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.AddressBookService_I_NODES = null;

                        /**
                         * A state identifier for the Topics state.
                         * @member {proto.Topic.$Properties|null|undefined} ConsensusService_I_TOPICS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.ConsensusService_I_TOPICS = null;

                        /**
                         * A state identifier for the hash of the next "upgrade" file.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} FreezeService_I_UPGRADE_FILE_HASH
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FreezeService_I_UPGRADE_FILE_HASH = null;

                        /**
                         * A state identifier for the next network freeze time. Singleton state.
                         * @member {proto.Timestamp.$Properties|null|undefined} FreezeService_I_FREEZE_TIME
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FreezeService_I_FREEZE_TIME = null;

                        /**
                         * A state identifier for the block stream status. Singleton state.
                         * @member {com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Properties|null|undefined} BlockStreamService_I_BLOCK_STREAM_INFO
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.BlockStreamService_I_BLOCK_STREAM_INFO = null;

                        /**
                         * A state identifier for pending airdrops.
                         * @member {proto.AccountPendingAirdrop.$Properties|null|undefined} TokenService_I_PENDING_AIRDROPS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TokenService_I_PENDING_AIRDROPS = null;

                        /**
                         * A state identifier for the platform state. Singleton state.
                         * @member {com.hedera.hapi.platform.state.PlatformState.$Properties|null|undefined} PlatformStateService_I_PLATFORM_STATE
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.PlatformStateService_I_PLATFORM_STATE = null;

                        /**
                         * A state identifier for the roster state. Singleton state.
                         * @member {com.hedera.hapi.node.state.roster.RosterState.$Properties|null|undefined} RosterService_I_ROSTER_STATE
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.RosterService_I_ROSTER_STATE = null;

                        /**
                         * A state identifier for the rosters.
                         * @member {com.hedera.hapi.node.state.roster.Roster.$Properties|null|undefined} RosterService_I_ROSTERS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.RosterService_I_ROSTERS = null;

                        /**
                         * A state identifier for counts of transactions scheduled and
                         * processed in a second.
                         * @member {proto.ScheduledCounts.$Properties|null|undefined} ScheduleService_I_SCHEDULED_COUNTS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.ScheduleService_I_SCHEDULED_COUNTS = null;

                        /**
                         * A state identifier for scheduled transaction deduplication.
                         * @member {proto.ScheduleID.$Properties|null|undefined} ScheduleService_I_SCHEDULE_ID_BY_EQUALITY
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY = null;

                        /**
                         * A state identifier for TSS messages.
                         * @member {com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Properties|null|undefined} TssBaseService_I_TSS_MESSAGES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TssBaseService_I_TSS_MESSAGES = null;

                        /**
                         * A state identifier for TSS votes.
                         * @member {com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Properties|null|undefined} TssBaseService_I_TSS_VOTES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TssBaseService_I_TSS_VOTES = null;

                        /**
                         * A state identifier for the ordering of scheduled transactions.
                         * @member {proto.ScheduleID.$Properties|null|undefined} ScheduleService_I_SCHEDULED_ORDERS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.ScheduleService_I_SCHEDULED_ORDERS = null;

                        /**
                         * A state identifier for scheduled throttle usage snapshots.
                         * @member {proto.ThrottleUsageSnapshots.$Properties|null|undefined} ScheduleService_I_SCHEDULED_USAGES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.ScheduleService_I_SCHEDULED_USAGES = null;

                        /**
                         * A state identifier for the TSS encryption keys.
                         * @member {com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Properties|null|undefined} TssBaseService_I_TSS_ENCRYPTION_KEYS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TssBaseService_I_TSS_ENCRYPTION_KEYS = null;

                        /**
                         * A state identifier for hinTS key sets.
                         * @member {com.hedera.hapi.node.state.hints.HintsKeySet.$Properties|null|undefined} HintsService_I_HINTS_KEY_SETS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HintsService_I_HINTS_KEY_SETS = null;

                        /**
                         * A state identifier for the active hinTS construction. Singleton state.
                         * @member {com.hedera.hapi.node.state.hints.HintsConstruction.$Properties|null|undefined} HintsService_I_ACTIVE_HINTS_CONSTRUCTION
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HintsService_I_ACTIVE_HINTS_CONSTRUCTION = null;

                        /**
                         * A state identifier for the next hinTS construction. Singleton state.
                         * @member {com.hedera.hapi.node.state.hints.HintsConstruction.$Properties|null|undefined} HintsService_I_NEXT_HINTS_CONSTRUCTION
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HintsService_I_NEXT_HINTS_CONSTRUCTION = null;

                        /**
                         * A state identifier for hinTS preprocessing output votes.
                         * @member {com.hedera.hapi.node.state.hints.PreprocessingVote.$Properties|null|undefined} HintsService_I_PREPROCESSING_VOTES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HintsService_I_PREPROCESSING_VOTES = null;

                        /**
                         * A state identifier for the entity counts. Singleton state.
                         * @member {com.hedera.hapi.node.state.entity.EntityCounts.$Properties|null|undefined} EntityIdService_I_ENTITY_COUNTS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.EntityIdService_I_ENTITY_COUNTS = null;

                        /**
                         * A state identifier for the ledger id. Singleton state.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} HistoryService_I_LEDGER_ID
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HistoryService_I_LEDGER_ID = null;

                        /**
                         * A state identifier for history proof key sets.
                         * @member {com.hedera.hapi.node.state.history.ProofKeySet.$Properties|null|undefined} HistoryService_I_PROOF_KEY_SETS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HistoryService_I_PROOF_KEY_SETS = null;

                        /**
                         * A state identifier for the active proof construction. Singleton state.
                         * @member {com.hedera.hapi.node.state.history.HistoryProofConstruction.$Properties|null|undefined} HistoryService_I_ACTIVE_PROOF_CONSTRUCTION
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION = null;

                        /**
                         * A state identifier for the next proof construction. Singleton state.
                         * @member {com.hedera.hapi.node.state.history.HistoryProofConstruction.$Properties|null|undefined} HistoryService_I_NEXT_PROOF_CONSTRUCTION
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HistoryService_I_NEXT_PROOF_CONSTRUCTION = null;

                        /**
                         * A state identifier for signatures on roster transition histories.
                         * @member {com.hedera.hapi.node.state.history.RecordedHistorySignature.$Properties|null|undefined} HistoryService_I_HISTORY_SIGNATURES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HistoryService_I_HISTORY_SIGNATURES = null;

                        /**
                         * A state identifier for votes on history proofs.
                         * @member {com.hedera.hapi.node.state.history.HistoryProofVote.$Properties|null|undefined} HistoryService_I_PROOF_VOTES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HistoryService_I_PROOF_VOTES = null;

                        /**
                         * A state identifier for the CRS state. Singleton state.
                         * @member {com.hedera.hapi.node.state.hints.CRSState.$Properties|null|undefined} HintsService_I_CRS_STATE
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HintsService_I_CRS_STATE = null;

                        /**
                         * A state identifier for the CRS publications.
                         * @member {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties|null|undefined} HintsService_I_CRS_PUBLICATIONS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HintsService_I_CRS_PUBLICATIONS = null;

                        /**
                         * A state identifier for the node rewards. Singleton state.
                         * @member {proto.NodeRewards.$Properties|null|undefined} TokenService_I_NODE_REWARDS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TokenService_I_NODE_REWARDS = null;

                        /**
                         * A state identifier for EVM hook states.
                         * @member {com.hedera.hapi.node.state.hooks.EvmHookState.$Properties|null|undefined} ContractService_I_EVM_HOOK_STATES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.ContractService_I_EVM_HOOK_STATES = null;

                        /**
                         * A state identifier for EVM hook storage.
                         * @member {proto.SlotValue.$Properties|null|undefined} ContractService_I_EVM_HOOK_STORAGE
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.ContractService_I_EVM_HOOK_STORAGE = null;

                        /**
                         * A state identifier for account to node relationships.
                         * @member {com.hedera.hapi.platform.state.NodeId.$Properties|null|undefined} AddressBookService_I_ACCOUNT_NODE_REL
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.AddressBookService_I_ACCOUNT_NODE_REL = null;

                        /**
                         * A state identifier for WRAPS message histories used in a roster transition proof.
                         * @member {com.hedera.hapi.node.state.history.WrapsMessageHistory.$Properties|null|undefined} HistoryService_I_WRAPS_MESSAGE_HISTORIES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HistoryService_I_WRAPS_MESSAGE_HISTORIES = null;

                        /**
                         * A state identifier for the node payments. Singleton state.
                         * @member {com.hedera.hapi.node.state.token.NodePayments.$Properties|null|undefined} TokenService_I_NODE_PAYMENTS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.TokenService_I_NODE_PAYMENTS = null;

                        /**
                         * A state identifier for registered nodes.
                         * @member {com.hedera.hapi.node.state.addressbook.RegisteredNode.$Properties|null|undefined} AddressBookService_I_REGISTERED_NODES
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.AddressBookService_I_REGISTERED_NODES = null;

                        /**
                         * A state identifier for the next node Identifier. Singleton state.
                         * @member {com.hedera.hapi.platform.state.NodeId.$Properties|null|undefined} EntityIdService_I_HIGHEST_NODE_ID
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.EntityIdService_I_HIGHEST_NODE_ID = null;

                        /**
                         * A state identifier for the expected WRAPS proving key hash. Singleton state.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} HistoryService_I_WRAPS_PROVING_KEY_HASH
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.HistoryService_I_WRAPS_PROVING_KEY_HASH = null;

                        /**
                         * Metadata of the round receipts queue.
                         * @member {proto.TransactionReceiptEntries.$Properties|null|undefined} RecordCache_I_TRANSACTION_RECEIPTS
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.RecordCache_I_TRANSACTION_RECEIPTS = null;

                        /**
                         * Metadata of the queue state.<br/>
                         * Reasons for the field number:
                         * <ul>
                         * <li>Intermediate state types are expected to be rare (field numbers 8000-9999 are allocated for that purpose).</li>
                         * <li>QueueState represents internal head/tail pointers of a queue, not a standalone Queue type (as with Singleton or KV).</li>
                         * </ul>
                         * @member {com.hedera.hapi.platform.state.QueueState.$Properties|null|undefined} queueState
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.queueState = null;

                        /**
                         * Metadata of the `150` upgrade file data queue.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} FileService_I_UPGRADE_DATA_150
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FileService_I_UPGRADE_DATA_150 = null;

                        /**
                         * Metadata of the`151` upgrade file data queue.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} FileService_I_UPGRADE_DATA_151
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FileService_I_UPGRADE_DATA_151 = null;

                        /**
                         * Metadata of the `152` upgrade file data queue.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} FileService_I_UPGRADE_DATA_152
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FileService_I_UPGRADE_DATA_152 = null;

                        /**
                         * Metadata of the `153` upgrade file data queue.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} FileService_I_UPGRADE_DATA_153
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FileService_I_UPGRADE_DATA_153 = null;

                        /**
                         * Metadata of the`154` upgrade file data queue.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} FileService_I_UPGRADE_DATA_154
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FileService_I_UPGRADE_DATA_154 = null;

                        /**
                         * Metadata of the `155` upgrade file data queue.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} FileService_I_UPGRADE_DATA_155
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FileService_I_UPGRADE_DATA_155 = null;

                        /**
                         * Metadata of the `156` upgrade file data queue.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} FileService_I_UPGRADE_DATA_156
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FileService_I_UPGRADE_DATA_156 = null;

                        /**
                         * Metadata of the `157` upgrade file data queue.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} FileService_I_UPGRADE_DATA_157
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FileService_I_UPGRADE_DATA_157 = null;

                        /**
                         * Metadata of the `158` upgrade file data queue.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} FileService_I_UPGRADE_DATA_158
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FileService_I_UPGRADE_DATA_158 = null;

                        /**
                         * Metadata of the `159` upgrade file data queue.
                         * @member {proto.ProtoBytes.$Properties|null|undefined} FileService_I_UPGRADE_DATA_159
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        StateValue.prototype.FileService_I_UPGRADE_DATA_159 = null;

                        // OneOf field names bound to virtual getters and setters
                        let $oneOfFields;

                        /**
                         * StateValue value.
                         * @member {"EntityIdService_I_ENTITY_ID"|"TokenService_I_ACCOUNTS"|"TokenService_I_ALIASES"|"ContractService_I_STORAGE"|"ContractService_I_BYTECODE"|"FileService_I_FILES"|"TokenService_I_TOKENS"|"TokenService_I_NFTS"|"TokenService_I_TOKEN_RELS"|"TokenService_I_STAKING_INFOS"|"TokenService_I_STAKING_NETWORK_REWARDS"|"CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS"|"CongestionThrottleService_I_CONGESTION_LEVEL_STARTS"|"ScheduleService_I_SCHEDULES_BY_ID"|"ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC"|"ScheduleService_I_SCHEDULES_BY_EQUALITY"|"FeeService_I_MIDNIGHT_RATES"|"BlockRecordService_I_RUNNING_HASHES"|"BlockRecordService_I_BLOCKS"|"AddressBookService_I_NODES"|"ConsensusService_I_TOPICS"|"FreezeService_I_UPGRADE_FILE_HASH"|"FreezeService_I_FREEZE_TIME"|"BlockStreamService_I_BLOCK_STREAM_INFO"|"TokenService_I_PENDING_AIRDROPS"|"PlatformStateService_I_PLATFORM_STATE"|"RosterService_I_ROSTER_STATE"|"RosterService_I_ROSTERS"|"ScheduleService_I_SCHEDULED_COUNTS"|"ScheduleService_I_SCHEDULE_ID_BY_EQUALITY"|"TssBaseService_I_TSS_MESSAGES"|"TssBaseService_I_TSS_VOTES"|"ScheduleService_I_SCHEDULED_ORDERS"|"ScheduleService_I_SCHEDULED_USAGES"|"TssBaseService_I_TSS_ENCRYPTION_KEYS"|"HintsService_I_HINTS_KEY_SETS"|"HintsService_I_ACTIVE_HINTS_CONSTRUCTION"|"HintsService_I_NEXT_HINTS_CONSTRUCTION"|"HintsService_I_PREPROCESSING_VOTES"|"EntityIdService_I_ENTITY_COUNTS"|"HistoryService_I_LEDGER_ID"|"HistoryService_I_PROOF_KEY_SETS"|"HistoryService_I_ACTIVE_PROOF_CONSTRUCTION"|"HistoryService_I_NEXT_PROOF_CONSTRUCTION"|"HistoryService_I_HISTORY_SIGNATURES"|"HistoryService_I_PROOF_VOTES"|"HintsService_I_CRS_STATE"|"HintsService_I_CRS_PUBLICATIONS"|"TokenService_I_NODE_REWARDS"|"ContractService_I_EVM_HOOK_STATES"|"ContractService_I_EVM_HOOK_STORAGE"|"AddressBookService_I_ACCOUNT_NODE_REL"|"HistoryService_I_WRAPS_MESSAGE_HISTORIES"|"TokenService_I_NODE_PAYMENTS"|"AddressBookService_I_REGISTERED_NODES"|"EntityIdService_I_HIGHEST_NODE_ID"|"HistoryService_I_WRAPS_PROVING_KEY_HASH"|"RecordCache_I_TRANSACTION_RECEIPTS"|"queueState"|"FileService_I_UPGRADE_DATA_150"|"FileService_I_UPGRADE_DATA_151"|"FileService_I_UPGRADE_DATA_152"|"FileService_I_UPGRADE_DATA_153"|"FileService_I_UPGRADE_DATA_154"|"FileService_I_UPGRADE_DATA_155"|"FileService_I_UPGRADE_DATA_156"|"FileService_I_UPGRADE_DATA_157"|"FileService_I_UPGRADE_DATA_158"|"FileService_I_UPGRADE_DATA_159"|undefined} value
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         */
                        $Object.defineProperty(StateValue.prototype, "value", {
                            get: $util.oneOfGetter($oneOfFields = ["EntityIdService_I_ENTITY_ID", "TokenService_I_ACCOUNTS", "TokenService_I_ALIASES", "ContractService_I_STORAGE", "ContractService_I_BYTECODE", "FileService_I_FILES", "TokenService_I_TOKENS", "TokenService_I_NFTS", "TokenService_I_TOKEN_RELS", "TokenService_I_STAKING_INFOS", "TokenService_I_STAKING_NETWORK_REWARDS", "CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS", "CongestionThrottleService_I_CONGESTION_LEVEL_STARTS", "ScheduleService_I_SCHEDULES_BY_ID", "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC", "ScheduleService_I_SCHEDULES_BY_EQUALITY", "FeeService_I_MIDNIGHT_RATES", "BlockRecordService_I_RUNNING_HASHES", "BlockRecordService_I_BLOCKS", "AddressBookService_I_NODES", "ConsensusService_I_TOPICS", "FreezeService_I_UPGRADE_FILE_HASH", "FreezeService_I_FREEZE_TIME", "BlockStreamService_I_BLOCK_STREAM_INFO", "TokenService_I_PENDING_AIRDROPS", "PlatformStateService_I_PLATFORM_STATE", "RosterService_I_ROSTER_STATE", "RosterService_I_ROSTERS", "ScheduleService_I_SCHEDULED_COUNTS", "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY", "TssBaseService_I_TSS_MESSAGES", "TssBaseService_I_TSS_VOTES", "ScheduleService_I_SCHEDULED_ORDERS", "ScheduleService_I_SCHEDULED_USAGES", "TssBaseService_I_TSS_ENCRYPTION_KEYS", "HintsService_I_HINTS_KEY_SETS", "HintsService_I_ACTIVE_HINTS_CONSTRUCTION", "HintsService_I_NEXT_HINTS_CONSTRUCTION", "HintsService_I_PREPROCESSING_VOTES", "EntityIdService_I_ENTITY_COUNTS", "HistoryService_I_LEDGER_ID", "HistoryService_I_PROOF_KEY_SETS", "HistoryService_I_ACTIVE_PROOF_CONSTRUCTION", "HistoryService_I_NEXT_PROOF_CONSTRUCTION", "HistoryService_I_HISTORY_SIGNATURES", "HistoryService_I_PROOF_VOTES", "HintsService_I_CRS_STATE", "HintsService_I_CRS_PUBLICATIONS", "TokenService_I_NODE_REWARDS", "ContractService_I_EVM_HOOK_STATES", "ContractService_I_EVM_HOOK_STORAGE", "AddressBookService_I_ACCOUNT_NODE_REL", "HistoryService_I_WRAPS_MESSAGE_HISTORIES", "TokenService_I_NODE_PAYMENTS", "AddressBookService_I_REGISTERED_NODES", "EntityIdService_I_HIGHEST_NODE_ID", "HistoryService_I_WRAPS_PROVING_KEY_HASH", "RecordCache_I_TRANSACTION_RECEIPTS", "queueState", "FileService_I_UPGRADE_DATA_150", "FileService_I_UPGRADE_DATA_151", "FileService_I_UPGRADE_DATA_152", "FileService_I_UPGRADE_DATA_153", "FileService_I_UPGRADE_DATA_154", "FileService_I_UPGRADE_DATA_155", "FileService_I_UPGRADE_DATA_156", "FileService_I_UPGRADE_DATA_157", "FileService_I_UPGRADE_DATA_158", "FileService_I_UPGRADE_DATA_159"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * Creates a new StateValue instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @static
                         * @param {com.hedera.hapi.platform.state.StateValue.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.platform.state.StateValue} StateValue instance
                         * @type {{
                         *   (properties: com.hedera.hapi.platform.state.StateValue.$Shape): com.hedera.hapi.platform.state.StateValue & com.hedera.hapi.platform.state.StateValue.$Shape;
                         *   (properties?: com.hedera.hapi.platform.state.StateValue.$Properties): com.hedera.hapi.platform.state.StateValue;
                         * }}
                         */
                        StateValue.create = function(properties) {
                            return new StateValue(properties);
                        };

                        /**
                         * Encodes the specified StateValue message. Does not implicitly {@link com.hedera.hapi.platform.state.StateValue.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @static
                         * @param {com.hedera.hapi.platform.state.StateValue.$Properties} message StateValue message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        StateValue.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.EntityIdService_I_ENTITY_ID != null && $Object.hasOwnProperty.call(message, "EntityIdService_I_ENTITY_ID"))
                                $root.proto.EntityNumber.encode(message.EntityIdService_I_ENTITY_ID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_ACCOUNTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_ACCOUNTS"))
                                $root.proto.Account.encode(message.TokenService_I_ACCOUNTS, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_ALIASES != null && $Object.hasOwnProperty.call(message, "TokenService_I_ALIASES"))
                                $root.proto.AccountID.encode(message.TokenService_I_ALIASES, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.ContractService_I_STORAGE != null && $Object.hasOwnProperty.call(message, "ContractService_I_STORAGE"))
                                $root.proto.SlotValue.encode(message.ContractService_I_STORAGE, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                            if (message.ContractService_I_BYTECODE != null && $Object.hasOwnProperty.call(message, "ContractService_I_BYTECODE"))
                                $root.proto.Bytecode.encode(message.ContractService_I_BYTECODE, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                            if (message.FileService_I_FILES != null && $Object.hasOwnProperty.call(message, "FileService_I_FILES"))
                                $root.proto.File.encode(message.FileService_I_FILES, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_TOKENS != null && $Object.hasOwnProperty.call(message, "TokenService_I_TOKENS"))
                                $root.proto.Token.encode(message.TokenService_I_TOKENS, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_NFTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_NFTS"))
                                $root.proto.Nft.encode(message.TokenService_I_NFTS, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_TOKEN_RELS != null && $Object.hasOwnProperty.call(message, "TokenService_I_TOKEN_RELS"))
                                $root.proto.TokenRelation.encode(message.TokenService_I_TOKEN_RELS, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_STAKING_INFOS != null && $Object.hasOwnProperty.call(message, "TokenService_I_STAKING_INFOS"))
                                $root.proto.StakingNodeInfo.encode(message.TokenService_I_STAKING_INFOS, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_STAKING_NETWORK_REWARDS != null && $Object.hasOwnProperty.call(message, "TokenService_I_STAKING_NETWORK_REWARDS"))
                                $root.proto.NetworkStakingRewards.encode(message.TokenService_I_STAKING_NETWORK_REWARDS, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                            if (message.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS != null && $Object.hasOwnProperty.call(message, "CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS"))
                                $root.proto.ThrottleUsageSnapshots.encode(message.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
                            if (message.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS != null && $Object.hasOwnProperty.call(message, "CongestionThrottleService_I_CONGESTION_LEVEL_STARTS"))
                                $root.proto.CongestionLevelStarts.encode(message.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULES_BY_ID != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_ID"))
                                $root.proto.Schedule.encode(message.ScheduleService_I_SCHEDULES_BY_ID, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC"))
                                $root.proto.ScheduleList.encode(message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULES_BY_EQUALITY != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_EQUALITY"))
                                $root.proto.ScheduleList.encode(message.ScheduleService_I_SCHEDULES_BY_EQUALITY, writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
                            if (message.FeeService_I_MIDNIGHT_RATES != null && $Object.hasOwnProperty.call(message, "FeeService_I_MIDNIGHT_RATES"))
                                $root.proto.ExchangeRateSet.encode(message.FeeService_I_MIDNIGHT_RATES, writer.uint32(/* id 17, wireType 2 =*/138).fork(), _depth + 1).ldelim();
                            if (message.BlockRecordService_I_RUNNING_HASHES != null && $Object.hasOwnProperty.call(message, "BlockRecordService_I_RUNNING_HASHES"))
                                $root.proto.RunningHashes.encode(message.BlockRecordService_I_RUNNING_HASHES, writer.uint32(/* id 18, wireType 2 =*/146).fork(), _depth + 1).ldelim();
                            if (message.BlockRecordService_I_BLOCKS != null && $Object.hasOwnProperty.call(message, "BlockRecordService_I_BLOCKS"))
                                $root.proto.BlockInfo.encode(message.BlockRecordService_I_BLOCKS, writer.uint32(/* id 19, wireType 2 =*/154).fork(), _depth + 1).ldelim();
                            if (message.AddressBookService_I_NODES != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_NODES"))
                                $root.com.hedera.hapi.node.state.addressbook.Node.encode(message.AddressBookService_I_NODES, writer.uint32(/* id 20, wireType 2 =*/162).fork(), _depth + 1).ldelim();
                            if (message.ConsensusService_I_TOPICS != null && $Object.hasOwnProperty.call(message, "ConsensusService_I_TOPICS"))
                                $root.proto.Topic.encode(message.ConsensusService_I_TOPICS, writer.uint32(/* id 21, wireType 2 =*/170).fork(), _depth + 1).ldelim();
                            if (message.FreezeService_I_UPGRADE_FILE_HASH != null && $Object.hasOwnProperty.call(message, "FreezeService_I_UPGRADE_FILE_HASH"))
                                $root.proto.ProtoBytes.encode(message.FreezeService_I_UPGRADE_FILE_HASH, writer.uint32(/* id 22, wireType 2 =*/178).fork(), _depth + 1).ldelim();
                            if (message.FreezeService_I_FREEZE_TIME != null && $Object.hasOwnProperty.call(message, "FreezeService_I_FREEZE_TIME"))
                                $root.proto.Timestamp.encode(message.FreezeService_I_FREEZE_TIME, writer.uint32(/* id 23, wireType 2 =*/186).fork(), _depth + 1).ldelim();
                            if (message.BlockStreamService_I_BLOCK_STREAM_INFO != null && $Object.hasOwnProperty.call(message, "BlockStreamService_I_BLOCK_STREAM_INFO"))
                                $root.com.hedera.hapi.node.state.blockstream.BlockStreamInfo.encode(message.BlockStreamService_I_BLOCK_STREAM_INFO, writer.uint32(/* id 24, wireType 2 =*/194).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_PENDING_AIRDROPS != null && $Object.hasOwnProperty.call(message, "TokenService_I_PENDING_AIRDROPS"))
                                $root.proto.AccountPendingAirdrop.encode(message.TokenService_I_PENDING_AIRDROPS, writer.uint32(/* id 25, wireType 2 =*/202).fork(), _depth + 1).ldelim();
                            if (message.PlatformStateService_I_PLATFORM_STATE != null && $Object.hasOwnProperty.call(message, "PlatformStateService_I_PLATFORM_STATE"))
                                $root.com.hedera.hapi.platform.state.PlatformState.encode(message.PlatformStateService_I_PLATFORM_STATE, writer.uint32(/* id 26, wireType 2 =*/210).fork(), _depth + 1).ldelim();
                            if (message.RosterService_I_ROSTER_STATE != null && $Object.hasOwnProperty.call(message, "RosterService_I_ROSTER_STATE"))
                                $root.com.hedera.hapi.node.state.roster.RosterState.encode(message.RosterService_I_ROSTER_STATE, writer.uint32(/* id 27, wireType 2 =*/218).fork(), _depth + 1).ldelim();
                            if (message.RosterService_I_ROSTERS != null && $Object.hasOwnProperty.call(message, "RosterService_I_ROSTERS"))
                                $root.com.hedera.hapi.node.state.roster.Roster.encode(message.RosterService_I_ROSTERS, writer.uint32(/* id 28, wireType 2 =*/226).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULED_COUNTS != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_COUNTS"))
                                $root.proto.ScheduledCounts.encode(message.ScheduleService_I_SCHEDULED_COUNTS, writer.uint32(/* id 29, wireType 2 =*/234).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY"))
                                $root.proto.ScheduleID.encode(message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY, writer.uint32(/* id 30, wireType 2 =*/242).fork(), _depth + 1).ldelim();
                            if (message.TssBaseService_I_TSS_MESSAGES != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_MESSAGES"))
                                $root.com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.encode(message.TssBaseService_I_TSS_MESSAGES, writer.uint32(/* id 31, wireType 2 =*/250).fork(), _depth + 1).ldelim();
                            if (message.TssBaseService_I_TSS_VOTES != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_VOTES"))
                                $root.com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.encode(message.TssBaseService_I_TSS_VOTES, writer.uint32(/* id 32, wireType 2 =*/258).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULED_ORDERS != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_ORDERS"))
                                $root.proto.ScheduleID.encode(message.ScheduleService_I_SCHEDULED_ORDERS, writer.uint32(/* id 33, wireType 2 =*/266).fork(), _depth + 1).ldelim();
                            if (message.ScheduleService_I_SCHEDULED_USAGES != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_USAGES"))
                                $root.proto.ThrottleUsageSnapshots.encode(message.ScheduleService_I_SCHEDULED_USAGES, writer.uint32(/* id 34, wireType 2 =*/274).fork(), _depth + 1).ldelim();
                            if (message.TssBaseService_I_TSS_ENCRYPTION_KEYS != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_ENCRYPTION_KEYS"))
                                $root.com.hedera.hapi.node.state.tss.TssEncryptionKeys.encode(message.TssBaseService_I_TSS_ENCRYPTION_KEYS, writer.uint32(/* id 35, wireType 2 =*/282).fork(), _depth + 1).ldelim();
                            if (message.HintsService_I_HINTS_KEY_SETS != null && $Object.hasOwnProperty.call(message, "HintsService_I_HINTS_KEY_SETS"))
                                $root.com.hedera.hapi.node.state.hints.HintsKeySet.encode(message.HintsService_I_HINTS_KEY_SETS, writer.uint32(/* id 37, wireType 2 =*/298).fork(), _depth + 1).ldelim();
                            if (message.HintsService_I_ACTIVE_HINTS_CONSTRUCTION != null && $Object.hasOwnProperty.call(message, "HintsService_I_ACTIVE_HINTS_CONSTRUCTION"))
                                $root.com.hedera.hapi.node.state.hints.HintsConstruction.encode(message.HintsService_I_ACTIVE_HINTS_CONSTRUCTION, writer.uint32(/* id 38, wireType 2 =*/306).fork(), _depth + 1).ldelim();
                            if (message.HintsService_I_NEXT_HINTS_CONSTRUCTION != null && $Object.hasOwnProperty.call(message, "HintsService_I_NEXT_HINTS_CONSTRUCTION"))
                                $root.com.hedera.hapi.node.state.hints.HintsConstruction.encode(message.HintsService_I_NEXT_HINTS_CONSTRUCTION, writer.uint32(/* id 39, wireType 2 =*/314).fork(), _depth + 1).ldelim();
                            if (message.HintsService_I_PREPROCESSING_VOTES != null && $Object.hasOwnProperty.call(message, "HintsService_I_PREPROCESSING_VOTES"))
                                $root.com.hedera.hapi.node.state.hints.PreprocessingVote.encode(message.HintsService_I_PREPROCESSING_VOTES, writer.uint32(/* id 40, wireType 2 =*/322).fork(), _depth + 1).ldelim();
                            if (message.EntityIdService_I_ENTITY_COUNTS != null && $Object.hasOwnProperty.call(message, "EntityIdService_I_ENTITY_COUNTS"))
                                $root.com.hedera.hapi.node.state.entity.EntityCounts.encode(message.EntityIdService_I_ENTITY_COUNTS, writer.uint32(/* id 41, wireType 2 =*/330).fork(), _depth + 1).ldelim();
                            if (message.HistoryService_I_LEDGER_ID != null && $Object.hasOwnProperty.call(message, "HistoryService_I_LEDGER_ID"))
                                $root.proto.ProtoBytes.encode(message.HistoryService_I_LEDGER_ID, writer.uint32(/* id 42, wireType 2 =*/338).fork(), _depth + 1).ldelim();
                            if (message.HistoryService_I_PROOF_KEY_SETS != null && $Object.hasOwnProperty.call(message, "HistoryService_I_PROOF_KEY_SETS"))
                                $root.com.hedera.hapi.node.state.history.ProofKeySet.encode(message.HistoryService_I_PROOF_KEY_SETS, writer.uint32(/* id 43, wireType 2 =*/346).fork(), _depth + 1).ldelim();
                            if (message.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION != null && $Object.hasOwnProperty.call(message, "HistoryService_I_ACTIVE_PROOF_CONSTRUCTION"))
                                $root.com.hedera.hapi.node.state.history.HistoryProofConstruction.encode(message.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION, writer.uint32(/* id 44, wireType 2 =*/354).fork(), _depth + 1).ldelim();
                            if (message.HistoryService_I_NEXT_PROOF_CONSTRUCTION != null && $Object.hasOwnProperty.call(message, "HistoryService_I_NEXT_PROOF_CONSTRUCTION"))
                                $root.com.hedera.hapi.node.state.history.HistoryProofConstruction.encode(message.HistoryService_I_NEXT_PROOF_CONSTRUCTION, writer.uint32(/* id 45, wireType 2 =*/362).fork(), _depth + 1).ldelim();
                            if (message.HistoryService_I_HISTORY_SIGNATURES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_HISTORY_SIGNATURES"))
                                $root.com.hedera.hapi.node.state.history.RecordedHistorySignature.encode(message.HistoryService_I_HISTORY_SIGNATURES, writer.uint32(/* id 46, wireType 2 =*/370).fork(), _depth + 1).ldelim();
                            if (message.HistoryService_I_PROOF_VOTES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_PROOF_VOTES"))
                                $root.com.hedera.hapi.node.state.history.HistoryProofVote.encode(message.HistoryService_I_PROOF_VOTES, writer.uint32(/* id 47, wireType 2 =*/378).fork(), _depth + 1).ldelim();
                            if (message.HintsService_I_CRS_STATE != null && $Object.hasOwnProperty.call(message, "HintsService_I_CRS_STATE"))
                                $root.com.hedera.hapi.node.state.hints.CRSState.encode(message.HintsService_I_CRS_STATE, writer.uint32(/* id 48, wireType 2 =*/386).fork(), _depth + 1).ldelim();
                            if (message.HintsService_I_CRS_PUBLICATIONS != null && $Object.hasOwnProperty.call(message, "HintsService_I_CRS_PUBLICATIONS"))
                                $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.encode(message.HintsService_I_CRS_PUBLICATIONS, writer.uint32(/* id 49, wireType 2 =*/394).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_NODE_REWARDS != null && $Object.hasOwnProperty.call(message, "TokenService_I_NODE_REWARDS"))
                                $root.proto.NodeRewards.encode(message.TokenService_I_NODE_REWARDS, writer.uint32(/* id 50, wireType 2 =*/402).fork(), _depth + 1).ldelim();
                            if (message.ContractService_I_EVM_HOOK_STATES != null && $Object.hasOwnProperty.call(message, "ContractService_I_EVM_HOOK_STATES"))
                                $root.com.hedera.hapi.node.state.hooks.EvmHookState.encode(message.ContractService_I_EVM_HOOK_STATES, writer.uint32(/* id 51, wireType 2 =*/410).fork(), _depth + 1).ldelim();
                            if (message.ContractService_I_EVM_HOOK_STORAGE != null && $Object.hasOwnProperty.call(message, "ContractService_I_EVM_HOOK_STORAGE"))
                                $root.proto.SlotValue.encode(message.ContractService_I_EVM_HOOK_STORAGE, writer.uint32(/* id 52, wireType 2 =*/418).fork(), _depth + 1).ldelim();
                            if (message.AddressBookService_I_ACCOUNT_NODE_REL != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_ACCOUNT_NODE_REL"))
                                $root.com.hedera.hapi.platform.state.NodeId.encode(message.AddressBookService_I_ACCOUNT_NODE_REL, writer.uint32(/* id 53, wireType 2 =*/426).fork(), _depth + 1).ldelim();
                            if (message.HistoryService_I_WRAPS_MESSAGE_HISTORIES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_WRAPS_MESSAGE_HISTORIES"))
                                $root.com.hedera.hapi.node.state.history.WrapsMessageHistory.encode(message.HistoryService_I_WRAPS_MESSAGE_HISTORIES, writer.uint32(/* id 54, wireType 2 =*/434).fork(), _depth + 1).ldelim();
                            if (message.TokenService_I_NODE_PAYMENTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_NODE_PAYMENTS"))
                                $root.com.hedera.hapi.node.state.token.NodePayments.encode(message.TokenService_I_NODE_PAYMENTS, writer.uint32(/* id 55, wireType 2 =*/442).fork(), _depth + 1).ldelim();
                            if (message.AddressBookService_I_REGISTERED_NODES != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_REGISTERED_NODES"))
                                $root.com.hedera.hapi.node.state.addressbook.RegisteredNode.encode(message.AddressBookService_I_REGISTERED_NODES, writer.uint32(/* id 56, wireType 2 =*/450).fork(), _depth + 1).ldelim();
                            if (message.EntityIdService_I_HIGHEST_NODE_ID != null && $Object.hasOwnProperty.call(message, "EntityIdService_I_HIGHEST_NODE_ID"))
                                $root.com.hedera.hapi.platform.state.NodeId.encode(message.EntityIdService_I_HIGHEST_NODE_ID, writer.uint32(/* id 57, wireType 2 =*/458).fork(), _depth + 1).ldelim();
                            if (message.HistoryService_I_WRAPS_PROVING_KEY_HASH != null && $Object.hasOwnProperty.call(message, "HistoryService_I_WRAPS_PROVING_KEY_HASH"))
                                $root.proto.ProtoBytes.encode(message.HistoryService_I_WRAPS_PROVING_KEY_HASH, writer.uint32(/* id 58, wireType 2 =*/466).fork(), _depth + 1).ldelim();
                            if (message.RecordCache_I_TRANSACTION_RECEIPTS != null && $Object.hasOwnProperty.call(message, "RecordCache_I_TRANSACTION_RECEIPTS"))
                                $root.proto.TransactionReceiptEntries.encode(message.RecordCache_I_TRANSACTION_RECEIPTS, writer.uint32(/* id 126, wireType 2 =*/1010).fork(), _depth + 1).ldelim();
                            if (message.queueState != null && $Object.hasOwnProperty.call(message, "queueState"))
                                $root.com.hedera.hapi.platform.state.QueueState.encode(message.queueState, writer.uint32(/* id 8001, wireType 2 =*/64010).fork(), _depth + 1).ldelim();
                            if (message.FileService_I_UPGRADE_DATA_150 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_150"))
                                $root.proto.ProtoBytes.encode(message.FileService_I_UPGRADE_DATA_150, writer.uint32(/* id 10001, wireType 2 =*/80010).fork(), _depth + 1).ldelim();
                            if (message.FileService_I_UPGRADE_DATA_151 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_151"))
                                $root.proto.ProtoBytes.encode(message.FileService_I_UPGRADE_DATA_151, writer.uint32(/* id 10002, wireType 2 =*/80018).fork(), _depth + 1).ldelim();
                            if (message.FileService_I_UPGRADE_DATA_152 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_152"))
                                $root.proto.ProtoBytes.encode(message.FileService_I_UPGRADE_DATA_152, writer.uint32(/* id 10003, wireType 2 =*/80026).fork(), _depth + 1).ldelim();
                            if (message.FileService_I_UPGRADE_DATA_153 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_153"))
                                $root.proto.ProtoBytes.encode(message.FileService_I_UPGRADE_DATA_153, writer.uint32(/* id 10004, wireType 2 =*/80034).fork(), _depth + 1).ldelim();
                            if (message.FileService_I_UPGRADE_DATA_154 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_154"))
                                $root.proto.ProtoBytes.encode(message.FileService_I_UPGRADE_DATA_154, writer.uint32(/* id 10005, wireType 2 =*/80042).fork(), _depth + 1).ldelim();
                            if (message.FileService_I_UPGRADE_DATA_155 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_155"))
                                $root.proto.ProtoBytes.encode(message.FileService_I_UPGRADE_DATA_155, writer.uint32(/* id 10006, wireType 2 =*/80050).fork(), _depth + 1).ldelim();
                            if (message.FileService_I_UPGRADE_DATA_156 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_156"))
                                $root.proto.ProtoBytes.encode(message.FileService_I_UPGRADE_DATA_156, writer.uint32(/* id 10007, wireType 2 =*/80058).fork(), _depth + 1).ldelim();
                            if (message.FileService_I_UPGRADE_DATA_157 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_157"))
                                $root.proto.ProtoBytes.encode(message.FileService_I_UPGRADE_DATA_157, writer.uint32(/* id 10008, wireType 2 =*/80066).fork(), _depth + 1).ldelim();
                            if (message.FileService_I_UPGRADE_DATA_158 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_158"))
                                $root.proto.ProtoBytes.encode(message.FileService_I_UPGRADE_DATA_158, writer.uint32(/* id 10009, wireType 2 =*/80074).fork(), _depth + 1).ldelim();
                            if (message.FileService_I_UPGRADE_DATA_159 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_159"))
                                $root.proto.ProtoBytes.encode(message.FileService_I_UPGRADE_DATA_159, writer.uint32(/* id 10010, wireType 2 =*/80082).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified StateValue message, length delimited. Does not implicitly {@link com.hedera.hapi.platform.state.StateValue.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @static
                         * @param {com.hedera.hapi.platform.state.StateValue.$Properties} message StateValue message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        StateValue.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a StateValue message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.platform.state.StateValue & com.hedera.hapi.platform.state.StateValue.$Shape} StateValue
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        StateValue.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.platform.state.StateValue();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.EntityIdService_I_ENTITY_ID = $root.proto.EntityNumber.decode(reader, reader.uint32(), $undefined, _depth + 1, message.EntityIdService_I_ENTITY_ID);
                                        message.value = "EntityIdService_I_ENTITY_ID";
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_ACCOUNTS = $root.proto.Account.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_ACCOUNTS);
                                        message.value = "TokenService_I_ACCOUNTS";
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_ALIASES = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_ALIASES);
                                        message.value = "TokenService_I_ALIASES";
                                        continue;
                                    }
                                case 4: {
                                        if (wireType !== 2)
                                            break;
                                        message.ContractService_I_STORAGE = $root.proto.SlotValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ContractService_I_STORAGE);
                                        message.value = "ContractService_I_STORAGE";
                                        continue;
                                    }
                                case 5: {
                                        if (wireType !== 2)
                                            break;
                                        message.ContractService_I_BYTECODE = $root.proto.Bytecode.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ContractService_I_BYTECODE);
                                        message.value = "ContractService_I_BYTECODE";
                                        continue;
                                    }
                                case 6: {
                                        if (wireType !== 2)
                                            break;
                                        message.FileService_I_FILES = $root.proto.File.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FileService_I_FILES);
                                        message.value = "FileService_I_FILES";
                                        continue;
                                    }
                                case 7: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_TOKENS = $root.proto.Token.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_TOKENS);
                                        message.value = "TokenService_I_TOKENS";
                                        continue;
                                    }
                                case 8: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_NFTS = $root.proto.Nft.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_NFTS);
                                        message.value = "TokenService_I_NFTS";
                                        continue;
                                    }
                                case 9: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_TOKEN_RELS = $root.proto.TokenRelation.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_TOKEN_RELS);
                                        message.value = "TokenService_I_TOKEN_RELS";
                                        continue;
                                    }
                                case 10: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_STAKING_INFOS = $root.proto.StakingNodeInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_STAKING_INFOS);
                                        message.value = "TokenService_I_STAKING_INFOS";
                                        continue;
                                    }
                                case 11: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_STAKING_NETWORK_REWARDS = $root.proto.NetworkStakingRewards.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_STAKING_NETWORK_REWARDS);
                                        message.value = "TokenService_I_STAKING_NETWORK_REWARDS";
                                        continue;
                                    }
                                case 12: {
                                        if (wireType !== 2)
                                            break;
                                        message.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS = $root.proto.ThrottleUsageSnapshots.decode(reader, reader.uint32(), $undefined, _depth + 1, message.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS);
                                        message.value = "CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS";
                                        continue;
                                    }
                                case 13: {
                                        if (wireType !== 2)
                                            break;
                                        message.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS = $root.proto.CongestionLevelStarts.decode(reader, reader.uint32(), $undefined, _depth + 1, message.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS);
                                        message.value = "CongestionThrottleService_I_CONGESTION_LEVEL_STARTS";
                                        continue;
                                    }
                                case 14: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULES_BY_ID = $root.proto.Schedule.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULES_BY_ID);
                                        message.value = "ScheduleService_I_SCHEDULES_BY_ID";
                                        continue;
                                    }
                                case 15: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC = $root.proto.ScheduleList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC);
                                        message.value = "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC";
                                        continue;
                                    }
                                case 16: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULES_BY_EQUALITY = $root.proto.ScheduleList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULES_BY_EQUALITY);
                                        message.value = "ScheduleService_I_SCHEDULES_BY_EQUALITY";
                                        continue;
                                    }
                                case 17: {
                                        if (wireType !== 2)
                                            break;
                                        message.FeeService_I_MIDNIGHT_RATES = $root.proto.ExchangeRateSet.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FeeService_I_MIDNIGHT_RATES);
                                        message.value = "FeeService_I_MIDNIGHT_RATES";
                                        continue;
                                    }
                                case 18: {
                                        if (wireType !== 2)
                                            break;
                                        message.BlockRecordService_I_RUNNING_HASHES = $root.proto.RunningHashes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.BlockRecordService_I_RUNNING_HASHES);
                                        message.value = "BlockRecordService_I_RUNNING_HASHES";
                                        continue;
                                    }
                                case 19: {
                                        if (wireType !== 2)
                                            break;
                                        message.BlockRecordService_I_BLOCKS = $root.proto.BlockInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.BlockRecordService_I_BLOCKS);
                                        message.value = "BlockRecordService_I_BLOCKS";
                                        continue;
                                    }
                                case 20: {
                                        if (wireType !== 2)
                                            break;
                                        message.AddressBookService_I_NODES = $root.com.hedera.hapi.node.state.addressbook.Node.decode(reader, reader.uint32(), $undefined, _depth + 1, message.AddressBookService_I_NODES);
                                        message.value = "AddressBookService_I_NODES";
                                        continue;
                                    }
                                case 21: {
                                        if (wireType !== 2)
                                            break;
                                        message.ConsensusService_I_TOPICS = $root.proto.Topic.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ConsensusService_I_TOPICS);
                                        message.value = "ConsensusService_I_TOPICS";
                                        continue;
                                    }
                                case 22: {
                                        if (wireType !== 2)
                                            break;
                                        message.FreezeService_I_UPGRADE_FILE_HASH = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FreezeService_I_UPGRADE_FILE_HASH);
                                        message.value = "FreezeService_I_UPGRADE_FILE_HASH";
                                        continue;
                                    }
                                case 23: {
                                        if (wireType !== 2)
                                            break;
                                        message.FreezeService_I_FREEZE_TIME = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FreezeService_I_FREEZE_TIME);
                                        message.value = "FreezeService_I_FREEZE_TIME";
                                        continue;
                                    }
                                case 24: {
                                        if (wireType !== 2)
                                            break;
                                        message.BlockStreamService_I_BLOCK_STREAM_INFO = $root.com.hedera.hapi.node.state.blockstream.BlockStreamInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.BlockStreamService_I_BLOCK_STREAM_INFO);
                                        message.value = "BlockStreamService_I_BLOCK_STREAM_INFO";
                                        continue;
                                    }
                                case 25: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_PENDING_AIRDROPS = $root.proto.AccountPendingAirdrop.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_PENDING_AIRDROPS);
                                        message.value = "TokenService_I_PENDING_AIRDROPS";
                                        continue;
                                    }
                                case 26: {
                                        if (wireType !== 2)
                                            break;
                                        message.PlatformStateService_I_PLATFORM_STATE = $root.com.hedera.hapi.platform.state.PlatformState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.PlatformStateService_I_PLATFORM_STATE);
                                        message.value = "PlatformStateService_I_PLATFORM_STATE";
                                        continue;
                                    }
                                case 27: {
                                        if (wireType !== 2)
                                            break;
                                        message.RosterService_I_ROSTER_STATE = $root.com.hedera.hapi.node.state.roster.RosterState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.RosterService_I_ROSTER_STATE);
                                        message.value = "RosterService_I_ROSTER_STATE";
                                        continue;
                                    }
                                case 28: {
                                        if (wireType !== 2)
                                            break;
                                        message.RosterService_I_ROSTERS = $root.com.hedera.hapi.node.state.roster.Roster.decode(reader, reader.uint32(), $undefined, _depth + 1, message.RosterService_I_ROSTERS);
                                        message.value = "RosterService_I_ROSTERS";
                                        continue;
                                    }
                                case 29: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULED_COUNTS = $root.proto.ScheduledCounts.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULED_COUNTS);
                                        message.value = "ScheduleService_I_SCHEDULED_COUNTS";
                                        continue;
                                    }
                                case 30: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY = $root.proto.ScheduleID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY);
                                        message.value = "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY";
                                        continue;
                                    }
                                case 31: {
                                        if (wireType !== 2)
                                            break;
                                        message.TssBaseService_I_TSS_MESSAGES = $root.com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TssBaseService_I_TSS_MESSAGES);
                                        message.value = "TssBaseService_I_TSS_MESSAGES";
                                        continue;
                                    }
                                case 32: {
                                        if (wireType !== 2)
                                            break;
                                        message.TssBaseService_I_TSS_VOTES = $root.com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TssBaseService_I_TSS_VOTES);
                                        message.value = "TssBaseService_I_TSS_VOTES";
                                        continue;
                                    }
                                case 33: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULED_ORDERS = $root.proto.ScheduleID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULED_ORDERS);
                                        message.value = "ScheduleService_I_SCHEDULED_ORDERS";
                                        continue;
                                    }
                                case 34: {
                                        if (wireType !== 2)
                                            break;
                                        message.ScheduleService_I_SCHEDULED_USAGES = $root.proto.ThrottleUsageSnapshots.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ScheduleService_I_SCHEDULED_USAGES);
                                        message.value = "ScheduleService_I_SCHEDULED_USAGES";
                                        continue;
                                    }
                                case 35: {
                                        if (wireType !== 2)
                                            break;
                                        message.TssBaseService_I_TSS_ENCRYPTION_KEYS = $root.com.hedera.hapi.node.state.tss.TssEncryptionKeys.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TssBaseService_I_TSS_ENCRYPTION_KEYS);
                                        message.value = "TssBaseService_I_TSS_ENCRYPTION_KEYS";
                                        continue;
                                    }
                                case 37: {
                                        if (wireType !== 2)
                                            break;
                                        message.HintsService_I_HINTS_KEY_SETS = $root.com.hedera.hapi.node.state.hints.HintsKeySet.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HintsService_I_HINTS_KEY_SETS);
                                        message.value = "HintsService_I_HINTS_KEY_SETS";
                                        continue;
                                    }
                                case 38: {
                                        if (wireType !== 2)
                                            break;
                                        message.HintsService_I_ACTIVE_HINTS_CONSTRUCTION = $root.com.hedera.hapi.node.state.hints.HintsConstruction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HintsService_I_ACTIVE_HINTS_CONSTRUCTION);
                                        message.value = "HintsService_I_ACTIVE_HINTS_CONSTRUCTION";
                                        continue;
                                    }
                                case 39: {
                                        if (wireType !== 2)
                                            break;
                                        message.HintsService_I_NEXT_HINTS_CONSTRUCTION = $root.com.hedera.hapi.node.state.hints.HintsConstruction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HintsService_I_NEXT_HINTS_CONSTRUCTION);
                                        message.value = "HintsService_I_NEXT_HINTS_CONSTRUCTION";
                                        continue;
                                    }
                                case 40: {
                                        if (wireType !== 2)
                                            break;
                                        message.HintsService_I_PREPROCESSING_VOTES = $root.com.hedera.hapi.node.state.hints.PreprocessingVote.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HintsService_I_PREPROCESSING_VOTES);
                                        message.value = "HintsService_I_PREPROCESSING_VOTES";
                                        continue;
                                    }
                                case 41: {
                                        if (wireType !== 2)
                                            break;
                                        message.EntityIdService_I_ENTITY_COUNTS = $root.com.hedera.hapi.node.state.entity.EntityCounts.decode(reader, reader.uint32(), $undefined, _depth + 1, message.EntityIdService_I_ENTITY_COUNTS);
                                        message.value = "EntityIdService_I_ENTITY_COUNTS";
                                        continue;
                                    }
                                case 42: {
                                        if (wireType !== 2)
                                            break;
                                        message.HistoryService_I_LEDGER_ID = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HistoryService_I_LEDGER_ID);
                                        message.value = "HistoryService_I_LEDGER_ID";
                                        continue;
                                    }
                                case 43: {
                                        if (wireType !== 2)
                                            break;
                                        message.HistoryService_I_PROOF_KEY_SETS = $root.com.hedera.hapi.node.state.history.ProofKeySet.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HistoryService_I_PROOF_KEY_SETS);
                                        message.value = "HistoryService_I_PROOF_KEY_SETS";
                                        continue;
                                    }
                                case 44: {
                                        if (wireType !== 2)
                                            break;
                                        message.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION = $root.com.hedera.hapi.node.state.history.HistoryProofConstruction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION);
                                        message.value = "HistoryService_I_ACTIVE_PROOF_CONSTRUCTION";
                                        continue;
                                    }
                                case 45: {
                                        if (wireType !== 2)
                                            break;
                                        message.HistoryService_I_NEXT_PROOF_CONSTRUCTION = $root.com.hedera.hapi.node.state.history.HistoryProofConstruction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HistoryService_I_NEXT_PROOF_CONSTRUCTION);
                                        message.value = "HistoryService_I_NEXT_PROOF_CONSTRUCTION";
                                        continue;
                                    }
                                case 46: {
                                        if (wireType !== 2)
                                            break;
                                        message.HistoryService_I_HISTORY_SIGNATURES = $root.com.hedera.hapi.node.state.history.RecordedHistorySignature.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HistoryService_I_HISTORY_SIGNATURES);
                                        message.value = "HistoryService_I_HISTORY_SIGNATURES";
                                        continue;
                                    }
                                case 47: {
                                        if (wireType !== 2)
                                            break;
                                        message.HistoryService_I_PROOF_VOTES = $root.com.hedera.hapi.node.state.history.HistoryProofVote.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HistoryService_I_PROOF_VOTES);
                                        message.value = "HistoryService_I_PROOF_VOTES";
                                        continue;
                                    }
                                case 48: {
                                        if (wireType !== 2)
                                            break;
                                        message.HintsService_I_CRS_STATE = $root.com.hedera.hapi.node.state.hints.CRSState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HintsService_I_CRS_STATE);
                                        message.value = "HintsService_I_CRS_STATE";
                                        continue;
                                    }
                                case 49: {
                                        if (wireType !== 2)
                                            break;
                                        message.HintsService_I_CRS_PUBLICATIONS = $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HintsService_I_CRS_PUBLICATIONS);
                                        message.value = "HintsService_I_CRS_PUBLICATIONS";
                                        continue;
                                    }
                                case 50: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_NODE_REWARDS = $root.proto.NodeRewards.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_NODE_REWARDS);
                                        message.value = "TokenService_I_NODE_REWARDS";
                                        continue;
                                    }
                                case 51: {
                                        if (wireType !== 2)
                                            break;
                                        message.ContractService_I_EVM_HOOK_STATES = $root.com.hedera.hapi.node.state.hooks.EvmHookState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ContractService_I_EVM_HOOK_STATES);
                                        message.value = "ContractService_I_EVM_HOOK_STATES";
                                        continue;
                                    }
                                case 52: {
                                        if (wireType !== 2)
                                            break;
                                        message.ContractService_I_EVM_HOOK_STORAGE = $root.proto.SlotValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ContractService_I_EVM_HOOK_STORAGE);
                                        message.value = "ContractService_I_EVM_HOOK_STORAGE";
                                        continue;
                                    }
                                case 53: {
                                        if (wireType !== 2)
                                            break;
                                        message.AddressBookService_I_ACCOUNT_NODE_REL = $root.com.hedera.hapi.platform.state.NodeId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.AddressBookService_I_ACCOUNT_NODE_REL);
                                        message.value = "AddressBookService_I_ACCOUNT_NODE_REL";
                                        continue;
                                    }
                                case 54: {
                                        if (wireType !== 2)
                                            break;
                                        message.HistoryService_I_WRAPS_MESSAGE_HISTORIES = $root.com.hedera.hapi.node.state.history.WrapsMessageHistory.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HistoryService_I_WRAPS_MESSAGE_HISTORIES);
                                        message.value = "HistoryService_I_WRAPS_MESSAGE_HISTORIES";
                                        continue;
                                    }
                                case 55: {
                                        if (wireType !== 2)
                                            break;
                                        message.TokenService_I_NODE_PAYMENTS = $root.com.hedera.hapi.node.state.token.NodePayments.decode(reader, reader.uint32(), $undefined, _depth + 1, message.TokenService_I_NODE_PAYMENTS);
                                        message.value = "TokenService_I_NODE_PAYMENTS";
                                        continue;
                                    }
                                case 56: {
                                        if (wireType !== 2)
                                            break;
                                        message.AddressBookService_I_REGISTERED_NODES = $root.com.hedera.hapi.node.state.addressbook.RegisteredNode.decode(reader, reader.uint32(), $undefined, _depth + 1, message.AddressBookService_I_REGISTERED_NODES);
                                        message.value = "AddressBookService_I_REGISTERED_NODES";
                                        continue;
                                    }
                                case 57: {
                                        if (wireType !== 2)
                                            break;
                                        message.EntityIdService_I_HIGHEST_NODE_ID = $root.com.hedera.hapi.platform.state.NodeId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.EntityIdService_I_HIGHEST_NODE_ID);
                                        message.value = "EntityIdService_I_HIGHEST_NODE_ID";
                                        continue;
                                    }
                                case 58: {
                                        if (wireType !== 2)
                                            break;
                                        message.HistoryService_I_WRAPS_PROVING_KEY_HASH = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.HistoryService_I_WRAPS_PROVING_KEY_HASH);
                                        message.value = "HistoryService_I_WRAPS_PROVING_KEY_HASH";
                                        continue;
                                    }
                                case 126: {
                                        if (wireType !== 2)
                                            break;
                                        message.RecordCache_I_TRANSACTION_RECEIPTS = $root.proto.TransactionReceiptEntries.decode(reader, reader.uint32(), $undefined, _depth + 1, message.RecordCache_I_TRANSACTION_RECEIPTS);
                                        message.value = "RecordCache_I_TRANSACTION_RECEIPTS";
                                        continue;
                                    }
                                case 8001: {
                                        if (wireType !== 2)
                                            break;
                                        message.queueState = $root.com.hedera.hapi.platform.state.QueueState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.queueState);
                                        message.value = "queueState";
                                        continue;
                                    }
                                case 10001: {
                                        if (wireType !== 2)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_150 = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FileService_I_UPGRADE_DATA_150);
                                        message.value = "FileService_I_UPGRADE_DATA_150";
                                        continue;
                                    }
                                case 10002: {
                                        if (wireType !== 2)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_151 = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FileService_I_UPGRADE_DATA_151);
                                        message.value = "FileService_I_UPGRADE_DATA_151";
                                        continue;
                                    }
                                case 10003: {
                                        if (wireType !== 2)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_152 = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FileService_I_UPGRADE_DATA_152);
                                        message.value = "FileService_I_UPGRADE_DATA_152";
                                        continue;
                                    }
                                case 10004: {
                                        if (wireType !== 2)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_153 = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FileService_I_UPGRADE_DATA_153);
                                        message.value = "FileService_I_UPGRADE_DATA_153";
                                        continue;
                                    }
                                case 10005: {
                                        if (wireType !== 2)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_154 = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FileService_I_UPGRADE_DATA_154);
                                        message.value = "FileService_I_UPGRADE_DATA_154";
                                        continue;
                                    }
                                case 10006: {
                                        if (wireType !== 2)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_155 = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FileService_I_UPGRADE_DATA_155);
                                        message.value = "FileService_I_UPGRADE_DATA_155";
                                        continue;
                                    }
                                case 10007: {
                                        if (wireType !== 2)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_156 = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FileService_I_UPGRADE_DATA_156);
                                        message.value = "FileService_I_UPGRADE_DATA_156";
                                        continue;
                                    }
                                case 10008: {
                                        if (wireType !== 2)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_157 = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FileService_I_UPGRADE_DATA_157);
                                        message.value = "FileService_I_UPGRADE_DATA_157";
                                        continue;
                                    }
                                case 10009: {
                                        if (wireType !== 2)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_158 = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FileService_I_UPGRADE_DATA_158);
                                        message.value = "FileService_I_UPGRADE_DATA_158";
                                        continue;
                                    }
                                case 10010: {
                                        if (wireType !== 2)
                                            break;
                                        message.FileService_I_UPGRADE_DATA_159 = $root.proto.ProtoBytes.decode(reader, reader.uint32(), $undefined, _depth + 1, message.FileService_I_UPGRADE_DATA_159);
                                        message.value = "FileService_I_UPGRADE_DATA_159";
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a StateValue message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.platform.state.StateValue & com.hedera.hapi.platform.state.StateValue.$Shape} StateValue
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        StateValue.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a StateValue message.
                         * @function verify
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        StateValue.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            let properties = {};
                            if (message.EntityIdService_I_ENTITY_ID != null && $Object.hasOwnProperty.call(message, "EntityIdService_I_ENTITY_ID")) {
                                properties.value = 1;
                                {
                                    let error = $root.proto.EntityNumber.verify(message.EntityIdService_I_ENTITY_ID, _depth + 1);
                                    if (error)
                                        return "EntityIdService_I_ENTITY_ID." + error;
                                }
                            }
                            if (message.TokenService_I_ACCOUNTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_ACCOUNTS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.Account.verify(message.TokenService_I_ACCOUNTS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_ACCOUNTS." + error;
                                }
                            }
                            if (message.TokenService_I_ALIASES != null && $Object.hasOwnProperty.call(message, "TokenService_I_ALIASES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.AccountID.verify(message.TokenService_I_ALIASES, _depth + 1);
                                    if (error)
                                        return "TokenService_I_ALIASES." + error;
                                }
                            }
                            if (message.ContractService_I_STORAGE != null && $Object.hasOwnProperty.call(message, "ContractService_I_STORAGE")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.SlotValue.verify(message.ContractService_I_STORAGE, _depth + 1);
                                    if (error)
                                        return "ContractService_I_STORAGE." + error;
                                }
                            }
                            if (message.ContractService_I_BYTECODE != null && $Object.hasOwnProperty.call(message, "ContractService_I_BYTECODE")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.Bytecode.verify(message.ContractService_I_BYTECODE, _depth + 1);
                                    if (error)
                                        return "ContractService_I_BYTECODE." + error;
                                }
                            }
                            if (message.FileService_I_FILES != null && $Object.hasOwnProperty.call(message, "FileService_I_FILES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.File.verify(message.FileService_I_FILES, _depth + 1);
                                    if (error)
                                        return "FileService_I_FILES." + error;
                                }
                            }
                            if (message.TokenService_I_TOKENS != null && $Object.hasOwnProperty.call(message, "TokenService_I_TOKENS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.Token.verify(message.TokenService_I_TOKENS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_TOKENS." + error;
                                }
                            }
                            if (message.TokenService_I_NFTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_NFTS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.Nft.verify(message.TokenService_I_NFTS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_NFTS." + error;
                                }
                            }
                            if (message.TokenService_I_TOKEN_RELS != null && $Object.hasOwnProperty.call(message, "TokenService_I_TOKEN_RELS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.TokenRelation.verify(message.TokenService_I_TOKEN_RELS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_TOKEN_RELS." + error;
                                }
                            }
                            if (message.TokenService_I_STAKING_INFOS != null && $Object.hasOwnProperty.call(message, "TokenService_I_STAKING_INFOS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.StakingNodeInfo.verify(message.TokenService_I_STAKING_INFOS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_STAKING_INFOS." + error;
                                }
                            }
                            if (message.TokenService_I_STAKING_NETWORK_REWARDS != null && $Object.hasOwnProperty.call(message, "TokenService_I_STAKING_NETWORK_REWARDS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.NetworkStakingRewards.verify(message.TokenService_I_STAKING_NETWORK_REWARDS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_STAKING_NETWORK_REWARDS." + error;
                                }
                            }
                            if (message.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS != null && $Object.hasOwnProperty.call(message, "CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ThrottleUsageSnapshots.verify(message.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS, _depth + 1);
                                    if (error)
                                        return "CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS." + error;
                                }
                            }
                            if (message.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS != null && $Object.hasOwnProperty.call(message, "CongestionThrottleService_I_CONGESTION_LEVEL_STARTS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.CongestionLevelStarts.verify(message.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS, _depth + 1);
                                    if (error)
                                        return "CongestionThrottleService_I_CONGESTION_LEVEL_STARTS." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULES_BY_ID != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_ID")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.Schedule.verify(message.ScheduleService_I_SCHEDULES_BY_ID, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULES_BY_ID." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ScheduleList.verify(message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULES_BY_EQUALITY != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_EQUALITY")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ScheduleList.verify(message.ScheduleService_I_SCHEDULES_BY_EQUALITY, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULES_BY_EQUALITY." + error;
                                }
                            }
                            if (message.FeeService_I_MIDNIGHT_RATES != null && $Object.hasOwnProperty.call(message, "FeeService_I_MIDNIGHT_RATES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ExchangeRateSet.verify(message.FeeService_I_MIDNIGHT_RATES, _depth + 1);
                                    if (error)
                                        return "FeeService_I_MIDNIGHT_RATES." + error;
                                }
                            }
                            if (message.BlockRecordService_I_RUNNING_HASHES != null && $Object.hasOwnProperty.call(message, "BlockRecordService_I_RUNNING_HASHES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.RunningHashes.verify(message.BlockRecordService_I_RUNNING_HASHES, _depth + 1);
                                    if (error)
                                        return "BlockRecordService_I_RUNNING_HASHES." + error;
                                }
                            }
                            if (message.BlockRecordService_I_BLOCKS != null && $Object.hasOwnProperty.call(message, "BlockRecordService_I_BLOCKS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.BlockInfo.verify(message.BlockRecordService_I_BLOCKS, _depth + 1);
                                    if (error)
                                        return "BlockRecordService_I_BLOCKS." + error;
                                }
                            }
                            if (message.AddressBookService_I_NODES != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_NODES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.addressbook.Node.verify(message.AddressBookService_I_NODES, _depth + 1);
                                    if (error)
                                        return "AddressBookService_I_NODES." + error;
                                }
                            }
                            if (message.ConsensusService_I_TOPICS != null && $Object.hasOwnProperty.call(message, "ConsensusService_I_TOPICS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.Topic.verify(message.ConsensusService_I_TOPICS, _depth + 1);
                                    if (error)
                                        return "ConsensusService_I_TOPICS." + error;
                                }
                            }
                            if (message.FreezeService_I_UPGRADE_FILE_HASH != null && $Object.hasOwnProperty.call(message, "FreezeService_I_UPGRADE_FILE_HASH")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.FreezeService_I_UPGRADE_FILE_HASH, _depth + 1);
                                    if (error)
                                        return "FreezeService_I_UPGRADE_FILE_HASH." + error;
                                }
                            }
                            if (message.FreezeService_I_FREEZE_TIME != null && $Object.hasOwnProperty.call(message, "FreezeService_I_FREEZE_TIME")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.Timestamp.verify(message.FreezeService_I_FREEZE_TIME, _depth + 1);
                                    if (error)
                                        return "FreezeService_I_FREEZE_TIME." + error;
                                }
                            }
                            if (message.BlockStreamService_I_BLOCK_STREAM_INFO != null && $Object.hasOwnProperty.call(message, "BlockStreamService_I_BLOCK_STREAM_INFO")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.blockstream.BlockStreamInfo.verify(message.BlockStreamService_I_BLOCK_STREAM_INFO, _depth + 1);
                                    if (error)
                                        return "BlockStreamService_I_BLOCK_STREAM_INFO." + error;
                                }
                            }
                            if (message.TokenService_I_PENDING_AIRDROPS != null && $Object.hasOwnProperty.call(message, "TokenService_I_PENDING_AIRDROPS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.AccountPendingAirdrop.verify(message.TokenService_I_PENDING_AIRDROPS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_PENDING_AIRDROPS." + error;
                                }
                            }
                            if (message.PlatformStateService_I_PLATFORM_STATE != null && $Object.hasOwnProperty.call(message, "PlatformStateService_I_PLATFORM_STATE")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.platform.state.PlatformState.verify(message.PlatformStateService_I_PLATFORM_STATE, _depth + 1);
                                    if (error)
                                        return "PlatformStateService_I_PLATFORM_STATE." + error;
                                }
                            }
                            if (message.RosterService_I_ROSTER_STATE != null && $Object.hasOwnProperty.call(message, "RosterService_I_ROSTER_STATE")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.roster.RosterState.verify(message.RosterService_I_ROSTER_STATE, _depth + 1);
                                    if (error)
                                        return "RosterService_I_ROSTER_STATE." + error;
                                }
                            }
                            if (message.RosterService_I_ROSTERS != null && $Object.hasOwnProperty.call(message, "RosterService_I_ROSTERS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.roster.Roster.verify(message.RosterService_I_ROSTERS, _depth + 1);
                                    if (error)
                                        return "RosterService_I_ROSTERS." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULED_COUNTS != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_COUNTS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ScheduledCounts.verify(message.ScheduleService_I_SCHEDULED_COUNTS, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULED_COUNTS." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ScheduleID.verify(message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY." + error;
                                }
                            }
                            if (message.TssBaseService_I_TSS_MESSAGES != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_MESSAGES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.verify(message.TssBaseService_I_TSS_MESSAGES, _depth + 1);
                                    if (error)
                                        return "TssBaseService_I_TSS_MESSAGES." + error;
                                }
                            }
                            if (message.TssBaseService_I_TSS_VOTES != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_VOTES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.verify(message.TssBaseService_I_TSS_VOTES, _depth + 1);
                                    if (error)
                                        return "TssBaseService_I_TSS_VOTES." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULED_ORDERS != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_ORDERS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ScheduleID.verify(message.ScheduleService_I_SCHEDULED_ORDERS, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULED_ORDERS." + error;
                                }
                            }
                            if (message.ScheduleService_I_SCHEDULED_USAGES != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_USAGES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ThrottleUsageSnapshots.verify(message.ScheduleService_I_SCHEDULED_USAGES, _depth + 1);
                                    if (error)
                                        return "ScheduleService_I_SCHEDULED_USAGES." + error;
                                }
                            }
                            if (message.TssBaseService_I_TSS_ENCRYPTION_KEYS != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_ENCRYPTION_KEYS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.tss.TssEncryptionKeys.verify(message.TssBaseService_I_TSS_ENCRYPTION_KEYS, _depth + 1);
                                    if (error)
                                        return "TssBaseService_I_TSS_ENCRYPTION_KEYS." + error;
                                }
                            }
                            if (message.HintsService_I_HINTS_KEY_SETS != null && $Object.hasOwnProperty.call(message, "HintsService_I_HINTS_KEY_SETS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.hints.HintsKeySet.verify(message.HintsService_I_HINTS_KEY_SETS, _depth + 1);
                                    if (error)
                                        return "HintsService_I_HINTS_KEY_SETS." + error;
                                }
                            }
                            if (message.HintsService_I_ACTIVE_HINTS_CONSTRUCTION != null && $Object.hasOwnProperty.call(message, "HintsService_I_ACTIVE_HINTS_CONSTRUCTION")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.hints.HintsConstruction.verify(message.HintsService_I_ACTIVE_HINTS_CONSTRUCTION, _depth + 1);
                                    if (error)
                                        return "HintsService_I_ACTIVE_HINTS_CONSTRUCTION." + error;
                                }
                            }
                            if (message.HintsService_I_NEXT_HINTS_CONSTRUCTION != null && $Object.hasOwnProperty.call(message, "HintsService_I_NEXT_HINTS_CONSTRUCTION")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.hints.HintsConstruction.verify(message.HintsService_I_NEXT_HINTS_CONSTRUCTION, _depth + 1);
                                    if (error)
                                        return "HintsService_I_NEXT_HINTS_CONSTRUCTION." + error;
                                }
                            }
                            if (message.HintsService_I_PREPROCESSING_VOTES != null && $Object.hasOwnProperty.call(message, "HintsService_I_PREPROCESSING_VOTES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.hints.PreprocessingVote.verify(message.HintsService_I_PREPROCESSING_VOTES, _depth + 1);
                                    if (error)
                                        return "HintsService_I_PREPROCESSING_VOTES." + error;
                                }
                            }
                            if (message.EntityIdService_I_ENTITY_COUNTS != null && $Object.hasOwnProperty.call(message, "EntityIdService_I_ENTITY_COUNTS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.entity.EntityCounts.verify(message.EntityIdService_I_ENTITY_COUNTS, _depth + 1);
                                    if (error)
                                        return "EntityIdService_I_ENTITY_COUNTS." + error;
                                }
                            }
                            if (message.HistoryService_I_LEDGER_ID != null && $Object.hasOwnProperty.call(message, "HistoryService_I_LEDGER_ID")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.HistoryService_I_LEDGER_ID, _depth + 1);
                                    if (error)
                                        return "HistoryService_I_LEDGER_ID." + error;
                                }
                            }
                            if (message.HistoryService_I_PROOF_KEY_SETS != null && $Object.hasOwnProperty.call(message, "HistoryService_I_PROOF_KEY_SETS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.history.ProofKeySet.verify(message.HistoryService_I_PROOF_KEY_SETS, _depth + 1);
                                    if (error)
                                        return "HistoryService_I_PROOF_KEY_SETS." + error;
                                }
                            }
                            if (message.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION != null && $Object.hasOwnProperty.call(message, "HistoryService_I_ACTIVE_PROOF_CONSTRUCTION")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.history.HistoryProofConstruction.verify(message.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION, _depth + 1);
                                    if (error)
                                        return "HistoryService_I_ACTIVE_PROOF_CONSTRUCTION." + error;
                                }
                            }
                            if (message.HistoryService_I_NEXT_PROOF_CONSTRUCTION != null && $Object.hasOwnProperty.call(message, "HistoryService_I_NEXT_PROOF_CONSTRUCTION")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.history.HistoryProofConstruction.verify(message.HistoryService_I_NEXT_PROOF_CONSTRUCTION, _depth + 1);
                                    if (error)
                                        return "HistoryService_I_NEXT_PROOF_CONSTRUCTION." + error;
                                }
                            }
                            if (message.HistoryService_I_HISTORY_SIGNATURES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_HISTORY_SIGNATURES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.history.RecordedHistorySignature.verify(message.HistoryService_I_HISTORY_SIGNATURES, _depth + 1);
                                    if (error)
                                        return "HistoryService_I_HISTORY_SIGNATURES." + error;
                                }
                            }
                            if (message.HistoryService_I_PROOF_VOTES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_PROOF_VOTES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.history.HistoryProofVote.verify(message.HistoryService_I_PROOF_VOTES, _depth + 1);
                                    if (error)
                                        return "HistoryService_I_PROOF_VOTES." + error;
                                }
                            }
                            if (message.HintsService_I_CRS_STATE != null && $Object.hasOwnProperty.call(message, "HintsService_I_CRS_STATE")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.hints.CRSState.verify(message.HintsService_I_CRS_STATE, _depth + 1);
                                    if (error)
                                        return "HintsService_I_CRS_STATE." + error;
                                }
                            }
                            if (message.HintsService_I_CRS_PUBLICATIONS != null && $Object.hasOwnProperty.call(message, "HintsService_I_CRS_PUBLICATIONS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.verify(message.HintsService_I_CRS_PUBLICATIONS, _depth + 1);
                                    if (error)
                                        return "HintsService_I_CRS_PUBLICATIONS." + error;
                                }
                            }
                            if (message.TokenService_I_NODE_REWARDS != null && $Object.hasOwnProperty.call(message, "TokenService_I_NODE_REWARDS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.NodeRewards.verify(message.TokenService_I_NODE_REWARDS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_NODE_REWARDS." + error;
                                }
                            }
                            if (message.ContractService_I_EVM_HOOK_STATES != null && $Object.hasOwnProperty.call(message, "ContractService_I_EVM_HOOK_STATES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.hooks.EvmHookState.verify(message.ContractService_I_EVM_HOOK_STATES, _depth + 1);
                                    if (error)
                                        return "ContractService_I_EVM_HOOK_STATES." + error;
                                }
                            }
                            if (message.ContractService_I_EVM_HOOK_STORAGE != null && $Object.hasOwnProperty.call(message, "ContractService_I_EVM_HOOK_STORAGE")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.SlotValue.verify(message.ContractService_I_EVM_HOOK_STORAGE, _depth + 1);
                                    if (error)
                                        return "ContractService_I_EVM_HOOK_STORAGE." + error;
                                }
                            }
                            if (message.AddressBookService_I_ACCOUNT_NODE_REL != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_ACCOUNT_NODE_REL")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.platform.state.NodeId.verify(message.AddressBookService_I_ACCOUNT_NODE_REL, _depth + 1);
                                    if (error)
                                        return "AddressBookService_I_ACCOUNT_NODE_REL." + error;
                                }
                            }
                            if (message.HistoryService_I_WRAPS_MESSAGE_HISTORIES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_WRAPS_MESSAGE_HISTORIES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.history.WrapsMessageHistory.verify(message.HistoryService_I_WRAPS_MESSAGE_HISTORIES, _depth + 1);
                                    if (error)
                                        return "HistoryService_I_WRAPS_MESSAGE_HISTORIES." + error;
                                }
                            }
                            if (message.TokenService_I_NODE_PAYMENTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_NODE_PAYMENTS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.token.NodePayments.verify(message.TokenService_I_NODE_PAYMENTS, _depth + 1);
                                    if (error)
                                        return "TokenService_I_NODE_PAYMENTS." + error;
                                }
                            }
                            if (message.AddressBookService_I_REGISTERED_NODES != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_REGISTERED_NODES")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.state.addressbook.RegisteredNode.verify(message.AddressBookService_I_REGISTERED_NODES, _depth + 1);
                                    if (error)
                                        return "AddressBookService_I_REGISTERED_NODES." + error;
                                }
                            }
                            if (message.EntityIdService_I_HIGHEST_NODE_ID != null && $Object.hasOwnProperty.call(message, "EntityIdService_I_HIGHEST_NODE_ID")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.platform.state.NodeId.verify(message.EntityIdService_I_HIGHEST_NODE_ID, _depth + 1);
                                    if (error)
                                        return "EntityIdService_I_HIGHEST_NODE_ID." + error;
                                }
                            }
                            if (message.HistoryService_I_WRAPS_PROVING_KEY_HASH != null && $Object.hasOwnProperty.call(message, "HistoryService_I_WRAPS_PROVING_KEY_HASH")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.HistoryService_I_WRAPS_PROVING_KEY_HASH, _depth + 1);
                                    if (error)
                                        return "HistoryService_I_WRAPS_PROVING_KEY_HASH." + error;
                                }
                            }
                            if (message.RecordCache_I_TRANSACTION_RECEIPTS != null && $Object.hasOwnProperty.call(message, "RecordCache_I_TRANSACTION_RECEIPTS")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.TransactionReceiptEntries.verify(message.RecordCache_I_TRANSACTION_RECEIPTS, _depth + 1);
                                    if (error)
                                        return "RecordCache_I_TRANSACTION_RECEIPTS." + error;
                                }
                            }
                            if (message.queueState != null && $Object.hasOwnProperty.call(message, "queueState")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.com.hedera.hapi.platform.state.QueueState.verify(message.queueState, _depth + 1);
                                    if (error)
                                        return "queueState." + error;
                                }
                            }
                            if (message.FileService_I_UPGRADE_DATA_150 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_150")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.FileService_I_UPGRADE_DATA_150, _depth + 1);
                                    if (error)
                                        return "FileService_I_UPGRADE_DATA_150." + error;
                                }
                            }
                            if (message.FileService_I_UPGRADE_DATA_151 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_151")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.FileService_I_UPGRADE_DATA_151, _depth + 1);
                                    if (error)
                                        return "FileService_I_UPGRADE_DATA_151." + error;
                                }
                            }
                            if (message.FileService_I_UPGRADE_DATA_152 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_152")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.FileService_I_UPGRADE_DATA_152, _depth + 1);
                                    if (error)
                                        return "FileService_I_UPGRADE_DATA_152." + error;
                                }
                            }
                            if (message.FileService_I_UPGRADE_DATA_153 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_153")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.FileService_I_UPGRADE_DATA_153, _depth + 1);
                                    if (error)
                                        return "FileService_I_UPGRADE_DATA_153." + error;
                                }
                            }
                            if (message.FileService_I_UPGRADE_DATA_154 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_154")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.FileService_I_UPGRADE_DATA_154, _depth + 1);
                                    if (error)
                                        return "FileService_I_UPGRADE_DATA_154." + error;
                                }
                            }
                            if (message.FileService_I_UPGRADE_DATA_155 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_155")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.FileService_I_UPGRADE_DATA_155, _depth + 1);
                                    if (error)
                                        return "FileService_I_UPGRADE_DATA_155." + error;
                                }
                            }
                            if (message.FileService_I_UPGRADE_DATA_156 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_156")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.FileService_I_UPGRADE_DATA_156, _depth + 1);
                                    if (error)
                                        return "FileService_I_UPGRADE_DATA_156." + error;
                                }
                            }
                            if (message.FileService_I_UPGRADE_DATA_157 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_157")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.FileService_I_UPGRADE_DATA_157, _depth + 1);
                                    if (error)
                                        return "FileService_I_UPGRADE_DATA_157." + error;
                                }
                            }
                            if (message.FileService_I_UPGRADE_DATA_158 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_158")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.FileService_I_UPGRADE_DATA_158, _depth + 1);
                                    if (error)
                                        return "FileService_I_UPGRADE_DATA_158." + error;
                                }
                            }
                            if (message.FileService_I_UPGRADE_DATA_159 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_159")) {
                                if (properties.value === 1)
                                    return "value: multiple values";
                                properties.value = 1;
                                {
                                    let error = $root.proto.ProtoBytes.verify(message.FileService_I_UPGRADE_DATA_159, _depth + 1);
                                    if (error)
                                        return "FileService_I_UPGRADE_DATA_159." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates a StateValue message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.platform.state.StateValue} StateValue
                         */
                        StateValue.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.platform.state.StateValue)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.platform.state.StateValue: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.platform.state.StateValue();
                            if (object.EntityIdService_I_ENTITY_ID != null) {
                                if (!$util.isObject(object.EntityIdService_I_ENTITY_ID))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.EntityIdService_I_ENTITY_ID: object expected");
                                message.EntityIdService_I_ENTITY_ID = $root.proto.EntityNumber.fromObject(object.EntityIdService_I_ENTITY_ID, _depth + 1);
                            }
                            if (object.TokenService_I_ACCOUNTS != null) {
                                if (!$util.isObject(object.TokenService_I_ACCOUNTS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TokenService_I_ACCOUNTS: object expected");
                                message.TokenService_I_ACCOUNTS = $root.proto.Account.fromObject(object.TokenService_I_ACCOUNTS, _depth + 1);
                            }
                            if (object.TokenService_I_ALIASES != null) {
                                if (!$util.isObject(object.TokenService_I_ALIASES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TokenService_I_ALIASES: object expected");
                                message.TokenService_I_ALIASES = $root.proto.AccountID.fromObject(object.TokenService_I_ALIASES, _depth + 1);
                            }
                            if (object.ContractService_I_STORAGE != null) {
                                if (!$util.isObject(object.ContractService_I_STORAGE))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.ContractService_I_STORAGE: object expected");
                                message.ContractService_I_STORAGE = $root.proto.SlotValue.fromObject(object.ContractService_I_STORAGE, _depth + 1);
                            }
                            if (object.ContractService_I_BYTECODE != null) {
                                if (!$util.isObject(object.ContractService_I_BYTECODE))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.ContractService_I_BYTECODE: object expected");
                                message.ContractService_I_BYTECODE = $root.proto.Bytecode.fromObject(object.ContractService_I_BYTECODE, _depth + 1);
                            }
                            if (object.FileService_I_FILES != null) {
                                if (!$util.isObject(object.FileService_I_FILES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FileService_I_FILES: object expected");
                                message.FileService_I_FILES = $root.proto.File.fromObject(object.FileService_I_FILES, _depth + 1);
                            }
                            if (object.TokenService_I_TOKENS != null) {
                                if (!$util.isObject(object.TokenService_I_TOKENS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TokenService_I_TOKENS: object expected");
                                message.TokenService_I_TOKENS = $root.proto.Token.fromObject(object.TokenService_I_TOKENS, _depth + 1);
                            }
                            if (object.TokenService_I_NFTS != null) {
                                if (!$util.isObject(object.TokenService_I_NFTS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TokenService_I_NFTS: object expected");
                                message.TokenService_I_NFTS = $root.proto.Nft.fromObject(object.TokenService_I_NFTS, _depth + 1);
                            }
                            if (object.TokenService_I_TOKEN_RELS != null) {
                                if (!$util.isObject(object.TokenService_I_TOKEN_RELS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TokenService_I_TOKEN_RELS: object expected");
                                message.TokenService_I_TOKEN_RELS = $root.proto.TokenRelation.fromObject(object.TokenService_I_TOKEN_RELS, _depth + 1);
                            }
                            if (object.TokenService_I_STAKING_INFOS != null) {
                                if (!$util.isObject(object.TokenService_I_STAKING_INFOS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TokenService_I_STAKING_INFOS: object expected");
                                message.TokenService_I_STAKING_INFOS = $root.proto.StakingNodeInfo.fromObject(object.TokenService_I_STAKING_INFOS, _depth + 1);
                            }
                            if (object.TokenService_I_STAKING_NETWORK_REWARDS != null) {
                                if (!$util.isObject(object.TokenService_I_STAKING_NETWORK_REWARDS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TokenService_I_STAKING_NETWORK_REWARDS: object expected");
                                message.TokenService_I_STAKING_NETWORK_REWARDS = $root.proto.NetworkStakingRewards.fromObject(object.TokenService_I_STAKING_NETWORK_REWARDS, _depth + 1);
                            }
                            if (object.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS != null) {
                                if (!$util.isObject(object.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS: object expected");
                                message.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS = $root.proto.ThrottleUsageSnapshots.fromObject(object.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS, _depth + 1);
                            }
                            if (object.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS != null) {
                                if (!$util.isObject(object.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS: object expected");
                                message.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS = $root.proto.CongestionLevelStarts.fromObject(object.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULES_BY_ID != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULES_BY_ID))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.ScheduleService_I_SCHEDULES_BY_ID: object expected");
                                message.ScheduleService_I_SCHEDULES_BY_ID = $root.proto.Schedule.fromObject(object.ScheduleService_I_SCHEDULES_BY_ID, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC: object expected");
                                message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC = $root.proto.ScheduleList.fromObject(object.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULES_BY_EQUALITY != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULES_BY_EQUALITY))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.ScheduleService_I_SCHEDULES_BY_EQUALITY: object expected");
                                message.ScheduleService_I_SCHEDULES_BY_EQUALITY = $root.proto.ScheduleList.fromObject(object.ScheduleService_I_SCHEDULES_BY_EQUALITY, _depth + 1);
                            }
                            if (object.FeeService_I_MIDNIGHT_RATES != null) {
                                if (!$util.isObject(object.FeeService_I_MIDNIGHT_RATES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FeeService_I_MIDNIGHT_RATES: object expected");
                                message.FeeService_I_MIDNIGHT_RATES = $root.proto.ExchangeRateSet.fromObject(object.FeeService_I_MIDNIGHT_RATES, _depth + 1);
                            }
                            if (object.BlockRecordService_I_RUNNING_HASHES != null) {
                                if (!$util.isObject(object.BlockRecordService_I_RUNNING_HASHES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.BlockRecordService_I_RUNNING_HASHES: object expected");
                                message.BlockRecordService_I_RUNNING_HASHES = $root.proto.RunningHashes.fromObject(object.BlockRecordService_I_RUNNING_HASHES, _depth + 1);
                            }
                            if (object.BlockRecordService_I_BLOCKS != null) {
                                if (!$util.isObject(object.BlockRecordService_I_BLOCKS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.BlockRecordService_I_BLOCKS: object expected");
                                message.BlockRecordService_I_BLOCKS = $root.proto.BlockInfo.fromObject(object.BlockRecordService_I_BLOCKS, _depth + 1);
                            }
                            if (object.AddressBookService_I_NODES != null) {
                                if (!$util.isObject(object.AddressBookService_I_NODES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.AddressBookService_I_NODES: object expected");
                                message.AddressBookService_I_NODES = $root.com.hedera.hapi.node.state.addressbook.Node.fromObject(object.AddressBookService_I_NODES, _depth + 1);
                            }
                            if (object.ConsensusService_I_TOPICS != null) {
                                if (!$util.isObject(object.ConsensusService_I_TOPICS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.ConsensusService_I_TOPICS: object expected");
                                message.ConsensusService_I_TOPICS = $root.proto.Topic.fromObject(object.ConsensusService_I_TOPICS, _depth + 1);
                            }
                            if (object.FreezeService_I_UPGRADE_FILE_HASH != null) {
                                if (!$util.isObject(object.FreezeService_I_UPGRADE_FILE_HASH))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FreezeService_I_UPGRADE_FILE_HASH: object expected");
                                message.FreezeService_I_UPGRADE_FILE_HASH = $root.proto.ProtoBytes.fromObject(object.FreezeService_I_UPGRADE_FILE_HASH, _depth + 1);
                            }
                            if (object.FreezeService_I_FREEZE_TIME != null) {
                                if (!$util.isObject(object.FreezeService_I_FREEZE_TIME))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FreezeService_I_FREEZE_TIME: object expected");
                                message.FreezeService_I_FREEZE_TIME = $root.proto.Timestamp.fromObject(object.FreezeService_I_FREEZE_TIME, _depth + 1);
                            }
                            if (object.BlockStreamService_I_BLOCK_STREAM_INFO != null) {
                                if (!$util.isObject(object.BlockStreamService_I_BLOCK_STREAM_INFO))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.BlockStreamService_I_BLOCK_STREAM_INFO: object expected");
                                message.BlockStreamService_I_BLOCK_STREAM_INFO = $root.com.hedera.hapi.node.state.blockstream.BlockStreamInfo.fromObject(object.BlockStreamService_I_BLOCK_STREAM_INFO, _depth + 1);
                            }
                            if (object.TokenService_I_PENDING_AIRDROPS != null) {
                                if (!$util.isObject(object.TokenService_I_PENDING_AIRDROPS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TokenService_I_PENDING_AIRDROPS: object expected");
                                message.TokenService_I_PENDING_AIRDROPS = $root.proto.AccountPendingAirdrop.fromObject(object.TokenService_I_PENDING_AIRDROPS, _depth + 1);
                            }
                            if (object.PlatformStateService_I_PLATFORM_STATE != null) {
                                if (!$util.isObject(object.PlatformStateService_I_PLATFORM_STATE))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.PlatformStateService_I_PLATFORM_STATE: object expected");
                                message.PlatformStateService_I_PLATFORM_STATE = $root.com.hedera.hapi.platform.state.PlatformState.fromObject(object.PlatformStateService_I_PLATFORM_STATE, _depth + 1);
                            }
                            if (object.RosterService_I_ROSTER_STATE != null) {
                                if (!$util.isObject(object.RosterService_I_ROSTER_STATE))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.RosterService_I_ROSTER_STATE: object expected");
                                message.RosterService_I_ROSTER_STATE = $root.com.hedera.hapi.node.state.roster.RosterState.fromObject(object.RosterService_I_ROSTER_STATE, _depth + 1);
                            }
                            if (object.RosterService_I_ROSTERS != null) {
                                if (!$util.isObject(object.RosterService_I_ROSTERS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.RosterService_I_ROSTERS: object expected");
                                message.RosterService_I_ROSTERS = $root.com.hedera.hapi.node.state.roster.Roster.fromObject(object.RosterService_I_ROSTERS, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULED_COUNTS != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULED_COUNTS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.ScheduleService_I_SCHEDULED_COUNTS: object expected");
                                message.ScheduleService_I_SCHEDULED_COUNTS = $root.proto.ScheduledCounts.fromObject(object.ScheduleService_I_SCHEDULED_COUNTS, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY: object expected");
                                message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY = $root.proto.ScheduleID.fromObject(object.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY, _depth + 1);
                            }
                            if (object.TssBaseService_I_TSS_MESSAGES != null) {
                                if (!$util.isObject(object.TssBaseService_I_TSS_MESSAGES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TssBaseService_I_TSS_MESSAGES: object expected");
                                message.TssBaseService_I_TSS_MESSAGES = $root.com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.fromObject(object.TssBaseService_I_TSS_MESSAGES, _depth + 1);
                            }
                            if (object.TssBaseService_I_TSS_VOTES != null) {
                                if (!$util.isObject(object.TssBaseService_I_TSS_VOTES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TssBaseService_I_TSS_VOTES: object expected");
                                message.TssBaseService_I_TSS_VOTES = $root.com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.fromObject(object.TssBaseService_I_TSS_VOTES, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULED_ORDERS != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULED_ORDERS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.ScheduleService_I_SCHEDULED_ORDERS: object expected");
                                message.ScheduleService_I_SCHEDULED_ORDERS = $root.proto.ScheduleID.fromObject(object.ScheduleService_I_SCHEDULED_ORDERS, _depth + 1);
                            }
                            if (object.ScheduleService_I_SCHEDULED_USAGES != null) {
                                if (!$util.isObject(object.ScheduleService_I_SCHEDULED_USAGES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.ScheduleService_I_SCHEDULED_USAGES: object expected");
                                message.ScheduleService_I_SCHEDULED_USAGES = $root.proto.ThrottleUsageSnapshots.fromObject(object.ScheduleService_I_SCHEDULED_USAGES, _depth + 1);
                            }
                            if (object.TssBaseService_I_TSS_ENCRYPTION_KEYS != null) {
                                if (!$util.isObject(object.TssBaseService_I_TSS_ENCRYPTION_KEYS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TssBaseService_I_TSS_ENCRYPTION_KEYS: object expected");
                                message.TssBaseService_I_TSS_ENCRYPTION_KEYS = $root.com.hedera.hapi.node.state.tss.TssEncryptionKeys.fromObject(object.TssBaseService_I_TSS_ENCRYPTION_KEYS, _depth + 1);
                            }
                            if (object.HintsService_I_HINTS_KEY_SETS != null) {
                                if (!$util.isObject(object.HintsService_I_HINTS_KEY_SETS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HintsService_I_HINTS_KEY_SETS: object expected");
                                message.HintsService_I_HINTS_KEY_SETS = $root.com.hedera.hapi.node.state.hints.HintsKeySet.fromObject(object.HintsService_I_HINTS_KEY_SETS, _depth + 1);
                            }
                            if (object.HintsService_I_ACTIVE_HINTS_CONSTRUCTION != null) {
                                if (!$util.isObject(object.HintsService_I_ACTIVE_HINTS_CONSTRUCTION))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HintsService_I_ACTIVE_HINTS_CONSTRUCTION: object expected");
                                message.HintsService_I_ACTIVE_HINTS_CONSTRUCTION = $root.com.hedera.hapi.node.state.hints.HintsConstruction.fromObject(object.HintsService_I_ACTIVE_HINTS_CONSTRUCTION, _depth + 1);
                            }
                            if (object.HintsService_I_NEXT_HINTS_CONSTRUCTION != null) {
                                if (!$util.isObject(object.HintsService_I_NEXT_HINTS_CONSTRUCTION))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HintsService_I_NEXT_HINTS_CONSTRUCTION: object expected");
                                message.HintsService_I_NEXT_HINTS_CONSTRUCTION = $root.com.hedera.hapi.node.state.hints.HintsConstruction.fromObject(object.HintsService_I_NEXT_HINTS_CONSTRUCTION, _depth + 1);
                            }
                            if (object.HintsService_I_PREPROCESSING_VOTES != null) {
                                if (!$util.isObject(object.HintsService_I_PREPROCESSING_VOTES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HintsService_I_PREPROCESSING_VOTES: object expected");
                                message.HintsService_I_PREPROCESSING_VOTES = $root.com.hedera.hapi.node.state.hints.PreprocessingVote.fromObject(object.HintsService_I_PREPROCESSING_VOTES, _depth + 1);
                            }
                            if (object.EntityIdService_I_ENTITY_COUNTS != null) {
                                if (!$util.isObject(object.EntityIdService_I_ENTITY_COUNTS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.EntityIdService_I_ENTITY_COUNTS: object expected");
                                message.EntityIdService_I_ENTITY_COUNTS = $root.com.hedera.hapi.node.state.entity.EntityCounts.fromObject(object.EntityIdService_I_ENTITY_COUNTS, _depth + 1);
                            }
                            if (object.HistoryService_I_LEDGER_ID != null) {
                                if (!$util.isObject(object.HistoryService_I_LEDGER_ID))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HistoryService_I_LEDGER_ID: object expected");
                                message.HistoryService_I_LEDGER_ID = $root.proto.ProtoBytes.fromObject(object.HistoryService_I_LEDGER_ID, _depth + 1);
                            }
                            if (object.HistoryService_I_PROOF_KEY_SETS != null) {
                                if (!$util.isObject(object.HistoryService_I_PROOF_KEY_SETS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HistoryService_I_PROOF_KEY_SETS: object expected");
                                message.HistoryService_I_PROOF_KEY_SETS = $root.com.hedera.hapi.node.state.history.ProofKeySet.fromObject(object.HistoryService_I_PROOF_KEY_SETS, _depth + 1);
                            }
                            if (object.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION != null) {
                                if (!$util.isObject(object.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION: object expected");
                                message.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION = $root.com.hedera.hapi.node.state.history.HistoryProofConstruction.fromObject(object.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION, _depth + 1);
                            }
                            if (object.HistoryService_I_NEXT_PROOF_CONSTRUCTION != null) {
                                if (!$util.isObject(object.HistoryService_I_NEXT_PROOF_CONSTRUCTION))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HistoryService_I_NEXT_PROOF_CONSTRUCTION: object expected");
                                message.HistoryService_I_NEXT_PROOF_CONSTRUCTION = $root.com.hedera.hapi.node.state.history.HistoryProofConstruction.fromObject(object.HistoryService_I_NEXT_PROOF_CONSTRUCTION, _depth + 1);
                            }
                            if (object.HistoryService_I_HISTORY_SIGNATURES != null) {
                                if (!$util.isObject(object.HistoryService_I_HISTORY_SIGNATURES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HistoryService_I_HISTORY_SIGNATURES: object expected");
                                message.HistoryService_I_HISTORY_SIGNATURES = $root.com.hedera.hapi.node.state.history.RecordedHistorySignature.fromObject(object.HistoryService_I_HISTORY_SIGNATURES, _depth + 1);
                            }
                            if (object.HistoryService_I_PROOF_VOTES != null) {
                                if (!$util.isObject(object.HistoryService_I_PROOF_VOTES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HistoryService_I_PROOF_VOTES: object expected");
                                message.HistoryService_I_PROOF_VOTES = $root.com.hedera.hapi.node.state.history.HistoryProofVote.fromObject(object.HistoryService_I_PROOF_VOTES, _depth + 1);
                            }
                            if (object.HintsService_I_CRS_STATE != null) {
                                if (!$util.isObject(object.HintsService_I_CRS_STATE))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HintsService_I_CRS_STATE: object expected");
                                message.HintsService_I_CRS_STATE = $root.com.hedera.hapi.node.state.hints.CRSState.fromObject(object.HintsService_I_CRS_STATE, _depth + 1);
                            }
                            if (object.HintsService_I_CRS_PUBLICATIONS != null) {
                                if (!$util.isObject(object.HintsService_I_CRS_PUBLICATIONS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HintsService_I_CRS_PUBLICATIONS: object expected");
                                message.HintsService_I_CRS_PUBLICATIONS = $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.fromObject(object.HintsService_I_CRS_PUBLICATIONS, _depth + 1);
                            }
                            if (object.TokenService_I_NODE_REWARDS != null) {
                                if (!$util.isObject(object.TokenService_I_NODE_REWARDS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TokenService_I_NODE_REWARDS: object expected");
                                message.TokenService_I_NODE_REWARDS = $root.proto.NodeRewards.fromObject(object.TokenService_I_NODE_REWARDS, _depth + 1);
                            }
                            if (object.ContractService_I_EVM_HOOK_STATES != null) {
                                if (!$util.isObject(object.ContractService_I_EVM_HOOK_STATES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.ContractService_I_EVM_HOOK_STATES: object expected");
                                message.ContractService_I_EVM_HOOK_STATES = $root.com.hedera.hapi.node.state.hooks.EvmHookState.fromObject(object.ContractService_I_EVM_HOOK_STATES, _depth + 1);
                            }
                            if (object.ContractService_I_EVM_HOOK_STORAGE != null) {
                                if (!$util.isObject(object.ContractService_I_EVM_HOOK_STORAGE))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.ContractService_I_EVM_HOOK_STORAGE: object expected");
                                message.ContractService_I_EVM_HOOK_STORAGE = $root.proto.SlotValue.fromObject(object.ContractService_I_EVM_HOOK_STORAGE, _depth + 1);
                            }
                            if (object.AddressBookService_I_ACCOUNT_NODE_REL != null) {
                                if (!$util.isObject(object.AddressBookService_I_ACCOUNT_NODE_REL))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.AddressBookService_I_ACCOUNT_NODE_REL: object expected");
                                message.AddressBookService_I_ACCOUNT_NODE_REL = $root.com.hedera.hapi.platform.state.NodeId.fromObject(object.AddressBookService_I_ACCOUNT_NODE_REL, _depth + 1);
                            }
                            if (object.HistoryService_I_WRAPS_MESSAGE_HISTORIES != null) {
                                if (!$util.isObject(object.HistoryService_I_WRAPS_MESSAGE_HISTORIES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HistoryService_I_WRAPS_MESSAGE_HISTORIES: object expected");
                                message.HistoryService_I_WRAPS_MESSAGE_HISTORIES = $root.com.hedera.hapi.node.state.history.WrapsMessageHistory.fromObject(object.HistoryService_I_WRAPS_MESSAGE_HISTORIES, _depth + 1);
                            }
                            if (object.TokenService_I_NODE_PAYMENTS != null) {
                                if (!$util.isObject(object.TokenService_I_NODE_PAYMENTS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.TokenService_I_NODE_PAYMENTS: object expected");
                                message.TokenService_I_NODE_PAYMENTS = $root.com.hedera.hapi.node.state.token.NodePayments.fromObject(object.TokenService_I_NODE_PAYMENTS, _depth + 1);
                            }
                            if (object.AddressBookService_I_REGISTERED_NODES != null) {
                                if (!$util.isObject(object.AddressBookService_I_REGISTERED_NODES))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.AddressBookService_I_REGISTERED_NODES: object expected");
                                message.AddressBookService_I_REGISTERED_NODES = $root.com.hedera.hapi.node.state.addressbook.RegisteredNode.fromObject(object.AddressBookService_I_REGISTERED_NODES, _depth + 1);
                            }
                            if (object.EntityIdService_I_HIGHEST_NODE_ID != null) {
                                if (!$util.isObject(object.EntityIdService_I_HIGHEST_NODE_ID))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.EntityIdService_I_HIGHEST_NODE_ID: object expected");
                                message.EntityIdService_I_HIGHEST_NODE_ID = $root.com.hedera.hapi.platform.state.NodeId.fromObject(object.EntityIdService_I_HIGHEST_NODE_ID, _depth + 1);
                            }
                            if (object.HistoryService_I_WRAPS_PROVING_KEY_HASH != null) {
                                if (!$util.isObject(object.HistoryService_I_WRAPS_PROVING_KEY_HASH))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.HistoryService_I_WRAPS_PROVING_KEY_HASH: object expected");
                                message.HistoryService_I_WRAPS_PROVING_KEY_HASH = $root.proto.ProtoBytes.fromObject(object.HistoryService_I_WRAPS_PROVING_KEY_HASH, _depth + 1);
                            }
                            if (object.RecordCache_I_TRANSACTION_RECEIPTS != null) {
                                if (!$util.isObject(object.RecordCache_I_TRANSACTION_RECEIPTS))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.RecordCache_I_TRANSACTION_RECEIPTS: object expected");
                                message.RecordCache_I_TRANSACTION_RECEIPTS = $root.proto.TransactionReceiptEntries.fromObject(object.RecordCache_I_TRANSACTION_RECEIPTS, _depth + 1);
                            }
                            if (object.queueState != null) {
                                if (!$util.isObject(object.queueState))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.queueState: object expected");
                                message.queueState = $root.com.hedera.hapi.platform.state.QueueState.fromObject(object.queueState, _depth + 1);
                            }
                            if (object.FileService_I_UPGRADE_DATA_150 != null) {
                                if (!$util.isObject(object.FileService_I_UPGRADE_DATA_150))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FileService_I_UPGRADE_DATA_150: object expected");
                                message.FileService_I_UPGRADE_DATA_150 = $root.proto.ProtoBytes.fromObject(object.FileService_I_UPGRADE_DATA_150, _depth + 1);
                            }
                            if (object.FileService_I_UPGRADE_DATA_151 != null) {
                                if (!$util.isObject(object.FileService_I_UPGRADE_DATA_151))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FileService_I_UPGRADE_DATA_151: object expected");
                                message.FileService_I_UPGRADE_DATA_151 = $root.proto.ProtoBytes.fromObject(object.FileService_I_UPGRADE_DATA_151, _depth + 1);
                            }
                            if (object.FileService_I_UPGRADE_DATA_152 != null) {
                                if (!$util.isObject(object.FileService_I_UPGRADE_DATA_152))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FileService_I_UPGRADE_DATA_152: object expected");
                                message.FileService_I_UPGRADE_DATA_152 = $root.proto.ProtoBytes.fromObject(object.FileService_I_UPGRADE_DATA_152, _depth + 1);
                            }
                            if (object.FileService_I_UPGRADE_DATA_153 != null) {
                                if (!$util.isObject(object.FileService_I_UPGRADE_DATA_153))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FileService_I_UPGRADE_DATA_153: object expected");
                                message.FileService_I_UPGRADE_DATA_153 = $root.proto.ProtoBytes.fromObject(object.FileService_I_UPGRADE_DATA_153, _depth + 1);
                            }
                            if (object.FileService_I_UPGRADE_DATA_154 != null) {
                                if (!$util.isObject(object.FileService_I_UPGRADE_DATA_154))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FileService_I_UPGRADE_DATA_154: object expected");
                                message.FileService_I_UPGRADE_DATA_154 = $root.proto.ProtoBytes.fromObject(object.FileService_I_UPGRADE_DATA_154, _depth + 1);
                            }
                            if (object.FileService_I_UPGRADE_DATA_155 != null) {
                                if (!$util.isObject(object.FileService_I_UPGRADE_DATA_155))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FileService_I_UPGRADE_DATA_155: object expected");
                                message.FileService_I_UPGRADE_DATA_155 = $root.proto.ProtoBytes.fromObject(object.FileService_I_UPGRADE_DATA_155, _depth + 1);
                            }
                            if (object.FileService_I_UPGRADE_DATA_156 != null) {
                                if (!$util.isObject(object.FileService_I_UPGRADE_DATA_156))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FileService_I_UPGRADE_DATA_156: object expected");
                                message.FileService_I_UPGRADE_DATA_156 = $root.proto.ProtoBytes.fromObject(object.FileService_I_UPGRADE_DATA_156, _depth + 1);
                            }
                            if (object.FileService_I_UPGRADE_DATA_157 != null) {
                                if (!$util.isObject(object.FileService_I_UPGRADE_DATA_157))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FileService_I_UPGRADE_DATA_157: object expected");
                                message.FileService_I_UPGRADE_DATA_157 = $root.proto.ProtoBytes.fromObject(object.FileService_I_UPGRADE_DATA_157, _depth + 1);
                            }
                            if (object.FileService_I_UPGRADE_DATA_158 != null) {
                                if (!$util.isObject(object.FileService_I_UPGRADE_DATA_158))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FileService_I_UPGRADE_DATA_158: object expected");
                                message.FileService_I_UPGRADE_DATA_158 = $root.proto.ProtoBytes.fromObject(object.FileService_I_UPGRADE_DATA_158, _depth + 1);
                            }
                            if (object.FileService_I_UPGRADE_DATA_159 != null) {
                                if (!$util.isObject(object.FileService_I_UPGRADE_DATA_159))
                                    throw $TypeError(".com.hedera.hapi.platform.state.StateValue.FileService_I_UPGRADE_DATA_159: object expected");
                                message.FileService_I_UPGRADE_DATA_159 = $root.proto.ProtoBytes.fromObject(object.FileService_I_UPGRADE_DATA_159, _depth + 1);
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a StateValue message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @static
                         * @param {com.hedera.hapi.platform.state.StateValue} message StateValue
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        StateValue.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (message.EntityIdService_I_ENTITY_ID != null && $Object.hasOwnProperty.call(message, "EntityIdService_I_ENTITY_ID")) {
                                object.EntityIdService_I_ENTITY_ID = $root.proto.EntityNumber.toObject(message.EntityIdService_I_ENTITY_ID, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "EntityIdService_I_ENTITY_ID";
                            }
                            if (message.TokenService_I_ACCOUNTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_ACCOUNTS")) {
                                object.TokenService_I_ACCOUNTS = $root.proto.Account.toObject(message.TokenService_I_ACCOUNTS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TokenService_I_ACCOUNTS";
                            }
                            if (message.TokenService_I_ALIASES != null && $Object.hasOwnProperty.call(message, "TokenService_I_ALIASES")) {
                                object.TokenService_I_ALIASES = $root.proto.AccountID.toObject(message.TokenService_I_ALIASES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TokenService_I_ALIASES";
                            }
                            if (message.ContractService_I_STORAGE != null && $Object.hasOwnProperty.call(message, "ContractService_I_STORAGE")) {
                                object.ContractService_I_STORAGE = $root.proto.SlotValue.toObject(message.ContractService_I_STORAGE, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "ContractService_I_STORAGE";
                            }
                            if (message.ContractService_I_BYTECODE != null && $Object.hasOwnProperty.call(message, "ContractService_I_BYTECODE")) {
                                object.ContractService_I_BYTECODE = $root.proto.Bytecode.toObject(message.ContractService_I_BYTECODE, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "ContractService_I_BYTECODE";
                            }
                            if (message.FileService_I_FILES != null && $Object.hasOwnProperty.call(message, "FileService_I_FILES")) {
                                object.FileService_I_FILES = $root.proto.File.toObject(message.FileService_I_FILES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FileService_I_FILES";
                            }
                            if (message.TokenService_I_TOKENS != null && $Object.hasOwnProperty.call(message, "TokenService_I_TOKENS")) {
                                object.TokenService_I_TOKENS = $root.proto.Token.toObject(message.TokenService_I_TOKENS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TokenService_I_TOKENS";
                            }
                            if (message.TokenService_I_NFTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_NFTS")) {
                                object.TokenService_I_NFTS = $root.proto.Nft.toObject(message.TokenService_I_NFTS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TokenService_I_NFTS";
                            }
                            if (message.TokenService_I_TOKEN_RELS != null && $Object.hasOwnProperty.call(message, "TokenService_I_TOKEN_RELS")) {
                                object.TokenService_I_TOKEN_RELS = $root.proto.TokenRelation.toObject(message.TokenService_I_TOKEN_RELS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TokenService_I_TOKEN_RELS";
                            }
                            if (message.TokenService_I_STAKING_INFOS != null && $Object.hasOwnProperty.call(message, "TokenService_I_STAKING_INFOS")) {
                                object.TokenService_I_STAKING_INFOS = $root.proto.StakingNodeInfo.toObject(message.TokenService_I_STAKING_INFOS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TokenService_I_STAKING_INFOS";
                            }
                            if (message.TokenService_I_STAKING_NETWORK_REWARDS != null && $Object.hasOwnProperty.call(message, "TokenService_I_STAKING_NETWORK_REWARDS")) {
                                object.TokenService_I_STAKING_NETWORK_REWARDS = $root.proto.NetworkStakingRewards.toObject(message.TokenService_I_STAKING_NETWORK_REWARDS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TokenService_I_STAKING_NETWORK_REWARDS";
                            }
                            if (message.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS != null && $Object.hasOwnProperty.call(message, "CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS")) {
                                object.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS = $root.proto.ThrottleUsageSnapshots.toObject(message.CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS";
                            }
                            if (message.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS != null && $Object.hasOwnProperty.call(message, "CongestionThrottleService_I_CONGESTION_LEVEL_STARTS")) {
                                object.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS = $root.proto.CongestionLevelStarts.toObject(message.CongestionThrottleService_I_CONGESTION_LEVEL_STARTS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "CongestionThrottleService_I_CONGESTION_LEVEL_STARTS";
                            }
                            if (message.ScheduleService_I_SCHEDULES_BY_ID != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_ID")) {
                                object.ScheduleService_I_SCHEDULES_BY_ID = $root.proto.Schedule.toObject(message.ScheduleService_I_SCHEDULES_BY_ID, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "ScheduleService_I_SCHEDULES_BY_ID";
                            }
                            if (message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC")) {
                                object.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC = $root.proto.ScheduleList.toObject(message.ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "ScheduleService_I_SCHEDULES_BY_EXPIRY_SEC";
                            }
                            if (message.ScheduleService_I_SCHEDULES_BY_EQUALITY != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULES_BY_EQUALITY")) {
                                object.ScheduleService_I_SCHEDULES_BY_EQUALITY = $root.proto.ScheduleList.toObject(message.ScheduleService_I_SCHEDULES_BY_EQUALITY, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "ScheduleService_I_SCHEDULES_BY_EQUALITY";
                            }
                            if (message.FeeService_I_MIDNIGHT_RATES != null && $Object.hasOwnProperty.call(message, "FeeService_I_MIDNIGHT_RATES")) {
                                object.FeeService_I_MIDNIGHT_RATES = $root.proto.ExchangeRateSet.toObject(message.FeeService_I_MIDNIGHT_RATES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FeeService_I_MIDNIGHT_RATES";
                            }
                            if (message.BlockRecordService_I_RUNNING_HASHES != null && $Object.hasOwnProperty.call(message, "BlockRecordService_I_RUNNING_HASHES")) {
                                object.BlockRecordService_I_RUNNING_HASHES = $root.proto.RunningHashes.toObject(message.BlockRecordService_I_RUNNING_HASHES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "BlockRecordService_I_RUNNING_HASHES";
                            }
                            if (message.BlockRecordService_I_BLOCKS != null && $Object.hasOwnProperty.call(message, "BlockRecordService_I_BLOCKS")) {
                                object.BlockRecordService_I_BLOCKS = $root.proto.BlockInfo.toObject(message.BlockRecordService_I_BLOCKS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "BlockRecordService_I_BLOCKS";
                            }
                            if (message.AddressBookService_I_NODES != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_NODES")) {
                                object.AddressBookService_I_NODES = $root.com.hedera.hapi.node.state.addressbook.Node.toObject(message.AddressBookService_I_NODES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "AddressBookService_I_NODES";
                            }
                            if (message.ConsensusService_I_TOPICS != null && $Object.hasOwnProperty.call(message, "ConsensusService_I_TOPICS")) {
                                object.ConsensusService_I_TOPICS = $root.proto.Topic.toObject(message.ConsensusService_I_TOPICS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "ConsensusService_I_TOPICS";
                            }
                            if (message.FreezeService_I_UPGRADE_FILE_HASH != null && $Object.hasOwnProperty.call(message, "FreezeService_I_UPGRADE_FILE_HASH")) {
                                object.FreezeService_I_UPGRADE_FILE_HASH = $root.proto.ProtoBytes.toObject(message.FreezeService_I_UPGRADE_FILE_HASH, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FreezeService_I_UPGRADE_FILE_HASH";
                            }
                            if (message.FreezeService_I_FREEZE_TIME != null && $Object.hasOwnProperty.call(message, "FreezeService_I_FREEZE_TIME")) {
                                object.FreezeService_I_FREEZE_TIME = $root.proto.Timestamp.toObject(message.FreezeService_I_FREEZE_TIME, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FreezeService_I_FREEZE_TIME";
                            }
                            if (message.BlockStreamService_I_BLOCK_STREAM_INFO != null && $Object.hasOwnProperty.call(message, "BlockStreamService_I_BLOCK_STREAM_INFO")) {
                                object.BlockStreamService_I_BLOCK_STREAM_INFO = $root.com.hedera.hapi.node.state.blockstream.BlockStreamInfo.toObject(message.BlockStreamService_I_BLOCK_STREAM_INFO, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "BlockStreamService_I_BLOCK_STREAM_INFO";
                            }
                            if (message.TokenService_I_PENDING_AIRDROPS != null && $Object.hasOwnProperty.call(message, "TokenService_I_PENDING_AIRDROPS")) {
                                object.TokenService_I_PENDING_AIRDROPS = $root.proto.AccountPendingAirdrop.toObject(message.TokenService_I_PENDING_AIRDROPS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TokenService_I_PENDING_AIRDROPS";
                            }
                            if (message.PlatformStateService_I_PLATFORM_STATE != null && $Object.hasOwnProperty.call(message, "PlatformStateService_I_PLATFORM_STATE")) {
                                object.PlatformStateService_I_PLATFORM_STATE = $root.com.hedera.hapi.platform.state.PlatformState.toObject(message.PlatformStateService_I_PLATFORM_STATE, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "PlatformStateService_I_PLATFORM_STATE";
                            }
                            if (message.RosterService_I_ROSTER_STATE != null && $Object.hasOwnProperty.call(message, "RosterService_I_ROSTER_STATE")) {
                                object.RosterService_I_ROSTER_STATE = $root.com.hedera.hapi.node.state.roster.RosterState.toObject(message.RosterService_I_ROSTER_STATE, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "RosterService_I_ROSTER_STATE";
                            }
                            if (message.RosterService_I_ROSTERS != null && $Object.hasOwnProperty.call(message, "RosterService_I_ROSTERS")) {
                                object.RosterService_I_ROSTERS = $root.com.hedera.hapi.node.state.roster.Roster.toObject(message.RosterService_I_ROSTERS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "RosterService_I_ROSTERS";
                            }
                            if (message.ScheduleService_I_SCHEDULED_COUNTS != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_COUNTS")) {
                                object.ScheduleService_I_SCHEDULED_COUNTS = $root.proto.ScheduledCounts.toObject(message.ScheduleService_I_SCHEDULED_COUNTS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "ScheduleService_I_SCHEDULED_COUNTS";
                            }
                            if (message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY")) {
                                object.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY = $root.proto.ScheduleID.toObject(message.ScheduleService_I_SCHEDULE_ID_BY_EQUALITY, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "ScheduleService_I_SCHEDULE_ID_BY_EQUALITY";
                            }
                            if (message.TssBaseService_I_TSS_MESSAGES != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_MESSAGES")) {
                                object.TssBaseService_I_TSS_MESSAGES = $root.com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.toObject(message.TssBaseService_I_TSS_MESSAGES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TssBaseService_I_TSS_MESSAGES";
                            }
                            if (message.TssBaseService_I_TSS_VOTES != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_VOTES")) {
                                object.TssBaseService_I_TSS_VOTES = $root.com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.toObject(message.TssBaseService_I_TSS_VOTES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TssBaseService_I_TSS_VOTES";
                            }
                            if (message.ScheduleService_I_SCHEDULED_ORDERS != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_ORDERS")) {
                                object.ScheduleService_I_SCHEDULED_ORDERS = $root.proto.ScheduleID.toObject(message.ScheduleService_I_SCHEDULED_ORDERS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "ScheduleService_I_SCHEDULED_ORDERS";
                            }
                            if (message.ScheduleService_I_SCHEDULED_USAGES != null && $Object.hasOwnProperty.call(message, "ScheduleService_I_SCHEDULED_USAGES")) {
                                object.ScheduleService_I_SCHEDULED_USAGES = $root.proto.ThrottleUsageSnapshots.toObject(message.ScheduleService_I_SCHEDULED_USAGES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "ScheduleService_I_SCHEDULED_USAGES";
                            }
                            if (message.TssBaseService_I_TSS_ENCRYPTION_KEYS != null && $Object.hasOwnProperty.call(message, "TssBaseService_I_TSS_ENCRYPTION_KEYS")) {
                                object.TssBaseService_I_TSS_ENCRYPTION_KEYS = $root.com.hedera.hapi.node.state.tss.TssEncryptionKeys.toObject(message.TssBaseService_I_TSS_ENCRYPTION_KEYS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TssBaseService_I_TSS_ENCRYPTION_KEYS";
                            }
                            if (message.HintsService_I_HINTS_KEY_SETS != null && $Object.hasOwnProperty.call(message, "HintsService_I_HINTS_KEY_SETS")) {
                                object.HintsService_I_HINTS_KEY_SETS = $root.com.hedera.hapi.node.state.hints.HintsKeySet.toObject(message.HintsService_I_HINTS_KEY_SETS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HintsService_I_HINTS_KEY_SETS";
                            }
                            if (message.HintsService_I_ACTIVE_HINTS_CONSTRUCTION != null && $Object.hasOwnProperty.call(message, "HintsService_I_ACTIVE_HINTS_CONSTRUCTION")) {
                                object.HintsService_I_ACTIVE_HINTS_CONSTRUCTION = $root.com.hedera.hapi.node.state.hints.HintsConstruction.toObject(message.HintsService_I_ACTIVE_HINTS_CONSTRUCTION, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HintsService_I_ACTIVE_HINTS_CONSTRUCTION";
                            }
                            if (message.HintsService_I_NEXT_HINTS_CONSTRUCTION != null && $Object.hasOwnProperty.call(message, "HintsService_I_NEXT_HINTS_CONSTRUCTION")) {
                                object.HintsService_I_NEXT_HINTS_CONSTRUCTION = $root.com.hedera.hapi.node.state.hints.HintsConstruction.toObject(message.HintsService_I_NEXT_HINTS_CONSTRUCTION, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HintsService_I_NEXT_HINTS_CONSTRUCTION";
                            }
                            if (message.HintsService_I_PREPROCESSING_VOTES != null && $Object.hasOwnProperty.call(message, "HintsService_I_PREPROCESSING_VOTES")) {
                                object.HintsService_I_PREPROCESSING_VOTES = $root.com.hedera.hapi.node.state.hints.PreprocessingVote.toObject(message.HintsService_I_PREPROCESSING_VOTES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HintsService_I_PREPROCESSING_VOTES";
                            }
                            if (message.EntityIdService_I_ENTITY_COUNTS != null && $Object.hasOwnProperty.call(message, "EntityIdService_I_ENTITY_COUNTS")) {
                                object.EntityIdService_I_ENTITY_COUNTS = $root.com.hedera.hapi.node.state.entity.EntityCounts.toObject(message.EntityIdService_I_ENTITY_COUNTS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "EntityIdService_I_ENTITY_COUNTS";
                            }
                            if (message.HistoryService_I_LEDGER_ID != null && $Object.hasOwnProperty.call(message, "HistoryService_I_LEDGER_ID")) {
                                object.HistoryService_I_LEDGER_ID = $root.proto.ProtoBytes.toObject(message.HistoryService_I_LEDGER_ID, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HistoryService_I_LEDGER_ID";
                            }
                            if (message.HistoryService_I_PROOF_KEY_SETS != null && $Object.hasOwnProperty.call(message, "HistoryService_I_PROOF_KEY_SETS")) {
                                object.HistoryService_I_PROOF_KEY_SETS = $root.com.hedera.hapi.node.state.history.ProofKeySet.toObject(message.HistoryService_I_PROOF_KEY_SETS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HistoryService_I_PROOF_KEY_SETS";
                            }
                            if (message.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION != null && $Object.hasOwnProperty.call(message, "HistoryService_I_ACTIVE_PROOF_CONSTRUCTION")) {
                                object.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION = $root.com.hedera.hapi.node.state.history.HistoryProofConstruction.toObject(message.HistoryService_I_ACTIVE_PROOF_CONSTRUCTION, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HistoryService_I_ACTIVE_PROOF_CONSTRUCTION";
                            }
                            if (message.HistoryService_I_NEXT_PROOF_CONSTRUCTION != null && $Object.hasOwnProperty.call(message, "HistoryService_I_NEXT_PROOF_CONSTRUCTION")) {
                                object.HistoryService_I_NEXT_PROOF_CONSTRUCTION = $root.com.hedera.hapi.node.state.history.HistoryProofConstruction.toObject(message.HistoryService_I_NEXT_PROOF_CONSTRUCTION, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HistoryService_I_NEXT_PROOF_CONSTRUCTION";
                            }
                            if (message.HistoryService_I_HISTORY_SIGNATURES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_HISTORY_SIGNATURES")) {
                                object.HistoryService_I_HISTORY_SIGNATURES = $root.com.hedera.hapi.node.state.history.RecordedHistorySignature.toObject(message.HistoryService_I_HISTORY_SIGNATURES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HistoryService_I_HISTORY_SIGNATURES";
                            }
                            if (message.HistoryService_I_PROOF_VOTES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_PROOF_VOTES")) {
                                object.HistoryService_I_PROOF_VOTES = $root.com.hedera.hapi.node.state.history.HistoryProofVote.toObject(message.HistoryService_I_PROOF_VOTES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HistoryService_I_PROOF_VOTES";
                            }
                            if (message.HintsService_I_CRS_STATE != null && $Object.hasOwnProperty.call(message, "HintsService_I_CRS_STATE")) {
                                object.HintsService_I_CRS_STATE = $root.com.hedera.hapi.node.state.hints.CRSState.toObject(message.HintsService_I_CRS_STATE, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HintsService_I_CRS_STATE";
                            }
                            if (message.HintsService_I_CRS_PUBLICATIONS != null && $Object.hasOwnProperty.call(message, "HintsService_I_CRS_PUBLICATIONS")) {
                                object.HintsService_I_CRS_PUBLICATIONS = $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.toObject(message.HintsService_I_CRS_PUBLICATIONS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HintsService_I_CRS_PUBLICATIONS";
                            }
                            if (message.TokenService_I_NODE_REWARDS != null && $Object.hasOwnProperty.call(message, "TokenService_I_NODE_REWARDS")) {
                                object.TokenService_I_NODE_REWARDS = $root.proto.NodeRewards.toObject(message.TokenService_I_NODE_REWARDS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TokenService_I_NODE_REWARDS";
                            }
                            if (message.ContractService_I_EVM_HOOK_STATES != null && $Object.hasOwnProperty.call(message, "ContractService_I_EVM_HOOK_STATES")) {
                                object.ContractService_I_EVM_HOOK_STATES = $root.com.hedera.hapi.node.state.hooks.EvmHookState.toObject(message.ContractService_I_EVM_HOOK_STATES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "ContractService_I_EVM_HOOK_STATES";
                            }
                            if (message.ContractService_I_EVM_HOOK_STORAGE != null && $Object.hasOwnProperty.call(message, "ContractService_I_EVM_HOOK_STORAGE")) {
                                object.ContractService_I_EVM_HOOK_STORAGE = $root.proto.SlotValue.toObject(message.ContractService_I_EVM_HOOK_STORAGE, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "ContractService_I_EVM_HOOK_STORAGE";
                            }
                            if (message.AddressBookService_I_ACCOUNT_NODE_REL != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_ACCOUNT_NODE_REL")) {
                                object.AddressBookService_I_ACCOUNT_NODE_REL = $root.com.hedera.hapi.platform.state.NodeId.toObject(message.AddressBookService_I_ACCOUNT_NODE_REL, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "AddressBookService_I_ACCOUNT_NODE_REL";
                            }
                            if (message.HistoryService_I_WRAPS_MESSAGE_HISTORIES != null && $Object.hasOwnProperty.call(message, "HistoryService_I_WRAPS_MESSAGE_HISTORIES")) {
                                object.HistoryService_I_WRAPS_MESSAGE_HISTORIES = $root.com.hedera.hapi.node.state.history.WrapsMessageHistory.toObject(message.HistoryService_I_WRAPS_MESSAGE_HISTORIES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HistoryService_I_WRAPS_MESSAGE_HISTORIES";
                            }
                            if (message.TokenService_I_NODE_PAYMENTS != null && $Object.hasOwnProperty.call(message, "TokenService_I_NODE_PAYMENTS")) {
                                object.TokenService_I_NODE_PAYMENTS = $root.com.hedera.hapi.node.state.token.NodePayments.toObject(message.TokenService_I_NODE_PAYMENTS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "TokenService_I_NODE_PAYMENTS";
                            }
                            if (message.AddressBookService_I_REGISTERED_NODES != null && $Object.hasOwnProperty.call(message, "AddressBookService_I_REGISTERED_NODES")) {
                                object.AddressBookService_I_REGISTERED_NODES = $root.com.hedera.hapi.node.state.addressbook.RegisteredNode.toObject(message.AddressBookService_I_REGISTERED_NODES, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "AddressBookService_I_REGISTERED_NODES";
                            }
                            if (message.EntityIdService_I_HIGHEST_NODE_ID != null && $Object.hasOwnProperty.call(message, "EntityIdService_I_HIGHEST_NODE_ID")) {
                                object.EntityIdService_I_HIGHEST_NODE_ID = $root.com.hedera.hapi.platform.state.NodeId.toObject(message.EntityIdService_I_HIGHEST_NODE_ID, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "EntityIdService_I_HIGHEST_NODE_ID";
                            }
                            if (message.HistoryService_I_WRAPS_PROVING_KEY_HASH != null && $Object.hasOwnProperty.call(message, "HistoryService_I_WRAPS_PROVING_KEY_HASH")) {
                                object.HistoryService_I_WRAPS_PROVING_KEY_HASH = $root.proto.ProtoBytes.toObject(message.HistoryService_I_WRAPS_PROVING_KEY_HASH, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "HistoryService_I_WRAPS_PROVING_KEY_HASH";
                            }
                            if (message.RecordCache_I_TRANSACTION_RECEIPTS != null && $Object.hasOwnProperty.call(message, "RecordCache_I_TRANSACTION_RECEIPTS")) {
                                object.RecordCache_I_TRANSACTION_RECEIPTS = $root.proto.TransactionReceiptEntries.toObject(message.RecordCache_I_TRANSACTION_RECEIPTS, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "RecordCache_I_TRANSACTION_RECEIPTS";
                            }
                            if (message.queueState != null && $Object.hasOwnProperty.call(message, "queueState")) {
                                object.queueState = $root.com.hedera.hapi.platform.state.QueueState.toObject(message.queueState, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "queueState";
                            }
                            if (message.FileService_I_UPGRADE_DATA_150 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_150")) {
                                object.FileService_I_UPGRADE_DATA_150 = $root.proto.ProtoBytes.toObject(message.FileService_I_UPGRADE_DATA_150, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FileService_I_UPGRADE_DATA_150";
                            }
                            if (message.FileService_I_UPGRADE_DATA_151 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_151")) {
                                object.FileService_I_UPGRADE_DATA_151 = $root.proto.ProtoBytes.toObject(message.FileService_I_UPGRADE_DATA_151, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FileService_I_UPGRADE_DATA_151";
                            }
                            if (message.FileService_I_UPGRADE_DATA_152 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_152")) {
                                object.FileService_I_UPGRADE_DATA_152 = $root.proto.ProtoBytes.toObject(message.FileService_I_UPGRADE_DATA_152, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FileService_I_UPGRADE_DATA_152";
                            }
                            if (message.FileService_I_UPGRADE_DATA_153 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_153")) {
                                object.FileService_I_UPGRADE_DATA_153 = $root.proto.ProtoBytes.toObject(message.FileService_I_UPGRADE_DATA_153, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FileService_I_UPGRADE_DATA_153";
                            }
                            if (message.FileService_I_UPGRADE_DATA_154 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_154")) {
                                object.FileService_I_UPGRADE_DATA_154 = $root.proto.ProtoBytes.toObject(message.FileService_I_UPGRADE_DATA_154, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FileService_I_UPGRADE_DATA_154";
                            }
                            if (message.FileService_I_UPGRADE_DATA_155 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_155")) {
                                object.FileService_I_UPGRADE_DATA_155 = $root.proto.ProtoBytes.toObject(message.FileService_I_UPGRADE_DATA_155, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FileService_I_UPGRADE_DATA_155";
                            }
                            if (message.FileService_I_UPGRADE_DATA_156 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_156")) {
                                object.FileService_I_UPGRADE_DATA_156 = $root.proto.ProtoBytes.toObject(message.FileService_I_UPGRADE_DATA_156, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FileService_I_UPGRADE_DATA_156";
                            }
                            if (message.FileService_I_UPGRADE_DATA_157 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_157")) {
                                object.FileService_I_UPGRADE_DATA_157 = $root.proto.ProtoBytes.toObject(message.FileService_I_UPGRADE_DATA_157, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FileService_I_UPGRADE_DATA_157";
                            }
                            if (message.FileService_I_UPGRADE_DATA_158 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_158")) {
                                object.FileService_I_UPGRADE_DATA_158 = $root.proto.ProtoBytes.toObject(message.FileService_I_UPGRADE_DATA_158, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FileService_I_UPGRADE_DATA_158";
                            }
                            if (message.FileService_I_UPGRADE_DATA_159 != null && $Object.hasOwnProperty.call(message, "FileService_I_UPGRADE_DATA_159")) {
                                object.FileService_I_UPGRADE_DATA_159 = $root.proto.ProtoBytes.toObject(message.FileService_I_UPGRADE_DATA_159, options, _depth + 1);
                                if (options.oneofs)
                                    object.value = "FileService_I_UPGRADE_DATA_159";
                            }
                            return object;
                        };

                        /**
                         * Converts this StateValue to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        StateValue.prototype.toJSON = function() {
                            return StateValue.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for StateValue
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.platform.state.StateValue
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        StateValue.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.platform.state.StateValue";
                        };

                        return StateValue;
                    })();

                    /**
                     * An enumeration of the singleton state types.
                     * @name com.hedera.hapi.platform.state.SingletonType
                     * @enum {number}
                     * @property {number} UNKNOWN=0 An "unset value" flag, this value SHALL NOT be used.<br/>
                     * This status indicates the server software failed to set a
                     * status, and SHALL be considered a software defect.
                     * @property {number} EntityIdService_I_ENTITY_ID=1 A state identifier for the next entity Identifier.
                     * @property {number} TokenService_I_STAKING_NETWORK_REWARDS=11 A state identifier for network staking rewards.
                     * @property {number} CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS=12 A state identifier for throttle usage.
                     * @property {number} CongestionThrottleService_I_CONGESTION_LEVEL_STARTS=13 A state identifier for network congestion start times.
                     * @property {number} FeeService_I_MIDNIGHT_RATES=17 A state identifier for conversion rate updates.
                     * @property {number} BlockRecordService_I_RUNNING_HASHES=18 A state identifier for the network running hash(es).
                     * @property {number} BlockRecordService_I_BLOCKS=19 A state identifier for network block information.
                     * @property {number} FreezeService_I_UPGRADE_FILE_HASH=22 A state identifier for the hash of the next "upgrade" file.
                     * @property {number} FreezeService_I_FREEZE_TIME=23 A state identifier for the next network freeze time. Singleton state.
                     * @property {number} BlockStreamService_I_BLOCK_STREAM_INFO=24 A state identifier for the block stream status. Singleton state.
                     * @property {number} PlatformStateService_I_PLATFORM_STATE=26 A state identifier for the platform state. Singleton state.
                     * @property {number} RosterService_I_ROSTER_STATE=27 A state identifier for the roster state. Singleton state.
                     * @property {number} HintsService_I_ACTIVE_HINTS_CONSTRUCTION=38 A state identifier for the active hinTS construction. Singleton state.
                     * @property {number} HintsService_I_NEXT_HINTS_CONSTRUCTION=39 A state identifier for the next hinTS construction. Singleton state.
                     * @property {number} EntityIdService_I_ENTITY_COUNTS=41 A state identifier for the entity counts. Singleton state.
                     * @property {number} HistoryService_I_LEDGER_ID=42 A state identifier for the ledger id. Singleton state.
                     * @property {number} HistoryService_I_ACTIVE_PROOF_CONSTRUCTION=44 A state identifier for the active proof construction. Singleton state.
                     * @property {number} HistoryService_I_NEXT_PROOF_CONSTRUCTION=45 A state identifier for the next proof construction. Singleton state.
                     * @property {number} HintsService_I_CRS_STATE=48 A state identifier for the CRS state. Singleton state.
                     * @property {number} TokenService_I_NODE_REWARDS=50 A state identifier for the node rewards. Singleton state.
                     * @property {number} TokenService_I_NODE_PAYMENTS=55 A state identifier for the node payments. Singleton state.
                     * @property {number} AddressBookService_I_REGISTERED_NODES=56 A state identifier for registered nodes.
                     * @property {number} EntityIdService_I_HIGHEST_NODE_ID=57 A state identifier for the highest node identifier allocated so far. Singleton state.
                     * @property {number} HistoryService_I_WRAPS_PROVING_KEY_HASH=58 A state identifier for the expected WRAPS proving key hash. Singleton state.
                     * @property {number} RecordCache_I_TRANSACTION_RECEIPTS=126 Metadata of the round receipts queue.
                     * @property {number} FileService_I_UPGRADE_DATA_150=10001 Metadata of the `150` upgrade file data queue.
                     * @property {number} FileService_I_UPGRADE_DATA_151=10002 Metadata of the`151` upgrade file data queue.
                     * @property {number} FileService_I_UPGRADE_DATA_152=10003 Metadata of the `152` upgrade file data queue.
                     * @property {number} FileService_I_UPGRADE_DATA_153=10004 Metadata of the `153` upgrade file data queue.
                     * @property {number} FileService_I_UPGRADE_DATA_154=10005 Metadata of the`154` upgrade file data queue.
                     * @property {number} FileService_I_UPGRADE_DATA_155=10006 Metadata of the `155` upgrade file data queue.
                     * @property {number} FileService_I_UPGRADE_DATA_156=10007 Metadata of the `156` upgrade file data queue.
                     * @property {number} FileService_I_UPGRADE_DATA_157=10008 Metadata of the `157` upgrade file data queue.
                     * @property {number} FileService_I_UPGRADE_DATA_158=10009 Metadata of the `158` upgrade file data queue.
                     * @property {number} FileService_I_UPGRADE_DATA_159=10010 Metadata of the `159` upgrade file data queue.
                     */
                    state.SingletonType = (function() {
                        const valuesById = $Object.create(null), values = $Object.create(valuesById);
                        values[valuesById[0] = "UNKNOWN"] = 0;
                        values[valuesById[1] = "EntityIdService_I_ENTITY_ID"] = 1;
                        values[valuesById[11] = "TokenService_I_STAKING_NETWORK_REWARDS"] = 11;
                        values[valuesById[12] = "CongestionThrottleService_I_THROTTLE_USAGE_SNAPSHOTS"] = 12;
                        values[valuesById[13] = "CongestionThrottleService_I_CONGESTION_LEVEL_STARTS"] = 13;
                        values[valuesById[17] = "FeeService_I_MIDNIGHT_RATES"] = 17;
                        values[valuesById[18] = "BlockRecordService_I_RUNNING_HASHES"] = 18;
                        values[valuesById[19] = "BlockRecordService_I_BLOCKS"] = 19;
                        values[valuesById[22] = "FreezeService_I_UPGRADE_FILE_HASH"] = 22;
                        values[valuesById[23] = "FreezeService_I_FREEZE_TIME"] = 23;
                        values[valuesById[24] = "BlockStreamService_I_BLOCK_STREAM_INFO"] = 24;
                        values[valuesById[26] = "PlatformStateService_I_PLATFORM_STATE"] = 26;
                        values[valuesById[27] = "RosterService_I_ROSTER_STATE"] = 27;
                        values[valuesById[38] = "HintsService_I_ACTIVE_HINTS_CONSTRUCTION"] = 38;
                        values[valuesById[39] = "HintsService_I_NEXT_HINTS_CONSTRUCTION"] = 39;
                        values[valuesById[41] = "EntityIdService_I_ENTITY_COUNTS"] = 41;
                        values[valuesById[42] = "HistoryService_I_LEDGER_ID"] = 42;
                        values[valuesById[44] = "HistoryService_I_ACTIVE_PROOF_CONSTRUCTION"] = 44;
                        values[valuesById[45] = "HistoryService_I_NEXT_PROOF_CONSTRUCTION"] = 45;
                        values[valuesById[48] = "HintsService_I_CRS_STATE"] = 48;
                        values[valuesById[50] = "TokenService_I_NODE_REWARDS"] = 50;
                        values[valuesById[55] = "TokenService_I_NODE_PAYMENTS"] = 55;
                        values[valuesById[56] = "AddressBookService_I_REGISTERED_NODES"] = 56;
                        values[valuesById[57] = "EntityIdService_I_HIGHEST_NODE_ID"] = 57;
                        values[valuesById[58] = "HistoryService_I_WRAPS_PROVING_KEY_HASH"] = 58;
                        values[valuesById[126] = "RecordCache_I_TRANSACTION_RECEIPTS"] = 126;
                        values[valuesById[10001] = "FileService_I_UPGRADE_DATA_150"] = 10001;
                        values[valuesById[10002] = "FileService_I_UPGRADE_DATA_151"] = 10002;
                        values[valuesById[10003] = "FileService_I_UPGRADE_DATA_152"] = 10003;
                        values[valuesById[10004] = "FileService_I_UPGRADE_DATA_153"] = 10004;
                        values[valuesById[10005] = "FileService_I_UPGRADE_DATA_154"] = 10005;
                        values[valuesById[10006] = "FileService_I_UPGRADE_DATA_155"] = 10006;
                        values[valuesById[10007] = "FileService_I_UPGRADE_DATA_156"] = 10007;
                        values[valuesById[10008] = "FileService_I_UPGRADE_DATA_157"] = 10008;
                        values[valuesById[10009] = "FileService_I_UPGRADE_DATA_158"] = 10009;
                        values[valuesById[10010] = "FileService_I_UPGRADE_DATA_159"] = 10010;
                        return values;
                    })();

                    return state;
                })();

                return platform;
            })();

            hapi.node = (function() {

                /**
                 * Namespace node.
                 * @memberof com.hedera.hapi
                 * @namespace
                 */
                const node = {};

                node.hooks = (function() {

                    /**
                     * Namespace hooks.
                     * @memberof com.hedera.hapi.node
                     * @namespace
                     */
                    const hooks = {};

                    /**
                     * The Hiero extension points that accept a hook.
                     * @name com.hedera.hapi.node.hooks.HookExtensionPoint
                     * @enum {number}
                     * @property {number} ACCOUNT_ALLOWANCE_HOOK=0 Used to customize an account's allowances during a CryptoTransfer transaction.
                     */
                    hooks.HookExtensionPoint = (function() {
                        const valuesById = $Object.create(null), values = $Object.create(valuesById);
                        values[valuesById[0] = "ACCOUNT_ALLOWANCE_HOOK"] = 0;
                        return values;
                    })();

                    hooks.HookCreation = (function() {

                        /**
                         * Properties of a HookCreation.
                         * @typedef {Object} com.hedera.hapi.node.hooks.HookCreation.$Properties
                         * @property {proto.HookEntityId.$Properties|null} [entityId] The id of the hook's entity.
                         * @property {com.hedera.hapi.node.hooks.HookCreationDetails.$Properties|null} [details] The creation details.
                         * @property {google.protobuf.Int64Value.$Properties|null} [nextHookId] If set, the id of the hook following this one in the owner's
                         * doubly-linked list of hooks.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a HookCreation.
                         * @memberof com.hedera.hapi.node.hooks
                         * @interface IHookCreation
                         * @augments com.hedera.hapi.node.hooks.HookCreation.$Properties
                         * @deprecated Use com.hedera.hapi.node.hooks.HookCreation.$Properties instead.
                         */

                        /**
                         * Shape of a HookCreation.
                         * @typedef {{
                         *   entityId?: proto.HookEntityId.$Shape|null;
                         *   details?: com.hedera.hapi.node.hooks.HookCreationDetails.$Shape|null;
                         *   nextHookId?: google.protobuf.Int64Value.$Shape|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * }} com.hedera.hapi.node.hooks.HookCreation.$Shape
                         */

                        /**
                         * Constructs a new HookCreation.
                         * @memberof com.hedera.hapi.node.hooks
                         * @classdesc Specifies the creation of a new hook at the given id for the given entity.
                         * @constructor
                         * @param {com.hedera.hapi.node.hooks.HookCreation.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const HookCreation = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The id of the hook's entity.
                         * @member {proto.HookEntityId.$Properties|null|undefined} entityId
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @instance
                         */
                        HookCreation.prototype.entityId = null;

                        /**
                         * The creation details.
                         * @member {com.hedera.hapi.node.hooks.HookCreationDetails.$Properties|null|undefined} details
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @instance
                         */
                        HookCreation.prototype.details = null;

                        /**
                         * If set, the id of the hook following this one in the owner's
                         * doubly-linked list of hooks.
                         * @member {google.protobuf.Int64Value.$Properties|null|undefined} nextHookId
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @instance
                         */
                        HookCreation.prototype.nextHookId = null;

                        /**
                         * Creates a new HookCreation instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookCreation.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.hooks.HookCreation} HookCreation instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.hooks.HookCreation.$Shape): com.hedera.hapi.node.hooks.HookCreation & com.hedera.hapi.node.hooks.HookCreation.$Shape;
                         *   (properties?: com.hedera.hapi.node.hooks.HookCreation.$Properties): com.hedera.hapi.node.hooks.HookCreation;
                         * }}
                         */
                        HookCreation.create = function(properties) {
                            return new HookCreation(properties);
                        };

                        /**
                         * Encodes the specified HookCreation message. Does not implicitly {@link com.hedera.hapi.node.hooks.HookCreation.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookCreation.$Properties} message HookCreation message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HookCreation.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.entityId != null && $Object.hasOwnProperty.call(message, "entityId"))
                                $root.proto.HookEntityId.encode(message.entityId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.details != null && $Object.hasOwnProperty.call(message, "details"))
                                $root.com.hedera.hapi.node.hooks.HookCreationDetails.encode(message.details, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                            if (message.nextHookId != null && $Object.hasOwnProperty.call(message, "nextHookId"))
                                $root.google.protobuf.Int64Value.encode(message.nextHookId, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified HookCreation message, length delimited. Does not implicitly {@link com.hedera.hapi.node.hooks.HookCreation.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookCreation.$Properties} message HookCreation message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HookCreation.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a HookCreation message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.hooks.HookCreation & com.hedera.hapi.node.hooks.HookCreation.$Shape} HookCreation
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HookCreation.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.hooks.HookCreation();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.entityId = $root.proto.HookEntityId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.entityId);
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        message.details = $root.com.hedera.hapi.node.hooks.HookCreationDetails.decode(reader, reader.uint32(), $undefined, _depth + 1, message.details);
                                        continue;
                                    }
                                case 9: {
                                        if (wireType !== 2)
                                            break;
                                        message.nextHookId = $root.google.protobuf.Int64Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nextHookId);
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a HookCreation message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.hooks.HookCreation & com.hedera.hapi.node.hooks.HookCreation.$Shape} HookCreation
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HookCreation.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a HookCreation message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        HookCreation.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.entityId != null && $Object.hasOwnProperty.call(message, "entityId")) {
                                let error = $root.proto.HookEntityId.verify(message.entityId, _depth + 1);
                                if (error)
                                    return "entityId." + error;
                            }
                            if (message.details != null && $Object.hasOwnProperty.call(message, "details")) {
                                let error = $root.com.hedera.hapi.node.hooks.HookCreationDetails.verify(message.details, _depth + 1);
                                if (error)
                                    return "details." + error;
                            }
                            if (message.nextHookId != null && $Object.hasOwnProperty.call(message, "nextHookId")) {
                                let error = $root.google.protobuf.Int64Value.verify(message.nextHookId, _depth + 1);
                                if (error)
                                    return "nextHookId." + error;
                            }
                            return null;
                        };

                        /**
                         * Creates a HookCreation message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.hooks.HookCreation} HookCreation
                         */
                        HookCreation.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.hooks.HookCreation)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.hooks.HookCreation: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.hooks.HookCreation();
                            if (object.entityId != null) {
                                if (!$util.isObject(object.entityId))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.HookCreation.entityId: object expected");
                                message.entityId = $root.proto.HookEntityId.fromObject(object.entityId, _depth + 1);
                            }
                            if (object.details != null) {
                                if (!$util.isObject(object.details))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.HookCreation.details: object expected");
                                message.details = $root.com.hedera.hapi.node.hooks.HookCreationDetails.fromObject(object.details, _depth + 1);
                            }
                            if (object.nextHookId != null) {
                                if (!$util.isObject(object.nextHookId))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.HookCreation.nextHookId: object expected");
                                message.nextHookId = $root.google.protobuf.Int64Value.fromObject(object.nextHookId, _depth + 1);
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a HookCreation message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookCreation} message HookCreation
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        HookCreation.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                object.entityId = null;
                                object.details = null;
                                object.nextHookId = null;
                            }
                            if (message.entityId != null && $Object.hasOwnProperty.call(message, "entityId"))
                                object.entityId = $root.proto.HookEntityId.toObject(message.entityId, options, _depth + 1);
                            if (message.details != null && $Object.hasOwnProperty.call(message, "details"))
                                object.details = $root.com.hedera.hapi.node.hooks.HookCreationDetails.toObject(message.details, options, _depth + 1);
                            if (message.nextHookId != null && $Object.hasOwnProperty.call(message, "nextHookId"))
                                object.nextHookId = $root.google.protobuf.Int64Value.toObject(message.nextHookId, options, _depth + 1);
                            return object;
                        };

                        /**
                         * Converts this HookCreation to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        HookCreation.prototype.toJSON = function() {
                            return HookCreation.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for HookCreation
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.hooks.HookCreation
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        HookCreation.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.hooks.HookCreation";
                        };

                        return HookCreation;
                    })();

                    hooks.HookCreationDetails = (function() {

                        /**
                         * Properties of a HookCreationDetails.
                         * @typedef {Object} com.hedera.hapi.node.hooks.HookCreationDetails.$Properties
                         * @property {com.hedera.hapi.node.hooks.HookExtensionPoint|null} [extensionPoint] The extension point for the hook.
                         * @property {number|Long|null} [hookId] The id to create the hook at.
                         * @property {com.hedera.hapi.node.hooks.EvmHook.$Properties|null} [evmHook] A general-purpose hook programmed in EVM bytecode that may access state
                         * or interact with external contracts.
                         * @property {proto.Key.$Properties|null} [adminKey] If set, a key that that can be used to remove or replace the hook; or (if
                         * applicable, as with an EVM hook) do transactions that customize the hook.
                         * @property {"evmHook"} [hook] The hook implementation.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a HookCreationDetails.
                         * @memberof com.hedera.hapi.node.hooks
                         * @interface IHookCreationDetails
                         * @augments com.hedera.hapi.node.hooks.HookCreationDetails.$Properties
                         * @deprecated Use com.hedera.hapi.node.hooks.HookCreationDetails.$Properties instead.
                         */

                        /**
                         * Narrowed shape of a HookCreationDetails.
                         * @typedef {{
                         *   extensionPoint?: com.hedera.hapi.node.hooks.HookExtensionPoint|null;
                         *   hookId?: number|Long|null;
                         *   evmHook?: com.hedera.hapi.node.hooks.EvmHook.$Shape|null;
                         *   adminKey?: proto.Key.$Shape|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * } & (
                         *   ({ hook?: undefined; evmHook?: null }|{ hook?: "evmHook"; evmHook: com.hedera.hapi.node.hooks.EvmHook.$Shape })
                         * )} com.hedera.hapi.node.hooks.HookCreationDetails.$Shape
                         */

                        /**
                         * Constructs a new HookCreationDetails.
                         * @memberof com.hedera.hapi.node.hooks
                         * @classdesc The details of a hook's creation.
                         * @constructor
                         * @param {com.hedera.hapi.node.hooks.HookCreationDetails.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const HookCreationDetails = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The extension point for the hook.
                         * @member {com.hedera.hapi.node.hooks.HookExtensionPoint} extensionPoint
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @instance
                         */
                        HookCreationDetails.prototype.extensionPoint = 0;

                        /**
                         * The id to create the hook at.
                         * @member {number|Long} hookId
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @instance
                         */
                        HookCreationDetails.prototype.hookId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

                        /**
                         * A general-purpose hook programmed in EVM bytecode that may access state
                         * or interact with external contracts.
                         * @member {com.hedera.hapi.node.hooks.EvmHook.$Properties|null|undefined} evmHook
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @instance
                         */
                        HookCreationDetails.prototype.evmHook = null;

                        /**
                         * If set, a key that that can be used to remove or replace the hook; or (if
                         * applicable, as with an EVM hook) do transactions that customize the hook.
                         * @member {proto.Key.$Properties|null|undefined} adminKey
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @instance
                         */
                        HookCreationDetails.prototype.adminKey = null;

                        // OneOf field names bound to virtual getters and setters
                        let $oneOfFields;

                        /**
                         * The hook implementation.
                         * @member {"evmHook"|undefined} hook
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @instance
                         */
                        $Object.defineProperty(HookCreationDetails.prototype, "hook", {
                            get: $util.oneOfGetter($oneOfFields = ["evmHook"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * Creates a new HookCreationDetails instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookCreationDetails.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.hooks.HookCreationDetails} HookCreationDetails instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.hooks.HookCreationDetails.$Shape): com.hedera.hapi.node.hooks.HookCreationDetails & com.hedera.hapi.node.hooks.HookCreationDetails.$Shape;
                         *   (properties?: com.hedera.hapi.node.hooks.HookCreationDetails.$Properties): com.hedera.hapi.node.hooks.HookCreationDetails;
                         * }}
                         */
                        HookCreationDetails.create = function(properties) {
                            return new HookCreationDetails(properties);
                        };

                        /**
                         * Encodes the specified HookCreationDetails message. Does not implicitly {@link com.hedera.hapi.node.hooks.HookCreationDetails.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookCreationDetails.$Properties} message HookCreationDetails message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HookCreationDetails.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.extensionPoint != null && $Object.hasOwnProperty.call(message, "extensionPoint") && message.extensionPoint !== 0)
                                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.extensionPoint);
                            if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId") && (typeof message.hookId === "object" ? message.hookId.low || message.hookId.high : message.hookId !== 0))
                                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.hookId);
                            if (message.evmHook != null && $Object.hasOwnProperty.call(message, "evmHook"))
                                $root.com.hedera.hapi.node.hooks.EvmHook.encode(message.evmHook, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified HookCreationDetails message, length delimited. Does not implicitly {@link com.hedera.hapi.node.hooks.HookCreationDetails.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookCreationDetails.$Properties} message HookCreationDetails message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HookCreationDetails.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a HookCreationDetails message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.hooks.HookCreationDetails & com.hedera.hapi.node.hooks.HookCreationDetails.$Shape} HookCreationDetails
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HookCreationDetails.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.hooks.HookCreationDetails();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (value = reader.int32())
                                            message.extensionPoint = value;
                                        else
                                            delete message.extensionPoint;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                            message.hookId = value;
                                        else
                                            delete message.hookId;
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        message.evmHook = $root.com.hedera.hapi.node.hooks.EvmHook.decode(reader, reader.uint32(), $undefined, _depth + 1, message.evmHook);
                                        message.hook = "evmHook";
                                        continue;
                                    }
                                case 4: {
                                        if (wireType !== 2)
                                            break;
                                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a HookCreationDetails message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.hooks.HookCreationDetails & com.hedera.hapi.node.hooks.HookCreationDetails.$Shape} HookCreationDetails
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HookCreationDetails.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a HookCreationDetails message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        HookCreationDetails.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            let properties = {};
                            if (message.extensionPoint != null && $Object.hasOwnProperty.call(message, "extensionPoint"))
                                if (typeof message.extensionPoint !== "number" || (message.extensionPoint | 0) !== message.extensionPoint)
                                    return "extensionPoint: enum value expected";
                            if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId"))
                                if (!$util.isInteger(message.hookId) && !(message.hookId && $util.isInteger(message.hookId.low) && $util.isInteger(message.hookId.high)))
                                    return "hookId: integer|Long expected";
                            if (message.evmHook != null && $Object.hasOwnProperty.call(message, "evmHook")) {
                                properties.hook = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.hooks.EvmHook.verify(message.evmHook, _depth + 1);
                                    if (error)
                                        return "evmHook." + error;
                                }
                            }
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                                if (error)
                                    return "adminKey." + error;
                            }
                            return null;
                        };

                        /**
                         * Creates a HookCreationDetails message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.hooks.HookCreationDetails} HookCreationDetails
                         */
                        HookCreationDetails.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.hooks.HookCreationDetails)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.hooks.HookCreationDetails: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.hooks.HookCreationDetails();
                            if (object.extensionPoint !== 0 && (typeof object.extensionPoint !== "string" || $root.com.hedera.hapi.node.hooks.HookExtensionPoint[object.extensionPoint] !== 0))
                                switch (object.extensionPoint) {
                                case "ACCOUNT_ALLOWANCE_HOOK":
                                case 0:
                                    message.extensionPoint = 0;
                                    break;
                                default:
                                    if (typeof object.extensionPoint === "number" && (object.extensionPoint | 0) === object.extensionPoint)
                                        message.extensionPoint = object.extensionPoint;
                                }
                            if (object.hookId != null)
                                if (typeof object.hookId === "object" ? object.hookId.low || object.hookId.high : $Number(object.hookId) !== 0)
                                    if ($util.Long)
                                        message.hookId = $util.Long.fromValue(object.hookId, false);
                                    else if (typeof object.hookId === "string")
                                        message.hookId = $parseInt(object.hookId, 10);
                                    else if (typeof object.hookId === "number")
                                        message.hookId = object.hookId;
                                    else if (typeof object.hookId === "object")
                                        message.hookId = new $util.LongBits(object.hookId.low >>> 0, object.hookId.high >>> 0).toNumber();
                            if (object.evmHook != null) {
                                if (!$util.isObject(object.evmHook))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.HookCreationDetails.evmHook: object expected");
                                message.evmHook = $root.com.hedera.hapi.node.hooks.EvmHook.fromObject(object.evmHook, _depth + 1);
                            }
                            if (object.adminKey != null) {
                                if (!$util.isObject(object.adminKey))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.HookCreationDetails.adminKey: object expected");
                                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a HookCreationDetails message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookCreationDetails} message HookCreationDetails
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        HookCreationDetails.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                object.extensionPoint = options.enums === $String ? "ACCOUNT_ALLOWANCE_HOOK" : 0;
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, false);
                                    object.hookId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.hookId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                object.adminKey = null;
                            }
                            if (message.extensionPoint != null && $Object.hasOwnProperty.call(message, "extensionPoint"))
                                object.extensionPoint = options.enums === $String ? $root.com.hedera.hapi.node.hooks.HookExtensionPoint[message.extensionPoint] === $undefined ? message.extensionPoint : $root.com.hedera.hapi.node.hooks.HookExtensionPoint[message.extensionPoint] : message.extensionPoint;
                            if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.hookId = typeof message.hookId === "number" ? $BigInt(message.hookId) : $util.Long.fromBits(message.hookId.low >>> 0, message.hookId.high >>> 0, false).toBigInt();
                                else if (typeof message.hookId === "number")
                                    object.hookId = options.longs === $String ? $String(message.hookId) : message.hookId;
                                else
                                    object.hookId = options.longs === $String ? $util.Long.prototype.toString.call(message.hookId) : options.longs === $Number ? new $util.LongBits(message.hookId.low >>> 0, message.hookId.high >>> 0).toNumber() : message.hookId;
                            if (message.evmHook != null && $Object.hasOwnProperty.call(message, "evmHook")) {
                                object.evmHook = $root.com.hedera.hapi.node.hooks.EvmHook.toObject(message.evmHook, options, _depth + 1);
                                if (options.oneofs)
                                    object.hook = "evmHook";
                            }
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
                            return object;
                        };

                        /**
                         * Converts this HookCreationDetails to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        HookCreationDetails.prototype.toJSON = function() {
                            return HookCreationDetails.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for HookCreationDetails
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.hooks.HookCreationDetails
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        HookCreationDetails.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.hooks.HookCreationDetails";
                        };

                        return HookCreationDetails;
                    })();

                    hooks.EvmHook = (function() {

                        /**
                         * Properties of an EvmHook.
                         * @typedef {Object} com.hedera.hapi.node.hooks.EvmHook.$Properties
                         * @property {com.hedera.hapi.node.hooks.EvmHookSpec.$Properties|null} [spec] The specification for the hook.
                         * @property {Array.<com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Properties>|null} [storageUpdates] Initial storage updates for the hook, if any.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of an EvmHook.
                         * @memberof com.hedera.hapi.node.hooks
                         * @interface IEvmHook
                         * @augments com.hedera.hapi.node.hooks.EvmHook.$Properties
                         * @deprecated Use com.hedera.hapi.node.hooks.EvmHook.$Properties instead.
                         */

                        /**
                         * Shape of an EvmHook.
                         * @typedef {{
                         *   spec?: com.hedera.hapi.node.hooks.EvmHookSpec.$Shape|null;
                         *   storageUpdates?: Array.<com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Shape>|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * }} com.hedera.hapi.node.hooks.EvmHook.$Shape
                         */

                        /**
                         * Constructs a new EvmHook.
                         * @memberof com.hedera.hapi.node.hooks
                         * @classdesc Definition of a general-purpose EVM hook.
                         * @constructor
                         * @param {com.hedera.hapi.node.hooks.EvmHook.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const EvmHook = function (properties) {
                            this.storageUpdates = [];
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The specification for the hook.
                         * @member {com.hedera.hapi.node.hooks.EvmHookSpec.$Properties|null|undefined} spec
                         * @memberof com.hedera.hapi.node.hooks.EvmHook
                         * @instance
                         */
                        EvmHook.prototype.spec = null;

                        /**
                         * Initial storage updates for the hook, if any.
                         * @member {Array.<com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Properties>} storageUpdates
                         * @memberof com.hedera.hapi.node.hooks.EvmHook
                         * @instance
                         */
                        EvmHook.prototype.storageUpdates = $util.emptyArray;

                        /**
                         * Creates a new EvmHook instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.hooks.EvmHook
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHook.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.hooks.EvmHook} EvmHook instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.hooks.EvmHook.$Shape): com.hedera.hapi.node.hooks.EvmHook & com.hedera.hapi.node.hooks.EvmHook.$Shape;
                         *   (properties?: com.hedera.hapi.node.hooks.EvmHook.$Properties): com.hedera.hapi.node.hooks.EvmHook;
                         * }}
                         */
                        EvmHook.create = function(properties) {
                            return new EvmHook(properties);
                        };

                        /**
                         * Encodes the specified EvmHook message. Does not implicitly {@link com.hedera.hapi.node.hooks.EvmHook.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.hooks.EvmHook
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHook.$Properties} message EvmHook message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EvmHook.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.spec != null && $Object.hasOwnProperty.call(message, "spec"))
                                $root.com.hedera.hapi.node.hooks.EvmHookSpec.encode(message.spec, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.storageUpdates != null && message.storageUpdates.length)
                                for (let i = 0; i < message.storageUpdates.length; ++i)
                                    $root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate.encode(message.storageUpdates[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified EvmHook message, length delimited. Does not implicitly {@link com.hedera.hapi.node.hooks.EvmHook.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.EvmHook
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHook.$Properties} message EvmHook message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EvmHook.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes an EvmHook message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.hooks.EvmHook
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.hooks.EvmHook & com.hedera.hapi.node.hooks.EvmHook.$Shape} EvmHook
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EvmHook.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.hooks.EvmHook();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.spec = $root.com.hedera.hapi.node.hooks.EvmHookSpec.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spec);
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.storageUpdates && message.storageUpdates.length))
                                            message.storageUpdates = [];
                                        message.storageUpdates.push($root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes an EvmHook message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.EvmHook
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.hooks.EvmHook & com.hedera.hapi.node.hooks.EvmHook.$Shape} EvmHook
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EvmHook.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies an EvmHook message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.hooks.EvmHook
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        EvmHook.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.spec != null && $Object.hasOwnProperty.call(message, "spec")) {
                                let error = $root.com.hedera.hapi.node.hooks.EvmHookSpec.verify(message.spec, _depth + 1);
                                if (error)
                                    return "spec." + error;
                            }
                            if (message.storageUpdates != null && $Object.hasOwnProperty.call(message, "storageUpdates")) {
                                if (!$Array.isArray(message.storageUpdates))
                                    return "storageUpdates: array expected";
                                for (let i = 0; i < message.storageUpdates.length; ++i) {
                                    let error = $root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate.verify(message.storageUpdates[i], _depth + 1);
                                    if (error)
                                        return "storageUpdates." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates an EvmHook message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.hooks.EvmHook
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.hooks.EvmHook} EvmHook
                         */
                        EvmHook.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.hooks.EvmHook)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.hooks.EvmHook: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.hooks.EvmHook();
                            if (object.spec != null) {
                                if (!$util.isObject(object.spec))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.EvmHook.spec: object expected");
                                message.spec = $root.com.hedera.hapi.node.hooks.EvmHookSpec.fromObject(object.spec, _depth + 1);
                            }
                            if (object.storageUpdates) {
                                if (!$Array.isArray(object.storageUpdates))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.EvmHook.storageUpdates: array expected");
                                message.storageUpdates = $Array(object.storageUpdates.length);
                                for (let i = 0; i < object.storageUpdates.length; ++i) {
                                    if (!$util.isObject(object.storageUpdates[i]))
                                        throw $TypeError(".com.hedera.hapi.node.hooks.EvmHook.storageUpdates: object expected");
                                    message.storageUpdates[i] = $root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate.fromObject(object.storageUpdates[i], _depth + 1);
                                }
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from an EvmHook message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.hooks.EvmHook
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHook} message EvmHook
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        EvmHook.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.arrays || options.defaults)
                                object.storageUpdates = [];
                            if (options.defaults)
                                object.spec = null;
                            if (message.spec != null && $Object.hasOwnProperty.call(message, "spec"))
                                object.spec = $root.com.hedera.hapi.node.hooks.EvmHookSpec.toObject(message.spec, options, _depth + 1);
                            if (message.storageUpdates && message.storageUpdates.length) {
                                object.storageUpdates = $Array(message.storageUpdates.length);
                                for (let j = 0; j < message.storageUpdates.length; ++j)
                                    object.storageUpdates[j] = $root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate.toObject(message.storageUpdates[j], options, _depth + 1);
                            }
                            return object;
                        };

                        /**
                         * Converts this EvmHook to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.hooks.EvmHook
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        EvmHook.prototype.toJSON = function() {
                            return EvmHook.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for EvmHook
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.hooks.EvmHook
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        EvmHook.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.hooks.EvmHook";
                        };

                        return EvmHook;
                    })();

                    hooks.EvmHookSpec = (function() {

                        /**
                         * Properties of an EvmHookSpec.
                         * @typedef {Object} com.hedera.hapi.node.hooks.EvmHookSpec.$Properties
                         * @property {proto.ContractID.$Properties|null} [contractId] The id of a contract that implements the extension point API with EVM bytecode.
                         * @property {"contractId"} [bytecodeSource] The source of the EVM bytecode for the hook.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of an EvmHookSpec.
                         * @memberof com.hedera.hapi.node.hooks
                         * @interface IEvmHookSpec
                         * @augments com.hedera.hapi.node.hooks.EvmHookSpec.$Properties
                         * @deprecated Use com.hedera.hapi.node.hooks.EvmHookSpec.$Properties instead.
                         */

                        /**
                         * Narrowed shape of an EvmHookSpec.
                         * @typedef {{
                         *   contractId?: proto.ContractID.$Shape|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * } & (
                         *   ({ bytecodeSource?: undefined; contractId?: null }|{ bytecodeSource?: "contractId"; contractId: proto.ContractID.$Shape })
                         * )} com.hedera.hapi.node.hooks.EvmHookSpec.$Shape
                         */

                        /**
                         * Constructs a new EvmHookSpec.
                         * @memberof com.hedera.hapi.node.hooks
                         * @classdesc Shared specifications for an EVM hook. May be used for any extension point.
                         * @constructor
                         * @param {com.hedera.hapi.node.hooks.EvmHookSpec.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const EvmHookSpec = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The id of a contract that implements the extension point API with EVM bytecode.
                         * @member {proto.ContractID.$Properties|null|undefined} contractId
                         * @memberof com.hedera.hapi.node.hooks.EvmHookSpec
                         * @instance
                         */
                        EvmHookSpec.prototype.contractId = null;

                        // OneOf field names bound to virtual getters and setters
                        let $oneOfFields;

                        /**
                         * The source of the EVM bytecode for the hook.
                         * @member {"contractId"|undefined} bytecodeSource
                         * @memberof com.hedera.hapi.node.hooks.EvmHookSpec
                         * @instance
                         */
                        $Object.defineProperty(EvmHookSpec.prototype, "bytecodeSource", {
                            get: $util.oneOfGetter($oneOfFields = ["contractId"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * Creates a new EvmHookSpec instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.hooks.EvmHookSpec
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookSpec.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.hooks.EvmHookSpec} EvmHookSpec instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.hooks.EvmHookSpec.$Shape): com.hedera.hapi.node.hooks.EvmHookSpec & com.hedera.hapi.node.hooks.EvmHookSpec.$Shape;
                         *   (properties?: com.hedera.hapi.node.hooks.EvmHookSpec.$Properties): com.hedera.hapi.node.hooks.EvmHookSpec;
                         * }}
                         */
                        EvmHookSpec.create = function(properties) {
                            return new EvmHookSpec(properties);
                        };

                        /**
                         * Encodes the specified EvmHookSpec message. Does not implicitly {@link com.hedera.hapi.node.hooks.EvmHookSpec.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.hooks.EvmHookSpec
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookSpec.$Properties} message EvmHookSpec message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EvmHookSpec.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId"))
                                $root.proto.ContractID.encode(message.contractId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified EvmHookSpec message, length delimited. Does not implicitly {@link com.hedera.hapi.node.hooks.EvmHookSpec.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.EvmHookSpec
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookSpec.$Properties} message EvmHookSpec message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EvmHookSpec.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes an EvmHookSpec message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.hooks.EvmHookSpec
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.hooks.EvmHookSpec & com.hedera.hapi.node.hooks.EvmHookSpec.$Shape} EvmHookSpec
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EvmHookSpec.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.hooks.EvmHookSpec();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.contractId = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractId);
                                        message.bytecodeSource = "contractId";
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes an EvmHookSpec message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.EvmHookSpec
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.hooks.EvmHookSpec & com.hedera.hapi.node.hooks.EvmHookSpec.$Shape} EvmHookSpec
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EvmHookSpec.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies an EvmHookSpec message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.hooks.EvmHookSpec
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        EvmHookSpec.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            let properties = {};
                            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId")) {
                                properties.bytecodeSource = 1;
                                {
                                    let error = $root.proto.ContractID.verify(message.contractId, _depth + 1);
                                    if (error)
                                        return "contractId." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates an EvmHookSpec message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.hooks.EvmHookSpec
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.hooks.EvmHookSpec} EvmHookSpec
                         */
                        EvmHookSpec.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.hooks.EvmHookSpec)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.hooks.EvmHookSpec: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.hooks.EvmHookSpec();
                            if (object.contractId != null) {
                                if (!$util.isObject(object.contractId))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.EvmHookSpec.contractId: object expected");
                                message.contractId = $root.proto.ContractID.fromObject(object.contractId, _depth + 1);
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from an EvmHookSpec message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.hooks.EvmHookSpec
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookSpec} message EvmHookSpec
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        EvmHookSpec.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId")) {
                                object.contractId = $root.proto.ContractID.toObject(message.contractId, options, _depth + 1);
                                if (options.oneofs)
                                    object.bytecodeSource = "contractId";
                            }
                            return object;
                        };

                        /**
                         * Converts this EvmHookSpec to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.hooks.EvmHookSpec
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        EvmHookSpec.prototype.toJSON = function() {
                            return EvmHookSpec.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for EvmHookSpec
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.hooks.EvmHookSpec
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        EvmHookSpec.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.hooks.EvmHookSpec";
                        };

                        return EvmHookSpec;
                    })();

                    hooks.EvmHookStorageUpdate = (function() {

                        /**
                         * Properties of an EvmHookStorageUpdate.
                         * @typedef {Object} com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Properties
                         * @property {com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Properties|null} [storageSlot] An explicit storage slot update.
                         * @property {com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Properties|null} [mappingEntries] Implicit storage slot updates specified as Solidity mapping entries.
                         * @property {"storageSlot"|"mappingEntries"} [update] EvmHookStorageUpdate update
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of an EvmHookStorageUpdate.
                         * @memberof com.hedera.hapi.node.hooks
                         * @interface IEvmHookStorageUpdate
                         * @augments com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Properties
                         * @deprecated Use com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Properties instead.
                         */

                        /**
                         * Narrowed shape of an EvmHookStorageUpdate.
                         * @typedef {{
                         *   storageSlot?: com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Shape|null;
                         *   mappingEntries?: com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Shape|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * } & (
                         *   ({ update?: undefined; storageSlot?: null; mappingEntries?: null }|{ update?: "storageSlot"; storageSlot: com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Shape; mappingEntries?: null }|{ update?: "mappingEntries"; storageSlot?: null; mappingEntries: com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Shape })
                         * )} com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Shape
                         */

                        /**
                         * Constructs a new EvmHookStorageUpdate.
                         * @memberof com.hedera.hapi.node.hooks
                         * @classdesc Specifies a key/value pair in the storage of an EVM hook, either by the explicit storage
                         * slot contents; or by a combination of a Solidity mapping's slot key and the key into
                         * that mapping.
                         * @constructor
                         * @param {com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const EvmHookStorageUpdate = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * An explicit storage slot update.
                         * @member {com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Properties|null|undefined} storageSlot
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @instance
                         */
                        EvmHookStorageUpdate.prototype.storageSlot = null;

                        /**
                         * Implicit storage slot updates specified as Solidity mapping entries.
                         * @member {com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Properties|null|undefined} mappingEntries
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @instance
                         */
                        EvmHookStorageUpdate.prototype.mappingEntries = null;

                        // OneOf field names bound to virtual getters and setters
                        let $oneOfFields;

                        /**
                         * EvmHookStorageUpdate update.
                         * @member {"storageSlot"|"mappingEntries"|undefined} update
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @instance
                         */
                        $Object.defineProperty(EvmHookStorageUpdate.prototype, "update", {
                            get: $util.oneOfGetter($oneOfFields = ["storageSlot", "mappingEntries"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * Creates a new EvmHookStorageUpdate instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.hooks.EvmHookStorageUpdate} EvmHookStorageUpdate instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Shape): com.hedera.hapi.node.hooks.EvmHookStorageUpdate & com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Shape;
                         *   (properties?: com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Properties): com.hedera.hapi.node.hooks.EvmHookStorageUpdate;
                         * }}
                         */
                        EvmHookStorageUpdate.create = function(properties) {
                            return new EvmHookStorageUpdate(properties);
                        };

                        /**
                         * Encodes the specified EvmHookStorageUpdate message. Does not implicitly {@link com.hedera.hapi.node.hooks.EvmHookStorageUpdate.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Properties} message EvmHookStorageUpdate message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EvmHookStorageUpdate.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.storageSlot != null && $Object.hasOwnProperty.call(message, "storageSlot"))
                                $root.com.hedera.hapi.node.hooks.EvmHookStorageSlot.encode(message.storageSlot, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.mappingEntries != null && $Object.hasOwnProperty.call(message, "mappingEntries"))
                                $root.com.hedera.hapi.node.hooks.EvmHookMappingEntries.encode(message.mappingEntries, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified EvmHookStorageUpdate message, length delimited. Does not implicitly {@link com.hedera.hapi.node.hooks.EvmHookStorageUpdate.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Properties} message EvmHookStorageUpdate message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EvmHookStorageUpdate.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes an EvmHookStorageUpdate message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.hooks.EvmHookStorageUpdate & com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Shape} EvmHookStorageUpdate
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EvmHookStorageUpdate.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.storageSlot = $root.com.hedera.hapi.node.hooks.EvmHookStorageSlot.decode(reader, reader.uint32(), $undefined, _depth + 1, message.storageSlot);
                                        message.update = "storageSlot";
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        message.mappingEntries = $root.com.hedera.hapi.node.hooks.EvmHookMappingEntries.decode(reader, reader.uint32(), $undefined, _depth + 1, message.mappingEntries);
                                        message.update = "mappingEntries";
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes an EvmHookStorageUpdate message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.hooks.EvmHookStorageUpdate & com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Shape} EvmHookStorageUpdate
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EvmHookStorageUpdate.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies an EvmHookStorageUpdate message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        EvmHookStorageUpdate.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            let properties = {};
                            if (message.storageSlot != null && $Object.hasOwnProperty.call(message, "storageSlot")) {
                                properties.update = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.hooks.EvmHookStorageSlot.verify(message.storageSlot, _depth + 1);
                                    if (error)
                                        return "storageSlot." + error;
                                }
                            }
                            if (message.mappingEntries != null && $Object.hasOwnProperty.call(message, "mappingEntries")) {
                                if (properties.update === 1)
                                    return "update: multiple values";
                                properties.update = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.hooks.EvmHookMappingEntries.verify(message.mappingEntries, _depth + 1);
                                    if (error)
                                        return "mappingEntries." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates an EvmHookStorageUpdate message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.hooks.EvmHookStorageUpdate} EvmHookStorageUpdate
                         */
                        EvmHookStorageUpdate.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.hooks.EvmHookStorageUpdate: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate();
                            if (object.storageSlot != null) {
                                if (!$util.isObject(object.storageSlot))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.EvmHookStorageUpdate.storageSlot: object expected");
                                message.storageSlot = $root.com.hedera.hapi.node.hooks.EvmHookStorageSlot.fromObject(object.storageSlot, _depth + 1);
                            }
                            if (object.mappingEntries != null) {
                                if (!$util.isObject(object.mappingEntries))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.EvmHookStorageUpdate.mappingEntries: object expected");
                                message.mappingEntries = $root.com.hedera.hapi.node.hooks.EvmHookMappingEntries.fromObject(object.mappingEntries, _depth + 1);
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from an EvmHookStorageUpdate message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookStorageUpdate} message EvmHookStorageUpdate
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        EvmHookStorageUpdate.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (message.storageSlot != null && $Object.hasOwnProperty.call(message, "storageSlot")) {
                                object.storageSlot = $root.com.hedera.hapi.node.hooks.EvmHookStorageSlot.toObject(message.storageSlot, options, _depth + 1);
                                if (options.oneofs)
                                    object.update = "storageSlot";
                            }
                            if (message.mappingEntries != null && $Object.hasOwnProperty.call(message, "mappingEntries")) {
                                object.mappingEntries = $root.com.hedera.hapi.node.hooks.EvmHookMappingEntries.toObject(message.mappingEntries, options, _depth + 1);
                                if (options.oneofs)
                                    object.update = "mappingEntries";
                            }
                            return object;
                        };

                        /**
                         * Converts this EvmHookStorageUpdate to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        EvmHookStorageUpdate.prototype.toJSON = function() {
                            return EvmHookStorageUpdate.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for EvmHookStorageUpdate
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageUpdate
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        EvmHookStorageUpdate.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.hooks.EvmHookStorageUpdate";
                        };

                        return EvmHookStorageUpdate;
                    })();

                    hooks.EvmHookMappingEntries = (function() {

                        /**
                         * Properties of an EvmHookMappingEntries.
                         * @typedef {Object} com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Properties
                         * @property {Uint8Array|null} [mappingSlot] The slot that corresponds to the Solidity mapping itself. Must use a
                         * minimal byte representation (no leading zeros).
                         * @property {Array.<com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Properties>|null} [entries] The entries in the mapping at the given slot.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of an EvmHookMappingEntries.
                         * @memberof com.hedera.hapi.node.hooks
                         * @interface IEvmHookMappingEntries
                         * @augments com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Properties
                         * @deprecated Use com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Properties instead.
                         */

                        /**
                         * Shape of an EvmHookMappingEntries.
                         * @typedef {{
                         *   mappingSlot?: Uint8Array|null;
                         *   entries?: Array.<com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Shape>|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * }} com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Shape
                         */

                        /**
                         * Constructs a new EvmHookMappingEntries.
                         * @memberof com.hedera.hapi.node.hooks
                         * @classdesc Specifies storage slot updates via indirection into a Solidity mapping.
                         * <p>
                         * Concretely, if the Solidity mapping is itself at slot `mapping_slot`, then
                         * the * storage slot for key `key` in the mapping is defined by the relationship
                         * `key_storage_slot = keccak256(abi.encodePacked(mapping_slot, key))`.
                         * <p>
                         * This message lets a metaprotocol be specified in terms of changes to a
                         * Solidity mapping's entries. If only raw slots could be updated, then a block
                         * stream consumer following the metaprotocol would have to invert the Keccak256
                         * hash to determine which mapping entry was being updated, which is not possible.
                         * @constructor
                         * @param {com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const EvmHookMappingEntries = function (properties) {
                            this.entries = [];
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The slot that corresponds to the Solidity mapping itself. Must use a
                         * minimal byte representation (no leading zeros).
                         * @member {Uint8Array} mappingSlot
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntries
                         * @instance
                         */
                        EvmHookMappingEntries.prototype.mappingSlot = $util.newBuffer([]);

                        /**
                         * The entries in the mapping at the given slot.
                         * @member {Array.<com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Properties>} entries
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntries
                         * @instance
                         */
                        EvmHookMappingEntries.prototype.entries = $util.emptyArray;

                        /**
                         * Creates a new EvmHookMappingEntries instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntries
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.hooks.EvmHookMappingEntries} EvmHookMappingEntries instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Shape): com.hedera.hapi.node.hooks.EvmHookMappingEntries & com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Shape;
                         *   (properties?: com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Properties): com.hedera.hapi.node.hooks.EvmHookMappingEntries;
                         * }}
                         */
                        EvmHookMappingEntries.create = function(properties) {
                            return new EvmHookMappingEntries(properties);
                        };

                        /**
                         * Encodes the specified EvmHookMappingEntries message. Does not implicitly {@link com.hedera.hapi.node.hooks.EvmHookMappingEntries.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntries
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Properties} message EvmHookMappingEntries message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EvmHookMappingEntries.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.mappingSlot != null && $Object.hasOwnProperty.call(message, "mappingSlot") && message.mappingSlot.length)
                                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.mappingSlot);
                            if (message.entries != null && message.entries.length)
                                for (let i = 0; i < message.entries.length; ++i)
                                    $root.com.hedera.hapi.node.hooks.EvmHookMappingEntry.encode(message.entries[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified EvmHookMappingEntries message, length delimited. Does not implicitly {@link com.hedera.hapi.node.hooks.EvmHookMappingEntries.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntries
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Properties} message EvmHookMappingEntries message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EvmHookMappingEntries.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes an EvmHookMappingEntries message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntries
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.hooks.EvmHookMappingEntries & com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Shape} EvmHookMappingEntries
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EvmHookMappingEntries.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.hooks.EvmHookMappingEntries();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.mappingSlot = value;
                                        else
                                            delete message.mappingSlot;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.entries && message.entries.length))
                                            message.entries = [];
                                        message.entries.push($root.com.hedera.hapi.node.hooks.EvmHookMappingEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes an EvmHookMappingEntries message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntries
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.hooks.EvmHookMappingEntries & com.hedera.hapi.node.hooks.EvmHookMappingEntries.$Shape} EvmHookMappingEntries
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EvmHookMappingEntries.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies an EvmHookMappingEntries message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntries
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        EvmHookMappingEntries.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.mappingSlot != null && $Object.hasOwnProperty.call(message, "mappingSlot"))
                                if (!(message.mappingSlot && typeof message.mappingSlot.length === "number" || $util.isString(message.mappingSlot)))
                                    return "mappingSlot: buffer expected";
                            if (message.entries != null && $Object.hasOwnProperty.call(message, "entries")) {
                                if (!$Array.isArray(message.entries))
                                    return "entries: array expected";
                                for (let i = 0; i < message.entries.length; ++i) {
                                    let error = $root.com.hedera.hapi.node.hooks.EvmHookMappingEntry.verify(message.entries[i], _depth + 1);
                                    if (error)
                                        return "entries." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates an EvmHookMappingEntries message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntries
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.hooks.EvmHookMappingEntries} EvmHookMappingEntries
                         */
                        EvmHookMappingEntries.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.hooks.EvmHookMappingEntries)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.hooks.EvmHookMappingEntries: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.hooks.EvmHookMappingEntries();
                            if (object.mappingSlot != null)
                                if (object.mappingSlot.length)
                                    if (typeof object.mappingSlot === "string")
                                        $util.base64.decode(object.mappingSlot, message.mappingSlot = $util.newBuffer($util.base64.length(object.mappingSlot)), 0);
                                    else if (object.mappingSlot.length >= 0)
                                        message.mappingSlot = object.mappingSlot;
                            if (object.entries) {
                                if (!$Array.isArray(object.entries))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.EvmHookMappingEntries.entries: array expected");
                                message.entries = $Array(object.entries.length);
                                for (let i = 0; i < object.entries.length; ++i) {
                                    if (!$util.isObject(object.entries[i]))
                                        throw $TypeError(".com.hedera.hapi.node.hooks.EvmHookMappingEntries.entries: object expected");
                                    message.entries[i] = $root.com.hedera.hapi.node.hooks.EvmHookMappingEntry.fromObject(object.entries[i], _depth + 1);
                                }
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from an EvmHookMappingEntries message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntries
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookMappingEntries} message EvmHookMappingEntries
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        EvmHookMappingEntries.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.arrays || options.defaults)
                                object.entries = [];
                            if (options.defaults)
                                if (options.bytes === $String)
                                    object.mappingSlot = "";
                                else {
                                    object.mappingSlot = [];
                                    if (options.bytes !== $Array)
                                        object.mappingSlot = $util.newBuffer(object.mappingSlot);
                                }
                            if (message.mappingSlot != null && $Object.hasOwnProperty.call(message, "mappingSlot"))
                                object.mappingSlot = options.bytes === $String ? $util.base64.encode(message.mappingSlot, 0, message.mappingSlot.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.mappingSlot) : message.mappingSlot;
                            if (message.entries && message.entries.length) {
                                object.entries = $Array(message.entries.length);
                                for (let j = 0; j < message.entries.length; ++j)
                                    object.entries[j] = $root.com.hedera.hapi.node.hooks.EvmHookMappingEntry.toObject(message.entries[j], options, _depth + 1);
                            }
                            return object;
                        };

                        /**
                         * Converts this EvmHookMappingEntries to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntries
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        EvmHookMappingEntries.prototype.toJSON = function() {
                            return EvmHookMappingEntries.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for EvmHookMappingEntries
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntries
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        EvmHookMappingEntries.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.hooks.EvmHookMappingEntries";
                        };

                        return EvmHookMappingEntries;
                    })();

                    hooks.EvmHookMappingEntry = (function() {

                        /**
                         * Properties of an EvmHookMappingEntry.
                         * @typedef {Object} com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Properties
                         * @property {Uint8Array|null} [key] The explicit bytes of the mapping entry. Must use a minimal byte representation;
                         * may not exceed 32 bytes in length.
                         * @property {Uint8Array|null} [preimage] The bytes that are the preimage of the Keccak256 hash that forms the mapping key.
                         * May be longer or shorter than 32 bytes and may have leading zeros, since Solidity
                         * supports variable-length keys in mappings.
                         * @property {Uint8Array|null} [value] If the mapping entry is present and non-zero, its value. May not be longer than
                         * 32 bytes in length; must use a minimal byte representation (no leading zeros).
                         * Leaving this field empty in an update removes the entry from the mapping.
                         * @property {"key"|"preimage"} [entryKey] EvmHookMappingEntry entryKey
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of an EvmHookMappingEntry.
                         * @memberof com.hedera.hapi.node.hooks
                         * @interface IEvmHookMappingEntry
                         * @augments com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Properties
                         * @deprecated Use com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Properties instead.
                         */

                        /**
                         * Narrowed shape of an EvmHookMappingEntry.
                         * @typedef {{
                         *   key?: Uint8Array|null;
                         *   preimage?: Uint8Array|null;
                         *   value?: Uint8Array|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * } & (
                         *   ({ entryKey?: undefined; key?: null; preimage?: null }|{ entryKey?: "key"; key: Uint8Array; preimage?: null }|{ entryKey?: "preimage"; key?: null; preimage: Uint8Array })
                         * )} com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Shape
                         */

                        /**
                         * Constructs a new EvmHookMappingEntry.
                         * @memberof com.hedera.hapi.node.hooks
                         * @classdesc An entry in a Solidity mapping. Very helpful for protocols that apply
                         * `HookStore` to manage the entries of a hook contract's mapping instead
                         * its raw storage slots.
                         * <p>
                         * This is especially attractive when the mapping value itself fits in a single
                         * word; for more complicated value storage layouts it becomes necessary to
                         * combine the mapping update with additional `EvmHookStorageSlot` updates that
                         * specify the complete storage slots of the value type.
                         * @constructor
                         * @param {com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const EvmHookMappingEntry = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The explicit bytes of the mapping entry. Must use a minimal byte representation;
                         * may not exceed 32 bytes in length.
                         * @member {Uint8Array|null|undefined} key
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @instance
                         */
                        EvmHookMappingEntry.prototype.key = null;

                        /**
                         * The bytes that are the preimage of the Keccak256 hash that forms the mapping key.
                         * May be longer or shorter than 32 bytes and may have leading zeros, since Solidity
                         * supports variable-length keys in mappings.
                         * @member {Uint8Array|null|undefined} preimage
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @instance
                         */
                        EvmHookMappingEntry.prototype.preimage = null;

                        /**
                         * If the mapping entry is present and non-zero, its value. May not be longer than
                         * 32 bytes in length; must use a minimal byte representation (no leading zeros).
                         * Leaving this field empty in an update removes the entry from the mapping.
                         * @member {Uint8Array} value
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @instance
                         */
                        EvmHookMappingEntry.prototype.value = $util.newBuffer([]);

                        // OneOf field names bound to virtual getters and setters
                        let $oneOfFields;

                        /**
                         * EvmHookMappingEntry entryKey.
                         * @member {"key"|"preimage"|undefined} entryKey
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @instance
                         */
                        $Object.defineProperty(EvmHookMappingEntry.prototype, "entryKey", {
                            get: $util.oneOfGetter($oneOfFields = ["key", "preimage"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * Creates a new EvmHookMappingEntry instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.hooks.EvmHookMappingEntry} EvmHookMappingEntry instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Shape): com.hedera.hapi.node.hooks.EvmHookMappingEntry & com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Shape;
                         *   (properties?: com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Properties): com.hedera.hapi.node.hooks.EvmHookMappingEntry;
                         * }}
                         */
                        EvmHookMappingEntry.create = function(properties) {
                            return new EvmHookMappingEntry(properties);
                        };

                        /**
                         * Encodes the specified EvmHookMappingEntry message. Does not implicitly {@link com.hedera.hapi.node.hooks.EvmHookMappingEntry.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Properties} message EvmHookMappingEntry message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EvmHookMappingEntry.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.key);
                            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage"))
                                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.preimage);
                            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value.length)
                                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.value);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified EvmHookMappingEntry message, length delimited. Does not implicitly {@link com.hedera.hapi.node.hooks.EvmHookMappingEntry.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Properties} message EvmHookMappingEntry message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EvmHookMappingEntry.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes an EvmHookMappingEntry message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.hooks.EvmHookMappingEntry & com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Shape} EvmHookMappingEntry
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EvmHookMappingEntry.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.hooks.EvmHookMappingEntry();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.key = reader.bytes();
                                        message.entryKey = "key";
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        message.preimage = reader.bytes();
                                        message.entryKey = "preimage";
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.value = value;
                                        else
                                            delete message.value;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes an EvmHookMappingEntry message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.hooks.EvmHookMappingEntry & com.hedera.hapi.node.hooks.EvmHookMappingEntry.$Shape} EvmHookMappingEntry
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EvmHookMappingEntry.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies an EvmHookMappingEntry message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        EvmHookMappingEntry.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            let properties = {};
                            if (message.key != null && $Object.hasOwnProperty.call(message, "key")) {
                                properties.entryKey = 1;
                                if (!(message.key && typeof message.key.length === "number" || $util.isString(message.key)))
                                    return "key: buffer expected";
                            }
                            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage")) {
                                if (properties.entryKey === 1)
                                    return "entryKey: multiple values";
                                properties.entryKey = 1;
                                if (!(message.preimage && typeof message.preimage.length === "number" || $util.isString(message.preimage)))
                                    return "preimage: buffer expected";
                            }
                            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                                if (!(message.value && typeof message.value.length === "number" || $util.isString(message.value)))
                                    return "value: buffer expected";
                            return null;
                        };

                        /**
                         * Creates an EvmHookMappingEntry message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.hooks.EvmHookMappingEntry} EvmHookMappingEntry
                         */
                        EvmHookMappingEntry.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.hooks.EvmHookMappingEntry)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.hooks.EvmHookMappingEntry: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.hooks.EvmHookMappingEntry();
                            if (object.key != null)
                                if (typeof object.key === "string")
                                    $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                                else if (object.key.length >= 0)
                                    message.key = object.key;
                            if (object.preimage != null)
                                if (typeof object.preimage === "string")
                                    $util.base64.decode(object.preimage, message.preimage = $util.newBuffer($util.base64.length(object.preimage)), 0);
                                else if (object.preimage.length >= 0)
                                    message.preimage = object.preimage;
                            if (object.value != null)
                                if (object.value.length)
                                    if (typeof object.value === "string")
                                        $util.base64.decode(object.value, message.value = $util.newBuffer($util.base64.length(object.value)), 0);
                                    else if (object.value.length >= 0)
                                        message.value = object.value;
                            return message;
                        };

                        /**
                         * Creates a plain object from an EvmHookMappingEntry message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookMappingEntry} message EvmHookMappingEntry
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        EvmHookMappingEntry.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults)
                                if (options.bytes === $String)
                                    object.value = "";
                                else {
                                    object.value = [];
                                    if (options.bytes !== $Array)
                                        object.value = $util.newBuffer(object.value);
                                }
                            if (message.key != null && $Object.hasOwnProperty.call(message, "key")) {
                                object.key = options.bytes === $String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.key) : message.key;
                                if (options.oneofs)
                                    object.entryKey = "key";
                            }
                            if (message.preimage != null && $Object.hasOwnProperty.call(message, "preimage")) {
                                object.preimage = options.bytes === $String ? $util.base64.encode(message.preimage, 0, message.preimage.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.preimage) : message.preimage;
                                if (options.oneofs)
                                    object.entryKey = "preimage";
                            }
                            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                                object.value = options.bytes === $String ? $util.base64.encode(message.value, 0, message.value.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.value) : message.value;
                            return object;
                        };

                        /**
                         * Converts this EvmHookMappingEntry to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        EvmHookMappingEntry.prototype.toJSON = function() {
                            return EvmHookMappingEntry.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for EvmHookMappingEntry
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.hooks.EvmHookMappingEntry
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        EvmHookMappingEntry.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.hooks.EvmHookMappingEntry";
                        };

                        return EvmHookMappingEntry;
                    })();

                    hooks.EvmHookStorageSlot = (function() {

                        /**
                         * Properties of an EvmHookStorageSlot.
                         * @typedef {Object} com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Properties
                         * @property {Uint8Array|null} [key] The key of the slot. Must use a minimal byte representation (no
                         * leading zeros); may not exceed 32 bytes in length.
                         * @property {Uint8Array|null} [value] If the slot is present and non-zero, its value. Must use a minimal
                         * byte representation (no leading zeros); may not exceed 32 bytes in
                         * length Leaving this field empty in an update removes the slot from
                         * storage.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of an EvmHookStorageSlot.
                         * @memberof com.hedera.hapi.node.hooks
                         * @interface IEvmHookStorageSlot
                         * @augments com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Properties
                         * @deprecated Use com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Properties instead.
                         */

                        /**
                         * Shape of an EvmHookStorageSlot.
                         * @typedef {com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Properties} com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Shape
                         */

                        /**
                         * Constructs a new EvmHookStorageSlot.
                         * @memberof com.hedera.hapi.node.hooks
                         * @classdesc A slot in the storage of an EVM hook.
                         * @constructor
                         * @param {com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const EvmHookStorageSlot = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The key of the slot. Must use a minimal byte representation (no
                         * leading zeros); may not exceed 32 bytes in length.
                         * @member {Uint8Array} key
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageSlot
                         * @instance
                         */
                        EvmHookStorageSlot.prototype.key = $util.newBuffer([]);

                        /**
                         * If the slot is present and non-zero, its value. Must use a minimal
                         * byte representation (no leading zeros); may not exceed 32 bytes in
                         * length Leaving this field empty in an update removes the slot from
                         * storage.
                         * @member {Uint8Array} value
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageSlot
                         * @instance
                         */
                        EvmHookStorageSlot.prototype.value = $util.newBuffer([]);

                        /**
                         * Creates a new EvmHookStorageSlot instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageSlot
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.hooks.EvmHookStorageSlot} EvmHookStorageSlot instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Shape): com.hedera.hapi.node.hooks.EvmHookStorageSlot & com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Shape;
                         *   (properties?: com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Properties): com.hedera.hapi.node.hooks.EvmHookStorageSlot;
                         * }}
                         */
                        EvmHookStorageSlot.create = function(properties) {
                            return new EvmHookStorageSlot(properties);
                        };

                        /**
                         * Encodes the specified EvmHookStorageSlot message. Does not implicitly {@link com.hedera.hapi.node.hooks.EvmHookStorageSlot.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageSlot
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Properties} message EvmHookStorageSlot message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EvmHookStorageSlot.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key.length)
                                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.key);
                            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value.length)
                                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.value);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified EvmHookStorageSlot message, length delimited. Does not implicitly {@link com.hedera.hapi.node.hooks.EvmHookStorageSlot.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageSlot
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Properties} message EvmHookStorageSlot message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        EvmHookStorageSlot.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes an EvmHookStorageSlot message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageSlot
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.hooks.EvmHookStorageSlot & com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Shape} EvmHookStorageSlot
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EvmHookStorageSlot.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.hooks.EvmHookStorageSlot();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.key = value;
                                        else
                                            delete message.key;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.value = value;
                                        else
                                            delete message.value;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes an EvmHookStorageSlot message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageSlot
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.hooks.EvmHookStorageSlot & com.hedera.hapi.node.hooks.EvmHookStorageSlot.$Shape} EvmHookStorageSlot
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        EvmHookStorageSlot.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies an EvmHookStorageSlot message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageSlot
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        EvmHookStorageSlot.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                if (!(message.key && typeof message.key.length === "number" || $util.isString(message.key)))
                                    return "key: buffer expected";
                            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                                if (!(message.value && typeof message.value.length === "number" || $util.isString(message.value)))
                                    return "value: buffer expected";
                            return null;
                        };

                        /**
                         * Creates an EvmHookStorageSlot message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageSlot
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.hooks.EvmHookStorageSlot} EvmHookStorageSlot
                         */
                        EvmHookStorageSlot.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.hooks.EvmHookStorageSlot)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.hooks.EvmHookStorageSlot: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.hooks.EvmHookStorageSlot();
                            if (object.key != null)
                                if (object.key.length)
                                    if (typeof object.key === "string")
                                        $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                                    else if (object.key.length >= 0)
                                        message.key = object.key;
                            if (object.value != null)
                                if (object.value.length)
                                    if (typeof object.value === "string")
                                        $util.base64.decode(object.value, message.value = $util.newBuffer($util.base64.length(object.value)), 0);
                                    else if (object.value.length >= 0)
                                        message.value = object.value;
                            return message;
                        };

                        /**
                         * Creates a plain object from an EvmHookStorageSlot message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageSlot
                         * @static
                         * @param {com.hedera.hapi.node.hooks.EvmHookStorageSlot} message EvmHookStorageSlot
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        EvmHookStorageSlot.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                if (options.bytes === $String)
                                    object.key = "";
                                else {
                                    object.key = [];
                                    if (options.bytes !== $Array)
                                        object.key = $util.newBuffer(object.key);
                                }
                                if (options.bytes === $String)
                                    object.value = "";
                                else {
                                    object.value = [];
                                    if (options.bytes !== $Array)
                                        object.value = $util.newBuffer(object.value);
                                }
                            }
                            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                object.key = options.bytes === $String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.key) : message.key;
                            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                                object.value = options.bytes === $String ? $util.base64.encode(message.value, 0, message.value.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.value) : message.value;
                            return object;
                        };

                        /**
                         * Converts this EvmHookStorageSlot to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageSlot
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        EvmHookStorageSlot.prototype.toJSON = function() {
                            return EvmHookStorageSlot.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for EvmHookStorageSlot
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.hooks.EvmHookStorageSlot
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        EvmHookStorageSlot.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.hooks.EvmHookStorageSlot";
                        };

                        return EvmHookStorageSlot;
                    })();

                    hooks.HookStoreTransactionBody = (function() {

                        /**
                         * Properties of a HookStoreTransactionBody.
                         * @typedef {Object} com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Properties
                         * @property {proto.HookId.$Properties|null} [hookId] The id of the EVM hook whose storage is being updated.
                         * @property {Array.<com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Properties>|null} [storageUpdates] The updates to the storage of the EVM hook.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a HookStoreTransactionBody.
                         * @memberof com.hedera.hapi.node.hooks
                         * @interface IHookStoreTransactionBody
                         * @augments com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Properties
                         * @deprecated Use com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Properties instead.
                         */

                        /**
                         * Shape of a HookStoreTransactionBody.
                         * @typedef {{
                         *   hookId?: proto.HookId.$Shape|null;
                         *   storageUpdates?: Array.<com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Shape>|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * }} com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Shape
                         */

                        /**
                         * Constructs a new HookStoreTransactionBody.
                         * @memberof com.hedera.hapi.node.hooks
                         * @classdesc Adds or removes key/value pairs in the storage of an EVM hook.
                         * Either the hook owner's key, or its admin key, must sign the transaction.
                         * @constructor
                         * @param {com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const HookStoreTransactionBody = function (properties) {
                            this.storageUpdates = [];
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The id of the EVM hook whose storage is being updated.
                         * @member {proto.HookId.$Properties|null|undefined} hookId
                         * @memberof com.hedera.hapi.node.hooks.HookStoreTransactionBody
                         * @instance
                         */
                        HookStoreTransactionBody.prototype.hookId = null;

                        /**
                         * The updates to the storage of the EVM hook.
                         * @member {Array.<com.hedera.hapi.node.hooks.EvmHookStorageUpdate.$Properties>} storageUpdates
                         * @memberof com.hedera.hapi.node.hooks.HookStoreTransactionBody
                         * @instance
                         */
                        HookStoreTransactionBody.prototype.storageUpdates = $util.emptyArray;

                        /**
                         * Creates a new HookStoreTransactionBody instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.hooks.HookStoreTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.hooks.HookStoreTransactionBody} HookStoreTransactionBody instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Shape): com.hedera.hapi.node.hooks.HookStoreTransactionBody & com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Shape;
                         *   (properties?: com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Properties): com.hedera.hapi.node.hooks.HookStoreTransactionBody;
                         * }}
                         */
                        HookStoreTransactionBody.create = function(properties) {
                            return new HookStoreTransactionBody(properties);
                        };

                        /**
                         * Encodes the specified HookStoreTransactionBody message. Does not implicitly {@link com.hedera.hapi.node.hooks.HookStoreTransactionBody.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.hooks.HookStoreTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Properties} message HookStoreTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HookStoreTransactionBody.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId"))
                                $root.proto.HookId.encode(message.hookId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.storageUpdates != null && message.storageUpdates.length)
                                for (let i = 0; i < message.storageUpdates.length; ++i)
                                    $root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate.encode(message.storageUpdates[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified HookStoreTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.node.hooks.HookStoreTransactionBody.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.HookStoreTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Properties} message HookStoreTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HookStoreTransactionBody.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a HookStoreTransactionBody message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.hooks.HookStoreTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.hooks.HookStoreTransactionBody & com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Shape} HookStoreTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HookStoreTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.hooks.HookStoreTransactionBody();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.hookId = $root.proto.HookId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hookId);
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.storageUpdates && message.storageUpdates.length))
                                            message.storageUpdates = [];
                                        message.storageUpdates.push($root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a HookStoreTransactionBody message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.HookStoreTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.hooks.HookStoreTransactionBody & com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Shape} HookStoreTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HookStoreTransactionBody.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a HookStoreTransactionBody message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.hooks.HookStoreTransactionBody
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        HookStoreTransactionBody.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId")) {
                                let error = $root.proto.HookId.verify(message.hookId, _depth + 1);
                                if (error)
                                    return "hookId." + error;
                            }
                            if (message.storageUpdates != null && $Object.hasOwnProperty.call(message, "storageUpdates")) {
                                if (!$Array.isArray(message.storageUpdates))
                                    return "storageUpdates: array expected";
                                for (let i = 0; i < message.storageUpdates.length; ++i) {
                                    let error = $root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate.verify(message.storageUpdates[i], _depth + 1);
                                    if (error)
                                        return "storageUpdates." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates a HookStoreTransactionBody message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.hooks.HookStoreTransactionBody
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.hooks.HookStoreTransactionBody} HookStoreTransactionBody
                         */
                        HookStoreTransactionBody.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.hooks.HookStoreTransactionBody)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.hooks.HookStoreTransactionBody: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.hooks.HookStoreTransactionBody();
                            if (object.hookId != null) {
                                if (!$util.isObject(object.hookId))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.HookStoreTransactionBody.hookId: object expected");
                                message.hookId = $root.proto.HookId.fromObject(object.hookId, _depth + 1);
                            }
                            if (object.storageUpdates) {
                                if (!$Array.isArray(object.storageUpdates))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.HookStoreTransactionBody.storageUpdates: array expected");
                                message.storageUpdates = $Array(object.storageUpdates.length);
                                for (let i = 0; i < object.storageUpdates.length; ++i) {
                                    if (!$util.isObject(object.storageUpdates[i]))
                                        throw $TypeError(".com.hedera.hapi.node.hooks.HookStoreTransactionBody.storageUpdates: object expected");
                                    message.storageUpdates[i] = $root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate.fromObject(object.storageUpdates[i], _depth + 1);
                                }
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a HookStoreTransactionBody message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.hooks.HookStoreTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookStoreTransactionBody} message HookStoreTransactionBody
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        HookStoreTransactionBody.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.arrays || options.defaults)
                                object.storageUpdates = [];
                            if (options.defaults)
                                object.hookId = null;
                            if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId"))
                                object.hookId = $root.proto.HookId.toObject(message.hookId, options, _depth + 1);
                            if (message.storageUpdates && message.storageUpdates.length) {
                                object.storageUpdates = $Array(message.storageUpdates.length);
                                for (let j = 0; j < message.storageUpdates.length; ++j)
                                    object.storageUpdates[j] = $root.com.hedera.hapi.node.hooks.EvmHookStorageUpdate.toObject(message.storageUpdates[j], options, _depth + 1);
                            }
                            return object;
                        };

                        /**
                         * Converts this HookStoreTransactionBody to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.hooks.HookStoreTransactionBody
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        HookStoreTransactionBody.prototype.toJSON = function() {
                            return HookStoreTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for HookStoreTransactionBody
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.hooks.HookStoreTransactionBody
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        HookStoreTransactionBody.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.hooks.HookStoreTransactionBody";
                        };

                        return HookStoreTransactionBody;
                    })();

                    hooks.HookDispatchTransactionBody = (function() {

                        /**
                         * Properties of a HookDispatchTransactionBody.
                         * @typedef {Object} com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Properties
                         * @property {proto.HookId.$Properties|null} [hookIdToDelete] The id of the hook to delete.
                         * @property {com.hedera.hapi.node.hooks.HookCreation.$Properties|null} [creation] Creation details for a new hook.
                         * @property {com.hedera.hapi.node.hooks.HookExecution.$Properties|null} [execution] Execution details of an existing hook.
                         * @property {"hookIdToDelete"|"creation"|"execution"} [action] HookDispatchTransactionBody action
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a HookDispatchTransactionBody.
                         * @memberof com.hedera.hapi.node.hooks
                         * @interface IHookDispatchTransactionBody
                         * @augments com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Properties
                         * @deprecated Use com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Properties instead.
                         */

                        /**
                         * Narrowed shape of a HookDispatchTransactionBody.
                         * @typedef {{
                         *   hookIdToDelete?: proto.HookId.$Shape|null;
                         *   creation?: com.hedera.hapi.node.hooks.HookCreation.$Shape|null;
                         *   execution?: com.hedera.hapi.node.hooks.HookExecution.$Shape|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * } & (
                         *   ({ action?: undefined; hookIdToDelete?: null; creation?: null; execution?: null }|{ action?: "hookIdToDelete"; hookIdToDelete: proto.HookId.$Shape; creation?: null; execution?: null }|{ action?: "creation"; hookIdToDelete?: null; creation: com.hedera.hapi.node.hooks.HookCreation.$Shape; execution?: null }|{ action?: "execution"; hookIdToDelete?: null; creation?: null; execution: com.hedera.hapi.node.hooks.HookExecution.$Shape })
                         * )} com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Shape
                         */

                        /**
                         * Constructs a new HookDispatchTransactionBody.
                         * @memberof com.hedera.hapi.node.hooks
                         * @classdesc Dispatches a hook action to an appropriate service.
                         * @constructor
                         * @param {com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const HookDispatchTransactionBody = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The id of the hook to delete.
                         * @member {proto.HookId.$Properties|null|undefined} hookIdToDelete
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @instance
                         */
                        HookDispatchTransactionBody.prototype.hookIdToDelete = null;

                        /**
                         * Creation details for a new hook.
                         * @member {com.hedera.hapi.node.hooks.HookCreation.$Properties|null|undefined} creation
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @instance
                         */
                        HookDispatchTransactionBody.prototype.creation = null;

                        /**
                         * Execution details of an existing hook.
                         * @member {com.hedera.hapi.node.hooks.HookExecution.$Properties|null|undefined} execution
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @instance
                         */
                        HookDispatchTransactionBody.prototype.execution = null;

                        // OneOf field names bound to virtual getters and setters
                        let $oneOfFields;

                        /**
                         * HookDispatchTransactionBody action.
                         * @member {"hookIdToDelete"|"creation"|"execution"|undefined} action
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @instance
                         */
                        $Object.defineProperty(HookDispatchTransactionBody.prototype, "action", {
                            get: $util.oneOfGetter($oneOfFields = ["hookIdToDelete", "creation", "execution"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * Creates a new HookDispatchTransactionBody instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.hooks.HookDispatchTransactionBody} HookDispatchTransactionBody instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Shape): com.hedera.hapi.node.hooks.HookDispatchTransactionBody & com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Shape;
                         *   (properties?: com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Properties): com.hedera.hapi.node.hooks.HookDispatchTransactionBody;
                         * }}
                         */
                        HookDispatchTransactionBody.create = function(properties) {
                            return new HookDispatchTransactionBody(properties);
                        };

                        /**
                         * Encodes the specified HookDispatchTransactionBody message. Does not implicitly {@link com.hedera.hapi.node.hooks.HookDispatchTransactionBody.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Properties} message HookDispatchTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HookDispatchTransactionBody.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.hookIdToDelete != null && $Object.hasOwnProperty.call(message, "hookIdToDelete"))
                                $root.proto.HookId.encode(message.hookIdToDelete, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.creation != null && $Object.hasOwnProperty.call(message, "creation"))
                                $root.com.hedera.hapi.node.hooks.HookCreation.encode(message.creation, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                            if (message.execution != null && $Object.hasOwnProperty.call(message, "execution"))
                                $root.com.hedera.hapi.node.hooks.HookExecution.encode(message.execution, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified HookDispatchTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.node.hooks.HookDispatchTransactionBody.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Properties} message HookDispatchTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HookDispatchTransactionBody.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a HookDispatchTransactionBody message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.hooks.HookDispatchTransactionBody & com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Shape} HookDispatchTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HookDispatchTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.hooks.HookDispatchTransactionBody();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.hookIdToDelete = $root.proto.HookId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hookIdToDelete);
                                        message.action = "hookIdToDelete";
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        message.creation = $root.com.hedera.hapi.node.hooks.HookCreation.decode(reader, reader.uint32(), $undefined, _depth + 1, message.creation);
                                        message.action = "creation";
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        message.execution = $root.com.hedera.hapi.node.hooks.HookExecution.decode(reader, reader.uint32(), $undefined, _depth + 1, message.execution);
                                        message.action = "execution";
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a HookDispatchTransactionBody message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.hooks.HookDispatchTransactionBody & com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Shape} HookDispatchTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HookDispatchTransactionBody.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a HookDispatchTransactionBody message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        HookDispatchTransactionBody.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            let properties = {};
                            if (message.hookIdToDelete != null && $Object.hasOwnProperty.call(message, "hookIdToDelete")) {
                                properties.action = 1;
                                {
                                    let error = $root.proto.HookId.verify(message.hookIdToDelete, _depth + 1);
                                    if (error)
                                        return "hookIdToDelete." + error;
                                }
                            }
                            if (message.creation != null && $Object.hasOwnProperty.call(message, "creation")) {
                                if (properties.action === 1)
                                    return "action: multiple values";
                                properties.action = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.hooks.HookCreation.verify(message.creation, _depth + 1);
                                    if (error)
                                        return "creation." + error;
                                }
                            }
                            if (message.execution != null && $Object.hasOwnProperty.call(message, "execution")) {
                                if (properties.action === 1)
                                    return "action: multiple values";
                                properties.action = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.hooks.HookExecution.verify(message.execution, _depth + 1);
                                    if (error)
                                        return "execution." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates a HookDispatchTransactionBody message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.hooks.HookDispatchTransactionBody} HookDispatchTransactionBody
                         */
                        HookDispatchTransactionBody.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.hooks.HookDispatchTransactionBody)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.hooks.HookDispatchTransactionBody: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.hooks.HookDispatchTransactionBody();
                            if (object.hookIdToDelete != null) {
                                if (!$util.isObject(object.hookIdToDelete))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.HookDispatchTransactionBody.hookIdToDelete: object expected");
                                message.hookIdToDelete = $root.proto.HookId.fromObject(object.hookIdToDelete, _depth + 1);
                            }
                            if (object.creation != null) {
                                if (!$util.isObject(object.creation))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.HookDispatchTransactionBody.creation: object expected");
                                message.creation = $root.com.hedera.hapi.node.hooks.HookCreation.fromObject(object.creation, _depth + 1);
                            }
                            if (object.execution != null) {
                                if (!$util.isObject(object.execution))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.HookDispatchTransactionBody.execution: object expected");
                                message.execution = $root.com.hedera.hapi.node.hooks.HookExecution.fromObject(object.execution, _depth + 1);
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a HookDispatchTransactionBody message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookDispatchTransactionBody} message HookDispatchTransactionBody
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        HookDispatchTransactionBody.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (message.hookIdToDelete != null && $Object.hasOwnProperty.call(message, "hookIdToDelete")) {
                                object.hookIdToDelete = $root.proto.HookId.toObject(message.hookIdToDelete, options, _depth + 1);
                                if (options.oneofs)
                                    object.action = "hookIdToDelete";
                            }
                            if (message.creation != null && $Object.hasOwnProperty.call(message, "creation")) {
                                object.creation = $root.com.hedera.hapi.node.hooks.HookCreation.toObject(message.creation, options, _depth + 1);
                                if (options.oneofs)
                                    object.action = "creation";
                            }
                            if (message.execution != null && $Object.hasOwnProperty.call(message, "execution")) {
                                object.execution = $root.com.hedera.hapi.node.hooks.HookExecution.toObject(message.execution, options, _depth + 1);
                                if (options.oneofs)
                                    object.action = "execution";
                            }
                            return object;
                        };

                        /**
                         * Converts this HookDispatchTransactionBody to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        HookDispatchTransactionBody.prototype.toJSON = function() {
                            return HookDispatchTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for HookDispatchTransactionBody
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.hooks.HookDispatchTransactionBody
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        HookDispatchTransactionBody.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.hooks.HookDispatchTransactionBody";
                        };

                        return HookDispatchTransactionBody;
                    })();

                    hooks.HookExecution = (function() {

                        /**
                         * Properties of a HookExecution.
                         * @typedef {Object} com.hedera.hapi.node.hooks.HookExecution.$Properties
                         * @property {proto.HookEntityId.$Properties|null} [hookEntityId] The id of the hook's entity.
                         * @property {proto.HookCall.$Properties|null} [call] The details of the call, including which hook id to call.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a HookExecution.
                         * @memberof com.hedera.hapi.node.hooks
                         * @interface IHookExecution
                         * @augments com.hedera.hapi.node.hooks.HookExecution.$Properties
                         * @deprecated Use com.hedera.hapi.node.hooks.HookExecution.$Properties instead.
                         */

                        /**
                         * Shape of a HookExecution.
                         * @typedef {{
                         *   hookEntityId?: proto.HookEntityId.$Shape|null;
                         *   call?: proto.HookCall.$Shape|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * }} com.hedera.hapi.node.hooks.HookExecution.$Shape
                         */

                        /**
                         * Constructs a new HookExecution.
                         * @memberof com.hedera.hapi.node.hooks
                         * @classdesc Details the execution of a hook.
                         * @constructor
                         * @param {com.hedera.hapi.node.hooks.HookExecution.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const HookExecution = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The id of the hook's entity.
                         * @member {proto.HookEntityId.$Properties|null|undefined} hookEntityId
                         * @memberof com.hedera.hapi.node.hooks.HookExecution
                         * @instance
                         */
                        HookExecution.prototype.hookEntityId = null;

                        /**
                         * The details of the call, including which hook id to call.
                         * @member {proto.HookCall.$Properties|null|undefined} call
                         * @memberof com.hedera.hapi.node.hooks.HookExecution
                         * @instance
                         */
                        HookExecution.prototype.call = null;

                        /**
                         * Creates a new HookExecution instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.hooks.HookExecution
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookExecution.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.hooks.HookExecution} HookExecution instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.hooks.HookExecution.$Shape): com.hedera.hapi.node.hooks.HookExecution & com.hedera.hapi.node.hooks.HookExecution.$Shape;
                         *   (properties?: com.hedera.hapi.node.hooks.HookExecution.$Properties): com.hedera.hapi.node.hooks.HookExecution;
                         * }}
                         */
                        HookExecution.create = function(properties) {
                            return new HookExecution(properties);
                        };

                        /**
                         * Encodes the specified HookExecution message. Does not implicitly {@link com.hedera.hapi.node.hooks.HookExecution.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.hooks.HookExecution
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookExecution.$Properties} message HookExecution message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HookExecution.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.hookEntityId != null && $Object.hasOwnProperty.call(message, "hookEntityId"))
                                $root.proto.HookEntityId.encode(message.hookEntityId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.call != null && $Object.hasOwnProperty.call(message, "call"))
                                $root.proto.HookCall.encode(message.call, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified HookExecution message, length delimited. Does not implicitly {@link com.hedera.hapi.node.hooks.HookExecution.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.HookExecution
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookExecution.$Properties} message HookExecution message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HookExecution.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a HookExecution message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.hooks.HookExecution
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.hooks.HookExecution & com.hedera.hapi.node.hooks.HookExecution.$Shape} HookExecution
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HookExecution.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.hooks.HookExecution();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.hookEntityId = $root.proto.HookEntityId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hookEntityId);
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        message.call = $root.proto.HookCall.decode(reader, reader.uint32(), $undefined, _depth + 1, message.call);
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a HookExecution message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.hooks.HookExecution
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.hooks.HookExecution & com.hedera.hapi.node.hooks.HookExecution.$Shape} HookExecution
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HookExecution.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a HookExecution message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.hooks.HookExecution
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        HookExecution.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.hookEntityId != null && $Object.hasOwnProperty.call(message, "hookEntityId")) {
                                let error = $root.proto.HookEntityId.verify(message.hookEntityId, _depth + 1);
                                if (error)
                                    return "hookEntityId." + error;
                            }
                            if (message.call != null && $Object.hasOwnProperty.call(message, "call")) {
                                let error = $root.proto.HookCall.verify(message.call, _depth + 1);
                                if (error)
                                    return "call." + error;
                            }
                            return null;
                        };

                        /**
                         * Creates a HookExecution message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.hooks.HookExecution
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.hooks.HookExecution} HookExecution
                         */
                        HookExecution.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.hooks.HookExecution)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.hooks.HookExecution: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.hooks.HookExecution();
                            if (object.hookEntityId != null) {
                                if (!$util.isObject(object.hookEntityId))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.HookExecution.hookEntityId: object expected");
                                message.hookEntityId = $root.proto.HookEntityId.fromObject(object.hookEntityId, _depth + 1);
                            }
                            if (object.call != null) {
                                if (!$util.isObject(object.call))
                                    throw $TypeError(".com.hedera.hapi.node.hooks.HookExecution.call: object expected");
                                message.call = $root.proto.HookCall.fromObject(object.call, _depth + 1);
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a HookExecution message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.hooks.HookExecution
                         * @static
                         * @param {com.hedera.hapi.node.hooks.HookExecution} message HookExecution
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        HookExecution.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                object.hookEntityId = null;
                                object.call = null;
                            }
                            if (message.hookEntityId != null && $Object.hasOwnProperty.call(message, "hookEntityId"))
                                object.hookEntityId = $root.proto.HookEntityId.toObject(message.hookEntityId, options, _depth + 1);
                            if (message.call != null && $Object.hasOwnProperty.call(message, "call"))
                                object.call = $root.proto.HookCall.toObject(message.call, options, _depth + 1);
                            return object;
                        };

                        /**
                         * Converts this HookExecution to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.hooks.HookExecution
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        HookExecution.prototype.toJSON = function() {
                            return HookExecution.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for HookExecution
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.hooks.HookExecution
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        HookExecution.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.hooks.HookExecution";
                        };

                        return HookExecution;
                    })();

                    return hooks;
                })();

                node.addressbook = (function() {

                    /**
                     * Namespace addressbook.
                     * @memberof com.hedera.hapi.node
                     * @namespace
                     */
                    const addressbook = {};

                    addressbook.NodeCreateTransactionBody = (function() {

                        /**
                         * Properties of a NodeCreateTransactionBody.
                         * @typedef {Object} com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Properties
                         * @property {proto.AccountID.$Properties|null} [accountId] A Node account identifier.
                         * <p>
                         * This account identifier MUST be in the "account number" form.<br/>
                         * This account identifier MUST NOT use the alias field.<br/>
                         * If the identified account does not exist, this transaction SHALL fail.<br/>
                         * Each account_id MUST NOT be associated with more than one node.<br/>
                         * This field is REQUIRED.
                         * @property {string|null} [description] A short description of the node.
                         * <p>
                         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
                         * (default 100) bytes when encoded as UTF-8.<br/>
                         * This field is OPTIONAL.
                         * @property {Array.<proto.ServiceEndpoint.$Properties>|null} [gossipEndpoint] A list of service endpoints for gossip.
                         * <p>
                         * These endpoints SHALL represent the published endpoints to which other
                         * consensus nodes may _gossip_ transactions.<br/>
                         * These endpoints MUST specify a port.<br/>
                         * This list MUST NOT be empty.<br/>
                         * This list MUST NOT contain more than `10` entries.<br/>
                         * The first two entries in this list SHALL be the endpoints published to
                         * all consensus nodes.<br/>
                         * All other entries SHALL be reserved for future use.
                         * <p>
                         * Each network may have additional requirements for these endpoints.
                         * A client MUST check network-specific documentation for those
                         * details.<br/>
                         * If the network configuration value `gossipFqdnRestricted` is set, then
                         * all endpoints in this list MUST supply only IP address.<br/>
                         * If the network configuration value `gossipFqdnRestricted` is _not_ set,
                         * then endpoints in this list MAY supply either IP address or FQDN, but
                         * MUST NOT supply both values for the same endpoint.
                         * @property {Array.<proto.ServiceEndpoint.$Properties>|null} [serviceEndpoint] A list of service endpoints for gRPC calls.
                         * <p>
                         * These endpoints SHALL represent the published gRPC endpoints to which
                         * clients may submit transactions.<br/>
                         * These endpoints MUST specify a port.<br/>
                         * Endpoints in this list MAY supply either IP address or FQDN, but MUST
                         * NOT supply both values for the same endpoint.<br/>
                         * This list MUST NOT be empty.<br/>
                         * This list MUST NOT contain more than `8` entries.
                         * @property {Uint8Array|null} [gossipCaCertificate] A certificate used to sign gossip events.
                         * <p>
                         * This value MUST be a certificate of a type permitted for gossip
                         * signatures.<br/>
                         * This value MUST be the DER encoding of the certificate presented.<br/>
                         * This field is REQUIRED and MUST NOT be empty.
                         * @property {Uint8Array|null} [grpcCertificateHash] A hash of the node gRPC TLS certificate.
                         * <p>
                         * This value MAY be used to verify the certificate presented by the node
                         * during TLS negotiation for gRPC.<br/>
                         * This value MUST be a SHA-384 hash.<br/>
                         * The TLS certificate to be hashed MUST first be in PEM format and MUST be
                         * encoded with UTF-8 NFKD encoding to a stream of bytes provided to
                         * the hash algorithm.<br/>
                         * This field is OPTIONAL.
                         * @property {proto.Key.$Properties|null} [adminKey] An administrative key controlled by the node operator.
                         * <p>
                         * This key MUST sign this transaction.<br/>
                         * This key MUST sign each transaction to update this node.<br/>
                         * This field MUST contain a valid `Key` value.<br/>
                         * This field is REQUIRED and MUST NOT be set to an empty `KeyList`.
                         * @property {boolean|null} [declineReward] A boolean flag indicating whether the node operator declines to receive
                         * node rewards.
                         * <p>
                         * If this flag is set to `true`, the node operator declines to receive
                         * node rewards.<br/>
                         * @property {proto.ServiceEndpoint.$Properties|null} [grpcProxyEndpoint] A web proxy for gRPC from non-gRPC clients.
                         * <p>
                         * This endpoint SHALL be a Fully Qualified Domain Name (FQDN) using the HTTPS
                         * protocol, and SHALL support gRPC-Web for use by browser-based clients.<br/>
                         * This endpoint MUST be signed by a trusted certificate authority.<br/>
                         * This endpoint MUST use a valid port and SHALL be reachable over TLS.<br/>
                         * This field MAY be omitted if the node does not support gRPC-Web access.<br/>
                         * This field MUST be updated if the gRPC-Web endpoint changes.<br/>
                         * This field SHALL enable frontend clients to avoid hard-coded proxy endpoints.
                         * @property {Array.<number|Long>|null} [associatedRegisteredNode] A list of registered nodes operated by the same entity as this node.<br/>
                         * This value may contain a list of "registered nodes" (as described in
                         * HIP-1137) that are operated by the same entity that operates this
                         * consensus node.
                         * <p>
                         * This field is OPTIONAL and MAY be empty.<br/>
                         * This field MUST NOT contain more than twenty(20) entries.<br/>
                         * Every entry in this list MUST be a valid `registered_node_id` for a
                         * current registered node.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a NodeCreateTransactionBody.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @interface INodeCreateTransactionBody
                         * @augments com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Properties
                         * @deprecated Use com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Properties instead.
                         */

                        /**
                         * Shape of a NodeCreateTransactionBody.
                         * @typedef {{
                         *   accountId?: proto.AccountID.$Shape|null;
                         *   description?: string|null;
                         *   gossipEndpoint?: Array.<proto.ServiceEndpoint.$Shape>|null;
                         *   serviceEndpoint?: Array.<proto.ServiceEndpoint.$Shape>|null;
                         *   gossipCaCertificate?: Uint8Array|null;
                         *   grpcCertificateHash?: Uint8Array|null;
                         *   adminKey?: proto.Key.$Shape|null;
                         *   declineReward?: boolean|null;
                         *   grpcProxyEndpoint?: proto.ServiceEndpoint.$Shape|null;
                         *   associatedRegisteredNode?: Array.<number|Long>|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * }} com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Shape
                         */

                        /**
                         * Constructs a new NodeCreateTransactionBody.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @classdesc A transaction body to add a new consensus node to the network address book.
                         * 
                         * This transaction body SHALL be considered a "privileged transaction".
                         * 
                         * This message supports a transaction to create a new node in the network
                         * address book. The transaction, once complete, enables a new consensus node
                         * to join the network, and requires governing council authorization.
                         * 
                         * - A `NodeCreateTransactionBody` MUST be signed by the `Key` assigned to the
                         * `admin_key` field and one of those keys: treasure account (2) key,
                         * systemAdmin(50) key, or addressBookAdmin(55) key.
                         * - This transaction MUST also be signed by the `Key` on the account
                         * identified by `account_id`.
                         * - The newly created node information SHALL be added to the network address
                         * book information in the network state.
                         * - The new entry SHALL be created in "state" but SHALL NOT participate in
                         * network consensus and SHALL NOT be present in network "configuration"
                         * until the next "upgrade" transaction (as noted below).
                         * - All new address book entries SHALL be added to the active network
                         * configuration during the next `freeze` transaction with the field
                         * `freeze_type` set to `PREPARE_UPGRADE`.
                         * 
                         * ### Block Stream Effects
                         * Upon completion the newly assigned `node_id` SHALL be recorded in
                         * the transaction receipt.<br/>
                         * This value SHALL be the next available node identifier.<br/>
                         * Node identifiers SHALL NOT be reused.
                         * @constructor
                         * @param {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const NodeCreateTransactionBody = function (properties) {
                            this.gossipEndpoint = [];
                            this.serviceEndpoint = [];
                            this.associatedRegisteredNode = [];
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A Node account identifier.
                         * <p>
                         * This account identifier MUST be in the "account number" form.<br/>
                         * This account identifier MUST NOT use the alias field.<br/>
                         * If the identified account does not exist, this transaction SHALL fail.<br/>
                         * Each account_id MUST NOT be associated with more than one node.<br/>
                         * This field is REQUIRED.
                         * @member {proto.AccountID.$Properties|null|undefined} accountId
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @instance
                         */
                        NodeCreateTransactionBody.prototype.accountId = null;

                        /**
                         * A short description of the node.
                         * <p>
                         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
                         * (default 100) bytes when encoded as UTF-8.<br/>
                         * This field is OPTIONAL.
                         * @member {string} description
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @instance
                         */
                        NodeCreateTransactionBody.prototype.description = "";

                        /**
                         * A list of service endpoints for gossip.
                         * <p>
                         * These endpoints SHALL represent the published endpoints to which other
                         * consensus nodes may _gossip_ transactions.<br/>
                         * These endpoints MUST specify a port.<br/>
                         * This list MUST NOT be empty.<br/>
                         * This list MUST NOT contain more than `10` entries.<br/>
                         * The first two entries in this list SHALL be the endpoints published to
                         * all consensus nodes.<br/>
                         * All other entries SHALL be reserved for future use.
                         * <p>
                         * Each network may have additional requirements for these endpoints.
                         * A client MUST check network-specific documentation for those
                         * details.<br/>
                         * If the network configuration value `gossipFqdnRestricted` is set, then
                         * all endpoints in this list MUST supply only IP address.<br/>
                         * If the network configuration value `gossipFqdnRestricted` is _not_ set,
                         * then endpoints in this list MAY supply either IP address or FQDN, but
                         * MUST NOT supply both values for the same endpoint.
                         * @member {Array.<proto.ServiceEndpoint.$Properties>} gossipEndpoint
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @instance
                         */
                        NodeCreateTransactionBody.prototype.gossipEndpoint = $util.emptyArray;

                        /**
                         * A list of service endpoints for gRPC calls.
                         * <p>
                         * These endpoints SHALL represent the published gRPC endpoints to which
                         * clients may submit transactions.<br/>
                         * These endpoints MUST specify a port.<br/>
                         * Endpoints in this list MAY supply either IP address or FQDN, but MUST
                         * NOT supply both values for the same endpoint.<br/>
                         * This list MUST NOT be empty.<br/>
                         * This list MUST NOT contain more than `8` entries.
                         * @member {Array.<proto.ServiceEndpoint.$Properties>} serviceEndpoint
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @instance
                         */
                        NodeCreateTransactionBody.prototype.serviceEndpoint = $util.emptyArray;

                        /**
                         * A certificate used to sign gossip events.
                         * <p>
                         * This value MUST be a certificate of a type permitted for gossip
                         * signatures.<br/>
                         * This value MUST be the DER encoding of the certificate presented.<br/>
                         * This field is REQUIRED and MUST NOT be empty.
                         * @member {Uint8Array} gossipCaCertificate
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @instance
                         */
                        NodeCreateTransactionBody.prototype.gossipCaCertificate = $util.newBuffer([]);

                        /**
                         * A hash of the node gRPC TLS certificate.
                         * <p>
                         * This value MAY be used to verify the certificate presented by the node
                         * during TLS negotiation for gRPC.<br/>
                         * This value MUST be a SHA-384 hash.<br/>
                         * The TLS certificate to be hashed MUST first be in PEM format and MUST be
                         * encoded with UTF-8 NFKD encoding to a stream of bytes provided to
                         * the hash algorithm.<br/>
                         * This field is OPTIONAL.
                         * @member {Uint8Array} grpcCertificateHash
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @instance
                         */
                        NodeCreateTransactionBody.prototype.grpcCertificateHash = $util.newBuffer([]);

                        /**
                         * An administrative key controlled by the node operator.
                         * <p>
                         * This key MUST sign this transaction.<br/>
                         * This key MUST sign each transaction to update this node.<br/>
                         * This field MUST contain a valid `Key` value.<br/>
                         * This field is REQUIRED and MUST NOT be set to an empty `KeyList`.
                         * @member {proto.Key.$Properties|null|undefined} adminKey
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @instance
                         */
                        NodeCreateTransactionBody.prototype.adminKey = null;

                        /**
                         * A boolean flag indicating whether the node operator declines to receive
                         * node rewards.
                         * <p>
                         * If this flag is set to `true`, the node operator declines to receive
                         * node rewards.<br/>
                         * @member {boolean} declineReward
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @instance
                         */
                        NodeCreateTransactionBody.prototype.declineReward = false;

                        /**
                         * A web proxy for gRPC from non-gRPC clients.
                         * <p>
                         * This endpoint SHALL be a Fully Qualified Domain Name (FQDN) using the HTTPS
                         * protocol, and SHALL support gRPC-Web for use by browser-based clients.<br/>
                         * This endpoint MUST be signed by a trusted certificate authority.<br/>
                         * This endpoint MUST use a valid port and SHALL be reachable over TLS.<br/>
                         * This field MAY be omitted if the node does not support gRPC-Web access.<br/>
                         * This field MUST be updated if the gRPC-Web endpoint changes.<br/>
                         * This field SHALL enable frontend clients to avoid hard-coded proxy endpoints.
                         * @member {proto.ServiceEndpoint.$Properties|null|undefined} grpcProxyEndpoint
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @instance
                         */
                        NodeCreateTransactionBody.prototype.grpcProxyEndpoint = null;

                        /**
                         * A list of registered nodes operated by the same entity as this node.<br/>
                         * This value may contain a list of "registered nodes" (as described in
                         * HIP-1137) that are operated by the same entity that operates this
                         * consensus node.
                         * <p>
                         * This field is OPTIONAL and MAY be empty.<br/>
                         * This field MUST NOT contain more than twenty(20) entries.<br/>
                         * Every entry in this list MUST be a valid `registered_node_id` for a
                         * current registered node.
                         * @member {Array.<number|Long>} associatedRegisteredNode
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @instance
                         */
                        NodeCreateTransactionBody.prototype.associatedRegisteredNode = $util.emptyArray;

                        /**
                         * Creates a new NodeCreateTransactionBody instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody} NodeCreateTransactionBody instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Shape): com.hedera.hapi.node.addressbook.NodeCreateTransactionBody & com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Shape;
                         *   (properties?: com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Properties): com.hedera.hapi.node.addressbook.NodeCreateTransactionBody;
                         * }}
                         */
                        NodeCreateTransactionBody.create = function(properties) {
                            return new NodeCreateTransactionBody(properties);
                        };

                        /**
                         * Encodes the specified NodeCreateTransactionBody message. Does not implicitly {@link com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Properties} message NodeCreateTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        NodeCreateTransactionBody.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                                $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
                                writer.uint32(/* id 2, wireType 2 =*/18).string(message.description);
                            if (message.gossipEndpoint != null && message.gossipEndpoint.length)
                                for (let i = 0; i < message.gossipEndpoint.length; ++i)
                                    $root.proto.ServiceEndpoint.encode(message.gossipEndpoint[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.serviceEndpoint != null && message.serviceEndpoint.length)
                                for (let i = 0; i < message.serviceEndpoint.length; ++i)
                                    $root.proto.ServiceEndpoint.encode(message.serviceEndpoint[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                            if (message.gossipCaCertificate != null && $Object.hasOwnProperty.call(message, "gossipCaCertificate") && message.gossipCaCertificate.length)
                                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.gossipCaCertificate);
                            if (message.grpcCertificateHash != null && $Object.hasOwnProperty.call(message, "grpcCertificateHash") && message.grpcCertificateHash.length)
                                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.grpcCertificateHash);
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward") && message.declineReward !== false)
                                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.declineReward);
                            if (message.grpcProxyEndpoint != null && $Object.hasOwnProperty.call(message, "grpcProxyEndpoint"))
                                $root.proto.ServiceEndpoint.encode(message.grpcProxyEndpoint, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
                            if (message.associatedRegisteredNode != null && message.associatedRegisteredNode.length)
                                writer.uint32(/* id 10, wireType 2 =*/82).uint64s(message.associatedRegisteredNode);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified NodeCreateTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Properties} message NodeCreateTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        NodeCreateTransactionBody.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a NodeCreateTransactionBody message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody & com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Shape} NodeCreateTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        NodeCreateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.stringVerify()).length)
                                            message.description = value;
                                        else
                                            delete message.description;
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.gossipEndpoint && message.gossipEndpoint.length))
                                            message.gossipEndpoint = [];
                                        message.gossipEndpoint.push($root.proto.ServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                case 4: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.serviceEndpoint && message.serviceEndpoint.length))
                                            message.serviceEndpoint = [];
                                        message.serviceEndpoint.push($root.proto.ServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                case 5: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.gossipCaCertificate = value;
                                        else
                                            delete message.gossipCaCertificate;
                                        continue;
                                    }
                                case 6: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.grpcCertificateHash = value;
                                        else
                                            delete message.grpcCertificateHash;
                                        continue;
                                    }
                                case 7: {
                                        if (wireType !== 2)
                                            break;
                                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                                        continue;
                                    }
                                case 8: {
                                        if (wireType !== 0)
                                            break;
                                        if (value = reader.bool())
                                            message.declineReward = value;
                                        else
                                            delete message.declineReward;
                                        continue;
                                    }
                                case 9: {
                                        if (wireType !== 2)
                                            break;
                                        message.grpcProxyEndpoint = $root.proto.ServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.grpcProxyEndpoint);
                                        continue;
                                    }
                                case 10: {
                                        if (wireType === 2) {
                                            if (!(message.associatedRegisteredNode && message.associatedRegisteredNode.length))
                                                message.associatedRegisteredNode = [];
                                            reader.uint64s(message.associatedRegisteredNode);
                                            continue;
                                        }
                                        if (wireType !== 0)
                                            break;
                                        if (!(message.associatedRegisteredNode && message.associatedRegisteredNode.length))
                                            message.associatedRegisteredNode = [];
                                        message.associatedRegisteredNode.push(reader.uint64());
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a NodeCreateTransactionBody message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody & com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Shape} NodeCreateTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        NodeCreateTransactionBody.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a NodeCreateTransactionBody message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        NodeCreateTransactionBody.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                                let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                                if (error)
                                    return "accountId." + error;
                            }
                            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                if (!$util.isString(message.description))
                                    return "description: string expected";
                            if (message.gossipEndpoint != null && $Object.hasOwnProperty.call(message, "gossipEndpoint")) {
                                if (!$Array.isArray(message.gossipEndpoint))
                                    return "gossipEndpoint: array expected";
                                for (let i = 0; i < message.gossipEndpoint.length; ++i) {
                                    let error = $root.proto.ServiceEndpoint.verify(message.gossipEndpoint[i], _depth + 1);
                                    if (error)
                                        return "gossipEndpoint." + error;
                                }
                            }
                            if (message.serviceEndpoint != null && $Object.hasOwnProperty.call(message, "serviceEndpoint")) {
                                if (!$Array.isArray(message.serviceEndpoint))
                                    return "serviceEndpoint: array expected";
                                for (let i = 0; i < message.serviceEndpoint.length; ++i) {
                                    let error = $root.proto.ServiceEndpoint.verify(message.serviceEndpoint[i], _depth + 1);
                                    if (error)
                                        return "serviceEndpoint." + error;
                                }
                            }
                            if (message.gossipCaCertificate != null && $Object.hasOwnProperty.call(message, "gossipCaCertificate"))
                                if (!(message.gossipCaCertificate && typeof message.gossipCaCertificate.length === "number" || $util.isString(message.gossipCaCertificate)))
                                    return "gossipCaCertificate: buffer expected";
                            if (message.grpcCertificateHash != null && $Object.hasOwnProperty.call(message, "grpcCertificateHash"))
                                if (!(message.grpcCertificateHash && typeof message.grpcCertificateHash.length === "number" || $util.isString(message.grpcCertificateHash)))
                                    return "grpcCertificateHash: buffer expected";
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                                if (error)
                                    return "adminKey." + error;
                            }
                            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                                if (typeof message.declineReward !== "boolean")
                                    return "declineReward: boolean expected";
                            if (message.grpcProxyEndpoint != null && $Object.hasOwnProperty.call(message, "grpcProxyEndpoint")) {
                                let error = $root.proto.ServiceEndpoint.verify(message.grpcProxyEndpoint, _depth + 1);
                                if (error)
                                    return "grpcProxyEndpoint." + error;
                            }
                            if (message.associatedRegisteredNode != null && $Object.hasOwnProperty.call(message, "associatedRegisteredNode")) {
                                if (!$Array.isArray(message.associatedRegisteredNode))
                                    return "associatedRegisteredNode: array expected";
                                for (let i = 0; i < message.associatedRegisteredNode.length; ++i)
                                    if (!$util.isInteger(message.associatedRegisteredNode[i]) && !(message.associatedRegisteredNode[i] && $util.isInteger(message.associatedRegisteredNode[i].low) && $util.isInteger(message.associatedRegisteredNode[i].high)))
                                        return "associatedRegisteredNode: integer|Long[] expected";
                            }
                            return null;
                        };

                        /**
                         * Creates a NodeCreateTransactionBody message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody} NodeCreateTransactionBody
                         */
                        NodeCreateTransactionBody.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.addressbook.NodeCreateTransactionBody: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody();
                            if (object.accountId != null) {
                                if (!$util.isObject(object.accountId))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.accountId: object expected");
                                message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
                            }
                            if (object.description != null)
                                if (typeof object.description !== "string" || object.description.length)
                                    message.description = $String(object.description);
                            if (object.gossipEndpoint) {
                                if (!$Array.isArray(object.gossipEndpoint))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.gossipEndpoint: array expected");
                                message.gossipEndpoint = $Array(object.gossipEndpoint.length);
                                for (let i = 0; i < object.gossipEndpoint.length; ++i) {
                                    if (!$util.isObject(object.gossipEndpoint[i]))
                                        throw $TypeError(".com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.gossipEndpoint: object expected");
                                    message.gossipEndpoint[i] = $root.proto.ServiceEndpoint.fromObject(object.gossipEndpoint[i], _depth + 1);
                                }
                            }
                            if (object.serviceEndpoint) {
                                if (!$Array.isArray(object.serviceEndpoint))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.serviceEndpoint: array expected");
                                message.serviceEndpoint = $Array(object.serviceEndpoint.length);
                                for (let i = 0; i < object.serviceEndpoint.length; ++i) {
                                    if (!$util.isObject(object.serviceEndpoint[i]))
                                        throw $TypeError(".com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.serviceEndpoint: object expected");
                                    message.serviceEndpoint[i] = $root.proto.ServiceEndpoint.fromObject(object.serviceEndpoint[i], _depth + 1);
                                }
                            }
                            if (object.gossipCaCertificate != null)
                                if (object.gossipCaCertificate.length)
                                    if (typeof object.gossipCaCertificate === "string")
                                        $util.base64.decode(object.gossipCaCertificate, message.gossipCaCertificate = $util.newBuffer($util.base64.length(object.gossipCaCertificate)), 0);
                                    else if (object.gossipCaCertificate.length >= 0)
                                        message.gossipCaCertificate = object.gossipCaCertificate;
                            if (object.grpcCertificateHash != null)
                                if (object.grpcCertificateHash.length)
                                    if (typeof object.grpcCertificateHash === "string")
                                        $util.base64.decode(object.grpcCertificateHash, message.grpcCertificateHash = $util.newBuffer($util.base64.length(object.grpcCertificateHash)), 0);
                                    else if (object.grpcCertificateHash.length >= 0)
                                        message.grpcCertificateHash = object.grpcCertificateHash;
                            if (object.adminKey != null) {
                                if (!$util.isObject(object.adminKey))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.adminKey: object expected");
                                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
                            }
                            if (object.declineReward != null)
                                if (object.declineReward)
                                    message.declineReward = $Boolean(object.declineReward);
                            if (object.grpcProxyEndpoint != null) {
                                if (!$util.isObject(object.grpcProxyEndpoint))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.grpcProxyEndpoint: object expected");
                                message.grpcProxyEndpoint = $root.proto.ServiceEndpoint.fromObject(object.grpcProxyEndpoint, _depth + 1);
                            }
                            if (object.associatedRegisteredNode) {
                                if (!$Array.isArray(object.associatedRegisteredNode))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.associatedRegisteredNode: array expected");
                                message.associatedRegisteredNode = $Array(object.associatedRegisteredNode.length);
                                for (let i = 0; i < object.associatedRegisteredNode.length; ++i)
                                    if ($util.Long)
                                        message.associatedRegisteredNode[i] = $util.Long.fromValue(object.associatedRegisteredNode[i], true);
                                    else if (typeof object.associatedRegisteredNode[i] === "string")
                                        message.associatedRegisteredNode[i] = $parseInt(object.associatedRegisteredNode[i], 10);
                                    else if (typeof object.associatedRegisteredNode[i] === "number")
                                        message.associatedRegisteredNode[i] = object.associatedRegisteredNode[i];
                                    else if (typeof object.associatedRegisteredNode[i] === "object")
                                        message.associatedRegisteredNode[i] = new $util.LongBits(object.associatedRegisteredNode[i].low >>> 0, object.associatedRegisteredNode[i].high >>> 0).toNumber(true);
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a NodeCreateTransactionBody message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody} message NodeCreateTransactionBody
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        NodeCreateTransactionBody.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.arrays || options.defaults) {
                                object.gossipEndpoint = [];
                                object.serviceEndpoint = [];
                                object.associatedRegisteredNode = [];
                            }
                            if (options.defaults) {
                                object.accountId = null;
                                object.description = "";
                                if (options.bytes === $String)
                                    object.gossipCaCertificate = "";
                                else {
                                    object.gossipCaCertificate = [];
                                    if (options.bytes !== $Array)
                                        object.gossipCaCertificate = $util.newBuffer(object.gossipCaCertificate);
                                }
                                if (options.bytes === $String)
                                    object.grpcCertificateHash = "";
                                else {
                                    object.grpcCertificateHash = [];
                                    if (options.bytes !== $Array)
                                        object.grpcCertificateHash = $util.newBuffer(object.grpcCertificateHash);
                                }
                                object.adminKey = null;
                                object.declineReward = false;
                                object.grpcProxyEndpoint = null;
                            }
                            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                                object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
                            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                object.description = message.description;
                            if (message.gossipEndpoint && message.gossipEndpoint.length) {
                                object.gossipEndpoint = $Array(message.gossipEndpoint.length);
                                for (let j = 0; j < message.gossipEndpoint.length; ++j)
                                    object.gossipEndpoint[j] = $root.proto.ServiceEndpoint.toObject(message.gossipEndpoint[j], options, _depth + 1);
                            }
                            if (message.serviceEndpoint && message.serviceEndpoint.length) {
                                object.serviceEndpoint = $Array(message.serviceEndpoint.length);
                                for (let j = 0; j < message.serviceEndpoint.length; ++j)
                                    object.serviceEndpoint[j] = $root.proto.ServiceEndpoint.toObject(message.serviceEndpoint[j], options, _depth + 1);
                            }
                            if (message.gossipCaCertificate != null && $Object.hasOwnProperty.call(message, "gossipCaCertificate"))
                                object.gossipCaCertificate = options.bytes === $String ? $util.base64.encode(message.gossipCaCertificate, 0, message.gossipCaCertificate.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.gossipCaCertificate) : message.gossipCaCertificate;
                            if (message.grpcCertificateHash != null && $Object.hasOwnProperty.call(message, "grpcCertificateHash"))
                                object.grpcCertificateHash = options.bytes === $String ? $util.base64.encode(message.grpcCertificateHash, 0, message.grpcCertificateHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.grpcCertificateHash) : message.grpcCertificateHash;
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
                            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                                object.declineReward = message.declineReward;
                            if (message.grpcProxyEndpoint != null && $Object.hasOwnProperty.call(message, "grpcProxyEndpoint"))
                                object.grpcProxyEndpoint = $root.proto.ServiceEndpoint.toObject(message.grpcProxyEndpoint, options, _depth + 1);
                            if (message.associatedRegisteredNode && message.associatedRegisteredNode.length) {
                                object.associatedRegisteredNode = $Array(message.associatedRegisteredNode.length);
                                for (let j = 0; j < message.associatedRegisteredNode.length; ++j)
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.associatedRegisteredNode[j] = typeof message.associatedRegisteredNode[j] === "number" ? $BigInt(message.associatedRegisteredNode[j]) : $util.Long.fromBits(message.associatedRegisteredNode[j].low >>> 0, message.associatedRegisteredNode[j].high >>> 0, true).toBigInt();
                                    else if (typeof message.associatedRegisteredNode[j] === "number")
                                        object.associatedRegisteredNode[j] = options.longs === $String ? $String(message.associatedRegisteredNode[j]) : message.associatedRegisteredNode[j];
                                    else
                                        object.associatedRegisteredNode[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.associatedRegisteredNode[j]) : options.longs === $Number ? new $util.LongBits(message.associatedRegisteredNode[j].low >>> 0, message.associatedRegisteredNode[j].high >>> 0).toNumber(true) : message.associatedRegisteredNode[j];
                            }
                            return object;
                        };

                        /**
                         * Converts this NodeCreateTransactionBody to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        NodeCreateTransactionBody.prototype.toJSON = function() {
                            return NodeCreateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for NodeCreateTransactionBody
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.addressbook.NodeCreateTransactionBody
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        NodeCreateTransactionBody.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.addressbook.NodeCreateTransactionBody";
                        };

                        return NodeCreateTransactionBody;
                    })();

                    addressbook.NodeUpdateTransactionBody = (function() {

                        /**
                         * Properties of a NodeUpdateTransactionBody.
                         * @typedef {Object} com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Properties
                         * @property {number|Long|null} [nodeId] A consensus node identifier in the network state.
                         * <p>
                         * The node identified MUST exist in the network address book.<br/>
                         * The node identified MUST NOT be deleted.<br/>
                         * This value is REQUIRED.
                         * @property {proto.AccountID.$Properties|null} [accountId] An account identifier.
                         * <p>
                         * If set, this SHALL replace the node account identifier.<br/>
                         * If set to a valid, non-zero account identifier, this transaction MUST
                         * be signed by the `Key` for the new account. Additionally, when only
                         * the `account_id` is updated (no other fields changed), the transaction
                         * MUST also be signed by either the current node account `Key` or
                         * the node `admin_key`. When other fields are also changed, the node
                         * `admin_key` MUST sign (as required for any non-account field update).<br/>
                         * If set to the sentinel value `0.0.0`, the node account association
                         * SHALL be removed, and this transaction MUST be signed by either
                         * the current node account `Key` or the node `admin_key`.<br/>
                         * Each `account_id` MUST NOT be associated with more than one node.
                         * @property {google.protobuf.StringValue.$Properties|null} [description] A short description of the node.
                         * <p>
                         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
                         * (default 100) bytes when encoded as UTF-8.<br/>
                         * If set, this value SHALL replace the previous value.
                         * @property {Array.<proto.ServiceEndpoint.$Properties>|null} [gossipEndpoint] A list of service endpoints for gossip.
                         * <p>
                         * If set, this list MUST meet the following requirements.
                         * <hr/>
                         * These endpoints SHALL represent the published endpoints to which other
                         * consensus nodes may _gossip_ transactions.<br/>
                         * These endpoints SHOULD NOT specify both address and DNS name.<br/>
                         * This list MUST NOT be empty.<br/>
                         * This list MUST NOT contain more than `10` entries.<br/>
                         * The first two entries in this list SHALL be the endpoints published to
                         * all consensus nodes.<br/>
                         * All other entries SHALL be reserved for future use.
                         * <p>
                         * Each network may have additional requirements for these endpoints.
                         * A client MUST check network-specific documentation for those
                         * details.<br/>
                         * <blockquote>Example<blockquote>
                         * Hedera Mainnet _requires_ that address be specified, and does not
                         * permit DNS name (FQDN) to be specified.
                         * </blockquote>
                         * <blockquote>
                         * Solo, however, _requires_ DNS name (FQDN) but also permits
                         * address.
                         * </blockquote></blockquote>
                         * <p>
                         * If set, the new list SHALL replace the existing list.
                         * @property {Array.<proto.ServiceEndpoint.$Properties>|null} [serviceEndpoint] A list of service endpoints for gRPC calls.
                         * <p>
                         * If set, this list MUST meet the following requirements.
                         * <hr/>
                         * These endpoints SHALL represent the published endpoints to which clients
                         * may submit transactions.<br/>
                         * These endpoints SHOULD specify address and port.<br/>
                         * These endpoints MAY specify a DNS name.<br/>
                         * These endpoints SHOULD NOT specify both address and DNS name.<br/>
                         * This list MUST NOT be empty.<br/>
                         * This list MUST NOT contain more than `8` entries.
                         * <p>
                         * Each network may have additional requirements for these endpoints.
                         * A client MUST check network-specific documentation for those
                         * details.
                         * <p>
                         * If set, the new list SHALL replace the existing list.
                         * @property {google.protobuf.BytesValue.$Properties|null} [gossipCaCertificate] A certificate used to sign gossip events.
                         * <p>
                         * This value MUST be a certificate of a type permitted for gossip
                         * signatures.<br/>
                         * This value MUST be the DER encoding of the certificate presented.
                         * <p>
                         * If set, the new value SHALL replace the existing bytes value.
                         * @property {google.protobuf.BytesValue.$Properties|null} [grpcCertificateHash] A hash of the node gRPC TLS certificate.
                         * <p>
                         * This value MAY be used to verify the certificate presented by the node
                         * during TLS negotiation for gRPC.<br/>
                         * This value MUST be a SHA-384 hash.<br/>
                         * The TLS certificate to be hashed MUST first be in PEM format and MUST be
                         * encoded with UTF-8 NFKD encoding to a stream of bytes provided to
                         * the hash algorithm.<br/>
                         * <p>
                         * If set, the new value SHALL replace the existing hash value.
                         * @property {proto.Key.$Properties|null} [adminKey] An administrative key controlled by the node operator.
                         * <p>
                         * This field is OPTIONAL.<br/>
                         * If set, this key MUST sign this transaction.<br/>
                         * If set, this key MUST sign each subsequent transaction to
                         * update this node.<br/>
                         * If set, this field MUST contain a valid `Key` value.<br/>
                         * If set, this field MUST NOT be set to an empty `KeyList`.
                         * @property {google.protobuf.BoolValue.$Properties|null} [declineReward] A boolean indicating that this node has chosen to decline node rewards
                         * distributed at the end of staking period.
                         * <p>
                         * This node SHALL NOT receive reward if this value is set, and `true`.
                         * @property {proto.ServiceEndpoint.$Properties|null} [grpcProxyEndpoint] A web proxy for gRPC from non-gRPC clients.
                         * <p>
                         * This endpoint SHALL be a Fully Qualified Domain Name (FQDN) using the HTTPS
                         * protocol, and SHALL support gRPC-Web for use by browser-based clients.<br/>
                         * This endpoint MUST be signed by a trusted certificate authority.<br/>
                         * This endpoint MUST use a valid port and SHALL be reachable over TLS.<br/>
                         * This field MAY be omitted if the node does not support gRPC-Web access.<br/>
                         * This field MUST be updated if the gRPC-Web endpoint changes.<br/>
                         * This field SHALL enable frontend clients to avoid hard-coded proxy endpoints.<br/>
                         * This field MAY be set to `ServiceEndpoint.DEFAULT` to remove a previously-valid
                         * web proxy.
                         * @property {com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Properties|null} [associatedRegisteredNodeList] A list of registered nodes operated by the same entity as this node.<br/>
                         * This value may contain a list of "registered nodes" (as described in
                         * HIP-1137) that are operated by the same entity that operates this
                         * consensus node.
                         * <p>
                         * This field is OPTIONAL.<br/>
                         * If this field is not set, the current list SHALL NOT change.<br/>
                         * If this field is set, but contains an empty list, any existing
                         * associated registered nodes SHALL be removed.<br/>
                         * This field MUST NOT contain more than twenty(20) entries.<br/>
                         * Every entry in this list MUST be a valid `registered_node_id` for a
                         * current registered node.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a NodeUpdateTransactionBody.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @interface INodeUpdateTransactionBody
                         * @augments com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Properties
                         * @deprecated Use com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Properties instead.
                         */

                        /**
                         * Shape of a NodeUpdateTransactionBody.
                         * @typedef {{
                         *   nodeId?: number|Long|null;
                         *   accountId?: proto.AccountID.$Shape|null;
                         *   description?: google.protobuf.StringValue.$Shape|null;
                         *   gossipEndpoint?: Array.<proto.ServiceEndpoint.$Shape>|null;
                         *   serviceEndpoint?: Array.<proto.ServiceEndpoint.$Shape>|null;
                         *   gossipCaCertificate?: google.protobuf.BytesValue.$Shape|null;
                         *   grpcCertificateHash?: google.protobuf.BytesValue.$Shape|null;
                         *   adminKey?: proto.Key.$Shape|null;
                         *   declineReward?: google.protobuf.BoolValue.$Shape|null;
                         *   grpcProxyEndpoint?: proto.ServiceEndpoint.$Shape|null;
                         *   associatedRegisteredNodeList?: com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Shape|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * }} com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Shape
                         */

                        /**
                         * Constructs a new NodeUpdateTransactionBody.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @classdesc Transaction body to modify address book node attributes.
                         * 
                         * - This transaction SHALL enable the node operator, as identified by the
                         * `admin_key`, to modify operational attributes of the node.
                         * - This transaction MUST be signed by the active `admin_key` for the node.
                         * - If this transaction sets a new value for the `admin_key`, then both the
                         * current `admin_key`, and the new `admin_key` MUST sign this transaction.
                         * - This transaction SHALL NOT change any field that is not set (is null) in
                         * this transaction body.
                         * - This SHALL create a pending update to the node, but the change SHALL NOT
                         * be immediately applied to the active configuration.
                         * - All pending node updates SHALL be applied to the active network
                         * configuration during the next `freeze` transaction with the field
                         * `freeze_type` set to `PREPARE_UPGRADE`.
                         * 
                         * ### Block Stream Effects
                         * None.
                         * @constructor
                         * @param {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const NodeUpdateTransactionBody = function (properties) {
                            this.gossipEndpoint = [];
                            this.serviceEndpoint = [];
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A consensus node identifier in the network state.
                         * <p>
                         * The node identified MUST exist in the network address book.<br/>
                         * The node identified MUST NOT be deleted.<br/>
                         * This value is REQUIRED.
                         * @member {number|Long} nodeId
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @instance
                         */
                        NodeUpdateTransactionBody.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * An account identifier.
                         * <p>
                         * If set, this SHALL replace the node account identifier.<br/>
                         * If set to a valid, non-zero account identifier, this transaction MUST
                         * be signed by the `Key` for the new account. Additionally, when only
                         * the `account_id` is updated (no other fields changed), the transaction
                         * MUST also be signed by either the current node account `Key` or
                         * the node `admin_key`. When other fields are also changed, the node
                         * `admin_key` MUST sign (as required for any non-account field update).<br/>
                         * If set to the sentinel value `0.0.0`, the node account association
                         * SHALL be removed, and this transaction MUST be signed by either
                         * the current node account `Key` or the node `admin_key`.<br/>
                         * Each `account_id` MUST NOT be associated with more than one node.
                         * @member {proto.AccountID.$Properties|null|undefined} accountId
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @instance
                         */
                        NodeUpdateTransactionBody.prototype.accountId = null;

                        /**
                         * A short description of the node.
                         * <p>
                         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
                         * (default 100) bytes when encoded as UTF-8.<br/>
                         * If set, this value SHALL replace the previous value.
                         * @member {google.protobuf.StringValue.$Properties|null|undefined} description
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @instance
                         */
                        NodeUpdateTransactionBody.prototype.description = null;

                        /**
                         * A list of service endpoints for gossip.
                         * <p>
                         * If set, this list MUST meet the following requirements.
                         * <hr/>
                         * These endpoints SHALL represent the published endpoints to which other
                         * consensus nodes may _gossip_ transactions.<br/>
                         * These endpoints SHOULD NOT specify both address and DNS name.<br/>
                         * This list MUST NOT be empty.<br/>
                         * This list MUST NOT contain more than `10` entries.<br/>
                         * The first two entries in this list SHALL be the endpoints published to
                         * all consensus nodes.<br/>
                         * All other entries SHALL be reserved for future use.
                         * <p>
                         * Each network may have additional requirements for these endpoints.
                         * A client MUST check network-specific documentation for those
                         * details.<br/>
                         * <blockquote>Example<blockquote>
                         * Hedera Mainnet _requires_ that address be specified, and does not
                         * permit DNS name (FQDN) to be specified.
                         * </blockquote>
                         * <blockquote>
                         * Solo, however, _requires_ DNS name (FQDN) but also permits
                         * address.
                         * </blockquote></blockquote>
                         * <p>
                         * If set, the new list SHALL replace the existing list.
                         * @member {Array.<proto.ServiceEndpoint.$Properties>} gossipEndpoint
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @instance
                         */
                        NodeUpdateTransactionBody.prototype.gossipEndpoint = $util.emptyArray;

                        /**
                         * A list of service endpoints for gRPC calls.
                         * <p>
                         * If set, this list MUST meet the following requirements.
                         * <hr/>
                         * These endpoints SHALL represent the published endpoints to which clients
                         * may submit transactions.<br/>
                         * These endpoints SHOULD specify address and port.<br/>
                         * These endpoints MAY specify a DNS name.<br/>
                         * These endpoints SHOULD NOT specify both address and DNS name.<br/>
                         * This list MUST NOT be empty.<br/>
                         * This list MUST NOT contain more than `8` entries.
                         * <p>
                         * Each network may have additional requirements for these endpoints.
                         * A client MUST check network-specific documentation for those
                         * details.
                         * <p>
                         * If set, the new list SHALL replace the existing list.
                         * @member {Array.<proto.ServiceEndpoint.$Properties>} serviceEndpoint
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @instance
                         */
                        NodeUpdateTransactionBody.prototype.serviceEndpoint = $util.emptyArray;

                        /**
                         * A certificate used to sign gossip events.
                         * <p>
                         * This value MUST be a certificate of a type permitted for gossip
                         * signatures.<br/>
                         * This value MUST be the DER encoding of the certificate presented.
                         * <p>
                         * If set, the new value SHALL replace the existing bytes value.
                         * @member {google.protobuf.BytesValue.$Properties|null|undefined} gossipCaCertificate
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @instance
                         */
                        NodeUpdateTransactionBody.prototype.gossipCaCertificate = null;

                        /**
                         * A hash of the node gRPC TLS certificate.
                         * <p>
                         * This value MAY be used to verify the certificate presented by the node
                         * during TLS negotiation for gRPC.<br/>
                         * This value MUST be a SHA-384 hash.<br/>
                         * The TLS certificate to be hashed MUST first be in PEM format and MUST be
                         * encoded with UTF-8 NFKD encoding to a stream of bytes provided to
                         * the hash algorithm.<br/>
                         * <p>
                         * If set, the new value SHALL replace the existing hash value.
                         * @member {google.protobuf.BytesValue.$Properties|null|undefined} grpcCertificateHash
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @instance
                         */
                        NodeUpdateTransactionBody.prototype.grpcCertificateHash = null;

                        /**
                         * An administrative key controlled by the node operator.
                         * <p>
                         * This field is OPTIONAL.<br/>
                         * If set, this key MUST sign this transaction.<br/>
                         * If set, this key MUST sign each subsequent transaction to
                         * update this node.<br/>
                         * If set, this field MUST contain a valid `Key` value.<br/>
                         * If set, this field MUST NOT be set to an empty `KeyList`.
                         * @member {proto.Key.$Properties|null|undefined} adminKey
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @instance
                         */
                        NodeUpdateTransactionBody.prototype.adminKey = null;

                        /**
                         * A boolean indicating that this node has chosen to decline node rewards
                         * distributed at the end of staking period.
                         * <p>
                         * This node SHALL NOT receive reward if this value is set, and `true`.
                         * @member {google.protobuf.BoolValue.$Properties|null|undefined} declineReward
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @instance
                         */
                        NodeUpdateTransactionBody.prototype.declineReward = null;

                        /**
                         * A web proxy for gRPC from non-gRPC clients.
                         * <p>
                         * This endpoint SHALL be a Fully Qualified Domain Name (FQDN) using the HTTPS
                         * protocol, and SHALL support gRPC-Web for use by browser-based clients.<br/>
                         * This endpoint MUST be signed by a trusted certificate authority.<br/>
                         * This endpoint MUST use a valid port and SHALL be reachable over TLS.<br/>
                         * This field MAY be omitted if the node does not support gRPC-Web access.<br/>
                         * This field MUST be updated if the gRPC-Web endpoint changes.<br/>
                         * This field SHALL enable frontend clients to avoid hard-coded proxy endpoints.<br/>
                         * This field MAY be set to `ServiceEndpoint.DEFAULT` to remove a previously-valid
                         * web proxy.
                         * @member {proto.ServiceEndpoint.$Properties|null|undefined} grpcProxyEndpoint
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @instance
                         */
                        NodeUpdateTransactionBody.prototype.grpcProxyEndpoint = null;

                        /**
                         * A list of registered nodes operated by the same entity as this node.<br/>
                         * This value may contain a list of "registered nodes" (as described in
                         * HIP-1137) that are operated by the same entity that operates this
                         * consensus node.
                         * <p>
                         * This field is OPTIONAL.<br/>
                         * If this field is not set, the current list SHALL NOT change.<br/>
                         * If this field is set, but contains an empty list, any existing
                         * associated registered nodes SHALL be removed.<br/>
                         * This field MUST NOT contain more than twenty(20) entries.<br/>
                         * Every entry in this list MUST be a valid `registered_node_id` for a
                         * current registered node.
                         * @member {com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Properties|null|undefined} associatedRegisteredNodeList
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @instance
                         */
                        NodeUpdateTransactionBody.prototype.associatedRegisteredNodeList = null;

                        /**
                         * Creates a new NodeUpdateTransactionBody instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody} NodeUpdateTransactionBody instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Shape): com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody & com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Shape;
                         *   (properties?: com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Properties): com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody;
                         * }}
                         */
                        NodeUpdateTransactionBody.create = function(properties) {
                            return new NodeUpdateTransactionBody(properties);
                        };

                        /**
                         * Encodes the specified NodeUpdateTransactionBody message. Does not implicitly {@link com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Properties} message NodeUpdateTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        NodeUpdateTransactionBody.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.nodeId);
                            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                                $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                $root.google.protobuf.StringValue.encode(message.description, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.gossipEndpoint != null && message.gossipEndpoint.length)
                                for (let i = 0; i < message.gossipEndpoint.length; ++i)
                                    $root.proto.ServiceEndpoint.encode(message.gossipEndpoint[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                            if (message.serviceEndpoint != null && message.serviceEndpoint.length)
                                for (let i = 0; i < message.serviceEndpoint.length; ++i)
                                    $root.proto.ServiceEndpoint.encode(message.serviceEndpoint[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                            if (message.gossipCaCertificate != null && $Object.hasOwnProperty.call(message, "gossipCaCertificate"))
                                $root.google.protobuf.BytesValue.encode(message.gossipCaCertificate, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                            if (message.grpcCertificateHash != null && $Object.hasOwnProperty.call(message, "grpcCertificateHash"))
                                $root.google.protobuf.BytesValue.encode(message.grpcCertificateHash, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                                $root.google.protobuf.BoolValue.encode(message.declineReward, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
                            if (message.grpcProxyEndpoint != null && $Object.hasOwnProperty.call(message, "grpcProxyEndpoint"))
                                $root.proto.ServiceEndpoint.encode(message.grpcProxyEndpoint, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
                            if (message.associatedRegisteredNodeList != null && $Object.hasOwnProperty.call(message, "associatedRegisteredNodeList"))
                                $root.com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.encode(message.associatedRegisteredNodeList, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified NodeUpdateTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Properties} message NodeUpdateTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        NodeUpdateTransactionBody.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a NodeUpdateTransactionBody message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody & com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Shape} NodeUpdateTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        NodeUpdateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.nodeId = value;
                                        else
                                            delete message.nodeId;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        message.description = $root.google.protobuf.StringValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.description);
                                        continue;
                                    }
                                case 4: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.gossipEndpoint && message.gossipEndpoint.length))
                                            message.gossipEndpoint = [];
                                        message.gossipEndpoint.push($root.proto.ServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                case 5: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.serviceEndpoint && message.serviceEndpoint.length))
                                            message.serviceEndpoint = [];
                                        message.serviceEndpoint.push($root.proto.ServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                case 6: {
                                        if (wireType !== 2)
                                            break;
                                        message.gossipCaCertificate = $root.google.protobuf.BytesValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.gossipCaCertificate);
                                        continue;
                                    }
                                case 7: {
                                        if (wireType !== 2)
                                            break;
                                        message.grpcCertificateHash = $root.google.protobuf.BytesValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.grpcCertificateHash);
                                        continue;
                                    }
                                case 8: {
                                        if (wireType !== 2)
                                            break;
                                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                                        continue;
                                    }
                                case 9: {
                                        if (wireType !== 2)
                                            break;
                                        message.declineReward = $root.google.protobuf.BoolValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.declineReward);
                                        continue;
                                    }
                                case 10: {
                                        if (wireType !== 2)
                                            break;
                                        message.grpcProxyEndpoint = $root.proto.ServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.grpcProxyEndpoint);
                                        continue;
                                    }
                                case 11: {
                                        if (wireType !== 2)
                                            break;
                                        message.associatedRegisteredNodeList = $root.com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.associatedRegisteredNodeList);
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a NodeUpdateTransactionBody message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody & com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Shape} NodeUpdateTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        NodeUpdateTransactionBody.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a NodeUpdateTransactionBody message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        NodeUpdateTransactionBody.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                    return "nodeId: integer|Long expected";
                            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                                let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                                if (error)
                                    return "accountId." + error;
                            }
                            if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                                let error = $root.google.protobuf.StringValue.verify(message.description, _depth + 1);
                                if (error)
                                    return "description." + error;
                            }
                            if (message.gossipEndpoint != null && $Object.hasOwnProperty.call(message, "gossipEndpoint")) {
                                if (!$Array.isArray(message.gossipEndpoint))
                                    return "gossipEndpoint: array expected";
                                for (let i = 0; i < message.gossipEndpoint.length; ++i) {
                                    let error = $root.proto.ServiceEndpoint.verify(message.gossipEndpoint[i], _depth + 1);
                                    if (error)
                                        return "gossipEndpoint." + error;
                                }
                            }
                            if (message.serviceEndpoint != null && $Object.hasOwnProperty.call(message, "serviceEndpoint")) {
                                if (!$Array.isArray(message.serviceEndpoint))
                                    return "serviceEndpoint: array expected";
                                for (let i = 0; i < message.serviceEndpoint.length; ++i) {
                                    let error = $root.proto.ServiceEndpoint.verify(message.serviceEndpoint[i], _depth + 1);
                                    if (error)
                                        return "serviceEndpoint." + error;
                                }
                            }
                            if (message.gossipCaCertificate != null && $Object.hasOwnProperty.call(message, "gossipCaCertificate")) {
                                let error = $root.google.protobuf.BytesValue.verify(message.gossipCaCertificate, _depth + 1);
                                if (error)
                                    return "gossipCaCertificate." + error;
                            }
                            if (message.grpcCertificateHash != null && $Object.hasOwnProperty.call(message, "grpcCertificateHash")) {
                                let error = $root.google.protobuf.BytesValue.verify(message.grpcCertificateHash, _depth + 1);
                                if (error)
                                    return "grpcCertificateHash." + error;
                            }
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                                if (error)
                                    return "adminKey." + error;
                            }
                            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward")) {
                                let error = $root.google.protobuf.BoolValue.verify(message.declineReward, _depth + 1);
                                if (error)
                                    return "declineReward." + error;
                            }
                            if (message.grpcProxyEndpoint != null && $Object.hasOwnProperty.call(message, "grpcProxyEndpoint")) {
                                let error = $root.proto.ServiceEndpoint.verify(message.grpcProxyEndpoint, _depth + 1);
                                if (error)
                                    return "grpcProxyEndpoint." + error;
                            }
                            if (message.associatedRegisteredNodeList != null && $Object.hasOwnProperty.call(message, "associatedRegisteredNodeList")) {
                                let error = $root.com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.verify(message.associatedRegisteredNodeList, _depth + 1);
                                if (error)
                                    return "associatedRegisteredNodeList." + error;
                            }
                            return null;
                        };

                        /**
                         * Creates a NodeUpdateTransactionBody message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody} NodeUpdateTransactionBody
                         */
                        NodeUpdateTransactionBody.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody();
                            if (object.nodeId != null)
                                if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                    if ($util.Long)
                                        message.nodeId = $util.Long.fromValue(object.nodeId, true);
                                    else if (typeof object.nodeId === "string")
                                        message.nodeId = $parseInt(object.nodeId, 10);
                                    else if (typeof object.nodeId === "number")
                                        message.nodeId = object.nodeId;
                                    else if (typeof object.nodeId === "object")
                                        message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
                            if (object.accountId != null) {
                                if (!$util.isObject(object.accountId))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.accountId: object expected");
                                message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
                            }
                            if (object.description != null) {
                                if (!$util.isObject(object.description))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.description: object expected");
                                message.description = $root.google.protobuf.StringValue.fromObject(object.description, _depth + 1);
                            }
                            if (object.gossipEndpoint) {
                                if (!$Array.isArray(object.gossipEndpoint))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.gossipEndpoint: array expected");
                                message.gossipEndpoint = $Array(object.gossipEndpoint.length);
                                for (let i = 0; i < object.gossipEndpoint.length; ++i) {
                                    if (!$util.isObject(object.gossipEndpoint[i]))
                                        throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.gossipEndpoint: object expected");
                                    message.gossipEndpoint[i] = $root.proto.ServiceEndpoint.fromObject(object.gossipEndpoint[i], _depth + 1);
                                }
                            }
                            if (object.serviceEndpoint) {
                                if (!$Array.isArray(object.serviceEndpoint))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.serviceEndpoint: array expected");
                                message.serviceEndpoint = $Array(object.serviceEndpoint.length);
                                for (let i = 0; i < object.serviceEndpoint.length; ++i) {
                                    if (!$util.isObject(object.serviceEndpoint[i]))
                                        throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.serviceEndpoint: object expected");
                                    message.serviceEndpoint[i] = $root.proto.ServiceEndpoint.fromObject(object.serviceEndpoint[i], _depth + 1);
                                }
                            }
                            if (object.gossipCaCertificate != null) {
                                if (!$util.isObject(object.gossipCaCertificate))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.gossipCaCertificate: object expected");
                                message.gossipCaCertificate = $root.google.protobuf.BytesValue.fromObject(object.gossipCaCertificate, _depth + 1);
                            }
                            if (object.grpcCertificateHash != null) {
                                if (!$util.isObject(object.grpcCertificateHash))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.grpcCertificateHash: object expected");
                                message.grpcCertificateHash = $root.google.protobuf.BytesValue.fromObject(object.grpcCertificateHash, _depth + 1);
                            }
                            if (object.adminKey != null) {
                                if (!$util.isObject(object.adminKey))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.adminKey: object expected");
                                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
                            }
                            if (object.declineReward != null) {
                                if (!$util.isObject(object.declineReward))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.declineReward: object expected");
                                message.declineReward = $root.google.protobuf.BoolValue.fromObject(object.declineReward, _depth + 1);
                            }
                            if (object.grpcProxyEndpoint != null) {
                                if (!$util.isObject(object.grpcProxyEndpoint))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.grpcProxyEndpoint: object expected");
                                message.grpcProxyEndpoint = $root.proto.ServiceEndpoint.fromObject(object.grpcProxyEndpoint, _depth + 1);
                            }
                            if (object.associatedRegisteredNodeList != null) {
                                if (!$util.isObject(object.associatedRegisteredNodeList))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.associatedRegisteredNodeList: object expected");
                                message.associatedRegisteredNodeList = $root.com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.fromObject(object.associatedRegisteredNodeList, _depth + 1);
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a NodeUpdateTransactionBody message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody} message NodeUpdateTransactionBody
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        NodeUpdateTransactionBody.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.arrays || options.defaults) {
                                object.gossipEndpoint = [];
                                object.serviceEndpoint = [];
                            }
                            if (options.defaults) {
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                object.accountId = null;
                                object.description = null;
                                object.gossipCaCertificate = null;
                                object.grpcCertificateHash = null;
                                object.adminKey = null;
                                object.declineReward = null;
                                object.grpcProxyEndpoint = null;
                                object.associatedRegisteredNodeList = null;
                            }
                            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                                else if (typeof message.nodeId === "number")
                                    object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                else
                                    object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
                            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                                object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
                            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                object.description = $root.google.protobuf.StringValue.toObject(message.description, options, _depth + 1);
                            if (message.gossipEndpoint && message.gossipEndpoint.length) {
                                object.gossipEndpoint = $Array(message.gossipEndpoint.length);
                                for (let j = 0; j < message.gossipEndpoint.length; ++j)
                                    object.gossipEndpoint[j] = $root.proto.ServiceEndpoint.toObject(message.gossipEndpoint[j], options, _depth + 1);
                            }
                            if (message.serviceEndpoint && message.serviceEndpoint.length) {
                                object.serviceEndpoint = $Array(message.serviceEndpoint.length);
                                for (let j = 0; j < message.serviceEndpoint.length; ++j)
                                    object.serviceEndpoint[j] = $root.proto.ServiceEndpoint.toObject(message.serviceEndpoint[j], options, _depth + 1);
                            }
                            if (message.gossipCaCertificate != null && $Object.hasOwnProperty.call(message, "gossipCaCertificate"))
                                object.gossipCaCertificate = $root.google.protobuf.BytesValue.toObject(message.gossipCaCertificate, options, _depth + 1);
                            if (message.grpcCertificateHash != null && $Object.hasOwnProperty.call(message, "grpcCertificateHash"))
                                object.grpcCertificateHash = $root.google.protobuf.BytesValue.toObject(message.grpcCertificateHash, options, _depth + 1);
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
                            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                                object.declineReward = $root.google.protobuf.BoolValue.toObject(message.declineReward, options, _depth + 1);
                            if (message.grpcProxyEndpoint != null && $Object.hasOwnProperty.call(message, "grpcProxyEndpoint"))
                                object.grpcProxyEndpoint = $root.proto.ServiceEndpoint.toObject(message.grpcProxyEndpoint, options, _depth + 1);
                            if (message.associatedRegisteredNodeList != null && $Object.hasOwnProperty.call(message, "associatedRegisteredNodeList"))
                                object.associatedRegisteredNodeList = $root.com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.toObject(message.associatedRegisteredNodeList, options, _depth + 1);
                            return object;
                        };

                        /**
                         * Converts this NodeUpdateTransactionBody to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        NodeUpdateTransactionBody.prototype.toJSON = function() {
                            return NodeUpdateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for NodeUpdateTransactionBody
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        NodeUpdateTransactionBody.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody";
                        };

                        return NodeUpdateTransactionBody;
                    })();

                    addressbook.AssociatedRegisteredNodeList = (function() {

                        /**
                         * Properties of an AssociatedRegisteredNodeList.
                         * @typedef {Object} com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Properties
                         * @property {Array.<number|Long>|null} [associatedRegisteredNode] A list of registered node identifiers.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of an AssociatedRegisteredNodeList.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @interface IAssociatedRegisteredNodeList
                         * @augments com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Properties
                         * @deprecated Use com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Properties instead.
                         */

                        /**
                         * Shape of an AssociatedRegisteredNodeList.
                         * @typedef {com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Properties} com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Shape
                         */

                        /**
                         * Constructs a new AssociatedRegisteredNodeList.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @classdesc A wrapper around a list of associated registered node identifiers.<br/>
                         * This wrapper exists to enable an update transaction to differentiate
                         * between a field that is not set and an empty list of values.
                         * <p>
                         * An _unset_ field of this type SHALL NOT modify existing values.<br/>
                         * A _set_ field of this type with an empty list SHALL remove any
                         * existing values.
                         * @constructor
                         * @param {com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const AssociatedRegisteredNodeList = function (properties) {
                            this.associatedRegisteredNode = [];
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A list of registered node identifiers.
                         * @member {Array.<number|Long>} associatedRegisteredNode
                         * @memberof com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList
                         * @instance
                         */
                        AssociatedRegisteredNodeList.prototype.associatedRegisteredNode = $util.emptyArray;

                        /**
                         * Creates a new AssociatedRegisteredNodeList instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList} AssociatedRegisteredNodeList instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Shape): com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList & com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Shape;
                         *   (properties?: com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Properties): com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList;
                         * }}
                         */
                        AssociatedRegisteredNodeList.create = function(properties) {
                            return new AssociatedRegisteredNodeList(properties);
                        };

                        /**
                         * Encodes the specified AssociatedRegisteredNodeList message. Does not implicitly {@link com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Properties} message AssociatedRegisteredNodeList message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        AssociatedRegisteredNodeList.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.associatedRegisteredNode != null && message.associatedRegisteredNode.length)
                                writer.uint32(/* id 1, wireType 2 =*/10).uint64s(message.associatedRegisteredNode);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified AssociatedRegisteredNodeList message, length delimited. Does not implicitly {@link com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Properties} message AssociatedRegisteredNodeList message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        AssociatedRegisteredNodeList.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes an AssociatedRegisteredNodeList message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList & com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Shape} AssociatedRegisteredNodeList
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        AssociatedRegisteredNodeList.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType === 2) {
                                            if (!(message.associatedRegisteredNode && message.associatedRegisteredNode.length))
                                                message.associatedRegisteredNode = [];
                                            reader.uint64s(message.associatedRegisteredNode);
                                            continue;
                                        }
                                        if (wireType !== 0)
                                            break;
                                        if (!(message.associatedRegisteredNode && message.associatedRegisteredNode.length))
                                            message.associatedRegisteredNode = [];
                                        message.associatedRegisteredNode.push(reader.uint64());
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes an AssociatedRegisteredNodeList message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList & com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.$Shape} AssociatedRegisteredNodeList
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        AssociatedRegisteredNodeList.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies an AssociatedRegisteredNodeList message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        AssociatedRegisteredNodeList.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.associatedRegisteredNode != null && $Object.hasOwnProperty.call(message, "associatedRegisteredNode")) {
                                if (!$Array.isArray(message.associatedRegisteredNode))
                                    return "associatedRegisteredNode: array expected";
                                for (let i = 0; i < message.associatedRegisteredNode.length; ++i)
                                    if (!$util.isInteger(message.associatedRegisteredNode[i]) && !(message.associatedRegisteredNode[i] && $util.isInteger(message.associatedRegisteredNode[i].low) && $util.isInteger(message.associatedRegisteredNode[i].high)))
                                        return "associatedRegisteredNode: integer|Long[] expected";
                            }
                            return null;
                        };

                        /**
                         * Creates an AssociatedRegisteredNodeList message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList} AssociatedRegisteredNodeList
                         */
                        AssociatedRegisteredNodeList.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList();
                            if (object.associatedRegisteredNode) {
                                if (!$Array.isArray(object.associatedRegisteredNode))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList.associatedRegisteredNode: array expected");
                                message.associatedRegisteredNode = $Array(object.associatedRegisteredNode.length);
                                for (let i = 0; i < object.associatedRegisteredNode.length; ++i)
                                    if ($util.Long)
                                        message.associatedRegisteredNode[i] = $util.Long.fromValue(object.associatedRegisteredNode[i], true);
                                    else if (typeof object.associatedRegisteredNode[i] === "string")
                                        message.associatedRegisteredNode[i] = $parseInt(object.associatedRegisteredNode[i], 10);
                                    else if (typeof object.associatedRegisteredNode[i] === "number")
                                        message.associatedRegisteredNode[i] = object.associatedRegisteredNode[i];
                                    else if (typeof object.associatedRegisteredNode[i] === "object")
                                        message.associatedRegisteredNode[i] = new $util.LongBits(object.associatedRegisteredNode[i].low >>> 0, object.associatedRegisteredNode[i].high >>> 0).toNumber(true);
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from an AssociatedRegisteredNodeList message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList} message AssociatedRegisteredNodeList
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        AssociatedRegisteredNodeList.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.arrays || options.defaults)
                                object.associatedRegisteredNode = [];
                            if (message.associatedRegisteredNode && message.associatedRegisteredNode.length) {
                                object.associatedRegisteredNode = $Array(message.associatedRegisteredNode.length);
                                for (let j = 0; j < message.associatedRegisteredNode.length; ++j)
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.associatedRegisteredNode[j] = typeof message.associatedRegisteredNode[j] === "number" ? $BigInt(message.associatedRegisteredNode[j]) : $util.Long.fromBits(message.associatedRegisteredNode[j].low >>> 0, message.associatedRegisteredNode[j].high >>> 0, true).toBigInt();
                                    else if (typeof message.associatedRegisteredNode[j] === "number")
                                        object.associatedRegisteredNode[j] = options.longs === $String ? $String(message.associatedRegisteredNode[j]) : message.associatedRegisteredNode[j];
                                    else
                                        object.associatedRegisteredNode[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.associatedRegisteredNode[j]) : options.longs === $Number ? new $util.LongBits(message.associatedRegisteredNode[j].low >>> 0, message.associatedRegisteredNode[j].high >>> 0).toNumber(true) : message.associatedRegisteredNode[j];
                            }
                            return object;
                        };

                        /**
                         * Converts this AssociatedRegisteredNodeList to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        AssociatedRegisteredNodeList.prototype.toJSON = function() {
                            return AssociatedRegisteredNodeList.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for AssociatedRegisteredNodeList
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        AssociatedRegisteredNodeList.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.addressbook.AssociatedRegisteredNodeList";
                        };

                        return AssociatedRegisteredNodeList;
                    })();

                    addressbook.NodeDeleteTransactionBody = (function() {

                        /**
                         * Properties of a NodeDeleteTransactionBody.
                         * @typedef {Object} com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties
                         * @property {number|Long|null} [nodeId] A consensus node identifier in the network state.
                         * <p>
                         * The node identified MUST exist in the network address book.<br/>
                         * The node identified MUST NOT be deleted.<br/>
                         * This value is REQUIRED.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a NodeDeleteTransactionBody.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @interface INodeDeleteTransactionBody
                         * @augments com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties
                         * @deprecated Use com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties instead.
                         */

                        /**
                         * Shape of a NodeDeleteTransactionBody.
                         * @typedef {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties} com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Shape
                         */

                        /**
                         * Constructs a new NodeDeleteTransactionBody.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @classdesc A transaction body to delete a node from the network address book.
                         * 
                         * - A `NodeDeleteTransactionBody` MUST be signed by one of those keys:
                         * adminKey, treasure account (2) key, systemAdmin(50) key, or
                         * addressBookAdmin(55) key.
                         * - Upon success, the address book entry SHALL enter a "pending delete"
                         * state.
                         * - All address book entries pending deletion SHALL be removed from the
                         * active network configuration during the next `freeze` transaction with
                         * the field `freeze_type` set to `PREPARE_UPGRADE`.<br/>
                         * - A deleted address book node SHALL be removed entirely from network state.
                         * - A deleted address book node identifier SHALL NOT be reused.
                         * 
                         * ### Block Stream Effects
                         * None.
                         * @constructor
                         * @param {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const NodeDeleteTransactionBody = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A consensus node identifier in the network state.
                         * <p>
                         * The node identified MUST exist in the network address book.<br/>
                         * The node identified MUST NOT be deleted.<br/>
                         * This value is REQUIRED.
                         * @member {number|Long} nodeId
                         * @memberof com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody
                         * @instance
                         */
                        NodeDeleteTransactionBody.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * Creates a new NodeDeleteTransactionBody instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody} NodeDeleteTransactionBody instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Shape): com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody & com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Shape;
                         *   (properties?: com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties): com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody;
                         * }}
                         */
                        NodeDeleteTransactionBody.create = function(properties) {
                            return new NodeDeleteTransactionBody(properties);
                        };

                        /**
                         * Encodes the specified NodeDeleteTransactionBody message. Does not implicitly {@link com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties} message NodeDeleteTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        NodeDeleteTransactionBody.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.nodeId);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified NodeDeleteTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties} message NodeDeleteTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        NodeDeleteTransactionBody.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a NodeDeleteTransactionBody message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody & com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Shape} NodeDeleteTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        NodeDeleteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.nodeId = value;
                                        else
                                            delete message.nodeId;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a NodeDeleteTransactionBody message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody & com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Shape} NodeDeleteTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        NodeDeleteTransactionBody.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a NodeDeleteTransactionBody message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        NodeDeleteTransactionBody.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                    return "nodeId: integer|Long expected";
                            return null;
                        };

                        /**
                         * Creates a NodeDeleteTransactionBody message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody} NodeDeleteTransactionBody
                         */
                        NodeDeleteTransactionBody.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody();
                            if (object.nodeId != null)
                                if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                    if ($util.Long)
                                        message.nodeId = $util.Long.fromValue(object.nodeId, true);
                                    else if (typeof object.nodeId === "string")
                                        message.nodeId = $parseInt(object.nodeId, 10);
                                    else if (typeof object.nodeId === "number")
                                        message.nodeId = object.nodeId;
                                    else if (typeof object.nodeId === "object")
                                        message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
                            return message;
                        };

                        /**
                         * Creates a plain object from a NodeDeleteTransactionBody message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody} message NodeDeleteTransactionBody
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        NodeDeleteTransactionBody.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults)
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                                else if (typeof message.nodeId === "number")
                                    object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                else
                                    object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
                            return object;
                        };

                        /**
                         * Converts this NodeDeleteTransactionBody to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        NodeDeleteTransactionBody.prototype.toJSON = function() {
                            return NodeDeleteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for NodeDeleteTransactionBody
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        NodeDeleteTransactionBody.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody";
                        };

                        return NodeDeleteTransactionBody;
                    })();

                    addressbook.RegisteredNodeCreateTransactionBody = (function() {

                        /**
                         * Properties of a RegisteredNodeCreateTransactionBody.
                         * @typedef {Object} com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Properties
                         * @property {proto.Key.$Properties|null} [adminKey] An administrative key controlled by the node operator.
                         * <p>
                         * This key MUST sign this transaction.<br/>
                         * This key MUST sign each transaction to update this node.<br/>
                         * This field MUST contain a valid `Key` value.<br/>
                         * This field is REQUIRED and MUST NOT be set to an empty `KeyList`.<br/>
                         * It is RECOMMENDED that this key be composed of one or more unique public
                         * keys that are not associated with any network account.<br/>
                         * This key MAY be a complex key containing `KeyList` or `ThresholdKey`
                         * elements, but SHOULD NOT be a contract ID key.
                         * @property {string|null} [description] A short description of the node.
                         * <p>
                         * This value, if set, MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
                         * This field is OPTIONAL.
                         * @property {Array.<com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties>|null} [serviceEndpoint] A list of service endpoints for client calls.
                         * <p>
                         * These endpoints SHALL represent the published endpoints to which
                         * clients may submit requests.<br/>
                         * Endpoints in this list MAY supply either IP address or FQDN, but MUST
                         * NOT supply both values for the same endpoint.<br/>
                         * Multiple endpoints in this list MAY resolve to the same interface.<br/>
                         * One Registered Node MAY expose endpoints for multiple service types.<br/>
                         * This list MUST NOT be empty.<br/>
                         * This list MUST NOT contain more than `50` entries.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a RegisteredNodeCreateTransactionBody.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @interface IRegisteredNodeCreateTransactionBody
                         * @augments com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Properties
                         * @deprecated Use com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Properties instead.
                         */

                        /**
                         * Shape of a RegisteredNodeCreateTransactionBody.
                         * @typedef {{
                         *   adminKey?: proto.Key.$Shape|null;
                         *   description?: string|null;
                         *   serviceEndpoint?: Array.<com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Shape>|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * }} com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Shape
                         */

                        /**
                         * Constructs a new RegisteredNodeCreateTransactionBody.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @classdesc A transaction body to create a new registered node in the network
                         * address book.
                         * 
                         * This transaction, once complete, SHALL add a new registered node to the
                         * network state.
                         * The new registered node SHALL be visible and discoverable upon
                         * completion of this transaction.
                         * 
                         * ### Block Stream Effects
                         * None.
                         * @constructor
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const RegisteredNodeCreateTransactionBody = function (properties) {
                            this.serviceEndpoint = [];
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * An administrative key controlled by the node operator.
                         * <p>
                         * This key MUST sign this transaction.<br/>
                         * This key MUST sign each transaction to update this node.<br/>
                         * This field MUST contain a valid `Key` value.<br/>
                         * This field is REQUIRED and MUST NOT be set to an empty `KeyList`.<br/>
                         * It is RECOMMENDED that this key be composed of one or more unique public
                         * keys that are not associated with any network account.<br/>
                         * This key MAY be a complex key containing `KeyList` or `ThresholdKey`
                         * elements, but SHOULD NOT be a contract ID key.
                         * @member {proto.Key.$Properties|null|undefined} adminKey
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @instance
                         */
                        RegisteredNodeCreateTransactionBody.prototype.adminKey = null;

                        /**
                         * A short description of the node.
                         * <p>
                         * This value, if set, MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
                         * This field is OPTIONAL.
                         * @member {string} description
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @instance
                         */
                        RegisteredNodeCreateTransactionBody.prototype.description = "";

                        /**
                         * A list of service endpoints for client calls.
                         * <p>
                         * These endpoints SHALL represent the published endpoints to which
                         * clients may submit requests.<br/>
                         * Endpoints in this list MAY supply either IP address or FQDN, but MUST
                         * NOT supply both values for the same endpoint.<br/>
                         * Multiple endpoints in this list MAY resolve to the same interface.<br/>
                         * One Registered Node MAY expose endpoints for multiple service types.<br/>
                         * This list MUST NOT be empty.<br/>
                         * This list MUST NOT contain more than `50` entries.
                         * @member {Array.<com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties>} serviceEndpoint
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @instance
                         */
                        RegisteredNodeCreateTransactionBody.prototype.serviceEndpoint = $util.emptyArray;

                        /**
                         * Creates a new RegisteredNodeCreateTransactionBody instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody} RegisteredNodeCreateTransactionBody instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Shape): com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody & com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Shape;
                         *   (properties?: com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Properties): com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody;
                         * }}
                         */
                        RegisteredNodeCreateTransactionBody.create = function(properties) {
                            return new RegisteredNodeCreateTransactionBody(properties);
                        };

                        /**
                         * Encodes the specified RegisteredNodeCreateTransactionBody message. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Properties} message RegisteredNodeCreateTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        RegisteredNodeCreateTransactionBody.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                            if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
                                writer.uint32(/* id 2, wireType 2 =*/18).string(message.description);
                            if (message.serviceEndpoint != null && message.serviceEndpoint.length)
                                for (let i = 0; i < message.serviceEndpoint.length; ++i)
                                    $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.encode(message.serviceEndpoint[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified RegisteredNodeCreateTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Properties} message RegisteredNodeCreateTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        RegisteredNodeCreateTransactionBody.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a RegisteredNodeCreateTransactionBody message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody & com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Shape} RegisteredNodeCreateTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        RegisteredNodeCreateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.stringVerify()).length)
                                            message.description = value;
                                        else
                                            delete message.description;
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.serviceEndpoint && message.serviceEndpoint.length))
                                            message.serviceEndpoint = [];
                                        message.serviceEndpoint.push($root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a RegisteredNodeCreateTransactionBody message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody & com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Shape} RegisteredNodeCreateTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        RegisteredNodeCreateTransactionBody.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a RegisteredNodeCreateTransactionBody message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        RegisteredNodeCreateTransactionBody.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                                if (error)
                                    return "adminKey." + error;
                            }
                            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                if (!$util.isString(message.description))
                                    return "description: string expected";
                            if (message.serviceEndpoint != null && $Object.hasOwnProperty.call(message, "serviceEndpoint")) {
                                if (!$Array.isArray(message.serviceEndpoint))
                                    return "serviceEndpoint: array expected";
                                for (let i = 0; i < message.serviceEndpoint.length; ++i) {
                                    let error = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.verify(message.serviceEndpoint[i], _depth + 1);
                                    if (error)
                                        return "serviceEndpoint." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates a RegisteredNodeCreateTransactionBody message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody} RegisteredNodeCreateTransactionBody
                         */
                        RegisteredNodeCreateTransactionBody.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody();
                            if (object.adminKey != null) {
                                if (!$util.isObject(object.adminKey))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.adminKey: object expected");
                                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
                            }
                            if (object.description != null)
                                if (typeof object.description !== "string" || object.description.length)
                                    message.description = $String(object.description);
                            if (object.serviceEndpoint) {
                                if (!$Array.isArray(object.serviceEndpoint))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.serviceEndpoint: array expected");
                                message.serviceEndpoint = $Array(object.serviceEndpoint.length);
                                for (let i = 0; i < object.serviceEndpoint.length; ++i) {
                                    if (!$util.isObject(object.serviceEndpoint[i]))
                                        throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.serviceEndpoint: object expected");
                                    message.serviceEndpoint[i] = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.fromObject(object.serviceEndpoint[i], _depth + 1);
                                }
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a RegisteredNodeCreateTransactionBody message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody} message RegisteredNodeCreateTransactionBody
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        RegisteredNodeCreateTransactionBody.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.arrays || options.defaults)
                                object.serviceEndpoint = [];
                            if (options.defaults) {
                                object.adminKey = null;
                                object.description = "";
                            }
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
                            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                object.description = message.description;
                            if (message.serviceEndpoint && message.serviceEndpoint.length) {
                                object.serviceEndpoint = $Array(message.serviceEndpoint.length);
                                for (let j = 0; j < message.serviceEndpoint.length; ++j)
                                    object.serviceEndpoint[j] = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.toObject(message.serviceEndpoint[j], options, _depth + 1);
                            }
                            return object;
                        };

                        /**
                         * Converts this RegisteredNodeCreateTransactionBody to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        RegisteredNodeCreateTransactionBody.prototype.toJSON = function() {
                            return RegisteredNodeCreateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for RegisteredNodeCreateTransactionBody
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        RegisteredNodeCreateTransactionBody.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody";
                        };

                        return RegisteredNodeCreateTransactionBody;
                    })();

                    addressbook.RegisteredServiceEndpoint = (function() {

                        /**
                         * Properties of a RegisteredServiceEndpoint.
                         * @typedef {Object} com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties
                         * @property {Uint8Array|null} [ipAddress] A 32-bit IPv4 address or 128-bit IPv6 address.<br/>
                         * This is the address of the endpoint, encoded in pure "big-endian"
                         * (i.e. left to right) order (e.g. IPv4 address `127.0.0.1` has
                         * hex bytes in the order `7F`, `00`, `00`, `01`.<br/>
                         * IPv6 address `::1` has hex bytes `00`, `00`, `00`, `00`, `00`, `00`,
                         * `00`, `00`, `00`, `00`, `00`, `00`, `00`, `00`, `00`, `01`).
                         * @property {string|null} [domainName] A node domain name.
                         * <p>
                         * This MUST be the fully qualified domain name of the node.<br/>
                         * This value MUST NOT exceed 250 ASCII characters.<br/>
                         * This value MUST meet all other DNS name requirements.
                         * @property {number|null} [port] A network port to use.
                         * <p>
                         * This value MUST be between 0 and 65535, inclusive.<br/>
                         * This value is REQUIRED.
                         * @property {boolean|null} [requiresTls] A flag indicating if this endpoint requires TLS.
                         * <p>
                         * If this value is set true, then connections to this endpoint MUST
                         * enable TLS.
                         * <p>
                         * TLS endpoints MAY use self-signed certificates for this purpose,
                         * but use of self-signed certificates SHOULD be limited to testing and
                         * development environments to ensure production environments meet all
                         * expected characteristics for transport layer security.
                         * @property {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Properties|null} [blockNode] A Block Node.<br/>
                         * A Block Node stores the block chain, provides content proof services,
                         * and delivers the block stream to other clients.
                         * @property {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Properties|null} [mirrorNode] A Mirror Node.<br/>
                         * A Mirror Node is an advanced indexing and query service that provides
                         * fast and flexible access to query the block chain and transaction
                         * history. A Mirror Node typically stores all recent blockchain data,
                         * and some store the entire history of the network.
                         * @property {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Properties|null} [rpcRelay] A RPC Relay.<br/>
                         * A RPC Relay is a proxy and translator between EVM tooling and a
                         * Hiero consensus network.
                         * @property {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Properties|null} [generalService] A general service.<br/>
                         * A general service endpoint represents any network accessible service
                         * that is provided by a registered node but that is not a service
                         * currently defined as part of the Hiero Ledger system.
                         * @property {"ipAddress"|"domainName"} [address] An IP address or fully qualified domain name.
                         * <p>
                         * This oneof is REQUIRED.
                         * @property {"blockNode"|"mirrorNode"|"rpcRelay"|"generalService"} [endpointType] An endpoint type.<br/>
                         * This declares the type of registered node endpoint and includes any
                         * type-specific fields.
                         * <p>
                         * This oneof is REQUIRED.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a RegisteredServiceEndpoint.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @interface IRegisteredServiceEndpoint
                         * @augments com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties
                         * @deprecated Use com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties instead.
                         */

                        /**
                         * Narrowed shape of a RegisteredServiceEndpoint.
                         * @typedef {{
                         *   ipAddress?: Uint8Array|null;
                         *   domainName?: string|null;
                         *   port?: number|null;
                         *   requiresTls?: boolean|null;
                         *   blockNode?: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Shape|null;
                         *   mirrorNode?: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Shape|null;
                         *   rpcRelay?: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Shape|null;
                         *   generalService?: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Shape|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * } & (
                         *   ({ address?: undefined; ipAddress?: null; domainName?: null }|{ address?: "ipAddress"; ipAddress: Uint8Array; domainName?: null }|{ address?: "domainName"; ipAddress?: null; domainName: string })
                         * ) & (
                         *   ({ endpointType?: undefined; blockNode?: null; mirrorNode?: null; rpcRelay?: null; generalService?: null }|{ endpointType?: "blockNode"; blockNode: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Shape; mirrorNode?: null; rpcRelay?: null; generalService?: null }|{ endpointType?: "mirrorNode"; blockNode?: null; mirrorNode: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Shape; rpcRelay?: null; generalService?: null }|{ endpointType?: "rpcRelay"; blockNode?: null; mirrorNode?: null; rpcRelay: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Shape; generalService?: null }|{ endpointType?: "generalService"; blockNode?: null; mirrorNode?: null; rpcRelay?: null; generalService: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Shape })
                         * )} com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Shape
                         */

                        /**
                         * Constructs a new RegisteredServiceEndpoint.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @classdesc A registered network node endpoint.<br/>
                         * Each registered network node in the global address book publishes one or
                         * more endpoints which enable the nodes to communicate both with other
                         * nodes and with clients to receive requests.
                         * 
                         * This message supports a TCP/UDP port and IP address (V4 or V6),
                         * or MAY include a FQDN _instead of_ an IP address.<br/>
                         * @constructor
                         * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const RegisteredServiceEndpoint = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A 32-bit IPv4 address or 128-bit IPv6 address.<br/>
                         * This is the address of the endpoint, encoded in pure "big-endian"
                         * (i.e. left to right) order (e.g. IPv4 address `127.0.0.1` has
                         * hex bytes in the order `7F`, `00`, `00`, `01`.<br/>
                         * IPv6 address `::1` has hex bytes `00`, `00`, `00`, `00`, `00`, `00`,
                         * `00`, `00`, `00`, `00`, `00`, `00`, `00`, `00`, `00`, `01`).
                         * @member {Uint8Array|null|undefined} ipAddress
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @instance
                         */
                        RegisteredServiceEndpoint.prototype.ipAddress = null;

                        /**
                         * A node domain name.
                         * <p>
                         * This MUST be the fully qualified domain name of the node.<br/>
                         * This value MUST NOT exceed 250 ASCII characters.<br/>
                         * This value MUST meet all other DNS name requirements.
                         * @member {string|null|undefined} domainName
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @instance
                         */
                        RegisteredServiceEndpoint.prototype.domainName = null;

                        /**
                         * A network port to use.
                         * <p>
                         * This value MUST be between 0 and 65535, inclusive.<br/>
                         * This value is REQUIRED.
                         * @member {number} port
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @instance
                         */
                        RegisteredServiceEndpoint.prototype.port = 0;

                        /**
                         * A flag indicating if this endpoint requires TLS.
                         * <p>
                         * If this value is set true, then connections to this endpoint MUST
                         * enable TLS.
                         * <p>
                         * TLS endpoints MAY use self-signed certificates for this purpose,
                         * but use of self-signed certificates SHOULD be limited to testing and
                         * development environments to ensure production environments meet all
                         * expected characteristics for transport layer security.
                         * @member {boolean} requiresTls
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @instance
                         */
                        RegisteredServiceEndpoint.prototype.requiresTls = false;

                        /**
                         * A Block Node.<br/>
                         * A Block Node stores the block chain, provides content proof services,
                         * and delivers the block stream to other clients.
                         * @member {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Properties|null|undefined} blockNode
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @instance
                         */
                        RegisteredServiceEndpoint.prototype.blockNode = null;

                        /**
                         * A Mirror Node.<br/>
                         * A Mirror Node is an advanced indexing and query service that provides
                         * fast and flexible access to query the block chain and transaction
                         * history. A Mirror Node typically stores all recent blockchain data,
                         * and some store the entire history of the network.
                         * @member {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Properties|null|undefined} mirrorNode
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @instance
                         */
                        RegisteredServiceEndpoint.prototype.mirrorNode = null;

                        /**
                         * A RPC Relay.<br/>
                         * A RPC Relay is a proxy and translator between EVM tooling and a
                         * Hiero consensus network.
                         * @member {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Properties|null|undefined} rpcRelay
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @instance
                         */
                        RegisteredServiceEndpoint.prototype.rpcRelay = null;

                        /**
                         * A general service.<br/>
                         * A general service endpoint represents any network accessible service
                         * that is provided by a registered node but that is not a service
                         * currently defined as part of the Hiero Ledger system.
                         * @member {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Properties|null|undefined} generalService
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @instance
                         */
                        RegisteredServiceEndpoint.prototype.generalService = null;

                        // OneOf field names bound to virtual getters and setters
                        let $oneOfFields;

                        /**
                         * An IP address or fully qualified domain name.
                         * <p>
                         * This oneof is REQUIRED.
                         * @member {"ipAddress"|"domainName"|undefined} address
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @instance
                         */
                        $Object.defineProperty(RegisteredServiceEndpoint.prototype, "address", {
                            get: $util.oneOfGetter($oneOfFields = ["ipAddress", "domainName"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * An endpoint type.<br/>
                         * This declares the type of registered node endpoint and includes any
                         * type-specific fields.
                         * <p>
                         * This oneof is REQUIRED.
                         * @member {"blockNode"|"mirrorNode"|"rpcRelay"|"generalService"|undefined} endpointType
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @instance
                         */
                        $Object.defineProperty(RegisteredServiceEndpoint.prototype, "endpointType", {
                            get: $util.oneOfGetter($oneOfFields = ["blockNode", "mirrorNode", "rpcRelay", "generalService"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * Creates a new RegisteredServiceEndpoint instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint} RegisteredServiceEndpoint instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Shape): com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Shape;
                         *   (properties?: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties): com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint;
                         * }}
                         */
                        RegisteredServiceEndpoint.create = function(properties) {
                            return new RegisteredServiceEndpoint(properties);
                        };

                        /**
                         * Encodes the specified RegisteredServiceEndpoint message. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties} message RegisteredServiceEndpoint message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        RegisteredServiceEndpoint.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.ipAddress != null && $Object.hasOwnProperty.call(message, "ipAddress"))
                                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ipAddress);
                            if (message.domainName != null && $Object.hasOwnProperty.call(message, "domainName"))
                                writer.uint32(/* id 2, wireType 2 =*/18).string(message.domainName);
                            if (message.port != null && $Object.hasOwnProperty.call(message, "port") && message.port !== 0)
                                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.port);
                            if (message.requiresTls != null && $Object.hasOwnProperty.call(message, "requiresTls") && message.requiresTls !== false)
                                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.requiresTls);
                            if (message.blockNode != null && $Object.hasOwnProperty.call(message, "blockNode"))
                                $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.encode(message.blockNode, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                            if (message.mirrorNode != null && $Object.hasOwnProperty.call(message, "mirrorNode"))
                                $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.encode(message.mirrorNode, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                            if (message.rpcRelay != null && $Object.hasOwnProperty.call(message, "rpcRelay"))
                                $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.encode(message.rpcRelay, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                            if (message.generalService != null && $Object.hasOwnProperty.call(message, "generalService"))
                                $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.encode(message.generalService, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified RegisteredServiceEndpoint message, length delimited. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties} message RegisteredServiceEndpoint message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        RegisteredServiceEndpoint.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a RegisteredServiceEndpoint message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Shape} RegisteredServiceEndpoint
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        RegisteredServiceEndpoint.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        message.ipAddress = reader.bytes();
                                        message.address = "ipAddress";
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        message.domainName = reader.stringVerify();
                                        message.address = "domainName";
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 0)
                                            break;
                                        if (value = reader.uint32())
                                            message.port = value;
                                        else
                                            delete message.port;
                                        continue;
                                    }
                                case 4: {
                                        if (wireType !== 0)
                                            break;
                                        if (value = reader.bool())
                                            message.requiresTls = value;
                                        else
                                            delete message.requiresTls;
                                        continue;
                                    }
                                case 5: {
                                        if (wireType !== 2)
                                            break;
                                        message.blockNode = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.blockNode);
                                        message.endpointType = "blockNode";
                                        continue;
                                    }
                                case 6: {
                                        if (wireType !== 2)
                                            break;
                                        message.mirrorNode = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.mirrorNode);
                                        message.endpointType = "mirrorNode";
                                        continue;
                                    }
                                case 7: {
                                        if (wireType !== 2)
                                            break;
                                        message.rpcRelay = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.rpcRelay);
                                        message.endpointType = "rpcRelay";
                                        continue;
                                    }
                                case 8: {
                                        if (wireType !== 2)
                                            break;
                                        message.generalService = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.generalService);
                                        message.endpointType = "generalService";
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a RegisteredServiceEndpoint message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Shape} RegisteredServiceEndpoint
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        RegisteredServiceEndpoint.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a RegisteredServiceEndpoint message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        RegisteredServiceEndpoint.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            let properties = {};
                            if (message.ipAddress != null && $Object.hasOwnProperty.call(message, "ipAddress")) {
                                properties.address = 1;
                                if (!(message.ipAddress && typeof message.ipAddress.length === "number" || $util.isString(message.ipAddress)))
                                    return "ipAddress: buffer expected";
                            }
                            if (message.domainName != null && $Object.hasOwnProperty.call(message, "domainName")) {
                                if (properties.address === 1)
                                    return "address: multiple values";
                                properties.address = 1;
                                if (!$util.isString(message.domainName))
                                    return "domainName: string expected";
                            }
                            if (message.port != null && $Object.hasOwnProperty.call(message, "port"))
                                if (!$util.isInteger(message.port))
                                    return "port: integer expected";
                            if (message.requiresTls != null && $Object.hasOwnProperty.call(message, "requiresTls"))
                                if (typeof message.requiresTls !== "boolean")
                                    return "requiresTls: boolean expected";
                            if (message.blockNode != null && $Object.hasOwnProperty.call(message, "blockNode")) {
                                properties.endpointType = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.verify(message.blockNode, _depth + 1);
                                    if (error)
                                        return "blockNode." + error;
                                }
                            }
                            if (message.mirrorNode != null && $Object.hasOwnProperty.call(message, "mirrorNode")) {
                                if (properties.endpointType === 1)
                                    return "endpointType: multiple values";
                                properties.endpointType = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.verify(message.mirrorNode, _depth + 1);
                                    if (error)
                                        return "mirrorNode." + error;
                                }
                            }
                            if (message.rpcRelay != null && $Object.hasOwnProperty.call(message, "rpcRelay")) {
                                if (properties.endpointType === 1)
                                    return "endpointType: multiple values";
                                properties.endpointType = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.verify(message.rpcRelay, _depth + 1);
                                    if (error)
                                        return "rpcRelay." + error;
                                }
                            }
                            if (message.generalService != null && $Object.hasOwnProperty.call(message, "generalService")) {
                                if (properties.endpointType === 1)
                                    return "endpointType: multiple values";
                                properties.endpointType = 1;
                                {
                                    let error = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.verify(message.generalService, _depth + 1);
                                    if (error)
                                        return "generalService." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates a RegisteredServiceEndpoint message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint} RegisteredServiceEndpoint
                         */
                        RegisteredServiceEndpoint.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint();
                            if (object.ipAddress != null)
                                if (typeof object.ipAddress === "string")
                                    $util.base64.decode(object.ipAddress, message.ipAddress = $util.newBuffer($util.base64.length(object.ipAddress)), 0);
                                else if (object.ipAddress.length >= 0)
                                    message.ipAddress = object.ipAddress;
                            if (object.domainName != null)
                                message.domainName = $String(object.domainName);
                            if (object.port != null)
                                if ($Number(object.port) !== 0)
                                    message.port = object.port >>> 0;
                            if (object.requiresTls != null)
                                if (object.requiresTls)
                                    message.requiresTls = $Boolean(object.requiresTls);
                            if (object.blockNode != null) {
                                if (!$util.isObject(object.blockNode))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.blockNode: object expected");
                                message.blockNode = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.fromObject(object.blockNode, _depth + 1);
                            }
                            if (object.mirrorNode != null) {
                                if (!$util.isObject(object.mirrorNode))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.mirrorNode: object expected");
                                message.mirrorNode = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.fromObject(object.mirrorNode, _depth + 1);
                            }
                            if (object.rpcRelay != null) {
                                if (!$util.isObject(object.rpcRelay))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.rpcRelay: object expected");
                                message.rpcRelay = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.fromObject(object.rpcRelay, _depth + 1);
                            }
                            if (object.generalService != null) {
                                if (!$util.isObject(object.generalService))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.generalService: object expected");
                                message.generalService = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.fromObject(object.generalService, _depth + 1);
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a RegisteredServiceEndpoint message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint} message RegisteredServiceEndpoint
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        RegisteredServiceEndpoint.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                object.port = 0;
                                object.requiresTls = false;
                            }
                            if (message.ipAddress != null && $Object.hasOwnProperty.call(message, "ipAddress")) {
                                object.ipAddress = options.bytes === $String ? $util.base64.encode(message.ipAddress, 0, message.ipAddress.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ipAddress) : message.ipAddress;
                                if (options.oneofs)
                                    object.address = "ipAddress";
                            }
                            if (message.domainName != null && $Object.hasOwnProperty.call(message, "domainName")) {
                                object.domainName = message.domainName;
                                if (options.oneofs)
                                    object.address = "domainName";
                            }
                            if (message.port != null && $Object.hasOwnProperty.call(message, "port"))
                                object.port = message.port;
                            if (message.requiresTls != null && $Object.hasOwnProperty.call(message, "requiresTls"))
                                object.requiresTls = message.requiresTls;
                            if (message.blockNode != null && $Object.hasOwnProperty.call(message, "blockNode")) {
                                object.blockNode = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.toObject(message.blockNode, options, _depth + 1);
                                if (options.oneofs)
                                    object.endpointType = "blockNode";
                            }
                            if (message.mirrorNode != null && $Object.hasOwnProperty.call(message, "mirrorNode")) {
                                object.mirrorNode = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.toObject(message.mirrorNode, options, _depth + 1);
                                if (options.oneofs)
                                    object.endpointType = "mirrorNode";
                            }
                            if (message.rpcRelay != null && $Object.hasOwnProperty.call(message, "rpcRelay")) {
                                object.rpcRelay = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.toObject(message.rpcRelay, options, _depth + 1);
                                if (options.oneofs)
                                    object.endpointType = "rpcRelay";
                            }
                            if (message.generalService != null && $Object.hasOwnProperty.call(message, "generalService")) {
                                object.generalService = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.toObject(message.generalService, options, _depth + 1);
                                if (options.oneofs)
                                    object.endpointType = "generalService";
                            }
                            return object;
                        };

                        /**
                         * Converts this RegisteredServiceEndpoint to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        RegisteredServiceEndpoint.prototype.toJSON = function() {
                            return RegisteredServiceEndpoint.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for RegisteredServiceEndpoint
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        RegisteredServiceEndpoint.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint";
                        };

                        RegisteredServiceEndpoint.BlockNodeEndpoint = (function() {

                            /**
                             * Properties of a BlockNodeEndpoint.
                             * @typedef {Object} com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Properties
                             * @property {Array.<com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.BlockNodeApi>|null} [endpointApi] An indicator of what API this endpoint supports.
                             * <p>
                             * This field is REQUIRED.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a BlockNodeEndpoint.
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                             * @interface IBlockNodeEndpoint
                             * @augments com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Properties
                             * @deprecated Use com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Properties instead.
                             */

                            /**
                             * Shape of a BlockNodeEndpoint.
                             * @typedef {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Properties} com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Shape
                             */

                            /**
                             * Constructs a new BlockNodeEndpoint.
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                             * @classdesc A message indicating this endpoint is a Block Node endpoint.<br/>
                             * Block Node endpoints offer one of several block node APIs, so this
                             * endpoint entry also declares which well-known Block Node API is
                             * supported by this endpoint, or "OTHER" for less common APIs.
                             * @constructor
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const BlockNodeEndpoint = function (properties) {
                                this.endpointApi = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * An indicator of what API this endpoint supports.
                             * <p>
                             * This field is REQUIRED.
                             * @member {Array.<com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.BlockNodeApi>} endpointApi
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint
                             * @instance
                             */
                            BlockNodeEndpoint.prototype.endpointApi = $util.emptyArray;

                            /**
                             * Creates a new BlockNodeEndpoint instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint} BlockNodeEndpoint instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Shape): com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Shape;
                             *   (properties?: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Properties): com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint;
                             * }}
                             */
                            BlockNodeEndpoint.create = function(properties) {
                                return new BlockNodeEndpoint(properties);
                            };

                            /**
                             * Encodes the specified BlockNodeEndpoint message. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Properties} message BlockNodeEndpoint message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            BlockNodeEndpoint.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.endpointApi != null && message.endpointApi.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).int32s(message.endpointApi);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified BlockNodeEndpoint message, length delimited. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Properties} message BlockNodeEndpoint message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            BlockNodeEndpoint.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a BlockNodeEndpoint message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Shape} BlockNodeEndpoint
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            BlockNodeEndpoint.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType === 2) {
                                                if (!(message.endpointApi && message.endpointApi.length))
                                                    message.endpointApi = [];
                                                reader.int32s(message.endpointApi);
                                                continue;
                                            }
                                            if (wireType !== 0)
                                                break;
                                            if (!(message.endpointApi && message.endpointApi.length))
                                                message.endpointApi = [];
                                            message.endpointApi.push(reader.int32());
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a BlockNodeEndpoint message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.$Shape} BlockNodeEndpoint
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            BlockNodeEndpoint.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a BlockNodeEndpoint message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            BlockNodeEndpoint.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.endpointApi != null && $Object.hasOwnProperty.call(message, "endpointApi")) {
                                    if (!$Array.isArray(message.endpointApi))
                                        return "endpointApi: array expected";
                                    for (let i = 0; i < message.endpointApi.length; ++i)
                                        if (typeof message.endpointApi[i] !== "number" || (message.endpointApi[i] | 0) !== message.endpointApi[i])
                                            return "endpointApi: enum value[] expected";
                                }
                                return null;
                            };

                            /**
                             * Creates a BlockNodeEndpoint message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint} BlockNodeEndpoint
                             */
                            BlockNodeEndpoint.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint();
                                if (object.endpointApi) {
                                    if (!$Array.isArray(object.endpointApi))
                                        throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.endpointApi: array expected");
                                    message.endpointApi = [];
                                    for (let i = 0; i < object.endpointApi.length; ++i)
                                        switch (object.endpointApi[i]) {
                                        case "OTHER":
                                        case 0:
                                            message.endpointApi[message.endpointApi.length] = 0;
                                            break;
                                        case "STATUS":
                                        case 1:
                                            message.endpointApi[message.endpointApi.length] = 1;
                                            break;
                                        case "PUBLISH":
                                        case 2:
                                            message.endpointApi[message.endpointApi.length] = 2;
                                            break;
                                        case "SUBSCRIBE_STREAM":
                                        case 3:
                                            message.endpointApi[message.endpointApi.length] = 3;
                                            break;
                                        case "STATE_PROOF":
                                        case 4:
                                            message.endpointApi[message.endpointApi.length] = 4;
                                            break;
                                        default:
                                            if (typeof object.endpointApi[i] === "number" && (object.endpointApi[i] | 0) === object.endpointApi[i])
                                                message.endpointApi[message.endpointApi.length] = object.endpointApi[i];
                                        }
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a BlockNodeEndpoint message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint} message BlockNodeEndpoint
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            BlockNodeEndpoint.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults)
                                    object.endpointApi = [];
                                if (message.endpointApi && message.endpointApi.length) {
                                    object.endpointApi = $Array(message.endpointApi.length);
                                    for (let j = 0; j < message.endpointApi.length; ++j)
                                        object.endpointApi[j] = options.enums === $String ? $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.BlockNodeApi[message.endpointApi[j]] === $undefined ? message.endpointApi[j] : $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.BlockNodeApi[message.endpointApi[j]] : message.endpointApi[j];
                                }
                                return object;
                            };

                            /**
                             * Converts this BlockNodeEndpoint to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            BlockNodeEndpoint.prototype.toJSON = function() {
                                return BlockNodeEndpoint.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for BlockNodeEndpoint
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            BlockNodeEndpoint.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint";
                            };

                            /**
                             * An enumeration of well-known block node endpoint APIs, and a
                             * catch-all option for otherwise unknown endpoint APIs.
                             * @name com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.BlockNodeApi
                             * @enum {number}
                             * @property {number} OTHER=0 Any other API type associated with a block node.<br/>
                             * The caller must consult local documentation to determine the
                             * correct call semantics.<br/>
                             * It is RECOMMENDED to call the detail status endpoint for further
                             * information before using this endpoint.
                             * @property {number} STATUS=1 The Block Node Status API.
                             * @property {number} PUBLISH=2 The Block Node Publish API.
                             * @property {number} SUBSCRIBE_STREAM=3 The Block Node Subscribe Stream API.
                             * @property {number} STATE_PROOF=4 The Block Node State Proof API.
                             */
                            BlockNodeEndpoint.BlockNodeApi = (function() {
                                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                                values[valuesById[0] = "OTHER"] = 0;
                                values[valuesById[1] = "STATUS"] = 1;
                                values[valuesById[2] = "PUBLISH"] = 2;
                                values[valuesById[3] = "SUBSCRIBE_STREAM"] = 3;
                                values[valuesById[4] = "STATE_PROOF"] = 4;
                                return values;
                            })();

                            return BlockNodeEndpoint;
                        })();

                        RegisteredServiceEndpoint.MirrorNodeEndpoint = (function() {

                            /**
                             * Properties of a MirrorNodeEndpoint.
                             * @typedef {Object} com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Properties
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a MirrorNodeEndpoint.
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                             * @interface IMirrorNodeEndpoint
                             * @augments com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Properties
                             * @deprecated Use com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Properties instead.
                             */

                            /**
                             * Shape of a MirrorNodeEndpoint.
                             * @typedef {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Properties} com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Shape
                             */

                            /**
                             * Constructs a new MirrorNodeEndpoint.
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                             * @classdesc A message indicating this endpoint is a Mirror Node endpoint.
                             * @constructor
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const MirrorNodeEndpoint = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * Creates a new MirrorNodeEndpoint instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint} MirrorNodeEndpoint instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Shape): com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Shape;
                             *   (properties?: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Properties): com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint;
                             * }}
                             */
                            MirrorNodeEndpoint.create = function(properties) {
                                return new MirrorNodeEndpoint(properties);
                            };

                            /**
                             * Encodes the specified MirrorNodeEndpoint message. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Properties} message MirrorNodeEndpoint message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            MirrorNodeEndpoint.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified MirrorNodeEndpoint message, length delimited. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Properties} message MirrorNodeEndpoint message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            MirrorNodeEndpoint.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a MirrorNodeEndpoint message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Shape} MirrorNodeEndpoint
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            MirrorNodeEndpoint.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    reader.skipType(tag & 7, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a MirrorNodeEndpoint message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint.$Shape} MirrorNodeEndpoint
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            MirrorNodeEndpoint.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a MirrorNodeEndpoint message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            MirrorNodeEndpoint.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                return null;
                            };

                            /**
                             * Creates a MirrorNodeEndpoint message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint} MirrorNodeEndpoint
                             */
                            MirrorNodeEndpoint.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                return new $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint();
                            };

                            /**
                             * Creates a plain object from a MirrorNodeEndpoint message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint} message MirrorNodeEndpoint
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            MirrorNodeEndpoint.toObject = function () {
                                return {};
                            };

                            /**
                             * Converts this MirrorNodeEndpoint to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            MirrorNodeEndpoint.prototype.toJSON = function() {
                                return MirrorNodeEndpoint.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for MirrorNodeEndpoint
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            MirrorNodeEndpoint.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.MirrorNodeEndpoint";
                            };

                            return MirrorNodeEndpoint;
                        })();

                        RegisteredServiceEndpoint.RpcRelayEndpoint = (function() {

                            /**
                             * Properties of a RpcRelayEndpoint.
                             * @typedef {Object} com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Properties
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a RpcRelayEndpoint.
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                             * @interface IRpcRelayEndpoint
                             * @augments com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Properties
                             * @deprecated Use com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Properties instead.
                             */

                            /**
                             * Shape of a RpcRelayEndpoint.
                             * @typedef {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Properties} com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Shape
                             */

                            /**
                             * Constructs a new RpcRelayEndpoint.
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                             * @classdesc A message indicating this endpoint is a RPC Relay endpoint.
                             * @constructor
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const RpcRelayEndpoint = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * Creates a new RpcRelayEndpoint instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint} RpcRelayEndpoint instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Shape): com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Shape;
                             *   (properties?: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Properties): com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint;
                             * }}
                             */
                            RpcRelayEndpoint.create = function(properties) {
                                return new RpcRelayEndpoint(properties);
                            };

                            /**
                             * Encodes the specified RpcRelayEndpoint message. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Properties} message RpcRelayEndpoint message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RpcRelayEndpoint.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified RpcRelayEndpoint message, length delimited. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Properties} message RpcRelayEndpoint message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RpcRelayEndpoint.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a RpcRelayEndpoint message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Shape} RpcRelayEndpoint
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RpcRelayEndpoint.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    reader.skipType(tag & 7, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a RpcRelayEndpoint message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint.$Shape} RpcRelayEndpoint
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RpcRelayEndpoint.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a RpcRelayEndpoint message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            RpcRelayEndpoint.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                return null;
                            };

                            /**
                             * Creates a RpcRelayEndpoint message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint} RpcRelayEndpoint
                             */
                            RpcRelayEndpoint.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                return new $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint();
                            };

                            /**
                             * Creates a plain object from a RpcRelayEndpoint message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint} message RpcRelayEndpoint
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            RpcRelayEndpoint.toObject = function () {
                                return {};
                            };

                            /**
                             * Converts this RpcRelayEndpoint to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            RpcRelayEndpoint.prototype.toJSON = function() {
                                return RpcRelayEndpoint.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for RpcRelayEndpoint
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            RpcRelayEndpoint.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.RpcRelayEndpoint";
                            };

                            return RpcRelayEndpoint;
                        })();

                        RegisteredServiceEndpoint.GeneralServiceEndpoint = (function() {

                            /**
                             * Properties of a GeneralServiceEndpoint.
                             * @typedef {Object} com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Properties
                             * @property {string|null} [description] A short description of the service provided.
                             * <p>
                             * This value, if set, MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
                             * This field is OPTIONAL.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a GeneralServiceEndpoint.
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                             * @interface IGeneralServiceEndpoint
                             * @augments com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Properties
                             * @deprecated Use com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Properties instead.
                             */

                            /**
                             * Shape of a GeneralServiceEndpoint.
                             * @typedef {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Properties} com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Shape
                             */

                            /**
                             * Constructs a new GeneralServiceEndpoint.
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint
                             * @classdesc A message indicating this endpoint is a General Service endpoint.
                             * @constructor
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const GeneralServiceEndpoint = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * A short description of the service provided.
                             * <p>
                             * This value, if set, MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
                             * This field is OPTIONAL.
                             * @member {string} description
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint
                             * @instance
                             */
                            GeneralServiceEndpoint.prototype.description = "";

                            /**
                             * Creates a new GeneralServiceEndpoint instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint} GeneralServiceEndpoint instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Shape): com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Shape;
                             *   (properties?: com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Properties): com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint;
                             * }}
                             */
                            GeneralServiceEndpoint.create = function(properties) {
                                return new GeneralServiceEndpoint(properties);
                            };

                            /**
                             * Encodes the specified GeneralServiceEndpoint message. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Properties} message GeneralServiceEndpoint message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            GeneralServiceEndpoint.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
                                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.description);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified GeneralServiceEndpoint message, length delimited. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Properties} message GeneralServiceEndpoint message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            GeneralServiceEndpoint.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a GeneralServiceEndpoint message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Shape} GeneralServiceEndpoint
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            GeneralServiceEndpoint.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.stringVerify()).length)
                                                message.description = value;
                                            else
                                                delete message.description;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a GeneralServiceEndpoint message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint & com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint.$Shape} GeneralServiceEndpoint
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            GeneralServiceEndpoint.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a GeneralServiceEndpoint message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            GeneralServiceEndpoint.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                    if (!$util.isString(message.description))
                                        return "description: string expected";
                                return null;
                            };

                            /**
                             * Creates a GeneralServiceEndpoint message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint} GeneralServiceEndpoint
                             */
                            GeneralServiceEndpoint.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint();
                                if (object.description != null)
                                    if (typeof object.description !== "string" || object.description.length)
                                        message.description = $String(object.description);
                                return message;
                            };

                            /**
                             * Creates a plain object from a GeneralServiceEndpoint message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint
                             * @static
                             * @param {com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint} message GeneralServiceEndpoint
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            GeneralServiceEndpoint.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults)
                                    object.description = "";
                                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                    object.description = message.description;
                                return object;
                            };

                            /**
                             * Converts this GeneralServiceEndpoint to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            GeneralServiceEndpoint.prototype.toJSON = function() {
                                return GeneralServiceEndpoint.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for GeneralServiceEndpoint
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            GeneralServiceEndpoint.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.GeneralServiceEndpoint";
                            };

                            return GeneralServiceEndpoint;
                        })();

                        return RegisteredServiceEndpoint;
                    })();

                    addressbook.RegisteredNodeUpdateTransactionBody = (function() {

                        /**
                         * Properties of a RegisteredNodeUpdateTransactionBody.
                         * @typedef {Object} com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Properties
                         * @property {number|Long|null} [registeredNodeId] A registered node identifier in the network state.
                         * <p>
                         * The node identified MUST exist in the registered address book.<br/>
                         * The node identified MUST NOT be deleted.<br/>
                         * This value is REQUIRED.
                         * <p>
                         * A given value for `registered_node_id` SHALL be unique within a given
                         * shard or realm.<br/>
                         * A given value for `registered_node_id` SHALL NOT be reused, even if the
                         * corresponding entry is deleted.
                         * @property {proto.Key.$Properties|null} [adminKey] An administrative key controlled by the node operator.
                         * <p>
                         * This field is OPTIONAL.<br/>
                         * If set, this key MUST sign this transaction.<br/>
                         * If set, this key MUST sign each subsequent transaction to
                         * update this node.<br/>
                         * If set, this field MUST contain a valid `Key` value.<br/>
                         * If set, this field MUST NOT be set to an empty `KeyList`.<br/>
                         * It is RECOMMENDED that this key be composed of one or more unique public
                         * keys that are not associated with any network account.<br/>
                         * This key MAY be a complex key containing `KeyList` or `ThresholdKey`
                         * elements, but SHOULD NOT be a contract ID key.
                         * @property {google.protobuf.StringValue.$Properties|null} [description] A short description of the node.
                         * <p>
                         * This field is OPTIONAL.<br/>
                         * This value, if set, MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
                         * If set, this value SHALL replace the previous value.
                         * @property {Array.<com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties>|null} [serviceEndpoint] A list of service endpoints for client calls.
                         * <p>
                         * This field is OPTIONAL.<br/>
                         * These endpoints SHALL represent the published endpoints to which
                         * clients may submit requests.<br/>
                         * Endpoints in this list MAY supply either IP address or FQDN, but MUST
                         * NOT supply both values for the same endpoint.<br/>
                         * One Registered Node MAY expose endpoints for multiple service types.<br/>
                         * If set, this list MUST NOT be empty.<br/>
                         * If set, this list MUST NOT contain more than `50` entries.<br/>
                         * If set, this list SHALL _replace_ the previous list.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a RegisteredNodeUpdateTransactionBody.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @interface IRegisteredNodeUpdateTransactionBody
                         * @augments com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Properties
                         * @deprecated Use com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Properties instead.
                         */

                        /**
                         * Shape of a RegisteredNodeUpdateTransactionBody.
                         * @typedef {{
                         *   registeredNodeId?: number|Long|null;
                         *   adminKey?: proto.Key.$Shape|null;
                         *   description?: google.protobuf.StringValue.$Shape|null;
                         *   serviceEndpoint?: Array.<com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Shape>|null;
                         *   $unknowns?: Array.<Uint8Array>;
                         * }} com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Shape
                         */

                        /**
                         * Constructs a new RegisteredNodeUpdateTransactionBody.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @classdesc A transaction body to update an existing registered node in the network
                         * address book.
                         * 
                         * This transaction, once complete, SHALL modify the identified registered
                         * node state as requested.
                         * 
                         * ### Block Stream Effects
                         * None.
                         * @constructor
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const RegisteredNodeUpdateTransactionBody = function (properties) {
                            this.serviceEndpoint = [];
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A registered node identifier in the network state.
                         * <p>
                         * The node identified MUST exist in the registered address book.<br/>
                         * The node identified MUST NOT be deleted.<br/>
                         * This value is REQUIRED.
                         * <p>
                         * A given value for `registered_node_id` SHALL be unique within a given
                         * shard or realm.<br/>
                         * A given value for `registered_node_id` SHALL NOT be reused, even if the
                         * corresponding entry is deleted.
                         * @member {number|Long} registeredNodeId
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @instance
                         */
                        RegisteredNodeUpdateTransactionBody.prototype.registeredNodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * An administrative key controlled by the node operator.
                         * <p>
                         * This field is OPTIONAL.<br/>
                         * If set, this key MUST sign this transaction.<br/>
                         * If set, this key MUST sign each subsequent transaction to
                         * update this node.<br/>
                         * If set, this field MUST contain a valid `Key` value.<br/>
                         * If set, this field MUST NOT be set to an empty `KeyList`.<br/>
                         * It is RECOMMENDED that this key be composed of one or more unique public
                         * keys that are not associated with any network account.<br/>
                         * This key MAY be a complex key containing `KeyList` or `ThresholdKey`
                         * elements, but SHOULD NOT be a contract ID key.
                         * @member {proto.Key.$Properties|null|undefined} adminKey
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @instance
                         */
                        RegisteredNodeUpdateTransactionBody.prototype.adminKey = null;

                        /**
                         * A short description of the node.
                         * <p>
                         * This field is OPTIONAL.<br/>
                         * This value, if set, MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
                         * If set, this value SHALL replace the previous value.
                         * @member {google.protobuf.StringValue.$Properties|null|undefined} description
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @instance
                         */
                        RegisteredNodeUpdateTransactionBody.prototype.description = null;

                        /**
                         * A list of service endpoints for client calls.
                         * <p>
                         * This field is OPTIONAL.<br/>
                         * These endpoints SHALL represent the published endpoints to which
                         * clients may submit requests.<br/>
                         * Endpoints in this list MAY supply either IP address or FQDN, but MUST
                         * NOT supply both values for the same endpoint.<br/>
                         * One Registered Node MAY expose endpoints for multiple service types.<br/>
                         * If set, this list MUST NOT be empty.<br/>
                         * If set, this list MUST NOT contain more than `50` entries.<br/>
                         * If set, this list SHALL _replace_ the previous list.
                         * @member {Array.<com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties>} serviceEndpoint
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @instance
                         */
                        RegisteredNodeUpdateTransactionBody.prototype.serviceEndpoint = $util.emptyArray;

                        /**
                         * Creates a new RegisteredNodeUpdateTransactionBody instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody} RegisteredNodeUpdateTransactionBody instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Shape): com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody & com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Shape;
                         *   (properties?: com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Properties): com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody;
                         * }}
                         */
                        RegisteredNodeUpdateTransactionBody.create = function(properties) {
                            return new RegisteredNodeUpdateTransactionBody(properties);
                        };

                        /**
                         * Encodes the specified RegisteredNodeUpdateTransactionBody message. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Properties} message RegisteredNodeUpdateTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        RegisteredNodeUpdateTransactionBody.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.registeredNodeId != null && $Object.hasOwnProperty.call(message, "registeredNodeId") && (typeof message.registeredNodeId === "object" ? message.registeredNodeId.low || message.registeredNodeId.high : message.registeredNodeId !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.registeredNodeId);
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                $root.google.protobuf.StringValue.encode(message.description, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.serviceEndpoint != null && message.serviceEndpoint.length)
                                for (let i = 0; i < message.serviceEndpoint.length; ++i)
                                    $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.encode(message.serviceEndpoint[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified RegisteredNodeUpdateTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Properties} message RegisteredNodeUpdateTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        RegisteredNodeUpdateTransactionBody.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a RegisteredNodeUpdateTransactionBody message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody & com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Shape} RegisteredNodeUpdateTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        RegisteredNodeUpdateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.registeredNodeId = value;
                                        else
                                            delete message.registeredNodeId;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        message.description = $root.google.protobuf.StringValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.description);
                                        continue;
                                    }
                                case 4: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.serviceEndpoint && message.serviceEndpoint.length))
                                            message.serviceEndpoint = [];
                                        message.serviceEndpoint.push($root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a RegisteredNodeUpdateTransactionBody message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody & com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Shape} RegisteredNodeUpdateTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        RegisteredNodeUpdateTransactionBody.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a RegisteredNodeUpdateTransactionBody message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        RegisteredNodeUpdateTransactionBody.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.registeredNodeId != null && $Object.hasOwnProperty.call(message, "registeredNodeId"))
                                if (!$util.isInteger(message.registeredNodeId) && !(message.registeredNodeId && $util.isInteger(message.registeredNodeId.low) && $util.isInteger(message.registeredNodeId.high)))
                                    return "registeredNodeId: integer|Long expected";
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                                if (error)
                                    return "adminKey." + error;
                            }
                            if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
                                let error = $root.google.protobuf.StringValue.verify(message.description, _depth + 1);
                                if (error)
                                    return "description." + error;
                            }
                            if (message.serviceEndpoint != null && $Object.hasOwnProperty.call(message, "serviceEndpoint")) {
                                if (!$Array.isArray(message.serviceEndpoint))
                                    return "serviceEndpoint: array expected";
                                for (let i = 0; i < message.serviceEndpoint.length; ++i) {
                                    let error = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.verify(message.serviceEndpoint[i], _depth + 1);
                                    if (error)
                                        return "serviceEndpoint." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates a RegisteredNodeUpdateTransactionBody message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody} RegisteredNodeUpdateTransactionBody
                         */
                        RegisteredNodeUpdateTransactionBody.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody();
                            if (object.registeredNodeId != null)
                                if (typeof object.registeredNodeId === "object" ? object.registeredNodeId.low || object.registeredNodeId.high : $Number(object.registeredNodeId) !== 0)
                                    if ($util.Long)
                                        message.registeredNodeId = $util.Long.fromValue(object.registeredNodeId, true);
                                    else if (typeof object.registeredNodeId === "string")
                                        message.registeredNodeId = $parseInt(object.registeredNodeId, 10);
                                    else if (typeof object.registeredNodeId === "number")
                                        message.registeredNodeId = object.registeredNodeId;
                                    else if (typeof object.registeredNodeId === "object")
                                        message.registeredNodeId = new $util.LongBits(object.registeredNodeId.low >>> 0, object.registeredNodeId.high >>> 0).toNumber(true);
                            if (object.adminKey != null) {
                                if (!$util.isObject(object.adminKey))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.adminKey: object expected");
                                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
                            }
                            if (object.description != null) {
                                if (!$util.isObject(object.description))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.description: object expected");
                                message.description = $root.google.protobuf.StringValue.fromObject(object.description, _depth + 1);
                            }
                            if (object.serviceEndpoint) {
                                if (!$Array.isArray(object.serviceEndpoint))
                                    throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.serviceEndpoint: array expected");
                                message.serviceEndpoint = $Array(object.serviceEndpoint.length);
                                for (let i = 0; i < object.serviceEndpoint.length; ++i) {
                                    if (!$util.isObject(object.serviceEndpoint[i]))
                                        throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.serviceEndpoint: object expected");
                                    message.serviceEndpoint[i] = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.fromObject(object.serviceEndpoint[i], _depth + 1);
                                }
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a RegisteredNodeUpdateTransactionBody message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody} message RegisteredNodeUpdateTransactionBody
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        RegisteredNodeUpdateTransactionBody.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.arrays || options.defaults)
                                object.serviceEndpoint = [];
                            if (options.defaults) {
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.registeredNodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.registeredNodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                object.adminKey = null;
                                object.description = null;
                            }
                            if (message.registeredNodeId != null && $Object.hasOwnProperty.call(message, "registeredNodeId"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.registeredNodeId = typeof message.registeredNodeId === "number" ? $BigInt(message.registeredNodeId) : $util.Long.fromBits(message.registeredNodeId.low >>> 0, message.registeredNodeId.high >>> 0, true).toBigInt();
                                else if (typeof message.registeredNodeId === "number")
                                    object.registeredNodeId = options.longs === $String ? $String(message.registeredNodeId) : message.registeredNodeId;
                                else
                                    object.registeredNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.registeredNodeId) : options.longs === $Number ? new $util.LongBits(message.registeredNodeId.low >>> 0, message.registeredNodeId.high >>> 0).toNumber(true) : message.registeredNodeId;
                            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
                            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                object.description = $root.google.protobuf.StringValue.toObject(message.description, options, _depth + 1);
                            if (message.serviceEndpoint && message.serviceEndpoint.length) {
                                object.serviceEndpoint = $Array(message.serviceEndpoint.length);
                                for (let j = 0; j < message.serviceEndpoint.length; ++j)
                                    object.serviceEndpoint[j] = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.toObject(message.serviceEndpoint[j], options, _depth + 1);
                            }
                            return object;
                        };

                        /**
                         * Converts this RegisteredNodeUpdateTransactionBody to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        RegisteredNodeUpdateTransactionBody.prototype.toJSON = function() {
                            return RegisteredNodeUpdateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for RegisteredNodeUpdateTransactionBody
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        RegisteredNodeUpdateTransactionBody.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody";
                        };

                        return RegisteredNodeUpdateTransactionBody;
                    })();

                    addressbook.RegisteredNodeDeleteTransactionBody = (function() {

                        /**
                         * Properties of a RegisteredNodeDeleteTransactionBody.
                         * @typedef {Object} com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties
                         * @property {number|Long|null} [registeredNodeId] A registered node identifier in the network state.
                         * <p>
                         * The node identified MUST exist in the registered address book.<br/>
                         * The node identified MUST NOT be deleted.<br/>
                         * This value is REQUIRED.
                         * <p>
                         * A given value for `registered_node_id` SHALL be unique within a given
                         * shard or realm.<br/>
                         * A given value for `registered_node_id` SHALL NOT be reused, even if the
                         * corresponding entry is deleted.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a RegisteredNodeDeleteTransactionBody.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @interface IRegisteredNodeDeleteTransactionBody
                         * @augments com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties
                         * @deprecated Use com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties instead.
                         */

                        /**
                         * Shape of a RegisteredNodeDeleteTransactionBody.
                         * @typedef {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties} com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Shape
                         */

                        /**
                         * Constructs a new RegisteredNodeDeleteTransactionBody.
                         * @memberof com.hedera.hapi.node.addressbook
                         * @classdesc A transaction body to delete a registered node from the network
                         * address book.
                         * 
                         * This transaction, once complete, SHALL remove the identified registered
                         * node from the network state.
                         * This transaction MUST be signed by the existing entry `admin_key` or
                         * authorized by the Hiero network governance structure.
                         * 
                         * ### Block Stream Effects
                         * None.
                         * @constructor
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const RegisteredNodeDeleteTransactionBody = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * A registered node identifier in the network state.
                         * <p>
                         * The node identified MUST exist in the registered address book.<br/>
                         * The node identified MUST NOT be deleted.<br/>
                         * This value is REQUIRED.
                         * <p>
                         * A given value for `registered_node_id` SHALL be unique within a given
                         * shard or realm.<br/>
                         * A given value for `registered_node_id` SHALL NOT be reused, even if the
                         * corresponding entry is deleted.
                         * @member {number|Long} registeredNodeId
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody
                         * @instance
                         */
                        RegisteredNodeDeleteTransactionBody.prototype.registeredNodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * Creates a new RegisteredNodeDeleteTransactionBody instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody} RegisteredNodeDeleteTransactionBody instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Shape): com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody & com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Shape;
                         *   (properties?: com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties): com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody;
                         * }}
                         */
                        RegisteredNodeDeleteTransactionBody.create = function(properties) {
                            return new RegisteredNodeDeleteTransactionBody(properties);
                        };

                        /**
                         * Encodes the specified RegisteredNodeDeleteTransactionBody message. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties} message RegisteredNodeDeleteTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        RegisteredNodeDeleteTransactionBody.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.registeredNodeId != null && $Object.hasOwnProperty.call(message, "registeredNodeId") && (typeof message.registeredNodeId === "object" ? message.registeredNodeId.low || message.registeredNodeId.high : message.registeredNodeId !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.registeredNodeId);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified RegisteredNodeDeleteTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties} message RegisteredNodeDeleteTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        RegisteredNodeDeleteTransactionBody.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a RegisteredNodeDeleteTransactionBody message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody & com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Shape} RegisteredNodeDeleteTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        RegisteredNodeDeleteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.registeredNodeId = value;
                                        else
                                            delete message.registeredNodeId;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a RegisteredNodeDeleteTransactionBody message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody & com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Shape} RegisteredNodeDeleteTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        RegisteredNodeDeleteTransactionBody.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a RegisteredNodeDeleteTransactionBody message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        RegisteredNodeDeleteTransactionBody.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.registeredNodeId != null && $Object.hasOwnProperty.call(message, "registeredNodeId"))
                                if (!$util.isInteger(message.registeredNodeId) && !(message.registeredNodeId && $util.isInteger(message.registeredNodeId.low) && $util.isInteger(message.registeredNodeId.high)))
                                    return "registeredNodeId: integer|Long expected";
                            return null;
                        };

                        /**
                         * Creates a RegisteredNodeDeleteTransactionBody message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody} RegisteredNodeDeleteTransactionBody
                         */
                        RegisteredNodeDeleteTransactionBody.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody();
                            if (object.registeredNodeId != null)
                                if (typeof object.registeredNodeId === "object" ? object.registeredNodeId.low || object.registeredNodeId.high : $Number(object.registeredNodeId) !== 0)
                                    if ($util.Long)
                                        message.registeredNodeId = $util.Long.fromValue(object.registeredNodeId, true);
                                    else if (typeof object.registeredNodeId === "string")
                                        message.registeredNodeId = $parseInt(object.registeredNodeId, 10);
                                    else if (typeof object.registeredNodeId === "number")
                                        message.registeredNodeId = object.registeredNodeId;
                                    else if (typeof object.registeredNodeId === "object")
                                        message.registeredNodeId = new $util.LongBits(object.registeredNodeId.low >>> 0, object.registeredNodeId.high >>> 0).toNumber(true);
                            return message;
                        };

                        /**
                         * Creates a plain object from a RegisteredNodeDeleteTransactionBody message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody} message RegisteredNodeDeleteTransactionBody
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        RegisteredNodeDeleteTransactionBody.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults)
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.registeredNodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.registeredNodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                            if (message.registeredNodeId != null && $Object.hasOwnProperty.call(message, "registeredNodeId"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.registeredNodeId = typeof message.registeredNodeId === "number" ? $BigInt(message.registeredNodeId) : $util.Long.fromBits(message.registeredNodeId.low >>> 0, message.registeredNodeId.high >>> 0, true).toBigInt();
                                else if (typeof message.registeredNodeId === "number")
                                    object.registeredNodeId = options.longs === $String ? $String(message.registeredNodeId) : message.registeredNodeId;
                                else
                                    object.registeredNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.registeredNodeId) : options.longs === $Number ? new $util.LongBits(message.registeredNodeId.low >>> 0, message.registeredNodeId.high >>> 0).toNumber(true) : message.registeredNodeId;
                            return object;
                        };

                        /**
                         * Converts this RegisteredNodeDeleteTransactionBody to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        RegisteredNodeDeleteTransactionBody.prototype.toJSON = function() {
                            return RegisteredNodeDeleteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for RegisteredNodeDeleteTransactionBody
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        RegisteredNodeDeleteTransactionBody.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody";
                        };

                        return RegisteredNodeDeleteTransactionBody;
                    })();

                    return addressbook;
                })();

                node.state = (function() {

                    /**
                     * Namespace state.
                     * @memberof com.hedera.hapi.node
                     * @namespace
                     */
                    const state = {};

                    state.hints = (function() {

                        /**
                         * Namespace hints.
                         * @memberof com.hedera.hapi.node.state
                         * @namespace
                         */
                        const hints = {};

                        hints.HintsPartyId = (function() {

                            /**
                             * Properties of a HintsPartyId.
                             * @typedef {Object} com.hedera.hapi.node.state.hints.HintsPartyId.$Properties
                             * @property {number|null} [partyId] The party id, in the range [0, num_parties).
                             * @property {number|null} [numParties] The number of parties in the hinTS scheme.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HintsPartyId.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @interface IHintsPartyId
                             * @augments com.hedera.hapi.node.state.hints.HintsPartyId.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.hints.HintsPartyId.$Properties instead.
                             */

                            /**
                             * Shape of a HintsPartyId.
                             * @typedef {com.hedera.hapi.node.state.hints.HintsPartyId.$Properties} com.hedera.hapi.node.state.hints.HintsPartyId.$Shape
                             */

                            /**
                             * Constructs a new HintsPartyId.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @classdesc The id of a party in a hinTS scheme with a certain
                             * number of parties.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.hints.HintsPartyId.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HintsPartyId = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The party id, in the range [0, num_parties).
                             * @member {number} partyId
                             * @memberof com.hedera.hapi.node.state.hints.HintsPartyId
                             * @instance
                             */
                            HintsPartyId.prototype.partyId = 0;

                            /**
                             * The number of parties in the hinTS scheme.
                             * @member {number} numParties
                             * @memberof com.hedera.hapi.node.state.hints.HintsPartyId
                             * @instance
                             */
                            HintsPartyId.prototype.numParties = 0;

                            /**
                             * Creates a new HintsPartyId instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.hints.HintsPartyId
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsPartyId.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.hints.HintsPartyId} HintsPartyId instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.hints.HintsPartyId.$Shape): com.hedera.hapi.node.state.hints.HintsPartyId & com.hedera.hapi.node.state.hints.HintsPartyId.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.hints.HintsPartyId.$Properties): com.hedera.hapi.node.state.hints.HintsPartyId;
                             * }}
                             */
                            HintsPartyId.create = function(properties) {
                                return new HintsPartyId(properties);
                            };

                            /**
                             * Encodes the specified HintsPartyId message. Does not implicitly {@link com.hedera.hapi.node.state.hints.HintsPartyId.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.hints.HintsPartyId
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsPartyId.$Properties} message HintsPartyId message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsPartyId.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.partyId != null && $Object.hasOwnProperty.call(message, "partyId") && message.partyId !== 0)
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.partyId);
                                if (message.numParties != null && $Object.hasOwnProperty.call(message, "numParties") && message.numParties !== 0)
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.numParties);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HintsPartyId message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.hints.HintsPartyId.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.HintsPartyId
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsPartyId.$Properties} message HintsPartyId message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsPartyId.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HintsPartyId message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.hints.HintsPartyId
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.hints.HintsPartyId & com.hedera.hapi.node.state.hints.HintsPartyId.$Shape} HintsPartyId
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsPartyId.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.hints.HintsPartyId();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.uint32())
                                                message.partyId = value;
                                            else
                                                delete message.partyId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.uint32())
                                                message.numParties = value;
                                            else
                                                delete message.numParties;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HintsPartyId message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.HintsPartyId
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.hints.HintsPartyId & com.hedera.hapi.node.state.hints.HintsPartyId.$Shape} HintsPartyId
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsPartyId.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HintsPartyId message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.hints.HintsPartyId
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HintsPartyId.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.partyId != null && $Object.hasOwnProperty.call(message, "partyId"))
                                    if (!$util.isInteger(message.partyId))
                                        return "partyId: integer expected";
                                if (message.numParties != null && $Object.hasOwnProperty.call(message, "numParties"))
                                    if (!$util.isInteger(message.numParties))
                                        return "numParties: integer expected";
                                return null;
                            };

                            /**
                             * Creates a HintsPartyId message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.hints.HintsPartyId
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.hints.HintsPartyId} HintsPartyId
                             */
                            HintsPartyId.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.hints.HintsPartyId)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.hints.HintsPartyId: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.hints.HintsPartyId();
                                if (object.partyId != null)
                                    if ($Number(object.partyId) !== 0)
                                        message.partyId = object.partyId >>> 0;
                                if (object.numParties != null)
                                    if ($Number(object.numParties) !== 0)
                                        message.numParties = object.numParties >>> 0;
                                return message;
                            };

                            /**
                             * Creates a plain object from a HintsPartyId message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.hints.HintsPartyId
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsPartyId} message HintsPartyId
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HintsPartyId.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    object.partyId = 0;
                                    object.numParties = 0;
                                }
                                if (message.partyId != null && $Object.hasOwnProperty.call(message, "partyId"))
                                    object.partyId = message.partyId;
                                if (message.numParties != null && $Object.hasOwnProperty.call(message, "numParties"))
                                    object.numParties = message.numParties;
                                return object;
                            };

                            /**
                             * Converts this HintsPartyId to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.hints.HintsPartyId
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HintsPartyId.prototype.toJSON = function() {
                                return HintsPartyId.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HintsPartyId
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.hints.HintsPartyId
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HintsPartyId.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.hints.HintsPartyId";
                            };

                            return HintsPartyId;
                        })();

                        hints.HintsKeySet = (function() {

                            /**
                             * Properties of a HintsKeySet.
                             * @typedef {Object} com.hedera.hapi.node.state.hints.HintsKeySet.$Properties
                             * @property {number|Long|null} [nodeId] The id of the node submitting these keys.
                             * @property {proto.Timestamp.$Properties|null} [adoptionTime] The consensus time at which the network adopted the active
                             * hinTS key in this set.
                             * @property {Uint8Array|null} [key] The party's active hinTS key.
                             * @property {Uint8Array|null} [nextKey] If set, the new hinTS key the node wants to use when
                             * the next construction begins.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HintsKeySet.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @interface IHintsKeySet
                             * @augments com.hedera.hapi.node.state.hints.HintsKeySet.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.hints.HintsKeySet.$Properties instead.
                             */

                            /**
                             * Shape of a HintsKeySet.
                             * @typedef {com.hedera.hapi.node.state.hints.HintsKeySet.$Properties} com.hedera.hapi.node.state.hints.HintsKeySet.$Shape
                             */

                            /**
                             * Constructs a new HintsKeySet.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @classdesc A set of hinTS keys submitted by a node.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.hints.HintsKeySet.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HintsKeySet = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The id of the node submitting these keys.
                             * @member {number|Long} nodeId
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @instance
                             */
                            HintsKeySet.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The consensus time at which the network adopted the active
                             * hinTS key in this set.
                             * @member {proto.Timestamp.$Properties|null|undefined} adoptionTime
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @instance
                             */
                            HintsKeySet.prototype.adoptionTime = null;

                            /**
                             * The party's active hinTS key.
                             * @member {Uint8Array} key
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @instance
                             */
                            HintsKeySet.prototype.key = $util.newBuffer([]);

                            /**
                             * If set, the new hinTS key the node wants to use when
                             * the next construction begins.
                             * @member {Uint8Array} nextKey
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @instance
                             */
                            HintsKeySet.prototype.nextKey = $util.newBuffer([]);

                            /**
                             * Creates a new HintsKeySet instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsKeySet.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.hints.HintsKeySet} HintsKeySet instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.hints.HintsKeySet.$Shape): com.hedera.hapi.node.state.hints.HintsKeySet & com.hedera.hapi.node.state.hints.HintsKeySet.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.hints.HintsKeySet.$Properties): com.hedera.hapi.node.state.hints.HintsKeySet;
                             * }}
                             */
                            HintsKeySet.create = function(properties) {
                                return new HintsKeySet(properties);
                            };

                            /**
                             * Encodes the specified HintsKeySet message. Does not implicitly {@link com.hedera.hapi.node.state.hints.HintsKeySet.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsKeySet.$Properties} message HintsKeySet message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsKeySet.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.nodeId);
                                if (message.adoptionTime != null && $Object.hasOwnProperty.call(message, "adoptionTime"))
                                    $root.proto.Timestamp.encode(message.adoptionTime, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key.length)
                                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.key);
                                if (message.nextKey != null && $Object.hasOwnProperty.call(message, "nextKey") && message.nextKey.length)
                                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.nextKey);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HintsKeySet message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.hints.HintsKeySet.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsKeySet.$Properties} message HintsKeySet message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsKeySet.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HintsKeySet message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.hints.HintsKeySet & com.hedera.hapi.node.state.hints.HintsKeySet.$Shape} HintsKeySet
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsKeySet.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.hints.HintsKeySet();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.nodeId = value;
                                            else
                                                delete message.nodeId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.adoptionTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adoptionTime);
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.key = value;
                                            else
                                                delete message.key;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.nextKey = value;
                                            else
                                                delete message.nextKey;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HintsKeySet message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.hints.HintsKeySet & com.hedera.hapi.node.state.hints.HintsKeySet.$Shape} HintsKeySet
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsKeySet.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HintsKeySet message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HintsKeySet.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                        return "nodeId: integer|Long expected";
                                if (message.adoptionTime != null && $Object.hasOwnProperty.call(message, "adoptionTime")) {
                                    let error = $root.proto.Timestamp.verify(message.adoptionTime, _depth + 1);
                                    if (error)
                                        return "adoptionTime." + error;
                                }
                                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                    if (!(message.key && typeof message.key.length === "number" || $util.isString(message.key)))
                                        return "key: buffer expected";
                                if (message.nextKey != null && $Object.hasOwnProperty.call(message, "nextKey"))
                                    if (!(message.nextKey && typeof message.nextKey.length === "number" || $util.isString(message.nextKey)))
                                        return "nextKey: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a HintsKeySet message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.hints.HintsKeySet} HintsKeySet
                             */
                            HintsKeySet.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.hints.HintsKeySet)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.hints.HintsKeySet: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.hints.HintsKeySet();
                                if (object.nodeId != null)
                                    if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                        if ($util.Long)
                                            message.nodeId = $util.Long.fromValue(object.nodeId, true);
                                        else if (typeof object.nodeId === "string")
                                            message.nodeId = $parseInt(object.nodeId, 10);
                                        else if (typeof object.nodeId === "number")
                                            message.nodeId = object.nodeId;
                                        else if (typeof object.nodeId === "object")
                                            message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
                                if (object.adoptionTime != null) {
                                    if (!$util.isObject(object.adoptionTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.hints.HintsKeySet.adoptionTime: object expected");
                                    message.adoptionTime = $root.proto.Timestamp.fromObject(object.adoptionTime, _depth + 1);
                                }
                                if (object.key != null)
                                    if (object.key.length)
                                        if (typeof object.key === "string")
                                            $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                                        else if (object.key.length >= 0)
                                            message.key = object.key;
                                if (object.nextKey != null)
                                    if (object.nextKey.length)
                                        if (typeof object.nextKey === "string")
                                            $util.base64.decode(object.nextKey, message.nextKey = $util.newBuffer($util.base64.length(object.nextKey)), 0);
                                        else if (object.nextKey.length >= 0)
                                            message.nextKey = object.nextKey;
                                return message;
                            };

                            /**
                             * Creates a plain object from a HintsKeySet message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsKeySet} message HintsKeySet
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HintsKeySet.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    object.adoptionTime = null;
                                    if (options.bytes === $String)
                                        object.key = "";
                                    else {
                                        object.key = [];
                                        if (options.bytes !== $Array)
                                            object.key = $util.newBuffer(object.key);
                                    }
                                    if (options.bytes === $String)
                                        object.nextKey = "";
                                    else {
                                        object.nextKey = [];
                                        if (options.bytes !== $Array)
                                            object.nextKey = $util.newBuffer(object.nextKey);
                                    }
                                }
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                                    else if (typeof message.nodeId === "number")
                                        object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                    else
                                        object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
                                if (message.adoptionTime != null && $Object.hasOwnProperty.call(message, "adoptionTime"))
                                    object.adoptionTime = $root.proto.Timestamp.toObject(message.adoptionTime, options, _depth + 1);
                                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                    object.key = options.bytes === $String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.key) : message.key;
                                if (message.nextKey != null && $Object.hasOwnProperty.call(message, "nextKey"))
                                    object.nextKey = options.bytes === $String ? $util.base64.encode(message.nextKey, 0, message.nextKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.nextKey) : message.nextKey;
                                return object;
                            };

                            /**
                             * Converts this HintsKeySet to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HintsKeySet.prototype.toJSON = function() {
                                return HintsKeySet.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HintsKeySet
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.hints.HintsKeySet
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HintsKeySet.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.hints.HintsKeySet";
                            };

                            return HintsKeySet;
                        })();

                        hints.PreprocessedKeys = (function() {

                            /**
                             * Properties of a PreprocessedKeys.
                             * @typedef {Object} com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties
                             * @property {Uint8Array|null} [aggregationKey] The aggregation key for the hinTS scheme
                             * @property {Uint8Array|null} [verificationKey] The succinct verification key for the hinTS scheme.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a PreprocessedKeys.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @interface IPreprocessedKeys
                             * @augments com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties instead.
                             */

                            /**
                             * Shape of a PreprocessedKeys.
                             * @typedef {com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties} com.hedera.hapi.node.state.hints.PreprocessedKeys.$Shape
                             */

                            /**
                             * Constructs a new PreprocessedKeys.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @classdesc The output of the hinTS preprocessing algorithm; that is, a
                             * linear-size aggregation key and a succinct verification key.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const PreprocessedKeys = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The aggregation key for the hinTS scheme
                             * @member {Uint8Array} aggregationKey
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessedKeys
                             * @instance
                             */
                            PreprocessedKeys.prototype.aggregationKey = $util.newBuffer([]);

                            /**
                             * The succinct verification key for the hinTS scheme.
                             * @member {Uint8Array} verificationKey
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessedKeys
                             * @instance
                             */
                            PreprocessedKeys.prototype.verificationKey = $util.newBuffer([]);

                            /**
                             * Creates a new PreprocessedKeys instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessedKeys
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.hints.PreprocessedKeys} PreprocessedKeys instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.hints.PreprocessedKeys.$Shape): com.hedera.hapi.node.state.hints.PreprocessedKeys & com.hedera.hapi.node.state.hints.PreprocessedKeys.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties): com.hedera.hapi.node.state.hints.PreprocessedKeys;
                             * }}
                             */
                            PreprocessedKeys.create = function(properties) {
                                return new PreprocessedKeys(properties);
                            };

                            /**
                             * Encodes the specified PreprocessedKeys message. Does not implicitly {@link com.hedera.hapi.node.state.hints.PreprocessedKeys.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessedKeys
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties} message PreprocessedKeys message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            PreprocessedKeys.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.aggregationKey != null && $Object.hasOwnProperty.call(message, "aggregationKey") && message.aggregationKey.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.aggregationKey);
                                if (message.verificationKey != null && $Object.hasOwnProperty.call(message, "verificationKey") && message.verificationKey.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.verificationKey);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified PreprocessedKeys message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.hints.PreprocessedKeys.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessedKeys
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties} message PreprocessedKeys message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            PreprocessedKeys.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a PreprocessedKeys message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessedKeys
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.hints.PreprocessedKeys & com.hedera.hapi.node.state.hints.PreprocessedKeys.$Shape} PreprocessedKeys
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            PreprocessedKeys.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.hints.PreprocessedKeys();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.aggregationKey = value;
                                            else
                                                delete message.aggregationKey;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.verificationKey = value;
                                            else
                                                delete message.verificationKey;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a PreprocessedKeys message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessedKeys
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.hints.PreprocessedKeys & com.hedera.hapi.node.state.hints.PreprocessedKeys.$Shape} PreprocessedKeys
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            PreprocessedKeys.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a PreprocessedKeys message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessedKeys
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            PreprocessedKeys.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.aggregationKey != null && $Object.hasOwnProperty.call(message, "aggregationKey"))
                                    if (!(message.aggregationKey && typeof message.aggregationKey.length === "number" || $util.isString(message.aggregationKey)))
                                        return "aggregationKey: buffer expected";
                                if (message.verificationKey != null && $Object.hasOwnProperty.call(message, "verificationKey"))
                                    if (!(message.verificationKey && typeof message.verificationKey.length === "number" || $util.isString(message.verificationKey)))
                                        return "verificationKey: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a PreprocessedKeys message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessedKeys
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.hints.PreprocessedKeys} PreprocessedKeys
                             */
                            PreprocessedKeys.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.hints.PreprocessedKeys)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.hints.PreprocessedKeys: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.hints.PreprocessedKeys();
                                if (object.aggregationKey != null)
                                    if (object.aggregationKey.length)
                                        if (typeof object.aggregationKey === "string")
                                            $util.base64.decode(object.aggregationKey, message.aggregationKey = $util.newBuffer($util.base64.length(object.aggregationKey)), 0);
                                        else if (object.aggregationKey.length >= 0)
                                            message.aggregationKey = object.aggregationKey;
                                if (object.verificationKey != null)
                                    if (object.verificationKey.length)
                                        if (typeof object.verificationKey === "string")
                                            $util.base64.decode(object.verificationKey, message.verificationKey = $util.newBuffer($util.base64.length(object.verificationKey)), 0);
                                        else if (object.verificationKey.length >= 0)
                                            message.verificationKey = object.verificationKey;
                                return message;
                            };

                            /**
                             * Creates a plain object from a PreprocessedKeys message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessedKeys
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.PreprocessedKeys} message PreprocessedKeys
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            PreprocessedKeys.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.aggregationKey = "";
                                    else {
                                        object.aggregationKey = [];
                                        if (options.bytes !== $Array)
                                            object.aggregationKey = $util.newBuffer(object.aggregationKey);
                                    }
                                    if (options.bytes === $String)
                                        object.verificationKey = "";
                                    else {
                                        object.verificationKey = [];
                                        if (options.bytes !== $Array)
                                            object.verificationKey = $util.newBuffer(object.verificationKey);
                                    }
                                }
                                if (message.aggregationKey != null && $Object.hasOwnProperty.call(message, "aggregationKey"))
                                    object.aggregationKey = options.bytes === $String ? $util.base64.encode(message.aggregationKey, 0, message.aggregationKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.aggregationKey) : message.aggregationKey;
                                if (message.verificationKey != null && $Object.hasOwnProperty.call(message, "verificationKey"))
                                    object.verificationKey = options.bytes === $String ? $util.base64.encode(message.verificationKey, 0, message.verificationKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.verificationKey) : message.verificationKey;
                                return object;
                            };

                            /**
                             * Converts this PreprocessedKeys to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessedKeys
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            PreprocessedKeys.prototype.toJSON = function() {
                                return PreprocessedKeys.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for PreprocessedKeys
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessedKeys
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            PreprocessedKeys.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.hints.PreprocessedKeys";
                            };

                            return PreprocessedKeys;
                        })();

                        hints.PreprocessingVoteId = (function() {

                            /**
                             * Properties of a PreprocessingVoteId.
                             * @typedef {Object} com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Properties
                             * @property {number|Long|null} [constructionId] The construction this vote is connected to.
                             * @property {number|Long|null} [nodeId] The id of the node submitting the vote.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a PreprocessingVoteId.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @interface IPreprocessingVoteId
                             * @augments com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Properties instead.
                             */

                            /**
                             * Shape of a PreprocessingVoteId.
                             * @typedef {com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Properties} com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Shape
                             */

                            /**
                             * Constructs a new PreprocessingVoteId.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @classdesc The id for a node's vote for the output of the
                             * preprocessing output of a hinTS construction.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const PreprocessingVoteId = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The construction this vote is connected to.
                             * @member {number|Long} constructionId
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVoteId
                             * @instance
                             */
                            PreprocessingVoteId.prototype.constructionId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The id of the node submitting the vote.
                             * @member {number|Long} nodeId
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVoteId
                             * @instance
                             */
                            PreprocessingVoteId.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * Creates a new PreprocessingVoteId instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVoteId
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.hints.PreprocessingVoteId} PreprocessingVoteId instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Shape): com.hedera.hapi.node.state.hints.PreprocessingVoteId & com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Properties): com.hedera.hapi.node.state.hints.PreprocessingVoteId;
                             * }}
                             */
                            PreprocessingVoteId.create = function(properties) {
                                return new PreprocessingVoteId(properties);
                            };

                            /**
                             * Encodes the specified PreprocessingVoteId message. Does not implicitly {@link com.hedera.hapi.node.state.hints.PreprocessingVoteId.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVoteId
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Properties} message PreprocessingVoteId message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            PreprocessingVoteId.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId") && (typeof message.constructionId === "object" ? message.constructionId.low || message.constructionId.high : message.constructionId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.constructionId);
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.nodeId);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified PreprocessingVoteId message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.hints.PreprocessingVoteId.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVoteId
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Properties} message PreprocessingVoteId message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            PreprocessingVoteId.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a PreprocessingVoteId message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVoteId
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.hints.PreprocessingVoteId & com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Shape} PreprocessingVoteId
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            PreprocessingVoteId.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.hints.PreprocessingVoteId();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.constructionId = value;
                                            else
                                                delete message.constructionId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.nodeId = value;
                                            else
                                                delete message.nodeId;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a PreprocessingVoteId message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVoteId
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.hints.PreprocessingVoteId & com.hedera.hapi.node.state.hints.PreprocessingVoteId.$Shape} PreprocessingVoteId
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            PreprocessingVoteId.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a PreprocessingVoteId message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVoteId
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            PreprocessingVoteId.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (!$util.isInteger(message.constructionId) && !(message.constructionId && $util.isInteger(message.constructionId.low) && $util.isInteger(message.constructionId.high)))
                                        return "constructionId: integer|Long expected";
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                        return "nodeId: integer|Long expected";
                                return null;
                            };

                            /**
                             * Creates a PreprocessingVoteId message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVoteId
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.hints.PreprocessingVoteId} PreprocessingVoteId
                             */
                            PreprocessingVoteId.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.hints.PreprocessingVoteId)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.hints.PreprocessingVoteId: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.hints.PreprocessingVoteId();
                                if (object.constructionId != null)
                                    if (typeof object.constructionId === "object" ? object.constructionId.low || object.constructionId.high : $Number(object.constructionId) !== 0)
                                        if ($util.Long)
                                            message.constructionId = $util.Long.fromValue(object.constructionId, true);
                                        else if (typeof object.constructionId === "string")
                                            message.constructionId = $parseInt(object.constructionId, 10);
                                        else if (typeof object.constructionId === "number")
                                            message.constructionId = object.constructionId;
                                        else if (typeof object.constructionId === "object")
                                            message.constructionId = new $util.LongBits(object.constructionId.low >>> 0, object.constructionId.high >>> 0).toNumber(true);
                                if (object.nodeId != null)
                                    if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                        if ($util.Long)
                                            message.nodeId = $util.Long.fromValue(object.nodeId, true);
                                        else if (typeof object.nodeId === "string")
                                            message.nodeId = $parseInt(object.nodeId, 10);
                                        else if (typeof object.nodeId === "number")
                                            message.nodeId = object.nodeId;
                                        else if (typeof object.nodeId === "object")
                                            message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
                                return message;
                            };

                            /**
                             * Creates a plain object from a PreprocessingVoteId message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVoteId
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.PreprocessingVoteId} message PreprocessingVoteId
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            PreprocessingVoteId.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.constructionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.constructionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                }
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.constructionId = typeof message.constructionId === "number" ? $BigInt(message.constructionId) : $util.Long.fromBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0, true).toBigInt();
                                    else if (typeof message.constructionId === "number")
                                        object.constructionId = options.longs === $String ? $String(message.constructionId) : message.constructionId;
                                    else
                                        object.constructionId = options.longs === $String ? $util.Long.prototype.toString.call(message.constructionId) : options.longs === $Number ? new $util.LongBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0).toNumber(true) : message.constructionId;
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                                    else if (typeof message.nodeId === "number")
                                        object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                    else
                                        object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
                                return object;
                            };

                            /**
                             * Converts this PreprocessingVoteId to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVoteId
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            PreprocessingVoteId.prototype.toJSON = function() {
                                return PreprocessingVoteId.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for PreprocessingVoteId
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVoteId
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            PreprocessingVoteId.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.hints.PreprocessingVoteId";
                            };

                            return PreprocessingVoteId;
                        })();

                        hints.PreprocessingVote = (function() {

                            /**
                             * Properties of a PreprocessingVote.
                             * @typedef {Object} com.hedera.hapi.node.state.hints.PreprocessingVote.$Properties
                             * @property {com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties|null} [preprocessedKeys] The preprocessed keys this node is voting for.
                             * @property {number|Long|null} [congruentNodeId] The id of any node that already voted for the exact keys
                             * that this node wanted to vote for.
                             * @property {"preprocessedKeys"|"congruentNodeId"} [vote] PreprocessingVote vote
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a PreprocessingVote.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @interface IPreprocessingVote
                             * @augments com.hedera.hapi.node.state.hints.PreprocessingVote.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.hints.PreprocessingVote.$Properties instead.
                             */

                            /**
                             * Narrowed shape of a PreprocessingVote.
                             * @typedef {{
                             *   preprocessedKeys?: com.hedera.hapi.node.state.hints.PreprocessedKeys.$Shape|null;
                             *   congruentNodeId?: number|Long|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * } & (
                             *   ({ vote?: undefined; preprocessedKeys?: null; congruentNodeId?: null }|{ vote?: "preprocessedKeys"; preprocessedKeys: com.hedera.hapi.node.state.hints.PreprocessedKeys.$Shape; congruentNodeId?: null }|{ vote?: "congruentNodeId"; preprocessedKeys?: null; congruentNodeId: number|Long })
                             * )} com.hedera.hapi.node.state.hints.PreprocessingVote.$Shape
                             */

                            /**
                             * Constructs a new PreprocessingVote.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @classdesc A node's vote for the consensus output of a hinTS preprocessing
                             * algorithm.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.hints.PreprocessingVote.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const PreprocessingVote = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The preprocessed keys this node is voting for.
                             * @member {com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties|null|undefined} preprocessedKeys
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @instance
                             */
                            PreprocessingVote.prototype.preprocessedKeys = null;

                            /**
                             * The id of any node that already voted for the exact keys
                             * that this node wanted to vote for.
                             * @member {number|Long|null|undefined} congruentNodeId
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @instance
                             */
                            PreprocessingVote.prototype.congruentNodeId = null;

                            // OneOf field names bound to virtual getters and setters
                            let $oneOfFields;

                            /**
                             * PreprocessingVote vote.
                             * @member {"preprocessedKeys"|"congruentNodeId"|undefined} vote
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @instance
                             */
                            $Object.defineProperty(PreprocessingVote.prototype, "vote", {
                                get: $util.oneOfGetter($oneOfFields = ["preprocessedKeys", "congruentNodeId"]),
                                set: $util.oneOfSetter($oneOfFields)
                            });

                            /**
                             * Creates a new PreprocessingVote instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.PreprocessingVote.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.hints.PreprocessingVote} PreprocessingVote instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.hints.PreprocessingVote.$Shape): com.hedera.hapi.node.state.hints.PreprocessingVote & com.hedera.hapi.node.state.hints.PreprocessingVote.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.hints.PreprocessingVote.$Properties): com.hedera.hapi.node.state.hints.PreprocessingVote;
                             * }}
                             */
                            PreprocessingVote.create = function(properties) {
                                return new PreprocessingVote(properties);
                            };

                            /**
                             * Encodes the specified PreprocessingVote message. Does not implicitly {@link com.hedera.hapi.node.state.hints.PreprocessingVote.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.PreprocessingVote.$Properties} message PreprocessingVote message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            PreprocessingVote.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.preprocessedKeys != null && $Object.hasOwnProperty.call(message, "preprocessedKeys"))
                                    $root.com.hedera.hapi.node.state.hints.PreprocessedKeys.encode(message.preprocessedKeys, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.congruentNodeId != null && $Object.hasOwnProperty.call(message, "congruentNodeId"))
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.congruentNodeId);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified PreprocessingVote message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.hints.PreprocessingVote.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.PreprocessingVote.$Properties} message PreprocessingVote message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            PreprocessingVote.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a PreprocessingVote message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.hints.PreprocessingVote & com.hedera.hapi.node.state.hints.PreprocessingVote.$Shape} PreprocessingVote
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            PreprocessingVote.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.hints.PreprocessingVote();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            message.preprocessedKeys = $root.com.hedera.hapi.node.state.hints.PreprocessedKeys.decode(reader, reader.uint32(), $undefined, _depth + 1, message.preprocessedKeys);
                                            message.vote = "preprocessedKeys";
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            message.congruentNodeId = reader.uint64();
                                            message.vote = "congruentNodeId";
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a PreprocessingVote message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.hints.PreprocessingVote & com.hedera.hapi.node.state.hints.PreprocessingVote.$Shape} PreprocessingVote
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            PreprocessingVote.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a PreprocessingVote message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            PreprocessingVote.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                let properties = {};
                                if (message.preprocessedKeys != null && $Object.hasOwnProperty.call(message, "preprocessedKeys")) {
                                    properties.vote = 1;
                                    {
                                        let error = $root.com.hedera.hapi.node.state.hints.PreprocessedKeys.verify(message.preprocessedKeys, _depth + 1);
                                        if (error)
                                            return "preprocessedKeys." + error;
                                    }
                                }
                                if (message.congruentNodeId != null && $Object.hasOwnProperty.call(message, "congruentNodeId")) {
                                    if (properties.vote === 1)
                                        return "vote: multiple values";
                                    properties.vote = 1;
                                    if (!$util.isInteger(message.congruentNodeId) && !(message.congruentNodeId && $util.isInteger(message.congruentNodeId.low) && $util.isInteger(message.congruentNodeId.high)))
                                        return "congruentNodeId: integer|Long expected";
                                }
                                return null;
                            };

                            /**
                             * Creates a PreprocessingVote message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.hints.PreprocessingVote} PreprocessingVote
                             */
                            PreprocessingVote.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.hints.PreprocessingVote)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.hints.PreprocessingVote: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.hints.PreprocessingVote();
                                if (object.preprocessedKeys != null) {
                                    if (!$util.isObject(object.preprocessedKeys))
                                        throw $TypeError(".com.hedera.hapi.node.state.hints.PreprocessingVote.preprocessedKeys: object expected");
                                    message.preprocessedKeys = $root.com.hedera.hapi.node.state.hints.PreprocessedKeys.fromObject(object.preprocessedKeys, _depth + 1);
                                }
                                if (object.congruentNodeId != null)
                                    if ($util.Long)
                                        message.congruentNodeId = $util.Long.fromValue(object.congruentNodeId, true);
                                    else if (typeof object.congruentNodeId === "string")
                                        message.congruentNodeId = $parseInt(object.congruentNodeId, 10);
                                    else if (typeof object.congruentNodeId === "number")
                                        message.congruentNodeId = object.congruentNodeId;
                                    else if (typeof object.congruentNodeId === "object")
                                        message.congruentNodeId = new $util.LongBits(object.congruentNodeId.low >>> 0, object.congruentNodeId.high >>> 0).toNumber(true);
                                return message;
                            };

                            /**
                             * Creates a plain object from a PreprocessingVote message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.PreprocessingVote} message PreprocessingVote
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            PreprocessingVote.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (message.preprocessedKeys != null && $Object.hasOwnProperty.call(message, "preprocessedKeys")) {
                                    object.preprocessedKeys = $root.com.hedera.hapi.node.state.hints.PreprocessedKeys.toObject(message.preprocessedKeys, options, _depth + 1);
                                    if (options.oneofs)
                                        object.vote = "preprocessedKeys";
                                }
                                if (message.congruentNodeId != null && $Object.hasOwnProperty.call(message, "congruentNodeId")) {
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.congruentNodeId = typeof message.congruentNodeId === "number" ? $BigInt(message.congruentNodeId) : $util.Long.fromBits(message.congruentNodeId.low >>> 0, message.congruentNodeId.high >>> 0, true).toBigInt();
                                    else if (typeof message.congruentNodeId === "number")
                                        object.congruentNodeId = options.longs === $String ? $String(message.congruentNodeId) : message.congruentNodeId;
                                    else
                                        object.congruentNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.congruentNodeId) : options.longs === $Number ? new $util.LongBits(message.congruentNodeId.low >>> 0, message.congruentNodeId.high >>> 0).toNumber(true) : message.congruentNodeId;
                                    if (options.oneofs)
                                        object.vote = "congruentNodeId";
                                }
                                return object;
                            };

                            /**
                             * Converts this PreprocessingVote to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            PreprocessingVote.prototype.toJSON = function() {
                                return PreprocessingVote.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for PreprocessingVote
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.hints.PreprocessingVote
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            PreprocessingVote.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.hints.PreprocessingVote";
                            };

                            return PreprocessingVote;
                        })();

                        hints.NodePartyId = (function() {

                            /**
                             * Properties of a NodePartyId.
                             * @typedef {Object} com.hedera.hapi.node.state.hints.NodePartyId.$Properties
                             * @property {number|Long|null} [nodeId] The node id.
                             * @property {number|null} [partyId] The party id.
                             * @property {number|Long|null} [partyWeight] The party's weight in the construction.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a NodePartyId.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @interface INodePartyId
                             * @augments com.hedera.hapi.node.state.hints.NodePartyId.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.hints.NodePartyId.$Properties instead.
                             */

                            /**
                             * Shape of a NodePartyId.
                             * @typedef {com.hedera.hapi.node.state.hints.NodePartyId.$Properties} com.hedera.hapi.node.state.hints.NodePartyId.$Shape
                             */

                            /**
                             * Constructs a new NodePartyId.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @classdesc A node's weighted hinTS party id.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.hints.NodePartyId.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const NodePartyId = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The node id.
                             * @member {number|Long} nodeId
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @instance
                             */
                            NodePartyId.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The party id.
                             * @member {number} partyId
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @instance
                             */
                            NodePartyId.prototype.partyId = 0;

                            /**
                             * The party's weight in the construction.
                             * @member {number|Long} partyWeight
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @instance
                             */
                            NodePartyId.prototype.partyWeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * Creates a new NodePartyId instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.NodePartyId.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.hints.NodePartyId} NodePartyId instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.hints.NodePartyId.$Shape): com.hedera.hapi.node.state.hints.NodePartyId & com.hedera.hapi.node.state.hints.NodePartyId.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.hints.NodePartyId.$Properties): com.hedera.hapi.node.state.hints.NodePartyId;
                             * }}
                             */
                            NodePartyId.create = function(properties) {
                                return new NodePartyId(properties);
                            };

                            /**
                             * Encodes the specified NodePartyId message. Does not implicitly {@link com.hedera.hapi.node.state.hints.NodePartyId.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.NodePartyId.$Properties} message NodePartyId message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            NodePartyId.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.nodeId);
                                if (message.partyId != null && $Object.hasOwnProperty.call(message, "partyId") && message.partyId !== 0)
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.partyId);
                                if (message.partyWeight != null && $Object.hasOwnProperty.call(message, "partyWeight") && (typeof message.partyWeight === "object" ? message.partyWeight.low || message.partyWeight.high : message.partyWeight !== 0))
                                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.partyWeight);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified NodePartyId message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.hints.NodePartyId.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.NodePartyId.$Properties} message NodePartyId message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            NodePartyId.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a NodePartyId message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.hints.NodePartyId & com.hedera.hapi.node.state.hints.NodePartyId.$Shape} NodePartyId
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            NodePartyId.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.hints.NodePartyId();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.nodeId = value;
                                            else
                                                delete message.nodeId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.uint32())
                                                message.partyId = value;
                                            else
                                                delete message.partyId;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.partyWeight = value;
                                            else
                                                delete message.partyWeight;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a NodePartyId message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.hints.NodePartyId & com.hedera.hapi.node.state.hints.NodePartyId.$Shape} NodePartyId
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            NodePartyId.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a NodePartyId message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            NodePartyId.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                        return "nodeId: integer|Long expected";
                                if (message.partyId != null && $Object.hasOwnProperty.call(message, "partyId"))
                                    if (!$util.isInteger(message.partyId))
                                        return "partyId: integer expected";
                                if (message.partyWeight != null && $Object.hasOwnProperty.call(message, "partyWeight"))
                                    if (!$util.isInteger(message.partyWeight) && !(message.partyWeight && $util.isInteger(message.partyWeight.low) && $util.isInteger(message.partyWeight.high)))
                                        return "partyWeight: integer|Long expected";
                                return null;
                            };

                            /**
                             * Creates a NodePartyId message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.hints.NodePartyId} NodePartyId
                             */
                            NodePartyId.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.hints.NodePartyId)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.hints.NodePartyId: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.hints.NodePartyId();
                                if (object.nodeId != null)
                                    if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                        if ($util.Long)
                                            message.nodeId = $util.Long.fromValue(object.nodeId, true);
                                        else if (typeof object.nodeId === "string")
                                            message.nodeId = $parseInt(object.nodeId, 10);
                                        else if (typeof object.nodeId === "number")
                                            message.nodeId = object.nodeId;
                                        else if (typeof object.nodeId === "object")
                                            message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
                                if (object.partyId != null)
                                    if ($Number(object.partyId) !== 0)
                                        message.partyId = object.partyId >>> 0;
                                if (object.partyWeight != null)
                                    if (typeof object.partyWeight === "object" ? object.partyWeight.low || object.partyWeight.high : $Number(object.partyWeight) !== 0)
                                        if ($util.Long)
                                            message.partyWeight = $util.Long.fromValue(object.partyWeight, true);
                                        else if (typeof object.partyWeight === "string")
                                            message.partyWeight = $parseInt(object.partyWeight, 10);
                                        else if (typeof object.partyWeight === "number")
                                            message.partyWeight = object.partyWeight;
                                        else if (typeof object.partyWeight === "object")
                                            message.partyWeight = new $util.LongBits(object.partyWeight.low >>> 0, object.partyWeight.high >>> 0).toNumber(true);
                                return message;
                            };

                            /**
                             * Creates a plain object from a NodePartyId message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.NodePartyId} message NodePartyId
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            NodePartyId.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    object.partyId = 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.partyWeight = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.partyWeight = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                }
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                                    else if (typeof message.nodeId === "number")
                                        object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                    else
                                        object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
                                if (message.partyId != null && $Object.hasOwnProperty.call(message, "partyId"))
                                    object.partyId = message.partyId;
                                if (message.partyWeight != null && $Object.hasOwnProperty.call(message, "partyWeight"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.partyWeight = typeof message.partyWeight === "number" ? $BigInt(message.partyWeight) : $util.Long.fromBits(message.partyWeight.low >>> 0, message.partyWeight.high >>> 0, true).toBigInt();
                                    else if (typeof message.partyWeight === "number")
                                        object.partyWeight = options.longs === $String ? $String(message.partyWeight) : message.partyWeight;
                                    else
                                        object.partyWeight = options.longs === $String ? $util.Long.prototype.toString.call(message.partyWeight) : options.longs === $Number ? new $util.LongBits(message.partyWeight.low >>> 0, message.partyWeight.high >>> 0).toNumber(true) : message.partyWeight;
                                return object;
                            };

                            /**
                             * Converts this NodePartyId to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            NodePartyId.prototype.toJSON = function() {
                                return NodePartyId.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for NodePartyId
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.hints.NodePartyId
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            NodePartyId.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.hints.NodePartyId";
                            };

                            return NodePartyId;
                        })();

                        hints.HintsScheme = (function() {

                            /**
                             * Properties of a HintsScheme.
                             * @typedef {Object} com.hedera.hapi.node.state.hints.HintsScheme.$Properties
                             * @property {com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties|null} [preprocessedKeys] The aggregation and verification keys for the scheme.
                             * @property {Array.<com.hedera.hapi.node.state.hints.NodePartyId.$Properties>|null} [nodePartyIds] The final party ids assigned to each node in the target roster.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HintsScheme.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @interface IHintsScheme
                             * @augments com.hedera.hapi.node.state.hints.HintsScheme.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.hints.HintsScheme.$Properties instead.
                             */

                            /**
                             * Shape of a HintsScheme.
                             * @typedef {com.hedera.hapi.node.state.hints.HintsScheme.$Properties} com.hedera.hapi.node.state.hints.HintsScheme.$Shape
                             */

                            /**
                             * Constructs a new HintsScheme.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @classdesc The information constituting the hinTS scheme Hiero TSS.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.hints.HintsScheme.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HintsScheme = function (properties) {
                                this.nodePartyIds = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The aggregation and verification keys for the scheme.
                             * @member {com.hedera.hapi.node.state.hints.PreprocessedKeys.$Properties|null|undefined} preprocessedKeys
                             * @memberof com.hedera.hapi.node.state.hints.HintsScheme
                             * @instance
                             */
                            HintsScheme.prototype.preprocessedKeys = null;

                            /**
                             * The final party ids assigned to each node in the target roster.
                             * @member {Array.<com.hedera.hapi.node.state.hints.NodePartyId.$Properties>} nodePartyIds
                             * @memberof com.hedera.hapi.node.state.hints.HintsScheme
                             * @instance
                             */
                            HintsScheme.prototype.nodePartyIds = $util.emptyArray;

                            /**
                             * Creates a new HintsScheme instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.hints.HintsScheme
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsScheme.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.hints.HintsScheme} HintsScheme instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.hints.HintsScheme.$Shape): com.hedera.hapi.node.state.hints.HintsScheme & com.hedera.hapi.node.state.hints.HintsScheme.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.hints.HintsScheme.$Properties): com.hedera.hapi.node.state.hints.HintsScheme;
                             * }}
                             */
                            HintsScheme.create = function(properties) {
                                return new HintsScheme(properties);
                            };

                            /**
                             * Encodes the specified HintsScheme message. Does not implicitly {@link com.hedera.hapi.node.state.hints.HintsScheme.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.hints.HintsScheme
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsScheme.$Properties} message HintsScheme message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsScheme.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.preprocessedKeys != null && $Object.hasOwnProperty.call(message, "preprocessedKeys"))
                                    $root.com.hedera.hapi.node.state.hints.PreprocessedKeys.encode(message.preprocessedKeys, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.nodePartyIds != null && message.nodePartyIds.length)
                                    for (let i = 0; i < message.nodePartyIds.length; ++i)
                                        $root.com.hedera.hapi.node.state.hints.NodePartyId.encode(message.nodePartyIds[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HintsScheme message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.hints.HintsScheme.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.HintsScheme
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsScheme.$Properties} message HintsScheme message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsScheme.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HintsScheme message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.hints.HintsScheme
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.hints.HintsScheme & com.hedera.hapi.node.state.hints.HintsScheme.$Shape} HintsScheme
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsScheme.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.hints.HintsScheme();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            message.preprocessedKeys = $root.com.hedera.hapi.node.state.hints.PreprocessedKeys.decode(reader, reader.uint32(), $undefined, _depth + 1, message.preprocessedKeys);
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.nodePartyIds && message.nodePartyIds.length))
                                                message.nodePartyIds = [];
                                            message.nodePartyIds.push($root.com.hedera.hapi.node.state.hints.NodePartyId.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HintsScheme message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.HintsScheme
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.hints.HintsScheme & com.hedera.hapi.node.state.hints.HintsScheme.$Shape} HintsScheme
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsScheme.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HintsScheme message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.hints.HintsScheme
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HintsScheme.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.preprocessedKeys != null && $Object.hasOwnProperty.call(message, "preprocessedKeys")) {
                                    let error = $root.com.hedera.hapi.node.state.hints.PreprocessedKeys.verify(message.preprocessedKeys, _depth + 1);
                                    if (error)
                                        return "preprocessedKeys." + error;
                                }
                                if (message.nodePartyIds != null && $Object.hasOwnProperty.call(message, "nodePartyIds")) {
                                    if (!$Array.isArray(message.nodePartyIds))
                                        return "nodePartyIds: array expected";
                                    for (let i = 0; i < message.nodePartyIds.length; ++i) {
                                        let error = $root.com.hedera.hapi.node.state.hints.NodePartyId.verify(message.nodePartyIds[i], _depth + 1);
                                        if (error)
                                            return "nodePartyIds." + error;
                                    }
                                }
                                return null;
                            };

                            /**
                             * Creates a HintsScheme message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.hints.HintsScheme
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.hints.HintsScheme} HintsScheme
                             */
                            HintsScheme.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.hints.HintsScheme)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.hints.HintsScheme: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.hints.HintsScheme();
                                if (object.preprocessedKeys != null) {
                                    if (!$util.isObject(object.preprocessedKeys))
                                        throw $TypeError(".com.hedera.hapi.node.state.hints.HintsScheme.preprocessedKeys: object expected");
                                    message.preprocessedKeys = $root.com.hedera.hapi.node.state.hints.PreprocessedKeys.fromObject(object.preprocessedKeys, _depth + 1);
                                }
                                if (object.nodePartyIds) {
                                    if (!$Array.isArray(object.nodePartyIds))
                                        throw $TypeError(".com.hedera.hapi.node.state.hints.HintsScheme.nodePartyIds: array expected");
                                    message.nodePartyIds = $Array(object.nodePartyIds.length);
                                    for (let i = 0; i < object.nodePartyIds.length; ++i) {
                                        if (!$util.isObject(object.nodePartyIds[i]))
                                            throw $TypeError(".com.hedera.hapi.node.state.hints.HintsScheme.nodePartyIds: object expected");
                                        message.nodePartyIds[i] = $root.com.hedera.hapi.node.state.hints.NodePartyId.fromObject(object.nodePartyIds[i], _depth + 1);
                                    }
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a HintsScheme message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.hints.HintsScheme
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsScheme} message HintsScheme
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HintsScheme.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults)
                                    object.nodePartyIds = [];
                                if (options.defaults)
                                    object.preprocessedKeys = null;
                                if (message.preprocessedKeys != null && $Object.hasOwnProperty.call(message, "preprocessedKeys"))
                                    object.preprocessedKeys = $root.com.hedera.hapi.node.state.hints.PreprocessedKeys.toObject(message.preprocessedKeys, options, _depth + 1);
                                if (message.nodePartyIds && message.nodePartyIds.length) {
                                    object.nodePartyIds = $Array(message.nodePartyIds.length);
                                    for (let j = 0; j < message.nodePartyIds.length; ++j)
                                        object.nodePartyIds[j] = $root.com.hedera.hapi.node.state.hints.NodePartyId.toObject(message.nodePartyIds[j], options, _depth + 1);
                                }
                                return object;
                            };

                            /**
                             * Converts this HintsScheme to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.hints.HintsScheme
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HintsScheme.prototype.toJSON = function() {
                                return HintsScheme.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HintsScheme
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.hints.HintsScheme
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HintsScheme.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.hints.HintsScheme";
                            };

                            return HintsScheme;
                        })();

                        hints.HintsConstruction = (function() {

                            /**
                             * Properties of a HintsConstruction.
                             * @typedef {Object} com.hedera.hapi.node.state.hints.HintsConstruction.$Properties
                             * @property {number|Long|null} [constructionId] The id of the construction.
                             * @property {Uint8Array|null} [sourceRosterHash] The hash of the roster whose weights are used to determine when
                             * the >=1/3 weight signing threshold is reached.
                             * @property {Uint8Array|null} [targetRosterHash] The hash of the roster whose weights are used to determine when
                             * the >2/3 weight availability threshold is reached.
                             * @property {proto.Timestamp.$Properties|null} [gracePeriodEndTime] If the network is still gathering hinTS keys for this construction,
                             * the time at which honest nodes should stop waiting for tardy
                             * publications and begin preprocessing as soon as there are valid
                             * hinTS keys for nodes with >2/3 weight in the target roster.
                             * @property {proto.Timestamp.$Properties|null} [preprocessingStartTime] If the network has gathered enough hinTS keys for this construction
                             * to begin preprocessing, the cutoff time by which keys must have been
                             * adopted to be included as input to the preprocessing algorithm.
                             * @property {com.hedera.hapi.node.state.hints.HintsScheme.$Properties|null} [hintsScheme] If set, the completed hinTS scheme.
                             * @property {"gracePeriodEndTime"|"preprocessingStartTime"|"hintsScheme"} [preprocessingState] HintsConstruction preprocessingState
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HintsConstruction.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @interface IHintsConstruction
                             * @augments com.hedera.hapi.node.state.hints.HintsConstruction.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.hints.HintsConstruction.$Properties instead.
                             */

                            /**
                             * Narrowed shape of a HintsConstruction.
                             * @typedef {{
                             *   constructionId?: number|Long|null;
                             *   sourceRosterHash?: Uint8Array|null;
                             *   targetRosterHash?: Uint8Array|null;
                             *   gracePeriodEndTime?: proto.Timestamp.$Shape|null;
                             *   preprocessingStartTime?: proto.Timestamp.$Shape|null;
                             *   hintsScheme?: com.hedera.hapi.node.state.hints.HintsScheme.$Shape|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * } & (
                             *   ({ preprocessingState?: undefined; gracePeriodEndTime?: null; preprocessingStartTime?: null; hintsScheme?: null }|{ preprocessingState?: "gracePeriodEndTime"; gracePeriodEndTime: proto.Timestamp.$Shape; preprocessingStartTime?: null; hintsScheme?: null }|{ preprocessingState?: "preprocessingStartTime"; gracePeriodEndTime?: null; preprocessingStartTime: proto.Timestamp.$Shape; hintsScheme?: null }|{ preprocessingState?: "hintsScheme"; gracePeriodEndTime?: null; preprocessingStartTime?: null; hintsScheme: com.hedera.hapi.node.state.hints.HintsScheme.$Shape })
                             * )} com.hedera.hapi.node.state.hints.HintsConstruction.$Shape
                             */

                            /**
                             * Constructs a new HintsConstruction.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @classdesc A summary of progress in constructing a hinTS scheme.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.hints.HintsConstruction.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HintsConstruction = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The id of the construction.
                             * @member {number|Long} constructionId
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @instance
                             */
                            HintsConstruction.prototype.constructionId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The hash of the roster whose weights are used to determine when
                             * the >=1/3 weight signing threshold is reached.
                             * @member {Uint8Array} sourceRosterHash
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @instance
                             */
                            HintsConstruction.prototype.sourceRosterHash = $util.newBuffer([]);

                            /**
                             * The hash of the roster whose weights are used to determine when
                             * the >2/3 weight availability threshold is reached.
                             * @member {Uint8Array} targetRosterHash
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @instance
                             */
                            HintsConstruction.prototype.targetRosterHash = $util.newBuffer([]);

                            /**
                             * If the network is still gathering hinTS keys for this construction,
                             * the time at which honest nodes should stop waiting for tardy
                             * publications and begin preprocessing as soon as there are valid
                             * hinTS keys for nodes with >2/3 weight in the target roster.
                             * @member {proto.Timestamp.$Properties|null|undefined} gracePeriodEndTime
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @instance
                             */
                            HintsConstruction.prototype.gracePeriodEndTime = null;

                            /**
                             * If the network has gathered enough hinTS keys for this construction
                             * to begin preprocessing, the cutoff time by which keys must have been
                             * adopted to be included as input to the preprocessing algorithm.
                             * @member {proto.Timestamp.$Properties|null|undefined} preprocessingStartTime
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @instance
                             */
                            HintsConstruction.prototype.preprocessingStartTime = null;

                            /**
                             * If set, the completed hinTS scheme.
                             * @member {com.hedera.hapi.node.state.hints.HintsScheme.$Properties|null|undefined} hintsScheme
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @instance
                             */
                            HintsConstruction.prototype.hintsScheme = null;

                            // OneOf field names bound to virtual getters and setters
                            let $oneOfFields;

                            /**
                             * HintsConstruction preprocessingState.
                             * @member {"gracePeriodEndTime"|"preprocessingStartTime"|"hintsScheme"|undefined} preprocessingState
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @instance
                             */
                            $Object.defineProperty(HintsConstruction.prototype, "preprocessingState", {
                                get: $util.oneOfGetter($oneOfFields = ["gracePeriodEndTime", "preprocessingStartTime", "hintsScheme"]),
                                set: $util.oneOfSetter($oneOfFields)
                            });

                            /**
                             * Creates a new HintsConstruction instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsConstruction.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.hints.HintsConstruction} HintsConstruction instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.hints.HintsConstruction.$Shape): com.hedera.hapi.node.state.hints.HintsConstruction & com.hedera.hapi.node.state.hints.HintsConstruction.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.hints.HintsConstruction.$Properties): com.hedera.hapi.node.state.hints.HintsConstruction;
                             * }}
                             */
                            HintsConstruction.create = function(properties) {
                                return new HintsConstruction(properties);
                            };

                            /**
                             * Encodes the specified HintsConstruction message. Does not implicitly {@link com.hedera.hapi.node.state.hints.HintsConstruction.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsConstruction.$Properties} message HintsConstruction message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsConstruction.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId") && (typeof message.constructionId === "object" ? message.constructionId.low || message.constructionId.high : message.constructionId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.constructionId);
                                if (message.sourceRosterHash != null && $Object.hasOwnProperty.call(message, "sourceRosterHash") && message.sourceRosterHash.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.sourceRosterHash);
                                if (message.targetRosterHash != null && $Object.hasOwnProperty.call(message, "targetRosterHash") && message.targetRosterHash.length)
                                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.targetRosterHash);
                                if (message.gracePeriodEndTime != null && $Object.hasOwnProperty.call(message, "gracePeriodEndTime"))
                                    $root.proto.Timestamp.encode(message.gracePeriodEndTime, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                                if (message.preprocessingStartTime != null && $Object.hasOwnProperty.call(message, "preprocessingStartTime"))
                                    $root.proto.Timestamp.encode(message.preprocessingStartTime, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                                if (message.hintsScheme != null && $Object.hasOwnProperty.call(message, "hintsScheme"))
                                    $root.com.hedera.hapi.node.state.hints.HintsScheme.encode(message.hintsScheme, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HintsConstruction message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.hints.HintsConstruction.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsConstruction.$Properties} message HintsConstruction message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsConstruction.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HintsConstruction message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.hints.HintsConstruction & com.hedera.hapi.node.state.hints.HintsConstruction.$Shape} HintsConstruction
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsConstruction.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.hints.HintsConstruction();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.constructionId = value;
                                            else
                                                delete message.constructionId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.sourceRosterHash = value;
                                            else
                                                delete message.sourceRosterHash;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.targetRosterHash = value;
                                            else
                                                delete message.targetRosterHash;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            message.gracePeriodEndTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.gracePeriodEndTime);
                                            message.preprocessingState = "gracePeriodEndTime";
                                            continue;
                                        }
                                    case 5: {
                                            if (wireType !== 2)
                                                break;
                                            message.preprocessingStartTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.preprocessingStartTime);
                                            message.preprocessingState = "preprocessingStartTime";
                                            continue;
                                        }
                                    case 6: {
                                            if (wireType !== 2)
                                                break;
                                            message.hintsScheme = $root.com.hedera.hapi.node.state.hints.HintsScheme.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hintsScheme);
                                            message.preprocessingState = "hintsScheme";
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HintsConstruction message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.hints.HintsConstruction & com.hedera.hapi.node.state.hints.HintsConstruction.$Shape} HintsConstruction
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsConstruction.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HintsConstruction message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HintsConstruction.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                let properties = {};
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (!$util.isInteger(message.constructionId) && !(message.constructionId && $util.isInteger(message.constructionId.low) && $util.isInteger(message.constructionId.high)))
                                        return "constructionId: integer|Long expected";
                                if (message.sourceRosterHash != null && $Object.hasOwnProperty.call(message, "sourceRosterHash"))
                                    if (!(message.sourceRosterHash && typeof message.sourceRosterHash.length === "number" || $util.isString(message.sourceRosterHash)))
                                        return "sourceRosterHash: buffer expected";
                                if (message.targetRosterHash != null && $Object.hasOwnProperty.call(message, "targetRosterHash"))
                                    if (!(message.targetRosterHash && typeof message.targetRosterHash.length === "number" || $util.isString(message.targetRosterHash)))
                                        return "targetRosterHash: buffer expected";
                                if (message.gracePeriodEndTime != null && $Object.hasOwnProperty.call(message, "gracePeriodEndTime")) {
                                    properties.preprocessingState = 1;
                                    {
                                        let error = $root.proto.Timestamp.verify(message.gracePeriodEndTime, _depth + 1);
                                        if (error)
                                            return "gracePeriodEndTime." + error;
                                    }
                                }
                                if (message.preprocessingStartTime != null && $Object.hasOwnProperty.call(message, "preprocessingStartTime")) {
                                    if (properties.preprocessingState === 1)
                                        return "preprocessingState: multiple values";
                                    properties.preprocessingState = 1;
                                    {
                                        let error = $root.proto.Timestamp.verify(message.preprocessingStartTime, _depth + 1);
                                        if (error)
                                            return "preprocessingStartTime." + error;
                                    }
                                }
                                if (message.hintsScheme != null && $Object.hasOwnProperty.call(message, "hintsScheme")) {
                                    if (properties.preprocessingState === 1)
                                        return "preprocessingState: multiple values";
                                    properties.preprocessingState = 1;
                                    {
                                        let error = $root.com.hedera.hapi.node.state.hints.HintsScheme.verify(message.hintsScheme, _depth + 1);
                                        if (error)
                                            return "hintsScheme." + error;
                                    }
                                }
                                return null;
                            };

                            /**
                             * Creates a HintsConstruction message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.hints.HintsConstruction} HintsConstruction
                             */
                            HintsConstruction.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.hints.HintsConstruction)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.hints.HintsConstruction: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.hints.HintsConstruction();
                                if (object.constructionId != null)
                                    if (typeof object.constructionId === "object" ? object.constructionId.low || object.constructionId.high : $Number(object.constructionId) !== 0)
                                        if ($util.Long)
                                            message.constructionId = $util.Long.fromValue(object.constructionId, true);
                                        else if (typeof object.constructionId === "string")
                                            message.constructionId = $parseInt(object.constructionId, 10);
                                        else if (typeof object.constructionId === "number")
                                            message.constructionId = object.constructionId;
                                        else if (typeof object.constructionId === "object")
                                            message.constructionId = new $util.LongBits(object.constructionId.low >>> 0, object.constructionId.high >>> 0).toNumber(true);
                                if (object.sourceRosterHash != null)
                                    if (object.sourceRosterHash.length)
                                        if (typeof object.sourceRosterHash === "string")
                                            $util.base64.decode(object.sourceRosterHash, message.sourceRosterHash = $util.newBuffer($util.base64.length(object.sourceRosterHash)), 0);
                                        else if (object.sourceRosterHash.length >= 0)
                                            message.sourceRosterHash = object.sourceRosterHash;
                                if (object.targetRosterHash != null)
                                    if (object.targetRosterHash.length)
                                        if (typeof object.targetRosterHash === "string")
                                            $util.base64.decode(object.targetRosterHash, message.targetRosterHash = $util.newBuffer($util.base64.length(object.targetRosterHash)), 0);
                                        else if (object.targetRosterHash.length >= 0)
                                            message.targetRosterHash = object.targetRosterHash;
                                if (object.gracePeriodEndTime != null) {
                                    if (!$util.isObject(object.gracePeriodEndTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.hints.HintsConstruction.gracePeriodEndTime: object expected");
                                    message.gracePeriodEndTime = $root.proto.Timestamp.fromObject(object.gracePeriodEndTime, _depth + 1);
                                }
                                if (object.preprocessingStartTime != null) {
                                    if (!$util.isObject(object.preprocessingStartTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.hints.HintsConstruction.preprocessingStartTime: object expected");
                                    message.preprocessingStartTime = $root.proto.Timestamp.fromObject(object.preprocessingStartTime, _depth + 1);
                                }
                                if (object.hintsScheme != null) {
                                    if (!$util.isObject(object.hintsScheme))
                                        throw $TypeError(".com.hedera.hapi.node.state.hints.HintsConstruction.hintsScheme: object expected");
                                    message.hintsScheme = $root.com.hedera.hapi.node.state.hints.HintsScheme.fromObject(object.hintsScheme, _depth + 1);
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a HintsConstruction message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.HintsConstruction} message HintsConstruction
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HintsConstruction.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.constructionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.constructionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if (options.bytes === $String)
                                        object.sourceRosterHash = "";
                                    else {
                                        object.sourceRosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.sourceRosterHash = $util.newBuffer(object.sourceRosterHash);
                                    }
                                    if (options.bytes === $String)
                                        object.targetRosterHash = "";
                                    else {
                                        object.targetRosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.targetRosterHash = $util.newBuffer(object.targetRosterHash);
                                    }
                                }
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.constructionId = typeof message.constructionId === "number" ? $BigInt(message.constructionId) : $util.Long.fromBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0, true).toBigInt();
                                    else if (typeof message.constructionId === "number")
                                        object.constructionId = options.longs === $String ? $String(message.constructionId) : message.constructionId;
                                    else
                                        object.constructionId = options.longs === $String ? $util.Long.prototype.toString.call(message.constructionId) : options.longs === $Number ? new $util.LongBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0).toNumber(true) : message.constructionId;
                                if (message.sourceRosterHash != null && $Object.hasOwnProperty.call(message, "sourceRosterHash"))
                                    object.sourceRosterHash = options.bytes === $String ? $util.base64.encode(message.sourceRosterHash, 0, message.sourceRosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.sourceRosterHash) : message.sourceRosterHash;
                                if (message.targetRosterHash != null && $Object.hasOwnProperty.call(message, "targetRosterHash"))
                                    object.targetRosterHash = options.bytes === $String ? $util.base64.encode(message.targetRosterHash, 0, message.targetRosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.targetRosterHash) : message.targetRosterHash;
                                if (message.gracePeriodEndTime != null && $Object.hasOwnProperty.call(message, "gracePeriodEndTime")) {
                                    object.gracePeriodEndTime = $root.proto.Timestamp.toObject(message.gracePeriodEndTime, options, _depth + 1);
                                    if (options.oneofs)
                                        object.preprocessingState = "gracePeriodEndTime";
                                }
                                if (message.preprocessingStartTime != null && $Object.hasOwnProperty.call(message, "preprocessingStartTime")) {
                                    object.preprocessingStartTime = $root.proto.Timestamp.toObject(message.preprocessingStartTime, options, _depth + 1);
                                    if (options.oneofs)
                                        object.preprocessingState = "preprocessingStartTime";
                                }
                                if (message.hintsScheme != null && $Object.hasOwnProperty.call(message, "hintsScheme")) {
                                    object.hintsScheme = $root.com.hedera.hapi.node.state.hints.HintsScheme.toObject(message.hintsScheme, options, _depth + 1);
                                    if (options.oneofs)
                                        object.preprocessingState = "hintsScheme";
                                }
                                return object;
                            };

                            /**
                             * Converts this HintsConstruction to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HintsConstruction.prototype.toJSON = function() {
                                return HintsConstruction.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HintsConstruction
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.hints.HintsConstruction
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HintsConstruction.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.hints.HintsConstruction";
                            };

                            return HintsConstruction;
                        })();

                        /**
                         * The stage of a CRS construction.
                         * @name com.hedera.hapi.node.state.hints.CRSStage
                         * @enum {number}
                         * @property {number} GATHERING_CONTRIBUTIONS=0 The network is gathering contributions to the CRS from all nodes.
                         * @property {number} WAITING_FOR_ADOPTING_FINAL_CRS=1 The network is waiting for some grace period to allow the verification future
                         * to be completed after the last node has contributed to the CRS.
                         * @property {number} COMPLETED=2 The network has completed the CRS construction and is set in the CrsState.
                         */
                        hints.CRSStage = (function() {
                            const valuesById = $Object.create(null), values = $Object.create(valuesById);
                            values[valuesById[0] = "GATHERING_CONTRIBUTIONS"] = 0;
                            values[valuesById[1] = "WAITING_FOR_ADOPTING_FINAL_CRS"] = 1;
                            values[valuesById[2] = "COMPLETED"] = 2;
                            return values;
                        })();

                        hints.CRSState = (function() {

                            /**
                             * Properties of a CRSState.
                             * @typedef {Object} com.hedera.hapi.node.state.hints.CRSState.$Properties
                             * @property {Uint8Array|null} [crs] The bytes of the CRS. Based on the CRSStage, this may be the initial CRS
                             * or the final CRS.
                             * @property {com.hedera.hapi.node.state.hints.CRSStage|null} [stage] The stage of the CRS construction.
                             * @property {google.protobuf.UInt64Value.$Properties|null} [nextContributingNodeId] The id of the next node that should contribute to the CRS. This is used
                             * to ensure that all nodes contribute to the CRS in a round-robin fashion.
                             * If this is null, then all nodes in the network have contributed to the CRS.
                             * @property {proto.Timestamp.$Properties|null} [contributionEndTime] The time at which the network should stop waiting for the node's contributions
                             * and move on to the next node in the round-robin fashion.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a CRSState.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @interface ICRSState
                             * @augments com.hedera.hapi.node.state.hints.CRSState.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.hints.CRSState.$Properties instead.
                             */

                            /**
                             * Shape of a CRSState.
                             * @typedef {com.hedera.hapi.node.state.hints.CRSState.$Properties} com.hedera.hapi.node.state.hints.CRSState.$Shape
                             */

                            /**
                             * Constructs a new CRSState.
                             * @memberof com.hedera.hapi.node.state.hints
                             * @classdesc The state of a CRS construction.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.hints.CRSState.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const CRSState = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The bytes of the CRS. Based on the CRSStage, this may be the initial CRS
                             * or the final CRS.
                             * @member {Uint8Array} crs
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @instance
                             */
                            CRSState.prototype.crs = $util.newBuffer([]);

                            /**
                             * The stage of the CRS construction.
                             * @member {com.hedera.hapi.node.state.hints.CRSStage} stage
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @instance
                             */
                            CRSState.prototype.stage = 0;

                            /**
                             * The id of the next node that should contribute to the CRS. This is used
                             * to ensure that all nodes contribute to the CRS in a round-robin fashion.
                             * If this is null, then all nodes in the network have contributed to the CRS.
                             * @member {google.protobuf.UInt64Value.$Properties|null|undefined} nextContributingNodeId
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @instance
                             */
                            CRSState.prototype.nextContributingNodeId = null;

                            /**
                             * The time at which the network should stop waiting for the node's contributions
                             * and move on to the next node in the round-robin fashion.
                             * @member {proto.Timestamp.$Properties|null|undefined} contributionEndTime
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @instance
                             */
                            CRSState.prototype.contributionEndTime = null;

                            /**
                             * Creates a new CRSState instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.CRSState.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.hints.CRSState} CRSState instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.hints.CRSState.$Shape): com.hedera.hapi.node.state.hints.CRSState & com.hedera.hapi.node.state.hints.CRSState.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.hints.CRSState.$Properties): com.hedera.hapi.node.state.hints.CRSState;
                             * }}
                             */
                            CRSState.create = function(properties) {
                                return new CRSState(properties);
                            };

                            /**
                             * Encodes the specified CRSState message. Does not implicitly {@link com.hedera.hapi.node.state.hints.CRSState.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.CRSState.$Properties} message CRSState message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            CRSState.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.crs != null && $Object.hasOwnProperty.call(message, "crs") && message.crs.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.crs);
                                if (message.stage != null && $Object.hasOwnProperty.call(message, "stage") && message.stage !== 0)
                                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.stage);
                                if (message.nextContributingNodeId != null && $Object.hasOwnProperty.call(message, "nextContributingNodeId"))
                                    $root.google.protobuf.UInt64Value.encode(message.nextContributingNodeId, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                                if (message.contributionEndTime != null && $Object.hasOwnProperty.call(message, "contributionEndTime"))
                                    $root.proto.Timestamp.encode(message.contributionEndTime, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified CRSState message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.hints.CRSState.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.CRSState.$Properties} message CRSState message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            CRSState.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a CRSState message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.hints.CRSState & com.hedera.hapi.node.state.hints.CRSState.$Shape} CRSState
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            CRSState.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.hints.CRSState();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.crs = value;
                                            else
                                                delete message.crs;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.int32())
                                                message.stage = value;
                                            else
                                                delete message.stage;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            message.nextContributingNodeId = $root.google.protobuf.UInt64Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nextContributingNodeId);
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            message.contributionEndTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contributionEndTime);
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a CRSState message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.hints.CRSState & com.hedera.hapi.node.state.hints.CRSState.$Shape} CRSState
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            CRSState.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a CRSState message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            CRSState.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.crs != null && $Object.hasOwnProperty.call(message, "crs"))
                                    if (!(message.crs && typeof message.crs.length === "number" || $util.isString(message.crs)))
                                        return "crs: buffer expected";
                                if (message.stage != null && $Object.hasOwnProperty.call(message, "stage"))
                                    if (typeof message.stage !== "number" || (message.stage | 0) !== message.stage)
                                        return "stage: enum value expected";
                                if (message.nextContributingNodeId != null && $Object.hasOwnProperty.call(message, "nextContributingNodeId")) {
                                    let error = $root.google.protobuf.UInt64Value.verify(message.nextContributingNodeId, _depth + 1);
                                    if (error)
                                        return "nextContributingNodeId." + error;
                                }
                                if (message.contributionEndTime != null && $Object.hasOwnProperty.call(message, "contributionEndTime")) {
                                    let error = $root.proto.Timestamp.verify(message.contributionEndTime, _depth + 1);
                                    if (error)
                                        return "contributionEndTime." + error;
                                }
                                return null;
                            };

                            /**
                             * Creates a CRSState message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.hints.CRSState} CRSState
                             */
                            CRSState.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.hints.CRSState)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.hints.CRSState: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.hints.CRSState();
                                if (object.crs != null)
                                    if (object.crs.length)
                                        if (typeof object.crs === "string")
                                            $util.base64.decode(object.crs, message.crs = $util.newBuffer($util.base64.length(object.crs)), 0);
                                        else if (object.crs.length >= 0)
                                            message.crs = object.crs;
                                if (object.stage !== 0 && (typeof object.stage !== "string" || $root.com.hedera.hapi.node.state.hints.CRSStage[object.stage] !== 0))
                                    switch (object.stage) {
                                    case "GATHERING_CONTRIBUTIONS":
                                    case 0:
                                        message.stage = 0;
                                        break;
                                    case "WAITING_FOR_ADOPTING_FINAL_CRS":
                                    case 1:
                                        message.stage = 1;
                                        break;
                                    case "COMPLETED":
                                    case 2:
                                        message.stage = 2;
                                        break;
                                    default:
                                        if (typeof object.stage === "number" && (object.stage | 0) === object.stage)
                                            message.stage = object.stage;
                                    }
                                if (object.nextContributingNodeId != null) {
                                    if (!$util.isObject(object.nextContributingNodeId))
                                        throw $TypeError(".com.hedera.hapi.node.state.hints.CRSState.nextContributingNodeId: object expected");
                                    message.nextContributingNodeId = $root.google.protobuf.UInt64Value.fromObject(object.nextContributingNodeId, _depth + 1);
                                }
                                if (object.contributionEndTime != null) {
                                    if (!$util.isObject(object.contributionEndTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.hints.CRSState.contributionEndTime: object expected");
                                    message.contributionEndTime = $root.proto.Timestamp.fromObject(object.contributionEndTime, _depth + 1);
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a CRSState message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @static
                             * @param {com.hedera.hapi.node.state.hints.CRSState} message CRSState
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            CRSState.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.crs = "";
                                    else {
                                        object.crs = [];
                                        if (options.bytes !== $Array)
                                            object.crs = $util.newBuffer(object.crs);
                                    }
                                    object.stage = options.enums === $String ? "GATHERING_CONTRIBUTIONS" : 0;
                                    object.nextContributingNodeId = null;
                                    object.contributionEndTime = null;
                                }
                                if (message.crs != null && $Object.hasOwnProperty.call(message, "crs"))
                                    object.crs = options.bytes === $String ? $util.base64.encode(message.crs, 0, message.crs.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.crs) : message.crs;
                                if (message.stage != null && $Object.hasOwnProperty.call(message, "stage"))
                                    object.stage = options.enums === $String ? $root.com.hedera.hapi.node.state.hints.CRSStage[message.stage] === $undefined ? message.stage : $root.com.hedera.hapi.node.state.hints.CRSStage[message.stage] : message.stage;
                                if (message.nextContributingNodeId != null && $Object.hasOwnProperty.call(message, "nextContributingNodeId"))
                                    object.nextContributingNodeId = $root.google.protobuf.UInt64Value.toObject(message.nextContributingNodeId, options, _depth + 1);
                                if (message.contributionEndTime != null && $Object.hasOwnProperty.call(message, "contributionEndTime"))
                                    object.contributionEndTime = $root.proto.Timestamp.toObject(message.contributionEndTime, options, _depth + 1);
                                return object;
                            };

                            /**
                             * Converts this CRSState to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            CRSState.prototype.toJSON = function() {
                                return CRSState.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for CRSState
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.hints.CRSState
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            CRSState.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.hints.CRSState";
                            };

                            return CRSState;
                        })();

                        return hints;
                    })();

                    state.history = (function() {

                        /**
                         * Namespace history.
                         * @memberof com.hedera.hapi.node.state
                         * @namespace
                         */
                        const history = {};

                        history.ProofKeySet = (function() {

                            /**
                             * Properties of a ProofKeySet.
                             * @typedef {Object} com.hedera.hapi.node.state.history.ProofKeySet.$Properties
                             * @property {proto.Timestamp.$Properties|null} [adoptionTime] The consensus time when the network adopted the active
                             * proof key in this set. An adoption time that is sufficiently
                             * tardy relative to the latest assembly start time may result
                             * in the node's key being omitted from the address book.
                             * @property {Uint8Array|null} [key] The proof key the node is using.
                             * @property {Uint8Array|null} [nextKey] If set, the proof key the node wants to start using in the
                             * address book.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a ProofKeySet.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IProofKeySet
                             * @augments com.hedera.hapi.node.state.history.ProofKeySet.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.ProofKeySet.$Properties instead.
                             */

                            /**
                             * Shape of a ProofKeySet.
                             * @typedef {com.hedera.hapi.node.state.history.ProofKeySet.$Properties} com.hedera.hapi.node.state.history.ProofKeySet.$Shape
                             */

                            /**
                             * Constructs a new ProofKeySet.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc A set of proof keys for a node; that is, the key the node is
                             * currently using and the key it wants to use in assembling the
                             * next address book in the ledger id's chain of trust.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.ProofKeySet.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const ProofKeySet = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The consensus time when the network adopted the active
                             * proof key in this set. An adoption time that is sufficiently
                             * tardy relative to the latest assembly start time may result
                             * in the node's key being omitted from the address book.
                             * @member {proto.Timestamp.$Properties|null|undefined} adoptionTime
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @instance
                             */
                            ProofKeySet.prototype.adoptionTime = null;

                            /**
                             * The proof key the node is using.
                             * @member {Uint8Array} key
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @instance
                             */
                            ProofKeySet.prototype.key = $util.newBuffer([]);

                            /**
                             * If set, the proof key the node wants to start using in the
                             * address book.
                             * @member {Uint8Array} nextKey
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @instance
                             */
                            ProofKeySet.prototype.nextKey = $util.newBuffer([]);

                            /**
                             * Creates a new ProofKeySet instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ProofKeySet.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.ProofKeySet} ProofKeySet instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.ProofKeySet.$Shape): com.hedera.hapi.node.state.history.ProofKeySet & com.hedera.hapi.node.state.history.ProofKeySet.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.ProofKeySet.$Properties): com.hedera.hapi.node.state.history.ProofKeySet;
                             * }}
                             */
                            ProofKeySet.create = function(properties) {
                                return new ProofKeySet(properties);
                            };

                            /**
                             * Encodes the specified ProofKeySet message. Does not implicitly {@link com.hedera.hapi.node.state.history.ProofKeySet.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ProofKeySet.$Properties} message ProofKeySet message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            ProofKeySet.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.adoptionTime != null && $Object.hasOwnProperty.call(message, "adoptionTime"))
                                    $root.proto.Timestamp.encode(message.adoptionTime, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key.length)
                                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.key);
                                if (message.nextKey != null && $Object.hasOwnProperty.call(message, "nextKey") && message.nextKey.length)
                                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.nextKey);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified ProofKeySet message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.ProofKeySet.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ProofKeySet.$Properties} message ProofKeySet message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            ProofKeySet.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a ProofKeySet message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.ProofKeySet & com.hedera.hapi.node.state.history.ProofKeySet.$Shape} ProofKeySet
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            ProofKeySet.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.ProofKeySet();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.adoptionTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adoptionTime);
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.key = value;
                                            else
                                                delete message.key;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.nextKey = value;
                                            else
                                                delete message.nextKey;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a ProofKeySet message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.ProofKeySet & com.hedera.hapi.node.state.history.ProofKeySet.$Shape} ProofKeySet
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            ProofKeySet.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a ProofKeySet message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            ProofKeySet.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.adoptionTime != null && $Object.hasOwnProperty.call(message, "adoptionTime")) {
                                    let error = $root.proto.Timestamp.verify(message.adoptionTime, _depth + 1);
                                    if (error)
                                        return "adoptionTime." + error;
                                }
                                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                    if (!(message.key && typeof message.key.length === "number" || $util.isString(message.key)))
                                        return "key: buffer expected";
                                if (message.nextKey != null && $Object.hasOwnProperty.call(message, "nextKey"))
                                    if (!(message.nextKey && typeof message.nextKey.length === "number" || $util.isString(message.nextKey)))
                                        return "nextKey: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a ProofKeySet message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.ProofKeySet} ProofKeySet
                             */
                            ProofKeySet.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.ProofKeySet)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.ProofKeySet: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.ProofKeySet();
                                if (object.adoptionTime != null) {
                                    if (!$util.isObject(object.adoptionTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.ProofKeySet.adoptionTime: object expected");
                                    message.adoptionTime = $root.proto.Timestamp.fromObject(object.adoptionTime, _depth + 1);
                                }
                                if (object.key != null)
                                    if (object.key.length)
                                        if (typeof object.key === "string")
                                            $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                                        else if (object.key.length >= 0)
                                            message.key = object.key;
                                if (object.nextKey != null)
                                    if (object.nextKey.length)
                                        if (typeof object.nextKey === "string")
                                            $util.base64.decode(object.nextKey, message.nextKey = $util.newBuffer($util.base64.length(object.nextKey)), 0);
                                        else if (object.nextKey.length >= 0)
                                            message.nextKey = object.nextKey;
                                return message;
                            };

                            /**
                             * Creates a plain object from a ProofKeySet message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ProofKeySet} message ProofKeySet
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            ProofKeySet.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    object.adoptionTime = null;
                                    if (options.bytes === $String)
                                        object.key = "";
                                    else {
                                        object.key = [];
                                        if (options.bytes !== $Array)
                                            object.key = $util.newBuffer(object.key);
                                    }
                                    if (options.bytes === $String)
                                        object.nextKey = "";
                                    else {
                                        object.nextKey = [];
                                        if (options.bytes !== $Array)
                                            object.nextKey = $util.newBuffer(object.nextKey);
                                    }
                                }
                                if (message.adoptionTime != null && $Object.hasOwnProperty.call(message, "adoptionTime"))
                                    object.adoptionTime = $root.proto.Timestamp.toObject(message.adoptionTime, options, _depth + 1);
                                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                    object.key = options.bytes === $String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.key) : message.key;
                                if (message.nextKey != null && $Object.hasOwnProperty.call(message, "nextKey"))
                                    object.nextKey = options.bytes === $String ? $util.base64.encode(message.nextKey, 0, message.nextKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.nextKey) : message.nextKey;
                                return object;
                            };

                            /**
                             * Converts this ProofKeySet to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            ProofKeySet.prototype.toJSON = function() {
                                return ProofKeySet.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for ProofKeySet
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.ProofKeySet
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            ProofKeySet.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.ProofKeySet";
                            };

                            return ProofKeySet;
                        })();

                        history.ProofKey = (function() {

                            /**
                             * Properties of a ProofKey.
                             * @typedef {Object} com.hedera.hapi.node.state.history.ProofKey.$Properties
                             * @property {number|Long|null} [nodeId] The node id.
                             * @property {Uint8Array|null} [key] The key.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a ProofKey.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IProofKey
                             * @augments com.hedera.hapi.node.state.history.ProofKey.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.ProofKey.$Properties instead.
                             */

                            /**
                             * Shape of a ProofKey.
                             * @typedef {com.hedera.hapi.node.state.history.ProofKey.$Properties} com.hedera.hapi.node.state.history.ProofKey.$Shape
                             */

                            /**
                             * Constructs a new ProofKey.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc A record of the proof key a node had in a particular address
                             * book. Necessary to keep at each point history so that nodes
                             * can verify the correct key was used to sign in transitions
                             * starting from the current address book; no matter how keys
                             * have been rotated from the time the address book was created.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.ProofKey.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const ProofKey = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The node id.
                             * @member {number|Long} nodeId
                             * @memberof com.hedera.hapi.node.state.history.ProofKey
                             * @instance
                             */
                            ProofKey.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The key.
                             * @member {Uint8Array} key
                             * @memberof com.hedera.hapi.node.state.history.ProofKey
                             * @instance
                             */
                            ProofKey.prototype.key = $util.newBuffer([]);

                            /**
                             * Creates a new ProofKey instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.ProofKey
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ProofKey.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.ProofKey} ProofKey instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.ProofKey.$Shape): com.hedera.hapi.node.state.history.ProofKey & com.hedera.hapi.node.state.history.ProofKey.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.ProofKey.$Properties): com.hedera.hapi.node.state.history.ProofKey;
                             * }}
                             */
                            ProofKey.create = function(properties) {
                                return new ProofKey(properties);
                            };

                            /**
                             * Encodes the specified ProofKey message. Does not implicitly {@link com.hedera.hapi.node.state.history.ProofKey.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.ProofKey
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ProofKey.$Properties} message ProofKey message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            ProofKey.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.nodeId);
                                if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.key);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified ProofKey message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.ProofKey.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.ProofKey
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ProofKey.$Properties} message ProofKey message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            ProofKey.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a ProofKey message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.ProofKey
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.ProofKey & com.hedera.hapi.node.state.history.ProofKey.$Shape} ProofKey
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            ProofKey.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.ProofKey();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.nodeId = value;
                                            else
                                                delete message.nodeId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.key = value;
                                            else
                                                delete message.key;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a ProofKey message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.ProofKey
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.ProofKey & com.hedera.hapi.node.state.history.ProofKey.$Shape} ProofKey
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            ProofKey.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a ProofKey message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.ProofKey
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            ProofKey.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                        return "nodeId: integer|Long expected";
                                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                    if (!(message.key && typeof message.key.length === "number" || $util.isString(message.key)))
                                        return "key: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a ProofKey message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.ProofKey
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.ProofKey} ProofKey
                             */
                            ProofKey.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.ProofKey)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.ProofKey: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.ProofKey();
                                if (object.nodeId != null)
                                    if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                        if ($util.Long)
                                            message.nodeId = $util.Long.fromValue(object.nodeId, true);
                                        else if (typeof object.nodeId === "string")
                                            message.nodeId = $parseInt(object.nodeId, 10);
                                        else if (typeof object.nodeId === "number")
                                            message.nodeId = object.nodeId;
                                        else if (typeof object.nodeId === "object")
                                            message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
                                if (object.key != null)
                                    if (object.key.length)
                                        if (typeof object.key === "string")
                                            $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                                        else if (object.key.length >= 0)
                                            message.key = object.key;
                                return message;
                            };

                            /**
                             * Creates a plain object from a ProofKey message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.ProofKey
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ProofKey} message ProofKey
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            ProofKey.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if (options.bytes === $String)
                                        object.key = "";
                                    else {
                                        object.key = [];
                                        if (options.bytes !== $Array)
                                            object.key = $util.newBuffer(object.key);
                                    }
                                }
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                                    else if (typeof message.nodeId === "number")
                                        object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                    else
                                        object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
                                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                    object.key = options.bytes === $String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.key) : message.key;
                                return object;
                            };

                            /**
                             * Converts this ProofKey to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.ProofKey
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            ProofKey.prototype.toJSON = function() {
                                return ProofKey.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for ProofKey
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.ProofKey
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            ProofKey.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.ProofKey";
                            };

                            return ProofKey;
                        })();

                        history.History = (function() {

                            /**
                             * Properties of a History.
                             * @typedef {Object} com.hedera.hapi.node.state.history.History.$Properties
                             * @property {Uint8Array|null} [addressBookHash] The address book hash of the new history.
                             * @property {Uint8Array|null} [metadata] The metadata associated to the address book.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a History.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IHistory
                             * @augments com.hedera.hapi.node.state.history.History.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.History.$Properties instead.
                             */

                            /**
                             * Shape of a History.
                             * @typedef {com.hedera.hapi.node.state.history.History.$Properties} com.hedera.hapi.node.state.history.History.$Shape
                             */

                            /**
                             * Constructs a new History.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc A piece of new history in the form of an address book hash and
                             * associated metadata.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.History.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const History = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The address book hash of the new history.
                             * @member {Uint8Array} addressBookHash
                             * @memberof com.hedera.hapi.node.state.history.History
                             * @instance
                             */
                            History.prototype.addressBookHash = $util.newBuffer([]);

                            /**
                             * The metadata associated to the address book.
                             * @member {Uint8Array} metadata
                             * @memberof com.hedera.hapi.node.state.history.History
                             * @instance
                             */
                            History.prototype.metadata = $util.newBuffer([]);

                            /**
                             * Creates a new History instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.History
                             * @static
                             * @param {com.hedera.hapi.node.state.history.History.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.History} History instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.History.$Shape): com.hedera.hapi.node.state.history.History & com.hedera.hapi.node.state.history.History.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.History.$Properties): com.hedera.hapi.node.state.history.History;
                             * }}
                             */
                            History.create = function(properties) {
                                return new History(properties);
                            };

                            /**
                             * Encodes the specified History message. Does not implicitly {@link com.hedera.hapi.node.state.history.History.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.History
                             * @static
                             * @param {com.hedera.hapi.node.state.history.History.$Properties} message History message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            History.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.addressBookHash != null && $Object.hasOwnProperty.call(message, "addressBookHash") && message.addressBookHash.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.addressBookHash);
                                if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata") && message.metadata.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.metadata);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified History message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.History.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.History
                             * @static
                             * @param {com.hedera.hapi.node.state.history.History.$Properties} message History message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            History.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a History message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.History
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.History & com.hedera.hapi.node.state.history.History.$Shape} History
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            History.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.History();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.addressBookHash = value;
                                            else
                                                delete message.addressBookHash;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.metadata = value;
                                            else
                                                delete message.metadata;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a History message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.History
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.History & com.hedera.hapi.node.state.history.History.$Shape} History
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            History.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a History message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.History
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            History.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.addressBookHash != null && $Object.hasOwnProperty.call(message, "addressBookHash"))
                                    if (!(message.addressBookHash && typeof message.addressBookHash.length === "number" || $util.isString(message.addressBookHash)))
                                        return "addressBookHash: buffer expected";
                                if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                                    if (!(message.metadata && typeof message.metadata.length === "number" || $util.isString(message.metadata)))
                                        return "metadata: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a History message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.History
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.History} History
                             */
                            History.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.History)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.History: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.History();
                                if (object.addressBookHash != null)
                                    if (object.addressBookHash.length)
                                        if (typeof object.addressBookHash === "string")
                                            $util.base64.decode(object.addressBookHash, message.addressBookHash = $util.newBuffer($util.base64.length(object.addressBookHash)), 0);
                                        else if (object.addressBookHash.length >= 0)
                                            message.addressBookHash = object.addressBookHash;
                                if (object.metadata != null)
                                    if (object.metadata.length)
                                        if (typeof object.metadata === "string")
                                            $util.base64.decode(object.metadata, message.metadata = $util.newBuffer($util.base64.length(object.metadata)), 0);
                                        else if (object.metadata.length >= 0)
                                            message.metadata = object.metadata;
                                return message;
                            };

                            /**
                             * Creates a plain object from a History message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.History
                             * @static
                             * @param {com.hedera.hapi.node.state.history.History} message History
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            History.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.addressBookHash = "";
                                    else {
                                        object.addressBookHash = [];
                                        if (options.bytes !== $Array)
                                            object.addressBookHash = $util.newBuffer(object.addressBookHash);
                                    }
                                    if (options.bytes === $String)
                                        object.metadata = "";
                                    else {
                                        object.metadata = [];
                                        if (options.bytes !== $Array)
                                            object.metadata = $util.newBuffer(object.metadata);
                                    }
                                }
                                if (message.addressBookHash != null && $Object.hasOwnProperty.call(message, "addressBookHash"))
                                    object.addressBookHash = options.bytes === $String ? $util.base64.encode(message.addressBookHash, 0, message.addressBookHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.addressBookHash) : message.addressBookHash;
                                if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                                    object.metadata = options.bytes === $String ? $util.base64.encode(message.metadata, 0, message.metadata.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.metadata) : message.metadata;
                                return object;
                            };

                            /**
                             * Converts this History to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.History
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            History.prototype.toJSON = function() {
                                return History.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for History
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.History
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            History.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.History";
                            };

                            return History;
                        })();

                        history.HistoryProof = (function() {

                            /**
                             * Properties of a HistoryProof.
                             * @typedef {Object} com.hedera.hapi.node.state.history.HistoryProof.$Properties
                             * @property {Array.<com.hedera.hapi.node.state.history.ProofKey.$Properties>|null} [targetProofKeys] The proof keys for the target address book, needed to keep
                             * constructing proofs after adopting the target address book's
                             * roster at a handoff.
                             * @property {com.hedera.hapi.node.state.history.History.$Properties|null} [targetHistory] The target history of the proof.
                             * @property {com.hedera.hapi.node.state.history.ChainOfTrustProof.$Properties|null} [chainOfTrustProof] The proof of chain of trust from the ledger id to the target
                             * history's metadata. May be switched from AggregatedNodeSignatures
                             * to a recursive proof when one becomes available.
                             * @property {Uint8Array|null} [uncompressedWrapsProof] If set, the uncompressed proof of chain of trust from the ledger id to
                             * the target address book; the uncompressed version of the WRAPS proof is
                             * required to keep extending the chain of trust.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HistoryProof.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IHistoryProof
                             * @augments com.hedera.hapi.node.state.history.HistoryProof.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.HistoryProof.$Properties instead.
                             */

                            /**
                             * Shape of a HistoryProof.
                             * @typedef {{
                             *   targetProofKeys?: Array.<com.hedera.hapi.node.state.history.ProofKey.$Shape>|null;
                             *   targetHistory?: com.hedera.hapi.node.state.history.History.$Shape|null;
                             *   chainOfTrustProof?: com.hedera.hapi.node.state.history.ChainOfTrustProof.$Shape|null;
                             *   uncompressedWrapsProof?: Uint8Array|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * }} com.hedera.hapi.node.state.history.HistoryProof.$Shape
                             */

                            /**
                             * Constructs a new HistoryProof.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc A proof that some address book history belongs to the ledger id's chain of trust.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.HistoryProof.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HistoryProof = function (properties) {
                                this.targetProofKeys = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The proof keys for the target address book, needed to keep
                             * constructing proofs after adopting the target address book's
                             * roster at a handoff.
                             * @member {Array.<com.hedera.hapi.node.state.history.ProofKey.$Properties>} targetProofKeys
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @instance
                             */
                            HistoryProof.prototype.targetProofKeys = $util.emptyArray;

                            /**
                             * The target history of the proof.
                             * @member {com.hedera.hapi.node.state.history.History.$Properties|null|undefined} targetHistory
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @instance
                             */
                            HistoryProof.prototype.targetHistory = null;

                            /**
                             * The proof of chain of trust from the ledger id to the target
                             * history's metadata. May be switched from AggregatedNodeSignatures
                             * to a recursive proof when one becomes available.
                             * @member {com.hedera.hapi.node.state.history.ChainOfTrustProof.$Properties|null|undefined} chainOfTrustProof
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @instance
                             */
                            HistoryProof.prototype.chainOfTrustProof = null;

                            /**
                             * If set, the uncompressed proof of chain of trust from the ledger id to
                             * the target address book; the uncompressed version of the WRAPS proof is
                             * required to keep extending the chain of trust.
                             * @member {Uint8Array} uncompressedWrapsProof
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @instance
                             */
                            HistoryProof.prototype.uncompressedWrapsProof = $util.newBuffer([]);

                            /**
                             * Creates a new HistoryProof instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistoryProof.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.HistoryProof} HistoryProof instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.HistoryProof.$Shape): com.hedera.hapi.node.state.history.HistoryProof & com.hedera.hapi.node.state.history.HistoryProof.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.HistoryProof.$Properties): com.hedera.hapi.node.state.history.HistoryProof;
                             * }}
                             */
                            HistoryProof.create = function(properties) {
                                return new HistoryProof(properties);
                            };

                            /**
                             * Encodes the specified HistoryProof message. Does not implicitly {@link com.hedera.hapi.node.state.history.HistoryProof.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistoryProof.$Properties} message HistoryProof message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistoryProof.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.targetProofKeys != null && message.targetProofKeys.length)
                                    for (let i = 0; i < message.targetProofKeys.length; ++i)
                                        $root.com.hedera.hapi.node.state.history.ProofKey.encode(message.targetProofKeys[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.targetHistory != null && $Object.hasOwnProperty.call(message, "targetHistory"))
                                    $root.com.hedera.hapi.node.state.history.History.encode(message.targetHistory, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.chainOfTrustProof != null && $Object.hasOwnProperty.call(message, "chainOfTrustProof"))
                                    $root.com.hedera.hapi.node.state.history.ChainOfTrustProof.encode(message.chainOfTrustProof, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                                if (message.uncompressedWrapsProof != null && $Object.hasOwnProperty.call(message, "uncompressedWrapsProof") && message.uncompressedWrapsProof.length)
                                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.uncompressedWrapsProof);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HistoryProof message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.HistoryProof.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistoryProof.$Properties} message HistoryProof message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistoryProof.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HistoryProof message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.HistoryProof & com.hedera.hapi.node.state.history.HistoryProof.$Shape} HistoryProof
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistoryProof.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.HistoryProof();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.targetProofKeys && message.targetProofKeys.length))
                                                message.targetProofKeys = [];
                                            message.targetProofKeys.push($root.com.hedera.hapi.node.state.history.ProofKey.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.targetHistory = $root.com.hedera.hapi.node.state.history.History.decode(reader, reader.uint32(), $undefined, _depth + 1, message.targetHistory);
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            message.chainOfTrustProof = $root.com.hedera.hapi.node.state.history.ChainOfTrustProof.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chainOfTrustProof);
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.uncompressedWrapsProof = value;
                                            else
                                                delete message.uncompressedWrapsProof;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HistoryProof message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.HistoryProof & com.hedera.hapi.node.state.history.HistoryProof.$Shape} HistoryProof
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistoryProof.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HistoryProof message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HistoryProof.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.targetProofKeys != null && $Object.hasOwnProperty.call(message, "targetProofKeys")) {
                                    if (!$Array.isArray(message.targetProofKeys))
                                        return "targetProofKeys: array expected";
                                    for (let i = 0; i < message.targetProofKeys.length; ++i) {
                                        let error = $root.com.hedera.hapi.node.state.history.ProofKey.verify(message.targetProofKeys[i], _depth + 1);
                                        if (error)
                                            return "targetProofKeys." + error;
                                    }
                                }
                                if (message.targetHistory != null && $Object.hasOwnProperty.call(message, "targetHistory")) {
                                    let error = $root.com.hedera.hapi.node.state.history.History.verify(message.targetHistory, _depth + 1);
                                    if (error)
                                        return "targetHistory." + error;
                                }
                                if (message.chainOfTrustProof != null && $Object.hasOwnProperty.call(message, "chainOfTrustProof")) {
                                    let error = $root.com.hedera.hapi.node.state.history.ChainOfTrustProof.verify(message.chainOfTrustProof, _depth + 1);
                                    if (error)
                                        return "chainOfTrustProof." + error;
                                }
                                if (message.uncompressedWrapsProof != null && $Object.hasOwnProperty.call(message, "uncompressedWrapsProof"))
                                    if (!(message.uncompressedWrapsProof && typeof message.uncompressedWrapsProof.length === "number" || $util.isString(message.uncompressedWrapsProof)))
                                        return "uncompressedWrapsProof: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a HistoryProof message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.HistoryProof} HistoryProof
                             */
                            HistoryProof.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.HistoryProof)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.HistoryProof: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.HistoryProof();
                                if (object.targetProofKeys) {
                                    if (!$Array.isArray(object.targetProofKeys))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.HistoryProof.targetProofKeys: array expected");
                                    message.targetProofKeys = $Array(object.targetProofKeys.length);
                                    for (let i = 0; i < object.targetProofKeys.length; ++i) {
                                        if (!$util.isObject(object.targetProofKeys[i]))
                                            throw $TypeError(".com.hedera.hapi.node.state.history.HistoryProof.targetProofKeys: object expected");
                                        message.targetProofKeys[i] = $root.com.hedera.hapi.node.state.history.ProofKey.fromObject(object.targetProofKeys[i], _depth + 1);
                                    }
                                }
                                if (object.targetHistory != null) {
                                    if (!$util.isObject(object.targetHistory))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.HistoryProof.targetHistory: object expected");
                                    message.targetHistory = $root.com.hedera.hapi.node.state.history.History.fromObject(object.targetHistory, _depth + 1);
                                }
                                if (object.chainOfTrustProof != null) {
                                    if (!$util.isObject(object.chainOfTrustProof))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.HistoryProof.chainOfTrustProof: object expected");
                                    message.chainOfTrustProof = $root.com.hedera.hapi.node.state.history.ChainOfTrustProof.fromObject(object.chainOfTrustProof, _depth + 1);
                                }
                                if (object.uncompressedWrapsProof != null)
                                    if (object.uncompressedWrapsProof.length)
                                        if (typeof object.uncompressedWrapsProof === "string")
                                            $util.base64.decode(object.uncompressedWrapsProof, message.uncompressedWrapsProof = $util.newBuffer($util.base64.length(object.uncompressedWrapsProof)), 0);
                                        else if (object.uncompressedWrapsProof.length >= 0)
                                            message.uncompressedWrapsProof = object.uncompressedWrapsProof;
                                return message;
                            };

                            /**
                             * Creates a plain object from a HistoryProof message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistoryProof} message HistoryProof
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HistoryProof.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults)
                                    object.targetProofKeys = [];
                                if (options.defaults) {
                                    object.targetHistory = null;
                                    object.chainOfTrustProof = null;
                                    if (options.bytes === $String)
                                        object.uncompressedWrapsProof = "";
                                    else {
                                        object.uncompressedWrapsProof = [];
                                        if (options.bytes !== $Array)
                                            object.uncompressedWrapsProof = $util.newBuffer(object.uncompressedWrapsProof);
                                    }
                                }
                                if (message.targetProofKeys && message.targetProofKeys.length) {
                                    object.targetProofKeys = $Array(message.targetProofKeys.length);
                                    for (let j = 0; j < message.targetProofKeys.length; ++j)
                                        object.targetProofKeys[j] = $root.com.hedera.hapi.node.state.history.ProofKey.toObject(message.targetProofKeys[j], options, _depth + 1);
                                }
                                if (message.targetHistory != null && $Object.hasOwnProperty.call(message, "targetHistory"))
                                    object.targetHistory = $root.com.hedera.hapi.node.state.history.History.toObject(message.targetHistory, options, _depth + 1);
                                if (message.chainOfTrustProof != null && $Object.hasOwnProperty.call(message, "chainOfTrustProof"))
                                    object.chainOfTrustProof = $root.com.hedera.hapi.node.state.history.ChainOfTrustProof.toObject(message.chainOfTrustProof, options, _depth + 1);
                                if (message.uncompressedWrapsProof != null && $Object.hasOwnProperty.call(message, "uncompressedWrapsProof"))
                                    object.uncompressedWrapsProof = options.bytes === $String ? $util.base64.encode(message.uncompressedWrapsProof, 0, message.uncompressedWrapsProof.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.uncompressedWrapsProof) : message.uncompressedWrapsProof;
                                return object;
                            };

                            /**
                             * Converts this HistoryProof to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HistoryProof.prototype.toJSON = function() {
                                return HistoryProof.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HistoryProof
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.HistoryProof
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HistoryProof.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.HistoryProof";
                            };

                            return HistoryProof;
                        })();

                        history.HistoryProofConstruction = (function() {

                            /**
                             * Properties of a HistoryProofConstruction.
                             * @typedef {Object} com.hedera.hapi.node.state.history.HistoryProofConstruction.$Properties
                             * @property {number|Long|null} [constructionId] The construction id.
                             * @property {Uint8Array|null} [sourceRosterHash] The hash of the roster whose weights are used to determine when
                             * certain thresholds are during construction.
                             * @property {Uint8Array|null} [targetRosterHash] The hash of the roster whose weights are used to assess progress
                             * toward obtaining proof keys for parties that hold at least a
                             * strong minority of the stake in that roster.
                             * @property {proto.Timestamp.$Properties|null} [gracePeriodEndTime] If the network is still gathering proof keys for this
                             * construction, the next time at which nodes should stop waiting
                             * for tardy proof keys and assemble the history to be proven as
                             * soon as it has the associated metadata and proof keys for nodes
                             * with >2/3 weight in the target roster.
                             * @property {proto.Timestamp.$Properties|null} [assemblyStartTime] If the network has gathered enough proof keys to assemble the
                             * history for this construction, the cutoff time at which those
                             * keys must have been adopted to be included in the final history.
                             * @property {com.hedera.hapi.node.state.history.HistoryProof.$Properties|null} [targetProof] When this construction is complete, the proof that the target
                             * roster's address book and associated metadata belong to the
                             * ledger id's chain of trust.
                             * @property {string|null} [failureReason] If set, the reason the construction failed.
                             * @property {com.hedera.hapi.node.state.history.WrapsSigningState.$Properties|null} [wrapsSigningState] If set, the state of the WRAPS signing protocol for this construction.
                             * @property {number|null} [wrapsRetryCount] The number of times the network has had to restart this construction.
                             * @property {"gracePeriodEndTime"|"assemblyStartTime"|"targetProof"|"failureReason"|"wrapsSigningState"} [proofState] HistoryProofConstruction proofState
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HistoryProofConstruction.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IHistoryProofConstruction
                             * @augments com.hedera.hapi.node.state.history.HistoryProofConstruction.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.HistoryProofConstruction.$Properties instead.
                             */

                            /**
                             * Narrowed shape of a HistoryProofConstruction.
                             * @typedef {{
                             *   constructionId?: number|Long|null;
                             *   sourceRosterHash?: Uint8Array|null;
                             *   targetRosterHash?: Uint8Array|null;
                             *   gracePeriodEndTime?: proto.Timestamp.$Shape|null;
                             *   assemblyStartTime?: proto.Timestamp.$Shape|null;
                             *   targetProof?: com.hedera.hapi.node.state.history.HistoryProof.$Shape|null;
                             *   failureReason?: string|null;
                             *   wrapsSigningState?: com.hedera.hapi.node.state.history.WrapsSigningState.$Shape|null;
                             *   wrapsRetryCount?: number|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * } & (
                             *   ({ proofState?: undefined; gracePeriodEndTime?: null; assemblyStartTime?: null; targetProof?: null; failureReason?: null; wrapsSigningState?: null }|{ proofState?: "gracePeriodEndTime"; gracePeriodEndTime: proto.Timestamp.$Shape; assemblyStartTime?: null; targetProof?: null; failureReason?: null; wrapsSigningState?: null }|{ proofState?: "assemblyStartTime"; gracePeriodEndTime?: null; assemblyStartTime: proto.Timestamp.$Shape; targetProof?: null; failureReason?: null; wrapsSigningState?: null }|{ proofState?: "targetProof"; gracePeriodEndTime?: null; assemblyStartTime?: null; targetProof: com.hedera.hapi.node.state.history.HistoryProof.$Shape; failureReason?: null; wrapsSigningState?: null }|{ proofState?: "failureReason"; gracePeriodEndTime?: null; assemblyStartTime?: null; targetProof?: null; failureReason: string; wrapsSigningState?: null }|{ proofState?: "wrapsSigningState"; gracePeriodEndTime?: null; assemblyStartTime?: null; targetProof?: null; failureReason?: null; wrapsSigningState: com.hedera.hapi.node.state.history.WrapsSigningState.$Shape })
                             * )} com.hedera.hapi.node.state.history.HistoryProofConstruction.$Shape
                             */

                            /**
                             * Constructs a new HistoryProofConstruction.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc Summary of the status of constructing a metadata proof, necessary to
                             * ensure deterministic construction ending in a roster with sufficient
                             * weight to enact its own constructions.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.HistoryProofConstruction.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HistoryProofConstruction = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The construction id.
                             * @member {number|Long} constructionId
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @instance
                             */
                            HistoryProofConstruction.prototype.constructionId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The hash of the roster whose weights are used to determine when
                             * certain thresholds are during construction.
                             * @member {Uint8Array} sourceRosterHash
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @instance
                             */
                            HistoryProofConstruction.prototype.sourceRosterHash = $util.newBuffer([]);

                            /**
                             * The hash of the roster whose weights are used to assess progress
                             * toward obtaining proof keys for parties that hold at least a
                             * strong minority of the stake in that roster.
                             * @member {Uint8Array} targetRosterHash
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @instance
                             */
                            HistoryProofConstruction.prototype.targetRosterHash = $util.newBuffer([]);

                            /**
                             * If the network is still gathering proof keys for this
                             * construction, the next time at which nodes should stop waiting
                             * for tardy proof keys and assemble the history to be proven as
                             * soon as it has the associated metadata and proof keys for nodes
                             * with >2/3 weight in the target roster.
                             * @member {proto.Timestamp.$Properties|null|undefined} gracePeriodEndTime
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @instance
                             */
                            HistoryProofConstruction.prototype.gracePeriodEndTime = null;

                            /**
                             * If the network has gathered enough proof keys to assemble the
                             * history for this construction, the cutoff time at which those
                             * keys must have been adopted to be included in the final history.
                             * @member {proto.Timestamp.$Properties|null|undefined} assemblyStartTime
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @instance
                             */
                            HistoryProofConstruction.prototype.assemblyStartTime = null;

                            /**
                             * When this construction is complete, the proof that the target
                             * roster's address book and associated metadata belong to the
                             * ledger id's chain of trust.
                             * @member {com.hedera.hapi.node.state.history.HistoryProof.$Properties|null|undefined} targetProof
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @instance
                             */
                            HistoryProofConstruction.prototype.targetProof = null;

                            /**
                             * If set, the reason the construction failed.
                             * @member {string|null|undefined} failureReason
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @instance
                             */
                            HistoryProofConstruction.prototype.failureReason = null;

                            /**
                             * If set, the state of the WRAPS signing protocol for this construction.
                             * @member {com.hedera.hapi.node.state.history.WrapsSigningState.$Properties|null|undefined} wrapsSigningState
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @instance
                             */
                            HistoryProofConstruction.prototype.wrapsSigningState = null;

                            /**
                             * The number of times the network has had to restart this construction.
                             * @member {number} wrapsRetryCount
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @instance
                             */
                            HistoryProofConstruction.prototype.wrapsRetryCount = 0;

                            // OneOf field names bound to virtual getters and setters
                            let $oneOfFields;

                            /**
                             * HistoryProofConstruction proofState.
                             * @member {"gracePeriodEndTime"|"assemblyStartTime"|"targetProof"|"failureReason"|"wrapsSigningState"|undefined} proofState
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @instance
                             */
                            $Object.defineProperty(HistoryProofConstruction.prototype, "proofState", {
                                get: $util.oneOfGetter($oneOfFields = ["gracePeriodEndTime", "assemblyStartTime", "targetProof", "failureReason", "wrapsSigningState"]),
                                set: $util.oneOfSetter($oneOfFields)
                            });

                            /**
                             * Creates a new HistoryProofConstruction instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistoryProofConstruction.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.HistoryProofConstruction} HistoryProofConstruction instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.HistoryProofConstruction.$Shape): com.hedera.hapi.node.state.history.HistoryProofConstruction & com.hedera.hapi.node.state.history.HistoryProofConstruction.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.HistoryProofConstruction.$Properties): com.hedera.hapi.node.state.history.HistoryProofConstruction;
                             * }}
                             */
                            HistoryProofConstruction.create = function(properties) {
                                return new HistoryProofConstruction(properties);
                            };

                            /**
                             * Encodes the specified HistoryProofConstruction message. Does not implicitly {@link com.hedera.hapi.node.state.history.HistoryProofConstruction.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistoryProofConstruction.$Properties} message HistoryProofConstruction message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistoryProofConstruction.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId") && (typeof message.constructionId === "object" ? message.constructionId.low || message.constructionId.high : message.constructionId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.constructionId);
                                if (message.sourceRosterHash != null && $Object.hasOwnProperty.call(message, "sourceRosterHash") && message.sourceRosterHash.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.sourceRosterHash);
                                if (message.targetRosterHash != null && $Object.hasOwnProperty.call(message, "targetRosterHash") && message.targetRosterHash.length)
                                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.targetRosterHash);
                                if (message.gracePeriodEndTime != null && $Object.hasOwnProperty.call(message, "gracePeriodEndTime"))
                                    $root.proto.Timestamp.encode(message.gracePeriodEndTime, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                                if (message.assemblyStartTime != null && $Object.hasOwnProperty.call(message, "assemblyStartTime"))
                                    $root.proto.Timestamp.encode(message.assemblyStartTime, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                                if (message.targetProof != null && $Object.hasOwnProperty.call(message, "targetProof"))
                                    $root.com.hedera.hapi.node.state.history.HistoryProof.encode(message.targetProof, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                                if (message.failureReason != null && $Object.hasOwnProperty.call(message, "failureReason"))
                                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.failureReason);
                                if (message.wrapsSigningState != null && $Object.hasOwnProperty.call(message, "wrapsSigningState"))
                                    $root.com.hedera.hapi.node.state.history.WrapsSigningState.encode(message.wrapsSigningState, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
                                if (message.wrapsRetryCount != null && $Object.hasOwnProperty.call(message, "wrapsRetryCount") && message.wrapsRetryCount !== 0)
                                    writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.wrapsRetryCount);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HistoryProofConstruction message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.HistoryProofConstruction.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistoryProofConstruction.$Properties} message HistoryProofConstruction message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistoryProofConstruction.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HistoryProofConstruction message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.HistoryProofConstruction & com.hedera.hapi.node.state.history.HistoryProofConstruction.$Shape} HistoryProofConstruction
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistoryProofConstruction.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.HistoryProofConstruction();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.constructionId = value;
                                            else
                                                delete message.constructionId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.sourceRosterHash = value;
                                            else
                                                delete message.sourceRosterHash;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.targetRosterHash = value;
                                            else
                                                delete message.targetRosterHash;
                                            continue;
                                        }
                                    case 5: {
                                            if (wireType !== 2)
                                                break;
                                            message.gracePeriodEndTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.gracePeriodEndTime);
                                            message.proofState = "gracePeriodEndTime";
                                            continue;
                                        }
                                    case 6: {
                                            if (wireType !== 2)
                                                break;
                                            message.assemblyStartTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.assemblyStartTime);
                                            message.proofState = "assemblyStartTime";
                                            continue;
                                        }
                                    case 7: {
                                            if (wireType !== 2)
                                                break;
                                            message.targetProof = $root.com.hedera.hapi.node.state.history.HistoryProof.decode(reader, reader.uint32(), $undefined, _depth + 1, message.targetProof);
                                            message.proofState = "targetProof";
                                            continue;
                                        }
                                    case 8: {
                                            if (wireType !== 2)
                                                break;
                                            message.failureReason = reader.stringVerify();
                                            message.proofState = "failureReason";
                                            continue;
                                        }
                                    case 9: {
                                            if (wireType !== 2)
                                                break;
                                            message.wrapsSigningState = $root.com.hedera.hapi.node.state.history.WrapsSigningState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.wrapsSigningState);
                                            message.proofState = "wrapsSigningState";
                                            continue;
                                        }
                                    case 10: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.uint32())
                                                message.wrapsRetryCount = value;
                                            else
                                                delete message.wrapsRetryCount;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HistoryProofConstruction message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.HistoryProofConstruction & com.hedera.hapi.node.state.history.HistoryProofConstruction.$Shape} HistoryProofConstruction
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistoryProofConstruction.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HistoryProofConstruction message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HistoryProofConstruction.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                let properties = {};
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (!$util.isInteger(message.constructionId) && !(message.constructionId && $util.isInteger(message.constructionId.low) && $util.isInteger(message.constructionId.high)))
                                        return "constructionId: integer|Long expected";
                                if (message.sourceRosterHash != null && $Object.hasOwnProperty.call(message, "sourceRosterHash"))
                                    if (!(message.sourceRosterHash && typeof message.sourceRosterHash.length === "number" || $util.isString(message.sourceRosterHash)))
                                        return "sourceRosterHash: buffer expected";
                                if (message.targetRosterHash != null && $Object.hasOwnProperty.call(message, "targetRosterHash"))
                                    if (!(message.targetRosterHash && typeof message.targetRosterHash.length === "number" || $util.isString(message.targetRosterHash)))
                                        return "targetRosterHash: buffer expected";
                                if (message.gracePeriodEndTime != null && $Object.hasOwnProperty.call(message, "gracePeriodEndTime")) {
                                    properties.proofState = 1;
                                    {
                                        let error = $root.proto.Timestamp.verify(message.gracePeriodEndTime, _depth + 1);
                                        if (error)
                                            return "gracePeriodEndTime." + error;
                                    }
                                }
                                if (message.assemblyStartTime != null && $Object.hasOwnProperty.call(message, "assemblyStartTime")) {
                                    if (properties.proofState === 1)
                                        return "proofState: multiple values";
                                    properties.proofState = 1;
                                    {
                                        let error = $root.proto.Timestamp.verify(message.assemblyStartTime, _depth + 1);
                                        if (error)
                                            return "assemblyStartTime." + error;
                                    }
                                }
                                if (message.targetProof != null && $Object.hasOwnProperty.call(message, "targetProof")) {
                                    if (properties.proofState === 1)
                                        return "proofState: multiple values";
                                    properties.proofState = 1;
                                    {
                                        let error = $root.com.hedera.hapi.node.state.history.HistoryProof.verify(message.targetProof, _depth + 1);
                                        if (error)
                                            return "targetProof." + error;
                                    }
                                }
                                if (message.failureReason != null && $Object.hasOwnProperty.call(message, "failureReason")) {
                                    if (properties.proofState === 1)
                                        return "proofState: multiple values";
                                    properties.proofState = 1;
                                    if (!$util.isString(message.failureReason))
                                        return "failureReason: string expected";
                                }
                                if (message.wrapsSigningState != null && $Object.hasOwnProperty.call(message, "wrapsSigningState")) {
                                    if (properties.proofState === 1)
                                        return "proofState: multiple values";
                                    properties.proofState = 1;
                                    {
                                        let error = $root.com.hedera.hapi.node.state.history.WrapsSigningState.verify(message.wrapsSigningState, _depth + 1);
                                        if (error)
                                            return "wrapsSigningState." + error;
                                    }
                                }
                                if (message.wrapsRetryCount != null && $Object.hasOwnProperty.call(message, "wrapsRetryCount"))
                                    if (!$util.isInteger(message.wrapsRetryCount))
                                        return "wrapsRetryCount: integer expected";
                                return null;
                            };

                            /**
                             * Creates a HistoryProofConstruction message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.HistoryProofConstruction} HistoryProofConstruction
                             */
                            HistoryProofConstruction.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.HistoryProofConstruction)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.HistoryProofConstruction: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.HistoryProofConstruction();
                                if (object.constructionId != null)
                                    if (typeof object.constructionId === "object" ? object.constructionId.low || object.constructionId.high : $Number(object.constructionId) !== 0)
                                        if ($util.Long)
                                            message.constructionId = $util.Long.fromValue(object.constructionId, true);
                                        else if (typeof object.constructionId === "string")
                                            message.constructionId = $parseInt(object.constructionId, 10);
                                        else if (typeof object.constructionId === "number")
                                            message.constructionId = object.constructionId;
                                        else if (typeof object.constructionId === "object")
                                            message.constructionId = new $util.LongBits(object.constructionId.low >>> 0, object.constructionId.high >>> 0).toNumber(true);
                                if (object.sourceRosterHash != null)
                                    if (object.sourceRosterHash.length)
                                        if (typeof object.sourceRosterHash === "string")
                                            $util.base64.decode(object.sourceRosterHash, message.sourceRosterHash = $util.newBuffer($util.base64.length(object.sourceRosterHash)), 0);
                                        else if (object.sourceRosterHash.length >= 0)
                                            message.sourceRosterHash = object.sourceRosterHash;
                                if (object.targetRosterHash != null)
                                    if (object.targetRosterHash.length)
                                        if (typeof object.targetRosterHash === "string")
                                            $util.base64.decode(object.targetRosterHash, message.targetRosterHash = $util.newBuffer($util.base64.length(object.targetRosterHash)), 0);
                                        else if (object.targetRosterHash.length >= 0)
                                            message.targetRosterHash = object.targetRosterHash;
                                if (object.gracePeriodEndTime != null) {
                                    if (!$util.isObject(object.gracePeriodEndTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.HistoryProofConstruction.gracePeriodEndTime: object expected");
                                    message.gracePeriodEndTime = $root.proto.Timestamp.fromObject(object.gracePeriodEndTime, _depth + 1);
                                }
                                if (object.assemblyStartTime != null) {
                                    if (!$util.isObject(object.assemblyStartTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.HistoryProofConstruction.assemblyStartTime: object expected");
                                    message.assemblyStartTime = $root.proto.Timestamp.fromObject(object.assemblyStartTime, _depth + 1);
                                }
                                if (object.targetProof != null) {
                                    if (!$util.isObject(object.targetProof))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.HistoryProofConstruction.targetProof: object expected");
                                    message.targetProof = $root.com.hedera.hapi.node.state.history.HistoryProof.fromObject(object.targetProof, _depth + 1);
                                }
                                if (object.failureReason != null)
                                    message.failureReason = $String(object.failureReason);
                                if (object.wrapsSigningState != null) {
                                    if (!$util.isObject(object.wrapsSigningState))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.HistoryProofConstruction.wrapsSigningState: object expected");
                                    message.wrapsSigningState = $root.com.hedera.hapi.node.state.history.WrapsSigningState.fromObject(object.wrapsSigningState, _depth + 1);
                                }
                                if (object.wrapsRetryCount != null)
                                    if ($Number(object.wrapsRetryCount) !== 0)
                                        message.wrapsRetryCount = object.wrapsRetryCount >>> 0;
                                return message;
                            };

                            /**
                             * Creates a plain object from a HistoryProofConstruction message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistoryProofConstruction} message HistoryProofConstruction
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HistoryProofConstruction.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.constructionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.constructionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if (options.bytes === $String)
                                        object.sourceRosterHash = "";
                                    else {
                                        object.sourceRosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.sourceRosterHash = $util.newBuffer(object.sourceRosterHash);
                                    }
                                    if (options.bytes === $String)
                                        object.targetRosterHash = "";
                                    else {
                                        object.targetRosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.targetRosterHash = $util.newBuffer(object.targetRosterHash);
                                    }
                                    object.wrapsRetryCount = 0;
                                }
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.constructionId = typeof message.constructionId === "number" ? $BigInt(message.constructionId) : $util.Long.fromBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0, true).toBigInt();
                                    else if (typeof message.constructionId === "number")
                                        object.constructionId = options.longs === $String ? $String(message.constructionId) : message.constructionId;
                                    else
                                        object.constructionId = options.longs === $String ? $util.Long.prototype.toString.call(message.constructionId) : options.longs === $Number ? new $util.LongBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0).toNumber(true) : message.constructionId;
                                if (message.sourceRosterHash != null && $Object.hasOwnProperty.call(message, "sourceRosterHash"))
                                    object.sourceRosterHash = options.bytes === $String ? $util.base64.encode(message.sourceRosterHash, 0, message.sourceRosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.sourceRosterHash) : message.sourceRosterHash;
                                if (message.targetRosterHash != null && $Object.hasOwnProperty.call(message, "targetRosterHash"))
                                    object.targetRosterHash = options.bytes === $String ? $util.base64.encode(message.targetRosterHash, 0, message.targetRosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.targetRosterHash) : message.targetRosterHash;
                                if (message.gracePeriodEndTime != null && $Object.hasOwnProperty.call(message, "gracePeriodEndTime")) {
                                    object.gracePeriodEndTime = $root.proto.Timestamp.toObject(message.gracePeriodEndTime, options, _depth + 1);
                                    if (options.oneofs)
                                        object.proofState = "gracePeriodEndTime";
                                }
                                if (message.assemblyStartTime != null && $Object.hasOwnProperty.call(message, "assemblyStartTime")) {
                                    object.assemblyStartTime = $root.proto.Timestamp.toObject(message.assemblyStartTime, options, _depth + 1);
                                    if (options.oneofs)
                                        object.proofState = "assemblyStartTime";
                                }
                                if (message.targetProof != null && $Object.hasOwnProperty.call(message, "targetProof")) {
                                    object.targetProof = $root.com.hedera.hapi.node.state.history.HistoryProof.toObject(message.targetProof, options, _depth + 1);
                                    if (options.oneofs)
                                        object.proofState = "targetProof";
                                }
                                if (message.failureReason != null && $Object.hasOwnProperty.call(message, "failureReason")) {
                                    object.failureReason = message.failureReason;
                                    if (options.oneofs)
                                        object.proofState = "failureReason";
                                }
                                if (message.wrapsSigningState != null && $Object.hasOwnProperty.call(message, "wrapsSigningState")) {
                                    object.wrapsSigningState = $root.com.hedera.hapi.node.state.history.WrapsSigningState.toObject(message.wrapsSigningState, options, _depth + 1);
                                    if (options.oneofs)
                                        object.proofState = "wrapsSigningState";
                                }
                                if (message.wrapsRetryCount != null && $Object.hasOwnProperty.call(message, "wrapsRetryCount"))
                                    object.wrapsRetryCount = message.wrapsRetryCount;
                                return object;
                            };

                            /**
                             * Converts this HistoryProofConstruction to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HistoryProofConstruction.prototype.toJSON = function() {
                                return HistoryProofConstruction.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HistoryProofConstruction
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofConstruction
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HistoryProofConstruction.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.HistoryProofConstruction";
                            };

                            return HistoryProofConstruction;
                        })();

                        history.WrapsSigningState = (function() {

                            /**
                             * Properties of a WrapsSigningState.
                             * @typedef {Object} com.hedera.hapi.node.state.history.WrapsSigningState.$Properties
                             * @property {com.hedera.hapi.node.state.history.WrapsPhase|null} [phase] The phase of the WRAPS protocol.
                             * @property {proto.Timestamp.$Properties|null} [gracePeriodEndTime] If the network is still gathering WRAPS messages for the R2 or R3
                             * protocol phase, the next time at which nodes should stop waiting
                             * for tardy messages from R1 participants and start over.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a WrapsSigningState.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IWrapsSigningState
                             * @augments com.hedera.hapi.node.state.history.WrapsSigningState.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.WrapsSigningState.$Properties instead.
                             */

                            /**
                             * Shape of a WrapsSigningState.
                             * @typedef {com.hedera.hapi.node.state.history.WrapsSigningState.$Properties} com.hedera.hapi.node.state.history.WrapsSigningState.$Shape
                             */

                            /**
                             * Constructs a new WrapsSigningState.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc The state of an ongoing WRAPS signature protocol.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.WrapsSigningState.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const WrapsSigningState = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The phase of the WRAPS protocol.
                             * @member {com.hedera.hapi.node.state.history.WrapsPhase} phase
                             * @memberof com.hedera.hapi.node.state.history.WrapsSigningState
                             * @instance
                             */
                            WrapsSigningState.prototype.phase = 0;

                            /**
                             * If the network is still gathering WRAPS messages for the R2 or R3
                             * protocol phase, the next time at which nodes should stop waiting
                             * for tardy messages from R1 participants and start over.
                             * @member {proto.Timestamp.$Properties|null|undefined} gracePeriodEndTime
                             * @memberof com.hedera.hapi.node.state.history.WrapsSigningState
                             * @instance
                             */
                            WrapsSigningState.prototype.gracePeriodEndTime = null;

                            /**
                             * Creates a new WrapsSigningState instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.WrapsSigningState
                             * @static
                             * @param {com.hedera.hapi.node.state.history.WrapsSigningState.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.WrapsSigningState} WrapsSigningState instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.WrapsSigningState.$Shape): com.hedera.hapi.node.state.history.WrapsSigningState & com.hedera.hapi.node.state.history.WrapsSigningState.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.WrapsSigningState.$Properties): com.hedera.hapi.node.state.history.WrapsSigningState;
                             * }}
                             */
                            WrapsSigningState.create = function(properties) {
                                return new WrapsSigningState(properties);
                            };

                            /**
                             * Encodes the specified WrapsSigningState message. Does not implicitly {@link com.hedera.hapi.node.state.history.WrapsSigningState.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.WrapsSigningState
                             * @static
                             * @param {com.hedera.hapi.node.state.history.WrapsSigningState.$Properties} message WrapsSigningState message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            WrapsSigningState.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.phase != null && $Object.hasOwnProperty.call(message, "phase") && message.phase !== 0)
                                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.phase);
                                if (message.gracePeriodEndTime != null && $Object.hasOwnProperty.call(message, "gracePeriodEndTime"))
                                    $root.proto.Timestamp.encode(message.gracePeriodEndTime, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified WrapsSigningState message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.WrapsSigningState.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.WrapsSigningState
                             * @static
                             * @param {com.hedera.hapi.node.state.history.WrapsSigningState.$Properties} message WrapsSigningState message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            WrapsSigningState.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a WrapsSigningState message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.WrapsSigningState
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.WrapsSigningState & com.hedera.hapi.node.state.history.WrapsSigningState.$Shape} WrapsSigningState
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            WrapsSigningState.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.WrapsSigningState();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.int32())
                                                message.phase = value;
                                            else
                                                delete message.phase;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.gracePeriodEndTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.gracePeriodEndTime);
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a WrapsSigningState message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.WrapsSigningState
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.WrapsSigningState & com.hedera.hapi.node.state.history.WrapsSigningState.$Shape} WrapsSigningState
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            WrapsSigningState.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a WrapsSigningState message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.WrapsSigningState
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            WrapsSigningState.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.phase != null && $Object.hasOwnProperty.call(message, "phase"))
                                    if (typeof message.phase !== "number" || (message.phase | 0) !== message.phase)
                                        return "phase: enum value expected";
                                if (message.gracePeriodEndTime != null && $Object.hasOwnProperty.call(message, "gracePeriodEndTime")) {
                                    let error = $root.proto.Timestamp.verify(message.gracePeriodEndTime, _depth + 1);
                                    if (error)
                                        return "gracePeriodEndTime." + error;
                                }
                                return null;
                            };

                            /**
                             * Creates a WrapsSigningState message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.WrapsSigningState
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.WrapsSigningState} WrapsSigningState
                             */
                            WrapsSigningState.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.WrapsSigningState)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.WrapsSigningState: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.WrapsSigningState();
                                if (object.phase !== 0 && (typeof object.phase !== "string" || $root.com.hedera.hapi.node.state.history.WrapsPhase[object.phase] !== 0))
                                    switch (object.phase) {
                                    case "R1":
                                    case 0:
                                        message.phase = 0;
                                        break;
                                    case "R2":
                                    case 1:
                                        message.phase = 1;
                                        break;
                                    case "R3":
                                    case 2:
                                        message.phase = 2;
                                        break;
                                    case "AGGREGATE":
                                    case 3:
                                        message.phase = 3;
                                        break;
                                    case "POST_AGGREGATION":
                                    case 4:
                                        message.phase = 4;
                                        break;
                                    default:
                                        if (typeof object.phase === "number" && (object.phase | 0) === object.phase)
                                            message.phase = object.phase;
                                    }
                                if (object.gracePeriodEndTime != null) {
                                    if (!$util.isObject(object.gracePeriodEndTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.WrapsSigningState.gracePeriodEndTime: object expected");
                                    message.gracePeriodEndTime = $root.proto.Timestamp.fromObject(object.gracePeriodEndTime, _depth + 1);
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a WrapsSigningState message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.WrapsSigningState
                             * @static
                             * @param {com.hedera.hapi.node.state.history.WrapsSigningState} message WrapsSigningState
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            WrapsSigningState.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    object.phase = options.enums === $String ? "R1" : 0;
                                    object.gracePeriodEndTime = null;
                                }
                                if (message.phase != null && $Object.hasOwnProperty.call(message, "phase"))
                                    object.phase = options.enums === $String ? $root.com.hedera.hapi.node.state.history.WrapsPhase[message.phase] === $undefined ? message.phase : $root.com.hedera.hapi.node.state.history.WrapsPhase[message.phase] : message.phase;
                                if (message.gracePeriodEndTime != null && $Object.hasOwnProperty.call(message, "gracePeriodEndTime"))
                                    object.gracePeriodEndTime = $root.proto.Timestamp.toObject(message.gracePeriodEndTime, options, _depth + 1);
                                return object;
                            };

                            /**
                             * Converts this WrapsSigningState to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.WrapsSigningState
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            WrapsSigningState.prototype.toJSON = function() {
                                return WrapsSigningState.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for WrapsSigningState
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.WrapsSigningState
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            WrapsSigningState.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.WrapsSigningState";
                            };

                            return WrapsSigningState;
                        })();

                        history.ConstructionNodeId = (function() {

                            /**
                             * Properties of a ConstructionNodeId.
                             * @typedef {Object} com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties
                             * @property {number|Long|null} [constructionId] The unique id of a history proof construction.
                             * @property {number|Long|null} [nodeId] The unique id of a node.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a ConstructionNodeId.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IConstructionNodeId
                             * @augments com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties instead.
                             */

                            /**
                             * Shape of a ConstructionNodeId.
                             * @typedef {com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties} com.hedera.hapi.node.state.history.ConstructionNodeId.$Shape
                             */

                            /**
                             * Constructs a new ConstructionNodeId.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc A construction-scoped node id.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const ConstructionNodeId = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The unique id of a history proof construction.
                             * @member {number|Long} constructionId
                             * @memberof com.hedera.hapi.node.state.history.ConstructionNodeId
                             * @instance
                             */
                            ConstructionNodeId.prototype.constructionId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The unique id of a node.
                             * @member {number|Long} nodeId
                             * @memberof com.hedera.hapi.node.state.history.ConstructionNodeId
                             * @instance
                             */
                            ConstructionNodeId.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * Creates a new ConstructionNodeId instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.ConstructionNodeId
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.ConstructionNodeId} ConstructionNodeId instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.ConstructionNodeId.$Shape): com.hedera.hapi.node.state.history.ConstructionNodeId & com.hedera.hapi.node.state.history.ConstructionNodeId.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties): com.hedera.hapi.node.state.history.ConstructionNodeId;
                             * }}
                             */
                            ConstructionNodeId.create = function(properties) {
                                return new ConstructionNodeId(properties);
                            };

                            /**
                             * Encodes the specified ConstructionNodeId message. Does not implicitly {@link com.hedera.hapi.node.state.history.ConstructionNodeId.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.ConstructionNodeId
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties} message ConstructionNodeId message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            ConstructionNodeId.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId") && (typeof message.constructionId === "object" ? message.constructionId.low || message.constructionId.high : message.constructionId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.constructionId);
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.nodeId);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified ConstructionNodeId message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.ConstructionNodeId.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.ConstructionNodeId
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ConstructionNodeId.$Properties} message ConstructionNodeId message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            ConstructionNodeId.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a ConstructionNodeId message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.ConstructionNodeId
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.ConstructionNodeId & com.hedera.hapi.node.state.history.ConstructionNodeId.$Shape} ConstructionNodeId
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            ConstructionNodeId.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.ConstructionNodeId();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.constructionId = value;
                                            else
                                                delete message.constructionId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.nodeId = value;
                                            else
                                                delete message.nodeId;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a ConstructionNodeId message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.ConstructionNodeId
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.ConstructionNodeId & com.hedera.hapi.node.state.history.ConstructionNodeId.$Shape} ConstructionNodeId
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            ConstructionNodeId.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a ConstructionNodeId message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.ConstructionNodeId
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            ConstructionNodeId.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (!$util.isInteger(message.constructionId) && !(message.constructionId && $util.isInteger(message.constructionId.low) && $util.isInteger(message.constructionId.high)))
                                        return "constructionId: integer|Long expected";
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                        return "nodeId: integer|Long expected";
                                return null;
                            };

                            /**
                             * Creates a ConstructionNodeId message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.ConstructionNodeId
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.ConstructionNodeId} ConstructionNodeId
                             */
                            ConstructionNodeId.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.ConstructionNodeId)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.ConstructionNodeId: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.ConstructionNodeId();
                                if (object.constructionId != null)
                                    if (typeof object.constructionId === "object" ? object.constructionId.low || object.constructionId.high : $Number(object.constructionId) !== 0)
                                        if ($util.Long)
                                            message.constructionId = $util.Long.fromValue(object.constructionId, true);
                                        else if (typeof object.constructionId === "string")
                                            message.constructionId = $parseInt(object.constructionId, 10);
                                        else if (typeof object.constructionId === "number")
                                            message.constructionId = object.constructionId;
                                        else if (typeof object.constructionId === "object")
                                            message.constructionId = new $util.LongBits(object.constructionId.low >>> 0, object.constructionId.high >>> 0).toNumber(true);
                                if (object.nodeId != null)
                                    if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                        if ($util.Long)
                                            message.nodeId = $util.Long.fromValue(object.nodeId, true);
                                        else if (typeof object.nodeId === "string")
                                            message.nodeId = $parseInt(object.nodeId, 10);
                                        else if (typeof object.nodeId === "number")
                                            message.nodeId = object.nodeId;
                                        else if (typeof object.nodeId === "object")
                                            message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
                                return message;
                            };

                            /**
                             * Creates a plain object from a ConstructionNodeId message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.ConstructionNodeId
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ConstructionNodeId} message ConstructionNodeId
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            ConstructionNodeId.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.constructionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.constructionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                }
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.constructionId = typeof message.constructionId === "number" ? $BigInt(message.constructionId) : $util.Long.fromBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0, true).toBigInt();
                                    else if (typeof message.constructionId === "number")
                                        object.constructionId = options.longs === $String ? $String(message.constructionId) : message.constructionId;
                                    else
                                        object.constructionId = options.longs === $String ? $util.Long.prototype.toString.call(message.constructionId) : options.longs === $Number ? new $util.LongBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0).toNumber(true) : message.constructionId;
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                                    else if (typeof message.nodeId === "number")
                                        object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                    else
                                        object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
                                return object;
                            };

                            /**
                             * Converts this ConstructionNodeId to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.ConstructionNodeId
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            ConstructionNodeId.prototype.toJSON = function() {
                                return ConstructionNodeId.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for ConstructionNodeId
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.ConstructionNodeId
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            ConstructionNodeId.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.ConstructionNodeId";
                            };

                            return ConstructionNodeId;
                        })();

                        history.HistoryProofVote = (function() {

                            /**
                             * Properties of a HistoryProofVote.
                             * @typedef {Object} com.hedera.hapi.node.state.history.HistoryProofVote.$Properties
                             * @property {com.hedera.hapi.node.state.history.HistoryProof.$Properties|null} [proof] The history proof the submitting node is voting for.
                             * @property {number|Long|null} [congruentNodeId] The id of another node that already voted for the exact proof
                             * the submitting node is voting for.
                             * @property {"proof"|"congruentNodeId"} [vote] HistoryProofVote vote
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HistoryProofVote.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IHistoryProofVote
                             * @augments com.hedera.hapi.node.state.history.HistoryProofVote.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.HistoryProofVote.$Properties instead.
                             */

                            /**
                             * Narrowed shape of a HistoryProofVote.
                             * @typedef {{
                             *   proof?: com.hedera.hapi.node.state.history.HistoryProof.$Shape|null;
                             *   congruentNodeId?: number|Long|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * } & (
                             *   ({ vote?: undefined; proof?: null; congruentNodeId?: null }|{ vote?: "proof"; proof: com.hedera.hapi.node.state.history.HistoryProof.$Shape; congruentNodeId?: null }|{ vote?: "congruentNodeId"; proof?: null; congruentNodeId: number|Long })
                             * )} com.hedera.hapi.node.state.history.HistoryProofVote.$Shape
                             */

                            /**
                             * Constructs a new HistoryProofVote.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc A node's vote for a particular history proof; either by explicitly
                             * giving the proof, or by identifying a node that already voted for it.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.HistoryProofVote.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HistoryProofVote = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The history proof the submitting node is voting for.
                             * @member {com.hedera.hapi.node.state.history.HistoryProof.$Properties|null|undefined} proof
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @instance
                             */
                            HistoryProofVote.prototype.proof = null;

                            /**
                             * The id of another node that already voted for the exact proof
                             * the submitting node is voting for.
                             * @member {number|Long|null|undefined} congruentNodeId
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @instance
                             */
                            HistoryProofVote.prototype.congruentNodeId = null;

                            // OneOf field names bound to virtual getters and setters
                            let $oneOfFields;

                            /**
                             * HistoryProofVote vote.
                             * @member {"proof"|"congruentNodeId"|undefined} vote
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @instance
                             */
                            $Object.defineProperty(HistoryProofVote.prototype, "vote", {
                                get: $util.oneOfGetter($oneOfFields = ["proof", "congruentNodeId"]),
                                set: $util.oneOfSetter($oneOfFields)
                            });

                            /**
                             * Creates a new HistoryProofVote instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistoryProofVote.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.HistoryProofVote} HistoryProofVote instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.HistoryProofVote.$Shape): com.hedera.hapi.node.state.history.HistoryProofVote & com.hedera.hapi.node.state.history.HistoryProofVote.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.HistoryProofVote.$Properties): com.hedera.hapi.node.state.history.HistoryProofVote;
                             * }}
                             */
                            HistoryProofVote.create = function(properties) {
                                return new HistoryProofVote(properties);
                            };

                            /**
                             * Encodes the specified HistoryProofVote message. Does not implicitly {@link com.hedera.hapi.node.state.history.HistoryProofVote.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistoryProofVote.$Properties} message HistoryProofVote message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistoryProofVote.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.proof != null && $Object.hasOwnProperty.call(message, "proof"))
                                    $root.com.hedera.hapi.node.state.history.HistoryProof.encode(message.proof, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.congruentNodeId != null && $Object.hasOwnProperty.call(message, "congruentNodeId"))
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.congruentNodeId);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HistoryProofVote message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.HistoryProofVote.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistoryProofVote.$Properties} message HistoryProofVote message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistoryProofVote.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HistoryProofVote message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.HistoryProofVote & com.hedera.hapi.node.state.history.HistoryProofVote.$Shape} HistoryProofVote
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistoryProofVote.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.HistoryProofVote();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            message.proof = $root.com.hedera.hapi.node.state.history.HistoryProof.decode(reader, reader.uint32(), $undefined, _depth + 1, message.proof);
                                            message.vote = "proof";
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            message.congruentNodeId = reader.uint64();
                                            message.vote = "congruentNodeId";
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HistoryProofVote message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.HistoryProofVote & com.hedera.hapi.node.state.history.HistoryProofVote.$Shape} HistoryProofVote
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistoryProofVote.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HistoryProofVote message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HistoryProofVote.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                let properties = {};
                                if (message.proof != null && $Object.hasOwnProperty.call(message, "proof")) {
                                    properties.vote = 1;
                                    {
                                        let error = $root.com.hedera.hapi.node.state.history.HistoryProof.verify(message.proof, _depth + 1);
                                        if (error)
                                            return "proof." + error;
                                    }
                                }
                                if (message.congruentNodeId != null && $Object.hasOwnProperty.call(message, "congruentNodeId")) {
                                    if (properties.vote === 1)
                                        return "vote: multiple values";
                                    properties.vote = 1;
                                    if (!$util.isInteger(message.congruentNodeId) && !(message.congruentNodeId && $util.isInteger(message.congruentNodeId.low) && $util.isInteger(message.congruentNodeId.high)))
                                        return "congruentNodeId: integer|Long expected";
                                }
                                return null;
                            };

                            /**
                             * Creates a HistoryProofVote message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.HistoryProofVote} HistoryProofVote
                             */
                            HistoryProofVote.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.HistoryProofVote)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.HistoryProofVote: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.HistoryProofVote();
                                if (object.proof != null) {
                                    if (!$util.isObject(object.proof))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.HistoryProofVote.proof: object expected");
                                    message.proof = $root.com.hedera.hapi.node.state.history.HistoryProof.fromObject(object.proof, _depth + 1);
                                }
                                if (object.congruentNodeId != null)
                                    if ($util.Long)
                                        message.congruentNodeId = $util.Long.fromValue(object.congruentNodeId, true);
                                    else if (typeof object.congruentNodeId === "string")
                                        message.congruentNodeId = $parseInt(object.congruentNodeId, 10);
                                    else if (typeof object.congruentNodeId === "number")
                                        message.congruentNodeId = object.congruentNodeId;
                                    else if (typeof object.congruentNodeId === "object")
                                        message.congruentNodeId = new $util.LongBits(object.congruentNodeId.low >>> 0, object.congruentNodeId.high >>> 0).toNumber(true);
                                return message;
                            };

                            /**
                             * Creates a plain object from a HistoryProofVote message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistoryProofVote} message HistoryProofVote
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HistoryProofVote.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (message.proof != null && $Object.hasOwnProperty.call(message, "proof")) {
                                    object.proof = $root.com.hedera.hapi.node.state.history.HistoryProof.toObject(message.proof, options, _depth + 1);
                                    if (options.oneofs)
                                        object.vote = "proof";
                                }
                                if (message.congruentNodeId != null && $Object.hasOwnProperty.call(message, "congruentNodeId")) {
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.congruentNodeId = typeof message.congruentNodeId === "number" ? $BigInt(message.congruentNodeId) : $util.Long.fromBits(message.congruentNodeId.low >>> 0, message.congruentNodeId.high >>> 0, true).toBigInt();
                                    else if (typeof message.congruentNodeId === "number")
                                        object.congruentNodeId = options.longs === $String ? $String(message.congruentNodeId) : message.congruentNodeId;
                                    else
                                        object.congruentNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.congruentNodeId) : options.longs === $Number ? new $util.LongBits(message.congruentNodeId.low >>> 0, message.congruentNodeId.high >>> 0).toNumber(true) : message.congruentNodeId;
                                    if (options.oneofs)
                                        object.vote = "congruentNodeId";
                                }
                                return object;
                            };

                            /**
                             * Converts this HistoryProofVote to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HistoryProofVote.prototype.toJSON = function() {
                                return HistoryProofVote.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HistoryProofVote
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.HistoryProofVote
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HistoryProofVote.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.HistoryProofVote";
                            };

                            return HistoryProofVote;
                        })();

                        history.HistorySignature = (function() {

                            /**
                             * Properties of a HistorySignature.
                             * @typedef {Object} com.hedera.hapi.node.state.history.HistorySignature.$Properties
                             * @property {com.hedera.hapi.node.state.history.History.$Properties|null} [history] The new history the node is signing.
                             * @property {Uint8Array|null} [signature] The node's signature on the canonical serialization of
                             * the new history.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HistorySignature.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IHistorySignature
                             * @augments com.hedera.hapi.node.state.history.HistorySignature.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.HistorySignature.$Properties instead.
                             */

                            /**
                             * Shape of a HistorySignature.
                             * @typedef {com.hedera.hapi.node.state.history.HistorySignature.$Properties} com.hedera.hapi.node.state.history.HistorySignature.$Shape
                             */

                            /**
                             * Constructs a new HistorySignature.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc A node's signature blessing some new history.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.HistorySignature.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HistorySignature = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The new history the node is signing.
                             * @member {com.hedera.hapi.node.state.history.History.$Properties|null|undefined} history
                             * @memberof com.hedera.hapi.node.state.history.HistorySignature
                             * @instance
                             */
                            HistorySignature.prototype.history = null;

                            /**
                             * The node's signature on the canonical serialization of
                             * the new history.
                             * @member {Uint8Array} signature
                             * @memberof com.hedera.hapi.node.state.history.HistorySignature
                             * @instance
                             */
                            HistorySignature.prototype.signature = $util.newBuffer([]);

                            /**
                             * Creates a new HistorySignature instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.HistorySignature
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistorySignature.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.HistorySignature} HistorySignature instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.HistorySignature.$Shape): com.hedera.hapi.node.state.history.HistorySignature & com.hedera.hapi.node.state.history.HistorySignature.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.HistorySignature.$Properties): com.hedera.hapi.node.state.history.HistorySignature;
                             * }}
                             */
                            HistorySignature.create = function(properties) {
                                return new HistorySignature(properties);
                            };

                            /**
                             * Encodes the specified HistorySignature message. Does not implicitly {@link com.hedera.hapi.node.state.history.HistorySignature.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.HistorySignature
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistorySignature.$Properties} message HistorySignature message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistorySignature.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.history != null && $Object.hasOwnProperty.call(message, "history"))
                                    $root.com.hedera.hapi.node.state.history.History.encode(message.history, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.signature != null && $Object.hasOwnProperty.call(message, "signature") && message.signature.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.signature);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HistorySignature message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.HistorySignature.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.HistorySignature
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistorySignature.$Properties} message HistorySignature message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistorySignature.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HistorySignature message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.HistorySignature
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.HistorySignature & com.hedera.hapi.node.state.history.HistorySignature.$Shape} HistorySignature
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistorySignature.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.HistorySignature();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            message.history = $root.com.hedera.hapi.node.state.history.History.decode(reader, reader.uint32(), $undefined, _depth + 1, message.history);
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.signature = value;
                                            else
                                                delete message.signature;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HistorySignature message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.HistorySignature
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.HistorySignature & com.hedera.hapi.node.state.history.HistorySignature.$Shape} HistorySignature
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistorySignature.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HistorySignature message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.HistorySignature
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HistorySignature.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.history != null && $Object.hasOwnProperty.call(message, "history")) {
                                    let error = $root.com.hedera.hapi.node.state.history.History.verify(message.history, _depth + 1);
                                    if (error)
                                        return "history." + error;
                                }
                                if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                                    if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                                        return "signature: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a HistorySignature message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.HistorySignature
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.HistorySignature} HistorySignature
                             */
                            HistorySignature.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.HistorySignature)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.HistorySignature: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.HistorySignature();
                                if (object.history != null) {
                                    if (!$util.isObject(object.history))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.HistorySignature.history: object expected");
                                    message.history = $root.com.hedera.hapi.node.state.history.History.fromObject(object.history, _depth + 1);
                                }
                                if (object.signature != null)
                                    if (object.signature.length)
                                        if (typeof object.signature === "string")
                                            $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                                        else if (object.signature.length >= 0)
                                            message.signature = object.signature;
                                return message;
                            };

                            /**
                             * Creates a plain object from a HistorySignature message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.HistorySignature
                             * @static
                             * @param {com.hedera.hapi.node.state.history.HistorySignature} message HistorySignature
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HistorySignature.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    object.history = null;
                                    if (options.bytes === $String)
                                        object.signature = "";
                                    else {
                                        object.signature = [];
                                        if (options.bytes !== $Array)
                                            object.signature = $util.newBuffer(object.signature);
                                    }
                                }
                                if (message.history != null && $Object.hasOwnProperty.call(message, "history"))
                                    object.history = $root.com.hedera.hapi.node.state.history.History.toObject(message.history, options, _depth + 1);
                                if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                                    object.signature = options.bytes === $String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.signature) : message.signature;
                                return object;
                            };

                            /**
                             * Converts this HistorySignature to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.HistorySignature
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HistorySignature.prototype.toJSON = function() {
                                return HistorySignature.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HistorySignature
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.HistorySignature
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HistorySignature.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.HistorySignature";
                            };

                            return HistorySignature;
                        })();

                        history.RecordedHistorySignature = (function() {

                            /**
                             * Properties of a RecordedHistorySignature.
                             * @typedef {Object} com.hedera.hapi.node.state.history.RecordedHistorySignature.$Properties
                             * @property {proto.Timestamp.$Properties|null} [signingTime] The time at which the signature was recorded.
                             * @property {com.hedera.hapi.node.state.history.HistorySignature.$Properties|null} [historySignature] The signature on some new history.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a RecordedHistorySignature.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IRecordedHistorySignature
                             * @augments com.hedera.hapi.node.state.history.RecordedHistorySignature.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.RecordedHistorySignature.$Properties instead.
                             */

                            /**
                             * Shape of a RecordedHistorySignature.
                             * @typedef {com.hedera.hapi.node.state.history.RecordedHistorySignature.$Properties} com.hedera.hapi.node.state.history.RecordedHistorySignature.$Shape
                             */

                            /**
                             * Constructs a new RecordedHistorySignature.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc A signature on some new history recorded at a certain time.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.RecordedHistorySignature.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const RecordedHistorySignature = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The time at which the signature was recorded.
                             * @member {proto.Timestamp.$Properties|null|undefined} signingTime
                             * @memberof com.hedera.hapi.node.state.history.RecordedHistorySignature
                             * @instance
                             */
                            RecordedHistorySignature.prototype.signingTime = null;

                            /**
                             * The signature on some new history.
                             * @member {com.hedera.hapi.node.state.history.HistorySignature.$Properties|null|undefined} historySignature
                             * @memberof com.hedera.hapi.node.state.history.RecordedHistorySignature
                             * @instance
                             */
                            RecordedHistorySignature.prototype.historySignature = null;

                            /**
                             * Creates a new RecordedHistorySignature instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.RecordedHistorySignature
                             * @static
                             * @param {com.hedera.hapi.node.state.history.RecordedHistorySignature.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.RecordedHistorySignature} RecordedHistorySignature instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.RecordedHistorySignature.$Shape): com.hedera.hapi.node.state.history.RecordedHistorySignature & com.hedera.hapi.node.state.history.RecordedHistorySignature.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.RecordedHistorySignature.$Properties): com.hedera.hapi.node.state.history.RecordedHistorySignature;
                             * }}
                             */
                            RecordedHistorySignature.create = function(properties) {
                                return new RecordedHistorySignature(properties);
                            };

                            /**
                             * Encodes the specified RecordedHistorySignature message. Does not implicitly {@link com.hedera.hapi.node.state.history.RecordedHistorySignature.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.RecordedHistorySignature
                             * @static
                             * @param {com.hedera.hapi.node.state.history.RecordedHistorySignature.$Properties} message RecordedHistorySignature message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RecordedHistorySignature.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.signingTime != null && $Object.hasOwnProperty.call(message, "signingTime"))
                                    $root.proto.Timestamp.encode(message.signingTime, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.historySignature != null && $Object.hasOwnProperty.call(message, "historySignature"))
                                    $root.com.hedera.hapi.node.state.history.HistorySignature.encode(message.historySignature, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified RecordedHistorySignature message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.RecordedHistorySignature.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.RecordedHistorySignature
                             * @static
                             * @param {com.hedera.hapi.node.state.history.RecordedHistorySignature.$Properties} message RecordedHistorySignature message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RecordedHistorySignature.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a RecordedHistorySignature message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.RecordedHistorySignature
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.RecordedHistorySignature & com.hedera.hapi.node.state.history.RecordedHistorySignature.$Shape} RecordedHistorySignature
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RecordedHistorySignature.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.RecordedHistorySignature();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            message.signingTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.signingTime);
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.historySignature = $root.com.hedera.hapi.node.state.history.HistorySignature.decode(reader, reader.uint32(), $undefined, _depth + 1, message.historySignature);
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a RecordedHistorySignature message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.RecordedHistorySignature
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.RecordedHistorySignature & com.hedera.hapi.node.state.history.RecordedHistorySignature.$Shape} RecordedHistorySignature
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RecordedHistorySignature.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a RecordedHistorySignature message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.RecordedHistorySignature
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            RecordedHistorySignature.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.signingTime != null && $Object.hasOwnProperty.call(message, "signingTime")) {
                                    let error = $root.proto.Timestamp.verify(message.signingTime, _depth + 1);
                                    if (error)
                                        return "signingTime." + error;
                                }
                                if (message.historySignature != null && $Object.hasOwnProperty.call(message, "historySignature")) {
                                    let error = $root.com.hedera.hapi.node.state.history.HistorySignature.verify(message.historySignature, _depth + 1);
                                    if (error)
                                        return "historySignature." + error;
                                }
                                return null;
                            };

                            /**
                             * Creates a RecordedHistorySignature message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.RecordedHistorySignature
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.RecordedHistorySignature} RecordedHistorySignature
                             */
                            RecordedHistorySignature.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.RecordedHistorySignature)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.RecordedHistorySignature: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.RecordedHistorySignature();
                                if (object.signingTime != null) {
                                    if (!$util.isObject(object.signingTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.RecordedHistorySignature.signingTime: object expected");
                                    message.signingTime = $root.proto.Timestamp.fromObject(object.signingTime, _depth + 1);
                                }
                                if (object.historySignature != null) {
                                    if (!$util.isObject(object.historySignature))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.RecordedHistorySignature.historySignature: object expected");
                                    message.historySignature = $root.com.hedera.hapi.node.state.history.HistorySignature.fromObject(object.historySignature, _depth + 1);
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a RecordedHistorySignature message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.RecordedHistorySignature
                             * @static
                             * @param {com.hedera.hapi.node.state.history.RecordedHistorySignature} message RecordedHistorySignature
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            RecordedHistorySignature.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    object.signingTime = null;
                                    object.historySignature = null;
                                }
                                if (message.signingTime != null && $Object.hasOwnProperty.call(message, "signingTime"))
                                    object.signingTime = $root.proto.Timestamp.toObject(message.signingTime, options, _depth + 1);
                                if (message.historySignature != null && $Object.hasOwnProperty.call(message, "historySignature"))
                                    object.historySignature = $root.com.hedera.hapi.node.state.history.HistorySignature.toObject(message.historySignature, options, _depth + 1);
                                return object;
                            };

                            /**
                             * Converts this RecordedHistorySignature to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.RecordedHistorySignature
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            RecordedHistorySignature.prototype.toJSON = function() {
                                return RecordedHistorySignature.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for RecordedHistorySignature
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.RecordedHistorySignature
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            RecordedHistorySignature.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.RecordedHistorySignature";
                            };

                            return RecordedHistorySignature;
                        })();

                        /**
                         * The phase of a WRAPS proof construction.
                         * @name com.hedera.hapi.node.state.history.WrapsPhase
                         * @enum {number}
                         * @property {number} R1=0 Nodes are publishing their proof keys and R1 messages.
                         * @property {number} R2=1 Nodes are publishing their R2 messages.
                         * @property {number} R3=2 Nodes are publishing their R3 messages.
                         * @property {number} AGGREGATE=3 Nodes are aggregating all messages to produce a final signature.
                         * @property {number} POST_AGGREGATION=4 Nodes are doing work that must occur in a different event loop
                         * than the one used for the above phases.
                         */
                        history.WrapsPhase = (function() {
                            const valuesById = $Object.create(null), values = $Object.create(valuesById);
                            values[valuesById[0] = "R1"] = 0;
                            values[valuesById[1] = "R2"] = 1;
                            values[valuesById[2] = "R3"] = 2;
                            values[valuesById[3] = "AGGREGATE"] = 3;
                            values[valuesById[4] = "POST_AGGREGATION"] = 4;
                            return values;
                        })();

                        history.WrapsMessageDetails = (function() {

                            /**
                             * Properties of a WrapsMessageDetails.
                             * @typedef {Object} com.hedera.hapi.node.state.history.WrapsMessageDetails.$Properties
                             * @property {proto.Timestamp.$Properties|null} [publicationTime] The time at which the message was published.
                             * @property {com.hedera.hapi.node.state.history.WrapsPhase|null} [phase] The phase of the construction the message applies to.
                             * @property {Uint8Array|null} [message] The message itself.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a WrapsMessageDetails.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IWrapsMessageDetails
                             * @augments com.hedera.hapi.node.state.history.WrapsMessageDetails.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.WrapsMessageDetails.$Properties instead.
                             */

                            /**
                             * Shape of a WrapsMessageDetails.
                             * @typedef {com.hedera.hapi.node.state.history.WrapsMessageDetails.$Properties} com.hedera.hapi.node.state.history.WrapsMessageDetails.$Shape
                             */

                            /**
                             * Constructs a new WrapsMessageDetails.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc A message published by a node during a WRAPS proof construction.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.WrapsMessageDetails.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const WrapsMessageDetails = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The time at which the message was published.
                             * @member {proto.Timestamp.$Properties|null|undefined} publicationTime
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @instance
                             */
                            WrapsMessageDetails.prototype.publicationTime = null;

                            /**
                             * The phase of the construction the message applies to.
                             * @member {com.hedera.hapi.node.state.history.WrapsPhase} phase
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @instance
                             */
                            WrapsMessageDetails.prototype.phase = 0;

                            /**
                             * The message itself.
                             * @member {Uint8Array} message
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @instance
                             */
                            WrapsMessageDetails.prototype.message = $util.newBuffer([]);

                            /**
                             * Creates a new WrapsMessageDetails instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @static
                             * @param {com.hedera.hapi.node.state.history.WrapsMessageDetails.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.WrapsMessageDetails} WrapsMessageDetails instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.WrapsMessageDetails.$Shape): com.hedera.hapi.node.state.history.WrapsMessageDetails & com.hedera.hapi.node.state.history.WrapsMessageDetails.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.WrapsMessageDetails.$Properties): com.hedera.hapi.node.state.history.WrapsMessageDetails;
                             * }}
                             */
                            WrapsMessageDetails.create = function(properties) {
                                return new WrapsMessageDetails(properties);
                            };

                            /**
                             * Encodes the specified WrapsMessageDetails message. Does not implicitly {@link com.hedera.hapi.node.state.history.WrapsMessageDetails.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @static
                             * @param {com.hedera.hapi.node.state.history.WrapsMessageDetails.$Properties} message WrapsMessageDetails message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            WrapsMessageDetails.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.publicationTime != null && $Object.hasOwnProperty.call(message, "publicationTime"))
                                    $root.proto.Timestamp.encode(message.publicationTime, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.phase != null && $Object.hasOwnProperty.call(message, "phase") && message.phase !== 0)
                                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.phase);
                                if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message.length)
                                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.message);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified WrapsMessageDetails message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.WrapsMessageDetails.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @static
                             * @param {com.hedera.hapi.node.state.history.WrapsMessageDetails.$Properties} message WrapsMessageDetails message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            WrapsMessageDetails.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a WrapsMessageDetails message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.WrapsMessageDetails & com.hedera.hapi.node.state.history.WrapsMessageDetails.$Shape} WrapsMessageDetails
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            WrapsMessageDetails.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.WrapsMessageDetails();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            message.publicationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.publicationTime);
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.int32())
                                                message.phase = value;
                                            else
                                                delete message.phase;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.message = value;
                                            else
                                                delete message.message;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a WrapsMessageDetails message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.WrapsMessageDetails & com.hedera.hapi.node.state.history.WrapsMessageDetails.$Shape} WrapsMessageDetails
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            WrapsMessageDetails.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a WrapsMessageDetails message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            WrapsMessageDetails.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.publicationTime != null && $Object.hasOwnProperty.call(message, "publicationTime")) {
                                    let error = $root.proto.Timestamp.verify(message.publicationTime, _depth + 1);
                                    if (error)
                                        return "publicationTime." + error;
                                }
                                if (message.phase != null && $Object.hasOwnProperty.call(message, "phase"))
                                    if (typeof message.phase !== "number" || (message.phase | 0) !== message.phase)
                                        return "phase: enum value expected";
                                if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                                    if (!(message.message && typeof message.message.length === "number" || $util.isString(message.message)))
                                        return "message: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a WrapsMessageDetails message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.WrapsMessageDetails} WrapsMessageDetails
                             */
                            WrapsMessageDetails.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.WrapsMessageDetails)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.WrapsMessageDetails: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.WrapsMessageDetails();
                                if (object.publicationTime != null) {
                                    if (!$util.isObject(object.publicationTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.WrapsMessageDetails.publicationTime: object expected");
                                    message.publicationTime = $root.proto.Timestamp.fromObject(object.publicationTime, _depth + 1);
                                }
                                if (object.phase !== 0 && (typeof object.phase !== "string" || $root.com.hedera.hapi.node.state.history.WrapsPhase[object.phase] !== 0))
                                    switch (object.phase) {
                                    case "R1":
                                    case 0:
                                        message.phase = 0;
                                        break;
                                    case "R2":
                                    case 1:
                                        message.phase = 1;
                                        break;
                                    case "R3":
                                    case 2:
                                        message.phase = 2;
                                        break;
                                    case "AGGREGATE":
                                    case 3:
                                        message.phase = 3;
                                        break;
                                    case "POST_AGGREGATION":
                                    case 4:
                                        message.phase = 4;
                                        break;
                                    default:
                                        if (typeof object.phase === "number" && (object.phase | 0) === object.phase)
                                            message.phase = object.phase;
                                    }
                                if (object.message != null)
                                    if (object.message.length)
                                        if (typeof object.message === "string")
                                            $util.base64.decode(object.message, message.message = $util.newBuffer($util.base64.length(object.message)), 0);
                                        else if (object.message.length >= 0)
                                            message.message = object.message;
                                return message;
                            };

                            /**
                             * Creates a plain object from a WrapsMessageDetails message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @static
                             * @param {com.hedera.hapi.node.state.history.WrapsMessageDetails} message WrapsMessageDetails
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            WrapsMessageDetails.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    object.publicationTime = null;
                                    object.phase = options.enums === $String ? "R1" : 0;
                                    if (options.bytes === $String)
                                        object.message = "";
                                    else {
                                        object.message = [];
                                        if (options.bytes !== $Array)
                                            object.message = $util.newBuffer(object.message);
                                    }
                                }
                                if (message.publicationTime != null && $Object.hasOwnProperty.call(message, "publicationTime"))
                                    object.publicationTime = $root.proto.Timestamp.toObject(message.publicationTime, options, _depth + 1);
                                if (message.phase != null && $Object.hasOwnProperty.call(message, "phase"))
                                    object.phase = options.enums === $String ? $root.com.hedera.hapi.node.state.history.WrapsPhase[message.phase] === $undefined ? message.phase : $root.com.hedera.hapi.node.state.history.WrapsPhase[message.phase] : message.phase;
                                if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                                    object.message = options.bytes === $String ? $util.base64.encode(message.message, 0, message.message.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.message) : message.message;
                                return object;
                            };

                            /**
                             * Converts this WrapsMessageDetails to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            WrapsMessageDetails.prototype.toJSON = function() {
                                return WrapsMessageDetails.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for WrapsMessageDetails
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageDetails
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            WrapsMessageDetails.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.WrapsMessageDetails";
                            };

                            return WrapsMessageDetails;
                        })();

                        history.WrapsMessageHistory = (function() {

                            /**
                             * Properties of a WrapsMessageHistory.
                             * @typedef {Object} com.hedera.hapi.node.state.history.WrapsMessageHistory.$Properties
                             * @property {Array.<com.hedera.hapi.node.state.history.WrapsMessageDetails.$Properties>|null} [messages] WrapsMessageHistory messages
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a WrapsMessageHistory.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IWrapsMessageHistory
                             * @augments com.hedera.hapi.node.state.history.WrapsMessageHistory.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.WrapsMessageHistory.$Properties instead.
                             */

                            /**
                             * Shape of a WrapsMessageHistory.
                             * @typedef {com.hedera.hapi.node.state.history.WrapsMessageHistory.$Properties} com.hedera.hapi.node.state.history.WrapsMessageHistory.$Shape
                             */

                            /**
                             * Constructs a new WrapsMessageHistory.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc A history of messages published during a WRAPS proof construction.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.WrapsMessageHistory.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const WrapsMessageHistory = function (properties) {
                                this.messages = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * WrapsMessageHistory messages.
                             * @member {Array.<com.hedera.hapi.node.state.history.WrapsMessageDetails.$Properties>} messages
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageHistory
                             * @instance
                             */
                            WrapsMessageHistory.prototype.messages = $util.emptyArray;

                            /**
                             * Creates a new WrapsMessageHistory instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageHistory
                             * @static
                             * @param {com.hedera.hapi.node.state.history.WrapsMessageHistory.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.WrapsMessageHistory} WrapsMessageHistory instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.WrapsMessageHistory.$Shape): com.hedera.hapi.node.state.history.WrapsMessageHistory & com.hedera.hapi.node.state.history.WrapsMessageHistory.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.WrapsMessageHistory.$Properties): com.hedera.hapi.node.state.history.WrapsMessageHistory;
                             * }}
                             */
                            WrapsMessageHistory.create = function(properties) {
                                return new WrapsMessageHistory(properties);
                            };

                            /**
                             * Encodes the specified WrapsMessageHistory message. Does not implicitly {@link com.hedera.hapi.node.state.history.WrapsMessageHistory.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageHistory
                             * @static
                             * @param {com.hedera.hapi.node.state.history.WrapsMessageHistory.$Properties} message WrapsMessageHistory message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            WrapsMessageHistory.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.messages != null && message.messages.length)
                                    for (let i = 0; i < message.messages.length; ++i)
                                        $root.com.hedera.hapi.node.state.history.WrapsMessageDetails.encode(message.messages[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified WrapsMessageHistory message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.WrapsMessageHistory.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageHistory
                             * @static
                             * @param {com.hedera.hapi.node.state.history.WrapsMessageHistory.$Properties} message WrapsMessageHistory message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            WrapsMessageHistory.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a WrapsMessageHistory message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageHistory
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.WrapsMessageHistory & com.hedera.hapi.node.state.history.WrapsMessageHistory.$Shape} WrapsMessageHistory
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            WrapsMessageHistory.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.WrapsMessageHistory();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.messages && message.messages.length))
                                                message.messages = [];
                                            message.messages.push($root.com.hedera.hapi.node.state.history.WrapsMessageDetails.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a WrapsMessageHistory message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageHistory
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.WrapsMessageHistory & com.hedera.hapi.node.state.history.WrapsMessageHistory.$Shape} WrapsMessageHistory
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            WrapsMessageHistory.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a WrapsMessageHistory message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageHistory
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            WrapsMessageHistory.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.messages != null && $Object.hasOwnProperty.call(message, "messages")) {
                                    if (!$Array.isArray(message.messages))
                                        return "messages: array expected";
                                    for (let i = 0; i < message.messages.length; ++i) {
                                        let error = $root.com.hedera.hapi.node.state.history.WrapsMessageDetails.verify(message.messages[i], _depth + 1);
                                        if (error)
                                            return "messages." + error;
                                    }
                                }
                                return null;
                            };

                            /**
                             * Creates a WrapsMessageHistory message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageHistory
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.WrapsMessageHistory} WrapsMessageHistory
                             */
                            WrapsMessageHistory.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.WrapsMessageHistory)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.WrapsMessageHistory: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.WrapsMessageHistory();
                                if (object.messages) {
                                    if (!$Array.isArray(object.messages))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.WrapsMessageHistory.messages: array expected");
                                    message.messages = $Array(object.messages.length);
                                    for (let i = 0; i < object.messages.length; ++i) {
                                        if (!$util.isObject(object.messages[i]))
                                            throw $TypeError(".com.hedera.hapi.node.state.history.WrapsMessageHistory.messages: object expected");
                                        message.messages[i] = $root.com.hedera.hapi.node.state.history.WrapsMessageDetails.fromObject(object.messages[i], _depth + 1);
                                    }
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a WrapsMessageHistory message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageHistory
                             * @static
                             * @param {com.hedera.hapi.node.state.history.WrapsMessageHistory} message WrapsMessageHistory
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            WrapsMessageHistory.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults)
                                    object.messages = [];
                                if (message.messages && message.messages.length) {
                                    object.messages = $Array(message.messages.length);
                                    for (let j = 0; j < message.messages.length; ++j)
                                        object.messages[j] = $root.com.hedera.hapi.node.state.history.WrapsMessageDetails.toObject(message.messages[j], options, _depth + 1);
                                }
                                return object;
                            };

                            /**
                             * Converts this WrapsMessageHistory to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageHistory
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            WrapsMessageHistory.prototype.toJSON = function() {
                                return WrapsMessageHistory.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for WrapsMessageHistory
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.WrapsMessageHistory
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            WrapsMessageHistory.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.WrapsMessageHistory";
                            };

                            return WrapsMessageHistory;
                        })();

                        history.ChainOfTrustProof = (function() {

                            /**
                             * Properties of a ChainOfTrustProof.
                             * @typedef {Object} com.hedera.hapi.node.state.history.ChainOfTrustProof.$Properties
                             * @property {com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Properties|null} [aggregatedNodeSignatures] If there is not yet a SNARK proving the chain of trust from ledger id to
                             * the hinTS verification key, an aggregation of Schnorr signatures on
                             * the concatenation of the ledger id and genesis hinTS verification key
                             * that serve as witnesses for the SNARK prover algorithm.
                             * @property {Uint8Array|null} [wrapsProof] If known, a ZK-compressed SNARK proof proving the chain of trust from
                             * the ledger id to this hinTS verification key.
                             * @property {"aggregatedNodeSignatures"|"wrapsProof"} [proof] ChainOfTrustProof proof
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a ChainOfTrustProof.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IChainOfTrustProof
                             * @augments com.hedera.hapi.node.state.history.ChainOfTrustProof.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.ChainOfTrustProof.$Properties instead.
                             */

                            /**
                             * Narrowed shape of a ChainOfTrustProof.
                             * @typedef {{
                             *   aggregatedNodeSignatures?: com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Shape|null;
                             *   wrapsProof?: Uint8Array|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * } & (
                             *   ({ proof?: undefined; aggregatedNodeSignatures?: null; wrapsProof?: null }|{ proof?: "aggregatedNodeSignatures"; aggregatedNodeSignatures: com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Shape; wrapsProof?: null }|{ proof?: "wrapsProof"; aggregatedNodeSignatures?: null; wrapsProof: Uint8Array })
                             * )} com.hedera.hapi.node.state.history.ChainOfTrustProof.$Shape
                             */

                            /**
                             * Constructs a new ChainOfTrustProof.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc Represents a ChainOfTrustProof.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.ChainOfTrustProof.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const ChainOfTrustProof = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * If there is not yet a SNARK proving the chain of trust from ledger id to
                             * the hinTS verification key, an aggregation of Schnorr signatures on
                             * the concatenation of the ledger id and genesis hinTS verification key
                             * that serve as witnesses for the SNARK prover algorithm.
                             * @member {com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Properties|null|undefined} aggregatedNodeSignatures
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @instance
                             */
                            ChainOfTrustProof.prototype.aggregatedNodeSignatures = null;

                            /**
                             * If known, a ZK-compressed SNARK proof proving the chain of trust from
                             * the ledger id to this hinTS verification key.
                             * @member {Uint8Array|null|undefined} wrapsProof
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @instance
                             */
                            ChainOfTrustProof.prototype.wrapsProof = null;

                            // OneOf field names bound to virtual getters and setters
                            let $oneOfFields;

                            /**
                             * ChainOfTrustProof proof.
                             * @member {"aggregatedNodeSignatures"|"wrapsProof"|undefined} proof
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @instance
                             */
                            $Object.defineProperty(ChainOfTrustProof.prototype, "proof", {
                                get: $util.oneOfGetter($oneOfFields = ["aggregatedNodeSignatures", "wrapsProof"]),
                                set: $util.oneOfSetter($oneOfFields)
                            });

                            /**
                             * Creates a new ChainOfTrustProof instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ChainOfTrustProof.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.ChainOfTrustProof} ChainOfTrustProof instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.ChainOfTrustProof.$Shape): com.hedera.hapi.node.state.history.ChainOfTrustProof & com.hedera.hapi.node.state.history.ChainOfTrustProof.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.ChainOfTrustProof.$Properties): com.hedera.hapi.node.state.history.ChainOfTrustProof;
                             * }}
                             */
                            ChainOfTrustProof.create = function(properties) {
                                return new ChainOfTrustProof(properties);
                            };

                            /**
                             * Encodes the specified ChainOfTrustProof message. Does not implicitly {@link com.hedera.hapi.node.state.history.ChainOfTrustProof.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ChainOfTrustProof.$Properties} message ChainOfTrustProof message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            ChainOfTrustProof.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.aggregatedNodeSignatures != null && $Object.hasOwnProperty.call(message, "aggregatedNodeSignatures"))
                                    $root.com.hedera.hapi.node.state.history.AggregatedNodeSignatures.encode(message.aggregatedNodeSignatures, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.wrapsProof != null && $Object.hasOwnProperty.call(message, "wrapsProof"))
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.wrapsProof);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified ChainOfTrustProof message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.ChainOfTrustProof.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ChainOfTrustProof.$Properties} message ChainOfTrustProof message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            ChainOfTrustProof.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a ChainOfTrustProof message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.ChainOfTrustProof & com.hedera.hapi.node.state.history.ChainOfTrustProof.$Shape} ChainOfTrustProof
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            ChainOfTrustProof.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.ChainOfTrustProof();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            message.aggregatedNodeSignatures = $root.com.hedera.hapi.node.state.history.AggregatedNodeSignatures.decode(reader, reader.uint32(), $undefined, _depth + 1, message.aggregatedNodeSignatures);
                                            message.proof = "aggregatedNodeSignatures";
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.wrapsProof = reader.bytes();
                                            message.proof = "wrapsProof";
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a ChainOfTrustProof message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.ChainOfTrustProof & com.hedera.hapi.node.state.history.ChainOfTrustProof.$Shape} ChainOfTrustProof
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            ChainOfTrustProof.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a ChainOfTrustProof message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            ChainOfTrustProof.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                let properties = {};
                                if (message.aggregatedNodeSignatures != null && $Object.hasOwnProperty.call(message, "aggregatedNodeSignatures")) {
                                    properties.proof = 1;
                                    {
                                        let error = $root.com.hedera.hapi.node.state.history.AggregatedNodeSignatures.verify(message.aggregatedNodeSignatures, _depth + 1);
                                        if (error)
                                            return "aggregatedNodeSignatures." + error;
                                    }
                                }
                                if (message.wrapsProof != null && $Object.hasOwnProperty.call(message, "wrapsProof")) {
                                    if (properties.proof === 1)
                                        return "proof: multiple values";
                                    properties.proof = 1;
                                    if (!(message.wrapsProof && typeof message.wrapsProof.length === "number" || $util.isString(message.wrapsProof)))
                                        return "wrapsProof: buffer expected";
                                }
                                return null;
                            };

                            /**
                             * Creates a ChainOfTrustProof message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.ChainOfTrustProof} ChainOfTrustProof
                             */
                            ChainOfTrustProof.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.ChainOfTrustProof)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.ChainOfTrustProof: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.ChainOfTrustProof();
                                if (object.aggregatedNodeSignatures != null) {
                                    if (!$util.isObject(object.aggregatedNodeSignatures))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.ChainOfTrustProof.aggregatedNodeSignatures: object expected");
                                    message.aggregatedNodeSignatures = $root.com.hedera.hapi.node.state.history.AggregatedNodeSignatures.fromObject(object.aggregatedNodeSignatures, _depth + 1);
                                }
                                if (object.wrapsProof != null)
                                    if (typeof object.wrapsProof === "string")
                                        $util.base64.decode(object.wrapsProof, message.wrapsProof = $util.newBuffer($util.base64.length(object.wrapsProof)), 0);
                                    else if (object.wrapsProof.length >= 0)
                                        message.wrapsProof = object.wrapsProof;
                                return message;
                            };

                            /**
                             * Creates a plain object from a ChainOfTrustProof message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @static
                             * @param {com.hedera.hapi.node.state.history.ChainOfTrustProof} message ChainOfTrustProof
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            ChainOfTrustProof.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (message.aggregatedNodeSignatures != null && $Object.hasOwnProperty.call(message, "aggregatedNodeSignatures")) {
                                    object.aggregatedNodeSignatures = $root.com.hedera.hapi.node.state.history.AggregatedNodeSignatures.toObject(message.aggregatedNodeSignatures, options, _depth + 1);
                                    if (options.oneofs)
                                        object.proof = "aggregatedNodeSignatures";
                                }
                                if (message.wrapsProof != null && $Object.hasOwnProperty.call(message, "wrapsProof")) {
                                    object.wrapsProof = options.bytes === $String ? $util.base64.encode(message.wrapsProof, 0, message.wrapsProof.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.wrapsProof) : message.wrapsProof;
                                    if (options.oneofs)
                                        object.proof = "wrapsProof";
                                }
                                return object;
                            };

                            /**
                             * Converts this ChainOfTrustProof to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            ChainOfTrustProof.prototype.toJSON = function() {
                                return ChainOfTrustProof.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for ChainOfTrustProof
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.ChainOfTrustProof
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            ChainOfTrustProof.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.ChainOfTrustProof";
                            };

                            return ChainOfTrustProof;
                        })();

                        history.AggregatedNodeSignatures = (function() {

                            /**
                             * Properties of an AggregatedNodeSignatures.
                             * @typedef {Object} com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Properties
                             * @property {Uint8Array|null} [aggregatedSignature] The aggregated signature.
                             * @property {Array.<number|Long>|null} [signingNodeIds] In ascending order, the ids of the nodes that contributed signatures.
                             * @property {Uint8Array|null} [verificationKey] The hinTS key that this signature witnesses.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of an AggregatedNodeSignatures.
                             * @memberof com.hedera.hapi.node.state.history
                             * @interface IAggregatedNodeSignatures
                             * @augments com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Properties instead.
                             */

                            /**
                             * Shape of an AggregatedNodeSignatures.
                             * @typedef {com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Properties} com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Shape
                             */

                            /**
                             * Constructs a new AggregatedNodeSignatures.
                             * @memberof com.hedera.hapi.node.state.history
                             * @classdesc An aggregation of node signatures on some data.
                             * <p>
                             * Can be used to prove the genesis hinTS verification key in a block proof; but
                             * not succinct and not recursive; hence in normal operations with TSS, used only
                             * until the first recursive proof is available.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const AggregatedNodeSignatures = function (properties) {
                                this.signingNodeIds = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The aggregated signature.
                             * @member {Uint8Array} aggregatedSignature
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @instance
                             */
                            AggregatedNodeSignatures.prototype.aggregatedSignature = $util.newBuffer([]);

                            /**
                             * In ascending order, the ids of the nodes that contributed signatures.
                             * @member {Array.<number|Long>} signingNodeIds
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @instance
                             */
                            AggregatedNodeSignatures.prototype.signingNodeIds = $util.emptyArray;

                            /**
                             * The hinTS key that this signature witnesses.
                             * @member {Uint8Array} verificationKey
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @instance
                             */
                            AggregatedNodeSignatures.prototype.verificationKey = $util.newBuffer([]);

                            /**
                             * Creates a new AggregatedNodeSignatures instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @static
                             * @param {com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.history.AggregatedNodeSignatures} AggregatedNodeSignatures instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Shape): com.hedera.hapi.node.state.history.AggregatedNodeSignatures & com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Properties): com.hedera.hapi.node.state.history.AggregatedNodeSignatures;
                             * }}
                             */
                            AggregatedNodeSignatures.create = function(properties) {
                                return new AggregatedNodeSignatures(properties);
                            };

                            /**
                             * Encodes the specified AggregatedNodeSignatures message. Does not implicitly {@link com.hedera.hapi.node.state.history.AggregatedNodeSignatures.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @static
                             * @param {com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Properties} message AggregatedNodeSignatures message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            AggregatedNodeSignatures.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.aggregatedSignature != null && $Object.hasOwnProperty.call(message, "aggregatedSignature") && message.aggregatedSignature.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.aggregatedSignature);
                                if (message.signingNodeIds != null && message.signingNodeIds.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).uint64s(message.signingNodeIds);
                                if (message.verificationKey != null && $Object.hasOwnProperty.call(message, "verificationKey") && message.verificationKey.length)
                                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.verificationKey);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified AggregatedNodeSignatures message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.history.AggregatedNodeSignatures.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @static
                             * @param {com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Properties} message AggregatedNodeSignatures message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            AggregatedNodeSignatures.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes an AggregatedNodeSignatures message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.history.AggregatedNodeSignatures & com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Shape} AggregatedNodeSignatures
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            AggregatedNodeSignatures.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.history.AggregatedNodeSignatures();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.aggregatedSignature = value;
                                            else
                                                delete message.aggregatedSignature;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType === 2) {
                                                if (!(message.signingNodeIds && message.signingNodeIds.length))
                                                    message.signingNodeIds = [];
                                                reader.uint64s(message.signingNodeIds);
                                                continue;
                                            }
                                            if (wireType !== 0)
                                                break;
                                            if (!(message.signingNodeIds && message.signingNodeIds.length))
                                                message.signingNodeIds = [];
                                            message.signingNodeIds.push(reader.uint64());
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.verificationKey = value;
                                            else
                                                delete message.verificationKey;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes an AggregatedNodeSignatures message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.history.AggregatedNodeSignatures & com.hedera.hapi.node.state.history.AggregatedNodeSignatures.$Shape} AggregatedNodeSignatures
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            AggregatedNodeSignatures.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies an AggregatedNodeSignatures message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            AggregatedNodeSignatures.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.aggregatedSignature != null && $Object.hasOwnProperty.call(message, "aggregatedSignature"))
                                    if (!(message.aggregatedSignature && typeof message.aggregatedSignature.length === "number" || $util.isString(message.aggregatedSignature)))
                                        return "aggregatedSignature: buffer expected";
                                if (message.signingNodeIds != null && $Object.hasOwnProperty.call(message, "signingNodeIds")) {
                                    if (!$Array.isArray(message.signingNodeIds))
                                        return "signingNodeIds: array expected";
                                    for (let i = 0; i < message.signingNodeIds.length; ++i)
                                        if (!$util.isInteger(message.signingNodeIds[i]) && !(message.signingNodeIds[i] && $util.isInteger(message.signingNodeIds[i].low) && $util.isInteger(message.signingNodeIds[i].high)))
                                            return "signingNodeIds: integer|Long[] expected";
                                }
                                if (message.verificationKey != null && $Object.hasOwnProperty.call(message, "verificationKey"))
                                    if (!(message.verificationKey && typeof message.verificationKey.length === "number" || $util.isString(message.verificationKey)))
                                        return "verificationKey: buffer expected";
                                return null;
                            };

                            /**
                             * Creates an AggregatedNodeSignatures message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.history.AggregatedNodeSignatures} AggregatedNodeSignatures
                             */
                            AggregatedNodeSignatures.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.history.AggregatedNodeSignatures)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.history.AggregatedNodeSignatures: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.history.AggregatedNodeSignatures();
                                if (object.aggregatedSignature != null)
                                    if (object.aggregatedSignature.length)
                                        if (typeof object.aggregatedSignature === "string")
                                            $util.base64.decode(object.aggregatedSignature, message.aggregatedSignature = $util.newBuffer($util.base64.length(object.aggregatedSignature)), 0);
                                        else if (object.aggregatedSignature.length >= 0)
                                            message.aggregatedSignature = object.aggregatedSignature;
                                if (object.signingNodeIds) {
                                    if (!$Array.isArray(object.signingNodeIds))
                                        throw $TypeError(".com.hedera.hapi.node.state.history.AggregatedNodeSignatures.signingNodeIds: array expected");
                                    message.signingNodeIds = $Array(object.signingNodeIds.length);
                                    for (let i = 0; i < object.signingNodeIds.length; ++i)
                                        if ($util.Long)
                                            message.signingNodeIds[i] = $util.Long.fromValue(object.signingNodeIds[i], true);
                                        else if (typeof object.signingNodeIds[i] === "string")
                                            message.signingNodeIds[i] = $parseInt(object.signingNodeIds[i], 10);
                                        else if (typeof object.signingNodeIds[i] === "number")
                                            message.signingNodeIds[i] = object.signingNodeIds[i];
                                        else if (typeof object.signingNodeIds[i] === "object")
                                            message.signingNodeIds[i] = new $util.LongBits(object.signingNodeIds[i].low >>> 0, object.signingNodeIds[i].high >>> 0).toNumber(true);
                                }
                                if (object.verificationKey != null)
                                    if (object.verificationKey.length)
                                        if (typeof object.verificationKey === "string")
                                            $util.base64.decode(object.verificationKey, message.verificationKey = $util.newBuffer($util.base64.length(object.verificationKey)), 0);
                                        else if (object.verificationKey.length >= 0)
                                            message.verificationKey = object.verificationKey;
                                return message;
                            };

                            /**
                             * Creates a plain object from an AggregatedNodeSignatures message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @static
                             * @param {com.hedera.hapi.node.state.history.AggregatedNodeSignatures} message AggregatedNodeSignatures
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            AggregatedNodeSignatures.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults)
                                    object.signingNodeIds = [];
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.aggregatedSignature = "";
                                    else {
                                        object.aggregatedSignature = [];
                                        if (options.bytes !== $Array)
                                            object.aggregatedSignature = $util.newBuffer(object.aggregatedSignature);
                                    }
                                    if (options.bytes === $String)
                                        object.verificationKey = "";
                                    else {
                                        object.verificationKey = [];
                                        if (options.bytes !== $Array)
                                            object.verificationKey = $util.newBuffer(object.verificationKey);
                                    }
                                }
                                if (message.aggregatedSignature != null && $Object.hasOwnProperty.call(message, "aggregatedSignature"))
                                    object.aggregatedSignature = options.bytes === $String ? $util.base64.encode(message.aggregatedSignature, 0, message.aggregatedSignature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.aggregatedSignature) : message.aggregatedSignature;
                                if (message.signingNodeIds && message.signingNodeIds.length) {
                                    object.signingNodeIds = $Array(message.signingNodeIds.length);
                                    for (let j = 0; j < message.signingNodeIds.length; ++j)
                                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                            object.signingNodeIds[j] = typeof message.signingNodeIds[j] === "number" ? $BigInt(message.signingNodeIds[j]) : $util.Long.fromBits(message.signingNodeIds[j].low >>> 0, message.signingNodeIds[j].high >>> 0, true).toBigInt();
                                        else if (typeof message.signingNodeIds[j] === "number")
                                            object.signingNodeIds[j] = options.longs === $String ? $String(message.signingNodeIds[j]) : message.signingNodeIds[j];
                                        else
                                            object.signingNodeIds[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.signingNodeIds[j]) : options.longs === $Number ? new $util.LongBits(message.signingNodeIds[j].low >>> 0, message.signingNodeIds[j].high >>> 0).toNumber(true) : message.signingNodeIds[j];
                                }
                                if (message.verificationKey != null && $Object.hasOwnProperty.call(message, "verificationKey"))
                                    object.verificationKey = options.bytes === $String ? $util.base64.encode(message.verificationKey, 0, message.verificationKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.verificationKey) : message.verificationKey;
                                return object;
                            };

                            /**
                             * Converts this AggregatedNodeSignatures to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            AggregatedNodeSignatures.prototype.toJSON = function() {
                                return AggregatedNodeSignatures.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for AggregatedNodeSignatures
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.history.AggregatedNodeSignatures
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            AggregatedNodeSignatures.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.history.AggregatedNodeSignatures";
                            };

                            return AggregatedNodeSignatures;
                        })();

                        return history;
                    })();

                    state.addressbook = (function() {

                        /**
                         * Namespace addressbook.
                         * @memberof com.hedera.hapi.node.state
                         * @namespace
                         */
                        const addressbook = {};

                        addressbook.Node = (function() {

                            /**
                             * Properties of a Node.
                             * @typedef {Object} com.hedera.hapi.node.state.addressbook.Node.$Properties
                             * @property {number|Long|null} [nodeId] A consensus node identifier.
                             * <p>
                             * Node identifiers SHALL be globally unique for a given ledger.
                             * @property {proto.AccountID.$Properties|null} [accountId] An account identifier.
                             * <p>
                             * This account SHALL be owned by the entity responsible for the node.<br/>
                             * This account SHALL be charged transaction fees for any transactions
                             * that are submitted to the network by this node and
                             * fail due diligence checks.<br/>
                             * This account SHALL be paid the node portion of transaction fees
                             * for transactions submitted by this node.
                             * @property {string|null} [description] A short description of the node.
                             * <p>
                             * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
                             * (default 100) bytes when encoded as UTF-8.
                             * @property {Array.<proto.ServiceEndpoint.$Properties>|null} [gossipEndpoint] A list of service endpoints for gossip.
                             * <p>
                             * These endpoints SHALL represent the published endpoints to which other
                             * consensus nodes may _gossip_ transactions.<br/>
                             * If the network configuration value `gossipFqdnRestricted` is set, then
                             * all endpoints in this list SHALL supply only IP address.<br/>
                             * If the network configuration value `gossipFqdnRestricted` is _not_ set,
                             * then endpoints in this list MAY supply either IP address or FQDN, but
                             * SHALL NOT supply both values for the same endpoint.<br/>
                             * This list SHALL NOT be empty.<br/>
                             * This list SHALL NOT contain more than `10` entries.<br/>
                             * The first two entries in this list SHALL be the endpoints published to
                             * all consensus nodes.<br/>
                             * All other entries SHALL be reserved for future use.
                             * @property {Array.<proto.ServiceEndpoint.$Properties>|null} [serviceEndpoint] A list of service endpoints for client calls.
                             * <p>
                             * These endpoints SHALL represent the published endpoints to which clients
                             * may submit transactions.<br/>
                             * These endpoints SHALL specify a port.<br/>
                             * Endpoints in this list MAY supply either IP address or FQDN, but SHALL
                             * NOT supply both values for the same endpoint.<br/>
                             * This list SHALL NOT be empty.<br/>
                             * This list SHALL NOT contain more than `8` entries.
                             * @property {Uint8Array|null} [gossipCaCertificate] A certificate used to sign gossip events.
                             * <p>
                             * This value SHALL be a certificate of a type permitted for gossip
                             * signatures.<br/>
                             * This value SHALL be the DER encoding of the certificate presented.<br/>
                             * This field is REQUIRED and MUST NOT be empty.
                             * @property {Uint8Array|null} [grpcCertificateHash] A hash of the node gRPC certificate.
                             * <p>
                             * This value MAY be used to verify the certificate presented by the node
                             * during TLS negotiation for gRPC.<br/>
                             * This value SHALL be a SHA-384 hash.<br/>
                             * The TLS certificate to be hashed SHALL first be in PEM format and SHALL
                             * be encoded with UTF-8 NFKD encoding to a stream of bytes provided to
                             * the hash algorithm.<br/>
                             * This field is OPTIONAL.
                             * @property {number|Long|null} [weight] A consensus weight.
                             * <p>
                             * Each node SHALL have a weight in consensus calculations.<br/>
                             * The consensus weight of a node SHALL be calculated based on the amount
                             * of HBAR staked to that node.<br/>
                             * Consensus SHALL be calculated based on agreement of greater than `2/3`
                             * of the total `weight` value of all nodes on the network.
                             * <p>
                             * This field is deprecated and SHALL NOT be used when RosterLifecycle
                             * is enabled.
                             * @property {boolean|null} [deleted] A flag indicating this node is deleted.
                             * <p>
                             * If this field is set, then this node SHALL NOT be included in the next
                             * update of the network address book.<br/>
                             * If this field is set, then this node SHALL be immutable and SHALL NOT
                             * be modified.<br/>
                             * If this field is set, then any `nodeUpdate` transaction to modify this
                             * node SHALL fail.
                             * @property {proto.Key.$Properties|null} [adminKey] An administrative key controlled by the node operator.
                             * <p>
                             * This key MUST sign each transaction to update this node.<br/>
                             * This field MUST contain a valid `Key` value.<br/>
                             * This field is REQUIRED and MUST NOT be set to an empty `KeyList`.
                             * @property {boolean|null} [declineReward] A flag indicating this node declines node rewards distributed at
                             * the end of staking period.
                             * <p>
                             * If this field is set, then this node SHALL NOT receive any node rewards
                             * distributed at the end of the staking period.
                             * @property {proto.ServiceEndpoint.$Properties|null} [grpcProxyEndpoint] A web proxy for gRPC from non-gRPC clients.
                             * <p>
                             * This endpoint SHALL be a Fully Qualified Domain Name (FQDN) using the HTTPS
                             * protocol, and SHALL support gRPC-Web for use by browser-based clients.<br/>
                             * This endpoint MUST be signed by a trusted certificate authority.<br/>
                             * This endpoint MUST use a valid port and SHALL be reachable over TLS.<br/>
                             * This field MAY be omitted if the node does not support gRPC-Web access.<br/>
                             * This field MUST be updated if the gRPC-Web endpoint changes.<br/>
                             * This field SHALL enable frontend clients to avoid hard-coded proxy endpoints.
                             * @property {Array.<number|Long>|null} [associatedRegisteredNode] A list of registered nodes operated by the same entity as this node.<br/>
                             * This value may contain a list of "registered nodes" (as described in
                             * HIP-1137) that are operated by the same entity that operates this
                             * consensus node.
                             * <p>
                             * This field is OPTIONAL and MAY be empty.<br/>
                             * This field MUST NOT contain more than twenty(20) entries.<br/>
                             * Every entry in this list MUST be a valid `registered_node_id` for a
                             * current registered node.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a Node.
                             * @memberof com.hedera.hapi.node.state.addressbook
                             * @interface INode
                             * @augments com.hedera.hapi.node.state.addressbook.Node.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.addressbook.Node.$Properties instead.
                             */

                            /**
                             * Shape of a Node.
                             * @typedef {{
                             *   nodeId?: number|Long|null;
                             *   accountId?: proto.AccountID.$Shape|null;
                             *   description?: string|null;
                             *   gossipEndpoint?: Array.<proto.ServiceEndpoint.$Shape>|null;
                             *   serviceEndpoint?: Array.<proto.ServiceEndpoint.$Shape>|null;
                             *   gossipCaCertificate?: Uint8Array|null;
                             *   grpcCertificateHash?: Uint8Array|null;
                             *   weight?: number|Long|null;
                             *   deleted?: boolean|null;
                             *   adminKey?: proto.Key.$Shape|null;
                             *   declineReward?: boolean|null;
                             *   grpcProxyEndpoint?: proto.ServiceEndpoint.$Shape|null;
                             *   associatedRegisteredNode?: Array.<number|Long>|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * }} com.hedera.hapi.node.state.addressbook.Node.$Shape
                             */

                            /**
                             * Constructs a new Node.
                             * @memberof com.hedera.hapi.node.state.addressbook
                             * @classdesc A single address book node in the network state.
                             * 
                             * Each node in the network address book SHALL represent a single actual
                             * consensus node that is eligible to participate in network consensus.
                             * 
                             * Address book nodes SHALL NOT be _globally_ uniquely identified. A given node
                             * is only valid within a single realm and shard combination, so the identifier
                             * for a network node SHALL only be unique within a single realm and shard
                             * combination.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.addressbook.Node.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const Node = function (properties) {
                                this.gossipEndpoint = [];
                                this.serviceEndpoint = [];
                                this.associatedRegisteredNode = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * A consensus node identifier.
                             * <p>
                             * Node identifiers SHALL be globally unique for a given ledger.
                             * @member {number|Long} nodeId
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * An account identifier.
                             * <p>
                             * This account SHALL be owned by the entity responsible for the node.<br/>
                             * This account SHALL be charged transaction fees for any transactions
                             * that are submitted to the network by this node and
                             * fail due diligence checks.<br/>
                             * This account SHALL be paid the node portion of transaction fees
                             * for transactions submitted by this node.
                             * @member {proto.AccountID.$Properties|null|undefined} accountId
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.accountId = null;

                            /**
                             * A short description of the node.
                             * <p>
                             * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
                             * (default 100) bytes when encoded as UTF-8.
                             * @member {string} description
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.description = "";

                            /**
                             * A list of service endpoints for gossip.
                             * <p>
                             * These endpoints SHALL represent the published endpoints to which other
                             * consensus nodes may _gossip_ transactions.<br/>
                             * If the network configuration value `gossipFqdnRestricted` is set, then
                             * all endpoints in this list SHALL supply only IP address.<br/>
                             * If the network configuration value `gossipFqdnRestricted` is _not_ set,
                             * then endpoints in this list MAY supply either IP address or FQDN, but
                             * SHALL NOT supply both values for the same endpoint.<br/>
                             * This list SHALL NOT be empty.<br/>
                             * This list SHALL NOT contain more than `10` entries.<br/>
                             * The first two entries in this list SHALL be the endpoints published to
                             * all consensus nodes.<br/>
                             * All other entries SHALL be reserved for future use.
                             * @member {Array.<proto.ServiceEndpoint.$Properties>} gossipEndpoint
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.gossipEndpoint = $util.emptyArray;

                            /**
                             * A list of service endpoints for client calls.
                             * <p>
                             * These endpoints SHALL represent the published endpoints to which clients
                             * may submit transactions.<br/>
                             * These endpoints SHALL specify a port.<br/>
                             * Endpoints in this list MAY supply either IP address or FQDN, but SHALL
                             * NOT supply both values for the same endpoint.<br/>
                             * This list SHALL NOT be empty.<br/>
                             * This list SHALL NOT contain more than `8` entries.
                             * @member {Array.<proto.ServiceEndpoint.$Properties>} serviceEndpoint
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.serviceEndpoint = $util.emptyArray;

                            /**
                             * A certificate used to sign gossip events.
                             * <p>
                             * This value SHALL be a certificate of a type permitted for gossip
                             * signatures.<br/>
                             * This value SHALL be the DER encoding of the certificate presented.<br/>
                             * This field is REQUIRED and MUST NOT be empty.
                             * @member {Uint8Array} gossipCaCertificate
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.gossipCaCertificate = $util.newBuffer([]);

                            /**
                             * A hash of the node gRPC certificate.
                             * <p>
                             * This value MAY be used to verify the certificate presented by the node
                             * during TLS negotiation for gRPC.<br/>
                             * This value SHALL be a SHA-384 hash.<br/>
                             * The TLS certificate to be hashed SHALL first be in PEM format and SHALL
                             * be encoded with UTF-8 NFKD encoding to a stream of bytes provided to
                             * the hash algorithm.<br/>
                             * This field is OPTIONAL.
                             * @member {Uint8Array} grpcCertificateHash
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.grpcCertificateHash = $util.newBuffer([]);

                            /**
                             * A consensus weight.
                             * <p>
                             * Each node SHALL have a weight in consensus calculations.<br/>
                             * The consensus weight of a node SHALL be calculated based on the amount
                             * of HBAR staked to that node.<br/>
                             * Consensus SHALL be calculated based on agreement of greater than `2/3`
                             * of the total `weight` value of all nodes on the network.
                             * <p>
                             * This field is deprecated and SHALL NOT be used when RosterLifecycle
                             * is enabled.
                             * @member {number|Long} weight
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.weight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * A flag indicating this node is deleted.
                             * <p>
                             * If this field is set, then this node SHALL NOT be included in the next
                             * update of the network address book.<br/>
                             * If this field is set, then this node SHALL be immutable and SHALL NOT
                             * be modified.<br/>
                             * If this field is set, then any `nodeUpdate` transaction to modify this
                             * node SHALL fail.
                             * @member {boolean} deleted
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.deleted = false;

                            /**
                             * An administrative key controlled by the node operator.
                             * <p>
                             * This key MUST sign each transaction to update this node.<br/>
                             * This field MUST contain a valid `Key` value.<br/>
                             * This field is REQUIRED and MUST NOT be set to an empty `KeyList`.
                             * @member {proto.Key.$Properties|null|undefined} adminKey
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.adminKey = null;

                            /**
                             * A flag indicating this node declines node rewards distributed at
                             * the end of staking period.
                             * <p>
                             * If this field is set, then this node SHALL NOT receive any node rewards
                             * distributed at the end of the staking period.
                             * @member {boolean} declineReward
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.declineReward = false;

                            /**
                             * A web proxy for gRPC from non-gRPC clients.
                             * <p>
                             * This endpoint SHALL be a Fully Qualified Domain Name (FQDN) using the HTTPS
                             * protocol, and SHALL support gRPC-Web for use by browser-based clients.<br/>
                             * This endpoint MUST be signed by a trusted certificate authority.<br/>
                             * This endpoint MUST use a valid port and SHALL be reachable over TLS.<br/>
                             * This field MAY be omitted if the node does not support gRPC-Web access.<br/>
                             * This field MUST be updated if the gRPC-Web endpoint changes.<br/>
                             * This field SHALL enable frontend clients to avoid hard-coded proxy endpoints.
                             * @member {proto.ServiceEndpoint.$Properties|null|undefined} grpcProxyEndpoint
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.grpcProxyEndpoint = null;

                            /**
                             * A list of registered nodes operated by the same entity as this node.<br/>
                             * This value may contain a list of "registered nodes" (as described in
                             * HIP-1137) that are operated by the same entity that operates this
                             * consensus node.
                             * <p>
                             * This field is OPTIONAL and MAY be empty.<br/>
                             * This field MUST NOT contain more than twenty(20) entries.<br/>
                             * Every entry in this list MUST be a valid `registered_node_id` for a
                             * current registered node.
                             * @member {Array.<number|Long>} associatedRegisteredNode
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             */
                            Node.prototype.associatedRegisteredNode = $util.emptyArray;

                            /**
                             * Creates a new Node instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @static
                             * @param {com.hedera.hapi.node.state.addressbook.Node.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.addressbook.Node} Node instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.addressbook.Node.$Shape): com.hedera.hapi.node.state.addressbook.Node & com.hedera.hapi.node.state.addressbook.Node.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.addressbook.Node.$Properties): com.hedera.hapi.node.state.addressbook.Node;
                             * }}
                             */
                            Node.create = function(properties) {
                                return new Node(properties);
                            };

                            /**
                             * Encodes the specified Node message. Does not implicitly {@link com.hedera.hapi.node.state.addressbook.Node.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @static
                             * @param {com.hedera.hapi.node.state.addressbook.Node.$Properties} message Node message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            Node.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.nodeId);
                                if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                                    $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
                                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.description);
                                if (message.gossipEndpoint != null && message.gossipEndpoint.length)
                                    for (let i = 0; i < message.gossipEndpoint.length; ++i)
                                        $root.proto.ServiceEndpoint.encode(message.gossipEndpoint[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                                if (message.serviceEndpoint != null && message.serviceEndpoint.length)
                                    for (let i = 0; i < message.serviceEndpoint.length; ++i)
                                        $root.proto.ServiceEndpoint.encode(message.serviceEndpoint[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                                if (message.gossipCaCertificate != null && $Object.hasOwnProperty.call(message, "gossipCaCertificate") && message.gossipCaCertificate.length)
                                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.gossipCaCertificate);
                                if (message.grpcCertificateHash != null && $Object.hasOwnProperty.call(message, "grpcCertificateHash") && message.grpcCertificateHash.length)
                                    writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.grpcCertificateHash);
                                if (message.weight != null && $Object.hasOwnProperty.call(message, "weight") && (typeof message.weight === "object" ? message.weight.low || message.weight.high : message.weight !== 0))
                                    writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.weight);
                                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                                    writer.uint32(/* id 9, wireType 0 =*/72).bool(message.deleted);
                                if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                    $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
                                if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward") && message.declineReward !== false)
                                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.declineReward);
                                if (message.grpcProxyEndpoint != null && $Object.hasOwnProperty.call(message, "grpcProxyEndpoint"))
                                    $root.proto.ServiceEndpoint.encode(message.grpcProxyEndpoint, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
                                if (message.associatedRegisteredNode != null && message.associatedRegisteredNode.length)
                                    writer.uint32(/* id 13, wireType 2 =*/106).uint64s(message.associatedRegisteredNode);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified Node message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.addressbook.Node.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @static
                             * @param {com.hedera.hapi.node.state.addressbook.Node.$Properties} message Node message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            Node.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a Node message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.addressbook.Node & com.hedera.hapi.node.state.addressbook.Node.$Shape} Node
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            Node.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.addressbook.Node();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.nodeId = value;
                                            else
                                                delete message.nodeId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.stringVerify()).length)
                                                message.description = value;
                                            else
                                                delete message.description;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.gossipEndpoint && message.gossipEndpoint.length))
                                                message.gossipEndpoint = [];
                                            message.gossipEndpoint.push($root.proto.ServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                            continue;
                                        }
                                    case 5: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.serviceEndpoint && message.serviceEndpoint.length))
                                                message.serviceEndpoint = [];
                                            message.serviceEndpoint.push($root.proto.ServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                            continue;
                                        }
                                    case 6: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.gossipCaCertificate = value;
                                            else
                                                delete message.gossipCaCertificate;
                                            continue;
                                        }
                                    case 7: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.grpcCertificateHash = value;
                                            else
                                                delete message.grpcCertificateHash;
                                            continue;
                                        }
                                    case 8: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.weight = value;
                                            else
                                                delete message.weight;
                                            continue;
                                        }
                                    case 9: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.bool())
                                                message.deleted = value;
                                            else
                                                delete message.deleted;
                                            continue;
                                        }
                                    case 10: {
                                            if (wireType !== 2)
                                                break;
                                            message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                                            continue;
                                        }
                                    case 11: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.bool())
                                                message.declineReward = value;
                                            else
                                                delete message.declineReward;
                                            continue;
                                        }
                                    case 12: {
                                            if (wireType !== 2)
                                                break;
                                            message.grpcProxyEndpoint = $root.proto.ServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1, message.grpcProxyEndpoint);
                                            continue;
                                        }
                                    case 13: {
                                            if (wireType === 2) {
                                                if (!(message.associatedRegisteredNode && message.associatedRegisteredNode.length))
                                                    message.associatedRegisteredNode = [];
                                                reader.uint64s(message.associatedRegisteredNode);
                                                continue;
                                            }
                                            if (wireType !== 0)
                                                break;
                                            if (!(message.associatedRegisteredNode && message.associatedRegisteredNode.length))
                                                message.associatedRegisteredNode = [];
                                            message.associatedRegisteredNode.push(reader.uint64());
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a Node message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.addressbook.Node & com.hedera.hapi.node.state.addressbook.Node.$Shape} Node
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            Node.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a Node message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            Node.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                        return "nodeId: integer|Long expected";
                                if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                                    let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                                    if (error)
                                        return "accountId." + error;
                                }
                                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                    if (!$util.isString(message.description))
                                        return "description: string expected";
                                if (message.gossipEndpoint != null && $Object.hasOwnProperty.call(message, "gossipEndpoint")) {
                                    if (!$Array.isArray(message.gossipEndpoint))
                                        return "gossipEndpoint: array expected";
                                    for (let i = 0; i < message.gossipEndpoint.length; ++i) {
                                        let error = $root.proto.ServiceEndpoint.verify(message.gossipEndpoint[i], _depth + 1);
                                        if (error)
                                            return "gossipEndpoint." + error;
                                    }
                                }
                                if (message.serviceEndpoint != null && $Object.hasOwnProperty.call(message, "serviceEndpoint")) {
                                    if (!$Array.isArray(message.serviceEndpoint))
                                        return "serviceEndpoint: array expected";
                                    for (let i = 0; i < message.serviceEndpoint.length; ++i) {
                                        let error = $root.proto.ServiceEndpoint.verify(message.serviceEndpoint[i], _depth + 1);
                                        if (error)
                                            return "serviceEndpoint." + error;
                                    }
                                }
                                if (message.gossipCaCertificate != null && $Object.hasOwnProperty.call(message, "gossipCaCertificate"))
                                    if (!(message.gossipCaCertificate && typeof message.gossipCaCertificate.length === "number" || $util.isString(message.gossipCaCertificate)))
                                        return "gossipCaCertificate: buffer expected";
                                if (message.grpcCertificateHash != null && $Object.hasOwnProperty.call(message, "grpcCertificateHash"))
                                    if (!(message.grpcCertificateHash && typeof message.grpcCertificateHash.length === "number" || $util.isString(message.grpcCertificateHash)))
                                        return "grpcCertificateHash: buffer expected";
                                if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                                    if (!$util.isInteger(message.weight) && !(message.weight && $util.isInteger(message.weight.low) && $util.isInteger(message.weight.high)))
                                        return "weight: integer|Long expected";
                                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                                    if (typeof message.deleted !== "boolean")
                                        return "deleted: boolean expected";
                                if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                                    let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                                    if (error)
                                        return "adminKey." + error;
                                }
                                if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                                    if (typeof message.declineReward !== "boolean")
                                        return "declineReward: boolean expected";
                                if (message.grpcProxyEndpoint != null && $Object.hasOwnProperty.call(message, "grpcProxyEndpoint")) {
                                    let error = $root.proto.ServiceEndpoint.verify(message.grpcProxyEndpoint, _depth + 1);
                                    if (error)
                                        return "grpcProxyEndpoint." + error;
                                }
                                if (message.associatedRegisteredNode != null && $Object.hasOwnProperty.call(message, "associatedRegisteredNode")) {
                                    if (!$Array.isArray(message.associatedRegisteredNode))
                                        return "associatedRegisteredNode: array expected";
                                    for (let i = 0; i < message.associatedRegisteredNode.length; ++i)
                                        if (!$util.isInteger(message.associatedRegisteredNode[i]) && !(message.associatedRegisteredNode[i] && $util.isInteger(message.associatedRegisteredNode[i].low) && $util.isInteger(message.associatedRegisteredNode[i].high)))
                                            return "associatedRegisteredNode: integer|Long[] expected";
                                }
                                return null;
                            };

                            /**
                             * Creates a Node message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.addressbook.Node} Node
                             */
                            Node.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.addressbook.Node)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.addressbook.Node: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.addressbook.Node();
                                if (object.nodeId != null)
                                    if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                        if ($util.Long)
                                            message.nodeId = $util.Long.fromValue(object.nodeId, true);
                                        else if (typeof object.nodeId === "string")
                                            message.nodeId = $parseInt(object.nodeId, 10);
                                        else if (typeof object.nodeId === "number")
                                            message.nodeId = object.nodeId;
                                        else if (typeof object.nodeId === "object")
                                            message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
                                if (object.accountId != null) {
                                    if (!$util.isObject(object.accountId))
                                        throw $TypeError(".com.hedera.hapi.node.state.addressbook.Node.accountId: object expected");
                                    message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
                                }
                                if (object.description != null)
                                    if (typeof object.description !== "string" || object.description.length)
                                        message.description = $String(object.description);
                                if (object.gossipEndpoint) {
                                    if (!$Array.isArray(object.gossipEndpoint))
                                        throw $TypeError(".com.hedera.hapi.node.state.addressbook.Node.gossipEndpoint: array expected");
                                    message.gossipEndpoint = $Array(object.gossipEndpoint.length);
                                    for (let i = 0; i < object.gossipEndpoint.length; ++i) {
                                        if (!$util.isObject(object.gossipEndpoint[i]))
                                            throw $TypeError(".com.hedera.hapi.node.state.addressbook.Node.gossipEndpoint: object expected");
                                        message.gossipEndpoint[i] = $root.proto.ServiceEndpoint.fromObject(object.gossipEndpoint[i], _depth + 1);
                                    }
                                }
                                if (object.serviceEndpoint) {
                                    if (!$Array.isArray(object.serviceEndpoint))
                                        throw $TypeError(".com.hedera.hapi.node.state.addressbook.Node.serviceEndpoint: array expected");
                                    message.serviceEndpoint = $Array(object.serviceEndpoint.length);
                                    for (let i = 0; i < object.serviceEndpoint.length; ++i) {
                                        if (!$util.isObject(object.serviceEndpoint[i]))
                                            throw $TypeError(".com.hedera.hapi.node.state.addressbook.Node.serviceEndpoint: object expected");
                                        message.serviceEndpoint[i] = $root.proto.ServiceEndpoint.fromObject(object.serviceEndpoint[i], _depth + 1);
                                    }
                                }
                                if (object.gossipCaCertificate != null)
                                    if (object.gossipCaCertificate.length)
                                        if (typeof object.gossipCaCertificate === "string")
                                            $util.base64.decode(object.gossipCaCertificate, message.gossipCaCertificate = $util.newBuffer($util.base64.length(object.gossipCaCertificate)), 0);
                                        else if (object.gossipCaCertificate.length >= 0)
                                            message.gossipCaCertificate = object.gossipCaCertificate;
                                if (object.grpcCertificateHash != null)
                                    if (object.grpcCertificateHash.length)
                                        if (typeof object.grpcCertificateHash === "string")
                                            $util.base64.decode(object.grpcCertificateHash, message.grpcCertificateHash = $util.newBuffer($util.base64.length(object.grpcCertificateHash)), 0);
                                        else if (object.grpcCertificateHash.length >= 0)
                                            message.grpcCertificateHash = object.grpcCertificateHash;
                                if (object.weight != null)
                                    if (typeof object.weight === "object" ? object.weight.low || object.weight.high : $Number(object.weight) !== 0)
                                        if ($util.Long)
                                            message.weight = $util.Long.fromValue(object.weight, true);
                                        else if (typeof object.weight === "string")
                                            message.weight = $parseInt(object.weight, 10);
                                        else if (typeof object.weight === "number")
                                            message.weight = object.weight;
                                        else if (typeof object.weight === "object")
                                            message.weight = new $util.LongBits(object.weight.low >>> 0, object.weight.high >>> 0).toNumber(true);
                                if (object.deleted != null)
                                    if (object.deleted)
                                        message.deleted = $Boolean(object.deleted);
                                if (object.adminKey != null) {
                                    if (!$util.isObject(object.adminKey))
                                        throw $TypeError(".com.hedera.hapi.node.state.addressbook.Node.adminKey: object expected");
                                    message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
                                }
                                if (object.declineReward != null)
                                    if (object.declineReward)
                                        message.declineReward = $Boolean(object.declineReward);
                                if (object.grpcProxyEndpoint != null) {
                                    if (!$util.isObject(object.grpcProxyEndpoint))
                                        throw $TypeError(".com.hedera.hapi.node.state.addressbook.Node.grpcProxyEndpoint: object expected");
                                    message.grpcProxyEndpoint = $root.proto.ServiceEndpoint.fromObject(object.grpcProxyEndpoint, _depth + 1);
                                }
                                if (object.associatedRegisteredNode) {
                                    if (!$Array.isArray(object.associatedRegisteredNode))
                                        throw $TypeError(".com.hedera.hapi.node.state.addressbook.Node.associatedRegisteredNode: array expected");
                                    message.associatedRegisteredNode = $Array(object.associatedRegisteredNode.length);
                                    for (let i = 0; i < object.associatedRegisteredNode.length; ++i)
                                        if ($util.Long)
                                            message.associatedRegisteredNode[i] = $util.Long.fromValue(object.associatedRegisteredNode[i], true);
                                        else if (typeof object.associatedRegisteredNode[i] === "string")
                                            message.associatedRegisteredNode[i] = $parseInt(object.associatedRegisteredNode[i], 10);
                                        else if (typeof object.associatedRegisteredNode[i] === "number")
                                            message.associatedRegisteredNode[i] = object.associatedRegisteredNode[i];
                                        else if (typeof object.associatedRegisteredNode[i] === "object")
                                            message.associatedRegisteredNode[i] = new $util.LongBits(object.associatedRegisteredNode[i].low >>> 0, object.associatedRegisteredNode[i].high >>> 0).toNumber(true);
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a Node message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @static
                             * @param {com.hedera.hapi.node.state.addressbook.Node} message Node
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            Node.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults) {
                                    object.gossipEndpoint = [];
                                    object.serviceEndpoint = [];
                                    object.associatedRegisteredNode = [];
                                }
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    object.accountId = null;
                                    object.description = "";
                                    if (options.bytes === $String)
                                        object.gossipCaCertificate = "";
                                    else {
                                        object.gossipCaCertificate = [];
                                        if (options.bytes !== $Array)
                                            object.gossipCaCertificate = $util.newBuffer(object.gossipCaCertificate);
                                    }
                                    if (options.bytes === $String)
                                        object.grpcCertificateHash = "";
                                    else {
                                        object.grpcCertificateHash = [];
                                        if (options.bytes !== $Array)
                                            object.grpcCertificateHash = $util.newBuffer(object.grpcCertificateHash);
                                    }
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.weight = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.weight = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    object.deleted = false;
                                    object.adminKey = null;
                                    object.declineReward = false;
                                    object.grpcProxyEndpoint = null;
                                }
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                                    else if (typeof message.nodeId === "number")
                                        object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                    else
                                        object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
                                if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                                    object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
                                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                    object.description = message.description;
                                if (message.gossipEndpoint && message.gossipEndpoint.length) {
                                    object.gossipEndpoint = $Array(message.gossipEndpoint.length);
                                    for (let j = 0; j < message.gossipEndpoint.length; ++j)
                                        object.gossipEndpoint[j] = $root.proto.ServiceEndpoint.toObject(message.gossipEndpoint[j], options, _depth + 1);
                                }
                                if (message.serviceEndpoint && message.serviceEndpoint.length) {
                                    object.serviceEndpoint = $Array(message.serviceEndpoint.length);
                                    for (let j = 0; j < message.serviceEndpoint.length; ++j)
                                        object.serviceEndpoint[j] = $root.proto.ServiceEndpoint.toObject(message.serviceEndpoint[j], options, _depth + 1);
                                }
                                if (message.gossipCaCertificate != null && $Object.hasOwnProperty.call(message, "gossipCaCertificate"))
                                    object.gossipCaCertificate = options.bytes === $String ? $util.base64.encode(message.gossipCaCertificate, 0, message.gossipCaCertificate.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.gossipCaCertificate) : message.gossipCaCertificate;
                                if (message.grpcCertificateHash != null && $Object.hasOwnProperty.call(message, "grpcCertificateHash"))
                                    object.grpcCertificateHash = options.bytes === $String ? $util.base64.encode(message.grpcCertificateHash, 0, message.grpcCertificateHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.grpcCertificateHash) : message.grpcCertificateHash;
                                if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.weight = typeof message.weight === "number" ? $BigInt(message.weight) : $util.Long.fromBits(message.weight.low >>> 0, message.weight.high >>> 0, true).toBigInt();
                                    else if (typeof message.weight === "number")
                                        object.weight = options.longs === $String ? $String(message.weight) : message.weight;
                                    else
                                        object.weight = options.longs === $String ? $util.Long.prototype.toString.call(message.weight) : options.longs === $Number ? new $util.LongBits(message.weight.low >>> 0, message.weight.high >>> 0).toNumber(true) : message.weight;
                                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                                    object.deleted = message.deleted;
                                if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                    object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
                                if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                                    object.declineReward = message.declineReward;
                                if (message.grpcProxyEndpoint != null && $Object.hasOwnProperty.call(message, "grpcProxyEndpoint"))
                                    object.grpcProxyEndpoint = $root.proto.ServiceEndpoint.toObject(message.grpcProxyEndpoint, options, _depth + 1);
                                if (message.associatedRegisteredNode && message.associatedRegisteredNode.length) {
                                    object.associatedRegisteredNode = $Array(message.associatedRegisteredNode.length);
                                    for (let j = 0; j < message.associatedRegisteredNode.length; ++j)
                                        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                            object.associatedRegisteredNode[j] = typeof message.associatedRegisteredNode[j] === "number" ? $BigInt(message.associatedRegisteredNode[j]) : $util.Long.fromBits(message.associatedRegisteredNode[j].low >>> 0, message.associatedRegisteredNode[j].high >>> 0, true).toBigInt();
                                        else if (typeof message.associatedRegisteredNode[j] === "number")
                                            object.associatedRegisteredNode[j] = options.longs === $String ? $String(message.associatedRegisteredNode[j]) : message.associatedRegisteredNode[j];
                                        else
                                            object.associatedRegisteredNode[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.associatedRegisteredNode[j]) : options.longs === $Number ? new $util.LongBits(message.associatedRegisteredNode[j].low >>> 0, message.associatedRegisteredNode[j].high >>> 0).toNumber(true) : message.associatedRegisteredNode[j];
                                }
                                return object;
                            };

                            /**
                             * Converts this Node to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            Node.prototype.toJSON = function() {
                                return Node.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for Node
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.addressbook.Node
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            Node.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.addressbook.Node";
                            };

                            return Node;
                        })();

                        addressbook.RegisteredNode = (function() {

                            /**
                             * Properties of a RegisteredNode.
                             * @typedef {Object} com.hedera.hapi.node.state.addressbook.RegisteredNode.$Properties
                             * @property {number|Long|null} [registeredNodeId] A registered node identifier.
                             * <p>
                             * This value SHALL be set.<br/>
                             * This value SHALL be unique within a given network.<br/>
                             * This value SHALL NOT match any consensus node ID in the same network.
                             * <p>
                             * A given value for `registered_node_id` MUST be unique within a given
                             * shard or realm.<br/>
                             * A given value for `registered_node_id` MUST NOT be reused, even if the
                             * corresponding entry is later deleted.
                             * @property {proto.Key.$Properties|null} [adminKey] An administrative key controlled by the node operator.
                             * <p>
                             * This key MUST sign each transaction to update this node.<br/>
                             * This field SHALL contain a valid `Key` value.<br/>
                             * This field SHALL NOT be set to an empty `KeyList`.<br/>
                             * It is RECOMMENDED that this key be composed of one or more unique public
                             * keys that are not associated with any network account.<br/>
                             * This key MAY be a complex key containing `KeyList` or `ThresholdKey`
                             * elements, but SHOULD NOT be a contract ID key.
                             * @property {string|null} [description] A short description of the node.
                             * <p>
                             * This value SHALL NOT exceed 100 bytes when encoded as UTF-8.
                             * @property {Array.<com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties>|null} [serviceEndpoint] A list of service endpoints for client calls.
                             * <p>
                             * These endpoints SHALL represent the published endpoints to which
                             * clients may submit requests.<br/>
                             * One Registered Node MAY expose endpoints for multiple service types.<br/>
                             * This list SHALL NOT be empty.<br/>
                             * This list SHALL NOT contain more than `50` entries.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a RegisteredNode.
                             * @memberof com.hedera.hapi.node.state.addressbook
                             * @interface IRegisteredNode
                             * @augments com.hedera.hapi.node.state.addressbook.RegisteredNode.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.addressbook.RegisteredNode.$Properties instead.
                             */

                            /**
                             * Shape of a RegisteredNode.
                             * @typedef {{
                             *   registeredNodeId?: number|Long|null;
                             *   adminKey?: proto.Key.$Shape|null;
                             *   description?: string|null;
                             *   serviceEndpoint?: Array.<com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Shape>|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * }} com.hedera.hapi.node.state.addressbook.RegisteredNode.$Shape
                             */

                            /**
                             * Constructs a new RegisteredNode.
                             * @memberof com.hedera.hapi.node.state.addressbook
                             * @classdesc A single registered node in the network state.
                             * 
                             * Each registered node in the network state SHALL represent a single
                             * non-consensus node that is registered on the network.
                             * 
                             * Registered node identifiers SHALL only be unique within a single
                             * realm and shard combination.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.addressbook.RegisteredNode.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const RegisteredNode = function (properties) {
                                this.serviceEndpoint = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * A registered node identifier.
                             * <p>
                             * This value SHALL be set.<br/>
                             * This value SHALL be unique within a given network.<br/>
                             * This value SHALL NOT match any consensus node ID in the same network.
                             * <p>
                             * A given value for `registered_node_id` MUST be unique within a given
                             * shard or realm.<br/>
                             * A given value for `registered_node_id` MUST NOT be reused, even if the
                             * corresponding entry is later deleted.
                             * @member {number|Long} registeredNodeId
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @instance
                             */
                            RegisteredNode.prototype.registeredNodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * An administrative key controlled by the node operator.
                             * <p>
                             * This key MUST sign each transaction to update this node.<br/>
                             * This field SHALL contain a valid `Key` value.<br/>
                             * This field SHALL NOT be set to an empty `KeyList`.<br/>
                             * It is RECOMMENDED that this key be composed of one or more unique public
                             * keys that are not associated with any network account.<br/>
                             * This key MAY be a complex key containing `KeyList` or `ThresholdKey`
                             * elements, but SHOULD NOT be a contract ID key.
                             * @member {proto.Key.$Properties|null|undefined} adminKey
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @instance
                             */
                            RegisteredNode.prototype.adminKey = null;

                            /**
                             * A short description of the node.
                             * <p>
                             * This value SHALL NOT exceed 100 bytes when encoded as UTF-8.
                             * @member {string} description
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @instance
                             */
                            RegisteredNode.prototype.description = "";

                            /**
                             * A list of service endpoints for client calls.
                             * <p>
                             * These endpoints SHALL represent the published endpoints to which
                             * clients may submit requests.<br/>
                             * One Registered Node MAY expose endpoints for multiple service types.<br/>
                             * This list SHALL NOT be empty.<br/>
                             * This list SHALL NOT contain more than `50` entries.
                             * @member {Array.<com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.$Properties>} serviceEndpoint
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @instance
                             */
                            RegisteredNode.prototype.serviceEndpoint = $util.emptyArray;

                            /**
                             * Creates a new RegisteredNode instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @static
                             * @param {com.hedera.hapi.node.state.addressbook.RegisteredNode.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.addressbook.RegisteredNode} RegisteredNode instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.addressbook.RegisteredNode.$Shape): com.hedera.hapi.node.state.addressbook.RegisteredNode & com.hedera.hapi.node.state.addressbook.RegisteredNode.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.addressbook.RegisteredNode.$Properties): com.hedera.hapi.node.state.addressbook.RegisteredNode;
                             * }}
                             */
                            RegisteredNode.create = function(properties) {
                                return new RegisteredNode(properties);
                            };

                            /**
                             * Encodes the specified RegisteredNode message. Does not implicitly {@link com.hedera.hapi.node.state.addressbook.RegisteredNode.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @static
                             * @param {com.hedera.hapi.node.state.addressbook.RegisteredNode.$Properties} message RegisteredNode message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RegisteredNode.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.registeredNodeId != null && $Object.hasOwnProperty.call(message, "registeredNodeId") && (typeof message.registeredNodeId === "object" ? message.registeredNodeId.low || message.registeredNodeId.high : message.registeredNodeId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.registeredNodeId);
                                if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                    $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
                                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.description);
                                if (message.serviceEndpoint != null && message.serviceEndpoint.length)
                                    for (let i = 0; i < message.serviceEndpoint.length; ++i)
                                        $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.encode(message.serviceEndpoint[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified RegisteredNode message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.addressbook.RegisteredNode.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @static
                             * @param {com.hedera.hapi.node.state.addressbook.RegisteredNode.$Properties} message RegisteredNode message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RegisteredNode.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a RegisteredNode message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.addressbook.RegisteredNode & com.hedera.hapi.node.state.addressbook.RegisteredNode.$Shape} RegisteredNode
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RegisteredNode.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.addressbook.RegisteredNode();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.registeredNodeId = value;
                                            else
                                                delete message.registeredNodeId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.stringVerify()).length)
                                                message.description = value;
                                            else
                                                delete message.description;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.serviceEndpoint && message.serviceEndpoint.length))
                                                message.serviceEndpoint = [];
                                            message.serviceEndpoint.push($root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a RegisteredNode message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.addressbook.RegisteredNode & com.hedera.hapi.node.state.addressbook.RegisteredNode.$Shape} RegisteredNode
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RegisteredNode.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a RegisteredNode message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            RegisteredNode.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.registeredNodeId != null && $Object.hasOwnProperty.call(message, "registeredNodeId"))
                                    if (!$util.isInteger(message.registeredNodeId) && !(message.registeredNodeId && $util.isInteger(message.registeredNodeId.low) && $util.isInteger(message.registeredNodeId.high)))
                                        return "registeredNodeId: integer|Long expected";
                                if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                                    let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                                    if (error)
                                        return "adminKey." + error;
                                }
                                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                    if (!$util.isString(message.description))
                                        return "description: string expected";
                                if (message.serviceEndpoint != null && $Object.hasOwnProperty.call(message, "serviceEndpoint")) {
                                    if (!$Array.isArray(message.serviceEndpoint))
                                        return "serviceEndpoint: array expected";
                                    for (let i = 0; i < message.serviceEndpoint.length; ++i) {
                                        let error = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.verify(message.serviceEndpoint[i], _depth + 1);
                                        if (error)
                                            return "serviceEndpoint." + error;
                                    }
                                }
                                return null;
                            };

                            /**
                             * Creates a RegisteredNode message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.addressbook.RegisteredNode} RegisteredNode
                             */
                            RegisteredNode.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.addressbook.RegisteredNode)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.addressbook.RegisteredNode: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.addressbook.RegisteredNode();
                                if (object.registeredNodeId != null)
                                    if (typeof object.registeredNodeId === "object" ? object.registeredNodeId.low || object.registeredNodeId.high : $Number(object.registeredNodeId) !== 0)
                                        if ($util.Long)
                                            message.registeredNodeId = $util.Long.fromValue(object.registeredNodeId, true);
                                        else if (typeof object.registeredNodeId === "string")
                                            message.registeredNodeId = $parseInt(object.registeredNodeId, 10);
                                        else if (typeof object.registeredNodeId === "number")
                                            message.registeredNodeId = object.registeredNodeId;
                                        else if (typeof object.registeredNodeId === "object")
                                            message.registeredNodeId = new $util.LongBits(object.registeredNodeId.low >>> 0, object.registeredNodeId.high >>> 0).toNumber(true);
                                if (object.adminKey != null) {
                                    if (!$util.isObject(object.adminKey))
                                        throw $TypeError(".com.hedera.hapi.node.state.addressbook.RegisteredNode.adminKey: object expected");
                                    message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
                                }
                                if (object.description != null)
                                    if (typeof object.description !== "string" || object.description.length)
                                        message.description = $String(object.description);
                                if (object.serviceEndpoint) {
                                    if (!$Array.isArray(object.serviceEndpoint))
                                        throw $TypeError(".com.hedera.hapi.node.state.addressbook.RegisteredNode.serviceEndpoint: array expected");
                                    message.serviceEndpoint = $Array(object.serviceEndpoint.length);
                                    for (let i = 0; i < object.serviceEndpoint.length; ++i) {
                                        if (!$util.isObject(object.serviceEndpoint[i]))
                                            throw $TypeError(".com.hedera.hapi.node.state.addressbook.RegisteredNode.serviceEndpoint: object expected");
                                        message.serviceEndpoint[i] = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.fromObject(object.serviceEndpoint[i], _depth + 1);
                                    }
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a RegisteredNode message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @static
                             * @param {com.hedera.hapi.node.state.addressbook.RegisteredNode} message RegisteredNode
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            RegisteredNode.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults)
                                    object.serviceEndpoint = [];
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.registeredNodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.registeredNodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    object.adminKey = null;
                                    object.description = "";
                                }
                                if (message.registeredNodeId != null && $Object.hasOwnProperty.call(message, "registeredNodeId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.registeredNodeId = typeof message.registeredNodeId === "number" ? $BigInt(message.registeredNodeId) : $util.Long.fromBits(message.registeredNodeId.low >>> 0, message.registeredNodeId.high >>> 0, true).toBigInt();
                                    else if (typeof message.registeredNodeId === "number")
                                        object.registeredNodeId = options.longs === $String ? $String(message.registeredNodeId) : message.registeredNodeId;
                                    else
                                        object.registeredNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.registeredNodeId) : options.longs === $Number ? new $util.LongBits(message.registeredNodeId.low >>> 0, message.registeredNodeId.high >>> 0).toNumber(true) : message.registeredNodeId;
                                if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                    object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
                                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                                    object.description = message.description;
                                if (message.serviceEndpoint && message.serviceEndpoint.length) {
                                    object.serviceEndpoint = $Array(message.serviceEndpoint.length);
                                    for (let j = 0; j < message.serviceEndpoint.length; ++j)
                                        object.serviceEndpoint[j] = $root.com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.toObject(message.serviceEndpoint[j], options, _depth + 1);
                                }
                                return object;
                            };

                            /**
                             * Converts this RegisteredNode to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            RegisteredNode.prototype.toJSON = function() {
                                return RegisteredNode.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for RegisteredNode
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.addressbook.RegisteredNode
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            RegisteredNode.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.addressbook.RegisteredNode";
                            };

                            return RegisteredNode;
                        })();

                        return addressbook;
                    })();

                    state.blockrecords = (function() {

                        /**
                         * Namespace blockrecords.
                         * @memberof com.hedera.hapi.node.state
                         * @namespace
                         */
                        const blockrecords = {};

                        blockrecords.MigrationWrappedHashes = (function() {

                            /**
                             * Properties of a MigrationWrappedHashes.
                             * @typedef {Object} com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Properties
                             * @property {number|Long|null} [blockNumber] The block number these wrapped hashes belong to.
                             * @property {Uint8Array|null} [consensusTimestampHash] The wrapped-record consensus timestamp hash for a block.
                             * @property {Uint8Array|null} [outputItemsTreeRootHash] The wrapped-record output items tree root hash for a block.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a MigrationWrappedHashes.
                             * @memberof com.hedera.hapi.node.state.blockrecords
                             * @interface IMigrationWrappedHashes
                             * @augments com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Properties instead.
                             */

                            /**
                             * Shape of a MigrationWrappedHashes.
                             * @typedef {com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Properties} com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Shape
                             */

                            /**
                             * Constructs a new MigrationWrappedHashes.
                             * @memberof com.hedera.hapi.node.state.blockrecords
                             * @classdesc Values needed to replay wrapped record hash progression while voting is in progress.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const MigrationWrappedHashes = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The block number these wrapped hashes belong to.
                             * @member {number|Long} blockNumber
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @instance
                             */
                            MigrationWrappedHashes.prototype.blockNumber = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The wrapped-record consensus timestamp hash for a block.
                             * @member {Uint8Array} consensusTimestampHash
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @instance
                             */
                            MigrationWrappedHashes.prototype.consensusTimestampHash = $util.newBuffer([]);

                            /**
                             * The wrapped-record output items tree root hash for a block.
                             * @member {Uint8Array} outputItemsTreeRootHash
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @instance
                             */
                            MigrationWrappedHashes.prototype.outputItemsTreeRootHash = $util.newBuffer([]);

                            /**
                             * Creates a new MigrationWrappedHashes instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @static
                             * @param {com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes} MigrationWrappedHashes instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Shape): com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes & com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Properties): com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes;
                             * }}
                             */
                            MigrationWrappedHashes.create = function(properties) {
                                return new MigrationWrappedHashes(properties);
                            };

                            /**
                             * Encodes the specified MigrationWrappedHashes message. Does not implicitly {@link com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @static
                             * @param {com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Properties} message MigrationWrappedHashes message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            MigrationWrappedHashes.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.blockNumber != null && $Object.hasOwnProperty.call(message, "blockNumber") && (typeof message.blockNumber === "object" ? message.blockNumber.low || message.blockNumber.high : message.blockNumber !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.blockNumber);
                                if (message.consensusTimestampHash != null && $Object.hasOwnProperty.call(message, "consensusTimestampHash") && message.consensusTimestampHash.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.consensusTimestampHash);
                                if (message.outputItemsTreeRootHash != null && $Object.hasOwnProperty.call(message, "outputItemsTreeRootHash") && message.outputItemsTreeRootHash.length)
                                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.outputItemsTreeRootHash);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified MigrationWrappedHashes message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @static
                             * @param {com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Properties} message MigrationWrappedHashes message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            MigrationWrappedHashes.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a MigrationWrappedHashes message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes & com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Shape} MigrationWrappedHashes
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            MigrationWrappedHashes.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.blockNumber = value;
                                            else
                                                delete message.blockNumber;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.consensusTimestampHash = value;
                                            else
                                                delete message.consensusTimestampHash;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.outputItemsTreeRootHash = value;
                                            else
                                                delete message.outputItemsTreeRootHash;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a MigrationWrappedHashes message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes & com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Shape} MigrationWrappedHashes
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            MigrationWrappedHashes.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a MigrationWrappedHashes message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            MigrationWrappedHashes.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.blockNumber != null && $Object.hasOwnProperty.call(message, "blockNumber"))
                                    if (!$util.isInteger(message.blockNumber) && !(message.blockNumber && $util.isInteger(message.blockNumber.low) && $util.isInteger(message.blockNumber.high)))
                                        return "blockNumber: integer|Long expected";
                                if (message.consensusTimestampHash != null && $Object.hasOwnProperty.call(message, "consensusTimestampHash"))
                                    if (!(message.consensusTimestampHash && typeof message.consensusTimestampHash.length === "number" || $util.isString(message.consensusTimestampHash)))
                                        return "consensusTimestampHash: buffer expected";
                                if (message.outputItemsTreeRootHash != null && $Object.hasOwnProperty.call(message, "outputItemsTreeRootHash"))
                                    if (!(message.outputItemsTreeRootHash && typeof message.outputItemsTreeRootHash.length === "number" || $util.isString(message.outputItemsTreeRootHash)))
                                        return "outputItemsTreeRootHash: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a MigrationWrappedHashes message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes} MigrationWrappedHashes
                             */
                            MigrationWrappedHashes.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes();
                                if (object.blockNumber != null)
                                    if (typeof object.blockNumber === "object" ? object.blockNumber.low || object.blockNumber.high : $Number(object.blockNumber) !== 0)
                                        if ($util.Long)
                                            message.blockNumber = $util.Long.fromValue(object.blockNumber, true);
                                        else if (typeof object.blockNumber === "string")
                                            message.blockNumber = $parseInt(object.blockNumber, 10);
                                        else if (typeof object.blockNumber === "number")
                                            message.blockNumber = object.blockNumber;
                                        else if (typeof object.blockNumber === "object")
                                            message.blockNumber = new $util.LongBits(object.blockNumber.low >>> 0, object.blockNumber.high >>> 0).toNumber(true);
                                if (object.consensusTimestampHash != null)
                                    if (object.consensusTimestampHash.length)
                                        if (typeof object.consensusTimestampHash === "string")
                                            $util.base64.decode(object.consensusTimestampHash, message.consensusTimestampHash = $util.newBuffer($util.base64.length(object.consensusTimestampHash)), 0);
                                        else if (object.consensusTimestampHash.length >= 0)
                                            message.consensusTimestampHash = object.consensusTimestampHash;
                                if (object.outputItemsTreeRootHash != null)
                                    if (object.outputItemsTreeRootHash.length)
                                        if (typeof object.outputItemsTreeRootHash === "string")
                                            $util.base64.decode(object.outputItemsTreeRootHash, message.outputItemsTreeRootHash = $util.newBuffer($util.base64.length(object.outputItemsTreeRootHash)), 0);
                                        else if (object.outputItemsTreeRootHash.length >= 0)
                                            message.outputItemsTreeRootHash = object.outputItemsTreeRootHash;
                                return message;
                            };

                            /**
                             * Creates a plain object from a MigrationWrappedHashes message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @static
                             * @param {com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes} message MigrationWrappedHashes
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            MigrationWrappedHashes.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.blockNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.blockNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if (options.bytes === $String)
                                        object.consensusTimestampHash = "";
                                    else {
                                        object.consensusTimestampHash = [];
                                        if (options.bytes !== $Array)
                                            object.consensusTimestampHash = $util.newBuffer(object.consensusTimestampHash);
                                    }
                                    if (options.bytes === $String)
                                        object.outputItemsTreeRootHash = "";
                                    else {
                                        object.outputItemsTreeRootHash = [];
                                        if (options.bytes !== $Array)
                                            object.outputItemsTreeRootHash = $util.newBuffer(object.outputItemsTreeRootHash);
                                    }
                                }
                                if (message.blockNumber != null && $Object.hasOwnProperty.call(message, "blockNumber"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.blockNumber = typeof message.blockNumber === "number" ? $BigInt(message.blockNumber) : $util.Long.fromBits(message.blockNumber.low >>> 0, message.blockNumber.high >>> 0, true).toBigInt();
                                    else if (typeof message.blockNumber === "number")
                                        object.blockNumber = options.longs === $String ? $String(message.blockNumber) : message.blockNumber;
                                    else
                                        object.blockNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.blockNumber) : options.longs === $Number ? new $util.LongBits(message.blockNumber.low >>> 0, message.blockNumber.high >>> 0).toNumber(true) : message.blockNumber;
                                if (message.consensusTimestampHash != null && $Object.hasOwnProperty.call(message, "consensusTimestampHash"))
                                    object.consensusTimestampHash = options.bytes === $String ? $util.base64.encode(message.consensusTimestampHash, 0, message.consensusTimestampHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.consensusTimestampHash) : message.consensusTimestampHash;
                                if (message.outputItemsTreeRootHash != null && $Object.hasOwnProperty.call(message, "outputItemsTreeRootHash"))
                                    object.outputItemsTreeRootHash = options.bytes === $String ? $util.base64.encode(message.outputItemsTreeRootHash, 0, message.outputItemsTreeRootHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.outputItemsTreeRootHash) : message.outputItemsTreeRootHash;
                                return object;
                            };

                            /**
                             * Converts this MigrationWrappedHashes to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            MigrationWrappedHashes.prototype.toJSON = function() {
                                return MigrationWrappedHashes.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for MigrationWrappedHashes
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            MigrationWrappedHashes.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes";
                            };

                            return MigrationWrappedHashes;
                        })();

                        return blockrecords;
                    })();

                    state.blockstream = (function() {

                        /**
                         * Namespace blockstream.
                         * @memberof com.hedera.hapi.node.state
                         * @namespace
                         */
                        const blockstream = {};

                        blockstream.BlockStreamInfo = (function() {

                            /**
                             * Properties of a BlockStreamInfo.
                             * @typedef {Object} com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Properties
                             * @property {number|Long|null} [blockNumber] A block number.<br/>
                             * This is the current block number.
                             * @property {proto.Timestamp.$Properties|null} [blockTime] A consensus time for the current block.<br/>
                             * This is the consensus time of the first round in the current block,
                             * which equates to the first contained transaction's consensus time.
                             * It is used to determine if this block was the first across an
                             * important boundary in consensus time, such as UTC midnight.
                             * This may also be used to purge entities expiring between the last
                             * block time and this time.
                             * @property {Uint8Array|null} [trailingOutputHashes] A concatenation of hash values.<br/>
                             * This combines several trailing output block item hashes and
                             * is used as a seed value for a pseudo-random number generator.<br/>
                             * This is also required to implement the EVM `PREVRANDAO` opcode.<br/>
                             * This MUST contain at least 256 bits of entropy.
                             * @property {Uint8Array|null} [trailingBlockHashes] A concatenation of hash values.<br/>
                             * This field combines up to 256 trailing block hashes.
                             * <p>
                             * If this message is for block number N, then the earliest available
                             * hash SHALL be for block number N-256.<br/>
                             * The latest available hash SHALL be for block N-1.<br/>
                             * This is REQUIRED to implement the EVM `BLOCKHASH` opcode.
                             * <p>
                             * ### Field Length
                             * Each hash value SHALL be the trailing 265 bits of a SHA2-384 hash.<br/>
                             * The length of this field SHALL be an integer multiple of 32 bytes.<br/>
                             * This field SHALL be at least 32 bytes.<br/>
                             * The maximum length of this field SHALL be 8192 bytes.
                             * @property {Uint8Array|null} [inputTreeRootHash] A SHA2-384 hash value.<br/>
                             * This is the final hash of the "input" subtree for this block.
                             * @property {Uint8Array|null} [startOfBlockStateHash] A SHA2-384 hash value.<br/>
                             * This is the hash of consensus state at the _start_ of this block.
                             * @property {number|Long|null} [numPrecedingStateChangesItems] A count of "state change" block items in this block.
                             * <p>
                             * This SHALL count the number of state change block items that _precede_
                             * the state change that updates this singleton.
                             * @property {Array.<Uint8Array>|null} [rightmostPrecedingStateChangesTreeHashes] A concatenation of SHA2-384 hash values.<br/>
                             * This is the "rightmost" values of the "output" subtree.
                             * <p>
                             * The subtree containing these hashes SHALL be constructed from all "output"
                             * `BlockItem`s in this block that _precede_ the update to this singleton.
                             * @property {proto.Timestamp.$Properties|null} [blockEndTime] A block-end consensus time stamp.
                             * <p>
                             * This field SHALL hold the last-used consensus time for
                             * the current block.
                             * @property {boolean|null} [postUpgradeWorkDone] Whether the post-upgrade work has been done.
                             * <p>
                             * This MUST be false if and only if the network just restarted
                             * after an upgrade and has not yet done the post-upgrade work.
                             * @property {proto.SemanticVersion.$Properties|null} [creationSoftwareVersion] A version describing the version of application software.
                             * <p>
                             * This SHALL be the software version that created this block.
                             * @property {proto.Timestamp.$Properties|null} [lastIntervalProcessTime] The time stamp at which the last interval process was done.
                             * <p>
                             * This field SHALL hold the consensus time for the last time
                             * at which an interval of time-dependent events were processed.
                             * @property {proto.Timestamp.$Properties|null} [lastHandleTime] The time stamp at which the last user transaction was handled.
                             * <p>
                             * This field SHALL hold the consensus time for the last time
                             * at which a user transaction was handled.
                             * @property {Uint8Array|null} [consensusHeaderRootHash] A SHA2-384 hash value.<br/>
                             * This is the final hash of the "consensus headers" subtree for this block.
                             * @property {Uint8Array|null} [outputItemRootHash] A SHA2-384 hash value.<br/>
                             * This is the final hash of the "output" subtree for this block.
                             * @property {Uint8Array|null} [traceDataRootHash] A SHA2-384 hash value.<br/>
                             * This is the final hash of the "trace data" subtree for this block.
                             * @property {Array.<Uint8Array>|null} [intermediatePreviousBlockRootHashes] The intermediate hashes needed for subroot 2 in the block merkle
                             * tree structure. These hashes SHALL include the minimum required
                             * block root hashes needed to construct subroot 2's final state at
                             * the end of the previous block.
                             * @property {number|Long|null} [intermediateBlockRootsLeafCount] The number of leaves in the intermediate block roots subtree.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a BlockStreamInfo.
                             * @memberof com.hedera.hapi.node.state.blockstream
                             * @interface IBlockStreamInfo
                             * @augments com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Properties instead.
                             */

                            /**
                             * Shape of a BlockStreamInfo.
                             * @typedef {com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Properties} com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Shape
                             */

                            /**
                             * Constructs a new BlockStreamInfo.
                             * @memberof com.hedera.hapi.node.state.blockstream
                             * @classdesc A message stored in state to maintain block stream parameters.<br/>
                             * Nodes use this information for three purposes.
                             * 1. To maintain hash chain continuity at restart and reconnect boundaries.
                             * 2. To store historical hashes for implementation of the EVM `BLOCKHASH`
                             * and `PREVRANDAO` opcodes.
                             * 3. To track the amount of consensus time that has passed between blocks.
                             * 
                             * This value MUST be updated for every block.<br/>
                             * This value MUST be transmitted in the "state changes" section of
                             * _each_ block, but MUST be updated at the beginning of the _next_ block.<br/>
                             * This value SHALL contain the block hash up to, and including, the
                             * immediately prior completed block.<br/>
                             * The state change to update this singleton MUST be the last
                             * block item in this block.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const BlockStreamInfo = function (properties) {
                                this.rightmostPrecedingStateChangesTreeHashes = [];
                                this.intermediatePreviousBlockRootHashes = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * A block number.<br/>
                             * This is the current block number.
                             * @member {number|Long} blockNumber
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.blockNumber = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * A consensus time for the current block.<br/>
                             * This is the consensus time of the first round in the current block,
                             * which equates to the first contained transaction's consensus time.
                             * It is used to determine if this block was the first across an
                             * important boundary in consensus time, such as UTC midnight.
                             * This may also be used to purge entities expiring between the last
                             * block time and this time.
                             * @member {proto.Timestamp.$Properties|null|undefined} blockTime
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.blockTime = null;

                            /**
                             * A concatenation of hash values.<br/>
                             * This combines several trailing output block item hashes and
                             * is used as a seed value for a pseudo-random number generator.<br/>
                             * This is also required to implement the EVM `PREVRANDAO` opcode.<br/>
                             * This MUST contain at least 256 bits of entropy.
                             * @member {Uint8Array} trailingOutputHashes
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.trailingOutputHashes = $util.newBuffer([]);

                            /**
                             * A concatenation of hash values.<br/>
                             * This field combines up to 256 trailing block hashes.
                             * <p>
                             * If this message is for block number N, then the earliest available
                             * hash SHALL be for block number N-256.<br/>
                             * The latest available hash SHALL be for block N-1.<br/>
                             * This is REQUIRED to implement the EVM `BLOCKHASH` opcode.
                             * <p>
                             * ### Field Length
                             * Each hash value SHALL be the trailing 265 bits of a SHA2-384 hash.<br/>
                             * The length of this field SHALL be an integer multiple of 32 bytes.<br/>
                             * This field SHALL be at least 32 bytes.<br/>
                             * The maximum length of this field SHALL be 8192 bytes.
                             * @member {Uint8Array} trailingBlockHashes
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.trailingBlockHashes = $util.newBuffer([]);

                            /**
                             * A SHA2-384 hash value.<br/>
                             * This is the final hash of the "input" subtree for this block.
                             * @member {Uint8Array} inputTreeRootHash
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.inputTreeRootHash = $util.newBuffer([]);

                            /**
                             * A SHA2-384 hash value.<br/>
                             * This is the hash of consensus state at the _start_ of this block.
                             * @member {Uint8Array} startOfBlockStateHash
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.startOfBlockStateHash = $util.newBuffer([]);

                            /**
                             * A count of "state change" block items in this block.
                             * <p>
                             * This SHALL count the number of state change block items that _precede_
                             * the state change that updates this singleton.
                             * @member {number|Long} numPrecedingStateChangesItems
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.numPrecedingStateChangesItems = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * A concatenation of SHA2-384 hash values.<br/>
                             * This is the "rightmost" values of the "output" subtree.
                             * <p>
                             * The subtree containing these hashes SHALL be constructed from all "output"
                             * `BlockItem`s in this block that _precede_ the update to this singleton.
                             * @member {Array.<Uint8Array>} rightmostPrecedingStateChangesTreeHashes
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.rightmostPrecedingStateChangesTreeHashes = $util.emptyArray;

                            /**
                             * A block-end consensus time stamp.
                             * <p>
                             * This field SHALL hold the last-used consensus time for
                             * the current block.
                             * @member {proto.Timestamp.$Properties|null|undefined} blockEndTime
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.blockEndTime = null;

                            /**
                             * Whether the post-upgrade work has been done.
                             * <p>
                             * This MUST be false if and only if the network just restarted
                             * after an upgrade and has not yet done the post-upgrade work.
                             * @member {boolean} postUpgradeWorkDone
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.postUpgradeWorkDone = false;

                            /**
                             * A version describing the version of application software.
                             * <p>
                             * This SHALL be the software version that created this block.
                             * @member {proto.SemanticVersion.$Properties|null|undefined} creationSoftwareVersion
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.creationSoftwareVersion = null;

                            /**
                             * The time stamp at which the last interval process was done.
                             * <p>
                             * This field SHALL hold the consensus time for the last time
                             * at which an interval of time-dependent events were processed.
                             * @member {proto.Timestamp.$Properties|null|undefined} lastIntervalProcessTime
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.lastIntervalProcessTime = null;

                            /**
                             * The time stamp at which the last user transaction was handled.
                             * <p>
                             * This field SHALL hold the consensus time for the last time
                             * at which a user transaction was handled.
                             * @member {proto.Timestamp.$Properties|null|undefined} lastHandleTime
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.lastHandleTime = null;

                            /**
                             * A SHA2-384 hash value.<br/>
                             * This is the final hash of the "consensus headers" subtree for this block.
                             * @member {Uint8Array} consensusHeaderRootHash
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.consensusHeaderRootHash = $util.newBuffer([]);

                            /**
                             * A SHA2-384 hash value.<br/>
                             * This is the final hash of the "output" subtree for this block.
                             * @member {Uint8Array} outputItemRootHash
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.outputItemRootHash = $util.newBuffer([]);

                            /**
                             * A SHA2-384 hash value.<br/>
                             * This is the final hash of the "trace data" subtree for this block.
                             * @member {Uint8Array} traceDataRootHash
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.traceDataRootHash = $util.newBuffer([]);

                            /**
                             * The intermediate hashes needed for subroot 2 in the block merkle
                             * tree structure. These hashes SHALL include the minimum required
                             * block root hashes needed to construct subroot 2's final state at
                             * the end of the previous block.
                             * @member {Array.<Uint8Array>} intermediatePreviousBlockRootHashes
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.intermediatePreviousBlockRootHashes = $util.emptyArray;

                            /**
                             * The number of leaves in the intermediate block roots subtree.
                             * @member {number|Long} intermediateBlockRootsLeafCount
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             */
                            BlockStreamInfo.prototype.intermediateBlockRootsLeafCount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * Creates a new BlockStreamInfo instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @static
                             * @param {com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.blockstream.BlockStreamInfo} BlockStreamInfo instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Shape): com.hedera.hapi.node.state.blockstream.BlockStreamInfo & com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Properties): com.hedera.hapi.node.state.blockstream.BlockStreamInfo;
                             * }}
                             */
                            BlockStreamInfo.create = function(properties) {
                                return new BlockStreamInfo(properties);
                            };

                            /**
                             * Encodes the specified BlockStreamInfo message. Does not implicitly {@link com.hedera.hapi.node.state.blockstream.BlockStreamInfo.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @static
                             * @param {com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Properties} message BlockStreamInfo message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            BlockStreamInfo.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.blockNumber != null && $Object.hasOwnProperty.call(message, "blockNumber") && (typeof message.blockNumber === "object" ? message.blockNumber.low || message.blockNumber.high : message.blockNumber !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.blockNumber);
                                if (message.blockTime != null && $Object.hasOwnProperty.call(message, "blockTime"))
                                    $root.proto.Timestamp.encode(message.blockTime, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.trailingOutputHashes != null && $Object.hasOwnProperty.call(message, "trailingOutputHashes") && message.trailingOutputHashes.length)
                                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.trailingOutputHashes);
                                if (message.trailingBlockHashes != null && $Object.hasOwnProperty.call(message, "trailingBlockHashes") && message.trailingBlockHashes.length)
                                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.trailingBlockHashes);
                                if (message.inputTreeRootHash != null && $Object.hasOwnProperty.call(message, "inputTreeRootHash") && message.inputTreeRootHash.length)
                                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.inputTreeRootHash);
                                if (message.startOfBlockStateHash != null && $Object.hasOwnProperty.call(message, "startOfBlockStateHash") && message.startOfBlockStateHash.length)
                                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.startOfBlockStateHash);
                                if (message.numPrecedingStateChangesItems != null && $Object.hasOwnProperty.call(message, "numPrecedingStateChangesItems") && (typeof message.numPrecedingStateChangesItems === "object" ? message.numPrecedingStateChangesItems.low || message.numPrecedingStateChangesItems.high : message.numPrecedingStateChangesItems !== 0))
                                    writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.numPrecedingStateChangesItems);
                                if (message.rightmostPrecedingStateChangesTreeHashes != null && message.rightmostPrecedingStateChangesTreeHashes.length)
                                    for (let i = 0; i < message.rightmostPrecedingStateChangesTreeHashes.length; ++i)
                                        writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.rightmostPrecedingStateChangesTreeHashes[i]);
                                if (message.blockEndTime != null && $Object.hasOwnProperty.call(message, "blockEndTime"))
                                    $root.proto.Timestamp.encode(message.blockEndTime, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
                                if (message.postUpgradeWorkDone != null && $Object.hasOwnProperty.call(message, "postUpgradeWorkDone") && message.postUpgradeWorkDone !== false)
                                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.postUpgradeWorkDone);
                                if (message.creationSoftwareVersion != null && $Object.hasOwnProperty.call(message, "creationSoftwareVersion"))
                                    $root.proto.SemanticVersion.encode(message.creationSoftwareVersion, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                                if (message.lastIntervalProcessTime != null && $Object.hasOwnProperty.call(message, "lastIntervalProcessTime"))
                                    $root.proto.Timestamp.encode(message.lastIntervalProcessTime, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
                                if (message.lastHandleTime != null && $Object.hasOwnProperty.call(message, "lastHandleTime"))
                                    $root.proto.Timestamp.encode(message.lastHandleTime, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
                                if (message.consensusHeaderRootHash != null && $Object.hasOwnProperty.call(message, "consensusHeaderRootHash") && message.consensusHeaderRootHash.length)
                                    writer.uint32(/* id 14, wireType 2 =*/114).bytes(message.consensusHeaderRootHash);
                                if (message.outputItemRootHash != null && $Object.hasOwnProperty.call(message, "outputItemRootHash") && message.outputItemRootHash.length)
                                    writer.uint32(/* id 15, wireType 2 =*/122).bytes(message.outputItemRootHash);
                                if (message.traceDataRootHash != null && $Object.hasOwnProperty.call(message, "traceDataRootHash") && message.traceDataRootHash.length)
                                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.traceDataRootHash);
                                if (message.intermediatePreviousBlockRootHashes != null && message.intermediatePreviousBlockRootHashes.length)
                                    for (let i = 0; i < message.intermediatePreviousBlockRootHashes.length; ++i)
                                        writer.uint32(/* id 17, wireType 2 =*/138).bytes(message.intermediatePreviousBlockRootHashes[i]);
                                if (message.intermediateBlockRootsLeafCount != null && $Object.hasOwnProperty.call(message, "intermediateBlockRootsLeafCount") && (typeof message.intermediateBlockRootsLeafCount === "object" ? message.intermediateBlockRootsLeafCount.low || message.intermediateBlockRootsLeafCount.high : message.intermediateBlockRootsLeafCount !== 0))
                                    writer.uint32(/* id 18, wireType 0 =*/144).uint64(message.intermediateBlockRootsLeafCount);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified BlockStreamInfo message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.blockstream.BlockStreamInfo.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @static
                             * @param {com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Properties} message BlockStreamInfo message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            BlockStreamInfo.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a BlockStreamInfo message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.blockstream.BlockStreamInfo & com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Shape} BlockStreamInfo
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            BlockStreamInfo.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.blockstream.BlockStreamInfo();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.blockNumber = value;
                                            else
                                                delete message.blockNumber;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.blockTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.blockTime);
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.trailingOutputHashes = value;
                                            else
                                                delete message.trailingOutputHashes;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.trailingBlockHashes = value;
                                            else
                                                delete message.trailingBlockHashes;
                                            continue;
                                        }
                                    case 5: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.inputTreeRootHash = value;
                                            else
                                                delete message.inputTreeRootHash;
                                            continue;
                                        }
                                    case 6: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.startOfBlockStateHash = value;
                                            else
                                                delete message.startOfBlockStateHash;
                                            continue;
                                        }
                                    case 7: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numPrecedingStateChangesItems = value;
                                            else
                                                delete message.numPrecedingStateChangesItems;
                                            continue;
                                        }
                                    case 8: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.rightmostPrecedingStateChangesTreeHashes && message.rightmostPrecedingStateChangesTreeHashes.length))
                                                message.rightmostPrecedingStateChangesTreeHashes = [];
                                            message.rightmostPrecedingStateChangesTreeHashes.push(reader.bytes());
                                            continue;
                                        }
                                    case 9: {
                                            if (wireType !== 2)
                                                break;
                                            message.blockEndTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.blockEndTime);
                                            continue;
                                        }
                                    case 10: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.bool())
                                                message.postUpgradeWorkDone = value;
                                            else
                                                delete message.postUpgradeWorkDone;
                                            continue;
                                        }
                                    case 11: {
                                            if (wireType !== 2)
                                                break;
                                            message.creationSoftwareVersion = $root.proto.SemanticVersion.decode(reader, reader.uint32(), $undefined, _depth + 1, message.creationSoftwareVersion);
                                            continue;
                                        }
                                    case 12: {
                                            if (wireType !== 2)
                                                break;
                                            message.lastIntervalProcessTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.lastIntervalProcessTime);
                                            continue;
                                        }
                                    case 13: {
                                            if (wireType !== 2)
                                                break;
                                            message.lastHandleTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.lastHandleTime);
                                            continue;
                                        }
                                    case 14: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.consensusHeaderRootHash = value;
                                            else
                                                delete message.consensusHeaderRootHash;
                                            continue;
                                        }
                                    case 15: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.outputItemRootHash = value;
                                            else
                                                delete message.outputItemRootHash;
                                            continue;
                                        }
                                    case 16: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.traceDataRootHash = value;
                                            else
                                                delete message.traceDataRootHash;
                                            continue;
                                        }
                                    case 17: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.intermediatePreviousBlockRootHashes && message.intermediatePreviousBlockRootHashes.length))
                                                message.intermediatePreviousBlockRootHashes = [];
                                            message.intermediatePreviousBlockRootHashes.push(reader.bytes());
                                            continue;
                                        }
                                    case 18: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.intermediateBlockRootsLeafCount = value;
                                            else
                                                delete message.intermediateBlockRootsLeafCount;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a BlockStreamInfo message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.blockstream.BlockStreamInfo & com.hedera.hapi.node.state.blockstream.BlockStreamInfo.$Shape} BlockStreamInfo
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            BlockStreamInfo.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a BlockStreamInfo message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            BlockStreamInfo.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.blockNumber != null && $Object.hasOwnProperty.call(message, "blockNumber"))
                                    if (!$util.isInteger(message.blockNumber) && !(message.blockNumber && $util.isInteger(message.blockNumber.low) && $util.isInteger(message.blockNumber.high)))
                                        return "blockNumber: integer|Long expected";
                                if (message.blockTime != null && $Object.hasOwnProperty.call(message, "blockTime")) {
                                    let error = $root.proto.Timestamp.verify(message.blockTime, _depth + 1);
                                    if (error)
                                        return "blockTime." + error;
                                }
                                if (message.trailingOutputHashes != null && $Object.hasOwnProperty.call(message, "trailingOutputHashes"))
                                    if (!(message.trailingOutputHashes && typeof message.trailingOutputHashes.length === "number" || $util.isString(message.trailingOutputHashes)))
                                        return "trailingOutputHashes: buffer expected";
                                if (message.trailingBlockHashes != null && $Object.hasOwnProperty.call(message, "trailingBlockHashes"))
                                    if (!(message.trailingBlockHashes && typeof message.trailingBlockHashes.length === "number" || $util.isString(message.trailingBlockHashes)))
                                        return "trailingBlockHashes: buffer expected";
                                if (message.inputTreeRootHash != null && $Object.hasOwnProperty.call(message, "inputTreeRootHash"))
                                    if (!(message.inputTreeRootHash && typeof message.inputTreeRootHash.length === "number" || $util.isString(message.inputTreeRootHash)))
                                        return "inputTreeRootHash: buffer expected";
                                if (message.startOfBlockStateHash != null && $Object.hasOwnProperty.call(message, "startOfBlockStateHash"))
                                    if (!(message.startOfBlockStateHash && typeof message.startOfBlockStateHash.length === "number" || $util.isString(message.startOfBlockStateHash)))
                                        return "startOfBlockStateHash: buffer expected";
                                if (message.numPrecedingStateChangesItems != null && $Object.hasOwnProperty.call(message, "numPrecedingStateChangesItems"))
                                    if (!$util.isInteger(message.numPrecedingStateChangesItems) && !(message.numPrecedingStateChangesItems && $util.isInteger(message.numPrecedingStateChangesItems.low) && $util.isInteger(message.numPrecedingStateChangesItems.high)))
                                        return "numPrecedingStateChangesItems: integer|Long expected";
                                if (message.rightmostPrecedingStateChangesTreeHashes != null && $Object.hasOwnProperty.call(message, "rightmostPrecedingStateChangesTreeHashes")) {
                                    if (!$Array.isArray(message.rightmostPrecedingStateChangesTreeHashes))
                                        return "rightmostPrecedingStateChangesTreeHashes: array expected";
                                    for (let i = 0; i < message.rightmostPrecedingStateChangesTreeHashes.length; ++i)
                                        if (!(message.rightmostPrecedingStateChangesTreeHashes[i] && typeof message.rightmostPrecedingStateChangesTreeHashes[i].length === "number" || $util.isString(message.rightmostPrecedingStateChangesTreeHashes[i])))
                                            return "rightmostPrecedingStateChangesTreeHashes: buffer[] expected";
                                }
                                if (message.blockEndTime != null && $Object.hasOwnProperty.call(message, "blockEndTime")) {
                                    let error = $root.proto.Timestamp.verify(message.blockEndTime, _depth + 1);
                                    if (error)
                                        return "blockEndTime." + error;
                                }
                                if (message.postUpgradeWorkDone != null && $Object.hasOwnProperty.call(message, "postUpgradeWorkDone"))
                                    if (typeof message.postUpgradeWorkDone !== "boolean")
                                        return "postUpgradeWorkDone: boolean expected";
                                if (message.creationSoftwareVersion != null && $Object.hasOwnProperty.call(message, "creationSoftwareVersion")) {
                                    let error = $root.proto.SemanticVersion.verify(message.creationSoftwareVersion, _depth + 1);
                                    if (error)
                                        return "creationSoftwareVersion." + error;
                                }
                                if (message.lastIntervalProcessTime != null && $Object.hasOwnProperty.call(message, "lastIntervalProcessTime")) {
                                    let error = $root.proto.Timestamp.verify(message.lastIntervalProcessTime, _depth + 1);
                                    if (error)
                                        return "lastIntervalProcessTime." + error;
                                }
                                if (message.lastHandleTime != null && $Object.hasOwnProperty.call(message, "lastHandleTime")) {
                                    let error = $root.proto.Timestamp.verify(message.lastHandleTime, _depth + 1);
                                    if (error)
                                        return "lastHandleTime." + error;
                                }
                                if (message.consensusHeaderRootHash != null && $Object.hasOwnProperty.call(message, "consensusHeaderRootHash"))
                                    if (!(message.consensusHeaderRootHash && typeof message.consensusHeaderRootHash.length === "number" || $util.isString(message.consensusHeaderRootHash)))
                                        return "consensusHeaderRootHash: buffer expected";
                                if (message.outputItemRootHash != null && $Object.hasOwnProperty.call(message, "outputItemRootHash"))
                                    if (!(message.outputItemRootHash && typeof message.outputItemRootHash.length === "number" || $util.isString(message.outputItemRootHash)))
                                        return "outputItemRootHash: buffer expected";
                                if (message.traceDataRootHash != null && $Object.hasOwnProperty.call(message, "traceDataRootHash"))
                                    if (!(message.traceDataRootHash && typeof message.traceDataRootHash.length === "number" || $util.isString(message.traceDataRootHash)))
                                        return "traceDataRootHash: buffer expected";
                                if (message.intermediatePreviousBlockRootHashes != null && $Object.hasOwnProperty.call(message, "intermediatePreviousBlockRootHashes")) {
                                    if (!$Array.isArray(message.intermediatePreviousBlockRootHashes))
                                        return "intermediatePreviousBlockRootHashes: array expected";
                                    for (let i = 0; i < message.intermediatePreviousBlockRootHashes.length; ++i)
                                        if (!(message.intermediatePreviousBlockRootHashes[i] && typeof message.intermediatePreviousBlockRootHashes[i].length === "number" || $util.isString(message.intermediatePreviousBlockRootHashes[i])))
                                            return "intermediatePreviousBlockRootHashes: buffer[] expected";
                                }
                                if (message.intermediateBlockRootsLeafCount != null && $Object.hasOwnProperty.call(message, "intermediateBlockRootsLeafCount"))
                                    if (!$util.isInteger(message.intermediateBlockRootsLeafCount) && !(message.intermediateBlockRootsLeafCount && $util.isInteger(message.intermediateBlockRootsLeafCount.low) && $util.isInteger(message.intermediateBlockRootsLeafCount.high)))
                                        return "intermediateBlockRootsLeafCount: integer|Long expected";
                                return null;
                            };

                            /**
                             * Creates a BlockStreamInfo message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.blockstream.BlockStreamInfo} BlockStreamInfo
                             */
                            BlockStreamInfo.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.blockstream.BlockStreamInfo)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.blockstream.BlockStreamInfo: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.blockstream.BlockStreamInfo();
                                if (object.blockNumber != null)
                                    if (typeof object.blockNumber === "object" ? object.blockNumber.low || object.blockNumber.high : $Number(object.blockNumber) !== 0)
                                        if ($util.Long)
                                            message.blockNumber = $util.Long.fromValue(object.blockNumber, true);
                                        else if (typeof object.blockNumber === "string")
                                            message.blockNumber = $parseInt(object.blockNumber, 10);
                                        else if (typeof object.blockNumber === "number")
                                            message.blockNumber = object.blockNumber;
                                        else if (typeof object.blockNumber === "object")
                                            message.blockNumber = new $util.LongBits(object.blockNumber.low >>> 0, object.blockNumber.high >>> 0).toNumber(true);
                                if (object.blockTime != null) {
                                    if (!$util.isObject(object.blockTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.blockstream.BlockStreamInfo.blockTime: object expected");
                                    message.blockTime = $root.proto.Timestamp.fromObject(object.blockTime, _depth + 1);
                                }
                                if (object.trailingOutputHashes != null)
                                    if (object.trailingOutputHashes.length)
                                        if (typeof object.trailingOutputHashes === "string")
                                            $util.base64.decode(object.trailingOutputHashes, message.trailingOutputHashes = $util.newBuffer($util.base64.length(object.trailingOutputHashes)), 0);
                                        else if (object.trailingOutputHashes.length >= 0)
                                            message.trailingOutputHashes = object.trailingOutputHashes;
                                if (object.trailingBlockHashes != null)
                                    if (object.trailingBlockHashes.length)
                                        if (typeof object.trailingBlockHashes === "string")
                                            $util.base64.decode(object.trailingBlockHashes, message.trailingBlockHashes = $util.newBuffer($util.base64.length(object.trailingBlockHashes)), 0);
                                        else if (object.trailingBlockHashes.length >= 0)
                                            message.trailingBlockHashes = object.trailingBlockHashes;
                                if (object.inputTreeRootHash != null)
                                    if (object.inputTreeRootHash.length)
                                        if (typeof object.inputTreeRootHash === "string")
                                            $util.base64.decode(object.inputTreeRootHash, message.inputTreeRootHash = $util.newBuffer($util.base64.length(object.inputTreeRootHash)), 0);
                                        else if (object.inputTreeRootHash.length >= 0)
                                            message.inputTreeRootHash = object.inputTreeRootHash;
                                if (object.startOfBlockStateHash != null)
                                    if (object.startOfBlockStateHash.length)
                                        if (typeof object.startOfBlockStateHash === "string")
                                            $util.base64.decode(object.startOfBlockStateHash, message.startOfBlockStateHash = $util.newBuffer($util.base64.length(object.startOfBlockStateHash)), 0);
                                        else if (object.startOfBlockStateHash.length >= 0)
                                            message.startOfBlockStateHash = object.startOfBlockStateHash;
                                if (object.numPrecedingStateChangesItems != null)
                                    if (typeof object.numPrecedingStateChangesItems === "object" ? object.numPrecedingStateChangesItems.low || object.numPrecedingStateChangesItems.high : $Number(object.numPrecedingStateChangesItems) !== 0)
                                        if ($util.Long)
                                            message.numPrecedingStateChangesItems = $util.Long.fromValue(object.numPrecedingStateChangesItems, true);
                                        else if (typeof object.numPrecedingStateChangesItems === "string")
                                            message.numPrecedingStateChangesItems = $parseInt(object.numPrecedingStateChangesItems, 10);
                                        else if (typeof object.numPrecedingStateChangesItems === "number")
                                            message.numPrecedingStateChangesItems = object.numPrecedingStateChangesItems;
                                        else if (typeof object.numPrecedingStateChangesItems === "object")
                                            message.numPrecedingStateChangesItems = new $util.LongBits(object.numPrecedingStateChangesItems.low >>> 0, object.numPrecedingStateChangesItems.high >>> 0).toNumber(true);
                                if (object.rightmostPrecedingStateChangesTreeHashes) {
                                    if (!$Array.isArray(object.rightmostPrecedingStateChangesTreeHashes))
                                        throw $TypeError(".com.hedera.hapi.node.state.blockstream.BlockStreamInfo.rightmostPrecedingStateChangesTreeHashes: array expected");
                                    message.rightmostPrecedingStateChangesTreeHashes = $Array(object.rightmostPrecedingStateChangesTreeHashes.length);
                                    for (let i = 0; i < object.rightmostPrecedingStateChangesTreeHashes.length; ++i)
                                        if (typeof object.rightmostPrecedingStateChangesTreeHashes[i] === "string")
                                            $util.base64.decode(object.rightmostPrecedingStateChangesTreeHashes[i], message.rightmostPrecedingStateChangesTreeHashes[i] = $util.newBuffer($util.base64.length(object.rightmostPrecedingStateChangesTreeHashes[i])), 0);
                                        else if (object.rightmostPrecedingStateChangesTreeHashes[i].length >= 0)
                                            message.rightmostPrecedingStateChangesTreeHashes[i] = object.rightmostPrecedingStateChangesTreeHashes[i];
                                }
                                if (object.blockEndTime != null) {
                                    if (!$util.isObject(object.blockEndTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.blockstream.BlockStreamInfo.blockEndTime: object expected");
                                    message.blockEndTime = $root.proto.Timestamp.fromObject(object.blockEndTime, _depth + 1);
                                }
                                if (object.postUpgradeWorkDone != null)
                                    if (object.postUpgradeWorkDone)
                                        message.postUpgradeWorkDone = $Boolean(object.postUpgradeWorkDone);
                                if (object.creationSoftwareVersion != null) {
                                    if (!$util.isObject(object.creationSoftwareVersion))
                                        throw $TypeError(".com.hedera.hapi.node.state.blockstream.BlockStreamInfo.creationSoftwareVersion: object expected");
                                    message.creationSoftwareVersion = $root.proto.SemanticVersion.fromObject(object.creationSoftwareVersion, _depth + 1);
                                }
                                if (object.lastIntervalProcessTime != null) {
                                    if (!$util.isObject(object.lastIntervalProcessTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.blockstream.BlockStreamInfo.lastIntervalProcessTime: object expected");
                                    message.lastIntervalProcessTime = $root.proto.Timestamp.fromObject(object.lastIntervalProcessTime, _depth + 1);
                                }
                                if (object.lastHandleTime != null) {
                                    if (!$util.isObject(object.lastHandleTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.blockstream.BlockStreamInfo.lastHandleTime: object expected");
                                    message.lastHandleTime = $root.proto.Timestamp.fromObject(object.lastHandleTime, _depth + 1);
                                }
                                if (object.consensusHeaderRootHash != null)
                                    if (object.consensusHeaderRootHash.length)
                                        if (typeof object.consensusHeaderRootHash === "string")
                                            $util.base64.decode(object.consensusHeaderRootHash, message.consensusHeaderRootHash = $util.newBuffer($util.base64.length(object.consensusHeaderRootHash)), 0);
                                        else if (object.consensusHeaderRootHash.length >= 0)
                                            message.consensusHeaderRootHash = object.consensusHeaderRootHash;
                                if (object.outputItemRootHash != null)
                                    if (object.outputItemRootHash.length)
                                        if (typeof object.outputItemRootHash === "string")
                                            $util.base64.decode(object.outputItemRootHash, message.outputItemRootHash = $util.newBuffer($util.base64.length(object.outputItemRootHash)), 0);
                                        else if (object.outputItemRootHash.length >= 0)
                                            message.outputItemRootHash = object.outputItemRootHash;
                                if (object.traceDataRootHash != null)
                                    if (object.traceDataRootHash.length)
                                        if (typeof object.traceDataRootHash === "string")
                                            $util.base64.decode(object.traceDataRootHash, message.traceDataRootHash = $util.newBuffer($util.base64.length(object.traceDataRootHash)), 0);
                                        else if (object.traceDataRootHash.length >= 0)
                                            message.traceDataRootHash = object.traceDataRootHash;
                                if (object.intermediatePreviousBlockRootHashes) {
                                    if (!$Array.isArray(object.intermediatePreviousBlockRootHashes))
                                        throw $TypeError(".com.hedera.hapi.node.state.blockstream.BlockStreamInfo.intermediatePreviousBlockRootHashes: array expected");
                                    message.intermediatePreviousBlockRootHashes = $Array(object.intermediatePreviousBlockRootHashes.length);
                                    for (let i = 0; i < object.intermediatePreviousBlockRootHashes.length; ++i)
                                        if (typeof object.intermediatePreviousBlockRootHashes[i] === "string")
                                            $util.base64.decode(object.intermediatePreviousBlockRootHashes[i], message.intermediatePreviousBlockRootHashes[i] = $util.newBuffer($util.base64.length(object.intermediatePreviousBlockRootHashes[i])), 0);
                                        else if (object.intermediatePreviousBlockRootHashes[i].length >= 0)
                                            message.intermediatePreviousBlockRootHashes[i] = object.intermediatePreviousBlockRootHashes[i];
                                }
                                if (object.intermediateBlockRootsLeafCount != null)
                                    if (typeof object.intermediateBlockRootsLeafCount === "object" ? object.intermediateBlockRootsLeafCount.low || object.intermediateBlockRootsLeafCount.high : $Number(object.intermediateBlockRootsLeafCount) !== 0)
                                        if ($util.Long)
                                            message.intermediateBlockRootsLeafCount = $util.Long.fromValue(object.intermediateBlockRootsLeafCount, true);
                                        else if (typeof object.intermediateBlockRootsLeafCount === "string")
                                            message.intermediateBlockRootsLeafCount = $parseInt(object.intermediateBlockRootsLeafCount, 10);
                                        else if (typeof object.intermediateBlockRootsLeafCount === "number")
                                            message.intermediateBlockRootsLeafCount = object.intermediateBlockRootsLeafCount;
                                        else if (typeof object.intermediateBlockRootsLeafCount === "object")
                                            message.intermediateBlockRootsLeafCount = new $util.LongBits(object.intermediateBlockRootsLeafCount.low >>> 0, object.intermediateBlockRootsLeafCount.high >>> 0).toNumber(true);
                                return message;
                            };

                            /**
                             * Creates a plain object from a BlockStreamInfo message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @static
                             * @param {com.hedera.hapi.node.state.blockstream.BlockStreamInfo} message BlockStreamInfo
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            BlockStreamInfo.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults) {
                                    object.rightmostPrecedingStateChangesTreeHashes = [];
                                    object.intermediatePreviousBlockRootHashes = [];
                                }
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.blockNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.blockNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    object.blockTime = null;
                                    if (options.bytes === $String)
                                        object.trailingOutputHashes = "";
                                    else {
                                        object.trailingOutputHashes = [];
                                        if (options.bytes !== $Array)
                                            object.trailingOutputHashes = $util.newBuffer(object.trailingOutputHashes);
                                    }
                                    if (options.bytes === $String)
                                        object.trailingBlockHashes = "";
                                    else {
                                        object.trailingBlockHashes = [];
                                        if (options.bytes !== $Array)
                                            object.trailingBlockHashes = $util.newBuffer(object.trailingBlockHashes);
                                    }
                                    if (options.bytes === $String)
                                        object.inputTreeRootHash = "";
                                    else {
                                        object.inputTreeRootHash = [];
                                        if (options.bytes !== $Array)
                                            object.inputTreeRootHash = $util.newBuffer(object.inputTreeRootHash);
                                    }
                                    if (options.bytes === $String)
                                        object.startOfBlockStateHash = "";
                                    else {
                                        object.startOfBlockStateHash = [];
                                        if (options.bytes !== $Array)
                                            object.startOfBlockStateHash = $util.newBuffer(object.startOfBlockStateHash);
                                    }
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numPrecedingStateChangesItems = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numPrecedingStateChangesItems = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    object.blockEndTime = null;
                                    object.postUpgradeWorkDone = false;
                                    object.creationSoftwareVersion = null;
                                    object.lastIntervalProcessTime = null;
                                    object.lastHandleTime = null;
                                    if (options.bytes === $String)
                                        object.consensusHeaderRootHash = "";
                                    else {
                                        object.consensusHeaderRootHash = [];
                                        if (options.bytes !== $Array)
                                            object.consensusHeaderRootHash = $util.newBuffer(object.consensusHeaderRootHash);
                                    }
                                    if (options.bytes === $String)
                                        object.outputItemRootHash = "";
                                    else {
                                        object.outputItemRootHash = [];
                                        if (options.bytes !== $Array)
                                            object.outputItemRootHash = $util.newBuffer(object.outputItemRootHash);
                                    }
                                    if (options.bytes === $String)
                                        object.traceDataRootHash = "";
                                    else {
                                        object.traceDataRootHash = [];
                                        if (options.bytes !== $Array)
                                            object.traceDataRootHash = $util.newBuffer(object.traceDataRootHash);
                                    }
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.intermediateBlockRootsLeafCount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.intermediateBlockRootsLeafCount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                }
                                if (message.blockNumber != null && $Object.hasOwnProperty.call(message, "blockNumber"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.blockNumber = typeof message.blockNumber === "number" ? $BigInt(message.blockNumber) : $util.Long.fromBits(message.blockNumber.low >>> 0, message.blockNumber.high >>> 0, true).toBigInt();
                                    else if (typeof message.blockNumber === "number")
                                        object.blockNumber = options.longs === $String ? $String(message.blockNumber) : message.blockNumber;
                                    else
                                        object.blockNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.blockNumber) : options.longs === $Number ? new $util.LongBits(message.blockNumber.low >>> 0, message.blockNumber.high >>> 0).toNumber(true) : message.blockNumber;
                                if (message.blockTime != null && $Object.hasOwnProperty.call(message, "blockTime"))
                                    object.blockTime = $root.proto.Timestamp.toObject(message.blockTime, options, _depth + 1);
                                if (message.trailingOutputHashes != null && $Object.hasOwnProperty.call(message, "trailingOutputHashes"))
                                    object.trailingOutputHashes = options.bytes === $String ? $util.base64.encode(message.trailingOutputHashes, 0, message.trailingOutputHashes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.trailingOutputHashes) : message.trailingOutputHashes;
                                if (message.trailingBlockHashes != null && $Object.hasOwnProperty.call(message, "trailingBlockHashes"))
                                    object.trailingBlockHashes = options.bytes === $String ? $util.base64.encode(message.trailingBlockHashes, 0, message.trailingBlockHashes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.trailingBlockHashes) : message.trailingBlockHashes;
                                if (message.inputTreeRootHash != null && $Object.hasOwnProperty.call(message, "inputTreeRootHash"))
                                    object.inputTreeRootHash = options.bytes === $String ? $util.base64.encode(message.inputTreeRootHash, 0, message.inputTreeRootHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.inputTreeRootHash) : message.inputTreeRootHash;
                                if (message.startOfBlockStateHash != null && $Object.hasOwnProperty.call(message, "startOfBlockStateHash"))
                                    object.startOfBlockStateHash = options.bytes === $String ? $util.base64.encode(message.startOfBlockStateHash, 0, message.startOfBlockStateHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.startOfBlockStateHash) : message.startOfBlockStateHash;
                                if (message.numPrecedingStateChangesItems != null && $Object.hasOwnProperty.call(message, "numPrecedingStateChangesItems"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numPrecedingStateChangesItems = typeof message.numPrecedingStateChangesItems === "number" ? $BigInt(message.numPrecedingStateChangesItems) : $util.Long.fromBits(message.numPrecedingStateChangesItems.low >>> 0, message.numPrecedingStateChangesItems.high >>> 0, true).toBigInt();
                                    else if (typeof message.numPrecedingStateChangesItems === "number")
                                        object.numPrecedingStateChangesItems = options.longs === $String ? $String(message.numPrecedingStateChangesItems) : message.numPrecedingStateChangesItems;
                                    else
                                        object.numPrecedingStateChangesItems = options.longs === $String ? $util.Long.prototype.toString.call(message.numPrecedingStateChangesItems) : options.longs === $Number ? new $util.LongBits(message.numPrecedingStateChangesItems.low >>> 0, message.numPrecedingStateChangesItems.high >>> 0).toNumber(true) : message.numPrecedingStateChangesItems;
                                if (message.rightmostPrecedingStateChangesTreeHashes && message.rightmostPrecedingStateChangesTreeHashes.length) {
                                    object.rightmostPrecedingStateChangesTreeHashes = $Array(message.rightmostPrecedingStateChangesTreeHashes.length);
                                    for (let j = 0; j < message.rightmostPrecedingStateChangesTreeHashes.length; ++j)
                                        object.rightmostPrecedingStateChangesTreeHashes[j] = options.bytes === $String ? $util.base64.encode(message.rightmostPrecedingStateChangesTreeHashes[j], 0, message.rightmostPrecedingStateChangesTreeHashes[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.rightmostPrecedingStateChangesTreeHashes[j]) : message.rightmostPrecedingStateChangesTreeHashes[j];
                                }
                                if (message.blockEndTime != null && $Object.hasOwnProperty.call(message, "blockEndTime"))
                                    object.blockEndTime = $root.proto.Timestamp.toObject(message.blockEndTime, options, _depth + 1);
                                if (message.postUpgradeWorkDone != null && $Object.hasOwnProperty.call(message, "postUpgradeWorkDone"))
                                    object.postUpgradeWorkDone = message.postUpgradeWorkDone;
                                if (message.creationSoftwareVersion != null && $Object.hasOwnProperty.call(message, "creationSoftwareVersion"))
                                    object.creationSoftwareVersion = $root.proto.SemanticVersion.toObject(message.creationSoftwareVersion, options, _depth + 1);
                                if (message.lastIntervalProcessTime != null && $Object.hasOwnProperty.call(message, "lastIntervalProcessTime"))
                                    object.lastIntervalProcessTime = $root.proto.Timestamp.toObject(message.lastIntervalProcessTime, options, _depth + 1);
                                if (message.lastHandleTime != null && $Object.hasOwnProperty.call(message, "lastHandleTime"))
                                    object.lastHandleTime = $root.proto.Timestamp.toObject(message.lastHandleTime, options, _depth + 1);
                                if (message.consensusHeaderRootHash != null && $Object.hasOwnProperty.call(message, "consensusHeaderRootHash"))
                                    object.consensusHeaderRootHash = options.bytes === $String ? $util.base64.encode(message.consensusHeaderRootHash, 0, message.consensusHeaderRootHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.consensusHeaderRootHash) : message.consensusHeaderRootHash;
                                if (message.outputItemRootHash != null && $Object.hasOwnProperty.call(message, "outputItemRootHash"))
                                    object.outputItemRootHash = options.bytes === $String ? $util.base64.encode(message.outputItemRootHash, 0, message.outputItemRootHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.outputItemRootHash) : message.outputItemRootHash;
                                if (message.traceDataRootHash != null && $Object.hasOwnProperty.call(message, "traceDataRootHash"))
                                    object.traceDataRootHash = options.bytes === $String ? $util.base64.encode(message.traceDataRootHash, 0, message.traceDataRootHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.traceDataRootHash) : message.traceDataRootHash;
                                if (message.intermediatePreviousBlockRootHashes && message.intermediatePreviousBlockRootHashes.length) {
                                    object.intermediatePreviousBlockRootHashes = $Array(message.intermediatePreviousBlockRootHashes.length);
                                    for (let j = 0; j < message.intermediatePreviousBlockRootHashes.length; ++j)
                                        object.intermediatePreviousBlockRootHashes[j] = options.bytes === $String ? $util.base64.encode(message.intermediatePreviousBlockRootHashes[j], 0, message.intermediatePreviousBlockRootHashes[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.intermediatePreviousBlockRootHashes[j]) : message.intermediatePreviousBlockRootHashes[j];
                                }
                                if (message.intermediateBlockRootsLeafCount != null && $Object.hasOwnProperty.call(message, "intermediateBlockRootsLeafCount"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.intermediateBlockRootsLeafCount = typeof message.intermediateBlockRootsLeafCount === "number" ? $BigInt(message.intermediateBlockRootsLeafCount) : $util.Long.fromBits(message.intermediateBlockRootsLeafCount.low >>> 0, message.intermediateBlockRootsLeafCount.high >>> 0, true).toBigInt();
                                    else if (typeof message.intermediateBlockRootsLeafCount === "number")
                                        object.intermediateBlockRootsLeafCount = options.longs === $String ? $String(message.intermediateBlockRootsLeafCount) : message.intermediateBlockRootsLeafCount;
                                    else
                                        object.intermediateBlockRootsLeafCount = options.longs === $String ? $util.Long.prototype.toString.call(message.intermediateBlockRootsLeafCount) : options.longs === $Number ? new $util.LongBits(message.intermediateBlockRootsLeafCount.low >>> 0, message.intermediateBlockRootsLeafCount.high >>> 0).toNumber(true) : message.intermediateBlockRootsLeafCount;
                                return object;
                            };

                            /**
                             * Converts this BlockStreamInfo to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            BlockStreamInfo.prototype.toJSON = function() {
                                return BlockStreamInfo.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for BlockStreamInfo
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.blockstream.BlockStreamInfo
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            BlockStreamInfo.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.blockstream.BlockStreamInfo";
                            };

                            return BlockStreamInfo;
                        })();

                        return blockstream;
                    })();

                    state.hooks = (function() {

                        /**
                         * Namespace hooks.
                         * @memberof com.hedera.hapi.node.state
                         * @namespace
                         */
                        const hooks = {};

                        hooks.EvmHookState = (function() {

                            /**
                             * Properties of an EvmHookState.
                             * @typedef {Object} com.hedera.hapi.node.state.hooks.EvmHookState.$Properties
                             * @property {proto.HookId.$Properties|null} [hookId] For state proofs, the id of this hook.
                             * @property {com.hedera.hapi.node.state.hooks.HookType|null} [type] The type of the EVM hook.
                             * @property {com.hedera.hapi.node.hooks.HookExtensionPoint|null} [extensionPoint] The type of the extension point the hook implements.
                             * @property {proto.ContractID.$Properties|null} [hookContractId] The id of the contract with this hook's bytecode.
                             * @property {proto.Key.$Properties|null} [adminKey] If set, a key that that can be used to remove or replace the hook; or (if
                             * applicable, as with an EVM hook) perform transactions that customize the hook.
                             * @property {Uint8Array|null} [firstContractStorageKey] For an EVM hook, its first storage key. Must be the minimal
                             * bytes representation (no leading zeros). An EVM hook with
                             * no storage slots and an EVM hook whose last-written storage
                             * slot was at the zero key can be differentiated by checking
                             * the `num_storage_slots` field.
                             * @property {google.protobuf.Int64Value.$Properties|null} [previousHookId] If set, the id of the hook preceding this one in the owner's
                             * doubly-linked list of hooks.
                             * @property {google.protobuf.Int64Value.$Properties|null} [nextHookId] If set, the id of the hook following this one in the owner's
                             * doubly-linked list of hooks.
                             * @property {number|null} [numStorageSlots] The number of storage slots an EVM hook is using.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of an EvmHookState.
                             * @memberof com.hedera.hapi.node.state.hooks
                             * @interface IEvmHookState
                             * @augments com.hedera.hapi.node.state.hooks.EvmHookState.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.hooks.EvmHookState.$Properties instead.
                             */

                            /**
                             * Shape of an EvmHookState.
                             * @typedef {{
                             *   hookId?: proto.HookId.$Shape|null;
                             *   type?: com.hedera.hapi.node.state.hooks.HookType|null;
                             *   extensionPoint?: com.hedera.hapi.node.hooks.HookExtensionPoint|null;
                             *   hookContractId?: proto.ContractID.$Shape|null;
                             *   adminKey?: proto.Key.$Shape|null;
                             *   firstContractStorageKey?: Uint8Array|null;
                             *   previousHookId?: google.protobuf.Int64Value.$Shape|null;
                             *   nextHookId?: google.protobuf.Int64Value.$Shape|null;
                             *   numStorageSlots?: number|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * }} com.hedera.hapi.node.state.hooks.EvmHookState.$Shape
                             */

                            /**
                             * Constructs a new EvmHookState.
                             * @memberof com.hedera.hapi.node.state.hooks
                             * @classdesc The representation of an EVM hook in state, including the previous and next hook ids in its owner's list.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.hooks.EvmHookState.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const EvmHookState = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * For state proofs, the id of this hook.
                             * @member {proto.HookId.$Properties|null|undefined} hookId
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @instance
                             */
                            EvmHookState.prototype.hookId = null;

                            /**
                             * The type of the EVM hook.
                             * @member {com.hedera.hapi.node.state.hooks.HookType} type
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @instance
                             */
                            EvmHookState.prototype.type = 0;

                            /**
                             * The type of the extension point the hook implements.
                             * @member {com.hedera.hapi.node.hooks.HookExtensionPoint} extensionPoint
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @instance
                             */
                            EvmHookState.prototype.extensionPoint = 0;

                            /**
                             * The id of the contract with this hook's bytecode.
                             * @member {proto.ContractID.$Properties|null|undefined} hookContractId
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @instance
                             */
                            EvmHookState.prototype.hookContractId = null;

                            /**
                             * If set, a key that that can be used to remove or replace the hook; or (if
                             * applicable, as with an EVM hook) perform transactions that customize the hook.
                             * @member {proto.Key.$Properties|null|undefined} adminKey
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @instance
                             */
                            EvmHookState.prototype.adminKey = null;

                            /**
                             * For an EVM hook, its first storage key. Must be the minimal
                             * bytes representation (no leading zeros). An EVM hook with
                             * no storage slots and an EVM hook whose last-written storage
                             * slot was at the zero key can be differentiated by checking
                             * the `num_storage_slots` field.
                             * @member {Uint8Array} firstContractStorageKey
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @instance
                             */
                            EvmHookState.prototype.firstContractStorageKey = $util.newBuffer([]);

                            /**
                             * If set, the id of the hook preceding this one in the owner's
                             * doubly-linked list of hooks.
                             * @member {google.protobuf.Int64Value.$Properties|null|undefined} previousHookId
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @instance
                             */
                            EvmHookState.prototype.previousHookId = null;

                            /**
                             * If set, the id of the hook following this one in the owner's
                             * doubly-linked list of hooks.
                             * @member {google.protobuf.Int64Value.$Properties|null|undefined} nextHookId
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @instance
                             */
                            EvmHookState.prototype.nextHookId = null;

                            /**
                             * The number of storage slots an EVM hook is using.
                             * @member {number} numStorageSlots
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @instance
                             */
                            EvmHookState.prototype.numStorageSlots = 0;

                            /**
                             * Creates a new EvmHookState instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @static
                             * @param {com.hedera.hapi.node.state.hooks.EvmHookState.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.hooks.EvmHookState} EvmHookState instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.hooks.EvmHookState.$Shape): com.hedera.hapi.node.state.hooks.EvmHookState & com.hedera.hapi.node.state.hooks.EvmHookState.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.hooks.EvmHookState.$Properties): com.hedera.hapi.node.state.hooks.EvmHookState;
                             * }}
                             */
                            EvmHookState.create = function(properties) {
                                return new EvmHookState(properties);
                            };

                            /**
                             * Encodes the specified EvmHookState message. Does not implicitly {@link com.hedera.hapi.node.state.hooks.EvmHookState.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @static
                             * @param {com.hedera.hapi.node.state.hooks.EvmHookState.$Properties} message EvmHookState message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            EvmHookState.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId"))
                                    $root.proto.HookId.encode(message.hookId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== 0)
                                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
                                if (message.extensionPoint != null && $Object.hasOwnProperty.call(message, "extensionPoint") && message.extensionPoint !== 0)
                                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.extensionPoint);
                                if (message.hookContractId != null && $Object.hasOwnProperty.call(message, "hookContractId"))
                                    $root.proto.ContractID.encode(message.hookContractId, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                                if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                    $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                                if (message.firstContractStorageKey != null && $Object.hasOwnProperty.call(message, "firstContractStorageKey") && message.firstContractStorageKey.length)
                                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.firstContractStorageKey);
                                if (message.previousHookId != null && $Object.hasOwnProperty.call(message, "previousHookId"))
                                    $root.google.protobuf.Int64Value.encode(message.previousHookId, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                                if (message.nextHookId != null && $Object.hasOwnProperty.call(message, "nextHookId"))
                                    $root.google.protobuf.Int64Value.encode(message.nextHookId, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
                                if (message.numStorageSlots != null && $Object.hasOwnProperty.call(message, "numStorageSlots") && message.numStorageSlots !== 0)
                                    writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.numStorageSlots);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified EvmHookState message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.hooks.EvmHookState.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @static
                             * @param {com.hedera.hapi.node.state.hooks.EvmHookState.$Properties} message EvmHookState message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            EvmHookState.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes an EvmHookState message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.hooks.EvmHookState & com.hedera.hapi.node.state.hooks.EvmHookState.$Shape} EvmHookState
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            EvmHookState.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.hooks.EvmHookState();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            message.hookId = $root.proto.HookId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hookId);
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.int32())
                                                message.type = value;
                                            else
                                                delete message.type;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.int32())
                                                message.extensionPoint = value;
                                            else
                                                delete message.extensionPoint;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            message.hookContractId = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hookContractId);
                                            continue;
                                        }
                                    case 5: {
                                            if (wireType !== 2)
                                                break;
                                            message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                                            continue;
                                        }
                                    case 6: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.firstContractStorageKey = value;
                                            else
                                                delete message.firstContractStorageKey;
                                            continue;
                                        }
                                    case 7: {
                                            if (wireType !== 2)
                                                break;
                                            message.previousHookId = $root.google.protobuf.Int64Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.previousHookId);
                                            continue;
                                        }
                                    case 8: {
                                            if (wireType !== 2)
                                                break;
                                            message.nextHookId = $root.google.protobuf.Int64Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nextHookId);
                                            continue;
                                        }
                                    case 9: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.uint32())
                                                message.numStorageSlots = value;
                                            else
                                                delete message.numStorageSlots;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes an EvmHookState message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.hooks.EvmHookState & com.hedera.hapi.node.state.hooks.EvmHookState.$Shape} EvmHookState
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            EvmHookState.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies an EvmHookState message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            EvmHookState.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId")) {
                                    let error = $root.proto.HookId.verify(message.hookId, _depth + 1);
                                    if (error)
                                        return "hookId." + error;
                                }
                                if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                                        return "type: enum value expected";
                                if (message.extensionPoint != null && $Object.hasOwnProperty.call(message, "extensionPoint"))
                                    if (typeof message.extensionPoint !== "number" || (message.extensionPoint | 0) !== message.extensionPoint)
                                        return "extensionPoint: enum value expected";
                                if (message.hookContractId != null && $Object.hasOwnProperty.call(message, "hookContractId")) {
                                    let error = $root.proto.ContractID.verify(message.hookContractId, _depth + 1);
                                    if (error)
                                        return "hookContractId." + error;
                                }
                                if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                                    let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                                    if (error)
                                        return "adminKey." + error;
                                }
                                if (message.firstContractStorageKey != null && $Object.hasOwnProperty.call(message, "firstContractStorageKey"))
                                    if (!(message.firstContractStorageKey && typeof message.firstContractStorageKey.length === "number" || $util.isString(message.firstContractStorageKey)))
                                        return "firstContractStorageKey: buffer expected";
                                if (message.previousHookId != null && $Object.hasOwnProperty.call(message, "previousHookId")) {
                                    let error = $root.google.protobuf.Int64Value.verify(message.previousHookId, _depth + 1);
                                    if (error)
                                        return "previousHookId." + error;
                                }
                                if (message.nextHookId != null && $Object.hasOwnProperty.call(message, "nextHookId")) {
                                    let error = $root.google.protobuf.Int64Value.verify(message.nextHookId, _depth + 1);
                                    if (error)
                                        return "nextHookId." + error;
                                }
                                if (message.numStorageSlots != null && $Object.hasOwnProperty.call(message, "numStorageSlots"))
                                    if (!$util.isInteger(message.numStorageSlots))
                                        return "numStorageSlots: integer expected";
                                return null;
                            };

                            /**
                             * Creates an EvmHookState message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.hooks.EvmHookState} EvmHookState
                             */
                            EvmHookState.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.hooks.EvmHookState)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.hooks.EvmHookState: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.hooks.EvmHookState();
                                if (object.hookId != null) {
                                    if (!$util.isObject(object.hookId))
                                        throw $TypeError(".com.hedera.hapi.node.state.hooks.EvmHookState.hookId: object expected");
                                    message.hookId = $root.proto.HookId.fromObject(object.hookId, _depth + 1);
                                }
                                if (object.type !== 0 && (typeof object.type !== "string" || $root.com.hedera.hapi.node.state.hooks.HookType[object.type] !== 0))
                                    switch (object.type) {
                                    case "EVM_HOOK":
                                    case 0:
                                        message.type = 0;
                                        break;
                                    default:
                                        if (typeof object.type === "number" && (object.type | 0) === object.type)
                                            message.type = object.type;
                                    }
                                if (object.extensionPoint !== 0 && (typeof object.extensionPoint !== "string" || $root.com.hedera.hapi.node.hooks.HookExtensionPoint[object.extensionPoint] !== 0))
                                    switch (object.extensionPoint) {
                                    case "ACCOUNT_ALLOWANCE_HOOK":
                                    case 0:
                                        message.extensionPoint = 0;
                                        break;
                                    default:
                                        if (typeof object.extensionPoint === "number" && (object.extensionPoint | 0) === object.extensionPoint)
                                            message.extensionPoint = object.extensionPoint;
                                    }
                                if (object.hookContractId != null) {
                                    if (!$util.isObject(object.hookContractId))
                                        throw $TypeError(".com.hedera.hapi.node.state.hooks.EvmHookState.hookContractId: object expected");
                                    message.hookContractId = $root.proto.ContractID.fromObject(object.hookContractId, _depth + 1);
                                }
                                if (object.adminKey != null) {
                                    if (!$util.isObject(object.adminKey))
                                        throw $TypeError(".com.hedera.hapi.node.state.hooks.EvmHookState.adminKey: object expected");
                                    message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
                                }
                                if (object.firstContractStorageKey != null)
                                    if (object.firstContractStorageKey.length)
                                        if (typeof object.firstContractStorageKey === "string")
                                            $util.base64.decode(object.firstContractStorageKey, message.firstContractStorageKey = $util.newBuffer($util.base64.length(object.firstContractStorageKey)), 0);
                                        else if (object.firstContractStorageKey.length >= 0)
                                            message.firstContractStorageKey = object.firstContractStorageKey;
                                if (object.previousHookId != null) {
                                    if (!$util.isObject(object.previousHookId))
                                        throw $TypeError(".com.hedera.hapi.node.state.hooks.EvmHookState.previousHookId: object expected");
                                    message.previousHookId = $root.google.protobuf.Int64Value.fromObject(object.previousHookId, _depth + 1);
                                }
                                if (object.nextHookId != null) {
                                    if (!$util.isObject(object.nextHookId))
                                        throw $TypeError(".com.hedera.hapi.node.state.hooks.EvmHookState.nextHookId: object expected");
                                    message.nextHookId = $root.google.protobuf.Int64Value.fromObject(object.nextHookId, _depth + 1);
                                }
                                if (object.numStorageSlots != null)
                                    if ($Number(object.numStorageSlots) !== 0)
                                        message.numStorageSlots = object.numStorageSlots >>> 0;
                                return message;
                            };

                            /**
                             * Creates a plain object from an EvmHookState message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @static
                             * @param {com.hedera.hapi.node.state.hooks.EvmHookState} message EvmHookState
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            EvmHookState.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    object.hookId = null;
                                    object.type = options.enums === $String ? "EVM_HOOK" : 0;
                                    object.extensionPoint = options.enums === $String ? "ACCOUNT_ALLOWANCE_HOOK" : 0;
                                    object.hookContractId = null;
                                    object.adminKey = null;
                                    if (options.bytes === $String)
                                        object.firstContractStorageKey = "";
                                    else {
                                        object.firstContractStorageKey = [];
                                        if (options.bytes !== $Array)
                                            object.firstContractStorageKey = $util.newBuffer(object.firstContractStorageKey);
                                    }
                                    object.previousHookId = null;
                                    object.nextHookId = null;
                                    object.numStorageSlots = 0;
                                }
                                if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId"))
                                    object.hookId = $root.proto.HookId.toObject(message.hookId, options, _depth + 1);
                                if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                                    object.type = options.enums === $String ? $root.com.hedera.hapi.node.state.hooks.HookType[message.type] === $undefined ? message.type : $root.com.hedera.hapi.node.state.hooks.HookType[message.type] : message.type;
                                if (message.extensionPoint != null && $Object.hasOwnProperty.call(message, "extensionPoint"))
                                    object.extensionPoint = options.enums === $String ? $root.com.hedera.hapi.node.hooks.HookExtensionPoint[message.extensionPoint] === $undefined ? message.extensionPoint : $root.com.hedera.hapi.node.hooks.HookExtensionPoint[message.extensionPoint] : message.extensionPoint;
                                if (message.hookContractId != null && $Object.hasOwnProperty.call(message, "hookContractId"))
                                    object.hookContractId = $root.proto.ContractID.toObject(message.hookContractId, options, _depth + 1);
                                if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                                    object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
                                if (message.firstContractStorageKey != null && $Object.hasOwnProperty.call(message, "firstContractStorageKey"))
                                    object.firstContractStorageKey = options.bytes === $String ? $util.base64.encode(message.firstContractStorageKey, 0, message.firstContractStorageKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.firstContractStorageKey) : message.firstContractStorageKey;
                                if (message.previousHookId != null && $Object.hasOwnProperty.call(message, "previousHookId"))
                                    object.previousHookId = $root.google.protobuf.Int64Value.toObject(message.previousHookId, options, _depth + 1);
                                if (message.nextHookId != null && $Object.hasOwnProperty.call(message, "nextHookId"))
                                    object.nextHookId = $root.google.protobuf.Int64Value.toObject(message.nextHookId, options, _depth + 1);
                                if (message.numStorageSlots != null && $Object.hasOwnProperty.call(message, "numStorageSlots"))
                                    object.numStorageSlots = message.numStorageSlots;
                                return object;
                            };

                            /**
                             * Converts this EvmHookState to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            EvmHookState.prototype.toJSON = function() {
                                return EvmHookState.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for EvmHookState
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookState
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            EvmHookState.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.hooks.EvmHookState";
                            };

                            return EvmHookState;
                        })();

                        /**
                         * The type of a hook.
                         * @name com.hedera.hapi.node.state.hooks.HookType
                         * @enum {number}
                         * @property {number} EVM_HOOK=0 A general-purpose EVM hook.
                         */
                        hooks.HookType = (function() {
                            const valuesById = $Object.create(null), values = $Object.create(valuesById);
                            values[valuesById[0] = "EVM_HOOK"] = 0;
                            return values;
                        })();

                        hooks.EvmHookSlotKey = (function() {

                            /**
                             * Properties of an EvmHookSlotKey.
                             * @typedef {Object} com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Properties
                             * @property {proto.HookId.$Properties|null} [hookId] The id of the EVM hook that owns this slot.
                             * @property {Uint8Array|null} [key] The EVM key of this slot. Must be the minimal bytes representation
                             * (no leading zeros).
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of an EvmHookSlotKey.
                             * @memberof com.hedera.hapi.node.state.hooks
                             * @interface IEvmHookSlotKey
                             * @augments com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Properties instead.
                             */

                            /**
                             * Shape of an EvmHookSlotKey.
                             * @typedef {{
                             *   hookId?: proto.HookId.$Shape|null;
                             *   key?: Uint8Array|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * }} com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Shape
                             */

                            /**
                             * Constructs a new EvmHookSlotKey.
                             * @memberof com.hedera.hapi.node.state.hooks
                             * @classdesc The key of an EVM hook's storage slot.
                             * 
                             * For each EVM hook, its storage is a mapping of 256-bit keys (or "words")
                             * to 256-bit values.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const EvmHookSlotKey = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The id of the EVM hook that owns this slot.
                             * @member {proto.HookId.$Properties|null|undefined} hookId
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookSlotKey
                             * @instance
                             */
                            EvmHookSlotKey.prototype.hookId = null;

                            /**
                             * The EVM key of this slot. Must be the minimal bytes representation
                             * (no leading zeros).
                             * @member {Uint8Array} key
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookSlotKey
                             * @instance
                             */
                            EvmHookSlotKey.prototype.key = $util.newBuffer([]);

                            /**
                             * Creates a new EvmHookSlotKey instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookSlotKey
                             * @static
                             * @param {com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.hooks.EvmHookSlotKey} EvmHookSlotKey instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Shape): com.hedera.hapi.node.state.hooks.EvmHookSlotKey & com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Properties): com.hedera.hapi.node.state.hooks.EvmHookSlotKey;
                             * }}
                             */
                            EvmHookSlotKey.create = function(properties) {
                                return new EvmHookSlotKey(properties);
                            };

                            /**
                             * Encodes the specified EvmHookSlotKey message. Does not implicitly {@link com.hedera.hapi.node.state.hooks.EvmHookSlotKey.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookSlotKey
                             * @static
                             * @param {com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Properties} message EvmHookSlotKey message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            EvmHookSlotKey.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId"))
                                    $root.proto.HookId.encode(message.hookId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.key);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified EvmHookSlotKey message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.hooks.EvmHookSlotKey.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookSlotKey
                             * @static
                             * @param {com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Properties} message EvmHookSlotKey message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            EvmHookSlotKey.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes an EvmHookSlotKey message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookSlotKey
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.hooks.EvmHookSlotKey & com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Shape} EvmHookSlotKey
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            EvmHookSlotKey.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.hooks.EvmHookSlotKey();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            message.hookId = $root.proto.HookId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hookId);
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.key = value;
                                            else
                                                delete message.key;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes an EvmHookSlotKey message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookSlotKey
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.hooks.EvmHookSlotKey & com.hedera.hapi.node.state.hooks.EvmHookSlotKey.$Shape} EvmHookSlotKey
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            EvmHookSlotKey.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies an EvmHookSlotKey message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookSlotKey
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            EvmHookSlotKey.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId")) {
                                    let error = $root.proto.HookId.verify(message.hookId, _depth + 1);
                                    if (error)
                                        return "hookId." + error;
                                }
                                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                    if (!(message.key && typeof message.key.length === "number" || $util.isString(message.key)))
                                        return "key: buffer expected";
                                return null;
                            };

                            /**
                             * Creates an EvmHookSlotKey message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookSlotKey
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.hooks.EvmHookSlotKey} EvmHookSlotKey
                             */
                            EvmHookSlotKey.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.hooks.EvmHookSlotKey)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.hooks.EvmHookSlotKey: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.hooks.EvmHookSlotKey();
                                if (object.hookId != null) {
                                    if (!$util.isObject(object.hookId))
                                        throw $TypeError(".com.hedera.hapi.node.state.hooks.EvmHookSlotKey.hookId: object expected");
                                    message.hookId = $root.proto.HookId.fromObject(object.hookId, _depth + 1);
                                }
                                if (object.key != null)
                                    if (object.key.length)
                                        if (typeof object.key === "string")
                                            $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                                        else if (object.key.length >= 0)
                                            message.key = object.key;
                                return message;
                            };

                            /**
                             * Creates a plain object from an EvmHookSlotKey message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookSlotKey
                             * @static
                             * @param {com.hedera.hapi.node.state.hooks.EvmHookSlotKey} message EvmHookSlotKey
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            EvmHookSlotKey.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    object.hookId = null;
                                    if (options.bytes === $String)
                                        object.key = "";
                                    else {
                                        object.key = [];
                                        if (options.bytes !== $Array)
                                            object.key = $util.newBuffer(object.key);
                                    }
                                }
                                if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId"))
                                    object.hookId = $root.proto.HookId.toObject(message.hookId, options, _depth + 1);
                                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                                    object.key = options.bytes === $String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.key) : message.key;
                                return object;
                            };

                            /**
                             * Converts this EvmHookSlotKey to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookSlotKey
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            EvmHookSlotKey.prototype.toJSON = function() {
                                return EvmHookSlotKey.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for EvmHookSlotKey
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.hooks.EvmHookSlotKey
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            EvmHookSlotKey.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.hooks.EvmHookSlotKey";
                            };

                            return EvmHookSlotKey;
                        })();

                        return hooks;
                    })();

                    state.entity = (function() {

                        /**
                         * Namespace entity.
                         * @memberof com.hedera.hapi.node.state
                         * @namespace
                         */
                        const entity = {};

                        entity.EntityCounts = (function() {

                            /**
                             * Properties of an EntityCounts.
                             * @typedef {Object} com.hedera.hapi.node.state.entity.EntityCounts.$Properties
                             * @property {number|Long|null} [numAccounts] The number of accounts in the network.
                             * @property {number|Long|null} [numAliases] The number of aliases in the network.
                             * @property {number|Long|null} [numTokens] The number of tokens in the network.
                             * @property {number|Long|null} [numTokenRelations] The number of token relationships in the network.
                             * @property {number|Long|null} [numNfts] The number of NFTs in the network.
                             * @property {number|Long|null} [numAirdrops] The number of airdrops in the network.
                             * @property {number|Long|null} [numStakingInfos] The number of staking infos in the network.
                             * @property {number|Long|null} [numTopics] The number of topics in the network.
                             * @property {number|Long|null} [numFiles] The number of files in the network.
                             * @property {number|Long|null} [numNodes] The number of nodes in the network.
                             * @property {number|Long|null} [numSchedules] The number of schedules in the network.
                             * @property {number|Long|null} [numContractStorageSlots] The number of contract storage slots in the network.
                             * @property {number|Long|null} [numContractBytecodes] The number of contract bytecodes in the network.
                             * @property {number|Long|null} [numHooks] The number of hooks in the network.
                             * @property {number|Long|null} [numEvmHookStorageSlots] The number of EVM hook storage slots in the network.
                             * @property {number|Long|null} [numRegisteredNodes] The number of registered nodes in the network.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of an EntityCounts.
                             * @memberof com.hedera.hapi.node.state.entity
                             * @interface IEntityCounts
                             * @augments com.hedera.hapi.node.state.entity.EntityCounts.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.entity.EntityCounts.$Properties instead.
                             */

                            /**
                             * Shape of an EntityCounts.
                             * @typedef {com.hedera.hapi.node.state.entity.EntityCounts.$Properties} com.hedera.hapi.node.state.entity.EntityCounts.$Shape
                             */

                            /**
                             * Constructs a new EntityCounts.
                             * @memberof com.hedera.hapi.node.state.entity
                             * @classdesc Representation of a Hedera Entity Service entity counts in the network Merkle tree.
                             * 
                             * This message is used to store the counts of various entities in the network.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.entity.EntityCounts.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const EntityCounts = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The number of accounts in the network.
                             * @member {number|Long} numAccounts
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numAccounts = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of aliases in the network.
                             * @member {number|Long} numAliases
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numAliases = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of tokens in the network.
                             * @member {number|Long} numTokens
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numTokens = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of token relationships in the network.
                             * @member {number|Long} numTokenRelations
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numTokenRelations = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of NFTs in the network.
                             * @member {number|Long} numNfts
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numNfts = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of airdrops in the network.
                             * @member {number|Long} numAirdrops
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numAirdrops = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of staking infos in the network.
                             * @member {number|Long} numStakingInfos
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numStakingInfos = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of topics in the network.
                             * @member {number|Long} numTopics
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numTopics = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of files in the network.
                             * @member {number|Long} numFiles
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numFiles = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of nodes in the network.
                             * @member {number|Long} numNodes
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numNodes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of schedules in the network.
                             * @member {number|Long} numSchedules
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numSchedules = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of contract storage slots in the network.
                             * @member {number|Long} numContractStorageSlots
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numContractStorageSlots = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of contract bytecodes in the network.
                             * @member {number|Long} numContractBytecodes
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numContractBytecodes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of hooks in the network.
                             * @member {number|Long} numHooks
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numHooks = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of EVM hook storage slots in the network.
                             * @member {number|Long} numEvmHookStorageSlots
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numEvmHookStorageSlots = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The number of registered nodes in the network.
                             * @member {number|Long} numRegisteredNodes
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             */
                            EntityCounts.prototype.numRegisteredNodes = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * Creates a new EntityCounts instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @static
                             * @param {com.hedera.hapi.node.state.entity.EntityCounts.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.entity.EntityCounts} EntityCounts instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.entity.EntityCounts.$Shape): com.hedera.hapi.node.state.entity.EntityCounts & com.hedera.hapi.node.state.entity.EntityCounts.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.entity.EntityCounts.$Properties): com.hedera.hapi.node.state.entity.EntityCounts;
                             * }}
                             */
                            EntityCounts.create = function(properties) {
                                return new EntityCounts(properties);
                            };

                            /**
                             * Encodes the specified EntityCounts message. Does not implicitly {@link com.hedera.hapi.node.state.entity.EntityCounts.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @static
                             * @param {com.hedera.hapi.node.state.entity.EntityCounts.$Properties} message EntityCounts message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            EntityCounts.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.numAccounts != null && $Object.hasOwnProperty.call(message, "numAccounts") && (typeof message.numAccounts === "object" ? message.numAccounts.low || message.numAccounts.high : message.numAccounts !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.numAccounts);
                                if (message.numAliases != null && $Object.hasOwnProperty.call(message, "numAliases") && (typeof message.numAliases === "object" ? message.numAliases.low || message.numAliases.high : message.numAliases !== 0))
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.numAliases);
                                if (message.numTokens != null && $Object.hasOwnProperty.call(message, "numTokens") && (typeof message.numTokens === "object" ? message.numTokens.low || message.numTokens.high : message.numTokens !== 0))
                                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.numTokens);
                                if (message.numTokenRelations != null && $Object.hasOwnProperty.call(message, "numTokenRelations") && (typeof message.numTokenRelations === "object" ? message.numTokenRelations.low || message.numTokenRelations.high : message.numTokenRelations !== 0))
                                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.numTokenRelations);
                                if (message.numNfts != null && $Object.hasOwnProperty.call(message, "numNfts") && (typeof message.numNfts === "object" ? message.numNfts.low || message.numNfts.high : message.numNfts !== 0))
                                    writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.numNfts);
                                if (message.numAirdrops != null && $Object.hasOwnProperty.call(message, "numAirdrops") && (typeof message.numAirdrops === "object" ? message.numAirdrops.low || message.numAirdrops.high : message.numAirdrops !== 0))
                                    writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.numAirdrops);
                                if (message.numStakingInfos != null && $Object.hasOwnProperty.call(message, "numStakingInfos") && (typeof message.numStakingInfos === "object" ? message.numStakingInfos.low || message.numStakingInfos.high : message.numStakingInfos !== 0))
                                    writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.numStakingInfos);
                                if (message.numTopics != null && $Object.hasOwnProperty.call(message, "numTopics") && (typeof message.numTopics === "object" ? message.numTopics.low || message.numTopics.high : message.numTopics !== 0))
                                    writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.numTopics);
                                if (message.numFiles != null && $Object.hasOwnProperty.call(message, "numFiles") && (typeof message.numFiles === "object" ? message.numFiles.low || message.numFiles.high : message.numFiles !== 0))
                                    writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.numFiles);
                                if (message.numNodes != null && $Object.hasOwnProperty.call(message, "numNodes") && (typeof message.numNodes === "object" ? message.numNodes.low || message.numNodes.high : message.numNodes !== 0))
                                    writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.numNodes);
                                if (message.numSchedules != null && $Object.hasOwnProperty.call(message, "numSchedules") && (typeof message.numSchedules === "object" ? message.numSchedules.low || message.numSchedules.high : message.numSchedules !== 0))
                                    writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.numSchedules);
                                if (message.numContractStorageSlots != null && $Object.hasOwnProperty.call(message, "numContractStorageSlots") && (typeof message.numContractStorageSlots === "object" ? message.numContractStorageSlots.low || message.numContractStorageSlots.high : message.numContractStorageSlots !== 0))
                                    writer.uint32(/* id 12, wireType 0 =*/96).uint64(message.numContractStorageSlots);
                                if (message.numContractBytecodes != null && $Object.hasOwnProperty.call(message, "numContractBytecodes") && (typeof message.numContractBytecodes === "object" ? message.numContractBytecodes.low || message.numContractBytecodes.high : message.numContractBytecodes !== 0))
                                    writer.uint32(/* id 13, wireType 0 =*/104).uint64(message.numContractBytecodes);
                                if (message.numHooks != null && $Object.hasOwnProperty.call(message, "numHooks") && (typeof message.numHooks === "object" ? message.numHooks.low || message.numHooks.high : message.numHooks !== 0))
                                    writer.uint32(/* id 14, wireType 0 =*/112).uint64(message.numHooks);
                                if (message.numEvmHookStorageSlots != null && $Object.hasOwnProperty.call(message, "numEvmHookStorageSlots") && (typeof message.numEvmHookStorageSlots === "object" ? message.numEvmHookStorageSlots.low || message.numEvmHookStorageSlots.high : message.numEvmHookStorageSlots !== 0))
                                    writer.uint32(/* id 15, wireType 0 =*/120).uint64(message.numEvmHookStorageSlots);
                                if (message.numRegisteredNodes != null && $Object.hasOwnProperty.call(message, "numRegisteredNodes") && (typeof message.numRegisteredNodes === "object" ? message.numRegisteredNodes.low || message.numRegisteredNodes.high : message.numRegisteredNodes !== 0))
                                    writer.uint32(/* id 16, wireType 0 =*/128).uint64(message.numRegisteredNodes);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified EntityCounts message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.entity.EntityCounts.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @static
                             * @param {com.hedera.hapi.node.state.entity.EntityCounts.$Properties} message EntityCounts message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            EntityCounts.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes an EntityCounts message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.entity.EntityCounts & com.hedera.hapi.node.state.entity.EntityCounts.$Shape} EntityCounts
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            EntityCounts.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.entity.EntityCounts();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numAccounts = value;
                                            else
                                                delete message.numAccounts;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numAliases = value;
                                            else
                                                delete message.numAliases;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numTokens = value;
                                            else
                                                delete message.numTokens;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numTokenRelations = value;
                                            else
                                                delete message.numTokenRelations;
                                            continue;
                                        }
                                    case 5: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numNfts = value;
                                            else
                                                delete message.numNfts;
                                            continue;
                                        }
                                    case 6: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numAirdrops = value;
                                            else
                                                delete message.numAirdrops;
                                            continue;
                                        }
                                    case 7: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numStakingInfos = value;
                                            else
                                                delete message.numStakingInfos;
                                            continue;
                                        }
                                    case 8: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numTopics = value;
                                            else
                                                delete message.numTopics;
                                            continue;
                                        }
                                    case 9: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numFiles = value;
                                            else
                                                delete message.numFiles;
                                            continue;
                                        }
                                    case 10: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numNodes = value;
                                            else
                                                delete message.numNodes;
                                            continue;
                                        }
                                    case 11: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numSchedules = value;
                                            else
                                                delete message.numSchedules;
                                            continue;
                                        }
                                    case 12: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numContractStorageSlots = value;
                                            else
                                                delete message.numContractStorageSlots;
                                            continue;
                                        }
                                    case 13: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numContractBytecodes = value;
                                            else
                                                delete message.numContractBytecodes;
                                            continue;
                                        }
                                    case 14: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numHooks = value;
                                            else
                                                delete message.numHooks;
                                            continue;
                                        }
                                    case 15: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numEvmHookStorageSlots = value;
                                            else
                                                delete message.numEvmHookStorageSlots;
                                            continue;
                                        }
                                    case 16: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.numRegisteredNodes = value;
                                            else
                                                delete message.numRegisteredNodes;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes an EntityCounts message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.entity.EntityCounts & com.hedera.hapi.node.state.entity.EntityCounts.$Shape} EntityCounts
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            EntityCounts.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies an EntityCounts message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            EntityCounts.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.numAccounts != null && $Object.hasOwnProperty.call(message, "numAccounts"))
                                    if (!$util.isInteger(message.numAccounts) && !(message.numAccounts && $util.isInteger(message.numAccounts.low) && $util.isInteger(message.numAccounts.high)))
                                        return "numAccounts: integer|Long expected";
                                if (message.numAliases != null && $Object.hasOwnProperty.call(message, "numAliases"))
                                    if (!$util.isInteger(message.numAliases) && !(message.numAliases && $util.isInteger(message.numAliases.low) && $util.isInteger(message.numAliases.high)))
                                        return "numAliases: integer|Long expected";
                                if (message.numTokens != null && $Object.hasOwnProperty.call(message, "numTokens"))
                                    if (!$util.isInteger(message.numTokens) && !(message.numTokens && $util.isInteger(message.numTokens.low) && $util.isInteger(message.numTokens.high)))
                                        return "numTokens: integer|Long expected";
                                if (message.numTokenRelations != null && $Object.hasOwnProperty.call(message, "numTokenRelations"))
                                    if (!$util.isInteger(message.numTokenRelations) && !(message.numTokenRelations && $util.isInteger(message.numTokenRelations.low) && $util.isInteger(message.numTokenRelations.high)))
                                        return "numTokenRelations: integer|Long expected";
                                if (message.numNfts != null && $Object.hasOwnProperty.call(message, "numNfts"))
                                    if (!$util.isInteger(message.numNfts) && !(message.numNfts && $util.isInteger(message.numNfts.low) && $util.isInteger(message.numNfts.high)))
                                        return "numNfts: integer|Long expected";
                                if (message.numAirdrops != null && $Object.hasOwnProperty.call(message, "numAirdrops"))
                                    if (!$util.isInteger(message.numAirdrops) && !(message.numAirdrops && $util.isInteger(message.numAirdrops.low) && $util.isInteger(message.numAirdrops.high)))
                                        return "numAirdrops: integer|Long expected";
                                if (message.numStakingInfos != null && $Object.hasOwnProperty.call(message, "numStakingInfos"))
                                    if (!$util.isInteger(message.numStakingInfos) && !(message.numStakingInfos && $util.isInteger(message.numStakingInfos.low) && $util.isInteger(message.numStakingInfos.high)))
                                        return "numStakingInfos: integer|Long expected";
                                if (message.numTopics != null && $Object.hasOwnProperty.call(message, "numTopics"))
                                    if (!$util.isInteger(message.numTopics) && !(message.numTopics && $util.isInteger(message.numTopics.low) && $util.isInteger(message.numTopics.high)))
                                        return "numTopics: integer|Long expected";
                                if (message.numFiles != null && $Object.hasOwnProperty.call(message, "numFiles"))
                                    if (!$util.isInteger(message.numFiles) && !(message.numFiles && $util.isInteger(message.numFiles.low) && $util.isInteger(message.numFiles.high)))
                                        return "numFiles: integer|Long expected";
                                if (message.numNodes != null && $Object.hasOwnProperty.call(message, "numNodes"))
                                    if (!$util.isInteger(message.numNodes) && !(message.numNodes && $util.isInteger(message.numNodes.low) && $util.isInteger(message.numNodes.high)))
                                        return "numNodes: integer|Long expected";
                                if (message.numSchedules != null && $Object.hasOwnProperty.call(message, "numSchedules"))
                                    if (!$util.isInteger(message.numSchedules) && !(message.numSchedules && $util.isInteger(message.numSchedules.low) && $util.isInteger(message.numSchedules.high)))
                                        return "numSchedules: integer|Long expected";
                                if (message.numContractStorageSlots != null && $Object.hasOwnProperty.call(message, "numContractStorageSlots"))
                                    if (!$util.isInteger(message.numContractStorageSlots) && !(message.numContractStorageSlots && $util.isInteger(message.numContractStorageSlots.low) && $util.isInteger(message.numContractStorageSlots.high)))
                                        return "numContractStorageSlots: integer|Long expected";
                                if (message.numContractBytecodes != null && $Object.hasOwnProperty.call(message, "numContractBytecodes"))
                                    if (!$util.isInteger(message.numContractBytecodes) && !(message.numContractBytecodes && $util.isInteger(message.numContractBytecodes.low) && $util.isInteger(message.numContractBytecodes.high)))
                                        return "numContractBytecodes: integer|Long expected";
                                if (message.numHooks != null && $Object.hasOwnProperty.call(message, "numHooks"))
                                    if (!$util.isInteger(message.numHooks) && !(message.numHooks && $util.isInteger(message.numHooks.low) && $util.isInteger(message.numHooks.high)))
                                        return "numHooks: integer|Long expected";
                                if (message.numEvmHookStorageSlots != null && $Object.hasOwnProperty.call(message, "numEvmHookStorageSlots"))
                                    if (!$util.isInteger(message.numEvmHookStorageSlots) && !(message.numEvmHookStorageSlots && $util.isInteger(message.numEvmHookStorageSlots.low) && $util.isInteger(message.numEvmHookStorageSlots.high)))
                                        return "numEvmHookStorageSlots: integer|Long expected";
                                if (message.numRegisteredNodes != null && $Object.hasOwnProperty.call(message, "numRegisteredNodes"))
                                    if (!$util.isInteger(message.numRegisteredNodes) && !(message.numRegisteredNodes && $util.isInteger(message.numRegisteredNodes.low) && $util.isInteger(message.numRegisteredNodes.high)))
                                        return "numRegisteredNodes: integer|Long expected";
                                return null;
                            };

                            /**
                             * Creates an EntityCounts message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.entity.EntityCounts} EntityCounts
                             */
                            EntityCounts.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.entity.EntityCounts)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.entity.EntityCounts: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.entity.EntityCounts();
                                if (object.numAccounts != null)
                                    if (typeof object.numAccounts === "object" ? object.numAccounts.low || object.numAccounts.high : $Number(object.numAccounts) !== 0)
                                        if ($util.Long)
                                            message.numAccounts = $util.Long.fromValue(object.numAccounts, true);
                                        else if (typeof object.numAccounts === "string")
                                            message.numAccounts = $parseInt(object.numAccounts, 10);
                                        else if (typeof object.numAccounts === "number")
                                            message.numAccounts = object.numAccounts;
                                        else if (typeof object.numAccounts === "object")
                                            message.numAccounts = new $util.LongBits(object.numAccounts.low >>> 0, object.numAccounts.high >>> 0).toNumber(true);
                                if (object.numAliases != null)
                                    if (typeof object.numAliases === "object" ? object.numAliases.low || object.numAliases.high : $Number(object.numAliases) !== 0)
                                        if ($util.Long)
                                            message.numAliases = $util.Long.fromValue(object.numAliases, true);
                                        else if (typeof object.numAliases === "string")
                                            message.numAliases = $parseInt(object.numAliases, 10);
                                        else if (typeof object.numAliases === "number")
                                            message.numAliases = object.numAliases;
                                        else if (typeof object.numAliases === "object")
                                            message.numAliases = new $util.LongBits(object.numAliases.low >>> 0, object.numAliases.high >>> 0).toNumber(true);
                                if (object.numTokens != null)
                                    if (typeof object.numTokens === "object" ? object.numTokens.low || object.numTokens.high : $Number(object.numTokens) !== 0)
                                        if ($util.Long)
                                            message.numTokens = $util.Long.fromValue(object.numTokens, true);
                                        else if (typeof object.numTokens === "string")
                                            message.numTokens = $parseInt(object.numTokens, 10);
                                        else if (typeof object.numTokens === "number")
                                            message.numTokens = object.numTokens;
                                        else if (typeof object.numTokens === "object")
                                            message.numTokens = new $util.LongBits(object.numTokens.low >>> 0, object.numTokens.high >>> 0).toNumber(true);
                                if (object.numTokenRelations != null)
                                    if (typeof object.numTokenRelations === "object" ? object.numTokenRelations.low || object.numTokenRelations.high : $Number(object.numTokenRelations) !== 0)
                                        if ($util.Long)
                                            message.numTokenRelations = $util.Long.fromValue(object.numTokenRelations, true);
                                        else if (typeof object.numTokenRelations === "string")
                                            message.numTokenRelations = $parseInt(object.numTokenRelations, 10);
                                        else if (typeof object.numTokenRelations === "number")
                                            message.numTokenRelations = object.numTokenRelations;
                                        else if (typeof object.numTokenRelations === "object")
                                            message.numTokenRelations = new $util.LongBits(object.numTokenRelations.low >>> 0, object.numTokenRelations.high >>> 0).toNumber(true);
                                if (object.numNfts != null)
                                    if (typeof object.numNfts === "object" ? object.numNfts.low || object.numNfts.high : $Number(object.numNfts) !== 0)
                                        if ($util.Long)
                                            message.numNfts = $util.Long.fromValue(object.numNfts, true);
                                        else if (typeof object.numNfts === "string")
                                            message.numNfts = $parseInt(object.numNfts, 10);
                                        else if (typeof object.numNfts === "number")
                                            message.numNfts = object.numNfts;
                                        else if (typeof object.numNfts === "object")
                                            message.numNfts = new $util.LongBits(object.numNfts.low >>> 0, object.numNfts.high >>> 0).toNumber(true);
                                if (object.numAirdrops != null)
                                    if (typeof object.numAirdrops === "object" ? object.numAirdrops.low || object.numAirdrops.high : $Number(object.numAirdrops) !== 0)
                                        if ($util.Long)
                                            message.numAirdrops = $util.Long.fromValue(object.numAirdrops, true);
                                        else if (typeof object.numAirdrops === "string")
                                            message.numAirdrops = $parseInt(object.numAirdrops, 10);
                                        else if (typeof object.numAirdrops === "number")
                                            message.numAirdrops = object.numAirdrops;
                                        else if (typeof object.numAirdrops === "object")
                                            message.numAirdrops = new $util.LongBits(object.numAirdrops.low >>> 0, object.numAirdrops.high >>> 0).toNumber(true);
                                if (object.numStakingInfos != null)
                                    if (typeof object.numStakingInfos === "object" ? object.numStakingInfos.low || object.numStakingInfos.high : $Number(object.numStakingInfos) !== 0)
                                        if ($util.Long)
                                            message.numStakingInfos = $util.Long.fromValue(object.numStakingInfos, true);
                                        else if (typeof object.numStakingInfos === "string")
                                            message.numStakingInfos = $parseInt(object.numStakingInfos, 10);
                                        else if (typeof object.numStakingInfos === "number")
                                            message.numStakingInfos = object.numStakingInfos;
                                        else if (typeof object.numStakingInfos === "object")
                                            message.numStakingInfos = new $util.LongBits(object.numStakingInfos.low >>> 0, object.numStakingInfos.high >>> 0).toNumber(true);
                                if (object.numTopics != null)
                                    if (typeof object.numTopics === "object" ? object.numTopics.low || object.numTopics.high : $Number(object.numTopics) !== 0)
                                        if ($util.Long)
                                            message.numTopics = $util.Long.fromValue(object.numTopics, true);
                                        else if (typeof object.numTopics === "string")
                                            message.numTopics = $parseInt(object.numTopics, 10);
                                        else if (typeof object.numTopics === "number")
                                            message.numTopics = object.numTopics;
                                        else if (typeof object.numTopics === "object")
                                            message.numTopics = new $util.LongBits(object.numTopics.low >>> 0, object.numTopics.high >>> 0).toNumber(true);
                                if (object.numFiles != null)
                                    if (typeof object.numFiles === "object" ? object.numFiles.low || object.numFiles.high : $Number(object.numFiles) !== 0)
                                        if ($util.Long)
                                            message.numFiles = $util.Long.fromValue(object.numFiles, true);
                                        else if (typeof object.numFiles === "string")
                                            message.numFiles = $parseInt(object.numFiles, 10);
                                        else if (typeof object.numFiles === "number")
                                            message.numFiles = object.numFiles;
                                        else if (typeof object.numFiles === "object")
                                            message.numFiles = new $util.LongBits(object.numFiles.low >>> 0, object.numFiles.high >>> 0).toNumber(true);
                                if (object.numNodes != null)
                                    if (typeof object.numNodes === "object" ? object.numNodes.low || object.numNodes.high : $Number(object.numNodes) !== 0)
                                        if ($util.Long)
                                            message.numNodes = $util.Long.fromValue(object.numNodes, true);
                                        else if (typeof object.numNodes === "string")
                                            message.numNodes = $parseInt(object.numNodes, 10);
                                        else if (typeof object.numNodes === "number")
                                            message.numNodes = object.numNodes;
                                        else if (typeof object.numNodes === "object")
                                            message.numNodes = new $util.LongBits(object.numNodes.low >>> 0, object.numNodes.high >>> 0).toNumber(true);
                                if (object.numSchedules != null)
                                    if (typeof object.numSchedules === "object" ? object.numSchedules.low || object.numSchedules.high : $Number(object.numSchedules) !== 0)
                                        if ($util.Long)
                                            message.numSchedules = $util.Long.fromValue(object.numSchedules, true);
                                        else if (typeof object.numSchedules === "string")
                                            message.numSchedules = $parseInt(object.numSchedules, 10);
                                        else if (typeof object.numSchedules === "number")
                                            message.numSchedules = object.numSchedules;
                                        else if (typeof object.numSchedules === "object")
                                            message.numSchedules = new $util.LongBits(object.numSchedules.low >>> 0, object.numSchedules.high >>> 0).toNumber(true);
                                if (object.numContractStorageSlots != null)
                                    if (typeof object.numContractStorageSlots === "object" ? object.numContractStorageSlots.low || object.numContractStorageSlots.high : $Number(object.numContractStorageSlots) !== 0)
                                        if ($util.Long)
                                            message.numContractStorageSlots = $util.Long.fromValue(object.numContractStorageSlots, true);
                                        else if (typeof object.numContractStorageSlots === "string")
                                            message.numContractStorageSlots = $parseInt(object.numContractStorageSlots, 10);
                                        else if (typeof object.numContractStorageSlots === "number")
                                            message.numContractStorageSlots = object.numContractStorageSlots;
                                        else if (typeof object.numContractStorageSlots === "object")
                                            message.numContractStorageSlots = new $util.LongBits(object.numContractStorageSlots.low >>> 0, object.numContractStorageSlots.high >>> 0).toNumber(true);
                                if (object.numContractBytecodes != null)
                                    if (typeof object.numContractBytecodes === "object" ? object.numContractBytecodes.low || object.numContractBytecodes.high : $Number(object.numContractBytecodes) !== 0)
                                        if ($util.Long)
                                            message.numContractBytecodes = $util.Long.fromValue(object.numContractBytecodes, true);
                                        else if (typeof object.numContractBytecodes === "string")
                                            message.numContractBytecodes = $parseInt(object.numContractBytecodes, 10);
                                        else if (typeof object.numContractBytecodes === "number")
                                            message.numContractBytecodes = object.numContractBytecodes;
                                        else if (typeof object.numContractBytecodes === "object")
                                            message.numContractBytecodes = new $util.LongBits(object.numContractBytecodes.low >>> 0, object.numContractBytecodes.high >>> 0).toNumber(true);
                                if (object.numHooks != null)
                                    if (typeof object.numHooks === "object" ? object.numHooks.low || object.numHooks.high : $Number(object.numHooks) !== 0)
                                        if ($util.Long)
                                            message.numHooks = $util.Long.fromValue(object.numHooks, true);
                                        else if (typeof object.numHooks === "string")
                                            message.numHooks = $parseInt(object.numHooks, 10);
                                        else if (typeof object.numHooks === "number")
                                            message.numHooks = object.numHooks;
                                        else if (typeof object.numHooks === "object")
                                            message.numHooks = new $util.LongBits(object.numHooks.low >>> 0, object.numHooks.high >>> 0).toNumber(true);
                                if (object.numEvmHookStorageSlots != null)
                                    if (typeof object.numEvmHookStorageSlots === "object" ? object.numEvmHookStorageSlots.low || object.numEvmHookStorageSlots.high : $Number(object.numEvmHookStorageSlots) !== 0)
                                        if ($util.Long)
                                            message.numEvmHookStorageSlots = $util.Long.fromValue(object.numEvmHookStorageSlots, true);
                                        else if (typeof object.numEvmHookStorageSlots === "string")
                                            message.numEvmHookStorageSlots = $parseInt(object.numEvmHookStorageSlots, 10);
                                        else if (typeof object.numEvmHookStorageSlots === "number")
                                            message.numEvmHookStorageSlots = object.numEvmHookStorageSlots;
                                        else if (typeof object.numEvmHookStorageSlots === "object")
                                            message.numEvmHookStorageSlots = new $util.LongBits(object.numEvmHookStorageSlots.low >>> 0, object.numEvmHookStorageSlots.high >>> 0).toNumber(true);
                                if (object.numRegisteredNodes != null)
                                    if (typeof object.numRegisteredNodes === "object" ? object.numRegisteredNodes.low || object.numRegisteredNodes.high : $Number(object.numRegisteredNodes) !== 0)
                                        if ($util.Long)
                                            message.numRegisteredNodes = $util.Long.fromValue(object.numRegisteredNodes, true);
                                        else if (typeof object.numRegisteredNodes === "string")
                                            message.numRegisteredNodes = $parseInt(object.numRegisteredNodes, 10);
                                        else if (typeof object.numRegisteredNodes === "number")
                                            message.numRegisteredNodes = object.numRegisteredNodes;
                                        else if (typeof object.numRegisteredNodes === "object")
                                            message.numRegisteredNodes = new $util.LongBits(object.numRegisteredNodes.low >>> 0, object.numRegisteredNodes.high >>> 0).toNumber(true);
                                return message;
                            };

                            /**
                             * Creates a plain object from an EntityCounts message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @static
                             * @param {com.hedera.hapi.node.state.entity.EntityCounts} message EntityCounts
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            EntityCounts.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numAccounts = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numAccounts = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numAliases = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numAliases = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numTokens = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numTokens = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numTokenRelations = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numTokenRelations = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numNfts = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numNfts = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numAirdrops = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numAirdrops = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numStakingInfos = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numStakingInfos = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numTopics = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numTopics = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numFiles = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numFiles = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numNodes = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numNodes = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numSchedules = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numSchedules = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numContractStorageSlots = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numContractStorageSlots = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numContractBytecodes = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numContractBytecodes = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numHooks = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numHooks = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numEvmHookStorageSlots = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numEvmHookStorageSlots = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.numRegisteredNodes = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.numRegisteredNodes = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                }
                                if (message.numAccounts != null && $Object.hasOwnProperty.call(message, "numAccounts"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numAccounts = typeof message.numAccounts === "number" ? $BigInt(message.numAccounts) : $util.Long.fromBits(message.numAccounts.low >>> 0, message.numAccounts.high >>> 0, true).toBigInt();
                                    else if (typeof message.numAccounts === "number")
                                        object.numAccounts = options.longs === $String ? $String(message.numAccounts) : message.numAccounts;
                                    else
                                        object.numAccounts = options.longs === $String ? $util.Long.prototype.toString.call(message.numAccounts) : options.longs === $Number ? new $util.LongBits(message.numAccounts.low >>> 0, message.numAccounts.high >>> 0).toNumber(true) : message.numAccounts;
                                if (message.numAliases != null && $Object.hasOwnProperty.call(message, "numAliases"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numAliases = typeof message.numAliases === "number" ? $BigInt(message.numAliases) : $util.Long.fromBits(message.numAliases.low >>> 0, message.numAliases.high >>> 0, true).toBigInt();
                                    else if (typeof message.numAliases === "number")
                                        object.numAliases = options.longs === $String ? $String(message.numAliases) : message.numAliases;
                                    else
                                        object.numAliases = options.longs === $String ? $util.Long.prototype.toString.call(message.numAliases) : options.longs === $Number ? new $util.LongBits(message.numAliases.low >>> 0, message.numAliases.high >>> 0).toNumber(true) : message.numAliases;
                                if (message.numTokens != null && $Object.hasOwnProperty.call(message, "numTokens"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numTokens = typeof message.numTokens === "number" ? $BigInt(message.numTokens) : $util.Long.fromBits(message.numTokens.low >>> 0, message.numTokens.high >>> 0, true).toBigInt();
                                    else if (typeof message.numTokens === "number")
                                        object.numTokens = options.longs === $String ? $String(message.numTokens) : message.numTokens;
                                    else
                                        object.numTokens = options.longs === $String ? $util.Long.prototype.toString.call(message.numTokens) : options.longs === $Number ? new $util.LongBits(message.numTokens.low >>> 0, message.numTokens.high >>> 0).toNumber(true) : message.numTokens;
                                if (message.numTokenRelations != null && $Object.hasOwnProperty.call(message, "numTokenRelations"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numTokenRelations = typeof message.numTokenRelations === "number" ? $BigInt(message.numTokenRelations) : $util.Long.fromBits(message.numTokenRelations.low >>> 0, message.numTokenRelations.high >>> 0, true).toBigInt();
                                    else if (typeof message.numTokenRelations === "number")
                                        object.numTokenRelations = options.longs === $String ? $String(message.numTokenRelations) : message.numTokenRelations;
                                    else
                                        object.numTokenRelations = options.longs === $String ? $util.Long.prototype.toString.call(message.numTokenRelations) : options.longs === $Number ? new $util.LongBits(message.numTokenRelations.low >>> 0, message.numTokenRelations.high >>> 0).toNumber(true) : message.numTokenRelations;
                                if (message.numNfts != null && $Object.hasOwnProperty.call(message, "numNfts"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numNfts = typeof message.numNfts === "number" ? $BigInt(message.numNfts) : $util.Long.fromBits(message.numNfts.low >>> 0, message.numNfts.high >>> 0, true).toBigInt();
                                    else if (typeof message.numNfts === "number")
                                        object.numNfts = options.longs === $String ? $String(message.numNfts) : message.numNfts;
                                    else
                                        object.numNfts = options.longs === $String ? $util.Long.prototype.toString.call(message.numNfts) : options.longs === $Number ? new $util.LongBits(message.numNfts.low >>> 0, message.numNfts.high >>> 0).toNumber(true) : message.numNfts;
                                if (message.numAirdrops != null && $Object.hasOwnProperty.call(message, "numAirdrops"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numAirdrops = typeof message.numAirdrops === "number" ? $BigInt(message.numAirdrops) : $util.Long.fromBits(message.numAirdrops.low >>> 0, message.numAirdrops.high >>> 0, true).toBigInt();
                                    else if (typeof message.numAirdrops === "number")
                                        object.numAirdrops = options.longs === $String ? $String(message.numAirdrops) : message.numAirdrops;
                                    else
                                        object.numAirdrops = options.longs === $String ? $util.Long.prototype.toString.call(message.numAirdrops) : options.longs === $Number ? new $util.LongBits(message.numAirdrops.low >>> 0, message.numAirdrops.high >>> 0).toNumber(true) : message.numAirdrops;
                                if (message.numStakingInfos != null && $Object.hasOwnProperty.call(message, "numStakingInfos"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numStakingInfos = typeof message.numStakingInfos === "number" ? $BigInt(message.numStakingInfos) : $util.Long.fromBits(message.numStakingInfos.low >>> 0, message.numStakingInfos.high >>> 0, true).toBigInt();
                                    else if (typeof message.numStakingInfos === "number")
                                        object.numStakingInfos = options.longs === $String ? $String(message.numStakingInfos) : message.numStakingInfos;
                                    else
                                        object.numStakingInfos = options.longs === $String ? $util.Long.prototype.toString.call(message.numStakingInfos) : options.longs === $Number ? new $util.LongBits(message.numStakingInfos.low >>> 0, message.numStakingInfos.high >>> 0).toNumber(true) : message.numStakingInfos;
                                if (message.numTopics != null && $Object.hasOwnProperty.call(message, "numTopics"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numTopics = typeof message.numTopics === "number" ? $BigInt(message.numTopics) : $util.Long.fromBits(message.numTopics.low >>> 0, message.numTopics.high >>> 0, true).toBigInt();
                                    else if (typeof message.numTopics === "number")
                                        object.numTopics = options.longs === $String ? $String(message.numTopics) : message.numTopics;
                                    else
                                        object.numTopics = options.longs === $String ? $util.Long.prototype.toString.call(message.numTopics) : options.longs === $Number ? new $util.LongBits(message.numTopics.low >>> 0, message.numTopics.high >>> 0).toNumber(true) : message.numTopics;
                                if (message.numFiles != null && $Object.hasOwnProperty.call(message, "numFiles"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numFiles = typeof message.numFiles === "number" ? $BigInt(message.numFiles) : $util.Long.fromBits(message.numFiles.low >>> 0, message.numFiles.high >>> 0, true).toBigInt();
                                    else if (typeof message.numFiles === "number")
                                        object.numFiles = options.longs === $String ? $String(message.numFiles) : message.numFiles;
                                    else
                                        object.numFiles = options.longs === $String ? $util.Long.prototype.toString.call(message.numFiles) : options.longs === $Number ? new $util.LongBits(message.numFiles.low >>> 0, message.numFiles.high >>> 0).toNumber(true) : message.numFiles;
                                if (message.numNodes != null && $Object.hasOwnProperty.call(message, "numNodes"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numNodes = typeof message.numNodes === "number" ? $BigInt(message.numNodes) : $util.Long.fromBits(message.numNodes.low >>> 0, message.numNodes.high >>> 0, true).toBigInt();
                                    else if (typeof message.numNodes === "number")
                                        object.numNodes = options.longs === $String ? $String(message.numNodes) : message.numNodes;
                                    else
                                        object.numNodes = options.longs === $String ? $util.Long.prototype.toString.call(message.numNodes) : options.longs === $Number ? new $util.LongBits(message.numNodes.low >>> 0, message.numNodes.high >>> 0).toNumber(true) : message.numNodes;
                                if (message.numSchedules != null && $Object.hasOwnProperty.call(message, "numSchedules"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numSchedules = typeof message.numSchedules === "number" ? $BigInt(message.numSchedules) : $util.Long.fromBits(message.numSchedules.low >>> 0, message.numSchedules.high >>> 0, true).toBigInt();
                                    else if (typeof message.numSchedules === "number")
                                        object.numSchedules = options.longs === $String ? $String(message.numSchedules) : message.numSchedules;
                                    else
                                        object.numSchedules = options.longs === $String ? $util.Long.prototype.toString.call(message.numSchedules) : options.longs === $Number ? new $util.LongBits(message.numSchedules.low >>> 0, message.numSchedules.high >>> 0).toNumber(true) : message.numSchedules;
                                if (message.numContractStorageSlots != null && $Object.hasOwnProperty.call(message, "numContractStorageSlots"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numContractStorageSlots = typeof message.numContractStorageSlots === "number" ? $BigInt(message.numContractStorageSlots) : $util.Long.fromBits(message.numContractStorageSlots.low >>> 0, message.numContractStorageSlots.high >>> 0, true).toBigInt();
                                    else if (typeof message.numContractStorageSlots === "number")
                                        object.numContractStorageSlots = options.longs === $String ? $String(message.numContractStorageSlots) : message.numContractStorageSlots;
                                    else
                                        object.numContractStorageSlots = options.longs === $String ? $util.Long.prototype.toString.call(message.numContractStorageSlots) : options.longs === $Number ? new $util.LongBits(message.numContractStorageSlots.low >>> 0, message.numContractStorageSlots.high >>> 0).toNumber(true) : message.numContractStorageSlots;
                                if (message.numContractBytecodes != null && $Object.hasOwnProperty.call(message, "numContractBytecodes"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numContractBytecodes = typeof message.numContractBytecodes === "number" ? $BigInt(message.numContractBytecodes) : $util.Long.fromBits(message.numContractBytecodes.low >>> 0, message.numContractBytecodes.high >>> 0, true).toBigInt();
                                    else if (typeof message.numContractBytecodes === "number")
                                        object.numContractBytecodes = options.longs === $String ? $String(message.numContractBytecodes) : message.numContractBytecodes;
                                    else
                                        object.numContractBytecodes = options.longs === $String ? $util.Long.prototype.toString.call(message.numContractBytecodes) : options.longs === $Number ? new $util.LongBits(message.numContractBytecodes.low >>> 0, message.numContractBytecodes.high >>> 0).toNumber(true) : message.numContractBytecodes;
                                if (message.numHooks != null && $Object.hasOwnProperty.call(message, "numHooks"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numHooks = typeof message.numHooks === "number" ? $BigInt(message.numHooks) : $util.Long.fromBits(message.numHooks.low >>> 0, message.numHooks.high >>> 0, true).toBigInt();
                                    else if (typeof message.numHooks === "number")
                                        object.numHooks = options.longs === $String ? $String(message.numHooks) : message.numHooks;
                                    else
                                        object.numHooks = options.longs === $String ? $util.Long.prototype.toString.call(message.numHooks) : options.longs === $Number ? new $util.LongBits(message.numHooks.low >>> 0, message.numHooks.high >>> 0).toNumber(true) : message.numHooks;
                                if (message.numEvmHookStorageSlots != null && $Object.hasOwnProperty.call(message, "numEvmHookStorageSlots"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numEvmHookStorageSlots = typeof message.numEvmHookStorageSlots === "number" ? $BigInt(message.numEvmHookStorageSlots) : $util.Long.fromBits(message.numEvmHookStorageSlots.low >>> 0, message.numEvmHookStorageSlots.high >>> 0, true).toBigInt();
                                    else if (typeof message.numEvmHookStorageSlots === "number")
                                        object.numEvmHookStorageSlots = options.longs === $String ? $String(message.numEvmHookStorageSlots) : message.numEvmHookStorageSlots;
                                    else
                                        object.numEvmHookStorageSlots = options.longs === $String ? $util.Long.prototype.toString.call(message.numEvmHookStorageSlots) : options.longs === $Number ? new $util.LongBits(message.numEvmHookStorageSlots.low >>> 0, message.numEvmHookStorageSlots.high >>> 0).toNumber(true) : message.numEvmHookStorageSlots;
                                if (message.numRegisteredNodes != null && $Object.hasOwnProperty.call(message, "numRegisteredNodes"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.numRegisteredNodes = typeof message.numRegisteredNodes === "number" ? $BigInt(message.numRegisteredNodes) : $util.Long.fromBits(message.numRegisteredNodes.low >>> 0, message.numRegisteredNodes.high >>> 0, true).toBigInt();
                                    else if (typeof message.numRegisteredNodes === "number")
                                        object.numRegisteredNodes = options.longs === $String ? $String(message.numRegisteredNodes) : message.numRegisteredNodes;
                                    else
                                        object.numRegisteredNodes = options.longs === $String ? $util.Long.prototype.toString.call(message.numRegisteredNodes) : options.longs === $Number ? new $util.LongBits(message.numRegisteredNodes.low >>> 0, message.numRegisteredNodes.high >>> 0).toNumber(true) : message.numRegisteredNodes;
                                return object;
                            };

                            /**
                             * Converts this EntityCounts to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            EntityCounts.prototype.toJSON = function() {
                                return EntityCounts.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for EntityCounts
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.entity.EntityCounts
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            EntityCounts.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.entity.EntityCounts";
                            };

                            return EntityCounts;
                        })();

                        return entity;
                    })();

                    state.roster = (function() {

                        /**
                         * Namespace roster.
                         * @memberof com.hedera.hapi.node.state
                         * @namespace
                         */
                        const roster = {};

                        roster.LedgerId = (function() {

                            /**
                             * Properties of a LedgerId.
                             * @typedef {Object} com.hedera.hapi.node.state.roster.LedgerId.$Properties
                             * @property {Uint8Array|null} [ledgerId] A public key.<br/>
                             * This key both identifies the ledger and can be used to verify ledger
                             * signatures.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * This value MUST contain a valid public key.
                             * @property {number|Long|null} [round] A round number.<br/>
                             * This identifies when this ledger id becomes active.<br/>
                             * This value is REQUIRED.
                             * @property {Uint8Array|null} [ledgerSignature] A signature from the prior ledger key.<br/>
                             * This signature is the _previous_ ledger ID signing _this_ ledger ID.<br/>
                             * This value MAY be unset, if there is no prior ledger ID.<br/>
                             * This value SHOULD be set if a prior ledger ID exists
                             * to generate the signature.
                             * @property {com.hedera.hapi.node.state.roster.RosterSignatures.$Properties|null} [rosterSignatures] The signatures from nodes in the active roster signing the new
                             * ledger id.<br/>
                             * These signatures establish a chain of trust from the network to the new
                             * ledger id.
                             * <p>
                             * This value MUST be present when the ledger signature of a previous ledger
                             * id is absent.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a LedgerId.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @interface ILedgerId
                             * @augments com.hedera.hapi.node.state.roster.LedgerId.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.roster.LedgerId.$Properties instead.
                             */

                            /**
                             * Shape of a LedgerId.
                             * @typedef {com.hedera.hapi.node.state.roster.LedgerId.$Properties} com.hedera.hapi.node.state.roster.LedgerId.$Shape
                             */

                            /**
                             * Constructs a new LedgerId.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @classdesc A ledger identifier.<br/>
                             * This message identifies a ledger and is used to verify ledger
                             * signatures in a Threshold Signature Scheme (TSS).
                             * 
                             * A ledger identifier SHALL be a public key defined according to the TSS
                             * process.<br/>
                             * A ledger identifier SHOULD NOT change, but MAY do so in rare
                             * circumstances.<br/>
                             * Clients SHOULD always check for the correct ledger identifier, according to
                             * the network roster, before attempting to verify any state proof or other
                             * ledger signature.
                             * 
                             * ### Block Stream Effects
                             * Every block in the Block Stream `BlockProof` SHALL be signed via TSS and
                             * MUST be verified with the ledger identifier current at the _start_ of that
                             * block.
                             * If the ledger identifier changes, the new value MUST be used to validate
                             * Block Proof items after the change.
                             * A change to the ledger identifier SHALL be reported in a State Change for
                             * the block containing that change, which SHALL be verified with the _prior_
                             * ledger identifier.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.roster.LedgerId.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const LedgerId = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * A public key.<br/>
                             * This key both identifies the ledger and can be used to verify ledger
                             * signatures.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * This value MUST contain a valid public key.
                             * @member {Uint8Array} ledgerId
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @instance
                             */
                            LedgerId.prototype.ledgerId = $util.newBuffer([]);

                            /**
                             * A round number.<br/>
                             * This identifies when this ledger id becomes active.<br/>
                             * This value is REQUIRED.
                             * @member {number|Long} round
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @instance
                             */
                            LedgerId.prototype.round = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * A signature from the prior ledger key.<br/>
                             * This signature is the _previous_ ledger ID signing _this_ ledger ID.<br/>
                             * This value MAY be unset, if there is no prior ledger ID.<br/>
                             * This value SHOULD be set if a prior ledger ID exists
                             * to generate the signature.
                             * @member {Uint8Array} ledgerSignature
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @instance
                             */
                            LedgerId.prototype.ledgerSignature = $util.newBuffer([]);

                            /**
                             * The signatures from nodes in the active roster signing the new
                             * ledger id.<br/>
                             * These signatures establish a chain of trust from the network to the new
                             * ledger id.
                             * <p>
                             * This value MUST be present when the ledger signature of a previous ledger
                             * id is absent.
                             * @member {com.hedera.hapi.node.state.roster.RosterSignatures.$Properties|null|undefined} rosterSignatures
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @instance
                             */
                            LedgerId.prototype.rosterSignatures = null;

                            /**
                             * Creates a new LedgerId instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.LedgerId.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.roster.LedgerId} LedgerId instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.roster.LedgerId.$Shape): com.hedera.hapi.node.state.roster.LedgerId & com.hedera.hapi.node.state.roster.LedgerId.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.roster.LedgerId.$Properties): com.hedera.hapi.node.state.roster.LedgerId;
                             * }}
                             */
                            LedgerId.create = function(properties) {
                                return new LedgerId(properties);
                            };

                            /**
                             * Encodes the specified LedgerId message. Does not implicitly {@link com.hedera.hapi.node.state.roster.LedgerId.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.LedgerId.$Properties} message LedgerId message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            LedgerId.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId") && message.ledgerId.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ledgerId);
                                if (message.round != null && $Object.hasOwnProperty.call(message, "round") && (typeof message.round === "object" ? message.round.low || message.round.high : message.round !== 0))
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.round);
                                if (message.ledgerSignature != null && $Object.hasOwnProperty.call(message, "ledgerSignature") && message.ledgerSignature.length)
                                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.ledgerSignature);
                                if (message.rosterSignatures != null && $Object.hasOwnProperty.call(message, "rosterSignatures"))
                                    $root.com.hedera.hapi.node.state.roster.RosterSignatures.encode(message.rosterSignatures, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified LedgerId message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.roster.LedgerId.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.LedgerId.$Properties} message LedgerId message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            LedgerId.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a LedgerId message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.roster.LedgerId & com.hedera.hapi.node.state.roster.LedgerId.$Shape} LedgerId
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            LedgerId.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.roster.LedgerId();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.ledgerId = value;
                                            else
                                                delete message.ledgerId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.round = value;
                                            else
                                                delete message.round;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.ledgerSignature = value;
                                            else
                                                delete message.ledgerSignature;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            message.rosterSignatures = $root.com.hedera.hapi.node.state.roster.RosterSignatures.decode(reader, reader.uint32(), $undefined, _depth + 1, message.rosterSignatures);
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a LedgerId message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.roster.LedgerId & com.hedera.hapi.node.state.roster.LedgerId.$Shape} LedgerId
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            LedgerId.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a LedgerId message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            LedgerId.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                                    if (!(message.ledgerId && typeof message.ledgerId.length === "number" || $util.isString(message.ledgerId)))
                                        return "ledgerId: buffer expected";
                                if (message.round != null && $Object.hasOwnProperty.call(message, "round"))
                                    if (!$util.isInteger(message.round) && !(message.round && $util.isInteger(message.round.low) && $util.isInteger(message.round.high)))
                                        return "round: integer|Long expected";
                                if (message.ledgerSignature != null && $Object.hasOwnProperty.call(message, "ledgerSignature"))
                                    if (!(message.ledgerSignature && typeof message.ledgerSignature.length === "number" || $util.isString(message.ledgerSignature)))
                                        return "ledgerSignature: buffer expected";
                                if (message.rosterSignatures != null && $Object.hasOwnProperty.call(message, "rosterSignatures")) {
                                    let error = $root.com.hedera.hapi.node.state.roster.RosterSignatures.verify(message.rosterSignatures, _depth + 1);
                                    if (error)
                                        return "rosterSignatures." + error;
                                }
                                return null;
                            };

                            /**
                             * Creates a LedgerId message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.roster.LedgerId} LedgerId
                             */
                            LedgerId.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.roster.LedgerId)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.roster.LedgerId: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.roster.LedgerId();
                                if (object.ledgerId != null)
                                    if (object.ledgerId.length)
                                        if (typeof object.ledgerId === "string")
                                            $util.base64.decode(object.ledgerId, message.ledgerId = $util.newBuffer($util.base64.length(object.ledgerId)), 0);
                                        else if (object.ledgerId.length >= 0)
                                            message.ledgerId = object.ledgerId;
                                if (object.round != null)
                                    if (typeof object.round === "object" ? object.round.low || object.round.high : $Number(object.round) !== 0)
                                        if ($util.Long)
                                            message.round = $util.Long.fromValue(object.round, true);
                                        else if (typeof object.round === "string")
                                            message.round = $parseInt(object.round, 10);
                                        else if (typeof object.round === "number")
                                            message.round = object.round;
                                        else if (typeof object.round === "object")
                                            message.round = new $util.LongBits(object.round.low >>> 0, object.round.high >>> 0).toNumber(true);
                                if (object.ledgerSignature != null)
                                    if (object.ledgerSignature.length)
                                        if (typeof object.ledgerSignature === "string")
                                            $util.base64.decode(object.ledgerSignature, message.ledgerSignature = $util.newBuffer($util.base64.length(object.ledgerSignature)), 0);
                                        else if (object.ledgerSignature.length >= 0)
                                            message.ledgerSignature = object.ledgerSignature;
                                if (object.rosterSignatures != null) {
                                    if (!$util.isObject(object.rosterSignatures))
                                        throw $TypeError(".com.hedera.hapi.node.state.roster.LedgerId.rosterSignatures: object expected");
                                    message.rosterSignatures = $root.com.hedera.hapi.node.state.roster.RosterSignatures.fromObject(object.rosterSignatures, _depth + 1);
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a LedgerId message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.LedgerId} message LedgerId
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            LedgerId.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.ledgerId = "";
                                    else {
                                        object.ledgerId = [];
                                        if (options.bytes !== $Array)
                                            object.ledgerId = $util.newBuffer(object.ledgerId);
                                    }
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.round = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.round = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if (options.bytes === $String)
                                        object.ledgerSignature = "";
                                    else {
                                        object.ledgerSignature = [];
                                        if (options.bytes !== $Array)
                                            object.ledgerSignature = $util.newBuffer(object.ledgerSignature);
                                    }
                                    object.rosterSignatures = null;
                                }
                                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                                    object.ledgerId = options.bytes === $String ? $util.base64.encode(message.ledgerId, 0, message.ledgerId.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ledgerId) : message.ledgerId;
                                if (message.round != null && $Object.hasOwnProperty.call(message, "round"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.round = typeof message.round === "number" ? $BigInt(message.round) : $util.Long.fromBits(message.round.low >>> 0, message.round.high >>> 0, true).toBigInt();
                                    else if (typeof message.round === "number")
                                        object.round = options.longs === $String ? $String(message.round) : message.round;
                                    else
                                        object.round = options.longs === $String ? $util.Long.prototype.toString.call(message.round) : options.longs === $Number ? new $util.LongBits(message.round.low >>> 0, message.round.high >>> 0).toNumber(true) : message.round;
                                if (message.ledgerSignature != null && $Object.hasOwnProperty.call(message, "ledgerSignature"))
                                    object.ledgerSignature = options.bytes === $String ? $util.base64.encode(message.ledgerSignature, 0, message.ledgerSignature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ledgerSignature) : message.ledgerSignature;
                                if (message.rosterSignatures != null && $Object.hasOwnProperty.call(message, "rosterSignatures"))
                                    object.rosterSignatures = $root.com.hedera.hapi.node.state.roster.RosterSignatures.toObject(message.rosterSignatures, options, _depth + 1);
                                return object;
                            };

                            /**
                             * Converts this LedgerId to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            LedgerId.prototype.toJSON = function() {
                                return LedgerId.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for LedgerId
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.roster.LedgerId
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            LedgerId.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.roster.LedgerId";
                            };

                            return LedgerId;
                        })();

                        roster.RosterSignatures = (function() {

                            /**
                             * Properties of a RosterSignatures.
                             * @typedef {Object} com.hedera.hapi.node.state.roster.RosterSignatures.$Properties
                             * @property {Uint8Array|null} [rosterHash] A roster hash for the roster that the node signatures are from.
                             * @property {Array.<com.hedera.hapi.node.state.roster.NodeSignature.$Properties>|null} [nodeSignatures] A list of node signatures on the same message where all node ids in the
                             * NodeSignature objects are from the roster that the roster_hash represents.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a RosterSignatures.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @interface IRosterSignatures
                             * @augments com.hedera.hapi.node.state.roster.RosterSignatures.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.roster.RosterSignatures.$Properties instead.
                             */

                            /**
                             * Shape of a RosterSignatures.
                             * @typedef {com.hedera.hapi.node.state.roster.RosterSignatures.$Properties} com.hedera.hapi.node.state.roster.RosterSignatures.$Shape
                             */

                            /**
                             * Constructs a new RosterSignatures.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @classdesc A collection of signatures from nodes in a roster.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.roster.RosterSignatures.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const RosterSignatures = function (properties) {
                                this.nodeSignatures = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * A roster hash for the roster that the node signatures are from.
                             * @member {Uint8Array} rosterHash
                             * @memberof com.hedera.hapi.node.state.roster.RosterSignatures
                             * @instance
                             */
                            RosterSignatures.prototype.rosterHash = $util.newBuffer([]);

                            /**
                             * A list of node signatures on the same message where all node ids in the
                             * NodeSignature objects are from the roster that the roster_hash represents.
                             * @member {Array.<com.hedera.hapi.node.state.roster.NodeSignature.$Properties>} nodeSignatures
                             * @memberof com.hedera.hapi.node.state.roster.RosterSignatures
                             * @instance
                             */
                            RosterSignatures.prototype.nodeSignatures = $util.emptyArray;

                            /**
                             * Creates a new RosterSignatures instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.roster.RosterSignatures
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RosterSignatures.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.roster.RosterSignatures} RosterSignatures instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.roster.RosterSignatures.$Shape): com.hedera.hapi.node.state.roster.RosterSignatures & com.hedera.hapi.node.state.roster.RosterSignatures.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.roster.RosterSignatures.$Properties): com.hedera.hapi.node.state.roster.RosterSignatures;
                             * }}
                             */
                            RosterSignatures.create = function(properties) {
                                return new RosterSignatures(properties);
                            };

                            /**
                             * Encodes the specified RosterSignatures message. Does not implicitly {@link com.hedera.hapi.node.state.roster.RosterSignatures.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.roster.RosterSignatures
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RosterSignatures.$Properties} message RosterSignatures message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RosterSignatures.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.rosterHash != null && $Object.hasOwnProperty.call(message, "rosterHash") && message.rosterHash.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.rosterHash);
                                if (message.nodeSignatures != null && message.nodeSignatures.length)
                                    for (let i = 0; i < message.nodeSignatures.length; ++i)
                                        $root.com.hedera.hapi.node.state.roster.NodeSignature.encode(message.nodeSignatures[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified RosterSignatures message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.roster.RosterSignatures.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.RosterSignatures
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RosterSignatures.$Properties} message RosterSignatures message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RosterSignatures.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a RosterSignatures message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.roster.RosterSignatures
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.roster.RosterSignatures & com.hedera.hapi.node.state.roster.RosterSignatures.$Shape} RosterSignatures
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RosterSignatures.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.roster.RosterSignatures();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.rosterHash = value;
                                            else
                                                delete message.rosterHash;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.nodeSignatures && message.nodeSignatures.length))
                                                message.nodeSignatures = [];
                                            message.nodeSignatures.push($root.com.hedera.hapi.node.state.roster.NodeSignature.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a RosterSignatures message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.RosterSignatures
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.roster.RosterSignatures & com.hedera.hapi.node.state.roster.RosterSignatures.$Shape} RosterSignatures
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RosterSignatures.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a RosterSignatures message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.roster.RosterSignatures
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            RosterSignatures.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.rosterHash != null && $Object.hasOwnProperty.call(message, "rosterHash"))
                                    if (!(message.rosterHash && typeof message.rosterHash.length === "number" || $util.isString(message.rosterHash)))
                                        return "rosterHash: buffer expected";
                                if (message.nodeSignatures != null && $Object.hasOwnProperty.call(message, "nodeSignatures")) {
                                    if (!$Array.isArray(message.nodeSignatures))
                                        return "nodeSignatures: array expected";
                                    for (let i = 0; i < message.nodeSignatures.length; ++i) {
                                        let error = $root.com.hedera.hapi.node.state.roster.NodeSignature.verify(message.nodeSignatures[i], _depth + 1);
                                        if (error)
                                            return "nodeSignatures." + error;
                                    }
                                }
                                return null;
                            };

                            /**
                             * Creates a RosterSignatures message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.roster.RosterSignatures
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.roster.RosterSignatures} RosterSignatures
                             */
                            RosterSignatures.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.roster.RosterSignatures)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.roster.RosterSignatures: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.roster.RosterSignatures();
                                if (object.rosterHash != null)
                                    if (object.rosterHash.length)
                                        if (typeof object.rosterHash === "string")
                                            $util.base64.decode(object.rosterHash, message.rosterHash = $util.newBuffer($util.base64.length(object.rosterHash)), 0);
                                        else if (object.rosterHash.length >= 0)
                                            message.rosterHash = object.rosterHash;
                                if (object.nodeSignatures) {
                                    if (!$Array.isArray(object.nodeSignatures))
                                        throw $TypeError(".com.hedera.hapi.node.state.roster.RosterSignatures.nodeSignatures: array expected");
                                    message.nodeSignatures = $Array(object.nodeSignatures.length);
                                    for (let i = 0; i < object.nodeSignatures.length; ++i) {
                                        if (!$util.isObject(object.nodeSignatures[i]))
                                            throw $TypeError(".com.hedera.hapi.node.state.roster.RosterSignatures.nodeSignatures: object expected");
                                        message.nodeSignatures[i] = $root.com.hedera.hapi.node.state.roster.NodeSignature.fromObject(object.nodeSignatures[i], _depth + 1);
                                    }
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a RosterSignatures message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.roster.RosterSignatures
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RosterSignatures} message RosterSignatures
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            RosterSignatures.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults)
                                    object.nodeSignatures = [];
                                if (options.defaults)
                                    if (options.bytes === $String)
                                        object.rosterHash = "";
                                    else {
                                        object.rosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.rosterHash = $util.newBuffer(object.rosterHash);
                                    }
                                if (message.rosterHash != null && $Object.hasOwnProperty.call(message, "rosterHash"))
                                    object.rosterHash = options.bytes === $String ? $util.base64.encode(message.rosterHash, 0, message.rosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.rosterHash) : message.rosterHash;
                                if (message.nodeSignatures && message.nodeSignatures.length) {
                                    object.nodeSignatures = $Array(message.nodeSignatures.length);
                                    for (let j = 0; j < message.nodeSignatures.length; ++j)
                                        object.nodeSignatures[j] = $root.com.hedera.hapi.node.state.roster.NodeSignature.toObject(message.nodeSignatures[j], options, _depth + 1);
                                }
                                return object;
                            };

                            /**
                             * Converts this RosterSignatures to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.roster.RosterSignatures
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            RosterSignatures.prototype.toJSON = function() {
                                return RosterSignatures.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for RosterSignatures
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.roster.RosterSignatures
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            RosterSignatures.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.roster.RosterSignatures";
                            };

                            return RosterSignatures;
                        })();

                        roster.NodeSignature = (function() {

                            /**
                             * Properties of a NodeSignature.
                             * @typedef {Object} com.hedera.hapi.node.state.roster.NodeSignature.$Properties
                             * @property {number|Long|null} [nodeId] The node id of the node that created the _RSA_ signature.
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * This value is REQUIRED.
                             * @property {Uint8Array|null} [nodeSignature] The bytes of an _RSA_ signature.
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * This value MUST contain a valid signature.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a NodeSignature.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @interface INodeSignature
                             * @augments com.hedera.hapi.node.state.roster.NodeSignature.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.roster.NodeSignature.$Properties instead.
                             */

                            /**
                             * Shape of a NodeSignature.
                             * @typedef {com.hedera.hapi.node.state.roster.NodeSignature.$Properties} com.hedera.hapi.node.state.roster.NodeSignature.$Shape
                             */

                            /**
                             * Constructs a new NodeSignature.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @classdesc A pair of a _RSA_ signature and the node id of the node that created the
                             * signature.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.roster.NodeSignature.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const NodeSignature = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The node id of the node that created the _RSA_ signature.
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * This value is REQUIRED.
                             * @member {number|Long} nodeId
                             * @memberof com.hedera.hapi.node.state.roster.NodeSignature
                             * @instance
                             */
                            NodeSignature.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The bytes of an _RSA_ signature.
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * This value MUST contain a valid signature.
                             * @member {Uint8Array} nodeSignature
                             * @memberof com.hedera.hapi.node.state.roster.NodeSignature
                             * @instance
                             */
                            NodeSignature.prototype.nodeSignature = $util.newBuffer([]);

                            /**
                             * Creates a new NodeSignature instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.roster.NodeSignature
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.NodeSignature.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.roster.NodeSignature} NodeSignature instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.roster.NodeSignature.$Shape): com.hedera.hapi.node.state.roster.NodeSignature & com.hedera.hapi.node.state.roster.NodeSignature.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.roster.NodeSignature.$Properties): com.hedera.hapi.node.state.roster.NodeSignature;
                             * }}
                             */
                            NodeSignature.create = function(properties) {
                                return new NodeSignature(properties);
                            };

                            /**
                             * Encodes the specified NodeSignature message. Does not implicitly {@link com.hedera.hapi.node.state.roster.NodeSignature.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.roster.NodeSignature
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.NodeSignature.$Properties} message NodeSignature message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            NodeSignature.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.nodeId);
                                if (message.nodeSignature != null && $Object.hasOwnProperty.call(message, "nodeSignature") && message.nodeSignature.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.nodeSignature);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified NodeSignature message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.roster.NodeSignature.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.NodeSignature
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.NodeSignature.$Properties} message NodeSignature message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            NodeSignature.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a NodeSignature message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.roster.NodeSignature
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.roster.NodeSignature & com.hedera.hapi.node.state.roster.NodeSignature.$Shape} NodeSignature
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            NodeSignature.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.roster.NodeSignature();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.nodeId = value;
                                            else
                                                delete message.nodeId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.nodeSignature = value;
                                            else
                                                delete message.nodeSignature;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a NodeSignature message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.NodeSignature
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.roster.NodeSignature & com.hedera.hapi.node.state.roster.NodeSignature.$Shape} NodeSignature
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            NodeSignature.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a NodeSignature message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.roster.NodeSignature
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            NodeSignature.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                        return "nodeId: integer|Long expected";
                                if (message.nodeSignature != null && $Object.hasOwnProperty.call(message, "nodeSignature"))
                                    if (!(message.nodeSignature && typeof message.nodeSignature.length === "number" || $util.isString(message.nodeSignature)))
                                        return "nodeSignature: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a NodeSignature message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.roster.NodeSignature
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.roster.NodeSignature} NodeSignature
                             */
                            NodeSignature.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.roster.NodeSignature)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.roster.NodeSignature: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.roster.NodeSignature();
                                if (object.nodeId != null)
                                    if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                        if ($util.Long)
                                            message.nodeId = $util.Long.fromValue(object.nodeId, true);
                                        else if (typeof object.nodeId === "string")
                                            message.nodeId = $parseInt(object.nodeId, 10);
                                        else if (typeof object.nodeId === "number")
                                            message.nodeId = object.nodeId;
                                        else if (typeof object.nodeId === "object")
                                            message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
                                if (object.nodeSignature != null)
                                    if (object.nodeSignature.length)
                                        if (typeof object.nodeSignature === "string")
                                            $util.base64.decode(object.nodeSignature, message.nodeSignature = $util.newBuffer($util.base64.length(object.nodeSignature)), 0);
                                        else if (object.nodeSignature.length >= 0)
                                            message.nodeSignature = object.nodeSignature;
                                return message;
                            };

                            /**
                             * Creates a plain object from a NodeSignature message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.roster.NodeSignature
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.NodeSignature} message NodeSignature
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            NodeSignature.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if (options.bytes === $String)
                                        object.nodeSignature = "";
                                    else {
                                        object.nodeSignature = [];
                                        if (options.bytes !== $Array)
                                            object.nodeSignature = $util.newBuffer(object.nodeSignature);
                                    }
                                }
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                                    else if (typeof message.nodeId === "number")
                                        object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                    else
                                        object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
                                if (message.nodeSignature != null && $Object.hasOwnProperty.call(message, "nodeSignature"))
                                    object.nodeSignature = options.bytes === $String ? $util.base64.encode(message.nodeSignature, 0, message.nodeSignature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.nodeSignature) : message.nodeSignature;
                                return object;
                            };

                            /**
                             * Converts this NodeSignature to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.roster.NodeSignature
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            NodeSignature.prototype.toJSON = function() {
                                return NodeSignature.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for NodeSignature
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.roster.NodeSignature
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            NodeSignature.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.roster.NodeSignature";
                            };

                            return NodeSignature;
                        })();

                        roster.Roster = (function() {

                            /**
                             * Properties of a Roster.
                             * @typedef {Object} com.hedera.hapi.node.state.roster.Roster.$Properties
                             * @property {Array.<com.hedera.hapi.node.state.roster.RosterEntry.$Properties>|null} [rosterEntries] List of roster entries, one per consensus node.
                             * <p>
                             * This list SHALL contain roster entries in natural order of ascending node ids.
                             * This list SHALL NOT be empty.<br/>
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a Roster.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @interface IRoster
                             * @augments com.hedera.hapi.node.state.roster.Roster.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.roster.Roster.$Properties instead.
                             */

                            /**
                             * Shape of a Roster.
                             * @typedef {com.hedera.hapi.node.state.roster.Roster.$Properties} com.hedera.hapi.node.state.roster.Roster.$Shape
                             */

                            /**
                             * Constructs a new Roster.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @classdesc A single roster in the network state.
                             * <p>
                             * The roster SHALL be a list of `RosterEntry` objects.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.roster.Roster.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const Roster = function (properties) {
                                this.rosterEntries = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * List of roster entries, one per consensus node.
                             * <p>
                             * This list SHALL contain roster entries in natural order of ascending node ids.
                             * This list SHALL NOT be empty.<br/>
                             * @member {Array.<com.hedera.hapi.node.state.roster.RosterEntry.$Properties>} rosterEntries
                             * @memberof com.hedera.hapi.node.state.roster.Roster
                             * @instance
                             */
                            Roster.prototype.rosterEntries = $util.emptyArray;

                            /**
                             * Creates a new Roster instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.roster.Roster
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.Roster.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.roster.Roster} Roster instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.roster.Roster.$Shape): com.hedera.hapi.node.state.roster.Roster & com.hedera.hapi.node.state.roster.Roster.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.roster.Roster.$Properties): com.hedera.hapi.node.state.roster.Roster;
                             * }}
                             */
                            Roster.create = function(properties) {
                                return new Roster(properties);
                            };

                            /**
                             * Encodes the specified Roster message. Does not implicitly {@link com.hedera.hapi.node.state.roster.Roster.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.roster.Roster
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.Roster.$Properties} message Roster message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            Roster.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.rosterEntries != null && message.rosterEntries.length)
                                    for (let i = 0; i < message.rosterEntries.length; ++i)
                                        $root.com.hedera.hapi.node.state.roster.RosterEntry.encode(message.rosterEntries[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified Roster message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.roster.Roster.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.Roster
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.Roster.$Properties} message Roster message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            Roster.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a Roster message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.roster.Roster
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.roster.Roster & com.hedera.hapi.node.state.roster.Roster.$Shape} Roster
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            Roster.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.roster.Roster();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.rosterEntries && message.rosterEntries.length))
                                                message.rosterEntries = [];
                                            message.rosterEntries.push($root.com.hedera.hapi.node.state.roster.RosterEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a Roster message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.Roster
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.roster.Roster & com.hedera.hapi.node.state.roster.Roster.$Shape} Roster
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            Roster.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a Roster message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.roster.Roster
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            Roster.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.rosterEntries != null && $Object.hasOwnProperty.call(message, "rosterEntries")) {
                                    if (!$Array.isArray(message.rosterEntries))
                                        return "rosterEntries: array expected";
                                    for (let i = 0; i < message.rosterEntries.length; ++i) {
                                        let error = $root.com.hedera.hapi.node.state.roster.RosterEntry.verify(message.rosterEntries[i], _depth + 1);
                                        if (error)
                                            return "rosterEntries." + error;
                                    }
                                }
                                return null;
                            };

                            /**
                             * Creates a Roster message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.roster.Roster
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.roster.Roster} Roster
                             */
                            Roster.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.roster.Roster)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.roster.Roster: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.roster.Roster();
                                if (object.rosterEntries) {
                                    if (!$Array.isArray(object.rosterEntries))
                                        throw $TypeError(".com.hedera.hapi.node.state.roster.Roster.rosterEntries: array expected");
                                    message.rosterEntries = $Array(object.rosterEntries.length);
                                    for (let i = 0; i < object.rosterEntries.length; ++i) {
                                        if (!$util.isObject(object.rosterEntries[i]))
                                            throw $TypeError(".com.hedera.hapi.node.state.roster.Roster.rosterEntries: object expected");
                                        message.rosterEntries[i] = $root.com.hedera.hapi.node.state.roster.RosterEntry.fromObject(object.rosterEntries[i], _depth + 1);
                                    }
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a Roster message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.roster.Roster
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.Roster} message Roster
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            Roster.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults)
                                    object.rosterEntries = [];
                                if (message.rosterEntries && message.rosterEntries.length) {
                                    object.rosterEntries = $Array(message.rosterEntries.length);
                                    for (let j = 0; j < message.rosterEntries.length; ++j)
                                        object.rosterEntries[j] = $root.com.hedera.hapi.node.state.roster.RosterEntry.toObject(message.rosterEntries[j], options, _depth + 1);
                                }
                                return object;
                            };

                            /**
                             * Converts this Roster to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.roster.Roster
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            Roster.prototype.toJSON = function() {
                                return Roster.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for Roster
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.roster.Roster
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            Roster.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.roster.Roster";
                            };

                            return Roster;
                        })();

                        roster.RosterEntry = (function() {

                            /**
                             * Properties of a RosterEntry.
                             * @typedef {Object} com.hedera.hapi.node.state.roster.RosterEntry.$Properties
                             * @property {number|Long|null} [nodeId] A consensus node identifier.
                             * <p>
                             * Node identifiers SHALL be unique _within_ a ledger,
                             * and MUST NOT be repeated _between_ shards and realms.
                             * @property {number|Long|null} [weight] A consensus weight.
                             * <p>
                             * Each node SHALL have a weight of zero or more in consensus calculations.<br/>
                             * The sum of the weights of all nodes in the roster SHALL form the total weight of the system,
                             * and each node's individual weight SHALL be proportional to that sum.<br/>
                             * @property {Uint8Array|null} [gossipCaCertificate] An RSA public certificate used for signing gossip events.
                             * <p>
                             * This value SHALL be a certificate of a type permitted for gossip
                             * signatures.<br/>
                             * This value SHALL be the DER encoding of the certificate presented.<br/>
                             * This field is REQUIRED and MUST NOT be empty.
                             * @property {Array.<proto.ServiceEndpoint.$Properties>|null} [gossipEndpoint] A list of service endpoints for gossip.
                             * <p>
                             * These endpoints SHALL represent the published endpoints to which other
                             * consensus nodes may _gossip_ transactions.<br/>
                             * If the network configuration value `gossipFqdnRestricted` is set, then
                             * all endpoints in this list SHALL supply only IP address.<br/>
                             * If the network configuration value `gossipFqdnRestricted` is _not_ set,
                             * then endpoints in this list MAY supply either IP address or FQDN, but
                             * SHALL NOT supply both values for the same endpoint.<br/>
                             * This list SHALL NOT be empty.<br/>
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a RosterEntry.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @interface IRosterEntry
                             * @augments com.hedera.hapi.node.state.roster.RosterEntry.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.roster.RosterEntry.$Properties instead.
                             */

                            /**
                             * Shape of a RosterEntry.
                             * @typedef {com.hedera.hapi.node.state.roster.RosterEntry.$Properties} com.hedera.hapi.node.state.roster.RosterEntry.$Shape
                             */

                            /**
                             * Constructs a new RosterEntry.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @classdesc A single roster entry in the network state.
                             * 
                             * Each roster entry SHALL encapsulate the elements required
                             * to manage node participation in the Threshold Signature Scheme (TSS).<br/>
                             * All fields are REQUIRED.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.roster.RosterEntry.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const RosterEntry = function (properties) {
                                this.gossipEndpoint = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * A consensus node identifier.
                             * <p>
                             * Node identifiers SHALL be unique _within_ a ledger,
                             * and MUST NOT be repeated _between_ shards and realms.
                             * @member {number|Long} nodeId
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @instance
                             */
                            RosterEntry.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * A consensus weight.
                             * <p>
                             * Each node SHALL have a weight of zero or more in consensus calculations.<br/>
                             * The sum of the weights of all nodes in the roster SHALL form the total weight of the system,
                             * and each node's individual weight SHALL be proportional to that sum.<br/>
                             * @member {number|Long} weight
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @instance
                             */
                            RosterEntry.prototype.weight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * An RSA public certificate used for signing gossip events.
                             * <p>
                             * This value SHALL be a certificate of a type permitted for gossip
                             * signatures.<br/>
                             * This value SHALL be the DER encoding of the certificate presented.<br/>
                             * This field is REQUIRED and MUST NOT be empty.
                             * @member {Uint8Array} gossipCaCertificate
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @instance
                             */
                            RosterEntry.prototype.gossipCaCertificate = $util.newBuffer([]);

                            /**
                             * A list of service endpoints for gossip.
                             * <p>
                             * These endpoints SHALL represent the published endpoints to which other
                             * consensus nodes may _gossip_ transactions.<br/>
                             * If the network configuration value `gossipFqdnRestricted` is set, then
                             * all endpoints in this list SHALL supply only IP address.<br/>
                             * If the network configuration value `gossipFqdnRestricted` is _not_ set,
                             * then endpoints in this list MAY supply either IP address or FQDN, but
                             * SHALL NOT supply both values for the same endpoint.<br/>
                             * This list SHALL NOT be empty.<br/>
                             * @member {Array.<proto.ServiceEndpoint.$Properties>} gossipEndpoint
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @instance
                             */
                            RosterEntry.prototype.gossipEndpoint = $util.emptyArray;

                            /**
                             * Creates a new RosterEntry instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RosterEntry.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.roster.RosterEntry} RosterEntry instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.roster.RosterEntry.$Shape): com.hedera.hapi.node.state.roster.RosterEntry & com.hedera.hapi.node.state.roster.RosterEntry.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.roster.RosterEntry.$Properties): com.hedera.hapi.node.state.roster.RosterEntry;
                             * }}
                             */
                            RosterEntry.create = function(properties) {
                                return new RosterEntry(properties);
                            };

                            /**
                             * Encodes the specified RosterEntry message. Does not implicitly {@link com.hedera.hapi.node.state.roster.RosterEntry.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RosterEntry.$Properties} message RosterEntry message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RosterEntry.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.nodeId);
                                if (message.weight != null && $Object.hasOwnProperty.call(message, "weight") && (typeof message.weight === "object" ? message.weight.low || message.weight.high : message.weight !== 0))
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.weight);
                                if (message.gossipCaCertificate != null && $Object.hasOwnProperty.call(message, "gossipCaCertificate") && message.gossipCaCertificate.length)
                                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.gossipCaCertificate);
                                if (message.gossipEndpoint != null && message.gossipEndpoint.length)
                                    for (let i = 0; i < message.gossipEndpoint.length; ++i)
                                        $root.proto.ServiceEndpoint.encode(message.gossipEndpoint[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified RosterEntry message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.roster.RosterEntry.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RosterEntry.$Properties} message RosterEntry message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RosterEntry.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a RosterEntry message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.roster.RosterEntry & com.hedera.hapi.node.state.roster.RosterEntry.$Shape} RosterEntry
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RosterEntry.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.roster.RosterEntry();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.nodeId = value;
                                            else
                                                delete message.nodeId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.weight = value;
                                            else
                                                delete message.weight;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.gossipCaCertificate = value;
                                            else
                                                delete message.gossipCaCertificate;
                                            continue;
                                        }
                                    case 5: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.gossipEndpoint && message.gossipEndpoint.length))
                                                message.gossipEndpoint = [];
                                            message.gossipEndpoint.push($root.proto.ServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a RosterEntry message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.roster.RosterEntry & com.hedera.hapi.node.state.roster.RosterEntry.$Shape} RosterEntry
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RosterEntry.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a RosterEntry message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            RosterEntry.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                        return "nodeId: integer|Long expected";
                                if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                                    if (!$util.isInteger(message.weight) && !(message.weight && $util.isInteger(message.weight.low) && $util.isInteger(message.weight.high)))
                                        return "weight: integer|Long expected";
                                if (message.gossipCaCertificate != null && $Object.hasOwnProperty.call(message, "gossipCaCertificate"))
                                    if (!(message.gossipCaCertificate && typeof message.gossipCaCertificate.length === "number" || $util.isString(message.gossipCaCertificate)))
                                        return "gossipCaCertificate: buffer expected";
                                if (message.gossipEndpoint != null && $Object.hasOwnProperty.call(message, "gossipEndpoint")) {
                                    if (!$Array.isArray(message.gossipEndpoint))
                                        return "gossipEndpoint: array expected";
                                    for (let i = 0; i < message.gossipEndpoint.length; ++i) {
                                        let error = $root.proto.ServiceEndpoint.verify(message.gossipEndpoint[i], _depth + 1);
                                        if (error)
                                            return "gossipEndpoint." + error;
                                    }
                                }
                                return null;
                            };

                            /**
                             * Creates a RosterEntry message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.roster.RosterEntry} RosterEntry
                             */
                            RosterEntry.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.roster.RosterEntry)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.roster.RosterEntry: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.roster.RosterEntry();
                                if (object.nodeId != null)
                                    if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                        if ($util.Long)
                                            message.nodeId = $util.Long.fromValue(object.nodeId, true);
                                        else if (typeof object.nodeId === "string")
                                            message.nodeId = $parseInt(object.nodeId, 10);
                                        else if (typeof object.nodeId === "number")
                                            message.nodeId = object.nodeId;
                                        else if (typeof object.nodeId === "object")
                                            message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
                                if (object.weight != null)
                                    if (typeof object.weight === "object" ? object.weight.low || object.weight.high : $Number(object.weight) !== 0)
                                        if ($util.Long)
                                            message.weight = $util.Long.fromValue(object.weight, true);
                                        else if (typeof object.weight === "string")
                                            message.weight = $parseInt(object.weight, 10);
                                        else if (typeof object.weight === "number")
                                            message.weight = object.weight;
                                        else if (typeof object.weight === "object")
                                            message.weight = new $util.LongBits(object.weight.low >>> 0, object.weight.high >>> 0).toNumber(true);
                                if (object.gossipCaCertificate != null)
                                    if (object.gossipCaCertificate.length)
                                        if (typeof object.gossipCaCertificate === "string")
                                            $util.base64.decode(object.gossipCaCertificate, message.gossipCaCertificate = $util.newBuffer($util.base64.length(object.gossipCaCertificate)), 0);
                                        else if (object.gossipCaCertificate.length >= 0)
                                            message.gossipCaCertificate = object.gossipCaCertificate;
                                if (object.gossipEndpoint) {
                                    if (!$Array.isArray(object.gossipEndpoint))
                                        throw $TypeError(".com.hedera.hapi.node.state.roster.RosterEntry.gossipEndpoint: array expected");
                                    message.gossipEndpoint = $Array(object.gossipEndpoint.length);
                                    for (let i = 0; i < object.gossipEndpoint.length; ++i) {
                                        if (!$util.isObject(object.gossipEndpoint[i]))
                                            throw $TypeError(".com.hedera.hapi.node.state.roster.RosterEntry.gossipEndpoint: object expected");
                                        message.gossipEndpoint[i] = $root.proto.ServiceEndpoint.fromObject(object.gossipEndpoint[i], _depth + 1);
                                    }
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a RosterEntry message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RosterEntry} message RosterEntry
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            RosterEntry.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults)
                                    object.gossipEndpoint = [];
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.weight = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.weight = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if (options.bytes === $String)
                                        object.gossipCaCertificate = "";
                                    else {
                                        object.gossipCaCertificate = [];
                                        if (options.bytes !== $Array)
                                            object.gossipCaCertificate = $util.newBuffer(object.gossipCaCertificate);
                                    }
                                }
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                                    else if (typeof message.nodeId === "number")
                                        object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                    else
                                        object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
                                if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.weight = typeof message.weight === "number" ? $BigInt(message.weight) : $util.Long.fromBits(message.weight.low >>> 0, message.weight.high >>> 0, true).toBigInt();
                                    else if (typeof message.weight === "number")
                                        object.weight = options.longs === $String ? $String(message.weight) : message.weight;
                                    else
                                        object.weight = options.longs === $String ? $util.Long.prototype.toString.call(message.weight) : options.longs === $Number ? new $util.LongBits(message.weight.low >>> 0, message.weight.high >>> 0).toNumber(true) : message.weight;
                                if (message.gossipCaCertificate != null && $Object.hasOwnProperty.call(message, "gossipCaCertificate"))
                                    object.gossipCaCertificate = options.bytes === $String ? $util.base64.encode(message.gossipCaCertificate, 0, message.gossipCaCertificate.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.gossipCaCertificate) : message.gossipCaCertificate;
                                if (message.gossipEndpoint && message.gossipEndpoint.length) {
                                    object.gossipEndpoint = $Array(message.gossipEndpoint.length);
                                    for (let j = 0; j < message.gossipEndpoint.length; ++j)
                                        object.gossipEndpoint[j] = $root.proto.ServiceEndpoint.toObject(message.gossipEndpoint[j], options, _depth + 1);
                                }
                                return object;
                            };

                            /**
                             * Converts this RosterEntry to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            RosterEntry.prototype.toJSON = function() {
                                return RosterEntry.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for RosterEntry
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.roster.RosterEntry
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            RosterEntry.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.roster.RosterEntry";
                            };

                            return RosterEntry;
                        })();

                        roster.RosterState = (function() {

                            /**
                             * Properties of a RosterState.
                             * @typedef {Object} com.hedera.hapi.node.state.roster.RosterState.$Properties
                             * @property {Uint8Array|null} [candidateRosterHash] The SHA-384 hash of a candidate roster.
                             * <p>
                             * This is the hash of the roster that is currently being considered
                             * for adoption.<br/>
                             * A Node SHALL NOT, ever, have more than one candidate roster
                             * at the same time.
                             * @property {Array.<com.hedera.hapi.node.state.roster.RoundRosterPair.$Properties>|null} [roundRosterPairs] A list of round numbers and roster hashes.<br/>
                             * The round number indicates the round in which the corresponding roster became active
                             * <p>
                             * This list SHALL be ordered by round numbers in descending order.
                             * @property {boolean|null} [transplantInProgress] Indicates whether the roster transplant is in progress.<br/>
                             * This is needed to determine if all the node creations and deletions are complete.
                             * True while a transplant roster is still being processed (nodes are
                             * being created/updated/deleted).  Reset to false once dispatch is finished.
                             * 
                             * <p>
                             * A transplant roster is one that has been adopted from another network
                             * using an override startup asset, rather than being generated by the current network's consensus.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a RosterState.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @interface IRosterState
                             * @augments com.hedera.hapi.node.state.roster.RosterState.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.roster.RosterState.$Properties instead.
                             */

                            /**
                             * Shape of a RosterState.
                             * @typedef {com.hedera.hapi.node.state.roster.RosterState.$Properties} com.hedera.hapi.node.state.roster.RosterState.$Shape
                             */

                            /**
                             * Constructs a new RosterState.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @classdesc The current state of platform rosters.<br/>
                             * This message stores a roster data for the platform in network state.
                             * 
                             * The roster state SHALL encapsulate the incoming candidate roster's hash,
                             * and a list of pairs of round number and active roster hash.<br/>
                             * This data SHALL be used to track round numbers and the rosters used in determining the consensus.<br/>
                             * @constructor
                             * @param {com.hedera.hapi.node.state.roster.RosterState.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const RosterState = function (properties) {
                                this.roundRosterPairs = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The SHA-384 hash of a candidate roster.
                             * <p>
                             * This is the hash of the roster that is currently being considered
                             * for adoption.<br/>
                             * A Node SHALL NOT, ever, have more than one candidate roster
                             * at the same time.
                             * @member {Uint8Array} candidateRosterHash
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @instance
                             */
                            RosterState.prototype.candidateRosterHash = $util.newBuffer([]);

                            /**
                             * A list of round numbers and roster hashes.<br/>
                             * The round number indicates the round in which the corresponding roster became active
                             * <p>
                             * This list SHALL be ordered by round numbers in descending order.
                             * @member {Array.<com.hedera.hapi.node.state.roster.RoundRosterPair.$Properties>} roundRosterPairs
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @instance
                             */
                            RosterState.prototype.roundRosterPairs = $util.emptyArray;

                            /**
                             * Indicates whether the roster transplant is in progress.<br/>
                             * This is needed to determine if all the node creations and deletions are complete.
                             * True while a transplant roster is still being processed (nodes are
                             * being created/updated/deleted).  Reset to false once dispatch is finished.
                             * 
                             * <p>
                             * A transplant roster is one that has been adopted from another network
                             * using an override startup asset, rather than being generated by the current network's consensus.
                             * @member {boolean} transplantInProgress
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @instance
                             */
                            RosterState.prototype.transplantInProgress = false;

                            /**
                             * Creates a new RosterState instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RosterState.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.roster.RosterState} RosterState instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.roster.RosterState.$Shape): com.hedera.hapi.node.state.roster.RosterState & com.hedera.hapi.node.state.roster.RosterState.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.roster.RosterState.$Properties): com.hedera.hapi.node.state.roster.RosterState;
                             * }}
                             */
                            RosterState.create = function(properties) {
                                return new RosterState(properties);
                            };

                            /**
                             * Encodes the specified RosterState message. Does not implicitly {@link com.hedera.hapi.node.state.roster.RosterState.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RosterState.$Properties} message RosterState message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RosterState.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.candidateRosterHash != null && $Object.hasOwnProperty.call(message, "candidateRosterHash") && message.candidateRosterHash.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.candidateRosterHash);
                                if (message.roundRosterPairs != null && message.roundRosterPairs.length)
                                    for (let i = 0; i < message.roundRosterPairs.length; ++i)
                                        $root.com.hedera.hapi.node.state.roster.RoundRosterPair.encode(message.roundRosterPairs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.transplantInProgress != null && $Object.hasOwnProperty.call(message, "transplantInProgress") && message.transplantInProgress !== false)
                                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.transplantInProgress);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified RosterState message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.roster.RosterState.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RosterState.$Properties} message RosterState message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RosterState.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a RosterState message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.roster.RosterState & com.hedera.hapi.node.state.roster.RosterState.$Shape} RosterState
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RosterState.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.roster.RosterState();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.candidateRosterHash = value;
                                            else
                                                delete message.candidateRosterHash;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.roundRosterPairs && message.roundRosterPairs.length))
                                                message.roundRosterPairs = [];
                                            message.roundRosterPairs.push($root.com.hedera.hapi.node.state.roster.RoundRosterPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.bool())
                                                message.transplantInProgress = value;
                                            else
                                                delete message.transplantInProgress;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a RosterState message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.roster.RosterState & com.hedera.hapi.node.state.roster.RosterState.$Shape} RosterState
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RosterState.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a RosterState message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            RosterState.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.candidateRosterHash != null && $Object.hasOwnProperty.call(message, "candidateRosterHash"))
                                    if (!(message.candidateRosterHash && typeof message.candidateRosterHash.length === "number" || $util.isString(message.candidateRosterHash)))
                                        return "candidateRosterHash: buffer expected";
                                if (message.roundRosterPairs != null && $Object.hasOwnProperty.call(message, "roundRosterPairs")) {
                                    if (!$Array.isArray(message.roundRosterPairs))
                                        return "roundRosterPairs: array expected";
                                    for (let i = 0; i < message.roundRosterPairs.length; ++i) {
                                        let error = $root.com.hedera.hapi.node.state.roster.RoundRosterPair.verify(message.roundRosterPairs[i], _depth + 1);
                                        if (error)
                                            return "roundRosterPairs." + error;
                                    }
                                }
                                if (message.transplantInProgress != null && $Object.hasOwnProperty.call(message, "transplantInProgress"))
                                    if (typeof message.transplantInProgress !== "boolean")
                                        return "transplantInProgress: boolean expected";
                                return null;
                            };

                            /**
                             * Creates a RosterState message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.roster.RosterState} RosterState
                             */
                            RosterState.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.roster.RosterState)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.roster.RosterState: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.roster.RosterState();
                                if (object.candidateRosterHash != null)
                                    if (object.candidateRosterHash.length)
                                        if (typeof object.candidateRosterHash === "string")
                                            $util.base64.decode(object.candidateRosterHash, message.candidateRosterHash = $util.newBuffer($util.base64.length(object.candidateRosterHash)), 0);
                                        else if (object.candidateRosterHash.length >= 0)
                                            message.candidateRosterHash = object.candidateRosterHash;
                                if (object.roundRosterPairs) {
                                    if (!$Array.isArray(object.roundRosterPairs))
                                        throw $TypeError(".com.hedera.hapi.node.state.roster.RosterState.roundRosterPairs: array expected");
                                    message.roundRosterPairs = $Array(object.roundRosterPairs.length);
                                    for (let i = 0; i < object.roundRosterPairs.length; ++i) {
                                        if (!$util.isObject(object.roundRosterPairs[i]))
                                            throw $TypeError(".com.hedera.hapi.node.state.roster.RosterState.roundRosterPairs: object expected");
                                        message.roundRosterPairs[i] = $root.com.hedera.hapi.node.state.roster.RoundRosterPair.fromObject(object.roundRosterPairs[i], _depth + 1);
                                    }
                                }
                                if (object.transplantInProgress != null)
                                    if (object.transplantInProgress)
                                        message.transplantInProgress = $Boolean(object.transplantInProgress);
                                return message;
                            };

                            /**
                             * Creates a plain object from a RosterState message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RosterState} message RosterState
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            RosterState.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults)
                                    object.roundRosterPairs = [];
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.candidateRosterHash = "";
                                    else {
                                        object.candidateRosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.candidateRosterHash = $util.newBuffer(object.candidateRosterHash);
                                    }
                                    object.transplantInProgress = false;
                                }
                                if (message.candidateRosterHash != null && $Object.hasOwnProperty.call(message, "candidateRosterHash"))
                                    object.candidateRosterHash = options.bytes === $String ? $util.base64.encode(message.candidateRosterHash, 0, message.candidateRosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.candidateRosterHash) : message.candidateRosterHash;
                                if (message.roundRosterPairs && message.roundRosterPairs.length) {
                                    object.roundRosterPairs = $Array(message.roundRosterPairs.length);
                                    for (let j = 0; j < message.roundRosterPairs.length; ++j)
                                        object.roundRosterPairs[j] = $root.com.hedera.hapi.node.state.roster.RoundRosterPair.toObject(message.roundRosterPairs[j], options, _depth + 1);
                                }
                                if (message.transplantInProgress != null && $Object.hasOwnProperty.call(message, "transplantInProgress"))
                                    object.transplantInProgress = message.transplantInProgress;
                                return object;
                            };

                            /**
                             * Converts this RosterState to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            RosterState.prototype.toJSON = function() {
                                return RosterState.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for RosterState
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.roster.RosterState
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            RosterState.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.roster.RosterState";
                            };

                            return RosterState;
                        })();

                        roster.RoundRosterPair = (function() {

                            /**
                             * Properties of a RoundRosterPair.
                             * @typedef {Object} com.hedera.hapi.node.state.roster.RoundRosterPair.$Properties
                             * @property {number|Long|null} [roundNumber] The round number.
                             * <p>
                             * This value SHALL be the round number of the consensus round in which this roster became active.
                             * @property {Uint8Array|null} [activeRosterHash] The SHA-384 hash of the active roster for the given round number.
                             * <p>
                             * This value SHALL be the hash of the active roster used for the round.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a RoundRosterPair.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @interface IRoundRosterPair
                             * @augments com.hedera.hapi.node.state.roster.RoundRosterPair.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.roster.RoundRosterPair.$Properties instead.
                             */

                            /**
                             * Shape of a RoundRosterPair.
                             * @typedef {com.hedera.hapi.node.state.roster.RoundRosterPair.$Properties} com.hedera.hapi.node.state.roster.RoundRosterPair.$Shape
                             */

                            /**
                             * Constructs a new RoundRosterPair.
                             * @memberof com.hedera.hapi.node.state.roster
                             * @classdesc A pair of round number and active roster hash.
                             * <p>
                             * This message SHALL encapsulate the round number and the hash of the
                             * active roster used for that round.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.roster.RoundRosterPair.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const RoundRosterPair = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The round number.
                             * <p>
                             * This value SHALL be the round number of the consensus round in which this roster became active.
                             * @member {number|Long} roundNumber
                             * @memberof com.hedera.hapi.node.state.roster.RoundRosterPair
                             * @instance
                             */
                            RoundRosterPair.prototype.roundNumber = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The SHA-384 hash of the active roster for the given round number.
                             * <p>
                             * This value SHALL be the hash of the active roster used for the round.
                             * @member {Uint8Array} activeRosterHash
                             * @memberof com.hedera.hapi.node.state.roster.RoundRosterPair
                             * @instance
                             */
                            RoundRosterPair.prototype.activeRosterHash = $util.newBuffer([]);

                            /**
                             * Creates a new RoundRosterPair instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.roster.RoundRosterPair
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RoundRosterPair.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.roster.RoundRosterPair} RoundRosterPair instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.roster.RoundRosterPair.$Shape): com.hedera.hapi.node.state.roster.RoundRosterPair & com.hedera.hapi.node.state.roster.RoundRosterPair.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.roster.RoundRosterPair.$Properties): com.hedera.hapi.node.state.roster.RoundRosterPair;
                             * }}
                             */
                            RoundRosterPair.create = function(properties) {
                                return new RoundRosterPair(properties);
                            };

                            /**
                             * Encodes the specified RoundRosterPair message. Does not implicitly {@link com.hedera.hapi.node.state.roster.RoundRosterPair.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.roster.RoundRosterPair
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RoundRosterPair.$Properties} message RoundRosterPair message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RoundRosterPair.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.roundNumber != null && $Object.hasOwnProperty.call(message, "roundNumber") && (typeof message.roundNumber === "object" ? message.roundNumber.low || message.roundNumber.high : message.roundNumber !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.roundNumber);
                                if (message.activeRosterHash != null && $Object.hasOwnProperty.call(message, "activeRosterHash") && message.activeRosterHash.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.activeRosterHash);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified RoundRosterPair message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.roster.RoundRosterPair.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.RoundRosterPair
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RoundRosterPair.$Properties} message RoundRosterPair message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            RoundRosterPair.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a RoundRosterPair message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.roster.RoundRosterPair
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.roster.RoundRosterPair & com.hedera.hapi.node.state.roster.RoundRosterPair.$Shape} RoundRosterPair
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RoundRosterPair.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.roster.RoundRosterPair();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.roundNumber = value;
                                            else
                                                delete message.roundNumber;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.activeRosterHash = value;
                                            else
                                                delete message.activeRosterHash;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a RoundRosterPair message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.roster.RoundRosterPair
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.roster.RoundRosterPair & com.hedera.hapi.node.state.roster.RoundRosterPair.$Shape} RoundRosterPair
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            RoundRosterPair.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a RoundRosterPair message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.roster.RoundRosterPair
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            RoundRosterPair.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.roundNumber != null && $Object.hasOwnProperty.call(message, "roundNumber"))
                                    if (!$util.isInteger(message.roundNumber) && !(message.roundNumber && $util.isInteger(message.roundNumber.low) && $util.isInteger(message.roundNumber.high)))
                                        return "roundNumber: integer|Long expected";
                                if (message.activeRosterHash != null && $Object.hasOwnProperty.call(message, "activeRosterHash"))
                                    if (!(message.activeRosterHash && typeof message.activeRosterHash.length === "number" || $util.isString(message.activeRosterHash)))
                                        return "activeRosterHash: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a RoundRosterPair message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.roster.RoundRosterPair
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.roster.RoundRosterPair} RoundRosterPair
                             */
                            RoundRosterPair.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.roster.RoundRosterPair)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.roster.RoundRosterPair: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.roster.RoundRosterPair();
                                if (object.roundNumber != null)
                                    if (typeof object.roundNumber === "object" ? object.roundNumber.low || object.roundNumber.high : $Number(object.roundNumber) !== 0)
                                        if ($util.Long)
                                            message.roundNumber = $util.Long.fromValue(object.roundNumber, true);
                                        else if (typeof object.roundNumber === "string")
                                            message.roundNumber = $parseInt(object.roundNumber, 10);
                                        else if (typeof object.roundNumber === "number")
                                            message.roundNumber = object.roundNumber;
                                        else if (typeof object.roundNumber === "object")
                                            message.roundNumber = new $util.LongBits(object.roundNumber.low >>> 0, object.roundNumber.high >>> 0).toNumber(true);
                                if (object.activeRosterHash != null)
                                    if (object.activeRosterHash.length)
                                        if (typeof object.activeRosterHash === "string")
                                            $util.base64.decode(object.activeRosterHash, message.activeRosterHash = $util.newBuffer($util.base64.length(object.activeRosterHash)), 0);
                                        else if (object.activeRosterHash.length >= 0)
                                            message.activeRosterHash = object.activeRosterHash;
                                return message;
                            };

                            /**
                             * Creates a plain object from a RoundRosterPair message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.roster.RoundRosterPair
                             * @static
                             * @param {com.hedera.hapi.node.state.roster.RoundRosterPair} message RoundRosterPair
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            RoundRosterPair.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.roundNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.roundNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if (options.bytes === $String)
                                        object.activeRosterHash = "";
                                    else {
                                        object.activeRosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.activeRosterHash = $util.newBuffer(object.activeRosterHash);
                                    }
                                }
                                if (message.roundNumber != null && $Object.hasOwnProperty.call(message, "roundNumber"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.roundNumber = typeof message.roundNumber === "number" ? $BigInt(message.roundNumber) : $util.Long.fromBits(message.roundNumber.low >>> 0, message.roundNumber.high >>> 0, true).toBigInt();
                                    else if (typeof message.roundNumber === "number")
                                        object.roundNumber = options.longs === $String ? $String(message.roundNumber) : message.roundNumber;
                                    else
                                        object.roundNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.roundNumber) : options.longs === $Number ? new $util.LongBits(message.roundNumber.low >>> 0, message.roundNumber.high >>> 0).toNumber(true) : message.roundNumber;
                                if (message.activeRosterHash != null && $Object.hasOwnProperty.call(message, "activeRosterHash"))
                                    object.activeRosterHash = options.bytes === $String ? $util.base64.encode(message.activeRosterHash, 0, message.activeRosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.activeRosterHash) : message.activeRosterHash;
                                return object;
                            };

                            /**
                             * Converts this RoundRosterPair to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.roster.RoundRosterPair
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            RoundRosterPair.prototype.toJSON = function() {
                                return RoundRosterPair.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for RoundRosterPair
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.roster.RoundRosterPair
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            RoundRosterPair.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.roster.RoundRosterPair";
                            };

                            return RoundRosterPair;
                        })();

                        return roster;
                    })();

                    state.token = (function() {

                        /**
                         * Namespace token.
                         * @memberof com.hedera.hapi.node.state
                         * @namespace
                         */
                        const token = {};

                        token.NodePayments = (function() {

                            /**
                             * Properties of a NodePayments.
                             * @typedef {Object} com.hedera.hapi.node.state.token.NodePayments.$Properties
                             * @property {proto.Timestamp.$Properties|null} [lastNodeFeeDistributionTime] The time when the last node fees were distributed
                             * @property {Array.<com.hedera.hapi.node.state.token.NodePayment.$Properties>|null} [payments] A list of node account id to the total amount of fees due to each node in the current staking period
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a NodePayments.
                             * @memberof com.hedera.hapi.node.state.token
                             * @interface INodePayments
                             * @augments com.hedera.hapi.node.state.token.NodePayments.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.token.NodePayments.$Properties instead.
                             */

                            /**
                             * Shape of a NodePayments.
                             * @typedef {{
                             *   lastNodeFeeDistributionTime?: proto.Timestamp.$Shape|null;
                             *   payments?: Array.<com.hedera.hapi.node.state.token.NodePayment.$Shape>|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * }} com.hedera.hapi.node.state.token.NodePayments.$Shape
                             */

                            /**
                             * Constructs a new NodePayments.
                             * @memberof com.hedera.hapi.node.state.token
                             * @classdesc A singleton state object that accumulates node fees for distribution<br/>
                             * This is used to record the total amount of fees due to each node for distribution in current staking period
                             * @constructor
                             * @param {com.hedera.hapi.node.state.token.NodePayments.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const NodePayments = function (properties) {
                                this.payments = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The time when the last node fees were distributed
                             * @member {proto.Timestamp.$Properties|null|undefined} lastNodeFeeDistributionTime
                             * @memberof com.hedera.hapi.node.state.token.NodePayments
                             * @instance
                             */
                            NodePayments.prototype.lastNodeFeeDistributionTime = null;

                            /**
                             * A list of node account id to the total amount of fees due to each node in the current staking period
                             * @member {Array.<com.hedera.hapi.node.state.token.NodePayment.$Properties>} payments
                             * @memberof com.hedera.hapi.node.state.token.NodePayments
                             * @instance
                             */
                            NodePayments.prototype.payments = $util.emptyArray;

                            /**
                             * Creates a new NodePayments instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.token.NodePayments
                             * @static
                             * @param {com.hedera.hapi.node.state.token.NodePayments.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.token.NodePayments} NodePayments instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.token.NodePayments.$Shape): com.hedera.hapi.node.state.token.NodePayments & com.hedera.hapi.node.state.token.NodePayments.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.token.NodePayments.$Properties): com.hedera.hapi.node.state.token.NodePayments;
                             * }}
                             */
                            NodePayments.create = function(properties) {
                                return new NodePayments(properties);
                            };

                            /**
                             * Encodes the specified NodePayments message. Does not implicitly {@link com.hedera.hapi.node.state.token.NodePayments.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.token.NodePayments
                             * @static
                             * @param {com.hedera.hapi.node.state.token.NodePayments.$Properties} message NodePayments message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            NodePayments.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.lastNodeFeeDistributionTime != null && $Object.hasOwnProperty.call(message, "lastNodeFeeDistributionTime"))
                                    $root.proto.Timestamp.encode(message.lastNodeFeeDistributionTime, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.payments != null && message.payments.length)
                                    for (let i = 0; i < message.payments.length; ++i)
                                        $root.com.hedera.hapi.node.state.token.NodePayment.encode(message.payments[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified NodePayments message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.token.NodePayments.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.token.NodePayments
                             * @static
                             * @param {com.hedera.hapi.node.state.token.NodePayments.$Properties} message NodePayments message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            NodePayments.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a NodePayments message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.token.NodePayments
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.token.NodePayments & com.hedera.hapi.node.state.token.NodePayments.$Shape} NodePayments
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            NodePayments.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.token.NodePayments();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            message.lastNodeFeeDistributionTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.lastNodeFeeDistributionTime);
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.payments && message.payments.length))
                                                message.payments = [];
                                            message.payments.push($root.com.hedera.hapi.node.state.token.NodePayment.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a NodePayments message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.token.NodePayments
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.token.NodePayments & com.hedera.hapi.node.state.token.NodePayments.$Shape} NodePayments
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            NodePayments.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a NodePayments message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.token.NodePayments
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            NodePayments.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.lastNodeFeeDistributionTime != null && $Object.hasOwnProperty.call(message, "lastNodeFeeDistributionTime")) {
                                    let error = $root.proto.Timestamp.verify(message.lastNodeFeeDistributionTime, _depth + 1);
                                    if (error)
                                        return "lastNodeFeeDistributionTime." + error;
                                }
                                if (message.payments != null && $Object.hasOwnProperty.call(message, "payments")) {
                                    if (!$Array.isArray(message.payments))
                                        return "payments: array expected";
                                    for (let i = 0; i < message.payments.length; ++i) {
                                        let error = $root.com.hedera.hapi.node.state.token.NodePayment.verify(message.payments[i], _depth + 1);
                                        if (error)
                                            return "payments." + error;
                                    }
                                }
                                return null;
                            };

                            /**
                             * Creates a NodePayments message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.token.NodePayments
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.token.NodePayments} NodePayments
                             */
                            NodePayments.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.token.NodePayments)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.token.NodePayments: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.token.NodePayments();
                                if (object.lastNodeFeeDistributionTime != null) {
                                    if (!$util.isObject(object.lastNodeFeeDistributionTime))
                                        throw $TypeError(".com.hedera.hapi.node.state.token.NodePayments.lastNodeFeeDistributionTime: object expected");
                                    message.lastNodeFeeDistributionTime = $root.proto.Timestamp.fromObject(object.lastNodeFeeDistributionTime, _depth + 1);
                                }
                                if (object.payments) {
                                    if (!$Array.isArray(object.payments))
                                        throw $TypeError(".com.hedera.hapi.node.state.token.NodePayments.payments: array expected");
                                    message.payments = $Array(object.payments.length);
                                    for (let i = 0; i < object.payments.length; ++i) {
                                        if (!$util.isObject(object.payments[i]))
                                            throw $TypeError(".com.hedera.hapi.node.state.token.NodePayments.payments: object expected");
                                        message.payments[i] = $root.com.hedera.hapi.node.state.token.NodePayment.fromObject(object.payments[i], _depth + 1);
                                    }
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a NodePayments message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.token.NodePayments
                             * @static
                             * @param {com.hedera.hapi.node.state.token.NodePayments} message NodePayments
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            NodePayments.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults)
                                    object.payments = [];
                                if (options.defaults)
                                    object.lastNodeFeeDistributionTime = null;
                                if (message.lastNodeFeeDistributionTime != null && $Object.hasOwnProperty.call(message, "lastNodeFeeDistributionTime"))
                                    object.lastNodeFeeDistributionTime = $root.proto.Timestamp.toObject(message.lastNodeFeeDistributionTime, options, _depth + 1);
                                if (message.payments && message.payments.length) {
                                    object.payments = $Array(message.payments.length);
                                    for (let j = 0; j < message.payments.length; ++j)
                                        object.payments[j] = $root.com.hedera.hapi.node.state.token.NodePayment.toObject(message.payments[j], options, _depth + 1);
                                }
                                return object;
                            };

                            /**
                             * Converts this NodePayments to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.token.NodePayments
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            NodePayments.prototype.toJSON = function() {
                                return NodePayments.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for NodePayments
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.token.NodePayments
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            NodePayments.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.token.NodePayments";
                            };

                            return NodePayments;
                        })();

                        token.NodePayment = (function() {

                            /**
                             * Properties of a NodePayment.
                             * @typedef {Object} com.hedera.hapi.node.state.token.NodePayment.$Properties
                             * @property {proto.AccountID.$Properties|null} [nodeAccountId] The node account id
                             * @property {number|Long|null} [fees] The total amount of fees due to the node in tinybars
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a NodePayment.
                             * @memberof com.hedera.hapi.node.state.token
                             * @interface INodePayment
                             * @augments com.hedera.hapi.node.state.token.NodePayment.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.token.NodePayment.$Properties instead.
                             */

                            /**
                             * Shape of a NodePayment.
                             * @typedef {{
                             *   nodeAccountId?: proto.AccountID.$Shape|null;
                             *   fees?: number|Long|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * }} com.hedera.hapi.node.state.token.NodePayment.$Shape
                             */

                            /**
                             * Constructs a new NodePayment.
                             * @memberof com.hedera.hapi.node.state.token
                             * @classdesc A record of the total amount of fees due to a node
                             * @constructor
                             * @param {com.hedera.hapi.node.state.token.NodePayment.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const NodePayment = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The node account id
                             * @member {proto.AccountID.$Properties|null|undefined} nodeAccountId
                             * @memberof com.hedera.hapi.node.state.token.NodePayment
                             * @instance
                             */
                            NodePayment.prototype.nodeAccountId = null;

                            /**
                             * The total amount of fees due to the node in tinybars
                             * @member {number|Long} fees
                             * @memberof com.hedera.hapi.node.state.token.NodePayment
                             * @instance
                             */
                            NodePayment.prototype.fees = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * Creates a new NodePayment instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.token.NodePayment
                             * @static
                             * @param {com.hedera.hapi.node.state.token.NodePayment.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.token.NodePayment} NodePayment instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.token.NodePayment.$Shape): com.hedera.hapi.node.state.token.NodePayment & com.hedera.hapi.node.state.token.NodePayment.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.token.NodePayment.$Properties): com.hedera.hapi.node.state.token.NodePayment;
                             * }}
                             */
                            NodePayment.create = function(properties) {
                                return new NodePayment(properties);
                            };

                            /**
                             * Encodes the specified NodePayment message. Does not implicitly {@link com.hedera.hapi.node.state.token.NodePayment.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.token.NodePayment
                             * @static
                             * @param {com.hedera.hapi.node.state.token.NodePayment.$Properties} message NodePayment message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            NodePayment.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.nodeAccountId != null && $Object.hasOwnProperty.call(message, "nodeAccountId"))
                                    $root.proto.AccountID.encode(message.nodeAccountId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                                if (message.fees != null && $Object.hasOwnProperty.call(message, "fees") && (typeof message.fees === "object" ? message.fees.low || message.fees.high : message.fees !== 0))
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.fees);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified NodePayment message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.token.NodePayment.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.token.NodePayment
                             * @static
                             * @param {com.hedera.hapi.node.state.token.NodePayment.$Properties} message NodePayment message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            NodePayment.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a NodePayment message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.token.NodePayment
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.token.NodePayment & com.hedera.hapi.node.state.token.NodePayment.$Shape} NodePayment
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            NodePayment.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.token.NodePayment();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            message.nodeAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodeAccountId);
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.fees = value;
                                            else
                                                delete message.fees;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a NodePayment message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.token.NodePayment
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.token.NodePayment & com.hedera.hapi.node.state.token.NodePayment.$Shape} NodePayment
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            NodePayment.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a NodePayment message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.token.NodePayment
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            NodePayment.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.nodeAccountId != null && $Object.hasOwnProperty.call(message, "nodeAccountId")) {
                                    let error = $root.proto.AccountID.verify(message.nodeAccountId, _depth + 1);
                                    if (error)
                                        return "nodeAccountId." + error;
                                }
                                if (message.fees != null && $Object.hasOwnProperty.call(message, "fees"))
                                    if (!$util.isInteger(message.fees) && !(message.fees && $util.isInteger(message.fees.low) && $util.isInteger(message.fees.high)))
                                        return "fees: integer|Long expected";
                                return null;
                            };

                            /**
                             * Creates a NodePayment message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.token.NodePayment
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.token.NodePayment} NodePayment
                             */
                            NodePayment.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.token.NodePayment)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.token.NodePayment: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.token.NodePayment();
                                if (object.nodeAccountId != null) {
                                    if (!$util.isObject(object.nodeAccountId))
                                        throw $TypeError(".com.hedera.hapi.node.state.token.NodePayment.nodeAccountId: object expected");
                                    message.nodeAccountId = $root.proto.AccountID.fromObject(object.nodeAccountId, _depth + 1);
                                }
                                if (object.fees != null)
                                    if (typeof object.fees === "object" ? object.fees.low || object.fees.high : $Number(object.fees) !== 0)
                                        if ($util.Long)
                                            message.fees = $util.Long.fromValue(object.fees, true);
                                        else if (typeof object.fees === "string")
                                            message.fees = $parseInt(object.fees, 10);
                                        else if (typeof object.fees === "number")
                                            message.fees = object.fees;
                                        else if (typeof object.fees === "object")
                                            message.fees = new $util.LongBits(object.fees.low >>> 0, object.fees.high >>> 0).toNumber(true);
                                return message;
                            };

                            /**
                             * Creates a plain object from a NodePayment message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.token.NodePayment
                             * @static
                             * @param {com.hedera.hapi.node.state.token.NodePayment} message NodePayment
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            NodePayment.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    object.nodeAccountId = null;
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.fees = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.fees = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                }
                                if (message.nodeAccountId != null && $Object.hasOwnProperty.call(message, "nodeAccountId"))
                                    object.nodeAccountId = $root.proto.AccountID.toObject(message.nodeAccountId, options, _depth + 1);
                                if (message.fees != null && $Object.hasOwnProperty.call(message, "fees"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.fees = typeof message.fees === "number" ? $BigInt(message.fees) : $util.Long.fromBits(message.fees.low >>> 0, message.fees.high >>> 0, true).toBigInt();
                                    else if (typeof message.fees === "number")
                                        object.fees = options.longs === $String ? $String(message.fees) : message.fees;
                                    else
                                        object.fees = options.longs === $String ? $util.Long.prototype.toString.call(message.fees) : options.longs === $Number ? new $util.LongBits(message.fees.low >>> 0, message.fees.high >>> 0).toNumber(true) : message.fees;
                                return object;
                            };

                            /**
                             * Converts this NodePayment to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.token.NodePayment
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            NodePayment.prototype.toJSON = function() {
                                return NodePayment.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for NodePayment
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.token.NodePayment
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            NodePayment.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.token.NodePayment";
                            };

                            return NodePayment;
                        })();

                        return token;
                    })();

                    state.tss = (function() {

                        /**
                         * Namespace tss.
                         * @memberof com.hedera.hapi.node.state
                         * @namespace
                         */
                        const tss = {};

                        tss.TssEncryptionKeys = (function() {

                            /**
                             * Properties of a TssEncryptionKeys.
                             * @typedef {Object} com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Properties
                             * @property {Uint8Array|null} [currentEncryptionKey] If non-empty, a node's current TSS encryption key.
                             * @property {Uint8Array|null} [nextEncryptionKey] If non-empty, the same node's next TSS encryption key.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a TssEncryptionKeys.
                             * @memberof com.hedera.hapi.node.state.tss
                             * @interface ITssEncryptionKeys
                             * @augments com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Properties instead.
                             */

                            /**
                             * Shape of a TssEncryptionKeys.
                             * @typedef {com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Properties} com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Shape
                             */

                            /**
                             * Constructs a new TssEncryptionKeys.
                             * @memberof com.hedera.hapi.node.state.tss
                             * @classdesc A message containing a node's current and next TSS encryption keys, where
                             * the next key (if present) will be switched to the node's current key during
                             * the first transaction at the beginning of a staking period.<br/>
                             * @constructor
                             * @param {com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const TssEncryptionKeys = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * If non-empty, a node's current TSS encryption key.
                             * @member {Uint8Array} currentEncryptionKey
                             * @memberof com.hedera.hapi.node.state.tss.TssEncryptionKeys
                             * @instance
                             */
                            TssEncryptionKeys.prototype.currentEncryptionKey = $util.newBuffer([]);

                            /**
                             * If non-empty, the same node's next TSS encryption key.
                             * @member {Uint8Array} nextEncryptionKey
                             * @memberof com.hedera.hapi.node.state.tss.TssEncryptionKeys
                             * @instance
                             */
                            TssEncryptionKeys.prototype.nextEncryptionKey = $util.newBuffer([]);

                            /**
                             * Creates a new TssEncryptionKeys instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.tss.TssEncryptionKeys
                             * @static
                             * @param {com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.tss.TssEncryptionKeys} TssEncryptionKeys instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Shape): com.hedera.hapi.node.state.tss.TssEncryptionKeys & com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Properties): com.hedera.hapi.node.state.tss.TssEncryptionKeys;
                             * }}
                             */
                            TssEncryptionKeys.create = function(properties) {
                                return new TssEncryptionKeys(properties);
                            };

                            /**
                             * Encodes the specified TssEncryptionKeys message. Does not implicitly {@link com.hedera.hapi.node.state.tss.TssEncryptionKeys.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.tss.TssEncryptionKeys
                             * @static
                             * @param {com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Properties} message TssEncryptionKeys message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            TssEncryptionKeys.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.currentEncryptionKey != null && $Object.hasOwnProperty.call(message, "currentEncryptionKey") && message.currentEncryptionKey.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.currentEncryptionKey);
                                if (message.nextEncryptionKey != null && $Object.hasOwnProperty.call(message, "nextEncryptionKey") && message.nextEncryptionKey.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.nextEncryptionKey);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified TssEncryptionKeys message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.tss.TssEncryptionKeys.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.tss.TssEncryptionKeys
                             * @static
                             * @param {com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Properties} message TssEncryptionKeys message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            TssEncryptionKeys.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a TssEncryptionKeys message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.tss.TssEncryptionKeys
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.tss.TssEncryptionKeys & com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Shape} TssEncryptionKeys
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            TssEncryptionKeys.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.tss.TssEncryptionKeys();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.currentEncryptionKey = value;
                                            else
                                                delete message.currentEncryptionKey;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.nextEncryptionKey = value;
                                            else
                                                delete message.nextEncryptionKey;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a TssEncryptionKeys message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.tss.TssEncryptionKeys
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.tss.TssEncryptionKeys & com.hedera.hapi.node.state.tss.TssEncryptionKeys.$Shape} TssEncryptionKeys
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            TssEncryptionKeys.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a TssEncryptionKeys message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.tss.TssEncryptionKeys
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            TssEncryptionKeys.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.currentEncryptionKey != null && $Object.hasOwnProperty.call(message, "currentEncryptionKey"))
                                    if (!(message.currentEncryptionKey && typeof message.currentEncryptionKey.length === "number" || $util.isString(message.currentEncryptionKey)))
                                        return "currentEncryptionKey: buffer expected";
                                if (message.nextEncryptionKey != null && $Object.hasOwnProperty.call(message, "nextEncryptionKey"))
                                    if (!(message.nextEncryptionKey && typeof message.nextEncryptionKey.length === "number" || $util.isString(message.nextEncryptionKey)))
                                        return "nextEncryptionKey: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a TssEncryptionKeys message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.tss.TssEncryptionKeys
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.tss.TssEncryptionKeys} TssEncryptionKeys
                             */
                            TssEncryptionKeys.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.tss.TssEncryptionKeys)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.tss.TssEncryptionKeys: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.tss.TssEncryptionKeys();
                                if (object.currentEncryptionKey != null)
                                    if (object.currentEncryptionKey.length)
                                        if (typeof object.currentEncryptionKey === "string")
                                            $util.base64.decode(object.currentEncryptionKey, message.currentEncryptionKey = $util.newBuffer($util.base64.length(object.currentEncryptionKey)), 0);
                                        else if (object.currentEncryptionKey.length >= 0)
                                            message.currentEncryptionKey = object.currentEncryptionKey;
                                if (object.nextEncryptionKey != null)
                                    if (object.nextEncryptionKey.length)
                                        if (typeof object.nextEncryptionKey === "string")
                                            $util.base64.decode(object.nextEncryptionKey, message.nextEncryptionKey = $util.newBuffer($util.base64.length(object.nextEncryptionKey)), 0);
                                        else if (object.nextEncryptionKey.length >= 0)
                                            message.nextEncryptionKey = object.nextEncryptionKey;
                                return message;
                            };

                            /**
                             * Creates a plain object from a TssEncryptionKeys message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.tss.TssEncryptionKeys
                             * @static
                             * @param {com.hedera.hapi.node.state.tss.TssEncryptionKeys} message TssEncryptionKeys
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            TssEncryptionKeys.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.currentEncryptionKey = "";
                                    else {
                                        object.currentEncryptionKey = [];
                                        if (options.bytes !== $Array)
                                            object.currentEncryptionKey = $util.newBuffer(object.currentEncryptionKey);
                                    }
                                    if (options.bytes === $String)
                                        object.nextEncryptionKey = "";
                                    else {
                                        object.nextEncryptionKey = [];
                                        if (options.bytes !== $Array)
                                            object.nextEncryptionKey = $util.newBuffer(object.nextEncryptionKey);
                                    }
                                }
                                if (message.currentEncryptionKey != null && $Object.hasOwnProperty.call(message, "currentEncryptionKey"))
                                    object.currentEncryptionKey = options.bytes === $String ? $util.base64.encode(message.currentEncryptionKey, 0, message.currentEncryptionKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.currentEncryptionKey) : message.currentEncryptionKey;
                                if (message.nextEncryptionKey != null && $Object.hasOwnProperty.call(message, "nextEncryptionKey"))
                                    object.nextEncryptionKey = options.bytes === $String ? $util.base64.encode(message.nextEncryptionKey, 0, message.nextEncryptionKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.nextEncryptionKey) : message.nextEncryptionKey;
                                return object;
                            };

                            /**
                             * Converts this TssEncryptionKeys to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.tss.TssEncryptionKeys
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            TssEncryptionKeys.prototype.toJSON = function() {
                                return TssEncryptionKeys.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for TssEncryptionKeys
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.tss.TssEncryptionKeys
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            TssEncryptionKeys.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.tss.TssEncryptionKeys";
                            };

                            return TssEncryptionKeys;
                        })();

                        tss.TssMessageMapKey = (function() {

                            /**
                             * Properties of a TssMessageMapKey.
                             * @typedef {Object} com.hedera.hapi.node.state.tss.TssMessageMapKey.$Properties
                             * @property {Uint8Array|null} [rosterHash] A hash that uniquely identifies the target roster for the associated value
                             * in the map.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * This value MUST contain a valid hash.
                             * @property {number|Long|null} [sequenceNumber] A number representing consensus order.<br/>
                             * This declares the order in which the mapped value came to consensus.
                             * <p>This value MUST be set.<br/>
                             * This value MUST be a valid sequence number.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a TssMessageMapKey.
                             * @memberof com.hedera.hapi.node.state.tss
                             * @interface ITssMessageMapKey
                             * @augments com.hedera.hapi.node.state.tss.TssMessageMapKey.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.tss.TssMessageMapKey.$Properties instead.
                             */

                            /**
                             * Shape of a TssMessageMapKey.
                             * @typedef {com.hedera.hapi.node.state.tss.TssMessageMapKey.$Properties} com.hedera.hapi.node.state.tss.TssMessageMapKey.$Shape
                             */

                            /**
                             * Constructs a new TssMessageMapKey.
                             * @memberof com.hedera.hapi.node.state.tss
                             * @classdesc A key for use in the Threshold Signature Scheme (TSS) TssMessageMaps.
                             * 
                             * This key SHALL be used to uniquely identify entries in the Message Maps.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.tss.TssMessageMapKey.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const TssMessageMapKey = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * A hash that uniquely identifies the target roster for the associated value
                             * in the map.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * This value MUST contain a valid hash.
                             * @member {Uint8Array} rosterHash
                             * @memberof com.hedera.hapi.node.state.tss.TssMessageMapKey
                             * @instance
                             */
                            TssMessageMapKey.prototype.rosterHash = $util.newBuffer([]);

                            /**
                             * A number representing consensus order.<br/>
                             * This declares the order in which the mapped value came to consensus.
                             * <p>This value MUST be set.<br/>
                             * This value MUST be a valid sequence number.
                             * @member {number|Long} sequenceNumber
                             * @memberof com.hedera.hapi.node.state.tss.TssMessageMapKey
                             * @instance
                             */
                            TssMessageMapKey.prototype.sequenceNumber = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * Creates a new TssMessageMapKey instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.tss.TssMessageMapKey
                             * @static
                             * @param {com.hedera.hapi.node.state.tss.TssMessageMapKey.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.tss.TssMessageMapKey} TssMessageMapKey instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.tss.TssMessageMapKey.$Shape): com.hedera.hapi.node.state.tss.TssMessageMapKey & com.hedera.hapi.node.state.tss.TssMessageMapKey.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.tss.TssMessageMapKey.$Properties): com.hedera.hapi.node.state.tss.TssMessageMapKey;
                             * }}
                             */
                            TssMessageMapKey.create = function(properties) {
                                return new TssMessageMapKey(properties);
                            };

                            /**
                             * Encodes the specified TssMessageMapKey message. Does not implicitly {@link com.hedera.hapi.node.state.tss.TssMessageMapKey.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.tss.TssMessageMapKey
                             * @static
                             * @param {com.hedera.hapi.node.state.tss.TssMessageMapKey.$Properties} message TssMessageMapKey message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            TssMessageMapKey.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.rosterHash != null && $Object.hasOwnProperty.call(message, "rosterHash") && message.rosterHash.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.rosterHash);
                                if (message.sequenceNumber != null && $Object.hasOwnProperty.call(message, "sequenceNumber") && (typeof message.sequenceNumber === "object" ? message.sequenceNumber.low || message.sequenceNumber.high : message.sequenceNumber !== 0))
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.sequenceNumber);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified TssMessageMapKey message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.tss.TssMessageMapKey.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.tss.TssMessageMapKey
                             * @static
                             * @param {com.hedera.hapi.node.state.tss.TssMessageMapKey.$Properties} message TssMessageMapKey message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            TssMessageMapKey.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a TssMessageMapKey message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.tss.TssMessageMapKey
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.tss.TssMessageMapKey & com.hedera.hapi.node.state.tss.TssMessageMapKey.$Shape} TssMessageMapKey
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            TssMessageMapKey.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.tss.TssMessageMapKey();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.rosterHash = value;
                                            else
                                                delete message.rosterHash;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.sequenceNumber = value;
                                            else
                                                delete message.sequenceNumber;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a TssMessageMapKey message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.tss.TssMessageMapKey
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.tss.TssMessageMapKey & com.hedera.hapi.node.state.tss.TssMessageMapKey.$Shape} TssMessageMapKey
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            TssMessageMapKey.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a TssMessageMapKey message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.tss.TssMessageMapKey
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            TssMessageMapKey.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.rosterHash != null && $Object.hasOwnProperty.call(message, "rosterHash"))
                                    if (!(message.rosterHash && typeof message.rosterHash.length === "number" || $util.isString(message.rosterHash)))
                                        return "rosterHash: buffer expected";
                                if (message.sequenceNumber != null && $Object.hasOwnProperty.call(message, "sequenceNumber"))
                                    if (!$util.isInteger(message.sequenceNumber) && !(message.sequenceNumber && $util.isInteger(message.sequenceNumber.low) && $util.isInteger(message.sequenceNumber.high)))
                                        return "sequenceNumber: integer|Long expected";
                                return null;
                            };

                            /**
                             * Creates a TssMessageMapKey message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.tss.TssMessageMapKey
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.tss.TssMessageMapKey} TssMessageMapKey
                             */
                            TssMessageMapKey.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.tss.TssMessageMapKey)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.tss.TssMessageMapKey: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.tss.TssMessageMapKey();
                                if (object.rosterHash != null)
                                    if (object.rosterHash.length)
                                        if (typeof object.rosterHash === "string")
                                            $util.base64.decode(object.rosterHash, message.rosterHash = $util.newBuffer($util.base64.length(object.rosterHash)), 0);
                                        else if (object.rosterHash.length >= 0)
                                            message.rosterHash = object.rosterHash;
                                if (object.sequenceNumber != null)
                                    if (typeof object.sequenceNumber === "object" ? object.sequenceNumber.low || object.sequenceNumber.high : $Number(object.sequenceNumber) !== 0)
                                        if ($util.Long)
                                            message.sequenceNumber = $util.Long.fromValue(object.sequenceNumber, true);
                                        else if (typeof object.sequenceNumber === "string")
                                            message.sequenceNumber = $parseInt(object.sequenceNumber, 10);
                                        else if (typeof object.sequenceNumber === "number")
                                            message.sequenceNumber = object.sequenceNumber;
                                        else if (typeof object.sequenceNumber === "object")
                                            message.sequenceNumber = new $util.LongBits(object.sequenceNumber.low >>> 0, object.sequenceNumber.high >>> 0).toNumber(true);
                                return message;
                            };

                            /**
                             * Creates a plain object from a TssMessageMapKey message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.tss.TssMessageMapKey
                             * @static
                             * @param {com.hedera.hapi.node.state.tss.TssMessageMapKey} message TssMessageMapKey
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            TssMessageMapKey.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.rosterHash = "";
                                    else {
                                        object.rosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.rosterHash = $util.newBuffer(object.rosterHash);
                                    }
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.sequenceNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.sequenceNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                }
                                if (message.rosterHash != null && $Object.hasOwnProperty.call(message, "rosterHash"))
                                    object.rosterHash = options.bytes === $String ? $util.base64.encode(message.rosterHash, 0, message.rosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.rosterHash) : message.rosterHash;
                                if (message.sequenceNumber != null && $Object.hasOwnProperty.call(message, "sequenceNumber"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.sequenceNumber = typeof message.sequenceNumber === "number" ? $BigInt(message.sequenceNumber) : $util.Long.fromBits(message.sequenceNumber.low >>> 0, message.sequenceNumber.high >>> 0, true).toBigInt();
                                    else if (typeof message.sequenceNumber === "number")
                                        object.sequenceNumber = options.longs === $String ? $String(message.sequenceNumber) : message.sequenceNumber;
                                    else
                                        object.sequenceNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.sequenceNumber) : options.longs === $Number ? new $util.LongBits(message.sequenceNumber.low >>> 0, message.sequenceNumber.high >>> 0).toNumber(true) : message.sequenceNumber;
                                return object;
                            };

                            /**
                             * Converts this TssMessageMapKey to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.tss.TssMessageMapKey
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            TssMessageMapKey.prototype.toJSON = function() {
                                return TssMessageMapKey.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for TssMessageMapKey
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.tss.TssMessageMapKey
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            TssMessageMapKey.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.tss.TssMessageMapKey";
                            };

                            return TssMessageMapKey;
                        })();

                        tss.TssVoteMapKey = (function() {

                            /**
                             * Properties of a TssVoteMapKey.
                             * @typedef {Object} com.hedera.hapi.node.state.tss.TssVoteMapKey.$Properties
                             * @property {Uint8Array|null} [rosterHash] A hash of the target roster for the associated value in the map. <br/>
                             * This hash uniquely identifies the target roster.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST contain a valid hash.
                             * @property {number|Long|null} [nodeId] The node id of the node that created the TssVote.<br/>
                             * This id uniquely identifies the node.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST be a valid node id.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a TssVoteMapKey.
                             * @memberof com.hedera.hapi.node.state.tss
                             * @interface ITssVoteMapKey
                             * @augments com.hedera.hapi.node.state.tss.TssVoteMapKey.$Properties
                             * @deprecated Use com.hedera.hapi.node.state.tss.TssVoteMapKey.$Properties instead.
                             */

                            /**
                             * Shape of a TssVoteMapKey.
                             * @typedef {com.hedera.hapi.node.state.tss.TssVoteMapKey.$Properties} com.hedera.hapi.node.state.tss.TssVoteMapKey.$Shape
                             */

                            /**
                             * Constructs a new TssVoteMapKey.
                             * @memberof com.hedera.hapi.node.state.tss
                             * @classdesc A key for use in the Threshold Signature Scheme (TSS) TssVoteMaps.
                             * 
                             * This key SHALL be used to uniquely identify entries in the Vote Maps.
                             * @constructor
                             * @param {com.hedera.hapi.node.state.tss.TssVoteMapKey.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const TssVoteMapKey = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * A hash of the target roster for the associated value in the map. <br/>
                             * This hash uniquely identifies the target roster.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST contain a valid hash.
                             * @member {Uint8Array} rosterHash
                             * @memberof com.hedera.hapi.node.state.tss.TssVoteMapKey
                             * @instance
                             */
                            TssVoteMapKey.prototype.rosterHash = $util.newBuffer([]);

                            /**
                             * The node id of the node that created the TssVote.<br/>
                             * This id uniquely identifies the node.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST be a valid node id.
                             * @member {number|Long} nodeId
                             * @memberof com.hedera.hapi.node.state.tss.TssVoteMapKey
                             * @instance
                             */
                            TssVoteMapKey.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * Creates a new TssVoteMapKey instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.node.state.tss.TssVoteMapKey
                             * @static
                             * @param {com.hedera.hapi.node.state.tss.TssVoteMapKey.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.node.state.tss.TssVoteMapKey} TssVoteMapKey instance
                             * @type {{
                             *   (properties: com.hedera.hapi.node.state.tss.TssVoteMapKey.$Shape): com.hedera.hapi.node.state.tss.TssVoteMapKey & com.hedera.hapi.node.state.tss.TssVoteMapKey.$Shape;
                             *   (properties?: com.hedera.hapi.node.state.tss.TssVoteMapKey.$Properties): com.hedera.hapi.node.state.tss.TssVoteMapKey;
                             * }}
                             */
                            TssVoteMapKey.create = function(properties) {
                                return new TssVoteMapKey(properties);
                            };

                            /**
                             * Encodes the specified TssVoteMapKey message. Does not implicitly {@link com.hedera.hapi.node.state.tss.TssVoteMapKey.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.node.state.tss.TssVoteMapKey
                             * @static
                             * @param {com.hedera.hapi.node.state.tss.TssVoteMapKey.$Properties} message TssVoteMapKey message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            TssVoteMapKey.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.rosterHash != null && $Object.hasOwnProperty.call(message, "rosterHash") && message.rosterHash.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.rosterHash);
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.nodeId);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified TssVoteMapKey message, length delimited. Does not implicitly {@link com.hedera.hapi.node.state.tss.TssVoteMapKey.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.node.state.tss.TssVoteMapKey
                             * @static
                             * @param {com.hedera.hapi.node.state.tss.TssVoteMapKey.$Properties} message TssVoteMapKey message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            TssVoteMapKey.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a TssVoteMapKey message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.node.state.tss.TssVoteMapKey
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.node.state.tss.TssVoteMapKey & com.hedera.hapi.node.state.tss.TssVoteMapKey.$Shape} TssVoteMapKey
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            TssVoteMapKey.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.node.state.tss.TssVoteMapKey();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.rosterHash = value;
                                            else
                                                delete message.rosterHash;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.nodeId = value;
                                            else
                                                delete message.nodeId;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a TssVoteMapKey message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.node.state.tss.TssVoteMapKey
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.node.state.tss.TssVoteMapKey & com.hedera.hapi.node.state.tss.TssVoteMapKey.$Shape} TssVoteMapKey
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            TssVoteMapKey.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a TssVoteMapKey message.
                             * @function verify
                             * @memberof com.hedera.hapi.node.state.tss.TssVoteMapKey
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            TssVoteMapKey.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.rosterHash != null && $Object.hasOwnProperty.call(message, "rosterHash"))
                                    if (!(message.rosterHash && typeof message.rosterHash.length === "number" || $util.isString(message.rosterHash)))
                                        return "rosterHash: buffer expected";
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                        return "nodeId: integer|Long expected";
                                return null;
                            };

                            /**
                             * Creates a TssVoteMapKey message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.node.state.tss.TssVoteMapKey
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.node.state.tss.TssVoteMapKey} TssVoteMapKey
                             */
                            TssVoteMapKey.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.node.state.tss.TssVoteMapKey)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.node.state.tss.TssVoteMapKey: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.node.state.tss.TssVoteMapKey();
                                if (object.rosterHash != null)
                                    if (object.rosterHash.length)
                                        if (typeof object.rosterHash === "string")
                                            $util.base64.decode(object.rosterHash, message.rosterHash = $util.newBuffer($util.base64.length(object.rosterHash)), 0);
                                        else if (object.rosterHash.length >= 0)
                                            message.rosterHash = object.rosterHash;
                                if (object.nodeId != null)
                                    if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                        if ($util.Long)
                                            message.nodeId = $util.Long.fromValue(object.nodeId, true);
                                        else if (typeof object.nodeId === "string")
                                            message.nodeId = $parseInt(object.nodeId, 10);
                                        else if (typeof object.nodeId === "number")
                                            message.nodeId = object.nodeId;
                                        else if (typeof object.nodeId === "object")
                                            message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
                                return message;
                            };

                            /**
                             * Creates a plain object from a TssVoteMapKey message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.node.state.tss.TssVoteMapKey
                             * @static
                             * @param {com.hedera.hapi.node.state.tss.TssVoteMapKey} message TssVoteMapKey
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            TssVoteMapKey.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.rosterHash = "";
                                    else {
                                        object.rosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.rosterHash = $util.newBuffer(object.rosterHash);
                                    }
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                }
                                if (message.rosterHash != null && $Object.hasOwnProperty.call(message, "rosterHash"))
                                    object.rosterHash = options.bytes === $String ? $util.base64.encode(message.rosterHash, 0, message.rosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.rosterHash) : message.rosterHash;
                                if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                                    else if (typeof message.nodeId === "number")
                                        object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                    else
                                        object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
                                return object;
                            };

                            /**
                             * Converts this TssVoteMapKey to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.node.state.tss.TssVoteMapKey
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            TssVoteMapKey.prototype.toJSON = function() {
                                return TssVoteMapKey.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for TssVoteMapKey
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.node.state.tss.TssVoteMapKey
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            TssVoteMapKey.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.node.state.tss.TssVoteMapKey";
                            };

                            return TssVoteMapKey;
                        })();

                        return tss;
                    })();

                    return state;
                })();

                node.tss = (function() {

                    /**
                     * Namespace tss.
                     * @memberof com.hedera.hapi.node
                     * @namespace
                     */
                    const tss = {};

                    tss.LedgerIdPublicationTransactionBody = (function() {

                        /**
                         * Properties of a LedgerIdPublicationTransactionBody.
                         * @typedef {Object} com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Properties
                         * @property {Uint8Array|null} [ledgerId] The new ledger id.
                         * @property {Uint8Array|null} [historyProofVerificationKey] The key to use when verifying recursive chain-of-trust proofs that extend
                         * from this ledger id.
                         * @property {Array.<com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Properties>|null} [nodeContributions] The node-specific contributions made to the new ledger id.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a LedgerIdPublicationTransactionBody.
                         * @memberof com.hedera.hapi.node.tss
                         * @interface ILedgerIdPublicationTransactionBody
                         * @augments com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Properties
                         * @deprecated Use com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Properties instead.
                         */

                        /**
                         * Shape of a LedgerIdPublicationTransactionBody.
                         * @typedef {com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Properties} com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Shape
                         */

                        /**
                         * Constructs a new LedgerIdPublicationTransactionBody.
                         * @memberof com.hedera.hapi.node.tss
                         * @classdesc A system initiated transaction to externalize the ledger id and verification
                         * key for recursive chain-of-trust proofs extending from the new ledger id.
                         * 
                         * This transaction SHALL be issued whenever the ledger id changes.<br/>
                         * This transaction MUST NOT be sent by a client and SHALL be rejected if
                         * received by any node.<br/>
                         * This transaction SHALL be present in the record stream or block stream.
                         * 
                         * ### Block Stream Effects
                         * None
                         * @constructor
                         * @param {com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const LedgerIdPublicationTransactionBody = function (properties) {
                            this.nodeContributions = [];
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The new ledger id.
                         * @member {Uint8Array} ledgerId
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @instance
                         */
                        LedgerIdPublicationTransactionBody.prototype.ledgerId = $util.newBuffer([]);

                        /**
                         * The key to use when verifying recursive chain-of-trust proofs that extend
                         * from this ledger id.
                         * @member {Uint8Array} historyProofVerificationKey
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @instance
                         */
                        LedgerIdPublicationTransactionBody.prototype.historyProofVerificationKey = $util.newBuffer([]);

                        /**
                         * The node-specific contributions made to the new ledger id.
                         * @member {Array.<com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Properties>} nodeContributions
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @instance
                         */
                        LedgerIdPublicationTransactionBody.prototype.nodeContributions = $util.emptyArray;

                        /**
                         * Creates a new LedgerIdPublicationTransactionBody instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody} LedgerIdPublicationTransactionBody instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Shape): com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody & com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Shape;
                         *   (properties?: com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Properties): com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody;
                         * }}
                         */
                        LedgerIdPublicationTransactionBody.create = function(properties) {
                            return new LedgerIdPublicationTransactionBody(properties);
                        };

                        /**
                         * Encodes the specified LedgerIdPublicationTransactionBody message. Does not implicitly {@link com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Properties} message LedgerIdPublicationTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        LedgerIdPublicationTransactionBody.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId") && message.ledgerId.length)
                                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ledgerId);
                            if (message.historyProofVerificationKey != null && $Object.hasOwnProperty.call(message, "historyProofVerificationKey") && message.historyProofVerificationKey.length)
                                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.historyProofVerificationKey);
                            if (message.nodeContributions != null && message.nodeContributions.length)
                                for (let i = 0; i < message.nodeContributions.length; ++i)
                                    $root.com.hedera.hapi.node.tss.LedgerIdNodeContribution.encode(message.nodeContributions[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified LedgerIdPublicationTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Properties} message LedgerIdPublicationTransactionBody message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        LedgerIdPublicationTransactionBody.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a LedgerIdPublicationTransactionBody message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody & com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Shape} LedgerIdPublicationTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        LedgerIdPublicationTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.ledgerId = value;
                                        else
                                            delete message.ledgerId;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.historyProofVerificationKey = value;
                                        else
                                            delete message.historyProofVerificationKey;
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        if (!(message.nodeContributions && message.nodeContributions.length))
                                            message.nodeContributions = [];
                                        message.nodeContributions.push($root.com.hedera.hapi.node.tss.LedgerIdNodeContribution.decode(reader, reader.uint32(), $undefined, _depth + 1));
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a LedgerIdPublicationTransactionBody message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody & com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Shape} LedgerIdPublicationTransactionBody
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        LedgerIdPublicationTransactionBody.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a LedgerIdPublicationTransactionBody message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        LedgerIdPublicationTransactionBody.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                                if (!(message.ledgerId && typeof message.ledgerId.length === "number" || $util.isString(message.ledgerId)))
                                    return "ledgerId: buffer expected";
                            if (message.historyProofVerificationKey != null && $Object.hasOwnProperty.call(message, "historyProofVerificationKey"))
                                if (!(message.historyProofVerificationKey && typeof message.historyProofVerificationKey.length === "number" || $util.isString(message.historyProofVerificationKey)))
                                    return "historyProofVerificationKey: buffer expected";
                            if (message.nodeContributions != null && $Object.hasOwnProperty.call(message, "nodeContributions")) {
                                if (!$Array.isArray(message.nodeContributions))
                                    return "nodeContributions: array expected";
                                for (let i = 0; i < message.nodeContributions.length; ++i) {
                                    let error = $root.com.hedera.hapi.node.tss.LedgerIdNodeContribution.verify(message.nodeContributions[i], _depth + 1);
                                    if (error)
                                        return "nodeContributions." + error;
                                }
                            }
                            return null;
                        };

                        /**
                         * Creates a LedgerIdPublicationTransactionBody message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody} LedgerIdPublicationTransactionBody
                         */
                        LedgerIdPublicationTransactionBody.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody();
                            if (object.ledgerId != null)
                                if (object.ledgerId.length)
                                    if (typeof object.ledgerId === "string")
                                        $util.base64.decode(object.ledgerId, message.ledgerId = $util.newBuffer($util.base64.length(object.ledgerId)), 0);
                                    else if (object.ledgerId.length >= 0)
                                        message.ledgerId = object.ledgerId;
                            if (object.historyProofVerificationKey != null)
                                if (object.historyProofVerificationKey.length)
                                    if (typeof object.historyProofVerificationKey === "string")
                                        $util.base64.decode(object.historyProofVerificationKey, message.historyProofVerificationKey = $util.newBuffer($util.base64.length(object.historyProofVerificationKey)), 0);
                                    else if (object.historyProofVerificationKey.length >= 0)
                                        message.historyProofVerificationKey = object.historyProofVerificationKey;
                            if (object.nodeContributions) {
                                if (!$Array.isArray(object.nodeContributions))
                                    throw $TypeError(".com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.nodeContributions: array expected");
                                message.nodeContributions = $Array(object.nodeContributions.length);
                                for (let i = 0; i < object.nodeContributions.length; ++i) {
                                    if (!$util.isObject(object.nodeContributions[i]))
                                        throw $TypeError(".com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.nodeContributions: object expected");
                                    message.nodeContributions[i] = $root.com.hedera.hapi.node.tss.LedgerIdNodeContribution.fromObject(object.nodeContributions[i], _depth + 1);
                                }
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a LedgerIdPublicationTransactionBody message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @static
                         * @param {com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody} message LedgerIdPublicationTransactionBody
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        LedgerIdPublicationTransactionBody.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.arrays || options.defaults)
                                object.nodeContributions = [];
                            if (options.defaults) {
                                if (options.bytes === $String)
                                    object.ledgerId = "";
                                else {
                                    object.ledgerId = [];
                                    if (options.bytes !== $Array)
                                        object.ledgerId = $util.newBuffer(object.ledgerId);
                                }
                                if (options.bytes === $String)
                                    object.historyProofVerificationKey = "";
                                else {
                                    object.historyProofVerificationKey = [];
                                    if (options.bytes !== $Array)
                                        object.historyProofVerificationKey = $util.newBuffer(object.historyProofVerificationKey);
                                }
                            }
                            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                                object.ledgerId = options.bytes === $String ? $util.base64.encode(message.ledgerId, 0, message.ledgerId.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ledgerId) : message.ledgerId;
                            if (message.historyProofVerificationKey != null && $Object.hasOwnProperty.call(message, "historyProofVerificationKey"))
                                object.historyProofVerificationKey = options.bytes === $String ? $util.base64.encode(message.historyProofVerificationKey, 0, message.historyProofVerificationKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.historyProofVerificationKey) : message.historyProofVerificationKey;
                            if (message.nodeContributions && message.nodeContributions.length) {
                                object.nodeContributions = $Array(message.nodeContributions.length);
                                for (let j = 0; j < message.nodeContributions.length; ++j)
                                    object.nodeContributions[j] = $root.com.hedera.hapi.node.tss.LedgerIdNodeContribution.toObject(message.nodeContributions[j], options, _depth + 1);
                            }
                            return object;
                        };

                        /**
                         * Converts this LedgerIdPublicationTransactionBody to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        LedgerIdPublicationTransactionBody.prototype.toJSON = function() {
                            return LedgerIdPublicationTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for LedgerIdPublicationTransactionBody
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        LedgerIdPublicationTransactionBody.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody";
                        };

                        return LedgerIdPublicationTransactionBody;
                    })();

                    tss.LedgerIdNodeContribution = (function() {

                        /**
                         * Properties of a LedgerIdNodeContribution.
                         * @typedef {Object} com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Properties
                         * @property {number|Long|null} [nodeId] The node id.
                         * @property {number|Long|null} [weight] The node's consensus weight when the ledger id was published.
                         * @property {Uint8Array|null} [historyProofKey] The public key the node used to contribute to history proofs when the
                         * ledger id was published.
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */

                        /**
                         * Properties of a LedgerIdNodeContribution.
                         * @memberof com.hedera.hapi.node.tss
                         * @interface ILedgerIdNodeContribution
                         * @augments com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Properties
                         * @deprecated Use com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Properties instead.
                         */

                        /**
                         * Shape of a LedgerIdNodeContribution.
                         * @typedef {com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Properties} com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Shape
                         */

                        /**
                         * Constructs a new LedgerIdNodeContribution.
                         * @memberof com.hedera.hapi.node.tss
                         * @classdesc A record of the proof key a node had in a particular address
                         * book. Necessary to keep at each point history so that nodes
                         * can verify the correct key was used to sign in transitions
                         * starting from the current address book; no matter how keys
                         * have been rotated from the time the address book was created.
                         * @constructor
                         * @param {com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Properties=} [properties] Properties to set
                         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                         */
                        const LedgerIdNodeContribution = function (properties) {
                            if (properties)
                                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        };

                        /**
                         * The node id.
                         * @member {number|Long} nodeId
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @instance
                         */
                        LedgerIdNodeContribution.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * The node's consensus weight when the ledger id was published.
                         * @member {number|Long} weight
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @instance
                         */
                        LedgerIdNodeContribution.prototype.weight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                        /**
                         * The public key the node used to contribute to history proofs when the
                         * ledger id was published.
                         * @member {Uint8Array} historyProofKey
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @instance
                         */
                        LedgerIdNodeContribution.prototype.historyProofKey = $util.newBuffer([]);

                        /**
                         * Creates a new LedgerIdNodeContribution instance using the specified properties.
                         * @function create
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @static
                         * @param {com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Properties=} [properties] Properties to set
                         * @returns {com.hedera.hapi.node.tss.LedgerIdNodeContribution} LedgerIdNodeContribution instance
                         * @type {{
                         *   (properties: com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Shape): com.hedera.hapi.node.tss.LedgerIdNodeContribution & com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Shape;
                         *   (properties?: com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Properties): com.hedera.hapi.node.tss.LedgerIdNodeContribution;
                         * }}
                         */
                        LedgerIdNodeContribution.create = function(properties) {
                            return new LedgerIdNodeContribution(properties);
                        };

                        /**
                         * Encodes the specified LedgerIdNodeContribution message. Does not implicitly {@link com.hedera.hapi.node.tss.LedgerIdNodeContribution.verify|verify} messages.
                         * @function encode
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @static
                         * @param {com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Properties} message LedgerIdNodeContribution message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        LedgerIdNodeContribution.encode = function (message, writer, _depth) {
                            if (!writer)
                                writer = $Writer.create();
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.nodeId);
                            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight") && (typeof message.weight === "object" ? message.weight.low || message.weight.high : message.weight !== 0))
                                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.weight);
                            if (message.historyProofKey != null && $Object.hasOwnProperty.call(message, "historyProofKey") && message.historyProofKey.length)
                                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.historyProofKey);
                            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                for (let i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified LedgerIdNodeContribution message, length delimited. Does not implicitly {@link com.hedera.hapi.node.tss.LedgerIdNodeContribution.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @static
                         * @param {com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Properties} message LedgerIdNodeContribution message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        LedgerIdNodeContribution.encodeDelimited = function(message, writer) {
                            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                        };

                        /**
                         * Decodes a LedgerIdNodeContribution message from the specified reader or buffer.
                         * @function decode
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {com.hedera.hapi.node.tss.LedgerIdNodeContribution & com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Shape} LedgerIdNodeContribution
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        LedgerIdNodeContribution.decode = function (reader, length, _end, _depth, _target) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            let end, message, value;
                            if (length === $undefined)
                                end = reader.len;
                            else {
                                end = reader.pos + length;
                                if (end > reader.len)
                                    throw $RangeError("index out of range");
                                length = reader.len;
                                reader.len = end;
                            }
                            message = _target || new $root.com.hedera.hapi.node.tss.LedgerIdNodeContribution();
                            while (reader.pos < end) {
                                let start = reader.pos;
                                let tag = reader.tag();
                                if (tag === _end) {
                                    _end = $undefined;
                                    break;
                                }
                                let wireType = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.nodeId = value;
                                        else
                                            delete message.nodeId;
                                        continue;
                                    }
                                case 2: {
                                        if (wireType !== 0)
                                            break;
                                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                            message.weight = value;
                                        else
                                            delete message.weight;
                                        continue;
                                    }
                                case 3: {
                                        if (wireType !== 2)
                                            break;
                                        if ((value = reader.bytes()).length)
                                            message.historyProofKey = value;
                                        else
                                            delete message.historyProofKey;
                                        continue;
                                    }
                                }
                                reader.skipType(wireType, _depth, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                }
                            }
                            if (length !== $undefined) {
                                if (reader.pos !== end)
                                    throw $RangeError("index out of range");
                                reader.len = length;
                            }
                            if (_end !== $undefined)
                                throw $Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a LedgerIdNodeContribution message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {com.hedera.hapi.node.tss.LedgerIdNodeContribution & com.hedera.hapi.node.tss.LedgerIdNodeContribution.$Shape} LedgerIdNodeContribution
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        LedgerIdNodeContribution.decodeDelimited = function(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a LedgerIdNodeContribution message.
                         * @function verify
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        LedgerIdNodeContribution.verify = function (message, _depth) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                return "max depth exceeded";
                            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                                    return "nodeId: integer|Long expected";
                            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                                if (!$util.isInteger(message.weight) && !(message.weight && $util.isInteger(message.weight.low) && $util.isInteger(message.weight.high)))
                                    return "weight: integer|Long expected";
                            if (message.historyProofKey != null && $Object.hasOwnProperty.call(message, "historyProofKey"))
                                if (!(message.historyProofKey && typeof message.historyProofKey.length === "number" || $util.isString(message.historyProofKey)))
                                    return "historyProofKey: buffer expected";
                            return null;
                        };

                        /**
                         * Creates a LedgerIdNodeContribution message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {com.hedera.hapi.node.tss.LedgerIdNodeContribution} LedgerIdNodeContribution
                         */
                        LedgerIdNodeContribution.fromObject = function (object, _depth) {
                            if (object instanceof $root.com.hedera.hapi.node.tss.LedgerIdNodeContribution)
                                return object;
                            if (!$util.isObject(object))
                                throw $TypeError(".com.hedera.hapi.node.tss.LedgerIdNodeContribution: object expected");
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let message = new $root.com.hedera.hapi.node.tss.LedgerIdNodeContribution();
                            if (object.nodeId != null)
                                if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                                    if ($util.Long)
                                        message.nodeId = $util.Long.fromValue(object.nodeId, true);
                                    else if (typeof object.nodeId === "string")
                                        message.nodeId = $parseInt(object.nodeId, 10);
                                    else if (typeof object.nodeId === "number")
                                        message.nodeId = object.nodeId;
                                    else if (typeof object.nodeId === "object")
                                        message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
                            if (object.weight != null)
                                if (typeof object.weight === "object" ? object.weight.low || object.weight.high : $Number(object.weight) !== 0)
                                    if ($util.Long)
                                        message.weight = $util.Long.fromValue(object.weight, true);
                                    else if (typeof object.weight === "string")
                                        message.weight = $parseInt(object.weight, 10);
                                    else if (typeof object.weight === "number")
                                        message.weight = object.weight;
                                    else if (typeof object.weight === "object")
                                        message.weight = new $util.LongBits(object.weight.low >>> 0, object.weight.high >>> 0).toNumber(true);
                            if (object.historyProofKey != null)
                                if (object.historyProofKey.length)
                                    if (typeof object.historyProofKey === "string")
                                        $util.base64.decode(object.historyProofKey, message.historyProofKey = $util.newBuffer($util.base64.length(object.historyProofKey)), 0);
                                    else if (object.historyProofKey.length >= 0)
                                        message.historyProofKey = object.historyProofKey;
                            return message;
                        };

                        /**
                         * Creates a plain object from a LedgerIdNodeContribution message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @static
                         * @param {com.hedera.hapi.node.tss.LedgerIdNodeContribution} message LedgerIdNodeContribution
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        LedgerIdNodeContribution.toObject = function (message, options, _depth) {
                            if (!options)
                                options = {};
                            if (_depth === $undefined)
                                _depth = 0;
                            if (_depth > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            let object = {};
                            if (options.defaults) {
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                if ($util.Long) {
                                    let long = new $util.Long(0, 0, true);
                                    object.weight = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                } else
                                    object.weight = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                if (options.bytes === $String)
                                    object.historyProofKey = "";
                                else {
                                    object.historyProofKey = [];
                                    if (options.bytes !== $Array)
                                        object.historyProofKey = $util.newBuffer(object.historyProofKey);
                                }
                            }
                            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                                else if (typeof message.nodeId === "number")
                                    object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                                else
                                    object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
                            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                    object.weight = typeof message.weight === "number" ? $BigInt(message.weight) : $util.Long.fromBits(message.weight.low >>> 0, message.weight.high >>> 0, true).toBigInt();
                                else if (typeof message.weight === "number")
                                    object.weight = options.longs === $String ? $String(message.weight) : message.weight;
                                else
                                    object.weight = options.longs === $String ? $util.Long.prototype.toString.call(message.weight) : options.longs === $Number ? new $util.LongBits(message.weight.low >>> 0, message.weight.high >>> 0).toNumber(true) : message.weight;
                            if (message.historyProofKey != null && $Object.hasOwnProperty.call(message, "historyProofKey"))
                                object.historyProofKey = options.bytes === $String ? $util.base64.encode(message.historyProofKey, 0, message.historyProofKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.historyProofKey) : message.historyProofKey;
                            return object;
                        };

                        /**
                         * Converts this LedgerIdNodeContribution to JSON.
                         * @function toJSON
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        LedgerIdNodeContribution.prototype.toJSON = function() {
                            return LedgerIdNodeContribution.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the type url for LedgerIdNodeContribution
                         * @function getTypeUrl
                         * @memberof com.hedera.hapi.node.tss.LedgerIdNodeContribution
                         * @static
                         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                         * @returns {string} The type url
                         */
                        LedgerIdNodeContribution.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/com.hedera.hapi.node.tss.LedgerIdNodeContribution";
                        };

                        return LedgerIdNodeContribution;
                    })();

                    return tss;
                })();

                return node;
            })();

            hapi.services = (function() {

                /**
                 * Namespace services.
                 * @memberof com.hedera.hapi
                 * @namespace
                 */
                const services = {};

                services.auxiliary = (function() {

                    /**
                     * Namespace auxiliary.
                     * @memberof com.hedera.hapi.services
                     * @namespace
                     */
                    const auxiliary = {};

                    auxiliary.hints = (function() {

                        /**
                         * Namespace hints.
                         * @memberof com.hedera.hapi.services.auxiliary
                         * @namespace
                         */
                        const hints = {};

                        hints.HintsKeyPublicationTransactionBody = (function() {

                            /**
                             * Properties of a HintsKeyPublicationTransactionBody.
                             * @typedef {Object} com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Properties
                             * @property {number|null} [partyId] The party id for which the hinTS key is being published;
                             * must be in the range [0, num_parties).
                             * <p>
                             * This value MUST be set to a non-negative integer.<br/>
                             * @property {number|null} [numParties] The number of parties in the hinTS scheme.
                             * @property {Uint8Array|null} [hintsKey] The party's hinTS key.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HintsKeyPublicationTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.hints
                             * @interface IHintsKeyPublicationTransactionBody
                             * @augments com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Properties
                             * @deprecated Use com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Properties instead.
                             */

                            /**
                             * Shape of a HintsKeyPublicationTransactionBody.
                             * @typedef {com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Properties} com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Shape
                             */

                            /**
                             * Constructs a new HintsKeyPublicationTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.hints
                             * @classdesc A transaction body to publish a node's hinTS key for a certain
                             * party id and number of parties. A hinTS key is an extended
                             * public key; that is, a BLS public key combined with "hints"
                             * derived from the matching private key that a signature
                             * aggregator can use to prove well-formedness of an aggregate
                             * public key by an efficiently verifiable SNARK.
                             * @constructor
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HintsKeyPublicationTransactionBody = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The party id for which the hinTS key is being published;
                             * must be in the range [0, num_parties).
                             * <p>
                             * This value MUST be set to a non-negative integer.<br/>
                             * @member {number} partyId
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @instance
                             */
                            HintsKeyPublicationTransactionBody.prototype.partyId = 0;

                            /**
                             * The number of parties in the hinTS scheme.
                             * @member {number} numParties
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @instance
                             */
                            HintsKeyPublicationTransactionBody.prototype.numParties = 0;

                            /**
                             * The party's hinTS key.
                             * @member {Uint8Array} hintsKey
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @instance
                             */
                            HintsKeyPublicationTransactionBody.prototype.hintsKey = $util.newBuffer([]);

                            /**
                             * Creates a new HintsKeyPublicationTransactionBody instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody} HintsKeyPublicationTransactionBody instance
                             * @type {{
                             *   (properties: com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Shape): com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody & com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Shape;
                             *   (properties?: com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Properties): com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody;
                             * }}
                             */
                            HintsKeyPublicationTransactionBody.create = function(properties) {
                                return new HintsKeyPublicationTransactionBody(properties);
                            };

                            /**
                             * Encodes the specified HintsKeyPublicationTransactionBody message. Does not implicitly {@link com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Properties} message HintsKeyPublicationTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsKeyPublicationTransactionBody.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.partyId != null && $Object.hasOwnProperty.call(message, "partyId") && message.partyId !== 0)
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.partyId);
                                if (message.numParties != null && $Object.hasOwnProperty.call(message, "numParties") && message.numParties !== 0)
                                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.numParties);
                                if (message.hintsKey != null && $Object.hasOwnProperty.call(message, "hintsKey") && message.hintsKey.length)
                                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.hintsKey);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HintsKeyPublicationTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Properties} message HintsKeyPublicationTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsKeyPublicationTransactionBody.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HintsKeyPublicationTransactionBody message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody & com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Shape} HintsKeyPublicationTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsKeyPublicationTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.uint32())
                                                message.partyId = value;
                                            else
                                                delete message.partyId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.uint32())
                                                message.numParties = value;
                                            else
                                                delete message.numParties;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.hintsKey = value;
                                            else
                                                delete message.hintsKey;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HintsKeyPublicationTransactionBody message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody & com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Shape} HintsKeyPublicationTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsKeyPublicationTransactionBody.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HintsKeyPublicationTransactionBody message.
                             * @function verify
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HintsKeyPublicationTransactionBody.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.partyId != null && $Object.hasOwnProperty.call(message, "partyId"))
                                    if (!$util.isInteger(message.partyId))
                                        return "partyId: integer expected";
                                if (message.numParties != null && $Object.hasOwnProperty.call(message, "numParties"))
                                    if (!$util.isInteger(message.numParties))
                                        return "numParties: integer expected";
                                if (message.hintsKey != null && $Object.hasOwnProperty.call(message, "hintsKey"))
                                    if (!(message.hintsKey && typeof message.hintsKey.length === "number" || $util.isString(message.hintsKey)))
                                        return "hintsKey: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a HintsKeyPublicationTransactionBody message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody} HintsKeyPublicationTransactionBody
                             */
                            HintsKeyPublicationTransactionBody.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody();
                                if (object.partyId != null)
                                    if ($Number(object.partyId) !== 0)
                                        message.partyId = object.partyId >>> 0;
                                if (object.numParties != null)
                                    if ($Number(object.numParties) !== 0)
                                        message.numParties = object.numParties >>> 0;
                                if (object.hintsKey != null)
                                    if (object.hintsKey.length)
                                        if (typeof object.hintsKey === "string")
                                            $util.base64.decode(object.hintsKey, message.hintsKey = $util.newBuffer($util.base64.length(object.hintsKey)), 0);
                                        else if (object.hintsKey.length >= 0)
                                            message.hintsKey = object.hintsKey;
                                return message;
                            };

                            /**
                             * Creates a plain object from a HintsKeyPublicationTransactionBody message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody} message HintsKeyPublicationTransactionBody
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HintsKeyPublicationTransactionBody.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    object.partyId = 0;
                                    object.numParties = 0;
                                    if (options.bytes === $String)
                                        object.hintsKey = "";
                                    else {
                                        object.hintsKey = [];
                                        if (options.bytes !== $Array)
                                            object.hintsKey = $util.newBuffer(object.hintsKey);
                                    }
                                }
                                if (message.partyId != null && $Object.hasOwnProperty.call(message, "partyId"))
                                    object.partyId = message.partyId;
                                if (message.numParties != null && $Object.hasOwnProperty.call(message, "numParties"))
                                    object.numParties = message.numParties;
                                if (message.hintsKey != null && $Object.hasOwnProperty.call(message, "hintsKey"))
                                    object.hintsKey = options.bytes === $String ? $util.base64.encode(message.hintsKey, 0, message.hintsKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hintsKey) : message.hintsKey;
                                return object;
                            };

                            /**
                             * Converts this HintsKeyPublicationTransactionBody to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HintsKeyPublicationTransactionBody.prototype.toJSON = function() {
                                return HintsKeyPublicationTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HintsKeyPublicationTransactionBody
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HintsKeyPublicationTransactionBody.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody";
                            };

                            return HintsKeyPublicationTransactionBody;
                        })();

                        hints.HintsPreprocessingVoteTransactionBody = (function() {

                            /**
                             * Properties of a HintsPreprocessingVoteTransactionBody.
                             * @typedef {Object} com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Properties
                             * @property {number|Long|null} [constructionId] The id of the hinTS construction this vote is for. Because
                             * the network only ever performs one construction a time, this
                             * is somewhat redundant, but included to detect any strange
                             * edge cases where a node is so far behind it votes on the wrong
                             * construction.
                             * @property {com.hedera.hapi.node.state.hints.PreprocessingVote.$Properties|null} [vote] The vote the node is casting for the aggregation of the hints
                             * in the given construction (which should be ongoing).
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HintsPreprocessingVoteTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.hints
                             * @interface IHintsPreprocessingVoteTransactionBody
                             * @augments com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Properties
                             * @deprecated Use com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Properties instead.
                             */

                            /**
                             * Shape of a HintsPreprocessingVoteTransactionBody.
                             * @typedef {{
                             *   constructionId?: number|Long|null;
                             *   vote?: com.hedera.hapi.node.state.hints.PreprocessingVote.$Shape|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * }} com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Shape
                             */

                            /**
                             * Constructs a new HintsPreprocessingVoteTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.hints
                             * @classdesc A transaction body to publish a node's vote for the results of
                             * aggregating the hinTS keys for a specified construction id. The
                             * node can either give their explicit aggregation results, or
                             * delegate their vote to another node's results.
                             * @constructor
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HintsPreprocessingVoteTransactionBody = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The id of the hinTS construction this vote is for. Because
                             * the network only ever performs one construction a time, this
                             * is somewhat redundant, but included to detect any strange
                             * edge cases where a node is so far behind it votes on the wrong
                             * construction.
                             * @member {number|Long} constructionId
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody
                             * @instance
                             */
                            HintsPreprocessingVoteTransactionBody.prototype.constructionId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The vote the node is casting for the aggregation of the hints
                             * in the given construction (which should be ongoing).
                             * @member {com.hedera.hapi.node.state.hints.PreprocessingVote.$Properties|null|undefined} vote
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody
                             * @instance
                             */
                            HintsPreprocessingVoteTransactionBody.prototype.vote = null;

                            /**
                             * Creates a new HintsPreprocessingVoteTransactionBody instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody} HintsPreprocessingVoteTransactionBody instance
                             * @type {{
                             *   (properties: com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Shape): com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody & com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Shape;
                             *   (properties?: com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Properties): com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody;
                             * }}
                             */
                            HintsPreprocessingVoteTransactionBody.create = function(properties) {
                                return new HintsPreprocessingVoteTransactionBody(properties);
                            };

                            /**
                             * Encodes the specified HintsPreprocessingVoteTransactionBody message. Does not implicitly {@link com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Properties} message HintsPreprocessingVoteTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsPreprocessingVoteTransactionBody.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId") && (typeof message.constructionId === "object" ? message.constructionId.low || message.constructionId.high : message.constructionId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.constructionId);
                                if (message.vote != null && $Object.hasOwnProperty.call(message, "vote"))
                                    $root.com.hedera.hapi.node.state.hints.PreprocessingVote.encode(message.vote, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HintsPreprocessingVoteTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Properties} message HintsPreprocessingVoteTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsPreprocessingVoteTransactionBody.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HintsPreprocessingVoteTransactionBody message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody & com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Shape} HintsPreprocessingVoteTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsPreprocessingVoteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.constructionId = value;
                                            else
                                                delete message.constructionId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.vote = $root.com.hedera.hapi.node.state.hints.PreprocessingVote.decode(reader, reader.uint32(), $undefined, _depth + 1, message.vote);
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HintsPreprocessingVoteTransactionBody message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody & com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Shape} HintsPreprocessingVoteTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsPreprocessingVoteTransactionBody.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HintsPreprocessingVoteTransactionBody message.
                             * @function verify
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HintsPreprocessingVoteTransactionBody.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (!$util.isInteger(message.constructionId) && !(message.constructionId && $util.isInteger(message.constructionId.low) && $util.isInteger(message.constructionId.high)))
                                        return "constructionId: integer|Long expected";
                                if (message.vote != null && $Object.hasOwnProperty.call(message, "vote")) {
                                    let error = $root.com.hedera.hapi.node.state.hints.PreprocessingVote.verify(message.vote, _depth + 1);
                                    if (error)
                                        return "vote." + error;
                                }
                                return null;
                            };

                            /**
                             * Creates a HintsPreprocessingVoteTransactionBody message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody} HintsPreprocessingVoteTransactionBody
                             */
                            HintsPreprocessingVoteTransactionBody.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody();
                                if (object.constructionId != null)
                                    if (typeof object.constructionId === "object" ? object.constructionId.low || object.constructionId.high : $Number(object.constructionId) !== 0)
                                        if ($util.Long)
                                            message.constructionId = $util.Long.fromValue(object.constructionId, true);
                                        else if (typeof object.constructionId === "string")
                                            message.constructionId = $parseInt(object.constructionId, 10);
                                        else if (typeof object.constructionId === "number")
                                            message.constructionId = object.constructionId;
                                        else if (typeof object.constructionId === "object")
                                            message.constructionId = new $util.LongBits(object.constructionId.low >>> 0, object.constructionId.high >>> 0).toNumber(true);
                                if (object.vote != null) {
                                    if (!$util.isObject(object.vote))
                                        throw $TypeError(".com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.vote: object expected");
                                    message.vote = $root.com.hedera.hapi.node.state.hints.PreprocessingVote.fromObject(object.vote, _depth + 1);
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a HintsPreprocessingVoteTransactionBody message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody} message HintsPreprocessingVoteTransactionBody
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HintsPreprocessingVoteTransactionBody.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.constructionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.constructionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    object.vote = null;
                                }
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.constructionId = typeof message.constructionId === "number" ? $BigInt(message.constructionId) : $util.Long.fromBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0, true).toBigInt();
                                    else if (typeof message.constructionId === "number")
                                        object.constructionId = options.longs === $String ? $String(message.constructionId) : message.constructionId;
                                    else
                                        object.constructionId = options.longs === $String ? $util.Long.prototype.toString.call(message.constructionId) : options.longs === $Number ? new $util.LongBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0).toNumber(true) : message.constructionId;
                                if (message.vote != null && $Object.hasOwnProperty.call(message, "vote"))
                                    object.vote = $root.com.hedera.hapi.node.state.hints.PreprocessingVote.toObject(message.vote, options, _depth + 1);
                                return object;
                            };

                            /**
                             * Converts this HintsPreprocessingVoteTransactionBody to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HintsPreprocessingVoteTransactionBody.prototype.toJSON = function() {
                                return HintsPreprocessingVoteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HintsPreprocessingVoteTransactionBody
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HintsPreprocessingVoteTransactionBody.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody";
                            };

                            return HintsPreprocessingVoteTransactionBody;
                        })();

                        hints.HintsPartialSignatureTransactionBody = (function() {

                            /**
                             * Properties of a HintsPartialSignatureTransactionBody.
                             * @typedef {Object} com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Properties
                             * @property {number|Long|null} [constructionId] The id of the hinTS construction this partial signature applies to.
                             * (The size of the target roster for this construction implies what
                             * hinTS party id the submitting node has in the construction; and
                             * hence also what weight the node's signature has.)
                             * @property {Uint8Array|null} [message] The message being signed.
                             * @property {Uint8Array|null} [partialSignature] The node's partial signature on the message.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HintsPartialSignatureTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.hints
                             * @interface IHintsPartialSignatureTransactionBody
                             * @augments com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Properties
                             * @deprecated Use com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Properties instead.
                             */

                            /**
                             * Shape of a HintsPartialSignatureTransactionBody.
                             * @typedef {com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Properties} com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Shape
                             */

                            /**
                             * Constructs a new HintsPartialSignatureTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.hints
                             * @classdesc A transaction body to publish a node's hinTS partial signature on
                             * a particular message for a certain construction id.
                             * @constructor
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HintsPartialSignatureTransactionBody = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The id of the hinTS construction this partial signature applies to.
                             * (The size of the target roster for this construction implies what
                             * hinTS party id the submitting node has in the construction; and
                             * hence also what weight the node's signature has.)
                             * @member {number|Long} constructionId
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @instance
                             */
                            HintsPartialSignatureTransactionBody.prototype.constructionId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The message being signed.
                             * @member {Uint8Array} message
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @instance
                             */
                            HintsPartialSignatureTransactionBody.prototype.message = $util.newBuffer([]);

                            /**
                             * The node's partial signature on the message.
                             * @member {Uint8Array} partialSignature
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @instance
                             */
                            HintsPartialSignatureTransactionBody.prototype.partialSignature = $util.newBuffer([]);

                            /**
                             * Creates a new HintsPartialSignatureTransactionBody instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody} HintsPartialSignatureTransactionBody instance
                             * @type {{
                             *   (properties: com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Shape): com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody & com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Shape;
                             *   (properties?: com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Properties): com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody;
                             * }}
                             */
                            HintsPartialSignatureTransactionBody.create = function(properties) {
                                return new HintsPartialSignatureTransactionBody(properties);
                            };

                            /**
                             * Encodes the specified HintsPartialSignatureTransactionBody message. Does not implicitly {@link com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Properties} message HintsPartialSignatureTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsPartialSignatureTransactionBody.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId") && (typeof message.constructionId === "object" ? message.constructionId.low || message.constructionId.high : message.constructionId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.constructionId);
                                if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.message);
                                if (message.partialSignature != null && $Object.hasOwnProperty.call(message, "partialSignature") && message.partialSignature.length)
                                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.partialSignature);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HintsPartialSignatureTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Properties} message HintsPartialSignatureTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HintsPartialSignatureTransactionBody.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HintsPartialSignatureTransactionBody message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody & com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Shape} HintsPartialSignatureTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsPartialSignatureTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.constructionId = value;
                                            else
                                                delete message.constructionId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.message = value;
                                            else
                                                delete message.message;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.partialSignature = value;
                                            else
                                                delete message.partialSignature;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HintsPartialSignatureTransactionBody message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody & com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Shape} HintsPartialSignatureTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HintsPartialSignatureTransactionBody.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HintsPartialSignatureTransactionBody message.
                             * @function verify
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HintsPartialSignatureTransactionBody.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (!$util.isInteger(message.constructionId) && !(message.constructionId && $util.isInteger(message.constructionId.low) && $util.isInteger(message.constructionId.high)))
                                        return "constructionId: integer|Long expected";
                                if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                                    if (!(message.message && typeof message.message.length === "number" || $util.isString(message.message)))
                                        return "message: buffer expected";
                                if (message.partialSignature != null && $Object.hasOwnProperty.call(message, "partialSignature"))
                                    if (!(message.partialSignature && typeof message.partialSignature.length === "number" || $util.isString(message.partialSignature)))
                                        return "partialSignature: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a HintsPartialSignatureTransactionBody message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody} HintsPartialSignatureTransactionBody
                             */
                            HintsPartialSignatureTransactionBody.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody();
                                if (object.constructionId != null)
                                    if (typeof object.constructionId === "object" ? object.constructionId.low || object.constructionId.high : $Number(object.constructionId) !== 0)
                                        if ($util.Long)
                                            message.constructionId = $util.Long.fromValue(object.constructionId, true);
                                        else if (typeof object.constructionId === "string")
                                            message.constructionId = $parseInt(object.constructionId, 10);
                                        else if (typeof object.constructionId === "number")
                                            message.constructionId = object.constructionId;
                                        else if (typeof object.constructionId === "object")
                                            message.constructionId = new $util.LongBits(object.constructionId.low >>> 0, object.constructionId.high >>> 0).toNumber(true);
                                if (object.message != null)
                                    if (object.message.length)
                                        if (typeof object.message === "string")
                                            $util.base64.decode(object.message, message.message = $util.newBuffer($util.base64.length(object.message)), 0);
                                        else if (object.message.length >= 0)
                                            message.message = object.message;
                                if (object.partialSignature != null)
                                    if (object.partialSignature.length)
                                        if (typeof object.partialSignature === "string")
                                            $util.base64.decode(object.partialSignature, message.partialSignature = $util.newBuffer($util.base64.length(object.partialSignature)), 0);
                                        else if (object.partialSignature.length >= 0)
                                            message.partialSignature = object.partialSignature;
                                return message;
                            };

                            /**
                             * Creates a plain object from a HintsPartialSignatureTransactionBody message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody} message HintsPartialSignatureTransactionBody
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HintsPartialSignatureTransactionBody.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.constructionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.constructionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if (options.bytes === $String)
                                        object.message = "";
                                    else {
                                        object.message = [];
                                        if (options.bytes !== $Array)
                                            object.message = $util.newBuffer(object.message);
                                    }
                                    if (options.bytes === $String)
                                        object.partialSignature = "";
                                    else {
                                        object.partialSignature = [];
                                        if (options.bytes !== $Array)
                                            object.partialSignature = $util.newBuffer(object.partialSignature);
                                    }
                                }
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.constructionId = typeof message.constructionId === "number" ? $BigInt(message.constructionId) : $util.Long.fromBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0, true).toBigInt();
                                    else if (typeof message.constructionId === "number")
                                        object.constructionId = options.longs === $String ? $String(message.constructionId) : message.constructionId;
                                    else
                                        object.constructionId = options.longs === $String ? $util.Long.prototype.toString.call(message.constructionId) : options.longs === $Number ? new $util.LongBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0).toNumber(true) : message.constructionId;
                                if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                                    object.message = options.bytes === $String ? $util.base64.encode(message.message, 0, message.message.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.message) : message.message;
                                if (message.partialSignature != null && $Object.hasOwnProperty.call(message, "partialSignature"))
                                    object.partialSignature = options.bytes === $String ? $util.base64.encode(message.partialSignature, 0, message.partialSignature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.partialSignature) : message.partialSignature;
                                return object;
                            };

                            /**
                             * Converts this HintsPartialSignatureTransactionBody to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HintsPartialSignatureTransactionBody.prototype.toJSON = function() {
                                return HintsPartialSignatureTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HintsPartialSignatureTransactionBody
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HintsPartialSignatureTransactionBody.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody";
                            };

                            return HintsPartialSignatureTransactionBody;
                        })();

                        hints.CrsPublicationTransactionBody = (function() {

                            /**
                             * Properties of a CrsPublicationTransactionBody.
                             * @typedef {Object} com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties
                             * @property {Uint8Array|null} [newCrs] The party's generated random CRS.
                             * @property {Uint8Array|null} [proof] The party's proof of the CRS update.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a CrsPublicationTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.hints
                             * @interface ICrsPublicationTransactionBody
                             * @augments com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties
                             * @deprecated Use com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties instead.
                             */

                            /**
                             * Shape of a CrsPublicationTransactionBody.
                             * @typedef {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties} com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Shape
                             */

                            /**
                             * Constructs a new CrsPublicationTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.hints
                             * @classdesc A transaction body to publish a node's Common Reference String (CRS) to the network. This is used
                             * when node wants to update to reach the final CRS for network to use the hinTS protocol
                             * @constructor
                             * @param {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const CrsPublicationTransactionBody = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The party's generated random CRS.
                             * @member {Uint8Array} newCrs
                             * @memberof com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody
                             * @instance
                             */
                            CrsPublicationTransactionBody.prototype.newCrs = $util.newBuffer([]);

                            /**
                             * The party's proof of the CRS update.
                             * @member {Uint8Array} proof
                             * @memberof com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody
                             * @instance
                             */
                            CrsPublicationTransactionBody.prototype.proof = $util.newBuffer([]);

                            /**
                             * Creates a new CrsPublicationTransactionBody instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody} CrsPublicationTransactionBody instance
                             * @type {{
                             *   (properties: com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Shape): com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody & com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Shape;
                             *   (properties?: com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties): com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody;
                             * }}
                             */
                            CrsPublicationTransactionBody.create = function(properties) {
                                return new CrsPublicationTransactionBody(properties);
                            };

                            /**
                             * Encodes the specified CrsPublicationTransactionBody message. Does not implicitly {@link com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties} message CrsPublicationTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            CrsPublicationTransactionBody.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.newCrs != null && $Object.hasOwnProperty.call(message, "newCrs") && message.newCrs.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.newCrs);
                                if (message.proof != null && $Object.hasOwnProperty.call(message, "proof") && message.proof.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.proof);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified CrsPublicationTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties} message CrsPublicationTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            CrsPublicationTransactionBody.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a CrsPublicationTransactionBody message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody & com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Shape} CrsPublicationTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            CrsPublicationTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.newCrs = value;
                                            else
                                                delete message.newCrs;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.proof = value;
                                            else
                                                delete message.proof;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a CrsPublicationTransactionBody message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody & com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Shape} CrsPublicationTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            CrsPublicationTransactionBody.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a CrsPublicationTransactionBody message.
                             * @function verify
                             * @memberof com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            CrsPublicationTransactionBody.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.newCrs != null && $Object.hasOwnProperty.call(message, "newCrs"))
                                    if (!(message.newCrs && typeof message.newCrs.length === "number" || $util.isString(message.newCrs)))
                                        return "newCrs: buffer expected";
                                if (message.proof != null && $Object.hasOwnProperty.call(message, "proof"))
                                    if (!(message.proof && typeof message.proof.length === "number" || $util.isString(message.proof)))
                                        return "proof: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a CrsPublicationTransactionBody message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody} CrsPublicationTransactionBody
                             */
                            CrsPublicationTransactionBody.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody();
                                if (object.newCrs != null)
                                    if (object.newCrs.length)
                                        if (typeof object.newCrs === "string")
                                            $util.base64.decode(object.newCrs, message.newCrs = $util.newBuffer($util.base64.length(object.newCrs)), 0);
                                        else if (object.newCrs.length >= 0)
                                            message.newCrs = object.newCrs;
                                if (object.proof != null)
                                    if (object.proof.length)
                                        if (typeof object.proof === "string")
                                            $util.base64.decode(object.proof, message.proof = $util.newBuffer($util.base64.length(object.proof)), 0);
                                        else if (object.proof.length >= 0)
                                            message.proof = object.proof;
                                return message;
                            };

                            /**
                             * Creates a plain object from a CrsPublicationTransactionBody message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody} message CrsPublicationTransactionBody
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            CrsPublicationTransactionBody.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.newCrs = "";
                                    else {
                                        object.newCrs = [];
                                        if (options.bytes !== $Array)
                                            object.newCrs = $util.newBuffer(object.newCrs);
                                    }
                                    if (options.bytes === $String)
                                        object.proof = "";
                                    else {
                                        object.proof = [];
                                        if (options.bytes !== $Array)
                                            object.proof = $util.newBuffer(object.proof);
                                    }
                                }
                                if (message.newCrs != null && $Object.hasOwnProperty.call(message, "newCrs"))
                                    object.newCrs = options.bytes === $String ? $util.base64.encode(message.newCrs, 0, message.newCrs.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.newCrs) : message.newCrs;
                                if (message.proof != null && $Object.hasOwnProperty.call(message, "proof"))
                                    object.proof = options.bytes === $String ? $util.base64.encode(message.proof, 0, message.proof.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.proof) : message.proof;
                                return object;
                            };

                            /**
                             * Converts this CrsPublicationTransactionBody to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            CrsPublicationTransactionBody.prototype.toJSON = function() {
                                return CrsPublicationTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for CrsPublicationTransactionBody
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            CrsPublicationTransactionBody.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody";
                            };

                            return CrsPublicationTransactionBody;
                        })();

                        return hints;
                    })();

                    auxiliary.history = (function() {

                        /**
                         * Namespace history.
                         * @memberof com.hedera.hapi.services.auxiliary
                         * @namespace
                         */
                        const history = {};

                        history.HistoryProofSignatureTransactionBody = (function() {

                            /**
                             * Properties of a HistoryProofSignatureTransactionBody.
                             * @typedef {Object} com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Properties
                             * @property {number|Long|null} [constructionId] The id of the proof construction this signature is contributing to.
                             * @property {com.hedera.hapi.node.state.history.HistorySignature.$Properties|null} [signature] This node's signature on its computed history.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HistoryProofSignatureTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.history
                             * @interface IHistoryProofSignatureTransactionBody
                             * @augments com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Properties
                             * @deprecated Use com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Properties instead.
                             */

                            /**
                             * Shape of a HistoryProofSignatureTransactionBody.
                             * @typedef {com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Properties} com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Shape
                             */

                            /**
                             * Constructs a new HistoryProofSignatureTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.history
                             * @classdesc A transaction body to publish a node's signature on a computed
                             * history of address book hash and associated metadata. This
                             * signature can contribute to a proof that the history belongs to
                             * to the chain of trust for the ledger id.
                             * @constructor
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HistoryProofSignatureTransactionBody = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The id of the proof construction this signature is contributing to.
                             * @member {number|Long} constructionId
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody
                             * @instance
                             */
                            HistoryProofSignatureTransactionBody.prototype.constructionId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * This node's signature on its computed history.
                             * @member {com.hedera.hapi.node.state.history.HistorySignature.$Properties|null|undefined} signature
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody
                             * @instance
                             */
                            HistoryProofSignatureTransactionBody.prototype.signature = null;

                            /**
                             * Creates a new HistoryProofSignatureTransactionBody instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody} HistoryProofSignatureTransactionBody instance
                             * @type {{
                             *   (properties: com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Shape): com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody & com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Shape;
                             *   (properties?: com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Properties): com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody;
                             * }}
                             */
                            HistoryProofSignatureTransactionBody.create = function(properties) {
                                return new HistoryProofSignatureTransactionBody(properties);
                            };

                            /**
                             * Encodes the specified HistoryProofSignatureTransactionBody message. Does not implicitly {@link com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Properties} message HistoryProofSignatureTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistoryProofSignatureTransactionBody.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId") && (typeof message.constructionId === "object" ? message.constructionId.low || message.constructionId.high : message.constructionId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.constructionId);
                                if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                                    $root.com.hedera.hapi.node.state.history.HistorySignature.encode(message.signature, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HistoryProofSignatureTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Properties} message HistoryProofSignatureTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistoryProofSignatureTransactionBody.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HistoryProofSignatureTransactionBody message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody & com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Shape} HistoryProofSignatureTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistoryProofSignatureTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.constructionId = value;
                                            else
                                                delete message.constructionId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.signature = $root.com.hedera.hapi.node.state.history.HistorySignature.decode(reader, reader.uint32(), $undefined, _depth + 1, message.signature);
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HistoryProofSignatureTransactionBody message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody & com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Shape} HistoryProofSignatureTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistoryProofSignatureTransactionBody.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HistoryProofSignatureTransactionBody message.
                             * @function verify
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HistoryProofSignatureTransactionBody.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (!$util.isInteger(message.constructionId) && !(message.constructionId && $util.isInteger(message.constructionId.low) && $util.isInteger(message.constructionId.high)))
                                        return "constructionId: integer|Long expected";
                                if (message.signature != null && $Object.hasOwnProperty.call(message, "signature")) {
                                    let error = $root.com.hedera.hapi.node.state.history.HistorySignature.verify(message.signature, _depth + 1);
                                    if (error)
                                        return "signature." + error;
                                }
                                return null;
                            };

                            /**
                             * Creates a HistoryProofSignatureTransactionBody message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody} HistoryProofSignatureTransactionBody
                             */
                            HistoryProofSignatureTransactionBody.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody();
                                if (object.constructionId != null)
                                    if (typeof object.constructionId === "object" ? object.constructionId.low || object.constructionId.high : $Number(object.constructionId) !== 0)
                                        if ($util.Long)
                                            message.constructionId = $util.Long.fromValue(object.constructionId, true);
                                        else if (typeof object.constructionId === "string")
                                            message.constructionId = $parseInt(object.constructionId, 10);
                                        else if (typeof object.constructionId === "number")
                                            message.constructionId = object.constructionId;
                                        else if (typeof object.constructionId === "object")
                                            message.constructionId = new $util.LongBits(object.constructionId.low >>> 0, object.constructionId.high >>> 0).toNumber(true);
                                if (object.signature != null) {
                                    if (!$util.isObject(object.signature))
                                        throw $TypeError(".com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.signature: object expected");
                                    message.signature = $root.com.hedera.hapi.node.state.history.HistorySignature.fromObject(object.signature, _depth + 1);
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a HistoryProofSignatureTransactionBody message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody} message HistoryProofSignatureTransactionBody
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HistoryProofSignatureTransactionBody.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.constructionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.constructionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    object.signature = null;
                                }
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.constructionId = typeof message.constructionId === "number" ? $BigInt(message.constructionId) : $util.Long.fromBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0, true).toBigInt();
                                    else if (typeof message.constructionId === "number")
                                        object.constructionId = options.longs === $String ? $String(message.constructionId) : message.constructionId;
                                    else
                                        object.constructionId = options.longs === $String ? $util.Long.prototype.toString.call(message.constructionId) : options.longs === $Number ? new $util.LongBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0).toNumber(true) : message.constructionId;
                                if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                                    object.signature = $root.com.hedera.hapi.node.state.history.HistorySignature.toObject(message.signature, options, _depth + 1);
                                return object;
                            };

                            /**
                             * Converts this HistoryProofSignatureTransactionBody to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HistoryProofSignatureTransactionBody.prototype.toJSON = function() {
                                return HistoryProofSignatureTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HistoryProofSignatureTransactionBody
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HistoryProofSignatureTransactionBody.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody";
                            };

                            return HistoryProofSignatureTransactionBody;
                        })();

                        history.HistoryProofKeyPublicationTransactionBody = (function() {

                            /**
                             * Properties of a HistoryProofKeyPublicationTransactionBody.
                             * @typedef {Object} com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Properties
                             * @property {Uint8Array|null} [proofKey] The proof key the submitting node intends to use when
                             * contributing WRAPS messages for use in proving history
                             * belongs to the chain of trust for the ledger id.
                             * @property {Uint8Array|null} [wrapsMessage] A WRAPS message from the submitting node.
                             * @property {number|Long|null} [constructionId] The id of the proof construction this publication is for.
                             * @property {com.hedera.hapi.node.state.history.WrapsPhase|null} [phase] The phase of the proof construction this key is being
                             * published for.
                             * @property {"proofKey"|"wrapsMessage"} [data] HistoryProofKeyPublicationTransactionBody data
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HistoryProofKeyPublicationTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.history
                             * @interface IHistoryProofKeyPublicationTransactionBody
                             * @augments com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Properties
                             * @deprecated Use com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Properties instead.
                             */

                            /**
                             * Narrowed shape of a HistoryProofKeyPublicationTransactionBody.
                             * @typedef {{
                             *   proofKey?: Uint8Array|null;
                             *   wrapsMessage?: Uint8Array|null;
                             *   constructionId?: number|Long|null;
                             *   phase?: com.hedera.hapi.node.state.history.WrapsPhase|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * } & (
                             *   ({ data?: undefined; proofKey?: null; wrapsMessage?: null }|{ data?: "proofKey"; proofKey: Uint8Array; wrapsMessage?: null }|{ data?: "wrapsMessage"; proofKey?: null; wrapsMessage: Uint8Array })
                             * )} com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Shape
                             */

                            /**
                             * Constructs a new HistoryProofKeyPublicationTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.history
                             * @classdesc A transaction body to publish a node's history proof key and
                             * WRAPS messages.
                             * @constructor
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HistoryProofKeyPublicationTransactionBody = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The proof key the submitting node intends to use when
                             * contributing WRAPS messages for use in proving history
                             * belongs to the chain of trust for the ledger id.
                             * @member {Uint8Array|null|undefined} proofKey
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @instance
                             */
                            HistoryProofKeyPublicationTransactionBody.prototype.proofKey = null;

                            /**
                             * A WRAPS message from the submitting node.
                             * @member {Uint8Array|null|undefined} wrapsMessage
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @instance
                             */
                            HistoryProofKeyPublicationTransactionBody.prototype.wrapsMessage = null;

                            /**
                             * The id of the proof construction this publication is for.
                             * @member {number|Long} constructionId
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @instance
                             */
                            HistoryProofKeyPublicationTransactionBody.prototype.constructionId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The phase of the proof construction this key is being
                             * published for.
                             * @member {com.hedera.hapi.node.state.history.WrapsPhase} phase
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @instance
                             */
                            HistoryProofKeyPublicationTransactionBody.prototype.phase = 0;

                            // OneOf field names bound to virtual getters and setters
                            let $oneOfFields;

                            /**
                             * HistoryProofKeyPublicationTransactionBody data.
                             * @member {"proofKey"|"wrapsMessage"|undefined} data
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @instance
                             */
                            $Object.defineProperty(HistoryProofKeyPublicationTransactionBody.prototype, "data", {
                                get: $util.oneOfGetter($oneOfFields = ["proofKey", "wrapsMessage"]),
                                set: $util.oneOfSetter($oneOfFields)
                            });

                            /**
                             * Creates a new HistoryProofKeyPublicationTransactionBody instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody} HistoryProofKeyPublicationTransactionBody instance
                             * @type {{
                             *   (properties: com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Shape): com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody & com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Shape;
                             *   (properties?: com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Properties): com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody;
                             * }}
                             */
                            HistoryProofKeyPublicationTransactionBody.create = function(properties) {
                                return new HistoryProofKeyPublicationTransactionBody(properties);
                            };

                            /**
                             * Encodes the specified HistoryProofKeyPublicationTransactionBody message. Does not implicitly {@link com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Properties} message HistoryProofKeyPublicationTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistoryProofKeyPublicationTransactionBody.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.proofKey != null && $Object.hasOwnProperty.call(message, "proofKey"))
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.proofKey);
                                if (message.wrapsMessage != null && $Object.hasOwnProperty.call(message, "wrapsMessage"))
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.wrapsMessage);
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId") && (typeof message.constructionId === "object" ? message.constructionId.low || message.constructionId.high : message.constructionId !== 0))
                                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.constructionId);
                                if (message.phase != null && $Object.hasOwnProperty.call(message, "phase") && message.phase !== 0)
                                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.phase);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HistoryProofKeyPublicationTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Properties} message HistoryProofKeyPublicationTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistoryProofKeyPublicationTransactionBody.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HistoryProofKeyPublicationTransactionBody message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody & com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Shape} HistoryProofKeyPublicationTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistoryProofKeyPublicationTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            message.proofKey = reader.bytes();
                                            message.data = "proofKey";
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.wrapsMessage = reader.bytes();
                                            message.data = "wrapsMessage";
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.constructionId = value;
                                            else
                                                delete message.constructionId;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 0)
                                                break;
                                            if (value = reader.int32())
                                                message.phase = value;
                                            else
                                                delete message.phase;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HistoryProofKeyPublicationTransactionBody message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody & com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Shape} HistoryProofKeyPublicationTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistoryProofKeyPublicationTransactionBody.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HistoryProofKeyPublicationTransactionBody message.
                             * @function verify
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HistoryProofKeyPublicationTransactionBody.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                let properties = {};
                                if (message.proofKey != null && $Object.hasOwnProperty.call(message, "proofKey")) {
                                    properties.data = 1;
                                    if (!(message.proofKey && typeof message.proofKey.length === "number" || $util.isString(message.proofKey)))
                                        return "proofKey: buffer expected";
                                }
                                if (message.wrapsMessage != null && $Object.hasOwnProperty.call(message, "wrapsMessage")) {
                                    if (properties.data === 1)
                                        return "data: multiple values";
                                    properties.data = 1;
                                    if (!(message.wrapsMessage && typeof message.wrapsMessage.length === "number" || $util.isString(message.wrapsMessage)))
                                        return "wrapsMessage: buffer expected";
                                }
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (!$util.isInteger(message.constructionId) && !(message.constructionId && $util.isInteger(message.constructionId.low) && $util.isInteger(message.constructionId.high)))
                                        return "constructionId: integer|Long expected";
                                if (message.phase != null && $Object.hasOwnProperty.call(message, "phase"))
                                    if (typeof message.phase !== "number" || (message.phase | 0) !== message.phase)
                                        return "phase: enum value expected";
                                return null;
                            };

                            /**
                             * Creates a HistoryProofKeyPublicationTransactionBody message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody} HistoryProofKeyPublicationTransactionBody
                             */
                            HistoryProofKeyPublicationTransactionBody.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody();
                                if (object.proofKey != null)
                                    if (typeof object.proofKey === "string")
                                        $util.base64.decode(object.proofKey, message.proofKey = $util.newBuffer($util.base64.length(object.proofKey)), 0);
                                    else if (object.proofKey.length >= 0)
                                        message.proofKey = object.proofKey;
                                if (object.wrapsMessage != null)
                                    if (typeof object.wrapsMessage === "string")
                                        $util.base64.decode(object.wrapsMessage, message.wrapsMessage = $util.newBuffer($util.base64.length(object.wrapsMessage)), 0);
                                    else if (object.wrapsMessage.length >= 0)
                                        message.wrapsMessage = object.wrapsMessage;
                                if (object.constructionId != null)
                                    if (typeof object.constructionId === "object" ? object.constructionId.low || object.constructionId.high : $Number(object.constructionId) !== 0)
                                        if ($util.Long)
                                            message.constructionId = $util.Long.fromValue(object.constructionId, true);
                                        else if (typeof object.constructionId === "string")
                                            message.constructionId = $parseInt(object.constructionId, 10);
                                        else if (typeof object.constructionId === "number")
                                            message.constructionId = object.constructionId;
                                        else if (typeof object.constructionId === "object")
                                            message.constructionId = new $util.LongBits(object.constructionId.low >>> 0, object.constructionId.high >>> 0).toNumber(true);
                                if (object.phase !== 0 && (typeof object.phase !== "string" || $root.com.hedera.hapi.node.state.history.WrapsPhase[object.phase] !== 0))
                                    switch (object.phase) {
                                    case "R1":
                                    case 0:
                                        message.phase = 0;
                                        break;
                                    case "R2":
                                    case 1:
                                        message.phase = 1;
                                        break;
                                    case "R3":
                                    case 2:
                                        message.phase = 2;
                                        break;
                                    case "AGGREGATE":
                                    case 3:
                                        message.phase = 3;
                                        break;
                                    case "POST_AGGREGATION":
                                    case 4:
                                        message.phase = 4;
                                        break;
                                    default:
                                        if (typeof object.phase === "number" && (object.phase | 0) === object.phase)
                                            message.phase = object.phase;
                                    }
                                return message;
                            };

                            /**
                             * Creates a plain object from a HistoryProofKeyPublicationTransactionBody message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody} message HistoryProofKeyPublicationTransactionBody
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HistoryProofKeyPublicationTransactionBody.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.constructionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.constructionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    object.phase = options.enums === $String ? "R1" : 0;
                                }
                                if (message.proofKey != null && $Object.hasOwnProperty.call(message, "proofKey")) {
                                    object.proofKey = options.bytes === $String ? $util.base64.encode(message.proofKey, 0, message.proofKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.proofKey) : message.proofKey;
                                    if (options.oneofs)
                                        object.data = "proofKey";
                                }
                                if (message.wrapsMessage != null && $Object.hasOwnProperty.call(message, "wrapsMessage")) {
                                    object.wrapsMessage = options.bytes === $String ? $util.base64.encode(message.wrapsMessage, 0, message.wrapsMessage.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.wrapsMessage) : message.wrapsMessage;
                                    if (options.oneofs)
                                        object.data = "wrapsMessage";
                                }
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.constructionId = typeof message.constructionId === "number" ? $BigInt(message.constructionId) : $util.Long.fromBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0, true).toBigInt();
                                    else if (typeof message.constructionId === "number")
                                        object.constructionId = options.longs === $String ? $String(message.constructionId) : message.constructionId;
                                    else
                                        object.constructionId = options.longs === $String ? $util.Long.prototype.toString.call(message.constructionId) : options.longs === $Number ? new $util.LongBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0).toNumber(true) : message.constructionId;
                                if (message.phase != null && $Object.hasOwnProperty.call(message, "phase"))
                                    object.phase = options.enums === $String ? $root.com.hedera.hapi.node.state.history.WrapsPhase[message.phase] === $undefined ? message.phase : $root.com.hedera.hapi.node.state.history.WrapsPhase[message.phase] : message.phase;
                                return object;
                            };

                            /**
                             * Converts this HistoryProofKeyPublicationTransactionBody to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HistoryProofKeyPublicationTransactionBody.prototype.toJSON = function() {
                                return HistoryProofKeyPublicationTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HistoryProofKeyPublicationTransactionBody
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HistoryProofKeyPublicationTransactionBody.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody";
                            };

                            return HistoryProofKeyPublicationTransactionBody;
                        })();

                        history.HistoryProofVoteTransactionBody = (function() {

                            /**
                             * Properties of a HistoryProofVoteTransactionBody.
                             * @typedef {Object} com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Properties
                             * @property {number|Long|null} [constructionId] The id of the proof construction this vote is for.
                             * @property {com.hedera.hapi.node.state.history.HistoryProofVote.$Properties|null} [vote] The submitting node's vote on the history proof.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a HistoryProofVoteTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.history
                             * @interface IHistoryProofVoteTransactionBody
                             * @augments com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Properties
                             * @deprecated Use com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Properties instead.
                             */

                            /**
                             * Shape of a HistoryProofVoteTransactionBody.
                             * @typedef {{
                             *   constructionId?: number|Long|null;
                             *   vote?: com.hedera.hapi.node.state.history.HistoryProofVote.$Shape|null;
                             *   $unknowns?: Array.<Uint8Array>;
                             * }} com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Shape
                             */

                            /**
                             * Constructs a new HistoryProofVoteTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.history
                             * @classdesc A transaction body to publish a node's vote for a
                             * proof of history associated to a construction id.
                             * @constructor
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const HistoryProofVoteTransactionBody = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The id of the proof construction this vote is for.
                             * @member {number|Long} constructionId
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody
                             * @instance
                             */
                            HistoryProofVoteTransactionBody.prototype.constructionId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * The submitting node's vote on the history proof.
                             * @member {com.hedera.hapi.node.state.history.HistoryProofVote.$Properties|null|undefined} vote
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody
                             * @instance
                             */
                            HistoryProofVoteTransactionBody.prototype.vote = null;

                            /**
                             * Creates a new HistoryProofVoteTransactionBody instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody} HistoryProofVoteTransactionBody instance
                             * @type {{
                             *   (properties: com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Shape): com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody & com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Shape;
                             *   (properties?: com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Properties): com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody;
                             * }}
                             */
                            HistoryProofVoteTransactionBody.create = function(properties) {
                                return new HistoryProofVoteTransactionBody(properties);
                            };

                            /**
                             * Encodes the specified HistoryProofVoteTransactionBody message. Does not implicitly {@link com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Properties} message HistoryProofVoteTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistoryProofVoteTransactionBody.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId") && (typeof message.constructionId === "object" ? message.constructionId.low || message.constructionId.high : message.constructionId !== 0))
                                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.constructionId);
                                if (message.vote != null && $Object.hasOwnProperty.call(message, "vote"))
                                    $root.com.hedera.hapi.node.state.history.HistoryProofVote.encode(message.vote, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified HistoryProofVoteTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Properties} message HistoryProofVoteTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            HistoryProofVoteTransactionBody.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a HistoryProofVoteTransactionBody message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody & com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Shape} HistoryProofVoteTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistoryProofVoteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.constructionId = value;
                                            else
                                                delete message.constructionId;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            message.vote = $root.com.hedera.hapi.node.state.history.HistoryProofVote.decode(reader, reader.uint32(), $undefined, _depth + 1, message.vote);
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a HistoryProofVoteTransactionBody message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody & com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Shape} HistoryProofVoteTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            HistoryProofVoteTransactionBody.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a HistoryProofVoteTransactionBody message.
                             * @function verify
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            HistoryProofVoteTransactionBody.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (!$util.isInteger(message.constructionId) && !(message.constructionId && $util.isInteger(message.constructionId.low) && $util.isInteger(message.constructionId.high)))
                                        return "constructionId: integer|Long expected";
                                if (message.vote != null && $Object.hasOwnProperty.call(message, "vote")) {
                                    let error = $root.com.hedera.hapi.node.state.history.HistoryProofVote.verify(message.vote, _depth + 1);
                                    if (error)
                                        return "vote." + error;
                                }
                                return null;
                            };

                            /**
                             * Creates a HistoryProofVoteTransactionBody message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody} HistoryProofVoteTransactionBody
                             */
                            HistoryProofVoteTransactionBody.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody();
                                if (object.constructionId != null)
                                    if (typeof object.constructionId === "object" ? object.constructionId.low || object.constructionId.high : $Number(object.constructionId) !== 0)
                                        if ($util.Long)
                                            message.constructionId = $util.Long.fromValue(object.constructionId, true);
                                        else if (typeof object.constructionId === "string")
                                            message.constructionId = $parseInt(object.constructionId, 10);
                                        else if (typeof object.constructionId === "number")
                                            message.constructionId = object.constructionId;
                                        else if (typeof object.constructionId === "object")
                                            message.constructionId = new $util.LongBits(object.constructionId.low >>> 0, object.constructionId.high >>> 0).toNumber(true);
                                if (object.vote != null) {
                                    if (!$util.isObject(object.vote))
                                        throw $TypeError(".com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.vote: object expected");
                                    message.vote = $root.com.hedera.hapi.node.state.history.HistoryProofVote.fromObject(object.vote, _depth + 1);
                                }
                                return message;
                            };

                            /**
                             * Creates a plain object from a HistoryProofVoteTransactionBody message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody} message HistoryProofVoteTransactionBody
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            HistoryProofVoteTransactionBody.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.constructionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.constructionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    object.vote = null;
                                }
                                if (message.constructionId != null && $Object.hasOwnProperty.call(message, "constructionId"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.constructionId = typeof message.constructionId === "number" ? $BigInt(message.constructionId) : $util.Long.fromBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0, true).toBigInt();
                                    else if (typeof message.constructionId === "number")
                                        object.constructionId = options.longs === $String ? $String(message.constructionId) : message.constructionId;
                                    else
                                        object.constructionId = options.longs === $String ? $util.Long.prototype.toString.call(message.constructionId) : options.longs === $Number ? new $util.LongBits(message.constructionId.low >>> 0, message.constructionId.high >>> 0).toNumber(true) : message.constructionId;
                                if (message.vote != null && $Object.hasOwnProperty.call(message, "vote"))
                                    object.vote = $root.com.hedera.hapi.node.state.history.HistoryProofVote.toObject(message.vote, options, _depth + 1);
                                return object;
                            };

                            /**
                             * Converts this HistoryProofVoteTransactionBody to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            HistoryProofVoteTransactionBody.prototype.toJSON = function() {
                                return HistoryProofVoteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for HistoryProofVoteTransactionBody
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            HistoryProofVoteTransactionBody.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody";
                            };

                            return HistoryProofVoteTransactionBody;
                        })();

                        return history;
                    })();

                    auxiliary.blockrecords = (function() {

                        /**
                         * Namespace blockrecords.
                         * @memberof com.hedera.hapi.services.auxiliary
                         * @namespace
                         */
                        const blockrecords = {};

                        blockrecords.MigrationRootHashVoteTransactionBody = (function() {

                            /**
                             * Properties of a MigrationRootHashVoteTransactionBody.
                             * @typedef {Object} com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties
                             * @property {Uint8Array|null} [previousWrappedRecordBlockRootHash] The previous wrapped record block root hash from startup migration.
                             * @property {Array.<Uint8Array>|null} [wrappedIntermediatePreviousBlockRootHashes] Intermediate hashing state for wrapped previous block roots.
                             * @property {number|Long|null} [wrappedIntermediateBlockRootsLeafCount] Leaf count of the wrapped previous block roots hasher.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a MigrationRootHashVoteTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords
                             * @interface IMigrationRootHashVoteTransactionBody
                             * @augments com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties
                             * @deprecated Use com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties instead.
                             */

                            /**
                             * Shape of a MigrationRootHashVoteTransactionBody.
                             * @typedef {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties} com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Shape
                             */

                            /**
                             * Constructs a new MigrationRootHashVoteTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords
                             * @classdesc A transaction body to publish a node's vote for wrapped record migration state.
                             * @constructor
                             * @param {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const MigrationRootHashVoteTransactionBody = function (properties) {
                                this.wrappedIntermediatePreviousBlockRootHashes = [];
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The previous wrapped record block root hash from startup migration.
                             * @member {Uint8Array} previousWrappedRecordBlockRootHash
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @instance
                             */
                            MigrationRootHashVoteTransactionBody.prototype.previousWrappedRecordBlockRootHash = $util.newBuffer([]);

                            /**
                             * Intermediate hashing state for wrapped previous block roots.
                             * @member {Array.<Uint8Array>} wrappedIntermediatePreviousBlockRootHashes
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @instance
                             */
                            MigrationRootHashVoteTransactionBody.prototype.wrappedIntermediatePreviousBlockRootHashes = $util.emptyArray;

                            /**
                             * Leaf count of the wrapped previous block roots hasher.
                             * @member {number|Long} wrappedIntermediateBlockRootsLeafCount
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @instance
                             */
                            MigrationRootHashVoteTransactionBody.prototype.wrappedIntermediateBlockRootsLeafCount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * Creates a new MigrationRootHashVoteTransactionBody instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody} MigrationRootHashVoteTransactionBody instance
                             * @type {{
                             *   (properties: com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Shape): com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody & com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Shape;
                             *   (properties?: com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties): com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody;
                             * }}
                             */
                            MigrationRootHashVoteTransactionBody.create = function(properties) {
                                return new MigrationRootHashVoteTransactionBody(properties);
                            };

                            /**
                             * Encodes the specified MigrationRootHashVoteTransactionBody message. Does not implicitly {@link com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties} message MigrationRootHashVoteTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            MigrationRootHashVoteTransactionBody.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.previousWrappedRecordBlockRootHash != null && $Object.hasOwnProperty.call(message, "previousWrappedRecordBlockRootHash") && message.previousWrappedRecordBlockRootHash.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.previousWrappedRecordBlockRootHash);
                                if (message.wrappedIntermediatePreviousBlockRootHashes != null && message.wrappedIntermediatePreviousBlockRootHashes.length)
                                    for (let i = 0; i < message.wrappedIntermediatePreviousBlockRootHashes.length; ++i)
                                        writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.wrappedIntermediatePreviousBlockRootHashes[i]);
                                if (message.wrappedIntermediateBlockRootsLeafCount != null && $Object.hasOwnProperty.call(message, "wrappedIntermediateBlockRootsLeafCount") && (typeof message.wrappedIntermediateBlockRootsLeafCount === "object" ? message.wrappedIntermediateBlockRootsLeafCount.low || message.wrappedIntermediateBlockRootsLeafCount.high : message.wrappedIntermediateBlockRootsLeafCount !== 0))
                                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.wrappedIntermediateBlockRootsLeafCount);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified MigrationRootHashVoteTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties} message MigrationRootHashVoteTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            MigrationRootHashVoteTransactionBody.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a MigrationRootHashVoteTransactionBody message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody & com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Shape} MigrationRootHashVoteTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            MigrationRootHashVoteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.previousWrappedRecordBlockRootHash = value;
                                            else
                                                delete message.previousWrappedRecordBlockRootHash;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if (!(message.wrappedIntermediatePreviousBlockRootHashes && message.wrappedIntermediatePreviousBlockRootHashes.length))
                                                message.wrappedIntermediatePreviousBlockRootHashes = [];
                                            message.wrappedIntermediatePreviousBlockRootHashes.push(reader.bytes());
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.wrappedIntermediateBlockRootsLeafCount = value;
                                            else
                                                delete message.wrappedIntermediateBlockRootsLeafCount;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a MigrationRootHashVoteTransactionBody message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody & com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Shape} MigrationRootHashVoteTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            MigrationRootHashVoteTransactionBody.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a MigrationRootHashVoteTransactionBody message.
                             * @function verify
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            MigrationRootHashVoteTransactionBody.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.previousWrappedRecordBlockRootHash != null && $Object.hasOwnProperty.call(message, "previousWrappedRecordBlockRootHash"))
                                    if (!(message.previousWrappedRecordBlockRootHash && typeof message.previousWrappedRecordBlockRootHash.length === "number" || $util.isString(message.previousWrappedRecordBlockRootHash)))
                                        return "previousWrappedRecordBlockRootHash: buffer expected";
                                if (message.wrappedIntermediatePreviousBlockRootHashes != null && $Object.hasOwnProperty.call(message, "wrappedIntermediatePreviousBlockRootHashes")) {
                                    if (!$Array.isArray(message.wrappedIntermediatePreviousBlockRootHashes))
                                        return "wrappedIntermediatePreviousBlockRootHashes: array expected";
                                    for (let i = 0; i < message.wrappedIntermediatePreviousBlockRootHashes.length; ++i)
                                        if (!(message.wrappedIntermediatePreviousBlockRootHashes[i] && typeof message.wrappedIntermediatePreviousBlockRootHashes[i].length === "number" || $util.isString(message.wrappedIntermediatePreviousBlockRootHashes[i])))
                                            return "wrappedIntermediatePreviousBlockRootHashes: buffer[] expected";
                                }
                                if (message.wrappedIntermediateBlockRootsLeafCount != null && $Object.hasOwnProperty.call(message, "wrappedIntermediateBlockRootsLeafCount"))
                                    if (!$util.isInteger(message.wrappedIntermediateBlockRootsLeafCount) && !(message.wrappedIntermediateBlockRootsLeafCount && $util.isInteger(message.wrappedIntermediateBlockRootsLeafCount.low) && $util.isInteger(message.wrappedIntermediateBlockRootsLeafCount.high)))
                                        return "wrappedIntermediateBlockRootsLeafCount: integer|Long expected";
                                return null;
                            };

                            /**
                             * Creates a MigrationRootHashVoteTransactionBody message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody} MigrationRootHashVoteTransactionBody
                             */
                            MigrationRootHashVoteTransactionBody.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody();
                                if (object.previousWrappedRecordBlockRootHash != null)
                                    if (object.previousWrappedRecordBlockRootHash.length)
                                        if (typeof object.previousWrappedRecordBlockRootHash === "string")
                                            $util.base64.decode(object.previousWrappedRecordBlockRootHash, message.previousWrappedRecordBlockRootHash = $util.newBuffer($util.base64.length(object.previousWrappedRecordBlockRootHash)), 0);
                                        else if (object.previousWrappedRecordBlockRootHash.length >= 0)
                                            message.previousWrappedRecordBlockRootHash = object.previousWrappedRecordBlockRootHash;
                                if (object.wrappedIntermediatePreviousBlockRootHashes) {
                                    if (!$Array.isArray(object.wrappedIntermediatePreviousBlockRootHashes))
                                        throw $TypeError(".com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.wrappedIntermediatePreviousBlockRootHashes: array expected");
                                    message.wrappedIntermediatePreviousBlockRootHashes = $Array(object.wrappedIntermediatePreviousBlockRootHashes.length);
                                    for (let i = 0; i < object.wrappedIntermediatePreviousBlockRootHashes.length; ++i)
                                        if (typeof object.wrappedIntermediatePreviousBlockRootHashes[i] === "string")
                                            $util.base64.decode(object.wrappedIntermediatePreviousBlockRootHashes[i], message.wrappedIntermediatePreviousBlockRootHashes[i] = $util.newBuffer($util.base64.length(object.wrappedIntermediatePreviousBlockRootHashes[i])), 0);
                                        else if (object.wrappedIntermediatePreviousBlockRootHashes[i].length >= 0)
                                            message.wrappedIntermediatePreviousBlockRootHashes[i] = object.wrappedIntermediatePreviousBlockRootHashes[i];
                                }
                                if (object.wrappedIntermediateBlockRootsLeafCount != null)
                                    if (typeof object.wrappedIntermediateBlockRootsLeafCount === "object" ? object.wrappedIntermediateBlockRootsLeafCount.low || object.wrappedIntermediateBlockRootsLeafCount.high : $Number(object.wrappedIntermediateBlockRootsLeafCount) !== 0)
                                        if ($util.Long)
                                            message.wrappedIntermediateBlockRootsLeafCount = $util.Long.fromValue(object.wrappedIntermediateBlockRootsLeafCount, true);
                                        else if (typeof object.wrappedIntermediateBlockRootsLeafCount === "string")
                                            message.wrappedIntermediateBlockRootsLeafCount = $parseInt(object.wrappedIntermediateBlockRootsLeafCount, 10);
                                        else if (typeof object.wrappedIntermediateBlockRootsLeafCount === "number")
                                            message.wrappedIntermediateBlockRootsLeafCount = object.wrappedIntermediateBlockRootsLeafCount;
                                        else if (typeof object.wrappedIntermediateBlockRootsLeafCount === "object")
                                            message.wrappedIntermediateBlockRootsLeafCount = new $util.LongBits(object.wrappedIntermediateBlockRootsLeafCount.low >>> 0, object.wrappedIntermediateBlockRootsLeafCount.high >>> 0).toNumber(true);
                                return message;
                            };

                            /**
                             * Creates a plain object from a MigrationRootHashVoteTransactionBody message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody} message MigrationRootHashVoteTransactionBody
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            MigrationRootHashVoteTransactionBody.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.arrays || options.defaults)
                                    object.wrappedIntermediatePreviousBlockRootHashes = [];
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.previousWrappedRecordBlockRootHash = "";
                                    else {
                                        object.previousWrappedRecordBlockRootHash = [];
                                        if (options.bytes !== $Array)
                                            object.previousWrappedRecordBlockRootHash = $util.newBuffer(object.previousWrappedRecordBlockRootHash);
                                    }
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.wrappedIntermediateBlockRootsLeafCount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.wrappedIntermediateBlockRootsLeafCount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                }
                                if (message.previousWrappedRecordBlockRootHash != null && $Object.hasOwnProperty.call(message, "previousWrappedRecordBlockRootHash"))
                                    object.previousWrappedRecordBlockRootHash = options.bytes === $String ? $util.base64.encode(message.previousWrappedRecordBlockRootHash, 0, message.previousWrappedRecordBlockRootHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.previousWrappedRecordBlockRootHash) : message.previousWrappedRecordBlockRootHash;
                                if (message.wrappedIntermediatePreviousBlockRootHashes && message.wrappedIntermediatePreviousBlockRootHashes.length) {
                                    object.wrappedIntermediatePreviousBlockRootHashes = $Array(message.wrappedIntermediatePreviousBlockRootHashes.length);
                                    for (let j = 0; j < message.wrappedIntermediatePreviousBlockRootHashes.length; ++j)
                                        object.wrappedIntermediatePreviousBlockRootHashes[j] = options.bytes === $String ? $util.base64.encode(message.wrappedIntermediatePreviousBlockRootHashes[j], 0, message.wrappedIntermediatePreviousBlockRootHashes[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.wrappedIntermediatePreviousBlockRootHashes[j]) : message.wrappedIntermediatePreviousBlockRootHashes[j];
                                }
                                if (message.wrappedIntermediateBlockRootsLeafCount != null && $Object.hasOwnProperty.call(message, "wrappedIntermediateBlockRootsLeafCount"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.wrappedIntermediateBlockRootsLeafCount = typeof message.wrappedIntermediateBlockRootsLeafCount === "number" ? $BigInt(message.wrappedIntermediateBlockRootsLeafCount) : $util.Long.fromBits(message.wrappedIntermediateBlockRootsLeafCount.low >>> 0, message.wrappedIntermediateBlockRootsLeafCount.high >>> 0, true).toBigInt();
                                    else if (typeof message.wrappedIntermediateBlockRootsLeafCount === "number")
                                        object.wrappedIntermediateBlockRootsLeafCount = options.longs === $String ? $String(message.wrappedIntermediateBlockRootsLeafCount) : message.wrappedIntermediateBlockRootsLeafCount;
                                    else
                                        object.wrappedIntermediateBlockRootsLeafCount = options.longs === $String ? $util.Long.prototype.toString.call(message.wrappedIntermediateBlockRootsLeafCount) : options.longs === $Number ? new $util.LongBits(message.wrappedIntermediateBlockRootsLeafCount.low >>> 0, message.wrappedIntermediateBlockRootsLeafCount.high >>> 0).toNumber(true) : message.wrappedIntermediateBlockRootsLeafCount;
                                return object;
                            };

                            /**
                             * Converts this MigrationRootHashVoteTransactionBody to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            MigrationRootHashVoteTransactionBody.prototype.toJSON = function() {
                                return MigrationRootHashVoteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for MigrationRootHashVoteTransactionBody
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            MigrationRootHashVoteTransactionBody.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody";
                            };

                            return MigrationRootHashVoteTransactionBody;
                        })();

                        return blockrecords;
                    })();

                    auxiliary.tss = (function() {

                        /**
                         * Namespace tss.
                         * @memberof com.hedera.hapi.services.auxiliary
                         * @namespace
                         */
                        const tss = {};

                        tss.TssEncryptionKeyTransactionBody = (function() {

                            /**
                             * Properties of a TssEncryptionKeyTransactionBody.
                             * @typedef {Object} com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Properties
                             * @property {Uint8Array|null} [publicTssEncryptionKey] The raw bytes of the public TSS encryption key of the node sending the transaction.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a TssEncryptionKeyTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.tss
                             * @interface ITssEncryptionKeyTransactionBody
                             * @augments com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Properties
                             * @deprecated Use com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Properties instead.
                             */

                            /**
                             * Shape of a TssEncryptionKeyTransactionBody.
                             * @typedef {com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Properties} com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Shape
                             */

                            /**
                             * Constructs a new TssEncryptionKeyTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.tss
                             * @classdesc A transaction body for sending the public TSS encryption key.
                             * @constructor
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const TssEncryptionKeyTransactionBody = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * The raw bytes of the public TSS encryption key of the node sending the transaction.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * @member {Uint8Array} publicTssEncryptionKey
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody
                             * @instance
                             */
                            TssEncryptionKeyTransactionBody.prototype.publicTssEncryptionKey = $util.newBuffer([]);

                            /**
                             * Creates a new TssEncryptionKeyTransactionBody instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody} TssEncryptionKeyTransactionBody instance
                             * @type {{
                             *   (properties: com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Shape): com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody & com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Shape;
                             *   (properties?: com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Properties): com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody;
                             * }}
                             */
                            TssEncryptionKeyTransactionBody.create = function(properties) {
                                return new TssEncryptionKeyTransactionBody(properties);
                            };

                            /**
                             * Encodes the specified TssEncryptionKeyTransactionBody message. Does not implicitly {@link com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Properties} message TssEncryptionKeyTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            TssEncryptionKeyTransactionBody.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.publicTssEncryptionKey != null && $Object.hasOwnProperty.call(message, "publicTssEncryptionKey") && message.publicTssEncryptionKey.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.publicTssEncryptionKey);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified TssEncryptionKeyTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Properties} message TssEncryptionKeyTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            TssEncryptionKeyTransactionBody.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a TssEncryptionKeyTransactionBody message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody & com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Shape} TssEncryptionKeyTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            TssEncryptionKeyTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.publicTssEncryptionKey = value;
                                            else
                                                delete message.publicTssEncryptionKey;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a TssEncryptionKeyTransactionBody message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody & com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody.$Shape} TssEncryptionKeyTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            TssEncryptionKeyTransactionBody.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a TssEncryptionKeyTransactionBody message.
                             * @function verify
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            TssEncryptionKeyTransactionBody.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.publicTssEncryptionKey != null && $Object.hasOwnProperty.call(message, "publicTssEncryptionKey"))
                                    if (!(message.publicTssEncryptionKey && typeof message.publicTssEncryptionKey.length === "number" || $util.isString(message.publicTssEncryptionKey)))
                                        return "publicTssEncryptionKey: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a TssEncryptionKeyTransactionBody message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody} TssEncryptionKeyTransactionBody
                             */
                            TssEncryptionKeyTransactionBody.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody();
                                if (object.publicTssEncryptionKey != null)
                                    if (object.publicTssEncryptionKey.length)
                                        if (typeof object.publicTssEncryptionKey === "string")
                                            $util.base64.decode(object.publicTssEncryptionKey, message.publicTssEncryptionKey = $util.newBuffer($util.base64.length(object.publicTssEncryptionKey)), 0);
                                        else if (object.publicTssEncryptionKey.length >= 0)
                                            message.publicTssEncryptionKey = object.publicTssEncryptionKey;
                                return message;
                            };

                            /**
                             * Creates a plain object from a TssEncryptionKeyTransactionBody message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody} message TssEncryptionKeyTransactionBody
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            TssEncryptionKeyTransactionBody.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults)
                                    if (options.bytes === $String)
                                        object.publicTssEncryptionKey = "";
                                    else {
                                        object.publicTssEncryptionKey = [];
                                        if (options.bytes !== $Array)
                                            object.publicTssEncryptionKey = $util.newBuffer(object.publicTssEncryptionKey);
                                    }
                                if (message.publicTssEncryptionKey != null && $Object.hasOwnProperty.call(message, "publicTssEncryptionKey"))
                                    object.publicTssEncryptionKey = options.bytes === $String ? $util.base64.encode(message.publicTssEncryptionKey, 0, message.publicTssEncryptionKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.publicTssEncryptionKey) : message.publicTssEncryptionKey;
                                return object;
                            };

                            /**
                             * Converts this TssEncryptionKeyTransactionBody to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            TssEncryptionKeyTransactionBody.prototype.toJSON = function() {
                                return TssEncryptionKeyTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for TssEncryptionKeyTransactionBody
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            TssEncryptionKeyTransactionBody.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.services.auxiliary.tss.TssEncryptionKeyTransactionBody";
                            };

                            return TssEncryptionKeyTransactionBody;
                        })();

                        tss.TssMessageTransactionBody = (function() {

                            /**
                             * Properties of a TssMessageTransactionBody.
                             * @typedef {Object} com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Properties
                             * @property {Uint8Array|null} [sourceRosterHash] A hash of the roster containing the node generating the TssMessage.<br/>
                             * This hash uniquely identifies the source roster, which will include
                             * an entry for the node generating this TssMessage.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * This value MUST contain a valid hash.
                             * @property {Uint8Array|null} [targetRosterHash] A hash of the roster that the TssMessage is for.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * This value MUST contain a valid hash.
                             * @property {number|Long|null} [shareIndex] An index to order shares.
                             * <p>
                             * A share index SHALL establish a global ordering of shares across all
                             * shares in the network.<br/>
                             * A share index MUST correspond to the index of the public share in the list
                             * returned from the TSS library when the share was created for the source
                             * roster.
                             * @property {Uint8Array|null} [tssMessage] A byte array.
                             * <p>
                             * This field SHALL contain the TssMessage data generated by the node
                             * for the specified `share_index`.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a TssMessageTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.tss
                             * @interface ITssMessageTransactionBody
                             * @augments com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Properties
                             * @deprecated Use com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Properties instead.
                             */

                            /**
                             * Shape of a TssMessageTransactionBody.
                             * @typedef {com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Properties} com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Shape
                             */

                            /**
                             * Constructs a new TssMessageTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.tss
                             * @classdesc A transaction body to to send a Threshold Signature Scheme (TSS)
                             * Message.<br/>
                             * This is a wrapper around several different TSS message types that a node
                             * might communicate with other nodes in the network.
                             * 
                             * - A `TssMessageTransactionBody` MUST identify the hash of the roster
                             * containing the node generating this TssMessage
                             * - A `TssMessageTransactionBody` MUST identify the hash of the roster that
                             * the TSS messages is for
                             * - A `TssMessageTransactionBody` SHALL contain the specificc TssMessage data
                             * that has been generated by the node for the share_index.
                             * @constructor
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const TssMessageTransactionBody = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * A hash of the roster containing the node generating the TssMessage.<br/>
                             * This hash uniquely identifies the source roster, which will include
                             * an entry for the node generating this TssMessage.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * This value MUST contain a valid hash.
                             * @member {Uint8Array} sourceRosterHash
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @instance
                             */
                            TssMessageTransactionBody.prototype.sourceRosterHash = $util.newBuffer([]);

                            /**
                             * A hash of the roster that the TssMessage is for.
                             * <p>
                             * This value MUST be set.<br/>
                             * This value MUST NOT be empty.<br/>
                             * This value MUST contain a valid hash.
                             * @member {Uint8Array} targetRosterHash
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @instance
                             */
                            TssMessageTransactionBody.prototype.targetRosterHash = $util.newBuffer([]);

                            /**
                             * An index to order shares.
                             * <p>
                             * A share index SHALL establish a global ordering of shares across all
                             * shares in the network.<br/>
                             * A share index MUST correspond to the index of the public share in the list
                             * returned from the TSS library when the share was created for the source
                             * roster.
                             * @member {number|Long} shareIndex
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @instance
                             */
                            TssMessageTransactionBody.prototype.shareIndex = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

                            /**
                             * A byte array.
                             * <p>
                             * This field SHALL contain the TssMessage data generated by the node
                             * for the specified `share_index`.
                             * @member {Uint8Array} tssMessage
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @instance
                             */
                            TssMessageTransactionBody.prototype.tssMessage = $util.newBuffer([]);

                            /**
                             * Creates a new TssMessageTransactionBody instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody} TssMessageTransactionBody instance
                             * @type {{
                             *   (properties: com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Shape): com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody & com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Shape;
                             *   (properties?: com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Properties): com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody;
                             * }}
                             */
                            TssMessageTransactionBody.create = function(properties) {
                                return new TssMessageTransactionBody(properties);
                            };

                            /**
                             * Encodes the specified TssMessageTransactionBody message. Does not implicitly {@link com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Properties} message TssMessageTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            TssMessageTransactionBody.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.sourceRosterHash != null && $Object.hasOwnProperty.call(message, "sourceRosterHash") && message.sourceRosterHash.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.sourceRosterHash);
                                if (message.targetRosterHash != null && $Object.hasOwnProperty.call(message, "targetRosterHash") && message.targetRosterHash.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.targetRosterHash);
                                if (message.shareIndex != null && $Object.hasOwnProperty.call(message, "shareIndex") && (typeof message.shareIndex === "object" ? message.shareIndex.low || message.shareIndex.high : message.shareIndex !== 0))
                                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.shareIndex);
                                if (message.tssMessage != null && $Object.hasOwnProperty.call(message, "tssMessage") && message.tssMessage.length)
                                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.tssMessage);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified TssMessageTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Properties} message TssMessageTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            TssMessageTransactionBody.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a TssMessageTransactionBody message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody & com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Shape} TssMessageTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            TssMessageTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.sourceRosterHash = value;
                                            else
                                                delete message.sourceRosterHash;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.targetRosterHash = value;
                                            else
                                                delete message.targetRosterHash;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 0)
                                                break;
                                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                                message.shareIndex = value;
                                            else
                                                delete message.shareIndex;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.tssMessage = value;
                                            else
                                                delete message.tssMessage;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a TssMessageTransactionBody message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody & com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody.$Shape} TssMessageTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            TssMessageTransactionBody.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a TssMessageTransactionBody message.
                             * @function verify
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            TssMessageTransactionBody.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.sourceRosterHash != null && $Object.hasOwnProperty.call(message, "sourceRosterHash"))
                                    if (!(message.sourceRosterHash && typeof message.sourceRosterHash.length === "number" || $util.isString(message.sourceRosterHash)))
                                        return "sourceRosterHash: buffer expected";
                                if (message.targetRosterHash != null && $Object.hasOwnProperty.call(message, "targetRosterHash"))
                                    if (!(message.targetRosterHash && typeof message.targetRosterHash.length === "number" || $util.isString(message.targetRosterHash)))
                                        return "targetRosterHash: buffer expected";
                                if (message.shareIndex != null && $Object.hasOwnProperty.call(message, "shareIndex"))
                                    if (!$util.isInteger(message.shareIndex) && !(message.shareIndex && $util.isInteger(message.shareIndex.low) && $util.isInteger(message.shareIndex.high)))
                                        return "shareIndex: integer|Long expected";
                                if (message.tssMessage != null && $Object.hasOwnProperty.call(message, "tssMessage"))
                                    if (!(message.tssMessage && typeof message.tssMessage.length === "number" || $util.isString(message.tssMessage)))
                                        return "tssMessage: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a TssMessageTransactionBody message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody} TssMessageTransactionBody
                             */
                            TssMessageTransactionBody.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody();
                                if (object.sourceRosterHash != null)
                                    if (object.sourceRosterHash.length)
                                        if (typeof object.sourceRosterHash === "string")
                                            $util.base64.decode(object.sourceRosterHash, message.sourceRosterHash = $util.newBuffer($util.base64.length(object.sourceRosterHash)), 0);
                                        else if (object.sourceRosterHash.length >= 0)
                                            message.sourceRosterHash = object.sourceRosterHash;
                                if (object.targetRosterHash != null)
                                    if (object.targetRosterHash.length)
                                        if (typeof object.targetRosterHash === "string")
                                            $util.base64.decode(object.targetRosterHash, message.targetRosterHash = $util.newBuffer($util.base64.length(object.targetRosterHash)), 0);
                                        else if (object.targetRosterHash.length >= 0)
                                            message.targetRosterHash = object.targetRosterHash;
                                if (object.shareIndex != null)
                                    if (typeof object.shareIndex === "object" ? object.shareIndex.low || object.shareIndex.high : $Number(object.shareIndex) !== 0)
                                        if ($util.Long)
                                            message.shareIndex = $util.Long.fromValue(object.shareIndex, true);
                                        else if (typeof object.shareIndex === "string")
                                            message.shareIndex = $parseInt(object.shareIndex, 10);
                                        else if (typeof object.shareIndex === "number")
                                            message.shareIndex = object.shareIndex;
                                        else if (typeof object.shareIndex === "object")
                                            message.shareIndex = new $util.LongBits(object.shareIndex.low >>> 0, object.shareIndex.high >>> 0).toNumber(true);
                                if (object.tssMessage != null)
                                    if (object.tssMessage.length)
                                        if (typeof object.tssMessage === "string")
                                            $util.base64.decode(object.tssMessage, message.tssMessage = $util.newBuffer($util.base64.length(object.tssMessage)), 0);
                                        else if (object.tssMessage.length >= 0)
                                            message.tssMessage = object.tssMessage;
                                return message;
                            };

                            /**
                             * Creates a plain object from a TssMessageTransactionBody message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody} message TssMessageTransactionBody
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            TssMessageTransactionBody.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.sourceRosterHash = "";
                                    else {
                                        object.sourceRosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.sourceRosterHash = $util.newBuffer(object.sourceRosterHash);
                                    }
                                    if (options.bytes === $String)
                                        object.targetRosterHash = "";
                                    else {
                                        object.targetRosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.targetRosterHash = $util.newBuffer(object.targetRosterHash);
                                    }
                                    if ($util.Long) {
                                        let long = new $util.Long(0, 0, true);
                                        object.shareIndex = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                                    } else
                                        object.shareIndex = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                                    if (options.bytes === $String)
                                        object.tssMessage = "";
                                    else {
                                        object.tssMessage = [];
                                        if (options.bytes !== $Array)
                                            object.tssMessage = $util.newBuffer(object.tssMessage);
                                    }
                                }
                                if (message.sourceRosterHash != null && $Object.hasOwnProperty.call(message, "sourceRosterHash"))
                                    object.sourceRosterHash = options.bytes === $String ? $util.base64.encode(message.sourceRosterHash, 0, message.sourceRosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.sourceRosterHash) : message.sourceRosterHash;
                                if (message.targetRosterHash != null && $Object.hasOwnProperty.call(message, "targetRosterHash"))
                                    object.targetRosterHash = options.bytes === $String ? $util.base64.encode(message.targetRosterHash, 0, message.targetRosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.targetRosterHash) : message.targetRosterHash;
                                if (message.shareIndex != null && $Object.hasOwnProperty.call(message, "shareIndex"))
                                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                                        object.shareIndex = typeof message.shareIndex === "number" ? $BigInt(message.shareIndex) : $util.Long.fromBits(message.shareIndex.low >>> 0, message.shareIndex.high >>> 0, true).toBigInt();
                                    else if (typeof message.shareIndex === "number")
                                        object.shareIndex = options.longs === $String ? $String(message.shareIndex) : message.shareIndex;
                                    else
                                        object.shareIndex = options.longs === $String ? $util.Long.prototype.toString.call(message.shareIndex) : options.longs === $Number ? new $util.LongBits(message.shareIndex.low >>> 0, message.shareIndex.high >>> 0).toNumber(true) : message.shareIndex;
                                if (message.tssMessage != null && $Object.hasOwnProperty.call(message, "tssMessage"))
                                    object.tssMessage = options.bytes === $String ? $util.base64.encode(message.tssMessage, 0, message.tssMessage.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.tssMessage) : message.tssMessage;
                                return object;
                            };

                            /**
                             * Converts this TssMessageTransactionBody to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            TssMessageTransactionBody.prototype.toJSON = function() {
                                return TssMessageTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for TssMessageTransactionBody
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            TssMessageTransactionBody.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.services.auxiliary.tss.TssMessageTransactionBody";
                            };

                            return TssMessageTransactionBody;
                        })();

                        tss.TssVoteTransactionBody = (function() {

                            /**
                             * Properties of a TssVoteTransactionBody.
                             * @typedef {Object} com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Properties
                             * @property {Uint8Array|null} [sourceRosterHash] A hash of the roster containing the node generating this TssVote.
                             * @property {Uint8Array|null} [targetRosterHash] A hash of the roster that this TssVote is for.
                             * @property {Uint8Array|null} [ledgerId] An identifier (and public key) computed from the TssMessages for the target
                             * roster.
                             * @property {Uint8Array|null} [nodeSignature] A signature produced by the node.
                             * <p>
                             * This signature SHALL be produced using the node RSA signing key to sign
                             * the ledger_id.<br/>
                             * This signature SHALL be used to establish a chain of trust in the ledger id.
                             * @property {Uint8Array|null} [tssVote] A bit vector of message statuses.
                             * <p>
                             * #### Example
                             * <ul><li>The least significant bit of byte[0] SHALL be the 0th item in the sequence.</li>
                             * <li>The most significant bit of byte[0] SHALL be the 7th item in the sequence.</li>
                             * <li>The least significant bit of byte[1] SHALL be the 8th item in the sequence.</li>
                             * <li>The most significant bit of byte[1] SHALL be the 15th item in the sequence.</li>
                             * </ul>
                             * A bit SHALL be set if the `TssMessage` for the `TssMessageTransaction`
                             * with a sequence number matching that bit index has been
                             * received, and is valid.<br/>
                             * A bit SHALL NOT be set if the `TssMessage` has not been received or was
                             * received but not valid.
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */

                            /**
                             * Properties of a TssVoteTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.tss
                             * @interface ITssVoteTransactionBody
                             * @augments com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Properties
                             * @deprecated Use com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Properties instead.
                             */

                            /**
                             * Shape of a TssVoteTransactionBody.
                             * @typedef {com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Properties} com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Shape
                             */

                            /**
                             * Constructs a new TssVoteTransactionBody.
                             * @memberof com.hedera.hapi.services.auxiliary.tss
                             * @classdesc A transaction body to vote on the validity of Threshold Signature Scheme
                             * (TSS) Messages for a candidate roster.
                             * 
                             * - A `TssVoteTransactionBody` MUST identify the hash of the roster containing
                             * the node generating this TssVote
                             * - A `TssVoteTransactionBody` MUST identify the hash of the roster that the
                             * TSS messages is for
                             * - If the candidate roster has received enough yes votes, the candidate
                             * roster SHALL be adopted.
                             * - Switching to the candidate roster MUST not happen until enough nodes have
                             * voted that they have verified a threshold number of TSS messages from the
                             * active roster.
                             * - A vote consists of a bit vector of message statuses where each bit
                             * corresponds to the order of TssMessages as they have come through
                             * consensus.
                             * - The threshold for votes to adopt a candidate roster SHALL be at least 1/3
                             * of the consensus weight of the active roster to ensure that at least 1
                             * honest node has validated the TSS key material.
                             * @constructor
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Properties=} [properties] Properties to set
                             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
                             */
                            const TssVoteTransactionBody = function (properties) {
                                if (properties)
                                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                            this[keys[i]] = properties[keys[i]];
                            };

                            /**
                             * A hash of the roster containing the node generating this TssVote.
                             * @member {Uint8Array} sourceRosterHash
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @instance
                             */
                            TssVoteTransactionBody.prototype.sourceRosterHash = $util.newBuffer([]);

                            /**
                             * A hash of the roster that this TssVote is for.
                             * @member {Uint8Array} targetRosterHash
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @instance
                             */
                            TssVoteTransactionBody.prototype.targetRosterHash = $util.newBuffer([]);

                            /**
                             * An identifier (and public key) computed from the TssMessages for the target
                             * roster.
                             * @member {Uint8Array} ledgerId
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @instance
                             */
                            TssVoteTransactionBody.prototype.ledgerId = $util.newBuffer([]);

                            /**
                             * A signature produced by the node.
                             * <p>
                             * This signature SHALL be produced using the node RSA signing key to sign
                             * the ledger_id.<br/>
                             * This signature SHALL be used to establish a chain of trust in the ledger id.
                             * @member {Uint8Array} nodeSignature
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @instance
                             */
                            TssVoteTransactionBody.prototype.nodeSignature = $util.newBuffer([]);

                            /**
                             * A bit vector of message statuses.
                             * <p>
                             * #### Example
                             * <ul><li>The least significant bit of byte[0] SHALL be the 0th item in the sequence.</li>
                             * <li>The most significant bit of byte[0] SHALL be the 7th item in the sequence.</li>
                             * <li>The least significant bit of byte[1] SHALL be the 8th item in the sequence.</li>
                             * <li>The most significant bit of byte[1] SHALL be the 15th item in the sequence.</li>
                             * </ul>
                             * A bit SHALL be set if the `TssMessage` for the `TssMessageTransaction`
                             * with a sequence number matching that bit index has been
                             * received, and is valid.<br/>
                             * A bit SHALL NOT be set if the `TssMessage` has not been received or was
                             * received but not valid.
                             * @member {Uint8Array} tssVote
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @instance
                             */
                            TssVoteTransactionBody.prototype.tssVote = $util.newBuffer([]);

                            /**
                             * Creates a new TssVoteTransactionBody instance using the specified properties.
                             * @function create
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Properties=} [properties] Properties to set
                             * @returns {com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody} TssVoteTransactionBody instance
                             * @type {{
                             *   (properties: com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Shape): com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody & com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Shape;
                             *   (properties?: com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Properties): com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody;
                             * }}
                             */
                            TssVoteTransactionBody.create = function(properties) {
                                return new TssVoteTransactionBody(properties);
                            };

                            /**
                             * Encodes the specified TssVoteTransactionBody message. Does not implicitly {@link com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.verify|verify} messages.
                             * @function encode
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Properties} message TssVoteTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            TssVoteTransactionBody.encode = function (message, writer, _depth) {
                                if (!writer)
                                    writer = $Writer.create();
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                if (message.sourceRosterHash != null && $Object.hasOwnProperty.call(message, "sourceRosterHash") && message.sourceRosterHash.length)
                                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.sourceRosterHash);
                                if (message.targetRosterHash != null && $Object.hasOwnProperty.call(message, "targetRosterHash") && message.targetRosterHash.length)
                                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.targetRosterHash);
                                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId") && message.ledgerId.length)
                                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.ledgerId);
                                if (message.nodeSignature != null && $Object.hasOwnProperty.call(message, "nodeSignature") && message.nodeSignature.length)
                                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.nodeSignature);
                                if (message.tssVote != null && $Object.hasOwnProperty.call(message, "tssVote") && message.tssVote.length)
                                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.tssVote);
                                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                                    for (let i = 0; i < message.$unknowns.length; ++i)
                                        writer.raw(message.$unknowns[i]);
                                return writer;
                            };

                            /**
                             * Encodes the specified TssVoteTransactionBody message, length delimited. Does not implicitly {@link com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.verify|verify} messages.
                             * @function encodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Properties} message TssVoteTransactionBody message or plain object to encode
                             * @param {$protobuf.Writer} [writer] Writer to encode to
                             * @returns {$protobuf.Writer} Writer
                             */
                            TssVoteTransactionBody.encodeDelimited = function(message, writer) {
                                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
                            };

                            /**
                             * Decodes a TssVoteTransactionBody message from the specified reader or buffer.
                             * @function decode
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @param {number} [length] Message length if known beforehand
                             * @returns {com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody & com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Shape} TssVoteTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            TssVoteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
                                if (!(reader instanceof $Reader))
                                    reader = $Reader.create(reader);
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $Reader.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let end, message, value;
                                if (length === $undefined)
                                    end = reader.len;
                                else {
                                    end = reader.pos + length;
                                    if (end > reader.len)
                                        throw $RangeError("index out of range");
                                    length = reader.len;
                                    reader.len = end;
                                }
                                message = _target || new $root.com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody();
                                while (reader.pos < end) {
                                    let start = reader.pos;
                                    let tag = reader.tag();
                                    if (tag === _end) {
                                        _end = $undefined;
                                        break;
                                    }
                                    let wireType = tag & 7;
                                    switch (tag >>>= 3) {
                                    case 1: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.sourceRosterHash = value;
                                            else
                                                delete message.sourceRosterHash;
                                            continue;
                                        }
                                    case 2: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.targetRosterHash = value;
                                            else
                                                delete message.targetRosterHash;
                                            continue;
                                        }
                                    case 3: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.ledgerId = value;
                                            else
                                                delete message.ledgerId;
                                            continue;
                                        }
                                    case 4: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.nodeSignature = value;
                                            else
                                                delete message.nodeSignature;
                                            continue;
                                        }
                                    case 5: {
                                            if (wireType !== 2)
                                                break;
                                            if ((value = reader.bytes()).length)
                                                message.tssVote = value;
                                            else
                                                delete message.tssVote;
                                            continue;
                                        }
                                    }
                                    reader.skipType(wireType, _depth, tag);
                                    if (!reader.discardUnknown) {
                                        $util.makeProp(message, "$unknowns", false);
                                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                                    }
                                }
                                if (length !== $undefined) {
                                    if (reader.pos !== end)
                                        throw $RangeError("index out of range");
                                    reader.len = length;
                                }
                                if (_end !== $undefined)
                                    throw $Error("missing end group");
                                return message;
                            };

                            /**
                             * Decodes a TssVoteTransactionBody message from the specified reader or buffer, length delimited.
                             * @function decodeDelimited
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @static
                             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                             * @returns {com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody & com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody.$Shape} TssVoteTransactionBody
                             * @throws {Error} If the payload is not a reader or valid buffer
                             * @throws {$protobuf.util.ProtocolError} If required fields are missing
                             */
                            TssVoteTransactionBody.decodeDelimited = function(reader) {
                                if (!(reader instanceof $Reader))
                                    reader = new $Reader(reader);
                                return this.decode(reader, reader.uint32());
                            };

                            /**
                             * Verifies a TssVoteTransactionBody message.
                             * @function verify
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @static
                             * @param {Object.<string,*>} message Plain object to verify
                             * @returns {string|null} `null` if valid, otherwise the reason why it is not
                             */
                            TssVoteTransactionBody.verify = function (message, _depth) {
                                if (typeof message !== "object" || message === null)
                                    return "object expected";
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    return "max depth exceeded";
                                if (message.sourceRosterHash != null && $Object.hasOwnProperty.call(message, "sourceRosterHash"))
                                    if (!(message.sourceRosterHash && typeof message.sourceRosterHash.length === "number" || $util.isString(message.sourceRosterHash)))
                                        return "sourceRosterHash: buffer expected";
                                if (message.targetRosterHash != null && $Object.hasOwnProperty.call(message, "targetRosterHash"))
                                    if (!(message.targetRosterHash && typeof message.targetRosterHash.length === "number" || $util.isString(message.targetRosterHash)))
                                        return "targetRosterHash: buffer expected";
                                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                                    if (!(message.ledgerId && typeof message.ledgerId.length === "number" || $util.isString(message.ledgerId)))
                                        return "ledgerId: buffer expected";
                                if (message.nodeSignature != null && $Object.hasOwnProperty.call(message, "nodeSignature"))
                                    if (!(message.nodeSignature && typeof message.nodeSignature.length === "number" || $util.isString(message.nodeSignature)))
                                        return "nodeSignature: buffer expected";
                                if (message.tssVote != null && $Object.hasOwnProperty.call(message, "tssVote"))
                                    if (!(message.tssVote && typeof message.tssVote.length === "number" || $util.isString(message.tssVote)))
                                        return "tssVote: buffer expected";
                                return null;
                            };

                            /**
                             * Creates a TssVoteTransactionBody message from a plain object. Also converts values to their respective internal types.
                             * @function fromObject
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @static
                             * @param {Object.<string,*>} object Plain object
                             * @returns {com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody} TssVoteTransactionBody
                             */
                            TssVoteTransactionBody.fromObject = function (object, _depth) {
                                if (object instanceof $root.com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody)
                                    return object;
                                if (!$util.isObject(object))
                                    throw $TypeError(".com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody: object expected");
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let message = new $root.com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody();
                                if (object.sourceRosterHash != null)
                                    if (object.sourceRosterHash.length)
                                        if (typeof object.sourceRosterHash === "string")
                                            $util.base64.decode(object.sourceRosterHash, message.sourceRosterHash = $util.newBuffer($util.base64.length(object.sourceRosterHash)), 0);
                                        else if (object.sourceRosterHash.length >= 0)
                                            message.sourceRosterHash = object.sourceRosterHash;
                                if (object.targetRosterHash != null)
                                    if (object.targetRosterHash.length)
                                        if (typeof object.targetRosterHash === "string")
                                            $util.base64.decode(object.targetRosterHash, message.targetRosterHash = $util.newBuffer($util.base64.length(object.targetRosterHash)), 0);
                                        else if (object.targetRosterHash.length >= 0)
                                            message.targetRosterHash = object.targetRosterHash;
                                if (object.ledgerId != null)
                                    if (object.ledgerId.length)
                                        if (typeof object.ledgerId === "string")
                                            $util.base64.decode(object.ledgerId, message.ledgerId = $util.newBuffer($util.base64.length(object.ledgerId)), 0);
                                        else if (object.ledgerId.length >= 0)
                                            message.ledgerId = object.ledgerId;
                                if (object.nodeSignature != null)
                                    if (object.nodeSignature.length)
                                        if (typeof object.nodeSignature === "string")
                                            $util.base64.decode(object.nodeSignature, message.nodeSignature = $util.newBuffer($util.base64.length(object.nodeSignature)), 0);
                                        else if (object.nodeSignature.length >= 0)
                                            message.nodeSignature = object.nodeSignature;
                                if (object.tssVote != null)
                                    if (object.tssVote.length)
                                        if (typeof object.tssVote === "string")
                                            $util.base64.decode(object.tssVote, message.tssVote = $util.newBuffer($util.base64.length(object.tssVote)), 0);
                                        else if (object.tssVote.length >= 0)
                                            message.tssVote = object.tssVote;
                                return message;
                            };

                            /**
                             * Creates a plain object from a TssVoteTransactionBody message. Also converts values to other types if specified.
                             * @function toObject
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @static
                             * @param {com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody} message TssVoteTransactionBody
                             * @param {$protobuf.IConversionOptions} [options] Conversion options
                             * @returns {Object.<string,*>} Plain object
                             */
                            TssVoteTransactionBody.toObject = function (message, options, _depth) {
                                if (!options)
                                    options = {};
                                if (_depth === $undefined)
                                    _depth = 0;
                                if (_depth > $util.recursionLimit)
                                    throw $Error("max depth exceeded");
                                let object = {};
                                if (options.defaults) {
                                    if (options.bytes === $String)
                                        object.sourceRosterHash = "";
                                    else {
                                        object.sourceRosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.sourceRosterHash = $util.newBuffer(object.sourceRosterHash);
                                    }
                                    if (options.bytes === $String)
                                        object.targetRosterHash = "";
                                    else {
                                        object.targetRosterHash = [];
                                        if (options.bytes !== $Array)
                                            object.targetRosterHash = $util.newBuffer(object.targetRosterHash);
                                    }
                                    if (options.bytes === $String)
                                        object.ledgerId = "";
                                    else {
                                        object.ledgerId = [];
                                        if (options.bytes !== $Array)
                                            object.ledgerId = $util.newBuffer(object.ledgerId);
                                    }
                                    if (options.bytes === $String)
                                        object.nodeSignature = "";
                                    else {
                                        object.nodeSignature = [];
                                        if (options.bytes !== $Array)
                                            object.nodeSignature = $util.newBuffer(object.nodeSignature);
                                    }
                                    if (options.bytes === $String)
                                        object.tssVote = "";
                                    else {
                                        object.tssVote = [];
                                        if (options.bytes !== $Array)
                                            object.tssVote = $util.newBuffer(object.tssVote);
                                    }
                                }
                                if (message.sourceRosterHash != null && $Object.hasOwnProperty.call(message, "sourceRosterHash"))
                                    object.sourceRosterHash = options.bytes === $String ? $util.base64.encode(message.sourceRosterHash, 0, message.sourceRosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.sourceRosterHash) : message.sourceRosterHash;
                                if (message.targetRosterHash != null && $Object.hasOwnProperty.call(message, "targetRosterHash"))
                                    object.targetRosterHash = options.bytes === $String ? $util.base64.encode(message.targetRosterHash, 0, message.targetRosterHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.targetRosterHash) : message.targetRosterHash;
                                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                                    object.ledgerId = options.bytes === $String ? $util.base64.encode(message.ledgerId, 0, message.ledgerId.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ledgerId) : message.ledgerId;
                                if (message.nodeSignature != null && $Object.hasOwnProperty.call(message, "nodeSignature"))
                                    object.nodeSignature = options.bytes === $String ? $util.base64.encode(message.nodeSignature, 0, message.nodeSignature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.nodeSignature) : message.nodeSignature;
                                if (message.tssVote != null && $Object.hasOwnProperty.call(message, "tssVote"))
                                    object.tssVote = options.bytes === $String ? $util.base64.encode(message.tssVote, 0, message.tssVote.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.tssVote) : message.tssVote;
                                return object;
                            };

                            /**
                             * Converts this TssVoteTransactionBody to JSON.
                             * @function toJSON
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @instance
                             * @returns {Object.<string,*>} JSON object
                             */
                            TssVoteTransactionBody.prototype.toJSON = function() {
                                return TssVoteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
                            };

                            /**
                             * Gets the type url for TssVoteTransactionBody
                             * @function getTypeUrl
                             * @memberof com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody
                             * @static
                             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
                             * @returns {string} The type url
                             */
                            TssVoteTransactionBody.getTypeUrl = function(prefix) {
                                if (prefix === $undefined)
                                    prefix = "type.googleapis.com";
                                return prefix + "/com.hedera.hapi.services.auxiliary.tss.TssVoteTransactionBody";
                            };

                            return TssVoteTransactionBody;
                        })();

                        return tss;
                    })();

                    return auxiliary;
                })();

                return services;
            })();

            return hapi;
        })();

        return hedera;
    })();

    return com;
})();

export const proto = $root.proto = (() => {

    /**
     * Namespace proto.
     * @exports proto
     * @namespace
     */
    const proto = {};

    proto.ShardID = (function() {

        /**
         * Properties of a ShardID.
         * @typedef {Object} proto.ShardID.$Properties
         * @property {number|Long|null} [shardNum] A whole number shard identifier.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ShardID.
         * @memberof proto
         * @interface IShardID
         * @augments proto.ShardID.$Properties
         * @deprecated Use proto.ShardID.$Properties instead.
         */

        /**
         * Shape of a ShardID.
         * @typedef {proto.ShardID.$Properties} proto.ShardID.$Shape
         */

        /**
         * Constructs a new ShardID.
         * @memberof proto
         * @classdesc A shard identifier.<br/>
         * A shard is a partition of nodes running the network that processes
         * transactions separately from other shards. Each shard is effectively an
         * independent instance of the overall network that shares the same virtual
         * distributed ledger, and may gossip cross-shard transactions with other
         * shards to maintain overall correct processing of the ledger.
         * @constructor
         * @param {proto.ShardID.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ShardID = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A whole number shard identifier.
         * @member {number|Long} shardNum
         * @memberof proto.ShardID
         * @instance
         */
        ShardID.prototype.shardNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ShardID instance using the specified properties.
         * @function create
         * @memberof proto.ShardID
         * @static
         * @param {proto.ShardID.$Properties=} [properties] Properties to set
         * @returns {proto.ShardID} ShardID instance
         * @type {{
         *   (properties: proto.ShardID.$Shape): proto.ShardID & proto.ShardID.$Shape;
         *   (properties?: proto.ShardID.$Properties): proto.ShardID;
         * }}
         */
        ShardID.create = function(properties) {
            return new ShardID(properties);
        };

        /**
         * Encodes the specified ShardID message. Does not implicitly {@link proto.ShardID.verify|verify} messages.
         * @function encode
         * @memberof proto.ShardID
         * @static
         * @param {proto.ShardID.$Properties} message ShardID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ShardID.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum") && (typeof message.shardNum === "object" ? message.shardNum.low || message.shardNum.high : message.shardNum !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.shardNum);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ShardID message, length delimited. Does not implicitly {@link proto.ShardID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ShardID
         * @static
         * @param {proto.ShardID.$Properties} message ShardID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ShardID.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ShardID message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ShardID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ShardID & proto.ShardID.$Shape} ShardID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ShardID.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ShardID();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.shardNum = value;
                        else
                            delete message.shardNum;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ShardID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ShardID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ShardID & proto.ShardID.$Shape} ShardID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ShardID.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ShardID message.
         * @function verify
         * @memberof proto.ShardID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ShardID.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (!$util.isInteger(message.shardNum) && !(message.shardNum && $util.isInteger(message.shardNum.low) && $util.isInteger(message.shardNum.high)))
                    return "shardNum: integer|Long expected";
            return null;
        };

        /**
         * Creates a ShardID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ShardID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ShardID} ShardID
         */
        ShardID.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ShardID)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ShardID: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ShardID();
            if (object.shardNum != null)
                if (typeof object.shardNum === "object" ? object.shardNum.low || object.shardNum.high : $Number(object.shardNum) !== 0)
                    if ($util.Long)
                        message.shardNum = $util.Long.fromValue(object.shardNum, false);
                    else if (typeof object.shardNum === "string")
                        message.shardNum = $parseInt(object.shardNum, 10);
                    else if (typeof object.shardNum === "number")
                        message.shardNum = object.shardNum;
                    else if (typeof object.shardNum === "object")
                        message.shardNum = new $util.LongBits(object.shardNum.low >>> 0, object.shardNum.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ShardID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ShardID
         * @static
         * @param {proto.ShardID} message ShardID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ShardID.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.shardNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.shardNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.shardNum = typeof message.shardNum === "number" ? $BigInt(message.shardNum) : $util.Long.fromBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0, false).toBigInt();
                else if (typeof message.shardNum === "number")
                    object.shardNum = options.longs === $String ? $String(message.shardNum) : message.shardNum;
                else
                    object.shardNum = options.longs === $String ? $util.Long.prototype.toString.call(message.shardNum) : options.longs === $Number ? new $util.LongBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0).toNumber() : message.shardNum;
            return object;
        };

        /**
         * Converts this ShardID to JSON.
         * @function toJSON
         * @memberof proto.ShardID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ShardID.prototype.toJSON = function() {
            return ShardID.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ShardID
         * @function getTypeUrl
         * @memberof proto.ShardID
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ShardID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ShardID";
        };

        return ShardID;
    })();

    proto.RealmID = (function() {

        /**
         * Properties of a RealmID.
         * @typedef {Object} proto.RealmID.$Properties
         * @property {number|Long|null} [shardNum] A whole number shard identifier.
         * @property {number|Long|null} [realmNum] A whole number realm identifier.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RealmID.
         * @memberof proto
         * @interface IRealmID
         * @augments proto.RealmID.$Properties
         * @deprecated Use proto.RealmID.$Properties instead.
         */

        /**
         * Shape of a RealmID.
         * @typedef {proto.RealmID.$Properties} proto.RealmID.$Shape
         */

        /**
         * Constructs a new RealmID.
         * @memberof proto
         * @classdesc A realm identifier.<br/>
         * Within a given shard, every realm has a unique numeric identifier.
         * Each account, file, and contract instance belongs to exactly one realm.
         * @constructor
         * @param {proto.RealmID.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RealmID = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A whole number shard identifier.
         * @member {number|Long} shardNum
         * @memberof proto.RealmID
         * @instance
         */
        RealmID.prototype.shardNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number realm identifier.
         * @member {number|Long} realmNum
         * @memberof proto.RealmID
         * @instance
         */
        RealmID.prototype.realmNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new RealmID instance using the specified properties.
         * @function create
         * @memberof proto.RealmID
         * @static
         * @param {proto.RealmID.$Properties=} [properties] Properties to set
         * @returns {proto.RealmID} RealmID instance
         * @type {{
         *   (properties: proto.RealmID.$Shape): proto.RealmID & proto.RealmID.$Shape;
         *   (properties?: proto.RealmID.$Properties): proto.RealmID;
         * }}
         */
        RealmID.create = function(properties) {
            return new RealmID(properties);
        };

        /**
         * Encodes the specified RealmID message. Does not implicitly {@link proto.RealmID.verify|verify} messages.
         * @function encode
         * @memberof proto.RealmID
         * @static
         * @param {proto.RealmID.$Properties} message RealmID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RealmID.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum") && (typeof message.shardNum === "object" ? message.shardNum.low || message.shardNum.high : message.shardNum !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.shardNum);
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum") && (typeof message.realmNum === "object" ? message.realmNum.low || message.realmNum.high : message.realmNum !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.realmNum);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RealmID message, length delimited. Does not implicitly {@link proto.RealmID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.RealmID
         * @static
         * @param {proto.RealmID.$Properties} message RealmID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RealmID.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RealmID message from the specified reader or buffer.
         * @function decode
         * @memberof proto.RealmID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.RealmID & proto.RealmID.$Shape} RealmID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RealmID.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.RealmID();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.shardNum = value;
                        else
                            delete message.shardNum;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.realmNum = value;
                        else
                            delete message.realmNum;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RealmID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.RealmID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.RealmID & proto.RealmID.$Shape} RealmID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RealmID.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RealmID message.
         * @function verify
         * @memberof proto.RealmID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RealmID.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (!$util.isInteger(message.shardNum) && !(message.shardNum && $util.isInteger(message.shardNum.low) && $util.isInteger(message.shardNum.high)))
                    return "shardNum: integer|Long expected";
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (!$util.isInteger(message.realmNum) && !(message.realmNum && $util.isInteger(message.realmNum.low) && $util.isInteger(message.realmNum.high)))
                    return "realmNum: integer|Long expected";
            return null;
        };

        /**
         * Creates a RealmID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.RealmID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.RealmID} RealmID
         */
        RealmID.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.RealmID)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.RealmID: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.RealmID();
            if (object.shardNum != null)
                if (typeof object.shardNum === "object" ? object.shardNum.low || object.shardNum.high : $Number(object.shardNum) !== 0)
                    if ($util.Long)
                        message.shardNum = $util.Long.fromValue(object.shardNum, false);
                    else if (typeof object.shardNum === "string")
                        message.shardNum = $parseInt(object.shardNum, 10);
                    else if (typeof object.shardNum === "number")
                        message.shardNum = object.shardNum;
                    else if (typeof object.shardNum === "object")
                        message.shardNum = new $util.LongBits(object.shardNum.low >>> 0, object.shardNum.high >>> 0).toNumber();
            if (object.realmNum != null)
                if (typeof object.realmNum === "object" ? object.realmNum.low || object.realmNum.high : $Number(object.realmNum) !== 0)
                    if ($util.Long)
                        message.realmNum = $util.Long.fromValue(object.realmNum, false);
                    else if (typeof object.realmNum === "string")
                        message.realmNum = $parseInt(object.realmNum, 10);
                    else if (typeof object.realmNum === "number")
                        message.realmNum = object.realmNum;
                    else if (typeof object.realmNum === "object")
                        message.realmNum = new $util.LongBits(object.realmNum.low >>> 0, object.realmNum.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a RealmID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.RealmID
         * @static
         * @param {proto.RealmID} message RealmID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RealmID.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.shardNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.shardNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.realmNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.realmNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.shardNum = typeof message.shardNum === "number" ? $BigInt(message.shardNum) : $util.Long.fromBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0, false).toBigInt();
                else if (typeof message.shardNum === "number")
                    object.shardNum = options.longs === $String ? $String(message.shardNum) : message.shardNum;
                else
                    object.shardNum = options.longs === $String ? $util.Long.prototype.toString.call(message.shardNum) : options.longs === $Number ? new $util.LongBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0).toNumber() : message.shardNum;
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.realmNum = typeof message.realmNum === "number" ? $BigInt(message.realmNum) : $util.Long.fromBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0, false).toBigInt();
                else if (typeof message.realmNum === "number")
                    object.realmNum = options.longs === $String ? $String(message.realmNum) : message.realmNum;
                else
                    object.realmNum = options.longs === $String ? $util.Long.prototype.toString.call(message.realmNum) : options.longs === $Number ? new $util.LongBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0).toNumber() : message.realmNum;
            return object;
        };

        /**
         * Converts this RealmID to JSON.
         * @function toJSON
         * @memberof proto.RealmID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RealmID.prototype.toJSON = function() {
            return RealmID.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RealmID
         * @function getTypeUrl
         * @memberof proto.RealmID
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RealmID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.RealmID";
        };

        return RealmID;
    })();

    proto.TokenID = (function() {

        /**
         * Properties of a TokenID.
         * @typedef {Object} proto.TokenID.$Properties
         * @property {number|Long|null} [shardNum] A whole number shard identifier.
         * @property {number|Long|null} [realmNum] A whole number realm identifier.
         * @property {number|Long|null} [tokenNum] A whole number token identifier.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenID.
         * @memberof proto
         * @interface ITokenID
         * @augments proto.TokenID.$Properties
         * @deprecated Use proto.TokenID.$Properties instead.
         */

        /**
         * Shape of a TokenID.
         * @typedef {proto.TokenID.$Properties} proto.TokenID.$Shape
         */

        /**
         * Constructs a new TokenID.
         * @memberof proto
         * @classdesc Unique identifier for a token.<br/>
         * As with all entity identifiers within the network, a token identifier
         * consists of a combination of shard number, realm number, and entity number.
         * Each of these numbers is unique within its scope (shard > realm > entity).
         * @constructor
         * @param {proto.TokenID.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenID = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A whole number shard identifier.
         * @member {number|Long} shardNum
         * @memberof proto.TokenID
         * @instance
         */
        TokenID.prototype.shardNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number realm identifier.
         * @member {number|Long} realmNum
         * @memberof proto.TokenID
         * @instance
         */
        TokenID.prototype.realmNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number token identifier.
         * @member {number|Long} tokenNum
         * @memberof proto.TokenID
         * @instance
         */
        TokenID.prototype.tokenNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new TokenID instance using the specified properties.
         * @function create
         * @memberof proto.TokenID
         * @static
         * @param {proto.TokenID.$Properties=} [properties] Properties to set
         * @returns {proto.TokenID} TokenID instance
         * @type {{
         *   (properties: proto.TokenID.$Shape): proto.TokenID & proto.TokenID.$Shape;
         *   (properties?: proto.TokenID.$Properties): proto.TokenID;
         * }}
         */
        TokenID.create = function(properties) {
            return new TokenID(properties);
        };

        /**
         * Encodes the specified TokenID message. Does not implicitly {@link proto.TokenID.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenID
         * @static
         * @param {proto.TokenID.$Properties} message TokenID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenID.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum") && (typeof message.shardNum === "object" ? message.shardNum.low || message.shardNum.high : message.shardNum !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.shardNum);
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum") && (typeof message.realmNum === "object" ? message.realmNum.low || message.realmNum.high : message.realmNum !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.realmNum);
            if (message.tokenNum != null && $Object.hasOwnProperty.call(message, "tokenNum") && (typeof message.tokenNum === "object" ? message.tokenNum.low || message.tokenNum.high : message.tokenNum !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.tokenNum);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenID message, length delimited. Does not implicitly {@link proto.TokenID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenID
         * @static
         * @param {proto.TokenID.$Properties} message TokenID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenID.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenID message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenID & proto.TokenID.$Shape} TokenID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenID.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenID();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.shardNum = value;
                        else
                            delete message.shardNum;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.realmNum = value;
                        else
                            delete message.realmNum;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.tokenNum = value;
                        else
                            delete message.tokenNum;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenID & proto.TokenID.$Shape} TokenID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenID.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenID message.
         * @function verify
         * @memberof proto.TokenID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenID.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (!$util.isInteger(message.shardNum) && !(message.shardNum && $util.isInteger(message.shardNum.low) && $util.isInteger(message.shardNum.high)))
                    return "shardNum: integer|Long expected";
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (!$util.isInteger(message.realmNum) && !(message.realmNum && $util.isInteger(message.realmNum.low) && $util.isInteger(message.realmNum.high)))
                    return "realmNum: integer|Long expected";
            if (message.tokenNum != null && $Object.hasOwnProperty.call(message, "tokenNum"))
                if (!$util.isInteger(message.tokenNum) && !(message.tokenNum && $util.isInteger(message.tokenNum.low) && $util.isInteger(message.tokenNum.high)))
                    return "tokenNum: integer|Long expected";
            return null;
        };

        /**
         * Creates a TokenID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenID} TokenID
         */
        TokenID.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenID)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenID: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenID();
            if (object.shardNum != null)
                if (typeof object.shardNum === "object" ? object.shardNum.low || object.shardNum.high : $Number(object.shardNum) !== 0)
                    if ($util.Long)
                        message.shardNum = $util.Long.fromValue(object.shardNum, false);
                    else if (typeof object.shardNum === "string")
                        message.shardNum = $parseInt(object.shardNum, 10);
                    else if (typeof object.shardNum === "number")
                        message.shardNum = object.shardNum;
                    else if (typeof object.shardNum === "object")
                        message.shardNum = new $util.LongBits(object.shardNum.low >>> 0, object.shardNum.high >>> 0).toNumber();
            if (object.realmNum != null)
                if (typeof object.realmNum === "object" ? object.realmNum.low || object.realmNum.high : $Number(object.realmNum) !== 0)
                    if ($util.Long)
                        message.realmNum = $util.Long.fromValue(object.realmNum, false);
                    else if (typeof object.realmNum === "string")
                        message.realmNum = $parseInt(object.realmNum, 10);
                    else if (typeof object.realmNum === "number")
                        message.realmNum = object.realmNum;
                    else if (typeof object.realmNum === "object")
                        message.realmNum = new $util.LongBits(object.realmNum.low >>> 0, object.realmNum.high >>> 0).toNumber();
            if (object.tokenNum != null)
                if (typeof object.tokenNum === "object" ? object.tokenNum.low || object.tokenNum.high : $Number(object.tokenNum) !== 0)
                    if ($util.Long)
                        message.tokenNum = $util.Long.fromValue(object.tokenNum, false);
                    else if (typeof object.tokenNum === "string")
                        message.tokenNum = $parseInt(object.tokenNum, 10);
                    else if (typeof object.tokenNum === "number")
                        message.tokenNum = object.tokenNum;
                    else if (typeof object.tokenNum === "object")
                        message.tokenNum = new $util.LongBits(object.tokenNum.low >>> 0, object.tokenNum.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a TokenID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenID
         * @static
         * @param {proto.TokenID} message TokenID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenID.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.shardNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.shardNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.realmNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.realmNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.tokenNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.tokenNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.shardNum = typeof message.shardNum === "number" ? $BigInt(message.shardNum) : $util.Long.fromBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0, false).toBigInt();
                else if (typeof message.shardNum === "number")
                    object.shardNum = options.longs === $String ? $String(message.shardNum) : message.shardNum;
                else
                    object.shardNum = options.longs === $String ? $util.Long.prototype.toString.call(message.shardNum) : options.longs === $Number ? new $util.LongBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0).toNumber() : message.shardNum;
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.realmNum = typeof message.realmNum === "number" ? $BigInt(message.realmNum) : $util.Long.fromBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0, false).toBigInt();
                else if (typeof message.realmNum === "number")
                    object.realmNum = options.longs === $String ? $String(message.realmNum) : message.realmNum;
                else
                    object.realmNum = options.longs === $String ? $util.Long.prototype.toString.call(message.realmNum) : options.longs === $Number ? new $util.LongBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0).toNumber() : message.realmNum;
            if (message.tokenNum != null && $Object.hasOwnProperty.call(message, "tokenNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.tokenNum = typeof message.tokenNum === "number" ? $BigInt(message.tokenNum) : $util.Long.fromBits(message.tokenNum.low >>> 0, message.tokenNum.high >>> 0, false).toBigInt();
                else if (typeof message.tokenNum === "number")
                    object.tokenNum = options.longs === $String ? $String(message.tokenNum) : message.tokenNum;
                else
                    object.tokenNum = options.longs === $String ? $util.Long.prototype.toString.call(message.tokenNum) : options.longs === $Number ? new $util.LongBits(message.tokenNum.low >>> 0, message.tokenNum.high >>> 0).toNumber() : message.tokenNum;
            return object;
        };

        /**
         * Converts this TokenID to JSON.
         * @function toJSON
         * @memberof proto.TokenID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenID.prototype.toJSON = function() {
            return TokenID.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenID
         * @function getTypeUrl
         * @memberof proto.TokenID
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenID";
        };

        return TokenID;
    })();

    /**
     * A specific hash algorithm.
     * 
     * We did not reuse Record Stream `HashAlgorithm` here because in all cases,
     * currently, this will be `SHA2_384` and if that is the default value then
     * we can save space by not serializing it, whereas `HASH_ALGORITHM_UNKNOWN`
     * is the default for Record Stream `HashAlgorithm`.
     * 
     * Note that enum values here MUST NOT match the name of any other enum value
     * in the same `package`, as protobuf follows `C++` scope rules and all enum
     * _names_ are treated as global constants within the `package`.
     * @name proto.BlockHashAlgorithm
     * @enum {number}
     * @property {number} SHA2_384=0 A SHA2 algorithm SHA-384 hash.
     * <p>
     * This is the default value, if a field of this enumerated type is
     * not set, then this is the value that will be decoded when the
     * serialized message is read.
     */
    proto.BlockHashAlgorithm = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "SHA2_384"] = 0;
        return values;
    })();

    proto.AccountID = (function() {

        /**
         * Properties of an AccountID.
         * @typedef {Object} proto.AccountID.$Properties
         * @property {number|Long|null} [shardNum] A whole number shard identifier.
         * @property {number|Long|null} [realmNum] A whole number realm identifier.
         * @property {number|Long|null} [accountNum] A whole number account number, unique within its realm and shard.
         * <p>
         * For any AccountID fields in the query response, transaction records,
         * transaction receipts, or block stream `accountNum` MUST be used.
         * @property {Uint8Array|null} [alias] An alias value.<br/>
         * Alias is a value used in some contexts to refer to an account when
         * account number is not available, and may be an alias public key, or
         * an EVM address.
         * @property {"accountNum"|"alias"} [account] AccountID account
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AccountID.
         * @memberof proto
         * @interface IAccountID
         * @augments proto.AccountID.$Properties
         * @deprecated Use proto.AccountID.$Properties instead.
         */

        /**
         * Narrowed shape of an AccountID.
         * @typedef {{
         *   shardNum?: number|Long|null;
         *   realmNum?: number|Long|null;
         *   accountNum?: number|Long|null;
         *   alias?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ account?: undefined; accountNum?: null; alias?: null }|{ account?: "accountNum"; accountNum: number|Long; alias?: null }|{ account?: "alias"; accountNum?: null; alias: Uint8Array })
         * )} proto.AccountID.$Shape
         */

        /**
         * Constructs a new AccountID.
         * @memberof proto
         * @classdesc A unique identifier for an Hedera account.
         * 
         * An account identifier is of the form `shard.realm.[number|alias]`.<br/>
         * The identifier MAY use the alias form when transferring HBAR to a public key
         * before the account for that key is created, when only the alias value is
         * known, or in some smart contracts that use the EVM address style alias to
         * refer to Accounts.<br/>
         * When the account entry is completed, the alias SHALL be stored separately in
         * the Account record, and the identifier in the Account SHALL use the
         * `accountNum` form.
         * 
         * ---
         * ### Additional Notes
         * 
         * #### Alias
         * There is considerable complexity with `alias` (aka `evm_address`) for
         * Accounts. Much of this comes from the existence of a "hidden" alias for
         * almost all accounts, and the reuse of the alias field for both EVM reference
         * and "automatic" account creation.<br/>
         * For the purposes of this specification, we will use the following terms for
         * clarity.
         * - `key_alias`<br/>
         * The account public key as a protobuf serialized message and used for
         * auto-creation and subsequent lookup. This is only valid if the account
         * key is a single `primitive` key, either Ed25519 or ECDSA_SECP256K1.
         * - `evm_address`<br/>
         * Exists for every account and is one of
         * - `contract_address`<br/>
         * The 20 byte EVM address prescribed by `CREATE` or `CREATE2`
         * - `evm_key_address`<br/>
         * An arbitrary 20 byte EVM address that, for a usable externally owned
         * account (EOA) SHALL be the rightmost 20 bytes of the Keccak-256 hash
         * of a ECDSA_SECP256K1 key.<br/>
         * Such accounts may be created in one of three ways:
         * - Sending hbar or fungible tokens to an unused
         * ECDSA_SECP256K1 key alias.
         * - Sending hbar or fungible tokens to an unassigned 20-byte
         * EVM address.
         * - Submitting a `CryptoCreate` signed with the corresponding
         * private key.
         * - `long_zero`<br/>
         * A synthetic 20 byte address inferred for "normally" created accounts.
         * It is constructed from the "standard" AccountID as follows.
         * 1. 4 byte big-endian shard number
         * 1. 8 byte big-endian realm number
         * 1. 8 byte big-endian entity number<br/>
         * 
         * The `alias` field in the `Account` message SHALL contain one of four values
         * for any given account.
         * - The `key_alias`, if the account was created by transferring HBAR to the
         * `key_alias` public key value.
         * - The `evm_key_address` if the account was created from an EVM public key
         * - The `contract_address` if the account belongs to an EVM contract
         * - Not-Set/null/Bytes.EMPTY (collectively `null`) if the account was
         * created normally
         * 
         * If the `alias` field of an `Account` is any form of `null`, then the account
         * MAY be referred to by `alias` in an `AccountID` by using the `long_zero`
         * address for the account.<br/>
         * This "hidden default" alias SHALL NOT be stored, but is synthesized by the
         * node software as needed, and may be synthesized by an EVM contract or client
         * software as well.
         * 
         * ---
         * 
         * #### Alias forms
         * An `AccountID` in a transaction MAY reference an `Account` with
         * `shard.realm.alias`.<br/>
         * If the account `alias` field is set for an Account, that value SHALL be the
         * account alias.<br/>
         * If the account `alias` field is not set for an Account, the `long_zero` alias
         * SHALL be the account alias.
         * @constructor
         * @param {proto.AccountID.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AccountID = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A whole number shard identifier.
         * @member {number|Long} shardNum
         * @memberof proto.AccountID
         * @instance
         */
        AccountID.prototype.shardNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number realm identifier.
         * @member {number|Long} realmNum
         * @memberof proto.AccountID
         * @instance
         */
        AccountID.prototype.realmNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number account number, unique within its realm and shard.
         * <p>
         * For any AccountID fields in the query response, transaction records,
         * transaction receipts, or block stream `accountNum` MUST be used.
         * @member {number|Long|null|undefined} accountNum
         * @memberof proto.AccountID
         * @instance
         */
        AccountID.prototype.accountNum = null;

        /**
         * An alias value.<br/>
         * Alias is a value used in some contexts to refer to an account when
         * account number is not available, and may be an alias public key, or
         * an EVM address.
         * @member {Uint8Array|null|undefined} alias
         * @memberof proto.AccountID
         * @instance
         */
        AccountID.prototype.alias = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * AccountID account.
         * @member {"accountNum"|"alias"|undefined} account
         * @memberof proto.AccountID
         * @instance
         */
        $Object.defineProperty(AccountID.prototype, "account", {
            get: $util.oneOfGetter($oneOfFields = ["accountNum", "alias"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AccountID instance using the specified properties.
         * @function create
         * @memberof proto.AccountID
         * @static
         * @param {proto.AccountID.$Properties=} [properties] Properties to set
         * @returns {proto.AccountID} AccountID instance
         * @type {{
         *   (properties: proto.AccountID.$Shape): proto.AccountID & proto.AccountID.$Shape;
         *   (properties?: proto.AccountID.$Properties): proto.AccountID;
         * }}
         */
        AccountID.create = function(properties) {
            return new AccountID(properties);
        };

        /**
         * Encodes the specified AccountID message. Does not implicitly {@link proto.AccountID.verify|verify} messages.
         * @function encode
         * @memberof proto.AccountID
         * @static
         * @param {proto.AccountID.$Properties} message AccountID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountID.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum") && (typeof message.shardNum === "object" ? message.shardNum.low || message.shardNum.high : message.shardNum !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.shardNum);
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum") && (typeof message.realmNum === "object" ? message.realmNum.low || message.realmNum.high : message.realmNum !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.realmNum);
            if (message.accountNum != null && $Object.hasOwnProperty.call(message, "accountNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.accountNum);
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.alias);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AccountID message, length delimited. Does not implicitly {@link proto.AccountID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AccountID
         * @static
         * @param {proto.AccountID.$Properties} message AccountID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountID.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AccountID message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AccountID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AccountID & proto.AccountID.$Shape} AccountID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountID.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.AccountID();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.shardNum = value;
                        else
                            delete message.shardNum;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.realmNum = value;
                        else
                            delete message.realmNum;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        message.accountNum = reader.int64();
                        message.account = "accountNum";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.alias = reader.bytes();
                        message.account = "alias";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AccountID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AccountID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AccountID & proto.AccountID.$Shape} AccountID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountID.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AccountID message.
         * @function verify
         * @memberof proto.AccountID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AccountID.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (!$util.isInteger(message.shardNum) && !(message.shardNum && $util.isInteger(message.shardNum.low) && $util.isInteger(message.shardNum.high)))
                    return "shardNum: integer|Long expected";
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (!$util.isInteger(message.realmNum) && !(message.realmNum && $util.isInteger(message.realmNum.low) && $util.isInteger(message.realmNum.high)))
                    return "realmNum: integer|Long expected";
            if (message.accountNum != null && $Object.hasOwnProperty.call(message, "accountNum")) {
                properties.account = 1;
                if (!$util.isInteger(message.accountNum) && !(message.accountNum && $util.isInteger(message.accountNum.low) && $util.isInteger(message.accountNum.high)))
                    return "accountNum: integer|Long expected";
            }
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias")) {
                if (properties.account === 1)
                    return "account: multiple values";
                properties.account = 1;
                if (!(message.alias && typeof message.alias.length === "number" || $util.isString(message.alias)))
                    return "alias: buffer expected";
            }
            return null;
        };

        /**
         * Creates an AccountID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AccountID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AccountID} AccountID
         */
        AccountID.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.AccountID)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.AccountID: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.AccountID();
            if (object.shardNum != null)
                if (typeof object.shardNum === "object" ? object.shardNum.low || object.shardNum.high : $Number(object.shardNum) !== 0)
                    if ($util.Long)
                        message.shardNum = $util.Long.fromValue(object.shardNum, false);
                    else if (typeof object.shardNum === "string")
                        message.shardNum = $parseInt(object.shardNum, 10);
                    else if (typeof object.shardNum === "number")
                        message.shardNum = object.shardNum;
                    else if (typeof object.shardNum === "object")
                        message.shardNum = new $util.LongBits(object.shardNum.low >>> 0, object.shardNum.high >>> 0).toNumber();
            if (object.realmNum != null)
                if (typeof object.realmNum === "object" ? object.realmNum.low || object.realmNum.high : $Number(object.realmNum) !== 0)
                    if ($util.Long)
                        message.realmNum = $util.Long.fromValue(object.realmNum, false);
                    else if (typeof object.realmNum === "string")
                        message.realmNum = $parseInt(object.realmNum, 10);
                    else if (typeof object.realmNum === "number")
                        message.realmNum = object.realmNum;
                    else if (typeof object.realmNum === "object")
                        message.realmNum = new $util.LongBits(object.realmNum.low >>> 0, object.realmNum.high >>> 0).toNumber();
            if (object.accountNum != null)
                if ($util.Long)
                    message.accountNum = $util.Long.fromValue(object.accountNum, false);
                else if (typeof object.accountNum === "string")
                    message.accountNum = $parseInt(object.accountNum, 10);
                else if (typeof object.accountNum === "number")
                    message.accountNum = object.accountNum;
                else if (typeof object.accountNum === "object")
                    message.accountNum = new $util.LongBits(object.accountNum.low >>> 0, object.accountNum.high >>> 0).toNumber();
            if (object.alias != null)
                if (typeof object.alias === "string")
                    $util.base64.decode(object.alias, message.alias = $util.newBuffer($util.base64.length(object.alias)), 0);
                else if (object.alias.length >= 0)
                    message.alias = object.alias;
            return message;
        };

        /**
         * Creates a plain object from an AccountID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AccountID
         * @static
         * @param {proto.AccountID} message AccountID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccountID.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.shardNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.shardNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.realmNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.realmNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.shardNum = typeof message.shardNum === "number" ? $BigInt(message.shardNum) : $util.Long.fromBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0, false).toBigInt();
                else if (typeof message.shardNum === "number")
                    object.shardNum = options.longs === $String ? $String(message.shardNum) : message.shardNum;
                else
                    object.shardNum = options.longs === $String ? $util.Long.prototype.toString.call(message.shardNum) : options.longs === $Number ? new $util.LongBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0).toNumber() : message.shardNum;
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.realmNum = typeof message.realmNum === "number" ? $BigInt(message.realmNum) : $util.Long.fromBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0, false).toBigInt();
                else if (typeof message.realmNum === "number")
                    object.realmNum = options.longs === $String ? $String(message.realmNum) : message.realmNum;
                else
                    object.realmNum = options.longs === $String ? $util.Long.prototype.toString.call(message.realmNum) : options.longs === $Number ? new $util.LongBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0).toNumber() : message.realmNum;
            if (message.accountNum != null && $Object.hasOwnProperty.call(message, "accountNum")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.accountNum = typeof message.accountNum === "number" ? $BigInt(message.accountNum) : $util.Long.fromBits(message.accountNum.low >>> 0, message.accountNum.high >>> 0, false).toBigInt();
                else if (typeof message.accountNum === "number")
                    object.accountNum = options.longs === $String ? $String(message.accountNum) : message.accountNum;
                else
                    object.accountNum = options.longs === $String ? $util.Long.prototype.toString.call(message.accountNum) : options.longs === $Number ? new $util.LongBits(message.accountNum.low >>> 0, message.accountNum.high >>> 0).toNumber() : message.accountNum;
                if (options.oneofs)
                    object.account = "accountNum";
            }
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias")) {
                object.alias = options.bytes === $String ? $util.base64.encode(message.alias, 0, message.alias.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.alias) : message.alias;
                if (options.oneofs)
                    object.account = "alias";
            }
            return object;
        };

        /**
         * Converts this AccountID to JSON.
         * @function toJSON
         * @memberof proto.AccountID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccountID.prototype.toJSON = function() {
            return AccountID.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AccountID
         * @function getTypeUrl
         * @memberof proto.AccountID
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AccountID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AccountID";
        };

        return AccountID;
    })();

    proto.NftID = (function() {

        /**
         * Properties of a NftID.
         * @typedef {Object} proto.NftID.$Properties
         * @property {proto.TokenID.$Properties|null} [token_ID] A token identifier.<br/>
         * This token represents the collection containing this NFT.
         * @property {number|Long|null} [serialNumber] A unique serial number.<br/>
         * This serial number is unique within its token type.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NftID.
         * @memberof proto
         * @interface INftID
         * @augments proto.NftID.$Properties
         * @deprecated Use proto.NftID.$Properties instead.
         */

        /**
         * Shape of a NftID.
         * @typedef {proto.NftID.$Properties} proto.NftID.$Shape
         */

        /**
         * Constructs a new NftID.
         * @memberof proto
         * @classdesc An identifier for a unique token (or "NFT"), used by both contract
         * and token services.
         * @constructor
         * @param {proto.NftID.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NftID = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.<br/>
         * This token represents the collection containing this NFT.
         * @member {proto.TokenID.$Properties|null|undefined} token_ID
         * @memberof proto.NftID
         * @instance
         */
        NftID.prototype.token_ID = null;

        /**
         * A unique serial number.<br/>
         * This serial number is unique within its token type.
         * @member {number|Long} serialNumber
         * @memberof proto.NftID
         * @instance
         */
        NftID.prototype.serialNumber = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new NftID instance using the specified properties.
         * @function create
         * @memberof proto.NftID
         * @static
         * @param {proto.NftID.$Properties=} [properties] Properties to set
         * @returns {proto.NftID} NftID instance
         * @type {{
         *   (properties: proto.NftID.$Shape): proto.NftID & proto.NftID.$Shape;
         *   (properties?: proto.NftID.$Properties): proto.NftID;
         * }}
         */
        NftID.create = function(properties) {
            return new NftID(properties);
        };

        /**
         * Encodes the specified NftID message. Does not implicitly {@link proto.NftID.verify|verify} messages.
         * @function encode
         * @memberof proto.NftID
         * @static
         * @param {proto.NftID.$Properties} message NftID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NftID.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token_ID != null && $Object.hasOwnProperty.call(message, "token_ID"))
                $root.proto.TokenID.encode(message.token_ID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.serialNumber != null && $Object.hasOwnProperty.call(message, "serialNumber") && (typeof message.serialNumber === "object" ? message.serialNumber.low || message.serialNumber.high : message.serialNumber !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.serialNumber);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NftID message, length delimited. Does not implicitly {@link proto.NftID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NftID
         * @static
         * @param {proto.NftID.$Properties} message NftID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NftID.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NftID message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NftID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NftID & proto.NftID.$Shape} NftID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NftID.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NftID();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token_ID = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token_ID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.serialNumber = value;
                        else
                            delete message.serialNumber;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NftID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NftID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NftID & proto.NftID.$Shape} NftID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NftID.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NftID message.
         * @function verify
         * @memberof proto.NftID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NftID.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token_ID != null && $Object.hasOwnProperty.call(message, "token_ID")) {
                let error = $root.proto.TokenID.verify(message.token_ID, _depth + 1);
                if (error)
                    return "token_ID." + error;
            }
            if (message.serialNumber != null && $Object.hasOwnProperty.call(message, "serialNumber"))
                if (!$util.isInteger(message.serialNumber) && !(message.serialNumber && $util.isInteger(message.serialNumber.low) && $util.isInteger(message.serialNumber.high)))
                    return "serialNumber: integer|Long expected";
            return null;
        };

        /**
         * Creates a NftID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NftID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NftID} NftID
         */
        NftID.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NftID)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NftID: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NftID();
            if (object.token_ID != null) {
                if (!$util.isObject(object.token_ID))
                    throw $TypeError(".proto.NftID.token_ID: object expected");
                message.token_ID = $root.proto.TokenID.fromObject(object.token_ID, _depth + 1);
            }
            if (object.serialNumber != null)
                if (typeof object.serialNumber === "object" ? object.serialNumber.low || object.serialNumber.high : $Number(object.serialNumber) !== 0)
                    if ($util.Long)
                        message.serialNumber = $util.Long.fromValue(object.serialNumber, false);
                    else if (typeof object.serialNumber === "string")
                        message.serialNumber = $parseInt(object.serialNumber, 10);
                    else if (typeof object.serialNumber === "number")
                        message.serialNumber = object.serialNumber;
                    else if (typeof object.serialNumber === "object")
                        message.serialNumber = new $util.LongBits(object.serialNumber.low >>> 0, object.serialNumber.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a NftID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NftID
         * @static
         * @param {proto.NftID} message NftID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NftID.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.token_ID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.serialNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.serialNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.token_ID != null && $Object.hasOwnProperty.call(message, "token_ID"))
                object.token_ID = $root.proto.TokenID.toObject(message.token_ID, options, _depth + 1);
            if (message.serialNumber != null && $Object.hasOwnProperty.call(message, "serialNumber"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.serialNumber = typeof message.serialNumber === "number" ? $BigInt(message.serialNumber) : $util.Long.fromBits(message.serialNumber.low >>> 0, message.serialNumber.high >>> 0, false).toBigInt();
                else if (typeof message.serialNumber === "number")
                    object.serialNumber = options.longs === $String ? $String(message.serialNumber) : message.serialNumber;
                else
                    object.serialNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.serialNumber) : options.longs === $Number ? new $util.LongBits(message.serialNumber.low >>> 0, message.serialNumber.high >>> 0).toNumber() : message.serialNumber;
            return object;
        };

        /**
         * Converts this NftID to JSON.
         * @function toJSON
         * @memberof proto.NftID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NftID.prototype.toJSON = function() {
            return NftID.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NftID
         * @function getTypeUrl
         * @memberof proto.NftID
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NftID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NftID";
        };

        return NftID;
    })();

    proto.FileID = (function() {

        /**
         * Properties of a FileID.
         * @typedef {Object} proto.FileID.$Properties
         * @property {number|Long|null} [shardNum] A whole number shard identifier.
         * @property {number|Long|null} [realmNum] A whole number realm identifier.
         * @property {number|Long|null} [fileNum] A whole number file identifier, unique within its realm and shard.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FileID.
         * @memberof proto
         * @interface IFileID
         * @augments proto.FileID.$Properties
         * @deprecated Use proto.FileID.$Properties instead.
         */

        /**
         * Shape of a FileID.
         * @typedef {proto.FileID.$Properties} proto.FileID.$Shape
         */

        /**
         * Constructs a new FileID.
         * @memberof proto
         * @classdesc An identifier for a File within the network.
         * @constructor
         * @param {proto.FileID.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FileID = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A whole number shard identifier.
         * @member {number|Long} shardNum
         * @memberof proto.FileID
         * @instance
         */
        FileID.prototype.shardNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number realm identifier.
         * @member {number|Long} realmNum
         * @memberof proto.FileID
         * @instance
         */
        FileID.prototype.realmNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number file identifier, unique within its realm and shard.
         * @member {number|Long} fileNum
         * @memberof proto.FileID
         * @instance
         */
        FileID.prototype.fileNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new FileID instance using the specified properties.
         * @function create
         * @memberof proto.FileID
         * @static
         * @param {proto.FileID.$Properties=} [properties] Properties to set
         * @returns {proto.FileID} FileID instance
         * @type {{
         *   (properties: proto.FileID.$Shape): proto.FileID & proto.FileID.$Shape;
         *   (properties?: proto.FileID.$Properties): proto.FileID;
         * }}
         */
        FileID.create = function(properties) {
            return new FileID(properties);
        };

        /**
         * Encodes the specified FileID message. Does not implicitly {@link proto.FileID.verify|verify} messages.
         * @function encode
         * @memberof proto.FileID
         * @static
         * @param {proto.FileID.$Properties} message FileID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileID.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum") && (typeof message.shardNum === "object" ? message.shardNum.low || message.shardNum.high : message.shardNum !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.shardNum);
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum") && (typeof message.realmNum === "object" ? message.realmNum.low || message.realmNum.high : message.realmNum !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.realmNum);
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum") && (typeof message.fileNum === "object" ? message.fileNum.low || message.fileNum.high : message.fileNum !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.fileNum);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FileID message, length delimited. Does not implicitly {@link proto.FileID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FileID
         * @static
         * @param {proto.FileID.$Properties} message FileID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileID.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FileID message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FileID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FileID & proto.FileID.$Shape} FileID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileID.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FileID();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.shardNum = value;
                        else
                            delete message.shardNum;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.realmNum = value;
                        else
                            delete message.realmNum;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.fileNum = value;
                        else
                            delete message.fileNum;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FileID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FileID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FileID & proto.FileID.$Shape} FileID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileID.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FileID message.
         * @function verify
         * @memberof proto.FileID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FileID.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (!$util.isInteger(message.shardNum) && !(message.shardNum && $util.isInteger(message.shardNum.low) && $util.isInteger(message.shardNum.high)))
                    return "shardNum: integer|Long expected";
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (!$util.isInteger(message.realmNum) && !(message.realmNum && $util.isInteger(message.realmNum.low) && $util.isInteger(message.realmNum.high)))
                    return "realmNum: integer|Long expected";
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum"))
                if (!$util.isInteger(message.fileNum) && !(message.fileNum && $util.isInteger(message.fileNum.low) && $util.isInteger(message.fileNum.high)))
                    return "fileNum: integer|Long expected";
            return null;
        };

        /**
         * Creates a FileID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FileID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FileID} FileID
         */
        FileID.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FileID)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FileID: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FileID();
            if (object.shardNum != null)
                if (typeof object.shardNum === "object" ? object.shardNum.low || object.shardNum.high : $Number(object.shardNum) !== 0)
                    if ($util.Long)
                        message.shardNum = $util.Long.fromValue(object.shardNum, false);
                    else if (typeof object.shardNum === "string")
                        message.shardNum = $parseInt(object.shardNum, 10);
                    else if (typeof object.shardNum === "number")
                        message.shardNum = object.shardNum;
                    else if (typeof object.shardNum === "object")
                        message.shardNum = new $util.LongBits(object.shardNum.low >>> 0, object.shardNum.high >>> 0).toNumber();
            if (object.realmNum != null)
                if (typeof object.realmNum === "object" ? object.realmNum.low || object.realmNum.high : $Number(object.realmNum) !== 0)
                    if ($util.Long)
                        message.realmNum = $util.Long.fromValue(object.realmNum, false);
                    else if (typeof object.realmNum === "string")
                        message.realmNum = $parseInt(object.realmNum, 10);
                    else if (typeof object.realmNum === "number")
                        message.realmNum = object.realmNum;
                    else if (typeof object.realmNum === "object")
                        message.realmNum = new $util.LongBits(object.realmNum.low >>> 0, object.realmNum.high >>> 0).toNumber();
            if (object.fileNum != null)
                if (typeof object.fileNum === "object" ? object.fileNum.low || object.fileNum.high : $Number(object.fileNum) !== 0)
                    if ($util.Long)
                        message.fileNum = $util.Long.fromValue(object.fileNum, false);
                    else if (typeof object.fileNum === "string")
                        message.fileNum = $parseInt(object.fileNum, 10);
                    else if (typeof object.fileNum === "number")
                        message.fileNum = object.fileNum;
                    else if (typeof object.fileNum === "object")
                        message.fileNum = new $util.LongBits(object.fileNum.low >>> 0, object.fileNum.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a FileID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FileID
         * @static
         * @param {proto.FileID} message FileID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FileID.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.shardNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.shardNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.realmNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.realmNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fileNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fileNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.shardNum = typeof message.shardNum === "number" ? $BigInt(message.shardNum) : $util.Long.fromBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0, false).toBigInt();
                else if (typeof message.shardNum === "number")
                    object.shardNum = options.longs === $String ? $String(message.shardNum) : message.shardNum;
                else
                    object.shardNum = options.longs === $String ? $util.Long.prototype.toString.call(message.shardNum) : options.longs === $Number ? new $util.LongBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0).toNumber() : message.shardNum;
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.realmNum = typeof message.realmNum === "number" ? $BigInt(message.realmNum) : $util.Long.fromBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0, false).toBigInt();
                else if (typeof message.realmNum === "number")
                    object.realmNum = options.longs === $String ? $String(message.realmNum) : message.realmNum;
                else
                    object.realmNum = options.longs === $String ? $util.Long.prototype.toString.call(message.realmNum) : options.longs === $Number ? new $util.LongBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0).toNumber() : message.realmNum;
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fileNum = typeof message.fileNum === "number" ? $BigInt(message.fileNum) : $util.Long.fromBits(message.fileNum.low >>> 0, message.fileNum.high >>> 0, false).toBigInt();
                else if (typeof message.fileNum === "number")
                    object.fileNum = options.longs === $String ? $String(message.fileNum) : message.fileNum;
                else
                    object.fileNum = options.longs === $String ? $util.Long.prototype.toString.call(message.fileNum) : options.longs === $Number ? new $util.LongBits(message.fileNum.low >>> 0, message.fileNum.high >>> 0).toNumber() : message.fileNum;
            return object;
        };

        /**
         * Converts this FileID to JSON.
         * @function toJSON
         * @memberof proto.FileID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FileID.prototype.toJSON = function() {
            return FileID.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FileID
         * @function getTypeUrl
         * @memberof proto.FileID
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FileID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FileID";
        };

        return FileID;
    })();

    proto.ContractID = (function() {

        /**
         * Properties of a ContractID.
         * @typedef {Object} proto.ContractID.$Properties
         * @property {number|Long|null} [shardNum] A whole number shard identifier.
         * @property {number|Long|null} [realmNum] A whole number realm identifier.
         * @property {number|Long|null} [contractNum] A whole number contract identifier, unique within its realm and shard.
         * @property {Uint8Array|null} [evmAddress] A 20-byte EVM address of the contract to call.
         * <p>
         * A contract created via a HAPI `ContractCreate` call SHALL have
         * an EVM address determined by its `shard.realm.num` identifier.<br/>
         * This address is as follows
         * <ol>
         * <li>4 byte big-endian shard number</li>
         * <li>8 byte big-endian realm number</li>
         * <li>8 byte big-endian contract number</li>
         * </ol>
         * This address is not stored in state, but is computed when needed.
         * <p>
         * Contracts created by any other means, including a HAPI
         * `EthereumTransaction` whose `to` address is the zero address,
         * SHALL have the EVM address prescribed by the `CREATE` or
         * `CREATE2` opcode, as applicable.
         * @property {"contractNum"|"evmAddress"} [contract] ContractID contract
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractID.
         * @memberof proto
         * @interface IContractID
         * @augments proto.ContractID.$Properties
         * @deprecated Use proto.ContractID.$Properties instead.
         */

        /**
         * Narrowed shape of a ContractID.
         * @typedef {{
         *   shardNum?: number|Long|null;
         *   realmNum?: number|Long|null;
         *   contractNum?: number|Long|null;
         *   evmAddress?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ contract?: undefined; contractNum?: null; evmAddress?: null }|{ contract?: "contractNum"; contractNum: number|Long; evmAddress?: null }|{ contract?: "evmAddress"; contractNum?: null; evmAddress: Uint8Array })
         * )} proto.ContractID.$Shape
         */

        /**
         * Constructs a new ContractID.
         * @memberof proto
         * @classdesc An identifier for a smart contract within the network.
         * @constructor
         * @param {proto.ContractID.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractID = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A whole number shard identifier.
         * @member {number|Long} shardNum
         * @memberof proto.ContractID
         * @instance
         */
        ContractID.prototype.shardNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number realm identifier.
         * @member {number|Long} realmNum
         * @memberof proto.ContractID
         * @instance
         */
        ContractID.prototype.realmNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number contract identifier, unique within its realm and shard.
         * @member {number|Long|null|undefined} contractNum
         * @memberof proto.ContractID
         * @instance
         */
        ContractID.prototype.contractNum = null;

        /**
         * A 20-byte EVM address of the contract to call.
         * <p>
         * A contract created via a HAPI `ContractCreate` call SHALL have
         * an EVM address determined by its `shard.realm.num` identifier.<br/>
         * This address is as follows
         * <ol>
         * <li>4 byte big-endian shard number</li>
         * <li>8 byte big-endian realm number</li>
         * <li>8 byte big-endian contract number</li>
         * </ol>
         * This address is not stored in state, but is computed when needed.
         * <p>
         * Contracts created by any other means, including a HAPI
         * `EthereumTransaction` whose `to` address is the zero address,
         * SHALL have the EVM address prescribed by the `CREATE` or
         * `CREATE2` opcode, as applicable.
         * @member {Uint8Array|null|undefined} evmAddress
         * @memberof proto.ContractID
         * @instance
         */
        ContractID.prototype.evmAddress = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ContractID contract.
         * @member {"contractNum"|"evmAddress"|undefined} contract
         * @memberof proto.ContractID
         * @instance
         */
        $Object.defineProperty(ContractID.prototype, "contract", {
            get: $util.oneOfGetter($oneOfFields = ["contractNum", "evmAddress"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ContractID instance using the specified properties.
         * @function create
         * @memberof proto.ContractID
         * @static
         * @param {proto.ContractID.$Properties=} [properties] Properties to set
         * @returns {proto.ContractID} ContractID instance
         * @type {{
         *   (properties: proto.ContractID.$Shape): proto.ContractID & proto.ContractID.$Shape;
         *   (properties?: proto.ContractID.$Properties): proto.ContractID;
         * }}
         */
        ContractID.create = function(properties) {
            return new ContractID(properties);
        };

        /**
         * Encodes the specified ContractID message. Does not implicitly {@link proto.ContractID.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractID
         * @static
         * @param {proto.ContractID.$Properties} message ContractID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractID.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum") && (typeof message.shardNum === "object" ? message.shardNum.low || message.shardNum.high : message.shardNum !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.shardNum);
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum") && (typeof message.realmNum === "object" ? message.realmNum.low || message.realmNum.high : message.realmNum !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.realmNum);
            if (message.contractNum != null && $Object.hasOwnProperty.call(message, "contractNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.contractNum);
            if (message.evmAddress != null && $Object.hasOwnProperty.call(message, "evmAddress"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.evmAddress);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractID message, length delimited. Does not implicitly {@link proto.ContractID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractID
         * @static
         * @param {proto.ContractID.$Properties} message ContractID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractID.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractID message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractID & proto.ContractID.$Shape} ContractID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractID.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractID();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.shardNum = value;
                        else
                            delete message.shardNum;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.realmNum = value;
                        else
                            delete message.realmNum;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        message.contractNum = reader.int64();
                        message.contract = "contractNum";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.evmAddress = reader.bytes();
                        message.contract = "evmAddress";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractID & proto.ContractID.$Shape} ContractID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractID.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractID message.
         * @function verify
         * @memberof proto.ContractID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractID.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (!$util.isInteger(message.shardNum) && !(message.shardNum && $util.isInteger(message.shardNum.low) && $util.isInteger(message.shardNum.high)))
                    return "shardNum: integer|Long expected";
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (!$util.isInteger(message.realmNum) && !(message.realmNum && $util.isInteger(message.realmNum.low) && $util.isInteger(message.realmNum.high)))
                    return "realmNum: integer|Long expected";
            if (message.contractNum != null && $Object.hasOwnProperty.call(message, "contractNum")) {
                properties.contract = 1;
                if (!$util.isInteger(message.contractNum) && !(message.contractNum && $util.isInteger(message.contractNum.low) && $util.isInteger(message.contractNum.high)))
                    return "contractNum: integer|Long expected";
            }
            if (message.evmAddress != null && $Object.hasOwnProperty.call(message, "evmAddress")) {
                if (properties.contract === 1)
                    return "contract: multiple values";
                properties.contract = 1;
                if (!(message.evmAddress && typeof message.evmAddress.length === "number" || $util.isString(message.evmAddress)))
                    return "evmAddress: buffer expected";
            }
            return null;
        };

        /**
         * Creates a ContractID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractID} ContractID
         */
        ContractID.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractID)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractID: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractID();
            if (object.shardNum != null)
                if (typeof object.shardNum === "object" ? object.shardNum.low || object.shardNum.high : $Number(object.shardNum) !== 0)
                    if ($util.Long)
                        message.shardNum = $util.Long.fromValue(object.shardNum, false);
                    else if (typeof object.shardNum === "string")
                        message.shardNum = $parseInt(object.shardNum, 10);
                    else if (typeof object.shardNum === "number")
                        message.shardNum = object.shardNum;
                    else if (typeof object.shardNum === "object")
                        message.shardNum = new $util.LongBits(object.shardNum.low >>> 0, object.shardNum.high >>> 0).toNumber();
            if (object.realmNum != null)
                if (typeof object.realmNum === "object" ? object.realmNum.low || object.realmNum.high : $Number(object.realmNum) !== 0)
                    if ($util.Long)
                        message.realmNum = $util.Long.fromValue(object.realmNum, false);
                    else if (typeof object.realmNum === "string")
                        message.realmNum = $parseInt(object.realmNum, 10);
                    else if (typeof object.realmNum === "number")
                        message.realmNum = object.realmNum;
                    else if (typeof object.realmNum === "object")
                        message.realmNum = new $util.LongBits(object.realmNum.low >>> 0, object.realmNum.high >>> 0).toNumber();
            if (object.contractNum != null)
                if ($util.Long)
                    message.contractNum = $util.Long.fromValue(object.contractNum, false);
                else if (typeof object.contractNum === "string")
                    message.contractNum = $parseInt(object.contractNum, 10);
                else if (typeof object.contractNum === "number")
                    message.contractNum = object.contractNum;
                else if (typeof object.contractNum === "object")
                    message.contractNum = new $util.LongBits(object.contractNum.low >>> 0, object.contractNum.high >>> 0).toNumber();
            if (object.evmAddress != null)
                if (typeof object.evmAddress === "string")
                    $util.base64.decode(object.evmAddress, message.evmAddress = $util.newBuffer($util.base64.length(object.evmAddress)), 0);
                else if (object.evmAddress.length >= 0)
                    message.evmAddress = object.evmAddress;
            return message;
        };

        /**
         * Creates a plain object from a ContractID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractID
         * @static
         * @param {proto.ContractID} message ContractID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractID.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.shardNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.shardNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.realmNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.realmNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.shardNum = typeof message.shardNum === "number" ? $BigInt(message.shardNum) : $util.Long.fromBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0, false).toBigInt();
                else if (typeof message.shardNum === "number")
                    object.shardNum = options.longs === $String ? $String(message.shardNum) : message.shardNum;
                else
                    object.shardNum = options.longs === $String ? $util.Long.prototype.toString.call(message.shardNum) : options.longs === $Number ? new $util.LongBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0).toNumber() : message.shardNum;
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.realmNum = typeof message.realmNum === "number" ? $BigInt(message.realmNum) : $util.Long.fromBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0, false).toBigInt();
                else if (typeof message.realmNum === "number")
                    object.realmNum = options.longs === $String ? $String(message.realmNum) : message.realmNum;
                else
                    object.realmNum = options.longs === $String ? $util.Long.prototype.toString.call(message.realmNum) : options.longs === $Number ? new $util.LongBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0).toNumber() : message.realmNum;
            if (message.contractNum != null && $Object.hasOwnProperty.call(message, "contractNum")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.contractNum = typeof message.contractNum === "number" ? $BigInt(message.contractNum) : $util.Long.fromBits(message.contractNum.low >>> 0, message.contractNum.high >>> 0, false).toBigInt();
                else if (typeof message.contractNum === "number")
                    object.contractNum = options.longs === $String ? $String(message.contractNum) : message.contractNum;
                else
                    object.contractNum = options.longs === $String ? $util.Long.prototype.toString.call(message.contractNum) : options.longs === $Number ? new $util.LongBits(message.contractNum.low >>> 0, message.contractNum.high >>> 0).toNumber() : message.contractNum;
                if (options.oneofs)
                    object.contract = "contractNum";
            }
            if (message.evmAddress != null && $Object.hasOwnProperty.call(message, "evmAddress")) {
                object.evmAddress = options.bytes === $String ? $util.base64.encode(message.evmAddress, 0, message.evmAddress.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.evmAddress) : message.evmAddress;
                if (options.oneofs)
                    object.contract = "evmAddress";
            }
            return object;
        };

        /**
         * Converts this ContractID to JSON.
         * @function toJSON
         * @memberof proto.ContractID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractID.prototype.toJSON = function() {
            return ContractID.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractID
         * @function getTypeUrl
         * @memberof proto.ContractID
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractID";
        };

        return ContractID;
    })();

    proto.TopicID = (function() {

        /**
         * Properties of a TopicID.
         * @typedef {Object} proto.TopicID.$Properties
         * @property {number|Long|null} [shardNum] A whole number shard identifier.
         * @property {number|Long|null} [realmNum] A whole number realm identifier.
         * @property {number|Long|null} [topicNum] A whole number topic identifier, unique within its realm and shard.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TopicID.
         * @memberof proto
         * @interface ITopicID
         * @augments proto.TopicID.$Properties
         * @deprecated Use proto.TopicID.$Properties instead.
         */

        /**
         * Shape of a TopicID.
         * @typedef {proto.TopicID.$Properties} proto.TopicID.$Shape
         */

        /**
         * Constructs a new TopicID.
         * @memberof proto
         * @classdesc An unique identifier for a topic.<br/>
         * Topics are part of the consensus service, messages are published to a topic.
         * @constructor
         * @param {proto.TopicID.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TopicID = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A whole number shard identifier.
         * @member {number|Long} shardNum
         * @memberof proto.TopicID
         * @instance
         */
        TopicID.prototype.shardNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number realm identifier.
         * @member {number|Long} realmNum
         * @memberof proto.TopicID
         * @instance
         */
        TopicID.prototype.realmNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number topic identifier, unique within its realm and shard.
         * @member {number|Long} topicNum
         * @memberof proto.TopicID
         * @instance
         */
        TopicID.prototype.topicNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new TopicID instance using the specified properties.
         * @function create
         * @memberof proto.TopicID
         * @static
         * @param {proto.TopicID.$Properties=} [properties] Properties to set
         * @returns {proto.TopicID} TopicID instance
         * @type {{
         *   (properties: proto.TopicID.$Shape): proto.TopicID & proto.TopicID.$Shape;
         *   (properties?: proto.TopicID.$Properties): proto.TopicID;
         * }}
         */
        TopicID.create = function(properties) {
            return new TopicID(properties);
        };

        /**
         * Encodes the specified TopicID message. Does not implicitly {@link proto.TopicID.verify|verify} messages.
         * @function encode
         * @memberof proto.TopicID
         * @static
         * @param {proto.TopicID.$Properties} message TopicID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TopicID.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum") && (typeof message.shardNum === "object" ? message.shardNum.low || message.shardNum.high : message.shardNum !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.shardNum);
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum") && (typeof message.realmNum === "object" ? message.realmNum.low || message.realmNum.high : message.realmNum !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.realmNum);
            if (message.topicNum != null && $Object.hasOwnProperty.call(message, "topicNum") && (typeof message.topicNum === "object" ? message.topicNum.low || message.topicNum.high : message.topicNum !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.topicNum);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TopicID message, length delimited. Does not implicitly {@link proto.TopicID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TopicID
         * @static
         * @param {proto.TopicID.$Properties} message TopicID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TopicID.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TopicID message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TopicID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TopicID & proto.TopicID.$Shape} TopicID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TopicID.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TopicID();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.shardNum = value;
                        else
                            delete message.shardNum;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.realmNum = value;
                        else
                            delete message.realmNum;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.topicNum = value;
                        else
                            delete message.topicNum;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TopicID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TopicID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TopicID & proto.TopicID.$Shape} TopicID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TopicID.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TopicID message.
         * @function verify
         * @memberof proto.TopicID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TopicID.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (!$util.isInteger(message.shardNum) && !(message.shardNum && $util.isInteger(message.shardNum.low) && $util.isInteger(message.shardNum.high)))
                    return "shardNum: integer|Long expected";
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (!$util.isInteger(message.realmNum) && !(message.realmNum && $util.isInteger(message.realmNum.low) && $util.isInteger(message.realmNum.high)))
                    return "realmNum: integer|Long expected";
            if (message.topicNum != null && $Object.hasOwnProperty.call(message, "topicNum"))
                if (!$util.isInteger(message.topicNum) && !(message.topicNum && $util.isInteger(message.topicNum.low) && $util.isInteger(message.topicNum.high)))
                    return "topicNum: integer|Long expected";
            return null;
        };

        /**
         * Creates a TopicID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TopicID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TopicID} TopicID
         */
        TopicID.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TopicID)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TopicID: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TopicID();
            if (object.shardNum != null)
                if (typeof object.shardNum === "object" ? object.shardNum.low || object.shardNum.high : $Number(object.shardNum) !== 0)
                    if ($util.Long)
                        message.shardNum = $util.Long.fromValue(object.shardNum, false);
                    else if (typeof object.shardNum === "string")
                        message.shardNum = $parseInt(object.shardNum, 10);
                    else if (typeof object.shardNum === "number")
                        message.shardNum = object.shardNum;
                    else if (typeof object.shardNum === "object")
                        message.shardNum = new $util.LongBits(object.shardNum.low >>> 0, object.shardNum.high >>> 0).toNumber();
            if (object.realmNum != null)
                if (typeof object.realmNum === "object" ? object.realmNum.low || object.realmNum.high : $Number(object.realmNum) !== 0)
                    if ($util.Long)
                        message.realmNum = $util.Long.fromValue(object.realmNum, false);
                    else if (typeof object.realmNum === "string")
                        message.realmNum = $parseInt(object.realmNum, 10);
                    else if (typeof object.realmNum === "number")
                        message.realmNum = object.realmNum;
                    else if (typeof object.realmNum === "object")
                        message.realmNum = new $util.LongBits(object.realmNum.low >>> 0, object.realmNum.high >>> 0).toNumber();
            if (object.topicNum != null)
                if (typeof object.topicNum === "object" ? object.topicNum.low || object.topicNum.high : $Number(object.topicNum) !== 0)
                    if ($util.Long)
                        message.topicNum = $util.Long.fromValue(object.topicNum, false);
                    else if (typeof object.topicNum === "string")
                        message.topicNum = $parseInt(object.topicNum, 10);
                    else if (typeof object.topicNum === "number")
                        message.topicNum = object.topicNum;
                    else if (typeof object.topicNum === "object")
                        message.topicNum = new $util.LongBits(object.topicNum.low >>> 0, object.topicNum.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a TopicID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TopicID
         * @static
         * @param {proto.TopicID} message TopicID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TopicID.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.shardNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.shardNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.realmNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.realmNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.topicNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.topicNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.shardNum = typeof message.shardNum === "number" ? $BigInt(message.shardNum) : $util.Long.fromBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0, false).toBigInt();
                else if (typeof message.shardNum === "number")
                    object.shardNum = options.longs === $String ? $String(message.shardNum) : message.shardNum;
                else
                    object.shardNum = options.longs === $String ? $util.Long.prototype.toString.call(message.shardNum) : options.longs === $Number ? new $util.LongBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0).toNumber() : message.shardNum;
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.realmNum = typeof message.realmNum === "number" ? $BigInt(message.realmNum) : $util.Long.fromBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0, false).toBigInt();
                else if (typeof message.realmNum === "number")
                    object.realmNum = options.longs === $String ? $String(message.realmNum) : message.realmNum;
                else
                    object.realmNum = options.longs === $String ? $util.Long.prototype.toString.call(message.realmNum) : options.longs === $Number ? new $util.LongBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0).toNumber() : message.realmNum;
            if (message.topicNum != null && $Object.hasOwnProperty.call(message, "topicNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.topicNum = typeof message.topicNum === "number" ? $BigInt(message.topicNum) : $util.Long.fromBits(message.topicNum.low >>> 0, message.topicNum.high >>> 0, false).toBigInt();
                else if (typeof message.topicNum === "number")
                    object.topicNum = options.longs === $String ? $String(message.topicNum) : message.topicNum;
                else
                    object.topicNum = options.longs === $String ? $util.Long.prototype.toString.call(message.topicNum) : options.longs === $Number ? new $util.LongBits(message.topicNum.low >>> 0, message.topicNum.high >>> 0).toNumber() : message.topicNum;
            return object;
        };

        /**
         * Converts this TopicID to JSON.
         * @function toJSON
         * @memberof proto.TopicID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TopicID.prototype.toJSON = function() {
            return TopicID.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TopicID
         * @function getTypeUrl
         * @memberof proto.TopicID
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TopicID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TopicID";
        };

        return TopicID;
    })();

    proto.ScheduleID = (function() {

        /**
         * Properties of a ScheduleID.
         * @typedef {Object} proto.ScheduleID.$Properties
         * @property {number|Long|null} [shardNum] A whole number shard
         * @property {number|Long|null} [realmNum] A whole number realm
         * @property {number|Long|null} [scheduleNum] A whole number schedule, unique within its realm and shard
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ScheduleID.
         * @memberof proto
         * @interface IScheduleID
         * @augments proto.ScheduleID.$Properties
         * @deprecated Use proto.ScheduleID.$Properties instead.
         */

        /**
         * Shape of a ScheduleID.
         * @typedef {proto.ScheduleID.$Properties} proto.ScheduleID.$Shape
         */

        /**
         * Constructs a new ScheduleID.
         * @memberof proto
         * @classdesc An unique identifier for a Schedule
         * @constructor
         * @param {proto.ScheduleID.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ScheduleID = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A whole number shard
         * @member {number|Long} shardNum
         * @memberof proto.ScheduleID
         * @instance
         */
        ScheduleID.prototype.shardNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number realm
         * @member {number|Long} realmNum
         * @memberof proto.ScheduleID
         * @instance
         */
        ScheduleID.prototype.realmNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A whole number schedule, unique within its realm and shard
         * @member {number|Long} scheduleNum
         * @memberof proto.ScheduleID
         * @instance
         */
        ScheduleID.prototype.scheduleNum = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ScheduleID instance using the specified properties.
         * @function create
         * @memberof proto.ScheduleID
         * @static
         * @param {proto.ScheduleID.$Properties=} [properties] Properties to set
         * @returns {proto.ScheduleID} ScheduleID instance
         * @type {{
         *   (properties: proto.ScheduleID.$Shape): proto.ScheduleID & proto.ScheduleID.$Shape;
         *   (properties?: proto.ScheduleID.$Properties): proto.ScheduleID;
         * }}
         */
        ScheduleID.create = function(properties) {
            return new ScheduleID(properties);
        };

        /**
         * Encodes the specified ScheduleID message. Does not implicitly {@link proto.ScheduleID.verify|verify} messages.
         * @function encode
         * @memberof proto.ScheduleID
         * @static
         * @param {proto.ScheduleID.$Properties} message ScheduleID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleID.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum") && (typeof message.shardNum === "object" ? message.shardNum.low || message.shardNum.high : message.shardNum !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.shardNum);
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum") && (typeof message.realmNum === "object" ? message.realmNum.low || message.realmNum.high : message.realmNum !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.realmNum);
            if (message.scheduleNum != null && $Object.hasOwnProperty.call(message, "scheduleNum") && (typeof message.scheduleNum === "object" ? message.scheduleNum.low || message.scheduleNum.high : message.scheduleNum !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.scheduleNum);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ScheduleID message, length delimited. Does not implicitly {@link proto.ScheduleID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ScheduleID
         * @static
         * @param {proto.ScheduleID.$Properties} message ScheduleID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleID.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ScheduleID message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ScheduleID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ScheduleID & proto.ScheduleID.$Shape} ScheduleID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleID.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ScheduleID();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.shardNum = value;
                        else
                            delete message.shardNum;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.realmNum = value;
                        else
                            delete message.realmNum;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.scheduleNum = value;
                        else
                            delete message.scheduleNum;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ScheduleID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ScheduleID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ScheduleID & proto.ScheduleID.$Shape} ScheduleID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleID.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScheduleID message.
         * @function verify
         * @memberof proto.ScheduleID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScheduleID.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (!$util.isInteger(message.shardNum) && !(message.shardNum && $util.isInteger(message.shardNum.low) && $util.isInteger(message.shardNum.high)))
                    return "shardNum: integer|Long expected";
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (!$util.isInteger(message.realmNum) && !(message.realmNum && $util.isInteger(message.realmNum.low) && $util.isInteger(message.realmNum.high)))
                    return "realmNum: integer|Long expected";
            if (message.scheduleNum != null && $Object.hasOwnProperty.call(message, "scheduleNum"))
                if (!$util.isInteger(message.scheduleNum) && !(message.scheduleNum && $util.isInteger(message.scheduleNum.low) && $util.isInteger(message.scheduleNum.high)))
                    return "scheduleNum: integer|Long expected";
            return null;
        };

        /**
         * Creates a ScheduleID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ScheduleID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ScheduleID} ScheduleID
         */
        ScheduleID.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ScheduleID)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ScheduleID: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ScheduleID();
            if (object.shardNum != null)
                if (typeof object.shardNum === "object" ? object.shardNum.low || object.shardNum.high : $Number(object.shardNum) !== 0)
                    if ($util.Long)
                        message.shardNum = $util.Long.fromValue(object.shardNum, false);
                    else if (typeof object.shardNum === "string")
                        message.shardNum = $parseInt(object.shardNum, 10);
                    else if (typeof object.shardNum === "number")
                        message.shardNum = object.shardNum;
                    else if (typeof object.shardNum === "object")
                        message.shardNum = new $util.LongBits(object.shardNum.low >>> 0, object.shardNum.high >>> 0).toNumber();
            if (object.realmNum != null)
                if (typeof object.realmNum === "object" ? object.realmNum.low || object.realmNum.high : $Number(object.realmNum) !== 0)
                    if ($util.Long)
                        message.realmNum = $util.Long.fromValue(object.realmNum, false);
                    else if (typeof object.realmNum === "string")
                        message.realmNum = $parseInt(object.realmNum, 10);
                    else if (typeof object.realmNum === "number")
                        message.realmNum = object.realmNum;
                    else if (typeof object.realmNum === "object")
                        message.realmNum = new $util.LongBits(object.realmNum.low >>> 0, object.realmNum.high >>> 0).toNumber();
            if (object.scheduleNum != null)
                if (typeof object.scheduleNum === "object" ? object.scheduleNum.low || object.scheduleNum.high : $Number(object.scheduleNum) !== 0)
                    if ($util.Long)
                        message.scheduleNum = $util.Long.fromValue(object.scheduleNum, false);
                    else if (typeof object.scheduleNum === "string")
                        message.scheduleNum = $parseInt(object.scheduleNum, 10);
                    else if (typeof object.scheduleNum === "number")
                        message.scheduleNum = object.scheduleNum;
                    else if (typeof object.scheduleNum === "object")
                        message.scheduleNum = new $util.LongBits(object.scheduleNum.low >>> 0, object.scheduleNum.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ScheduleID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ScheduleID
         * @static
         * @param {proto.ScheduleID} message ScheduleID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScheduleID.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.shardNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.shardNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.realmNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.realmNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.scheduleNum = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.scheduleNum = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.shardNum != null && $Object.hasOwnProperty.call(message, "shardNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.shardNum = typeof message.shardNum === "number" ? $BigInt(message.shardNum) : $util.Long.fromBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0, false).toBigInt();
                else if (typeof message.shardNum === "number")
                    object.shardNum = options.longs === $String ? $String(message.shardNum) : message.shardNum;
                else
                    object.shardNum = options.longs === $String ? $util.Long.prototype.toString.call(message.shardNum) : options.longs === $Number ? new $util.LongBits(message.shardNum.low >>> 0, message.shardNum.high >>> 0).toNumber() : message.shardNum;
            if (message.realmNum != null && $Object.hasOwnProperty.call(message, "realmNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.realmNum = typeof message.realmNum === "number" ? $BigInt(message.realmNum) : $util.Long.fromBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0, false).toBigInt();
                else if (typeof message.realmNum === "number")
                    object.realmNum = options.longs === $String ? $String(message.realmNum) : message.realmNum;
                else
                    object.realmNum = options.longs === $String ? $util.Long.prototype.toString.call(message.realmNum) : options.longs === $Number ? new $util.LongBits(message.realmNum.low >>> 0, message.realmNum.high >>> 0).toNumber() : message.realmNum;
            if (message.scheduleNum != null && $Object.hasOwnProperty.call(message, "scheduleNum"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.scheduleNum = typeof message.scheduleNum === "number" ? $BigInt(message.scheduleNum) : $util.Long.fromBits(message.scheduleNum.low >>> 0, message.scheduleNum.high >>> 0, false).toBigInt();
                else if (typeof message.scheduleNum === "number")
                    object.scheduleNum = options.longs === $String ? $String(message.scheduleNum) : message.scheduleNum;
                else
                    object.scheduleNum = options.longs === $String ? $util.Long.prototype.toString.call(message.scheduleNum) : options.longs === $Number ? new $util.LongBits(message.scheduleNum.low >>> 0, message.scheduleNum.high >>> 0).toNumber() : message.scheduleNum;
            return object;
        };

        /**
         * Converts this ScheduleID to JSON.
         * @function toJSON
         * @memberof proto.ScheduleID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScheduleID.prototype.toJSON = function() {
            return ScheduleID.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ScheduleID
         * @function getTypeUrl
         * @memberof proto.ScheduleID
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ScheduleID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ScheduleID";
        };

        return ScheduleID;
    })();

    proto.TransactionID = (function() {

        /**
         * Properties of a TransactionID.
         * @typedef {Object} proto.TransactionID.$Properties
         * @property {proto.Timestamp.$Properties|null} [transactionValidStart] A timestamp for the transaction start time.<br/>
         * This is the earliest expected start time for this transaction.
         * <p>
         * This value MUST be strictly less than `consensusTimestamp` when the
         * transaction is submitted.
         * @property {proto.AccountID.$Properties|null} [accountID] An Account identifier.
         * <p>
         * The identified account SHALL pay transaction fees for this transaction.
         * @property {boolean|null} [scheduled] A scheduled transaction flag.<br/>
         * If set, this transaction represents the execution of a Schedule after
         * all necessary signatures are gathered.
         * <p>
         * This flag MUST NOT be set in a user-submitted transaction.
         * @property {number|null} [nonce] An identifier for an internal transaction.<br/>
         * An internal transaction is one that was spawned as part of handling a
         * user transaction. These internal transactions share the
         * transactionValidStart and accountID of the user transaction, so a nonce
         * is necessary to give them a unique TransactionID.
         * <p>
         * An example is when a "parent" ContractCreate or ContractCall transaction
         * calls one or more HTS precompiled contracts; each of the "child"
         * transactions spawned for a precompile has a transaction id with a
         * different nonce.
         * <p>
         * This value MUST be unset for user-submitted transactions.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionID.
         * @memberof proto
         * @interface ITransactionID
         * @augments proto.TransactionID.$Properties
         * @deprecated Use proto.TransactionID.$Properties instead.
         */

        /**
         * Shape of a TransactionID.
         * @typedef {{
         *   transactionValidStart?: proto.Timestamp.$Shape|null;
         *   accountID?: proto.AccountID.$Shape|null;
         *   scheduled?: boolean|null;
         *   nonce?: number|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TransactionID.$Shape
         */

        /**
         * Constructs a new TransactionID.
         * @memberof proto
         * @classdesc A transaction identifier.<br/>
         * This is used for retrieving receipts and records for a transaction
         * and internally by the network for detecting when duplicate transactions are
         * submitted.
         * 
         * A transaction may be processed more reliably by submitting it to
         * several nodes, each with a different node account, but all with the same
         * TransactionID. Then, the transaction will take effect when the first of all
         * those nodes submits the transaction and it reaches consensus. The other
         * transactions SHALL NOT be executed (and SHALL result in a
         * `DUPLICATE_TRANSACTION` response).<br/>
         * Multiple submission increase reliability on the assumption that an error in,
         * for example, network connectivity will not affect all nodes equally. Latency
         * might be slightly lower, if one node is handling intake significantly slower
         * than others, for example. The base transaction fee is required for each
         * submission, however, so the total fees charged are significantly higher when
         * using this approach.
         * 
         * ### Requirements
         * Each transaction identifier MUST be unique.<br/>
         * Multiple transactions MAY be submitted with the same transaction
         * identifier, but all except the first SHALL be rejected as duplicate
         * transactions.<br/>
         * An identifier MUST specify a `payer` account to be charged all fees
         * associated with the transaction.<br/>
         * The `payer` account MUST exist and MUST have sufficient HBAR to pay all
         * transaction fees.<br/>
         * An identifier MUST specify a "valid start time".<br/>
         * The "valid start time" MUST be strictly _earlier_ than the current
         * network consensus time when submitted.<br/>
         * The "valid start time" MUST NOT be more than `transaction.maxValidDuration`
         * seconds before the current network consensus time when submitted.<br/>
         * A client-submitted transaction MUST NOT set the `scheduled` flag.
         * 
         * ### Additional Notes
         * 
         * Additional items applicable to Scheduled Transactions:
         * 
         * - The ID of a Scheduled Transaction, once executed, SHALL inherit both
         * `transactionValidStart` and `accountID` from the `ScheduleCreate`
         * transaction that created the schedule.
         * - The `scheduled` property SHALL be set for Scheduled Transactions.
         * @constructor
         * @param {proto.TransactionID.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionID = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A timestamp for the transaction start time.<br/>
         * This is the earliest expected start time for this transaction.
         * <p>
         * This value MUST be strictly less than `consensusTimestamp` when the
         * transaction is submitted.
         * @member {proto.Timestamp.$Properties|null|undefined} transactionValidStart
         * @memberof proto.TransactionID
         * @instance
         */
        TransactionID.prototype.transactionValidStart = null;

        /**
         * An Account identifier.
         * <p>
         * The identified account SHALL pay transaction fees for this transaction.
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.TransactionID
         * @instance
         */
        TransactionID.prototype.accountID = null;

        /**
         * A scheduled transaction flag.<br/>
         * If set, this transaction represents the execution of a Schedule after
         * all necessary signatures are gathered.
         * <p>
         * This flag MUST NOT be set in a user-submitted transaction.
         * @member {boolean} scheduled
         * @memberof proto.TransactionID
         * @instance
         */
        TransactionID.prototype.scheduled = false;

        /**
         * An identifier for an internal transaction.<br/>
         * An internal transaction is one that was spawned as part of handling a
         * user transaction. These internal transactions share the
         * transactionValidStart and accountID of the user transaction, so a nonce
         * is necessary to give them a unique TransactionID.
         * <p>
         * An example is when a "parent" ContractCreate or ContractCall transaction
         * calls one or more HTS precompiled contracts; each of the "child"
         * transactions spawned for a precompile has a transaction id with a
         * different nonce.
         * <p>
         * This value MUST be unset for user-submitted transactions.
         * @member {number} nonce
         * @memberof proto.TransactionID
         * @instance
         */
        TransactionID.prototype.nonce = 0;

        /**
         * Creates a new TransactionID instance using the specified properties.
         * @function create
         * @memberof proto.TransactionID
         * @static
         * @param {proto.TransactionID.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionID} TransactionID instance
         * @type {{
         *   (properties: proto.TransactionID.$Shape): proto.TransactionID & proto.TransactionID.$Shape;
         *   (properties?: proto.TransactionID.$Properties): proto.TransactionID;
         * }}
         */
        TransactionID.create = function(properties) {
            return new TransactionID(properties);
        };

        /**
         * Encodes the specified TransactionID message. Does not implicitly {@link proto.TransactionID.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionID
         * @static
         * @param {proto.TransactionID.$Properties} message TransactionID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionID.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.transactionValidStart != null && $Object.hasOwnProperty.call(message, "transactionValidStart"))
                $root.proto.Timestamp.encode(message.transactionValidStart, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.scheduled != null && $Object.hasOwnProperty.call(message, "scheduled") && message.scheduled !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.scheduled);
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce") && message.nonce !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.nonce);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionID message, length delimited. Does not implicitly {@link proto.TransactionID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionID
         * @static
         * @param {proto.TransactionID.$Properties} message TransactionID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionID.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionID message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionID & proto.TransactionID.$Shape} TransactionID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionID.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionID();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.transactionValidStart = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionValidStart);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.scheduled = value;
                        else
                            delete message.scheduled;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.nonce = value;
                        else
                            delete message.nonce;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionID & proto.TransactionID.$Shape} TransactionID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionID.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionID message.
         * @function verify
         * @memberof proto.TransactionID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionID.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.transactionValidStart != null && $Object.hasOwnProperty.call(message, "transactionValidStart")) {
                let error = $root.proto.Timestamp.verify(message.transactionValidStart, _depth + 1);
                if (error)
                    return "transactionValidStart." + error;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            if (message.scheduled != null && $Object.hasOwnProperty.call(message, "scheduled"))
                if (typeof message.scheduled !== "boolean")
                    return "scheduled: boolean expected";
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce"))
                if (!$util.isInteger(message.nonce))
                    return "nonce: integer expected";
            return null;
        };

        /**
         * Creates a TransactionID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionID} TransactionID
         */
        TransactionID.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionID)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionID: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionID();
            if (object.transactionValidStart != null) {
                if (!$util.isObject(object.transactionValidStart))
                    throw $TypeError(".proto.TransactionID.transactionValidStart: object expected");
                message.transactionValidStart = $root.proto.Timestamp.fromObject(object.transactionValidStart, _depth + 1);
            }
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.TransactionID.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.scheduled != null)
                if (object.scheduled)
                    message.scheduled = $Boolean(object.scheduled);
            if (object.nonce != null)
                if ($Number(object.nonce) !== 0)
                    message.nonce = object.nonce | 0;
            return message;
        };

        /**
         * Creates a plain object from a TransactionID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionID
         * @static
         * @param {proto.TransactionID} message TransactionID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionID.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.transactionValidStart = null;
                object.accountID = null;
                object.scheduled = false;
                object.nonce = 0;
            }
            if (message.transactionValidStart != null && $Object.hasOwnProperty.call(message, "transactionValidStart"))
                object.transactionValidStart = $root.proto.Timestamp.toObject(message.transactionValidStart, options, _depth + 1);
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            if (message.scheduled != null && $Object.hasOwnProperty.call(message, "scheduled"))
                object.scheduled = message.scheduled;
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce"))
                object.nonce = message.nonce;
            return object;
        };

        /**
         * Converts this TransactionID to JSON.
         * @function toJSON
         * @memberof proto.TransactionID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionID.prototype.toJSON = function() {
            return TransactionID.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionID
         * @function getTypeUrl
         * @memberof proto.TransactionID
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionID";
        };

        return TransactionID;
    })();

    proto.HookId = (function() {

        /**
         * Properties of a HookId.
         * @typedef {Object} proto.HookId.$Properties
         * @property {proto.HookEntityId.$Properties|null} [entityId] The hook's creating entity id.
         * @property {number|Long|null} [hookId] An arbitrary 64-bit identifier.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a HookId.
         * @memberof proto
         * @interface IHookId
         * @augments proto.HookId.$Properties
         * @deprecated Use proto.HookId.$Properties instead.
         */

        /**
         * Shape of a HookId.
         * @typedef {{
         *   entityId?: proto.HookEntityId.$Shape|null;
         *   hookId?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.HookId.$Shape
         */

        /**
         * Constructs a new HookId.
         * @memberof proto
         * @classdesc Once a hook is created, its full id.
         * <p>
         * A composite of its creating entity's id and an arbitrary 64-bit hook id
         * (which need not be sequential).
         * @constructor
         * @param {proto.HookId.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const HookId = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The hook's creating entity id.
         * @member {proto.HookEntityId.$Properties|null|undefined} entityId
         * @memberof proto.HookId
         * @instance
         */
        HookId.prototype.entityId = null;

        /**
         * An arbitrary 64-bit identifier.
         * @member {number|Long} hookId
         * @memberof proto.HookId
         * @instance
         */
        HookId.prototype.hookId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new HookId instance using the specified properties.
         * @function create
         * @memberof proto.HookId
         * @static
         * @param {proto.HookId.$Properties=} [properties] Properties to set
         * @returns {proto.HookId} HookId instance
         * @type {{
         *   (properties: proto.HookId.$Shape): proto.HookId & proto.HookId.$Shape;
         *   (properties?: proto.HookId.$Properties): proto.HookId;
         * }}
         */
        HookId.create = function(properties) {
            return new HookId(properties);
        };

        /**
         * Encodes the specified HookId message. Does not implicitly {@link proto.HookId.verify|verify} messages.
         * @function encode
         * @memberof proto.HookId
         * @static
         * @param {proto.HookId.$Properties} message HookId message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HookId.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.entityId != null && $Object.hasOwnProperty.call(message, "entityId"))
                $root.proto.HookEntityId.encode(message.entityId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId") && (typeof message.hookId === "object" ? message.hookId.low || message.hookId.high : message.hookId !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.hookId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HookId message, length delimited. Does not implicitly {@link proto.HookId.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.HookId
         * @static
         * @param {proto.HookId.$Properties} message HookId message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HookId.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a HookId message from the specified reader or buffer.
         * @function decode
         * @memberof proto.HookId
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.HookId & proto.HookId.$Shape} HookId
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HookId.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.HookId();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.entityId = $root.proto.HookEntityId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.entityId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.hookId = value;
                        else
                            delete message.hookId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HookId message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.HookId
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.HookId & proto.HookId.$Shape} HookId
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HookId.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HookId message.
         * @function verify
         * @memberof proto.HookId
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HookId.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.entityId != null && $Object.hasOwnProperty.call(message, "entityId")) {
                let error = $root.proto.HookEntityId.verify(message.entityId, _depth + 1);
                if (error)
                    return "entityId." + error;
            }
            if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId"))
                if (!$util.isInteger(message.hookId) && !(message.hookId && $util.isInteger(message.hookId.low) && $util.isInteger(message.hookId.high)))
                    return "hookId: integer|Long expected";
            return null;
        };

        /**
         * Creates a HookId message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.HookId
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.HookId} HookId
         */
        HookId.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.HookId)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.HookId: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.HookId();
            if (object.entityId != null) {
                if (!$util.isObject(object.entityId))
                    throw $TypeError(".proto.HookId.entityId: object expected");
                message.entityId = $root.proto.HookEntityId.fromObject(object.entityId, _depth + 1);
            }
            if (object.hookId != null)
                if (typeof object.hookId === "object" ? object.hookId.low || object.hookId.high : $Number(object.hookId) !== 0)
                    if ($util.Long)
                        message.hookId = $util.Long.fromValue(object.hookId, false);
                    else if (typeof object.hookId === "string")
                        message.hookId = $parseInt(object.hookId, 10);
                    else if (typeof object.hookId === "number")
                        message.hookId = object.hookId;
                    else if (typeof object.hookId === "object")
                        message.hookId = new $util.LongBits(object.hookId.low >>> 0, object.hookId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a HookId message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.HookId
         * @static
         * @param {proto.HookId} message HookId
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HookId.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.entityId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.hookId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.hookId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.entityId != null && $Object.hasOwnProperty.call(message, "entityId"))
                object.entityId = $root.proto.HookEntityId.toObject(message.entityId, options, _depth + 1);
            if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.hookId = typeof message.hookId === "number" ? $BigInt(message.hookId) : $util.Long.fromBits(message.hookId.low >>> 0, message.hookId.high >>> 0, false).toBigInt();
                else if (typeof message.hookId === "number")
                    object.hookId = options.longs === $String ? $String(message.hookId) : message.hookId;
                else
                    object.hookId = options.longs === $String ? $util.Long.prototype.toString.call(message.hookId) : options.longs === $Number ? new $util.LongBits(message.hookId.low >>> 0, message.hookId.high >>> 0).toNumber() : message.hookId;
            return object;
        };

        /**
         * Converts this HookId to JSON.
         * @function toJSON
         * @memberof proto.HookId
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HookId.prototype.toJSON = function() {
            return HookId.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HookId
         * @function getTypeUrl
         * @memberof proto.HookId
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HookId.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.HookId";
        };

        return HookId;
    })();

    proto.HookEntityId = (function() {

        /**
         * Properties of a HookEntityId.
         * @typedef {Object} proto.HookEntityId.$Properties
         * @property {proto.AccountID.$Properties|null} [accountId] An account using a hook.
         * @property {proto.ContractID.$Properties|null} [contractId] A contract using a hook.
         * @property {"accountId"|"contractId"} [entityId] HookEntityId entityId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a HookEntityId.
         * @memberof proto
         * @interface IHookEntityId
         * @augments proto.HookEntityId.$Properties
         * @deprecated Use proto.HookEntityId.$Properties instead.
         */

        /**
         * Narrowed shape of a HookEntityId.
         * @typedef {{
         *   accountId?: proto.AccountID.$Shape|null;
         *   contractId?: proto.ContractID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ entityId?: undefined; accountId?: null; contractId?: null }|{ entityId?: "accountId"; accountId: proto.AccountID.$Shape; contractId?: null }|{ entityId?: "contractId"; accountId?: null; contractId: proto.ContractID.$Shape })
         * )} proto.HookEntityId.$Shape
         */

        /**
         * Constructs a new HookEntityId.
         * @memberof proto
         * @classdesc The id of an entity using a hook.
         * @constructor
         * @param {proto.HookEntityId.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const HookEntityId = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An account using a hook.
         * @member {proto.AccountID.$Properties|null|undefined} accountId
         * @memberof proto.HookEntityId
         * @instance
         */
        HookEntityId.prototype.accountId = null;

        /**
         * A contract using a hook.
         * @member {proto.ContractID.$Properties|null|undefined} contractId
         * @memberof proto.HookEntityId
         * @instance
         */
        HookEntityId.prototype.contractId = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * HookEntityId entityId.
         * @member {"accountId"|"contractId"|undefined} entityId
         * @memberof proto.HookEntityId
         * @instance
         */
        $Object.defineProperty(HookEntityId.prototype, "entityId", {
            get: $util.oneOfGetter($oneOfFields = ["accountId", "contractId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new HookEntityId instance using the specified properties.
         * @function create
         * @memberof proto.HookEntityId
         * @static
         * @param {proto.HookEntityId.$Properties=} [properties] Properties to set
         * @returns {proto.HookEntityId} HookEntityId instance
         * @type {{
         *   (properties: proto.HookEntityId.$Shape): proto.HookEntityId & proto.HookEntityId.$Shape;
         *   (properties?: proto.HookEntityId.$Properties): proto.HookEntityId;
         * }}
         */
        HookEntityId.create = function(properties) {
            return new HookEntityId(properties);
        };

        /**
         * Encodes the specified HookEntityId message. Does not implicitly {@link proto.HookEntityId.verify|verify} messages.
         * @function encode
         * @memberof proto.HookEntityId
         * @static
         * @param {proto.HookEntityId.$Properties} message HookEntityId message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HookEntityId.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId"))
                $root.proto.ContractID.encode(message.contractId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HookEntityId message, length delimited. Does not implicitly {@link proto.HookEntityId.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.HookEntityId
         * @static
         * @param {proto.HookEntityId.$Properties} message HookEntityId message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HookEntityId.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a HookEntityId message from the specified reader or buffer.
         * @function decode
         * @memberof proto.HookEntityId
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.HookEntityId & proto.HookEntityId.$Shape} HookEntityId
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HookEntityId.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.HookEntityId();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                        message.entityId = "accountId";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.contractId = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractId);
                        message.entityId = "contractId";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HookEntityId message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.HookEntityId
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.HookEntityId & proto.HookEntityId.$Shape} HookEntityId
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HookEntityId.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HookEntityId message.
         * @function verify
         * @memberof proto.HookEntityId
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HookEntityId.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                properties.entityId = 1;
                {
                    let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                    if (error)
                        return "accountId." + error;
                }
            }
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId")) {
                if (properties.entityId === 1)
                    return "entityId: multiple values";
                properties.entityId = 1;
                {
                    let error = $root.proto.ContractID.verify(message.contractId, _depth + 1);
                    if (error)
                        return "contractId." + error;
                }
            }
            return null;
        };

        /**
         * Creates a HookEntityId message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.HookEntityId
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.HookEntityId} HookEntityId
         */
        HookEntityId.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.HookEntityId)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.HookEntityId: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.HookEntityId();
            if (object.accountId != null) {
                if (!$util.isObject(object.accountId))
                    throw $TypeError(".proto.HookEntityId.accountId: object expected");
                message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
            }
            if (object.contractId != null) {
                if (!$util.isObject(object.contractId))
                    throw $TypeError(".proto.HookEntityId.contractId: object expected");
                message.contractId = $root.proto.ContractID.fromObject(object.contractId, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a HookEntityId message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.HookEntityId
         * @static
         * @param {proto.HookEntityId} message HookEntityId
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HookEntityId.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
                if (options.oneofs)
                    object.entityId = "accountId";
            }
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId")) {
                object.contractId = $root.proto.ContractID.toObject(message.contractId, options, _depth + 1);
                if (options.oneofs)
                    object.entityId = "contractId";
            }
            return object;
        };

        /**
         * Converts this HookEntityId to JSON.
         * @function toJSON
         * @memberof proto.HookEntityId
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HookEntityId.prototype.toJSON = function() {
            return HookEntityId.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HookEntityId
         * @function getTypeUrl
         * @memberof proto.HookEntityId
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HookEntityId.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.HookEntityId";
        };

        return HookEntityId;
    })();

    proto.HookCall = (function() {

        /**
         * Properties of a HookCall.
         * @typedef {Object} proto.HookCall.$Properties
         * @property {number|Long|null} [hookId] The numeric id of the hook to call, when the owning entity is forced by the call site.
         * @property {proto.EvmHookCall.$Properties|null} [evmHookCall] Specification of how to call an EVM hook.
         * @property {"hookId"} [id] HookCall id
         * @property {"evmHookCall"} [callSpec] Specifies details of the call.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a HookCall.
         * @memberof proto
         * @interface IHookCall
         * @augments proto.HookCall.$Properties
         * @deprecated Use proto.HookCall.$Properties instead.
         */

        /**
         * Narrowed shape of a HookCall.
         * @typedef {{
         *   hookId?: number|Long|null;
         *   evmHookCall?: proto.EvmHookCall.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ id?: undefined; hookId?: null }|{ id?: "hookId"; hookId: number|Long })
         * ) & (
         *   ({ callSpec?: undefined; evmHookCall?: null }|{ callSpec?: "evmHookCall"; evmHookCall: proto.EvmHookCall.$Shape })
         * )} proto.HookCall.$Shape
         */

        /**
         * Constructs a new HookCall.
         * @memberof proto
         * @classdesc Specifies a call to a hook from within a transaction.
         * <p>
         * Often the hook's entity is implied by the nature of the call site. For example, when using an account allowance hook
         * inside a crypto transfer, the hook's entity is necessarily the account whose authorization is required.
         * <p>
         * For future extension points where the hook owner is not forced by the context, we include the option to fully
         * specify the hook id for the call.
         * @constructor
         * @param {proto.HookCall.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const HookCall = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The numeric id of the hook to call, when the owning entity is forced by the call site.
         * @member {number|Long|null|undefined} hookId
         * @memberof proto.HookCall
         * @instance
         */
        HookCall.prototype.hookId = null;

        /**
         * Specification of how to call an EVM hook.
         * @member {proto.EvmHookCall.$Properties|null|undefined} evmHookCall
         * @memberof proto.HookCall
         * @instance
         */
        HookCall.prototype.evmHookCall = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * HookCall id.
         * @member {"hookId"|undefined} id
         * @memberof proto.HookCall
         * @instance
         */
        $Object.defineProperty(HookCall.prototype, "id", {
            get: $util.oneOfGetter($oneOfFields = ["hookId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Specifies details of the call.
         * @member {"evmHookCall"|undefined} callSpec
         * @memberof proto.HookCall
         * @instance
         */
        $Object.defineProperty(HookCall.prototype, "callSpec", {
            get: $util.oneOfGetter($oneOfFields = ["evmHookCall"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new HookCall instance using the specified properties.
         * @function create
         * @memberof proto.HookCall
         * @static
         * @param {proto.HookCall.$Properties=} [properties] Properties to set
         * @returns {proto.HookCall} HookCall instance
         * @type {{
         *   (properties: proto.HookCall.$Shape): proto.HookCall & proto.HookCall.$Shape;
         *   (properties?: proto.HookCall.$Properties): proto.HookCall;
         * }}
         */
        HookCall.create = function(properties) {
            return new HookCall(properties);
        };

        /**
         * Encodes the specified HookCall message. Does not implicitly {@link proto.HookCall.verify|verify} messages.
         * @function encode
         * @memberof proto.HookCall
         * @static
         * @param {proto.HookCall.$Properties} message HookCall message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HookCall.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.hookId);
            if (message.evmHookCall != null && $Object.hasOwnProperty.call(message, "evmHookCall"))
                $root.proto.EvmHookCall.encode(message.evmHookCall, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HookCall message, length delimited. Does not implicitly {@link proto.HookCall.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.HookCall
         * @static
         * @param {proto.HookCall.$Properties} message HookCall message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HookCall.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a HookCall message from the specified reader or buffer.
         * @function decode
         * @memberof proto.HookCall
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.HookCall & proto.HookCall.$Shape} HookCall
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HookCall.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.HookCall();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        message.hookId = reader.int64();
                        message.id = "hookId";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.evmHookCall = $root.proto.EvmHookCall.decode(reader, reader.uint32(), $undefined, _depth + 1, message.evmHookCall);
                        message.callSpec = "evmHookCall";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HookCall message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.HookCall
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.HookCall & proto.HookCall.$Shape} HookCall
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HookCall.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HookCall message.
         * @function verify
         * @memberof proto.HookCall
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HookCall.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId")) {
                properties.id = 1;
                if (!$util.isInteger(message.hookId) && !(message.hookId && $util.isInteger(message.hookId.low) && $util.isInteger(message.hookId.high)))
                    return "hookId: integer|Long expected";
            }
            if (message.evmHookCall != null && $Object.hasOwnProperty.call(message, "evmHookCall")) {
                properties.callSpec = 1;
                {
                    let error = $root.proto.EvmHookCall.verify(message.evmHookCall, _depth + 1);
                    if (error)
                        return "evmHookCall." + error;
                }
            }
            return null;
        };

        /**
         * Creates a HookCall message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.HookCall
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.HookCall} HookCall
         */
        HookCall.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.HookCall)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.HookCall: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.HookCall();
            if (object.hookId != null)
                if ($util.Long)
                    message.hookId = $util.Long.fromValue(object.hookId, false);
                else if (typeof object.hookId === "string")
                    message.hookId = $parseInt(object.hookId, 10);
                else if (typeof object.hookId === "number")
                    message.hookId = object.hookId;
                else if (typeof object.hookId === "object")
                    message.hookId = new $util.LongBits(object.hookId.low >>> 0, object.hookId.high >>> 0).toNumber();
            if (object.evmHookCall != null) {
                if (!$util.isObject(object.evmHookCall))
                    throw $TypeError(".proto.HookCall.evmHookCall: object expected");
                message.evmHookCall = $root.proto.EvmHookCall.fromObject(object.evmHookCall, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a HookCall message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.HookCall
         * @static
         * @param {proto.HookCall} message HookCall
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HookCall.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.hookId != null && $Object.hasOwnProperty.call(message, "hookId")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.hookId = typeof message.hookId === "number" ? $BigInt(message.hookId) : $util.Long.fromBits(message.hookId.low >>> 0, message.hookId.high >>> 0, false).toBigInt();
                else if (typeof message.hookId === "number")
                    object.hookId = options.longs === $String ? $String(message.hookId) : message.hookId;
                else
                    object.hookId = options.longs === $String ? $util.Long.prototype.toString.call(message.hookId) : options.longs === $Number ? new $util.LongBits(message.hookId.low >>> 0, message.hookId.high >>> 0).toNumber() : message.hookId;
                if (options.oneofs)
                    object.id = "hookId";
            }
            if (message.evmHookCall != null && $Object.hasOwnProperty.call(message, "evmHookCall")) {
                object.evmHookCall = $root.proto.EvmHookCall.toObject(message.evmHookCall, options, _depth + 1);
                if (options.oneofs)
                    object.callSpec = "evmHookCall";
            }
            return object;
        };

        /**
         * Converts this HookCall to JSON.
         * @function toJSON
         * @memberof proto.HookCall
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HookCall.prototype.toJSON = function() {
            return HookCall.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HookCall
         * @function getTypeUrl
         * @memberof proto.HookCall
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HookCall.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.HookCall";
        };

        return HookCall;
    })();

    proto.EvmHookCall = (function() {

        /**
         * Properties of an EvmHookCall.
         * @typedef {Object} proto.EvmHookCall.$Properties
         * @property {Uint8Array|null} [data] Call data to pass to the hook via the IHieroHook.HookContext#data field.
         * @property {number|Long|null} [gasLimit] The gas limit to use.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an EvmHookCall.
         * @memberof proto
         * @interface IEvmHookCall
         * @augments proto.EvmHookCall.$Properties
         * @deprecated Use proto.EvmHookCall.$Properties instead.
         */

        /**
         * Shape of an EvmHookCall.
         * @typedef {proto.EvmHookCall.$Properties} proto.EvmHookCall.$Shape
         */

        /**
         * Constructs a new EvmHookCall.
         * @memberof proto
         * @classdesc Specifies details of a call to an EVM hook.
         * @constructor
         * @param {proto.EvmHookCall.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const EvmHookCall = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Call data to pass to the hook via the IHieroHook.HookContext#data field.
         * @member {Uint8Array} data
         * @memberof proto.EvmHookCall
         * @instance
         */
        EvmHookCall.prototype.data = $util.newBuffer([]);

        /**
         * The gas limit to use.
         * @member {number|Long} gasLimit
         * @memberof proto.EvmHookCall
         * @instance
         */
        EvmHookCall.prototype.gasLimit = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new EvmHookCall instance using the specified properties.
         * @function create
         * @memberof proto.EvmHookCall
         * @static
         * @param {proto.EvmHookCall.$Properties=} [properties] Properties to set
         * @returns {proto.EvmHookCall} EvmHookCall instance
         * @type {{
         *   (properties: proto.EvmHookCall.$Shape): proto.EvmHookCall & proto.EvmHookCall.$Shape;
         *   (properties?: proto.EvmHookCall.$Properties): proto.EvmHookCall;
         * }}
         */
        EvmHookCall.create = function(properties) {
            return new EvmHookCall(properties);
        };

        /**
         * Encodes the specified EvmHookCall message. Does not implicitly {@link proto.EvmHookCall.verify|verify} messages.
         * @function encode
         * @memberof proto.EvmHookCall
         * @static
         * @param {proto.EvmHookCall.$Properties} message EvmHookCall message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EvmHookCall.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.data);
            if (message.gasLimit != null && $Object.hasOwnProperty.call(message, "gasLimit") && (typeof message.gasLimit === "object" ? message.gasLimit.low || message.gasLimit.high : message.gasLimit !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.gasLimit);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EvmHookCall message, length delimited. Does not implicitly {@link proto.EvmHookCall.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EvmHookCall
         * @static
         * @param {proto.EvmHookCall.$Properties} message EvmHookCall message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EvmHookCall.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an EvmHookCall message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EvmHookCall
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EvmHookCall & proto.EvmHookCall.$Shape} EvmHookCall
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EvmHookCall.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.EvmHookCall();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.gasLimit = value;
                        else
                            delete message.gasLimit;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EvmHookCall message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EvmHookCall
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EvmHookCall & proto.EvmHookCall.$Shape} EvmHookCall
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EvmHookCall.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EvmHookCall message.
         * @function verify
         * @memberof proto.EvmHookCall
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EvmHookCall.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            if (message.gasLimit != null && $Object.hasOwnProperty.call(message, "gasLimit"))
                if (!$util.isInteger(message.gasLimit) && !(message.gasLimit && $util.isInteger(message.gasLimit.low) && $util.isInteger(message.gasLimit.high)))
                    return "gasLimit: integer|Long expected";
            return null;
        };

        /**
         * Creates an EvmHookCall message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EvmHookCall
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EvmHookCall} EvmHookCall
         */
        EvmHookCall.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.EvmHookCall)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.EvmHookCall: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.EvmHookCall();
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            if (object.gasLimit != null)
                if (typeof object.gasLimit === "object" ? object.gasLimit.low || object.gasLimit.high : $Number(object.gasLimit) !== 0)
                    if ($util.Long)
                        message.gasLimit = $util.Long.fromValue(object.gasLimit, true);
                    else if (typeof object.gasLimit === "string")
                        message.gasLimit = $parseInt(object.gasLimit, 10);
                    else if (typeof object.gasLimit === "number")
                        message.gasLimit = object.gasLimit;
                    else if (typeof object.gasLimit === "object")
                        message.gasLimit = new $util.LongBits(object.gasLimit.low >>> 0, object.gasLimit.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from an EvmHookCall message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EvmHookCall
         * @static
         * @param {proto.EvmHookCall} message EvmHookCall
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EvmHookCall.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== $Array)
                        object.data = $util.newBuffer(object.data);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.gasLimit = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gasLimit = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === $String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.data) : message.data;
            if (message.gasLimit != null && $Object.hasOwnProperty.call(message, "gasLimit"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gasLimit = typeof message.gasLimit === "number" ? $BigInt(message.gasLimit) : $util.Long.fromBits(message.gasLimit.low >>> 0, message.gasLimit.high >>> 0, true).toBigInt();
                else if (typeof message.gasLimit === "number")
                    object.gasLimit = options.longs === $String ? $String(message.gasLimit) : message.gasLimit;
                else
                    object.gasLimit = options.longs === $String ? $util.Long.prototype.toString.call(message.gasLimit) : options.longs === $Number ? new $util.LongBits(message.gasLimit.low >>> 0, message.gasLimit.high >>> 0).toNumber(true) : message.gasLimit;
            return object;
        };

        /**
         * Converts this EvmHookCall to JSON.
         * @function toJSON
         * @memberof proto.EvmHookCall
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EvmHookCall.prototype.toJSON = function() {
            return EvmHookCall.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EvmHookCall
         * @function getTypeUrl
         * @memberof proto.EvmHookCall
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EvmHookCall.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EvmHookCall";
        };

        return EvmHookCall;
    })();

    proto.AccountAmount = (function() {

        /**
         * Properties of an AccountAmount.
         * @typedef {Object} proto.AccountAmount.$Properties
         * @property {proto.AccountID.$Properties|null} [accountID] An account identifier that will send or receive token(s).
         * @property {number|Long|null} [amount] An amount to send (negative) or receive (positive).
         * <p>
         * This amount MUST be denominated in the smallest unit of the relevant
         * token.<br/>
         * For HBAR this SHALL be tinybar (10<sup>-8</sup> HBAR).<br/>
         * For other fungible/common tokens this SHALL depend on the value of
         * `decimals` for that token.
         * @property {boolean|null} [isApproval] An approved allowance flag.<br/>
         * If true then the transfer is expected to be an approved allowance.
         * <p>
         * If set, `accountID` SHALL be the owner that previously approved
         * the allowance.<br/>
         * The default value SHALL be false (unset).
         * @property {proto.HookCall.$Properties|null} [preTxAllowanceHook] A single call made before attempting the CryptoTransfer, to a
         * method with logical signature allow(HookContext, ProposedTransfers)
         * @property {proto.HookCall.$Properties|null} [prePostTxAllowanceHook] Two calls, the first call before attempting the CryptoTransfer, to a
         * method with logical signature allowPre(HookContext, ProposedTransfers);
         * and the second call after attempting the CryptoTransfer, to a method
         * with logical signature allowPost(HookContext, ProposedTransfers).
         * @property {"preTxAllowanceHook"|"prePostTxAllowanceHook"} [hookCall] If set, a call to a hook of type `ACCOUNT_ALLOWANCE_HOOK` on scoped
         * account; the hook's invoked methods must not revert and must return
         * true for the containing CryptoTransfer to succeed.
         * <p>
         * Cannot be set if `is_approval` is true.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AccountAmount.
         * @memberof proto
         * @interface IAccountAmount
         * @augments proto.AccountAmount.$Properties
         * @deprecated Use proto.AccountAmount.$Properties instead.
         */

        /**
         * Narrowed shape of an AccountAmount.
         * @typedef {{
         *   accountID?: proto.AccountID.$Shape|null;
         *   amount?: number|Long|null;
         *   isApproval?: boolean|null;
         *   preTxAllowanceHook?: proto.HookCall.$Shape|null;
         *   prePostTxAllowanceHook?: proto.HookCall.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ hookCall?: undefined; preTxAllowanceHook?: null; prePostTxAllowanceHook?: null }|{ hookCall?: "preTxAllowanceHook"; preTxAllowanceHook: proto.HookCall.$Shape; prePostTxAllowanceHook?: null }|{ hookCall?: "prePostTxAllowanceHook"; preTxAllowanceHook?: null; prePostTxAllowanceHook: proto.HookCall.$Shape })
         * )} proto.AccountAmount.$Shape
         */

        /**
         * Constructs a new AccountAmount.
         * @memberof proto
         * @classdesc An account, and the amount that it sends or receives during a token transfer.
         * 
         * This message is only relevant to fungible/common token transfers.
         * Non-fungible/unique (NFT) token transfers MUST use the NftTransfer message.
         * @constructor
         * @param {proto.AccountAmount.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AccountAmount = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An account identifier that will send or receive token(s).
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.AccountAmount
         * @instance
         */
        AccountAmount.prototype.accountID = null;

        /**
         * An amount to send (negative) or receive (positive).
         * <p>
         * This amount MUST be denominated in the smallest unit of the relevant
         * token.<br/>
         * For HBAR this SHALL be tinybar (10<sup>-8</sup> HBAR).<br/>
         * For other fungible/common tokens this SHALL depend on the value of
         * `decimals` for that token.
         * @member {number|Long} amount
         * @memberof proto.AccountAmount
         * @instance
         */
        AccountAmount.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An approved allowance flag.<br/>
         * If true then the transfer is expected to be an approved allowance.
         * <p>
         * If set, `accountID` SHALL be the owner that previously approved
         * the allowance.<br/>
         * The default value SHALL be false (unset).
         * @member {boolean} isApproval
         * @memberof proto.AccountAmount
         * @instance
         */
        AccountAmount.prototype.isApproval = false;

        /**
         * A single call made before attempting the CryptoTransfer, to a
         * method with logical signature allow(HookContext, ProposedTransfers)
         * @member {proto.HookCall.$Properties|null|undefined} preTxAllowanceHook
         * @memberof proto.AccountAmount
         * @instance
         */
        AccountAmount.prototype.preTxAllowanceHook = null;

        /**
         * Two calls, the first call before attempting the CryptoTransfer, to a
         * method with logical signature allowPre(HookContext, ProposedTransfers);
         * and the second call after attempting the CryptoTransfer, to a method
         * with logical signature allowPost(HookContext, ProposedTransfers).
         * @member {proto.HookCall.$Properties|null|undefined} prePostTxAllowanceHook
         * @memberof proto.AccountAmount
         * @instance
         */
        AccountAmount.prototype.prePostTxAllowanceHook = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * If set, a call to a hook of type `ACCOUNT_ALLOWANCE_HOOK` on scoped
         * account; the hook's invoked methods must not revert and must return
         * true for the containing CryptoTransfer to succeed.
         * <p>
         * Cannot be set if `is_approval` is true.
         * @member {"preTxAllowanceHook"|"prePostTxAllowanceHook"|undefined} hookCall
         * @memberof proto.AccountAmount
         * @instance
         */
        $Object.defineProperty(AccountAmount.prototype, "hookCall", {
            get: $util.oneOfGetter($oneOfFields = ["preTxAllowanceHook", "prePostTxAllowanceHook"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AccountAmount instance using the specified properties.
         * @function create
         * @memberof proto.AccountAmount
         * @static
         * @param {proto.AccountAmount.$Properties=} [properties] Properties to set
         * @returns {proto.AccountAmount} AccountAmount instance
         * @type {{
         *   (properties: proto.AccountAmount.$Shape): proto.AccountAmount & proto.AccountAmount.$Shape;
         *   (properties?: proto.AccountAmount.$Properties): proto.AccountAmount;
         * }}
         */
        AccountAmount.create = function(properties) {
            return new AccountAmount(properties);
        };

        /**
         * Encodes the specified AccountAmount message. Does not implicitly {@link proto.AccountAmount.verify|verify} messages.
         * @function encode
         * @memberof proto.AccountAmount
         * @static
         * @param {proto.AccountAmount.$Properties} message AccountAmount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountAmount.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).sint64(message.amount);
            if (message.isApproval != null && $Object.hasOwnProperty.call(message, "isApproval") && message.isApproval !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.isApproval);
            if (message.preTxAllowanceHook != null && $Object.hasOwnProperty.call(message, "preTxAllowanceHook"))
                $root.proto.HookCall.encode(message.preTxAllowanceHook, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.prePostTxAllowanceHook != null && $Object.hasOwnProperty.call(message, "prePostTxAllowanceHook"))
                $root.proto.HookCall.encode(message.prePostTxAllowanceHook, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AccountAmount message, length delimited. Does not implicitly {@link proto.AccountAmount.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AccountAmount
         * @static
         * @param {proto.AccountAmount.$Properties} message AccountAmount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountAmount.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AccountAmount message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AccountAmount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AccountAmount & proto.AccountAmount.$Shape} AccountAmount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountAmount.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.AccountAmount();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.sint64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.isApproval = value;
                        else
                            delete message.isApproval;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.preTxAllowanceHook = $root.proto.HookCall.decode(reader, reader.uint32(), $undefined, _depth + 1, message.preTxAllowanceHook);
                        message.hookCall = "preTxAllowanceHook";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.prePostTxAllowanceHook = $root.proto.HookCall.decode(reader, reader.uint32(), $undefined, _depth + 1, message.prePostTxAllowanceHook);
                        message.hookCall = "prePostTxAllowanceHook";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AccountAmount message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AccountAmount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AccountAmount & proto.AccountAmount.$Shape} AccountAmount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountAmount.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AccountAmount message.
         * @function verify
         * @memberof proto.AccountAmount
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AccountAmount.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.isApproval != null && $Object.hasOwnProperty.call(message, "isApproval"))
                if (typeof message.isApproval !== "boolean")
                    return "isApproval: boolean expected";
            if (message.preTxAllowanceHook != null && $Object.hasOwnProperty.call(message, "preTxAllowanceHook")) {
                properties.hookCall = 1;
                {
                    let error = $root.proto.HookCall.verify(message.preTxAllowanceHook, _depth + 1);
                    if (error)
                        return "preTxAllowanceHook." + error;
                }
            }
            if (message.prePostTxAllowanceHook != null && $Object.hasOwnProperty.call(message, "prePostTxAllowanceHook")) {
                if (properties.hookCall === 1)
                    return "hookCall: multiple values";
                properties.hookCall = 1;
                {
                    let error = $root.proto.HookCall.verify(message.prePostTxAllowanceHook, _depth + 1);
                    if (error)
                        return "prePostTxAllowanceHook." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AccountAmount message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AccountAmount
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AccountAmount} AccountAmount
         */
        AccountAmount.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.AccountAmount)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.AccountAmount: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.AccountAmount();
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.AccountAmount.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            if (object.isApproval != null)
                if (object.isApproval)
                    message.isApproval = $Boolean(object.isApproval);
            if (object.preTxAllowanceHook != null) {
                if (!$util.isObject(object.preTxAllowanceHook))
                    throw $TypeError(".proto.AccountAmount.preTxAllowanceHook: object expected");
                message.preTxAllowanceHook = $root.proto.HookCall.fromObject(object.preTxAllowanceHook, _depth + 1);
            }
            if (object.prePostTxAllowanceHook != null) {
                if (!$util.isObject(object.prePostTxAllowanceHook))
                    throw $TypeError(".proto.AccountAmount.prePostTxAllowanceHook: object expected");
                message.prePostTxAllowanceHook = $root.proto.HookCall.fromObject(object.prePostTxAllowanceHook, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an AccountAmount message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AccountAmount
         * @static
         * @param {proto.AccountAmount} message AccountAmount
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccountAmount.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.accountID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.isApproval = false;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            if (message.isApproval != null && $Object.hasOwnProperty.call(message, "isApproval"))
                object.isApproval = message.isApproval;
            if (message.preTxAllowanceHook != null && $Object.hasOwnProperty.call(message, "preTxAllowanceHook")) {
                object.preTxAllowanceHook = $root.proto.HookCall.toObject(message.preTxAllowanceHook, options, _depth + 1);
                if (options.oneofs)
                    object.hookCall = "preTxAllowanceHook";
            }
            if (message.prePostTxAllowanceHook != null && $Object.hasOwnProperty.call(message, "prePostTxAllowanceHook")) {
                object.prePostTxAllowanceHook = $root.proto.HookCall.toObject(message.prePostTxAllowanceHook, options, _depth + 1);
                if (options.oneofs)
                    object.hookCall = "prePostTxAllowanceHook";
            }
            return object;
        };

        /**
         * Converts this AccountAmount to JSON.
         * @function toJSON
         * @memberof proto.AccountAmount
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccountAmount.prototype.toJSON = function() {
            return AccountAmount.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AccountAmount
         * @function getTypeUrl
         * @memberof proto.AccountAmount
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AccountAmount.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AccountAmount";
        };

        return AccountAmount;
    })();

    proto.TransferList = (function() {

        /**
         * Properties of a TransferList.
         * @typedef {Object} proto.TransferList.$Properties
         * @property {Array.<proto.AccountAmount.$Properties>|null} [accountAmounts] A list of AccountAmount pairs.<br/>
         * Each entry in this list is an account and an amount to transfer
         * into it (positive) or out of it (negative)
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransferList.
         * @memberof proto
         * @interface ITransferList
         * @augments proto.TransferList.$Properties
         * @deprecated Use proto.TransferList.$Properties instead.
         */

        /**
         * Shape of a TransferList.
         * @typedef {{
         *   accountAmounts?: Array.<proto.AccountAmount.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TransferList.$Shape
         */

        /**
         * Constructs a new TransferList.
         * @memberof proto
         * @classdesc A list of accounts and amounts to transfer.
         * 
         * Each `AccountAmount` SHALL specify the account and the amount to
         * send(negative) or receive(positive).<br/>
         * Each `TransferList` SHALL be contained in another message that contains
         * other details required to complete a transfer. This is typically a
         * `CryptoTransferTransactionBody` or `TransactionRecord`.<br/>
         * The `TransferList` SHALL only be used for HBAR transfers. Other token types
         * MUST use the `TokenTransferList` message.
         * @constructor
         * @param {proto.TransferList.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransferList = function (properties) {
            this.accountAmounts = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of AccountAmount pairs.<br/>
         * Each entry in this list is an account and an amount to transfer
         * into it (positive) or out of it (negative)
         * @member {Array.<proto.AccountAmount.$Properties>} accountAmounts
         * @memberof proto.TransferList
         * @instance
         */
        TransferList.prototype.accountAmounts = $util.emptyArray;

        /**
         * Creates a new TransferList instance using the specified properties.
         * @function create
         * @memberof proto.TransferList
         * @static
         * @param {proto.TransferList.$Properties=} [properties] Properties to set
         * @returns {proto.TransferList} TransferList instance
         * @type {{
         *   (properties: proto.TransferList.$Shape): proto.TransferList & proto.TransferList.$Shape;
         *   (properties?: proto.TransferList.$Properties): proto.TransferList;
         * }}
         */
        TransferList.create = function(properties) {
            return new TransferList(properties);
        };

        /**
         * Encodes the specified TransferList message. Does not implicitly {@link proto.TransferList.verify|verify} messages.
         * @function encode
         * @memberof proto.TransferList
         * @static
         * @param {proto.TransferList.$Properties} message TransferList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransferList.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountAmounts != null && message.accountAmounts.length)
                for (let i = 0; i < message.accountAmounts.length; ++i)
                    $root.proto.AccountAmount.encode(message.accountAmounts[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransferList message, length delimited. Does not implicitly {@link proto.TransferList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransferList
         * @static
         * @param {proto.TransferList.$Properties} message TransferList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransferList.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransferList message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransferList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransferList & proto.TransferList.$Shape} TransferList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransferList.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransferList();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.accountAmounts && message.accountAmounts.length))
                            message.accountAmounts = [];
                        message.accountAmounts.push($root.proto.AccountAmount.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransferList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransferList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransferList & proto.TransferList.$Shape} TransferList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransferList.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransferList message.
         * @function verify
         * @memberof proto.TransferList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransferList.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.accountAmounts != null && $Object.hasOwnProperty.call(message, "accountAmounts")) {
                if (!$Array.isArray(message.accountAmounts))
                    return "accountAmounts: array expected";
                for (let i = 0; i < message.accountAmounts.length; ++i) {
                    let error = $root.proto.AccountAmount.verify(message.accountAmounts[i], _depth + 1);
                    if (error)
                        return "accountAmounts." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TransferList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransferList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransferList} TransferList
         */
        TransferList.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransferList)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransferList: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransferList();
            if (object.accountAmounts) {
                if (!$Array.isArray(object.accountAmounts))
                    throw $TypeError(".proto.TransferList.accountAmounts: array expected");
                message.accountAmounts = $Array(object.accountAmounts.length);
                for (let i = 0; i < object.accountAmounts.length; ++i) {
                    if (!$util.isObject(object.accountAmounts[i]))
                        throw $TypeError(".proto.TransferList.accountAmounts: object expected");
                    message.accountAmounts[i] = $root.proto.AccountAmount.fromObject(object.accountAmounts[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TransferList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransferList
         * @static
         * @param {proto.TransferList} message TransferList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransferList.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.accountAmounts = [];
            if (message.accountAmounts && message.accountAmounts.length) {
                object.accountAmounts = $Array(message.accountAmounts.length);
                for (let j = 0; j < message.accountAmounts.length; ++j)
                    object.accountAmounts[j] = $root.proto.AccountAmount.toObject(message.accountAmounts[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TransferList to JSON.
         * @function toJSON
         * @memberof proto.TransferList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransferList.prototype.toJSON = function() {
            return TransferList.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransferList
         * @function getTypeUrl
         * @memberof proto.TransferList
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransferList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransferList";
        };

        return TransferList;
    })();

    proto.NftTransfer = (function() {

        /**
         * Properties of a NftTransfer.
         * @typedef {Object} proto.NftTransfer.$Properties
         * @property {proto.AccountID.$Properties|null} [senderAccountID] An Account identifier for the sender.
         * @property {proto.AccountID.$Properties|null} [receiverAccountID] An Account identifier for the receiver.
         * @property {number|Long|null} [serialNumber] A serial number for the NFT to transfer.
         * @property {boolean|null} [isApproval] An approved allowance flag.<br/>
         * If true then the transfer is expected to be an approved allowance.
         * <p>
         * If set, `senderAccountID` SHALL be the owner that previously approved
         * the allowance.<br/>
         * If set, the `senderAccountID` MUST be the "payer" account for
         * the transaction <br/>
         * The default value SHALL be false (unset).
         * @property {proto.HookCall.$Properties|null} [preTxSenderAllowanceHook] A single call made before attempting the CryptoTransfer, to a
         * method with logical signature allow(HookContext, ProposedTransfers)
         * @property {proto.HookCall.$Properties|null} [prePostTxSenderAllowanceHook] Two calls, the first call before attempting the CryptoTransfer, to a
         * method with logical signature allowPre(HookContext, ProposedTransfers);
         * and the second call after attempting the CryptoTransfer, to a method
         * with logical signature allowPost(HookContext, ProposedTransfers).
         * @property {proto.HookCall.$Properties|null} [preTxReceiverAllowanceHook] A single call made before attempting the CryptoTransfer, to a
         * method with logical signature allow(HookContext, ProposedTransfers)
         * @property {proto.HookCall.$Properties|null} [prePostTxReceiverAllowanceHook] Two calls, the first call before attempting the CryptoTransfer, to a
         * method with logical signature allowPre(HookContext, ProposedTransfers);
         * and the second call after attempting the CryptoTransfer, to a method
         * with logical signature allowPost(HookContext, ProposedTransfers).
         * @property {"preTxSenderAllowanceHook"|"prePostTxSenderAllowanceHook"} [senderAllowanceHookCall] If set, a call to a hook of type `ACCOUNT_ALLOWANCE_HOOK` installed on
         * senderAccountID that must succeed for the transaction to occur.
         * <p>
         * Cannot be set if `is_approval` is true.
         * @property {"preTxReceiverAllowanceHook"|"prePostTxReceiverAllowanceHook"} [receiverAllowanceHookCall] If set, a call to a hook of type `ACCOUNT_ALLOWANCE_HOOK` installed on
         * receiverAccountID that must succeed for the transaction to occur.
         * <p>
         * May be set even if `is_approval` is true. In this case, the approval applies
         * to the sender authorization, and the hook applies to the receiver authorization
         * (if needed, e.g. because of a fallback royalty fee or receiver signature
         * requirement).
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NftTransfer.
         * @memberof proto
         * @interface INftTransfer
         * @augments proto.NftTransfer.$Properties
         * @deprecated Use proto.NftTransfer.$Properties instead.
         */

        /**
         * Narrowed shape of a NftTransfer.
         * @typedef {{
         *   senderAccountID?: proto.AccountID.$Shape|null;
         *   receiverAccountID?: proto.AccountID.$Shape|null;
         *   serialNumber?: number|Long|null;
         *   isApproval?: boolean|null;
         *   preTxSenderAllowanceHook?: proto.HookCall.$Shape|null;
         *   prePostTxSenderAllowanceHook?: proto.HookCall.$Shape|null;
         *   preTxReceiverAllowanceHook?: proto.HookCall.$Shape|null;
         *   prePostTxReceiverAllowanceHook?: proto.HookCall.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ senderAllowanceHookCall?: undefined; preTxSenderAllowanceHook?: null; prePostTxSenderAllowanceHook?: null }|{ senderAllowanceHookCall?: "preTxSenderAllowanceHook"; preTxSenderAllowanceHook: proto.HookCall.$Shape; prePostTxSenderAllowanceHook?: null }|{ senderAllowanceHookCall?: "prePostTxSenderAllowanceHook"; preTxSenderAllowanceHook?: null; prePostTxSenderAllowanceHook: proto.HookCall.$Shape })
         * ) & (
         *   ({ receiverAllowanceHookCall?: undefined; preTxReceiverAllowanceHook?: null; prePostTxReceiverAllowanceHook?: null }|{ receiverAllowanceHookCall?: "preTxReceiverAllowanceHook"; preTxReceiverAllowanceHook: proto.HookCall.$Shape; prePostTxReceiverAllowanceHook?: null }|{ receiverAllowanceHookCall?: "prePostTxReceiverAllowanceHook"; preTxReceiverAllowanceHook?: null; prePostTxReceiverAllowanceHook: proto.HookCall.$Shape })
         * )} proto.NftTransfer.$Shape
         */

        /**
         * Constructs a new NftTransfer.
         * @memberof proto
         * @classdesc A NFT transfer.<br/>
         * This refers to a sender account, a receiver account, and the serial number
         * of an NFT to transfer from sender to receiver.
         * 
         * Each `NftTransfer` SHALL be contained in another message (typically
         * `TokenTransferList`) that details which `Token` type applies to this NFT
         * transfer.
         * @constructor
         * @param {proto.NftTransfer.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NftTransfer = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An Account identifier for the sender.
         * @member {proto.AccountID.$Properties|null|undefined} senderAccountID
         * @memberof proto.NftTransfer
         * @instance
         */
        NftTransfer.prototype.senderAccountID = null;

        /**
         * An Account identifier for the receiver.
         * @member {proto.AccountID.$Properties|null|undefined} receiverAccountID
         * @memberof proto.NftTransfer
         * @instance
         */
        NftTransfer.prototype.receiverAccountID = null;

        /**
         * A serial number for the NFT to transfer.
         * @member {number|Long} serialNumber
         * @memberof proto.NftTransfer
         * @instance
         */
        NftTransfer.prototype.serialNumber = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An approved allowance flag.<br/>
         * If true then the transfer is expected to be an approved allowance.
         * <p>
         * If set, `senderAccountID` SHALL be the owner that previously approved
         * the allowance.<br/>
         * If set, the `senderAccountID` MUST be the "payer" account for
         * the transaction <br/>
         * The default value SHALL be false (unset).
         * @member {boolean} isApproval
         * @memberof proto.NftTransfer
         * @instance
         */
        NftTransfer.prototype.isApproval = false;

        /**
         * A single call made before attempting the CryptoTransfer, to a
         * method with logical signature allow(HookContext, ProposedTransfers)
         * @member {proto.HookCall.$Properties|null|undefined} preTxSenderAllowanceHook
         * @memberof proto.NftTransfer
         * @instance
         */
        NftTransfer.prototype.preTxSenderAllowanceHook = null;

        /**
         * Two calls, the first call before attempting the CryptoTransfer, to a
         * method with logical signature allowPre(HookContext, ProposedTransfers);
         * and the second call after attempting the CryptoTransfer, to a method
         * with logical signature allowPost(HookContext, ProposedTransfers).
         * @member {proto.HookCall.$Properties|null|undefined} prePostTxSenderAllowanceHook
         * @memberof proto.NftTransfer
         * @instance
         */
        NftTransfer.prototype.prePostTxSenderAllowanceHook = null;

        /**
         * A single call made before attempting the CryptoTransfer, to a
         * method with logical signature allow(HookContext, ProposedTransfers)
         * @member {proto.HookCall.$Properties|null|undefined} preTxReceiverAllowanceHook
         * @memberof proto.NftTransfer
         * @instance
         */
        NftTransfer.prototype.preTxReceiverAllowanceHook = null;

        /**
         * Two calls, the first call before attempting the CryptoTransfer, to a
         * method with logical signature allowPre(HookContext, ProposedTransfers);
         * and the second call after attempting the CryptoTransfer, to a method
         * with logical signature allowPost(HookContext, ProposedTransfers).
         * @member {proto.HookCall.$Properties|null|undefined} prePostTxReceiverAllowanceHook
         * @memberof proto.NftTransfer
         * @instance
         */
        NftTransfer.prototype.prePostTxReceiverAllowanceHook = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * If set, a call to a hook of type `ACCOUNT_ALLOWANCE_HOOK` installed on
         * senderAccountID that must succeed for the transaction to occur.
         * <p>
         * Cannot be set if `is_approval` is true.
         * @member {"preTxSenderAllowanceHook"|"prePostTxSenderAllowanceHook"|undefined} senderAllowanceHookCall
         * @memberof proto.NftTransfer
         * @instance
         */
        $Object.defineProperty(NftTransfer.prototype, "senderAllowanceHookCall", {
            get: $util.oneOfGetter($oneOfFields = ["preTxSenderAllowanceHook", "prePostTxSenderAllowanceHook"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * If set, a call to a hook of type `ACCOUNT_ALLOWANCE_HOOK` installed on
         * receiverAccountID that must succeed for the transaction to occur.
         * <p>
         * May be set even if `is_approval` is true. In this case, the approval applies
         * to the sender authorization, and the hook applies to the receiver authorization
         * (if needed, e.g. because of a fallback royalty fee or receiver signature
         * requirement).
         * @member {"preTxReceiverAllowanceHook"|"prePostTxReceiverAllowanceHook"|undefined} receiverAllowanceHookCall
         * @memberof proto.NftTransfer
         * @instance
         */
        $Object.defineProperty(NftTransfer.prototype, "receiverAllowanceHookCall", {
            get: $util.oneOfGetter($oneOfFields = ["preTxReceiverAllowanceHook", "prePostTxReceiverAllowanceHook"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new NftTransfer instance using the specified properties.
         * @function create
         * @memberof proto.NftTransfer
         * @static
         * @param {proto.NftTransfer.$Properties=} [properties] Properties to set
         * @returns {proto.NftTransfer} NftTransfer instance
         * @type {{
         *   (properties: proto.NftTransfer.$Shape): proto.NftTransfer & proto.NftTransfer.$Shape;
         *   (properties?: proto.NftTransfer.$Properties): proto.NftTransfer;
         * }}
         */
        NftTransfer.create = function(properties) {
            return new NftTransfer(properties);
        };

        /**
         * Encodes the specified NftTransfer message. Does not implicitly {@link proto.NftTransfer.verify|verify} messages.
         * @function encode
         * @memberof proto.NftTransfer
         * @static
         * @param {proto.NftTransfer.$Properties} message NftTransfer message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NftTransfer.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.senderAccountID != null && $Object.hasOwnProperty.call(message, "senderAccountID"))
                $root.proto.AccountID.encode(message.senderAccountID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.receiverAccountID != null && $Object.hasOwnProperty.call(message, "receiverAccountID"))
                $root.proto.AccountID.encode(message.receiverAccountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.serialNumber != null && $Object.hasOwnProperty.call(message, "serialNumber") && (typeof message.serialNumber === "object" ? message.serialNumber.low || message.serialNumber.high : message.serialNumber !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.serialNumber);
            if (message.isApproval != null && $Object.hasOwnProperty.call(message, "isApproval") && message.isApproval !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isApproval);
            if (message.preTxSenderAllowanceHook != null && $Object.hasOwnProperty.call(message, "preTxSenderAllowanceHook"))
                $root.proto.HookCall.encode(message.preTxSenderAllowanceHook, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.prePostTxSenderAllowanceHook != null && $Object.hasOwnProperty.call(message, "prePostTxSenderAllowanceHook"))
                $root.proto.HookCall.encode(message.prePostTxSenderAllowanceHook, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.preTxReceiverAllowanceHook != null && $Object.hasOwnProperty.call(message, "preTxReceiverAllowanceHook"))
                $root.proto.HookCall.encode(message.preTxReceiverAllowanceHook, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.prePostTxReceiverAllowanceHook != null && $Object.hasOwnProperty.call(message, "prePostTxReceiverAllowanceHook"))
                $root.proto.HookCall.encode(message.prePostTxReceiverAllowanceHook, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NftTransfer message, length delimited. Does not implicitly {@link proto.NftTransfer.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NftTransfer
         * @static
         * @param {proto.NftTransfer.$Properties} message NftTransfer message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NftTransfer.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NftTransfer message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NftTransfer
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NftTransfer & proto.NftTransfer.$Shape} NftTransfer
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NftTransfer.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NftTransfer();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.senderAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.senderAccountID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.receiverAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.receiverAccountID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.serialNumber = value;
                        else
                            delete message.serialNumber;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.isApproval = value;
                        else
                            delete message.isApproval;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.preTxSenderAllowanceHook = $root.proto.HookCall.decode(reader, reader.uint32(), $undefined, _depth + 1, message.preTxSenderAllowanceHook);
                        message.senderAllowanceHookCall = "preTxSenderAllowanceHook";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.prePostTxSenderAllowanceHook = $root.proto.HookCall.decode(reader, reader.uint32(), $undefined, _depth + 1, message.prePostTxSenderAllowanceHook);
                        message.senderAllowanceHookCall = "prePostTxSenderAllowanceHook";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.preTxReceiverAllowanceHook = $root.proto.HookCall.decode(reader, reader.uint32(), $undefined, _depth + 1, message.preTxReceiverAllowanceHook);
                        message.receiverAllowanceHookCall = "preTxReceiverAllowanceHook";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.prePostTxReceiverAllowanceHook = $root.proto.HookCall.decode(reader, reader.uint32(), $undefined, _depth + 1, message.prePostTxReceiverAllowanceHook);
                        message.receiverAllowanceHookCall = "prePostTxReceiverAllowanceHook";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NftTransfer message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NftTransfer
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NftTransfer & proto.NftTransfer.$Shape} NftTransfer
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NftTransfer.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NftTransfer message.
         * @function verify
         * @memberof proto.NftTransfer
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NftTransfer.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.senderAccountID != null && $Object.hasOwnProperty.call(message, "senderAccountID")) {
                let error = $root.proto.AccountID.verify(message.senderAccountID, _depth + 1);
                if (error)
                    return "senderAccountID." + error;
            }
            if (message.receiverAccountID != null && $Object.hasOwnProperty.call(message, "receiverAccountID")) {
                let error = $root.proto.AccountID.verify(message.receiverAccountID, _depth + 1);
                if (error)
                    return "receiverAccountID." + error;
            }
            if (message.serialNumber != null && $Object.hasOwnProperty.call(message, "serialNumber"))
                if (!$util.isInteger(message.serialNumber) && !(message.serialNumber && $util.isInteger(message.serialNumber.low) && $util.isInteger(message.serialNumber.high)))
                    return "serialNumber: integer|Long expected";
            if (message.isApproval != null && $Object.hasOwnProperty.call(message, "isApproval"))
                if (typeof message.isApproval !== "boolean")
                    return "isApproval: boolean expected";
            if (message.preTxSenderAllowanceHook != null && $Object.hasOwnProperty.call(message, "preTxSenderAllowanceHook")) {
                properties.senderAllowanceHookCall = 1;
                {
                    let error = $root.proto.HookCall.verify(message.preTxSenderAllowanceHook, _depth + 1);
                    if (error)
                        return "preTxSenderAllowanceHook." + error;
                }
            }
            if (message.prePostTxSenderAllowanceHook != null && $Object.hasOwnProperty.call(message, "prePostTxSenderAllowanceHook")) {
                if (properties.senderAllowanceHookCall === 1)
                    return "senderAllowanceHookCall: multiple values";
                properties.senderAllowanceHookCall = 1;
                {
                    let error = $root.proto.HookCall.verify(message.prePostTxSenderAllowanceHook, _depth + 1);
                    if (error)
                        return "prePostTxSenderAllowanceHook." + error;
                }
            }
            if (message.preTxReceiverAllowanceHook != null && $Object.hasOwnProperty.call(message, "preTxReceiverAllowanceHook")) {
                properties.receiverAllowanceHookCall = 1;
                {
                    let error = $root.proto.HookCall.verify(message.preTxReceiverAllowanceHook, _depth + 1);
                    if (error)
                        return "preTxReceiverAllowanceHook." + error;
                }
            }
            if (message.prePostTxReceiverAllowanceHook != null && $Object.hasOwnProperty.call(message, "prePostTxReceiverAllowanceHook")) {
                if (properties.receiverAllowanceHookCall === 1)
                    return "receiverAllowanceHookCall: multiple values";
                properties.receiverAllowanceHookCall = 1;
                {
                    let error = $root.proto.HookCall.verify(message.prePostTxReceiverAllowanceHook, _depth + 1);
                    if (error)
                        return "prePostTxReceiverAllowanceHook." + error;
                }
            }
            return null;
        };

        /**
         * Creates a NftTransfer message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NftTransfer
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NftTransfer} NftTransfer
         */
        NftTransfer.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NftTransfer)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NftTransfer: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NftTransfer();
            if (object.senderAccountID != null) {
                if (!$util.isObject(object.senderAccountID))
                    throw $TypeError(".proto.NftTransfer.senderAccountID: object expected");
                message.senderAccountID = $root.proto.AccountID.fromObject(object.senderAccountID, _depth + 1);
            }
            if (object.receiverAccountID != null) {
                if (!$util.isObject(object.receiverAccountID))
                    throw $TypeError(".proto.NftTransfer.receiverAccountID: object expected");
                message.receiverAccountID = $root.proto.AccountID.fromObject(object.receiverAccountID, _depth + 1);
            }
            if (object.serialNumber != null)
                if (typeof object.serialNumber === "object" ? object.serialNumber.low || object.serialNumber.high : $Number(object.serialNumber) !== 0)
                    if ($util.Long)
                        message.serialNumber = $util.Long.fromValue(object.serialNumber, false);
                    else if (typeof object.serialNumber === "string")
                        message.serialNumber = $parseInt(object.serialNumber, 10);
                    else if (typeof object.serialNumber === "number")
                        message.serialNumber = object.serialNumber;
                    else if (typeof object.serialNumber === "object")
                        message.serialNumber = new $util.LongBits(object.serialNumber.low >>> 0, object.serialNumber.high >>> 0).toNumber();
            if (object.isApproval != null)
                if (object.isApproval)
                    message.isApproval = $Boolean(object.isApproval);
            if (object.preTxSenderAllowanceHook != null) {
                if (!$util.isObject(object.preTxSenderAllowanceHook))
                    throw $TypeError(".proto.NftTransfer.preTxSenderAllowanceHook: object expected");
                message.preTxSenderAllowanceHook = $root.proto.HookCall.fromObject(object.preTxSenderAllowanceHook, _depth + 1);
            }
            if (object.prePostTxSenderAllowanceHook != null) {
                if (!$util.isObject(object.prePostTxSenderAllowanceHook))
                    throw $TypeError(".proto.NftTransfer.prePostTxSenderAllowanceHook: object expected");
                message.prePostTxSenderAllowanceHook = $root.proto.HookCall.fromObject(object.prePostTxSenderAllowanceHook, _depth + 1);
            }
            if (object.preTxReceiverAllowanceHook != null) {
                if (!$util.isObject(object.preTxReceiverAllowanceHook))
                    throw $TypeError(".proto.NftTransfer.preTxReceiverAllowanceHook: object expected");
                message.preTxReceiverAllowanceHook = $root.proto.HookCall.fromObject(object.preTxReceiverAllowanceHook, _depth + 1);
            }
            if (object.prePostTxReceiverAllowanceHook != null) {
                if (!$util.isObject(object.prePostTxReceiverAllowanceHook))
                    throw $TypeError(".proto.NftTransfer.prePostTxReceiverAllowanceHook: object expected");
                message.prePostTxReceiverAllowanceHook = $root.proto.HookCall.fromObject(object.prePostTxReceiverAllowanceHook, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a NftTransfer message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NftTransfer
         * @static
         * @param {proto.NftTransfer} message NftTransfer
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NftTransfer.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.senderAccountID = null;
                object.receiverAccountID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.serialNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.serialNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.isApproval = false;
            }
            if (message.senderAccountID != null && $Object.hasOwnProperty.call(message, "senderAccountID"))
                object.senderAccountID = $root.proto.AccountID.toObject(message.senderAccountID, options, _depth + 1);
            if (message.receiverAccountID != null && $Object.hasOwnProperty.call(message, "receiverAccountID"))
                object.receiverAccountID = $root.proto.AccountID.toObject(message.receiverAccountID, options, _depth + 1);
            if (message.serialNumber != null && $Object.hasOwnProperty.call(message, "serialNumber"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.serialNumber = typeof message.serialNumber === "number" ? $BigInt(message.serialNumber) : $util.Long.fromBits(message.serialNumber.low >>> 0, message.serialNumber.high >>> 0, false).toBigInt();
                else if (typeof message.serialNumber === "number")
                    object.serialNumber = options.longs === $String ? $String(message.serialNumber) : message.serialNumber;
                else
                    object.serialNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.serialNumber) : options.longs === $Number ? new $util.LongBits(message.serialNumber.low >>> 0, message.serialNumber.high >>> 0).toNumber() : message.serialNumber;
            if (message.isApproval != null && $Object.hasOwnProperty.call(message, "isApproval"))
                object.isApproval = message.isApproval;
            if (message.preTxSenderAllowanceHook != null && $Object.hasOwnProperty.call(message, "preTxSenderAllowanceHook")) {
                object.preTxSenderAllowanceHook = $root.proto.HookCall.toObject(message.preTxSenderAllowanceHook, options, _depth + 1);
                if (options.oneofs)
                    object.senderAllowanceHookCall = "preTxSenderAllowanceHook";
            }
            if (message.prePostTxSenderAllowanceHook != null && $Object.hasOwnProperty.call(message, "prePostTxSenderAllowanceHook")) {
                object.prePostTxSenderAllowanceHook = $root.proto.HookCall.toObject(message.prePostTxSenderAllowanceHook, options, _depth + 1);
                if (options.oneofs)
                    object.senderAllowanceHookCall = "prePostTxSenderAllowanceHook";
            }
            if (message.preTxReceiverAllowanceHook != null && $Object.hasOwnProperty.call(message, "preTxReceiverAllowanceHook")) {
                object.preTxReceiverAllowanceHook = $root.proto.HookCall.toObject(message.preTxReceiverAllowanceHook, options, _depth + 1);
                if (options.oneofs)
                    object.receiverAllowanceHookCall = "preTxReceiverAllowanceHook";
            }
            if (message.prePostTxReceiverAllowanceHook != null && $Object.hasOwnProperty.call(message, "prePostTxReceiverAllowanceHook")) {
                object.prePostTxReceiverAllowanceHook = $root.proto.HookCall.toObject(message.prePostTxReceiverAllowanceHook, options, _depth + 1);
                if (options.oneofs)
                    object.receiverAllowanceHookCall = "prePostTxReceiverAllowanceHook";
            }
            return object;
        };

        /**
         * Converts this NftTransfer to JSON.
         * @function toJSON
         * @memberof proto.NftTransfer
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NftTransfer.prototype.toJSON = function() {
            return NftTransfer.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NftTransfer
         * @function getTypeUrl
         * @memberof proto.NftTransfer
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NftTransfer.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NftTransfer";
        };

        return NftTransfer;
    })();

    proto.TokenTransferList = (function() {

        /**
         * Properties of a TokenTransferList.
         * @typedef {Object} proto.TokenTransferList.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.<br/>
         * This is the token to be transferred.
         * @property {Array.<proto.AccountAmount.$Properties>|null} [transfers] A list of account amounts.
         * <p>
         * Each entry SHALL have an account and amount.<br/>
         * These transfers SHALL be "double-entry" style; the credits (positive
         * amount) and debits (negative amount) MUST sum to 0, unless this
         * transfer list is part of a `mint` or `burn` operation.<br/>
         * This SHALL be be set for fungible/common tokens and MUST be
         * empty otherwise.
         * @property {Array.<proto.NftTransfer.$Properties>|null} [nftTransfers] A list of NftTransfers.
         * <p>
         * Each entry SHALL have a sender and receiver account, and the
         * serial number of the unique token to transfer.<br/>
         * This SHALL be be set for non-fungible/unique tokens and SHALL be
         * empty otherwise.
         * @property {google.protobuf.UInt32Value.$Properties|null} [expectedDecimals] An expected decimal precision.<br/>
         * This is the number of decimals a fungible/common token type is
         * _expected_ to have.
         * <p>
         * The transfer SHALL fail with response code `UNEXPECTED_TOKEN_DECIMALS`
         * if this is set and the actual decimals specified for the `Token` differ
         * from this value.<br/>
         * If `nftTransfers` is set, then this value SHOULD NOT be set.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenTransferList.
         * @memberof proto
         * @interface ITokenTransferList
         * @augments proto.TokenTransferList.$Properties
         * @deprecated Use proto.TokenTransferList.$Properties instead.
         */

        /**
         * Shape of a TokenTransferList.
         * @typedef {{
         *   token?: proto.TokenID.$Shape|null;
         *   transfers?: Array.<proto.AccountAmount.$Shape>|null;
         *   nftTransfers?: Array.<proto.NftTransfer.$Shape>|null;
         *   expectedDecimals?: google.protobuf.UInt32Value.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenTransferList.$Shape
         */

        /**
         * Constructs a new TokenTransferList.
         * @memberof proto
         * @classdesc A list of transfers for a particular (non-HBAR) token type.
         * 
         * A `TokenTransferList` applies to a single token type, but may contain many
         * individual transfers.<br/>
         * Each transfer of a fungible/common token MUST specify an `accountID` and
         * `amount`. Amount SHALL be positive when the account receives tokens, and
         * SHALL be negative when the account sends tokens. The amount SHOULD NOT be
         * `0`.<br/>
         * In a transfer list containing fungible/common tokens in the `transfers`
         * list, the sum of all such transfers MUST be zero (`0`).
         * Each transfer of a unique token SHALL specify both sender and receiver, as
         * well as the serial number transferred.<br/>
         * A single `TokenTransferList` MUST contain `transfers` or `nftTransfers`,
         * but MUST NOT contain both.
         * @constructor
         * @param {proto.TokenTransferList.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenTransferList = function (properties) {
            this.transfers = [];
            this.nftTransfers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.<br/>
         * This is the token to be transferred.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenTransferList
         * @instance
         */
        TokenTransferList.prototype.token = null;

        /**
         * A list of account amounts.
         * <p>
         * Each entry SHALL have an account and amount.<br/>
         * These transfers SHALL be "double-entry" style; the credits (positive
         * amount) and debits (negative amount) MUST sum to 0, unless this
         * transfer list is part of a `mint` or `burn` operation.<br/>
         * This SHALL be be set for fungible/common tokens and MUST be
         * empty otherwise.
         * @member {Array.<proto.AccountAmount.$Properties>} transfers
         * @memberof proto.TokenTransferList
         * @instance
         */
        TokenTransferList.prototype.transfers = $util.emptyArray;

        /**
         * A list of NftTransfers.
         * <p>
         * Each entry SHALL have a sender and receiver account, and the
         * serial number of the unique token to transfer.<br/>
         * This SHALL be be set for non-fungible/unique tokens and SHALL be
         * empty otherwise.
         * @member {Array.<proto.NftTransfer.$Properties>} nftTransfers
         * @memberof proto.TokenTransferList
         * @instance
         */
        TokenTransferList.prototype.nftTransfers = $util.emptyArray;

        /**
         * An expected decimal precision.<br/>
         * This is the number of decimals a fungible/common token type is
         * _expected_ to have.
         * <p>
         * The transfer SHALL fail with response code `UNEXPECTED_TOKEN_DECIMALS`
         * if this is set and the actual decimals specified for the `Token` differ
         * from this value.<br/>
         * If `nftTransfers` is set, then this value SHOULD NOT be set.
         * @member {google.protobuf.UInt32Value.$Properties|null|undefined} expectedDecimals
         * @memberof proto.TokenTransferList
         * @instance
         */
        TokenTransferList.prototype.expectedDecimals = null;

        /**
         * Creates a new TokenTransferList instance using the specified properties.
         * @function create
         * @memberof proto.TokenTransferList
         * @static
         * @param {proto.TokenTransferList.$Properties=} [properties] Properties to set
         * @returns {proto.TokenTransferList} TokenTransferList instance
         * @type {{
         *   (properties: proto.TokenTransferList.$Shape): proto.TokenTransferList & proto.TokenTransferList.$Shape;
         *   (properties?: proto.TokenTransferList.$Properties): proto.TokenTransferList;
         * }}
         */
        TokenTransferList.create = function(properties) {
            return new TokenTransferList(properties);
        };

        /**
         * Encodes the specified TokenTransferList message. Does not implicitly {@link proto.TokenTransferList.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenTransferList
         * @static
         * @param {proto.TokenTransferList.$Properties} message TokenTransferList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenTransferList.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.transfers != null && message.transfers.length)
                for (let i = 0; i < message.transfers.length; ++i)
                    $root.proto.AccountAmount.encode(message.transfers[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.nftTransfers != null && message.nftTransfers.length)
                for (let i = 0; i < message.nftTransfers.length; ++i)
                    $root.proto.NftTransfer.encode(message.nftTransfers[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.expectedDecimals != null && $Object.hasOwnProperty.call(message, "expectedDecimals"))
                $root.google.protobuf.UInt32Value.encode(message.expectedDecimals, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenTransferList message, length delimited. Does not implicitly {@link proto.TokenTransferList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenTransferList
         * @static
         * @param {proto.TokenTransferList.$Properties} message TokenTransferList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenTransferList.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenTransferList message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenTransferList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenTransferList & proto.TokenTransferList.$Shape} TokenTransferList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenTransferList.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenTransferList();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.transfers && message.transfers.length))
                            message.transfers = [];
                        message.transfers.push($root.proto.AccountAmount.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.nftTransfers && message.nftTransfers.length))
                            message.nftTransfers = [];
                        message.nftTransfers.push($root.proto.NftTransfer.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.expectedDecimals = $root.google.protobuf.UInt32Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expectedDecimals);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenTransferList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenTransferList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenTransferList & proto.TokenTransferList.$Shape} TokenTransferList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenTransferList.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenTransferList message.
         * @function verify
         * @memberof proto.TokenTransferList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenTransferList.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            if (message.transfers != null && $Object.hasOwnProperty.call(message, "transfers")) {
                if (!$Array.isArray(message.transfers))
                    return "transfers: array expected";
                for (let i = 0; i < message.transfers.length; ++i) {
                    let error = $root.proto.AccountAmount.verify(message.transfers[i], _depth + 1);
                    if (error)
                        return "transfers." + error;
                }
            }
            if (message.nftTransfers != null && $Object.hasOwnProperty.call(message, "nftTransfers")) {
                if (!$Array.isArray(message.nftTransfers))
                    return "nftTransfers: array expected";
                for (let i = 0; i < message.nftTransfers.length; ++i) {
                    let error = $root.proto.NftTransfer.verify(message.nftTransfers[i], _depth + 1);
                    if (error)
                        return "nftTransfers." + error;
                }
            }
            if (message.expectedDecimals != null && $Object.hasOwnProperty.call(message, "expectedDecimals")) {
                let error = $root.google.protobuf.UInt32Value.verify(message.expectedDecimals, _depth + 1);
                if (error)
                    return "expectedDecimals." + error;
            }
            return null;
        };

        /**
         * Creates a TokenTransferList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenTransferList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenTransferList} TokenTransferList
         */
        TokenTransferList.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenTransferList)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenTransferList: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenTransferList();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenTransferList.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            if (object.transfers) {
                if (!$Array.isArray(object.transfers))
                    throw $TypeError(".proto.TokenTransferList.transfers: array expected");
                message.transfers = $Array(object.transfers.length);
                for (let i = 0; i < object.transfers.length; ++i) {
                    if (!$util.isObject(object.transfers[i]))
                        throw $TypeError(".proto.TokenTransferList.transfers: object expected");
                    message.transfers[i] = $root.proto.AccountAmount.fromObject(object.transfers[i], _depth + 1);
                }
            }
            if (object.nftTransfers) {
                if (!$Array.isArray(object.nftTransfers))
                    throw $TypeError(".proto.TokenTransferList.nftTransfers: array expected");
                message.nftTransfers = $Array(object.nftTransfers.length);
                for (let i = 0; i < object.nftTransfers.length; ++i) {
                    if (!$util.isObject(object.nftTransfers[i]))
                        throw $TypeError(".proto.TokenTransferList.nftTransfers: object expected");
                    message.nftTransfers[i] = $root.proto.NftTransfer.fromObject(object.nftTransfers[i], _depth + 1);
                }
            }
            if (object.expectedDecimals != null) {
                if (!$util.isObject(object.expectedDecimals))
                    throw $TypeError(".proto.TokenTransferList.expectedDecimals: object expected");
                message.expectedDecimals = $root.google.protobuf.UInt32Value.fromObject(object.expectedDecimals, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenTransferList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenTransferList
         * @static
         * @param {proto.TokenTransferList} message TokenTransferList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenTransferList.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.transfers = [];
                object.nftTransfers = [];
            }
            if (options.defaults) {
                object.token = null;
                object.expectedDecimals = null;
            }
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            if (message.transfers && message.transfers.length) {
                object.transfers = $Array(message.transfers.length);
                for (let j = 0; j < message.transfers.length; ++j)
                    object.transfers[j] = $root.proto.AccountAmount.toObject(message.transfers[j], options, _depth + 1);
            }
            if (message.nftTransfers && message.nftTransfers.length) {
                object.nftTransfers = $Array(message.nftTransfers.length);
                for (let j = 0; j < message.nftTransfers.length; ++j)
                    object.nftTransfers[j] = $root.proto.NftTransfer.toObject(message.nftTransfers[j], options, _depth + 1);
            }
            if (message.expectedDecimals != null && $Object.hasOwnProperty.call(message, "expectedDecimals"))
                object.expectedDecimals = $root.google.protobuf.UInt32Value.toObject(message.expectedDecimals, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenTransferList to JSON.
         * @function toJSON
         * @memberof proto.TokenTransferList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenTransferList.prototype.toJSON = function() {
            return TokenTransferList.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenTransferList
         * @function getTypeUrl
         * @memberof proto.TokenTransferList
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenTransferList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenTransferList";
        };

        return TokenTransferList;
    })();

    proto.Fraction = (function() {

        /**
         * Properties of a Fraction.
         * @typedef {Object} proto.Fraction.$Properties
         * @property {number|Long|null} [numerator] A fractional number's numerator.
         * @property {number|Long|null} [denominator] A fractional number's denominator.
         * <p>
         * A zero value SHALL fail with response code `FRACTION_DIVIDES_BY_ZERO`.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Fraction.
         * @memberof proto
         * @interface IFraction
         * @augments proto.Fraction.$Properties
         * @deprecated Use proto.Fraction.$Properties instead.
         */

        /**
         * Shape of a Fraction.
         * @typedef {proto.Fraction.$Properties} proto.Fraction.$Shape
         */

        /**
         * Constructs a new Fraction.
         * @memberof proto
         * @classdesc A rational number.<br/>
         * A common use is to set the amount of a value transfer to collect as a
         * custom fee.
         * 
         * It is RECOMMENDED that both numerator and denominator be no larger than
         * necessary to express the required fraction. A very large numerator, in
         * particular, may not be reliable.
         * Both fields are REQUIRED and SHOULD be positive integers.
         * @constructor
         * @param {proto.Fraction.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Fraction = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A fractional number's numerator.
         * @member {number|Long} numerator
         * @memberof proto.Fraction
         * @instance
         */
        Fraction.prototype.numerator = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A fractional number's denominator.
         * <p>
         * A zero value SHALL fail with response code `FRACTION_DIVIDES_BY_ZERO`.
         * @member {number|Long} denominator
         * @memberof proto.Fraction
         * @instance
         */
        Fraction.prototype.denominator = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new Fraction instance using the specified properties.
         * @function create
         * @memberof proto.Fraction
         * @static
         * @param {proto.Fraction.$Properties=} [properties] Properties to set
         * @returns {proto.Fraction} Fraction instance
         * @type {{
         *   (properties: proto.Fraction.$Shape): proto.Fraction & proto.Fraction.$Shape;
         *   (properties?: proto.Fraction.$Properties): proto.Fraction;
         * }}
         */
        Fraction.create = function(properties) {
            return new Fraction(properties);
        };

        /**
         * Encodes the specified Fraction message. Does not implicitly {@link proto.Fraction.verify|verify} messages.
         * @function encode
         * @memberof proto.Fraction
         * @static
         * @param {proto.Fraction.$Properties} message Fraction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Fraction.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.numerator != null && $Object.hasOwnProperty.call(message, "numerator") && (typeof message.numerator === "object" ? message.numerator.low || message.numerator.high : message.numerator !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.numerator);
            if (message.denominator != null && $Object.hasOwnProperty.call(message, "denominator") && (typeof message.denominator === "object" ? message.denominator.low || message.denominator.high : message.denominator !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.denominator);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Fraction message, length delimited. Does not implicitly {@link proto.Fraction.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Fraction
         * @static
         * @param {proto.Fraction.$Properties} message Fraction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Fraction.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Fraction message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Fraction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Fraction & proto.Fraction.$Shape} Fraction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Fraction.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Fraction();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.numerator = value;
                        else
                            delete message.numerator;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.denominator = value;
                        else
                            delete message.denominator;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Fraction message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Fraction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Fraction & proto.Fraction.$Shape} Fraction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Fraction.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Fraction message.
         * @function verify
         * @memberof proto.Fraction
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Fraction.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.numerator != null && $Object.hasOwnProperty.call(message, "numerator"))
                if (!$util.isInteger(message.numerator) && !(message.numerator && $util.isInteger(message.numerator.low) && $util.isInteger(message.numerator.high)))
                    return "numerator: integer|Long expected";
            if (message.denominator != null && $Object.hasOwnProperty.call(message, "denominator"))
                if (!$util.isInteger(message.denominator) && !(message.denominator && $util.isInteger(message.denominator.low) && $util.isInteger(message.denominator.high)))
                    return "denominator: integer|Long expected";
            return null;
        };

        /**
         * Creates a Fraction message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Fraction
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Fraction} Fraction
         */
        Fraction.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Fraction)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Fraction: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Fraction();
            if (object.numerator != null)
                if (typeof object.numerator === "object" ? object.numerator.low || object.numerator.high : $Number(object.numerator) !== 0)
                    if ($util.Long)
                        message.numerator = $util.Long.fromValue(object.numerator, false);
                    else if (typeof object.numerator === "string")
                        message.numerator = $parseInt(object.numerator, 10);
                    else if (typeof object.numerator === "number")
                        message.numerator = object.numerator;
                    else if (typeof object.numerator === "object")
                        message.numerator = new $util.LongBits(object.numerator.low >>> 0, object.numerator.high >>> 0).toNumber();
            if (object.denominator != null)
                if (typeof object.denominator === "object" ? object.denominator.low || object.denominator.high : $Number(object.denominator) !== 0)
                    if ($util.Long)
                        message.denominator = $util.Long.fromValue(object.denominator, false);
                    else if (typeof object.denominator === "string")
                        message.denominator = $parseInt(object.denominator, 10);
                    else if (typeof object.denominator === "number")
                        message.denominator = object.denominator;
                    else if (typeof object.denominator === "object")
                        message.denominator = new $util.LongBits(object.denominator.low >>> 0, object.denominator.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a Fraction message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Fraction
         * @static
         * @param {proto.Fraction} message Fraction
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Fraction.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.numerator = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.numerator = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.denominator = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.denominator = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.numerator != null && $Object.hasOwnProperty.call(message, "numerator"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.numerator = typeof message.numerator === "number" ? $BigInt(message.numerator) : $util.Long.fromBits(message.numerator.low >>> 0, message.numerator.high >>> 0, false).toBigInt();
                else if (typeof message.numerator === "number")
                    object.numerator = options.longs === $String ? $String(message.numerator) : message.numerator;
                else
                    object.numerator = options.longs === $String ? $util.Long.prototype.toString.call(message.numerator) : options.longs === $Number ? new $util.LongBits(message.numerator.low >>> 0, message.numerator.high >>> 0).toNumber() : message.numerator;
            if (message.denominator != null && $Object.hasOwnProperty.call(message, "denominator"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.denominator = typeof message.denominator === "number" ? $BigInt(message.denominator) : $util.Long.fromBits(message.denominator.low >>> 0, message.denominator.high >>> 0, false).toBigInt();
                else if (typeof message.denominator === "number")
                    object.denominator = options.longs === $String ? $String(message.denominator) : message.denominator;
                else
                    object.denominator = options.longs === $String ? $util.Long.prototype.toString.call(message.denominator) : options.longs === $Number ? new $util.LongBits(message.denominator.low >>> 0, message.denominator.high >>> 0).toNumber() : message.denominator;
            return object;
        };

        /**
         * Converts this Fraction to JSON.
         * @function toJSON
         * @memberof proto.Fraction
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Fraction.prototype.toJSON = function() {
            return Fraction.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Fraction
         * @function getTypeUrl
         * @memberof proto.Fraction
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Fraction.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Fraction";
        };

        return Fraction;
    })();

    /**
     * Possible Token Types (IWA Compatibility).
     * 
     * Apart from fungible and non-fungible, Tokens can have either a common or
     * unique representation. Furthermore, tokens can have intrinsic or referential
     * value, and can be whole and indivisible or fractional.<br/>
     * These distinction might seem subtle, but it is important when considering
     * how tokens can be traced, used, transferred, and if they can have isolated
     * unique properties.
     * 
     * A few examples (these may not match enumerations below) using IWA taxonomy.
     * <dl>
     * <dt>fungible, whole, intrinsic, unique</dt>
     * <dd>Physical fiat currency</dd>
     * <dt>fungible, fractional, intrinsic, common</dt>
     * <dd>bank balance fiat currency</dd>
     * <dt>non-fungible, fractional, reference, unique</dt>
     * <dd>"mutual" collectible/art/property ownership</dd>
     * <dt>non-fungible, whole, intrinsic, unique</dt>
     * <dd>Physical work of fine art</dd>
     * <dt>non-fungible, whole, reference, unique</dt>
     * <dd>Registered property title</dd>
     * </dl>
     * @name proto.TokenType
     * @enum {number}
     * @property {number} FUNGIBLE_COMMON=0 A fungible/common token.<br/>
     * Tokens of this type are interchangeable with one another, where any
     * quantity of tokens has the same value as another equal quantity, if
     * they are in the same class. Tokens share a single set of properties,
     * and are not distinct from one another. Ownership is represented as a
     * balance or quantity associated to a given account. Tokens may be
     * divided into fractional tokens, within reasonable limits.
     * <p>
     * IWA taxonomy _fungible, fractional, intrinsic, common_
     * @property {number} NON_FUNGIBLE_UNIQUE=1 A non-fungible/unique token.<br/>
     * Tokens of this type are unique, and are not interchangeable with other
     * tokens of the same type. Each token carries a serial number which is
     * unique for that token, these tokens may have a different trade value
     * for each individual token. The tokens are individually accounted and
     * often carry additional unique properties. Tokens cannot be subdivided,
     * and value is related to what the individual token represents.
     * <p>
     * IWA taxonomy _non-fungible, whole, reference, unique_
     */
    proto.TokenType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "FUNGIBLE_COMMON"] = 0;
        values[valuesById[1] = "NON_FUNGIBLE_UNIQUE"] = 1;
        return values;
    })();

    /**
     * A transaction sub type.<br/>
     * This enumeration enables a set of transaction base fees to be broadly
     * defined for a type of operation and also be modified, when necessary,
     * based on specifics of the operation.
     * 
     * ### Explanation
     * The resource cost for a TokenMint operation is different between minting
     * fungible/common and non-fungible/unique tokens. This `enum` is used to
     * "mark" a cost as applying to one or the other.<br/>
     * Similarly, the resource cost for a basic `tokenCreate` without a custom
     * fee schedule may yield a _base_ fee of $1. The resource cost for a
     * `tokenCreate` _with_ a custom fee schedule is different and may yield a
     * _base_ fee of $2 or more.
     * @name proto.SubType
     * @enum {number}
     * @property {number} DEFAULT=0 The resource cost for the transaction type has no additional attributes
     * @property {number} TOKEN_FUNGIBLE_COMMON=1 The resource cost for the transaction type includes an operation on a
     * fungible/common token
     * @property {number} TOKEN_NON_FUNGIBLE_UNIQUE=2 The resource cost for the transaction type includes an operation on
     * a non-fungible/unique token
     * @property {number} TOKEN_FUNGIBLE_COMMON_WITH_CUSTOM_FEES=3 The resource cost for the transaction type includes an operation on a
     * fungible/common token with a custom fee schedule
     * @property {number} TOKEN_NON_FUNGIBLE_UNIQUE_WITH_CUSTOM_FEES=4 The resource cost for the transaction type includes an operation on a
     * non-fungible/unique token with a custom fee schedule
     * @property {number} SCHEDULE_CREATE_CONTRACT_CALL=5 The resource cost for the transaction type includes a ScheduleCreate
     * containing a ContractCall.
     * @property {number} TOPIC_CREATE_WITH_CUSTOM_FEES=6 The resource cost for the transaction type includes a TopicCreate
     * with custom fees.
     * @property {number} SUBMIT_MESSAGE_WITH_CUSTOM_FEES=7 The resource cost for the transaction type includes a ConsensusSubmitMessage
     * for a topic with custom fees.
     * @property {number} CRYPTO_TRANSFER_WITH_HOOKS=8 The resource cost for the transaction type that includes a CryptoTransfer with hook invocations
     */
    proto.SubType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "DEFAULT"] = 0;
        values[valuesById[1] = "TOKEN_FUNGIBLE_COMMON"] = 1;
        values[valuesById[2] = "TOKEN_NON_FUNGIBLE_UNIQUE"] = 2;
        values[valuesById[3] = "TOKEN_FUNGIBLE_COMMON_WITH_CUSTOM_FEES"] = 3;
        values[valuesById[4] = "TOKEN_NON_FUNGIBLE_UNIQUE_WITH_CUSTOM_FEES"] = 4;
        values[valuesById[5] = "SCHEDULE_CREATE_CONTRACT_CALL"] = 5;
        values[valuesById[6] = "TOPIC_CREATE_WITH_CUSTOM_FEES"] = 6;
        values[valuesById[7] = "SUBMIT_MESSAGE_WITH_CUSTOM_FEES"] = 7;
        values[valuesById[8] = "CRYPTO_TRANSFER_WITH_HOOKS"] = 8;
        return values;
    })();

    /**
     * Possible Token Supply Types (IWA Compatibility).
     * 
     * This `enum` indicates the limit of tokens that can exist during the
     * lifetime of a token definition. The "infinite" supply is only theoretically
     * infinite, as it is still limited to the magnitude of a 64-bit signed
     * integer. A "finite" supply is further limited to a value specified when
     * the token is created (or updated, if not immutable).
     * @name proto.TokenSupplyType
     * @enum {number}
     * @property {number} INFINITE=0 An unlimited supply.<br/>
     * This indicates that tokens of this type have an upper bound of
     * Long.MAX_VALUE.<br/>
     * The supply is accounted in the smallest units of the token
     * (i.e. 10<sup>-`decimals`</sup> whole tokens)
     * @property {number} FINITE=1 A limited supply.<br/>
     * This indicates that tokens of this type have an upper bound of
     * `maxSupply`.<br/>
     * The maximum supply SHALL be provided on token creation, but MAY be
     * changed thereafter if the token has an `admin_key` set.
     */
    proto.TokenSupplyType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "INFINITE"] = 0;
        values[valuesById[1] = "FINITE"] = 1;
        return values;
    })();

    /**
     * Types of validation strategies for token keys.
     * @name proto.TokenKeyValidation
     * @enum {number}
     * @property {number} FULL_VALIDATION=0 Perform all token key validations.<br/>
     * This is the default value and behavior.
     * @property {number} NO_VALIDATION=1 Perform no validations at all for all passed token keys.
     */
    proto.TokenKeyValidation = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "FULL_VALIDATION"] = 0;
        values[valuesById[1] = "NO_VALIDATION"] = 1;
        return values;
    })();

    /**
     * Possible token freeze status values.
     * 
     * This is returned by `TokenGetInfoQuery` or `CryptoGetInfoResponse`
     * in `TokenRelationship`.
     * @name proto.TokenFreezeStatus
     * @enum {number}
     * @property {number} FreezeNotApplicable=0 The token does not support freeze or cannot be frozen for the designated
     * account.<br/>
     * Typically this indicates that the token does not have a `freeze_key` set.
     * @property {number} Frozen=1 The token is currently frozen for the designated account.
     * @property {number} Unfrozen=2 The token is not currently frozen for the designated account.
     */
    proto.TokenFreezeStatus = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "FreezeNotApplicable"] = 0;
        values[valuesById[1] = "Frozen"] = 1;
        values[valuesById[2] = "Unfrozen"] = 2;
        return values;
    })();

    /**
     * Possible token "KYC" status values.
     * 
     * This is returned by `TokenGetInfoQuery` or `CryptoGetInfoResponse`
     * in `TokenRelationship`.
     * @name proto.TokenKycStatus
     * @enum {number}
     * @property {number} KycNotApplicable=0 The token does not support KYC or cannot grant KYC for the
     * designated account.<br/>
     * Typically this indicates that the token does not have a `kyc_key` set.
     * @property {number} Granted=1 The designated account is currently granted KYC status for the
     * designated token.
     * @property {number} Revoked=2 The designated account is not currently granted KYC status for the
     * designated token.
     */
    proto.TokenKycStatus = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "KycNotApplicable"] = 0;
        values[valuesById[1] = "Granted"] = 1;
        values[valuesById[2] = "Revoked"] = 2;
        return values;
    })();

    /**
     * Possible Pause status values.
     * 
     * This is returned by `TokenGetInfoQuery` in `TokenRelationship`.
     * @name proto.TokenPauseStatus
     * @enum {number}
     * @property {number} PauseNotApplicable=0 The token does not support pause or cannot be paused.<br/>
     * Typically this indicates that the token does not have a `pause_key` set.
     * @property {number} Paused=1 The token is currently paused.
     * @property {number} Unpaused=2 The token is not currently paused.
     */
    proto.TokenPauseStatus = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "PauseNotApplicable"] = 0;
        values[valuesById[1] = "Paused"] = 1;
        values[valuesById[2] = "Unpaused"] = 2;
        return values;
    })();

    proto.Key = (function() {

        /**
         * Properties of a Key.
         * @typedef {Object} proto.Key.$Properties
         * @property {proto.ContractID.$Properties|null} [contractID] A smart contract instance that is authorized implicitly.
         * <p>
         * This key type SHALL require that the code in the active message frame
         * belong to the contract with the given id.
         * @property {Uint8Array|null} [ed25519] An array of Ed25519 public key bytes.
         * @property {Uint8Array|null} [RSA_3072] This option is not currently supported.<br/>
         * An array of RSA-3072 public key bytes.
         * @property {Uint8Array|null} [ECDSA_384] This option is not currently supported.<br/>
         * An array of ECDSA, using the p-384 curve, public key bytes.
         * @property {proto.ThresholdKey.$Properties|null} [thresholdKey] A threshold, M, combined with a list of N keys, any M of which are
         * sufficient to form a valid signature.
         * @property {proto.KeyList.$Properties|null} [keyList] A list of keys. This may be treated like a "N-of-N" threshold key,
         * as a component of another key, or in some other manner as documented.
         * @property {Uint8Array|null} [ECDSASecp256k1] A set of compressed ECDSA(secp256k1) public key bytes.<br/>
         * This is an EVM compatibility format.
         * @property {proto.ContractID.$Properties|null} [delegatableContractId] A smart contract that, if the recipient of the active message frame,
         * SHALL be imputed authorization.<br/>
         * Setting this key type is a more permissive version of setting a
         * contractID key.
         * <p>
         * This key form SHALL NOT strictly require that the code being executed
         * in the frame belong to the given contract. The code in frame MAY be
         * running another contract via a `delegatecall`.
         * @property {"contractID"|"ed25519"|"RSA_3072"|"ECDSA_384"|"thresholdKey"|"keyList"|"ECDSASecp256k1"|"delegatableContractId"} [key] Key key
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Key.
         * @memberof proto
         * @interface IKey
         * @augments proto.Key.$Properties
         * @deprecated Use proto.Key.$Properties instead.
         */

        /**
         * Narrowed shape of a Key.
         * @typedef {{
         *   contractID?: proto.ContractID.$Shape|null;
         *   ed25519?: Uint8Array|null;
         *   RSA_3072?: Uint8Array|null;
         *   ECDSA_384?: Uint8Array|null;
         *   thresholdKey?: proto.ThresholdKey.$Shape|null;
         *   keyList?: proto.KeyList.$Shape|null;
         *   ECDSASecp256k1?: Uint8Array|null;
         *   delegatableContractId?: proto.ContractID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ key?: undefined; contractID?: null; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; thresholdKey?: null; keyList?: null; ECDSASecp256k1?: null; delegatableContractId?: null }|{ key?: "contractID"; contractID: proto.ContractID.$Shape; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; thresholdKey?: null; keyList?: null; ECDSASecp256k1?: null; delegatableContractId?: null }|{ key?: "ed25519"; contractID?: null; ed25519: Uint8Array; RSA_3072?: null; ECDSA_384?: null; thresholdKey?: null; keyList?: null; ECDSASecp256k1?: null; delegatableContractId?: null }|{ key?: "RSA_3072"; contractID?: null; ed25519?: null; RSA_3072: Uint8Array; ECDSA_384?: null; thresholdKey?: null; keyList?: null; ECDSASecp256k1?: null; delegatableContractId?: null }|{ key?: "ECDSA_384"; contractID?: null; ed25519?: null; RSA_3072?: null; ECDSA_384: Uint8Array; thresholdKey?: null; keyList?: null; ECDSASecp256k1?: null; delegatableContractId?: null }|{ key?: "thresholdKey"; contractID?: null; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; thresholdKey: proto.ThresholdKey.$Shape; keyList?: null; ECDSASecp256k1?: null; delegatableContractId?: null }|{ key?: "keyList"; contractID?: null; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; thresholdKey?: null; keyList: proto.KeyList.$Shape; ECDSASecp256k1?: null; delegatableContractId?: null }|{ key?: "ECDSASecp256k1"; contractID?: null; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; thresholdKey?: null; keyList?: null; ECDSASecp256k1: Uint8Array; delegatableContractId?: null }|{ key?: "delegatableContractId"; contractID?: null; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; thresholdKey?: null; keyList?: null; ECDSASecp256k1?: null; delegatableContractId: proto.ContractID.$Shape })
         * )} proto.Key.$Shape
         */

        /**
         * Constructs a new Key.
         * @memberof proto
         * @classdesc A Key is an entity representing one or more cryptographic public/private key
         * pairs and, optionally, the structure for how multiple signatures may be
         * composed to meet complex multiple-signature authorization requirements.
         * 
         * A Key can be a public key from either the Ed25519 or ECDSA(secp256k1)
         * signature schemes. In the ECDSA(secp256k1) case we require the 33-byte
         * compressed form of the public key. For simplicity, we call these
         * cryptographic public keys `primitive` keys.<br/>
         * If an entity has a primitive key associated to it, then the corresponding
         * private key must sign any transaction to send tokens or perform other
         * actions requiring authorization.
         * 
         * A Key can also be the ID of a smart contract, which SHALL authorize that
         * contract to execute any system contract with signing requirements that are
         * met by the key.<br/>
         * > Example
         * >> If account `0.0.A` has a threshold key whose threshold is satisfied
         * >> by a contract ID key for contract `0.0.C`, then when `0.0.C` is called,
         * >> it is authorized to use system contracts to manage any asset owned by
         * >> `0.0.A`. If the contract ID key is "delegatable", then `0.0.C` can even
         * >> perform these actions when running code accessed via `DELEGATECALL`.
         * 
         * A Key can be a "threshold key", which is a list of N keys, any M of which
         * may sign in order for the signature to be considered valid. The value of
         * M for a given threshold key MUST be less than or equal to N. A threshold
         * key is sometimes called a "M-of-N" key.
         * 
         * A Key can be a "key list" where all keys in the list must sign unless
         * specified otherwise in the documentation for a specific transaction
         * type (e.g. FileDeleteTransactionBody).<br/>
         * This implies that the use of a key list is dependent on context. For
         * example, an Hedera file that is created with a list of keys, SHALL require
         * that all of those keys must sign a transaction to create or modify the file,
         * but only one key from that list MUST sign a transaction to delete the file.
         * So it is a single list that sometimes acts as a N-of-N threshold key, and
         * sometimes acts as a 1-of-N threshold key.<br/>
         * To reduce confusion this may cause, a key list SHALL always be considered
         * N-of-N, unless specified otherwise in official documentation.<br/>
         * A key list MAY have repeated primitive public keys, but the signature
         * requirement for all keys in a repeated set SHALL be satisfied by a single
         * valid signature. There is no mechanism to require a single key to sign a
         * single transaction more than once.
         * 
         * Any list or threshold key MAY have nested key lists or threshold keys.
         * This allows, for example, the keys within a threshold signature to
         * themselves be threshold, list, contract, or primitive keys. This nesting
         * structure enables complex asymmetric multi-party signature requirements to
         * be met.
         * 
         * To ensure adequate performance and transaction security, key nesting is
         * limited to at most fifteen(15) levels.
         * @constructor
         * @param {proto.Key.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Key = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A smart contract instance that is authorized implicitly.
         * <p>
         * This key type SHALL require that the code in the active message frame
         * belong to the contract with the given id.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.Key
         * @instance
         */
        Key.prototype.contractID = null;

        /**
         * An array of Ed25519 public key bytes.
         * @member {Uint8Array|null|undefined} ed25519
         * @memberof proto.Key
         * @instance
         */
        Key.prototype.ed25519 = null;

        /**
         * This option is not currently supported.<br/>
         * An array of RSA-3072 public key bytes.
         * @member {Uint8Array|null|undefined} RSA_3072
         * @memberof proto.Key
         * @instance
         */
        Key.prototype.RSA_3072 = null;

        /**
         * This option is not currently supported.<br/>
         * An array of ECDSA, using the p-384 curve, public key bytes.
         * @member {Uint8Array|null|undefined} ECDSA_384
         * @memberof proto.Key
         * @instance
         */
        Key.prototype.ECDSA_384 = null;

        /**
         * A threshold, M, combined with a list of N keys, any M of which are
         * sufficient to form a valid signature.
         * @member {proto.ThresholdKey.$Properties|null|undefined} thresholdKey
         * @memberof proto.Key
         * @instance
         */
        Key.prototype.thresholdKey = null;

        /**
         * A list of keys. This may be treated like a "N-of-N" threshold key,
         * as a component of another key, or in some other manner as documented.
         * @member {proto.KeyList.$Properties|null|undefined} keyList
         * @memberof proto.Key
         * @instance
         */
        Key.prototype.keyList = null;

        /**
         * A set of compressed ECDSA(secp256k1) public key bytes.<br/>
         * This is an EVM compatibility format.
         * @member {Uint8Array|null|undefined} ECDSASecp256k1
         * @memberof proto.Key
         * @instance
         */
        Key.prototype.ECDSASecp256k1 = null;

        /**
         * A smart contract that, if the recipient of the active message frame,
         * SHALL be imputed authorization.<br/>
         * Setting this key type is a more permissive version of setting a
         * contractID key.
         * <p>
         * This key form SHALL NOT strictly require that the code being executed
         * in the frame belong to the given contract. The code in frame MAY be
         * running another contract via a `delegatecall`.
         * @member {proto.ContractID.$Properties|null|undefined} delegatableContractId
         * @memberof proto.Key
         * @instance
         */
        Key.prototype.delegatableContractId = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * Key key.
         * @member {"contractID"|"ed25519"|"RSA_3072"|"ECDSA_384"|"thresholdKey"|"keyList"|"ECDSASecp256k1"|"delegatableContractId"|undefined} key
         * @memberof proto.Key
         * @instance
         */
        $Object.defineProperty(Key.prototype, "key", {
            get: $util.oneOfGetter($oneOfFields = ["contractID", "ed25519", "RSA_3072", "ECDSA_384", "thresholdKey", "keyList", "ECDSASecp256k1", "delegatableContractId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Key instance using the specified properties.
         * @function create
         * @memberof proto.Key
         * @static
         * @param {proto.Key.$Properties=} [properties] Properties to set
         * @returns {proto.Key} Key instance
         * @type {{
         *   (properties: proto.Key.$Shape): proto.Key & proto.Key.$Shape;
         *   (properties?: proto.Key.$Properties): proto.Key;
         * }}
         */
        Key.create = function(properties) {
            return new Key(properties);
        };

        /**
         * Encodes the specified Key message. Does not implicitly {@link proto.Key.verify|verify} messages.
         * @function encode
         * @memberof proto.Key
         * @static
         * @param {proto.Key.$Properties} message Key message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Key.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.ed25519 != null && $Object.hasOwnProperty.call(message, "ed25519"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.ed25519);
            if (message.RSA_3072 != null && $Object.hasOwnProperty.call(message, "RSA_3072"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.RSA_3072);
            if (message.ECDSA_384 != null && $Object.hasOwnProperty.call(message, "ECDSA_384"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.ECDSA_384);
            if (message.thresholdKey != null && $Object.hasOwnProperty.call(message, "thresholdKey"))
                $root.proto.ThresholdKey.encode(message.thresholdKey, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.keyList != null && $Object.hasOwnProperty.call(message, "keyList"))
                $root.proto.KeyList.encode(message.keyList, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.ECDSASecp256k1 != null && $Object.hasOwnProperty.call(message, "ECDSASecp256k1"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.ECDSASecp256k1);
            if (message.delegatableContractId != null && $Object.hasOwnProperty.call(message, "delegatableContractId"))
                $root.proto.ContractID.encode(message.delegatableContractId, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Key message, length delimited. Does not implicitly {@link proto.Key.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Key
         * @static
         * @param {proto.Key.$Properties} message Key message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Key.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Key message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Key
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Key & proto.Key.$Shape} Key
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Key.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Key();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        message.key = "contractID";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.ed25519 = reader.bytes();
                        message.key = "ed25519";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.RSA_3072 = reader.bytes();
                        message.key = "RSA_3072";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.ECDSA_384 = reader.bytes();
                        message.key = "ECDSA_384";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.thresholdKey = $root.proto.ThresholdKey.decode(reader, reader.uint32(), $undefined, _depth + 1, message.thresholdKey);
                        message.key = "thresholdKey";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.keyList = $root.proto.KeyList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.keyList);
                        message.key = "keyList";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.ECDSASecp256k1 = reader.bytes();
                        message.key = "ECDSASecp256k1";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.delegatableContractId = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.delegatableContractId);
                        message.key = "delegatableContractId";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Key message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Key
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Key & proto.Key.$Shape} Key
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Key.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Key message.
         * @function verify
         * @memberof proto.Key
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Key.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                properties.key = 1;
                {
                    let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                    if (error)
                        return "contractID." + error;
                }
            }
            if (message.ed25519 != null && $Object.hasOwnProperty.call(message, "ed25519")) {
                if (properties.key === 1)
                    return "key: multiple values";
                properties.key = 1;
                if (!(message.ed25519 && typeof message.ed25519.length === "number" || $util.isString(message.ed25519)))
                    return "ed25519: buffer expected";
            }
            if (message.RSA_3072 != null && $Object.hasOwnProperty.call(message, "RSA_3072")) {
                if (properties.key === 1)
                    return "key: multiple values";
                properties.key = 1;
                if (!(message.RSA_3072 && typeof message.RSA_3072.length === "number" || $util.isString(message.RSA_3072)))
                    return "RSA_3072: buffer expected";
            }
            if (message.ECDSA_384 != null && $Object.hasOwnProperty.call(message, "ECDSA_384")) {
                if (properties.key === 1)
                    return "key: multiple values";
                properties.key = 1;
                if (!(message.ECDSA_384 && typeof message.ECDSA_384.length === "number" || $util.isString(message.ECDSA_384)))
                    return "ECDSA_384: buffer expected";
            }
            if (message.thresholdKey != null && $Object.hasOwnProperty.call(message, "thresholdKey")) {
                if (properties.key === 1)
                    return "key: multiple values";
                properties.key = 1;
                {
                    let error = $root.proto.ThresholdKey.verify(message.thresholdKey, _depth + 1);
                    if (error)
                        return "thresholdKey." + error;
                }
            }
            if (message.keyList != null && $Object.hasOwnProperty.call(message, "keyList")) {
                if (properties.key === 1)
                    return "key: multiple values";
                properties.key = 1;
                {
                    let error = $root.proto.KeyList.verify(message.keyList, _depth + 1);
                    if (error)
                        return "keyList." + error;
                }
            }
            if (message.ECDSASecp256k1 != null && $Object.hasOwnProperty.call(message, "ECDSASecp256k1")) {
                if (properties.key === 1)
                    return "key: multiple values";
                properties.key = 1;
                if (!(message.ECDSASecp256k1 && typeof message.ECDSASecp256k1.length === "number" || $util.isString(message.ECDSASecp256k1)))
                    return "ECDSASecp256k1: buffer expected";
            }
            if (message.delegatableContractId != null && $Object.hasOwnProperty.call(message, "delegatableContractId")) {
                if (properties.key === 1)
                    return "key: multiple values";
                properties.key = 1;
                {
                    let error = $root.proto.ContractID.verify(message.delegatableContractId, _depth + 1);
                    if (error)
                        return "delegatableContractId." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Key message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Key
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Key} Key
         */
        Key.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Key)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Key: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Key();
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.Key.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            if (object.ed25519 != null)
                if (typeof object.ed25519 === "string")
                    $util.base64.decode(object.ed25519, message.ed25519 = $util.newBuffer($util.base64.length(object.ed25519)), 0);
                else if (object.ed25519.length >= 0)
                    message.ed25519 = object.ed25519;
            if (object.RSA_3072 != null)
                if (typeof object.RSA_3072 === "string")
                    $util.base64.decode(object.RSA_3072, message.RSA_3072 = $util.newBuffer($util.base64.length(object.RSA_3072)), 0);
                else if (object.RSA_3072.length >= 0)
                    message.RSA_3072 = object.RSA_3072;
            if (object.ECDSA_384 != null)
                if (typeof object.ECDSA_384 === "string")
                    $util.base64.decode(object.ECDSA_384, message.ECDSA_384 = $util.newBuffer($util.base64.length(object.ECDSA_384)), 0);
                else if (object.ECDSA_384.length >= 0)
                    message.ECDSA_384 = object.ECDSA_384;
            if (object.thresholdKey != null) {
                if (!$util.isObject(object.thresholdKey))
                    throw $TypeError(".proto.Key.thresholdKey: object expected");
                message.thresholdKey = $root.proto.ThresholdKey.fromObject(object.thresholdKey, _depth + 1);
            }
            if (object.keyList != null) {
                if (!$util.isObject(object.keyList))
                    throw $TypeError(".proto.Key.keyList: object expected");
                message.keyList = $root.proto.KeyList.fromObject(object.keyList, _depth + 1);
            }
            if (object.ECDSASecp256k1 != null)
                if (typeof object.ECDSASecp256k1 === "string")
                    $util.base64.decode(object.ECDSASecp256k1, message.ECDSASecp256k1 = $util.newBuffer($util.base64.length(object.ECDSASecp256k1)), 0);
                else if (object.ECDSASecp256k1.length >= 0)
                    message.ECDSASecp256k1 = object.ECDSASecp256k1;
            if (object.delegatableContractId != null) {
                if (!$util.isObject(object.delegatableContractId))
                    throw $TypeError(".proto.Key.delegatableContractId: object expected");
                message.delegatableContractId = $root.proto.ContractID.fromObject(object.delegatableContractId, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Key message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Key
         * @static
         * @param {proto.Key} message Key
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Key.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
                if (options.oneofs)
                    object.key = "contractID";
            }
            if (message.ed25519 != null && $Object.hasOwnProperty.call(message, "ed25519")) {
                object.ed25519 = options.bytes === $String ? $util.base64.encode(message.ed25519, 0, message.ed25519.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ed25519) : message.ed25519;
                if (options.oneofs)
                    object.key = "ed25519";
            }
            if (message.RSA_3072 != null && $Object.hasOwnProperty.call(message, "RSA_3072")) {
                object.RSA_3072 = options.bytes === $String ? $util.base64.encode(message.RSA_3072, 0, message.RSA_3072.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.RSA_3072) : message.RSA_3072;
                if (options.oneofs)
                    object.key = "RSA_3072";
            }
            if (message.ECDSA_384 != null && $Object.hasOwnProperty.call(message, "ECDSA_384")) {
                object.ECDSA_384 = options.bytes === $String ? $util.base64.encode(message.ECDSA_384, 0, message.ECDSA_384.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ECDSA_384) : message.ECDSA_384;
                if (options.oneofs)
                    object.key = "ECDSA_384";
            }
            if (message.thresholdKey != null && $Object.hasOwnProperty.call(message, "thresholdKey")) {
                object.thresholdKey = $root.proto.ThresholdKey.toObject(message.thresholdKey, options, _depth + 1);
                if (options.oneofs)
                    object.key = "thresholdKey";
            }
            if (message.keyList != null && $Object.hasOwnProperty.call(message, "keyList")) {
                object.keyList = $root.proto.KeyList.toObject(message.keyList, options, _depth + 1);
                if (options.oneofs)
                    object.key = "keyList";
            }
            if (message.ECDSASecp256k1 != null && $Object.hasOwnProperty.call(message, "ECDSASecp256k1")) {
                object.ECDSASecp256k1 = options.bytes === $String ? $util.base64.encode(message.ECDSASecp256k1, 0, message.ECDSASecp256k1.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ECDSASecp256k1) : message.ECDSASecp256k1;
                if (options.oneofs)
                    object.key = "ECDSASecp256k1";
            }
            if (message.delegatableContractId != null && $Object.hasOwnProperty.call(message, "delegatableContractId")) {
                object.delegatableContractId = $root.proto.ContractID.toObject(message.delegatableContractId, options, _depth + 1);
                if (options.oneofs)
                    object.key = "delegatableContractId";
            }
            return object;
        };

        /**
         * Converts this Key to JSON.
         * @function toJSON
         * @memberof proto.Key
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Key.prototype.toJSON = function() {
            return Key.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Key
         * @function getTypeUrl
         * @memberof proto.Key
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Key.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Key";
        };

        return Key;
    })();

    proto.ThresholdKey = (function() {

        /**
         * Properties of a ThresholdKey.
         * @typedef {Object} proto.ThresholdKey.$Properties
         * @property {number|null} [threshold] A transaction MUST have valid signatures from at least this number of
         * separate keys, from the `keys` list to be authorized by this key.
         * @property {proto.KeyList.$Properties|null} [keys] A list of the keys that MAY satisfy signature requirements of this key.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ThresholdKey.
         * @memberof proto
         * @interface IThresholdKey
         * @augments proto.ThresholdKey.$Properties
         * @deprecated Use proto.ThresholdKey.$Properties instead.
         */

        /**
         * Shape of a ThresholdKey.
         * @typedef {{
         *   threshold?: number|null;
         *   keys?: proto.KeyList.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ThresholdKey.$Shape
         */

        /**
         * Constructs a new ThresholdKey.
         * @memberof proto
         * @classdesc A threshold value and a list of public keys that, together, form a threshold
         * signature requirement. Any subset of the keys in the list may satisfy the
         * signature requirements of this type of key, provided the number of keys meets
         * or exceeds the threshold. For example, if a particular key has a threshold of
         * three(3) and eight(8) keys in the list, then any three(3) signatures, from
         * the list of eight(8), is sufficient to authorize that key.
         * 
         * For threshold purposes, all signatures from a single `primitive` key are
         * considered a single signature, so that signature(s) from a single key SHALL
         * NOT _directly_ meet a threshold greater than one(1).
         * 
         * #### Note
         * > It is possible to construct a complex key structure that _would_ enable a
         * > single primitive key to successfully meet a threshold requirement. All
         * > threshold keys SHOULD be carefully audited to ensure no one `primitive`
         * > key, or smart contract, has disproportionate capability.
         * @constructor
         * @param {proto.ThresholdKey.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ThresholdKey = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A transaction MUST have valid signatures from at least this number of
         * separate keys, from the `keys` list to be authorized by this key.
         * @member {number} threshold
         * @memberof proto.ThresholdKey
         * @instance
         */
        ThresholdKey.prototype.threshold = 0;

        /**
         * A list of the keys that MAY satisfy signature requirements of this key.
         * @member {proto.KeyList.$Properties|null|undefined} keys
         * @memberof proto.ThresholdKey
         * @instance
         */
        ThresholdKey.prototype.keys = null;

        /**
         * Creates a new ThresholdKey instance using the specified properties.
         * @function create
         * @memberof proto.ThresholdKey
         * @static
         * @param {proto.ThresholdKey.$Properties=} [properties] Properties to set
         * @returns {proto.ThresholdKey} ThresholdKey instance
         * @type {{
         *   (properties: proto.ThresholdKey.$Shape): proto.ThresholdKey & proto.ThresholdKey.$Shape;
         *   (properties?: proto.ThresholdKey.$Properties): proto.ThresholdKey;
         * }}
         */
        ThresholdKey.create = function(properties) {
            return new ThresholdKey(properties);
        };

        /**
         * Encodes the specified ThresholdKey message. Does not implicitly {@link proto.ThresholdKey.verify|verify} messages.
         * @function encode
         * @memberof proto.ThresholdKey
         * @static
         * @param {proto.ThresholdKey.$Properties} message ThresholdKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThresholdKey.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.threshold != null && $Object.hasOwnProperty.call(message, "threshold") && message.threshold !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.threshold);
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys"))
                $root.proto.KeyList.encode(message.keys, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ThresholdKey message, length delimited. Does not implicitly {@link proto.ThresholdKey.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ThresholdKey
         * @static
         * @param {proto.ThresholdKey.$Properties} message ThresholdKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThresholdKey.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ThresholdKey message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ThresholdKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ThresholdKey & proto.ThresholdKey.$Shape} ThresholdKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThresholdKey.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ThresholdKey();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.threshold = value;
                        else
                            delete message.threshold;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.keys = $root.proto.KeyList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.keys);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ThresholdKey message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ThresholdKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ThresholdKey & proto.ThresholdKey.$Shape} ThresholdKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThresholdKey.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ThresholdKey message.
         * @function verify
         * @memberof proto.ThresholdKey
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ThresholdKey.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.threshold != null && $Object.hasOwnProperty.call(message, "threshold"))
                if (!$util.isInteger(message.threshold))
                    return "threshold: integer expected";
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys")) {
                let error = $root.proto.KeyList.verify(message.keys, _depth + 1);
                if (error)
                    return "keys." + error;
            }
            return null;
        };

        /**
         * Creates a ThresholdKey message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ThresholdKey
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ThresholdKey} ThresholdKey
         */
        ThresholdKey.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ThresholdKey)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ThresholdKey: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ThresholdKey();
            if (object.threshold != null)
                if ($Number(object.threshold) !== 0)
                    message.threshold = object.threshold >>> 0;
            if (object.keys != null) {
                if (!$util.isObject(object.keys))
                    throw $TypeError(".proto.ThresholdKey.keys: object expected");
                message.keys = $root.proto.KeyList.fromObject(object.keys, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ThresholdKey message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ThresholdKey
         * @static
         * @param {proto.ThresholdKey} message ThresholdKey
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThresholdKey.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.threshold = 0;
                object.keys = null;
            }
            if (message.threshold != null && $Object.hasOwnProperty.call(message, "threshold"))
                object.threshold = message.threshold;
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys"))
                object.keys = $root.proto.KeyList.toObject(message.keys, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ThresholdKey to JSON.
         * @function toJSON
         * @memberof proto.ThresholdKey
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThresholdKey.prototype.toJSON = function() {
            return ThresholdKey.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ThresholdKey
         * @function getTypeUrl
         * @memberof proto.ThresholdKey
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ThresholdKey.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ThresholdKey";
        };

        return ThresholdKey;
    })();

    proto.KeyList = (function() {

        /**
         * Properties of a KeyList.
         * @typedef {Object} proto.KeyList.$Properties
         * @property {Array.<proto.Key.$Properties>|null} [keys] A list of keys. All values in this list SHALL be non-null.
         * <p>
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a KeyList.
         * @memberof proto
         * @interface IKeyList
         * @augments proto.KeyList.$Properties
         * @deprecated Use proto.KeyList.$Properties instead.
         */

        /**
         * Shape of a KeyList.
         * @typedef {{
         *   keys?: Array.<proto.Key.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.KeyList.$Shape
         */

        /**
         * Constructs a new KeyList.
         * @memberof proto
         * @classdesc A list of keys.<br/>
         * A `KeyList` requires all keys (N-of-N) to sign, unless otherwise
         * specified in official documentation. A KeyList may contain repeated keys,
         * but all such repeated keys are considered a single key when determining
         * signature authorization.
         * 
         * ### Additional Notes
         * 1. An empty key list is the "standard" mechanism to represent an
         * unassigned key. For example, if the `admin_key` of a token is set
         * to the empty key list, then that token has no admin key, and
         * functionality that requires an admin key to sign the
         * transaction is disabled.
         * @constructor
         * @param {proto.KeyList.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const KeyList = function (properties) {
            this.keys = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of keys. All values in this list SHALL be non-null.
         * <p>
         * @member {Array.<proto.Key.$Properties>} keys
         * @memberof proto.KeyList
         * @instance
         */
        KeyList.prototype.keys = $util.emptyArray;

        /**
         * Creates a new KeyList instance using the specified properties.
         * @function create
         * @memberof proto.KeyList
         * @static
         * @param {proto.KeyList.$Properties=} [properties] Properties to set
         * @returns {proto.KeyList} KeyList instance
         * @type {{
         *   (properties: proto.KeyList.$Shape): proto.KeyList & proto.KeyList.$Shape;
         *   (properties?: proto.KeyList.$Properties): proto.KeyList;
         * }}
         */
        KeyList.create = function(properties) {
            return new KeyList(properties);
        };

        /**
         * Encodes the specified KeyList message. Does not implicitly {@link proto.KeyList.verify|verify} messages.
         * @function encode
         * @memberof proto.KeyList
         * @static
         * @param {proto.KeyList.$Properties} message KeyList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyList.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.keys != null && message.keys.length)
                for (let i = 0; i < message.keys.length; ++i)
                    $root.proto.Key.encode(message.keys[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified KeyList message, length delimited. Does not implicitly {@link proto.KeyList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.KeyList
         * @static
         * @param {proto.KeyList.$Properties} message KeyList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyList.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a KeyList message from the specified reader or buffer.
         * @function decode
         * @memberof proto.KeyList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.KeyList & proto.KeyList.$Shape} KeyList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyList.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.KeyList();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.keys && message.keys.length))
                            message.keys = [];
                        message.keys.push($root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a KeyList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.KeyList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.KeyList & proto.KeyList.$Shape} KeyList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyList.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a KeyList message.
         * @function verify
         * @memberof proto.KeyList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        KeyList.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys")) {
                if (!$Array.isArray(message.keys))
                    return "keys: array expected";
                for (let i = 0; i < message.keys.length; ++i) {
                    let error = $root.proto.Key.verify(message.keys[i], _depth + 1);
                    if (error)
                        return "keys." + error;
                }
            }
            return null;
        };

        /**
         * Creates a KeyList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.KeyList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.KeyList} KeyList
         */
        KeyList.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.KeyList)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.KeyList: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.KeyList();
            if (object.keys) {
                if (!$Array.isArray(object.keys))
                    throw $TypeError(".proto.KeyList.keys: array expected");
                message.keys = $Array(object.keys.length);
                for (let i = 0; i < object.keys.length; ++i) {
                    if (!$util.isObject(object.keys[i]))
                        throw $TypeError(".proto.KeyList.keys: object expected");
                    message.keys[i] = $root.proto.Key.fromObject(object.keys[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a KeyList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.KeyList
         * @static
         * @param {proto.KeyList} message KeyList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        KeyList.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.keys = [];
            if (message.keys && message.keys.length) {
                object.keys = $Array(message.keys.length);
                for (let j = 0; j < message.keys.length; ++j)
                    object.keys[j] = $root.proto.Key.toObject(message.keys[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this KeyList to JSON.
         * @function toJSON
         * @memberof proto.KeyList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        KeyList.prototype.toJSON = function() {
            return KeyList.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for KeyList
         * @function getTypeUrl
         * @memberof proto.KeyList
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        KeyList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.KeyList";
        };

        return KeyList;
    })();

    proto.Signature = (function() {

        /**
         * Properties of a Signature.
         * @typedef {Object} proto.Signature.$Properties
         * @property {Uint8Array|null} [contract] Smart contract virtual signature (always length zero).
         * @property {Uint8Array|null} [ed25519] Ed25519 signature bytes.
         * @property {Uint8Array|null} [RSA_3072] RSA-3072 signature bytes.
         * @property {Uint8Array|null} [ECDSA_384] ECDSA p-384 signature bytes.
         * @property {proto.ThresholdSignature.$Properties|null} [thresholdSignature] A list of signatures for a single N-of-M threshold Key. This must be
         * a list of exactly M signatures, at least N of which are non-null.
         * @property {proto.SignatureList.$Properties|null} [signatureList] A list of M signatures, each corresponding to a Key in a KeyList
         * of the same length.
         * @property {"contract"|"ed25519"|"RSA_3072"|"ECDSA_384"|"thresholdSignature"|"signatureList"} [signature] Signature signature
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Signature.
         * @memberof proto
         * @interface ISignature
         * @augments proto.Signature.$Properties
         * @deprecated Use proto.Signature.$Properties instead.
         */

        /**
         * Narrowed shape of a Signature.
         * @typedef {{
         *   contract?: Uint8Array|null;
         *   ed25519?: Uint8Array|null;
         *   RSA_3072?: Uint8Array|null;
         *   ECDSA_384?: Uint8Array|null;
         *   thresholdSignature?: proto.ThresholdSignature.$Shape|null;
         *   signatureList?: proto.SignatureList.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ signature?: undefined; contract?: null; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; thresholdSignature?: null; signatureList?: null }|{ signature?: "contract"; contract: Uint8Array; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; thresholdSignature?: null; signatureList?: null }|{ signature?: "ed25519"; contract?: null; ed25519: Uint8Array; RSA_3072?: null; ECDSA_384?: null; thresholdSignature?: null; signatureList?: null }|{ signature?: "RSA_3072"; contract?: null; ed25519?: null; RSA_3072: Uint8Array; ECDSA_384?: null; thresholdSignature?: null; signatureList?: null }|{ signature?: "ECDSA_384"; contract?: null; ed25519?: null; RSA_3072?: null; ECDSA_384: Uint8Array; thresholdSignature?: null; signatureList?: null }|{ signature?: "thresholdSignature"; contract?: null; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; thresholdSignature: proto.ThresholdSignature.$Shape; signatureList?: null }|{ signature?: "signatureList"; contract?: null; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; thresholdSignature?: null; signatureList: proto.SignatureList.$Shape })
         * )} proto.Signature.$Shape
         */

        /**
         * Constructs a new Signature.
         * @memberof proto
         * @classdesc This message is deprecated and MUST NOT be used to communicate with
         * network nodes. It is retained here only for historical reasons.
         * 
         * Client software MUST NOT include this message in any request. <br/>
         * Compliant nodes SHALL NOT accept any request containing this message.
         * 
         * Please use the `SignaturePair` and `SignatureMap` messages instead of
         * this message.
         * @constructor
         * @param {proto.Signature.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Signature = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Smart contract virtual signature (always length zero).
         * @member {Uint8Array|null|undefined} contract
         * @memberof proto.Signature
         * @instance
         */
        Signature.prototype.contract = null;

        /**
         * Ed25519 signature bytes.
         * @member {Uint8Array|null|undefined} ed25519
         * @memberof proto.Signature
         * @instance
         */
        Signature.prototype.ed25519 = null;

        /**
         * RSA-3072 signature bytes.
         * @member {Uint8Array|null|undefined} RSA_3072
         * @memberof proto.Signature
         * @instance
         */
        Signature.prototype.RSA_3072 = null;

        /**
         * ECDSA p-384 signature bytes.
         * @member {Uint8Array|null|undefined} ECDSA_384
         * @memberof proto.Signature
         * @instance
         */
        Signature.prototype.ECDSA_384 = null;

        /**
         * A list of signatures for a single N-of-M threshold Key. This must be
         * a list of exactly M signatures, at least N of which are non-null.
         * @member {proto.ThresholdSignature.$Properties|null|undefined} thresholdSignature
         * @memberof proto.Signature
         * @instance
         */
        Signature.prototype.thresholdSignature = null;

        /**
         * A list of M signatures, each corresponding to a Key in a KeyList
         * of the same length.
         * @member {proto.SignatureList.$Properties|null|undefined} signatureList
         * @memberof proto.Signature
         * @instance
         */
        Signature.prototype.signatureList = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * Signature signature.
         * @member {"contract"|"ed25519"|"RSA_3072"|"ECDSA_384"|"thresholdSignature"|"signatureList"|undefined} signature
         * @memberof proto.Signature
         * @instance
         */
        $Object.defineProperty(Signature.prototype, "signature", {
            get: $util.oneOfGetter($oneOfFields = ["contract", "ed25519", "RSA_3072", "ECDSA_384", "thresholdSignature", "signatureList"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Signature instance using the specified properties.
         * @function create
         * @memberof proto.Signature
         * @static
         * @param {proto.Signature.$Properties=} [properties] Properties to set
         * @returns {proto.Signature} Signature instance
         * @type {{
         *   (properties: proto.Signature.$Shape): proto.Signature & proto.Signature.$Shape;
         *   (properties?: proto.Signature.$Properties): proto.Signature;
         * }}
         */
        Signature.create = function(properties) {
            return new Signature(properties);
        };

        /**
         * Encodes the specified Signature message. Does not implicitly {@link proto.Signature.verify|verify} messages.
         * @function encode
         * @memberof proto.Signature
         * @static
         * @param {proto.Signature.$Properties} message Signature message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Signature.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contract != null && $Object.hasOwnProperty.call(message, "contract"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.contract);
            if (message.ed25519 != null && $Object.hasOwnProperty.call(message, "ed25519"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.ed25519);
            if (message.RSA_3072 != null && $Object.hasOwnProperty.call(message, "RSA_3072"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.RSA_3072);
            if (message.ECDSA_384 != null && $Object.hasOwnProperty.call(message, "ECDSA_384"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.ECDSA_384);
            if (message.thresholdSignature != null && $Object.hasOwnProperty.call(message, "thresholdSignature"))
                $root.proto.ThresholdSignature.encode(message.thresholdSignature, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.signatureList != null && $Object.hasOwnProperty.call(message, "signatureList"))
                $root.proto.SignatureList.encode(message.signatureList, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Signature message, length delimited. Does not implicitly {@link proto.Signature.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Signature
         * @static
         * @param {proto.Signature.$Properties} message Signature message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Signature.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Signature message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Signature
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Signature & proto.Signature.$Shape} Signature
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Signature.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Signature();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.contract = reader.bytes();
                        message.signature = "contract";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.ed25519 = reader.bytes();
                        message.signature = "ed25519";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.RSA_3072 = reader.bytes();
                        message.signature = "RSA_3072";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.ECDSA_384 = reader.bytes();
                        message.signature = "ECDSA_384";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.thresholdSignature = $root.proto.ThresholdSignature.decode(reader, reader.uint32(), $undefined, _depth + 1, message.thresholdSignature);
                        message.signature = "thresholdSignature";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.signatureList = $root.proto.SignatureList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.signatureList);
                        message.signature = "signatureList";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Signature message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Signature
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Signature & proto.Signature.$Shape} Signature
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Signature.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Signature message.
         * @function verify
         * @memberof proto.Signature
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Signature.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.contract != null && $Object.hasOwnProperty.call(message, "contract")) {
                properties.signature = 1;
                if (!(message.contract && typeof message.contract.length === "number" || $util.isString(message.contract)))
                    return "contract: buffer expected";
            }
            if (message.ed25519 != null && $Object.hasOwnProperty.call(message, "ed25519")) {
                if (properties.signature === 1)
                    return "signature: multiple values";
                properties.signature = 1;
                if (!(message.ed25519 && typeof message.ed25519.length === "number" || $util.isString(message.ed25519)))
                    return "ed25519: buffer expected";
            }
            if (message.RSA_3072 != null && $Object.hasOwnProperty.call(message, "RSA_3072")) {
                if (properties.signature === 1)
                    return "signature: multiple values";
                properties.signature = 1;
                if (!(message.RSA_3072 && typeof message.RSA_3072.length === "number" || $util.isString(message.RSA_3072)))
                    return "RSA_3072: buffer expected";
            }
            if (message.ECDSA_384 != null && $Object.hasOwnProperty.call(message, "ECDSA_384")) {
                if (properties.signature === 1)
                    return "signature: multiple values";
                properties.signature = 1;
                if (!(message.ECDSA_384 && typeof message.ECDSA_384.length === "number" || $util.isString(message.ECDSA_384)))
                    return "ECDSA_384: buffer expected";
            }
            if (message.thresholdSignature != null && $Object.hasOwnProperty.call(message, "thresholdSignature")) {
                if (properties.signature === 1)
                    return "signature: multiple values";
                properties.signature = 1;
                {
                    let error = $root.proto.ThresholdSignature.verify(message.thresholdSignature, _depth + 1);
                    if (error)
                        return "thresholdSignature." + error;
                }
            }
            if (message.signatureList != null && $Object.hasOwnProperty.call(message, "signatureList")) {
                if (properties.signature === 1)
                    return "signature: multiple values";
                properties.signature = 1;
                {
                    let error = $root.proto.SignatureList.verify(message.signatureList, _depth + 1);
                    if (error)
                        return "signatureList." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Signature message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Signature
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Signature} Signature
         */
        Signature.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Signature)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Signature: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Signature();
            if (object.contract != null)
                if (typeof object.contract === "string")
                    $util.base64.decode(object.contract, message.contract = $util.newBuffer($util.base64.length(object.contract)), 0);
                else if (object.contract.length >= 0)
                    message.contract = object.contract;
            if (object.ed25519 != null)
                if (typeof object.ed25519 === "string")
                    $util.base64.decode(object.ed25519, message.ed25519 = $util.newBuffer($util.base64.length(object.ed25519)), 0);
                else if (object.ed25519.length >= 0)
                    message.ed25519 = object.ed25519;
            if (object.RSA_3072 != null)
                if (typeof object.RSA_3072 === "string")
                    $util.base64.decode(object.RSA_3072, message.RSA_3072 = $util.newBuffer($util.base64.length(object.RSA_3072)), 0);
                else if (object.RSA_3072.length >= 0)
                    message.RSA_3072 = object.RSA_3072;
            if (object.ECDSA_384 != null)
                if (typeof object.ECDSA_384 === "string")
                    $util.base64.decode(object.ECDSA_384, message.ECDSA_384 = $util.newBuffer($util.base64.length(object.ECDSA_384)), 0);
                else if (object.ECDSA_384.length >= 0)
                    message.ECDSA_384 = object.ECDSA_384;
            if (object.thresholdSignature != null) {
                if (!$util.isObject(object.thresholdSignature))
                    throw $TypeError(".proto.Signature.thresholdSignature: object expected");
                message.thresholdSignature = $root.proto.ThresholdSignature.fromObject(object.thresholdSignature, _depth + 1);
            }
            if (object.signatureList != null) {
                if (!$util.isObject(object.signatureList))
                    throw $TypeError(".proto.Signature.signatureList: object expected");
                message.signatureList = $root.proto.SignatureList.fromObject(object.signatureList, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Signature message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Signature
         * @static
         * @param {proto.Signature} message Signature
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Signature.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.contract != null && $Object.hasOwnProperty.call(message, "contract")) {
                object.contract = options.bytes === $String ? $util.base64.encode(message.contract, 0, message.contract.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.contract) : message.contract;
                if (options.oneofs)
                    object.signature = "contract";
            }
            if (message.ed25519 != null && $Object.hasOwnProperty.call(message, "ed25519")) {
                object.ed25519 = options.bytes === $String ? $util.base64.encode(message.ed25519, 0, message.ed25519.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ed25519) : message.ed25519;
                if (options.oneofs)
                    object.signature = "ed25519";
            }
            if (message.RSA_3072 != null && $Object.hasOwnProperty.call(message, "RSA_3072")) {
                object.RSA_3072 = options.bytes === $String ? $util.base64.encode(message.RSA_3072, 0, message.RSA_3072.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.RSA_3072) : message.RSA_3072;
                if (options.oneofs)
                    object.signature = "RSA_3072";
            }
            if (message.ECDSA_384 != null && $Object.hasOwnProperty.call(message, "ECDSA_384")) {
                object.ECDSA_384 = options.bytes === $String ? $util.base64.encode(message.ECDSA_384, 0, message.ECDSA_384.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ECDSA_384) : message.ECDSA_384;
                if (options.oneofs)
                    object.signature = "ECDSA_384";
            }
            if (message.thresholdSignature != null && $Object.hasOwnProperty.call(message, "thresholdSignature")) {
                object.thresholdSignature = $root.proto.ThresholdSignature.toObject(message.thresholdSignature, options, _depth + 1);
                if (options.oneofs)
                    object.signature = "thresholdSignature";
            }
            if (message.signatureList != null && $Object.hasOwnProperty.call(message, "signatureList")) {
                object.signatureList = $root.proto.SignatureList.toObject(message.signatureList, options, _depth + 1);
                if (options.oneofs)
                    object.signature = "signatureList";
            }
            return object;
        };

        /**
         * Converts this Signature to JSON.
         * @function toJSON
         * @memberof proto.Signature
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Signature.prototype.toJSON = function() {
            return Signature.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Signature
         * @function getTypeUrl
         * @memberof proto.Signature
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Signature.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Signature";
        };

        return Signature;
    })();

    proto.ThresholdSignature = (function() {

        /**
         * Properties of a ThresholdSignature.
         * @typedef {Object} proto.ThresholdSignature.$Properties
         * @property {proto.SignatureList.$Properties|null} [sigs] For an N-of-M threshold key, this is a list of M signatures, at least N
         * of which must be non-null.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ThresholdSignature.
         * @memberof proto
         * @interface IThresholdSignature
         * @augments proto.ThresholdSignature.$Properties
         * @deprecated Use proto.ThresholdSignature.$Properties instead.
         */

        /**
         * Shape of a ThresholdSignature.
         * @typedef {{
         *   sigs?: proto.SignatureList.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ThresholdSignature.$Shape
         */

        /**
         * Constructs a new ThresholdSignature.
         * @memberof proto
         * @classdesc This message is deprecated and MUST NOT be used to communicate with network
         * nodes. It is retained here only for historical reasons.
         * 
         * Client software MUST NOT include this message in any request. <br/>
         * Compliant nodes SHALL NOT accept any request containing this message.
         * 
         * Please use the `SignaturePair` and `SignatureMap` messages, in combination
         * with `ThresholdKey` keys, instead of this message.
         * @constructor
         * @param {proto.ThresholdSignature.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ThresholdSignature = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * For an N-of-M threshold key, this is a list of M signatures, at least N
         * of which must be non-null.
         * @member {proto.SignatureList.$Properties|null|undefined} sigs
         * @memberof proto.ThresholdSignature
         * @instance
         */
        ThresholdSignature.prototype.sigs = null;

        /**
         * Creates a new ThresholdSignature instance using the specified properties.
         * @function create
         * @memberof proto.ThresholdSignature
         * @static
         * @param {proto.ThresholdSignature.$Properties=} [properties] Properties to set
         * @returns {proto.ThresholdSignature} ThresholdSignature instance
         * @type {{
         *   (properties: proto.ThresholdSignature.$Shape): proto.ThresholdSignature & proto.ThresholdSignature.$Shape;
         *   (properties?: proto.ThresholdSignature.$Properties): proto.ThresholdSignature;
         * }}
         */
        ThresholdSignature.create = function(properties) {
            return new ThresholdSignature(properties);
        };

        /**
         * Encodes the specified ThresholdSignature message. Does not implicitly {@link proto.ThresholdSignature.verify|verify} messages.
         * @function encode
         * @memberof proto.ThresholdSignature
         * @static
         * @param {proto.ThresholdSignature.$Properties} message ThresholdSignature message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThresholdSignature.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sigs != null && $Object.hasOwnProperty.call(message, "sigs"))
                $root.proto.SignatureList.encode(message.sigs, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ThresholdSignature message, length delimited. Does not implicitly {@link proto.ThresholdSignature.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ThresholdSignature
         * @static
         * @param {proto.ThresholdSignature.$Properties} message ThresholdSignature message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThresholdSignature.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ThresholdSignature message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ThresholdSignature
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ThresholdSignature & proto.ThresholdSignature.$Shape} ThresholdSignature
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThresholdSignature.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ThresholdSignature();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.sigs = $root.proto.SignatureList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.sigs);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ThresholdSignature message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ThresholdSignature
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ThresholdSignature & proto.ThresholdSignature.$Shape} ThresholdSignature
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThresholdSignature.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ThresholdSignature message.
         * @function verify
         * @memberof proto.ThresholdSignature
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ThresholdSignature.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sigs != null && $Object.hasOwnProperty.call(message, "sigs")) {
                let error = $root.proto.SignatureList.verify(message.sigs, _depth + 1);
                if (error)
                    return "sigs." + error;
            }
            return null;
        };

        /**
         * Creates a ThresholdSignature message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ThresholdSignature
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ThresholdSignature} ThresholdSignature
         */
        ThresholdSignature.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ThresholdSignature)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ThresholdSignature: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ThresholdSignature();
            if (object.sigs != null) {
                if (!$util.isObject(object.sigs))
                    throw $TypeError(".proto.ThresholdSignature.sigs: object expected");
                message.sigs = $root.proto.SignatureList.fromObject(object.sigs, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ThresholdSignature message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ThresholdSignature
         * @static
         * @param {proto.ThresholdSignature} message ThresholdSignature
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThresholdSignature.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.sigs = null;
            if (message.sigs != null && $Object.hasOwnProperty.call(message, "sigs"))
                object.sigs = $root.proto.SignatureList.toObject(message.sigs, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ThresholdSignature to JSON.
         * @function toJSON
         * @memberof proto.ThresholdSignature
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThresholdSignature.prototype.toJSON = function() {
            return ThresholdSignature.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ThresholdSignature
         * @function getTypeUrl
         * @memberof proto.ThresholdSignature
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ThresholdSignature.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ThresholdSignature";
        };

        return ThresholdSignature;
    })();

    proto.SignatureList = (function() {

        /**
         * Properties of a SignatureList.
         * @typedef {Object} proto.SignatureList.$Properties
         * @property {Array.<proto.Signature.$Properties>|null} [sigs] Each signature corresponds to a Key in the KeyList.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SignatureList.
         * @memberof proto
         * @interface ISignatureList
         * @augments proto.SignatureList.$Properties
         * @deprecated Use proto.SignatureList.$Properties instead.
         */

        /**
         * Shape of a SignatureList.
         * @typedef {{
         *   sigs?: Array.<proto.Signature.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.SignatureList.$Shape
         */

        /**
         * Constructs a new SignatureList.
         * @memberof proto
         * @classdesc This message is deprecated and MUST NOT be used to communicate with network
         * nodes. It is retained here only for historical reasons.
         * 
         * Client software MUST NOT include this message in any request. <br/>
         * Compliant nodes SHALL NOT accept any request containing this message.
         * 
         * Please use the `SignaturePair` and `SignatureMap` messages instead of
         * this message.
         * @constructor
         * @param {proto.SignatureList.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SignatureList = function (properties) {
            this.sigs = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Each signature corresponds to a Key in the KeyList.
         * @member {Array.<proto.Signature.$Properties>} sigs
         * @memberof proto.SignatureList
         * @instance
         */
        SignatureList.prototype.sigs = $util.emptyArray;

        /**
         * Creates a new SignatureList instance using the specified properties.
         * @function create
         * @memberof proto.SignatureList
         * @static
         * @param {proto.SignatureList.$Properties=} [properties] Properties to set
         * @returns {proto.SignatureList} SignatureList instance
         * @type {{
         *   (properties: proto.SignatureList.$Shape): proto.SignatureList & proto.SignatureList.$Shape;
         *   (properties?: proto.SignatureList.$Properties): proto.SignatureList;
         * }}
         */
        SignatureList.create = function(properties) {
            return new SignatureList(properties);
        };

        /**
         * Encodes the specified SignatureList message. Does not implicitly {@link proto.SignatureList.verify|verify} messages.
         * @function encode
         * @memberof proto.SignatureList
         * @static
         * @param {proto.SignatureList.$Properties} message SignatureList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignatureList.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sigs != null && message.sigs.length)
                for (let i = 0; i < message.sigs.length; ++i)
                    $root.proto.Signature.encode(message.sigs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignatureList message, length delimited. Does not implicitly {@link proto.SignatureList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SignatureList
         * @static
         * @param {proto.SignatureList.$Properties} message SignatureList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignatureList.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SignatureList message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SignatureList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SignatureList & proto.SignatureList.$Shape} SignatureList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignatureList.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SignatureList();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.sigs && message.sigs.length))
                            message.sigs = [];
                        message.sigs.push($root.proto.Signature.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignatureList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SignatureList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SignatureList & proto.SignatureList.$Shape} SignatureList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignatureList.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignatureList message.
         * @function verify
         * @memberof proto.SignatureList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignatureList.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sigs != null && $Object.hasOwnProperty.call(message, "sigs")) {
                if (!$Array.isArray(message.sigs))
                    return "sigs: array expected";
                for (let i = 0; i < message.sigs.length; ++i) {
                    let error = $root.proto.Signature.verify(message.sigs[i], _depth + 1);
                    if (error)
                        return "sigs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SignatureList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SignatureList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SignatureList} SignatureList
         */
        SignatureList.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SignatureList)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SignatureList: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SignatureList();
            if (object.sigs) {
                if (!$Array.isArray(object.sigs))
                    throw $TypeError(".proto.SignatureList.sigs: array expected");
                message.sigs = $Array(object.sigs.length);
                for (let i = 0; i < object.sigs.length; ++i) {
                    if (!$util.isObject(object.sigs[i]))
                        throw $TypeError(".proto.SignatureList.sigs: object expected");
                    message.sigs[i] = $root.proto.Signature.fromObject(object.sigs[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SignatureList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SignatureList
         * @static
         * @param {proto.SignatureList} message SignatureList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignatureList.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.sigs = [];
            if (message.sigs && message.sigs.length) {
                object.sigs = $Array(message.sigs.length);
                for (let j = 0; j < message.sigs.length; ++j)
                    object.sigs[j] = $root.proto.Signature.toObject(message.sigs[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this SignatureList to JSON.
         * @function toJSON
         * @memberof proto.SignatureList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignatureList.prototype.toJSON = function() {
            return SignatureList.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignatureList
         * @function getTypeUrl
         * @memberof proto.SignatureList
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignatureList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SignatureList";
        };

        return SignatureList;
    })();

    proto.SignaturePair = (function() {

        /**
         * Properties of a SignaturePair.
         * @typedef {Object} proto.SignaturePair.$Properties
         * @property {Uint8Array|null} [pubKeyPrefix] Prefix bytes of the public key.
         * <p>
         * The client may use any number of bytes from zero to the whole length of
         * the public key for pubKeyPrefix. If zero bytes are used, then it MUST be
         * true that only one cryptographic key is required to sign the associated
         * transaction.<br/>
         * If the `pubKeyPrefix` is 0 bytes and more than a single cryptographic
         * key is required to sign the transaction, the request SHALL resolve to
         * `INVALID_SIGNATURE`.
         * <blockquote>Important Note<blockquote>
         * In the special case that a signature is provided to authorize a
         * precompiled contract, the `pubKeyPrefix` MUST contain the _entire public
         * key_.<br/>
         * That is, if the key is an Ed25519 key, the `pubKeyPrefix` MUST be
         * 32 bytes long and contain the full public key bytes.<br/>
         * If the key is an ECDSA(secp256k1) key, the `pubKeyPrefix` MUST be
         * 33 bytes long and contain the full _compressed_ form of the public key.
         * </blockquote></blockquote>
         * <p>
         * <dl><dt>Purpose</dt>
         * <dd>The `pubKeyPrefix` exists to save cost. A signed transaction with
         * shorter prefixes will have fewer bytes, and so will have a lower
         * transaction fee.
         * The prefixes, however, MUST be long enough to distinguish between all
         * of the public keys that might be signing the transaction. Therefore,
         * software signing a transaction SHOULD evaluate which keys might possibly
         * be required to sign a transaction, and ensure that the shortest prefix
         * that is sufficient to unambiguously identify the correct key is used.
         * </dd></dl>
         * @property {Uint8Array|null} [contract] A smart contract virtual signature.
         * <p>
         * This value MUST be length zero, if set.
         * @property {Uint8Array|null} [ed25519] An Ed25519 signature.
         * @property {Uint8Array|null} [RSA_3072] This option is not supported.<br/>
         * A RSA-3072 signature.
         * @property {Uint8Array|null} [ECDSA_384] This option is not supported.<br/>
         * ECDSA p-384 signature.
         * @property {Uint8Array|null} [ECDSASecp256k1] An ECDSA(secp256k1) signature.
         * @property {"contract"|"ed25519"|"RSA_3072"|"ECDSA_384"|"ECDSASecp256k1"} [signature] SignaturePair signature
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SignaturePair.
         * @memberof proto
         * @interface ISignaturePair
         * @augments proto.SignaturePair.$Properties
         * @deprecated Use proto.SignaturePair.$Properties instead.
         */

        /**
         * Narrowed shape of a SignaturePair.
         * @typedef {{
         *   pubKeyPrefix?: Uint8Array|null;
         *   contract?: Uint8Array|null;
         *   ed25519?: Uint8Array|null;
         *   RSA_3072?: Uint8Array|null;
         *   ECDSA_384?: Uint8Array|null;
         *   ECDSASecp256k1?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ signature?: undefined; contract?: null; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; ECDSASecp256k1?: null }|{ signature?: "contract"; contract: Uint8Array; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; ECDSASecp256k1?: null }|{ signature?: "ed25519"; contract?: null; ed25519: Uint8Array; RSA_3072?: null; ECDSA_384?: null; ECDSASecp256k1?: null }|{ signature?: "RSA_3072"; contract?: null; ed25519?: null; RSA_3072: Uint8Array; ECDSA_384?: null; ECDSASecp256k1?: null }|{ signature?: "ECDSA_384"; contract?: null; ed25519?: null; RSA_3072?: null; ECDSA_384: Uint8Array; ECDSASecp256k1?: null }|{ signature?: "ECDSASecp256k1"; contract?: null; ed25519?: null; RSA_3072?: null; ECDSA_384?: null; ECDSASecp256k1: Uint8Array })
         * )} proto.SignaturePair.$Shape
         */

        /**
         * Constructs a new SignaturePair.
         * @memberof proto
         * @classdesc A public key and signature pair.<br/>
         * Only Ed25519 and ECDSA(secp256k1) keys and signatures are currently supported
         * as cryptographic (non-implied) signatures.
         * @constructor
         * @param {proto.SignaturePair.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SignaturePair = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Prefix bytes of the public key.
         * <p>
         * The client may use any number of bytes from zero to the whole length of
         * the public key for pubKeyPrefix. If zero bytes are used, then it MUST be
         * true that only one cryptographic key is required to sign the associated
         * transaction.<br/>
         * If the `pubKeyPrefix` is 0 bytes and more than a single cryptographic
         * key is required to sign the transaction, the request SHALL resolve to
         * `INVALID_SIGNATURE`.
         * <blockquote>Important Note<blockquote>
         * In the special case that a signature is provided to authorize a
         * precompiled contract, the `pubKeyPrefix` MUST contain the _entire public
         * key_.<br/>
         * That is, if the key is an Ed25519 key, the `pubKeyPrefix` MUST be
         * 32 bytes long and contain the full public key bytes.<br/>
         * If the key is an ECDSA(secp256k1) key, the `pubKeyPrefix` MUST be
         * 33 bytes long and contain the full _compressed_ form of the public key.
         * </blockquote></blockquote>
         * <p>
         * <dl><dt>Purpose</dt>
         * <dd>The `pubKeyPrefix` exists to save cost. A signed transaction with
         * shorter prefixes will have fewer bytes, and so will have a lower
         * transaction fee.
         * The prefixes, however, MUST be long enough to distinguish between all
         * of the public keys that might be signing the transaction. Therefore,
         * software signing a transaction SHOULD evaluate which keys might possibly
         * be required to sign a transaction, and ensure that the shortest prefix
         * that is sufficient to unambiguously identify the correct key is used.
         * </dd></dl>
         * @member {Uint8Array} pubKeyPrefix
         * @memberof proto.SignaturePair
         * @instance
         */
        SignaturePair.prototype.pubKeyPrefix = $util.newBuffer([]);

        /**
         * A smart contract virtual signature.
         * <p>
         * This value MUST be length zero, if set.
         * @member {Uint8Array|null|undefined} contract
         * @memberof proto.SignaturePair
         * @instance
         */
        SignaturePair.prototype.contract = null;

        /**
         * An Ed25519 signature.
         * @member {Uint8Array|null|undefined} ed25519
         * @memberof proto.SignaturePair
         * @instance
         */
        SignaturePair.prototype.ed25519 = null;

        /**
         * This option is not supported.<br/>
         * A RSA-3072 signature.
         * @member {Uint8Array|null|undefined} RSA_3072
         * @memberof proto.SignaturePair
         * @instance
         */
        SignaturePair.prototype.RSA_3072 = null;

        /**
         * This option is not supported.<br/>
         * ECDSA p-384 signature.
         * @member {Uint8Array|null|undefined} ECDSA_384
         * @memberof proto.SignaturePair
         * @instance
         */
        SignaturePair.prototype.ECDSA_384 = null;

        /**
         * An ECDSA(secp256k1) signature.
         * @member {Uint8Array|null|undefined} ECDSASecp256k1
         * @memberof proto.SignaturePair
         * @instance
         */
        SignaturePair.prototype.ECDSASecp256k1 = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * SignaturePair signature.
         * @member {"contract"|"ed25519"|"RSA_3072"|"ECDSA_384"|"ECDSASecp256k1"|undefined} signature
         * @memberof proto.SignaturePair
         * @instance
         */
        $Object.defineProperty(SignaturePair.prototype, "signature", {
            get: $util.oneOfGetter($oneOfFields = ["contract", "ed25519", "RSA_3072", "ECDSA_384", "ECDSASecp256k1"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SignaturePair instance using the specified properties.
         * @function create
         * @memberof proto.SignaturePair
         * @static
         * @param {proto.SignaturePair.$Properties=} [properties] Properties to set
         * @returns {proto.SignaturePair} SignaturePair instance
         * @type {{
         *   (properties: proto.SignaturePair.$Shape): proto.SignaturePair & proto.SignaturePair.$Shape;
         *   (properties?: proto.SignaturePair.$Properties): proto.SignaturePair;
         * }}
         */
        SignaturePair.create = function(properties) {
            return new SignaturePair(properties);
        };

        /**
         * Encodes the specified SignaturePair message. Does not implicitly {@link proto.SignaturePair.verify|verify} messages.
         * @function encode
         * @memberof proto.SignaturePair
         * @static
         * @param {proto.SignaturePair.$Properties} message SignaturePair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignaturePair.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pubKeyPrefix != null && $Object.hasOwnProperty.call(message, "pubKeyPrefix") && message.pubKeyPrefix.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.pubKeyPrefix);
            if (message.contract != null && $Object.hasOwnProperty.call(message, "contract"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.contract);
            if (message.ed25519 != null && $Object.hasOwnProperty.call(message, "ed25519"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.ed25519);
            if (message.RSA_3072 != null && $Object.hasOwnProperty.call(message, "RSA_3072"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.RSA_3072);
            if (message.ECDSA_384 != null && $Object.hasOwnProperty.call(message, "ECDSA_384"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.ECDSA_384);
            if (message.ECDSASecp256k1 != null && $Object.hasOwnProperty.call(message, "ECDSASecp256k1"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.ECDSASecp256k1);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignaturePair message, length delimited. Does not implicitly {@link proto.SignaturePair.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SignaturePair
         * @static
         * @param {proto.SignaturePair.$Properties} message SignaturePair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignaturePair.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SignaturePair message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SignaturePair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SignaturePair & proto.SignaturePair.$Shape} SignaturePair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignaturePair.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SignaturePair();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pubKeyPrefix = value;
                        else
                            delete message.pubKeyPrefix;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.contract = reader.bytes();
                        message.signature = "contract";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.ed25519 = reader.bytes();
                        message.signature = "ed25519";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.RSA_3072 = reader.bytes();
                        message.signature = "RSA_3072";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.ECDSA_384 = reader.bytes();
                        message.signature = "ECDSA_384";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.ECDSASecp256k1 = reader.bytes();
                        message.signature = "ECDSASecp256k1";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignaturePair message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SignaturePair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SignaturePair & proto.SignaturePair.$Shape} SignaturePair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignaturePair.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignaturePair message.
         * @function verify
         * @memberof proto.SignaturePair
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignaturePair.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.pubKeyPrefix != null && $Object.hasOwnProperty.call(message, "pubKeyPrefix"))
                if (!(message.pubKeyPrefix && typeof message.pubKeyPrefix.length === "number" || $util.isString(message.pubKeyPrefix)))
                    return "pubKeyPrefix: buffer expected";
            if (message.contract != null && $Object.hasOwnProperty.call(message, "contract")) {
                properties.signature = 1;
                if (!(message.contract && typeof message.contract.length === "number" || $util.isString(message.contract)))
                    return "contract: buffer expected";
            }
            if (message.ed25519 != null && $Object.hasOwnProperty.call(message, "ed25519")) {
                if (properties.signature === 1)
                    return "signature: multiple values";
                properties.signature = 1;
                if (!(message.ed25519 && typeof message.ed25519.length === "number" || $util.isString(message.ed25519)))
                    return "ed25519: buffer expected";
            }
            if (message.RSA_3072 != null && $Object.hasOwnProperty.call(message, "RSA_3072")) {
                if (properties.signature === 1)
                    return "signature: multiple values";
                properties.signature = 1;
                if (!(message.RSA_3072 && typeof message.RSA_3072.length === "number" || $util.isString(message.RSA_3072)))
                    return "RSA_3072: buffer expected";
            }
            if (message.ECDSA_384 != null && $Object.hasOwnProperty.call(message, "ECDSA_384")) {
                if (properties.signature === 1)
                    return "signature: multiple values";
                properties.signature = 1;
                if (!(message.ECDSA_384 && typeof message.ECDSA_384.length === "number" || $util.isString(message.ECDSA_384)))
                    return "ECDSA_384: buffer expected";
            }
            if (message.ECDSASecp256k1 != null && $Object.hasOwnProperty.call(message, "ECDSASecp256k1")) {
                if (properties.signature === 1)
                    return "signature: multiple values";
                properties.signature = 1;
                if (!(message.ECDSASecp256k1 && typeof message.ECDSASecp256k1.length === "number" || $util.isString(message.ECDSASecp256k1)))
                    return "ECDSASecp256k1: buffer expected";
            }
            return null;
        };

        /**
         * Creates a SignaturePair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SignaturePair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SignaturePair} SignaturePair
         */
        SignaturePair.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SignaturePair)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SignaturePair: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SignaturePair();
            if (object.pubKeyPrefix != null)
                if (object.pubKeyPrefix.length)
                    if (typeof object.pubKeyPrefix === "string")
                        $util.base64.decode(object.pubKeyPrefix, message.pubKeyPrefix = $util.newBuffer($util.base64.length(object.pubKeyPrefix)), 0);
                    else if (object.pubKeyPrefix.length >= 0)
                        message.pubKeyPrefix = object.pubKeyPrefix;
            if (object.contract != null)
                if (typeof object.contract === "string")
                    $util.base64.decode(object.contract, message.contract = $util.newBuffer($util.base64.length(object.contract)), 0);
                else if (object.contract.length >= 0)
                    message.contract = object.contract;
            if (object.ed25519 != null)
                if (typeof object.ed25519 === "string")
                    $util.base64.decode(object.ed25519, message.ed25519 = $util.newBuffer($util.base64.length(object.ed25519)), 0);
                else if (object.ed25519.length >= 0)
                    message.ed25519 = object.ed25519;
            if (object.RSA_3072 != null)
                if (typeof object.RSA_3072 === "string")
                    $util.base64.decode(object.RSA_3072, message.RSA_3072 = $util.newBuffer($util.base64.length(object.RSA_3072)), 0);
                else if (object.RSA_3072.length >= 0)
                    message.RSA_3072 = object.RSA_3072;
            if (object.ECDSA_384 != null)
                if (typeof object.ECDSA_384 === "string")
                    $util.base64.decode(object.ECDSA_384, message.ECDSA_384 = $util.newBuffer($util.base64.length(object.ECDSA_384)), 0);
                else if (object.ECDSA_384.length >= 0)
                    message.ECDSA_384 = object.ECDSA_384;
            if (object.ECDSASecp256k1 != null)
                if (typeof object.ECDSASecp256k1 === "string")
                    $util.base64.decode(object.ECDSASecp256k1, message.ECDSASecp256k1 = $util.newBuffer($util.base64.length(object.ECDSASecp256k1)), 0);
                else if (object.ECDSASecp256k1.length >= 0)
                    message.ECDSASecp256k1 = object.ECDSASecp256k1;
            return message;
        };

        /**
         * Creates a plain object from a SignaturePair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SignaturePair
         * @static
         * @param {proto.SignaturePair} message SignaturePair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignaturePair.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.pubKeyPrefix = "";
                else {
                    object.pubKeyPrefix = [];
                    if (options.bytes !== $Array)
                        object.pubKeyPrefix = $util.newBuffer(object.pubKeyPrefix);
                }
            if (message.pubKeyPrefix != null && $Object.hasOwnProperty.call(message, "pubKeyPrefix"))
                object.pubKeyPrefix = options.bytes === $String ? $util.base64.encode(message.pubKeyPrefix, 0, message.pubKeyPrefix.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pubKeyPrefix) : message.pubKeyPrefix;
            if (message.contract != null && $Object.hasOwnProperty.call(message, "contract")) {
                object.contract = options.bytes === $String ? $util.base64.encode(message.contract, 0, message.contract.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.contract) : message.contract;
                if (options.oneofs)
                    object.signature = "contract";
            }
            if (message.ed25519 != null && $Object.hasOwnProperty.call(message, "ed25519")) {
                object.ed25519 = options.bytes === $String ? $util.base64.encode(message.ed25519, 0, message.ed25519.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ed25519) : message.ed25519;
                if (options.oneofs)
                    object.signature = "ed25519";
            }
            if (message.RSA_3072 != null && $Object.hasOwnProperty.call(message, "RSA_3072")) {
                object.RSA_3072 = options.bytes === $String ? $util.base64.encode(message.RSA_3072, 0, message.RSA_3072.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.RSA_3072) : message.RSA_3072;
                if (options.oneofs)
                    object.signature = "RSA_3072";
            }
            if (message.ECDSA_384 != null && $Object.hasOwnProperty.call(message, "ECDSA_384")) {
                object.ECDSA_384 = options.bytes === $String ? $util.base64.encode(message.ECDSA_384, 0, message.ECDSA_384.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ECDSA_384) : message.ECDSA_384;
                if (options.oneofs)
                    object.signature = "ECDSA_384";
            }
            if (message.ECDSASecp256k1 != null && $Object.hasOwnProperty.call(message, "ECDSASecp256k1")) {
                object.ECDSASecp256k1 = options.bytes === $String ? $util.base64.encode(message.ECDSASecp256k1, 0, message.ECDSASecp256k1.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ECDSASecp256k1) : message.ECDSASecp256k1;
                if (options.oneofs)
                    object.signature = "ECDSASecp256k1";
            }
            return object;
        };

        /**
         * Converts this SignaturePair to JSON.
         * @function toJSON
         * @memberof proto.SignaturePair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignaturePair.prototype.toJSON = function() {
            return SignaturePair.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignaturePair
         * @function getTypeUrl
         * @memberof proto.SignaturePair
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignaturePair.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SignaturePair";
        };

        return SignaturePair;
    })();

    proto.SignatureMap = (function() {

        /**
         * Properties of a SignatureMap.
         * @typedef {Object} proto.SignatureMap.$Properties
         * @property {Array.<proto.SignaturePair.$Properties>|null} [sigPair] A list of signature pairs for a specific transaction.<br/>
         * Each signature pair represents a single cryptographic (`primitive`)
         * public key identified by a "prefix" value and the cryptographic
         * signature produced for that key.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SignatureMap.
         * @memberof proto
         * @interface ISignatureMap
         * @augments proto.SignatureMap.$Properties
         * @deprecated Use proto.SignatureMap.$Properties instead.
         */

        /**
         * Shape of a SignatureMap.
         * @typedef {{
         *   sigPair?: Array.<proto.SignaturePair.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.SignatureMap.$Shape
         */

        /**
         * Constructs a new SignatureMap.
         * @memberof proto
         * @classdesc A set of signatures corresponding to every unique public key that
         * signed a given transaction.
         * 
         * If any public key matches more than one prefix in the signature map,
         * the transaction containing that map SHALL fail immediately with the
         * response code `KEY_PREFIX_MISMATCH`.
         * @constructor
         * @param {proto.SignatureMap.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SignatureMap = function (properties) {
            this.sigPair = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of signature pairs for a specific transaction.<br/>
         * Each signature pair represents a single cryptographic (`primitive`)
         * public key identified by a "prefix" value and the cryptographic
         * signature produced for that key.
         * @member {Array.<proto.SignaturePair.$Properties>} sigPair
         * @memberof proto.SignatureMap
         * @instance
         */
        SignatureMap.prototype.sigPair = $util.emptyArray;

        /**
         * Creates a new SignatureMap instance using the specified properties.
         * @function create
         * @memberof proto.SignatureMap
         * @static
         * @param {proto.SignatureMap.$Properties=} [properties] Properties to set
         * @returns {proto.SignatureMap} SignatureMap instance
         * @type {{
         *   (properties: proto.SignatureMap.$Shape): proto.SignatureMap & proto.SignatureMap.$Shape;
         *   (properties?: proto.SignatureMap.$Properties): proto.SignatureMap;
         * }}
         */
        SignatureMap.create = function(properties) {
            return new SignatureMap(properties);
        };

        /**
         * Encodes the specified SignatureMap message. Does not implicitly {@link proto.SignatureMap.verify|verify} messages.
         * @function encode
         * @memberof proto.SignatureMap
         * @static
         * @param {proto.SignatureMap.$Properties} message SignatureMap message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignatureMap.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sigPair != null && message.sigPair.length)
                for (let i = 0; i < message.sigPair.length; ++i)
                    $root.proto.SignaturePair.encode(message.sigPair[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignatureMap message, length delimited. Does not implicitly {@link proto.SignatureMap.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SignatureMap
         * @static
         * @param {proto.SignatureMap.$Properties} message SignatureMap message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignatureMap.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SignatureMap message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SignatureMap
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SignatureMap & proto.SignatureMap.$Shape} SignatureMap
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignatureMap.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SignatureMap();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.sigPair && message.sigPair.length))
                            message.sigPair = [];
                        message.sigPair.push($root.proto.SignaturePair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignatureMap message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SignatureMap
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SignatureMap & proto.SignatureMap.$Shape} SignatureMap
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignatureMap.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignatureMap message.
         * @function verify
         * @memberof proto.SignatureMap
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignatureMap.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sigPair != null && $Object.hasOwnProperty.call(message, "sigPair")) {
                if (!$Array.isArray(message.sigPair))
                    return "sigPair: array expected";
                for (let i = 0; i < message.sigPair.length; ++i) {
                    let error = $root.proto.SignaturePair.verify(message.sigPair[i], _depth + 1);
                    if (error)
                        return "sigPair." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SignatureMap message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SignatureMap
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SignatureMap} SignatureMap
         */
        SignatureMap.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SignatureMap)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SignatureMap: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SignatureMap();
            if (object.sigPair) {
                if (!$Array.isArray(object.sigPair))
                    throw $TypeError(".proto.SignatureMap.sigPair: array expected");
                message.sigPair = $Array(object.sigPair.length);
                for (let i = 0; i < object.sigPair.length; ++i) {
                    if (!$util.isObject(object.sigPair[i]))
                        throw $TypeError(".proto.SignatureMap.sigPair: object expected");
                    message.sigPair[i] = $root.proto.SignaturePair.fromObject(object.sigPair[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SignatureMap message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SignatureMap
         * @static
         * @param {proto.SignatureMap} message SignatureMap
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignatureMap.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.sigPair = [];
            if (message.sigPair && message.sigPair.length) {
                object.sigPair = $Array(message.sigPair.length);
                for (let j = 0; j < message.sigPair.length; ++j)
                    object.sigPair[j] = $root.proto.SignaturePair.toObject(message.sigPair[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this SignatureMap to JSON.
         * @function toJSON
         * @memberof proto.SignatureMap
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignatureMap.prototype.toJSON = function() {
            return SignatureMap.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignatureMap
         * @function getTypeUrl
         * @memberof proto.SignatureMap
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignatureMap.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SignatureMap";
        };

        return SignatureMap;
    })();

    /**
     * The transactions and queries supported by Hedera Hashgraph.
     * @name proto.HederaFunctionality
     * @enum {number}
     * @property {number} NONE=0 Unused - The first value is unused because this default value is
     * ambiguous with an "unset" value and therefore should not be used.
     * @property {number} CryptoTransfer=1 Transfer tokens among accounts.
     * @property {number} CryptoUpdate=2 Update an account.
     * @property {number} CryptoDelete=3 Delete an account.
     * @property {number} CryptoAddLiveHash=4 Add a livehash to an account
     * @property {number} CryptoDeleteLiveHash=5 Delete a livehash from an account
     * @property {number} ContractCall=6 Execute a smart contract call.
     * @property {number} ContractCreate=7 Create a smart contract.
     * @property {number} ContractUpdate=8 Update a smart contract.
     * @property {number} FileCreate=9 Create a "file" stored in the ledger.
     * @property {number} FileAppend=10 Append data to a "file" stored in the ledger.
     * @property {number} FileUpdate=11 Update a "file" stored in the ledger.
     * @property {number} FileDelete=12 Delete a "file" stored in the ledger.
     * @property {number} CryptoGetAccountBalance=13 Get the balance for an account.
     * @property {number} CryptoGetAccountRecords=14 Get a full account record.
     * @property {number} CryptoGetInfo=15 Get information about a token.
     * @property {number} ContractCallLocal=16 Execute a local smart contract call.<br/>
     * Used by contracts to call other contracts.
     * @property {number} ContractGetInfo=17 Get information about a smart contract.
     * @property {number} ContractGetBytecode=18 Get the compiled bytecode that implements a smart contract.
     * @property {number} GetBySolidityID=19 Get a smart contract record by reference to the solidity ID.
     * @property {number} GetByKey=20 Get a smart contract by reference to the contract key.
     * @property {number} CryptoGetLiveHash=21 Get the live hash for an account
     * @property {number} CryptoGetStakers=22 Get the accounts proxy staking to a given account.
     * @property {number} FileGetContents=23 Get the contents of a "file" stored in the ledger.
     * @property {number} FileGetInfo=24 Get the metadata for a "file" stored in the ledger.
     * @property {number} TransactionGetRecord=25 Get transaction record(s) for a specified transaction ID.
     * @property {number} ContractGetRecords=26 Get all transaction records for a specified contract ID in
     * the past 24 hours.<br/>
     * deprecated since version 0.9.0
     * @property {number} CryptoCreate=27 Create a new account
     * @property {number} SystemDelete=28 Delete a "system" "file" stored in the ledger.<br/>
     * "System" files are files with special purpose and ID values within a
     * specific range.<br/>
     * These files require additional controls and can only be deleted when
     * authorized by accounts with elevated privilege.
     * @property {number} SystemUndelete=29 Undo the delete of a "system" "file" stored in the ledger.<br/>
     * "System" files are files with special purpose and ID values within a
     * specific range.<br/>
     * These files require additional controls and can only be deleted when
     * authorized by accounts with elevated privilege. This operation allows
     * such files to be restored, within a reasonable timeframe, if
     * deleted improperly.
     * @property {number} ContractDelete=30 Delete a smart contract
     * @property {number} Freeze=31 Stop all processing and "freeze" the entire network.<br/>
     * This is generally sent immediately prior to upgrading the network.<br/>
     * After processing this transactions all nodes enter a quiescent state.
     * @property {number} CreateTransactionRecord=32 Create a Transaction Record.<br/>
     * This appears to be purely internal and unused.
     * @property {number} CryptoAccountAutoRenew=33 Auto-renew an account.<br/>
     * This is used for internal fee calculations.
     * @property {number} ContractAutoRenew=34 Auto-renew a smart contract.<br/>
     * This is used for internal fee calculations.
     * @property {number} GetVersionInfo=35 Get version information for the ledger.<br/>
     * This returns a the version of the software currently running the network
     * for both the protocol buffers and the network services (node).
     * @property {number} TransactionGetReceipt=36 Get a receipt for a specified transaction ID.
     * @property {number} ConsensusCreateTopic=50 Create a topic for the Hedera Consensus Service (HCS).
     * @property {number} ConsensusUpdateTopic=51 Update an HCS topic.
     * @property {number} ConsensusDeleteTopic=52 Delete an HCS topic.
     * @property {number} ConsensusGetTopicInfo=53 Get metadata (information) for an HCS topic.
     * @property {number} ConsensusSubmitMessage=54 Publish a message to an HCS topic.
     * @property {number} UncheckedSubmit=55 Submit a transaction, bypassing intake checking.
     * Only enabled in local-mode.
     * @property {number} TokenCreate=56 Create a token for the Hedera Token Service (HTS).
     * @property {number} TokenGetInfo=58 Get metadata (information) for an HTS token.
     * @property {number} TokenFreezeAccount=59 Freeze a specific account with respect to a specific HTS token.
     * <p>
     * Once this transaction completes that account CANNOT send or receive
     * the specified token.
     * @property {number} TokenUnfreezeAccount=60 Remove a "freeze" from an account with respect to a specific HTS token.
     * @property {number} TokenGrantKycToAccount=61 Grant KYC status to an account for a specific HTS token.
     * @property {number} TokenRevokeKycFromAccount=62 Revoke KYC status from an account for a specific HTS token.
     * @property {number} TokenDelete=63 Delete a specific HTS token.
     * @property {number} TokenUpdate=64 Update a specific HTS token.
     * @property {number} TokenMint=65 Mint HTS token amounts to the treasury account for that token.
     * @property {number} TokenBurn=66 Burn HTS token amounts from the treasury account for that token.
     * @property {number} TokenAccountWipe=67 Wipe all amounts for a specific HTS token from a specified account.
     * @property {number} TokenAssociateToAccount=68 Associate a specific HTS token to an account.
     * @property {number} TokenDissociateFromAccount=69 Dissociate a specific HTS token from an account.
     * @property {number} ScheduleCreate=70 Create a scheduled transaction
     * @property {number} ScheduleDelete=71 Delete a scheduled transaction
     * @property {number} ScheduleSign=72 Sign a scheduled transaction
     * @property {number} ScheduleGetInfo=73 Get metadata (information) for a scheduled transaction
     * @property {number} TokenGetAccountNftInfos=74 Get NFT metadata (information) for a range of NFTs associated to a
     * specific non-fungible/unique HTS token and owned by a specific account.
     * @property {number} TokenGetNftInfo=75 Get metadata (information) for a specific NFT identified by token and
     * serial number.
     * @property {number} TokenGetNftInfos=76 Get NFT metadata (information) for a range of NFTs associated to a
     * specific non-fungible/unique HTS token.
     * @property {number} TokenFeeScheduleUpdate=77 Update a token's custom fee schedule.
     * <p>
     * If a transaction of this type is not signed by the token
     * `fee_schedule_key` it SHALL fail with INVALID_SIGNATURE, or
     * TOKEN_HAS_NO_FEE_SCHEDULE_KEY if there is no `fee_schedule_key` set.
     * @property {number} NetworkGetExecutionTime=78 Get execution time(s) for one or more "recent" TransactionIDs.
     * @property {number} TokenPause=79 Pause a specific HTS token
     * @property {number} TokenUnpause=80 Unpause a paused HTS token.
     * @property {number} CryptoApproveAllowance=81 Approve an allowance for a spender relative to the owner account, which
     * MUST sign the transaction.
     * @property {number} CryptoDeleteAllowance=82 Delete (unapprove) an allowance previously approved
     * for the owner account.
     * @property {number} GetAccountDetails=83 Get all the information about an account, including balance
     * and allowances.<br/>
     * This does not get a list of account records.
     * @property {number} EthereumTransaction=84 Perform an Ethereum (EVM) transaction.<br/>
     * CallData may be inline if small, or in a "file" if large.
     * @property {number} NodeStakeUpdate=85 Used to indicate when the network has updated the staking information
     * at the end of a staking period and to indicate a new staking period
     * has started.
     * @property {number} UtilPrng=86 Generate and return a pseudorandom number based on network state.
     * @property {number} TransactionGetFastRecord=87 Get a record for a "recent" transaction.
     * @property {number} TokenUpdateNfts=88 Update the metadata of one or more NFT's of a specific token type.
     * @property {number} NodeCreate=89 Create a node
     * @property {number} NodeUpdate=90 Update a node
     * @property {number} NodeDelete=91 Delete a node
     * @property {number} TokenReject=92 Transfer one or more token balances held by the requesting account
     * to the treasury for each token type.
     * @property {number} TokenAirdrop=93 Airdrop one or more tokens to one or more accounts.
     * @property {number} TokenCancelAirdrop=94 Remove one or more pending airdrops from state on behalf of
     * the sender(s) for each airdrop.
     * @property {number} TokenClaimAirdrop=95 Claim one or more pending airdrops
     * @property {number} StateSignatureTransaction=100 Submit a signature of a state root hash gossiped to other nodes
     * @property {number} HintsKeyPublication=101 Publish a hinTS key to the network.
     * @property {number} HintsPreprocessingVote=102 Vote for a particular preprocessing output of a hinTS construction.
     * @property {number} HintsPartialSignature=103 Sign a partial signature for the active hinTS construction.
     * @property {number} HistoryAssemblySignature=104 Sign a particular history assembly.
     * @property {number} HistoryProofKeyPublication=105 Publish a roster history proof key to the network.
     * @property {number} HistoryProofVote=106 Vote for a particular history proof.
     * @property {number} CrsPublication=107 Publish a random CRS to the network.
     * @property {number} AtomicBatch=108 Submit a batch of transactions to run atomically
     * @property {number} LambdaSStore=109 (DEPRECATED) Remove once no production throttle assets reference it.
     * @property {number} HookDispatch=110 (Internal-only) Dispatch a hook action.
     * @property {number} HookStore=111 Update one or more storage slots in an EVM hook.
     * @property {number} LedgerIdPublication=112 (Internal-only) Publish a new ledger id and chain-of-trust key.
     * @property {number} RegisteredNodeCreate=113 Create a registered node
     * @property {number} RegisteredNodeUpdate=114 Update a registered node
     * @property {number} RegisteredNodeDelete=115 Delete a registered node
     * @property {number} MigrationRootHashVote=116 Vote on wrapped record migration hash state.
     */
    proto.HederaFunctionality = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "NONE"] = 0;
        values[valuesById[1] = "CryptoTransfer"] = 1;
        values[valuesById[2] = "CryptoUpdate"] = 2;
        values[valuesById[3] = "CryptoDelete"] = 3;
        values[valuesById[4] = "CryptoAddLiveHash"] = 4;
        values[valuesById[5] = "CryptoDeleteLiveHash"] = 5;
        values[valuesById[6] = "ContractCall"] = 6;
        values[valuesById[7] = "ContractCreate"] = 7;
        values[valuesById[8] = "ContractUpdate"] = 8;
        values[valuesById[9] = "FileCreate"] = 9;
        values[valuesById[10] = "FileAppend"] = 10;
        values[valuesById[11] = "FileUpdate"] = 11;
        values[valuesById[12] = "FileDelete"] = 12;
        values[valuesById[13] = "CryptoGetAccountBalance"] = 13;
        values[valuesById[14] = "CryptoGetAccountRecords"] = 14;
        values[valuesById[15] = "CryptoGetInfo"] = 15;
        values[valuesById[16] = "ContractCallLocal"] = 16;
        values[valuesById[17] = "ContractGetInfo"] = 17;
        values[valuesById[18] = "ContractGetBytecode"] = 18;
        values[valuesById[19] = "GetBySolidityID"] = 19;
        values[valuesById[20] = "GetByKey"] = 20;
        values[valuesById[21] = "CryptoGetLiveHash"] = 21;
        values[valuesById[22] = "CryptoGetStakers"] = 22;
        values[valuesById[23] = "FileGetContents"] = 23;
        values[valuesById[24] = "FileGetInfo"] = 24;
        values[valuesById[25] = "TransactionGetRecord"] = 25;
        values[valuesById[26] = "ContractGetRecords"] = 26;
        values[valuesById[27] = "CryptoCreate"] = 27;
        values[valuesById[28] = "SystemDelete"] = 28;
        values[valuesById[29] = "SystemUndelete"] = 29;
        values[valuesById[30] = "ContractDelete"] = 30;
        values[valuesById[31] = "Freeze"] = 31;
        values[valuesById[32] = "CreateTransactionRecord"] = 32;
        values[valuesById[33] = "CryptoAccountAutoRenew"] = 33;
        values[valuesById[34] = "ContractAutoRenew"] = 34;
        values[valuesById[35] = "GetVersionInfo"] = 35;
        values[valuesById[36] = "TransactionGetReceipt"] = 36;
        values[valuesById[50] = "ConsensusCreateTopic"] = 50;
        values[valuesById[51] = "ConsensusUpdateTopic"] = 51;
        values[valuesById[52] = "ConsensusDeleteTopic"] = 52;
        values[valuesById[53] = "ConsensusGetTopicInfo"] = 53;
        values[valuesById[54] = "ConsensusSubmitMessage"] = 54;
        values[valuesById[55] = "UncheckedSubmit"] = 55;
        values[valuesById[56] = "TokenCreate"] = 56;
        values[valuesById[58] = "TokenGetInfo"] = 58;
        values[valuesById[59] = "TokenFreezeAccount"] = 59;
        values[valuesById[60] = "TokenUnfreezeAccount"] = 60;
        values[valuesById[61] = "TokenGrantKycToAccount"] = 61;
        values[valuesById[62] = "TokenRevokeKycFromAccount"] = 62;
        values[valuesById[63] = "TokenDelete"] = 63;
        values[valuesById[64] = "TokenUpdate"] = 64;
        values[valuesById[65] = "TokenMint"] = 65;
        values[valuesById[66] = "TokenBurn"] = 66;
        values[valuesById[67] = "TokenAccountWipe"] = 67;
        values[valuesById[68] = "TokenAssociateToAccount"] = 68;
        values[valuesById[69] = "TokenDissociateFromAccount"] = 69;
        values[valuesById[70] = "ScheduleCreate"] = 70;
        values[valuesById[71] = "ScheduleDelete"] = 71;
        values[valuesById[72] = "ScheduleSign"] = 72;
        values[valuesById[73] = "ScheduleGetInfo"] = 73;
        values[valuesById[74] = "TokenGetAccountNftInfos"] = 74;
        values[valuesById[75] = "TokenGetNftInfo"] = 75;
        values[valuesById[76] = "TokenGetNftInfos"] = 76;
        values[valuesById[77] = "TokenFeeScheduleUpdate"] = 77;
        values[valuesById[78] = "NetworkGetExecutionTime"] = 78;
        values[valuesById[79] = "TokenPause"] = 79;
        values[valuesById[80] = "TokenUnpause"] = 80;
        values[valuesById[81] = "CryptoApproveAllowance"] = 81;
        values[valuesById[82] = "CryptoDeleteAllowance"] = 82;
        values[valuesById[83] = "GetAccountDetails"] = 83;
        values[valuesById[84] = "EthereumTransaction"] = 84;
        values[valuesById[85] = "NodeStakeUpdate"] = 85;
        values[valuesById[86] = "UtilPrng"] = 86;
        values[valuesById[87] = "TransactionGetFastRecord"] = 87;
        values[valuesById[88] = "TokenUpdateNfts"] = 88;
        values[valuesById[89] = "NodeCreate"] = 89;
        values[valuesById[90] = "NodeUpdate"] = 90;
        values[valuesById[91] = "NodeDelete"] = 91;
        values[valuesById[92] = "TokenReject"] = 92;
        values[valuesById[93] = "TokenAirdrop"] = 93;
        values[valuesById[94] = "TokenCancelAirdrop"] = 94;
        values[valuesById[95] = "TokenClaimAirdrop"] = 95;
        values[valuesById[100] = "StateSignatureTransaction"] = 100;
        values[valuesById[101] = "HintsKeyPublication"] = 101;
        values[valuesById[102] = "HintsPreprocessingVote"] = 102;
        values[valuesById[103] = "HintsPartialSignature"] = 103;
        values[valuesById[104] = "HistoryAssemblySignature"] = 104;
        values[valuesById[105] = "HistoryProofKeyPublication"] = 105;
        values[valuesById[106] = "HistoryProofVote"] = 106;
        values[valuesById[107] = "CrsPublication"] = 107;
        values[valuesById[108] = "AtomicBatch"] = 108;
        values[valuesById[109] = "LambdaSStore"] = 109;
        values[valuesById[110] = "HookDispatch"] = 110;
        values[valuesById[111] = "HookStore"] = 111;
        values[valuesById[112] = "LedgerIdPublication"] = 112;
        values[valuesById[113] = "RegisteredNodeCreate"] = 113;
        values[valuesById[114] = "RegisteredNodeUpdate"] = 114;
        values[valuesById[115] = "RegisteredNodeDelete"] = 115;
        values[valuesById[116] = "MigrationRootHashVote"] = 116;
        return values;
    })();

    proto.FeeComponents = (function() {

        /**
         * Properties of a FeeComponents.
         * @typedef {Object} proto.FeeComponents.$Properties
         * @property {number|Long|null} [min] Base: "minimum total fee".
         * <p>
         * The calculated fee MUST be greater than this value.
         * @property {number|Long|null} [max] Base: "maximum total fee".
         * <p>
         * The calculated fee MUST be less than this value.
         * @property {number|Long|null} [constant] Base: "constant fee".<br/>
         * A baseline constant contribution to total fee.
         * @property {number|Long|null} [bpt] Bandwidth: "bytes per transaction".<br/>
         * The fee for bandwidth consumed by a transaction, measured in bytes
         * @property {number|Long|null} [vpt] Signatures: "validations per transaction".<br/>
         * The fee for signature verifications required by a transaction
         * @property {number|Long|null} [rbh] Memory: "RAM byte-hours".<br/>
         * The fee for RAM required to process a transaction,
         * measured in byte-hours
         * @property {number|Long|null} [sbh] Disk: "storage byte-hours".<br/>
         * The fee for storage required by a transaction, measured in byte-hours
         * @property {number|Long|null} [gas] Compute: Ethereum term for a derivative EVM compute resource.<br/>
         * The fee of computation for a smart contract transaction. The value of
         * gas is set by a conversion rate, and is regularly updated to reflect
         * reasonable and customary costs.
         * @property {number|Long|null} [tv] Ad valorem: "transferred value".<br/>
         * The fee for HBAR transferred by a transaction.
         * @property {number|Long|null} [bpr] Response memory: "bytes per response".<br/>
         * The fee for data retrieved from memory to deliver a response,
         * measured in bytes
         * @property {number|Long|null} [sbpr] Response disk: "storage bytes per response".<br/>
         * The fee for data retrieved from disk to deliver a response,
         * measured in bytes
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FeeComponents.
         * @memberof proto
         * @interface IFeeComponents
         * @augments proto.FeeComponents.$Properties
         * @deprecated Use proto.FeeComponents.$Properties instead.
         */

        /**
         * Shape of a FeeComponents.
         * @typedef {proto.FeeComponents.$Properties} proto.FeeComponents.$Shape
         */

        /**
         * Constructs a new FeeComponents.
         * @memberof proto
         * @classdesc A set of values the nodes use in determining transaction and query fees, and
         * constants involved in fee calculations.
         * 
         * Nodes SHALL multiply the amount of "resources" allocated to a transaction or
         * query by the corresponding price to calculate the appropriate fee. Units are
         * one-thousandth of a `tinyCent`. The "resource" allocations SHALL be estimated
         * based on transaction characteristics and current network state, and MAY be
         * further adjusted based on network load and congestion.
         * 
         * This SHALL be used, in different contexts, for the cost _factors_ used to
         * calculate charged amounts, for the resource accumulation, and for actual
         * amounts to be charged.<br/>
         * Amounts recorded here MUST be converted to tinybar according to the
         * current active `ExchangeRate` for the network.
         * @constructor
         * @param {proto.FeeComponents.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FeeComponents = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Base: "minimum total fee".
         * <p>
         * The calculated fee MUST be greater than this value.
         * @member {number|Long} min
         * @memberof proto.FeeComponents
         * @instance
         */
        FeeComponents.prototype.min = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Base: "maximum total fee".
         * <p>
         * The calculated fee MUST be less than this value.
         * @member {number|Long} max
         * @memberof proto.FeeComponents
         * @instance
         */
        FeeComponents.prototype.max = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Base: "constant fee".<br/>
         * A baseline constant contribution to total fee.
         * @member {number|Long} constant
         * @memberof proto.FeeComponents
         * @instance
         */
        FeeComponents.prototype.constant = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Bandwidth: "bytes per transaction".<br/>
         * The fee for bandwidth consumed by a transaction, measured in bytes
         * @member {number|Long} bpt
         * @memberof proto.FeeComponents
         * @instance
         */
        FeeComponents.prototype.bpt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Signatures: "validations per transaction".<br/>
         * The fee for signature verifications required by a transaction
         * @member {number|Long} vpt
         * @memberof proto.FeeComponents
         * @instance
         */
        FeeComponents.prototype.vpt = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Memory: "RAM byte-hours".<br/>
         * The fee for RAM required to process a transaction,
         * measured in byte-hours
         * @member {number|Long} rbh
         * @memberof proto.FeeComponents
         * @instance
         */
        FeeComponents.prototype.rbh = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Disk: "storage byte-hours".<br/>
         * The fee for storage required by a transaction, measured in byte-hours
         * @member {number|Long} sbh
         * @memberof proto.FeeComponents
         * @instance
         */
        FeeComponents.prototype.sbh = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Compute: Ethereum term for a derivative EVM compute resource.<br/>
         * The fee of computation for a smart contract transaction. The value of
         * gas is set by a conversion rate, and is regularly updated to reflect
         * reasonable and customary costs.
         * @member {number|Long} gas
         * @memberof proto.FeeComponents
         * @instance
         */
        FeeComponents.prototype.gas = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Ad valorem: "transferred value".<br/>
         * The fee for HBAR transferred by a transaction.
         * @member {number|Long} tv
         * @memberof proto.FeeComponents
         * @instance
         */
        FeeComponents.prototype.tv = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Response memory: "bytes per response".<br/>
         * The fee for data retrieved from memory to deliver a response,
         * measured in bytes
         * @member {number|Long} bpr
         * @memberof proto.FeeComponents
         * @instance
         */
        FeeComponents.prototype.bpr = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Response disk: "storage bytes per response".<br/>
         * The fee for data retrieved from disk to deliver a response,
         * measured in bytes
         * @member {number|Long} sbpr
         * @memberof proto.FeeComponents
         * @instance
         */
        FeeComponents.prototype.sbpr = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new FeeComponents instance using the specified properties.
         * @function create
         * @memberof proto.FeeComponents
         * @static
         * @param {proto.FeeComponents.$Properties=} [properties] Properties to set
         * @returns {proto.FeeComponents} FeeComponents instance
         * @type {{
         *   (properties: proto.FeeComponents.$Shape): proto.FeeComponents & proto.FeeComponents.$Shape;
         *   (properties?: proto.FeeComponents.$Properties): proto.FeeComponents;
         * }}
         */
        FeeComponents.create = function(properties) {
            return new FeeComponents(properties);
        };

        /**
         * Encodes the specified FeeComponents message. Does not implicitly {@link proto.FeeComponents.verify|verify} messages.
         * @function encode
         * @memberof proto.FeeComponents
         * @static
         * @param {proto.FeeComponents.$Properties} message FeeComponents message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeComponents.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.min != null && $Object.hasOwnProperty.call(message, "min") && (typeof message.min === "object" ? message.min.low || message.min.high : message.min !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.min);
            if (message.max != null && $Object.hasOwnProperty.call(message, "max") && (typeof message.max === "object" ? message.max.low || message.max.high : message.max !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.max);
            if (message.constant != null && $Object.hasOwnProperty.call(message, "constant") && (typeof message.constant === "object" ? message.constant.low || message.constant.high : message.constant !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.constant);
            if (message.bpt != null && $Object.hasOwnProperty.call(message, "bpt") && (typeof message.bpt === "object" ? message.bpt.low || message.bpt.high : message.bpt !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.bpt);
            if (message.vpt != null && $Object.hasOwnProperty.call(message, "vpt") && (typeof message.vpt === "object" ? message.vpt.low || message.vpt.high : message.vpt !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.vpt);
            if (message.rbh != null && $Object.hasOwnProperty.call(message, "rbh") && (typeof message.rbh === "object" ? message.rbh.low || message.rbh.high : message.rbh !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.rbh);
            if (message.sbh != null && $Object.hasOwnProperty.call(message, "sbh") && (typeof message.sbh === "object" ? message.sbh.low || message.sbh.high : message.sbh !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.sbh);
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas") && (typeof message.gas === "object" ? message.gas.low || message.gas.high : message.gas !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.gas);
            if (message.tv != null && $Object.hasOwnProperty.call(message, "tv") && (typeof message.tv === "object" ? message.tv.low || message.tv.high : message.tv !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.tv);
            if (message.bpr != null && $Object.hasOwnProperty.call(message, "bpr") && (typeof message.bpr === "object" ? message.bpr.low || message.bpr.high : message.bpr !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.bpr);
            if (message.sbpr != null && $Object.hasOwnProperty.call(message, "sbpr") && (typeof message.sbpr === "object" ? message.sbpr.low || message.sbpr.high : message.sbpr !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.sbpr);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FeeComponents message, length delimited. Does not implicitly {@link proto.FeeComponents.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FeeComponents
         * @static
         * @param {proto.FeeComponents.$Properties} message FeeComponents message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeComponents.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FeeComponents message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FeeComponents
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FeeComponents & proto.FeeComponents.$Shape} FeeComponents
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeComponents.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FeeComponents();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.min = value;
                        else
                            delete message.min;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.max = value;
                        else
                            delete message.max;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.constant = value;
                        else
                            delete message.constant;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.bpt = value;
                        else
                            delete message.bpt;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.vpt = value;
                        else
                            delete message.vpt;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.rbh = value;
                        else
                            delete message.rbh;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.sbh = value;
                        else
                            delete message.sbh;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.gas = value;
                        else
                            delete message.gas;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.tv = value;
                        else
                            delete message.tv;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.bpr = value;
                        else
                            delete message.bpr;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.sbpr = value;
                        else
                            delete message.sbpr;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FeeComponents message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FeeComponents
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FeeComponents & proto.FeeComponents.$Shape} FeeComponents
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeComponents.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FeeComponents message.
         * @function verify
         * @memberof proto.FeeComponents
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FeeComponents.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.min != null && $Object.hasOwnProperty.call(message, "min"))
                if (!$util.isInteger(message.min) && !(message.min && $util.isInteger(message.min.low) && $util.isInteger(message.min.high)))
                    return "min: integer|Long expected";
            if (message.max != null && $Object.hasOwnProperty.call(message, "max"))
                if (!$util.isInteger(message.max) && !(message.max && $util.isInteger(message.max.low) && $util.isInteger(message.max.high)))
                    return "max: integer|Long expected";
            if (message.constant != null && $Object.hasOwnProperty.call(message, "constant"))
                if (!$util.isInteger(message.constant) && !(message.constant && $util.isInteger(message.constant.low) && $util.isInteger(message.constant.high)))
                    return "constant: integer|Long expected";
            if (message.bpt != null && $Object.hasOwnProperty.call(message, "bpt"))
                if (!$util.isInteger(message.bpt) && !(message.bpt && $util.isInteger(message.bpt.low) && $util.isInteger(message.bpt.high)))
                    return "bpt: integer|Long expected";
            if (message.vpt != null && $Object.hasOwnProperty.call(message, "vpt"))
                if (!$util.isInteger(message.vpt) && !(message.vpt && $util.isInteger(message.vpt.low) && $util.isInteger(message.vpt.high)))
                    return "vpt: integer|Long expected";
            if (message.rbh != null && $Object.hasOwnProperty.call(message, "rbh"))
                if (!$util.isInteger(message.rbh) && !(message.rbh && $util.isInteger(message.rbh.low) && $util.isInteger(message.rbh.high)))
                    return "rbh: integer|Long expected";
            if (message.sbh != null && $Object.hasOwnProperty.call(message, "sbh"))
                if (!$util.isInteger(message.sbh) && !(message.sbh && $util.isInteger(message.sbh.low) && $util.isInteger(message.sbh.high)))
                    return "sbh: integer|Long expected";
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (!$util.isInteger(message.gas) && !(message.gas && $util.isInteger(message.gas.low) && $util.isInteger(message.gas.high)))
                    return "gas: integer|Long expected";
            if (message.tv != null && $Object.hasOwnProperty.call(message, "tv"))
                if (!$util.isInteger(message.tv) && !(message.tv && $util.isInteger(message.tv.low) && $util.isInteger(message.tv.high)))
                    return "tv: integer|Long expected";
            if (message.bpr != null && $Object.hasOwnProperty.call(message, "bpr"))
                if (!$util.isInteger(message.bpr) && !(message.bpr && $util.isInteger(message.bpr.low) && $util.isInteger(message.bpr.high)))
                    return "bpr: integer|Long expected";
            if (message.sbpr != null && $Object.hasOwnProperty.call(message, "sbpr"))
                if (!$util.isInteger(message.sbpr) && !(message.sbpr && $util.isInteger(message.sbpr.low) && $util.isInteger(message.sbpr.high)))
                    return "sbpr: integer|Long expected";
            return null;
        };

        /**
         * Creates a FeeComponents message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FeeComponents
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FeeComponents} FeeComponents
         */
        FeeComponents.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FeeComponents)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FeeComponents: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FeeComponents();
            if (object.min != null)
                if (typeof object.min === "object" ? object.min.low || object.min.high : $Number(object.min) !== 0)
                    if ($util.Long)
                        message.min = $util.Long.fromValue(object.min, false);
                    else if (typeof object.min === "string")
                        message.min = $parseInt(object.min, 10);
                    else if (typeof object.min === "number")
                        message.min = object.min;
                    else if (typeof object.min === "object")
                        message.min = new $util.LongBits(object.min.low >>> 0, object.min.high >>> 0).toNumber();
            if (object.max != null)
                if (typeof object.max === "object" ? object.max.low || object.max.high : $Number(object.max) !== 0)
                    if ($util.Long)
                        message.max = $util.Long.fromValue(object.max, false);
                    else if (typeof object.max === "string")
                        message.max = $parseInt(object.max, 10);
                    else if (typeof object.max === "number")
                        message.max = object.max;
                    else if (typeof object.max === "object")
                        message.max = new $util.LongBits(object.max.low >>> 0, object.max.high >>> 0).toNumber();
            if (object.constant != null)
                if (typeof object.constant === "object" ? object.constant.low || object.constant.high : $Number(object.constant) !== 0)
                    if ($util.Long)
                        message.constant = $util.Long.fromValue(object.constant, false);
                    else if (typeof object.constant === "string")
                        message.constant = $parseInt(object.constant, 10);
                    else if (typeof object.constant === "number")
                        message.constant = object.constant;
                    else if (typeof object.constant === "object")
                        message.constant = new $util.LongBits(object.constant.low >>> 0, object.constant.high >>> 0).toNumber();
            if (object.bpt != null)
                if (typeof object.bpt === "object" ? object.bpt.low || object.bpt.high : $Number(object.bpt) !== 0)
                    if ($util.Long)
                        message.bpt = $util.Long.fromValue(object.bpt, false);
                    else if (typeof object.bpt === "string")
                        message.bpt = $parseInt(object.bpt, 10);
                    else if (typeof object.bpt === "number")
                        message.bpt = object.bpt;
                    else if (typeof object.bpt === "object")
                        message.bpt = new $util.LongBits(object.bpt.low >>> 0, object.bpt.high >>> 0).toNumber();
            if (object.vpt != null)
                if (typeof object.vpt === "object" ? object.vpt.low || object.vpt.high : $Number(object.vpt) !== 0)
                    if ($util.Long)
                        message.vpt = $util.Long.fromValue(object.vpt, false);
                    else if (typeof object.vpt === "string")
                        message.vpt = $parseInt(object.vpt, 10);
                    else if (typeof object.vpt === "number")
                        message.vpt = object.vpt;
                    else if (typeof object.vpt === "object")
                        message.vpt = new $util.LongBits(object.vpt.low >>> 0, object.vpt.high >>> 0).toNumber();
            if (object.rbh != null)
                if (typeof object.rbh === "object" ? object.rbh.low || object.rbh.high : $Number(object.rbh) !== 0)
                    if ($util.Long)
                        message.rbh = $util.Long.fromValue(object.rbh, false);
                    else if (typeof object.rbh === "string")
                        message.rbh = $parseInt(object.rbh, 10);
                    else if (typeof object.rbh === "number")
                        message.rbh = object.rbh;
                    else if (typeof object.rbh === "object")
                        message.rbh = new $util.LongBits(object.rbh.low >>> 0, object.rbh.high >>> 0).toNumber();
            if (object.sbh != null)
                if (typeof object.sbh === "object" ? object.sbh.low || object.sbh.high : $Number(object.sbh) !== 0)
                    if ($util.Long)
                        message.sbh = $util.Long.fromValue(object.sbh, false);
                    else if (typeof object.sbh === "string")
                        message.sbh = $parseInt(object.sbh, 10);
                    else if (typeof object.sbh === "number")
                        message.sbh = object.sbh;
                    else if (typeof object.sbh === "object")
                        message.sbh = new $util.LongBits(object.sbh.low >>> 0, object.sbh.high >>> 0).toNumber();
            if (object.gas != null)
                if (typeof object.gas === "object" ? object.gas.low || object.gas.high : $Number(object.gas) !== 0)
                    if ($util.Long)
                        message.gas = $util.Long.fromValue(object.gas, false);
                    else if (typeof object.gas === "string")
                        message.gas = $parseInt(object.gas, 10);
                    else if (typeof object.gas === "number")
                        message.gas = object.gas;
                    else if (typeof object.gas === "object")
                        message.gas = new $util.LongBits(object.gas.low >>> 0, object.gas.high >>> 0).toNumber();
            if (object.tv != null)
                if (typeof object.tv === "object" ? object.tv.low || object.tv.high : $Number(object.tv) !== 0)
                    if ($util.Long)
                        message.tv = $util.Long.fromValue(object.tv, false);
                    else if (typeof object.tv === "string")
                        message.tv = $parseInt(object.tv, 10);
                    else if (typeof object.tv === "number")
                        message.tv = object.tv;
                    else if (typeof object.tv === "object")
                        message.tv = new $util.LongBits(object.tv.low >>> 0, object.tv.high >>> 0).toNumber();
            if (object.bpr != null)
                if (typeof object.bpr === "object" ? object.bpr.low || object.bpr.high : $Number(object.bpr) !== 0)
                    if ($util.Long)
                        message.bpr = $util.Long.fromValue(object.bpr, false);
                    else if (typeof object.bpr === "string")
                        message.bpr = $parseInt(object.bpr, 10);
                    else if (typeof object.bpr === "number")
                        message.bpr = object.bpr;
                    else if (typeof object.bpr === "object")
                        message.bpr = new $util.LongBits(object.bpr.low >>> 0, object.bpr.high >>> 0).toNumber();
            if (object.sbpr != null)
                if (typeof object.sbpr === "object" ? object.sbpr.low || object.sbpr.high : $Number(object.sbpr) !== 0)
                    if ($util.Long)
                        message.sbpr = $util.Long.fromValue(object.sbpr, false);
                    else if (typeof object.sbpr === "string")
                        message.sbpr = $parseInt(object.sbpr, 10);
                    else if (typeof object.sbpr === "number")
                        message.sbpr = object.sbpr;
                    else if (typeof object.sbpr === "object")
                        message.sbpr = new $util.LongBits(object.sbpr.low >>> 0, object.sbpr.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a FeeComponents message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FeeComponents
         * @static
         * @param {proto.FeeComponents} message FeeComponents
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FeeComponents.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.min = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.min = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.max = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.max = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.constant = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.constant = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.bpt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.bpt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.vpt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.vpt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.rbh = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.rbh = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sbh = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sbh = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.gas = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gas = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.tv = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.tv = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.bpr = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.bpr = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sbpr = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sbpr = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.min != null && $Object.hasOwnProperty.call(message, "min"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.min = typeof message.min === "number" ? $BigInt(message.min) : $util.Long.fromBits(message.min.low >>> 0, message.min.high >>> 0, false).toBigInt();
                else if (typeof message.min === "number")
                    object.min = options.longs === $String ? $String(message.min) : message.min;
                else
                    object.min = options.longs === $String ? $util.Long.prototype.toString.call(message.min) : options.longs === $Number ? new $util.LongBits(message.min.low >>> 0, message.min.high >>> 0).toNumber() : message.min;
            if (message.max != null && $Object.hasOwnProperty.call(message, "max"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.max = typeof message.max === "number" ? $BigInt(message.max) : $util.Long.fromBits(message.max.low >>> 0, message.max.high >>> 0, false).toBigInt();
                else if (typeof message.max === "number")
                    object.max = options.longs === $String ? $String(message.max) : message.max;
                else
                    object.max = options.longs === $String ? $util.Long.prototype.toString.call(message.max) : options.longs === $Number ? new $util.LongBits(message.max.low >>> 0, message.max.high >>> 0).toNumber() : message.max;
            if (message.constant != null && $Object.hasOwnProperty.call(message, "constant"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.constant = typeof message.constant === "number" ? $BigInt(message.constant) : $util.Long.fromBits(message.constant.low >>> 0, message.constant.high >>> 0, false).toBigInt();
                else if (typeof message.constant === "number")
                    object.constant = options.longs === $String ? $String(message.constant) : message.constant;
                else
                    object.constant = options.longs === $String ? $util.Long.prototype.toString.call(message.constant) : options.longs === $Number ? new $util.LongBits(message.constant.low >>> 0, message.constant.high >>> 0).toNumber() : message.constant;
            if (message.bpt != null && $Object.hasOwnProperty.call(message, "bpt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.bpt = typeof message.bpt === "number" ? $BigInt(message.bpt) : $util.Long.fromBits(message.bpt.low >>> 0, message.bpt.high >>> 0, false).toBigInt();
                else if (typeof message.bpt === "number")
                    object.bpt = options.longs === $String ? $String(message.bpt) : message.bpt;
                else
                    object.bpt = options.longs === $String ? $util.Long.prototype.toString.call(message.bpt) : options.longs === $Number ? new $util.LongBits(message.bpt.low >>> 0, message.bpt.high >>> 0).toNumber() : message.bpt;
            if (message.vpt != null && $Object.hasOwnProperty.call(message, "vpt"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.vpt = typeof message.vpt === "number" ? $BigInt(message.vpt) : $util.Long.fromBits(message.vpt.low >>> 0, message.vpt.high >>> 0, false).toBigInt();
                else if (typeof message.vpt === "number")
                    object.vpt = options.longs === $String ? $String(message.vpt) : message.vpt;
                else
                    object.vpt = options.longs === $String ? $util.Long.prototype.toString.call(message.vpt) : options.longs === $Number ? new $util.LongBits(message.vpt.low >>> 0, message.vpt.high >>> 0).toNumber() : message.vpt;
            if (message.rbh != null && $Object.hasOwnProperty.call(message, "rbh"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.rbh = typeof message.rbh === "number" ? $BigInt(message.rbh) : $util.Long.fromBits(message.rbh.low >>> 0, message.rbh.high >>> 0, false).toBigInt();
                else if (typeof message.rbh === "number")
                    object.rbh = options.longs === $String ? $String(message.rbh) : message.rbh;
                else
                    object.rbh = options.longs === $String ? $util.Long.prototype.toString.call(message.rbh) : options.longs === $Number ? new $util.LongBits(message.rbh.low >>> 0, message.rbh.high >>> 0).toNumber() : message.rbh;
            if (message.sbh != null && $Object.hasOwnProperty.call(message, "sbh"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sbh = typeof message.sbh === "number" ? $BigInt(message.sbh) : $util.Long.fromBits(message.sbh.low >>> 0, message.sbh.high >>> 0, false).toBigInt();
                else if (typeof message.sbh === "number")
                    object.sbh = options.longs === $String ? $String(message.sbh) : message.sbh;
                else
                    object.sbh = options.longs === $String ? $util.Long.prototype.toString.call(message.sbh) : options.longs === $Number ? new $util.LongBits(message.sbh.low >>> 0, message.sbh.high >>> 0).toNumber() : message.sbh;
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gas = typeof message.gas === "number" ? $BigInt(message.gas) : $util.Long.fromBits(message.gas.low >>> 0, message.gas.high >>> 0, false).toBigInt();
                else if (typeof message.gas === "number")
                    object.gas = options.longs === $String ? $String(message.gas) : message.gas;
                else
                    object.gas = options.longs === $String ? $util.Long.prototype.toString.call(message.gas) : options.longs === $Number ? new $util.LongBits(message.gas.low >>> 0, message.gas.high >>> 0).toNumber() : message.gas;
            if (message.tv != null && $Object.hasOwnProperty.call(message, "tv"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.tv = typeof message.tv === "number" ? $BigInt(message.tv) : $util.Long.fromBits(message.tv.low >>> 0, message.tv.high >>> 0, false).toBigInt();
                else if (typeof message.tv === "number")
                    object.tv = options.longs === $String ? $String(message.tv) : message.tv;
                else
                    object.tv = options.longs === $String ? $util.Long.prototype.toString.call(message.tv) : options.longs === $Number ? new $util.LongBits(message.tv.low >>> 0, message.tv.high >>> 0).toNumber() : message.tv;
            if (message.bpr != null && $Object.hasOwnProperty.call(message, "bpr"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.bpr = typeof message.bpr === "number" ? $BigInt(message.bpr) : $util.Long.fromBits(message.bpr.low >>> 0, message.bpr.high >>> 0, false).toBigInt();
                else if (typeof message.bpr === "number")
                    object.bpr = options.longs === $String ? $String(message.bpr) : message.bpr;
                else
                    object.bpr = options.longs === $String ? $util.Long.prototype.toString.call(message.bpr) : options.longs === $Number ? new $util.LongBits(message.bpr.low >>> 0, message.bpr.high >>> 0).toNumber() : message.bpr;
            if (message.sbpr != null && $Object.hasOwnProperty.call(message, "sbpr"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sbpr = typeof message.sbpr === "number" ? $BigInt(message.sbpr) : $util.Long.fromBits(message.sbpr.low >>> 0, message.sbpr.high >>> 0, false).toBigInt();
                else if (typeof message.sbpr === "number")
                    object.sbpr = options.longs === $String ? $String(message.sbpr) : message.sbpr;
                else
                    object.sbpr = options.longs === $String ? $util.Long.prototype.toString.call(message.sbpr) : options.longs === $Number ? new $util.LongBits(message.sbpr.low >>> 0, message.sbpr.high >>> 0).toNumber() : message.sbpr;
            return object;
        };

        /**
         * Converts this FeeComponents to JSON.
         * @function toJSON
         * @memberof proto.FeeComponents
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FeeComponents.prototype.toJSON = function() {
            return FeeComponents.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FeeComponents
         * @function getTypeUrl
         * @memberof proto.FeeComponents
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FeeComponents.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FeeComponents";
        };

        return FeeComponents;
    })();

    proto.TransactionFeeSchedule = (function() {

        /**
         * Properties of a TransactionFeeSchedule.
         * @typedef {Object} proto.TransactionFeeSchedule.$Properties
         * @property {proto.HederaFunctionality|null} [hederaFunctionality] An enumeration for a particular transaction or query.<br/>
         * The functionality type determines the base cost parameters.
         * @property {proto.FeeData.$Properties|null} [feeData] Use `fees` instead of this field.<br/>
         * Resource price coefficients.
         * @property {Array.<proto.FeeData.$Properties>|null} [fees] The resource price coefficients for transaction type and any applicable
         * subtypes.<br/>
         * The multiple entries enable support for subtype price definitions.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionFeeSchedule.
         * @memberof proto
         * @interface ITransactionFeeSchedule
         * @augments proto.TransactionFeeSchedule.$Properties
         * @deprecated Use proto.TransactionFeeSchedule.$Properties instead.
         */

        /**
         * Shape of a TransactionFeeSchedule.
         * @typedef {proto.TransactionFeeSchedule.$Properties} proto.TransactionFeeSchedule.$Shape
         */

        /**
         * Constructs a new TransactionFeeSchedule.
         * @memberof proto
         * @classdesc The fee schedule for a specific transaction or query based on the fee data.
         * @constructor
         * @param {proto.TransactionFeeSchedule.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionFeeSchedule = function (properties) {
            this.fees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An enumeration for a particular transaction or query.<br/>
         * The functionality type determines the base cost parameters.
         * @member {proto.HederaFunctionality} hederaFunctionality
         * @memberof proto.TransactionFeeSchedule
         * @instance
         */
        TransactionFeeSchedule.prototype.hederaFunctionality = 0;

        /**
         * Use `fees` instead of this field.<br/>
         * Resource price coefficients.
         * @member {proto.FeeData.$Properties|null|undefined} feeData
         * @memberof proto.TransactionFeeSchedule
         * @instance
         */
        TransactionFeeSchedule.prototype.feeData = null;

        /**
         * The resource price coefficients for transaction type and any applicable
         * subtypes.<br/>
         * The multiple entries enable support for subtype price definitions.
         * @member {Array.<proto.FeeData.$Properties>} fees
         * @memberof proto.TransactionFeeSchedule
         * @instance
         */
        TransactionFeeSchedule.prototype.fees = $util.emptyArray;

        /**
         * Creates a new TransactionFeeSchedule instance using the specified properties.
         * @function create
         * @memberof proto.TransactionFeeSchedule
         * @static
         * @param {proto.TransactionFeeSchedule.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionFeeSchedule} TransactionFeeSchedule instance
         * @type {{
         *   (properties: proto.TransactionFeeSchedule.$Shape): proto.TransactionFeeSchedule & proto.TransactionFeeSchedule.$Shape;
         *   (properties?: proto.TransactionFeeSchedule.$Properties): proto.TransactionFeeSchedule;
         * }}
         */
        TransactionFeeSchedule.create = function(properties) {
            return new TransactionFeeSchedule(properties);
        };

        /**
         * Encodes the specified TransactionFeeSchedule message. Does not implicitly {@link proto.TransactionFeeSchedule.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionFeeSchedule
         * @static
         * @param {proto.TransactionFeeSchedule.$Properties} message TransactionFeeSchedule message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionFeeSchedule.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hederaFunctionality != null && $Object.hasOwnProperty.call(message, "hederaFunctionality") && message.hederaFunctionality !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.hederaFunctionality);
            if (message.feeData != null && $Object.hasOwnProperty.call(message, "feeData"))
                $root.proto.FeeData.encode(message.feeData, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.fees != null && message.fees.length)
                for (let i = 0; i < message.fees.length; ++i)
                    $root.proto.FeeData.encode(message.fees[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionFeeSchedule message, length delimited. Does not implicitly {@link proto.TransactionFeeSchedule.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionFeeSchedule
         * @static
         * @param {proto.TransactionFeeSchedule.$Properties} message TransactionFeeSchedule message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionFeeSchedule.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionFeeSchedule message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionFeeSchedule
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionFeeSchedule & proto.TransactionFeeSchedule.$Shape} TransactionFeeSchedule
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionFeeSchedule.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionFeeSchedule();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.hederaFunctionality = value;
                        else
                            delete message.hederaFunctionality;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.feeData = $root.proto.FeeData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeData);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.fees && message.fees.length))
                            message.fees = [];
                        message.fees.push($root.proto.FeeData.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionFeeSchedule message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionFeeSchedule
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionFeeSchedule & proto.TransactionFeeSchedule.$Shape} TransactionFeeSchedule
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionFeeSchedule.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionFeeSchedule message.
         * @function verify
         * @memberof proto.TransactionFeeSchedule
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionFeeSchedule.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hederaFunctionality != null && $Object.hasOwnProperty.call(message, "hederaFunctionality"))
                if (typeof message.hederaFunctionality !== "number" || (message.hederaFunctionality | 0) !== message.hederaFunctionality)
                    return "hederaFunctionality: enum value expected";
            if (message.feeData != null && $Object.hasOwnProperty.call(message, "feeData")) {
                let error = $root.proto.FeeData.verify(message.feeData, _depth + 1);
                if (error)
                    return "feeData." + error;
            }
            if (message.fees != null && $Object.hasOwnProperty.call(message, "fees")) {
                if (!$Array.isArray(message.fees))
                    return "fees: array expected";
                for (let i = 0; i < message.fees.length; ++i) {
                    let error = $root.proto.FeeData.verify(message.fees[i], _depth + 1);
                    if (error)
                        return "fees." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TransactionFeeSchedule message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionFeeSchedule
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionFeeSchedule} TransactionFeeSchedule
         */
        TransactionFeeSchedule.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionFeeSchedule)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionFeeSchedule: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionFeeSchedule();
            if (object.hederaFunctionality !== 0 && (typeof object.hederaFunctionality !== "string" || $root.proto.HederaFunctionality[object.hederaFunctionality] !== 0))
                switch (object.hederaFunctionality) {
                case "NONE":
                case 0:
                    message.hederaFunctionality = 0;
                    break;
                case "CryptoTransfer":
                case 1:
                    message.hederaFunctionality = 1;
                    break;
                case "CryptoUpdate":
                case 2:
                    message.hederaFunctionality = 2;
                    break;
                case "CryptoDelete":
                case 3:
                    message.hederaFunctionality = 3;
                    break;
                case "CryptoAddLiveHash":
                case 4:
                    message.hederaFunctionality = 4;
                    break;
                case "CryptoDeleteLiveHash":
                case 5:
                    message.hederaFunctionality = 5;
                    break;
                case "ContractCall":
                case 6:
                    message.hederaFunctionality = 6;
                    break;
                case "ContractCreate":
                case 7:
                    message.hederaFunctionality = 7;
                    break;
                case "ContractUpdate":
                case 8:
                    message.hederaFunctionality = 8;
                    break;
                case "FileCreate":
                case 9:
                    message.hederaFunctionality = 9;
                    break;
                case "FileAppend":
                case 10:
                    message.hederaFunctionality = 10;
                    break;
                case "FileUpdate":
                case 11:
                    message.hederaFunctionality = 11;
                    break;
                case "FileDelete":
                case 12:
                    message.hederaFunctionality = 12;
                    break;
                case "CryptoGetAccountBalance":
                case 13:
                    message.hederaFunctionality = 13;
                    break;
                case "CryptoGetAccountRecords":
                case 14:
                    message.hederaFunctionality = 14;
                    break;
                case "CryptoGetInfo":
                case 15:
                    message.hederaFunctionality = 15;
                    break;
                case "ContractCallLocal":
                case 16:
                    message.hederaFunctionality = 16;
                    break;
                case "ContractGetInfo":
                case 17:
                    message.hederaFunctionality = 17;
                    break;
                case "ContractGetBytecode":
                case 18:
                    message.hederaFunctionality = 18;
                    break;
                case "GetBySolidityID":
                case 19:
                    message.hederaFunctionality = 19;
                    break;
                case "GetByKey":
                case 20:
                    message.hederaFunctionality = 20;
                    break;
                case "CryptoGetLiveHash":
                case 21:
                    message.hederaFunctionality = 21;
                    break;
                case "CryptoGetStakers":
                case 22:
                    message.hederaFunctionality = 22;
                    break;
                case "FileGetContents":
                case 23:
                    message.hederaFunctionality = 23;
                    break;
                case "FileGetInfo":
                case 24:
                    message.hederaFunctionality = 24;
                    break;
                case "TransactionGetRecord":
                case 25:
                    message.hederaFunctionality = 25;
                    break;
                case "ContractGetRecords":
                case 26:
                    message.hederaFunctionality = 26;
                    break;
                case "CryptoCreate":
                case 27:
                    message.hederaFunctionality = 27;
                    break;
                case "SystemDelete":
                case 28:
                    message.hederaFunctionality = 28;
                    break;
                case "SystemUndelete":
                case 29:
                    message.hederaFunctionality = 29;
                    break;
                case "ContractDelete":
                case 30:
                    message.hederaFunctionality = 30;
                    break;
                case "Freeze":
                case 31:
                    message.hederaFunctionality = 31;
                    break;
                case "CreateTransactionRecord":
                case 32:
                    message.hederaFunctionality = 32;
                    break;
                case "CryptoAccountAutoRenew":
                case 33:
                    message.hederaFunctionality = 33;
                    break;
                case "ContractAutoRenew":
                case 34:
                    message.hederaFunctionality = 34;
                    break;
                case "GetVersionInfo":
                case 35:
                    message.hederaFunctionality = 35;
                    break;
                case "TransactionGetReceipt":
                case 36:
                    message.hederaFunctionality = 36;
                    break;
                case "ConsensusCreateTopic":
                case 50:
                    message.hederaFunctionality = 50;
                    break;
                case "ConsensusUpdateTopic":
                case 51:
                    message.hederaFunctionality = 51;
                    break;
                case "ConsensusDeleteTopic":
                case 52:
                    message.hederaFunctionality = 52;
                    break;
                case "ConsensusGetTopicInfo":
                case 53:
                    message.hederaFunctionality = 53;
                    break;
                case "ConsensusSubmitMessage":
                case 54:
                    message.hederaFunctionality = 54;
                    break;
                case "UncheckedSubmit":
                case 55:
                    message.hederaFunctionality = 55;
                    break;
                case "TokenCreate":
                case 56:
                    message.hederaFunctionality = 56;
                    break;
                case "TokenGetInfo":
                case 58:
                    message.hederaFunctionality = 58;
                    break;
                case "TokenFreezeAccount":
                case 59:
                    message.hederaFunctionality = 59;
                    break;
                case "TokenUnfreezeAccount":
                case 60:
                    message.hederaFunctionality = 60;
                    break;
                case "TokenGrantKycToAccount":
                case 61:
                    message.hederaFunctionality = 61;
                    break;
                case "TokenRevokeKycFromAccount":
                case 62:
                    message.hederaFunctionality = 62;
                    break;
                case "TokenDelete":
                case 63:
                    message.hederaFunctionality = 63;
                    break;
                case "TokenUpdate":
                case 64:
                    message.hederaFunctionality = 64;
                    break;
                case "TokenMint":
                case 65:
                    message.hederaFunctionality = 65;
                    break;
                case "TokenBurn":
                case 66:
                    message.hederaFunctionality = 66;
                    break;
                case "TokenAccountWipe":
                case 67:
                    message.hederaFunctionality = 67;
                    break;
                case "TokenAssociateToAccount":
                case 68:
                    message.hederaFunctionality = 68;
                    break;
                case "TokenDissociateFromAccount":
                case 69:
                    message.hederaFunctionality = 69;
                    break;
                case "ScheduleCreate":
                case 70:
                    message.hederaFunctionality = 70;
                    break;
                case "ScheduleDelete":
                case 71:
                    message.hederaFunctionality = 71;
                    break;
                case "ScheduleSign":
                case 72:
                    message.hederaFunctionality = 72;
                    break;
                case "ScheduleGetInfo":
                case 73:
                    message.hederaFunctionality = 73;
                    break;
                case "TokenGetAccountNftInfos":
                case 74:
                    message.hederaFunctionality = 74;
                    break;
                case "TokenGetNftInfo":
                case 75:
                    message.hederaFunctionality = 75;
                    break;
                case "TokenGetNftInfos":
                case 76:
                    message.hederaFunctionality = 76;
                    break;
                case "TokenFeeScheduleUpdate":
                case 77:
                    message.hederaFunctionality = 77;
                    break;
                case "NetworkGetExecutionTime":
                case 78:
                    message.hederaFunctionality = 78;
                    break;
                case "TokenPause":
                case 79:
                    message.hederaFunctionality = 79;
                    break;
                case "TokenUnpause":
                case 80:
                    message.hederaFunctionality = 80;
                    break;
                case "CryptoApproveAllowance":
                case 81:
                    message.hederaFunctionality = 81;
                    break;
                case "CryptoDeleteAllowance":
                case 82:
                    message.hederaFunctionality = 82;
                    break;
                case "GetAccountDetails":
                case 83:
                    message.hederaFunctionality = 83;
                    break;
                case "EthereumTransaction":
                case 84:
                    message.hederaFunctionality = 84;
                    break;
                case "NodeStakeUpdate":
                case 85:
                    message.hederaFunctionality = 85;
                    break;
                case "UtilPrng":
                case 86:
                    message.hederaFunctionality = 86;
                    break;
                case "TransactionGetFastRecord":
                case 87:
                    message.hederaFunctionality = 87;
                    break;
                case "TokenUpdateNfts":
                case 88:
                    message.hederaFunctionality = 88;
                    break;
                case "NodeCreate":
                case 89:
                    message.hederaFunctionality = 89;
                    break;
                case "NodeUpdate":
                case 90:
                    message.hederaFunctionality = 90;
                    break;
                case "NodeDelete":
                case 91:
                    message.hederaFunctionality = 91;
                    break;
                case "TokenReject":
                case 92:
                    message.hederaFunctionality = 92;
                    break;
                case "TokenAirdrop":
                case 93:
                    message.hederaFunctionality = 93;
                    break;
                case "TokenCancelAirdrop":
                case 94:
                    message.hederaFunctionality = 94;
                    break;
                case "TokenClaimAirdrop":
                case 95:
                    message.hederaFunctionality = 95;
                    break;
                case "StateSignatureTransaction":
                case 100:
                    message.hederaFunctionality = 100;
                    break;
                case "HintsKeyPublication":
                case 101:
                    message.hederaFunctionality = 101;
                    break;
                case "HintsPreprocessingVote":
                case 102:
                    message.hederaFunctionality = 102;
                    break;
                case "HintsPartialSignature":
                case 103:
                    message.hederaFunctionality = 103;
                    break;
                case "HistoryAssemblySignature":
                case 104:
                    message.hederaFunctionality = 104;
                    break;
                case "HistoryProofKeyPublication":
                case 105:
                    message.hederaFunctionality = 105;
                    break;
                case "HistoryProofVote":
                case 106:
                    message.hederaFunctionality = 106;
                    break;
                case "CrsPublication":
                case 107:
                    message.hederaFunctionality = 107;
                    break;
                case "AtomicBatch":
                case 108:
                    message.hederaFunctionality = 108;
                    break;
                case "LambdaSStore":
                case 109:
                    message.hederaFunctionality = 109;
                    break;
                case "HookDispatch":
                case 110:
                    message.hederaFunctionality = 110;
                    break;
                case "HookStore":
                case 111:
                    message.hederaFunctionality = 111;
                    break;
                case "LedgerIdPublication":
                case 112:
                    message.hederaFunctionality = 112;
                    break;
                case "RegisteredNodeCreate":
                case 113:
                    message.hederaFunctionality = 113;
                    break;
                case "RegisteredNodeUpdate":
                case 114:
                    message.hederaFunctionality = 114;
                    break;
                case "RegisteredNodeDelete":
                case 115:
                    message.hederaFunctionality = 115;
                    break;
                case "MigrationRootHashVote":
                case 116:
                    message.hederaFunctionality = 116;
                    break;
                default:
                    if (typeof object.hederaFunctionality === "number" && (object.hederaFunctionality | 0) === object.hederaFunctionality)
                        message.hederaFunctionality = object.hederaFunctionality;
                }
            if (object.feeData != null) {
                if (!$util.isObject(object.feeData))
                    throw $TypeError(".proto.TransactionFeeSchedule.feeData: object expected");
                message.feeData = $root.proto.FeeData.fromObject(object.feeData, _depth + 1);
            }
            if (object.fees) {
                if (!$Array.isArray(object.fees))
                    throw $TypeError(".proto.TransactionFeeSchedule.fees: array expected");
                message.fees = $Array(object.fees.length);
                for (let i = 0; i < object.fees.length; ++i) {
                    if (!$util.isObject(object.fees[i]))
                        throw $TypeError(".proto.TransactionFeeSchedule.fees: object expected");
                    message.fees[i] = $root.proto.FeeData.fromObject(object.fees[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TransactionFeeSchedule message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionFeeSchedule
         * @static
         * @param {proto.TransactionFeeSchedule} message TransactionFeeSchedule
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionFeeSchedule.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.fees = [];
            if (options.defaults) {
                object.hederaFunctionality = options.enums === $String ? "NONE" : 0;
                object.feeData = null;
            }
            if (message.hederaFunctionality != null && $Object.hasOwnProperty.call(message, "hederaFunctionality"))
                object.hederaFunctionality = options.enums === $String ? $root.proto.HederaFunctionality[message.hederaFunctionality] === $undefined ? message.hederaFunctionality : $root.proto.HederaFunctionality[message.hederaFunctionality] : message.hederaFunctionality;
            if (message.feeData != null && $Object.hasOwnProperty.call(message, "feeData"))
                object.feeData = $root.proto.FeeData.toObject(message.feeData, options, _depth + 1);
            if (message.fees && message.fees.length) {
                object.fees = $Array(message.fees.length);
                for (let j = 0; j < message.fees.length; ++j)
                    object.fees[j] = $root.proto.FeeData.toObject(message.fees[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TransactionFeeSchedule to JSON.
         * @function toJSON
         * @memberof proto.TransactionFeeSchedule
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionFeeSchedule.prototype.toJSON = function() {
            return TransactionFeeSchedule.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionFeeSchedule
         * @function getTypeUrl
         * @memberof proto.TransactionFeeSchedule
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionFeeSchedule.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionFeeSchedule";
        };

        return TransactionFeeSchedule;
    })();

    proto.FeeData = (function() {

        /**
         * Properties of a FeeData.
         * @typedef {Object} proto.FeeData.$Properties
         * @property {proto.FeeComponents.$Properties|null} [nodedata] Fee components to be paid to the submitting node.
         * @property {proto.FeeComponents.$Properties|null} [networkdata] Fee components to be paid to the network for bringing a
         * transaction to consensus.
         * @property {proto.FeeComponents.$Properties|null} [servicedata] Fee components to be paid to the network for providing the immediate and
         * ongoing services associated with executing the transaction, maintaining
         * the network, and developing the network software.
         * @property {proto.SubType|null} [subType] A sub-type distinguishing between different types of `FeeData` that may
         * apply to the same base transaction type (associated with
         * an `HederaFunctionality`).
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FeeData.
         * @memberof proto
         * @interface IFeeData
         * @augments proto.FeeData.$Properties
         * @deprecated Use proto.FeeData.$Properties instead.
         */

        /**
         * Shape of a FeeData.
         * @typedef {proto.FeeData.$Properties} proto.FeeData.$Shape
         */

        /**
         * Constructs a new FeeData.
         * @memberof proto
         * @classdesc A total fee, in component amounts charged for a transaction.
         * 
         * Total fees are composed of three sets of components.
         * - Node data, components that compensate the specific node that submitted
         * the transaction.
         * - Network data, components that compensate the Hedera network for gossiping
         * the transaction and determining the consensus timestamp.
         * - Service data, components that compensate the Hedera network for the ongoing
         * maintenance and operation of the network, as well as ongoing development
         * of network services.
         * 
         * Fee components are recorded in thousandths of a tiny cent, and the network
         * exchange rate converts these to tinybar amounts, which are what the network
         * charges for transactions and what the network reports in the record stream.
         * @constructor
         * @param {proto.FeeData.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FeeData = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Fee components to be paid to the submitting node.
         * @member {proto.FeeComponents.$Properties|null|undefined} nodedata
         * @memberof proto.FeeData
         * @instance
         */
        FeeData.prototype.nodedata = null;

        /**
         * Fee components to be paid to the network for bringing a
         * transaction to consensus.
         * @member {proto.FeeComponents.$Properties|null|undefined} networkdata
         * @memberof proto.FeeData
         * @instance
         */
        FeeData.prototype.networkdata = null;

        /**
         * Fee components to be paid to the network for providing the immediate and
         * ongoing services associated with executing the transaction, maintaining
         * the network, and developing the network software.
         * @member {proto.FeeComponents.$Properties|null|undefined} servicedata
         * @memberof proto.FeeData
         * @instance
         */
        FeeData.prototype.servicedata = null;

        /**
         * A sub-type distinguishing between different types of `FeeData` that may
         * apply to the same base transaction type (associated with
         * an `HederaFunctionality`).
         * @member {proto.SubType} subType
         * @memberof proto.FeeData
         * @instance
         */
        FeeData.prototype.subType = 0;

        /**
         * Creates a new FeeData instance using the specified properties.
         * @function create
         * @memberof proto.FeeData
         * @static
         * @param {proto.FeeData.$Properties=} [properties] Properties to set
         * @returns {proto.FeeData} FeeData instance
         * @type {{
         *   (properties: proto.FeeData.$Shape): proto.FeeData & proto.FeeData.$Shape;
         *   (properties?: proto.FeeData.$Properties): proto.FeeData;
         * }}
         */
        FeeData.create = function(properties) {
            return new FeeData(properties);
        };

        /**
         * Encodes the specified FeeData message. Does not implicitly {@link proto.FeeData.verify|verify} messages.
         * @function encode
         * @memberof proto.FeeData
         * @static
         * @param {proto.FeeData.$Properties} message FeeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeData.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nodedata != null && $Object.hasOwnProperty.call(message, "nodedata"))
                $root.proto.FeeComponents.encode(message.nodedata, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.networkdata != null && $Object.hasOwnProperty.call(message, "networkdata"))
                $root.proto.FeeComponents.encode(message.networkdata, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.servicedata != null && $Object.hasOwnProperty.call(message, "servicedata"))
                $root.proto.FeeComponents.encode(message.servicedata, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.subType != null && $Object.hasOwnProperty.call(message, "subType") && message.subType !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.subType);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FeeData message, length delimited. Does not implicitly {@link proto.FeeData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FeeData
         * @static
         * @param {proto.FeeData.$Properties} message FeeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeData.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FeeData message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FeeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FeeData & proto.FeeData.$Shape} FeeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeData.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FeeData();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.nodedata = $root.proto.FeeComponents.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodedata);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.networkdata = $root.proto.FeeComponents.decode(reader, reader.uint32(), $undefined, _depth + 1, message.networkdata);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.servicedata = $root.proto.FeeComponents.decode(reader, reader.uint32(), $undefined, _depth + 1, message.servicedata);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.subType = value;
                        else
                            delete message.subType;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FeeData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FeeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FeeData & proto.FeeData.$Shape} FeeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeData.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FeeData message.
         * @function verify
         * @memberof proto.FeeData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FeeData.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nodedata != null && $Object.hasOwnProperty.call(message, "nodedata")) {
                let error = $root.proto.FeeComponents.verify(message.nodedata, _depth + 1);
                if (error)
                    return "nodedata." + error;
            }
            if (message.networkdata != null && $Object.hasOwnProperty.call(message, "networkdata")) {
                let error = $root.proto.FeeComponents.verify(message.networkdata, _depth + 1);
                if (error)
                    return "networkdata." + error;
            }
            if (message.servicedata != null && $Object.hasOwnProperty.call(message, "servicedata")) {
                let error = $root.proto.FeeComponents.verify(message.servicedata, _depth + 1);
                if (error)
                    return "servicedata." + error;
            }
            if (message.subType != null && $Object.hasOwnProperty.call(message, "subType"))
                if (typeof message.subType !== "number" || (message.subType | 0) !== message.subType)
                    return "subType: enum value expected";
            return null;
        };

        /**
         * Creates a FeeData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FeeData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FeeData} FeeData
         */
        FeeData.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FeeData)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FeeData: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FeeData();
            if (object.nodedata != null) {
                if (!$util.isObject(object.nodedata))
                    throw $TypeError(".proto.FeeData.nodedata: object expected");
                message.nodedata = $root.proto.FeeComponents.fromObject(object.nodedata, _depth + 1);
            }
            if (object.networkdata != null) {
                if (!$util.isObject(object.networkdata))
                    throw $TypeError(".proto.FeeData.networkdata: object expected");
                message.networkdata = $root.proto.FeeComponents.fromObject(object.networkdata, _depth + 1);
            }
            if (object.servicedata != null) {
                if (!$util.isObject(object.servicedata))
                    throw $TypeError(".proto.FeeData.servicedata: object expected");
                message.servicedata = $root.proto.FeeComponents.fromObject(object.servicedata, _depth + 1);
            }
            if (object.subType !== 0 && (typeof object.subType !== "string" || $root.proto.SubType[object.subType] !== 0))
                switch (object.subType) {
                case "DEFAULT":
                case 0:
                    message.subType = 0;
                    break;
                case "TOKEN_FUNGIBLE_COMMON":
                case 1:
                    message.subType = 1;
                    break;
                case "TOKEN_NON_FUNGIBLE_UNIQUE":
                case 2:
                    message.subType = 2;
                    break;
                case "TOKEN_FUNGIBLE_COMMON_WITH_CUSTOM_FEES":
                case 3:
                    message.subType = 3;
                    break;
                case "TOKEN_NON_FUNGIBLE_UNIQUE_WITH_CUSTOM_FEES":
                case 4:
                    message.subType = 4;
                    break;
                case "SCHEDULE_CREATE_CONTRACT_CALL":
                case 5:
                    message.subType = 5;
                    break;
                case "TOPIC_CREATE_WITH_CUSTOM_FEES":
                case 6:
                    message.subType = 6;
                    break;
                case "SUBMIT_MESSAGE_WITH_CUSTOM_FEES":
                case 7:
                    message.subType = 7;
                    break;
                case "CRYPTO_TRANSFER_WITH_HOOKS":
                case 8:
                    message.subType = 8;
                    break;
                default:
                    if (typeof object.subType === "number" && (object.subType | 0) === object.subType)
                        message.subType = object.subType;
                }
            return message;
        };

        /**
         * Creates a plain object from a FeeData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FeeData
         * @static
         * @param {proto.FeeData} message FeeData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FeeData.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.nodedata = null;
                object.networkdata = null;
                object.servicedata = null;
                object.subType = options.enums === $String ? "DEFAULT" : 0;
            }
            if (message.nodedata != null && $Object.hasOwnProperty.call(message, "nodedata"))
                object.nodedata = $root.proto.FeeComponents.toObject(message.nodedata, options, _depth + 1);
            if (message.networkdata != null && $Object.hasOwnProperty.call(message, "networkdata"))
                object.networkdata = $root.proto.FeeComponents.toObject(message.networkdata, options, _depth + 1);
            if (message.servicedata != null && $Object.hasOwnProperty.call(message, "servicedata"))
                object.servicedata = $root.proto.FeeComponents.toObject(message.servicedata, options, _depth + 1);
            if (message.subType != null && $Object.hasOwnProperty.call(message, "subType"))
                object.subType = options.enums === $String ? $root.proto.SubType[message.subType] === $undefined ? message.subType : $root.proto.SubType[message.subType] : message.subType;
            return object;
        };

        /**
         * Converts this FeeData to JSON.
         * @function toJSON
         * @memberof proto.FeeData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FeeData.prototype.toJSON = function() {
            return FeeData.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FeeData
         * @function getTypeUrl
         * @memberof proto.FeeData
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FeeData.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FeeData";
        };

        return FeeData;
    })();

    proto.FeeSchedule = (function() {

        /**
         * Properties of a FeeSchedule.
         * @typedef {Object} proto.FeeSchedule.$Properties
         * @property {Array.<proto.TransactionFeeSchedule.$Properties>|null} [transactionFeeSchedule] Sets of fee coefficients for various transaction or query types.
         * @property {proto.TimestampSeconds.$Properties|null} [expiryTime] A time, in seconds since the `epoch`, when this fee schedule
         * will expire.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch
         * with 0 at `1970-01-01T00:00:00.000Z`.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FeeSchedule.
         * @memberof proto
         * @interface IFeeSchedule
         * @augments proto.FeeSchedule.$Properties
         * @deprecated Use proto.FeeSchedule.$Properties instead.
         */

        /**
         * Shape of a FeeSchedule.
         * @typedef {proto.FeeSchedule.$Properties} proto.FeeSchedule.$Shape
         */

        /**
         * Constructs a new FeeSchedule.
         * @memberof proto
         * @classdesc A set of fee schedules covering all transaction types and query types, along
         * with a specific time at which this fee schedule will expire.
         * 
         * Nodes SHALL use the most recent unexpired fee schedule to determine the fees
         * for all transactions based on various resource components imputed to each
         * transaction.
         * @constructor
         * @param {proto.FeeSchedule.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FeeSchedule = function (properties) {
            this.transactionFeeSchedule = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Sets of fee coefficients for various transaction or query types.
         * @member {Array.<proto.TransactionFeeSchedule.$Properties>} transactionFeeSchedule
         * @memberof proto.FeeSchedule
         * @instance
         */
        FeeSchedule.prototype.transactionFeeSchedule = $util.emptyArray;

        /**
         * A time, in seconds since the `epoch`, when this fee schedule
         * will expire.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch
         * with 0 at `1970-01-01T00:00:00.000Z`.
         * @member {proto.TimestampSeconds.$Properties|null|undefined} expiryTime
         * @memberof proto.FeeSchedule
         * @instance
         */
        FeeSchedule.prototype.expiryTime = null;

        /**
         * Creates a new FeeSchedule instance using the specified properties.
         * @function create
         * @memberof proto.FeeSchedule
         * @static
         * @param {proto.FeeSchedule.$Properties=} [properties] Properties to set
         * @returns {proto.FeeSchedule} FeeSchedule instance
         * @type {{
         *   (properties: proto.FeeSchedule.$Shape): proto.FeeSchedule & proto.FeeSchedule.$Shape;
         *   (properties?: proto.FeeSchedule.$Properties): proto.FeeSchedule;
         * }}
         */
        FeeSchedule.create = function(properties) {
            return new FeeSchedule(properties);
        };

        /**
         * Encodes the specified FeeSchedule message. Does not implicitly {@link proto.FeeSchedule.verify|verify} messages.
         * @function encode
         * @memberof proto.FeeSchedule
         * @static
         * @param {proto.FeeSchedule.$Properties} message FeeSchedule message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeSchedule.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.transactionFeeSchedule != null && message.transactionFeeSchedule.length)
                for (let i = 0; i < message.transactionFeeSchedule.length; ++i)
                    $root.proto.TransactionFeeSchedule.encode(message.transactionFeeSchedule[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.expiryTime != null && $Object.hasOwnProperty.call(message, "expiryTime"))
                $root.proto.TimestampSeconds.encode(message.expiryTime, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FeeSchedule message, length delimited. Does not implicitly {@link proto.FeeSchedule.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FeeSchedule
         * @static
         * @param {proto.FeeSchedule.$Properties} message FeeSchedule message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeSchedule.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FeeSchedule message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FeeSchedule
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FeeSchedule & proto.FeeSchedule.$Shape} FeeSchedule
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeSchedule.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FeeSchedule();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.transactionFeeSchedule && message.transactionFeeSchedule.length))
                            message.transactionFeeSchedule = [];
                        message.transactionFeeSchedule.push($root.proto.TransactionFeeSchedule.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.expiryTime = $root.proto.TimestampSeconds.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expiryTime);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FeeSchedule message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FeeSchedule
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FeeSchedule & proto.FeeSchedule.$Shape} FeeSchedule
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeSchedule.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FeeSchedule message.
         * @function verify
         * @memberof proto.FeeSchedule
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FeeSchedule.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.transactionFeeSchedule != null && $Object.hasOwnProperty.call(message, "transactionFeeSchedule")) {
                if (!$Array.isArray(message.transactionFeeSchedule))
                    return "transactionFeeSchedule: array expected";
                for (let i = 0; i < message.transactionFeeSchedule.length; ++i) {
                    let error = $root.proto.TransactionFeeSchedule.verify(message.transactionFeeSchedule[i], _depth + 1);
                    if (error)
                        return "transactionFeeSchedule." + error;
                }
            }
            if (message.expiryTime != null && $Object.hasOwnProperty.call(message, "expiryTime")) {
                let error = $root.proto.TimestampSeconds.verify(message.expiryTime, _depth + 1);
                if (error)
                    return "expiryTime." + error;
            }
            return null;
        };

        /**
         * Creates a FeeSchedule message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FeeSchedule
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FeeSchedule} FeeSchedule
         */
        FeeSchedule.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FeeSchedule)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FeeSchedule: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FeeSchedule();
            if (object.transactionFeeSchedule) {
                if (!$Array.isArray(object.transactionFeeSchedule))
                    throw $TypeError(".proto.FeeSchedule.transactionFeeSchedule: array expected");
                message.transactionFeeSchedule = $Array(object.transactionFeeSchedule.length);
                for (let i = 0; i < object.transactionFeeSchedule.length; ++i) {
                    if (!$util.isObject(object.transactionFeeSchedule[i]))
                        throw $TypeError(".proto.FeeSchedule.transactionFeeSchedule: object expected");
                    message.transactionFeeSchedule[i] = $root.proto.TransactionFeeSchedule.fromObject(object.transactionFeeSchedule[i], _depth + 1);
                }
            }
            if (object.expiryTime != null) {
                if (!$util.isObject(object.expiryTime))
                    throw $TypeError(".proto.FeeSchedule.expiryTime: object expected");
                message.expiryTime = $root.proto.TimestampSeconds.fromObject(object.expiryTime, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a FeeSchedule message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FeeSchedule
         * @static
         * @param {proto.FeeSchedule} message FeeSchedule
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FeeSchedule.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.transactionFeeSchedule = [];
            if (options.defaults)
                object.expiryTime = null;
            if (message.transactionFeeSchedule && message.transactionFeeSchedule.length) {
                object.transactionFeeSchedule = $Array(message.transactionFeeSchedule.length);
                for (let j = 0; j < message.transactionFeeSchedule.length; ++j)
                    object.transactionFeeSchedule[j] = $root.proto.TransactionFeeSchedule.toObject(message.transactionFeeSchedule[j], options, _depth + 1);
            }
            if (message.expiryTime != null && $Object.hasOwnProperty.call(message, "expiryTime"))
                object.expiryTime = $root.proto.TimestampSeconds.toObject(message.expiryTime, options, _depth + 1);
            return object;
        };

        /**
         * Converts this FeeSchedule to JSON.
         * @function toJSON
         * @memberof proto.FeeSchedule
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FeeSchedule.prototype.toJSON = function() {
            return FeeSchedule.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FeeSchedule
         * @function getTypeUrl
         * @memberof proto.FeeSchedule
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FeeSchedule.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FeeSchedule";
        };

        return FeeSchedule;
    })();

    proto.CurrentAndNextFeeSchedule = (function() {

        /**
         * Properties of a CurrentAndNextFeeSchedule.
         * @typedef {Object} proto.CurrentAndNextFeeSchedule.$Properties
         * @property {proto.FeeSchedule.$Properties|null} [currentFeeSchedule] A current, unexpired, fee schedule.
         * @property {proto.FeeSchedule.$Properties|null} [nextFeeSchedule] A future fee schedule to use when the current schedule expires.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CurrentAndNextFeeSchedule.
         * @memberof proto
         * @interface ICurrentAndNextFeeSchedule
         * @augments proto.CurrentAndNextFeeSchedule.$Properties
         * @deprecated Use proto.CurrentAndNextFeeSchedule.$Properties instead.
         */

        /**
         * Shape of a CurrentAndNextFeeSchedule.
         * @typedef {proto.CurrentAndNextFeeSchedule.$Properties} proto.CurrentAndNextFeeSchedule.$Shape
         */

        /**
         * Constructs a new CurrentAndNextFeeSchedule.
         * @memberof proto
         * @classdesc The "current" fee schedule and the "next" fee schedule.
         * 
         * The current fee schedule is the schedule that SHALL apply to the current
         * transaction.<br/>
         * The next fee schedule is the schedule that SHALL apply after the current
         * schedule expires.<br/>
         * We store both to avoid a condition where transactions are processed very
         * near the time when a fee schedule expires and it might be indeterminate
         * which fees to apply. With both current and next fee schedule the network
         * can deterministically apply the correct fee schedule based on consensus
         * timestamp for each transaction.
         * @constructor
         * @param {proto.CurrentAndNextFeeSchedule.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CurrentAndNextFeeSchedule = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A current, unexpired, fee schedule.
         * @member {proto.FeeSchedule.$Properties|null|undefined} currentFeeSchedule
         * @memberof proto.CurrentAndNextFeeSchedule
         * @instance
         */
        CurrentAndNextFeeSchedule.prototype.currentFeeSchedule = null;

        /**
         * A future fee schedule to use when the current schedule expires.
         * @member {proto.FeeSchedule.$Properties|null|undefined} nextFeeSchedule
         * @memberof proto.CurrentAndNextFeeSchedule
         * @instance
         */
        CurrentAndNextFeeSchedule.prototype.nextFeeSchedule = null;

        /**
         * Creates a new CurrentAndNextFeeSchedule instance using the specified properties.
         * @function create
         * @memberof proto.CurrentAndNextFeeSchedule
         * @static
         * @param {proto.CurrentAndNextFeeSchedule.$Properties=} [properties] Properties to set
         * @returns {proto.CurrentAndNextFeeSchedule} CurrentAndNextFeeSchedule instance
         * @type {{
         *   (properties: proto.CurrentAndNextFeeSchedule.$Shape): proto.CurrentAndNextFeeSchedule & proto.CurrentAndNextFeeSchedule.$Shape;
         *   (properties?: proto.CurrentAndNextFeeSchedule.$Properties): proto.CurrentAndNextFeeSchedule;
         * }}
         */
        CurrentAndNextFeeSchedule.create = function(properties) {
            return new CurrentAndNextFeeSchedule(properties);
        };

        /**
         * Encodes the specified CurrentAndNextFeeSchedule message. Does not implicitly {@link proto.CurrentAndNextFeeSchedule.verify|verify} messages.
         * @function encode
         * @memberof proto.CurrentAndNextFeeSchedule
         * @static
         * @param {proto.CurrentAndNextFeeSchedule.$Properties} message CurrentAndNextFeeSchedule message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CurrentAndNextFeeSchedule.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.currentFeeSchedule != null && $Object.hasOwnProperty.call(message, "currentFeeSchedule"))
                $root.proto.FeeSchedule.encode(message.currentFeeSchedule, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.nextFeeSchedule != null && $Object.hasOwnProperty.call(message, "nextFeeSchedule"))
                $root.proto.FeeSchedule.encode(message.nextFeeSchedule, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CurrentAndNextFeeSchedule message, length delimited. Does not implicitly {@link proto.CurrentAndNextFeeSchedule.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CurrentAndNextFeeSchedule
         * @static
         * @param {proto.CurrentAndNextFeeSchedule.$Properties} message CurrentAndNextFeeSchedule message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CurrentAndNextFeeSchedule.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CurrentAndNextFeeSchedule message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CurrentAndNextFeeSchedule
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CurrentAndNextFeeSchedule & proto.CurrentAndNextFeeSchedule.$Shape} CurrentAndNextFeeSchedule
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CurrentAndNextFeeSchedule.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CurrentAndNextFeeSchedule();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.currentFeeSchedule = $root.proto.FeeSchedule.decode(reader, reader.uint32(), $undefined, _depth + 1, message.currentFeeSchedule);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.nextFeeSchedule = $root.proto.FeeSchedule.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nextFeeSchedule);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CurrentAndNextFeeSchedule message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CurrentAndNextFeeSchedule
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CurrentAndNextFeeSchedule & proto.CurrentAndNextFeeSchedule.$Shape} CurrentAndNextFeeSchedule
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CurrentAndNextFeeSchedule.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CurrentAndNextFeeSchedule message.
         * @function verify
         * @memberof proto.CurrentAndNextFeeSchedule
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CurrentAndNextFeeSchedule.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.currentFeeSchedule != null && $Object.hasOwnProperty.call(message, "currentFeeSchedule")) {
                let error = $root.proto.FeeSchedule.verify(message.currentFeeSchedule, _depth + 1);
                if (error)
                    return "currentFeeSchedule." + error;
            }
            if (message.nextFeeSchedule != null && $Object.hasOwnProperty.call(message, "nextFeeSchedule")) {
                let error = $root.proto.FeeSchedule.verify(message.nextFeeSchedule, _depth + 1);
                if (error)
                    return "nextFeeSchedule." + error;
            }
            return null;
        };

        /**
         * Creates a CurrentAndNextFeeSchedule message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CurrentAndNextFeeSchedule
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CurrentAndNextFeeSchedule} CurrentAndNextFeeSchedule
         */
        CurrentAndNextFeeSchedule.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CurrentAndNextFeeSchedule)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CurrentAndNextFeeSchedule: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CurrentAndNextFeeSchedule();
            if (object.currentFeeSchedule != null) {
                if (!$util.isObject(object.currentFeeSchedule))
                    throw $TypeError(".proto.CurrentAndNextFeeSchedule.currentFeeSchedule: object expected");
                message.currentFeeSchedule = $root.proto.FeeSchedule.fromObject(object.currentFeeSchedule, _depth + 1);
            }
            if (object.nextFeeSchedule != null) {
                if (!$util.isObject(object.nextFeeSchedule))
                    throw $TypeError(".proto.CurrentAndNextFeeSchedule.nextFeeSchedule: object expected");
                message.nextFeeSchedule = $root.proto.FeeSchedule.fromObject(object.nextFeeSchedule, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CurrentAndNextFeeSchedule message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CurrentAndNextFeeSchedule
         * @static
         * @param {proto.CurrentAndNextFeeSchedule} message CurrentAndNextFeeSchedule
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CurrentAndNextFeeSchedule.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.currentFeeSchedule = null;
                object.nextFeeSchedule = null;
            }
            if (message.currentFeeSchedule != null && $Object.hasOwnProperty.call(message, "currentFeeSchedule"))
                object.currentFeeSchedule = $root.proto.FeeSchedule.toObject(message.currentFeeSchedule, options, _depth + 1);
            if (message.nextFeeSchedule != null && $Object.hasOwnProperty.call(message, "nextFeeSchedule"))
                object.nextFeeSchedule = $root.proto.FeeSchedule.toObject(message.nextFeeSchedule, options, _depth + 1);
            return object;
        };

        /**
         * Converts this CurrentAndNextFeeSchedule to JSON.
         * @function toJSON
         * @memberof proto.CurrentAndNextFeeSchedule
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CurrentAndNextFeeSchedule.prototype.toJSON = function() {
            return CurrentAndNextFeeSchedule.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CurrentAndNextFeeSchedule
         * @function getTypeUrl
         * @memberof proto.CurrentAndNextFeeSchedule
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CurrentAndNextFeeSchedule.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CurrentAndNextFeeSchedule";
        };

        return CurrentAndNextFeeSchedule;
    })();

    proto.ServiceEndpoint = (function() {

        /**
         * Properties of a ServiceEndpoint.
         * @typedef {Object} proto.ServiceEndpoint.$Properties
         * @property {Uint8Array|null} [ipAddressV4] A 32-bit IPv4 address.<br/>
         * This is the address of the endpoint, encoded in pure "big-endian"
         * (i.e. left to right) order (e.g. `127.0.0.1` has hex bytes in the
         * order `7F`, `00`, `00`, `01`).
         * @property {number|null} [port] A TCP port to use.
         * <p>
         * This value MUST be between 0 and 65535, inclusive.
         * @property {string|null} [domainName] A node domain name.
         * <p>
         * This MUST be the fully qualified domain name of the node.<br/>
         * This value MUST NOT exceed 253 characters.<br/>
         * When the `domain_name` field is set, the `ipAddressV4`
         * field MUST NOT be set.<br/>
         * When the `ipAddressV4` field is set, the `domain_name`
         * field MUST NOT be set.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ServiceEndpoint.
         * @memberof proto
         * @interface IServiceEndpoint
         * @augments proto.ServiceEndpoint.$Properties
         * @deprecated Use proto.ServiceEndpoint.$Properties instead.
         */

        /**
         * Shape of a ServiceEndpoint.
         * @typedef {proto.ServiceEndpoint.$Properties} proto.ServiceEndpoint.$Shape
         */

        /**
         * Constructs a new ServiceEndpoint.
         * @memberof proto
         * @classdesc A network node endpoint.<br/>
         * Each network node in the global address book publishes one or more endpoints
         * which enable the nodes to communicate both with other nodes, for gossip, and
         * with clients to receive transaction requests.
         * 
         * This message supports IPv4 with address and TCP port,
         * and MAY include a FQDN instead of an IP address.<br/>
         * IPv6 is not currently supported.
         * 
         * When the `domain_name` field is set, the `ipAddressV4` field
         * MUST NOT be set.<br/>
         * When the `ipAddressV4` field is set, the `domain_name` field
         * MUST NOT be set.
         * @constructor
         * @param {proto.ServiceEndpoint.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ServiceEndpoint = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A 32-bit IPv4 address.<br/>
         * This is the address of the endpoint, encoded in pure "big-endian"
         * (i.e. left to right) order (e.g. `127.0.0.1` has hex bytes in the
         * order `7F`, `00`, `00`, `01`).
         * @member {Uint8Array} ipAddressV4
         * @memberof proto.ServiceEndpoint
         * @instance
         */
        ServiceEndpoint.prototype.ipAddressV4 = $util.newBuffer([]);

        /**
         * A TCP port to use.
         * <p>
         * This value MUST be between 0 and 65535, inclusive.
         * @member {number} port
         * @memberof proto.ServiceEndpoint
         * @instance
         */
        ServiceEndpoint.prototype.port = 0;

        /**
         * A node domain name.
         * <p>
         * This MUST be the fully qualified domain name of the node.<br/>
         * This value MUST NOT exceed 253 characters.<br/>
         * When the `domain_name` field is set, the `ipAddressV4`
         * field MUST NOT be set.<br/>
         * When the `ipAddressV4` field is set, the `domain_name`
         * field MUST NOT be set.
         * @member {string} domainName
         * @memberof proto.ServiceEndpoint
         * @instance
         */
        ServiceEndpoint.prototype.domainName = "";

        /**
         * Creates a new ServiceEndpoint instance using the specified properties.
         * @function create
         * @memberof proto.ServiceEndpoint
         * @static
         * @param {proto.ServiceEndpoint.$Properties=} [properties] Properties to set
         * @returns {proto.ServiceEndpoint} ServiceEndpoint instance
         * @type {{
         *   (properties: proto.ServiceEndpoint.$Shape): proto.ServiceEndpoint & proto.ServiceEndpoint.$Shape;
         *   (properties?: proto.ServiceEndpoint.$Properties): proto.ServiceEndpoint;
         * }}
         */
        ServiceEndpoint.create = function(properties) {
            return new ServiceEndpoint(properties);
        };

        /**
         * Encodes the specified ServiceEndpoint message. Does not implicitly {@link proto.ServiceEndpoint.verify|verify} messages.
         * @function encode
         * @memberof proto.ServiceEndpoint
         * @static
         * @param {proto.ServiceEndpoint.$Properties} message ServiceEndpoint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServiceEndpoint.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.ipAddressV4 != null && $Object.hasOwnProperty.call(message, "ipAddressV4") && message.ipAddressV4.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ipAddressV4);
            if (message.port != null && $Object.hasOwnProperty.call(message, "port") && message.port !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.port);
            if (message.domainName != null && $Object.hasOwnProperty.call(message, "domainName") && message.domainName !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.domainName);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ServiceEndpoint message, length delimited. Does not implicitly {@link proto.ServiceEndpoint.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ServiceEndpoint
         * @static
         * @param {proto.ServiceEndpoint.$Properties} message ServiceEndpoint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServiceEndpoint.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ServiceEndpoint message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ServiceEndpoint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ServiceEndpoint & proto.ServiceEndpoint.$Shape} ServiceEndpoint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServiceEndpoint.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ServiceEndpoint();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.ipAddressV4 = value;
                        else
                            delete message.ipAddressV4;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.port = value;
                        else
                            delete message.port;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.domainName = value;
                        else
                            delete message.domainName;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ServiceEndpoint message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ServiceEndpoint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ServiceEndpoint & proto.ServiceEndpoint.$Shape} ServiceEndpoint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServiceEndpoint.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ServiceEndpoint message.
         * @function verify
         * @memberof proto.ServiceEndpoint
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ServiceEndpoint.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.ipAddressV4 != null && $Object.hasOwnProperty.call(message, "ipAddressV4"))
                if (!(message.ipAddressV4 && typeof message.ipAddressV4.length === "number" || $util.isString(message.ipAddressV4)))
                    return "ipAddressV4: buffer expected";
            if (message.port != null && $Object.hasOwnProperty.call(message, "port"))
                if (!$util.isInteger(message.port))
                    return "port: integer expected";
            if (message.domainName != null && $Object.hasOwnProperty.call(message, "domainName"))
                if (!$util.isString(message.domainName))
                    return "domainName: string expected";
            return null;
        };

        /**
         * Creates a ServiceEndpoint message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ServiceEndpoint
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ServiceEndpoint} ServiceEndpoint
         */
        ServiceEndpoint.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ServiceEndpoint)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ServiceEndpoint: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ServiceEndpoint();
            if (object.ipAddressV4 != null)
                if (object.ipAddressV4.length)
                    if (typeof object.ipAddressV4 === "string")
                        $util.base64.decode(object.ipAddressV4, message.ipAddressV4 = $util.newBuffer($util.base64.length(object.ipAddressV4)), 0);
                    else if (object.ipAddressV4.length >= 0)
                        message.ipAddressV4 = object.ipAddressV4;
            if (object.port != null)
                if ($Number(object.port) !== 0)
                    message.port = object.port | 0;
            if (object.domainName != null)
                if (typeof object.domainName !== "string" || object.domainName.length)
                    message.domainName = $String(object.domainName);
            return message;
        };

        /**
         * Creates a plain object from a ServiceEndpoint message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ServiceEndpoint
         * @static
         * @param {proto.ServiceEndpoint} message ServiceEndpoint
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ServiceEndpoint.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.ipAddressV4 = "";
                else {
                    object.ipAddressV4 = [];
                    if (options.bytes !== $Array)
                        object.ipAddressV4 = $util.newBuffer(object.ipAddressV4);
                }
                object.port = 0;
                object.domainName = "";
            }
            if (message.ipAddressV4 != null && $Object.hasOwnProperty.call(message, "ipAddressV4"))
                object.ipAddressV4 = options.bytes === $String ? $util.base64.encode(message.ipAddressV4, 0, message.ipAddressV4.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ipAddressV4) : message.ipAddressV4;
            if (message.port != null && $Object.hasOwnProperty.call(message, "port"))
                object.port = message.port;
            if (message.domainName != null && $Object.hasOwnProperty.call(message, "domainName"))
                object.domainName = message.domainName;
            return object;
        };

        /**
         * Converts this ServiceEndpoint to JSON.
         * @function toJSON
         * @memberof proto.ServiceEndpoint
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ServiceEndpoint.prototype.toJSON = function() {
            return ServiceEndpoint.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ServiceEndpoint
         * @function getTypeUrl
         * @memberof proto.ServiceEndpoint
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ServiceEndpoint.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ServiceEndpoint";
        };

        return ServiceEndpoint;
    })();

    proto.NodeAddress = (function() {

        /**
         * Properties of a NodeAddress.
         * @typedef {Object} proto.NodeAddress.$Properties
         * @property {Uint8Array|null} [ipAddress] ServiceEndpoint is now used to retrieve a node's list of IP
         * addresses and ports.<br/>
         * The IP address of the Node, as a string, encoded in UTF-8.<br/>
         * This value SHALL NOT be populated.
         * @property {number|null} [portno] ServiceEndpoint is now used to retrieve a node's list of IP
         * addresses and ports.<br/>
         * The port number of the grpc server for the node.<br/>
         * This value SHALL NOT be populated.
         * @property {Uint8Array|null} [memo] Description provides short text functionality.<br/>
         * A short description of the node.
         * <p>
         * This field SHALL NOT be populated.
         * @property {string|null} [RSA_PubKey] A hexadecimal String encoding of an X509 public key.
         * <p>
         * This X509 RSA _public_ key SHALL be used to verify record stream files
         * (e.g., record stream files).<br/>
         * This field SHALL be a string of hexadecimal characters, encoded UTF-8,
         * which, translated to binary, form the public key DER encoding.
         * @property {number|Long|null} [nodeId] A numeric identifier for the node.
         * <p>
         * This value SHALL NOT be sequential.
         * <p>
         * A `0.0.101` field
         * @property {proto.AccountID.$Properties|null} [nodeAccountId] An account to be paid the "node" portion of transaction fees.<br/>
         * The "node" fees are paid to the node that submitted the transaction.
         * <p>
         * A `0.0.101` field
         * @property {Uint8Array|null} [nodeCertHash] A hash of the node's TLS certificate.
         * <p>
         * This field SHALL be a string of hexadecimal characters, encoded UTF-8,
         * which, translated to binary, form a SHA-384 hash of the node's TLS
         * certificate in PEM format.
         * This TLS certificate MUST be encoded UTF-8 and normalized according to
         * the NFKD form prior to computing the hash value.<br/>
         * The value of this field SHALL be used to verify the node TLS
         * certificate when presented during protocol negotiation.
         * <p>
         * A `0.0.101` field
         * @property {Array.<proto.ServiceEndpoint.$Properties>|null} [serviceEndpoint] A node's service IP addresses and TCP ports.<br/>
         * Nodes require multiple endpoints to ensure that inter-node communication
         * (e.g. gossip) is properly separated from client communication to
         * API endpoints.
         * <p>
         * A `0.0.101` field
         * @property {string|null} [description] A short description of the node.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {number|Long|null} [stake] This is replaced by per-account stake tracking and dynamic
         * calculation.<br/>
         * The amount of tinybar staked to the node.<br/>
         * This value SHOULD NOT be populated, and SHALL be ignored.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NodeAddress.
         * @memberof proto
         * @interface INodeAddress
         * @augments proto.NodeAddress.$Properties
         * @deprecated Use proto.NodeAddress.$Properties instead.
         */

        /**
         * Shape of a NodeAddress.
         * @typedef {{
         *   ipAddress?: Uint8Array|null;
         *   portno?: number|null;
         *   memo?: Uint8Array|null;
         *   RSA_PubKey?: string|null;
         *   nodeId?: number|Long|null;
         *   nodeAccountId?: proto.AccountID.$Shape|null;
         *   nodeCertHash?: Uint8Array|null;
         *   serviceEndpoint?: Array.<proto.ServiceEndpoint.$Shape>|null;
         *   description?: string|null;
         *   stake?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.NodeAddress.$Shape
         */

        /**
         * Constructs a new NodeAddress.
         * @memberof proto
         * @classdesc The data about a node, including its service endpoints and the Hedera account
         * to be paid for services provided by the node (that is, queries answered and
         * transactions submitted).
         * 
         * All active fields are populated in the `0.0.102` address book file.<br/>
         * Only fields documented with "`0.0.101` field" are populated in the 0.0.101
         * address book file.
         * 
         * This message MAY be superseded by messages in state/addressbook/node.proto
         * and node_get_info.proto.
         * @constructor
         * @param {proto.NodeAddress.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NodeAddress = function (properties) {
            this.serviceEndpoint = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ServiceEndpoint is now used to retrieve a node's list of IP
         * addresses and ports.<br/>
         * The IP address of the Node, as a string, encoded in UTF-8.<br/>
         * This value SHALL NOT be populated.
         * @member {Uint8Array} ipAddress
         * @memberof proto.NodeAddress
         * @instance
         */
        NodeAddress.prototype.ipAddress = $util.newBuffer([]);

        /**
         * ServiceEndpoint is now used to retrieve a node's list of IP
         * addresses and ports.<br/>
         * The port number of the grpc server for the node.<br/>
         * This value SHALL NOT be populated.
         * @member {number} portno
         * @memberof proto.NodeAddress
         * @instance
         */
        NodeAddress.prototype.portno = 0;

        /**
         * Description provides short text functionality.<br/>
         * A short description of the node.
         * <p>
         * This field SHALL NOT be populated.
         * @member {Uint8Array} memo
         * @memberof proto.NodeAddress
         * @instance
         */
        NodeAddress.prototype.memo = $util.newBuffer([]);

        /**
         * A hexadecimal String encoding of an X509 public key.
         * <p>
         * This X509 RSA _public_ key SHALL be used to verify record stream files
         * (e.g., record stream files).<br/>
         * This field SHALL be a string of hexadecimal characters, encoded UTF-8,
         * which, translated to binary, form the public key DER encoding.
         * @member {string} RSA_PubKey
         * @memberof proto.NodeAddress
         * @instance
         */
        NodeAddress.prototype.RSA_PubKey = "";

        /**
         * A numeric identifier for the node.
         * <p>
         * This value SHALL NOT be sequential.
         * <p>
         * A `0.0.101` field
         * @member {number|Long} nodeId
         * @memberof proto.NodeAddress
         * @instance
         */
        NodeAddress.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An account to be paid the "node" portion of transaction fees.<br/>
         * The "node" fees are paid to the node that submitted the transaction.
         * <p>
         * A `0.0.101` field
         * @member {proto.AccountID.$Properties|null|undefined} nodeAccountId
         * @memberof proto.NodeAddress
         * @instance
         */
        NodeAddress.prototype.nodeAccountId = null;

        /**
         * A hash of the node's TLS certificate.
         * <p>
         * This field SHALL be a string of hexadecimal characters, encoded UTF-8,
         * which, translated to binary, form a SHA-384 hash of the node's TLS
         * certificate in PEM format.
         * This TLS certificate MUST be encoded UTF-8 and normalized according to
         * the NFKD form prior to computing the hash value.<br/>
         * The value of this field SHALL be used to verify the node TLS
         * certificate when presented during protocol negotiation.
         * <p>
         * A `0.0.101` field
         * @member {Uint8Array} nodeCertHash
         * @memberof proto.NodeAddress
         * @instance
         */
        NodeAddress.prototype.nodeCertHash = $util.newBuffer([]);

        /**
         * A node's service IP addresses and TCP ports.<br/>
         * Nodes require multiple endpoints to ensure that inter-node communication
         * (e.g. gossip) is properly separated from client communication to
         * API endpoints.
         * <p>
         * A `0.0.101` field
         * @member {Array.<proto.ServiceEndpoint.$Properties>} serviceEndpoint
         * @memberof proto.NodeAddress
         * @instance
         */
        NodeAddress.prototype.serviceEndpoint = $util.emptyArray;

        /**
         * A short description of the node.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} description
         * @memberof proto.NodeAddress
         * @instance
         */
        NodeAddress.prototype.description = "";

        /**
         * This is replaced by per-account stake tracking and dynamic
         * calculation.<br/>
         * The amount of tinybar staked to the node.<br/>
         * This value SHOULD NOT be populated, and SHALL be ignored.
         * @member {number|Long} stake
         * @memberof proto.NodeAddress
         * @instance
         */
        NodeAddress.prototype.stake = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new NodeAddress instance using the specified properties.
         * @function create
         * @memberof proto.NodeAddress
         * @static
         * @param {proto.NodeAddress.$Properties=} [properties] Properties to set
         * @returns {proto.NodeAddress} NodeAddress instance
         * @type {{
         *   (properties: proto.NodeAddress.$Shape): proto.NodeAddress & proto.NodeAddress.$Shape;
         *   (properties?: proto.NodeAddress.$Properties): proto.NodeAddress;
         * }}
         */
        NodeAddress.create = function(properties) {
            return new NodeAddress(properties);
        };

        /**
         * Encodes the specified NodeAddress message. Does not implicitly {@link proto.NodeAddress.verify|verify} messages.
         * @function encode
         * @memberof proto.NodeAddress
         * @static
         * @param {proto.NodeAddress.$Properties} message NodeAddress message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeAddress.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.ipAddress != null && $Object.hasOwnProperty.call(message, "ipAddress") && message.ipAddress.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ipAddress);
            if (message.portno != null && $Object.hasOwnProperty.call(message, "portno") && message.portno !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.portno);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.memo);
            if (message.RSA_PubKey != null && $Object.hasOwnProperty.call(message, "RSA_PubKey") && message.RSA_PubKey !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.RSA_PubKey);
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.nodeId);
            if (message.nodeAccountId != null && $Object.hasOwnProperty.call(message, "nodeAccountId"))
                $root.proto.AccountID.encode(message.nodeAccountId, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.nodeCertHash != null && $Object.hasOwnProperty.call(message, "nodeCertHash") && message.nodeCertHash.length)
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.nodeCertHash);
            if (message.serviceEndpoint != null && message.serviceEndpoint.length)
                for (let i = 0; i < message.serviceEndpoint.length; ++i)
                    $root.proto.ServiceEndpoint.encode(message.serviceEndpoint[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.description);
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake") && (typeof message.stake === "object" ? message.stake.low || message.stake.high : message.stake !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.stake);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeAddress message, length delimited. Does not implicitly {@link proto.NodeAddress.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NodeAddress
         * @static
         * @param {proto.NodeAddress.$Properties} message NodeAddress message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeAddress.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NodeAddress message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NodeAddress
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NodeAddress & proto.NodeAddress.$Shape} NodeAddress
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeAddress.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NodeAddress();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.ipAddress = value;
                        else
                            delete message.ipAddress;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.portno = value;
                        else
                            delete message.portno;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.RSA_PubKey = value;
                        else
                            delete message.RSA_PubKey;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nodeId = value;
                        else
                            delete message.nodeId;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.nodeAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodeAccountId);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.nodeCertHash = value;
                        else
                            delete message.nodeCertHash;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.serviceEndpoint && message.serviceEndpoint.length))
                            message.serviceEndpoint = [];
                        message.serviceEndpoint.push($root.proto.ServiceEndpoint.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.description = value;
                        else
                            delete message.description;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stake = value;
                        else
                            delete message.stake;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeAddress message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NodeAddress
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NodeAddress & proto.NodeAddress.$Shape} NodeAddress
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeAddress.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeAddress message.
         * @function verify
         * @memberof proto.NodeAddress
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeAddress.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.ipAddress != null && $Object.hasOwnProperty.call(message, "ipAddress"))
                if (!(message.ipAddress && typeof message.ipAddress.length === "number" || $util.isString(message.ipAddress)))
                    return "ipAddress: buffer expected";
            if (message.portno != null && $Object.hasOwnProperty.call(message, "portno"))
                if (!$util.isInteger(message.portno))
                    return "portno: integer expected";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!(message.memo && typeof message.memo.length === "number" || $util.isString(message.memo)))
                    return "memo: buffer expected";
            if (message.RSA_PubKey != null && $Object.hasOwnProperty.call(message, "RSA_PubKey"))
                if (!$util.isString(message.RSA_PubKey))
                    return "RSA_PubKey: string expected";
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                    return "nodeId: integer|Long expected";
            if (message.nodeAccountId != null && $Object.hasOwnProperty.call(message, "nodeAccountId")) {
                let error = $root.proto.AccountID.verify(message.nodeAccountId, _depth + 1);
                if (error)
                    return "nodeAccountId." + error;
            }
            if (message.nodeCertHash != null && $Object.hasOwnProperty.call(message, "nodeCertHash"))
                if (!(message.nodeCertHash && typeof message.nodeCertHash.length === "number" || $util.isString(message.nodeCertHash)))
                    return "nodeCertHash: buffer expected";
            if (message.serviceEndpoint != null && $Object.hasOwnProperty.call(message, "serviceEndpoint")) {
                if (!$Array.isArray(message.serviceEndpoint))
                    return "serviceEndpoint: array expected";
                for (let i = 0; i < message.serviceEndpoint.length; ++i) {
                    let error = $root.proto.ServiceEndpoint.verify(message.serviceEndpoint[i], _depth + 1);
                    if (error)
                        return "serviceEndpoint." + error;
                }
            }
            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                if (!$util.isString(message.description))
                    return "description: string expected";
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                if (!$util.isInteger(message.stake) && !(message.stake && $util.isInteger(message.stake.low) && $util.isInteger(message.stake.high)))
                    return "stake: integer|Long expected";
            return null;
        };

        /**
         * Creates a NodeAddress message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NodeAddress
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NodeAddress} NodeAddress
         */
        NodeAddress.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NodeAddress)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NodeAddress: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NodeAddress();
            if (object.ipAddress != null)
                if (object.ipAddress.length)
                    if (typeof object.ipAddress === "string")
                        $util.base64.decode(object.ipAddress, message.ipAddress = $util.newBuffer($util.base64.length(object.ipAddress)), 0);
                    else if (object.ipAddress.length >= 0)
                        message.ipAddress = object.ipAddress;
            if (object.portno != null)
                if ($Number(object.portno) !== 0)
                    message.portno = object.portno | 0;
            if (object.memo != null)
                if (object.memo.length)
                    if (typeof object.memo === "string")
                        $util.base64.decode(object.memo, message.memo = $util.newBuffer($util.base64.length(object.memo)), 0);
                    else if (object.memo.length >= 0)
                        message.memo = object.memo;
            if (object.RSA_PubKey != null)
                if (typeof object.RSA_PubKey !== "string" || object.RSA_PubKey.length)
                    message.RSA_PubKey = $String(object.RSA_PubKey);
            if (object.nodeId != null)
                if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                    if ($util.Long)
                        message.nodeId = $util.Long.fromValue(object.nodeId, false);
                    else if (typeof object.nodeId === "string")
                        message.nodeId = $parseInt(object.nodeId, 10);
                    else if (typeof object.nodeId === "number")
                        message.nodeId = object.nodeId;
                    else if (typeof object.nodeId === "object")
                        message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber();
            if (object.nodeAccountId != null) {
                if (!$util.isObject(object.nodeAccountId))
                    throw $TypeError(".proto.NodeAddress.nodeAccountId: object expected");
                message.nodeAccountId = $root.proto.AccountID.fromObject(object.nodeAccountId, _depth + 1);
            }
            if (object.nodeCertHash != null)
                if (object.nodeCertHash.length)
                    if (typeof object.nodeCertHash === "string")
                        $util.base64.decode(object.nodeCertHash, message.nodeCertHash = $util.newBuffer($util.base64.length(object.nodeCertHash)), 0);
                    else if (object.nodeCertHash.length >= 0)
                        message.nodeCertHash = object.nodeCertHash;
            if (object.serviceEndpoint) {
                if (!$Array.isArray(object.serviceEndpoint))
                    throw $TypeError(".proto.NodeAddress.serviceEndpoint: array expected");
                message.serviceEndpoint = $Array(object.serviceEndpoint.length);
                for (let i = 0; i < object.serviceEndpoint.length; ++i) {
                    if (!$util.isObject(object.serviceEndpoint[i]))
                        throw $TypeError(".proto.NodeAddress.serviceEndpoint: object expected");
                    message.serviceEndpoint[i] = $root.proto.ServiceEndpoint.fromObject(object.serviceEndpoint[i], _depth + 1);
                }
            }
            if (object.description != null)
                if (typeof object.description !== "string" || object.description.length)
                    message.description = $String(object.description);
            if (object.stake != null)
                if (typeof object.stake === "object" ? object.stake.low || object.stake.high : $Number(object.stake) !== 0)
                    if ($util.Long)
                        message.stake = $util.Long.fromValue(object.stake, false);
                    else if (typeof object.stake === "string")
                        message.stake = $parseInt(object.stake, 10);
                    else if (typeof object.stake === "number")
                        message.stake = object.stake;
                    else if (typeof object.stake === "object")
                        message.stake = new $util.LongBits(object.stake.low >>> 0, object.stake.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a NodeAddress message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NodeAddress
         * @static
         * @param {proto.NodeAddress} message NodeAddress
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeAddress.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.serviceEndpoint = [];
            if (options.defaults) {
                if (options.bytes === $String)
                    object.ipAddress = "";
                else {
                    object.ipAddress = [];
                    if (options.bytes !== $Array)
                        object.ipAddress = $util.newBuffer(object.ipAddress);
                }
                object.portno = 0;
                if (options.bytes === $String)
                    object.memo = "";
                else {
                    object.memo = [];
                    if (options.bytes !== $Array)
                        object.memo = $util.newBuffer(object.memo);
                }
                object.RSA_PubKey = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.nodeAccountId = null;
                if (options.bytes === $String)
                    object.nodeCertHash = "";
                else {
                    object.nodeCertHash = [];
                    if (options.bytes !== $Array)
                        object.nodeCertHash = $util.newBuffer(object.nodeCertHash);
                }
                object.description = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stake = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stake = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.ipAddress != null && $Object.hasOwnProperty.call(message, "ipAddress"))
                object.ipAddress = options.bytes === $String ? $util.base64.encode(message.ipAddress, 0, message.ipAddress.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ipAddress) : message.ipAddress;
            if (message.portno != null && $Object.hasOwnProperty.call(message, "portno"))
                object.portno = message.portno;
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = options.bytes === $String ? $util.base64.encode(message.memo, 0, message.memo.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.memo) : message.memo;
            if (message.RSA_PubKey != null && $Object.hasOwnProperty.call(message, "RSA_PubKey"))
                object.RSA_PubKey = message.RSA_PubKey;
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, false).toBigInt();
                else if (typeof message.nodeId === "number")
                    object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                else
                    object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber() : message.nodeId;
            if (message.nodeAccountId != null && $Object.hasOwnProperty.call(message, "nodeAccountId"))
                object.nodeAccountId = $root.proto.AccountID.toObject(message.nodeAccountId, options, _depth + 1);
            if (message.nodeCertHash != null && $Object.hasOwnProperty.call(message, "nodeCertHash"))
                object.nodeCertHash = options.bytes === $String ? $util.base64.encode(message.nodeCertHash, 0, message.nodeCertHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.nodeCertHash) : message.nodeCertHash;
            if (message.serviceEndpoint && message.serviceEndpoint.length) {
                object.serviceEndpoint = $Array(message.serviceEndpoint.length);
                for (let j = 0; j < message.serviceEndpoint.length; ++j)
                    object.serviceEndpoint[j] = $root.proto.ServiceEndpoint.toObject(message.serviceEndpoint[j], options, _depth + 1);
            }
            if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stake = typeof message.stake === "number" ? $BigInt(message.stake) : $util.Long.fromBits(message.stake.low >>> 0, message.stake.high >>> 0, false).toBigInt();
                else if (typeof message.stake === "number")
                    object.stake = options.longs === $String ? $String(message.stake) : message.stake;
                else
                    object.stake = options.longs === $String ? $util.Long.prototype.toString.call(message.stake) : options.longs === $Number ? new $util.LongBits(message.stake.low >>> 0, message.stake.high >>> 0).toNumber() : message.stake;
            return object;
        };

        /**
         * Converts this NodeAddress to JSON.
         * @function toJSON
         * @memberof proto.NodeAddress
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeAddress.prototype.toJSON = function() {
            return NodeAddress.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeAddress
         * @function getTypeUrl
         * @memberof proto.NodeAddress
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeAddress.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NodeAddress";
        };

        return NodeAddress;
    })();

    proto.NodeAddressBook = (function() {

        /**
         * Properties of a NodeAddressBook.
         * @typedef {Object} proto.NodeAddressBook.$Properties
         * @property {Array.<proto.NodeAddress.$Properties>|null} [nodeAddress] Published data for all nodes in the network
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NodeAddressBook.
         * @memberof proto
         * @interface INodeAddressBook
         * @augments proto.NodeAddressBook.$Properties
         * @deprecated Use proto.NodeAddressBook.$Properties instead.
         */

        /**
         * Shape of a NodeAddressBook.
         * @typedef {{
         *   nodeAddress?: Array.<proto.NodeAddress.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.NodeAddressBook.$Shape
         */

        /**
         * Constructs a new NodeAddressBook.
         * @memberof proto
         * @classdesc A list of nodes and their metadata that contains details of the nodes
         * running the network.
         * 
         * Used to parse the contents of system files `0.0.101` and `0.0.102`.
         * @constructor
         * @param {proto.NodeAddressBook.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NodeAddressBook = function (properties) {
            this.nodeAddress = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Published data for all nodes in the network
         * @member {Array.<proto.NodeAddress.$Properties>} nodeAddress
         * @memberof proto.NodeAddressBook
         * @instance
         */
        NodeAddressBook.prototype.nodeAddress = $util.emptyArray;

        /**
         * Creates a new NodeAddressBook instance using the specified properties.
         * @function create
         * @memberof proto.NodeAddressBook
         * @static
         * @param {proto.NodeAddressBook.$Properties=} [properties] Properties to set
         * @returns {proto.NodeAddressBook} NodeAddressBook instance
         * @type {{
         *   (properties: proto.NodeAddressBook.$Shape): proto.NodeAddressBook & proto.NodeAddressBook.$Shape;
         *   (properties?: proto.NodeAddressBook.$Properties): proto.NodeAddressBook;
         * }}
         */
        NodeAddressBook.create = function(properties) {
            return new NodeAddressBook(properties);
        };

        /**
         * Encodes the specified NodeAddressBook message. Does not implicitly {@link proto.NodeAddressBook.verify|verify} messages.
         * @function encode
         * @memberof proto.NodeAddressBook
         * @static
         * @param {proto.NodeAddressBook.$Properties} message NodeAddressBook message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeAddressBook.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nodeAddress != null && message.nodeAddress.length)
                for (let i = 0; i < message.nodeAddress.length; ++i)
                    $root.proto.NodeAddress.encode(message.nodeAddress[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeAddressBook message, length delimited. Does not implicitly {@link proto.NodeAddressBook.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NodeAddressBook
         * @static
         * @param {proto.NodeAddressBook.$Properties} message NodeAddressBook message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeAddressBook.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NodeAddressBook message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NodeAddressBook
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NodeAddressBook & proto.NodeAddressBook.$Shape} NodeAddressBook
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeAddressBook.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NodeAddressBook();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.nodeAddress && message.nodeAddress.length))
                            message.nodeAddress = [];
                        message.nodeAddress.push($root.proto.NodeAddress.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeAddressBook message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NodeAddressBook
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NodeAddressBook & proto.NodeAddressBook.$Shape} NodeAddressBook
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeAddressBook.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeAddressBook message.
         * @function verify
         * @memberof proto.NodeAddressBook
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeAddressBook.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nodeAddress != null && $Object.hasOwnProperty.call(message, "nodeAddress")) {
                if (!$Array.isArray(message.nodeAddress))
                    return "nodeAddress: array expected";
                for (let i = 0; i < message.nodeAddress.length; ++i) {
                    let error = $root.proto.NodeAddress.verify(message.nodeAddress[i], _depth + 1);
                    if (error)
                        return "nodeAddress." + error;
                }
            }
            return null;
        };

        /**
         * Creates a NodeAddressBook message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NodeAddressBook
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NodeAddressBook} NodeAddressBook
         */
        NodeAddressBook.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NodeAddressBook)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NodeAddressBook: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NodeAddressBook();
            if (object.nodeAddress) {
                if (!$Array.isArray(object.nodeAddress))
                    throw $TypeError(".proto.NodeAddressBook.nodeAddress: array expected");
                message.nodeAddress = $Array(object.nodeAddress.length);
                for (let i = 0; i < object.nodeAddress.length; ++i) {
                    if (!$util.isObject(object.nodeAddress[i]))
                        throw $TypeError(".proto.NodeAddressBook.nodeAddress: object expected");
                    message.nodeAddress[i] = $root.proto.NodeAddress.fromObject(object.nodeAddress[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a NodeAddressBook message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NodeAddressBook
         * @static
         * @param {proto.NodeAddressBook} message NodeAddressBook
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeAddressBook.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.nodeAddress = [];
            if (message.nodeAddress && message.nodeAddress.length) {
                object.nodeAddress = $Array(message.nodeAddress.length);
                for (let j = 0; j < message.nodeAddress.length; ++j)
                    object.nodeAddress[j] = $root.proto.NodeAddress.toObject(message.nodeAddress[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this NodeAddressBook to JSON.
         * @function toJSON
         * @memberof proto.NodeAddressBook
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeAddressBook.prototype.toJSON = function() {
            return NodeAddressBook.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeAddressBook
         * @function getTypeUrl
         * @memberof proto.NodeAddressBook
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeAddressBook.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NodeAddressBook";
        };

        return NodeAddressBook;
    })();

    proto.SemanticVersion = (function() {

        /**
         * Properties of a SemanticVersion.
         * @typedef {Object} proto.SemanticVersion.$Properties
         * @property {number|null} [major] A major version.<br/>
         * Hedera does not increment this value and retains a `0` value to
         * indicate that API may change for any release.
         * <p>
         * This value SHALL increment for an incompatible API change.<br/>
         * @property {number|null} [minor] A minor version.<br/>
         * Hedera increments this value with each release.<br/>
         * There may be incompatible API changes in any Hedera Services release.
         * <p>
         * This value SHALL increment for backwards-compatible new
         * functionality.
         * @property {number|null} [patch] A patch version.
         * <p>
         * This value SHALL increment for backwards-compatible bug fixes.
         * @property {string|null} [pre] A pre-release version.
         * <p>
         * This MAY be denoted by appending a hyphen and a series of dot separated
         * identifiers per [Semver Specification](https://semver.org/#spec-item-9);
         * given a string `0.14.0-alpha.1+21AF26D3`, this field would contain
         * 'alpha.1'
         * @property {string|null} [build] A build version.
         * <p>
         * Build version MAY be denoted by appending a plus sign and a series of
         * dot separated identifiers immediately following the patch or pre-release
         * version per [Semver Specification](https://semver.org/#spec-item-10); so
         * given a string `0.14.0-alpha.1+21AF26D3`, this field
         * would contain '21AF26D3'
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SemanticVersion.
         * @memberof proto
         * @interface ISemanticVersion
         * @augments proto.SemanticVersion.$Properties
         * @deprecated Use proto.SemanticVersion.$Properties instead.
         */

        /**
         * Shape of a SemanticVersion.
         * @typedef {proto.SemanticVersion.$Properties} proto.SemanticVersion.$Shape
         */

        /**
         * Constructs a new SemanticVersion.
         * @memberof proto
         * @classdesc A software version according to "[semantic versioning](https://semver.org/)"
         * or "date versioning".
         * 
         * Hedera currently modifies the "typical" semantic versioning somewhat, the
         * `major` version is always `0`, and each release increments the `minor`
         * version. The `patch` and `pre` components are used in the typical manner.
         * The `build` component is not generally used.
         * @constructor
         * @param {proto.SemanticVersion.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SemanticVersion = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A major version.<br/>
         * Hedera does not increment this value and retains a `0` value to
         * indicate that API may change for any release.
         * <p>
         * This value SHALL increment for an incompatible API change.<br/>
         * @member {number} major
         * @memberof proto.SemanticVersion
         * @instance
         */
        SemanticVersion.prototype.major = 0;

        /**
         * A minor version.<br/>
         * Hedera increments this value with each release.<br/>
         * There may be incompatible API changes in any Hedera Services release.
         * <p>
         * This value SHALL increment for backwards-compatible new
         * functionality.
         * @member {number} minor
         * @memberof proto.SemanticVersion
         * @instance
         */
        SemanticVersion.prototype.minor = 0;

        /**
         * A patch version.
         * <p>
         * This value SHALL increment for backwards-compatible bug fixes.
         * @member {number} patch
         * @memberof proto.SemanticVersion
         * @instance
         */
        SemanticVersion.prototype.patch = 0;

        /**
         * A pre-release version.
         * <p>
         * This MAY be denoted by appending a hyphen and a series of dot separated
         * identifiers per [Semver Specification](https://semver.org/#spec-item-9);
         * given a string `0.14.0-alpha.1+21AF26D3`, this field would contain
         * 'alpha.1'
         * @member {string} pre
         * @memberof proto.SemanticVersion
         * @instance
         */
        SemanticVersion.prototype.pre = "";

        /**
         * A build version.
         * <p>
         * Build version MAY be denoted by appending a plus sign and a series of
         * dot separated identifiers immediately following the patch or pre-release
         * version per [Semver Specification](https://semver.org/#spec-item-10); so
         * given a string `0.14.0-alpha.1+21AF26D3`, this field
         * would contain '21AF26D3'
         * @member {string} build
         * @memberof proto.SemanticVersion
         * @instance
         */
        SemanticVersion.prototype.build = "";

        /**
         * Creates a new SemanticVersion instance using the specified properties.
         * @function create
         * @memberof proto.SemanticVersion
         * @static
         * @param {proto.SemanticVersion.$Properties=} [properties] Properties to set
         * @returns {proto.SemanticVersion} SemanticVersion instance
         * @type {{
         *   (properties: proto.SemanticVersion.$Shape): proto.SemanticVersion & proto.SemanticVersion.$Shape;
         *   (properties?: proto.SemanticVersion.$Properties): proto.SemanticVersion;
         * }}
         */
        SemanticVersion.create = function(properties) {
            return new SemanticVersion(properties);
        };

        /**
         * Encodes the specified SemanticVersion message. Does not implicitly {@link proto.SemanticVersion.verify|verify} messages.
         * @function encode
         * @memberof proto.SemanticVersion
         * @static
         * @param {proto.SemanticVersion.$Properties} message SemanticVersion message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SemanticVersion.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.major != null && $Object.hasOwnProperty.call(message, "major") && message.major !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.major);
            if (message.minor != null && $Object.hasOwnProperty.call(message, "minor") && message.minor !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.minor);
            if (message.patch != null && $Object.hasOwnProperty.call(message, "patch") && message.patch !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.patch);
            if (message.pre != null && $Object.hasOwnProperty.call(message, "pre") && message.pre !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.pre);
            if (message.build != null && $Object.hasOwnProperty.call(message, "build") && message.build !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.build);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SemanticVersion message, length delimited. Does not implicitly {@link proto.SemanticVersion.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SemanticVersion
         * @static
         * @param {proto.SemanticVersion.$Properties} message SemanticVersion message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SemanticVersion.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SemanticVersion message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SemanticVersion
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SemanticVersion & proto.SemanticVersion.$Shape} SemanticVersion
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SemanticVersion.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SemanticVersion();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.major = value;
                        else
                            delete message.major;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.minor = value;
                        else
                            delete message.minor;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.patch = value;
                        else
                            delete message.patch;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.pre = value;
                        else
                            delete message.pre;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.build = value;
                        else
                            delete message.build;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SemanticVersion message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SemanticVersion
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SemanticVersion & proto.SemanticVersion.$Shape} SemanticVersion
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SemanticVersion.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SemanticVersion message.
         * @function verify
         * @memberof proto.SemanticVersion
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SemanticVersion.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.major != null && $Object.hasOwnProperty.call(message, "major"))
                if (!$util.isInteger(message.major))
                    return "major: integer expected";
            if (message.minor != null && $Object.hasOwnProperty.call(message, "minor"))
                if (!$util.isInteger(message.minor))
                    return "minor: integer expected";
            if (message.patch != null && $Object.hasOwnProperty.call(message, "patch"))
                if (!$util.isInteger(message.patch))
                    return "patch: integer expected";
            if (message.pre != null && $Object.hasOwnProperty.call(message, "pre"))
                if (!$util.isString(message.pre))
                    return "pre: string expected";
            if (message.build != null && $Object.hasOwnProperty.call(message, "build"))
                if (!$util.isString(message.build))
                    return "build: string expected";
            return null;
        };

        /**
         * Creates a SemanticVersion message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SemanticVersion
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SemanticVersion} SemanticVersion
         */
        SemanticVersion.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SemanticVersion)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SemanticVersion: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SemanticVersion();
            if (object.major != null)
                if ($Number(object.major) !== 0)
                    message.major = object.major | 0;
            if (object.minor != null)
                if ($Number(object.minor) !== 0)
                    message.minor = object.minor | 0;
            if (object.patch != null)
                if ($Number(object.patch) !== 0)
                    message.patch = object.patch | 0;
            if (object.pre != null)
                if (typeof object.pre !== "string" || object.pre.length)
                    message.pre = $String(object.pre);
            if (object.build != null)
                if (typeof object.build !== "string" || object.build.length)
                    message.build = $String(object.build);
            return message;
        };

        /**
         * Creates a plain object from a SemanticVersion message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SemanticVersion
         * @static
         * @param {proto.SemanticVersion} message SemanticVersion
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SemanticVersion.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.major = 0;
                object.minor = 0;
                object.patch = 0;
                object.pre = "";
                object.build = "";
            }
            if (message.major != null && $Object.hasOwnProperty.call(message, "major"))
                object.major = message.major;
            if (message.minor != null && $Object.hasOwnProperty.call(message, "minor"))
                object.minor = message.minor;
            if (message.patch != null && $Object.hasOwnProperty.call(message, "patch"))
                object.patch = message.patch;
            if (message.pre != null && $Object.hasOwnProperty.call(message, "pre"))
                object.pre = message.pre;
            if (message.build != null && $Object.hasOwnProperty.call(message, "build"))
                object.build = message.build;
            return object;
        };

        /**
         * Converts this SemanticVersion to JSON.
         * @function toJSON
         * @memberof proto.SemanticVersion
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SemanticVersion.prototype.toJSON = function() {
            return SemanticVersion.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SemanticVersion
         * @function getTypeUrl
         * @memberof proto.SemanticVersion
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SemanticVersion.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SemanticVersion";
        };

        return SemanticVersion;
    })();

    proto.Setting = (function() {

        /**
         * Properties of a Setting.
         * @typedef {Object} proto.Setting.$Properties
         * @property {string|null} [name] A name for this setting property.
         * @property {string|null} [value] A value for this setting property.
         * @property {Uint8Array|null} [data] A small quantity of data associated with this setting.
         * <p>
         * This SHOULD be less than 100 bytes.<br/>
         * If the value is a string, it MUST be encoded UTF-8.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Setting.
         * @memberof proto
         * @interface ISetting
         * @augments proto.Setting.$Properties
         * @deprecated Use proto.Setting.$Properties instead.
         */

        /**
         * Shape of a Setting.
         * @typedef {proto.Setting.$Properties} proto.Setting.$Shape
         */

        /**
         * Constructs a new Setting.
         * @memberof proto
         * @classdesc A single runtime configuration setting.
         * 
         * Typically a name-value pair, this may also contain a small amount of
         * associated data.
         * @constructor
         * @param {proto.Setting.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Setting = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A name for this setting property.
         * @member {string} name
         * @memberof proto.Setting
         * @instance
         */
        Setting.prototype.name = "";

        /**
         * A value for this setting property.
         * @member {string} value
         * @memberof proto.Setting
         * @instance
         */
        Setting.prototype.value = "";

        /**
         * A small quantity of data associated with this setting.
         * <p>
         * This SHOULD be less than 100 bytes.<br/>
         * If the value is a string, it MUST be encoded UTF-8.
         * @member {Uint8Array} data
         * @memberof proto.Setting
         * @instance
         */
        Setting.prototype.data = $util.newBuffer([]);

        /**
         * Creates a new Setting instance using the specified properties.
         * @function create
         * @memberof proto.Setting
         * @static
         * @param {proto.Setting.$Properties=} [properties] Properties to set
         * @returns {proto.Setting} Setting instance
         * @type {{
         *   (properties: proto.Setting.$Shape): proto.Setting & proto.Setting.$Shape;
         *   (properties?: proto.Setting.$Properties): proto.Setting;
         * }}
         */
        Setting.create = function(properties) {
            return new Setting(properties);
        };

        /**
         * Encodes the specified Setting message. Does not implicitly {@link proto.Setting.verify|verify} messages.
         * @function encode
         * @memberof proto.Setting
         * @static
         * @param {proto.Setting.$Properties} message Setting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Setting.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.value);
            if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Setting message, length delimited. Does not implicitly {@link proto.Setting.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Setting
         * @static
         * @param {proto.Setting.$Properties} message Setting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Setting.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Setting message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Setting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Setting & proto.Setting.$Shape} Setting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Setting.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Setting();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Setting message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Setting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Setting & proto.Setting.$Shape} Setting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Setting.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Setting message.
         * @function verify
         * @memberof proto.Setting
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Setting.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isString(message.value))
                    return "value: string expected";
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            return null;
        };

        /**
         * Creates a Setting message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Setting
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Setting} Setting
         */
        Setting.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Setting)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Setting: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Setting();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.value != null)
                if (typeof object.value !== "string" || object.value.length)
                    message.value = $String(object.value);
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            return message;
        };

        /**
         * Creates a plain object from a Setting message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Setting
         * @static
         * @param {proto.Setting} message Setting
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Setting.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.value = "";
                if (options.bytes === $String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== $Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === $String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        /**
         * Converts this Setting to JSON.
         * @function toJSON
         * @memberof proto.Setting
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Setting.prototype.toJSON = function() {
            return Setting.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Setting
         * @function getTypeUrl
         * @memberof proto.Setting
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Setting.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Setting";
        };

        return Setting;
    })();

    proto.ServicesConfigurationList = (function() {

        /**
         * Properties of a ServicesConfigurationList.
         * @typedef {Object} proto.ServicesConfigurationList.$Properties
         * @property {Array.<proto.Setting.$Properties>|null} [nameValue] A List of `Setting` values, typically read from application properties.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ServicesConfigurationList.
         * @memberof proto
         * @interface IServicesConfigurationList
         * @augments proto.ServicesConfigurationList.$Properties
         * @deprecated Use proto.ServicesConfigurationList.$Properties instead.
         */

        /**
         * Shape of a ServicesConfigurationList.
         * @typedef {proto.ServicesConfigurationList.$Properties} proto.ServicesConfigurationList.$Shape
         */

        /**
         * Constructs a new ServicesConfigurationList.
         * @memberof proto
         * @classdesc Setting values representing a source of runtime configuration information.
         * @constructor
         * @param {proto.ServicesConfigurationList.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ServicesConfigurationList = function (properties) {
            this.nameValue = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A List of `Setting` values, typically read from application properties.
         * @member {Array.<proto.Setting.$Properties>} nameValue
         * @memberof proto.ServicesConfigurationList
         * @instance
         */
        ServicesConfigurationList.prototype.nameValue = $util.emptyArray;

        /**
         * Creates a new ServicesConfigurationList instance using the specified properties.
         * @function create
         * @memberof proto.ServicesConfigurationList
         * @static
         * @param {proto.ServicesConfigurationList.$Properties=} [properties] Properties to set
         * @returns {proto.ServicesConfigurationList} ServicesConfigurationList instance
         * @type {{
         *   (properties: proto.ServicesConfigurationList.$Shape): proto.ServicesConfigurationList & proto.ServicesConfigurationList.$Shape;
         *   (properties?: proto.ServicesConfigurationList.$Properties): proto.ServicesConfigurationList;
         * }}
         */
        ServicesConfigurationList.create = function(properties) {
            return new ServicesConfigurationList(properties);
        };

        /**
         * Encodes the specified ServicesConfigurationList message. Does not implicitly {@link proto.ServicesConfigurationList.verify|verify} messages.
         * @function encode
         * @memberof proto.ServicesConfigurationList
         * @static
         * @param {proto.ServicesConfigurationList.$Properties} message ServicesConfigurationList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServicesConfigurationList.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nameValue != null && message.nameValue.length)
                for (let i = 0; i < message.nameValue.length; ++i)
                    $root.proto.Setting.encode(message.nameValue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ServicesConfigurationList message, length delimited. Does not implicitly {@link proto.ServicesConfigurationList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ServicesConfigurationList
         * @static
         * @param {proto.ServicesConfigurationList.$Properties} message ServicesConfigurationList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServicesConfigurationList.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ServicesConfigurationList message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ServicesConfigurationList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ServicesConfigurationList & proto.ServicesConfigurationList.$Shape} ServicesConfigurationList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServicesConfigurationList.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ServicesConfigurationList();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.nameValue && message.nameValue.length))
                            message.nameValue = [];
                        message.nameValue.push($root.proto.Setting.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ServicesConfigurationList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ServicesConfigurationList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ServicesConfigurationList & proto.ServicesConfigurationList.$Shape} ServicesConfigurationList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServicesConfigurationList.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ServicesConfigurationList message.
         * @function verify
         * @memberof proto.ServicesConfigurationList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ServicesConfigurationList.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nameValue != null && $Object.hasOwnProperty.call(message, "nameValue")) {
                if (!$Array.isArray(message.nameValue))
                    return "nameValue: array expected";
                for (let i = 0; i < message.nameValue.length; ++i) {
                    let error = $root.proto.Setting.verify(message.nameValue[i], _depth + 1);
                    if (error)
                        return "nameValue." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ServicesConfigurationList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ServicesConfigurationList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ServicesConfigurationList} ServicesConfigurationList
         */
        ServicesConfigurationList.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ServicesConfigurationList)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ServicesConfigurationList: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ServicesConfigurationList();
            if (object.nameValue) {
                if (!$Array.isArray(object.nameValue))
                    throw $TypeError(".proto.ServicesConfigurationList.nameValue: array expected");
                message.nameValue = $Array(object.nameValue.length);
                for (let i = 0; i < object.nameValue.length; ++i) {
                    if (!$util.isObject(object.nameValue[i]))
                        throw $TypeError(".proto.ServicesConfigurationList.nameValue: object expected");
                    message.nameValue[i] = $root.proto.Setting.fromObject(object.nameValue[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ServicesConfigurationList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ServicesConfigurationList
         * @static
         * @param {proto.ServicesConfigurationList} message ServicesConfigurationList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ServicesConfigurationList.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.nameValue = [];
            if (message.nameValue && message.nameValue.length) {
                object.nameValue = $Array(message.nameValue.length);
                for (let j = 0; j < message.nameValue.length; ++j)
                    object.nameValue[j] = $root.proto.Setting.toObject(message.nameValue[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ServicesConfigurationList to JSON.
         * @function toJSON
         * @memberof proto.ServicesConfigurationList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ServicesConfigurationList.prototype.toJSON = function() {
            return ServicesConfigurationList.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ServicesConfigurationList
         * @function getTypeUrl
         * @memberof proto.ServicesConfigurationList
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ServicesConfigurationList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ServicesConfigurationList";
        };

        return ServicesConfigurationList;
    })();

    proto.TokenRelationship = (function() {

        /**
         * Properties of a TokenRelationship.
         * @typedef {Object} proto.TokenRelationship.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] A token identifier.
         * <p>
         * This MUST match an existing token that is not deleted.
         * @property {string|null} [symbol] A token symbol.
         * <p>
         * This MUST match an existing token that is not deleted.<br/>
         * This MUST match the value for the token identified in `tokenId`.
         * @property {number|Long|null} [balance] An account balance for this token.
         * <p>
         * For fungible/common tokens this SHALL be the balance that the
         * account holds of that token. The value is provided as an integer amount
         * of the smallest unit of the token (i.e. 10<sup>`-decimals`</sup> whole
         * tokens).<br/>
         * For non-fungible/unique tokens this SHALL be the whole number of
         * unique tokens held by the account for this token type.
         * @property {proto.TokenKycStatus|null} [kycStatus] A KYC status for the account with respect to this token.
         * <p>
         * This may be `KycNotApplicable`, `Granted` or `Revoked` and, if KYC is
         * not supported for this token (e.g. the `kyc_key` of the token is not
         * set), this SHALL be `KycNotApplicable`.
         * @property {proto.TokenFreezeStatus|null} [freezeStatus] A Freeze status for the account with respect to this token.
         * <p>
         * This value SHALL be one of `FreezeNotApplicable`, `Frozen`
         * or `Unfrozen`.<br/>
         * If the token cannot freeze account assets (e.g. the `freeze_key` of the
         * token is not set), this SHALL be `FreezeNotApplicable`.
         * @property {number|null} [decimals] A maximum "precision" for this token.
         * <p>
         * This value MUST match the `decimals` field of the token identified in
         * the `tokenId` field.<br/>
         * A single whole token SHALL be divided into at most
         * 10<sup>`decimals`</sup> sub-units.
         * @property {boolean|null} [automaticAssociation] An automatic association flag.
         * <p>
         * This SHALL be set if the relationship was created implicitly
         * (automatically).<br/>
         * This SHALL be unset if the relationship was created explicitly
         * (manually) via a `TokenAssociate` transaction.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenRelationship.
         * @memberof proto
         * @interface ITokenRelationship
         * @augments proto.TokenRelationship.$Properties
         * @deprecated Use proto.TokenRelationship.$Properties instead.
         */

        /**
         * Shape of a TokenRelationship.
         * @typedef {proto.TokenRelationship.$Properties} proto.TokenRelationship.$Shape
         */

        /**
         * Constructs a new TokenRelationship.
         * @memberof proto
         * @classdesc An Hedera Token Service token relationship. A token relationship describes
         * the connection between an Account and a Token type, including the current
         * account balance in that token.
         * 
         * A `TokenRelationship` SHALL contain, for the designated token and enclosing
         * account, The account's current balance, whether the account has KYC granted,
         * whether the assets are frozen and whether the association was automatic.<br/>
         * A `TokenRelationship` MAY also contain the `symbol` and `decimals` values
         * copied from the token.<br/>
         * `TokenRelationship` entries SHALL be valid only within the context of a
         * `GetAccountDetails` query response, or other enclosing message, which
         * specifies the account side of the relationship.
         * @constructor
         * @param {proto.TokenRelationship.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenRelationship = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * This MUST match an existing token that is not deleted.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.TokenRelationship
         * @instance
         */
        TokenRelationship.prototype.tokenId = null;

        /**
         * A token symbol.
         * <p>
         * This MUST match an existing token that is not deleted.<br/>
         * This MUST match the value for the token identified in `tokenId`.
         * @member {string} symbol
         * @memberof proto.TokenRelationship
         * @instance
         */
        TokenRelationship.prototype.symbol = "";

        /**
         * An account balance for this token.
         * <p>
         * For fungible/common tokens this SHALL be the balance that the
         * account holds of that token. The value is provided as an integer amount
         * of the smallest unit of the token (i.e. 10<sup>`-decimals`</sup> whole
         * tokens).<br/>
         * For non-fungible/unique tokens this SHALL be the whole number of
         * unique tokens held by the account for this token type.
         * @member {number|Long} balance
         * @memberof proto.TokenRelationship
         * @instance
         */
        TokenRelationship.prototype.balance = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A KYC status for the account with respect to this token.
         * <p>
         * This may be `KycNotApplicable`, `Granted` or `Revoked` and, if KYC is
         * not supported for this token (e.g. the `kyc_key` of the token is not
         * set), this SHALL be `KycNotApplicable`.
         * @member {proto.TokenKycStatus} kycStatus
         * @memberof proto.TokenRelationship
         * @instance
         */
        TokenRelationship.prototype.kycStatus = 0;

        /**
         * A Freeze status for the account with respect to this token.
         * <p>
         * This value SHALL be one of `FreezeNotApplicable`, `Frozen`
         * or `Unfrozen`.<br/>
         * If the token cannot freeze account assets (e.g. the `freeze_key` of the
         * token is not set), this SHALL be `FreezeNotApplicable`.
         * @member {proto.TokenFreezeStatus} freezeStatus
         * @memberof proto.TokenRelationship
         * @instance
         */
        TokenRelationship.prototype.freezeStatus = 0;

        /**
         * A maximum "precision" for this token.
         * <p>
         * This value MUST match the `decimals` field of the token identified in
         * the `tokenId` field.<br/>
         * A single whole token SHALL be divided into at most
         * 10<sup>`decimals`</sup> sub-units.
         * @member {number} decimals
         * @memberof proto.TokenRelationship
         * @instance
         */
        TokenRelationship.prototype.decimals = 0;

        /**
         * An automatic association flag.
         * <p>
         * This SHALL be set if the relationship was created implicitly
         * (automatically).<br/>
         * This SHALL be unset if the relationship was created explicitly
         * (manually) via a `TokenAssociate` transaction.
         * @member {boolean} automaticAssociation
         * @memberof proto.TokenRelationship
         * @instance
         */
        TokenRelationship.prototype.automaticAssociation = false;

        /**
         * Creates a new TokenRelationship instance using the specified properties.
         * @function create
         * @memberof proto.TokenRelationship
         * @static
         * @param {proto.TokenRelationship.$Properties=} [properties] Properties to set
         * @returns {proto.TokenRelationship} TokenRelationship instance
         * @type {{
         *   (properties: proto.TokenRelationship.$Shape): proto.TokenRelationship & proto.TokenRelationship.$Shape;
         *   (properties?: proto.TokenRelationship.$Properties): proto.TokenRelationship;
         * }}
         */
        TokenRelationship.create = function(properties) {
            return new TokenRelationship(properties);
        };

        /**
         * Encodes the specified TokenRelationship message. Does not implicitly {@link proto.TokenRelationship.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenRelationship
         * @static
         * @param {proto.TokenRelationship.$Properties} message TokenRelationship message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenRelationship.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol") && message.symbol !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.symbol);
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance") && (typeof message.balance === "object" ? message.balance.low || message.balance.high : message.balance !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.balance);
            if (message.kycStatus != null && $Object.hasOwnProperty.call(message, "kycStatus") && message.kycStatus !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.kycStatus);
            if (message.freezeStatus != null && $Object.hasOwnProperty.call(message, "freezeStatus") && message.freezeStatus !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.freezeStatus);
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals") && message.decimals !== 0)
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.decimals);
            if (message.automaticAssociation != null && $Object.hasOwnProperty.call(message, "automaticAssociation") && message.automaticAssociation !== false)
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.automaticAssociation);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenRelationship message, length delimited. Does not implicitly {@link proto.TokenRelationship.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenRelationship
         * @static
         * @param {proto.TokenRelationship.$Properties} message TokenRelationship message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenRelationship.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenRelationship message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenRelationship
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenRelationship & proto.TokenRelationship.$Shape} TokenRelationship
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenRelationship.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenRelationship();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.symbol = value;
                        else
                            delete message.symbol;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.balance = value;
                        else
                            delete message.balance;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.kycStatus = value;
                        else
                            delete message.kycStatus;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.freezeStatus = value;
                        else
                            delete message.freezeStatus;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.decimals = value;
                        else
                            delete message.decimals;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.automaticAssociation = value;
                        else
                            delete message.automaticAssociation;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenRelationship message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenRelationship
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenRelationship & proto.TokenRelationship.$Shape} TokenRelationship
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenRelationship.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenRelationship message.
         * @function verify
         * @memberof proto.TokenRelationship
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenRelationship.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                if (!$util.isString(message.symbol))
                    return "symbol: string expected";
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (!$util.isInteger(message.balance) && !(message.balance && $util.isInteger(message.balance.low) && $util.isInteger(message.balance.high)))
                    return "balance: integer|Long expected";
            if (message.kycStatus != null && $Object.hasOwnProperty.call(message, "kycStatus"))
                if (typeof message.kycStatus !== "number" || (message.kycStatus | 0) !== message.kycStatus)
                    return "kycStatus: enum value expected";
            if (message.freezeStatus != null && $Object.hasOwnProperty.call(message, "freezeStatus"))
                if (typeof message.freezeStatus !== "number" || (message.freezeStatus | 0) !== message.freezeStatus)
                    return "freezeStatus: enum value expected";
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals"))
                if (!$util.isInteger(message.decimals))
                    return "decimals: integer expected";
            if (message.automaticAssociation != null && $Object.hasOwnProperty.call(message, "automaticAssociation"))
                if (typeof message.automaticAssociation !== "boolean")
                    return "automaticAssociation: boolean expected";
            return null;
        };

        /**
         * Creates a TokenRelationship message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenRelationship
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenRelationship} TokenRelationship
         */
        TokenRelationship.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenRelationship)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenRelationship: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenRelationship();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.TokenRelationship.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.symbol != null)
                if (typeof object.symbol !== "string" || object.symbol.length)
                    message.symbol = $String(object.symbol);
            if (object.balance != null)
                if (typeof object.balance === "object" ? object.balance.low || object.balance.high : $Number(object.balance) !== 0)
                    if ($util.Long)
                        message.balance = $util.Long.fromValue(object.balance, true);
                    else if (typeof object.balance === "string")
                        message.balance = $parseInt(object.balance, 10);
                    else if (typeof object.balance === "number")
                        message.balance = object.balance;
                    else if (typeof object.balance === "object")
                        message.balance = new $util.LongBits(object.balance.low >>> 0, object.balance.high >>> 0).toNumber(true);
            if (object.kycStatus !== 0 && (typeof object.kycStatus !== "string" || $root.proto.TokenKycStatus[object.kycStatus] !== 0))
                switch (object.kycStatus) {
                case "KycNotApplicable":
                case 0:
                    message.kycStatus = 0;
                    break;
                case "Granted":
                case 1:
                    message.kycStatus = 1;
                    break;
                case "Revoked":
                case 2:
                    message.kycStatus = 2;
                    break;
                default:
                    if (typeof object.kycStatus === "number" && (object.kycStatus | 0) === object.kycStatus)
                        message.kycStatus = object.kycStatus;
                }
            if (object.freezeStatus !== 0 && (typeof object.freezeStatus !== "string" || $root.proto.TokenFreezeStatus[object.freezeStatus] !== 0))
                switch (object.freezeStatus) {
                case "FreezeNotApplicable":
                case 0:
                    message.freezeStatus = 0;
                    break;
                case "Frozen":
                case 1:
                    message.freezeStatus = 1;
                    break;
                case "Unfrozen":
                case 2:
                    message.freezeStatus = 2;
                    break;
                default:
                    if (typeof object.freezeStatus === "number" && (object.freezeStatus | 0) === object.freezeStatus)
                        message.freezeStatus = object.freezeStatus;
                }
            if (object.decimals != null)
                if ($Number(object.decimals) !== 0)
                    message.decimals = object.decimals >>> 0;
            if (object.automaticAssociation != null)
                if (object.automaticAssociation)
                    message.automaticAssociation = $Boolean(object.automaticAssociation);
            return message;
        };

        /**
         * Creates a plain object from a TokenRelationship message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenRelationship
         * @static
         * @param {proto.TokenRelationship} message TokenRelationship
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenRelationship.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.tokenId = null;
                object.symbol = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.kycStatus = options.enums === $String ? "KycNotApplicable" : 0;
                object.freezeStatus = options.enums === $String ? "FreezeNotApplicable" : 0;
                object.decimals = 0;
                object.automaticAssociation = false;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                object.symbol = message.symbol;
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.balance = typeof message.balance === "number" ? $BigInt(message.balance) : $util.Long.fromBits(message.balance.low >>> 0, message.balance.high >>> 0, true).toBigInt();
                else if (typeof message.balance === "number")
                    object.balance = options.longs === $String ? $String(message.balance) : message.balance;
                else
                    object.balance = options.longs === $String ? $util.Long.prototype.toString.call(message.balance) : options.longs === $Number ? new $util.LongBits(message.balance.low >>> 0, message.balance.high >>> 0).toNumber(true) : message.balance;
            if (message.kycStatus != null && $Object.hasOwnProperty.call(message, "kycStatus"))
                object.kycStatus = options.enums === $String ? $root.proto.TokenKycStatus[message.kycStatus] === $undefined ? message.kycStatus : $root.proto.TokenKycStatus[message.kycStatus] : message.kycStatus;
            if (message.freezeStatus != null && $Object.hasOwnProperty.call(message, "freezeStatus"))
                object.freezeStatus = options.enums === $String ? $root.proto.TokenFreezeStatus[message.freezeStatus] === $undefined ? message.freezeStatus : $root.proto.TokenFreezeStatus[message.freezeStatus] : message.freezeStatus;
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals"))
                object.decimals = message.decimals;
            if (message.automaticAssociation != null && $Object.hasOwnProperty.call(message, "automaticAssociation"))
                object.automaticAssociation = message.automaticAssociation;
            return object;
        };

        /**
         * Converts this TokenRelationship to JSON.
         * @function toJSON
         * @memberof proto.TokenRelationship
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenRelationship.prototype.toJSON = function() {
            return TokenRelationship.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenRelationship
         * @function getTypeUrl
         * @memberof proto.TokenRelationship
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenRelationship.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenRelationship";
        };

        return TokenRelationship;
    })();

    proto.TokenBalance = (function() {

        /**
         * Properties of a TokenBalance.
         * @typedef {Object} proto.TokenBalance.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] A token identifier.
         * @property {number|Long|null} [balance] A number of transferable units of the identified token.
         * <p>
         * For fungible/common tokens this SHALL be the balance, in units of
         * 10<sup>`-decimals`</sup> whole tokens.<br/>
         * For non-fungible/unique tokens, this SHALL be the number of
         * individual unique tokens in this balance.
         * @property {number|null} [decimals] A number of "decimals" precision.
         * <p>
         * This MUST match the `decimals` value for the token identified by the
         * `tokenId` field.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenBalance.
         * @memberof proto
         * @interface ITokenBalance
         * @augments proto.TokenBalance.$Properties
         * @deprecated Use proto.TokenBalance.$Properties instead.
         */

        /**
         * Shape of a TokenBalance.
         * @typedef {proto.TokenBalance.$Properties} proto.TokenBalance.$Shape
         */

        /**
         * Constructs a new TokenBalance.
         * @memberof proto
         * @classdesc A number of _transferable units_ of a specified token.
         * 
         * The transferable unit of a token is its smallest denomination, as given by
         * the token's `decimals` property. Each minted token contains
         * 10<sup>`decimals`</sup> transferable units. For example, we could think of
         * the cent as the transferable unit of the US dollar (`decimals=2`); and the
         * tinybar as the transferable unit of HBAR (`decimals=8`).
         * 
         * Transferable units are not directly comparable across different tokens.
         * @constructor
         * @param {proto.TokenBalance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenBalance = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.TokenBalance
         * @instance
         */
        TokenBalance.prototype.tokenId = null;

        /**
         * A number of transferable units of the identified token.
         * <p>
         * For fungible/common tokens this SHALL be the balance, in units of
         * 10<sup>`-decimals`</sup> whole tokens.<br/>
         * For non-fungible/unique tokens, this SHALL be the number of
         * individual unique tokens in this balance.
         * @member {number|Long} balance
         * @memberof proto.TokenBalance
         * @instance
         */
        TokenBalance.prototype.balance = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A number of "decimals" precision.
         * <p>
         * This MUST match the `decimals` value for the token identified by the
         * `tokenId` field.
         * @member {number} decimals
         * @memberof proto.TokenBalance
         * @instance
         */
        TokenBalance.prototype.decimals = 0;

        /**
         * Creates a new TokenBalance instance using the specified properties.
         * @function create
         * @memberof proto.TokenBalance
         * @static
         * @param {proto.TokenBalance.$Properties=} [properties] Properties to set
         * @returns {proto.TokenBalance} TokenBalance instance
         * @type {{
         *   (properties: proto.TokenBalance.$Shape): proto.TokenBalance & proto.TokenBalance.$Shape;
         *   (properties?: proto.TokenBalance.$Properties): proto.TokenBalance;
         * }}
         */
        TokenBalance.create = function(properties) {
            return new TokenBalance(properties);
        };

        /**
         * Encodes the specified TokenBalance message. Does not implicitly {@link proto.TokenBalance.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenBalance
         * @static
         * @param {proto.TokenBalance.$Properties} message TokenBalance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenBalance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance") && (typeof message.balance === "object" ? message.balance.low || message.balance.high : message.balance !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.balance);
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals") && message.decimals !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.decimals);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenBalance message, length delimited. Does not implicitly {@link proto.TokenBalance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenBalance
         * @static
         * @param {proto.TokenBalance.$Properties} message TokenBalance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenBalance.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenBalance message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenBalance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenBalance & proto.TokenBalance.$Shape} TokenBalance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenBalance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenBalance();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.balance = value;
                        else
                            delete message.balance;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.decimals = value;
                        else
                            delete message.decimals;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenBalance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenBalance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenBalance & proto.TokenBalance.$Shape} TokenBalance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenBalance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenBalance message.
         * @function verify
         * @memberof proto.TokenBalance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenBalance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (!$util.isInteger(message.balance) && !(message.balance && $util.isInteger(message.balance.low) && $util.isInteger(message.balance.high)))
                    return "balance: integer|Long expected";
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals"))
                if (!$util.isInteger(message.decimals))
                    return "decimals: integer expected";
            return null;
        };

        /**
         * Creates a TokenBalance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenBalance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenBalance} TokenBalance
         */
        TokenBalance.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenBalance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenBalance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenBalance();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.TokenBalance.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.balance != null)
                if (typeof object.balance === "object" ? object.balance.low || object.balance.high : $Number(object.balance) !== 0)
                    if ($util.Long)
                        message.balance = $util.Long.fromValue(object.balance, true);
                    else if (typeof object.balance === "string")
                        message.balance = $parseInt(object.balance, 10);
                    else if (typeof object.balance === "number")
                        message.balance = object.balance;
                    else if (typeof object.balance === "object")
                        message.balance = new $util.LongBits(object.balance.low >>> 0, object.balance.high >>> 0).toNumber(true);
            if (object.decimals != null)
                if ($Number(object.decimals) !== 0)
                    message.decimals = object.decimals >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a TokenBalance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenBalance
         * @static
         * @param {proto.TokenBalance} message TokenBalance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenBalance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.tokenId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.decimals = 0;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.balance = typeof message.balance === "number" ? $BigInt(message.balance) : $util.Long.fromBits(message.balance.low >>> 0, message.balance.high >>> 0, true).toBigInt();
                else if (typeof message.balance === "number")
                    object.balance = options.longs === $String ? $String(message.balance) : message.balance;
                else
                    object.balance = options.longs === $String ? $util.Long.prototype.toString.call(message.balance) : options.longs === $Number ? new $util.LongBits(message.balance.low >>> 0, message.balance.high >>> 0).toNumber(true) : message.balance;
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals"))
                object.decimals = message.decimals;
            return object;
        };

        /**
         * Converts this TokenBalance to JSON.
         * @function toJSON
         * @memberof proto.TokenBalance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenBalance.prototype.toJSON = function() {
            return TokenBalance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenBalance
         * @function getTypeUrl
         * @memberof proto.TokenBalance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenBalance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenBalance";
        };

        return TokenBalance;
    })();

    proto.TokenBalances = (function() {

        /**
         * Properties of a TokenBalances.
         * @typedef {Object} proto.TokenBalances.$Properties
         * @property {Array.<proto.TokenBalance.$Properties>|null} [tokenBalances] A list of token balance values.<br/>
         * Each entry represents a single account balance for a single token.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenBalances.
         * @memberof proto
         * @interface ITokenBalances
         * @augments proto.TokenBalances.$Properties
         * @deprecated Use proto.TokenBalances.$Properties instead.
         */

        /**
         * Shape of a TokenBalances.
         * @typedef {proto.TokenBalances.$Properties} proto.TokenBalances.$Shape
         */

        /**
         * Constructs a new TokenBalances.
         * @memberof proto
         * @classdesc A set of token balance values.
         * 
         * Each entry describes the balance the enclosing account holds for a specific
         * token. The balance is an amount for a fungible/common token or a count for
         * a non-fungible/unique token.
         * @constructor
         * @param {proto.TokenBalances.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenBalances = function (properties) {
            this.tokenBalances = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of token balance values.<br/>
         * Each entry represents a single account balance for a single token.
         * @member {Array.<proto.TokenBalance.$Properties>} tokenBalances
         * @memberof proto.TokenBalances
         * @instance
         */
        TokenBalances.prototype.tokenBalances = $util.emptyArray;

        /**
         * Creates a new TokenBalances instance using the specified properties.
         * @function create
         * @memberof proto.TokenBalances
         * @static
         * @param {proto.TokenBalances.$Properties=} [properties] Properties to set
         * @returns {proto.TokenBalances} TokenBalances instance
         * @type {{
         *   (properties: proto.TokenBalances.$Shape): proto.TokenBalances & proto.TokenBalances.$Shape;
         *   (properties?: proto.TokenBalances.$Properties): proto.TokenBalances;
         * }}
         */
        TokenBalances.create = function(properties) {
            return new TokenBalances(properties);
        };

        /**
         * Encodes the specified TokenBalances message. Does not implicitly {@link proto.TokenBalances.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenBalances
         * @static
         * @param {proto.TokenBalances.$Properties} message TokenBalances message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenBalances.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenBalances != null && message.tokenBalances.length)
                for (let i = 0; i < message.tokenBalances.length; ++i)
                    $root.proto.TokenBalance.encode(message.tokenBalances[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenBalances message, length delimited. Does not implicitly {@link proto.TokenBalances.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenBalances
         * @static
         * @param {proto.TokenBalances.$Properties} message TokenBalances message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenBalances.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenBalances message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenBalances
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenBalances & proto.TokenBalances.$Shape} TokenBalances
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenBalances.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenBalances();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tokenBalances && message.tokenBalances.length))
                            message.tokenBalances = [];
                        message.tokenBalances.push($root.proto.TokenBalance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenBalances message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenBalances
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenBalances & proto.TokenBalances.$Shape} TokenBalances
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenBalances.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenBalances message.
         * @function verify
         * @memberof proto.TokenBalances
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenBalances.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenBalances != null && $Object.hasOwnProperty.call(message, "tokenBalances")) {
                if (!$Array.isArray(message.tokenBalances))
                    return "tokenBalances: array expected";
                for (let i = 0; i < message.tokenBalances.length; ++i) {
                    let error = $root.proto.TokenBalance.verify(message.tokenBalances[i], _depth + 1);
                    if (error)
                        return "tokenBalances." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TokenBalances message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenBalances
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenBalances} TokenBalances
         */
        TokenBalances.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenBalances)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenBalances: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenBalances();
            if (object.tokenBalances) {
                if (!$Array.isArray(object.tokenBalances))
                    throw $TypeError(".proto.TokenBalances.tokenBalances: array expected");
                message.tokenBalances = $Array(object.tokenBalances.length);
                for (let i = 0; i < object.tokenBalances.length; ++i) {
                    if (!$util.isObject(object.tokenBalances[i]))
                        throw $TypeError(".proto.TokenBalances.tokenBalances: object expected");
                    message.tokenBalances[i] = $root.proto.TokenBalance.fromObject(object.tokenBalances[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenBalances message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenBalances
         * @static
         * @param {proto.TokenBalances} message TokenBalances
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenBalances.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tokenBalances = [];
            if (message.tokenBalances && message.tokenBalances.length) {
                object.tokenBalances = $Array(message.tokenBalances.length);
                for (let j = 0; j < message.tokenBalances.length; ++j)
                    object.tokenBalances[j] = $root.proto.TokenBalance.toObject(message.tokenBalances[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TokenBalances to JSON.
         * @function toJSON
         * @memberof proto.TokenBalances
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenBalances.prototype.toJSON = function() {
            return TokenBalances.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenBalances
         * @function getTypeUrl
         * @memberof proto.TokenBalances
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenBalances.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenBalances";
        };

        return TokenBalances;
    })();

    proto.TokenAssociation = (function() {

        /**
         * Properties of a TokenAssociation.
         * @typedef {Object} proto.TokenAssociation.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] A token identifier for the associated token.
         * @property {proto.AccountID.$Properties|null} [accountId] An account identifier for the associated account.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenAssociation.
         * @memberof proto
         * @interface ITokenAssociation
         * @augments proto.TokenAssociation.$Properties
         * @deprecated Use proto.TokenAssociation.$Properties instead.
         */

        /**
         * Shape of a TokenAssociation.
         * @typedef {{
         *   tokenId?: proto.TokenID.$Shape|null;
         *   accountId?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenAssociation.$Shape
         */

        /**
         * Constructs a new TokenAssociation.
         * @memberof proto
         * @classdesc An association between a token and an account.
         * 
         * An account must be associated with a token before that account can transact
         * in (send or receive) that token.
         * @constructor
         * @param {proto.TokenAssociation.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenAssociation = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier for the associated token.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.TokenAssociation
         * @instance
         */
        TokenAssociation.prototype.tokenId = null;

        /**
         * An account identifier for the associated account.
         * @member {proto.AccountID.$Properties|null|undefined} accountId
         * @memberof proto.TokenAssociation
         * @instance
         */
        TokenAssociation.prototype.accountId = null;

        /**
         * Creates a new TokenAssociation instance using the specified properties.
         * @function create
         * @memberof proto.TokenAssociation
         * @static
         * @param {proto.TokenAssociation.$Properties=} [properties] Properties to set
         * @returns {proto.TokenAssociation} TokenAssociation instance
         * @type {{
         *   (properties: proto.TokenAssociation.$Shape): proto.TokenAssociation & proto.TokenAssociation.$Shape;
         *   (properties?: proto.TokenAssociation.$Properties): proto.TokenAssociation;
         * }}
         */
        TokenAssociation.create = function(properties) {
            return new TokenAssociation(properties);
        };

        /**
         * Encodes the specified TokenAssociation message. Does not implicitly {@link proto.TokenAssociation.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenAssociation
         * @static
         * @param {proto.TokenAssociation.$Properties} message TokenAssociation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenAssociation.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenAssociation message, length delimited. Does not implicitly {@link proto.TokenAssociation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenAssociation
         * @static
         * @param {proto.TokenAssociation.$Properties} message TokenAssociation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenAssociation.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenAssociation message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenAssociation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenAssociation & proto.TokenAssociation.$Shape} TokenAssociation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenAssociation.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenAssociation();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenAssociation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenAssociation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenAssociation & proto.TokenAssociation.$Shape} TokenAssociation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenAssociation.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenAssociation message.
         * @function verify
         * @memberof proto.TokenAssociation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenAssociation.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                if (error)
                    return "accountId." + error;
            }
            return null;
        };

        /**
         * Creates a TokenAssociation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenAssociation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenAssociation} TokenAssociation
         */
        TokenAssociation.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenAssociation)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenAssociation: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenAssociation();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.TokenAssociation.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.accountId != null) {
                if (!$util.isObject(object.accountId))
                    throw $TypeError(".proto.TokenAssociation.accountId: object expected");
                message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenAssociation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenAssociation
         * @static
         * @param {proto.TokenAssociation} message TokenAssociation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenAssociation.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.tokenId = null;
                object.accountId = null;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenAssociation to JSON.
         * @function toJSON
         * @memberof proto.TokenAssociation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenAssociation.prototype.toJSON = function() {
            return TokenAssociation.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenAssociation
         * @function getTypeUrl
         * @memberof proto.TokenAssociation
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenAssociation.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenAssociation";
        };

        return TokenAssociation;
    })();

    proto.AccountTokenAssociation = (function() {

        /**
         * Properties of an AccountTokenAssociation.
         * @typedef {Object} proto.AccountTokenAssociation.$Properties
         * @property {proto.AccountID.$Properties|null} [accountId] An account identifier for the associated account.
         * @property {proto.TokenID.$Properties|null} [tokenId] A token identifier for the associated token.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AccountTokenAssociation.
         * @memberof proto
         * @interface IAccountTokenAssociation
         * @augments proto.AccountTokenAssociation.$Properties
         * @deprecated Use proto.AccountTokenAssociation.$Properties instead.
         */

        /**
         * Shape of an AccountTokenAssociation.
         * @typedef {{
         *   accountId?: proto.AccountID.$Shape|null;
         *   tokenId?: proto.TokenID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.AccountTokenAssociation.$Shape
         */

        /**
         * Constructs a new AccountTokenAssociation.
         * @memberof proto
         * @classdesc An association between an account and a token; this message exists in addition
         * to TokenAssociation because we wanted a more readable name for EntityIDPair;
         * but for the block stream state changes, we *also* needed to preserve the relative
         * order of the account id and token id. (TokenAssociation accidentally reversed the
         * order of those fields, changing the serialized bytes and breaking binary-only
         * replay of state changes.)
         * @constructor
         * @param {proto.AccountTokenAssociation.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AccountTokenAssociation = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An account identifier for the associated account.
         * @member {proto.AccountID.$Properties|null|undefined} accountId
         * @memberof proto.AccountTokenAssociation
         * @instance
         */
        AccountTokenAssociation.prototype.accountId = null;

        /**
         * A token identifier for the associated token.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.AccountTokenAssociation
         * @instance
         */
        AccountTokenAssociation.prototype.tokenId = null;

        /**
         * Creates a new AccountTokenAssociation instance using the specified properties.
         * @function create
         * @memberof proto.AccountTokenAssociation
         * @static
         * @param {proto.AccountTokenAssociation.$Properties=} [properties] Properties to set
         * @returns {proto.AccountTokenAssociation} AccountTokenAssociation instance
         * @type {{
         *   (properties: proto.AccountTokenAssociation.$Shape): proto.AccountTokenAssociation & proto.AccountTokenAssociation.$Shape;
         *   (properties?: proto.AccountTokenAssociation.$Properties): proto.AccountTokenAssociation;
         * }}
         */
        AccountTokenAssociation.create = function(properties) {
            return new AccountTokenAssociation(properties);
        };

        /**
         * Encodes the specified AccountTokenAssociation message. Does not implicitly {@link proto.AccountTokenAssociation.verify|verify} messages.
         * @function encode
         * @memberof proto.AccountTokenAssociation
         * @static
         * @param {proto.AccountTokenAssociation.$Properties} message AccountTokenAssociation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountTokenAssociation.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AccountTokenAssociation message, length delimited. Does not implicitly {@link proto.AccountTokenAssociation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AccountTokenAssociation
         * @static
         * @param {proto.AccountTokenAssociation.$Properties} message AccountTokenAssociation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountTokenAssociation.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AccountTokenAssociation message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AccountTokenAssociation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AccountTokenAssociation & proto.AccountTokenAssociation.$Shape} AccountTokenAssociation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountTokenAssociation.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.AccountTokenAssociation();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AccountTokenAssociation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AccountTokenAssociation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AccountTokenAssociation & proto.AccountTokenAssociation.$Shape} AccountTokenAssociation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountTokenAssociation.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AccountTokenAssociation message.
         * @function verify
         * @memberof proto.AccountTokenAssociation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AccountTokenAssociation.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                if (error)
                    return "accountId." + error;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            return null;
        };

        /**
         * Creates an AccountTokenAssociation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AccountTokenAssociation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AccountTokenAssociation} AccountTokenAssociation
         */
        AccountTokenAssociation.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.AccountTokenAssociation)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.AccountTokenAssociation: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.AccountTokenAssociation();
            if (object.accountId != null) {
                if (!$util.isObject(object.accountId))
                    throw $TypeError(".proto.AccountTokenAssociation.accountId: object expected");
                message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
            }
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.AccountTokenAssociation.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an AccountTokenAssociation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AccountTokenAssociation
         * @static
         * @param {proto.AccountTokenAssociation} message AccountTokenAssociation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccountTokenAssociation.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.accountId = null;
                object.tokenId = null;
            }
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            return object;
        };

        /**
         * Converts this AccountTokenAssociation to JSON.
         * @function toJSON
         * @memberof proto.AccountTokenAssociation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccountTokenAssociation.prototype.toJSON = function() {
            return AccountTokenAssociation.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AccountTokenAssociation
         * @function getTypeUrl
         * @memberof proto.AccountTokenAssociation
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AccountTokenAssociation.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AccountTokenAssociation";
        };

        return AccountTokenAssociation;
    })();

    proto.StakingInfo = (function() {

        /**
         * Properties of a StakingInfo.
         * @typedef {Object} proto.StakingInfo.$Properties
         * @property {boolean|null} [declineReward] A flag indicating that the holder of this account has chosen to decline
         * staking rewards.
         * @property {proto.Timestamp.$Properties|null} [stakePeriodStart] A `Timestamp` of the start time for the latest active staking period.
         * <p>
         * This MUST be a period during which either the staking settings for this
         * account or contract changed or the account or contract received staking
         * rewards, whichever is later. Examples of a change in staking settings
         * include starting staking or changing the staked_node_id.<br/>
         * If this account or contract is not currently staked to a node, then this
         * field SHALL NOT be set.
         * @property {number|Long|null} [pendingReward] An amount, in tinybar, to be received in the next reward payout.<br/>
         * Rewards are not paid out immediately; for efficiency reasons rewards are
         * only paid out as part of another transaction involving that account.
         * @property {number|Long|null} [stakedToMe] A proxy-staked balance.<br/>
         * The total HBAR balance of all accounts that delegate staking to this
         * account or contract.
         * @property {proto.AccountID.$Properties|null} [stakedAccountId] A delegated stake.
         * <p>
         * This account delegates to the indicated account for staking purposes.
         * @property {number|Long|null} [stakedNodeId] A direct stake.
         * <p>
         * This accounts stakes its balance to the designated node.
         * @property {"stakedAccountId"|"stakedNodeId"} [stakedId] StakingInfo stakedId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a StakingInfo.
         * @memberof proto
         * @interface IStakingInfo
         * @augments proto.StakingInfo.$Properties
         * @deprecated Use proto.StakingInfo.$Properties instead.
         */

        /**
         * Narrowed shape of a StakingInfo.
         * @typedef {{
         *   declineReward?: boolean|null;
         *   stakePeriodStart?: proto.Timestamp.$Shape|null;
         *   pendingReward?: number|Long|null;
         *   stakedToMe?: number|Long|null;
         *   stakedAccountId?: proto.AccountID.$Shape|null;
         *   stakedNodeId?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ stakedId?: undefined; stakedAccountId?: null; stakedNodeId?: null }|{ stakedId?: "stakedAccountId"; stakedAccountId: proto.AccountID.$Shape; stakedNodeId?: null }|{ stakedId?: "stakedNodeId"; stakedAccountId?: null; stakedNodeId: number|Long })
         * )} proto.StakingInfo.$Shape
         */

        /**
         * Constructs a new StakingInfo.
         * @memberof proto
         * @classdesc Staking information for an account or a contract.
         * 
         * This is used for responses returned from `CryptoGetInfo` or
         * `ContractGetInfo` queries.
         * @constructor
         * @param {proto.StakingInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const StakingInfo = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A flag indicating that the holder of this account has chosen to decline
         * staking rewards.
         * @member {boolean} declineReward
         * @memberof proto.StakingInfo
         * @instance
         */
        StakingInfo.prototype.declineReward = false;

        /**
         * A `Timestamp` of the start time for the latest active staking period.
         * <p>
         * This MUST be a period during which either the staking settings for this
         * account or contract changed or the account or contract received staking
         * rewards, whichever is later. Examples of a change in staking settings
         * include starting staking or changing the staked_node_id.<br/>
         * If this account or contract is not currently staked to a node, then this
         * field SHALL NOT be set.
         * @member {proto.Timestamp.$Properties|null|undefined} stakePeriodStart
         * @memberof proto.StakingInfo
         * @instance
         */
        StakingInfo.prototype.stakePeriodStart = null;

        /**
         * An amount, in tinybar, to be received in the next reward payout.<br/>
         * Rewards are not paid out immediately; for efficiency reasons rewards are
         * only paid out as part of another transaction involving that account.
         * @member {number|Long} pendingReward
         * @memberof proto.StakingInfo
         * @instance
         */
        StakingInfo.prototype.pendingReward = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A proxy-staked balance.<br/>
         * The total HBAR balance of all accounts that delegate staking to this
         * account or contract.
         * @member {number|Long} stakedToMe
         * @memberof proto.StakingInfo
         * @instance
         */
        StakingInfo.prototype.stakedToMe = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A delegated stake.
         * <p>
         * This account delegates to the indicated account for staking purposes.
         * @member {proto.AccountID.$Properties|null|undefined} stakedAccountId
         * @memberof proto.StakingInfo
         * @instance
         */
        StakingInfo.prototype.stakedAccountId = null;

        /**
         * A direct stake.
         * <p>
         * This accounts stakes its balance to the designated node.
         * @member {number|Long|null|undefined} stakedNodeId
         * @memberof proto.StakingInfo
         * @instance
         */
        StakingInfo.prototype.stakedNodeId = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * StakingInfo stakedId.
         * @member {"stakedAccountId"|"stakedNodeId"|undefined} stakedId
         * @memberof proto.StakingInfo
         * @instance
         */
        $Object.defineProperty(StakingInfo.prototype, "stakedId", {
            get: $util.oneOfGetter($oneOfFields = ["stakedAccountId", "stakedNodeId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new StakingInfo instance using the specified properties.
         * @function create
         * @memberof proto.StakingInfo
         * @static
         * @param {proto.StakingInfo.$Properties=} [properties] Properties to set
         * @returns {proto.StakingInfo} StakingInfo instance
         * @type {{
         *   (properties: proto.StakingInfo.$Shape): proto.StakingInfo & proto.StakingInfo.$Shape;
         *   (properties?: proto.StakingInfo.$Properties): proto.StakingInfo;
         * }}
         */
        StakingInfo.create = function(properties) {
            return new StakingInfo(properties);
        };

        /**
         * Encodes the specified StakingInfo message. Does not implicitly {@link proto.StakingInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.StakingInfo
         * @static
         * @param {proto.StakingInfo.$Properties} message StakingInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StakingInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward") && message.declineReward !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.declineReward);
            if (message.stakePeriodStart != null && $Object.hasOwnProperty.call(message, "stakePeriodStart"))
                $root.proto.Timestamp.encode(message.stakePeriodStart, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.pendingReward != null && $Object.hasOwnProperty.call(message, "pendingReward") && (typeof message.pendingReward === "object" ? message.pendingReward.low || message.pendingReward.high : message.pendingReward !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.pendingReward);
            if (message.stakedToMe != null && $Object.hasOwnProperty.call(message, "stakedToMe") && (typeof message.stakedToMe === "object" ? message.stakedToMe.low || message.stakedToMe.high : message.stakedToMe !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.stakedToMe);
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId"))
                $root.proto.AccountID.encode(message.stakedAccountId, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId"))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.stakedNodeId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StakingInfo message, length delimited. Does not implicitly {@link proto.StakingInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.StakingInfo
         * @static
         * @param {proto.StakingInfo.$Properties} message StakingInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StakingInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a StakingInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.StakingInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.StakingInfo & proto.StakingInfo.$Shape} StakingInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StakingInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.StakingInfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.declineReward = value;
                        else
                            delete message.declineReward;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.stakePeriodStart = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stakePeriodStart);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.pendingReward = value;
                        else
                            delete message.pendingReward;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakedToMe = value;
                        else
                            delete message.stakedToMe;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.stakedAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stakedAccountId);
                        message.stakedId = "stakedAccountId";
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        message.stakedNodeId = reader.int64();
                        message.stakedId = "stakedNodeId";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StakingInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.StakingInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.StakingInfo & proto.StakingInfo.$Shape} StakingInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StakingInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StakingInfo message.
         * @function verify
         * @memberof proto.StakingInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StakingInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                if (typeof message.declineReward !== "boolean")
                    return "declineReward: boolean expected";
            if (message.stakePeriodStart != null && $Object.hasOwnProperty.call(message, "stakePeriodStart")) {
                let error = $root.proto.Timestamp.verify(message.stakePeriodStart, _depth + 1);
                if (error)
                    return "stakePeriodStart." + error;
            }
            if (message.pendingReward != null && $Object.hasOwnProperty.call(message, "pendingReward"))
                if (!$util.isInteger(message.pendingReward) && !(message.pendingReward && $util.isInteger(message.pendingReward.low) && $util.isInteger(message.pendingReward.high)))
                    return "pendingReward: integer|Long expected";
            if (message.stakedToMe != null && $Object.hasOwnProperty.call(message, "stakedToMe"))
                if (!$util.isInteger(message.stakedToMe) && !(message.stakedToMe && $util.isInteger(message.stakedToMe.low) && $util.isInteger(message.stakedToMe.high)))
                    return "stakedToMe: integer|Long expected";
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId")) {
                properties.stakedId = 1;
                {
                    let error = $root.proto.AccountID.verify(message.stakedAccountId, _depth + 1);
                    if (error)
                        return "stakedAccountId." + error;
                }
            }
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId")) {
                if (properties.stakedId === 1)
                    return "stakedId: multiple values";
                properties.stakedId = 1;
                if (!$util.isInteger(message.stakedNodeId) && !(message.stakedNodeId && $util.isInteger(message.stakedNodeId.low) && $util.isInteger(message.stakedNodeId.high)))
                    return "stakedNodeId: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a StakingInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.StakingInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.StakingInfo} StakingInfo
         */
        StakingInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.StakingInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.StakingInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.StakingInfo();
            if (object.declineReward != null)
                if (object.declineReward)
                    message.declineReward = $Boolean(object.declineReward);
            if (object.stakePeriodStart != null) {
                if (!$util.isObject(object.stakePeriodStart))
                    throw $TypeError(".proto.StakingInfo.stakePeriodStart: object expected");
                message.stakePeriodStart = $root.proto.Timestamp.fromObject(object.stakePeriodStart, _depth + 1);
            }
            if (object.pendingReward != null)
                if (typeof object.pendingReward === "object" ? object.pendingReward.low || object.pendingReward.high : $Number(object.pendingReward) !== 0)
                    if ($util.Long)
                        message.pendingReward = $util.Long.fromValue(object.pendingReward, false);
                    else if (typeof object.pendingReward === "string")
                        message.pendingReward = $parseInt(object.pendingReward, 10);
                    else if (typeof object.pendingReward === "number")
                        message.pendingReward = object.pendingReward;
                    else if (typeof object.pendingReward === "object")
                        message.pendingReward = new $util.LongBits(object.pendingReward.low >>> 0, object.pendingReward.high >>> 0).toNumber();
            if (object.stakedToMe != null)
                if (typeof object.stakedToMe === "object" ? object.stakedToMe.low || object.stakedToMe.high : $Number(object.stakedToMe) !== 0)
                    if ($util.Long)
                        message.stakedToMe = $util.Long.fromValue(object.stakedToMe, false);
                    else if (typeof object.stakedToMe === "string")
                        message.stakedToMe = $parseInt(object.stakedToMe, 10);
                    else if (typeof object.stakedToMe === "number")
                        message.stakedToMe = object.stakedToMe;
                    else if (typeof object.stakedToMe === "object")
                        message.stakedToMe = new $util.LongBits(object.stakedToMe.low >>> 0, object.stakedToMe.high >>> 0).toNumber();
            if (object.stakedAccountId != null) {
                if (!$util.isObject(object.stakedAccountId))
                    throw $TypeError(".proto.StakingInfo.stakedAccountId: object expected");
                message.stakedAccountId = $root.proto.AccountID.fromObject(object.stakedAccountId, _depth + 1);
            }
            if (object.stakedNodeId != null)
                if ($util.Long)
                    message.stakedNodeId = $util.Long.fromValue(object.stakedNodeId, false);
                else if (typeof object.stakedNodeId === "string")
                    message.stakedNodeId = $parseInt(object.stakedNodeId, 10);
                else if (typeof object.stakedNodeId === "number")
                    message.stakedNodeId = object.stakedNodeId;
                else if (typeof object.stakedNodeId === "object")
                    message.stakedNodeId = new $util.LongBits(object.stakedNodeId.low >>> 0, object.stakedNodeId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a StakingInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.StakingInfo
         * @static
         * @param {proto.StakingInfo} message StakingInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StakingInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.declineReward = false;
                object.stakePeriodStart = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.pendingReward = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.pendingReward = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakedToMe = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakedToMe = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                object.declineReward = message.declineReward;
            if (message.stakePeriodStart != null && $Object.hasOwnProperty.call(message, "stakePeriodStart"))
                object.stakePeriodStart = $root.proto.Timestamp.toObject(message.stakePeriodStart, options, _depth + 1);
            if (message.pendingReward != null && $Object.hasOwnProperty.call(message, "pendingReward"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.pendingReward = typeof message.pendingReward === "number" ? $BigInt(message.pendingReward) : $util.Long.fromBits(message.pendingReward.low >>> 0, message.pendingReward.high >>> 0, false).toBigInt();
                else if (typeof message.pendingReward === "number")
                    object.pendingReward = options.longs === $String ? $String(message.pendingReward) : message.pendingReward;
                else
                    object.pendingReward = options.longs === $String ? $util.Long.prototype.toString.call(message.pendingReward) : options.longs === $Number ? new $util.LongBits(message.pendingReward.low >>> 0, message.pendingReward.high >>> 0).toNumber() : message.pendingReward;
            if (message.stakedToMe != null && $Object.hasOwnProperty.call(message, "stakedToMe"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakedToMe = typeof message.stakedToMe === "number" ? $BigInt(message.stakedToMe) : $util.Long.fromBits(message.stakedToMe.low >>> 0, message.stakedToMe.high >>> 0, false).toBigInt();
                else if (typeof message.stakedToMe === "number")
                    object.stakedToMe = options.longs === $String ? $String(message.stakedToMe) : message.stakedToMe;
                else
                    object.stakedToMe = options.longs === $String ? $util.Long.prototype.toString.call(message.stakedToMe) : options.longs === $Number ? new $util.LongBits(message.stakedToMe.low >>> 0, message.stakedToMe.high >>> 0).toNumber() : message.stakedToMe;
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId")) {
                object.stakedAccountId = $root.proto.AccountID.toObject(message.stakedAccountId, options, _depth + 1);
                if (options.oneofs)
                    object.stakedId = "stakedAccountId";
            }
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakedNodeId = typeof message.stakedNodeId === "number" ? $BigInt(message.stakedNodeId) : $util.Long.fromBits(message.stakedNodeId.low >>> 0, message.stakedNodeId.high >>> 0, false).toBigInt();
                else if (typeof message.stakedNodeId === "number")
                    object.stakedNodeId = options.longs === $String ? $String(message.stakedNodeId) : message.stakedNodeId;
                else
                    object.stakedNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.stakedNodeId) : options.longs === $Number ? new $util.LongBits(message.stakedNodeId.low >>> 0, message.stakedNodeId.high >>> 0).toNumber() : message.stakedNodeId;
                if (options.oneofs)
                    object.stakedId = "stakedNodeId";
            }
            return object;
        };

        /**
         * Converts this StakingInfo to JSON.
         * @function toJSON
         * @memberof proto.StakingInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StakingInfo.prototype.toJSON = function() {
            return StakingInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StakingInfo
         * @function getTypeUrl
         * @memberof proto.StakingInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StakingInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.StakingInfo";
        };

        return StakingInfo;
    })();

    proto.PendingAirdropId = (function() {

        /**
         * Properties of a PendingAirdropId.
         * @typedef {Object} proto.PendingAirdropId.$Properties
         * @property {proto.AccountID.$Properties|null} [senderId] A sending account.
         * <p>
         * This is the account that initiated, and SHALL fund,
         * this pending airdrop.<br/>
         * This field is REQUIRED.
         * @property {proto.AccountID.$Properties|null} [receiverId] A receiving account.
         * <p>
         * This is the ID of the account that SHALL receive the airdrop.<br/>
         * This field is REQUIRED.
         * @property {proto.TokenID.$Properties|null} [fungibleTokenType] A token identifier.<br/>
         * This is the type of token for a fungible/common token airdrop.
         * <p>
         * This field is REQUIRED for a fungible/common token and MUST NOT
         * be used for a non-fungible/unique token.
         * @property {proto.NftID.$Properties|null} [nonFungibleToken] The id of a single NFT<br/>
         * This is the type of token for a non-fungible/unique token airdrop
         * and consists of a Token ID and serial number.
         * <p>
         * This field is REQUIRED for a non-fungible/unique token and
         * MUST NOT be used for a fungible/common token.
         * @property {"fungibleTokenType"|"nonFungibleToken"} [tokenReference] PendingAirdropId tokenReference
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PendingAirdropId.
         * @memberof proto
         * @interface IPendingAirdropId
         * @augments proto.PendingAirdropId.$Properties
         * @deprecated Use proto.PendingAirdropId.$Properties instead.
         */

        /**
         * Narrowed shape of a PendingAirdropId.
         * @typedef {{
         *   senderId?: proto.AccountID.$Shape|null;
         *   receiverId?: proto.AccountID.$Shape|null;
         *   fungibleTokenType?: proto.TokenID.$Shape|null;
         *   nonFungibleToken?: proto.NftID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ tokenReference?: undefined; fungibleTokenType?: null; nonFungibleToken?: null }|{ tokenReference?: "fungibleTokenType"; fungibleTokenType: proto.TokenID.$Shape; nonFungibleToken?: null }|{ tokenReference?: "nonFungibleToken"; fungibleTokenType?: null; nonFungibleToken: proto.NftID.$Shape })
         * )} proto.PendingAirdropId.$Shape
         */

        /**
         * Constructs a new PendingAirdropId.
         * @memberof proto
         * @classdesc A unique, composite, identifier for a pending airdrop.
         * 
         * Each pending airdrop SHALL be uniquely identified by
         * a `PendingAirdropId`.<br/>
         * A `PendingAirdropId` SHALL be recorded when created and MUST be provided in
         * any transaction that would modify that pending airdrop
         * (such as a `claimAirdrop` or `cancelAirdrop`).
         * @constructor
         * @param {proto.PendingAirdropId.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PendingAirdropId = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A sending account.
         * <p>
         * This is the account that initiated, and SHALL fund,
         * this pending airdrop.<br/>
         * This field is REQUIRED.
         * @member {proto.AccountID.$Properties|null|undefined} senderId
         * @memberof proto.PendingAirdropId
         * @instance
         */
        PendingAirdropId.prototype.senderId = null;

        /**
         * A receiving account.
         * <p>
         * This is the ID of the account that SHALL receive the airdrop.<br/>
         * This field is REQUIRED.
         * @member {proto.AccountID.$Properties|null|undefined} receiverId
         * @memberof proto.PendingAirdropId
         * @instance
         */
        PendingAirdropId.prototype.receiverId = null;

        /**
         * A token identifier.<br/>
         * This is the type of token for a fungible/common token airdrop.
         * <p>
         * This field is REQUIRED for a fungible/common token and MUST NOT
         * be used for a non-fungible/unique token.
         * @member {proto.TokenID.$Properties|null|undefined} fungibleTokenType
         * @memberof proto.PendingAirdropId
         * @instance
         */
        PendingAirdropId.prototype.fungibleTokenType = null;

        /**
         * The id of a single NFT<br/>
         * This is the type of token for a non-fungible/unique token airdrop
         * and consists of a Token ID and serial number.
         * <p>
         * This field is REQUIRED for a non-fungible/unique token and
         * MUST NOT be used for a fungible/common token.
         * @member {proto.NftID.$Properties|null|undefined} nonFungibleToken
         * @memberof proto.PendingAirdropId
         * @instance
         */
        PendingAirdropId.prototype.nonFungibleToken = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * PendingAirdropId tokenReference.
         * @member {"fungibleTokenType"|"nonFungibleToken"|undefined} tokenReference
         * @memberof proto.PendingAirdropId
         * @instance
         */
        $Object.defineProperty(PendingAirdropId.prototype, "tokenReference", {
            get: $util.oneOfGetter($oneOfFields = ["fungibleTokenType", "nonFungibleToken"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PendingAirdropId instance using the specified properties.
         * @function create
         * @memberof proto.PendingAirdropId
         * @static
         * @param {proto.PendingAirdropId.$Properties=} [properties] Properties to set
         * @returns {proto.PendingAirdropId} PendingAirdropId instance
         * @type {{
         *   (properties: proto.PendingAirdropId.$Shape): proto.PendingAirdropId & proto.PendingAirdropId.$Shape;
         *   (properties?: proto.PendingAirdropId.$Properties): proto.PendingAirdropId;
         * }}
         */
        PendingAirdropId.create = function(properties) {
            return new PendingAirdropId(properties);
        };

        /**
         * Encodes the specified PendingAirdropId message. Does not implicitly {@link proto.PendingAirdropId.verify|verify} messages.
         * @function encode
         * @memberof proto.PendingAirdropId
         * @static
         * @param {proto.PendingAirdropId.$Properties} message PendingAirdropId message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingAirdropId.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                $root.proto.AccountID.encode(message.senderId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.receiverId != null && $Object.hasOwnProperty.call(message, "receiverId"))
                $root.proto.AccountID.encode(message.receiverId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.fungibleTokenType != null && $Object.hasOwnProperty.call(message, "fungibleTokenType"))
                $root.proto.TokenID.encode(message.fungibleTokenType, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.nonFungibleToken != null && $Object.hasOwnProperty.call(message, "nonFungibleToken"))
                $root.proto.NftID.encode(message.nonFungibleToken, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PendingAirdropId message, length delimited. Does not implicitly {@link proto.PendingAirdropId.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PendingAirdropId
         * @static
         * @param {proto.PendingAirdropId.$Properties} message PendingAirdropId message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingAirdropId.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PendingAirdropId message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PendingAirdropId
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PendingAirdropId & proto.PendingAirdropId.$Shape} PendingAirdropId
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingAirdropId.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.PendingAirdropId();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.senderId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.senderId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.receiverId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.receiverId);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.fungibleTokenType = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fungibleTokenType);
                        message.tokenReference = "fungibleTokenType";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.nonFungibleToken = $root.proto.NftID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nonFungibleToken);
                        message.tokenReference = "nonFungibleToken";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PendingAirdropId message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PendingAirdropId
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PendingAirdropId & proto.PendingAirdropId.$Shape} PendingAirdropId
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingAirdropId.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PendingAirdropId message.
         * @function verify
         * @memberof proto.PendingAirdropId
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PendingAirdropId.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId")) {
                let error = $root.proto.AccountID.verify(message.senderId, _depth + 1);
                if (error)
                    return "senderId." + error;
            }
            if (message.receiverId != null && $Object.hasOwnProperty.call(message, "receiverId")) {
                let error = $root.proto.AccountID.verify(message.receiverId, _depth + 1);
                if (error)
                    return "receiverId." + error;
            }
            if (message.fungibleTokenType != null && $Object.hasOwnProperty.call(message, "fungibleTokenType")) {
                properties.tokenReference = 1;
                {
                    let error = $root.proto.TokenID.verify(message.fungibleTokenType, _depth + 1);
                    if (error)
                        return "fungibleTokenType." + error;
                }
            }
            if (message.nonFungibleToken != null && $Object.hasOwnProperty.call(message, "nonFungibleToken")) {
                if (properties.tokenReference === 1)
                    return "tokenReference: multiple values";
                properties.tokenReference = 1;
                {
                    let error = $root.proto.NftID.verify(message.nonFungibleToken, _depth + 1);
                    if (error)
                        return "nonFungibleToken." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PendingAirdropId message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PendingAirdropId
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PendingAirdropId} PendingAirdropId
         */
        PendingAirdropId.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.PendingAirdropId)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.PendingAirdropId: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.PendingAirdropId();
            if (object.senderId != null) {
                if (!$util.isObject(object.senderId))
                    throw $TypeError(".proto.PendingAirdropId.senderId: object expected");
                message.senderId = $root.proto.AccountID.fromObject(object.senderId, _depth + 1);
            }
            if (object.receiverId != null) {
                if (!$util.isObject(object.receiverId))
                    throw $TypeError(".proto.PendingAirdropId.receiverId: object expected");
                message.receiverId = $root.proto.AccountID.fromObject(object.receiverId, _depth + 1);
            }
            if (object.fungibleTokenType != null) {
                if (!$util.isObject(object.fungibleTokenType))
                    throw $TypeError(".proto.PendingAirdropId.fungibleTokenType: object expected");
                message.fungibleTokenType = $root.proto.TokenID.fromObject(object.fungibleTokenType, _depth + 1);
            }
            if (object.nonFungibleToken != null) {
                if (!$util.isObject(object.nonFungibleToken))
                    throw $TypeError(".proto.PendingAirdropId.nonFungibleToken: object expected");
                message.nonFungibleToken = $root.proto.NftID.fromObject(object.nonFungibleToken, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a PendingAirdropId message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PendingAirdropId
         * @static
         * @param {proto.PendingAirdropId} message PendingAirdropId
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PendingAirdropId.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.senderId = null;
                object.receiverId = null;
            }
            if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                object.senderId = $root.proto.AccountID.toObject(message.senderId, options, _depth + 1);
            if (message.receiverId != null && $Object.hasOwnProperty.call(message, "receiverId"))
                object.receiverId = $root.proto.AccountID.toObject(message.receiverId, options, _depth + 1);
            if (message.fungibleTokenType != null && $Object.hasOwnProperty.call(message, "fungibleTokenType")) {
                object.fungibleTokenType = $root.proto.TokenID.toObject(message.fungibleTokenType, options, _depth + 1);
                if (options.oneofs)
                    object.tokenReference = "fungibleTokenType";
            }
            if (message.nonFungibleToken != null && $Object.hasOwnProperty.call(message, "nonFungibleToken")) {
                object.nonFungibleToken = $root.proto.NftID.toObject(message.nonFungibleToken, options, _depth + 1);
                if (options.oneofs)
                    object.tokenReference = "nonFungibleToken";
            }
            return object;
        };

        /**
         * Converts this PendingAirdropId to JSON.
         * @function toJSON
         * @memberof proto.PendingAirdropId
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PendingAirdropId.prototype.toJSON = function() {
            return PendingAirdropId.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PendingAirdropId
         * @function getTypeUrl
         * @memberof proto.PendingAirdropId
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PendingAirdropId.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PendingAirdropId";
        };

        return PendingAirdropId;
    })();

    proto.PendingAirdropValue = (function() {

        /**
         * Properties of a PendingAirdropValue.
         * @typedef {Object} proto.PendingAirdropValue.$Properties
         * @property {number|Long|null} [amount] An amount to transfer for fungible/common tokens.<br/>
         * This is expressed in the smallest available units for that token
         * (i.e. 10<sup>-`decimals`</sup> whole tokens).
         * <p>
         * This amount SHALL be transferred from the sender to the receiver,
         * if claimed.<br/>
         * If the token is a fungible/common token, this value MUST be strictly
         * greater than `0`.<br/>
         * If the token is a non-fungible/unique token, this message SHOULD NOT
         * be set, and if set, this field MUST be `0`.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PendingAirdropValue.
         * @memberof proto
         * @interface IPendingAirdropValue
         * @augments proto.PendingAirdropValue.$Properties
         * @deprecated Use proto.PendingAirdropValue.$Properties instead.
         */

        /**
         * Shape of a PendingAirdropValue.
         * @typedef {proto.PendingAirdropValue.$Properties} proto.PendingAirdropValue.$Shape
         */

        /**
         * Constructs a new PendingAirdropValue.
         * @memberof proto
         * @classdesc A single pending airdrop value.
         * 
         * This message SHALL record the airdrop amount for a
         * fungible/common token.<br/>
         * This message SHOULD be null for a non-fungible/unique token.<br/>
         * If a non-null `PendingAirdropValue` is set for a non-fungible/unique
         * token, the amount field MUST be `0`.
         * 
         * It is RECOMMENDED that implementations store pending airdrop information
         * as a key-value map from `PendingAirdropId` to `PendingAirdropValue`, with
         * a `null` value used for non-fungible pending airdrops.
         * @constructor
         * @param {proto.PendingAirdropValue.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PendingAirdropValue = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An amount to transfer for fungible/common tokens.<br/>
         * This is expressed in the smallest available units for that token
         * (i.e. 10<sup>-`decimals`</sup> whole tokens).
         * <p>
         * This amount SHALL be transferred from the sender to the receiver,
         * if claimed.<br/>
         * If the token is a fungible/common token, this value MUST be strictly
         * greater than `0`.<br/>
         * If the token is a non-fungible/unique token, this message SHOULD NOT
         * be set, and if set, this field MUST be `0`.
         * @member {number|Long} amount
         * @memberof proto.PendingAirdropValue
         * @instance
         */
        PendingAirdropValue.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new PendingAirdropValue instance using the specified properties.
         * @function create
         * @memberof proto.PendingAirdropValue
         * @static
         * @param {proto.PendingAirdropValue.$Properties=} [properties] Properties to set
         * @returns {proto.PendingAirdropValue} PendingAirdropValue instance
         * @type {{
         *   (properties: proto.PendingAirdropValue.$Shape): proto.PendingAirdropValue & proto.PendingAirdropValue.$Shape;
         *   (properties?: proto.PendingAirdropValue.$Properties): proto.PendingAirdropValue;
         * }}
         */
        PendingAirdropValue.create = function(properties) {
            return new PendingAirdropValue(properties);
        };

        /**
         * Encodes the specified PendingAirdropValue message. Does not implicitly {@link proto.PendingAirdropValue.verify|verify} messages.
         * @function encode
         * @memberof proto.PendingAirdropValue
         * @static
         * @param {proto.PendingAirdropValue.$Properties} message PendingAirdropValue message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingAirdropValue.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.amount);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PendingAirdropValue message, length delimited. Does not implicitly {@link proto.PendingAirdropValue.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PendingAirdropValue
         * @static
         * @param {proto.PendingAirdropValue.$Properties} message PendingAirdropValue message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingAirdropValue.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PendingAirdropValue message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PendingAirdropValue
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PendingAirdropValue & proto.PendingAirdropValue.$Shape} PendingAirdropValue
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingAirdropValue.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.PendingAirdropValue();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PendingAirdropValue message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PendingAirdropValue
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PendingAirdropValue & proto.PendingAirdropValue.$Shape} PendingAirdropValue
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingAirdropValue.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PendingAirdropValue message.
         * @function verify
         * @memberof proto.PendingAirdropValue
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PendingAirdropValue.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            return null;
        };

        /**
         * Creates a PendingAirdropValue message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PendingAirdropValue
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PendingAirdropValue} PendingAirdropValue
         */
        PendingAirdropValue.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.PendingAirdropValue)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.PendingAirdropValue: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.PendingAirdropValue();
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, true);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a PendingAirdropValue message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PendingAirdropValue
         * @static
         * @param {proto.PendingAirdropValue} message PendingAirdropValue
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PendingAirdropValue.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, true).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber(true) : message.amount;
            return object;
        };

        /**
         * Converts this PendingAirdropValue to JSON.
         * @function toJSON
         * @memberof proto.PendingAirdropValue
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PendingAirdropValue.prototype.toJSON = function() {
            return PendingAirdropValue.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PendingAirdropValue
         * @function getTypeUrl
         * @memberof proto.PendingAirdropValue
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PendingAirdropValue.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PendingAirdropValue";
        };

        return PendingAirdropValue;
    })();

    proto.Timestamp = (function() {

        /**
         * Properties of a Timestamp.
         * @typedef {Object} proto.Timestamp.$Properties
         * @property {number|Long|null} [seconds] The number of complete seconds since the start of the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0
         * at `1970-01-01T00:00:00.000Z`.<br/>
         * This value MUST be greater than 0.<br/>
         * This value SHOULD be strictly greater than `946684800`.
         * @property {number|null} [nanos] The number of nanoseconds after the start of the second referenced
         * in `seconds`.
         * <p>
         * This value MUST be greater than or equal to 0.<br/>
         * This value MUST be strictly less than 1,000,000,000.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Timestamp.
         * @memberof proto
         * @interface ITimestamp
         * @augments proto.Timestamp.$Properties
         * @deprecated Use proto.Timestamp.$Properties instead.
         */

        /**
         * Shape of a Timestamp.
         * @typedef {proto.Timestamp.$Properties} proto.Timestamp.$Shape
         */

        /**
         * Constructs a new Timestamp.
         * @memberof proto
         * @classdesc An exact date and time.<br/>
         * This is the same data structure as the Google protobuf Timestamp.proto.
         * 
         * #### Additional Notes
         * Useful information is present in comments on the
         * [Google version](https://github.com/google/protobuf/blob/master/src/google/protobuf/timestamp.proto).
         * @constructor
         * @param {proto.Timestamp.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Timestamp = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The number of complete seconds since the start of the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0
         * at `1970-01-01T00:00:00.000Z`.<br/>
         * This value MUST be greater than 0.<br/>
         * This value SHOULD be strictly greater than `946684800`.
         * @member {number|Long} seconds
         * @memberof proto.Timestamp
         * @instance
         */
        Timestamp.prototype.seconds = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The number of nanoseconds after the start of the second referenced
         * in `seconds`.
         * <p>
         * This value MUST be greater than or equal to 0.<br/>
         * This value MUST be strictly less than 1,000,000,000.
         * @member {number} nanos
         * @memberof proto.Timestamp
         * @instance
         */
        Timestamp.prototype.nanos = 0;

        /**
         * Creates a new Timestamp instance using the specified properties.
         * @function create
         * @memberof proto.Timestamp
         * @static
         * @param {proto.Timestamp.$Properties=} [properties] Properties to set
         * @returns {proto.Timestamp} Timestamp instance
         * @type {{
         *   (properties: proto.Timestamp.$Shape): proto.Timestamp & proto.Timestamp.$Shape;
         *   (properties?: proto.Timestamp.$Properties): proto.Timestamp;
         * }}
         */
        Timestamp.create = function(properties) {
            return new Timestamp(properties);
        };

        /**
         * Encodes the specified Timestamp message. Does not implicitly {@link proto.Timestamp.verify|verify} messages.
         * @function encode
         * @memberof proto.Timestamp
         * @static
         * @param {proto.Timestamp.$Properties} message Timestamp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Timestamp.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.seconds != null && $Object.hasOwnProperty.call(message, "seconds") && (typeof message.seconds === "object" ? message.seconds.low || message.seconds.high : message.seconds !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.seconds);
            if (message.nanos != null && $Object.hasOwnProperty.call(message, "nanos") && message.nanos !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.nanos);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Timestamp message, length delimited. Does not implicitly {@link proto.Timestamp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Timestamp
         * @static
         * @param {proto.Timestamp.$Properties} message Timestamp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Timestamp.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Timestamp message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Timestamp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Timestamp & proto.Timestamp.$Shape} Timestamp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Timestamp.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Timestamp();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.seconds = value;
                        else
                            delete message.seconds;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.nanos = value;
                        else
                            delete message.nanos;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Timestamp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Timestamp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Timestamp & proto.Timestamp.$Shape} Timestamp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Timestamp.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Timestamp message.
         * @function verify
         * @memberof proto.Timestamp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Timestamp.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seconds != null && $Object.hasOwnProperty.call(message, "seconds"))
                if (!$util.isInteger(message.seconds) && !(message.seconds && $util.isInteger(message.seconds.low) && $util.isInteger(message.seconds.high)))
                    return "seconds: integer|Long expected";
            if (message.nanos != null && $Object.hasOwnProperty.call(message, "nanos"))
                if (!$util.isInteger(message.nanos))
                    return "nanos: integer expected";
            return null;
        };

        /**
         * Creates a Timestamp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Timestamp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Timestamp} Timestamp
         */
        Timestamp.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Timestamp)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Timestamp: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Timestamp();
            if (object.seconds != null)
                if (typeof object.seconds === "object" ? object.seconds.low || object.seconds.high : $Number(object.seconds) !== 0)
                    if ($util.Long)
                        message.seconds = $util.Long.fromValue(object.seconds, false);
                    else if (typeof object.seconds === "string")
                        message.seconds = $parseInt(object.seconds, 10);
                    else if (typeof object.seconds === "number")
                        message.seconds = object.seconds;
                    else if (typeof object.seconds === "object")
                        message.seconds = new $util.LongBits(object.seconds.low >>> 0, object.seconds.high >>> 0).toNumber();
            if (object.nanos != null)
                if ($Number(object.nanos) !== 0)
                    message.nanos = object.nanos | 0;
            return message;
        };

        /**
         * Creates a plain object from a Timestamp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Timestamp
         * @static
         * @param {proto.Timestamp} message Timestamp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Timestamp.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.seconds = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.seconds = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.nanos = 0;
            }
            if (message.seconds != null && $Object.hasOwnProperty.call(message, "seconds"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.seconds = typeof message.seconds === "number" ? $BigInt(message.seconds) : $util.Long.fromBits(message.seconds.low >>> 0, message.seconds.high >>> 0, false).toBigInt();
                else if (typeof message.seconds === "number")
                    object.seconds = options.longs === $String ? $String(message.seconds) : message.seconds;
                else
                    object.seconds = options.longs === $String ? $util.Long.prototype.toString.call(message.seconds) : options.longs === $Number ? new $util.LongBits(message.seconds.low >>> 0, message.seconds.high >>> 0).toNumber() : message.seconds;
            if (message.nanos != null && $Object.hasOwnProperty.call(message, "nanos"))
                object.nanos = message.nanos;
            return object;
        };

        /**
         * Converts this Timestamp to JSON.
         * @function toJSON
         * @memberof proto.Timestamp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Timestamp.prototype.toJSON = function() {
            return Timestamp.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Timestamp
         * @function getTypeUrl
         * @memberof proto.Timestamp
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Timestamp.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Timestamp";
        };

        return Timestamp;
    })();

    proto.TimestampSeconds = (function() {

        /**
         * Properties of a TimestampSeconds.
         * @typedef {Object} proto.TimestampSeconds.$Properties
         * @property {number|Long|null} [seconds] The number of complete seconds since the start of the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0
         * at `1970-01-01T00:00:00.000Z`.<br/>
         * This value MUST be greater than 0.<br/>
         * This value SHOULD be strictly greater than `946684800`.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TimestampSeconds.
         * @memberof proto
         * @interface ITimestampSeconds
         * @augments proto.TimestampSeconds.$Properties
         * @deprecated Use proto.TimestampSeconds.$Properties instead.
         */

        /**
         * Shape of a TimestampSeconds.
         * @typedef {proto.TimestampSeconds.$Properties} proto.TimestampSeconds.$Shape
         */

        /**
         * Constructs a new TimestampSeconds.
         * @memberof proto
         * @classdesc An exact date and time, with a resolution of one second.
         * @constructor
         * @param {proto.TimestampSeconds.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TimestampSeconds = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The number of complete seconds since the start of the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0
         * at `1970-01-01T00:00:00.000Z`.<br/>
         * This value MUST be greater than 0.<br/>
         * This value SHOULD be strictly greater than `946684800`.
         * @member {number|Long} seconds
         * @memberof proto.TimestampSeconds
         * @instance
         */
        TimestampSeconds.prototype.seconds = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new TimestampSeconds instance using the specified properties.
         * @function create
         * @memberof proto.TimestampSeconds
         * @static
         * @param {proto.TimestampSeconds.$Properties=} [properties] Properties to set
         * @returns {proto.TimestampSeconds} TimestampSeconds instance
         * @type {{
         *   (properties: proto.TimestampSeconds.$Shape): proto.TimestampSeconds & proto.TimestampSeconds.$Shape;
         *   (properties?: proto.TimestampSeconds.$Properties): proto.TimestampSeconds;
         * }}
         */
        TimestampSeconds.create = function(properties) {
            return new TimestampSeconds(properties);
        };

        /**
         * Encodes the specified TimestampSeconds message. Does not implicitly {@link proto.TimestampSeconds.verify|verify} messages.
         * @function encode
         * @memberof proto.TimestampSeconds
         * @static
         * @param {proto.TimestampSeconds.$Properties} message TimestampSeconds message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TimestampSeconds.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.seconds != null && $Object.hasOwnProperty.call(message, "seconds") && (typeof message.seconds === "object" ? message.seconds.low || message.seconds.high : message.seconds !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.seconds);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TimestampSeconds message, length delimited. Does not implicitly {@link proto.TimestampSeconds.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TimestampSeconds
         * @static
         * @param {proto.TimestampSeconds.$Properties} message TimestampSeconds message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TimestampSeconds.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TimestampSeconds message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TimestampSeconds
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TimestampSeconds & proto.TimestampSeconds.$Shape} TimestampSeconds
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TimestampSeconds.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TimestampSeconds();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.seconds = value;
                        else
                            delete message.seconds;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TimestampSeconds message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TimestampSeconds
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TimestampSeconds & proto.TimestampSeconds.$Shape} TimestampSeconds
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TimestampSeconds.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TimestampSeconds message.
         * @function verify
         * @memberof proto.TimestampSeconds
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TimestampSeconds.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seconds != null && $Object.hasOwnProperty.call(message, "seconds"))
                if (!$util.isInteger(message.seconds) && !(message.seconds && $util.isInteger(message.seconds.low) && $util.isInteger(message.seconds.high)))
                    return "seconds: integer|Long expected";
            return null;
        };

        /**
         * Creates a TimestampSeconds message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TimestampSeconds
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TimestampSeconds} TimestampSeconds
         */
        TimestampSeconds.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TimestampSeconds)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TimestampSeconds: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TimestampSeconds();
            if (object.seconds != null)
                if (typeof object.seconds === "object" ? object.seconds.low || object.seconds.high : $Number(object.seconds) !== 0)
                    if ($util.Long)
                        message.seconds = $util.Long.fromValue(object.seconds, false);
                    else if (typeof object.seconds === "string")
                        message.seconds = $parseInt(object.seconds, 10);
                    else if (typeof object.seconds === "number")
                        message.seconds = object.seconds;
                    else if (typeof object.seconds === "object")
                        message.seconds = new $util.LongBits(object.seconds.low >>> 0, object.seconds.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a TimestampSeconds message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TimestampSeconds
         * @static
         * @param {proto.TimestampSeconds} message TimestampSeconds
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TimestampSeconds.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.seconds = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.seconds = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.seconds != null && $Object.hasOwnProperty.call(message, "seconds"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.seconds = typeof message.seconds === "number" ? $BigInt(message.seconds) : $util.Long.fromBits(message.seconds.low >>> 0, message.seconds.high >>> 0, false).toBigInt();
                else if (typeof message.seconds === "number")
                    object.seconds = options.longs === $String ? $String(message.seconds) : message.seconds;
                else
                    object.seconds = options.longs === $String ? $util.Long.prototype.toString.call(message.seconds) : options.longs === $Number ? new $util.LongBits(message.seconds.low >>> 0, message.seconds.high >>> 0).toNumber() : message.seconds;
            return object;
        };

        /**
         * Converts this TimestampSeconds to JSON.
         * @function toJSON
         * @memberof proto.TimestampSeconds
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TimestampSeconds.prototype.toJSON = function() {
            return TimestampSeconds.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TimestampSeconds
         * @function getTypeUrl
         * @memberof proto.TimestampSeconds
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TimestampSeconds.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TimestampSeconds";
        };

        return TimestampSeconds;
    })();

    proto.AddressBookService = (function() {

        /**
         * Constructs a new AddressBookService service.
         * @memberof proto
         * @classdesc The Address Book service provides the ability for Hedera network node
         * administrators to add, update, and remove consensus nodes. This addition,
         * update, or removal of a consensus node requires governing council approval,
         * but each node operator may update their own operational attributes without
         * additional approval, reducing overhead for routine operations.
         * 
         * Most operations are `privileged operations` and require governing council
         * approval.
         * 
         * ### For a node creation transaction.
         * - The node operator SHALL create a `createNode` transaction.
         * - The node operator MUST sign this transaction with the `Key`
         * set as the `admin_key` for the new `Node`.
         * - The node operator SHALL deliver the signed transaction to the Hedera
         * council representative.
         * - The Hedera council representative SHALL arrange for council members to
         * review and sign the transaction.
         * - Once sufficient council members have signed the transaction, the
         * Hedera council representative SHALL submit the transaction to the
         * network.
         * - Upon receipt of a valid and signed node creation transaction the network
         * software SHALL
         * - Validate the threshold signature for the Hedera governing council
         * - Validate the signature of the `Key` provided as the new `admin_key`
         * for the `Node`.
         * - Create the new node in state, this new node SHALL NOT be active in the
         * network at this time.
         * - When executing the next `freeze` transaction with `freeze_type` set to
         * `PREPARE_UPGRADE`, update network configuration and bring the
         * new node to an active status within the network. The node to be added
         * SHALL be active in the network following this upgrade.
         * 
         * ### For a node deletion transaction.
         * - The node operator or Hedera council representative SHALL create a
         * `deleteNode` transaction.
         * - If the node operator creates the transaction
         * - The node operator MUST sign this transaction with the `Key`
         * set as the `admin_key` for the existing `Node`.
         * - The node operator SHALL deliver the signed transaction to the Hedera
         * council representative.
         * - The Hedera council representative SHALL arrange for council members to
         * review and sign the transaction.
         * - Once sufficient council members have signed the transaction, the
         * Hedera council representative SHALL submit the transaction to the
         * network.
         * - Upon receipt of a valid and signed node deletion transaction the network
         * software SHALL
         * - Validate the signature for the Hedera governing council
         * - Remove the existing node from network state. The node SHALL still
         * be active in the network at this time.
         * - When executing the next `freeze` transaction with `freeze_type` set to
         * `PREPARE_UPGRADE`, update network configuration and remove the
         * node to be deleted from the network. The node to be deleted SHALL NOT
         * be active in the network following this upgrade.
         * 
         * ### For a node update transaction.
         * - The node operator SHALL create an `updateNode` transaction.
         * - The node operator MUST sign this transaction with the active `key`
         * assigned as the `admin_key`.
         * - The node operator SHALL submit the transaction to the
         * network.  Hedera council approval SHALL NOT be sought for this
         * transaction
         * - Upon receipt of a valid and signed node update transaction the network
         * software SHALL
         * - If the transaction modifies the value of the "node account",
         * - Validate the signature of the active `key` for the account
         * assigned as the _current_ "node account".
         * - Validate the signature of the active `key` for the account to be
         * assigned as the _new_ "node account".
         * - Modify the node information held in network state with the changes
         * requested in the update transaction. The node changes SHALL NOT be
         * applied to network configuration, and SHALL NOT affect network
         * operation at this time.
         * - When executing the next `freeze` transaction with `freeze_type` set to
         * `PREPARE_UPGRADE`, update network configuration according to the
         * modified information in network state. The requested changes SHALL
         * affect network operation following this upgrade.
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const AddressBookService = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        $Object.defineProperty(AddressBookService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: AddressBookService, writable: true, enumerable: false, configurable: true });

        /**
         * Creates new AddressBookService service using the specified rpc implementation.
         * @function create
         * @memberof proto.AddressBookService
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {AddressBookService} RPC service. Useful where requests and/or responses are streamed.
         */
        AddressBookService.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link proto.AddressBookService#createNode}.
         * @memberof proto.AddressBookService
         * @typedef createNodeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * A transaction to create a new consensus node in the network
         * address book.
         * <p>
         * This transaction, once complete, SHALL add a new consensus node to the
         * network state.<br/>
         * The new consensus node SHALL remain in state, but SHALL NOT participate
         * in network consensus until the network updates the network configuration.
         * <p>
         * Hedera governing council authorization is REQUIRED for this transaction.
         * @memberof proto.AddressBookService
         * @typedef createNode
         * @type {{
         *   (request: proto.ITransaction, callback: proto.AddressBookService.createNodeCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "createNode";
         *   readonly path: "/proto.AddressBookService/createNode";
         *   readonly requestType: "proto.Transaction";
         *   readonly responseType: "proto.TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * A transaction to create a new consensus node in the network
         * address book.
         * <p>
         * This transaction, once complete, SHALL add a new consensus node to the
         * network state.<br/>
         * The new consensus node SHALL remain in state, but SHALL NOT participate
         * in network consensus until the network updates the network configuration.
         * <p>
         * Hedera governing council authorization is REQUIRED for this transaction.
         * @name proto.AddressBookService#createNode
         * @type {proto.AddressBookService.createNode}
         */
        $Object.defineProperties(AddressBookService.prototype.createNode = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, AddressBookService.prototype.createNode, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "createNode" },
            path: { value: "/proto.AddressBookService/createNode" },
            requestType: { value: "proto.Transaction" },
            responseType: { value: "proto.TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.AddressBookService#deleteNode}.
         * @memberof proto.AddressBookService
         * @typedef deleteNodeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * A transaction to remove a consensus node from the network address
         * book.
         * <p>
         * This transaction, once complete, SHALL remove the identified consensus
         * node from the network state.
         * <p>
         * Hedera governing council authorization is REQUIRED for this transaction.
         * @memberof proto.AddressBookService
         * @typedef deleteNode
         * @type {{
         *   (request: proto.ITransaction, callback: proto.AddressBookService.deleteNodeCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "deleteNode";
         *   readonly path: "/proto.AddressBookService/deleteNode";
         *   readonly requestType: "proto.Transaction";
         *   readonly responseType: "proto.TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * A transaction to remove a consensus node from the network address
         * book.
         * <p>
         * This transaction, once complete, SHALL remove the identified consensus
         * node from the network state.
         * <p>
         * Hedera governing council authorization is REQUIRED for this transaction.
         * @name proto.AddressBookService#deleteNode
         * @type {proto.AddressBookService.deleteNode}
         */
        $Object.defineProperties(AddressBookService.prototype.deleteNode = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, AddressBookService.prototype.deleteNode, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "deleteNode" },
            path: { value: "/proto.AddressBookService/deleteNode" },
            requestType: { value: "proto.Transaction" },
            responseType: { value: "proto.TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.AddressBookService#updateNode}.
         * @memberof proto.AddressBookService
         * @typedef updateNodeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * A transaction to update an existing consensus node from the network
         * address book.
         * <p>
         * This transaction, once complete, SHALL modify the identified consensus
         * node state as requested.
         * <p>
         * This transaction is authorized by the node operator
         * @memberof proto.AddressBookService
         * @typedef updateNode
         * @type {{
         *   (request: proto.ITransaction, callback: proto.AddressBookService.updateNodeCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "updateNode";
         *   readonly path: "/proto.AddressBookService/updateNode";
         *   readonly requestType: "proto.Transaction";
         *   readonly responseType: "proto.TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * A transaction to update an existing consensus node from the network
         * address book.
         * <p>
         * This transaction, once complete, SHALL modify the identified consensus
         * node state as requested.
         * <p>
         * This transaction is authorized by the node operator
         * @name proto.AddressBookService#updateNode
         * @type {proto.AddressBookService.updateNode}
         */
        $Object.defineProperties(AddressBookService.prototype.updateNode = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, AddressBookService.prototype.updateNode, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "updateNode" },
            path: { value: "/proto.AddressBookService/updateNode" },
            requestType: { value: "proto.Transaction" },
            responseType: { value: "proto.TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.AddressBookService#createRegisteredNode}.
         * @memberof proto.AddressBookService
         * @typedef createRegisteredNodeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * A transaction to create a new registered node in the network
         * address book.
         * <p>
         * This transaction, once complete, SHALL add a new registered node to the
         * network state.<br/>
         * The new registered node SHALL be visible and discoverable upon
         * completion of this transaction.
         * @memberof proto.AddressBookService
         * @typedef createRegisteredNode
         * @type {{
         *   (request: proto.ITransaction, callback: proto.AddressBookService.createRegisteredNodeCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "createRegisteredNode";
         *   readonly path: "/proto.AddressBookService/createRegisteredNode";
         *   readonly requestType: "proto.Transaction";
         *   readonly responseType: "proto.TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * A transaction to create a new registered node in the network
         * address book.
         * <p>
         * This transaction, once complete, SHALL add a new registered node to the
         * network state.<br/>
         * The new registered node SHALL be visible and discoverable upon
         * completion of this transaction.
         * @name proto.AddressBookService#createRegisteredNode
         * @type {proto.AddressBookService.createRegisteredNode}
         */
        $Object.defineProperties(AddressBookService.prototype.createRegisteredNode = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, AddressBookService.prototype.createRegisteredNode, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "createRegisteredNode" },
            path: { value: "/proto.AddressBookService/createRegisteredNode" },
            requestType: { value: "proto.Transaction" },
            responseType: { value: "proto.TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.AddressBookService#deleteRegisteredNode}.
         * @memberof proto.AddressBookService
         * @typedef deleteRegisteredNodeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * A transaction to remove a registered node from the network address
         * book.
         * <p>
         * This transaction, once complete, SHALL remove the identified registered
         * node from the network state.<br/>
         * This transaction MUST be signed by the existing entry `admin_key` or
         * authorized by the Hiero network governance structure.
         * @memberof proto.AddressBookService
         * @typedef deleteRegisteredNode
         * @type {{
         *   (request: proto.ITransaction, callback: proto.AddressBookService.deleteRegisteredNodeCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "deleteRegisteredNode";
         *   readonly path: "/proto.AddressBookService/deleteRegisteredNode";
         *   readonly requestType: "proto.Transaction";
         *   readonly responseType: "proto.TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * A transaction to remove a registered node from the network address
         * book.
         * <p>
         * This transaction, once complete, SHALL remove the identified registered
         * node from the network state.<br/>
         * This transaction MUST be signed by the existing entry `admin_key` or
         * authorized by the Hiero network governance structure.
         * @name proto.AddressBookService#deleteRegisteredNode
         * @type {proto.AddressBookService.deleteRegisteredNode}
         */
        $Object.defineProperties(AddressBookService.prototype.deleteRegisteredNode = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, AddressBookService.prototype.deleteRegisteredNode, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "deleteRegisteredNode" },
            path: { value: "/proto.AddressBookService/deleteRegisteredNode" },
            requestType: { value: "proto.Transaction" },
            responseType: { value: "proto.TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.AddressBookService#updateRegisteredNode}.
         * @memberof proto.AddressBookService
         * @typedef updateRegisteredNodeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * A transaction to update an existing registered node in the network
         * address book.
         * <p>
         * This transaction, once complete, SHALL modify the identified registered
         * node state as requested.
         * @memberof proto.AddressBookService
         * @typedef updateRegisteredNode
         * @type {{
         *   (request: proto.ITransaction, callback: proto.AddressBookService.updateRegisteredNodeCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "updateRegisteredNode";
         *   readonly path: "/proto.AddressBookService/updateRegisteredNode";
         *   readonly requestType: "proto.Transaction";
         *   readonly responseType: "proto.TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * A transaction to update an existing registered node in the network
         * address book.
         * <p>
         * This transaction, once complete, SHALL modify the identified registered
         * node state as requested.
         * @name proto.AddressBookService#updateRegisteredNode
         * @type {proto.AddressBookService.updateRegisteredNode}
         */
        $Object.defineProperties(AddressBookService.prototype.updateRegisteredNode = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, AddressBookService.prototype.updateRegisteredNode, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "updateRegisteredNode" },
            path: { value: "/proto.AddressBookService/updateRegisteredNode" },
            requestType: { value: "proto.Transaction" },
            responseType: { value: "proto.TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return AddressBookService;
    })();

    proto.TransactionResponse = (function() {

        /**
         * Properties of a TransactionResponse.
         * @typedef {Object} proto.TransactionResponse.$Properties
         * @property {proto.ResponseCodeEnum|null} [nodeTransactionPrecheckCode] A pre-consensus response code.
         * <p>
         * This response SHALL represent the response of the individual node, and
         * SHALL NOT represent the consensus of the network.
         * @property {number|Long|null} [cost] An approximate transaction fee.
         * <p>
         * This value SHALL be `0` unless the `nodeTransactionPrecheckCode` is
         * `INSUFFICIENT_TX_FEE`.<br/>
         * This value SHOULD be an amount, in tinybar, that _would have_ succeeded
         * at the time the transaction was submitted.<br/>
         * Note that this amount is not guaranteed to succeed in a future
         * transaction due to uncontrolled variables, such as network congestion,
         * but should be considered a close approximation.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionResponse.
         * @memberof proto
         * @interface ITransactionResponse
         * @augments proto.TransactionResponse.$Properties
         * @deprecated Use proto.TransactionResponse.$Properties instead.
         */

        /**
         * Shape of a TransactionResponse.
         * @typedef {proto.TransactionResponse.$Properties} proto.TransactionResponse.$Shape
         */

        /**
         * Constructs a new TransactionResponse.
         * @memberof proto
         * @classdesc A message sent by a node in response to a transaction submission.<br/>
         * This message only acknowledges that the individual node has checked
         * the transaction, completed pre-check, and checked the fee offered.
         * 
         * If the transaction fee is not sufficient, the `nodeTransactionPrecheckCode`
         * value SHALL be `INSUFFICIENT_TX_FEE` and the `cost` field SHALL be the
         * actual transaction fee, in tinybar, required.<br/>
         * If the client requires acknowledgement of the network consensus result
         * for a transaction, the client SHOULD request a transaction receipt or
         * detailed transaction record. A client MAY also obtain network consensus
         * results from a mirror node.
         * @constructor
         * @param {proto.TransactionResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A pre-consensus response code.
         * <p>
         * This response SHALL represent the response of the individual node, and
         * SHALL NOT represent the consensus of the network.
         * @member {proto.ResponseCodeEnum} nodeTransactionPrecheckCode
         * @memberof proto.TransactionResponse
         * @instance
         */
        TransactionResponse.prototype.nodeTransactionPrecheckCode = 0;

        /**
         * An approximate transaction fee.
         * <p>
         * This value SHALL be `0` unless the `nodeTransactionPrecheckCode` is
         * `INSUFFICIENT_TX_FEE`.<br/>
         * This value SHOULD be an amount, in tinybar, that _would have_ succeeded
         * at the time the transaction was submitted.<br/>
         * Note that this amount is not guaranteed to succeed in a future
         * transaction due to uncontrolled variables, such as network congestion,
         * but should be considered a close approximation.
         * @member {number|Long} cost
         * @memberof proto.TransactionResponse
         * @instance
         */
        TransactionResponse.prototype.cost = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new TransactionResponse instance using the specified properties.
         * @function create
         * @memberof proto.TransactionResponse
         * @static
         * @param {proto.TransactionResponse.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionResponse} TransactionResponse instance
         * @type {{
         *   (properties: proto.TransactionResponse.$Shape): proto.TransactionResponse & proto.TransactionResponse.$Shape;
         *   (properties?: proto.TransactionResponse.$Properties): proto.TransactionResponse;
         * }}
         */
        TransactionResponse.create = function(properties) {
            return new TransactionResponse(properties);
        };

        /**
         * Encodes the specified TransactionResponse message. Does not implicitly {@link proto.TransactionResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionResponse
         * @static
         * @param {proto.TransactionResponse.$Properties} message TransactionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nodeTransactionPrecheckCode != null && $Object.hasOwnProperty.call(message, "nodeTransactionPrecheckCode") && message.nodeTransactionPrecheckCode !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.nodeTransactionPrecheckCode);
            if (message.cost != null && $Object.hasOwnProperty.call(message, "cost") && (typeof message.cost === "object" ? message.cost.low || message.cost.high : message.cost !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.cost);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionResponse message, length delimited. Does not implicitly {@link proto.TransactionResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionResponse
         * @static
         * @param {proto.TransactionResponse.$Properties} message TransactionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionResponse & proto.TransactionResponse.$Shape} TransactionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.nodeTransactionPrecheckCode = value;
                        else
                            delete message.nodeTransactionPrecheckCode;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.cost = value;
                        else
                            delete message.cost;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionResponse & proto.TransactionResponse.$Shape} TransactionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionResponse message.
         * @function verify
         * @memberof proto.TransactionResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nodeTransactionPrecheckCode != null && $Object.hasOwnProperty.call(message, "nodeTransactionPrecheckCode"))
                if (typeof message.nodeTransactionPrecheckCode !== "number" || (message.nodeTransactionPrecheckCode | 0) !== message.nodeTransactionPrecheckCode)
                    return "nodeTransactionPrecheckCode: enum value expected";
            if (message.cost != null && $Object.hasOwnProperty.call(message, "cost"))
                if (!$util.isInteger(message.cost) && !(message.cost && $util.isInteger(message.cost.low) && $util.isInteger(message.cost.high)))
                    return "cost: integer|Long expected";
            return null;
        };

        /**
         * Creates a TransactionResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionResponse} TransactionResponse
         */
        TransactionResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionResponse();
            if (object.nodeTransactionPrecheckCode !== 0 && (typeof object.nodeTransactionPrecheckCode !== "string" || $root.proto.ResponseCodeEnum[object.nodeTransactionPrecheckCode] !== 0))
                switch (object.nodeTransactionPrecheckCode) {
                case "OK":
                case 0:
                    message.nodeTransactionPrecheckCode = 0;
                    break;
                case "INVALID_TRANSACTION":
                case 1:
                    message.nodeTransactionPrecheckCode = 1;
                    break;
                case "PAYER_ACCOUNT_NOT_FOUND":
                case 2:
                    message.nodeTransactionPrecheckCode = 2;
                    break;
                case "INVALID_NODE_ACCOUNT":
                case 3:
                    message.nodeTransactionPrecheckCode = 3;
                    break;
                case "TRANSACTION_EXPIRED":
                case 4:
                    message.nodeTransactionPrecheckCode = 4;
                    break;
                case "INVALID_TRANSACTION_START":
                case 5:
                    message.nodeTransactionPrecheckCode = 5;
                    break;
                case "INVALID_TRANSACTION_DURATION":
                case 6:
                    message.nodeTransactionPrecheckCode = 6;
                    break;
                case "INVALID_SIGNATURE":
                case 7:
                    message.nodeTransactionPrecheckCode = 7;
                    break;
                case "MEMO_TOO_LONG":
                case 8:
                    message.nodeTransactionPrecheckCode = 8;
                    break;
                case "INSUFFICIENT_TX_FEE":
                case 9:
                    message.nodeTransactionPrecheckCode = 9;
                    break;
                case "INSUFFICIENT_PAYER_BALANCE":
                case 10:
                    message.nodeTransactionPrecheckCode = 10;
                    break;
                case "DUPLICATE_TRANSACTION":
                case 11:
                    message.nodeTransactionPrecheckCode = 11;
                    break;
                case "BUSY":
                case 12:
                    message.nodeTransactionPrecheckCode = 12;
                    break;
                case "NOT_SUPPORTED":
                case 13:
                    message.nodeTransactionPrecheckCode = 13;
                    break;
                case "INVALID_FILE_ID":
                case 14:
                    message.nodeTransactionPrecheckCode = 14;
                    break;
                case "INVALID_ACCOUNT_ID":
                case 15:
                    message.nodeTransactionPrecheckCode = 15;
                    break;
                case "INVALID_CONTRACT_ID":
                case 16:
                    message.nodeTransactionPrecheckCode = 16;
                    break;
                case "INVALID_TRANSACTION_ID":
                case 17:
                    message.nodeTransactionPrecheckCode = 17;
                    break;
                case "RECEIPT_NOT_FOUND":
                case 18:
                    message.nodeTransactionPrecheckCode = 18;
                    break;
                case "RECORD_NOT_FOUND":
                case 19:
                    message.nodeTransactionPrecheckCode = 19;
                    break;
                case "INVALID_SOLIDITY_ID":
                case 20:
                    message.nodeTransactionPrecheckCode = 20;
                    break;
                case "UNKNOWN":
                case 21:
                    message.nodeTransactionPrecheckCode = 21;
                    break;
                case "SUCCESS":
                case 22:
                    message.nodeTransactionPrecheckCode = 22;
                    break;
                case "FAIL_INVALID":
                case 23:
                    message.nodeTransactionPrecheckCode = 23;
                    break;
                case "FAIL_FEE":
                case 24:
                    message.nodeTransactionPrecheckCode = 24;
                    break;
                case "FAIL_BALANCE":
                case 25:
                    message.nodeTransactionPrecheckCode = 25;
                    break;
                case "KEY_REQUIRED":
                case 26:
                    message.nodeTransactionPrecheckCode = 26;
                    break;
                case "BAD_ENCODING":
                case 27:
                    message.nodeTransactionPrecheckCode = 27;
                    break;
                case "INSUFFICIENT_ACCOUNT_BALANCE":
                case 28:
                    message.nodeTransactionPrecheckCode = 28;
                    break;
                case "INVALID_SOLIDITY_ADDRESS":
                case 29:
                    message.nodeTransactionPrecheckCode = 29;
                    break;
                case "INSUFFICIENT_GAS":
                case 30:
                    message.nodeTransactionPrecheckCode = 30;
                    break;
                case "CONTRACT_SIZE_LIMIT_EXCEEDED":
                case 31:
                    message.nodeTransactionPrecheckCode = 31;
                    break;
                case "LOCAL_CALL_MODIFICATION_EXCEPTION":
                case 32:
                    message.nodeTransactionPrecheckCode = 32;
                    break;
                case "CONTRACT_REVERT_EXECUTED":
                case 33:
                    message.nodeTransactionPrecheckCode = 33;
                    break;
                case "CONTRACT_EXECUTION_EXCEPTION":
                case 34:
                    message.nodeTransactionPrecheckCode = 34;
                    break;
                case "INVALID_RECEIVING_NODE_ACCOUNT":
                case 35:
                    message.nodeTransactionPrecheckCode = 35;
                    break;
                case "MISSING_QUERY_HEADER":
                case 36:
                    message.nodeTransactionPrecheckCode = 36;
                    break;
                case "ACCOUNT_UPDATE_FAILED":
                case 37:
                    message.nodeTransactionPrecheckCode = 37;
                    break;
                case "INVALID_KEY_ENCODING":
                case 38:
                    message.nodeTransactionPrecheckCode = 38;
                    break;
                case "NULL_SOLIDITY_ADDRESS":
                case 39:
                    message.nodeTransactionPrecheckCode = 39;
                    break;
                case "CONTRACT_UPDATE_FAILED":
                case 40:
                    message.nodeTransactionPrecheckCode = 40;
                    break;
                case "INVALID_QUERY_HEADER":
                case 41:
                    message.nodeTransactionPrecheckCode = 41;
                    break;
                case "INVALID_FEE_SUBMITTED":
                case 42:
                    message.nodeTransactionPrecheckCode = 42;
                    break;
                case "INVALID_PAYER_SIGNATURE":
                case 43:
                    message.nodeTransactionPrecheckCode = 43;
                    break;
                case "KEY_NOT_PROVIDED":
                case 44:
                    message.nodeTransactionPrecheckCode = 44;
                    break;
                case "INVALID_EXPIRATION_TIME":
                case 45:
                    message.nodeTransactionPrecheckCode = 45;
                    break;
                case "NO_WACL_KEY":
                case 46:
                    message.nodeTransactionPrecheckCode = 46;
                    break;
                case "FILE_CONTENT_EMPTY":
                case 47:
                    message.nodeTransactionPrecheckCode = 47;
                    break;
                case "INVALID_ACCOUNT_AMOUNTS":
                case 48:
                    message.nodeTransactionPrecheckCode = 48;
                    break;
                case "EMPTY_TRANSACTION_BODY":
                case 49:
                    message.nodeTransactionPrecheckCode = 49;
                    break;
                case "INVALID_TRANSACTION_BODY":
                case 50:
                    message.nodeTransactionPrecheckCode = 50;
                    break;
                case "INVALID_SIGNATURE_TYPE_MISMATCHING_KEY":
                case 51:
                    message.nodeTransactionPrecheckCode = 51;
                    break;
                case "INVALID_SIGNATURE_COUNT_MISMATCHING_KEY":
                case 52:
                    message.nodeTransactionPrecheckCode = 52;
                    break;
                case "EMPTY_LIVE_HASH_BODY":
                case 53:
                    message.nodeTransactionPrecheckCode = 53;
                    break;
                case "EMPTY_LIVE_HASH":
                case 54:
                    message.nodeTransactionPrecheckCode = 54;
                    break;
                case "EMPTY_LIVE_HASH_KEYS":
                case 55:
                    message.nodeTransactionPrecheckCode = 55;
                    break;
                case "INVALID_LIVE_HASH_SIZE":
                case 56:
                    message.nodeTransactionPrecheckCode = 56;
                    break;
                case "EMPTY_QUERY_BODY":
                case 57:
                    message.nodeTransactionPrecheckCode = 57;
                    break;
                case "EMPTY_LIVE_HASH_QUERY":
                case 58:
                    message.nodeTransactionPrecheckCode = 58;
                    break;
                case "LIVE_HASH_NOT_FOUND":
                case 59:
                    message.nodeTransactionPrecheckCode = 59;
                    break;
                case "ACCOUNT_ID_DOES_NOT_EXIST":
                case 60:
                    message.nodeTransactionPrecheckCode = 60;
                    break;
                case "LIVE_HASH_ALREADY_EXISTS":
                case 61:
                    message.nodeTransactionPrecheckCode = 61;
                    break;
                case "INVALID_FILE_WACL":
                case 62:
                    message.nodeTransactionPrecheckCode = 62;
                    break;
                case "SERIALIZATION_FAILED":
                case 63:
                    message.nodeTransactionPrecheckCode = 63;
                    break;
                case "TRANSACTION_OVERSIZE":
                case 64:
                    message.nodeTransactionPrecheckCode = 64;
                    break;
                case "TRANSACTION_TOO_MANY_LAYERS":
                case 65:
                    message.nodeTransactionPrecheckCode = 65;
                    break;
                case "CONTRACT_DELETED":
                case 66:
                    message.nodeTransactionPrecheckCode = 66;
                    break;
                case "PLATFORM_NOT_ACTIVE":
                case 67:
                    message.nodeTransactionPrecheckCode = 67;
                    break;
                case "KEY_PREFIX_MISMATCH":
                case 68:
                    message.nodeTransactionPrecheckCode = 68;
                    break;
                case "PLATFORM_TRANSACTION_NOT_CREATED":
                case 69:
                    message.nodeTransactionPrecheckCode = 69;
                    break;
                case "INVALID_RENEWAL_PERIOD":
                case 70:
                    message.nodeTransactionPrecheckCode = 70;
                    break;
                case "INVALID_PAYER_ACCOUNT_ID":
                case 71:
                    message.nodeTransactionPrecheckCode = 71;
                    break;
                case "ACCOUNT_DELETED":
                case 72:
                    message.nodeTransactionPrecheckCode = 72;
                    break;
                case "FILE_DELETED":
                case 73:
                    message.nodeTransactionPrecheckCode = 73;
                    break;
                case "ACCOUNT_REPEATED_IN_ACCOUNT_AMOUNTS":
                case 74:
                    message.nodeTransactionPrecheckCode = 74;
                    break;
                case "SETTING_NEGATIVE_ACCOUNT_BALANCE":
                case 75:
                    message.nodeTransactionPrecheckCode = 75;
                    break;
                case "OBTAINER_REQUIRED":
                case 76:
                    message.nodeTransactionPrecheckCode = 76;
                    break;
                case "OBTAINER_SAME_CONTRACT_ID":
                case 77:
                    message.nodeTransactionPrecheckCode = 77;
                    break;
                case "OBTAINER_DOES_NOT_EXIST":
                case 78:
                    message.nodeTransactionPrecheckCode = 78;
                    break;
                case "MODIFYING_IMMUTABLE_CONTRACT":
                case 79:
                    message.nodeTransactionPrecheckCode = 79;
                    break;
                case "FILE_SYSTEM_EXCEPTION":
                case 80:
                    message.nodeTransactionPrecheckCode = 80;
                    break;
                case "AUTORENEW_DURATION_NOT_IN_RANGE":
                case 81:
                    message.nodeTransactionPrecheckCode = 81;
                    break;
                case "ERROR_DECODING_BYTESTRING":
                case 82:
                    message.nodeTransactionPrecheckCode = 82;
                    break;
                case "CONTRACT_FILE_EMPTY":
                case 83:
                    message.nodeTransactionPrecheckCode = 83;
                    break;
                case "CONTRACT_BYTECODE_EMPTY":
                case 84:
                    message.nodeTransactionPrecheckCode = 84;
                    break;
                case "INVALID_INITIAL_BALANCE":
                case 85:
                    message.nodeTransactionPrecheckCode = 85;
                    break;
                case "INVALID_RECEIVE_RECORD_THRESHOLD":
                case 86:
                    message.nodeTransactionPrecheckCode = 86;
                    break;
                case "INVALID_SEND_RECORD_THRESHOLD":
                case 87:
                    message.nodeTransactionPrecheckCode = 87;
                    break;
                case "ACCOUNT_IS_NOT_GENESIS_ACCOUNT":
                case 88:
                    message.nodeTransactionPrecheckCode = 88;
                    break;
                case "PAYER_ACCOUNT_UNAUTHORIZED":
                case 89:
                    message.nodeTransactionPrecheckCode = 89;
                    break;
                case "INVALID_FREEZE_TRANSACTION_BODY":
                case 90:
                    message.nodeTransactionPrecheckCode = 90;
                    break;
                case "FREEZE_TRANSACTION_BODY_NOT_FOUND":
                case 91:
                    message.nodeTransactionPrecheckCode = 91;
                    break;
                case "TRANSFER_LIST_SIZE_LIMIT_EXCEEDED":
                case 92:
                    message.nodeTransactionPrecheckCode = 92;
                    break;
                case "RESULT_SIZE_LIMIT_EXCEEDED":
                case 93:
                    message.nodeTransactionPrecheckCode = 93;
                    break;
                case "NOT_SPECIAL_ACCOUNT":
                case 94:
                    message.nodeTransactionPrecheckCode = 94;
                    break;
                case "CONTRACT_NEGATIVE_GAS":
                case 95:
                    message.nodeTransactionPrecheckCode = 95;
                    break;
                case "CONTRACT_NEGATIVE_VALUE":
                case 96:
                    message.nodeTransactionPrecheckCode = 96;
                    break;
                case "INVALID_FEE_FILE":
                case 97:
                    message.nodeTransactionPrecheckCode = 97;
                    break;
                case "INVALID_EXCHANGE_RATE_FILE":
                case 98:
                    message.nodeTransactionPrecheckCode = 98;
                    break;
                case "INSUFFICIENT_LOCAL_CALL_GAS":
                case 99:
                    message.nodeTransactionPrecheckCode = 99;
                    break;
                case "ENTITY_NOT_ALLOWED_TO_DELETE":
                case 100:
                    message.nodeTransactionPrecheckCode = 100;
                    break;
                case "AUTHORIZATION_FAILED":
                case 101:
                    message.nodeTransactionPrecheckCode = 101;
                    break;
                case "FILE_UPLOADED_PROTO_INVALID":
                case 102:
                    message.nodeTransactionPrecheckCode = 102;
                    break;
                case "FILE_UPLOADED_PROTO_NOT_SAVED_TO_DISK":
                case 103:
                    message.nodeTransactionPrecheckCode = 103;
                    break;
                case "FEE_SCHEDULE_FILE_PART_UPLOADED":
                case 104:
                    message.nodeTransactionPrecheckCode = 104;
                    break;
                case "EXCHANGE_RATE_CHANGE_LIMIT_EXCEEDED":
                case 105:
                    message.nodeTransactionPrecheckCode = 105;
                    break;
                case "MAX_CONTRACT_STORAGE_EXCEEDED":
                case 106:
                    message.nodeTransactionPrecheckCode = 106;
                    break;
                case "TRANSFER_ACCOUNT_SAME_AS_DELETE_ACCOUNT":
                case 107:
                    message.nodeTransactionPrecheckCode = 107;
                    break;
                case "TOTAL_LEDGER_BALANCE_INVALID":
                case 108:
                    message.nodeTransactionPrecheckCode = 108;
                    break;
                case "EXPIRATION_REDUCTION_NOT_ALLOWED":
                case 110:
                    message.nodeTransactionPrecheckCode = 110;
                    break;
                case "MAX_GAS_LIMIT_EXCEEDED":
                case 111:
                    message.nodeTransactionPrecheckCode = 111;
                    break;
                case "MAX_FILE_SIZE_EXCEEDED":
                case 112:
                    message.nodeTransactionPrecheckCode = 112;
                    break;
                case "RECEIVER_SIG_REQUIRED":
                case 113:
                    message.nodeTransactionPrecheckCode = 113;
                    break;
                case "INVALID_TOPIC_ID":
                case 150:
                    message.nodeTransactionPrecheckCode = 150;
                    break;
                case "INVALID_ADMIN_KEY":
                case 155:
                    message.nodeTransactionPrecheckCode = 155;
                    break;
                case "INVALID_SUBMIT_KEY":
                case 156:
                    message.nodeTransactionPrecheckCode = 156;
                    break;
                case "UNAUTHORIZED":
                case 157:
                    message.nodeTransactionPrecheckCode = 157;
                    break;
                case "INVALID_TOPIC_MESSAGE":
                case 158:
                    message.nodeTransactionPrecheckCode = 158;
                    break;
                case "INVALID_AUTORENEW_ACCOUNT":
                case 159:
                    message.nodeTransactionPrecheckCode = 159;
                    break;
                case "AUTORENEW_ACCOUNT_NOT_ALLOWED":
                case 160:
                    message.nodeTransactionPrecheckCode = 160;
                    break;
                case "TOPIC_EXPIRED":
                case 162:
                    message.nodeTransactionPrecheckCode = 162;
                    break;
                case "INVALID_CHUNK_NUMBER":
                case 163:
                    message.nodeTransactionPrecheckCode = 163;
                    break;
                case "INVALID_CHUNK_TRANSACTION_ID":
                case 164:
                    message.nodeTransactionPrecheckCode = 164;
                    break;
                case "ACCOUNT_FROZEN_FOR_TOKEN":
                case 165:
                    message.nodeTransactionPrecheckCode = 165;
                    break;
                case "TOKENS_PER_ACCOUNT_LIMIT_EXCEEDED":
                case 166:
                    message.nodeTransactionPrecheckCode = 166;
                    break;
                case "INVALID_TOKEN_ID":
                case 167:
                    message.nodeTransactionPrecheckCode = 167;
                    break;
                case "INVALID_TOKEN_DECIMALS":
                case 168:
                    message.nodeTransactionPrecheckCode = 168;
                    break;
                case "INVALID_TOKEN_INITIAL_SUPPLY":
                case 169:
                    message.nodeTransactionPrecheckCode = 169;
                    break;
                case "INVALID_TREASURY_ACCOUNT_FOR_TOKEN":
                case 170:
                    message.nodeTransactionPrecheckCode = 170;
                    break;
                case "INVALID_TOKEN_SYMBOL":
                case 171:
                    message.nodeTransactionPrecheckCode = 171;
                    break;
                case "TOKEN_HAS_NO_FREEZE_KEY":
                case 172:
                    message.nodeTransactionPrecheckCode = 172;
                    break;
                case "TRANSFERS_NOT_ZERO_SUM_FOR_TOKEN":
                case 173:
                    message.nodeTransactionPrecheckCode = 173;
                    break;
                case "MISSING_TOKEN_SYMBOL":
                case 174:
                    message.nodeTransactionPrecheckCode = 174;
                    break;
                case "TOKEN_SYMBOL_TOO_LONG":
                case 175:
                    message.nodeTransactionPrecheckCode = 175;
                    break;
                case "ACCOUNT_KYC_NOT_GRANTED_FOR_TOKEN":
                case 176:
                    message.nodeTransactionPrecheckCode = 176;
                    break;
                case "TOKEN_HAS_NO_KYC_KEY":
                case 177:
                    message.nodeTransactionPrecheckCode = 177;
                    break;
                case "INSUFFICIENT_TOKEN_BALANCE":
                case 178:
                    message.nodeTransactionPrecheckCode = 178;
                    break;
                case "TOKEN_WAS_DELETED":
                case 179:
                    message.nodeTransactionPrecheckCode = 179;
                    break;
                case "TOKEN_HAS_NO_SUPPLY_KEY":
                case 180:
                    message.nodeTransactionPrecheckCode = 180;
                    break;
                case "TOKEN_HAS_NO_WIPE_KEY":
                case 181:
                    message.nodeTransactionPrecheckCode = 181;
                    break;
                case "INVALID_TOKEN_MINT_AMOUNT":
                case 182:
                    message.nodeTransactionPrecheckCode = 182;
                    break;
                case "INVALID_TOKEN_BURN_AMOUNT":
                case 183:
                    message.nodeTransactionPrecheckCode = 183;
                    break;
                case "TOKEN_NOT_ASSOCIATED_TO_ACCOUNT":
                case 184:
                    message.nodeTransactionPrecheckCode = 184;
                    break;
                case "CANNOT_WIPE_TOKEN_TREASURY_ACCOUNT":
                case 185:
                    message.nodeTransactionPrecheckCode = 185;
                    break;
                case "INVALID_KYC_KEY":
                case 186:
                    message.nodeTransactionPrecheckCode = 186;
                    break;
                case "INVALID_WIPE_KEY":
                case 187:
                    message.nodeTransactionPrecheckCode = 187;
                    break;
                case "INVALID_FREEZE_KEY":
                case 188:
                    message.nodeTransactionPrecheckCode = 188;
                    break;
                case "INVALID_SUPPLY_KEY":
                case 189:
                    message.nodeTransactionPrecheckCode = 189;
                    break;
                case "MISSING_TOKEN_NAME":
                case 190:
                    message.nodeTransactionPrecheckCode = 190;
                    break;
                case "TOKEN_NAME_TOO_LONG":
                case 191:
                    message.nodeTransactionPrecheckCode = 191;
                    break;
                case "INVALID_WIPING_AMOUNT":
                case 192:
                    message.nodeTransactionPrecheckCode = 192;
                    break;
                case "TOKEN_IS_IMMUTABLE":
                case 193:
                    message.nodeTransactionPrecheckCode = 193;
                    break;
                case "TOKEN_ALREADY_ASSOCIATED_TO_ACCOUNT":
                case 194:
                    message.nodeTransactionPrecheckCode = 194;
                    break;
                case "TRANSACTION_REQUIRES_ZERO_TOKEN_BALANCES":
                case 195:
                    message.nodeTransactionPrecheckCode = 195;
                    break;
                case "ACCOUNT_IS_TREASURY":
                case 196:
                    message.nodeTransactionPrecheckCode = 196;
                    break;
                case "TOKEN_ID_REPEATED_IN_TOKEN_LIST":
                case 197:
                    message.nodeTransactionPrecheckCode = 197;
                    break;
                case "TOKEN_TRANSFER_LIST_SIZE_LIMIT_EXCEEDED":
                case 198:
                    message.nodeTransactionPrecheckCode = 198;
                    break;
                case "EMPTY_TOKEN_TRANSFER_BODY":
                case 199:
                    message.nodeTransactionPrecheckCode = 199;
                    break;
                case "EMPTY_TOKEN_TRANSFER_ACCOUNT_AMOUNTS":
                case 200:
                    message.nodeTransactionPrecheckCode = 200;
                    break;
                case "INVALID_SCHEDULE_ID":
                case 201:
                    message.nodeTransactionPrecheckCode = 201;
                    break;
                case "SCHEDULE_IS_IMMUTABLE":
                case 202:
                    message.nodeTransactionPrecheckCode = 202;
                    break;
                case "INVALID_SCHEDULE_PAYER_ID":
                case 203:
                    message.nodeTransactionPrecheckCode = 203;
                    break;
                case "INVALID_SCHEDULE_ACCOUNT_ID":
                case 204:
                    message.nodeTransactionPrecheckCode = 204;
                    break;
                case "NO_NEW_VALID_SIGNATURES":
                case 205:
                    message.nodeTransactionPrecheckCode = 205;
                    break;
                case "UNRESOLVABLE_REQUIRED_SIGNERS":
                case 206:
                    message.nodeTransactionPrecheckCode = 206;
                    break;
                case "SCHEDULED_TRANSACTION_NOT_IN_WHITELIST":
                case 207:
                    message.nodeTransactionPrecheckCode = 207;
                    break;
                case "SOME_SIGNATURES_WERE_INVALID":
                case 208:
                    message.nodeTransactionPrecheckCode = 208;
                    break;
                case "TRANSACTION_ID_FIELD_NOT_ALLOWED":
                case 209:
                    message.nodeTransactionPrecheckCode = 209;
                    break;
                case "IDENTICAL_SCHEDULE_ALREADY_CREATED":
                case 210:
                    message.nodeTransactionPrecheckCode = 210;
                    break;
                case "INVALID_ZERO_BYTE_IN_STRING":
                case 211:
                    message.nodeTransactionPrecheckCode = 211;
                    break;
                case "SCHEDULE_ALREADY_DELETED":
                case 212:
                    message.nodeTransactionPrecheckCode = 212;
                    break;
                case "SCHEDULE_ALREADY_EXECUTED":
                case 213:
                    message.nodeTransactionPrecheckCode = 213;
                    break;
                case "MESSAGE_SIZE_TOO_LARGE":
                case 214:
                    message.nodeTransactionPrecheckCode = 214;
                    break;
                case "OPERATION_REPEATED_IN_BUCKET_GROUPS":
                case 215:
                    message.nodeTransactionPrecheckCode = 215;
                    break;
                case "BUCKET_CAPACITY_OVERFLOW":
                case 216:
                    message.nodeTransactionPrecheckCode = 216;
                    break;
                case "NODE_CAPACITY_NOT_SUFFICIENT_FOR_OPERATION":
                case 217:
                    message.nodeTransactionPrecheckCode = 217;
                    break;
                case "BUCKET_HAS_NO_THROTTLE_GROUPS":
                case 218:
                    message.nodeTransactionPrecheckCode = 218;
                    break;
                case "THROTTLE_GROUP_HAS_ZERO_OPS_PER_SEC":
                case 219:
                    message.nodeTransactionPrecheckCode = 219;
                    break;
                case "SUCCESS_BUT_MISSING_EXPECTED_OPERATION":
                case 220:
                    message.nodeTransactionPrecheckCode = 220;
                    break;
                case "UNPARSEABLE_THROTTLE_DEFINITIONS":
                case 221:
                    message.nodeTransactionPrecheckCode = 221;
                    break;
                case "INVALID_THROTTLE_DEFINITIONS":
                case 222:
                    message.nodeTransactionPrecheckCode = 222;
                    break;
                case "ACCOUNT_EXPIRED_AND_PENDING_REMOVAL":
                case 223:
                    message.nodeTransactionPrecheckCode = 223;
                    break;
                case "INVALID_TOKEN_MAX_SUPPLY":
                case 224:
                    message.nodeTransactionPrecheckCode = 224;
                    break;
                case "INVALID_TOKEN_NFT_SERIAL_NUMBER":
                case 225:
                    message.nodeTransactionPrecheckCode = 225;
                    break;
                case "INVALID_NFT_ID":
                case 226:
                    message.nodeTransactionPrecheckCode = 226;
                    break;
                case "METADATA_TOO_LONG":
                case 227:
                    message.nodeTransactionPrecheckCode = 227;
                    break;
                case "BATCH_SIZE_LIMIT_EXCEEDED":
                case 228:
                    message.nodeTransactionPrecheckCode = 228;
                    break;
                case "INVALID_QUERY_RANGE":
                case 229:
                    message.nodeTransactionPrecheckCode = 229;
                    break;
                case "FRACTION_DIVIDES_BY_ZERO":
                case 230:
                    message.nodeTransactionPrecheckCode = 230;
                    break;
                case "INSUFFICIENT_PAYER_BALANCE_FOR_CUSTOM_FEE":
                case 231:
                    message.nodeTransactionPrecheckCode = 231;
                    break;
                case "CUSTOM_FEES_LIST_TOO_LONG":
                case 232:
                    message.nodeTransactionPrecheckCode = 232;
                    break;
                case "INVALID_CUSTOM_FEE_COLLECTOR":
                case 233:
                    message.nodeTransactionPrecheckCode = 233;
                    break;
                case "INVALID_TOKEN_ID_IN_CUSTOM_FEES":
                case 234:
                    message.nodeTransactionPrecheckCode = 234;
                    break;
                case "TOKEN_NOT_ASSOCIATED_TO_FEE_COLLECTOR":
                case 235:
                    message.nodeTransactionPrecheckCode = 235;
                    break;
                case "TOKEN_MAX_SUPPLY_REACHED":
                case 236:
                    message.nodeTransactionPrecheckCode = 236;
                    break;
                case "SENDER_DOES_NOT_OWN_NFT_SERIAL_NO":
                case 237:
                    message.nodeTransactionPrecheckCode = 237;
                    break;
                case "CUSTOM_FEE_NOT_FULLY_SPECIFIED":
                case 238:
                    message.nodeTransactionPrecheckCode = 238;
                    break;
                case "CUSTOM_FEE_MUST_BE_POSITIVE":
                case 239:
                    message.nodeTransactionPrecheckCode = 239;
                    break;
                case "TOKEN_HAS_NO_FEE_SCHEDULE_KEY":
                case 240:
                    message.nodeTransactionPrecheckCode = 240;
                    break;
                case "CUSTOM_FEE_OUTSIDE_NUMERIC_RANGE":
                case 241:
                    message.nodeTransactionPrecheckCode = 241;
                    break;
                case "ROYALTY_FRACTION_CANNOT_EXCEED_ONE":
                case 242:
                    message.nodeTransactionPrecheckCode = 242;
                    break;
                case "FRACTIONAL_FEE_MAX_AMOUNT_LESS_THAN_MIN_AMOUNT":
                case 243:
                    message.nodeTransactionPrecheckCode = 243;
                    break;
                case "CUSTOM_SCHEDULE_ALREADY_HAS_NO_FEES":
                case 244:
                    message.nodeTransactionPrecheckCode = 244;
                    break;
                case "CUSTOM_FEE_DENOMINATION_MUST_BE_FUNGIBLE_COMMON":
                case 245:
                    message.nodeTransactionPrecheckCode = 245;
                    break;
                case "CUSTOM_FRACTIONAL_FEE_ONLY_ALLOWED_FOR_FUNGIBLE_COMMON":
                case 246:
                    message.nodeTransactionPrecheckCode = 246;
                    break;
                case "INVALID_CUSTOM_FEE_SCHEDULE_KEY":
                case 247:
                    message.nodeTransactionPrecheckCode = 247;
                    break;
                case "INVALID_TOKEN_MINT_METADATA":
                case 248:
                    message.nodeTransactionPrecheckCode = 248;
                    break;
                case "INVALID_TOKEN_BURN_METADATA":
                case 249:
                    message.nodeTransactionPrecheckCode = 249;
                    break;
                case "CURRENT_TREASURY_STILL_OWNS_NFTS":
                case 250:
                    message.nodeTransactionPrecheckCode = 250;
                    break;
                case "ACCOUNT_STILL_OWNS_NFTS":
                case 251:
                    message.nodeTransactionPrecheckCode = 251;
                    break;
                case "TREASURY_MUST_OWN_BURNED_NFT":
                case 252:
                    message.nodeTransactionPrecheckCode = 252;
                    break;
                case "ACCOUNT_DOES_NOT_OWN_WIPED_NFT":
                case 253:
                    message.nodeTransactionPrecheckCode = 253;
                    break;
                case "ACCOUNT_AMOUNT_TRANSFERS_ONLY_ALLOWED_FOR_FUNGIBLE_COMMON":
                case 254:
                    message.nodeTransactionPrecheckCode = 254;
                    break;
                case "MAX_NFTS_IN_PRICE_REGIME_HAVE_BEEN_MINTED":
                case 255:
                    message.nodeTransactionPrecheckCode = 255;
                    break;
                case "PAYER_ACCOUNT_DELETED":
                case 256:
                    message.nodeTransactionPrecheckCode = 256;
                    break;
                case "CUSTOM_FEE_CHARGING_EXCEEDED_MAX_RECURSION_DEPTH":
                case 257:
                    message.nodeTransactionPrecheckCode = 257;
                    break;
                case "CUSTOM_FEE_CHARGING_EXCEEDED_MAX_ACCOUNT_AMOUNTS":
                case 258:
                    message.nodeTransactionPrecheckCode = 258;
                    break;
                case "INSUFFICIENT_SENDER_ACCOUNT_BALANCE_FOR_CUSTOM_FEE":
                case 259:
                    message.nodeTransactionPrecheckCode = 259;
                    break;
                case "SERIAL_NUMBER_LIMIT_REACHED":
                case 260:
                    message.nodeTransactionPrecheckCode = 260;
                    break;
                case "CUSTOM_ROYALTY_FEE_ONLY_ALLOWED_FOR_NON_FUNGIBLE_UNIQUE":
                case 261:
                    message.nodeTransactionPrecheckCode = 261;
                    break;
                case "NO_REMAINING_AUTOMATIC_ASSOCIATIONS":
                case 262:
                    message.nodeTransactionPrecheckCode = 262;
                    break;
                case "EXISTING_AUTOMATIC_ASSOCIATIONS_EXCEED_GIVEN_LIMIT":
                case 263:
                    message.nodeTransactionPrecheckCode = 263;
                    break;
                case "REQUESTED_NUM_AUTOMATIC_ASSOCIATIONS_EXCEEDS_ASSOCIATION_LIMIT":
                case 264:
                    message.nodeTransactionPrecheckCode = 264;
                    break;
                case "TOKEN_IS_PAUSED":
                case 265:
                    message.nodeTransactionPrecheckCode = 265;
                    break;
                case "TOKEN_HAS_NO_PAUSE_KEY":
                case 266:
                    message.nodeTransactionPrecheckCode = 266;
                    break;
                case "INVALID_PAUSE_KEY":
                case 267:
                    message.nodeTransactionPrecheckCode = 267;
                    break;
                case "FREEZE_UPDATE_FILE_DOES_NOT_EXIST":
                case 268:
                    message.nodeTransactionPrecheckCode = 268;
                    break;
                case "FREEZE_UPDATE_FILE_HASH_DOES_NOT_MATCH":
                case 269:
                    message.nodeTransactionPrecheckCode = 269;
                    break;
                case "NO_UPGRADE_HAS_BEEN_PREPARED":
                case 270:
                    message.nodeTransactionPrecheckCode = 270;
                    break;
                case "NO_FREEZE_IS_SCHEDULED":
                case 271:
                    message.nodeTransactionPrecheckCode = 271;
                    break;
                case "UPDATE_FILE_HASH_CHANGED_SINCE_PREPARE_UPGRADE":
                case 272:
                    message.nodeTransactionPrecheckCode = 272;
                    break;
                case "FREEZE_START_TIME_MUST_BE_FUTURE":
                case 273:
                    message.nodeTransactionPrecheckCode = 273;
                    break;
                case "PREPARED_UPDATE_FILE_IS_IMMUTABLE":
                case 274:
                    message.nodeTransactionPrecheckCode = 274;
                    break;
                case "FREEZE_ALREADY_SCHEDULED":
                case 275:
                    message.nodeTransactionPrecheckCode = 275;
                    break;
                case "FREEZE_UPGRADE_IN_PROGRESS":
                case 276:
                    message.nodeTransactionPrecheckCode = 276;
                    break;
                case "UPDATE_FILE_ID_DOES_NOT_MATCH_PREPARED":
                case 277:
                    message.nodeTransactionPrecheckCode = 277;
                    break;
                case "UPDATE_FILE_HASH_DOES_NOT_MATCH_PREPARED":
                case 278:
                    message.nodeTransactionPrecheckCode = 278;
                    break;
                case "CONSENSUS_GAS_EXHAUSTED":
                case 279:
                    message.nodeTransactionPrecheckCode = 279;
                    break;
                case "REVERTED_SUCCESS":
                case 280:
                    message.nodeTransactionPrecheckCode = 280;
                    break;
                case "MAX_STORAGE_IN_PRICE_REGIME_HAS_BEEN_USED":
                case 281:
                    message.nodeTransactionPrecheckCode = 281;
                    break;
                case "INVALID_ALIAS_KEY":
                case 282:
                    message.nodeTransactionPrecheckCode = 282;
                    break;
                case "UNEXPECTED_TOKEN_DECIMALS":
                case 283:
                    message.nodeTransactionPrecheckCode = 283;
                    break;
                case "INVALID_PROXY_ACCOUNT_ID":
                case 284:
                    message.nodeTransactionPrecheckCode = 284;
                    break;
                case "INVALID_TRANSFER_ACCOUNT_ID":
                case 285:
                    message.nodeTransactionPrecheckCode = 285;
                    break;
                case "INVALID_FEE_COLLECTOR_ACCOUNT_ID":
                case 286:
                    message.nodeTransactionPrecheckCode = 286;
                    break;
                case "ALIAS_IS_IMMUTABLE":
                case 287:
                    message.nodeTransactionPrecheckCode = 287;
                    break;
                case "SPENDER_ACCOUNT_SAME_AS_OWNER":
                case 288:
                    message.nodeTransactionPrecheckCode = 288;
                    break;
                case "AMOUNT_EXCEEDS_TOKEN_MAX_SUPPLY":
                case 289:
                    message.nodeTransactionPrecheckCode = 289;
                    break;
                case "NEGATIVE_ALLOWANCE_AMOUNT":
                case 290:
                    message.nodeTransactionPrecheckCode = 290;
                    break;
                case "CANNOT_APPROVE_FOR_ALL_FUNGIBLE_COMMON":
                case 291:
                    message.nodeTransactionPrecheckCode = 291;
                    break;
                case "SPENDER_DOES_NOT_HAVE_ALLOWANCE":
                case 292:
                    message.nodeTransactionPrecheckCode = 292;
                    break;
                case "AMOUNT_EXCEEDS_ALLOWANCE":
                case 293:
                    message.nodeTransactionPrecheckCode = 293;
                    break;
                case "MAX_ALLOWANCES_EXCEEDED":
                case 294:
                    message.nodeTransactionPrecheckCode = 294;
                    break;
                case "EMPTY_ALLOWANCES":
                case 295:
                    message.nodeTransactionPrecheckCode = 295;
                    break;
                case "SPENDER_ACCOUNT_REPEATED_IN_ALLOWANCES":
                case 296:
                    message.nodeTransactionPrecheckCode = 296;
                    break;
                case "REPEATED_SERIAL_NUMS_IN_NFT_ALLOWANCES":
                case 297:
                    message.nodeTransactionPrecheckCode = 297;
                    break;
                case "FUNGIBLE_TOKEN_IN_NFT_ALLOWANCES":
                case 298:
                    message.nodeTransactionPrecheckCode = 298;
                    break;
                case "NFT_IN_FUNGIBLE_TOKEN_ALLOWANCES":
                case 299:
                    message.nodeTransactionPrecheckCode = 299;
                    break;
                case "INVALID_ALLOWANCE_OWNER_ID":
                case 300:
                    message.nodeTransactionPrecheckCode = 300;
                    break;
                case "INVALID_ALLOWANCE_SPENDER_ID":
                case 301:
                    message.nodeTransactionPrecheckCode = 301;
                    break;
                case "REPEATED_ALLOWANCES_TO_DELETE":
                case 302:
                    message.nodeTransactionPrecheckCode = 302;
                    break;
                case "INVALID_DELEGATING_SPENDER":
                case 303:
                    message.nodeTransactionPrecheckCode = 303;
                    break;
                case "DELEGATING_SPENDER_CANNOT_GRANT_APPROVE_FOR_ALL":
                case 304:
                    message.nodeTransactionPrecheckCode = 304;
                    break;
                case "DELEGATING_SPENDER_DOES_NOT_HAVE_APPROVE_FOR_ALL":
                case 305:
                    message.nodeTransactionPrecheckCode = 305;
                    break;
                case "SCHEDULE_EXPIRATION_TIME_TOO_FAR_IN_FUTURE":
                case 306:
                    message.nodeTransactionPrecheckCode = 306;
                    break;
                case "SCHEDULE_EXPIRATION_TIME_MUST_BE_HIGHER_THAN_CONSENSUS_TIME":
                case 307:
                    message.nodeTransactionPrecheckCode = 307;
                    break;
                case "SCHEDULE_FUTURE_THROTTLE_EXCEEDED":
                case 308:
                    message.nodeTransactionPrecheckCode = 308;
                    break;
                case "SCHEDULE_FUTURE_GAS_LIMIT_EXCEEDED":
                case 309:
                    message.nodeTransactionPrecheckCode = 309;
                    break;
                case "INVALID_ETHEREUM_TRANSACTION":
                case 310:
                    message.nodeTransactionPrecheckCode = 310;
                    break;
                case "WRONG_CHAIN_ID":
                case 311:
                    message.nodeTransactionPrecheckCode = 311;
                    break;
                case "WRONG_NONCE":
                case 312:
                    message.nodeTransactionPrecheckCode = 312;
                    break;
                case "ACCESS_LIST_UNSUPPORTED":
                case 313:
                    message.nodeTransactionPrecheckCode = 313;
                    break;
                case "SCHEDULE_PENDING_EXPIRATION":
                case 314:
                    message.nodeTransactionPrecheckCode = 314;
                    break;
                case "CONTRACT_IS_TOKEN_TREASURY":
                case 315:
                    message.nodeTransactionPrecheckCode = 315;
                    break;
                case "CONTRACT_HAS_NON_ZERO_TOKEN_BALANCES":
                case 316:
                    message.nodeTransactionPrecheckCode = 316;
                    break;
                case "CONTRACT_EXPIRED_AND_PENDING_REMOVAL":
                case 317:
                    message.nodeTransactionPrecheckCode = 317;
                    break;
                case "CONTRACT_HAS_NO_AUTO_RENEW_ACCOUNT":
                case 318:
                    message.nodeTransactionPrecheckCode = 318;
                    break;
                case "PERMANENT_REMOVAL_REQUIRES_SYSTEM_INITIATION":
                case 319:
                    message.nodeTransactionPrecheckCode = 319;
                    break;
                case "PROXY_ACCOUNT_ID_FIELD_IS_DEPRECATED":
                case 320:
                    message.nodeTransactionPrecheckCode = 320;
                    break;
                case "SELF_STAKING_IS_NOT_ALLOWED":
                case 321:
                    message.nodeTransactionPrecheckCode = 321;
                    break;
                case "INVALID_STAKING_ID":
                case 322:
                    message.nodeTransactionPrecheckCode = 322;
                    break;
                case "STAKING_NOT_ENABLED":
                case 323:
                    message.nodeTransactionPrecheckCode = 323;
                    break;
                case "INVALID_PRNG_RANGE":
                case 324:
                    message.nodeTransactionPrecheckCode = 324;
                    break;
                case "MAX_ENTITIES_IN_PRICE_REGIME_HAVE_BEEN_CREATED":
                case 325:
                    message.nodeTransactionPrecheckCode = 325;
                    break;
                case "INVALID_FULL_PREFIX_SIGNATURE_FOR_PRECOMPILE":
                case 326:
                    message.nodeTransactionPrecheckCode = 326;
                    break;
                case "INSUFFICIENT_BALANCES_FOR_STORAGE_RENT":
                case 327:
                    message.nodeTransactionPrecheckCode = 327;
                    break;
                case "MAX_CHILD_RECORDS_EXCEEDED":
                case 328:
                    message.nodeTransactionPrecheckCode = 328;
                    break;
                case "INSUFFICIENT_BALANCES_FOR_RENEWAL_FEES":
                case 329:
                    message.nodeTransactionPrecheckCode = 329;
                    break;
                case "TRANSACTION_HAS_UNKNOWN_FIELDS":
                case 330:
                    message.nodeTransactionPrecheckCode = 330;
                    break;
                case "ACCOUNT_IS_IMMUTABLE":
                case 331:
                    message.nodeTransactionPrecheckCode = 331;
                    break;
                case "ALIAS_ALREADY_ASSIGNED":
                case 332:
                    message.nodeTransactionPrecheckCode = 332;
                    break;
                case "INVALID_METADATA_KEY":
                case 333:
                    message.nodeTransactionPrecheckCode = 333;
                    break;
                case "TOKEN_HAS_NO_METADATA_KEY":
                case 334:
                    message.nodeTransactionPrecheckCode = 334;
                    break;
                case "MISSING_TOKEN_METADATA":
                case 335:
                    message.nodeTransactionPrecheckCode = 335;
                    break;
                case "MISSING_SERIAL_NUMBERS":
                case 336:
                    message.nodeTransactionPrecheckCode = 336;
                    break;
                case "TOKEN_HAS_NO_ADMIN_KEY":
                case 337:
                    message.nodeTransactionPrecheckCode = 337;
                    break;
                case "NODE_DELETED":
                case 338:
                    message.nodeTransactionPrecheckCode = 338;
                    break;
                case "INVALID_NODE_ID":
                case 339:
                    message.nodeTransactionPrecheckCode = 339;
                    break;
                case "INVALID_GOSSIP_ENDPOINT":
                case 340:
                    message.nodeTransactionPrecheckCode = 340;
                    break;
                case "INVALID_NODE_ACCOUNT_ID":
                case 341:
                    message.nodeTransactionPrecheckCode = 341;
                    break;
                case "INVALID_NODE_DESCRIPTION":
                case 342:
                    message.nodeTransactionPrecheckCode = 342;
                    break;
                case "INVALID_SERVICE_ENDPOINT":
                case 343:
                    message.nodeTransactionPrecheckCode = 343;
                    break;
                case "INVALID_GOSSIP_CA_CERTIFICATE":
                case 344:
                    message.nodeTransactionPrecheckCode = 344;
                    break;
                case "INVALID_GRPC_CERTIFICATE":
                case 345:
                    message.nodeTransactionPrecheckCode = 345;
                    break;
                case "INVALID_MAX_AUTO_ASSOCIATIONS":
                case 346:
                    message.nodeTransactionPrecheckCode = 346;
                    break;
                case "MAX_NODES_CREATED":
                case 347:
                    message.nodeTransactionPrecheckCode = 347;
                    break;
                case "IP_FQDN_CANNOT_BE_SET_FOR_SAME_ENDPOINT":
                case 348:
                    message.nodeTransactionPrecheckCode = 348;
                    break;
                case "GOSSIP_ENDPOINT_CANNOT_HAVE_FQDN":
                case 349:
                    message.nodeTransactionPrecheckCode = 349;
                    break;
                case "FQDN_SIZE_TOO_LARGE":
                case 350:
                    message.nodeTransactionPrecheckCode = 350;
                    break;
                case "INVALID_ENDPOINT":
                case 351:
                    message.nodeTransactionPrecheckCode = 351;
                    break;
                case "GOSSIP_ENDPOINTS_EXCEEDED_LIMIT":
                case 352:
                    message.nodeTransactionPrecheckCode = 352;
                    break;
                case "TOKEN_REFERENCE_REPEATED":
                case 353:
                    message.nodeTransactionPrecheckCode = 353;
                    break;
                case "INVALID_OWNER_ID":
                case 354:
                    message.nodeTransactionPrecheckCode = 354;
                    break;
                case "TOKEN_REFERENCE_LIST_SIZE_LIMIT_EXCEEDED":
                case 355:
                    message.nodeTransactionPrecheckCode = 355;
                    break;
                case "SERVICE_ENDPOINTS_EXCEEDED_LIMIT":
                case 356:
                    message.nodeTransactionPrecheckCode = 356;
                    break;
                case "INVALID_IPV4_ADDRESS":
                case 357:
                    message.nodeTransactionPrecheckCode = 357;
                    break;
                case "EMPTY_TOKEN_REFERENCE_LIST":
                case 358:
                    message.nodeTransactionPrecheckCode = 358;
                    break;
                case "UPDATE_NODE_ACCOUNT_NOT_ALLOWED":
                case 359:
                    message.nodeTransactionPrecheckCode = 359;
                    break;
                case "TOKEN_HAS_NO_METADATA_OR_SUPPLY_KEY":
                case 360:
                    message.nodeTransactionPrecheckCode = 360;
                    break;
                case "EMPTY_PENDING_AIRDROP_ID_LIST":
                case 361:
                    message.nodeTransactionPrecheckCode = 361;
                    break;
                case "PENDING_AIRDROP_ID_REPEATED":
                case 362:
                    message.nodeTransactionPrecheckCode = 362;
                    break;
                case "PENDING_AIRDROP_ID_LIST_TOO_LONG":
                case 363:
                    message.nodeTransactionPrecheckCode = 363;
                    break;
                case "PENDING_NFT_AIRDROP_ALREADY_EXISTS":
                case 364:
                    message.nodeTransactionPrecheckCode = 364;
                    break;
                case "ACCOUNT_HAS_PENDING_AIRDROPS":
                case 365:
                    message.nodeTransactionPrecheckCode = 365;
                    break;
                case "THROTTLED_AT_CONSENSUS":
                case 366:
                    message.nodeTransactionPrecheckCode = 366;
                    break;
                case "INVALID_PENDING_AIRDROP_ID":
                case 367:
                    message.nodeTransactionPrecheckCode = 367;
                    break;
                case "TOKEN_AIRDROP_WITH_FALLBACK_ROYALTY":
                case 368:
                    message.nodeTransactionPrecheckCode = 368;
                    break;
                case "INVALID_TOKEN_IN_PENDING_AIRDROP":
                case 369:
                    message.nodeTransactionPrecheckCode = 369;
                    break;
                case "SCHEDULE_EXPIRY_IS_BUSY":
                case 370:
                    message.nodeTransactionPrecheckCode = 370;
                    break;
                case "INVALID_GRPC_CERTIFICATE_HASH":
                case 371:
                    message.nodeTransactionPrecheckCode = 371;
                    break;
                case "MISSING_EXPIRY_TIME":
                case 372:
                    message.nodeTransactionPrecheckCode = 372;
                    break;
                case "NO_SCHEDULING_ALLOWED_AFTER_SCHEDULED_RECURSION":
                case 373:
                    message.nodeTransactionPrecheckCode = 373;
                    break;
                case "RECURSIVE_SCHEDULING_LIMIT_REACHED":
                case 374:
                    message.nodeTransactionPrecheckCode = 374;
                    break;
                case "WAITING_FOR_LEDGER_ID":
                case 375:
                    message.nodeTransactionPrecheckCode = 375;
                    break;
                case "MAX_ENTRIES_FOR_FEE_EXEMPT_KEY_LIST_EXCEEDED":
                case 376:
                    message.nodeTransactionPrecheckCode = 376;
                    break;
                case "FEE_EXEMPT_KEY_LIST_CONTAINS_DUPLICATED_KEYS":
                case 377:
                    message.nodeTransactionPrecheckCode = 377;
                    break;
                case "INVALID_KEY_IN_FEE_EXEMPT_KEY_LIST":
                case 378:
                    message.nodeTransactionPrecheckCode = 378;
                    break;
                case "INVALID_FEE_SCHEDULE_KEY":
                case 379:
                    message.nodeTransactionPrecheckCode = 379;
                    break;
                case "FEE_SCHEDULE_KEY_CANNOT_BE_UPDATED":
                case 380:
                    message.nodeTransactionPrecheckCode = 380;
                    break;
                case "FEE_SCHEDULE_KEY_NOT_SET":
                case 381:
                    message.nodeTransactionPrecheckCode = 381;
                    break;
                case "MAX_CUSTOM_FEE_LIMIT_EXCEEDED":
                case 382:
                    message.nodeTransactionPrecheckCode = 382;
                    break;
                case "NO_VALID_MAX_CUSTOM_FEE":
                case 383:
                    message.nodeTransactionPrecheckCode = 383;
                    break;
                case "INVALID_MAX_CUSTOM_FEES":
                case 384:
                    message.nodeTransactionPrecheckCode = 384;
                    break;
                case "DUPLICATE_DENOMINATION_IN_MAX_CUSTOM_FEE_LIST":
                case 385:
                    message.nodeTransactionPrecheckCode = 385;
                    break;
                case "DUPLICATE_ACCOUNT_ID_IN_MAX_CUSTOM_FEE_LIST":
                case 386:
                    message.nodeTransactionPrecheckCode = 386;
                    break;
                case "MAX_CUSTOM_FEES_IS_NOT_SUPPORTED":
                case 387:
                    message.nodeTransactionPrecheckCode = 387;
                    break;
                case "BATCH_LIST_EMPTY":
                case 388:
                    message.nodeTransactionPrecheckCode = 388;
                    break;
                case "BATCH_LIST_CONTAINS_DUPLICATES":
                case 389:
                    message.nodeTransactionPrecheckCode = 389;
                    break;
                case "BATCH_TRANSACTION_IN_BLACKLIST":
                case 390:
                    message.nodeTransactionPrecheckCode = 390;
                    break;
                case "INNER_TRANSACTION_FAILED":
                case 391:
                    message.nodeTransactionPrecheckCode = 391;
                    break;
                case "MISSING_BATCH_KEY":
                case 392:
                    message.nodeTransactionPrecheckCode = 392;
                    break;
                case "BATCH_KEY_SET_ON_NON_INNER_TRANSACTION":
                case 393:
                    message.nodeTransactionPrecheckCode = 393;
                    break;
                case "INVALID_BATCH_KEY":
                case 394:
                    message.nodeTransactionPrecheckCode = 394;
                    break;
                case "SCHEDULE_EXPIRY_NOT_CONFIGURABLE":
                case 395:
                    message.nodeTransactionPrecheckCode = 395;
                    break;
                case "CREATING_SYSTEM_ENTITIES":
                case 396:
                    message.nodeTransactionPrecheckCode = 396;
                    break;
                case "THROTTLE_GROUP_LCM_OVERFLOW":
                case 397:
                    message.nodeTransactionPrecheckCode = 397;
                    break;
                case "AIRDROP_CONTAINS_MULTIPLE_SENDERS_FOR_A_TOKEN":
                case 398:
                    message.nodeTransactionPrecheckCode = 398;
                    break;
                case "GRPC_WEB_PROXY_NOT_SUPPORTED":
                case 399:
                    message.nodeTransactionPrecheckCode = 399;
                    break;
                case "NFT_TRANSFERS_ONLY_ALLOWED_FOR_NON_FUNGIBLE_UNIQUE":
                case 400:
                    message.nodeTransactionPrecheckCode = 400;
                    break;
                case "INVALID_SERIALIZED_TX_MESSAGE_HASH_ALGORITHM":
                case 401:
                    message.nodeTransactionPrecheckCode = 401;
                    break;
                case "WRONG_HOOK_ENTITY_TYPE":
                case 499:
                    message.nodeTransactionPrecheckCode = 499;
                    break;
                case "EVM_HOOK_GAS_THROTTLED":
                case 500:
                    message.nodeTransactionPrecheckCode = 500;
                    break;
                case "HOOK_ID_IN_USE":
                case 501:
                    message.nodeTransactionPrecheckCode = 501;
                    break;
                case "BAD_HOOK_REQUEST":
                case 502:
                    message.nodeTransactionPrecheckCode = 502;
                    break;
                case "REJECTED_BY_ACCOUNT_ALLOWANCE_HOOK":
                case 503:
                    message.nodeTransactionPrecheckCode = 503;
                    break;
                case "HOOK_NOT_FOUND":
                case 504:
                    message.nodeTransactionPrecheckCode = 504;
                    break;
                case "EVM_HOOK_STORAGE_UPDATE_BYTES_TOO_LONG":
                case 505:
                    message.nodeTransactionPrecheckCode = 505;
                    break;
                case "EVM_HOOK_STORAGE_UPDATE_BYTES_MUST_USE_MINIMAL_REPRESENTATION":
                case 506:
                    message.nodeTransactionPrecheckCode = 506;
                    break;
                case "INVALID_HOOK_ID":
                case 507:
                    message.nodeTransactionPrecheckCode = 507;
                    break;
                case "EMPTY_EVM_HOOK_STORAGE_UPDATE":
                case 508:
                    message.nodeTransactionPrecheckCode = 508;
                    break;
                case "HOOK_ID_REPEATED_IN_CREATION_DETAILS":
                case 509:
                    message.nodeTransactionPrecheckCode = 509;
                    break;
                case "HOOKS_NOT_ENABLED":
                case 510:
                    message.nodeTransactionPrecheckCode = 510;
                    break;
                case "HOOK_IS_NOT_AN_EVM_HOOK":
                case 511:
                    message.nodeTransactionPrecheckCode = 511;
                    break;
                case "HOOK_DELETED":
                case 512:
                    message.nodeTransactionPrecheckCode = 512;
                    break;
                case "TOO_MANY_EVM_HOOK_STORAGE_UPDATES":
                case 513:
                    message.nodeTransactionPrecheckCode = 513;
                    break;
                case "HOOK_CREATION_BYTES_MUST_USE_MINIMAL_REPRESENTATION":
                case 514:
                    message.nodeTransactionPrecheckCode = 514;
                    break;
                case "HOOK_CREATION_BYTES_TOO_LONG":
                case 515:
                    message.nodeTransactionPrecheckCode = 515;
                    break;
                case "INVALID_HOOK_CREATION_SPEC":
                case 516:
                    message.nodeTransactionPrecheckCode = 516;
                    break;
                case "HOOK_EXTENSION_EMPTY":
                case 517:
                    message.nodeTransactionPrecheckCode = 517;
                    break;
                case "INVALID_HOOK_ADMIN_KEY":
                case 518:
                    message.nodeTransactionPrecheckCode = 518;
                    break;
                case "HOOK_DELETION_REQUIRES_ZERO_STORAGE_SLOTS":
                case 519:
                    message.nodeTransactionPrecheckCode = 519;
                    break;
                case "CANNOT_SET_HOOKS_AND_APPROVAL":
                case 520:
                    message.nodeTransactionPrecheckCode = 520;
                    break;
                case "TRANSACTION_REQUIRES_ZERO_HOOKS":
                case 521:
                    message.nodeTransactionPrecheckCode = 521;
                    break;
                case "INVALID_HOOK_CALL":
                case 522:
                    message.nodeTransactionPrecheckCode = 522;
                    break;
                case "HOOKS_ARE_NOT_SUPPORTED_IN_AIRDROPS":
                case 523:
                    message.nodeTransactionPrecheckCode = 523;
                    break;
                case "ACCOUNT_IS_LINKED_TO_A_NODE":
                case 524:
                    message.nodeTransactionPrecheckCode = 524;
                    break;
                case "HOOKS_EXECUTIONS_REQUIRE_TOP_LEVEL_CRYPTO_TRANSFER":
                case 525:
                    message.nodeTransactionPrecheckCode = 525;
                    break;
                case "NODE_ACCOUNT_HAS_ZERO_BALANCE":
                case 526:
                    message.nodeTransactionPrecheckCode = 526;
                    break;
                case "TRANSFER_TO_FEE_COLLECTION_ACCOUNT_NOT_ALLOWED":
                case 527:
                    message.nodeTransactionPrecheckCode = 527;
                    break;
                case "TOO_MANY_HOOK_INVOCATIONS":
                case 528:
                    message.nodeTransactionPrecheckCode = 528;
                    break;
                case "INVALID_REGISTERED_NODE_ID":
                case 529:
                    message.nodeTransactionPrecheckCode = 529;
                    break;
                case "INVALID_REGISTERED_ENDPOINT":
                case 530:
                    message.nodeTransactionPrecheckCode = 530;
                    break;
                case "REGISTERED_ENDPOINTS_EXCEEDED_LIMIT":
                case 531:
                    message.nodeTransactionPrecheckCode = 531;
                    break;
                case "INVALID_REGISTERED_ENDPOINT_ADDRESS":
                case 532:
                    message.nodeTransactionPrecheckCode = 532;
                    break;
                case "INVALID_REGISTERED_ENDPOINT_TYPE":
                case 533:
                    message.nodeTransactionPrecheckCode = 533;
                    break;
                case "REGISTERED_NODE_STILL_ASSOCIATED":
                case 534:
                    message.nodeTransactionPrecheckCode = 534;
                    break;
                case "MAX_REGISTERED_NODES_EXCEEDED":
                case 535:
                    message.nodeTransactionPrecheckCode = 535;
                    break;
                default:
                    if (typeof object.nodeTransactionPrecheckCode === "number" && (object.nodeTransactionPrecheckCode | 0) === object.nodeTransactionPrecheckCode)
                        message.nodeTransactionPrecheckCode = object.nodeTransactionPrecheckCode;
                }
            if (object.cost != null)
                if (typeof object.cost === "object" ? object.cost.low || object.cost.high : $Number(object.cost) !== 0)
                    if ($util.Long)
                        message.cost = $util.Long.fromValue(object.cost, true);
                    else if (typeof object.cost === "string")
                        message.cost = $parseInt(object.cost, 10);
                    else if (typeof object.cost === "number")
                        message.cost = object.cost;
                    else if (typeof object.cost === "object")
                        message.cost = new $util.LongBits(object.cost.low >>> 0, object.cost.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a TransactionResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionResponse
         * @static
         * @param {proto.TransactionResponse} message TransactionResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.nodeTransactionPrecheckCode = options.enums === $String ? "OK" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.cost = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.cost = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.nodeTransactionPrecheckCode != null && $Object.hasOwnProperty.call(message, "nodeTransactionPrecheckCode"))
                object.nodeTransactionPrecheckCode = options.enums === $String ? $root.proto.ResponseCodeEnum[message.nodeTransactionPrecheckCode] === $undefined ? message.nodeTransactionPrecheckCode : $root.proto.ResponseCodeEnum[message.nodeTransactionPrecheckCode] : message.nodeTransactionPrecheckCode;
            if (message.cost != null && $Object.hasOwnProperty.call(message, "cost"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.cost = typeof message.cost === "number" ? $BigInt(message.cost) : $util.Long.fromBits(message.cost.low >>> 0, message.cost.high >>> 0, true).toBigInt();
                else if (typeof message.cost === "number")
                    object.cost = options.longs === $String ? $String(message.cost) : message.cost;
                else
                    object.cost = options.longs === $String ? $util.Long.prototype.toString.call(message.cost) : options.longs === $Number ? new $util.LongBits(message.cost.low >>> 0, message.cost.high >>> 0).toNumber(true) : message.cost;
            return object;
        };

        /**
         * Converts this TransactionResponse to JSON.
         * @function toJSON
         * @memberof proto.TransactionResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionResponse.prototype.toJSON = function() {
            return TransactionResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionResponse
         * @function getTypeUrl
         * @memberof proto.TransactionResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionResponse";
        };

        return TransactionResponse;
    })();

    /**
     * An enumeration of possible response codes.
     * @name proto.ResponseCodeEnum
     * @enum {number}
     * @property {number} OK=0 The transaction passed the precheck validations.
     * @property {number} INVALID_TRANSACTION=1 For any error not handled by specific error codes listed below.
     * @property {number} PAYER_ACCOUNT_NOT_FOUND=2 Payer account does not exist.
     * @property {number} INVALID_NODE_ACCOUNT=3 Node Account provided does not match the node account of the node the transaction was submitted
     * to.
     * @property {number} TRANSACTION_EXPIRED=4 Pre-Check error when TransactionValidStart + transactionValidDuration is less than current
     * consensus time.
     * @property {number} INVALID_TRANSACTION_START=5 Transaction start time is greater than current consensus time
     * @property {number} INVALID_TRANSACTION_DURATION=6 The given transactionValidDuration was either non-positive, or greater than the maximum
     * valid duration of 180 secs.
     * @property {number} INVALID_SIGNATURE=7 The transaction signature is not valid
     * @property {number} MEMO_TOO_LONG=8 Transaction memo size exceeded 100 bytes
     * @property {number} INSUFFICIENT_TX_FEE=9 The fee provided in the transaction is insufficient for this type of transaction
     * @property {number} INSUFFICIENT_PAYER_BALANCE=10 The payer account has insufficient cryptocurrency to pay the transaction fee
     * @property {number} DUPLICATE_TRANSACTION=11 This transaction ID is a duplicate of one that was submitted to this node or reached consensus
     * in the last 180 seconds (receipt period)
     * @property {number} BUSY=12 If API is throttled out
     * @property {number} NOT_SUPPORTED=13 The API is not currently supported
     * @property {number} INVALID_FILE_ID=14 The file id is invalid or does not exist
     * @property {number} INVALID_ACCOUNT_ID=15 The account id is invalid or does not exist
     * @property {number} INVALID_CONTRACT_ID=16 The contract id is invalid or does not exist
     * @property {number} INVALID_TRANSACTION_ID=17 Transaction id is not valid
     * @property {number} RECEIPT_NOT_FOUND=18 Receipt for given transaction id does not exist
     * @property {number} RECORD_NOT_FOUND=19 Record for given transaction id does not exist
     * @property {number} INVALID_SOLIDITY_ID=20 The solidity id is invalid or entity with this solidity id does not exist
     * @property {number} UNKNOWN=21 The responding node has submitted the transaction to the network. Its final status is still
     * unknown.
     * @property {number} SUCCESS=22 The transaction succeeded
     * @property {number} FAIL_INVALID=23 There was a system error and the transaction failed because of invalid request parameters.
     * @property {number} FAIL_FEE=24 There was a system error while performing fee calculation, reserved for future.
     * @property {number} FAIL_BALANCE=25 There was a system error while performing balance checks, reserved for future.
     * @property {number} KEY_REQUIRED=26 Key not provided in the transaction body
     * @property {number} BAD_ENCODING=27 Unsupported algorithm/encoding used for keys in the transaction
     * @property {number} INSUFFICIENT_ACCOUNT_BALANCE=28 When the account balance is not sufficient for the transfer
     * @property {number} INVALID_SOLIDITY_ADDRESS=29 During an update transaction when the system is not able to find the Users Solidity address
     * @property {number} INSUFFICIENT_GAS=30 Not enough gas was supplied to execute transaction
     * @property {number} CONTRACT_SIZE_LIMIT_EXCEEDED=31 contract byte code size is over the limit
     * @property {number} LOCAL_CALL_MODIFICATION_EXCEPTION=32 local execution (query) is requested for a function which changes state
     * @property {number} CONTRACT_REVERT_EXECUTED=33 Contract REVERT OPCODE executed
     * @property {number} CONTRACT_EXECUTION_EXCEPTION=34 For any contract execution related error not handled by specific error codes listed above.
     * @property {number} INVALID_RECEIVING_NODE_ACCOUNT=35 In Query validation, account with +ve(amount) value should be Receiving node account, the
     * receiver account should be only one account in the list
     * @property {number} MISSING_QUERY_HEADER=36 Header is missing in Query request
     * @property {number} ACCOUNT_UPDATE_FAILED=37 The update of the account failed
     * @property {number} INVALID_KEY_ENCODING=38 Provided key encoding was not supported by the system
     * @property {number} NULL_SOLIDITY_ADDRESS=39 null solidity address
     * @property {number} CONTRACT_UPDATE_FAILED=40 update of the contract failed
     * @property {number} INVALID_QUERY_HEADER=41 the query header is invalid
     * @property {number} INVALID_FEE_SUBMITTED=42 Invalid fee submitted
     * @property {number} INVALID_PAYER_SIGNATURE=43 Payer signature is invalid
     * @property {number} KEY_NOT_PROVIDED=44 The keys were not provided in the request.
     * @property {number} INVALID_EXPIRATION_TIME=45 Expiration time provided in the transaction was invalid.
     * @property {number} NO_WACL_KEY=46 WriteAccess Control Keys are not provided for the file
     * @property {number} FILE_CONTENT_EMPTY=47 The contents of file are provided as empty.
     * @property {number} INVALID_ACCOUNT_AMOUNTS=48 The crypto transfer credit and debit do not sum equal to 0
     * @property {number} EMPTY_TRANSACTION_BODY=49 Transaction body provided is empty
     * @property {number} INVALID_TRANSACTION_BODY=50 Invalid transaction body provided
     * @property {number} INVALID_SIGNATURE_TYPE_MISMATCHING_KEY=51 the type of key (base ed25519 key, KeyList, or ThresholdKey) does not match the type of
     * signature (base ed25519 signature, SignatureList, or ThresholdKeySignature)
     * @property {number} INVALID_SIGNATURE_COUNT_MISMATCHING_KEY=52 the number of key (KeyList, or ThresholdKey) does not match that of signature (SignatureList,
     * or ThresholdKeySignature). e.g. if a keyList has 3 base keys, then the corresponding
     * signatureList should also have 3 base signatures.
     * @property {number} EMPTY_LIVE_HASH_BODY=53 the livehash body is empty
     * @property {number} EMPTY_LIVE_HASH=54 the livehash data is missing
     * @property {number} EMPTY_LIVE_HASH_KEYS=55 the keys for a livehash are missing
     * @property {number} INVALID_LIVE_HASH_SIZE=56 the livehash data is not the output of a SHA-384 digest
     * @property {number} EMPTY_QUERY_BODY=57 the query body is empty
     * @property {number} EMPTY_LIVE_HASH_QUERY=58 the crypto livehash query is empty
     * @property {number} LIVE_HASH_NOT_FOUND=59 the livehash is not present
     * @property {number} ACCOUNT_ID_DOES_NOT_EXIST=60 the account id passed has not yet been created.
     * @property {number} LIVE_HASH_ALREADY_EXISTS=61 the livehash already exists for a given account
     * @property {number} INVALID_FILE_WACL=62 File WACL keys are invalid
     * @property {number} SERIALIZATION_FAILED=63 Serialization failure
     * @property {number} TRANSACTION_OVERSIZE=64 The size of the Transaction is greater than transactionMaxBytes
     * @property {number} TRANSACTION_TOO_MANY_LAYERS=65 The Transaction has more than 50 levels
     * @property {number} CONTRACT_DELETED=66 Contract is marked as deleted
     * @property {number} PLATFORM_NOT_ACTIVE=67 the platform node is either disconnected or lagging behind.
     * @property {number} KEY_PREFIX_MISMATCH=68 one public key matches more than one prefixes on the signature map
     * @property {number} PLATFORM_TRANSACTION_NOT_CREATED=69 transaction not created by platform due to large backlog
     * @property {number} INVALID_RENEWAL_PERIOD=70 auto renewal period is not a positive number of seconds
     * @property {number} INVALID_PAYER_ACCOUNT_ID=71 the response code when a smart contract id is passed for a crypto API request
     * @property {number} ACCOUNT_DELETED=72 the account has been marked as deleted
     * @property {number} FILE_DELETED=73 the file has been marked as deleted
     * @property {number} ACCOUNT_REPEATED_IN_ACCOUNT_AMOUNTS=74 same accounts repeated in the transfer account list
     * @property {number} SETTING_NEGATIVE_ACCOUNT_BALANCE=75 attempting to set negative balance value for crypto account
     * @property {number} OBTAINER_REQUIRED=76 when deleting smart contract that has crypto balance either transfer account or transfer smart
     * contract is required
     * @property {number} OBTAINER_SAME_CONTRACT_ID=77 when deleting smart contract that has crypto balance you can not use the same contract id as
     * transferContractId as the one being deleted
     * @property {number} OBTAINER_DOES_NOT_EXIST=78 transferAccountId or transferContractId specified for contract delete does not exist
     * @property {number} MODIFYING_IMMUTABLE_CONTRACT=79 attempting to modify (update or delete a immutable smart contract, i.e. one created without a
     * admin key)
     * @property {number} FILE_SYSTEM_EXCEPTION=80 Unexpected exception thrown by file system functions
     * @property {number} AUTORENEW_DURATION_NOT_IN_RANGE=81 the duration is not a subset of [MINIMUM_AUTORENEW_DURATION,MAXIMUM_AUTORENEW_DURATION]
     * @property {number} ERROR_DECODING_BYTESTRING=82 Decoding the smart contract binary to a byte array failed. Check that the input is a valid hex
     * string.
     * @property {number} CONTRACT_FILE_EMPTY=83 File to create a smart contract was of length zero
     * @property {number} CONTRACT_BYTECODE_EMPTY=84 Bytecode for smart contract is of length zero
     * @property {number} INVALID_INITIAL_BALANCE=85 Attempt to set negative initial balance
     * @property {number} INVALID_RECEIVE_RECORD_THRESHOLD=86 Attempt to set negative receive record threshold
     * @property {number} INVALID_SEND_RECORD_THRESHOLD=87 Attempt to set negative send record threshold
     * @property {number} ACCOUNT_IS_NOT_GENESIS_ACCOUNT=88 Special Account Operations should be performed by only Genesis account, return this code if it
     * is not Genesis Account
     * @property {number} PAYER_ACCOUNT_UNAUTHORIZED=89 The fee payer account doesn't have permission to submit such Transaction
     * @property {number} INVALID_FREEZE_TRANSACTION_BODY=90 FreezeTransactionBody is invalid
     * @property {number} FREEZE_TRANSACTION_BODY_NOT_FOUND=91 FreezeTransactionBody does not exist
     * @property {number} TRANSFER_LIST_SIZE_LIMIT_EXCEEDED=92 Exceeded the number of accounts (both from and to) allowed for crypto transfer list
     * @property {number} RESULT_SIZE_LIMIT_EXCEEDED=93 Smart contract result size greater than specified maxResultSize
     * @property {number} NOT_SPECIAL_ACCOUNT=94 The payer account is not a special account(account 0.0.55)
     * @property {number} CONTRACT_NEGATIVE_GAS=95 Negative gas was offered in smart contract call
     * @property {number} CONTRACT_NEGATIVE_VALUE=96 Negative value / initial balance was specified in a smart contract call / create
     * @property {number} INVALID_FEE_FILE=97 Failed to update fee file
     * @property {number} INVALID_EXCHANGE_RATE_FILE=98 Failed to update exchange rate file
     * @property {number} INSUFFICIENT_LOCAL_CALL_GAS=99 Payment tendered for contract local call cannot cover both the fee and the gas
     * @property {number} ENTITY_NOT_ALLOWED_TO_DELETE=100 Entities with Entity ID below 1000 are not allowed to be deleted
     * @property {number} AUTHORIZATION_FAILED=101 Violating one of these rules: 1) treasury account can update all entities below 0.0.1000, 2)
     * account 0.0.50 can update all entities from 0.0.51 - 0.0.80, 3) Network Function Master Account
     * A/c 0.0.50 - Update all Network Function accounts & perform all the Network Functions listed
     * below, 4) Network Function Accounts: i) A/c 0.0.55 - Update Address Book files (0.0.101/102),
     * ii) A/c 0.0.56 - Update Fee schedule (0.0.111), iii) A/c 0.0.57 - Update Exchange Rate
     * (0.0.112).
     * @property {number} FILE_UPLOADED_PROTO_INVALID=102 Fee Schedule Proto uploaded but not valid (append or update is required)
     * @property {number} FILE_UPLOADED_PROTO_NOT_SAVED_TO_DISK=103 Fee Schedule Proto uploaded but not valid (append or update is required)
     * @property {number} FEE_SCHEDULE_FILE_PART_UPLOADED=104 Fee Schedule Proto File Part uploaded
     * @property {number} EXCHANGE_RATE_CHANGE_LIMIT_EXCEEDED=105 The change on Exchange Rate exceeds Exchange_Rate_Allowed_Percentage
     * @property {number} MAX_CONTRACT_STORAGE_EXCEEDED=106 Contract permanent storage exceeded the currently allowable limit
     * @property {number} TRANSFER_ACCOUNT_SAME_AS_DELETE_ACCOUNT=107 Transfer Account should not be same as Account to be deleted
     * @property {number} TOTAL_LEDGER_BALANCE_INVALID=108 TOTAL_LEDGER_BALANCE_INVALID value
     * @property {number} EXPIRATION_REDUCTION_NOT_ALLOWED=110 The expiration date/time on a smart contract may not be reduced
     * @property {number} MAX_GAS_LIMIT_EXCEEDED=111 Gas exceeded currently allowable gas limit per transaction
     * @property {number} MAX_FILE_SIZE_EXCEEDED=112 File size exceeded the currently allowable limit
     * @property {number} RECEIVER_SIG_REQUIRED=113 When a valid signature is not provided for operations on account with receiverSigRequired=true
     * @property {number} INVALID_TOPIC_ID=150 The Topic ID specified is not in the system.
     * @property {number} INVALID_ADMIN_KEY=155 A provided admin key was invalid. Verify the bytes for an Ed25519 public key are exactly 32 bytes; and the bytes for a compressed ECDSA(secp256k1) key are exactly 33 bytes, with the first byte either 0x02 or 0x03..
     * @property {number} INVALID_SUBMIT_KEY=156 A provided submit key was invalid.
     * @property {number} UNAUTHORIZED=157 An attempted operation was not authorized (ie - a deleteTopic for a topic with no adminKey).
     * @property {number} INVALID_TOPIC_MESSAGE=158 A ConsensusService message is empty.
     * @property {number} INVALID_AUTORENEW_ACCOUNT=159 The autoRenewAccount specified is not a valid, active account.
     * @property {number} AUTORENEW_ACCOUNT_NOT_ALLOWED=160 An adminKey was not specified on the topic, so there must not be an autoRenewAccount.
     * @property {number} TOPIC_EXPIRED=162 The topic has expired, was not automatically renewed, and is in a 7 day grace period before the
     * topic will be deleted unrecoverably. This error response code will not be returned until
     * autoRenew functionality is supported by HAPI.
     * @property {number} INVALID_CHUNK_NUMBER=163 INVALID_CHUNK_NUMBER value
     * @property {number} INVALID_CHUNK_TRANSACTION_ID=164 INVALID_CHUNK_TRANSACTION_ID value
     * @property {number} ACCOUNT_FROZEN_FOR_TOKEN=165 ACCOUNT_FROZEN_FOR_TOKEN value
     * @property {number} TOKENS_PER_ACCOUNT_LIMIT_EXCEEDED=166 TOKENS_PER_ACCOUNT_LIMIT_EXCEEDED value
     * @property {number} INVALID_TOKEN_ID=167 INVALID_TOKEN_ID value
     * @property {number} INVALID_TOKEN_DECIMALS=168 INVALID_TOKEN_DECIMALS value
     * @property {number} INVALID_TOKEN_INITIAL_SUPPLY=169 INVALID_TOKEN_INITIAL_SUPPLY value
     * @property {number} INVALID_TREASURY_ACCOUNT_FOR_TOKEN=170 INVALID_TREASURY_ACCOUNT_FOR_TOKEN value
     * @property {number} INVALID_TOKEN_SYMBOL=171 INVALID_TOKEN_SYMBOL value
     * @property {number} TOKEN_HAS_NO_FREEZE_KEY=172 TOKEN_HAS_NO_FREEZE_KEY value
     * @property {number} TRANSFERS_NOT_ZERO_SUM_FOR_TOKEN=173 TRANSFERS_NOT_ZERO_SUM_FOR_TOKEN value
     * @property {number} MISSING_TOKEN_SYMBOL=174 MISSING_TOKEN_SYMBOL value
     * @property {number} TOKEN_SYMBOL_TOO_LONG=175 TOKEN_SYMBOL_TOO_LONG value
     * @property {number} ACCOUNT_KYC_NOT_GRANTED_FOR_TOKEN=176 ACCOUNT_KYC_NOT_GRANTED_FOR_TOKEN value
     * @property {number} TOKEN_HAS_NO_KYC_KEY=177 TOKEN_HAS_NO_KYC_KEY value
     * @property {number} INSUFFICIENT_TOKEN_BALANCE=178 INSUFFICIENT_TOKEN_BALANCE value
     * @property {number} TOKEN_WAS_DELETED=179 TOKEN_WAS_DELETED value
     * @property {number} TOKEN_HAS_NO_SUPPLY_KEY=180 TOKEN_HAS_NO_SUPPLY_KEY value
     * @property {number} TOKEN_HAS_NO_WIPE_KEY=181 TOKEN_HAS_NO_WIPE_KEY value
     * @property {number} INVALID_TOKEN_MINT_AMOUNT=182 INVALID_TOKEN_MINT_AMOUNT value
     * @property {number} INVALID_TOKEN_BURN_AMOUNT=183 INVALID_TOKEN_BURN_AMOUNT value
     * @property {number} TOKEN_NOT_ASSOCIATED_TO_ACCOUNT=184 TOKEN_NOT_ASSOCIATED_TO_ACCOUNT value
     * @property {number} CANNOT_WIPE_TOKEN_TREASURY_ACCOUNT=185 CANNOT_WIPE_TOKEN_TREASURY_ACCOUNT value
     * @property {number} INVALID_KYC_KEY=186 INVALID_KYC_KEY value
     * @property {number} INVALID_WIPE_KEY=187 INVALID_WIPE_KEY value
     * @property {number} INVALID_FREEZE_KEY=188 INVALID_FREEZE_KEY value
     * @property {number} INVALID_SUPPLY_KEY=189 INVALID_SUPPLY_KEY value
     * @property {number} MISSING_TOKEN_NAME=190 MISSING_TOKEN_NAME value
     * @property {number} TOKEN_NAME_TOO_LONG=191 TOKEN_NAME_TOO_LONG value
     * @property {number} INVALID_WIPING_AMOUNT=192 INVALID_WIPING_AMOUNT value
     * @property {number} TOKEN_IS_IMMUTABLE=193 TOKEN_IS_IMMUTABLE value
     * @property {number} TOKEN_ALREADY_ASSOCIATED_TO_ACCOUNT=194 TOKEN_ALREADY_ASSOCIATED_TO_ACCOUNT value
     * @property {number} TRANSACTION_REQUIRES_ZERO_TOKEN_BALANCES=195 TRANSACTION_REQUIRES_ZERO_TOKEN_BALANCES value
     * @property {number} ACCOUNT_IS_TREASURY=196 ACCOUNT_IS_TREASURY value
     * @property {number} TOKEN_ID_REPEATED_IN_TOKEN_LIST=197 TOKEN_ID_REPEATED_IN_TOKEN_LIST value
     * @property {number} TOKEN_TRANSFER_LIST_SIZE_LIMIT_EXCEEDED=198 TOKEN_TRANSFER_LIST_SIZE_LIMIT_EXCEEDED value
     * @property {number} EMPTY_TOKEN_TRANSFER_BODY=199 EMPTY_TOKEN_TRANSFER_BODY value
     * @property {number} EMPTY_TOKEN_TRANSFER_ACCOUNT_AMOUNTS=200 EMPTY_TOKEN_TRANSFER_ACCOUNT_AMOUNTS value
     * @property {number} INVALID_SCHEDULE_ID=201 The Scheduled entity does not exist; or has now expired, been deleted, or been executed
     * @property {number} SCHEDULE_IS_IMMUTABLE=202 The Scheduled entity cannot be modified. Admin key not set
     * @property {number} INVALID_SCHEDULE_PAYER_ID=203 The provided Scheduled Payer does not exist
     * @property {number} INVALID_SCHEDULE_ACCOUNT_ID=204 The Schedule Create Transaction TransactionID account does not exist
     * @property {number} NO_NEW_VALID_SIGNATURES=205 The provided sig map did not contain any new valid signatures from required signers of the scheduled transaction
     * @property {number} UNRESOLVABLE_REQUIRED_SIGNERS=206 The required signers for a scheduled transaction cannot be resolved, for example because they do not exist or have been deleted
     * @property {number} SCHEDULED_TRANSACTION_NOT_IN_WHITELIST=207 Only whitelisted transaction types may be scheduled
     * @property {number} SOME_SIGNATURES_WERE_INVALID=208 At least one of the signatures in the provided sig map did not represent a valid signature for any required signer
     * @property {number} TRANSACTION_ID_FIELD_NOT_ALLOWED=209 The scheduled field in the TransactionID may not be set to true
     * @property {number} IDENTICAL_SCHEDULE_ALREADY_CREATED=210 A schedule already exists with the same identifying fields of an attempted ScheduleCreate (that is, all fields other than scheduledPayerAccountID)
     * @property {number} INVALID_ZERO_BYTE_IN_STRING=211 A string field in the transaction has a UTF-8 encoding with the prohibited zero byte
     * @property {number} SCHEDULE_ALREADY_DELETED=212 A schedule being signed or deleted has already been deleted
     * @property {number} SCHEDULE_ALREADY_EXECUTED=213 A schedule being signed or deleted has already been executed
     * @property {number} MESSAGE_SIZE_TOO_LARGE=214 ConsensusSubmitMessage request's message size is larger than allowed.
     * @property {number} OPERATION_REPEATED_IN_BUCKET_GROUPS=215 An operation was assigned to more than one throttle group in a given bucket
     * @property {number} BUCKET_CAPACITY_OVERFLOW=216 The capacity needed to satisfy all opsPerSec groups in a bucket overflowed a signed 8-byte integral type
     * @property {number} NODE_CAPACITY_NOT_SUFFICIENT_FOR_OPERATION=217 Given the network size in the address book, the node-level capacity for an operation would never be enough to accept a single request; usually means a bucket burstPeriod should be increased
     * @property {number} BUCKET_HAS_NO_THROTTLE_GROUPS=218 A bucket was defined without any throttle groups
     * @property {number} THROTTLE_GROUP_HAS_ZERO_OPS_PER_SEC=219 A throttle group was granted zero opsPerSec
     * @property {number} SUCCESS_BUT_MISSING_EXPECTED_OPERATION=220 The throttle definitions file was updated, but some supported operations were not assigned a bucket
     * @property {number} UNPARSEABLE_THROTTLE_DEFINITIONS=221 The new contents for the throttle definitions system file were not valid protobuf
     * @property {number} INVALID_THROTTLE_DEFINITIONS=222 The new throttle definitions system file were invalid, and no more specific error could be divined
     * @property {number} ACCOUNT_EXPIRED_AND_PENDING_REMOVAL=223 The transaction references an account which has passed its expiration without renewal funds available, and currently remains in the ledger only because of the grace period given to expired entities
     * @property {number} INVALID_TOKEN_MAX_SUPPLY=224 Invalid token max supply
     * @property {number} INVALID_TOKEN_NFT_SERIAL_NUMBER=225 Invalid token nft serial number
     * @property {number} INVALID_NFT_ID=226 Invalid nft id
     * @property {number} METADATA_TOO_LONG=227 Nft metadata is too long
     * @property {number} BATCH_SIZE_LIMIT_EXCEEDED=228 Repeated operations count exceeds the limit
     * @property {number} INVALID_QUERY_RANGE=229 The range of data to be gathered is out of the set boundaries
     * @property {number} FRACTION_DIVIDES_BY_ZERO=230 A custom fractional fee set a denominator of zero
     * @property {number} INSUFFICIENT_PAYER_BALANCE_FOR_CUSTOM_FEE=231 The transaction payer could not afford a custom fee
     * @property {number} CUSTOM_FEES_LIST_TOO_LONG=232 More than 10 custom fees were specified
     * @property {number} INVALID_CUSTOM_FEE_COLLECTOR=233 Any of the feeCollector accounts for customFees is invalid
     * @property {number} INVALID_TOKEN_ID_IN_CUSTOM_FEES=234 Any of the token Ids in customFees is invalid
     * @property {number} TOKEN_NOT_ASSOCIATED_TO_FEE_COLLECTOR=235 Any of the token Ids in customFees are not associated to feeCollector
     * @property {number} TOKEN_MAX_SUPPLY_REACHED=236 A token cannot have more units minted due to its configured supply ceiling
     * @property {number} SENDER_DOES_NOT_OWN_NFT_SERIAL_NO=237 The transaction attempted to move an NFT serial number from an account other than its owner
     * @property {number} CUSTOM_FEE_NOT_FULLY_SPECIFIED=238 A custom fee schedule entry did not specify either a fixed or fractional fee
     * @property {number} CUSTOM_FEE_MUST_BE_POSITIVE=239 Only positive fees may be assessed at this time
     * @property {number} TOKEN_HAS_NO_FEE_SCHEDULE_KEY=240 Fee schedule key is not set on token
     * @property {number} CUSTOM_FEE_OUTSIDE_NUMERIC_RANGE=241 A fractional custom fee exceeded the range of a 64-bit signed integer
     * @property {number} ROYALTY_FRACTION_CANNOT_EXCEED_ONE=242 A royalty cannot exceed the total fungible value exchanged for an NFT
     * @property {number} FRACTIONAL_FEE_MAX_AMOUNT_LESS_THAN_MIN_AMOUNT=243 Each fractional custom fee must have its maximum_amount, if specified, at least its minimum_amount
     * @property {number} CUSTOM_SCHEDULE_ALREADY_HAS_NO_FEES=244 A fee schedule update tried to clear the custom fees from a token whose fee schedule was already empty
     * @property {number} CUSTOM_FEE_DENOMINATION_MUST_BE_FUNGIBLE_COMMON=245 Only tokens of type FUNGIBLE_COMMON can be used to as fee schedule denominations
     * @property {number} CUSTOM_FRACTIONAL_FEE_ONLY_ALLOWED_FOR_FUNGIBLE_COMMON=246 Only tokens of type FUNGIBLE_COMMON can have fractional fees
     * @property {number} INVALID_CUSTOM_FEE_SCHEDULE_KEY=247 The provided custom fee schedule key was invalid
     * @property {number} INVALID_TOKEN_MINT_METADATA=248 The requested token mint metadata was invalid
     * @property {number} INVALID_TOKEN_BURN_METADATA=249 The requested token burn metadata was invalid
     * @property {number} CURRENT_TREASURY_STILL_OWNS_NFTS=250 The treasury for a unique token cannot be changed until it owns no NFTs
     * @property {number} ACCOUNT_STILL_OWNS_NFTS=251 An account cannot be dissociated from a unique token if it owns NFTs for the token
     * @property {number} TREASURY_MUST_OWN_BURNED_NFT=252 A NFT can only be burned when owned by the unique token's treasury
     * @property {number} ACCOUNT_DOES_NOT_OWN_WIPED_NFT=253 An account did not own the NFT to be wiped
     * @property {number} ACCOUNT_AMOUNT_TRANSFERS_ONLY_ALLOWED_FOR_FUNGIBLE_COMMON=254 An AccountAmount token transfers list referenced a token type other than FUNGIBLE_COMMON
     * @property {number} MAX_NFTS_IN_PRICE_REGIME_HAVE_BEEN_MINTED=255 All the NFTs allowed in the current price regime have already been minted
     * @property {number} PAYER_ACCOUNT_DELETED=256 The payer account has been marked as deleted
     * @property {number} CUSTOM_FEE_CHARGING_EXCEEDED_MAX_RECURSION_DEPTH=257 The reference chain of custom fees for a transferred token exceeded the maximum length of 2
     * @property {number} CUSTOM_FEE_CHARGING_EXCEEDED_MAX_ACCOUNT_AMOUNTS=258 More than 20 balance adjustments were to satisfy a CryptoTransfer and its implied custom fee payments
     * @property {number} INSUFFICIENT_SENDER_ACCOUNT_BALANCE_FOR_CUSTOM_FEE=259 The sender account in the token transfer transaction could not afford a custom fee
     * @property {number} SERIAL_NUMBER_LIMIT_REACHED=260 Currently no more than 4,294,967,295 NFTs may be minted for a given unique token type
     * @property {number} CUSTOM_ROYALTY_FEE_ONLY_ALLOWED_FOR_NON_FUNGIBLE_UNIQUE=261 Only tokens of type NON_FUNGIBLE_UNIQUE can have royalty fees
     * @property {number} NO_REMAINING_AUTOMATIC_ASSOCIATIONS=262 The account has reached the limit on the automatic associations count.
     * @property {number} EXISTING_AUTOMATIC_ASSOCIATIONS_EXCEED_GIVEN_LIMIT=263 Already existing automatic associations are more than the new maximum automatic associations.
     * @property {number} REQUESTED_NUM_AUTOMATIC_ASSOCIATIONS_EXCEEDS_ASSOCIATION_LIMIT=264 Cannot set the number of automatic associations for an account more than the maximum allowed
     * token associations <tt>tokens.maxPerAccount</tt>.
     * @property {number} TOKEN_IS_PAUSED=265 Token is paused. This Token cannot be a part of any kind of Transaction until unpaused.
     * @property {number} TOKEN_HAS_NO_PAUSE_KEY=266 Pause key is not set on token
     * @property {number} INVALID_PAUSE_KEY=267 The provided pause key was invalid
     * @property {number} FREEZE_UPDATE_FILE_DOES_NOT_EXIST=268 The update file in a freeze transaction body must exist.
     * @property {number} FREEZE_UPDATE_FILE_HASH_DOES_NOT_MATCH=269 The hash of the update file in a freeze transaction body must match the in-memory hash.
     * @property {number} NO_UPGRADE_HAS_BEEN_PREPARED=270 A FREEZE_UPGRADE transaction was handled with no previous update prepared.
     * @property {number} NO_FREEZE_IS_SCHEDULED=271 A FREEZE_ABORT transaction was handled with no scheduled freeze.
     * @property {number} UPDATE_FILE_HASH_CHANGED_SINCE_PREPARE_UPGRADE=272 The update file hash when handling a FREEZE_UPGRADE transaction differs from the file
     * hash at the time of handling the PREPARE_UPGRADE transaction.
     * @property {number} FREEZE_START_TIME_MUST_BE_FUTURE=273 The given freeze start time was in the (consensus) past.
     * @property {number} PREPARED_UPDATE_FILE_IS_IMMUTABLE=274 The prepared update file cannot be updated or appended until either the upgrade has
     * been completed, or a FREEZE_ABORT has been handled.
     * @property {number} FREEZE_ALREADY_SCHEDULED=275 Once a freeze is scheduled, it must be aborted before any other type of freeze can
     * can be performed.
     * @property {number} FREEZE_UPGRADE_IN_PROGRESS=276 If an NMT upgrade has been prepared, the following operation must be a FREEZE_UPGRADE.
     * (To issue a FREEZE_ONLY, submit a FREEZE_ABORT first.)
     * @property {number} UPDATE_FILE_ID_DOES_NOT_MATCH_PREPARED=277 If an NMT upgrade has been prepared, the subsequent FREEZE_UPGRADE transaction must
     * confirm the id of the file to be used in the upgrade.
     * @property {number} UPDATE_FILE_HASH_DOES_NOT_MATCH_PREPARED=278 If an NMT upgrade has been prepared, the subsequent FREEZE_UPGRADE transaction must
     * confirm the hash of the file to be used in the upgrade.
     * @property {number} CONSENSUS_GAS_EXHAUSTED=279 Consensus throttle did not allow execution of this transaction. System is throttled at
     * consensus level.
     * @property {number} REVERTED_SUCCESS=280 A precompiled contract succeeded, but was later reverted.
     * @property {number} MAX_STORAGE_IN_PRICE_REGIME_HAS_BEEN_USED=281 All contract storage allocated to the current price regime has been consumed.
     * @property {number} INVALID_ALIAS_KEY=282 An alias used in a CryptoTransfer transaction is not the serialization of a primitive Key
     * message--that is, a Key with a single Ed25519 or ECDSA(secp256k1) public key and no
     * unknown protobuf fields.
     * @property {number} UNEXPECTED_TOKEN_DECIMALS=283 A fungible token transfer expected a different number of decimals than the involved
     * type actually has.
     * @property {number} INVALID_PROXY_ACCOUNT_ID=284 The proxy account id is invalid or does not exist.
     * @property {number} INVALID_TRANSFER_ACCOUNT_ID=285 The transfer account id in CryptoDelete transaction is invalid or does not exist.
     * @property {number} INVALID_FEE_COLLECTOR_ACCOUNT_ID=286 The fee collector account id in TokenFeeScheduleUpdate is invalid or does not exist.
     * @property {number} ALIAS_IS_IMMUTABLE=287 The alias already set on an account cannot be updated using CryptoUpdate transaction.
     * @property {number} SPENDER_ACCOUNT_SAME_AS_OWNER=288 An approved allowance specifies a spender account that is the same as the hbar/token
     * owner account.
     * @property {number} AMOUNT_EXCEEDS_TOKEN_MAX_SUPPLY=289 The establishment or adjustment of an approved allowance cause the token allowance
     * to exceed the token maximum supply.
     * @property {number} NEGATIVE_ALLOWANCE_AMOUNT=290 The specified amount for an approved allowance cannot be negative.
     * @property {number} CANNOT_APPROVE_FOR_ALL_FUNGIBLE_COMMON=291 The approveForAll flag cannot be set for a fungible token.
     * @property {number} SPENDER_DOES_NOT_HAVE_ALLOWANCE=292 The spender does not have an existing approved allowance with the hbar/token owner.
     * @property {number} AMOUNT_EXCEEDS_ALLOWANCE=293 The transfer amount exceeds the current approved allowance for the spender account.
     * @property {number} MAX_ALLOWANCES_EXCEEDED=294 The payer account of an approveAllowances or adjustAllowance transaction is attempting
     * to go beyond the maximum allowed number of allowances.
     * @property {number} EMPTY_ALLOWANCES=295 No allowances have been specified in the approval transaction.
     * @property {number} SPENDER_ACCOUNT_REPEATED_IN_ALLOWANCES=296 Spender is repeated more than once in Crypto or Token or NFT allowance lists in a single
     * CryptoApproveAllowance transaction.
     * @property {number} REPEATED_SERIAL_NUMS_IN_NFT_ALLOWANCES=297 Serial numbers are repeated in nft allowance for a single spender account
     * @property {number} FUNGIBLE_TOKEN_IN_NFT_ALLOWANCES=298 Fungible common token used in NFT allowances
     * @property {number} NFT_IN_FUNGIBLE_TOKEN_ALLOWANCES=299 Non fungible token used in fungible token allowances
     * @property {number} INVALID_ALLOWANCE_OWNER_ID=300 The account id specified as the owner is invalid or does not exist.
     * @property {number} INVALID_ALLOWANCE_SPENDER_ID=301 The account id specified as the spender is invalid or does not exist.
     * @property {number} REPEATED_ALLOWANCES_TO_DELETE=302 [Deprecated] If the CryptoDeleteAllowance transaction has repeated crypto or token or Nft allowances to delete.
     * @property {number} INVALID_DELEGATING_SPENDER=303 If the account Id specified as the delegating spender is invalid or does not exist.
     * @property {number} DELEGATING_SPENDER_CANNOT_GRANT_APPROVE_FOR_ALL=304 The delegating Spender cannot grant approveForAll allowance on a NFT token type for another spender.
     * @property {number} DELEGATING_SPENDER_DOES_NOT_HAVE_APPROVE_FOR_ALL=305 The delegating Spender cannot grant allowance on a NFT serial for another spender as it doesnt not have approveForAll
     * granted on token-owner.
     * @property {number} SCHEDULE_EXPIRATION_TIME_TOO_FAR_IN_FUTURE=306 The scheduled transaction could not be created because it's expiration_time was too far in the future.
     * @property {number} SCHEDULE_EXPIRATION_TIME_MUST_BE_HIGHER_THAN_CONSENSUS_TIME=307 The scheduled transaction could not be created because it's expiration_time was less than or equal to the consensus time.
     * @property {number} SCHEDULE_FUTURE_THROTTLE_EXCEEDED=308 The scheduled transaction could not be created because it would cause throttles to be violated on the specified expiration_time.
     * @property {number} SCHEDULE_FUTURE_GAS_LIMIT_EXCEEDED=309 The scheduled transaction could not be created because it would cause the gas limit to be violated on the specified expiration_time.
     * @property {number} INVALID_ETHEREUM_TRANSACTION=310 The ethereum transaction either failed parsing or failed signature validation, or some other EthereumTransaction error not covered by another response code.
     * @property {number} WRONG_CHAIN_ID=311 EthereumTransaction was signed against a chainId that this network does not support.
     * @property {number} WRONG_NONCE=312 This transaction specified an ethereumNonce that is not the current ethereumNonce of the account.
     * @property {number} ACCESS_LIST_UNSUPPORTED=313 The ethereum transaction specified an access list, which the network does not support.
     * @property {number} SCHEDULE_PENDING_EXPIRATION=314 A schedule being signed or deleted has passed it's expiration date and is pending execution if needed and then expiration.
     * @property {number} CONTRACT_IS_TOKEN_TREASURY=315 A selfdestruct or ContractDelete targeted a contract that is a token treasury.
     * @property {number} CONTRACT_HAS_NON_ZERO_TOKEN_BALANCES=316 A selfdestruct or ContractDelete targeted a contract with non-zero token balances.
     * @property {number} CONTRACT_EXPIRED_AND_PENDING_REMOVAL=317 A contract referenced by a transaction is "detached"; that is, expired and lacking any
     * hbar funds for auto-renewal payment---but still within its post-expiry grace period.
     * @property {number} CONTRACT_HAS_NO_AUTO_RENEW_ACCOUNT=318 A ContractUpdate requested removal of a contract's auto-renew account, but that contract has
     * no auto-renew account.
     * @property {number} PERMANENT_REMOVAL_REQUIRES_SYSTEM_INITIATION=319 A delete transaction submitted via HAPI set permanent_removal=true
     * @property {number} PROXY_ACCOUNT_ID_FIELD_IS_DEPRECATED=320 PROXY_ACCOUNT_ID_FIELD_IS_DEPRECATED value
     * @property {number} SELF_STAKING_IS_NOT_ALLOWED=321 An account set the staked_account_id to itself in CryptoUpdate or ContractUpdate transactions.
     * @property {number} INVALID_STAKING_ID=322 The staking account id or staking node id given is invalid or does not exist.
     * @property {number} STAKING_NOT_ENABLED=323 Native staking, while implemented, has not yet enabled by the council.
     * @property {number} INVALID_PRNG_RANGE=324 The range provided in UtilPrng transaction is negative.
     * @property {number} MAX_ENTITIES_IN_PRICE_REGIME_HAVE_BEEN_CREATED=325 The maximum number of entities allowed in the current price regime have been created.
     * @property {number} INVALID_FULL_PREFIX_SIGNATURE_FOR_PRECOMPILE=326 The full prefix signature for precompile is not valid
     * @property {number} INSUFFICIENT_BALANCES_FOR_STORAGE_RENT=327 The combined balances of a contract and its auto-renew account (if any) did not cover
     * the rent charged for net new storage used in a transaction.
     * @property {number} MAX_CHILD_RECORDS_EXCEEDED=328 A contract transaction tried to use more than the allowed number of child records, via
     * either system contract records or internal contract creations.
     * @property {number} INSUFFICIENT_BALANCES_FOR_RENEWAL_FEES=329 The combined balances of a contract and its auto-renew account (if any) or balance of an account did not cover
     * the auto-renewal fees in a transaction.
     * @property {number} TRANSACTION_HAS_UNKNOWN_FIELDS=330 A transaction's protobuf message includes unknown fields; could mean that a client
     * expects not-yet-released functionality to be available.
     * @property {number} ACCOUNT_IS_IMMUTABLE=331 The account cannot be modified. Account's key is not set
     * @property {number} ALIAS_ALREADY_ASSIGNED=332 An alias that is assigned to an account or contract cannot be assigned to another account or contract.
     * @property {number} INVALID_METADATA_KEY=333 A provided metadata key was invalid. Verification includes, for example, checking the size of Ed25519 and ECDSA(secp256k1) public keys.
     * @property {number} TOKEN_HAS_NO_METADATA_KEY=334 Metadata key is not set on token
     * @property {number} MISSING_TOKEN_METADATA=335 Token Metadata is not provided
     * @property {number} MISSING_SERIAL_NUMBERS=336 NFT serial numbers are missing in the TokenUpdateNftsTransactionBody
     * @property {number} TOKEN_HAS_NO_ADMIN_KEY=337 Admin key is not set on token
     * @property {number} NODE_DELETED=338 A transaction failed because the consensus node identified is
     * deleted from the address book.
     * @property {number} INVALID_NODE_ID=339 A transaction failed because the consensus node identified is not valid or
     * does not exist in state.
     * @property {number} INVALID_GOSSIP_ENDPOINT=340 A transaction failed because one or more entries in the list of
     * service endpoints for the `gossip_endpoint` field is invalid.<br/>
     * The most common cause for this response is a service endpoint that has
     * the domain name (DNS) set rather than address and port.
     * @property {number} INVALID_NODE_ACCOUNT_ID=341 A transaction failed because the node account identifier provided
     * does not exist or is not valid.<br/>
     * One common source of this error is providing a node account identifier
     * using the "alias" form rather than "numeric" form.
     * It is also used for atomic batch transaction for child transaction if the node account id is not 0.0.0.
     * @property {number} INVALID_NODE_DESCRIPTION=342 A transaction failed because the description field cannot be encoded
     * as UTF-8 or is more than 100 bytes when encoded.
     * @property {number} INVALID_SERVICE_ENDPOINT=343 A transaction failed because one or more entries in the list of
     * service endpoints for the `service_endpoint` field is invalid.<br/>
     * The most common cause for this response is a service endpoint that has
     * the domain name (DNS) set rather than address and port.
     * @property {number} INVALID_GOSSIP_CA_CERTIFICATE=344 A transaction failed because the TLS certificate provided for the
     * node is missing or invalid.
     * <p>
     * #### Probable Causes
     * The certificate MUST be a TLS certificate of a type permitted for gossip
     * signatures.<br/>
     * The value presented MUST be a UTF-8 NFKD encoding of the TLS
     * certificate.<br/>
     * The certificate encoded MUST be in PEM format.<br/>
     * The `gossip_ca_certificate` field is REQUIRED and MUST NOT be empty.
     * @property {number} INVALID_GRPC_CERTIFICATE=345 A transaction failed because the hash provided for the gRPC certificate
     * is present but invalid.
     * <p>
     * #### Probable Causes
     * The `grpc_certificate_hash` MUST be a SHA-384 hash.<br/>
     * The input hashed MUST be a UTF-8 NFKD encoding of the actual TLS
     * certificate.<br/>
     * The certificate to be encoded MUST be in PEM format.
     * @property {number} INVALID_MAX_AUTO_ASSOCIATIONS=346 The maximum automatic associations value is not valid.<br/>
     * The most common cause for this error is a value less than `-1`.
     * @property {number} MAX_NODES_CREATED=347 The maximum number of nodes allowed in the address book have been created.
     * @property {number} IP_FQDN_CANNOT_BE_SET_FOR_SAME_ENDPOINT=348 In ServiceEndpoint, domain_name and ipAddressV4 are mutually exclusive
     * @property {number} GOSSIP_ENDPOINT_CANNOT_HAVE_FQDN=349 Fully qualified domain name is not allowed in gossip_endpoint
     * @property {number} FQDN_SIZE_TOO_LARGE=350 In ServiceEndpoint, domain_name size too large
     * @property {number} INVALID_ENDPOINT=351 ServiceEndpoint is invalid
     * @property {number} GOSSIP_ENDPOINTS_EXCEEDED_LIMIT=352 The number of gossip endpoints exceeds the limit
     * @property {number} TOKEN_REFERENCE_REPEATED=353 The transaction attempted to use duplicate `TokenReference`.<br/>
     * This affects `TokenReject` attempting to reject same token reference more than once.
     * @property {number} INVALID_OWNER_ID=354 The account id specified as the owner in `TokenReject` is invalid or does not exist.
     * @property {number} TOKEN_REFERENCE_LIST_SIZE_LIMIT_EXCEEDED=355 The transaction attempted to use more than the allowed number of `TokenReference`.
     * @property {number} SERVICE_ENDPOINTS_EXCEEDED_LIMIT=356 The number of service endpoints exceeds the limit
     * @property {number} INVALID_IPV4_ADDRESS=357 INVALID_IPV4_ADDRESS value
     * @property {number} EMPTY_TOKEN_REFERENCE_LIST=358 The transaction attempted to use empty `TokenReference` list.
     * @property {number} UPDATE_NODE_ACCOUNT_NOT_ALLOWED=359 UPDATE_NODE_ACCOUNT_NOT_ALLOWED value
     * @property {number} TOKEN_HAS_NO_METADATA_OR_SUPPLY_KEY=360 TOKEN_HAS_NO_METADATA_OR_SUPPLY_KEY value
     * @property {number} EMPTY_PENDING_AIRDROP_ID_LIST=361 The list of `PendingAirdropId`s is empty and MUST NOT be empty.
     * @property {number} PENDING_AIRDROP_ID_REPEATED=362 A `PendingAirdropId` is repeated in a `claim` or `cancel` transaction.
     * @property {number} PENDING_AIRDROP_ID_LIST_TOO_LONG=363 The number of `PendingAirdropId` values in the list exceeds the maximum
     * allowable number.
     * @property {number} PENDING_NFT_AIRDROP_ALREADY_EXISTS=364 PENDING_NFT_AIRDROP_ALREADY_EXISTS value
     * @property {number} ACCOUNT_HAS_PENDING_AIRDROPS=365 ACCOUNT_HAS_PENDING_AIRDROPS value
     * @property {number} THROTTLED_AT_CONSENSUS=366 Consensus throttle did not allow execution of this transaction.<br/>
     * The transaction should be retried after a modest delay.
     * @property {number} INVALID_PENDING_AIRDROP_ID=367 The provided pending airdrop id is invalid.<br/>
     * This pending airdrop MAY already be claimed or cancelled.
     * <p>
     * The client SHOULD query a mirror node to determine the current status of
     * the pending airdrop.
     * @property {number} TOKEN_AIRDROP_WITH_FALLBACK_ROYALTY=368 The token to be airdropped has a fallback royalty fee and cannot be
     * sent or claimed via an airdrop transaction.
     * @property {number} INVALID_TOKEN_IN_PENDING_AIRDROP=369 This airdrop claim is for a pending airdrop with an invalid token.<br/>
     * The token might be deleted, or the sender may not have enough tokens
     * to fulfill the offer.
     * <p>
     * The client SHOULD query mirror node to determine the status of the
     * pending airdrop and whether the sender can fulfill the offer.
     * @property {number} SCHEDULE_EXPIRY_IS_BUSY=370 A scheduled transaction configured to wait for expiry to execute was given
     * an expiry time at which there is already too many transactions scheduled to
     * expire; its creation must be retried with a different expiry.
     * @property {number} INVALID_GRPC_CERTIFICATE_HASH=371 The provided gRPC certificate hash is invalid.
     * @property {number} MISSING_EXPIRY_TIME=372 A scheduled transaction configured to wait for expiry to execute was not
     * given an explicit expiration time.
     * @property {number} NO_SCHEDULING_ALLOWED_AFTER_SCHEDULED_RECURSION=373 A contract operation attempted to schedule another transaction after it
     * had already scheduled a recursive contract call.
     * @property {number} RECURSIVE_SCHEDULING_LIMIT_REACHED=374 A contract can schedule recursive calls a finite number of times (this is
     * approximately four million times with typical network configuration.)
     * @property {number} WAITING_FOR_LEDGER_ID=375 The target network is waiting for the ledger ID to be set, which is a
     * side effect of finishing the network's TSS construction.
     * @property {number} MAX_ENTRIES_FOR_FEE_EXEMPT_KEY_LIST_EXCEEDED=376 The provided fee exempt key list size exceeded the limit.
     * @property {number} FEE_EXEMPT_KEY_LIST_CONTAINS_DUPLICATED_KEYS=377 The provided fee exempt key list contains duplicated keys.
     * @property {number} INVALID_KEY_IN_FEE_EXEMPT_KEY_LIST=378 The provided fee exempt key list contains an invalid key.
     * @property {number} INVALID_FEE_SCHEDULE_KEY=379 The provided fee schedule key contains an invalid key.
     * @property {number} FEE_SCHEDULE_KEY_CANNOT_BE_UPDATED=380 If a fee schedule key is not set when we create a topic
     * we cannot add it on update.
     * @property {number} FEE_SCHEDULE_KEY_NOT_SET=381 If the topic's custom fees are updated the topic SHOULD have a
     * fee schedule key
     * @property {number} MAX_CUSTOM_FEE_LIMIT_EXCEEDED=382 The fee amount is exceeding the amount that the payer
     * is willing to pay.
     * @property {number} NO_VALID_MAX_CUSTOM_FEE=383 There are no corresponding custom fees.
     * @property {number} INVALID_MAX_CUSTOM_FEES=384 The provided list contains invalid max custom fee.
     * @property {number} DUPLICATE_DENOMINATION_IN_MAX_CUSTOM_FEE_LIST=385 The provided max custom fee list contains fees with
     * duplicate denominations.
     * @property {number} DUPLICATE_ACCOUNT_ID_IN_MAX_CUSTOM_FEE_LIST=386 The provided max custom fee list contains fees with
     * duplicate account id.
     * @property {number} MAX_CUSTOM_FEES_IS_NOT_SUPPORTED=387 Max custom fees list is not supported for this operation.
     * @property {number} BATCH_LIST_EMPTY=388 The list of batch transactions is empty
     * @property {number} BATCH_LIST_CONTAINS_DUPLICATES=389 The list of batch transactions contains duplicated transactions
     * @property {number} BATCH_TRANSACTION_IN_BLACKLIST=390 The list of batch transactions contains a transaction type that is
     * in the AtomicBatch blacklist as configured in the network.
     * @property {number} INNER_TRANSACTION_FAILED=391 The inner transaction of a batch transaction failed
     * @property {number} MISSING_BATCH_KEY=392 The inner transaction of a batch transaction is missing a batch key
     * @property {number} BATCH_KEY_SET_ON_NON_INNER_TRANSACTION=393 The batch key is set for a non batch transaction
     * @property {number} INVALID_BATCH_KEY=394 The batch key is not valid
     * @property {number} SCHEDULE_EXPIRY_NOT_CONFIGURABLE=395 The provided schedule expiry time is not configurable.
     * @property {number} CREATING_SYSTEM_ENTITIES=396 The network just started at genesis and is creating system entities.
     * @property {number} THROTTLE_GROUP_LCM_OVERFLOW=397 The least common multiple of the throttle group's milliOpsPerSec is
     * too large and it's overflowing.
     * @property {number} AIRDROP_CONTAINS_MULTIPLE_SENDERS_FOR_A_TOKEN=398 Token airdrop transactions can not contain multiple senders for a single token.
     * @property {number} GRPC_WEB_PROXY_NOT_SUPPORTED=399 The GRPC proxy endpoint is set in the NodeCreate or NodeUpdate transaction,
     * which the network does not support.
     * @property {number} NFT_TRANSFERS_ONLY_ALLOWED_FOR_NON_FUNGIBLE_UNIQUE=400 An NFT transfers list referenced a token type other than NON_FUNGIBLE_UNIQUE.
     * @property {number} INVALID_SERIALIZED_TX_MESSAGE_HASH_ALGORITHM=401 A HAPI client cannot set the SignedTransaction#use_serialized_tx_message_hash_algorithm field.
     * @property {number} WRONG_HOOK_ENTITY_TYPE=499 A HookStore referenced a valid entity number but with the wrong entity type.
     * @property {number} EVM_HOOK_GAS_THROTTLED=500 An EVM hook execution was throttled due to high network gas utilization.
     * @property {number} HOOK_ID_IN_USE=501 A user tried to create a hook with an id already in use.
     * @property {number} BAD_HOOK_REQUEST=502 A transaction tried to execute a hook that did not match the specified
     * type or was malformed in some other way.
     * @property {number} REJECTED_BY_ACCOUNT_ALLOWANCE_HOOK=503 A CryptoTransfer relying on a ACCOUNT_ALLOWANCE hook was rejected.
     * @property {number} HOOK_NOT_FOUND=504 A hook id was not found.
     * @property {number} EVM_HOOK_STORAGE_UPDATE_BYTES_TOO_LONG=505 An EVM hook mapping slot, storage key, or storage value exceeded 32 bytes.
     * @property {number} EVM_HOOK_STORAGE_UPDATE_BYTES_MUST_USE_MINIMAL_REPRESENTATION=506 An EVM hook's mapping slot, storage key, or storage value failed to use the
     * minimal representation (i.e., no leading zeros).
     * @property {number} INVALID_HOOK_ID=507 A hook id was invalid.
     * @property {number} EMPTY_EVM_HOOK_STORAGE_UPDATE=508 An EVM hook storage update had no contents.
     * @property {number} HOOK_ID_REPEATED_IN_CREATION_DETAILS=509 A user repeated the same hook id in a creation details list.
     * @property {number} HOOKS_NOT_ENABLED=510 Hooks are not not enabled on the target Hiero network.
     * @property {number} HOOK_IS_NOT_AN_EVM_HOOK=511 The target hook is not an EVM hook.
     * @property {number} HOOK_DELETED=512 A hook was deleted.
     * @property {number} TOO_MANY_EVM_HOOK_STORAGE_UPDATES=513 The HookStore tried to update too many storage slots in a single transaction.
     * @property {number} HOOK_CREATION_BYTES_MUST_USE_MINIMAL_REPRESENTATION=514 An EVM hook mapping slot, storage key, or storage value failed to use the
     * minimal representation (i.e., no leading zeros).
     * @property {number} HOOK_CREATION_BYTES_TOO_LONG=515 A EVM hook mapping slot, storage key, or storage value exceeded 32 bytes.
     * @property {number} INVALID_HOOK_CREATION_SPEC=516 A hook creation spec was not found.
     * @property {number} HOOK_EXTENSION_EMPTY=517 A hook extension point was empty.
     * @property {number} INVALID_HOOK_ADMIN_KEY=518 A hook admin key was invalid.
     * @property {number} HOOK_DELETION_REQUIRES_ZERO_STORAGE_SLOTS=519 The hook deletion requires the hook to have zero storage slots.
     * @property {number} CANNOT_SET_HOOKS_AND_APPROVAL=520 Cannot set both a hook call and an approval on the same AccountAmount or NftTransfer message.
     * @property {number} TRANSACTION_REQUIRES_ZERO_HOOKS=521 The attempted operation is invalid until all the target entity's hooks have been deleted.
     * @property {number} INVALID_HOOK_CALL=522 The HookCall set in the transaction is invalid
     * @property {number} HOOKS_ARE_NOT_SUPPORTED_IN_AIRDROPS=523 Hooks are not supported to be used in TokenAirdrop transactions
     * @property {number} ACCOUNT_IS_LINKED_TO_A_NODE=524 This operation cannot be completed because the target
     * account is a "Node Account".<br/>
     * This account is currently in use as the "Node Account" for a
     * consensus node, and therefore the requested change is
     * not permitted. The transaction may be resubmitted once the
     * account is no longer in use as a "Node Account" for any
     * consensus node.
     * @property {number} HOOKS_EXECUTIONS_REQUIRE_TOP_LEVEL_CRYPTO_TRANSFER=525 Hooks are not supported to be used in Batch transactions and Scheduled transactions.
     * They are only supported in a top level CryptoTransfer transaction.
     * @property {number} NODE_ACCOUNT_HAS_ZERO_BALANCE=526 This operation cannot be completed because the target
     * account has a zero balance.<br/>
     * Node accounts require a positive balance. The transaction may be
     * resubmitted once the account has been funded.
     * @property {number} TRANSFER_TO_FEE_COLLECTION_ACCOUNT_NOT_ALLOWED=527 This operation cannot be completed because the target
     * account is a "Fee Collection Account".<br/>
     * Any attempt to transfer to a fee collection account is not permitted.
     * @property {number} TOO_MANY_HOOK_INVOCATIONS=528 The number of hook invocations exceeds the maximum allowed per transaction.
     * @property {number} INVALID_REGISTERED_NODE_ID=529 A registered node ID is invalid or does not exist.
     * @property {number} INVALID_REGISTERED_ENDPOINT=530 A registered service endpoint is invalid.<br/>
     * The port is out of range, or the address field is not set.
     * @property {number} REGISTERED_ENDPOINTS_EXCEEDED_LIMIT=531 The number of registered service endpoints exceeds the configured limit.
     * @property {number} INVALID_REGISTERED_ENDPOINT_ADDRESS=532 A registered service endpoint has an invalid address.<br/>
     * The IP address length is not 4 (IPv4) or 16 (IPv6), or the
     * domain name is not a valid ASCII FQDN.
     * @property {number} INVALID_REGISTERED_ENDPOINT_TYPE=533 A registered service endpoint does not specify an endpoint type.<br/>
     * Exactly one of block_node, mirror_node, or rpc_relay MUST be set.
     * @property {number} REGISTERED_NODE_STILL_ASSOCIATED=534 A registered node cannot be deleted because it is still associated
     * with a consensus node via their associated registered node list.
     * @property {number} MAX_REGISTERED_NODES_EXCEEDED=535 The number of associated registered nodes exceeds the maximum allowed limit.
     */
    proto.ResponseCodeEnum = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "OK"] = 0;
        values[valuesById[1] = "INVALID_TRANSACTION"] = 1;
        values[valuesById[2] = "PAYER_ACCOUNT_NOT_FOUND"] = 2;
        values[valuesById[3] = "INVALID_NODE_ACCOUNT"] = 3;
        values[valuesById[4] = "TRANSACTION_EXPIRED"] = 4;
        values[valuesById[5] = "INVALID_TRANSACTION_START"] = 5;
        values[valuesById[6] = "INVALID_TRANSACTION_DURATION"] = 6;
        values[valuesById[7] = "INVALID_SIGNATURE"] = 7;
        values[valuesById[8] = "MEMO_TOO_LONG"] = 8;
        values[valuesById[9] = "INSUFFICIENT_TX_FEE"] = 9;
        values[valuesById[10] = "INSUFFICIENT_PAYER_BALANCE"] = 10;
        values[valuesById[11] = "DUPLICATE_TRANSACTION"] = 11;
        values[valuesById[12] = "BUSY"] = 12;
        values[valuesById[13] = "NOT_SUPPORTED"] = 13;
        values[valuesById[14] = "INVALID_FILE_ID"] = 14;
        values[valuesById[15] = "INVALID_ACCOUNT_ID"] = 15;
        values[valuesById[16] = "INVALID_CONTRACT_ID"] = 16;
        values[valuesById[17] = "INVALID_TRANSACTION_ID"] = 17;
        values[valuesById[18] = "RECEIPT_NOT_FOUND"] = 18;
        values[valuesById[19] = "RECORD_NOT_FOUND"] = 19;
        values[valuesById[20] = "INVALID_SOLIDITY_ID"] = 20;
        values[valuesById[21] = "UNKNOWN"] = 21;
        values[valuesById[22] = "SUCCESS"] = 22;
        values[valuesById[23] = "FAIL_INVALID"] = 23;
        values[valuesById[24] = "FAIL_FEE"] = 24;
        values[valuesById[25] = "FAIL_BALANCE"] = 25;
        values[valuesById[26] = "KEY_REQUIRED"] = 26;
        values[valuesById[27] = "BAD_ENCODING"] = 27;
        values[valuesById[28] = "INSUFFICIENT_ACCOUNT_BALANCE"] = 28;
        values[valuesById[29] = "INVALID_SOLIDITY_ADDRESS"] = 29;
        values[valuesById[30] = "INSUFFICIENT_GAS"] = 30;
        values[valuesById[31] = "CONTRACT_SIZE_LIMIT_EXCEEDED"] = 31;
        values[valuesById[32] = "LOCAL_CALL_MODIFICATION_EXCEPTION"] = 32;
        values[valuesById[33] = "CONTRACT_REVERT_EXECUTED"] = 33;
        values[valuesById[34] = "CONTRACT_EXECUTION_EXCEPTION"] = 34;
        values[valuesById[35] = "INVALID_RECEIVING_NODE_ACCOUNT"] = 35;
        values[valuesById[36] = "MISSING_QUERY_HEADER"] = 36;
        values[valuesById[37] = "ACCOUNT_UPDATE_FAILED"] = 37;
        values[valuesById[38] = "INVALID_KEY_ENCODING"] = 38;
        values[valuesById[39] = "NULL_SOLIDITY_ADDRESS"] = 39;
        values[valuesById[40] = "CONTRACT_UPDATE_FAILED"] = 40;
        values[valuesById[41] = "INVALID_QUERY_HEADER"] = 41;
        values[valuesById[42] = "INVALID_FEE_SUBMITTED"] = 42;
        values[valuesById[43] = "INVALID_PAYER_SIGNATURE"] = 43;
        values[valuesById[44] = "KEY_NOT_PROVIDED"] = 44;
        values[valuesById[45] = "INVALID_EXPIRATION_TIME"] = 45;
        values[valuesById[46] = "NO_WACL_KEY"] = 46;
        values[valuesById[47] = "FILE_CONTENT_EMPTY"] = 47;
        values[valuesById[48] = "INVALID_ACCOUNT_AMOUNTS"] = 48;
        values[valuesById[49] = "EMPTY_TRANSACTION_BODY"] = 49;
        values[valuesById[50] = "INVALID_TRANSACTION_BODY"] = 50;
        values[valuesById[51] = "INVALID_SIGNATURE_TYPE_MISMATCHING_KEY"] = 51;
        values[valuesById[52] = "INVALID_SIGNATURE_COUNT_MISMATCHING_KEY"] = 52;
        values[valuesById[53] = "EMPTY_LIVE_HASH_BODY"] = 53;
        values[valuesById[54] = "EMPTY_LIVE_HASH"] = 54;
        values[valuesById[55] = "EMPTY_LIVE_HASH_KEYS"] = 55;
        values[valuesById[56] = "INVALID_LIVE_HASH_SIZE"] = 56;
        values[valuesById[57] = "EMPTY_QUERY_BODY"] = 57;
        values[valuesById[58] = "EMPTY_LIVE_HASH_QUERY"] = 58;
        values[valuesById[59] = "LIVE_HASH_NOT_FOUND"] = 59;
        values[valuesById[60] = "ACCOUNT_ID_DOES_NOT_EXIST"] = 60;
        values[valuesById[61] = "LIVE_HASH_ALREADY_EXISTS"] = 61;
        values[valuesById[62] = "INVALID_FILE_WACL"] = 62;
        values[valuesById[63] = "SERIALIZATION_FAILED"] = 63;
        values[valuesById[64] = "TRANSACTION_OVERSIZE"] = 64;
        values[valuesById[65] = "TRANSACTION_TOO_MANY_LAYERS"] = 65;
        values[valuesById[66] = "CONTRACT_DELETED"] = 66;
        values[valuesById[67] = "PLATFORM_NOT_ACTIVE"] = 67;
        values[valuesById[68] = "KEY_PREFIX_MISMATCH"] = 68;
        values[valuesById[69] = "PLATFORM_TRANSACTION_NOT_CREATED"] = 69;
        values[valuesById[70] = "INVALID_RENEWAL_PERIOD"] = 70;
        values[valuesById[71] = "INVALID_PAYER_ACCOUNT_ID"] = 71;
        values[valuesById[72] = "ACCOUNT_DELETED"] = 72;
        values[valuesById[73] = "FILE_DELETED"] = 73;
        values[valuesById[74] = "ACCOUNT_REPEATED_IN_ACCOUNT_AMOUNTS"] = 74;
        values[valuesById[75] = "SETTING_NEGATIVE_ACCOUNT_BALANCE"] = 75;
        values[valuesById[76] = "OBTAINER_REQUIRED"] = 76;
        values[valuesById[77] = "OBTAINER_SAME_CONTRACT_ID"] = 77;
        values[valuesById[78] = "OBTAINER_DOES_NOT_EXIST"] = 78;
        values[valuesById[79] = "MODIFYING_IMMUTABLE_CONTRACT"] = 79;
        values[valuesById[80] = "FILE_SYSTEM_EXCEPTION"] = 80;
        values[valuesById[81] = "AUTORENEW_DURATION_NOT_IN_RANGE"] = 81;
        values[valuesById[82] = "ERROR_DECODING_BYTESTRING"] = 82;
        values[valuesById[83] = "CONTRACT_FILE_EMPTY"] = 83;
        values[valuesById[84] = "CONTRACT_BYTECODE_EMPTY"] = 84;
        values[valuesById[85] = "INVALID_INITIAL_BALANCE"] = 85;
        values[valuesById[86] = "INVALID_RECEIVE_RECORD_THRESHOLD"] = 86;
        values[valuesById[87] = "INVALID_SEND_RECORD_THRESHOLD"] = 87;
        values[valuesById[88] = "ACCOUNT_IS_NOT_GENESIS_ACCOUNT"] = 88;
        values[valuesById[89] = "PAYER_ACCOUNT_UNAUTHORIZED"] = 89;
        values[valuesById[90] = "INVALID_FREEZE_TRANSACTION_BODY"] = 90;
        values[valuesById[91] = "FREEZE_TRANSACTION_BODY_NOT_FOUND"] = 91;
        values[valuesById[92] = "TRANSFER_LIST_SIZE_LIMIT_EXCEEDED"] = 92;
        values[valuesById[93] = "RESULT_SIZE_LIMIT_EXCEEDED"] = 93;
        values[valuesById[94] = "NOT_SPECIAL_ACCOUNT"] = 94;
        values[valuesById[95] = "CONTRACT_NEGATIVE_GAS"] = 95;
        values[valuesById[96] = "CONTRACT_NEGATIVE_VALUE"] = 96;
        values[valuesById[97] = "INVALID_FEE_FILE"] = 97;
        values[valuesById[98] = "INVALID_EXCHANGE_RATE_FILE"] = 98;
        values[valuesById[99] = "INSUFFICIENT_LOCAL_CALL_GAS"] = 99;
        values[valuesById[100] = "ENTITY_NOT_ALLOWED_TO_DELETE"] = 100;
        values[valuesById[101] = "AUTHORIZATION_FAILED"] = 101;
        values[valuesById[102] = "FILE_UPLOADED_PROTO_INVALID"] = 102;
        values[valuesById[103] = "FILE_UPLOADED_PROTO_NOT_SAVED_TO_DISK"] = 103;
        values[valuesById[104] = "FEE_SCHEDULE_FILE_PART_UPLOADED"] = 104;
        values[valuesById[105] = "EXCHANGE_RATE_CHANGE_LIMIT_EXCEEDED"] = 105;
        values[valuesById[106] = "MAX_CONTRACT_STORAGE_EXCEEDED"] = 106;
        values[valuesById[107] = "TRANSFER_ACCOUNT_SAME_AS_DELETE_ACCOUNT"] = 107;
        values[valuesById[108] = "TOTAL_LEDGER_BALANCE_INVALID"] = 108;
        values[valuesById[110] = "EXPIRATION_REDUCTION_NOT_ALLOWED"] = 110;
        values[valuesById[111] = "MAX_GAS_LIMIT_EXCEEDED"] = 111;
        values[valuesById[112] = "MAX_FILE_SIZE_EXCEEDED"] = 112;
        values[valuesById[113] = "RECEIVER_SIG_REQUIRED"] = 113;
        values[valuesById[150] = "INVALID_TOPIC_ID"] = 150;
        values[valuesById[155] = "INVALID_ADMIN_KEY"] = 155;
        values[valuesById[156] = "INVALID_SUBMIT_KEY"] = 156;
        values[valuesById[157] = "UNAUTHORIZED"] = 157;
        values[valuesById[158] = "INVALID_TOPIC_MESSAGE"] = 158;
        values[valuesById[159] = "INVALID_AUTORENEW_ACCOUNT"] = 159;
        values[valuesById[160] = "AUTORENEW_ACCOUNT_NOT_ALLOWED"] = 160;
        values[valuesById[162] = "TOPIC_EXPIRED"] = 162;
        values[valuesById[163] = "INVALID_CHUNK_NUMBER"] = 163;
        values[valuesById[164] = "INVALID_CHUNK_TRANSACTION_ID"] = 164;
        values[valuesById[165] = "ACCOUNT_FROZEN_FOR_TOKEN"] = 165;
        values[valuesById[166] = "TOKENS_PER_ACCOUNT_LIMIT_EXCEEDED"] = 166;
        values[valuesById[167] = "INVALID_TOKEN_ID"] = 167;
        values[valuesById[168] = "INVALID_TOKEN_DECIMALS"] = 168;
        values[valuesById[169] = "INVALID_TOKEN_INITIAL_SUPPLY"] = 169;
        values[valuesById[170] = "INVALID_TREASURY_ACCOUNT_FOR_TOKEN"] = 170;
        values[valuesById[171] = "INVALID_TOKEN_SYMBOL"] = 171;
        values[valuesById[172] = "TOKEN_HAS_NO_FREEZE_KEY"] = 172;
        values[valuesById[173] = "TRANSFERS_NOT_ZERO_SUM_FOR_TOKEN"] = 173;
        values[valuesById[174] = "MISSING_TOKEN_SYMBOL"] = 174;
        values[valuesById[175] = "TOKEN_SYMBOL_TOO_LONG"] = 175;
        values[valuesById[176] = "ACCOUNT_KYC_NOT_GRANTED_FOR_TOKEN"] = 176;
        values[valuesById[177] = "TOKEN_HAS_NO_KYC_KEY"] = 177;
        values[valuesById[178] = "INSUFFICIENT_TOKEN_BALANCE"] = 178;
        values[valuesById[179] = "TOKEN_WAS_DELETED"] = 179;
        values[valuesById[180] = "TOKEN_HAS_NO_SUPPLY_KEY"] = 180;
        values[valuesById[181] = "TOKEN_HAS_NO_WIPE_KEY"] = 181;
        values[valuesById[182] = "INVALID_TOKEN_MINT_AMOUNT"] = 182;
        values[valuesById[183] = "INVALID_TOKEN_BURN_AMOUNT"] = 183;
        values[valuesById[184] = "TOKEN_NOT_ASSOCIATED_TO_ACCOUNT"] = 184;
        values[valuesById[185] = "CANNOT_WIPE_TOKEN_TREASURY_ACCOUNT"] = 185;
        values[valuesById[186] = "INVALID_KYC_KEY"] = 186;
        values[valuesById[187] = "INVALID_WIPE_KEY"] = 187;
        values[valuesById[188] = "INVALID_FREEZE_KEY"] = 188;
        values[valuesById[189] = "INVALID_SUPPLY_KEY"] = 189;
        values[valuesById[190] = "MISSING_TOKEN_NAME"] = 190;
        values[valuesById[191] = "TOKEN_NAME_TOO_LONG"] = 191;
        values[valuesById[192] = "INVALID_WIPING_AMOUNT"] = 192;
        values[valuesById[193] = "TOKEN_IS_IMMUTABLE"] = 193;
        values[valuesById[194] = "TOKEN_ALREADY_ASSOCIATED_TO_ACCOUNT"] = 194;
        values[valuesById[195] = "TRANSACTION_REQUIRES_ZERO_TOKEN_BALANCES"] = 195;
        values[valuesById[196] = "ACCOUNT_IS_TREASURY"] = 196;
        values[valuesById[197] = "TOKEN_ID_REPEATED_IN_TOKEN_LIST"] = 197;
        values[valuesById[198] = "TOKEN_TRANSFER_LIST_SIZE_LIMIT_EXCEEDED"] = 198;
        values[valuesById[199] = "EMPTY_TOKEN_TRANSFER_BODY"] = 199;
        values[valuesById[200] = "EMPTY_TOKEN_TRANSFER_ACCOUNT_AMOUNTS"] = 200;
        values[valuesById[201] = "INVALID_SCHEDULE_ID"] = 201;
        values[valuesById[202] = "SCHEDULE_IS_IMMUTABLE"] = 202;
        values[valuesById[203] = "INVALID_SCHEDULE_PAYER_ID"] = 203;
        values[valuesById[204] = "INVALID_SCHEDULE_ACCOUNT_ID"] = 204;
        values[valuesById[205] = "NO_NEW_VALID_SIGNATURES"] = 205;
        values[valuesById[206] = "UNRESOLVABLE_REQUIRED_SIGNERS"] = 206;
        values[valuesById[207] = "SCHEDULED_TRANSACTION_NOT_IN_WHITELIST"] = 207;
        values[valuesById[208] = "SOME_SIGNATURES_WERE_INVALID"] = 208;
        values[valuesById[209] = "TRANSACTION_ID_FIELD_NOT_ALLOWED"] = 209;
        values[valuesById[210] = "IDENTICAL_SCHEDULE_ALREADY_CREATED"] = 210;
        values[valuesById[211] = "INVALID_ZERO_BYTE_IN_STRING"] = 211;
        values[valuesById[212] = "SCHEDULE_ALREADY_DELETED"] = 212;
        values[valuesById[213] = "SCHEDULE_ALREADY_EXECUTED"] = 213;
        values[valuesById[214] = "MESSAGE_SIZE_TOO_LARGE"] = 214;
        values[valuesById[215] = "OPERATION_REPEATED_IN_BUCKET_GROUPS"] = 215;
        values[valuesById[216] = "BUCKET_CAPACITY_OVERFLOW"] = 216;
        values[valuesById[217] = "NODE_CAPACITY_NOT_SUFFICIENT_FOR_OPERATION"] = 217;
        values[valuesById[218] = "BUCKET_HAS_NO_THROTTLE_GROUPS"] = 218;
        values[valuesById[219] = "THROTTLE_GROUP_HAS_ZERO_OPS_PER_SEC"] = 219;
        values[valuesById[220] = "SUCCESS_BUT_MISSING_EXPECTED_OPERATION"] = 220;
        values[valuesById[221] = "UNPARSEABLE_THROTTLE_DEFINITIONS"] = 221;
        values[valuesById[222] = "INVALID_THROTTLE_DEFINITIONS"] = 222;
        values[valuesById[223] = "ACCOUNT_EXPIRED_AND_PENDING_REMOVAL"] = 223;
        values[valuesById[224] = "INVALID_TOKEN_MAX_SUPPLY"] = 224;
        values[valuesById[225] = "INVALID_TOKEN_NFT_SERIAL_NUMBER"] = 225;
        values[valuesById[226] = "INVALID_NFT_ID"] = 226;
        values[valuesById[227] = "METADATA_TOO_LONG"] = 227;
        values[valuesById[228] = "BATCH_SIZE_LIMIT_EXCEEDED"] = 228;
        values[valuesById[229] = "INVALID_QUERY_RANGE"] = 229;
        values[valuesById[230] = "FRACTION_DIVIDES_BY_ZERO"] = 230;
        values[valuesById[231] = "INSUFFICIENT_PAYER_BALANCE_FOR_CUSTOM_FEE"] = 231;
        values[valuesById[232] = "CUSTOM_FEES_LIST_TOO_LONG"] = 232;
        values[valuesById[233] = "INVALID_CUSTOM_FEE_COLLECTOR"] = 233;
        values[valuesById[234] = "INVALID_TOKEN_ID_IN_CUSTOM_FEES"] = 234;
        values[valuesById[235] = "TOKEN_NOT_ASSOCIATED_TO_FEE_COLLECTOR"] = 235;
        values[valuesById[236] = "TOKEN_MAX_SUPPLY_REACHED"] = 236;
        values[valuesById[237] = "SENDER_DOES_NOT_OWN_NFT_SERIAL_NO"] = 237;
        values[valuesById[238] = "CUSTOM_FEE_NOT_FULLY_SPECIFIED"] = 238;
        values[valuesById[239] = "CUSTOM_FEE_MUST_BE_POSITIVE"] = 239;
        values[valuesById[240] = "TOKEN_HAS_NO_FEE_SCHEDULE_KEY"] = 240;
        values[valuesById[241] = "CUSTOM_FEE_OUTSIDE_NUMERIC_RANGE"] = 241;
        values[valuesById[242] = "ROYALTY_FRACTION_CANNOT_EXCEED_ONE"] = 242;
        values[valuesById[243] = "FRACTIONAL_FEE_MAX_AMOUNT_LESS_THAN_MIN_AMOUNT"] = 243;
        values[valuesById[244] = "CUSTOM_SCHEDULE_ALREADY_HAS_NO_FEES"] = 244;
        values[valuesById[245] = "CUSTOM_FEE_DENOMINATION_MUST_BE_FUNGIBLE_COMMON"] = 245;
        values[valuesById[246] = "CUSTOM_FRACTIONAL_FEE_ONLY_ALLOWED_FOR_FUNGIBLE_COMMON"] = 246;
        values[valuesById[247] = "INVALID_CUSTOM_FEE_SCHEDULE_KEY"] = 247;
        values[valuesById[248] = "INVALID_TOKEN_MINT_METADATA"] = 248;
        values[valuesById[249] = "INVALID_TOKEN_BURN_METADATA"] = 249;
        values[valuesById[250] = "CURRENT_TREASURY_STILL_OWNS_NFTS"] = 250;
        values[valuesById[251] = "ACCOUNT_STILL_OWNS_NFTS"] = 251;
        values[valuesById[252] = "TREASURY_MUST_OWN_BURNED_NFT"] = 252;
        values[valuesById[253] = "ACCOUNT_DOES_NOT_OWN_WIPED_NFT"] = 253;
        values[valuesById[254] = "ACCOUNT_AMOUNT_TRANSFERS_ONLY_ALLOWED_FOR_FUNGIBLE_COMMON"] = 254;
        values[valuesById[255] = "MAX_NFTS_IN_PRICE_REGIME_HAVE_BEEN_MINTED"] = 255;
        values[valuesById[256] = "PAYER_ACCOUNT_DELETED"] = 256;
        values[valuesById[257] = "CUSTOM_FEE_CHARGING_EXCEEDED_MAX_RECURSION_DEPTH"] = 257;
        values[valuesById[258] = "CUSTOM_FEE_CHARGING_EXCEEDED_MAX_ACCOUNT_AMOUNTS"] = 258;
        values[valuesById[259] = "INSUFFICIENT_SENDER_ACCOUNT_BALANCE_FOR_CUSTOM_FEE"] = 259;
        values[valuesById[260] = "SERIAL_NUMBER_LIMIT_REACHED"] = 260;
        values[valuesById[261] = "CUSTOM_ROYALTY_FEE_ONLY_ALLOWED_FOR_NON_FUNGIBLE_UNIQUE"] = 261;
        values[valuesById[262] = "NO_REMAINING_AUTOMATIC_ASSOCIATIONS"] = 262;
        values[valuesById[263] = "EXISTING_AUTOMATIC_ASSOCIATIONS_EXCEED_GIVEN_LIMIT"] = 263;
        values[valuesById[264] = "REQUESTED_NUM_AUTOMATIC_ASSOCIATIONS_EXCEEDS_ASSOCIATION_LIMIT"] = 264;
        values[valuesById[265] = "TOKEN_IS_PAUSED"] = 265;
        values[valuesById[266] = "TOKEN_HAS_NO_PAUSE_KEY"] = 266;
        values[valuesById[267] = "INVALID_PAUSE_KEY"] = 267;
        values[valuesById[268] = "FREEZE_UPDATE_FILE_DOES_NOT_EXIST"] = 268;
        values[valuesById[269] = "FREEZE_UPDATE_FILE_HASH_DOES_NOT_MATCH"] = 269;
        values[valuesById[270] = "NO_UPGRADE_HAS_BEEN_PREPARED"] = 270;
        values[valuesById[271] = "NO_FREEZE_IS_SCHEDULED"] = 271;
        values[valuesById[272] = "UPDATE_FILE_HASH_CHANGED_SINCE_PREPARE_UPGRADE"] = 272;
        values[valuesById[273] = "FREEZE_START_TIME_MUST_BE_FUTURE"] = 273;
        values[valuesById[274] = "PREPARED_UPDATE_FILE_IS_IMMUTABLE"] = 274;
        values[valuesById[275] = "FREEZE_ALREADY_SCHEDULED"] = 275;
        values[valuesById[276] = "FREEZE_UPGRADE_IN_PROGRESS"] = 276;
        values[valuesById[277] = "UPDATE_FILE_ID_DOES_NOT_MATCH_PREPARED"] = 277;
        values[valuesById[278] = "UPDATE_FILE_HASH_DOES_NOT_MATCH_PREPARED"] = 278;
        values[valuesById[279] = "CONSENSUS_GAS_EXHAUSTED"] = 279;
        values[valuesById[280] = "REVERTED_SUCCESS"] = 280;
        values[valuesById[281] = "MAX_STORAGE_IN_PRICE_REGIME_HAS_BEEN_USED"] = 281;
        values[valuesById[282] = "INVALID_ALIAS_KEY"] = 282;
        values[valuesById[283] = "UNEXPECTED_TOKEN_DECIMALS"] = 283;
        values[valuesById[284] = "INVALID_PROXY_ACCOUNT_ID"] = 284;
        values[valuesById[285] = "INVALID_TRANSFER_ACCOUNT_ID"] = 285;
        values[valuesById[286] = "INVALID_FEE_COLLECTOR_ACCOUNT_ID"] = 286;
        values[valuesById[287] = "ALIAS_IS_IMMUTABLE"] = 287;
        values[valuesById[288] = "SPENDER_ACCOUNT_SAME_AS_OWNER"] = 288;
        values[valuesById[289] = "AMOUNT_EXCEEDS_TOKEN_MAX_SUPPLY"] = 289;
        values[valuesById[290] = "NEGATIVE_ALLOWANCE_AMOUNT"] = 290;
        values[valuesById[291] = "CANNOT_APPROVE_FOR_ALL_FUNGIBLE_COMMON"] = 291;
        values[valuesById[292] = "SPENDER_DOES_NOT_HAVE_ALLOWANCE"] = 292;
        values[valuesById[293] = "AMOUNT_EXCEEDS_ALLOWANCE"] = 293;
        values[valuesById[294] = "MAX_ALLOWANCES_EXCEEDED"] = 294;
        values[valuesById[295] = "EMPTY_ALLOWANCES"] = 295;
        values[valuesById[296] = "SPENDER_ACCOUNT_REPEATED_IN_ALLOWANCES"] = 296;
        values[valuesById[297] = "REPEATED_SERIAL_NUMS_IN_NFT_ALLOWANCES"] = 297;
        values[valuesById[298] = "FUNGIBLE_TOKEN_IN_NFT_ALLOWANCES"] = 298;
        values[valuesById[299] = "NFT_IN_FUNGIBLE_TOKEN_ALLOWANCES"] = 299;
        values[valuesById[300] = "INVALID_ALLOWANCE_OWNER_ID"] = 300;
        values[valuesById[301] = "INVALID_ALLOWANCE_SPENDER_ID"] = 301;
        values[valuesById[302] = "REPEATED_ALLOWANCES_TO_DELETE"] = 302;
        values[valuesById[303] = "INVALID_DELEGATING_SPENDER"] = 303;
        values[valuesById[304] = "DELEGATING_SPENDER_CANNOT_GRANT_APPROVE_FOR_ALL"] = 304;
        values[valuesById[305] = "DELEGATING_SPENDER_DOES_NOT_HAVE_APPROVE_FOR_ALL"] = 305;
        values[valuesById[306] = "SCHEDULE_EXPIRATION_TIME_TOO_FAR_IN_FUTURE"] = 306;
        values[valuesById[307] = "SCHEDULE_EXPIRATION_TIME_MUST_BE_HIGHER_THAN_CONSENSUS_TIME"] = 307;
        values[valuesById[308] = "SCHEDULE_FUTURE_THROTTLE_EXCEEDED"] = 308;
        values[valuesById[309] = "SCHEDULE_FUTURE_GAS_LIMIT_EXCEEDED"] = 309;
        values[valuesById[310] = "INVALID_ETHEREUM_TRANSACTION"] = 310;
        values[valuesById[311] = "WRONG_CHAIN_ID"] = 311;
        values[valuesById[312] = "WRONG_NONCE"] = 312;
        values[valuesById[313] = "ACCESS_LIST_UNSUPPORTED"] = 313;
        values[valuesById[314] = "SCHEDULE_PENDING_EXPIRATION"] = 314;
        values[valuesById[315] = "CONTRACT_IS_TOKEN_TREASURY"] = 315;
        values[valuesById[316] = "CONTRACT_HAS_NON_ZERO_TOKEN_BALANCES"] = 316;
        values[valuesById[317] = "CONTRACT_EXPIRED_AND_PENDING_REMOVAL"] = 317;
        values[valuesById[318] = "CONTRACT_HAS_NO_AUTO_RENEW_ACCOUNT"] = 318;
        values[valuesById[319] = "PERMANENT_REMOVAL_REQUIRES_SYSTEM_INITIATION"] = 319;
        values[valuesById[320] = "PROXY_ACCOUNT_ID_FIELD_IS_DEPRECATED"] = 320;
        values[valuesById[321] = "SELF_STAKING_IS_NOT_ALLOWED"] = 321;
        values[valuesById[322] = "INVALID_STAKING_ID"] = 322;
        values[valuesById[323] = "STAKING_NOT_ENABLED"] = 323;
        values[valuesById[324] = "INVALID_PRNG_RANGE"] = 324;
        values[valuesById[325] = "MAX_ENTITIES_IN_PRICE_REGIME_HAVE_BEEN_CREATED"] = 325;
        values[valuesById[326] = "INVALID_FULL_PREFIX_SIGNATURE_FOR_PRECOMPILE"] = 326;
        values[valuesById[327] = "INSUFFICIENT_BALANCES_FOR_STORAGE_RENT"] = 327;
        values[valuesById[328] = "MAX_CHILD_RECORDS_EXCEEDED"] = 328;
        values[valuesById[329] = "INSUFFICIENT_BALANCES_FOR_RENEWAL_FEES"] = 329;
        values[valuesById[330] = "TRANSACTION_HAS_UNKNOWN_FIELDS"] = 330;
        values[valuesById[331] = "ACCOUNT_IS_IMMUTABLE"] = 331;
        values[valuesById[332] = "ALIAS_ALREADY_ASSIGNED"] = 332;
        values[valuesById[333] = "INVALID_METADATA_KEY"] = 333;
        values[valuesById[334] = "TOKEN_HAS_NO_METADATA_KEY"] = 334;
        values[valuesById[335] = "MISSING_TOKEN_METADATA"] = 335;
        values[valuesById[336] = "MISSING_SERIAL_NUMBERS"] = 336;
        values[valuesById[337] = "TOKEN_HAS_NO_ADMIN_KEY"] = 337;
        values[valuesById[338] = "NODE_DELETED"] = 338;
        values[valuesById[339] = "INVALID_NODE_ID"] = 339;
        values[valuesById[340] = "INVALID_GOSSIP_ENDPOINT"] = 340;
        values[valuesById[341] = "INVALID_NODE_ACCOUNT_ID"] = 341;
        values[valuesById[342] = "INVALID_NODE_DESCRIPTION"] = 342;
        values[valuesById[343] = "INVALID_SERVICE_ENDPOINT"] = 343;
        values[valuesById[344] = "INVALID_GOSSIP_CA_CERTIFICATE"] = 344;
        values[valuesById[345] = "INVALID_GRPC_CERTIFICATE"] = 345;
        values[valuesById[346] = "INVALID_MAX_AUTO_ASSOCIATIONS"] = 346;
        values[valuesById[347] = "MAX_NODES_CREATED"] = 347;
        values[valuesById[348] = "IP_FQDN_CANNOT_BE_SET_FOR_SAME_ENDPOINT"] = 348;
        values[valuesById[349] = "GOSSIP_ENDPOINT_CANNOT_HAVE_FQDN"] = 349;
        values[valuesById[350] = "FQDN_SIZE_TOO_LARGE"] = 350;
        values[valuesById[351] = "INVALID_ENDPOINT"] = 351;
        values[valuesById[352] = "GOSSIP_ENDPOINTS_EXCEEDED_LIMIT"] = 352;
        values[valuesById[353] = "TOKEN_REFERENCE_REPEATED"] = 353;
        values[valuesById[354] = "INVALID_OWNER_ID"] = 354;
        values[valuesById[355] = "TOKEN_REFERENCE_LIST_SIZE_LIMIT_EXCEEDED"] = 355;
        values[valuesById[356] = "SERVICE_ENDPOINTS_EXCEEDED_LIMIT"] = 356;
        values[valuesById[357] = "INVALID_IPV4_ADDRESS"] = 357;
        values[valuesById[358] = "EMPTY_TOKEN_REFERENCE_LIST"] = 358;
        values[valuesById[359] = "UPDATE_NODE_ACCOUNT_NOT_ALLOWED"] = 359;
        values[valuesById[360] = "TOKEN_HAS_NO_METADATA_OR_SUPPLY_KEY"] = 360;
        values[valuesById[361] = "EMPTY_PENDING_AIRDROP_ID_LIST"] = 361;
        values[valuesById[362] = "PENDING_AIRDROP_ID_REPEATED"] = 362;
        values[valuesById[363] = "PENDING_AIRDROP_ID_LIST_TOO_LONG"] = 363;
        values[valuesById[364] = "PENDING_NFT_AIRDROP_ALREADY_EXISTS"] = 364;
        values[valuesById[365] = "ACCOUNT_HAS_PENDING_AIRDROPS"] = 365;
        values[valuesById[366] = "THROTTLED_AT_CONSENSUS"] = 366;
        values[valuesById[367] = "INVALID_PENDING_AIRDROP_ID"] = 367;
        values[valuesById[368] = "TOKEN_AIRDROP_WITH_FALLBACK_ROYALTY"] = 368;
        values[valuesById[369] = "INVALID_TOKEN_IN_PENDING_AIRDROP"] = 369;
        values[valuesById[370] = "SCHEDULE_EXPIRY_IS_BUSY"] = 370;
        values[valuesById[371] = "INVALID_GRPC_CERTIFICATE_HASH"] = 371;
        values[valuesById[372] = "MISSING_EXPIRY_TIME"] = 372;
        values[valuesById[373] = "NO_SCHEDULING_ALLOWED_AFTER_SCHEDULED_RECURSION"] = 373;
        values[valuesById[374] = "RECURSIVE_SCHEDULING_LIMIT_REACHED"] = 374;
        values[valuesById[375] = "WAITING_FOR_LEDGER_ID"] = 375;
        values[valuesById[376] = "MAX_ENTRIES_FOR_FEE_EXEMPT_KEY_LIST_EXCEEDED"] = 376;
        values[valuesById[377] = "FEE_EXEMPT_KEY_LIST_CONTAINS_DUPLICATED_KEYS"] = 377;
        values[valuesById[378] = "INVALID_KEY_IN_FEE_EXEMPT_KEY_LIST"] = 378;
        values[valuesById[379] = "INVALID_FEE_SCHEDULE_KEY"] = 379;
        values[valuesById[380] = "FEE_SCHEDULE_KEY_CANNOT_BE_UPDATED"] = 380;
        values[valuesById[381] = "FEE_SCHEDULE_KEY_NOT_SET"] = 381;
        values[valuesById[382] = "MAX_CUSTOM_FEE_LIMIT_EXCEEDED"] = 382;
        values[valuesById[383] = "NO_VALID_MAX_CUSTOM_FEE"] = 383;
        values[valuesById[384] = "INVALID_MAX_CUSTOM_FEES"] = 384;
        values[valuesById[385] = "DUPLICATE_DENOMINATION_IN_MAX_CUSTOM_FEE_LIST"] = 385;
        values[valuesById[386] = "DUPLICATE_ACCOUNT_ID_IN_MAX_CUSTOM_FEE_LIST"] = 386;
        values[valuesById[387] = "MAX_CUSTOM_FEES_IS_NOT_SUPPORTED"] = 387;
        values[valuesById[388] = "BATCH_LIST_EMPTY"] = 388;
        values[valuesById[389] = "BATCH_LIST_CONTAINS_DUPLICATES"] = 389;
        values[valuesById[390] = "BATCH_TRANSACTION_IN_BLACKLIST"] = 390;
        values[valuesById[391] = "INNER_TRANSACTION_FAILED"] = 391;
        values[valuesById[392] = "MISSING_BATCH_KEY"] = 392;
        values[valuesById[393] = "BATCH_KEY_SET_ON_NON_INNER_TRANSACTION"] = 393;
        values[valuesById[394] = "INVALID_BATCH_KEY"] = 394;
        values[valuesById[395] = "SCHEDULE_EXPIRY_NOT_CONFIGURABLE"] = 395;
        values[valuesById[396] = "CREATING_SYSTEM_ENTITIES"] = 396;
        values[valuesById[397] = "THROTTLE_GROUP_LCM_OVERFLOW"] = 397;
        values[valuesById[398] = "AIRDROP_CONTAINS_MULTIPLE_SENDERS_FOR_A_TOKEN"] = 398;
        values[valuesById[399] = "GRPC_WEB_PROXY_NOT_SUPPORTED"] = 399;
        values[valuesById[400] = "NFT_TRANSFERS_ONLY_ALLOWED_FOR_NON_FUNGIBLE_UNIQUE"] = 400;
        values[valuesById[401] = "INVALID_SERIALIZED_TX_MESSAGE_HASH_ALGORITHM"] = 401;
        values[valuesById[499] = "WRONG_HOOK_ENTITY_TYPE"] = 499;
        values[valuesById[500] = "EVM_HOOK_GAS_THROTTLED"] = 500;
        values[valuesById[501] = "HOOK_ID_IN_USE"] = 501;
        values[valuesById[502] = "BAD_HOOK_REQUEST"] = 502;
        values[valuesById[503] = "REJECTED_BY_ACCOUNT_ALLOWANCE_HOOK"] = 503;
        values[valuesById[504] = "HOOK_NOT_FOUND"] = 504;
        values[valuesById[505] = "EVM_HOOK_STORAGE_UPDATE_BYTES_TOO_LONG"] = 505;
        values[valuesById[506] = "EVM_HOOK_STORAGE_UPDATE_BYTES_MUST_USE_MINIMAL_REPRESENTATION"] = 506;
        values[valuesById[507] = "INVALID_HOOK_ID"] = 507;
        values[valuesById[508] = "EMPTY_EVM_HOOK_STORAGE_UPDATE"] = 508;
        values[valuesById[509] = "HOOK_ID_REPEATED_IN_CREATION_DETAILS"] = 509;
        values[valuesById[510] = "HOOKS_NOT_ENABLED"] = 510;
        values[valuesById[511] = "HOOK_IS_NOT_AN_EVM_HOOK"] = 511;
        values[valuesById[512] = "HOOK_DELETED"] = 512;
        values[valuesById[513] = "TOO_MANY_EVM_HOOK_STORAGE_UPDATES"] = 513;
        values[valuesById[514] = "HOOK_CREATION_BYTES_MUST_USE_MINIMAL_REPRESENTATION"] = 514;
        values[valuesById[515] = "HOOK_CREATION_BYTES_TOO_LONG"] = 515;
        values[valuesById[516] = "INVALID_HOOK_CREATION_SPEC"] = 516;
        values[valuesById[517] = "HOOK_EXTENSION_EMPTY"] = 517;
        values[valuesById[518] = "INVALID_HOOK_ADMIN_KEY"] = 518;
        values[valuesById[519] = "HOOK_DELETION_REQUIRES_ZERO_STORAGE_SLOTS"] = 519;
        values[valuesById[520] = "CANNOT_SET_HOOKS_AND_APPROVAL"] = 520;
        values[valuesById[521] = "TRANSACTION_REQUIRES_ZERO_HOOKS"] = 521;
        values[valuesById[522] = "INVALID_HOOK_CALL"] = 522;
        values[valuesById[523] = "HOOKS_ARE_NOT_SUPPORTED_IN_AIRDROPS"] = 523;
        values[valuesById[524] = "ACCOUNT_IS_LINKED_TO_A_NODE"] = 524;
        values[valuesById[525] = "HOOKS_EXECUTIONS_REQUIRE_TOP_LEVEL_CRYPTO_TRANSFER"] = 525;
        values[valuesById[526] = "NODE_ACCOUNT_HAS_ZERO_BALANCE"] = 526;
        values[valuesById[527] = "TRANSFER_TO_FEE_COLLECTION_ACCOUNT_NOT_ALLOWED"] = 527;
        values[valuesById[528] = "TOO_MANY_HOOK_INVOCATIONS"] = 528;
        values[valuesById[529] = "INVALID_REGISTERED_NODE_ID"] = 529;
        values[valuesById[530] = "INVALID_REGISTERED_ENDPOINT"] = 530;
        values[valuesById[531] = "REGISTERED_ENDPOINTS_EXCEEDED_LIMIT"] = 531;
        values[valuesById[532] = "INVALID_REGISTERED_ENDPOINT_ADDRESS"] = 532;
        values[valuesById[533] = "INVALID_REGISTERED_ENDPOINT_TYPE"] = 533;
        values[valuesById[534] = "REGISTERED_NODE_STILL_ASSOCIATED"] = 534;
        values[valuesById[535] = "MAX_REGISTERED_NODES_EXCEEDED"] = 535;
        return values;
    })();

    proto.Transaction = (function() {

        /**
         * Properties of a Transaction.
         * @typedef {Object} proto.Transaction.$Properties
         * @property {proto.TransactionBody.$Properties|null} [body] Replaced with `signedTransactionBytes`.<br/>
         * The body of the transaction.
         * @property {proto.SignatureList.$Properties|null} [sigs] Replaced with `signedTransactionBytes`.<br/>
         * The signatures on the body.
         * @property {proto.SignatureMap.$Properties|null} [sigMap] Replaced with `signedTransactionBytes`.<br/>
         * The signatures on the body with a newer format.
         * @property {Uint8Array|null} [bodyBytes] Replaced with `signedTransactionBytes`.<br/>
         * TransactionBody serialized into bytes.
         * @property {Uint8Array|null} [signedTransactionBytes] A valid, serialized, `SignedTransaction` message.
         * <p>
         * This field MUST be present.
         * This field MUST NOT exceed the current network transaction size limit
         * (currently 6144 bytes).
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Transaction.
         * @memberof proto
         * @interface ITransaction
         * @augments proto.Transaction.$Properties
         * @deprecated Use proto.Transaction.$Properties instead.
         */

        /**
         * Shape of a Transaction.
         * @typedef {{
         *   body?: proto.TransactionBody.$Shape|null;
         *   sigs?: proto.SignatureList.$Shape|null;
         *   sigMap?: proto.SignatureMap.$Shape|null;
         *   bodyBytes?: Uint8Array|null;
         *   signedTransactionBytes?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.Transaction.$Shape
         */

        /**
         * Constructs a new Transaction.
         * @memberof proto
         * @classdesc A wrapper around signed transaction bytes.<br/>
         * This was originally a transaction with body, signatures, and/or bytes,
         * but is not only a wrapper around a byte array containing signed transction
         * bytes.
         * 
         * The `signedTransactionBytes` field is REQUIRED and MUST contain a valid,
         * serialized, `SignedTransaction` message.<br/>
         * All other fields are deprecated and MUST NOT be set.
         * 
         * #### Additional Notes
         * The four deprecated fields will be removed and reserved in a future release.
         * @constructor
         * @param {proto.Transaction.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Transaction = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Replaced with `signedTransactionBytes`.<br/>
         * The body of the transaction.
         * @member {proto.TransactionBody.$Properties|null|undefined} body
         * @memberof proto.Transaction
         * @instance
         */
        Transaction.prototype.body = null;

        /**
         * Replaced with `signedTransactionBytes`.<br/>
         * The signatures on the body.
         * @member {proto.SignatureList.$Properties|null|undefined} sigs
         * @memberof proto.Transaction
         * @instance
         */
        Transaction.prototype.sigs = null;

        /**
         * Replaced with `signedTransactionBytes`.<br/>
         * The signatures on the body with a newer format.
         * @member {proto.SignatureMap.$Properties|null|undefined} sigMap
         * @memberof proto.Transaction
         * @instance
         */
        Transaction.prototype.sigMap = null;

        /**
         * Replaced with `signedTransactionBytes`.<br/>
         * TransactionBody serialized into bytes.
         * @member {Uint8Array} bodyBytes
         * @memberof proto.Transaction
         * @instance
         */
        Transaction.prototype.bodyBytes = $util.newBuffer([]);

        /**
         * A valid, serialized, `SignedTransaction` message.
         * <p>
         * This field MUST be present.
         * This field MUST NOT exceed the current network transaction size limit
         * (currently 6144 bytes).
         * @member {Uint8Array} signedTransactionBytes
         * @memberof proto.Transaction
         * @instance
         */
        Transaction.prototype.signedTransactionBytes = $util.newBuffer([]);

        /**
         * Creates a new Transaction instance using the specified properties.
         * @function create
         * @memberof proto.Transaction
         * @static
         * @param {proto.Transaction.$Properties=} [properties] Properties to set
         * @returns {proto.Transaction} Transaction instance
         * @type {{
         *   (properties: proto.Transaction.$Shape): proto.Transaction & proto.Transaction.$Shape;
         *   (properties?: proto.Transaction.$Properties): proto.Transaction;
         * }}
         */
        Transaction.create = function(properties) {
            return new Transaction(properties);
        };

        /**
         * Encodes the specified Transaction message. Does not implicitly {@link proto.Transaction.verify|verify} messages.
         * @function encode
         * @memberof proto.Transaction
         * @static
         * @param {proto.Transaction.$Properties} message Transaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Transaction.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.body != null && $Object.hasOwnProperty.call(message, "body"))
                $root.proto.TransactionBody.encode(message.body, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.sigs != null && $Object.hasOwnProperty.call(message, "sigs"))
                $root.proto.SignatureList.encode(message.sigs, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.sigMap != null && $Object.hasOwnProperty.call(message, "sigMap"))
                $root.proto.SignatureMap.encode(message.sigMap, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.bodyBytes != null && $Object.hasOwnProperty.call(message, "bodyBytes") && message.bodyBytes.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.bodyBytes);
            if (message.signedTransactionBytes != null && $Object.hasOwnProperty.call(message, "signedTransactionBytes") && message.signedTransactionBytes.length)
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.signedTransactionBytes);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Transaction message, length delimited. Does not implicitly {@link proto.Transaction.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Transaction
         * @static
         * @param {proto.Transaction.$Properties} message Transaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Transaction.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Transaction message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Transaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Transaction & proto.Transaction.$Shape} Transaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Transaction.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Transaction();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.body = $root.proto.TransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.body);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.sigs = $root.proto.SignatureList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.sigs);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.sigMap = $root.proto.SignatureMap.decode(reader, reader.uint32(), $undefined, _depth + 1, message.sigMap);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.bodyBytes = value;
                        else
                            delete message.bodyBytes;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.signedTransactionBytes = value;
                        else
                            delete message.signedTransactionBytes;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Transaction message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Transaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Transaction & proto.Transaction.$Shape} Transaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Transaction.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Transaction message.
         * @function verify
         * @memberof proto.Transaction
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Transaction.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.body != null && $Object.hasOwnProperty.call(message, "body")) {
                let error = $root.proto.TransactionBody.verify(message.body, _depth + 1);
                if (error)
                    return "body." + error;
            }
            if (message.sigs != null && $Object.hasOwnProperty.call(message, "sigs")) {
                let error = $root.proto.SignatureList.verify(message.sigs, _depth + 1);
                if (error)
                    return "sigs." + error;
            }
            if (message.sigMap != null && $Object.hasOwnProperty.call(message, "sigMap")) {
                let error = $root.proto.SignatureMap.verify(message.sigMap, _depth + 1);
                if (error)
                    return "sigMap." + error;
            }
            if (message.bodyBytes != null && $Object.hasOwnProperty.call(message, "bodyBytes"))
                if (!(message.bodyBytes && typeof message.bodyBytes.length === "number" || $util.isString(message.bodyBytes)))
                    return "bodyBytes: buffer expected";
            if (message.signedTransactionBytes != null && $Object.hasOwnProperty.call(message, "signedTransactionBytes"))
                if (!(message.signedTransactionBytes && typeof message.signedTransactionBytes.length === "number" || $util.isString(message.signedTransactionBytes)))
                    return "signedTransactionBytes: buffer expected";
            return null;
        };

        /**
         * Creates a Transaction message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Transaction
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Transaction} Transaction
         */
        Transaction.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Transaction)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Transaction: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Transaction();
            if (object.body != null) {
                if (!$util.isObject(object.body))
                    throw $TypeError(".proto.Transaction.body: object expected");
                message.body = $root.proto.TransactionBody.fromObject(object.body, _depth + 1);
            }
            if (object.sigs != null) {
                if (!$util.isObject(object.sigs))
                    throw $TypeError(".proto.Transaction.sigs: object expected");
                message.sigs = $root.proto.SignatureList.fromObject(object.sigs, _depth + 1);
            }
            if (object.sigMap != null) {
                if (!$util.isObject(object.sigMap))
                    throw $TypeError(".proto.Transaction.sigMap: object expected");
                message.sigMap = $root.proto.SignatureMap.fromObject(object.sigMap, _depth + 1);
            }
            if (object.bodyBytes != null)
                if (object.bodyBytes.length)
                    if (typeof object.bodyBytes === "string")
                        $util.base64.decode(object.bodyBytes, message.bodyBytes = $util.newBuffer($util.base64.length(object.bodyBytes)), 0);
                    else if (object.bodyBytes.length >= 0)
                        message.bodyBytes = object.bodyBytes;
            if (object.signedTransactionBytes != null)
                if (object.signedTransactionBytes.length)
                    if (typeof object.signedTransactionBytes === "string")
                        $util.base64.decode(object.signedTransactionBytes, message.signedTransactionBytes = $util.newBuffer($util.base64.length(object.signedTransactionBytes)), 0);
                    else if (object.signedTransactionBytes.length >= 0)
                        message.signedTransactionBytes = object.signedTransactionBytes;
            return message;
        };

        /**
         * Creates a plain object from a Transaction message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Transaction
         * @static
         * @param {proto.Transaction} message Transaction
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Transaction.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.body = null;
                object.sigs = null;
                object.sigMap = null;
                if (options.bytes === $String)
                    object.bodyBytes = "";
                else {
                    object.bodyBytes = [];
                    if (options.bytes !== $Array)
                        object.bodyBytes = $util.newBuffer(object.bodyBytes);
                }
                if (options.bytes === $String)
                    object.signedTransactionBytes = "";
                else {
                    object.signedTransactionBytes = [];
                    if (options.bytes !== $Array)
                        object.signedTransactionBytes = $util.newBuffer(object.signedTransactionBytes);
                }
            }
            if (message.body != null && $Object.hasOwnProperty.call(message, "body"))
                object.body = $root.proto.TransactionBody.toObject(message.body, options, _depth + 1);
            if (message.sigs != null && $Object.hasOwnProperty.call(message, "sigs"))
                object.sigs = $root.proto.SignatureList.toObject(message.sigs, options, _depth + 1);
            if (message.sigMap != null && $Object.hasOwnProperty.call(message, "sigMap"))
                object.sigMap = $root.proto.SignatureMap.toObject(message.sigMap, options, _depth + 1);
            if (message.bodyBytes != null && $Object.hasOwnProperty.call(message, "bodyBytes"))
                object.bodyBytes = options.bytes === $String ? $util.base64.encode(message.bodyBytes, 0, message.bodyBytes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.bodyBytes) : message.bodyBytes;
            if (message.signedTransactionBytes != null && $Object.hasOwnProperty.call(message, "signedTransactionBytes"))
                object.signedTransactionBytes = options.bytes === $String ? $util.base64.encode(message.signedTransactionBytes, 0, message.signedTransactionBytes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.signedTransactionBytes) : message.signedTransactionBytes;
            return object;
        };

        /**
         * Converts this Transaction to JSON.
         * @function toJSON
         * @memberof proto.Transaction
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Transaction.prototype.toJSON = function() {
            return Transaction.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Transaction
         * @function getTypeUrl
         * @memberof proto.Transaction
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Transaction.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Transaction";
        };

        return Transaction;
    })();

    proto.TransactionBody = (function() {

        /**
         * Properties of a TransactionBody.
         * @typedef {Object} proto.TransactionBody.$Properties
         * @property {proto.TransactionID.$Properties|null} [transactionID] A transaction identifier.<br/>
         * Each transaction is uniquely identified by its transaction
         * identifier.
         * <p>
         * Each transaction identifier MUST be unique.<br/>
         * Multiple transactions MAY be submitted with the same transaction
         * identifier, but all except the first SHALL be rejected as duplicate
         * transactions.<br/>
         * This identifier MUST specify a `payer` account to be charged
         * all fees associated with the transaction.<br/>
         * This identifier MUST specify a "valid start time".<br/>
         * The "valid start time" MUST be strictly _earlier_ than the current
         * network consensus time.<br/>
         * The "valid start time" MUST NOT be more than the current network
         * configuration value for `transaction.maxValidDuration` seconds
         * before the current network consensus time.<br/>
         * This identifier MUST NOT set the `scheduled` flag.<br/>
         * This identifier MUST NOT set a nonce value.
         * @property {proto.AccountID.$Properties|null} [nodeAccountID] A node account identifier.
         * <p>
         * This MUST identify the account of the consensus node to which
         * this transaction is submitted.
         * @property {number|Long|null} [transactionFee] A maximum transaction fee, in tinybar.
         * <p>
         * The network SHALL NOT charge a transaction fee that exceeds this
         * amount.<br/>
         * The network MAY charge up to this amount, and reject the transaction,
         * if the amount offered is insufficient to cover the required fees.<br/>
         * The network MAY charge a minimum fee equal to 80% of the amount offered
         * if the amount offered is much larger than the required fees.
         * @property {proto.Duration.$Properties|null} [transactionValidDuration] A maximum duration in which to execute this transaction.
         * <p>
         * This transaction SHALL be rejected as expired if the valid start time,
         * extended by this duration, is less than the current network consensus
         * time when the transaction is submitted.<br/>
         * This transaction SHALL be rejected with an invalid duration if this
         * value is greater than the current network configuration value for
         * `transaction.maxValidDuration`.
         * @property {boolean|null} [generateRecord] Records are always generated.<br/>
         * Obsolete option to not generate a record.
         * <p>
         * This flag SHALL be ignored. Every transaction SHALL generate a record,
         * or block stream equivalent.
         * @property {string|null} [memo] A short description for this transaction.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {proto.Key.$Properties|null} [batchKey] The <b>public key</b> of the trusted batch assembler.
         * @property {proto.ContractCallTransactionBody.$Properties|null} [contractCall] Call a function defined on a smart contract.
         * @property {proto.ContractCreateTransactionBody.$Properties|null} [contractCreateInstance] Create a smart contract.
         * @property {proto.ContractUpdateTransactionBody.$Properties|null} [contractUpdateInstance] Update a smart contract.
         * @property {proto.CryptoAddLiveHashTransactionBody.$Properties|null} [cryptoAddLiveHash] An obsolete, and unsupported, operation to add a "live hash" to
         * an account.
         * @property {proto.CryptoCreateTransactionBody.$Properties|null} [cryptoCreateAccount] Create a new Hedera account.
         * @property {proto.CryptoDeleteTransactionBody.$Properties|null} [cryptoDelete] Delete an Hedera account.<br/>
         * This will mark the account as deleted, and transfer all remaining
         * HBAR to a receiver account.
         * @property {proto.CryptoDeleteLiveHashTransactionBody.$Properties|null} [cryptoDeleteLiveHash] An obsolete, and unsupported, operation to remove a "live hash" from
         * an account.
         * @property {proto.CryptoTransferTransactionBody.$Properties|null} [cryptoTransfer] Transfer HBAR between accounts.
         * @property {proto.CryptoUpdateTransactionBody.$Properties|null} [cryptoUpdateAccount] Modify an Hedera account.
         * @property {proto.FileAppendTransactionBody.$Properties|null} [fileAppend] Append data to the end of a file.
         * @property {proto.FileCreateTransactionBody.$Properties|null} [fileCreate] Create a new file.
         * @property {proto.FileDeleteTransactionBody.$Properties|null} [fileDelete] Delete a file.<br/>
         * This will remove the content of the file, and mark the file as
         * deleted.
         * @property {proto.FileUpdateTransactionBody.$Properties|null} [fileUpdate] Modify a file.<br/>
         * This may modify any metadata, and/or _replace_ the content.
         * @property {proto.SystemDeleteTransactionBody.$Properties|null} [systemDelete] Delete a file as an Hedera administrative function.<br/>
         * This is a privileged operation.
         * @property {proto.SystemUndeleteTransactionBody.$Properties|null} [systemUndelete] Restore a file deleted via `systemDelete`.<br/>
         * This is a privileged operation.
         * @property {proto.ContractDeleteTransactionBody.$Properties|null} [contractDeleteInstance] Delete a smart contract and transfer remaining balance
         * to a specified account.
         * @property {proto.FreezeTransactionBody.$Properties|null} [freeze] Freeze the network.<br/>
         * This is actually several possible operations, and the caller
         * should examine the "freeze service" for more detail.<br/>
         * This is a privileged operation.
         * @property {proto.ConsensusCreateTopicTransactionBody.$Properties|null} [consensusCreateTopic] Create a topic.
         * @property {proto.ConsensusUpdateTopicTransactionBody.$Properties|null} [consensusUpdateTopic] Update a topic.
         * @property {proto.ConsensusDeleteTopicTransactionBody.$Properties|null} [consensusDeleteTopic] Delete a topic.
         * @property {proto.ConsensusSubmitMessageTransactionBody.$Properties|null} [consensusSubmitMessage] Submit a message to a topic.<br/>
         * A message may be "chunked", and submitted in parts, if the total
         * message size exceeds the limit for a single transaction.
         * @property {proto.UncheckedSubmitBody.$Properties|null} [uncheckedSubmit] Unsupported system transaction.
         * <p>
         * This transaction MAY be implemented in testing networks, but
         * SHALL NOT be enabled or supported in production environments.<br/>
         * Clients MUST NOT call this method, and any such transaction SHALL
         * be rejected.<br/>
         * A network MAY choose to charge punitive fees for attempting to
         * execute an `uncheckedSubmit`.
         * @property {proto.TokenCreateTransactionBody.$Properties|null} [tokenCreation] Create a new Hedera token.
         * @property {proto.TokenFreezeAccountTransactionBody.$Properties|null} [tokenFreeze] Freeze an account with respect to a token.<br/>
         * A frozen account cannot transact in that token until unfrozen.
         * @property {proto.TokenUnfreezeAccountTransactionBody.$Properties|null} [tokenUnfreeze] Unfreeze an account with respect to a token.
         * @property {proto.TokenGrantKycTransactionBody.$Properties|null} [tokenGrantKyc] Grant KYC to an account with respect to a token.<br/>
         * KYC is generally a "know your customer" assertion that a
         * responsible entity has sufficient information to positively
         * identify the account holder to relevant authorities.
         * @property {proto.TokenRevokeKycTransactionBody.$Properties|null} [tokenRevokeKyc] Revoke KYC from an account with respect to a token.
         * @property {proto.TokenDeleteTransactionBody.$Properties|null} [tokenDeletion] Delete an Hedera token.<br/>
         * The token will be marked deleted.
         * @property {proto.TokenUpdateTransactionBody.$Properties|null} [tokenUpdate] Update an Hedera token.<br/>
         * Depending on what fields are to be modified, the signature
         * requirements will vary. See `TokenUpdateTransactionBody` for
         * further detail.
         * @property {proto.TokenMintTransactionBody.$Properties|null} [tokenMint] Mint new tokens.<br/>
         * All minted tokens will be delivered to the treasury account for
         * the token type. The "mint key" for the token must sign this
         * transaction.
         * @property {proto.TokenBurnTransactionBody.$Properties|null} [tokenBurn] Burn tokens from the treasury account.<br/>
         * The "burn key" for the token must sign this transaction.
         * @property {proto.TokenWipeAccountTransactionBody.$Properties|null} [tokenWipe] Wipe tokens from an account.<br/>
         * This will remove a specified amount of fungible/common tokens or
         * a specified list of non-fungible/unique serial numbered tokens
         * of a given token type from an Hedera account. The removed tokens
         * are _burned_ as if by a `tokenBurn` transaction.<br/>
         * The "wipe key" for the token must sign this transaction.
         * @property {proto.TokenAssociateTransactionBody.$Properties|null} [tokenAssociate] Associate tokens to an account.
         * @property {proto.TokenDissociateTransactionBody.$Properties|null} [tokenDissociate] Dissociate tokens from an account.
         * @property {proto.ScheduleCreateTransactionBody.$Properties|null} [scheduleCreate] Create a schedule.<br/>
         * A schedule is a request to execute a specific transaction, included
         * in the create body, in the future. The scheduled transaction may
         * execute as soon as all signature requirements are met with the
         * schedule create or a subsequent schedule sign transaction.
         * A schedule may, alternatively, execute on expiration if
         * long-term schedules are enabled and the schedule meets signature
         * requirements at that time.
         * @property {proto.ScheduleDeleteTransactionBody.$Properties|null} [scheduleDelete] Delete a schedule.<br/>
         * The schedule will be marked as deleted.
         * @property {proto.ScheduleSignTransactionBody.$Properties|null} [scheduleSign] Sign a schedule.<br/>
         * Add one or more cryptographic keys to the list of keys that have
         * signed a schedule, and which may serve to meet the signature
         * requirements for the scheduled transaction.
         * @property {proto.TokenFeeScheduleUpdateTransactionBody.$Properties|null} [tokenFeeScheduleUpdate] Update the custom fee schedule for a token.<br/>
         * This transaction must be signed by the "fee schedule key"
         * for the token.
         * @property {proto.TokenPauseTransactionBody.$Properties|null} [tokenPause] Pause a Token.
         * <p>
         * This transaction MUST be signed by the "pause key" for the token.
         * @property {proto.TokenUnpauseTransactionBody.$Properties|null} [tokenUnpause] Unpause a Token.
         * <p>
         * This transaction MUST be signed by the "pause key" for the token.
         * @property {proto.CryptoApproveAllowanceTransactionBody.$Properties|null} [cryptoApproveAllowance] Add one or more approved allowances for spenders to transfer the
         * paying account's hbar or tokens.
         * @property {proto.CryptoDeleteAllowanceTransactionBody.$Properties|null} [cryptoDeleteAllowance] Delete one or more approvals for spenders to transfer the
         * paying account's hbar or tokens.
         * @property {proto.EthereumTransactionBody.$Properties|null} [ethereumTransaction] Perform an Ethereum encoded transaction.
         * @property {proto.NodeStakeUpdateTransactionBody.$Properties|null} [nodeStakeUpdate] Update the staking information.<br/>
         * This internal transaction is performed at the end of a staking
         * period to complete staking calculations and indicate that new
         * staking period has started.
         * @property {proto.UtilPrngTransactionBody.$Properties|null} [utilPrng] Provide a deterministic pseudorandom number based on network state.
         * @property {proto.TokenUpdateNftsTransactionBody.$Properties|null} [tokenUpdateNfts] Update one or more non-fungible/unique tokens.<br/>
         * This will update metadata for one or more serial numbers within
         * a collection (token type).
         * @property {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Properties|null} [nodeCreate] Create a new node in the network address book.<br/>
         * This is a privileged operation.
         * <p>
         * This transaction SHALL create a new consensus node record and add
         * that record to the network address book.
         * @property {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Properties|null} [nodeUpdate] Update a node in the network address book.<br/>
         * This is a privileged operation.
         * <p>
         * This transaction SHALL update an existing consensus node record in
         * the network address book.
         * @property {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties|null} [nodeDelete] Delete a node from the network address book.<br/>
         * This is a privileged operation.
         * <p>
         * This transaction SHALL mark an existing consensus node record as
         * deleted and remove that node from the network address book.
         * @property {proto.TokenRejectTransactionBody.$Properties|null} [tokenReject] Reject and return a token to treasury.<br/>
         * This transaction will transfer one or more tokens or token
         * balances held by the requesting account to the treasury
         * for each token type.
         * <p>
         * Each transfer MUST be one of the following:
         * <ul>
         * <li>A single non-fungible/unique token.</li>
         * <li>The full balance held for a fungible/common
         * token type.</li>
         * </ul>
         * When complete, the requesting account SHALL NOT hold the
         * rejected tokens.<br/>
         * Custom fees and royalties defined for the tokens rejected
         * SHALL NOT be charged for this transaction.
         * @property {proto.TokenAirdropTransactionBody.$Properties|null} [tokenAirdrop] "Airdrop" tokens.<br/>
         * This transaction sends tokens from one or more "sender" accounts
         * to one or more "recipient" accounts.
         * <p>
         * If a recipient account cannot immediately receive the token(s) sent,
         * a "pending" airdrop SHALL be created and MUST be claimed.
         * @property {proto.TokenCancelAirdropTransactionBody.$Properties|null} [tokenCancelAirdrop] Cancel one or more "pending" airdrops that are not yet claimed.
         * @property {proto.TokenClaimAirdropTransactionBody.$Properties|null} [tokenClaimAirdrop] Claim one or more "pending" airdrops.
         * @property {com.hedera.hapi.platform.event.StateSignatureTransaction.$Properties|null} [stateSignatureTransaction] A transaction body for signature of a state root hash gossiped to other nodes
         * @property {com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Properties|null} [hintsPreprocessingVote] A transaction body for voting on hinTS aggregation keys.
         * @property {com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Properties|null} [hintsKeyPublication] A transaction body for publishing a node's hintTS key.
         * @property {com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Properties|null} [hintsPartialSignature] A transaction body for broadcasting a node's hintTS partial signature on a message.
         * @property {com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Properties|null} [historyProofSignature] A transaction body for contributed a signature with a node's proof key to a history proof.
         * @property {com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Properties|null} [historyProofKeyPublication] A transaction body for publishing a node's metadata proof key.
         * @property {com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Properties|null} [historyProofVote] A transaction body for voting on a metadata proof descending from the ledger id.
         * @property {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties|null} [crsPublication] A transaction body for broadcasting a node's crs publication
         * @property {proto.AtomicBatchTransactionBody.$Properties|null} [atomicBatch] A transaction body for handling a set of transactions atomically.
         * @property {com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Properties|null} [hookStore] A transaction body for updating the storage of an EVM hook.
         * @property {com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Properties|null} [hookDispatch] An internal-only transaction body for dispatching a hook CRUD operation.
         * @property {com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Properties|null} [ledgerIdPublication] An internal-only transaction body for publishing the ledger id.
         * @property {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Properties|null} [registeredNodeCreate] Create a new registered node in the network address book.
         * <p>
         * This transaction SHALL create a new registered node record and add
         * that record to the network state.
         * @property {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Properties|null} [registeredNodeUpdate] Update a registered node in the network address book.
         * <p>
         * This transaction SHALL update an existing registered node record in
         * the network state.
         * @property {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties|null} [registeredNodeDelete] Delete a registered node from the network address book.
         * <p>
         * This transaction SHALL mark an existing registered node record as
         * deleted.
         * @property {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties|null} [migrationRootHashVote] A transaction body for voting on migration wrapped record root hash state.
         * @property {Array.<proto.CustomFeeLimit.$Properties>|null} [maxCustomFees] A list of maximum custom fees that the users are willing to pay.
         * <p>
         * This field is OPTIONAL.<br/>
         * If left empty, the users are accepting to pay any custom fee.<br/>
         * If used with a transaction type that does not support custom fee limits, the transaction will fail.
         * @property {boolean|null} [highVolume] If set to true, this transaction uses high-volume throttles and pricing
         * for entity creation. It only affects supported transaction types; otherwise,
         * it is ignored.
         * @property {"contractCall"|"contractCreateInstance"|"contractUpdateInstance"|"cryptoAddLiveHash"|"cryptoCreateAccount"|"cryptoDelete"|"cryptoDeleteLiveHash"|"cryptoTransfer"|"cryptoUpdateAccount"|"fileAppend"|"fileCreate"|"fileDelete"|"fileUpdate"|"systemDelete"|"systemUndelete"|"contractDeleteInstance"|"freeze"|"consensusCreateTopic"|"consensusUpdateTopic"|"consensusDeleteTopic"|"consensusSubmitMessage"|"uncheckedSubmit"|"tokenCreation"|"tokenFreeze"|"tokenUnfreeze"|"tokenGrantKyc"|"tokenRevokeKyc"|"tokenDeletion"|"tokenUpdate"|"tokenMint"|"tokenBurn"|"tokenWipe"|"tokenAssociate"|"tokenDissociate"|"scheduleCreate"|"scheduleDelete"|"scheduleSign"|"tokenFeeScheduleUpdate"|"tokenPause"|"tokenUnpause"|"cryptoApproveAllowance"|"cryptoDeleteAllowance"|"ethereumTransaction"|"nodeStakeUpdate"|"utilPrng"|"tokenUpdateNfts"|"nodeCreate"|"nodeUpdate"|"nodeDelete"|"tokenReject"|"tokenAirdrop"|"tokenCancelAirdrop"|"tokenClaimAirdrop"|"stateSignatureTransaction"|"hintsPreprocessingVote"|"hintsKeyPublication"|"hintsPartialSignature"|"historyProofSignature"|"historyProofKeyPublication"|"historyProofVote"|"crsPublication"|"atomicBatch"|"hookStore"|"hookDispatch"|"ledgerIdPublication"|"registeredNodeCreate"|"registeredNodeUpdate"|"registeredNodeDelete"|"migrationRootHashVote"} [data] TransactionBody data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionBody.
         * @memberof proto
         * @interface ITransactionBody
         * @augments proto.TransactionBody.$Properties
         * @deprecated Use proto.TransactionBody.$Properties instead.
         */

        /**
         * Narrowed shape of a TransactionBody.
         * @typedef {{
         *   transactionID?: proto.TransactionID.$Shape|null;
         *   nodeAccountID?: proto.AccountID.$Shape|null;
         *   transactionFee?: number|Long|null;
         *   transactionValidDuration?: proto.Duration.$Shape|null;
         *   generateRecord?: boolean|null;
         *   memo?: string|null;
         *   batchKey?: proto.Key.$Shape|null;
         *   contractCall?: proto.ContractCallTransactionBody.$Shape|null;
         *   contractCreateInstance?: proto.ContractCreateTransactionBody.$Shape|null;
         *   contractUpdateInstance?: proto.ContractUpdateTransactionBody.$Shape|null;
         *   cryptoAddLiveHash?: proto.CryptoAddLiveHashTransactionBody.$Shape|null;
         *   cryptoCreateAccount?: proto.CryptoCreateTransactionBody.$Shape|null;
         *   cryptoDelete?: proto.CryptoDeleteTransactionBody.$Shape|null;
         *   cryptoDeleteLiveHash?: proto.CryptoDeleteLiveHashTransactionBody.$Shape|null;
         *   cryptoTransfer?: proto.CryptoTransferTransactionBody.$Shape|null;
         *   cryptoUpdateAccount?: proto.CryptoUpdateTransactionBody.$Shape|null;
         *   fileAppend?: proto.FileAppendTransactionBody.$Shape|null;
         *   fileCreate?: proto.FileCreateTransactionBody.$Shape|null;
         *   fileDelete?: proto.FileDeleteTransactionBody.$Shape|null;
         *   fileUpdate?: proto.FileUpdateTransactionBody.$Shape|null;
         *   systemDelete?: proto.SystemDeleteTransactionBody.$Shape|null;
         *   systemUndelete?: proto.SystemUndeleteTransactionBody.$Shape|null;
         *   contractDeleteInstance?: proto.ContractDeleteTransactionBody.$Shape|null;
         *   freeze?: proto.FreezeTransactionBody.$Shape|null;
         *   consensusCreateTopic?: proto.ConsensusCreateTopicTransactionBody.$Shape|null;
         *   consensusUpdateTopic?: proto.ConsensusUpdateTopicTransactionBody.$Shape|null;
         *   consensusDeleteTopic?: proto.ConsensusDeleteTopicTransactionBody.$Shape|null;
         *   consensusSubmitMessage?: proto.ConsensusSubmitMessageTransactionBody.$Shape|null;
         *   uncheckedSubmit?: proto.UncheckedSubmitBody.$Shape|null;
         *   tokenCreation?: proto.TokenCreateTransactionBody.$Shape|null;
         *   tokenFreeze?: proto.TokenFreezeAccountTransactionBody.$Shape|null;
         *   tokenUnfreeze?: proto.TokenUnfreezeAccountTransactionBody.$Shape|null;
         *   tokenGrantKyc?: proto.TokenGrantKycTransactionBody.$Shape|null;
         *   tokenRevokeKyc?: proto.TokenRevokeKycTransactionBody.$Shape|null;
         *   tokenDeletion?: proto.TokenDeleteTransactionBody.$Shape|null;
         *   tokenUpdate?: proto.TokenUpdateTransactionBody.$Shape|null;
         *   tokenMint?: proto.TokenMintTransactionBody.$Shape|null;
         *   tokenBurn?: proto.TokenBurnTransactionBody.$Shape|null;
         *   tokenWipe?: proto.TokenWipeAccountTransactionBody.$Shape|null;
         *   tokenAssociate?: proto.TokenAssociateTransactionBody.$Shape|null;
         *   tokenDissociate?: proto.TokenDissociateTransactionBody.$Shape|null;
         *   scheduleCreate?: proto.ScheduleCreateTransactionBody.$Shape|null;
         *   scheduleDelete?: proto.ScheduleDeleteTransactionBody.$Shape|null;
         *   scheduleSign?: proto.ScheduleSignTransactionBody.$Shape|null;
         *   tokenFeeScheduleUpdate?: proto.TokenFeeScheduleUpdateTransactionBody.$Shape|null;
         *   tokenPause?: proto.TokenPauseTransactionBody.$Shape|null;
         *   tokenUnpause?: proto.TokenUnpauseTransactionBody.$Shape|null;
         *   cryptoApproveAllowance?: proto.CryptoApproveAllowanceTransactionBody.$Shape|null;
         *   cryptoDeleteAllowance?: proto.CryptoDeleteAllowanceTransactionBody.$Shape|null;
         *   ethereumTransaction?: proto.EthereumTransactionBody.$Shape|null;
         *   nodeStakeUpdate?: proto.NodeStakeUpdateTransactionBody.$Shape|null;
         *   utilPrng?: proto.UtilPrngTransactionBody.$Shape|null;
         *   tokenUpdateNfts?: proto.TokenUpdateNftsTransactionBody.$Shape|null;
         *   nodeCreate?: com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Shape|null;
         *   nodeUpdate?: com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Shape|null;
         *   nodeDelete?: com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Shape|null;
         *   tokenReject?: proto.TokenRejectTransactionBody.$Shape|null;
         *   tokenAirdrop?: proto.TokenAirdropTransactionBody.$Shape|null;
         *   tokenCancelAirdrop?: proto.TokenCancelAirdropTransactionBody.$Shape|null;
         *   tokenClaimAirdrop?: proto.TokenClaimAirdropTransactionBody.$Shape|null;
         *   stateSignatureTransaction?: com.hedera.hapi.platform.event.StateSignatureTransaction.$Shape|null;
         *   hintsPreprocessingVote?: com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Shape|null;
         *   hintsKeyPublication?: com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Shape|null;
         *   hintsPartialSignature?: com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Shape|null;
         *   historyProofSignature?: com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Shape|null;
         *   historyProofKeyPublication?: com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Shape|null;
         *   historyProofVote?: com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Shape|null;
         *   crsPublication?: com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Shape|null;
         *   atomicBatch?: proto.AtomicBatchTransactionBody.$Shape|null;
         *   hookStore?: com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Shape|null;
         *   hookDispatch?: com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Shape|null;
         *   ledgerIdPublication?: com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Shape|null;
         *   registeredNodeCreate?: com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Shape|null;
         *   registeredNodeUpdate?: com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Shape|null;
         *   registeredNodeDelete?: com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Shape|null;
         *   migrationRootHashVote?: com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Shape|null;
         *   maxCustomFees?: Array.<proto.CustomFeeLimit.$Shape>|null;
         *   highVolume?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ data?: undefined; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "contractCall"; contractCall: proto.ContractCallTransactionBody.$Shape; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "contractCreateInstance"; contractCall?: null; contractCreateInstance: proto.ContractCreateTransactionBody.$Shape; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "contractUpdateInstance"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance: proto.ContractUpdateTransactionBody.$Shape; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "cryptoAddLiveHash"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash: proto.CryptoAddLiveHashTransactionBody.$Shape; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "cryptoCreateAccount"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount: proto.CryptoCreateTransactionBody.$Shape; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "cryptoDelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete: proto.CryptoDeleteTransactionBody.$Shape; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "cryptoDeleteLiveHash"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash: proto.CryptoDeleteLiveHashTransactionBody.$Shape; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "cryptoTransfer"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer: proto.CryptoTransferTransactionBody.$Shape; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "cryptoUpdateAccount"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount: proto.CryptoUpdateTransactionBody.$Shape; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "fileAppend"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend: proto.FileAppendTransactionBody.$Shape; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "fileCreate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate: proto.FileCreateTransactionBody.$Shape; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "fileDelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete: proto.FileDeleteTransactionBody.$Shape; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "fileUpdate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate: proto.FileUpdateTransactionBody.$Shape; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "systemDelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete: proto.SystemDeleteTransactionBody.$Shape; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "systemUndelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete: proto.SystemUndeleteTransactionBody.$Shape; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "contractDeleteInstance"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance: proto.ContractDeleteTransactionBody.$Shape; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "freeze"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze: proto.FreezeTransactionBody.$Shape; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "consensusCreateTopic"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic: proto.ConsensusCreateTopicTransactionBody.$Shape; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "consensusUpdateTopic"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic: proto.ConsensusUpdateTopicTransactionBody.$Shape; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "consensusDeleteTopic"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic: proto.ConsensusDeleteTopicTransactionBody.$Shape; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "consensusSubmitMessage"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage: proto.ConsensusSubmitMessageTransactionBody.$Shape; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "uncheckedSubmit"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit: proto.UncheckedSubmitBody.$Shape; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenCreation"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation: proto.TokenCreateTransactionBody.$Shape; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenFreeze"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze: proto.TokenFreezeAccountTransactionBody.$Shape; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenUnfreeze"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze: proto.TokenUnfreezeAccountTransactionBody.$Shape; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenGrantKyc"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc: proto.TokenGrantKycTransactionBody.$Shape; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenRevokeKyc"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc: proto.TokenRevokeKycTransactionBody.$Shape; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenDeletion"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion: proto.TokenDeleteTransactionBody.$Shape; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenUpdate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate: proto.TokenUpdateTransactionBody.$Shape; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenMint"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint: proto.TokenMintTransactionBody.$Shape; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenBurn"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn: proto.TokenBurnTransactionBody.$Shape; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenWipe"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe: proto.TokenWipeAccountTransactionBody.$Shape; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenAssociate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate: proto.TokenAssociateTransactionBody.$Shape; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenDissociate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate: proto.TokenDissociateTransactionBody.$Shape; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "scheduleCreate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate: proto.ScheduleCreateTransactionBody.$Shape; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "scheduleDelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete: proto.ScheduleDeleteTransactionBody.$Shape; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "scheduleSign"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign: proto.ScheduleSignTransactionBody.$Shape; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenFeeScheduleUpdate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate: proto.TokenFeeScheduleUpdateTransactionBody.$Shape; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenPause"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause: proto.TokenPauseTransactionBody.$Shape; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenUnpause"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause: proto.TokenUnpauseTransactionBody.$Shape; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "cryptoApproveAllowance"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance: proto.CryptoApproveAllowanceTransactionBody.$Shape; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "cryptoDeleteAllowance"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance: proto.CryptoDeleteAllowanceTransactionBody.$Shape; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "ethereumTransaction"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction: proto.EthereumTransactionBody.$Shape; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "nodeStakeUpdate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate: proto.NodeStakeUpdateTransactionBody.$Shape; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "utilPrng"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng: proto.UtilPrngTransactionBody.$Shape; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenUpdateNfts"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts: proto.TokenUpdateNftsTransactionBody.$Shape; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "nodeCreate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate: com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Shape; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "nodeUpdate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate: com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Shape; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "nodeDelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete: com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Shape; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenReject"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject: proto.TokenRejectTransactionBody.$Shape; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenAirdrop"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop: proto.TokenAirdropTransactionBody.$Shape; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenCancelAirdrop"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop: proto.TokenCancelAirdropTransactionBody.$Shape; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "tokenClaimAirdrop"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop: proto.TokenClaimAirdropTransactionBody.$Shape; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "stateSignatureTransaction"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction: com.hedera.hapi.platform.event.StateSignatureTransaction.$Shape; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "hintsPreprocessingVote"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote: com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Shape; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "hintsKeyPublication"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication: com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Shape; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "hintsPartialSignature"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature: com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Shape; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "historyProofSignature"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature: com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Shape; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "historyProofKeyPublication"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication: com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Shape; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "historyProofVote"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote: com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Shape; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "crsPublication"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication: com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Shape; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "atomicBatch"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch: proto.AtomicBatchTransactionBody.$Shape; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "hookStore"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore: com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Shape; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "hookDispatch"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch: com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Shape; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "ledgerIdPublication"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication: com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Shape; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "registeredNodeCreate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate: com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Shape; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "registeredNodeUpdate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate: com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Shape; registeredNodeDelete?: null; migrationRootHashVote?: null }|{ data?: "registeredNodeDelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete: com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Shape; migrationRootHashVote?: null }|{ data?: "migrationRootHashVote"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; cryptoAddLiveHash?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoDeleteLiveHash?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; contractDeleteInstance?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; uncheckedSubmit?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleCreate?: null; scheduleDelete?: null; scheduleSign?: null; tokenFeeScheduleUpdate?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; ethereumTransaction?: null; nodeStakeUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenAirdrop?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; stateSignatureTransaction?: null; hintsPreprocessingVote?: null; hintsKeyPublication?: null; hintsPartialSignature?: null; historyProofSignature?: null; historyProofKeyPublication?: null; historyProofVote?: null; crsPublication?: null; atomicBatch?: null; hookStore?: null; hookDispatch?: null; ledgerIdPublication?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null; migrationRootHashVote: com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Shape })
         * )} proto.TransactionBody.$Shape
         */

        /**
         * Constructs a new TransactionBody.
         * @memberof proto
         * @classdesc A transaction body.
         * 
         * Every transaction is structured as a signed byte array. That byte array
         * is a serialized `TransactionBody`.  The transaction body contains the full
         * content of the transaction, while the `SignedTransaction` includes a
         * signature map for signatures authenticating that byte array, and that is
         * serialized and transmitted wrapped in a `Transaction` message.<br/>
         * The bulk of this message is a `oneof` block which offers the option for
         * any one of the transaction messages for the network.
         * This message also includes several additional fields to specify
         * various parameters required to process a transaction.
         * @constructor
         * @param {proto.TransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionBody = function (properties) {
            this.maxCustomFees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A transaction identifier.<br/>
         * Each transaction is uniquely identified by its transaction
         * identifier.
         * <p>
         * Each transaction identifier MUST be unique.<br/>
         * Multiple transactions MAY be submitted with the same transaction
         * identifier, but all except the first SHALL be rejected as duplicate
         * transactions.<br/>
         * This identifier MUST specify a `payer` account to be charged
         * all fees associated with the transaction.<br/>
         * This identifier MUST specify a "valid start time".<br/>
         * The "valid start time" MUST be strictly _earlier_ than the current
         * network consensus time.<br/>
         * The "valid start time" MUST NOT be more than the current network
         * configuration value for `transaction.maxValidDuration` seconds
         * before the current network consensus time.<br/>
         * This identifier MUST NOT set the `scheduled` flag.<br/>
         * This identifier MUST NOT set a nonce value.
         * @member {proto.TransactionID.$Properties|null|undefined} transactionID
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.transactionID = null;

        /**
         * A node account identifier.
         * <p>
         * This MUST identify the account of the consensus node to which
         * this transaction is submitted.
         * @member {proto.AccountID.$Properties|null|undefined} nodeAccountID
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.nodeAccountID = null;

        /**
         * A maximum transaction fee, in tinybar.
         * <p>
         * The network SHALL NOT charge a transaction fee that exceeds this
         * amount.<br/>
         * The network MAY charge up to this amount, and reject the transaction,
         * if the amount offered is insufficient to cover the required fees.<br/>
         * The network MAY charge a minimum fee equal to 80% of the amount offered
         * if the amount offered is much larger than the required fees.
         * @member {number|Long} transactionFee
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.transactionFee = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A maximum duration in which to execute this transaction.
         * <p>
         * This transaction SHALL be rejected as expired if the valid start time,
         * extended by this duration, is less than the current network consensus
         * time when the transaction is submitted.<br/>
         * This transaction SHALL be rejected with an invalid duration if this
         * value is greater than the current network configuration value for
         * `transaction.maxValidDuration`.
         * @member {proto.Duration.$Properties|null|undefined} transactionValidDuration
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.transactionValidDuration = null;

        /**
         * Records are always generated.<br/>
         * Obsolete option to not generate a record.
         * <p>
         * This flag SHALL be ignored. Every transaction SHALL generate a record,
         * or block stream equivalent.
         * @member {boolean} generateRecord
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.generateRecord = false;

        /**
         * A short description for this transaction.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.memo = "";

        /**
         * The <b>public key</b> of the trusted batch assembler.
         * @member {proto.Key.$Properties|null|undefined} batchKey
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.batchKey = null;

        /**
         * Call a function defined on a smart contract.
         * @member {proto.ContractCallTransactionBody.$Properties|null|undefined} contractCall
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.contractCall = null;

        /**
         * Create a smart contract.
         * @member {proto.ContractCreateTransactionBody.$Properties|null|undefined} contractCreateInstance
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.contractCreateInstance = null;

        /**
         * Update a smart contract.
         * @member {proto.ContractUpdateTransactionBody.$Properties|null|undefined} contractUpdateInstance
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.contractUpdateInstance = null;

        /**
         * An obsolete, and unsupported, operation to add a "live hash" to
         * an account.
         * @member {proto.CryptoAddLiveHashTransactionBody.$Properties|null|undefined} cryptoAddLiveHash
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.cryptoAddLiveHash = null;

        /**
         * Create a new Hedera account.
         * @member {proto.CryptoCreateTransactionBody.$Properties|null|undefined} cryptoCreateAccount
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.cryptoCreateAccount = null;

        /**
         * Delete an Hedera account.<br/>
         * This will mark the account as deleted, and transfer all remaining
         * HBAR to a receiver account.
         * @member {proto.CryptoDeleteTransactionBody.$Properties|null|undefined} cryptoDelete
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.cryptoDelete = null;

        /**
         * An obsolete, and unsupported, operation to remove a "live hash" from
         * an account.
         * @member {proto.CryptoDeleteLiveHashTransactionBody.$Properties|null|undefined} cryptoDeleteLiveHash
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.cryptoDeleteLiveHash = null;

        /**
         * Transfer HBAR between accounts.
         * @member {proto.CryptoTransferTransactionBody.$Properties|null|undefined} cryptoTransfer
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.cryptoTransfer = null;

        /**
         * Modify an Hedera account.
         * @member {proto.CryptoUpdateTransactionBody.$Properties|null|undefined} cryptoUpdateAccount
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.cryptoUpdateAccount = null;

        /**
         * Append data to the end of a file.
         * @member {proto.FileAppendTransactionBody.$Properties|null|undefined} fileAppend
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.fileAppend = null;

        /**
         * Create a new file.
         * @member {proto.FileCreateTransactionBody.$Properties|null|undefined} fileCreate
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.fileCreate = null;

        /**
         * Delete a file.<br/>
         * This will remove the content of the file, and mark the file as
         * deleted.
         * @member {proto.FileDeleteTransactionBody.$Properties|null|undefined} fileDelete
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.fileDelete = null;

        /**
         * Modify a file.<br/>
         * This may modify any metadata, and/or _replace_ the content.
         * @member {proto.FileUpdateTransactionBody.$Properties|null|undefined} fileUpdate
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.fileUpdate = null;

        /**
         * Delete a file as an Hedera administrative function.<br/>
         * This is a privileged operation.
         * @member {proto.SystemDeleteTransactionBody.$Properties|null|undefined} systemDelete
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.systemDelete = null;

        /**
         * Restore a file deleted via `systemDelete`.<br/>
         * This is a privileged operation.
         * @member {proto.SystemUndeleteTransactionBody.$Properties|null|undefined} systemUndelete
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.systemUndelete = null;

        /**
         * Delete a smart contract and transfer remaining balance
         * to a specified account.
         * @member {proto.ContractDeleteTransactionBody.$Properties|null|undefined} contractDeleteInstance
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.contractDeleteInstance = null;

        /**
         * Freeze the network.<br/>
         * This is actually several possible operations, and the caller
         * should examine the "freeze service" for more detail.<br/>
         * This is a privileged operation.
         * @member {proto.FreezeTransactionBody.$Properties|null|undefined} freeze
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.freeze = null;

        /**
         * Create a topic.
         * @member {proto.ConsensusCreateTopicTransactionBody.$Properties|null|undefined} consensusCreateTopic
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.consensusCreateTopic = null;

        /**
         * Update a topic.
         * @member {proto.ConsensusUpdateTopicTransactionBody.$Properties|null|undefined} consensusUpdateTopic
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.consensusUpdateTopic = null;

        /**
         * Delete a topic.
         * @member {proto.ConsensusDeleteTopicTransactionBody.$Properties|null|undefined} consensusDeleteTopic
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.consensusDeleteTopic = null;

        /**
         * Submit a message to a topic.<br/>
         * A message may be "chunked", and submitted in parts, if the total
         * message size exceeds the limit for a single transaction.
         * @member {proto.ConsensusSubmitMessageTransactionBody.$Properties|null|undefined} consensusSubmitMessage
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.consensusSubmitMessage = null;

        /**
         * Unsupported system transaction.
         * <p>
         * This transaction MAY be implemented in testing networks, but
         * SHALL NOT be enabled or supported in production environments.<br/>
         * Clients MUST NOT call this method, and any such transaction SHALL
         * be rejected.<br/>
         * A network MAY choose to charge punitive fees for attempting to
         * execute an `uncheckedSubmit`.
         * @member {proto.UncheckedSubmitBody.$Properties|null|undefined} uncheckedSubmit
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.uncheckedSubmit = null;

        /**
         * Create a new Hedera token.
         * @member {proto.TokenCreateTransactionBody.$Properties|null|undefined} tokenCreation
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenCreation = null;

        /**
         * Freeze an account with respect to a token.<br/>
         * A frozen account cannot transact in that token until unfrozen.
         * @member {proto.TokenFreezeAccountTransactionBody.$Properties|null|undefined} tokenFreeze
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenFreeze = null;

        /**
         * Unfreeze an account with respect to a token.
         * @member {proto.TokenUnfreezeAccountTransactionBody.$Properties|null|undefined} tokenUnfreeze
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenUnfreeze = null;

        /**
         * Grant KYC to an account with respect to a token.<br/>
         * KYC is generally a "know your customer" assertion that a
         * responsible entity has sufficient information to positively
         * identify the account holder to relevant authorities.
         * @member {proto.TokenGrantKycTransactionBody.$Properties|null|undefined} tokenGrantKyc
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenGrantKyc = null;

        /**
         * Revoke KYC from an account with respect to a token.
         * @member {proto.TokenRevokeKycTransactionBody.$Properties|null|undefined} tokenRevokeKyc
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenRevokeKyc = null;

        /**
         * Delete an Hedera token.<br/>
         * The token will be marked deleted.
         * @member {proto.TokenDeleteTransactionBody.$Properties|null|undefined} tokenDeletion
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenDeletion = null;

        /**
         * Update an Hedera token.<br/>
         * Depending on what fields are to be modified, the signature
         * requirements will vary. See `TokenUpdateTransactionBody` for
         * further detail.
         * @member {proto.TokenUpdateTransactionBody.$Properties|null|undefined} tokenUpdate
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenUpdate = null;

        /**
         * Mint new tokens.<br/>
         * All minted tokens will be delivered to the treasury account for
         * the token type. The "mint key" for the token must sign this
         * transaction.
         * @member {proto.TokenMintTransactionBody.$Properties|null|undefined} tokenMint
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenMint = null;

        /**
         * Burn tokens from the treasury account.<br/>
         * The "burn key" for the token must sign this transaction.
         * @member {proto.TokenBurnTransactionBody.$Properties|null|undefined} tokenBurn
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenBurn = null;

        /**
         * Wipe tokens from an account.<br/>
         * This will remove a specified amount of fungible/common tokens or
         * a specified list of non-fungible/unique serial numbered tokens
         * of a given token type from an Hedera account. The removed tokens
         * are _burned_ as if by a `tokenBurn` transaction.<br/>
         * The "wipe key" for the token must sign this transaction.
         * @member {proto.TokenWipeAccountTransactionBody.$Properties|null|undefined} tokenWipe
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenWipe = null;

        /**
         * Associate tokens to an account.
         * @member {proto.TokenAssociateTransactionBody.$Properties|null|undefined} tokenAssociate
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenAssociate = null;

        /**
         * Dissociate tokens from an account.
         * @member {proto.TokenDissociateTransactionBody.$Properties|null|undefined} tokenDissociate
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenDissociate = null;

        /**
         * Create a schedule.<br/>
         * A schedule is a request to execute a specific transaction, included
         * in the create body, in the future. The scheduled transaction may
         * execute as soon as all signature requirements are met with the
         * schedule create or a subsequent schedule sign transaction.
         * A schedule may, alternatively, execute on expiration if
         * long-term schedules are enabled and the schedule meets signature
         * requirements at that time.
         * @member {proto.ScheduleCreateTransactionBody.$Properties|null|undefined} scheduleCreate
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.scheduleCreate = null;

        /**
         * Delete a schedule.<br/>
         * The schedule will be marked as deleted.
         * @member {proto.ScheduleDeleteTransactionBody.$Properties|null|undefined} scheduleDelete
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.scheduleDelete = null;

        /**
         * Sign a schedule.<br/>
         * Add one or more cryptographic keys to the list of keys that have
         * signed a schedule, and which may serve to meet the signature
         * requirements for the scheduled transaction.
         * @member {proto.ScheduleSignTransactionBody.$Properties|null|undefined} scheduleSign
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.scheduleSign = null;

        /**
         * Update the custom fee schedule for a token.<br/>
         * This transaction must be signed by the "fee schedule key"
         * for the token.
         * @member {proto.TokenFeeScheduleUpdateTransactionBody.$Properties|null|undefined} tokenFeeScheduleUpdate
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenFeeScheduleUpdate = null;

        /**
         * Pause a Token.
         * <p>
         * This transaction MUST be signed by the "pause key" for the token.
         * @member {proto.TokenPauseTransactionBody.$Properties|null|undefined} tokenPause
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenPause = null;

        /**
         * Unpause a Token.
         * <p>
         * This transaction MUST be signed by the "pause key" for the token.
         * @member {proto.TokenUnpauseTransactionBody.$Properties|null|undefined} tokenUnpause
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenUnpause = null;

        /**
         * Add one or more approved allowances for spenders to transfer the
         * paying account's hbar or tokens.
         * @member {proto.CryptoApproveAllowanceTransactionBody.$Properties|null|undefined} cryptoApproveAllowance
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.cryptoApproveAllowance = null;

        /**
         * Delete one or more approvals for spenders to transfer the
         * paying account's hbar or tokens.
         * @member {proto.CryptoDeleteAllowanceTransactionBody.$Properties|null|undefined} cryptoDeleteAllowance
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.cryptoDeleteAllowance = null;

        /**
         * Perform an Ethereum encoded transaction.
         * @member {proto.EthereumTransactionBody.$Properties|null|undefined} ethereumTransaction
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.ethereumTransaction = null;

        /**
         * Update the staking information.<br/>
         * This internal transaction is performed at the end of a staking
         * period to complete staking calculations and indicate that new
         * staking period has started.
         * @member {proto.NodeStakeUpdateTransactionBody.$Properties|null|undefined} nodeStakeUpdate
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.nodeStakeUpdate = null;

        /**
         * Provide a deterministic pseudorandom number based on network state.
         * @member {proto.UtilPrngTransactionBody.$Properties|null|undefined} utilPrng
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.utilPrng = null;

        /**
         * Update one or more non-fungible/unique tokens.<br/>
         * This will update metadata for one or more serial numbers within
         * a collection (token type).
         * @member {proto.TokenUpdateNftsTransactionBody.$Properties|null|undefined} tokenUpdateNfts
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenUpdateNfts = null;

        /**
         * Create a new node in the network address book.<br/>
         * This is a privileged operation.
         * <p>
         * This transaction SHALL create a new consensus node record and add
         * that record to the network address book.
         * @member {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Properties|null|undefined} nodeCreate
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.nodeCreate = null;

        /**
         * Update a node in the network address book.<br/>
         * This is a privileged operation.
         * <p>
         * This transaction SHALL update an existing consensus node record in
         * the network address book.
         * @member {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Properties|null|undefined} nodeUpdate
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.nodeUpdate = null;

        /**
         * Delete a node from the network address book.<br/>
         * This is a privileged operation.
         * <p>
         * This transaction SHALL mark an existing consensus node record as
         * deleted and remove that node from the network address book.
         * @member {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties|null|undefined} nodeDelete
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.nodeDelete = null;

        /**
         * Reject and return a token to treasury.<br/>
         * This transaction will transfer one or more tokens or token
         * balances held by the requesting account to the treasury
         * for each token type.
         * <p>
         * Each transfer MUST be one of the following:
         * <ul>
         * <li>A single non-fungible/unique token.</li>
         * <li>The full balance held for a fungible/common
         * token type.</li>
         * </ul>
         * When complete, the requesting account SHALL NOT hold the
         * rejected tokens.<br/>
         * Custom fees and royalties defined for the tokens rejected
         * SHALL NOT be charged for this transaction.
         * @member {proto.TokenRejectTransactionBody.$Properties|null|undefined} tokenReject
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenReject = null;

        /**
         * "Airdrop" tokens.<br/>
         * This transaction sends tokens from one or more "sender" accounts
         * to one or more "recipient" accounts.
         * <p>
         * If a recipient account cannot immediately receive the token(s) sent,
         * a "pending" airdrop SHALL be created and MUST be claimed.
         * @member {proto.TokenAirdropTransactionBody.$Properties|null|undefined} tokenAirdrop
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenAirdrop = null;

        /**
         * Cancel one or more "pending" airdrops that are not yet claimed.
         * @member {proto.TokenCancelAirdropTransactionBody.$Properties|null|undefined} tokenCancelAirdrop
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenCancelAirdrop = null;

        /**
         * Claim one or more "pending" airdrops.
         * @member {proto.TokenClaimAirdropTransactionBody.$Properties|null|undefined} tokenClaimAirdrop
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.tokenClaimAirdrop = null;

        /**
         * A transaction body for signature of a state root hash gossiped to other nodes
         * @member {com.hedera.hapi.platform.event.StateSignatureTransaction.$Properties|null|undefined} stateSignatureTransaction
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.stateSignatureTransaction = null;

        /**
         * A transaction body for voting on hinTS aggregation keys.
         * @member {com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.$Properties|null|undefined} hintsPreprocessingVote
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.hintsPreprocessingVote = null;

        /**
         * A transaction body for publishing a node's hintTS key.
         * @member {com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.$Properties|null|undefined} hintsKeyPublication
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.hintsKeyPublication = null;

        /**
         * A transaction body for broadcasting a node's hintTS partial signature on a message.
         * @member {com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.$Properties|null|undefined} hintsPartialSignature
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.hintsPartialSignature = null;

        /**
         * A transaction body for contributed a signature with a node's proof key to a history proof.
         * @member {com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.$Properties|null|undefined} historyProofSignature
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.historyProofSignature = null;

        /**
         * A transaction body for publishing a node's metadata proof key.
         * @member {com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.$Properties|null|undefined} historyProofKeyPublication
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.historyProofKeyPublication = null;

        /**
         * A transaction body for voting on a metadata proof descending from the ledger id.
         * @member {com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.$Properties|null|undefined} historyProofVote
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.historyProofVote = null;

        /**
         * A transaction body for broadcasting a node's crs publication
         * @member {com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.$Properties|null|undefined} crsPublication
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.crsPublication = null;

        /**
         * A transaction body for handling a set of transactions atomically.
         * @member {proto.AtomicBatchTransactionBody.$Properties|null|undefined} atomicBatch
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.atomicBatch = null;

        /**
         * A transaction body for updating the storage of an EVM hook.
         * @member {com.hedera.hapi.node.hooks.HookStoreTransactionBody.$Properties|null|undefined} hookStore
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.hookStore = null;

        /**
         * An internal-only transaction body for dispatching a hook CRUD operation.
         * @member {com.hedera.hapi.node.hooks.HookDispatchTransactionBody.$Properties|null|undefined} hookDispatch
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.hookDispatch = null;

        /**
         * An internal-only transaction body for publishing the ledger id.
         * @member {com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.$Properties|null|undefined} ledgerIdPublication
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.ledgerIdPublication = null;

        /**
         * Create a new registered node in the network address book.
         * <p>
         * This transaction SHALL create a new registered node record and add
         * that record to the network state.
         * @member {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Properties|null|undefined} registeredNodeCreate
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.registeredNodeCreate = null;

        /**
         * Update a registered node in the network address book.
         * <p>
         * This transaction SHALL update an existing registered node record in
         * the network state.
         * @member {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Properties|null|undefined} registeredNodeUpdate
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.registeredNodeUpdate = null;

        /**
         * Delete a registered node from the network address book.
         * <p>
         * This transaction SHALL mark an existing registered node record as
         * deleted.
         * @member {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties|null|undefined} registeredNodeDelete
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.registeredNodeDelete = null;

        /**
         * A transaction body for voting on migration wrapped record root hash state.
         * @member {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties|null|undefined} migrationRootHashVote
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.migrationRootHashVote = null;

        /**
         * A list of maximum custom fees that the users are willing to pay.
         * <p>
         * This field is OPTIONAL.<br/>
         * If left empty, the users are accepting to pay any custom fee.<br/>
         * If used with a transaction type that does not support custom fee limits, the transaction will fail.
         * @member {Array.<proto.CustomFeeLimit.$Properties>} maxCustomFees
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.maxCustomFees = $util.emptyArray;

        /**
         * If set to true, this transaction uses high-volume throttles and pricing
         * for entity creation. It only affects supported transaction types; otherwise,
         * it is ignored.
         * @member {boolean} highVolume
         * @memberof proto.TransactionBody
         * @instance
         */
        TransactionBody.prototype.highVolume = false;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * TransactionBody data.
         * @member {"contractCall"|"contractCreateInstance"|"contractUpdateInstance"|"cryptoAddLiveHash"|"cryptoCreateAccount"|"cryptoDelete"|"cryptoDeleteLiveHash"|"cryptoTransfer"|"cryptoUpdateAccount"|"fileAppend"|"fileCreate"|"fileDelete"|"fileUpdate"|"systemDelete"|"systemUndelete"|"contractDeleteInstance"|"freeze"|"consensusCreateTopic"|"consensusUpdateTopic"|"consensusDeleteTopic"|"consensusSubmitMessage"|"uncheckedSubmit"|"tokenCreation"|"tokenFreeze"|"tokenUnfreeze"|"tokenGrantKyc"|"tokenRevokeKyc"|"tokenDeletion"|"tokenUpdate"|"tokenMint"|"tokenBurn"|"tokenWipe"|"tokenAssociate"|"tokenDissociate"|"scheduleCreate"|"scheduleDelete"|"scheduleSign"|"tokenFeeScheduleUpdate"|"tokenPause"|"tokenUnpause"|"cryptoApproveAllowance"|"cryptoDeleteAllowance"|"ethereumTransaction"|"nodeStakeUpdate"|"utilPrng"|"tokenUpdateNfts"|"nodeCreate"|"nodeUpdate"|"nodeDelete"|"tokenReject"|"tokenAirdrop"|"tokenCancelAirdrop"|"tokenClaimAirdrop"|"stateSignatureTransaction"|"hintsPreprocessingVote"|"hintsKeyPublication"|"hintsPartialSignature"|"historyProofSignature"|"historyProofKeyPublication"|"historyProofVote"|"crsPublication"|"atomicBatch"|"hookStore"|"hookDispatch"|"ledgerIdPublication"|"registeredNodeCreate"|"registeredNodeUpdate"|"registeredNodeDelete"|"migrationRootHashVote"|undefined} data
         * @memberof proto.TransactionBody
         * @instance
         */
        $Object.defineProperty(TransactionBody.prototype, "data", {
            get: $util.oneOfGetter($oneOfFields = ["contractCall", "contractCreateInstance", "contractUpdateInstance", "cryptoAddLiveHash", "cryptoCreateAccount", "cryptoDelete", "cryptoDeleteLiveHash", "cryptoTransfer", "cryptoUpdateAccount", "fileAppend", "fileCreate", "fileDelete", "fileUpdate", "systemDelete", "systemUndelete", "contractDeleteInstance", "freeze", "consensusCreateTopic", "consensusUpdateTopic", "consensusDeleteTopic", "consensusSubmitMessage", "uncheckedSubmit", "tokenCreation", "tokenFreeze", "tokenUnfreeze", "tokenGrantKyc", "tokenRevokeKyc", "tokenDeletion", "tokenUpdate", "tokenMint", "tokenBurn", "tokenWipe", "tokenAssociate", "tokenDissociate", "scheduleCreate", "scheduleDelete", "scheduleSign", "tokenFeeScheduleUpdate", "tokenPause", "tokenUnpause", "cryptoApproveAllowance", "cryptoDeleteAllowance", "ethereumTransaction", "nodeStakeUpdate", "utilPrng", "tokenUpdateNfts", "nodeCreate", "nodeUpdate", "nodeDelete", "tokenReject", "tokenAirdrop", "tokenCancelAirdrop", "tokenClaimAirdrop", "stateSignatureTransaction", "hintsPreprocessingVote", "hintsKeyPublication", "hintsPartialSignature", "historyProofSignature", "historyProofKeyPublication", "historyProofVote", "crsPublication", "atomicBatch", "hookStore", "hookDispatch", "ledgerIdPublication", "registeredNodeCreate", "registeredNodeUpdate", "registeredNodeDelete", "migrationRootHashVote"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new TransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TransactionBody
         * @static
         * @param {proto.TransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionBody} TransactionBody instance
         * @type {{
         *   (properties: proto.TransactionBody.$Shape): proto.TransactionBody & proto.TransactionBody.$Shape;
         *   (properties?: proto.TransactionBody.$Properties): proto.TransactionBody;
         * }}
         */
        TransactionBody.create = function(properties) {
            return new TransactionBody(properties);
        };

        /**
         * Encodes the specified TransactionBody message. Does not implicitly {@link proto.TransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionBody
         * @static
         * @param {proto.TransactionBody.$Properties} message TransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID"))
                $root.proto.TransactionID.encode(message.transactionID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.nodeAccountID != null && $Object.hasOwnProperty.call(message, "nodeAccountID"))
                $root.proto.AccountID.encode(message.nodeAccountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.transactionFee != null && $Object.hasOwnProperty.call(message, "transactionFee") && (typeof message.transactionFee === "object" ? message.transactionFee.low || message.transactionFee.high : message.transactionFee !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.transactionFee);
            if (message.transactionValidDuration != null && $Object.hasOwnProperty.call(message, "transactionValidDuration"))
                $root.proto.Duration.encode(message.transactionValidDuration, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.generateRecord != null && $Object.hasOwnProperty.call(message, "generateRecord") && message.generateRecord !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.generateRecord);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.memo);
            if (message.contractCall != null && $Object.hasOwnProperty.call(message, "contractCall"))
                $root.proto.ContractCallTransactionBody.encode(message.contractCall, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.contractCreateInstance != null && $Object.hasOwnProperty.call(message, "contractCreateInstance"))
                $root.proto.ContractCreateTransactionBody.encode(message.contractCreateInstance, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.contractUpdateInstance != null && $Object.hasOwnProperty.call(message, "contractUpdateInstance"))
                $root.proto.ContractUpdateTransactionBody.encode(message.contractUpdateInstance, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.cryptoAddLiveHash != null && $Object.hasOwnProperty.call(message, "cryptoAddLiveHash"))
                $root.proto.CryptoAddLiveHashTransactionBody.encode(message.cryptoAddLiveHash, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.cryptoCreateAccount != null && $Object.hasOwnProperty.call(message, "cryptoCreateAccount"))
                $root.proto.CryptoCreateTransactionBody.encode(message.cryptoCreateAccount, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.cryptoDelete != null && $Object.hasOwnProperty.call(message, "cryptoDelete"))
                $root.proto.CryptoDeleteTransactionBody.encode(message.cryptoDelete, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.cryptoDeleteLiveHash != null && $Object.hasOwnProperty.call(message, "cryptoDeleteLiveHash"))
                $root.proto.CryptoDeleteLiveHashTransactionBody.encode(message.cryptoDeleteLiveHash, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.cryptoTransfer != null && $Object.hasOwnProperty.call(message, "cryptoTransfer"))
                $root.proto.CryptoTransferTransactionBody.encode(message.cryptoTransfer, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.cryptoUpdateAccount != null && $Object.hasOwnProperty.call(message, "cryptoUpdateAccount"))
                $root.proto.CryptoUpdateTransactionBody.encode(message.cryptoUpdateAccount, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.fileAppend != null && $Object.hasOwnProperty.call(message, "fileAppend"))
                $root.proto.FileAppendTransactionBody.encode(message.fileAppend, writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.fileCreate != null && $Object.hasOwnProperty.call(message, "fileCreate"))
                $root.proto.FileCreateTransactionBody.encode(message.fileCreate, writer.uint32(/* id 17, wireType 2 =*/138).fork(), _depth + 1).ldelim();
            if (message.fileDelete != null && $Object.hasOwnProperty.call(message, "fileDelete"))
                $root.proto.FileDeleteTransactionBody.encode(message.fileDelete, writer.uint32(/* id 18, wireType 2 =*/146).fork(), _depth + 1).ldelim();
            if (message.fileUpdate != null && $Object.hasOwnProperty.call(message, "fileUpdate"))
                $root.proto.FileUpdateTransactionBody.encode(message.fileUpdate, writer.uint32(/* id 19, wireType 2 =*/154).fork(), _depth + 1).ldelim();
            if (message.systemDelete != null && $Object.hasOwnProperty.call(message, "systemDelete"))
                $root.proto.SystemDeleteTransactionBody.encode(message.systemDelete, writer.uint32(/* id 20, wireType 2 =*/162).fork(), _depth + 1).ldelim();
            if (message.systemUndelete != null && $Object.hasOwnProperty.call(message, "systemUndelete"))
                $root.proto.SystemUndeleteTransactionBody.encode(message.systemUndelete, writer.uint32(/* id 21, wireType 2 =*/170).fork(), _depth + 1).ldelim();
            if (message.contractDeleteInstance != null && $Object.hasOwnProperty.call(message, "contractDeleteInstance"))
                $root.proto.ContractDeleteTransactionBody.encode(message.contractDeleteInstance, writer.uint32(/* id 22, wireType 2 =*/178).fork(), _depth + 1).ldelim();
            if (message.freeze != null && $Object.hasOwnProperty.call(message, "freeze"))
                $root.proto.FreezeTransactionBody.encode(message.freeze, writer.uint32(/* id 23, wireType 2 =*/186).fork(), _depth + 1).ldelim();
            if (message.consensusCreateTopic != null && $Object.hasOwnProperty.call(message, "consensusCreateTopic"))
                $root.proto.ConsensusCreateTopicTransactionBody.encode(message.consensusCreateTopic, writer.uint32(/* id 24, wireType 2 =*/194).fork(), _depth + 1).ldelim();
            if (message.consensusUpdateTopic != null && $Object.hasOwnProperty.call(message, "consensusUpdateTopic"))
                $root.proto.ConsensusUpdateTopicTransactionBody.encode(message.consensusUpdateTopic, writer.uint32(/* id 25, wireType 2 =*/202).fork(), _depth + 1).ldelim();
            if (message.consensusDeleteTopic != null && $Object.hasOwnProperty.call(message, "consensusDeleteTopic"))
                $root.proto.ConsensusDeleteTopicTransactionBody.encode(message.consensusDeleteTopic, writer.uint32(/* id 26, wireType 2 =*/210).fork(), _depth + 1).ldelim();
            if (message.consensusSubmitMessage != null && $Object.hasOwnProperty.call(message, "consensusSubmitMessage"))
                $root.proto.ConsensusSubmitMessageTransactionBody.encode(message.consensusSubmitMessage, writer.uint32(/* id 27, wireType 2 =*/218).fork(), _depth + 1).ldelim();
            if (message.uncheckedSubmit != null && $Object.hasOwnProperty.call(message, "uncheckedSubmit"))
                $root.proto.UncheckedSubmitBody.encode(message.uncheckedSubmit, writer.uint32(/* id 28, wireType 2 =*/226).fork(), _depth + 1).ldelim();
            if (message.tokenCreation != null && $Object.hasOwnProperty.call(message, "tokenCreation"))
                $root.proto.TokenCreateTransactionBody.encode(message.tokenCreation, writer.uint32(/* id 29, wireType 2 =*/234).fork(), _depth + 1).ldelim();
            if (message.tokenFreeze != null && $Object.hasOwnProperty.call(message, "tokenFreeze"))
                $root.proto.TokenFreezeAccountTransactionBody.encode(message.tokenFreeze, writer.uint32(/* id 31, wireType 2 =*/250).fork(), _depth + 1).ldelim();
            if (message.tokenUnfreeze != null && $Object.hasOwnProperty.call(message, "tokenUnfreeze"))
                $root.proto.TokenUnfreezeAccountTransactionBody.encode(message.tokenUnfreeze, writer.uint32(/* id 32, wireType 2 =*/258).fork(), _depth + 1).ldelim();
            if (message.tokenGrantKyc != null && $Object.hasOwnProperty.call(message, "tokenGrantKyc"))
                $root.proto.TokenGrantKycTransactionBody.encode(message.tokenGrantKyc, writer.uint32(/* id 33, wireType 2 =*/266).fork(), _depth + 1).ldelim();
            if (message.tokenRevokeKyc != null && $Object.hasOwnProperty.call(message, "tokenRevokeKyc"))
                $root.proto.TokenRevokeKycTransactionBody.encode(message.tokenRevokeKyc, writer.uint32(/* id 34, wireType 2 =*/274).fork(), _depth + 1).ldelim();
            if (message.tokenDeletion != null && $Object.hasOwnProperty.call(message, "tokenDeletion"))
                $root.proto.TokenDeleteTransactionBody.encode(message.tokenDeletion, writer.uint32(/* id 35, wireType 2 =*/282).fork(), _depth + 1).ldelim();
            if (message.tokenUpdate != null && $Object.hasOwnProperty.call(message, "tokenUpdate"))
                $root.proto.TokenUpdateTransactionBody.encode(message.tokenUpdate, writer.uint32(/* id 36, wireType 2 =*/290).fork(), _depth + 1).ldelim();
            if (message.tokenMint != null && $Object.hasOwnProperty.call(message, "tokenMint"))
                $root.proto.TokenMintTransactionBody.encode(message.tokenMint, writer.uint32(/* id 37, wireType 2 =*/298).fork(), _depth + 1).ldelim();
            if (message.tokenBurn != null && $Object.hasOwnProperty.call(message, "tokenBurn"))
                $root.proto.TokenBurnTransactionBody.encode(message.tokenBurn, writer.uint32(/* id 38, wireType 2 =*/306).fork(), _depth + 1).ldelim();
            if (message.tokenWipe != null && $Object.hasOwnProperty.call(message, "tokenWipe"))
                $root.proto.TokenWipeAccountTransactionBody.encode(message.tokenWipe, writer.uint32(/* id 39, wireType 2 =*/314).fork(), _depth + 1).ldelim();
            if (message.tokenAssociate != null && $Object.hasOwnProperty.call(message, "tokenAssociate"))
                $root.proto.TokenAssociateTransactionBody.encode(message.tokenAssociate, writer.uint32(/* id 40, wireType 2 =*/322).fork(), _depth + 1).ldelim();
            if (message.tokenDissociate != null && $Object.hasOwnProperty.call(message, "tokenDissociate"))
                $root.proto.TokenDissociateTransactionBody.encode(message.tokenDissociate, writer.uint32(/* id 41, wireType 2 =*/330).fork(), _depth + 1).ldelim();
            if (message.scheduleCreate != null && $Object.hasOwnProperty.call(message, "scheduleCreate"))
                $root.proto.ScheduleCreateTransactionBody.encode(message.scheduleCreate, writer.uint32(/* id 42, wireType 2 =*/338).fork(), _depth + 1).ldelim();
            if (message.scheduleDelete != null && $Object.hasOwnProperty.call(message, "scheduleDelete"))
                $root.proto.ScheduleDeleteTransactionBody.encode(message.scheduleDelete, writer.uint32(/* id 43, wireType 2 =*/346).fork(), _depth + 1).ldelim();
            if (message.scheduleSign != null && $Object.hasOwnProperty.call(message, "scheduleSign"))
                $root.proto.ScheduleSignTransactionBody.encode(message.scheduleSign, writer.uint32(/* id 44, wireType 2 =*/354).fork(), _depth + 1).ldelim();
            if (message.tokenFeeScheduleUpdate != null && $Object.hasOwnProperty.call(message, "tokenFeeScheduleUpdate"))
                $root.proto.TokenFeeScheduleUpdateTransactionBody.encode(message.tokenFeeScheduleUpdate, writer.uint32(/* id 45, wireType 2 =*/362).fork(), _depth + 1).ldelim();
            if (message.tokenPause != null && $Object.hasOwnProperty.call(message, "tokenPause"))
                $root.proto.TokenPauseTransactionBody.encode(message.tokenPause, writer.uint32(/* id 46, wireType 2 =*/370).fork(), _depth + 1).ldelim();
            if (message.tokenUnpause != null && $Object.hasOwnProperty.call(message, "tokenUnpause"))
                $root.proto.TokenUnpauseTransactionBody.encode(message.tokenUnpause, writer.uint32(/* id 47, wireType 2 =*/378).fork(), _depth + 1).ldelim();
            if (message.cryptoApproveAllowance != null && $Object.hasOwnProperty.call(message, "cryptoApproveAllowance"))
                $root.proto.CryptoApproveAllowanceTransactionBody.encode(message.cryptoApproveAllowance, writer.uint32(/* id 48, wireType 2 =*/386).fork(), _depth + 1).ldelim();
            if (message.cryptoDeleteAllowance != null && $Object.hasOwnProperty.call(message, "cryptoDeleteAllowance"))
                $root.proto.CryptoDeleteAllowanceTransactionBody.encode(message.cryptoDeleteAllowance, writer.uint32(/* id 49, wireType 2 =*/394).fork(), _depth + 1).ldelim();
            if (message.ethereumTransaction != null && $Object.hasOwnProperty.call(message, "ethereumTransaction"))
                $root.proto.EthereumTransactionBody.encode(message.ethereumTransaction, writer.uint32(/* id 50, wireType 2 =*/402).fork(), _depth + 1).ldelim();
            if (message.nodeStakeUpdate != null && $Object.hasOwnProperty.call(message, "nodeStakeUpdate"))
                $root.proto.NodeStakeUpdateTransactionBody.encode(message.nodeStakeUpdate, writer.uint32(/* id 51, wireType 2 =*/410).fork(), _depth + 1).ldelim();
            if (message.utilPrng != null && $Object.hasOwnProperty.call(message, "utilPrng"))
                $root.proto.UtilPrngTransactionBody.encode(message.utilPrng, writer.uint32(/* id 52, wireType 2 =*/418).fork(), _depth + 1).ldelim();
            if (message.tokenUpdateNfts != null && $Object.hasOwnProperty.call(message, "tokenUpdateNfts"))
                $root.proto.TokenUpdateNftsTransactionBody.encode(message.tokenUpdateNfts, writer.uint32(/* id 53, wireType 2 =*/426).fork(), _depth + 1).ldelim();
            if (message.nodeCreate != null && $Object.hasOwnProperty.call(message, "nodeCreate"))
                $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.encode(message.nodeCreate, writer.uint32(/* id 54, wireType 2 =*/434).fork(), _depth + 1).ldelim();
            if (message.nodeUpdate != null && $Object.hasOwnProperty.call(message, "nodeUpdate"))
                $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.encode(message.nodeUpdate, writer.uint32(/* id 55, wireType 2 =*/442).fork(), _depth + 1).ldelim();
            if (message.nodeDelete != null && $Object.hasOwnProperty.call(message, "nodeDelete"))
                $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.encode(message.nodeDelete, writer.uint32(/* id 56, wireType 2 =*/450).fork(), _depth + 1).ldelim();
            if (message.tokenReject != null && $Object.hasOwnProperty.call(message, "tokenReject"))
                $root.proto.TokenRejectTransactionBody.encode(message.tokenReject, writer.uint32(/* id 57, wireType 2 =*/458).fork(), _depth + 1).ldelim();
            if (message.tokenAirdrop != null && $Object.hasOwnProperty.call(message, "tokenAirdrop"))
                $root.proto.TokenAirdropTransactionBody.encode(message.tokenAirdrop, writer.uint32(/* id 58, wireType 2 =*/466).fork(), _depth + 1).ldelim();
            if (message.tokenCancelAirdrop != null && $Object.hasOwnProperty.call(message, "tokenCancelAirdrop"))
                $root.proto.TokenCancelAirdropTransactionBody.encode(message.tokenCancelAirdrop, writer.uint32(/* id 59, wireType 2 =*/474).fork(), _depth + 1).ldelim();
            if (message.tokenClaimAirdrop != null && $Object.hasOwnProperty.call(message, "tokenClaimAirdrop"))
                $root.proto.TokenClaimAirdropTransactionBody.encode(message.tokenClaimAirdrop, writer.uint32(/* id 60, wireType 2 =*/482).fork(), _depth + 1).ldelim();
            if (message.stateSignatureTransaction != null && $Object.hasOwnProperty.call(message, "stateSignatureTransaction"))
                $root.com.hedera.hapi.platform.event.StateSignatureTransaction.encode(message.stateSignatureTransaction, writer.uint32(/* id 65, wireType 2 =*/522).fork(), _depth + 1).ldelim();
            if (message.hintsPreprocessingVote != null && $Object.hasOwnProperty.call(message, "hintsPreprocessingVote"))
                $root.com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.encode(message.hintsPreprocessingVote, writer.uint32(/* id 66, wireType 2 =*/530).fork(), _depth + 1).ldelim();
            if (message.hintsKeyPublication != null && $Object.hasOwnProperty.call(message, "hintsKeyPublication"))
                $root.com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.encode(message.hintsKeyPublication, writer.uint32(/* id 67, wireType 2 =*/538).fork(), _depth + 1).ldelim();
            if (message.hintsPartialSignature != null && $Object.hasOwnProperty.call(message, "hintsPartialSignature"))
                $root.com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.encode(message.hintsPartialSignature, writer.uint32(/* id 68, wireType 2 =*/546).fork(), _depth + 1).ldelim();
            if (message.historyProofSignature != null && $Object.hasOwnProperty.call(message, "historyProofSignature"))
                $root.com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.encode(message.historyProofSignature, writer.uint32(/* id 69, wireType 2 =*/554).fork(), _depth + 1).ldelim();
            if (message.historyProofKeyPublication != null && $Object.hasOwnProperty.call(message, "historyProofKeyPublication"))
                $root.com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.encode(message.historyProofKeyPublication, writer.uint32(/* id 70, wireType 2 =*/562).fork(), _depth + 1).ldelim();
            if (message.historyProofVote != null && $Object.hasOwnProperty.call(message, "historyProofVote"))
                $root.com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.encode(message.historyProofVote, writer.uint32(/* id 71, wireType 2 =*/570).fork(), _depth + 1).ldelim();
            if (message.crsPublication != null && $Object.hasOwnProperty.call(message, "crsPublication"))
                $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.encode(message.crsPublication, writer.uint32(/* id 72, wireType 2 =*/578).fork(), _depth + 1).ldelim();
            if (message.batchKey != null && $Object.hasOwnProperty.call(message, "batchKey"))
                $root.proto.Key.encode(message.batchKey, writer.uint32(/* id 73, wireType 2 =*/586).fork(), _depth + 1).ldelim();
            if (message.atomicBatch != null && $Object.hasOwnProperty.call(message, "atomicBatch"))
                $root.proto.AtomicBatchTransactionBody.encode(message.atomicBatch, writer.uint32(/* id 74, wireType 2 =*/594).fork(), _depth + 1).ldelim();
            if (message.hookStore != null && $Object.hasOwnProperty.call(message, "hookStore"))
                $root.com.hedera.hapi.node.hooks.HookStoreTransactionBody.encode(message.hookStore, writer.uint32(/* id 75, wireType 2 =*/602).fork(), _depth + 1).ldelim();
            if (message.hookDispatch != null && $Object.hasOwnProperty.call(message, "hookDispatch"))
                $root.com.hedera.hapi.node.hooks.HookDispatchTransactionBody.encode(message.hookDispatch, writer.uint32(/* id 76, wireType 2 =*/610).fork(), _depth + 1).ldelim();
            if (message.ledgerIdPublication != null && $Object.hasOwnProperty.call(message, "ledgerIdPublication"))
                $root.com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.encode(message.ledgerIdPublication, writer.uint32(/* id 77, wireType 2 =*/618).fork(), _depth + 1).ldelim();
            if (message.registeredNodeCreate != null && $Object.hasOwnProperty.call(message, "registeredNodeCreate"))
                $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.encode(message.registeredNodeCreate, writer.uint32(/* id 78, wireType 2 =*/626).fork(), _depth + 1).ldelim();
            if (message.registeredNodeUpdate != null && $Object.hasOwnProperty.call(message, "registeredNodeUpdate"))
                $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.encode(message.registeredNodeUpdate, writer.uint32(/* id 79, wireType 2 =*/634).fork(), _depth + 1).ldelim();
            if (message.registeredNodeDelete != null && $Object.hasOwnProperty.call(message, "registeredNodeDelete"))
                $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.encode(message.registeredNodeDelete, writer.uint32(/* id 80, wireType 2 =*/642).fork(), _depth + 1).ldelim();
            if (message.migrationRootHashVote != null && $Object.hasOwnProperty.call(message, "migrationRootHashVote"))
                $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.encode(message.migrationRootHashVote, writer.uint32(/* id 81, wireType 2 =*/650).fork(), _depth + 1).ldelim();
            if (message.maxCustomFees != null && message.maxCustomFees.length)
                for (let i = 0; i < message.maxCustomFees.length; ++i)
                    $root.proto.CustomFeeLimit.encode(message.maxCustomFees[i], writer.uint32(/* id 1001, wireType 2 =*/8010).fork(), _depth + 1).ldelim();
            if (message.highVolume != null && $Object.hasOwnProperty.call(message, "highVolume") && message.highVolume !== false)
                writer.uint32(/* id 1002, wireType 0 =*/8016).bool(message.highVolume);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionBody message, length delimited. Does not implicitly {@link proto.TransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionBody
         * @static
         * @param {proto.TransactionBody.$Properties} message TransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionBody & proto.TransactionBody.$Shape} TransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.transactionID = $root.proto.TransactionID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.nodeAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodeAccountID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.transactionFee = value;
                        else
                            delete message.transactionFee;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.transactionValidDuration = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionValidDuration);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.generateRecord = value;
                        else
                            delete message.generateRecord;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 73: {
                        if (wireType !== 2)
                            break;
                        message.batchKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.batchKey);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.contractCall = $root.proto.ContractCallTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractCall);
                        message.data = "contractCall";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.contractCreateInstance = $root.proto.ContractCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractCreateInstance);
                        message.data = "contractCreateInstance";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.contractUpdateInstance = $root.proto.ContractUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractUpdateInstance);
                        message.data = "contractUpdateInstance";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.cryptoAddLiveHash = $root.proto.CryptoAddLiveHashTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoAddLiveHash);
                        message.data = "cryptoAddLiveHash";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.cryptoCreateAccount = $root.proto.CryptoCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoCreateAccount);
                        message.data = "cryptoCreateAccount";
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.cryptoDelete = $root.proto.CryptoDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoDelete);
                        message.data = "cryptoDelete";
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.cryptoDeleteLiveHash = $root.proto.CryptoDeleteLiveHashTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoDeleteLiveHash);
                        message.data = "cryptoDeleteLiveHash";
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.cryptoTransfer = $root.proto.CryptoTransferTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoTransfer);
                        message.data = "cryptoTransfer";
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.cryptoUpdateAccount = $root.proto.CryptoUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoUpdateAccount);
                        message.data = "cryptoUpdateAccount";
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.fileAppend = $root.proto.FileAppendTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileAppend);
                        message.data = "fileAppend";
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        message.fileCreate = $root.proto.FileCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileCreate);
                        message.data = "fileCreate";
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        message.fileDelete = $root.proto.FileDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileDelete);
                        message.data = "fileDelete";
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        message.fileUpdate = $root.proto.FileUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileUpdate);
                        message.data = "fileUpdate";
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        message.systemDelete = $root.proto.SystemDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.systemDelete);
                        message.data = "systemDelete";
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        message.systemUndelete = $root.proto.SystemUndeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.systemUndelete);
                        message.data = "systemUndelete";
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        message.contractDeleteInstance = $root.proto.ContractDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractDeleteInstance);
                        message.data = "contractDeleteInstance";
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        message.freeze = $root.proto.FreezeTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.freeze);
                        message.data = "freeze";
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        message.consensusCreateTopic = $root.proto.ConsensusCreateTopicTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusCreateTopic);
                        message.data = "consensusCreateTopic";
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        message.consensusUpdateTopic = $root.proto.ConsensusUpdateTopicTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusUpdateTopic);
                        message.data = "consensusUpdateTopic";
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        message.consensusDeleteTopic = $root.proto.ConsensusDeleteTopicTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusDeleteTopic);
                        message.data = "consensusDeleteTopic";
                        continue;
                    }
                case 27: {
                        if (wireType !== 2)
                            break;
                        message.consensusSubmitMessage = $root.proto.ConsensusSubmitMessageTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusSubmitMessage);
                        message.data = "consensusSubmitMessage";
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        message.uncheckedSubmit = $root.proto.UncheckedSubmitBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.uncheckedSubmit);
                        message.data = "uncheckedSubmit";
                        continue;
                    }
                case 29: {
                        if (wireType !== 2)
                            break;
                        message.tokenCreation = $root.proto.TokenCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenCreation);
                        message.data = "tokenCreation";
                        continue;
                    }
                case 31: {
                        if (wireType !== 2)
                            break;
                        message.tokenFreeze = $root.proto.TokenFreezeAccountTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenFreeze);
                        message.data = "tokenFreeze";
                        continue;
                    }
                case 32: {
                        if (wireType !== 2)
                            break;
                        message.tokenUnfreeze = $root.proto.TokenUnfreezeAccountTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenUnfreeze);
                        message.data = "tokenUnfreeze";
                        continue;
                    }
                case 33: {
                        if (wireType !== 2)
                            break;
                        message.tokenGrantKyc = $root.proto.TokenGrantKycTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenGrantKyc);
                        message.data = "tokenGrantKyc";
                        continue;
                    }
                case 34: {
                        if (wireType !== 2)
                            break;
                        message.tokenRevokeKyc = $root.proto.TokenRevokeKycTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenRevokeKyc);
                        message.data = "tokenRevokeKyc";
                        continue;
                    }
                case 35: {
                        if (wireType !== 2)
                            break;
                        message.tokenDeletion = $root.proto.TokenDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenDeletion);
                        message.data = "tokenDeletion";
                        continue;
                    }
                case 36: {
                        if (wireType !== 2)
                            break;
                        message.tokenUpdate = $root.proto.TokenUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenUpdate);
                        message.data = "tokenUpdate";
                        continue;
                    }
                case 37: {
                        if (wireType !== 2)
                            break;
                        message.tokenMint = $root.proto.TokenMintTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenMint);
                        message.data = "tokenMint";
                        continue;
                    }
                case 38: {
                        if (wireType !== 2)
                            break;
                        message.tokenBurn = $root.proto.TokenBurnTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenBurn);
                        message.data = "tokenBurn";
                        continue;
                    }
                case 39: {
                        if (wireType !== 2)
                            break;
                        message.tokenWipe = $root.proto.TokenWipeAccountTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenWipe);
                        message.data = "tokenWipe";
                        continue;
                    }
                case 40: {
                        if (wireType !== 2)
                            break;
                        message.tokenAssociate = $root.proto.TokenAssociateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenAssociate);
                        message.data = "tokenAssociate";
                        continue;
                    }
                case 41: {
                        if (wireType !== 2)
                            break;
                        message.tokenDissociate = $root.proto.TokenDissociateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenDissociate);
                        message.data = "tokenDissociate";
                        continue;
                    }
                case 42: {
                        if (wireType !== 2)
                            break;
                        message.scheduleCreate = $root.proto.ScheduleCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleCreate);
                        message.data = "scheduleCreate";
                        continue;
                    }
                case 43: {
                        if (wireType !== 2)
                            break;
                        message.scheduleDelete = $root.proto.ScheduleDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleDelete);
                        message.data = "scheduleDelete";
                        continue;
                    }
                case 44: {
                        if (wireType !== 2)
                            break;
                        message.scheduleSign = $root.proto.ScheduleSignTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleSign);
                        message.data = "scheduleSign";
                        continue;
                    }
                case 45: {
                        if (wireType !== 2)
                            break;
                        message.tokenFeeScheduleUpdate = $root.proto.TokenFeeScheduleUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenFeeScheduleUpdate);
                        message.data = "tokenFeeScheduleUpdate";
                        continue;
                    }
                case 46: {
                        if (wireType !== 2)
                            break;
                        message.tokenPause = $root.proto.TokenPauseTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenPause);
                        message.data = "tokenPause";
                        continue;
                    }
                case 47: {
                        if (wireType !== 2)
                            break;
                        message.tokenUnpause = $root.proto.TokenUnpauseTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenUnpause);
                        message.data = "tokenUnpause";
                        continue;
                    }
                case 48: {
                        if (wireType !== 2)
                            break;
                        message.cryptoApproveAllowance = $root.proto.CryptoApproveAllowanceTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoApproveAllowance);
                        message.data = "cryptoApproveAllowance";
                        continue;
                    }
                case 49: {
                        if (wireType !== 2)
                            break;
                        message.cryptoDeleteAllowance = $root.proto.CryptoDeleteAllowanceTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoDeleteAllowance);
                        message.data = "cryptoDeleteAllowance";
                        continue;
                    }
                case 50: {
                        if (wireType !== 2)
                            break;
                        message.ethereumTransaction = $root.proto.EthereumTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ethereumTransaction);
                        message.data = "ethereumTransaction";
                        continue;
                    }
                case 51: {
                        if (wireType !== 2)
                            break;
                        message.nodeStakeUpdate = $root.proto.NodeStakeUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodeStakeUpdate);
                        message.data = "nodeStakeUpdate";
                        continue;
                    }
                case 52: {
                        if (wireType !== 2)
                            break;
                        message.utilPrng = $root.proto.UtilPrngTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.utilPrng);
                        message.data = "utilPrng";
                        continue;
                    }
                case 53: {
                        if (wireType !== 2)
                            break;
                        message.tokenUpdateNfts = $root.proto.TokenUpdateNftsTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenUpdateNfts);
                        message.data = "tokenUpdateNfts";
                        continue;
                    }
                case 54: {
                        if (wireType !== 2)
                            break;
                        message.nodeCreate = $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodeCreate);
                        message.data = "nodeCreate";
                        continue;
                    }
                case 55: {
                        if (wireType !== 2)
                            break;
                        message.nodeUpdate = $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodeUpdate);
                        message.data = "nodeUpdate";
                        continue;
                    }
                case 56: {
                        if (wireType !== 2)
                            break;
                        message.nodeDelete = $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodeDelete);
                        message.data = "nodeDelete";
                        continue;
                    }
                case 57: {
                        if (wireType !== 2)
                            break;
                        message.tokenReject = $root.proto.TokenRejectTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenReject);
                        message.data = "tokenReject";
                        continue;
                    }
                case 58: {
                        if (wireType !== 2)
                            break;
                        message.tokenAirdrop = $root.proto.TokenAirdropTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenAirdrop);
                        message.data = "tokenAirdrop";
                        continue;
                    }
                case 59: {
                        if (wireType !== 2)
                            break;
                        message.tokenCancelAirdrop = $root.proto.TokenCancelAirdropTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenCancelAirdrop);
                        message.data = "tokenCancelAirdrop";
                        continue;
                    }
                case 60: {
                        if (wireType !== 2)
                            break;
                        message.tokenClaimAirdrop = $root.proto.TokenClaimAirdropTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenClaimAirdrop);
                        message.data = "tokenClaimAirdrop";
                        continue;
                    }
                case 65: {
                        if (wireType !== 2)
                            break;
                        message.stateSignatureTransaction = $root.com.hedera.hapi.platform.event.StateSignatureTransaction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stateSignatureTransaction);
                        message.data = "stateSignatureTransaction";
                        continue;
                    }
                case 66: {
                        if (wireType !== 2)
                            break;
                        message.hintsPreprocessingVote = $root.com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hintsPreprocessingVote);
                        message.data = "hintsPreprocessingVote";
                        continue;
                    }
                case 67: {
                        if (wireType !== 2)
                            break;
                        message.hintsKeyPublication = $root.com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hintsKeyPublication);
                        message.data = "hintsKeyPublication";
                        continue;
                    }
                case 68: {
                        if (wireType !== 2)
                            break;
                        message.hintsPartialSignature = $root.com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hintsPartialSignature);
                        message.data = "hintsPartialSignature";
                        continue;
                    }
                case 69: {
                        if (wireType !== 2)
                            break;
                        message.historyProofSignature = $root.com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.historyProofSignature);
                        message.data = "historyProofSignature";
                        continue;
                    }
                case 70: {
                        if (wireType !== 2)
                            break;
                        message.historyProofKeyPublication = $root.com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.historyProofKeyPublication);
                        message.data = "historyProofKeyPublication";
                        continue;
                    }
                case 71: {
                        if (wireType !== 2)
                            break;
                        message.historyProofVote = $root.com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.historyProofVote);
                        message.data = "historyProofVote";
                        continue;
                    }
                case 72: {
                        if (wireType !== 2)
                            break;
                        message.crsPublication = $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.crsPublication);
                        message.data = "crsPublication";
                        continue;
                    }
                case 74: {
                        if (wireType !== 2)
                            break;
                        message.atomicBatch = $root.proto.AtomicBatchTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.atomicBatch);
                        message.data = "atomicBatch";
                        continue;
                    }
                case 75: {
                        if (wireType !== 2)
                            break;
                        message.hookStore = $root.com.hedera.hapi.node.hooks.HookStoreTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hookStore);
                        message.data = "hookStore";
                        continue;
                    }
                case 76: {
                        if (wireType !== 2)
                            break;
                        message.hookDispatch = $root.com.hedera.hapi.node.hooks.HookDispatchTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hookDispatch);
                        message.data = "hookDispatch";
                        continue;
                    }
                case 77: {
                        if (wireType !== 2)
                            break;
                        message.ledgerIdPublication = $root.com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ledgerIdPublication);
                        message.data = "ledgerIdPublication";
                        continue;
                    }
                case 78: {
                        if (wireType !== 2)
                            break;
                        message.registeredNodeCreate = $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.registeredNodeCreate);
                        message.data = "registeredNodeCreate";
                        continue;
                    }
                case 79: {
                        if (wireType !== 2)
                            break;
                        message.registeredNodeUpdate = $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.registeredNodeUpdate);
                        message.data = "registeredNodeUpdate";
                        continue;
                    }
                case 80: {
                        if (wireType !== 2)
                            break;
                        message.registeredNodeDelete = $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.registeredNodeDelete);
                        message.data = "registeredNodeDelete";
                        continue;
                    }
                case 81: {
                        if (wireType !== 2)
                            break;
                        message.migrationRootHashVote = $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.migrationRootHashVote);
                        message.data = "migrationRootHashVote";
                        continue;
                    }
                case 1001: {
                        if (wireType !== 2)
                            break;
                        if (!(message.maxCustomFees && message.maxCustomFees.length))
                            message.maxCustomFees = [];
                        message.maxCustomFees.push($root.proto.CustomFeeLimit.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 1002: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.highVolume = value;
                        else
                            delete message.highVolume;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionBody & proto.TransactionBody.$Shape} TransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionBody message.
         * @function verify
         * @memberof proto.TransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID")) {
                let error = $root.proto.TransactionID.verify(message.transactionID, _depth + 1);
                if (error)
                    return "transactionID." + error;
            }
            if (message.nodeAccountID != null && $Object.hasOwnProperty.call(message, "nodeAccountID")) {
                let error = $root.proto.AccountID.verify(message.nodeAccountID, _depth + 1);
                if (error)
                    return "nodeAccountID." + error;
            }
            if (message.transactionFee != null && $Object.hasOwnProperty.call(message, "transactionFee"))
                if (!$util.isInteger(message.transactionFee) && !(message.transactionFee && $util.isInteger(message.transactionFee.low) && $util.isInteger(message.transactionFee.high)))
                    return "transactionFee: integer|Long expected";
            if (message.transactionValidDuration != null && $Object.hasOwnProperty.call(message, "transactionValidDuration")) {
                let error = $root.proto.Duration.verify(message.transactionValidDuration, _depth + 1);
                if (error)
                    return "transactionValidDuration." + error;
            }
            if (message.generateRecord != null && $Object.hasOwnProperty.call(message, "generateRecord"))
                if (typeof message.generateRecord !== "boolean")
                    return "generateRecord: boolean expected";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.batchKey != null && $Object.hasOwnProperty.call(message, "batchKey")) {
                let error = $root.proto.Key.verify(message.batchKey, _depth + 1);
                if (error)
                    return "batchKey." + error;
            }
            if (message.contractCall != null && $Object.hasOwnProperty.call(message, "contractCall")) {
                properties.data = 1;
                {
                    let error = $root.proto.ContractCallTransactionBody.verify(message.contractCall, _depth + 1);
                    if (error)
                        return "contractCall." + error;
                }
            }
            if (message.contractCreateInstance != null && $Object.hasOwnProperty.call(message, "contractCreateInstance")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ContractCreateTransactionBody.verify(message.contractCreateInstance, _depth + 1);
                    if (error)
                        return "contractCreateInstance." + error;
                }
            }
            if (message.contractUpdateInstance != null && $Object.hasOwnProperty.call(message, "contractUpdateInstance")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ContractUpdateTransactionBody.verify(message.contractUpdateInstance, _depth + 1);
                    if (error)
                        return "contractUpdateInstance." + error;
                }
            }
            if (message.cryptoAddLiveHash != null && $Object.hasOwnProperty.call(message, "cryptoAddLiveHash")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoAddLiveHashTransactionBody.verify(message.cryptoAddLiveHash, _depth + 1);
                    if (error)
                        return "cryptoAddLiveHash." + error;
                }
            }
            if (message.cryptoCreateAccount != null && $Object.hasOwnProperty.call(message, "cryptoCreateAccount")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoCreateTransactionBody.verify(message.cryptoCreateAccount, _depth + 1);
                    if (error)
                        return "cryptoCreateAccount." + error;
                }
            }
            if (message.cryptoDelete != null && $Object.hasOwnProperty.call(message, "cryptoDelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoDeleteTransactionBody.verify(message.cryptoDelete, _depth + 1);
                    if (error)
                        return "cryptoDelete." + error;
                }
            }
            if (message.cryptoDeleteLiveHash != null && $Object.hasOwnProperty.call(message, "cryptoDeleteLiveHash")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoDeleteLiveHashTransactionBody.verify(message.cryptoDeleteLiveHash, _depth + 1);
                    if (error)
                        return "cryptoDeleteLiveHash." + error;
                }
            }
            if (message.cryptoTransfer != null && $Object.hasOwnProperty.call(message, "cryptoTransfer")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoTransferTransactionBody.verify(message.cryptoTransfer, _depth + 1);
                    if (error)
                        return "cryptoTransfer." + error;
                }
            }
            if (message.cryptoUpdateAccount != null && $Object.hasOwnProperty.call(message, "cryptoUpdateAccount")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoUpdateTransactionBody.verify(message.cryptoUpdateAccount, _depth + 1);
                    if (error)
                        return "cryptoUpdateAccount." + error;
                }
            }
            if (message.fileAppend != null && $Object.hasOwnProperty.call(message, "fileAppend")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.FileAppendTransactionBody.verify(message.fileAppend, _depth + 1);
                    if (error)
                        return "fileAppend." + error;
                }
            }
            if (message.fileCreate != null && $Object.hasOwnProperty.call(message, "fileCreate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.FileCreateTransactionBody.verify(message.fileCreate, _depth + 1);
                    if (error)
                        return "fileCreate." + error;
                }
            }
            if (message.fileDelete != null && $Object.hasOwnProperty.call(message, "fileDelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.FileDeleteTransactionBody.verify(message.fileDelete, _depth + 1);
                    if (error)
                        return "fileDelete." + error;
                }
            }
            if (message.fileUpdate != null && $Object.hasOwnProperty.call(message, "fileUpdate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.FileUpdateTransactionBody.verify(message.fileUpdate, _depth + 1);
                    if (error)
                        return "fileUpdate." + error;
                }
            }
            if (message.systemDelete != null && $Object.hasOwnProperty.call(message, "systemDelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.SystemDeleteTransactionBody.verify(message.systemDelete, _depth + 1);
                    if (error)
                        return "systemDelete." + error;
                }
            }
            if (message.systemUndelete != null && $Object.hasOwnProperty.call(message, "systemUndelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.SystemUndeleteTransactionBody.verify(message.systemUndelete, _depth + 1);
                    if (error)
                        return "systemUndelete." + error;
                }
            }
            if (message.contractDeleteInstance != null && $Object.hasOwnProperty.call(message, "contractDeleteInstance")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ContractDeleteTransactionBody.verify(message.contractDeleteInstance, _depth + 1);
                    if (error)
                        return "contractDeleteInstance." + error;
                }
            }
            if (message.freeze != null && $Object.hasOwnProperty.call(message, "freeze")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.FreezeTransactionBody.verify(message.freeze, _depth + 1);
                    if (error)
                        return "freeze." + error;
                }
            }
            if (message.consensusCreateTopic != null && $Object.hasOwnProperty.call(message, "consensusCreateTopic")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ConsensusCreateTopicTransactionBody.verify(message.consensusCreateTopic, _depth + 1);
                    if (error)
                        return "consensusCreateTopic." + error;
                }
            }
            if (message.consensusUpdateTopic != null && $Object.hasOwnProperty.call(message, "consensusUpdateTopic")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ConsensusUpdateTopicTransactionBody.verify(message.consensusUpdateTopic, _depth + 1);
                    if (error)
                        return "consensusUpdateTopic." + error;
                }
            }
            if (message.consensusDeleteTopic != null && $Object.hasOwnProperty.call(message, "consensusDeleteTopic")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ConsensusDeleteTopicTransactionBody.verify(message.consensusDeleteTopic, _depth + 1);
                    if (error)
                        return "consensusDeleteTopic." + error;
                }
            }
            if (message.consensusSubmitMessage != null && $Object.hasOwnProperty.call(message, "consensusSubmitMessage")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ConsensusSubmitMessageTransactionBody.verify(message.consensusSubmitMessage, _depth + 1);
                    if (error)
                        return "consensusSubmitMessage." + error;
                }
            }
            if (message.uncheckedSubmit != null && $Object.hasOwnProperty.call(message, "uncheckedSubmit")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.UncheckedSubmitBody.verify(message.uncheckedSubmit, _depth + 1);
                    if (error)
                        return "uncheckedSubmit." + error;
                }
            }
            if (message.tokenCreation != null && $Object.hasOwnProperty.call(message, "tokenCreation")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenCreateTransactionBody.verify(message.tokenCreation, _depth + 1);
                    if (error)
                        return "tokenCreation." + error;
                }
            }
            if (message.tokenFreeze != null && $Object.hasOwnProperty.call(message, "tokenFreeze")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenFreezeAccountTransactionBody.verify(message.tokenFreeze, _depth + 1);
                    if (error)
                        return "tokenFreeze." + error;
                }
            }
            if (message.tokenUnfreeze != null && $Object.hasOwnProperty.call(message, "tokenUnfreeze")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenUnfreezeAccountTransactionBody.verify(message.tokenUnfreeze, _depth + 1);
                    if (error)
                        return "tokenUnfreeze." + error;
                }
            }
            if (message.tokenGrantKyc != null && $Object.hasOwnProperty.call(message, "tokenGrantKyc")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenGrantKycTransactionBody.verify(message.tokenGrantKyc, _depth + 1);
                    if (error)
                        return "tokenGrantKyc." + error;
                }
            }
            if (message.tokenRevokeKyc != null && $Object.hasOwnProperty.call(message, "tokenRevokeKyc")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenRevokeKycTransactionBody.verify(message.tokenRevokeKyc, _depth + 1);
                    if (error)
                        return "tokenRevokeKyc." + error;
                }
            }
            if (message.tokenDeletion != null && $Object.hasOwnProperty.call(message, "tokenDeletion")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenDeleteTransactionBody.verify(message.tokenDeletion, _depth + 1);
                    if (error)
                        return "tokenDeletion." + error;
                }
            }
            if (message.tokenUpdate != null && $Object.hasOwnProperty.call(message, "tokenUpdate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenUpdateTransactionBody.verify(message.tokenUpdate, _depth + 1);
                    if (error)
                        return "tokenUpdate." + error;
                }
            }
            if (message.tokenMint != null && $Object.hasOwnProperty.call(message, "tokenMint")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenMintTransactionBody.verify(message.tokenMint, _depth + 1);
                    if (error)
                        return "tokenMint." + error;
                }
            }
            if (message.tokenBurn != null && $Object.hasOwnProperty.call(message, "tokenBurn")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenBurnTransactionBody.verify(message.tokenBurn, _depth + 1);
                    if (error)
                        return "tokenBurn." + error;
                }
            }
            if (message.tokenWipe != null && $Object.hasOwnProperty.call(message, "tokenWipe")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenWipeAccountTransactionBody.verify(message.tokenWipe, _depth + 1);
                    if (error)
                        return "tokenWipe." + error;
                }
            }
            if (message.tokenAssociate != null && $Object.hasOwnProperty.call(message, "tokenAssociate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenAssociateTransactionBody.verify(message.tokenAssociate, _depth + 1);
                    if (error)
                        return "tokenAssociate." + error;
                }
            }
            if (message.tokenDissociate != null && $Object.hasOwnProperty.call(message, "tokenDissociate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenDissociateTransactionBody.verify(message.tokenDissociate, _depth + 1);
                    if (error)
                        return "tokenDissociate." + error;
                }
            }
            if (message.scheduleCreate != null && $Object.hasOwnProperty.call(message, "scheduleCreate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ScheduleCreateTransactionBody.verify(message.scheduleCreate, _depth + 1);
                    if (error)
                        return "scheduleCreate." + error;
                }
            }
            if (message.scheduleDelete != null && $Object.hasOwnProperty.call(message, "scheduleDelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ScheduleDeleteTransactionBody.verify(message.scheduleDelete, _depth + 1);
                    if (error)
                        return "scheduleDelete." + error;
                }
            }
            if (message.scheduleSign != null && $Object.hasOwnProperty.call(message, "scheduleSign")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ScheduleSignTransactionBody.verify(message.scheduleSign, _depth + 1);
                    if (error)
                        return "scheduleSign." + error;
                }
            }
            if (message.tokenFeeScheduleUpdate != null && $Object.hasOwnProperty.call(message, "tokenFeeScheduleUpdate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenFeeScheduleUpdateTransactionBody.verify(message.tokenFeeScheduleUpdate, _depth + 1);
                    if (error)
                        return "tokenFeeScheduleUpdate." + error;
                }
            }
            if (message.tokenPause != null && $Object.hasOwnProperty.call(message, "tokenPause")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenPauseTransactionBody.verify(message.tokenPause, _depth + 1);
                    if (error)
                        return "tokenPause." + error;
                }
            }
            if (message.tokenUnpause != null && $Object.hasOwnProperty.call(message, "tokenUnpause")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenUnpauseTransactionBody.verify(message.tokenUnpause, _depth + 1);
                    if (error)
                        return "tokenUnpause." + error;
                }
            }
            if (message.cryptoApproveAllowance != null && $Object.hasOwnProperty.call(message, "cryptoApproveAllowance")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoApproveAllowanceTransactionBody.verify(message.cryptoApproveAllowance, _depth + 1);
                    if (error)
                        return "cryptoApproveAllowance." + error;
                }
            }
            if (message.cryptoDeleteAllowance != null && $Object.hasOwnProperty.call(message, "cryptoDeleteAllowance")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoDeleteAllowanceTransactionBody.verify(message.cryptoDeleteAllowance, _depth + 1);
                    if (error)
                        return "cryptoDeleteAllowance." + error;
                }
            }
            if (message.ethereumTransaction != null && $Object.hasOwnProperty.call(message, "ethereumTransaction")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.EthereumTransactionBody.verify(message.ethereumTransaction, _depth + 1);
                    if (error)
                        return "ethereumTransaction." + error;
                }
            }
            if (message.nodeStakeUpdate != null && $Object.hasOwnProperty.call(message, "nodeStakeUpdate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.NodeStakeUpdateTransactionBody.verify(message.nodeStakeUpdate, _depth + 1);
                    if (error)
                        return "nodeStakeUpdate." + error;
                }
            }
            if (message.utilPrng != null && $Object.hasOwnProperty.call(message, "utilPrng")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.UtilPrngTransactionBody.verify(message.utilPrng, _depth + 1);
                    if (error)
                        return "utilPrng." + error;
                }
            }
            if (message.tokenUpdateNfts != null && $Object.hasOwnProperty.call(message, "tokenUpdateNfts")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenUpdateNftsTransactionBody.verify(message.tokenUpdateNfts, _depth + 1);
                    if (error)
                        return "tokenUpdateNfts." + error;
                }
            }
            if (message.nodeCreate != null && $Object.hasOwnProperty.call(message, "nodeCreate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.verify(message.nodeCreate, _depth + 1);
                    if (error)
                        return "nodeCreate." + error;
                }
            }
            if (message.nodeUpdate != null && $Object.hasOwnProperty.call(message, "nodeUpdate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.verify(message.nodeUpdate, _depth + 1);
                    if (error)
                        return "nodeUpdate." + error;
                }
            }
            if (message.nodeDelete != null && $Object.hasOwnProperty.call(message, "nodeDelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.verify(message.nodeDelete, _depth + 1);
                    if (error)
                        return "nodeDelete." + error;
                }
            }
            if (message.tokenReject != null && $Object.hasOwnProperty.call(message, "tokenReject")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenRejectTransactionBody.verify(message.tokenReject, _depth + 1);
                    if (error)
                        return "tokenReject." + error;
                }
            }
            if (message.tokenAirdrop != null && $Object.hasOwnProperty.call(message, "tokenAirdrop")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenAirdropTransactionBody.verify(message.tokenAirdrop, _depth + 1);
                    if (error)
                        return "tokenAirdrop." + error;
                }
            }
            if (message.tokenCancelAirdrop != null && $Object.hasOwnProperty.call(message, "tokenCancelAirdrop")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenCancelAirdropTransactionBody.verify(message.tokenCancelAirdrop, _depth + 1);
                    if (error)
                        return "tokenCancelAirdrop." + error;
                }
            }
            if (message.tokenClaimAirdrop != null && $Object.hasOwnProperty.call(message, "tokenClaimAirdrop")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenClaimAirdropTransactionBody.verify(message.tokenClaimAirdrop, _depth + 1);
                    if (error)
                        return "tokenClaimAirdrop." + error;
                }
            }
            if (message.stateSignatureTransaction != null && $Object.hasOwnProperty.call(message, "stateSignatureTransaction")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.platform.event.StateSignatureTransaction.verify(message.stateSignatureTransaction, _depth + 1);
                    if (error)
                        return "stateSignatureTransaction." + error;
                }
            }
            if (message.hintsPreprocessingVote != null && $Object.hasOwnProperty.call(message, "hintsPreprocessingVote")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.verify(message.hintsPreprocessingVote, _depth + 1);
                    if (error)
                        return "hintsPreprocessingVote." + error;
                }
            }
            if (message.hintsKeyPublication != null && $Object.hasOwnProperty.call(message, "hintsKeyPublication")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.verify(message.hintsKeyPublication, _depth + 1);
                    if (error)
                        return "hintsKeyPublication." + error;
                }
            }
            if (message.hintsPartialSignature != null && $Object.hasOwnProperty.call(message, "hintsPartialSignature")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.verify(message.hintsPartialSignature, _depth + 1);
                    if (error)
                        return "hintsPartialSignature." + error;
                }
            }
            if (message.historyProofSignature != null && $Object.hasOwnProperty.call(message, "historyProofSignature")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.verify(message.historyProofSignature, _depth + 1);
                    if (error)
                        return "historyProofSignature." + error;
                }
            }
            if (message.historyProofKeyPublication != null && $Object.hasOwnProperty.call(message, "historyProofKeyPublication")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.verify(message.historyProofKeyPublication, _depth + 1);
                    if (error)
                        return "historyProofKeyPublication." + error;
                }
            }
            if (message.historyProofVote != null && $Object.hasOwnProperty.call(message, "historyProofVote")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.verify(message.historyProofVote, _depth + 1);
                    if (error)
                        return "historyProofVote." + error;
                }
            }
            if (message.crsPublication != null && $Object.hasOwnProperty.call(message, "crsPublication")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.verify(message.crsPublication, _depth + 1);
                    if (error)
                        return "crsPublication." + error;
                }
            }
            if (message.atomicBatch != null && $Object.hasOwnProperty.call(message, "atomicBatch")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.AtomicBatchTransactionBody.verify(message.atomicBatch, _depth + 1);
                    if (error)
                        return "atomicBatch." + error;
                }
            }
            if (message.hookStore != null && $Object.hasOwnProperty.call(message, "hookStore")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.hooks.HookStoreTransactionBody.verify(message.hookStore, _depth + 1);
                    if (error)
                        return "hookStore." + error;
                }
            }
            if (message.hookDispatch != null && $Object.hasOwnProperty.call(message, "hookDispatch")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.hooks.HookDispatchTransactionBody.verify(message.hookDispatch, _depth + 1);
                    if (error)
                        return "hookDispatch." + error;
                }
            }
            if (message.ledgerIdPublication != null && $Object.hasOwnProperty.call(message, "ledgerIdPublication")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.verify(message.ledgerIdPublication, _depth + 1);
                    if (error)
                        return "ledgerIdPublication." + error;
                }
            }
            if (message.registeredNodeCreate != null && $Object.hasOwnProperty.call(message, "registeredNodeCreate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.verify(message.registeredNodeCreate, _depth + 1);
                    if (error)
                        return "registeredNodeCreate." + error;
                }
            }
            if (message.registeredNodeUpdate != null && $Object.hasOwnProperty.call(message, "registeredNodeUpdate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.verify(message.registeredNodeUpdate, _depth + 1);
                    if (error)
                        return "registeredNodeUpdate." + error;
                }
            }
            if (message.registeredNodeDelete != null && $Object.hasOwnProperty.call(message, "registeredNodeDelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.verify(message.registeredNodeDelete, _depth + 1);
                    if (error)
                        return "registeredNodeDelete." + error;
                }
            }
            if (message.migrationRootHashVote != null && $Object.hasOwnProperty.call(message, "migrationRootHashVote")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.verify(message.migrationRootHashVote, _depth + 1);
                    if (error)
                        return "migrationRootHashVote." + error;
                }
            }
            if (message.maxCustomFees != null && $Object.hasOwnProperty.call(message, "maxCustomFees")) {
                if (!$Array.isArray(message.maxCustomFees))
                    return "maxCustomFees: array expected";
                for (let i = 0; i < message.maxCustomFees.length; ++i) {
                    let error = $root.proto.CustomFeeLimit.verify(message.maxCustomFees[i], _depth + 1);
                    if (error)
                        return "maxCustomFees." + error;
                }
            }
            if (message.highVolume != null && $Object.hasOwnProperty.call(message, "highVolume"))
                if (typeof message.highVolume !== "boolean")
                    return "highVolume: boolean expected";
            return null;
        };

        /**
         * Creates a TransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionBody} TransactionBody
         */
        TransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionBody();
            if (object.transactionID != null) {
                if (!$util.isObject(object.transactionID))
                    throw $TypeError(".proto.TransactionBody.transactionID: object expected");
                message.transactionID = $root.proto.TransactionID.fromObject(object.transactionID, _depth + 1);
            }
            if (object.nodeAccountID != null) {
                if (!$util.isObject(object.nodeAccountID))
                    throw $TypeError(".proto.TransactionBody.nodeAccountID: object expected");
                message.nodeAccountID = $root.proto.AccountID.fromObject(object.nodeAccountID, _depth + 1);
            }
            if (object.transactionFee != null)
                if (typeof object.transactionFee === "object" ? object.transactionFee.low || object.transactionFee.high : $Number(object.transactionFee) !== 0)
                    if ($util.Long)
                        message.transactionFee = $util.Long.fromValue(object.transactionFee, true);
                    else if (typeof object.transactionFee === "string")
                        message.transactionFee = $parseInt(object.transactionFee, 10);
                    else if (typeof object.transactionFee === "number")
                        message.transactionFee = object.transactionFee;
                    else if (typeof object.transactionFee === "object")
                        message.transactionFee = new $util.LongBits(object.transactionFee.low >>> 0, object.transactionFee.high >>> 0).toNumber(true);
            if (object.transactionValidDuration != null) {
                if (!$util.isObject(object.transactionValidDuration))
                    throw $TypeError(".proto.TransactionBody.transactionValidDuration: object expected");
                message.transactionValidDuration = $root.proto.Duration.fromObject(object.transactionValidDuration, _depth + 1);
            }
            if (object.generateRecord != null)
                if (object.generateRecord)
                    message.generateRecord = $Boolean(object.generateRecord);
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.batchKey != null) {
                if (!$util.isObject(object.batchKey))
                    throw $TypeError(".proto.TransactionBody.batchKey: object expected");
                message.batchKey = $root.proto.Key.fromObject(object.batchKey, _depth + 1);
            }
            if (object.contractCall != null) {
                if (!$util.isObject(object.contractCall))
                    throw $TypeError(".proto.TransactionBody.contractCall: object expected");
                message.contractCall = $root.proto.ContractCallTransactionBody.fromObject(object.contractCall, _depth + 1);
            }
            if (object.contractCreateInstance != null) {
                if (!$util.isObject(object.contractCreateInstance))
                    throw $TypeError(".proto.TransactionBody.contractCreateInstance: object expected");
                message.contractCreateInstance = $root.proto.ContractCreateTransactionBody.fromObject(object.contractCreateInstance, _depth + 1);
            }
            if (object.contractUpdateInstance != null) {
                if (!$util.isObject(object.contractUpdateInstance))
                    throw $TypeError(".proto.TransactionBody.contractUpdateInstance: object expected");
                message.contractUpdateInstance = $root.proto.ContractUpdateTransactionBody.fromObject(object.contractUpdateInstance, _depth + 1);
            }
            if (object.cryptoAddLiveHash != null) {
                if (!$util.isObject(object.cryptoAddLiveHash))
                    throw $TypeError(".proto.TransactionBody.cryptoAddLiveHash: object expected");
                message.cryptoAddLiveHash = $root.proto.CryptoAddLiveHashTransactionBody.fromObject(object.cryptoAddLiveHash, _depth + 1);
            }
            if (object.cryptoCreateAccount != null) {
                if (!$util.isObject(object.cryptoCreateAccount))
                    throw $TypeError(".proto.TransactionBody.cryptoCreateAccount: object expected");
                message.cryptoCreateAccount = $root.proto.CryptoCreateTransactionBody.fromObject(object.cryptoCreateAccount, _depth + 1);
            }
            if (object.cryptoDelete != null) {
                if (!$util.isObject(object.cryptoDelete))
                    throw $TypeError(".proto.TransactionBody.cryptoDelete: object expected");
                message.cryptoDelete = $root.proto.CryptoDeleteTransactionBody.fromObject(object.cryptoDelete, _depth + 1);
            }
            if (object.cryptoDeleteLiveHash != null) {
                if (!$util.isObject(object.cryptoDeleteLiveHash))
                    throw $TypeError(".proto.TransactionBody.cryptoDeleteLiveHash: object expected");
                message.cryptoDeleteLiveHash = $root.proto.CryptoDeleteLiveHashTransactionBody.fromObject(object.cryptoDeleteLiveHash, _depth + 1);
            }
            if (object.cryptoTransfer != null) {
                if (!$util.isObject(object.cryptoTransfer))
                    throw $TypeError(".proto.TransactionBody.cryptoTransfer: object expected");
                message.cryptoTransfer = $root.proto.CryptoTransferTransactionBody.fromObject(object.cryptoTransfer, _depth + 1);
            }
            if (object.cryptoUpdateAccount != null) {
                if (!$util.isObject(object.cryptoUpdateAccount))
                    throw $TypeError(".proto.TransactionBody.cryptoUpdateAccount: object expected");
                message.cryptoUpdateAccount = $root.proto.CryptoUpdateTransactionBody.fromObject(object.cryptoUpdateAccount, _depth + 1);
            }
            if (object.fileAppend != null) {
                if (!$util.isObject(object.fileAppend))
                    throw $TypeError(".proto.TransactionBody.fileAppend: object expected");
                message.fileAppend = $root.proto.FileAppendTransactionBody.fromObject(object.fileAppend, _depth + 1);
            }
            if (object.fileCreate != null) {
                if (!$util.isObject(object.fileCreate))
                    throw $TypeError(".proto.TransactionBody.fileCreate: object expected");
                message.fileCreate = $root.proto.FileCreateTransactionBody.fromObject(object.fileCreate, _depth + 1);
            }
            if (object.fileDelete != null) {
                if (!$util.isObject(object.fileDelete))
                    throw $TypeError(".proto.TransactionBody.fileDelete: object expected");
                message.fileDelete = $root.proto.FileDeleteTransactionBody.fromObject(object.fileDelete, _depth + 1);
            }
            if (object.fileUpdate != null) {
                if (!$util.isObject(object.fileUpdate))
                    throw $TypeError(".proto.TransactionBody.fileUpdate: object expected");
                message.fileUpdate = $root.proto.FileUpdateTransactionBody.fromObject(object.fileUpdate, _depth + 1);
            }
            if (object.systemDelete != null) {
                if (!$util.isObject(object.systemDelete))
                    throw $TypeError(".proto.TransactionBody.systemDelete: object expected");
                message.systemDelete = $root.proto.SystemDeleteTransactionBody.fromObject(object.systemDelete, _depth + 1);
            }
            if (object.systemUndelete != null) {
                if (!$util.isObject(object.systemUndelete))
                    throw $TypeError(".proto.TransactionBody.systemUndelete: object expected");
                message.systemUndelete = $root.proto.SystemUndeleteTransactionBody.fromObject(object.systemUndelete, _depth + 1);
            }
            if (object.contractDeleteInstance != null) {
                if (!$util.isObject(object.contractDeleteInstance))
                    throw $TypeError(".proto.TransactionBody.contractDeleteInstance: object expected");
                message.contractDeleteInstance = $root.proto.ContractDeleteTransactionBody.fromObject(object.contractDeleteInstance, _depth + 1);
            }
            if (object.freeze != null) {
                if (!$util.isObject(object.freeze))
                    throw $TypeError(".proto.TransactionBody.freeze: object expected");
                message.freeze = $root.proto.FreezeTransactionBody.fromObject(object.freeze, _depth + 1);
            }
            if (object.consensusCreateTopic != null) {
                if (!$util.isObject(object.consensusCreateTopic))
                    throw $TypeError(".proto.TransactionBody.consensusCreateTopic: object expected");
                message.consensusCreateTopic = $root.proto.ConsensusCreateTopicTransactionBody.fromObject(object.consensusCreateTopic, _depth + 1);
            }
            if (object.consensusUpdateTopic != null) {
                if (!$util.isObject(object.consensusUpdateTopic))
                    throw $TypeError(".proto.TransactionBody.consensusUpdateTopic: object expected");
                message.consensusUpdateTopic = $root.proto.ConsensusUpdateTopicTransactionBody.fromObject(object.consensusUpdateTopic, _depth + 1);
            }
            if (object.consensusDeleteTopic != null) {
                if (!$util.isObject(object.consensusDeleteTopic))
                    throw $TypeError(".proto.TransactionBody.consensusDeleteTopic: object expected");
                message.consensusDeleteTopic = $root.proto.ConsensusDeleteTopicTransactionBody.fromObject(object.consensusDeleteTopic, _depth + 1);
            }
            if (object.consensusSubmitMessage != null) {
                if (!$util.isObject(object.consensusSubmitMessage))
                    throw $TypeError(".proto.TransactionBody.consensusSubmitMessage: object expected");
                message.consensusSubmitMessage = $root.proto.ConsensusSubmitMessageTransactionBody.fromObject(object.consensusSubmitMessage, _depth + 1);
            }
            if (object.uncheckedSubmit != null) {
                if (!$util.isObject(object.uncheckedSubmit))
                    throw $TypeError(".proto.TransactionBody.uncheckedSubmit: object expected");
                message.uncheckedSubmit = $root.proto.UncheckedSubmitBody.fromObject(object.uncheckedSubmit, _depth + 1);
            }
            if (object.tokenCreation != null) {
                if (!$util.isObject(object.tokenCreation))
                    throw $TypeError(".proto.TransactionBody.tokenCreation: object expected");
                message.tokenCreation = $root.proto.TokenCreateTransactionBody.fromObject(object.tokenCreation, _depth + 1);
            }
            if (object.tokenFreeze != null) {
                if (!$util.isObject(object.tokenFreeze))
                    throw $TypeError(".proto.TransactionBody.tokenFreeze: object expected");
                message.tokenFreeze = $root.proto.TokenFreezeAccountTransactionBody.fromObject(object.tokenFreeze, _depth + 1);
            }
            if (object.tokenUnfreeze != null) {
                if (!$util.isObject(object.tokenUnfreeze))
                    throw $TypeError(".proto.TransactionBody.tokenUnfreeze: object expected");
                message.tokenUnfreeze = $root.proto.TokenUnfreezeAccountTransactionBody.fromObject(object.tokenUnfreeze, _depth + 1);
            }
            if (object.tokenGrantKyc != null) {
                if (!$util.isObject(object.tokenGrantKyc))
                    throw $TypeError(".proto.TransactionBody.tokenGrantKyc: object expected");
                message.tokenGrantKyc = $root.proto.TokenGrantKycTransactionBody.fromObject(object.tokenGrantKyc, _depth + 1);
            }
            if (object.tokenRevokeKyc != null) {
                if (!$util.isObject(object.tokenRevokeKyc))
                    throw $TypeError(".proto.TransactionBody.tokenRevokeKyc: object expected");
                message.tokenRevokeKyc = $root.proto.TokenRevokeKycTransactionBody.fromObject(object.tokenRevokeKyc, _depth + 1);
            }
            if (object.tokenDeletion != null) {
                if (!$util.isObject(object.tokenDeletion))
                    throw $TypeError(".proto.TransactionBody.tokenDeletion: object expected");
                message.tokenDeletion = $root.proto.TokenDeleteTransactionBody.fromObject(object.tokenDeletion, _depth + 1);
            }
            if (object.tokenUpdate != null) {
                if (!$util.isObject(object.tokenUpdate))
                    throw $TypeError(".proto.TransactionBody.tokenUpdate: object expected");
                message.tokenUpdate = $root.proto.TokenUpdateTransactionBody.fromObject(object.tokenUpdate, _depth + 1);
            }
            if (object.tokenMint != null) {
                if (!$util.isObject(object.tokenMint))
                    throw $TypeError(".proto.TransactionBody.tokenMint: object expected");
                message.tokenMint = $root.proto.TokenMintTransactionBody.fromObject(object.tokenMint, _depth + 1);
            }
            if (object.tokenBurn != null) {
                if (!$util.isObject(object.tokenBurn))
                    throw $TypeError(".proto.TransactionBody.tokenBurn: object expected");
                message.tokenBurn = $root.proto.TokenBurnTransactionBody.fromObject(object.tokenBurn, _depth + 1);
            }
            if (object.tokenWipe != null) {
                if (!$util.isObject(object.tokenWipe))
                    throw $TypeError(".proto.TransactionBody.tokenWipe: object expected");
                message.tokenWipe = $root.proto.TokenWipeAccountTransactionBody.fromObject(object.tokenWipe, _depth + 1);
            }
            if (object.tokenAssociate != null) {
                if (!$util.isObject(object.tokenAssociate))
                    throw $TypeError(".proto.TransactionBody.tokenAssociate: object expected");
                message.tokenAssociate = $root.proto.TokenAssociateTransactionBody.fromObject(object.tokenAssociate, _depth + 1);
            }
            if (object.tokenDissociate != null) {
                if (!$util.isObject(object.tokenDissociate))
                    throw $TypeError(".proto.TransactionBody.tokenDissociate: object expected");
                message.tokenDissociate = $root.proto.TokenDissociateTransactionBody.fromObject(object.tokenDissociate, _depth + 1);
            }
            if (object.scheduleCreate != null) {
                if (!$util.isObject(object.scheduleCreate))
                    throw $TypeError(".proto.TransactionBody.scheduleCreate: object expected");
                message.scheduleCreate = $root.proto.ScheduleCreateTransactionBody.fromObject(object.scheduleCreate, _depth + 1);
            }
            if (object.scheduleDelete != null) {
                if (!$util.isObject(object.scheduleDelete))
                    throw $TypeError(".proto.TransactionBody.scheduleDelete: object expected");
                message.scheduleDelete = $root.proto.ScheduleDeleteTransactionBody.fromObject(object.scheduleDelete, _depth + 1);
            }
            if (object.scheduleSign != null) {
                if (!$util.isObject(object.scheduleSign))
                    throw $TypeError(".proto.TransactionBody.scheduleSign: object expected");
                message.scheduleSign = $root.proto.ScheduleSignTransactionBody.fromObject(object.scheduleSign, _depth + 1);
            }
            if (object.tokenFeeScheduleUpdate != null) {
                if (!$util.isObject(object.tokenFeeScheduleUpdate))
                    throw $TypeError(".proto.TransactionBody.tokenFeeScheduleUpdate: object expected");
                message.tokenFeeScheduleUpdate = $root.proto.TokenFeeScheduleUpdateTransactionBody.fromObject(object.tokenFeeScheduleUpdate, _depth + 1);
            }
            if (object.tokenPause != null) {
                if (!$util.isObject(object.tokenPause))
                    throw $TypeError(".proto.TransactionBody.tokenPause: object expected");
                message.tokenPause = $root.proto.TokenPauseTransactionBody.fromObject(object.tokenPause, _depth + 1);
            }
            if (object.tokenUnpause != null) {
                if (!$util.isObject(object.tokenUnpause))
                    throw $TypeError(".proto.TransactionBody.tokenUnpause: object expected");
                message.tokenUnpause = $root.proto.TokenUnpauseTransactionBody.fromObject(object.tokenUnpause, _depth + 1);
            }
            if (object.cryptoApproveAllowance != null) {
                if (!$util.isObject(object.cryptoApproveAllowance))
                    throw $TypeError(".proto.TransactionBody.cryptoApproveAllowance: object expected");
                message.cryptoApproveAllowance = $root.proto.CryptoApproveAllowanceTransactionBody.fromObject(object.cryptoApproveAllowance, _depth + 1);
            }
            if (object.cryptoDeleteAllowance != null) {
                if (!$util.isObject(object.cryptoDeleteAllowance))
                    throw $TypeError(".proto.TransactionBody.cryptoDeleteAllowance: object expected");
                message.cryptoDeleteAllowance = $root.proto.CryptoDeleteAllowanceTransactionBody.fromObject(object.cryptoDeleteAllowance, _depth + 1);
            }
            if (object.ethereumTransaction != null) {
                if (!$util.isObject(object.ethereumTransaction))
                    throw $TypeError(".proto.TransactionBody.ethereumTransaction: object expected");
                message.ethereumTransaction = $root.proto.EthereumTransactionBody.fromObject(object.ethereumTransaction, _depth + 1);
            }
            if (object.nodeStakeUpdate != null) {
                if (!$util.isObject(object.nodeStakeUpdate))
                    throw $TypeError(".proto.TransactionBody.nodeStakeUpdate: object expected");
                message.nodeStakeUpdate = $root.proto.NodeStakeUpdateTransactionBody.fromObject(object.nodeStakeUpdate, _depth + 1);
            }
            if (object.utilPrng != null) {
                if (!$util.isObject(object.utilPrng))
                    throw $TypeError(".proto.TransactionBody.utilPrng: object expected");
                message.utilPrng = $root.proto.UtilPrngTransactionBody.fromObject(object.utilPrng, _depth + 1);
            }
            if (object.tokenUpdateNfts != null) {
                if (!$util.isObject(object.tokenUpdateNfts))
                    throw $TypeError(".proto.TransactionBody.tokenUpdateNfts: object expected");
                message.tokenUpdateNfts = $root.proto.TokenUpdateNftsTransactionBody.fromObject(object.tokenUpdateNfts, _depth + 1);
            }
            if (object.nodeCreate != null) {
                if (!$util.isObject(object.nodeCreate))
                    throw $TypeError(".proto.TransactionBody.nodeCreate: object expected");
                message.nodeCreate = $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.fromObject(object.nodeCreate, _depth + 1);
            }
            if (object.nodeUpdate != null) {
                if (!$util.isObject(object.nodeUpdate))
                    throw $TypeError(".proto.TransactionBody.nodeUpdate: object expected");
                message.nodeUpdate = $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.fromObject(object.nodeUpdate, _depth + 1);
            }
            if (object.nodeDelete != null) {
                if (!$util.isObject(object.nodeDelete))
                    throw $TypeError(".proto.TransactionBody.nodeDelete: object expected");
                message.nodeDelete = $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.fromObject(object.nodeDelete, _depth + 1);
            }
            if (object.tokenReject != null) {
                if (!$util.isObject(object.tokenReject))
                    throw $TypeError(".proto.TransactionBody.tokenReject: object expected");
                message.tokenReject = $root.proto.TokenRejectTransactionBody.fromObject(object.tokenReject, _depth + 1);
            }
            if (object.tokenAirdrop != null) {
                if (!$util.isObject(object.tokenAirdrop))
                    throw $TypeError(".proto.TransactionBody.tokenAirdrop: object expected");
                message.tokenAirdrop = $root.proto.TokenAirdropTransactionBody.fromObject(object.tokenAirdrop, _depth + 1);
            }
            if (object.tokenCancelAirdrop != null) {
                if (!$util.isObject(object.tokenCancelAirdrop))
                    throw $TypeError(".proto.TransactionBody.tokenCancelAirdrop: object expected");
                message.tokenCancelAirdrop = $root.proto.TokenCancelAirdropTransactionBody.fromObject(object.tokenCancelAirdrop, _depth + 1);
            }
            if (object.tokenClaimAirdrop != null) {
                if (!$util.isObject(object.tokenClaimAirdrop))
                    throw $TypeError(".proto.TransactionBody.tokenClaimAirdrop: object expected");
                message.tokenClaimAirdrop = $root.proto.TokenClaimAirdropTransactionBody.fromObject(object.tokenClaimAirdrop, _depth + 1);
            }
            if (object.stateSignatureTransaction != null) {
                if (!$util.isObject(object.stateSignatureTransaction))
                    throw $TypeError(".proto.TransactionBody.stateSignatureTransaction: object expected");
                message.stateSignatureTransaction = $root.com.hedera.hapi.platform.event.StateSignatureTransaction.fromObject(object.stateSignatureTransaction, _depth + 1);
            }
            if (object.hintsPreprocessingVote != null) {
                if (!$util.isObject(object.hintsPreprocessingVote))
                    throw $TypeError(".proto.TransactionBody.hintsPreprocessingVote: object expected");
                message.hintsPreprocessingVote = $root.com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.fromObject(object.hintsPreprocessingVote, _depth + 1);
            }
            if (object.hintsKeyPublication != null) {
                if (!$util.isObject(object.hintsKeyPublication))
                    throw $TypeError(".proto.TransactionBody.hintsKeyPublication: object expected");
                message.hintsKeyPublication = $root.com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.fromObject(object.hintsKeyPublication, _depth + 1);
            }
            if (object.hintsPartialSignature != null) {
                if (!$util.isObject(object.hintsPartialSignature))
                    throw $TypeError(".proto.TransactionBody.hintsPartialSignature: object expected");
                message.hintsPartialSignature = $root.com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.fromObject(object.hintsPartialSignature, _depth + 1);
            }
            if (object.historyProofSignature != null) {
                if (!$util.isObject(object.historyProofSignature))
                    throw $TypeError(".proto.TransactionBody.historyProofSignature: object expected");
                message.historyProofSignature = $root.com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.fromObject(object.historyProofSignature, _depth + 1);
            }
            if (object.historyProofKeyPublication != null) {
                if (!$util.isObject(object.historyProofKeyPublication))
                    throw $TypeError(".proto.TransactionBody.historyProofKeyPublication: object expected");
                message.historyProofKeyPublication = $root.com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.fromObject(object.historyProofKeyPublication, _depth + 1);
            }
            if (object.historyProofVote != null) {
                if (!$util.isObject(object.historyProofVote))
                    throw $TypeError(".proto.TransactionBody.historyProofVote: object expected");
                message.historyProofVote = $root.com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.fromObject(object.historyProofVote, _depth + 1);
            }
            if (object.crsPublication != null) {
                if (!$util.isObject(object.crsPublication))
                    throw $TypeError(".proto.TransactionBody.crsPublication: object expected");
                message.crsPublication = $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.fromObject(object.crsPublication, _depth + 1);
            }
            if (object.atomicBatch != null) {
                if (!$util.isObject(object.atomicBatch))
                    throw $TypeError(".proto.TransactionBody.atomicBatch: object expected");
                message.atomicBatch = $root.proto.AtomicBatchTransactionBody.fromObject(object.atomicBatch, _depth + 1);
            }
            if (object.hookStore != null) {
                if (!$util.isObject(object.hookStore))
                    throw $TypeError(".proto.TransactionBody.hookStore: object expected");
                message.hookStore = $root.com.hedera.hapi.node.hooks.HookStoreTransactionBody.fromObject(object.hookStore, _depth + 1);
            }
            if (object.hookDispatch != null) {
                if (!$util.isObject(object.hookDispatch))
                    throw $TypeError(".proto.TransactionBody.hookDispatch: object expected");
                message.hookDispatch = $root.com.hedera.hapi.node.hooks.HookDispatchTransactionBody.fromObject(object.hookDispatch, _depth + 1);
            }
            if (object.ledgerIdPublication != null) {
                if (!$util.isObject(object.ledgerIdPublication))
                    throw $TypeError(".proto.TransactionBody.ledgerIdPublication: object expected");
                message.ledgerIdPublication = $root.com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.fromObject(object.ledgerIdPublication, _depth + 1);
            }
            if (object.registeredNodeCreate != null) {
                if (!$util.isObject(object.registeredNodeCreate))
                    throw $TypeError(".proto.TransactionBody.registeredNodeCreate: object expected");
                message.registeredNodeCreate = $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.fromObject(object.registeredNodeCreate, _depth + 1);
            }
            if (object.registeredNodeUpdate != null) {
                if (!$util.isObject(object.registeredNodeUpdate))
                    throw $TypeError(".proto.TransactionBody.registeredNodeUpdate: object expected");
                message.registeredNodeUpdate = $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.fromObject(object.registeredNodeUpdate, _depth + 1);
            }
            if (object.registeredNodeDelete != null) {
                if (!$util.isObject(object.registeredNodeDelete))
                    throw $TypeError(".proto.TransactionBody.registeredNodeDelete: object expected");
                message.registeredNodeDelete = $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.fromObject(object.registeredNodeDelete, _depth + 1);
            }
            if (object.migrationRootHashVote != null) {
                if (!$util.isObject(object.migrationRootHashVote))
                    throw $TypeError(".proto.TransactionBody.migrationRootHashVote: object expected");
                message.migrationRootHashVote = $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.fromObject(object.migrationRootHashVote, _depth + 1);
            }
            if (object.maxCustomFees) {
                if (!$Array.isArray(object.maxCustomFees))
                    throw $TypeError(".proto.TransactionBody.maxCustomFees: array expected");
                message.maxCustomFees = $Array(object.maxCustomFees.length);
                for (let i = 0; i < object.maxCustomFees.length; ++i) {
                    if (!$util.isObject(object.maxCustomFees[i]))
                        throw $TypeError(".proto.TransactionBody.maxCustomFees: object expected");
                    message.maxCustomFees[i] = $root.proto.CustomFeeLimit.fromObject(object.maxCustomFees[i], _depth + 1);
                }
            }
            if (object.highVolume != null)
                if (object.highVolume)
                    message.highVolume = $Boolean(object.highVolume);
            return message;
        };

        /**
         * Creates a plain object from a TransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionBody
         * @static
         * @param {proto.TransactionBody} message TransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.maxCustomFees = [];
            if (options.defaults) {
                object.transactionID = null;
                object.nodeAccountID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.transactionFee = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.transactionFee = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.transactionValidDuration = null;
                object.generateRecord = false;
                object.memo = "";
                object.batchKey = null;
                object.highVolume = false;
            }
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID"))
                object.transactionID = $root.proto.TransactionID.toObject(message.transactionID, options, _depth + 1);
            if (message.nodeAccountID != null && $Object.hasOwnProperty.call(message, "nodeAccountID"))
                object.nodeAccountID = $root.proto.AccountID.toObject(message.nodeAccountID, options, _depth + 1);
            if (message.transactionFee != null && $Object.hasOwnProperty.call(message, "transactionFee"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.transactionFee = typeof message.transactionFee === "number" ? $BigInt(message.transactionFee) : $util.Long.fromBits(message.transactionFee.low >>> 0, message.transactionFee.high >>> 0, true).toBigInt();
                else if (typeof message.transactionFee === "number")
                    object.transactionFee = options.longs === $String ? $String(message.transactionFee) : message.transactionFee;
                else
                    object.transactionFee = options.longs === $String ? $util.Long.prototype.toString.call(message.transactionFee) : options.longs === $Number ? new $util.LongBits(message.transactionFee.low >>> 0, message.transactionFee.high >>> 0).toNumber(true) : message.transactionFee;
            if (message.transactionValidDuration != null && $Object.hasOwnProperty.call(message, "transactionValidDuration"))
                object.transactionValidDuration = $root.proto.Duration.toObject(message.transactionValidDuration, options, _depth + 1);
            if (message.generateRecord != null && $Object.hasOwnProperty.call(message, "generateRecord"))
                object.generateRecord = message.generateRecord;
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.contractCall != null && $Object.hasOwnProperty.call(message, "contractCall")) {
                object.contractCall = $root.proto.ContractCallTransactionBody.toObject(message.contractCall, options, _depth + 1);
                if (options.oneofs)
                    object.data = "contractCall";
            }
            if (message.contractCreateInstance != null && $Object.hasOwnProperty.call(message, "contractCreateInstance")) {
                object.contractCreateInstance = $root.proto.ContractCreateTransactionBody.toObject(message.contractCreateInstance, options, _depth + 1);
                if (options.oneofs)
                    object.data = "contractCreateInstance";
            }
            if (message.contractUpdateInstance != null && $Object.hasOwnProperty.call(message, "contractUpdateInstance")) {
                object.contractUpdateInstance = $root.proto.ContractUpdateTransactionBody.toObject(message.contractUpdateInstance, options, _depth + 1);
                if (options.oneofs)
                    object.data = "contractUpdateInstance";
            }
            if (message.cryptoAddLiveHash != null && $Object.hasOwnProperty.call(message, "cryptoAddLiveHash")) {
                object.cryptoAddLiveHash = $root.proto.CryptoAddLiveHashTransactionBody.toObject(message.cryptoAddLiveHash, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoAddLiveHash";
            }
            if (message.cryptoCreateAccount != null && $Object.hasOwnProperty.call(message, "cryptoCreateAccount")) {
                object.cryptoCreateAccount = $root.proto.CryptoCreateTransactionBody.toObject(message.cryptoCreateAccount, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoCreateAccount";
            }
            if (message.cryptoDelete != null && $Object.hasOwnProperty.call(message, "cryptoDelete")) {
                object.cryptoDelete = $root.proto.CryptoDeleteTransactionBody.toObject(message.cryptoDelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoDelete";
            }
            if (message.cryptoDeleteLiveHash != null && $Object.hasOwnProperty.call(message, "cryptoDeleteLiveHash")) {
                object.cryptoDeleteLiveHash = $root.proto.CryptoDeleteLiveHashTransactionBody.toObject(message.cryptoDeleteLiveHash, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoDeleteLiveHash";
            }
            if (message.cryptoTransfer != null && $Object.hasOwnProperty.call(message, "cryptoTransfer")) {
                object.cryptoTransfer = $root.proto.CryptoTransferTransactionBody.toObject(message.cryptoTransfer, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoTransfer";
            }
            if (message.cryptoUpdateAccount != null && $Object.hasOwnProperty.call(message, "cryptoUpdateAccount")) {
                object.cryptoUpdateAccount = $root.proto.CryptoUpdateTransactionBody.toObject(message.cryptoUpdateAccount, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoUpdateAccount";
            }
            if (message.fileAppend != null && $Object.hasOwnProperty.call(message, "fileAppend")) {
                object.fileAppend = $root.proto.FileAppendTransactionBody.toObject(message.fileAppend, options, _depth + 1);
                if (options.oneofs)
                    object.data = "fileAppend";
            }
            if (message.fileCreate != null && $Object.hasOwnProperty.call(message, "fileCreate")) {
                object.fileCreate = $root.proto.FileCreateTransactionBody.toObject(message.fileCreate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "fileCreate";
            }
            if (message.fileDelete != null && $Object.hasOwnProperty.call(message, "fileDelete")) {
                object.fileDelete = $root.proto.FileDeleteTransactionBody.toObject(message.fileDelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "fileDelete";
            }
            if (message.fileUpdate != null && $Object.hasOwnProperty.call(message, "fileUpdate")) {
                object.fileUpdate = $root.proto.FileUpdateTransactionBody.toObject(message.fileUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "fileUpdate";
            }
            if (message.systemDelete != null && $Object.hasOwnProperty.call(message, "systemDelete")) {
                object.systemDelete = $root.proto.SystemDeleteTransactionBody.toObject(message.systemDelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "systemDelete";
            }
            if (message.systemUndelete != null && $Object.hasOwnProperty.call(message, "systemUndelete")) {
                object.systemUndelete = $root.proto.SystemUndeleteTransactionBody.toObject(message.systemUndelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "systemUndelete";
            }
            if (message.contractDeleteInstance != null && $Object.hasOwnProperty.call(message, "contractDeleteInstance")) {
                object.contractDeleteInstance = $root.proto.ContractDeleteTransactionBody.toObject(message.contractDeleteInstance, options, _depth + 1);
                if (options.oneofs)
                    object.data = "contractDeleteInstance";
            }
            if (message.freeze != null && $Object.hasOwnProperty.call(message, "freeze")) {
                object.freeze = $root.proto.FreezeTransactionBody.toObject(message.freeze, options, _depth + 1);
                if (options.oneofs)
                    object.data = "freeze";
            }
            if (message.consensusCreateTopic != null && $Object.hasOwnProperty.call(message, "consensusCreateTopic")) {
                object.consensusCreateTopic = $root.proto.ConsensusCreateTopicTransactionBody.toObject(message.consensusCreateTopic, options, _depth + 1);
                if (options.oneofs)
                    object.data = "consensusCreateTopic";
            }
            if (message.consensusUpdateTopic != null && $Object.hasOwnProperty.call(message, "consensusUpdateTopic")) {
                object.consensusUpdateTopic = $root.proto.ConsensusUpdateTopicTransactionBody.toObject(message.consensusUpdateTopic, options, _depth + 1);
                if (options.oneofs)
                    object.data = "consensusUpdateTopic";
            }
            if (message.consensusDeleteTopic != null && $Object.hasOwnProperty.call(message, "consensusDeleteTopic")) {
                object.consensusDeleteTopic = $root.proto.ConsensusDeleteTopicTransactionBody.toObject(message.consensusDeleteTopic, options, _depth + 1);
                if (options.oneofs)
                    object.data = "consensusDeleteTopic";
            }
            if (message.consensusSubmitMessage != null && $Object.hasOwnProperty.call(message, "consensusSubmitMessage")) {
                object.consensusSubmitMessage = $root.proto.ConsensusSubmitMessageTransactionBody.toObject(message.consensusSubmitMessage, options, _depth + 1);
                if (options.oneofs)
                    object.data = "consensusSubmitMessage";
            }
            if (message.uncheckedSubmit != null && $Object.hasOwnProperty.call(message, "uncheckedSubmit")) {
                object.uncheckedSubmit = $root.proto.UncheckedSubmitBody.toObject(message.uncheckedSubmit, options, _depth + 1);
                if (options.oneofs)
                    object.data = "uncheckedSubmit";
            }
            if (message.tokenCreation != null && $Object.hasOwnProperty.call(message, "tokenCreation")) {
                object.tokenCreation = $root.proto.TokenCreateTransactionBody.toObject(message.tokenCreation, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenCreation";
            }
            if (message.tokenFreeze != null && $Object.hasOwnProperty.call(message, "tokenFreeze")) {
                object.tokenFreeze = $root.proto.TokenFreezeAccountTransactionBody.toObject(message.tokenFreeze, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenFreeze";
            }
            if (message.tokenUnfreeze != null && $Object.hasOwnProperty.call(message, "tokenUnfreeze")) {
                object.tokenUnfreeze = $root.proto.TokenUnfreezeAccountTransactionBody.toObject(message.tokenUnfreeze, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenUnfreeze";
            }
            if (message.tokenGrantKyc != null && $Object.hasOwnProperty.call(message, "tokenGrantKyc")) {
                object.tokenGrantKyc = $root.proto.TokenGrantKycTransactionBody.toObject(message.tokenGrantKyc, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenGrantKyc";
            }
            if (message.tokenRevokeKyc != null && $Object.hasOwnProperty.call(message, "tokenRevokeKyc")) {
                object.tokenRevokeKyc = $root.proto.TokenRevokeKycTransactionBody.toObject(message.tokenRevokeKyc, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenRevokeKyc";
            }
            if (message.tokenDeletion != null && $Object.hasOwnProperty.call(message, "tokenDeletion")) {
                object.tokenDeletion = $root.proto.TokenDeleteTransactionBody.toObject(message.tokenDeletion, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenDeletion";
            }
            if (message.tokenUpdate != null && $Object.hasOwnProperty.call(message, "tokenUpdate")) {
                object.tokenUpdate = $root.proto.TokenUpdateTransactionBody.toObject(message.tokenUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenUpdate";
            }
            if (message.tokenMint != null && $Object.hasOwnProperty.call(message, "tokenMint")) {
                object.tokenMint = $root.proto.TokenMintTransactionBody.toObject(message.tokenMint, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenMint";
            }
            if (message.tokenBurn != null && $Object.hasOwnProperty.call(message, "tokenBurn")) {
                object.tokenBurn = $root.proto.TokenBurnTransactionBody.toObject(message.tokenBurn, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenBurn";
            }
            if (message.tokenWipe != null && $Object.hasOwnProperty.call(message, "tokenWipe")) {
                object.tokenWipe = $root.proto.TokenWipeAccountTransactionBody.toObject(message.tokenWipe, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenWipe";
            }
            if (message.tokenAssociate != null && $Object.hasOwnProperty.call(message, "tokenAssociate")) {
                object.tokenAssociate = $root.proto.TokenAssociateTransactionBody.toObject(message.tokenAssociate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenAssociate";
            }
            if (message.tokenDissociate != null && $Object.hasOwnProperty.call(message, "tokenDissociate")) {
                object.tokenDissociate = $root.proto.TokenDissociateTransactionBody.toObject(message.tokenDissociate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenDissociate";
            }
            if (message.scheduleCreate != null && $Object.hasOwnProperty.call(message, "scheduleCreate")) {
                object.scheduleCreate = $root.proto.ScheduleCreateTransactionBody.toObject(message.scheduleCreate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "scheduleCreate";
            }
            if (message.scheduleDelete != null && $Object.hasOwnProperty.call(message, "scheduleDelete")) {
                object.scheduleDelete = $root.proto.ScheduleDeleteTransactionBody.toObject(message.scheduleDelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "scheduleDelete";
            }
            if (message.scheduleSign != null && $Object.hasOwnProperty.call(message, "scheduleSign")) {
                object.scheduleSign = $root.proto.ScheduleSignTransactionBody.toObject(message.scheduleSign, options, _depth + 1);
                if (options.oneofs)
                    object.data = "scheduleSign";
            }
            if (message.tokenFeeScheduleUpdate != null && $Object.hasOwnProperty.call(message, "tokenFeeScheduleUpdate")) {
                object.tokenFeeScheduleUpdate = $root.proto.TokenFeeScheduleUpdateTransactionBody.toObject(message.tokenFeeScheduleUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenFeeScheduleUpdate";
            }
            if (message.tokenPause != null && $Object.hasOwnProperty.call(message, "tokenPause")) {
                object.tokenPause = $root.proto.TokenPauseTransactionBody.toObject(message.tokenPause, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenPause";
            }
            if (message.tokenUnpause != null && $Object.hasOwnProperty.call(message, "tokenUnpause")) {
                object.tokenUnpause = $root.proto.TokenUnpauseTransactionBody.toObject(message.tokenUnpause, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenUnpause";
            }
            if (message.cryptoApproveAllowance != null && $Object.hasOwnProperty.call(message, "cryptoApproveAllowance")) {
                object.cryptoApproveAllowance = $root.proto.CryptoApproveAllowanceTransactionBody.toObject(message.cryptoApproveAllowance, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoApproveAllowance";
            }
            if (message.cryptoDeleteAllowance != null && $Object.hasOwnProperty.call(message, "cryptoDeleteAllowance")) {
                object.cryptoDeleteAllowance = $root.proto.CryptoDeleteAllowanceTransactionBody.toObject(message.cryptoDeleteAllowance, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoDeleteAllowance";
            }
            if (message.ethereumTransaction != null && $Object.hasOwnProperty.call(message, "ethereumTransaction")) {
                object.ethereumTransaction = $root.proto.EthereumTransactionBody.toObject(message.ethereumTransaction, options, _depth + 1);
                if (options.oneofs)
                    object.data = "ethereumTransaction";
            }
            if (message.nodeStakeUpdate != null && $Object.hasOwnProperty.call(message, "nodeStakeUpdate")) {
                object.nodeStakeUpdate = $root.proto.NodeStakeUpdateTransactionBody.toObject(message.nodeStakeUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "nodeStakeUpdate";
            }
            if (message.utilPrng != null && $Object.hasOwnProperty.call(message, "utilPrng")) {
                object.utilPrng = $root.proto.UtilPrngTransactionBody.toObject(message.utilPrng, options, _depth + 1);
                if (options.oneofs)
                    object.data = "utilPrng";
            }
            if (message.tokenUpdateNfts != null && $Object.hasOwnProperty.call(message, "tokenUpdateNfts")) {
                object.tokenUpdateNfts = $root.proto.TokenUpdateNftsTransactionBody.toObject(message.tokenUpdateNfts, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenUpdateNfts";
            }
            if (message.nodeCreate != null && $Object.hasOwnProperty.call(message, "nodeCreate")) {
                object.nodeCreate = $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.toObject(message.nodeCreate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "nodeCreate";
            }
            if (message.nodeUpdate != null && $Object.hasOwnProperty.call(message, "nodeUpdate")) {
                object.nodeUpdate = $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.toObject(message.nodeUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "nodeUpdate";
            }
            if (message.nodeDelete != null && $Object.hasOwnProperty.call(message, "nodeDelete")) {
                object.nodeDelete = $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.toObject(message.nodeDelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "nodeDelete";
            }
            if (message.tokenReject != null && $Object.hasOwnProperty.call(message, "tokenReject")) {
                object.tokenReject = $root.proto.TokenRejectTransactionBody.toObject(message.tokenReject, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenReject";
            }
            if (message.tokenAirdrop != null && $Object.hasOwnProperty.call(message, "tokenAirdrop")) {
                object.tokenAirdrop = $root.proto.TokenAirdropTransactionBody.toObject(message.tokenAirdrop, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenAirdrop";
            }
            if (message.tokenCancelAirdrop != null && $Object.hasOwnProperty.call(message, "tokenCancelAirdrop")) {
                object.tokenCancelAirdrop = $root.proto.TokenCancelAirdropTransactionBody.toObject(message.tokenCancelAirdrop, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenCancelAirdrop";
            }
            if (message.tokenClaimAirdrop != null && $Object.hasOwnProperty.call(message, "tokenClaimAirdrop")) {
                object.tokenClaimAirdrop = $root.proto.TokenClaimAirdropTransactionBody.toObject(message.tokenClaimAirdrop, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenClaimAirdrop";
            }
            if (message.stateSignatureTransaction != null && $Object.hasOwnProperty.call(message, "stateSignatureTransaction")) {
                object.stateSignatureTransaction = $root.com.hedera.hapi.platform.event.StateSignatureTransaction.toObject(message.stateSignatureTransaction, options, _depth + 1);
                if (options.oneofs)
                    object.data = "stateSignatureTransaction";
            }
            if (message.hintsPreprocessingVote != null && $Object.hasOwnProperty.call(message, "hintsPreprocessingVote")) {
                object.hintsPreprocessingVote = $root.com.hedera.hapi.services.auxiliary.hints.HintsPreprocessingVoteTransactionBody.toObject(message.hintsPreprocessingVote, options, _depth + 1);
                if (options.oneofs)
                    object.data = "hintsPreprocessingVote";
            }
            if (message.hintsKeyPublication != null && $Object.hasOwnProperty.call(message, "hintsKeyPublication")) {
                object.hintsKeyPublication = $root.com.hedera.hapi.services.auxiliary.hints.HintsKeyPublicationTransactionBody.toObject(message.hintsKeyPublication, options, _depth + 1);
                if (options.oneofs)
                    object.data = "hintsKeyPublication";
            }
            if (message.hintsPartialSignature != null && $Object.hasOwnProperty.call(message, "hintsPartialSignature")) {
                object.hintsPartialSignature = $root.com.hedera.hapi.services.auxiliary.hints.HintsPartialSignatureTransactionBody.toObject(message.hintsPartialSignature, options, _depth + 1);
                if (options.oneofs)
                    object.data = "hintsPartialSignature";
            }
            if (message.historyProofSignature != null && $Object.hasOwnProperty.call(message, "historyProofSignature")) {
                object.historyProofSignature = $root.com.hedera.hapi.services.auxiliary.history.HistoryProofSignatureTransactionBody.toObject(message.historyProofSignature, options, _depth + 1);
                if (options.oneofs)
                    object.data = "historyProofSignature";
            }
            if (message.historyProofKeyPublication != null && $Object.hasOwnProperty.call(message, "historyProofKeyPublication")) {
                object.historyProofKeyPublication = $root.com.hedera.hapi.services.auxiliary.history.HistoryProofKeyPublicationTransactionBody.toObject(message.historyProofKeyPublication, options, _depth + 1);
                if (options.oneofs)
                    object.data = "historyProofKeyPublication";
            }
            if (message.historyProofVote != null && $Object.hasOwnProperty.call(message, "historyProofVote")) {
                object.historyProofVote = $root.com.hedera.hapi.services.auxiliary.history.HistoryProofVoteTransactionBody.toObject(message.historyProofVote, options, _depth + 1);
                if (options.oneofs)
                    object.data = "historyProofVote";
            }
            if (message.crsPublication != null && $Object.hasOwnProperty.call(message, "crsPublication")) {
                object.crsPublication = $root.com.hedera.hapi.services.auxiliary.hints.CrsPublicationTransactionBody.toObject(message.crsPublication, options, _depth + 1);
                if (options.oneofs)
                    object.data = "crsPublication";
            }
            if (message.batchKey != null && $Object.hasOwnProperty.call(message, "batchKey"))
                object.batchKey = $root.proto.Key.toObject(message.batchKey, options, _depth + 1);
            if (message.atomicBatch != null && $Object.hasOwnProperty.call(message, "atomicBatch")) {
                object.atomicBatch = $root.proto.AtomicBatchTransactionBody.toObject(message.atomicBatch, options, _depth + 1);
                if (options.oneofs)
                    object.data = "atomicBatch";
            }
            if (message.hookStore != null && $Object.hasOwnProperty.call(message, "hookStore")) {
                object.hookStore = $root.com.hedera.hapi.node.hooks.HookStoreTransactionBody.toObject(message.hookStore, options, _depth + 1);
                if (options.oneofs)
                    object.data = "hookStore";
            }
            if (message.hookDispatch != null && $Object.hasOwnProperty.call(message, "hookDispatch")) {
                object.hookDispatch = $root.com.hedera.hapi.node.hooks.HookDispatchTransactionBody.toObject(message.hookDispatch, options, _depth + 1);
                if (options.oneofs)
                    object.data = "hookDispatch";
            }
            if (message.ledgerIdPublication != null && $Object.hasOwnProperty.call(message, "ledgerIdPublication")) {
                object.ledgerIdPublication = $root.com.hedera.hapi.node.tss.LedgerIdPublicationTransactionBody.toObject(message.ledgerIdPublication, options, _depth + 1);
                if (options.oneofs)
                    object.data = "ledgerIdPublication";
            }
            if (message.registeredNodeCreate != null && $Object.hasOwnProperty.call(message, "registeredNodeCreate")) {
                object.registeredNodeCreate = $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.toObject(message.registeredNodeCreate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "registeredNodeCreate";
            }
            if (message.registeredNodeUpdate != null && $Object.hasOwnProperty.call(message, "registeredNodeUpdate")) {
                object.registeredNodeUpdate = $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.toObject(message.registeredNodeUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "registeredNodeUpdate";
            }
            if (message.registeredNodeDelete != null && $Object.hasOwnProperty.call(message, "registeredNodeDelete")) {
                object.registeredNodeDelete = $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.toObject(message.registeredNodeDelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "registeredNodeDelete";
            }
            if (message.migrationRootHashVote != null && $Object.hasOwnProperty.call(message, "migrationRootHashVote")) {
                object.migrationRootHashVote = $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.toObject(message.migrationRootHashVote, options, _depth + 1);
                if (options.oneofs)
                    object.data = "migrationRootHashVote";
            }
            if (message.maxCustomFees && message.maxCustomFees.length) {
                object.maxCustomFees = $Array(message.maxCustomFees.length);
                for (let j = 0; j < message.maxCustomFees.length; ++j)
                    object.maxCustomFees[j] = $root.proto.CustomFeeLimit.toObject(message.maxCustomFees[j], options, _depth + 1);
            }
            if (message.highVolume != null && $Object.hasOwnProperty.call(message, "highVolume"))
                object.highVolume = message.highVolume;
            return object;
        };

        /**
         * Converts this TransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionBody.prototype.toJSON = function() {
            return TransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionBody
         * @function getTypeUrl
         * @memberof proto.TransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionBody";
        };

        return TransactionBody;
    })();

    proto.AtomicBatchTransactionBody = (function() {

        /**
         * Properties of an AtomicBatchTransactionBody.
         * @typedef {Object} proto.AtomicBatchTransactionBody.$Properties
         * @property {Array.<Uint8Array>|null} [transactions] A list of signed bytes that represent the batch transactions.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AtomicBatchTransactionBody.
         * @memberof proto
         * @interface IAtomicBatchTransactionBody
         * @augments proto.AtomicBatchTransactionBody.$Properties
         * @deprecated Use proto.AtomicBatchTransactionBody.$Properties instead.
         */

        /**
         * Shape of an AtomicBatchTransactionBody.
         * @typedef {proto.AtomicBatchTransactionBody.$Properties} proto.AtomicBatchTransactionBody.$Shape
         */

        /**
         * Constructs a new AtomicBatchTransactionBody.
         * @memberof proto
         * @classdesc A transaction body for handling a set of transactions atomically.
         * @constructor
         * @param {proto.AtomicBatchTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AtomicBatchTransactionBody = function (properties) {
            this.transactions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of signed bytes that represent the batch transactions.
         * @member {Array.<Uint8Array>} transactions
         * @memberof proto.AtomicBatchTransactionBody
         * @instance
         */
        AtomicBatchTransactionBody.prototype.transactions = $util.emptyArray;

        /**
         * Creates a new AtomicBatchTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.AtomicBatchTransactionBody
         * @static
         * @param {proto.AtomicBatchTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.AtomicBatchTransactionBody} AtomicBatchTransactionBody instance
         * @type {{
         *   (properties: proto.AtomicBatchTransactionBody.$Shape): proto.AtomicBatchTransactionBody & proto.AtomicBatchTransactionBody.$Shape;
         *   (properties?: proto.AtomicBatchTransactionBody.$Properties): proto.AtomicBatchTransactionBody;
         * }}
         */
        AtomicBatchTransactionBody.create = function(properties) {
            return new AtomicBatchTransactionBody(properties);
        };

        /**
         * Encodes the specified AtomicBatchTransactionBody message. Does not implicitly {@link proto.AtomicBatchTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.AtomicBatchTransactionBody
         * @static
         * @param {proto.AtomicBatchTransactionBody.$Properties} message AtomicBatchTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AtomicBatchTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.transactions != null && message.transactions.length)
                for (let i = 0; i < message.transactions.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.transactions[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AtomicBatchTransactionBody message, length delimited. Does not implicitly {@link proto.AtomicBatchTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AtomicBatchTransactionBody
         * @static
         * @param {proto.AtomicBatchTransactionBody.$Properties} message AtomicBatchTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AtomicBatchTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AtomicBatchTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AtomicBatchTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AtomicBatchTransactionBody & proto.AtomicBatchTransactionBody.$Shape} AtomicBatchTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AtomicBatchTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.AtomicBatchTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.transactions && message.transactions.length))
                            message.transactions = [];
                        message.transactions.push(reader.bytes());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AtomicBatchTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AtomicBatchTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AtomicBatchTransactionBody & proto.AtomicBatchTransactionBody.$Shape} AtomicBatchTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AtomicBatchTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AtomicBatchTransactionBody message.
         * @function verify
         * @memberof proto.AtomicBatchTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AtomicBatchTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.transactions != null && $Object.hasOwnProperty.call(message, "transactions")) {
                if (!$Array.isArray(message.transactions))
                    return "transactions: array expected";
                for (let i = 0; i < message.transactions.length; ++i)
                    if (!(message.transactions[i] && typeof message.transactions[i].length === "number" || $util.isString(message.transactions[i])))
                        return "transactions: buffer[] expected";
            }
            return null;
        };

        /**
         * Creates an AtomicBatchTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AtomicBatchTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AtomicBatchTransactionBody} AtomicBatchTransactionBody
         */
        AtomicBatchTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.AtomicBatchTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.AtomicBatchTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.AtomicBatchTransactionBody();
            if (object.transactions) {
                if (!$Array.isArray(object.transactions))
                    throw $TypeError(".proto.AtomicBatchTransactionBody.transactions: array expected");
                message.transactions = $Array(object.transactions.length);
                for (let i = 0; i < object.transactions.length; ++i)
                    if (typeof object.transactions[i] === "string")
                        $util.base64.decode(object.transactions[i], message.transactions[i] = $util.newBuffer($util.base64.length(object.transactions[i])), 0);
                    else if (object.transactions[i].length >= 0)
                        message.transactions[i] = object.transactions[i];
            }
            return message;
        };

        /**
         * Creates a plain object from an AtomicBatchTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AtomicBatchTransactionBody
         * @static
         * @param {proto.AtomicBatchTransactionBody} message AtomicBatchTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AtomicBatchTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.transactions = [];
            if (message.transactions && message.transactions.length) {
                object.transactions = $Array(message.transactions.length);
                for (let j = 0; j < message.transactions.length; ++j)
                    object.transactions[j] = options.bytes === $String ? $util.base64.encode(message.transactions[j], 0, message.transactions[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.transactions[j]) : message.transactions[j];
            }
            return object;
        };

        /**
         * Converts this AtomicBatchTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.AtomicBatchTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AtomicBatchTransactionBody.prototype.toJSON = function() {
            return AtomicBatchTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AtomicBatchTransactionBody
         * @function getTypeUrl
         * @memberof proto.AtomicBatchTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AtomicBatchTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AtomicBatchTransactionBody";
        };

        return AtomicBatchTransactionBody;
    })();

    proto.SystemDeleteTransactionBody = (function() {

        /**
         * Properties of a SystemDeleteTransactionBody.
         * @typedef {Object} proto.SystemDeleteTransactionBody.$Properties
         * @property {proto.FileID.$Properties|null} [fileID] A file identifier.
         * <p>
         * The identified file MUST exist in the HFS.<br/>
         * The identified file MUST NOT be deleted.<br/>
         * The identified file MUST NOT be a "system" file.<br/>
         * This field is REQUIRED.
         * @property {proto.ContractID.$Properties|null} [contractID] A contract identifier.
         * <p>
         * The identified contract MUST exist in network state.<br/>
         * The identified contract bytecode MUST NOT be deleted.<br/>
         * <p>
         * This option is _unsupported_.
         * @property {proto.TimestampSeconds.$Properties|null} [expirationTime] A timestamp indicating when the file will be removed from state.
         * <p>
         * This value SHALL be expressed in seconds since the `epoch`. The `epoch`
         * SHALL be the UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.<br/>
         * This field is REQUIRED.
         * @property {"fileID"|"contractID"} [id] SystemDeleteTransactionBody id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SystemDeleteTransactionBody.
         * @memberof proto
         * @interface ISystemDeleteTransactionBody
         * @augments proto.SystemDeleteTransactionBody.$Properties
         * @deprecated Use proto.SystemDeleteTransactionBody.$Properties instead.
         */

        /**
         * Narrowed shape of a SystemDeleteTransactionBody.
         * @typedef {{
         *   fileID?: proto.FileID.$Shape|null;
         *   contractID?: proto.ContractID.$Shape|null;
         *   expirationTime?: proto.TimestampSeconds.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ id?: undefined; fileID?: null; contractID?: null }|{ id?: "fileID"; fileID: proto.FileID.$Shape; contractID?: null }|{ id?: "contractID"; fileID?: null; contractID: proto.ContractID.$Shape })
         * )} proto.SystemDeleteTransactionBody.$Shape
         */

        /**
         * Constructs a new SystemDeleteTransactionBody.
         * @memberof proto
         * @classdesc Delete a file or contract bytecode as an administrative transaction.
         * 
         * > Note
         * >> A system delete/undelete for a `contractID` is not supported and
         * >> SHALL return `INVALID_FILE_ID` or `MISSING_ENTITY_ID`.
         * 
         * This transaction MAY be reversed by the `systemUndelete` transaction.
         * A file deleted via `fileDelete`, however SHALL be irrecoverable.<br/>
         * This transaction MUST specify an expiration timestamp (with seconds
         * precision). The file SHALL be permanently removed from state when
         * network consensus time exceeds the specified expiration time.<br/>
         * This transaction MUST be signed by an Hedera administrative ("system")
         * account.
         * 
         * ### What is a "system" file
         * A "system" file is any file with a file number less than or equal to the
         * current configuration value for `ledger.numReservedSystemEntities`,
         * typically `750`.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.SystemDeleteTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SystemDeleteTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A file identifier.
         * <p>
         * The identified file MUST exist in the HFS.<br/>
         * The identified file MUST NOT be deleted.<br/>
         * The identified file MUST NOT be a "system" file.<br/>
         * This field is REQUIRED.
         * @member {proto.FileID.$Properties|null|undefined} fileID
         * @memberof proto.SystemDeleteTransactionBody
         * @instance
         */
        SystemDeleteTransactionBody.prototype.fileID = null;

        /**
         * A contract identifier.
         * <p>
         * The identified contract MUST exist in network state.<br/>
         * The identified contract bytecode MUST NOT be deleted.<br/>
         * <p>
         * This option is _unsupported_.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.SystemDeleteTransactionBody
         * @instance
         */
        SystemDeleteTransactionBody.prototype.contractID = null;

        /**
         * A timestamp indicating when the file will be removed from state.
         * <p>
         * This value SHALL be expressed in seconds since the `epoch`. The `epoch`
         * SHALL be the UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.<br/>
         * This field is REQUIRED.
         * @member {proto.TimestampSeconds.$Properties|null|undefined} expirationTime
         * @memberof proto.SystemDeleteTransactionBody
         * @instance
         */
        SystemDeleteTransactionBody.prototype.expirationTime = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * SystemDeleteTransactionBody id.
         * @member {"fileID"|"contractID"|undefined} id
         * @memberof proto.SystemDeleteTransactionBody
         * @instance
         */
        $Object.defineProperty(SystemDeleteTransactionBody.prototype, "id", {
            get: $util.oneOfGetter($oneOfFields = ["fileID", "contractID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SystemDeleteTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.SystemDeleteTransactionBody
         * @static
         * @param {proto.SystemDeleteTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.SystemDeleteTransactionBody} SystemDeleteTransactionBody instance
         * @type {{
         *   (properties: proto.SystemDeleteTransactionBody.$Shape): proto.SystemDeleteTransactionBody & proto.SystemDeleteTransactionBody.$Shape;
         *   (properties?: proto.SystemDeleteTransactionBody.$Properties): proto.SystemDeleteTransactionBody;
         * }}
         */
        SystemDeleteTransactionBody.create = function(properties) {
            return new SystemDeleteTransactionBody(properties);
        };

        /**
         * Encodes the specified SystemDeleteTransactionBody message. Does not implicitly {@link proto.SystemDeleteTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.SystemDeleteTransactionBody
         * @static
         * @param {proto.SystemDeleteTransactionBody.$Properties} message SystemDeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SystemDeleteTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                $root.proto.TimestampSeconds.encode(message.expirationTime, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SystemDeleteTransactionBody message, length delimited. Does not implicitly {@link proto.SystemDeleteTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SystemDeleteTransactionBody
         * @static
         * @param {proto.SystemDeleteTransactionBody.$Properties} message SystemDeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SystemDeleteTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SystemDeleteTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SystemDeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SystemDeleteTransactionBody & proto.SystemDeleteTransactionBody.$Shape} SystemDeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SystemDeleteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SystemDeleteTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                        message.id = "fileID";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        message.id = "contractID";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.expirationTime = $root.proto.TimestampSeconds.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SystemDeleteTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SystemDeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SystemDeleteTransactionBody & proto.SystemDeleteTransactionBody.$Shape} SystemDeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SystemDeleteTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SystemDeleteTransactionBody message.
         * @function verify
         * @memberof proto.SystemDeleteTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SystemDeleteTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                properties.id = 1;
                {
                    let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                    if (error)
                        return "fileID." + error;
                }
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                if (properties.id === 1)
                    return "id: multiple values";
                properties.id = 1;
                {
                    let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                    if (error)
                        return "contractID." + error;
                }
            }
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                let error = $root.proto.TimestampSeconds.verify(message.expirationTime, _depth + 1);
                if (error)
                    return "expirationTime." + error;
            }
            return null;
        };

        /**
         * Creates a SystemDeleteTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SystemDeleteTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SystemDeleteTransactionBody} SystemDeleteTransactionBody
         */
        SystemDeleteTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SystemDeleteTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SystemDeleteTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SystemDeleteTransactionBody();
            if (object.fileID != null) {
                if (!$util.isObject(object.fileID))
                    throw $TypeError(".proto.SystemDeleteTransactionBody.fileID: object expected");
                message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
            }
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.SystemDeleteTransactionBody.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            if (object.expirationTime != null) {
                if (!$util.isObject(object.expirationTime))
                    throw $TypeError(".proto.SystemDeleteTransactionBody.expirationTime: object expected");
                message.expirationTime = $root.proto.TimestampSeconds.fromObject(object.expirationTime, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SystemDeleteTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SystemDeleteTransactionBody
         * @static
         * @param {proto.SystemDeleteTransactionBody} message SystemDeleteTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SystemDeleteTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.expirationTime = null;
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
                if (options.oneofs)
                    object.id = "fileID";
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
                if (options.oneofs)
                    object.id = "contractID";
            }
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                object.expirationTime = $root.proto.TimestampSeconds.toObject(message.expirationTime, options, _depth + 1);
            return object;
        };

        /**
         * Converts this SystemDeleteTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.SystemDeleteTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SystemDeleteTransactionBody.prototype.toJSON = function() {
            return SystemDeleteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SystemDeleteTransactionBody
         * @function getTypeUrl
         * @memberof proto.SystemDeleteTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SystemDeleteTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SystemDeleteTransactionBody";
        };

        return SystemDeleteTransactionBody;
    })();

    proto.SystemUndeleteTransactionBody = (function() {

        /**
         * Properties of a SystemUndeleteTransactionBody.
         * @typedef {Object} proto.SystemUndeleteTransactionBody.$Properties
         * @property {proto.FileID.$Properties|null} [fileID] A file identifier.
         * <p>
         * The identified file MUST exist in the HFS.<br/>
         * The identified file MUST be deleted.<br/>
         * The identified file deletion MUST be a result of a
         * `systemDelete` transaction.<br/>
         * The identified file MUST NOT be a "system" file.<br/>
         * This field is REQUIRED.
         * @property {proto.ContractID.$Properties|null} [contractID] A contract identifier.
         * <p>
         * The identified contract MUST exist in network state.<br/>
         * The identified contract bytecode MUST be deleted.<br/>
         * The identified contract deletion MUST be a result of a
         * `systemDelete` transaction.
         * <p>
         * This option is _unsupported_.
         * @property {"fileID"|"contractID"} [id] SystemUndeleteTransactionBody id
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SystemUndeleteTransactionBody.
         * @memberof proto
         * @interface ISystemUndeleteTransactionBody
         * @augments proto.SystemUndeleteTransactionBody.$Properties
         * @deprecated Use proto.SystemUndeleteTransactionBody.$Properties instead.
         */

        /**
         * Narrowed shape of a SystemUndeleteTransactionBody.
         * @typedef {{
         *   fileID?: proto.FileID.$Shape|null;
         *   contractID?: proto.ContractID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ id?: undefined; fileID?: null; contractID?: null }|{ id?: "fileID"; fileID: proto.FileID.$Shape; contractID?: null }|{ id?: "contractID"; fileID?: null; contractID: proto.ContractID.$Shape })
         * )} proto.SystemUndeleteTransactionBody.$Shape
         */

        /**
         * Constructs a new SystemUndeleteTransactionBody.
         * @memberof proto
         * @classdesc Recover a file or contract bytecode deleted from the Hedera File
         * System (HFS) by a `systemDelete` transaction.
         * 
         * > Note
         * >> A system delete/undelete for a `contractID` is not supported and
         * >> SHALL return `INVALID_FILE_ID` or `MISSING_ENTITY_ID`.
         * 
         * This transaction can _only_ recover a file removed with the `systemDelete`
         * transaction. A file deleted via `fileDelete` SHALL be irrecoverable.<br/>
         * This transaction MUST be signed by an Hedera administrative ("system")
         * account.
         * 
         * ### What is a "system" file
         * A "system" file is any file with a file number less than or equal to the
         * current configuration value for `ledger.numReservedSystemEntities`,
         * typically `750`.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.SystemUndeleteTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SystemUndeleteTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A file identifier.
         * <p>
         * The identified file MUST exist in the HFS.<br/>
         * The identified file MUST be deleted.<br/>
         * The identified file deletion MUST be a result of a
         * `systemDelete` transaction.<br/>
         * The identified file MUST NOT be a "system" file.<br/>
         * This field is REQUIRED.
         * @member {proto.FileID.$Properties|null|undefined} fileID
         * @memberof proto.SystemUndeleteTransactionBody
         * @instance
         */
        SystemUndeleteTransactionBody.prototype.fileID = null;

        /**
         * A contract identifier.
         * <p>
         * The identified contract MUST exist in network state.<br/>
         * The identified contract bytecode MUST be deleted.<br/>
         * The identified contract deletion MUST be a result of a
         * `systemDelete` transaction.
         * <p>
         * This option is _unsupported_.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.SystemUndeleteTransactionBody
         * @instance
         */
        SystemUndeleteTransactionBody.prototype.contractID = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * SystemUndeleteTransactionBody id.
         * @member {"fileID"|"contractID"|undefined} id
         * @memberof proto.SystemUndeleteTransactionBody
         * @instance
         */
        $Object.defineProperty(SystemUndeleteTransactionBody.prototype, "id", {
            get: $util.oneOfGetter($oneOfFields = ["fileID", "contractID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SystemUndeleteTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.SystemUndeleteTransactionBody
         * @static
         * @param {proto.SystemUndeleteTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.SystemUndeleteTransactionBody} SystemUndeleteTransactionBody instance
         * @type {{
         *   (properties: proto.SystemUndeleteTransactionBody.$Shape): proto.SystemUndeleteTransactionBody & proto.SystemUndeleteTransactionBody.$Shape;
         *   (properties?: proto.SystemUndeleteTransactionBody.$Properties): proto.SystemUndeleteTransactionBody;
         * }}
         */
        SystemUndeleteTransactionBody.create = function(properties) {
            return new SystemUndeleteTransactionBody(properties);
        };

        /**
         * Encodes the specified SystemUndeleteTransactionBody message. Does not implicitly {@link proto.SystemUndeleteTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.SystemUndeleteTransactionBody
         * @static
         * @param {proto.SystemUndeleteTransactionBody.$Properties} message SystemUndeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SystemUndeleteTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SystemUndeleteTransactionBody message, length delimited. Does not implicitly {@link proto.SystemUndeleteTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SystemUndeleteTransactionBody
         * @static
         * @param {proto.SystemUndeleteTransactionBody.$Properties} message SystemUndeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SystemUndeleteTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SystemUndeleteTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SystemUndeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SystemUndeleteTransactionBody & proto.SystemUndeleteTransactionBody.$Shape} SystemUndeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SystemUndeleteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SystemUndeleteTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                        message.id = "fileID";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        message.id = "contractID";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SystemUndeleteTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SystemUndeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SystemUndeleteTransactionBody & proto.SystemUndeleteTransactionBody.$Shape} SystemUndeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SystemUndeleteTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SystemUndeleteTransactionBody message.
         * @function verify
         * @memberof proto.SystemUndeleteTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SystemUndeleteTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                properties.id = 1;
                {
                    let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                    if (error)
                        return "fileID." + error;
                }
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                if (properties.id === 1)
                    return "id: multiple values";
                properties.id = 1;
                {
                    let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                    if (error)
                        return "contractID." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SystemUndeleteTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SystemUndeleteTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SystemUndeleteTransactionBody} SystemUndeleteTransactionBody
         */
        SystemUndeleteTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SystemUndeleteTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SystemUndeleteTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SystemUndeleteTransactionBody();
            if (object.fileID != null) {
                if (!$util.isObject(object.fileID))
                    throw $TypeError(".proto.SystemUndeleteTransactionBody.fileID: object expected");
                message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
            }
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.SystemUndeleteTransactionBody.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SystemUndeleteTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SystemUndeleteTransactionBody
         * @static
         * @param {proto.SystemUndeleteTransactionBody} message SystemUndeleteTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SystemUndeleteTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
                if (options.oneofs)
                    object.id = "fileID";
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
                if (options.oneofs)
                    object.id = "contractID";
            }
            return object;
        };

        /**
         * Converts this SystemUndeleteTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.SystemUndeleteTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SystemUndeleteTransactionBody.prototype.toJSON = function() {
            return SystemUndeleteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SystemUndeleteTransactionBody
         * @function getTypeUrl
         * @memberof proto.SystemUndeleteTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SystemUndeleteTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SystemUndeleteTransactionBody";
        };

        return SystemUndeleteTransactionBody;
    })();

    proto.FreezeTransactionBody = (function() {

        /**
         * Properties of a FreezeTransactionBody.
         * @typedef {Object} proto.FreezeTransactionBody.$Properties
         * @property {number|null} [startHour] Rejected if set; replace with `start_time`.<br/>
         * The start hour (in UTC time), a value between 0 and 23
         * @property {number|null} [startMin] Rejected if set; replace with `start_time`.<br/>
         * The start minute (in UTC time), a value between 0 and 59
         * @property {number|null} [endHour] Rejected if set; end time is neither assigned nor guaranteed and depends
         * on many uncontrolled factors.<br/>
         * The end hour (in UTC time), a value between 0 and 23
         * @property {number|null} [endMin] Rejected if set; end time is neither assigned nor guaranteed and depends
         * on many uncontrolled factors.<br/>
         * The end minute (in UTC time), a value between 0 and 59
         * @property {proto.FileID.$Properties|null} [updateFile] An upgrade file.
         * <p>
         * If set, the identifier of a file in network state.<br/>
         * The contents of this file MUST be a `zip` file and this data
         * SHALL be extracted to the node filesystem during a
         * `PREPARE_UPGRADE` or `TELEMETRY_UPGRADE` freeze type.<br/>
         * The `file_hash` field MUST match the SHA384 hash of the content
         * of this file.<br/>
         * The extracted data SHALL be used to perform a network software update
         * if a `FREEZE_UPGRADE` freeze type is subsequently processed.
         * @property {Uint8Array|null} [fileHash] A SHA384 hash of file content.<br/>
         * This is a hash of the file identified by `update_file`.
         * <p>
         * This MUST be set if `update_file` is set, and MUST match the
         * SHA384 hash of the contents of that file.
         * @property {proto.Timestamp.$Properties|null} [startTime] A start time for the freeze.
         * <p>
         * If this field is REQUIRED for the specified `freeze_type`, then
         * when the network consensus time reaches this instant<ol>
         * <li>The network SHALL stop accepting transactions.</li>
         * <li>The network SHALL gossip a freeze state.</li>
         * <li>The nodes SHALL, in coordinated order, disconnect and
         * shut down.</li>
         * <li>The nodes SHALL halt or perform a software upgrade, depending
         * on `freeze_type`.</li>
         * <li>If the `freeze_type` is `FREEZE_UPGRADE`, the nodes SHALL
         * restart and rejoin the network upon completion of the
         * software upgrade.</li>
         * </ol>
         * <blockquote>
         * If the `freeze_type` is `TELEMETRY_UPGRADE`, the start time is required,
         * but the network SHALL NOT stop, halt, or interrupt transaction
         * processing. The required field is an historical anomaly and SHOULD
         * change in a future release.</blockquote>
         * @property {proto.FreezeType|null} [freezeType] The type of freeze.
         * <p>
         * This REQUIRED field effectively selects between five quite different
         * transactions in the same transaction body. Depending on this value
         * the service may schedule a freeze, prepare upgrades, perform upgrades,
         * or even abort a previously scheduled freeze.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FreezeTransactionBody.
         * @memberof proto
         * @interface IFreezeTransactionBody
         * @augments proto.FreezeTransactionBody.$Properties
         * @deprecated Use proto.FreezeTransactionBody.$Properties instead.
         */

        /**
         * Shape of a FreezeTransactionBody.
         * @typedef {proto.FreezeTransactionBody.$Properties} proto.FreezeTransactionBody.$Shape
         */

        /**
         * Constructs a new FreezeTransactionBody.
         * @memberof proto
         * @classdesc A transaction body for all five freeze transactions.
         * 
         * Combining five different transactions into a single message, this
         * transaction body MUST support options to schedule a freeze, abort a
         * scheduled freeze, prepare a software upgrade, prepare a telemetry
         * upgrade, or initiate a software upgrade.
         * 
         * For a scheduled freeze, at the scheduled time, according to
         * network consensus time
         * - A freeze (`FREEZE_ONLY`) causes the network nodes to stop creating
         * events or accepting transactions, and enter a persistent
         * maintenance state.
         * - A freeze upgrade (`FREEZE_UPGRADE`) causes the network nodes to stop
         * creating events or accepting transactions, and upgrade the node software
         * from a previously prepared upgrade package. The network nodes then
         * restart and rejoin the network after upgrading.
         * 
         * For other freeze types, immediately upon processing the freeze transaction
         * - A Freeze Abort (`FREEZE_ABORT`) cancels any pending scheduled freeze.
         * - A prepare upgrade (`PREPARE_UPGRADE`) begins to extract the contents of
         * the specified upgrade file to the local filesystem.
         * - A telemetry upgrade (`TELEMETRY_UPGRADE`) causes the network nodes to
         * extract a telemetry upgrade package to the local filesystem and signal
         * other software on the machine to upgrade, without impacting the node or
         * network processing.
         * 
         * ### Block Stream Effects
         * Unknown
         * @constructor
         * @param {proto.FreezeTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FreezeTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Rejected if set; replace with `start_time`.<br/>
         * The start hour (in UTC time), a value between 0 and 23
         * @member {number} startHour
         * @memberof proto.FreezeTransactionBody
         * @instance
         */
        FreezeTransactionBody.prototype.startHour = 0;

        /**
         * Rejected if set; replace with `start_time`.<br/>
         * The start minute (in UTC time), a value between 0 and 59
         * @member {number} startMin
         * @memberof proto.FreezeTransactionBody
         * @instance
         */
        FreezeTransactionBody.prototype.startMin = 0;

        /**
         * Rejected if set; end time is neither assigned nor guaranteed and depends
         * on many uncontrolled factors.<br/>
         * The end hour (in UTC time), a value between 0 and 23
         * @member {number} endHour
         * @memberof proto.FreezeTransactionBody
         * @instance
         */
        FreezeTransactionBody.prototype.endHour = 0;

        /**
         * Rejected if set; end time is neither assigned nor guaranteed and depends
         * on many uncontrolled factors.<br/>
         * The end minute (in UTC time), a value between 0 and 59
         * @member {number} endMin
         * @memberof proto.FreezeTransactionBody
         * @instance
         */
        FreezeTransactionBody.prototype.endMin = 0;

        /**
         * An upgrade file.
         * <p>
         * If set, the identifier of a file in network state.<br/>
         * The contents of this file MUST be a `zip` file and this data
         * SHALL be extracted to the node filesystem during a
         * `PREPARE_UPGRADE` or `TELEMETRY_UPGRADE` freeze type.<br/>
         * The `file_hash` field MUST match the SHA384 hash of the content
         * of this file.<br/>
         * The extracted data SHALL be used to perform a network software update
         * if a `FREEZE_UPGRADE` freeze type is subsequently processed.
         * @member {proto.FileID.$Properties|null|undefined} updateFile
         * @memberof proto.FreezeTransactionBody
         * @instance
         */
        FreezeTransactionBody.prototype.updateFile = null;

        /**
         * A SHA384 hash of file content.<br/>
         * This is a hash of the file identified by `update_file`.
         * <p>
         * This MUST be set if `update_file` is set, and MUST match the
         * SHA384 hash of the contents of that file.
         * @member {Uint8Array} fileHash
         * @memberof proto.FreezeTransactionBody
         * @instance
         */
        FreezeTransactionBody.prototype.fileHash = $util.newBuffer([]);

        /**
         * A start time for the freeze.
         * <p>
         * If this field is REQUIRED for the specified `freeze_type`, then
         * when the network consensus time reaches this instant<ol>
         * <li>The network SHALL stop accepting transactions.</li>
         * <li>The network SHALL gossip a freeze state.</li>
         * <li>The nodes SHALL, in coordinated order, disconnect and
         * shut down.</li>
         * <li>The nodes SHALL halt or perform a software upgrade, depending
         * on `freeze_type`.</li>
         * <li>If the `freeze_type` is `FREEZE_UPGRADE`, the nodes SHALL
         * restart and rejoin the network upon completion of the
         * software upgrade.</li>
         * </ol>
         * <blockquote>
         * If the `freeze_type` is `TELEMETRY_UPGRADE`, the start time is required,
         * but the network SHALL NOT stop, halt, or interrupt transaction
         * processing. The required field is an historical anomaly and SHOULD
         * change in a future release.</blockquote>
         * @member {proto.Timestamp.$Properties|null|undefined} startTime
         * @memberof proto.FreezeTransactionBody
         * @instance
         */
        FreezeTransactionBody.prototype.startTime = null;

        /**
         * The type of freeze.
         * <p>
         * This REQUIRED field effectively selects between five quite different
         * transactions in the same transaction body. Depending on this value
         * the service may schedule a freeze, prepare upgrades, perform upgrades,
         * or even abort a previously scheduled freeze.
         * @member {proto.FreezeType} freezeType
         * @memberof proto.FreezeTransactionBody
         * @instance
         */
        FreezeTransactionBody.prototype.freezeType = 0;

        /**
         * Creates a new FreezeTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.FreezeTransactionBody
         * @static
         * @param {proto.FreezeTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.FreezeTransactionBody} FreezeTransactionBody instance
         * @type {{
         *   (properties: proto.FreezeTransactionBody.$Shape): proto.FreezeTransactionBody & proto.FreezeTransactionBody.$Shape;
         *   (properties?: proto.FreezeTransactionBody.$Properties): proto.FreezeTransactionBody;
         * }}
         */
        FreezeTransactionBody.create = function(properties) {
            return new FreezeTransactionBody(properties);
        };

        /**
         * Encodes the specified FreezeTransactionBody message. Does not implicitly {@link proto.FreezeTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.FreezeTransactionBody
         * @static
         * @param {proto.FreezeTransactionBody.$Properties} message FreezeTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreezeTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.startHour != null && $Object.hasOwnProperty.call(message, "startHour") && message.startHour !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.startHour);
            if (message.startMin != null && $Object.hasOwnProperty.call(message, "startMin") && message.startMin !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.startMin);
            if (message.endHour != null && $Object.hasOwnProperty.call(message, "endHour") && message.endHour !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.endHour);
            if (message.endMin != null && $Object.hasOwnProperty.call(message, "endMin") && message.endMin !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.endMin);
            if (message.updateFile != null && $Object.hasOwnProperty.call(message, "updateFile"))
                $root.proto.FileID.encode(message.updateFile, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.fileHash != null && $Object.hasOwnProperty.call(message, "fileHash") && message.fileHash.length)
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.fileHash);
            if (message.startTime != null && $Object.hasOwnProperty.call(message, "startTime"))
                $root.proto.Timestamp.encode(message.startTime, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.freezeType != null && $Object.hasOwnProperty.call(message, "freezeType") && message.freezeType !== 0)
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.freezeType);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FreezeTransactionBody message, length delimited. Does not implicitly {@link proto.FreezeTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FreezeTransactionBody
         * @static
         * @param {proto.FreezeTransactionBody.$Properties} message FreezeTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreezeTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FreezeTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FreezeTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FreezeTransactionBody & proto.FreezeTransactionBody.$Shape} FreezeTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreezeTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FreezeTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.startHour = value;
                        else
                            delete message.startHour;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.startMin = value;
                        else
                            delete message.startMin;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.endHour = value;
                        else
                            delete message.endHour;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.endMin = value;
                        else
                            delete message.endMin;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.updateFile = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.updateFile);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.fileHash = value;
                        else
                            delete message.fileHash;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.startTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.startTime);
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.freezeType = value;
                        else
                            delete message.freezeType;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FreezeTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FreezeTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FreezeTransactionBody & proto.FreezeTransactionBody.$Shape} FreezeTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreezeTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreezeTransactionBody message.
         * @function verify
         * @memberof proto.FreezeTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreezeTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.startHour != null && $Object.hasOwnProperty.call(message, "startHour"))
                if (!$util.isInteger(message.startHour))
                    return "startHour: integer expected";
            if (message.startMin != null && $Object.hasOwnProperty.call(message, "startMin"))
                if (!$util.isInteger(message.startMin))
                    return "startMin: integer expected";
            if (message.endHour != null && $Object.hasOwnProperty.call(message, "endHour"))
                if (!$util.isInteger(message.endHour))
                    return "endHour: integer expected";
            if (message.endMin != null && $Object.hasOwnProperty.call(message, "endMin"))
                if (!$util.isInteger(message.endMin))
                    return "endMin: integer expected";
            if (message.updateFile != null && $Object.hasOwnProperty.call(message, "updateFile")) {
                let error = $root.proto.FileID.verify(message.updateFile, _depth + 1);
                if (error)
                    return "updateFile." + error;
            }
            if (message.fileHash != null && $Object.hasOwnProperty.call(message, "fileHash"))
                if (!(message.fileHash && typeof message.fileHash.length === "number" || $util.isString(message.fileHash)))
                    return "fileHash: buffer expected";
            if (message.startTime != null && $Object.hasOwnProperty.call(message, "startTime")) {
                let error = $root.proto.Timestamp.verify(message.startTime, _depth + 1);
                if (error)
                    return "startTime." + error;
            }
            if (message.freezeType != null && $Object.hasOwnProperty.call(message, "freezeType"))
                if (typeof message.freezeType !== "number" || (message.freezeType | 0) !== message.freezeType)
                    return "freezeType: enum value expected";
            return null;
        };

        /**
         * Creates a FreezeTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FreezeTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FreezeTransactionBody} FreezeTransactionBody
         */
        FreezeTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FreezeTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FreezeTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FreezeTransactionBody();
            if (object.startHour != null)
                if ($Number(object.startHour) !== 0)
                    message.startHour = object.startHour | 0;
            if (object.startMin != null)
                if ($Number(object.startMin) !== 0)
                    message.startMin = object.startMin | 0;
            if (object.endHour != null)
                if ($Number(object.endHour) !== 0)
                    message.endHour = object.endHour | 0;
            if (object.endMin != null)
                if ($Number(object.endMin) !== 0)
                    message.endMin = object.endMin | 0;
            if (object.updateFile != null) {
                if (!$util.isObject(object.updateFile))
                    throw $TypeError(".proto.FreezeTransactionBody.updateFile: object expected");
                message.updateFile = $root.proto.FileID.fromObject(object.updateFile, _depth + 1);
            }
            if (object.fileHash != null)
                if (object.fileHash.length)
                    if (typeof object.fileHash === "string")
                        $util.base64.decode(object.fileHash, message.fileHash = $util.newBuffer($util.base64.length(object.fileHash)), 0);
                    else if (object.fileHash.length >= 0)
                        message.fileHash = object.fileHash;
            if (object.startTime != null) {
                if (!$util.isObject(object.startTime))
                    throw $TypeError(".proto.FreezeTransactionBody.startTime: object expected");
                message.startTime = $root.proto.Timestamp.fromObject(object.startTime, _depth + 1);
            }
            if (object.freezeType !== 0 && (typeof object.freezeType !== "string" || $root.proto.FreezeType[object.freezeType] !== 0))
                switch (object.freezeType) {
                case "UNKNOWN_FREEZE_TYPE":
                case 0:
                    message.freezeType = 0;
                    break;
                case "FREEZE_ONLY":
                case 1:
                    message.freezeType = 1;
                    break;
                case "PREPARE_UPGRADE":
                case 2:
                    message.freezeType = 2;
                    break;
                case "FREEZE_UPGRADE":
                case 3:
                    message.freezeType = 3;
                    break;
                case "FREEZE_ABORT":
                case 4:
                    message.freezeType = 4;
                    break;
                case "TELEMETRY_UPGRADE":
                case 5:
                    message.freezeType = 5;
                    break;
                default:
                    if (typeof object.freezeType === "number" && (object.freezeType | 0) === object.freezeType)
                        message.freezeType = object.freezeType;
                }
            return message;
        };

        /**
         * Creates a plain object from a FreezeTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FreezeTransactionBody
         * @static
         * @param {proto.FreezeTransactionBody} message FreezeTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreezeTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.startHour = 0;
                object.startMin = 0;
                object.endHour = 0;
                object.endMin = 0;
                object.updateFile = null;
                if (options.bytes === $String)
                    object.fileHash = "";
                else {
                    object.fileHash = [];
                    if (options.bytes !== $Array)
                        object.fileHash = $util.newBuffer(object.fileHash);
                }
                object.startTime = null;
                object.freezeType = options.enums === $String ? "UNKNOWN_FREEZE_TYPE" : 0;
            }
            if (message.startHour != null && $Object.hasOwnProperty.call(message, "startHour"))
                object.startHour = message.startHour;
            if (message.startMin != null && $Object.hasOwnProperty.call(message, "startMin"))
                object.startMin = message.startMin;
            if (message.endHour != null && $Object.hasOwnProperty.call(message, "endHour"))
                object.endHour = message.endHour;
            if (message.endMin != null && $Object.hasOwnProperty.call(message, "endMin"))
                object.endMin = message.endMin;
            if (message.updateFile != null && $Object.hasOwnProperty.call(message, "updateFile"))
                object.updateFile = $root.proto.FileID.toObject(message.updateFile, options, _depth + 1);
            if (message.fileHash != null && $Object.hasOwnProperty.call(message, "fileHash"))
                object.fileHash = options.bytes === $String ? $util.base64.encode(message.fileHash, 0, message.fileHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.fileHash) : message.fileHash;
            if (message.startTime != null && $Object.hasOwnProperty.call(message, "startTime"))
                object.startTime = $root.proto.Timestamp.toObject(message.startTime, options, _depth + 1);
            if (message.freezeType != null && $Object.hasOwnProperty.call(message, "freezeType"))
                object.freezeType = options.enums === $String ? $root.proto.FreezeType[message.freezeType] === $undefined ? message.freezeType : $root.proto.FreezeType[message.freezeType] : message.freezeType;
            return object;
        };

        /**
         * Converts this FreezeTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.FreezeTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreezeTransactionBody.prototype.toJSON = function() {
            return FreezeTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FreezeTransactionBody
         * @function getTypeUrl
         * @memberof proto.FreezeTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FreezeTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FreezeTransactionBody";
        };

        return FreezeTransactionBody;
    })();

    /**
     * An enumeration of possible network freeze types.
     * 
     * Each enumerated value SHALL be associated to a single network freeze
     * scenario. Each freeze scenario defines the specific parameters
     * REQUIRED for that freeze.
     * @name proto.FreezeType
     * @enum {number}
     * @property {number} UNKNOWN_FREEZE_TYPE=0 An invalid freeze type.
     * <p>
     * The first value in a protobuf enum is a default value. This default
     * is RECOMMENDED to be an invalid value to aid in detecting unset fields.
     * @property {number} FREEZE_ONLY=1 Freeze the network, and take no further action.
     * <p>
     * The `start_time` field is REQUIRED, MUST be strictly later than the
     * consensus time when this transaction is handled, and SHOULD be between
     * `300` and `3600` seconds after the transaction identifier
     * `transactionValidStart` field.<br/>
     * The fields `update_file` and `file_hash` SHALL be ignored.<br/>
     * A `FREEZE_ONLY` transaction SHALL NOT perform any network
     * changes or upgrades.<br/>
     * After this freeze is processed manual intervention is REQUIRED
     * to restart the network.
     * @property {number} PREPARE_UPGRADE=2 This freeze type does not freeze the network, but begins
     * "preparation" to upgrade the network.
     * <p>
     * The fields `update_file` and `file_hash` are REQUIRED
     * and MUST be valid.<br/>
     * The `start_time` field SHALL be ignored.<br/>
     * A `PREPARE_UPGRADE` transaction SHALL NOT freeze the network or
     * interfere with general transaction processing.<br/>
     * If this freeze type is initiated after a `TELEMETRY_UPGRADE`, the
     * prepared telemetry upgrade SHALL be reset and all telemetry upgrade
     * artifacts in the filesystem SHALL be deleted.<br/>
     * At some point after this freeze type completes (dependent on the size
     * of the upgrade file), the network SHALL be prepared to complete
     * a software upgrade of all nodes.
     * @property {number} FREEZE_UPGRADE=3 Freeze the network to perform a software upgrade.
     * <p>
     * The `start_time` field is REQUIRED, MUST be strictly later than the
     * consensus time when this transaction is handled, and SHOULD be between
     * `300` and `3600` seconds after the transaction identifier
     * `transactionValidStart` field.<br/>
     * A software upgrade file MUST be prepared prior to this transaction.<br/>
     * After this transaction completes, the network SHALL initiate an
     * upgrade and restart of all nodes at the start time specified.
     * @property {number} FREEZE_ABORT=4 Abort a pending network freeze operation.
     * <p>
     * All fields SHALL be ignored for this freeze type.<br/>
     * This freeze type MAY be submitted after a `FREEZE_ONLY`,
     * `FREEZE_UPGRADE`, or `TELEMETRY_UPGRADE` is initiated.<br/>
     * This freeze type MUST be submitted and reach consensus
     * before the `start_time` designated for the current pending
     * freeze to be effective.<br/>
     * After this freeze type is processed, the upgrade file hash
     * and pending freeze start time stored in the network SHALL
     * be reset to default (empty) values.
     * @property {number} TELEMETRY_UPGRADE=5 Prepare an upgrade of auxiliary services and containers
     * providing telemetry/metrics.
     * <p>
     * The `start_time` field is REQUIRED, MUST be strictly later than the
     * consensus time when this transaction is handled, and SHOULD be between
     * `300` and `3600` seconds after the transaction identifier
     * `transactionValidStart` field.<br/>
     * The `update_file` field is REQUIRED and MUST be valid.<br/>
     * A `TELEMETRY_UPGRADE` transaction SHALL NOT freeze the network or
     * interfere with general transaction processing.<br/>
     * This freeze type MUST NOT be initiated between a `PREPARE_UPGRADE`
     * and `FREEZE_UPGRADE`. If this freeze type is initiated after a
     * `PREPARE_UPGRADE`, the prepared upgrade SHALL be reset and all software
     * upgrade artifacts in the filesystem SHALL be deleted.<br/>
     * At some point after this freeze type completes (dependent on the
     * size of the upgrade file), the network SHALL automatically upgrade
     * the telemetry/metrics services and containers as directed in
     * the specified telemetry upgrade file.
     * <blockquote> The condition that `start_time` is REQUIRED is an
     * historical anomaly and SHOULD change in a future release.</blockquote>
     */
    proto.FreezeType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN_FREEZE_TYPE"] = 0;
        values[valuesById[1] = "FREEZE_ONLY"] = 1;
        values[valuesById[2] = "PREPARE_UPGRADE"] = 2;
        values[valuesById[3] = "FREEZE_UPGRADE"] = 3;
        values[valuesById[4] = "FREEZE_ABORT"] = 4;
        values[valuesById[5] = "TELEMETRY_UPGRADE"] = 5;
        return values;
    })();

    proto.ContractCallTransactionBody = (function() {

        /**
         * Properties of a ContractCallTransactionBody.
         * @typedef {Object} proto.ContractCallTransactionBody.$Properties
         * @property {proto.ContractID.$Properties|null} [contractID] The ID of a smart contract to call.
         * @property {number|Long|null} [gas] A maximum limit to the amount of gas to use for this call.
         * <p>
         * The network SHALL charge the greater of the following, but
         * SHALL NOT charge more than the value of this field.
         * <ol>
         * <li>The actual gas consumed by the smart contract call.</li>
         * <li>`80%` of this value.</li>
         * </ol>
         * The `80%` factor encourages reasonable estimation, while allowing for
         * some overage to ensure successful execution.
         * @property {number|Long|null} [amount] An amount of tinybar sent via this contract call.
         * <p>
         * If this is non-zero, the function MUST be `payable`.
         * @property {Uint8Array|null} [functionParameters] The smart contract function to call.
         * <p>
         * This MUST contain The application binary interface (ABI) encoding of the
         * function call per the Ethereum contract ABI standard, giving the
         * function signature and arguments being passed to the function.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractCallTransactionBody.
         * @memberof proto
         * @interface IContractCallTransactionBody
         * @augments proto.ContractCallTransactionBody.$Properties
         * @deprecated Use proto.ContractCallTransactionBody.$Properties instead.
         */

        /**
         * Shape of a ContractCallTransactionBody.
         * @typedef {{
         *   contractID?: proto.ContractID.$Shape|null;
         *   gas?: number|Long|null;
         *   amount?: number|Long|null;
         *   functionParameters?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractCallTransactionBody.$Shape
         */

        /**
         * Constructs a new ContractCallTransactionBody.
         * @memberof proto
         * @classdesc Call a function of a given smart contract, providing function parameter
         * inputs as needed.
         * 
         * Resource ("gas") charges SHALL include all relevant fees incurred by the
         * contract execution, including any storage required.<br/>
         * The total transaction fee SHALL incorporate all of the "gas" actually
         * consumed as well as the standard fees for transaction handling, data
         * transfers, signature verification, etc...<br/>
         * The response SHALL contain the output returned by the function call.
         * 
         * ### Block Stream Effects
         * A `CallContractOutput` message SHALL be emitted for each transaction.
         * @constructor
         * @param {proto.ContractCallTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractCallTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The ID of a smart contract to call.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.ContractCallTransactionBody
         * @instance
         */
        ContractCallTransactionBody.prototype.contractID = null;

        /**
         * A maximum limit to the amount of gas to use for this call.
         * <p>
         * The network SHALL charge the greater of the following, but
         * SHALL NOT charge more than the value of this field.
         * <ol>
         * <li>The actual gas consumed by the smart contract call.</li>
         * <li>`80%` of this value.</li>
         * </ol>
         * The `80%` factor encourages reasonable estimation, while allowing for
         * some overage to ensure successful execution.
         * @member {number|Long} gas
         * @memberof proto.ContractCallTransactionBody
         * @instance
         */
        ContractCallTransactionBody.prototype.gas = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An amount of tinybar sent via this contract call.
         * <p>
         * If this is non-zero, the function MUST be `payable`.
         * @member {number|Long} amount
         * @memberof proto.ContractCallTransactionBody
         * @instance
         */
        ContractCallTransactionBody.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The smart contract function to call.
         * <p>
         * This MUST contain The application binary interface (ABI) encoding of the
         * function call per the Ethereum contract ABI standard, giving the
         * function signature and arguments being passed to the function.
         * @member {Uint8Array} functionParameters
         * @memberof proto.ContractCallTransactionBody
         * @instance
         */
        ContractCallTransactionBody.prototype.functionParameters = $util.newBuffer([]);

        /**
         * Creates a new ContractCallTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.ContractCallTransactionBody
         * @static
         * @param {proto.ContractCallTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.ContractCallTransactionBody} ContractCallTransactionBody instance
         * @type {{
         *   (properties: proto.ContractCallTransactionBody.$Shape): proto.ContractCallTransactionBody & proto.ContractCallTransactionBody.$Shape;
         *   (properties?: proto.ContractCallTransactionBody.$Properties): proto.ContractCallTransactionBody;
         * }}
         */
        ContractCallTransactionBody.create = function(properties) {
            return new ContractCallTransactionBody(properties);
        };

        /**
         * Encodes the specified ContractCallTransactionBody message. Does not implicitly {@link proto.ContractCallTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractCallTransactionBody
         * @static
         * @param {proto.ContractCallTransactionBody.$Properties} message ContractCallTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractCallTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas") && (typeof message.gas === "object" ? message.gas.low || message.gas.high : message.gas !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.gas);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.amount);
            if (message.functionParameters != null && $Object.hasOwnProperty.call(message, "functionParameters") && message.functionParameters.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.functionParameters);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractCallTransactionBody message, length delimited. Does not implicitly {@link proto.ContractCallTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractCallTransactionBody
         * @static
         * @param {proto.ContractCallTransactionBody.$Properties} message ContractCallTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractCallTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractCallTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractCallTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractCallTransactionBody & proto.ContractCallTransactionBody.$Shape} ContractCallTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractCallTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractCallTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.gas = value;
                        else
                            delete message.gas;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.functionParameters = value;
                        else
                            delete message.functionParameters;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractCallTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractCallTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractCallTransactionBody & proto.ContractCallTransactionBody.$Shape} ContractCallTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractCallTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractCallTransactionBody message.
         * @function verify
         * @memberof proto.ContractCallTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractCallTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (!$util.isInteger(message.gas) && !(message.gas && $util.isInteger(message.gas.low) && $util.isInteger(message.gas.high)))
                    return "gas: integer|Long expected";
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.functionParameters != null && $Object.hasOwnProperty.call(message, "functionParameters"))
                if (!(message.functionParameters && typeof message.functionParameters.length === "number" || $util.isString(message.functionParameters)))
                    return "functionParameters: buffer expected";
            return null;
        };

        /**
         * Creates a ContractCallTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractCallTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractCallTransactionBody} ContractCallTransactionBody
         */
        ContractCallTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractCallTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractCallTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractCallTransactionBody();
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.ContractCallTransactionBody.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            if (object.gas != null)
                if (typeof object.gas === "object" ? object.gas.low || object.gas.high : $Number(object.gas) !== 0)
                    if ($util.Long)
                        message.gas = $util.Long.fromValue(object.gas, false);
                    else if (typeof object.gas === "string")
                        message.gas = $parseInt(object.gas, 10);
                    else if (typeof object.gas === "number")
                        message.gas = object.gas;
                    else if (typeof object.gas === "object")
                        message.gas = new $util.LongBits(object.gas.low >>> 0, object.gas.high >>> 0).toNumber();
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            if (object.functionParameters != null)
                if (object.functionParameters.length)
                    if (typeof object.functionParameters === "string")
                        $util.base64.decode(object.functionParameters, message.functionParameters = $util.newBuffer($util.base64.length(object.functionParameters)), 0);
                    else if (object.functionParameters.length >= 0)
                        message.functionParameters = object.functionParameters;
            return message;
        };

        /**
         * Creates a plain object from a ContractCallTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractCallTransactionBody
         * @static
         * @param {proto.ContractCallTransactionBody} message ContractCallTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractCallTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.contractID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.gas = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gas = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.functionParameters = "";
                else {
                    object.functionParameters = [];
                    if (options.bytes !== $Array)
                        object.functionParameters = $util.newBuffer(object.functionParameters);
                }
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gas = typeof message.gas === "number" ? $BigInt(message.gas) : $util.Long.fromBits(message.gas.low >>> 0, message.gas.high >>> 0, false).toBigInt();
                else if (typeof message.gas === "number")
                    object.gas = options.longs === $String ? $String(message.gas) : message.gas;
                else
                    object.gas = options.longs === $String ? $util.Long.prototype.toString.call(message.gas) : options.longs === $Number ? new $util.LongBits(message.gas.low >>> 0, message.gas.high >>> 0).toNumber() : message.gas;
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            if (message.functionParameters != null && $Object.hasOwnProperty.call(message, "functionParameters"))
                object.functionParameters = options.bytes === $String ? $util.base64.encode(message.functionParameters, 0, message.functionParameters.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.functionParameters) : message.functionParameters;
            return object;
        };

        /**
         * Converts this ContractCallTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.ContractCallTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractCallTransactionBody.prototype.toJSON = function() {
            return ContractCallTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractCallTransactionBody
         * @function getTypeUrl
         * @memberof proto.ContractCallTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractCallTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractCallTransactionBody";
        };

        return ContractCallTransactionBody;
    })();

    proto.ContractCreateTransactionBody = (function() {

        /**
         * Properties of a ContractCreateTransactionBody.
         * @typedef {Object} proto.ContractCreateTransactionBody.$Properties
         * @property {proto.FileID.$Properties|null} [fileID] The source for the smart contract EVM bytecode.
         * <p>
         * The file containing the smart contract initcode.
         * A copy of the contents SHALL be made and held as `bytes`
         * in smart contract state.<br/>
         * The contract bytecode is limited in size only by the
         * network file size limit.
         * @property {Uint8Array|null} [initcode] The source for the smart contract EVM bytecode.
         * <p>
         * The bytes of the smart contract initcode. A copy of the contents
         * SHALL be made and held as `bytes` in smart contract state.<br/>
         * This value is limited in length by the network transaction size
         * limit. This entire transaction, including all fields and signatures,
         * MUST be less than the network transaction size limit.
         * @property {proto.Key.$Properties|null} [adminKey] Access control for modification of the smart contract after
         * it is created.
         * <p>
         * If this field is set, that key MUST sign this transaction.<br/>
         * If this field is set, that key MUST sign each future transaction to
         * update or delete the contract.<br/>
         * An updateContract transaction that _only_ extends the topic
         * expirationTime (a "manual renewal" transaction) SHALL NOT require
         * admin key signature.
         * <p>
         * A contract without an admin key SHALL be immutable, except for
         * expiration and renewal.
         * @property {number|Long|null} [gas] A maximum limit to the amount of gas to use for the constructor call.
         * <p>
         * The network SHALL charge the greater of the following, but SHALL NOT
         * charge more than the value of this field.
         * <ol>
         * <li>The actual gas consumed by the smart contract
         * constructor call.</li>
         * <li>`80%` of this value.</li>
         * </ol>
         * The `80%` factor encourages reasonable estimation, while allowing for
         * some overage to ensure successful execution.
         * @property {number|Long|null} [initialBalance] The amount of HBAR to use as an initial balance for the account
         * representing the new smart contract.
         * <p>
         * This value is presented in tinybar
         * (10<sup><strong>-</strong>8</sup> HBAR).<br/>
         * The HBAR provided here will be withdrawn from the payer account that
         * signed this transaction.
         * @property {proto.AccountID.$Properties|null} [proxyAccountID] Proxy account staking is handled via `staked_id`.
         * <p>
         * Former field to designate a proxy account for HBAR staking.
         * This field MUST NOT be set.
         * @property {proto.Duration.$Properties|null} [autoRenewPeriod] The initial lifetime, in seconds, for the smart contract, and the number
         * of seconds for which the smart contract will be automatically renewed
         * upon expiration.
         * <p>
         * This value MUST be set.<br/>
         * This value MUST be greater than the configured MIN_AUTORENEW_PERIOD.<br/>
         * This value MUST be less than the configured MAX_AUTORENEW_PERIOD.<br/>
         * @property {Uint8Array|null} [constructorParameters] An array of bytes containing the EVM-encoded parameters to pass to
         * the smart contract constructor defined in the smart contract init
         * code provided.
         * @property {proto.ShardID.$Properties|null} [shardID] <blockquote>Review Question<br/>
         * <blockquote>Should this be deprecated?<br/>
         * It's never been used and probably never should be used...<br/>
         * Shard should be determined by the node the transaction is submitted to.
         * </blockquote></blockquote>
         * <p>
         * The shard in which to create the new smart contract.<br/>
         * This value is currently ignored.
         * @property {proto.RealmID.$Properties|null} [realmID] <blockquote>Review Question<br/>
         * <blockquote>Should this be deprecated?<br/>
         * It's never been used and probably never should be used...<br/>
         * Realm should be determined by node and network parameters.
         * </blockquote></blockquote>
         * <p>
         * The shard/realm in which to create the new smart contract.<br/>
         * This value is currently ignored.
         * @property {proto.Key.$Properties|null} [newRealmAdminKey] <blockquote>Review Question<br/>
         * <blockquote>Should this be deprecated?<br/>
         * It's never been used and probably never should be used...<br/>
         * If a realm is used, it must already exist; we shouldn't be creating it
         * without a separate transaction.</blockquote></blockquote>
         * <p>
         * This was intended to provide an admin key for any new realm created
         * during the creation of the smart contract.<br/>
         * This value is currently ignored. a new realm SHALL NOT be created,
         * regardless of the value of `realmID`.
         * @property {string|null} [memo] A short memo for this smart contract.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {number|null} [maxAutomaticTokenAssociations] The maximum number of tokens that can be auto-associated with this
         * smart contract.
         * <p>
         * If this is less than or equal to `used_auto_associations` (or 0), then
         * this contract MUST manually associate with a token before transacting
         * in that token.<br/>
         * Following HIP-904 This value may also be `-1` to indicate no limit.<br/>
         * This value MUST NOT be less than `-1`.
         * @property {proto.AccountID.$Properties|null} [autoRenewAccountId] The id of an account, in the same shard and realm as this smart
         * contract, that has signed this transaction, allowing the network to use
         * its balance, when needed, to automatically extend this contract's
         * expiration time.
         * <p>
         * If this field is set, that key MUST sign this transaction.<br/>
         * If this field is set, then the network SHALL deduct the necessary fees
         * from the designated auto renew account, if that account has sufficient
         * balance. If the auto renew account does not have sufficient balance,
         * then the fees for contract renewal SHALL be deducted from the HBAR
         * balance held by the smart contract.<br/>
         * If this field is not set, then all renewal fees SHALL be deducted from
         * the HBAR balance held by this contract.
         * @property {proto.AccountID.$Properties|null} [stakedAccountId] An account ID.
         * <p>
         * This smart contract SHALL stake its HBAR via this account as proxy.
         * @property {number|Long|null} [stakedNodeId] The ID of a network node.
         * <p>
         * This smart contract SHALL stake its HBAR to this node.
         * <p>
         * <blockquote>Note: node IDs do fluctuate as node operators change.
         * Most contracts are immutable, and a contract staking to an invalid
         * node ID SHALL NOT participate in staking. Immutable contracts MAY
         * find it more reliable to use a proxy account for staking
         * (via `staked_account_id`) to enable updating the _effective_ staking
         * node ID when necessary through updating the proxy
         * account.</blockquote>
         * @property {boolean|null} [declineReward] A flag indicating that this smart contract declines to receive any
         * reward for staking its HBAR balance to help secure the network.
         * <p>
         * If set to true, this smart contract SHALL NOT receive any reward for
         * staking its HBAR balance to help secure the network, regardless of
         * staking configuration, but MAY stake HBAR to support the network
         * without reward.
         * @property {Array.<com.hedera.hapi.node.hooks.HookCreationDetails.$Properties>|null} [hookCreationDetails] Details of hooks to add immediately after creating this contract.
         * @property {"fileID"|"initcode"} [initcodeSource] ContractCreateTransactionBody initcodeSource
         * @property {"stakedAccountId"|"stakedNodeId"} [stakedId] ContractCreateTransactionBody stakedId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractCreateTransactionBody.
         * @memberof proto
         * @interface IContractCreateTransactionBody
         * @augments proto.ContractCreateTransactionBody.$Properties
         * @deprecated Use proto.ContractCreateTransactionBody.$Properties instead.
         */

        /**
         * Narrowed shape of a ContractCreateTransactionBody.
         * @typedef {{
         *   fileID?: proto.FileID.$Shape|null;
         *   initcode?: Uint8Array|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   gas?: number|Long|null;
         *   initialBalance?: number|Long|null;
         *   proxyAccountID?: proto.AccountID.$Shape|null;
         *   autoRenewPeriod?: proto.Duration.$Shape|null;
         *   constructorParameters?: Uint8Array|null;
         *   shardID?: proto.ShardID.$Shape|null;
         *   realmID?: proto.RealmID.$Shape|null;
         *   newRealmAdminKey?: proto.Key.$Shape|null;
         *   memo?: string|null;
         *   maxAutomaticTokenAssociations?: number|null;
         *   autoRenewAccountId?: proto.AccountID.$Shape|null;
         *   stakedAccountId?: proto.AccountID.$Shape|null;
         *   stakedNodeId?: number|Long|null;
         *   declineReward?: boolean|null;
         *   hookCreationDetails?: Array.<com.hedera.hapi.node.hooks.HookCreationDetails.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ initcodeSource?: undefined; fileID?: null; initcode?: null }|{ initcodeSource?: "fileID"; fileID: proto.FileID.$Shape; initcode?: null }|{ initcodeSource?: "initcode"; fileID?: null; initcode: Uint8Array })
         * ) & (
         *   ({ stakedId?: undefined; stakedAccountId?: null; stakedNodeId?: null }|{ stakedId?: "stakedAccountId"; stakedAccountId: proto.AccountID.$Shape; stakedNodeId?: null }|{ stakedId?: "stakedNodeId"; stakedAccountId?: null; stakedNodeId: number|Long })
         * )} proto.ContractCreateTransactionBody.$Shape
         */

        /**
         * Constructs a new ContractCreateTransactionBody.
         * @memberof proto
         * @classdesc Create a new smart contract.
         * 
         * If this transaction succeeds, the `ContractID` for the new smart contract
         * SHALL be set in the transaction receipt.<br/>
         * The contract is defined by the initial bytecode (or `initcode`). The
         * `initcode` SHALL be stored either in a previously created file, or in the
         * transaction body itself for very small contracts.
         * 
         * As part of contract creation, the constructor defined for the new smart
         * contract SHALL run with the parameters provided in the
         * `constructorParameters` field.<br/>
         * The gas to "power" that constructor MUST be provided via the `gas` field,
         * and SHALL be charged to the payer for this transaction.<br/>
         * If the contract _constructor_ stores information, it is charged gas for that
         * storage. There is a separate fee in HBAR to maintain that storage until the
         * expiration, and that fee SHALL be added to this transaction as part of the
         * _transaction fee_, rather than gas.
         * 
         * ### Block Stream Effects
         * A `CreateContractOutput` message SHALL be emitted for each transaction.
         * @constructor
         * @param {proto.ContractCreateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractCreateTransactionBody = function (properties) {
            this.hookCreationDetails = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The source for the smart contract EVM bytecode.
         * <p>
         * The file containing the smart contract initcode.
         * A copy of the contents SHALL be made and held as `bytes`
         * in smart contract state.<br/>
         * The contract bytecode is limited in size only by the
         * network file size limit.
         * @member {proto.FileID.$Properties|null|undefined} fileID
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.fileID = null;

        /**
         * The source for the smart contract EVM bytecode.
         * <p>
         * The bytes of the smart contract initcode. A copy of the contents
         * SHALL be made and held as `bytes` in smart contract state.<br/>
         * This value is limited in length by the network transaction size
         * limit. This entire transaction, including all fields and signatures,
         * MUST be less than the network transaction size limit.
         * @member {Uint8Array|null|undefined} initcode
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.initcode = null;

        /**
         * Access control for modification of the smart contract after
         * it is created.
         * <p>
         * If this field is set, that key MUST sign this transaction.<br/>
         * If this field is set, that key MUST sign each future transaction to
         * update or delete the contract.<br/>
         * An updateContract transaction that _only_ extends the topic
         * expirationTime (a "manual renewal" transaction) SHALL NOT require
         * admin key signature.
         * <p>
         * A contract without an admin key SHALL be immutable, except for
         * expiration and renewal.
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.adminKey = null;

        /**
         * A maximum limit to the amount of gas to use for the constructor call.
         * <p>
         * The network SHALL charge the greater of the following, but SHALL NOT
         * charge more than the value of this field.
         * <ol>
         * <li>The actual gas consumed by the smart contract
         * constructor call.</li>
         * <li>`80%` of this value.</li>
         * </ol>
         * The `80%` factor encourages reasonable estimation, while allowing for
         * some overage to ensure successful execution.
         * @member {number|Long} gas
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.gas = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The amount of HBAR to use as an initial balance for the account
         * representing the new smart contract.
         * <p>
         * This value is presented in tinybar
         * (10<sup><strong>-</strong>8</sup> HBAR).<br/>
         * The HBAR provided here will be withdrawn from the payer account that
         * signed this transaction.
         * @member {number|Long} initialBalance
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.initialBalance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Proxy account staking is handled via `staked_id`.
         * <p>
         * Former field to designate a proxy account for HBAR staking.
         * This field MUST NOT be set.
         * @member {proto.AccountID.$Properties|null|undefined} proxyAccountID
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.proxyAccountID = null;

        /**
         * The initial lifetime, in seconds, for the smart contract, and the number
         * of seconds for which the smart contract will be automatically renewed
         * upon expiration.
         * <p>
         * This value MUST be set.<br/>
         * This value MUST be greater than the configured MIN_AUTORENEW_PERIOD.<br/>
         * This value MUST be less than the configured MAX_AUTORENEW_PERIOD.<br/>
         * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.autoRenewPeriod = null;

        /**
         * An array of bytes containing the EVM-encoded parameters to pass to
         * the smart contract constructor defined in the smart contract init
         * code provided.
         * @member {Uint8Array} constructorParameters
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.constructorParameters = $util.newBuffer([]);

        /**
         * <blockquote>Review Question<br/>
         * <blockquote>Should this be deprecated?<br/>
         * It's never been used and probably never should be used...<br/>
         * Shard should be determined by the node the transaction is submitted to.
         * </blockquote></blockquote>
         * <p>
         * The shard in which to create the new smart contract.<br/>
         * This value is currently ignored.
         * @member {proto.ShardID.$Properties|null|undefined} shardID
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.shardID = null;

        /**
         * <blockquote>Review Question<br/>
         * <blockquote>Should this be deprecated?<br/>
         * It's never been used and probably never should be used...<br/>
         * Realm should be determined by node and network parameters.
         * </blockquote></blockquote>
         * <p>
         * The shard/realm in which to create the new smart contract.<br/>
         * This value is currently ignored.
         * @member {proto.RealmID.$Properties|null|undefined} realmID
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.realmID = null;

        /**
         * <blockquote>Review Question<br/>
         * <blockquote>Should this be deprecated?<br/>
         * It's never been used and probably never should be used...<br/>
         * If a realm is used, it must already exist; we shouldn't be creating it
         * without a separate transaction.</blockquote></blockquote>
         * <p>
         * This was intended to provide an admin key for any new realm created
         * during the creation of the smart contract.<br/>
         * This value is currently ignored. a new realm SHALL NOT be created,
         * regardless of the value of `realmID`.
         * @member {proto.Key.$Properties|null|undefined} newRealmAdminKey
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.newRealmAdminKey = null;

        /**
         * A short memo for this smart contract.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.memo = "";

        /**
         * The maximum number of tokens that can be auto-associated with this
         * smart contract.
         * <p>
         * If this is less than or equal to `used_auto_associations` (or 0), then
         * this contract MUST manually associate with a token before transacting
         * in that token.<br/>
         * Following HIP-904 This value may also be `-1` to indicate no limit.<br/>
         * This value MUST NOT be less than `-1`.
         * @member {number} maxAutomaticTokenAssociations
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.maxAutomaticTokenAssociations = 0;

        /**
         * The id of an account, in the same shard and realm as this smart
         * contract, that has signed this transaction, allowing the network to use
         * its balance, when needed, to automatically extend this contract's
         * expiration time.
         * <p>
         * If this field is set, that key MUST sign this transaction.<br/>
         * If this field is set, then the network SHALL deduct the necessary fees
         * from the designated auto renew account, if that account has sufficient
         * balance. If the auto renew account does not have sufficient balance,
         * then the fees for contract renewal SHALL be deducted from the HBAR
         * balance held by the smart contract.<br/>
         * If this field is not set, then all renewal fees SHALL be deducted from
         * the HBAR balance held by this contract.
         * @member {proto.AccountID.$Properties|null|undefined} autoRenewAccountId
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.autoRenewAccountId = null;

        /**
         * An account ID.
         * <p>
         * This smart contract SHALL stake its HBAR via this account as proxy.
         * @member {proto.AccountID.$Properties|null|undefined} stakedAccountId
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.stakedAccountId = null;

        /**
         * The ID of a network node.
         * <p>
         * This smart contract SHALL stake its HBAR to this node.
         * <p>
         * <blockquote>Note: node IDs do fluctuate as node operators change.
         * Most contracts are immutable, and a contract staking to an invalid
         * node ID SHALL NOT participate in staking. Immutable contracts MAY
         * find it more reliable to use a proxy account for staking
         * (via `staked_account_id`) to enable updating the _effective_ staking
         * node ID when necessary through updating the proxy
         * account.</blockquote>
         * @member {number|Long|null|undefined} stakedNodeId
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.stakedNodeId = null;

        /**
         * A flag indicating that this smart contract declines to receive any
         * reward for staking its HBAR balance to help secure the network.
         * <p>
         * If set to true, this smart contract SHALL NOT receive any reward for
         * staking its HBAR balance to help secure the network, regardless of
         * staking configuration, but MAY stake HBAR to support the network
         * without reward.
         * @member {boolean} declineReward
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.declineReward = false;

        /**
         * Details of hooks to add immediately after creating this contract.
         * @member {Array.<com.hedera.hapi.node.hooks.HookCreationDetails.$Properties>} hookCreationDetails
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        ContractCreateTransactionBody.prototype.hookCreationDetails = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ContractCreateTransactionBody initcodeSource.
         * @member {"fileID"|"initcode"|undefined} initcodeSource
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        $Object.defineProperty(ContractCreateTransactionBody.prototype, "initcodeSource", {
            get: $util.oneOfGetter($oneOfFields = ["fileID", "initcode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * ContractCreateTransactionBody stakedId.
         * @member {"stakedAccountId"|"stakedNodeId"|undefined} stakedId
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         */
        $Object.defineProperty(ContractCreateTransactionBody.prototype, "stakedId", {
            get: $util.oneOfGetter($oneOfFields = ["stakedAccountId", "stakedNodeId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ContractCreateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.ContractCreateTransactionBody
         * @static
         * @param {proto.ContractCreateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.ContractCreateTransactionBody} ContractCreateTransactionBody instance
         * @type {{
         *   (properties: proto.ContractCreateTransactionBody.$Shape): proto.ContractCreateTransactionBody & proto.ContractCreateTransactionBody.$Shape;
         *   (properties?: proto.ContractCreateTransactionBody.$Properties): proto.ContractCreateTransactionBody;
         * }}
         */
        ContractCreateTransactionBody.create = function(properties) {
            return new ContractCreateTransactionBody(properties);
        };

        /**
         * Encodes the specified ContractCreateTransactionBody message. Does not implicitly {@link proto.ContractCreateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractCreateTransactionBody
         * @static
         * @param {proto.ContractCreateTransactionBody.$Properties} message ContractCreateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractCreateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas") && (typeof message.gas === "object" ? message.gas.low || message.gas.high : message.gas !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.gas);
            if (message.initialBalance != null && $Object.hasOwnProperty.call(message, "initialBalance") && (typeof message.initialBalance === "object" ? message.initialBalance.low || message.initialBalance.high : message.initialBalance !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.initialBalance);
            if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID"))
                $root.proto.AccountID.encode(message.proxyAccountID, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.constructorParameters != null && $Object.hasOwnProperty.call(message, "constructorParameters") && message.constructorParameters.length)
                writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.constructorParameters);
            if (message.shardID != null && $Object.hasOwnProperty.call(message, "shardID"))
                $root.proto.ShardID.encode(message.shardID, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.realmID != null && $Object.hasOwnProperty.call(message, "realmID"))
                $root.proto.RealmID.encode(message.realmID, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.newRealmAdminKey != null && $Object.hasOwnProperty.call(message, "newRealmAdminKey"))
                $root.proto.Key.encode(message.newRealmAdminKey, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.memo);
            if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations") && message.maxAutomaticTokenAssociations !== 0)
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.maxAutomaticTokenAssociations);
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId"))
                $root.proto.AccountID.encode(message.autoRenewAccountId, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.initcode != null && $Object.hasOwnProperty.call(message, "initcode"))
                writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.initcode);
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId"))
                $root.proto.AccountID.encode(message.stakedAccountId, writer.uint32(/* id 17, wireType 2 =*/138).fork(), _depth + 1).ldelim();
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId"))
                writer.uint32(/* id 18, wireType 0 =*/144).int64(message.stakedNodeId);
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward") && message.declineReward !== false)
                writer.uint32(/* id 19, wireType 0 =*/152).bool(message.declineReward);
            if (message.hookCreationDetails != null && message.hookCreationDetails.length)
                for (let i = 0; i < message.hookCreationDetails.length; ++i)
                    $root.com.hedera.hapi.node.hooks.HookCreationDetails.encode(message.hookCreationDetails[i], writer.uint32(/* id 20, wireType 2 =*/162).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractCreateTransactionBody message, length delimited. Does not implicitly {@link proto.ContractCreateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractCreateTransactionBody
         * @static
         * @param {proto.ContractCreateTransactionBody.$Properties} message ContractCreateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractCreateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractCreateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractCreateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractCreateTransactionBody & proto.ContractCreateTransactionBody.$Shape} ContractCreateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractCreateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractCreateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                        message.initcodeSource = "fileID";
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.initcode = reader.bytes();
                        message.initcodeSource = "initcode";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.gas = value;
                        else
                            delete message.gas;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.initialBalance = value;
                        else
                            delete message.initialBalance;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.proxyAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.proxyAccountID);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.constructorParameters = value;
                        else
                            delete message.constructorParameters;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.shardID = $root.proto.ShardID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.shardID);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.realmID = $root.proto.RealmID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.realmID);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.newRealmAdminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.newRealmAdminKey);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.maxAutomaticTokenAssociations = value;
                        else
                            delete message.maxAutomaticTokenAssociations;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewAccountId);
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        message.stakedAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stakedAccountId);
                        message.stakedId = "stakedAccountId";
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        message.stakedNodeId = reader.int64();
                        message.stakedId = "stakedNodeId";
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.declineReward = value;
                        else
                            delete message.declineReward;
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hookCreationDetails && message.hookCreationDetails.length))
                            message.hookCreationDetails = [];
                        message.hookCreationDetails.push($root.com.hedera.hapi.node.hooks.HookCreationDetails.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractCreateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractCreateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractCreateTransactionBody & proto.ContractCreateTransactionBody.$Shape} ContractCreateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractCreateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractCreateTransactionBody message.
         * @function verify
         * @memberof proto.ContractCreateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractCreateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                properties.initcodeSource = 1;
                {
                    let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                    if (error)
                        return "fileID." + error;
                }
            }
            if (message.initcode != null && $Object.hasOwnProperty.call(message, "initcode")) {
                if (properties.initcodeSource === 1)
                    return "initcodeSource: multiple values";
                properties.initcodeSource = 1;
                if (!(message.initcode && typeof message.initcode.length === "number" || $util.isString(message.initcode)))
                    return "initcode: buffer expected";
            }
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (!$util.isInteger(message.gas) && !(message.gas && $util.isInteger(message.gas.low) && $util.isInteger(message.gas.high)))
                    return "gas: integer|Long expected";
            if (message.initialBalance != null && $Object.hasOwnProperty.call(message, "initialBalance"))
                if (!$util.isInteger(message.initialBalance) && !(message.initialBalance && $util.isInteger(message.initialBalance.low) && $util.isInteger(message.initialBalance.high)))
                    return "initialBalance: integer|Long expected";
            if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID")) {
                let error = $root.proto.AccountID.verify(message.proxyAccountID, _depth + 1);
                if (error)
                    return "proxyAccountID." + error;
            }
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                if (error)
                    return "autoRenewPeriod." + error;
            }
            if (message.constructorParameters != null && $Object.hasOwnProperty.call(message, "constructorParameters"))
                if (!(message.constructorParameters && typeof message.constructorParameters.length === "number" || $util.isString(message.constructorParameters)))
                    return "constructorParameters: buffer expected";
            if (message.shardID != null && $Object.hasOwnProperty.call(message, "shardID")) {
                let error = $root.proto.ShardID.verify(message.shardID, _depth + 1);
                if (error)
                    return "shardID." + error;
            }
            if (message.realmID != null && $Object.hasOwnProperty.call(message, "realmID")) {
                let error = $root.proto.RealmID.verify(message.realmID, _depth + 1);
                if (error)
                    return "realmID." + error;
            }
            if (message.newRealmAdminKey != null && $Object.hasOwnProperty.call(message, "newRealmAdminKey")) {
                let error = $root.proto.Key.verify(message.newRealmAdminKey, _depth + 1);
                if (error)
                    return "newRealmAdminKey." + error;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                if (!$util.isInteger(message.maxAutomaticTokenAssociations))
                    return "maxAutomaticTokenAssociations: integer expected";
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId")) {
                let error = $root.proto.AccountID.verify(message.autoRenewAccountId, _depth + 1);
                if (error)
                    return "autoRenewAccountId." + error;
            }
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId")) {
                properties.stakedId = 1;
                {
                    let error = $root.proto.AccountID.verify(message.stakedAccountId, _depth + 1);
                    if (error)
                        return "stakedAccountId." + error;
                }
            }
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId")) {
                if (properties.stakedId === 1)
                    return "stakedId: multiple values";
                properties.stakedId = 1;
                if (!$util.isInteger(message.stakedNodeId) && !(message.stakedNodeId && $util.isInteger(message.stakedNodeId.low) && $util.isInteger(message.stakedNodeId.high)))
                    return "stakedNodeId: integer|Long expected";
            }
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                if (typeof message.declineReward !== "boolean")
                    return "declineReward: boolean expected";
            if (message.hookCreationDetails != null && $Object.hasOwnProperty.call(message, "hookCreationDetails")) {
                if (!$Array.isArray(message.hookCreationDetails))
                    return "hookCreationDetails: array expected";
                for (let i = 0; i < message.hookCreationDetails.length; ++i) {
                    let error = $root.com.hedera.hapi.node.hooks.HookCreationDetails.verify(message.hookCreationDetails[i], _depth + 1);
                    if (error)
                        return "hookCreationDetails." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ContractCreateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractCreateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractCreateTransactionBody} ContractCreateTransactionBody
         */
        ContractCreateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractCreateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractCreateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractCreateTransactionBody();
            if (object.fileID != null) {
                if (!$util.isObject(object.fileID))
                    throw $TypeError(".proto.ContractCreateTransactionBody.fileID: object expected");
                message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
            }
            if (object.initcode != null)
                if (typeof object.initcode === "string")
                    $util.base64.decode(object.initcode, message.initcode = $util.newBuffer($util.base64.length(object.initcode)), 0);
                else if (object.initcode.length >= 0)
                    message.initcode = object.initcode;
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.ContractCreateTransactionBody.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.gas != null)
                if (typeof object.gas === "object" ? object.gas.low || object.gas.high : $Number(object.gas) !== 0)
                    if ($util.Long)
                        message.gas = $util.Long.fromValue(object.gas, false);
                    else if (typeof object.gas === "string")
                        message.gas = $parseInt(object.gas, 10);
                    else if (typeof object.gas === "number")
                        message.gas = object.gas;
                    else if (typeof object.gas === "object")
                        message.gas = new $util.LongBits(object.gas.low >>> 0, object.gas.high >>> 0).toNumber();
            if (object.initialBalance != null)
                if (typeof object.initialBalance === "object" ? object.initialBalance.low || object.initialBalance.high : $Number(object.initialBalance) !== 0)
                    if ($util.Long)
                        message.initialBalance = $util.Long.fromValue(object.initialBalance, false);
                    else if (typeof object.initialBalance === "string")
                        message.initialBalance = $parseInt(object.initialBalance, 10);
                    else if (typeof object.initialBalance === "number")
                        message.initialBalance = object.initialBalance;
                    else if (typeof object.initialBalance === "object")
                        message.initialBalance = new $util.LongBits(object.initialBalance.low >>> 0, object.initialBalance.high >>> 0).toNumber();
            if (object.proxyAccountID != null) {
                if (!$util.isObject(object.proxyAccountID))
                    throw $TypeError(".proto.ContractCreateTransactionBody.proxyAccountID: object expected");
                message.proxyAccountID = $root.proto.AccountID.fromObject(object.proxyAccountID, _depth + 1);
            }
            if (object.autoRenewPeriod != null) {
                if (!$util.isObject(object.autoRenewPeriod))
                    throw $TypeError(".proto.ContractCreateTransactionBody.autoRenewPeriod: object expected");
                message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
            }
            if (object.constructorParameters != null)
                if (object.constructorParameters.length)
                    if (typeof object.constructorParameters === "string")
                        $util.base64.decode(object.constructorParameters, message.constructorParameters = $util.newBuffer($util.base64.length(object.constructorParameters)), 0);
                    else if (object.constructorParameters.length >= 0)
                        message.constructorParameters = object.constructorParameters;
            if (object.shardID != null) {
                if (!$util.isObject(object.shardID))
                    throw $TypeError(".proto.ContractCreateTransactionBody.shardID: object expected");
                message.shardID = $root.proto.ShardID.fromObject(object.shardID, _depth + 1);
            }
            if (object.realmID != null) {
                if (!$util.isObject(object.realmID))
                    throw $TypeError(".proto.ContractCreateTransactionBody.realmID: object expected");
                message.realmID = $root.proto.RealmID.fromObject(object.realmID, _depth + 1);
            }
            if (object.newRealmAdminKey != null) {
                if (!$util.isObject(object.newRealmAdminKey))
                    throw $TypeError(".proto.ContractCreateTransactionBody.newRealmAdminKey: object expected");
                message.newRealmAdminKey = $root.proto.Key.fromObject(object.newRealmAdminKey, _depth + 1);
            }
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.maxAutomaticTokenAssociations != null)
                if ($Number(object.maxAutomaticTokenAssociations) !== 0)
                    message.maxAutomaticTokenAssociations = object.maxAutomaticTokenAssociations | 0;
            if (object.autoRenewAccountId != null) {
                if (!$util.isObject(object.autoRenewAccountId))
                    throw $TypeError(".proto.ContractCreateTransactionBody.autoRenewAccountId: object expected");
                message.autoRenewAccountId = $root.proto.AccountID.fromObject(object.autoRenewAccountId, _depth + 1);
            }
            if (object.stakedAccountId != null) {
                if (!$util.isObject(object.stakedAccountId))
                    throw $TypeError(".proto.ContractCreateTransactionBody.stakedAccountId: object expected");
                message.stakedAccountId = $root.proto.AccountID.fromObject(object.stakedAccountId, _depth + 1);
            }
            if (object.stakedNodeId != null)
                if ($util.Long)
                    message.stakedNodeId = $util.Long.fromValue(object.stakedNodeId, false);
                else if (typeof object.stakedNodeId === "string")
                    message.stakedNodeId = $parseInt(object.stakedNodeId, 10);
                else if (typeof object.stakedNodeId === "number")
                    message.stakedNodeId = object.stakedNodeId;
                else if (typeof object.stakedNodeId === "object")
                    message.stakedNodeId = new $util.LongBits(object.stakedNodeId.low >>> 0, object.stakedNodeId.high >>> 0).toNumber();
            if (object.declineReward != null)
                if (object.declineReward)
                    message.declineReward = $Boolean(object.declineReward);
            if (object.hookCreationDetails) {
                if (!$Array.isArray(object.hookCreationDetails))
                    throw $TypeError(".proto.ContractCreateTransactionBody.hookCreationDetails: array expected");
                message.hookCreationDetails = $Array(object.hookCreationDetails.length);
                for (let i = 0; i < object.hookCreationDetails.length; ++i) {
                    if (!$util.isObject(object.hookCreationDetails[i]))
                        throw $TypeError(".proto.ContractCreateTransactionBody.hookCreationDetails: object expected");
                    message.hookCreationDetails[i] = $root.com.hedera.hapi.node.hooks.HookCreationDetails.fromObject(object.hookCreationDetails[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractCreateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractCreateTransactionBody
         * @static
         * @param {proto.ContractCreateTransactionBody} message ContractCreateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractCreateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.hookCreationDetails = [];
            if (options.defaults) {
                object.adminKey = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.gas = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gas = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.initialBalance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.initialBalance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.proxyAccountID = null;
                object.autoRenewPeriod = null;
                if (options.bytes === $String)
                    object.constructorParameters = "";
                else {
                    object.constructorParameters = [];
                    if (options.bytes !== $Array)
                        object.constructorParameters = $util.newBuffer(object.constructorParameters);
                }
                object.shardID = null;
                object.realmID = null;
                object.newRealmAdminKey = null;
                object.memo = "";
                object.maxAutomaticTokenAssociations = 0;
                object.autoRenewAccountId = null;
                object.declineReward = false;
            }
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
                if (options.oneofs)
                    object.initcodeSource = "fileID";
            }
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gas = typeof message.gas === "number" ? $BigInt(message.gas) : $util.Long.fromBits(message.gas.low >>> 0, message.gas.high >>> 0, false).toBigInt();
                else if (typeof message.gas === "number")
                    object.gas = options.longs === $String ? $String(message.gas) : message.gas;
                else
                    object.gas = options.longs === $String ? $util.Long.prototype.toString.call(message.gas) : options.longs === $Number ? new $util.LongBits(message.gas.low >>> 0, message.gas.high >>> 0).toNumber() : message.gas;
            if (message.initialBalance != null && $Object.hasOwnProperty.call(message, "initialBalance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.initialBalance = typeof message.initialBalance === "number" ? $BigInt(message.initialBalance) : $util.Long.fromBits(message.initialBalance.low >>> 0, message.initialBalance.high >>> 0, false).toBigInt();
                else if (typeof message.initialBalance === "number")
                    object.initialBalance = options.longs === $String ? $String(message.initialBalance) : message.initialBalance;
                else
                    object.initialBalance = options.longs === $String ? $util.Long.prototype.toString.call(message.initialBalance) : options.longs === $Number ? new $util.LongBits(message.initialBalance.low >>> 0, message.initialBalance.high >>> 0).toNumber() : message.initialBalance;
            if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID"))
                object.proxyAccountID = $root.proto.AccountID.toObject(message.proxyAccountID, options, _depth + 1);
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
            if (message.constructorParameters != null && $Object.hasOwnProperty.call(message, "constructorParameters"))
                object.constructorParameters = options.bytes === $String ? $util.base64.encode(message.constructorParameters, 0, message.constructorParameters.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.constructorParameters) : message.constructorParameters;
            if (message.shardID != null && $Object.hasOwnProperty.call(message, "shardID"))
                object.shardID = $root.proto.ShardID.toObject(message.shardID, options, _depth + 1);
            if (message.realmID != null && $Object.hasOwnProperty.call(message, "realmID"))
                object.realmID = $root.proto.RealmID.toObject(message.realmID, options, _depth + 1);
            if (message.newRealmAdminKey != null && $Object.hasOwnProperty.call(message, "newRealmAdminKey"))
                object.newRealmAdminKey = $root.proto.Key.toObject(message.newRealmAdminKey, options, _depth + 1);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                object.maxAutomaticTokenAssociations = message.maxAutomaticTokenAssociations;
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId"))
                object.autoRenewAccountId = $root.proto.AccountID.toObject(message.autoRenewAccountId, options, _depth + 1);
            if (message.initcode != null && $Object.hasOwnProperty.call(message, "initcode")) {
                object.initcode = options.bytes === $String ? $util.base64.encode(message.initcode, 0, message.initcode.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.initcode) : message.initcode;
                if (options.oneofs)
                    object.initcodeSource = "initcode";
            }
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId")) {
                object.stakedAccountId = $root.proto.AccountID.toObject(message.stakedAccountId, options, _depth + 1);
                if (options.oneofs)
                    object.stakedId = "stakedAccountId";
            }
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakedNodeId = typeof message.stakedNodeId === "number" ? $BigInt(message.stakedNodeId) : $util.Long.fromBits(message.stakedNodeId.low >>> 0, message.stakedNodeId.high >>> 0, false).toBigInt();
                else if (typeof message.stakedNodeId === "number")
                    object.stakedNodeId = options.longs === $String ? $String(message.stakedNodeId) : message.stakedNodeId;
                else
                    object.stakedNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.stakedNodeId) : options.longs === $Number ? new $util.LongBits(message.stakedNodeId.low >>> 0, message.stakedNodeId.high >>> 0).toNumber() : message.stakedNodeId;
                if (options.oneofs)
                    object.stakedId = "stakedNodeId";
            }
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                object.declineReward = message.declineReward;
            if (message.hookCreationDetails && message.hookCreationDetails.length) {
                object.hookCreationDetails = $Array(message.hookCreationDetails.length);
                for (let j = 0; j < message.hookCreationDetails.length; ++j)
                    object.hookCreationDetails[j] = $root.com.hedera.hapi.node.hooks.HookCreationDetails.toObject(message.hookCreationDetails[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ContractCreateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.ContractCreateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractCreateTransactionBody.prototype.toJSON = function() {
            return ContractCreateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractCreateTransactionBody
         * @function getTypeUrl
         * @memberof proto.ContractCreateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractCreateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractCreateTransactionBody";
        };

        return ContractCreateTransactionBody;
    })();

    proto.Duration = (function() {

        /**
         * Properties of a Duration.
         * @typedef {Object} proto.Duration.$Properties
         * @property {number|Long|null} [seconds] The number of seconds for this duration.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Duration.
         * @memberof proto
         * @interface IDuration
         * @augments proto.Duration.$Properties
         * @deprecated Use proto.Duration.$Properties instead.
         */

        /**
         * Shape of a Duration.
         * @typedef {proto.Duration.$Properties} proto.Duration.$Shape
         */

        /**
         * Constructs a new Duration.
         * @memberof proto
         * @classdesc A length of time in seconds.
         * 
         * It is RECOMMENDED that this message be used whenever an amount of time,
         * rather than a specific point in time, is needed.
         * @constructor
         * @param {proto.Duration.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Duration = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The number of seconds for this duration.
         * @member {number|Long} seconds
         * @memberof proto.Duration
         * @instance
         */
        Duration.prototype.seconds = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new Duration instance using the specified properties.
         * @function create
         * @memberof proto.Duration
         * @static
         * @param {proto.Duration.$Properties=} [properties] Properties to set
         * @returns {proto.Duration} Duration instance
         * @type {{
         *   (properties: proto.Duration.$Shape): proto.Duration & proto.Duration.$Shape;
         *   (properties?: proto.Duration.$Properties): proto.Duration;
         * }}
         */
        Duration.create = function(properties) {
            return new Duration(properties);
        };

        /**
         * Encodes the specified Duration message. Does not implicitly {@link proto.Duration.verify|verify} messages.
         * @function encode
         * @memberof proto.Duration
         * @static
         * @param {proto.Duration.$Properties} message Duration message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Duration.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.seconds != null && $Object.hasOwnProperty.call(message, "seconds") && (typeof message.seconds === "object" ? message.seconds.low || message.seconds.high : message.seconds !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.seconds);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Duration message, length delimited. Does not implicitly {@link proto.Duration.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Duration
         * @static
         * @param {proto.Duration.$Properties} message Duration message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Duration.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Duration message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Duration
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Duration & proto.Duration.$Shape} Duration
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Duration.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Duration();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.seconds = value;
                        else
                            delete message.seconds;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Duration message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Duration
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Duration & proto.Duration.$Shape} Duration
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Duration.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Duration message.
         * @function verify
         * @memberof proto.Duration
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Duration.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seconds != null && $Object.hasOwnProperty.call(message, "seconds"))
                if (!$util.isInteger(message.seconds) && !(message.seconds && $util.isInteger(message.seconds.low) && $util.isInteger(message.seconds.high)))
                    return "seconds: integer|Long expected";
            return null;
        };

        /**
         * Creates a Duration message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Duration
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Duration} Duration
         */
        Duration.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Duration)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Duration: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Duration();
            if (object.seconds != null)
                if (typeof object.seconds === "object" ? object.seconds.low || object.seconds.high : $Number(object.seconds) !== 0)
                    if ($util.Long)
                        message.seconds = $util.Long.fromValue(object.seconds, false);
                    else if (typeof object.seconds === "string")
                        message.seconds = $parseInt(object.seconds, 10);
                    else if (typeof object.seconds === "number")
                        message.seconds = object.seconds;
                    else if (typeof object.seconds === "object")
                        message.seconds = new $util.LongBits(object.seconds.low >>> 0, object.seconds.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a Duration message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Duration
         * @static
         * @param {proto.Duration} message Duration
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Duration.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.seconds = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.seconds = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.seconds != null && $Object.hasOwnProperty.call(message, "seconds"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.seconds = typeof message.seconds === "number" ? $BigInt(message.seconds) : $util.Long.fromBits(message.seconds.low >>> 0, message.seconds.high >>> 0, false).toBigInt();
                else if (typeof message.seconds === "number")
                    object.seconds = options.longs === $String ? $String(message.seconds) : message.seconds;
                else
                    object.seconds = options.longs === $String ? $util.Long.prototype.toString.call(message.seconds) : options.longs === $Number ? new $util.LongBits(message.seconds.low >>> 0, message.seconds.high >>> 0).toNumber() : message.seconds;
            return object;
        };

        /**
         * Converts this Duration to JSON.
         * @function toJSON
         * @memberof proto.Duration
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Duration.prototype.toJSON = function() {
            return Duration.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Duration
         * @function getTypeUrl
         * @memberof proto.Duration
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Duration.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Duration";
        };

        return Duration;
    })();

    proto.ContractUpdateTransactionBody = (function() {

        /**
         * Properties of a ContractUpdateTransactionBody.
         * @typedef {Object} proto.ContractUpdateTransactionBody.$Properties
         * @property {proto.ContractID.$Properties|null} [contractID] The contact ID that identifies the smart contract to be updated.<br/>
         * This field MUST be set, and MUST NOT be a default ID (`0.0.0`).
         * @property {proto.Timestamp.$Properties|null} [expirationTime] If set, modify the time at which this contract will expire.<br/>
         * An expired contract requires a rent payment to "renew" the contract.
         * A transaction to update this field is how that rent payment is made.
         * <p>
         * This value MUST NOT be less than the current `expirationTime`
         * of the contract. If this value is earlier than the current
         * value, the transaction SHALL fail with response
         * code `EXPIRATION_REDUCTION_NOT_ALLOWED`.
         * @property {proto.Key.$Properties|null} [adminKey] If set, modify the key that authorizes updates to the contract.
         * <p>
         * If this field is set to a valid Key, this key and the previously set key
         * MUST both sign this transaction.<br/>
         * If this value is an empty `KeyList`, the prior key MUST sign this
         * transaction, and the smart contract SHALL be immutable after this
         * transaction completes, except for expiration and renewal.<br/>
         * If this value is not an empty `KeyList`, but does not contain any
         * cryptographic keys, or is otherwise malformed, this transaction SHALL
         * fail with response code `INVALID_ADMIN_KEY`.
         * @property {proto.AccountID.$Properties|null} [proxyAccountID] Replaced with `staked_id` alternatives.
         * This field is unused and SHALL NOT modify the contract state.<br/>
         * The id of an account to which the contract is proxy staked
         * @property {proto.Duration.$Properties|null} [autoRenewPeriod] If set, modify the duration added to expiration time by each
         * auto-renewal to this value.
         * @property {proto.FileID.$Properties|null} [fileID] This field is unused and SHALL NOT modify the contract state.<br/>
         * Previously, an ID of a file containing the bytecode of the Solidity
         * transaction that created this contract.
         * @property {string|null} [memo] This value could not accurately distinguish unset or deliberately
         * empty. memoWrapper should be used instead.<br/>
         * @property {google.protobuf.StringValue.$Properties|null} [memoWrapper] If set, modify the short memo for this smart contract.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {google.protobuf.Int32Value.$Properties|null} [maxAutomaticTokenAssociations] If set, modify the maximum number of tokens that can be auto-associated with the
         * contract.
         * <p>
         * If this is set and less than or equal to `used_auto_associations`, or 0, then this contract
         * MUST manually associate with a token before transacting in that token.<br/>
         * This value MAY also be `-1` to indicate no limit.<br/>
         * This value MUST NOT be less than `-1`.
         * @property {proto.AccountID.$Properties|null} [autoRenewAccountId] If set, modify the account, in the same shard and realm as this smart
         * contract, that has agreed to allow the network to use its balance, when
         * needed, to automatically extend this contract's expiration time.
         * <p>
         * If this field is set to a non-default value, that Account MUST sign this
         * transaction.<br/>
         * If this field is set to a default AccountID value (`0.0.0`), any
         * pre-existing `auto_renew_account_id` value SHALL be removed on success.
         * @property {proto.AccountID.$Properties|null} [stakedAccountId] An account identifier.<br/>
         * A staked account acts as a proxy, and this contract effectively
         * nominates the same node as the identified account.
         * <p>
         * If set, modify this smart contract such that it SHALL stake its HBAR
         * to the same node as the identified account.<br/>
         * If this field is set to a default AccountID value (`0.0.0`), any
         * pre-existing `staked_account_id` value SHALL be removed on success.
         * @property {number|Long|null} [stakedNodeId] A node identifier.<br/>
         * A staked node identifier indicates the consensus node that this
         * account nominates for staking.
         * <p>
         * If set, modify this smart contract such that it SHALL stake its HBAR
         * to this node.
         * If set to a the value `-1` any pre-existing `staked_node_id` value
         * SHALL be removed on success.
         * <p>
         * <blockquote>Note: node IDs do fluctuate as node operators change.
         * Most contracts are immutable, and a contract staking to an invalid
         * node ID SHALL NOT participate in staking. Immutable contracts may
         * find it more reliable to use a proxy account for staking (via
         * `staked_account_id`) to enable updating the _effective_ staking node
         * ID when necessary through updating the proxy account.</blockquote>
         * @property {google.protobuf.BoolValue.$Properties|null} [declineReward] A flag indicating if staking rewards are declined.<br/>
         * If set, modify the flag indicating if this contract declines to accept
         * rewards for staking its HBAR to secure the network.
         * <p>
         * If set to true, this smart contract SHALL NOT receive any reward for
         * staking its HBAR balance to help secure the network, regardless of
         * staking configuration, but MAY stake HBAR to support the network
         * without reward.
         * @property {Array.<number|Long>|null} [hookIdsToDelete] The ids the hooks to delete from the contract.
         * @property {Array.<com.hedera.hapi.node.hooks.HookCreationDetails.$Properties>|null} [hookCreationDetails] The hooks to create for the contract.
         * @property {"memo"|"memoWrapper"} [memoField] ContractUpdateTransactionBody memoField
         * @property {"stakedAccountId"|"stakedNodeId"} [stakedId] ContractUpdateTransactionBody stakedId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractUpdateTransactionBody.
         * @memberof proto
         * @interface IContractUpdateTransactionBody
         * @augments proto.ContractUpdateTransactionBody.$Properties
         * @deprecated Use proto.ContractUpdateTransactionBody.$Properties instead.
         */

        /**
         * Narrowed shape of a ContractUpdateTransactionBody.
         * @typedef {{
         *   contractID?: proto.ContractID.$Shape|null;
         *   expirationTime?: proto.Timestamp.$Shape|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   proxyAccountID?: proto.AccountID.$Shape|null;
         *   autoRenewPeriod?: proto.Duration.$Shape|null;
         *   fileID?: proto.FileID.$Shape|null;
         *   memo?: string|null;
         *   memoWrapper?: google.protobuf.StringValue.$Shape|null;
         *   maxAutomaticTokenAssociations?: google.protobuf.Int32Value.$Shape|null;
         *   autoRenewAccountId?: proto.AccountID.$Shape|null;
         *   stakedAccountId?: proto.AccountID.$Shape|null;
         *   stakedNodeId?: number|Long|null;
         *   declineReward?: google.protobuf.BoolValue.$Shape|null;
         *   hookIdsToDelete?: Array.<number|Long>|null;
         *   hookCreationDetails?: Array.<com.hedera.hapi.node.hooks.HookCreationDetails.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ memoField?: undefined; memo?: null; memoWrapper?: null }|{ memoField?: "memo"; memo: string; memoWrapper?: null }|{ memoField?: "memoWrapper"; memo?: null; memoWrapper: google.protobuf.StringValue.$Shape })
         * ) & (
         *   ({ stakedId?: undefined; stakedAccountId?: null; stakedNodeId?: null }|{ stakedId?: "stakedAccountId"; stakedAccountId: proto.AccountID.$Shape; stakedNodeId?: null }|{ stakedId?: "stakedNodeId"; stakedAccountId?: null; stakedNodeId: number|Long })
         * )} proto.ContractUpdateTransactionBody.$Shape
         */

        /**
         * Constructs a new ContractUpdateTransactionBody.
         * @memberof proto
         * @classdesc Modify the current state of a smart contract.
         * 
         * ### Requirements
         * - The `adminKey` MUST sign all contract update transactions except one
         * that only updates the `expirationTime`.
         * - A transaction that modifies any field other than `expirationTime` for
         * a contract without a valid `adminKey` set SHALL fail with response
         * code `MODIFYING_IMMUTABLE_CONTRACT`.
         * - Fields set to non-default values in this transaction SHALL be updated on
         * success. Fields not set to non-default values SHALL NOT be
         * updated on success.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.ContractUpdateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractUpdateTransactionBody = function (properties) {
            this.hookIdsToDelete = [];
            this.hookCreationDetails = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The contact ID that identifies the smart contract to be updated.<br/>
         * This field MUST be set, and MUST NOT be a default ID (`0.0.0`).
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.contractID = null;

        /**
         * If set, modify the time at which this contract will expire.<br/>
         * An expired contract requires a rent payment to "renew" the contract.
         * A transaction to update this field is how that rent payment is made.
         * <p>
         * This value MUST NOT be less than the current `expirationTime`
         * of the contract. If this value is earlier than the current
         * value, the transaction SHALL fail with response
         * code `EXPIRATION_REDUCTION_NOT_ALLOWED`.
         * @member {proto.Timestamp.$Properties|null|undefined} expirationTime
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.expirationTime = null;

        /**
         * If set, modify the key that authorizes updates to the contract.
         * <p>
         * If this field is set to a valid Key, this key and the previously set key
         * MUST both sign this transaction.<br/>
         * If this value is an empty `KeyList`, the prior key MUST sign this
         * transaction, and the smart contract SHALL be immutable after this
         * transaction completes, except for expiration and renewal.<br/>
         * If this value is not an empty `KeyList`, but does not contain any
         * cryptographic keys, or is otherwise malformed, this transaction SHALL
         * fail with response code `INVALID_ADMIN_KEY`.
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.adminKey = null;

        /**
         * Replaced with `staked_id` alternatives.
         * This field is unused and SHALL NOT modify the contract state.<br/>
         * The id of an account to which the contract is proxy staked
         * @member {proto.AccountID.$Properties|null|undefined} proxyAccountID
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.proxyAccountID = null;

        /**
         * If set, modify the duration added to expiration time by each
         * auto-renewal to this value.
         * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.autoRenewPeriod = null;

        /**
         * This field is unused and SHALL NOT modify the contract state.<br/>
         * Previously, an ID of a file containing the bytecode of the Solidity
         * transaction that created this contract.
         * @member {proto.FileID.$Properties|null|undefined} fileID
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.fileID = null;

        /**
         * This value could not accurately distinguish unset or deliberately
         * empty. memoWrapper should be used instead.<br/>
         * @member {string|null|undefined} memo
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.memo = null;

        /**
         * If set, modify the short memo for this smart contract.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {google.protobuf.StringValue.$Properties|null|undefined} memoWrapper
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.memoWrapper = null;

        /**
         * If set, modify the maximum number of tokens that can be auto-associated with the
         * contract.
         * <p>
         * If this is set and less than or equal to `used_auto_associations`, or 0, then this contract
         * MUST manually associate with a token before transacting in that token.<br/>
         * This value MAY also be `-1` to indicate no limit.<br/>
         * This value MUST NOT be less than `-1`.
         * @member {google.protobuf.Int32Value.$Properties|null|undefined} maxAutomaticTokenAssociations
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.maxAutomaticTokenAssociations = null;

        /**
         * If set, modify the account, in the same shard and realm as this smart
         * contract, that has agreed to allow the network to use its balance, when
         * needed, to automatically extend this contract's expiration time.
         * <p>
         * If this field is set to a non-default value, that Account MUST sign this
         * transaction.<br/>
         * If this field is set to a default AccountID value (`0.0.0`), any
         * pre-existing `auto_renew_account_id` value SHALL be removed on success.
         * @member {proto.AccountID.$Properties|null|undefined} autoRenewAccountId
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.autoRenewAccountId = null;

        /**
         * An account identifier.<br/>
         * A staked account acts as a proxy, and this contract effectively
         * nominates the same node as the identified account.
         * <p>
         * If set, modify this smart contract such that it SHALL stake its HBAR
         * to the same node as the identified account.<br/>
         * If this field is set to a default AccountID value (`0.0.0`), any
         * pre-existing `staked_account_id` value SHALL be removed on success.
         * @member {proto.AccountID.$Properties|null|undefined} stakedAccountId
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.stakedAccountId = null;

        /**
         * A node identifier.<br/>
         * A staked node identifier indicates the consensus node that this
         * account nominates for staking.
         * <p>
         * If set, modify this smart contract such that it SHALL stake its HBAR
         * to this node.
         * If set to a the value `-1` any pre-existing `staked_node_id` value
         * SHALL be removed on success.
         * <p>
         * <blockquote>Note: node IDs do fluctuate as node operators change.
         * Most contracts are immutable, and a contract staking to an invalid
         * node ID SHALL NOT participate in staking. Immutable contracts may
         * find it more reliable to use a proxy account for staking (via
         * `staked_account_id`) to enable updating the _effective_ staking node
         * ID when necessary through updating the proxy account.</blockquote>
         * @member {number|Long|null|undefined} stakedNodeId
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.stakedNodeId = null;

        /**
         * A flag indicating if staking rewards are declined.<br/>
         * If set, modify the flag indicating if this contract declines to accept
         * rewards for staking its HBAR to secure the network.
         * <p>
         * If set to true, this smart contract SHALL NOT receive any reward for
         * staking its HBAR balance to help secure the network, regardless of
         * staking configuration, but MAY stake HBAR to support the network
         * without reward.
         * @member {google.protobuf.BoolValue.$Properties|null|undefined} declineReward
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.declineReward = null;

        /**
         * The ids the hooks to delete from the contract.
         * @member {Array.<number|Long>} hookIdsToDelete
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.hookIdsToDelete = $util.emptyArray;

        /**
         * The hooks to create for the contract.
         * @member {Array.<com.hedera.hapi.node.hooks.HookCreationDetails.$Properties>} hookCreationDetails
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        ContractUpdateTransactionBody.prototype.hookCreationDetails = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ContractUpdateTransactionBody memoField.
         * @member {"memo"|"memoWrapper"|undefined} memoField
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        $Object.defineProperty(ContractUpdateTransactionBody.prototype, "memoField", {
            get: $util.oneOfGetter($oneOfFields = ["memo", "memoWrapper"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * ContractUpdateTransactionBody stakedId.
         * @member {"stakedAccountId"|"stakedNodeId"|undefined} stakedId
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         */
        $Object.defineProperty(ContractUpdateTransactionBody.prototype, "stakedId", {
            get: $util.oneOfGetter($oneOfFields = ["stakedAccountId", "stakedNodeId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ContractUpdateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.ContractUpdateTransactionBody
         * @static
         * @param {proto.ContractUpdateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.ContractUpdateTransactionBody} ContractUpdateTransactionBody instance
         * @type {{
         *   (properties: proto.ContractUpdateTransactionBody.$Shape): proto.ContractUpdateTransactionBody & proto.ContractUpdateTransactionBody.$Shape;
         *   (properties?: proto.ContractUpdateTransactionBody.$Properties): proto.ContractUpdateTransactionBody;
         * }}
         */
        ContractUpdateTransactionBody.create = function(properties) {
            return new ContractUpdateTransactionBody(properties);
        };

        /**
         * Encodes the specified ContractUpdateTransactionBody message. Does not implicitly {@link proto.ContractUpdateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractUpdateTransactionBody
         * @static
         * @param {proto.ContractUpdateTransactionBody.$Properties} message ContractUpdateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractUpdateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                $root.proto.Timestamp.encode(message.expirationTime, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID"))
                $root.proto.AccountID.encode(message.proxyAccountID, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.memo);
            if (message.memoWrapper != null && $Object.hasOwnProperty.call(message, "memoWrapper"))
                $root.google.protobuf.StringValue.encode(message.memoWrapper, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                $root.google.protobuf.Int32Value.encode(message.maxAutomaticTokenAssociations, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId"))
                $root.proto.AccountID.encode(message.autoRenewAccountId, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId"))
                $root.proto.AccountID.encode(message.stakedAccountId, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId"))
                writer.uint32(/* id 14, wireType 0 =*/112).int64(message.stakedNodeId);
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                $root.google.protobuf.BoolValue.encode(message.declineReward, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.hookIdsToDelete != null && message.hookIdsToDelete.length)
                writer.uint32(/* id 16, wireType 2 =*/130).int64s(message.hookIdsToDelete);
            if (message.hookCreationDetails != null && message.hookCreationDetails.length)
                for (let i = 0; i < message.hookCreationDetails.length; ++i)
                    $root.com.hedera.hapi.node.hooks.HookCreationDetails.encode(message.hookCreationDetails[i], writer.uint32(/* id 17, wireType 2 =*/138).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractUpdateTransactionBody message, length delimited. Does not implicitly {@link proto.ContractUpdateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractUpdateTransactionBody
         * @static
         * @param {proto.ContractUpdateTransactionBody.$Properties} message ContractUpdateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractUpdateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractUpdateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractUpdateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractUpdateTransactionBody & proto.ContractUpdateTransactionBody.$Shape} ContractUpdateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractUpdateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractUpdateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.expirationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.proxyAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.proxyAccountID);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.memo = reader.stringVerify();
                        message.memoField = "memo";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.memoWrapper = $root.google.protobuf.StringValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.memoWrapper);
                        message.memoField = "memoWrapper";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.maxAutomaticTokenAssociations = $root.google.protobuf.Int32Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.maxAutomaticTokenAssociations);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewAccountId);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.stakedAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stakedAccountId);
                        message.stakedId = "stakedAccountId";
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        message.stakedNodeId = reader.int64();
                        message.stakedId = "stakedNodeId";
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.declineReward = $root.google.protobuf.BoolValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.declineReward);
                        continue;
                    }
                case 16: {
                        if (wireType === 2) {
                            if (!(message.hookIdsToDelete && message.hookIdsToDelete.length))
                                message.hookIdsToDelete = [];
                            reader.int64s(message.hookIdsToDelete);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.hookIdsToDelete && message.hookIdsToDelete.length))
                            message.hookIdsToDelete = [];
                        message.hookIdsToDelete.push(reader.int64());
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hookCreationDetails && message.hookCreationDetails.length))
                            message.hookCreationDetails = [];
                        message.hookCreationDetails.push($root.com.hedera.hapi.node.hooks.HookCreationDetails.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractUpdateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractUpdateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractUpdateTransactionBody & proto.ContractUpdateTransactionBody.$Shape} ContractUpdateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractUpdateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractUpdateTransactionBody message.
         * @function verify
         * @memberof proto.ContractUpdateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractUpdateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                let error = $root.proto.Timestamp.verify(message.expirationTime, _depth + 1);
                if (error)
                    return "expirationTime." + error;
            }
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID")) {
                let error = $root.proto.AccountID.verify(message.proxyAccountID, _depth + 1);
                if (error)
                    return "proxyAccountID." + error;
            }
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                if (error)
                    return "autoRenewPeriod." + error;
            }
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                if (error)
                    return "fileID." + error;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo")) {
                properties.memoField = 1;
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            }
            if (message.memoWrapper != null && $Object.hasOwnProperty.call(message, "memoWrapper")) {
                if (properties.memoField === 1)
                    return "memoField: multiple values";
                properties.memoField = 1;
                {
                    let error = $root.google.protobuf.StringValue.verify(message.memoWrapper, _depth + 1);
                    if (error)
                        return "memoWrapper." + error;
                }
            }
            if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations")) {
                let error = $root.google.protobuf.Int32Value.verify(message.maxAutomaticTokenAssociations, _depth + 1);
                if (error)
                    return "maxAutomaticTokenAssociations." + error;
            }
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId")) {
                let error = $root.proto.AccountID.verify(message.autoRenewAccountId, _depth + 1);
                if (error)
                    return "autoRenewAccountId." + error;
            }
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId")) {
                properties.stakedId = 1;
                {
                    let error = $root.proto.AccountID.verify(message.stakedAccountId, _depth + 1);
                    if (error)
                        return "stakedAccountId." + error;
                }
            }
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId")) {
                if (properties.stakedId === 1)
                    return "stakedId: multiple values";
                properties.stakedId = 1;
                if (!$util.isInteger(message.stakedNodeId) && !(message.stakedNodeId && $util.isInteger(message.stakedNodeId.low) && $util.isInteger(message.stakedNodeId.high)))
                    return "stakedNodeId: integer|Long expected";
            }
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward")) {
                let error = $root.google.protobuf.BoolValue.verify(message.declineReward, _depth + 1);
                if (error)
                    return "declineReward." + error;
            }
            if (message.hookIdsToDelete != null && $Object.hasOwnProperty.call(message, "hookIdsToDelete")) {
                if (!$Array.isArray(message.hookIdsToDelete))
                    return "hookIdsToDelete: array expected";
                for (let i = 0; i < message.hookIdsToDelete.length; ++i)
                    if (!$util.isInteger(message.hookIdsToDelete[i]) && !(message.hookIdsToDelete[i] && $util.isInteger(message.hookIdsToDelete[i].low) && $util.isInteger(message.hookIdsToDelete[i].high)))
                        return "hookIdsToDelete: integer|Long[] expected";
            }
            if (message.hookCreationDetails != null && $Object.hasOwnProperty.call(message, "hookCreationDetails")) {
                if (!$Array.isArray(message.hookCreationDetails))
                    return "hookCreationDetails: array expected";
                for (let i = 0; i < message.hookCreationDetails.length; ++i) {
                    let error = $root.com.hedera.hapi.node.hooks.HookCreationDetails.verify(message.hookCreationDetails[i], _depth + 1);
                    if (error)
                        return "hookCreationDetails." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ContractUpdateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractUpdateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractUpdateTransactionBody} ContractUpdateTransactionBody
         */
        ContractUpdateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractUpdateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractUpdateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractUpdateTransactionBody();
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            if (object.expirationTime != null) {
                if (!$util.isObject(object.expirationTime))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.expirationTime: object expected");
                message.expirationTime = $root.proto.Timestamp.fromObject(object.expirationTime, _depth + 1);
            }
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.proxyAccountID != null) {
                if (!$util.isObject(object.proxyAccountID))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.proxyAccountID: object expected");
                message.proxyAccountID = $root.proto.AccountID.fromObject(object.proxyAccountID, _depth + 1);
            }
            if (object.autoRenewPeriod != null) {
                if (!$util.isObject(object.autoRenewPeriod))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.autoRenewPeriod: object expected");
                message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
            }
            if (object.fileID != null) {
                if (!$util.isObject(object.fileID))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.fileID: object expected");
                message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
            }
            if (object.memo != null)
                message.memo = $String(object.memo);
            if (object.memoWrapper != null) {
                if (!$util.isObject(object.memoWrapper))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.memoWrapper: object expected");
                message.memoWrapper = $root.google.protobuf.StringValue.fromObject(object.memoWrapper, _depth + 1);
            }
            if (object.maxAutomaticTokenAssociations != null) {
                if (!$util.isObject(object.maxAutomaticTokenAssociations))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.maxAutomaticTokenAssociations: object expected");
                message.maxAutomaticTokenAssociations = $root.google.protobuf.Int32Value.fromObject(object.maxAutomaticTokenAssociations, _depth + 1);
            }
            if (object.autoRenewAccountId != null) {
                if (!$util.isObject(object.autoRenewAccountId))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.autoRenewAccountId: object expected");
                message.autoRenewAccountId = $root.proto.AccountID.fromObject(object.autoRenewAccountId, _depth + 1);
            }
            if (object.stakedAccountId != null) {
                if (!$util.isObject(object.stakedAccountId))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.stakedAccountId: object expected");
                message.stakedAccountId = $root.proto.AccountID.fromObject(object.stakedAccountId, _depth + 1);
            }
            if (object.stakedNodeId != null)
                if ($util.Long)
                    message.stakedNodeId = $util.Long.fromValue(object.stakedNodeId, false);
                else if (typeof object.stakedNodeId === "string")
                    message.stakedNodeId = $parseInt(object.stakedNodeId, 10);
                else if (typeof object.stakedNodeId === "number")
                    message.stakedNodeId = object.stakedNodeId;
                else if (typeof object.stakedNodeId === "object")
                    message.stakedNodeId = new $util.LongBits(object.stakedNodeId.low >>> 0, object.stakedNodeId.high >>> 0).toNumber();
            if (object.declineReward != null) {
                if (!$util.isObject(object.declineReward))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.declineReward: object expected");
                message.declineReward = $root.google.protobuf.BoolValue.fromObject(object.declineReward, _depth + 1);
            }
            if (object.hookIdsToDelete) {
                if (!$Array.isArray(object.hookIdsToDelete))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.hookIdsToDelete: array expected");
                message.hookIdsToDelete = $Array(object.hookIdsToDelete.length);
                for (let i = 0; i < object.hookIdsToDelete.length; ++i)
                    if ($util.Long)
                        message.hookIdsToDelete[i] = $util.Long.fromValue(object.hookIdsToDelete[i], false);
                    else if (typeof object.hookIdsToDelete[i] === "string")
                        message.hookIdsToDelete[i] = $parseInt(object.hookIdsToDelete[i], 10);
                    else if (typeof object.hookIdsToDelete[i] === "number")
                        message.hookIdsToDelete[i] = object.hookIdsToDelete[i];
                    else if (typeof object.hookIdsToDelete[i] === "object")
                        message.hookIdsToDelete[i] = new $util.LongBits(object.hookIdsToDelete[i].low >>> 0, object.hookIdsToDelete[i].high >>> 0).toNumber();
            }
            if (object.hookCreationDetails) {
                if (!$Array.isArray(object.hookCreationDetails))
                    throw $TypeError(".proto.ContractUpdateTransactionBody.hookCreationDetails: array expected");
                message.hookCreationDetails = $Array(object.hookCreationDetails.length);
                for (let i = 0; i < object.hookCreationDetails.length; ++i) {
                    if (!$util.isObject(object.hookCreationDetails[i]))
                        throw $TypeError(".proto.ContractUpdateTransactionBody.hookCreationDetails: object expected");
                    message.hookCreationDetails[i] = $root.com.hedera.hapi.node.hooks.HookCreationDetails.fromObject(object.hookCreationDetails[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractUpdateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractUpdateTransactionBody
         * @static
         * @param {proto.ContractUpdateTransactionBody} message ContractUpdateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractUpdateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.hookIdsToDelete = [];
                object.hookCreationDetails = [];
            }
            if (options.defaults) {
                object.contractID = null;
                object.expirationTime = null;
                object.adminKey = null;
                object.proxyAccountID = null;
                object.autoRenewPeriod = null;
                object.fileID = null;
                object.maxAutomaticTokenAssociations = null;
                object.autoRenewAccountId = null;
                object.declineReward = null;
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                object.expirationTime = $root.proto.Timestamp.toObject(message.expirationTime, options, _depth + 1);
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID"))
                object.proxyAccountID = $root.proto.AccountID.toObject(message.proxyAccountID, options, _depth + 1);
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo")) {
                object.memo = message.memo;
                if (options.oneofs)
                    object.memoField = "memo";
            }
            if (message.memoWrapper != null && $Object.hasOwnProperty.call(message, "memoWrapper")) {
                object.memoWrapper = $root.google.protobuf.StringValue.toObject(message.memoWrapper, options, _depth + 1);
                if (options.oneofs)
                    object.memoField = "memoWrapper";
            }
            if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                object.maxAutomaticTokenAssociations = $root.google.protobuf.Int32Value.toObject(message.maxAutomaticTokenAssociations, options, _depth + 1);
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId"))
                object.autoRenewAccountId = $root.proto.AccountID.toObject(message.autoRenewAccountId, options, _depth + 1);
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId")) {
                object.stakedAccountId = $root.proto.AccountID.toObject(message.stakedAccountId, options, _depth + 1);
                if (options.oneofs)
                    object.stakedId = "stakedAccountId";
            }
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakedNodeId = typeof message.stakedNodeId === "number" ? $BigInt(message.stakedNodeId) : $util.Long.fromBits(message.stakedNodeId.low >>> 0, message.stakedNodeId.high >>> 0, false).toBigInt();
                else if (typeof message.stakedNodeId === "number")
                    object.stakedNodeId = options.longs === $String ? $String(message.stakedNodeId) : message.stakedNodeId;
                else
                    object.stakedNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.stakedNodeId) : options.longs === $Number ? new $util.LongBits(message.stakedNodeId.low >>> 0, message.stakedNodeId.high >>> 0).toNumber() : message.stakedNodeId;
                if (options.oneofs)
                    object.stakedId = "stakedNodeId";
            }
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                object.declineReward = $root.google.protobuf.BoolValue.toObject(message.declineReward, options, _depth + 1);
            if (message.hookIdsToDelete && message.hookIdsToDelete.length) {
                object.hookIdsToDelete = $Array(message.hookIdsToDelete.length);
                for (let j = 0; j < message.hookIdsToDelete.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.hookIdsToDelete[j] = typeof message.hookIdsToDelete[j] === "number" ? $BigInt(message.hookIdsToDelete[j]) : $util.Long.fromBits(message.hookIdsToDelete[j].low >>> 0, message.hookIdsToDelete[j].high >>> 0, false).toBigInt();
                    else if (typeof message.hookIdsToDelete[j] === "number")
                        object.hookIdsToDelete[j] = options.longs === $String ? $String(message.hookIdsToDelete[j]) : message.hookIdsToDelete[j];
                    else
                        object.hookIdsToDelete[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.hookIdsToDelete[j]) : options.longs === $Number ? new $util.LongBits(message.hookIdsToDelete[j].low >>> 0, message.hookIdsToDelete[j].high >>> 0).toNumber() : message.hookIdsToDelete[j];
            }
            if (message.hookCreationDetails && message.hookCreationDetails.length) {
                object.hookCreationDetails = $Array(message.hookCreationDetails.length);
                for (let j = 0; j < message.hookCreationDetails.length; ++j)
                    object.hookCreationDetails[j] = $root.com.hedera.hapi.node.hooks.HookCreationDetails.toObject(message.hookCreationDetails[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ContractUpdateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.ContractUpdateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractUpdateTransactionBody.prototype.toJSON = function() {
            return ContractUpdateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractUpdateTransactionBody
         * @function getTypeUrl
         * @memberof proto.ContractUpdateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractUpdateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractUpdateTransactionBody";
        };

        return ContractUpdateTransactionBody;
    })();

    proto.LiveHash = (function() {

        /**
         * Properties of a LiveHash.
         * @typedef {Object} proto.LiveHash.$Properties
         * @property {proto.AccountID.$Properties|null} [accountId] An account associated via this live hash to the hashed content.
         * @property {Uint8Array|null} [hash] A SHA-384 hash of some content that is associated to the account
         * or account holder.
         * @property {proto.KeyList.$Properties|null} [keys] A list of keys, all of which MUST sign the transaction to add the
         * live hash.<br/>
         * Any one of these keys may, however, remove the live hash to revoke
         * the association.
         * @property {proto.Duration.$Properties|null} [duration] A duration describing how long this Live Hash SHALL remain valid.<br/>
         * A Live Hash SHOULD NOT be relied upon after this duration has elapsed.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a LiveHash.
         * @memberof proto
         * @interface ILiveHash
         * @augments proto.LiveHash.$Properties
         * @deprecated Use proto.LiveHash.$Properties instead.
         */

        /**
         * Shape of a LiveHash.
         * @typedef {{
         *   accountId?: proto.AccountID.$Shape|null;
         *   hash?: Uint8Array|null;
         *   keys?: proto.KeyList.$Shape|null;
         *   duration?: proto.Duration.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.LiveHash.$Shape
         */

        /**
         * Constructs a new LiveHash.
         * @memberof proto
         * @classdesc A Live Hash value associating some item of content to an account.
         * 
         * This message represents a desired entry in the ledger for a SHA-384
         * hash of some content, an associated specific account, a list of authorized
         * keys, and a duration the live hash is "valid".
         * @constructor
         * @param {proto.LiveHash.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const LiveHash = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An account associated via this live hash to the hashed content.
         * @member {proto.AccountID.$Properties|null|undefined} accountId
         * @memberof proto.LiveHash
         * @instance
         */
        LiveHash.prototype.accountId = null;

        /**
         * A SHA-384 hash of some content that is associated to the account
         * or account holder.
         * @member {Uint8Array} hash
         * @memberof proto.LiveHash
         * @instance
         */
        LiveHash.prototype.hash = $util.newBuffer([]);

        /**
         * A list of keys, all of which MUST sign the transaction to add the
         * live hash.<br/>
         * Any one of these keys may, however, remove the live hash to revoke
         * the association.
         * @member {proto.KeyList.$Properties|null|undefined} keys
         * @memberof proto.LiveHash
         * @instance
         */
        LiveHash.prototype.keys = null;

        /**
         * A duration describing how long this Live Hash SHALL remain valid.<br/>
         * A Live Hash SHOULD NOT be relied upon after this duration has elapsed.
         * @member {proto.Duration.$Properties|null|undefined} duration
         * @memberof proto.LiveHash
         * @instance
         */
        LiveHash.prototype.duration = null;

        /**
         * Creates a new LiveHash instance using the specified properties.
         * @function create
         * @memberof proto.LiveHash
         * @static
         * @param {proto.LiveHash.$Properties=} [properties] Properties to set
         * @returns {proto.LiveHash} LiveHash instance
         * @type {{
         *   (properties: proto.LiveHash.$Shape): proto.LiveHash & proto.LiveHash.$Shape;
         *   (properties?: proto.LiveHash.$Properties): proto.LiveHash;
         * }}
         */
        LiveHash.create = function(properties) {
            return new LiveHash(properties);
        };

        /**
         * Encodes the specified LiveHash message. Does not implicitly {@link proto.LiveHash.verify|verify} messages.
         * @function encode
         * @memberof proto.LiveHash
         * @static
         * @param {proto.LiveHash.$Properties} message LiveHash message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LiveHash.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.hash);
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys"))
                $root.proto.KeyList.encode(message.keys, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.duration != null && $Object.hasOwnProperty.call(message, "duration"))
                $root.proto.Duration.encode(message.duration, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LiveHash message, length delimited. Does not implicitly {@link proto.LiveHash.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.LiveHash
         * @static
         * @param {proto.LiveHash.$Properties} message LiveHash message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LiveHash.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a LiveHash message from the specified reader or buffer.
         * @function decode
         * @memberof proto.LiveHash
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.LiveHash & proto.LiveHash.$Shape} LiveHash
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LiveHash.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.LiveHash();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.keys = $root.proto.KeyList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.keys);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.duration = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.duration);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a LiveHash message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.LiveHash
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.LiveHash & proto.LiveHash.$Shape} LiveHash
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LiveHash.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LiveHash message.
         * @function verify
         * @memberof proto.LiveHash
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LiveHash.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                if (error)
                    return "accountId." + error;
            }
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                    return "hash: buffer expected";
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys")) {
                let error = $root.proto.KeyList.verify(message.keys, _depth + 1);
                if (error)
                    return "keys." + error;
            }
            if (message.duration != null && $Object.hasOwnProperty.call(message, "duration")) {
                let error = $root.proto.Duration.verify(message.duration, _depth + 1);
                if (error)
                    return "duration." + error;
            }
            return null;
        };

        /**
         * Creates a LiveHash message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.LiveHash
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.LiveHash} LiveHash
         */
        LiveHash.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.LiveHash)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.LiveHash: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.LiveHash();
            if (object.accountId != null) {
                if (!$util.isObject(object.accountId))
                    throw $TypeError(".proto.LiveHash.accountId: object expected");
                message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
            }
            if (object.hash != null)
                if (object.hash.length)
                    if (typeof object.hash === "string")
                        $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                    else if (object.hash.length >= 0)
                        message.hash = object.hash;
            if (object.keys != null) {
                if (!$util.isObject(object.keys))
                    throw $TypeError(".proto.LiveHash.keys: object expected");
                message.keys = $root.proto.KeyList.fromObject(object.keys, _depth + 1);
            }
            if (object.duration != null) {
                if (!$util.isObject(object.duration))
                    throw $TypeError(".proto.LiveHash.duration: object expected");
                message.duration = $root.proto.Duration.fromObject(object.duration, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a LiveHash message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.LiveHash
         * @static
         * @param {proto.LiveHash} message LiveHash
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LiveHash.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.accountId = null;
                if (options.bytes === $String)
                    object.hash = "";
                else {
                    object.hash = [];
                    if (options.bytes !== $Array)
                        object.hash = $util.newBuffer(object.hash);
                }
                object.keys = null;
                object.duration = null;
            }
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = options.bytes === $String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hash) : message.hash;
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys"))
                object.keys = $root.proto.KeyList.toObject(message.keys, options, _depth + 1);
            if (message.duration != null && $Object.hasOwnProperty.call(message, "duration"))
                object.duration = $root.proto.Duration.toObject(message.duration, options, _depth + 1);
            return object;
        };

        /**
         * Converts this LiveHash to JSON.
         * @function toJSON
         * @memberof proto.LiveHash
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LiveHash.prototype.toJSON = function() {
            return LiveHash.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for LiveHash
         * @function getTypeUrl
         * @memberof proto.LiveHash
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        LiveHash.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.LiveHash";
        };

        return LiveHash;
    })();

    proto.CryptoAddLiveHashTransactionBody = (function() {

        /**
         * Properties of a CryptoAddLiveHashTransactionBody.
         * @typedef {Object} proto.CryptoAddLiveHashTransactionBody.$Properties
         * @property {proto.LiveHash.$Properties|null} [liveHash] A Live Hash to be added to the ledger and associated with
         * the identified account.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoAddLiveHashTransactionBody.
         * @memberof proto
         * @interface ICryptoAddLiveHashTransactionBody
         * @augments proto.CryptoAddLiveHashTransactionBody.$Properties
         * @deprecated Use proto.CryptoAddLiveHashTransactionBody.$Properties instead.
         */

        /**
         * Shape of a CryptoAddLiveHashTransactionBody.
         * @typedef {{
         *   liveHash?: proto.LiveHash.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoAddLiveHashTransactionBody.$Shape
         */

        /**
         * Constructs a new CryptoAddLiveHashTransactionBody.
         * @memberof proto
         * @classdesc Add a hash value to the ledger and associate it with an account.
         * 
         * Create an entry in the ledger for a SHA-384 hash of some content, and
         * associate that with a specific account.  This is sometimes used to associate
         * a credential or certificate with an account as a public record.<br/>
         * The entry created is also associated with a list of keys, all of which
         * MUST sign this transaction.<br/>
         * The account key for the associated account MUST sign this transaction.<br/>
         * 
         * The live hash, once created, MAY be removed from the ledger with one
         * or more signatures.
         * - The account key of the account associated to the live hash.
         * - Any one key from the key list in the live hash entry.
         * - Any combination of keys from the key list in the live hash entry.
         * @constructor
         * @param {proto.CryptoAddLiveHashTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoAddLiveHashTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A Live Hash to be added to the ledger and associated with
         * the identified account.
         * @member {proto.LiveHash.$Properties|null|undefined} liveHash
         * @memberof proto.CryptoAddLiveHashTransactionBody
         * @instance
         */
        CryptoAddLiveHashTransactionBody.prototype.liveHash = null;

        /**
         * Creates a new CryptoAddLiveHashTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.CryptoAddLiveHashTransactionBody
         * @static
         * @param {proto.CryptoAddLiveHashTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoAddLiveHashTransactionBody} CryptoAddLiveHashTransactionBody instance
         * @type {{
         *   (properties: proto.CryptoAddLiveHashTransactionBody.$Shape): proto.CryptoAddLiveHashTransactionBody & proto.CryptoAddLiveHashTransactionBody.$Shape;
         *   (properties?: proto.CryptoAddLiveHashTransactionBody.$Properties): proto.CryptoAddLiveHashTransactionBody;
         * }}
         */
        CryptoAddLiveHashTransactionBody.create = function(properties) {
            return new CryptoAddLiveHashTransactionBody(properties);
        };

        /**
         * Encodes the specified CryptoAddLiveHashTransactionBody message. Does not implicitly {@link proto.CryptoAddLiveHashTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoAddLiveHashTransactionBody
         * @static
         * @param {proto.CryptoAddLiveHashTransactionBody.$Properties} message CryptoAddLiveHashTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoAddLiveHashTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.liveHash != null && $Object.hasOwnProperty.call(message, "liveHash"))
                $root.proto.LiveHash.encode(message.liveHash, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoAddLiveHashTransactionBody message, length delimited. Does not implicitly {@link proto.CryptoAddLiveHashTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoAddLiveHashTransactionBody
         * @static
         * @param {proto.CryptoAddLiveHashTransactionBody.$Properties} message CryptoAddLiveHashTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoAddLiveHashTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoAddLiveHashTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoAddLiveHashTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoAddLiveHashTransactionBody & proto.CryptoAddLiveHashTransactionBody.$Shape} CryptoAddLiveHashTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoAddLiveHashTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoAddLiveHashTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.liveHash = $root.proto.LiveHash.decode(reader, reader.uint32(), $undefined, _depth + 1, message.liveHash);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoAddLiveHashTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoAddLiveHashTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoAddLiveHashTransactionBody & proto.CryptoAddLiveHashTransactionBody.$Shape} CryptoAddLiveHashTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoAddLiveHashTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoAddLiveHashTransactionBody message.
         * @function verify
         * @memberof proto.CryptoAddLiveHashTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoAddLiveHashTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.liveHash != null && $Object.hasOwnProperty.call(message, "liveHash")) {
                let error = $root.proto.LiveHash.verify(message.liveHash, _depth + 1);
                if (error)
                    return "liveHash." + error;
            }
            return null;
        };

        /**
         * Creates a CryptoAddLiveHashTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoAddLiveHashTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoAddLiveHashTransactionBody} CryptoAddLiveHashTransactionBody
         */
        CryptoAddLiveHashTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoAddLiveHashTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoAddLiveHashTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoAddLiveHashTransactionBody();
            if (object.liveHash != null) {
                if (!$util.isObject(object.liveHash))
                    throw $TypeError(".proto.CryptoAddLiveHashTransactionBody.liveHash: object expected");
                message.liveHash = $root.proto.LiveHash.fromObject(object.liveHash, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoAddLiveHashTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoAddLiveHashTransactionBody
         * @static
         * @param {proto.CryptoAddLiveHashTransactionBody} message CryptoAddLiveHashTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoAddLiveHashTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.liveHash = null;
            if (message.liveHash != null && $Object.hasOwnProperty.call(message, "liveHash"))
                object.liveHash = $root.proto.LiveHash.toObject(message.liveHash, options, _depth + 1);
            return object;
        };

        /**
         * Converts this CryptoAddLiveHashTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.CryptoAddLiveHashTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoAddLiveHashTransactionBody.prototype.toJSON = function() {
            return CryptoAddLiveHashTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoAddLiveHashTransactionBody
         * @function getTypeUrl
         * @memberof proto.CryptoAddLiveHashTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoAddLiveHashTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoAddLiveHashTransactionBody";
        };

        return CryptoAddLiveHashTransactionBody;
    })();

    proto.CryptoCreateTransactionBody = (function() {

        /**
         * Properties of a CryptoCreateTransactionBody.
         * @typedef {Object} proto.CryptoCreateTransactionBody.$Properties
         * @property {proto.Key.$Properties|null} [key] The identifying key for this account.
         * This key represents the account owner, and is required for most actions
         * involving this account that do not modify the account itself. This key
         * may also identify the account for smart contracts.
         * <p>
         * This field is REQUIRED.
         * This `Key` MUST NOT be an empty `KeyList` and MUST contain at least one
         * "primitive" (i.e. cryptographic) key value.
         * @property {number|Long|null} [initialBalance] An amount, in tinybar, to deposit to the newly created account.
         * <p>
         * The deposited amount SHALL be debited to the "payer" account for this
         * transaction.
         * @property {proto.AccountID.$Properties|null} [proxyAccountID] Use `staked_id` instead.<br/>
         * An account identifier for a staking proxy.
         * @property {number|Long|null} [sendRecordThreshold] Removed prior to the first available history, and may be related to an
         * early design dead-end.<br/>
         * An amount below which record stream records would not be created for
         * a transaction that reduces this account balance.
         * @property {number|Long|null} [receiveRecordThreshold] Removed prior to the first available history, and may be related to an
         * early design dead-end.<br/>
         * An amount below which record stream records would not be created for
         * a transaction that increases this account balance.
         * @property {boolean|null} [receiverSigRequired] A flag indicating the account holder must authorize all incoming
         * token transfers.
         * <p>
         * If this flag is set then any transaction that would result in adding
         * hbar or other tokens to this account balance MUST be signed by the
         * identifying key of this account (that is, the `key` field).<br/>
         * If this flag is set, then the account key (`key` field) MUST sign
         * this create transaction, in addition to the transaction payer.
         * @property {proto.Duration.$Properties|null} [autoRenewPeriod] The duration between account automatic renewals.<br/>
         * All entities in state may be charged "rent" occasionally (typically
         * every 90 days) to prevent unnecessary growth of the ledger. This value
         * sets the interval between such events for this account.
         * <p>
         * If the account balance (in HBAR) is insufficient to pay the full renewal
         * fee, the entire HBAR balance SHALL be consumed and the expiration for
         * the account SHALL be extended as far as the available balance can
         * support.<br/>
         * If the account HBAR balance is `0` when the account must be renewed, then
         * the account SHALL be deleted, and subsequently removed from state.
         * @property {proto.ShardID.$Properties|null} [shardID] The shard in which this account is created
         * <p>
         * Currently, this MUST be `0`.<br/>
         * If the desired shard is `0`, this SHOULD NOT be set.
         * @property {proto.RealmID.$Properties|null} [realmID] The realm in which this account is created.
         * <p>
         * The shard number for this realm MUST match the value in `shardID`.<br/>
         * Currently, this MUST be `0` for both fields.<br/>
         * If the desired realm is `0`, this SHOULD NOT be set.
         * @property {proto.Key.$Properties|null} [newRealmAdminKey] This field was never actually used or enabled, and is not expected to
         * ever be used in the future.<br/>
         * @property {string|null} [memo] A short description of this Account.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {number|null} [maxAutomaticTokenAssociations] A maximum number of tokens that can be auto-associated
         * with this account.<br/>
         * By default this value is 0 for all accounts except for automatically
         * created accounts (e.g. smart contracts), which default to -1.
         * <p>
         * If this value is `0`, then this account MUST manually associate to
         * a token before holding or transacting in that token.<br/>
         * This value MAY also be `-1` to indicate no limit.<br/>
         * This value MUST NOT be less than `-1`.
         * @property {proto.AccountID.$Properties|null} [stakedAccountId] ID of the account to which this account is staking its balances.
         * <p>
         * If this account is not currently staking its balances, then this
         * field, if set, MUST be the sentinel value of `0.0.0`.
         * @property {number|Long|null} [stakedNodeId] ID of the node this account is staked to.
         * <p>
         * If this account is not currently staking its balances, then this
         * field, if set, SHALL be the sentinel value of `-1`.<br/>
         * Wallet software SHOULD surface staking issues to users and provide a
         * simple mechanism to update staking to a new node ID in the event the
         * prior staked node ID ceases to be valid.
         * @property {boolean|null} [declineReward] A boolean indicating that this account has chosen to decline rewards for
         * staking its balances.
         * <p>
         * This account MAY still stake its balances, but SHALL NOT receive reward
         * payments for doing so, if this value is set.
         * @property {Uint8Array|null} [alias] Bytes to be used as the account's alias.
         * <p>
         * This value, if set, MUST be one of the following values<br/>
         * <ul>
         * <li>The 32-byte serialized form of the ED25519 account key.</li>
         * <li>The 33-byte _compressed_ serialized form of the ECDSA(secp256k1)
         * account key.</li>
         * <li>The 20-byte EVM address derived from a keccak-256 hash of the
         * ECDSA(secp256k1) account key</li>
         * </ul>
         * All aliases within the network MUST be unique. If this value matches an
         * existing account alias, this `create` transaction SHALL fail.<br/>
         * If an account exists with a particular alias value, any transaction to
         * transfer value _to_ that alias SHALL deposit the transferred value in
         * the existing account, and SHALL NOT assess an account creation fee.<br/>
         * Once set, an account alias is immutable and MUST NOT be changed.
         * @property {Array.<com.hedera.hapi.node.hooks.HookCreationDetails.$Properties>|null} [hookCreationDetails] Details of hooks to add immediately after creating this account.
         * @property {Uint8Array|null} [delegationAddress] The initial delegated contract address to set for the new account.
         * <p>
         * If set, the value MUST be 20 bytes long (EVM address).
         * A value of `0x0000000000000000000000000000000000000000` will be ignored.
         * 
         * If this field is set on the account, anytime a call is made to the account's address
         * (either an EVM call within HSCS or a child call dispatched by CryptoTransfer)
         * the EVM code of the contract referenced by `delegation_address` will be
         * executed in the context of the account.
         * @property {"stakedAccountId"|"stakedNodeId"} [stakedId] CryptoCreateTransactionBody stakedId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoCreateTransactionBody.
         * @memberof proto
         * @interface ICryptoCreateTransactionBody
         * @augments proto.CryptoCreateTransactionBody.$Properties
         * @deprecated Use proto.CryptoCreateTransactionBody.$Properties instead.
         */

        /**
         * Narrowed shape of a CryptoCreateTransactionBody.
         * @typedef {{
         *   key?: proto.Key.$Shape|null;
         *   initialBalance?: number|Long|null;
         *   proxyAccountID?: proto.AccountID.$Shape|null;
         *   sendRecordThreshold?: number|Long|null;
         *   receiveRecordThreshold?: number|Long|null;
         *   receiverSigRequired?: boolean|null;
         *   autoRenewPeriod?: proto.Duration.$Shape|null;
         *   shardID?: proto.ShardID.$Shape|null;
         *   realmID?: proto.RealmID.$Shape|null;
         *   newRealmAdminKey?: proto.Key.$Shape|null;
         *   memo?: string|null;
         *   maxAutomaticTokenAssociations?: number|null;
         *   stakedAccountId?: proto.AccountID.$Shape|null;
         *   stakedNodeId?: number|Long|null;
         *   declineReward?: boolean|null;
         *   alias?: Uint8Array|null;
         *   hookCreationDetails?: Array.<com.hedera.hapi.node.hooks.HookCreationDetails.$Shape>|null;
         *   delegationAddress?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ stakedId?: undefined; stakedAccountId?: null; stakedNodeId?: null }|{ stakedId?: "stakedAccountId"; stakedAccountId: proto.AccountID.$Shape; stakedNodeId?: null }|{ stakedId?: "stakedNodeId"; stakedAccountId?: null; stakedNodeId: number|Long })
         * )} proto.CryptoCreateTransactionBody.$Shape
         */

        /**
         * Constructs a new CryptoCreateTransactionBody.
         * @memberof proto
         * @classdesc Represents a CryptoCreateTransactionBody.
         * @constructor
         * @param {proto.CryptoCreateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoCreateTransactionBody = function (properties) {
            this.hookCreationDetails = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The identifying key for this account.
         * This key represents the account owner, and is required for most actions
         * involving this account that do not modify the account itself. This key
         * may also identify the account for smart contracts.
         * <p>
         * This field is REQUIRED.
         * This `Key` MUST NOT be an empty `KeyList` and MUST contain at least one
         * "primitive" (i.e. cryptographic) key value.
         * @member {proto.Key.$Properties|null|undefined} key
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.key = null;

        /**
         * An amount, in tinybar, to deposit to the newly created account.
         * <p>
         * The deposited amount SHALL be debited to the "payer" account for this
         * transaction.
         * @member {number|Long} initialBalance
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.initialBalance = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Use `staked_id` instead.<br/>
         * An account identifier for a staking proxy.
         * @member {proto.AccountID.$Properties|null|undefined} proxyAccountID
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.proxyAccountID = null;

        /**
         * Removed prior to the first available history, and may be related to an
         * early design dead-end.<br/>
         * An amount below which record stream records would not be created for
         * a transaction that reduces this account balance.
         * @member {number|Long} sendRecordThreshold
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.sendRecordThreshold = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Removed prior to the first available history, and may be related to an
         * early design dead-end.<br/>
         * An amount below which record stream records would not be created for
         * a transaction that increases this account balance.
         * @member {number|Long} receiveRecordThreshold
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.receiveRecordThreshold = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A flag indicating the account holder must authorize all incoming
         * token transfers.
         * <p>
         * If this flag is set then any transaction that would result in adding
         * hbar or other tokens to this account balance MUST be signed by the
         * identifying key of this account (that is, the `key` field).<br/>
         * If this flag is set, then the account key (`key` field) MUST sign
         * this create transaction, in addition to the transaction payer.
         * @member {boolean} receiverSigRequired
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.receiverSigRequired = false;

        /**
         * The duration between account automatic renewals.<br/>
         * All entities in state may be charged "rent" occasionally (typically
         * every 90 days) to prevent unnecessary growth of the ledger. This value
         * sets the interval between such events for this account.
         * <p>
         * If the account balance (in HBAR) is insufficient to pay the full renewal
         * fee, the entire HBAR balance SHALL be consumed and the expiration for
         * the account SHALL be extended as far as the available balance can
         * support.<br/>
         * If the account HBAR balance is `0` when the account must be renewed, then
         * the account SHALL be deleted, and subsequently removed from state.
         * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.autoRenewPeriod = null;

        /**
         * The shard in which this account is created
         * <p>
         * Currently, this MUST be `0`.<br/>
         * If the desired shard is `0`, this SHOULD NOT be set.
         * @member {proto.ShardID.$Properties|null|undefined} shardID
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.shardID = null;

        /**
         * The realm in which this account is created.
         * <p>
         * The shard number for this realm MUST match the value in `shardID`.<br/>
         * Currently, this MUST be `0` for both fields.<br/>
         * If the desired realm is `0`, this SHOULD NOT be set.
         * @member {proto.RealmID.$Properties|null|undefined} realmID
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.realmID = null;

        /**
         * This field was never actually used or enabled, and is not expected to
         * ever be used in the future.<br/>
         * @member {proto.Key.$Properties|null|undefined} newRealmAdminKey
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.newRealmAdminKey = null;

        /**
         * A short description of this Account.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.memo = "";

        /**
         * A maximum number of tokens that can be auto-associated
         * with this account.<br/>
         * By default this value is 0 for all accounts except for automatically
         * created accounts (e.g. smart contracts), which default to -1.
         * <p>
         * If this value is `0`, then this account MUST manually associate to
         * a token before holding or transacting in that token.<br/>
         * This value MAY also be `-1` to indicate no limit.<br/>
         * This value MUST NOT be less than `-1`.
         * @member {number} maxAutomaticTokenAssociations
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.maxAutomaticTokenAssociations = 0;

        /**
         * ID of the account to which this account is staking its balances.
         * <p>
         * If this account is not currently staking its balances, then this
         * field, if set, MUST be the sentinel value of `0.0.0`.
         * @member {proto.AccountID.$Properties|null|undefined} stakedAccountId
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.stakedAccountId = null;

        /**
         * ID of the node this account is staked to.
         * <p>
         * If this account is not currently staking its balances, then this
         * field, if set, SHALL be the sentinel value of `-1`.<br/>
         * Wallet software SHOULD surface staking issues to users and provide a
         * simple mechanism to update staking to a new node ID in the event the
         * prior staked node ID ceases to be valid.
         * @member {number|Long|null|undefined} stakedNodeId
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.stakedNodeId = null;

        /**
         * A boolean indicating that this account has chosen to decline rewards for
         * staking its balances.
         * <p>
         * This account MAY still stake its balances, but SHALL NOT receive reward
         * payments for doing so, if this value is set.
         * @member {boolean} declineReward
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.declineReward = false;

        /**
         * Bytes to be used as the account's alias.
         * <p>
         * This value, if set, MUST be one of the following values<br/>
         * <ul>
         * <li>The 32-byte serialized form of the ED25519 account key.</li>
         * <li>The 33-byte _compressed_ serialized form of the ECDSA(secp256k1)
         * account key.</li>
         * <li>The 20-byte EVM address derived from a keccak-256 hash of the
         * ECDSA(secp256k1) account key</li>
         * </ul>
         * All aliases within the network MUST be unique. If this value matches an
         * existing account alias, this `create` transaction SHALL fail.<br/>
         * If an account exists with a particular alias value, any transaction to
         * transfer value _to_ that alias SHALL deposit the transferred value in
         * the existing account, and SHALL NOT assess an account creation fee.<br/>
         * Once set, an account alias is immutable and MUST NOT be changed.
         * @member {Uint8Array} alias
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.alias = $util.newBuffer([]);

        /**
         * Details of hooks to add immediately after creating this account.
         * @member {Array.<com.hedera.hapi.node.hooks.HookCreationDetails.$Properties>} hookCreationDetails
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.hookCreationDetails = $util.emptyArray;

        /**
         * The initial delegated contract address to set for the new account.
         * <p>
         * If set, the value MUST be 20 bytes long (EVM address).
         * A value of `0x0000000000000000000000000000000000000000` will be ignored.
         * 
         * If this field is set on the account, anytime a call is made to the account's address
         * (either an EVM call within HSCS or a child call dispatched by CryptoTransfer)
         * the EVM code of the contract referenced by `delegation_address` will be
         * executed in the context of the account.
         * @member {Uint8Array} delegationAddress
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        CryptoCreateTransactionBody.prototype.delegationAddress = $util.newBuffer([]);

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * CryptoCreateTransactionBody stakedId.
         * @member {"stakedAccountId"|"stakedNodeId"|undefined} stakedId
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         */
        $Object.defineProperty(CryptoCreateTransactionBody.prototype, "stakedId", {
            get: $util.oneOfGetter($oneOfFields = ["stakedAccountId", "stakedNodeId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new CryptoCreateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.CryptoCreateTransactionBody
         * @static
         * @param {proto.CryptoCreateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoCreateTransactionBody} CryptoCreateTransactionBody instance
         * @type {{
         *   (properties: proto.CryptoCreateTransactionBody.$Shape): proto.CryptoCreateTransactionBody & proto.CryptoCreateTransactionBody.$Shape;
         *   (properties?: proto.CryptoCreateTransactionBody.$Properties): proto.CryptoCreateTransactionBody;
         * }}
         */
        CryptoCreateTransactionBody.create = function(properties) {
            return new CryptoCreateTransactionBody(properties);
        };

        /**
         * Encodes the specified CryptoCreateTransactionBody message. Does not implicitly {@link proto.CryptoCreateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoCreateTransactionBody
         * @static
         * @param {proto.CryptoCreateTransactionBody.$Properties} message CryptoCreateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoCreateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                $root.proto.Key.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.initialBalance != null && $Object.hasOwnProperty.call(message, "initialBalance") && (typeof message.initialBalance === "object" ? message.initialBalance.low || message.initialBalance.high : message.initialBalance !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.initialBalance);
            if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID"))
                $root.proto.AccountID.encode(message.proxyAccountID, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.sendRecordThreshold != null && $Object.hasOwnProperty.call(message, "sendRecordThreshold") && (typeof message.sendRecordThreshold === "object" ? message.sendRecordThreshold.low || message.sendRecordThreshold.high : message.sendRecordThreshold !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.sendRecordThreshold);
            if (message.receiveRecordThreshold != null && $Object.hasOwnProperty.call(message, "receiveRecordThreshold") && (typeof message.receiveRecordThreshold === "object" ? message.receiveRecordThreshold.low || message.receiveRecordThreshold.high : message.receiveRecordThreshold !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.receiveRecordThreshold);
            if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired") && message.receiverSigRequired !== false)
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.receiverSigRequired);
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.shardID != null && $Object.hasOwnProperty.call(message, "shardID"))
                $root.proto.ShardID.encode(message.shardID, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.realmID != null && $Object.hasOwnProperty.call(message, "realmID"))
                $root.proto.RealmID.encode(message.realmID, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.newRealmAdminKey != null && $Object.hasOwnProperty.call(message, "newRealmAdminKey"))
                $root.proto.Key.encode(message.newRealmAdminKey, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.memo);
            if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations") && message.maxAutomaticTokenAssociations !== 0)
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.maxAutomaticTokenAssociations);
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId"))
                $root.proto.AccountID.encode(message.stakedAccountId, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId"))
                writer.uint32(/* id 16, wireType 0 =*/128).int64(message.stakedNodeId);
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward") && message.declineReward !== false)
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message.declineReward);
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias") && message.alias.length)
                writer.uint32(/* id 18, wireType 2 =*/146).bytes(message.alias);
            if (message.hookCreationDetails != null && message.hookCreationDetails.length)
                for (let i = 0; i < message.hookCreationDetails.length; ++i)
                    $root.com.hedera.hapi.node.hooks.HookCreationDetails.encode(message.hookCreationDetails[i], writer.uint32(/* id 19, wireType 2 =*/154).fork(), _depth + 1).ldelim();
            if (message.delegationAddress != null && $Object.hasOwnProperty.call(message, "delegationAddress") && message.delegationAddress.length)
                writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.delegationAddress);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoCreateTransactionBody message, length delimited. Does not implicitly {@link proto.CryptoCreateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoCreateTransactionBody
         * @static
         * @param {proto.CryptoCreateTransactionBody.$Properties} message CryptoCreateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoCreateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoCreateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoCreateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoCreateTransactionBody & proto.CryptoCreateTransactionBody.$Shape} CryptoCreateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoCreateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoCreateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.key = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.initialBalance = value;
                        else
                            delete message.initialBalance;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.proxyAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.proxyAccountID);
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.sendRecordThreshold = value;
                        else
                            delete message.sendRecordThreshold;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.receiveRecordThreshold = value;
                        else
                            delete message.receiveRecordThreshold;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.receiverSigRequired = value;
                        else
                            delete message.receiverSigRequired;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.shardID = $root.proto.ShardID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.shardID);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.realmID = $root.proto.RealmID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.realmID);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.newRealmAdminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.newRealmAdminKey);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.maxAutomaticTokenAssociations = value;
                        else
                            delete message.maxAutomaticTokenAssociations;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.stakedAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stakedAccountId);
                        message.stakedId = "stakedAccountId";
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        message.stakedNodeId = reader.int64();
                        message.stakedId = "stakedNodeId";
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.declineReward = value;
                        else
                            delete message.declineReward;
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.alias = value;
                        else
                            delete message.alias;
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hookCreationDetails && message.hookCreationDetails.length))
                            message.hookCreationDetails = [];
                        message.hookCreationDetails.push($root.com.hedera.hapi.node.hooks.HookCreationDetails.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.delegationAddress = value;
                        else
                            delete message.delegationAddress;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoCreateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoCreateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoCreateTransactionBody & proto.CryptoCreateTransactionBody.$Shape} CryptoCreateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoCreateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoCreateTransactionBody message.
         * @function verify
         * @memberof proto.CryptoCreateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoCreateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.key != null && $Object.hasOwnProperty.call(message, "key")) {
                let error = $root.proto.Key.verify(message.key, _depth + 1);
                if (error)
                    return "key." + error;
            }
            if (message.initialBalance != null && $Object.hasOwnProperty.call(message, "initialBalance"))
                if (!$util.isInteger(message.initialBalance) && !(message.initialBalance && $util.isInteger(message.initialBalance.low) && $util.isInteger(message.initialBalance.high)))
                    return "initialBalance: integer|Long expected";
            if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID")) {
                let error = $root.proto.AccountID.verify(message.proxyAccountID, _depth + 1);
                if (error)
                    return "proxyAccountID." + error;
            }
            if (message.sendRecordThreshold != null && $Object.hasOwnProperty.call(message, "sendRecordThreshold"))
                if (!$util.isInteger(message.sendRecordThreshold) && !(message.sendRecordThreshold && $util.isInteger(message.sendRecordThreshold.low) && $util.isInteger(message.sendRecordThreshold.high)))
                    return "sendRecordThreshold: integer|Long expected";
            if (message.receiveRecordThreshold != null && $Object.hasOwnProperty.call(message, "receiveRecordThreshold"))
                if (!$util.isInteger(message.receiveRecordThreshold) && !(message.receiveRecordThreshold && $util.isInteger(message.receiveRecordThreshold.low) && $util.isInteger(message.receiveRecordThreshold.high)))
                    return "receiveRecordThreshold: integer|Long expected";
            if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired"))
                if (typeof message.receiverSigRequired !== "boolean")
                    return "receiverSigRequired: boolean expected";
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                if (error)
                    return "autoRenewPeriod." + error;
            }
            if (message.shardID != null && $Object.hasOwnProperty.call(message, "shardID")) {
                let error = $root.proto.ShardID.verify(message.shardID, _depth + 1);
                if (error)
                    return "shardID." + error;
            }
            if (message.realmID != null && $Object.hasOwnProperty.call(message, "realmID")) {
                let error = $root.proto.RealmID.verify(message.realmID, _depth + 1);
                if (error)
                    return "realmID." + error;
            }
            if (message.newRealmAdminKey != null && $Object.hasOwnProperty.call(message, "newRealmAdminKey")) {
                let error = $root.proto.Key.verify(message.newRealmAdminKey, _depth + 1);
                if (error)
                    return "newRealmAdminKey." + error;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                if (!$util.isInteger(message.maxAutomaticTokenAssociations))
                    return "maxAutomaticTokenAssociations: integer expected";
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId")) {
                properties.stakedId = 1;
                {
                    let error = $root.proto.AccountID.verify(message.stakedAccountId, _depth + 1);
                    if (error)
                        return "stakedAccountId." + error;
                }
            }
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId")) {
                if (properties.stakedId === 1)
                    return "stakedId: multiple values";
                properties.stakedId = 1;
                if (!$util.isInteger(message.stakedNodeId) && !(message.stakedNodeId && $util.isInteger(message.stakedNodeId.low) && $util.isInteger(message.stakedNodeId.high)))
                    return "stakedNodeId: integer|Long expected";
            }
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                if (typeof message.declineReward !== "boolean")
                    return "declineReward: boolean expected";
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                if (!(message.alias && typeof message.alias.length === "number" || $util.isString(message.alias)))
                    return "alias: buffer expected";
            if (message.hookCreationDetails != null && $Object.hasOwnProperty.call(message, "hookCreationDetails")) {
                if (!$Array.isArray(message.hookCreationDetails))
                    return "hookCreationDetails: array expected";
                for (let i = 0; i < message.hookCreationDetails.length; ++i) {
                    let error = $root.com.hedera.hapi.node.hooks.HookCreationDetails.verify(message.hookCreationDetails[i], _depth + 1);
                    if (error)
                        return "hookCreationDetails." + error;
                }
            }
            if (message.delegationAddress != null && $Object.hasOwnProperty.call(message, "delegationAddress"))
                if (!(message.delegationAddress && typeof message.delegationAddress.length === "number" || $util.isString(message.delegationAddress)))
                    return "delegationAddress: buffer expected";
            return null;
        };

        /**
         * Creates a CryptoCreateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoCreateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoCreateTransactionBody} CryptoCreateTransactionBody
         */
        CryptoCreateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoCreateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoCreateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoCreateTransactionBody();
            if (object.key != null) {
                if (!$util.isObject(object.key))
                    throw $TypeError(".proto.CryptoCreateTransactionBody.key: object expected");
                message.key = $root.proto.Key.fromObject(object.key, _depth + 1);
            }
            if (object.initialBalance != null)
                if (typeof object.initialBalance === "object" ? object.initialBalance.low || object.initialBalance.high : $Number(object.initialBalance) !== 0)
                    if ($util.Long)
                        message.initialBalance = $util.Long.fromValue(object.initialBalance, true);
                    else if (typeof object.initialBalance === "string")
                        message.initialBalance = $parseInt(object.initialBalance, 10);
                    else if (typeof object.initialBalance === "number")
                        message.initialBalance = object.initialBalance;
                    else if (typeof object.initialBalance === "object")
                        message.initialBalance = new $util.LongBits(object.initialBalance.low >>> 0, object.initialBalance.high >>> 0).toNumber(true);
            if (object.proxyAccountID != null) {
                if (!$util.isObject(object.proxyAccountID))
                    throw $TypeError(".proto.CryptoCreateTransactionBody.proxyAccountID: object expected");
                message.proxyAccountID = $root.proto.AccountID.fromObject(object.proxyAccountID, _depth + 1);
            }
            if (object.sendRecordThreshold != null)
                if (typeof object.sendRecordThreshold === "object" ? object.sendRecordThreshold.low || object.sendRecordThreshold.high : $Number(object.sendRecordThreshold) !== 0)
                    if ($util.Long)
                        message.sendRecordThreshold = $util.Long.fromValue(object.sendRecordThreshold, true);
                    else if (typeof object.sendRecordThreshold === "string")
                        message.sendRecordThreshold = $parseInt(object.sendRecordThreshold, 10);
                    else if (typeof object.sendRecordThreshold === "number")
                        message.sendRecordThreshold = object.sendRecordThreshold;
                    else if (typeof object.sendRecordThreshold === "object")
                        message.sendRecordThreshold = new $util.LongBits(object.sendRecordThreshold.low >>> 0, object.sendRecordThreshold.high >>> 0).toNumber(true);
            if (object.receiveRecordThreshold != null)
                if (typeof object.receiveRecordThreshold === "object" ? object.receiveRecordThreshold.low || object.receiveRecordThreshold.high : $Number(object.receiveRecordThreshold) !== 0)
                    if ($util.Long)
                        message.receiveRecordThreshold = $util.Long.fromValue(object.receiveRecordThreshold, true);
                    else if (typeof object.receiveRecordThreshold === "string")
                        message.receiveRecordThreshold = $parseInt(object.receiveRecordThreshold, 10);
                    else if (typeof object.receiveRecordThreshold === "number")
                        message.receiveRecordThreshold = object.receiveRecordThreshold;
                    else if (typeof object.receiveRecordThreshold === "object")
                        message.receiveRecordThreshold = new $util.LongBits(object.receiveRecordThreshold.low >>> 0, object.receiveRecordThreshold.high >>> 0).toNumber(true);
            if (object.receiverSigRequired != null)
                if (object.receiverSigRequired)
                    message.receiverSigRequired = $Boolean(object.receiverSigRequired);
            if (object.autoRenewPeriod != null) {
                if (!$util.isObject(object.autoRenewPeriod))
                    throw $TypeError(".proto.CryptoCreateTransactionBody.autoRenewPeriod: object expected");
                message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
            }
            if (object.shardID != null) {
                if (!$util.isObject(object.shardID))
                    throw $TypeError(".proto.CryptoCreateTransactionBody.shardID: object expected");
                message.shardID = $root.proto.ShardID.fromObject(object.shardID, _depth + 1);
            }
            if (object.realmID != null) {
                if (!$util.isObject(object.realmID))
                    throw $TypeError(".proto.CryptoCreateTransactionBody.realmID: object expected");
                message.realmID = $root.proto.RealmID.fromObject(object.realmID, _depth + 1);
            }
            if (object.newRealmAdminKey != null) {
                if (!$util.isObject(object.newRealmAdminKey))
                    throw $TypeError(".proto.CryptoCreateTransactionBody.newRealmAdminKey: object expected");
                message.newRealmAdminKey = $root.proto.Key.fromObject(object.newRealmAdminKey, _depth + 1);
            }
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.maxAutomaticTokenAssociations != null)
                if ($Number(object.maxAutomaticTokenAssociations) !== 0)
                    message.maxAutomaticTokenAssociations = object.maxAutomaticTokenAssociations | 0;
            if (object.stakedAccountId != null) {
                if (!$util.isObject(object.stakedAccountId))
                    throw $TypeError(".proto.CryptoCreateTransactionBody.stakedAccountId: object expected");
                message.stakedAccountId = $root.proto.AccountID.fromObject(object.stakedAccountId, _depth + 1);
            }
            if (object.stakedNodeId != null)
                if ($util.Long)
                    message.stakedNodeId = $util.Long.fromValue(object.stakedNodeId, false);
                else if (typeof object.stakedNodeId === "string")
                    message.stakedNodeId = $parseInt(object.stakedNodeId, 10);
                else if (typeof object.stakedNodeId === "number")
                    message.stakedNodeId = object.stakedNodeId;
                else if (typeof object.stakedNodeId === "object")
                    message.stakedNodeId = new $util.LongBits(object.stakedNodeId.low >>> 0, object.stakedNodeId.high >>> 0).toNumber();
            if (object.declineReward != null)
                if (object.declineReward)
                    message.declineReward = $Boolean(object.declineReward);
            if (object.alias != null)
                if (object.alias.length)
                    if (typeof object.alias === "string")
                        $util.base64.decode(object.alias, message.alias = $util.newBuffer($util.base64.length(object.alias)), 0);
                    else if (object.alias.length >= 0)
                        message.alias = object.alias;
            if (object.hookCreationDetails) {
                if (!$Array.isArray(object.hookCreationDetails))
                    throw $TypeError(".proto.CryptoCreateTransactionBody.hookCreationDetails: array expected");
                message.hookCreationDetails = $Array(object.hookCreationDetails.length);
                for (let i = 0; i < object.hookCreationDetails.length; ++i) {
                    if (!$util.isObject(object.hookCreationDetails[i]))
                        throw $TypeError(".proto.CryptoCreateTransactionBody.hookCreationDetails: object expected");
                    message.hookCreationDetails[i] = $root.com.hedera.hapi.node.hooks.HookCreationDetails.fromObject(object.hookCreationDetails[i], _depth + 1);
                }
            }
            if (object.delegationAddress != null)
                if (object.delegationAddress.length)
                    if (typeof object.delegationAddress === "string")
                        $util.base64.decode(object.delegationAddress, message.delegationAddress = $util.newBuffer($util.base64.length(object.delegationAddress)), 0);
                    else if (object.delegationAddress.length >= 0)
                        message.delegationAddress = object.delegationAddress;
            return message;
        };

        /**
         * Creates a plain object from a CryptoCreateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoCreateTransactionBody
         * @static
         * @param {proto.CryptoCreateTransactionBody} message CryptoCreateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoCreateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.hookCreationDetails = [];
            if (options.defaults) {
                object.key = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.initialBalance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.initialBalance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.proxyAccountID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.sendRecordThreshold = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sendRecordThreshold = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.receiveRecordThreshold = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.receiveRecordThreshold = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.receiverSigRequired = false;
                object.autoRenewPeriod = null;
                object.shardID = null;
                object.realmID = null;
                object.newRealmAdminKey = null;
                object.memo = "";
                object.maxAutomaticTokenAssociations = 0;
                object.declineReward = false;
                if (options.bytes === $String)
                    object.alias = "";
                else {
                    object.alias = [];
                    if (options.bytes !== $Array)
                        object.alias = $util.newBuffer(object.alias);
                }
                if (options.bytes === $String)
                    object.delegationAddress = "";
                else {
                    object.delegationAddress = [];
                    if (options.bytes !== $Array)
                        object.delegationAddress = $util.newBuffer(object.delegationAddress);
                }
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = $root.proto.Key.toObject(message.key, options, _depth + 1);
            if (message.initialBalance != null && $Object.hasOwnProperty.call(message, "initialBalance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.initialBalance = typeof message.initialBalance === "number" ? $BigInt(message.initialBalance) : $util.Long.fromBits(message.initialBalance.low >>> 0, message.initialBalance.high >>> 0, true).toBigInt();
                else if (typeof message.initialBalance === "number")
                    object.initialBalance = options.longs === $String ? $String(message.initialBalance) : message.initialBalance;
                else
                    object.initialBalance = options.longs === $String ? $util.Long.prototype.toString.call(message.initialBalance) : options.longs === $Number ? new $util.LongBits(message.initialBalance.low >>> 0, message.initialBalance.high >>> 0).toNumber(true) : message.initialBalance;
            if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID"))
                object.proxyAccountID = $root.proto.AccountID.toObject(message.proxyAccountID, options, _depth + 1);
            if (message.sendRecordThreshold != null && $Object.hasOwnProperty.call(message, "sendRecordThreshold"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sendRecordThreshold = typeof message.sendRecordThreshold === "number" ? $BigInt(message.sendRecordThreshold) : $util.Long.fromBits(message.sendRecordThreshold.low >>> 0, message.sendRecordThreshold.high >>> 0, true).toBigInt();
                else if (typeof message.sendRecordThreshold === "number")
                    object.sendRecordThreshold = options.longs === $String ? $String(message.sendRecordThreshold) : message.sendRecordThreshold;
                else
                    object.sendRecordThreshold = options.longs === $String ? $util.Long.prototype.toString.call(message.sendRecordThreshold) : options.longs === $Number ? new $util.LongBits(message.sendRecordThreshold.low >>> 0, message.sendRecordThreshold.high >>> 0).toNumber(true) : message.sendRecordThreshold;
            if (message.receiveRecordThreshold != null && $Object.hasOwnProperty.call(message, "receiveRecordThreshold"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.receiveRecordThreshold = typeof message.receiveRecordThreshold === "number" ? $BigInt(message.receiveRecordThreshold) : $util.Long.fromBits(message.receiveRecordThreshold.low >>> 0, message.receiveRecordThreshold.high >>> 0, true).toBigInt();
                else if (typeof message.receiveRecordThreshold === "number")
                    object.receiveRecordThreshold = options.longs === $String ? $String(message.receiveRecordThreshold) : message.receiveRecordThreshold;
                else
                    object.receiveRecordThreshold = options.longs === $String ? $util.Long.prototype.toString.call(message.receiveRecordThreshold) : options.longs === $Number ? new $util.LongBits(message.receiveRecordThreshold.low >>> 0, message.receiveRecordThreshold.high >>> 0).toNumber(true) : message.receiveRecordThreshold;
            if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired"))
                object.receiverSigRequired = message.receiverSigRequired;
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
            if (message.shardID != null && $Object.hasOwnProperty.call(message, "shardID"))
                object.shardID = $root.proto.ShardID.toObject(message.shardID, options, _depth + 1);
            if (message.realmID != null && $Object.hasOwnProperty.call(message, "realmID"))
                object.realmID = $root.proto.RealmID.toObject(message.realmID, options, _depth + 1);
            if (message.newRealmAdminKey != null && $Object.hasOwnProperty.call(message, "newRealmAdminKey"))
                object.newRealmAdminKey = $root.proto.Key.toObject(message.newRealmAdminKey, options, _depth + 1);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                object.maxAutomaticTokenAssociations = message.maxAutomaticTokenAssociations;
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId")) {
                object.stakedAccountId = $root.proto.AccountID.toObject(message.stakedAccountId, options, _depth + 1);
                if (options.oneofs)
                    object.stakedId = "stakedAccountId";
            }
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakedNodeId = typeof message.stakedNodeId === "number" ? $BigInt(message.stakedNodeId) : $util.Long.fromBits(message.stakedNodeId.low >>> 0, message.stakedNodeId.high >>> 0, false).toBigInt();
                else if (typeof message.stakedNodeId === "number")
                    object.stakedNodeId = options.longs === $String ? $String(message.stakedNodeId) : message.stakedNodeId;
                else
                    object.stakedNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.stakedNodeId) : options.longs === $Number ? new $util.LongBits(message.stakedNodeId.low >>> 0, message.stakedNodeId.high >>> 0).toNumber() : message.stakedNodeId;
                if (options.oneofs)
                    object.stakedId = "stakedNodeId";
            }
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                object.declineReward = message.declineReward;
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                object.alias = options.bytes === $String ? $util.base64.encode(message.alias, 0, message.alias.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.alias) : message.alias;
            if (message.hookCreationDetails && message.hookCreationDetails.length) {
                object.hookCreationDetails = $Array(message.hookCreationDetails.length);
                for (let j = 0; j < message.hookCreationDetails.length; ++j)
                    object.hookCreationDetails[j] = $root.com.hedera.hapi.node.hooks.HookCreationDetails.toObject(message.hookCreationDetails[j], options, _depth + 1);
            }
            if (message.delegationAddress != null && $Object.hasOwnProperty.call(message, "delegationAddress"))
                object.delegationAddress = options.bytes === $String ? $util.base64.encode(message.delegationAddress, 0, message.delegationAddress.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.delegationAddress) : message.delegationAddress;
            return object;
        };

        /**
         * Converts this CryptoCreateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.CryptoCreateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoCreateTransactionBody.prototype.toJSON = function() {
            return CryptoCreateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoCreateTransactionBody
         * @function getTypeUrl
         * @memberof proto.CryptoCreateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoCreateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoCreateTransactionBody";
        };

        return CryptoCreateTransactionBody;
    })();

    proto.CryptoDeleteTransactionBody = (function() {

        /**
         * Properties of a CryptoDeleteTransactionBody.
         * @typedef {Object} proto.CryptoDeleteTransactionBody.$Properties
         * @property {proto.AccountID.$Properties|null} [transferAccountID] An account identifier.
         * <p>
         * The identified account SHALL receive all tokens, token balances,
         * and non-fungible/unique from the deleted account.<br/>
         * The identified account MUST sign this transaction.<br/>
         * If not set, the account to be deleted MUST NOT have a balance in any
         * token, a balance in HBAR, or hold any NFT.
         * @property {proto.AccountID.$Properties|null} [deleteAccountID] An account identifier.
         * <p>
         * This account SHALL be deleted if this transaction succeeds.<br/>
         * This account SHOULD NOT hold any balance other than HBAR.<br/>
         * If this account _does_ hold balances, the `transferAccountID` value
         * MUST be set to a valid transfer account.<br/>
         * This account MUST sign this transaction.<br/>
         * This field MUST be set to a valid account identifier.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoDeleteTransactionBody.
         * @memberof proto
         * @interface ICryptoDeleteTransactionBody
         * @augments proto.CryptoDeleteTransactionBody.$Properties
         * @deprecated Use proto.CryptoDeleteTransactionBody.$Properties instead.
         */

        /**
         * Shape of a CryptoDeleteTransactionBody.
         * @typedef {{
         *   transferAccountID?: proto.AccountID.$Shape|null;
         *   deleteAccountID?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoDeleteTransactionBody.$Shape
         */

        /**
         * Constructs a new CryptoDeleteTransactionBody.
         * @memberof proto
         * @classdesc Delete an account.<br/>
         * This will mark an account deleted, and transfer all tokens to a "sweep"
         * account.
         * 
         * A deleted account SHALL NOT hold a balance in any token type.<br/>
         * A deleted account SHALL remain in state until it expires.<br/>
         * Transfers that would increase the balance of a deleted account
         * SHALL fail.<br/>
         * A deleted account MAY be subject of a `cryptoUpdate` transaction to extend
         * its expiration.<br/>
         * When a deleted account expires it SHALL be removed entirely, and SHALL NOT
         * be archived.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.CryptoDeleteTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoDeleteTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An account identifier.
         * <p>
         * The identified account SHALL receive all tokens, token balances,
         * and non-fungible/unique from the deleted account.<br/>
         * The identified account MUST sign this transaction.<br/>
         * If not set, the account to be deleted MUST NOT have a balance in any
         * token, a balance in HBAR, or hold any NFT.
         * @member {proto.AccountID.$Properties|null|undefined} transferAccountID
         * @memberof proto.CryptoDeleteTransactionBody
         * @instance
         */
        CryptoDeleteTransactionBody.prototype.transferAccountID = null;

        /**
         * An account identifier.
         * <p>
         * This account SHALL be deleted if this transaction succeeds.<br/>
         * This account SHOULD NOT hold any balance other than HBAR.<br/>
         * If this account _does_ hold balances, the `transferAccountID` value
         * MUST be set to a valid transfer account.<br/>
         * This account MUST sign this transaction.<br/>
         * This field MUST be set to a valid account identifier.
         * @member {proto.AccountID.$Properties|null|undefined} deleteAccountID
         * @memberof proto.CryptoDeleteTransactionBody
         * @instance
         */
        CryptoDeleteTransactionBody.prototype.deleteAccountID = null;

        /**
         * Creates a new CryptoDeleteTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.CryptoDeleteTransactionBody
         * @static
         * @param {proto.CryptoDeleteTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoDeleteTransactionBody} CryptoDeleteTransactionBody instance
         * @type {{
         *   (properties: proto.CryptoDeleteTransactionBody.$Shape): proto.CryptoDeleteTransactionBody & proto.CryptoDeleteTransactionBody.$Shape;
         *   (properties?: proto.CryptoDeleteTransactionBody.$Properties): proto.CryptoDeleteTransactionBody;
         * }}
         */
        CryptoDeleteTransactionBody.create = function(properties) {
            return new CryptoDeleteTransactionBody(properties);
        };

        /**
         * Encodes the specified CryptoDeleteTransactionBody message. Does not implicitly {@link proto.CryptoDeleteTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoDeleteTransactionBody
         * @static
         * @param {proto.CryptoDeleteTransactionBody.$Properties} message CryptoDeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoDeleteTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.transferAccountID != null && $Object.hasOwnProperty.call(message, "transferAccountID"))
                $root.proto.AccountID.encode(message.transferAccountID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.deleteAccountID != null && $Object.hasOwnProperty.call(message, "deleteAccountID"))
                $root.proto.AccountID.encode(message.deleteAccountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoDeleteTransactionBody message, length delimited. Does not implicitly {@link proto.CryptoDeleteTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoDeleteTransactionBody
         * @static
         * @param {proto.CryptoDeleteTransactionBody.$Properties} message CryptoDeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoDeleteTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoDeleteTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoDeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoDeleteTransactionBody & proto.CryptoDeleteTransactionBody.$Shape} CryptoDeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoDeleteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoDeleteTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.transferAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transferAccountID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.deleteAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.deleteAccountID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoDeleteTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoDeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoDeleteTransactionBody & proto.CryptoDeleteTransactionBody.$Shape} CryptoDeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoDeleteTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoDeleteTransactionBody message.
         * @function verify
         * @memberof proto.CryptoDeleteTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoDeleteTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.transferAccountID != null && $Object.hasOwnProperty.call(message, "transferAccountID")) {
                let error = $root.proto.AccountID.verify(message.transferAccountID, _depth + 1);
                if (error)
                    return "transferAccountID." + error;
            }
            if (message.deleteAccountID != null && $Object.hasOwnProperty.call(message, "deleteAccountID")) {
                let error = $root.proto.AccountID.verify(message.deleteAccountID, _depth + 1);
                if (error)
                    return "deleteAccountID." + error;
            }
            return null;
        };

        /**
         * Creates a CryptoDeleteTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoDeleteTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoDeleteTransactionBody} CryptoDeleteTransactionBody
         */
        CryptoDeleteTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoDeleteTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoDeleteTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoDeleteTransactionBody();
            if (object.transferAccountID != null) {
                if (!$util.isObject(object.transferAccountID))
                    throw $TypeError(".proto.CryptoDeleteTransactionBody.transferAccountID: object expected");
                message.transferAccountID = $root.proto.AccountID.fromObject(object.transferAccountID, _depth + 1);
            }
            if (object.deleteAccountID != null) {
                if (!$util.isObject(object.deleteAccountID))
                    throw $TypeError(".proto.CryptoDeleteTransactionBody.deleteAccountID: object expected");
                message.deleteAccountID = $root.proto.AccountID.fromObject(object.deleteAccountID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoDeleteTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoDeleteTransactionBody
         * @static
         * @param {proto.CryptoDeleteTransactionBody} message CryptoDeleteTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoDeleteTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.transferAccountID = null;
                object.deleteAccountID = null;
            }
            if (message.transferAccountID != null && $Object.hasOwnProperty.call(message, "transferAccountID"))
                object.transferAccountID = $root.proto.AccountID.toObject(message.transferAccountID, options, _depth + 1);
            if (message.deleteAccountID != null && $Object.hasOwnProperty.call(message, "deleteAccountID"))
                object.deleteAccountID = $root.proto.AccountID.toObject(message.deleteAccountID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this CryptoDeleteTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.CryptoDeleteTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoDeleteTransactionBody.prototype.toJSON = function() {
            return CryptoDeleteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoDeleteTransactionBody
         * @function getTypeUrl
         * @memberof proto.CryptoDeleteTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoDeleteTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoDeleteTransactionBody";
        };

        return CryptoDeleteTransactionBody;
    })();

    proto.CryptoDeleteLiveHashTransactionBody = (function() {

        /**
         * Properties of a CryptoDeleteLiveHashTransactionBody.
         * @typedef {Object} proto.CryptoDeleteLiveHashTransactionBody.$Properties
         * @property {proto.AccountID.$Properties|null} [accountOfLiveHash] An account associated to a live hash.
         * @property {Uint8Array|null} [liveHashToDelete] The SHA-384 value of a specific live hash to delete.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoDeleteLiveHashTransactionBody.
         * @memberof proto
         * @interface ICryptoDeleteLiveHashTransactionBody
         * @augments proto.CryptoDeleteLiveHashTransactionBody.$Properties
         * @deprecated Use proto.CryptoDeleteLiveHashTransactionBody.$Properties instead.
         */

        /**
         * Shape of a CryptoDeleteLiveHashTransactionBody.
         * @typedef {{
         *   accountOfLiveHash?: proto.AccountID.$Shape|null;
         *   liveHashToDelete?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoDeleteLiveHashTransactionBody.$Shape
         */

        /**
         * Constructs a new CryptoDeleteLiveHashTransactionBody.
         * @memberof proto
         * @classdesc Delete a specific live hash associated to a given account.
         * 
         * This transaction MUST be signed by either the key of the associated account,
         * or at least one of the keys listed in the live hash.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.CryptoDeleteLiveHashTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoDeleteLiveHashTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An account associated to a live hash.
         * @member {proto.AccountID.$Properties|null|undefined} accountOfLiveHash
         * @memberof proto.CryptoDeleteLiveHashTransactionBody
         * @instance
         */
        CryptoDeleteLiveHashTransactionBody.prototype.accountOfLiveHash = null;

        /**
         * The SHA-384 value of a specific live hash to delete.
         * @member {Uint8Array} liveHashToDelete
         * @memberof proto.CryptoDeleteLiveHashTransactionBody
         * @instance
         */
        CryptoDeleteLiveHashTransactionBody.prototype.liveHashToDelete = $util.newBuffer([]);

        /**
         * Creates a new CryptoDeleteLiveHashTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.CryptoDeleteLiveHashTransactionBody
         * @static
         * @param {proto.CryptoDeleteLiveHashTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoDeleteLiveHashTransactionBody} CryptoDeleteLiveHashTransactionBody instance
         * @type {{
         *   (properties: proto.CryptoDeleteLiveHashTransactionBody.$Shape): proto.CryptoDeleteLiveHashTransactionBody & proto.CryptoDeleteLiveHashTransactionBody.$Shape;
         *   (properties?: proto.CryptoDeleteLiveHashTransactionBody.$Properties): proto.CryptoDeleteLiveHashTransactionBody;
         * }}
         */
        CryptoDeleteLiveHashTransactionBody.create = function(properties) {
            return new CryptoDeleteLiveHashTransactionBody(properties);
        };

        /**
         * Encodes the specified CryptoDeleteLiveHashTransactionBody message. Does not implicitly {@link proto.CryptoDeleteLiveHashTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoDeleteLiveHashTransactionBody
         * @static
         * @param {proto.CryptoDeleteLiveHashTransactionBody.$Properties} message CryptoDeleteLiveHashTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoDeleteLiveHashTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountOfLiveHash != null && $Object.hasOwnProperty.call(message, "accountOfLiveHash"))
                $root.proto.AccountID.encode(message.accountOfLiveHash, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.liveHashToDelete != null && $Object.hasOwnProperty.call(message, "liveHashToDelete") && message.liveHashToDelete.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.liveHashToDelete);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoDeleteLiveHashTransactionBody message, length delimited. Does not implicitly {@link proto.CryptoDeleteLiveHashTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoDeleteLiveHashTransactionBody
         * @static
         * @param {proto.CryptoDeleteLiveHashTransactionBody.$Properties} message CryptoDeleteLiveHashTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoDeleteLiveHashTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoDeleteLiveHashTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoDeleteLiveHashTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoDeleteLiveHashTransactionBody & proto.CryptoDeleteLiveHashTransactionBody.$Shape} CryptoDeleteLiveHashTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoDeleteLiveHashTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoDeleteLiveHashTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.accountOfLiveHash = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountOfLiveHash);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.liveHashToDelete = value;
                        else
                            delete message.liveHashToDelete;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoDeleteLiveHashTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoDeleteLiveHashTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoDeleteLiveHashTransactionBody & proto.CryptoDeleteLiveHashTransactionBody.$Shape} CryptoDeleteLiveHashTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoDeleteLiveHashTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoDeleteLiveHashTransactionBody message.
         * @function verify
         * @memberof proto.CryptoDeleteLiveHashTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoDeleteLiveHashTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.accountOfLiveHash != null && $Object.hasOwnProperty.call(message, "accountOfLiveHash")) {
                let error = $root.proto.AccountID.verify(message.accountOfLiveHash, _depth + 1);
                if (error)
                    return "accountOfLiveHash." + error;
            }
            if (message.liveHashToDelete != null && $Object.hasOwnProperty.call(message, "liveHashToDelete"))
                if (!(message.liveHashToDelete && typeof message.liveHashToDelete.length === "number" || $util.isString(message.liveHashToDelete)))
                    return "liveHashToDelete: buffer expected";
            return null;
        };

        /**
         * Creates a CryptoDeleteLiveHashTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoDeleteLiveHashTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoDeleteLiveHashTransactionBody} CryptoDeleteLiveHashTransactionBody
         */
        CryptoDeleteLiveHashTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoDeleteLiveHashTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoDeleteLiveHashTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoDeleteLiveHashTransactionBody();
            if (object.accountOfLiveHash != null) {
                if (!$util.isObject(object.accountOfLiveHash))
                    throw $TypeError(".proto.CryptoDeleteLiveHashTransactionBody.accountOfLiveHash: object expected");
                message.accountOfLiveHash = $root.proto.AccountID.fromObject(object.accountOfLiveHash, _depth + 1);
            }
            if (object.liveHashToDelete != null)
                if (object.liveHashToDelete.length)
                    if (typeof object.liveHashToDelete === "string")
                        $util.base64.decode(object.liveHashToDelete, message.liveHashToDelete = $util.newBuffer($util.base64.length(object.liveHashToDelete)), 0);
                    else if (object.liveHashToDelete.length >= 0)
                        message.liveHashToDelete = object.liveHashToDelete;
            return message;
        };

        /**
         * Creates a plain object from a CryptoDeleteLiveHashTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoDeleteLiveHashTransactionBody
         * @static
         * @param {proto.CryptoDeleteLiveHashTransactionBody} message CryptoDeleteLiveHashTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoDeleteLiveHashTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.accountOfLiveHash = null;
                if (options.bytes === $String)
                    object.liveHashToDelete = "";
                else {
                    object.liveHashToDelete = [];
                    if (options.bytes !== $Array)
                        object.liveHashToDelete = $util.newBuffer(object.liveHashToDelete);
                }
            }
            if (message.accountOfLiveHash != null && $Object.hasOwnProperty.call(message, "accountOfLiveHash"))
                object.accountOfLiveHash = $root.proto.AccountID.toObject(message.accountOfLiveHash, options, _depth + 1);
            if (message.liveHashToDelete != null && $Object.hasOwnProperty.call(message, "liveHashToDelete"))
                object.liveHashToDelete = options.bytes === $String ? $util.base64.encode(message.liveHashToDelete, 0, message.liveHashToDelete.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.liveHashToDelete) : message.liveHashToDelete;
            return object;
        };

        /**
         * Converts this CryptoDeleteLiveHashTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.CryptoDeleteLiveHashTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoDeleteLiveHashTransactionBody.prototype.toJSON = function() {
            return CryptoDeleteLiveHashTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoDeleteLiveHashTransactionBody
         * @function getTypeUrl
         * @memberof proto.CryptoDeleteLiveHashTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoDeleteLiveHashTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoDeleteLiveHashTransactionBody";
        };

        return CryptoDeleteLiveHashTransactionBody;
    })();

    proto.CryptoTransferTransactionBody = (function() {

        /**
         * Properties of a CryptoTransferTransactionBody.
         * @typedef {Object} proto.CryptoTransferTransactionBody.$Properties
         * @property {proto.TransferList.$Properties|null} [transfers] A list of HBAR transfers.
         * <p>
         * Each transfer in this list MUST be denominated in tinybar.
         * @property {Array.<proto.TokenTransferList.$Properties>|null} [tokenTransfers] One or more lists of token transfers.
         * <p>
         * This list MUST NOT contain more than 10 entries.<br/>
         * If custom fees must be charged, the fee SHALL be assessed against the
         * effective "payer" for this transaction.<br/>
         * If the effective "payer" for this transaction lacks sufficient balance
         * to pay custom fees assessed, the entire transaction SHALL fail with a
         * response code `INSUFFICIENT_PAYER_BALANCE_FOR_CUSTOM_FEE`.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoTransferTransactionBody.
         * @memberof proto
         * @interface ICryptoTransferTransactionBody
         * @augments proto.CryptoTransferTransactionBody.$Properties
         * @deprecated Use proto.CryptoTransferTransactionBody.$Properties instead.
         */

        /**
         * Shape of a CryptoTransferTransactionBody.
         * @typedef {{
         *   transfers?: proto.TransferList.$Shape|null;
         *   tokenTransfers?: Array.<proto.TokenTransferList.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoTransferTransactionBody.$Shape
         */

        /**
         * Constructs a new CryptoTransferTransactionBody.
         * @memberof proto
         * @classdesc Transfer HBAR and/or other tokens among two or more accounts and/or smart
         * contracts.
         * 
         * Transfers of HBAR or fungible/common tokens in this transaction are
         * structured as a "double-entry" transfer list which debits one or more
         * accounts, and separately credits one or more accounts. Each such transfer
         * list may specify up to 10 individual credits or debits.<br/>
         * Transfers of non-fungible/unique tokens in this transaction are
         * structured as a "single-entry" transfer list, which both debits one account
         * and credits another account in a single entry.
         * 
         * At least one transfer MUST be present, this MAY be an HBAR transfer in
         * `transfers`, or MAY be a token transfer in `tokenTransfers`.<br/>
         * Either `transfers` or `tokenTransfers` MAY be unset, provided the other
         * is set and not empty.<br/>
         * If any one account with a debit in any transfer list holds insufficient
         * balance to complete the transfer, the entire transaction SHALL fail, and
         * all transfers SHALL NOT be completed.<br/>
         * If any one account that is _sending_ an individual non-fungible/unique (NFT)
         * token does not currently hold that unique NFT, the entire transaction SHALL
         * FAIL, and all transfers SHALL NOT be completed.
         * The transaction fee SHALL be charged for a transaction that fails due to
         * insufficient balance or not holding the NFT to be transferred.<br/>
         * Each account with any debit amounts in any transfer list MUST sign this
         * transaction.<br/>
         * Each account with any credit amounts in any transfer list that also has the
         * `receiverSigRequired` flag set MUST sign this transaction.
         * 
         * ### Block Stream Effects
         * All debits and credits completed by this transaction SHALL be included in
         * the transaction result transfer list.<br/>
         * Multiple fungible/common debits from one account, or credits to one account,
         * MAY be consolidated to a single debit or credit entry in the
         * transaction result.<br/>
         * Multiple non-fungible/unique transfers SHALL NOT be consolidated in the
         * transaction result.
         * @constructor
         * @param {proto.CryptoTransferTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoTransferTransactionBody = function (properties) {
            this.tokenTransfers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of HBAR transfers.
         * <p>
         * Each transfer in this list MUST be denominated in tinybar.
         * @member {proto.TransferList.$Properties|null|undefined} transfers
         * @memberof proto.CryptoTransferTransactionBody
         * @instance
         */
        CryptoTransferTransactionBody.prototype.transfers = null;

        /**
         * One or more lists of token transfers.
         * <p>
         * This list MUST NOT contain more than 10 entries.<br/>
         * If custom fees must be charged, the fee SHALL be assessed against the
         * effective "payer" for this transaction.<br/>
         * If the effective "payer" for this transaction lacks sufficient balance
         * to pay custom fees assessed, the entire transaction SHALL fail with a
         * response code `INSUFFICIENT_PAYER_BALANCE_FOR_CUSTOM_FEE`.
         * @member {Array.<proto.TokenTransferList.$Properties>} tokenTransfers
         * @memberof proto.CryptoTransferTransactionBody
         * @instance
         */
        CryptoTransferTransactionBody.prototype.tokenTransfers = $util.emptyArray;

        /**
         * Creates a new CryptoTransferTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.CryptoTransferTransactionBody
         * @static
         * @param {proto.CryptoTransferTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoTransferTransactionBody} CryptoTransferTransactionBody instance
         * @type {{
         *   (properties: proto.CryptoTransferTransactionBody.$Shape): proto.CryptoTransferTransactionBody & proto.CryptoTransferTransactionBody.$Shape;
         *   (properties?: proto.CryptoTransferTransactionBody.$Properties): proto.CryptoTransferTransactionBody;
         * }}
         */
        CryptoTransferTransactionBody.create = function(properties) {
            return new CryptoTransferTransactionBody(properties);
        };

        /**
         * Encodes the specified CryptoTransferTransactionBody message. Does not implicitly {@link proto.CryptoTransferTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoTransferTransactionBody
         * @static
         * @param {proto.CryptoTransferTransactionBody.$Properties} message CryptoTransferTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoTransferTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.transfers != null && $Object.hasOwnProperty.call(message, "transfers"))
                $root.proto.TransferList.encode(message.transfers, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.tokenTransfers != null && message.tokenTransfers.length)
                for (let i = 0; i < message.tokenTransfers.length; ++i)
                    $root.proto.TokenTransferList.encode(message.tokenTransfers[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoTransferTransactionBody message, length delimited. Does not implicitly {@link proto.CryptoTransferTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoTransferTransactionBody
         * @static
         * @param {proto.CryptoTransferTransactionBody.$Properties} message CryptoTransferTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoTransferTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoTransferTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoTransferTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoTransferTransactionBody & proto.CryptoTransferTransactionBody.$Shape} CryptoTransferTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoTransferTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoTransferTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.transfers = $root.proto.TransferList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transfers);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tokenTransfers && message.tokenTransfers.length))
                            message.tokenTransfers = [];
                        message.tokenTransfers.push($root.proto.TokenTransferList.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoTransferTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoTransferTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoTransferTransactionBody & proto.CryptoTransferTransactionBody.$Shape} CryptoTransferTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoTransferTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoTransferTransactionBody message.
         * @function verify
         * @memberof proto.CryptoTransferTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoTransferTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.transfers != null && $Object.hasOwnProperty.call(message, "transfers")) {
                let error = $root.proto.TransferList.verify(message.transfers, _depth + 1);
                if (error)
                    return "transfers." + error;
            }
            if (message.tokenTransfers != null && $Object.hasOwnProperty.call(message, "tokenTransfers")) {
                if (!$Array.isArray(message.tokenTransfers))
                    return "tokenTransfers: array expected";
                for (let i = 0; i < message.tokenTransfers.length; ++i) {
                    let error = $root.proto.TokenTransferList.verify(message.tokenTransfers[i], _depth + 1);
                    if (error)
                        return "tokenTransfers." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CryptoTransferTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoTransferTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoTransferTransactionBody} CryptoTransferTransactionBody
         */
        CryptoTransferTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoTransferTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoTransferTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoTransferTransactionBody();
            if (object.transfers != null) {
                if (!$util.isObject(object.transfers))
                    throw $TypeError(".proto.CryptoTransferTransactionBody.transfers: object expected");
                message.transfers = $root.proto.TransferList.fromObject(object.transfers, _depth + 1);
            }
            if (object.tokenTransfers) {
                if (!$Array.isArray(object.tokenTransfers))
                    throw $TypeError(".proto.CryptoTransferTransactionBody.tokenTransfers: array expected");
                message.tokenTransfers = $Array(object.tokenTransfers.length);
                for (let i = 0; i < object.tokenTransfers.length; ++i) {
                    if (!$util.isObject(object.tokenTransfers[i]))
                        throw $TypeError(".proto.CryptoTransferTransactionBody.tokenTransfers: object expected");
                    message.tokenTransfers[i] = $root.proto.TokenTransferList.fromObject(object.tokenTransfers[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoTransferTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoTransferTransactionBody
         * @static
         * @param {proto.CryptoTransferTransactionBody} message CryptoTransferTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoTransferTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tokenTransfers = [];
            if (options.defaults)
                object.transfers = null;
            if (message.transfers != null && $Object.hasOwnProperty.call(message, "transfers"))
                object.transfers = $root.proto.TransferList.toObject(message.transfers, options, _depth + 1);
            if (message.tokenTransfers && message.tokenTransfers.length) {
                object.tokenTransfers = $Array(message.tokenTransfers.length);
                for (let j = 0; j < message.tokenTransfers.length; ++j)
                    object.tokenTransfers[j] = $root.proto.TokenTransferList.toObject(message.tokenTransfers[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this CryptoTransferTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.CryptoTransferTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoTransferTransactionBody.prototype.toJSON = function() {
            return CryptoTransferTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoTransferTransactionBody
         * @function getTypeUrl
         * @memberof proto.CryptoTransferTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoTransferTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoTransferTransactionBody";
        };

        return CryptoTransferTransactionBody;
    })();

    proto.CryptoUpdateTransactionBody = (function() {

        /**
         * Properties of a CryptoUpdateTransactionBody.
         * @typedef {Object} proto.CryptoUpdateTransactionBody.$Properties
         * @property {proto.AccountID.$Properties|null} [accountIDToUpdate] An account identifier.<br/>
         * This identifies the account which is to be modified in this transaction.
         * <p>
         * This field is REQUIRED.
         * @property {proto.Key.$Properties|null} [key] An account key.<br/>
         * This may be a "primitive" key (a singly cryptographic key), or a
         * composite key.
         * <p>
         * If set, this key MUST be a valid key.<br/>
         * If set, the previous key and new key MUST both sign this transaction.
         * @property {proto.AccountID.$Properties|null} [proxyAccountID] Removed in favor of the `staked_id` oneOf.<br/>
         * An account identifier for a "proxy" account. This account's HBAR are
         * staked to a node selected by the proxy account.
         * @property {number|null} [proxyFraction] Removed prior to the first available history.<br/>
         * A fraction to split staking rewards between this account and the proxy
         * account.
         * @property {number|Long|null} [sendRecordThreshold] Removed prior to the first available history, and may be related
         * to an early design dead-end.<br/>
         * The new threshold amount (in tinybars) for which an account record is
         * created for any send/withdraw transaction
         * @property {google.protobuf.UInt64Value.$Properties|null} [sendRecordThresholdWrapper] Removed prior to the first available history, and may be related
         * to an early design dead-end.<br/>
         * The new threshold amount (in tinybars) for which an account record is
         * created for any send/withdraw transaction
         * @property {number|Long|null} [receiveRecordThreshold] Removed prior to the first available history, and may be related
         * to an early design dead-end.<br/>
         * The new threshold amount (in tinybars) for which an account record is
         * created for any receive/deposit transaction.
         * @property {google.protobuf.UInt64Value.$Properties|null} [receiveRecordThresholdWrapper] Removed prior to the first available history, and may be related
         * to an early design dead-end.<br/>
         * The new threshold amount (in tinybars) for which an account record is
         * created for any receive/deposit transaction.
         * @property {proto.Duration.$Properties|null} [autoRenewPeriod] A duration to extend account expiration.<br/>
         * An amount of time, in seconds, to extend the expiration date for this
         * account when _automatically_ renewed.
         * <p>
         * This duration MUST be between the current configured minimum and maximum
         * values defined for the network.<br/>
         * This duration SHALL be applied only when _automatically_ extending the
         * account expiration.
         * @property {proto.Timestamp.$Properties|null} [expirationTime] A new account expiration time, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0
         * at `1970-01-01T00:00:00.000Z`.<br/>
         * If set, this value MUST be later than the current consensus time.<br/>
         * If set, this value MUST be earlier than the current consensus time
         * extended by the current maximum expiration time configured for the
         * network.
         * @property {boolean|null} [receiverSigRequired] Removed to distinguish between unset and a default value.<br/>
         * Do NOT use this field to set a false value because the server cannot
         * distinguish from the default value. Use receiverSigRequiredWrapper
         * field for this purpose.
         * @property {google.protobuf.BoolValue.$Properties|null} [receiverSigRequiredWrapper] A flag indicating the account holder must authorize all incoming
         * token transfers.
         * <p>
         * If this flag is set then any transaction that would result in adding
         * hbar or other tokens to this account balance MUST be signed by the
         * identifying key of this account (that is, the `key` field).
         * @property {google.protobuf.StringValue.$Properties|null} [memo] A short description of this Account.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {google.protobuf.Int32Value.$Properties|null} [maxAutomaticTokenAssociations] A maximum number of tokens that can be auto-associated
         * with this account.<br/>
         * By default this value is 0 for all accounts except for automatically
         * created accounts (i.e smart contracts) which default to -1.
         * <p>
         * If this value is `0`, then this account MUST manually associate to
         * a token before holding or transacting in that token.<br/>
         * This value MAY also be `-1` to indicate no limit.<br/>
         * If set, this value MUST NOT be less than `-1`.<br/>
         * @property {proto.AccountID.$Properties|null} [stakedAccountId] ID of the account to which this account is staking its balances.
         * <p>
         * If this account is not currently staking its balances, then this
         * field, if set, MUST be the sentinel value of `0.0.0`.
         * @property {number|Long|null} [stakedNodeId] ID of the node this account is staked to.
         * <p>
         * If this account is not currently staking its balances, then this
         * field, if set, SHALL be the sentinel value of `-1`.<br/>
         * Wallet software SHOULD surface staking issues to users and provide a
         * simple mechanism to update staking to a new node ID in the event the
         * prior staked node ID ceases to be valid.
         * @property {google.protobuf.BoolValue.$Properties|null} [declineReward] A boolean indicating that this account has chosen to decline rewards for
         * staking its balances.
         * <p>
         * This account MAY still stake its balances, but SHALL NOT receive reward
         * payments for doing so, if this value is set, and `true`.
         * @property {Array.<number|Long>|null} [hookIdsToDelete] The ids the hooks to delete from the account.
         * @property {Array.<com.hedera.hapi.node.hooks.HookCreationDetails.$Properties>|null} [hookCreationDetails] The hooks to create for the account.
         * @property {Uint8Array|null} [delegationAddress] The delegated contract address to set for the account.
         * <p>
         * If this field is unset (empty), no change is made to account's `delegation_address`.
         * If set, the value MUST be 20 bytes long (EVM address).
         * Submitting a value of `0x0000000000000000000000000000000000000000` will clear
         * account's current `delegation_address`.
         * 
         * If this field is set on the account, anytime a call is made to the account's address
         * (either an EVM call within HSCS or a child call dispatched by CryptoTransfer)
         * the EVM code of the contract referenced by `delegation_address` will be
         * executed in the context of the account.
         * @property {"sendRecordThreshold"|"sendRecordThresholdWrapper"} [sendRecordThresholdField] CryptoUpdateTransactionBody sendRecordThresholdField
         * @property {"receiveRecordThreshold"|"receiveRecordThresholdWrapper"} [receiveRecordThresholdField] CryptoUpdateTransactionBody receiveRecordThresholdField
         * @property {"receiverSigRequired"|"receiverSigRequiredWrapper"} [receiverSigRequiredField] CryptoUpdateTransactionBody receiverSigRequiredField
         * @property {"stakedAccountId"|"stakedNodeId"} [stakedId] CryptoUpdateTransactionBody stakedId
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoUpdateTransactionBody.
         * @memberof proto
         * @interface ICryptoUpdateTransactionBody
         * @augments proto.CryptoUpdateTransactionBody.$Properties
         * @deprecated Use proto.CryptoUpdateTransactionBody.$Properties instead.
         */

        /**
         * Narrowed shape of a CryptoUpdateTransactionBody.
         * @typedef {{
         *   accountIDToUpdate?: proto.AccountID.$Shape|null;
         *   key?: proto.Key.$Shape|null;
         *   proxyAccountID?: proto.AccountID.$Shape|null;
         *   proxyFraction?: number|null;
         *   sendRecordThreshold?: number|Long|null;
         *   sendRecordThresholdWrapper?: google.protobuf.UInt64Value.$Shape|null;
         *   receiveRecordThreshold?: number|Long|null;
         *   receiveRecordThresholdWrapper?: google.protobuf.UInt64Value.$Shape|null;
         *   autoRenewPeriod?: proto.Duration.$Shape|null;
         *   expirationTime?: proto.Timestamp.$Shape|null;
         *   receiverSigRequired?: boolean|null;
         *   receiverSigRequiredWrapper?: google.protobuf.BoolValue.$Shape|null;
         *   memo?: google.protobuf.StringValue.$Shape|null;
         *   maxAutomaticTokenAssociations?: google.protobuf.Int32Value.$Shape|null;
         *   stakedAccountId?: proto.AccountID.$Shape|null;
         *   stakedNodeId?: number|Long|null;
         *   declineReward?: google.protobuf.BoolValue.$Shape|null;
         *   hookIdsToDelete?: Array.<number|Long>|null;
         *   hookCreationDetails?: Array.<com.hedera.hapi.node.hooks.HookCreationDetails.$Shape>|null;
         *   delegationAddress?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ sendRecordThresholdField?: undefined; sendRecordThreshold?: null; sendRecordThresholdWrapper?: null }|{ sendRecordThresholdField?: "sendRecordThreshold"; sendRecordThreshold: number|Long; sendRecordThresholdWrapper?: null }|{ sendRecordThresholdField?: "sendRecordThresholdWrapper"; sendRecordThreshold?: null; sendRecordThresholdWrapper: google.protobuf.UInt64Value.$Shape })
         * ) & (
         *   ({ receiveRecordThresholdField?: undefined; receiveRecordThreshold?: null; receiveRecordThresholdWrapper?: null }|{ receiveRecordThresholdField?: "receiveRecordThreshold"; receiveRecordThreshold: number|Long; receiveRecordThresholdWrapper?: null }|{ receiveRecordThresholdField?: "receiveRecordThresholdWrapper"; receiveRecordThreshold?: null; receiveRecordThresholdWrapper: google.protobuf.UInt64Value.$Shape })
         * ) & (
         *   ({ receiverSigRequiredField?: undefined; receiverSigRequired?: null; receiverSigRequiredWrapper?: null }|{ receiverSigRequiredField?: "receiverSigRequired"; receiverSigRequired: boolean; receiverSigRequiredWrapper?: null }|{ receiverSigRequiredField?: "receiverSigRequiredWrapper"; receiverSigRequired?: null; receiverSigRequiredWrapper: google.protobuf.BoolValue.$Shape })
         * ) & (
         *   ({ stakedId?: undefined; stakedAccountId?: null; stakedNodeId?: null }|{ stakedId?: "stakedAccountId"; stakedAccountId: proto.AccountID.$Shape; stakedNodeId?: null }|{ stakedId?: "stakedNodeId"; stakedAccountId?: null; stakedNodeId: number|Long })
         * )} proto.CryptoUpdateTransactionBody.$Shape
         */

        /**
         * Constructs a new CryptoUpdateTransactionBody.
         * @memberof proto
         * @classdesc Modify the current state of an account.
         * 
         * ### Requirements
         * - The `key` for this account MUST sign all account update transactions.
         * - If the `key` field is set for this transaction, then _both_ the current
         * `key` and the new `key` MUST sign this transaction, for security and to
         * prevent setting the `key` field to an invalid value.
         * - If the `auto_renew_account` field is set for this transaction, the account
         * identified in that field MUST sign this transaction.
         * - Fields set to non-default values in this transaction SHALL be updated on
         * success. Fields not set to non-default values SHALL NOT be
         * updated on success.
         * - All fields that may be modified in this transaction SHALL have a
         * default value of unset (a.k.a. `null`).
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.CryptoUpdateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoUpdateTransactionBody = function (properties) {
            this.hookIdsToDelete = [];
            this.hookCreationDetails = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An account identifier.<br/>
         * This identifies the account which is to be modified in this transaction.
         * <p>
         * This field is REQUIRED.
         * @member {proto.AccountID.$Properties|null|undefined} accountIDToUpdate
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.accountIDToUpdate = null;

        /**
         * An account key.<br/>
         * This may be a "primitive" key (a singly cryptographic key), or a
         * composite key.
         * <p>
         * If set, this key MUST be a valid key.<br/>
         * If set, the previous key and new key MUST both sign this transaction.
         * @member {proto.Key.$Properties|null|undefined} key
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.key = null;

        /**
         * Removed in favor of the `staked_id` oneOf.<br/>
         * An account identifier for a "proxy" account. This account's HBAR are
         * staked to a node selected by the proxy account.
         * @member {proto.AccountID.$Properties|null|undefined} proxyAccountID
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.proxyAccountID = null;

        /**
         * Removed prior to the first available history.<br/>
         * A fraction to split staking rewards between this account and the proxy
         * account.
         * @member {number} proxyFraction
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.proxyFraction = 0;

        /**
         * Removed prior to the first available history, and may be related
         * to an early design dead-end.<br/>
         * The new threshold amount (in tinybars) for which an account record is
         * created for any send/withdraw transaction
         * @member {number|Long|null|undefined} sendRecordThreshold
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.sendRecordThreshold = null;

        /**
         * Removed prior to the first available history, and may be related
         * to an early design dead-end.<br/>
         * The new threshold amount (in tinybars) for which an account record is
         * created for any send/withdraw transaction
         * @member {google.protobuf.UInt64Value.$Properties|null|undefined} sendRecordThresholdWrapper
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.sendRecordThresholdWrapper = null;

        /**
         * Removed prior to the first available history, and may be related
         * to an early design dead-end.<br/>
         * The new threshold amount (in tinybars) for which an account record is
         * created for any receive/deposit transaction.
         * @member {number|Long|null|undefined} receiveRecordThreshold
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.receiveRecordThreshold = null;

        /**
         * Removed prior to the first available history, and may be related
         * to an early design dead-end.<br/>
         * The new threshold amount (in tinybars) for which an account record is
         * created for any receive/deposit transaction.
         * @member {google.protobuf.UInt64Value.$Properties|null|undefined} receiveRecordThresholdWrapper
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.receiveRecordThresholdWrapper = null;

        /**
         * A duration to extend account expiration.<br/>
         * An amount of time, in seconds, to extend the expiration date for this
         * account when _automatically_ renewed.
         * <p>
         * This duration MUST be between the current configured minimum and maximum
         * values defined for the network.<br/>
         * This duration SHALL be applied only when _automatically_ extending the
         * account expiration.
         * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.autoRenewPeriod = null;

        /**
         * A new account expiration time, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0
         * at `1970-01-01T00:00:00.000Z`.<br/>
         * If set, this value MUST be later than the current consensus time.<br/>
         * If set, this value MUST be earlier than the current consensus time
         * extended by the current maximum expiration time configured for the
         * network.
         * @member {proto.Timestamp.$Properties|null|undefined} expirationTime
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.expirationTime = null;

        /**
         * Removed to distinguish between unset and a default value.<br/>
         * Do NOT use this field to set a false value because the server cannot
         * distinguish from the default value. Use receiverSigRequiredWrapper
         * field for this purpose.
         * @member {boolean|null|undefined} receiverSigRequired
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.receiverSigRequired = null;

        /**
         * A flag indicating the account holder must authorize all incoming
         * token transfers.
         * <p>
         * If this flag is set then any transaction that would result in adding
         * hbar or other tokens to this account balance MUST be signed by the
         * identifying key of this account (that is, the `key` field).
         * @member {google.protobuf.BoolValue.$Properties|null|undefined} receiverSigRequiredWrapper
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.receiverSigRequiredWrapper = null;

        /**
         * A short description of this Account.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {google.protobuf.StringValue.$Properties|null|undefined} memo
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.memo = null;

        /**
         * A maximum number of tokens that can be auto-associated
         * with this account.<br/>
         * By default this value is 0 for all accounts except for automatically
         * created accounts (i.e smart contracts) which default to -1.
         * <p>
         * If this value is `0`, then this account MUST manually associate to
         * a token before holding or transacting in that token.<br/>
         * This value MAY also be `-1` to indicate no limit.<br/>
         * If set, this value MUST NOT be less than `-1`.<br/>
         * @member {google.protobuf.Int32Value.$Properties|null|undefined} maxAutomaticTokenAssociations
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.maxAutomaticTokenAssociations = null;

        /**
         * ID of the account to which this account is staking its balances.
         * <p>
         * If this account is not currently staking its balances, then this
         * field, if set, MUST be the sentinel value of `0.0.0`.
         * @member {proto.AccountID.$Properties|null|undefined} stakedAccountId
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.stakedAccountId = null;

        /**
         * ID of the node this account is staked to.
         * <p>
         * If this account is not currently staking its balances, then this
         * field, if set, SHALL be the sentinel value of `-1`.<br/>
         * Wallet software SHOULD surface staking issues to users and provide a
         * simple mechanism to update staking to a new node ID in the event the
         * prior staked node ID ceases to be valid.
         * @member {number|Long|null|undefined} stakedNodeId
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.stakedNodeId = null;

        /**
         * A boolean indicating that this account has chosen to decline rewards for
         * staking its balances.
         * <p>
         * This account MAY still stake its balances, but SHALL NOT receive reward
         * payments for doing so, if this value is set, and `true`.
         * @member {google.protobuf.BoolValue.$Properties|null|undefined} declineReward
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.declineReward = null;

        /**
         * The ids the hooks to delete from the account.
         * @member {Array.<number|Long>} hookIdsToDelete
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.hookIdsToDelete = $util.emptyArray;

        /**
         * The hooks to create for the account.
         * @member {Array.<com.hedera.hapi.node.hooks.HookCreationDetails.$Properties>} hookCreationDetails
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.hookCreationDetails = $util.emptyArray;

        /**
         * The delegated contract address to set for the account.
         * <p>
         * If this field is unset (empty), no change is made to account's `delegation_address`.
         * If set, the value MUST be 20 bytes long (EVM address).
         * Submitting a value of `0x0000000000000000000000000000000000000000` will clear
         * account's current `delegation_address`.
         * 
         * If this field is set on the account, anytime a call is made to the account's address
         * (either an EVM call within HSCS or a child call dispatched by CryptoTransfer)
         * the EVM code of the contract referenced by `delegation_address` will be
         * executed in the context of the account.
         * @member {Uint8Array} delegationAddress
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        CryptoUpdateTransactionBody.prototype.delegationAddress = $util.newBuffer([]);

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * CryptoUpdateTransactionBody sendRecordThresholdField.
         * @member {"sendRecordThreshold"|"sendRecordThresholdWrapper"|undefined} sendRecordThresholdField
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        $Object.defineProperty(CryptoUpdateTransactionBody.prototype, "sendRecordThresholdField", {
            get: $util.oneOfGetter($oneOfFields = ["sendRecordThreshold", "sendRecordThresholdWrapper"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * CryptoUpdateTransactionBody receiveRecordThresholdField.
         * @member {"receiveRecordThreshold"|"receiveRecordThresholdWrapper"|undefined} receiveRecordThresholdField
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        $Object.defineProperty(CryptoUpdateTransactionBody.prototype, "receiveRecordThresholdField", {
            get: $util.oneOfGetter($oneOfFields = ["receiveRecordThreshold", "receiveRecordThresholdWrapper"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * CryptoUpdateTransactionBody receiverSigRequiredField.
         * @member {"receiverSigRequired"|"receiverSigRequiredWrapper"|undefined} receiverSigRequiredField
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        $Object.defineProperty(CryptoUpdateTransactionBody.prototype, "receiverSigRequiredField", {
            get: $util.oneOfGetter($oneOfFields = ["receiverSigRequired", "receiverSigRequiredWrapper"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * CryptoUpdateTransactionBody stakedId.
         * @member {"stakedAccountId"|"stakedNodeId"|undefined} stakedId
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         */
        $Object.defineProperty(CryptoUpdateTransactionBody.prototype, "stakedId", {
            get: $util.oneOfGetter($oneOfFields = ["stakedAccountId", "stakedNodeId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new CryptoUpdateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.CryptoUpdateTransactionBody
         * @static
         * @param {proto.CryptoUpdateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoUpdateTransactionBody} CryptoUpdateTransactionBody instance
         * @type {{
         *   (properties: proto.CryptoUpdateTransactionBody.$Shape): proto.CryptoUpdateTransactionBody & proto.CryptoUpdateTransactionBody.$Shape;
         *   (properties?: proto.CryptoUpdateTransactionBody.$Properties): proto.CryptoUpdateTransactionBody;
         * }}
         */
        CryptoUpdateTransactionBody.create = function(properties) {
            return new CryptoUpdateTransactionBody(properties);
        };

        /**
         * Encodes the specified CryptoUpdateTransactionBody message. Does not implicitly {@link proto.CryptoUpdateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoUpdateTransactionBody
         * @static
         * @param {proto.CryptoUpdateTransactionBody.$Properties} message CryptoUpdateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoUpdateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountIDToUpdate != null && $Object.hasOwnProperty.call(message, "accountIDToUpdate"))
                $root.proto.AccountID.encode(message.accountIDToUpdate, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                $root.proto.Key.encode(message.key, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID"))
                $root.proto.AccountID.encode(message.proxyAccountID, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.proxyFraction != null && $Object.hasOwnProperty.call(message, "proxyFraction") && message.proxyFraction !== 0)
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.proxyFraction);
            if (message.sendRecordThreshold != null && $Object.hasOwnProperty.call(message, "sendRecordThreshold"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.sendRecordThreshold);
            if (message.receiveRecordThreshold != null && $Object.hasOwnProperty.call(message, "receiveRecordThreshold"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.receiveRecordThreshold);
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                $root.proto.Timestamp.encode(message.expirationTime, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.receiverSigRequired);
            if (message.sendRecordThresholdWrapper != null && $Object.hasOwnProperty.call(message, "sendRecordThresholdWrapper"))
                $root.google.protobuf.UInt64Value.encode(message.sendRecordThresholdWrapper, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.receiveRecordThresholdWrapper != null && $Object.hasOwnProperty.call(message, "receiveRecordThresholdWrapper"))
                $root.google.protobuf.UInt64Value.encode(message.receiveRecordThresholdWrapper, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.receiverSigRequiredWrapper != null && $Object.hasOwnProperty.call(message, "receiverSigRequiredWrapper"))
                $root.google.protobuf.BoolValue.encode(message.receiverSigRequiredWrapper, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                $root.google.protobuf.StringValue.encode(message.memo, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                $root.google.protobuf.Int32Value.encode(message.maxAutomaticTokenAssociations, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId"))
                $root.proto.AccountID.encode(message.stakedAccountId, writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId"))
                writer.uint32(/* id 17, wireType 0 =*/136).int64(message.stakedNodeId);
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                $root.google.protobuf.BoolValue.encode(message.declineReward, writer.uint32(/* id 18, wireType 2 =*/146).fork(), _depth + 1).ldelim();
            if (message.hookIdsToDelete != null && message.hookIdsToDelete.length)
                writer.uint32(/* id 19, wireType 2 =*/154).int64s(message.hookIdsToDelete);
            if (message.hookCreationDetails != null && message.hookCreationDetails.length)
                for (let i = 0; i < message.hookCreationDetails.length; ++i)
                    $root.com.hedera.hapi.node.hooks.HookCreationDetails.encode(message.hookCreationDetails[i], writer.uint32(/* id 20, wireType 2 =*/162).fork(), _depth + 1).ldelim();
            if (message.delegationAddress != null && $Object.hasOwnProperty.call(message, "delegationAddress") && message.delegationAddress.length)
                writer.uint32(/* id 21, wireType 2 =*/170).bytes(message.delegationAddress);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoUpdateTransactionBody message, length delimited. Does not implicitly {@link proto.CryptoUpdateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoUpdateTransactionBody
         * @static
         * @param {proto.CryptoUpdateTransactionBody.$Properties} message CryptoUpdateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoUpdateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoUpdateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoUpdateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoUpdateTransactionBody & proto.CryptoUpdateTransactionBody.$Shape} CryptoUpdateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoUpdateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoUpdateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountIDToUpdate = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountIDToUpdate);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.key = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.proxyAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.proxyAccountID);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.proxyFraction = value;
                        else
                            delete message.proxyFraction;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        message.sendRecordThreshold = reader.uint64();
                        message.sendRecordThresholdField = "sendRecordThreshold";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.sendRecordThresholdWrapper = $root.google.protobuf.UInt64Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.sendRecordThresholdWrapper);
                        message.sendRecordThresholdField = "sendRecordThresholdWrapper";
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        message.receiveRecordThreshold = reader.uint64();
                        message.receiveRecordThresholdField = "receiveRecordThreshold";
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.receiveRecordThresholdWrapper = $root.google.protobuf.UInt64Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.receiveRecordThresholdWrapper);
                        message.receiveRecordThresholdField = "receiveRecordThresholdWrapper";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.expirationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        message.receiverSigRequired = reader.bool();
                        message.receiverSigRequiredField = "receiverSigRequired";
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.receiverSigRequiredWrapper = $root.google.protobuf.BoolValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.receiverSigRequiredWrapper);
                        message.receiverSigRequiredField = "receiverSigRequiredWrapper";
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.memo = $root.google.protobuf.StringValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.memo);
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.maxAutomaticTokenAssociations = $root.google.protobuf.Int32Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.maxAutomaticTokenAssociations);
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.stakedAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stakedAccountId);
                        message.stakedId = "stakedAccountId";
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        message.stakedNodeId = reader.int64();
                        message.stakedId = "stakedNodeId";
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        message.declineReward = $root.google.protobuf.BoolValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.declineReward);
                        continue;
                    }
                case 19: {
                        if (wireType === 2) {
                            if (!(message.hookIdsToDelete && message.hookIdsToDelete.length))
                                message.hookIdsToDelete = [];
                            reader.int64s(message.hookIdsToDelete);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.hookIdsToDelete && message.hookIdsToDelete.length))
                            message.hookIdsToDelete = [];
                        message.hookIdsToDelete.push(reader.int64());
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hookCreationDetails && message.hookCreationDetails.length))
                            message.hookCreationDetails = [];
                        message.hookCreationDetails.push($root.com.hedera.hapi.node.hooks.HookCreationDetails.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.delegationAddress = value;
                        else
                            delete message.delegationAddress;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoUpdateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoUpdateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoUpdateTransactionBody & proto.CryptoUpdateTransactionBody.$Shape} CryptoUpdateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoUpdateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoUpdateTransactionBody message.
         * @function verify
         * @memberof proto.CryptoUpdateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoUpdateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.accountIDToUpdate != null && $Object.hasOwnProperty.call(message, "accountIDToUpdate")) {
                let error = $root.proto.AccountID.verify(message.accountIDToUpdate, _depth + 1);
                if (error)
                    return "accountIDToUpdate." + error;
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key")) {
                let error = $root.proto.Key.verify(message.key, _depth + 1);
                if (error)
                    return "key." + error;
            }
            if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID")) {
                let error = $root.proto.AccountID.verify(message.proxyAccountID, _depth + 1);
                if (error)
                    return "proxyAccountID." + error;
            }
            if (message.proxyFraction != null && $Object.hasOwnProperty.call(message, "proxyFraction"))
                if (!$util.isInteger(message.proxyFraction))
                    return "proxyFraction: integer expected";
            if (message.sendRecordThreshold != null && $Object.hasOwnProperty.call(message, "sendRecordThreshold")) {
                properties.sendRecordThresholdField = 1;
                if (!$util.isInteger(message.sendRecordThreshold) && !(message.sendRecordThreshold && $util.isInteger(message.sendRecordThreshold.low) && $util.isInteger(message.sendRecordThreshold.high)))
                    return "sendRecordThreshold: integer|Long expected";
            }
            if (message.sendRecordThresholdWrapper != null && $Object.hasOwnProperty.call(message, "sendRecordThresholdWrapper")) {
                if (properties.sendRecordThresholdField === 1)
                    return "sendRecordThresholdField: multiple values";
                properties.sendRecordThresholdField = 1;
                {
                    let error = $root.google.protobuf.UInt64Value.verify(message.sendRecordThresholdWrapper, _depth + 1);
                    if (error)
                        return "sendRecordThresholdWrapper." + error;
                }
            }
            if (message.receiveRecordThreshold != null && $Object.hasOwnProperty.call(message, "receiveRecordThreshold")) {
                properties.receiveRecordThresholdField = 1;
                if (!$util.isInteger(message.receiveRecordThreshold) && !(message.receiveRecordThreshold && $util.isInteger(message.receiveRecordThreshold.low) && $util.isInteger(message.receiveRecordThreshold.high)))
                    return "receiveRecordThreshold: integer|Long expected";
            }
            if (message.receiveRecordThresholdWrapper != null && $Object.hasOwnProperty.call(message, "receiveRecordThresholdWrapper")) {
                if (properties.receiveRecordThresholdField === 1)
                    return "receiveRecordThresholdField: multiple values";
                properties.receiveRecordThresholdField = 1;
                {
                    let error = $root.google.protobuf.UInt64Value.verify(message.receiveRecordThresholdWrapper, _depth + 1);
                    if (error)
                        return "receiveRecordThresholdWrapper." + error;
                }
            }
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                if (error)
                    return "autoRenewPeriod." + error;
            }
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                let error = $root.proto.Timestamp.verify(message.expirationTime, _depth + 1);
                if (error)
                    return "expirationTime." + error;
            }
            if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired")) {
                properties.receiverSigRequiredField = 1;
                if (typeof message.receiverSigRequired !== "boolean")
                    return "receiverSigRequired: boolean expected";
            }
            if (message.receiverSigRequiredWrapper != null && $Object.hasOwnProperty.call(message, "receiverSigRequiredWrapper")) {
                if (properties.receiverSigRequiredField === 1)
                    return "receiverSigRequiredField: multiple values";
                properties.receiverSigRequiredField = 1;
                {
                    let error = $root.google.protobuf.BoolValue.verify(message.receiverSigRequiredWrapper, _depth + 1);
                    if (error)
                        return "receiverSigRequiredWrapper." + error;
                }
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo")) {
                let error = $root.google.protobuf.StringValue.verify(message.memo, _depth + 1);
                if (error)
                    return "memo." + error;
            }
            if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations")) {
                let error = $root.google.protobuf.Int32Value.verify(message.maxAutomaticTokenAssociations, _depth + 1);
                if (error)
                    return "maxAutomaticTokenAssociations." + error;
            }
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId")) {
                properties.stakedId = 1;
                {
                    let error = $root.proto.AccountID.verify(message.stakedAccountId, _depth + 1);
                    if (error)
                        return "stakedAccountId." + error;
                }
            }
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId")) {
                if (properties.stakedId === 1)
                    return "stakedId: multiple values";
                properties.stakedId = 1;
                if (!$util.isInteger(message.stakedNodeId) && !(message.stakedNodeId && $util.isInteger(message.stakedNodeId.low) && $util.isInteger(message.stakedNodeId.high)))
                    return "stakedNodeId: integer|Long expected";
            }
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward")) {
                let error = $root.google.protobuf.BoolValue.verify(message.declineReward, _depth + 1);
                if (error)
                    return "declineReward." + error;
            }
            if (message.hookIdsToDelete != null && $Object.hasOwnProperty.call(message, "hookIdsToDelete")) {
                if (!$Array.isArray(message.hookIdsToDelete))
                    return "hookIdsToDelete: array expected";
                for (let i = 0; i < message.hookIdsToDelete.length; ++i)
                    if (!$util.isInteger(message.hookIdsToDelete[i]) && !(message.hookIdsToDelete[i] && $util.isInteger(message.hookIdsToDelete[i].low) && $util.isInteger(message.hookIdsToDelete[i].high)))
                        return "hookIdsToDelete: integer|Long[] expected";
            }
            if (message.hookCreationDetails != null && $Object.hasOwnProperty.call(message, "hookCreationDetails")) {
                if (!$Array.isArray(message.hookCreationDetails))
                    return "hookCreationDetails: array expected";
                for (let i = 0; i < message.hookCreationDetails.length; ++i) {
                    let error = $root.com.hedera.hapi.node.hooks.HookCreationDetails.verify(message.hookCreationDetails[i], _depth + 1);
                    if (error)
                        return "hookCreationDetails." + error;
                }
            }
            if (message.delegationAddress != null && $Object.hasOwnProperty.call(message, "delegationAddress"))
                if (!(message.delegationAddress && typeof message.delegationAddress.length === "number" || $util.isString(message.delegationAddress)))
                    return "delegationAddress: buffer expected";
            return null;
        };

        /**
         * Creates a CryptoUpdateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoUpdateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoUpdateTransactionBody} CryptoUpdateTransactionBody
         */
        CryptoUpdateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoUpdateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoUpdateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoUpdateTransactionBody();
            if (object.accountIDToUpdate != null) {
                if (!$util.isObject(object.accountIDToUpdate))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.accountIDToUpdate: object expected");
                message.accountIDToUpdate = $root.proto.AccountID.fromObject(object.accountIDToUpdate, _depth + 1);
            }
            if (object.key != null) {
                if (!$util.isObject(object.key))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.key: object expected");
                message.key = $root.proto.Key.fromObject(object.key, _depth + 1);
            }
            if (object.proxyAccountID != null) {
                if (!$util.isObject(object.proxyAccountID))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.proxyAccountID: object expected");
                message.proxyAccountID = $root.proto.AccountID.fromObject(object.proxyAccountID, _depth + 1);
            }
            if (object.proxyFraction != null)
                if ($Number(object.proxyFraction) !== 0)
                    message.proxyFraction = object.proxyFraction | 0;
            if (object.sendRecordThreshold != null)
                if ($util.Long)
                    message.sendRecordThreshold = $util.Long.fromValue(object.sendRecordThreshold, true);
                else if (typeof object.sendRecordThreshold === "string")
                    message.sendRecordThreshold = $parseInt(object.sendRecordThreshold, 10);
                else if (typeof object.sendRecordThreshold === "number")
                    message.sendRecordThreshold = object.sendRecordThreshold;
                else if (typeof object.sendRecordThreshold === "object")
                    message.sendRecordThreshold = new $util.LongBits(object.sendRecordThreshold.low >>> 0, object.sendRecordThreshold.high >>> 0).toNumber(true);
            if (object.sendRecordThresholdWrapper != null) {
                if (!$util.isObject(object.sendRecordThresholdWrapper))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.sendRecordThresholdWrapper: object expected");
                message.sendRecordThresholdWrapper = $root.google.protobuf.UInt64Value.fromObject(object.sendRecordThresholdWrapper, _depth + 1);
            }
            if (object.receiveRecordThreshold != null)
                if ($util.Long)
                    message.receiveRecordThreshold = $util.Long.fromValue(object.receiveRecordThreshold, true);
                else if (typeof object.receiveRecordThreshold === "string")
                    message.receiveRecordThreshold = $parseInt(object.receiveRecordThreshold, 10);
                else if (typeof object.receiveRecordThreshold === "number")
                    message.receiveRecordThreshold = object.receiveRecordThreshold;
                else if (typeof object.receiveRecordThreshold === "object")
                    message.receiveRecordThreshold = new $util.LongBits(object.receiveRecordThreshold.low >>> 0, object.receiveRecordThreshold.high >>> 0).toNumber(true);
            if (object.receiveRecordThresholdWrapper != null) {
                if (!$util.isObject(object.receiveRecordThresholdWrapper))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.receiveRecordThresholdWrapper: object expected");
                message.receiveRecordThresholdWrapper = $root.google.protobuf.UInt64Value.fromObject(object.receiveRecordThresholdWrapper, _depth + 1);
            }
            if (object.autoRenewPeriod != null) {
                if (!$util.isObject(object.autoRenewPeriod))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.autoRenewPeriod: object expected");
                message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
            }
            if (object.expirationTime != null) {
                if (!$util.isObject(object.expirationTime))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.expirationTime: object expected");
                message.expirationTime = $root.proto.Timestamp.fromObject(object.expirationTime, _depth + 1);
            }
            if (object.receiverSigRequired != null)
                message.receiverSigRequired = $Boolean(object.receiverSigRequired);
            if (object.receiverSigRequiredWrapper != null) {
                if (!$util.isObject(object.receiverSigRequiredWrapper))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.receiverSigRequiredWrapper: object expected");
                message.receiverSigRequiredWrapper = $root.google.protobuf.BoolValue.fromObject(object.receiverSigRequiredWrapper, _depth + 1);
            }
            if (object.memo != null) {
                if (!$util.isObject(object.memo))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.memo: object expected");
                message.memo = $root.google.protobuf.StringValue.fromObject(object.memo, _depth + 1);
            }
            if (object.maxAutomaticTokenAssociations != null) {
                if (!$util.isObject(object.maxAutomaticTokenAssociations))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.maxAutomaticTokenAssociations: object expected");
                message.maxAutomaticTokenAssociations = $root.google.protobuf.Int32Value.fromObject(object.maxAutomaticTokenAssociations, _depth + 1);
            }
            if (object.stakedAccountId != null) {
                if (!$util.isObject(object.stakedAccountId))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.stakedAccountId: object expected");
                message.stakedAccountId = $root.proto.AccountID.fromObject(object.stakedAccountId, _depth + 1);
            }
            if (object.stakedNodeId != null)
                if ($util.Long)
                    message.stakedNodeId = $util.Long.fromValue(object.stakedNodeId, false);
                else if (typeof object.stakedNodeId === "string")
                    message.stakedNodeId = $parseInt(object.stakedNodeId, 10);
                else if (typeof object.stakedNodeId === "number")
                    message.stakedNodeId = object.stakedNodeId;
                else if (typeof object.stakedNodeId === "object")
                    message.stakedNodeId = new $util.LongBits(object.stakedNodeId.low >>> 0, object.stakedNodeId.high >>> 0).toNumber();
            if (object.declineReward != null) {
                if (!$util.isObject(object.declineReward))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.declineReward: object expected");
                message.declineReward = $root.google.protobuf.BoolValue.fromObject(object.declineReward, _depth + 1);
            }
            if (object.hookIdsToDelete) {
                if (!$Array.isArray(object.hookIdsToDelete))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.hookIdsToDelete: array expected");
                message.hookIdsToDelete = $Array(object.hookIdsToDelete.length);
                for (let i = 0; i < object.hookIdsToDelete.length; ++i)
                    if ($util.Long)
                        message.hookIdsToDelete[i] = $util.Long.fromValue(object.hookIdsToDelete[i], false);
                    else if (typeof object.hookIdsToDelete[i] === "string")
                        message.hookIdsToDelete[i] = $parseInt(object.hookIdsToDelete[i], 10);
                    else if (typeof object.hookIdsToDelete[i] === "number")
                        message.hookIdsToDelete[i] = object.hookIdsToDelete[i];
                    else if (typeof object.hookIdsToDelete[i] === "object")
                        message.hookIdsToDelete[i] = new $util.LongBits(object.hookIdsToDelete[i].low >>> 0, object.hookIdsToDelete[i].high >>> 0).toNumber();
            }
            if (object.hookCreationDetails) {
                if (!$Array.isArray(object.hookCreationDetails))
                    throw $TypeError(".proto.CryptoUpdateTransactionBody.hookCreationDetails: array expected");
                message.hookCreationDetails = $Array(object.hookCreationDetails.length);
                for (let i = 0; i < object.hookCreationDetails.length; ++i) {
                    if (!$util.isObject(object.hookCreationDetails[i]))
                        throw $TypeError(".proto.CryptoUpdateTransactionBody.hookCreationDetails: object expected");
                    message.hookCreationDetails[i] = $root.com.hedera.hapi.node.hooks.HookCreationDetails.fromObject(object.hookCreationDetails[i], _depth + 1);
                }
            }
            if (object.delegationAddress != null)
                if (object.delegationAddress.length)
                    if (typeof object.delegationAddress === "string")
                        $util.base64.decode(object.delegationAddress, message.delegationAddress = $util.newBuffer($util.base64.length(object.delegationAddress)), 0);
                    else if (object.delegationAddress.length >= 0)
                        message.delegationAddress = object.delegationAddress;
            return message;
        };

        /**
         * Creates a plain object from a CryptoUpdateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoUpdateTransactionBody
         * @static
         * @param {proto.CryptoUpdateTransactionBody} message CryptoUpdateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoUpdateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.hookIdsToDelete = [];
                object.hookCreationDetails = [];
            }
            if (options.defaults) {
                object.accountIDToUpdate = null;
                object.key = null;
                object.proxyAccountID = null;
                object.proxyFraction = 0;
                object.autoRenewPeriod = null;
                object.expirationTime = null;
                object.memo = null;
                object.maxAutomaticTokenAssociations = null;
                object.declineReward = null;
                if (options.bytes === $String)
                    object.delegationAddress = "";
                else {
                    object.delegationAddress = [];
                    if (options.bytes !== $Array)
                        object.delegationAddress = $util.newBuffer(object.delegationAddress);
                }
            }
            if (message.accountIDToUpdate != null && $Object.hasOwnProperty.call(message, "accountIDToUpdate"))
                object.accountIDToUpdate = $root.proto.AccountID.toObject(message.accountIDToUpdate, options, _depth + 1);
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = $root.proto.Key.toObject(message.key, options, _depth + 1);
            if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID"))
                object.proxyAccountID = $root.proto.AccountID.toObject(message.proxyAccountID, options, _depth + 1);
            if (message.proxyFraction != null && $Object.hasOwnProperty.call(message, "proxyFraction"))
                object.proxyFraction = message.proxyFraction;
            if (message.sendRecordThreshold != null && $Object.hasOwnProperty.call(message, "sendRecordThreshold")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sendRecordThreshold = typeof message.sendRecordThreshold === "number" ? $BigInt(message.sendRecordThreshold) : $util.Long.fromBits(message.sendRecordThreshold.low >>> 0, message.sendRecordThreshold.high >>> 0, true).toBigInt();
                else if (typeof message.sendRecordThreshold === "number")
                    object.sendRecordThreshold = options.longs === $String ? $String(message.sendRecordThreshold) : message.sendRecordThreshold;
                else
                    object.sendRecordThreshold = options.longs === $String ? $util.Long.prototype.toString.call(message.sendRecordThreshold) : options.longs === $Number ? new $util.LongBits(message.sendRecordThreshold.low >>> 0, message.sendRecordThreshold.high >>> 0).toNumber(true) : message.sendRecordThreshold;
                if (options.oneofs)
                    object.sendRecordThresholdField = "sendRecordThreshold";
            }
            if (message.receiveRecordThreshold != null && $Object.hasOwnProperty.call(message, "receiveRecordThreshold")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.receiveRecordThreshold = typeof message.receiveRecordThreshold === "number" ? $BigInt(message.receiveRecordThreshold) : $util.Long.fromBits(message.receiveRecordThreshold.low >>> 0, message.receiveRecordThreshold.high >>> 0, true).toBigInt();
                else if (typeof message.receiveRecordThreshold === "number")
                    object.receiveRecordThreshold = options.longs === $String ? $String(message.receiveRecordThreshold) : message.receiveRecordThreshold;
                else
                    object.receiveRecordThreshold = options.longs === $String ? $util.Long.prototype.toString.call(message.receiveRecordThreshold) : options.longs === $Number ? new $util.LongBits(message.receiveRecordThreshold.low >>> 0, message.receiveRecordThreshold.high >>> 0).toNumber(true) : message.receiveRecordThreshold;
                if (options.oneofs)
                    object.receiveRecordThresholdField = "receiveRecordThreshold";
            }
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                object.expirationTime = $root.proto.Timestamp.toObject(message.expirationTime, options, _depth + 1);
            if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired")) {
                object.receiverSigRequired = message.receiverSigRequired;
                if (options.oneofs)
                    object.receiverSigRequiredField = "receiverSigRequired";
            }
            if (message.sendRecordThresholdWrapper != null && $Object.hasOwnProperty.call(message, "sendRecordThresholdWrapper")) {
                object.sendRecordThresholdWrapper = $root.google.protobuf.UInt64Value.toObject(message.sendRecordThresholdWrapper, options, _depth + 1);
                if (options.oneofs)
                    object.sendRecordThresholdField = "sendRecordThresholdWrapper";
            }
            if (message.receiveRecordThresholdWrapper != null && $Object.hasOwnProperty.call(message, "receiveRecordThresholdWrapper")) {
                object.receiveRecordThresholdWrapper = $root.google.protobuf.UInt64Value.toObject(message.receiveRecordThresholdWrapper, options, _depth + 1);
                if (options.oneofs)
                    object.receiveRecordThresholdField = "receiveRecordThresholdWrapper";
            }
            if (message.receiverSigRequiredWrapper != null && $Object.hasOwnProperty.call(message, "receiverSigRequiredWrapper")) {
                object.receiverSigRequiredWrapper = $root.google.protobuf.BoolValue.toObject(message.receiverSigRequiredWrapper, options, _depth + 1);
                if (options.oneofs)
                    object.receiverSigRequiredField = "receiverSigRequiredWrapper";
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = $root.google.protobuf.StringValue.toObject(message.memo, options, _depth + 1);
            if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                object.maxAutomaticTokenAssociations = $root.google.protobuf.Int32Value.toObject(message.maxAutomaticTokenAssociations, options, _depth + 1);
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId")) {
                object.stakedAccountId = $root.proto.AccountID.toObject(message.stakedAccountId, options, _depth + 1);
                if (options.oneofs)
                    object.stakedId = "stakedAccountId";
            }
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakedNodeId = typeof message.stakedNodeId === "number" ? $BigInt(message.stakedNodeId) : $util.Long.fromBits(message.stakedNodeId.low >>> 0, message.stakedNodeId.high >>> 0, false).toBigInt();
                else if (typeof message.stakedNodeId === "number")
                    object.stakedNodeId = options.longs === $String ? $String(message.stakedNodeId) : message.stakedNodeId;
                else
                    object.stakedNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.stakedNodeId) : options.longs === $Number ? new $util.LongBits(message.stakedNodeId.low >>> 0, message.stakedNodeId.high >>> 0).toNumber() : message.stakedNodeId;
                if (options.oneofs)
                    object.stakedId = "stakedNodeId";
            }
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                object.declineReward = $root.google.protobuf.BoolValue.toObject(message.declineReward, options, _depth + 1);
            if (message.hookIdsToDelete && message.hookIdsToDelete.length) {
                object.hookIdsToDelete = $Array(message.hookIdsToDelete.length);
                for (let j = 0; j < message.hookIdsToDelete.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.hookIdsToDelete[j] = typeof message.hookIdsToDelete[j] === "number" ? $BigInt(message.hookIdsToDelete[j]) : $util.Long.fromBits(message.hookIdsToDelete[j].low >>> 0, message.hookIdsToDelete[j].high >>> 0, false).toBigInt();
                    else if (typeof message.hookIdsToDelete[j] === "number")
                        object.hookIdsToDelete[j] = options.longs === $String ? $String(message.hookIdsToDelete[j]) : message.hookIdsToDelete[j];
                    else
                        object.hookIdsToDelete[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.hookIdsToDelete[j]) : options.longs === $Number ? new $util.LongBits(message.hookIdsToDelete[j].low >>> 0, message.hookIdsToDelete[j].high >>> 0).toNumber() : message.hookIdsToDelete[j];
            }
            if (message.hookCreationDetails && message.hookCreationDetails.length) {
                object.hookCreationDetails = $Array(message.hookCreationDetails.length);
                for (let j = 0; j < message.hookCreationDetails.length; ++j)
                    object.hookCreationDetails[j] = $root.com.hedera.hapi.node.hooks.HookCreationDetails.toObject(message.hookCreationDetails[j], options, _depth + 1);
            }
            if (message.delegationAddress != null && $Object.hasOwnProperty.call(message, "delegationAddress"))
                object.delegationAddress = options.bytes === $String ? $util.base64.encode(message.delegationAddress, 0, message.delegationAddress.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.delegationAddress) : message.delegationAddress;
            return object;
        };

        /**
         * Converts this CryptoUpdateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.CryptoUpdateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoUpdateTransactionBody.prototype.toJSON = function() {
            return CryptoUpdateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoUpdateTransactionBody
         * @function getTypeUrl
         * @memberof proto.CryptoUpdateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoUpdateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoUpdateTransactionBody";
        };

        return CryptoUpdateTransactionBody;
    })();

    proto.CryptoApproveAllowanceTransactionBody = (function() {

        /**
         * Properties of a CryptoApproveAllowanceTransactionBody.
         * @typedef {Object} proto.CryptoApproveAllowanceTransactionBody.$Properties
         * @property {Array.<proto.CryptoAllowance.$Properties>|null} [cryptoAllowances] List of hbar allowances approved by the account owner.
         * <p>
         * This list MAY be empty, provided at least one other list is
         * not empty.
         * @property {Array.<proto.NftAllowance.$Properties>|null} [nftAllowances] List of non-fungible token allowances approved by the account owner.
         * <p>
         * This list MAY be empty, provided at least one other list is
         * not empty.
         * @property {Array.<proto.TokenAllowance.$Properties>|null} [tokenAllowances] List of fungible token allowances approved by the account owner.
         * <p>
         * This list MAY be empty, provided at least one other list is
         * not empty.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoApproveAllowanceTransactionBody.
         * @memberof proto
         * @interface ICryptoApproveAllowanceTransactionBody
         * @augments proto.CryptoApproveAllowanceTransactionBody.$Properties
         * @deprecated Use proto.CryptoApproveAllowanceTransactionBody.$Properties instead.
         */

        /**
         * Shape of a CryptoApproveAllowanceTransactionBody.
         * @typedef {{
         *   cryptoAllowances?: Array.<proto.CryptoAllowance.$Shape>|null;
         *   nftAllowances?: Array.<proto.NftAllowance.$Shape>|null;
         *   tokenAllowances?: Array.<proto.TokenAllowance.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoApproveAllowanceTransactionBody.$Shape
         */

        /**
         * Constructs a new CryptoApproveAllowanceTransactionBody.
         * @memberof proto
         * @classdesc Create ("Approve") allowances for one account to transfer tokens owned
         * by a different account.<br/>
         * An allowance permits a "spender" account to independently transfer tokens
         * owned by a separate "owner" account. Each such allowance permits spending
         * any amount, up to a specified limit, for fungible/common tokens; a single
         * specified non-fungible/unique token, or all non-fungible/unique tokens
         * of a particular token type held by the "owner" account.
         * 
         * If the "owner" account is not specified for any allowance in this
         * transaction (the `owner` field is not set), the `payer` account for this
         * transaction SHALL be owner for that allowance.<br/>
         * Each `owner` account specified in any allowance approved in this
         * transaction MUST sign this transaction.<br/>
         * If the `amount` field for any fungible/common allowance in this
         * transaction is `0`, then that allowance SHOULD match an existing,
         * previously approved, allowance which SHALL be removed.<br/>
         * There are three lists in this message. Each list MAY be empty, but
         * _at least one_ list MUST contain _at least one_ entry.
         * 
         * Example for the `payer` rule.<br/>
         * - Given an account `0.0.X` that pays for this transaction, and owner
         * is not specified in an allowance of `200` HBAR to spender account
         * `0.0.Y`. At consensus the spender account `0.0.Y` will have a new
         * allowance to spend `200` HBAR from the balance of account `0.0.X`.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.CryptoApproveAllowanceTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoApproveAllowanceTransactionBody = function (properties) {
            this.cryptoAllowances = [];
            this.nftAllowances = [];
            this.tokenAllowances = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * List of hbar allowances approved by the account owner.
         * <p>
         * This list MAY be empty, provided at least one other list is
         * not empty.
         * @member {Array.<proto.CryptoAllowance.$Properties>} cryptoAllowances
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @instance
         */
        CryptoApproveAllowanceTransactionBody.prototype.cryptoAllowances = $util.emptyArray;

        /**
         * List of non-fungible token allowances approved by the account owner.
         * <p>
         * This list MAY be empty, provided at least one other list is
         * not empty.
         * @member {Array.<proto.NftAllowance.$Properties>} nftAllowances
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @instance
         */
        CryptoApproveAllowanceTransactionBody.prototype.nftAllowances = $util.emptyArray;

        /**
         * List of fungible token allowances approved by the account owner.
         * <p>
         * This list MAY be empty, provided at least one other list is
         * not empty.
         * @member {Array.<proto.TokenAllowance.$Properties>} tokenAllowances
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @instance
         */
        CryptoApproveAllowanceTransactionBody.prototype.tokenAllowances = $util.emptyArray;

        /**
         * Creates a new CryptoApproveAllowanceTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @static
         * @param {proto.CryptoApproveAllowanceTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoApproveAllowanceTransactionBody} CryptoApproveAllowanceTransactionBody instance
         * @type {{
         *   (properties: proto.CryptoApproveAllowanceTransactionBody.$Shape): proto.CryptoApproveAllowanceTransactionBody & proto.CryptoApproveAllowanceTransactionBody.$Shape;
         *   (properties?: proto.CryptoApproveAllowanceTransactionBody.$Properties): proto.CryptoApproveAllowanceTransactionBody;
         * }}
         */
        CryptoApproveAllowanceTransactionBody.create = function(properties) {
            return new CryptoApproveAllowanceTransactionBody(properties);
        };

        /**
         * Encodes the specified CryptoApproveAllowanceTransactionBody message. Does not implicitly {@link proto.CryptoApproveAllowanceTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @static
         * @param {proto.CryptoApproveAllowanceTransactionBody.$Properties} message CryptoApproveAllowanceTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoApproveAllowanceTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.cryptoAllowances != null && message.cryptoAllowances.length)
                for (let i = 0; i < message.cryptoAllowances.length; ++i)
                    $root.proto.CryptoAllowance.encode(message.cryptoAllowances[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.nftAllowances != null && message.nftAllowances.length)
                for (let i = 0; i < message.nftAllowances.length; ++i)
                    $root.proto.NftAllowance.encode(message.nftAllowances[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.tokenAllowances != null && message.tokenAllowances.length)
                for (let i = 0; i < message.tokenAllowances.length; ++i)
                    $root.proto.TokenAllowance.encode(message.tokenAllowances[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoApproveAllowanceTransactionBody message, length delimited. Does not implicitly {@link proto.CryptoApproveAllowanceTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @static
         * @param {proto.CryptoApproveAllowanceTransactionBody.$Properties} message CryptoApproveAllowanceTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoApproveAllowanceTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoApproveAllowanceTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoApproveAllowanceTransactionBody & proto.CryptoApproveAllowanceTransactionBody.$Shape} CryptoApproveAllowanceTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoApproveAllowanceTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoApproveAllowanceTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.cryptoAllowances && message.cryptoAllowances.length))
                            message.cryptoAllowances = [];
                        message.cryptoAllowances.push($root.proto.CryptoAllowance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.nftAllowances && message.nftAllowances.length))
                            message.nftAllowances = [];
                        message.nftAllowances.push($root.proto.NftAllowance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tokenAllowances && message.tokenAllowances.length))
                            message.tokenAllowances = [];
                        message.tokenAllowances.push($root.proto.TokenAllowance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoApproveAllowanceTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoApproveAllowanceTransactionBody & proto.CryptoApproveAllowanceTransactionBody.$Shape} CryptoApproveAllowanceTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoApproveAllowanceTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoApproveAllowanceTransactionBody message.
         * @function verify
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoApproveAllowanceTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.cryptoAllowances != null && $Object.hasOwnProperty.call(message, "cryptoAllowances")) {
                if (!$Array.isArray(message.cryptoAllowances))
                    return "cryptoAllowances: array expected";
                for (let i = 0; i < message.cryptoAllowances.length; ++i) {
                    let error = $root.proto.CryptoAllowance.verify(message.cryptoAllowances[i], _depth + 1);
                    if (error)
                        return "cryptoAllowances." + error;
                }
            }
            if (message.nftAllowances != null && $Object.hasOwnProperty.call(message, "nftAllowances")) {
                if (!$Array.isArray(message.nftAllowances))
                    return "nftAllowances: array expected";
                for (let i = 0; i < message.nftAllowances.length; ++i) {
                    let error = $root.proto.NftAllowance.verify(message.nftAllowances[i], _depth + 1);
                    if (error)
                        return "nftAllowances." + error;
                }
            }
            if (message.tokenAllowances != null && $Object.hasOwnProperty.call(message, "tokenAllowances")) {
                if (!$Array.isArray(message.tokenAllowances))
                    return "tokenAllowances: array expected";
                for (let i = 0; i < message.tokenAllowances.length; ++i) {
                    let error = $root.proto.TokenAllowance.verify(message.tokenAllowances[i], _depth + 1);
                    if (error)
                        return "tokenAllowances." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CryptoApproveAllowanceTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoApproveAllowanceTransactionBody} CryptoApproveAllowanceTransactionBody
         */
        CryptoApproveAllowanceTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoApproveAllowanceTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoApproveAllowanceTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoApproveAllowanceTransactionBody();
            if (object.cryptoAllowances) {
                if (!$Array.isArray(object.cryptoAllowances))
                    throw $TypeError(".proto.CryptoApproveAllowanceTransactionBody.cryptoAllowances: array expected");
                message.cryptoAllowances = $Array(object.cryptoAllowances.length);
                for (let i = 0; i < object.cryptoAllowances.length; ++i) {
                    if (!$util.isObject(object.cryptoAllowances[i]))
                        throw $TypeError(".proto.CryptoApproveAllowanceTransactionBody.cryptoAllowances: object expected");
                    message.cryptoAllowances[i] = $root.proto.CryptoAllowance.fromObject(object.cryptoAllowances[i], _depth + 1);
                }
            }
            if (object.nftAllowances) {
                if (!$Array.isArray(object.nftAllowances))
                    throw $TypeError(".proto.CryptoApproveAllowanceTransactionBody.nftAllowances: array expected");
                message.nftAllowances = $Array(object.nftAllowances.length);
                for (let i = 0; i < object.nftAllowances.length; ++i) {
                    if (!$util.isObject(object.nftAllowances[i]))
                        throw $TypeError(".proto.CryptoApproveAllowanceTransactionBody.nftAllowances: object expected");
                    message.nftAllowances[i] = $root.proto.NftAllowance.fromObject(object.nftAllowances[i], _depth + 1);
                }
            }
            if (object.tokenAllowances) {
                if (!$Array.isArray(object.tokenAllowances))
                    throw $TypeError(".proto.CryptoApproveAllowanceTransactionBody.tokenAllowances: array expected");
                message.tokenAllowances = $Array(object.tokenAllowances.length);
                for (let i = 0; i < object.tokenAllowances.length; ++i) {
                    if (!$util.isObject(object.tokenAllowances[i]))
                        throw $TypeError(".proto.CryptoApproveAllowanceTransactionBody.tokenAllowances: object expected");
                    message.tokenAllowances[i] = $root.proto.TokenAllowance.fromObject(object.tokenAllowances[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoApproveAllowanceTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @static
         * @param {proto.CryptoApproveAllowanceTransactionBody} message CryptoApproveAllowanceTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoApproveAllowanceTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.cryptoAllowances = [];
                object.nftAllowances = [];
                object.tokenAllowances = [];
            }
            if (message.cryptoAllowances && message.cryptoAllowances.length) {
                object.cryptoAllowances = $Array(message.cryptoAllowances.length);
                for (let j = 0; j < message.cryptoAllowances.length; ++j)
                    object.cryptoAllowances[j] = $root.proto.CryptoAllowance.toObject(message.cryptoAllowances[j], options, _depth + 1);
            }
            if (message.nftAllowances && message.nftAllowances.length) {
                object.nftAllowances = $Array(message.nftAllowances.length);
                for (let j = 0; j < message.nftAllowances.length; ++j)
                    object.nftAllowances[j] = $root.proto.NftAllowance.toObject(message.nftAllowances[j], options, _depth + 1);
            }
            if (message.tokenAllowances && message.tokenAllowances.length) {
                object.tokenAllowances = $Array(message.tokenAllowances.length);
                for (let j = 0; j < message.tokenAllowances.length; ++j)
                    object.tokenAllowances[j] = $root.proto.TokenAllowance.toObject(message.tokenAllowances[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this CryptoApproveAllowanceTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoApproveAllowanceTransactionBody.prototype.toJSON = function() {
            return CryptoApproveAllowanceTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoApproveAllowanceTransactionBody
         * @function getTypeUrl
         * @memberof proto.CryptoApproveAllowanceTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoApproveAllowanceTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoApproveAllowanceTransactionBody";
        };

        return CryptoApproveAllowanceTransactionBody;
    })();

    proto.CryptoAllowance = (function() {

        /**
         * Properties of a CryptoAllowance.
         * @typedef {Object} proto.CryptoAllowance.$Properties
         * @property {proto.AccountID.$Properties|null} [owner] An owner account identifier.<br/>
         * This is the account identifier of the account granting an allowance
         * for the `spender` to transfer tokens held by this account.
         * @property {proto.AccountID.$Properties|null} [spender] A spender account identifier.<br/>
         * This is the account identifier of the account permitted to transfer
         * tokens held by the `owner`.
         * @property {number|Long|null} [amount] An amount of tinybar (10<sup>-8</sup> HBAR).<br/>
         * This is the amount of HBAR held by the `owner` that the
         * `spender` is permitted to transfer.
         * <p>
         * This value MUST be a whole number.<br/>
         * This value MUST be greater than 0 to create a new allowance.<br/>
         * This value MAY be exactly `0` to _remove_ an existing allowance.<br/>
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoAllowance.
         * @memberof proto
         * @interface ICryptoAllowance
         * @augments proto.CryptoAllowance.$Properties
         * @deprecated Use proto.CryptoAllowance.$Properties instead.
         */

        /**
         * Shape of a CryptoAllowance.
         * @typedef {{
         *   owner?: proto.AccountID.$Shape|null;
         *   spender?: proto.AccountID.$Shape|null;
         *   amount?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoAllowance.$Shape
         */

        /**
         * Constructs a new CryptoAllowance.
         * @memberof proto
         * @classdesc An approved allowance of hbar transfers.
         * This message specifies one allowance for a single, unique, combination
         * of owner, spender, and amount.
         * 
         * If `owner` is not set, the effective `owner` SHALL be the `payer` for the
         * enclosing transaction.<br/>
         * The `spender` MUST be specified and MUST be a valid account.<br/>
         * The `amount` MUST be a whole number, and SHOULD be greater than `0` unless
         * this allowance is intended to _remove_ a previously approved allowance.
         * @constructor
         * @param {proto.CryptoAllowance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoAllowance = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An owner account identifier.<br/>
         * This is the account identifier of the account granting an allowance
         * for the `spender` to transfer tokens held by this account.
         * @member {proto.AccountID.$Properties|null|undefined} owner
         * @memberof proto.CryptoAllowance
         * @instance
         */
        CryptoAllowance.prototype.owner = null;

        /**
         * A spender account identifier.<br/>
         * This is the account identifier of the account permitted to transfer
         * tokens held by the `owner`.
         * @member {proto.AccountID.$Properties|null|undefined} spender
         * @memberof proto.CryptoAllowance
         * @instance
         */
        CryptoAllowance.prototype.spender = null;

        /**
         * An amount of tinybar (10<sup>-8</sup> HBAR).<br/>
         * This is the amount of HBAR held by the `owner` that the
         * `spender` is permitted to transfer.
         * <p>
         * This value MUST be a whole number.<br/>
         * This value MUST be greater than 0 to create a new allowance.<br/>
         * This value MAY be exactly `0` to _remove_ an existing allowance.<br/>
         * @member {number|Long} amount
         * @memberof proto.CryptoAllowance
         * @instance
         */
        CryptoAllowance.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new CryptoAllowance instance using the specified properties.
         * @function create
         * @memberof proto.CryptoAllowance
         * @static
         * @param {proto.CryptoAllowance.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoAllowance} CryptoAllowance instance
         * @type {{
         *   (properties: proto.CryptoAllowance.$Shape): proto.CryptoAllowance & proto.CryptoAllowance.$Shape;
         *   (properties?: proto.CryptoAllowance.$Properties): proto.CryptoAllowance;
         * }}
         */
        CryptoAllowance.create = function(properties) {
            return new CryptoAllowance(properties);
        };

        /**
         * Encodes the specified CryptoAllowance message. Does not implicitly {@link proto.CryptoAllowance.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoAllowance
         * @static
         * @param {proto.CryptoAllowance.$Properties} message CryptoAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoAllowance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner"))
                $root.proto.AccountID.encode(message.owner, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender"))
                $root.proto.AccountID.encode(message.spender, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.amount);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoAllowance message, length delimited. Does not implicitly {@link proto.CryptoAllowance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoAllowance
         * @static
         * @param {proto.CryptoAllowance.$Properties} message CryptoAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoAllowance.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoAllowance message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoAllowance & proto.CryptoAllowance.$Shape} CryptoAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoAllowance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoAllowance();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.owner = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.owner);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.spender = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spender);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoAllowance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoAllowance & proto.CryptoAllowance.$Shape} CryptoAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoAllowance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoAllowance message.
         * @function verify
         * @memberof proto.CryptoAllowance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoAllowance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner")) {
                let error = $root.proto.AccountID.verify(message.owner, _depth + 1);
                if (error)
                    return "owner." + error;
            }
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender")) {
                let error = $root.proto.AccountID.verify(message.spender, _depth + 1);
                if (error)
                    return "spender." + error;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            return null;
        };

        /**
         * Creates a CryptoAllowance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoAllowance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoAllowance} CryptoAllowance
         */
        CryptoAllowance.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoAllowance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoAllowance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoAllowance();
            if (object.owner != null) {
                if (!$util.isObject(object.owner))
                    throw $TypeError(".proto.CryptoAllowance.owner: object expected");
                message.owner = $root.proto.AccountID.fromObject(object.owner, _depth + 1);
            }
            if (object.spender != null) {
                if (!$util.isObject(object.spender))
                    throw $TypeError(".proto.CryptoAllowance.spender: object expected");
                message.spender = $root.proto.AccountID.fromObject(object.spender, _depth + 1);
            }
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a CryptoAllowance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoAllowance
         * @static
         * @param {proto.CryptoAllowance} message CryptoAllowance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoAllowance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.owner = null;
                object.spender = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner"))
                object.owner = $root.proto.AccountID.toObject(message.owner, options, _depth + 1);
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender"))
                object.spender = $root.proto.AccountID.toObject(message.spender, options, _depth + 1);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            return object;
        };

        /**
         * Converts this CryptoAllowance to JSON.
         * @function toJSON
         * @memberof proto.CryptoAllowance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoAllowance.prototype.toJSON = function() {
            return CryptoAllowance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoAllowance
         * @function getTypeUrl
         * @memberof proto.CryptoAllowance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoAllowance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoAllowance";
        };

        return CryptoAllowance;
    })();

    proto.NftAllowance = (function() {

        /**
         * Properties of a NftAllowance.
         * @typedef {Object} proto.NftAllowance.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] A token identifier.<br/>
         * This identifies the type of token the `spender` is permitted to
         * transfer from the `owner`.
         * <p>
         * The identified token type MUST be a non-fungible/unique token.
         * @property {proto.AccountID.$Properties|null} [owner] An owner account identifier.<br/>
         * This is the account identifier of the account granting an allowance
         * for the `spender` to transfer tokens held by this account.
         * @property {proto.AccountID.$Properties|null} [spender] A spender account identifier.<br/>
         * This is the account identifier of the account permitted to transfer
         * tokens held by the `owner`.
         * @property {Array.<number|Long>|null} [serialNumbers] A list of token serial numbers.<br/>
         * The list of serial numbers that the spender is permitted to transfer.
         * <p>
         * The `owner` MUST currently hold each token identified in this list.
         * @property {google.protobuf.BoolValue.$Properties|null} [approvedForAll] A flag indicating this allowance applies to all tokens of the
         * specified (non-fungible/unique) type.
         * <p>
         * If true, the `spender` SHALL be permitted to transfer any or all
         * of the `owner`'s tokens of the specified token type.
         * This SHALL apply not only to currently owned tokens, but to all
         * such tokens acquired in the future, unless the
         * allowance is `delete`d.
         * @property {proto.AccountID.$Properties|null} [delegatingSpender] A spender-owner account identifier.<br/>
         * This account identifier identifies a `spender` for whom an existing
         * `approved_for_all` allowance was previously created. This enables
         * an account with such broad access to grant allowances to transfer
         * individual tokens from the original owner without involving that
         * original owner.
         * <p>
         * If this is set, the account identified MUST sign this transaction, but
         * the `owner` account MAY NOT sign this transaction.<br/>
         * If this is set, there MUST exist an active `approved_for_all`
         * allowance from the `owner` for the `delegating_spender` to transfer
         * all tokens of the type identified by the `tokenId` field.<br/>
         * If this value is set, the `approved_for_all` flag MUST be `false`.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NftAllowance.
         * @memberof proto
         * @interface INftAllowance
         * @augments proto.NftAllowance.$Properties
         * @deprecated Use proto.NftAllowance.$Properties instead.
         */

        /**
         * Shape of a NftAllowance.
         * @typedef {{
         *   tokenId?: proto.TokenID.$Shape|null;
         *   owner?: proto.AccountID.$Shape|null;
         *   spender?: proto.AccountID.$Shape|null;
         *   serialNumbers?: Array.<number|Long>|null;
         *   approvedForAll?: google.protobuf.BoolValue.$Shape|null;
         *   delegatingSpender?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.NftAllowance.$Shape
         */

        /**
         * Constructs a new NftAllowance.
         * @memberof proto
         * @classdesc An approved allowance of non-fungible tokens.<br/>
         * This type of allowance may permit transfers for one or more individual
         * unique tokens, or may permit transfers for all unique tokens of the
         * specified type.
         * 
         * If `owner` is not set, the effective `owner` SHALL be the `payer` for the
         * enclosing transaction.<br/>
         * The `spender` MUST be specified and MUST be a valid account.<br/>
         * If `approve_for_all` is set, then `serial_numbers` SHOULD be empty
         * and SHALL be ignored.
         * If `approve_for_all` is unset, then `serial_numbers` MUST NOT be empty.
         * @constructor
         * @param {proto.NftAllowance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NftAllowance = function (properties) {
            this.serialNumbers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.<br/>
         * This identifies the type of token the `spender` is permitted to
         * transfer from the `owner`.
         * <p>
         * The identified token type MUST be a non-fungible/unique token.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.NftAllowance
         * @instance
         */
        NftAllowance.prototype.tokenId = null;

        /**
         * An owner account identifier.<br/>
         * This is the account identifier of the account granting an allowance
         * for the `spender` to transfer tokens held by this account.
         * @member {proto.AccountID.$Properties|null|undefined} owner
         * @memberof proto.NftAllowance
         * @instance
         */
        NftAllowance.prototype.owner = null;

        /**
         * A spender account identifier.<br/>
         * This is the account identifier of the account permitted to transfer
         * tokens held by the `owner`.
         * @member {proto.AccountID.$Properties|null|undefined} spender
         * @memberof proto.NftAllowance
         * @instance
         */
        NftAllowance.prototype.spender = null;

        /**
         * A list of token serial numbers.<br/>
         * The list of serial numbers that the spender is permitted to transfer.
         * <p>
         * The `owner` MUST currently hold each token identified in this list.
         * @member {Array.<number|Long>} serialNumbers
         * @memberof proto.NftAllowance
         * @instance
         */
        NftAllowance.prototype.serialNumbers = $util.emptyArray;

        /**
         * A flag indicating this allowance applies to all tokens of the
         * specified (non-fungible/unique) type.
         * <p>
         * If true, the `spender` SHALL be permitted to transfer any or all
         * of the `owner`'s tokens of the specified token type.
         * This SHALL apply not only to currently owned tokens, but to all
         * such tokens acquired in the future, unless the
         * allowance is `delete`d.
         * @member {google.protobuf.BoolValue.$Properties|null|undefined} approvedForAll
         * @memberof proto.NftAllowance
         * @instance
         */
        NftAllowance.prototype.approvedForAll = null;

        /**
         * A spender-owner account identifier.<br/>
         * This account identifier identifies a `spender` for whom an existing
         * `approved_for_all` allowance was previously created. This enables
         * an account with such broad access to grant allowances to transfer
         * individual tokens from the original owner without involving that
         * original owner.
         * <p>
         * If this is set, the account identified MUST sign this transaction, but
         * the `owner` account MAY NOT sign this transaction.<br/>
         * If this is set, there MUST exist an active `approved_for_all`
         * allowance from the `owner` for the `delegating_spender` to transfer
         * all tokens of the type identified by the `tokenId` field.<br/>
         * If this value is set, the `approved_for_all` flag MUST be `false`.
         * @member {proto.AccountID.$Properties|null|undefined} delegatingSpender
         * @memberof proto.NftAllowance
         * @instance
         */
        NftAllowance.prototype.delegatingSpender = null;

        /**
         * Creates a new NftAllowance instance using the specified properties.
         * @function create
         * @memberof proto.NftAllowance
         * @static
         * @param {proto.NftAllowance.$Properties=} [properties] Properties to set
         * @returns {proto.NftAllowance} NftAllowance instance
         * @type {{
         *   (properties: proto.NftAllowance.$Shape): proto.NftAllowance & proto.NftAllowance.$Shape;
         *   (properties?: proto.NftAllowance.$Properties): proto.NftAllowance;
         * }}
         */
        NftAllowance.create = function(properties) {
            return new NftAllowance(properties);
        };

        /**
         * Encodes the specified NftAllowance message. Does not implicitly {@link proto.NftAllowance.verify|verify} messages.
         * @function encode
         * @memberof proto.NftAllowance
         * @static
         * @param {proto.NftAllowance.$Properties} message NftAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NftAllowance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner"))
                $root.proto.AccountID.encode(message.owner, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender"))
                $root.proto.AccountID.encode(message.spender, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.serialNumbers != null && message.serialNumbers.length)
                writer.uint32(/* id 4, wireType 2 =*/34).int64s(message.serialNumbers);
            if (message.approvedForAll != null && $Object.hasOwnProperty.call(message, "approvedForAll"))
                $root.google.protobuf.BoolValue.encode(message.approvedForAll, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.delegatingSpender != null && $Object.hasOwnProperty.call(message, "delegatingSpender"))
                $root.proto.AccountID.encode(message.delegatingSpender, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NftAllowance message, length delimited. Does not implicitly {@link proto.NftAllowance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NftAllowance
         * @static
         * @param {proto.NftAllowance.$Properties} message NftAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NftAllowance.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NftAllowance message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NftAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NftAllowance & proto.NftAllowance.$Shape} NftAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NftAllowance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NftAllowance();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.owner = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.owner);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.spender = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spender);
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.serialNumbers && message.serialNumbers.length))
                                message.serialNumbers = [];
                            reader.int64s(message.serialNumbers);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.serialNumbers && message.serialNumbers.length))
                            message.serialNumbers = [];
                        message.serialNumbers.push(reader.int64());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.approvedForAll = $root.google.protobuf.BoolValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.approvedForAll);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.delegatingSpender = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.delegatingSpender);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NftAllowance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NftAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NftAllowance & proto.NftAllowance.$Shape} NftAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NftAllowance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NftAllowance message.
         * @function verify
         * @memberof proto.NftAllowance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NftAllowance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner")) {
                let error = $root.proto.AccountID.verify(message.owner, _depth + 1);
                if (error)
                    return "owner." + error;
            }
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender")) {
                let error = $root.proto.AccountID.verify(message.spender, _depth + 1);
                if (error)
                    return "spender." + error;
            }
            if (message.serialNumbers != null && $Object.hasOwnProperty.call(message, "serialNumbers")) {
                if (!$Array.isArray(message.serialNumbers))
                    return "serialNumbers: array expected";
                for (let i = 0; i < message.serialNumbers.length; ++i)
                    if (!$util.isInteger(message.serialNumbers[i]) && !(message.serialNumbers[i] && $util.isInteger(message.serialNumbers[i].low) && $util.isInteger(message.serialNumbers[i].high)))
                        return "serialNumbers: integer|Long[] expected";
            }
            if (message.approvedForAll != null && $Object.hasOwnProperty.call(message, "approvedForAll")) {
                let error = $root.google.protobuf.BoolValue.verify(message.approvedForAll, _depth + 1);
                if (error)
                    return "approvedForAll." + error;
            }
            if (message.delegatingSpender != null && $Object.hasOwnProperty.call(message, "delegatingSpender")) {
                let error = $root.proto.AccountID.verify(message.delegatingSpender, _depth + 1);
                if (error)
                    return "delegatingSpender." + error;
            }
            return null;
        };

        /**
         * Creates a NftAllowance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NftAllowance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NftAllowance} NftAllowance
         */
        NftAllowance.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NftAllowance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NftAllowance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NftAllowance();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.NftAllowance.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.owner != null) {
                if (!$util.isObject(object.owner))
                    throw $TypeError(".proto.NftAllowance.owner: object expected");
                message.owner = $root.proto.AccountID.fromObject(object.owner, _depth + 1);
            }
            if (object.spender != null) {
                if (!$util.isObject(object.spender))
                    throw $TypeError(".proto.NftAllowance.spender: object expected");
                message.spender = $root.proto.AccountID.fromObject(object.spender, _depth + 1);
            }
            if (object.serialNumbers) {
                if (!$Array.isArray(object.serialNumbers))
                    throw $TypeError(".proto.NftAllowance.serialNumbers: array expected");
                message.serialNumbers = $Array(object.serialNumbers.length);
                for (let i = 0; i < object.serialNumbers.length; ++i)
                    if ($util.Long)
                        message.serialNumbers[i] = $util.Long.fromValue(object.serialNumbers[i], false);
                    else if (typeof object.serialNumbers[i] === "string")
                        message.serialNumbers[i] = $parseInt(object.serialNumbers[i], 10);
                    else if (typeof object.serialNumbers[i] === "number")
                        message.serialNumbers[i] = object.serialNumbers[i];
                    else if (typeof object.serialNumbers[i] === "object")
                        message.serialNumbers[i] = new $util.LongBits(object.serialNumbers[i].low >>> 0, object.serialNumbers[i].high >>> 0).toNumber();
            }
            if (object.approvedForAll != null) {
                if (!$util.isObject(object.approvedForAll))
                    throw $TypeError(".proto.NftAllowance.approvedForAll: object expected");
                message.approvedForAll = $root.google.protobuf.BoolValue.fromObject(object.approvedForAll, _depth + 1);
            }
            if (object.delegatingSpender != null) {
                if (!$util.isObject(object.delegatingSpender))
                    throw $TypeError(".proto.NftAllowance.delegatingSpender: object expected");
                message.delegatingSpender = $root.proto.AccountID.fromObject(object.delegatingSpender, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a NftAllowance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NftAllowance
         * @static
         * @param {proto.NftAllowance} message NftAllowance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NftAllowance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.serialNumbers = [];
            if (options.defaults) {
                object.tokenId = null;
                object.owner = null;
                object.spender = null;
                object.approvedForAll = null;
                object.delegatingSpender = null;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner"))
                object.owner = $root.proto.AccountID.toObject(message.owner, options, _depth + 1);
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender"))
                object.spender = $root.proto.AccountID.toObject(message.spender, options, _depth + 1);
            if (message.serialNumbers && message.serialNumbers.length) {
                object.serialNumbers = $Array(message.serialNumbers.length);
                for (let j = 0; j < message.serialNumbers.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.serialNumbers[j] = typeof message.serialNumbers[j] === "number" ? $BigInt(message.serialNumbers[j]) : $util.Long.fromBits(message.serialNumbers[j].low >>> 0, message.serialNumbers[j].high >>> 0, false).toBigInt();
                    else if (typeof message.serialNumbers[j] === "number")
                        object.serialNumbers[j] = options.longs === $String ? $String(message.serialNumbers[j]) : message.serialNumbers[j];
                    else
                        object.serialNumbers[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.serialNumbers[j]) : options.longs === $Number ? new $util.LongBits(message.serialNumbers[j].low >>> 0, message.serialNumbers[j].high >>> 0).toNumber() : message.serialNumbers[j];
            }
            if (message.approvedForAll != null && $Object.hasOwnProperty.call(message, "approvedForAll"))
                object.approvedForAll = $root.google.protobuf.BoolValue.toObject(message.approvedForAll, options, _depth + 1);
            if (message.delegatingSpender != null && $Object.hasOwnProperty.call(message, "delegatingSpender"))
                object.delegatingSpender = $root.proto.AccountID.toObject(message.delegatingSpender, options, _depth + 1);
            return object;
        };

        /**
         * Converts this NftAllowance to JSON.
         * @function toJSON
         * @memberof proto.NftAllowance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NftAllowance.prototype.toJSON = function() {
            return NftAllowance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NftAllowance
         * @function getTypeUrl
         * @memberof proto.NftAllowance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NftAllowance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NftAllowance";
        };

        return NftAllowance;
    })();

    proto.TokenAllowance = (function() {

        /**
         * Properties of a TokenAllowance.
         * @typedef {Object} proto.TokenAllowance.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] A token identifier.<br/>
         * This identifies the type of token the `spender` is permitted to
         * transfer from the `owner`.
         * <p>
         * The identified token type MUST be a fungible/common token.
         * @property {proto.AccountID.$Properties|null} [owner] An owner account identifier.<br/>
         * This is the account identifier of the account granting an allowance
         * for the `spender` to transfer tokens held by this account.
         * @property {proto.AccountID.$Properties|null} [spender] A spender account identifier.<br/>
         * This is the account identifier of the account permitted to transfer
         * tokens held by the `owner`.
         * @property {number|Long|null} [amount] An amount of fractional tokens (10<sup>-decimals</sup> tokens).<br/>
         * This is the amount of tokens held by the `owner` that the
         * `spender` is permitted to transfer.
         * <p>
         * This value MUST be a whole number.<br/>
         * This value MUST be greater than 0 to create a new allowance.<br/>
         * This value MAY be exactly `0` to _remove_ an existing allowance.<br/>
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenAllowance.
         * @memberof proto
         * @interface ITokenAllowance
         * @augments proto.TokenAllowance.$Properties
         * @deprecated Use proto.TokenAllowance.$Properties instead.
         */

        /**
         * Shape of a TokenAllowance.
         * @typedef {{
         *   tokenId?: proto.TokenID.$Shape|null;
         *   owner?: proto.AccountID.$Shape|null;
         *   spender?: proto.AccountID.$Shape|null;
         *   amount?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenAllowance.$Shape
         */

        /**
         * Constructs a new TokenAllowance.
         * @memberof proto
         * @classdesc An approved allowance of fungible/common token transfers.
         * This message specifies one allowance for a single, unique, combination
         * of token, owner, spender, and amount.
         * 
         * If `owner` is not set, the effective `owner` SHALL be the `payer` for the
         * enclosing transaction.<br/>
         * The `tokenId` MUST be specified and MUST be a valid
         * fungible/common token type.<br/>
         * The `spender` MUST be specified and MUST be a valid account.<br/>
         * The `amount` MUST be a whole number, and SHOULD be greater than `0` unless
         * this allowance is intended to _remove_ a previously approved allowance.
         * @constructor
         * @param {proto.TokenAllowance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenAllowance = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.<br/>
         * This identifies the type of token the `spender` is permitted to
         * transfer from the `owner`.
         * <p>
         * The identified token type MUST be a fungible/common token.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.TokenAllowance
         * @instance
         */
        TokenAllowance.prototype.tokenId = null;

        /**
         * An owner account identifier.<br/>
         * This is the account identifier of the account granting an allowance
         * for the `spender` to transfer tokens held by this account.
         * @member {proto.AccountID.$Properties|null|undefined} owner
         * @memberof proto.TokenAllowance
         * @instance
         */
        TokenAllowance.prototype.owner = null;

        /**
         * A spender account identifier.<br/>
         * This is the account identifier of the account permitted to transfer
         * tokens held by the `owner`.
         * @member {proto.AccountID.$Properties|null|undefined} spender
         * @memberof proto.TokenAllowance
         * @instance
         */
        TokenAllowance.prototype.spender = null;

        /**
         * An amount of fractional tokens (10<sup>-decimals</sup> tokens).<br/>
         * This is the amount of tokens held by the `owner` that the
         * `spender` is permitted to transfer.
         * <p>
         * This value MUST be a whole number.<br/>
         * This value MUST be greater than 0 to create a new allowance.<br/>
         * This value MAY be exactly `0` to _remove_ an existing allowance.<br/>
         * @member {number|Long} amount
         * @memberof proto.TokenAllowance
         * @instance
         */
        TokenAllowance.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new TokenAllowance instance using the specified properties.
         * @function create
         * @memberof proto.TokenAllowance
         * @static
         * @param {proto.TokenAllowance.$Properties=} [properties] Properties to set
         * @returns {proto.TokenAllowance} TokenAllowance instance
         * @type {{
         *   (properties: proto.TokenAllowance.$Shape): proto.TokenAllowance & proto.TokenAllowance.$Shape;
         *   (properties?: proto.TokenAllowance.$Properties): proto.TokenAllowance;
         * }}
         */
        TokenAllowance.create = function(properties) {
            return new TokenAllowance(properties);
        };

        /**
         * Encodes the specified TokenAllowance message. Does not implicitly {@link proto.TokenAllowance.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenAllowance
         * @static
         * @param {proto.TokenAllowance.$Properties} message TokenAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenAllowance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner"))
                $root.proto.AccountID.encode(message.owner, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender"))
                $root.proto.AccountID.encode(message.spender, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.amount);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenAllowance message, length delimited. Does not implicitly {@link proto.TokenAllowance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenAllowance
         * @static
         * @param {proto.TokenAllowance.$Properties} message TokenAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenAllowance.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenAllowance message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenAllowance & proto.TokenAllowance.$Shape} TokenAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenAllowance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenAllowance();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.owner = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.owner);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.spender = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spender);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenAllowance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenAllowance & proto.TokenAllowance.$Shape} TokenAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenAllowance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenAllowance message.
         * @function verify
         * @memberof proto.TokenAllowance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenAllowance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner")) {
                let error = $root.proto.AccountID.verify(message.owner, _depth + 1);
                if (error)
                    return "owner." + error;
            }
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender")) {
                let error = $root.proto.AccountID.verify(message.spender, _depth + 1);
                if (error)
                    return "spender." + error;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            return null;
        };

        /**
         * Creates a TokenAllowance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenAllowance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenAllowance} TokenAllowance
         */
        TokenAllowance.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenAllowance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenAllowance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenAllowance();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.TokenAllowance.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.owner != null) {
                if (!$util.isObject(object.owner))
                    throw $TypeError(".proto.TokenAllowance.owner: object expected");
                message.owner = $root.proto.AccountID.fromObject(object.owner, _depth + 1);
            }
            if (object.spender != null) {
                if (!$util.isObject(object.spender))
                    throw $TypeError(".proto.TokenAllowance.spender: object expected");
                message.spender = $root.proto.AccountID.fromObject(object.spender, _depth + 1);
            }
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a TokenAllowance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenAllowance
         * @static
         * @param {proto.TokenAllowance} message TokenAllowance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenAllowance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.tokenId = null;
                object.owner = null;
                object.spender = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner"))
                object.owner = $root.proto.AccountID.toObject(message.owner, options, _depth + 1);
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender"))
                object.spender = $root.proto.AccountID.toObject(message.spender, options, _depth + 1);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            return object;
        };

        /**
         * Converts this TokenAllowance to JSON.
         * @function toJSON
         * @memberof proto.TokenAllowance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenAllowance.prototype.toJSON = function() {
            return TokenAllowance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenAllowance
         * @function getTypeUrl
         * @memberof proto.TokenAllowance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenAllowance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenAllowance";
        };

        return TokenAllowance;
    })();

    proto.CryptoDeleteAllowanceTransactionBody = (function() {

        /**
         * Properties of a CryptoDeleteAllowanceTransactionBody.
         * @typedef {Object} proto.CryptoDeleteAllowanceTransactionBody.$Properties
         * @property {Array.<proto.NftRemoveAllowance.$Properties>|null} [nftAllowances] List of non-fungible/unique token allowances to remove.
         * <p>
         * This list MUST NOT be empty.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoDeleteAllowanceTransactionBody.
         * @memberof proto
         * @interface ICryptoDeleteAllowanceTransactionBody
         * @augments proto.CryptoDeleteAllowanceTransactionBody.$Properties
         * @deprecated Use proto.CryptoDeleteAllowanceTransactionBody.$Properties instead.
         */

        /**
         * Shape of a CryptoDeleteAllowanceTransactionBody.
         * @typedef {{
         *   nftAllowances?: Array.<proto.NftRemoveAllowance.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoDeleteAllowanceTransactionBody.$Shape
         */

        /**
         * Constructs a new CryptoDeleteAllowanceTransactionBody.
         * @memberof proto
         * @classdesc Delete one or more allowances.<br/>
         * Given one or more, previously approved, allowances for non-fungible/unique
         * tokens to be transferred by a spending account from an owning account;
         * this transaction removes a specified set of those allowances.
         * 
         * The owner account for each listed allowance MUST sign this transaction.<br/>
         * Allowances for HBAR cannot be removed with this transaction. The owner
         * account MUST submit a new `cryptoApproveAllowance` transaction with the
         * amount set to `0` to "remove" that allowance.<br/>
         * Allowances for fungible/common tokens cannot be removed with this
         * transaction. The owner account MUST submit a new `cryptoApproveAllowance`
         * transaction with the amount set to `0` to "remove" that allowance.<br/>
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.CryptoDeleteAllowanceTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoDeleteAllowanceTransactionBody = function (properties) {
            this.nftAllowances = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * List of non-fungible/unique token allowances to remove.
         * <p>
         * This list MUST NOT be empty.
         * @member {Array.<proto.NftRemoveAllowance.$Properties>} nftAllowances
         * @memberof proto.CryptoDeleteAllowanceTransactionBody
         * @instance
         */
        CryptoDeleteAllowanceTransactionBody.prototype.nftAllowances = $util.emptyArray;

        /**
         * Creates a new CryptoDeleteAllowanceTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.CryptoDeleteAllowanceTransactionBody
         * @static
         * @param {proto.CryptoDeleteAllowanceTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoDeleteAllowanceTransactionBody} CryptoDeleteAllowanceTransactionBody instance
         * @type {{
         *   (properties: proto.CryptoDeleteAllowanceTransactionBody.$Shape): proto.CryptoDeleteAllowanceTransactionBody & proto.CryptoDeleteAllowanceTransactionBody.$Shape;
         *   (properties?: proto.CryptoDeleteAllowanceTransactionBody.$Properties): proto.CryptoDeleteAllowanceTransactionBody;
         * }}
         */
        CryptoDeleteAllowanceTransactionBody.create = function(properties) {
            return new CryptoDeleteAllowanceTransactionBody(properties);
        };

        /**
         * Encodes the specified CryptoDeleteAllowanceTransactionBody message. Does not implicitly {@link proto.CryptoDeleteAllowanceTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoDeleteAllowanceTransactionBody
         * @static
         * @param {proto.CryptoDeleteAllowanceTransactionBody.$Properties} message CryptoDeleteAllowanceTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoDeleteAllowanceTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nftAllowances != null && message.nftAllowances.length)
                for (let i = 0; i < message.nftAllowances.length; ++i)
                    $root.proto.NftRemoveAllowance.encode(message.nftAllowances[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoDeleteAllowanceTransactionBody message, length delimited. Does not implicitly {@link proto.CryptoDeleteAllowanceTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoDeleteAllowanceTransactionBody
         * @static
         * @param {proto.CryptoDeleteAllowanceTransactionBody.$Properties} message CryptoDeleteAllowanceTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoDeleteAllowanceTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoDeleteAllowanceTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoDeleteAllowanceTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoDeleteAllowanceTransactionBody & proto.CryptoDeleteAllowanceTransactionBody.$Shape} CryptoDeleteAllowanceTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoDeleteAllowanceTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoDeleteAllowanceTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.nftAllowances && message.nftAllowances.length))
                            message.nftAllowances = [];
                        message.nftAllowances.push($root.proto.NftRemoveAllowance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoDeleteAllowanceTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoDeleteAllowanceTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoDeleteAllowanceTransactionBody & proto.CryptoDeleteAllowanceTransactionBody.$Shape} CryptoDeleteAllowanceTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoDeleteAllowanceTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoDeleteAllowanceTransactionBody message.
         * @function verify
         * @memberof proto.CryptoDeleteAllowanceTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoDeleteAllowanceTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nftAllowances != null && $Object.hasOwnProperty.call(message, "nftAllowances")) {
                if (!$Array.isArray(message.nftAllowances))
                    return "nftAllowances: array expected";
                for (let i = 0; i < message.nftAllowances.length; ++i) {
                    let error = $root.proto.NftRemoveAllowance.verify(message.nftAllowances[i], _depth + 1);
                    if (error)
                        return "nftAllowances." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CryptoDeleteAllowanceTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoDeleteAllowanceTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoDeleteAllowanceTransactionBody} CryptoDeleteAllowanceTransactionBody
         */
        CryptoDeleteAllowanceTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoDeleteAllowanceTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoDeleteAllowanceTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoDeleteAllowanceTransactionBody();
            if (object.nftAllowances) {
                if (!$Array.isArray(object.nftAllowances))
                    throw $TypeError(".proto.CryptoDeleteAllowanceTransactionBody.nftAllowances: array expected");
                message.nftAllowances = $Array(object.nftAllowances.length);
                for (let i = 0; i < object.nftAllowances.length; ++i) {
                    if (!$util.isObject(object.nftAllowances[i]))
                        throw $TypeError(".proto.CryptoDeleteAllowanceTransactionBody.nftAllowances: object expected");
                    message.nftAllowances[i] = $root.proto.NftRemoveAllowance.fromObject(object.nftAllowances[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoDeleteAllowanceTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoDeleteAllowanceTransactionBody
         * @static
         * @param {proto.CryptoDeleteAllowanceTransactionBody} message CryptoDeleteAllowanceTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoDeleteAllowanceTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.nftAllowances = [];
            if (message.nftAllowances && message.nftAllowances.length) {
                object.nftAllowances = $Array(message.nftAllowances.length);
                for (let j = 0; j < message.nftAllowances.length; ++j)
                    object.nftAllowances[j] = $root.proto.NftRemoveAllowance.toObject(message.nftAllowances[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this CryptoDeleteAllowanceTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.CryptoDeleteAllowanceTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoDeleteAllowanceTransactionBody.prototype.toJSON = function() {
            return CryptoDeleteAllowanceTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoDeleteAllowanceTransactionBody
         * @function getTypeUrl
         * @memberof proto.CryptoDeleteAllowanceTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoDeleteAllowanceTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoDeleteAllowanceTransactionBody";
        };

        return CryptoDeleteAllowanceTransactionBody;
    })();

    proto.NftRemoveAllowance = (function() {

        /**
         * Properties of a NftRemoveAllowance.
         * @typedef {Object} proto.NftRemoveAllowance.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] A token identifier.
         * <p>
         * This MUST be a valid token identifier for a non-fungible/unique
         * token type.
         * @property {proto.AccountID.$Properties|null} [owner] An `owner` account identifier.
         * <p>
         * This account MUST sign the transaction containing this message.
         * @property {Array.<number|Long>|null} [serialNumbers] The list of serial numbers to remove allowances from.
         * <p>
         * This list MUST NOT be empty.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NftRemoveAllowance.
         * @memberof proto
         * @interface INftRemoveAllowance
         * @augments proto.NftRemoveAllowance.$Properties
         * @deprecated Use proto.NftRemoveAllowance.$Properties instead.
         */

        /**
         * Shape of a NftRemoveAllowance.
         * @typedef {{
         *   tokenId?: proto.TokenID.$Shape|null;
         *   owner?: proto.AccountID.$Shape|null;
         *   serialNumbers?: Array.<number|Long>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.NftRemoveAllowance.$Shape
         */

        /**
         * Constructs a new NftRemoveAllowance.
         * @memberof proto
         * @classdesc A single allowance for one non-fungible/unique token.
         * This is specific to removal, and the allowance is identified for that
         * specific purpose.
         * 
         * All fields in this message are REQUIRED.
         * The `serial_numbers` list MUST NOT be empty.
         * The combination of field values in this message MUST match existing
         * allowances for one or more individual non-fungible/unique tokens.
         * 
         * ### Removing an allowance that is `approve_for_all`
         * To remove an allowance that has set the `approve_for_all` flag, the
         * `owner` account must first _approve_ a **new** allowance for a specific
         * serial number using a `cryptoApproveAllowance`, and then, if desired,
         * that newly approved allowance to a specific serial number may be
         * deleted in a separate `cryptoDeleteAllowance` transaction.
         * @constructor
         * @param {proto.NftRemoveAllowance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NftRemoveAllowance = function (properties) {
            this.serialNumbers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * This MUST be a valid token identifier for a non-fungible/unique
         * token type.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.NftRemoveAllowance
         * @instance
         */
        NftRemoveAllowance.prototype.tokenId = null;

        /**
         * An `owner` account identifier.
         * <p>
         * This account MUST sign the transaction containing this message.
         * @member {proto.AccountID.$Properties|null|undefined} owner
         * @memberof proto.NftRemoveAllowance
         * @instance
         */
        NftRemoveAllowance.prototype.owner = null;

        /**
         * The list of serial numbers to remove allowances from.
         * <p>
         * This list MUST NOT be empty.
         * @member {Array.<number|Long>} serialNumbers
         * @memberof proto.NftRemoveAllowance
         * @instance
         */
        NftRemoveAllowance.prototype.serialNumbers = $util.emptyArray;

        /**
         * Creates a new NftRemoveAllowance instance using the specified properties.
         * @function create
         * @memberof proto.NftRemoveAllowance
         * @static
         * @param {proto.NftRemoveAllowance.$Properties=} [properties] Properties to set
         * @returns {proto.NftRemoveAllowance} NftRemoveAllowance instance
         * @type {{
         *   (properties: proto.NftRemoveAllowance.$Shape): proto.NftRemoveAllowance & proto.NftRemoveAllowance.$Shape;
         *   (properties?: proto.NftRemoveAllowance.$Properties): proto.NftRemoveAllowance;
         * }}
         */
        NftRemoveAllowance.create = function(properties) {
            return new NftRemoveAllowance(properties);
        };

        /**
         * Encodes the specified NftRemoveAllowance message. Does not implicitly {@link proto.NftRemoveAllowance.verify|verify} messages.
         * @function encode
         * @memberof proto.NftRemoveAllowance
         * @static
         * @param {proto.NftRemoveAllowance.$Properties} message NftRemoveAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NftRemoveAllowance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner"))
                $root.proto.AccountID.encode(message.owner, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.serialNumbers != null && message.serialNumbers.length)
                writer.uint32(/* id 3, wireType 2 =*/26).int64s(message.serialNumbers);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NftRemoveAllowance message, length delimited. Does not implicitly {@link proto.NftRemoveAllowance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NftRemoveAllowance
         * @static
         * @param {proto.NftRemoveAllowance.$Properties} message NftRemoveAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NftRemoveAllowance.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NftRemoveAllowance message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NftRemoveAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NftRemoveAllowance & proto.NftRemoveAllowance.$Shape} NftRemoveAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NftRemoveAllowance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NftRemoveAllowance();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.owner = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.owner);
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.serialNumbers && message.serialNumbers.length))
                                message.serialNumbers = [];
                            reader.int64s(message.serialNumbers);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.serialNumbers && message.serialNumbers.length))
                            message.serialNumbers = [];
                        message.serialNumbers.push(reader.int64());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NftRemoveAllowance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NftRemoveAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NftRemoveAllowance & proto.NftRemoveAllowance.$Shape} NftRemoveAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NftRemoveAllowance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NftRemoveAllowance message.
         * @function verify
         * @memberof proto.NftRemoveAllowance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NftRemoveAllowance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner")) {
                let error = $root.proto.AccountID.verify(message.owner, _depth + 1);
                if (error)
                    return "owner." + error;
            }
            if (message.serialNumbers != null && $Object.hasOwnProperty.call(message, "serialNumbers")) {
                if (!$Array.isArray(message.serialNumbers))
                    return "serialNumbers: array expected";
                for (let i = 0; i < message.serialNumbers.length; ++i)
                    if (!$util.isInteger(message.serialNumbers[i]) && !(message.serialNumbers[i] && $util.isInteger(message.serialNumbers[i].low) && $util.isInteger(message.serialNumbers[i].high)))
                        return "serialNumbers: integer|Long[] expected";
            }
            return null;
        };

        /**
         * Creates a NftRemoveAllowance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NftRemoveAllowance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NftRemoveAllowance} NftRemoveAllowance
         */
        NftRemoveAllowance.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NftRemoveAllowance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NftRemoveAllowance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NftRemoveAllowance();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.NftRemoveAllowance.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.owner != null) {
                if (!$util.isObject(object.owner))
                    throw $TypeError(".proto.NftRemoveAllowance.owner: object expected");
                message.owner = $root.proto.AccountID.fromObject(object.owner, _depth + 1);
            }
            if (object.serialNumbers) {
                if (!$Array.isArray(object.serialNumbers))
                    throw $TypeError(".proto.NftRemoveAllowance.serialNumbers: array expected");
                message.serialNumbers = $Array(object.serialNumbers.length);
                for (let i = 0; i < object.serialNumbers.length; ++i)
                    if ($util.Long)
                        message.serialNumbers[i] = $util.Long.fromValue(object.serialNumbers[i], false);
                    else if (typeof object.serialNumbers[i] === "string")
                        message.serialNumbers[i] = $parseInt(object.serialNumbers[i], 10);
                    else if (typeof object.serialNumbers[i] === "number")
                        message.serialNumbers[i] = object.serialNumbers[i];
                    else if (typeof object.serialNumbers[i] === "object")
                        message.serialNumbers[i] = new $util.LongBits(object.serialNumbers[i].low >>> 0, object.serialNumbers[i].high >>> 0).toNumber();
            }
            return message;
        };

        /**
         * Creates a plain object from a NftRemoveAllowance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NftRemoveAllowance
         * @static
         * @param {proto.NftRemoveAllowance} message NftRemoveAllowance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NftRemoveAllowance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.serialNumbers = [];
            if (options.defaults) {
                object.tokenId = null;
                object.owner = null;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner"))
                object.owner = $root.proto.AccountID.toObject(message.owner, options, _depth + 1);
            if (message.serialNumbers && message.serialNumbers.length) {
                object.serialNumbers = $Array(message.serialNumbers.length);
                for (let j = 0; j < message.serialNumbers.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.serialNumbers[j] = typeof message.serialNumbers[j] === "number" ? $BigInt(message.serialNumbers[j]) : $util.Long.fromBits(message.serialNumbers[j].low >>> 0, message.serialNumbers[j].high >>> 0, false).toBigInt();
                    else if (typeof message.serialNumbers[j] === "number")
                        object.serialNumbers[j] = options.longs === $String ? $String(message.serialNumbers[j]) : message.serialNumbers[j];
                    else
                        object.serialNumbers[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.serialNumbers[j]) : options.longs === $Number ? new $util.LongBits(message.serialNumbers[j].low >>> 0, message.serialNumbers[j].high >>> 0).toNumber() : message.serialNumbers[j];
            }
            return object;
        };

        /**
         * Converts this NftRemoveAllowance to JSON.
         * @function toJSON
         * @memberof proto.NftRemoveAllowance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NftRemoveAllowance.prototype.toJSON = function() {
            return NftRemoveAllowance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NftRemoveAllowance
         * @function getTypeUrl
         * @memberof proto.NftRemoveAllowance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NftRemoveAllowance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NftRemoveAllowance";
        };

        return NftRemoveAllowance;
    })();

    proto.EthereumTransactionBody = (function() {

        /**
         * Properties of an EthereumTransactionBody.
         * @typedef {Object} proto.EthereumTransactionBody.$Properties
         * @property {Uint8Array|null} [ethereumData] The raw Ethereum transaction data.
         * <p>
         * This transaction MUST be RLP encoded.<br/>
         * This SHALL be the complete transaction data unless the `call_data`
         * field is set.<br/>
         * If `call_data` is set, this field SHALL be modified to replace the
         * `callData` element with the content of the referenced file.<br/>
         * The transaction signature SHALL be validated after `callData` is
         * complete, if necessary.
         * @property {proto.FileID.$Properties|null} [callData] The `callData` for the Ethereum transaction.
         * <p>
         * If this field is set, the data in the `ethereum_data` field SHALL be
         * re-written to replace the `callData` element with the contents of this
         * file at time of execution.<br/>
         * The Ethereum transaction MUST be "rehydrated" with this modified
         * `callData` before signature validation MAY be performed.
         * @property {number|Long|null} [maxGasAllowance] A maximum amount of "gas" offered to pay the Ethereum transaction costs.
         * <p>
         * This gas offered is in addition to any gas supplied with the Ethereum
         * transaction as declared in the `ethereum_data`.<br/>
         * In most circumstances the account with an alias matching the public
         * key available from the Ethereum transaction signature offers sufficient
         * gas to power the transaction, but in some cases it MAY be desirable
         * for the account submitting this transaction to either supplement
         * or entirely fund the transaction cost.<br/>
         * The amount of gas offered here SHALL be used to pay for
         * transaction costs _in excess_ of any gas offered within
         * the Ethereum transaction.<br/>
         * If the gas offered within the Ethereum transaction is sufficient
         * for all costs, the gas offered in this field SHALL NOT be expended.<br/>
         * Regardless of actual transaction cost, the payer for this transaction
         * SHALL NOT be charged more gas than the amount offered here.<br/>
         * If the sum of both gas amounts is not sufficient to pay for the
         * transaction, the entire total amount of gas offered SHALL be expended,
         * the transaction SHALL fail, and the response code `INSUFFICIENT_GAS`
         * SHALL be set.<br/>
         * If any amount of gas is charged to the payer of this transaction,
         * at least 80% of the value offered in this field SHALL be charged
         * as a minimum fee.<br/>
         * If the amount of gas authorized in the Ethereum transaction data is `0`,
         * then the payer of this transaction SHALL be charged the entire cost of
         * the Ethereum transaction, subject to the limit set in this field.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an EthereumTransactionBody.
         * @memberof proto
         * @interface IEthereumTransactionBody
         * @augments proto.EthereumTransactionBody.$Properties
         * @deprecated Use proto.EthereumTransactionBody.$Properties instead.
         */

        /**
         * Shape of an EthereumTransactionBody.
         * @typedef {proto.EthereumTransactionBody.$Properties} proto.EthereumTransactionBody.$Shape
         */

        /**
         * Constructs a new EthereumTransactionBody.
         * @memberof proto
         * @classdesc A transaction in Ethereum format.<br/>
         * Make an Ethereum transaction "call" with all data in Ethereum formats,
         * including the contract alias. Call data may be in the transaction, or
         * stored within an Hedera File.
         * 
         * The caller MAY offer additional gas above what is offered in the call data,
         * but MAY be charged up to 80% of that value if the amount required is less
         * than this "floor" amount.
         * 
         * ### Block Stream Effects
         * An `EthereumOutput` message SHALL be emitted for each transaction.
         * @constructor
         * @param {proto.EthereumTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const EthereumTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The raw Ethereum transaction data.
         * <p>
         * This transaction MUST be RLP encoded.<br/>
         * This SHALL be the complete transaction data unless the `call_data`
         * field is set.<br/>
         * If `call_data` is set, this field SHALL be modified to replace the
         * `callData` element with the content of the referenced file.<br/>
         * The transaction signature SHALL be validated after `callData` is
         * complete, if necessary.
         * @member {Uint8Array} ethereumData
         * @memberof proto.EthereumTransactionBody
         * @instance
         */
        EthereumTransactionBody.prototype.ethereumData = $util.newBuffer([]);

        /**
         * The `callData` for the Ethereum transaction.
         * <p>
         * If this field is set, the data in the `ethereum_data` field SHALL be
         * re-written to replace the `callData` element with the contents of this
         * file at time of execution.<br/>
         * The Ethereum transaction MUST be "rehydrated" with this modified
         * `callData` before signature validation MAY be performed.
         * @member {proto.FileID.$Properties|null|undefined} callData
         * @memberof proto.EthereumTransactionBody
         * @instance
         */
        EthereumTransactionBody.prototype.callData = null;

        /**
         * A maximum amount of "gas" offered to pay the Ethereum transaction costs.
         * <p>
         * This gas offered is in addition to any gas supplied with the Ethereum
         * transaction as declared in the `ethereum_data`.<br/>
         * In most circumstances the account with an alias matching the public
         * key available from the Ethereum transaction signature offers sufficient
         * gas to power the transaction, but in some cases it MAY be desirable
         * for the account submitting this transaction to either supplement
         * or entirely fund the transaction cost.<br/>
         * The amount of gas offered here SHALL be used to pay for
         * transaction costs _in excess_ of any gas offered within
         * the Ethereum transaction.<br/>
         * If the gas offered within the Ethereum transaction is sufficient
         * for all costs, the gas offered in this field SHALL NOT be expended.<br/>
         * Regardless of actual transaction cost, the payer for this transaction
         * SHALL NOT be charged more gas than the amount offered here.<br/>
         * If the sum of both gas amounts is not sufficient to pay for the
         * transaction, the entire total amount of gas offered SHALL be expended,
         * the transaction SHALL fail, and the response code `INSUFFICIENT_GAS`
         * SHALL be set.<br/>
         * If any amount of gas is charged to the payer of this transaction,
         * at least 80% of the value offered in this field SHALL be charged
         * as a minimum fee.<br/>
         * If the amount of gas authorized in the Ethereum transaction data is `0`,
         * then the payer of this transaction SHALL be charged the entire cost of
         * the Ethereum transaction, subject to the limit set in this field.
         * @member {number|Long} maxGasAllowance
         * @memberof proto.EthereumTransactionBody
         * @instance
         */
        EthereumTransactionBody.prototype.maxGasAllowance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new EthereumTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.EthereumTransactionBody
         * @static
         * @param {proto.EthereumTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.EthereumTransactionBody} EthereumTransactionBody instance
         * @type {{
         *   (properties: proto.EthereumTransactionBody.$Shape): proto.EthereumTransactionBody & proto.EthereumTransactionBody.$Shape;
         *   (properties?: proto.EthereumTransactionBody.$Properties): proto.EthereumTransactionBody;
         * }}
         */
        EthereumTransactionBody.create = function(properties) {
            return new EthereumTransactionBody(properties);
        };

        /**
         * Encodes the specified EthereumTransactionBody message. Does not implicitly {@link proto.EthereumTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.EthereumTransactionBody
         * @static
         * @param {proto.EthereumTransactionBody.$Properties} message EthereumTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EthereumTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.ethereumData != null && $Object.hasOwnProperty.call(message, "ethereumData") && message.ethereumData.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ethereumData);
            if (message.callData != null && $Object.hasOwnProperty.call(message, "callData"))
                $root.proto.FileID.encode(message.callData, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.maxGasAllowance != null && $Object.hasOwnProperty.call(message, "maxGasAllowance") && (typeof message.maxGasAllowance === "object" ? message.maxGasAllowance.low || message.maxGasAllowance.high : message.maxGasAllowance !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.maxGasAllowance);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EthereumTransactionBody message, length delimited. Does not implicitly {@link proto.EthereumTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EthereumTransactionBody
         * @static
         * @param {proto.EthereumTransactionBody.$Properties} message EthereumTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EthereumTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an EthereumTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EthereumTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EthereumTransactionBody & proto.EthereumTransactionBody.$Shape} EthereumTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EthereumTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.EthereumTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.ethereumData = value;
                        else
                            delete message.ethereumData;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.callData = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.callData);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maxGasAllowance = value;
                        else
                            delete message.maxGasAllowance;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EthereumTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EthereumTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EthereumTransactionBody & proto.EthereumTransactionBody.$Shape} EthereumTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EthereumTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EthereumTransactionBody message.
         * @function verify
         * @memberof proto.EthereumTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EthereumTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.ethereumData != null && $Object.hasOwnProperty.call(message, "ethereumData"))
                if (!(message.ethereumData && typeof message.ethereumData.length === "number" || $util.isString(message.ethereumData)))
                    return "ethereumData: buffer expected";
            if (message.callData != null && $Object.hasOwnProperty.call(message, "callData")) {
                let error = $root.proto.FileID.verify(message.callData, _depth + 1);
                if (error)
                    return "callData." + error;
            }
            if (message.maxGasAllowance != null && $Object.hasOwnProperty.call(message, "maxGasAllowance"))
                if (!$util.isInteger(message.maxGasAllowance) && !(message.maxGasAllowance && $util.isInteger(message.maxGasAllowance.low) && $util.isInteger(message.maxGasAllowance.high)))
                    return "maxGasAllowance: integer|Long expected";
            return null;
        };

        /**
         * Creates an EthereumTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EthereumTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EthereumTransactionBody} EthereumTransactionBody
         */
        EthereumTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.EthereumTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.EthereumTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.EthereumTransactionBody();
            if (object.ethereumData != null)
                if (object.ethereumData.length)
                    if (typeof object.ethereumData === "string")
                        $util.base64.decode(object.ethereumData, message.ethereumData = $util.newBuffer($util.base64.length(object.ethereumData)), 0);
                    else if (object.ethereumData.length >= 0)
                        message.ethereumData = object.ethereumData;
            if (object.callData != null) {
                if (!$util.isObject(object.callData))
                    throw $TypeError(".proto.EthereumTransactionBody.callData: object expected");
                message.callData = $root.proto.FileID.fromObject(object.callData, _depth + 1);
            }
            if (object.maxGasAllowance != null)
                if (typeof object.maxGasAllowance === "object" ? object.maxGasAllowance.low || object.maxGasAllowance.high : $Number(object.maxGasAllowance) !== 0)
                    if ($util.Long)
                        message.maxGasAllowance = $util.Long.fromValue(object.maxGasAllowance, false);
                    else if (typeof object.maxGasAllowance === "string")
                        message.maxGasAllowance = $parseInt(object.maxGasAllowance, 10);
                    else if (typeof object.maxGasAllowance === "number")
                        message.maxGasAllowance = object.maxGasAllowance;
                    else if (typeof object.maxGasAllowance === "object")
                        message.maxGasAllowance = new $util.LongBits(object.maxGasAllowance.low >>> 0, object.maxGasAllowance.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an EthereumTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EthereumTransactionBody
         * @static
         * @param {proto.EthereumTransactionBody} message EthereumTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EthereumTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.ethereumData = "";
                else {
                    object.ethereumData = [];
                    if (options.bytes !== $Array)
                        object.ethereumData = $util.newBuffer(object.ethereumData);
                }
                object.callData = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxGasAllowance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maxGasAllowance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.ethereumData != null && $Object.hasOwnProperty.call(message, "ethereumData"))
                object.ethereumData = options.bytes === $String ? $util.base64.encode(message.ethereumData, 0, message.ethereumData.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ethereumData) : message.ethereumData;
            if (message.callData != null && $Object.hasOwnProperty.call(message, "callData"))
                object.callData = $root.proto.FileID.toObject(message.callData, options, _depth + 1);
            if (message.maxGasAllowance != null && $Object.hasOwnProperty.call(message, "maxGasAllowance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maxGasAllowance = typeof message.maxGasAllowance === "number" ? $BigInt(message.maxGasAllowance) : $util.Long.fromBits(message.maxGasAllowance.low >>> 0, message.maxGasAllowance.high >>> 0, false).toBigInt();
                else if (typeof message.maxGasAllowance === "number")
                    object.maxGasAllowance = options.longs === $String ? $String(message.maxGasAllowance) : message.maxGasAllowance;
                else
                    object.maxGasAllowance = options.longs === $String ? $util.Long.prototype.toString.call(message.maxGasAllowance) : options.longs === $Number ? new $util.LongBits(message.maxGasAllowance.low >>> 0, message.maxGasAllowance.high >>> 0).toNumber() : message.maxGasAllowance;
            return object;
        };

        /**
         * Converts this EthereumTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.EthereumTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EthereumTransactionBody.prototype.toJSON = function() {
            return EthereumTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EthereumTransactionBody
         * @function getTypeUrl
         * @memberof proto.EthereumTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EthereumTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EthereumTransactionBody";
        };

        return EthereumTransactionBody;
    })();

    proto.FileAppendTransactionBody = (function() {

        /**
         * Properties of a FileAppendTransactionBody.
         * @typedef {Object} proto.FileAppendTransactionBody.$Properties
         * @property {proto.FileID.$Properties|null} [fileID] A file identifier.<br/>
         * This identifies the file to which the `contents` will be appended.
         * <p>
         * This field is REQUIRED.<br/>
         * The identified file MUST exist.<br/>
         * The identified file MUST NOT be larger than the current maximum file
         * size limit.<br/>
         * The identified file MUST NOT be deleted.<br/>
         * The identified file MUST NOT be immutable.
         * @property {Uint8Array|null} [contents] An array of bytes to append.<br/>
         * <p>
         * This content SHALL be appended to the identified file if this
         * transaction succeeds.<br/>
         * This field is REQUIRED.<br/>
         * This field MUST NOT be empty.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FileAppendTransactionBody.
         * @memberof proto
         * @interface IFileAppendTransactionBody
         * @augments proto.FileAppendTransactionBody.$Properties
         * @deprecated Use proto.FileAppendTransactionBody.$Properties instead.
         */

        /**
         * Shape of a FileAppendTransactionBody.
         * @typedef {proto.FileAppendTransactionBody.$Properties} proto.FileAppendTransactionBody.$Shape
         */

        /**
         * Constructs a new FileAppendTransactionBody.
         * @memberof proto
         * @classdesc A transaction body for an `appendContent` transaction.<br/>
         * This transaction body provides a mechanism to append content to a "file" in
         * network state. Hedera transactions are limited in size, but there are many
         * uses for in-state byte arrays (e.g. smart contract bytecode) which require
         * more than may fit within a single transaction. The `appendFile` transaction
         * exists to support these requirements. The typical pattern is to create a
         * file, append more data until the full content is stored, verify the file is
         * correct, then update the file entry with any final metadata changes (e.g.
         * adding threshold keys and removing the initial upload key).
         * 
         * Each append transaction MUST remain within the total transaction size limit
         * for the network (typically 6144 bytes).<br/>
         * The total size of a file MUST remain within the maximum file size limit for
         * the network (typically 1048576 bytes).
         * 
         * #### Signature Requirements
         * Append transactions MUST have signatures from _all_ keys in the `KeyList`
         * assigned to the `keys` field of the file.<br/>
         * See the [File Service](#FileService) specification for a detailed
         * explanation of the signature requirements for all file transactions.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.FileAppendTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FileAppendTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A file identifier.<br/>
         * This identifies the file to which the `contents` will be appended.
         * <p>
         * This field is REQUIRED.<br/>
         * The identified file MUST exist.<br/>
         * The identified file MUST NOT be larger than the current maximum file
         * size limit.<br/>
         * The identified file MUST NOT be deleted.<br/>
         * The identified file MUST NOT be immutable.
         * @member {proto.FileID.$Properties|null|undefined} fileID
         * @memberof proto.FileAppendTransactionBody
         * @instance
         */
        FileAppendTransactionBody.prototype.fileID = null;

        /**
         * An array of bytes to append.<br/>
         * <p>
         * This content SHALL be appended to the identified file if this
         * transaction succeeds.<br/>
         * This field is REQUIRED.<br/>
         * This field MUST NOT be empty.
         * @member {Uint8Array} contents
         * @memberof proto.FileAppendTransactionBody
         * @instance
         */
        FileAppendTransactionBody.prototype.contents = $util.newBuffer([]);

        /**
         * Creates a new FileAppendTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.FileAppendTransactionBody
         * @static
         * @param {proto.FileAppendTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.FileAppendTransactionBody} FileAppendTransactionBody instance
         * @type {{
         *   (properties: proto.FileAppendTransactionBody.$Shape): proto.FileAppendTransactionBody & proto.FileAppendTransactionBody.$Shape;
         *   (properties?: proto.FileAppendTransactionBody.$Properties): proto.FileAppendTransactionBody;
         * }}
         */
        FileAppendTransactionBody.create = function(properties) {
            return new FileAppendTransactionBody(properties);
        };

        /**
         * Encodes the specified FileAppendTransactionBody message. Does not implicitly {@link proto.FileAppendTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.FileAppendTransactionBody
         * @static
         * @param {proto.FileAppendTransactionBody.$Properties} message FileAppendTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileAppendTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.contents != null && $Object.hasOwnProperty.call(message, "contents") && message.contents.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.contents);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FileAppendTransactionBody message, length delimited. Does not implicitly {@link proto.FileAppendTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FileAppendTransactionBody
         * @static
         * @param {proto.FileAppendTransactionBody.$Properties} message FileAppendTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileAppendTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FileAppendTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FileAppendTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FileAppendTransactionBody & proto.FileAppendTransactionBody.$Shape} FileAppendTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileAppendTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FileAppendTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.contents = value;
                        else
                            delete message.contents;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FileAppendTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FileAppendTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FileAppendTransactionBody & proto.FileAppendTransactionBody.$Shape} FileAppendTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileAppendTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FileAppendTransactionBody message.
         * @function verify
         * @memberof proto.FileAppendTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FileAppendTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                if (error)
                    return "fileID." + error;
            }
            if (message.contents != null && $Object.hasOwnProperty.call(message, "contents"))
                if (!(message.contents && typeof message.contents.length === "number" || $util.isString(message.contents)))
                    return "contents: buffer expected";
            return null;
        };

        /**
         * Creates a FileAppendTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FileAppendTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FileAppendTransactionBody} FileAppendTransactionBody
         */
        FileAppendTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FileAppendTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FileAppendTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FileAppendTransactionBody();
            if (object.fileID != null) {
                if (!$util.isObject(object.fileID))
                    throw $TypeError(".proto.FileAppendTransactionBody.fileID: object expected");
                message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
            }
            if (object.contents != null)
                if (object.contents.length)
                    if (typeof object.contents === "string")
                        $util.base64.decode(object.contents, message.contents = $util.newBuffer($util.base64.length(object.contents)), 0);
                    else if (object.contents.length >= 0)
                        message.contents = object.contents;
            return message;
        };

        /**
         * Creates a plain object from a FileAppendTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FileAppendTransactionBody
         * @static
         * @param {proto.FileAppendTransactionBody} message FileAppendTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FileAppendTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.fileID = null;
                if (options.bytes === $String)
                    object.contents = "";
                else {
                    object.contents = [];
                    if (options.bytes !== $Array)
                        object.contents = $util.newBuffer(object.contents);
                }
            }
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
            if (message.contents != null && $Object.hasOwnProperty.call(message, "contents"))
                object.contents = options.bytes === $String ? $util.base64.encode(message.contents, 0, message.contents.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.contents) : message.contents;
            return object;
        };

        /**
         * Converts this FileAppendTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.FileAppendTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FileAppendTransactionBody.prototype.toJSON = function() {
            return FileAppendTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FileAppendTransactionBody
         * @function getTypeUrl
         * @memberof proto.FileAppendTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FileAppendTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FileAppendTransactionBody";
        };

        return FileAppendTransactionBody;
    })();

    proto.FileCreateTransactionBody = (function() {

        /**
         * Properties of a FileCreateTransactionBody.
         * @typedef {Object} proto.FileCreateTransactionBody.$Properties
         * @property {proto.Timestamp.$Properties|null} [expirationTime] An expiration timestamp.
         * <p>
         * When the network consensus time exceeds this value, the network
         * SHALL expire the file.
         * @property {proto.KeyList.$Properties|null} [keys] A list of keys that represent file "owners".
         * <p>
         * Every `Key` in this list MUST sign this `fileCreate` transaction, as
         * well as any `fileUpdate` or `fileAppend` that modifies this file.<br/>
         * At least one `Key` in this list MUST sign any `fileDelete` transaction
         * to delete this file.<br/>
         * If this `KeyList` is empty, the file SHALL be created immutable and the
         * only field that may be changed subsequently is the `expirationTime`.
         * An immutable file cannot be deleted except with a `systemDelete`
         * transaction, or by expiration.
         * @property {Uint8Array|null} [contents] A byte array of file content.
         * <p>
         * The file SHALL be created with initial content equal to this field.
         * @property {proto.ShardID.$Properties|null} [shardID] A shard in which this file is created
         * @property {proto.RealmID.$Properties|null} [realmID] A realm in which this file is created.
         * <p>
         * The shard number for this realm MUST match the value in `shardID`.<br/>
         * Currently, this MUST be `0` for both fields.<br/>
         * If the desired realm is `0.0`, this SHOULD NOT be set.
         * @property {proto.Key.$Properties|null} [newRealmAdminKey] The "create realm" was never enabled, and
         * should not be possible on file creation.<br/>
         * An admin key for a new realm, if one is created.
         * Added deprecated tag 2024-05.
         * @property {string|null} [memo] A short description of this file.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FileCreateTransactionBody.
         * @memberof proto
         * @interface IFileCreateTransactionBody
         * @augments proto.FileCreateTransactionBody.$Properties
         * @deprecated Use proto.FileCreateTransactionBody.$Properties instead.
         */

        /**
         * Shape of a FileCreateTransactionBody.
         * @typedef {{
         *   expirationTime?: proto.Timestamp.$Shape|null;
         *   keys?: proto.KeyList.$Shape|null;
         *   contents?: Uint8Array|null;
         *   shardID?: proto.ShardID.$Shape|null;
         *   realmID?: proto.RealmID.$Shape|null;
         *   newRealmAdminKey?: proto.Key.$Shape|null;
         *   memo?: string|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.FileCreateTransactionBody.$Shape
         */

        /**
         * Constructs a new FileCreateTransactionBody.
         * @memberof proto
         * @classdesc Create a new file.
         * 
         * If successful, the new file SHALL contain the (possibly empty) content
         * provided in the `contents` field.<br/>
         * When the current consensus time exceeds the `expirationTime` value, the
         * network SHALL expire the file, and MAY archive the state entry.
         * 
         * #### Signature Requirements
         * The HFS manages file authorization in a manner that can be confusing.
         * The core element of file authorization is the `keys` field,
         * which is a `KeyList`; a list of individual `Key` messages, each of which
         * may represent a simple or complex key.<br/>
         * The file service transactions treat this list differently.<br/>
         * A `fileCreate`, `fileAppend`, or `fileUpdate` MUST have a valid signature
         * from _each_ key in the list.<br/>
         * A `fileDelete` MUST have a valid signature from _at least one_ key in
         * the list. This is different, and allows a file "owned" by many entities
         * to be deleted by any one of those entities. A deleted file cannot be
         * restored, so it is important to consider this when assigning keys for
         * a file.<br/>
         * If any of the keys in a `KeyList` are complex, the full requirements of
         * each complex key must be met to count as a "valid signature" for that key.
         * A complex key structure (i.e. a `ThresholdKey`, or `KeyList`, possibly
         * including additional `ThresholdKey` or `KeyList` descendants) may be
         * assigned as the sole entry in a file `keys` field to ensure all transactions
         * have the same signature requirements.
         * 
         * If the `keys` field is an empty `KeyList`, then the file SHALL be immutable
         * and the only transaction permitted to modify that file SHALL be a
         * `fileUpdate` transaction with _only_ the `expirationTime` set.
         * 
         * #### Shard and Realm
         * The current API ignores shardID and realmID. All files are created in
         * shard 0 and realm 0. Future versions of the API may support multiple
         * realms and multiple shards.
         * 
         * ### Block Stream Effects
         * After the file is created, the FileID for it SHALL be returned in the
         * transaction receipt, and SHALL be recorded in the transaction record.
         * @constructor
         * @param {proto.FileCreateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FileCreateTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An expiration timestamp.
         * <p>
         * When the network consensus time exceeds this value, the network
         * SHALL expire the file.
         * @member {proto.Timestamp.$Properties|null|undefined} expirationTime
         * @memberof proto.FileCreateTransactionBody
         * @instance
         */
        FileCreateTransactionBody.prototype.expirationTime = null;

        /**
         * A list of keys that represent file "owners".
         * <p>
         * Every `Key` in this list MUST sign this `fileCreate` transaction, as
         * well as any `fileUpdate` or `fileAppend` that modifies this file.<br/>
         * At least one `Key` in this list MUST sign any `fileDelete` transaction
         * to delete this file.<br/>
         * If this `KeyList` is empty, the file SHALL be created immutable and the
         * only field that may be changed subsequently is the `expirationTime`.
         * An immutable file cannot be deleted except with a `systemDelete`
         * transaction, or by expiration.
         * @member {proto.KeyList.$Properties|null|undefined} keys
         * @memberof proto.FileCreateTransactionBody
         * @instance
         */
        FileCreateTransactionBody.prototype.keys = null;

        /**
         * A byte array of file content.
         * <p>
         * The file SHALL be created with initial content equal to this field.
         * @member {Uint8Array} contents
         * @memberof proto.FileCreateTransactionBody
         * @instance
         */
        FileCreateTransactionBody.prototype.contents = $util.newBuffer([]);

        /**
         * A shard in which this file is created
         * @member {proto.ShardID.$Properties|null|undefined} shardID
         * @memberof proto.FileCreateTransactionBody
         * @instance
         */
        FileCreateTransactionBody.prototype.shardID = null;

        /**
         * A realm in which this file is created.
         * <p>
         * The shard number for this realm MUST match the value in `shardID`.<br/>
         * Currently, this MUST be `0` for both fields.<br/>
         * If the desired realm is `0.0`, this SHOULD NOT be set.
         * @member {proto.RealmID.$Properties|null|undefined} realmID
         * @memberof proto.FileCreateTransactionBody
         * @instance
         */
        FileCreateTransactionBody.prototype.realmID = null;

        /**
         * The "create realm" was never enabled, and
         * should not be possible on file creation.<br/>
         * An admin key for a new realm, if one is created.
         * Added deprecated tag 2024-05.
         * @member {proto.Key.$Properties|null|undefined} newRealmAdminKey
         * @memberof proto.FileCreateTransactionBody
         * @instance
         */
        FileCreateTransactionBody.prototype.newRealmAdminKey = null;

        /**
         * A short description of this file.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.FileCreateTransactionBody
         * @instance
         */
        FileCreateTransactionBody.prototype.memo = "";

        /**
         * Creates a new FileCreateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.FileCreateTransactionBody
         * @static
         * @param {proto.FileCreateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.FileCreateTransactionBody} FileCreateTransactionBody instance
         * @type {{
         *   (properties: proto.FileCreateTransactionBody.$Shape): proto.FileCreateTransactionBody & proto.FileCreateTransactionBody.$Shape;
         *   (properties?: proto.FileCreateTransactionBody.$Properties): proto.FileCreateTransactionBody;
         * }}
         */
        FileCreateTransactionBody.create = function(properties) {
            return new FileCreateTransactionBody(properties);
        };

        /**
         * Encodes the specified FileCreateTransactionBody message. Does not implicitly {@link proto.FileCreateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.FileCreateTransactionBody
         * @static
         * @param {proto.FileCreateTransactionBody.$Properties} message FileCreateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileCreateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                $root.proto.Timestamp.encode(message.expirationTime, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys"))
                $root.proto.KeyList.encode(message.keys, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.contents != null && $Object.hasOwnProperty.call(message, "contents") && message.contents.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.contents);
            if (message.shardID != null && $Object.hasOwnProperty.call(message, "shardID"))
                $root.proto.ShardID.encode(message.shardID, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.realmID != null && $Object.hasOwnProperty.call(message, "realmID"))
                $root.proto.RealmID.encode(message.realmID, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.newRealmAdminKey != null && $Object.hasOwnProperty.call(message, "newRealmAdminKey"))
                $root.proto.Key.encode(message.newRealmAdminKey, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.memo);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FileCreateTransactionBody message, length delimited. Does not implicitly {@link proto.FileCreateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FileCreateTransactionBody
         * @static
         * @param {proto.FileCreateTransactionBody.$Properties} message FileCreateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileCreateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FileCreateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FileCreateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FileCreateTransactionBody & proto.FileCreateTransactionBody.$Shape} FileCreateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileCreateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FileCreateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.expirationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.keys = $root.proto.KeyList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.keys);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.contents = value;
                        else
                            delete message.contents;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.shardID = $root.proto.ShardID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.shardID);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.realmID = $root.proto.RealmID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.realmID);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.newRealmAdminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.newRealmAdminKey);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FileCreateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FileCreateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FileCreateTransactionBody & proto.FileCreateTransactionBody.$Shape} FileCreateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileCreateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FileCreateTransactionBody message.
         * @function verify
         * @memberof proto.FileCreateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FileCreateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                let error = $root.proto.Timestamp.verify(message.expirationTime, _depth + 1);
                if (error)
                    return "expirationTime." + error;
            }
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys")) {
                let error = $root.proto.KeyList.verify(message.keys, _depth + 1);
                if (error)
                    return "keys." + error;
            }
            if (message.contents != null && $Object.hasOwnProperty.call(message, "contents"))
                if (!(message.contents && typeof message.contents.length === "number" || $util.isString(message.contents)))
                    return "contents: buffer expected";
            if (message.shardID != null && $Object.hasOwnProperty.call(message, "shardID")) {
                let error = $root.proto.ShardID.verify(message.shardID, _depth + 1);
                if (error)
                    return "shardID." + error;
            }
            if (message.realmID != null && $Object.hasOwnProperty.call(message, "realmID")) {
                let error = $root.proto.RealmID.verify(message.realmID, _depth + 1);
                if (error)
                    return "realmID." + error;
            }
            if (message.newRealmAdminKey != null && $Object.hasOwnProperty.call(message, "newRealmAdminKey")) {
                let error = $root.proto.Key.verify(message.newRealmAdminKey, _depth + 1);
                if (error)
                    return "newRealmAdminKey." + error;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            return null;
        };

        /**
         * Creates a FileCreateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FileCreateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FileCreateTransactionBody} FileCreateTransactionBody
         */
        FileCreateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FileCreateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FileCreateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FileCreateTransactionBody();
            if (object.expirationTime != null) {
                if (!$util.isObject(object.expirationTime))
                    throw $TypeError(".proto.FileCreateTransactionBody.expirationTime: object expected");
                message.expirationTime = $root.proto.Timestamp.fromObject(object.expirationTime, _depth + 1);
            }
            if (object.keys != null) {
                if (!$util.isObject(object.keys))
                    throw $TypeError(".proto.FileCreateTransactionBody.keys: object expected");
                message.keys = $root.proto.KeyList.fromObject(object.keys, _depth + 1);
            }
            if (object.contents != null)
                if (object.contents.length)
                    if (typeof object.contents === "string")
                        $util.base64.decode(object.contents, message.contents = $util.newBuffer($util.base64.length(object.contents)), 0);
                    else if (object.contents.length >= 0)
                        message.contents = object.contents;
            if (object.shardID != null) {
                if (!$util.isObject(object.shardID))
                    throw $TypeError(".proto.FileCreateTransactionBody.shardID: object expected");
                message.shardID = $root.proto.ShardID.fromObject(object.shardID, _depth + 1);
            }
            if (object.realmID != null) {
                if (!$util.isObject(object.realmID))
                    throw $TypeError(".proto.FileCreateTransactionBody.realmID: object expected");
                message.realmID = $root.proto.RealmID.fromObject(object.realmID, _depth + 1);
            }
            if (object.newRealmAdminKey != null) {
                if (!$util.isObject(object.newRealmAdminKey))
                    throw $TypeError(".proto.FileCreateTransactionBody.newRealmAdminKey: object expected");
                message.newRealmAdminKey = $root.proto.Key.fromObject(object.newRealmAdminKey, _depth + 1);
            }
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            return message;
        };

        /**
         * Creates a plain object from a FileCreateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FileCreateTransactionBody
         * @static
         * @param {proto.FileCreateTransactionBody} message FileCreateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FileCreateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.expirationTime = null;
                object.keys = null;
                if (options.bytes === $String)
                    object.contents = "";
                else {
                    object.contents = [];
                    if (options.bytes !== $Array)
                        object.contents = $util.newBuffer(object.contents);
                }
                object.shardID = null;
                object.realmID = null;
                object.newRealmAdminKey = null;
                object.memo = "";
            }
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                object.expirationTime = $root.proto.Timestamp.toObject(message.expirationTime, options, _depth + 1);
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys"))
                object.keys = $root.proto.KeyList.toObject(message.keys, options, _depth + 1);
            if (message.contents != null && $Object.hasOwnProperty.call(message, "contents"))
                object.contents = options.bytes === $String ? $util.base64.encode(message.contents, 0, message.contents.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.contents) : message.contents;
            if (message.shardID != null && $Object.hasOwnProperty.call(message, "shardID"))
                object.shardID = $root.proto.ShardID.toObject(message.shardID, options, _depth + 1);
            if (message.realmID != null && $Object.hasOwnProperty.call(message, "realmID"))
                object.realmID = $root.proto.RealmID.toObject(message.realmID, options, _depth + 1);
            if (message.newRealmAdminKey != null && $Object.hasOwnProperty.call(message, "newRealmAdminKey"))
                object.newRealmAdminKey = $root.proto.Key.toObject(message.newRealmAdminKey, options, _depth + 1);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            return object;
        };

        /**
         * Converts this FileCreateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.FileCreateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FileCreateTransactionBody.prototype.toJSON = function() {
            return FileCreateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FileCreateTransactionBody
         * @function getTypeUrl
         * @memberof proto.FileCreateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FileCreateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FileCreateTransactionBody";
        };

        return FileCreateTransactionBody;
    })();

    proto.FileDeleteTransactionBody = (function() {

        /**
         * Properties of a FileDeleteTransactionBody.
         * @typedef {Object} proto.FileDeleteTransactionBody.$Properties
         * @property {proto.FileID.$Properties|null} [fileID] A file identifier.<br/>
         * This identifies the file to delete.
         * <p>
         * The identified file MUST NOT be a "system" file.<br/>
         * This field is REQUIRED.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FileDeleteTransactionBody.
         * @memberof proto
         * @interface IFileDeleteTransactionBody
         * @augments proto.FileDeleteTransactionBody.$Properties
         * @deprecated Use proto.FileDeleteTransactionBody.$Properties instead.
         */

        /**
         * Shape of a FileDeleteTransactionBody.
         * @typedef {proto.FileDeleteTransactionBody.$Properties} proto.FileDeleteTransactionBody.$Shape
         */

        /**
         * Constructs a new FileDeleteTransactionBody.
         * @memberof proto
         * @classdesc Mark a file as deleted and remove its content from network state.
         * 
         * The metadata for a deleted file SHALL be retained at least until the
         * expiration time for the file is exceeded.<br/>
         * On completion, the identified file SHALL be marked `deleted`.<br/>
         * On completion, the identified file SHALL have an empty `contents` array.<br/>
         * This transaction SHALL be final and irreversible.<br/>
         * 
         * #### Signature Requirements
         * At least _one_ key from the `KeyList` in the `keys` field of the
         * identified file MUST sign this transaction.<br/>
         * If the keys field for the identified file is an empty `KeyList` (because that
         * file was previously created or updated to have an empty `KeyList`), then the
         * file is considered immutable and this message SHALL fail as UNAUTHORIZED.
         * See the [File Service](#FileService) specification for a detailed
         * explanation of the signature requirements for all file transactions.
         * 
         * ### What is a "system" file
         * A "system" file is any file with a file number less than or equal to the
         * current configuration value for `ledger.numReservedSystemEntities`,
         * typically `750`.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.FileDeleteTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FileDeleteTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A file identifier.<br/>
         * This identifies the file to delete.
         * <p>
         * The identified file MUST NOT be a "system" file.<br/>
         * This field is REQUIRED.
         * @member {proto.FileID.$Properties|null|undefined} fileID
         * @memberof proto.FileDeleteTransactionBody
         * @instance
         */
        FileDeleteTransactionBody.prototype.fileID = null;

        /**
         * Creates a new FileDeleteTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.FileDeleteTransactionBody
         * @static
         * @param {proto.FileDeleteTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.FileDeleteTransactionBody} FileDeleteTransactionBody instance
         * @type {{
         *   (properties: proto.FileDeleteTransactionBody.$Shape): proto.FileDeleteTransactionBody & proto.FileDeleteTransactionBody.$Shape;
         *   (properties?: proto.FileDeleteTransactionBody.$Properties): proto.FileDeleteTransactionBody;
         * }}
         */
        FileDeleteTransactionBody.create = function(properties) {
            return new FileDeleteTransactionBody(properties);
        };

        /**
         * Encodes the specified FileDeleteTransactionBody message. Does not implicitly {@link proto.FileDeleteTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.FileDeleteTransactionBody
         * @static
         * @param {proto.FileDeleteTransactionBody.$Properties} message FileDeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileDeleteTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FileDeleteTransactionBody message, length delimited. Does not implicitly {@link proto.FileDeleteTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FileDeleteTransactionBody
         * @static
         * @param {proto.FileDeleteTransactionBody.$Properties} message FileDeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileDeleteTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FileDeleteTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FileDeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FileDeleteTransactionBody & proto.FileDeleteTransactionBody.$Shape} FileDeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileDeleteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FileDeleteTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FileDeleteTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FileDeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FileDeleteTransactionBody & proto.FileDeleteTransactionBody.$Shape} FileDeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileDeleteTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FileDeleteTransactionBody message.
         * @function verify
         * @memberof proto.FileDeleteTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FileDeleteTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                if (error)
                    return "fileID." + error;
            }
            return null;
        };

        /**
         * Creates a FileDeleteTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FileDeleteTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FileDeleteTransactionBody} FileDeleteTransactionBody
         */
        FileDeleteTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FileDeleteTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FileDeleteTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FileDeleteTransactionBody();
            if (object.fileID != null) {
                if (!$util.isObject(object.fileID))
                    throw $TypeError(".proto.FileDeleteTransactionBody.fileID: object expected");
                message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a FileDeleteTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FileDeleteTransactionBody
         * @static
         * @param {proto.FileDeleteTransactionBody} message FileDeleteTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FileDeleteTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.fileID = null;
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this FileDeleteTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.FileDeleteTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FileDeleteTransactionBody.prototype.toJSON = function() {
            return FileDeleteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FileDeleteTransactionBody
         * @function getTypeUrl
         * @memberof proto.FileDeleteTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FileDeleteTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FileDeleteTransactionBody";
        };

        return FileDeleteTransactionBody;
    })();

    proto.FileUpdateTransactionBody = (function() {

        /**
         * Properties of a FileUpdateTransactionBody.
         * @typedef {Object} proto.FileUpdateTransactionBody.$Properties
         * @property {proto.FileID.$Properties|null} [fileID] A file identifier for the file to update.
         * <p>
         * This field is REQUIRED.
         * @property {proto.Timestamp.$Properties|null} [expirationTime] An expiration timestamp.
         * <p>
         * If set, this value MUST be strictly later than the existing
         * `expirationTime` value, or else it will be ignored.<br/>
         * If set, this value SHALL replace the existing `expirationTime`.<br/>
         * If this field is the only field set, then this transaction SHALL NOT
         * require any signature other than the `payer` for the transaction.<br/>
         * When the network consensus time exceeds the then-current
         * `expirationTime`, the network SHALL expire the file.
         * @property {proto.KeyList.$Properties|null} [keys] The new list of keys that "own" this file.
         * <p>
         * If set, every key in this `KeyList` MUST sign this transaction.<br/>
         * If set, every key in the _previous_ `KeyList` MUST _also_
         * sign this transaction.<br/>
         * If this value is an empty `KeyList`, then the file SHALL be immutable
         * after completion of this transaction.
         * @property {Uint8Array|null} [contents] An array of bytes.
         * <p>
         * This value, if set, SHALL _replace_ the existing file content.
         * If this value is set to an empty byte array, the content of the file
         * SHALL be unchanged.
         * @property {google.protobuf.StringValue.$Properties|null} [memo] A short description of this file.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FileUpdateTransactionBody.
         * @memberof proto
         * @interface IFileUpdateTransactionBody
         * @augments proto.FileUpdateTransactionBody.$Properties
         * @deprecated Use proto.FileUpdateTransactionBody.$Properties instead.
         */

        /**
         * Shape of a FileUpdateTransactionBody.
         * @typedef {{
         *   fileID?: proto.FileID.$Shape|null;
         *   expirationTime?: proto.Timestamp.$Shape|null;
         *   keys?: proto.KeyList.$Shape|null;
         *   contents?: Uint8Array|null;
         *   memo?: google.protobuf.StringValue.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.FileUpdateTransactionBody.$Shape
         */

        /**
         * Constructs a new FileUpdateTransactionBody.
         * @memberof proto
         * @classdesc Update the metadata, and/or replace the content, of a file in the
         * Hedera File Service (HFS).
         * 
         * Any field which is not set (i.e. is null) in this message, other than
         * `fileID`, SHALL be ignored.<br/>
         * If the `keys` list for the identified file is an empty `KeyList`, then
         * this message MUST NOT set any field except `expirationTime`.
         * 
         * #### Signature Requirements
         * Every `Key` in the `keys` list for the identified file MUST sign this
         * transaction, if any field other than `expirationTime` is to be updated.<br/>
         * If the `keys` list for the identified file is an empty `KeyList` (because
         * this file was previously created or updated to have an empty `KeyList`),
         * then the file is considered immutable and this message MUST NOT set any
         * field except `expirationTime`.<br/>
         * See the [File Service](#FileService) specification for a detailed
         * explanation of the signature requirements for all file transactions.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.FileUpdateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FileUpdateTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A file identifier for the file to update.
         * <p>
         * This field is REQUIRED.
         * @member {proto.FileID.$Properties|null|undefined} fileID
         * @memberof proto.FileUpdateTransactionBody
         * @instance
         */
        FileUpdateTransactionBody.prototype.fileID = null;

        /**
         * An expiration timestamp.
         * <p>
         * If set, this value MUST be strictly later than the existing
         * `expirationTime` value, or else it will be ignored.<br/>
         * If set, this value SHALL replace the existing `expirationTime`.<br/>
         * If this field is the only field set, then this transaction SHALL NOT
         * require any signature other than the `payer` for the transaction.<br/>
         * When the network consensus time exceeds the then-current
         * `expirationTime`, the network SHALL expire the file.
         * @member {proto.Timestamp.$Properties|null|undefined} expirationTime
         * @memberof proto.FileUpdateTransactionBody
         * @instance
         */
        FileUpdateTransactionBody.prototype.expirationTime = null;

        /**
         * The new list of keys that "own" this file.
         * <p>
         * If set, every key in this `KeyList` MUST sign this transaction.<br/>
         * If set, every key in the _previous_ `KeyList` MUST _also_
         * sign this transaction.<br/>
         * If this value is an empty `KeyList`, then the file SHALL be immutable
         * after completion of this transaction.
         * @member {proto.KeyList.$Properties|null|undefined} keys
         * @memberof proto.FileUpdateTransactionBody
         * @instance
         */
        FileUpdateTransactionBody.prototype.keys = null;

        /**
         * An array of bytes.
         * <p>
         * This value, if set, SHALL _replace_ the existing file content.
         * If this value is set to an empty byte array, the content of the file
         * SHALL be unchanged.
         * @member {Uint8Array} contents
         * @memberof proto.FileUpdateTransactionBody
         * @instance
         */
        FileUpdateTransactionBody.prototype.contents = $util.newBuffer([]);

        /**
         * A short description of this file.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {google.protobuf.StringValue.$Properties|null|undefined} memo
         * @memberof proto.FileUpdateTransactionBody
         * @instance
         */
        FileUpdateTransactionBody.prototype.memo = null;

        /**
         * Creates a new FileUpdateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.FileUpdateTransactionBody
         * @static
         * @param {proto.FileUpdateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.FileUpdateTransactionBody} FileUpdateTransactionBody instance
         * @type {{
         *   (properties: proto.FileUpdateTransactionBody.$Shape): proto.FileUpdateTransactionBody & proto.FileUpdateTransactionBody.$Shape;
         *   (properties?: proto.FileUpdateTransactionBody.$Properties): proto.FileUpdateTransactionBody;
         * }}
         */
        FileUpdateTransactionBody.create = function(properties) {
            return new FileUpdateTransactionBody(properties);
        };

        /**
         * Encodes the specified FileUpdateTransactionBody message. Does not implicitly {@link proto.FileUpdateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.FileUpdateTransactionBody
         * @static
         * @param {proto.FileUpdateTransactionBody.$Properties} message FileUpdateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileUpdateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                $root.proto.Timestamp.encode(message.expirationTime, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys"))
                $root.proto.KeyList.encode(message.keys, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.contents != null && $Object.hasOwnProperty.call(message, "contents") && message.contents.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.contents);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                $root.google.protobuf.StringValue.encode(message.memo, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FileUpdateTransactionBody message, length delimited. Does not implicitly {@link proto.FileUpdateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FileUpdateTransactionBody
         * @static
         * @param {proto.FileUpdateTransactionBody.$Properties} message FileUpdateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileUpdateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FileUpdateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FileUpdateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FileUpdateTransactionBody & proto.FileUpdateTransactionBody.$Shape} FileUpdateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileUpdateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FileUpdateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.expirationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.keys = $root.proto.KeyList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.keys);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.contents = value;
                        else
                            delete message.contents;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.memo = $root.google.protobuf.StringValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.memo);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FileUpdateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FileUpdateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FileUpdateTransactionBody & proto.FileUpdateTransactionBody.$Shape} FileUpdateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileUpdateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FileUpdateTransactionBody message.
         * @function verify
         * @memberof proto.FileUpdateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FileUpdateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                if (error)
                    return "fileID." + error;
            }
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                let error = $root.proto.Timestamp.verify(message.expirationTime, _depth + 1);
                if (error)
                    return "expirationTime." + error;
            }
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys")) {
                let error = $root.proto.KeyList.verify(message.keys, _depth + 1);
                if (error)
                    return "keys." + error;
            }
            if (message.contents != null && $Object.hasOwnProperty.call(message, "contents"))
                if (!(message.contents && typeof message.contents.length === "number" || $util.isString(message.contents)))
                    return "contents: buffer expected";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo")) {
                let error = $root.google.protobuf.StringValue.verify(message.memo, _depth + 1);
                if (error)
                    return "memo." + error;
            }
            return null;
        };

        /**
         * Creates a FileUpdateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FileUpdateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FileUpdateTransactionBody} FileUpdateTransactionBody
         */
        FileUpdateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FileUpdateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FileUpdateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FileUpdateTransactionBody();
            if (object.fileID != null) {
                if (!$util.isObject(object.fileID))
                    throw $TypeError(".proto.FileUpdateTransactionBody.fileID: object expected");
                message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
            }
            if (object.expirationTime != null) {
                if (!$util.isObject(object.expirationTime))
                    throw $TypeError(".proto.FileUpdateTransactionBody.expirationTime: object expected");
                message.expirationTime = $root.proto.Timestamp.fromObject(object.expirationTime, _depth + 1);
            }
            if (object.keys != null) {
                if (!$util.isObject(object.keys))
                    throw $TypeError(".proto.FileUpdateTransactionBody.keys: object expected");
                message.keys = $root.proto.KeyList.fromObject(object.keys, _depth + 1);
            }
            if (object.contents != null)
                if (object.contents.length)
                    if (typeof object.contents === "string")
                        $util.base64.decode(object.contents, message.contents = $util.newBuffer($util.base64.length(object.contents)), 0);
                    else if (object.contents.length >= 0)
                        message.contents = object.contents;
            if (object.memo != null) {
                if (!$util.isObject(object.memo))
                    throw $TypeError(".proto.FileUpdateTransactionBody.memo: object expected");
                message.memo = $root.google.protobuf.StringValue.fromObject(object.memo, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a FileUpdateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FileUpdateTransactionBody
         * @static
         * @param {proto.FileUpdateTransactionBody} message FileUpdateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FileUpdateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.fileID = null;
                object.expirationTime = null;
                object.keys = null;
                if (options.bytes === $String)
                    object.contents = "";
                else {
                    object.contents = [];
                    if (options.bytes !== $Array)
                        object.contents = $util.newBuffer(object.contents);
                }
                object.memo = null;
            }
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                object.expirationTime = $root.proto.Timestamp.toObject(message.expirationTime, options, _depth + 1);
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys"))
                object.keys = $root.proto.KeyList.toObject(message.keys, options, _depth + 1);
            if (message.contents != null && $Object.hasOwnProperty.call(message, "contents"))
                object.contents = options.bytes === $String ? $util.base64.encode(message.contents, 0, message.contents.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.contents) : message.contents;
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = $root.google.protobuf.StringValue.toObject(message.memo, options, _depth + 1);
            return object;
        };

        /**
         * Converts this FileUpdateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.FileUpdateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FileUpdateTransactionBody.prototype.toJSON = function() {
            return FileUpdateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FileUpdateTransactionBody
         * @function getTypeUrl
         * @memberof proto.FileUpdateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FileUpdateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FileUpdateTransactionBody";
        };

        return FileUpdateTransactionBody;
    })();

    proto.ContractDeleteTransactionBody = (function() {

        /**
         * Properties of a ContractDeleteTransactionBody.
         * @typedef {Object} proto.ContractDeleteTransactionBody.$Properties
         * @property {proto.ContractID.$Properties|null} [contractID] The id of the contract to be deleted.
         * <p>
         * This field is REQUIRED.
         * @property {proto.AccountID.$Properties|null} [transferAccountID] An Account ID recipient.
         * <p>
         * This account SHALL receive all HBAR and other tokens still owned by
         * the contract that is removed.
         * @property {proto.ContractID.$Properties|null} [transferContractID] A contract ID recipient.
         * <p>
         * This contract SHALL receive all HBAR and other tokens still owned by
         * the contract that is removed.
         * @property {boolean|null} [permanentRemoval] A flag indicating that this transaction is "synthetic"; initiated by the
         * node software.
         * <p>
         * The consensus nodes create such "synthetic" transactions to both to
         * properly manage state changes and to communicate those changes to other
         * systems via the Block Stream.<br/>
         * A user-initiated transaction MUST NOT set this flag.
         * @property {"transferAccountID"|"transferContractID"} [obtainers] ContractDeleteTransactionBody obtainers
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractDeleteTransactionBody.
         * @memberof proto
         * @interface IContractDeleteTransactionBody
         * @augments proto.ContractDeleteTransactionBody.$Properties
         * @deprecated Use proto.ContractDeleteTransactionBody.$Properties instead.
         */

        /**
         * Narrowed shape of a ContractDeleteTransactionBody.
         * @typedef {{
         *   contractID?: proto.ContractID.$Shape|null;
         *   transferAccountID?: proto.AccountID.$Shape|null;
         *   transferContractID?: proto.ContractID.$Shape|null;
         *   permanentRemoval?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ obtainers?: undefined; transferAccountID?: null; transferContractID?: null }|{ obtainers?: "transferAccountID"; transferAccountID: proto.AccountID.$Shape; transferContractID?: null }|{ obtainers?: "transferContractID"; transferAccountID?: null; transferContractID: proto.ContractID.$Shape })
         * )} proto.ContractDeleteTransactionBody.$Shape
         */

        /**
         * Constructs a new ContractDeleteTransactionBody.
         * @memberof proto
         * @classdesc Delete a smart contract, and transfer any remaining HBAR balance to a
         * designated account.
         * 
         * If this call succeeds then all subsequent calls to that smart contract
         * SHALL execute the `0x0` opcode, as required for EVM equivalence.
         * 
         * ### Requirements
         * - An account or smart contract MUST be designated to receive all remaining
         * account balances.
         * - The smart contract MUST have an admin key set. If the contract does not
         * have `admin_key` set, then this transaction SHALL fail and response code
         * `MODIFYING_IMMUTABLE_CONTRACT` SHALL be set.
         * - If `admin_key` is, or contains, an empty `KeyList` key, it SHALL be
         * treated the same as an admin key that is not set.
         * - The `Key` set for `admin_key` on the smart contract MUST have a valid
         * signature set on this transaction.
         * - The designated receiving account MAY have `receiver_sig_required` set. If
         * that field is set, the receiver account MUST also sign this transaction.
         * - The field `permanent_removal` MUST NOT be set. That field is reserved for
         * internal system use when purging the smart contract from state. Any user
         * transaction with that field set SHALL be rejected and a response code
         * `PERMANENT_REMOVAL_REQUIRES_SYSTEM_INITIATION` SHALL be set.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.ContractDeleteTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractDeleteTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The id of the contract to be deleted.
         * <p>
         * This field is REQUIRED.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.ContractDeleteTransactionBody
         * @instance
         */
        ContractDeleteTransactionBody.prototype.contractID = null;

        /**
         * An Account ID recipient.
         * <p>
         * This account SHALL receive all HBAR and other tokens still owned by
         * the contract that is removed.
         * @member {proto.AccountID.$Properties|null|undefined} transferAccountID
         * @memberof proto.ContractDeleteTransactionBody
         * @instance
         */
        ContractDeleteTransactionBody.prototype.transferAccountID = null;

        /**
         * A contract ID recipient.
         * <p>
         * This contract SHALL receive all HBAR and other tokens still owned by
         * the contract that is removed.
         * @member {proto.ContractID.$Properties|null|undefined} transferContractID
         * @memberof proto.ContractDeleteTransactionBody
         * @instance
         */
        ContractDeleteTransactionBody.prototype.transferContractID = null;

        /**
         * A flag indicating that this transaction is "synthetic"; initiated by the
         * node software.
         * <p>
         * The consensus nodes create such "synthetic" transactions to both to
         * properly manage state changes and to communicate those changes to other
         * systems via the Block Stream.<br/>
         * A user-initiated transaction MUST NOT set this flag.
         * @member {boolean} permanentRemoval
         * @memberof proto.ContractDeleteTransactionBody
         * @instance
         */
        ContractDeleteTransactionBody.prototype.permanentRemoval = false;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ContractDeleteTransactionBody obtainers.
         * @member {"transferAccountID"|"transferContractID"|undefined} obtainers
         * @memberof proto.ContractDeleteTransactionBody
         * @instance
         */
        $Object.defineProperty(ContractDeleteTransactionBody.prototype, "obtainers", {
            get: $util.oneOfGetter($oneOfFields = ["transferAccountID", "transferContractID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ContractDeleteTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.ContractDeleteTransactionBody
         * @static
         * @param {proto.ContractDeleteTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.ContractDeleteTransactionBody} ContractDeleteTransactionBody instance
         * @type {{
         *   (properties: proto.ContractDeleteTransactionBody.$Shape): proto.ContractDeleteTransactionBody & proto.ContractDeleteTransactionBody.$Shape;
         *   (properties?: proto.ContractDeleteTransactionBody.$Properties): proto.ContractDeleteTransactionBody;
         * }}
         */
        ContractDeleteTransactionBody.create = function(properties) {
            return new ContractDeleteTransactionBody(properties);
        };

        /**
         * Encodes the specified ContractDeleteTransactionBody message. Does not implicitly {@link proto.ContractDeleteTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractDeleteTransactionBody
         * @static
         * @param {proto.ContractDeleteTransactionBody.$Properties} message ContractDeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractDeleteTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.transferAccountID != null && $Object.hasOwnProperty.call(message, "transferAccountID"))
                $root.proto.AccountID.encode(message.transferAccountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.transferContractID != null && $Object.hasOwnProperty.call(message, "transferContractID"))
                $root.proto.ContractID.encode(message.transferContractID, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.permanentRemoval != null && $Object.hasOwnProperty.call(message, "permanentRemoval") && message.permanentRemoval !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.permanentRemoval);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractDeleteTransactionBody message, length delimited. Does not implicitly {@link proto.ContractDeleteTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractDeleteTransactionBody
         * @static
         * @param {proto.ContractDeleteTransactionBody.$Properties} message ContractDeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractDeleteTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractDeleteTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractDeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractDeleteTransactionBody & proto.ContractDeleteTransactionBody.$Shape} ContractDeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractDeleteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractDeleteTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.transferAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transferAccountID);
                        message.obtainers = "transferAccountID";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.transferContractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transferContractID);
                        message.obtainers = "transferContractID";
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.permanentRemoval = value;
                        else
                            delete message.permanentRemoval;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractDeleteTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractDeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractDeleteTransactionBody & proto.ContractDeleteTransactionBody.$Shape} ContractDeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractDeleteTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractDeleteTransactionBody message.
         * @function verify
         * @memberof proto.ContractDeleteTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractDeleteTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            if (message.transferAccountID != null && $Object.hasOwnProperty.call(message, "transferAccountID")) {
                properties.obtainers = 1;
                {
                    let error = $root.proto.AccountID.verify(message.transferAccountID, _depth + 1);
                    if (error)
                        return "transferAccountID." + error;
                }
            }
            if (message.transferContractID != null && $Object.hasOwnProperty.call(message, "transferContractID")) {
                if (properties.obtainers === 1)
                    return "obtainers: multiple values";
                properties.obtainers = 1;
                {
                    let error = $root.proto.ContractID.verify(message.transferContractID, _depth + 1);
                    if (error)
                        return "transferContractID." + error;
                }
            }
            if (message.permanentRemoval != null && $Object.hasOwnProperty.call(message, "permanentRemoval"))
                if (typeof message.permanentRemoval !== "boolean")
                    return "permanentRemoval: boolean expected";
            return null;
        };

        /**
         * Creates a ContractDeleteTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractDeleteTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractDeleteTransactionBody} ContractDeleteTransactionBody
         */
        ContractDeleteTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractDeleteTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractDeleteTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractDeleteTransactionBody();
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.ContractDeleteTransactionBody.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            if (object.transferAccountID != null) {
                if (!$util.isObject(object.transferAccountID))
                    throw $TypeError(".proto.ContractDeleteTransactionBody.transferAccountID: object expected");
                message.transferAccountID = $root.proto.AccountID.fromObject(object.transferAccountID, _depth + 1);
            }
            if (object.transferContractID != null) {
                if (!$util.isObject(object.transferContractID))
                    throw $TypeError(".proto.ContractDeleteTransactionBody.transferContractID: object expected");
                message.transferContractID = $root.proto.ContractID.fromObject(object.transferContractID, _depth + 1);
            }
            if (object.permanentRemoval != null)
                if (object.permanentRemoval)
                    message.permanentRemoval = $Boolean(object.permanentRemoval);
            return message;
        };

        /**
         * Creates a plain object from a ContractDeleteTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractDeleteTransactionBody
         * @static
         * @param {proto.ContractDeleteTransactionBody} message ContractDeleteTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractDeleteTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.contractID = null;
                object.permanentRemoval = false;
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            if (message.transferAccountID != null && $Object.hasOwnProperty.call(message, "transferAccountID")) {
                object.transferAccountID = $root.proto.AccountID.toObject(message.transferAccountID, options, _depth + 1);
                if (options.oneofs)
                    object.obtainers = "transferAccountID";
            }
            if (message.transferContractID != null && $Object.hasOwnProperty.call(message, "transferContractID")) {
                object.transferContractID = $root.proto.ContractID.toObject(message.transferContractID, options, _depth + 1);
                if (options.oneofs)
                    object.obtainers = "transferContractID";
            }
            if (message.permanentRemoval != null && $Object.hasOwnProperty.call(message, "permanentRemoval"))
                object.permanentRemoval = message.permanentRemoval;
            return object;
        };

        /**
         * Converts this ContractDeleteTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.ContractDeleteTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractDeleteTransactionBody.prototype.toJSON = function() {
            return ContractDeleteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractDeleteTransactionBody
         * @function getTypeUrl
         * @memberof proto.ContractDeleteTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractDeleteTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractDeleteTransactionBody";
        };

        return ContractDeleteTransactionBody;
    })();

    proto.ConsensusCreateTopicTransactionBody = (function() {

        /**
         * Properties of a ConsensusCreateTopicTransactionBody.
         * @typedef {Object} proto.ConsensusCreateTopicTransactionBody.$Properties
         * @property {string|null} [memo] A short memo for this topic.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {proto.Key.$Properties|null} [adminKey] Access control for modification of the topic after it is created.
         * <p>
         * If this field is set, that key MUST sign this transaction.<br/>
         * If this field is set, that key MUST sign each future transaction to
         * update or delete the topic.<br/>
         * An updateTopic transaction that _only_ extends the topic expirationTime
         * (a "manual renewal" transaction) SHALL NOT require admin key
         * signature.<br/>
         * A topic without an admin key SHALL be immutable, except for expiration
         * and renewal.<br/>
         * If adminKey is not set, then `autoRenewAccount` SHALL NOT be set.
         * @property {proto.Key.$Properties|null} [submitKey] Access control for message submission to the topic.
         * <p>
         * If this field is set, that key MUST sign each consensus submit message
         * for this topic.<br/>
         * If this field is not set then any account may submit a message on the
         * topic, without restriction.
         * @property {proto.Duration.$Properties|null} [autoRenewPeriod] The initial lifetime, in seconds, for the topic.<br/>
         * This is also the number of seconds for which the topic SHALL be
         * automatically renewed upon expiring, if it has a valid auto-renew
         * account.
         * <p>
         * This value MUST be set.<br/>
         * This value MUST be greater than the configured
         * MIN_AUTORENEW_PERIOD.<br/>
         * This value MUST be less than the configured MAX_AUTORENEW_PERIOD.
         * @property {proto.AccountID.$Properties|null} [autoRenewAccount] The ID of the account to be charged renewal fees at the topic's
         * expirationTime to extend the lifetime of the topic.
         * <p>
         * The topic lifetime SHALL be extended by the smallest of the following:
         * <ul>
         * <li>The current `autoRenewPeriod` duration.</li>
         * <li>The maximum duration that this account has funds to purchase.</li>
         * <li>The configured MAX_AUTORENEW_PERIOD at the time of automatic
         * renewal.</li>
         * </ul>
         * If this value is set, the referenced account MUST sign this
         * transaction.<br/>
         * If this value is set, the `adminKey` field MUST also be set (though that
         * key MAY not have any correlation to this account).
         * @property {proto.Key.$Properties|null} [feeScheduleKey] Access control for update or delete of custom fees.
         * <p>
         * If set, subsequent `consensus_update_topic` transactions signed with this
         * key MAY update or delete the custom fees for this topic.<br/>
         * If not set, the custom fees for this topic SHALL BE immutable.<br/>
         * If not set when the topic is created, this field CANNOT be set via
         * update.<br/>
         * If set when the topic is created, this field MAY be changed via update.
         * @property {Array.<proto.Key.$Properties>|null} [feeExemptKeyList] A set of keys.<br/>
         * Keys in this list are permitted to submit messages to this topic without
         * paying custom fees associated with this topic.
         * <p>
         * If a submit transaction is signed by _any_ key included in this set,
         * custom fees SHALL NOT be charged for that transaction.<br/>
         * This field MUST NOT contain more than 10 keys.<br/>
         * fee_exempt_key_list SHALL NOT contain any duplicate keys.<br/>
         * fee_exempt_key_list MAY contain keys for accounts that are inactive,
         * deleted, or non-existent.<br/>
         * If fee_exempt_key_list is unset in this transaction, there SHALL NOT be
         * any fee-exempt keys.  In particular, the following keys SHALL NOT be
         * implicitly or automatically added to this list:
         * `adminKey`, `submitKey`, `fee_schedule_key`.
         * @property {Array.<proto.FixedCustomFee.$Properties>|null} [customFees] A set of custom fee definitions.<br/>
         * These are fees to be assessed for each submit to this topic.
         * <p>
         * Each fee defined in this set SHALL be evaluated for
         * each message submitted to this topic, and the resultant
         * total assessed fees SHALL be charged.<br/>
         * Custom fees defined here SHALL be assessed in addition to the base
         * network and node fees.<br/>
         * custom_fees list SHALL NOT contain more than
         * `MAX_CUSTOM_FEE_ENTRIES_FOR_TOPICS` entries.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ConsensusCreateTopicTransactionBody.
         * @memberof proto
         * @interface IConsensusCreateTopicTransactionBody
         * @augments proto.ConsensusCreateTopicTransactionBody.$Properties
         * @deprecated Use proto.ConsensusCreateTopicTransactionBody.$Properties instead.
         */

        /**
         * Shape of a ConsensusCreateTopicTransactionBody.
         * @typedef {{
         *   memo?: string|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   submitKey?: proto.Key.$Shape|null;
         *   autoRenewPeriod?: proto.Duration.$Shape|null;
         *   autoRenewAccount?: proto.AccountID.$Shape|null;
         *   feeScheduleKey?: proto.Key.$Shape|null;
         *   feeExemptKeyList?: Array.<proto.Key.$Shape>|null;
         *   customFees?: Array.<proto.FixedCustomFee.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ConsensusCreateTopicTransactionBody.$Shape
         */

        /**
         * Constructs a new ConsensusCreateTopicTransactionBody.
         * @memberof proto
         * @classdesc Create a topic to accept and group consensus messages.
         * 
         * If `autoRenewAccount` is specified, that account Key MUST also sign this
         * transaction.<br/>
         * If `adminKey` is set, that Key MUST sign the transaction.<br/>
         * On success, the resulting `TransactionReceipt` SHALL contain the newly
         * created `TopicId`.
         * 
         * The `autoRenewPeriod` on a topic MUST be set to a value between
         * `autoRenewPeriod.minDuration` and `autoRenewPeriod.maxDuration`. These
         * values are configurable, typically 30 and 92 days.<br/>
         * This also sets the initial expirationTime of the topic.
         * 
         * If no `adminKey` is set on a topic
         * -`autoRenewAccount` SHALL NOT be set on the topic.
         * - A `deleteTopic` transaction SHALL fail.
         * - An `updateTopic` transaction that only extends the expirationTime MAY
         * succeed.
         * - Any other `updateTopic` transaction SHALL fail.
         * 
         * If the topic expires and is not automatically renewed, the topic SHALL enter
         * the `EXPIRED` state.
         * - All transactions on the topic SHALL fail with TOPIC_EXPIRED
         * - Except an updateTopic() call that only extends the expirationTime.
         * - getTopicInfo() SHALL succeed, and show the topic is expired.
         * The topic SHALL remain in the `EXPIRED` state for a time determined by the
         * `autorenew.gracePeriod` (configurable, originally 7 days).<br/>
         * After the grace period, if the topic's expirationTime is not extended, the
         * topic SHALL be automatically deleted from state entirely, and cannot be
         * recovered or recreated.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.ConsensusCreateTopicTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ConsensusCreateTopicTransactionBody = function (properties) {
            this.feeExemptKeyList = [];
            this.customFees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A short memo for this topic.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @instance
         */
        ConsensusCreateTopicTransactionBody.prototype.memo = "";

        /**
         * Access control for modification of the topic after it is created.
         * <p>
         * If this field is set, that key MUST sign this transaction.<br/>
         * If this field is set, that key MUST sign each future transaction to
         * update or delete the topic.<br/>
         * An updateTopic transaction that _only_ extends the topic expirationTime
         * (a "manual renewal" transaction) SHALL NOT require admin key
         * signature.<br/>
         * A topic without an admin key SHALL be immutable, except for expiration
         * and renewal.<br/>
         * If adminKey is not set, then `autoRenewAccount` SHALL NOT be set.
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @instance
         */
        ConsensusCreateTopicTransactionBody.prototype.adminKey = null;

        /**
         * Access control for message submission to the topic.
         * <p>
         * If this field is set, that key MUST sign each consensus submit message
         * for this topic.<br/>
         * If this field is not set then any account may submit a message on the
         * topic, without restriction.
         * @member {proto.Key.$Properties|null|undefined} submitKey
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @instance
         */
        ConsensusCreateTopicTransactionBody.prototype.submitKey = null;

        /**
         * The initial lifetime, in seconds, for the topic.<br/>
         * This is also the number of seconds for which the topic SHALL be
         * automatically renewed upon expiring, if it has a valid auto-renew
         * account.
         * <p>
         * This value MUST be set.<br/>
         * This value MUST be greater than the configured
         * MIN_AUTORENEW_PERIOD.<br/>
         * This value MUST be less than the configured MAX_AUTORENEW_PERIOD.
         * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @instance
         */
        ConsensusCreateTopicTransactionBody.prototype.autoRenewPeriod = null;

        /**
         * The ID of the account to be charged renewal fees at the topic's
         * expirationTime to extend the lifetime of the topic.
         * <p>
         * The topic lifetime SHALL be extended by the smallest of the following:
         * <ul>
         * <li>The current `autoRenewPeriod` duration.</li>
         * <li>The maximum duration that this account has funds to purchase.</li>
         * <li>The configured MAX_AUTORENEW_PERIOD at the time of automatic
         * renewal.</li>
         * </ul>
         * If this value is set, the referenced account MUST sign this
         * transaction.<br/>
         * If this value is set, the `adminKey` field MUST also be set (though that
         * key MAY not have any correlation to this account).
         * @member {proto.AccountID.$Properties|null|undefined} autoRenewAccount
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @instance
         */
        ConsensusCreateTopicTransactionBody.prototype.autoRenewAccount = null;

        /**
         * Access control for update or delete of custom fees.
         * <p>
         * If set, subsequent `consensus_update_topic` transactions signed with this
         * key MAY update or delete the custom fees for this topic.<br/>
         * If not set, the custom fees for this topic SHALL BE immutable.<br/>
         * If not set when the topic is created, this field CANNOT be set via
         * update.<br/>
         * If set when the topic is created, this field MAY be changed via update.
         * @member {proto.Key.$Properties|null|undefined} feeScheduleKey
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @instance
         */
        ConsensusCreateTopicTransactionBody.prototype.feeScheduleKey = null;

        /**
         * A set of keys.<br/>
         * Keys in this list are permitted to submit messages to this topic without
         * paying custom fees associated with this topic.
         * <p>
         * If a submit transaction is signed by _any_ key included in this set,
         * custom fees SHALL NOT be charged for that transaction.<br/>
         * This field MUST NOT contain more than 10 keys.<br/>
         * fee_exempt_key_list SHALL NOT contain any duplicate keys.<br/>
         * fee_exempt_key_list MAY contain keys for accounts that are inactive,
         * deleted, or non-existent.<br/>
         * If fee_exempt_key_list is unset in this transaction, there SHALL NOT be
         * any fee-exempt keys.  In particular, the following keys SHALL NOT be
         * implicitly or automatically added to this list:
         * `adminKey`, `submitKey`, `fee_schedule_key`.
         * @member {Array.<proto.Key.$Properties>} feeExemptKeyList
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @instance
         */
        ConsensusCreateTopicTransactionBody.prototype.feeExemptKeyList = $util.emptyArray;

        /**
         * A set of custom fee definitions.<br/>
         * These are fees to be assessed for each submit to this topic.
         * <p>
         * Each fee defined in this set SHALL be evaluated for
         * each message submitted to this topic, and the resultant
         * total assessed fees SHALL be charged.<br/>
         * Custom fees defined here SHALL be assessed in addition to the base
         * network and node fees.<br/>
         * custom_fees list SHALL NOT contain more than
         * `MAX_CUSTOM_FEE_ENTRIES_FOR_TOPICS` entries.
         * @member {Array.<proto.FixedCustomFee.$Properties>} customFees
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @instance
         */
        ConsensusCreateTopicTransactionBody.prototype.customFees = $util.emptyArray;

        /**
         * Creates a new ConsensusCreateTopicTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @static
         * @param {proto.ConsensusCreateTopicTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.ConsensusCreateTopicTransactionBody} ConsensusCreateTopicTransactionBody instance
         * @type {{
         *   (properties: proto.ConsensusCreateTopicTransactionBody.$Shape): proto.ConsensusCreateTopicTransactionBody & proto.ConsensusCreateTopicTransactionBody.$Shape;
         *   (properties?: proto.ConsensusCreateTopicTransactionBody.$Properties): proto.ConsensusCreateTopicTransactionBody;
         * }}
         */
        ConsensusCreateTopicTransactionBody.create = function(properties) {
            return new ConsensusCreateTopicTransactionBody(properties);
        };

        /**
         * Encodes the specified ConsensusCreateTopicTransactionBody message. Does not implicitly {@link proto.ConsensusCreateTopicTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @static
         * @param {proto.ConsensusCreateTopicTransactionBody.$Properties} message ConsensusCreateTopicTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusCreateTopicTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.memo);
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.submitKey != null && $Object.hasOwnProperty.call(message, "submitKey"))
                $root.proto.Key.encode(message.submitKey, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount"))
                $root.proto.AccountID.encode(message.autoRenewAccount, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                $root.proto.Key.encode(message.feeScheduleKey, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.feeExemptKeyList != null && message.feeExemptKeyList.length)
                for (let i = 0; i < message.feeExemptKeyList.length; ++i)
                    $root.proto.Key.encode(message.feeExemptKeyList[i], writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.customFees != null && message.customFees.length)
                for (let i = 0; i < message.customFees.length; ++i)
                    $root.proto.FixedCustomFee.encode(message.customFees[i], writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConsensusCreateTopicTransactionBody message, length delimited. Does not implicitly {@link proto.ConsensusCreateTopicTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @static
         * @param {proto.ConsensusCreateTopicTransactionBody.$Properties} message ConsensusCreateTopicTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusCreateTopicTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ConsensusCreateTopicTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ConsensusCreateTopicTransactionBody & proto.ConsensusCreateTopicTransactionBody.$Shape} ConsensusCreateTopicTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusCreateTopicTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ConsensusCreateTopicTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.submitKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.submitKey);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewAccount = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewAccount);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.feeScheduleKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeScheduleKey);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.feeExemptKeyList && message.feeExemptKeyList.length))
                            message.feeExemptKeyList = [];
                        message.feeExemptKeyList.push($root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.customFees && message.customFees.length))
                            message.customFees = [];
                        message.customFees.push($root.proto.FixedCustomFee.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConsensusCreateTopicTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ConsensusCreateTopicTransactionBody & proto.ConsensusCreateTopicTransactionBody.$Shape} ConsensusCreateTopicTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusCreateTopicTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConsensusCreateTopicTransactionBody message.
         * @function verify
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConsensusCreateTopicTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.submitKey != null && $Object.hasOwnProperty.call(message, "submitKey")) {
                let error = $root.proto.Key.verify(message.submitKey, _depth + 1);
                if (error)
                    return "submitKey." + error;
            }
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                if (error)
                    return "autoRenewPeriod." + error;
            }
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount")) {
                let error = $root.proto.AccountID.verify(message.autoRenewAccount, _depth + 1);
                if (error)
                    return "autoRenewAccount." + error;
            }
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey")) {
                let error = $root.proto.Key.verify(message.feeScheduleKey, _depth + 1);
                if (error)
                    return "feeScheduleKey." + error;
            }
            if (message.feeExemptKeyList != null && $Object.hasOwnProperty.call(message, "feeExemptKeyList")) {
                if (!$Array.isArray(message.feeExemptKeyList))
                    return "feeExemptKeyList: array expected";
                for (let i = 0; i < message.feeExemptKeyList.length; ++i) {
                    let error = $root.proto.Key.verify(message.feeExemptKeyList[i], _depth + 1);
                    if (error)
                        return "feeExemptKeyList." + error;
                }
            }
            if (message.customFees != null && $Object.hasOwnProperty.call(message, "customFees")) {
                if (!$Array.isArray(message.customFees))
                    return "customFees: array expected";
                for (let i = 0; i < message.customFees.length; ++i) {
                    let error = $root.proto.FixedCustomFee.verify(message.customFees[i], _depth + 1);
                    if (error)
                        return "customFees." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ConsensusCreateTopicTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ConsensusCreateTopicTransactionBody} ConsensusCreateTopicTransactionBody
         */
        ConsensusCreateTopicTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ConsensusCreateTopicTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ConsensusCreateTopicTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ConsensusCreateTopicTransactionBody();
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.ConsensusCreateTopicTransactionBody.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.submitKey != null) {
                if (!$util.isObject(object.submitKey))
                    throw $TypeError(".proto.ConsensusCreateTopicTransactionBody.submitKey: object expected");
                message.submitKey = $root.proto.Key.fromObject(object.submitKey, _depth + 1);
            }
            if (object.autoRenewPeriod != null) {
                if (!$util.isObject(object.autoRenewPeriod))
                    throw $TypeError(".proto.ConsensusCreateTopicTransactionBody.autoRenewPeriod: object expected");
                message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
            }
            if (object.autoRenewAccount != null) {
                if (!$util.isObject(object.autoRenewAccount))
                    throw $TypeError(".proto.ConsensusCreateTopicTransactionBody.autoRenewAccount: object expected");
                message.autoRenewAccount = $root.proto.AccountID.fromObject(object.autoRenewAccount, _depth + 1);
            }
            if (object.feeScheduleKey != null) {
                if (!$util.isObject(object.feeScheduleKey))
                    throw $TypeError(".proto.ConsensusCreateTopicTransactionBody.feeScheduleKey: object expected");
                message.feeScheduleKey = $root.proto.Key.fromObject(object.feeScheduleKey, _depth + 1);
            }
            if (object.feeExemptKeyList) {
                if (!$Array.isArray(object.feeExemptKeyList))
                    throw $TypeError(".proto.ConsensusCreateTopicTransactionBody.feeExemptKeyList: array expected");
                message.feeExemptKeyList = $Array(object.feeExemptKeyList.length);
                for (let i = 0; i < object.feeExemptKeyList.length; ++i) {
                    if (!$util.isObject(object.feeExemptKeyList[i]))
                        throw $TypeError(".proto.ConsensusCreateTopicTransactionBody.feeExemptKeyList: object expected");
                    message.feeExemptKeyList[i] = $root.proto.Key.fromObject(object.feeExemptKeyList[i], _depth + 1);
                }
            }
            if (object.customFees) {
                if (!$Array.isArray(object.customFees))
                    throw $TypeError(".proto.ConsensusCreateTopicTransactionBody.customFees: array expected");
                message.customFees = $Array(object.customFees.length);
                for (let i = 0; i < object.customFees.length; ++i) {
                    if (!$util.isObject(object.customFees[i]))
                        throw $TypeError(".proto.ConsensusCreateTopicTransactionBody.customFees: object expected");
                    message.customFees[i] = $root.proto.FixedCustomFee.fromObject(object.customFees[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ConsensusCreateTopicTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @static
         * @param {proto.ConsensusCreateTopicTransactionBody} message ConsensusCreateTopicTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConsensusCreateTopicTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.feeExemptKeyList = [];
                object.customFees = [];
            }
            if (options.defaults) {
                object.memo = "";
                object.adminKey = null;
                object.submitKey = null;
                object.autoRenewPeriod = null;
                object.autoRenewAccount = null;
                object.feeScheduleKey = null;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.submitKey != null && $Object.hasOwnProperty.call(message, "submitKey"))
                object.submitKey = $root.proto.Key.toObject(message.submitKey, options, _depth + 1);
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount"))
                object.autoRenewAccount = $root.proto.AccountID.toObject(message.autoRenewAccount, options, _depth + 1);
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                object.feeScheduleKey = $root.proto.Key.toObject(message.feeScheduleKey, options, _depth + 1);
            if (message.feeExemptKeyList && message.feeExemptKeyList.length) {
                object.feeExemptKeyList = $Array(message.feeExemptKeyList.length);
                for (let j = 0; j < message.feeExemptKeyList.length; ++j)
                    object.feeExemptKeyList[j] = $root.proto.Key.toObject(message.feeExemptKeyList[j], options, _depth + 1);
            }
            if (message.customFees && message.customFees.length) {
                object.customFees = $Array(message.customFees.length);
                for (let j = 0; j < message.customFees.length; ++j)
                    object.customFees[j] = $root.proto.FixedCustomFee.toObject(message.customFees[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ConsensusCreateTopicTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConsensusCreateTopicTransactionBody.prototype.toJSON = function() {
            return ConsensusCreateTopicTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConsensusCreateTopicTransactionBody
         * @function getTypeUrl
         * @memberof proto.ConsensusCreateTopicTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConsensusCreateTopicTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ConsensusCreateTopicTransactionBody";
        };

        return ConsensusCreateTopicTransactionBody;
    })();

    proto.FractionalFee = (function() {

        /**
         * Properties of a FractionalFee.
         * @typedef {Object} proto.FractionalFee.$Properties
         * @property {proto.Fraction.$Properties|null} [fractionalAmount] A Fraction of the transferred tokens to assess as a fee.<br/>
         * This value MUST be less than or equal to one.<br/>
         * This value MUST be greater than zero.
         * @property {number|Long|null} [minimumAmount] A minimum fee to charge, in units of 10<sup>-decimals</sup> tokens.
         * <p>
         * This value is OPTIONAL, with a default of `0` indicating no minimum.<br/>
         * If set, this value MUST be greater than zero.<br/>
         * If set, all transfers SHALL pay at least this amount.
         * @property {number|Long|null} [maximumAmount] A maximum fee to charge, in units of 10<sup>-decimals</sup> tokens.
         * <p>
         * This value is OPTIONAL, with a default of `0` indicating no maximum.<br/>
         * If set, this value MUST be greater than zero.<br/>
         * If set, any fee charged SHALL NOT exceed this value.<br/>
         * This value SHOULD be strictly greater than `minimum_amount`.
         * If this amount is less than or equal to `minimum_amount`, then
         * the fee charged SHALL always be equal to this value and
         * `fractional_amount` SHALL NOT have any effect.
         * @property {boolean|null} [netOfTransfers] Flag requesting to assess the calculated fee against the sender,
         * without reducing the amount transferred.<br/>
         * #### Effects of this flag
         * <ol>
         * <li>If this value is true
         * <ul>
         * <li>The receiver of a transfer SHALL receive the entire
         * amount sent.</li>
         * <li>The fee SHALL be charged to the sender as an additional
         * amount, increasing the token transfer debit.</li>
         * </ul>
         * </li>
         * <li>If this value is false
         * <ul>
         * <li>The receiver of a transfer SHALL receive the amount sent
         * _after_ deduction of the calculated fee.</li>
         * </ul>
         * </li>
         * </ol>
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FractionalFee.
         * @memberof proto
         * @interface IFractionalFee
         * @augments proto.FractionalFee.$Properties
         * @deprecated Use proto.FractionalFee.$Properties instead.
         */

        /**
         * Shape of a FractionalFee.
         * @typedef {proto.FractionalFee.$Properties} proto.FractionalFee.$Shape
         */

        /**
         * Constructs a new FractionalFee.
         * @memberof proto
         * @classdesc A descriptor for a fee based on a portion of the tokens transferred.
         * 
         * This fee option describes fees as a fraction of the amount of
         * fungible/common token(s) transferred.  The fee also describes a minimum
         * and maximum amount, both of which are OPTIONAL.
         * 
         * This type of fee SHALL be assessed only for fungible/common tokens.<br/>
         * This type of fee MUST NOT be defined for a non-fungible/unique
         * token type.<br/>
         * This fee SHALL be paid with the same type of tokens as those
         * transferred.<br/>
         * The fee MAY be subtracted from the transferred tokens, or MAY be assessed
         * to the sender in addition to the tokens actually transferred, based on
         * the `net_of_transfers` field.
         * 
         * When a single transaction sends tokens from one sender to multiple
         * recipients, and the `net_of_transfers` flag is false, the network
         * SHALL attempt to evenly assess the total fee across all recipients
         * proportionally. This may be inexact and, particularly when there are
         * large differences between recipients, MAY result in small deviations
         * from an ideal "fair" distribution.<br/>
         * If the sender lacks sufficient tokens to pay fees, or the assessment
         * of custom fees reduces the net amount transferred to or below zero,
         * the transaction MAY fail due to insufficient funds to pay all fees.
         * @constructor
         * @param {proto.FractionalFee.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FractionalFee = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A Fraction of the transferred tokens to assess as a fee.<br/>
         * This value MUST be less than or equal to one.<br/>
         * This value MUST be greater than zero.
         * @member {proto.Fraction.$Properties|null|undefined} fractionalAmount
         * @memberof proto.FractionalFee
         * @instance
         */
        FractionalFee.prototype.fractionalAmount = null;

        /**
         * A minimum fee to charge, in units of 10<sup>-decimals</sup> tokens.
         * <p>
         * This value is OPTIONAL, with a default of `0` indicating no minimum.<br/>
         * If set, this value MUST be greater than zero.<br/>
         * If set, all transfers SHALL pay at least this amount.
         * @member {number|Long} minimumAmount
         * @memberof proto.FractionalFee
         * @instance
         */
        FractionalFee.prototype.minimumAmount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A maximum fee to charge, in units of 10<sup>-decimals</sup> tokens.
         * <p>
         * This value is OPTIONAL, with a default of `0` indicating no maximum.<br/>
         * If set, this value MUST be greater than zero.<br/>
         * If set, any fee charged SHALL NOT exceed this value.<br/>
         * This value SHOULD be strictly greater than `minimum_amount`.
         * If this amount is less than or equal to `minimum_amount`, then
         * the fee charged SHALL always be equal to this value and
         * `fractional_amount` SHALL NOT have any effect.
         * @member {number|Long} maximumAmount
         * @memberof proto.FractionalFee
         * @instance
         */
        FractionalFee.prototype.maximumAmount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Flag requesting to assess the calculated fee against the sender,
         * without reducing the amount transferred.<br/>
         * #### Effects of this flag
         * <ol>
         * <li>If this value is true
         * <ul>
         * <li>The receiver of a transfer SHALL receive the entire
         * amount sent.</li>
         * <li>The fee SHALL be charged to the sender as an additional
         * amount, increasing the token transfer debit.</li>
         * </ul>
         * </li>
         * <li>If this value is false
         * <ul>
         * <li>The receiver of a transfer SHALL receive the amount sent
         * _after_ deduction of the calculated fee.</li>
         * </ul>
         * </li>
         * </ol>
         * @member {boolean} netOfTransfers
         * @memberof proto.FractionalFee
         * @instance
         */
        FractionalFee.prototype.netOfTransfers = false;

        /**
         * Creates a new FractionalFee instance using the specified properties.
         * @function create
         * @memberof proto.FractionalFee
         * @static
         * @param {proto.FractionalFee.$Properties=} [properties] Properties to set
         * @returns {proto.FractionalFee} FractionalFee instance
         * @type {{
         *   (properties: proto.FractionalFee.$Shape): proto.FractionalFee & proto.FractionalFee.$Shape;
         *   (properties?: proto.FractionalFee.$Properties): proto.FractionalFee;
         * }}
         */
        FractionalFee.create = function(properties) {
            return new FractionalFee(properties);
        };

        /**
         * Encodes the specified FractionalFee message. Does not implicitly {@link proto.FractionalFee.verify|verify} messages.
         * @function encode
         * @memberof proto.FractionalFee
         * @static
         * @param {proto.FractionalFee.$Properties} message FractionalFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FractionalFee.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fractionalAmount != null && $Object.hasOwnProperty.call(message, "fractionalAmount"))
                $root.proto.Fraction.encode(message.fractionalAmount, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.minimumAmount != null && $Object.hasOwnProperty.call(message, "minimumAmount") && (typeof message.minimumAmount === "object" ? message.minimumAmount.low || message.minimumAmount.high : message.minimumAmount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.minimumAmount);
            if (message.maximumAmount != null && $Object.hasOwnProperty.call(message, "maximumAmount") && (typeof message.maximumAmount === "object" ? message.maximumAmount.low || message.maximumAmount.high : message.maximumAmount !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.maximumAmount);
            if (message.netOfTransfers != null && $Object.hasOwnProperty.call(message, "netOfTransfers") && message.netOfTransfers !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.netOfTransfers);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FractionalFee message, length delimited. Does not implicitly {@link proto.FractionalFee.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FractionalFee
         * @static
         * @param {proto.FractionalFee.$Properties} message FractionalFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FractionalFee.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FractionalFee message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FractionalFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FractionalFee & proto.FractionalFee.$Shape} FractionalFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FractionalFee.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FractionalFee();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.fractionalAmount = $root.proto.Fraction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fractionalAmount);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.minimumAmount = value;
                        else
                            delete message.minimumAmount;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maximumAmount = value;
                        else
                            delete message.maximumAmount;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.netOfTransfers = value;
                        else
                            delete message.netOfTransfers;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FractionalFee message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FractionalFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FractionalFee & proto.FractionalFee.$Shape} FractionalFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FractionalFee.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FractionalFee message.
         * @function verify
         * @memberof proto.FractionalFee
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FractionalFee.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fractionalAmount != null && $Object.hasOwnProperty.call(message, "fractionalAmount")) {
                let error = $root.proto.Fraction.verify(message.fractionalAmount, _depth + 1);
                if (error)
                    return "fractionalAmount." + error;
            }
            if (message.minimumAmount != null && $Object.hasOwnProperty.call(message, "minimumAmount"))
                if (!$util.isInteger(message.minimumAmount) && !(message.minimumAmount && $util.isInteger(message.minimumAmount.low) && $util.isInteger(message.minimumAmount.high)))
                    return "minimumAmount: integer|Long expected";
            if (message.maximumAmount != null && $Object.hasOwnProperty.call(message, "maximumAmount"))
                if (!$util.isInteger(message.maximumAmount) && !(message.maximumAmount && $util.isInteger(message.maximumAmount.low) && $util.isInteger(message.maximumAmount.high)))
                    return "maximumAmount: integer|Long expected";
            if (message.netOfTransfers != null && $Object.hasOwnProperty.call(message, "netOfTransfers"))
                if (typeof message.netOfTransfers !== "boolean")
                    return "netOfTransfers: boolean expected";
            return null;
        };

        /**
         * Creates a FractionalFee message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FractionalFee
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FractionalFee} FractionalFee
         */
        FractionalFee.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FractionalFee)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FractionalFee: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FractionalFee();
            if (object.fractionalAmount != null) {
                if (!$util.isObject(object.fractionalAmount))
                    throw $TypeError(".proto.FractionalFee.fractionalAmount: object expected");
                message.fractionalAmount = $root.proto.Fraction.fromObject(object.fractionalAmount, _depth + 1);
            }
            if (object.minimumAmount != null)
                if (typeof object.minimumAmount === "object" ? object.minimumAmount.low || object.minimumAmount.high : $Number(object.minimumAmount) !== 0)
                    if ($util.Long)
                        message.minimumAmount = $util.Long.fromValue(object.minimumAmount, false);
                    else if (typeof object.minimumAmount === "string")
                        message.minimumAmount = $parseInt(object.minimumAmount, 10);
                    else if (typeof object.minimumAmount === "number")
                        message.minimumAmount = object.minimumAmount;
                    else if (typeof object.minimumAmount === "object")
                        message.minimumAmount = new $util.LongBits(object.minimumAmount.low >>> 0, object.minimumAmount.high >>> 0).toNumber();
            if (object.maximumAmount != null)
                if (typeof object.maximumAmount === "object" ? object.maximumAmount.low || object.maximumAmount.high : $Number(object.maximumAmount) !== 0)
                    if ($util.Long)
                        message.maximumAmount = $util.Long.fromValue(object.maximumAmount, false);
                    else if (typeof object.maximumAmount === "string")
                        message.maximumAmount = $parseInt(object.maximumAmount, 10);
                    else if (typeof object.maximumAmount === "number")
                        message.maximumAmount = object.maximumAmount;
                    else if (typeof object.maximumAmount === "object")
                        message.maximumAmount = new $util.LongBits(object.maximumAmount.low >>> 0, object.maximumAmount.high >>> 0).toNumber();
            if (object.netOfTransfers != null)
                if (object.netOfTransfers)
                    message.netOfTransfers = $Boolean(object.netOfTransfers);
            return message;
        };

        /**
         * Creates a plain object from a FractionalFee message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FractionalFee
         * @static
         * @param {proto.FractionalFee} message FractionalFee
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FractionalFee.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.fractionalAmount = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.minimumAmount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.minimumAmount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maximumAmount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maximumAmount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.netOfTransfers = false;
            }
            if (message.fractionalAmount != null && $Object.hasOwnProperty.call(message, "fractionalAmount"))
                object.fractionalAmount = $root.proto.Fraction.toObject(message.fractionalAmount, options, _depth + 1);
            if (message.minimumAmount != null && $Object.hasOwnProperty.call(message, "minimumAmount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.minimumAmount = typeof message.minimumAmount === "number" ? $BigInt(message.minimumAmount) : $util.Long.fromBits(message.minimumAmount.low >>> 0, message.minimumAmount.high >>> 0, false).toBigInt();
                else if (typeof message.minimumAmount === "number")
                    object.minimumAmount = options.longs === $String ? $String(message.minimumAmount) : message.minimumAmount;
                else
                    object.minimumAmount = options.longs === $String ? $util.Long.prototype.toString.call(message.minimumAmount) : options.longs === $Number ? new $util.LongBits(message.minimumAmount.low >>> 0, message.minimumAmount.high >>> 0).toNumber() : message.minimumAmount;
            if (message.maximumAmount != null && $Object.hasOwnProperty.call(message, "maximumAmount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maximumAmount = typeof message.maximumAmount === "number" ? $BigInt(message.maximumAmount) : $util.Long.fromBits(message.maximumAmount.low >>> 0, message.maximumAmount.high >>> 0, false).toBigInt();
                else if (typeof message.maximumAmount === "number")
                    object.maximumAmount = options.longs === $String ? $String(message.maximumAmount) : message.maximumAmount;
                else
                    object.maximumAmount = options.longs === $String ? $util.Long.prototype.toString.call(message.maximumAmount) : options.longs === $Number ? new $util.LongBits(message.maximumAmount.low >>> 0, message.maximumAmount.high >>> 0).toNumber() : message.maximumAmount;
            if (message.netOfTransfers != null && $Object.hasOwnProperty.call(message, "netOfTransfers"))
                object.netOfTransfers = message.netOfTransfers;
            return object;
        };

        /**
         * Converts this FractionalFee to JSON.
         * @function toJSON
         * @memberof proto.FractionalFee
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FractionalFee.prototype.toJSON = function() {
            return FractionalFee.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FractionalFee
         * @function getTypeUrl
         * @memberof proto.FractionalFee
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FractionalFee.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FractionalFee";
        };

        return FractionalFee;
    })();

    proto.FixedFee = (function() {

        /**
         * Properties of a FixedFee.
         * @typedef {Object} proto.FixedFee.$Properties
         * @property {number|Long|null} [amount] The amount to assess for each transfer.
         * <p>
         * This value MUST be greater than `0`.<br/>
         * This amount is expressed in units of 10<sup>-decimals</sup> tokens.
         * @property {proto.TokenID.$Properties|null} [denominatingTokenId] The token type used to pay the assessed fee.
         * <p>
         * If this is unset, the fee SHALL be assessed in HBAR.<br/>
         * If this is set, the fee SHALL be assessed in the token identified.
         * This MAY be any token type. Custom fees assessed in other token types
         * are more likely to fail, however, and it is RECOMMENDED that token
         * creators denominate custom fees in the transferred token, HBAR, or
         * well documented and closely related token types.<br/>
         * If this value is set to `0.0.0` in the `tokenCreate` transaction, it
         * SHALL be replaced with the `TokenID` of the newly created token.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FixedFee.
         * @memberof proto
         * @interface IFixedFee
         * @augments proto.FixedFee.$Properties
         * @deprecated Use proto.FixedFee.$Properties instead.
         */

        /**
         * Shape of a FixedFee.
         * @typedef {proto.FixedFee.$Properties} proto.FixedFee.$Shape
         */

        /**
         * Constructs a new FixedFee.
         * @memberof proto
         * @classdesc A fixed fee to assess for each token transfer, regardless of the
         * amount transferred.<br/>
         * This fee type describes a fixed fee for each transfer of a token type.
         * 
         * The fee SHALL be charged to the `sender` for the token transfer
         * transaction.<br/>
         * This fee MAY be assessed in HBAR, the token type transferred, or any
         * other token type, as determined by the `denominating_token_id` field.
         * @constructor
         * @param {proto.FixedFee.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FixedFee = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The amount to assess for each transfer.
         * <p>
         * This value MUST be greater than `0`.<br/>
         * This amount is expressed in units of 10<sup>-decimals</sup> tokens.
         * @member {number|Long} amount
         * @memberof proto.FixedFee
         * @instance
         */
        FixedFee.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The token type used to pay the assessed fee.
         * <p>
         * If this is unset, the fee SHALL be assessed in HBAR.<br/>
         * If this is set, the fee SHALL be assessed in the token identified.
         * This MAY be any token type. Custom fees assessed in other token types
         * are more likely to fail, however, and it is RECOMMENDED that token
         * creators denominate custom fees in the transferred token, HBAR, or
         * well documented and closely related token types.<br/>
         * If this value is set to `0.0.0` in the `tokenCreate` transaction, it
         * SHALL be replaced with the `TokenID` of the newly created token.
         * @member {proto.TokenID.$Properties|null|undefined} denominatingTokenId
         * @memberof proto.FixedFee
         * @instance
         */
        FixedFee.prototype.denominatingTokenId = null;

        /**
         * Creates a new FixedFee instance using the specified properties.
         * @function create
         * @memberof proto.FixedFee
         * @static
         * @param {proto.FixedFee.$Properties=} [properties] Properties to set
         * @returns {proto.FixedFee} FixedFee instance
         * @type {{
         *   (properties: proto.FixedFee.$Shape): proto.FixedFee & proto.FixedFee.$Shape;
         *   (properties?: proto.FixedFee.$Properties): proto.FixedFee;
         * }}
         */
        FixedFee.create = function(properties) {
            return new FixedFee(properties);
        };

        /**
         * Encodes the specified FixedFee message. Does not implicitly {@link proto.FixedFee.verify|verify} messages.
         * @function encode
         * @memberof proto.FixedFee
         * @static
         * @param {proto.FixedFee.$Properties} message FixedFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FixedFee.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.amount);
            if (message.denominatingTokenId != null && $Object.hasOwnProperty.call(message, "denominatingTokenId"))
                $root.proto.TokenID.encode(message.denominatingTokenId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FixedFee message, length delimited. Does not implicitly {@link proto.FixedFee.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FixedFee
         * @static
         * @param {proto.FixedFee.$Properties} message FixedFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FixedFee.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FixedFee message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FixedFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FixedFee & proto.FixedFee.$Shape} FixedFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FixedFee.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FixedFee();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.denominatingTokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.denominatingTokenId);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FixedFee message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FixedFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FixedFee & proto.FixedFee.$Shape} FixedFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FixedFee.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FixedFee message.
         * @function verify
         * @memberof proto.FixedFee
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FixedFee.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.denominatingTokenId != null && $Object.hasOwnProperty.call(message, "denominatingTokenId")) {
                let error = $root.proto.TokenID.verify(message.denominatingTokenId, _depth + 1);
                if (error)
                    return "denominatingTokenId." + error;
            }
            return null;
        };

        /**
         * Creates a FixedFee message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FixedFee
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FixedFee} FixedFee
         */
        FixedFee.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FixedFee)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FixedFee: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FixedFee();
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            if (object.denominatingTokenId != null) {
                if (!$util.isObject(object.denominatingTokenId))
                    throw $TypeError(".proto.FixedFee.denominatingTokenId: object expected");
                message.denominatingTokenId = $root.proto.TokenID.fromObject(object.denominatingTokenId, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a FixedFee message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FixedFee
         * @static
         * @param {proto.FixedFee} message FixedFee
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FixedFee.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.denominatingTokenId = null;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            if (message.denominatingTokenId != null && $Object.hasOwnProperty.call(message, "denominatingTokenId"))
                object.denominatingTokenId = $root.proto.TokenID.toObject(message.denominatingTokenId, options, _depth + 1);
            return object;
        };

        /**
         * Converts this FixedFee to JSON.
         * @function toJSON
         * @memberof proto.FixedFee
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FixedFee.prototype.toJSON = function() {
            return FixedFee.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FixedFee
         * @function getTypeUrl
         * @memberof proto.FixedFee
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FixedFee.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FixedFee";
        };

        return FixedFee;
    })();

    proto.RoyaltyFee = (function() {

        /**
         * Properties of a RoyaltyFee.
         * @typedef {Object} proto.RoyaltyFee.$Properties
         * @property {proto.Fraction.$Properties|null} [exchangeValueFraction] The fraction of fungible value exchanged for an NFT to collect
         * as royalty.
         * <p>
         * This SHALL be applied once to the total fungible value transferred
         * for the transaction.<br/>
         * There SHALL NOT be any adjustment based on multiple transfers
         * involving the NFT sender as part of a single transaction.
         * @property {proto.FixedFee.$Properties|null} [fallbackFee] A fixed fee to assess if no fungible value is known to be traded
         * for the NFT.
         * <p>
         * If an NFT is transferred without a corresponding transfer of
         * _fungible_ value returned in the same transaction, the network
         * SHALL charge this fee as a fallback.<br/>
         * Fallback fees MAY have unexpected effects when interacting with
         * escrow, market transfers, and smart contracts.
         * It is RECOMMENDED that developers carefully consider possible
         * effects from fallback fees when designing systems that facilitate
         * the transfer of NFTs.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RoyaltyFee.
         * @memberof proto
         * @interface IRoyaltyFee
         * @augments proto.RoyaltyFee.$Properties
         * @deprecated Use proto.RoyaltyFee.$Properties instead.
         */

        /**
         * Shape of a RoyaltyFee.
         * @typedef {proto.RoyaltyFee.$Properties} proto.RoyaltyFee.$Shape
         */

        /**
         * Constructs a new RoyaltyFee.
         * @memberof proto
         * @classdesc A fee to assess during a CryptoTransfer that changes ownership of a
         * non-fungible/unique (NFT) token.<br/>
         * This message defines the fraction of the fungible value exchanged for an
         * NFT that the ledger should collect as a royalty.
         * "Fungible value" includes both HBAR (ℏ) and units of fungible HTS tokens.
         * When the NFT sender does not receive any fungible value, the ledger will
         * assess the fallback fee, if present, to the new NFT owner. Royalty fees
         * can only be added to non-fungible/unique tokens.
         * 
         * #### Important Note
         * > Users should be aware that native royalty fees are _strictly_ a
         * > convenience feature, SHALL NOT be guaranteed, and the network SHALL NOT
         * > enforce _inescapable_ royalties on the exchange of a unique NFT.<br/>
         * > For _one_ example, if the counterparties agree to split their value
         * > transfer and NFT exchange into separate transactions, the network cannot
         * > possibly determine the value exchanged. Even trustless transactions,
         * > using a smart contract or other form of escrow, can arrange such split
         * > transactions as a single _logical_ transfer.
         * 
         * Counterparties that wish to _respect_ creator royalties MUST follow the
         * pattern the network recognizes.
         * <div style="margin-left: 2em; margin-top: -0.8em">
         * A single transaction MUST contain all three elements, transfer of the NFT,
         * debit of fungible value from the receiver, and credit of fungible value to
         * the sender, in order for the network to accurately assess royalty fees.
         * </div>
         * <div style="margin-left: 1em; margin-top: -0.8em">
         * Two examples are presented here.
         * <div style="margin-left: 1em">
         * The NFT sender and receiver MUST both sign a single `cryptoTransfer` that
         * transfers the NFT from sender to receiver, debits the fungible value from
         * the receiver, and credits the sender with the fungible value the receiver
         * is exchanging for the NFT.<br/>
         * A marketplace using an approved spender account for an escrow transaction
         * MUST credit the account selling the NFT in the same `cryptoTransfer`
         * transaction that transfers the NFT to, and deducts fungible value from,
         * the buying account.
         * </div></div>
         * This type of fee MAY NOT produce accurate results if multiple transfers
         * are executed in a single transaction. It is RECOMMENDED that each
         * NFT subject to royalty fees be transferred separately and without
         * unrelated fungible token transfers.
         * 
         * The network SHALL NOT consider third-party transfers, including
         * "approved spender" accounts, in collecting royalty fees. An honest
         * broker MUST ensure that transfer of an NFT and payment delivered to
         * the sender are present in the same transaction.
         * There is an
         * [open suggestion](https://github.com/hashgraph/hedera-improvement-proposal/discussions/578)
         * that proposes to broaden the scope of transfers from which the network
         * automatically collects royalties to cover related third parties. If this
         * interests or concerns you, please add your voice to that discussion.
         * @constructor
         * @param {proto.RoyaltyFee.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RoyaltyFee = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The fraction of fungible value exchanged for an NFT to collect
         * as royalty.
         * <p>
         * This SHALL be applied once to the total fungible value transferred
         * for the transaction.<br/>
         * There SHALL NOT be any adjustment based on multiple transfers
         * involving the NFT sender as part of a single transaction.
         * @member {proto.Fraction.$Properties|null|undefined} exchangeValueFraction
         * @memberof proto.RoyaltyFee
         * @instance
         */
        RoyaltyFee.prototype.exchangeValueFraction = null;

        /**
         * A fixed fee to assess if no fungible value is known to be traded
         * for the NFT.
         * <p>
         * If an NFT is transferred without a corresponding transfer of
         * _fungible_ value returned in the same transaction, the network
         * SHALL charge this fee as a fallback.<br/>
         * Fallback fees MAY have unexpected effects when interacting with
         * escrow, market transfers, and smart contracts.
         * It is RECOMMENDED that developers carefully consider possible
         * effects from fallback fees when designing systems that facilitate
         * the transfer of NFTs.
         * @member {proto.FixedFee.$Properties|null|undefined} fallbackFee
         * @memberof proto.RoyaltyFee
         * @instance
         */
        RoyaltyFee.prototype.fallbackFee = null;

        /**
         * Creates a new RoyaltyFee instance using the specified properties.
         * @function create
         * @memberof proto.RoyaltyFee
         * @static
         * @param {proto.RoyaltyFee.$Properties=} [properties] Properties to set
         * @returns {proto.RoyaltyFee} RoyaltyFee instance
         * @type {{
         *   (properties: proto.RoyaltyFee.$Shape): proto.RoyaltyFee & proto.RoyaltyFee.$Shape;
         *   (properties?: proto.RoyaltyFee.$Properties): proto.RoyaltyFee;
         * }}
         */
        RoyaltyFee.create = function(properties) {
            return new RoyaltyFee(properties);
        };

        /**
         * Encodes the specified RoyaltyFee message. Does not implicitly {@link proto.RoyaltyFee.verify|verify} messages.
         * @function encode
         * @memberof proto.RoyaltyFee
         * @static
         * @param {proto.RoyaltyFee.$Properties} message RoyaltyFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoyaltyFee.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.exchangeValueFraction != null && $Object.hasOwnProperty.call(message, "exchangeValueFraction"))
                $root.proto.Fraction.encode(message.exchangeValueFraction, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.fallbackFee != null && $Object.hasOwnProperty.call(message, "fallbackFee"))
                $root.proto.FixedFee.encode(message.fallbackFee, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RoyaltyFee message, length delimited. Does not implicitly {@link proto.RoyaltyFee.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.RoyaltyFee
         * @static
         * @param {proto.RoyaltyFee.$Properties} message RoyaltyFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoyaltyFee.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RoyaltyFee message from the specified reader or buffer.
         * @function decode
         * @memberof proto.RoyaltyFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.RoyaltyFee & proto.RoyaltyFee.$Shape} RoyaltyFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoyaltyFee.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.RoyaltyFee();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.exchangeValueFraction = $root.proto.Fraction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.exchangeValueFraction);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.fallbackFee = $root.proto.FixedFee.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fallbackFee);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RoyaltyFee message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.RoyaltyFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.RoyaltyFee & proto.RoyaltyFee.$Shape} RoyaltyFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoyaltyFee.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoyaltyFee message.
         * @function verify
         * @memberof proto.RoyaltyFee
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoyaltyFee.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.exchangeValueFraction != null && $Object.hasOwnProperty.call(message, "exchangeValueFraction")) {
                let error = $root.proto.Fraction.verify(message.exchangeValueFraction, _depth + 1);
                if (error)
                    return "exchangeValueFraction." + error;
            }
            if (message.fallbackFee != null && $Object.hasOwnProperty.call(message, "fallbackFee")) {
                let error = $root.proto.FixedFee.verify(message.fallbackFee, _depth + 1);
                if (error)
                    return "fallbackFee." + error;
            }
            return null;
        };

        /**
         * Creates a RoyaltyFee message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.RoyaltyFee
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.RoyaltyFee} RoyaltyFee
         */
        RoyaltyFee.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.RoyaltyFee)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.RoyaltyFee: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.RoyaltyFee();
            if (object.exchangeValueFraction != null) {
                if (!$util.isObject(object.exchangeValueFraction))
                    throw $TypeError(".proto.RoyaltyFee.exchangeValueFraction: object expected");
                message.exchangeValueFraction = $root.proto.Fraction.fromObject(object.exchangeValueFraction, _depth + 1);
            }
            if (object.fallbackFee != null) {
                if (!$util.isObject(object.fallbackFee))
                    throw $TypeError(".proto.RoyaltyFee.fallbackFee: object expected");
                message.fallbackFee = $root.proto.FixedFee.fromObject(object.fallbackFee, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RoyaltyFee message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.RoyaltyFee
         * @static
         * @param {proto.RoyaltyFee} message RoyaltyFee
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoyaltyFee.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.exchangeValueFraction = null;
                object.fallbackFee = null;
            }
            if (message.exchangeValueFraction != null && $Object.hasOwnProperty.call(message, "exchangeValueFraction"))
                object.exchangeValueFraction = $root.proto.Fraction.toObject(message.exchangeValueFraction, options, _depth + 1);
            if (message.fallbackFee != null && $Object.hasOwnProperty.call(message, "fallbackFee"))
                object.fallbackFee = $root.proto.FixedFee.toObject(message.fallbackFee, options, _depth + 1);
            return object;
        };

        /**
         * Converts this RoyaltyFee to JSON.
         * @function toJSON
         * @memberof proto.RoyaltyFee
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoyaltyFee.prototype.toJSON = function() {
            return RoyaltyFee.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RoyaltyFee
         * @function getTypeUrl
         * @memberof proto.RoyaltyFee
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RoyaltyFee.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.RoyaltyFee";
        };

        return RoyaltyFee;
    })();

    proto.CustomFee = (function() {

        /**
         * Properties of a CustomFee.
         * @typedef {Object} proto.CustomFee.$Properties
         * @property {proto.FixedFee.$Properties|null} [fixedFee] A fixed fee to be charged to the `sender` for every token transfer.
         * <p>
         * This type of fee MAY be defined for any token type.<br/>
         * This type of fee MAY be more consistent and reliable than
         * other types.
         * @property {proto.FractionalFee.$Properties|null} [fractionalFee] A fee defined as a fraction of the tokens transferred.
         * <p>
         * This type of fee MUST NOT be defined for a non-fungible/unique
         * token type.<br/>
         * This fee MAY be charged to either sender, as an increase to the
         * amount sent, or receiver, as a reduction to the amount received.
         * @property {proto.RoyaltyFee.$Properties|null} [royaltyFee] A fee charged as royalty for any transfer of a
         * non-fungible/unique token.
         * <p>
         * This type of fee MUST NOT be defined for a
         * fungible/common token type.
         * @property {proto.AccountID.$Properties|null} [feeCollectorAccountId] The account to receive the custom fee.
         * @property {boolean|null} [allCollectorsAreExempt] Flag indicating to exempt all custom fee collector accounts for this
         * token type from paying this custom fee when sending tokens.
         * <p>
         * The treasury account for a token, and the account identified by the
         * `fee_collector_account_id` field of this `CustomFee` are always exempt
         * from this custom fee to avoid redundant and unnecessary transfers.
         * If this value is `true` then the account(s) identified in
         * `fee_collector_account_id` for _all_ custom fee definitions for this
         * token type SHALL also be exempt from this custom fee.
         * This behavior is specified in HIP-573.
         * @property {"fixedFee"|"fractionalFee"|"royaltyFee"} [fee] CustomFee fee
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CustomFee.
         * @memberof proto
         * @interface ICustomFee
         * @augments proto.CustomFee.$Properties
         * @deprecated Use proto.CustomFee.$Properties instead.
         */

        /**
         * Narrowed shape of a CustomFee.
         * @typedef {{
         *   fixedFee?: proto.FixedFee.$Shape|null;
         *   fractionalFee?: proto.FractionalFee.$Shape|null;
         *   royaltyFee?: proto.RoyaltyFee.$Shape|null;
         *   feeCollectorAccountId?: proto.AccountID.$Shape|null;
         *   allCollectorsAreExempt?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ fee?: undefined; fixedFee?: null; fractionalFee?: null; royaltyFee?: null }|{ fee?: "fixedFee"; fixedFee: proto.FixedFee.$Shape; fractionalFee?: null; royaltyFee?: null }|{ fee?: "fractionalFee"; fixedFee?: null; fractionalFee: proto.FractionalFee.$Shape; royaltyFee?: null }|{ fee?: "royaltyFee"; fixedFee?: null; fractionalFee?: null; royaltyFee: proto.RoyaltyFee.$Shape })
         * )} proto.CustomFee.$Shape
         */

        /**
         * Constructs a new CustomFee.
         * @memberof proto
         * @classdesc A transfer fee to assess during a CryptoTransfer.<br/>
         * This fee applies to transactions that transfer units of the token to
         * which the fee is attached. A custom fee may be either fixed or fractional,
         * and must specify a fee collector account to receive the assessed fees.
         * 
         * Custom fees MUST be greater than zero (0).
         * @constructor
         * @param {proto.CustomFee.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CustomFee = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A fixed fee to be charged to the `sender` for every token transfer.
         * <p>
         * This type of fee MAY be defined for any token type.<br/>
         * This type of fee MAY be more consistent and reliable than
         * other types.
         * @member {proto.FixedFee.$Properties|null|undefined} fixedFee
         * @memberof proto.CustomFee
         * @instance
         */
        CustomFee.prototype.fixedFee = null;

        /**
         * A fee defined as a fraction of the tokens transferred.
         * <p>
         * This type of fee MUST NOT be defined for a non-fungible/unique
         * token type.<br/>
         * This fee MAY be charged to either sender, as an increase to the
         * amount sent, or receiver, as a reduction to the amount received.
         * @member {proto.FractionalFee.$Properties|null|undefined} fractionalFee
         * @memberof proto.CustomFee
         * @instance
         */
        CustomFee.prototype.fractionalFee = null;

        /**
         * A fee charged as royalty for any transfer of a
         * non-fungible/unique token.
         * <p>
         * This type of fee MUST NOT be defined for a
         * fungible/common token type.
         * @member {proto.RoyaltyFee.$Properties|null|undefined} royaltyFee
         * @memberof proto.CustomFee
         * @instance
         */
        CustomFee.prototype.royaltyFee = null;

        /**
         * The account to receive the custom fee.
         * @member {proto.AccountID.$Properties|null|undefined} feeCollectorAccountId
         * @memberof proto.CustomFee
         * @instance
         */
        CustomFee.prototype.feeCollectorAccountId = null;

        /**
         * Flag indicating to exempt all custom fee collector accounts for this
         * token type from paying this custom fee when sending tokens.
         * <p>
         * The treasury account for a token, and the account identified by the
         * `fee_collector_account_id` field of this `CustomFee` are always exempt
         * from this custom fee to avoid redundant and unnecessary transfers.
         * If this value is `true` then the account(s) identified in
         * `fee_collector_account_id` for _all_ custom fee definitions for this
         * token type SHALL also be exempt from this custom fee.
         * This behavior is specified in HIP-573.
         * @member {boolean} allCollectorsAreExempt
         * @memberof proto.CustomFee
         * @instance
         */
        CustomFee.prototype.allCollectorsAreExempt = false;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * CustomFee fee.
         * @member {"fixedFee"|"fractionalFee"|"royaltyFee"|undefined} fee
         * @memberof proto.CustomFee
         * @instance
         */
        $Object.defineProperty(CustomFee.prototype, "fee", {
            get: $util.oneOfGetter($oneOfFields = ["fixedFee", "fractionalFee", "royaltyFee"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new CustomFee instance using the specified properties.
         * @function create
         * @memberof proto.CustomFee
         * @static
         * @param {proto.CustomFee.$Properties=} [properties] Properties to set
         * @returns {proto.CustomFee} CustomFee instance
         * @type {{
         *   (properties: proto.CustomFee.$Shape): proto.CustomFee & proto.CustomFee.$Shape;
         *   (properties?: proto.CustomFee.$Properties): proto.CustomFee;
         * }}
         */
        CustomFee.create = function(properties) {
            return new CustomFee(properties);
        };

        /**
         * Encodes the specified CustomFee message. Does not implicitly {@link proto.CustomFee.verify|verify} messages.
         * @function encode
         * @memberof proto.CustomFee
         * @static
         * @param {proto.CustomFee.$Properties} message CustomFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CustomFee.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fixedFee != null && $Object.hasOwnProperty.call(message, "fixedFee"))
                $root.proto.FixedFee.encode(message.fixedFee, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.fractionalFee != null && $Object.hasOwnProperty.call(message, "fractionalFee"))
                $root.proto.FractionalFee.encode(message.fractionalFee, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.feeCollectorAccountId != null && $Object.hasOwnProperty.call(message, "feeCollectorAccountId"))
                $root.proto.AccountID.encode(message.feeCollectorAccountId, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.royaltyFee != null && $Object.hasOwnProperty.call(message, "royaltyFee"))
                $root.proto.RoyaltyFee.encode(message.royaltyFee, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.allCollectorsAreExempt != null && $Object.hasOwnProperty.call(message, "allCollectorsAreExempt") && message.allCollectorsAreExempt !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.allCollectorsAreExempt);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CustomFee message, length delimited. Does not implicitly {@link proto.CustomFee.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CustomFee
         * @static
         * @param {proto.CustomFee.$Properties} message CustomFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CustomFee.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CustomFee message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CustomFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CustomFee & proto.CustomFee.$Shape} CustomFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CustomFee.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CustomFee();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.fixedFee = $root.proto.FixedFee.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fixedFee);
                        message.fee = "fixedFee";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.fractionalFee = $root.proto.FractionalFee.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fractionalFee);
                        message.fee = "fractionalFee";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.royaltyFee = $root.proto.RoyaltyFee.decode(reader, reader.uint32(), $undefined, _depth + 1, message.royaltyFee);
                        message.fee = "royaltyFee";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.feeCollectorAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeCollectorAccountId);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.allCollectorsAreExempt = value;
                        else
                            delete message.allCollectorsAreExempt;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CustomFee message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CustomFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CustomFee & proto.CustomFee.$Shape} CustomFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CustomFee.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CustomFee message.
         * @function verify
         * @memberof proto.CustomFee
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CustomFee.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.fixedFee != null && $Object.hasOwnProperty.call(message, "fixedFee")) {
                properties.fee = 1;
                {
                    let error = $root.proto.FixedFee.verify(message.fixedFee, _depth + 1);
                    if (error)
                        return "fixedFee." + error;
                }
            }
            if (message.fractionalFee != null && $Object.hasOwnProperty.call(message, "fractionalFee")) {
                if (properties.fee === 1)
                    return "fee: multiple values";
                properties.fee = 1;
                {
                    let error = $root.proto.FractionalFee.verify(message.fractionalFee, _depth + 1);
                    if (error)
                        return "fractionalFee." + error;
                }
            }
            if (message.royaltyFee != null && $Object.hasOwnProperty.call(message, "royaltyFee")) {
                if (properties.fee === 1)
                    return "fee: multiple values";
                properties.fee = 1;
                {
                    let error = $root.proto.RoyaltyFee.verify(message.royaltyFee, _depth + 1);
                    if (error)
                        return "royaltyFee." + error;
                }
            }
            if (message.feeCollectorAccountId != null && $Object.hasOwnProperty.call(message, "feeCollectorAccountId")) {
                let error = $root.proto.AccountID.verify(message.feeCollectorAccountId, _depth + 1);
                if (error)
                    return "feeCollectorAccountId." + error;
            }
            if (message.allCollectorsAreExempt != null && $Object.hasOwnProperty.call(message, "allCollectorsAreExempt"))
                if (typeof message.allCollectorsAreExempt !== "boolean")
                    return "allCollectorsAreExempt: boolean expected";
            return null;
        };

        /**
         * Creates a CustomFee message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CustomFee
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CustomFee} CustomFee
         */
        CustomFee.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CustomFee)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CustomFee: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CustomFee();
            if (object.fixedFee != null) {
                if (!$util.isObject(object.fixedFee))
                    throw $TypeError(".proto.CustomFee.fixedFee: object expected");
                message.fixedFee = $root.proto.FixedFee.fromObject(object.fixedFee, _depth + 1);
            }
            if (object.fractionalFee != null) {
                if (!$util.isObject(object.fractionalFee))
                    throw $TypeError(".proto.CustomFee.fractionalFee: object expected");
                message.fractionalFee = $root.proto.FractionalFee.fromObject(object.fractionalFee, _depth + 1);
            }
            if (object.royaltyFee != null) {
                if (!$util.isObject(object.royaltyFee))
                    throw $TypeError(".proto.CustomFee.royaltyFee: object expected");
                message.royaltyFee = $root.proto.RoyaltyFee.fromObject(object.royaltyFee, _depth + 1);
            }
            if (object.feeCollectorAccountId != null) {
                if (!$util.isObject(object.feeCollectorAccountId))
                    throw $TypeError(".proto.CustomFee.feeCollectorAccountId: object expected");
                message.feeCollectorAccountId = $root.proto.AccountID.fromObject(object.feeCollectorAccountId, _depth + 1);
            }
            if (object.allCollectorsAreExempt != null)
                if (object.allCollectorsAreExempt)
                    message.allCollectorsAreExempt = $Boolean(object.allCollectorsAreExempt);
            return message;
        };

        /**
         * Creates a plain object from a CustomFee message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CustomFee
         * @static
         * @param {proto.CustomFee} message CustomFee
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CustomFee.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.feeCollectorAccountId = null;
                object.allCollectorsAreExempt = false;
            }
            if (message.fixedFee != null && $Object.hasOwnProperty.call(message, "fixedFee")) {
                object.fixedFee = $root.proto.FixedFee.toObject(message.fixedFee, options, _depth + 1);
                if (options.oneofs)
                    object.fee = "fixedFee";
            }
            if (message.fractionalFee != null && $Object.hasOwnProperty.call(message, "fractionalFee")) {
                object.fractionalFee = $root.proto.FractionalFee.toObject(message.fractionalFee, options, _depth + 1);
                if (options.oneofs)
                    object.fee = "fractionalFee";
            }
            if (message.feeCollectorAccountId != null && $Object.hasOwnProperty.call(message, "feeCollectorAccountId"))
                object.feeCollectorAccountId = $root.proto.AccountID.toObject(message.feeCollectorAccountId, options, _depth + 1);
            if (message.royaltyFee != null && $Object.hasOwnProperty.call(message, "royaltyFee")) {
                object.royaltyFee = $root.proto.RoyaltyFee.toObject(message.royaltyFee, options, _depth + 1);
                if (options.oneofs)
                    object.fee = "royaltyFee";
            }
            if (message.allCollectorsAreExempt != null && $Object.hasOwnProperty.call(message, "allCollectorsAreExempt"))
                object.allCollectorsAreExempt = message.allCollectorsAreExempt;
            return object;
        };

        /**
         * Converts this CustomFee to JSON.
         * @function toJSON
         * @memberof proto.CustomFee
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CustomFee.prototype.toJSON = function() {
            return CustomFee.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CustomFee
         * @function getTypeUrl
         * @memberof proto.CustomFee
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CustomFee.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CustomFee";
        };

        return CustomFee;
    })();

    proto.AssessedCustomFee = (function() {

        /**
         * Properties of an AssessedCustomFee.
         * @typedef {Object} proto.AssessedCustomFee.$Properties
         * @property {number|Long|null} [amount] An amount of tokens assessed for this custom fee.
         * <p>
         * This shall be expressed in units of 10<sup>-decimals</sup> tokens.
         * @property {proto.TokenID.$Properties|null} [tokenId] The token transferred to satisfy this fee.
         * <p>
         * If the token transferred is HBAR, this field SHALL NOT be set.
         * @property {proto.AccountID.$Properties|null} [feeCollectorAccountId] An account that received the fee assessed.
         * <p>
         * This SHALL NOT be the sender or receiver of the original
         * cryptoTransfer transaction.
         * @property {Array.<proto.AccountID.$Properties>|null} [effectivePayerAccountId] An account that provided the tokens assessed as a fee.
         * <p>
         * This SHALL be the account that _would have_ had a higher balance
         * absent the fee. In most cases this SHALL be the `sender`, but
         * some _fractional_ fees reduce the amount transferred, and in those
         * cases the `receiver` SHALL be the effective payer for the fee.<br/>
         * There are currently no situations where a third party pays a custom
         * fee. This MAY change in a future release.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AssessedCustomFee.
         * @memberof proto
         * @interface IAssessedCustomFee
         * @augments proto.AssessedCustomFee.$Properties
         * @deprecated Use proto.AssessedCustomFee.$Properties instead.
         */

        /**
         * Shape of an AssessedCustomFee.
         * @typedef {{
         *   amount?: number|Long|null;
         *   tokenId?: proto.TokenID.$Shape|null;
         *   feeCollectorAccountId?: proto.AccountID.$Shape|null;
         *   effectivePayerAccountId?: Array.<proto.AccountID.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.AssessedCustomFee.$Shape
         */

        /**
         * Constructs a new AssessedCustomFee.
         * @memberof proto
         * @classdesc Description of a transfer added to a `cryptoTransfer` transaction that
         * satisfies custom fee requirements.
         * 
         * It is important to note that this is not the actual transfer. The transfer
         * of value SHALL be merged into the original transaction to minimize the
         * number of actual transfers. This descriptor presents the fee assessed
         * separately in the record stream so that the details of the fee assessed
         * are not hidden in this process.
         * @constructor
         * @param {proto.AssessedCustomFee.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AssessedCustomFee = function (properties) {
            this.effectivePayerAccountId = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An amount of tokens assessed for this custom fee.
         * <p>
         * This shall be expressed in units of 10<sup>-decimals</sup> tokens.
         * @member {number|Long} amount
         * @memberof proto.AssessedCustomFee
         * @instance
         */
        AssessedCustomFee.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The token transferred to satisfy this fee.
         * <p>
         * If the token transferred is HBAR, this field SHALL NOT be set.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.AssessedCustomFee
         * @instance
         */
        AssessedCustomFee.prototype.tokenId = null;

        /**
         * An account that received the fee assessed.
         * <p>
         * This SHALL NOT be the sender or receiver of the original
         * cryptoTransfer transaction.
         * @member {proto.AccountID.$Properties|null|undefined} feeCollectorAccountId
         * @memberof proto.AssessedCustomFee
         * @instance
         */
        AssessedCustomFee.prototype.feeCollectorAccountId = null;

        /**
         * An account that provided the tokens assessed as a fee.
         * <p>
         * This SHALL be the account that _would have_ had a higher balance
         * absent the fee. In most cases this SHALL be the `sender`, but
         * some _fractional_ fees reduce the amount transferred, and in those
         * cases the `receiver` SHALL be the effective payer for the fee.<br/>
         * There are currently no situations where a third party pays a custom
         * fee. This MAY change in a future release.
         * @member {Array.<proto.AccountID.$Properties>} effectivePayerAccountId
         * @memberof proto.AssessedCustomFee
         * @instance
         */
        AssessedCustomFee.prototype.effectivePayerAccountId = $util.emptyArray;

        /**
         * Creates a new AssessedCustomFee instance using the specified properties.
         * @function create
         * @memberof proto.AssessedCustomFee
         * @static
         * @param {proto.AssessedCustomFee.$Properties=} [properties] Properties to set
         * @returns {proto.AssessedCustomFee} AssessedCustomFee instance
         * @type {{
         *   (properties: proto.AssessedCustomFee.$Shape): proto.AssessedCustomFee & proto.AssessedCustomFee.$Shape;
         *   (properties?: proto.AssessedCustomFee.$Properties): proto.AssessedCustomFee;
         * }}
         */
        AssessedCustomFee.create = function(properties) {
            return new AssessedCustomFee(properties);
        };

        /**
         * Encodes the specified AssessedCustomFee message. Does not implicitly {@link proto.AssessedCustomFee.verify|verify} messages.
         * @function encode
         * @memberof proto.AssessedCustomFee
         * @static
         * @param {proto.AssessedCustomFee.$Properties} message AssessedCustomFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AssessedCustomFee.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.amount);
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.feeCollectorAccountId != null && $Object.hasOwnProperty.call(message, "feeCollectorAccountId"))
                $root.proto.AccountID.encode(message.feeCollectorAccountId, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.effectivePayerAccountId != null && message.effectivePayerAccountId.length)
                for (let i = 0; i < message.effectivePayerAccountId.length; ++i)
                    $root.proto.AccountID.encode(message.effectivePayerAccountId[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AssessedCustomFee message, length delimited. Does not implicitly {@link proto.AssessedCustomFee.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AssessedCustomFee
         * @static
         * @param {proto.AssessedCustomFee.$Properties} message AssessedCustomFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AssessedCustomFee.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AssessedCustomFee message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AssessedCustomFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AssessedCustomFee & proto.AssessedCustomFee.$Shape} AssessedCustomFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AssessedCustomFee.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.AssessedCustomFee();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.feeCollectorAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeCollectorAccountId);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.effectivePayerAccountId && message.effectivePayerAccountId.length))
                            message.effectivePayerAccountId = [];
                        message.effectivePayerAccountId.push($root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AssessedCustomFee message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AssessedCustomFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AssessedCustomFee & proto.AssessedCustomFee.$Shape} AssessedCustomFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AssessedCustomFee.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AssessedCustomFee message.
         * @function verify
         * @memberof proto.AssessedCustomFee
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AssessedCustomFee.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.feeCollectorAccountId != null && $Object.hasOwnProperty.call(message, "feeCollectorAccountId")) {
                let error = $root.proto.AccountID.verify(message.feeCollectorAccountId, _depth + 1);
                if (error)
                    return "feeCollectorAccountId." + error;
            }
            if (message.effectivePayerAccountId != null && $Object.hasOwnProperty.call(message, "effectivePayerAccountId")) {
                if (!$Array.isArray(message.effectivePayerAccountId))
                    return "effectivePayerAccountId: array expected";
                for (let i = 0; i < message.effectivePayerAccountId.length; ++i) {
                    let error = $root.proto.AccountID.verify(message.effectivePayerAccountId[i], _depth + 1);
                    if (error)
                        return "effectivePayerAccountId." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AssessedCustomFee message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AssessedCustomFee
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AssessedCustomFee} AssessedCustomFee
         */
        AssessedCustomFee.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.AssessedCustomFee)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.AssessedCustomFee: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.AssessedCustomFee();
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.AssessedCustomFee.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.feeCollectorAccountId != null) {
                if (!$util.isObject(object.feeCollectorAccountId))
                    throw $TypeError(".proto.AssessedCustomFee.feeCollectorAccountId: object expected");
                message.feeCollectorAccountId = $root.proto.AccountID.fromObject(object.feeCollectorAccountId, _depth + 1);
            }
            if (object.effectivePayerAccountId) {
                if (!$Array.isArray(object.effectivePayerAccountId))
                    throw $TypeError(".proto.AssessedCustomFee.effectivePayerAccountId: array expected");
                message.effectivePayerAccountId = $Array(object.effectivePayerAccountId.length);
                for (let i = 0; i < object.effectivePayerAccountId.length; ++i) {
                    if (!$util.isObject(object.effectivePayerAccountId[i]))
                        throw $TypeError(".proto.AssessedCustomFee.effectivePayerAccountId: object expected");
                    message.effectivePayerAccountId[i] = $root.proto.AccountID.fromObject(object.effectivePayerAccountId[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AssessedCustomFee message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AssessedCustomFee
         * @static
         * @param {proto.AssessedCustomFee} message AssessedCustomFee
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AssessedCustomFee.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.effectivePayerAccountId = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.tokenId = null;
                object.feeCollectorAccountId = null;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.feeCollectorAccountId != null && $Object.hasOwnProperty.call(message, "feeCollectorAccountId"))
                object.feeCollectorAccountId = $root.proto.AccountID.toObject(message.feeCollectorAccountId, options, _depth + 1);
            if (message.effectivePayerAccountId && message.effectivePayerAccountId.length) {
                object.effectivePayerAccountId = $Array(message.effectivePayerAccountId.length);
                for (let j = 0; j < message.effectivePayerAccountId.length; ++j)
                    object.effectivePayerAccountId[j] = $root.proto.AccountID.toObject(message.effectivePayerAccountId[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this AssessedCustomFee to JSON.
         * @function toJSON
         * @memberof proto.AssessedCustomFee
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AssessedCustomFee.prototype.toJSON = function() {
            return AssessedCustomFee.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AssessedCustomFee
         * @function getTypeUrl
         * @memberof proto.AssessedCustomFee
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AssessedCustomFee.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AssessedCustomFee";
        };

        return AssessedCustomFee;
    })();

    proto.FixedCustomFee = (function() {

        /**
         * Properties of a FixedCustomFee.
         * @typedef {Object} proto.FixedCustomFee.$Properties
         * @property {proto.FixedFee.$Properties|null} [fixedFee] A fixed custom fee.
         * <p>
         * The amount of HBAR or other token described by this `FixedFee` SHALL
         * be charged to the transction payer for each message submitted to a
         * topic that assigns this consensus custom fee.
         * @property {proto.AccountID.$Properties|null} [feeCollectorAccountId] A collection account identifier.
         * <p>
         * All amounts collected for this consensus custom fee SHALL be transferred
         * to the account identified by this field.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FixedCustomFee.
         * @memberof proto
         * @interface IFixedCustomFee
         * @augments proto.FixedCustomFee.$Properties
         * @deprecated Use proto.FixedCustomFee.$Properties instead.
         */

        /**
         * Shape of a FixedCustomFee.
         * @typedef {{
         *   fixedFee?: proto.FixedFee.$Shape|null;
         *   feeCollectorAccountId?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.FixedCustomFee.$Shape
         */

        /**
         * Constructs a new FixedCustomFee.
         * @memberof proto
         * @classdesc A custom fee definition for a consensus topic.
         * <p>
         * This fee definition is specific to an Hedera Consensus Service (HCS) topic
         * and SHOULD NOT be used in any other context.<br/>
         * All fields for this message are REQUIRED.<br/>
         * Only "fixed" fee definitions are supported because there is no basis for
         * a fractional fee on a consensus submit transaction.
         * @constructor
         * @param {proto.FixedCustomFee.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FixedCustomFee = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A fixed custom fee.
         * <p>
         * The amount of HBAR or other token described by this `FixedFee` SHALL
         * be charged to the transction payer for each message submitted to a
         * topic that assigns this consensus custom fee.
         * @member {proto.FixedFee.$Properties|null|undefined} fixedFee
         * @memberof proto.FixedCustomFee
         * @instance
         */
        FixedCustomFee.prototype.fixedFee = null;

        /**
         * A collection account identifier.
         * <p>
         * All amounts collected for this consensus custom fee SHALL be transferred
         * to the account identified by this field.
         * @member {proto.AccountID.$Properties|null|undefined} feeCollectorAccountId
         * @memberof proto.FixedCustomFee
         * @instance
         */
        FixedCustomFee.prototype.feeCollectorAccountId = null;

        /**
         * Creates a new FixedCustomFee instance using the specified properties.
         * @function create
         * @memberof proto.FixedCustomFee
         * @static
         * @param {proto.FixedCustomFee.$Properties=} [properties] Properties to set
         * @returns {proto.FixedCustomFee} FixedCustomFee instance
         * @type {{
         *   (properties: proto.FixedCustomFee.$Shape): proto.FixedCustomFee & proto.FixedCustomFee.$Shape;
         *   (properties?: proto.FixedCustomFee.$Properties): proto.FixedCustomFee;
         * }}
         */
        FixedCustomFee.create = function(properties) {
            return new FixedCustomFee(properties);
        };

        /**
         * Encodes the specified FixedCustomFee message. Does not implicitly {@link proto.FixedCustomFee.verify|verify} messages.
         * @function encode
         * @memberof proto.FixedCustomFee
         * @static
         * @param {proto.FixedCustomFee.$Properties} message FixedCustomFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FixedCustomFee.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fixedFee != null && $Object.hasOwnProperty.call(message, "fixedFee"))
                $root.proto.FixedFee.encode(message.fixedFee, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.feeCollectorAccountId != null && $Object.hasOwnProperty.call(message, "feeCollectorAccountId"))
                $root.proto.AccountID.encode(message.feeCollectorAccountId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FixedCustomFee message, length delimited. Does not implicitly {@link proto.FixedCustomFee.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FixedCustomFee
         * @static
         * @param {proto.FixedCustomFee.$Properties} message FixedCustomFee message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FixedCustomFee.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FixedCustomFee message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FixedCustomFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FixedCustomFee & proto.FixedCustomFee.$Shape} FixedCustomFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FixedCustomFee.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FixedCustomFee();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.fixedFee = $root.proto.FixedFee.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fixedFee);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.feeCollectorAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeCollectorAccountId);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FixedCustomFee message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FixedCustomFee
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FixedCustomFee & proto.FixedCustomFee.$Shape} FixedCustomFee
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FixedCustomFee.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FixedCustomFee message.
         * @function verify
         * @memberof proto.FixedCustomFee
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FixedCustomFee.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fixedFee != null && $Object.hasOwnProperty.call(message, "fixedFee")) {
                let error = $root.proto.FixedFee.verify(message.fixedFee, _depth + 1);
                if (error)
                    return "fixedFee." + error;
            }
            if (message.feeCollectorAccountId != null && $Object.hasOwnProperty.call(message, "feeCollectorAccountId")) {
                let error = $root.proto.AccountID.verify(message.feeCollectorAccountId, _depth + 1);
                if (error)
                    return "feeCollectorAccountId." + error;
            }
            return null;
        };

        /**
         * Creates a FixedCustomFee message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FixedCustomFee
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FixedCustomFee} FixedCustomFee
         */
        FixedCustomFee.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FixedCustomFee)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FixedCustomFee: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FixedCustomFee();
            if (object.fixedFee != null) {
                if (!$util.isObject(object.fixedFee))
                    throw $TypeError(".proto.FixedCustomFee.fixedFee: object expected");
                message.fixedFee = $root.proto.FixedFee.fromObject(object.fixedFee, _depth + 1);
            }
            if (object.feeCollectorAccountId != null) {
                if (!$util.isObject(object.feeCollectorAccountId))
                    throw $TypeError(".proto.FixedCustomFee.feeCollectorAccountId: object expected");
                message.feeCollectorAccountId = $root.proto.AccountID.fromObject(object.feeCollectorAccountId, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a FixedCustomFee message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FixedCustomFee
         * @static
         * @param {proto.FixedCustomFee} message FixedCustomFee
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FixedCustomFee.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.fixedFee = null;
                object.feeCollectorAccountId = null;
            }
            if (message.fixedFee != null && $Object.hasOwnProperty.call(message, "fixedFee"))
                object.fixedFee = $root.proto.FixedFee.toObject(message.fixedFee, options, _depth + 1);
            if (message.feeCollectorAccountId != null && $Object.hasOwnProperty.call(message, "feeCollectorAccountId"))
                object.feeCollectorAccountId = $root.proto.AccountID.toObject(message.feeCollectorAccountId, options, _depth + 1);
            return object;
        };

        /**
         * Converts this FixedCustomFee to JSON.
         * @function toJSON
         * @memberof proto.FixedCustomFee
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FixedCustomFee.prototype.toJSON = function() {
            return FixedCustomFee.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FixedCustomFee
         * @function getTypeUrl
         * @memberof proto.FixedCustomFee
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FixedCustomFee.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FixedCustomFee";
        };

        return FixedCustomFee;
    })();

    proto.FixedCustomFeeList = (function() {

        /**
         * Properties of a FixedCustomFeeList.
         * @typedef {Object} proto.FixedCustomFeeList.$Properties
         * @property {Array.<proto.FixedCustomFee.$Properties>|null} [fees] A set of custom fee definitions.<br/>
         * These are fees to be assessed for each submit to a topic.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FixedCustomFeeList.
         * @memberof proto
         * @interface IFixedCustomFeeList
         * @augments proto.FixedCustomFeeList.$Properties
         * @deprecated Use proto.FixedCustomFeeList.$Properties instead.
         */

        /**
         * Shape of a FixedCustomFeeList.
         * @typedef {{
         *   fees?: Array.<proto.FixedCustomFee.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.FixedCustomFeeList.$Shape
         */

        /**
         * Constructs a new FixedCustomFeeList.
         * @memberof proto
         * @classdesc A wrapper around a consensus custom fee list.<br/>
         * This wrapper exists to enable an update transaction to differentiate between
         * a field that is not set and an empty list of custom fees.
         * <p>
         * An _unset_ field of this type SHALL NOT modify existing values.<br/>
         * A _set_ field of this type with an empty `fees` list SHALL remove any
         * existing values.
         * @constructor
         * @param {proto.FixedCustomFeeList.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FixedCustomFeeList = function (properties) {
            this.fees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A set of custom fee definitions.<br/>
         * These are fees to be assessed for each submit to a topic.
         * @member {Array.<proto.FixedCustomFee.$Properties>} fees
         * @memberof proto.FixedCustomFeeList
         * @instance
         */
        FixedCustomFeeList.prototype.fees = $util.emptyArray;

        /**
         * Creates a new FixedCustomFeeList instance using the specified properties.
         * @function create
         * @memberof proto.FixedCustomFeeList
         * @static
         * @param {proto.FixedCustomFeeList.$Properties=} [properties] Properties to set
         * @returns {proto.FixedCustomFeeList} FixedCustomFeeList instance
         * @type {{
         *   (properties: proto.FixedCustomFeeList.$Shape): proto.FixedCustomFeeList & proto.FixedCustomFeeList.$Shape;
         *   (properties?: proto.FixedCustomFeeList.$Properties): proto.FixedCustomFeeList;
         * }}
         */
        FixedCustomFeeList.create = function(properties) {
            return new FixedCustomFeeList(properties);
        };

        /**
         * Encodes the specified FixedCustomFeeList message. Does not implicitly {@link proto.FixedCustomFeeList.verify|verify} messages.
         * @function encode
         * @memberof proto.FixedCustomFeeList
         * @static
         * @param {proto.FixedCustomFeeList.$Properties} message FixedCustomFeeList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FixedCustomFeeList.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fees != null && message.fees.length)
                for (let i = 0; i < message.fees.length; ++i)
                    $root.proto.FixedCustomFee.encode(message.fees[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FixedCustomFeeList message, length delimited. Does not implicitly {@link proto.FixedCustomFeeList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FixedCustomFeeList
         * @static
         * @param {proto.FixedCustomFeeList.$Properties} message FixedCustomFeeList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FixedCustomFeeList.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FixedCustomFeeList message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FixedCustomFeeList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FixedCustomFeeList & proto.FixedCustomFeeList.$Shape} FixedCustomFeeList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FixedCustomFeeList.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FixedCustomFeeList();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.fees && message.fees.length))
                            message.fees = [];
                        message.fees.push($root.proto.FixedCustomFee.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FixedCustomFeeList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FixedCustomFeeList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FixedCustomFeeList & proto.FixedCustomFeeList.$Shape} FixedCustomFeeList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FixedCustomFeeList.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FixedCustomFeeList message.
         * @function verify
         * @memberof proto.FixedCustomFeeList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FixedCustomFeeList.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fees != null && $Object.hasOwnProperty.call(message, "fees")) {
                if (!$Array.isArray(message.fees))
                    return "fees: array expected";
                for (let i = 0; i < message.fees.length; ++i) {
                    let error = $root.proto.FixedCustomFee.verify(message.fees[i], _depth + 1);
                    if (error)
                        return "fees." + error;
                }
            }
            return null;
        };

        /**
         * Creates a FixedCustomFeeList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FixedCustomFeeList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FixedCustomFeeList} FixedCustomFeeList
         */
        FixedCustomFeeList.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FixedCustomFeeList)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FixedCustomFeeList: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FixedCustomFeeList();
            if (object.fees) {
                if (!$Array.isArray(object.fees))
                    throw $TypeError(".proto.FixedCustomFeeList.fees: array expected");
                message.fees = $Array(object.fees.length);
                for (let i = 0; i < object.fees.length; ++i) {
                    if (!$util.isObject(object.fees[i]))
                        throw $TypeError(".proto.FixedCustomFeeList.fees: object expected");
                    message.fees[i] = $root.proto.FixedCustomFee.fromObject(object.fees[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a FixedCustomFeeList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FixedCustomFeeList
         * @static
         * @param {proto.FixedCustomFeeList} message FixedCustomFeeList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FixedCustomFeeList.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.fees = [];
            if (message.fees && message.fees.length) {
                object.fees = $Array(message.fees.length);
                for (let j = 0; j < message.fees.length; ++j)
                    object.fees[j] = $root.proto.FixedCustomFee.toObject(message.fees[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this FixedCustomFeeList to JSON.
         * @function toJSON
         * @memberof proto.FixedCustomFeeList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FixedCustomFeeList.prototype.toJSON = function() {
            return FixedCustomFeeList.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FixedCustomFeeList
         * @function getTypeUrl
         * @memberof proto.FixedCustomFeeList
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FixedCustomFeeList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FixedCustomFeeList";
        };

        return FixedCustomFeeList;
    })();

    proto.FeeExemptKeyList = (function() {

        /**
         * Properties of a FeeExemptKeyList.
         * @typedef {Object} proto.FeeExemptKeyList.$Properties
         * @property {Array.<proto.Key.$Properties>|null} [keys] A set of keys.<br/>
         * The keys in this list are permitted to submit messages to the
         * topic without paying the topic's custom fees.
         * <p>
         * If a submit transaction is signed by _any_ key included in this set,
         * custom fees SHALL NOT be charged for that transaction.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FeeExemptKeyList.
         * @memberof proto
         * @interface IFeeExemptKeyList
         * @augments proto.FeeExemptKeyList.$Properties
         * @deprecated Use proto.FeeExemptKeyList.$Properties instead.
         */

        /**
         * Shape of a FeeExemptKeyList.
         * @typedef {{
         *   keys?: Array.<proto.Key.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.FeeExemptKeyList.$Shape
         */

        /**
         * Constructs a new FeeExemptKeyList.
         * @memberof proto
         * @classdesc A wrapper for fee exempt key list.<br/>
         * This wrapper exists to enable an update transaction to differentiate between
         * a field that is not set and an empty list of keys.
         * <p>
         * An _unset_ field of this type SHALL NOT modify existing values.<br/>
         * A _set_ field of this type with an empty `keys` list SHALL remove any
         * existing values.
         * @constructor
         * @param {proto.FeeExemptKeyList.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FeeExemptKeyList = function (properties) {
            this.keys = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A set of keys.<br/>
         * The keys in this list are permitted to submit messages to the
         * topic without paying the topic's custom fees.
         * <p>
         * If a submit transaction is signed by _any_ key included in this set,
         * custom fees SHALL NOT be charged for that transaction.
         * @member {Array.<proto.Key.$Properties>} keys
         * @memberof proto.FeeExemptKeyList
         * @instance
         */
        FeeExemptKeyList.prototype.keys = $util.emptyArray;

        /**
         * Creates a new FeeExemptKeyList instance using the specified properties.
         * @function create
         * @memberof proto.FeeExemptKeyList
         * @static
         * @param {proto.FeeExemptKeyList.$Properties=} [properties] Properties to set
         * @returns {proto.FeeExemptKeyList} FeeExemptKeyList instance
         * @type {{
         *   (properties: proto.FeeExemptKeyList.$Shape): proto.FeeExemptKeyList & proto.FeeExemptKeyList.$Shape;
         *   (properties?: proto.FeeExemptKeyList.$Properties): proto.FeeExemptKeyList;
         * }}
         */
        FeeExemptKeyList.create = function(properties) {
            return new FeeExemptKeyList(properties);
        };

        /**
         * Encodes the specified FeeExemptKeyList message. Does not implicitly {@link proto.FeeExemptKeyList.verify|verify} messages.
         * @function encode
         * @memberof proto.FeeExemptKeyList
         * @static
         * @param {proto.FeeExemptKeyList.$Properties} message FeeExemptKeyList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeExemptKeyList.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.keys != null && message.keys.length)
                for (let i = 0; i < message.keys.length; ++i)
                    $root.proto.Key.encode(message.keys[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FeeExemptKeyList message, length delimited. Does not implicitly {@link proto.FeeExemptKeyList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FeeExemptKeyList
         * @static
         * @param {proto.FeeExemptKeyList.$Properties} message FeeExemptKeyList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeeExemptKeyList.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FeeExemptKeyList message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FeeExemptKeyList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FeeExemptKeyList & proto.FeeExemptKeyList.$Shape} FeeExemptKeyList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeExemptKeyList.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FeeExemptKeyList();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.keys && message.keys.length))
                            message.keys = [];
                        message.keys.push($root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FeeExemptKeyList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FeeExemptKeyList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FeeExemptKeyList & proto.FeeExemptKeyList.$Shape} FeeExemptKeyList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeeExemptKeyList.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FeeExemptKeyList message.
         * @function verify
         * @memberof proto.FeeExemptKeyList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FeeExemptKeyList.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys")) {
                if (!$Array.isArray(message.keys))
                    return "keys: array expected";
                for (let i = 0; i < message.keys.length; ++i) {
                    let error = $root.proto.Key.verify(message.keys[i], _depth + 1);
                    if (error)
                        return "keys." + error;
                }
            }
            return null;
        };

        /**
         * Creates a FeeExemptKeyList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FeeExemptKeyList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FeeExemptKeyList} FeeExemptKeyList
         */
        FeeExemptKeyList.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FeeExemptKeyList)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FeeExemptKeyList: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FeeExemptKeyList();
            if (object.keys) {
                if (!$Array.isArray(object.keys))
                    throw $TypeError(".proto.FeeExemptKeyList.keys: array expected");
                message.keys = $Array(object.keys.length);
                for (let i = 0; i < object.keys.length; ++i) {
                    if (!$util.isObject(object.keys[i]))
                        throw $TypeError(".proto.FeeExemptKeyList.keys: object expected");
                    message.keys[i] = $root.proto.Key.fromObject(object.keys[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a FeeExemptKeyList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FeeExemptKeyList
         * @static
         * @param {proto.FeeExemptKeyList} message FeeExemptKeyList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FeeExemptKeyList.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.keys = [];
            if (message.keys && message.keys.length) {
                object.keys = $Array(message.keys.length);
                for (let j = 0; j < message.keys.length; ++j)
                    object.keys[j] = $root.proto.Key.toObject(message.keys[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this FeeExemptKeyList to JSON.
         * @function toJSON
         * @memberof proto.FeeExemptKeyList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FeeExemptKeyList.prototype.toJSON = function() {
            return FeeExemptKeyList.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FeeExemptKeyList
         * @function getTypeUrl
         * @memberof proto.FeeExemptKeyList
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FeeExemptKeyList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FeeExemptKeyList";
        };

        return FeeExemptKeyList;
    })();

    proto.CustomFeeLimit = (function() {

        /**
         * Properties of a CustomFeeLimit.
         * @typedef {Object} proto.CustomFeeLimit.$Properties
         * @property {proto.AccountID.$Properties|null} [accountId] A payer account identifier.
         * @property {Array.<proto.FixedFee.$Properties>|null} [fees] The maximum fees that the user is willing to pay for the message.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CustomFeeLimit.
         * @memberof proto
         * @interface ICustomFeeLimit
         * @augments proto.CustomFeeLimit.$Properties
         * @deprecated Use proto.CustomFeeLimit.$Properties instead.
         */

        /**
         * Shape of a CustomFeeLimit.
         * @typedef {{
         *   accountId?: proto.AccountID.$Shape|null;
         *   fees?: Array.<proto.FixedFee.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CustomFeeLimit.$Shape
         */

        /**
         * Constructs a new CustomFeeLimit.
         * @memberof proto
         * @classdesc A maximum custom fee that the user is willing to pay.
         * <p>
         * This message is used to specify the maximum custom fee that given user is
         * willing to pay.
         * @constructor
         * @param {proto.CustomFeeLimit.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CustomFeeLimit = function (properties) {
            this.fees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A payer account identifier.
         * @member {proto.AccountID.$Properties|null|undefined} accountId
         * @memberof proto.CustomFeeLimit
         * @instance
         */
        CustomFeeLimit.prototype.accountId = null;

        /**
         * The maximum fees that the user is willing to pay for the message.
         * @member {Array.<proto.FixedFee.$Properties>} fees
         * @memberof proto.CustomFeeLimit
         * @instance
         */
        CustomFeeLimit.prototype.fees = $util.emptyArray;

        /**
         * Creates a new CustomFeeLimit instance using the specified properties.
         * @function create
         * @memberof proto.CustomFeeLimit
         * @static
         * @param {proto.CustomFeeLimit.$Properties=} [properties] Properties to set
         * @returns {proto.CustomFeeLimit} CustomFeeLimit instance
         * @type {{
         *   (properties: proto.CustomFeeLimit.$Shape): proto.CustomFeeLimit & proto.CustomFeeLimit.$Shape;
         *   (properties?: proto.CustomFeeLimit.$Properties): proto.CustomFeeLimit;
         * }}
         */
        CustomFeeLimit.create = function(properties) {
            return new CustomFeeLimit(properties);
        };

        /**
         * Encodes the specified CustomFeeLimit message. Does not implicitly {@link proto.CustomFeeLimit.verify|verify} messages.
         * @function encode
         * @memberof proto.CustomFeeLimit
         * @static
         * @param {proto.CustomFeeLimit.$Properties} message CustomFeeLimit message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CustomFeeLimit.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.fees != null && message.fees.length)
                for (let i = 0; i < message.fees.length; ++i)
                    $root.proto.FixedFee.encode(message.fees[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CustomFeeLimit message, length delimited. Does not implicitly {@link proto.CustomFeeLimit.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CustomFeeLimit
         * @static
         * @param {proto.CustomFeeLimit.$Properties} message CustomFeeLimit message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CustomFeeLimit.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CustomFeeLimit message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CustomFeeLimit
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CustomFeeLimit & proto.CustomFeeLimit.$Shape} CustomFeeLimit
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CustomFeeLimit.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CustomFeeLimit();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.fees && message.fees.length))
                            message.fees = [];
                        message.fees.push($root.proto.FixedFee.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CustomFeeLimit message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CustomFeeLimit
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CustomFeeLimit & proto.CustomFeeLimit.$Shape} CustomFeeLimit
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CustomFeeLimit.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CustomFeeLimit message.
         * @function verify
         * @memberof proto.CustomFeeLimit
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CustomFeeLimit.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                if (error)
                    return "accountId." + error;
            }
            if (message.fees != null && $Object.hasOwnProperty.call(message, "fees")) {
                if (!$Array.isArray(message.fees))
                    return "fees: array expected";
                for (let i = 0; i < message.fees.length; ++i) {
                    let error = $root.proto.FixedFee.verify(message.fees[i], _depth + 1);
                    if (error)
                        return "fees." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CustomFeeLimit message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CustomFeeLimit
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CustomFeeLimit} CustomFeeLimit
         */
        CustomFeeLimit.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CustomFeeLimit)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CustomFeeLimit: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CustomFeeLimit();
            if (object.accountId != null) {
                if (!$util.isObject(object.accountId))
                    throw $TypeError(".proto.CustomFeeLimit.accountId: object expected");
                message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
            }
            if (object.fees) {
                if (!$Array.isArray(object.fees))
                    throw $TypeError(".proto.CustomFeeLimit.fees: array expected");
                message.fees = $Array(object.fees.length);
                for (let i = 0; i < object.fees.length; ++i) {
                    if (!$util.isObject(object.fees[i]))
                        throw $TypeError(".proto.CustomFeeLimit.fees: object expected");
                    message.fees[i] = $root.proto.FixedFee.fromObject(object.fees[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CustomFeeLimit message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CustomFeeLimit
         * @static
         * @param {proto.CustomFeeLimit} message CustomFeeLimit
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CustomFeeLimit.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.fees = [];
            if (options.defaults)
                object.accountId = null;
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
            if (message.fees && message.fees.length) {
                object.fees = $Array(message.fees.length);
                for (let j = 0; j < message.fees.length; ++j)
                    object.fees[j] = $root.proto.FixedFee.toObject(message.fees[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this CustomFeeLimit to JSON.
         * @function toJSON
         * @memberof proto.CustomFeeLimit
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CustomFeeLimit.prototype.toJSON = function() {
            return CustomFeeLimit.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CustomFeeLimit
         * @function getTypeUrl
         * @memberof proto.CustomFeeLimit
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CustomFeeLimit.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CustomFeeLimit";
        };

        return CustomFeeLimit;
    })();

    proto.ConsensusUpdateTopicTransactionBody = (function() {

        /**
         * Properties of a ConsensusUpdateTopicTransactionBody.
         * @typedef {Object} proto.ConsensusUpdateTopicTransactionBody.$Properties
         * @property {proto.TopicID.$Properties|null} [topicID] The topic ID specifying the topic to update.
         * <p>
         * A topic with this ID MUST exist and MUST NOT be deleted.<br/>
         * This value is REQUIRED.
         * @property {google.protobuf.StringValue.$Properties|null} [memo] An updated memo to be associated with this topic.
         * <p>
         * If this value is set, the current `adminKey` for the topic MUST sign
         * this transaction.<br/>
         * This value, if set, SHALL be encoded UTF-8 and SHALL NOT exceed
         * 100 bytes when so encoded.
         * @property {proto.Timestamp.$Properties|null} [expirationTime] An updated expiration time for this topic, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch
         * with 0 at `1970-01-01T00:00:00.000Z`.<br/>
         * The expirationTime MUST NOT be greater than the sum of
         * `MAX_AUTORENEW_PERIOD` and the actual consensus timestamp of
         * this transaction.<br/>
         * If `adminKey` is <b>unset</b> for the _topic_, this transaction MUST NOT
         * modify any other field.
         * @property {proto.Key.$Properties|null} [adminKey] Updated access control for modification of the topic.
         * <p>
         * If this field is set, that key and the previously set key MUST both
         * sign this transaction.<br/>
         * If this value is an empty `KeyList`, the prior key MUST sign this
         * transaction, and the topic SHALL be immutable after this transaction
         * completes, except for expiration and renewal.
         * @property {proto.Key.$Properties|null} [submitKey] Updated access control for message submission to the topic.
         * <p>
         * If this value is set, the current `adminKey` for the topic MUST sign
         * this transaction.<br/>
         * If this value is set to an empty `KeyList`, the `submitKey` for the
         * topic will be unset after this transaction completes. When the
         * `submitKey` is unset, any account may submit a message on the topic,
         * without restriction.
         * @property {proto.Duration.$Properties|null} [autoRenewPeriod] ConsensusUpdateTopicTransactionBody autoRenewPeriod
         * @property {proto.AccountID.$Properties|null} [autoRenewAccount] An updated ID for the account to be charged renewal fees at the topic's
         * `expirationTime` to extend the lifetime of the topic.
         * <p>
         * If this value is set and not the "sentinel account", the referenced
         * account MUST sign this transaction.<br/>
         * If this value is set, the current `adminKey` for the topic MUST sign
         * this transaction.<br/>
         * If this value is set to the "sentinel account", which is `0.0.0`, the
         * `autoRenewAccount` SHALL be removed from the topic.
         * @property {proto.Key.$Properties|null} [feeScheduleKey] Access control for update/delete of custom fees.
         * <p>
         * If set, subsequent consensus_update_topic transactions signed with this
         * key MAY update or delete the custom fees for this topic.<br/>
         * If this field is set, the admin key MUST sign this transaction.<br/>
         * If this field is set, the previous value SHALL be replaced.<br/>
         * If set to a 'Key' containing an empty 'KeyList', the previous value
         * SHALL be cleared.<br/>
         * If not set, the current key SHALL NOT change.<br/>
         * If unset in state, this field MUST NOT be set in this transaction.<br/>
         * If not set when the topic is created, this field CANNOT be set via
         * update.<br/>
         * @property {proto.FeeExemptKeyList.$Properties|null} [feeExemptKeyList] A set of keys<br/>
         * Keys in this list are permitted to submit messages to this topic without
         * paying custom fees associated with this topic.
         * <p>
         * If a submit transaction is signed by _any_ key included in this set,
         * custom fees SHALL NOT be charged for that transaction.<br/>
         * If this field is not set, the current set of keys SHALL NOT change.<br/>
         * If this field is set, but contains an empty list, any existing fee-exempt
         * keys SHALL be removed.<br/>
         * A `fee_exempt_key_list` MUST NOT contain more than
         * `MAX_ENTRIES_FOR_FEE_EXEMPT_KEY_LIST` keys.<br/>
         * A `fee_exempt_key_list` MUST NOT contain any duplicate keys.<br/>
         * A `fee_exempt_key_list` MAY contain keys for accounts that are inactive,
         * deleted, or non-existent.
         * @property {proto.FixedCustomFeeList.$Properties|null} [customFees] A set of custom fee definitions.<br/>
         * These are fees to be assessed for each submit to this topic.
         * <p>
         * Each fee defined in this set SHALL be evaluated for
         * each message submitted to this topic, and the resultant
         * total assessed fees SHALL be charged.<br/>
         * Custom fees defined here SHALL be assessed in addition to the base
         * network and node fees.<br/>
         * If this field is not set, the current set of custom fees
         * SHALL NOT change.<br/>
         * If this field is set, but contains an empty list, all current custom fees
         * SHALL be removed.
         * custom_fees list SHALL NOT contain more than
         * `MAX_CUSTOM_FEE_ENTRIES_FOR_TOPICS` entries.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ConsensusUpdateTopicTransactionBody.
         * @memberof proto
         * @interface IConsensusUpdateTopicTransactionBody
         * @augments proto.ConsensusUpdateTopicTransactionBody.$Properties
         * @deprecated Use proto.ConsensusUpdateTopicTransactionBody.$Properties instead.
         */

        /**
         * Shape of a ConsensusUpdateTopicTransactionBody.
         * @typedef {{
         *   topicID?: proto.TopicID.$Shape|null;
         *   memo?: google.protobuf.StringValue.$Shape|null;
         *   expirationTime?: proto.Timestamp.$Shape|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   submitKey?: proto.Key.$Shape|null;
         *   autoRenewPeriod?: proto.Duration.$Shape|null;
         *   autoRenewAccount?: proto.AccountID.$Shape|null;
         *   feeScheduleKey?: proto.Key.$Shape|null;
         *   feeExemptKeyList?: proto.FeeExemptKeyList.$Shape|null;
         *   customFees?: proto.FixedCustomFeeList.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ConsensusUpdateTopicTransactionBody.$Shape
         */

        /**
         * Constructs a new ConsensusUpdateTopicTransactionBody.
         * @memberof proto
         * @classdesc Update the fields of an existing HCS topic.
         * 
         * The topicID field is REQUIRED. All other fields are OPTIONAL.<br/>
         * Fields set on this transaction SHALL be updated.<br/>
         * Fields _not_ set on this transaction SHALL NOT be updated.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.ConsensusUpdateTopicTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ConsensusUpdateTopicTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The topic ID specifying the topic to update.
         * <p>
         * A topic with this ID MUST exist and MUST NOT be deleted.<br/>
         * This value is REQUIRED.
         * @member {proto.TopicID.$Properties|null|undefined} topicID
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @instance
         */
        ConsensusUpdateTopicTransactionBody.prototype.topicID = null;

        /**
         * An updated memo to be associated with this topic.
         * <p>
         * If this value is set, the current `adminKey` for the topic MUST sign
         * this transaction.<br/>
         * This value, if set, SHALL be encoded UTF-8 and SHALL NOT exceed
         * 100 bytes when so encoded.
         * @member {google.protobuf.StringValue.$Properties|null|undefined} memo
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @instance
         */
        ConsensusUpdateTopicTransactionBody.prototype.memo = null;

        /**
         * An updated expiration time for this topic, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch
         * with 0 at `1970-01-01T00:00:00.000Z`.<br/>
         * The expirationTime MUST NOT be greater than the sum of
         * `MAX_AUTORENEW_PERIOD` and the actual consensus timestamp of
         * this transaction.<br/>
         * If `adminKey` is <b>unset</b> for the _topic_, this transaction MUST NOT
         * modify any other field.
         * @member {proto.Timestamp.$Properties|null|undefined} expirationTime
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @instance
         */
        ConsensusUpdateTopicTransactionBody.prototype.expirationTime = null;

        /**
         * Updated access control for modification of the topic.
         * <p>
         * If this field is set, that key and the previously set key MUST both
         * sign this transaction.<br/>
         * If this value is an empty `KeyList`, the prior key MUST sign this
         * transaction, and the topic SHALL be immutable after this transaction
         * completes, except for expiration and renewal.
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @instance
         */
        ConsensusUpdateTopicTransactionBody.prototype.adminKey = null;

        /**
         * Updated access control for message submission to the topic.
         * <p>
         * If this value is set, the current `adminKey` for the topic MUST sign
         * this transaction.<br/>
         * If this value is set to an empty `KeyList`, the `submitKey` for the
         * topic will be unset after this transaction completes. When the
         * `submitKey` is unset, any account may submit a message on the topic,
         * without restriction.
         * @member {proto.Key.$Properties|null|undefined} submitKey
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @instance
         */
        ConsensusUpdateTopicTransactionBody.prototype.submitKey = null;

        /**
         * ConsensusUpdateTopicTransactionBody autoRenewPeriod.
         * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @instance
         */
        ConsensusUpdateTopicTransactionBody.prototype.autoRenewPeriod = null;

        /**
         * An updated ID for the account to be charged renewal fees at the topic's
         * `expirationTime` to extend the lifetime of the topic.
         * <p>
         * If this value is set and not the "sentinel account", the referenced
         * account MUST sign this transaction.<br/>
         * If this value is set, the current `adminKey` for the topic MUST sign
         * this transaction.<br/>
         * If this value is set to the "sentinel account", which is `0.0.0`, the
         * `autoRenewAccount` SHALL be removed from the topic.
         * @member {proto.AccountID.$Properties|null|undefined} autoRenewAccount
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @instance
         */
        ConsensusUpdateTopicTransactionBody.prototype.autoRenewAccount = null;

        /**
         * Access control for update/delete of custom fees.
         * <p>
         * If set, subsequent consensus_update_topic transactions signed with this
         * key MAY update or delete the custom fees for this topic.<br/>
         * If this field is set, the admin key MUST sign this transaction.<br/>
         * If this field is set, the previous value SHALL be replaced.<br/>
         * If set to a 'Key' containing an empty 'KeyList', the previous value
         * SHALL be cleared.<br/>
         * If not set, the current key SHALL NOT change.<br/>
         * If unset in state, this field MUST NOT be set in this transaction.<br/>
         * If not set when the topic is created, this field CANNOT be set via
         * update.<br/>
         * @member {proto.Key.$Properties|null|undefined} feeScheduleKey
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @instance
         */
        ConsensusUpdateTopicTransactionBody.prototype.feeScheduleKey = null;

        /**
         * A set of keys<br/>
         * Keys in this list are permitted to submit messages to this topic without
         * paying custom fees associated with this topic.
         * <p>
         * If a submit transaction is signed by _any_ key included in this set,
         * custom fees SHALL NOT be charged for that transaction.<br/>
         * If this field is not set, the current set of keys SHALL NOT change.<br/>
         * If this field is set, but contains an empty list, any existing fee-exempt
         * keys SHALL be removed.<br/>
         * A `fee_exempt_key_list` MUST NOT contain more than
         * `MAX_ENTRIES_FOR_FEE_EXEMPT_KEY_LIST` keys.<br/>
         * A `fee_exempt_key_list` MUST NOT contain any duplicate keys.<br/>
         * A `fee_exempt_key_list` MAY contain keys for accounts that are inactive,
         * deleted, or non-existent.
         * @member {proto.FeeExemptKeyList.$Properties|null|undefined} feeExemptKeyList
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @instance
         */
        ConsensusUpdateTopicTransactionBody.prototype.feeExemptKeyList = null;

        /**
         * A set of custom fee definitions.<br/>
         * These are fees to be assessed for each submit to this topic.
         * <p>
         * Each fee defined in this set SHALL be evaluated for
         * each message submitted to this topic, and the resultant
         * total assessed fees SHALL be charged.<br/>
         * Custom fees defined here SHALL be assessed in addition to the base
         * network and node fees.<br/>
         * If this field is not set, the current set of custom fees
         * SHALL NOT change.<br/>
         * If this field is set, but contains an empty list, all current custom fees
         * SHALL be removed.
         * custom_fees list SHALL NOT contain more than
         * `MAX_CUSTOM_FEE_ENTRIES_FOR_TOPICS` entries.
         * @member {proto.FixedCustomFeeList.$Properties|null|undefined} customFees
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @instance
         */
        ConsensusUpdateTopicTransactionBody.prototype.customFees = null;

        /**
         * Creates a new ConsensusUpdateTopicTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @static
         * @param {proto.ConsensusUpdateTopicTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.ConsensusUpdateTopicTransactionBody} ConsensusUpdateTopicTransactionBody instance
         * @type {{
         *   (properties: proto.ConsensusUpdateTopicTransactionBody.$Shape): proto.ConsensusUpdateTopicTransactionBody & proto.ConsensusUpdateTopicTransactionBody.$Shape;
         *   (properties?: proto.ConsensusUpdateTopicTransactionBody.$Properties): proto.ConsensusUpdateTopicTransactionBody;
         * }}
         */
        ConsensusUpdateTopicTransactionBody.create = function(properties) {
            return new ConsensusUpdateTopicTransactionBody(properties);
        };

        /**
         * Encodes the specified ConsensusUpdateTopicTransactionBody message. Does not implicitly {@link proto.ConsensusUpdateTopicTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @static
         * @param {proto.ConsensusUpdateTopicTransactionBody.$Properties} message ConsensusUpdateTopicTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusUpdateTopicTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID"))
                $root.proto.TopicID.encode(message.topicID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                $root.google.protobuf.StringValue.encode(message.memo, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                $root.proto.Timestamp.encode(message.expirationTime, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.submitKey != null && $Object.hasOwnProperty.call(message, "submitKey"))
                $root.proto.Key.encode(message.submitKey, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount"))
                $root.proto.AccountID.encode(message.autoRenewAccount, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                $root.proto.Key.encode(message.feeScheduleKey, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.feeExemptKeyList != null && $Object.hasOwnProperty.call(message, "feeExemptKeyList"))
                $root.proto.FeeExemptKeyList.encode(message.feeExemptKeyList, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.customFees != null && $Object.hasOwnProperty.call(message, "customFees"))
                $root.proto.FixedCustomFeeList.encode(message.customFees, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConsensusUpdateTopicTransactionBody message, length delimited. Does not implicitly {@link proto.ConsensusUpdateTopicTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @static
         * @param {proto.ConsensusUpdateTopicTransactionBody.$Properties} message ConsensusUpdateTopicTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusUpdateTopicTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ConsensusUpdateTopicTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ConsensusUpdateTopicTransactionBody & proto.ConsensusUpdateTopicTransactionBody.$Shape} ConsensusUpdateTopicTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusUpdateTopicTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ConsensusUpdateTopicTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.topicID = $root.proto.TopicID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.topicID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.memo = $root.google.protobuf.StringValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.memo);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.expirationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.submitKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.submitKey);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewAccount = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewAccount);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.feeScheduleKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeScheduleKey);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.feeExemptKeyList = $root.proto.FeeExemptKeyList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeExemptKeyList);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.customFees = $root.proto.FixedCustomFeeList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.customFees);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConsensusUpdateTopicTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ConsensusUpdateTopicTransactionBody & proto.ConsensusUpdateTopicTransactionBody.$Shape} ConsensusUpdateTopicTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusUpdateTopicTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConsensusUpdateTopicTransactionBody message.
         * @function verify
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConsensusUpdateTopicTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID")) {
                let error = $root.proto.TopicID.verify(message.topicID, _depth + 1);
                if (error)
                    return "topicID." + error;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo")) {
                let error = $root.google.protobuf.StringValue.verify(message.memo, _depth + 1);
                if (error)
                    return "memo." + error;
            }
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                let error = $root.proto.Timestamp.verify(message.expirationTime, _depth + 1);
                if (error)
                    return "expirationTime." + error;
            }
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.submitKey != null && $Object.hasOwnProperty.call(message, "submitKey")) {
                let error = $root.proto.Key.verify(message.submitKey, _depth + 1);
                if (error)
                    return "submitKey." + error;
            }
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                if (error)
                    return "autoRenewPeriod." + error;
            }
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount")) {
                let error = $root.proto.AccountID.verify(message.autoRenewAccount, _depth + 1);
                if (error)
                    return "autoRenewAccount." + error;
            }
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey")) {
                let error = $root.proto.Key.verify(message.feeScheduleKey, _depth + 1);
                if (error)
                    return "feeScheduleKey." + error;
            }
            if (message.feeExemptKeyList != null && $Object.hasOwnProperty.call(message, "feeExemptKeyList")) {
                let error = $root.proto.FeeExemptKeyList.verify(message.feeExemptKeyList, _depth + 1);
                if (error)
                    return "feeExemptKeyList." + error;
            }
            if (message.customFees != null && $Object.hasOwnProperty.call(message, "customFees")) {
                let error = $root.proto.FixedCustomFeeList.verify(message.customFees, _depth + 1);
                if (error)
                    return "customFees." + error;
            }
            return null;
        };

        /**
         * Creates a ConsensusUpdateTopicTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ConsensusUpdateTopicTransactionBody} ConsensusUpdateTopicTransactionBody
         */
        ConsensusUpdateTopicTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ConsensusUpdateTopicTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ConsensusUpdateTopicTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ConsensusUpdateTopicTransactionBody();
            if (object.topicID != null) {
                if (!$util.isObject(object.topicID))
                    throw $TypeError(".proto.ConsensusUpdateTopicTransactionBody.topicID: object expected");
                message.topicID = $root.proto.TopicID.fromObject(object.topicID, _depth + 1);
            }
            if (object.memo != null) {
                if (!$util.isObject(object.memo))
                    throw $TypeError(".proto.ConsensusUpdateTopicTransactionBody.memo: object expected");
                message.memo = $root.google.protobuf.StringValue.fromObject(object.memo, _depth + 1);
            }
            if (object.expirationTime != null) {
                if (!$util.isObject(object.expirationTime))
                    throw $TypeError(".proto.ConsensusUpdateTopicTransactionBody.expirationTime: object expected");
                message.expirationTime = $root.proto.Timestamp.fromObject(object.expirationTime, _depth + 1);
            }
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.ConsensusUpdateTopicTransactionBody.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.submitKey != null) {
                if (!$util.isObject(object.submitKey))
                    throw $TypeError(".proto.ConsensusUpdateTopicTransactionBody.submitKey: object expected");
                message.submitKey = $root.proto.Key.fromObject(object.submitKey, _depth + 1);
            }
            if (object.autoRenewPeriod != null) {
                if (!$util.isObject(object.autoRenewPeriod))
                    throw $TypeError(".proto.ConsensusUpdateTopicTransactionBody.autoRenewPeriod: object expected");
                message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
            }
            if (object.autoRenewAccount != null) {
                if (!$util.isObject(object.autoRenewAccount))
                    throw $TypeError(".proto.ConsensusUpdateTopicTransactionBody.autoRenewAccount: object expected");
                message.autoRenewAccount = $root.proto.AccountID.fromObject(object.autoRenewAccount, _depth + 1);
            }
            if (object.feeScheduleKey != null) {
                if (!$util.isObject(object.feeScheduleKey))
                    throw $TypeError(".proto.ConsensusUpdateTopicTransactionBody.feeScheduleKey: object expected");
                message.feeScheduleKey = $root.proto.Key.fromObject(object.feeScheduleKey, _depth + 1);
            }
            if (object.feeExemptKeyList != null) {
                if (!$util.isObject(object.feeExemptKeyList))
                    throw $TypeError(".proto.ConsensusUpdateTopicTransactionBody.feeExemptKeyList: object expected");
                message.feeExemptKeyList = $root.proto.FeeExemptKeyList.fromObject(object.feeExemptKeyList, _depth + 1);
            }
            if (object.customFees != null) {
                if (!$util.isObject(object.customFees))
                    throw $TypeError(".proto.ConsensusUpdateTopicTransactionBody.customFees: object expected");
                message.customFees = $root.proto.FixedCustomFeeList.fromObject(object.customFees, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ConsensusUpdateTopicTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @static
         * @param {proto.ConsensusUpdateTopicTransactionBody} message ConsensusUpdateTopicTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConsensusUpdateTopicTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.topicID = null;
                object.memo = null;
                object.expirationTime = null;
                object.adminKey = null;
                object.submitKey = null;
                object.autoRenewPeriod = null;
                object.autoRenewAccount = null;
                object.feeScheduleKey = null;
                object.feeExemptKeyList = null;
                object.customFees = null;
            }
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID"))
                object.topicID = $root.proto.TopicID.toObject(message.topicID, options, _depth + 1);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = $root.google.protobuf.StringValue.toObject(message.memo, options, _depth + 1);
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                object.expirationTime = $root.proto.Timestamp.toObject(message.expirationTime, options, _depth + 1);
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.submitKey != null && $Object.hasOwnProperty.call(message, "submitKey"))
                object.submitKey = $root.proto.Key.toObject(message.submitKey, options, _depth + 1);
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount"))
                object.autoRenewAccount = $root.proto.AccountID.toObject(message.autoRenewAccount, options, _depth + 1);
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                object.feeScheduleKey = $root.proto.Key.toObject(message.feeScheduleKey, options, _depth + 1);
            if (message.feeExemptKeyList != null && $Object.hasOwnProperty.call(message, "feeExemptKeyList"))
                object.feeExemptKeyList = $root.proto.FeeExemptKeyList.toObject(message.feeExemptKeyList, options, _depth + 1);
            if (message.customFees != null && $Object.hasOwnProperty.call(message, "customFees"))
                object.customFees = $root.proto.FixedCustomFeeList.toObject(message.customFees, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ConsensusUpdateTopicTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConsensusUpdateTopicTransactionBody.prototype.toJSON = function() {
            return ConsensusUpdateTopicTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConsensusUpdateTopicTransactionBody
         * @function getTypeUrl
         * @memberof proto.ConsensusUpdateTopicTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConsensusUpdateTopicTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ConsensusUpdateTopicTransactionBody";
        };

        return ConsensusUpdateTopicTransactionBody;
    })();

    proto.ConsensusDeleteTopicTransactionBody = (function() {

        /**
         * Properties of a ConsensusDeleteTopicTransactionBody.
         * @typedef {Object} proto.ConsensusDeleteTopicTransactionBody.$Properties
         * @property {proto.TopicID.$Properties|null} [topicID] Topic to be deleted.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ConsensusDeleteTopicTransactionBody.
         * @memberof proto
         * @interface IConsensusDeleteTopicTransactionBody
         * @augments proto.ConsensusDeleteTopicTransactionBody.$Properties
         * @deprecated Use proto.ConsensusDeleteTopicTransactionBody.$Properties instead.
         */

        /**
         * Shape of a ConsensusDeleteTopicTransactionBody.
         * @typedef {proto.ConsensusDeleteTopicTransactionBody.$Properties} proto.ConsensusDeleteTopicTransactionBody.$Shape
         */

        /**
         * Constructs a new ConsensusDeleteTopicTransactionBody.
         * @memberof proto
         * @classdesc Delete a topic.
         * 
         * Once deleted, subsequent transactions or queries for that topic SHALL NOT
         * succeed.<br/>
         * If adminKey is set on the topic, this transaction MUST be signed by that
         * key.<br/>
         * If adminKey is not set on the topic, this transaction SHALL fail with a
         * response code of `UNAUTHORIZED`. A topic without an adminKey cannot be
         * deleted (but MAY expire).
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.ConsensusDeleteTopicTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ConsensusDeleteTopicTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Topic to be deleted.
         * @member {proto.TopicID.$Properties|null|undefined} topicID
         * @memberof proto.ConsensusDeleteTopicTransactionBody
         * @instance
         */
        ConsensusDeleteTopicTransactionBody.prototype.topicID = null;

        /**
         * Creates a new ConsensusDeleteTopicTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.ConsensusDeleteTopicTransactionBody
         * @static
         * @param {proto.ConsensusDeleteTopicTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.ConsensusDeleteTopicTransactionBody} ConsensusDeleteTopicTransactionBody instance
         * @type {{
         *   (properties: proto.ConsensusDeleteTopicTransactionBody.$Shape): proto.ConsensusDeleteTopicTransactionBody & proto.ConsensusDeleteTopicTransactionBody.$Shape;
         *   (properties?: proto.ConsensusDeleteTopicTransactionBody.$Properties): proto.ConsensusDeleteTopicTransactionBody;
         * }}
         */
        ConsensusDeleteTopicTransactionBody.create = function(properties) {
            return new ConsensusDeleteTopicTransactionBody(properties);
        };

        /**
         * Encodes the specified ConsensusDeleteTopicTransactionBody message. Does not implicitly {@link proto.ConsensusDeleteTopicTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.ConsensusDeleteTopicTransactionBody
         * @static
         * @param {proto.ConsensusDeleteTopicTransactionBody.$Properties} message ConsensusDeleteTopicTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusDeleteTopicTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID"))
                $root.proto.TopicID.encode(message.topicID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConsensusDeleteTopicTransactionBody message, length delimited. Does not implicitly {@link proto.ConsensusDeleteTopicTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ConsensusDeleteTopicTransactionBody
         * @static
         * @param {proto.ConsensusDeleteTopicTransactionBody.$Properties} message ConsensusDeleteTopicTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusDeleteTopicTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ConsensusDeleteTopicTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ConsensusDeleteTopicTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ConsensusDeleteTopicTransactionBody & proto.ConsensusDeleteTopicTransactionBody.$Shape} ConsensusDeleteTopicTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusDeleteTopicTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ConsensusDeleteTopicTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.topicID = $root.proto.TopicID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.topicID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConsensusDeleteTopicTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ConsensusDeleteTopicTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ConsensusDeleteTopicTransactionBody & proto.ConsensusDeleteTopicTransactionBody.$Shape} ConsensusDeleteTopicTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusDeleteTopicTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConsensusDeleteTopicTransactionBody message.
         * @function verify
         * @memberof proto.ConsensusDeleteTopicTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConsensusDeleteTopicTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID")) {
                let error = $root.proto.TopicID.verify(message.topicID, _depth + 1);
                if (error)
                    return "topicID." + error;
            }
            return null;
        };

        /**
         * Creates a ConsensusDeleteTopicTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ConsensusDeleteTopicTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ConsensusDeleteTopicTransactionBody} ConsensusDeleteTopicTransactionBody
         */
        ConsensusDeleteTopicTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ConsensusDeleteTopicTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ConsensusDeleteTopicTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ConsensusDeleteTopicTransactionBody();
            if (object.topicID != null) {
                if (!$util.isObject(object.topicID))
                    throw $TypeError(".proto.ConsensusDeleteTopicTransactionBody.topicID: object expected");
                message.topicID = $root.proto.TopicID.fromObject(object.topicID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ConsensusDeleteTopicTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ConsensusDeleteTopicTransactionBody
         * @static
         * @param {proto.ConsensusDeleteTopicTransactionBody} message ConsensusDeleteTopicTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConsensusDeleteTopicTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.topicID = null;
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID"))
                object.topicID = $root.proto.TopicID.toObject(message.topicID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ConsensusDeleteTopicTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.ConsensusDeleteTopicTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConsensusDeleteTopicTransactionBody.prototype.toJSON = function() {
            return ConsensusDeleteTopicTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConsensusDeleteTopicTransactionBody
         * @function getTypeUrl
         * @memberof proto.ConsensusDeleteTopicTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConsensusDeleteTopicTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ConsensusDeleteTopicTransactionBody";
        };

        return ConsensusDeleteTopicTransactionBody;
    })();

    proto.ConsensusMessageChunkInfo = (function() {

        /**
         * Properties of a ConsensusMessageChunkInfo.
         * @typedef {Object} proto.ConsensusMessageChunkInfo.$Properties
         * @property {proto.TransactionID.$Properties|null} [initialTransactionID] The TransactionID of the first chunk.
         * <p>
         * This MUST be set for every chunk in a fragmented message.
         * @property {number|null} [total] The total number of chunks in the message.
         * @property {number|null} [number] The sequence number (from 1 to total) of the current chunk
         * in the message.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ConsensusMessageChunkInfo.
         * @memberof proto
         * @interface IConsensusMessageChunkInfo
         * @augments proto.ConsensusMessageChunkInfo.$Properties
         * @deprecated Use proto.ConsensusMessageChunkInfo.$Properties instead.
         */

        /**
         * Shape of a ConsensusMessageChunkInfo.
         * @typedef {{
         *   initialTransactionID?: proto.TransactionID.$Shape|null;
         *   total?: number|null;
         *   number?: number|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ConsensusMessageChunkInfo.$Shape
         */

        /**
         * Constructs a new ConsensusMessageChunkInfo.
         * @memberof proto
         * @classdesc Consensus message "chunk" detail.<br/>
         * This message carries information describing the way in which a message
         * submitted for consensus is broken into multiple fragments to fit within
         * network transaction size limits.
         * 
         * The use of multiple message fragments is RECOMMENDED for any message
         * greater than 4KiB in total size.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.ConsensusMessageChunkInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ConsensusMessageChunkInfo = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The TransactionID of the first chunk.
         * <p>
         * This MUST be set for every chunk in a fragmented message.
         * @member {proto.TransactionID.$Properties|null|undefined} initialTransactionID
         * @memberof proto.ConsensusMessageChunkInfo
         * @instance
         */
        ConsensusMessageChunkInfo.prototype.initialTransactionID = null;

        /**
         * The total number of chunks in the message.
         * @member {number} total
         * @memberof proto.ConsensusMessageChunkInfo
         * @instance
         */
        ConsensusMessageChunkInfo.prototype.total = 0;

        /**
         * The sequence number (from 1 to total) of the current chunk
         * in the message.
         * @member {number} number
         * @memberof proto.ConsensusMessageChunkInfo
         * @instance
         */
        ConsensusMessageChunkInfo.prototype.number = 0;

        /**
         * Creates a new ConsensusMessageChunkInfo instance using the specified properties.
         * @function create
         * @memberof proto.ConsensusMessageChunkInfo
         * @static
         * @param {proto.ConsensusMessageChunkInfo.$Properties=} [properties] Properties to set
         * @returns {proto.ConsensusMessageChunkInfo} ConsensusMessageChunkInfo instance
         * @type {{
         *   (properties: proto.ConsensusMessageChunkInfo.$Shape): proto.ConsensusMessageChunkInfo & proto.ConsensusMessageChunkInfo.$Shape;
         *   (properties?: proto.ConsensusMessageChunkInfo.$Properties): proto.ConsensusMessageChunkInfo;
         * }}
         */
        ConsensusMessageChunkInfo.create = function(properties) {
            return new ConsensusMessageChunkInfo(properties);
        };

        /**
         * Encodes the specified ConsensusMessageChunkInfo message. Does not implicitly {@link proto.ConsensusMessageChunkInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.ConsensusMessageChunkInfo
         * @static
         * @param {proto.ConsensusMessageChunkInfo.$Properties} message ConsensusMessageChunkInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusMessageChunkInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.initialTransactionID != null && $Object.hasOwnProperty.call(message, "initialTransactionID"))
                $root.proto.TransactionID.encode(message.initialTransactionID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.total != null && $Object.hasOwnProperty.call(message, "total") && message.total !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.total);
            if (message.number != null && $Object.hasOwnProperty.call(message, "number") && message.number !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.number);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConsensusMessageChunkInfo message, length delimited. Does not implicitly {@link proto.ConsensusMessageChunkInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ConsensusMessageChunkInfo
         * @static
         * @param {proto.ConsensusMessageChunkInfo.$Properties} message ConsensusMessageChunkInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusMessageChunkInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ConsensusMessageChunkInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ConsensusMessageChunkInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ConsensusMessageChunkInfo & proto.ConsensusMessageChunkInfo.$Shape} ConsensusMessageChunkInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusMessageChunkInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ConsensusMessageChunkInfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.initialTransactionID = $root.proto.TransactionID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.initialTransactionID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.total = value;
                        else
                            delete message.total;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.number = value;
                        else
                            delete message.number;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConsensusMessageChunkInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ConsensusMessageChunkInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ConsensusMessageChunkInfo & proto.ConsensusMessageChunkInfo.$Shape} ConsensusMessageChunkInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusMessageChunkInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConsensusMessageChunkInfo message.
         * @function verify
         * @memberof proto.ConsensusMessageChunkInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConsensusMessageChunkInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.initialTransactionID != null && $Object.hasOwnProperty.call(message, "initialTransactionID")) {
                let error = $root.proto.TransactionID.verify(message.initialTransactionID, _depth + 1);
                if (error)
                    return "initialTransactionID." + error;
            }
            if (message.total != null && $Object.hasOwnProperty.call(message, "total"))
                if (!$util.isInteger(message.total))
                    return "total: integer expected";
            if (message.number != null && $Object.hasOwnProperty.call(message, "number"))
                if (!$util.isInteger(message.number))
                    return "number: integer expected";
            return null;
        };

        /**
         * Creates a ConsensusMessageChunkInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ConsensusMessageChunkInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ConsensusMessageChunkInfo} ConsensusMessageChunkInfo
         */
        ConsensusMessageChunkInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ConsensusMessageChunkInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ConsensusMessageChunkInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ConsensusMessageChunkInfo();
            if (object.initialTransactionID != null) {
                if (!$util.isObject(object.initialTransactionID))
                    throw $TypeError(".proto.ConsensusMessageChunkInfo.initialTransactionID: object expected");
                message.initialTransactionID = $root.proto.TransactionID.fromObject(object.initialTransactionID, _depth + 1);
            }
            if (object.total != null)
                if ($Number(object.total) !== 0)
                    message.total = object.total | 0;
            if (object.number != null)
                if ($Number(object.number) !== 0)
                    message.number = object.number | 0;
            return message;
        };

        /**
         * Creates a plain object from a ConsensusMessageChunkInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ConsensusMessageChunkInfo
         * @static
         * @param {proto.ConsensusMessageChunkInfo} message ConsensusMessageChunkInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConsensusMessageChunkInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.initialTransactionID = null;
                object.total = 0;
                object.number = 0;
            }
            if (message.initialTransactionID != null && $Object.hasOwnProperty.call(message, "initialTransactionID"))
                object.initialTransactionID = $root.proto.TransactionID.toObject(message.initialTransactionID, options, _depth + 1);
            if (message.total != null && $Object.hasOwnProperty.call(message, "total"))
                object.total = message.total;
            if (message.number != null && $Object.hasOwnProperty.call(message, "number"))
                object.number = message.number;
            return object;
        };

        /**
         * Converts this ConsensusMessageChunkInfo to JSON.
         * @function toJSON
         * @memberof proto.ConsensusMessageChunkInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConsensusMessageChunkInfo.prototype.toJSON = function() {
            return ConsensusMessageChunkInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConsensusMessageChunkInfo
         * @function getTypeUrl
         * @memberof proto.ConsensusMessageChunkInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConsensusMessageChunkInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ConsensusMessageChunkInfo";
        };

        return ConsensusMessageChunkInfo;
    })();

    proto.ConsensusSubmitMessageTransactionBody = (function() {

        /**
         * Properties of a ConsensusSubmitMessageTransactionBody.
         * @typedef {Object} proto.ConsensusSubmitMessageTransactionBody.$Properties
         * @property {proto.TopicID.$Properties|null} [topicID] Topic to submit message to.
         * @property {Uint8Array|null} [message] A message to be submitted.
         * <p>
         * This Transaction (including signatures) MUST be less than 6KiB.<br/>
         * Messages SHOULD be less than 4KiB. A "chunked" message MAY be submitted
         * if a message larger than this is required.
         * @property {proto.ConsensusMessageChunkInfo.$Properties|null} [chunkInfo] Information for the current "chunk" in a fragmented message.
         * <p>
         * This value is REQUIRED if the full `message` is submitted in two or
         * more fragments due to transaction size limits.<br/>
         * If the message is submitted in a single transaction, then this
         * field SHOULD NOT be set.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ConsensusSubmitMessageTransactionBody.
         * @memberof proto
         * @interface IConsensusSubmitMessageTransactionBody
         * @augments proto.ConsensusSubmitMessageTransactionBody.$Properties
         * @deprecated Use proto.ConsensusSubmitMessageTransactionBody.$Properties instead.
         */

        /**
         * Shape of a ConsensusSubmitMessageTransactionBody.
         * @typedef {{
         *   topicID?: proto.TopicID.$Shape|null;
         *   message?: Uint8Array|null;
         *   chunkInfo?: proto.ConsensusMessageChunkInfo.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ConsensusSubmitMessageTransactionBody.$Shape
         */

        /**
         * Constructs a new ConsensusSubmitMessageTransactionBody.
         * @memberof proto
         * @classdesc Submit a message for consensus.<br/>
         * This transaction adds a new entry to the "end" of a topic, and provides
         * the core function of the consensus service.
         * 
         * Valid and authorized messages on valid topics SHALL be ordered by the
         * consensus service, published in the block stream, and available to all
         * subscribers on this topic via the mirror nodes.<br/>
         * If this transaction succeeds the resulting `TransactionReceipt` SHALL contain
         * the latest `topicSequenceNumber` and `topicRunningHash` for the topic.<br/>
         * If the topic `submitKey` is set, and not an empty `KeyList`, then that key
         * MUST sign this transaction.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.ConsensusSubmitMessageTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ConsensusSubmitMessageTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Topic to submit message to.
         * @member {proto.TopicID.$Properties|null|undefined} topicID
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @instance
         */
        ConsensusSubmitMessageTransactionBody.prototype.topicID = null;

        /**
         * A message to be submitted.
         * <p>
         * This Transaction (including signatures) MUST be less than 6KiB.<br/>
         * Messages SHOULD be less than 4KiB. A "chunked" message MAY be submitted
         * if a message larger than this is required.
         * @member {Uint8Array} message
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @instance
         */
        ConsensusSubmitMessageTransactionBody.prototype.message = $util.newBuffer([]);

        /**
         * Information for the current "chunk" in a fragmented message.
         * <p>
         * This value is REQUIRED if the full `message` is submitted in two or
         * more fragments due to transaction size limits.<br/>
         * If the message is submitted in a single transaction, then this
         * field SHOULD NOT be set.
         * @member {proto.ConsensusMessageChunkInfo.$Properties|null|undefined} chunkInfo
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @instance
         */
        ConsensusSubmitMessageTransactionBody.prototype.chunkInfo = null;

        /**
         * Creates a new ConsensusSubmitMessageTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @static
         * @param {proto.ConsensusSubmitMessageTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.ConsensusSubmitMessageTransactionBody} ConsensusSubmitMessageTransactionBody instance
         * @type {{
         *   (properties: proto.ConsensusSubmitMessageTransactionBody.$Shape): proto.ConsensusSubmitMessageTransactionBody & proto.ConsensusSubmitMessageTransactionBody.$Shape;
         *   (properties?: proto.ConsensusSubmitMessageTransactionBody.$Properties): proto.ConsensusSubmitMessageTransactionBody;
         * }}
         */
        ConsensusSubmitMessageTransactionBody.create = function(properties) {
            return new ConsensusSubmitMessageTransactionBody(properties);
        };

        /**
         * Encodes the specified ConsensusSubmitMessageTransactionBody message. Does not implicitly {@link proto.ConsensusSubmitMessageTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @static
         * @param {proto.ConsensusSubmitMessageTransactionBody.$Properties} message ConsensusSubmitMessageTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusSubmitMessageTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID"))
                $root.proto.TopicID.encode(message.topicID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.message);
            if (message.chunkInfo != null && $Object.hasOwnProperty.call(message, "chunkInfo"))
                $root.proto.ConsensusMessageChunkInfo.encode(message.chunkInfo, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConsensusSubmitMessageTransactionBody message, length delimited. Does not implicitly {@link proto.ConsensusSubmitMessageTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @static
         * @param {proto.ConsensusSubmitMessageTransactionBody.$Properties} message ConsensusSubmitMessageTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusSubmitMessageTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ConsensusSubmitMessageTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ConsensusSubmitMessageTransactionBody & proto.ConsensusSubmitMessageTransactionBody.$Shape} ConsensusSubmitMessageTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusSubmitMessageTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ConsensusSubmitMessageTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.topicID = $root.proto.TopicID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.topicID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.chunkInfo = $root.proto.ConsensusMessageChunkInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chunkInfo);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConsensusSubmitMessageTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ConsensusSubmitMessageTransactionBody & proto.ConsensusSubmitMessageTransactionBody.$Shape} ConsensusSubmitMessageTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusSubmitMessageTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConsensusSubmitMessageTransactionBody message.
         * @function verify
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConsensusSubmitMessageTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID")) {
                let error = $root.proto.TopicID.verify(message.topicID, _depth + 1);
                if (error)
                    return "topicID." + error;
            }
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                if (!(message.message && typeof message.message.length === "number" || $util.isString(message.message)))
                    return "message: buffer expected";
            if (message.chunkInfo != null && $Object.hasOwnProperty.call(message, "chunkInfo")) {
                let error = $root.proto.ConsensusMessageChunkInfo.verify(message.chunkInfo, _depth + 1);
                if (error)
                    return "chunkInfo." + error;
            }
            return null;
        };

        /**
         * Creates a ConsensusSubmitMessageTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ConsensusSubmitMessageTransactionBody} ConsensusSubmitMessageTransactionBody
         */
        ConsensusSubmitMessageTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ConsensusSubmitMessageTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ConsensusSubmitMessageTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ConsensusSubmitMessageTransactionBody();
            if (object.topicID != null) {
                if (!$util.isObject(object.topicID))
                    throw $TypeError(".proto.ConsensusSubmitMessageTransactionBody.topicID: object expected");
                message.topicID = $root.proto.TopicID.fromObject(object.topicID, _depth + 1);
            }
            if (object.message != null)
                if (object.message.length)
                    if (typeof object.message === "string")
                        $util.base64.decode(object.message, message.message = $util.newBuffer($util.base64.length(object.message)), 0);
                    else if (object.message.length >= 0)
                        message.message = object.message;
            if (object.chunkInfo != null) {
                if (!$util.isObject(object.chunkInfo))
                    throw $TypeError(".proto.ConsensusSubmitMessageTransactionBody.chunkInfo: object expected");
                message.chunkInfo = $root.proto.ConsensusMessageChunkInfo.fromObject(object.chunkInfo, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ConsensusSubmitMessageTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @static
         * @param {proto.ConsensusSubmitMessageTransactionBody} message ConsensusSubmitMessageTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConsensusSubmitMessageTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.topicID = null;
                if (options.bytes === $String)
                    object.message = "";
                else {
                    object.message = [];
                    if (options.bytes !== $Array)
                        object.message = $util.newBuffer(object.message);
                }
                object.chunkInfo = null;
            }
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID"))
                object.topicID = $root.proto.TopicID.toObject(message.topicID, options, _depth + 1);
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                object.message = options.bytes === $String ? $util.base64.encode(message.message, 0, message.message.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.message) : message.message;
            if (message.chunkInfo != null && $Object.hasOwnProperty.call(message, "chunkInfo"))
                object.chunkInfo = $root.proto.ConsensusMessageChunkInfo.toObject(message.chunkInfo, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ConsensusSubmitMessageTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConsensusSubmitMessageTransactionBody.prototype.toJSON = function() {
            return ConsensusSubmitMessageTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConsensusSubmitMessageTransactionBody
         * @function getTypeUrl
         * @memberof proto.ConsensusSubmitMessageTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConsensusSubmitMessageTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ConsensusSubmitMessageTransactionBody";
        };

        return ConsensusSubmitMessageTransactionBody;
    })();

    proto.UncheckedSubmitBody = (function() {

        /**
         * Properties of an UncheckedSubmitBody.
         * @typedef {Object} proto.UncheckedSubmitBody.$Properties
         * @property {Uint8Array|null} [transactionBytes] The serialized bytes of a `Transaction`.
         * <p>
         * This transaction SHALL be deserialized and submitted for consensus
         * with no further validation.<br/>
         * Specifically, the transaction may violate basic limits and
         * constraints such as size limits, minimum or maximum values,
         * valid start time, fee calculations, etc...
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an UncheckedSubmitBody.
         * @memberof proto
         * @interface IUncheckedSubmitBody
         * @augments proto.UncheckedSubmitBody.$Properties
         * @deprecated Use proto.UncheckedSubmitBody.$Properties instead.
         */

        /**
         * Shape of an UncheckedSubmitBody.
         * @typedef {proto.UncheckedSubmitBody.$Properties} proto.UncheckedSubmitBody.$Shape
         */

        /**
         * Constructs a new UncheckedSubmitBody.
         * @memberof proto
         * @classdesc Submit an arbitrary (serialized) Transaction to the network
         * without pre-check.
         * 
         * This transaction SHALL require `superuser` privileges
         * (e.g. the `treasury` or `systemAdmin` accounts).
         * @constructor
         * @param {proto.UncheckedSubmitBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const UncheckedSubmitBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The serialized bytes of a `Transaction`.
         * <p>
         * This transaction SHALL be deserialized and submitted for consensus
         * with no further validation.<br/>
         * Specifically, the transaction may violate basic limits and
         * constraints such as size limits, minimum or maximum values,
         * valid start time, fee calculations, etc...
         * @member {Uint8Array} transactionBytes
         * @memberof proto.UncheckedSubmitBody
         * @instance
         */
        UncheckedSubmitBody.prototype.transactionBytes = $util.newBuffer([]);

        /**
         * Creates a new UncheckedSubmitBody instance using the specified properties.
         * @function create
         * @memberof proto.UncheckedSubmitBody
         * @static
         * @param {proto.UncheckedSubmitBody.$Properties=} [properties] Properties to set
         * @returns {proto.UncheckedSubmitBody} UncheckedSubmitBody instance
         * @type {{
         *   (properties: proto.UncheckedSubmitBody.$Shape): proto.UncheckedSubmitBody & proto.UncheckedSubmitBody.$Shape;
         *   (properties?: proto.UncheckedSubmitBody.$Properties): proto.UncheckedSubmitBody;
         * }}
         */
        UncheckedSubmitBody.create = function(properties) {
            return new UncheckedSubmitBody(properties);
        };

        /**
         * Encodes the specified UncheckedSubmitBody message. Does not implicitly {@link proto.UncheckedSubmitBody.verify|verify} messages.
         * @function encode
         * @memberof proto.UncheckedSubmitBody
         * @static
         * @param {proto.UncheckedSubmitBody.$Properties} message UncheckedSubmitBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UncheckedSubmitBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.transactionBytes != null && $Object.hasOwnProperty.call(message, "transactionBytes") && message.transactionBytes.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.transactionBytes);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UncheckedSubmitBody message, length delimited. Does not implicitly {@link proto.UncheckedSubmitBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.UncheckedSubmitBody
         * @static
         * @param {proto.UncheckedSubmitBody.$Properties} message UncheckedSubmitBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UncheckedSubmitBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an UncheckedSubmitBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.UncheckedSubmitBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.UncheckedSubmitBody & proto.UncheckedSubmitBody.$Shape} UncheckedSubmitBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UncheckedSubmitBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.UncheckedSubmitBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.transactionBytes = value;
                        else
                            delete message.transactionBytes;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an UncheckedSubmitBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.UncheckedSubmitBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.UncheckedSubmitBody & proto.UncheckedSubmitBody.$Shape} UncheckedSubmitBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UncheckedSubmitBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an UncheckedSubmitBody message.
         * @function verify
         * @memberof proto.UncheckedSubmitBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UncheckedSubmitBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.transactionBytes != null && $Object.hasOwnProperty.call(message, "transactionBytes"))
                if (!(message.transactionBytes && typeof message.transactionBytes.length === "number" || $util.isString(message.transactionBytes)))
                    return "transactionBytes: buffer expected";
            return null;
        };

        /**
         * Creates an UncheckedSubmitBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.UncheckedSubmitBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.UncheckedSubmitBody} UncheckedSubmitBody
         */
        UncheckedSubmitBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.UncheckedSubmitBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.UncheckedSubmitBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.UncheckedSubmitBody();
            if (object.transactionBytes != null)
                if (object.transactionBytes.length)
                    if (typeof object.transactionBytes === "string")
                        $util.base64.decode(object.transactionBytes, message.transactionBytes = $util.newBuffer($util.base64.length(object.transactionBytes)), 0);
                    else if (object.transactionBytes.length >= 0)
                        message.transactionBytes = object.transactionBytes;
            return message;
        };

        /**
         * Creates a plain object from an UncheckedSubmitBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.UncheckedSubmitBody
         * @static
         * @param {proto.UncheckedSubmitBody} message UncheckedSubmitBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UncheckedSubmitBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.transactionBytes = "";
                else {
                    object.transactionBytes = [];
                    if (options.bytes !== $Array)
                        object.transactionBytes = $util.newBuffer(object.transactionBytes);
                }
            if (message.transactionBytes != null && $Object.hasOwnProperty.call(message, "transactionBytes"))
                object.transactionBytes = options.bytes === $String ? $util.base64.encode(message.transactionBytes, 0, message.transactionBytes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.transactionBytes) : message.transactionBytes;
            return object;
        };

        /**
         * Converts this UncheckedSubmitBody to JSON.
         * @function toJSON
         * @memberof proto.UncheckedSubmitBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UncheckedSubmitBody.prototype.toJSON = function() {
            return UncheckedSubmitBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for UncheckedSubmitBody
         * @function getTypeUrl
         * @memberof proto.UncheckedSubmitBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        UncheckedSubmitBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.UncheckedSubmitBody";
        };

        return UncheckedSubmitBody;
    })();

    proto.TokenCreateTransactionBody = (function() {

        /**
         * Properties of a TokenCreateTransactionBody.
         * @typedef {Object} proto.TokenCreateTransactionBody.$Properties
         * @property {string|null} [name] A name for the token.<br/>
         * This is generally the "full name" displayed in wallet software.
         * <p>
         * This field is REQUIRED.<br/>
         * This value MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
         * This value MUST NOT contain the Unicode NUL codepoint.
         * @property {string|null} [symbol] A symbol to use for the token.
         * <p>
         * This field is REQUIRED.<br/>
         * This value MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
         * This value MUST NOT contain the Unicode NUL codepoint.
         * @property {number|null} [decimals] A decimal precision of the token's smallest denomination.<br/>
         * Most values are described in terms of this smallest denomination,
         * so the token initial supply, for instance, must be divided by
         * <tt>10<sup>decimals</sup></tt> to get whole tokens.
         * <p>
         * This MUST be zero(`0`) for non-fungible/unique tokens.
         * @property {number|Long|null} [initialSupply] An initial supply, in the smallest denomination for the token.
         * <p>
         * This amount SHALL be transferred to the treasury account as part
         * of this transaction.<br/>
         * This amount MUST be specified in the smallest denomination for the
         * token (i.e. <tt>10<sup>-decimals</sup></tt> whole tokens).<br/>
         * This MUST be zero(`0`) for a non-fungible/unique token.
         * @property {proto.AccountID.$Properties|null} [treasury] A treasury account identifier.
         * <p>
         * This field is REQUIRED.<br/>
         * The identified account SHALL be designated the "treasury" for the
         * new token, and all tokens "minted" SHALL be delivered to that account,
         * including the initial supply, if any.<br/>
         * The identified account MUST exist, MUST NOT be expired, and SHOULD
         * have a non-zero HBAR balance.<br/>
         * The identified account SHALL be associated to the new token.
         * @property {proto.Key.$Properties|null} [adminKey] An Hedera key for token administration.
         * <p>
         * This key, if set, SHALL have administrative authority for this token and
         * MAY authorize token update and/or token delete transactions.<br/>
         * If this key is not set, or is an empty `KeyList`, this token SHALL be
         * immutable, except for expiration and renewal.
         * @property {proto.Key.$Properties|null} [kycKey] An Hedera key for managing account KYC.
         * <p>
         * This key, if set, SHALL have KYC authority for this token and
         * MAY authorize transactions to grant or revoke KYC for accounts.<br/>
         * If this key is not set, or is an empty `KeyList`, KYC status for this
         * token SHALL NOT be granted or revoked for any account.<br/>
         * If this key is removed after granting KYC, those grants SHALL remain
         * and cannot be revoked.
         * @property {proto.Key.$Properties|null} [freezeKey] An Hedera key for managing asset "freeze".
         * <p>
         * This key, if set, SHALL have "freeze" authority for this token and
         * MAY authorize transactions to freeze or unfreeze accounts
         * with respect to this token.<br/>
         * If this key is not set, or is an empty `KeyList`, this token
         * SHALL NOT be frozen or unfrozen for any account.<br/>
         * If this key is removed after freezing accounts, those accounts
         * SHALL remain frozen and cannot be unfrozen.
         * @property {proto.Key.$Properties|null} [wipeKey] An Hedera key for wiping tokens from accounts.
         * <p>
         * This key, if set, SHALL have "wipe" authority for this token and
         * MAY authorize transactions to "wipe" any amount of this token from
         * any account, effectively burning the tokens "wiped".<br/>
         * If this key is not set, or is an empty `KeyList`, it SHALL NOT be
         * possible to "wipe" this token from an account.
         * @property {proto.Key.$Properties|null} [supplyKey] An Hedera key for "minting" and "burning" tokens.
         * <p>
         * This key, if set, MAY authorize transactions to "mint" new tokens to
         * be delivered to the token treasury or "burn" tokens held by the
         * token treasury.<br/>
         * If this key is not set, or is an empty `KeyList`, it SHALL NOT be
         * possible to change the supply of tokens and neither "mint" nor "burn"
         * transactions SHALL be permitted.
         * @property {boolean|null} [freezeDefault] An initial Freeze status for accounts associated to this token.
         * <p>
         * If this value is set, an account MUST be the subject of a
         * `tokenUnfreeze` transaction after associating to the token before
         * that account can send or receive this token.<br/>
         * If this value is set, the `freezeKey` SHOULD be set.<br/>
         * If the `freezeKey` is not set, any account associated to this token
         * while this value is set SHALL be permanently frozen.
         * <p>
         * <blockquote>REVIEW NOTE<blockquote>
         * Should we prevent setting this value true for tokens with no freeze
         * key?<br/>
         * Should we set this value to false if a freeze key is removed?
         * </blockquote></blockquote>
         * @property {proto.Timestamp.$Properties|null} [expiry] An expiration timestamp.
         * <p>
         * If the `autoRenewAccount` and `autoRenewPeriod` fields are set, this
         * value SHALL be replaced with the current consensus time extended
         * by the `autoRenewPeriod` duration.<br/>
         * If this value is set and token expiration is enabled in network
         * configuration, this token SHALL expire when consensus time exceeds
         * this value, and MAY be subsequently removed from the network state.<br/>
         * If this value is not set, and the automatic renewal account is also not
         * set, then this value SHALL default to the current consensus time
         * extended by the "default" expiration period from network configuration.
         * @property {proto.AccountID.$Properties|null} [autoRenewAccount] An identifier for the account to be charged renewal fees at the token's
         * expiry to extend the lifetime of the token.
         * <p>
         * If this value is set, the token lifetime SHALL be extended by the
         * _smallest_ of the following:
         * <ul>
         * <li>The current `autoRenewPeriod` duration.</li>
         * <li>The maximum duration that this account has funds to purchase.</li>
         * <li>The configured MAX_AUTORENEW_PERIOD at the time of automatic
         * renewal.</li>
         * </ul>
         * If this account's HBAR balance is `0` when the token must be
         * renewed, then the token SHALL be expired, and MAY be subsequently
         * removed from state.<br/>
         * If this value is set, the referenced account MUST sign this
         * transaction.
         * @property {proto.Duration.$Properties|null} [autoRenewPeriod] A duration between token automatic renewals.<br/>
         * All entities in state may be charged "rent" occasionally (typically
         * every 90 days) to prevent unnecessary growth of the ledger. This value
         * sets the interval between such events for this token.
         * <p>
         * This value MUST be set.<br/>
         * This value MUST be greater than the configured
         * MIN_AUTORENEW_PERIOD.<br/>
         * This value MUST be less than the configured MAX_AUTORENEW_PERIOD.
         * @property {string|null} [memo] A short description for this token.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {proto.TokenType|null} [tokenType] A type for this token, according to IWA classification.
         * <p>
         * If this value is not set, the token SHALL have the default type of
         * fungible/common.<br/>
         * This field SHALL be immutable.
         * @property {proto.TokenSupplyType|null} [supplyType] A supply type for this token, according to IWA classification.
         * <p>
         * If this value is not set, the token SHALL have the default supply
         * type of "infinite" (which is, as a practical matter,
         * (2<sup><i>63</i></sup>-1)/10<sup><i>decimals</i></sup>).<br/>
         * This field SHALL be immutable.
         * @property {number|Long|null} [maxSupply] A maximum supply for this token.
         * <p>
         * This SHALL be interpreted in terms of the smallest fractional unit for
         * this token.<br/>
         * If `supplyType` is "infinite", this MUST be `0`.<br/>
         * This field SHALL be immutable.
         * @property {proto.Key.$Properties|null} [feeScheduleKey] An Hedera key for managing the token custom fee schedule.
         * <p>
         * This key, if set, MAY authorize transactions to modify the
         * `custom_fees` for this token.<br/>
         * If this key is not set, or is an empty `KeyList`, the `custom_fees`
         * for this token SHALL NOT be modified.
         * @property {Array.<proto.CustomFee.$Properties>|null} [customFees] A list of custom fees representing a fee schedule.
         * <p>
         * This list MAY be empty, which SHALL mean that there
         * are no custom fees for this token.<br/>
         * If this token is a non-fungible/unique type, the entries
         * in this list MUST NOT declare a `fractional_fee`.<br/>
         * If this token is a fungible/common type, the entries in this
         * list MUST NOT declare a `royalty_fee`.<br/>
         * Any token type MAY include entries that declare a `fixed_fee`.
         * @property {proto.Key.$Properties|null} [pauseKey] An Hedera key for managing token "pause".
         * <p>
         * This key, if set, SHALL have "pause" authority for this token and
         * MAY authorize transactions to pause or unpause this token.<br/>
         * If this key is not set, or is an empty `KeyList`, this token
         * SHALL NOT be paused or unpaused.<br/>
         * If this key is removed while the token is paused, the token cannot
         * be unpaused and SHALL remain paused.
         * @property {Uint8Array|null} [metadata] Token "Metadata".
         * <p>
         * The value, if set, MUST NOT exceed 100 bytes.<br/>
         * <dl><dt>Examples</dt>
         * <dd>hcs://1/0.0.4896575</dd>
         * <dd>ipfs://bafkreifd7tcjjuwxxf4qkaibkj62pj4mhfuud7plwrc3pfoygt55al6syi</dd>
         * </dl>
         * @property {proto.Key.$Properties|null} [metadataKey] An Hedera key for managing the token `metadata`.
         * <p>
         * This key, if set, MAY authorize transactions to modify the
         * `metadata` for this token.<br/>
         * If this key is not set, or is an empty `KeyList`, the `metadata`
         * for this token SHALL NOT be modified.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenCreateTransactionBody.
         * @memberof proto
         * @interface ITokenCreateTransactionBody
         * @augments proto.TokenCreateTransactionBody.$Properties
         * @deprecated Use proto.TokenCreateTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenCreateTransactionBody.
         * @typedef {{
         *   name?: string|null;
         *   symbol?: string|null;
         *   decimals?: number|null;
         *   initialSupply?: number|Long|null;
         *   treasury?: proto.AccountID.$Shape|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   kycKey?: proto.Key.$Shape|null;
         *   freezeKey?: proto.Key.$Shape|null;
         *   wipeKey?: proto.Key.$Shape|null;
         *   supplyKey?: proto.Key.$Shape|null;
         *   freezeDefault?: boolean|null;
         *   expiry?: proto.Timestamp.$Shape|null;
         *   autoRenewAccount?: proto.AccountID.$Shape|null;
         *   autoRenewPeriod?: proto.Duration.$Shape|null;
         *   memo?: string|null;
         *   tokenType?: proto.TokenType|null;
         *   supplyType?: proto.TokenSupplyType|null;
         *   maxSupply?: number|Long|null;
         *   feeScheduleKey?: proto.Key.$Shape|null;
         *   customFees?: Array.<proto.CustomFee.$Shape>|null;
         *   pauseKey?: proto.Key.$Shape|null;
         *   metadata?: Uint8Array|null;
         *   metadataKey?: proto.Key.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenCreateTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenCreateTransactionBody.
         * @memberof proto
         * @classdesc Create an HTS token.
         * 
         * #### Keys
         * Each token has several keys that, separately, control different functions
         * for that token. It is *_strongly_* recommended that each key assigned to
         * a token be unique, or disabled by assigning an empty `KeyList`.
         * Keys and purpose
         * - `adminKey` is a general access and may authorize a token update
         * transaction as well as _update the other keys_. Even the admin key
         * cannot authorize _adding_ a key that is not present, however.<br/>
         * The admin key may also delete the token entirely.
         * - `fee_schedule` may authorize updating the token custom fees. If this
         * key is not present, the custom fees for the token are fixed and immutable.
         * - `freeze` may authorize a token freeze or unfreeze transaction.
         * If this key is not present, accounts holding this token cannot have
         * their tokens frozen or unfrozen.
         * - `kyc` may authorize a token grant KYC or revoke KYC transaction.
         * If this key is not present, accounts holding this token cannot have
         * KYC status granted or revoked.
         * - `metadata` may authorize token update nfts transactions.
         * If this key is not present, the token metadata values for that
         * non-fungible/unique token _type_ will be immutable.
         * - `pause` may authorize a token pause or token unpause transaction.
         * If this key is not present, the token cannot be paused (preventing any
         * account from transacting in that token) or resumed.
         * - `supply` may authorize a token mint or burn transaction.
         * If this key is not present, the token cannot mint additional supply and
         * existing tokens cannot be "burned" from the treasury (but _might_ still be
         * "burned" from individual accounts, c.f. `wipeKey` and `tokenWipe`).
         * - `wipe` may authorize a token wipe account transaction.
         * If this key is not present, accounts holding this token cannot have
         * their balance or NFTs wiped (effectively burned).
         * 
         * #### Requirements
         * If `tokenType` is fungible/common, the `initialSupply` MUST be strictly
         * greater than zero(`0`).<br/>
         * If `tokenType` is non-fungible/unique, the `initialSupply` MUST
         * be zero(`0`).<br/>
         * If `tokenSupplyType` is "infinite", the `maxSupply` MUST be zero(`0`).<br/>
         * If `tokenSupplyType` is "finite", the `maxSupply` MUST be strictly
         * greater than zero(`0`).<br/>
         * 
         * ### Block Stream Effects
         * If the token is created, the Token Identifier SHALL be in the receipt.<br/>
         * @constructor
         * @param {proto.TokenCreateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenCreateTransactionBody = function (properties) {
            this.customFees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A name for the token.<br/>
         * This is generally the "full name" displayed in wallet software.
         * <p>
         * This field is REQUIRED.<br/>
         * This value MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
         * This value MUST NOT contain the Unicode NUL codepoint.
         * @member {string} name
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.name = "";

        /**
         * A symbol to use for the token.
         * <p>
         * This field is REQUIRED.<br/>
         * This value MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
         * This value MUST NOT contain the Unicode NUL codepoint.
         * @member {string} symbol
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.symbol = "";

        /**
         * A decimal precision of the token's smallest denomination.<br/>
         * Most values are described in terms of this smallest denomination,
         * so the token initial supply, for instance, must be divided by
         * <tt>10<sup>decimals</sup></tt> to get whole tokens.
         * <p>
         * This MUST be zero(`0`) for non-fungible/unique tokens.
         * @member {number} decimals
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.decimals = 0;

        /**
         * An initial supply, in the smallest denomination for the token.
         * <p>
         * This amount SHALL be transferred to the treasury account as part
         * of this transaction.<br/>
         * This amount MUST be specified in the smallest denomination for the
         * token (i.e. <tt>10<sup>-decimals</sup></tt> whole tokens).<br/>
         * This MUST be zero(`0`) for a non-fungible/unique token.
         * @member {number|Long} initialSupply
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.initialSupply = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A treasury account identifier.
         * <p>
         * This field is REQUIRED.<br/>
         * The identified account SHALL be designated the "treasury" for the
         * new token, and all tokens "minted" SHALL be delivered to that account,
         * including the initial supply, if any.<br/>
         * The identified account MUST exist, MUST NOT be expired, and SHOULD
         * have a non-zero HBAR balance.<br/>
         * The identified account SHALL be associated to the new token.
         * @member {proto.AccountID.$Properties|null|undefined} treasury
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.treasury = null;

        /**
         * An Hedera key for token administration.
         * <p>
         * This key, if set, SHALL have administrative authority for this token and
         * MAY authorize token update and/or token delete transactions.<br/>
         * If this key is not set, or is an empty `KeyList`, this token SHALL be
         * immutable, except for expiration and renewal.
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.adminKey = null;

        /**
         * An Hedera key for managing account KYC.
         * <p>
         * This key, if set, SHALL have KYC authority for this token and
         * MAY authorize transactions to grant or revoke KYC for accounts.<br/>
         * If this key is not set, or is an empty `KeyList`, KYC status for this
         * token SHALL NOT be granted or revoked for any account.<br/>
         * If this key is removed after granting KYC, those grants SHALL remain
         * and cannot be revoked.
         * @member {proto.Key.$Properties|null|undefined} kycKey
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.kycKey = null;

        /**
         * An Hedera key for managing asset "freeze".
         * <p>
         * This key, if set, SHALL have "freeze" authority for this token and
         * MAY authorize transactions to freeze or unfreeze accounts
         * with respect to this token.<br/>
         * If this key is not set, or is an empty `KeyList`, this token
         * SHALL NOT be frozen or unfrozen for any account.<br/>
         * If this key is removed after freezing accounts, those accounts
         * SHALL remain frozen and cannot be unfrozen.
         * @member {proto.Key.$Properties|null|undefined} freezeKey
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.freezeKey = null;

        /**
         * An Hedera key for wiping tokens from accounts.
         * <p>
         * This key, if set, SHALL have "wipe" authority for this token and
         * MAY authorize transactions to "wipe" any amount of this token from
         * any account, effectively burning the tokens "wiped".<br/>
         * If this key is not set, or is an empty `KeyList`, it SHALL NOT be
         * possible to "wipe" this token from an account.
         * @member {proto.Key.$Properties|null|undefined} wipeKey
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.wipeKey = null;

        /**
         * An Hedera key for "minting" and "burning" tokens.
         * <p>
         * This key, if set, MAY authorize transactions to "mint" new tokens to
         * be delivered to the token treasury or "burn" tokens held by the
         * token treasury.<br/>
         * If this key is not set, or is an empty `KeyList`, it SHALL NOT be
         * possible to change the supply of tokens and neither "mint" nor "burn"
         * transactions SHALL be permitted.
         * @member {proto.Key.$Properties|null|undefined} supplyKey
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.supplyKey = null;

        /**
         * An initial Freeze status for accounts associated to this token.
         * <p>
         * If this value is set, an account MUST be the subject of a
         * `tokenUnfreeze` transaction after associating to the token before
         * that account can send or receive this token.<br/>
         * If this value is set, the `freezeKey` SHOULD be set.<br/>
         * If the `freezeKey` is not set, any account associated to this token
         * while this value is set SHALL be permanently frozen.
         * <p>
         * <blockquote>REVIEW NOTE<blockquote>
         * Should we prevent setting this value true for tokens with no freeze
         * key?<br/>
         * Should we set this value to false if a freeze key is removed?
         * </blockquote></blockquote>
         * @member {boolean} freezeDefault
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.freezeDefault = false;

        /**
         * An expiration timestamp.
         * <p>
         * If the `autoRenewAccount` and `autoRenewPeriod` fields are set, this
         * value SHALL be replaced with the current consensus time extended
         * by the `autoRenewPeriod` duration.<br/>
         * If this value is set and token expiration is enabled in network
         * configuration, this token SHALL expire when consensus time exceeds
         * this value, and MAY be subsequently removed from the network state.<br/>
         * If this value is not set, and the automatic renewal account is also not
         * set, then this value SHALL default to the current consensus time
         * extended by the "default" expiration period from network configuration.
         * @member {proto.Timestamp.$Properties|null|undefined} expiry
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.expiry = null;

        /**
         * An identifier for the account to be charged renewal fees at the token's
         * expiry to extend the lifetime of the token.
         * <p>
         * If this value is set, the token lifetime SHALL be extended by the
         * _smallest_ of the following:
         * <ul>
         * <li>The current `autoRenewPeriod` duration.</li>
         * <li>The maximum duration that this account has funds to purchase.</li>
         * <li>The configured MAX_AUTORENEW_PERIOD at the time of automatic
         * renewal.</li>
         * </ul>
         * If this account's HBAR balance is `0` when the token must be
         * renewed, then the token SHALL be expired, and MAY be subsequently
         * removed from state.<br/>
         * If this value is set, the referenced account MUST sign this
         * transaction.
         * @member {proto.AccountID.$Properties|null|undefined} autoRenewAccount
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.autoRenewAccount = null;

        /**
         * A duration between token automatic renewals.<br/>
         * All entities in state may be charged "rent" occasionally (typically
         * every 90 days) to prevent unnecessary growth of the ledger. This value
         * sets the interval between such events for this token.
         * <p>
         * This value MUST be set.<br/>
         * This value MUST be greater than the configured
         * MIN_AUTORENEW_PERIOD.<br/>
         * This value MUST be less than the configured MAX_AUTORENEW_PERIOD.
         * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.autoRenewPeriod = null;

        /**
         * A short description for this token.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.memo = "";

        /**
         * A type for this token, according to IWA classification.
         * <p>
         * If this value is not set, the token SHALL have the default type of
         * fungible/common.<br/>
         * This field SHALL be immutable.
         * @member {proto.TokenType} tokenType
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.tokenType = 0;

        /**
         * A supply type for this token, according to IWA classification.
         * <p>
         * If this value is not set, the token SHALL have the default supply
         * type of "infinite" (which is, as a practical matter,
         * (2<sup><i>63</i></sup>-1)/10<sup><i>decimals</i></sup>).<br/>
         * This field SHALL be immutable.
         * @member {proto.TokenSupplyType} supplyType
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.supplyType = 0;

        /**
         * A maximum supply for this token.
         * <p>
         * This SHALL be interpreted in terms of the smallest fractional unit for
         * this token.<br/>
         * If `supplyType` is "infinite", this MUST be `0`.<br/>
         * This field SHALL be immutable.
         * @member {number|Long} maxSupply
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.maxSupply = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An Hedera key for managing the token custom fee schedule.
         * <p>
         * This key, if set, MAY authorize transactions to modify the
         * `custom_fees` for this token.<br/>
         * If this key is not set, or is an empty `KeyList`, the `custom_fees`
         * for this token SHALL NOT be modified.
         * @member {proto.Key.$Properties|null|undefined} feeScheduleKey
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.feeScheduleKey = null;

        /**
         * A list of custom fees representing a fee schedule.
         * <p>
         * This list MAY be empty, which SHALL mean that there
         * are no custom fees for this token.<br/>
         * If this token is a non-fungible/unique type, the entries
         * in this list MUST NOT declare a `fractional_fee`.<br/>
         * If this token is a fungible/common type, the entries in this
         * list MUST NOT declare a `royalty_fee`.<br/>
         * Any token type MAY include entries that declare a `fixed_fee`.
         * @member {Array.<proto.CustomFee.$Properties>} customFees
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.customFees = $util.emptyArray;

        /**
         * An Hedera key for managing token "pause".
         * <p>
         * This key, if set, SHALL have "pause" authority for this token and
         * MAY authorize transactions to pause or unpause this token.<br/>
         * If this key is not set, or is an empty `KeyList`, this token
         * SHALL NOT be paused or unpaused.<br/>
         * If this key is removed while the token is paused, the token cannot
         * be unpaused and SHALL remain paused.
         * @member {proto.Key.$Properties|null|undefined} pauseKey
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.pauseKey = null;

        /**
         * Token "Metadata".
         * <p>
         * The value, if set, MUST NOT exceed 100 bytes.<br/>
         * <dl><dt>Examples</dt>
         * <dd>hcs://1/0.0.4896575</dd>
         * <dd>ipfs://bafkreifd7tcjjuwxxf4qkaibkj62pj4mhfuud7plwrc3pfoygt55al6syi</dd>
         * </dl>
         * @member {Uint8Array} metadata
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.metadata = $util.newBuffer([]);

        /**
         * An Hedera key for managing the token `metadata`.
         * <p>
         * This key, if set, MAY authorize transactions to modify the
         * `metadata` for this token.<br/>
         * If this key is not set, or is an empty `KeyList`, the `metadata`
         * for this token SHALL NOT be modified.
         * @member {proto.Key.$Properties|null|undefined} metadataKey
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         */
        TokenCreateTransactionBody.prototype.metadataKey = null;

        /**
         * Creates a new TokenCreateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenCreateTransactionBody
         * @static
         * @param {proto.TokenCreateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenCreateTransactionBody} TokenCreateTransactionBody instance
         * @type {{
         *   (properties: proto.TokenCreateTransactionBody.$Shape): proto.TokenCreateTransactionBody & proto.TokenCreateTransactionBody.$Shape;
         *   (properties?: proto.TokenCreateTransactionBody.$Properties): proto.TokenCreateTransactionBody;
         * }}
         */
        TokenCreateTransactionBody.create = function(properties) {
            return new TokenCreateTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenCreateTransactionBody message. Does not implicitly {@link proto.TokenCreateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenCreateTransactionBody
         * @static
         * @param {proto.TokenCreateTransactionBody.$Properties} message TokenCreateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenCreateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol") && message.symbol !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.symbol);
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals") && message.decimals !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.decimals);
            if (message.initialSupply != null && $Object.hasOwnProperty.call(message, "initialSupply") && (typeof message.initialSupply === "object" ? message.initialSupply.low || message.initialSupply.high : message.initialSupply !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.initialSupply);
            if (message.treasury != null && $Object.hasOwnProperty.call(message, "treasury"))
                $root.proto.AccountID.encode(message.treasury, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.kycKey != null && $Object.hasOwnProperty.call(message, "kycKey"))
                $root.proto.Key.encode(message.kycKey, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.freezeKey != null && $Object.hasOwnProperty.call(message, "freezeKey"))
                $root.proto.Key.encode(message.freezeKey, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.wipeKey != null && $Object.hasOwnProperty.call(message, "wipeKey"))
                $root.proto.Key.encode(message.wipeKey, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.supplyKey != null && $Object.hasOwnProperty.call(message, "supplyKey"))
                $root.proto.Key.encode(message.supplyKey, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.freezeDefault != null && $Object.hasOwnProperty.call(message, "freezeDefault") && message.freezeDefault !== false)
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.freezeDefault);
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                $root.proto.Timestamp.encode(message.expiry, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount"))
                $root.proto.AccountID.encode(message.autoRenewAccount, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.memo);
            if (message.tokenType != null && $Object.hasOwnProperty.call(message, "tokenType") && message.tokenType !== 0)
                writer.uint32(/* id 17, wireType 0 =*/136).int32(message.tokenType);
            if (message.supplyType != null && $Object.hasOwnProperty.call(message, "supplyType") && message.supplyType !== 0)
                writer.uint32(/* id 18, wireType 0 =*/144).int32(message.supplyType);
            if (message.maxSupply != null && $Object.hasOwnProperty.call(message, "maxSupply") && (typeof message.maxSupply === "object" ? message.maxSupply.low || message.maxSupply.high : message.maxSupply !== 0))
                writer.uint32(/* id 19, wireType 0 =*/152).int64(message.maxSupply);
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                $root.proto.Key.encode(message.feeScheduleKey, writer.uint32(/* id 20, wireType 2 =*/162).fork(), _depth + 1).ldelim();
            if (message.customFees != null && message.customFees.length)
                for (let i = 0; i < message.customFees.length; ++i)
                    $root.proto.CustomFee.encode(message.customFees[i], writer.uint32(/* id 21, wireType 2 =*/170).fork(), _depth + 1).ldelim();
            if (message.pauseKey != null && $Object.hasOwnProperty.call(message, "pauseKey"))
                $root.proto.Key.encode(message.pauseKey, writer.uint32(/* id 22, wireType 2 =*/178).fork(), _depth + 1).ldelim();
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata") && message.metadata.length)
                writer.uint32(/* id 23, wireType 2 =*/186).bytes(message.metadata);
            if (message.metadataKey != null && $Object.hasOwnProperty.call(message, "metadataKey"))
                $root.proto.Key.encode(message.metadataKey, writer.uint32(/* id 24, wireType 2 =*/194).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenCreateTransactionBody message, length delimited. Does not implicitly {@link proto.TokenCreateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenCreateTransactionBody
         * @static
         * @param {proto.TokenCreateTransactionBody.$Properties} message TokenCreateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenCreateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenCreateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenCreateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenCreateTransactionBody & proto.TokenCreateTransactionBody.$Shape} TokenCreateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenCreateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenCreateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.symbol = value;
                        else
                            delete message.symbol;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.decimals = value;
                        else
                            delete message.decimals;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.initialSupply = value;
                        else
                            delete message.initialSupply;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.treasury = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.treasury);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.kycKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.kycKey);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.freezeKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.freezeKey);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.wipeKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.wipeKey);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.supplyKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.supplyKey);
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.freezeDefault = value;
                        else
                            delete message.freezeDefault;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.expiry = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expiry);
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewAccount = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewAccount);
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.tokenType = value;
                        else
                            delete message.tokenType;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.supplyType = value;
                        else
                            delete message.supplyType;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maxSupply = value;
                        else
                            delete message.maxSupply;
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        message.feeScheduleKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeScheduleKey);
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        if (!(message.customFees && message.customFees.length))
                            message.customFees = [];
                        message.customFees.push($root.proto.CustomFee.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        message.pauseKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pauseKey);
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.metadata = value;
                        else
                            delete message.metadata;
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        message.metadataKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.metadataKey);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenCreateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenCreateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenCreateTransactionBody & proto.TokenCreateTransactionBody.$Shape} TokenCreateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenCreateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenCreateTransactionBody message.
         * @function verify
         * @memberof proto.TokenCreateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenCreateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                if (!$util.isString(message.symbol))
                    return "symbol: string expected";
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals"))
                if (!$util.isInteger(message.decimals))
                    return "decimals: integer expected";
            if (message.initialSupply != null && $Object.hasOwnProperty.call(message, "initialSupply"))
                if (!$util.isInteger(message.initialSupply) && !(message.initialSupply && $util.isInteger(message.initialSupply.low) && $util.isInteger(message.initialSupply.high)))
                    return "initialSupply: integer|Long expected";
            if (message.treasury != null && $Object.hasOwnProperty.call(message, "treasury")) {
                let error = $root.proto.AccountID.verify(message.treasury, _depth + 1);
                if (error)
                    return "treasury." + error;
            }
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.kycKey != null && $Object.hasOwnProperty.call(message, "kycKey")) {
                let error = $root.proto.Key.verify(message.kycKey, _depth + 1);
                if (error)
                    return "kycKey." + error;
            }
            if (message.freezeKey != null && $Object.hasOwnProperty.call(message, "freezeKey")) {
                let error = $root.proto.Key.verify(message.freezeKey, _depth + 1);
                if (error)
                    return "freezeKey." + error;
            }
            if (message.wipeKey != null && $Object.hasOwnProperty.call(message, "wipeKey")) {
                let error = $root.proto.Key.verify(message.wipeKey, _depth + 1);
                if (error)
                    return "wipeKey." + error;
            }
            if (message.supplyKey != null && $Object.hasOwnProperty.call(message, "supplyKey")) {
                let error = $root.proto.Key.verify(message.supplyKey, _depth + 1);
                if (error)
                    return "supplyKey." + error;
            }
            if (message.freezeDefault != null && $Object.hasOwnProperty.call(message, "freezeDefault"))
                if (typeof message.freezeDefault !== "boolean")
                    return "freezeDefault: boolean expected";
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry")) {
                let error = $root.proto.Timestamp.verify(message.expiry, _depth + 1);
                if (error)
                    return "expiry." + error;
            }
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount")) {
                let error = $root.proto.AccountID.verify(message.autoRenewAccount, _depth + 1);
                if (error)
                    return "autoRenewAccount." + error;
            }
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                if (error)
                    return "autoRenewPeriod." + error;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.tokenType != null && $Object.hasOwnProperty.call(message, "tokenType"))
                if (typeof message.tokenType !== "number" || (message.tokenType | 0) !== message.tokenType)
                    return "tokenType: enum value expected";
            if (message.supplyType != null && $Object.hasOwnProperty.call(message, "supplyType"))
                if (typeof message.supplyType !== "number" || (message.supplyType | 0) !== message.supplyType)
                    return "supplyType: enum value expected";
            if (message.maxSupply != null && $Object.hasOwnProperty.call(message, "maxSupply"))
                if (!$util.isInteger(message.maxSupply) && !(message.maxSupply && $util.isInteger(message.maxSupply.low) && $util.isInteger(message.maxSupply.high)))
                    return "maxSupply: integer|Long expected";
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey")) {
                let error = $root.proto.Key.verify(message.feeScheduleKey, _depth + 1);
                if (error)
                    return "feeScheduleKey." + error;
            }
            if (message.customFees != null && $Object.hasOwnProperty.call(message, "customFees")) {
                if (!$Array.isArray(message.customFees))
                    return "customFees: array expected";
                for (let i = 0; i < message.customFees.length; ++i) {
                    let error = $root.proto.CustomFee.verify(message.customFees[i], _depth + 1);
                    if (error)
                        return "customFees." + error;
                }
            }
            if (message.pauseKey != null && $Object.hasOwnProperty.call(message, "pauseKey")) {
                let error = $root.proto.Key.verify(message.pauseKey, _depth + 1);
                if (error)
                    return "pauseKey." + error;
            }
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                if (!(message.metadata && typeof message.metadata.length === "number" || $util.isString(message.metadata)))
                    return "metadata: buffer expected";
            if (message.metadataKey != null && $Object.hasOwnProperty.call(message, "metadataKey")) {
                let error = $root.proto.Key.verify(message.metadataKey, _depth + 1);
                if (error)
                    return "metadataKey." + error;
            }
            return null;
        };

        /**
         * Creates a TokenCreateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenCreateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenCreateTransactionBody} TokenCreateTransactionBody
         */
        TokenCreateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenCreateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenCreateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenCreateTransactionBody();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.symbol != null)
                if (typeof object.symbol !== "string" || object.symbol.length)
                    message.symbol = $String(object.symbol);
            if (object.decimals != null)
                if ($Number(object.decimals) !== 0)
                    message.decimals = object.decimals >>> 0;
            if (object.initialSupply != null)
                if (typeof object.initialSupply === "object" ? object.initialSupply.low || object.initialSupply.high : $Number(object.initialSupply) !== 0)
                    if ($util.Long)
                        message.initialSupply = $util.Long.fromValue(object.initialSupply, true);
                    else if (typeof object.initialSupply === "string")
                        message.initialSupply = $parseInt(object.initialSupply, 10);
                    else if (typeof object.initialSupply === "number")
                        message.initialSupply = object.initialSupply;
                    else if (typeof object.initialSupply === "object")
                        message.initialSupply = new $util.LongBits(object.initialSupply.low >>> 0, object.initialSupply.high >>> 0).toNumber(true);
            if (object.treasury != null) {
                if (!$util.isObject(object.treasury))
                    throw $TypeError(".proto.TokenCreateTransactionBody.treasury: object expected");
                message.treasury = $root.proto.AccountID.fromObject(object.treasury, _depth + 1);
            }
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.TokenCreateTransactionBody.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.kycKey != null) {
                if (!$util.isObject(object.kycKey))
                    throw $TypeError(".proto.TokenCreateTransactionBody.kycKey: object expected");
                message.kycKey = $root.proto.Key.fromObject(object.kycKey, _depth + 1);
            }
            if (object.freezeKey != null) {
                if (!$util.isObject(object.freezeKey))
                    throw $TypeError(".proto.TokenCreateTransactionBody.freezeKey: object expected");
                message.freezeKey = $root.proto.Key.fromObject(object.freezeKey, _depth + 1);
            }
            if (object.wipeKey != null) {
                if (!$util.isObject(object.wipeKey))
                    throw $TypeError(".proto.TokenCreateTransactionBody.wipeKey: object expected");
                message.wipeKey = $root.proto.Key.fromObject(object.wipeKey, _depth + 1);
            }
            if (object.supplyKey != null) {
                if (!$util.isObject(object.supplyKey))
                    throw $TypeError(".proto.TokenCreateTransactionBody.supplyKey: object expected");
                message.supplyKey = $root.proto.Key.fromObject(object.supplyKey, _depth + 1);
            }
            if (object.freezeDefault != null)
                if (object.freezeDefault)
                    message.freezeDefault = $Boolean(object.freezeDefault);
            if (object.expiry != null) {
                if (!$util.isObject(object.expiry))
                    throw $TypeError(".proto.TokenCreateTransactionBody.expiry: object expected");
                message.expiry = $root.proto.Timestamp.fromObject(object.expiry, _depth + 1);
            }
            if (object.autoRenewAccount != null) {
                if (!$util.isObject(object.autoRenewAccount))
                    throw $TypeError(".proto.TokenCreateTransactionBody.autoRenewAccount: object expected");
                message.autoRenewAccount = $root.proto.AccountID.fromObject(object.autoRenewAccount, _depth + 1);
            }
            if (object.autoRenewPeriod != null) {
                if (!$util.isObject(object.autoRenewPeriod))
                    throw $TypeError(".proto.TokenCreateTransactionBody.autoRenewPeriod: object expected");
                message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
            }
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.tokenType !== 0 && (typeof object.tokenType !== "string" || $root.proto.TokenType[object.tokenType] !== 0))
                switch (object.tokenType) {
                case "FUNGIBLE_COMMON":
                case 0:
                    message.tokenType = 0;
                    break;
                case "NON_FUNGIBLE_UNIQUE":
                case 1:
                    message.tokenType = 1;
                    break;
                default:
                    if (typeof object.tokenType === "number" && (object.tokenType | 0) === object.tokenType)
                        message.tokenType = object.tokenType;
                }
            if (object.supplyType !== 0 && (typeof object.supplyType !== "string" || $root.proto.TokenSupplyType[object.supplyType] !== 0))
                switch (object.supplyType) {
                case "INFINITE":
                case 0:
                    message.supplyType = 0;
                    break;
                case "FINITE":
                case 1:
                    message.supplyType = 1;
                    break;
                default:
                    if (typeof object.supplyType === "number" && (object.supplyType | 0) === object.supplyType)
                        message.supplyType = object.supplyType;
                }
            if (object.maxSupply != null)
                if (typeof object.maxSupply === "object" ? object.maxSupply.low || object.maxSupply.high : $Number(object.maxSupply) !== 0)
                    if ($util.Long)
                        message.maxSupply = $util.Long.fromValue(object.maxSupply, false);
                    else if (typeof object.maxSupply === "string")
                        message.maxSupply = $parseInt(object.maxSupply, 10);
                    else if (typeof object.maxSupply === "number")
                        message.maxSupply = object.maxSupply;
                    else if (typeof object.maxSupply === "object")
                        message.maxSupply = new $util.LongBits(object.maxSupply.low >>> 0, object.maxSupply.high >>> 0).toNumber();
            if (object.feeScheduleKey != null) {
                if (!$util.isObject(object.feeScheduleKey))
                    throw $TypeError(".proto.TokenCreateTransactionBody.feeScheduleKey: object expected");
                message.feeScheduleKey = $root.proto.Key.fromObject(object.feeScheduleKey, _depth + 1);
            }
            if (object.customFees) {
                if (!$Array.isArray(object.customFees))
                    throw $TypeError(".proto.TokenCreateTransactionBody.customFees: array expected");
                message.customFees = $Array(object.customFees.length);
                for (let i = 0; i < object.customFees.length; ++i) {
                    if (!$util.isObject(object.customFees[i]))
                        throw $TypeError(".proto.TokenCreateTransactionBody.customFees: object expected");
                    message.customFees[i] = $root.proto.CustomFee.fromObject(object.customFees[i], _depth + 1);
                }
            }
            if (object.pauseKey != null) {
                if (!$util.isObject(object.pauseKey))
                    throw $TypeError(".proto.TokenCreateTransactionBody.pauseKey: object expected");
                message.pauseKey = $root.proto.Key.fromObject(object.pauseKey, _depth + 1);
            }
            if (object.metadata != null)
                if (object.metadata.length)
                    if (typeof object.metadata === "string")
                        $util.base64.decode(object.metadata, message.metadata = $util.newBuffer($util.base64.length(object.metadata)), 0);
                    else if (object.metadata.length >= 0)
                        message.metadata = object.metadata;
            if (object.metadataKey != null) {
                if (!$util.isObject(object.metadataKey))
                    throw $TypeError(".proto.TokenCreateTransactionBody.metadataKey: object expected");
                message.metadataKey = $root.proto.Key.fromObject(object.metadataKey, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenCreateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenCreateTransactionBody
         * @static
         * @param {proto.TokenCreateTransactionBody} message TokenCreateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenCreateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.customFees = [];
            if (options.defaults) {
                object.name = "";
                object.symbol = "";
                object.decimals = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.initialSupply = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.initialSupply = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.treasury = null;
                object.adminKey = null;
                object.kycKey = null;
                object.freezeKey = null;
                object.wipeKey = null;
                object.supplyKey = null;
                object.freezeDefault = false;
                object.expiry = null;
                object.autoRenewAccount = null;
                object.autoRenewPeriod = null;
                object.memo = "";
                object.tokenType = options.enums === $String ? "FUNGIBLE_COMMON" : 0;
                object.supplyType = options.enums === $String ? "INFINITE" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxSupply = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maxSupply = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.feeScheduleKey = null;
                object.pauseKey = null;
                if (options.bytes === $String)
                    object.metadata = "";
                else {
                    object.metadata = [];
                    if (options.bytes !== $Array)
                        object.metadata = $util.newBuffer(object.metadata);
                }
                object.metadataKey = null;
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                object.symbol = message.symbol;
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals"))
                object.decimals = message.decimals;
            if (message.initialSupply != null && $Object.hasOwnProperty.call(message, "initialSupply"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.initialSupply = typeof message.initialSupply === "number" ? $BigInt(message.initialSupply) : $util.Long.fromBits(message.initialSupply.low >>> 0, message.initialSupply.high >>> 0, true).toBigInt();
                else if (typeof message.initialSupply === "number")
                    object.initialSupply = options.longs === $String ? $String(message.initialSupply) : message.initialSupply;
                else
                    object.initialSupply = options.longs === $String ? $util.Long.prototype.toString.call(message.initialSupply) : options.longs === $Number ? new $util.LongBits(message.initialSupply.low >>> 0, message.initialSupply.high >>> 0).toNumber(true) : message.initialSupply;
            if (message.treasury != null && $Object.hasOwnProperty.call(message, "treasury"))
                object.treasury = $root.proto.AccountID.toObject(message.treasury, options, _depth + 1);
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.kycKey != null && $Object.hasOwnProperty.call(message, "kycKey"))
                object.kycKey = $root.proto.Key.toObject(message.kycKey, options, _depth + 1);
            if (message.freezeKey != null && $Object.hasOwnProperty.call(message, "freezeKey"))
                object.freezeKey = $root.proto.Key.toObject(message.freezeKey, options, _depth + 1);
            if (message.wipeKey != null && $Object.hasOwnProperty.call(message, "wipeKey"))
                object.wipeKey = $root.proto.Key.toObject(message.wipeKey, options, _depth + 1);
            if (message.supplyKey != null && $Object.hasOwnProperty.call(message, "supplyKey"))
                object.supplyKey = $root.proto.Key.toObject(message.supplyKey, options, _depth + 1);
            if (message.freezeDefault != null && $Object.hasOwnProperty.call(message, "freezeDefault"))
                object.freezeDefault = message.freezeDefault;
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                object.expiry = $root.proto.Timestamp.toObject(message.expiry, options, _depth + 1);
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount"))
                object.autoRenewAccount = $root.proto.AccountID.toObject(message.autoRenewAccount, options, _depth + 1);
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.tokenType != null && $Object.hasOwnProperty.call(message, "tokenType"))
                object.tokenType = options.enums === $String ? $root.proto.TokenType[message.tokenType] === $undefined ? message.tokenType : $root.proto.TokenType[message.tokenType] : message.tokenType;
            if (message.supplyType != null && $Object.hasOwnProperty.call(message, "supplyType"))
                object.supplyType = options.enums === $String ? $root.proto.TokenSupplyType[message.supplyType] === $undefined ? message.supplyType : $root.proto.TokenSupplyType[message.supplyType] : message.supplyType;
            if (message.maxSupply != null && $Object.hasOwnProperty.call(message, "maxSupply"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maxSupply = typeof message.maxSupply === "number" ? $BigInt(message.maxSupply) : $util.Long.fromBits(message.maxSupply.low >>> 0, message.maxSupply.high >>> 0, false).toBigInt();
                else if (typeof message.maxSupply === "number")
                    object.maxSupply = options.longs === $String ? $String(message.maxSupply) : message.maxSupply;
                else
                    object.maxSupply = options.longs === $String ? $util.Long.prototype.toString.call(message.maxSupply) : options.longs === $Number ? new $util.LongBits(message.maxSupply.low >>> 0, message.maxSupply.high >>> 0).toNumber() : message.maxSupply;
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                object.feeScheduleKey = $root.proto.Key.toObject(message.feeScheduleKey, options, _depth + 1);
            if (message.customFees && message.customFees.length) {
                object.customFees = $Array(message.customFees.length);
                for (let j = 0; j < message.customFees.length; ++j)
                    object.customFees[j] = $root.proto.CustomFee.toObject(message.customFees[j], options, _depth + 1);
            }
            if (message.pauseKey != null && $Object.hasOwnProperty.call(message, "pauseKey"))
                object.pauseKey = $root.proto.Key.toObject(message.pauseKey, options, _depth + 1);
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                object.metadata = options.bytes === $String ? $util.base64.encode(message.metadata, 0, message.metadata.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.metadata) : message.metadata;
            if (message.metadataKey != null && $Object.hasOwnProperty.call(message, "metadataKey"))
                object.metadataKey = $root.proto.Key.toObject(message.metadataKey, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenCreateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenCreateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenCreateTransactionBody.prototype.toJSON = function() {
            return TokenCreateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenCreateTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenCreateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenCreateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenCreateTransactionBody";
        };

        return TokenCreateTransactionBody;
    })();

    proto.TokenFreezeAccountTransactionBody = (function() {

        /**
         * Properties of a TokenFreezeAccountTransactionBody.
         * @typedef {Object} proto.TokenFreezeAccountTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.
         * <p>
         * This SHALL identify the token type to "freeze".<br/>
         * The identified token MUST exist, MUST NOT be deleted, and MUST be
         * associated to the identified account.
         * @property {proto.AccountID.$Properties|null} [account] An account identifier.
         * <p>
         * This shall identify the account to "freeze".<br/>
         * The identified account MUST exist, MUST NOT be deleted, MUST NOT be
         * expired, and MUST be associated to the identified token.<br/>
         * The identified account SHOULD NOT be "frozen" with respect to the
         * identified token.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenFreezeAccountTransactionBody.
         * @memberof proto
         * @interface ITokenFreezeAccountTransactionBody
         * @augments proto.TokenFreezeAccountTransactionBody.$Properties
         * @deprecated Use proto.TokenFreezeAccountTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenFreezeAccountTransactionBody.
         * @typedef {{
         *   token?: proto.TokenID.$Shape|null;
         *   account?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenFreezeAccountTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenFreezeAccountTransactionBody.
         * @memberof proto
         * @classdesc Block transfers of a token type for an account.<br/>
         * This, effectively, freezes assets of one account with respect to
         * one token type. While frozen, that account cannot send or receive tokens
         * of the identified type.
         * 
         * The token MUST have a `freeze_key` set and that key MUST NOT
         * be an empty `KeyList`.<br/>
         * The token `freeze_key` MUST sign this transaction.<br/>
         * The identified token MUST exist, MUST NOT be deleted, MUST NOT be paused,
         * and MUST NOT be expired.<br/>
         * The identified account MUST exist, MUST NOT be deleted, and
         * MUST NOT be expired.<br/>
         * If the identified account is already frozen with respect to the identified
         * token, the transaction SHALL succeed, but no change SHALL be made.<br/>
         * An association between the identified account and the identified
         * token MUST exist.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenFreezeAccountTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenFreezeAccountTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * This SHALL identify the token type to "freeze".<br/>
         * The identified token MUST exist, MUST NOT be deleted, and MUST be
         * associated to the identified account.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenFreezeAccountTransactionBody
         * @instance
         */
        TokenFreezeAccountTransactionBody.prototype.token = null;

        /**
         * An account identifier.
         * <p>
         * This shall identify the account to "freeze".<br/>
         * The identified account MUST exist, MUST NOT be deleted, MUST NOT be
         * expired, and MUST be associated to the identified token.<br/>
         * The identified account SHOULD NOT be "frozen" with respect to the
         * identified token.
         * @member {proto.AccountID.$Properties|null|undefined} account
         * @memberof proto.TokenFreezeAccountTransactionBody
         * @instance
         */
        TokenFreezeAccountTransactionBody.prototype.account = null;

        /**
         * Creates a new TokenFreezeAccountTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenFreezeAccountTransactionBody
         * @static
         * @param {proto.TokenFreezeAccountTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenFreezeAccountTransactionBody} TokenFreezeAccountTransactionBody instance
         * @type {{
         *   (properties: proto.TokenFreezeAccountTransactionBody.$Shape): proto.TokenFreezeAccountTransactionBody & proto.TokenFreezeAccountTransactionBody.$Shape;
         *   (properties?: proto.TokenFreezeAccountTransactionBody.$Properties): proto.TokenFreezeAccountTransactionBody;
         * }}
         */
        TokenFreezeAccountTransactionBody.create = function(properties) {
            return new TokenFreezeAccountTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenFreezeAccountTransactionBody message. Does not implicitly {@link proto.TokenFreezeAccountTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenFreezeAccountTransactionBody
         * @static
         * @param {proto.TokenFreezeAccountTransactionBody.$Properties} message TokenFreezeAccountTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenFreezeAccountTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                $root.proto.AccountID.encode(message.account, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenFreezeAccountTransactionBody message, length delimited. Does not implicitly {@link proto.TokenFreezeAccountTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenFreezeAccountTransactionBody
         * @static
         * @param {proto.TokenFreezeAccountTransactionBody.$Properties} message TokenFreezeAccountTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenFreezeAccountTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenFreezeAccountTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenFreezeAccountTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenFreezeAccountTransactionBody & proto.TokenFreezeAccountTransactionBody.$Shape} TokenFreezeAccountTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenFreezeAccountTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenFreezeAccountTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.account = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.account);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenFreezeAccountTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenFreezeAccountTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenFreezeAccountTransactionBody & proto.TokenFreezeAccountTransactionBody.$Shape} TokenFreezeAccountTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenFreezeAccountTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenFreezeAccountTransactionBody message.
         * @function verify
         * @memberof proto.TokenFreezeAccountTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenFreezeAccountTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            if (message.account != null && $Object.hasOwnProperty.call(message, "account")) {
                let error = $root.proto.AccountID.verify(message.account, _depth + 1);
                if (error)
                    return "account." + error;
            }
            return null;
        };

        /**
         * Creates a TokenFreezeAccountTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenFreezeAccountTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenFreezeAccountTransactionBody} TokenFreezeAccountTransactionBody
         */
        TokenFreezeAccountTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenFreezeAccountTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenFreezeAccountTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenFreezeAccountTransactionBody();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenFreezeAccountTransactionBody.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            if (object.account != null) {
                if (!$util.isObject(object.account))
                    throw $TypeError(".proto.TokenFreezeAccountTransactionBody.account: object expected");
                message.account = $root.proto.AccountID.fromObject(object.account, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenFreezeAccountTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenFreezeAccountTransactionBody
         * @static
         * @param {proto.TokenFreezeAccountTransactionBody} message TokenFreezeAccountTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenFreezeAccountTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.token = null;
                object.account = null;
            }
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = $root.proto.AccountID.toObject(message.account, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenFreezeAccountTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenFreezeAccountTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenFreezeAccountTransactionBody.prototype.toJSON = function() {
            return TokenFreezeAccountTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenFreezeAccountTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenFreezeAccountTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenFreezeAccountTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenFreezeAccountTransactionBody";
        };

        return TokenFreezeAccountTransactionBody;
    })();

    proto.TokenUnfreezeAccountTransactionBody = (function() {

        /**
         * Properties of a TokenUnfreezeAccountTransactionBody.
         * @typedef {Object} proto.TokenUnfreezeAccountTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.
         * <p>
         * This SHALL identify the token type to "unfreeze".<br/>
         * The identified token MUST exist, MUST NOT be deleted, and MUST be
         * associated to the identified account.
         * @property {proto.AccountID.$Properties|null} [account] An account identifier.
         * <p>
         * This shall identify the account to "unfreeze".<br/>
         * The identified account MUST exist, MUST NOT be deleted, MUST NOT be
         * expired, and MUST be associated to the identified token.<br/>
         * The identified account SHOULD be "frozen" with respect to the
         * identified token.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenUnfreezeAccountTransactionBody.
         * @memberof proto
         * @interface ITokenUnfreezeAccountTransactionBody
         * @augments proto.TokenUnfreezeAccountTransactionBody.$Properties
         * @deprecated Use proto.TokenUnfreezeAccountTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenUnfreezeAccountTransactionBody.
         * @typedef {{
         *   token?: proto.TokenID.$Shape|null;
         *   account?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenUnfreezeAccountTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenUnfreezeAccountTransactionBody.
         * @memberof proto
         * @classdesc Resume transfers of a token type for an account.<br/>
         * This releases previously frozen assets of one account with respect to
         * one token type. Once unfrozen, that account can once again send or
         * receive tokens of the identified type.
         * 
         * The token MUST have a `freeze_key` set and that key MUST NOT
         * be an empty `KeyList`.<br/>
         * The token `freeze_key` MUST sign this transaction.<br/>
         * The identified token MUST exist, MUST NOT be deleted, MUST NOT be paused,
         * and MUST NOT be expired.<br/>
         * The identified account MUST exist, MUST NOT be deleted, and
         * MUST NOT be expired.<br/>
         * If the identified account is not frozen with respect to the identified
         * token, the transaction SHALL succeed, but no change SHALL be made.<br/>
         * An association between the identified account and the identified
         * token MUST exist.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenUnfreezeAccountTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenUnfreezeAccountTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * This SHALL identify the token type to "unfreeze".<br/>
         * The identified token MUST exist, MUST NOT be deleted, and MUST be
         * associated to the identified account.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenUnfreezeAccountTransactionBody
         * @instance
         */
        TokenUnfreezeAccountTransactionBody.prototype.token = null;

        /**
         * An account identifier.
         * <p>
         * This shall identify the account to "unfreeze".<br/>
         * The identified account MUST exist, MUST NOT be deleted, MUST NOT be
         * expired, and MUST be associated to the identified token.<br/>
         * The identified account SHOULD be "frozen" with respect to the
         * identified token.
         * @member {proto.AccountID.$Properties|null|undefined} account
         * @memberof proto.TokenUnfreezeAccountTransactionBody
         * @instance
         */
        TokenUnfreezeAccountTransactionBody.prototype.account = null;

        /**
         * Creates a new TokenUnfreezeAccountTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenUnfreezeAccountTransactionBody
         * @static
         * @param {proto.TokenUnfreezeAccountTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenUnfreezeAccountTransactionBody} TokenUnfreezeAccountTransactionBody instance
         * @type {{
         *   (properties: proto.TokenUnfreezeAccountTransactionBody.$Shape): proto.TokenUnfreezeAccountTransactionBody & proto.TokenUnfreezeAccountTransactionBody.$Shape;
         *   (properties?: proto.TokenUnfreezeAccountTransactionBody.$Properties): proto.TokenUnfreezeAccountTransactionBody;
         * }}
         */
        TokenUnfreezeAccountTransactionBody.create = function(properties) {
            return new TokenUnfreezeAccountTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenUnfreezeAccountTransactionBody message. Does not implicitly {@link proto.TokenUnfreezeAccountTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenUnfreezeAccountTransactionBody
         * @static
         * @param {proto.TokenUnfreezeAccountTransactionBody.$Properties} message TokenUnfreezeAccountTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenUnfreezeAccountTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                $root.proto.AccountID.encode(message.account, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenUnfreezeAccountTransactionBody message, length delimited. Does not implicitly {@link proto.TokenUnfreezeAccountTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenUnfreezeAccountTransactionBody
         * @static
         * @param {proto.TokenUnfreezeAccountTransactionBody.$Properties} message TokenUnfreezeAccountTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenUnfreezeAccountTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenUnfreezeAccountTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenUnfreezeAccountTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenUnfreezeAccountTransactionBody & proto.TokenUnfreezeAccountTransactionBody.$Shape} TokenUnfreezeAccountTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenUnfreezeAccountTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenUnfreezeAccountTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.account = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.account);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenUnfreezeAccountTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenUnfreezeAccountTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenUnfreezeAccountTransactionBody & proto.TokenUnfreezeAccountTransactionBody.$Shape} TokenUnfreezeAccountTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenUnfreezeAccountTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenUnfreezeAccountTransactionBody message.
         * @function verify
         * @memberof proto.TokenUnfreezeAccountTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenUnfreezeAccountTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            if (message.account != null && $Object.hasOwnProperty.call(message, "account")) {
                let error = $root.proto.AccountID.verify(message.account, _depth + 1);
                if (error)
                    return "account." + error;
            }
            return null;
        };

        /**
         * Creates a TokenUnfreezeAccountTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenUnfreezeAccountTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenUnfreezeAccountTransactionBody} TokenUnfreezeAccountTransactionBody
         */
        TokenUnfreezeAccountTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenUnfreezeAccountTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenUnfreezeAccountTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenUnfreezeAccountTransactionBody();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenUnfreezeAccountTransactionBody.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            if (object.account != null) {
                if (!$util.isObject(object.account))
                    throw $TypeError(".proto.TokenUnfreezeAccountTransactionBody.account: object expected");
                message.account = $root.proto.AccountID.fromObject(object.account, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenUnfreezeAccountTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenUnfreezeAccountTransactionBody
         * @static
         * @param {proto.TokenUnfreezeAccountTransactionBody} message TokenUnfreezeAccountTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenUnfreezeAccountTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.token = null;
                object.account = null;
            }
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = $root.proto.AccountID.toObject(message.account, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenUnfreezeAccountTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenUnfreezeAccountTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenUnfreezeAccountTransactionBody.prototype.toJSON = function() {
            return TokenUnfreezeAccountTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenUnfreezeAccountTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenUnfreezeAccountTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenUnfreezeAccountTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenUnfreezeAccountTransactionBody";
        };

        return TokenUnfreezeAccountTransactionBody;
    })();

    proto.TokenGrantKycTransactionBody = (function() {

        /**
         * Properties of a TokenGrantKycTransactionBody.
         * @typedef {Object} proto.TokenGrantKycTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.
         * <p>
         * The identified token SHALL grant "KYC" for the account
         * identified by the `account` field.<br/>
         * The identified token MUST be associated to the account identified
         * by the `account` field.
         * @property {proto.AccountID.$Properties|null} [account] An account identifier.
         * <p>
         * The token identified by the `token` field SHALL grant "KYC" for the
         * identified account.<br/>
         * This account MUST be associated to the token identified
         * by the `token` field.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenGrantKycTransactionBody.
         * @memberof proto
         * @interface ITokenGrantKycTransactionBody
         * @augments proto.TokenGrantKycTransactionBody.$Properties
         * @deprecated Use proto.TokenGrantKycTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenGrantKycTransactionBody.
         * @typedef {{
         *   token?: proto.TokenID.$Shape|null;
         *   account?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenGrantKycTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenGrantKycTransactionBody.
         * @memberof proto
         * @classdesc Grant "Know Your Customer"(KYC) for one account for a single token.
         * 
         * This transaction MUST be signed by the `kyc_key` for the token.<br/>
         * The identified token MUST have a `kyc_key` set to a valid `Key` value.<br/>
         * The token `kyc_key` MUST NOT be an empty `KeyList`.<br/>
         * The identified token MUST exist and MUST NOT be deleted.<br/>
         * The identified account MUST exist and MUST NOT be deleted.<br/>
         * The identified account MUST have an association to the identified token.<br/>
         * On success the association between the identified account and the identified
         * token SHALL be marked as "KYC granted".
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenGrantKycTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenGrantKycTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * The identified token SHALL grant "KYC" for the account
         * identified by the `account` field.<br/>
         * The identified token MUST be associated to the account identified
         * by the `account` field.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenGrantKycTransactionBody
         * @instance
         */
        TokenGrantKycTransactionBody.prototype.token = null;

        /**
         * An account identifier.
         * <p>
         * The token identified by the `token` field SHALL grant "KYC" for the
         * identified account.<br/>
         * This account MUST be associated to the token identified
         * by the `token` field.
         * @member {proto.AccountID.$Properties|null|undefined} account
         * @memberof proto.TokenGrantKycTransactionBody
         * @instance
         */
        TokenGrantKycTransactionBody.prototype.account = null;

        /**
         * Creates a new TokenGrantKycTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenGrantKycTransactionBody
         * @static
         * @param {proto.TokenGrantKycTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenGrantKycTransactionBody} TokenGrantKycTransactionBody instance
         * @type {{
         *   (properties: proto.TokenGrantKycTransactionBody.$Shape): proto.TokenGrantKycTransactionBody & proto.TokenGrantKycTransactionBody.$Shape;
         *   (properties?: proto.TokenGrantKycTransactionBody.$Properties): proto.TokenGrantKycTransactionBody;
         * }}
         */
        TokenGrantKycTransactionBody.create = function(properties) {
            return new TokenGrantKycTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenGrantKycTransactionBody message. Does not implicitly {@link proto.TokenGrantKycTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenGrantKycTransactionBody
         * @static
         * @param {proto.TokenGrantKycTransactionBody.$Properties} message TokenGrantKycTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGrantKycTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                $root.proto.AccountID.encode(message.account, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenGrantKycTransactionBody message, length delimited. Does not implicitly {@link proto.TokenGrantKycTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenGrantKycTransactionBody
         * @static
         * @param {proto.TokenGrantKycTransactionBody.$Properties} message TokenGrantKycTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGrantKycTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenGrantKycTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenGrantKycTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenGrantKycTransactionBody & proto.TokenGrantKycTransactionBody.$Shape} TokenGrantKycTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGrantKycTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenGrantKycTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.account = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.account);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenGrantKycTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenGrantKycTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenGrantKycTransactionBody & proto.TokenGrantKycTransactionBody.$Shape} TokenGrantKycTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGrantKycTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenGrantKycTransactionBody message.
         * @function verify
         * @memberof proto.TokenGrantKycTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenGrantKycTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            if (message.account != null && $Object.hasOwnProperty.call(message, "account")) {
                let error = $root.proto.AccountID.verify(message.account, _depth + 1);
                if (error)
                    return "account." + error;
            }
            return null;
        };

        /**
         * Creates a TokenGrantKycTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenGrantKycTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenGrantKycTransactionBody} TokenGrantKycTransactionBody
         */
        TokenGrantKycTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenGrantKycTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenGrantKycTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenGrantKycTransactionBody();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenGrantKycTransactionBody.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            if (object.account != null) {
                if (!$util.isObject(object.account))
                    throw $TypeError(".proto.TokenGrantKycTransactionBody.account: object expected");
                message.account = $root.proto.AccountID.fromObject(object.account, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenGrantKycTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenGrantKycTransactionBody
         * @static
         * @param {proto.TokenGrantKycTransactionBody} message TokenGrantKycTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenGrantKycTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.token = null;
                object.account = null;
            }
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = $root.proto.AccountID.toObject(message.account, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenGrantKycTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenGrantKycTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenGrantKycTransactionBody.prototype.toJSON = function() {
            return TokenGrantKycTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenGrantKycTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenGrantKycTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenGrantKycTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenGrantKycTransactionBody";
        };

        return TokenGrantKycTransactionBody;
    })();

    proto.TokenRevokeKycTransactionBody = (function() {

        /**
         * Properties of a TokenRevokeKycTransactionBody.
         * @typedef {Object} proto.TokenRevokeKycTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.
         * <p>
         * The identified token SHALL revoke "KYC" for the account
         * identified by the `account` field.<br/>
         * The identified token MUST be associated to the account identified
         * by the `account` field.
         * @property {proto.AccountID.$Properties|null} [account] An account identifier.
         * <p>
         * The token identified by the `token` field SHALL revoke "KYC" for the
         * identified account.<br/>
         * This account MUST be associated to the token identified
         * by the `token` field.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenRevokeKycTransactionBody.
         * @memberof proto
         * @interface ITokenRevokeKycTransactionBody
         * @augments proto.TokenRevokeKycTransactionBody.$Properties
         * @deprecated Use proto.TokenRevokeKycTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenRevokeKycTransactionBody.
         * @typedef {{
         *   token?: proto.TokenID.$Shape|null;
         *   account?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenRevokeKycTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenRevokeKycTransactionBody.
         * @memberof proto
         * @classdesc Revoke "Know Your Customer"(KYC) from one account for a single token.
         * 
         * This transaction MUST be signed by the `kyc_key` for the token.<br/>
         * The identified token MUST have a `kyc_key` set to a valid `Key` value.<br/>
         * The token `kyc_key` MUST NOT be an empty `KeyList`.<br/>
         * The identified token MUST exist and MUST NOT be deleted.<br/>
         * The identified account MUST exist and MUST NOT be deleted.<br/>
         * The identified account MUST have an association to the identified token.<br/>
         * On success the association between the identified account and the identified
         * token SHALL NOT be marked as "KYC granted".
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenRevokeKycTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenRevokeKycTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * The identified token SHALL revoke "KYC" for the account
         * identified by the `account` field.<br/>
         * The identified token MUST be associated to the account identified
         * by the `account` field.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenRevokeKycTransactionBody
         * @instance
         */
        TokenRevokeKycTransactionBody.prototype.token = null;

        /**
         * An account identifier.
         * <p>
         * The token identified by the `token` field SHALL revoke "KYC" for the
         * identified account.<br/>
         * This account MUST be associated to the token identified
         * by the `token` field.
         * @member {proto.AccountID.$Properties|null|undefined} account
         * @memberof proto.TokenRevokeKycTransactionBody
         * @instance
         */
        TokenRevokeKycTransactionBody.prototype.account = null;

        /**
         * Creates a new TokenRevokeKycTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenRevokeKycTransactionBody
         * @static
         * @param {proto.TokenRevokeKycTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenRevokeKycTransactionBody} TokenRevokeKycTransactionBody instance
         * @type {{
         *   (properties: proto.TokenRevokeKycTransactionBody.$Shape): proto.TokenRevokeKycTransactionBody & proto.TokenRevokeKycTransactionBody.$Shape;
         *   (properties?: proto.TokenRevokeKycTransactionBody.$Properties): proto.TokenRevokeKycTransactionBody;
         * }}
         */
        TokenRevokeKycTransactionBody.create = function(properties) {
            return new TokenRevokeKycTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenRevokeKycTransactionBody message. Does not implicitly {@link proto.TokenRevokeKycTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenRevokeKycTransactionBody
         * @static
         * @param {proto.TokenRevokeKycTransactionBody.$Properties} message TokenRevokeKycTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenRevokeKycTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                $root.proto.AccountID.encode(message.account, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenRevokeKycTransactionBody message, length delimited. Does not implicitly {@link proto.TokenRevokeKycTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenRevokeKycTransactionBody
         * @static
         * @param {proto.TokenRevokeKycTransactionBody.$Properties} message TokenRevokeKycTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenRevokeKycTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenRevokeKycTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenRevokeKycTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenRevokeKycTransactionBody & proto.TokenRevokeKycTransactionBody.$Shape} TokenRevokeKycTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenRevokeKycTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenRevokeKycTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.account = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.account);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenRevokeKycTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenRevokeKycTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenRevokeKycTransactionBody & proto.TokenRevokeKycTransactionBody.$Shape} TokenRevokeKycTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenRevokeKycTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenRevokeKycTransactionBody message.
         * @function verify
         * @memberof proto.TokenRevokeKycTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenRevokeKycTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            if (message.account != null && $Object.hasOwnProperty.call(message, "account")) {
                let error = $root.proto.AccountID.verify(message.account, _depth + 1);
                if (error)
                    return "account." + error;
            }
            return null;
        };

        /**
         * Creates a TokenRevokeKycTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenRevokeKycTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenRevokeKycTransactionBody} TokenRevokeKycTransactionBody
         */
        TokenRevokeKycTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenRevokeKycTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenRevokeKycTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenRevokeKycTransactionBody();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenRevokeKycTransactionBody.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            if (object.account != null) {
                if (!$util.isObject(object.account))
                    throw $TypeError(".proto.TokenRevokeKycTransactionBody.account: object expected");
                message.account = $root.proto.AccountID.fromObject(object.account, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenRevokeKycTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenRevokeKycTransactionBody
         * @static
         * @param {proto.TokenRevokeKycTransactionBody} message TokenRevokeKycTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenRevokeKycTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.token = null;
                object.account = null;
            }
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = $root.proto.AccountID.toObject(message.account, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenRevokeKycTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenRevokeKycTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenRevokeKycTransactionBody.prototype.toJSON = function() {
            return TokenRevokeKycTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenRevokeKycTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenRevokeKycTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenRevokeKycTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenRevokeKycTransactionBody";
        };

        return TokenRevokeKycTransactionBody;
    })();

    proto.TokenDeleteTransactionBody = (function() {

        /**
         * Properties of a TokenDeleteTransactionBody.
         * @typedef {Object} proto.TokenDeleteTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.
         * <p>
         * This SHALL identify the token type to delete.<br/>
         * The identified token MUST exist, and MUST NOT be deleted.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenDeleteTransactionBody.
         * @memberof proto
         * @interface ITokenDeleteTransactionBody
         * @augments proto.TokenDeleteTransactionBody.$Properties
         * @deprecated Use proto.TokenDeleteTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenDeleteTransactionBody.
         * @typedef {proto.TokenDeleteTransactionBody.$Properties} proto.TokenDeleteTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenDeleteTransactionBody.
         * @memberof proto
         * @classdesc Mark a token as deleted.<br/>
         * A deleted token remains present in the network state, but is no longer
         * active, cannot be held in a balance, and all operations on that token
         * fail. A deleted token is removed from network state when it expires.
         * 
         * #### Operations on a deleted token
         * All operations on a deleted token SHALL fail with a
         * status code `TOKEN_WAS_DELETED`.<br/>
         * Any attempt to transfer a deleted token between accounts SHALL fail with
         * a status code `TOKEN_WAS_DELETED`.
         * 
         * > QUESTIONS
         * >> What happens to existing balances/NFTs?
         * >> Are these removed; are they stuck on the accounts?
         * >
         * >> If balances/NFTs remain, can a `tokenReject` remove them?
         * 
         * #### Requirements
         * The `admin_key` for the token MUST be set, and MUST
         * sign this transaction.<br/>
         * If the `admin_key` for the token is not set, this transaction SHALL
         * fail with a status code `TOKEN_IS_IMMUTABlE`.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenDeleteTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenDeleteTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * This SHALL identify the token type to delete.<br/>
         * The identified token MUST exist, and MUST NOT be deleted.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenDeleteTransactionBody
         * @instance
         */
        TokenDeleteTransactionBody.prototype.token = null;

        /**
         * Creates a new TokenDeleteTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenDeleteTransactionBody
         * @static
         * @param {proto.TokenDeleteTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenDeleteTransactionBody} TokenDeleteTransactionBody instance
         * @type {{
         *   (properties: proto.TokenDeleteTransactionBody.$Shape): proto.TokenDeleteTransactionBody & proto.TokenDeleteTransactionBody.$Shape;
         *   (properties?: proto.TokenDeleteTransactionBody.$Properties): proto.TokenDeleteTransactionBody;
         * }}
         */
        TokenDeleteTransactionBody.create = function(properties) {
            return new TokenDeleteTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenDeleteTransactionBody message. Does not implicitly {@link proto.TokenDeleteTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenDeleteTransactionBody
         * @static
         * @param {proto.TokenDeleteTransactionBody.$Properties} message TokenDeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenDeleteTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenDeleteTransactionBody message, length delimited. Does not implicitly {@link proto.TokenDeleteTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenDeleteTransactionBody
         * @static
         * @param {proto.TokenDeleteTransactionBody.$Properties} message TokenDeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenDeleteTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenDeleteTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenDeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenDeleteTransactionBody & proto.TokenDeleteTransactionBody.$Shape} TokenDeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenDeleteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenDeleteTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenDeleteTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenDeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenDeleteTransactionBody & proto.TokenDeleteTransactionBody.$Shape} TokenDeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenDeleteTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenDeleteTransactionBody message.
         * @function verify
         * @memberof proto.TokenDeleteTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenDeleteTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            return null;
        };

        /**
         * Creates a TokenDeleteTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenDeleteTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenDeleteTransactionBody} TokenDeleteTransactionBody
         */
        TokenDeleteTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenDeleteTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenDeleteTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenDeleteTransactionBody();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenDeleteTransactionBody.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenDeleteTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenDeleteTransactionBody
         * @static
         * @param {proto.TokenDeleteTransactionBody} message TokenDeleteTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenDeleteTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.token = null;
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenDeleteTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenDeleteTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenDeleteTransactionBody.prototype.toJSON = function() {
            return TokenDeleteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenDeleteTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenDeleteTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenDeleteTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenDeleteTransactionBody";
        };

        return TokenDeleteTransactionBody;
    })();

    proto.TokenUpdateTransactionBody = (function() {

        /**
         * Properties of a TokenUpdateTransactionBody.
         * @typedef {Object} proto.TokenUpdateTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.
         * <p>
         * This SHALL identify the token type to delete.<br/>
         * The identified token MUST exist, and MUST NOT be deleted.<br/>
         * If any field other than `expiry` is set, the identified token MUST
         * have a valid `admin_key`.
         * @property {string|null} [symbol] A new symbol to use for the token.
         * <p>
         * This value, if set, MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
         * This value, if set, MUST NOT contain the Unicode NUL codepoint.
         * @property {string|null} [name] A new name for the token.<br/>
         * This is generally the "full name" displayed in wallet software.
         * <p>
         * This value, if set, MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
         * This value, if set, MUST NOT contain the Unicode NUL codepoint.
         * @property {proto.AccountID.$Properties|null} [treasury] A new treasury account identifier.
         * <p>
         * If set,
         * - The identified account SHALL be designated the "treasury" for the
         * token, and all tokens "minted" SHALL be delivered to that account
         * following this transaction.<br/>
         * - The identified account MUST exist, MUST NOT be expired, MUST NOT be
         * deleted, and SHOULD have a non-zero HBAR balance.<br/>
         * - The identified account SHALL be associated to this token.
         * - The full balance of this token held by the prior treasury account
         * SHALL be transferred to the new treasury account, if the token type
         * is fungible/common.
         * - If the token type is non-fungible/unique, the previous treasury
         * account MUST NOT hold any tokens of this type.
         * - The new treasury account key MUST sign this transaction.
         * @property {proto.Key.$Properties|null} [adminKey] An Hedera key for token administration.
         * <p>
         * This key, if set, SHALL have administrative authority for this token and
         * MAY authorize token update and/or token delete transactions.<br/>
         * If this key is set to an empty `KeyList`, this token SHALL be
         * immutable thereafter, except for expiration and renewal.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @property {proto.Key.$Properties|null} [kycKey] An Hedera key for managing account KYC.
         * <p>
         * This key, if set, SHALL have KYC authority for this token and
         * MAY authorize transactions to grant or revoke KYC for accounts.<br/>
         * If this key is not set, or is an empty `KeyList`, KYC status for this
         * token SHALL NOT be granted or revoked for any account.<br/>
         * If this key is removed after granting KYC, those grants SHALL remain
         * and cannot be revoked.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @property {proto.Key.$Properties|null} [freezeKey] An Hedera key for managing asset "freeze".
         * <p>
         * This key, if set, SHALL have "freeze" authority for this token and
         * MAY authorize transactions to freeze or unfreeze accounts
         * with respect to this token.<br/>
         * If this key is set to an empty `KeyList`, this token
         * SHALL NOT be frozen or unfrozen for any account.<br/>
         * If this key is removed after freezing accounts, those accounts
         * SHALL remain frozen and cannot be unfrozen.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @property {proto.Key.$Properties|null} [wipeKey] An Hedera key for wiping tokens from accounts.
         * <p>
         * This key, if set, SHALL have "wipe" authority for this token and
         * MAY authorize transactions to "wipe" any amount of this token from
         * any account, effectively burning the tokens "wiped".<br/>
         * If this key is set to an empty `KeyList`, it SHALL NOT be
         * possible to "wipe" this token from an account.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @property {proto.Key.$Properties|null} [supplyKey] An Hedera key for "minting" and "burning" tokens.
         * <p>
         * This key, if set, MAY authorize transactions to "mint" new tokens to
         * be delivered to the token treasury or "burn" tokens held by the
         * token treasury.<br/>
         * If this key is set to an empty `KeyList`, it SHALL NOT be
         * possible to change the supply of tokens and neither "mint" nor "burn"
         * transactions SHALL be permitted.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @property {proto.AccountID.$Properties|null} [autoRenewAccount] An identifier for the account to be charged renewal fees at the token's
         * expiry to extend the lifetime of the token.
         * <p>
         * If this value is set for the identified token, the token lifetime SHALL
         * be extended by the _smallest_ of the following at expiration:
         * <ul>
         * <li>The current `autoRenewPeriod` duration.</li>
         * <li>The maximum duration that this account has funds to purchase.</li>
         * <li>The configured MAX_AUTORENEW_PERIOD at the time of automatic
         * renewal.</li>
         * </ul>
         * If this account's HBAR balance is `0` when the token must be
         * renewed, then the token SHALL be expired, and MAY be subsequently
         * removed from state.<br/>
         * If this value is set, the referenced account MUST sign this
         * transaction.
         * <p>
         * <blockquote>Note<blockquote>
         * It is not currently possible to remove an automatic renewal account.
         * Once set, it can only be replaced by a valid account.
         * </blockquote></blockquote>
         * @property {proto.Duration.$Properties|null} [autoRenewPeriod] A duration between token automatic renewals.<br/>
         * All entities in state may be charged "rent" occasionally (typically
         * every 90 days) to prevent unnecessary growth of the ledger. This value
         * sets the interval between such events for this token.
         * <p>
         * If set, this value MUST be greater than the configured
         * `MIN_AUTORENEW_PERIOD`.<br/>
         * If set, this value MUST be less than the configured
         * `MAX_AUTORENEW_PERIOD`.
         * @property {proto.Timestamp.$Properties|null} [expiry] An expiration timestamp.
         * <p>
         * If this value is set, the automatic renewal account is not set for the
         * identified token, and token expiration is enabled in network
         * configuration, this token SHALL expire when the consensus time exceeds
         * this value, and MAY be subsequently removed from the network state.<br/>
         * If `autoRenewAccount` is set or the `auto_renew_account_id` is set for
         * the identified token, the token SHALL be subject to automatic renewal
         * when the consensus time exceeds this value.
         * @property {google.protobuf.StringValue.$Properties|null} [memo] A short description for this token.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {proto.Key.$Properties|null} [feeScheduleKey] An Hedera key for managing the token custom fee schedule.
         * <p>
         * This key, if set, MAY authorize transactions to modify the
         * `custom_fees` for this token.<br/>
         * If this key is set to an empty `KeyList`, the `custom_fees`
         * for this token SHALL NOT be modified.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @property {proto.Key.$Properties|null} [pauseKey] An Hedera key for managing token "pause".
         * <p>
         * This key, if set, SHALL have "pause" authority for this token and
         * MAY authorize transactions to pause or unpause this token.<br/>
         * If this key is set to an empty `KeyList`, this token
         * SHALL NOT be paused or unpaused.<br/>
         * If this key is removed while the token is paused, the token cannot
         * be unpaused and SHALL remain paused.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @property {google.protobuf.BytesValue.$Properties|null} [metadata] Token "Metadata".
         * <p>
         * The value, if set, MUST NOT exceed 100 bytes.<br/>
         * <dl><dt>Examples</dt>
         * <dd>hcs://1/0.0.4896575</dd>
         * <dd>ipfs://bafkreifd7tcjjuwxxf4qkaibkj62pj4mhfuud7plwrc3pfoygt55al6syi</dd>
         * </dl>
         * @property {proto.Key.$Properties|null} [metadataKey] An Hedera key for managing the token `metadata`.
         * <p>
         * This key, if set, MAY authorize transactions to modify the
         * `metadata` for this token.<br/>
         * If this key is set to an empty `KeyList`, the `metadata`
         * for this token SHALL NOT be modified.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @property {proto.TokenKeyValidation|null} [keyVerificationMode] Set a key validation mode.<br/>
         * Any key may be updated by a transaction signed by the token `admin_key`.
         * Each role key may _also_ sign a transaction to update that key.
         * If a role key signs an update to change that role key both old
         * and new key must sign the transaction, _unless_ this field is set
         * to `NO_VALIDATION`, in which case the _new_ key is not required to
         * sign the transaction (the existing key is still required).<br/>
         * The primary intent for this field is to allow a role key (e.g. a
         * `pause_key`) holder to "remove" that key from the token by signing
         * a transaction to set that role key to an empty `KeyList`.
         * <p>
         * If set to `FULL_VALIDATION`, either the `admin_key` or _both_ current
         * and new key MUST sign this transaction to update a "key" field for the
         * identified token.<br/>
         * If set to `NO_VALIDATION`, either the `admin_key` or the current
         * key MUST sign this transaction to update a "key" field for the
         * identified token.<br/>
         * This field SHALL be treated as `FULL_VALIDATION` if not set.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenUpdateTransactionBody.
         * @memberof proto
         * @interface ITokenUpdateTransactionBody
         * @augments proto.TokenUpdateTransactionBody.$Properties
         * @deprecated Use proto.TokenUpdateTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenUpdateTransactionBody.
         * @typedef {{
         *   token?: proto.TokenID.$Shape|null;
         *   symbol?: string|null;
         *   name?: string|null;
         *   treasury?: proto.AccountID.$Shape|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   kycKey?: proto.Key.$Shape|null;
         *   freezeKey?: proto.Key.$Shape|null;
         *   wipeKey?: proto.Key.$Shape|null;
         *   supplyKey?: proto.Key.$Shape|null;
         *   autoRenewAccount?: proto.AccountID.$Shape|null;
         *   autoRenewPeriod?: proto.Duration.$Shape|null;
         *   expiry?: proto.Timestamp.$Shape|null;
         *   memo?: google.protobuf.StringValue.$Shape|null;
         *   feeScheduleKey?: proto.Key.$Shape|null;
         *   pauseKey?: proto.Key.$Shape|null;
         *   metadata?: google.protobuf.BytesValue.$Shape|null;
         *   metadataKey?: proto.Key.$Shape|null;
         *   keyVerificationMode?: proto.TokenKeyValidation|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenUpdateTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenUpdateTransactionBody.
         * @memberof proto
         * @classdesc Update an existing token.
         * 
         * This transaction SHALL NOT update any field that is not set.<br/>
         * Most changes MUST be signed by the current `admin_key` of the token. If the
         * token does not currently have a valid `admin_key`, then this transaction
         * MUST NOT set any value other than `expiry` or a non-admin key.<br/>
         * If the `treasury` is set to a new account, the new account MUST sign this
         * transaction.<br/>
         * If the `treasury` is set to a new account for a _non-fungible/unique_ token,
         * The current treasury MAY hold some tokens.
         * 
         * #### Requirements for Keys
         * Any of the key values may be changed, even without an admin key, but the
         * key to be changed MUST have an existing valid key assigned, and both the
         * current key and the new key MUST sign the transaction.<br/>
         * A key value MAY be set to an empty `KeyList`. In this case the existing
         * key MUST sign this transaction, but the new value is not a valid key, and the
         * update SHALL effectively remove the existing key.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenUpdateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenUpdateTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * This SHALL identify the token type to delete.<br/>
         * The identified token MUST exist, and MUST NOT be deleted.<br/>
         * If any field other than `expiry` is set, the identified token MUST
         * have a valid `admin_key`.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.token = null;

        /**
         * A new symbol to use for the token.
         * <p>
         * This value, if set, MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
         * This value, if set, MUST NOT contain the Unicode NUL codepoint.
         * @member {string} symbol
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.symbol = "";

        /**
         * A new name for the token.<br/>
         * This is generally the "full name" displayed in wallet software.
         * <p>
         * This value, if set, MUST NOT exceed 100 bytes when encoded as UTF-8.<br/>
         * This value, if set, MUST NOT contain the Unicode NUL codepoint.
         * @member {string} name
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.name = "";

        /**
         * A new treasury account identifier.
         * <p>
         * If set,
         * - The identified account SHALL be designated the "treasury" for the
         * token, and all tokens "minted" SHALL be delivered to that account
         * following this transaction.<br/>
         * - The identified account MUST exist, MUST NOT be expired, MUST NOT be
         * deleted, and SHOULD have a non-zero HBAR balance.<br/>
         * - The identified account SHALL be associated to this token.
         * - The full balance of this token held by the prior treasury account
         * SHALL be transferred to the new treasury account, if the token type
         * is fungible/common.
         * - If the token type is non-fungible/unique, the previous treasury
         * account MUST NOT hold any tokens of this type.
         * - The new treasury account key MUST sign this transaction.
         * @member {proto.AccountID.$Properties|null|undefined} treasury
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.treasury = null;

        /**
         * An Hedera key for token administration.
         * <p>
         * This key, if set, SHALL have administrative authority for this token and
         * MAY authorize token update and/or token delete transactions.<br/>
         * If this key is set to an empty `KeyList`, this token SHALL be
         * immutable thereafter, except for expiration and renewal.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.adminKey = null;

        /**
         * An Hedera key for managing account KYC.
         * <p>
         * This key, if set, SHALL have KYC authority for this token and
         * MAY authorize transactions to grant or revoke KYC for accounts.<br/>
         * If this key is not set, or is an empty `KeyList`, KYC status for this
         * token SHALL NOT be granted or revoked for any account.<br/>
         * If this key is removed after granting KYC, those grants SHALL remain
         * and cannot be revoked.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @member {proto.Key.$Properties|null|undefined} kycKey
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.kycKey = null;

        /**
         * An Hedera key for managing asset "freeze".
         * <p>
         * This key, if set, SHALL have "freeze" authority for this token and
         * MAY authorize transactions to freeze or unfreeze accounts
         * with respect to this token.<br/>
         * If this key is set to an empty `KeyList`, this token
         * SHALL NOT be frozen or unfrozen for any account.<br/>
         * If this key is removed after freezing accounts, those accounts
         * SHALL remain frozen and cannot be unfrozen.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @member {proto.Key.$Properties|null|undefined} freezeKey
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.freezeKey = null;

        /**
         * An Hedera key for wiping tokens from accounts.
         * <p>
         * This key, if set, SHALL have "wipe" authority for this token and
         * MAY authorize transactions to "wipe" any amount of this token from
         * any account, effectively burning the tokens "wiped".<br/>
         * If this key is set to an empty `KeyList`, it SHALL NOT be
         * possible to "wipe" this token from an account.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @member {proto.Key.$Properties|null|undefined} wipeKey
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.wipeKey = null;

        /**
         * An Hedera key for "minting" and "burning" tokens.
         * <p>
         * This key, if set, MAY authorize transactions to "mint" new tokens to
         * be delivered to the token treasury or "burn" tokens held by the
         * token treasury.<br/>
         * If this key is set to an empty `KeyList`, it SHALL NOT be
         * possible to change the supply of tokens and neither "mint" nor "burn"
         * transactions SHALL be permitted.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @member {proto.Key.$Properties|null|undefined} supplyKey
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.supplyKey = null;

        /**
         * An identifier for the account to be charged renewal fees at the token's
         * expiry to extend the lifetime of the token.
         * <p>
         * If this value is set for the identified token, the token lifetime SHALL
         * be extended by the _smallest_ of the following at expiration:
         * <ul>
         * <li>The current `autoRenewPeriod` duration.</li>
         * <li>The maximum duration that this account has funds to purchase.</li>
         * <li>The configured MAX_AUTORENEW_PERIOD at the time of automatic
         * renewal.</li>
         * </ul>
         * If this account's HBAR balance is `0` when the token must be
         * renewed, then the token SHALL be expired, and MAY be subsequently
         * removed from state.<br/>
         * If this value is set, the referenced account MUST sign this
         * transaction.
         * <p>
         * <blockquote>Note<blockquote>
         * It is not currently possible to remove an automatic renewal account.
         * Once set, it can only be replaced by a valid account.
         * </blockquote></blockquote>
         * @member {proto.AccountID.$Properties|null|undefined} autoRenewAccount
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.autoRenewAccount = null;

        /**
         * A duration between token automatic renewals.<br/>
         * All entities in state may be charged "rent" occasionally (typically
         * every 90 days) to prevent unnecessary growth of the ledger. This value
         * sets the interval between such events for this token.
         * <p>
         * If set, this value MUST be greater than the configured
         * `MIN_AUTORENEW_PERIOD`.<br/>
         * If set, this value MUST be less than the configured
         * `MAX_AUTORENEW_PERIOD`.
         * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.autoRenewPeriod = null;

        /**
         * An expiration timestamp.
         * <p>
         * If this value is set, the automatic renewal account is not set for the
         * identified token, and token expiration is enabled in network
         * configuration, this token SHALL expire when the consensus time exceeds
         * this value, and MAY be subsequently removed from the network state.<br/>
         * If `autoRenewAccount` is set or the `auto_renew_account_id` is set for
         * the identified token, the token SHALL be subject to automatic renewal
         * when the consensus time exceeds this value.
         * @member {proto.Timestamp.$Properties|null|undefined} expiry
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.expiry = null;

        /**
         * A short description for this token.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {google.protobuf.StringValue.$Properties|null|undefined} memo
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.memo = null;

        /**
         * An Hedera key for managing the token custom fee schedule.
         * <p>
         * This key, if set, MAY authorize transactions to modify the
         * `custom_fees` for this token.<br/>
         * If this key is set to an empty `KeyList`, the `custom_fees`
         * for this token SHALL NOT be modified.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @member {proto.Key.$Properties|null|undefined} feeScheduleKey
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.feeScheduleKey = null;

        /**
         * An Hedera key for managing token "pause".
         * <p>
         * This key, if set, SHALL have "pause" authority for this token and
         * MAY authorize transactions to pause or unpause this token.<br/>
         * If this key is set to an empty `KeyList`, this token
         * SHALL NOT be paused or unpaused.<br/>
         * If this key is removed while the token is paused, the token cannot
         * be unpaused and SHALL remain paused.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @member {proto.Key.$Properties|null|undefined} pauseKey
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.pauseKey = null;

        /**
         * Token "Metadata".
         * <p>
         * The value, if set, MUST NOT exceed 100 bytes.<br/>
         * <dl><dt>Examples</dt>
         * <dd>hcs://1/0.0.4896575</dd>
         * <dd>ipfs://bafkreifd7tcjjuwxxf4qkaibkj62pj4mhfuud7plwrc3pfoygt55al6syi</dd>
         * </dl>
         * @member {google.protobuf.BytesValue.$Properties|null|undefined} metadata
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.metadata = null;

        /**
         * An Hedera key for managing the token `metadata`.
         * <p>
         * This key, if set, MAY authorize transactions to modify the
         * `metadata` for this token.<br/>
         * If this key is set to an empty `KeyList`, the `metadata`
         * for this token SHALL NOT be modified.<br/>
         * If set, this key MUST be a valid key or an empty `KeyList`.<br/>
         * If set to a valid key, the previous key and new key MUST both
         * sign this transaction.
         * @member {proto.Key.$Properties|null|undefined} metadataKey
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.metadataKey = null;

        /**
         * Set a key validation mode.<br/>
         * Any key may be updated by a transaction signed by the token `admin_key`.
         * Each role key may _also_ sign a transaction to update that key.
         * If a role key signs an update to change that role key both old
         * and new key must sign the transaction, _unless_ this field is set
         * to `NO_VALIDATION`, in which case the _new_ key is not required to
         * sign the transaction (the existing key is still required).<br/>
         * The primary intent for this field is to allow a role key (e.g. a
         * `pause_key`) holder to "remove" that key from the token by signing
         * a transaction to set that role key to an empty `KeyList`.
         * <p>
         * If set to `FULL_VALIDATION`, either the `admin_key` or _both_ current
         * and new key MUST sign this transaction to update a "key" field for the
         * identified token.<br/>
         * If set to `NO_VALIDATION`, either the `admin_key` or the current
         * key MUST sign this transaction to update a "key" field for the
         * identified token.<br/>
         * This field SHALL be treated as `FULL_VALIDATION` if not set.
         * @member {proto.TokenKeyValidation} keyVerificationMode
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         */
        TokenUpdateTransactionBody.prototype.keyVerificationMode = 0;

        /**
         * Creates a new TokenUpdateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenUpdateTransactionBody
         * @static
         * @param {proto.TokenUpdateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenUpdateTransactionBody} TokenUpdateTransactionBody instance
         * @type {{
         *   (properties: proto.TokenUpdateTransactionBody.$Shape): proto.TokenUpdateTransactionBody & proto.TokenUpdateTransactionBody.$Shape;
         *   (properties?: proto.TokenUpdateTransactionBody.$Properties): proto.TokenUpdateTransactionBody;
         * }}
         */
        TokenUpdateTransactionBody.create = function(properties) {
            return new TokenUpdateTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenUpdateTransactionBody message. Does not implicitly {@link proto.TokenUpdateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenUpdateTransactionBody
         * @static
         * @param {proto.TokenUpdateTransactionBody.$Properties} message TokenUpdateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenUpdateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol") && message.symbol !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.symbol);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
            if (message.treasury != null && $Object.hasOwnProperty.call(message, "treasury"))
                $root.proto.AccountID.encode(message.treasury, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.kycKey != null && $Object.hasOwnProperty.call(message, "kycKey"))
                $root.proto.Key.encode(message.kycKey, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.freezeKey != null && $Object.hasOwnProperty.call(message, "freezeKey"))
                $root.proto.Key.encode(message.freezeKey, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.wipeKey != null && $Object.hasOwnProperty.call(message, "wipeKey"))
                $root.proto.Key.encode(message.wipeKey, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.supplyKey != null && $Object.hasOwnProperty.call(message, "supplyKey"))
                $root.proto.Key.encode(message.supplyKey, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount"))
                $root.proto.AccountID.encode(message.autoRenewAccount, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                $root.proto.Timestamp.encode(message.expiry, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                $root.google.protobuf.StringValue.encode(message.memo, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                $root.proto.Key.encode(message.feeScheduleKey, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.pauseKey != null && $Object.hasOwnProperty.call(message, "pauseKey"))
                $root.proto.Key.encode(message.pauseKey, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                $root.google.protobuf.BytesValue.encode(message.metadata, writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.metadataKey != null && $Object.hasOwnProperty.call(message, "metadataKey"))
                $root.proto.Key.encode(message.metadataKey, writer.uint32(/* id 17, wireType 2 =*/138).fork(), _depth + 1).ldelim();
            if (message.keyVerificationMode != null && $Object.hasOwnProperty.call(message, "keyVerificationMode") && message.keyVerificationMode !== 0)
                writer.uint32(/* id 18, wireType 0 =*/144).int32(message.keyVerificationMode);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenUpdateTransactionBody message, length delimited. Does not implicitly {@link proto.TokenUpdateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenUpdateTransactionBody
         * @static
         * @param {proto.TokenUpdateTransactionBody.$Properties} message TokenUpdateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenUpdateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenUpdateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenUpdateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenUpdateTransactionBody & proto.TokenUpdateTransactionBody.$Shape} TokenUpdateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenUpdateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenUpdateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.symbol = value;
                        else
                            delete message.symbol;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.treasury = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.treasury);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.kycKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.kycKey);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.freezeKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.freezeKey);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.wipeKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.wipeKey);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.supplyKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.supplyKey);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewAccount = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewAccount);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.expiry = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expiry);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.memo = $root.google.protobuf.StringValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.memo);
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.feeScheduleKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeScheduleKey);
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.pauseKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pauseKey);
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.metadata = $root.google.protobuf.BytesValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.metadata);
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        message.metadataKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.metadataKey);
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.keyVerificationMode = value;
                        else
                            delete message.keyVerificationMode;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenUpdateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenUpdateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenUpdateTransactionBody & proto.TokenUpdateTransactionBody.$Shape} TokenUpdateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenUpdateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenUpdateTransactionBody message.
         * @function verify
         * @memberof proto.TokenUpdateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenUpdateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                if (!$util.isString(message.symbol))
                    return "symbol: string expected";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.treasury != null && $Object.hasOwnProperty.call(message, "treasury")) {
                let error = $root.proto.AccountID.verify(message.treasury, _depth + 1);
                if (error)
                    return "treasury." + error;
            }
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.kycKey != null && $Object.hasOwnProperty.call(message, "kycKey")) {
                let error = $root.proto.Key.verify(message.kycKey, _depth + 1);
                if (error)
                    return "kycKey." + error;
            }
            if (message.freezeKey != null && $Object.hasOwnProperty.call(message, "freezeKey")) {
                let error = $root.proto.Key.verify(message.freezeKey, _depth + 1);
                if (error)
                    return "freezeKey." + error;
            }
            if (message.wipeKey != null && $Object.hasOwnProperty.call(message, "wipeKey")) {
                let error = $root.proto.Key.verify(message.wipeKey, _depth + 1);
                if (error)
                    return "wipeKey." + error;
            }
            if (message.supplyKey != null && $Object.hasOwnProperty.call(message, "supplyKey")) {
                let error = $root.proto.Key.verify(message.supplyKey, _depth + 1);
                if (error)
                    return "supplyKey." + error;
            }
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount")) {
                let error = $root.proto.AccountID.verify(message.autoRenewAccount, _depth + 1);
                if (error)
                    return "autoRenewAccount." + error;
            }
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                if (error)
                    return "autoRenewPeriod." + error;
            }
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry")) {
                let error = $root.proto.Timestamp.verify(message.expiry, _depth + 1);
                if (error)
                    return "expiry." + error;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo")) {
                let error = $root.google.protobuf.StringValue.verify(message.memo, _depth + 1);
                if (error)
                    return "memo." + error;
            }
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey")) {
                let error = $root.proto.Key.verify(message.feeScheduleKey, _depth + 1);
                if (error)
                    return "feeScheduleKey." + error;
            }
            if (message.pauseKey != null && $Object.hasOwnProperty.call(message, "pauseKey")) {
                let error = $root.proto.Key.verify(message.pauseKey, _depth + 1);
                if (error)
                    return "pauseKey." + error;
            }
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata")) {
                let error = $root.google.protobuf.BytesValue.verify(message.metadata, _depth + 1);
                if (error)
                    return "metadata." + error;
            }
            if (message.metadataKey != null && $Object.hasOwnProperty.call(message, "metadataKey")) {
                let error = $root.proto.Key.verify(message.metadataKey, _depth + 1);
                if (error)
                    return "metadataKey." + error;
            }
            if (message.keyVerificationMode != null && $Object.hasOwnProperty.call(message, "keyVerificationMode"))
                if (typeof message.keyVerificationMode !== "number" || (message.keyVerificationMode | 0) !== message.keyVerificationMode)
                    return "keyVerificationMode: enum value expected";
            return null;
        };

        /**
         * Creates a TokenUpdateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenUpdateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenUpdateTransactionBody} TokenUpdateTransactionBody
         */
        TokenUpdateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenUpdateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenUpdateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenUpdateTransactionBody();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            if (object.symbol != null)
                if (typeof object.symbol !== "string" || object.symbol.length)
                    message.symbol = $String(object.symbol);
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.treasury != null) {
                if (!$util.isObject(object.treasury))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.treasury: object expected");
                message.treasury = $root.proto.AccountID.fromObject(object.treasury, _depth + 1);
            }
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.kycKey != null) {
                if (!$util.isObject(object.kycKey))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.kycKey: object expected");
                message.kycKey = $root.proto.Key.fromObject(object.kycKey, _depth + 1);
            }
            if (object.freezeKey != null) {
                if (!$util.isObject(object.freezeKey))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.freezeKey: object expected");
                message.freezeKey = $root.proto.Key.fromObject(object.freezeKey, _depth + 1);
            }
            if (object.wipeKey != null) {
                if (!$util.isObject(object.wipeKey))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.wipeKey: object expected");
                message.wipeKey = $root.proto.Key.fromObject(object.wipeKey, _depth + 1);
            }
            if (object.supplyKey != null) {
                if (!$util.isObject(object.supplyKey))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.supplyKey: object expected");
                message.supplyKey = $root.proto.Key.fromObject(object.supplyKey, _depth + 1);
            }
            if (object.autoRenewAccount != null) {
                if (!$util.isObject(object.autoRenewAccount))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.autoRenewAccount: object expected");
                message.autoRenewAccount = $root.proto.AccountID.fromObject(object.autoRenewAccount, _depth + 1);
            }
            if (object.autoRenewPeriod != null) {
                if (!$util.isObject(object.autoRenewPeriod))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.autoRenewPeriod: object expected");
                message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
            }
            if (object.expiry != null) {
                if (!$util.isObject(object.expiry))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.expiry: object expected");
                message.expiry = $root.proto.Timestamp.fromObject(object.expiry, _depth + 1);
            }
            if (object.memo != null) {
                if (!$util.isObject(object.memo))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.memo: object expected");
                message.memo = $root.google.protobuf.StringValue.fromObject(object.memo, _depth + 1);
            }
            if (object.feeScheduleKey != null) {
                if (!$util.isObject(object.feeScheduleKey))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.feeScheduleKey: object expected");
                message.feeScheduleKey = $root.proto.Key.fromObject(object.feeScheduleKey, _depth + 1);
            }
            if (object.pauseKey != null) {
                if (!$util.isObject(object.pauseKey))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.pauseKey: object expected");
                message.pauseKey = $root.proto.Key.fromObject(object.pauseKey, _depth + 1);
            }
            if (object.metadata != null) {
                if (!$util.isObject(object.metadata))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.metadata: object expected");
                message.metadata = $root.google.protobuf.BytesValue.fromObject(object.metadata, _depth + 1);
            }
            if (object.metadataKey != null) {
                if (!$util.isObject(object.metadataKey))
                    throw $TypeError(".proto.TokenUpdateTransactionBody.metadataKey: object expected");
                message.metadataKey = $root.proto.Key.fromObject(object.metadataKey, _depth + 1);
            }
            if (object.keyVerificationMode !== 0 && (typeof object.keyVerificationMode !== "string" || $root.proto.TokenKeyValidation[object.keyVerificationMode] !== 0))
                switch (object.keyVerificationMode) {
                case "FULL_VALIDATION":
                case 0:
                    message.keyVerificationMode = 0;
                    break;
                case "NO_VALIDATION":
                case 1:
                    message.keyVerificationMode = 1;
                    break;
                default:
                    if (typeof object.keyVerificationMode === "number" && (object.keyVerificationMode | 0) === object.keyVerificationMode)
                        message.keyVerificationMode = object.keyVerificationMode;
                }
            return message;
        };

        /**
         * Creates a plain object from a TokenUpdateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenUpdateTransactionBody
         * @static
         * @param {proto.TokenUpdateTransactionBody} message TokenUpdateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenUpdateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.token = null;
                object.symbol = "";
                object.name = "";
                object.treasury = null;
                object.adminKey = null;
                object.kycKey = null;
                object.freezeKey = null;
                object.wipeKey = null;
                object.supplyKey = null;
                object.autoRenewAccount = null;
                object.autoRenewPeriod = null;
                object.expiry = null;
                object.memo = null;
                object.feeScheduleKey = null;
                object.pauseKey = null;
                object.metadata = null;
                object.metadataKey = null;
                object.keyVerificationMode = options.enums === $String ? "FULL_VALIDATION" : 0;
            }
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                object.symbol = message.symbol;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.treasury != null && $Object.hasOwnProperty.call(message, "treasury"))
                object.treasury = $root.proto.AccountID.toObject(message.treasury, options, _depth + 1);
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.kycKey != null && $Object.hasOwnProperty.call(message, "kycKey"))
                object.kycKey = $root.proto.Key.toObject(message.kycKey, options, _depth + 1);
            if (message.freezeKey != null && $Object.hasOwnProperty.call(message, "freezeKey"))
                object.freezeKey = $root.proto.Key.toObject(message.freezeKey, options, _depth + 1);
            if (message.wipeKey != null && $Object.hasOwnProperty.call(message, "wipeKey"))
                object.wipeKey = $root.proto.Key.toObject(message.wipeKey, options, _depth + 1);
            if (message.supplyKey != null && $Object.hasOwnProperty.call(message, "supplyKey"))
                object.supplyKey = $root.proto.Key.toObject(message.supplyKey, options, _depth + 1);
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount"))
                object.autoRenewAccount = $root.proto.AccountID.toObject(message.autoRenewAccount, options, _depth + 1);
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                object.expiry = $root.proto.Timestamp.toObject(message.expiry, options, _depth + 1);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = $root.google.protobuf.StringValue.toObject(message.memo, options, _depth + 1);
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                object.feeScheduleKey = $root.proto.Key.toObject(message.feeScheduleKey, options, _depth + 1);
            if (message.pauseKey != null && $Object.hasOwnProperty.call(message, "pauseKey"))
                object.pauseKey = $root.proto.Key.toObject(message.pauseKey, options, _depth + 1);
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                object.metadata = $root.google.protobuf.BytesValue.toObject(message.metadata, options, _depth + 1);
            if (message.metadataKey != null && $Object.hasOwnProperty.call(message, "metadataKey"))
                object.metadataKey = $root.proto.Key.toObject(message.metadataKey, options, _depth + 1);
            if (message.keyVerificationMode != null && $Object.hasOwnProperty.call(message, "keyVerificationMode"))
                object.keyVerificationMode = options.enums === $String ? $root.proto.TokenKeyValidation[message.keyVerificationMode] === $undefined ? message.keyVerificationMode : $root.proto.TokenKeyValidation[message.keyVerificationMode] : message.keyVerificationMode;
            return object;
        };

        /**
         * Converts this TokenUpdateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenUpdateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenUpdateTransactionBody.prototype.toJSON = function() {
            return TokenUpdateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenUpdateTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenUpdateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenUpdateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenUpdateTransactionBody";
        };

        return TokenUpdateTransactionBody;
    })();

    proto.TokenMintTransactionBody = (function() {

        /**
         * Properties of a TokenMintTransactionBody.
         * @typedef {Object} proto.TokenMintTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.
         * <p>
         * This SHALL identify the token type to "mint".<br/>
         * The identified token MUST exist, and MUST NOT be deleted.
         * @property {number|Long|null} [amount] An amount to mint to the Treasury Account.
         * <p>
         * This is interpreted as an amount in the smallest possible denomination
         * for the token (10<sup>-decimals</sup> whole tokens).<br/>
         * The balance for the token treasury account SHALL receive the newly
         * minted tokens.<br/>
         * If this value is equal to zero (`0`), the token SHOULD be a
         * non-fungible/unique type.<br/>
         * If this value is non-zero, the token MUST be a fungible/common type.
         * @property {Array.<Uint8Array>|null} [metadata] A list of metadata bytes.<br/>
         * <p>
         * One non-fungible/unique token SHALL be minted for each entry
         * in this list.<br/>
         * Each entry in this list MUST NOT be larger than the limit set by the
         * current network configuration value `tokens.maxMetadataBytes`.<br/>
         * This list MUST NOT contain more entries than the current limit set by
         * the network configuration value `tokens.nfts.maxBatchSizeMint`.<br/>
         * If this list is not empty, the token MUST be a
         * non-fungible/unique type.<br/>
         * If this list is empty, the token MUST be a fungible/common type.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenMintTransactionBody.
         * @memberof proto
         * @interface ITokenMintTransactionBody
         * @augments proto.TokenMintTransactionBody.$Properties
         * @deprecated Use proto.TokenMintTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenMintTransactionBody.
         * @typedef {proto.TokenMintTransactionBody.$Properties} proto.TokenMintTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenMintTransactionBody.
         * @memberof proto
         * @classdesc Mint tokens and deliver the new tokens to the token treasury account.
         * 
         * The token MUST have a `supply_key` set and that key MUST NOT
         * be an empty `KeyList`.<br/>
         * The token `supply_key` MUST sign this transaction.<br/>
         * This operation SHALL increase the total supply for the token type by
         * the number of tokens "minted".<br/>
         * The total supply for the token type MUST NOT be increased above the
         * maximum supply limit (2^63-1) by this transaction.<br/>
         * The tokens minted SHALL be credited to the token treasury account.<br/>
         * If the token is a fungible/common type, the amount MUST be specified.<br/>
         * If the token is a non-fungible/unique type, the metadata bytes for each
         * unique token MUST be specified in the `metadata` list.<br/>
         * Each unique metadata MUST not exceed the global metadata size limit defined
         * by the network configuration value `tokens.maxMetadataBytes`.<br/>
         * The global batch size limit (`tokens.nfts.maxBatchSizeMint`) SHALL set
         * the maximum number of individual NFT metadata permitted in a single
         * `tokenMint` transaction.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenMintTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenMintTransactionBody = function (properties) {
            this.metadata = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * This SHALL identify the token type to "mint".<br/>
         * The identified token MUST exist, and MUST NOT be deleted.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenMintTransactionBody
         * @instance
         */
        TokenMintTransactionBody.prototype.token = null;

        /**
         * An amount to mint to the Treasury Account.
         * <p>
         * This is interpreted as an amount in the smallest possible denomination
         * for the token (10<sup>-decimals</sup> whole tokens).<br/>
         * The balance for the token treasury account SHALL receive the newly
         * minted tokens.<br/>
         * If this value is equal to zero (`0`), the token SHOULD be a
         * non-fungible/unique type.<br/>
         * If this value is non-zero, the token MUST be a fungible/common type.
         * @member {number|Long} amount
         * @memberof proto.TokenMintTransactionBody
         * @instance
         */
        TokenMintTransactionBody.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A list of metadata bytes.<br/>
         * <p>
         * One non-fungible/unique token SHALL be minted for each entry
         * in this list.<br/>
         * Each entry in this list MUST NOT be larger than the limit set by the
         * current network configuration value `tokens.maxMetadataBytes`.<br/>
         * This list MUST NOT contain more entries than the current limit set by
         * the network configuration value `tokens.nfts.maxBatchSizeMint`.<br/>
         * If this list is not empty, the token MUST be a
         * non-fungible/unique type.<br/>
         * If this list is empty, the token MUST be a fungible/common type.
         * @member {Array.<Uint8Array>} metadata
         * @memberof proto.TokenMintTransactionBody
         * @instance
         */
        TokenMintTransactionBody.prototype.metadata = $util.emptyArray;

        /**
         * Creates a new TokenMintTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenMintTransactionBody
         * @static
         * @param {proto.TokenMintTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenMintTransactionBody} TokenMintTransactionBody instance
         * @type {{
         *   (properties: proto.TokenMintTransactionBody.$Shape): proto.TokenMintTransactionBody & proto.TokenMintTransactionBody.$Shape;
         *   (properties?: proto.TokenMintTransactionBody.$Properties): proto.TokenMintTransactionBody;
         * }}
         */
        TokenMintTransactionBody.create = function(properties) {
            return new TokenMintTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenMintTransactionBody message. Does not implicitly {@link proto.TokenMintTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenMintTransactionBody
         * @static
         * @param {proto.TokenMintTransactionBody.$Properties} message TokenMintTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenMintTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.amount);
            if (message.metadata != null && message.metadata.length)
                for (let i = 0; i < message.metadata.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.metadata[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenMintTransactionBody message, length delimited. Does not implicitly {@link proto.TokenMintTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenMintTransactionBody
         * @static
         * @param {proto.TokenMintTransactionBody.$Properties} message TokenMintTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenMintTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenMintTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenMintTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenMintTransactionBody & proto.TokenMintTransactionBody.$Shape} TokenMintTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenMintTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenMintTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.metadata && message.metadata.length))
                            message.metadata = [];
                        message.metadata.push(reader.bytes());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenMintTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenMintTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenMintTransactionBody & proto.TokenMintTransactionBody.$Shape} TokenMintTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenMintTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenMintTransactionBody message.
         * @function verify
         * @memberof proto.TokenMintTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenMintTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata")) {
                if (!$Array.isArray(message.metadata))
                    return "metadata: array expected";
                for (let i = 0; i < message.metadata.length; ++i)
                    if (!(message.metadata[i] && typeof message.metadata[i].length === "number" || $util.isString(message.metadata[i])))
                        return "metadata: buffer[] expected";
            }
            return null;
        };

        /**
         * Creates a TokenMintTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenMintTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenMintTransactionBody} TokenMintTransactionBody
         */
        TokenMintTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenMintTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenMintTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenMintTransactionBody();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenMintTransactionBody.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, true);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber(true);
            if (object.metadata) {
                if (!$Array.isArray(object.metadata))
                    throw $TypeError(".proto.TokenMintTransactionBody.metadata: array expected");
                message.metadata = $Array(object.metadata.length);
                for (let i = 0; i < object.metadata.length; ++i)
                    if (typeof object.metadata[i] === "string")
                        $util.base64.decode(object.metadata[i], message.metadata[i] = $util.newBuffer($util.base64.length(object.metadata[i])), 0);
                    else if (object.metadata[i].length >= 0)
                        message.metadata[i] = object.metadata[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenMintTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenMintTransactionBody
         * @static
         * @param {proto.TokenMintTransactionBody} message TokenMintTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenMintTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.metadata = [];
            if (options.defaults) {
                object.token = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, true).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber(true) : message.amount;
            if (message.metadata && message.metadata.length) {
                object.metadata = $Array(message.metadata.length);
                for (let j = 0; j < message.metadata.length; ++j)
                    object.metadata[j] = options.bytes === $String ? $util.base64.encode(message.metadata[j], 0, message.metadata[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.metadata[j]) : message.metadata[j];
            }
            return object;
        };

        /**
         * Converts this TokenMintTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenMintTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenMintTransactionBody.prototype.toJSON = function() {
            return TokenMintTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenMintTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenMintTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenMintTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenMintTransactionBody";
        };

        return TokenMintTransactionBody;
    })();

    proto.TokenBurnTransactionBody = (function() {

        /**
         * Properties of a TokenBurnTransactionBody.
         * @typedef {Object} proto.TokenBurnTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.
         * <p>
         * This SHALL identify the token type to "burn".<br/>
         * The identified token MUST exist, and MUST NOT be deleted.
         * @property {number|Long|null} [amount] An amount to burn from the Treasury Account.
         * <p>
         * This is interpreted as an amount in the smallest possible denomination
         * for the token (10<sup>-decimals</sup> whole tokens).<br/>
         * The balance for the token treasury account MUST contain sufficient
         * tokens to complete this transaction with a non-negative balance.<br/>
         * If this value is equal to zero (`0`), the token SHOULD be a
         * non-fungible/unique type.<br/>
         * If this value is non-zero, the token MUST be a fungible/common type.
         * @property {Array.<number|Long>|null} [serialNumbers] A list of serial numbers to burn from the Treasury Account.
         * <p>
         * This list MUST NOT contain more entries than the current limit set by
         * the network configuration value `tokens.nfts.maxBatchSizeBurn`.<br/>
         * The treasury account for the token MUST hold each unique token
         * identified in this list.<br/>
         * If this list is not empty, the token MUST be a
         * non-fungible/unique type.<br/>
         * If this list is empty, the token MUST be a fungible/common type.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenBurnTransactionBody.
         * @memberof proto
         * @interface ITokenBurnTransactionBody
         * @augments proto.TokenBurnTransactionBody.$Properties
         * @deprecated Use proto.TokenBurnTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenBurnTransactionBody.
         * @typedef {proto.TokenBurnTransactionBody.$Properties} proto.TokenBurnTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenBurnTransactionBody.
         * @memberof proto
         * @classdesc Burns tokens from the Token's treasury Account.
         * 
         * The token MUST have a `supply_key` set and that key MUST NOT
         * be an empty `KeyList`.<br/>
         * The token `supply_key` MUST sign this transaction.<br/>
         * This operation SHALL decrease the total supply for the token type by
         * the number of tokens "burned".<br/>
         * The total supply for the token type MUST NOT be reduced below zero (`0`)
         * by this transaction.<br/>
         * The tokens to burn SHALL be deducted from the token treasury account.<br/>
         * If the token is a fungible/common type, the amount MUST be specified.<br/>
         * If the token is a non-fungible/unique type, the specific serial numbers
         * MUST be specified.<br/>
         * The global batch size limit (`tokens.nfts.maxBatchSizeBurn`) SHALL set
         * the maximum number of individual NFT serial numbers permitted in a single
         * `tokenBurn` transaction.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenBurnTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenBurnTransactionBody = function (properties) {
            this.serialNumbers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * This SHALL identify the token type to "burn".<br/>
         * The identified token MUST exist, and MUST NOT be deleted.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenBurnTransactionBody
         * @instance
         */
        TokenBurnTransactionBody.prototype.token = null;

        /**
         * An amount to burn from the Treasury Account.
         * <p>
         * This is interpreted as an amount in the smallest possible denomination
         * for the token (10<sup>-decimals</sup> whole tokens).<br/>
         * The balance for the token treasury account MUST contain sufficient
         * tokens to complete this transaction with a non-negative balance.<br/>
         * If this value is equal to zero (`0`), the token SHOULD be a
         * non-fungible/unique type.<br/>
         * If this value is non-zero, the token MUST be a fungible/common type.
         * @member {number|Long} amount
         * @memberof proto.TokenBurnTransactionBody
         * @instance
         */
        TokenBurnTransactionBody.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A list of serial numbers to burn from the Treasury Account.
         * <p>
         * This list MUST NOT contain more entries than the current limit set by
         * the network configuration value `tokens.nfts.maxBatchSizeBurn`.<br/>
         * The treasury account for the token MUST hold each unique token
         * identified in this list.<br/>
         * If this list is not empty, the token MUST be a
         * non-fungible/unique type.<br/>
         * If this list is empty, the token MUST be a fungible/common type.
         * @member {Array.<number|Long>} serialNumbers
         * @memberof proto.TokenBurnTransactionBody
         * @instance
         */
        TokenBurnTransactionBody.prototype.serialNumbers = $util.emptyArray;

        /**
         * Creates a new TokenBurnTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenBurnTransactionBody
         * @static
         * @param {proto.TokenBurnTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenBurnTransactionBody} TokenBurnTransactionBody instance
         * @type {{
         *   (properties: proto.TokenBurnTransactionBody.$Shape): proto.TokenBurnTransactionBody & proto.TokenBurnTransactionBody.$Shape;
         *   (properties?: proto.TokenBurnTransactionBody.$Properties): proto.TokenBurnTransactionBody;
         * }}
         */
        TokenBurnTransactionBody.create = function(properties) {
            return new TokenBurnTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenBurnTransactionBody message. Does not implicitly {@link proto.TokenBurnTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenBurnTransactionBody
         * @static
         * @param {proto.TokenBurnTransactionBody.$Properties} message TokenBurnTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenBurnTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.amount);
            if (message.serialNumbers != null && message.serialNumbers.length)
                writer.uint32(/* id 3, wireType 2 =*/26).int64s(message.serialNumbers);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenBurnTransactionBody message, length delimited. Does not implicitly {@link proto.TokenBurnTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenBurnTransactionBody
         * @static
         * @param {proto.TokenBurnTransactionBody.$Properties} message TokenBurnTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenBurnTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenBurnTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenBurnTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenBurnTransactionBody & proto.TokenBurnTransactionBody.$Shape} TokenBurnTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenBurnTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenBurnTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.serialNumbers && message.serialNumbers.length))
                                message.serialNumbers = [];
                            reader.int64s(message.serialNumbers);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.serialNumbers && message.serialNumbers.length))
                            message.serialNumbers = [];
                        message.serialNumbers.push(reader.int64());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenBurnTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenBurnTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenBurnTransactionBody & proto.TokenBurnTransactionBody.$Shape} TokenBurnTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenBurnTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenBurnTransactionBody message.
         * @function verify
         * @memberof proto.TokenBurnTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenBurnTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.serialNumbers != null && $Object.hasOwnProperty.call(message, "serialNumbers")) {
                if (!$Array.isArray(message.serialNumbers))
                    return "serialNumbers: array expected";
                for (let i = 0; i < message.serialNumbers.length; ++i)
                    if (!$util.isInteger(message.serialNumbers[i]) && !(message.serialNumbers[i] && $util.isInteger(message.serialNumbers[i].low) && $util.isInteger(message.serialNumbers[i].high)))
                        return "serialNumbers: integer|Long[] expected";
            }
            return null;
        };

        /**
         * Creates a TokenBurnTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenBurnTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenBurnTransactionBody} TokenBurnTransactionBody
         */
        TokenBurnTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenBurnTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenBurnTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenBurnTransactionBody();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenBurnTransactionBody.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, true);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber(true);
            if (object.serialNumbers) {
                if (!$Array.isArray(object.serialNumbers))
                    throw $TypeError(".proto.TokenBurnTransactionBody.serialNumbers: array expected");
                message.serialNumbers = $Array(object.serialNumbers.length);
                for (let i = 0; i < object.serialNumbers.length; ++i)
                    if ($util.Long)
                        message.serialNumbers[i] = $util.Long.fromValue(object.serialNumbers[i], false);
                    else if (typeof object.serialNumbers[i] === "string")
                        message.serialNumbers[i] = $parseInt(object.serialNumbers[i], 10);
                    else if (typeof object.serialNumbers[i] === "number")
                        message.serialNumbers[i] = object.serialNumbers[i];
                    else if (typeof object.serialNumbers[i] === "object")
                        message.serialNumbers[i] = new $util.LongBits(object.serialNumbers[i].low >>> 0, object.serialNumbers[i].high >>> 0).toNumber();
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenBurnTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenBurnTransactionBody
         * @static
         * @param {proto.TokenBurnTransactionBody} message TokenBurnTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenBurnTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.serialNumbers = [];
            if (options.defaults) {
                object.token = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, true).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber(true) : message.amount;
            if (message.serialNumbers && message.serialNumbers.length) {
                object.serialNumbers = $Array(message.serialNumbers.length);
                for (let j = 0; j < message.serialNumbers.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.serialNumbers[j] = typeof message.serialNumbers[j] === "number" ? $BigInt(message.serialNumbers[j]) : $util.Long.fromBits(message.serialNumbers[j].low >>> 0, message.serialNumbers[j].high >>> 0, false).toBigInt();
                    else if (typeof message.serialNumbers[j] === "number")
                        object.serialNumbers[j] = options.longs === $String ? $String(message.serialNumbers[j]) : message.serialNumbers[j];
                    else
                        object.serialNumbers[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.serialNumbers[j]) : options.longs === $Number ? new $util.LongBits(message.serialNumbers[j].low >>> 0, message.serialNumbers[j].high >>> 0).toNumber() : message.serialNumbers[j];
            }
            return object;
        };

        /**
         * Converts this TokenBurnTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenBurnTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenBurnTransactionBody.prototype.toJSON = function() {
            return TokenBurnTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenBurnTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenBurnTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenBurnTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenBurnTransactionBody";
        };

        return TokenBurnTransactionBody;
    })();

    proto.TokenWipeAccountTransactionBody = (function() {

        /**
         * Properties of a TokenWipeAccountTransactionBody.
         * @typedef {Object} proto.TokenWipeAccountTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.
         * <p>
         * This field is REQUIRED.<br/>
         * The identified token MUST exist, MUST NOT be paused, MUST NOT be
         * deleted, and MUST NOT be expired.
         * @property {proto.AccountID.$Properties|null} [account] An account identifier.<br/>
         * This identifies the account from which tokens will be wiped.
         * <p>
         * This field is REQUIRED.<br/>
         * The identified account MUST NOT be deleted or expired.<br/>
         * If the identified token `kyc_key` is set to a valid key, the
         * identified account MUST have "KYC" granted.<br/>
         * The identified account MUST NOT be the `treasury` account for the
         * identified token.
         * @property {number|Long|null} [amount] An amount of fungible/common tokens to wipe.
         * <p>
         * If the identified token is a non-fungible/unique token type,
         * this value MUST be exactly zero(`0`).<br/>
         * If the identified token type is fungible/common:
         * <ul>
         * <li>This value SHALL be specified in units of the smallest
         * denomination possible for the identified token
         * (<tt>10<sup>-decimals</sup></tt> whole tokens).</li>
         * <li>This value MUST be strictly less than `Long.MAX_VALUE`.</li>
         * <li>This value MUST be less than or equal to the current total
         * supply for the identified token.</li>
         * <li>This value MUST be less than or equal to the current balance
         * held by the identified account.</li>
         * <li>This value MAY be zero(`0`).</li>
         * </ul>
         * @property {Array.<number|Long>|null} [serialNumbers] A list of serial numbers to wipe.<br/>
         * The non-fungible/unique tokens with these serial numbers will be
         * destroyed and cannot be recovered or reused.
         * <p>
         * If the identified token type is a fungible/common type, this
         * list MUST be empty.<br/>
         * If the identified token type is non-fungible/unique:
         * <ul>
         * <li>This list MUST contain at least one entry if the identified token
         * type is non-fungible/unique.>/li>
         * <li>This list MUST NOT contain more entries than the current total
         * supply for the identified token.</li>
         * <li>Every entry in this list MUST be a valid serial number for the
         * identified token (i.e. "collection").</li>
         * <li>Every entry in this list MUST be owned by the
         * identified account</li>
         * <li></li>
         * </ul>
         * This list MUST NOT contain more entries than the network configuration
         * value for batch size limit, typically ten(`10`).
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenWipeAccountTransactionBody.
         * @memberof proto
         * @interface ITokenWipeAccountTransactionBody
         * @augments proto.TokenWipeAccountTransactionBody.$Properties
         * @deprecated Use proto.TokenWipeAccountTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenWipeAccountTransactionBody.
         * @typedef {{
         *   token?: proto.TokenID.$Shape|null;
         *   account?: proto.AccountID.$Shape|null;
         *   amount?: number|Long|null;
         *   serialNumbers?: Array.<number|Long>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenWipeAccountTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenWipeAccountTransactionBody.
         * @memberof proto
         * @classdesc Wipe (administratively burn) tokens held by a non-treasury account.<br/>
         * On success, the requested tokens will be removed from the identified account
         * and the token supply will be reduced by the amount "wiped".
         * 
         * This transaction MUST be signed by the token `wipe_key`.<br/>
         * The identified token MUST exist, MUST NOT be deleted,
         * and MUST NOT be paused.<br/>
         * The identified token MUST have a valid `Key` set for the `wipe_key` field,
         * and that key MUST NOT be an empty `KeyList`.<br/>
         * The identified account MUST exist, MUST NOT be deleted, MUST be
         * associated to the identified token, MUST NOT be frozen for the identified
         * token, MUST NOT be the token `treasury`, and MUST hold a balance for the
         * token or the specific serial numbers provided.<br/>
         * This transaction SHOULD provide a value for `amount` or `serialNumbers`,
         * but MUST NOT set both fields.
         * 
         * ### Block Stream Effects
         * The new total supply for the wiped token type SHALL be recorded.
         * @constructor
         * @param {proto.TokenWipeAccountTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenWipeAccountTransactionBody = function (properties) {
            this.serialNumbers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * This field is REQUIRED.<br/>
         * The identified token MUST exist, MUST NOT be paused, MUST NOT be
         * deleted, and MUST NOT be expired.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenWipeAccountTransactionBody
         * @instance
         */
        TokenWipeAccountTransactionBody.prototype.token = null;

        /**
         * An account identifier.<br/>
         * This identifies the account from which tokens will be wiped.
         * <p>
         * This field is REQUIRED.<br/>
         * The identified account MUST NOT be deleted or expired.<br/>
         * If the identified token `kyc_key` is set to a valid key, the
         * identified account MUST have "KYC" granted.<br/>
         * The identified account MUST NOT be the `treasury` account for the
         * identified token.
         * @member {proto.AccountID.$Properties|null|undefined} account
         * @memberof proto.TokenWipeAccountTransactionBody
         * @instance
         */
        TokenWipeAccountTransactionBody.prototype.account = null;

        /**
         * An amount of fungible/common tokens to wipe.
         * <p>
         * If the identified token is a non-fungible/unique token type,
         * this value MUST be exactly zero(`0`).<br/>
         * If the identified token type is fungible/common:
         * <ul>
         * <li>This value SHALL be specified in units of the smallest
         * denomination possible for the identified token
         * (<tt>10<sup>-decimals</sup></tt> whole tokens).</li>
         * <li>This value MUST be strictly less than `Long.MAX_VALUE`.</li>
         * <li>This value MUST be less than or equal to the current total
         * supply for the identified token.</li>
         * <li>This value MUST be less than or equal to the current balance
         * held by the identified account.</li>
         * <li>This value MAY be zero(`0`).</li>
         * </ul>
         * @member {number|Long} amount
         * @memberof proto.TokenWipeAccountTransactionBody
         * @instance
         */
        TokenWipeAccountTransactionBody.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A list of serial numbers to wipe.<br/>
         * The non-fungible/unique tokens with these serial numbers will be
         * destroyed and cannot be recovered or reused.
         * <p>
         * If the identified token type is a fungible/common type, this
         * list MUST be empty.<br/>
         * If the identified token type is non-fungible/unique:
         * <ul>
         * <li>This list MUST contain at least one entry if the identified token
         * type is non-fungible/unique.>/li>
         * <li>This list MUST NOT contain more entries than the current total
         * supply for the identified token.</li>
         * <li>Every entry in this list MUST be a valid serial number for the
         * identified token (i.e. "collection").</li>
         * <li>Every entry in this list MUST be owned by the
         * identified account</li>
         * <li></li>
         * </ul>
         * This list MUST NOT contain more entries than the network configuration
         * value for batch size limit, typically ten(`10`).
         * @member {Array.<number|Long>} serialNumbers
         * @memberof proto.TokenWipeAccountTransactionBody
         * @instance
         */
        TokenWipeAccountTransactionBody.prototype.serialNumbers = $util.emptyArray;

        /**
         * Creates a new TokenWipeAccountTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenWipeAccountTransactionBody
         * @static
         * @param {proto.TokenWipeAccountTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenWipeAccountTransactionBody} TokenWipeAccountTransactionBody instance
         * @type {{
         *   (properties: proto.TokenWipeAccountTransactionBody.$Shape): proto.TokenWipeAccountTransactionBody & proto.TokenWipeAccountTransactionBody.$Shape;
         *   (properties?: proto.TokenWipeAccountTransactionBody.$Properties): proto.TokenWipeAccountTransactionBody;
         * }}
         */
        TokenWipeAccountTransactionBody.create = function(properties) {
            return new TokenWipeAccountTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenWipeAccountTransactionBody message. Does not implicitly {@link proto.TokenWipeAccountTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenWipeAccountTransactionBody
         * @static
         * @param {proto.TokenWipeAccountTransactionBody.$Properties} message TokenWipeAccountTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenWipeAccountTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                $root.proto.AccountID.encode(message.account, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.amount);
            if (message.serialNumbers != null && message.serialNumbers.length)
                writer.uint32(/* id 4, wireType 2 =*/34).int64s(message.serialNumbers);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenWipeAccountTransactionBody message, length delimited. Does not implicitly {@link proto.TokenWipeAccountTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenWipeAccountTransactionBody
         * @static
         * @param {proto.TokenWipeAccountTransactionBody.$Properties} message TokenWipeAccountTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenWipeAccountTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenWipeAccountTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenWipeAccountTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenWipeAccountTransactionBody & proto.TokenWipeAccountTransactionBody.$Shape} TokenWipeAccountTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenWipeAccountTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenWipeAccountTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.account = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.account);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.serialNumbers && message.serialNumbers.length))
                                message.serialNumbers = [];
                            reader.int64s(message.serialNumbers);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.serialNumbers && message.serialNumbers.length))
                            message.serialNumbers = [];
                        message.serialNumbers.push(reader.int64());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenWipeAccountTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenWipeAccountTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenWipeAccountTransactionBody & proto.TokenWipeAccountTransactionBody.$Shape} TokenWipeAccountTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenWipeAccountTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenWipeAccountTransactionBody message.
         * @function verify
         * @memberof proto.TokenWipeAccountTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenWipeAccountTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            if (message.account != null && $Object.hasOwnProperty.call(message, "account")) {
                let error = $root.proto.AccountID.verify(message.account, _depth + 1);
                if (error)
                    return "account." + error;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.serialNumbers != null && $Object.hasOwnProperty.call(message, "serialNumbers")) {
                if (!$Array.isArray(message.serialNumbers))
                    return "serialNumbers: array expected";
                for (let i = 0; i < message.serialNumbers.length; ++i)
                    if (!$util.isInteger(message.serialNumbers[i]) && !(message.serialNumbers[i] && $util.isInteger(message.serialNumbers[i].low) && $util.isInteger(message.serialNumbers[i].high)))
                        return "serialNumbers: integer|Long[] expected";
            }
            return null;
        };

        /**
         * Creates a TokenWipeAccountTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenWipeAccountTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenWipeAccountTransactionBody} TokenWipeAccountTransactionBody
         */
        TokenWipeAccountTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenWipeAccountTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenWipeAccountTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenWipeAccountTransactionBody();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenWipeAccountTransactionBody.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            if (object.account != null) {
                if (!$util.isObject(object.account))
                    throw $TypeError(".proto.TokenWipeAccountTransactionBody.account: object expected");
                message.account = $root.proto.AccountID.fromObject(object.account, _depth + 1);
            }
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, true);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber(true);
            if (object.serialNumbers) {
                if (!$Array.isArray(object.serialNumbers))
                    throw $TypeError(".proto.TokenWipeAccountTransactionBody.serialNumbers: array expected");
                message.serialNumbers = $Array(object.serialNumbers.length);
                for (let i = 0; i < object.serialNumbers.length; ++i)
                    if ($util.Long)
                        message.serialNumbers[i] = $util.Long.fromValue(object.serialNumbers[i], false);
                    else if (typeof object.serialNumbers[i] === "string")
                        message.serialNumbers[i] = $parseInt(object.serialNumbers[i], 10);
                    else if (typeof object.serialNumbers[i] === "number")
                        message.serialNumbers[i] = object.serialNumbers[i];
                    else if (typeof object.serialNumbers[i] === "object")
                        message.serialNumbers[i] = new $util.LongBits(object.serialNumbers[i].low >>> 0, object.serialNumbers[i].high >>> 0).toNumber();
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenWipeAccountTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenWipeAccountTransactionBody
         * @static
         * @param {proto.TokenWipeAccountTransactionBody} message TokenWipeAccountTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenWipeAccountTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.serialNumbers = [];
            if (options.defaults) {
                object.token = null;
                object.account = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = $root.proto.AccountID.toObject(message.account, options, _depth + 1);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, true).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber(true) : message.amount;
            if (message.serialNumbers && message.serialNumbers.length) {
                object.serialNumbers = $Array(message.serialNumbers.length);
                for (let j = 0; j < message.serialNumbers.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.serialNumbers[j] = typeof message.serialNumbers[j] === "number" ? $BigInt(message.serialNumbers[j]) : $util.Long.fromBits(message.serialNumbers[j].low >>> 0, message.serialNumbers[j].high >>> 0, false).toBigInt();
                    else if (typeof message.serialNumbers[j] === "number")
                        object.serialNumbers[j] = options.longs === $String ? $String(message.serialNumbers[j]) : message.serialNumbers[j];
                    else
                        object.serialNumbers[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.serialNumbers[j]) : options.longs === $Number ? new $util.LongBits(message.serialNumbers[j].low >>> 0, message.serialNumbers[j].high >>> 0).toNumber() : message.serialNumbers[j];
            }
            return object;
        };

        /**
         * Converts this TokenWipeAccountTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenWipeAccountTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenWipeAccountTransactionBody.prototype.toJSON = function() {
            return TokenWipeAccountTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenWipeAccountTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenWipeAccountTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenWipeAccountTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenWipeAccountTransactionBody";
        };

        return TokenWipeAccountTransactionBody;
    })();

    proto.TokenAssociateTransactionBody = (function() {

        /**
         * Properties of a TokenAssociateTransactionBody.
         * @typedef {Object} proto.TokenAssociateTransactionBody.$Properties
         * @property {proto.AccountID.$Properties|null} [account] An account identifier.
         * <p>
         * The identified account SHALL be associated to each of the
         * tokens identified in the `tokens` field.<br/>
         * This field is REQUIRED and MUST be a valid account identifier.<br/>
         * The identified account MUST exist in state.<br/>
         * The identified account MUST NOT be deleted.<br/>
         * The identified account MUST NOT be expired.
         * @property {Array.<proto.TokenID.$Properties>|null} [tokens] A list of token identifiers.
         * <p>
         * Each token identified in this list SHALL be separately associated with
         * the account identified in the `account` field.<br/>
         * This list MUST NOT be empty.
         * Each entry in this list MUST be a valid token identifier.<br/>
         * Each entry in this list MUST NOT be currently associated to the
         * account identified in `account`.<br/>
         * Each entry in this list MUST NOT be expired.<br/>
         * Each entry in this list MUST NOT be deleted.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenAssociateTransactionBody.
         * @memberof proto
         * @interface ITokenAssociateTransactionBody
         * @augments proto.TokenAssociateTransactionBody.$Properties
         * @deprecated Use proto.TokenAssociateTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenAssociateTransactionBody.
         * @typedef {{
         *   account?: proto.AccountID.$Shape|null;
         *   tokens?: Array.<proto.TokenID.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenAssociateTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenAssociateTransactionBody.
         * @memberof proto
         * @classdesc Associate an Hedera Token Service (HTS) token and an account.
         * 
         * An association MUST exist between an account and a token before that
         * account may transfer or receive that token.<br/>
         * If the identified account is not found,
         * the transaction SHALL return `INVALID_ACCOUNT_ID`.<br/>
         * If the identified account has been deleted,
         * the transaction SHALL return `ACCOUNT_DELETED`.<br/>
         * If any of the identified tokens is not found,
         * the transaction SHALL return `INVALID_TOKEN_REF`.<br/>
         * If any of the identified tokens has been deleted,
         * the transaction SHALL return `TOKEN_WAS_DELETED`.<br/>
         * If an association already exists for any of the identified tokens,
         * the transaction SHALL return `TOKEN_ALREADY_ASSOCIATED_TO_ACCOUNT`.<br/>
         * The identified account MUST sign this transaction.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenAssociateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenAssociateTransactionBody = function (properties) {
            this.tokens = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An account identifier.
         * <p>
         * The identified account SHALL be associated to each of the
         * tokens identified in the `tokens` field.<br/>
         * This field is REQUIRED and MUST be a valid account identifier.<br/>
         * The identified account MUST exist in state.<br/>
         * The identified account MUST NOT be deleted.<br/>
         * The identified account MUST NOT be expired.
         * @member {proto.AccountID.$Properties|null|undefined} account
         * @memberof proto.TokenAssociateTransactionBody
         * @instance
         */
        TokenAssociateTransactionBody.prototype.account = null;

        /**
         * A list of token identifiers.
         * <p>
         * Each token identified in this list SHALL be separately associated with
         * the account identified in the `account` field.<br/>
         * This list MUST NOT be empty.
         * Each entry in this list MUST be a valid token identifier.<br/>
         * Each entry in this list MUST NOT be currently associated to the
         * account identified in `account`.<br/>
         * Each entry in this list MUST NOT be expired.<br/>
         * Each entry in this list MUST NOT be deleted.
         * @member {Array.<proto.TokenID.$Properties>} tokens
         * @memberof proto.TokenAssociateTransactionBody
         * @instance
         */
        TokenAssociateTransactionBody.prototype.tokens = $util.emptyArray;

        /**
         * Creates a new TokenAssociateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenAssociateTransactionBody
         * @static
         * @param {proto.TokenAssociateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenAssociateTransactionBody} TokenAssociateTransactionBody instance
         * @type {{
         *   (properties: proto.TokenAssociateTransactionBody.$Shape): proto.TokenAssociateTransactionBody & proto.TokenAssociateTransactionBody.$Shape;
         *   (properties?: proto.TokenAssociateTransactionBody.$Properties): proto.TokenAssociateTransactionBody;
         * }}
         */
        TokenAssociateTransactionBody.create = function(properties) {
            return new TokenAssociateTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenAssociateTransactionBody message. Does not implicitly {@link proto.TokenAssociateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenAssociateTransactionBody
         * @static
         * @param {proto.TokenAssociateTransactionBody.$Properties} message TokenAssociateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenAssociateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                $root.proto.AccountID.encode(message.account, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.tokens != null && message.tokens.length)
                for (let i = 0; i < message.tokens.length; ++i)
                    $root.proto.TokenID.encode(message.tokens[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenAssociateTransactionBody message, length delimited. Does not implicitly {@link proto.TokenAssociateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenAssociateTransactionBody
         * @static
         * @param {proto.TokenAssociateTransactionBody.$Properties} message TokenAssociateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenAssociateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenAssociateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenAssociateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenAssociateTransactionBody & proto.TokenAssociateTransactionBody.$Shape} TokenAssociateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenAssociateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenAssociateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.account = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.account);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tokens && message.tokens.length))
                            message.tokens = [];
                        message.tokens.push($root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenAssociateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenAssociateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenAssociateTransactionBody & proto.TokenAssociateTransactionBody.$Shape} TokenAssociateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenAssociateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenAssociateTransactionBody message.
         * @function verify
         * @memberof proto.TokenAssociateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenAssociateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account != null && $Object.hasOwnProperty.call(message, "account")) {
                let error = $root.proto.AccountID.verify(message.account, _depth + 1);
                if (error)
                    return "account." + error;
            }
            if (message.tokens != null && $Object.hasOwnProperty.call(message, "tokens")) {
                if (!$Array.isArray(message.tokens))
                    return "tokens: array expected";
                for (let i = 0; i < message.tokens.length; ++i) {
                    let error = $root.proto.TokenID.verify(message.tokens[i], _depth + 1);
                    if (error)
                        return "tokens." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TokenAssociateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenAssociateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenAssociateTransactionBody} TokenAssociateTransactionBody
         */
        TokenAssociateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenAssociateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenAssociateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenAssociateTransactionBody();
            if (object.account != null) {
                if (!$util.isObject(object.account))
                    throw $TypeError(".proto.TokenAssociateTransactionBody.account: object expected");
                message.account = $root.proto.AccountID.fromObject(object.account, _depth + 1);
            }
            if (object.tokens) {
                if (!$Array.isArray(object.tokens))
                    throw $TypeError(".proto.TokenAssociateTransactionBody.tokens: array expected");
                message.tokens = $Array(object.tokens.length);
                for (let i = 0; i < object.tokens.length; ++i) {
                    if (!$util.isObject(object.tokens[i]))
                        throw $TypeError(".proto.TokenAssociateTransactionBody.tokens: object expected");
                    message.tokens[i] = $root.proto.TokenID.fromObject(object.tokens[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenAssociateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenAssociateTransactionBody
         * @static
         * @param {proto.TokenAssociateTransactionBody} message TokenAssociateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenAssociateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tokens = [];
            if (options.defaults)
                object.account = null;
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = $root.proto.AccountID.toObject(message.account, options, _depth + 1);
            if (message.tokens && message.tokens.length) {
                object.tokens = $Array(message.tokens.length);
                for (let j = 0; j < message.tokens.length; ++j)
                    object.tokens[j] = $root.proto.TokenID.toObject(message.tokens[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TokenAssociateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenAssociateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenAssociateTransactionBody.prototype.toJSON = function() {
            return TokenAssociateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenAssociateTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenAssociateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenAssociateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenAssociateTransactionBody";
        };

        return TokenAssociateTransactionBody;
    })();

    proto.TokenDissociateTransactionBody = (function() {

        /**
         * Properties of a TokenDissociateTransactionBody.
         * @typedef {Object} proto.TokenDissociateTransactionBody.$Properties
         * @property {proto.AccountID.$Properties|null} [account] An account identifier.
         * <p>
         * The identified account SHALL be dissociated from each of the
         * tokens identified in the `tokens` field.
         * This field is REQUIRED and MUST be a valid account identifier.<br/>
         * The identified account MUST exist in state.<br/>
         * The identified account MUST NOT be deleted.<br/>
         * The identified account MUST NOT be expired.
         * @property {Array.<proto.TokenID.$Properties>|null} [tokens] A list of token identifiers.
         * <p>
         * Each token identified in this list SHALL be dissociated from
         * the account identified in the `account` field.<br/>
         * This list MUST NOT be empty.
         * Each entry in this list MUST be a valid token identifier.<br/>
         * Each entry in this list MUST be currently associated to the
         * account identified in `account`.<br/>
         * Entries in this list MAY be expired, if the token type is
         * fungible/common.<br/>
         * Each entry in this list MUST NOT be deleted.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenDissociateTransactionBody.
         * @memberof proto
         * @interface ITokenDissociateTransactionBody
         * @augments proto.TokenDissociateTransactionBody.$Properties
         * @deprecated Use proto.TokenDissociateTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenDissociateTransactionBody.
         * @typedef {{
         *   account?: proto.AccountID.$Shape|null;
         *   tokens?: Array.<proto.TokenID.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenDissociateTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenDissociateTransactionBody.
         * @memberof proto
         * @classdesc Dissociate an account from one or more HTS tokens.
         * 
         * If the identified account is not found,
         * the transaction SHALL return `INVALID_ACCOUNT_ID`.<br/>
         * If the identified account has been deleted,
         * the transaction SHALL return `ACCOUNT_DELETED`.<br/>
         * If any of the identified tokens is not found,
         * the transaction SHALL return `INVALID_TOKEN_REF`.<br/>
         * If any of the identified tokens has been deleted,
         * the transaction SHALL return `TOKEN_WAS_DELETED`.<br/>
         * If an association does not exist for any of the identified tokens,
         * the transaction SHALL return `TOKEN_NOT_ASSOCIATED_TO_ACCOUNT`.<br/>
         * If the identified account has a nonzero balance for any of the identified
         * tokens, and that token is neither deleted nor expired, the
         * transaction SHALL return `TRANSACTION_REQUIRES_ZERO_TOKEN_BALANCES`.<br/>
         * If one of the identified tokens is a fungible/common token that is expired,
         * the account MAY disassociate from that token, even if that token balance is
         * not zero for that account.<br/>
         * If one of the identified tokens is a non-fungible/unique token that is
         * expired, the account MUST NOT disassociate if that account holds any
         * individual NFT of that token. In this situation the transaction SHALL
         * return `TRANSACTION_REQUIRED_ZERO_TOKEN_BALANCES`.<br/>
         * The identified account MUST sign this transaction.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenDissociateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenDissociateTransactionBody = function (properties) {
            this.tokens = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An account identifier.
         * <p>
         * The identified account SHALL be dissociated from each of the
         * tokens identified in the `tokens` field.
         * This field is REQUIRED and MUST be a valid account identifier.<br/>
         * The identified account MUST exist in state.<br/>
         * The identified account MUST NOT be deleted.<br/>
         * The identified account MUST NOT be expired.
         * @member {proto.AccountID.$Properties|null|undefined} account
         * @memberof proto.TokenDissociateTransactionBody
         * @instance
         */
        TokenDissociateTransactionBody.prototype.account = null;

        /**
         * A list of token identifiers.
         * <p>
         * Each token identified in this list SHALL be dissociated from
         * the account identified in the `account` field.<br/>
         * This list MUST NOT be empty.
         * Each entry in this list MUST be a valid token identifier.<br/>
         * Each entry in this list MUST be currently associated to the
         * account identified in `account`.<br/>
         * Entries in this list MAY be expired, if the token type is
         * fungible/common.<br/>
         * Each entry in this list MUST NOT be deleted.
         * @member {Array.<proto.TokenID.$Properties>} tokens
         * @memberof proto.TokenDissociateTransactionBody
         * @instance
         */
        TokenDissociateTransactionBody.prototype.tokens = $util.emptyArray;

        /**
         * Creates a new TokenDissociateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenDissociateTransactionBody
         * @static
         * @param {proto.TokenDissociateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenDissociateTransactionBody} TokenDissociateTransactionBody instance
         * @type {{
         *   (properties: proto.TokenDissociateTransactionBody.$Shape): proto.TokenDissociateTransactionBody & proto.TokenDissociateTransactionBody.$Shape;
         *   (properties?: proto.TokenDissociateTransactionBody.$Properties): proto.TokenDissociateTransactionBody;
         * }}
         */
        TokenDissociateTransactionBody.create = function(properties) {
            return new TokenDissociateTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenDissociateTransactionBody message. Does not implicitly {@link proto.TokenDissociateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenDissociateTransactionBody
         * @static
         * @param {proto.TokenDissociateTransactionBody.$Properties} message TokenDissociateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenDissociateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                $root.proto.AccountID.encode(message.account, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.tokens != null && message.tokens.length)
                for (let i = 0; i < message.tokens.length; ++i)
                    $root.proto.TokenID.encode(message.tokens[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenDissociateTransactionBody message, length delimited. Does not implicitly {@link proto.TokenDissociateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenDissociateTransactionBody
         * @static
         * @param {proto.TokenDissociateTransactionBody.$Properties} message TokenDissociateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenDissociateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenDissociateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenDissociateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenDissociateTransactionBody & proto.TokenDissociateTransactionBody.$Shape} TokenDissociateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenDissociateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenDissociateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.account = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.account);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tokens && message.tokens.length))
                            message.tokens = [];
                        message.tokens.push($root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenDissociateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenDissociateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenDissociateTransactionBody & proto.TokenDissociateTransactionBody.$Shape} TokenDissociateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenDissociateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenDissociateTransactionBody message.
         * @function verify
         * @memberof proto.TokenDissociateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenDissociateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account != null && $Object.hasOwnProperty.call(message, "account")) {
                let error = $root.proto.AccountID.verify(message.account, _depth + 1);
                if (error)
                    return "account." + error;
            }
            if (message.tokens != null && $Object.hasOwnProperty.call(message, "tokens")) {
                if (!$Array.isArray(message.tokens))
                    return "tokens: array expected";
                for (let i = 0; i < message.tokens.length; ++i) {
                    let error = $root.proto.TokenID.verify(message.tokens[i], _depth + 1);
                    if (error)
                        return "tokens." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TokenDissociateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenDissociateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenDissociateTransactionBody} TokenDissociateTransactionBody
         */
        TokenDissociateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenDissociateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenDissociateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenDissociateTransactionBody();
            if (object.account != null) {
                if (!$util.isObject(object.account))
                    throw $TypeError(".proto.TokenDissociateTransactionBody.account: object expected");
                message.account = $root.proto.AccountID.fromObject(object.account, _depth + 1);
            }
            if (object.tokens) {
                if (!$Array.isArray(object.tokens))
                    throw $TypeError(".proto.TokenDissociateTransactionBody.tokens: array expected");
                message.tokens = $Array(object.tokens.length);
                for (let i = 0; i < object.tokens.length; ++i) {
                    if (!$util.isObject(object.tokens[i]))
                        throw $TypeError(".proto.TokenDissociateTransactionBody.tokens: object expected");
                    message.tokens[i] = $root.proto.TokenID.fromObject(object.tokens[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenDissociateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenDissociateTransactionBody
         * @static
         * @param {proto.TokenDissociateTransactionBody} message TokenDissociateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenDissociateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tokens = [];
            if (options.defaults)
                object.account = null;
            if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
                object.account = $root.proto.AccountID.toObject(message.account, options, _depth + 1);
            if (message.tokens && message.tokens.length) {
                object.tokens = $Array(message.tokens.length);
                for (let j = 0; j < message.tokens.length; ++j)
                    object.tokens[j] = $root.proto.TokenID.toObject(message.tokens[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TokenDissociateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenDissociateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenDissociateTransactionBody.prototype.toJSON = function() {
            return TokenDissociateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenDissociateTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenDissociateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenDissociateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenDissociateTransactionBody";
        };

        return TokenDissociateTransactionBody;
    })();

    proto.TokenFeeScheduleUpdateTransactionBody = (function() {

        /**
         * Properties of a TokenFeeScheduleUpdateTransactionBody.
         * @typedef {Object} proto.TokenFeeScheduleUpdateTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] A token identifier.
         * <p>
         * This SHALL identify the token type to modify with an updated
         * custom fee schedule.<br/>
         * The identified token MUST exist, and MUST NOT be deleted.
         * @property {Array.<proto.CustomFee.$Properties>|null} [customFees] A list of custom fees representing a fee schedule.
         * <p>
         * This list MAY be empty to remove custom fees from a token.<br/>
         * If the identified token is a non-fungible/unique type, the entries
         * in this list MUST NOT declare a `fractional_fee`.<br/>
         * If the identified token is a fungible/common type, the entries in this
         * list MUST NOT declare a `royalty_fee`.<br/>
         * Any token type MAY include entries that declare a `fixed_fee`.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenFeeScheduleUpdateTransactionBody.
         * @memberof proto
         * @interface ITokenFeeScheduleUpdateTransactionBody
         * @augments proto.TokenFeeScheduleUpdateTransactionBody.$Properties
         * @deprecated Use proto.TokenFeeScheduleUpdateTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenFeeScheduleUpdateTransactionBody.
         * @typedef {{
         *   tokenId?: proto.TokenID.$Shape|null;
         *   customFees?: Array.<proto.CustomFee.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenFeeScheduleUpdateTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenFeeScheduleUpdateTransactionBody.
         * @memberof proto
         * @classdesc Update the custom fee schedule for a token type.
         * 
         * The token MUST have a `fee_schedule_key` set and that key MUST NOT
         * be an empty `KeyList`.<br/>
         * The token `fee_schedule_key` MUST sign this transaction.<br/>
         * The token MUST exist, MUST NOT be deleted, and MUST NOT be expired.<br/>
         * 
         * If the custom_fees list is empty, clears the fee schedule or resolves to
         * CUSTOM_SCHEDULE_ALREADY_HAS_NO_FEES if the fee schedule was already empty.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenFeeScheduleUpdateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenFeeScheduleUpdateTransactionBody = function (properties) {
            this.customFees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * This SHALL identify the token type to modify with an updated
         * custom fee schedule.<br/>
         * The identified token MUST exist, and MUST NOT be deleted.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.TokenFeeScheduleUpdateTransactionBody
         * @instance
         */
        TokenFeeScheduleUpdateTransactionBody.prototype.tokenId = null;

        /**
         * A list of custom fees representing a fee schedule.
         * <p>
         * This list MAY be empty to remove custom fees from a token.<br/>
         * If the identified token is a non-fungible/unique type, the entries
         * in this list MUST NOT declare a `fractional_fee`.<br/>
         * If the identified token is a fungible/common type, the entries in this
         * list MUST NOT declare a `royalty_fee`.<br/>
         * Any token type MAY include entries that declare a `fixed_fee`.
         * @member {Array.<proto.CustomFee.$Properties>} customFees
         * @memberof proto.TokenFeeScheduleUpdateTransactionBody
         * @instance
         */
        TokenFeeScheduleUpdateTransactionBody.prototype.customFees = $util.emptyArray;

        /**
         * Creates a new TokenFeeScheduleUpdateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenFeeScheduleUpdateTransactionBody
         * @static
         * @param {proto.TokenFeeScheduleUpdateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenFeeScheduleUpdateTransactionBody} TokenFeeScheduleUpdateTransactionBody instance
         * @type {{
         *   (properties: proto.TokenFeeScheduleUpdateTransactionBody.$Shape): proto.TokenFeeScheduleUpdateTransactionBody & proto.TokenFeeScheduleUpdateTransactionBody.$Shape;
         *   (properties?: proto.TokenFeeScheduleUpdateTransactionBody.$Properties): proto.TokenFeeScheduleUpdateTransactionBody;
         * }}
         */
        TokenFeeScheduleUpdateTransactionBody.create = function(properties) {
            return new TokenFeeScheduleUpdateTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenFeeScheduleUpdateTransactionBody message. Does not implicitly {@link proto.TokenFeeScheduleUpdateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenFeeScheduleUpdateTransactionBody
         * @static
         * @param {proto.TokenFeeScheduleUpdateTransactionBody.$Properties} message TokenFeeScheduleUpdateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenFeeScheduleUpdateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.customFees != null && message.customFees.length)
                for (let i = 0; i < message.customFees.length; ++i)
                    $root.proto.CustomFee.encode(message.customFees[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenFeeScheduleUpdateTransactionBody message, length delimited. Does not implicitly {@link proto.TokenFeeScheduleUpdateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenFeeScheduleUpdateTransactionBody
         * @static
         * @param {proto.TokenFeeScheduleUpdateTransactionBody.$Properties} message TokenFeeScheduleUpdateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenFeeScheduleUpdateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenFeeScheduleUpdateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenFeeScheduleUpdateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenFeeScheduleUpdateTransactionBody & proto.TokenFeeScheduleUpdateTransactionBody.$Shape} TokenFeeScheduleUpdateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenFeeScheduleUpdateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenFeeScheduleUpdateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.customFees && message.customFees.length))
                            message.customFees = [];
                        message.customFees.push($root.proto.CustomFee.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenFeeScheduleUpdateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenFeeScheduleUpdateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenFeeScheduleUpdateTransactionBody & proto.TokenFeeScheduleUpdateTransactionBody.$Shape} TokenFeeScheduleUpdateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenFeeScheduleUpdateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenFeeScheduleUpdateTransactionBody message.
         * @function verify
         * @memberof proto.TokenFeeScheduleUpdateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenFeeScheduleUpdateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.customFees != null && $Object.hasOwnProperty.call(message, "customFees")) {
                if (!$Array.isArray(message.customFees))
                    return "customFees: array expected";
                for (let i = 0; i < message.customFees.length; ++i) {
                    let error = $root.proto.CustomFee.verify(message.customFees[i], _depth + 1);
                    if (error)
                        return "customFees." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TokenFeeScheduleUpdateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenFeeScheduleUpdateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenFeeScheduleUpdateTransactionBody} TokenFeeScheduleUpdateTransactionBody
         */
        TokenFeeScheduleUpdateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenFeeScheduleUpdateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenFeeScheduleUpdateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenFeeScheduleUpdateTransactionBody();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.TokenFeeScheduleUpdateTransactionBody.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.customFees) {
                if (!$Array.isArray(object.customFees))
                    throw $TypeError(".proto.TokenFeeScheduleUpdateTransactionBody.customFees: array expected");
                message.customFees = $Array(object.customFees.length);
                for (let i = 0; i < object.customFees.length; ++i) {
                    if (!$util.isObject(object.customFees[i]))
                        throw $TypeError(".proto.TokenFeeScheduleUpdateTransactionBody.customFees: object expected");
                    message.customFees[i] = $root.proto.CustomFee.fromObject(object.customFees[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenFeeScheduleUpdateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenFeeScheduleUpdateTransactionBody
         * @static
         * @param {proto.TokenFeeScheduleUpdateTransactionBody} message TokenFeeScheduleUpdateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenFeeScheduleUpdateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.customFees = [];
            if (options.defaults)
                object.tokenId = null;
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.customFees && message.customFees.length) {
                object.customFees = $Array(message.customFees.length);
                for (let j = 0; j < message.customFees.length; ++j)
                    object.customFees[j] = $root.proto.CustomFee.toObject(message.customFees[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TokenFeeScheduleUpdateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenFeeScheduleUpdateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenFeeScheduleUpdateTransactionBody.prototype.toJSON = function() {
            return TokenFeeScheduleUpdateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenFeeScheduleUpdateTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenFeeScheduleUpdateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenFeeScheduleUpdateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenFeeScheduleUpdateTransactionBody";
        };

        return TokenFeeScheduleUpdateTransactionBody;
    })();

    proto.TokenPauseTransactionBody = (function() {

        /**
         * Properties of a TokenPauseTransactionBody.
         * @typedef {Object} proto.TokenPauseTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.
         * <p>
         * The identified token SHALL be paused. Subsequent transactions
         * involving that token SHALL fail until the token is "unpaused".
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenPauseTransactionBody.
         * @memberof proto
         * @interface ITokenPauseTransactionBody
         * @augments proto.TokenPauseTransactionBody.$Properties
         * @deprecated Use proto.TokenPauseTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenPauseTransactionBody.
         * @typedef {proto.TokenPauseTransactionBody.$Properties} proto.TokenPauseTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenPauseTransactionBody.
         * @memberof proto
         * @classdesc Pause transaction activity for a token.
         * 
         * This transaction MUST be signed by the Token `pause_key`.<br/>
         * The `token` identified MUST exist, and MUST NOT be deleted.<br/>
         * The `token` identified MAY be paused; if the token is already paused,
         * this transaction SHALL have no effect.
         * The `token` identified MUST have a `pause_key` set, the `pause_key` MUST be
         * a valid `Key`, and the `pause_key` MUST NOT be an empty `KeyList`.<br/>
         * A `paused` token SHALL NOT be transferred or otherwise modified except to
         * "up-pause" the token with `unpauseToken` or in a `rejectToken` transaction.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenPauseTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenPauseTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * The identified token SHALL be paused. Subsequent transactions
         * involving that token SHALL fail until the token is "unpaused".
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenPauseTransactionBody
         * @instance
         */
        TokenPauseTransactionBody.prototype.token = null;

        /**
         * Creates a new TokenPauseTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenPauseTransactionBody
         * @static
         * @param {proto.TokenPauseTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenPauseTransactionBody} TokenPauseTransactionBody instance
         * @type {{
         *   (properties: proto.TokenPauseTransactionBody.$Shape): proto.TokenPauseTransactionBody & proto.TokenPauseTransactionBody.$Shape;
         *   (properties?: proto.TokenPauseTransactionBody.$Properties): proto.TokenPauseTransactionBody;
         * }}
         */
        TokenPauseTransactionBody.create = function(properties) {
            return new TokenPauseTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenPauseTransactionBody message. Does not implicitly {@link proto.TokenPauseTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenPauseTransactionBody
         * @static
         * @param {proto.TokenPauseTransactionBody.$Properties} message TokenPauseTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenPauseTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenPauseTransactionBody message, length delimited. Does not implicitly {@link proto.TokenPauseTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenPauseTransactionBody
         * @static
         * @param {proto.TokenPauseTransactionBody.$Properties} message TokenPauseTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenPauseTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenPauseTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenPauseTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenPauseTransactionBody & proto.TokenPauseTransactionBody.$Shape} TokenPauseTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenPauseTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenPauseTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenPauseTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenPauseTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenPauseTransactionBody & proto.TokenPauseTransactionBody.$Shape} TokenPauseTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenPauseTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenPauseTransactionBody message.
         * @function verify
         * @memberof proto.TokenPauseTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenPauseTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            return null;
        };

        /**
         * Creates a TokenPauseTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenPauseTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenPauseTransactionBody} TokenPauseTransactionBody
         */
        TokenPauseTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenPauseTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenPauseTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenPauseTransactionBody();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenPauseTransactionBody.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenPauseTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenPauseTransactionBody
         * @static
         * @param {proto.TokenPauseTransactionBody} message TokenPauseTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenPauseTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.token = null;
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenPauseTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenPauseTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenPauseTransactionBody.prototype.toJSON = function() {
            return TokenPauseTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenPauseTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenPauseTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenPauseTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenPauseTransactionBody";
        };

        return TokenPauseTransactionBody;
    })();

    proto.TokenUnpauseTransactionBody = (function() {

        /**
         * Properties of a TokenUnpauseTransactionBody.
         * @typedef {Object} proto.TokenUnpauseTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.
         * <p>
         * The identified token SHALL be "unpaused". Subsequent transactions
         * involving that token MAY succeed.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenUnpauseTransactionBody.
         * @memberof proto
         * @interface ITokenUnpauseTransactionBody
         * @augments proto.TokenUnpauseTransactionBody.$Properties
         * @deprecated Use proto.TokenUnpauseTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenUnpauseTransactionBody.
         * @typedef {proto.TokenUnpauseTransactionBody.$Properties} proto.TokenUnpauseTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenUnpauseTransactionBody.
         * @memberof proto
         * @classdesc Resume transaction activity for a token.
         * 
         * This transaction MUST be signed by the Token `pause_key`.<br/>
         * The `token` identified MUST exist, and MUST NOT be deleted.<br/>
         * The `token` identified MAY not be paused; if the token is not paused,
         * this transaction SHALL have no effect.
         * The `token` identified MUST have a `pause_key` set, the `pause_key` MUST be
         * a valid `Key`, and the `pause_key` MUST NOT be an empty `KeyList`.<br/>
         * An `unpaused` token MAY be transferred or otherwise modified.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenUnpauseTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenUnpauseTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * The identified token SHALL be "unpaused". Subsequent transactions
         * involving that token MAY succeed.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenUnpauseTransactionBody
         * @instance
         */
        TokenUnpauseTransactionBody.prototype.token = null;

        /**
         * Creates a new TokenUnpauseTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenUnpauseTransactionBody
         * @static
         * @param {proto.TokenUnpauseTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenUnpauseTransactionBody} TokenUnpauseTransactionBody instance
         * @type {{
         *   (properties: proto.TokenUnpauseTransactionBody.$Shape): proto.TokenUnpauseTransactionBody & proto.TokenUnpauseTransactionBody.$Shape;
         *   (properties?: proto.TokenUnpauseTransactionBody.$Properties): proto.TokenUnpauseTransactionBody;
         * }}
         */
        TokenUnpauseTransactionBody.create = function(properties) {
            return new TokenUnpauseTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenUnpauseTransactionBody message. Does not implicitly {@link proto.TokenUnpauseTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenUnpauseTransactionBody
         * @static
         * @param {proto.TokenUnpauseTransactionBody.$Properties} message TokenUnpauseTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenUnpauseTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenUnpauseTransactionBody message, length delimited. Does not implicitly {@link proto.TokenUnpauseTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenUnpauseTransactionBody
         * @static
         * @param {proto.TokenUnpauseTransactionBody.$Properties} message TokenUnpauseTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenUnpauseTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenUnpauseTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenUnpauseTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenUnpauseTransactionBody & proto.TokenUnpauseTransactionBody.$Shape} TokenUnpauseTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenUnpauseTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenUnpauseTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenUnpauseTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenUnpauseTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenUnpauseTransactionBody & proto.TokenUnpauseTransactionBody.$Shape} TokenUnpauseTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenUnpauseTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenUnpauseTransactionBody message.
         * @function verify
         * @memberof proto.TokenUnpauseTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenUnpauseTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            return null;
        };

        /**
         * Creates a TokenUnpauseTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenUnpauseTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenUnpauseTransactionBody} TokenUnpauseTransactionBody
         */
        TokenUnpauseTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenUnpauseTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenUnpauseTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenUnpauseTransactionBody();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenUnpauseTransactionBody.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenUnpauseTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenUnpauseTransactionBody
         * @static
         * @param {proto.TokenUnpauseTransactionBody} message TokenUnpauseTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenUnpauseTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.token = null;
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenUnpauseTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenUnpauseTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenUnpauseTransactionBody.prototype.toJSON = function() {
            return TokenUnpauseTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenUnpauseTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenUnpauseTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenUnpauseTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenUnpauseTransactionBody";
        };

        return TokenUnpauseTransactionBody;
    })();

    proto.TokenUpdateNftsTransactionBody = (function() {

        /**
         * Properties of a TokenUpdateNftsTransactionBody.
         * @typedef {Object} proto.TokenUpdateNftsTransactionBody.$Properties
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.<br/>
         * This is the token type (i.e. collection) for which to update NFTs.
         * <p>
         * This field is REQUIRED.<br/>
         * The identified token MUST exist, MUST NOT be paused, MUST have the type
         * non-fungible/unique, and MUST have a valid `metadata_key`.
         * @property {Array.<number|Long>|null} [serialNumbers] A list of serial numbers to be updated.
         * <p>
         * This field is REQUIRED.<br/>
         * This list MUST have at least one(1) entry.<br/>
         * This list MUST NOT have more than ten(10) entries.
         * @property {google.protobuf.BytesValue.$Properties|null} [metadata] A new value for the metadata.
         * <p>
         * If this field is not set, the metadata SHALL NOT change.<br/>
         * This value, if set, MUST NOT exceed 100 bytes.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenUpdateNftsTransactionBody.
         * @memberof proto
         * @interface ITokenUpdateNftsTransactionBody
         * @augments proto.TokenUpdateNftsTransactionBody.$Properties
         * @deprecated Use proto.TokenUpdateNftsTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenUpdateNftsTransactionBody.
         * @typedef {proto.TokenUpdateNftsTransactionBody.$Properties} proto.TokenUpdateNftsTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenUpdateNftsTransactionBody.
         * @memberof proto
         * @classdesc Modify the metadata field for an individual non-fungible/unique token (NFT).
         * 
         * Updating the metadata of an NFT SHALL NOT affect ownership or
         * the ability to transfer that NFT.<br/>
         * This transaction SHALL affect only the specific serial numbered tokens
         * identified.
         * This transaction SHALL modify individual token metadata.<br/>
         * This transaction MUST be signed by the token `metadata_key`.<br/>
         * The token `metadata_key` MUST be a valid `Key`.<br/>
         * The token `metadata_key` MUST NOT be an empty `KeyList`.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenUpdateNftsTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenUpdateNftsTransactionBody = function (properties) {
            this.serialNumbers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.<br/>
         * This is the token type (i.e. collection) for which to update NFTs.
         * <p>
         * This field is REQUIRED.<br/>
         * The identified token MUST exist, MUST NOT be paused, MUST have the type
         * non-fungible/unique, and MUST have a valid `metadata_key`.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @instance
         */
        TokenUpdateNftsTransactionBody.prototype.token = null;

        /**
         * A list of serial numbers to be updated.
         * <p>
         * This field is REQUIRED.<br/>
         * This list MUST have at least one(1) entry.<br/>
         * This list MUST NOT have more than ten(10) entries.
         * @member {Array.<number|Long>} serialNumbers
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @instance
         */
        TokenUpdateNftsTransactionBody.prototype.serialNumbers = $util.emptyArray;

        /**
         * A new value for the metadata.
         * <p>
         * If this field is not set, the metadata SHALL NOT change.<br/>
         * This value, if set, MUST NOT exceed 100 bytes.
         * @member {google.protobuf.BytesValue.$Properties|null|undefined} metadata
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @instance
         */
        TokenUpdateNftsTransactionBody.prototype.metadata = null;

        /**
         * Creates a new TokenUpdateNftsTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @static
         * @param {proto.TokenUpdateNftsTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenUpdateNftsTransactionBody} TokenUpdateNftsTransactionBody instance
         * @type {{
         *   (properties: proto.TokenUpdateNftsTransactionBody.$Shape): proto.TokenUpdateNftsTransactionBody & proto.TokenUpdateNftsTransactionBody.$Shape;
         *   (properties?: proto.TokenUpdateNftsTransactionBody.$Properties): proto.TokenUpdateNftsTransactionBody;
         * }}
         */
        TokenUpdateNftsTransactionBody.create = function(properties) {
            return new TokenUpdateNftsTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenUpdateNftsTransactionBody message. Does not implicitly {@link proto.TokenUpdateNftsTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @static
         * @param {proto.TokenUpdateNftsTransactionBody.$Properties} message TokenUpdateNftsTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenUpdateNftsTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.serialNumbers != null && message.serialNumbers.length)
                writer.uint32(/* id 2, wireType 2 =*/18).int64s(message.serialNumbers);
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                $root.google.protobuf.BytesValue.encode(message.metadata, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenUpdateNftsTransactionBody message, length delimited. Does not implicitly {@link proto.TokenUpdateNftsTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @static
         * @param {proto.TokenUpdateNftsTransactionBody.$Properties} message TokenUpdateNftsTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenUpdateNftsTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenUpdateNftsTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenUpdateNftsTransactionBody & proto.TokenUpdateNftsTransactionBody.$Shape} TokenUpdateNftsTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenUpdateNftsTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenUpdateNftsTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.serialNumbers && message.serialNumbers.length))
                                message.serialNumbers = [];
                            reader.int64s(message.serialNumbers);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.serialNumbers && message.serialNumbers.length))
                            message.serialNumbers = [];
                        message.serialNumbers.push(reader.int64());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.metadata = $root.google.protobuf.BytesValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.metadata);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenUpdateNftsTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenUpdateNftsTransactionBody & proto.TokenUpdateNftsTransactionBody.$Shape} TokenUpdateNftsTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenUpdateNftsTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenUpdateNftsTransactionBody message.
         * @function verify
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenUpdateNftsTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            if (message.serialNumbers != null && $Object.hasOwnProperty.call(message, "serialNumbers")) {
                if (!$Array.isArray(message.serialNumbers))
                    return "serialNumbers: array expected";
                for (let i = 0; i < message.serialNumbers.length; ++i)
                    if (!$util.isInteger(message.serialNumbers[i]) && !(message.serialNumbers[i] && $util.isInteger(message.serialNumbers[i].low) && $util.isInteger(message.serialNumbers[i].high)))
                        return "serialNumbers: integer|Long[] expected";
            }
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata")) {
                let error = $root.google.protobuf.BytesValue.verify(message.metadata, _depth + 1);
                if (error)
                    return "metadata." + error;
            }
            return null;
        };

        /**
         * Creates a TokenUpdateNftsTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenUpdateNftsTransactionBody} TokenUpdateNftsTransactionBody
         */
        TokenUpdateNftsTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenUpdateNftsTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenUpdateNftsTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenUpdateNftsTransactionBody();
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenUpdateNftsTransactionBody.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            if (object.serialNumbers) {
                if (!$Array.isArray(object.serialNumbers))
                    throw $TypeError(".proto.TokenUpdateNftsTransactionBody.serialNumbers: array expected");
                message.serialNumbers = $Array(object.serialNumbers.length);
                for (let i = 0; i < object.serialNumbers.length; ++i)
                    if ($util.Long)
                        message.serialNumbers[i] = $util.Long.fromValue(object.serialNumbers[i], false);
                    else if (typeof object.serialNumbers[i] === "string")
                        message.serialNumbers[i] = $parseInt(object.serialNumbers[i], 10);
                    else if (typeof object.serialNumbers[i] === "number")
                        message.serialNumbers[i] = object.serialNumbers[i];
                    else if (typeof object.serialNumbers[i] === "object")
                        message.serialNumbers[i] = new $util.LongBits(object.serialNumbers[i].low >>> 0, object.serialNumbers[i].high >>> 0).toNumber();
            }
            if (object.metadata != null) {
                if (!$util.isObject(object.metadata))
                    throw $TypeError(".proto.TokenUpdateNftsTransactionBody.metadata: object expected");
                message.metadata = $root.google.protobuf.BytesValue.fromObject(object.metadata, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenUpdateNftsTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @static
         * @param {proto.TokenUpdateNftsTransactionBody} message TokenUpdateNftsTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenUpdateNftsTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.serialNumbers = [];
            if (options.defaults) {
                object.token = null;
                object.metadata = null;
            }
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            if (message.serialNumbers && message.serialNumbers.length) {
                object.serialNumbers = $Array(message.serialNumbers.length);
                for (let j = 0; j < message.serialNumbers.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.serialNumbers[j] = typeof message.serialNumbers[j] === "number" ? $BigInt(message.serialNumbers[j]) : $util.Long.fromBits(message.serialNumbers[j].low >>> 0, message.serialNumbers[j].high >>> 0, false).toBigInt();
                    else if (typeof message.serialNumbers[j] === "number")
                        object.serialNumbers[j] = options.longs === $String ? $String(message.serialNumbers[j]) : message.serialNumbers[j];
                    else
                        object.serialNumbers[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.serialNumbers[j]) : options.longs === $Number ? new $util.LongBits(message.serialNumbers[j].low >>> 0, message.serialNumbers[j].high >>> 0).toNumber() : message.serialNumbers[j];
            }
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                object.metadata = $root.google.protobuf.BytesValue.toObject(message.metadata, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenUpdateNftsTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenUpdateNftsTransactionBody.prototype.toJSON = function() {
            return TokenUpdateNftsTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenUpdateNftsTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenUpdateNftsTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenUpdateNftsTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenUpdateNftsTransactionBody";
        };

        return TokenUpdateNftsTransactionBody;
    })();

    proto.TokenRejectTransactionBody = (function() {

        /**
         * Properties of a TokenRejectTransactionBody.
         * @typedef {Object} proto.TokenRejectTransactionBody.$Properties
         * @property {proto.AccountID.$Properties|null} [owner] An account identifier.<br/>
         * This OPTIONAL field identifies the account holding the
         * tokens to be rejected.
         * <p>
         * If set, this account MUST sign this transaction.
         * If not set, the `payer` for this transaction SHALL be the effective
         * `owner` for this transaction.
         * @property {Array.<proto.TokenReference.$Properties>|null} [rejections] A list of one or more token rejections.
         * <p>
         * On success each rejected token serial number or balance SHALL be
         * transferred from the requesting account to the treasury account for
         * that token type.<br/>
         * After rejection the requesting account SHALL continue to be associated
         * with the token.<br/>
         * If dissociation is desired then a separate `TokenDissociate` transaction
         * MUST be submitted to remove the association.<br/>
         * This list MUST contain at least one (1) entry and MUST NOT contain more
         * than ten (10) entries.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenRejectTransactionBody.
         * @memberof proto
         * @interface ITokenRejectTransactionBody
         * @augments proto.TokenRejectTransactionBody.$Properties
         * @deprecated Use proto.TokenRejectTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenRejectTransactionBody.
         * @typedef {{
         *   owner?: proto.AccountID.$Shape|null;
         *   rejections?: Array.<proto.TokenReference.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenRejectTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenRejectTransactionBody.
         * @memberof proto
         * @classdesc Reject undesired token(s).<br/>
         * Transfer one or more token balances held by the requesting account to the
         * treasury for each token type.
         * 
         * Each transfer SHALL be one of the following
         * - A single non-fungible/unique token.
         * - The full balance held for a fungible/common token.
         * A single `tokenReject` transaction SHALL support a maximum
         * of 10 transfers.<br/>
         * A token that is `pause`d MUST NOT be rejected.<br/>
         * If the `owner` account is `frozen` with respect to the identified token(s)
         * the token(s) MUST NOT be rejected.<br/>
         * The `payer` for this transaction, and `owner` if set, SHALL NOT be charged
         * any custom fees or other fees beyond the `tokenReject` transaction fee.
         * 
         * ### Block Stream Effects
         * - Each successful transfer from `payer` to `treasury` SHALL be recorded in
         * the `token_transfer_list` for the transaction record.
         * @constructor
         * @param {proto.TokenRejectTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenRejectTransactionBody = function (properties) {
            this.rejections = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An account identifier.<br/>
         * This OPTIONAL field identifies the account holding the
         * tokens to be rejected.
         * <p>
         * If set, this account MUST sign this transaction.
         * If not set, the `payer` for this transaction SHALL be the effective
         * `owner` for this transaction.
         * @member {proto.AccountID.$Properties|null|undefined} owner
         * @memberof proto.TokenRejectTransactionBody
         * @instance
         */
        TokenRejectTransactionBody.prototype.owner = null;

        /**
         * A list of one or more token rejections.
         * <p>
         * On success each rejected token serial number or balance SHALL be
         * transferred from the requesting account to the treasury account for
         * that token type.<br/>
         * After rejection the requesting account SHALL continue to be associated
         * with the token.<br/>
         * If dissociation is desired then a separate `TokenDissociate` transaction
         * MUST be submitted to remove the association.<br/>
         * This list MUST contain at least one (1) entry and MUST NOT contain more
         * than ten (10) entries.
         * @member {Array.<proto.TokenReference.$Properties>} rejections
         * @memberof proto.TokenRejectTransactionBody
         * @instance
         */
        TokenRejectTransactionBody.prototype.rejections = $util.emptyArray;

        /**
         * Creates a new TokenRejectTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenRejectTransactionBody
         * @static
         * @param {proto.TokenRejectTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenRejectTransactionBody} TokenRejectTransactionBody instance
         * @type {{
         *   (properties: proto.TokenRejectTransactionBody.$Shape): proto.TokenRejectTransactionBody & proto.TokenRejectTransactionBody.$Shape;
         *   (properties?: proto.TokenRejectTransactionBody.$Properties): proto.TokenRejectTransactionBody;
         * }}
         */
        TokenRejectTransactionBody.create = function(properties) {
            return new TokenRejectTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenRejectTransactionBody message. Does not implicitly {@link proto.TokenRejectTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenRejectTransactionBody
         * @static
         * @param {proto.TokenRejectTransactionBody.$Properties} message TokenRejectTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenRejectTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner"))
                $root.proto.AccountID.encode(message.owner, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.rejections != null && message.rejections.length)
                for (let i = 0; i < message.rejections.length; ++i)
                    $root.proto.TokenReference.encode(message.rejections[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenRejectTransactionBody message, length delimited. Does not implicitly {@link proto.TokenRejectTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenRejectTransactionBody
         * @static
         * @param {proto.TokenRejectTransactionBody.$Properties} message TokenRejectTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenRejectTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenRejectTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenRejectTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenRejectTransactionBody & proto.TokenRejectTransactionBody.$Shape} TokenRejectTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenRejectTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenRejectTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.owner = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.owner);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rejections && message.rejections.length))
                            message.rejections = [];
                        message.rejections.push($root.proto.TokenReference.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenRejectTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenRejectTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenRejectTransactionBody & proto.TokenRejectTransactionBody.$Shape} TokenRejectTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenRejectTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenRejectTransactionBody message.
         * @function verify
         * @memberof proto.TokenRejectTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenRejectTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner")) {
                let error = $root.proto.AccountID.verify(message.owner, _depth + 1);
                if (error)
                    return "owner." + error;
            }
            if (message.rejections != null && $Object.hasOwnProperty.call(message, "rejections")) {
                if (!$Array.isArray(message.rejections))
                    return "rejections: array expected";
                for (let i = 0; i < message.rejections.length; ++i) {
                    let error = $root.proto.TokenReference.verify(message.rejections[i], _depth + 1);
                    if (error)
                        return "rejections." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TokenRejectTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenRejectTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenRejectTransactionBody} TokenRejectTransactionBody
         */
        TokenRejectTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenRejectTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenRejectTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenRejectTransactionBody();
            if (object.owner != null) {
                if (!$util.isObject(object.owner))
                    throw $TypeError(".proto.TokenRejectTransactionBody.owner: object expected");
                message.owner = $root.proto.AccountID.fromObject(object.owner, _depth + 1);
            }
            if (object.rejections) {
                if (!$Array.isArray(object.rejections))
                    throw $TypeError(".proto.TokenRejectTransactionBody.rejections: array expected");
                message.rejections = $Array(object.rejections.length);
                for (let i = 0; i < object.rejections.length; ++i) {
                    if (!$util.isObject(object.rejections[i]))
                        throw $TypeError(".proto.TokenRejectTransactionBody.rejections: object expected");
                    message.rejections[i] = $root.proto.TokenReference.fromObject(object.rejections[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenRejectTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenRejectTransactionBody
         * @static
         * @param {proto.TokenRejectTransactionBody} message TokenRejectTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenRejectTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.rejections = [];
            if (options.defaults)
                object.owner = null;
            if (message.owner != null && $Object.hasOwnProperty.call(message, "owner"))
                object.owner = $root.proto.AccountID.toObject(message.owner, options, _depth + 1);
            if (message.rejections && message.rejections.length) {
                object.rejections = $Array(message.rejections.length);
                for (let j = 0; j < message.rejections.length; ++j)
                    object.rejections[j] = $root.proto.TokenReference.toObject(message.rejections[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TokenRejectTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenRejectTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenRejectTransactionBody.prototype.toJSON = function() {
            return TokenRejectTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenRejectTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenRejectTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenRejectTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenRejectTransactionBody";
        };

        return TokenRejectTransactionBody;
    })();

    proto.TokenReference = (function() {

        /**
         * Properties of a TokenReference.
         * @typedef {Object} proto.TokenReference.$Properties
         * @property {proto.TokenID.$Properties|null} [fungibleToken] A fungible/common token type.
         * @property {proto.NftID.$Properties|null} [nft] A single specific serialized non-fungible/unique token.
         * @property {"fungibleToken"|"nft"} [tokenIdentifier] TokenReference tokenIdentifier
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenReference.
         * @memberof proto
         * @interface ITokenReference
         * @augments proto.TokenReference.$Properties
         * @deprecated Use proto.TokenReference.$Properties instead.
         */

        /**
         * Narrowed shape of a TokenReference.
         * @typedef {{
         *   fungibleToken?: proto.TokenID.$Shape|null;
         *   nft?: proto.NftID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ tokenIdentifier?: undefined; fungibleToken?: null; nft?: null }|{ tokenIdentifier?: "fungibleToken"; fungibleToken: proto.TokenID.$Shape; nft?: null }|{ tokenIdentifier?: "nft"; fungibleToken?: null; nft: proto.NftID.$Shape })
         * )} proto.TokenReference.$Shape
         */

        /**
         * Constructs a new TokenReference.
         * @memberof proto
         * @classdesc A union token identifier.
         * 
         * Identify a fungible/common token type, or a single
         * non-fungible/unique token serial.
         * @constructor
         * @param {proto.TokenReference.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenReference = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A fungible/common token type.
         * @member {proto.TokenID.$Properties|null|undefined} fungibleToken
         * @memberof proto.TokenReference
         * @instance
         */
        TokenReference.prototype.fungibleToken = null;

        /**
         * A single specific serialized non-fungible/unique token.
         * @member {proto.NftID.$Properties|null|undefined} nft
         * @memberof proto.TokenReference
         * @instance
         */
        TokenReference.prototype.nft = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * TokenReference tokenIdentifier.
         * @member {"fungibleToken"|"nft"|undefined} tokenIdentifier
         * @memberof proto.TokenReference
         * @instance
         */
        $Object.defineProperty(TokenReference.prototype, "tokenIdentifier", {
            get: $util.oneOfGetter($oneOfFields = ["fungibleToken", "nft"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new TokenReference instance using the specified properties.
         * @function create
         * @memberof proto.TokenReference
         * @static
         * @param {proto.TokenReference.$Properties=} [properties] Properties to set
         * @returns {proto.TokenReference} TokenReference instance
         * @type {{
         *   (properties: proto.TokenReference.$Shape): proto.TokenReference & proto.TokenReference.$Shape;
         *   (properties?: proto.TokenReference.$Properties): proto.TokenReference;
         * }}
         */
        TokenReference.create = function(properties) {
            return new TokenReference(properties);
        };

        /**
         * Encodes the specified TokenReference message. Does not implicitly {@link proto.TokenReference.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenReference
         * @static
         * @param {proto.TokenReference.$Properties} message TokenReference message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenReference.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fungibleToken != null && $Object.hasOwnProperty.call(message, "fungibleToken"))
                $root.proto.TokenID.encode(message.fungibleToken, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.nft != null && $Object.hasOwnProperty.call(message, "nft"))
                $root.proto.NftID.encode(message.nft, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenReference message, length delimited. Does not implicitly {@link proto.TokenReference.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenReference
         * @static
         * @param {proto.TokenReference.$Properties} message TokenReference message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenReference.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenReference message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenReference
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenReference & proto.TokenReference.$Shape} TokenReference
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenReference.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenReference();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.fungibleToken = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fungibleToken);
                        message.tokenIdentifier = "fungibleToken";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.nft = $root.proto.NftID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nft);
                        message.tokenIdentifier = "nft";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenReference message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenReference
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenReference & proto.TokenReference.$Shape} TokenReference
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenReference.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenReference message.
         * @function verify
         * @memberof proto.TokenReference
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenReference.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.fungibleToken != null && $Object.hasOwnProperty.call(message, "fungibleToken")) {
                properties.tokenIdentifier = 1;
                {
                    let error = $root.proto.TokenID.verify(message.fungibleToken, _depth + 1);
                    if (error)
                        return "fungibleToken." + error;
                }
            }
            if (message.nft != null && $Object.hasOwnProperty.call(message, "nft")) {
                if (properties.tokenIdentifier === 1)
                    return "tokenIdentifier: multiple values";
                properties.tokenIdentifier = 1;
                {
                    let error = $root.proto.NftID.verify(message.nft, _depth + 1);
                    if (error)
                        return "nft." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TokenReference message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenReference
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenReference} TokenReference
         */
        TokenReference.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenReference)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenReference: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenReference();
            if (object.fungibleToken != null) {
                if (!$util.isObject(object.fungibleToken))
                    throw $TypeError(".proto.TokenReference.fungibleToken: object expected");
                message.fungibleToken = $root.proto.TokenID.fromObject(object.fungibleToken, _depth + 1);
            }
            if (object.nft != null) {
                if (!$util.isObject(object.nft))
                    throw $TypeError(".proto.TokenReference.nft: object expected");
                message.nft = $root.proto.NftID.fromObject(object.nft, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenReference message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenReference
         * @static
         * @param {proto.TokenReference} message TokenReference
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenReference.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.fungibleToken != null && $Object.hasOwnProperty.call(message, "fungibleToken")) {
                object.fungibleToken = $root.proto.TokenID.toObject(message.fungibleToken, options, _depth + 1);
                if (options.oneofs)
                    object.tokenIdentifier = "fungibleToken";
            }
            if (message.nft != null && $Object.hasOwnProperty.call(message, "nft")) {
                object.nft = $root.proto.NftID.toObject(message.nft, options, _depth + 1);
                if (options.oneofs)
                    object.tokenIdentifier = "nft";
            }
            return object;
        };

        /**
         * Converts this TokenReference to JSON.
         * @function toJSON
         * @memberof proto.TokenReference
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenReference.prototype.toJSON = function() {
            return TokenReference.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenReference
         * @function getTypeUrl
         * @memberof proto.TokenReference
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenReference.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenReference";
        };

        return TokenReference;
    })();

    proto.TokenAirdropTransactionBody = (function() {

        /**
         * Properties of a TokenAirdropTransactionBody.
         * @typedef {Object} proto.TokenAirdropTransactionBody.$Properties
         * @property {Array.<proto.TokenTransferList.$Properties>|null} [tokenTransfers] A list of token transfers representing one or more airdrops.
         * <p>
         * The sender for each transfer MUST have sufficient balance to complete
         * the transfers.<br/>
         * All token transfers MUST successfully transfer tokens or create a
         * pending airdrop for this transaction to succeed.<br/>
         * This list MUST contain between 1 and 10 transfers, inclusive.
         * <p>
         * Note that each transfer of fungible/common tokens requires both a debit
         * and a credit, so each _fungible_ token transfer MUST have _balanced_
         * entries in the TokenTransferList for that transfer.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenAirdropTransactionBody.
         * @memberof proto
         * @interface ITokenAirdropTransactionBody
         * @augments proto.TokenAirdropTransactionBody.$Properties
         * @deprecated Use proto.TokenAirdropTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenAirdropTransactionBody.
         * @typedef {{
         *   tokenTransfers?: Array.<proto.TokenTransferList.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenAirdropTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenAirdropTransactionBody.
         * @memberof proto
         * @classdesc Airdrop one or more tokens to one or more accounts.
         * 
         * ### Effects
         * This distributes tokens from the balance of one or more sending account(s)
         * to the balance of one or more recipient accounts. Accounts MAY receive the
         * tokens in one of four ways.
         * 
         * - An account already associated to the token to be distributed SHALL
         * receive the airdropped tokens immediately to the recipient account
         * balance.<br/>
         * The fee for this transfer SHALL include the transfer, the airdrop fee,
         * and any custom fees.
         * - An account with available automatic association slots SHALL be
         * automatically associated to the token, and SHALL immediately receive
         * the airdropped tokens to the recipient account balance.<br/>
         * The fee for this transfer SHALL include the transfer, the association,
         * the cost to renew that association once, the airdrop fee, and
         * any custom fees.
         * - An account with "receiver signature required" set SHALL have a
         * "Pending Airdrop" created and must claim that airdrop with a
         * `claimAirdrop` transaction.<br/>
         * The fee for this transfer SHALL include the transfer, the association,
         * the cost to renew that association once, the airdrop fee, and
         * any custom fees.<br/>
         * If the pending airdrop is not claimed immediately, the `sender` SHALL
         * pay the cost to renew the token association, and the cost to maintain
         * the pending airdrop, until the pending airdrop is claimed or cancelled.
         * - An account with no available automatic association slots SHALL have a
         * "Pending Airdrop" created and must claim that airdrop with a
         * `claimAirdrop` transaction.<br/>
         * The fee for this transfer SHALL include the transfer, the association,
         * the cost to renew that association once, the airdrop fee, and any custom
         * fees.<br/>
         * If the pending airdrop is not claimed immediately, the `sender` SHALL
         * pay the cost to renew the token association, and the cost to maintain
         * the pending airdrop, until the pending airdrop is claimed or cancelled.
         * 
         * If an airdrop would create a pending airdrop for a fungible/common token,
         * and a pending airdrop for the same sender, receiver, and token already
         * exists, the existing pending airdrop SHALL be updated to add the new
         * amount to the existing airdrop, rather than creating
         * a new pending airdrop.<br/>
         * Any airdrop that completes immediately SHALL be irreversible. Any airdrop
         * that results in a "Pending Airdrop" MAY be canceled via a `cancelAirdrop`
         * transaction.<br/>
         * All transfer fees (including custom fees and royalties), as well as the
         * rent cost for the first auto-renewal period for any automatic-association
         * slot occupied by the airdropped tokens, SHALL be charged to the account
         * paying for this transaction.<br/>
         * 
         * ### Block Stream Effects
         * - Each successful transfer SHALL be recorded in `token_transfer_list`
         * for the transaction record.
         * - Each successful transfer that consumes an automatic association slot
         * SHALL populate the `automatic_association` field for the record.
         * - Each pending transfer _created_ SHALL be added to the
         * `pending_airdrops` field for the record.
         * - Each pending transfer _updated_ SHALL be added to the
         * `pending_airdrops` field for the record.
         * @constructor
         * @param {proto.TokenAirdropTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenAirdropTransactionBody = function (properties) {
            this.tokenTransfers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of token transfers representing one or more airdrops.
         * <p>
         * The sender for each transfer MUST have sufficient balance to complete
         * the transfers.<br/>
         * All token transfers MUST successfully transfer tokens or create a
         * pending airdrop for this transaction to succeed.<br/>
         * This list MUST contain between 1 and 10 transfers, inclusive.
         * <p>
         * Note that each transfer of fungible/common tokens requires both a debit
         * and a credit, so each _fungible_ token transfer MUST have _balanced_
         * entries in the TokenTransferList for that transfer.
         * @member {Array.<proto.TokenTransferList.$Properties>} tokenTransfers
         * @memberof proto.TokenAirdropTransactionBody
         * @instance
         */
        TokenAirdropTransactionBody.prototype.tokenTransfers = $util.emptyArray;

        /**
         * Creates a new TokenAirdropTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenAirdropTransactionBody
         * @static
         * @param {proto.TokenAirdropTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenAirdropTransactionBody} TokenAirdropTransactionBody instance
         * @type {{
         *   (properties: proto.TokenAirdropTransactionBody.$Shape): proto.TokenAirdropTransactionBody & proto.TokenAirdropTransactionBody.$Shape;
         *   (properties?: proto.TokenAirdropTransactionBody.$Properties): proto.TokenAirdropTransactionBody;
         * }}
         */
        TokenAirdropTransactionBody.create = function(properties) {
            return new TokenAirdropTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenAirdropTransactionBody message. Does not implicitly {@link proto.TokenAirdropTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenAirdropTransactionBody
         * @static
         * @param {proto.TokenAirdropTransactionBody.$Properties} message TokenAirdropTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenAirdropTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenTransfers != null && message.tokenTransfers.length)
                for (let i = 0; i < message.tokenTransfers.length; ++i)
                    $root.proto.TokenTransferList.encode(message.tokenTransfers[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenAirdropTransactionBody message, length delimited. Does not implicitly {@link proto.TokenAirdropTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenAirdropTransactionBody
         * @static
         * @param {proto.TokenAirdropTransactionBody.$Properties} message TokenAirdropTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenAirdropTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenAirdropTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenAirdropTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenAirdropTransactionBody & proto.TokenAirdropTransactionBody.$Shape} TokenAirdropTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenAirdropTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenAirdropTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tokenTransfers && message.tokenTransfers.length))
                            message.tokenTransfers = [];
                        message.tokenTransfers.push($root.proto.TokenTransferList.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenAirdropTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenAirdropTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenAirdropTransactionBody & proto.TokenAirdropTransactionBody.$Shape} TokenAirdropTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenAirdropTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenAirdropTransactionBody message.
         * @function verify
         * @memberof proto.TokenAirdropTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenAirdropTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenTransfers != null && $Object.hasOwnProperty.call(message, "tokenTransfers")) {
                if (!$Array.isArray(message.tokenTransfers))
                    return "tokenTransfers: array expected";
                for (let i = 0; i < message.tokenTransfers.length; ++i) {
                    let error = $root.proto.TokenTransferList.verify(message.tokenTransfers[i], _depth + 1);
                    if (error)
                        return "tokenTransfers." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TokenAirdropTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenAirdropTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenAirdropTransactionBody} TokenAirdropTransactionBody
         */
        TokenAirdropTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenAirdropTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenAirdropTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenAirdropTransactionBody();
            if (object.tokenTransfers) {
                if (!$Array.isArray(object.tokenTransfers))
                    throw $TypeError(".proto.TokenAirdropTransactionBody.tokenTransfers: array expected");
                message.tokenTransfers = $Array(object.tokenTransfers.length);
                for (let i = 0; i < object.tokenTransfers.length; ++i) {
                    if (!$util.isObject(object.tokenTransfers[i]))
                        throw $TypeError(".proto.TokenAirdropTransactionBody.tokenTransfers: object expected");
                    message.tokenTransfers[i] = $root.proto.TokenTransferList.fromObject(object.tokenTransfers[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenAirdropTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenAirdropTransactionBody
         * @static
         * @param {proto.TokenAirdropTransactionBody} message TokenAirdropTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenAirdropTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tokenTransfers = [];
            if (message.tokenTransfers && message.tokenTransfers.length) {
                object.tokenTransfers = $Array(message.tokenTransfers.length);
                for (let j = 0; j < message.tokenTransfers.length; ++j)
                    object.tokenTransfers[j] = $root.proto.TokenTransferList.toObject(message.tokenTransfers[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TokenAirdropTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenAirdropTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenAirdropTransactionBody.prototype.toJSON = function() {
            return TokenAirdropTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenAirdropTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenAirdropTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenAirdropTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenAirdropTransactionBody";
        };

        return TokenAirdropTransactionBody;
    })();

    proto.TokenCancelAirdropTransactionBody = (function() {

        /**
         * Properties of a TokenCancelAirdropTransactionBody.
         * @typedef {Object} proto.TokenCancelAirdropTransactionBody.$Properties
         * @property {Array.<proto.PendingAirdropId.$Properties>|null} [pendingAirdrops] A list of one or more pending airdrop identifiers.<br/>
         * This list declares the set of pending airdrop entries that the client
         * wishes to cancel; on success all listed pending airdrop entries
         * will be removed.
         * <p>
         * This transaction MUST be signed by the account identified by a
         * `sender_id` for each entry in this list.<br/>
         * This list MUST NOT have any duplicate entries.<br/>
         * This list MUST contain between 1 and 10 entries, inclusive.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenCancelAirdropTransactionBody.
         * @memberof proto
         * @interface ITokenCancelAirdropTransactionBody
         * @augments proto.TokenCancelAirdropTransactionBody.$Properties
         * @deprecated Use proto.TokenCancelAirdropTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenCancelAirdropTransactionBody.
         * @typedef {{
         *   pendingAirdrops?: Array.<proto.PendingAirdropId.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenCancelAirdropTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenCancelAirdropTransactionBody.
         * @memberof proto
         * @classdesc Token cancel airdrop<br/>
         * Remove one or more pending airdrops from state on behalf of the
         * sender(s) for each airdrop.
         * 
         * Each pending airdrop canceled SHALL be removed from state and
         * SHALL NOT be available to claim.<br/>
         * Each cancellation SHALL be represented in the transaction body and
         * SHALL NOT be restated in the record file.<br/>
         * All cancellations MUST succeed for this transaction to succeed.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.TokenCancelAirdropTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenCancelAirdropTransactionBody = function (properties) {
            this.pendingAirdrops = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of one or more pending airdrop identifiers.<br/>
         * This list declares the set of pending airdrop entries that the client
         * wishes to cancel; on success all listed pending airdrop entries
         * will be removed.
         * <p>
         * This transaction MUST be signed by the account identified by a
         * `sender_id` for each entry in this list.<br/>
         * This list MUST NOT have any duplicate entries.<br/>
         * This list MUST contain between 1 and 10 entries, inclusive.
         * @member {Array.<proto.PendingAirdropId.$Properties>} pendingAirdrops
         * @memberof proto.TokenCancelAirdropTransactionBody
         * @instance
         */
        TokenCancelAirdropTransactionBody.prototype.pendingAirdrops = $util.emptyArray;

        /**
         * Creates a new TokenCancelAirdropTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenCancelAirdropTransactionBody
         * @static
         * @param {proto.TokenCancelAirdropTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenCancelAirdropTransactionBody} TokenCancelAirdropTransactionBody instance
         * @type {{
         *   (properties: proto.TokenCancelAirdropTransactionBody.$Shape): proto.TokenCancelAirdropTransactionBody & proto.TokenCancelAirdropTransactionBody.$Shape;
         *   (properties?: proto.TokenCancelAirdropTransactionBody.$Properties): proto.TokenCancelAirdropTransactionBody;
         * }}
         */
        TokenCancelAirdropTransactionBody.create = function(properties) {
            return new TokenCancelAirdropTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenCancelAirdropTransactionBody message. Does not implicitly {@link proto.TokenCancelAirdropTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenCancelAirdropTransactionBody
         * @static
         * @param {proto.TokenCancelAirdropTransactionBody.$Properties} message TokenCancelAirdropTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenCancelAirdropTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pendingAirdrops != null && message.pendingAirdrops.length)
                for (let i = 0; i < message.pendingAirdrops.length; ++i)
                    $root.proto.PendingAirdropId.encode(message.pendingAirdrops[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenCancelAirdropTransactionBody message, length delimited. Does not implicitly {@link proto.TokenCancelAirdropTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenCancelAirdropTransactionBody
         * @static
         * @param {proto.TokenCancelAirdropTransactionBody.$Properties} message TokenCancelAirdropTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenCancelAirdropTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenCancelAirdropTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenCancelAirdropTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenCancelAirdropTransactionBody & proto.TokenCancelAirdropTransactionBody.$Shape} TokenCancelAirdropTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenCancelAirdropTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenCancelAirdropTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pendingAirdrops && message.pendingAirdrops.length))
                            message.pendingAirdrops = [];
                        message.pendingAirdrops.push($root.proto.PendingAirdropId.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenCancelAirdropTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenCancelAirdropTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenCancelAirdropTransactionBody & proto.TokenCancelAirdropTransactionBody.$Shape} TokenCancelAirdropTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenCancelAirdropTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenCancelAirdropTransactionBody message.
         * @function verify
         * @memberof proto.TokenCancelAirdropTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenCancelAirdropTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pendingAirdrops != null && $Object.hasOwnProperty.call(message, "pendingAirdrops")) {
                if (!$Array.isArray(message.pendingAirdrops))
                    return "pendingAirdrops: array expected";
                for (let i = 0; i < message.pendingAirdrops.length; ++i) {
                    let error = $root.proto.PendingAirdropId.verify(message.pendingAirdrops[i], _depth + 1);
                    if (error)
                        return "pendingAirdrops." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TokenCancelAirdropTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenCancelAirdropTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenCancelAirdropTransactionBody} TokenCancelAirdropTransactionBody
         */
        TokenCancelAirdropTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenCancelAirdropTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenCancelAirdropTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenCancelAirdropTransactionBody();
            if (object.pendingAirdrops) {
                if (!$Array.isArray(object.pendingAirdrops))
                    throw $TypeError(".proto.TokenCancelAirdropTransactionBody.pendingAirdrops: array expected");
                message.pendingAirdrops = $Array(object.pendingAirdrops.length);
                for (let i = 0; i < object.pendingAirdrops.length; ++i) {
                    if (!$util.isObject(object.pendingAirdrops[i]))
                        throw $TypeError(".proto.TokenCancelAirdropTransactionBody.pendingAirdrops: object expected");
                    message.pendingAirdrops[i] = $root.proto.PendingAirdropId.fromObject(object.pendingAirdrops[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenCancelAirdropTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenCancelAirdropTransactionBody
         * @static
         * @param {proto.TokenCancelAirdropTransactionBody} message TokenCancelAirdropTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenCancelAirdropTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.pendingAirdrops = [];
            if (message.pendingAirdrops && message.pendingAirdrops.length) {
                object.pendingAirdrops = $Array(message.pendingAirdrops.length);
                for (let j = 0; j < message.pendingAirdrops.length; ++j)
                    object.pendingAirdrops[j] = $root.proto.PendingAirdropId.toObject(message.pendingAirdrops[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TokenCancelAirdropTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenCancelAirdropTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenCancelAirdropTransactionBody.prototype.toJSON = function() {
            return TokenCancelAirdropTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenCancelAirdropTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenCancelAirdropTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenCancelAirdropTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenCancelAirdropTransactionBody";
        };

        return TokenCancelAirdropTransactionBody;
    })();

    proto.TokenClaimAirdropTransactionBody = (function() {

        /**
         * Properties of a TokenClaimAirdropTransactionBody.
         * @typedef {Object} proto.TokenClaimAirdropTransactionBody.$Properties
         * @property {Array.<proto.PendingAirdropId.$Properties>|null} [pendingAirdrops] A list of one or more pending airdrop identifiers.
         * <p>
         * This transaction MUST be signed by the account identified by
         * the `receiver_id` for each entry in this list.<br/>
         * This list MUST contain between 1 and 10 entries, inclusive.<br/>
         * This list MUST NOT have any duplicate entries.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenClaimAirdropTransactionBody.
         * @memberof proto
         * @interface ITokenClaimAirdropTransactionBody
         * @augments proto.TokenClaimAirdropTransactionBody.$Properties
         * @deprecated Use proto.TokenClaimAirdropTransactionBody.$Properties instead.
         */

        /**
         * Shape of a TokenClaimAirdropTransactionBody.
         * @typedef {{
         *   pendingAirdrops?: Array.<proto.PendingAirdropId.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenClaimAirdropTransactionBody.$Shape
         */

        /**
         * Constructs a new TokenClaimAirdropTransactionBody.
         * @memberof proto
         * @classdesc Token claim airdrop<br/>
         * Complete one or more pending transfers on behalf of the
         * recipient(s) for an airdrop.
         * 
         * The sender MUST have sufficient balance to fulfill the airdrop at the
         * time of claim. If the sender does not have sufficient balance, the
         * claim SHALL fail.<br/>
         * Each pending airdrop successfully claimed SHALL be removed from state and
         * SHALL NOT be available to claim again.<br/>
         * Each claim SHALL be represented in the transaction body and
         * SHALL NOT be restated in the record file.<br/>
         * All claims MUST succeed for this transaction to succeed.
         * 
         * ### Block Stream Effects
         * The completed transfers SHALL be present in the transfer list.
         * @constructor
         * @param {proto.TokenClaimAirdropTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenClaimAirdropTransactionBody = function (properties) {
            this.pendingAirdrops = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of one or more pending airdrop identifiers.
         * <p>
         * This transaction MUST be signed by the account identified by
         * the `receiver_id` for each entry in this list.<br/>
         * This list MUST contain between 1 and 10 entries, inclusive.<br/>
         * This list MUST NOT have any duplicate entries.
         * @member {Array.<proto.PendingAirdropId.$Properties>} pendingAirdrops
         * @memberof proto.TokenClaimAirdropTransactionBody
         * @instance
         */
        TokenClaimAirdropTransactionBody.prototype.pendingAirdrops = $util.emptyArray;

        /**
         * Creates a new TokenClaimAirdropTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.TokenClaimAirdropTransactionBody
         * @static
         * @param {proto.TokenClaimAirdropTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.TokenClaimAirdropTransactionBody} TokenClaimAirdropTransactionBody instance
         * @type {{
         *   (properties: proto.TokenClaimAirdropTransactionBody.$Shape): proto.TokenClaimAirdropTransactionBody & proto.TokenClaimAirdropTransactionBody.$Shape;
         *   (properties?: proto.TokenClaimAirdropTransactionBody.$Properties): proto.TokenClaimAirdropTransactionBody;
         * }}
         */
        TokenClaimAirdropTransactionBody.create = function(properties) {
            return new TokenClaimAirdropTransactionBody(properties);
        };

        /**
         * Encodes the specified TokenClaimAirdropTransactionBody message. Does not implicitly {@link proto.TokenClaimAirdropTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenClaimAirdropTransactionBody
         * @static
         * @param {proto.TokenClaimAirdropTransactionBody.$Properties} message TokenClaimAirdropTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenClaimAirdropTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pendingAirdrops != null && message.pendingAirdrops.length)
                for (let i = 0; i < message.pendingAirdrops.length; ++i)
                    $root.proto.PendingAirdropId.encode(message.pendingAirdrops[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenClaimAirdropTransactionBody message, length delimited. Does not implicitly {@link proto.TokenClaimAirdropTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenClaimAirdropTransactionBody
         * @static
         * @param {proto.TokenClaimAirdropTransactionBody.$Properties} message TokenClaimAirdropTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenClaimAirdropTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenClaimAirdropTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenClaimAirdropTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenClaimAirdropTransactionBody & proto.TokenClaimAirdropTransactionBody.$Shape} TokenClaimAirdropTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenClaimAirdropTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenClaimAirdropTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pendingAirdrops && message.pendingAirdrops.length))
                            message.pendingAirdrops = [];
                        message.pendingAirdrops.push($root.proto.PendingAirdropId.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenClaimAirdropTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenClaimAirdropTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenClaimAirdropTransactionBody & proto.TokenClaimAirdropTransactionBody.$Shape} TokenClaimAirdropTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenClaimAirdropTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenClaimAirdropTransactionBody message.
         * @function verify
         * @memberof proto.TokenClaimAirdropTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenClaimAirdropTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pendingAirdrops != null && $Object.hasOwnProperty.call(message, "pendingAirdrops")) {
                if (!$Array.isArray(message.pendingAirdrops))
                    return "pendingAirdrops: array expected";
                for (let i = 0; i < message.pendingAirdrops.length; ++i) {
                    let error = $root.proto.PendingAirdropId.verify(message.pendingAirdrops[i], _depth + 1);
                    if (error)
                        return "pendingAirdrops." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TokenClaimAirdropTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenClaimAirdropTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenClaimAirdropTransactionBody} TokenClaimAirdropTransactionBody
         */
        TokenClaimAirdropTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenClaimAirdropTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenClaimAirdropTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenClaimAirdropTransactionBody();
            if (object.pendingAirdrops) {
                if (!$Array.isArray(object.pendingAirdrops))
                    throw $TypeError(".proto.TokenClaimAirdropTransactionBody.pendingAirdrops: array expected");
                message.pendingAirdrops = $Array(object.pendingAirdrops.length);
                for (let i = 0; i < object.pendingAirdrops.length; ++i) {
                    if (!$util.isObject(object.pendingAirdrops[i]))
                        throw $TypeError(".proto.TokenClaimAirdropTransactionBody.pendingAirdrops: object expected");
                    message.pendingAirdrops[i] = $root.proto.PendingAirdropId.fromObject(object.pendingAirdrops[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenClaimAirdropTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenClaimAirdropTransactionBody
         * @static
         * @param {proto.TokenClaimAirdropTransactionBody} message TokenClaimAirdropTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenClaimAirdropTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.pendingAirdrops = [];
            if (message.pendingAirdrops && message.pendingAirdrops.length) {
                object.pendingAirdrops = $Array(message.pendingAirdrops.length);
                for (let j = 0; j < message.pendingAirdrops.length; ++j)
                    object.pendingAirdrops[j] = $root.proto.PendingAirdropId.toObject(message.pendingAirdrops[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TokenClaimAirdropTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.TokenClaimAirdropTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenClaimAirdropTransactionBody.prototype.toJSON = function() {
            return TokenClaimAirdropTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenClaimAirdropTransactionBody
         * @function getTypeUrl
         * @memberof proto.TokenClaimAirdropTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenClaimAirdropTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenClaimAirdropTransactionBody";
        };

        return TokenClaimAirdropTransactionBody;
    })();

    proto.ScheduleCreateTransactionBody = (function() {

        /**
         * Properties of a ScheduleCreateTransactionBody.
         * @typedef {Object} proto.ScheduleCreateTransactionBody.$Properties
         * @property {proto.SchedulableTransactionBody.$Properties|null} [scheduledTransactionBody] A scheduled transaction.
         * <p>
         * This value is REQUIRED.<br/>
         * This transaction body MUST be one of the types enabled in the
         * network configuration value `scheduling.whitelist`.
         * @property {string|null} [memo] A short description of the schedule.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {proto.Key.$Properties|null} [adminKey] A `Key` required to delete this schedule.
         * <p>
         * If this is not set, or is an empty `KeyList`, this schedule SHALL be
         * immutable and SHALL NOT be deleted.
         * @property {proto.AccountID.$Properties|null} [payerAccountID] An account identifier of a `payer` for the scheduled transaction.
         * <p>
         * This value MAY be unset. If unset, the `payer` for this `scheduleCreate`
         * transaction SHALL be the `payer` for the scheduled transaction.<br/>
         * If this is set, the identified account SHALL be charged the fees
         * required for the scheduled transaction when it is executed.<br/>
         * If the actual `payer` for the _scheduled_ transaction lacks
         * sufficient HBAR balance to pay service fees for the scheduled
         * transaction _when it executes_, the scheduled transaction
         * SHALL fail with `INSUFFICIENT_PAYER_BALANCE`.<br/>
         * @property {proto.Timestamp.$Properties|null} [expirationTime] An expiration time.
         * <p>
         * If not set, the expiration SHALL default to the current consensus time
         * advanced by either the network configuration value
         * `scheduling.maxExpirationFutureSeconds`, if `wait_for_expiry` is set and
         * "long term" schedules are enabled, or the network configuration value
         * `ledger.scheduleTxExpiryTimeSecs` otherwise.
         * @property {boolean|null} [waitForExpiry] A flag to delay execution until expiration.
         * <p>
         * If this flag is set the scheduled transaction SHALL NOT be evaluated for
         * execution before the network consensus time matches or exceeds the
         * `expiration_time`.<br/>
         * If this flag is not set, the scheduled transaction SHALL be executed
         * immediately when all required signatures are received, whether in this
         * `scheduleCreate` transaction or a later `scheduleSign` transaction.<br/>
         * This value SHALL NOT be used and MUST NOT be set when the network
         * configuration value `scheduling.longTermEnabled` is not enabled.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ScheduleCreateTransactionBody.
         * @memberof proto
         * @interface IScheduleCreateTransactionBody
         * @augments proto.ScheduleCreateTransactionBody.$Properties
         * @deprecated Use proto.ScheduleCreateTransactionBody.$Properties instead.
         */

        /**
         * Shape of a ScheduleCreateTransactionBody.
         * @typedef {{
         *   scheduledTransactionBody?: proto.SchedulableTransactionBody.$Shape|null;
         *   memo?: string|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   payerAccountID?: proto.AccountID.$Shape|null;
         *   expirationTime?: proto.Timestamp.$Shape|null;
         *   waitForExpiry?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ScheduleCreateTransactionBody.$Shape
         */

        /**
         * Constructs a new ScheduleCreateTransactionBody.
         * @memberof proto
         * @classdesc Create a new Schedule.
         * 
         * #### Requirements
         * This transaction SHALL create a new _schedule_ entity in network state.<br/>
         * The schedule created SHALL contain the `scheduledTransactionBody` to be
         * executed.<br/>
         * If successful the receipt SHALL contain a `scheduleID` with the full
         * identifier of the schedule created.<br/>
         * When a schedule _executes_ successfully, the receipt SHALL include a
         * `scheduledTransactionID` with the `TransactionID` of the transaction that
         * executed.<br/>
         * When a scheduled transaction is executed the network SHALL charge the
         * regular _service_ fee for the transaction to the `payerAccountID` for
         * that schedule, but SHALL NOT charge node or network fees.<br/>
         * If the `payerAccountID` field is not set, the effective `payerAccountID`
         * SHALL be the `payer` for this create transaction.<br/>
         * If an `adminKey` is not specified, or is an empty `KeyList`, the schedule
         * created SHALL be immutable.<br/>
         * An immutable schedule MAY be signed, and MAY execute, but SHALL NOT be
         * deleted.<br/>
         * If two schedules have the same values for all fields except `payerAccountID`
         * then those two schedules SHALL be deemed "identical".<br/>
         * If a `scheduleCreate` requests a new schedule that is identical to an
         * existing schedule, the transaction SHALL fail and SHALL return a status
         * code of `IDENTICAL_SCHEDULE_ALREADY_CREATED` in the receipt.<br/>
         * The receipt for a duplicate schedule SHALL include the `ScheduleID` of the
         * existing schedule and the `TransactionID` of the earlier `scheduleCreate`
         * so that the earlier schedule may be queried and/or referred to in a
         * subsequent `scheduleSign`.
         * 
         * #### Signature Requirements
         * A `scheduleSign` transaction SHALL be used to add additional signatures
         * to an existing schedule.<br/>
         * Each signature SHALL "activate" the corresponding cryptographic("primitive")
         * key for that schedule.<br/>
         * Signature requirements SHALL be met when the set of active keys includes
         * all keys required by the scheduled transaction.<br/>
         * A scheduled transaction for a "long term" schedule SHALL NOT execute if
         * the signature requirements for that transaction are not met when the
         * network consensus time reaches the schedule `expiration_time`.<br/>
         * A "short term" schedule SHALL execute immediately once signature
         * requirements are met. This MAY be immediately when created.
         * 
         * #### Long Term Schedules
         * A "short term" schedule SHALL have the flag `wait_for_expiry` _unset_.<br/>
         * A "long term" schedule SHALL have the flag  `wait_for_expiry` _set_.<br/>
         * A "long term" schedule SHALL NOT be accepted if the network configuration
         * `scheduling.longTermEnabled` is not enabled.<br/>
         * A "long term" schedule SHALL execute when the current consensus time
         * matches or exceeds the `expiration_time` for that schedule, if the
         * signature requirements for the scheduled transaction
         * are met at that instant.<br/>
         * A "long term" schedule SHALL NOT execute before the current consensus time
         * matches or exceeds the `expiration_time` for that schedule.<br/>
         * A "long term" schedule SHALL expire, and be removed from state, after the
         * network consensus time exceeds the schedule `expiration_time`.<br/>
         * A short term schedule SHALL expire, and be removed from state,
         * after the network consensus time exceeds the current network
         * configuration for `ledger.scheduleTxExpiryTimeSecs`.
         * 
         * > Note
         * >> Long term schedules are not (as of release 0.56.0) enabled. Any schedule
         * >> created currently MUST NOT set the `wait_for_expiry` flag.<br/>
         * >> When long term schedules are not enabled, schedules SHALL NOT be
         * >> executed at expiration, and MUST meet signature requirements strictly
         * >> before expiration to be executed.
         * 
         * ### Block Stream Effects
         * If the scheduled transaction is executed immediately, the transaction
         * record SHALL include a `scheduleRef` with the schedule identifier of the
         * schedule created.
         * @constructor
         * @param {proto.ScheduleCreateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ScheduleCreateTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A scheduled transaction.
         * <p>
         * This value is REQUIRED.<br/>
         * This transaction body MUST be one of the types enabled in the
         * network configuration value `scheduling.whitelist`.
         * @member {proto.SchedulableTransactionBody.$Properties|null|undefined} scheduledTransactionBody
         * @memberof proto.ScheduleCreateTransactionBody
         * @instance
         */
        ScheduleCreateTransactionBody.prototype.scheduledTransactionBody = null;

        /**
         * A short description of the schedule.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.ScheduleCreateTransactionBody
         * @instance
         */
        ScheduleCreateTransactionBody.prototype.memo = "";

        /**
         * A `Key` required to delete this schedule.
         * <p>
         * If this is not set, or is an empty `KeyList`, this schedule SHALL be
         * immutable and SHALL NOT be deleted.
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.ScheduleCreateTransactionBody
         * @instance
         */
        ScheduleCreateTransactionBody.prototype.adminKey = null;

        /**
         * An account identifier of a `payer` for the scheduled transaction.
         * <p>
         * This value MAY be unset. If unset, the `payer` for this `scheduleCreate`
         * transaction SHALL be the `payer` for the scheduled transaction.<br/>
         * If this is set, the identified account SHALL be charged the fees
         * required for the scheduled transaction when it is executed.<br/>
         * If the actual `payer` for the _scheduled_ transaction lacks
         * sufficient HBAR balance to pay service fees for the scheduled
         * transaction _when it executes_, the scheduled transaction
         * SHALL fail with `INSUFFICIENT_PAYER_BALANCE`.<br/>
         * @member {proto.AccountID.$Properties|null|undefined} payerAccountID
         * @memberof proto.ScheduleCreateTransactionBody
         * @instance
         */
        ScheduleCreateTransactionBody.prototype.payerAccountID = null;

        /**
         * An expiration time.
         * <p>
         * If not set, the expiration SHALL default to the current consensus time
         * advanced by either the network configuration value
         * `scheduling.maxExpirationFutureSeconds`, if `wait_for_expiry` is set and
         * "long term" schedules are enabled, or the network configuration value
         * `ledger.scheduleTxExpiryTimeSecs` otherwise.
         * @member {proto.Timestamp.$Properties|null|undefined} expirationTime
         * @memberof proto.ScheduleCreateTransactionBody
         * @instance
         */
        ScheduleCreateTransactionBody.prototype.expirationTime = null;

        /**
         * A flag to delay execution until expiration.
         * <p>
         * If this flag is set the scheduled transaction SHALL NOT be evaluated for
         * execution before the network consensus time matches or exceeds the
         * `expiration_time`.<br/>
         * If this flag is not set, the scheduled transaction SHALL be executed
         * immediately when all required signatures are received, whether in this
         * `scheduleCreate` transaction or a later `scheduleSign` transaction.<br/>
         * This value SHALL NOT be used and MUST NOT be set when the network
         * configuration value `scheduling.longTermEnabled` is not enabled.
         * @member {boolean} waitForExpiry
         * @memberof proto.ScheduleCreateTransactionBody
         * @instance
         */
        ScheduleCreateTransactionBody.prototype.waitForExpiry = false;

        /**
         * Creates a new ScheduleCreateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.ScheduleCreateTransactionBody
         * @static
         * @param {proto.ScheduleCreateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.ScheduleCreateTransactionBody} ScheduleCreateTransactionBody instance
         * @type {{
         *   (properties: proto.ScheduleCreateTransactionBody.$Shape): proto.ScheduleCreateTransactionBody & proto.ScheduleCreateTransactionBody.$Shape;
         *   (properties?: proto.ScheduleCreateTransactionBody.$Properties): proto.ScheduleCreateTransactionBody;
         * }}
         */
        ScheduleCreateTransactionBody.create = function(properties) {
            return new ScheduleCreateTransactionBody(properties);
        };

        /**
         * Encodes the specified ScheduleCreateTransactionBody message. Does not implicitly {@link proto.ScheduleCreateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.ScheduleCreateTransactionBody
         * @static
         * @param {proto.ScheduleCreateTransactionBody.$Properties} message ScheduleCreateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleCreateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.scheduledTransactionBody != null && $Object.hasOwnProperty.call(message, "scheduledTransactionBody"))
                $root.proto.SchedulableTransactionBody.encode(message.scheduledTransactionBody, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.memo);
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.payerAccountID != null && $Object.hasOwnProperty.call(message, "payerAccountID"))
                $root.proto.AccountID.encode(message.payerAccountID, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                $root.proto.Timestamp.encode(message.expirationTime, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.waitForExpiry != null && $Object.hasOwnProperty.call(message, "waitForExpiry") && message.waitForExpiry !== false)
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.waitForExpiry);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ScheduleCreateTransactionBody message, length delimited. Does not implicitly {@link proto.ScheduleCreateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ScheduleCreateTransactionBody
         * @static
         * @param {proto.ScheduleCreateTransactionBody.$Properties} message ScheduleCreateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleCreateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ScheduleCreateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ScheduleCreateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ScheduleCreateTransactionBody & proto.ScheduleCreateTransactionBody.$Shape} ScheduleCreateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleCreateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ScheduleCreateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.scheduledTransactionBody = $root.proto.SchedulableTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduledTransactionBody);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.payerAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.payerAccountID);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.expirationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.waitForExpiry = value;
                        else
                            delete message.waitForExpiry;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ScheduleCreateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ScheduleCreateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ScheduleCreateTransactionBody & proto.ScheduleCreateTransactionBody.$Shape} ScheduleCreateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleCreateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScheduleCreateTransactionBody message.
         * @function verify
         * @memberof proto.ScheduleCreateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScheduleCreateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.scheduledTransactionBody != null && $Object.hasOwnProperty.call(message, "scheduledTransactionBody")) {
                let error = $root.proto.SchedulableTransactionBody.verify(message.scheduledTransactionBody, _depth + 1);
                if (error)
                    return "scheduledTransactionBody." + error;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.payerAccountID != null && $Object.hasOwnProperty.call(message, "payerAccountID")) {
                let error = $root.proto.AccountID.verify(message.payerAccountID, _depth + 1);
                if (error)
                    return "payerAccountID." + error;
            }
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                let error = $root.proto.Timestamp.verify(message.expirationTime, _depth + 1);
                if (error)
                    return "expirationTime." + error;
            }
            if (message.waitForExpiry != null && $Object.hasOwnProperty.call(message, "waitForExpiry"))
                if (typeof message.waitForExpiry !== "boolean")
                    return "waitForExpiry: boolean expected";
            return null;
        };

        /**
         * Creates a ScheduleCreateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ScheduleCreateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ScheduleCreateTransactionBody} ScheduleCreateTransactionBody
         */
        ScheduleCreateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ScheduleCreateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ScheduleCreateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ScheduleCreateTransactionBody();
            if (object.scheduledTransactionBody != null) {
                if (!$util.isObject(object.scheduledTransactionBody))
                    throw $TypeError(".proto.ScheduleCreateTransactionBody.scheduledTransactionBody: object expected");
                message.scheduledTransactionBody = $root.proto.SchedulableTransactionBody.fromObject(object.scheduledTransactionBody, _depth + 1);
            }
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.ScheduleCreateTransactionBody.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.payerAccountID != null) {
                if (!$util.isObject(object.payerAccountID))
                    throw $TypeError(".proto.ScheduleCreateTransactionBody.payerAccountID: object expected");
                message.payerAccountID = $root.proto.AccountID.fromObject(object.payerAccountID, _depth + 1);
            }
            if (object.expirationTime != null) {
                if (!$util.isObject(object.expirationTime))
                    throw $TypeError(".proto.ScheduleCreateTransactionBody.expirationTime: object expected");
                message.expirationTime = $root.proto.Timestamp.fromObject(object.expirationTime, _depth + 1);
            }
            if (object.waitForExpiry != null)
                if (object.waitForExpiry)
                    message.waitForExpiry = $Boolean(object.waitForExpiry);
            return message;
        };

        /**
         * Creates a plain object from a ScheduleCreateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ScheduleCreateTransactionBody
         * @static
         * @param {proto.ScheduleCreateTransactionBody} message ScheduleCreateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScheduleCreateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.scheduledTransactionBody = null;
                object.memo = "";
                object.adminKey = null;
                object.payerAccountID = null;
                object.expirationTime = null;
                object.waitForExpiry = false;
            }
            if (message.scheduledTransactionBody != null && $Object.hasOwnProperty.call(message, "scheduledTransactionBody"))
                object.scheduledTransactionBody = $root.proto.SchedulableTransactionBody.toObject(message.scheduledTransactionBody, options, _depth + 1);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.payerAccountID != null && $Object.hasOwnProperty.call(message, "payerAccountID"))
                object.payerAccountID = $root.proto.AccountID.toObject(message.payerAccountID, options, _depth + 1);
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                object.expirationTime = $root.proto.Timestamp.toObject(message.expirationTime, options, _depth + 1);
            if (message.waitForExpiry != null && $Object.hasOwnProperty.call(message, "waitForExpiry"))
                object.waitForExpiry = message.waitForExpiry;
            return object;
        };

        /**
         * Converts this ScheduleCreateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.ScheduleCreateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScheduleCreateTransactionBody.prototype.toJSON = function() {
            return ScheduleCreateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ScheduleCreateTransactionBody
         * @function getTypeUrl
         * @memberof proto.ScheduleCreateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ScheduleCreateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ScheduleCreateTransactionBody";
        };

        return ScheduleCreateTransactionBody;
    })();

    proto.SchedulableTransactionBody = (function() {

        /**
         * Properties of a SchedulableTransactionBody.
         * @typedef {Object} proto.SchedulableTransactionBody.$Properties
         * @property {number|Long|null} [transactionFee] A limit for the transaction fee the client is willing to pay.
         * <p>
         * The network SHALL NOT charge fees greater than this value.
         * @property {string|null} [memo] A short description of the schedulable transaction.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {proto.ContractCallTransactionBody.$Properties|null} [contractCall] Call a function defined on a smart contract.
         * @property {proto.ContractCreateTransactionBody.$Properties|null} [contractCreateInstance] Create a smart contract.
         * @property {proto.ContractUpdateTransactionBody.$Properties|null} [contractUpdateInstance] Update a smart contract.
         * @property {proto.ContractDeleteTransactionBody.$Properties|null} [contractDeleteInstance] Delete a smart contract and transfer remaining balance
         * to a specified account.
         * @property {proto.CryptoCreateTransactionBody.$Properties|null} [cryptoCreateAccount] Create a new Hedera account.
         * @property {proto.CryptoDeleteTransactionBody.$Properties|null} [cryptoDelete] Delete an Hedera account.<br/>
         * This will mark the account as deleted, and transfer all remaining
         * HBAR to a receiver account.
         * @property {proto.CryptoTransferTransactionBody.$Properties|null} [cryptoTransfer] Transfer HBAR between accounts.
         * @property {proto.CryptoUpdateTransactionBody.$Properties|null} [cryptoUpdateAccount] Modify an Hedera account.
         * @property {proto.FileAppendTransactionBody.$Properties|null} [fileAppend] Append data to the end of a file.
         * @property {proto.FileCreateTransactionBody.$Properties|null} [fileCreate] Create a new file.
         * @property {proto.FileDeleteTransactionBody.$Properties|null} [fileDelete] Delete a file.<br/>
         * This will remove the content of the file, and mark the file as
         * deleted.
         * @property {proto.FileUpdateTransactionBody.$Properties|null} [fileUpdate] Modify a file.<br/>
         * This may modify any metadata, and/or _replace_ the content.
         * @property {proto.SystemDeleteTransactionBody.$Properties|null} [systemDelete] Delete a file as an Hedera administrative function.<br/>
         * This is a privileged operation.
         * @property {proto.SystemUndeleteTransactionBody.$Properties|null} [systemUndelete] Restore a file deleted via `systemDelete`.<br/>
         * This is a privileged operation.
         * @property {proto.FreezeTransactionBody.$Properties|null} [freeze] Freeze the network.<br/>
         * This is actually several possible operations, and the caller
         * should examine the "freeze service" for more detail.<br/>
         * This is a privileged operation.
         * @property {proto.ConsensusCreateTopicTransactionBody.$Properties|null} [consensusCreateTopic] Create a topic.
         * @property {proto.ConsensusUpdateTopicTransactionBody.$Properties|null} [consensusUpdateTopic] Update a topic.
         * @property {proto.ConsensusDeleteTopicTransactionBody.$Properties|null} [consensusDeleteTopic] Delete a topic.
         * @property {proto.ConsensusSubmitMessageTransactionBody.$Properties|null} [consensusSubmitMessage] Submit a message to a topic.<br/>
         * A message may be "chunked", and submitted in parts, if the total
         * message size exceeds the limit for a single transaction.
         * @property {proto.TokenCreateTransactionBody.$Properties|null} [tokenCreation] Create a new Hedera token.
         * @property {proto.TokenFreezeAccountTransactionBody.$Properties|null} [tokenFreeze] Freeze an account with respect to a token.<br/>
         * A frozen account cannot transact in that token until unfrozen.
         * @property {proto.TokenUnfreezeAccountTransactionBody.$Properties|null} [tokenUnfreeze] Unfreeze an account with respect to a token.
         * @property {proto.TokenGrantKycTransactionBody.$Properties|null} [tokenGrantKyc] Grant KYC to an account with respect to a token.<br/>
         * KYC is generally a "know your customer" assertion that a
         * responsible entity has sufficient information to positively
         * identify the account holder to relevant authorities.
         * @property {proto.TokenRevokeKycTransactionBody.$Properties|null} [tokenRevokeKyc] Revoke KYC from an account with respect to a token.
         * @property {proto.TokenDeleteTransactionBody.$Properties|null} [tokenDeletion] Deletes an Hedera token.<br/>
         * The token will be marked deleted.
         * @property {proto.TokenUpdateTransactionBody.$Properties|null} [tokenUpdate] Update an Hedera token.<br/>
         * Depending on what fields are to be modified, the signature
         * requirements will vary. See `TokenUpdateTransactionBody` for
         * further detail.
         * @property {proto.TokenMintTransactionBody.$Properties|null} [tokenMint] Mint new tokens.<br/>
         * All minted tokens will be delivered to the treasury account for
         * the token type. The "mint key" for the token must sign this
         * transaction.
         * @property {proto.TokenBurnTransactionBody.$Properties|null} [tokenBurn] Burn tokens from the treasury account.<br/>
         * The "burn key" for the token must sign this transaction.
         * @property {proto.TokenWipeAccountTransactionBody.$Properties|null} [tokenWipe] Wipe tokens from an account.<br/>
         * This will remove a specified amount of fungible/common tokens or
         * a specified list of non-fungible/unique serial numbered tokens
         * of a given token type from an Hedera account. The removed tokens
         * are _burned_ as if by a `tokenBurn` transaction.<br/>
         * The "wipe key" for the token must sign this transaction.
         * @property {proto.TokenAssociateTransactionBody.$Properties|null} [tokenAssociate] Associate tokens to an account.
         * @property {proto.TokenDissociateTransactionBody.$Properties|null} [tokenDissociate] Dissociate tokens from an account.
         * @property {proto.ScheduleDeleteTransactionBody.$Properties|null} [scheduleDelete] Delete a schedule.<br/>
         * The schedule will be marked as deleted.
         * @property {proto.TokenPauseTransactionBody.$Properties|null} [tokenPause] Pause a Token.<br/>
         * This transaction must be signed by the "pause key" for the token.
         * @property {proto.TokenUnpauseTransactionBody.$Properties|null} [tokenUnpause] Unpause a Token.<br/>
         * This transaction must be signed by the "pause key" for the token.
         * @property {proto.CryptoApproveAllowanceTransactionBody.$Properties|null} [cryptoApproveAllowance] Add one or more approved allowances for spenders to transfer the
         * paying account's hbar or tokens.
         * @property {proto.CryptoDeleteAllowanceTransactionBody.$Properties|null} [cryptoDeleteAllowance] Delete one or more approvals for spenders to transfer the
         * paying account's hbar or tokens.
         * @property {proto.TokenFeeScheduleUpdateTransactionBody.$Properties|null} [tokenFeeScheduleUpdate] Update the custom fee schedule for a token.<br/>
         * This transaction must be signed by the "fee schedule key"
         * for the token.
         * @property {proto.UtilPrngTransactionBody.$Properties|null} [utilPrng] Provide a deterministic pseudorandom number based on network state.
         * @property {proto.TokenUpdateNftsTransactionBody.$Properties|null} [tokenUpdateNfts] Update one or more non-fungible/unique tokens.<br/>
         * This will update metadata for one or more serial numbers within
         * a collection (token type).
         * @property {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Properties|null} [nodeCreate] Create a new node in the network address book.<br/>
         * This is a privileged operation.
         * @property {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Properties|null} [nodeUpdate] Update a node in the network address book.<br/>
         * This is a privileged operation.
         * @property {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties|null} [nodeDelete] Delete a node from the network address book.<br/>
         * This will mark the node as deleted.<br/>
         * This is a privileged operation.
         * @property {proto.TokenRejectTransactionBody.$Properties|null} [tokenReject] "Reject" undesired tokens.<br/>
         * This transaction will transfer one or more tokens or token
         * balances held by the requesting account to the treasury
         * for each token type.
         * <p>
         * Each transfer MUST be one of the following:
         * <ul>
         * <li>A single non-fungible/unique token.</li>
         * <li>The full balance held for a fungible/common
         * token type.</li>
         * </ul>
         * When complete, the requesting account SHALL NOT hold the
         * rejected tokens.<br/>
         * Custom fees and royalties defined for the tokens rejected
         * SHALL NOT be charged for this transaction.
         * @property {proto.TokenCancelAirdropTransactionBody.$Properties|null} [tokenCancelAirdrop] Cancel an "airdrop".<br/>
         * This transaction cancels a pending airdrop for one or more
         * recipients.
         * <p>
         * The airdrop(s) to cancel MUST be pending, and not claimed.<br/>
         * @property {proto.TokenClaimAirdropTransactionBody.$Properties|null} [tokenClaimAirdrop] Claim an "airdrop".
         * This transaction "claims" one or more pending "airdrops".
         * <p>
         * The airdrop(s) to claim MUST be pending, and not
         * already claimed.<br/>
         * @property {proto.TokenAirdropTransactionBody.$Properties|null} [tokenAirdrop] Send an "airdrop" of tokens to one or more recipients.
         * <p>
         * This transaction unilaterally "gifts" tokens by transferring them
         * from a "sender" account to the "recipient" account(s). If any
         * recipient is not already associated to the token to airdrop, or
         * has set a "reciever signature required" flag, then that recipient
         * is recorded as a "pending" airdrop which must be "claimed".  All
         * other recipients receive the "airdropped" tokens immediately.
         * @property {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Properties|null} [registeredNodeCreate] Create a new registered node in the network address book.
         * @property {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Properties|null} [registeredNodeUpdate] Update a registered node in the network address book.
         * @property {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties|null} [registeredNodeDelete] Delete a registered node from the network address book.
         * @property {Array.<proto.CustomFeeLimit.$Properties>|null} [maxCustomFees] A list of maximum custom fees that the users are willing to pay.
         * <p>
         * This field is OPTIONAL.<br/>
         * If left empty, the users are accepting to pay any custom fee.<br/>
         * If used with a transaction type that does not support custom fee limits, the transaction will fail.
         * @property {"contractCall"|"contractCreateInstance"|"contractUpdateInstance"|"contractDeleteInstance"|"cryptoCreateAccount"|"cryptoDelete"|"cryptoTransfer"|"cryptoUpdateAccount"|"fileAppend"|"fileCreate"|"fileDelete"|"fileUpdate"|"systemDelete"|"systemUndelete"|"freeze"|"consensusCreateTopic"|"consensusUpdateTopic"|"consensusDeleteTopic"|"consensusSubmitMessage"|"tokenCreation"|"tokenFreeze"|"tokenUnfreeze"|"tokenGrantKyc"|"tokenRevokeKyc"|"tokenDeletion"|"tokenUpdate"|"tokenMint"|"tokenBurn"|"tokenWipe"|"tokenAssociate"|"tokenDissociate"|"scheduleDelete"|"tokenPause"|"tokenUnpause"|"cryptoApproveAllowance"|"cryptoDeleteAllowance"|"tokenFeeScheduleUpdate"|"utilPrng"|"tokenUpdateNfts"|"nodeCreate"|"nodeUpdate"|"nodeDelete"|"tokenReject"|"tokenCancelAirdrop"|"tokenClaimAirdrop"|"tokenAirdrop"|"registeredNodeCreate"|"registeredNodeUpdate"|"registeredNodeDelete"} [data] SchedulableTransactionBody data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SchedulableTransactionBody.
         * @memberof proto
         * @interface ISchedulableTransactionBody
         * @augments proto.SchedulableTransactionBody.$Properties
         * @deprecated Use proto.SchedulableTransactionBody.$Properties instead.
         */

        /**
         * Narrowed shape of a SchedulableTransactionBody.
         * @typedef {{
         *   transactionFee?: number|Long|null;
         *   memo?: string|null;
         *   contractCall?: proto.ContractCallTransactionBody.$Shape|null;
         *   contractCreateInstance?: proto.ContractCreateTransactionBody.$Shape|null;
         *   contractUpdateInstance?: proto.ContractUpdateTransactionBody.$Shape|null;
         *   contractDeleteInstance?: proto.ContractDeleteTransactionBody.$Shape|null;
         *   cryptoCreateAccount?: proto.CryptoCreateTransactionBody.$Shape|null;
         *   cryptoDelete?: proto.CryptoDeleteTransactionBody.$Shape|null;
         *   cryptoTransfer?: proto.CryptoTransferTransactionBody.$Shape|null;
         *   cryptoUpdateAccount?: proto.CryptoUpdateTransactionBody.$Shape|null;
         *   fileAppend?: proto.FileAppendTransactionBody.$Shape|null;
         *   fileCreate?: proto.FileCreateTransactionBody.$Shape|null;
         *   fileDelete?: proto.FileDeleteTransactionBody.$Shape|null;
         *   fileUpdate?: proto.FileUpdateTransactionBody.$Shape|null;
         *   systemDelete?: proto.SystemDeleteTransactionBody.$Shape|null;
         *   systemUndelete?: proto.SystemUndeleteTransactionBody.$Shape|null;
         *   freeze?: proto.FreezeTransactionBody.$Shape|null;
         *   consensusCreateTopic?: proto.ConsensusCreateTopicTransactionBody.$Shape|null;
         *   consensusUpdateTopic?: proto.ConsensusUpdateTopicTransactionBody.$Shape|null;
         *   consensusDeleteTopic?: proto.ConsensusDeleteTopicTransactionBody.$Shape|null;
         *   consensusSubmitMessage?: proto.ConsensusSubmitMessageTransactionBody.$Shape|null;
         *   tokenCreation?: proto.TokenCreateTransactionBody.$Shape|null;
         *   tokenFreeze?: proto.TokenFreezeAccountTransactionBody.$Shape|null;
         *   tokenUnfreeze?: proto.TokenUnfreezeAccountTransactionBody.$Shape|null;
         *   tokenGrantKyc?: proto.TokenGrantKycTransactionBody.$Shape|null;
         *   tokenRevokeKyc?: proto.TokenRevokeKycTransactionBody.$Shape|null;
         *   tokenDeletion?: proto.TokenDeleteTransactionBody.$Shape|null;
         *   tokenUpdate?: proto.TokenUpdateTransactionBody.$Shape|null;
         *   tokenMint?: proto.TokenMintTransactionBody.$Shape|null;
         *   tokenBurn?: proto.TokenBurnTransactionBody.$Shape|null;
         *   tokenWipe?: proto.TokenWipeAccountTransactionBody.$Shape|null;
         *   tokenAssociate?: proto.TokenAssociateTransactionBody.$Shape|null;
         *   tokenDissociate?: proto.TokenDissociateTransactionBody.$Shape|null;
         *   scheduleDelete?: proto.ScheduleDeleteTransactionBody.$Shape|null;
         *   tokenPause?: proto.TokenPauseTransactionBody.$Shape|null;
         *   tokenUnpause?: proto.TokenUnpauseTransactionBody.$Shape|null;
         *   cryptoApproveAllowance?: proto.CryptoApproveAllowanceTransactionBody.$Shape|null;
         *   cryptoDeleteAllowance?: proto.CryptoDeleteAllowanceTransactionBody.$Shape|null;
         *   tokenFeeScheduleUpdate?: proto.TokenFeeScheduleUpdateTransactionBody.$Shape|null;
         *   utilPrng?: proto.UtilPrngTransactionBody.$Shape|null;
         *   tokenUpdateNfts?: proto.TokenUpdateNftsTransactionBody.$Shape|null;
         *   nodeCreate?: com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Shape|null;
         *   nodeUpdate?: com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Shape|null;
         *   nodeDelete?: com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Shape|null;
         *   tokenReject?: proto.TokenRejectTransactionBody.$Shape|null;
         *   tokenCancelAirdrop?: proto.TokenCancelAirdropTransactionBody.$Shape|null;
         *   tokenClaimAirdrop?: proto.TokenClaimAirdropTransactionBody.$Shape|null;
         *   tokenAirdrop?: proto.TokenAirdropTransactionBody.$Shape|null;
         *   registeredNodeCreate?: com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Shape|null;
         *   registeredNodeUpdate?: com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Shape|null;
         *   registeredNodeDelete?: com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Shape|null;
         *   maxCustomFees?: Array.<proto.CustomFeeLimit.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ data?: undefined; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "contractCall"; contractCall: proto.ContractCallTransactionBody.$Shape; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "contractCreateInstance"; contractCall?: null; contractCreateInstance: proto.ContractCreateTransactionBody.$Shape; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "contractUpdateInstance"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance: proto.ContractUpdateTransactionBody.$Shape; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "contractDeleteInstance"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance: proto.ContractDeleteTransactionBody.$Shape; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "cryptoCreateAccount"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount: proto.CryptoCreateTransactionBody.$Shape; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "cryptoDelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete: proto.CryptoDeleteTransactionBody.$Shape; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "cryptoTransfer"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer: proto.CryptoTransferTransactionBody.$Shape; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "cryptoUpdateAccount"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount: proto.CryptoUpdateTransactionBody.$Shape; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "fileAppend"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend: proto.FileAppendTransactionBody.$Shape; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "fileCreate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate: proto.FileCreateTransactionBody.$Shape; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "fileDelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete: proto.FileDeleteTransactionBody.$Shape; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "fileUpdate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate: proto.FileUpdateTransactionBody.$Shape; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "systemDelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete: proto.SystemDeleteTransactionBody.$Shape; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "systemUndelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete: proto.SystemUndeleteTransactionBody.$Shape; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "freeze"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze: proto.FreezeTransactionBody.$Shape; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "consensusCreateTopic"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic: proto.ConsensusCreateTopicTransactionBody.$Shape; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "consensusUpdateTopic"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic: proto.ConsensusUpdateTopicTransactionBody.$Shape; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "consensusDeleteTopic"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic: proto.ConsensusDeleteTopicTransactionBody.$Shape; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "consensusSubmitMessage"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage: proto.ConsensusSubmitMessageTransactionBody.$Shape; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenCreation"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation: proto.TokenCreateTransactionBody.$Shape; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenFreeze"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze: proto.TokenFreezeAccountTransactionBody.$Shape; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenUnfreeze"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze: proto.TokenUnfreezeAccountTransactionBody.$Shape; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenGrantKyc"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc: proto.TokenGrantKycTransactionBody.$Shape; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenRevokeKyc"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc: proto.TokenRevokeKycTransactionBody.$Shape; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenDeletion"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion: proto.TokenDeleteTransactionBody.$Shape; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenUpdate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate: proto.TokenUpdateTransactionBody.$Shape; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenMint"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint: proto.TokenMintTransactionBody.$Shape; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenBurn"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn: proto.TokenBurnTransactionBody.$Shape; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenWipe"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe: proto.TokenWipeAccountTransactionBody.$Shape; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenAssociate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate: proto.TokenAssociateTransactionBody.$Shape; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenDissociate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate: proto.TokenDissociateTransactionBody.$Shape; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "scheduleDelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete: proto.ScheduleDeleteTransactionBody.$Shape; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenPause"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause: proto.TokenPauseTransactionBody.$Shape; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenUnpause"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause: proto.TokenUnpauseTransactionBody.$Shape; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "cryptoApproveAllowance"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance: proto.CryptoApproveAllowanceTransactionBody.$Shape; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "cryptoDeleteAllowance"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance: proto.CryptoDeleteAllowanceTransactionBody.$Shape; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenFeeScheduleUpdate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate: proto.TokenFeeScheduleUpdateTransactionBody.$Shape; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "utilPrng"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng: proto.UtilPrngTransactionBody.$Shape; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenUpdateNfts"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts: proto.TokenUpdateNftsTransactionBody.$Shape; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "nodeCreate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate: com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Shape; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "nodeUpdate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate: com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Shape; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "nodeDelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete: com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Shape; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenReject"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject: proto.TokenRejectTransactionBody.$Shape; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenCancelAirdrop"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop: proto.TokenCancelAirdropTransactionBody.$Shape; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenClaimAirdrop"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop: proto.TokenClaimAirdropTransactionBody.$Shape; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "tokenAirdrop"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop: proto.TokenAirdropTransactionBody.$Shape; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "registeredNodeCreate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate: com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Shape; registeredNodeUpdate?: null; registeredNodeDelete?: null }|{ data?: "registeredNodeUpdate"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate: com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Shape; registeredNodeDelete?: null }|{ data?: "registeredNodeDelete"; contractCall?: null; contractCreateInstance?: null; contractUpdateInstance?: null; contractDeleteInstance?: null; cryptoCreateAccount?: null; cryptoDelete?: null; cryptoTransfer?: null; cryptoUpdateAccount?: null; fileAppend?: null; fileCreate?: null; fileDelete?: null; fileUpdate?: null; systemDelete?: null; systemUndelete?: null; freeze?: null; consensusCreateTopic?: null; consensusUpdateTopic?: null; consensusDeleteTopic?: null; consensusSubmitMessage?: null; tokenCreation?: null; tokenFreeze?: null; tokenUnfreeze?: null; tokenGrantKyc?: null; tokenRevokeKyc?: null; tokenDeletion?: null; tokenUpdate?: null; tokenMint?: null; tokenBurn?: null; tokenWipe?: null; tokenAssociate?: null; tokenDissociate?: null; scheduleDelete?: null; tokenPause?: null; tokenUnpause?: null; cryptoApproveAllowance?: null; cryptoDeleteAllowance?: null; tokenFeeScheduleUpdate?: null; utilPrng?: null; tokenUpdateNfts?: null; nodeCreate?: null; nodeUpdate?: null; nodeDelete?: null; tokenReject?: null; tokenCancelAirdrop?: null; tokenClaimAirdrop?: null; tokenAirdrop?: null; registeredNodeCreate?: null; registeredNodeUpdate?: null; registeredNodeDelete: com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Shape })
         * )} proto.SchedulableTransactionBody.$Shape
         */

        /**
         * Constructs a new SchedulableTransactionBody.
         * @memberof proto
         * @classdesc A schedulable transaction.
         * 
         * The network configuration `scheduling.whitelist` limits which of these
         * transaction types may actually be scheduled. As of version `0.50.0` of the
         * consensus node software this list contains only `CryptoTransfer`,
         * `ConsensusSubmitMessage`, `TokenBurn`, `TokenMint`,
         * and `CryptoApproveAllowance`.
         * @constructor
         * @param {proto.SchedulableTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SchedulableTransactionBody = function (properties) {
            this.maxCustomFees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A limit for the transaction fee the client is willing to pay.
         * <p>
         * The network SHALL NOT charge fees greater than this value.
         * @member {number|Long} transactionFee
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.transactionFee = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A short description of the schedulable transaction.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.memo = "";

        /**
         * Call a function defined on a smart contract.
         * @member {proto.ContractCallTransactionBody.$Properties|null|undefined} contractCall
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.contractCall = null;

        /**
         * Create a smart contract.
         * @member {proto.ContractCreateTransactionBody.$Properties|null|undefined} contractCreateInstance
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.contractCreateInstance = null;

        /**
         * Update a smart contract.
         * @member {proto.ContractUpdateTransactionBody.$Properties|null|undefined} contractUpdateInstance
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.contractUpdateInstance = null;

        /**
         * Delete a smart contract and transfer remaining balance
         * to a specified account.
         * @member {proto.ContractDeleteTransactionBody.$Properties|null|undefined} contractDeleteInstance
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.contractDeleteInstance = null;

        /**
         * Create a new Hedera account.
         * @member {proto.CryptoCreateTransactionBody.$Properties|null|undefined} cryptoCreateAccount
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.cryptoCreateAccount = null;

        /**
         * Delete an Hedera account.<br/>
         * This will mark the account as deleted, and transfer all remaining
         * HBAR to a receiver account.
         * @member {proto.CryptoDeleteTransactionBody.$Properties|null|undefined} cryptoDelete
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.cryptoDelete = null;

        /**
         * Transfer HBAR between accounts.
         * @member {proto.CryptoTransferTransactionBody.$Properties|null|undefined} cryptoTransfer
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.cryptoTransfer = null;

        /**
         * Modify an Hedera account.
         * @member {proto.CryptoUpdateTransactionBody.$Properties|null|undefined} cryptoUpdateAccount
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.cryptoUpdateAccount = null;

        /**
         * Append data to the end of a file.
         * @member {proto.FileAppendTransactionBody.$Properties|null|undefined} fileAppend
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.fileAppend = null;

        /**
         * Create a new file.
         * @member {proto.FileCreateTransactionBody.$Properties|null|undefined} fileCreate
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.fileCreate = null;

        /**
         * Delete a file.<br/>
         * This will remove the content of the file, and mark the file as
         * deleted.
         * @member {proto.FileDeleteTransactionBody.$Properties|null|undefined} fileDelete
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.fileDelete = null;

        /**
         * Modify a file.<br/>
         * This may modify any metadata, and/or _replace_ the content.
         * @member {proto.FileUpdateTransactionBody.$Properties|null|undefined} fileUpdate
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.fileUpdate = null;

        /**
         * Delete a file as an Hedera administrative function.<br/>
         * This is a privileged operation.
         * @member {proto.SystemDeleteTransactionBody.$Properties|null|undefined} systemDelete
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.systemDelete = null;

        /**
         * Restore a file deleted via `systemDelete`.<br/>
         * This is a privileged operation.
         * @member {proto.SystemUndeleteTransactionBody.$Properties|null|undefined} systemUndelete
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.systemUndelete = null;

        /**
         * Freeze the network.<br/>
         * This is actually several possible operations, and the caller
         * should examine the "freeze service" for more detail.<br/>
         * This is a privileged operation.
         * @member {proto.FreezeTransactionBody.$Properties|null|undefined} freeze
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.freeze = null;

        /**
         * Create a topic.
         * @member {proto.ConsensusCreateTopicTransactionBody.$Properties|null|undefined} consensusCreateTopic
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.consensusCreateTopic = null;

        /**
         * Update a topic.
         * @member {proto.ConsensusUpdateTopicTransactionBody.$Properties|null|undefined} consensusUpdateTopic
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.consensusUpdateTopic = null;

        /**
         * Delete a topic.
         * @member {proto.ConsensusDeleteTopicTransactionBody.$Properties|null|undefined} consensusDeleteTopic
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.consensusDeleteTopic = null;

        /**
         * Submit a message to a topic.<br/>
         * A message may be "chunked", and submitted in parts, if the total
         * message size exceeds the limit for a single transaction.
         * @member {proto.ConsensusSubmitMessageTransactionBody.$Properties|null|undefined} consensusSubmitMessage
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.consensusSubmitMessage = null;

        /**
         * Create a new Hedera token.
         * @member {proto.TokenCreateTransactionBody.$Properties|null|undefined} tokenCreation
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenCreation = null;

        /**
         * Freeze an account with respect to a token.<br/>
         * A frozen account cannot transact in that token until unfrozen.
         * @member {proto.TokenFreezeAccountTransactionBody.$Properties|null|undefined} tokenFreeze
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenFreeze = null;

        /**
         * Unfreeze an account with respect to a token.
         * @member {proto.TokenUnfreezeAccountTransactionBody.$Properties|null|undefined} tokenUnfreeze
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenUnfreeze = null;

        /**
         * Grant KYC to an account with respect to a token.<br/>
         * KYC is generally a "know your customer" assertion that a
         * responsible entity has sufficient information to positively
         * identify the account holder to relevant authorities.
         * @member {proto.TokenGrantKycTransactionBody.$Properties|null|undefined} tokenGrantKyc
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenGrantKyc = null;

        /**
         * Revoke KYC from an account with respect to a token.
         * @member {proto.TokenRevokeKycTransactionBody.$Properties|null|undefined} tokenRevokeKyc
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenRevokeKyc = null;

        /**
         * Deletes an Hedera token.<br/>
         * The token will be marked deleted.
         * @member {proto.TokenDeleteTransactionBody.$Properties|null|undefined} tokenDeletion
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenDeletion = null;

        /**
         * Update an Hedera token.<br/>
         * Depending on what fields are to be modified, the signature
         * requirements will vary. See `TokenUpdateTransactionBody` for
         * further detail.
         * @member {proto.TokenUpdateTransactionBody.$Properties|null|undefined} tokenUpdate
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenUpdate = null;

        /**
         * Mint new tokens.<br/>
         * All minted tokens will be delivered to the treasury account for
         * the token type. The "mint key" for the token must sign this
         * transaction.
         * @member {proto.TokenMintTransactionBody.$Properties|null|undefined} tokenMint
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenMint = null;

        /**
         * Burn tokens from the treasury account.<br/>
         * The "burn key" for the token must sign this transaction.
         * @member {proto.TokenBurnTransactionBody.$Properties|null|undefined} tokenBurn
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenBurn = null;

        /**
         * Wipe tokens from an account.<br/>
         * This will remove a specified amount of fungible/common tokens or
         * a specified list of non-fungible/unique serial numbered tokens
         * of a given token type from an Hedera account. The removed tokens
         * are _burned_ as if by a `tokenBurn` transaction.<br/>
         * The "wipe key" for the token must sign this transaction.
         * @member {proto.TokenWipeAccountTransactionBody.$Properties|null|undefined} tokenWipe
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenWipe = null;

        /**
         * Associate tokens to an account.
         * @member {proto.TokenAssociateTransactionBody.$Properties|null|undefined} tokenAssociate
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenAssociate = null;

        /**
         * Dissociate tokens from an account.
         * @member {proto.TokenDissociateTransactionBody.$Properties|null|undefined} tokenDissociate
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenDissociate = null;

        /**
         * Delete a schedule.<br/>
         * The schedule will be marked as deleted.
         * @member {proto.ScheduleDeleteTransactionBody.$Properties|null|undefined} scheduleDelete
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.scheduleDelete = null;

        /**
         * Pause a Token.<br/>
         * This transaction must be signed by the "pause key" for the token.
         * @member {proto.TokenPauseTransactionBody.$Properties|null|undefined} tokenPause
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenPause = null;

        /**
         * Unpause a Token.<br/>
         * This transaction must be signed by the "pause key" for the token.
         * @member {proto.TokenUnpauseTransactionBody.$Properties|null|undefined} tokenUnpause
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenUnpause = null;

        /**
         * Add one or more approved allowances for spenders to transfer the
         * paying account's hbar or tokens.
         * @member {proto.CryptoApproveAllowanceTransactionBody.$Properties|null|undefined} cryptoApproveAllowance
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.cryptoApproveAllowance = null;

        /**
         * Delete one or more approvals for spenders to transfer the
         * paying account's hbar or tokens.
         * @member {proto.CryptoDeleteAllowanceTransactionBody.$Properties|null|undefined} cryptoDeleteAllowance
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.cryptoDeleteAllowance = null;

        /**
         * Update the custom fee schedule for a token.<br/>
         * This transaction must be signed by the "fee schedule key"
         * for the token.
         * @member {proto.TokenFeeScheduleUpdateTransactionBody.$Properties|null|undefined} tokenFeeScheduleUpdate
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenFeeScheduleUpdate = null;

        /**
         * Provide a deterministic pseudorandom number based on network state.
         * @member {proto.UtilPrngTransactionBody.$Properties|null|undefined} utilPrng
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.utilPrng = null;

        /**
         * Update one or more non-fungible/unique tokens.<br/>
         * This will update metadata for one or more serial numbers within
         * a collection (token type).
         * @member {proto.TokenUpdateNftsTransactionBody.$Properties|null|undefined} tokenUpdateNfts
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenUpdateNfts = null;

        /**
         * Create a new node in the network address book.<br/>
         * This is a privileged operation.
         * @member {com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.$Properties|null|undefined} nodeCreate
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.nodeCreate = null;

        /**
         * Update a node in the network address book.<br/>
         * This is a privileged operation.
         * @member {com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.$Properties|null|undefined} nodeUpdate
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.nodeUpdate = null;

        /**
         * Delete a node from the network address book.<br/>
         * This will mark the node as deleted.<br/>
         * This is a privileged operation.
         * @member {com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.$Properties|null|undefined} nodeDelete
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.nodeDelete = null;

        /**
         * "Reject" undesired tokens.<br/>
         * This transaction will transfer one or more tokens or token
         * balances held by the requesting account to the treasury
         * for each token type.
         * <p>
         * Each transfer MUST be one of the following:
         * <ul>
         * <li>A single non-fungible/unique token.</li>
         * <li>The full balance held for a fungible/common
         * token type.</li>
         * </ul>
         * When complete, the requesting account SHALL NOT hold the
         * rejected tokens.<br/>
         * Custom fees and royalties defined for the tokens rejected
         * SHALL NOT be charged for this transaction.
         * @member {proto.TokenRejectTransactionBody.$Properties|null|undefined} tokenReject
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenReject = null;

        /**
         * Cancel an "airdrop".<br/>
         * This transaction cancels a pending airdrop for one or more
         * recipients.
         * <p>
         * The airdrop(s) to cancel MUST be pending, and not claimed.<br/>
         * @member {proto.TokenCancelAirdropTransactionBody.$Properties|null|undefined} tokenCancelAirdrop
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenCancelAirdrop = null;

        /**
         * Claim an "airdrop".
         * This transaction "claims" one or more pending "airdrops".
         * <p>
         * The airdrop(s) to claim MUST be pending, and not
         * already claimed.<br/>
         * @member {proto.TokenClaimAirdropTransactionBody.$Properties|null|undefined} tokenClaimAirdrop
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenClaimAirdrop = null;

        /**
         * Send an "airdrop" of tokens to one or more recipients.
         * <p>
         * This transaction unilaterally "gifts" tokens by transferring them
         * from a "sender" account to the "recipient" account(s). If any
         * recipient is not already associated to the token to airdrop, or
         * has set a "reciever signature required" flag, then that recipient
         * is recorded as a "pending" airdrop which must be "claimed".  All
         * other recipients receive the "airdropped" tokens immediately.
         * @member {proto.TokenAirdropTransactionBody.$Properties|null|undefined} tokenAirdrop
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.tokenAirdrop = null;

        /**
         * Create a new registered node in the network address book.
         * @member {com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.$Properties|null|undefined} registeredNodeCreate
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.registeredNodeCreate = null;

        /**
         * Update a registered node in the network address book.
         * @member {com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.$Properties|null|undefined} registeredNodeUpdate
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.registeredNodeUpdate = null;

        /**
         * Delete a registered node from the network address book.
         * @member {com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.$Properties|null|undefined} registeredNodeDelete
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.registeredNodeDelete = null;

        /**
         * A list of maximum custom fees that the users are willing to pay.
         * <p>
         * This field is OPTIONAL.<br/>
         * If left empty, the users are accepting to pay any custom fee.<br/>
         * If used with a transaction type that does not support custom fee limits, the transaction will fail.
         * @member {Array.<proto.CustomFeeLimit.$Properties>} maxCustomFees
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        SchedulableTransactionBody.prototype.maxCustomFees = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * SchedulableTransactionBody data.
         * @member {"contractCall"|"contractCreateInstance"|"contractUpdateInstance"|"contractDeleteInstance"|"cryptoCreateAccount"|"cryptoDelete"|"cryptoTransfer"|"cryptoUpdateAccount"|"fileAppend"|"fileCreate"|"fileDelete"|"fileUpdate"|"systemDelete"|"systemUndelete"|"freeze"|"consensusCreateTopic"|"consensusUpdateTopic"|"consensusDeleteTopic"|"consensusSubmitMessage"|"tokenCreation"|"tokenFreeze"|"tokenUnfreeze"|"tokenGrantKyc"|"tokenRevokeKyc"|"tokenDeletion"|"tokenUpdate"|"tokenMint"|"tokenBurn"|"tokenWipe"|"tokenAssociate"|"tokenDissociate"|"scheduleDelete"|"tokenPause"|"tokenUnpause"|"cryptoApproveAllowance"|"cryptoDeleteAllowance"|"tokenFeeScheduleUpdate"|"utilPrng"|"tokenUpdateNfts"|"nodeCreate"|"nodeUpdate"|"nodeDelete"|"tokenReject"|"tokenCancelAirdrop"|"tokenClaimAirdrop"|"tokenAirdrop"|"registeredNodeCreate"|"registeredNodeUpdate"|"registeredNodeDelete"|undefined} data
         * @memberof proto.SchedulableTransactionBody
         * @instance
         */
        $Object.defineProperty(SchedulableTransactionBody.prototype, "data", {
            get: $util.oneOfGetter($oneOfFields = ["contractCall", "contractCreateInstance", "contractUpdateInstance", "contractDeleteInstance", "cryptoCreateAccount", "cryptoDelete", "cryptoTransfer", "cryptoUpdateAccount", "fileAppend", "fileCreate", "fileDelete", "fileUpdate", "systemDelete", "systemUndelete", "freeze", "consensusCreateTopic", "consensusUpdateTopic", "consensusDeleteTopic", "consensusSubmitMessage", "tokenCreation", "tokenFreeze", "tokenUnfreeze", "tokenGrantKyc", "tokenRevokeKyc", "tokenDeletion", "tokenUpdate", "tokenMint", "tokenBurn", "tokenWipe", "tokenAssociate", "tokenDissociate", "scheduleDelete", "tokenPause", "tokenUnpause", "cryptoApproveAllowance", "cryptoDeleteAllowance", "tokenFeeScheduleUpdate", "utilPrng", "tokenUpdateNfts", "nodeCreate", "nodeUpdate", "nodeDelete", "tokenReject", "tokenCancelAirdrop", "tokenClaimAirdrop", "tokenAirdrop", "registeredNodeCreate", "registeredNodeUpdate", "registeredNodeDelete"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SchedulableTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.SchedulableTransactionBody
         * @static
         * @param {proto.SchedulableTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.SchedulableTransactionBody} SchedulableTransactionBody instance
         * @type {{
         *   (properties: proto.SchedulableTransactionBody.$Shape): proto.SchedulableTransactionBody & proto.SchedulableTransactionBody.$Shape;
         *   (properties?: proto.SchedulableTransactionBody.$Properties): proto.SchedulableTransactionBody;
         * }}
         */
        SchedulableTransactionBody.create = function(properties) {
            return new SchedulableTransactionBody(properties);
        };

        /**
         * Encodes the specified SchedulableTransactionBody message. Does not implicitly {@link proto.SchedulableTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.SchedulableTransactionBody
         * @static
         * @param {proto.SchedulableTransactionBody.$Properties} message SchedulableTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SchedulableTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.transactionFee != null && $Object.hasOwnProperty.call(message, "transactionFee") && (typeof message.transactionFee === "object" ? message.transactionFee.low || message.transactionFee.high : message.transactionFee !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.transactionFee);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.memo);
            if (message.contractCall != null && $Object.hasOwnProperty.call(message, "contractCall"))
                $root.proto.ContractCallTransactionBody.encode(message.contractCall, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.contractCreateInstance != null && $Object.hasOwnProperty.call(message, "contractCreateInstance"))
                $root.proto.ContractCreateTransactionBody.encode(message.contractCreateInstance, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.contractUpdateInstance != null && $Object.hasOwnProperty.call(message, "contractUpdateInstance"))
                $root.proto.ContractUpdateTransactionBody.encode(message.contractUpdateInstance, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.contractDeleteInstance != null && $Object.hasOwnProperty.call(message, "contractDeleteInstance"))
                $root.proto.ContractDeleteTransactionBody.encode(message.contractDeleteInstance, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.cryptoCreateAccount != null && $Object.hasOwnProperty.call(message, "cryptoCreateAccount"))
                $root.proto.CryptoCreateTransactionBody.encode(message.cryptoCreateAccount, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.cryptoDelete != null && $Object.hasOwnProperty.call(message, "cryptoDelete"))
                $root.proto.CryptoDeleteTransactionBody.encode(message.cryptoDelete, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.cryptoTransfer != null && $Object.hasOwnProperty.call(message, "cryptoTransfer"))
                $root.proto.CryptoTransferTransactionBody.encode(message.cryptoTransfer, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.cryptoUpdateAccount != null && $Object.hasOwnProperty.call(message, "cryptoUpdateAccount"))
                $root.proto.CryptoUpdateTransactionBody.encode(message.cryptoUpdateAccount, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.fileAppend != null && $Object.hasOwnProperty.call(message, "fileAppend"))
                $root.proto.FileAppendTransactionBody.encode(message.fileAppend, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.fileCreate != null && $Object.hasOwnProperty.call(message, "fileCreate"))
                $root.proto.FileCreateTransactionBody.encode(message.fileCreate, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.fileDelete != null && $Object.hasOwnProperty.call(message, "fileDelete"))
                $root.proto.FileDeleteTransactionBody.encode(message.fileDelete, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.fileUpdate != null && $Object.hasOwnProperty.call(message, "fileUpdate"))
                $root.proto.FileUpdateTransactionBody.encode(message.fileUpdate, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.systemDelete != null && $Object.hasOwnProperty.call(message, "systemDelete"))
                $root.proto.SystemDeleteTransactionBody.encode(message.systemDelete, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.systemUndelete != null && $Object.hasOwnProperty.call(message, "systemUndelete"))
                $root.proto.SystemUndeleteTransactionBody.encode(message.systemUndelete, writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.freeze != null && $Object.hasOwnProperty.call(message, "freeze"))
                $root.proto.FreezeTransactionBody.encode(message.freeze, writer.uint32(/* id 17, wireType 2 =*/138).fork(), _depth + 1).ldelim();
            if (message.consensusCreateTopic != null && $Object.hasOwnProperty.call(message, "consensusCreateTopic"))
                $root.proto.ConsensusCreateTopicTransactionBody.encode(message.consensusCreateTopic, writer.uint32(/* id 18, wireType 2 =*/146).fork(), _depth + 1).ldelim();
            if (message.consensusUpdateTopic != null && $Object.hasOwnProperty.call(message, "consensusUpdateTopic"))
                $root.proto.ConsensusUpdateTopicTransactionBody.encode(message.consensusUpdateTopic, writer.uint32(/* id 19, wireType 2 =*/154).fork(), _depth + 1).ldelim();
            if (message.consensusDeleteTopic != null && $Object.hasOwnProperty.call(message, "consensusDeleteTopic"))
                $root.proto.ConsensusDeleteTopicTransactionBody.encode(message.consensusDeleteTopic, writer.uint32(/* id 20, wireType 2 =*/162).fork(), _depth + 1).ldelim();
            if (message.consensusSubmitMessage != null && $Object.hasOwnProperty.call(message, "consensusSubmitMessage"))
                $root.proto.ConsensusSubmitMessageTransactionBody.encode(message.consensusSubmitMessage, writer.uint32(/* id 21, wireType 2 =*/170).fork(), _depth + 1).ldelim();
            if (message.tokenCreation != null && $Object.hasOwnProperty.call(message, "tokenCreation"))
                $root.proto.TokenCreateTransactionBody.encode(message.tokenCreation, writer.uint32(/* id 22, wireType 2 =*/178).fork(), _depth + 1).ldelim();
            if (message.tokenFreeze != null && $Object.hasOwnProperty.call(message, "tokenFreeze"))
                $root.proto.TokenFreezeAccountTransactionBody.encode(message.tokenFreeze, writer.uint32(/* id 23, wireType 2 =*/186).fork(), _depth + 1).ldelim();
            if (message.tokenUnfreeze != null && $Object.hasOwnProperty.call(message, "tokenUnfreeze"))
                $root.proto.TokenUnfreezeAccountTransactionBody.encode(message.tokenUnfreeze, writer.uint32(/* id 24, wireType 2 =*/194).fork(), _depth + 1).ldelim();
            if (message.tokenGrantKyc != null && $Object.hasOwnProperty.call(message, "tokenGrantKyc"))
                $root.proto.TokenGrantKycTransactionBody.encode(message.tokenGrantKyc, writer.uint32(/* id 25, wireType 2 =*/202).fork(), _depth + 1).ldelim();
            if (message.tokenRevokeKyc != null && $Object.hasOwnProperty.call(message, "tokenRevokeKyc"))
                $root.proto.TokenRevokeKycTransactionBody.encode(message.tokenRevokeKyc, writer.uint32(/* id 26, wireType 2 =*/210).fork(), _depth + 1).ldelim();
            if (message.tokenDeletion != null && $Object.hasOwnProperty.call(message, "tokenDeletion"))
                $root.proto.TokenDeleteTransactionBody.encode(message.tokenDeletion, writer.uint32(/* id 27, wireType 2 =*/218).fork(), _depth + 1).ldelim();
            if (message.tokenUpdate != null && $Object.hasOwnProperty.call(message, "tokenUpdate"))
                $root.proto.TokenUpdateTransactionBody.encode(message.tokenUpdate, writer.uint32(/* id 28, wireType 2 =*/226).fork(), _depth + 1).ldelim();
            if (message.tokenMint != null && $Object.hasOwnProperty.call(message, "tokenMint"))
                $root.proto.TokenMintTransactionBody.encode(message.tokenMint, writer.uint32(/* id 29, wireType 2 =*/234).fork(), _depth + 1).ldelim();
            if (message.tokenBurn != null && $Object.hasOwnProperty.call(message, "tokenBurn"))
                $root.proto.TokenBurnTransactionBody.encode(message.tokenBurn, writer.uint32(/* id 30, wireType 2 =*/242).fork(), _depth + 1).ldelim();
            if (message.tokenWipe != null && $Object.hasOwnProperty.call(message, "tokenWipe"))
                $root.proto.TokenWipeAccountTransactionBody.encode(message.tokenWipe, writer.uint32(/* id 31, wireType 2 =*/250).fork(), _depth + 1).ldelim();
            if (message.tokenAssociate != null && $Object.hasOwnProperty.call(message, "tokenAssociate"))
                $root.proto.TokenAssociateTransactionBody.encode(message.tokenAssociate, writer.uint32(/* id 32, wireType 2 =*/258).fork(), _depth + 1).ldelim();
            if (message.tokenDissociate != null && $Object.hasOwnProperty.call(message, "tokenDissociate"))
                $root.proto.TokenDissociateTransactionBody.encode(message.tokenDissociate, writer.uint32(/* id 33, wireType 2 =*/266).fork(), _depth + 1).ldelim();
            if (message.scheduleDelete != null && $Object.hasOwnProperty.call(message, "scheduleDelete"))
                $root.proto.ScheduleDeleteTransactionBody.encode(message.scheduleDelete, writer.uint32(/* id 34, wireType 2 =*/274).fork(), _depth + 1).ldelim();
            if (message.tokenPause != null && $Object.hasOwnProperty.call(message, "tokenPause"))
                $root.proto.TokenPauseTransactionBody.encode(message.tokenPause, writer.uint32(/* id 35, wireType 2 =*/282).fork(), _depth + 1).ldelim();
            if (message.tokenUnpause != null && $Object.hasOwnProperty.call(message, "tokenUnpause"))
                $root.proto.TokenUnpauseTransactionBody.encode(message.tokenUnpause, writer.uint32(/* id 36, wireType 2 =*/290).fork(), _depth + 1).ldelim();
            if (message.cryptoApproveAllowance != null && $Object.hasOwnProperty.call(message, "cryptoApproveAllowance"))
                $root.proto.CryptoApproveAllowanceTransactionBody.encode(message.cryptoApproveAllowance, writer.uint32(/* id 37, wireType 2 =*/298).fork(), _depth + 1).ldelim();
            if (message.cryptoDeleteAllowance != null && $Object.hasOwnProperty.call(message, "cryptoDeleteAllowance"))
                $root.proto.CryptoDeleteAllowanceTransactionBody.encode(message.cryptoDeleteAllowance, writer.uint32(/* id 38, wireType 2 =*/306).fork(), _depth + 1).ldelim();
            if (message.tokenFeeScheduleUpdate != null && $Object.hasOwnProperty.call(message, "tokenFeeScheduleUpdate"))
                $root.proto.TokenFeeScheduleUpdateTransactionBody.encode(message.tokenFeeScheduleUpdate, writer.uint32(/* id 39, wireType 2 =*/314).fork(), _depth + 1).ldelim();
            if (message.utilPrng != null && $Object.hasOwnProperty.call(message, "utilPrng"))
                $root.proto.UtilPrngTransactionBody.encode(message.utilPrng, writer.uint32(/* id 40, wireType 2 =*/322).fork(), _depth + 1).ldelim();
            if (message.tokenUpdateNfts != null && $Object.hasOwnProperty.call(message, "tokenUpdateNfts"))
                $root.proto.TokenUpdateNftsTransactionBody.encode(message.tokenUpdateNfts, writer.uint32(/* id 41, wireType 2 =*/330).fork(), _depth + 1).ldelim();
            if (message.nodeCreate != null && $Object.hasOwnProperty.call(message, "nodeCreate"))
                $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.encode(message.nodeCreate, writer.uint32(/* id 42, wireType 2 =*/338).fork(), _depth + 1).ldelim();
            if (message.nodeUpdate != null && $Object.hasOwnProperty.call(message, "nodeUpdate"))
                $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.encode(message.nodeUpdate, writer.uint32(/* id 43, wireType 2 =*/346).fork(), _depth + 1).ldelim();
            if (message.nodeDelete != null && $Object.hasOwnProperty.call(message, "nodeDelete"))
                $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.encode(message.nodeDelete, writer.uint32(/* id 44, wireType 2 =*/354).fork(), _depth + 1).ldelim();
            if (message.tokenReject != null && $Object.hasOwnProperty.call(message, "tokenReject"))
                $root.proto.TokenRejectTransactionBody.encode(message.tokenReject, writer.uint32(/* id 45, wireType 2 =*/362).fork(), _depth + 1).ldelim();
            if (message.tokenCancelAirdrop != null && $Object.hasOwnProperty.call(message, "tokenCancelAirdrop"))
                $root.proto.TokenCancelAirdropTransactionBody.encode(message.tokenCancelAirdrop, writer.uint32(/* id 46, wireType 2 =*/370).fork(), _depth + 1).ldelim();
            if (message.tokenClaimAirdrop != null && $Object.hasOwnProperty.call(message, "tokenClaimAirdrop"))
                $root.proto.TokenClaimAirdropTransactionBody.encode(message.tokenClaimAirdrop, writer.uint32(/* id 47, wireType 2 =*/378).fork(), _depth + 1).ldelim();
            if (message.tokenAirdrop != null && $Object.hasOwnProperty.call(message, "tokenAirdrop"))
                $root.proto.TokenAirdropTransactionBody.encode(message.tokenAirdrop, writer.uint32(/* id 48, wireType 2 =*/386).fork(), _depth + 1).ldelim();
            if (message.registeredNodeCreate != null && $Object.hasOwnProperty.call(message, "registeredNodeCreate"))
                $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.encode(message.registeredNodeCreate, writer.uint32(/* id 49, wireType 2 =*/394).fork(), _depth + 1).ldelim();
            if (message.registeredNodeUpdate != null && $Object.hasOwnProperty.call(message, "registeredNodeUpdate"))
                $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.encode(message.registeredNodeUpdate, writer.uint32(/* id 50, wireType 2 =*/402).fork(), _depth + 1).ldelim();
            if (message.registeredNodeDelete != null && $Object.hasOwnProperty.call(message, "registeredNodeDelete"))
                $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.encode(message.registeredNodeDelete, writer.uint32(/* id 51, wireType 2 =*/410).fork(), _depth + 1).ldelim();
            if (message.maxCustomFees != null && message.maxCustomFees.length)
                for (let i = 0; i < message.maxCustomFees.length; ++i)
                    $root.proto.CustomFeeLimit.encode(message.maxCustomFees[i], writer.uint32(/* id 1001, wireType 2 =*/8010).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SchedulableTransactionBody message, length delimited. Does not implicitly {@link proto.SchedulableTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SchedulableTransactionBody
         * @static
         * @param {proto.SchedulableTransactionBody.$Properties} message SchedulableTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SchedulableTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SchedulableTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SchedulableTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SchedulableTransactionBody & proto.SchedulableTransactionBody.$Shape} SchedulableTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SchedulableTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SchedulableTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.transactionFee = value;
                        else
                            delete message.transactionFee;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.contractCall = $root.proto.ContractCallTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractCall);
                        message.data = "contractCall";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.contractCreateInstance = $root.proto.ContractCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractCreateInstance);
                        message.data = "contractCreateInstance";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.contractUpdateInstance = $root.proto.ContractUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractUpdateInstance);
                        message.data = "contractUpdateInstance";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.contractDeleteInstance = $root.proto.ContractDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractDeleteInstance);
                        message.data = "contractDeleteInstance";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.cryptoCreateAccount = $root.proto.CryptoCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoCreateAccount);
                        message.data = "cryptoCreateAccount";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.cryptoDelete = $root.proto.CryptoDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoDelete);
                        message.data = "cryptoDelete";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.cryptoTransfer = $root.proto.CryptoTransferTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoTransfer);
                        message.data = "cryptoTransfer";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.cryptoUpdateAccount = $root.proto.CryptoUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoUpdateAccount);
                        message.data = "cryptoUpdateAccount";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.fileAppend = $root.proto.FileAppendTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileAppend);
                        message.data = "fileAppend";
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.fileCreate = $root.proto.FileCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileCreate);
                        message.data = "fileCreate";
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.fileDelete = $root.proto.FileDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileDelete);
                        message.data = "fileDelete";
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.fileUpdate = $root.proto.FileUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileUpdate);
                        message.data = "fileUpdate";
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.systemDelete = $root.proto.SystemDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.systemDelete);
                        message.data = "systemDelete";
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.systemUndelete = $root.proto.SystemUndeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.systemUndelete);
                        message.data = "systemUndelete";
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        message.freeze = $root.proto.FreezeTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.freeze);
                        message.data = "freeze";
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        message.consensusCreateTopic = $root.proto.ConsensusCreateTopicTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusCreateTopic);
                        message.data = "consensusCreateTopic";
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        message.consensusUpdateTopic = $root.proto.ConsensusUpdateTopicTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusUpdateTopic);
                        message.data = "consensusUpdateTopic";
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        message.consensusDeleteTopic = $root.proto.ConsensusDeleteTopicTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusDeleteTopic);
                        message.data = "consensusDeleteTopic";
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        message.consensusSubmitMessage = $root.proto.ConsensusSubmitMessageTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusSubmitMessage);
                        message.data = "consensusSubmitMessage";
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        message.tokenCreation = $root.proto.TokenCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenCreation);
                        message.data = "tokenCreation";
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        message.tokenFreeze = $root.proto.TokenFreezeAccountTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenFreeze);
                        message.data = "tokenFreeze";
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        message.tokenUnfreeze = $root.proto.TokenUnfreezeAccountTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenUnfreeze);
                        message.data = "tokenUnfreeze";
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        message.tokenGrantKyc = $root.proto.TokenGrantKycTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenGrantKyc);
                        message.data = "tokenGrantKyc";
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        message.tokenRevokeKyc = $root.proto.TokenRevokeKycTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenRevokeKyc);
                        message.data = "tokenRevokeKyc";
                        continue;
                    }
                case 27: {
                        if (wireType !== 2)
                            break;
                        message.tokenDeletion = $root.proto.TokenDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenDeletion);
                        message.data = "tokenDeletion";
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        message.tokenUpdate = $root.proto.TokenUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenUpdate);
                        message.data = "tokenUpdate";
                        continue;
                    }
                case 29: {
                        if (wireType !== 2)
                            break;
                        message.tokenMint = $root.proto.TokenMintTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenMint);
                        message.data = "tokenMint";
                        continue;
                    }
                case 30: {
                        if (wireType !== 2)
                            break;
                        message.tokenBurn = $root.proto.TokenBurnTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenBurn);
                        message.data = "tokenBurn";
                        continue;
                    }
                case 31: {
                        if (wireType !== 2)
                            break;
                        message.tokenWipe = $root.proto.TokenWipeAccountTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenWipe);
                        message.data = "tokenWipe";
                        continue;
                    }
                case 32: {
                        if (wireType !== 2)
                            break;
                        message.tokenAssociate = $root.proto.TokenAssociateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenAssociate);
                        message.data = "tokenAssociate";
                        continue;
                    }
                case 33: {
                        if (wireType !== 2)
                            break;
                        message.tokenDissociate = $root.proto.TokenDissociateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenDissociate);
                        message.data = "tokenDissociate";
                        continue;
                    }
                case 34: {
                        if (wireType !== 2)
                            break;
                        message.scheduleDelete = $root.proto.ScheduleDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleDelete);
                        message.data = "scheduleDelete";
                        continue;
                    }
                case 35: {
                        if (wireType !== 2)
                            break;
                        message.tokenPause = $root.proto.TokenPauseTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenPause);
                        message.data = "tokenPause";
                        continue;
                    }
                case 36: {
                        if (wireType !== 2)
                            break;
                        message.tokenUnpause = $root.proto.TokenUnpauseTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenUnpause);
                        message.data = "tokenUnpause";
                        continue;
                    }
                case 37: {
                        if (wireType !== 2)
                            break;
                        message.cryptoApproveAllowance = $root.proto.CryptoApproveAllowanceTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoApproveAllowance);
                        message.data = "cryptoApproveAllowance";
                        continue;
                    }
                case 38: {
                        if (wireType !== 2)
                            break;
                        message.cryptoDeleteAllowance = $root.proto.CryptoDeleteAllowanceTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoDeleteAllowance);
                        message.data = "cryptoDeleteAllowance";
                        continue;
                    }
                case 39: {
                        if (wireType !== 2)
                            break;
                        message.tokenFeeScheduleUpdate = $root.proto.TokenFeeScheduleUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenFeeScheduleUpdate);
                        message.data = "tokenFeeScheduleUpdate";
                        continue;
                    }
                case 40: {
                        if (wireType !== 2)
                            break;
                        message.utilPrng = $root.proto.UtilPrngTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.utilPrng);
                        message.data = "utilPrng";
                        continue;
                    }
                case 41: {
                        if (wireType !== 2)
                            break;
                        message.tokenUpdateNfts = $root.proto.TokenUpdateNftsTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenUpdateNfts);
                        message.data = "tokenUpdateNfts";
                        continue;
                    }
                case 42: {
                        if (wireType !== 2)
                            break;
                        message.nodeCreate = $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodeCreate);
                        message.data = "nodeCreate";
                        continue;
                    }
                case 43: {
                        if (wireType !== 2)
                            break;
                        message.nodeUpdate = $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodeUpdate);
                        message.data = "nodeUpdate";
                        continue;
                    }
                case 44: {
                        if (wireType !== 2)
                            break;
                        message.nodeDelete = $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodeDelete);
                        message.data = "nodeDelete";
                        continue;
                    }
                case 45: {
                        if (wireType !== 2)
                            break;
                        message.tokenReject = $root.proto.TokenRejectTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenReject);
                        message.data = "tokenReject";
                        continue;
                    }
                case 46: {
                        if (wireType !== 2)
                            break;
                        message.tokenCancelAirdrop = $root.proto.TokenCancelAirdropTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenCancelAirdrop);
                        message.data = "tokenCancelAirdrop";
                        continue;
                    }
                case 47: {
                        if (wireType !== 2)
                            break;
                        message.tokenClaimAirdrop = $root.proto.TokenClaimAirdropTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenClaimAirdrop);
                        message.data = "tokenClaimAirdrop";
                        continue;
                    }
                case 48: {
                        if (wireType !== 2)
                            break;
                        message.tokenAirdrop = $root.proto.TokenAirdropTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenAirdrop);
                        message.data = "tokenAirdrop";
                        continue;
                    }
                case 49: {
                        if (wireType !== 2)
                            break;
                        message.registeredNodeCreate = $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.registeredNodeCreate);
                        message.data = "registeredNodeCreate";
                        continue;
                    }
                case 50: {
                        if (wireType !== 2)
                            break;
                        message.registeredNodeUpdate = $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.registeredNodeUpdate);
                        message.data = "registeredNodeUpdate";
                        continue;
                    }
                case 51: {
                        if (wireType !== 2)
                            break;
                        message.registeredNodeDelete = $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.registeredNodeDelete);
                        message.data = "registeredNodeDelete";
                        continue;
                    }
                case 1001: {
                        if (wireType !== 2)
                            break;
                        if (!(message.maxCustomFees && message.maxCustomFees.length))
                            message.maxCustomFees = [];
                        message.maxCustomFees.push($root.proto.CustomFeeLimit.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SchedulableTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SchedulableTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SchedulableTransactionBody & proto.SchedulableTransactionBody.$Shape} SchedulableTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SchedulableTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SchedulableTransactionBody message.
         * @function verify
         * @memberof proto.SchedulableTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SchedulableTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.transactionFee != null && $Object.hasOwnProperty.call(message, "transactionFee"))
                if (!$util.isInteger(message.transactionFee) && !(message.transactionFee && $util.isInteger(message.transactionFee.low) && $util.isInteger(message.transactionFee.high)))
                    return "transactionFee: integer|Long expected";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.contractCall != null && $Object.hasOwnProperty.call(message, "contractCall")) {
                properties.data = 1;
                {
                    let error = $root.proto.ContractCallTransactionBody.verify(message.contractCall, _depth + 1);
                    if (error)
                        return "contractCall." + error;
                }
            }
            if (message.contractCreateInstance != null && $Object.hasOwnProperty.call(message, "contractCreateInstance")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ContractCreateTransactionBody.verify(message.contractCreateInstance, _depth + 1);
                    if (error)
                        return "contractCreateInstance." + error;
                }
            }
            if (message.contractUpdateInstance != null && $Object.hasOwnProperty.call(message, "contractUpdateInstance")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ContractUpdateTransactionBody.verify(message.contractUpdateInstance, _depth + 1);
                    if (error)
                        return "contractUpdateInstance." + error;
                }
            }
            if (message.contractDeleteInstance != null && $Object.hasOwnProperty.call(message, "contractDeleteInstance")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ContractDeleteTransactionBody.verify(message.contractDeleteInstance, _depth + 1);
                    if (error)
                        return "contractDeleteInstance." + error;
                }
            }
            if (message.cryptoCreateAccount != null && $Object.hasOwnProperty.call(message, "cryptoCreateAccount")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoCreateTransactionBody.verify(message.cryptoCreateAccount, _depth + 1);
                    if (error)
                        return "cryptoCreateAccount." + error;
                }
            }
            if (message.cryptoDelete != null && $Object.hasOwnProperty.call(message, "cryptoDelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoDeleteTransactionBody.verify(message.cryptoDelete, _depth + 1);
                    if (error)
                        return "cryptoDelete." + error;
                }
            }
            if (message.cryptoTransfer != null && $Object.hasOwnProperty.call(message, "cryptoTransfer")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoTransferTransactionBody.verify(message.cryptoTransfer, _depth + 1);
                    if (error)
                        return "cryptoTransfer." + error;
                }
            }
            if (message.cryptoUpdateAccount != null && $Object.hasOwnProperty.call(message, "cryptoUpdateAccount")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoUpdateTransactionBody.verify(message.cryptoUpdateAccount, _depth + 1);
                    if (error)
                        return "cryptoUpdateAccount." + error;
                }
            }
            if (message.fileAppend != null && $Object.hasOwnProperty.call(message, "fileAppend")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.FileAppendTransactionBody.verify(message.fileAppend, _depth + 1);
                    if (error)
                        return "fileAppend." + error;
                }
            }
            if (message.fileCreate != null && $Object.hasOwnProperty.call(message, "fileCreate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.FileCreateTransactionBody.verify(message.fileCreate, _depth + 1);
                    if (error)
                        return "fileCreate." + error;
                }
            }
            if (message.fileDelete != null && $Object.hasOwnProperty.call(message, "fileDelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.FileDeleteTransactionBody.verify(message.fileDelete, _depth + 1);
                    if (error)
                        return "fileDelete." + error;
                }
            }
            if (message.fileUpdate != null && $Object.hasOwnProperty.call(message, "fileUpdate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.FileUpdateTransactionBody.verify(message.fileUpdate, _depth + 1);
                    if (error)
                        return "fileUpdate." + error;
                }
            }
            if (message.systemDelete != null && $Object.hasOwnProperty.call(message, "systemDelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.SystemDeleteTransactionBody.verify(message.systemDelete, _depth + 1);
                    if (error)
                        return "systemDelete." + error;
                }
            }
            if (message.systemUndelete != null && $Object.hasOwnProperty.call(message, "systemUndelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.SystemUndeleteTransactionBody.verify(message.systemUndelete, _depth + 1);
                    if (error)
                        return "systemUndelete." + error;
                }
            }
            if (message.freeze != null && $Object.hasOwnProperty.call(message, "freeze")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.FreezeTransactionBody.verify(message.freeze, _depth + 1);
                    if (error)
                        return "freeze." + error;
                }
            }
            if (message.consensusCreateTopic != null && $Object.hasOwnProperty.call(message, "consensusCreateTopic")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ConsensusCreateTopicTransactionBody.verify(message.consensusCreateTopic, _depth + 1);
                    if (error)
                        return "consensusCreateTopic." + error;
                }
            }
            if (message.consensusUpdateTopic != null && $Object.hasOwnProperty.call(message, "consensusUpdateTopic")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ConsensusUpdateTopicTransactionBody.verify(message.consensusUpdateTopic, _depth + 1);
                    if (error)
                        return "consensusUpdateTopic." + error;
                }
            }
            if (message.consensusDeleteTopic != null && $Object.hasOwnProperty.call(message, "consensusDeleteTopic")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ConsensusDeleteTopicTransactionBody.verify(message.consensusDeleteTopic, _depth + 1);
                    if (error)
                        return "consensusDeleteTopic." + error;
                }
            }
            if (message.consensusSubmitMessage != null && $Object.hasOwnProperty.call(message, "consensusSubmitMessage")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ConsensusSubmitMessageTransactionBody.verify(message.consensusSubmitMessage, _depth + 1);
                    if (error)
                        return "consensusSubmitMessage." + error;
                }
            }
            if (message.tokenCreation != null && $Object.hasOwnProperty.call(message, "tokenCreation")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenCreateTransactionBody.verify(message.tokenCreation, _depth + 1);
                    if (error)
                        return "tokenCreation." + error;
                }
            }
            if (message.tokenFreeze != null && $Object.hasOwnProperty.call(message, "tokenFreeze")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenFreezeAccountTransactionBody.verify(message.tokenFreeze, _depth + 1);
                    if (error)
                        return "tokenFreeze." + error;
                }
            }
            if (message.tokenUnfreeze != null && $Object.hasOwnProperty.call(message, "tokenUnfreeze")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenUnfreezeAccountTransactionBody.verify(message.tokenUnfreeze, _depth + 1);
                    if (error)
                        return "tokenUnfreeze." + error;
                }
            }
            if (message.tokenGrantKyc != null && $Object.hasOwnProperty.call(message, "tokenGrantKyc")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenGrantKycTransactionBody.verify(message.tokenGrantKyc, _depth + 1);
                    if (error)
                        return "tokenGrantKyc." + error;
                }
            }
            if (message.tokenRevokeKyc != null && $Object.hasOwnProperty.call(message, "tokenRevokeKyc")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenRevokeKycTransactionBody.verify(message.tokenRevokeKyc, _depth + 1);
                    if (error)
                        return "tokenRevokeKyc." + error;
                }
            }
            if (message.tokenDeletion != null && $Object.hasOwnProperty.call(message, "tokenDeletion")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenDeleteTransactionBody.verify(message.tokenDeletion, _depth + 1);
                    if (error)
                        return "tokenDeletion." + error;
                }
            }
            if (message.tokenUpdate != null && $Object.hasOwnProperty.call(message, "tokenUpdate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenUpdateTransactionBody.verify(message.tokenUpdate, _depth + 1);
                    if (error)
                        return "tokenUpdate." + error;
                }
            }
            if (message.tokenMint != null && $Object.hasOwnProperty.call(message, "tokenMint")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenMintTransactionBody.verify(message.tokenMint, _depth + 1);
                    if (error)
                        return "tokenMint." + error;
                }
            }
            if (message.tokenBurn != null && $Object.hasOwnProperty.call(message, "tokenBurn")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenBurnTransactionBody.verify(message.tokenBurn, _depth + 1);
                    if (error)
                        return "tokenBurn." + error;
                }
            }
            if (message.tokenWipe != null && $Object.hasOwnProperty.call(message, "tokenWipe")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenWipeAccountTransactionBody.verify(message.tokenWipe, _depth + 1);
                    if (error)
                        return "tokenWipe." + error;
                }
            }
            if (message.tokenAssociate != null && $Object.hasOwnProperty.call(message, "tokenAssociate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenAssociateTransactionBody.verify(message.tokenAssociate, _depth + 1);
                    if (error)
                        return "tokenAssociate." + error;
                }
            }
            if (message.tokenDissociate != null && $Object.hasOwnProperty.call(message, "tokenDissociate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenDissociateTransactionBody.verify(message.tokenDissociate, _depth + 1);
                    if (error)
                        return "tokenDissociate." + error;
                }
            }
            if (message.scheduleDelete != null && $Object.hasOwnProperty.call(message, "scheduleDelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.ScheduleDeleteTransactionBody.verify(message.scheduleDelete, _depth + 1);
                    if (error)
                        return "scheduleDelete." + error;
                }
            }
            if (message.tokenPause != null && $Object.hasOwnProperty.call(message, "tokenPause")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenPauseTransactionBody.verify(message.tokenPause, _depth + 1);
                    if (error)
                        return "tokenPause." + error;
                }
            }
            if (message.tokenUnpause != null && $Object.hasOwnProperty.call(message, "tokenUnpause")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenUnpauseTransactionBody.verify(message.tokenUnpause, _depth + 1);
                    if (error)
                        return "tokenUnpause." + error;
                }
            }
            if (message.cryptoApproveAllowance != null && $Object.hasOwnProperty.call(message, "cryptoApproveAllowance")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoApproveAllowanceTransactionBody.verify(message.cryptoApproveAllowance, _depth + 1);
                    if (error)
                        return "cryptoApproveAllowance." + error;
                }
            }
            if (message.cryptoDeleteAllowance != null && $Object.hasOwnProperty.call(message, "cryptoDeleteAllowance")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.CryptoDeleteAllowanceTransactionBody.verify(message.cryptoDeleteAllowance, _depth + 1);
                    if (error)
                        return "cryptoDeleteAllowance." + error;
                }
            }
            if (message.tokenFeeScheduleUpdate != null && $Object.hasOwnProperty.call(message, "tokenFeeScheduleUpdate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenFeeScheduleUpdateTransactionBody.verify(message.tokenFeeScheduleUpdate, _depth + 1);
                    if (error)
                        return "tokenFeeScheduleUpdate." + error;
                }
            }
            if (message.utilPrng != null && $Object.hasOwnProperty.call(message, "utilPrng")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.UtilPrngTransactionBody.verify(message.utilPrng, _depth + 1);
                    if (error)
                        return "utilPrng." + error;
                }
            }
            if (message.tokenUpdateNfts != null && $Object.hasOwnProperty.call(message, "tokenUpdateNfts")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenUpdateNftsTransactionBody.verify(message.tokenUpdateNfts, _depth + 1);
                    if (error)
                        return "tokenUpdateNfts." + error;
                }
            }
            if (message.nodeCreate != null && $Object.hasOwnProperty.call(message, "nodeCreate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.verify(message.nodeCreate, _depth + 1);
                    if (error)
                        return "nodeCreate." + error;
                }
            }
            if (message.nodeUpdate != null && $Object.hasOwnProperty.call(message, "nodeUpdate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.verify(message.nodeUpdate, _depth + 1);
                    if (error)
                        return "nodeUpdate." + error;
                }
            }
            if (message.nodeDelete != null && $Object.hasOwnProperty.call(message, "nodeDelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.verify(message.nodeDelete, _depth + 1);
                    if (error)
                        return "nodeDelete." + error;
                }
            }
            if (message.tokenReject != null && $Object.hasOwnProperty.call(message, "tokenReject")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenRejectTransactionBody.verify(message.tokenReject, _depth + 1);
                    if (error)
                        return "tokenReject." + error;
                }
            }
            if (message.tokenCancelAirdrop != null && $Object.hasOwnProperty.call(message, "tokenCancelAirdrop")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenCancelAirdropTransactionBody.verify(message.tokenCancelAirdrop, _depth + 1);
                    if (error)
                        return "tokenCancelAirdrop." + error;
                }
            }
            if (message.tokenClaimAirdrop != null && $Object.hasOwnProperty.call(message, "tokenClaimAirdrop")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenClaimAirdropTransactionBody.verify(message.tokenClaimAirdrop, _depth + 1);
                    if (error)
                        return "tokenClaimAirdrop." + error;
                }
            }
            if (message.tokenAirdrop != null && $Object.hasOwnProperty.call(message, "tokenAirdrop")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.TokenAirdropTransactionBody.verify(message.tokenAirdrop, _depth + 1);
                    if (error)
                        return "tokenAirdrop." + error;
                }
            }
            if (message.registeredNodeCreate != null && $Object.hasOwnProperty.call(message, "registeredNodeCreate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.verify(message.registeredNodeCreate, _depth + 1);
                    if (error)
                        return "registeredNodeCreate." + error;
                }
            }
            if (message.registeredNodeUpdate != null && $Object.hasOwnProperty.call(message, "registeredNodeUpdate")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.verify(message.registeredNodeUpdate, _depth + 1);
                    if (error)
                        return "registeredNodeUpdate." + error;
                }
            }
            if (message.registeredNodeDelete != null && $Object.hasOwnProperty.call(message, "registeredNodeDelete")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.verify(message.registeredNodeDelete, _depth + 1);
                    if (error)
                        return "registeredNodeDelete." + error;
                }
            }
            if (message.maxCustomFees != null && $Object.hasOwnProperty.call(message, "maxCustomFees")) {
                if (!$Array.isArray(message.maxCustomFees))
                    return "maxCustomFees: array expected";
                for (let i = 0; i < message.maxCustomFees.length; ++i) {
                    let error = $root.proto.CustomFeeLimit.verify(message.maxCustomFees[i], _depth + 1);
                    if (error)
                        return "maxCustomFees." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SchedulableTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SchedulableTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SchedulableTransactionBody} SchedulableTransactionBody
         */
        SchedulableTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SchedulableTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SchedulableTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SchedulableTransactionBody();
            if (object.transactionFee != null)
                if (typeof object.transactionFee === "object" ? object.transactionFee.low || object.transactionFee.high : $Number(object.transactionFee) !== 0)
                    if ($util.Long)
                        message.transactionFee = $util.Long.fromValue(object.transactionFee, true);
                    else if (typeof object.transactionFee === "string")
                        message.transactionFee = $parseInt(object.transactionFee, 10);
                    else if (typeof object.transactionFee === "number")
                        message.transactionFee = object.transactionFee;
                    else if (typeof object.transactionFee === "object")
                        message.transactionFee = new $util.LongBits(object.transactionFee.low >>> 0, object.transactionFee.high >>> 0).toNumber(true);
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.contractCall != null) {
                if (!$util.isObject(object.contractCall))
                    throw $TypeError(".proto.SchedulableTransactionBody.contractCall: object expected");
                message.contractCall = $root.proto.ContractCallTransactionBody.fromObject(object.contractCall, _depth + 1);
            }
            if (object.contractCreateInstance != null) {
                if (!$util.isObject(object.contractCreateInstance))
                    throw $TypeError(".proto.SchedulableTransactionBody.contractCreateInstance: object expected");
                message.contractCreateInstance = $root.proto.ContractCreateTransactionBody.fromObject(object.contractCreateInstance, _depth + 1);
            }
            if (object.contractUpdateInstance != null) {
                if (!$util.isObject(object.contractUpdateInstance))
                    throw $TypeError(".proto.SchedulableTransactionBody.contractUpdateInstance: object expected");
                message.contractUpdateInstance = $root.proto.ContractUpdateTransactionBody.fromObject(object.contractUpdateInstance, _depth + 1);
            }
            if (object.contractDeleteInstance != null) {
                if (!$util.isObject(object.contractDeleteInstance))
                    throw $TypeError(".proto.SchedulableTransactionBody.contractDeleteInstance: object expected");
                message.contractDeleteInstance = $root.proto.ContractDeleteTransactionBody.fromObject(object.contractDeleteInstance, _depth + 1);
            }
            if (object.cryptoCreateAccount != null) {
                if (!$util.isObject(object.cryptoCreateAccount))
                    throw $TypeError(".proto.SchedulableTransactionBody.cryptoCreateAccount: object expected");
                message.cryptoCreateAccount = $root.proto.CryptoCreateTransactionBody.fromObject(object.cryptoCreateAccount, _depth + 1);
            }
            if (object.cryptoDelete != null) {
                if (!$util.isObject(object.cryptoDelete))
                    throw $TypeError(".proto.SchedulableTransactionBody.cryptoDelete: object expected");
                message.cryptoDelete = $root.proto.CryptoDeleteTransactionBody.fromObject(object.cryptoDelete, _depth + 1);
            }
            if (object.cryptoTransfer != null) {
                if (!$util.isObject(object.cryptoTransfer))
                    throw $TypeError(".proto.SchedulableTransactionBody.cryptoTransfer: object expected");
                message.cryptoTransfer = $root.proto.CryptoTransferTransactionBody.fromObject(object.cryptoTransfer, _depth + 1);
            }
            if (object.cryptoUpdateAccount != null) {
                if (!$util.isObject(object.cryptoUpdateAccount))
                    throw $TypeError(".proto.SchedulableTransactionBody.cryptoUpdateAccount: object expected");
                message.cryptoUpdateAccount = $root.proto.CryptoUpdateTransactionBody.fromObject(object.cryptoUpdateAccount, _depth + 1);
            }
            if (object.fileAppend != null) {
                if (!$util.isObject(object.fileAppend))
                    throw $TypeError(".proto.SchedulableTransactionBody.fileAppend: object expected");
                message.fileAppend = $root.proto.FileAppendTransactionBody.fromObject(object.fileAppend, _depth + 1);
            }
            if (object.fileCreate != null) {
                if (!$util.isObject(object.fileCreate))
                    throw $TypeError(".proto.SchedulableTransactionBody.fileCreate: object expected");
                message.fileCreate = $root.proto.FileCreateTransactionBody.fromObject(object.fileCreate, _depth + 1);
            }
            if (object.fileDelete != null) {
                if (!$util.isObject(object.fileDelete))
                    throw $TypeError(".proto.SchedulableTransactionBody.fileDelete: object expected");
                message.fileDelete = $root.proto.FileDeleteTransactionBody.fromObject(object.fileDelete, _depth + 1);
            }
            if (object.fileUpdate != null) {
                if (!$util.isObject(object.fileUpdate))
                    throw $TypeError(".proto.SchedulableTransactionBody.fileUpdate: object expected");
                message.fileUpdate = $root.proto.FileUpdateTransactionBody.fromObject(object.fileUpdate, _depth + 1);
            }
            if (object.systemDelete != null) {
                if (!$util.isObject(object.systemDelete))
                    throw $TypeError(".proto.SchedulableTransactionBody.systemDelete: object expected");
                message.systemDelete = $root.proto.SystemDeleteTransactionBody.fromObject(object.systemDelete, _depth + 1);
            }
            if (object.systemUndelete != null) {
                if (!$util.isObject(object.systemUndelete))
                    throw $TypeError(".proto.SchedulableTransactionBody.systemUndelete: object expected");
                message.systemUndelete = $root.proto.SystemUndeleteTransactionBody.fromObject(object.systemUndelete, _depth + 1);
            }
            if (object.freeze != null) {
                if (!$util.isObject(object.freeze))
                    throw $TypeError(".proto.SchedulableTransactionBody.freeze: object expected");
                message.freeze = $root.proto.FreezeTransactionBody.fromObject(object.freeze, _depth + 1);
            }
            if (object.consensusCreateTopic != null) {
                if (!$util.isObject(object.consensusCreateTopic))
                    throw $TypeError(".proto.SchedulableTransactionBody.consensusCreateTopic: object expected");
                message.consensusCreateTopic = $root.proto.ConsensusCreateTopicTransactionBody.fromObject(object.consensusCreateTopic, _depth + 1);
            }
            if (object.consensusUpdateTopic != null) {
                if (!$util.isObject(object.consensusUpdateTopic))
                    throw $TypeError(".proto.SchedulableTransactionBody.consensusUpdateTopic: object expected");
                message.consensusUpdateTopic = $root.proto.ConsensusUpdateTopicTransactionBody.fromObject(object.consensusUpdateTopic, _depth + 1);
            }
            if (object.consensusDeleteTopic != null) {
                if (!$util.isObject(object.consensusDeleteTopic))
                    throw $TypeError(".proto.SchedulableTransactionBody.consensusDeleteTopic: object expected");
                message.consensusDeleteTopic = $root.proto.ConsensusDeleteTopicTransactionBody.fromObject(object.consensusDeleteTopic, _depth + 1);
            }
            if (object.consensusSubmitMessage != null) {
                if (!$util.isObject(object.consensusSubmitMessage))
                    throw $TypeError(".proto.SchedulableTransactionBody.consensusSubmitMessage: object expected");
                message.consensusSubmitMessage = $root.proto.ConsensusSubmitMessageTransactionBody.fromObject(object.consensusSubmitMessage, _depth + 1);
            }
            if (object.tokenCreation != null) {
                if (!$util.isObject(object.tokenCreation))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenCreation: object expected");
                message.tokenCreation = $root.proto.TokenCreateTransactionBody.fromObject(object.tokenCreation, _depth + 1);
            }
            if (object.tokenFreeze != null) {
                if (!$util.isObject(object.tokenFreeze))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenFreeze: object expected");
                message.tokenFreeze = $root.proto.TokenFreezeAccountTransactionBody.fromObject(object.tokenFreeze, _depth + 1);
            }
            if (object.tokenUnfreeze != null) {
                if (!$util.isObject(object.tokenUnfreeze))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenUnfreeze: object expected");
                message.tokenUnfreeze = $root.proto.TokenUnfreezeAccountTransactionBody.fromObject(object.tokenUnfreeze, _depth + 1);
            }
            if (object.tokenGrantKyc != null) {
                if (!$util.isObject(object.tokenGrantKyc))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenGrantKyc: object expected");
                message.tokenGrantKyc = $root.proto.TokenGrantKycTransactionBody.fromObject(object.tokenGrantKyc, _depth + 1);
            }
            if (object.tokenRevokeKyc != null) {
                if (!$util.isObject(object.tokenRevokeKyc))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenRevokeKyc: object expected");
                message.tokenRevokeKyc = $root.proto.TokenRevokeKycTransactionBody.fromObject(object.tokenRevokeKyc, _depth + 1);
            }
            if (object.tokenDeletion != null) {
                if (!$util.isObject(object.tokenDeletion))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenDeletion: object expected");
                message.tokenDeletion = $root.proto.TokenDeleteTransactionBody.fromObject(object.tokenDeletion, _depth + 1);
            }
            if (object.tokenUpdate != null) {
                if (!$util.isObject(object.tokenUpdate))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenUpdate: object expected");
                message.tokenUpdate = $root.proto.TokenUpdateTransactionBody.fromObject(object.tokenUpdate, _depth + 1);
            }
            if (object.tokenMint != null) {
                if (!$util.isObject(object.tokenMint))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenMint: object expected");
                message.tokenMint = $root.proto.TokenMintTransactionBody.fromObject(object.tokenMint, _depth + 1);
            }
            if (object.tokenBurn != null) {
                if (!$util.isObject(object.tokenBurn))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenBurn: object expected");
                message.tokenBurn = $root.proto.TokenBurnTransactionBody.fromObject(object.tokenBurn, _depth + 1);
            }
            if (object.tokenWipe != null) {
                if (!$util.isObject(object.tokenWipe))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenWipe: object expected");
                message.tokenWipe = $root.proto.TokenWipeAccountTransactionBody.fromObject(object.tokenWipe, _depth + 1);
            }
            if (object.tokenAssociate != null) {
                if (!$util.isObject(object.tokenAssociate))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenAssociate: object expected");
                message.tokenAssociate = $root.proto.TokenAssociateTransactionBody.fromObject(object.tokenAssociate, _depth + 1);
            }
            if (object.tokenDissociate != null) {
                if (!$util.isObject(object.tokenDissociate))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenDissociate: object expected");
                message.tokenDissociate = $root.proto.TokenDissociateTransactionBody.fromObject(object.tokenDissociate, _depth + 1);
            }
            if (object.scheduleDelete != null) {
                if (!$util.isObject(object.scheduleDelete))
                    throw $TypeError(".proto.SchedulableTransactionBody.scheduleDelete: object expected");
                message.scheduleDelete = $root.proto.ScheduleDeleteTransactionBody.fromObject(object.scheduleDelete, _depth + 1);
            }
            if (object.tokenPause != null) {
                if (!$util.isObject(object.tokenPause))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenPause: object expected");
                message.tokenPause = $root.proto.TokenPauseTransactionBody.fromObject(object.tokenPause, _depth + 1);
            }
            if (object.tokenUnpause != null) {
                if (!$util.isObject(object.tokenUnpause))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenUnpause: object expected");
                message.tokenUnpause = $root.proto.TokenUnpauseTransactionBody.fromObject(object.tokenUnpause, _depth + 1);
            }
            if (object.cryptoApproveAllowance != null) {
                if (!$util.isObject(object.cryptoApproveAllowance))
                    throw $TypeError(".proto.SchedulableTransactionBody.cryptoApproveAllowance: object expected");
                message.cryptoApproveAllowance = $root.proto.CryptoApproveAllowanceTransactionBody.fromObject(object.cryptoApproveAllowance, _depth + 1);
            }
            if (object.cryptoDeleteAllowance != null) {
                if (!$util.isObject(object.cryptoDeleteAllowance))
                    throw $TypeError(".proto.SchedulableTransactionBody.cryptoDeleteAllowance: object expected");
                message.cryptoDeleteAllowance = $root.proto.CryptoDeleteAllowanceTransactionBody.fromObject(object.cryptoDeleteAllowance, _depth + 1);
            }
            if (object.tokenFeeScheduleUpdate != null) {
                if (!$util.isObject(object.tokenFeeScheduleUpdate))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenFeeScheduleUpdate: object expected");
                message.tokenFeeScheduleUpdate = $root.proto.TokenFeeScheduleUpdateTransactionBody.fromObject(object.tokenFeeScheduleUpdate, _depth + 1);
            }
            if (object.utilPrng != null) {
                if (!$util.isObject(object.utilPrng))
                    throw $TypeError(".proto.SchedulableTransactionBody.utilPrng: object expected");
                message.utilPrng = $root.proto.UtilPrngTransactionBody.fromObject(object.utilPrng, _depth + 1);
            }
            if (object.tokenUpdateNfts != null) {
                if (!$util.isObject(object.tokenUpdateNfts))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenUpdateNfts: object expected");
                message.tokenUpdateNfts = $root.proto.TokenUpdateNftsTransactionBody.fromObject(object.tokenUpdateNfts, _depth + 1);
            }
            if (object.nodeCreate != null) {
                if (!$util.isObject(object.nodeCreate))
                    throw $TypeError(".proto.SchedulableTransactionBody.nodeCreate: object expected");
                message.nodeCreate = $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.fromObject(object.nodeCreate, _depth + 1);
            }
            if (object.nodeUpdate != null) {
                if (!$util.isObject(object.nodeUpdate))
                    throw $TypeError(".proto.SchedulableTransactionBody.nodeUpdate: object expected");
                message.nodeUpdate = $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.fromObject(object.nodeUpdate, _depth + 1);
            }
            if (object.nodeDelete != null) {
                if (!$util.isObject(object.nodeDelete))
                    throw $TypeError(".proto.SchedulableTransactionBody.nodeDelete: object expected");
                message.nodeDelete = $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.fromObject(object.nodeDelete, _depth + 1);
            }
            if (object.tokenReject != null) {
                if (!$util.isObject(object.tokenReject))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenReject: object expected");
                message.tokenReject = $root.proto.TokenRejectTransactionBody.fromObject(object.tokenReject, _depth + 1);
            }
            if (object.tokenCancelAirdrop != null) {
                if (!$util.isObject(object.tokenCancelAirdrop))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenCancelAirdrop: object expected");
                message.tokenCancelAirdrop = $root.proto.TokenCancelAirdropTransactionBody.fromObject(object.tokenCancelAirdrop, _depth + 1);
            }
            if (object.tokenClaimAirdrop != null) {
                if (!$util.isObject(object.tokenClaimAirdrop))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenClaimAirdrop: object expected");
                message.tokenClaimAirdrop = $root.proto.TokenClaimAirdropTransactionBody.fromObject(object.tokenClaimAirdrop, _depth + 1);
            }
            if (object.tokenAirdrop != null) {
                if (!$util.isObject(object.tokenAirdrop))
                    throw $TypeError(".proto.SchedulableTransactionBody.tokenAirdrop: object expected");
                message.tokenAirdrop = $root.proto.TokenAirdropTransactionBody.fromObject(object.tokenAirdrop, _depth + 1);
            }
            if (object.registeredNodeCreate != null) {
                if (!$util.isObject(object.registeredNodeCreate))
                    throw $TypeError(".proto.SchedulableTransactionBody.registeredNodeCreate: object expected");
                message.registeredNodeCreate = $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.fromObject(object.registeredNodeCreate, _depth + 1);
            }
            if (object.registeredNodeUpdate != null) {
                if (!$util.isObject(object.registeredNodeUpdate))
                    throw $TypeError(".proto.SchedulableTransactionBody.registeredNodeUpdate: object expected");
                message.registeredNodeUpdate = $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.fromObject(object.registeredNodeUpdate, _depth + 1);
            }
            if (object.registeredNodeDelete != null) {
                if (!$util.isObject(object.registeredNodeDelete))
                    throw $TypeError(".proto.SchedulableTransactionBody.registeredNodeDelete: object expected");
                message.registeredNodeDelete = $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.fromObject(object.registeredNodeDelete, _depth + 1);
            }
            if (object.maxCustomFees) {
                if (!$Array.isArray(object.maxCustomFees))
                    throw $TypeError(".proto.SchedulableTransactionBody.maxCustomFees: array expected");
                message.maxCustomFees = $Array(object.maxCustomFees.length);
                for (let i = 0; i < object.maxCustomFees.length; ++i) {
                    if (!$util.isObject(object.maxCustomFees[i]))
                        throw $TypeError(".proto.SchedulableTransactionBody.maxCustomFees: object expected");
                    message.maxCustomFees[i] = $root.proto.CustomFeeLimit.fromObject(object.maxCustomFees[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SchedulableTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SchedulableTransactionBody
         * @static
         * @param {proto.SchedulableTransactionBody} message SchedulableTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SchedulableTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.maxCustomFees = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.transactionFee = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.transactionFee = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.memo = "";
            }
            if (message.transactionFee != null && $Object.hasOwnProperty.call(message, "transactionFee"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.transactionFee = typeof message.transactionFee === "number" ? $BigInt(message.transactionFee) : $util.Long.fromBits(message.transactionFee.low >>> 0, message.transactionFee.high >>> 0, true).toBigInt();
                else if (typeof message.transactionFee === "number")
                    object.transactionFee = options.longs === $String ? $String(message.transactionFee) : message.transactionFee;
                else
                    object.transactionFee = options.longs === $String ? $util.Long.prototype.toString.call(message.transactionFee) : options.longs === $Number ? new $util.LongBits(message.transactionFee.low >>> 0, message.transactionFee.high >>> 0).toNumber(true) : message.transactionFee;
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.contractCall != null && $Object.hasOwnProperty.call(message, "contractCall")) {
                object.contractCall = $root.proto.ContractCallTransactionBody.toObject(message.contractCall, options, _depth + 1);
                if (options.oneofs)
                    object.data = "contractCall";
            }
            if (message.contractCreateInstance != null && $Object.hasOwnProperty.call(message, "contractCreateInstance")) {
                object.contractCreateInstance = $root.proto.ContractCreateTransactionBody.toObject(message.contractCreateInstance, options, _depth + 1);
                if (options.oneofs)
                    object.data = "contractCreateInstance";
            }
            if (message.contractUpdateInstance != null && $Object.hasOwnProperty.call(message, "contractUpdateInstance")) {
                object.contractUpdateInstance = $root.proto.ContractUpdateTransactionBody.toObject(message.contractUpdateInstance, options, _depth + 1);
                if (options.oneofs)
                    object.data = "contractUpdateInstance";
            }
            if (message.contractDeleteInstance != null && $Object.hasOwnProperty.call(message, "contractDeleteInstance")) {
                object.contractDeleteInstance = $root.proto.ContractDeleteTransactionBody.toObject(message.contractDeleteInstance, options, _depth + 1);
                if (options.oneofs)
                    object.data = "contractDeleteInstance";
            }
            if (message.cryptoCreateAccount != null && $Object.hasOwnProperty.call(message, "cryptoCreateAccount")) {
                object.cryptoCreateAccount = $root.proto.CryptoCreateTransactionBody.toObject(message.cryptoCreateAccount, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoCreateAccount";
            }
            if (message.cryptoDelete != null && $Object.hasOwnProperty.call(message, "cryptoDelete")) {
                object.cryptoDelete = $root.proto.CryptoDeleteTransactionBody.toObject(message.cryptoDelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoDelete";
            }
            if (message.cryptoTransfer != null && $Object.hasOwnProperty.call(message, "cryptoTransfer")) {
                object.cryptoTransfer = $root.proto.CryptoTransferTransactionBody.toObject(message.cryptoTransfer, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoTransfer";
            }
            if (message.cryptoUpdateAccount != null && $Object.hasOwnProperty.call(message, "cryptoUpdateAccount")) {
                object.cryptoUpdateAccount = $root.proto.CryptoUpdateTransactionBody.toObject(message.cryptoUpdateAccount, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoUpdateAccount";
            }
            if (message.fileAppend != null && $Object.hasOwnProperty.call(message, "fileAppend")) {
                object.fileAppend = $root.proto.FileAppendTransactionBody.toObject(message.fileAppend, options, _depth + 1);
                if (options.oneofs)
                    object.data = "fileAppend";
            }
            if (message.fileCreate != null && $Object.hasOwnProperty.call(message, "fileCreate")) {
                object.fileCreate = $root.proto.FileCreateTransactionBody.toObject(message.fileCreate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "fileCreate";
            }
            if (message.fileDelete != null && $Object.hasOwnProperty.call(message, "fileDelete")) {
                object.fileDelete = $root.proto.FileDeleteTransactionBody.toObject(message.fileDelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "fileDelete";
            }
            if (message.fileUpdate != null && $Object.hasOwnProperty.call(message, "fileUpdate")) {
                object.fileUpdate = $root.proto.FileUpdateTransactionBody.toObject(message.fileUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "fileUpdate";
            }
            if (message.systemDelete != null && $Object.hasOwnProperty.call(message, "systemDelete")) {
                object.systemDelete = $root.proto.SystemDeleteTransactionBody.toObject(message.systemDelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "systemDelete";
            }
            if (message.systemUndelete != null && $Object.hasOwnProperty.call(message, "systemUndelete")) {
                object.systemUndelete = $root.proto.SystemUndeleteTransactionBody.toObject(message.systemUndelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "systemUndelete";
            }
            if (message.freeze != null && $Object.hasOwnProperty.call(message, "freeze")) {
                object.freeze = $root.proto.FreezeTransactionBody.toObject(message.freeze, options, _depth + 1);
                if (options.oneofs)
                    object.data = "freeze";
            }
            if (message.consensusCreateTopic != null && $Object.hasOwnProperty.call(message, "consensusCreateTopic")) {
                object.consensusCreateTopic = $root.proto.ConsensusCreateTopicTransactionBody.toObject(message.consensusCreateTopic, options, _depth + 1);
                if (options.oneofs)
                    object.data = "consensusCreateTopic";
            }
            if (message.consensusUpdateTopic != null && $Object.hasOwnProperty.call(message, "consensusUpdateTopic")) {
                object.consensusUpdateTopic = $root.proto.ConsensusUpdateTopicTransactionBody.toObject(message.consensusUpdateTopic, options, _depth + 1);
                if (options.oneofs)
                    object.data = "consensusUpdateTopic";
            }
            if (message.consensusDeleteTopic != null && $Object.hasOwnProperty.call(message, "consensusDeleteTopic")) {
                object.consensusDeleteTopic = $root.proto.ConsensusDeleteTopicTransactionBody.toObject(message.consensusDeleteTopic, options, _depth + 1);
                if (options.oneofs)
                    object.data = "consensusDeleteTopic";
            }
            if (message.consensusSubmitMessage != null && $Object.hasOwnProperty.call(message, "consensusSubmitMessage")) {
                object.consensusSubmitMessage = $root.proto.ConsensusSubmitMessageTransactionBody.toObject(message.consensusSubmitMessage, options, _depth + 1);
                if (options.oneofs)
                    object.data = "consensusSubmitMessage";
            }
            if (message.tokenCreation != null && $Object.hasOwnProperty.call(message, "tokenCreation")) {
                object.tokenCreation = $root.proto.TokenCreateTransactionBody.toObject(message.tokenCreation, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenCreation";
            }
            if (message.tokenFreeze != null && $Object.hasOwnProperty.call(message, "tokenFreeze")) {
                object.tokenFreeze = $root.proto.TokenFreezeAccountTransactionBody.toObject(message.tokenFreeze, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenFreeze";
            }
            if (message.tokenUnfreeze != null && $Object.hasOwnProperty.call(message, "tokenUnfreeze")) {
                object.tokenUnfreeze = $root.proto.TokenUnfreezeAccountTransactionBody.toObject(message.tokenUnfreeze, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenUnfreeze";
            }
            if (message.tokenGrantKyc != null && $Object.hasOwnProperty.call(message, "tokenGrantKyc")) {
                object.tokenGrantKyc = $root.proto.TokenGrantKycTransactionBody.toObject(message.tokenGrantKyc, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenGrantKyc";
            }
            if (message.tokenRevokeKyc != null && $Object.hasOwnProperty.call(message, "tokenRevokeKyc")) {
                object.tokenRevokeKyc = $root.proto.TokenRevokeKycTransactionBody.toObject(message.tokenRevokeKyc, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenRevokeKyc";
            }
            if (message.tokenDeletion != null && $Object.hasOwnProperty.call(message, "tokenDeletion")) {
                object.tokenDeletion = $root.proto.TokenDeleteTransactionBody.toObject(message.tokenDeletion, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenDeletion";
            }
            if (message.tokenUpdate != null && $Object.hasOwnProperty.call(message, "tokenUpdate")) {
                object.tokenUpdate = $root.proto.TokenUpdateTransactionBody.toObject(message.tokenUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenUpdate";
            }
            if (message.tokenMint != null && $Object.hasOwnProperty.call(message, "tokenMint")) {
                object.tokenMint = $root.proto.TokenMintTransactionBody.toObject(message.tokenMint, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenMint";
            }
            if (message.tokenBurn != null && $Object.hasOwnProperty.call(message, "tokenBurn")) {
                object.tokenBurn = $root.proto.TokenBurnTransactionBody.toObject(message.tokenBurn, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenBurn";
            }
            if (message.tokenWipe != null && $Object.hasOwnProperty.call(message, "tokenWipe")) {
                object.tokenWipe = $root.proto.TokenWipeAccountTransactionBody.toObject(message.tokenWipe, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenWipe";
            }
            if (message.tokenAssociate != null && $Object.hasOwnProperty.call(message, "tokenAssociate")) {
                object.tokenAssociate = $root.proto.TokenAssociateTransactionBody.toObject(message.tokenAssociate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenAssociate";
            }
            if (message.tokenDissociate != null && $Object.hasOwnProperty.call(message, "tokenDissociate")) {
                object.tokenDissociate = $root.proto.TokenDissociateTransactionBody.toObject(message.tokenDissociate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenDissociate";
            }
            if (message.scheduleDelete != null && $Object.hasOwnProperty.call(message, "scheduleDelete")) {
                object.scheduleDelete = $root.proto.ScheduleDeleteTransactionBody.toObject(message.scheduleDelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "scheduleDelete";
            }
            if (message.tokenPause != null && $Object.hasOwnProperty.call(message, "tokenPause")) {
                object.tokenPause = $root.proto.TokenPauseTransactionBody.toObject(message.tokenPause, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenPause";
            }
            if (message.tokenUnpause != null && $Object.hasOwnProperty.call(message, "tokenUnpause")) {
                object.tokenUnpause = $root.proto.TokenUnpauseTransactionBody.toObject(message.tokenUnpause, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenUnpause";
            }
            if (message.cryptoApproveAllowance != null && $Object.hasOwnProperty.call(message, "cryptoApproveAllowance")) {
                object.cryptoApproveAllowance = $root.proto.CryptoApproveAllowanceTransactionBody.toObject(message.cryptoApproveAllowance, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoApproveAllowance";
            }
            if (message.cryptoDeleteAllowance != null && $Object.hasOwnProperty.call(message, "cryptoDeleteAllowance")) {
                object.cryptoDeleteAllowance = $root.proto.CryptoDeleteAllowanceTransactionBody.toObject(message.cryptoDeleteAllowance, options, _depth + 1);
                if (options.oneofs)
                    object.data = "cryptoDeleteAllowance";
            }
            if (message.tokenFeeScheduleUpdate != null && $Object.hasOwnProperty.call(message, "tokenFeeScheduleUpdate")) {
                object.tokenFeeScheduleUpdate = $root.proto.TokenFeeScheduleUpdateTransactionBody.toObject(message.tokenFeeScheduleUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenFeeScheduleUpdate";
            }
            if (message.utilPrng != null && $Object.hasOwnProperty.call(message, "utilPrng")) {
                object.utilPrng = $root.proto.UtilPrngTransactionBody.toObject(message.utilPrng, options, _depth + 1);
                if (options.oneofs)
                    object.data = "utilPrng";
            }
            if (message.tokenUpdateNfts != null && $Object.hasOwnProperty.call(message, "tokenUpdateNfts")) {
                object.tokenUpdateNfts = $root.proto.TokenUpdateNftsTransactionBody.toObject(message.tokenUpdateNfts, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenUpdateNfts";
            }
            if (message.nodeCreate != null && $Object.hasOwnProperty.call(message, "nodeCreate")) {
                object.nodeCreate = $root.com.hedera.hapi.node.addressbook.NodeCreateTransactionBody.toObject(message.nodeCreate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "nodeCreate";
            }
            if (message.nodeUpdate != null && $Object.hasOwnProperty.call(message, "nodeUpdate")) {
                object.nodeUpdate = $root.com.hedera.hapi.node.addressbook.NodeUpdateTransactionBody.toObject(message.nodeUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "nodeUpdate";
            }
            if (message.nodeDelete != null && $Object.hasOwnProperty.call(message, "nodeDelete")) {
                object.nodeDelete = $root.com.hedera.hapi.node.addressbook.NodeDeleteTransactionBody.toObject(message.nodeDelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "nodeDelete";
            }
            if (message.tokenReject != null && $Object.hasOwnProperty.call(message, "tokenReject")) {
                object.tokenReject = $root.proto.TokenRejectTransactionBody.toObject(message.tokenReject, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenReject";
            }
            if (message.tokenCancelAirdrop != null && $Object.hasOwnProperty.call(message, "tokenCancelAirdrop")) {
                object.tokenCancelAirdrop = $root.proto.TokenCancelAirdropTransactionBody.toObject(message.tokenCancelAirdrop, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenCancelAirdrop";
            }
            if (message.tokenClaimAirdrop != null && $Object.hasOwnProperty.call(message, "tokenClaimAirdrop")) {
                object.tokenClaimAirdrop = $root.proto.TokenClaimAirdropTransactionBody.toObject(message.tokenClaimAirdrop, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenClaimAirdrop";
            }
            if (message.tokenAirdrop != null && $Object.hasOwnProperty.call(message, "tokenAirdrop")) {
                object.tokenAirdrop = $root.proto.TokenAirdropTransactionBody.toObject(message.tokenAirdrop, options, _depth + 1);
                if (options.oneofs)
                    object.data = "tokenAirdrop";
            }
            if (message.registeredNodeCreate != null && $Object.hasOwnProperty.call(message, "registeredNodeCreate")) {
                object.registeredNodeCreate = $root.com.hedera.hapi.node.addressbook.RegisteredNodeCreateTransactionBody.toObject(message.registeredNodeCreate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "registeredNodeCreate";
            }
            if (message.registeredNodeUpdate != null && $Object.hasOwnProperty.call(message, "registeredNodeUpdate")) {
                object.registeredNodeUpdate = $root.com.hedera.hapi.node.addressbook.RegisteredNodeUpdateTransactionBody.toObject(message.registeredNodeUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.data = "registeredNodeUpdate";
            }
            if (message.registeredNodeDelete != null && $Object.hasOwnProperty.call(message, "registeredNodeDelete")) {
                object.registeredNodeDelete = $root.com.hedera.hapi.node.addressbook.RegisteredNodeDeleteTransactionBody.toObject(message.registeredNodeDelete, options, _depth + 1);
                if (options.oneofs)
                    object.data = "registeredNodeDelete";
            }
            if (message.maxCustomFees && message.maxCustomFees.length) {
                object.maxCustomFees = $Array(message.maxCustomFees.length);
                for (let j = 0; j < message.maxCustomFees.length; ++j)
                    object.maxCustomFees[j] = $root.proto.CustomFeeLimit.toObject(message.maxCustomFees[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this SchedulableTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.SchedulableTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SchedulableTransactionBody.prototype.toJSON = function() {
            return SchedulableTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SchedulableTransactionBody
         * @function getTypeUrl
         * @memberof proto.SchedulableTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SchedulableTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SchedulableTransactionBody";
        };

        return SchedulableTransactionBody;
    })();

    proto.ScheduleDeleteTransactionBody = (function() {

        /**
         * Properties of a ScheduleDeleteTransactionBody.
         * @typedef {Object} proto.ScheduleDeleteTransactionBody.$Properties
         * @property {proto.ScheduleID.$Properties|null} [scheduleID] A schedule identifier.
         * <p>
         * This MUST identify the schedule which SHALL be deleted.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ScheduleDeleteTransactionBody.
         * @memberof proto
         * @interface IScheduleDeleteTransactionBody
         * @augments proto.ScheduleDeleteTransactionBody.$Properties
         * @deprecated Use proto.ScheduleDeleteTransactionBody.$Properties instead.
         */

        /**
         * Shape of a ScheduleDeleteTransactionBody.
         * @typedef {proto.ScheduleDeleteTransactionBody.$Properties} proto.ScheduleDeleteTransactionBody.$Shape
         */

        /**
         * Constructs a new ScheduleDeleteTransactionBody.
         * @memberof proto
         * @classdesc Mark a schedule in the network state as deleted.
         * 
         * This transaction MUST be signed by the `adminKey` for the
         * identified schedule.<br/>
         * If a schedule does not have `adminKey` set or if `adminKey` is an empty
         * `KeyList`, that schedule SHALL be immutable and MUST NOT be deleted.<br/>
         * A deleted schedule SHALL not be executed.<br/>
         * A deleted schedule MUST NOT be the subject of a subsequent
         * `scheduleSign` transaction.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.ScheduleDeleteTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ScheduleDeleteTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A schedule identifier.
         * <p>
         * This MUST identify the schedule which SHALL be deleted.
         * @member {proto.ScheduleID.$Properties|null|undefined} scheduleID
         * @memberof proto.ScheduleDeleteTransactionBody
         * @instance
         */
        ScheduleDeleteTransactionBody.prototype.scheduleID = null;

        /**
         * Creates a new ScheduleDeleteTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.ScheduleDeleteTransactionBody
         * @static
         * @param {proto.ScheduleDeleteTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.ScheduleDeleteTransactionBody} ScheduleDeleteTransactionBody instance
         * @type {{
         *   (properties: proto.ScheduleDeleteTransactionBody.$Shape): proto.ScheduleDeleteTransactionBody & proto.ScheduleDeleteTransactionBody.$Shape;
         *   (properties?: proto.ScheduleDeleteTransactionBody.$Properties): proto.ScheduleDeleteTransactionBody;
         * }}
         */
        ScheduleDeleteTransactionBody.create = function(properties) {
            return new ScheduleDeleteTransactionBody(properties);
        };

        /**
         * Encodes the specified ScheduleDeleteTransactionBody message. Does not implicitly {@link proto.ScheduleDeleteTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.ScheduleDeleteTransactionBody
         * @static
         * @param {proto.ScheduleDeleteTransactionBody.$Properties} message ScheduleDeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleDeleteTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID"))
                $root.proto.ScheduleID.encode(message.scheduleID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ScheduleDeleteTransactionBody message, length delimited. Does not implicitly {@link proto.ScheduleDeleteTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ScheduleDeleteTransactionBody
         * @static
         * @param {proto.ScheduleDeleteTransactionBody.$Properties} message ScheduleDeleteTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleDeleteTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ScheduleDeleteTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ScheduleDeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ScheduleDeleteTransactionBody & proto.ScheduleDeleteTransactionBody.$Shape} ScheduleDeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleDeleteTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ScheduleDeleteTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.scheduleID = $root.proto.ScheduleID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ScheduleDeleteTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ScheduleDeleteTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ScheduleDeleteTransactionBody & proto.ScheduleDeleteTransactionBody.$Shape} ScheduleDeleteTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleDeleteTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScheduleDeleteTransactionBody message.
         * @function verify
         * @memberof proto.ScheduleDeleteTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScheduleDeleteTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID")) {
                let error = $root.proto.ScheduleID.verify(message.scheduleID, _depth + 1);
                if (error)
                    return "scheduleID." + error;
            }
            return null;
        };

        /**
         * Creates a ScheduleDeleteTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ScheduleDeleteTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ScheduleDeleteTransactionBody} ScheduleDeleteTransactionBody
         */
        ScheduleDeleteTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ScheduleDeleteTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ScheduleDeleteTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ScheduleDeleteTransactionBody();
            if (object.scheduleID != null) {
                if (!$util.isObject(object.scheduleID))
                    throw $TypeError(".proto.ScheduleDeleteTransactionBody.scheduleID: object expected");
                message.scheduleID = $root.proto.ScheduleID.fromObject(object.scheduleID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ScheduleDeleteTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ScheduleDeleteTransactionBody
         * @static
         * @param {proto.ScheduleDeleteTransactionBody} message ScheduleDeleteTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScheduleDeleteTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.scheduleID = null;
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID"))
                object.scheduleID = $root.proto.ScheduleID.toObject(message.scheduleID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ScheduleDeleteTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.ScheduleDeleteTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScheduleDeleteTransactionBody.prototype.toJSON = function() {
            return ScheduleDeleteTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ScheduleDeleteTransactionBody
         * @function getTypeUrl
         * @memberof proto.ScheduleDeleteTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ScheduleDeleteTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ScheduleDeleteTransactionBody";
        };

        return ScheduleDeleteTransactionBody;
    })();

    proto.UtilPrngTransactionBody = (function() {

        /**
         * Properties of an UtilPrngTransactionBody.
         * @typedef {Object} proto.UtilPrngTransactionBody.$Properties
         * @property {number|null} [range] A range for the requested value.
         * <p>
         * If this is greater than `0`, the service SHALL return a 32-bit
         * pseudo-random number between 0 and the value provided in the
         * transaction record.<br/>
         * If this is unset, zero, or negative; the service SHALL return a
         * 384-bit unsigned pseudo-random number in the record.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an UtilPrngTransactionBody.
         * @memberof proto
         * @interface IUtilPrngTransactionBody
         * @augments proto.UtilPrngTransactionBody.$Properties
         * @deprecated Use proto.UtilPrngTransactionBody.$Properties instead.
         */

        /**
         * Shape of an UtilPrngTransactionBody.
         * @typedef {proto.UtilPrngTransactionBody.$Properties} proto.UtilPrngTransactionBody.$Shape
         */

        /**
         * Constructs a new UtilPrngTransactionBody.
         * @memberof proto
         * @classdesc Request a deterministic pseudo-random number.
         * 
         * The value returned SHALL be deterministic, but not easily predicted.
         * The value returned SHALL NOT be suitable for cryptographic use.
         * 
         * ### Block Stream Effects
         * The result of this transaction is reported in a `UtilPrngOutput` message.
         * @constructor
         * @param {proto.UtilPrngTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const UtilPrngTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A range for the requested value.
         * <p>
         * If this is greater than `0`, the service SHALL return a 32-bit
         * pseudo-random number between 0 and the value provided in the
         * transaction record.<br/>
         * If this is unset, zero, or negative; the service SHALL return a
         * 384-bit unsigned pseudo-random number in the record.
         * @member {number} range
         * @memberof proto.UtilPrngTransactionBody
         * @instance
         */
        UtilPrngTransactionBody.prototype.range = 0;

        /**
         * Creates a new UtilPrngTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.UtilPrngTransactionBody
         * @static
         * @param {proto.UtilPrngTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.UtilPrngTransactionBody} UtilPrngTransactionBody instance
         * @type {{
         *   (properties: proto.UtilPrngTransactionBody.$Shape): proto.UtilPrngTransactionBody & proto.UtilPrngTransactionBody.$Shape;
         *   (properties?: proto.UtilPrngTransactionBody.$Properties): proto.UtilPrngTransactionBody;
         * }}
         */
        UtilPrngTransactionBody.create = function(properties) {
            return new UtilPrngTransactionBody(properties);
        };

        /**
         * Encodes the specified UtilPrngTransactionBody message. Does not implicitly {@link proto.UtilPrngTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.UtilPrngTransactionBody
         * @static
         * @param {proto.UtilPrngTransactionBody.$Properties} message UtilPrngTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UtilPrngTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.range != null && $Object.hasOwnProperty.call(message, "range") && message.range !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.range);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UtilPrngTransactionBody message, length delimited. Does not implicitly {@link proto.UtilPrngTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.UtilPrngTransactionBody
         * @static
         * @param {proto.UtilPrngTransactionBody.$Properties} message UtilPrngTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UtilPrngTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an UtilPrngTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.UtilPrngTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.UtilPrngTransactionBody & proto.UtilPrngTransactionBody.$Shape} UtilPrngTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UtilPrngTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.UtilPrngTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.range = value;
                        else
                            delete message.range;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an UtilPrngTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.UtilPrngTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.UtilPrngTransactionBody & proto.UtilPrngTransactionBody.$Shape} UtilPrngTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UtilPrngTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an UtilPrngTransactionBody message.
         * @function verify
         * @memberof proto.UtilPrngTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UtilPrngTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.range != null && $Object.hasOwnProperty.call(message, "range"))
                if (!$util.isInteger(message.range))
                    return "range: integer expected";
            return null;
        };

        /**
         * Creates an UtilPrngTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.UtilPrngTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.UtilPrngTransactionBody} UtilPrngTransactionBody
         */
        UtilPrngTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.UtilPrngTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.UtilPrngTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.UtilPrngTransactionBody();
            if (object.range != null)
                if ($Number(object.range) !== 0)
                    message.range = object.range | 0;
            return message;
        };

        /**
         * Creates a plain object from an UtilPrngTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.UtilPrngTransactionBody
         * @static
         * @param {proto.UtilPrngTransactionBody} message UtilPrngTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UtilPrngTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.range = 0;
            if (message.range != null && $Object.hasOwnProperty.call(message, "range"))
                object.range = message.range;
            return object;
        };

        /**
         * Converts this UtilPrngTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.UtilPrngTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UtilPrngTransactionBody.prototype.toJSON = function() {
            return UtilPrngTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for UtilPrngTransactionBody
         * @function getTypeUrl
         * @memberof proto.UtilPrngTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        UtilPrngTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.UtilPrngTransactionBody";
        };

        return UtilPrngTransactionBody;
    })();

    proto.ScheduleSignTransactionBody = (function() {

        /**
         * Properties of a ScheduleSignTransactionBody.
         * @typedef {Object} proto.ScheduleSignTransactionBody.$Properties
         * @property {proto.ScheduleID.$Properties|null} [scheduleID] A schedule identifier.
         * <p>
         * This MUST identify the schedule to which signatures SHALL be added.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ScheduleSignTransactionBody.
         * @memberof proto
         * @interface IScheduleSignTransactionBody
         * @augments proto.ScheduleSignTransactionBody.$Properties
         * @deprecated Use proto.ScheduleSignTransactionBody.$Properties instead.
         */

        /**
         * Shape of a ScheduleSignTransactionBody.
         * @typedef {proto.ScheduleSignTransactionBody.$Properties} proto.ScheduleSignTransactionBody.$Shape
         */

        /**
         * Constructs a new ScheduleSignTransactionBody.
         * @memberof proto
         * @classdesc Add signatures to an existing scheduled transaction.
         * 
         * When a schedule _executes_ successfully, the receipt SHALL include a
         * `scheduledTransactionID` with the `TransactionID` of the transaction that
         * executed.<br/>
         * When a scheduled transaction is executed the network SHALL charge the
         * regular _service_ fee for the transaction to the `payerAccountID` for
         * that schedule, but SHALL NOT charge node or network fees.<br/>
         * If the `payerAccountID` field is not set, the effective `payerAccountID`
         * SHALL be the `payer` for this create transaction.<br/>
         * Each signature on this transaction SHALL "activate" the corresponding
         * cryptographic("primitive") key for the schedule identified.<br/>
         * Signature requirements SHALL be met when the set of active keys includes
         * all keys required by the scheduled transaction.<br/>
         * A scheduled transaction for a "long term" schedule SHALL NOT execute if
         * the signature requirements for that transaction are not met when the
         * network consensus time reaches the schedule `expiration_time`.<br/>
         * A "short term" schedule SHALL execute immediately once signature
         * requirements are met. This MAY be immediately when created.<br/>
         * 
         * ### Block Stream Effects
         * If the scheduled transaction is executed immediately following this
         * `scheduleSign` transaction, the transaction record SHALL include a
         * `scheduleRef` with the schedule identifier `scheduleID`.
         * @constructor
         * @param {proto.ScheduleSignTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ScheduleSignTransactionBody = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A schedule identifier.
         * <p>
         * This MUST identify the schedule to which signatures SHALL be added.
         * @member {proto.ScheduleID.$Properties|null|undefined} scheduleID
         * @memberof proto.ScheduleSignTransactionBody
         * @instance
         */
        ScheduleSignTransactionBody.prototype.scheduleID = null;

        /**
         * Creates a new ScheduleSignTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.ScheduleSignTransactionBody
         * @static
         * @param {proto.ScheduleSignTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.ScheduleSignTransactionBody} ScheduleSignTransactionBody instance
         * @type {{
         *   (properties: proto.ScheduleSignTransactionBody.$Shape): proto.ScheduleSignTransactionBody & proto.ScheduleSignTransactionBody.$Shape;
         *   (properties?: proto.ScheduleSignTransactionBody.$Properties): proto.ScheduleSignTransactionBody;
         * }}
         */
        ScheduleSignTransactionBody.create = function(properties) {
            return new ScheduleSignTransactionBody(properties);
        };

        /**
         * Encodes the specified ScheduleSignTransactionBody message. Does not implicitly {@link proto.ScheduleSignTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.ScheduleSignTransactionBody
         * @static
         * @param {proto.ScheduleSignTransactionBody.$Properties} message ScheduleSignTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleSignTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID"))
                $root.proto.ScheduleID.encode(message.scheduleID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ScheduleSignTransactionBody message, length delimited. Does not implicitly {@link proto.ScheduleSignTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ScheduleSignTransactionBody
         * @static
         * @param {proto.ScheduleSignTransactionBody.$Properties} message ScheduleSignTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleSignTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ScheduleSignTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ScheduleSignTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ScheduleSignTransactionBody & proto.ScheduleSignTransactionBody.$Shape} ScheduleSignTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleSignTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ScheduleSignTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.scheduleID = $root.proto.ScheduleID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ScheduleSignTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ScheduleSignTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ScheduleSignTransactionBody & proto.ScheduleSignTransactionBody.$Shape} ScheduleSignTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleSignTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScheduleSignTransactionBody message.
         * @function verify
         * @memberof proto.ScheduleSignTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScheduleSignTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID")) {
                let error = $root.proto.ScheduleID.verify(message.scheduleID, _depth + 1);
                if (error)
                    return "scheduleID." + error;
            }
            return null;
        };

        /**
         * Creates a ScheduleSignTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ScheduleSignTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ScheduleSignTransactionBody} ScheduleSignTransactionBody
         */
        ScheduleSignTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ScheduleSignTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ScheduleSignTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ScheduleSignTransactionBody();
            if (object.scheduleID != null) {
                if (!$util.isObject(object.scheduleID))
                    throw $TypeError(".proto.ScheduleSignTransactionBody.scheduleID: object expected");
                message.scheduleID = $root.proto.ScheduleID.fromObject(object.scheduleID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ScheduleSignTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ScheduleSignTransactionBody
         * @static
         * @param {proto.ScheduleSignTransactionBody} message ScheduleSignTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScheduleSignTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.scheduleID = null;
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID"))
                object.scheduleID = $root.proto.ScheduleID.toObject(message.scheduleID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ScheduleSignTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.ScheduleSignTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScheduleSignTransactionBody.prototype.toJSON = function() {
            return ScheduleSignTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ScheduleSignTransactionBody
         * @function getTypeUrl
         * @memberof proto.ScheduleSignTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ScheduleSignTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ScheduleSignTransactionBody";
        };

        return ScheduleSignTransactionBody;
    })();

    proto.NodeStakeUpdateTransactionBody = (function() {

        /**
         * Properties of a NodeStakeUpdateTransactionBody.
         * @typedef {Object} proto.NodeStakeUpdateTransactionBody.$Properties
         * @property {proto.Timestamp.$Properties|null} [endOfStakingPeriod] A timestamp indicating the end of the staking period.
         * <p>
         * This value SHALL be one nanosecond prior to midnight prior to the
         * consensus time of the parent transaction.
         * @property {Array.<proto.NodeStake.$Properties>|null} [nodeStake] A list of `NodeStake` entries for each node at the beginning of the new
         * staking period.
         * <p>
         * This list SHALL have one entry for each node participating in network
         * consensus.
         * @property {number|Long|null} [maxStakingRewardRatePerHbar] A maximum reward rate for this staking period.
         * <p>
         * This SHALL be a ratio of tinybar to HBAR.<br/>
         * An account SHALL NOT receive a reward greater than the product of this
         * ratio and the total number of HBAR staked by that account.
         * @property {proto.Fraction.$Properties|null} [nodeRewardFeeFraction] A fraction of network and service fees paid to
         * the "node" reward account.<br/>
         * The node staking rewards are paid from the designated reward account
         * `0.0.801`, which receives a fraction of network and service fees for
         * each transaction. This field is the value of that fraction for the
         * last staking period.
         * @property {number|Long|null} [stakingPeriodsStored] A limit to the number of staking periods held for inactive accounts.<br/>
         * This is the maximum number of trailing staking periods for which an
         * account can collect staking rewards.<br/>
         * #### Example
         * If this value is 365 with a calendar day period, then each account must
         * collect rewards at least once per calendar year to receive the full
         * amount of staking rewards earned.
         * <p>
         * Staking rewards SHALL be stored in network state for no more than
         * `staking_periods_stored` staking periods.<br/>
         * Each account MUST participate in at least one transaction that affects
         * its balance, staking, or staking metadata within this time limit
         * to receive all available staking rewards.
         * @property {number|Long|null} [stakingPeriod] A number of minutes representing a staking period.<br/>
         * <blockquote>Note<blockquote>
         * For the special case of `1440` minutes, periods are treated as
         * calendar days aligned to midnight UTC, rather than repeating `1440`
         * minute periods left-aligned at the epoch.</blockquote></blockquote>
         * @property {proto.Fraction.$Properties|null} [stakingRewardFeeFraction] A fraction of network and service fees paid to
         * the "general" reward account.<br/>
         * The general staking rewards are paid from the designated reward account
         * `0.0.800`, which receives a fraction of network and service fees for
         * each transaction. This field is the value of that fraction for the
         * last staking period.
         * @property {number|Long|null} [stakingStartThreshold] A minimum balance required to pay general staking rewards.
         * <p>
         * If the balance of the staking reward account `0.0.800` is below this
         * threshold, staking rewards SHALL NOT be paid in full.
         * @property {number|Long|null} [stakingRewardRate] HIP-786 replaced this field with `max_total_reward`.<br/>
         * This was a maximum total number of tinybars to be distributed as staking
         * rewards in the staking period that just ended.
         * @property {number|Long|null} [reservedStakingRewards] An amount reserved in the staking reward account.<br/>
         * This is an amount "reserved" in the balance of account `0.0.800` that
         * is already owed for pending rewards that were previously earned but
         * have not yet been collected.<br/>
         * This value is further detailed in HIP-786.
         * @property {number|Long|null} [unreservedStakingRewardBalance] An available, unreserved, amount in the staking reward account.<br/>
         * This is the balance of the staking reward account `0.0.800` at the close
         * of the staking period that just ended, after reduction for all "reserved"
         * funds necessary to pay previously earned rewards.<br/>
         * This value is further detailed in HIP-786.
         * <p>
         * This value SHALL be used to calculate the reward ratio according to
         * the formula detailed in HIP-782.
         * @property {number|Long|null} [rewardBalanceThreshold] A minimum balance required for maximum staking rewards.<br/>
         * This value is further detailed in HIP-786.
         * The formula to calculate staking rewards is detailed in HIP-782.
         * <p>
         * The value of `unreserved_staking_reward_balance` MUST match or exceed the
         * value of this field to support the maximum staking reward ratio.<br/>
         * @property {number|Long|null} [maxStakeRewarded] A maximum network-wide stake that can earn full rewards.<br/>
         * If the network-wide stake, in tinybar, exceeds this value, then staking
         * rewards must be reduced to maintain "smooth" reward adjustments as
         * defined in HIP-782.<br/>
         * This value is further detailed in HIP-786.
         * <p>
         * If the total network-wide stake exceeds this value, the effective
         * staking reward ratio MUST be reduced to maintain solvency of the
         * staking reward account.
         * @property {number|Long|null} [maxTotalReward] A limit amount that could be paid as staking rewards.<br/>
         * In the limit case, the network could pay at most this amount, in tinybar
         * as staking rewards for the staking period that just ended, if all other
         * conditions were met to perfection.<br/>
         * <p>
         * This value SHALL reflect the result of a maximum reward calculation that
         * takes into account the balance thresholds and maximum stake thresholds
         * as defined in HIP-782 and HIP-786.<br/>
         * This value is a convenience. The actual defined calculation SHALL be
         * authoritative in the unlikely event this value differs.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NodeStakeUpdateTransactionBody.
         * @memberof proto
         * @interface INodeStakeUpdateTransactionBody
         * @augments proto.NodeStakeUpdateTransactionBody.$Properties
         * @deprecated Use proto.NodeStakeUpdateTransactionBody.$Properties instead.
         */

        /**
         * Shape of a NodeStakeUpdateTransactionBody.
         * @typedef {proto.NodeStakeUpdateTransactionBody.$Properties} proto.NodeStakeUpdateTransactionBody.$Shape
         */

        /**
         * Constructs a new NodeStakeUpdateTransactionBody.
         * @memberof proto
         * @classdesc A system initiated transaction to update staking information.
         * 
         * This transaction SHALL be issued at the end of each staking period to update
         * node stakes and reward limits.<br/>
         * This transaction SHALL be a child of the first transaction to reach
         * consensus following the end of the previous staking period.<br/>
         * This transaction MUST NOT be sent by a client and SHALL be rejected if
         * received by any node.<br/>
         * This transaction SHALL be present in the record stream or block stream.
         * 
         * ### Block Stream Effects
         * None
         * @constructor
         * @param {proto.NodeStakeUpdateTransactionBody.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NodeStakeUpdateTransactionBody = function (properties) {
            this.nodeStake = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A timestamp indicating the end of the staking period.
         * <p>
         * This value SHALL be one nanosecond prior to midnight prior to the
         * consensus time of the parent transaction.
         * @member {proto.Timestamp.$Properties|null|undefined} endOfStakingPeriod
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.endOfStakingPeriod = null;

        /**
         * A list of `NodeStake` entries for each node at the beginning of the new
         * staking period.
         * <p>
         * This list SHALL have one entry for each node participating in network
         * consensus.
         * @member {Array.<proto.NodeStake.$Properties>} nodeStake
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.nodeStake = $util.emptyArray;

        /**
         * A maximum reward rate for this staking period.
         * <p>
         * This SHALL be a ratio of tinybar to HBAR.<br/>
         * An account SHALL NOT receive a reward greater than the product of this
         * ratio and the total number of HBAR staked by that account.
         * @member {number|Long} maxStakingRewardRatePerHbar
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.maxStakingRewardRatePerHbar = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A fraction of network and service fees paid to
         * the "node" reward account.<br/>
         * The node staking rewards are paid from the designated reward account
         * `0.0.801`, which receives a fraction of network and service fees for
         * each transaction. This field is the value of that fraction for the
         * last staking period.
         * @member {proto.Fraction.$Properties|null|undefined} nodeRewardFeeFraction
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.nodeRewardFeeFraction = null;

        /**
         * A limit to the number of staking periods held for inactive accounts.<br/>
         * This is the maximum number of trailing staking periods for which an
         * account can collect staking rewards.<br/>
         * #### Example
         * If this value is 365 with a calendar day period, then each account must
         * collect rewards at least once per calendar year to receive the full
         * amount of staking rewards earned.
         * <p>
         * Staking rewards SHALL be stored in network state for no more than
         * `staking_periods_stored` staking periods.<br/>
         * Each account MUST participate in at least one transaction that affects
         * its balance, staking, or staking metadata within this time limit
         * to receive all available staking rewards.
         * @member {number|Long} stakingPeriodsStored
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.stakingPeriodsStored = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A number of minutes representing a staking period.<br/>
         * <blockquote>Note<blockquote>
         * For the special case of `1440` minutes, periods are treated as
         * calendar days aligned to midnight UTC, rather than repeating `1440`
         * minute periods left-aligned at the epoch.</blockquote></blockquote>
         * @member {number|Long} stakingPeriod
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.stakingPeriod = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A fraction of network and service fees paid to
         * the "general" reward account.<br/>
         * The general staking rewards are paid from the designated reward account
         * `0.0.800`, which receives a fraction of network and service fees for
         * each transaction. This field is the value of that fraction for the
         * last staking period.
         * @member {proto.Fraction.$Properties|null|undefined} stakingRewardFeeFraction
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.stakingRewardFeeFraction = null;

        /**
         * A minimum balance required to pay general staking rewards.
         * <p>
         * If the balance of the staking reward account `0.0.800` is below this
         * threshold, staking rewards SHALL NOT be paid in full.
         * @member {number|Long} stakingStartThreshold
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.stakingStartThreshold = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * HIP-786 replaced this field with `max_total_reward`.<br/>
         * This was a maximum total number of tinybars to be distributed as staking
         * rewards in the staking period that just ended.
         * @member {number|Long} stakingRewardRate
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.stakingRewardRate = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An amount reserved in the staking reward account.<br/>
         * This is an amount "reserved" in the balance of account `0.0.800` that
         * is already owed for pending rewards that were previously earned but
         * have not yet been collected.<br/>
         * This value is further detailed in HIP-786.
         * @member {number|Long} reservedStakingRewards
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.reservedStakingRewards = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An available, unreserved, amount in the staking reward account.<br/>
         * This is the balance of the staking reward account `0.0.800` at the close
         * of the staking period that just ended, after reduction for all "reserved"
         * funds necessary to pay previously earned rewards.<br/>
         * This value is further detailed in HIP-786.
         * <p>
         * This value SHALL be used to calculate the reward ratio according to
         * the formula detailed in HIP-782.
         * @member {number|Long} unreservedStakingRewardBalance
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.unreservedStakingRewardBalance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A minimum balance required for maximum staking rewards.<br/>
         * This value is further detailed in HIP-786.
         * The formula to calculate staking rewards is detailed in HIP-782.
         * <p>
         * The value of `unreserved_staking_reward_balance` MUST match or exceed the
         * value of this field to support the maximum staking reward ratio.<br/>
         * @member {number|Long} rewardBalanceThreshold
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.rewardBalanceThreshold = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A maximum network-wide stake that can earn full rewards.<br/>
         * If the network-wide stake, in tinybar, exceeds this value, then staking
         * rewards must be reduced to maintain "smooth" reward adjustments as
         * defined in HIP-782.<br/>
         * This value is further detailed in HIP-786.
         * <p>
         * If the total network-wide stake exceeds this value, the effective
         * staking reward ratio MUST be reduced to maintain solvency of the
         * staking reward account.
         * @member {number|Long} maxStakeRewarded
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.maxStakeRewarded = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A limit amount that could be paid as staking rewards.<br/>
         * In the limit case, the network could pay at most this amount, in tinybar
         * as staking rewards for the staking period that just ended, if all other
         * conditions were met to perfection.<br/>
         * <p>
         * This value SHALL reflect the result of a maximum reward calculation that
         * takes into account the balance thresholds and maximum stake thresholds
         * as defined in HIP-782 and HIP-786.<br/>
         * This value is a convenience. The actual defined calculation SHALL be
         * authoritative in the unlikely event this value differs.
         * @member {number|Long} maxTotalReward
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         */
        NodeStakeUpdateTransactionBody.prototype.maxTotalReward = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new NodeStakeUpdateTransactionBody instance using the specified properties.
         * @function create
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @static
         * @param {proto.NodeStakeUpdateTransactionBody.$Properties=} [properties] Properties to set
         * @returns {proto.NodeStakeUpdateTransactionBody} NodeStakeUpdateTransactionBody instance
         * @type {{
         *   (properties: proto.NodeStakeUpdateTransactionBody.$Shape): proto.NodeStakeUpdateTransactionBody & proto.NodeStakeUpdateTransactionBody.$Shape;
         *   (properties?: proto.NodeStakeUpdateTransactionBody.$Properties): proto.NodeStakeUpdateTransactionBody;
         * }}
         */
        NodeStakeUpdateTransactionBody.create = function(properties) {
            return new NodeStakeUpdateTransactionBody(properties);
        };

        /**
         * Encodes the specified NodeStakeUpdateTransactionBody message. Does not implicitly {@link proto.NodeStakeUpdateTransactionBody.verify|verify} messages.
         * @function encode
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @static
         * @param {proto.NodeStakeUpdateTransactionBody.$Properties} message NodeStakeUpdateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeStakeUpdateTransactionBody.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.endOfStakingPeriod != null && $Object.hasOwnProperty.call(message, "endOfStakingPeriod"))
                $root.proto.Timestamp.encode(message.endOfStakingPeriod, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.nodeStake != null && message.nodeStake.length)
                for (let i = 0; i < message.nodeStake.length; ++i)
                    $root.proto.NodeStake.encode(message.nodeStake[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.maxStakingRewardRatePerHbar != null && $Object.hasOwnProperty.call(message, "maxStakingRewardRatePerHbar") && (typeof message.maxStakingRewardRatePerHbar === "object" ? message.maxStakingRewardRatePerHbar.low || message.maxStakingRewardRatePerHbar.high : message.maxStakingRewardRatePerHbar !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.maxStakingRewardRatePerHbar);
            if (message.nodeRewardFeeFraction != null && $Object.hasOwnProperty.call(message, "nodeRewardFeeFraction"))
                $root.proto.Fraction.encode(message.nodeRewardFeeFraction, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.stakingPeriodsStored != null && $Object.hasOwnProperty.call(message, "stakingPeriodsStored") && (typeof message.stakingPeriodsStored === "object" ? message.stakingPeriodsStored.low || message.stakingPeriodsStored.high : message.stakingPeriodsStored !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.stakingPeriodsStored);
            if (message.stakingPeriod != null && $Object.hasOwnProperty.call(message, "stakingPeriod") && (typeof message.stakingPeriod === "object" ? message.stakingPeriod.low || message.stakingPeriod.high : message.stakingPeriod !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.stakingPeriod);
            if (message.stakingRewardFeeFraction != null && $Object.hasOwnProperty.call(message, "stakingRewardFeeFraction"))
                $root.proto.Fraction.encode(message.stakingRewardFeeFraction, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.stakingStartThreshold != null && $Object.hasOwnProperty.call(message, "stakingStartThreshold") && (typeof message.stakingStartThreshold === "object" ? message.stakingStartThreshold.low || message.stakingStartThreshold.high : message.stakingStartThreshold !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.stakingStartThreshold);
            if (message.stakingRewardRate != null && $Object.hasOwnProperty.call(message, "stakingRewardRate") && (typeof message.stakingRewardRate === "object" ? message.stakingRewardRate.low || message.stakingRewardRate.high : message.stakingRewardRate !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.stakingRewardRate);
            if (message.reservedStakingRewards != null && $Object.hasOwnProperty.call(message, "reservedStakingRewards") && (typeof message.reservedStakingRewards === "object" ? message.reservedStakingRewards.low || message.reservedStakingRewards.high : message.reservedStakingRewards !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.reservedStakingRewards);
            if (message.unreservedStakingRewardBalance != null && $Object.hasOwnProperty.call(message, "unreservedStakingRewardBalance") && (typeof message.unreservedStakingRewardBalance === "object" ? message.unreservedStakingRewardBalance.low || message.unreservedStakingRewardBalance.high : message.unreservedStakingRewardBalance !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.unreservedStakingRewardBalance);
            if (message.rewardBalanceThreshold != null && $Object.hasOwnProperty.call(message, "rewardBalanceThreshold") && (typeof message.rewardBalanceThreshold === "object" ? message.rewardBalanceThreshold.low || message.rewardBalanceThreshold.high : message.rewardBalanceThreshold !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.rewardBalanceThreshold);
            if (message.maxStakeRewarded != null && $Object.hasOwnProperty.call(message, "maxStakeRewarded") && (typeof message.maxStakeRewarded === "object" ? message.maxStakeRewarded.low || message.maxStakeRewarded.high : message.maxStakeRewarded !== 0))
                writer.uint32(/* id 13, wireType 0 =*/104).int64(message.maxStakeRewarded);
            if (message.maxTotalReward != null && $Object.hasOwnProperty.call(message, "maxTotalReward") && (typeof message.maxTotalReward === "object" ? message.maxTotalReward.low || message.maxTotalReward.high : message.maxTotalReward !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).int64(message.maxTotalReward);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeStakeUpdateTransactionBody message, length delimited. Does not implicitly {@link proto.NodeStakeUpdateTransactionBody.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @static
         * @param {proto.NodeStakeUpdateTransactionBody.$Properties} message NodeStakeUpdateTransactionBody message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeStakeUpdateTransactionBody.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NodeStakeUpdateTransactionBody message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NodeStakeUpdateTransactionBody & proto.NodeStakeUpdateTransactionBody.$Shape} NodeStakeUpdateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeStakeUpdateTransactionBody.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NodeStakeUpdateTransactionBody();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.endOfStakingPeriod = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.endOfStakingPeriod);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.nodeStake && message.nodeStake.length))
                            message.nodeStake = [];
                        message.nodeStake.push($root.proto.NodeStake.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maxStakingRewardRatePerHbar = value;
                        else
                            delete message.maxStakingRewardRatePerHbar;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.nodeRewardFeeFraction = $root.proto.Fraction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodeRewardFeeFraction);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakingPeriodsStored = value;
                        else
                            delete message.stakingPeriodsStored;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakingPeriod = value;
                        else
                            delete message.stakingPeriod;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.stakingRewardFeeFraction = $root.proto.Fraction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stakingRewardFeeFraction);
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakingStartThreshold = value;
                        else
                            delete message.stakingStartThreshold;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakingRewardRate = value;
                        else
                            delete message.stakingRewardRate;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.reservedStakingRewards = value;
                        else
                            delete message.reservedStakingRewards;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.unreservedStakingRewardBalance = value;
                        else
                            delete message.unreservedStakingRewardBalance;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.rewardBalanceThreshold = value;
                        else
                            delete message.rewardBalanceThreshold;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maxStakeRewarded = value;
                        else
                            delete message.maxStakeRewarded;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maxTotalReward = value;
                        else
                            delete message.maxTotalReward;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeStakeUpdateTransactionBody message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NodeStakeUpdateTransactionBody & proto.NodeStakeUpdateTransactionBody.$Shape} NodeStakeUpdateTransactionBody
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeStakeUpdateTransactionBody.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeStakeUpdateTransactionBody message.
         * @function verify
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeStakeUpdateTransactionBody.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.endOfStakingPeriod != null && $Object.hasOwnProperty.call(message, "endOfStakingPeriod")) {
                let error = $root.proto.Timestamp.verify(message.endOfStakingPeriod, _depth + 1);
                if (error)
                    return "endOfStakingPeriod." + error;
            }
            if (message.nodeStake != null && $Object.hasOwnProperty.call(message, "nodeStake")) {
                if (!$Array.isArray(message.nodeStake))
                    return "nodeStake: array expected";
                for (let i = 0; i < message.nodeStake.length; ++i) {
                    let error = $root.proto.NodeStake.verify(message.nodeStake[i], _depth + 1);
                    if (error)
                        return "nodeStake." + error;
                }
            }
            if (message.maxStakingRewardRatePerHbar != null && $Object.hasOwnProperty.call(message, "maxStakingRewardRatePerHbar"))
                if (!$util.isInteger(message.maxStakingRewardRatePerHbar) && !(message.maxStakingRewardRatePerHbar && $util.isInteger(message.maxStakingRewardRatePerHbar.low) && $util.isInteger(message.maxStakingRewardRatePerHbar.high)))
                    return "maxStakingRewardRatePerHbar: integer|Long expected";
            if (message.nodeRewardFeeFraction != null && $Object.hasOwnProperty.call(message, "nodeRewardFeeFraction")) {
                let error = $root.proto.Fraction.verify(message.nodeRewardFeeFraction, _depth + 1);
                if (error)
                    return "nodeRewardFeeFraction." + error;
            }
            if (message.stakingPeriodsStored != null && $Object.hasOwnProperty.call(message, "stakingPeriodsStored"))
                if (!$util.isInteger(message.stakingPeriodsStored) && !(message.stakingPeriodsStored && $util.isInteger(message.stakingPeriodsStored.low) && $util.isInteger(message.stakingPeriodsStored.high)))
                    return "stakingPeriodsStored: integer|Long expected";
            if (message.stakingPeriod != null && $Object.hasOwnProperty.call(message, "stakingPeriod"))
                if (!$util.isInteger(message.stakingPeriod) && !(message.stakingPeriod && $util.isInteger(message.stakingPeriod.low) && $util.isInteger(message.stakingPeriod.high)))
                    return "stakingPeriod: integer|Long expected";
            if (message.stakingRewardFeeFraction != null && $Object.hasOwnProperty.call(message, "stakingRewardFeeFraction")) {
                let error = $root.proto.Fraction.verify(message.stakingRewardFeeFraction, _depth + 1);
                if (error)
                    return "stakingRewardFeeFraction." + error;
            }
            if (message.stakingStartThreshold != null && $Object.hasOwnProperty.call(message, "stakingStartThreshold"))
                if (!$util.isInteger(message.stakingStartThreshold) && !(message.stakingStartThreshold && $util.isInteger(message.stakingStartThreshold.low) && $util.isInteger(message.stakingStartThreshold.high)))
                    return "stakingStartThreshold: integer|Long expected";
            if (message.stakingRewardRate != null && $Object.hasOwnProperty.call(message, "stakingRewardRate"))
                if (!$util.isInteger(message.stakingRewardRate) && !(message.stakingRewardRate && $util.isInteger(message.stakingRewardRate.low) && $util.isInteger(message.stakingRewardRate.high)))
                    return "stakingRewardRate: integer|Long expected";
            if (message.reservedStakingRewards != null && $Object.hasOwnProperty.call(message, "reservedStakingRewards"))
                if (!$util.isInteger(message.reservedStakingRewards) && !(message.reservedStakingRewards && $util.isInteger(message.reservedStakingRewards.low) && $util.isInteger(message.reservedStakingRewards.high)))
                    return "reservedStakingRewards: integer|Long expected";
            if (message.unreservedStakingRewardBalance != null && $Object.hasOwnProperty.call(message, "unreservedStakingRewardBalance"))
                if (!$util.isInteger(message.unreservedStakingRewardBalance) && !(message.unreservedStakingRewardBalance && $util.isInteger(message.unreservedStakingRewardBalance.low) && $util.isInteger(message.unreservedStakingRewardBalance.high)))
                    return "unreservedStakingRewardBalance: integer|Long expected";
            if (message.rewardBalanceThreshold != null && $Object.hasOwnProperty.call(message, "rewardBalanceThreshold"))
                if (!$util.isInteger(message.rewardBalanceThreshold) && !(message.rewardBalanceThreshold && $util.isInteger(message.rewardBalanceThreshold.low) && $util.isInteger(message.rewardBalanceThreshold.high)))
                    return "rewardBalanceThreshold: integer|Long expected";
            if (message.maxStakeRewarded != null && $Object.hasOwnProperty.call(message, "maxStakeRewarded"))
                if (!$util.isInteger(message.maxStakeRewarded) && !(message.maxStakeRewarded && $util.isInteger(message.maxStakeRewarded.low) && $util.isInteger(message.maxStakeRewarded.high)))
                    return "maxStakeRewarded: integer|Long expected";
            if (message.maxTotalReward != null && $Object.hasOwnProperty.call(message, "maxTotalReward"))
                if (!$util.isInteger(message.maxTotalReward) && !(message.maxTotalReward && $util.isInteger(message.maxTotalReward.low) && $util.isInteger(message.maxTotalReward.high)))
                    return "maxTotalReward: integer|Long expected";
            return null;
        };

        /**
         * Creates a NodeStakeUpdateTransactionBody message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NodeStakeUpdateTransactionBody} NodeStakeUpdateTransactionBody
         */
        NodeStakeUpdateTransactionBody.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NodeStakeUpdateTransactionBody)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NodeStakeUpdateTransactionBody: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NodeStakeUpdateTransactionBody();
            if (object.endOfStakingPeriod != null) {
                if (!$util.isObject(object.endOfStakingPeriod))
                    throw $TypeError(".proto.NodeStakeUpdateTransactionBody.endOfStakingPeriod: object expected");
                message.endOfStakingPeriod = $root.proto.Timestamp.fromObject(object.endOfStakingPeriod, _depth + 1);
            }
            if (object.nodeStake) {
                if (!$Array.isArray(object.nodeStake))
                    throw $TypeError(".proto.NodeStakeUpdateTransactionBody.nodeStake: array expected");
                message.nodeStake = $Array(object.nodeStake.length);
                for (let i = 0; i < object.nodeStake.length; ++i) {
                    if (!$util.isObject(object.nodeStake[i]))
                        throw $TypeError(".proto.NodeStakeUpdateTransactionBody.nodeStake: object expected");
                    message.nodeStake[i] = $root.proto.NodeStake.fromObject(object.nodeStake[i], _depth + 1);
                }
            }
            if (object.maxStakingRewardRatePerHbar != null)
                if (typeof object.maxStakingRewardRatePerHbar === "object" ? object.maxStakingRewardRatePerHbar.low || object.maxStakingRewardRatePerHbar.high : $Number(object.maxStakingRewardRatePerHbar) !== 0)
                    if ($util.Long)
                        message.maxStakingRewardRatePerHbar = $util.Long.fromValue(object.maxStakingRewardRatePerHbar, false);
                    else if (typeof object.maxStakingRewardRatePerHbar === "string")
                        message.maxStakingRewardRatePerHbar = $parseInt(object.maxStakingRewardRatePerHbar, 10);
                    else if (typeof object.maxStakingRewardRatePerHbar === "number")
                        message.maxStakingRewardRatePerHbar = object.maxStakingRewardRatePerHbar;
                    else if (typeof object.maxStakingRewardRatePerHbar === "object")
                        message.maxStakingRewardRatePerHbar = new $util.LongBits(object.maxStakingRewardRatePerHbar.low >>> 0, object.maxStakingRewardRatePerHbar.high >>> 0).toNumber();
            if (object.nodeRewardFeeFraction != null) {
                if (!$util.isObject(object.nodeRewardFeeFraction))
                    throw $TypeError(".proto.NodeStakeUpdateTransactionBody.nodeRewardFeeFraction: object expected");
                message.nodeRewardFeeFraction = $root.proto.Fraction.fromObject(object.nodeRewardFeeFraction, _depth + 1);
            }
            if (object.stakingPeriodsStored != null)
                if (typeof object.stakingPeriodsStored === "object" ? object.stakingPeriodsStored.low || object.stakingPeriodsStored.high : $Number(object.stakingPeriodsStored) !== 0)
                    if ($util.Long)
                        message.stakingPeriodsStored = $util.Long.fromValue(object.stakingPeriodsStored, false);
                    else if (typeof object.stakingPeriodsStored === "string")
                        message.stakingPeriodsStored = $parseInt(object.stakingPeriodsStored, 10);
                    else if (typeof object.stakingPeriodsStored === "number")
                        message.stakingPeriodsStored = object.stakingPeriodsStored;
                    else if (typeof object.stakingPeriodsStored === "object")
                        message.stakingPeriodsStored = new $util.LongBits(object.stakingPeriodsStored.low >>> 0, object.stakingPeriodsStored.high >>> 0).toNumber();
            if (object.stakingPeriod != null)
                if (typeof object.stakingPeriod === "object" ? object.stakingPeriod.low || object.stakingPeriod.high : $Number(object.stakingPeriod) !== 0)
                    if ($util.Long)
                        message.stakingPeriod = $util.Long.fromValue(object.stakingPeriod, false);
                    else if (typeof object.stakingPeriod === "string")
                        message.stakingPeriod = $parseInt(object.stakingPeriod, 10);
                    else if (typeof object.stakingPeriod === "number")
                        message.stakingPeriod = object.stakingPeriod;
                    else if (typeof object.stakingPeriod === "object")
                        message.stakingPeriod = new $util.LongBits(object.stakingPeriod.low >>> 0, object.stakingPeriod.high >>> 0).toNumber();
            if (object.stakingRewardFeeFraction != null) {
                if (!$util.isObject(object.stakingRewardFeeFraction))
                    throw $TypeError(".proto.NodeStakeUpdateTransactionBody.stakingRewardFeeFraction: object expected");
                message.stakingRewardFeeFraction = $root.proto.Fraction.fromObject(object.stakingRewardFeeFraction, _depth + 1);
            }
            if (object.stakingStartThreshold != null)
                if (typeof object.stakingStartThreshold === "object" ? object.stakingStartThreshold.low || object.stakingStartThreshold.high : $Number(object.stakingStartThreshold) !== 0)
                    if ($util.Long)
                        message.stakingStartThreshold = $util.Long.fromValue(object.stakingStartThreshold, false);
                    else if (typeof object.stakingStartThreshold === "string")
                        message.stakingStartThreshold = $parseInt(object.stakingStartThreshold, 10);
                    else if (typeof object.stakingStartThreshold === "number")
                        message.stakingStartThreshold = object.stakingStartThreshold;
                    else if (typeof object.stakingStartThreshold === "object")
                        message.stakingStartThreshold = new $util.LongBits(object.stakingStartThreshold.low >>> 0, object.stakingStartThreshold.high >>> 0).toNumber();
            if (object.stakingRewardRate != null)
                if (typeof object.stakingRewardRate === "object" ? object.stakingRewardRate.low || object.stakingRewardRate.high : $Number(object.stakingRewardRate) !== 0)
                    if ($util.Long)
                        message.stakingRewardRate = $util.Long.fromValue(object.stakingRewardRate, false);
                    else if (typeof object.stakingRewardRate === "string")
                        message.stakingRewardRate = $parseInt(object.stakingRewardRate, 10);
                    else if (typeof object.stakingRewardRate === "number")
                        message.stakingRewardRate = object.stakingRewardRate;
                    else if (typeof object.stakingRewardRate === "object")
                        message.stakingRewardRate = new $util.LongBits(object.stakingRewardRate.low >>> 0, object.stakingRewardRate.high >>> 0).toNumber();
            if (object.reservedStakingRewards != null)
                if (typeof object.reservedStakingRewards === "object" ? object.reservedStakingRewards.low || object.reservedStakingRewards.high : $Number(object.reservedStakingRewards) !== 0)
                    if ($util.Long)
                        message.reservedStakingRewards = $util.Long.fromValue(object.reservedStakingRewards, false);
                    else if (typeof object.reservedStakingRewards === "string")
                        message.reservedStakingRewards = $parseInt(object.reservedStakingRewards, 10);
                    else if (typeof object.reservedStakingRewards === "number")
                        message.reservedStakingRewards = object.reservedStakingRewards;
                    else if (typeof object.reservedStakingRewards === "object")
                        message.reservedStakingRewards = new $util.LongBits(object.reservedStakingRewards.low >>> 0, object.reservedStakingRewards.high >>> 0).toNumber();
            if (object.unreservedStakingRewardBalance != null)
                if (typeof object.unreservedStakingRewardBalance === "object" ? object.unreservedStakingRewardBalance.low || object.unreservedStakingRewardBalance.high : $Number(object.unreservedStakingRewardBalance) !== 0)
                    if ($util.Long)
                        message.unreservedStakingRewardBalance = $util.Long.fromValue(object.unreservedStakingRewardBalance, false);
                    else if (typeof object.unreservedStakingRewardBalance === "string")
                        message.unreservedStakingRewardBalance = $parseInt(object.unreservedStakingRewardBalance, 10);
                    else if (typeof object.unreservedStakingRewardBalance === "number")
                        message.unreservedStakingRewardBalance = object.unreservedStakingRewardBalance;
                    else if (typeof object.unreservedStakingRewardBalance === "object")
                        message.unreservedStakingRewardBalance = new $util.LongBits(object.unreservedStakingRewardBalance.low >>> 0, object.unreservedStakingRewardBalance.high >>> 0).toNumber();
            if (object.rewardBalanceThreshold != null)
                if (typeof object.rewardBalanceThreshold === "object" ? object.rewardBalanceThreshold.low || object.rewardBalanceThreshold.high : $Number(object.rewardBalanceThreshold) !== 0)
                    if ($util.Long)
                        message.rewardBalanceThreshold = $util.Long.fromValue(object.rewardBalanceThreshold, false);
                    else if (typeof object.rewardBalanceThreshold === "string")
                        message.rewardBalanceThreshold = $parseInt(object.rewardBalanceThreshold, 10);
                    else if (typeof object.rewardBalanceThreshold === "number")
                        message.rewardBalanceThreshold = object.rewardBalanceThreshold;
                    else if (typeof object.rewardBalanceThreshold === "object")
                        message.rewardBalanceThreshold = new $util.LongBits(object.rewardBalanceThreshold.low >>> 0, object.rewardBalanceThreshold.high >>> 0).toNumber();
            if (object.maxStakeRewarded != null)
                if (typeof object.maxStakeRewarded === "object" ? object.maxStakeRewarded.low || object.maxStakeRewarded.high : $Number(object.maxStakeRewarded) !== 0)
                    if ($util.Long)
                        message.maxStakeRewarded = $util.Long.fromValue(object.maxStakeRewarded, false);
                    else if (typeof object.maxStakeRewarded === "string")
                        message.maxStakeRewarded = $parseInt(object.maxStakeRewarded, 10);
                    else if (typeof object.maxStakeRewarded === "number")
                        message.maxStakeRewarded = object.maxStakeRewarded;
                    else if (typeof object.maxStakeRewarded === "object")
                        message.maxStakeRewarded = new $util.LongBits(object.maxStakeRewarded.low >>> 0, object.maxStakeRewarded.high >>> 0).toNumber();
            if (object.maxTotalReward != null)
                if (typeof object.maxTotalReward === "object" ? object.maxTotalReward.low || object.maxTotalReward.high : $Number(object.maxTotalReward) !== 0)
                    if ($util.Long)
                        message.maxTotalReward = $util.Long.fromValue(object.maxTotalReward, false);
                    else if (typeof object.maxTotalReward === "string")
                        message.maxTotalReward = $parseInt(object.maxTotalReward, 10);
                    else if (typeof object.maxTotalReward === "number")
                        message.maxTotalReward = object.maxTotalReward;
                    else if (typeof object.maxTotalReward === "object")
                        message.maxTotalReward = new $util.LongBits(object.maxTotalReward.low >>> 0, object.maxTotalReward.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a NodeStakeUpdateTransactionBody message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @static
         * @param {proto.NodeStakeUpdateTransactionBody} message NodeStakeUpdateTransactionBody
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeStakeUpdateTransactionBody.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.nodeStake = [];
            if (options.defaults) {
                object.endOfStakingPeriod = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxStakingRewardRatePerHbar = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maxStakingRewardRatePerHbar = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.nodeRewardFeeFraction = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakingPeriodsStored = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakingPeriodsStored = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakingPeriod = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakingPeriod = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.stakingRewardFeeFraction = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakingStartThreshold = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakingStartThreshold = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakingRewardRate = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakingRewardRate = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.reservedStakingRewards = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.reservedStakingRewards = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.unreservedStakingRewardBalance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.unreservedStakingRewardBalance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.rewardBalanceThreshold = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.rewardBalanceThreshold = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxStakeRewarded = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maxStakeRewarded = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxTotalReward = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maxTotalReward = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.endOfStakingPeriod != null && $Object.hasOwnProperty.call(message, "endOfStakingPeriod"))
                object.endOfStakingPeriod = $root.proto.Timestamp.toObject(message.endOfStakingPeriod, options, _depth + 1);
            if (message.nodeStake && message.nodeStake.length) {
                object.nodeStake = $Array(message.nodeStake.length);
                for (let j = 0; j < message.nodeStake.length; ++j)
                    object.nodeStake[j] = $root.proto.NodeStake.toObject(message.nodeStake[j], options, _depth + 1);
            }
            if (message.maxStakingRewardRatePerHbar != null && $Object.hasOwnProperty.call(message, "maxStakingRewardRatePerHbar"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maxStakingRewardRatePerHbar = typeof message.maxStakingRewardRatePerHbar === "number" ? $BigInt(message.maxStakingRewardRatePerHbar) : $util.Long.fromBits(message.maxStakingRewardRatePerHbar.low >>> 0, message.maxStakingRewardRatePerHbar.high >>> 0, false).toBigInt();
                else if (typeof message.maxStakingRewardRatePerHbar === "number")
                    object.maxStakingRewardRatePerHbar = options.longs === $String ? $String(message.maxStakingRewardRatePerHbar) : message.maxStakingRewardRatePerHbar;
                else
                    object.maxStakingRewardRatePerHbar = options.longs === $String ? $util.Long.prototype.toString.call(message.maxStakingRewardRatePerHbar) : options.longs === $Number ? new $util.LongBits(message.maxStakingRewardRatePerHbar.low >>> 0, message.maxStakingRewardRatePerHbar.high >>> 0).toNumber() : message.maxStakingRewardRatePerHbar;
            if (message.nodeRewardFeeFraction != null && $Object.hasOwnProperty.call(message, "nodeRewardFeeFraction"))
                object.nodeRewardFeeFraction = $root.proto.Fraction.toObject(message.nodeRewardFeeFraction, options, _depth + 1);
            if (message.stakingPeriodsStored != null && $Object.hasOwnProperty.call(message, "stakingPeriodsStored"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakingPeriodsStored = typeof message.stakingPeriodsStored === "number" ? $BigInt(message.stakingPeriodsStored) : $util.Long.fromBits(message.stakingPeriodsStored.low >>> 0, message.stakingPeriodsStored.high >>> 0, false).toBigInt();
                else if (typeof message.stakingPeriodsStored === "number")
                    object.stakingPeriodsStored = options.longs === $String ? $String(message.stakingPeriodsStored) : message.stakingPeriodsStored;
                else
                    object.stakingPeriodsStored = options.longs === $String ? $util.Long.prototype.toString.call(message.stakingPeriodsStored) : options.longs === $Number ? new $util.LongBits(message.stakingPeriodsStored.low >>> 0, message.stakingPeriodsStored.high >>> 0).toNumber() : message.stakingPeriodsStored;
            if (message.stakingPeriod != null && $Object.hasOwnProperty.call(message, "stakingPeriod"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakingPeriod = typeof message.stakingPeriod === "number" ? $BigInt(message.stakingPeriod) : $util.Long.fromBits(message.stakingPeriod.low >>> 0, message.stakingPeriod.high >>> 0, false).toBigInt();
                else if (typeof message.stakingPeriod === "number")
                    object.stakingPeriod = options.longs === $String ? $String(message.stakingPeriod) : message.stakingPeriod;
                else
                    object.stakingPeriod = options.longs === $String ? $util.Long.prototype.toString.call(message.stakingPeriod) : options.longs === $Number ? new $util.LongBits(message.stakingPeriod.low >>> 0, message.stakingPeriod.high >>> 0).toNumber() : message.stakingPeriod;
            if (message.stakingRewardFeeFraction != null && $Object.hasOwnProperty.call(message, "stakingRewardFeeFraction"))
                object.stakingRewardFeeFraction = $root.proto.Fraction.toObject(message.stakingRewardFeeFraction, options, _depth + 1);
            if (message.stakingStartThreshold != null && $Object.hasOwnProperty.call(message, "stakingStartThreshold"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakingStartThreshold = typeof message.stakingStartThreshold === "number" ? $BigInt(message.stakingStartThreshold) : $util.Long.fromBits(message.stakingStartThreshold.low >>> 0, message.stakingStartThreshold.high >>> 0, false).toBigInt();
                else if (typeof message.stakingStartThreshold === "number")
                    object.stakingStartThreshold = options.longs === $String ? $String(message.stakingStartThreshold) : message.stakingStartThreshold;
                else
                    object.stakingStartThreshold = options.longs === $String ? $util.Long.prototype.toString.call(message.stakingStartThreshold) : options.longs === $Number ? new $util.LongBits(message.stakingStartThreshold.low >>> 0, message.stakingStartThreshold.high >>> 0).toNumber() : message.stakingStartThreshold;
            if (message.stakingRewardRate != null && $Object.hasOwnProperty.call(message, "stakingRewardRate"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakingRewardRate = typeof message.stakingRewardRate === "number" ? $BigInt(message.stakingRewardRate) : $util.Long.fromBits(message.stakingRewardRate.low >>> 0, message.stakingRewardRate.high >>> 0, false).toBigInt();
                else if (typeof message.stakingRewardRate === "number")
                    object.stakingRewardRate = options.longs === $String ? $String(message.stakingRewardRate) : message.stakingRewardRate;
                else
                    object.stakingRewardRate = options.longs === $String ? $util.Long.prototype.toString.call(message.stakingRewardRate) : options.longs === $Number ? new $util.LongBits(message.stakingRewardRate.low >>> 0, message.stakingRewardRate.high >>> 0).toNumber() : message.stakingRewardRate;
            if (message.reservedStakingRewards != null && $Object.hasOwnProperty.call(message, "reservedStakingRewards"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.reservedStakingRewards = typeof message.reservedStakingRewards === "number" ? $BigInt(message.reservedStakingRewards) : $util.Long.fromBits(message.reservedStakingRewards.low >>> 0, message.reservedStakingRewards.high >>> 0, false).toBigInt();
                else if (typeof message.reservedStakingRewards === "number")
                    object.reservedStakingRewards = options.longs === $String ? $String(message.reservedStakingRewards) : message.reservedStakingRewards;
                else
                    object.reservedStakingRewards = options.longs === $String ? $util.Long.prototype.toString.call(message.reservedStakingRewards) : options.longs === $Number ? new $util.LongBits(message.reservedStakingRewards.low >>> 0, message.reservedStakingRewards.high >>> 0).toNumber() : message.reservedStakingRewards;
            if (message.unreservedStakingRewardBalance != null && $Object.hasOwnProperty.call(message, "unreservedStakingRewardBalance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.unreservedStakingRewardBalance = typeof message.unreservedStakingRewardBalance === "number" ? $BigInt(message.unreservedStakingRewardBalance) : $util.Long.fromBits(message.unreservedStakingRewardBalance.low >>> 0, message.unreservedStakingRewardBalance.high >>> 0, false).toBigInt();
                else if (typeof message.unreservedStakingRewardBalance === "number")
                    object.unreservedStakingRewardBalance = options.longs === $String ? $String(message.unreservedStakingRewardBalance) : message.unreservedStakingRewardBalance;
                else
                    object.unreservedStakingRewardBalance = options.longs === $String ? $util.Long.prototype.toString.call(message.unreservedStakingRewardBalance) : options.longs === $Number ? new $util.LongBits(message.unreservedStakingRewardBalance.low >>> 0, message.unreservedStakingRewardBalance.high >>> 0).toNumber() : message.unreservedStakingRewardBalance;
            if (message.rewardBalanceThreshold != null && $Object.hasOwnProperty.call(message, "rewardBalanceThreshold"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.rewardBalanceThreshold = typeof message.rewardBalanceThreshold === "number" ? $BigInt(message.rewardBalanceThreshold) : $util.Long.fromBits(message.rewardBalanceThreshold.low >>> 0, message.rewardBalanceThreshold.high >>> 0, false).toBigInt();
                else if (typeof message.rewardBalanceThreshold === "number")
                    object.rewardBalanceThreshold = options.longs === $String ? $String(message.rewardBalanceThreshold) : message.rewardBalanceThreshold;
                else
                    object.rewardBalanceThreshold = options.longs === $String ? $util.Long.prototype.toString.call(message.rewardBalanceThreshold) : options.longs === $Number ? new $util.LongBits(message.rewardBalanceThreshold.low >>> 0, message.rewardBalanceThreshold.high >>> 0).toNumber() : message.rewardBalanceThreshold;
            if (message.maxStakeRewarded != null && $Object.hasOwnProperty.call(message, "maxStakeRewarded"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maxStakeRewarded = typeof message.maxStakeRewarded === "number" ? $BigInt(message.maxStakeRewarded) : $util.Long.fromBits(message.maxStakeRewarded.low >>> 0, message.maxStakeRewarded.high >>> 0, false).toBigInt();
                else if (typeof message.maxStakeRewarded === "number")
                    object.maxStakeRewarded = options.longs === $String ? $String(message.maxStakeRewarded) : message.maxStakeRewarded;
                else
                    object.maxStakeRewarded = options.longs === $String ? $util.Long.prototype.toString.call(message.maxStakeRewarded) : options.longs === $Number ? new $util.LongBits(message.maxStakeRewarded.low >>> 0, message.maxStakeRewarded.high >>> 0).toNumber() : message.maxStakeRewarded;
            if (message.maxTotalReward != null && $Object.hasOwnProperty.call(message, "maxTotalReward"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maxTotalReward = typeof message.maxTotalReward === "number" ? $BigInt(message.maxTotalReward) : $util.Long.fromBits(message.maxTotalReward.low >>> 0, message.maxTotalReward.high >>> 0, false).toBigInt();
                else if (typeof message.maxTotalReward === "number")
                    object.maxTotalReward = options.longs === $String ? $String(message.maxTotalReward) : message.maxTotalReward;
                else
                    object.maxTotalReward = options.longs === $String ? $util.Long.prototype.toString.call(message.maxTotalReward) : options.longs === $Number ? new $util.LongBits(message.maxTotalReward.low >>> 0, message.maxTotalReward.high >>> 0).toNumber() : message.maxTotalReward;
            return object;
        };

        /**
         * Converts this NodeStakeUpdateTransactionBody to JSON.
         * @function toJSON
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeStakeUpdateTransactionBody.prototype.toJSON = function() {
            return NodeStakeUpdateTransactionBody.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeStakeUpdateTransactionBody
         * @function getTypeUrl
         * @memberof proto.NodeStakeUpdateTransactionBody
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeStakeUpdateTransactionBody.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NodeStakeUpdateTransactionBody";
        };

        return NodeStakeUpdateTransactionBody;
    })();

    proto.NodeStake = (function() {

        /**
         * Properties of a NodeStake.
         * @typedef {Object} proto.NodeStake.$Properties
         * @property {number|Long|null} [maxStake] A limit to the amount of stake considered for consensus weight.
         * <p>
         * The amount of stake (whether accepting rewards or not) assigned to a
         * node that exceeds this limit SHALL NOT be considered for
         * consensus weight calculation.<br/>
         * If stake to _reward_ for a node exceeds this threshold, then all
         * accounts staking to that node SHALL receive a lower reward rate in
         * proportion to the excess stake.
         * @property {number|Long|null} [minStake] A minimum amount of HBAR staked to a node to receive rewards.
         * <p>
         * If the amount of stake (whether accepting rewards or not) assigned to
         * a node at the start of a staking period is less than this threshold,
         * then no rewards SHALL be paid to that node or to any accounts
         * staking to that node.
         * @property {number|Long|null} [nodeId] A node identifier.<br/>
         * This value uniquely identifies this node within the
         * network address book.
         * @property {number|Long|null} [rewardRate] The rate of rewards, in tinybar per HBAR, for the staking reward
         * period that just ended.
         * @property {number|Long|null} [stake] A consensus weight assigned to this node for the next staking period.
         * @property {number|Long|null} [stakeNotRewarded] The total amount staked to this node, while declining rewards.
         * <p>
         * This SHALL be the total staked amount, in tinybar, that is staked to
         * this node with the value of the `decline_reward` set.<br/>
         * This value MUST be calculated at the beginning of the staking period.
         * @property {number|Long|null} [stakeRewarded] The total amount staked to this node, while accepting rewards.
         * <p>
         * This SHALL be the total staked amount, in tinybar, that is staked to
         * this node with the value of the `decline_reward` not set.<br/>
         * This value MUST be calculated at the beginning of the staking period.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NodeStake.
         * @memberof proto
         * @interface INodeStake
         * @augments proto.NodeStake.$Properties
         * @deprecated Use proto.NodeStake.$Properties instead.
         */

        /**
         * Shape of a NodeStake.
         * @typedef {proto.NodeStake.$Properties} proto.NodeStake.$Shape
         */

        /**
         * Constructs a new NodeStake.
         * @memberof proto
         * @classdesc Staking information for one node at the end of a staking period.
         * 
         * This SHALL be one entry in a list reported at the end of each
         * full staking period.
         * @constructor
         * @param {proto.NodeStake.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NodeStake = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A limit to the amount of stake considered for consensus weight.
         * <p>
         * The amount of stake (whether accepting rewards or not) assigned to a
         * node that exceeds this limit SHALL NOT be considered for
         * consensus weight calculation.<br/>
         * If stake to _reward_ for a node exceeds this threshold, then all
         * accounts staking to that node SHALL receive a lower reward rate in
         * proportion to the excess stake.
         * @member {number|Long} maxStake
         * @memberof proto.NodeStake
         * @instance
         */
        NodeStake.prototype.maxStake = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A minimum amount of HBAR staked to a node to receive rewards.
         * <p>
         * If the amount of stake (whether accepting rewards or not) assigned to
         * a node at the start of a staking period is less than this threshold,
         * then no rewards SHALL be paid to that node or to any accounts
         * staking to that node.
         * @member {number|Long} minStake
         * @memberof proto.NodeStake
         * @instance
         */
        NodeStake.prototype.minStake = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A node identifier.<br/>
         * This value uniquely identifies this node within the
         * network address book.
         * @member {number|Long} nodeId
         * @memberof proto.NodeStake
         * @instance
         */
        NodeStake.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The rate of rewards, in tinybar per HBAR, for the staking reward
         * period that just ended.
         * @member {number|Long} rewardRate
         * @memberof proto.NodeStake
         * @instance
         */
        NodeStake.prototype.rewardRate = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A consensus weight assigned to this node for the next staking period.
         * @member {number|Long} stake
         * @memberof proto.NodeStake
         * @instance
         */
        NodeStake.prototype.stake = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The total amount staked to this node, while declining rewards.
         * <p>
         * This SHALL be the total staked amount, in tinybar, that is staked to
         * this node with the value of the `decline_reward` set.<br/>
         * This value MUST be calculated at the beginning of the staking period.
         * @member {number|Long} stakeNotRewarded
         * @memberof proto.NodeStake
         * @instance
         */
        NodeStake.prototype.stakeNotRewarded = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The total amount staked to this node, while accepting rewards.
         * <p>
         * This SHALL be the total staked amount, in tinybar, that is staked to
         * this node with the value of the `decline_reward` not set.<br/>
         * This value MUST be calculated at the beginning of the staking period.
         * @member {number|Long} stakeRewarded
         * @memberof proto.NodeStake
         * @instance
         */
        NodeStake.prototype.stakeRewarded = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new NodeStake instance using the specified properties.
         * @function create
         * @memberof proto.NodeStake
         * @static
         * @param {proto.NodeStake.$Properties=} [properties] Properties to set
         * @returns {proto.NodeStake} NodeStake instance
         * @type {{
         *   (properties: proto.NodeStake.$Shape): proto.NodeStake & proto.NodeStake.$Shape;
         *   (properties?: proto.NodeStake.$Properties): proto.NodeStake;
         * }}
         */
        NodeStake.create = function(properties) {
            return new NodeStake(properties);
        };

        /**
         * Encodes the specified NodeStake message. Does not implicitly {@link proto.NodeStake.verify|verify} messages.
         * @function encode
         * @memberof proto.NodeStake
         * @static
         * @param {proto.NodeStake.$Properties} message NodeStake message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeStake.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.maxStake != null && $Object.hasOwnProperty.call(message, "maxStake") && (typeof message.maxStake === "object" ? message.maxStake.low || message.maxStake.high : message.maxStake !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.maxStake);
            if (message.minStake != null && $Object.hasOwnProperty.call(message, "minStake") && (typeof message.minStake === "object" ? message.minStake.low || message.minStake.high : message.minStake !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.minStake);
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.nodeId);
            if (message.rewardRate != null && $Object.hasOwnProperty.call(message, "rewardRate") && (typeof message.rewardRate === "object" ? message.rewardRate.low || message.rewardRate.high : message.rewardRate !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.rewardRate);
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake") && (typeof message.stake === "object" ? message.stake.low || message.stake.high : message.stake !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.stake);
            if (message.stakeNotRewarded != null && $Object.hasOwnProperty.call(message, "stakeNotRewarded") && (typeof message.stakeNotRewarded === "object" ? message.stakeNotRewarded.low || message.stakeNotRewarded.high : message.stakeNotRewarded !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.stakeNotRewarded);
            if (message.stakeRewarded != null && $Object.hasOwnProperty.call(message, "stakeRewarded") && (typeof message.stakeRewarded === "object" ? message.stakeRewarded.low || message.stakeRewarded.high : message.stakeRewarded !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.stakeRewarded);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeStake message, length delimited. Does not implicitly {@link proto.NodeStake.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NodeStake
         * @static
         * @param {proto.NodeStake.$Properties} message NodeStake message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeStake.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NodeStake message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NodeStake
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NodeStake & proto.NodeStake.$Shape} NodeStake
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeStake.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NodeStake();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maxStake = value;
                        else
                            delete message.maxStake;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.minStake = value;
                        else
                            delete message.minStake;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nodeId = value;
                        else
                            delete message.nodeId;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.rewardRate = value;
                        else
                            delete message.rewardRate;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stake = value;
                        else
                            delete message.stake;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakeNotRewarded = value;
                        else
                            delete message.stakeNotRewarded;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakeRewarded = value;
                        else
                            delete message.stakeRewarded;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeStake message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NodeStake
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NodeStake & proto.NodeStake.$Shape} NodeStake
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeStake.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeStake message.
         * @function verify
         * @memberof proto.NodeStake
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeStake.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.maxStake != null && $Object.hasOwnProperty.call(message, "maxStake"))
                if (!$util.isInteger(message.maxStake) && !(message.maxStake && $util.isInteger(message.maxStake.low) && $util.isInteger(message.maxStake.high)))
                    return "maxStake: integer|Long expected";
            if (message.minStake != null && $Object.hasOwnProperty.call(message, "minStake"))
                if (!$util.isInteger(message.minStake) && !(message.minStake && $util.isInteger(message.minStake.low) && $util.isInteger(message.minStake.high)))
                    return "minStake: integer|Long expected";
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                    return "nodeId: integer|Long expected";
            if (message.rewardRate != null && $Object.hasOwnProperty.call(message, "rewardRate"))
                if (!$util.isInteger(message.rewardRate) && !(message.rewardRate && $util.isInteger(message.rewardRate.low) && $util.isInteger(message.rewardRate.high)))
                    return "rewardRate: integer|Long expected";
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                if (!$util.isInteger(message.stake) && !(message.stake && $util.isInteger(message.stake.low) && $util.isInteger(message.stake.high)))
                    return "stake: integer|Long expected";
            if (message.stakeNotRewarded != null && $Object.hasOwnProperty.call(message, "stakeNotRewarded"))
                if (!$util.isInteger(message.stakeNotRewarded) && !(message.stakeNotRewarded && $util.isInteger(message.stakeNotRewarded.low) && $util.isInteger(message.stakeNotRewarded.high)))
                    return "stakeNotRewarded: integer|Long expected";
            if (message.stakeRewarded != null && $Object.hasOwnProperty.call(message, "stakeRewarded"))
                if (!$util.isInteger(message.stakeRewarded) && !(message.stakeRewarded && $util.isInteger(message.stakeRewarded.low) && $util.isInteger(message.stakeRewarded.high)))
                    return "stakeRewarded: integer|Long expected";
            return null;
        };

        /**
         * Creates a NodeStake message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NodeStake
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NodeStake} NodeStake
         */
        NodeStake.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NodeStake)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NodeStake: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NodeStake();
            if (object.maxStake != null)
                if (typeof object.maxStake === "object" ? object.maxStake.low || object.maxStake.high : $Number(object.maxStake) !== 0)
                    if ($util.Long)
                        message.maxStake = $util.Long.fromValue(object.maxStake, false);
                    else if (typeof object.maxStake === "string")
                        message.maxStake = $parseInt(object.maxStake, 10);
                    else if (typeof object.maxStake === "number")
                        message.maxStake = object.maxStake;
                    else if (typeof object.maxStake === "object")
                        message.maxStake = new $util.LongBits(object.maxStake.low >>> 0, object.maxStake.high >>> 0).toNumber();
            if (object.minStake != null)
                if (typeof object.minStake === "object" ? object.minStake.low || object.minStake.high : $Number(object.minStake) !== 0)
                    if ($util.Long)
                        message.minStake = $util.Long.fromValue(object.minStake, false);
                    else if (typeof object.minStake === "string")
                        message.minStake = $parseInt(object.minStake, 10);
                    else if (typeof object.minStake === "number")
                        message.minStake = object.minStake;
                    else if (typeof object.minStake === "object")
                        message.minStake = new $util.LongBits(object.minStake.low >>> 0, object.minStake.high >>> 0).toNumber();
            if (object.nodeId != null)
                if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                    if ($util.Long)
                        message.nodeId = $util.Long.fromValue(object.nodeId, false);
                    else if (typeof object.nodeId === "string")
                        message.nodeId = $parseInt(object.nodeId, 10);
                    else if (typeof object.nodeId === "number")
                        message.nodeId = object.nodeId;
                    else if (typeof object.nodeId === "object")
                        message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber();
            if (object.rewardRate != null)
                if (typeof object.rewardRate === "object" ? object.rewardRate.low || object.rewardRate.high : $Number(object.rewardRate) !== 0)
                    if ($util.Long)
                        message.rewardRate = $util.Long.fromValue(object.rewardRate, false);
                    else if (typeof object.rewardRate === "string")
                        message.rewardRate = $parseInt(object.rewardRate, 10);
                    else if (typeof object.rewardRate === "number")
                        message.rewardRate = object.rewardRate;
                    else if (typeof object.rewardRate === "object")
                        message.rewardRate = new $util.LongBits(object.rewardRate.low >>> 0, object.rewardRate.high >>> 0).toNumber();
            if (object.stake != null)
                if (typeof object.stake === "object" ? object.stake.low || object.stake.high : $Number(object.stake) !== 0)
                    if ($util.Long)
                        message.stake = $util.Long.fromValue(object.stake, false);
                    else if (typeof object.stake === "string")
                        message.stake = $parseInt(object.stake, 10);
                    else if (typeof object.stake === "number")
                        message.stake = object.stake;
                    else if (typeof object.stake === "object")
                        message.stake = new $util.LongBits(object.stake.low >>> 0, object.stake.high >>> 0).toNumber();
            if (object.stakeNotRewarded != null)
                if (typeof object.stakeNotRewarded === "object" ? object.stakeNotRewarded.low || object.stakeNotRewarded.high : $Number(object.stakeNotRewarded) !== 0)
                    if ($util.Long)
                        message.stakeNotRewarded = $util.Long.fromValue(object.stakeNotRewarded, false);
                    else if (typeof object.stakeNotRewarded === "string")
                        message.stakeNotRewarded = $parseInt(object.stakeNotRewarded, 10);
                    else if (typeof object.stakeNotRewarded === "number")
                        message.stakeNotRewarded = object.stakeNotRewarded;
                    else if (typeof object.stakeNotRewarded === "object")
                        message.stakeNotRewarded = new $util.LongBits(object.stakeNotRewarded.low >>> 0, object.stakeNotRewarded.high >>> 0).toNumber();
            if (object.stakeRewarded != null)
                if (typeof object.stakeRewarded === "object" ? object.stakeRewarded.low || object.stakeRewarded.high : $Number(object.stakeRewarded) !== 0)
                    if ($util.Long)
                        message.stakeRewarded = $util.Long.fromValue(object.stakeRewarded, false);
                    else if (typeof object.stakeRewarded === "string")
                        message.stakeRewarded = $parseInt(object.stakeRewarded, 10);
                    else if (typeof object.stakeRewarded === "number")
                        message.stakeRewarded = object.stakeRewarded;
                    else if (typeof object.stakeRewarded === "object")
                        message.stakeRewarded = new $util.LongBits(object.stakeRewarded.low >>> 0, object.stakeRewarded.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a NodeStake message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NodeStake
         * @static
         * @param {proto.NodeStake} message NodeStake
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeStake.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxStake = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maxStake = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.minStake = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.minStake = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.rewardRate = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.rewardRate = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stake = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stake = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakeNotRewarded = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakeNotRewarded = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakeRewarded = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakeRewarded = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.maxStake != null && $Object.hasOwnProperty.call(message, "maxStake"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maxStake = typeof message.maxStake === "number" ? $BigInt(message.maxStake) : $util.Long.fromBits(message.maxStake.low >>> 0, message.maxStake.high >>> 0, false).toBigInt();
                else if (typeof message.maxStake === "number")
                    object.maxStake = options.longs === $String ? $String(message.maxStake) : message.maxStake;
                else
                    object.maxStake = options.longs === $String ? $util.Long.prototype.toString.call(message.maxStake) : options.longs === $Number ? new $util.LongBits(message.maxStake.low >>> 0, message.maxStake.high >>> 0).toNumber() : message.maxStake;
            if (message.minStake != null && $Object.hasOwnProperty.call(message, "minStake"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.minStake = typeof message.minStake === "number" ? $BigInt(message.minStake) : $util.Long.fromBits(message.minStake.low >>> 0, message.minStake.high >>> 0, false).toBigInt();
                else if (typeof message.minStake === "number")
                    object.minStake = options.longs === $String ? $String(message.minStake) : message.minStake;
                else
                    object.minStake = options.longs === $String ? $util.Long.prototype.toString.call(message.minStake) : options.longs === $Number ? new $util.LongBits(message.minStake.low >>> 0, message.minStake.high >>> 0).toNumber() : message.minStake;
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, false).toBigInt();
                else if (typeof message.nodeId === "number")
                    object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                else
                    object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber() : message.nodeId;
            if (message.rewardRate != null && $Object.hasOwnProperty.call(message, "rewardRate"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.rewardRate = typeof message.rewardRate === "number" ? $BigInt(message.rewardRate) : $util.Long.fromBits(message.rewardRate.low >>> 0, message.rewardRate.high >>> 0, false).toBigInt();
                else if (typeof message.rewardRate === "number")
                    object.rewardRate = options.longs === $String ? $String(message.rewardRate) : message.rewardRate;
                else
                    object.rewardRate = options.longs === $String ? $util.Long.prototype.toString.call(message.rewardRate) : options.longs === $Number ? new $util.LongBits(message.rewardRate.low >>> 0, message.rewardRate.high >>> 0).toNumber() : message.rewardRate;
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stake = typeof message.stake === "number" ? $BigInt(message.stake) : $util.Long.fromBits(message.stake.low >>> 0, message.stake.high >>> 0, false).toBigInt();
                else if (typeof message.stake === "number")
                    object.stake = options.longs === $String ? $String(message.stake) : message.stake;
                else
                    object.stake = options.longs === $String ? $util.Long.prototype.toString.call(message.stake) : options.longs === $Number ? new $util.LongBits(message.stake.low >>> 0, message.stake.high >>> 0).toNumber() : message.stake;
            if (message.stakeNotRewarded != null && $Object.hasOwnProperty.call(message, "stakeNotRewarded"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakeNotRewarded = typeof message.stakeNotRewarded === "number" ? $BigInt(message.stakeNotRewarded) : $util.Long.fromBits(message.stakeNotRewarded.low >>> 0, message.stakeNotRewarded.high >>> 0, false).toBigInt();
                else if (typeof message.stakeNotRewarded === "number")
                    object.stakeNotRewarded = options.longs === $String ? $String(message.stakeNotRewarded) : message.stakeNotRewarded;
                else
                    object.stakeNotRewarded = options.longs === $String ? $util.Long.prototype.toString.call(message.stakeNotRewarded) : options.longs === $Number ? new $util.LongBits(message.stakeNotRewarded.low >>> 0, message.stakeNotRewarded.high >>> 0).toNumber() : message.stakeNotRewarded;
            if (message.stakeRewarded != null && $Object.hasOwnProperty.call(message, "stakeRewarded"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakeRewarded = typeof message.stakeRewarded === "number" ? $BigInt(message.stakeRewarded) : $util.Long.fromBits(message.stakeRewarded.low >>> 0, message.stakeRewarded.high >>> 0, false).toBigInt();
                else if (typeof message.stakeRewarded === "number")
                    object.stakeRewarded = options.longs === $String ? $String(message.stakeRewarded) : message.stakeRewarded;
                else
                    object.stakeRewarded = options.longs === $String ? $util.Long.prototype.toString.call(message.stakeRewarded) : options.longs === $Number ? new $util.LongBits(message.stakeRewarded.low >>> 0, message.stakeRewarded.high >>> 0).toNumber() : message.stakeRewarded;
            return object;
        };

        /**
         * Converts this NodeStake to JSON.
         * @function toJSON
         * @memberof proto.NodeStake
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeStake.prototype.toJSON = function() {
            return NodeStake.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeStake
         * @function getTypeUrl
         * @memberof proto.NodeStake
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeStake.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NodeStake";
        };

        return NodeStake;
    })();

    proto.ConsensusGetTopicInfoQuery = (function() {

        /**
         * Properties of a ConsensusGetTopicInfoQuery.
         * @typedef {Object} proto.ConsensusGetTopicInfoQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.TopicID.$Properties|null} [topicID] A topic ID.
         * <p>
         * The network SHALL return information for this topic, if successful.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ConsensusGetTopicInfoQuery.
         * @memberof proto
         * @interface IConsensusGetTopicInfoQuery
         * @augments proto.ConsensusGetTopicInfoQuery.$Properties
         * @deprecated Use proto.ConsensusGetTopicInfoQuery.$Properties instead.
         */

        /**
         * Shape of a ConsensusGetTopicInfoQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   topicID?: proto.TopicID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ConsensusGetTopicInfoQuery.$Shape
         */

        /**
         * Constructs a new ConsensusGetTopicInfoQuery.
         * @memberof proto
         * @classdesc Retrieve the latest state of a topic.
         * 
         * This method is unrestricted and allowed on any topic by any payer
         * account.<br/>
         * A query for a deleted topic MAY succeed if the topic is within the
         * "autorenew grace period".<br/>
         * A query for a topic removed from state SHALL NOT succeed.
         * @constructor
         * @param {proto.ConsensusGetTopicInfoQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ConsensusGetTopicInfoQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.ConsensusGetTopicInfoQuery
         * @instance
         */
        ConsensusGetTopicInfoQuery.prototype.header = null;

        /**
         * A topic ID.
         * <p>
         * The network SHALL return information for this topic, if successful.
         * @member {proto.TopicID.$Properties|null|undefined} topicID
         * @memberof proto.ConsensusGetTopicInfoQuery
         * @instance
         */
        ConsensusGetTopicInfoQuery.prototype.topicID = null;

        /**
         * Creates a new ConsensusGetTopicInfoQuery instance using the specified properties.
         * @function create
         * @memberof proto.ConsensusGetTopicInfoQuery
         * @static
         * @param {proto.ConsensusGetTopicInfoQuery.$Properties=} [properties] Properties to set
         * @returns {proto.ConsensusGetTopicInfoQuery} ConsensusGetTopicInfoQuery instance
         * @type {{
         *   (properties: proto.ConsensusGetTopicInfoQuery.$Shape): proto.ConsensusGetTopicInfoQuery & proto.ConsensusGetTopicInfoQuery.$Shape;
         *   (properties?: proto.ConsensusGetTopicInfoQuery.$Properties): proto.ConsensusGetTopicInfoQuery;
         * }}
         */
        ConsensusGetTopicInfoQuery.create = function(properties) {
            return new ConsensusGetTopicInfoQuery(properties);
        };

        /**
         * Encodes the specified ConsensusGetTopicInfoQuery message. Does not implicitly {@link proto.ConsensusGetTopicInfoQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.ConsensusGetTopicInfoQuery
         * @static
         * @param {proto.ConsensusGetTopicInfoQuery.$Properties} message ConsensusGetTopicInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusGetTopicInfoQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID"))
                $root.proto.TopicID.encode(message.topicID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConsensusGetTopicInfoQuery message, length delimited. Does not implicitly {@link proto.ConsensusGetTopicInfoQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ConsensusGetTopicInfoQuery
         * @static
         * @param {proto.ConsensusGetTopicInfoQuery.$Properties} message ConsensusGetTopicInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusGetTopicInfoQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ConsensusGetTopicInfoQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ConsensusGetTopicInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ConsensusGetTopicInfoQuery & proto.ConsensusGetTopicInfoQuery.$Shape} ConsensusGetTopicInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusGetTopicInfoQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ConsensusGetTopicInfoQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.topicID = $root.proto.TopicID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.topicID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConsensusGetTopicInfoQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ConsensusGetTopicInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ConsensusGetTopicInfoQuery & proto.ConsensusGetTopicInfoQuery.$Shape} ConsensusGetTopicInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusGetTopicInfoQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConsensusGetTopicInfoQuery message.
         * @function verify
         * @memberof proto.ConsensusGetTopicInfoQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConsensusGetTopicInfoQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID")) {
                let error = $root.proto.TopicID.verify(message.topicID, _depth + 1);
                if (error)
                    return "topicID." + error;
            }
            return null;
        };

        /**
         * Creates a ConsensusGetTopicInfoQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ConsensusGetTopicInfoQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ConsensusGetTopicInfoQuery} ConsensusGetTopicInfoQuery
         */
        ConsensusGetTopicInfoQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ConsensusGetTopicInfoQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ConsensusGetTopicInfoQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ConsensusGetTopicInfoQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.ConsensusGetTopicInfoQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.topicID != null) {
                if (!$util.isObject(object.topicID))
                    throw $TypeError(".proto.ConsensusGetTopicInfoQuery.topicID: object expected");
                message.topicID = $root.proto.TopicID.fromObject(object.topicID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ConsensusGetTopicInfoQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ConsensusGetTopicInfoQuery
         * @static
         * @param {proto.ConsensusGetTopicInfoQuery} message ConsensusGetTopicInfoQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConsensusGetTopicInfoQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.topicID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID"))
                object.topicID = $root.proto.TopicID.toObject(message.topicID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ConsensusGetTopicInfoQuery to JSON.
         * @function toJSON
         * @memberof proto.ConsensusGetTopicInfoQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConsensusGetTopicInfoQuery.prototype.toJSON = function() {
            return ConsensusGetTopicInfoQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConsensusGetTopicInfoQuery
         * @function getTypeUrl
         * @memberof proto.ConsensusGetTopicInfoQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConsensusGetTopicInfoQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ConsensusGetTopicInfoQuery";
        };

        return ConsensusGetTopicInfoQuery;
    })();

    proto.ConsensusGetTopicInfoResponse = (function() {

        /**
         * Properties of a ConsensusGetTopicInfoResponse.
         * @typedef {Object} proto.ConsensusGetTopicInfoResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.TopicID.$Properties|null} [topicID] The ID of the topic requested in the query.
         * @property {proto.ConsensusTopicInfo.$Properties|null} [topicInfo] Information describing the current state of the topic.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ConsensusGetTopicInfoResponse.
         * @memberof proto
         * @interface IConsensusGetTopicInfoResponse
         * @augments proto.ConsensusGetTopicInfoResponse.$Properties
         * @deprecated Use proto.ConsensusGetTopicInfoResponse.$Properties instead.
         */

        /**
         * Shape of a ConsensusGetTopicInfoResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   topicID?: proto.TopicID.$Shape|null;
         *   topicInfo?: proto.ConsensusTopicInfo.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ConsensusGetTopicInfoResponse.$Shape
         */

        /**
         * Constructs a new ConsensusGetTopicInfoResponse.
         * @memberof proto
         * @classdesc Query response to describe the current state of a topic in the Hedera
         * Consensus Service(HCS).
         * @constructor
         * @param {proto.ConsensusGetTopicInfoResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ConsensusGetTopicInfoResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @instance
         */
        ConsensusGetTopicInfoResponse.prototype.header = null;

        /**
         * The ID of the topic requested in the query.
         * @member {proto.TopicID.$Properties|null|undefined} topicID
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @instance
         */
        ConsensusGetTopicInfoResponse.prototype.topicID = null;

        /**
         * Information describing the current state of the topic.
         * @member {proto.ConsensusTopicInfo.$Properties|null|undefined} topicInfo
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @instance
         */
        ConsensusGetTopicInfoResponse.prototype.topicInfo = null;

        /**
         * Creates a new ConsensusGetTopicInfoResponse instance using the specified properties.
         * @function create
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @static
         * @param {proto.ConsensusGetTopicInfoResponse.$Properties=} [properties] Properties to set
         * @returns {proto.ConsensusGetTopicInfoResponse} ConsensusGetTopicInfoResponse instance
         * @type {{
         *   (properties: proto.ConsensusGetTopicInfoResponse.$Shape): proto.ConsensusGetTopicInfoResponse & proto.ConsensusGetTopicInfoResponse.$Shape;
         *   (properties?: proto.ConsensusGetTopicInfoResponse.$Properties): proto.ConsensusGetTopicInfoResponse;
         * }}
         */
        ConsensusGetTopicInfoResponse.create = function(properties) {
            return new ConsensusGetTopicInfoResponse(properties);
        };

        /**
         * Encodes the specified ConsensusGetTopicInfoResponse message. Does not implicitly {@link proto.ConsensusGetTopicInfoResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @static
         * @param {proto.ConsensusGetTopicInfoResponse.$Properties} message ConsensusGetTopicInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusGetTopicInfoResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID"))
                $root.proto.TopicID.encode(message.topicID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.topicInfo != null && $Object.hasOwnProperty.call(message, "topicInfo"))
                $root.proto.ConsensusTopicInfo.encode(message.topicInfo, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConsensusGetTopicInfoResponse message, length delimited. Does not implicitly {@link proto.ConsensusGetTopicInfoResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @static
         * @param {proto.ConsensusGetTopicInfoResponse.$Properties} message ConsensusGetTopicInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusGetTopicInfoResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ConsensusGetTopicInfoResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ConsensusGetTopicInfoResponse & proto.ConsensusGetTopicInfoResponse.$Shape} ConsensusGetTopicInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusGetTopicInfoResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ConsensusGetTopicInfoResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.topicID = $root.proto.TopicID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.topicID);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.topicInfo = $root.proto.ConsensusTopicInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.topicInfo);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConsensusGetTopicInfoResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ConsensusGetTopicInfoResponse & proto.ConsensusGetTopicInfoResponse.$Shape} ConsensusGetTopicInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusGetTopicInfoResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConsensusGetTopicInfoResponse message.
         * @function verify
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConsensusGetTopicInfoResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID")) {
                let error = $root.proto.TopicID.verify(message.topicID, _depth + 1);
                if (error)
                    return "topicID." + error;
            }
            if (message.topicInfo != null && $Object.hasOwnProperty.call(message, "topicInfo")) {
                let error = $root.proto.ConsensusTopicInfo.verify(message.topicInfo, _depth + 1);
                if (error)
                    return "topicInfo." + error;
            }
            return null;
        };

        /**
         * Creates a ConsensusGetTopicInfoResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ConsensusGetTopicInfoResponse} ConsensusGetTopicInfoResponse
         */
        ConsensusGetTopicInfoResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ConsensusGetTopicInfoResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ConsensusGetTopicInfoResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ConsensusGetTopicInfoResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.ConsensusGetTopicInfoResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.topicID != null) {
                if (!$util.isObject(object.topicID))
                    throw $TypeError(".proto.ConsensusGetTopicInfoResponse.topicID: object expected");
                message.topicID = $root.proto.TopicID.fromObject(object.topicID, _depth + 1);
            }
            if (object.topicInfo != null) {
                if (!$util.isObject(object.topicInfo))
                    throw $TypeError(".proto.ConsensusGetTopicInfoResponse.topicInfo: object expected");
                message.topicInfo = $root.proto.ConsensusTopicInfo.fromObject(object.topicInfo, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ConsensusGetTopicInfoResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @static
         * @param {proto.ConsensusGetTopicInfoResponse} message ConsensusGetTopicInfoResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConsensusGetTopicInfoResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.topicID = null;
                object.topicInfo = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID"))
                object.topicID = $root.proto.TopicID.toObject(message.topicID, options, _depth + 1);
            if (message.topicInfo != null && $Object.hasOwnProperty.call(message, "topicInfo"))
                object.topicInfo = $root.proto.ConsensusTopicInfo.toObject(message.topicInfo, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ConsensusGetTopicInfoResponse to JSON.
         * @function toJSON
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConsensusGetTopicInfoResponse.prototype.toJSON = function() {
            return ConsensusGetTopicInfoResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConsensusGetTopicInfoResponse
         * @function getTypeUrl
         * @memberof proto.ConsensusGetTopicInfoResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConsensusGetTopicInfoResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ConsensusGetTopicInfoResponse";
        };

        return ConsensusGetTopicInfoResponse;
    })();

    /**
     * The type of query response.<br/>
     * 
     * This SHALL be answer-only as a default.<br/>
     * This value SHALL support an "estimated cost" type.<br/>
     * This value SHOULD support a "state proof" type, when available.
     * @name proto.ResponseType
     * @enum {number}
     * @property {number} ANSWER_ONLY=0 A response with the query answer.
     * @property {number} ANSWER_STATE_PROOF=1 A response with both the query answer and a state proof.
     * @property {number} COST_ANSWER=2 A response with the estimated cost to answer the query.
     * @property {number} COST_ANSWER_STATE_PROOF=3 A response with the estimated cost to answer and a state proof.
     */
    proto.ResponseType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "ANSWER_ONLY"] = 0;
        values[valuesById[1] = "ANSWER_STATE_PROOF"] = 1;
        values[valuesById[2] = "COST_ANSWER"] = 2;
        values[valuesById[3] = "COST_ANSWER_STATE_PROOF"] = 3;
        return values;
    })();

    proto.QueryHeader = (function() {

        /**
         * Properties of a QueryHeader.
         * @typedef {Object} proto.QueryHeader.$Properties
         * @property {proto.Transaction.$Properties|null} [payment] A signed `CryptoTransferTransaction` to pay query fees.
         * <p>
         * This MUST transfer HBAR from the "payer" to the responding node account
         * sufficient to pay the query fees.
         * @property {proto.ResponseType|null} [responseType] A type of query response requested.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a QueryHeader.
         * @memberof proto
         * @interface IQueryHeader
         * @augments proto.QueryHeader.$Properties
         * @deprecated Use proto.QueryHeader.$Properties instead.
         */

        /**
         * Shape of a QueryHeader.
         * @typedef {{
         *   payment?: proto.Transaction.$Shape|null;
         *   responseType?: proto.ResponseType|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.QueryHeader.$Shape
         */

        /**
         * Constructs a new QueryHeader.
         * @memberof proto
         * @classdesc A standard query header.<br/>
         * Each query from the client to the node must contain a QueryHeader, which
         * specifies the desired response type, and includes a payment transaction
         * that will compensate the network for responding to the query.
         * The payment may be blank if the query is free.
         * 
         * The payment transaction MUST be a `cryptoTransfer` from the payer account
         * to the account of the node where the query is submitted.<br/>
         * If the payment is sufficient, the network SHALL respond with the response
         * type requested.<br/>
         * If the response type is `COST_ANSWER` the payment MUST be unset.
         * A state proof SHALL be available for some types of information.<br/>
         * A state proof SHALL be available for a Record, but not a receipt, and the
         * response entry for each supported "get info" query.
         * @constructor
         * @param {proto.QueryHeader.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const QueryHeader = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A signed `CryptoTransferTransaction` to pay query fees.
         * <p>
         * This MUST transfer HBAR from the "payer" to the responding node account
         * sufficient to pay the query fees.
         * @member {proto.Transaction.$Properties|null|undefined} payment
         * @memberof proto.QueryHeader
         * @instance
         */
        QueryHeader.prototype.payment = null;

        /**
         * A type of query response requested.
         * @member {proto.ResponseType} responseType
         * @memberof proto.QueryHeader
         * @instance
         */
        QueryHeader.prototype.responseType = 0;

        /**
         * Creates a new QueryHeader instance using the specified properties.
         * @function create
         * @memberof proto.QueryHeader
         * @static
         * @param {proto.QueryHeader.$Properties=} [properties] Properties to set
         * @returns {proto.QueryHeader} QueryHeader instance
         * @type {{
         *   (properties: proto.QueryHeader.$Shape): proto.QueryHeader & proto.QueryHeader.$Shape;
         *   (properties?: proto.QueryHeader.$Properties): proto.QueryHeader;
         * }}
         */
        QueryHeader.create = function(properties) {
            return new QueryHeader(properties);
        };

        /**
         * Encodes the specified QueryHeader message. Does not implicitly {@link proto.QueryHeader.verify|verify} messages.
         * @function encode
         * @memberof proto.QueryHeader
         * @static
         * @param {proto.QueryHeader.$Properties} message QueryHeader message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryHeader.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.payment != null && $Object.hasOwnProperty.call(message, "payment"))
                $root.proto.Transaction.encode(message.payment, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.responseType != null && $Object.hasOwnProperty.call(message, "responseType") && message.responseType !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.responseType);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified QueryHeader message, length delimited. Does not implicitly {@link proto.QueryHeader.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.QueryHeader
         * @static
         * @param {proto.QueryHeader.$Properties} message QueryHeader message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryHeader.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a QueryHeader message from the specified reader or buffer.
         * @function decode
         * @memberof proto.QueryHeader
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.QueryHeader & proto.QueryHeader.$Shape} QueryHeader
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryHeader.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.QueryHeader();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.payment = $root.proto.Transaction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.payment);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.responseType = value;
                        else
                            delete message.responseType;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a QueryHeader message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.QueryHeader
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.QueryHeader & proto.QueryHeader.$Shape} QueryHeader
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryHeader.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QueryHeader message.
         * @function verify
         * @memberof proto.QueryHeader
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QueryHeader.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.payment != null && $Object.hasOwnProperty.call(message, "payment")) {
                let error = $root.proto.Transaction.verify(message.payment, _depth + 1);
                if (error)
                    return "payment." + error;
            }
            if (message.responseType != null && $Object.hasOwnProperty.call(message, "responseType"))
                if (typeof message.responseType !== "number" || (message.responseType | 0) !== message.responseType)
                    return "responseType: enum value expected";
            return null;
        };

        /**
         * Creates a QueryHeader message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.QueryHeader
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.QueryHeader} QueryHeader
         */
        QueryHeader.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.QueryHeader)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.QueryHeader: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.QueryHeader();
            if (object.payment != null) {
                if (!$util.isObject(object.payment))
                    throw $TypeError(".proto.QueryHeader.payment: object expected");
                message.payment = $root.proto.Transaction.fromObject(object.payment, _depth + 1);
            }
            if (object.responseType !== 0 && (typeof object.responseType !== "string" || $root.proto.ResponseType[object.responseType] !== 0))
                switch (object.responseType) {
                case "ANSWER_ONLY":
                case 0:
                    message.responseType = 0;
                    break;
                case "ANSWER_STATE_PROOF":
                case 1:
                    message.responseType = 1;
                    break;
                case "COST_ANSWER":
                case 2:
                    message.responseType = 2;
                    break;
                case "COST_ANSWER_STATE_PROOF":
                case 3:
                    message.responseType = 3;
                    break;
                default:
                    if (typeof object.responseType === "number" && (object.responseType | 0) === object.responseType)
                        message.responseType = object.responseType;
                }
            return message;
        };

        /**
         * Creates a plain object from a QueryHeader message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.QueryHeader
         * @static
         * @param {proto.QueryHeader} message QueryHeader
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QueryHeader.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.payment = null;
                object.responseType = options.enums === $String ? "ANSWER_ONLY" : 0;
            }
            if (message.payment != null && $Object.hasOwnProperty.call(message, "payment"))
                object.payment = $root.proto.Transaction.toObject(message.payment, options, _depth + 1);
            if (message.responseType != null && $Object.hasOwnProperty.call(message, "responseType"))
                object.responseType = options.enums === $String ? $root.proto.ResponseType[message.responseType] === $undefined ? message.responseType : $root.proto.ResponseType[message.responseType] : message.responseType;
            return object;
        };

        /**
         * Converts this QueryHeader to JSON.
         * @function toJSON
         * @memberof proto.QueryHeader
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QueryHeader.prototype.toJSON = function() {
            return QueryHeader.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for QueryHeader
         * @function getTypeUrl
         * @memberof proto.QueryHeader
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        QueryHeader.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.QueryHeader";
        };

        return QueryHeader;
    })();

    proto.ResponseHeader = (function() {

        /**
         * Properties of a ResponseHeader.
         * @typedef {Object} proto.ResponseHeader.$Properties
         * @property {proto.ResponseCodeEnum|null} [nodeTransactionPrecheckCode] The result code for this query.
         * <p>
         * This value SHALL indicate either success or the reason for failure.
         * @property {proto.ResponseType|null} [responseType] The response type requested for this query.
         * <p>
         * This SHALL be the response type requested in the query header.
         * @property {number|Long|null} [cost] Requested cost estimate.<br/>
         * This is the fee that _would be_ charged if the query was executed.
         * <p>
         * This value SHALL be set if the response type requested requires cost
         * information, and SHALL NOT be set otherwise.<br/>
         * This value SHALL include the query fee, but SHALL NOT include the
         * transfer fee required to execute the fee payment transaction.
         * @property {Uint8Array|null} [stateProof] A state proof for the information requested.
         * 
         * This field SHALL NOT be set if the response type does not require
         * a state proof.<br/>
         * This field SHALL NOT be set if a state proof is not available for
         * the query type.<br/>
         * This field SHALL be set if the response type requested a state proof
         * and a state proof is available.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ResponseHeader.
         * @memberof proto
         * @interface IResponseHeader
         * @augments proto.ResponseHeader.$Properties
         * @deprecated Use proto.ResponseHeader.$Properties instead.
         */

        /**
         * Shape of a ResponseHeader.
         * @typedef {proto.ResponseHeader.$Properties} proto.ResponseHeader.$Shape
         */

        /**
         * Constructs a new ResponseHeader.
         * @memberof proto
         * @classdesc A standard header returned with every query response.
         * 
         * The fields for `cost` or `stateProof` MAY be unset if the requested
         * `ResponseType` does not request those values.<br/>
         * The `responseType` SHALL match the request response type.<br/>
         * The `nodeTransactionPrecheckCode` field SHALL contain the result code
         * for the query.
         * @constructor
         * @param {proto.ResponseHeader.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ResponseHeader = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The result code for this query.
         * <p>
         * This value SHALL indicate either success or the reason for failure.
         * @member {proto.ResponseCodeEnum} nodeTransactionPrecheckCode
         * @memberof proto.ResponseHeader
         * @instance
         */
        ResponseHeader.prototype.nodeTransactionPrecheckCode = 0;

        /**
         * The response type requested for this query.
         * <p>
         * This SHALL be the response type requested in the query header.
         * @member {proto.ResponseType} responseType
         * @memberof proto.ResponseHeader
         * @instance
         */
        ResponseHeader.prototype.responseType = 0;

        /**
         * Requested cost estimate.<br/>
         * This is the fee that _would be_ charged if the query was executed.
         * <p>
         * This value SHALL be set if the response type requested requires cost
         * information, and SHALL NOT be set otherwise.<br/>
         * This value SHALL include the query fee, but SHALL NOT include the
         * transfer fee required to execute the fee payment transaction.
         * @member {number|Long} cost
         * @memberof proto.ResponseHeader
         * @instance
         */
        ResponseHeader.prototype.cost = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A state proof for the information requested.
         * 
         * This field SHALL NOT be set if the response type does not require
         * a state proof.<br/>
         * This field SHALL NOT be set if a state proof is not available for
         * the query type.<br/>
         * This field SHALL be set if the response type requested a state proof
         * and a state proof is available.
         * @member {Uint8Array} stateProof
         * @memberof proto.ResponseHeader
         * @instance
         */
        ResponseHeader.prototype.stateProof = $util.newBuffer([]);

        /**
         * Creates a new ResponseHeader instance using the specified properties.
         * @function create
         * @memberof proto.ResponseHeader
         * @static
         * @param {proto.ResponseHeader.$Properties=} [properties] Properties to set
         * @returns {proto.ResponseHeader} ResponseHeader instance
         * @type {{
         *   (properties: proto.ResponseHeader.$Shape): proto.ResponseHeader & proto.ResponseHeader.$Shape;
         *   (properties?: proto.ResponseHeader.$Properties): proto.ResponseHeader;
         * }}
         */
        ResponseHeader.create = function(properties) {
            return new ResponseHeader(properties);
        };

        /**
         * Encodes the specified ResponseHeader message. Does not implicitly {@link proto.ResponseHeader.verify|verify} messages.
         * @function encode
         * @memberof proto.ResponseHeader
         * @static
         * @param {proto.ResponseHeader.$Properties} message ResponseHeader message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResponseHeader.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nodeTransactionPrecheckCode != null && $Object.hasOwnProperty.call(message, "nodeTransactionPrecheckCode") && message.nodeTransactionPrecheckCode !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.nodeTransactionPrecheckCode);
            if (message.responseType != null && $Object.hasOwnProperty.call(message, "responseType") && message.responseType !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.responseType);
            if (message.cost != null && $Object.hasOwnProperty.call(message, "cost") && (typeof message.cost === "object" ? message.cost.low || message.cost.high : message.cost !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.cost);
            if (message.stateProof != null && $Object.hasOwnProperty.call(message, "stateProof") && message.stateProof.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.stateProof);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ResponseHeader message, length delimited. Does not implicitly {@link proto.ResponseHeader.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ResponseHeader
         * @static
         * @param {proto.ResponseHeader.$Properties} message ResponseHeader message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResponseHeader.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ResponseHeader message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ResponseHeader
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ResponseHeader & proto.ResponseHeader.$Shape} ResponseHeader
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResponseHeader.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ResponseHeader();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.nodeTransactionPrecheckCode = value;
                        else
                            delete message.nodeTransactionPrecheckCode;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.responseType = value;
                        else
                            delete message.responseType;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.cost = value;
                        else
                            delete message.cost;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.stateProof = value;
                        else
                            delete message.stateProof;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ResponseHeader message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ResponseHeader
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ResponseHeader & proto.ResponseHeader.$Shape} ResponseHeader
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResponseHeader.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ResponseHeader message.
         * @function verify
         * @memberof proto.ResponseHeader
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ResponseHeader.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nodeTransactionPrecheckCode != null && $Object.hasOwnProperty.call(message, "nodeTransactionPrecheckCode"))
                if (typeof message.nodeTransactionPrecheckCode !== "number" || (message.nodeTransactionPrecheckCode | 0) !== message.nodeTransactionPrecheckCode)
                    return "nodeTransactionPrecheckCode: enum value expected";
            if (message.responseType != null && $Object.hasOwnProperty.call(message, "responseType"))
                if (typeof message.responseType !== "number" || (message.responseType | 0) !== message.responseType)
                    return "responseType: enum value expected";
            if (message.cost != null && $Object.hasOwnProperty.call(message, "cost"))
                if (!$util.isInteger(message.cost) && !(message.cost && $util.isInteger(message.cost.low) && $util.isInteger(message.cost.high)))
                    return "cost: integer|Long expected";
            if (message.stateProof != null && $Object.hasOwnProperty.call(message, "stateProof"))
                if (!(message.stateProof && typeof message.stateProof.length === "number" || $util.isString(message.stateProof)))
                    return "stateProof: buffer expected";
            return null;
        };

        /**
         * Creates a ResponseHeader message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ResponseHeader
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ResponseHeader} ResponseHeader
         */
        ResponseHeader.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ResponseHeader)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ResponseHeader: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ResponseHeader();
            if (object.nodeTransactionPrecheckCode !== 0 && (typeof object.nodeTransactionPrecheckCode !== "string" || $root.proto.ResponseCodeEnum[object.nodeTransactionPrecheckCode] !== 0))
                switch (object.nodeTransactionPrecheckCode) {
                case "OK":
                case 0:
                    message.nodeTransactionPrecheckCode = 0;
                    break;
                case "INVALID_TRANSACTION":
                case 1:
                    message.nodeTransactionPrecheckCode = 1;
                    break;
                case "PAYER_ACCOUNT_NOT_FOUND":
                case 2:
                    message.nodeTransactionPrecheckCode = 2;
                    break;
                case "INVALID_NODE_ACCOUNT":
                case 3:
                    message.nodeTransactionPrecheckCode = 3;
                    break;
                case "TRANSACTION_EXPIRED":
                case 4:
                    message.nodeTransactionPrecheckCode = 4;
                    break;
                case "INVALID_TRANSACTION_START":
                case 5:
                    message.nodeTransactionPrecheckCode = 5;
                    break;
                case "INVALID_TRANSACTION_DURATION":
                case 6:
                    message.nodeTransactionPrecheckCode = 6;
                    break;
                case "INVALID_SIGNATURE":
                case 7:
                    message.nodeTransactionPrecheckCode = 7;
                    break;
                case "MEMO_TOO_LONG":
                case 8:
                    message.nodeTransactionPrecheckCode = 8;
                    break;
                case "INSUFFICIENT_TX_FEE":
                case 9:
                    message.nodeTransactionPrecheckCode = 9;
                    break;
                case "INSUFFICIENT_PAYER_BALANCE":
                case 10:
                    message.nodeTransactionPrecheckCode = 10;
                    break;
                case "DUPLICATE_TRANSACTION":
                case 11:
                    message.nodeTransactionPrecheckCode = 11;
                    break;
                case "BUSY":
                case 12:
                    message.nodeTransactionPrecheckCode = 12;
                    break;
                case "NOT_SUPPORTED":
                case 13:
                    message.nodeTransactionPrecheckCode = 13;
                    break;
                case "INVALID_FILE_ID":
                case 14:
                    message.nodeTransactionPrecheckCode = 14;
                    break;
                case "INVALID_ACCOUNT_ID":
                case 15:
                    message.nodeTransactionPrecheckCode = 15;
                    break;
                case "INVALID_CONTRACT_ID":
                case 16:
                    message.nodeTransactionPrecheckCode = 16;
                    break;
                case "INVALID_TRANSACTION_ID":
                case 17:
                    message.nodeTransactionPrecheckCode = 17;
                    break;
                case "RECEIPT_NOT_FOUND":
                case 18:
                    message.nodeTransactionPrecheckCode = 18;
                    break;
                case "RECORD_NOT_FOUND":
                case 19:
                    message.nodeTransactionPrecheckCode = 19;
                    break;
                case "INVALID_SOLIDITY_ID":
                case 20:
                    message.nodeTransactionPrecheckCode = 20;
                    break;
                case "UNKNOWN":
                case 21:
                    message.nodeTransactionPrecheckCode = 21;
                    break;
                case "SUCCESS":
                case 22:
                    message.nodeTransactionPrecheckCode = 22;
                    break;
                case "FAIL_INVALID":
                case 23:
                    message.nodeTransactionPrecheckCode = 23;
                    break;
                case "FAIL_FEE":
                case 24:
                    message.nodeTransactionPrecheckCode = 24;
                    break;
                case "FAIL_BALANCE":
                case 25:
                    message.nodeTransactionPrecheckCode = 25;
                    break;
                case "KEY_REQUIRED":
                case 26:
                    message.nodeTransactionPrecheckCode = 26;
                    break;
                case "BAD_ENCODING":
                case 27:
                    message.nodeTransactionPrecheckCode = 27;
                    break;
                case "INSUFFICIENT_ACCOUNT_BALANCE":
                case 28:
                    message.nodeTransactionPrecheckCode = 28;
                    break;
                case "INVALID_SOLIDITY_ADDRESS":
                case 29:
                    message.nodeTransactionPrecheckCode = 29;
                    break;
                case "INSUFFICIENT_GAS":
                case 30:
                    message.nodeTransactionPrecheckCode = 30;
                    break;
                case "CONTRACT_SIZE_LIMIT_EXCEEDED":
                case 31:
                    message.nodeTransactionPrecheckCode = 31;
                    break;
                case "LOCAL_CALL_MODIFICATION_EXCEPTION":
                case 32:
                    message.nodeTransactionPrecheckCode = 32;
                    break;
                case "CONTRACT_REVERT_EXECUTED":
                case 33:
                    message.nodeTransactionPrecheckCode = 33;
                    break;
                case "CONTRACT_EXECUTION_EXCEPTION":
                case 34:
                    message.nodeTransactionPrecheckCode = 34;
                    break;
                case "INVALID_RECEIVING_NODE_ACCOUNT":
                case 35:
                    message.nodeTransactionPrecheckCode = 35;
                    break;
                case "MISSING_QUERY_HEADER":
                case 36:
                    message.nodeTransactionPrecheckCode = 36;
                    break;
                case "ACCOUNT_UPDATE_FAILED":
                case 37:
                    message.nodeTransactionPrecheckCode = 37;
                    break;
                case "INVALID_KEY_ENCODING":
                case 38:
                    message.nodeTransactionPrecheckCode = 38;
                    break;
                case "NULL_SOLIDITY_ADDRESS":
                case 39:
                    message.nodeTransactionPrecheckCode = 39;
                    break;
                case "CONTRACT_UPDATE_FAILED":
                case 40:
                    message.nodeTransactionPrecheckCode = 40;
                    break;
                case "INVALID_QUERY_HEADER":
                case 41:
                    message.nodeTransactionPrecheckCode = 41;
                    break;
                case "INVALID_FEE_SUBMITTED":
                case 42:
                    message.nodeTransactionPrecheckCode = 42;
                    break;
                case "INVALID_PAYER_SIGNATURE":
                case 43:
                    message.nodeTransactionPrecheckCode = 43;
                    break;
                case "KEY_NOT_PROVIDED":
                case 44:
                    message.nodeTransactionPrecheckCode = 44;
                    break;
                case "INVALID_EXPIRATION_TIME":
                case 45:
                    message.nodeTransactionPrecheckCode = 45;
                    break;
                case "NO_WACL_KEY":
                case 46:
                    message.nodeTransactionPrecheckCode = 46;
                    break;
                case "FILE_CONTENT_EMPTY":
                case 47:
                    message.nodeTransactionPrecheckCode = 47;
                    break;
                case "INVALID_ACCOUNT_AMOUNTS":
                case 48:
                    message.nodeTransactionPrecheckCode = 48;
                    break;
                case "EMPTY_TRANSACTION_BODY":
                case 49:
                    message.nodeTransactionPrecheckCode = 49;
                    break;
                case "INVALID_TRANSACTION_BODY":
                case 50:
                    message.nodeTransactionPrecheckCode = 50;
                    break;
                case "INVALID_SIGNATURE_TYPE_MISMATCHING_KEY":
                case 51:
                    message.nodeTransactionPrecheckCode = 51;
                    break;
                case "INVALID_SIGNATURE_COUNT_MISMATCHING_KEY":
                case 52:
                    message.nodeTransactionPrecheckCode = 52;
                    break;
                case "EMPTY_LIVE_HASH_BODY":
                case 53:
                    message.nodeTransactionPrecheckCode = 53;
                    break;
                case "EMPTY_LIVE_HASH":
                case 54:
                    message.nodeTransactionPrecheckCode = 54;
                    break;
                case "EMPTY_LIVE_HASH_KEYS":
                case 55:
                    message.nodeTransactionPrecheckCode = 55;
                    break;
                case "INVALID_LIVE_HASH_SIZE":
                case 56:
                    message.nodeTransactionPrecheckCode = 56;
                    break;
                case "EMPTY_QUERY_BODY":
                case 57:
                    message.nodeTransactionPrecheckCode = 57;
                    break;
                case "EMPTY_LIVE_HASH_QUERY":
                case 58:
                    message.nodeTransactionPrecheckCode = 58;
                    break;
                case "LIVE_HASH_NOT_FOUND":
                case 59:
                    message.nodeTransactionPrecheckCode = 59;
                    break;
                case "ACCOUNT_ID_DOES_NOT_EXIST":
                case 60:
                    message.nodeTransactionPrecheckCode = 60;
                    break;
                case "LIVE_HASH_ALREADY_EXISTS":
                case 61:
                    message.nodeTransactionPrecheckCode = 61;
                    break;
                case "INVALID_FILE_WACL":
                case 62:
                    message.nodeTransactionPrecheckCode = 62;
                    break;
                case "SERIALIZATION_FAILED":
                case 63:
                    message.nodeTransactionPrecheckCode = 63;
                    break;
                case "TRANSACTION_OVERSIZE":
                case 64:
                    message.nodeTransactionPrecheckCode = 64;
                    break;
                case "TRANSACTION_TOO_MANY_LAYERS":
                case 65:
                    message.nodeTransactionPrecheckCode = 65;
                    break;
                case "CONTRACT_DELETED":
                case 66:
                    message.nodeTransactionPrecheckCode = 66;
                    break;
                case "PLATFORM_NOT_ACTIVE":
                case 67:
                    message.nodeTransactionPrecheckCode = 67;
                    break;
                case "KEY_PREFIX_MISMATCH":
                case 68:
                    message.nodeTransactionPrecheckCode = 68;
                    break;
                case "PLATFORM_TRANSACTION_NOT_CREATED":
                case 69:
                    message.nodeTransactionPrecheckCode = 69;
                    break;
                case "INVALID_RENEWAL_PERIOD":
                case 70:
                    message.nodeTransactionPrecheckCode = 70;
                    break;
                case "INVALID_PAYER_ACCOUNT_ID":
                case 71:
                    message.nodeTransactionPrecheckCode = 71;
                    break;
                case "ACCOUNT_DELETED":
                case 72:
                    message.nodeTransactionPrecheckCode = 72;
                    break;
                case "FILE_DELETED":
                case 73:
                    message.nodeTransactionPrecheckCode = 73;
                    break;
                case "ACCOUNT_REPEATED_IN_ACCOUNT_AMOUNTS":
                case 74:
                    message.nodeTransactionPrecheckCode = 74;
                    break;
                case "SETTING_NEGATIVE_ACCOUNT_BALANCE":
                case 75:
                    message.nodeTransactionPrecheckCode = 75;
                    break;
                case "OBTAINER_REQUIRED":
                case 76:
                    message.nodeTransactionPrecheckCode = 76;
                    break;
                case "OBTAINER_SAME_CONTRACT_ID":
                case 77:
                    message.nodeTransactionPrecheckCode = 77;
                    break;
                case "OBTAINER_DOES_NOT_EXIST":
                case 78:
                    message.nodeTransactionPrecheckCode = 78;
                    break;
                case "MODIFYING_IMMUTABLE_CONTRACT":
                case 79:
                    message.nodeTransactionPrecheckCode = 79;
                    break;
                case "FILE_SYSTEM_EXCEPTION":
                case 80:
                    message.nodeTransactionPrecheckCode = 80;
                    break;
                case "AUTORENEW_DURATION_NOT_IN_RANGE":
                case 81:
                    message.nodeTransactionPrecheckCode = 81;
                    break;
                case "ERROR_DECODING_BYTESTRING":
                case 82:
                    message.nodeTransactionPrecheckCode = 82;
                    break;
                case "CONTRACT_FILE_EMPTY":
                case 83:
                    message.nodeTransactionPrecheckCode = 83;
                    break;
                case "CONTRACT_BYTECODE_EMPTY":
                case 84:
                    message.nodeTransactionPrecheckCode = 84;
                    break;
                case "INVALID_INITIAL_BALANCE":
                case 85:
                    message.nodeTransactionPrecheckCode = 85;
                    break;
                case "INVALID_RECEIVE_RECORD_THRESHOLD":
                case 86:
                    message.nodeTransactionPrecheckCode = 86;
                    break;
                case "INVALID_SEND_RECORD_THRESHOLD":
                case 87:
                    message.nodeTransactionPrecheckCode = 87;
                    break;
                case "ACCOUNT_IS_NOT_GENESIS_ACCOUNT":
                case 88:
                    message.nodeTransactionPrecheckCode = 88;
                    break;
                case "PAYER_ACCOUNT_UNAUTHORIZED":
                case 89:
                    message.nodeTransactionPrecheckCode = 89;
                    break;
                case "INVALID_FREEZE_TRANSACTION_BODY":
                case 90:
                    message.nodeTransactionPrecheckCode = 90;
                    break;
                case "FREEZE_TRANSACTION_BODY_NOT_FOUND":
                case 91:
                    message.nodeTransactionPrecheckCode = 91;
                    break;
                case "TRANSFER_LIST_SIZE_LIMIT_EXCEEDED":
                case 92:
                    message.nodeTransactionPrecheckCode = 92;
                    break;
                case "RESULT_SIZE_LIMIT_EXCEEDED":
                case 93:
                    message.nodeTransactionPrecheckCode = 93;
                    break;
                case "NOT_SPECIAL_ACCOUNT":
                case 94:
                    message.nodeTransactionPrecheckCode = 94;
                    break;
                case "CONTRACT_NEGATIVE_GAS":
                case 95:
                    message.nodeTransactionPrecheckCode = 95;
                    break;
                case "CONTRACT_NEGATIVE_VALUE":
                case 96:
                    message.nodeTransactionPrecheckCode = 96;
                    break;
                case "INVALID_FEE_FILE":
                case 97:
                    message.nodeTransactionPrecheckCode = 97;
                    break;
                case "INVALID_EXCHANGE_RATE_FILE":
                case 98:
                    message.nodeTransactionPrecheckCode = 98;
                    break;
                case "INSUFFICIENT_LOCAL_CALL_GAS":
                case 99:
                    message.nodeTransactionPrecheckCode = 99;
                    break;
                case "ENTITY_NOT_ALLOWED_TO_DELETE":
                case 100:
                    message.nodeTransactionPrecheckCode = 100;
                    break;
                case "AUTHORIZATION_FAILED":
                case 101:
                    message.nodeTransactionPrecheckCode = 101;
                    break;
                case "FILE_UPLOADED_PROTO_INVALID":
                case 102:
                    message.nodeTransactionPrecheckCode = 102;
                    break;
                case "FILE_UPLOADED_PROTO_NOT_SAVED_TO_DISK":
                case 103:
                    message.nodeTransactionPrecheckCode = 103;
                    break;
                case "FEE_SCHEDULE_FILE_PART_UPLOADED":
                case 104:
                    message.nodeTransactionPrecheckCode = 104;
                    break;
                case "EXCHANGE_RATE_CHANGE_LIMIT_EXCEEDED":
                case 105:
                    message.nodeTransactionPrecheckCode = 105;
                    break;
                case "MAX_CONTRACT_STORAGE_EXCEEDED":
                case 106:
                    message.nodeTransactionPrecheckCode = 106;
                    break;
                case "TRANSFER_ACCOUNT_SAME_AS_DELETE_ACCOUNT":
                case 107:
                    message.nodeTransactionPrecheckCode = 107;
                    break;
                case "TOTAL_LEDGER_BALANCE_INVALID":
                case 108:
                    message.nodeTransactionPrecheckCode = 108;
                    break;
                case "EXPIRATION_REDUCTION_NOT_ALLOWED":
                case 110:
                    message.nodeTransactionPrecheckCode = 110;
                    break;
                case "MAX_GAS_LIMIT_EXCEEDED":
                case 111:
                    message.nodeTransactionPrecheckCode = 111;
                    break;
                case "MAX_FILE_SIZE_EXCEEDED":
                case 112:
                    message.nodeTransactionPrecheckCode = 112;
                    break;
                case "RECEIVER_SIG_REQUIRED":
                case 113:
                    message.nodeTransactionPrecheckCode = 113;
                    break;
                case "INVALID_TOPIC_ID":
                case 150:
                    message.nodeTransactionPrecheckCode = 150;
                    break;
                case "INVALID_ADMIN_KEY":
                case 155:
                    message.nodeTransactionPrecheckCode = 155;
                    break;
                case "INVALID_SUBMIT_KEY":
                case 156:
                    message.nodeTransactionPrecheckCode = 156;
                    break;
                case "UNAUTHORIZED":
                case 157:
                    message.nodeTransactionPrecheckCode = 157;
                    break;
                case "INVALID_TOPIC_MESSAGE":
                case 158:
                    message.nodeTransactionPrecheckCode = 158;
                    break;
                case "INVALID_AUTORENEW_ACCOUNT":
                case 159:
                    message.nodeTransactionPrecheckCode = 159;
                    break;
                case "AUTORENEW_ACCOUNT_NOT_ALLOWED":
                case 160:
                    message.nodeTransactionPrecheckCode = 160;
                    break;
                case "TOPIC_EXPIRED":
                case 162:
                    message.nodeTransactionPrecheckCode = 162;
                    break;
                case "INVALID_CHUNK_NUMBER":
                case 163:
                    message.nodeTransactionPrecheckCode = 163;
                    break;
                case "INVALID_CHUNK_TRANSACTION_ID":
                case 164:
                    message.nodeTransactionPrecheckCode = 164;
                    break;
                case "ACCOUNT_FROZEN_FOR_TOKEN":
                case 165:
                    message.nodeTransactionPrecheckCode = 165;
                    break;
                case "TOKENS_PER_ACCOUNT_LIMIT_EXCEEDED":
                case 166:
                    message.nodeTransactionPrecheckCode = 166;
                    break;
                case "INVALID_TOKEN_ID":
                case 167:
                    message.nodeTransactionPrecheckCode = 167;
                    break;
                case "INVALID_TOKEN_DECIMALS":
                case 168:
                    message.nodeTransactionPrecheckCode = 168;
                    break;
                case "INVALID_TOKEN_INITIAL_SUPPLY":
                case 169:
                    message.nodeTransactionPrecheckCode = 169;
                    break;
                case "INVALID_TREASURY_ACCOUNT_FOR_TOKEN":
                case 170:
                    message.nodeTransactionPrecheckCode = 170;
                    break;
                case "INVALID_TOKEN_SYMBOL":
                case 171:
                    message.nodeTransactionPrecheckCode = 171;
                    break;
                case "TOKEN_HAS_NO_FREEZE_KEY":
                case 172:
                    message.nodeTransactionPrecheckCode = 172;
                    break;
                case "TRANSFERS_NOT_ZERO_SUM_FOR_TOKEN":
                case 173:
                    message.nodeTransactionPrecheckCode = 173;
                    break;
                case "MISSING_TOKEN_SYMBOL":
                case 174:
                    message.nodeTransactionPrecheckCode = 174;
                    break;
                case "TOKEN_SYMBOL_TOO_LONG":
                case 175:
                    message.nodeTransactionPrecheckCode = 175;
                    break;
                case "ACCOUNT_KYC_NOT_GRANTED_FOR_TOKEN":
                case 176:
                    message.nodeTransactionPrecheckCode = 176;
                    break;
                case "TOKEN_HAS_NO_KYC_KEY":
                case 177:
                    message.nodeTransactionPrecheckCode = 177;
                    break;
                case "INSUFFICIENT_TOKEN_BALANCE":
                case 178:
                    message.nodeTransactionPrecheckCode = 178;
                    break;
                case "TOKEN_WAS_DELETED":
                case 179:
                    message.nodeTransactionPrecheckCode = 179;
                    break;
                case "TOKEN_HAS_NO_SUPPLY_KEY":
                case 180:
                    message.nodeTransactionPrecheckCode = 180;
                    break;
                case "TOKEN_HAS_NO_WIPE_KEY":
                case 181:
                    message.nodeTransactionPrecheckCode = 181;
                    break;
                case "INVALID_TOKEN_MINT_AMOUNT":
                case 182:
                    message.nodeTransactionPrecheckCode = 182;
                    break;
                case "INVALID_TOKEN_BURN_AMOUNT":
                case 183:
                    message.nodeTransactionPrecheckCode = 183;
                    break;
                case "TOKEN_NOT_ASSOCIATED_TO_ACCOUNT":
                case 184:
                    message.nodeTransactionPrecheckCode = 184;
                    break;
                case "CANNOT_WIPE_TOKEN_TREASURY_ACCOUNT":
                case 185:
                    message.nodeTransactionPrecheckCode = 185;
                    break;
                case "INVALID_KYC_KEY":
                case 186:
                    message.nodeTransactionPrecheckCode = 186;
                    break;
                case "INVALID_WIPE_KEY":
                case 187:
                    message.nodeTransactionPrecheckCode = 187;
                    break;
                case "INVALID_FREEZE_KEY":
                case 188:
                    message.nodeTransactionPrecheckCode = 188;
                    break;
                case "INVALID_SUPPLY_KEY":
                case 189:
                    message.nodeTransactionPrecheckCode = 189;
                    break;
                case "MISSING_TOKEN_NAME":
                case 190:
                    message.nodeTransactionPrecheckCode = 190;
                    break;
                case "TOKEN_NAME_TOO_LONG":
                case 191:
                    message.nodeTransactionPrecheckCode = 191;
                    break;
                case "INVALID_WIPING_AMOUNT":
                case 192:
                    message.nodeTransactionPrecheckCode = 192;
                    break;
                case "TOKEN_IS_IMMUTABLE":
                case 193:
                    message.nodeTransactionPrecheckCode = 193;
                    break;
                case "TOKEN_ALREADY_ASSOCIATED_TO_ACCOUNT":
                case 194:
                    message.nodeTransactionPrecheckCode = 194;
                    break;
                case "TRANSACTION_REQUIRES_ZERO_TOKEN_BALANCES":
                case 195:
                    message.nodeTransactionPrecheckCode = 195;
                    break;
                case "ACCOUNT_IS_TREASURY":
                case 196:
                    message.nodeTransactionPrecheckCode = 196;
                    break;
                case "TOKEN_ID_REPEATED_IN_TOKEN_LIST":
                case 197:
                    message.nodeTransactionPrecheckCode = 197;
                    break;
                case "TOKEN_TRANSFER_LIST_SIZE_LIMIT_EXCEEDED":
                case 198:
                    message.nodeTransactionPrecheckCode = 198;
                    break;
                case "EMPTY_TOKEN_TRANSFER_BODY":
                case 199:
                    message.nodeTransactionPrecheckCode = 199;
                    break;
                case "EMPTY_TOKEN_TRANSFER_ACCOUNT_AMOUNTS":
                case 200:
                    message.nodeTransactionPrecheckCode = 200;
                    break;
                case "INVALID_SCHEDULE_ID":
                case 201:
                    message.nodeTransactionPrecheckCode = 201;
                    break;
                case "SCHEDULE_IS_IMMUTABLE":
                case 202:
                    message.nodeTransactionPrecheckCode = 202;
                    break;
                case "INVALID_SCHEDULE_PAYER_ID":
                case 203:
                    message.nodeTransactionPrecheckCode = 203;
                    break;
                case "INVALID_SCHEDULE_ACCOUNT_ID":
                case 204:
                    message.nodeTransactionPrecheckCode = 204;
                    break;
                case "NO_NEW_VALID_SIGNATURES":
                case 205:
                    message.nodeTransactionPrecheckCode = 205;
                    break;
                case "UNRESOLVABLE_REQUIRED_SIGNERS":
                case 206:
                    message.nodeTransactionPrecheckCode = 206;
                    break;
                case "SCHEDULED_TRANSACTION_NOT_IN_WHITELIST":
                case 207:
                    message.nodeTransactionPrecheckCode = 207;
                    break;
                case "SOME_SIGNATURES_WERE_INVALID":
                case 208:
                    message.nodeTransactionPrecheckCode = 208;
                    break;
                case "TRANSACTION_ID_FIELD_NOT_ALLOWED":
                case 209:
                    message.nodeTransactionPrecheckCode = 209;
                    break;
                case "IDENTICAL_SCHEDULE_ALREADY_CREATED":
                case 210:
                    message.nodeTransactionPrecheckCode = 210;
                    break;
                case "INVALID_ZERO_BYTE_IN_STRING":
                case 211:
                    message.nodeTransactionPrecheckCode = 211;
                    break;
                case "SCHEDULE_ALREADY_DELETED":
                case 212:
                    message.nodeTransactionPrecheckCode = 212;
                    break;
                case "SCHEDULE_ALREADY_EXECUTED":
                case 213:
                    message.nodeTransactionPrecheckCode = 213;
                    break;
                case "MESSAGE_SIZE_TOO_LARGE":
                case 214:
                    message.nodeTransactionPrecheckCode = 214;
                    break;
                case "OPERATION_REPEATED_IN_BUCKET_GROUPS":
                case 215:
                    message.nodeTransactionPrecheckCode = 215;
                    break;
                case "BUCKET_CAPACITY_OVERFLOW":
                case 216:
                    message.nodeTransactionPrecheckCode = 216;
                    break;
                case "NODE_CAPACITY_NOT_SUFFICIENT_FOR_OPERATION":
                case 217:
                    message.nodeTransactionPrecheckCode = 217;
                    break;
                case "BUCKET_HAS_NO_THROTTLE_GROUPS":
                case 218:
                    message.nodeTransactionPrecheckCode = 218;
                    break;
                case "THROTTLE_GROUP_HAS_ZERO_OPS_PER_SEC":
                case 219:
                    message.nodeTransactionPrecheckCode = 219;
                    break;
                case "SUCCESS_BUT_MISSING_EXPECTED_OPERATION":
                case 220:
                    message.nodeTransactionPrecheckCode = 220;
                    break;
                case "UNPARSEABLE_THROTTLE_DEFINITIONS":
                case 221:
                    message.nodeTransactionPrecheckCode = 221;
                    break;
                case "INVALID_THROTTLE_DEFINITIONS":
                case 222:
                    message.nodeTransactionPrecheckCode = 222;
                    break;
                case "ACCOUNT_EXPIRED_AND_PENDING_REMOVAL":
                case 223:
                    message.nodeTransactionPrecheckCode = 223;
                    break;
                case "INVALID_TOKEN_MAX_SUPPLY":
                case 224:
                    message.nodeTransactionPrecheckCode = 224;
                    break;
                case "INVALID_TOKEN_NFT_SERIAL_NUMBER":
                case 225:
                    message.nodeTransactionPrecheckCode = 225;
                    break;
                case "INVALID_NFT_ID":
                case 226:
                    message.nodeTransactionPrecheckCode = 226;
                    break;
                case "METADATA_TOO_LONG":
                case 227:
                    message.nodeTransactionPrecheckCode = 227;
                    break;
                case "BATCH_SIZE_LIMIT_EXCEEDED":
                case 228:
                    message.nodeTransactionPrecheckCode = 228;
                    break;
                case "INVALID_QUERY_RANGE":
                case 229:
                    message.nodeTransactionPrecheckCode = 229;
                    break;
                case "FRACTION_DIVIDES_BY_ZERO":
                case 230:
                    message.nodeTransactionPrecheckCode = 230;
                    break;
                case "INSUFFICIENT_PAYER_BALANCE_FOR_CUSTOM_FEE":
                case 231:
                    message.nodeTransactionPrecheckCode = 231;
                    break;
                case "CUSTOM_FEES_LIST_TOO_LONG":
                case 232:
                    message.nodeTransactionPrecheckCode = 232;
                    break;
                case "INVALID_CUSTOM_FEE_COLLECTOR":
                case 233:
                    message.nodeTransactionPrecheckCode = 233;
                    break;
                case "INVALID_TOKEN_ID_IN_CUSTOM_FEES":
                case 234:
                    message.nodeTransactionPrecheckCode = 234;
                    break;
                case "TOKEN_NOT_ASSOCIATED_TO_FEE_COLLECTOR":
                case 235:
                    message.nodeTransactionPrecheckCode = 235;
                    break;
                case "TOKEN_MAX_SUPPLY_REACHED":
                case 236:
                    message.nodeTransactionPrecheckCode = 236;
                    break;
                case "SENDER_DOES_NOT_OWN_NFT_SERIAL_NO":
                case 237:
                    message.nodeTransactionPrecheckCode = 237;
                    break;
                case "CUSTOM_FEE_NOT_FULLY_SPECIFIED":
                case 238:
                    message.nodeTransactionPrecheckCode = 238;
                    break;
                case "CUSTOM_FEE_MUST_BE_POSITIVE":
                case 239:
                    message.nodeTransactionPrecheckCode = 239;
                    break;
                case "TOKEN_HAS_NO_FEE_SCHEDULE_KEY":
                case 240:
                    message.nodeTransactionPrecheckCode = 240;
                    break;
                case "CUSTOM_FEE_OUTSIDE_NUMERIC_RANGE":
                case 241:
                    message.nodeTransactionPrecheckCode = 241;
                    break;
                case "ROYALTY_FRACTION_CANNOT_EXCEED_ONE":
                case 242:
                    message.nodeTransactionPrecheckCode = 242;
                    break;
                case "FRACTIONAL_FEE_MAX_AMOUNT_LESS_THAN_MIN_AMOUNT":
                case 243:
                    message.nodeTransactionPrecheckCode = 243;
                    break;
                case "CUSTOM_SCHEDULE_ALREADY_HAS_NO_FEES":
                case 244:
                    message.nodeTransactionPrecheckCode = 244;
                    break;
                case "CUSTOM_FEE_DENOMINATION_MUST_BE_FUNGIBLE_COMMON":
                case 245:
                    message.nodeTransactionPrecheckCode = 245;
                    break;
                case "CUSTOM_FRACTIONAL_FEE_ONLY_ALLOWED_FOR_FUNGIBLE_COMMON":
                case 246:
                    message.nodeTransactionPrecheckCode = 246;
                    break;
                case "INVALID_CUSTOM_FEE_SCHEDULE_KEY":
                case 247:
                    message.nodeTransactionPrecheckCode = 247;
                    break;
                case "INVALID_TOKEN_MINT_METADATA":
                case 248:
                    message.nodeTransactionPrecheckCode = 248;
                    break;
                case "INVALID_TOKEN_BURN_METADATA":
                case 249:
                    message.nodeTransactionPrecheckCode = 249;
                    break;
                case "CURRENT_TREASURY_STILL_OWNS_NFTS":
                case 250:
                    message.nodeTransactionPrecheckCode = 250;
                    break;
                case "ACCOUNT_STILL_OWNS_NFTS":
                case 251:
                    message.nodeTransactionPrecheckCode = 251;
                    break;
                case "TREASURY_MUST_OWN_BURNED_NFT":
                case 252:
                    message.nodeTransactionPrecheckCode = 252;
                    break;
                case "ACCOUNT_DOES_NOT_OWN_WIPED_NFT":
                case 253:
                    message.nodeTransactionPrecheckCode = 253;
                    break;
                case "ACCOUNT_AMOUNT_TRANSFERS_ONLY_ALLOWED_FOR_FUNGIBLE_COMMON":
                case 254:
                    message.nodeTransactionPrecheckCode = 254;
                    break;
                case "MAX_NFTS_IN_PRICE_REGIME_HAVE_BEEN_MINTED":
                case 255:
                    message.nodeTransactionPrecheckCode = 255;
                    break;
                case "PAYER_ACCOUNT_DELETED":
                case 256:
                    message.nodeTransactionPrecheckCode = 256;
                    break;
                case "CUSTOM_FEE_CHARGING_EXCEEDED_MAX_RECURSION_DEPTH":
                case 257:
                    message.nodeTransactionPrecheckCode = 257;
                    break;
                case "CUSTOM_FEE_CHARGING_EXCEEDED_MAX_ACCOUNT_AMOUNTS":
                case 258:
                    message.nodeTransactionPrecheckCode = 258;
                    break;
                case "INSUFFICIENT_SENDER_ACCOUNT_BALANCE_FOR_CUSTOM_FEE":
                case 259:
                    message.nodeTransactionPrecheckCode = 259;
                    break;
                case "SERIAL_NUMBER_LIMIT_REACHED":
                case 260:
                    message.nodeTransactionPrecheckCode = 260;
                    break;
                case "CUSTOM_ROYALTY_FEE_ONLY_ALLOWED_FOR_NON_FUNGIBLE_UNIQUE":
                case 261:
                    message.nodeTransactionPrecheckCode = 261;
                    break;
                case "NO_REMAINING_AUTOMATIC_ASSOCIATIONS":
                case 262:
                    message.nodeTransactionPrecheckCode = 262;
                    break;
                case "EXISTING_AUTOMATIC_ASSOCIATIONS_EXCEED_GIVEN_LIMIT":
                case 263:
                    message.nodeTransactionPrecheckCode = 263;
                    break;
                case "REQUESTED_NUM_AUTOMATIC_ASSOCIATIONS_EXCEEDS_ASSOCIATION_LIMIT":
                case 264:
                    message.nodeTransactionPrecheckCode = 264;
                    break;
                case "TOKEN_IS_PAUSED":
                case 265:
                    message.nodeTransactionPrecheckCode = 265;
                    break;
                case "TOKEN_HAS_NO_PAUSE_KEY":
                case 266:
                    message.nodeTransactionPrecheckCode = 266;
                    break;
                case "INVALID_PAUSE_KEY":
                case 267:
                    message.nodeTransactionPrecheckCode = 267;
                    break;
                case "FREEZE_UPDATE_FILE_DOES_NOT_EXIST":
                case 268:
                    message.nodeTransactionPrecheckCode = 268;
                    break;
                case "FREEZE_UPDATE_FILE_HASH_DOES_NOT_MATCH":
                case 269:
                    message.nodeTransactionPrecheckCode = 269;
                    break;
                case "NO_UPGRADE_HAS_BEEN_PREPARED":
                case 270:
                    message.nodeTransactionPrecheckCode = 270;
                    break;
                case "NO_FREEZE_IS_SCHEDULED":
                case 271:
                    message.nodeTransactionPrecheckCode = 271;
                    break;
                case "UPDATE_FILE_HASH_CHANGED_SINCE_PREPARE_UPGRADE":
                case 272:
                    message.nodeTransactionPrecheckCode = 272;
                    break;
                case "FREEZE_START_TIME_MUST_BE_FUTURE":
                case 273:
                    message.nodeTransactionPrecheckCode = 273;
                    break;
                case "PREPARED_UPDATE_FILE_IS_IMMUTABLE":
                case 274:
                    message.nodeTransactionPrecheckCode = 274;
                    break;
                case "FREEZE_ALREADY_SCHEDULED":
                case 275:
                    message.nodeTransactionPrecheckCode = 275;
                    break;
                case "FREEZE_UPGRADE_IN_PROGRESS":
                case 276:
                    message.nodeTransactionPrecheckCode = 276;
                    break;
                case "UPDATE_FILE_ID_DOES_NOT_MATCH_PREPARED":
                case 277:
                    message.nodeTransactionPrecheckCode = 277;
                    break;
                case "UPDATE_FILE_HASH_DOES_NOT_MATCH_PREPARED":
                case 278:
                    message.nodeTransactionPrecheckCode = 278;
                    break;
                case "CONSENSUS_GAS_EXHAUSTED":
                case 279:
                    message.nodeTransactionPrecheckCode = 279;
                    break;
                case "REVERTED_SUCCESS":
                case 280:
                    message.nodeTransactionPrecheckCode = 280;
                    break;
                case "MAX_STORAGE_IN_PRICE_REGIME_HAS_BEEN_USED":
                case 281:
                    message.nodeTransactionPrecheckCode = 281;
                    break;
                case "INVALID_ALIAS_KEY":
                case 282:
                    message.nodeTransactionPrecheckCode = 282;
                    break;
                case "UNEXPECTED_TOKEN_DECIMALS":
                case 283:
                    message.nodeTransactionPrecheckCode = 283;
                    break;
                case "INVALID_PROXY_ACCOUNT_ID":
                case 284:
                    message.nodeTransactionPrecheckCode = 284;
                    break;
                case "INVALID_TRANSFER_ACCOUNT_ID":
                case 285:
                    message.nodeTransactionPrecheckCode = 285;
                    break;
                case "INVALID_FEE_COLLECTOR_ACCOUNT_ID":
                case 286:
                    message.nodeTransactionPrecheckCode = 286;
                    break;
                case "ALIAS_IS_IMMUTABLE":
                case 287:
                    message.nodeTransactionPrecheckCode = 287;
                    break;
                case "SPENDER_ACCOUNT_SAME_AS_OWNER":
                case 288:
                    message.nodeTransactionPrecheckCode = 288;
                    break;
                case "AMOUNT_EXCEEDS_TOKEN_MAX_SUPPLY":
                case 289:
                    message.nodeTransactionPrecheckCode = 289;
                    break;
                case "NEGATIVE_ALLOWANCE_AMOUNT":
                case 290:
                    message.nodeTransactionPrecheckCode = 290;
                    break;
                case "CANNOT_APPROVE_FOR_ALL_FUNGIBLE_COMMON":
                case 291:
                    message.nodeTransactionPrecheckCode = 291;
                    break;
                case "SPENDER_DOES_NOT_HAVE_ALLOWANCE":
                case 292:
                    message.nodeTransactionPrecheckCode = 292;
                    break;
                case "AMOUNT_EXCEEDS_ALLOWANCE":
                case 293:
                    message.nodeTransactionPrecheckCode = 293;
                    break;
                case "MAX_ALLOWANCES_EXCEEDED":
                case 294:
                    message.nodeTransactionPrecheckCode = 294;
                    break;
                case "EMPTY_ALLOWANCES":
                case 295:
                    message.nodeTransactionPrecheckCode = 295;
                    break;
                case "SPENDER_ACCOUNT_REPEATED_IN_ALLOWANCES":
                case 296:
                    message.nodeTransactionPrecheckCode = 296;
                    break;
                case "REPEATED_SERIAL_NUMS_IN_NFT_ALLOWANCES":
                case 297:
                    message.nodeTransactionPrecheckCode = 297;
                    break;
                case "FUNGIBLE_TOKEN_IN_NFT_ALLOWANCES":
                case 298:
                    message.nodeTransactionPrecheckCode = 298;
                    break;
                case "NFT_IN_FUNGIBLE_TOKEN_ALLOWANCES":
                case 299:
                    message.nodeTransactionPrecheckCode = 299;
                    break;
                case "INVALID_ALLOWANCE_OWNER_ID":
                case 300:
                    message.nodeTransactionPrecheckCode = 300;
                    break;
                case "INVALID_ALLOWANCE_SPENDER_ID":
                case 301:
                    message.nodeTransactionPrecheckCode = 301;
                    break;
                case "REPEATED_ALLOWANCES_TO_DELETE":
                case 302:
                    message.nodeTransactionPrecheckCode = 302;
                    break;
                case "INVALID_DELEGATING_SPENDER":
                case 303:
                    message.nodeTransactionPrecheckCode = 303;
                    break;
                case "DELEGATING_SPENDER_CANNOT_GRANT_APPROVE_FOR_ALL":
                case 304:
                    message.nodeTransactionPrecheckCode = 304;
                    break;
                case "DELEGATING_SPENDER_DOES_NOT_HAVE_APPROVE_FOR_ALL":
                case 305:
                    message.nodeTransactionPrecheckCode = 305;
                    break;
                case "SCHEDULE_EXPIRATION_TIME_TOO_FAR_IN_FUTURE":
                case 306:
                    message.nodeTransactionPrecheckCode = 306;
                    break;
                case "SCHEDULE_EXPIRATION_TIME_MUST_BE_HIGHER_THAN_CONSENSUS_TIME":
                case 307:
                    message.nodeTransactionPrecheckCode = 307;
                    break;
                case "SCHEDULE_FUTURE_THROTTLE_EXCEEDED":
                case 308:
                    message.nodeTransactionPrecheckCode = 308;
                    break;
                case "SCHEDULE_FUTURE_GAS_LIMIT_EXCEEDED":
                case 309:
                    message.nodeTransactionPrecheckCode = 309;
                    break;
                case "INVALID_ETHEREUM_TRANSACTION":
                case 310:
                    message.nodeTransactionPrecheckCode = 310;
                    break;
                case "WRONG_CHAIN_ID":
                case 311:
                    message.nodeTransactionPrecheckCode = 311;
                    break;
                case "WRONG_NONCE":
                case 312:
                    message.nodeTransactionPrecheckCode = 312;
                    break;
                case "ACCESS_LIST_UNSUPPORTED":
                case 313:
                    message.nodeTransactionPrecheckCode = 313;
                    break;
                case "SCHEDULE_PENDING_EXPIRATION":
                case 314:
                    message.nodeTransactionPrecheckCode = 314;
                    break;
                case "CONTRACT_IS_TOKEN_TREASURY":
                case 315:
                    message.nodeTransactionPrecheckCode = 315;
                    break;
                case "CONTRACT_HAS_NON_ZERO_TOKEN_BALANCES":
                case 316:
                    message.nodeTransactionPrecheckCode = 316;
                    break;
                case "CONTRACT_EXPIRED_AND_PENDING_REMOVAL":
                case 317:
                    message.nodeTransactionPrecheckCode = 317;
                    break;
                case "CONTRACT_HAS_NO_AUTO_RENEW_ACCOUNT":
                case 318:
                    message.nodeTransactionPrecheckCode = 318;
                    break;
                case "PERMANENT_REMOVAL_REQUIRES_SYSTEM_INITIATION":
                case 319:
                    message.nodeTransactionPrecheckCode = 319;
                    break;
                case "PROXY_ACCOUNT_ID_FIELD_IS_DEPRECATED":
                case 320:
                    message.nodeTransactionPrecheckCode = 320;
                    break;
                case "SELF_STAKING_IS_NOT_ALLOWED":
                case 321:
                    message.nodeTransactionPrecheckCode = 321;
                    break;
                case "INVALID_STAKING_ID":
                case 322:
                    message.nodeTransactionPrecheckCode = 322;
                    break;
                case "STAKING_NOT_ENABLED":
                case 323:
                    message.nodeTransactionPrecheckCode = 323;
                    break;
                case "INVALID_PRNG_RANGE":
                case 324:
                    message.nodeTransactionPrecheckCode = 324;
                    break;
                case "MAX_ENTITIES_IN_PRICE_REGIME_HAVE_BEEN_CREATED":
                case 325:
                    message.nodeTransactionPrecheckCode = 325;
                    break;
                case "INVALID_FULL_PREFIX_SIGNATURE_FOR_PRECOMPILE":
                case 326:
                    message.nodeTransactionPrecheckCode = 326;
                    break;
                case "INSUFFICIENT_BALANCES_FOR_STORAGE_RENT":
                case 327:
                    message.nodeTransactionPrecheckCode = 327;
                    break;
                case "MAX_CHILD_RECORDS_EXCEEDED":
                case 328:
                    message.nodeTransactionPrecheckCode = 328;
                    break;
                case "INSUFFICIENT_BALANCES_FOR_RENEWAL_FEES":
                case 329:
                    message.nodeTransactionPrecheckCode = 329;
                    break;
                case "TRANSACTION_HAS_UNKNOWN_FIELDS":
                case 330:
                    message.nodeTransactionPrecheckCode = 330;
                    break;
                case "ACCOUNT_IS_IMMUTABLE":
                case 331:
                    message.nodeTransactionPrecheckCode = 331;
                    break;
                case "ALIAS_ALREADY_ASSIGNED":
                case 332:
                    message.nodeTransactionPrecheckCode = 332;
                    break;
                case "INVALID_METADATA_KEY":
                case 333:
                    message.nodeTransactionPrecheckCode = 333;
                    break;
                case "TOKEN_HAS_NO_METADATA_KEY":
                case 334:
                    message.nodeTransactionPrecheckCode = 334;
                    break;
                case "MISSING_TOKEN_METADATA":
                case 335:
                    message.nodeTransactionPrecheckCode = 335;
                    break;
                case "MISSING_SERIAL_NUMBERS":
                case 336:
                    message.nodeTransactionPrecheckCode = 336;
                    break;
                case "TOKEN_HAS_NO_ADMIN_KEY":
                case 337:
                    message.nodeTransactionPrecheckCode = 337;
                    break;
                case "NODE_DELETED":
                case 338:
                    message.nodeTransactionPrecheckCode = 338;
                    break;
                case "INVALID_NODE_ID":
                case 339:
                    message.nodeTransactionPrecheckCode = 339;
                    break;
                case "INVALID_GOSSIP_ENDPOINT":
                case 340:
                    message.nodeTransactionPrecheckCode = 340;
                    break;
                case "INVALID_NODE_ACCOUNT_ID":
                case 341:
                    message.nodeTransactionPrecheckCode = 341;
                    break;
                case "INVALID_NODE_DESCRIPTION":
                case 342:
                    message.nodeTransactionPrecheckCode = 342;
                    break;
                case "INVALID_SERVICE_ENDPOINT":
                case 343:
                    message.nodeTransactionPrecheckCode = 343;
                    break;
                case "INVALID_GOSSIP_CA_CERTIFICATE":
                case 344:
                    message.nodeTransactionPrecheckCode = 344;
                    break;
                case "INVALID_GRPC_CERTIFICATE":
                case 345:
                    message.nodeTransactionPrecheckCode = 345;
                    break;
                case "INVALID_MAX_AUTO_ASSOCIATIONS":
                case 346:
                    message.nodeTransactionPrecheckCode = 346;
                    break;
                case "MAX_NODES_CREATED":
                case 347:
                    message.nodeTransactionPrecheckCode = 347;
                    break;
                case "IP_FQDN_CANNOT_BE_SET_FOR_SAME_ENDPOINT":
                case 348:
                    message.nodeTransactionPrecheckCode = 348;
                    break;
                case "GOSSIP_ENDPOINT_CANNOT_HAVE_FQDN":
                case 349:
                    message.nodeTransactionPrecheckCode = 349;
                    break;
                case "FQDN_SIZE_TOO_LARGE":
                case 350:
                    message.nodeTransactionPrecheckCode = 350;
                    break;
                case "INVALID_ENDPOINT":
                case 351:
                    message.nodeTransactionPrecheckCode = 351;
                    break;
                case "GOSSIP_ENDPOINTS_EXCEEDED_LIMIT":
                case 352:
                    message.nodeTransactionPrecheckCode = 352;
                    break;
                case "TOKEN_REFERENCE_REPEATED":
                case 353:
                    message.nodeTransactionPrecheckCode = 353;
                    break;
                case "INVALID_OWNER_ID":
                case 354:
                    message.nodeTransactionPrecheckCode = 354;
                    break;
                case "TOKEN_REFERENCE_LIST_SIZE_LIMIT_EXCEEDED":
                case 355:
                    message.nodeTransactionPrecheckCode = 355;
                    break;
                case "SERVICE_ENDPOINTS_EXCEEDED_LIMIT":
                case 356:
                    message.nodeTransactionPrecheckCode = 356;
                    break;
                case "INVALID_IPV4_ADDRESS":
                case 357:
                    message.nodeTransactionPrecheckCode = 357;
                    break;
                case "EMPTY_TOKEN_REFERENCE_LIST":
                case 358:
                    message.nodeTransactionPrecheckCode = 358;
                    break;
                case "UPDATE_NODE_ACCOUNT_NOT_ALLOWED":
                case 359:
                    message.nodeTransactionPrecheckCode = 359;
                    break;
                case "TOKEN_HAS_NO_METADATA_OR_SUPPLY_KEY":
                case 360:
                    message.nodeTransactionPrecheckCode = 360;
                    break;
                case "EMPTY_PENDING_AIRDROP_ID_LIST":
                case 361:
                    message.nodeTransactionPrecheckCode = 361;
                    break;
                case "PENDING_AIRDROP_ID_REPEATED":
                case 362:
                    message.nodeTransactionPrecheckCode = 362;
                    break;
                case "PENDING_AIRDROP_ID_LIST_TOO_LONG":
                case 363:
                    message.nodeTransactionPrecheckCode = 363;
                    break;
                case "PENDING_NFT_AIRDROP_ALREADY_EXISTS":
                case 364:
                    message.nodeTransactionPrecheckCode = 364;
                    break;
                case "ACCOUNT_HAS_PENDING_AIRDROPS":
                case 365:
                    message.nodeTransactionPrecheckCode = 365;
                    break;
                case "THROTTLED_AT_CONSENSUS":
                case 366:
                    message.nodeTransactionPrecheckCode = 366;
                    break;
                case "INVALID_PENDING_AIRDROP_ID":
                case 367:
                    message.nodeTransactionPrecheckCode = 367;
                    break;
                case "TOKEN_AIRDROP_WITH_FALLBACK_ROYALTY":
                case 368:
                    message.nodeTransactionPrecheckCode = 368;
                    break;
                case "INVALID_TOKEN_IN_PENDING_AIRDROP":
                case 369:
                    message.nodeTransactionPrecheckCode = 369;
                    break;
                case "SCHEDULE_EXPIRY_IS_BUSY":
                case 370:
                    message.nodeTransactionPrecheckCode = 370;
                    break;
                case "INVALID_GRPC_CERTIFICATE_HASH":
                case 371:
                    message.nodeTransactionPrecheckCode = 371;
                    break;
                case "MISSING_EXPIRY_TIME":
                case 372:
                    message.nodeTransactionPrecheckCode = 372;
                    break;
                case "NO_SCHEDULING_ALLOWED_AFTER_SCHEDULED_RECURSION":
                case 373:
                    message.nodeTransactionPrecheckCode = 373;
                    break;
                case "RECURSIVE_SCHEDULING_LIMIT_REACHED":
                case 374:
                    message.nodeTransactionPrecheckCode = 374;
                    break;
                case "WAITING_FOR_LEDGER_ID":
                case 375:
                    message.nodeTransactionPrecheckCode = 375;
                    break;
                case "MAX_ENTRIES_FOR_FEE_EXEMPT_KEY_LIST_EXCEEDED":
                case 376:
                    message.nodeTransactionPrecheckCode = 376;
                    break;
                case "FEE_EXEMPT_KEY_LIST_CONTAINS_DUPLICATED_KEYS":
                case 377:
                    message.nodeTransactionPrecheckCode = 377;
                    break;
                case "INVALID_KEY_IN_FEE_EXEMPT_KEY_LIST":
                case 378:
                    message.nodeTransactionPrecheckCode = 378;
                    break;
                case "INVALID_FEE_SCHEDULE_KEY":
                case 379:
                    message.nodeTransactionPrecheckCode = 379;
                    break;
                case "FEE_SCHEDULE_KEY_CANNOT_BE_UPDATED":
                case 380:
                    message.nodeTransactionPrecheckCode = 380;
                    break;
                case "FEE_SCHEDULE_KEY_NOT_SET":
                case 381:
                    message.nodeTransactionPrecheckCode = 381;
                    break;
                case "MAX_CUSTOM_FEE_LIMIT_EXCEEDED":
                case 382:
                    message.nodeTransactionPrecheckCode = 382;
                    break;
                case "NO_VALID_MAX_CUSTOM_FEE":
                case 383:
                    message.nodeTransactionPrecheckCode = 383;
                    break;
                case "INVALID_MAX_CUSTOM_FEES":
                case 384:
                    message.nodeTransactionPrecheckCode = 384;
                    break;
                case "DUPLICATE_DENOMINATION_IN_MAX_CUSTOM_FEE_LIST":
                case 385:
                    message.nodeTransactionPrecheckCode = 385;
                    break;
                case "DUPLICATE_ACCOUNT_ID_IN_MAX_CUSTOM_FEE_LIST":
                case 386:
                    message.nodeTransactionPrecheckCode = 386;
                    break;
                case "MAX_CUSTOM_FEES_IS_NOT_SUPPORTED":
                case 387:
                    message.nodeTransactionPrecheckCode = 387;
                    break;
                case "BATCH_LIST_EMPTY":
                case 388:
                    message.nodeTransactionPrecheckCode = 388;
                    break;
                case "BATCH_LIST_CONTAINS_DUPLICATES":
                case 389:
                    message.nodeTransactionPrecheckCode = 389;
                    break;
                case "BATCH_TRANSACTION_IN_BLACKLIST":
                case 390:
                    message.nodeTransactionPrecheckCode = 390;
                    break;
                case "INNER_TRANSACTION_FAILED":
                case 391:
                    message.nodeTransactionPrecheckCode = 391;
                    break;
                case "MISSING_BATCH_KEY":
                case 392:
                    message.nodeTransactionPrecheckCode = 392;
                    break;
                case "BATCH_KEY_SET_ON_NON_INNER_TRANSACTION":
                case 393:
                    message.nodeTransactionPrecheckCode = 393;
                    break;
                case "INVALID_BATCH_KEY":
                case 394:
                    message.nodeTransactionPrecheckCode = 394;
                    break;
                case "SCHEDULE_EXPIRY_NOT_CONFIGURABLE":
                case 395:
                    message.nodeTransactionPrecheckCode = 395;
                    break;
                case "CREATING_SYSTEM_ENTITIES":
                case 396:
                    message.nodeTransactionPrecheckCode = 396;
                    break;
                case "THROTTLE_GROUP_LCM_OVERFLOW":
                case 397:
                    message.nodeTransactionPrecheckCode = 397;
                    break;
                case "AIRDROP_CONTAINS_MULTIPLE_SENDERS_FOR_A_TOKEN":
                case 398:
                    message.nodeTransactionPrecheckCode = 398;
                    break;
                case "GRPC_WEB_PROXY_NOT_SUPPORTED":
                case 399:
                    message.nodeTransactionPrecheckCode = 399;
                    break;
                case "NFT_TRANSFERS_ONLY_ALLOWED_FOR_NON_FUNGIBLE_UNIQUE":
                case 400:
                    message.nodeTransactionPrecheckCode = 400;
                    break;
                case "INVALID_SERIALIZED_TX_MESSAGE_HASH_ALGORITHM":
                case 401:
                    message.nodeTransactionPrecheckCode = 401;
                    break;
                case "WRONG_HOOK_ENTITY_TYPE":
                case 499:
                    message.nodeTransactionPrecheckCode = 499;
                    break;
                case "EVM_HOOK_GAS_THROTTLED":
                case 500:
                    message.nodeTransactionPrecheckCode = 500;
                    break;
                case "HOOK_ID_IN_USE":
                case 501:
                    message.nodeTransactionPrecheckCode = 501;
                    break;
                case "BAD_HOOK_REQUEST":
                case 502:
                    message.nodeTransactionPrecheckCode = 502;
                    break;
                case "REJECTED_BY_ACCOUNT_ALLOWANCE_HOOK":
                case 503:
                    message.nodeTransactionPrecheckCode = 503;
                    break;
                case "HOOK_NOT_FOUND":
                case 504:
                    message.nodeTransactionPrecheckCode = 504;
                    break;
                case "EVM_HOOK_STORAGE_UPDATE_BYTES_TOO_LONG":
                case 505:
                    message.nodeTransactionPrecheckCode = 505;
                    break;
                case "EVM_HOOK_STORAGE_UPDATE_BYTES_MUST_USE_MINIMAL_REPRESENTATION":
                case 506:
                    message.nodeTransactionPrecheckCode = 506;
                    break;
                case "INVALID_HOOK_ID":
                case 507:
                    message.nodeTransactionPrecheckCode = 507;
                    break;
                case "EMPTY_EVM_HOOK_STORAGE_UPDATE":
                case 508:
                    message.nodeTransactionPrecheckCode = 508;
                    break;
                case "HOOK_ID_REPEATED_IN_CREATION_DETAILS":
                case 509:
                    message.nodeTransactionPrecheckCode = 509;
                    break;
                case "HOOKS_NOT_ENABLED":
                case 510:
                    message.nodeTransactionPrecheckCode = 510;
                    break;
                case "HOOK_IS_NOT_AN_EVM_HOOK":
                case 511:
                    message.nodeTransactionPrecheckCode = 511;
                    break;
                case "HOOK_DELETED":
                case 512:
                    message.nodeTransactionPrecheckCode = 512;
                    break;
                case "TOO_MANY_EVM_HOOK_STORAGE_UPDATES":
                case 513:
                    message.nodeTransactionPrecheckCode = 513;
                    break;
                case "HOOK_CREATION_BYTES_MUST_USE_MINIMAL_REPRESENTATION":
                case 514:
                    message.nodeTransactionPrecheckCode = 514;
                    break;
                case "HOOK_CREATION_BYTES_TOO_LONG":
                case 515:
                    message.nodeTransactionPrecheckCode = 515;
                    break;
                case "INVALID_HOOK_CREATION_SPEC":
                case 516:
                    message.nodeTransactionPrecheckCode = 516;
                    break;
                case "HOOK_EXTENSION_EMPTY":
                case 517:
                    message.nodeTransactionPrecheckCode = 517;
                    break;
                case "INVALID_HOOK_ADMIN_KEY":
                case 518:
                    message.nodeTransactionPrecheckCode = 518;
                    break;
                case "HOOK_DELETION_REQUIRES_ZERO_STORAGE_SLOTS":
                case 519:
                    message.nodeTransactionPrecheckCode = 519;
                    break;
                case "CANNOT_SET_HOOKS_AND_APPROVAL":
                case 520:
                    message.nodeTransactionPrecheckCode = 520;
                    break;
                case "TRANSACTION_REQUIRES_ZERO_HOOKS":
                case 521:
                    message.nodeTransactionPrecheckCode = 521;
                    break;
                case "INVALID_HOOK_CALL":
                case 522:
                    message.nodeTransactionPrecheckCode = 522;
                    break;
                case "HOOKS_ARE_NOT_SUPPORTED_IN_AIRDROPS":
                case 523:
                    message.nodeTransactionPrecheckCode = 523;
                    break;
                case "ACCOUNT_IS_LINKED_TO_A_NODE":
                case 524:
                    message.nodeTransactionPrecheckCode = 524;
                    break;
                case "HOOKS_EXECUTIONS_REQUIRE_TOP_LEVEL_CRYPTO_TRANSFER":
                case 525:
                    message.nodeTransactionPrecheckCode = 525;
                    break;
                case "NODE_ACCOUNT_HAS_ZERO_BALANCE":
                case 526:
                    message.nodeTransactionPrecheckCode = 526;
                    break;
                case "TRANSFER_TO_FEE_COLLECTION_ACCOUNT_NOT_ALLOWED":
                case 527:
                    message.nodeTransactionPrecheckCode = 527;
                    break;
                case "TOO_MANY_HOOK_INVOCATIONS":
                case 528:
                    message.nodeTransactionPrecheckCode = 528;
                    break;
                case "INVALID_REGISTERED_NODE_ID":
                case 529:
                    message.nodeTransactionPrecheckCode = 529;
                    break;
                case "INVALID_REGISTERED_ENDPOINT":
                case 530:
                    message.nodeTransactionPrecheckCode = 530;
                    break;
                case "REGISTERED_ENDPOINTS_EXCEEDED_LIMIT":
                case 531:
                    message.nodeTransactionPrecheckCode = 531;
                    break;
                case "INVALID_REGISTERED_ENDPOINT_ADDRESS":
                case 532:
                    message.nodeTransactionPrecheckCode = 532;
                    break;
                case "INVALID_REGISTERED_ENDPOINT_TYPE":
                case 533:
                    message.nodeTransactionPrecheckCode = 533;
                    break;
                case "REGISTERED_NODE_STILL_ASSOCIATED":
                case 534:
                    message.nodeTransactionPrecheckCode = 534;
                    break;
                case "MAX_REGISTERED_NODES_EXCEEDED":
                case 535:
                    message.nodeTransactionPrecheckCode = 535;
                    break;
                default:
                    if (typeof object.nodeTransactionPrecheckCode === "number" && (object.nodeTransactionPrecheckCode | 0) === object.nodeTransactionPrecheckCode)
                        message.nodeTransactionPrecheckCode = object.nodeTransactionPrecheckCode;
                }
            if (object.responseType !== 0 && (typeof object.responseType !== "string" || $root.proto.ResponseType[object.responseType] !== 0))
                switch (object.responseType) {
                case "ANSWER_ONLY":
                case 0:
                    message.responseType = 0;
                    break;
                case "ANSWER_STATE_PROOF":
                case 1:
                    message.responseType = 1;
                    break;
                case "COST_ANSWER":
                case 2:
                    message.responseType = 2;
                    break;
                case "COST_ANSWER_STATE_PROOF":
                case 3:
                    message.responseType = 3;
                    break;
                default:
                    if (typeof object.responseType === "number" && (object.responseType | 0) === object.responseType)
                        message.responseType = object.responseType;
                }
            if (object.cost != null)
                if (typeof object.cost === "object" ? object.cost.low || object.cost.high : $Number(object.cost) !== 0)
                    if ($util.Long)
                        message.cost = $util.Long.fromValue(object.cost, true);
                    else if (typeof object.cost === "string")
                        message.cost = $parseInt(object.cost, 10);
                    else if (typeof object.cost === "number")
                        message.cost = object.cost;
                    else if (typeof object.cost === "object")
                        message.cost = new $util.LongBits(object.cost.low >>> 0, object.cost.high >>> 0).toNumber(true);
            if (object.stateProof != null)
                if (object.stateProof.length)
                    if (typeof object.stateProof === "string")
                        $util.base64.decode(object.stateProof, message.stateProof = $util.newBuffer($util.base64.length(object.stateProof)), 0);
                    else if (object.stateProof.length >= 0)
                        message.stateProof = object.stateProof;
            return message;
        };

        /**
         * Creates a plain object from a ResponseHeader message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ResponseHeader
         * @static
         * @param {proto.ResponseHeader} message ResponseHeader
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ResponseHeader.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.nodeTransactionPrecheckCode = options.enums === $String ? "OK" : 0;
                object.responseType = options.enums === $String ? "ANSWER_ONLY" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.cost = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.cost = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.stateProof = "";
                else {
                    object.stateProof = [];
                    if (options.bytes !== $Array)
                        object.stateProof = $util.newBuffer(object.stateProof);
                }
            }
            if (message.nodeTransactionPrecheckCode != null && $Object.hasOwnProperty.call(message, "nodeTransactionPrecheckCode"))
                object.nodeTransactionPrecheckCode = options.enums === $String ? $root.proto.ResponseCodeEnum[message.nodeTransactionPrecheckCode] === $undefined ? message.nodeTransactionPrecheckCode : $root.proto.ResponseCodeEnum[message.nodeTransactionPrecheckCode] : message.nodeTransactionPrecheckCode;
            if (message.responseType != null && $Object.hasOwnProperty.call(message, "responseType"))
                object.responseType = options.enums === $String ? $root.proto.ResponseType[message.responseType] === $undefined ? message.responseType : $root.proto.ResponseType[message.responseType] : message.responseType;
            if (message.cost != null && $Object.hasOwnProperty.call(message, "cost"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.cost = typeof message.cost === "number" ? $BigInt(message.cost) : $util.Long.fromBits(message.cost.low >>> 0, message.cost.high >>> 0, true).toBigInt();
                else if (typeof message.cost === "number")
                    object.cost = options.longs === $String ? $String(message.cost) : message.cost;
                else
                    object.cost = options.longs === $String ? $util.Long.prototype.toString.call(message.cost) : options.longs === $Number ? new $util.LongBits(message.cost.low >>> 0, message.cost.high >>> 0).toNumber(true) : message.cost;
            if (message.stateProof != null && $Object.hasOwnProperty.call(message, "stateProof"))
                object.stateProof = options.bytes === $String ? $util.base64.encode(message.stateProof, 0, message.stateProof.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.stateProof) : message.stateProof;
            return object;
        };

        /**
         * Converts this ResponseHeader to JSON.
         * @function toJSON
         * @memberof proto.ResponseHeader
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ResponseHeader.prototype.toJSON = function() {
            return ResponseHeader.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ResponseHeader
         * @function getTypeUrl
         * @memberof proto.ResponseHeader
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ResponseHeader.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ResponseHeader";
        };

        return ResponseHeader;
    })();

    proto.ConsensusTopicInfo = (function() {

        /**
         * Properties of a ConsensusTopicInfo.
         * @typedef {Object} proto.ConsensusTopicInfo.$Properties
         * @property {string|null} [memo] A short description of this topic.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {Uint8Array|null} [runningHash] The latest running hash of the topic.
         * <p>
         * This 48-byte field is the output of a SHA-384 digest with input
         * data determined by the current version of the running hash algorithm
         * used by the network.<br/>
         * All new transactions SHALL use algorithm version `3`.<br/>
         * The bytes of each uint64 or uint32 encoded for the hash input
         * MUST be in Big-Endian format.
         * <p>
         * <hr/>
         * If the algorithm version is '3', then the input data to the
         * SHA-384 digest are, in order:
         * <ol>
         * <li>The previous running hash of the topic (48 bytes)</li>
         * <li>The `topicRunningHashVersion` (8 bytes)</li>
         * <li>The payer account's shard (8 bytes)</li>
         * <li>The payer account's realm (8 bytes)</li>
         * <li>The payer account's number (8 bytes)</li>
         * <li>The topic's shard (8 bytes)</li>
         * <li>The topic's realm (8 bytes)</li>
         * <li>The topic's number (8 bytes)</li>
         * <li>The number of seconds since the epoch when the
         * `ConsensusSubmitMessage` reached consensus (8 bytes)</li>
         * <li>The number of nanoseconds within the second when the
         * `ConsensusSubmitMessage` reached consensus (4 bytes)</li>
         * <li>The `topicSequenceNumber` (8 bytes)</li>
         * <li>The output of a SHA-384 digest of the message bytes from the
         * `ConsensusSubmitMessage` (48 bytes)</li>
         * </ol>
         * @property {number|Long|null} [sequenceNumber] A current sequence number (starting at 1 for the first message)
         * for messages on this topic.
         * @property {proto.Timestamp.$Properties|null} [expirationTime] An expiration time for this topic, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch
         * with 0 at `1970-01-01T00:00:00.000Z`.
         * @property {proto.Key.$Properties|null} [adminKey] A key that MUST sign any transaction to update or delete this topic.
         * <p>
         * If this value is not set (null) then the topic CANNOT be deleted,
         * modified, or updated.
         * @property {proto.Key.$Properties|null} [submitKey] A key that MUST sign any transaction to submit a message to this topic.
         * <p>
         * If this value is not set (null) then any account MAY submit messages to
         * this topic.
         * @property {proto.Duration.$Properties|null} [autoRenewPeriod] A duration, in seconds, to extend the `expirationTime` value when
         * this topic is automatically renewed.
         * <p>
         * If the `autoRenewAccount` value for this topic is set to a valid account
         * with sufficient HBAR balance to pay renewal fees when this topic
         * expires, the system SHALL automatically renew this topic, extending the
         * `expirationTime` value by the number of seconds described here.<br/>
         * If, however, the `autoRenewAccount` lacks sufficient HBAR balance
         * to pay renewal fees when this topic expires, this topic SHALL be
         * deleted after the time period specified in the `AUTORENEW_GRACE_PERIOD`
         * configuration value.
         * @property {proto.AccountID.$Properties|null} [autoRenewAccount] An account that is designated to pay automatic renewal fees.
         * <p>
         * If this value is a valid account ID when this topic expires,
         * this account SHALL be charged the renewal fees for this topic,
         * if it holds sufficient HBAR balance. If the account does not hold
         * sufficient HBAR balance to pay renewal fees when necessary, then
         * this topic SHALL be deleted.<br/>
         * If this value is not set (null), or is not a valid account ID, when
         * this topic expires, then this topic SHALL be deleted after the time
         * period specified in the `AUTORENEW_GRACE_PERIOD` configuration value.
         * @property {Uint8Array|null} [ledgerId] A ledger ID of the network that generated this response.
         * <p>
         * This value SHALL identify the distributed ledger that responded to
         * this query.
         * @property {proto.Key.$Properties|null} [feeScheduleKey] Access control for update/delete of custom fees.
         * <p>
         * If unset, custom fees CANNOT be set for this topic.<br/>
         * If not set when the topic is created, this field CANNOT be set via
         * update.<br/>
         * If set when the topic is created, this field MAY be changed via update.
         * @property {Array.<proto.Key.$Properties>|null} [feeExemptKeyList] A set of keys.<br/>
         * Keys in this list are permitted to submit messages to this topic without
         * paying custom fees associated with this topic.
         * <p>
         * If a topic submit message is signed by _any_ key included in this set,
         * custom fees SHALL NOT be charged for that transaction.<br/>
         * `fee_exempt_key_list` MAY contain keys for accounts that are inactive,
         * deleted, or non-existent.<br/>
         * If not set, there SHALL NOT be any fee-exempt keys.  In particular, the
         * following keys SHALL NOT be implicitly or automatically added to this
         * list: `adminKey`, `submitKey`, `fee_schedule_key`.
         * A `fee_exempt_key_list` MUST NOT contain more than
         * `MAX_ENTRIES_FOR_FEE_EXEMPT_KEY_LIST` keys.
         * A `fee_exempt_key_list` MUST NOT contain any duplicate keys.
         * @property {Array.<proto.FixedCustomFee.$Properties>|null} [customFees] A set of custom fee definitions.<br/>
         * These are fees to be assessed for each submit to this topic.
         * <p>
         * Each fee defined in this set SHALL be evaluated for
         * each message submitted to this topic, and the resultant
         * total assessed fees SHALL be charged.<br/>
         * Custom fees defined here SHALL be assessed in addition to the base
         * network and node fees.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ConsensusTopicInfo.
         * @memberof proto
         * @interface IConsensusTopicInfo
         * @augments proto.ConsensusTopicInfo.$Properties
         * @deprecated Use proto.ConsensusTopicInfo.$Properties instead.
         */

        /**
         * Shape of a ConsensusTopicInfo.
         * @typedef {{
         *   memo?: string|null;
         *   runningHash?: Uint8Array|null;
         *   sequenceNumber?: number|Long|null;
         *   expirationTime?: proto.Timestamp.$Shape|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   submitKey?: proto.Key.$Shape|null;
         *   autoRenewPeriod?: proto.Duration.$Shape|null;
         *   autoRenewAccount?: proto.AccountID.$Shape|null;
         *   ledgerId?: Uint8Array|null;
         *   feeScheduleKey?: proto.Key.$Shape|null;
         *   feeExemptKeyList?: Array.<proto.Key.$Shape>|null;
         *   customFees?: Array.<proto.FixedCustomFee.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ConsensusTopicInfo.$Shape
         */

        /**
         * Constructs a new ConsensusTopicInfo.
         * @memberof proto
         * @classdesc A query response describing the current state of a topic for the Hedera
         * Consensus Service (HCS).
         * @constructor
         * @param {proto.ConsensusTopicInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ConsensusTopicInfo = function (properties) {
            this.feeExemptKeyList = [];
            this.customFees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A short description of this topic.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.ConsensusTopicInfo
         * @instance
         */
        ConsensusTopicInfo.prototype.memo = "";

        /**
         * The latest running hash of the topic.
         * <p>
         * This 48-byte field is the output of a SHA-384 digest with input
         * data determined by the current version of the running hash algorithm
         * used by the network.<br/>
         * All new transactions SHALL use algorithm version `3`.<br/>
         * The bytes of each uint64 or uint32 encoded for the hash input
         * MUST be in Big-Endian format.
         * <p>
         * <hr/>
         * If the algorithm version is '3', then the input data to the
         * SHA-384 digest are, in order:
         * <ol>
         * <li>The previous running hash of the topic (48 bytes)</li>
         * <li>The `topicRunningHashVersion` (8 bytes)</li>
         * <li>The payer account's shard (8 bytes)</li>
         * <li>The payer account's realm (8 bytes)</li>
         * <li>The payer account's number (8 bytes)</li>
         * <li>The topic's shard (8 bytes)</li>
         * <li>The topic's realm (8 bytes)</li>
         * <li>The topic's number (8 bytes)</li>
         * <li>The number of seconds since the epoch when the
         * `ConsensusSubmitMessage` reached consensus (8 bytes)</li>
         * <li>The number of nanoseconds within the second when the
         * `ConsensusSubmitMessage` reached consensus (4 bytes)</li>
         * <li>The `topicSequenceNumber` (8 bytes)</li>
         * <li>The output of a SHA-384 digest of the message bytes from the
         * `ConsensusSubmitMessage` (48 bytes)</li>
         * </ol>
         * @member {Uint8Array} runningHash
         * @memberof proto.ConsensusTopicInfo
         * @instance
         */
        ConsensusTopicInfo.prototype.runningHash = $util.newBuffer([]);

        /**
         * A current sequence number (starting at 1 for the first message)
         * for messages on this topic.
         * @member {number|Long} sequenceNumber
         * @memberof proto.ConsensusTopicInfo
         * @instance
         */
        ConsensusTopicInfo.prototype.sequenceNumber = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * An expiration time for this topic, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch
         * with 0 at `1970-01-01T00:00:00.000Z`.
         * @member {proto.Timestamp.$Properties|null|undefined} expirationTime
         * @memberof proto.ConsensusTopicInfo
         * @instance
         */
        ConsensusTopicInfo.prototype.expirationTime = null;

        /**
         * A key that MUST sign any transaction to update or delete this topic.
         * <p>
         * If this value is not set (null) then the topic CANNOT be deleted,
         * modified, or updated.
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.ConsensusTopicInfo
         * @instance
         */
        ConsensusTopicInfo.prototype.adminKey = null;

        /**
         * A key that MUST sign any transaction to submit a message to this topic.
         * <p>
         * If this value is not set (null) then any account MAY submit messages to
         * this topic.
         * @member {proto.Key.$Properties|null|undefined} submitKey
         * @memberof proto.ConsensusTopicInfo
         * @instance
         */
        ConsensusTopicInfo.prototype.submitKey = null;

        /**
         * A duration, in seconds, to extend the `expirationTime` value when
         * this topic is automatically renewed.
         * <p>
         * If the `autoRenewAccount` value for this topic is set to a valid account
         * with sufficient HBAR balance to pay renewal fees when this topic
         * expires, the system SHALL automatically renew this topic, extending the
         * `expirationTime` value by the number of seconds described here.<br/>
         * If, however, the `autoRenewAccount` lacks sufficient HBAR balance
         * to pay renewal fees when this topic expires, this topic SHALL be
         * deleted after the time period specified in the `AUTORENEW_GRACE_PERIOD`
         * configuration value.
         * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
         * @memberof proto.ConsensusTopicInfo
         * @instance
         */
        ConsensusTopicInfo.prototype.autoRenewPeriod = null;

        /**
         * An account that is designated to pay automatic renewal fees.
         * <p>
         * If this value is a valid account ID when this topic expires,
         * this account SHALL be charged the renewal fees for this topic,
         * if it holds sufficient HBAR balance. If the account does not hold
         * sufficient HBAR balance to pay renewal fees when necessary, then
         * this topic SHALL be deleted.<br/>
         * If this value is not set (null), or is not a valid account ID, when
         * this topic expires, then this topic SHALL be deleted after the time
         * period specified in the `AUTORENEW_GRACE_PERIOD` configuration value.
         * @member {proto.AccountID.$Properties|null|undefined} autoRenewAccount
         * @memberof proto.ConsensusTopicInfo
         * @instance
         */
        ConsensusTopicInfo.prototype.autoRenewAccount = null;

        /**
         * A ledger ID of the network that generated this response.
         * <p>
         * This value SHALL identify the distributed ledger that responded to
         * this query.
         * @member {Uint8Array} ledgerId
         * @memberof proto.ConsensusTopicInfo
         * @instance
         */
        ConsensusTopicInfo.prototype.ledgerId = $util.newBuffer([]);

        /**
         * Access control for update/delete of custom fees.
         * <p>
         * If unset, custom fees CANNOT be set for this topic.<br/>
         * If not set when the topic is created, this field CANNOT be set via
         * update.<br/>
         * If set when the topic is created, this field MAY be changed via update.
         * @member {proto.Key.$Properties|null|undefined} feeScheduleKey
         * @memberof proto.ConsensusTopicInfo
         * @instance
         */
        ConsensusTopicInfo.prototype.feeScheduleKey = null;

        /**
         * A set of keys.<br/>
         * Keys in this list are permitted to submit messages to this topic without
         * paying custom fees associated with this topic.
         * <p>
         * If a topic submit message is signed by _any_ key included in this set,
         * custom fees SHALL NOT be charged for that transaction.<br/>
         * `fee_exempt_key_list` MAY contain keys for accounts that are inactive,
         * deleted, or non-existent.<br/>
         * If not set, there SHALL NOT be any fee-exempt keys.  In particular, the
         * following keys SHALL NOT be implicitly or automatically added to this
         * list: `adminKey`, `submitKey`, `fee_schedule_key`.
         * A `fee_exempt_key_list` MUST NOT contain more than
         * `MAX_ENTRIES_FOR_FEE_EXEMPT_KEY_LIST` keys.
         * A `fee_exempt_key_list` MUST NOT contain any duplicate keys.
         * @member {Array.<proto.Key.$Properties>} feeExemptKeyList
         * @memberof proto.ConsensusTopicInfo
         * @instance
         */
        ConsensusTopicInfo.prototype.feeExemptKeyList = $util.emptyArray;

        /**
         * A set of custom fee definitions.<br/>
         * These are fees to be assessed for each submit to this topic.
         * <p>
         * Each fee defined in this set SHALL be evaluated for
         * each message submitted to this topic, and the resultant
         * total assessed fees SHALL be charged.<br/>
         * Custom fees defined here SHALL be assessed in addition to the base
         * network and node fees.
         * @member {Array.<proto.FixedCustomFee.$Properties>} customFees
         * @memberof proto.ConsensusTopicInfo
         * @instance
         */
        ConsensusTopicInfo.prototype.customFees = $util.emptyArray;

        /**
         * Creates a new ConsensusTopicInfo instance using the specified properties.
         * @function create
         * @memberof proto.ConsensusTopicInfo
         * @static
         * @param {proto.ConsensusTopicInfo.$Properties=} [properties] Properties to set
         * @returns {proto.ConsensusTopicInfo} ConsensusTopicInfo instance
         * @type {{
         *   (properties: proto.ConsensusTopicInfo.$Shape): proto.ConsensusTopicInfo & proto.ConsensusTopicInfo.$Shape;
         *   (properties?: proto.ConsensusTopicInfo.$Properties): proto.ConsensusTopicInfo;
         * }}
         */
        ConsensusTopicInfo.create = function(properties) {
            return new ConsensusTopicInfo(properties);
        };

        /**
         * Encodes the specified ConsensusTopicInfo message. Does not implicitly {@link proto.ConsensusTopicInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.ConsensusTopicInfo
         * @static
         * @param {proto.ConsensusTopicInfo.$Properties} message ConsensusTopicInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusTopicInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.memo);
            if (message.runningHash != null && $Object.hasOwnProperty.call(message, "runningHash") && message.runningHash.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.runningHash);
            if (message.sequenceNumber != null && $Object.hasOwnProperty.call(message, "sequenceNumber") && (typeof message.sequenceNumber === "object" ? message.sequenceNumber.low || message.sequenceNumber.high : message.sequenceNumber !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.sequenceNumber);
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                $root.proto.Timestamp.encode(message.expirationTime, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.submitKey != null && $Object.hasOwnProperty.call(message, "submitKey"))
                $root.proto.Key.encode(message.submitKey, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount"))
                $root.proto.AccountID.encode(message.autoRenewAccount, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId") && message.ledgerId.length)
                writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.ledgerId);
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                $root.proto.Key.encode(message.feeScheduleKey, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.feeExemptKeyList != null && message.feeExemptKeyList.length)
                for (let i = 0; i < message.feeExemptKeyList.length; ++i)
                    $root.proto.Key.encode(message.feeExemptKeyList[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.customFees != null && message.customFees.length)
                for (let i = 0; i < message.customFees.length; ++i)
                    $root.proto.FixedCustomFee.encode(message.customFees[i], writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ConsensusTopicInfo message, length delimited. Does not implicitly {@link proto.ConsensusTopicInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ConsensusTopicInfo
         * @static
         * @param {proto.ConsensusTopicInfo.$Properties} message ConsensusTopicInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusTopicInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ConsensusTopicInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ConsensusTopicInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ConsensusTopicInfo & proto.ConsensusTopicInfo.$Shape} ConsensusTopicInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusTopicInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ConsensusTopicInfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.runningHash = value;
                        else
                            delete message.runningHash;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.sequenceNumber = value;
                        else
                            delete message.sequenceNumber;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.expirationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.submitKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.submitKey);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewAccount = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewAccount);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.ledgerId = value;
                        else
                            delete message.ledgerId;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.feeScheduleKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeScheduleKey);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.feeExemptKeyList && message.feeExemptKeyList.length))
                            message.feeExemptKeyList = [];
                        message.feeExemptKeyList.push($root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.customFees && message.customFees.length))
                            message.customFees = [];
                        message.customFees.push($root.proto.FixedCustomFee.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ConsensusTopicInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ConsensusTopicInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ConsensusTopicInfo & proto.ConsensusTopicInfo.$Shape} ConsensusTopicInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusTopicInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConsensusTopicInfo message.
         * @function verify
         * @memberof proto.ConsensusTopicInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConsensusTopicInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.runningHash != null && $Object.hasOwnProperty.call(message, "runningHash"))
                if (!(message.runningHash && typeof message.runningHash.length === "number" || $util.isString(message.runningHash)))
                    return "runningHash: buffer expected";
            if (message.sequenceNumber != null && $Object.hasOwnProperty.call(message, "sequenceNumber"))
                if (!$util.isInteger(message.sequenceNumber) && !(message.sequenceNumber && $util.isInteger(message.sequenceNumber.low) && $util.isInteger(message.sequenceNumber.high)))
                    return "sequenceNumber: integer|Long expected";
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                let error = $root.proto.Timestamp.verify(message.expirationTime, _depth + 1);
                if (error)
                    return "expirationTime." + error;
            }
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.submitKey != null && $Object.hasOwnProperty.call(message, "submitKey")) {
                let error = $root.proto.Key.verify(message.submitKey, _depth + 1);
                if (error)
                    return "submitKey." + error;
            }
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                if (error)
                    return "autoRenewPeriod." + error;
            }
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount")) {
                let error = $root.proto.AccountID.verify(message.autoRenewAccount, _depth + 1);
                if (error)
                    return "autoRenewAccount." + error;
            }
            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                if (!(message.ledgerId && typeof message.ledgerId.length === "number" || $util.isString(message.ledgerId)))
                    return "ledgerId: buffer expected";
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey")) {
                let error = $root.proto.Key.verify(message.feeScheduleKey, _depth + 1);
                if (error)
                    return "feeScheduleKey." + error;
            }
            if (message.feeExemptKeyList != null && $Object.hasOwnProperty.call(message, "feeExemptKeyList")) {
                if (!$Array.isArray(message.feeExemptKeyList))
                    return "feeExemptKeyList: array expected";
                for (let i = 0; i < message.feeExemptKeyList.length; ++i) {
                    let error = $root.proto.Key.verify(message.feeExemptKeyList[i], _depth + 1);
                    if (error)
                        return "feeExemptKeyList." + error;
                }
            }
            if (message.customFees != null && $Object.hasOwnProperty.call(message, "customFees")) {
                if (!$Array.isArray(message.customFees))
                    return "customFees: array expected";
                for (let i = 0; i < message.customFees.length; ++i) {
                    let error = $root.proto.FixedCustomFee.verify(message.customFees[i], _depth + 1);
                    if (error)
                        return "customFees." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ConsensusTopicInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ConsensusTopicInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ConsensusTopicInfo} ConsensusTopicInfo
         */
        ConsensusTopicInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ConsensusTopicInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ConsensusTopicInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ConsensusTopicInfo();
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.runningHash != null)
                if (object.runningHash.length)
                    if (typeof object.runningHash === "string")
                        $util.base64.decode(object.runningHash, message.runningHash = $util.newBuffer($util.base64.length(object.runningHash)), 0);
                    else if (object.runningHash.length >= 0)
                        message.runningHash = object.runningHash;
            if (object.sequenceNumber != null)
                if (typeof object.sequenceNumber === "object" ? object.sequenceNumber.low || object.sequenceNumber.high : $Number(object.sequenceNumber) !== 0)
                    if ($util.Long)
                        message.sequenceNumber = $util.Long.fromValue(object.sequenceNumber, true);
                    else if (typeof object.sequenceNumber === "string")
                        message.sequenceNumber = $parseInt(object.sequenceNumber, 10);
                    else if (typeof object.sequenceNumber === "number")
                        message.sequenceNumber = object.sequenceNumber;
                    else if (typeof object.sequenceNumber === "object")
                        message.sequenceNumber = new $util.LongBits(object.sequenceNumber.low >>> 0, object.sequenceNumber.high >>> 0).toNumber(true);
            if (object.expirationTime != null) {
                if (!$util.isObject(object.expirationTime))
                    throw $TypeError(".proto.ConsensusTopicInfo.expirationTime: object expected");
                message.expirationTime = $root.proto.Timestamp.fromObject(object.expirationTime, _depth + 1);
            }
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.ConsensusTopicInfo.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.submitKey != null) {
                if (!$util.isObject(object.submitKey))
                    throw $TypeError(".proto.ConsensusTopicInfo.submitKey: object expected");
                message.submitKey = $root.proto.Key.fromObject(object.submitKey, _depth + 1);
            }
            if (object.autoRenewPeriod != null) {
                if (!$util.isObject(object.autoRenewPeriod))
                    throw $TypeError(".proto.ConsensusTopicInfo.autoRenewPeriod: object expected");
                message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
            }
            if (object.autoRenewAccount != null) {
                if (!$util.isObject(object.autoRenewAccount))
                    throw $TypeError(".proto.ConsensusTopicInfo.autoRenewAccount: object expected");
                message.autoRenewAccount = $root.proto.AccountID.fromObject(object.autoRenewAccount, _depth + 1);
            }
            if (object.ledgerId != null)
                if (object.ledgerId.length)
                    if (typeof object.ledgerId === "string")
                        $util.base64.decode(object.ledgerId, message.ledgerId = $util.newBuffer($util.base64.length(object.ledgerId)), 0);
                    else if (object.ledgerId.length >= 0)
                        message.ledgerId = object.ledgerId;
            if (object.feeScheduleKey != null) {
                if (!$util.isObject(object.feeScheduleKey))
                    throw $TypeError(".proto.ConsensusTopicInfo.feeScheduleKey: object expected");
                message.feeScheduleKey = $root.proto.Key.fromObject(object.feeScheduleKey, _depth + 1);
            }
            if (object.feeExemptKeyList) {
                if (!$Array.isArray(object.feeExemptKeyList))
                    throw $TypeError(".proto.ConsensusTopicInfo.feeExemptKeyList: array expected");
                message.feeExemptKeyList = $Array(object.feeExemptKeyList.length);
                for (let i = 0; i < object.feeExemptKeyList.length; ++i) {
                    if (!$util.isObject(object.feeExemptKeyList[i]))
                        throw $TypeError(".proto.ConsensusTopicInfo.feeExemptKeyList: object expected");
                    message.feeExemptKeyList[i] = $root.proto.Key.fromObject(object.feeExemptKeyList[i], _depth + 1);
                }
            }
            if (object.customFees) {
                if (!$Array.isArray(object.customFees))
                    throw $TypeError(".proto.ConsensusTopicInfo.customFees: array expected");
                message.customFees = $Array(object.customFees.length);
                for (let i = 0; i < object.customFees.length; ++i) {
                    if (!$util.isObject(object.customFees[i]))
                        throw $TypeError(".proto.ConsensusTopicInfo.customFees: object expected");
                    message.customFees[i] = $root.proto.FixedCustomFee.fromObject(object.customFees[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ConsensusTopicInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ConsensusTopicInfo
         * @static
         * @param {proto.ConsensusTopicInfo} message ConsensusTopicInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConsensusTopicInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.feeExemptKeyList = [];
                object.customFees = [];
            }
            if (options.defaults) {
                object.memo = "";
                if (options.bytes === $String)
                    object.runningHash = "";
                else {
                    object.runningHash = [];
                    if (options.bytes !== $Array)
                        object.runningHash = $util.newBuffer(object.runningHash);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.sequenceNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sequenceNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.expirationTime = null;
                object.adminKey = null;
                object.submitKey = null;
                object.autoRenewPeriod = null;
                object.autoRenewAccount = null;
                if (options.bytes === $String)
                    object.ledgerId = "";
                else {
                    object.ledgerId = [];
                    if (options.bytes !== $Array)
                        object.ledgerId = $util.newBuffer(object.ledgerId);
                }
                object.feeScheduleKey = null;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.runningHash != null && $Object.hasOwnProperty.call(message, "runningHash"))
                object.runningHash = options.bytes === $String ? $util.base64.encode(message.runningHash, 0, message.runningHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.runningHash) : message.runningHash;
            if (message.sequenceNumber != null && $Object.hasOwnProperty.call(message, "sequenceNumber"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sequenceNumber = typeof message.sequenceNumber === "number" ? $BigInt(message.sequenceNumber) : $util.Long.fromBits(message.sequenceNumber.low >>> 0, message.sequenceNumber.high >>> 0, true).toBigInt();
                else if (typeof message.sequenceNumber === "number")
                    object.sequenceNumber = options.longs === $String ? $String(message.sequenceNumber) : message.sequenceNumber;
                else
                    object.sequenceNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.sequenceNumber) : options.longs === $Number ? new $util.LongBits(message.sequenceNumber.low >>> 0, message.sequenceNumber.high >>> 0).toNumber(true) : message.sequenceNumber;
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                object.expirationTime = $root.proto.Timestamp.toObject(message.expirationTime, options, _depth + 1);
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.submitKey != null && $Object.hasOwnProperty.call(message, "submitKey"))
                object.submitKey = $root.proto.Key.toObject(message.submitKey, options, _depth + 1);
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount"))
                object.autoRenewAccount = $root.proto.AccountID.toObject(message.autoRenewAccount, options, _depth + 1);
            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                object.ledgerId = options.bytes === $String ? $util.base64.encode(message.ledgerId, 0, message.ledgerId.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ledgerId) : message.ledgerId;
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                object.feeScheduleKey = $root.proto.Key.toObject(message.feeScheduleKey, options, _depth + 1);
            if (message.feeExemptKeyList && message.feeExemptKeyList.length) {
                object.feeExemptKeyList = $Array(message.feeExemptKeyList.length);
                for (let j = 0; j < message.feeExemptKeyList.length; ++j)
                    object.feeExemptKeyList[j] = $root.proto.Key.toObject(message.feeExemptKeyList[j], options, _depth + 1);
            }
            if (message.customFees && message.customFees.length) {
                object.customFees = $Array(message.customFees.length);
                for (let j = 0; j < message.customFees.length; ++j)
                    object.customFees[j] = $root.proto.FixedCustomFee.toObject(message.customFees[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ConsensusTopicInfo to JSON.
         * @function toJSON
         * @memberof proto.ConsensusTopicInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConsensusTopicInfo.prototype.toJSON = function() {
            return ConsensusTopicInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ConsensusTopicInfo
         * @function getTypeUrl
         * @memberof proto.ConsensusTopicInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ConsensusTopicInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ConsensusTopicInfo";
        };

        return ConsensusTopicInfo;
    })();

    proto.ConsensusService = (function() {

        /**
         * Constructs a new ConsensusService service.
         * @memberof proto
         * @classdesc The Hedera Consensus Service (HCS) provides the ability for a Hashgraph to
         * provide aBFT consensus as to the order and validity of messages submitted to
         * a *topic*, as well as a *consensus timestamp* for those messages.
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const ConsensusService = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        $Object.defineProperty(ConsensusService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: ConsensusService, writable: true, enumerable: false, configurable: true });

        /**
         * Creates new ConsensusService service using the specified rpc implementation.
         * @function create
         * @memberof proto.ConsensusService
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {ConsensusService} RPC service. Useful where requests and/or responses are streamed.
         */
        ConsensusService.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link proto.ConsensusService#createTopic}.
         * @memberof proto.ConsensusService
         * @typedef createTopicCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Create an HCS topic.
         * <p>
         * On success, the resulting TransactionReceipt SHALL contain the newly
         * created TopicId.<br/>
         * If the `adminKey` is set on the topic, this transaction MUST be signed
         * by that key.<br/>
         * If the `adminKey` is _not_ set on the topic, this transaction MUST NOT
         * set an `autoRenewAccount`. The new topic will be immutable and must be
         * renewed manually.<br/>
         * If the `autoRenewAccount` is set on the topic, this transaction MUST be
         * signed by that account.<br/>
         * <p>
         * The request body MUST be a
         * [ConsensusCreateTopicTransactionBody](#proto.ConsensusCreateTopicTransactionBody)
         * @memberof proto.ConsensusService
         * @typedef createTopic
         * @type {{
         *   (request: proto.ITransaction, callback: proto.ConsensusService.createTopicCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "createTopic";
         *   readonly path: "/proto.ConsensusService/createTopic";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Create an HCS topic.
         * <p>
         * On success, the resulting TransactionReceipt SHALL contain the newly
         * created TopicId.<br/>
         * If the `adminKey` is set on the topic, this transaction MUST be signed
         * by that key.<br/>
         * If the `adminKey` is _not_ set on the topic, this transaction MUST NOT
         * set an `autoRenewAccount`. The new topic will be immutable and must be
         * renewed manually.<br/>
         * If the `autoRenewAccount` is set on the topic, this transaction MUST be
         * signed by that account.<br/>
         * <p>
         * The request body MUST be a
         * [ConsensusCreateTopicTransactionBody](#proto.ConsensusCreateTopicTransactionBody)
         * @name proto.ConsensusService#createTopic
         * @type {proto.ConsensusService.createTopic}
         */
        $Object.defineProperties(ConsensusService.prototype.createTopic = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, ConsensusService.prototype.createTopic, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "createTopic" },
            path: { value: "/proto.ConsensusService/createTopic" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.ConsensusService#updateTopic}.
         * @memberof proto.ConsensusService
         * @typedef updateTopicCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Update an HCS topic.
         * <p>
         * If the `adminKey` is not set on the topic, this transaction MUST extend
         * the `expirationTime` and MUST NOT modify any other field.<br/>
         * If the `adminKey` is set on the topic, this transaction MUST be signed
         * by that key.<br/>
         * If this transaction sets a new `adminKey`, this transaction MUST be
         * signed by <strong>_both_</strong> keys, the pre-update `adminKey` and
         * the post-update `adminKey`.<br/>
         * If this transaction sets a new, non-null, `autoRenewAccount`, the newly
         * set account MUST sign this transaction.<br/>
         * <p>
         * The request body MUST be a
         * [ConsensusUpdateTopicTransactionBody](#proto.ConsensusUpdateTopicTransactionBody)
         * @memberof proto.ConsensusService
         * @typedef updateTopic
         * @type {{
         *   (request: proto.ITransaction, callback: proto.ConsensusService.updateTopicCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "updateTopic";
         *   readonly path: "/proto.ConsensusService/updateTopic";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Update an HCS topic.
         * <p>
         * If the `adminKey` is not set on the topic, this transaction MUST extend
         * the `expirationTime` and MUST NOT modify any other field.<br/>
         * If the `adminKey` is set on the topic, this transaction MUST be signed
         * by that key.<br/>
         * If this transaction sets a new `adminKey`, this transaction MUST be
         * signed by <strong>_both_</strong> keys, the pre-update `adminKey` and
         * the post-update `adminKey`.<br/>
         * If this transaction sets a new, non-null, `autoRenewAccount`, the newly
         * set account MUST sign this transaction.<br/>
         * <p>
         * The request body MUST be a
         * [ConsensusUpdateTopicTransactionBody](#proto.ConsensusUpdateTopicTransactionBody)
         * @name proto.ConsensusService#updateTopic
         * @type {proto.ConsensusService.updateTopic}
         */
        $Object.defineProperties(ConsensusService.prototype.updateTopic = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, ConsensusService.prototype.updateTopic, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "updateTopic" },
            path: { value: "/proto.ConsensusService/updateTopic" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.ConsensusService#deleteTopic}.
         * @memberof proto.ConsensusService
         * @typedef deleteTopicCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Delete an HCS topic.
         * <p>
         * If this transaction succeeds, all subsequent transactions referencing
         * the deleted topic SHALL fail.<br/>
         * The `adminKey` MUST be set on the topic and this transaction MUST be
         * signed by that key.<br/>
         * If the `adminKey` is not set on the topic, this transaction SHALL fail
         * with a response code of `UNAUTHORIZED`. A topic without an `adminKey`
         * cannot be deleted, but MAY expire.<br/>
         * <p>
         * The request body MUST be a
         * [ConsensusDeleteTopicTransactionBody](#proto.ConsensusDeleteTopicTransactionBody)
         * @memberof proto.ConsensusService
         * @typedef deleteTopic
         * @type {{
         *   (request: proto.ITransaction, callback: proto.ConsensusService.deleteTopicCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "deleteTopic";
         *   readonly path: "/proto.ConsensusService/deleteTopic";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Delete an HCS topic.
         * <p>
         * If this transaction succeeds, all subsequent transactions referencing
         * the deleted topic SHALL fail.<br/>
         * The `adminKey` MUST be set on the topic and this transaction MUST be
         * signed by that key.<br/>
         * If the `adminKey` is not set on the topic, this transaction SHALL fail
         * with a response code of `UNAUTHORIZED`. A topic without an `adminKey`
         * cannot be deleted, but MAY expire.<br/>
         * <p>
         * The request body MUST be a
         * [ConsensusDeleteTopicTransactionBody](#proto.ConsensusDeleteTopicTransactionBody)
         * @name proto.ConsensusService#deleteTopic
         * @type {proto.ConsensusService.deleteTopic}
         */
        $Object.defineProperties(ConsensusService.prototype.deleteTopic = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, ConsensusService.prototype.deleteTopic, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "deleteTopic" },
            path: { value: "/proto.ConsensusService/deleteTopic" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.ConsensusService#submitMessage}.
         * @memberof proto.ConsensusService
         * @typedef submitMessageCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Submit a message to an HCS topic.
         * <p>
         * Valid and authorized messages on valid topics will be ordered by the
         * consensus service, published in the block stream, and available to all
         * subscribers on this topic via the mirror nodes.<br/>
         * If this transaction succeeds the resulting TransactionReceipt SHALL
         * contain the latest topicSequenceNumber and topicRunningHash for the
         * topic.<br/>
         * If the topic has a `submitKey` then that key MUST sign this
         * transaction.<br/>
         * <p>
         * The request body MUST be a
         * [ConsensusSubmitMessageTransactionBody](#proto.ConsensusSubmitMessageTransactionBody)
         * @memberof proto.ConsensusService
         * @typedef submitMessage
         * @type {{
         *   (request: proto.ITransaction, callback: proto.ConsensusService.submitMessageCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "submitMessage";
         *   readonly path: "/proto.ConsensusService/submitMessage";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Submit a message to an HCS topic.
         * <p>
         * Valid and authorized messages on valid topics will be ordered by the
         * consensus service, published in the block stream, and available to all
         * subscribers on this topic via the mirror nodes.<br/>
         * If this transaction succeeds the resulting TransactionReceipt SHALL
         * contain the latest topicSequenceNumber and topicRunningHash for the
         * topic.<br/>
         * If the topic has a `submitKey` then that key MUST sign this
         * transaction.<br/>
         * <p>
         * The request body MUST be a
         * [ConsensusSubmitMessageTransactionBody](#proto.ConsensusSubmitMessageTransactionBody)
         * @name proto.ConsensusService#submitMessage
         * @type {proto.ConsensusService.submitMessage}
         */
        $Object.defineProperties(ConsensusService.prototype.submitMessage = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, ConsensusService.prototype.submitMessage, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "submitMessage" },
            path: { value: "/proto.ConsensusService/submitMessage" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.ConsensusService#getTopicInfo}.
         * @memberof proto.ConsensusService
         * @typedef getTopicInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve the latest state of a topic. This method is unrestricted and
         * allowed on any topic by any payer account.
         * <p>
         * The request body MUST be a
         * [ConsensusGetTopicInfoQuery](#proto.ConsensusGetTopicInfoQuery)<br/>
         * The response body SHALL be a
         * [ConsensusGetTopicInfoResponse](#proto.ConsensusGetTopicInfoResponse)
         * @memberof proto.ConsensusService
         * @typedef getTopicInfo
         * @type {{
         *   (request: proto.IQuery, callback: proto.ConsensusService.getTopicInfoCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getTopicInfo";
         *   readonly path: "/proto.ConsensusService/getTopicInfo";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve the latest state of a topic. This method is unrestricted and
         * allowed on any topic by any payer account.
         * <p>
         * The request body MUST be a
         * [ConsensusGetTopicInfoQuery](#proto.ConsensusGetTopicInfoQuery)<br/>
         * The response body SHALL be a
         * [ConsensusGetTopicInfoResponse](#proto.ConsensusGetTopicInfoResponse)
         * @name proto.ConsensusService#getTopicInfo
         * @type {proto.ConsensusService.getTopicInfo}
         */
        $Object.defineProperties(ConsensusService.prototype.getTopicInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, ConsensusService.prototype.getTopicInfo, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getTopicInfo" },
            path: { value: "/proto.ConsensusService/getTopicInfo" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return ConsensusService;
    })();

    proto.Query = (function() {

        /**
         * Properties of a Query.
         * @typedef {Object} proto.Query.$Properties
         * @property {proto.GetByKeyQuery.$Properties|null} [getByKey] Get all entities associated with a given key.
         * @property {proto.GetBySolidityIDQuery.$Properties|null} [getBySolidityID] Get an Hedera identifier associated with an identifier in EVM
         * "Solidity" form.<br/>
         * Most often used in smart contracts to find an Hedera account,
         * file, or contract identifier to pass to a system contract or
         * precompile.
         * @property {proto.ContractCallLocalQuery.$Properties|null} [contractCallLocal] Call a function of a smart contract.<br/>
         * This call is executed exclusively on the node to which it is
         * submitted, and is much less expensive than a `contractCall`
         * transaction.
         * @property {proto.ContractGetInfoQuery.$Properties|null} [contractGetInfo] Get information about a smart contract.
         * @property {proto.ContractGetBytecodeQuery.$Properties|null} [contractGetBytecode] Get runtime bytecode used by a smart contract.
         * @property {proto.ContractGetRecordsQuery.$Properties|null} [ContractGetRecords] This query is unsupported and SHALL fail.<br/>
         * Requests for this information MUST be directed to a mirror node.
         * <p>
         * Get Records of a smart contract.
         * @property {proto.CryptoGetAccountBalanceQuery.$Properties|null} [cryptogetAccountBalance] Get the current HBAR balance of an Hedera account or smart contract.
         * @property {proto.CryptoGetAccountRecordsQuery.$Properties|null} [cryptoGetAccountRecords] Get records of all "recent" transactions for which a specified
         * account is the effective payer.
         * @property {proto.CryptoGetInfoQuery.$Properties|null} [cryptoGetInfo] Get information about an account, including the balance.<br/>
         * This does not get the list of account records.
         * @property {proto.CryptoGetLiveHashQuery.$Properties|null} [cryptoGetLiveHash] This query is unsupported and SHALL fail.<br/>
         * Requests for this information MUST be directed to a mirror node.
         * <p>
         * Get a single livehash from a single account, if present.
         * @property {proto.CryptoGetStakersQuery.$Properties|null} [cryptoGetProxyStakers] This query is unsupported and SHALL fail.<br/>
         * Requests for this information MUST be directed to a mirror node.
         * <p>
         * Get all the accounts that are proxy staking to this account.
         * @property {proto.FileGetContentsQuery.$Properties|null} [fileGetContents] Get the content of a file.
         * @property {proto.FileGetInfoQuery.$Properties|null} [fileGetInfo] Get metadata for a file.
         * @property {proto.TransactionGetReceiptQuery.$Properties|null} [transactionGetReceipt] Get a receipt for a transaction.<br/>
         * This only returns a receipt if the transaction is "recent", which
         * is typically within the previous 180 seconds (3 minutes).
         * @property {proto.TransactionGetRecordQuery.$Properties|null} [transactionGetRecord] Get a record for a transaction.
         * This only returns a record if the transaction is "available", which
         * is typically within the previous 1 hour.
         * @property {proto.TransactionGetFastRecordQuery.$Properties|null} [transactionGetFastRecord] Get a record for a transaction.
         * This only returns a record if the transaction is "recent", which
         * is typically within the previous 180 seconds (3 minutes).
         * @property {proto.ConsensusGetTopicInfoQuery.$Properties|null} [consensusGetTopicInfo] Get metadata for a consensus topic.
         * @property {proto.NetworkGetVersionInfoQuery.$Properties|null} [networkGetVersionInfo] Get the versions of Hedera Services and the HAPI API deployed on
         * the responding consensus node.
         * @property {proto.TokenGetInfoQuery.$Properties|null} [tokenGetInfo] Get metadata for a token.
         * @property {proto.ScheduleGetInfoQuery.$Properties|null} [scheduleGetInfo] Get metadata for a schedule.<br/>
         * A schedule is a request to execute a transaction at a future
         * time.
         * @property {proto.TokenGetAccountNftInfosQuery.$Properties|null} [tokenGetAccountNftInfos] This query is unsupported and SHALL fail.<br/>
         * Requests for this information MUST be directed to a mirror node.
         * <p>
         * Get a list of non-fungible/unique tokens associated with an account.
         * @property {proto.TokenGetNftInfoQuery.$Properties|null} [tokenGetNftInfo] Get metadata for a specific, serial numbered, non-fungible/unique
         * token (NFT).
         * @property {proto.TokenGetNftInfosQuery.$Properties|null} [tokenGetNftInfos] This query is unsupported and SHALL fail.<br/>
         * Requests for this information MUST be directed to a mirror node.
         * <p>
         * Get metadata for all non-fungible/unique tokens (NFTs) of a single
         * type within a range of indices (0-based count of minted tokens).
         * @property {proto.NetworkGetExecutionTimeQuery.$Properties|null} [networkGetExecutionTime] This query is unsupported and SHALL fail.<br/>
         * Requests for this information MUST be directed to a mirror node.
         * <p>
         * Get the execution time for a recent transaction.
         * @property {proto.GetAccountDetailsQuery.$Properties|null} [accountDetails] Get detail metadata for an account.
         * @property {"getByKey"|"getBySolidityID"|"contractCallLocal"|"contractGetInfo"|"contractGetBytecode"|"ContractGetRecords"|"cryptogetAccountBalance"|"cryptoGetAccountRecords"|"cryptoGetInfo"|"cryptoGetLiveHash"|"cryptoGetProxyStakers"|"fileGetContents"|"fileGetInfo"|"transactionGetReceipt"|"transactionGetRecord"|"transactionGetFastRecord"|"consensusGetTopicInfo"|"networkGetVersionInfo"|"tokenGetInfo"|"scheduleGetInfo"|"tokenGetAccountNftInfos"|"tokenGetNftInfo"|"tokenGetNftInfos"|"networkGetExecutionTime"|"accountDetails"} [query] Query query
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Query.
         * @memberof proto
         * @interface IQuery
         * @augments proto.Query.$Properties
         * @deprecated Use proto.Query.$Properties instead.
         */

        /**
         * Narrowed shape of a Query.
         * @typedef {{
         *   getByKey?: proto.GetByKeyQuery.$Shape|null;
         *   getBySolidityID?: proto.GetBySolidityIDQuery.$Shape|null;
         *   contractCallLocal?: proto.ContractCallLocalQuery.$Shape|null;
         *   contractGetInfo?: proto.ContractGetInfoQuery.$Shape|null;
         *   contractGetBytecode?: proto.ContractGetBytecodeQuery.$Shape|null;
         *   ContractGetRecords?: proto.ContractGetRecordsQuery.$Shape|null;
         *   cryptogetAccountBalance?: proto.CryptoGetAccountBalanceQuery.$Shape|null;
         *   cryptoGetAccountRecords?: proto.CryptoGetAccountRecordsQuery.$Shape|null;
         *   cryptoGetInfo?: proto.CryptoGetInfoQuery.$Shape|null;
         *   cryptoGetLiveHash?: proto.CryptoGetLiveHashQuery.$Shape|null;
         *   cryptoGetProxyStakers?: proto.CryptoGetStakersQuery.$Shape|null;
         *   fileGetContents?: proto.FileGetContentsQuery.$Shape|null;
         *   fileGetInfo?: proto.FileGetInfoQuery.$Shape|null;
         *   transactionGetReceipt?: proto.TransactionGetReceiptQuery.$Shape|null;
         *   transactionGetRecord?: proto.TransactionGetRecordQuery.$Shape|null;
         *   transactionGetFastRecord?: proto.TransactionGetFastRecordQuery.$Shape|null;
         *   consensusGetTopicInfo?: proto.ConsensusGetTopicInfoQuery.$Shape|null;
         *   networkGetVersionInfo?: proto.NetworkGetVersionInfoQuery.$Shape|null;
         *   tokenGetInfo?: proto.TokenGetInfoQuery.$Shape|null;
         *   scheduleGetInfo?: proto.ScheduleGetInfoQuery.$Shape|null;
         *   tokenGetAccountNftInfos?: proto.TokenGetAccountNftInfosQuery.$Shape|null;
         *   tokenGetNftInfo?: proto.TokenGetNftInfoQuery.$Shape|null;
         *   tokenGetNftInfos?: proto.TokenGetNftInfosQuery.$Shape|null;
         *   networkGetExecutionTime?: proto.NetworkGetExecutionTimeQuery.$Shape|null;
         *   accountDetails?: proto.GetAccountDetailsQuery.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ query?: undefined; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "getByKey"; getByKey: proto.GetByKeyQuery.$Shape; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "getBySolidityID"; getByKey?: null; getBySolidityID: proto.GetBySolidityIDQuery.$Shape; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "contractCallLocal"; getByKey?: null; getBySolidityID?: null; contractCallLocal: proto.ContractCallLocalQuery.$Shape; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "contractGetInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo: proto.ContractGetInfoQuery.$Shape; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "contractGetBytecode"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode: proto.ContractGetBytecodeQuery.$Shape; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "ContractGetRecords"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords: proto.ContractGetRecordsQuery.$Shape; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "cryptogetAccountBalance"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance: proto.CryptoGetAccountBalanceQuery.$Shape; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "cryptoGetAccountRecords"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords: proto.CryptoGetAccountRecordsQuery.$Shape; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "cryptoGetInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo: proto.CryptoGetInfoQuery.$Shape; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "cryptoGetLiveHash"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash: proto.CryptoGetLiveHashQuery.$Shape; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "cryptoGetProxyStakers"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers: proto.CryptoGetStakersQuery.$Shape; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "fileGetContents"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents: proto.FileGetContentsQuery.$Shape; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "fileGetInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo: proto.FileGetInfoQuery.$Shape; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "transactionGetReceipt"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt: proto.TransactionGetReceiptQuery.$Shape; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "transactionGetRecord"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord: proto.TransactionGetRecordQuery.$Shape; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "transactionGetFastRecord"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord: proto.TransactionGetFastRecordQuery.$Shape; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "consensusGetTopicInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo: proto.ConsensusGetTopicInfoQuery.$Shape; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "networkGetVersionInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo: proto.NetworkGetVersionInfoQuery.$Shape; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "tokenGetInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo: proto.TokenGetInfoQuery.$Shape; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "scheduleGetInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo: proto.ScheduleGetInfoQuery.$Shape; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "tokenGetAccountNftInfos"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos: proto.TokenGetAccountNftInfosQuery.$Shape; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "tokenGetNftInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo: proto.TokenGetNftInfoQuery.$Shape; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "tokenGetNftInfos"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos: proto.TokenGetNftInfosQuery.$Shape; networkGetExecutionTime?: null; accountDetails?: null }|{ query?: "networkGetExecutionTime"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime: proto.NetworkGetExecutionTimeQuery.$Shape; accountDetails?: null }|{ query?: "accountDetails"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetInfo?: null; contractGetBytecode?: null; ContractGetRecords?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails: proto.GetAccountDetailsQuery.$Shape })
         * )} proto.Query.$Shape
         */

        /**
         * Constructs a new Query.
         * @memberof proto
         * @classdesc A query transaction.<br/>
         * This message is serialized to bytes and those bytes are signed by the
         * submitter, with the signature included in the QueryHeader for the query
         * request.
         * @constructor
         * @param {proto.Query.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Query = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Get all entities associated with a given key.
         * @member {proto.GetByKeyQuery.$Properties|null|undefined} getByKey
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.getByKey = null;

        /**
         * Get an Hedera identifier associated with an identifier in EVM
         * "Solidity" form.<br/>
         * Most often used in smart contracts to find an Hedera account,
         * file, or contract identifier to pass to a system contract or
         * precompile.
         * @member {proto.GetBySolidityIDQuery.$Properties|null|undefined} getBySolidityID
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.getBySolidityID = null;

        /**
         * Call a function of a smart contract.<br/>
         * This call is executed exclusively on the node to which it is
         * submitted, and is much less expensive than a `contractCall`
         * transaction.
         * @member {proto.ContractCallLocalQuery.$Properties|null|undefined} contractCallLocal
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.contractCallLocal = null;

        /**
         * Get information about a smart contract.
         * @member {proto.ContractGetInfoQuery.$Properties|null|undefined} contractGetInfo
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.contractGetInfo = null;

        /**
         * Get runtime bytecode used by a smart contract.
         * @member {proto.ContractGetBytecodeQuery.$Properties|null|undefined} contractGetBytecode
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.contractGetBytecode = null;

        /**
         * This query is unsupported and SHALL fail.<br/>
         * Requests for this information MUST be directed to a mirror node.
         * <p>
         * Get Records of a smart contract.
         * @member {proto.ContractGetRecordsQuery.$Properties|null|undefined} ContractGetRecords
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.ContractGetRecords = null;

        /**
         * Get the current HBAR balance of an Hedera account or smart contract.
         * @member {proto.CryptoGetAccountBalanceQuery.$Properties|null|undefined} cryptogetAccountBalance
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.cryptogetAccountBalance = null;

        /**
         * Get records of all "recent" transactions for which a specified
         * account is the effective payer.
         * @member {proto.CryptoGetAccountRecordsQuery.$Properties|null|undefined} cryptoGetAccountRecords
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.cryptoGetAccountRecords = null;

        /**
         * Get information about an account, including the balance.<br/>
         * This does not get the list of account records.
         * @member {proto.CryptoGetInfoQuery.$Properties|null|undefined} cryptoGetInfo
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.cryptoGetInfo = null;

        /**
         * This query is unsupported and SHALL fail.<br/>
         * Requests for this information MUST be directed to a mirror node.
         * <p>
         * Get a single livehash from a single account, if present.
         * @member {proto.CryptoGetLiveHashQuery.$Properties|null|undefined} cryptoGetLiveHash
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.cryptoGetLiveHash = null;

        /**
         * This query is unsupported and SHALL fail.<br/>
         * Requests for this information MUST be directed to a mirror node.
         * <p>
         * Get all the accounts that are proxy staking to this account.
         * @member {proto.CryptoGetStakersQuery.$Properties|null|undefined} cryptoGetProxyStakers
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.cryptoGetProxyStakers = null;

        /**
         * Get the content of a file.
         * @member {proto.FileGetContentsQuery.$Properties|null|undefined} fileGetContents
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.fileGetContents = null;

        /**
         * Get metadata for a file.
         * @member {proto.FileGetInfoQuery.$Properties|null|undefined} fileGetInfo
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.fileGetInfo = null;

        /**
         * Get a receipt for a transaction.<br/>
         * This only returns a receipt if the transaction is "recent", which
         * is typically within the previous 180 seconds (3 minutes).
         * @member {proto.TransactionGetReceiptQuery.$Properties|null|undefined} transactionGetReceipt
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.transactionGetReceipt = null;

        /**
         * Get a record for a transaction.
         * This only returns a record if the transaction is "available", which
         * is typically within the previous 1 hour.
         * @member {proto.TransactionGetRecordQuery.$Properties|null|undefined} transactionGetRecord
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.transactionGetRecord = null;

        /**
         * Get a record for a transaction.
         * This only returns a record if the transaction is "recent", which
         * is typically within the previous 180 seconds (3 minutes).
         * @member {proto.TransactionGetFastRecordQuery.$Properties|null|undefined} transactionGetFastRecord
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.transactionGetFastRecord = null;

        /**
         * Get metadata for a consensus topic.
         * @member {proto.ConsensusGetTopicInfoQuery.$Properties|null|undefined} consensusGetTopicInfo
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.consensusGetTopicInfo = null;

        /**
         * Get the versions of Hedera Services and the HAPI API deployed on
         * the responding consensus node.
         * @member {proto.NetworkGetVersionInfoQuery.$Properties|null|undefined} networkGetVersionInfo
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.networkGetVersionInfo = null;

        /**
         * Get metadata for a token.
         * @member {proto.TokenGetInfoQuery.$Properties|null|undefined} tokenGetInfo
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.tokenGetInfo = null;

        /**
         * Get metadata for a schedule.<br/>
         * A schedule is a request to execute a transaction at a future
         * time.
         * @member {proto.ScheduleGetInfoQuery.$Properties|null|undefined} scheduleGetInfo
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.scheduleGetInfo = null;

        /**
         * This query is unsupported and SHALL fail.<br/>
         * Requests for this information MUST be directed to a mirror node.
         * <p>
         * Get a list of non-fungible/unique tokens associated with an account.
         * @member {proto.TokenGetAccountNftInfosQuery.$Properties|null|undefined} tokenGetAccountNftInfos
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.tokenGetAccountNftInfos = null;

        /**
         * Get metadata for a specific, serial numbered, non-fungible/unique
         * token (NFT).
         * @member {proto.TokenGetNftInfoQuery.$Properties|null|undefined} tokenGetNftInfo
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.tokenGetNftInfo = null;

        /**
         * This query is unsupported and SHALL fail.<br/>
         * Requests for this information MUST be directed to a mirror node.
         * <p>
         * Get metadata for all non-fungible/unique tokens (NFTs) of a single
         * type within a range of indices (0-based count of minted tokens).
         * @member {proto.TokenGetNftInfosQuery.$Properties|null|undefined} tokenGetNftInfos
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.tokenGetNftInfos = null;

        /**
         * This query is unsupported and SHALL fail.<br/>
         * Requests for this information MUST be directed to a mirror node.
         * <p>
         * Get the execution time for a recent transaction.
         * @member {proto.NetworkGetExecutionTimeQuery.$Properties|null|undefined} networkGetExecutionTime
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.networkGetExecutionTime = null;

        /**
         * Get detail metadata for an account.
         * @member {proto.GetAccountDetailsQuery.$Properties|null|undefined} accountDetails
         * @memberof proto.Query
         * @instance
         */
        Query.prototype.accountDetails = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * Query query.
         * @member {"getByKey"|"getBySolidityID"|"contractCallLocal"|"contractGetInfo"|"contractGetBytecode"|"ContractGetRecords"|"cryptogetAccountBalance"|"cryptoGetAccountRecords"|"cryptoGetInfo"|"cryptoGetLiveHash"|"cryptoGetProxyStakers"|"fileGetContents"|"fileGetInfo"|"transactionGetReceipt"|"transactionGetRecord"|"transactionGetFastRecord"|"consensusGetTopicInfo"|"networkGetVersionInfo"|"tokenGetInfo"|"scheduleGetInfo"|"tokenGetAccountNftInfos"|"tokenGetNftInfo"|"tokenGetNftInfos"|"networkGetExecutionTime"|"accountDetails"|undefined} query
         * @memberof proto.Query
         * @instance
         */
        $Object.defineProperty(Query.prototype, "query", {
            get: $util.oneOfGetter($oneOfFields = ["getByKey", "getBySolidityID", "contractCallLocal", "contractGetInfo", "contractGetBytecode", "ContractGetRecords", "cryptogetAccountBalance", "cryptoGetAccountRecords", "cryptoGetInfo", "cryptoGetLiveHash", "cryptoGetProxyStakers", "fileGetContents", "fileGetInfo", "transactionGetReceipt", "transactionGetRecord", "transactionGetFastRecord", "consensusGetTopicInfo", "networkGetVersionInfo", "tokenGetInfo", "scheduleGetInfo", "tokenGetAccountNftInfos", "tokenGetNftInfo", "tokenGetNftInfos", "networkGetExecutionTime", "accountDetails"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Query instance using the specified properties.
         * @function create
         * @memberof proto.Query
         * @static
         * @param {proto.Query.$Properties=} [properties] Properties to set
         * @returns {proto.Query} Query instance
         * @type {{
         *   (properties: proto.Query.$Shape): proto.Query & proto.Query.$Shape;
         *   (properties?: proto.Query.$Properties): proto.Query;
         * }}
         */
        Query.create = function(properties) {
            return new Query(properties);
        };

        /**
         * Encodes the specified Query message. Does not implicitly {@link proto.Query.verify|verify} messages.
         * @function encode
         * @memberof proto.Query
         * @static
         * @param {proto.Query.$Properties} message Query message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Query.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.getByKey != null && $Object.hasOwnProperty.call(message, "getByKey"))
                $root.proto.GetByKeyQuery.encode(message.getByKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.getBySolidityID != null && $Object.hasOwnProperty.call(message, "getBySolidityID"))
                $root.proto.GetBySolidityIDQuery.encode(message.getBySolidityID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.contractCallLocal != null && $Object.hasOwnProperty.call(message, "contractCallLocal"))
                $root.proto.ContractCallLocalQuery.encode(message.contractCallLocal, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.contractGetInfo != null && $Object.hasOwnProperty.call(message, "contractGetInfo"))
                $root.proto.ContractGetInfoQuery.encode(message.contractGetInfo, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.contractGetBytecode != null && $Object.hasOwnProperty.call(message, "contractGetBytecode"))
                $root.proto.ContractGetBytecodeQuery.encode(message.contractGetBytecode, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.ContractGetRecords != null && $Object.hasOwnProperty.call(message, "ContractGetRecords"))
                $root.proto.ContractGetRecordsQuery.encode(message.ContractGetRecords, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.cryptogetAccountBalance != null && $Object.hasOwnProperty.call(message, "cryptogetAccountBalance"))
                $root.proto.CryptoGetAccountBalanceQuery.encode(message.cryptogetAccountBalance, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.cryptoGetAccountRecords != null && $Object.hasOwnProperty.call(message, "cryptoGetAccountRecords"))
                $root.proto.CryptoGetAccountRecordsQuery.encode(message.cryptoGetAccountRecords, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.cryptoGetInfo != null && $Object.hasOwnProperty.call(message, "cryptoGetInfo"))
                $root.proto.CryptoGetInfoQuery.encode(message.cryptoGetInfo, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.cryptoGetLiveHash != null && $Object.hasOwnProperty.call(message, "cryptoGetLiveHash"))
                $root.proto.CryptoGetLiveHashQuery.encode(message.cryptoGetLiveHash, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.cryptoGetProxyStakers != null && $Object.hasOwnProperty.call(message, "cryptoGetProxyStakers"))
                $root.proto.CryptoGetStakersQuery.encode(message.cryptoGetProxyStakers, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.fileGetContents != null && $Object.hasOwnProperty.call(message, "fileGetContents"))
                $root.proto.FileGetContentsQuery.encode(message.fileGetContents, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.fileGetInfo != null && $Object.hasOwnProperty.call(message, "fileGetInfo"))
                $root.proto.FileGetInfoQuery.encode(message.fileGetInfo, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.transactionGetReceipt != null && $Object.hasOwnProperty.call(message, "transactionGetReceipt"))
                $root.proto.TransactionGetReceiptQuery.encode(message.transactionGetReceipt, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.transactionGetRecord != null && $Object.hasOwnProperty.call(message, "transactionGetRecord"))
                $root.proto.TransactionGetRecordQuery.encode(message.transactionGetRecord, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.transactionGetFastRecord != null && $Object.hasOwnProperty.call(message, "transactionGetFastRecord"))
                $root.proto.TransactionGetFastRecordQuery.encode(message.transactionGetFastRecord, writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.consensusGetTopicInfo != null && $Object.hasOwnProperty.call(message, "consensusGetTopicInfo"))
                $root.proto.ConsensusGetTopicInfoQuery.encode(message.consensusGetTopicInfo, writer.uint32(/* id 50, wireType 2 =*/402).fork(), _depth + 1).ldelim();
            if (message.networkGetVersionInfo != null && $Object.hasOwnProperty.call(message, "networkGetVersionInfo"))
                $root.proto.NetworkGetVersionInfoQuery.encode(message.networkGetVersionInfo, writer.uint32(/* id 51, wireType 2 =*/410).fork(), _depth + 1).ldelim();
            if (message.tokenGetInfo != null && $Object.hasOwnProperty.call(message, "tokenGetInfo"))
                $root.proto.TokenGetInfoQuery.encode(message.tokenGetInfo, writer.uint32(/* id 52, wireType 2 =*/418).fork(), _depth + 1).ldelim();
            if (message.scheduleGetInfo != null && $Object.hasOwnProperty.call(message, "scheduleGetInfo"))
                $root.proto.ScheduleGetInfoQuery.encode(message.scheduleGetInfo, writer.uint32(/* id 53, wireType 2 =*/426).fork(), _depth + 1).ldelim();
            if (message.tokenGetAccountNftInfos != null && $Object.hasOwnProperty.call(message, "tokenGetAccountNftInfos"))
                $root.proto.TokenGetAccountNftInfosQuery.encode(message.tokenGetAccountNftInfos, writer.uint32(/* id 54, wireType 2 =*/434).fork(), _depth + 1).ldelim();
            if (message.tokenGetNftInfo != null && $Object.hasOwnProperty.call(message, "tokenGetNftInfo"))
                $root.proto.TokenGetNftInfoQuery.encode(message.tokenGetNftInfo, writer.uint32(/* id 55, wireType 2 =*/442).fork(), _depth + 1).ldelim();
            if (message.tokenGetNftInfos != null && $Object.hasOwnProperty.call(message, "tokenGetNftInfos"))
                $root.proto.TokenGetNftInfosQuery.encode(message.tokenGetNftInfos, writer.uint32(/* id 56, wireType 2 =*/450).fork(), _depth + 1).ldelim();
            if (message.networkGetExecutionTime != null && $Object.hasOwnProperty.call(message, "networkGetExecutionTime"))
                $root.proto.NetworkGetExecutionTimeQuery.encode(message.networkGetExecutionTime, writer.uint32(/* id 57, wireType 2 =*/458).fork(), _depth + 1).ldelim();
            if (message.accountDetails != null && $Object.hasOwnProperty.call(message, "accountDetails"))
                $root.proto.GetAccountDetailsQuery.encode(message.accountDetails, writer.uint32(/* id 58, wireType 2 =*/466).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Query message, length delimited. Does not implicitly {@link proto.Query.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Query
         * @static
         * @param {proto.Query.$Properties} message Query message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Query.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Query message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Query
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Query & proto.Query.$Shape} Query
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Query.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Query();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.getByKey = $root.proto.GetByKeyQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.getByKey);
                        message.query = "getByKey";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.getBySolidityID = $root.proto.GetBySolidityIDQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.getBySolidityID);
                        message.query = "getBySolidityID";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.contractCallLocal = $root.proto.ContractCallLocalQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractCallLocal);
                        message.query = "contractCallLocal";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.contractGetInfo = $root.proto.ContractGetInfoQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractGetInfo);
                        message.query = "contractGetInfo";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.contractGetBytecode = $root.proto.ContractGetBytecodeQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractGetBytecode);
                        message.query = "contractGetBytecode";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.ContractGetRecords = $root.proto.ContractGetRecordsQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ContractGetRecords);
                        message.query = "ContractGetRecords";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.cryptogetAccountBalance = $root.proto.CryptoGetAccountBalanceQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptogetAccountBalance);
                        message.query = "cryptogetAccountBalance";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.cryptoGetAccountRecords = $root.proto.CryptoGetAccountRecordsQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoGetAccountRecords);
                        message.query = "cryptoGetAccountRecords";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.cryptoGetInfo = $root.proto.CryptoGetInfoQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoGetInfo);
                        message.query = "cryptoGetInfo";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.cryptoGetLiveHash = $root.proto.CryptoGetLiveHashQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoGetLiveHash);
                        message.query = "cryptoGetLiveHash";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.cryptoGetProxyStakers = $root.proto.CryptoGetStakersQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoGetProxyStakers);
                        message.query = "cryptoGetProxyStakers";
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.fileGetContents = $root.proto.FileGetContentsQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileGetContents);
                        message.query = "fileGetContents";
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.fileGetInfo = $root.proto.FileGetInfoQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileGetInfo);
                        message.query = "fileGetInfo";
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.transactionGetReceipt = $root.proto.TransactionGetReceiptQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionGetReceipt);
                        message.query = "transactionGetReceipt";
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.transactionGetRecord = $root.proto.TransactionGetRecordQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionGetRecord);
                        message.query = "transactionGetRecord";
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.transactionGetFastRecord = $root.proto.TransactionGetFastRecordQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionGetFastRecord);
                        message.query = "transactionGetFastRecord";
                        continue;
                    }
                case 50: {
                        if (wireType !== 2)
                            break;
                        message.consensusGetTopicInfo = $root.proto.ConsensusGetTopicInfoQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusGetTopicInfo);
                        message.query = "consensusGetTopicInfo";
                        continue;
                    }
                case 51: {
                        if (wireType !== 2)
                            break;
                        message.networkGetVersionInfo = $root.proto.NetworkGetVersionInfoQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.networkGetVersionInfo);
                        message.query = "networkGetVersionInfo";
                        continue;
                    }
                case 52: {
                        if (wireType !== 2)
                            break;
                        message.tokenGetInfo = $root.proto.TokenGetInfoQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenGetInfo);
                        message.query = "tokenGetInfo";
                        continue;
                    }
                case 53: {
                        if (wireType !== 2)
                            break;
                        message.scheduleGetInfo = $root.proto.ScheduleGetInfoQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleGetInfo);
                        message.query = "scheduleGetInfo";
                        continue;
                    }
                case 54: {
                        if (wireType !== 2)
                            break;
                        message.tokenGetAccountNftInfos = $root.proto.TokenGetAccountNftInfosQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenGetAccountNftInfos);
                        message.query = "tokenGetAccountNftInfos";
                        continue;
                    }
                case 55: {
                        if (wireType !== 2)
                            break;
                        message.tokenGetNftInfo = $root.proto.TokenGetNftInfoQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenGetNftInfo);
                        message.query = "tokenGetNftInfo";
                        continue;
                    }
                case 56: {
                        if (wireType !== 2)
                            break;
                        message.tokenGetNftInfos = $root.proto.TokenGetNftInfosQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenGetNftInfos);
                        message.query = "tokenGetNftInfos";
                        continue;
                    }
                case 57: {
                        if (wireType !== 2)
                            break;
                        message.networkGetExecutionTime = $root.proto.NetworkGetExecutionTimeQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.networkGetExecutionTime);
                        message.query = "networkGetExecutionTime";
                        continue;
                    }
                case 58: {
                        if (wireType !== 2)
                            break;
                        message.accountDetails = $root.proto.GetAccountDetailsQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountDetails);
                        message.query = "accountDetails";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Query message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Query
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Query & proto.Query.$Shape} Query
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Query.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Query message.
         * @function verify
         * @memberof proto.Query
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Query.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.getByKey != null && $Object.hasOwnProperty.call(message, "getByKey")) {
                properties.query = 1;
                {
                    let error = $root.proto.GetByKeyQuery.verify(message.getByKey, _depth + 1);
                    if (error)
                        return "getByKey." + error;
                }
            }
            if (message.getBySolidityID != null && $Object.hasOwnProperty.call(message, "getBySolidityID")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.GetBySolidityIDQuery.verify(message.getBySolidityID, _depth + 1);
                    if (error)
                        return "getBySolidityID." + error;
                }
            }
            if (message.contractCallLocal != null && $Object.hasOwnProperty.call(message, "contractCallLocal")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.ContractCallLocalQuery.verify(message.contractCallLocal, _depth + 1);
                    if (error)
                        return "contractCallLocal." + error;
                }
            }
            if (message.contractGetInfo != null && $Object.hasOwnProperty.call(message, "contractGetInfo")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.ContractGetInfoQuery.verify(message.contractGetInfo, _depth + 1);
                    if (error)
                        return "contractGetInfo." + error;
                }
            }
            if (message.contractGetBytecode != null && $Object.hasOwnProperty.call(message, "contractGetBytecode")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.ContractGetBytecodeQuery.verify(message.contractGetBytecode, _depth + 1);
                    if (error)
                        return "contractGetBytecode." + error;
                }
            }
            if (message.ContractGetRecords != null && $Object.hasOwnProperty.call(message, "ContractGetRecords")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.ContractGetRecordsQuery.verify(message.ContractGetRecords, _depth + 1);
                    if (error)
                        return "ContractGetRecords." + error;
                }
            }
            if (message.cryptogetAccountBalance != null && $Object.hasOwnProperty.call(message, "cryptogetAccountBalance")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.CryptoGetAccountBalanceQuery.verify(message.cryptogetAccountBalance, _depth + 1);
                    if (error)
                        return "cryptogetAccountBalance." + error;
                }
            }
            if (message.cryptoGetAccountRecords != null && $Object.hasOwnProperty.call(message, "cryptoGetAccountRecords")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.CryptoGetAccountRecordsQuery.verify(message.cryptoGetAccountRecords, _depth + 1);
                    if (error)
                        return "cryptoGetAccountRecords." + error;
                }
            }
            if (message.cryptoGetInfo != null && $Object.hasOwnProperty.call(message, "cryptoGetInfo")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.CryptoGetInfoQuery.verify(message.cryptoGetInfo, _depth + 1);
                    if (error)
                        return "cryptoGetInfo." + error;
                }
            }
            if (message.cryptoGetLiveHash != null && $Object.hasOwnProperty.call(message, "cryptoGetLiveHash")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.CryptoGetLiveHashQuery.verify(message.cryptoGetLiveHash, _depth + 1);
                    if (error)
                        return "cryptoGetLiveHash." + error;
                }
            }
            if (message.cryptoGetProxyStakers != null && $Object.hasOwnProperty.call(message, "cryptoGetProxyStakers")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.CryptoGetStakersQuery.verify(message.cryptoGetProxyStakers, _depth + 1);
                    if (error)
                        return "cryptoGetProxyStakers." + error;
                }
            }
            if (message.fileGetContents != null && $Object.hasOwnProperty.call(message, "fileGetContents")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.FileGetContentsQuery.verify(message.fileGetContents, _depth + 1);
                    if (error)
                        return "fileGetContents." + error;
                }
            }
            if (message.fileGetInfo != null && $Object.hasOwnProperty.call(message, "fileGetInfo")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.FileGetInfoQuery.verify(message.fileGetInfo, _depth + 1);
                    if (error)
                        return "fileGetInfo." + error;
                }
            }
            if (message.transactionGetReceipt != null && $Object.hasOwnProperty.call(message, "transactionGetReceipt")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.TransactionGetReceiptQuery.verify(message.transactionGetReceipt, _depth + 1);
                    if (error)
                        return "transactionGetReceipt." + error;
                }
            }
            if (message.transactionGetRecord != null && $Object.hasOwnProperty.call(message, "transactionGetRecord")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.TransactionGetRecordQuery.verify(message.transactionGetRecord, _depth + 1);
                    if (error)
                        return "transactionGetRecord." + error;
                }
            }
            if (message.transactionGetFastRecord != null && $Object.hasOwnProperty.call(message, "transactionGetFastRecord")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.TransactionGetFastRecordQuery.verify(message.transactionGetFastRecord, _depth + 1);
                    if (error)
                        return "transactionGetFastRecord." + error;
                }
            }
            if (message.consensusGetTopicInfo != null && $Object.hasOwnProperty.call(message, "consensusGetTopicInfo")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.ConsensusGetTopicInfoQuery.verify(message.consensusGetTopicInfo, _depth + 1);
                    if (error)
                        return "consensusGetTopicInfo." + error;
                }
            }
            if (message.networkGetVersionInfo != null && $Object.hasOwnProperty.call(message, "networkGetVersionInfo")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.NetworkGetVersionInfoQuery.verify(message.networkGetVersionInfo, _depth + 1);
                    if (error)
                        return "networkGetVersionInfo." + error;
                }
            }
            if (message.tokenGetInfo != null && $Object.hasOwnProperty.call(message, "tokenGetInfo")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.TokenGetInfoQuery.verify(message.tokenGetInfo, _depth + 1);
                    if (error)
                        return "tokenGetInfo." + error;
                }
            }
            if (message.scheduleGetInfo != null && $Object.hasOwnProperty.call(message, "scheduleGetInfo")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.ScheduleGetInfoQuery.verify(message.scheduleGetInfo, _depth + 1);
                    if (error)
                        return "scheduleGetInfo." + error;
                }
            }
            if (message.tokenGetAccountNftInfos != null && $Object.hasOwnProperty.call(message, "tokenGetAccountNftInfos")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.TokenGetAccountNftInfosQuery.verify(message.tokenGetAccountNftInfos, _depth + 1);
                    if (error)
                        return "tokenGetAccountNftInfos." + error;
                }
            }
            if (message.tokenGetNftInfo != null && $Object.hasOwnProperty.call(message, "tokenGetNftInfo")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.TokenGetNftInfoQuery.verify(message.tokenGetNftInfo, _depth + 1);
                    if (error)
                        return "tokenGetNftInfo." + error;
                }
            }
            if (message.tokenGetNftInfos != null && $Object.hasOwnProperty.call(message, "tokenGetNftInfos")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.TokenGetNftInfosQuery.verify(message.tokenGetNftInfos, _depth + 1);
                    if (error)
                        return "tokenGetNftInfos." + error;
                }
            }
            if (message.networkGetExecutionTime != null && $Object.hasOwnProperty.call(message, "networkGetExecutionTime")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.NetworkGetExecutionTimeQuery.verify(message.networkGetExecutionTime, _depth + 1);
                    if (error)
                        return "networkGetExecutionTime." + error;
                }
            }
            if (message.accountDetails != null && $Object.hasOwnProperty.call(message, "accountDetails")) {
                if (properties.query === 1)
                    return "query: multiple values";
                properties.query = 1;
                {
                    let error = $root.proto.GetAccountDetailsQuery.verify(message.accountDetails, _depth + 1);
                    if (error)
                        return "accountDetails." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Query message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Query
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Query} Query
         */
        Query.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Query)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Query: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Query();
            if (object.getByKey != null) {
                if (!$util.isObject(object.getByKey))
                    throw $TypeError(".proto.Query.getByKey: object expected");
                message.getByKey = $root.proto.GetByKeyQuery.fromObject(object.getByKey, _depth + 1);
            }
            if (object.getBySolidityID != null) {
                if (!$util.isObject(object.getBySolidityID))
                    throw $TypeError(".proto.Query.getBySolidityID: object expected");
                message.getBySolidityID = $root.proto.GetBySolidityIDQuery.fromObject(object.getBySolidityID, _depth + 1);
            }
            if (object.contractCallLocal != null) {
                if (!$util.isObject(object.contractCallLocal))
                    throw $TypeError(".proto.Query.contractCallLocal: object expected");
                message.contractCallLocal = $root.proto.ContractCallLocalQuery.fromObject(object.contractCallLocal, _depth + 1);
            }
            if (object.contractGetInfo != null) {
                if (!$util.isObject(object.contractGetInfo))
                    throw $TypeError(".proto.Query.contractGetInfo: object expected");
                message.contractGetInfo = $root.proto.ContractGetInfoQuery.fromObject(object.contractGetInfo, _depth + 1);
            }
            if (object.contractGetBytecode != null) {
                if (!$util.isObject(object.contractGetBytecode))
                    throw $TypeError(".proto.Query.contractGetBytecode: object expected");
                message.contractGetBytecode = $root.proto.ContractGetBytecodeQuery.fromObject(object.contractGetBytecode, _depth + 1);
            }
            if (object.ContractGetRecords != null) {
                if (!$util.isObject(object.ContractGetRecords))
                    throw $TypeError(".proto.Query.ContractGetRecords: object expected");
                message.ContractGetRecords = $root.proto.ContractGetRecordsQuery.fromObject(object.ContractGetRecords, _depth + 1);
            }
            if (object.cryptogetAccountBalance != null) {
                if (!$util.isObject(object.cryptogetAccountBalance))
                    throw $TypeError(".proto.Query.cryptogetAccountBalance: object expected");
                message.cryptogetAccountBalance = $root.proto.CryptoGetAccountBalanceQuery.fromObject(object.cryptogetAccountBalance, _depth + 1);
            }
            if (object.cryptoGetAccountRecords != null) {
                if (!$util.isObject(object.cryptoGetAccountRecords))
                    throw $TypeError(".proto.Query.cryptoGetAccountRecords: object expected");
                message.cryptoGetAccountRecords = $root.proto.CryptoGetAccountRecordsQuery.fromObject(object.cryptoGetAccountRecords, _depth + 1);
            }
            if (object.cryptoGetInfo != null) {
                if (!$util.isObject(object.cryptoGetInfo))
                    throw $TypeError(".proto.Query.cryptoGetInfo: object expected");
                message.cryptoGetInfo = $root.proto.CryptoGetInfoQuery.fromObject(object.cryptoGetInfo, _depth + 1);
            }
            if (object.cryptoGetLiveHash != null) {
                if (!$util.isObject(object.cryptoGetLiveHash))
                    throw $TypeError(".proto.Query.cryptoGetLiveHash: object expected");
                message.cryptoGetLiveHash = $root.proto.CryptoGetLiveHashQuery.fromObject(object.cryptoGetLiveHash, _depth + 1);
            }
            if (object.cryptoGetProxyStakers != null) {
                if (!$util.isObject(object.cryptoGetProxyStakers))
                    throw $TypeError(".proto.Query.cryptoGetProxyStakers: object expected");
                message.cryptoGetProxyStakers = $root.proto.CryptoGetStakersQuery.fromObject(object.cryptoGetProxyStakers, _depth + 1);
            }
            if (object.fileGetContents != null) {
                if (!$util.isObject(object.fileGetContents))
                    throw $TypeError(".proto.Query.fileGetContents: object expected");
                message.fileGetContents = $root.proto.FileGetContentsQuery.fromObject(object.fileGetContents, _depth + 1);
            }
            if (object.fileGetInfo != null) {
                if (!$util.isObject(object.fileGetInfo))
                    throw $TypeError(".proto.Query.fileGetInfo: object expected");
                message.fileGetInfo = $root.proto.FileGetInfoQuery.fromObject(object.fileGetInfo, _depth + 1);
            }
            if (object.transactionGetReceipt != null) {
                if (!$util.isObject(object.transactionGetReceipt))
                    throw $TypeError(".proto.Query.transactionGetReceipt: object expected");
                message.transactionGetReceipt = $root.proto.TransactionGetReceiptQuery.fromObject(object.transactionGetReceipt, _depth + 1);
            }
            if (object.transactionGetRecord != null) {
                if (!$util.isObject(object.transactionGetRecord))
                    throw $TypeError(".proto.Query.transactionGetRecord: object expected");
                message.transactionGetRecord = $root.proto.TransactionGetRecordQuery.fromObject(object.transactionGetRecord, _depth + 1);
            }
            if (object.transactionGetFastRecord != null) {
                if (!$util.isObject(object.transactionGetFastRecord))
                    throw $TypeError(".proto.Query.transactionGetFastRecord: object expected");
                message.transactionGetFastRecord = $root.proto.TransactionGetFastRecordQuery.fromObject(object.transactionGetFastRecord, _depth + 1);
            }
            if (object.consensusGetTopicInfo != null) {
                if (!$util.isObject(object.consensusGetTopicInfo))
                    throw $TypeError(".proto.Query.consensusGetTopicInfo: object expected");
                message.consensusGetTopicInfo = $root.proto.ConsensusGetTopicInfoQuery.fromObject(object.consensusGetTopicInfo, _depth + 1);
            }
            if (object.networkGetVersionInfo != null) {
                if (!$util.isObject(object.networkGetVersionInfo))
                    throw $TypeError(".proto.Query.networkGetVersionInfo: object expected");
                message.networkGetVersionInfo = $root.proto.NetworkGetVersionInfoQuery.fromObject(object.networkGetVersionInfo, _depth + 1);
            }
            if (object.tokenGetInfo != null) {
                if (!$util.isObject(object.tokenGetInfo))
                    throw $TypeError(".proto.Query.tokenGetInfo: object expected");
                message.tokenGetInfo = $root.proto.TokenGetInfoQuery.fromObject(object.tokenGetInfo, _depth + 1);
            }
            if (object.scheduleGetInfo != null) {
                if (!$util.isObject(object.scheduleGetInfo))
                    throw $TypeError(".proto.Query.scheduleGetInfo: object expected");
                message.scheduleGetInfo = $root.proto.ScheduleGetInfoQuery.fromObject(object.scheduleGetInfo, _depth + 1);
            }
            if (object.tokenGetAccountNftInfos != null) {
                if (!$util.isObject(object.tokenGetAccountNftInfos))
                    throw $TypeError(".proto.Query.tokenGetAccountNftInfos: object expected");
                message.tokenGetAccountNftInfos = $root.proto.TokenGetAccountNftInfosQuery.fromObject(object.tokenGetAccountNftInfos, _depth + 1);
            }
            if (object.tokenGetNftInfo != null) {
                if (!$util.isObject(object.tokenGetNftInfo))
                    throw $TypeError(".proto.Query.tokenGetNftInfo: object expected");
                message.tokenGetNftInfo = $root.proto.TokenGetNftInfoQuery.fromObject(object.tokenGetNftInfo, _depth + 1);
            }
            if (object.tokenGetNftInfos != null) {
                if (!$util.isObject(object.tokenGetNftInfos))
                    throw $TypeError(".proto.Query.tokenGetNftInfos: object expected");
                message.tokenGetNftInfos = $root.proto.TokenGetNftInfosQuery.fromObject(object.tokenGetNftInfos, _depth + 1);
            }
            if (object.networkGetExecutionTime != null) {
                if (!$util.isObject(object.networkGetExecutionTime))
                    throw $TypeError(".proto.Query.networkGetExecutionTime: object expected");
                message.networkGetExecutionTime = $root.proto.NetworkGetExecutionTimeQuery.fromObject(object.networkGetExecutionTime, _depth + 1);
            }
            if (object.accountDetails != null) {
                if (!$util.isObject(object.accountDetails))
                    throw $TypeError(".proto.Query.accountDetails: object expected");
                message.accountDetails = $root.proto.GetAccountDetailsQuery.fromObject(object.accountDetails, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Query message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Query
         * @static
         * @param {proto.Query} message Query
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Query.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.getByKey != null && $Object.hasOwnProperty.call(message, "getByKey")) {
                object.getByKey = $root.proto.GetByKeyQuery.toObject(message.getByKey, options, _depth + 1);
                if (options.oneofs)
                    object.query = "getByKey";
            }
            if (message.getBySolidityID != null && $Object.hasOwnProperty.call(message, "getBySolidityID")) {
                object.getBySolidityID = $root.proto.GetBySolidityIDQuery.toObject(message.getBySolidityID, options, _depth + 1);
                if (options.oneofs)
                    object.query = "getBySolidityID";
            }
            if (message.contractCallLocal != null && $Object.hasOwnProperty.call(message, "contractCallLocal")) {
                object.contractCallLocal = $root.proto.ContractCallLocalQuery.toObject(message.contractCallLocal, options, _depth + 1);
                if (options.oneofs)
                    object.query = "contractCallLocal";
            }
            if (message.contractGetInfo != null && $Object.hasOwnProperty.call(message, "contractGetInfo")) {
                object.contractGetInfo = $root.proto.ContractGetInfoQuery.toObject(message.contractGetInfo, options, _depth + 1);
                if (options.oneofs)
                    object.query = "contractGetInfo";
            }
            if (message.contractGetBytecode != null && $Object.hasOwnProperty.call(message, "contractGetBytecode")) {
                object.contractGetBytecode = $root.proto.ContractGetBytecodeQuery.toObject(message.contractGetBytecode, options, _depth + 1);
                if (options.oneofs)
                    object.query = "contractGetBytecode";
            }
            if (message.ContractGetRecords != null && $Object.hasOwnProperty.call(message, "ContractGetRecords")) {
                object.ContractGetRecords = $root.proto.ContractGetRecordsQuery.toObject(message.ContractGetRecords, options, _depth + 1);
                if (options.oneofs)
                    object.query = "ContractGetRecords";
            }
            if (message.cryptogetAccountBalance != null && $Object.hasOwnProperty.call(message, "cryptogetAccountBalance")) {
                object.cryptogetAccountBalance = $root.proto.CryptoGetAccountBalanceQuery.toObject(message.cryptogetAccountBalance, options, _depth + 1);
                if (options.oneofs)
                    object.query = "cryptogetAccountBalance";
            }
            if (message.cryptoGetAccountRecords != null && $Object.hasOwnProperty.call(message, "cryptoGetAccountRecords")) {
                object.cryptoGetAccountRecords = $root.proto.CryptoGetAccountRecordsQuery.toObject(message.cryptoGetAccountRecords, options, _depth + 1);
                if (options.oneofs)
                    object.query = "cryptoGetAccountRecords";
            }
            if (message.cryptoGetInfo != null && $Object.hasOwnProperty.call(message, "cryptoGetInfo")) {
                object.cryptoGetInfo = $root.proto.CryptoGetInfoQuery.toObject(message.cryptoGetInfo, options, _depth + 1);
                if (options.oneofs)
                    object.query = "cryptoGetInfo";
            }
            if (message.cryptoGetLiveHash != null && $Object.hasOwnProperty.call(message, "cryptoGetLiveHash")) {
                object.cryptoGetLiveHash = $root.proto.CryptoGetLiveHashQuery.toObject(message.cryptoGetLiveHash, options, _depth + 1);
                if (options.oneofs)
                    object.query = "cryptoGetLiveHash";
            }
            if (message.cryptoGetProxyStakers != null && $Object.hasOwnProperty.call(message, "cryptoGetProxyStakers")) {
                object.cryptoGetProxyStakers = $root.proto.CryptoGetStakersQuery.toObject(message.cryptoGetProxyStakers, options, _depth + 1);
                if (options.oneofs)
                    object.query = "cryptoGetProxyStakers";
            }
            if (message.fileGetContents != null && $Object.hasOwnProperty.call(message, "fileGetContents")) {
                object.fileGetContents = $root.proto.FileGetContentsQuery.toObject(message.fileGetContents, options, _depth + 1);
                if (options.oneofs)
                    object.query = "fileGetContents";
            }
            if (message.fileGetInfo != null && $Object.hasOwnProperty.call(message, "fileGetInfo")) {
                object.fileGetInfo = $root.proto.FileGetInfoQuery.toObject(message.fileGetInfo, options, _depth + 1);
                if (options.oneofs)
                    object.query = "fileGetInfo";
            }
            if (message.transactionGetReceipt != null && $Object.hasOwnProperty.call(message, "transactionGetReceipt")) {
                object.transactionGetReceipt = $root.proto.TransactionGetReceiptQuery.toObject(message.transactionGetReceipt, options, _depth + 1);
                if (options.oneofs)
                    object.query = "transactionGetReceipt";
            }
            if (message.transactionGetRecord != null && $Object.hasOwnProperty.call(message, "transactionGetRecord")) {
                object.transactionGetRecord = $root.proto.TransactionGetRecordQuery.toObject(message.transactionGetRecord, options, _depth + 1);
                if (options.oneofs)
                    object.query = "transactionGetRecord";
            }
            if (message.transactionGetFastRecord != null && $Object.hasOwnProperty.call(message, "transactionGetFastRecord")) {
                object.transactionGetFastRecord = $root.proto.TransactionGetFastRecordQuery.toObject(message.transactionGetFastRecord, options, _depth + 1);
                if (options.oneofs)
                    object.query = "transactionGetFastRecord";
            }
            if (message.consensusGetTopicInfo != null && $Object.hasOwnProperty.call(message, "consensusGetTopicInfo")) {
                object.consensusGetTopicInfo = $root.proto.ConsensusGetTopicInfoQuery.toObject(message.consensusGetTopicInfo, options, _depth + 1);
                if (options.oneofs)
                    object.query = "consensusGetTopicInfo";
            }
            if (message.networkGetVersionInfo != null && $Object.hasOwnProperty.call(message, "networkGetVersionInfo")) {
                object.networkGetVersionInfo = $root.proto.NetworkGetVersionInfoQuery.toObject(message.networkGetVersionInfo, options, _depth + 1);
                if (options.oneofs)
                    object.query = "networkGetVersionInfo";
            }
            if (message.tokenGetInfo != null && $Object.hasOwnProperty.call(message, "tokenGetInfo")) {
                object.tokenGetInfo = $root.proto.TokenGetInfoQuery.toObject(message.tokenGetInfo, options, _depth + 1);
                if (options.oneofs)
                    object.query = "tokenGetInfo";
            }
            if (message.scheduleGetInfo != null && $Object.hasOwnProperty.call(message, "scheduleGetInfo")) {
                object.scheduleGetInfo = $root.proto.ScheduleGetInfoQuery.toObject(message.scheduleGetInfo, options, _depth + 1);
                if (options.oneofs)
                    object.query = "scheduleGetInfo";
            }
            if (message.tokenGetAccountNftInfos != null && $Object.hasOwnProperty.call(message, "tokenGetAccountNftInfos")) {
                object.tokenGetAccountNftInfos = $root.proto.TokenGetAccountNftInfosQuery.toObject(message.tokenGetAccountNftInfos, options, _depth + 1);
                if (options.oneofs)
                    object.query = "tokenGetAccountNftInfos";
            }
            if (message.tokenGetNftInfo != null && $Object.hasOwnProperty.call(message, "tokenGetNftInfo")) {
                object.tokenGetNftInfo = $root.proto.TokenGetNftInfoQuery.toObject(message.tokenGetNftInfo, options, _depth + 1);
                if (options.oneofs)
                    object.query = "tokenGetNftInfo";
            }
            if (message.tokenGetNftInfos != null && $Object.hasOwnProperty.call(message, "tokenGetNftInfos")) {
                object.tokenGetNftInfos = $root.proto.TokenGetNftInfosQuery.toObject(message.tokenGetNftInfos, options, _depth + 1);
                if (options.oneofs)
                    object.query = "tokenGetNftInfos";
            }
            if (message.networkGetExecutionTime != null && $Object.hasOwnProperty.call(message, "networkGetExecutionTime")) {
                object.networkGetExecutionTime = $root.proto.NetworkGetExecutionTimeQuery.toObject(message.networkGetExecutionTime, options, _depth + 1);
                if (options.oneofs)
                    object.query = "networkGetExecutionTime";
            }
            if (message.accountDetails != null && $Object.hasOwnProperty.call(message, "accountDetails")) {
                object.accountDetails = $root.proto.GetAccountDetailsQuery.toObject(message.accountDetails, options, _depth + 1);
                if (options.oneofs)
                    object.query = "accountDetails";
            }
            return object;
        };

        /**
         * Converts this Query to JSON.
         * @function toJSON
         * @memberof proto.Query
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Query.prototype.toJSON = function() {
            return Query.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Query
         * @function getTypeUrl
         * @memberof proto.Query
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Query.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Query";
        };

        return Query;
    })();

    proto.GetByKeyQuery = (function() {

        /**
         * Properties of a GetByKeyQuery.
         * @typedef {Object} proto.GetByKeyQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.Key.$Properties|null} [key] The key to search for. It MUST NOT contain a contractID nor
         * a ThresholdKey.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GetByKeyQuery.
         * @memberof proto
         * @interface IGetByKeyQuery
         * @augments proto.GetByKeyQuery.$Properties
         * @deprecated Use proto.GetByKeyQuery.$Properties instead.
         */

        /**
         * Shape of a GetByKeyQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   key?: proto.Key.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.GetByKeyQuery.$Shape
         */

        /**
         * Constructs a new GetByKeyQuery.
         * @memberof proto
         * @classdesc Query all accounts, claims, files, and smart contract instances whose
         * associated keys include the given Key.
         * 
         * > This query is no longer supported.
         * @constructor
         * @param {proto.GetByKeyQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GetByKeyQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.GetByKeyQuery
         * @instance
         */
        GetByKeyQuery.prototype.header = null;

        /**
         * The key to search for. It MUST NOT contain a contractID nor
         * a ThresholdKey.
         * @member {proto.Key.$Properties|null|undefined} key
         * @memberof proto.GetByKeyQuery
         * @instance
         */
        GetByKeyQuery.prototype.key = null;

        /**
         * Creates a new GetByKeyQuery instance using the specified properties.
         * @function create
         * @memberof proto.GetByKeyQuery
         * @static
         * @param {proto.GetByKeyQuery.$Properties=} [properties] Properties to set
         * @returns {proto.GetByKeyQuery} GetByKeyQuery instance
         * @type {{
         *   (properties: proto.GetByKeyQuery.$Shape): proto.GetByKeyQuery & proto.GetByKeyQuery.$Shape;
         *   (properties?: proto.GetByKeyQuery.$Properties): proto.GetByKeyQuery;
         * }}
         */
        GetByKeyQuery.create = function(properties) {
            return new GetByKeyQuery(properties);
        };

        /**
         * Encodes the specified GetByKeyQuery message. Does not implicitly {@link proto.GetByKeyQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.GetByKeyQuery
         * @static
         * @param {proto.GetByKeyQuery.$Properties} message GetByKeyQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetByKeyQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                $root.proto.Key.encode(message.key, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetByKeyQuery message, length delimited. Does not implicitly {@link proto.GetByKeyQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GetByKeyQuery
         * @static
         * @param {proto.GetByKeyQuery.$Properties} message GetByKeyQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetByKeyQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GetByKeyQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GetByKeyQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GetByKeyQuery & proto.GetByKeyQuery.$Shape} GetByKeyQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetByKeyQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.GetByKeyQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.key = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetByKeyQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GetByKeyQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GetByKeyQuery & proto.GetByKeyQuery.$Shape} GetByKeyQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetByKeyQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetByKeyQuery message.
         * @function verify
         * @memberof proto.GetByKeyQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetByKeyQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key")) {
                let error = $root.proto.Key.verify(message.key, _depth + 1);
                if (error)
                    return "key." + error;
            }
            return null;
        };

        /**
         * Creates a GetByKeyQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GetByKeyQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GetByKeyQuery} GetByKeyQuery
         */
        GetByKeyQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.GetByKeyQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.GetByKeyQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.GetByKeyQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.GetByKeyQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.key != null) {
                if (!$util.isObject(object.key))
                    throw $TypeError(".proto.GetByKeyQuery.key: object expected");
                message.key = $root.proto.Key.fromObject(object.key, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a GetByKeyQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GetByKeyQuery
         * @static
         * @param {proto.GetByKeyQuery} message GetByKeyQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetByKeyQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.key = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = $root.proto.Key.toObject(message.key, options, _depth + 1);
            return object;
        };

        /**
         * Converts this GetByKeyQuery to JSON.
         * @function toJSON
         * @memberof proto.GetByKeyQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetByKeyQuery.prototype.toJSON = function() {
            return GetByKeyQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetByKeyQuery
         * @function getTypeUrl
         * @memberof proto.GetByKeyQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetByKeyQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GetByKeyQuery";
        };

        return GetByKeyQuery;
    })();

    proto.EntityID = (function() {

        /**
         * Properties of an EntityID.
         * @typedef {Object} proto.EntityID.$Properties
         * @property {proto.AccountID.$Properties|null} [accountID] The Account ID for the cryptocurrency account
         * @property {proto.LiveHash.$Properties|null} [liveHash] A uniquely identifying livehash of an account
         * @property {proto.FileID.$Properties|null} [fileID] The file ID of the file
         * @property {proto.ContractID.$Properties|null} [contractID] The smart contract ID that identifies instance
         * @property {"accountID"|"liveHash"|"fileID"|"contractID"} [entity] EntityID entity
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an EntityID.
         * @memberof proto
         * @interface IEntityID
         * @augments proto.EntityID.$Properties
         * @deprecated Use proto.EntityID.$Properties instead.
         */

        /**
         * Narrowed shape of an EntityID.
         * @typedef {{
         *   accountID?: proto.AccountID.$Shape|null;
         *   liveHash?: proto.LiveHash.$Shape|null;
         *   fileID?: proto.FileID.$Shape|null;
         *   contractID?: proto.ContractID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ entity?: undefined; accountID?: null; liveHash?: null; fileID?: null; contractID?: null }|{ entity?: "accountID"; accountID: proto.AccountID.$Shape; liveHash?: null; fileID?: null; contractID?: null }|{ entity?: "liveHash"; accountID?: null; liveHash: proto.LiveHash.$Shape; fileID?: null; contractID?: null }|{ entity?: "fileID"; accountID?: null; liveHash?: null; fileID: proto.FileID.$Shape; contractID?: null }|{ entity?: "contractID"; accountID?: null; liveHash?: null; fileID?: null; contractID: proto.ContractID.$Shape })
         * )} proto.EntityID.$Shape
         */

        /**
         * Constructs a new EntityID.
         * @memberof proto
         * @classdesc The ID for a single entity (account, livehash, file, or smart contract)
         * 
         * > The query that defines this message is no longer supported.
         * @constructor
         * @param {proto.EntityID.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const EntityID = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The Account ID for the cryptocurrency account
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.EntityID
         * @instance
         */
        EntityID.prototype.accountID = null;

        /**
         * A uniquely identifying livehash of an account
         * @member {proto.LiveHash.$Properties|null|undefined} liveHash
         * @memberof proto.EntityID
         * @instance
         */
        EntityID.prototype.liveHash = null;

        /**
         * The file ID of the file
         * @member {proto.FileID.$Properties|null|undefined} fileID
         * @memberof proto.EntityID
         * @instance
         */
        EntityID.prototype.fileID = null;

        /**
         * The smart contract ID that identifies instance
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.EntityID
         * @instance
         */
        EntityID.prototype.contractID = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * EntityID entity.
         * @member {"accountID"|"liveHash"|"fileID"|"contractID"|undefined} entity
         * @memberof proto.EntityID
         * @instance
         */
        $Object.defineProperty(EntityID.prototype, "entity", {
            get: $util.oneOfGetter($oneOfFields = ["accountID", "liveHash", "fileID", "contractID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new EntityID instance using the specified properties.
         * @function create
         * @memberof proto.EntityID
         * @static
         * @param {proto.EntityID.$Properties=} [properties] Properties to set
         * @returns {proto.EntityID} EntityID instance
         * @type {{
         *   (properties: proto.EntityID.$Shape): proto.EntityID & proto.EntityID.$Shape;
         *   (properties?: proto.EntityID.$Properties): proto.EntityID;
         * }}
         */
        EntityID.create = function(properties) {
            return new EntityID(properties);
        };

        /**
         * Encodes the specified EntityID message. Does not implicitly {@link proto.EntityID.verify|verify} messages.
         * @function encode
         * @memberof proto.EntityID
         * @static
         * @param {proto.EntityID.$Properties} message EntityID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EntityID.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.liveHash != null && $Object.hasOwnProperty.call(message, "liveHash"))
                $root.proto.LiveHash.encode(message.liveHash, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EntityID message, length delimited. Does not implicitly {@link proto.EntityID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EntityID
         * @static
         * @param {proto.EntityID.$Properties} message EntityID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EntityID.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an EntityID message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EntityID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EntityID & proto.EntityID.$Shape} EntityID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EntityID.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.EntityID();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        message.entity = "accountID";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.liveHash = $root.proto.LiveHash.decode(reader, reader.uint32(), $undefined, _depth + 1, message.liveHash);
                        message.entity = "liveHash";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                        message.entity = "fileID";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        message.entity = "contractID";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EntityID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EntityID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EntityID & proto.EntityID.$Shape} EntityID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EntityID.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EntityID message.
         * @function verify
         * @memberof proto.EntityID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EntityID.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                properties.entity = 1;
                {
                    let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                    if (error)
                        return "accountID." + error;
                }
            }
            if (message.liveHash != null && $Object.hasOwnProperty.call(message, "liveHash")) {
                if (properties.entity === 1)
                    return "entity: multiple values";
                properties.entity = 1;
                {
                    let error = $root.proto.LiveHash.verify(message.liveHash, _depth + 1);
                    if (error)
                        return "liveHash." + error;
                }
            }
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                if (properties.entity === 1)
                    return "entity: multiple values";
                properties.entity = 1;
                {
                    let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                    if (error)
                        return "fileID." + error;
                }
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                if (properties.entity === 1)
                    return "entity: multiple values";
                properties.entity = 1;
                {
                    let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                    if (error)
                        return "contractID." + error;
                }
            }
            return null;
        };

        /**
         * Creates an EntityID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EntityID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EntityID} EntityID
         */
        EntityID.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.EntityID)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.EntityID: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.EntityID();
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.EntityID.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.liveHash != null) {
                if (!$util.isObject(object.liveHash))
                    throw $TypeError(".proto.EntityID.liveHash: object expected");
                message.liveHash = $root.proto.LiveHash.fromObject(object.liveHash, _depth + 1);
            }
            if (object.fileID != null) {
                if (!$util.isObject(object.fileID))
                    throw $TypeError(".proto.EntityID.fileID: object expected");
                message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
            }
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.EntityID.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an EntityID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EntityID
         * @static
         * @param {proto.EntityID} message EntityID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EntityID.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
                if (options.oneofs)
                    object.entity = "accountID";
            }
            if (message.liveHash != null && $Object.hasOwnProperty.call(message, "liveHash")) {
                object.liveHash = $root.proto.LiveHash.toObject(message.liveHash, options, _depth + 1);
                if (options.oneofs)
                    object.entity = "liveHash";
            }
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
                if (options.oneofs)
                    object.entity = "fileID";
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
                if (options.oneofs)
                    object.entity = "contractID";
            }
            return object;
        };

        /**
         * Converts this EntityID to JSON.
         * @function toJSON
         * @memberof proto.EntityID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EntityID.prototype.toJSON = function() {
            return EntityID.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EntityID
         * @function getTypeUrl
         * @memberof proto.EntityID
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EntityID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EntityID";
        };

        return EntityID;
    })();

    proto.GetByKeyResponse = (function() {

        /**
         * Properties of a GetByKeyResponse.
         * @typedef {Object} proto.GetByKeyResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {Array.<proto.EntityID.$Properties>|null} [entities] The list of entities that include this public key in their
         * associated Key list
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GetByKeyResponse.
         * @memberof proto
         * @interface IGetByKeyResponse
         * @augments proto.GetByKeyResponse.$Properties
         * @deprecated Use proto.GetByKeyResponse.$Properties instead.
         */

        /**
         * Shape of a GetByKeyResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   entities?: Array.<proto.EntityID.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.GetByKeyResponse.$Shape
         */

        /**
         * Constructs a new GetByKeyResponse.
         * @memberof proto
         * @classdesc Response when the client sends the node GetByKeyQuery
         * 
         * > This query is no longer supported.
         * @constructor
         * @param {proto.GetByKeyResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GetByKeyResponse = function (properties) {
            this.entities = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.GetByKeyResponse
         * @instance
         */
        GetByKeyResponse.prototype.header = null;

        /**
         * The list of entities that include this public key in their
         * associated Key list
         * @member {Array.<proto.EntityID.$Properties>} entities
         * @memberof proto.GetByKeyResponse
         * @instance
         */
        GetByKeyResponse.prototype.entities = $util.emptyArray;

        /**
         * Creates a new GetByKeyResponse instance using the specified properties.
         * @function create
         * @memberof proto.GetByKeyResponse
         * @static
         * @param {proto.GetByKeyResponse.$Properties=} [properties] Properties to set
         * @returns {proto.GetByKeyResponse} GetByKeyResponse instance
         * @type {{
         *   (properties: proto.GetByKeyResponse.$Shape): proto.GetByKeyResponse & proto.GetByKeyResponse.$Shape;
         *   (properties?: proto.GetByKeyResponse.$Properties): proto.GetByKeyResponse;
         * }}
         */
        GetByKeyResponse.create = function(properties) {
            return new GetByKeyResponse(properties);
        };

        /**
         * Encodes the specified GetByKeyResponse message. Does not implicitly {@link proto.GetByKeyResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.GetByKeyResponse
         * @static
         * @param {proto.GetByKeyResponse.$Properties} message GetByKeyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetByKeyResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.entities != null && message.entities.length)
                for (let i = 0; i < message.entities.length; ++i)
                    $root.proto.EntityID.encode(message.entities[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetByKeyResponse message, length delimited. Does not implicitly {@link proto.GetByKeyResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GetByKeyResponse
         * @static
         * @param {proto.GetByKeyResponse.$Properties} message GetByKeyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetByKeyResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GetByKeyResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GetByKeyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GetByKeyResponse & proto.GetByKeyResponse.$Shape} GetByKeyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetByKeyResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.GetByKeyResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.entities && message.entities.length))
                            message.entities = [];
                        message.entities.push($root.proto.EntityID.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetByKeyResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GetByKeyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GetByKeyResponse & proto.GetByKeyResponse.$Shape} GetByKeyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetByKeyResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetByKeyResponse message.
         * @function verify
         * @memberof proto.GetByKeyResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetByKeyResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.entities != null && $Object.hasOwnProperty.call(message, "entities")) {
                if (!$Array.isArray(message.entities))
                    return "entities: array expected";
                for (let i = 0; i < message.entities.length; ++i) {
                    let error = $root.proto.EntityID.verify(message.entities[i], _depth + 1);
                    if (error)
                        return "entities." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GetByKeyResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GetByKeyResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GetByKeyResponse} GetByKeyResponse
         */
        GetByKeyResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.GetByKeyResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.GetByKeyResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.GetByKeyResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.GetByKeyResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.entities) {
                if (!$Array.isArray(object.entities))
                    throw $TypeError(".proto.GetByKeyResponse.entities: array expected");
                message.entities = $Array(object.entities.length);
                for (let i = 0; i < object.entities.length; ++i) {
                    if (!$util.isObject(object.entities[i]))
                        throw $TypeError(".proto.GetByKeyResponse.entities: object expected");
                    message.entities[i] = $root.proto.EntityID.fromObject(object.entities[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GetByKeyResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GetByKeyResponse
         * @static
         * @param {proto.GetByKeyResponse} message GetByKeyResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetByKeyResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.entities = [];
            if (options.defaults)
                object.header = null;
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.entities && message.entities.length) {
                object.entities = $Array(message.entities.length);
                for (let j = 0; j < message.entities.length; ++j)
                    object.entities[j] = $root.proto.EntityID.toObject(message.entities[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this GetByKeyResponse to JSON.
         * @function toJSON
         * @memberof proto.GetByKeyResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetByKeyResponse.prototype.toJSON = function() {
            return GetByKeyResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetByKeyResponse
         * @function getTypeUrl
         * @memberof proto.GetByKeyResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetByKeyResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GetByKeyResponse";
        };

        return GetByKeyResponse;
    })();

    proto.GetBySolidityIDQuery = (function() {

        /**
         * Properties of a GetBySolidityIDQuery.
         * @typedef {Object} proto.GetBySolidityIDQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {string|null} [solidityID] A contract ID in the format used by Solidity.
         * <p>
         * This field is REQUIRED.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GetBySolidityIDQuery.
         * @memberof proto
         * @interface IGetBySolidityIDQuery
         * @augments proto.GetBySolidityIDQuery.$Properties
         * @deprecated Use proto.GetBySolidityIDQuery.$Properties instead.
         */

        /**
         * Shape of a GetBySolidityIDQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   solidityID?: string|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.GetBySolidityIDQuery.$Shape
         */

        /**
         * Constructs a new GetBySolidityIDQuery.
         * @memberof proto
         * @classdesc Query to read Contract, Account, and File identifiers for a smart
         * contract given a Solidity identifier.
         * @constructor
         * @param {proto.GetBySolidityIDQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GetBySolidityIDQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.GetBySolidityIDQuery
         * @instance
         */
        GetBySolidityIDQuery.prototype.header = null;

        /**
         * A contract ID in the format used by Solidity.
         * <p>
         * This field is REQUIRED.
         * @member {string} solidityID
         * @memberof proto.GetBySolidityIDQuery
         * @instance
         */
        GetBySolidityIDQuery.prototype.solidityID = "";

        /**
         * Creates a new GetBySolidityIDQuery instance using the specified properties.
         * @function create
         * @memberof proto.GetBySolidityIDQuery
         * @static
         * @param {proto.GetBySolidityIDQuery.$Properties=} [properties] Properties to set
         * @returns {proto.GetBySolidityIDQuery} GetBySolidityIDQuery instance
         * @type {{
         *   (properties: proto.GetBySolidityIDQuery.$Shape): proto.GetBySolidityIDQuery & proto.GetBySolidityIDQuery.$Shape;
         *   (properties?: proto.GetBySolidityIDQuery.$Properties): proto.GetBySolidityIDQuery;
         * }}
         */
        GetBySolidityIDQuery.create = function(properties) {
            return new GetBySolidityIDQuery(properties);
        };

        /**
         * Encodes the specified GetBySolidityIDQuery message. Does not implicitly {@link proto.GetBySolidityIDQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.GetBySolidityIDQuery
         * @static
         * @param {proto.GetBySolidityIDQuery.$Properties} message GetBySolidityIDQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBySolidityIDQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.solidityID != null && $Object.hasOwnProperty.call(message, "solidityID") && message.solidityID !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.solidityID);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetBySolidityIDQuery message, length delimited. Does not implicitly {@link proto.GetBySolidityIDQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GetBySolidityIDQuery
         * @static
         * @param {proto.GetBySolidityIDQuery.$Properties} message GetBySolidityIDQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBySolidityIDQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GetBySolidityIDQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GetBySolidityIDQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GetBySolidityIDQuery & proto.GetBySolidityIDQuery.$Shape} GetBySolidityIDQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBySolidityIDQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.GetBySolidityIDQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.solidityID = value;
                        else
                            delete message.solidityID;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetBySolidityIDQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GetBySolidityIDQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GetBySolidityIDQuery & proto.GetBySolidityIDQuery.$Shape} GetBySolidityIDQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBySolidityIDQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetBySolidityIDQuery message.
         * @function verify
         * @memberof proto.GetBySolidityIDQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetBySolidityIDQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.solidityID != null && $Object.hasOwnProperty.call(message, "solidityID"))
                if (!$util.isString(message.solidityID))
                    return "solidityID: string expected";
            return null;
        };

        /**
         * Creates a GetBySolidityIDQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GetBySolidityIDQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GetBySolidityIDQuery} GetBySolidityIDQuery
         */
        GetBySolidityIDQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.GetBySolidityIDQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.GetBySolidityIDQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.GetBySolidityIDQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.GetBySolidityIDQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.solidityID != null)
                if (typeof object.solidityID !== "string" || object.solidityID.length)
                    message.solidityID = $String(object.solidityID);
            return message;
        };

        /**
         * Creates a plain object from a GetBySolidityIDQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GetBySolidityIDQuery
         * @static
         * @param {proto.GetBySolidityIDQuery} message GetBySolidityIDQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetBySolidityIDQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.solidityID = "";
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.solidityID != null && $Object.hasOwnProperty.call(message, "solidityID"))
                object.solidityID = message.solidityID;
            return object;
        };

        /**
         * Converts this GetBySolidityIDQuery to JSON.
         * @function toJSON
         * @memberof proto.GetBySolidityIDQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetBySolidityIDQuery.prototype.toJSON = function() {
            return GetBySolidityIDQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetBySolidityIDQuery
         * @function getTypeUrl
         * @memberof proto.GetBySolidityIDQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetBySolidityIDQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GetBySolidityIDQuery";
        };

        return GetBySolidityIDQuery;
    })();

    proto.GetBySolidityIDResponse = (function() {

        /**
         * Properties of a GetBySolidityIDResponse.
         * @typedef {Object} proto.GetBySolidityIDResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.AccountID.$Properties|null} [accountID] An account identifier.
         * <p>
         * This SHALL identify an account that backs the requested smart contract.
         * @property {proto.FileID.$Properties|null} [fileID] A file identifier.
         * <p>
         * This SHALL identify a file, the contents of which are the EVM
         * bytecode for the requested smart contract.
         * @property {proto.ContractID.$Properties|null} [contractID] A contract identifier.
         * <p>
         * This SHALL identify the requested smart contract.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GetBySolidityIDResponse.
         * @memberof proto
         * @interface IGetBySolidityIDResponse
         * @augments proto.GetBySolidityIDResponse.$Properties
         * @deprecated Use proto.GetBySolidityIDResponse.$Properties instead.
         */

        /**
         * Shape of a GetBySolidityIDResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   accountID?: proto.AccountID.$Shape|null;
         *   fileID?: proto.FileID.$Shape|null;
         *   contractID?: proto.ContractID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.GetBySolidityIDResponse.$Shape
         */

        /**
         * Constructs a new GetBySolidityIDResponse.
         * @memberof proto
         * @classdesc Response to a getBySolidityId query.
         * 
         * This message returns the account, contract, and file identifiers for
         * a smart contract.
         * @constructor
         * @param {proto.GetBySolidityIDResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GetBySolidityIDResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.GetBySolidityIDResponse
         * @instance
         */
        GetBySolidityIDResponse.prototype.header = null;

        /**
         * An account identifier.
         * <p>
         * This SHALL identify an account that backs the requested smart contract.
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.GetBySolidityIDResponse
         * @instance
         */
        GetBySolidityIDResponse.prototype.accountID = null;

        /**
         * A file identifier.
         * <p>
         * This SHALL identify a file, the contents of which are the EVM
         * bytecode for the requested smart contract.
         * @member {proto.FileID.$Properties|null|undefined} fileID
         * @memberof proto.GetBySolidityIDResponse
         * @instance
         */
        GetBySolidityIDResponse.prototype.fileID = null;

        /**
         * A contract identifier.
         * <p>
         * This SHALL identify the requested smart contract.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.GetBySolidityIDResponse
         * @instance
         */
        GetBySolidityIDResponse.prototype.contractID = null;

        /**
         * Creates a new GetBySolidityIDResponse instance using the specified properties.
         * @function create
         * @memberof proto.GetBySolidityIDResponse
         * @static
         * @param {proto.GetBySolidityIDResponse.$Properties=} [properties] Properties to set
         * @returns {proto.GetBySolidityIDResponse} GetBySolidityIDResponse instance
         * @type {{
         *   (properties: proto.GetBySolidityIDResponse.$Shape): proto.GetBySolidityIDResponse & proto.GetBySolidityIDResponse.$Shape;
         *   (properties?: proto.GetBySolidityIDResponse.$Properties): proto.GetBySolidityIDResponse;
         * }}
         */
        GetBySolidityIDResponse.create = function(properties) {
            return new GetBySolidityIDResponse(properties);
        };

        /**
         * Encodes the specified GetBySolidityIDResponse message. Does not implicitly {@link proto.GetBySolidityIDResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.GetBySolidityIDResponse
         * @static
         * @param {proto.GetBySolidityIDResponse.$Properties} message GetBySolidityIDResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBySolidityIDResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetBySolidityIDResponse message, length delimited. Does not implicitly {@link proto.GetBySolidityIDResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GetBySolidityIDResponse
         * @static
         * @param {proto.GetBySolidityIDResponse.$Properties} message GetBySolidityIDResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBySolidityIDResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GetBySolidityIDResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GetBySolidityIDResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GetBySolidityIDResponse & proto.GetBySolidityIDResponse.$Shape} GetBySolidityIDResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBySolidityIDResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.GetBySolidityIDResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetBySolidityIDResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GetBySolidityIDResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GetBySolidityIDResponse & proto.GetBySolidityIDResponse.$Shape} GetBySolidityIDResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBySolidityIDResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetBySolidityIDResponse message.
         * @function verify
         * @memberof proto.GetBySolidityIDResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetBySolidityIDResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                if (error)
                    return "fileID." + error;
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            return null;
        };

        /**
         * Creates a GetBySolidityIDResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GetBySolidityIDResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GetBySolidityIDResponse} GetBySolidityIDResponse
         */
        GetBySolidityIDResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.GetBySolidityIDResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.GetBySolidityIDResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.GetBySolidityIDResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.GetBySolidityIDResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.GetBySolidityIDResponse.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.fileID != null) {
                if (!$util.isObject(object.fileID))
                    throw $TypeError(".proto.GetBySolidityIDResponse.fileID: object expected");
                message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
            }
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.GetBySolidityIDResponse.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a GetBySolidityIDResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GetBySolidityIDResponse
         * @static
         * @param {proto.GetBySolidityIDResponse} message GetBySolidityIDResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetBySolidityIDResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.accountID = null;
                object.fileID = null;
                object.contractID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this GetBySolidityIDResponse to JSON.
         * @function toJSON
         * @memberof proto.GetBySolidityIDResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetBySolidityIDResponse.prototype.toJSON = function() {
            return GetBySolidityIDResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetBySolidityIDResponse
         * @function getTypeUrl
         * @memberof proto.GetBySolidityIDResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetBySolidityIDResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GetBySolidityIDResponse";
        };

        return GetBySolidityIDResponse;
    })();

    proto.ContractCallLocalQuery = (function() {

        /**
         * Properties of a ContractCallLocalQuery.
         * @typedef {Object} proto.ContractCallLocalQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * <p>
         * The payment MUST be sufficient for the base fees _and_ the full amount
         * in the `gas` field.
         * @property {proto.ContractID.$Properties|null} [contractID] The ID of a smart contract to call.
         * @property {number|Long|null} [gas] The amount of "gas" to use for this call.
         * <p>
         * This transaction SHALL consume all of the gas offered and charge the
         * corresponding fee according to the current exchange rate between
         * HBAR and "gas".
         * @property {Uint8Array|null} [functionParameters] The smart contract function to call, and the parameters to pass to that
         * function.
         * <p>
         * These SHALL be presented in EVM bytecode function call format.
         * @property {number|Long|null} [maxResultSize] Do not use this field; it is ignored in the current software.
         * <p>
         * The maximum number of bytes that the result might include.<br/>
         * The call will fail if it would have returned more than this number
         * of bytes.
         * @property {proto.AccountID.$Properties|null} [senderId] The account that is the "sender" for this contract call.
         * <p>
         * If this is not set it SHALL be interpreted as the accountId from the
         * associated transactionId.<br/>
         * If this is set then either the associated transaction or the foreign
         * transaction data MUST be signed by the referenced account.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractCallLocalQuery.
         * @memberof proto
         * @interface IContractCallLocalQuery
         * @augments proto.ContractCallLocalQuery.$Properties
         * @deprecated Use proto.ContractCallLocalQuery.$Properties instead.
         */

        /**
         * Shape of a ContractCallLocalQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   contractID?: proto.ContractID.$Shape|null;
         *   gas?: number|Long|null;
         *   functionParameters?: Uint8Array|null;
         *   maxResultSize?: number|Long|null;
         *   senderId?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractCallLocalQuery.$Shape
         */

        /**
         * Constructs a new ContractCallLocalQuery.
         * @memberof proto
         * @classdesc Call a view function of a given smart contract<br/>
         * The call must provide function parameter inputs as needed.<br/>
         * This is potentially useful for calling view functions that will not revert
         * when executed in a static EVM context. Many such use cases will be better
         * served by using a Mirror Node API, however.
         * 
         * This is performed locally on the particular node that the client is
         * communicating with. Executing the call locally is faster and less costly,
         * but imposes certain restrictions.<br/>
         * The call MUST NOT change the state of the contract instance. This also
         * precludes any expenditure or transfer of HBAR or other tokens.<br/>
         * The call SHALL NOT have a separate consensus timestamp.<br/>
         * The call SHALL NOT generate a record nor a receipt.<br/>
         * The response SHALL contain the output returned by the function call.<br/>
         * Any contract call that would use the `STATICCALL` opcode MAY be called via
         * contract call local with performance and cost benefits.
         * 
         * Unlike a ContractCall transaction, the node SHALL always consume the
         * _entire_ amount of offered "gas" in determining the fee for this query, so
         * accurate gas estimation is important.
         * @constructor
         * @param {proto.ContractCallLocalQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractCallLocalQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * <p>
         * The payment MUST be sufficient for the base fees _and_ the full amount
         * in the `gas` field.
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.ContractCallLocalQuery
         * @instance
         */
        ContractCallLocalQuery.prototype.header = null;

        /**
         * The ID of a smart contract to call.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.ContractCallLocalQuery
         * @instance
         */
        ContractCallLocalQuery.prototype.contractID = null;

        /**
         * The amount of "gas" to use for this call.
         * <p>
         * This transaction SHALL consume all of the gas offered and charge the
         * corresponding fee according to the current exchange rate between
         * HBAR and "gas".
         * @member {number|Long} gas
         * @memberof proto.ContractCallLocalQuery
         * @instance
         */
        ContractCallLocalQuery.prototype.gas = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The smart contract function to call, and the parameters to pass to that
         * function.
         * <p>
         * These SHALL be presented in EVM bytecode function call format.
         * @member {Uint8Array} functionParameters
         * @memberof proto.ContractCallLocalQuery
         * @instance
         */
        ContractCallLocalQuery.prototype.functionParameters = $util.newBuffer([]);

        /**
         * Do not use this field; it is ignored in the current software.
         * <p>
         * The maximum number of bytes that the result might include.<br/>
         * The call will fail if it would have returned more than this number
         * of bytes.
         * @member {number|Long} maxResultSize
         * @memberof proto.ContractCallLocalQuery
         * @instance
         */
        ContractCallLocalQuery.prototype.maxResultSize = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The account that is the "sender" for this contract call.
         * <p>
         * If this is not set it SHALL be interpreted as the accountId from the
         * associated transactionId.<br/>
         * If this is set then either the associated transaction or the foreign
         * transaction data MUST be signed by the referenced account.
         * @member {proto.AccountID.$Properties|null|undefined} senderId
         * @memberof proto.ContractCallLocalQuery
         * @instance
         */
        ContractCallLocalQuery.prototype.senderId = null;

        /**
         * Creates a new ContractCallLocalQuery instance using the specified properties.
         * @function create
         * @memberof proto.ContractCallLocalQuery
         * @static
         * @param {proto.ContractCallLocalQuery.$Properties=} [properties] Properties to set
         * @returns {proto.ContractCallLocalQuery} ContractCallLocalQuery instance
         * @type {{
         *   (properties: proto.ContractCallLocalQuery.$Shape): proto.ContractCallLocalQuery & proto.ContractCallLocalQuery.$Shape;
         *   (properties?: proto.ContractCallLocalQuery.$Properties): proto.ContractCallLocalQuery;
         * }}
         */
        ContractCallLocalQuery.create = function(properties) {
            return new ContractCallLocalQuery(properties);
        };

        /**
         * Encodes the specified ContractCallLocalQuery message. Does not implicitly {@link proto.ContractCallLocalQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractCallLocalQuery
         * @static
         * @param {proto.ContractCallLocalQuery.$Properties} message ContractCallLocalQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractCallLocalQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas") && (typeof message.gas === "object" ? message.gas.low || message.gas.high : message.gas !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.gas);
            if (message.functionParameters != null && $Object.hasOwnProperty.call(message, "functionParameters") && message.functionParameters.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.functionParameters);
            if (message.maxResultSize != null && $Object.hasOwnProperty.call(message, "maxResultSize") && (typeof message.maxResultSize === "object" ? message.maxResultSize.low || message.maxResultSize.high : message.maxResultSize !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.maxResultSize);
            if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                $root.proto.AccountID.encode(message.senderId, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractCallLocalQuery message, length delimited. Does not implicitly {@link proto.ContractCallLocalQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractCallLocalQuery
         * @static
         * @param {proto.ContractCallLocalQuery.$Properties} message ContractCallLocalQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractCallLocalQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractCallLocalQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractCallLocalQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractCallLocalQuery & proto.ContractCallLocalQuery.$Shape} ContractCallLocalQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractCallLocalQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractCallLocalQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.gas = value;
                        else
                            delete message.gas;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.functionParameters = value;
                        else
                            delete message.functionParameters;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maxResultSize = value;
                        else
                            delete message.maxResultSize;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.senderId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.senderId);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractCallLocalQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractCallLocalQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractCallLocalQuery & proto.ContractCallLocalQuery.$Shape} ContractCallLocalQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractCallLocalQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractCallLocalQuery message.
         * @function verify
         * @memberof proto.ContractCallLocalQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractCallLocalQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (!$util.isInteger(message.gas) && !(message.gas && $util.isInteger(message.gas.low) && $util.isInteger(message.gas.high)))
                    return "gas: integer|Long expected";
            if (message.functionParameters != null && $Object.hasOwnProperty.call(message, "functionParameters"))
                if (!(message.functionParameters && typeof message.functionParameters.length === "number" || $util.isString(message.functionParameters)))
                    return "functionParameters: buffer expected";
            if (message.maxResultSize != null && $Object.hasOwnProperty.call(message, "maxResultSize"))
                if (!$util.isInteger(message.maxResultSize) && !(message.maxResultSize && $util.isInteger(message.maxResultSize.low) && $util.isInteger(message.maxResultSize.high)))
                    return "maxResultSize: integer|Long expected";
            if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId")) {
                let error = $root.proto.AccountID.verify(message.senderId, _depth + 1);
                if (error)
                    return "senderId." + error;
            }
            return null;
        };

        /**
         * Creates a ContractCallLocalQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractCallLocalQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractCallLocalQuery} ContractCallLocalQuery
         */
        ContractCallLocalQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractCallLocalQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractCallLocalQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractCallLocalQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.ContractCallLocalQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.ContractCallLocalQuery.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            if (object.gas != null)
                if (typeof object.gas === "object" ? object.gas.low || object.gas.high : $Number(object.gas) !== 0)
                    if ($util.Long)
                        message.gas = $util.Long.fromValue(object.gas, false);
                    else if (typeof object.gas === "string")
                        message.gas = $parseInt(object.gas, 10);
                    else if (typeof object.gas === "number")
                        message.gas = object.gas;
                    else if (typeof object.gas === "object")
                        message.gas = new $util.LongBits(object.gas.low >>> 0, object.gas.high >>> 0).toNumber();
            if (object.functionParameters != null)
                if (object.functionParameters.length)
                    if (typeof object.functionParameters === "string")
                        $util.base64.decode(object.functionParameters, message.functionParameters = $util.newBuffer($util.base64.length(object.functionParameters)), 0);
                    else if (object.functionParameters.length >= 0)
                        message.functionParameters = object.functionParameters;
            if (object.maxResultSize != null)
                if (typeof object.maxResultSize === "object" ? object.maxResultSize.low || object.maxResultSize.high : $Number(object.maxResultSize) !== 0)
                    if ($util.Long)
                        message.maxResultSize = $util.Long.fromValue(object.maxResultSize, false);
                    else if (typeof object.maxResultSize === "string")
                        message.maxResultSize = $parseInt(object.maxResultSize, 10);
                    else if (typeof object.maxResultSize === "number")
                        message.maxResultSize = object.maxResultSize;
                    else if (typeof object.maxResultSize === "object")
                        message.maxResultSize = new $util.LongBits(object.maxResultSize.low >>> 0, object.maxResultSize.high >>> 0).toNumber();
            if (object.senderId != null) {
                if (!$util.isObject(object.senderId))
                    throw $TypeError(".proto.ContractCallLocalQuery.senderId: object expected");
                message.senderId = $root.proto.AccountID.fromObject(object.senderId, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractCallLocalQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractCallLocalQuery
         * @static
         * @param {proto.ContractCallLocalQuery} message ContractCallLocalQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractCallLocalQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.contractID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.gas = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gas = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.functionParameters = "";
                else {
                    object.functionParameters = [];
                    if (options.bytes !== $Array)
                        object.functionParameters = $util.newBuffer(object.functionParameters);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxResultSize = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maxResultSize = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.senderId = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gas = typeof message.gas === "number" ? $BigInt(message.gas) : $util.Long.fromBits(message.gas.low >>> 0, message.gas.high >>> 0, false).toBigInt();
                else if (typeof message.gas === "number")
                    object.gas = options.longs === $String ? $String(message.gas) : message.gas;
                else
                    object.gas = options.longs === $String ? $util.Long.prototype.toString.call(message.gas) : options.longs === $Number ? new $util.LongBits(message.gas.low >>> 0, message.gas.high >>> 0).toNumber() : message.gas;
            if (message.functionParameters != null && $Object.hasOwnProperty.call(message, "functionParameters"))
                object.functionParameters = options.bytes === $String ? $util.base64.encode(message.functionParameters, 0, message.functionParameters.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.functionParameters) : message.functionParameters;
            if (message.maxResultSize != null && $Object.hasOwnProperty.call(message, "maxResultSize"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maxResultSize = typeof message.maxResultSize === "number" ? $BigInt(message.maxResultSize) : $util.Long.fromBits(message.maxResultSize.low >>> 0, message.maxResultSize.high >>> 0, false).toBigInt();
                else if (typeof message.maxResultSize === "number")
                    object.maxResultSize = options.longs === $String ? $String(message.maxResultSize) : message.maxResultSize;
                else
                    object.maxResultSize = options.longs === $String ? $util.Long.prototype.toString.call(message.maxResultSize) : options.longs === $Number ? new $util.LongBits(message.maxResultSize.low >>> 0, message.maxResultSize.high >>> 0).toNumber() : message.maxResultSize;
            if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                object.senderId = $root.proto.AccountID.toObject(message.senderId, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ContractCallLocalQuery to JSON.
         * @function toJSON
         * @memberof proto.ContractCallLocalQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractCallLocalQuery.prototype.toJSON = function() {
            return ContractCallLocalQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractCallLocalQuery
         * @function getTypeUrl
         * @memberof proto.ContractCallLocalQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractCallLocalQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractCallLocalQuery";
        };

        return ContractCallLocalQuery;
    })();

    proto.ContractCallLocalResponse = (function() {

        /**
         * Properties of a ContractCallLocalResponse.
         * @typedef {Object} proto.ContractCallLocalResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.ContractFunctionResult.$Properties|null} [functionResult] The result(s) returned by the function call, if successful.
         * <p>
         * If the call failed this value SHALL be unset.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractCallLocalResponse.
         * @memberof proto
         * @interface IContractCallLocalResponse
         * @augments proto.ContractCallLocalResponse.$Properties
         * @deprecated Use proto.ContractCallLocalResponse.$Properties instead.
         */

        /**
         * Shape of a ContractCallLocalResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   functionResult?: proto.ContractFunctionResult.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractCallLocalResponse.$Shape
         */

        /**
         * Constructs a new ContractCallLocalResponse.
         * @memberof proto
         * @classdesc The response returned by a `ContractCallLocalQuery` transaction.
         * @constructor
         * @param {proto.ContractCallLocalResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractCallLocalResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.ContractCallLocalResponse
         * @instance
         */
        ContractCallLocalResponse.prototype.header = null;

        /**
         * The result(s) returned by the function call, if successful.
         * <p>
         * If the call failed this value SHALL be unset.
         * @member {proto.ContractFunctionResult.$Properties|null|undefined} functionResult
         * @memberof proto.ContractCallLocalResponse
         * @instance
         */
        ContractCallLocalResponse.prototype.functionResult = null;

        /**
         * Creates a new ContractCallLocalResponse instance using the specified properties.
         * @function create
         * @memberof proto.ContractCallLocalResponse
         * @static
         * @param {proto.ContractCallLocalResponse.$Properties=} [properties] Properties to set
         * @returns {proto.ContractCallLocalResponse} ContractCallLocalResponse instance
         * @type {{
         *   (properties: proto.ContractCallLocalResponse.$Shape): proto.ContractCallLocalResponse & proto.ContractCallLocalResponse.$Shape;
         *   (properties?: proto.ContractCallLocalResponse.$Properties): proto.ContractCallLocalResponse;
         * }}
         */
        ContractCallLocalResponse.create = function(properties) {
            return new ContractCallLocalResponse(properties);
        };

        /**
         * Encodes the specified ContractCallLocalResponse message. Does not implicitly {@link proto.ContractCallLocalResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractCallLocalResponse
         * @static
         * @param {proto.ContractCallLocalResponse.$Properties} message ContractCallLocalResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractCallLocalResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.functionResult != null && $Object.hasOwnProperty.call(message, "functionResult"))
                $root.proto.ContractFunctionResult.encode(message.functionResult, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractCallLocalResponse message, length delimited. Does not implicitly {@link proto.ContractCallLocalResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractCallLocalResponse
         * @static
         * @param {proto.ContractCallLocalResponse.$Properties} message ContractCallLocalResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractCallLocalResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractCallLocalResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractCallLocalResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractCallLocalResponse & proto.ContractCallLocalResponse.$Shape} ContractCallLocalResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractCallLocalResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractCallLocalResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.functionResult = $root.proto.ContractFunctionResult.decode(reader, reader.uint32(), $undefined, _depth + 1, message.functionResult);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractCallLocalResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractCallLocalResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractCallLocalResponse & proto.ContractCallLocalResponse.$Shape} ContractCallLocalResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractCallLocalResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractCallLocalResponse message.
         * @function verify
         * @memberof proto.ContractCallLocalResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractCallLocalResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.functionResult != null && $Object.hasOwnProperty.call(message, "functionResult")) {
                let error = $root.proto.ContractFunctionResult.verify(message.functionResult, _depth + 1);
                if (error)
                    return "functionResult." + error;
            }
            return null;
        };

        /**
         * Creates a ContractCallLocalResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractCallLocalResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractCallLocalResponse} ContractCallLocalResponse
         */
        ContractCallLocalResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractCallLocalResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractCallLocalResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractCallLocalResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.ContractCallLocalResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.functionResult != null) {
                if (!$util.isObject(object.functionResult))
                    throw $TypeError(".proto.ContractCallLocalResponse.functionResult: object expected");
                message.functionResult = $root.proto.ContractFunctionResult.fromObject(object.functionResult, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractCallLocalResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractCallLocalResponse
         * @static
         * @param {proto.ContractCallLocalResponse} message ContractCallLocalResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractCallLocalResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.functionResult = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.functionResult != null && $Object.hasOwnProperty.call(message, "functionResult"))
                object.functionResult = $root.proto.ContractFunctionResult.toObject(message.functionResult, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ContractCallLocalResponse to JSON.
         * @function toJSON
         * @memberof proto.ContractCallLocalResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractCallLocalResponse.prototype.toJSON = function() {
            return ContractCallLocalResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractCallLocalResponse
         * @function getTypeUrl
         * @memberof proto.ContractCallLocalResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractCallLocalResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractCallLocalResponse";
        };

        return ContractCallLocalResponse;
    })();

    proto.InternalCallContext = (function() {

        /**
         * Properties of an InternalCallContext.
         * @typedef {Object} proto.InternalCallContext.$Properties
         * @property {number|Long|null} [gas] The gas available for the call.<br/>
         * @property {number|Long|null} [value] The value sent with the call.<br/>
         * @property {Uint8Array|null} [callData] The call data for the call.<br/>
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an InternalCallContext.
         * @memberof proto
         * @interface IInternalCallContext
         * @augments proto.InternalCallContext.$Properties
         * @deprecated Use proto.InternalCallContext.$Properties instead.
         */

        /**
         * Shape of an InternalCallContext.
         * @typedef {proto.InternalCallContext.$Properties} proto.InternalCallContext.$Shape
         */

        /**
         * Constructs a new InternalCallContext.
         * @memberof proto
         * @classdesc Context of an internal call in an EVM transaction that is not otherwise externalized.<br/>
         * This message does not say anything about whether an EVM transaction is itself a logical
         * transaction in a Hiero transactional unit. It simply provides context on an internal
         * message call within an EVM transaction.
         * @constructor
         * @param {proto.InternalCallContext.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const InternalCallContext = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The gas available for the call.<br/>
         * @member {number|Long} gas
         * @memberof proto.InternalCallContext
         * @instance
         */
        InternalCallContext.prototype.gas = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * The value sent with the call.<br/>
         * @member {number|Long} value
         * @memberof proto.InternalCallContext
         * @instance
         */
        InternalCallContext.prototype.value = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * The call data for the call.<br/>
         * @member {Uint8Array} callData
         * @memberof proto.InternalCallContext
         * @instance
         */
        InternalCallContext.prototype.callData = $util.newBuffer([]);

        /**
         * Creates a new InternalCallContext instance using the specified properties.
         * @function create
         * @memberof proto.InternalCallContext
         * @static
         * @param {proto.InternalCallContext.$Properties=} [properties] Properties to set
         * @returns {proto.InternalCallContext} InternalCallContext instance
         * @type {{
         *   (properties: proto.InternalCallContext.$Shape): proto.InternalCallContext & proto.InternalCallContext.$Shape;
         *   (properties?: proto.InternalCallContext.$Properties): proto.InternalCallContext;
         * }}
         */
        InternalCallContext.create = function(properties) {
            return new InternalCallContext(properties);
        };

        /**
         * Encodes the specified InternalCallContext message. Does not implicitly {@link proto.InternalCallContext.verify|verify} messages.
         * @function encode
         * @memberof proto.InternalCallContext
         * @static
         * @param {proto.InternalCallContext.$Properties} message InternalCallContext message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InternalCallContext.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas") && (typeof message.gas === "object" ? message.gas.low || message.gas.high : message.gas !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.gas);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && (typeof message.value === "object" ? message.value.low || message.value.high : message.value !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.value);
            if (message.callData != null && $Object.hasOwnProperty.call(message, "callData") && message.callData.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.callData);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InternalCallContext message, length delimited. Does not implicitly {@link proto.InternalCallContext.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.InternalCallContext
         * @static
         * @param {proto.InternalCallContext.$Properties} message InternalCallContext message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InternalCallContext.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an InternalCallContext message from the specified reader or buffer.
         * @function decode
         * @memberof proto.InternalCallContext
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.InternalCallContext & proto.InternalCallContext.$Shape} InternalCallContext
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InternalCallContext.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.InternalCallContext();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.gas = value;
                        else
                            delete message.gas;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.callData = value;
                        else
                            delete message.callData;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an InternalCallContext message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.InternalCallContext
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.InternalCallContext & proto.InternalCallContext.$Shape} InternalCallContext
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InternalCallContext.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InternalCallContext message.
         * @function verify
         * @memberof proto.InternalCallContext
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InternalCallContext.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (!$util.isInteger(message.gas) && !(message.gas && $util.isInteger(message.gas.low) && $util.isInteger(message.gas.high)))
                    return "gas: integer|Long expected";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isInteger(message.value) && !(message.value && $util.isInteger(message.value.low) && $util.isInteger(message.value.high)))
                    return "value: integer|Long expected";
            if (message.callData != null && $Object.hasOwnProperty.call(message, "callData"))
                if (!(message.callData && typeof message.callData.length === "number" || $util.isString(message.callData)))
                    return "callData: buffer expected";
            return null;
        };

        /**
         * Creates an InternalCallContext message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.InternalCallContext
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.InternalCallContext} InternalCallContext
         */
        InternalCallContext.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.InternalCallContext)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.InternalCallContext: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.InternalCallContext();
            if (object.gas != null)
                if (typeof object.gas === "object" ? object.gas.low || object.gas.high : $Number(object.gas) !== 0)
                    if ($util.Long)
                        message.gas = $util.Long.fromValue(object.gas, true);
                    else if (typeof object.gas === "string")
                        message.gas = $parseInt(object.gas, 10);
                    else if (typeof object.gas === "number")
                        message.gas = object.gas;
                    else if (typeof object.gas === "object")
                        message.gas = new $util.LongBits(object.gas.low >>> 0, object.gas.high >>> 0).toNumber(true);
            if (object.value != null)
                if (typeof object.value === "object" ? object.value.low || object.value.high : $Number(object.value) !== 0)
                    if ($util.Long)
                        message.value = $util.Long.fromValue(object.value, true);
                    else if (typeof object.value === "string")
                        message.value = $parseInt(object.value, 10);
                    else if (typeof object.value === "number")
                        message.value = object.value;
                    else if (typeof object.value === "object")
                        message.value = new $util.LongBits(object.value.low >>> 0, object.value.high >>> 0).toNumber(true);
            if (object.callData != null)
                if (object.callData.length)
                    if (typeof object.callData === "string")
                        $util.base64.decode(object.callData, message.callData = $util.newBuffer($util.base64.length(object.callData)), 0);
                    else if (object.callData.length >= 0)
                        message.callData = object.callData;
            return message;
        };

        /**
         * Creates a plain object from an InternalCallContext message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.InternalCallContext
         * @static
         * @param {proto.InternalCallContext} message InternalCallContext
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InternalCallContext.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.gas = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gas = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.value = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.value = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.callData = "";
                else {
                    object.callData = [];
                    if (options.bytes !== $Array)
                        object.callData = $util.newBuffer(object.callData);
                }
            }
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gas = typeof message.gas === "number" ? $BigInt(message.gas) : $util.Long.fromBits(message.gas.low >>> 0, message.gas.high >>> 0, true).toBigInt();
                else if (typeof message.gas === "number")
                    object.gas = options.longs === $String ? $String(message.gas) : message.gas;
                else
                    object.gas = options.longs === $String ? $util.Long.prototype.toString.call(message.gas) : options.longs === $Number ? new $util.LongBits(message.gas.low >>> 0, message.gas.high >>> 0).toNumber(true) : message.gas;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.value = typeof message.value === "number" ? $BigInt(message.value) : $util.Long.fromBits(message.value.low >>> 0, message.value.high >>> 0, true).toBigInt();
                else if (typeof message.value === "number")
                    object.value = options.longs === $String ? $String(message.value) : message.value;
                else
                    object.value = options.longs === $String ? $util.Long.prototype.toString.call(message.value) : options.longs === $Number ? new $util.LongBits(message.value.low >>> 0, message.value.high >>> 0).toNumber(true) : message.value;
            if (message.callData != null && $Object.hasOwnProperty.call(message, "callData"))
                object.callData = options.bytes === $String ? $util.base64.encode(message.callData, 0, message.callData.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.callData) : message.callData;
            return object;
        };

        /**
         * Converts this InternalCallContext to JSON.
         * @function toJSON
         * @memberof proto.InternalCallContext
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InternalCallContext.prototype.toJSON = function() {
            return InternalCallContext.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for InternalCallContext
         * @function getTypeUrl
         * @memberof proto.InternalCallContext
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        InternalCallContext.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.InternalCallContext";
        };

        return InternalCallContext;
    })();

    proto.EvmTransactionResult = (function() {

        /**
         * Properties of an EvmTransactionResult.
         * @typedef {Object} proto.EvmTransactionResult.$Properties
         * @property {proto.AccountID.$Properties|null} [senderId] The Hedera id of the caller.<br/>
         * @property {proto.ContractID.$Properties|null} [contractId] The Hedera id of the contract receiving the call.<br/>
         * @property {Uint8Array|null} [resultData] Result data from the function call.
         * @property {string|null} [errorMessage] Any error message produced by the contract call. Max size of 100 bytes.
         * <p>
         * This SHALL be unset if the contract call succeeded.
         * @property {number|Long|null} [gasUsed] EVM gas used.
         * @property {proto.InternalCallContext.$Properties|null} [internalCallContext] If not already externalized in a transaction body, the context of the
         * internal call producing this result.
         * @property {proto.HookId.$Properties|null} [executedHookId] If set, the id of the executed hook.
         * @property {Array.<proto.ContractNonceInfo.$Properties>|null} [contractNonces] A list of contract account nonce values.<br/>
         * This list SHALL contain a nonce value for each contract account modified
         * as a result of this contract call. These nonce values SHALL be the value
         * after the contract call is completed.
         * @property {google.protobuf.Int64Value.$Properties|null} [signerNonce] In an EthereumTransaction, the nonce of the signer account at the end of
         * the transaction.<br/>
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an EvmTransactionResult.
         * @memberof proto
         * @interface IEvmTransactionResult
         * @augments proto.EvmTransactionResult.$Properties
         * @deprecated Use proto.EvmTransactionResult.$Properties instead.
         */

        /**
         * Shape of an EvmTransactionResult.
         * @typedef {{
         *   senderId?: proto.AccountID.$Shape|null;
         *   contractId?: proto.ContractID.$Shape|null;
         *   resultData?: Uint8Array|null;
         *   errorMessage?: string|null;
         *   gasUsed?: number|Long|null;
         *   internalCallContext?: proto.InternalCallContext.$Shape|null;
         *   executedHookId?: proto.HookId.$Shape|null;
         *   contractNonces?: Array.<proto.ContractNonceInfo.$Shape>|null;
         *   signerNonce?: google.protobuf.Int64Value.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.EvmTransactionResult.$Shape
         */

        /**
         * Constructs a new EvmTransactionResult.
         * @memberof proto
         * @classdesc Results of executing a EVM transaction.<br/>
         * @constructor
         * @param {proto.EvmTransactionResult.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const EvmTransactionResult = function (properties) {
            this.contractNonces = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The Hedera id of the caller.<br/>
         * @member {proto.AccountID.$Properties|null|undefined} senderId
         * @memberof proto.EvmTransactionResult
         * @instance
         */
        EvmTransactionResult.prototype.senderId = null;

        /**
         * The Hedera id of the contract receiving the call.<br/>
         * @member {proto.ContractID.$Properties|null|undefined} contractId
         * @memberof proto.EvmTransactionResult
         * @instance
         */
        EvmTransactionResult.prototype.contractId = null;

        /**
         * Result data from the function call.
         * @member {Uint8Array} resultData
         * @memberof proto.EvmTransactionResult
         * @instance
         */
        EvmTransactionResult.prototype.resultData = $util.newBuffer([]);

        /**
         * Any error message produced by the contract call. Max size of 100 bytes.
         * <p>
         * This SHALL be unset if the contract call succeeded.
         * @member {string} errorMessage
         * @memberof proto.EvmTransactionResult
         * @instance
         */
        EvmTransactionResult.prototype.errorMessage = "";

        /**
         * EVM gas used.
         * @member {number|Long} gasUsed
         * @memberof proto.EvmTransactionResult
         * @instance
         */
        EvmTransactionResult.prototype.gasUsed = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * If not already externalized in a transaction body, the context of the
         * internal call producing this result.
         * @member {proto.InternalCallContext.$Properties|null|undefined} internalCallContext
         * @memberof proto.EvmTransactionResult
         * @instance
         */
        EvmTransactionResult.prototype.internalCallContext = null;

        /**
         * If set, the id of the executed hook.
         * @member {proto.HookId.$Properties|null|undefined} executedHookId
         * @memberof proto.EvmTransactionResult
         * @instance
         */
        EvmTransactionResult.prototype.executedHookId = null;

        /**
         * A list of contract account nonce values.<br/>
         * This list SHALL contain a nonce value for each contract account modified
         * as a result of this contract call. These nonce values SHALL be the value
         * after the contract call is completed.
         * @member {Array.<proto.ContractNonceInfo.$Properties>} contractNonces
         * @memberof proto.EvmTransactionResult
         * @instance
         */
        EvmTransactionResult.prototype.contractNonces = $util.emptyArray;

        /**
         * In an EthereumTransaction, the nonce of the signer account at the end of
         * the transaction.<br/>
         * @member {google.protobuf.Int64Value.$Properties|null|undefined} signerNonce
         * @memberof proto.EvmTransactionResult
         * @instance
         */
        EvmTransactionResult.prototype.signerNonce = null;

        /**
         * Creates a new EvmTransactionResult instance using the specified properties.
         * @function create
         * @memberof proto.EvmTransactionResult
         * @static
         * @param {proto.EvmTransactionResult.$Properties=} [properties] Properties to set
         * @returns {proto.EvmTransactionResult} EvmTransactionResult instance
         * @type {{
         *   (properties: proto.EvmTransactionResult.$Shape): proto.EvmTransactionResult & proto.EvmTransactionResult.$Shape;
         *   (properties?: proto.EvmTransactionResult.$Properties): proto.EvmTransactionResult;
         * }}
         */
        EvmTransactionResult.create = function(properties) {
            return new EvmTransactionResult(properties);
        };

        /**
         * Encodes the specified EvmTransactionResult message. Does not implicitly {@link proto.EvmTransactionResult.verify|verify} messages.
         * @function encode
         * @memberof proto.EvmTransactionResult
         * @static
         * @param {proto.EvmTransactionResult.$Properties} message EvmTransactionResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EvmTransactionResult.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                $root.proto.AccountID.encode(message.senderId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId"))
                $root.proto.ContractID.encode(message.contractId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.resultData != null && $Object.hasOwnProperty.call(message, "resultData") && message.resultData.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.resultData);
            if (message.errorMessage != null && $Object.hasOwnProperty.call(message, "errorMessage") && message.errorMessage !== "")
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.errorMessage);
            if (message.gasUsed != null && $Object.hasOwnProperty.call(message, "gasUsed") && (typeof message.gasUsed === "object" ? message.gasUsed.low || message.gasUsed.high : message.gasUsed !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.gasUsed);
            if (message.internalCallContext != null && $Object.hasOwnProperty.call(message, "internalCallContext"))
                $root.proto.InternalCallContext.encode(message.internalCallContext, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.executedHookId != null && $Object.hasOwnProperty.call(message, "executedHookId"))
                $root.proto.HookId.encode(message.executedHookId, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.contractNonces != null && message.contractNonces.length)
                for (let i = 0; i < message.contractNonces.length; ++i)
                    $root.proto.ContractNonceInfo.encode(message.contractNonces[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.signerNonce != null && $Object.hasOwnProperty.call(message, "signerNonce"))
                $root.google.protobuf.Int64Value.encode(message.signerNonce, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EvmTransactionResult message, length delimited. Does not implicitly {@link proto.EvmTransactionResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EvmTransactionResult
         * @static
         * @param {proto.EvmTransactionResult.$Properties} message EvmTransactionResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EvmTransactionResult.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an EvmTransactionResult message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EvmTransactionResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EvmTransactionResult & proto.EvmTransactionResult.$Shape} EvmTransactionResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EvmTransactionResult.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.EvmTransactionResult();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.senderId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.senderId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.contractId = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractId);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.resultData = value;
                        else
                            delete message.resultData;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.errorMessage = value;
                        else
                            delete message.errorMessage;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.gasUsed = value;
                        else
                            delete message.gasUsed;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.internalCallContext = $root.proto.InternalCallContext.decode(reader, reader.uint32(), $undefined, _depth + 1, message.internalCallContext);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.executedHookId = $root.proto.HookId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.executedHookId);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.contractNonces && message.contractNonces.length))
                            message.contractNonces = [];
                        message.contractNonces.push($root.proto.ContractNonceInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.signerNonce = $root.google.protobuf.Int64Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.signerNonce);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EvmTransactionResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EvmTransactionResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EvmTransactionResult & proto.EvmTransactionResult.$Shape} EvmTransactionResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EvmTransactionResult.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EvmTransactionResult message.
         * @function verify
         * @memberof proto.EvmTransactionResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EvmTransactionResult.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId")) {
                let error = $root.proto.AccountID.verify(message.senderId, _depth + 1);
                if (error)
                    return "senderId." + error;
            }
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId")) {
                let error = $root.proto.ContractID.verify(message.contractId, _depth + 1);
                if (error)
                    return "contractId." + error;
            }
            if (message.resultData != null && $Object.hasOwnProperty.call(message, "resultData"))
                if (!(message.resultData && typeof message.resultData.length === "number" || $util.isString(message.resultData)))
                    return "resultData: buffer expected";
            if (message.errorMessage != null && $Object.hasOwnProperty.call(message, "errorMessage"))
                if (!$util.isString(message.errorMessage))
                    return "errorMessage: string expected";
            if (message.gasUsed != null && $Object.hasOwnProperty.call(message, "gasUsed"))
                if (!$util.isInteger(message.gasUsed) && !(message.gasUsed && $util.isInteger(message.gasUsed.low) && $util.isInteger(message.gasUsed.high)))
                    return "gasUsed: integer|Long expected";
            if (message.internalCallContext != null && $Object.hasOwnProperty.call(message, "internalCallContext")) {
                let error = $root.proto.InternalCallContext.verify(message.internalCallContext, _depth + 1);
                if (error)
                    return "internalCallContext." + error;
            }
            if (message.executedHookId != null && $Object.hasOwnProperty.call(message, "executedHookId")) {
                let error = $root.proto.HookId.verify(message.executedHookId, _depth + 1);
                if (error)
                    return "executedHookId." + error;
            }
            if (message.contractNonces != null && $Object.hasOwnProperty.call(message, "contractNonces")) {
                if (!$Array.isArray(message.contractNonces))
                    return "contractNonces: array expected";
                for (let i = 0; i < message.contractNonces.length; ++i) {
                    let error = $root.proto.ContractNonceInfo.verify(message.contractNonces[i], _depth + 1);
                    if (error)
                        return "contractNonces." + error;
                }
            }
            if (message.signerNonce != null && $Object.hasOwnProperty.call(message, "signerNonce")) {
                let error = $root.google.protobuf.Int64Value.verify(message.signerNonce, _depth + 1);
                if (error)
                    return "signerNonce." + error;
            }
            return null;
        };

        /**
         * Creates an EvmTransactionResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EvmTransactionResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EvmTransactionResult} EvmTransactionResult
         */
        EvmTransactionResult.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.EvmTransactionResult)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.EvmTransactionResult: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.EvmTransactionResult();
            if (object.senderId != null) {
                if (!$util.isObject(object.senderId))
                    throw $TypeError(".proto.EvmTransactionResult.senderId: object expected");
                message.senderId = $root.proto.AccountID.fromObject(object.senderId, _depth + 1);
            }
            if (object.contractId != null) {
                if (!$util.isObject(object.contractId))
                    throw $TypeError(".proto.EvmTransactionResult.contractId: object expected");
                message.contractId = $root.proto.ContractID.fromObject(object.contractId, _depth + 1);
            }
            if (object.resultData != null)
                if (object.resultData.length)
                    if (typeof object.resultData === "string")
                        $util.base64.decode(object.resultData, message.resultData = $util.newBuffer($util.base64.length(object.resultData)), 0);
                    else if (object.resultData.length >= 0)
                        message.resultData = object.resultData;
            if (object.errorMessage != null)
                if (typeof object.errorMessage !== "string" || object.errorMessage.length)
                    message.errorMessage = $String(object.errorMessage);
            if (object.gasUsed != null)
                if (typeof object.gasUsed === "object" ? object.gasUsed.low || object.gasUsed.high : $Number(object.gasUsed) !== 0)
                    if ($util.Long)
                        message.gasUsed = $util.Long.fromValue(object.gasUsed, true);
                    else if (typeof object.gasUsed === "string")
                        message.gasUsed = $parseInt(object.gasUsed, 10);
                    else if (typeof object.gasUsed === "number")
                        message.gasUsed = object.gasUsed;
                    else if (typeof object.gasUsed === "object")
                        message.gasUsed = new $util.LongBits(object.gasUsed.low >>> 0, object.gasUsed.high >>> 0).toNumber(true);
            if (object.internalCallContext != null) {
                if (!$util.isObject(object.internalCallContext))
                    throw $TypeError(".proto.EvmTransactionResult.internalCallContext: object expected");
                message.internalCallContext = $root.proto.InternalCallContext.fromObject(object.internalCallContext, _depth + 1);
            }
            if (object.executedHookId != null) {
                if (!$util.isObject(object.executedHookId))
                    throw $TypeError(".proto.EvmTransactionResult.executedHookId: object expected");
                message.executedHookId = $root.proto.HookId.fromObject(object.executedHookId, _depth + 1);
            }
            if (object.contractNonces) {
                if (!$Array.isArray(object.contractNonces))
                    throw $TypeError(".proto.EvmTransactionResult.contractNonces: array expected");
                message.contractNonces = $Array(object.contractNonces.length);
                for (let i = 0; i < object.contractNonces.length; ++i) {
                    if (!$util.isObject(object.contractNonces[i]))
                        throw $TypeError(".proto.EvmTransactionResult.contractNonces: object expected");
                    message.contractNonces[i] = $root.proto.ContractNonceInfo.fromObject(object.contractNonces[i], _depth + 1);
                }
            }
            if (object.signerNonce != null) {
                if (!$util.isObject(object.signerNonce))
                    throw $TypeError(".proto.EvmTransactionResult.signerNonce: object expected");
                message.signerNonce = $root.google.protobuf.Int64Value.fromObject(object.signerNonce, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an EvmTransactionResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EvmTransactionResult
         * @static
         * @param {proto.EvmTransactionResult} message EvmTransactionResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EvmTransactionResult.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.contractNonces = [];
            if (options.defaults) {
                object.senderId = null;
                object.contractId = null;
                if (options.bytes === $String)
                    object.resultData = "";
                else {
                    object.resultData = [];
                    if (options.bytes !== $Array)
                        object.resultData = $util.newBuffer(object.resultData);
                }
                object.errorMessage = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.gasUsed = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gasUsed = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.internalCallContext = null;
                object.executedHookId = null;
                object.signerNonce = null;
            }
            if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                object.senderId = $root.proto.AccountID.toObject(message.senderId, options, _depth + 1);
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId"))
                object.contractId = $root.proto.ContractID.toObject(message.contractId, options, _depth + 1);
            if (message.resultData != null && $Object.hasOwnProperty.call(message, "resultData"))
                object.resultData = options.bytes === $String ? $util.base64.encode(message.resultData, 0, message.resultData.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.resultData) : message.resultData;
            if (message.errorMessage != null && $Object.hasOwnProperty.call(message, "errorMessage"))
                object.errorMessage = message.errorMessage;
            if (message.gasUsed != null && $Object.hasOwnProperty.call(message, "gasUsed"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gasUsed = typeof message.gasUsed === "number" ? $BigInt(message.gasUsed) : $util.Long.fromBits(message.gasUsed.low >>> 0, message.gasUsed.high >>> 0, true).toBigInt();
                else if (typeof message.gasUsed === "number")
                    object.gasUsed = options.longs === $String ? $String(message.gasUsed) : message.gasUsed;
                else
                    object.gasUsed = options.longs === $String ? $util.Long.prototype.toString.call(message.gasUsed) : options.longs === $Number ? new $util.LongBits(message.gasUsed.low >>> 0, message.gasUsed.high >>> 0).toNumber(true) : message.gasUsed;
            if (message.internalCallContext != null && $Object.hasOwnProperty.call(message, "internalCallContext"))
                object.internalCallContext = $root.proto.InternalCallContext.toObject(message.internalCallContext, options, _depth + 1);
            if (message.executedHookId != null && $Object.hasOwnProperty.call(message, "executedHookId"))
                object.executedHookId = $root.proto.HookId.toObject(message.executedHookId, options, _depth + 1);
            if (message.contractNonces && message.contractNonces.length) {
                object.contractNonces = $Array(message.contractNonces.length);
                for (let j = 0; j < message.contractNonces.length; ++j)
                    object.contractNonces[j] = $root.proto.ContractNonceInfo.toObject(message.contractNonces[j], options, _depth + 1);
            }
            if (message.signerNonce != null && $Object.hasOwnProperty.call(message, "signerNonce"))
                object.signerNonce = $root.google.protobuf.Int64Value.toObject(message.signerNonce, options, _depth + 1);
            return object;
        };

        /**
         * Converts this EvmTransactionResult to JSON.
         * @function toJSON
         * @memberof proto.EvmTransactionResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EvmTransactionResult.prototype.toJSON = function() {
            return EvmTransactionResult.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EvmTransactionResult
         * @function getTypeUrl
         * @memberof proto.EvmTransactionResult
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EvmTransactionResult.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EvmTransactionResult";
        };

        return EvmTransactionResult;
    })();

    proto.ContractNonceInfo = (function() {

        /**
         * Properties of a ContractNonceInfo.
         * @typedef {Object} proto.ContractNonceInfo.$Properties
         * @property {proto.ContractID.$Properties|null} [contractId] A contract identifier.<br/>
         * This refers to the contract that holds this nonce value.
         * @property {number|Long|null} [nonce] A "nonce" value.
         * The current value of the nonce associated with the identified contract.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractNonceInfo.
         * @memberof proto
         * @interface IContractNonceInfo
         * @augments proto.ContractNonceInfo.$Properties
         * @deprecated Use proto.ContractNonceInfo.$Properties instead.
         */

        /**
         * Shape of a ContractNonceInfo.
         * @typedef {{
         *   contractId?: proto.ContractID.$Shape|null;
         *   nonce?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractNonceInfo.$Shape
         */

        /**
         * Constructs a new ContractNonceInfo.
         * @memberof proto
         * @classdesc A contract "nonce" reference.<br/>
         * This connects a contract and its "nonce" value, and is primarily for use in
         * query responses.  A "nonce" is short for "nonsense" and is usually a value
         * with no particular meaning.
         * 
         * The nonce of a contract SHALL be incremented when that contract creates
         * another contract.
         * @constructor
         * @param {proto.ContractNonceInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractNonceInfo = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A contract identifier.<br/>
         * This refers to the contract that holds this nonce value.
         * @member {proto.ContractID.$Properties|null|undefined} contractId
         * @memberof proto.ContractNonceInfo
         * @instance
         */
        ContractNonceInfo.prototype.contractId = null;

        /**
         * A "nonce" value.
         * The current value of the nonce associated with the identified contract.
         * @member {number|Long} nonce
         * @memberof proto.ContractNonceInfo
         * @instance
         */
        ContractNonceInfo.prototype.nonce = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ContractNonceInfo instance using the specified properties.
         * @function create
         * @memberof proto.ContractNonceInfo
         * @static
         * @param {proto.ContractNonceInfo.$Properties=} [properties] Properties to set
         * @returns {proto.ContractNonceInfo} ContractNonceInfo instance
         * @type {{
         *   (properties: proto.ContractNonceInfo.$Shape): proto.ContractNonceInfo & proto.ContractNonceInfo.$Shape;
         *   (properties?: proto.ContractNonceInfo.$Properties): proto.ContractNonceInfo;
         * }}
         */
        ContractNonceInfo.create = function(properties) {
            return new ContractNonceInfo(properties);
        };

        /**
         * Encodes the specified ContractNonceInfo message. Does not implicitly {@link proto.ContractNonceInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractNonceInfo
         * @static
         * @param {proto.ContractNonceInfo.$Properties} message ContractNonceInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractNonceInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId"))
                $root.proto.ContractID.encode(message.contractId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce") && (typeof message.nonce === "object" ? message.nonce.low || message.nonce.high : message.nonce !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.nonce);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractNonceInfo message, length delimited. Does not implicitly {@link proto.ContractNonceInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractNonceInfo
         * @static
         * @param {proto.ContractNonceInfo.$Properties} message ContractNonceInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractNonceInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractNonceInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractNonceInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractNonceInfo & proto.ContractNonceInfo.$Shape} ContractNonceInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractNonceInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractNonceInfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.contractId = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nonce = value;
                        else
                            delete message.nonce;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractNonceInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractNonceInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractNonceInfo & proto.ContractNonceInfo.$Shape} ContractNonceInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractNonceInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractNonceInfo message.
         * @function verify
         * @memberof proto.ContractNonceInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractNonceInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId")) {
                let error = $root.proto.ContractID.verify(message.contractId, _depth + 1);
                if (error)
                    return "contractId." + error;
            }
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce"))
                if (!$util.isInteger(message.nonce) && !(message.nonce && $util.isInteger(message.nonce.low) && $util.isInteger(message.nonce.high)))
                    return "nonce: integer|Long expected";
            return null;
        };

        /**
         * Creates a ContractNonceInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractNonceInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractNonceInfo} ContractNonceInfo
         */
        ContractNonceInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractNonceInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractNonceInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractNonceInfo();
            if (object.contractId != null) {
                if (!$util.isObject(object.contractId))
                    throw $TypeError(".proto.ContractNonceInfo.contractId: object expected");
                message.contractId = $root.proto.ContractID.fromObject(object.contractId, _depth + 1);
            }
            if (object.nonce != null)
                if (typeof object.nonce === "object" ? object.nonce.low || object.nonce.high : $Number(object.nonce) !== 0)
                    if ($util.Long)
                        message.nonce = $util.Long.fromValue(object.nonce, false);
                    else if (typeof object.nonce === "string")
                        message.nonce = $parseInt(object.nonce, 10);
                    else if (typeof object.nonce === "number")
                        message.nonce = object.nonce;
                    else if (typeof object.nonce === "object")
                        message.nonce = new $util.LongBits(object.nonce.low >>> 0, object.nonce.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ContractNonceInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractNonceInfo
         * @static
         * @param {proto.ContractNonceInfo} message ContractNonceInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractNonceInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.contractId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nonce = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nonce = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId"))
                object.contractId = $root.proto.ContractID.toObject(message.contractId, options, _depth + 1);
            if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nonce = typeof message.nonce === "number" ? $BigInt(message.nonce) : $util.Long.fromBits(message.nonce.low >>> 0, message.nonce.high >>> 0, false).toBigInt();
                else if (typeof message.nonce === "number")
                    object.nonce = options.longs === $String ? $String(message.nonce) : message.nonce;
                else
                    object.nonce = options.longs === $String ? $util.Long.prototype.toString.call(message.nonce) : options.longs === $Number ? new $util.LongBits(message.nonce.low >>> 0, message.nonce.high >>> 0).toNumber() : message.nonce;
            return object;
        };

        /**
         * Converts this ContractNonceInfo to JSON.
         * @function toJSON
         * @memberof proto.ContractNonceInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractNonceInfo.prototype.toJSON = function() {
            return ContractNonceInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractNonceInfo
         * @function getTypeUrl
         * @memberof proto.ContractNonceInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractNonceInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractNonceInfo";
        };

        return ContractNonceInfo;
    })();

    proto.ContractLoginfo = (function() {

        /**
         * Properties of a ContractLoginfo.
         * @typedef {Object} proto.ContractLoginfo.$Properties
         * @property {proto.ContractID.$Properties|null} [contractID] A contract identifier.<br/>
         * This refers to the contract that generated this log entry.
         * @property {Uint8Array|null} [bloom] A bloom filter.<br/>
         * This filter applies to this log entry and indexes the contract log data
         * in the full data of the Ethereum block.<br/>
         * EIP-7668 proposes to remove bloom filters as they are quite low value
         * in practice and separate indexing services are more effective.
         * @property {Array.<Uint8Array>|null} [topic] A list of the "topics" in this log entry.<br/>
         * The EVM permits up to 4 topics, each of which is 32 bytes (one EVM word).
         * <p>
         * The first "topic" is special, and MUST contain the keccak256 hash of the
         * event signature, if the event is not anonymous.
         * @property {Uint8Array|null} [data] Event data for this log entry.<br/>
         * This is binary data consisting of an arbitrary number of 256 bit
         * (32 byte) words. The content of that data is determined by the smart
         * contract code.<br/>
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractLoginfo.
         * @memberof proto
         * @interface IContractLoginfo
         * @augments proto.ContractLoginfo.$Properties
         * @deprecated Use proto.ContractLoginfo.$Properties instead.
         */

        /**
         * Shape of a ContractLoginfo.
         * @typedef {{
         *   contractID?: proto.ContractID.$Shape|null;
         *   bloom?: Uint8Array|null;
         *   topic?: Array.<Uint8Array>|null;
         *   data?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractLoginfo.$Shape
         */

        /**
         * Constructs a new ContractLoginfo.
         * @memberof proto
         * @classdesc EVM log data for a contract call.<br/>
         * The EVM log information produced by a smart contract function call.
         * 
         * Each contract function call MAY return zero or more log events.
         * @constructor
         * @param {proto.ContractLoginfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractLoginfo = function (properties) {
            this.topic = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A contract identifier.<br/>
         * This refers to the contract that generated this log entry.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.ContractLoginfo
         * @instance
         */
        ContractLoginfo.prototype.contractID = null;

        /**
         * A bloom filter.<br/>
         * This filter applies to this log entry and indexes the contract log data
         * in the full data of the Ethereum block.<br/>
         * EIP-7668 proposes to remove bloom filters as they are quite low value
         * in practice and separate indexing services are more effective.
         * @member {Uint8Array} bloom
         * @memberof proto.ContractLoginfo
         * @instance
         */
        ContractLoginfo.prototype.bloom = $util.newBuffer([]);

        /**
         * A list of the "topics" in this log entry.<br/>
         * The EVM permits up to 4 topics, each of which is 32 bytes (one EVM word).
         * <p>
         * The first "topic" is special, and MUST contain the keccak256 hash of the
         * event signature, if the event is not anonymous.
         * @member {Array.<Uint8Array>} topic
         * @memberof proto.ContractLoginfo
         * @instance
         */
        ContractLoginfo.prototype.topic = $util.emptyArray;

        /**
         * Event data for this log entry.<br/>
         * This is binary data consisting of an arbitrary number of 256 bit
         * (32 byte) words. The content of that data is determined by the smart
         * contract code.<br/>
         * @member {Uint8Array} data
         * @memberof proto.ContractLoginfo
         * @instance
         */
        ContractLoginfo.prototype.data = $util.newBuffer([]);

        /**
         * Creates a new ContractLoginfo instance using the specified properties.
         * @function create
         * @memberof proto.ContractLoginfo
         * @static
         * @param {proto.ContractLoginfo.$Properties=} [properties] Properties to set
         * @returns {proto.ContractLoginfo} ContractLoginfo instance
         * @type {{
         *   (properties: proto.ContractLoginfo.$Shape): proto.ContractLoginfo & proto.ContractLoginfo.$Shape;
         *   (properties?: proto.ContractLoginfo.$Properties): proto.ContractLoginfo;
         * }}
         */
        ContractLoginfo.create = function(properties) {
            return new ContractLoginfo(properties);
        };

        /**
         * Encodes the specified ContractLoginfo message. Does not implicitly {@link proto.ContractLoginfo.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractLoginfo
         * @static
         * @param {proto.ContractLoginfo.$Properties} message ContractLoginfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractLoginfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.bloom != null && $Object.hasOwnProperty.call(message, "bloom") && message.bloom.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.bloom);
            if (message.topic != null && message.topic.length)
                for (let i = 0; i < message.topic.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.topic[i]);
            if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractLoginfo message, length delimited. Does not implicitly {@link proto.ContractLoginfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractLoginfo
         * @static
         * @param {proto.ContractLoginfo.$Properties} message ContractLoginfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractLoginfo.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractLoginfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractLoginfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractLoginfo & proto.ContractLoginfo.$Shape} ContractLoginfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractLoginfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractLoginfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.bloom = value;
                        else
                            delete message.bloom;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.topic && message.topic.length))
                            message.topic = [];
                        message.topic.push(reader.bytes());
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractLoginfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractLoginfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractLoginfo & proto.ContractLoginfo.$Shape} ContractLoginfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractLoginfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractLoginfo message.
         * @function verify
         * @memberof proto.ContractLoginfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractLoginfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            if (message.bloom != null && $Object.hasOwnProperty.call(message, "bloom"))
                if (!(message.bloom && typeof message.bloom.length === "number" || $util.isString(message.bloom)))
                    return "bloom: buffer expected";
            if (message.topic != null && $Object.hasOwnProperty.call(message, "topic")) {
                if (!$Array.isArray(message.topic))
                    return "topic: array expected";
                for (let i = 0; i < message.topic.length; ++i)
                    if (!(message.topic[i] && typeof message.topic[i].length === "number" || $util.isString(message.topic[i])))
                        return "topic: buffer[] expected";
            }
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            return null;
        };

        /**
         * Creates a ContractLoginfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractLoginfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractLoginfo} ContractLoginfo
         */
        ContractLoginfo.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractLoginfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractLoginfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractLoginfo();
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.ContractLoginfo.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            if (object.bloom != null)
                if (object.bloom.length)
                    if (typeof object.bloom === "string")
                        $util.base64.decode(object.bloom, message.bloom = $util.newBuffer($util.base64.length(object.bloom)), 0);
                    else if (object.bloom.length >= 0)
                        message.bloom = object.bloom;
            if (object.topic) {
                if (!$Array.isArray(object.topic))
                    throw $TypeError(".proto.ContractLoginfo.topic: array expected");
                message.topic = $Array(object.topic.length);
                for (let i = 0; i < object.topic.length; ++i)
                    if (typeof object.topic[i] === "string")
                        $util.base64.decode(object.topic[i], message.topic[i] = $util.newBuffer($util.base64.length(object.topic[i])), 0);
                    else if (object.topic[i].length >= 0)
                        message.topic[i] = object.topic[i];
            }
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            return message;
        };

        /**
         * Creates a plain object from a ContractLoginfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractLoginfo
         * @static
         * @param {proto.ContractLoginfo} message ContractLoginfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractLoginfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.topic = [];
            if (options.defaults) {
                object.contractID = null;
                if (options.bytes === $String)
                    object.bloom = "";
                else {
                    object.bloom = [];
                    if (options.bytes !== $Array)
                        object.bloom = $util.newBuffer(object.bloom);
                }
                if (options.bytes === $String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== $Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            if (message.bloom != null && $Object.hasOwnProperty.call(message, "bloom"))
                object.bloom = options.bytes === $String ? $util.base64.encode(message.bloom, 0, message.bloom.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.bloom) : message.bloom;
            if (message.topic && message.topic.length) {
                object.topic = $Array(message.topic.length);
                for (let j = 0; j < message.topic.length; ++j)
                    object.topic[j] = options.bytes === $String ? $util.base64.encode(message.topic[j], 0, message.topic[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.topic[j]) : message.topic[j];
            }
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === $String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        /**
         * Converts this ContractLoginfo to JSON.
         * @function toJSON
         * @memberof proto.ContractLoginfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractLoginfo.prototype.toJSON = function() {
            return ContractLoginfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractLoginfo
         * @function getTypeUrl
         * @memberof proto.ContractLoginfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractLoginfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractLoginfo";
        };

        return ContractLoginfo;
    })();

    proto.ContractFunctionResult = (function() {

        /**
         * Properties of a ContractFunctionResult.
         * @typedef {Object} proto.ContractFunctionResult.$Properties
         * @property {proto.ContractID.$Properties|null} [contractID] A contract identifier.<br/>
         * This identifies the smart contract that defines the function called.
         * @property {Uint8Array|null} [contractCallResult] Result data from the function call.
         * <p>
         * This SHALL be encoded in RLP bytecode format.
         * @property {string|null} [errorMessage] Any error message produced by the contract call.
         * <p>
         * This SHALL be unset if the contract call succeeded.
         * @property {Uint8Array|null} [bloom] A bloom filter produced by this contract call.<br/>
         * Ethereum uses this bloom filter to search for call results in the
         * Ethereum block history. High false positive rates make the bloom
         * filters quite limited value.
         * @property {number|Long|null} [gasUsed] A quantity of "gas" used.<br/>
         * This represents the resource units expended to execute this
         * contract call, and correlates to transaction costs.
         * @property {Array.<proto.ContractLoginfo.$Properties>|null} [logInfo] Any Log events produced by this contract call.
         * @property {Array.<proto.ContractID.$Properties>|null} [createdContractIDs] Replaced by values in transaction records to support
         * `CREATE2` calls.<br/>
         * <p>
         * The list of smart contracts that were created by the function call.<br/>
         * The created ids will now _also_ be externalized through internal
         * transaction records, where each record has its alias field populated
         * with the new contract's EVM address.<br/>
         * This is needed for contracts created with CREATE2, which removes the
         * trivial relationship between a new contract's Identifier and its
         * Solidity address.
         * @property {google.protobuf.BytesValue.$Properties|null} [evmAddress] A created contract address.<br/>
         * If the function created a new contract (e.g. `CREATE2`),
         * this is the primary 20-byte EVM address for that contract.
         * <p>
         * Every contract SHALL have a "base" EVM address that is determined by its
         * `shard.realm.num` contract ID.<br/>
         * This address is constructed as follows
         * <ol>
         * <li>The first 4 bytes are the big-endian
         * representation of the shard.</li>
         * <li>The next 8 bytes are the big-endian
         * representation of the realm.</li>
         * <li>The final 8 bytes are the big-endian
         * representation of the number.</li>
         * </ol>
         * <p>
         * Contracts created via `CREATE2` SHALL have an _additional_, primary,
         * address that is derived from the
         * <a href="https://eips.ethereum.org/EIPS/eip-1014"><tt>EIP-1014</tt></a>
         * specification. This additional address SHALL NOT be directly related to
         * the `shard.realm.num` contract ID.<br/>
         * It should be emphasized that Contracts created via a `CREATE2` call can
         * also be referenced via the same "base" EVM address as described above.
         * @property {number|Long|null} [gas] The amount of gas available for this call, sometimes referred to as the
         * gasLimit.<br/>
         * This field SHALL NOT be populated when the associated `TransactionBody`
         * in the block stream is a `ContractCreateTransactionBody` or
         * a `ContractCallTransactionBody`.
         * @property {number|Long|null} [amount] An amount, in tinybar, sent by this function call.<br/>
         * This SHALL be zero(0) if the function called is not `payable`.<br/>
         * This field SHALL NOT be populated when the associated `TransactionBody`
         * in the block stream is a `ContractCreateTransactionBody` or
         * a `ContractCallTransactionBody`.
         * @property {Uint8Array|null} [functionParameters] The smart contract function to call, and the parameters to pass to that
         * function.<br/>
         * These SHALL be presented in EVM bytecode function call format.<br/>
         * This field SHALL NOT be populated when the associated `TransactionBody`
         * in the block stream is a `ContractCreateTransactionBody` or
         * a `ContractCallTransactionBody`.
         * @property {proto.AccountID.$Properties|null} [senderId] The account that was the "sender" for this contract call.<br/>
         * If this is not set it SHALL be read from the accountId in the
         * transactionId for the contract call.<br/>
         * This field SHALL NOT be populated when the associated `TransactionBody`
         * in the block stream is a `ContractCreateTransactionBody` or
         * a `ContractCallTransactionBody`.
         * @property {Array.<proto.ContractNonceInfo.$Properties>|null} [contractNonces] A list of contract account nonce values.<br/>
         * This list SHALL contain a nonce value for each contract account modified
         * as a result of this contract call. These nonce values SHALL be the value
         * after the contract call is completed.
         * @property {google.protobuf.Int64Value.$Properties|null} [signerNonce] A nonce value for the "signer account".<br/>
         * If the contract call updated the signer nonce for the signer account
         * (i.e. by creating another contract), this field SHALL contain the
         * updated value.<br/>
         * If the signer account nonce was not updated, this field SHALL be `null`.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractFunctionResult.
         * @memberof proto
         * @interface IContractFunctionResult
         * @augments proto.ContractFunctionResult.$Properties
         * @deprecated Use proto.ContractFunctionResult.$Properties instead.
         */

        /**
         * Shape of a ContractFunctionResult.
         * @typedef {{
         *   contractID?: proto.ContractID.$Shape|null;
         *   contractCallResult?: Uint8Array|null;
         *   errorMessage?: string|null;
         *   bloom?: Uint8Array|null;
         *   gasUsed?: number|Long|null;
         *   logInfo?: Array.<proto.ContractLoginfo.$Shape>|null;
         *   createdContractIDs?: Array.<proto.ContractID.$Shape>|null;
         *   evmAddress?: google.protobuf.BytesValue.$Shape|null;
         *   gas?: number|Long|null;
         *   amount?: number|Long|null;
         *   functionParameters?: Uint8Array|null;
         *   senderId?: proto.AccountID.$Shape|null;
         *   contractNonces?: Array.<proto.ContractNonceInfo.$Shape>|null;
         *   signerNonce?: google.protobuf.Int64Value.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractFunctionResult.$Shape
         */

        /**
         * Constructs a new ContractFunctionResult.
         * @memberof proto
         * @classdesc A contract function result.<br/>
         * The result returned by a call to a smart contract function. This is part of
         * the response to a ContractCallLocal query, and is in the record for a
         * ContractCall. The ContractCreateInstance transaction record also carries a
         * function result, which is the results of the call to the constructor.
         * @constructor
         * @param {proto.ContractFunctionResult.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractFunctionResult = function (properties) {
            this.logInfo = [];
            this.createdContractIDs = [];
            this.contractNonces = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A contract identifier.<br/>
         * This identifies the smart contract that defines the function called.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.contractID = null;

        /**
         * Result data from the function call.
         * <p>
         * This SHALL be encoded in RLP bytecode format.
         * @member {Uint8Array} contractCallResult
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.contractCallResult = $util.newBuffer([]);

        /**
         * Any error message produced by the contract call.
         * <p>
         * This SHALL be unset if the contract call succeeded.
         * @member {string} errorMessage
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.errorMessage = "";

        /**
         * A bloom filter produced by this contract call.<br/>
         * Ethereum uses this bloom filter to search for call results in the
         * Ethereum block history. High false positive rates make the bloom
         * filters quite limited value.
         * @member {Uint8Array} bloom
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.bloom = $util.newBuffer([]);

        /**
         * A quantity of "gas" used.<br/>
         * This represents the resource units expended to execute this
         * contract call, and correlates to transaction costs.
         * @member {number|Long} gasUsed
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.gasUsed = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Any Log events produced by this contract call.
         * @member {Array.<proto.ContractLoginfo.$Properties>} logInfo
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.logInfo = $util.emptyArray;

        /**
         * Replaced by values in transaction records to support
         * `CREATE2` calls.<br/>
         * <p>
         * The list of smart contracts that were created by the function call.<br/>
         * The created ids will now _also_ be externalized through internal
         * transaction records, where each record has its alias field populated
         * with the new contract's EVM address.<br/>
         * This is needed for contracts created with CREATE2, which removes the
         * trivial relationship between a new contract's Identifier and its
         * Solidity address.
         * @member {Array.<proto.ContractID.$Properties>} createdContractIDs
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.createdContractIDs = $util.emptyArray;

        /**
         * A created contract address.<br/>
         * If the function created a new contract (e.g. `CREATE2`),
         * this is the primary 20-byte EVM address for that contract.
         * <p>
         * Every contract SHALL have a "base" EVM address that is determined by its
         * `shard.realm.num` contract ID.<br/>
         * This address is constructed as follows
         * <ol>
         * <li>The first 4 bytes are the big-endian
         * representation of the shard.</li>
         * <li>The next 8 bytes are the big-endian
         * representation of the realm.</li>
         * <li>The final 8 bytes are the big-endian
         * representation of the number.</li>
         * </ol>
         * <p>
         * Contracts created via `CREATE2` SHALL have an _additional_, primary,
         * address that is derived from the
         * <a href="https://eips.ethereum.org/EIPS/eip-1014"><tt>EIP-1014</tt></a>
         * specification. This additional address SHALL NOT be directly related to
         * the `shard.realm.num` contract ID.<br/>
         * It should be emphasized that Contracts created via a `CREATE2` call can
         * also be referenced via the same "base" EVM address as described above.
         * @member {google.protobuf.BytesValue.$Properties|null|undefined} evmAddress
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.evmAddress = null;

        /**
         * The amount of gas available for this call, sometimes referred to as the
         * gasLimit.<br/>
         * This field SHALL NOT be populated when the associated `TransactionBody`
         * in the block stream is a `ContractCreateTransactionBody` or
         * a `ContractCallTransactionBody`.
         * @member {number|Long} gas
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.gas = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An amount, in tinybar, sent by this function call.<br/>
         * This SHALL be zero(0) if the function called is not `payable`.<br/>
         * This field SHALL NOT be populated when the associated `TransactionBody`
         * in the block stream is a `ContractCreateTransactionBody` or
         * a `ContractCallTransactionBody`.
         * @member {number|Long} amount
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The smart contract function to call, and the parameters to pass to that
         * function.<br/>
         * These SHALL be presented in EVM bytecode function call format.<br/>
         * This field SHALL NOT be populated when the associated `TransactionBody`
         * in the block stream is a `ContractCreateTransactionBody` or
         * a `ContractCallTransactionBody`.
         * @member {Uint8Array} functionParameters
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.functionParameters = $util.newBuffer([]);

        /**
         * The account that was the "sender" for this contract call.<br/>
         * If this is not set it SHALL be read from the accountId in the
         * transactionId for the contract call.<br/>
         * This field SHALL NOT be populated when the associated `TransactionBody`
         * in the block stream is a `ContractCreateTransactionBody` or
         * a `ContractCallTransactionBody`.
         * @member {proto.AccountID.$Properties|null|undefined} senderId
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.senderId = null;

        /**
         * A list of contract account nonce values.<br/>
         * This list SHALL contain a nonce value for each contract account modified
         * as a result of this contract call. These nonce values SHALL be the value
         * after the contract call is completed.
         * @member {Array.<proto.ContractNonceInfo.$Properties>} contractNonces
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.contractNonces = $util.emptyArray;

        /**
         * A nonce value for the "signer account".<br/>
         * If the contract call updated the signer nonce for the signer account
         * (i.e. by creating another contract), this field SHALL contain the
         * updated value.<br/>
         * If the signer account nonce was not updated, this field SHALL be `null`.
         * @member {google.protobuf.Int64Value.$Properties|null|undefined} signerNonce
         * @memberof proto.ContractFunctionResult
         * @instance
         */
        ContractFunctionResult.prototype.signerNonce = null;

        /**
         * Creates a new ContractFunctionResult instance using the specified properties.
         * @function create
         * @memberof proto.ContractFunctionResult
         * @static
         * @param {proto.ContractFunctionResult.$Properties=} [properties] Properties to set
         * @returns {proto.ContractFunctionResult} ContractFunctionResult instance
         * @type {{
         *   (properties: proto.ContractFunctionResult.$Shape): proto.ContractFunctionResult & proto.ContractFunctionResult.$Shape;
         *   (properties?: proto.ContractFunctionResult.$Properties): proto.ContractFunctionResult;
         * }}
         */
        ContractFunctionResult.create = function(properties) {
            return new ContractFunctionResult(properties);
        };

        /**
         * Encodes the specified ContractFunctionResult message. Does not implicitly {@link proto.ContractFunctionResult.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractFunctionResult
         * @static
         * @param {proto.ContractFunctionResult.$Properties} message ContractFunctionResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractFunctionResult.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.contractCallResult != null && $Object.hasOwnProperty.call(message, "contractCallResult") && message.contractCallResult.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.contractCallResult);
            if (message.errorMessage != null && $Object.hasOwnProperty.call(message, "errorMessage") && message.errorMessage !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.errorMessage);
            if (message.bloom != null && $Object.hasOwnProperty.call(message, "bloom") && message.bloom.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.bloom);
            if (message.gasUsed != null && $Object.hasOwnProperty.call(message, "gasUsed") && (typeof message.gasUsed === "object" ? message.gasUsed.low || message.gasUsed.high : message.gasUsed !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.gasUsed);
            if (message.logInfo != null && message.logInfo.length)
                for (let i = 0; i < message.logInfo.length; ++i)
                    $root.proto.ContractLoginfo.encode(message.logInfo[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.createdContractIDs != null && message.createdContractIDs.length)
                for (let i = 0; i < message.createdContractIDs.length; ++i)
                    $root.proto.ContractID.encode(message.createdContractIDs[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.evmAddress != null && $Object.hasOwnProperty.call(message, "evmAddress"))
                $root.google.protobuf.BytesValue.encode(message.evmAddress, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas") && (typeof message.gas === "object" ? message.gas.low || message.gas.high : message.gas !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.gas);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.amount);
            if (message.functionParameters != null && $Object.hasOwnProperty.call(message, "functionParameters") && message.functionParameters.length)
                writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.functionParameters);
            if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                $root.proto.AccountID.encode(message.senderId, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.contractNonces != null && message.contractNonces.length)
                for (let i = 0; i < message.contractNonces.length; ++i)
                    $root.proto.ContractNonceInfo.encode(message.contractNonces[i], writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.signerNonce != null && $Object.hasOwnProperty.call(message, "signerNonce"))
                $root.google.protobuf.Int64Value.encode(message.signerNonce, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractFunctionResult message, length delimited. Does not implicitly {@link proto.ContractFunctionResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractFunctionResult
         * @static
         * @param {proto.ContractFunctionResult.$Properties} message ContractFunctionResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractFunctionResult.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractFunctionResult message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractFunctionResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractFunctionResult & proto.ContractFunctionResult.$Shape} ContractFunctionResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractFunctionResult.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractFunctionResult();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.contractCallResult = value;
                        else
                            delete message.contractCallResult;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.errorMessage = value;
                        else
                            delete message.errorMessage;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.bloom = value;
                        else
                            delete message.bloom;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.gasUsed = value;
                        else
                            delete message.gasUsed;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.logInfo && message.logInfo.length))
                            message.logInfo = [];
                        message.logInfo.push($root.proto.ContractLoginfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.createdContractIDs && message.createdContractIDs.length))
                            message.createdContractIDs = [];
                        message.createdContractIDs.push($root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.evmAddress = $root.google.protobuf.BytesValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.evmAddress);
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.gas = value;
                        else
                            delete message.gas;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.functionParameters = value;
                        else
                            delete message.functionParameters;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.senderId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.senderId);
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.contractNonces && message.contractNonces.length))
                            message.contractNonces = [];
                        message.contractNonces.push($root.proto.ContractNonceInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.signerNonce = $root.google.protobuf.Int64Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.signerNonce);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractFunctionResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractFunctionResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractFunctionResult & proto.ContractFunctionResult.$Shape} ContractFunctionResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractFunctionResult.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractFunctionResult message.
         * @function verify
         * @memberof proto.ContractFunctionResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractFunctionResult.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            if (message.contractCallResult != null && $Object.hasOwnProperty.call(message, "contractCallResult"))
                if (!(message.contractCallResult && typeof message.contractCallResult.length === "number" || $util.isString(message.contractCallResult)))
                    return "contractCallResult: buffer expected";
            if (message.errorMessage != null && $Object.hasOwnProperty.call(message, "errorMessage"))
                if (!$util.isString(message.errorMessage))
                    return "errorMessage: string expected";
            if (message.bloom != null && $Object.hasOwnProperty.call(message, "bloom"))
                if (!(message.bloom && typeof message.bloom.length === "number" || $util.isString(message.bloom)))
                    return "bloom: buffer expected";
            if (message.gasUsed != null && $Object.hasOwnProperty.call(message, "gasUsed"))
                if (!$util.isInteger(message.gasUsed) && !(message.gasUsed && $util.isInteger(message.gasUsed.low) && $util.isInteger(message.gasUsed.high)))
                    return "gasUsed: integer|Long expected";
            if (message.logInfo != null && $Object.hasOwnProperty.call(message, "logInfo")) {
                if (!$Array.isArray(message.logInfo))
                    return "logInfo: array expected";
                for (let i = 0; i < message.logInfo.length; ++i) {
                    let error = $root.proto.ContractLoginfo.verify(message.logInfo[i], _depth + 1);
                    if (error)
                        return "logInfo." + error;
                }
            }
            if (message.createdContractIDs != null && $Object.hasOwnProperty.call(message, "createdContractIDs")) {
                if (!$Array.isArray(message.createdContractIDs))
                    return "createdContractIDs: array expected";
                for (let i = 0; i < message.createdContractIDs.length; ++i) {
                    let error = $root.proto.ContractID.verify(message.createdContractIDs[i], _depth + 1);
                    if (error)
                        return "createdContractIDs." + error;
                }
            }
            if (message.evmAddress != null && $Object.hasOwnProperty.call(message, "evmAddress")) {
                let error = $root.google.protobuf.BytesValue.verify(message.evmAddress, _depth + 1);
                if (error)
                    return "evmAddress." + error;
            }
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (!$util.isInteger(message.gas) && !(message.gas && $util.isInteger(message.gas.low) && $util.isInteger(message.gas.high)))
                    return "gas: integer|Long expected";
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.functionParameters != null && $Object.hasOwnProperty.call(message, "functionParameters"))
                if (!(message.functionParameters && typeof message.functionParameters.length === "number" || $util.isString(message.functionParameters)))
                    return "functionParameters: buffer expected";
            if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId")) {
                let error = $root.proto.AccountID.verify(message.senderId, _depth + 1);
                if (error)
                    return "senderId." + error;
            }
            if (message.contractNonces != null && $Object.hasOwnProperty.call(message, "contractNonces")) {
                if (!$Array.isArray(message.contractNonces))
                    return "contractNonces: array expected";
                for (let i = 0; i < message.contractNonces.length; ++i) {
                    let error = $root.proto.ContractNonceInfo.verify(message.contractNonces[i], _depth + 1);
                    if (error)
                        return "contractNonces." + error;
                }
            }
            if (message.signerNonce != null && $Object.hasOwnProperty.call(message, "signerNonce")) {
                let error = $root.google.protobuf.Int64Value.verify(message.signerNonce, _depth + 1);
                if (error)
                    return "signerNonce." + error;
            }
            return null;
        };

        /**
         * Creates a ContractFunctionResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractFunctionResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractFunctionResult} ContractFunctionResult
         */
        ContractFunctionResult.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractFunctionResult)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractFunctionResult: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractFunctionResult();
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.ContractFunctionResult.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            if (object.contractCallResult != null)
                if (object.contractCallResult.length)
                    if (typeof object.contractCallResult === "string")
                        $util.base64.decode(object.contractCallResult, message.contractCallResult = $util.newBuffer($util.base64.length(object.contractCallResult)), 0);
                    else if (object.contractCallResult.length >= 0)
                        message.contractCallResult = object.contractCallResult;
            if (object.errorMessage != null)
                if (typeof object.errorMessage !== "string" || object.errorMessage.length)
                    message.errorMessage = $String(object.errorMessage);
            if (object.bloom != null)
                if (object.bloom.length)
                    if (typeof object.bloom === "string")
                        $util.base64.decode(object.bloom, message.bloom = $util.newBuffer($util.base64.length(object.bloom)), 0);
                    else if (object.bloom.length >= 0)
                        message.bloom = object.bloom;
            if (object.gasUsed != null)
                if (typeof object.gasUsed === "object" ? object.gasUsed.low || object.gasUsed.high : $Number(object.gasUsed) !== 0)
                    if ($util.Long)
                        message.gasUsed = $util.Long.fromValue(object.gasUsed, true);
                    else if (typeof object.gasUsed === "string")
                        message.gasUsed = $parseInt(object.gasUsed, 10);
                    else if (typeof object.gasUsed === "number")
                        message.gasUsed = object.gasUsed;
                    else if (typeof object.gasUsed === "object")
                        message.gasUsed = new $util.LongBits(object.gasUsed.low >>> 0, object.gasUsed.high >>> 0).toNumber(true);
            if (object.logInfo) {
                if (!$Array.isArray(object.logInfo))
                    throw $TypeError(".proto.ContractFunctionResult.logInfo: array expected");
                message.logInfo = $Array(object.logInfo.length);
                for (let i = 0; i < object.logInfo.length; ++i) {
                    if (!$util.isObject(object.logInfo[i]))
                        throw $TypeError(".proto.ContractFunctionResult.logInfo: object expected");
                    message.logInfo[i] = $root.proto.ContractLoginfo.fromObject(object.logInfo[i], _depth + 1);
                }
            }
            if (object.createdContractIDs) {
                if (!$Array.isArray(object.createdContractIDs))
                    throw $TypeError(".proto.ContractFunctionResult.createdContractIDs: array expected");
                message.createdContractIDs = $Array(object.createdContractIDs.length);
                for (let i = 0; i < object.createdContractIDs.length; ++i) {
                    if (!$util.isObject(object.createdContractIDs[i]))
                        throw $TypeError(".proto.ContractFunctionResult.createdContractIDs: object expected");
                    message.createdContractIDs[i] = $root.proto.ContractID.fromObject(object.createdContractIDs[i], _depth + 1);
                }
            }
            if (object.evmAddress != null) {
                if (!$util.isObject(object.evmAddress))
                    throw $TypeError(".proto.ContractFunctionResult.evmAddress: object expected");
                message.evmAddress = $root.google.protobuf.BytesValue.fromObject(object.evmAddress, _depth + 1);
            }
            if (object.gas != null)
                if (typeof object.gas === "object" ? object.gas.low || object.gas.high : $Number(object.gas) !== 0)
                    if ($util.Long)
                        message.gas = $util.Long.fromValue(object.gas, false);
                    else if (typeof object.gas === "string")
                        message.gas = $parseInt(object.gas, 10);
                    else if (typeof object.gas === "number")
                        message.gas = object.gas;
                    else if (typeof object.gas === "object")
                        message.gas = new $util.LongBits(object.gas.low >>> 0, object.gas.high >>> 0).toNumber();
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            if (object.functionParameters != null)
                if (object.functionParameters.length)
                    if (typeof object.functionParameters === "string")
                        $util.base64.decode(object.functionParameters, message.functionParameters = $util.newBuffer($util.base64.length(object.functionParameters)), 0);
                    else if (object.functionParameters.length >= 0)
                        message.functionParameters = object.functionParameters;
            if (object.senderId != null) {
                if (!$util.isObject(object.senderId))
                    throw $TypeError(".proto.ContractFunctionResult.senderId: object expected");
                message.senderId = $root.proto.AccountID.fromObject(object.senderId, _depth + 1);
            }
            if (object.contractNonces) {
                if (!$Array.isArray(object.contractNonces))
                    throw $TypeError(".proto.ContractFunctionResult.contractNonces: array expected");
                message.contractNonces = $Array(object.contractNonces.length);
                for (let i = 0; i < object.contractNonces.length; ++i) {
                    if (!$util.isObject(object.contractNonces[i]))
                        throw $TypeError(".proto.ContractFunctionResult.contractNonces: object expected");
                    message.contractNonces[i] = $root.proto.ContractNonceInfo.fromObject(object.contractNonces[i], _depth + 1);
                }
            }
            if (object.signerNonce != null) {
                if (!$util.isObject(object.signerNonce))
                    throw $TypeError(".proto.ContractFunctionResult.signerNonce: object expected");
                message.signerNonce = $root.google.protobuf.Int64Value.fromObject(object.signerNonce, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractFunctionResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractFunctionResult
         * @static
         * @param {proto.ContractFunctionResult} message ContractFunctionResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractFunctionResult.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.logInfo = [];
                object.createdContractIDs = [];
                object.contractNonces = [];
            }
            if (options.defaults) {
                object.contractID = null;
                if (options.bytes === $String)
                    object.contractCallResult = "";
                else {
                    object.contractCallResult = [];
                    if (options.bytes !== $Array)
                        object.contractCallResult = $util.newBuffer(object.contractCallResult);
                }
                object.errorMessage = "";
                if (options.bytes === $String)
                    object.bloom = "";
                else {
                    object.bloom = [];
                    if (options.bytes !== $Array)
                        object.bloom = $util.newBuffer(object.bloom);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.gasUsed = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gasUsed = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.evmAddress = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.gas = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gas = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.functionParameters = "";
                else {
                    object.functionParameters = [];
                    if (options.bytes !== $Array)
                        object.functionParameters = $util.newBuffer(object.functionParameters);
                }
                object.senderId = null;
                object.signerNonce = null;
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            if (message.contractCallResult != null && $Object.hasOwnProperty.call(message, "contractCallResult"))
                object.contractCallResult = options.bytes === $String ? $util.base64.encode(message.contractCallResult, 0, message.contractCallResult.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.contractCallResult) : message.contractCallResult;
            if (message.errorMessage != null && $Object.hasOwnProperty.call(message, "errorMessage"))
                object.errorMessage = message.errorMessage;
            if (message.bloom != null && $Object.hasOwnProperty.call(message, "bloom"))
                object.bloom = options.bytes === $String ? $util.base64.encode(message.bloom, 0, message.bloom.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.bloom) : message.bloom;
            if (message.gasUsed != null && $Object.hasOwnProperty.call(message, "gasUsed"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gasUsed = typeof message.gasUsed === "number" ? $BigInt(message.gasUsed) : $util.Long.fromBits(message.gasUsed.low >>> 0, message.gasUsed.high >>> 0, true).toBigInt();
                else if (typeof message.gasUsed === "number")
                    object.gasUsed = options.longs === $String ? $String(message.gasUsed) : message.gasUsed;
                else
                    object.gasUsed = options.longs === $String ? $util.Long.prototype.toString.call(message.gasUsed) : options.longs === $Number ? new $util.LongBits(message.gasUsed.low >>> 0, message.gasUsed.high >>> 0).toNumber(true) : message.gasUsed;
            if (message.logInfo && message.logInfo.length) {
                object.logInfo = $Array(message.logInfo.length);
                for (let j = 0; j < message.logInfo.length; ++j)
                    object.logInfo[j] = $root.proto.ContractLoginfo.toObject(message.logInfo[j], options, _depth + 1);
            }
            if (message.createdContractIDs && message.createdContractIDs.length) {
                object.createdContractIDs = $Array(message.createdContractIDs.length);
                for (let j = 0; j < message.createdContractIDs.length; ++j)
                    object.createdContractIDs[j] = $root.proto.ContractID.toObject(message.createdContractIDs[j], options, _depth + 1);
            }
            if (message.evmAddress != null && $Object.hasOwnProperty.call(message, "evmAddress"))
                object.evmAddress = $root.google.protobuf.BytesValue.toObject(message.evmAddress, options, _depth + 1);
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gas = typeof message.gas === "number" ? $BigInt(message.gas) : $util.Long.fromBits(message.gas.low >>> 0, message.gas.high >>> 0, false).toBigInt();
                else if (typeof message.gas === "number")
                    object.gas = options.longs === $String ? $String(message.gas) : message.gas;
                else
                    object.gas = options.longs === $String ? $util.Long.prototype.toString.call(message.gas) : options.longs === $Number ? new $util.LongBits(message.gas.low >>> 0, message.gas.high >>> 0).toNumber() : message.gas;
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            if (message.functionParameters != null && $Object.hasOwnProperty.call(message, "functionParameters"))
                object.functionParameters = options.bytes === $String ? $util.base64.encode(message.functionParameters, 0, message.functionParameters.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.functionParameters) : message.functionParameters;
            if (message.senderId != null && $Object.hasOwnProperty.call(message, "senderId"))
                object.senderId = $root.proto.AccountID.toObject(message.senderId, options, _depth + 1);
            if (message.contractNonces && message.contractNonces.length) {
                object.contractNonces = $Array(message.contractNonces.length);
                for (let j = 0; j < message.contractNonces.length; ++j)
                    object.contractNonces[j] = $root.proto.ContractNonceInfo.toObject(message.contractNonces[j], options, _depth + 1);
            }
            if (message.signerNonce != null && $Object.hasOwnProperty.call(message, "signerNonce"))
                object.signerNonce = $root.google.protobuf.Int64Value.toObject(message.signerNonce, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ContractFunctionResult to JSON.
         * @function toJSON
         * @memberof proto.ContractFunctionResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractFunctionResult.prototype.toJSON = function() {
            return ContractFunctionResult.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractFunctionResult
         * @function getTypeUrl
         * @memberof proto.ContractFunctionResult
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractFunctionResult.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractFunctionResult";
        };

        return ContractFunctionResult;
    })();

    proto.ContractGetInfoQuery = (function() {

        /**
         * Properties of a ContractGetInfoQuery.
         * @typedef {Object} proto.ContractGetInfoQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.ContractID.$Properties|null} [contractID] A smart contract ID.
         * <p>
         * The network SHALL return information for this smart contract,
         * if successful.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractGetInfoQuery.
         * @memberof proto
         * @interface IContractGetInfoQuery
         * @augments proto.ContractGetInfoQuery.$Properties
         * @deprecated Use proto.ContractGetInfoQuery.$Properties instead.
         */

        /**
         * Shape of a ContractGetInfoQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   contractID?: proto.ContractID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractGetInfoQuery.$Shape
         */

        /**
         * Constructs a new ContractGetInfoQuery.
         * @memberof proto
         * @classdesc Request detailed information about a smart contract.
         * @constructor
         * @param {proto.ContractGetInfoQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractGetInfoQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.ContractGetInfoQuery
         * @instance
         */
        ContractGetInfoQuery.prototype.header = null;

        /**
         * A smart contract ID.
         * <p>
         * The network SHALL return information for this smart contract,
         * if successful.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.ContractGetInfoQuery
         * @instance
         */
        ContractGetInfoQuery.prototype.contractID = null;

        /**
         * Creates a new ContractGetInfoQuery instance using the specified properties.
         * @function create
         * @memberof proto.ContractGetInfoQuery
         * @static
         * @param {proto.ContractGetInfoQuery.$Properties=} [properties] Properties to set
         * @returns {proto.ContractGetInfoQuery} ContractGetInfoQuery instance
         * @type {{
         *   (properties: proto.ContractGetInfoQuery.$Shape): proto.ContractGetInfoQuery & proto.ContractGetInfoQuery.$Shape;
         *   (properties?: proto.ContractGetInfoQuery.$Properties): proto.ContractGetInfoQuery;
         * }}
         */
        ContractGetInfoQuery.create = function(properties) {
            return new ContractGetInfoQuery(properties);
        };

        /**
         * Encodes the specified ContractGetInfoQuery message. Does not implicitly {@link proto.ContractGetInfoQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractGetInfoQuery
         * @static
         * @param {proto.ContractGetInfoQuery.$Properties} message ContractGetInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractGetInfoQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractGetInfoQuery message, length delimited. Does not implicitly {@link proto.ContractGetInfoQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractGetInfoQuery
         * @static
         * @param {proto.ContractGetInfoQuery.$Properties} message ContractGetInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractGetInfoQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractGetInfoQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractGetInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractGetInfoQuery & proto.ContractGetInfoQuery.$Shape} ContractGetInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractGetInfoQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractGetInfoQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractGetInfoQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractGetInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractGetInfoQuery & proto.ContractGetInfoQuery.$Shape} ContractGetInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractGetInfoQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractGetInfoQuery message.
         * @function verify
         * @memberof proto.ContractGetInfoQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractGetInfoQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            return null;
        };

        /**
         * Creates a ContractGetInfoQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractGetInfoQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractGetInfoQuery} ContractGetInfoQuery
         */
        ContractGetInfoQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractGetInfoQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractGetInfoQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractGetInfoQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.ContractGetInfoQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.ContractGetInfoQuery.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractGetInfoQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractGetInfoQuery
         * @static
         * @param {proto.ContractGetInfoQuery} message ContractGetInfoQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractGetInfoQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.contractID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ContractGetInfoQuery to JSON.
         * @function toJSON
         * @memberof proto.ContractGetInfoQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractGetInfoQuery.prototype.toJSON = function() {
            return ContractGetInfoQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractGetInfoQuery
         * @function getTypeUrl
         * @memberof proto.ContractGetInfoQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractGetInfoQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractGetInfoQuery";
        };

        return ContractGetInfoQuery;
    })();

    proto.ContractGetInfoResponse = (function() {

        /**
         * Properties of a ContractGetInfoResponse.
         * @typedef {Object} proto.ContractGetInfoResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.ContractGetInfoResponse.ContractInfo.$Properties|null} [contractInfo] The information, as requested, for a smart contract.
         * A state proof MAY be generated for this value.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractGetInfoResponse.
         * @memberof proto
         * @interface IContractGetInfoResponse
         * @augments proto.ContractGetInfoResponse.$Properties
         * @deprecated Use proto.ContractGetInfoResponse.$Properties instead.
         */

        /**
         * Shape of a ContractGetInfoResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   contractInfo?: proto.ContractGetInfoResponse.ContractInfo.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractGetInfoResponse.$Shape
         */

        /**
         * Constructs a new ContractGetInfoResponse.
         * @memberof proto
         * @classdesc Information returned in response to a "get info" query for a smart contract.
         * @constructor
         * @param {proto.ContractGetInfoResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractGetInfoResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.ContractGetInfoResponse
         * @instance
         */
        ContractGetInfoResponse.prototype.header = null;

        /**
         * The information, as requested, for a smart contract.
         * A state proof MAY be generated for this value.
         * @member {proto.ContractGetInfoResponse.ContractInfo.$Properties|null|undefined} contractInfo
         * @memberof proto.ContractGetInfoResponse
         * @instance
         */
        ContractGetInfoResponse.prototype.contractInfo = null;

        /**
         * Creates a new ContractGetInfoResponse instance using the specified properties.
         * @function create
         * @memberof proto.ContractGetInfoResponse
         * @static
         * @param {proto.ContractGetInfoResponse.$Properties=} [properties] Properties to set
         * @returns {proto.ContractGetInfoResponse} ContractGetInfoResponse instance
         * @type {{
         *   (properties: proto.ContractGetInfoResponse.$Shape): proto.ContractGetInfoResponse & proto.ContractGetInfoResponse.$Shape;
         *   (properties?: proto.ContractGetInfoResponse.$Properties): proto.ContractGetInfoResponse;
         * }}
         */
        ContractGetInfoResponse.create = function(properties) {
            return new ContractGetInfoResponse(properties);
        };

        /**
         * Encodes the specified ContractGetInfoResponse message. Does not implicitly {@link proto.ContractGetInfoResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractGetInfoResponse
         * @static
         * @param {proto.ContractGetInfoResponse.$Properties} message ContractGetInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractGetInfoResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.contractInfo != null && $Object.hasOwnProperty.call(message, "contractInfo"))
                $root.proto.ContractGetInfoResponse.ContractInfo.encode(message.contractInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractGetInfoResponse message, length delimited. Does not implicitly {@link proto.ContractGetInfoResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractGetInfoResponse
         * @static
         * @param {proto.ContractGetInfoResponse.$Properties} message ContractGetInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractGetInfoResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractGetInfoResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractGetInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractGetInfoResponse & proto.ContractGetInfoResponse.$Shape} ContractGetInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractGetInfoResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractGetInfoResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.contractInfo = $root.proto.ContractGetInfoResponse.ContractInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractInfo);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractGetInfoResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractGetInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractGetInfoResponse & proto.ContractGetInfoResponse.$Shape} ContractGetInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractGetInfoResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractGetInfoResponse message.
         * @function verify
         * @memberof proto.ContractGetInfoResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractGetInfoResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.contractInfo != null && $Object.hasOwnProperty.call(message, "contractInfo")) {
                let error = $root.proto.ContractGetInfoResponse.ContractInfo.verify(message.contractInfo, _depth + 1);
                if (error)
                    return "contractInfo." + error;
            }
            return null;
        };

        /**
         * Creates a ContractGetInfoResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractGetInfoResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractGetInfoResponse} ContractGetInfoResponse
         */
        ContractGetInfoResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractGetInfoResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractGetInfoResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractGetInfoResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.ContractGetInfoResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.contractInfo != null) {
                if (!$util.isObject(object.contractInfo))
                    throw $TypeError(".proto.ContractGetInfoResponse.contractInfo: object expected");
                message.contractInfo = $root.proto.ContractGetInfoResponse.ContractInfo.fromObject(object.contractInfo, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractGetInfoResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractGetInfoResponse
         * @static
         * @param {proto.ContractGetInfoResponse} message ContractGetInfoResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractGetInfoResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.contractInfo = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.contractInfo != null && $Object.hasOwnProperty.call(message, "contractInfo"))
                object.contractInfo = $root.proto.ContractGetInfoResponse.ContractInfo.toObject(message.contractInfo, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ContractGetInfoResponse to JSON.
         * @function toJSON
         * @memberof proto.ContractGetInfoResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractGetInfoResponse.prototype.toJSON = function() {
            return ContractGetInfoResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractGetInfoResponse
         * @function getTypeUrl
         * @memberof proto.ContractGetInfoResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractGetInfoResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractGetInfoResponse";
        };

        ContractGetInfoResponse.ContractInfo = (function() {

            /**
             * Properties of a ContractInfo.
             * @typedef {Object} proto.ContractGetInfoResponse.ContractInfo.$Properties
             * @property {proto.ContractID.$Properties|null} [contractID] The ID of the smart contract requested in the query.
             * @property {proto.AccountID.$Properties|null} [accountID] The Account ID for the account entry associated with this
             * smart contract.
             * @property {string|null} [contractAccountID] The "Solidity" form contract ID.<br/>
             * This is a hexadecimal string form of the 20-byte EVM address
             * of the contract.
             * @property {proto.Key.$Properties|null} [adminKey] The key that MUST sign any transaction to update or modify this
             * smart contract.
             * <p>
             * If this value is null, or is an empty `KeyList` then the contract
             * CANNOT be deleted, modified, or updated, but MAY still expire.
             * @property {proto.Timestamp.$Properties|null} [expirationTime] The point in time at which this contract will expire.
             * @property {proto.Duration.$Properties|null} [autoRenewPeriod] The duration, in seconds, for which the contract lifetime will be
             * automatically extended upon expiration, provide sufficient HBAR is
             * available at that time to pay the renewal fee.<br/>
             * See `auto_renew_account_id` for additional conditions.
             * @property {number|Long|null} [storage] The amount of storage used by this smart contract.
             * @property {string|null} [memo] A short description of this smart contract.
             * <p>
             * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
             * (default 100) bytes when encoded as UTF-8.
             * @property {number|Long|null} [balance] The current HBAR balance, in tinybar, of the smart contract account.
             * @property {boolean|null} [deleted] A flag indicating that this contract is deleted.
             * @property {Array.<proto.TokenRelationship.$Properties>|null} [tokenRelationships] Because <a href="https://hips.hedera.com/hip/hip-367">HIP-367</a>,
             * which allows an account to be associated to an unlimited number of
             * tokens, it became necessary to only provide this information from
             * a Mirror Node.<br/>
             * The list of tokens associated to this contract.
             * @property {Uint8Array|null} [ledgerId] The ledger ID of the network that generated this response.
             * <p>
             * This value SHALL identify the distributed ledger that responded to
             * this query.
             * @property {proto.AccountID.$Properties|null} [autoRenewAccountId] An account designated to pay the renewal fee upon automatic renewal
             * of this contract.
             * <p>
             * If this is not set, or is set to an account with zero HBAR
             * available, the HBAR balance of the contract, if available,
             * SHALL be used to pay the renewal fee.
             * @property {number|null} [maxAutomaticTokenAssociations] The maximum number of tokens that the contract can be
             * associated to automatically.
             * @property {proto.StakingInfo.$Properties|null} [stakingInfo] Staking information for this contract.
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ContractInfo.
             * @memberof proto.ContractGetInfoResponse
             * @interface IContractInfo
             * @augments proto.ContractGetInfoResponse.ContractInfo.$Properties
             * @deprecated Use proto.ContractGetInfoResponse.ContractInfo.$Properties instead.
             */

            /**
             * Shape of a ContractInfo.
             * @typedef {{
             *   contractID?: proto.ContractID.$Shape|null;
             *   accountID?: proto.AccountID.$Shape|null;
             *   contractAccountID?: string|null;
             *   adminKey?: proto.Key.$Shape|null;
             *   expirationTime?: proto.Timestamp.$Shape|null;
             *   autoRenewPeriod?: proto.Duration.$Shape|null;
             *   storage?: number|Long|null;
             *   memo?: string|null;
             *   balance?: number|Long|null;
             *   deleted?: boolean|null;
             *   tokenRelationships?: Array.<proto.TokenRelationship.$Shape>|null;
             *   ledgerId?: Uint8Array|null;
             *   autoRenewAccountId?: proto.AccountID.$Shape|null;
             *   maxAutomaticTokenAssociations?: number|null;
             *   stakingInfo?: proto.StakingInfo.$Shape|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} proto.ContractGetInfoResponse.ContractInfo.$Shape
             */

            /**
             * Constructs a new ContractInfo.
             * @memberof proto.ContractGetInfoResponse
             * @classdesc Represents a ContractInfo.
             * @constructor
             * @param {proto.ContractGetInfoResponse.ContractInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const ContractInfo = function (properties) {
                this.tokenRelationships = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * The ID of the smart contract requested in the query.
             * @member {proto.ContractID.$Properties|null|undefined} contractID
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.contractID = null;

            /**
             * The Account ID for the account entry associated with this
             * smart contract.
             * @member {proto.AccountID.$Properties|null|undefined} accountID
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.accountID = null;

            /**
             * The "Solidity" form contract ID.<br/>
             * This is a hexadecimal string form of the 20-byte EVM address
             * of the contract.
             * @member {string} contractAccountID
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.contractAccountID = "";

            /**
             * The key that MUST sign any transaction to update or modify this
             * smart contract.
             * <p>
             * If this value is null, or is an empty `KeyList` then the contract
             * CANNOT be deleted, modified, or updated, but MAY still expire.
             * @member {proto.Key.$Properties|null|undefined} adminKey
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.adminKey = null;

            /**
             * The point in time at which this contract will expire.
             * @member {proto.Timestamp.$Properties|null|undefined} expirationTime
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.expirationTime = null;

            /**
             * The duration, in seconds, for which the contract lifetime will be
             * automatically extended upon expiration, provide sufficient HBAR is
             * available at that time to pay the renewal fee.<br/>
             * See `auto_renew_account_id` for additional conditions.
             * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.autoRenewPeriod = null;

            /**
             * The amount of storage used by this smart contract.
             * @member {number|Long} storage
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.storage = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * A short description of this smart contract.
             * <p>
             * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
             * (default 100) bytes when encoded as UTF-8.
             * @member {string} memo
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.memo = "";

            /**
             * The current HBAR balance, in tinybar, of the smart contract account.
             * @member {number|Long} balance
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.balance = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * A flag indicating that this contract is deleted.
             * @member {boolean} deleted
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.deleted = false;

            /**
             * Because <a href="https://hips.hedera.com/hip/hip-367">HIP-367</a>,
             * which allows an account to be associated to an unlimited number of
             * tokens, it became necessary to only provide this information from
             * a Mirror Node.<br/>
             * The list of tokens associated to this contract.
             * @member {Array.<proto.TokenRelationship.$Properties>} tokenRelationships
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.tokenRelationships = $util.emptyArray;

            /**
             * The ledger ID of the network that generated this response.
             * <p>
             * This value SHALL identify the distributed ledger that responded to
             * this query.
             * @member {Uint8Array} ledgerId
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.ledgerId = $util.newBuffer([]);

            /**
             * An account designated to pay the renewal fee upon automatic renewal
             * of this contract.
             * <p>
             * If this is not set, or is set to an account with zero HBAR
             * available, the HBAR balance of the contract, if available,
             * SHALL be used to pay the renewal fee.
             * @member {proto.AccountID.$Properties|null|undefined} autoRenewAccountId
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.autoRenewAccountId = null;

            /**
             * The maximum number of tokens that the contract can be
             * associated to automatically.
             * @member {number} maxAutomaticTokenAssociations
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.maxAutomaticTokenAssociations = 0;

            /**
             * Staking information for this contract.
             * @member {proto.StakingInfo.$Properties|null|undefined} stakingInfo
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             */
            ContractInfo.prototype.stakingInfo = null;

            /**
             * Creates a new ContractInfo instance using the specified properties.
             * @function create
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @static
             * @param {proto.ContractGetInfoResponse.ContractInfo.$Properties=} [properties] Properties to set
             * @returns {proto.ContractGetInfoResponse.ContractInfo} ContractInfo instance
             * @type {{
             *   (properties: proto.ContractGetInfoResponse.ContractInfo.$Shape): proto.ContractGetInfoResponse.ContractInfo & proto.ContractGetInfoResponse.ContractInfo.$Shape;
             *   (properties?: proto.ContractGetInfoResponse.ContractInfo.$Properties): proto.ContractGetInfoResponse.ContractInfo;
             * }}
             */
            ContractInfo.create = function(properties) {
                return new ContractInfo(properties);
            };

            /**
             * Encodes the specified ContractInfo message. Does not implicitly {@link proto.ContractGetInfoResponse.ContractInfo.verify|verify} messages.
             * @function encode
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @static
             * @param {proto.ContractGetInfoResponse.ContractInfo.$Properties} message ContractInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ContractInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                    $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                    $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.contractAccountID != null && $Object.hasOwnProperty.call(message, "contractAccountID") && message.contractAccountID !== "")
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.contractAccountID);
                if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                    $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                    $root.proto.Timestamp.encode(message.expirationTime, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                    $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                if (message.storage != null && $Object.hasOwnProperty.call(message, "storage") && (typeof message.storage === "object" ? message.storage.low || message.storage.high : message.storage !== 0))
                    writer.uint32(/* id 7, wireType 0 =*/56).int64(message.storage);
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.memo);
                if (message.balance != null && $Object.hasOwnProperty.call(message, "balance") && (typeof message.balance === "object" ? message.balance.low || message.balance.high : message.balance !== 0))
                    writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.balance);
                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.deleted);
                if (message.tokenRelationships != null && message.tokenRelationships.length)
                    for (let i = 0; i < message.tokenRelationships.length; ++i)
                        $root.proto.TokenRelationship.encode(message.tokenRelationships[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId") && message.ledgerId.length)
                    writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.ledgerId);
                if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId"))
                    $root.proto.AccountID.encode(message.autoRenewAccountId, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
                if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations") && message.maxAutomaticTokenAssociations !== 0)
                    writer.uint32(/* id 14, wireType 0 =*/112).int32(message.maxAutomaticTokenAssociations);
                if (message.stakingInfo != null && $Object.hasOwnProperty.call(message, "stakingInfo"))
                    $root.proto.StakingInfo.encode(message.stakingInfo, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ContractInfo message, length delimited. Does not implicitly {@link proto.ContractGetInfoResponse.ContractInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @static
             * @param {proto.ContractGetInfoResponse.ContractInfo.$Properties} message ContractInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ContractInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ContractInfo message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ContractGetInfoResponse.ContractInfo & proto.ContractGetInfoResponse.ContractInfo.$Shape} ContractInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ContractInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.proto.ContractGetInfoResponse.ContractInfo();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.contractAccountID = value;
                            else
                                delete message.contractAccountID;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.expirationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                            continue;
                        }
                    case 7: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.storage = value;
                            else
                                delete message.storage;
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.memo = value;
                            else
                                delete message.memo;
                            continue;
                        }
                    case 9: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.balance = value;
                            else
                                delete message.balance;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.deleted = value;
                            else
                                delete message.deleted;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if (!(message.tokenRelationships && message.tokenRelationships.length))
                                message.tokenRelationships = [];
                            message.tokenRelationships.push($root.proto.TokenRelationship.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.bytes()).length)
                                message.ledgerId = value;
                            else
                                delete message.ledgerId;
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            message.autoRenewAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewAccountId);
                            continue;
                        }
                    case 14: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.maxAutomaticTokenAssociations = value;
                            else
                                delete message.maxAutomaticTokenAssociations;
                            continue;
                        }
                    case 15: {
                            if (wireType !== 2)
                                break;
                            message.stakingInfo = $root.proto.StakingInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stakingInfo);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ContractInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ContractGetInfoResponse.ContractInfo & proto.ContractGetInfoResponse.ContractInfo.$Shape} ContractInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ContractInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ContractInfo message.
             * @function verify
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ContractInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                    let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                    if (error)
                        return "contractID." + error;
                }
                if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                    let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                    if (error)
                        return "accountID." + error;
                }
                if (message.contractAccountID != null && $Object.hasOwnProperty.call(message, "contractAccountID"))
                    if (!$util.isString(message.contractAccountID))
                        return "contractAccountID: string expected";
                if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                    let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                    if (error)
                        return "adminKey." + error;
                }
                if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                    let error = $root.proto.Timestamp.verify(message.expirationTime, _depth + 1);
                    if (error)
                        return "expirationTime." + error;
                }
                if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                    let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                    if (error)
                        return "autoRenewPeriod." + error;
                }
                if (message.storage != null && $Object.hasOwnProperty.call(message, "storage"))
                    if (!$util.isInteger(message.storage) && !(message.storage && $util.isInteger(message.storage.low) && $util.isInteger(message.storage.high)))
                        return "storage: integer|Long expected";
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                    if (!$util.isString(message.memo))
                        return "memo: string expected";
                if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                    if (!$util.isInteger(message.balance) && !(message.balance && $util.isInteger(message.balance.low) && $util.isInteger(message.balance.high)))
                        return "balance: integer|Long expected";
                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                    if (typeof message.deleted !== "boolean")
                        return "deleted: boolean expected";
                if (message.tokenRelationships != null && $Object.hasOwnProperty.call(message, "tokenRelationships")) {
                    if (!$Array.isArray(message.tokenRelationships))
                        return "tokenRelationships: array expected";
                    for (let i = 0; i < message.tokenRelationships.length; ++i) {
                        let error = $root.proto.TokenRelationship.verify(message.tokenRelationships[i], _depth + 1);
                        if (error)
                            return "tokenRelationships." + error;
                    }
                }
                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                    if (!(message.ledgerId && typeof message.ledgerId.length === "number" || $util.isString(message.ledgerId)))
                        return "ledgerId: buffer expected";
                if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId")) {
                    let error = $root.proto.AccountID.verify(message.autoRenewAccountId, _depth + 1);
                    if (error)
                        return "autoRenewAccountId." + error;
                }
                if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                    if (!$util.isInteger(message.maxAutomaticTokenAssociations))
                        return "maxAutomaticTokenAssociations: integer expected";
                if (message.stakingInfo != null && $Object.hasOwnProperty.call(message, "stakingInfo")) {
                    let error = $root.proto.StakingInfo.verify(message.stakingInfo, _depth + 1);
                    if (error)
                        return "stakingInfo." + error;
                }
                return null;
            };

            /**
             * Creates a ContractInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ContractGetInfoResponse.ContractInfo} ContractInfo
             */
            ContractInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.proto.ContractGetInfoResponse.ContractInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".proto.ContractGetInfoResponse.ContractInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.proto.ContractGetInfoResponse.ContractInfo();
                if (object.contractID != null) {
                    if (!$util.isObject(object.contractID))
                        throw $TypeError(".proto.ContractGetInfoResponse.ContractInfo.contractID: object expected");
                    message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
                }
                if (object.accountID != null) {
                    if (!$util.isObject(object.accountID))
                        throw $TypeError(".proto.ContractGetInfoResponse.ContractInfo.accountID: object expected");
                    message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
                }
                if (object.contractAccountID != null)
                    if (typeof object.contractAccountID !== "string" || object.contractAccountID.length)
                        message.contractAccountID = $String(object.contractAccountID);
                if (object.adminKey != null) {
                    if (!$util.isObject(object.adminKey))
                        throw $TypeError(".proto.ContractGetInfoResponse.ContractInfo.adminKey: object expected");
                    message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
                }
                if (object.expirationTime != null) {
                    if (!$util.isObject(object.expirationTime))
                        throw $TypeError(".proto.ContractGetInfoResponse.ContractInfo.expirationTime: object expected");
                    message.expirationTime = $root.proto.Timestamp.fromObject(object.expirationTime, _depth + 1);
                }
                if (object.autoRenewPeriod != null) {
                    if (!$util.isObject(object.autoRenewPeriod))
                        throw $TypeError(".proto.ContractGetInfoResponse.ContractInfo.autoRenewPeriod: object expected");
                    message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
                }
                if (object.storage != null)
                    if (typeof object.storage === "object" ? object.storage.low || object.storage.high : $Number(object.storage) !== 0)
                        if ($util.Long)
                            message.storage = $util.Long.fromValue(object.storage, false);
                        else if (typeof object.storage === "string")
                            message.storage = $parseInt(object.storage, 10);
                        else if (typeof object.storage === "number")
                            message.storage = object.storage;
                        else if (typeof object.storage === "object")
                            message.storage = new $util.LongBits(object.storage.low >>> 0, object.storage.high >>> 0).toNumber();
                if (object.memo != null)
                    if (typeof object.memo !== "string" || object.memo.length)
                        message.memo = $String(object.memo);
                if (object.balance != null)
                    if (typeof object.balance === "object" ? object.balance.low || object.balance.high : $Number(object.balance) !== 0)
                        if ($util.Long)
                            message.balance = $util.Long.fromValue(object.balance, true);
                        else if (typeof object.balance === "string")
                            message.balance = $parseInt(object.balance, 10);
                        else if (typeof object.balance === "number")
                            message.balance = object.balance;
                        else if (typeof object.balance === "object")
                            message.balance = new $util.LongBits(object.balance.low >>> 0, object.balance.high >>> 0).toNumber(true);
                if (object.deleted != null)
                    if (object.deleted)
                        message.deleted = $Boolean(object.deleted);
                if (object.tokenRelationships) {
                    if (!$Array.isArray(object.tokenRelationships))
                        throw $TypeError(".proto.ContractGetInfoResponse.ContractInfo.tokenRelationships: array expected");
                    message.tokenRelationships = $Array(object.tokenRelationships.length);
                    for (let i = 0; i < object.tokenRelationships.length; ++i) {
                        if (!$util.isObject(object.tokenRelationships[i]))
                            throw $TypeError(".proto.ContractGetInfoResponse.ContractInfo.tokenRelationships: object expected");
                        message.tokenRelationships[i] = $root.proto.TokenRelationship.fromObject(object.tokenRelationships[i], _depth + 1);
                    }
                }
                if (object.ledgerId != null)
                    if (object.ledgerId.length)
                        if (typeof object.ledgerId === "string")
                            $util.base64.decode(object.ledgerId, message.ledgerId = $util.newBuffer($util.base64.length(object.ledgerId)), 0);
                        else if (object.ledgerId.length >= 0)
                            message.ledgerId = object.ledgerId;
                if (object.autoRenewAccountId != null) {
                    if (!$util.isObject(object.autoRenewAccountId))
                        throw $TypeError(".proto.ContractGetInfoResponse.ContractInfo.autoRenewAccountId: object expected");
                    message.autoRenewAccountId = $root.proto.AccountID.fromObject(object.autoRenewAccountId, _depth + 1);
                }
                if (object.maxAutomaticTokenAssociations != null)
                    if ($Number(object.maxAutomaticTokenAssociations) !== 0)
                        message.maxAutomaticTokenAssociations = object.maxAutomaticTokenAssociations | 0;
                if (object.stakingInfo != null) {
                    if (!$util.isObject(object.stakingInfo))
                        throw $TypeError(".proto.ContractGetInfoResponse.ContractInfo.stakingInfo: object expected");
                    message.stakingInfo = $root.proto.StakingInfo.fromObject(object.stakingInfo, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a ContractInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @static
             * @param {proto.ContractGetInfoResponse.ContractInfo} message ContractInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ContractInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.tokenRelationships = [];
                if (options.defaults) {
                    object.contractID = null;
                    object.accountID = null;
                    object.contractAccountID = "";
                    object.adminKey = null;
                    object.expirationTime = null;
                    object.autoRenewPeriod = null;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.storage = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.storage = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.memo = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.deleted = false;
                    if (options.bytes === $String)
                        object.ledgerId = "";
                    else {
                        object.ledgerId = [];
                        if (options.bytes !== $Array)
                            object.ledgerId = $util.newBuffer(object.ledgerId);
                    }
                    object.autoRenewAccountId = null;
                    object.maxAutomaticTokenAssociations = 0;
                    object.stakingInfo = null;
                }
                if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                    object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
                if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                    object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
                if (message.contractAccountID != null && $Object.hasOwnProperty.call(message, "contractAccountID"))
                    object.contractAccountID = message.contractAccountID;
                if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                    object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
                if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                    object.expirationTime = $root.proto.Timestamp.toObject(message.expirationTime, options, _depth + 1);
                if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                    object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
                if (message.storage != null && $Object.hasOwnProperty.call(message, "storage"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.storage = typeof message.storage === "number" ? $BigInt(message.storage) : $util.Long.fromBits(message.storage.low >>> 0, message.storage.high >>> 0, false).toBigInt();
                    else if (typeof message.storage === "number")
                        object.storage = options.longs === $String ? $String(message.storage) : message.storage;
                    else
                        object.storage = options.longs === $String ? $util.Long.prototype.toString.call(message.storage) : options.longs === $Number ? new $util.LongBits(message.storage.low >>> 0, message.storage.high >>> 0).toNumber() : message.storage;
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                    object.memo = message.memo;
                if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.balance = typeof message.balance === "number" ? $BigInt(message.balance) : $util.Long.fromBits(message.balance.low >>> 0, message.balance.high >>> 0, true).toBigInt();
                    else if (typeof message.balance === "number")
                        object.balance = options.longs === $String ? $String(message.balance) : message.balance;
                    else
                        object.balance = options.longs === $String ? $util.Long.prototype.toString.call(message.balance) : options.longs === $Number ? new $util.LongBits(message.balance.low >>> 0, message.balance.high >>> 0).toNumber(true) : message.balance;
                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                    object.deleted = message.deleted;
                if (message.tokenRelationships && message.tokenRelationships.length) {
                    object.tokenRelationships = $Array(message.tokenRelationships.length);
                    for (let j = 0; j < message.tokenRelationships.length; ++j)
                        object.tokenRelationships[j] = $root.proto.TokenRelationship.toObject(message.tokenRelationships[j], options, _depth + 1);
                }
                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                    object.ledgerId = options.bytes === $String ? $util.base64.encode(message.ledgerId, 0, message.ledgerId.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ledgerId) : message.ledgerId;
                if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId"))
                    object.autoRenewAccountId = $root.proto.AccountID.toObject(message.autoRenewAccountId, options, _depth + 1);
                if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                    object.maxAutomaticTokenAssociations = message.maxAutomaticTokenAssociations;
                if (message.stakingInfo != null && $Object.hasOwnProperty.call(message, "stakingInfo"))
                    object.stakingInfo = $root.proto.StakingInfo.toObject(message.stakingInfo, options, _depth + 1);
                return object;
            };

            /**
             * Converts this ContractInfo to JSON.
             * @function toJSON
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ContractInfo.prototype.toJSON = function() {
                return ContractInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ContractInfo
             * @function getTypeUrl
             * @memberof proto.ContractGetInfoResponse.ContractInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ContractInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContractGetInfoResponse.ContractInfo";
            };

            return ContractInfo;
        })();

        return ContractGetInfoResponse;
    })();

    proto.ContractGetBytecodeQuery = (function() {

        /**
         * Properties of a ContractGetBytecodeQuery.
         * @typedef {Object} proto.ContractGetBytecodeQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.ContractID.$Properties|null} [contractID] A smart contract ID.
         * <p>
         * The network SHALL return bytecode for this smart contract, if successful.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractGetBytecodeQuery.
         * @memberof proto
         * @interface IContractGetBytecodeQuery
         * @augments proto.ContractGetBytecodeQuery.$Properties
         * @deprecated Use proto.ContractGetBytecodeQuery.$Properties instead.
         */

        /**
         * Shape of a ContractGetBytecodeQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   contractID?: proto.ContractID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractGetBytecodeQuery.$Shape
         */

        /**
         * Constructs a new ContractGetBytecodeQuery.
         * @memberof proto
         * @classdesc A transaction body to request the current bytecode for a smart contract.
         * @constructor
         * @param {proto.ContractGetBytecodeQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractGetBytecodeQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.ContractGetBytecodeQuery
         * @instance
         */
        ContractGetBytecodeQuery.prototype.header = null;

        /**
         * A smart contract ID.
         * <p>
         * The network SHALL return bytecode for this smart contract, if successful.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.ContractGetBytecodeQuery
         * @instance
         */
        ContractGetBytecodeQuery.prototype.contractID = null;

        /**
         * Creates a new ContractGetBytecodeQuery instance using the specified properties.
         * @function create
         * @memberof proto.ContractGetBytecodeQuery
         * @static
         * @param {proto.ContractGetBytecodeQuery.$Properties=} [properties] Properties to set
         * @returns {proto.ContractGetBytecodeQuery} ContractGetBytecodeQuery instance
         * @type {{
         *   (properties: proto.ContractGetBytecodeQuery.$Shape): proto.ContractGetBytecodeQuery & proto.ContractGetBytecodeQuery.$Shape;
         *   (properties?: proto.ContractGetBytecodeQuery.$Properties): proto.ContractGetBytecodeQuery;
         * }}
         */
        ContractGetBytecodeQuery.create = function(properties) {
            return new ContractGetBytecodeQuery(properties);
        };

        /**
         * Encodes the specified ContractGetBytecodeQuery message. Does not implicitly {@link proto.ContractGetBytecodeQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractGetBytecodeQuery
         * @static
         * @param {proto.ContractGetBytecodeQuery.$Properties} message ContractGetBytecodeQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractGetBytecodeQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractGetBytecodeQuery message, length delimited. Does not implicitly {@link proto.ContractGetBytecodeQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractGetBytecodeQuery
         * @static
         * @param {proto.ContractGetBytecodeQuery.$Properties} message ContractGetBytecodeQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractGetBytecodeQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractGetBytecodeQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractGetBytecodeQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractGetBytecodeQuery & proto.ContractGetBytecodeQuery.$Shape} ContractGetBytecodeQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractGetBytecodeQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractGetBytecodeQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractGetBytecodeQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractGetBytecodeQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractGetBytecodeQuery & proto.ContractGetBytecodeQuery.$Shape} ContractGetBytecodeQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractGetBytecodeQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractGetBytecodeQuery message.
         * @function verify
         * @memberof proto.ContractGetBytecodeQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractGetBytecodeQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            return null;
        };

        /**
         * Creates a ContractGetBytecodeQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractGetBytecodeQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractGetBytecodeQuery} ContractGetBytecodeQuery
         */
        ContractGetBytecodeQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractGetBytecodeQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractGetBytecodeQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractGetBytecodeQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.ContractGetBytecodeQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.ContractGetBytecodeQuery.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractGetBytecodeQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractGetBytecodeQuery
         * @static
         * @param {proto.ContractGetBytecodeQuery} message ContractGetBytecodeQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractGetBytecodeQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.contractID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ContractGetBytecodeQuery to JSON.
         * @function toJSON
         * @memberof proto.ContractGetBytecodeQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractGetBytecodeQuery.prototype.toJSON = function() {
            return ContractGetBytecodeQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractGetBytecodeQuery
         * @function getTypeUrl
         * @memberof proto.ContractGetBytecodeQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractGetBytecodeQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractGetBytecodeQuery";
        };

        return ContractGetBytecodeQuery;
    })();

    proto.ContractGetBytecodeResponse = (function() {

        /**
         * Properties of a ContractGetBytecodeResponse.
         * @typedef {Object} proto.ContractGetBytecodeResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {Uint8Array|null} [bytecode] The current bytecode of the requested smart contract.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractGetBytecodeResponse.
         * @memberof proto
         * @interface IContractGetBytecodeResponse
         * @augments proto.ContractGetBytecodeResponse.$Properties
         * @deprecated Use proto.ContractGetBytecodeResponse.$Properties instead.
         */

        /**
         * Shape of a ContractGetBytecodeResponse.
         * @typedef {proto.ContractGetBytecodeResponse.$Properties} proto.ContractGetBytecodeResponse.$Shape
         */

        /**
         * Constructs a new ContractGetBytecodeResponse.
         * @memberof proto
         * @classdesc Information returned in response to a "get bytecode" query for a
         * smart contract.
         * @constructor
         * @param {proto.ContractGetBytecodeResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractGetBytecodeResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.ContractGetBytecodeResponse
         * @instance
         */
        ContractGetBytecodeResponse.prototype.header = null;

        /**
         * The current bytecode of the requested smart contract.
         * @member {Uint8Array} bytecode
         * @memberof proto.ContractGetBytecodeResponse
         * @instance
         */
        ContractGetBytecodeResponse.prototype.bytecode = $util.newBuffer([]);

        /**
         * Creates a new ContractGetBytecodeResponse instance using the specified properties.
         * @function create
         * @memberof proto.ContractGetBytecodeResponse
         * @static
         * @param {proto.ContractGetBytecodeResponse.$Properties=} [properties] Properties to set
         * @returns {proto.ContractGetBytecodeResponse} ContractGetBytecodeResponse instance
         * @type {{
         *   (properties: proto.ContractGetBytecodeResponse.$Shape): proto.ContractGetBytecodeResponse & proto.ContractGetBytecodeResponse.$Shape;
         *   (properties?: proto.ContractGetBytecodeResponse.$Properties): proto.ContractGetBytecodeResponse;
         * }}
         */
        ContractGetBytecodeResponse.create = function(properties) {
            return new ContractGetBytecodeResponse(properties);
        };

        /**
         * Encodes the specified ContractGetBytecodeResponse message. Does not implicitly {@link proto.ContractGetBytecodeResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractGetBytecodeResponse
         * @static
         * @param {proto.ContractGetBytecodeResponse.$Properties} message ContractGetBytecodeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractGetBytecodeResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.bytecode != null && $Object.hasOwnProperty.call(message, "bytecode") && message.bytecode.length)
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.bytecode);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractGetBytecodeResponse message, length delimited. Does not implicitly {@link proto.ContractGetBytecodeResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractGetBytecodeResponse
         * @static
         * @param {proto.ContractGetBytecodeResponse.$Properties} message ContractGetBytecodeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractGetBytecodeResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractGetBytecodeResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractGetBytecodeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractGetBytecodeResponse & proto.ContractGetBytecodeResponse.$Shape} ContractGetBytecodeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractGetBytecodeResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractGetBytecodeResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.bytecode = value;
                        else
                            delete message.bytecode;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractGetBytecodeResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractGetBytecodeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractGetBytecodeResponse & proto.ContractGetBytecodeResponse.$Shape} ContractGetBytecodeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractGetBytecodeResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractGetBytecodeResponse message.
         * @function verify
         * @memberof proto.ContractGetBytecodeResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractGetBytecodeResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.bytecode != null && $Object.hasOwnProperty.call(message, "bytecode"))
                if (!(message.bytecode && typeof message.bytecode.length === "number" || $util.isString(message.bytecode)))
                    return "bytecode: buffer expected";
            return null;
        };

        /**
         * Creates a ContractGetBytecodeResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractGetBytecodeResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractGetBytecodeResponse} ContractGetBytecodeResponse
         */
        ContractGetBytecodeResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractGetBytecodeResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractGetBytecodeResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractGetBytecodeResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.ContractGetBytecodeResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.bytecode != null)
                if (object.bytecode.length)
                    if (typeof object.bytecode === "string")
                        $util.base64.decode(object.bytecode, message.bytecode = $util.newBuffer($util.base64.length(object.bytecode)), 0);
                    else if (object.bytecode.length >= 0)
                        message.bytecode = object.bytecode;
            return message;
        };

        /**
         * Creates a plain object from a ContractGetBytecodeResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractGetBytecodeResponse
         * @static
         * @param {proto.ContractGetBytecodeResponse} message ContractGetBytecodeResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractGetBytecodeResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                if (options.bytes === $String)
                    object.bytecode = "";
                else {
                    object.bytecode = [];
                    if (options.bytes !== $Array)
                        object.bytecode = $util.newBuffer(object.bytecode);
                }
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.bytecode != null && $Object.hasOwnProperty.call(message, "bytecode"))
                object.bytecode = options.bytes === $String ? $util.base64.encode(message.bytecode, 0, message.bytecode.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.bytecode) : message.bytecode;
            return object;
        };

        /**
         * Converts this ContractGetBytecodeResponse to JSON.
         * @function toJSON
         * @memberof proto.ContractGetBytecodeResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractGetBytecodeResponse.prototype.toJSON = function() {
            return ContractGetBytecodeResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractGetBytecodeResponse
         * @function getTypeUrl
         * @memberof proto.ContractGetBytecodeResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractGetBytecodeResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractGetBytecodeResponse";
        };

        return ContractGetBytecodeResponse;
    })();

    proto.ContractGetRecordsQuery = (function() {

        /**
         * Properties of a ContractGetRecordsQuery.
         * @typedef {Object} proto.ContractGetRecordsQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.ContractID.$Properties|null} [contractID] A smart contract ID.
         * <p>
         * The network SHALL return information for this smart contract, if successful.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractGetRecordsQuery.
         * @memberof proto
         * @interface IContractGetRecordsQuery
         * @augments proto.ContractGetRecordsQuery.$Properties
         * @deprecated Use proto.ContractGetRecordsQuery.$Properties instead.
         */

        /**
         * Shape of a ContractGetRecordsQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   contractID?: proto.ContractID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractGetRecordsQuery.$Shape
         */

        /**
         * Constructs a new ContractGetRecordsQuery.
         * @memberof proto
         * @classdesc Deprecated and not supported after release `0.9.0`.
         * Request records of all transactions against the given contract in the last 25 hours.
         * @constructor
         * @param {proto.ContractGetRecordsQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractGetRecordsQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.ContractGetRecordsQuery
         * @instance
         */
        ContractGetRecordsQuery.prototype.header = null;

        /**
         * A smart contract ID.
         * <p>
         * The network SHALL return information for this smart contract, if successful.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.ContractGetRecordsQuery
         * @instance
         */
        ContractGetRecordsQuery.prototype.contractID = null;

        /**
         * Creates a new ContractGetRecordsQuery instance using the specified properties.
         * @function create
         * @memberof proto.ContractGetRecordsQuery
         * @static
         * @param {proto.ContractGetRecordsQuery.$Properties=} [properties] Properties to set
         * @returns {proto.ContractGetRecordsQuery} ContractGetRecordsQuery instance
         * @type {{
         *   (properties: proto.ContractGetRecordsQuery.$Shape): proto.ContractGetRecordsQuery & proto.ContractGetRecordsQuery.$Shape;
         *   (properties?: proto.ContractGetRecordsQuery.$Properties): proto.ContractGetRecordsQuery;
         * }}
         */
        ContractGetRecordsQuery.create = function(properties) {
            return new ContractGetRecordsQuery(properties);
        };

        /**
         * Encodes the specified ContractGetRecordsQuery message. Does not implicitly {@link proto.ContractGetRecordsQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractGetRecordsQuery
         * @static
         * @param {proto.ContractGetRecordsQuery.$Properties} message ContractGetRecordsQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractGetRecordsQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractGetRecordsQuery message, length delimited. Does not implicitly {@link proto.ContractGetRecordsQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractGetRecordsQuery
         * @static
         * @param {proto.ContractGetRecordsQuery.$Properties} message ContractGetRecordsQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractGetRecordsQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractGetRecordsQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractGetRecordsQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractGetRecordsQuery & proto.ContractGetRecordsQuery.$Shape} ContractGetRecordsQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractGetRecordsQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractGetRecordsQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractGetRecordsQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractGetRecordsQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractGetRecordsQuery & proto.ContractGetRecordsQuery.$Shape} ContractGetRecordsQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractGetRecordsQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractGetRecordsQuery message.
         * @function verify
         * @memberof proto.ContractGetRecordsQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractGetRecordsQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            return null;
        };

        /**
         * Creates a ContractGetRecordsQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractGetRecordsQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractGetRecordsQuery} ContractGetRecordsQuery
         */
        ContractGetRecordsQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractGetRecordsQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractGetRecordsQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractGetRecordsQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.ContractGetRecordsQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.ContractGetRecordsQuery.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractGetRecordsQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractGetRecordsQuery
         * @static
         * @param {proto.ContractGetRecordsQuery} message ContractGetRecordsQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractGetRecordsQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.contractID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ContractGetRecordsQuery to JSON.
         * @function toJSON
         * @memberof proto.ContractGetRecordsQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractGetRecordsQuery.prototype.toJSON = function() {
            return ContractGetRecordsQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractGetRecordsQuery
         * @function getTypeUrl
         * @memberof proto.ContractGetRecordsQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractGetRecordsQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractGetRecordsQuery";
        };

        return ContractGetRecordsQuery;
    })();

    proto.ContractGetRecordsResponse = (function() {

        /**
         * Properties of a ContractGetRecordsResponse.
         * @typedef {Object} proto.ContractGetRecordsResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.ContractID.$Properties|null} [contractID] A smart contract that this response describes.
         * @property {Array.<proto.TransactionRecord.$Properties>|null} [records] A list of records, each with contractCreateResult or contractCallResult as its body
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractGetRecordsResponse.
         * @memberof proto
         * @interface IContractGetRecordsResponse
         * @augments proto.ContractGetRecordsResponse.$Properties
         * @deprecated Use proto.ContractGetRecordsResponse.$Properties instead.
         */

        /**
         * Shape of a ContractGetRecordsResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   contractID?: proto.ContractID.$Shape|null;
         *   records?: Array.<proto.TransactionRecord.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractGetRecordsResponse.$Shape
         */

        /**
         * Constructs a new ContractGetRecordsResponse.
         * @memberof proto
         * @classdesc Deprecated and not supported after release `0.9.0`.
         * Response with records of all transactions against the given contract in the last 25 hours.
         * @constructor
         * @param {proto.ContractGetRecordsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractGetRecordsResponse = function (properties) {
            this.records = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.ContractGetRecordsResponse
         * @instance
         */
        ContractGetRecordsResponse.prototype.header = null;

        /**
         * A smart contract that this response describes.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.ContractGetRecordsResponse
         * @instance
         */
        ContractGetRecordsResponse.prototype.contractID = null;

        /**
         * A list of records, each with contractCreateResult or contractCallResult as its body
         * @member {Array.<proto.TransactionRecord.$Properties>} records
         * @memberof proto.ContractGetRecordsResponse
         * @instance
         */
        ContractGetRecordsResponse.prototype.records = $util.emptyArray;

        /**
         * Creates a new ContractGetRecordsResponse instance using the specified properties.
         * @function create
         * @memberof proto.ContractGetRecordsResponse
         * @static
         * @param {proto.ContractGetRecordsResponse.$Properties=} [properties] Properties to set
         * @returns {proto.ContractGetRecordsResponse} ContractGetRecordsResponse instance
         * @type {{
         *   (properties: proto.ContractGetRecordsResponse.$Shape): proto.ContractGetRecordsResponse & proto.ContractGetRecordsResponse.$Shape;
         *   (properties?: proto.ContractGetRecordsResponse.$Properties): proto.ContractGetRecordsResponse;
         * }}
         */
        ContractGetRecordsResponse.create = function(properties) {
            return new ContractGetRecordsResponse(properties);
        };

        /**
         * Encodes the specified ContractGetRecordsResponse message. Does not implicitly {@link proto.ContractGetRecordsResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractGetRecordsResponse
         * @static
         * @param {proto.ContractGetRecordsResponse.$Properties} message ContractGetRecordsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractGetRecordsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.records != null && message.records.length)
                for (let i = 0; i < message.records.length; ++i)
                    $root.proto.TransactionRecord.encode(message.records[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractGetRecordsResponse message, length delimited. Does not implicitly {@link proto.ContractGetRecordsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractGetRecordsResponse
         * @static
         * @param {proto.ContractGetRecordsResponse.$Properties} message ContractGetRecordsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractGetRecordsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractGetRecordsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractGetRecordsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractGetRecordsResponse & proto.ContractGetRecordsResponse.$Shape} ContractGetRecordsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractGetRecordsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractGetRecordsResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.records && message.records.length))
                            message.records = [];
                        message.records.push($root.proto.TransactionRecord.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractGetRecordsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractGetRecordsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractGetRecordsResponse & proto.ContractGetRecordsResponse.$Shape} ContractGetRecordsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractGetRecordsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractGetRecordsResponse message.
         * @function verify
         * @memberof proto.ContractGetRecordsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractGetRecordsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            if (message.records != null && $Object.hasOwnProperty.call(message, "records")) {
                if (!$Array.isArray(message.records))
                    return "records: array expected";
                for (let i = 0; i < message.records.length; ++i) {
                    let error = $root.proto.TransactionRecord.verify(message.records[i], _depth + 1);
                    if (error)
                        return "records." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ContractGetRecordsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractGetRecordsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractGetRecordsResponse} ContractGetRecordsResponse
         */
        ContractGetRecordsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractGetRecordsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractGetRecordsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractGetRecordsResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.ContractGetRecordsResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.ContractGetRecordsResponse.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            if (object.records) {
                if (!$Array.isArray(object.records))
                    throw $TypeError(".proto.ContractGetRecordsResponse.records: array expected");
                message.records = $Array(object.records.length);
                for (let i = 0; i < object.records.length; ++i) {
                    if (!$util.isObject(object.records[i]))
                        throw $TypeError(".proto.ContractGetRecordsResponse.records: object expected");
                    message.records[i] = $root.proto.TransactionRecord.fromObject(object.records[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractGetRecordsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractGetRecordsResponse
         * @static
         * @param {proto.ContractGetRecordsResponse} message ContractGetRecordsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractGetRecordsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.records = [];
            if (options.defaults) {
                object.header = null;
                object.contractID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            if (message.records && message.records.length) {
                object.records = $Array(message.records.length);
                for (let j = 0; j < message.records.length; ++j)
                    object.records[j] = $root.proto.TransactionRecord.toObject(message.records[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ContractGetRecordsResponse to JSON.
         * @function toJSON
         * @memberof proto.ContractGetRecordsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractGetRecordsResponse.prototype.toJSON = function() {
            return ContractGetRecordsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractGetRecordsResponse
         * @function getTypeUrl
         * @memberof proto.ContractGetRecordsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractGetRecordsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractGetRecordsResponse";
        };

        return ContractGetRecordsResponse;
    })();

    proto.TransactionRecord = (function() {

        /**
         * Properties of a TransactionRecord.
         * @typedef {Object} proto.TransactionRecord.$Properties
         * @property {proto.TransactionReceipt.$Properties|null} [receipt] A transaction receipt.
         * <p>
         * This SHALL report consensus status (reach consensus, failed,
         * unknown) and the ID of any new entity (i.e. account, file,
         * contract, schedule, etc...) created.
         * @property {Uint8Array|null} [transactionHash] A transaction hash value.
         * <p>
         * This SHALL be the hash of the Transaction that executed and
         * SHALL NOT be the hash of any Transaction that failed for
         * having a duplicate TransactionID.
         * @property {proto.Timestamp.$Properties|null} [consensusTimestamp] A consensus timestamp.
         * <p>
         * This SHALL be null if the transaction did not reach consensus yet.
         * @property {proto.TransactionID.$Properties|null} [transactionID] A transaction identifier to the transaction associated to this record.
         * @property {string|null} [memo] A transaction memo.<br/>
         * This is the memo that was submitted as part of the transaction.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {number|Long|null} [transactionFee] A transaction fee charged.
         * <p>
         * This SHALL be the actual transaction fee charged.<br/>
         * This MAY NOT match the original `transactionFee` value
         * from the `TransactionBody`.
         * @property {proto.ContractFunctionResult.$Properties|null} [contractCallResult] A contract call result.<br/>
         * A record of the value returned by the smart contract function (if
         * it completed and didn't fail) from a `ContractCallTransaction`.
         * @property {proto.ContractFunctionResult.$Properties|null} [contractCreateResult] A contract creation result.<br/>
         * A record of the value returned by the smart contract constructor (if
         * it completed and didn't fail) from a `ContractCreateTransaction`.
         * @property {proto.TransferList.$Properties|null} [transferList] A transfer list for this transaction.<br/>
         * This is a list of all HBAR transfers completed for this transaction.
         * <p>
         * This MAY include fees, transfers performed by the transaction,
         * transfers initiated by a smart contract it calls, or the creation
         * of threshold records that it triggers.
         * @property {Array.<proto.TokenTransferList.$Properties>|null} [tokenTransferLists] A token transfer list for this transaction.<br/>
         * This is a list of all non-HBAR token transfers
         * completed for this transaction.<br/>
         * @property {proto.ScheduleID.$Properties|null} [scheduleRef] A schedule reference.<br/>
         * The reference to a schedule ID for the schedule that initiated this
         * transaction, if this this transaction record represents a scheduled
         * transaction.
         * @property {Array.<proto.AssessedCustomFee.$Properties>|null} [assessedCustomFees] A list of all custom fees that were assessed during a CryptoTransfer.
         * <p>
         * These SHALL be paid if the transaction status resolved to SUCCESS.
         * @property {Array.<proto.TokenAssociation.$Properties>|null} [automaticTokenAssociations] A list of all token associations implicitly or automatically
         * created while handling this transaction.
         * @property {proto.Timestamp.$Properties|null} [parentConsensusTimestamp] A consensus timestamp for a child record.
         * <p>
         * This SHALL be the consensus timestamp of a user transaction that
         * spawned an internal child transaction.
         * @property {Uint8Array|null} [alias] A new account alias.<br/>
         * <p>
         * This is the new alias assigned to an account created as part
         * of a CryptoCreate transaction triggered by a user transaction
         * with a (previously unused) alias.
         * @property {Uint8Array|null} [ethereumHash] A keccak256 hash of the ethereumData.
         * <p>
         * This field SHALL only be populated for EthereumTransaction.
         * @property {Array.<proto.AccountAmount.$Properties>|null} [paidStakingRewards] A list of staking rewards paid.
         * <p>
         * This SHALL be a list accounts with the corresponding staking
         * rewards paid as a result of this transaction.
         * @property {Uint8Array|null} [prngBytes] A pseudorandom 384-bit sequence.
         * <p>
         * This SHALL be returned in the record of a UtilPrng transaction
         * with no output range,
         * @property {number|null} [prngNumber] A pseudorandom 32-bit integer.<br/>
         * <p>
         * This SHALL be returned in the record of a PRNG transaction with
         * an output range specified.
         * @property {Uint8Array|null} [evmAddress] A new default EVM address for an account created by
         * this transaction.
         * <p>
         * This field SHALL be populated only when the EVM address is not
         * specified in the related transaction body.
         * @property {Array.<proto.PendingAirdropRecord.$Properties>|null} [newPendingAirdrops] A list of pending token airdrops.
         * <p>
         * Each pending airdrop SHALL represent a single requested transfer
         * from a sending account to a recipient account.<br/>
         * These pending transfers are issued unilaterally by the sending
         * account, and MUST be claimed by the recipient account before
         * the transfer SHALL complete.<br/>
         * A sender MAY cancel a pending airdrop before it is claimed.<br/>
         * An airdrop transaction SHALL emit a pending airdrop when the
         * recipient has no available automatic association slots available
         * or when the recipient has set `receiver_sig_required`.
         * @property {number|Long|null} [highVolumePricingMultiplier] A high volume pricing multiplier.
         * <p>
         * This SHALL be the multiplier that is applied to the transaction
         * fees charged for this transaction if the high volume flag is set.
         * This is scaled by 1000.
         * @property {"contractCallResult"|"contractCreateResult"} [body] TransactionRecord body
         * @property {"prngBytes"|"prngNumber"} [entropy] TransactionRecord entropy
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionRecord.
         * @memberof proto
         * @interface ITransactionRecord
         * @augments proto.TransactionRecord.$Properties
         * @deprecated Use proto.TransactionRecord.$Properties instead.
         */

        /**
         * Narrowed shape of a TransactionRecord.
         * @typedef {{
         *   receipt?: proto.TransactionReceipt.$Shape|null;
         *   transactionHash?: Uint8Array|null;
         *   consensusTimestamp?: proto.Timestamp.$Shape|null;
         *   transactionID?: proto.TransactionID.$Shape|null;
         *   memo?: string|null;
         *   transactionFee?: number|Long|null;
         *   contractCallResult?: proto.ContractFunctionResult.$Shape|null;
         *   contractCreateResult?: proto.ContractFunctionResult.$Shape|null;
         *   transferList?: proto.TransferList.$Shape|null;
         *   tokenTransferLists?: Array.<proto.TokenTransferList.$Shape>|null;
         *   scheduleRef?: proto.ScheduleID.$Shape|null;
         *   assessedCustomFees?: Array.<proto.AssessedCustomFee.$Shape>|null;
         *   automaticTokenAssociations?: Array.<proto.TokenAssociation.$Shape>|null;
         *   parentConsensusTimestamp?: proto.Timestamp.$Shape|null;
         *   alias?: Uint8Array|null;
         *   ethereumHash?: Uint8Array|null;
         *   paidStakingRewards?: Array.<proto.AccountAmount.$Shape>|null;
         *   prngBytes?: Uint8Array|null;
         *   prngNumber?: number|null;
         *   evmAddress?: Uint8Array|null;
         *   newPendingAirdrops?: Array.<proto.PendingAirdropRecord.$Shape>|null;
         *   highVolumePricingMultiplier?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ body?: undefined; contractCallResult?: null; contractCreateResult?: null }|{ body?: "contractCallResult"; contractCallResult: proto.ContractFunctionResult.$Shape; contractCreateResult?: null }|{ body?: "contractCreateResult"; contractCallResult?: null; contractCreateResult: proto.ContractFunctionResult.$Shape })
         * ) & (
         *   ({ entropy?: undefined; prngBytes?: null; prngNumber?: null }|{ entropy?: "prngBytes"; prngBytes: Uint8Array; prngNumber?: null }|{ entropy?: "prngNumber"; prngBytes?: null; prngNumber: number })
         * )} proto.TransactionRecord.$Shape
         */

        /**
         * Constructs a new TransactionRecord.
         * @memberof proto
         * @classdesc Response when the client sends the node TransactionGetRecordResponse
         * @constructor
         * @param {proto.TransactionRecord.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionRecord = function (properties) {
            this.tokenTransferLists = [];
            this.assessedCustomFees = [];
            this.automaticTokenAssociations = [];
            this.paidStakingRewards = [];
            this.newPendingAirdrops = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A transaction receipt.
         * <p>
         * This SHALL report consensus status (reach consensus, failed,
         * unknown) and the ID of any new entity (i.e. account, file,
         * contract, schedule, etc...) created.
         * @member {proto.TransactionReceipt.$Properties|null|undefined} receipt
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.receipt = null;

        /**
         * A transaction hash value.
         * <p>
         * This SHALL be the hash of the Transaction that executed and
         * SHALL NOT be the hash of any Transaction that failed for
         * having a duplicate TransactionID.
         * @member {Uint8Array} transactionHash
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.transactionHash = $util.newBuffer([]);

        /**
         * A consensus timestamp.
         * <p>
         * This SHALL be null if the transaction did not reach consensus yet.
         * @member {proto.Timestamp.$Properties|null|undefined} consensusTimestamp
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.consensusTimestamp = null;

        /**
         * A transaction identifier to the transaction associated to this record.
         * @member {proto.TransactionID.$Properties|null|undefined} transactionID
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.transactionID = null;

        /**
         * A transaction memo.<br/>
         * This is the memo that was submitted as part of the transaction.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.memo = "";

        /**
         * A transaction fee charged.
         * <p>
         * This SHALL be the actual transaction fee charged.<br/>
         * This MAY NOT match the original `transactionFee` value
         * from the `TransactionBody`.
         * @member {number|Long} transactionFee
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.transactionFee = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A contract call result.<br/>
         * A record of the value returned by the smart contract function (if
         * it completed and didn't fail) from a `ContractCallTransaction`.
         * @member {proto.ContractFunctionResult.$Properties|null|undefined} contractCallResult
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.contractCallResult = null;

        /**
         * A contract creation result.<br/>
         * A record of the value returned by the smart contract constructor (if
         * it completed and didn't fail) from a `ContractCreateTransaction`.
         * @member {proto.ContractFunctionResult.$Properties|null|undefined} contractCreateResult
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.contractCreateResult = null;

        /**
         * A transfer list for this transaction.<br/>
         * This is a list of all HBAR transfers completed for this transaction.
         * <p>
         * This MAY include fees, transfers performed by the transaction,
         * transfers initiated by a smart contract it calls, or the creation
         * of threshold records that it triggers.
         * @member {proto.TransferList.$Properties|null|undefined} transferList
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.transferList = null;

        /**
         * A token transfer list for this transaction.<br/>
         * This is a list of all non-HBAR token transfers
         * completed for this transaction.<br/>
         * @member {Array.<proto.TokenTransferList.$Properties>} tokenTransferLists
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.tokenTransferLists = $util.emptyArray;

        /**
         * A schedule reference.<br/>
         * The reference to a schedule ID for the schedule that initiated this
         * transaction, if this this transaction record represents a scheduled
         * transaction.
         * @member {proto.ScheduleID.$Properties|null|undefined} scheduleRef
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.scheduleRef = null;

        /**
         * A list of all custom fees that were assessed during a CryptoTransfer.
         * <p>
         * These SHALL be paid if the transaction status resolved to SUCCESS.
         * @member {Array.<proto.AssessedCustomFee.$Properties>} assessedCustomFees
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.assessedCustomFees = $util.emptyArray;

        /**
         * A list of all token associations implicitly or automatically
         * created while handling this transaction.
         * @member {Array.<proto.TokenAssociation.$Properties>} automaticTokenAssociations
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.automaticTokenAssociations = $util.emptyArray;

        /**
         * A consensus timestamp for a child record.
         * <p>
         * This SHALL be the consensus timestamp of a user transaction that
         * spawned an internal child transaction.
         * @member {proto.Timestamp.$Properties|null|undefined} parentConsensusTimestamp
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.parentConsensusTimestamp = null;

        /**
         * A new account alias.<br/>
         * <p>
         * This is the new alias assigned to an account created as part
         * of a CryptoCreate transaction triggered by a user transaction
         * with a (previously unused) alias.
         * @member {Uint8Array} alias
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.alias = $util.newBuffer([]);

        /**
         * A keccak256 hash of the ethereumData.
         * <p>
         * This field SHALL only be populated for EthereumTransaction.
         * @member {Uint8Array} ethereumHash
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.ethereumHash = $util.newBuffer([]);

        /**
         * A list of staking rewards paid.
         * <p>
         * This SHALL be a list accounts with the corresponding staking
         * rewards paid as a result of this transaction.
         * @member {Array.<proto.AccountAmount.$Properties>} paidStakingRewards
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.paidStakingRewards = $util.emptyArray;

        /**
         * A pseudorandom 384-bit sequence.
         * <p>
         * This SHALL be returned in the record of a UtilPrng transaction
         * with no output range,
         * @member {Uint8Array|null|undefined} prngBytes
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.prngBytes = null;

        /**
         * A pseudorandom 32-bit integer.<br/>
         * <p>
         * This SHALL be returned in the record of a PRNG transaction with
         * an output range specified.
         * @member {number|null|undefined} prngNumber
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.prngNumber = null;

        /**
         * A new default EVM address for an account created by
         * this transaction.
         * <p>
         * This field SHALL be populated only when the EVM address is not
         * specified in the related transaction body.
         * @member {Uint8Array} evmAddress
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.evmAddress = $util.newBuffer([]);

        /**
         * A list of pending token airdrops.
         * <p>
         * Each pending airdrop SHALL represent a single requested transfer
         * from a sending account to a recipient account.<br/>
         * These pending transfers are issued unilaterally by the sending
         * account, and MUST be claimed by the recipient account before
         * the transfer SHALL complete.<br/>
         * A sender MAY cancel a pending airdrop before it is claimed.<br/>
         * An airdrop transaction SHALL emit a pending airdrop when the
         * recipient has no available automatic association slots available
         * or when the recipient has set `receiver_sig_required`.
         * @member {Array.<proto.PendingAirdropRecord.$Properties>} newPendingAirdrops
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.newPendingAirdrops = $util.emptyArray;

        /**
         * A high volume pricing multiplier.
         * <p>
         * This SHALL be the multiplier that is applied to the transaction
         * fees charged for this transaction if the high volume flag is set.
         * This is scaled by 1000.
         * @member {number|Long} highVolumePricingMultiplier
         * @memberof proto.TransactionRecord
         * @instance
         */
        TransactionRecord.prototype.highVolumePricingMultiplier = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * TransactionRecord body.
         * @member {"contractCallResult"|"contractCreateResult"|undefined} body
         * @memberof proto.TransactionRecord
         * @instance
         */
        $Object.defineProperty(TransactionRecord.prototype, "body", {
            get: $util.oneOfGetter($oneOfFields = ["contractCallResult", "contractCreateResult"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * TransactionRecord entropy.
         * @member {"prngBytes"|"prngNumber"|undefined} entropy
         * @memberof proto.TransactionRecord
         * @instance
         */
        $Object.defineProperty(TransactionRecord.prototype, "entropy", {
            get: $util.oneOfGetter($oneOfFields = ["prngBytes", "prngNumber"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new TransactionRecord instance using the specified properties.
         * @function create
         * @memberof proto.TransactionRecord
         * @static
         * @param {proto.TransactionRecord.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionRecord} TransactionRecord instance
         * @type {{
         *   (properties: proto.TransactionRecord.$Shape): proto.TransactionRecord & proto.TransactionRecord.$Shape;
         *   (properties?: proto.TransactionRecord.$Properties): proto.TransactionRecord;
         * }}
         */
        TransactionRecord.create = function(properties) {
            return new TransactionRecord(properties);
        };

        /**
         * Encodes the specified TransactionRecord message. Does not implicitly {@link proto.TransactionRecord.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionRecord
         * @static
         * @param {proto.TransactionRecord.$Properties} message TransactionRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionRecord.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt"))
                $root.proto.TransactionReceipt.encode(message.receipt, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.transactionHash != null && $Object.hasOwnProperty.call(message, "transactionHash") && message.transactionHash.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.transactionHash);
            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp"))
                $root.proto.Timestamp.encode(message.consensusTimestamp, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID"))
                $root.proto.TransactionID.encode(message.transactionID, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.memo);
            if (message.transactionFee != null && $Object.hasOwnProperty.call(message, "transactionFee") && (typeof message.transactionFee === "object" ? message.transactionFee.low || message.transactionFee.high : message.transactionFee !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.transactionFee);
            if (message.contractCallResult != null && $Object.hasOwnProperty.call(message, "contractCallResult"))
                $root.proto.ContractFunctionResult.encode(message.contractCallResult, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.contractCreateResult != null && $Object.hasOwnProperty.call(message, "contractCreateResult"))
                $root.proto.ContractFunctionResult.encode(message.contractCreateResult, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.transferList != null && $Object.hasOwnProperty.call(message, "transferList"))
                $root.proto.TransferList.encode(message.transferList, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.tokenTransferLists != null && message.tokenTransferLists.length)
                for (let i = 0; i < message.tokenTransferLists.length; ++i)
                    $root.proto.TokenTransferList.encode(message.tokenTransferLists[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.scheduleRef != null && $Object.hasOwnProperty.call(message, "scheduleRef"))
                $root.proto.ScheduleID.encode(message.scheduleRef, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.assessedCustomFees != null && message.assessedCustomFees.length)
                for (let i = 0; i < message.assessedCustomFees.length; ++i)
                    $root.proto.AssessedCustomFee.encode(message.assessedCustomFees[i], writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.automaticTokenAssociations != null && message.automaticTokenAssociations.length)
                for (let i = 0; i < message.automaticTokenAssociations.length; ++i)
                    $root.proto.TokenAssociation.encode(message.automaticTokenAssociations[i], writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.parentConsensusTimestamp != null && $Object.hasOwnProperty.call(message, "parentConsensusTimestamp"))
                $root.proto.Timestamp.encode(message.parentConsensusTimestamp, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias") && message.alias.length)
                writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.alias);
            if (message.ethereumHash != null && $Object.hasOwnProperty.call(message, "ethereumHash") && message.ethereumHash.length)
                writer.uint32(/* id 17, wireType 2 =*/138).bytes(message.ethereumHash);
            if (message.paidStakingRewards != null && message.paidStakingRewards.length)
                for (let i = 0; i < message.paidStakingRewards.length; ++i)
                    $root.proto.AccountAmount.encode(message.paidStakingRewards[i], writer.uint32(/* id 18, wireType 2 =*/146).fork(), _depth + 1).ldelim();
            if (message.prngBytes != null && $Object.hasOwnProperty.call(message, "prngBytes"))
                writer.uint32(/* id 19, wireType 2 =*/154).bytes(message.prngBytes);
            if (message.prngNumber != null && $Object.hasOwnProperty.call(message, "prngNumber"))
                writer.uint32(/* id 20, wireType 0 =*/160).int32(message.prngNumber);
            if (message.evmAddress != null && $Object.hasOwnProperty.call(message, "evmAddress") && message.evmAddress.length)
                writer.uint32(/* id 21, wireType 2 =*/170).bytes(message.evmAddress);
            if (message.newPendingAirdrops != null && message.newPendingAirdrops.length)
                for (let i = 0; i < message.newPendingAirdrops.length; ++i)
                    $root.proto.PendingAirdropRecord.encode(message.newPendingAirdrops[i], writer.uint32(/* id 22, wireType 2 =*/178).fork(), _depth + 1).ldelim();
            if (message.highVolumePricingMultiplier != null && $Object.hasOwnProperty.call(message, "highVolumePricingMultiplier") && (typeof message.highVolumePricingMultiplier === "object" ? message.highVolumePricingMultiplier.low || message.highVolumePricingMultiplier.high : message.highVolumePricingMultiplier !== 0))
                writer.uint32(/* id 23, wireType 0 =*/184).uint64(message.highVolumePricingMultiplier);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionRecord message, length delimited. Does not implicitly {@link proto.TransactionRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionRecord
         * @static
         * @param {proto.TransactionRecord.$Properties} message TransactionRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionRecord.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionRecord message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionRecord & proto.TransactionRecord.$Shape} TransactionRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionRecord.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionRecord();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.receipt = $root.proto.TransactionReceipt.decode(reader, reader.uint32(), $undefined, _depth + 1, message.receipt);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.transactionHash = value;
                        else
                            delete message.transactionHash;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.consensusTimestamp = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusTimestamp);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.transactionID = $root.proto.TransactionID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionID);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.transactionFee = value;
                        else
                            delete message.transactionFee;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.contractCallResult = $root.proto.ContractFunctionResult.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractCallResult);
                        message.body = "contractCallResult";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.contractCreateResult = $root.proto.ContractFunctionResult.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractCreateResult);
                        message.body = "contractCreateResult";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.transferList = $root.proto.TransferList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transferList);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tokenTransferLists && message.tokenTransferLists.length))
                            message.tokenTransferLists = [];
                        message.tokenTransferLists.push($root.proto.TokenTransferList.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.scheduleRef = $root.proto.ScheduleID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleRef);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if (!(message.assessedCustomFees && message.assessedCustomFees.length))
                            message.assessedCustomFees = [];
                        message.assessedCustomFees.push($root.proto.AssessedCustomFee.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.automaticTokenAssociations && message.automaticTokenAssociations.length))
                            message.automaticTokenAssociations = [];
                        message.automaticTokenAssociations.push($root.proto.TokenAssociation.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.parentConsensusTimestamp = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.parentConsensusTimestamp);
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.alias = value;
                        else
                            delete message.alias;
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.ethereumHash = value;
                        else
                            delete message.ethereumHash;
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        if (!(message.paidStakingRewards && message.paidStakingRewards.length))
                            message.paidStakingRewards = [];
                        message.paidStakingRewards.push($root.proto.AccountAmount.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        message.prngBytes = reader.bytes();
                        message.entropy = "prngBytes";
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        message.prngNumber = reader.int32();
                        message.entropy = "prngNumber";
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.evmAddress = value;
                        else
                            delete message.evmAddress;
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        if (!(message.newPendingAirdrops && message.newPendingAirdrops.length))
                            message.newPendingAirdrops = [];
                        message.newPendingAirdrops.push($root.proto.PendingAirdropRecord.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 23: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.highVolumePricingMultiplier = value;
                        else
                            delete message.highVolumePricingMultiplier;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionRecord & proto.TransactionRecord.$Shape} TransactionRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionRecord.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionRecord message.
         * @function verify
         * @memberof proto.TransactionRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionRecord.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt")) {
                let error = $root.proto.TransactionReceipt.verify(message.receipt, _depth + 1);
                if (error)
                    return "receipt." + error;
            }
            if (message.transactionHash != null && $Object.hasOwnProperty.call(message, "transactionHash"))
                if (!(message.transactionHash && typeof message.transactionHash.length === "number" || $util.isString(message.transactionHash)))
                    return "transactionHash: buffer expected";
            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp")) {
                let error = $root.proto.Timestamp.verify(message.consensusTimestamp, _depth + 1);
                if (error)
                    return "consensusTimestamp." + error;
            }
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID")) {
                let error = $root.proto.TransactionID.verify(message.transactionID, _depth + 1);
                if (error)
                    return "transactionID." + error;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.transactionFee != null && $Object.hasOwnProperty.call(message, "transactionFee"))
                if (!$util.isInteger(message.transactionFee) && !(message.transactionFee && $util.isInteger(message.transactionFee.low) && $util.isInteger(message.transactionFee.high)))
                    return "transactionFee: integer|Long expected";
            if (message.contractCallResult != null && $Object.hasOwnProperty.call(message, "contractCallResult")) {
                properties.body = 1;
                {
                    let error = $root.proto.ContractFunctionResult.verify(message.contractCallResult, _depth + 1);
                    if (error)
                        return "contractCallResult." + error;
                }
            }
            if (message.contractCreateResult != null && $Object.hasOwnProperty.call(message, "contractCreateResult")) {
                if (properties.body === 1)
                    return "body: multiple values";
                properties.body = 1;
                {
                    let error = $root.proto.ContractFunctionResult.verify(message.contractCreateResult, _depth + 1);
                    if (error)
                        return "contractCreateResult." + error;
                }
            }
            if (message.transferList != null && $Object.hasOwnProperty.call(message, "transferList")) {
                let error = $root.proto.TransferList.verify(message.transferList, _depth + 1);
                if (error)
                    return "transferList." + error;
            }
            if (message.tokenTransferLists != null && $Object.hasOwnProperty.call(message, "tokenTransferLists")) {
                if (!$Array.isArray(message.tokenTransferLists))
                    return "tokenTransferLists: array expected";
                for (let i = 0; i < message.tokenTransferLists.length; ++i) {
                    let error = $root.proto.TokenTransferList.verify(message.tokenTransferLists[i], _depth + 1);
                    if (error)
                        return "tokenTransferLists." + error;
                }
            }
            if (message.scheduleRef != null && $Object.hasOwnProperty.call(message, "scheduleRef")) {
                let error = $root.proto.ScheduleID.verify(message.scheduleRef, _depth + 1);
                if (error)
                    return "scheduleRef." + error;
            }
            if (message.assessedCustomFees != null && $Object.hasOwnProperty.call(message, "assessedCustomFees")) {
                if (!$Array.isArray(message.assessedCustomFees))
                    return "assessedCustomFees: array expected";
                for (let i = 0; i < message.assessedCustomFees.length; ++i) {
                    let error = $root.proto.AssessedCustomFee.verify(message.assessedCustomFees[i], _depth + 1);
                    if (error)
                        return "assessedCustomFees." + error;
                }
            }
            if (message.automaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "automaticTokenAssociations")) {
                if (!$Array.isArray(message.automaticTokenAssociations))
                    return "automaticTokenAssociations: array expected";
                for (let i = 0; i < message.automaticTokenAssociations.length; ++i) {
                    let error = $root.proto.TokenAssociation.verify(message.automaticTokenAssociations[i], _depth + 1);
                    if (error)
                        return "automaticTokenAssociations." + error;
                }
            }
            if (message.parentConsensusTimestamp != null && $Object.hasOwnProperty.call(message, "parentConsensusTimestamp")) {
                let error = $root.proto.Timestamp.verify(message.parentConsensusTimestamp, _depth + 1);
                if (error)
                    return "parentConsensusTimestamp." + error;
            }
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                if (!(message.alias && typeof message.alias.length === "number" || $util.isString(message.alias)))
                    return "alias: buffer expected";
            if (message.ethereumHash != null && $Object.hasOwnProperty.call(message, "ethereumHash"))
                if (!(message.ethereumHash && typeof message.ethereumHash.length === "number" || $util.isString(message.ethereumHash)))
                    return "ethereumHash: buffer expected";
            if (message.paidStakingRewards != null && $Object.hasOwnProperty.call(message, "paidStakingRewards")) {
                if (!$Array.isArray(message.paidStakingRewards))
                    return "paidStakingRewards: array expected";
                for (let i = 0; i < message.paidStakingRewards.length; ++i) {
                    let error = $root.proto.AccountAmount.verify(message.paidStakingRewards[i], _depth + 1);
                    if (error)
                        return "paidStakingRewards." + error;
                }
            }
            if (message.prngBytes != null && $Object.hasOwnProperty.call(message, "prngBytes")) {
                properties.entropy = 1;
                if (!(message.prngBytes && typeof message.prngBytes.length === "number" || $util.isString(message.prngBytes)))
                    return "prngBytes: buffer expected";
            }
            if (message.prngNumber != null && $Object.hasOwnProperty.call(message, "prngNumber")) {
                if (properties.entropy === 1)
                    return "entropy: multiple values";
                properties.entropy = 1;
                if (!$util.isInteger(message.prngNumber))
                    return "prngNumber: integer expected";
            }
            if (message.evmAddress != null && $Object.hasOwnProperty.call(message, "evmAddress"))
                if (!(message.evmAddress && typeof message.evmAddress.length === "number" || $util.isString(message.evmAddress)))
                    return "evmAddress: buffer expected";
            if (message.newPendingAirdrops != null && $Object.hasOwnProperty.call(message, "newPendingAirdrops")) {
                if (!$Array.isArray(message.newPendingAirdrops))
                    return "newPendingAirdrops: array expected";
                for (let i = 0; i < message.newPendingAirdrops.length; ++i) {
                    let error = $root.proto.PendingAirdropRecord.verify(message.newPendingAirdrops[i], _depth + 1);
                    if (error)
                        return "newPendingAirdrops." + error;
                }
            }
            if (message.highVolumePricingMultiplier != null && $Object.hasOwnProperty.call(message, "highVolumePricingMultiplier"))
                if (!$util.isInteger(message.highVolumePricingMultiplier) && !(message.highVolumePricingMultiplier && $util.isInteger(message.highVolumePricingMultiplier.low) && $util.isInteger(message.highVolumePricingMultiplier.high)))
                    return "highVolumePricingMultiplier: integer|Long expected";
            return null;
        };

        /**
         * Creates a TransactionRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionRecord} TransactionRecord
         */
        TransactionRecord.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionRecord)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionRecord: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionRecord();
            if (object.receipt != null) {
                if (!$util.isObject(object.receipt))
                    throw $TypeError(".proto.TransactionRecord.receipt: object expected");
                message.receipt = $root.proto.TransactionReceipt.fromObject(object.receipt, _depth + 1);
            }
            if (object.transactionHash != null)
                if (object.transactionHash.length)
                    if (typeof object.transactionHash === "string")
                        $util.base64.decode(object.transactionHash, message.transactionHash = $util.newBuffer($util.base64.length(object.transactionHash)), 0);
                    else if (object.transactionHash.length >= 0)
                        message.transactionHash = object.transactionHash;
            if (object.consensusTimestamp != null) {
                if (!$util.isObject(object.consensusTimestamp))
                    throw $TypeError(".proto.TransactionRecord.consensusTimestamp: object expected");
                message.consensusTimestamp = $root.proto.Timestamp.fromObject(object.consensusTimestamp, _depth + 1);
            }
            if (object.transactionID != null) {
                if (!$util.isObject(object.transactionID))
                    throw $TypeError(".proto.TransactionRecord.transactionID: object expected");
                message.transactionID = $root.proto.TransactionID.fromObject(object.transactionID, _depth + 1);
            }
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.transactionFee != null)
                if (typeof object.transactionFee === "object" ? object.transactionFee.low || object.transactionFee.high : $Number(object.transactionFee) !== 0)
                    if ($util.Long)
                        message.transactionFee = $util.Long.fromValue(object.transactionFee, true);
                    else if (typeof object.transactionFee === "string")
                        message.transactionFee = $parseInt(object.transactionFee, 10);
                    else if (typeof object.transactionFee === "number")
                        message.transactionFee = object.transactionFee;
                    else if (typeof object.transactionFee === "object")
                        message.transactionFee = new $util.LongBits(object.transactionFee.low >>> 0, object.transactionFee.high >>> 0).toNumber(true);
            if (object.contractCallResult != null) {
                if (!$util.isObject(object.contractCallResult))
                    throw $TypeError(".proto.TransactionRecord.contractCallResult: object expected");
                message.contractCallResult = $root.proto.ContractFunctionResult.fromObject(object.contractCallResult, _depth + 1);
            }
            if (object.contractCreateResult != null) {
                if (!$util.isObject(object.contractCreateResult))
                    throw $TypeError(".proto.TransactionRecord.contractCreateResult: object expected");
                message.contractCreateResult = $root.proto.ContractFunctionResult.fromObject(object.contractCreateResult, _depth + 1);
            }
            if (object.transferList != null) {
                if (!$util.isObject(object.transferList))
                    throw $TypeError(".proto.TransactionRecord.transferList: object expected");
                message.transferList = $root.proto.TransferList.fromObject(object.transferList, _depth + 1);
            }
            if (object.tokenTransferLists) {
                if (!$Array.isArray(object.tokenTransferLists))
                    throw $TypeError(".proto.TransactionRecord.tokenTransferLists: array expected");
                message.tokenTransferLists = $Array(object.tokenTransferLists.length);
                for (let i = 0; i < object.tokenTransferLists.length; ++i) {
                    if (!$util.isObject(object.tokenTransferLists[i]))
                        throw $TypeError(".proto.TransactionRecord.tokenTransferLists: object expected");
                    message.tokenTransferLists[i] = $root.proto.TokenTransferList.fromObject(object.tokenTransferLists[i], _depth + 1);
                }
            }
            if (object.scheduleRef != null) {
                if (!$util.isObject(object.scheduleRef))
                    throw $TypeError(".proto.TransactionRecord.scheduleRef: object expected");
                message.scheduleRef = $root.proto.ScheduleID.fromObject(object.scheduleRef, _depth + 1);
            }
            if (object.assessedCustomFees) {
                if (!$Array.isArray(object.assessedCustomFees))
                    throw $TypeError(".proto.TransactionRecord.assessedCustomFees: array expected");
                message.assessedCustomFees = $Array(object.assessedCustomFees.length);
                for (let i = 0; i < object.assessedCustomFees.length; ++i) {
                    if (!$util.isObject(object.assessedCustomFees[i]))
                        throw $TypeError(".proto.TransactionRecord.assessedCustomFees: object expected");
                    message.assessedCustomFees[i] = $root.proto.AssessedCustomFee.fromObject(object.assessedCustomFees[i], _depth + 1);
                }
            }
            if (object.automaticTokenAssociations) {
                if (!$Array.isArray(object.automaticTokenAssociations))
                    throw $TypeError(".proto.TransactionRecord.automaticTokenAssociations: array expected");
                message.automaticTokenAssociations = $Array(object.automaticTokenAssociations.length);
                for (let i = 0; i < object.automaticTokenAssociations.length; ++i) {
                    if (!$util.isObject(object.automaticTokenAssociations[i]))
                        throw $TypeError(".proto.TransactionRecord.automaticTokenAssociations: object expected");
                    message.automaticTokenAssociations[i] = $root.proto.TokenAssociation.fromObject(object.automaticTokenAssociations[i], _depth + 1);
                }
            }
            if (object.parentConsensusTimestamp != null) {
                if (!$util.isObject(object.parentConsensusTimestamp))
                    throw $TypeError(".proto.TransactionRecord.parentConsensusTimestamp: object expected");
                message.parentConsensusTimestamp = $root.proto.Timestamp.fromObject(object.parentConsensusTimestamp, _depth + 1);
            }
            if (object.alias != null)
                if (object.alias.length)
                    if (typeof object.alias === "string")
                        $util.base64.decode(object.alias, message.alias = $util.newBuffer($util.base64.length(object.alias)), 0);
                    else if (object.alias.length >= 0)
                        message.alias = object.alias;
            if (object.ethereumHash != null)
                if (object.ethereumHash.length)
                    if (typeof object.ethereumHash === "string")
                        $util.base64.decode(object.ethereumHash, message.ethereumHash = $util.newBuffer($util.base64.length(object.ethereumHash)), 0);
                    else if (object.ethereumHash.length >= 0)
                        message.ethereumHash = object.ethereumHash;
            if (object.paidStakingRewards) {
                if (!$Array.isArray(object.paidStakingRewards))
                    throw $TypeError(".proto.TransactionRecord.paidStakingRewards: array expected");
                message.paidStakingRewards = $Array(object.paidStakingRewards.length);
                for (let i = 0; i < object.paidStakingRewards.length; ++i) {
                    if (!$util.isObject(object.paidStakingRewards[i]))
                        throw $TypeError(".proto.TransactionRecord.paidStakingRewards: object expected");
                    message.paidStakingRewards[i] = $root.proto.AccountAmount.fromObject(object.paidStakingRewards[i], _depth + 1);
                }
            }
            if (object.prngBytes != null)
                if (typeof object.prngBytes === "string")
                    $util.base64.decode(object.prngBytes, message.prngBytes = $util.newBuffer($util.base64.length(object.prngBytes)), 0);
                else if (object.prngBytes.length >= 0)
                    message.prngBytes = object.prngBytes;
            if (object.prngNumber != null)
                message.prngNumber = object.prngNumber | 0;
            if (object.evmAddress != null)
                if (object.evmAddress.length)
                    if (typeof object.evmAddress === "string")
                        $util.base64.decode(object.evmAddress, message.evmAddress = $util.newBuffer($util.base64.length(object.evmAddress)), 0);
                    else if (object.evmAddress.length >= 0)
                        message.evmAddress = object.evmAddress;
            if (object.newPendingAirdrops) {
                if (!$Array.isArray(object.newPendingAirdrops))
                    throw $TypeError(".proto.TransactionRecord.newPendingAirdrops: array expected");
                message.newPendingAirdrops = $Array(object.newPendingAirdrops.length);
                for (let i = 0; i < object.newPendingAirdrops.length; ++i) {
                    if (!$util.isObject(object.newPendingAirdrops[i]))
                        throw $TypeError(".proto.TransactionRecord.newPendingAirdrops: object expected");
                    message.newPendingAirdrops[i] = $root.proto.PendingAirdropRecord.fromObject(object.newPendingAirdrops[i], _depth + 1);
                }
            }
            if (object.highVolumePricingMultiplier != null)
                if (typeof object.highVolumePricingMultiplier === "object" ? object.highVolumePricingMultiplier.low || object.highVolumePricingMultiplier.high : $Number(object.highVolumePricingMultiplier) !== 0)
                    if ($util.Long)
                        message.highVolumePricingMultiplier = $util.Long.fromValue(object.highVolumePricingMultiplier, true);
                    else if (typeof object.highVolumePricingMultiplier === "string")
                        message.highVolumePricingMultiplier = $parseInt(object.highVolumePricingMultiplier, 10);
                    else if (typeof object.highVolumePricingMultiplier === "number")
                        message.highVolumePricingMultiplier = object.highVolumePricingMultiplier;
                    else if (typeof object.highVolumePricingMultiplier === "object")
                        message.highVolumePricingMultiplier = new $util.LongBits(object.highVolumePricingMultiplier.low >>> 0, object.highVolumePricingMultiplier.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a TransactionRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionRecord
         * @static
         * @param {proto.TransactionRecord} message TransactionRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionRecord.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.tokenTransferLists = [];
                object.assessedCustomFees = [];
                object.automaticTokenAssociations = [];
                object.paidStakingRewards = [];
                object.newPendingAirdrops = [];
            }
            if (options.defaults) {
                object.receipt = null;
                if (options.bytes === $String)
                    object.transactionHash = "";
                else {
                    object.transactionHash = [];
                    if (options.bytes !== $Array)
                        object.transactionHash = $util.newBuffer(object.transactionHash);
                }
                object.consensusTimestamp = null;
                object.transactionID = null;
                object.memo = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.transactionFee = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.transactionFee = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.transferList = null;
                object.scheduleRef = null;
                object.parentConsensusTimestamp = null;
                if (options.bytes === $String)
                    object.alias = "";
                else {
                    object.alias = [];
                    if (options.bytes !== $Array)
                        object.alias = $util.newBuffer(object.alias);
                }
                if (options.bytes === $String)
                    object.ethereumHash = "";
                else {
                    object.ethereumHash = [];
                    if (options.bytes !== $Array)
                        object.ethereumHash = $util.newBuffer(object.ethereumHash);
                }
                if (options.bytes === $String)
                    object.evmAddress = "";
                else {
                    object.evmAddress = [];
                    if (options.bytes !== $Array)
                        object.evmAddress = $util.newBuffer(object.evmAddress);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.highVolumePricingMultiplier = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.highVolumePricingMultiplier = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt"))
                object.receipt = $root.proto.TransactionReceipt.toObject(message.receipt, options, _depth + 1);
            if (message.transactionHash != null && $Object.hasOwnProperty.call(message, "transactionHash"))
                object.transactionHash = options.bytes === $String ? $util.base64.encode(message.transactionHash, 0, message.transactionHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.transactionHash) : message.transactionHash;
            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp"))
                object.consensusTimestamp = $root.proto.Timestamp.toObject(message.consensusTimestamp, options, _depth + 1);
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID"))
                object.transactionID = $root.proto.TransactionID.toObject(message.transactionID, options, _depth + 1);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.transactionFee != null && $Object.hasOwnProperty.call(message, "transactionFee"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.transactionFee = typeof message.transactionFee === "number" ? $BigInt(message.transactionFee) : $util.Long.fromBits(message.transactionFee.low >>> 0, message.transactionFee.high >>> 0, true).toBigInt();
                else if (typeof message.transactionFee === "number")
                    object.transactionFee = options.longs === $String ? $String(message.transactionFee) : message.transactionFee;
                else
                    object.transactionFee = options.longs === $String ? $util.Long.prototype.toString.call(message.transactionFee) : options.longs === $Number ? new $util.LongBits(message.transactionFee.low >>> 0, message.transactionFee.high >>> 0).toNumber(true) : message.transactionFee;
            if (message.contractCallResult != null && $Object.hasOwnProperty.call(message, "contractCallResult")) {
                object.contractCallResult = $root.proto.ContractFunctionResult.toObject(message.contractCallResult, options, _depth + 1);
                if (options.oneofs)
                    object.body = "contractCallResult";
            }
            if (message.contractCreateResult != null && $Object.hasOwnProperty.call(message, "contractCreateResult")) {
                object.contractCreateResult = $root.proto.ContractFunctionResult.toObject(message.contractCreateResult, options, _depth + 1);
                if (options.oneofs)
                    object.body = "contractCreateResult";
            }
            if (message.transferList != null && $Object.hasOwnProperty.call(message, "transferList"))
                object.transferList = $root.proto.TransferList.toObject(message.transferList, options, _depth + 1);
            if (message.tokenTransferLists && message.tokenTransferLists.length) {
                object.tokenTransferLists = $Array(message.tokenTransferLists.length);
                for (let j = 0; j < message.tokenTransferLists.length; ++j)
                    object.tokenTransferLists[j] = $root.proto.TokenTransferList.toObject(message.tokenTransferLists[j], options, _depth + 1);
            }
            if (message.scheduleRef != null && $Object.hasOwnProperty.call(message, "scheduleRef"))
                object.scheduleRef = $root.proto.ScheduleID.toObject(message.scheduleRef, options, _depth + 1);
            if (message.assessedCustomFees && message.assessedCustomFees.length) {
                object.assessedCustomFees = $Array(message.assessedCustomFees.length);
                for (let j = 0; j < message.assessedCustomFees.length; ++j)
                    object.assessedCustomFees[j] = $root.proto.AssessedCustomFee.toObject(message.assessedCustomFees[j], options, _depth + 1);
            }
            if (message.automaticTokenAssociations && message.automaticTokenAssociations.length) {
                object.automaticTokenAssociations = $Array(message.automaticTokenAssociations.length);
                for (let j = 0; j < message.automaticTokenAssociations.length; ++j)
                    object.automaticTokenAssociations[j] = $root.proto.TokenAssociation.toObject(message.automaticTokenAssociations[j], options, _depth + 1);
            }
            if (message.parentConsensusTimestamp != null && $Object.hasOwnProperty.call(message, "parentConsensusTimestamp"))
                object.parentConsensusTimestamp = $root.proto.Timestamp.toObject(message.parentConsensusTimestamp, options, _depth + 1);
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                object.alias = options.bytes === $String ? $util.base64.encode(message.alias, 0, message.alias.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.alias) : message.alias;
            if (message.ethereumHash != null && $Object.hasOwnProperty.call(message, "ethereumHash"))
                object.ethereumHash = options.bytes === $String ? $util.base64.encode(message.ethereumHash, 0, message.ethereumHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ethereumHash) : message.ethereumHash;
            if (message.paidStakingRewards && message.paidStakingRewards.length) {
                object.paidStakingRewards = $Array(message.paidStakingRewards.length);
                for (let j = 0; j < message.paidStakingRewards.length; ++j)
                    object.paidStakingRewards[j] = $root.proto.AccountAmount.toObject(message.paidStakingRewards[j], options, _depth + 1);
            }
            if (message.prngBytes != null && $Object.hasOwnProperty.call(message, "prngBytes")) {
                object.prngBytes = options.bytes === $String ? $util.base64.encode(message.prngBytes, 0, message.prngBytes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.prngBytes) : message.prngBytes;
                if (options.oneofs)
                    object.entropy = "prngBytes";
            }
            if (message.prngNumber != null && $Object.hasOwnProperty.call(message, "prngNumber")) {
                object.prngNumber = message.prngNumber;
                if (options.oneofs)
                    object.entropy = "prngNumber";
            }
            if (message.evmAddress != null && $Object.hasOwnProperty.call(message, "evmAddress"))
                object.evmAddress = options.bytes === $String ? $util.base64.encode(message.evmAddress, 0, message.evmAddress.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.evmAddress) : message.evmAddress;
            if (message.newPendingAirdrops && message.newPendingAirdrops.length) {
                object.newPendingAirdrops = $Array(message.newPendingAirdrops.length);
                for (let j = 0; j < message.newPendingAirdrops.length; ++j)
                    object.newPendingAirdrops[j] = $root.proto.PendingAirdropRecord.toObject(message.newPendingAirdrops[j], options, _depth + 1);
            }
            if (message.highVolumePricingMultiplier != null && $Object.hasOwnProperty.call(message, "highVolumePricingMultiplier"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.highVolumePricingMultiplier = typeof message.highVolumePricingMultiplier === "number" ? $BigInt(message.highVolumePricingMultiplier) : $util.Long.fromBits(message.highVolumePricingMultiplier.low >>> 0, message.highVolumePricingMultiplier.high >>> 0, true).toBigInt();
                else if (typeof message.highVolumePricingMultiplier === "number")
                    object.highVolumePricingMultiplier = options.longs === $String ? $String(message.highVolumePricingMultiplier) : message.highVolumePricingMultiplier;
                else
                    object.highVolumePricingMultiplier = options.longs === $String ? $util.Long.prototype.toString.call(message.highVolumePricingMultiplier) : options.longs === $Number ? new $util.LongBits(message.highVolumePricingMultiplier.low >>> 0, message.highVolumePricingMultiplier.high >>> 0).toNumber(true) : message.highVolumePricingMultiplier;
            return object;
        };

        /**
         * Converts this TransactionRecord to JSON.
         * @function toJSON
         * @memberof proto.TransactionRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionRecord.prototype.toJSON = function() {
            return TransactionRecord.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionRecord
         * @function getTypeUrl
         * @memberof proto.TransactionRecord
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionRecord.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionRecord";
        };

        return TransactionRecord;
    })();

    proto.PendingAirdropRecord = (function() {

        /**
         * Properties of a PendingAirdropRecord.
         * @typedef {Object} proto.PendingAirdropRecord.$Properties
         * @property {proto.PendingAirdropId.$Properties|null} [pendingAirdropId] A unique, composite, identifier for a pending airdrop.
         * <p>
         * This field is REQUIRED.
         * @property {proto.PendingAirdropValue.$Properties|null} [pendingAirdropValue] A single pending airdrop amount.
         * <p>
         * If the pending airdrop is for a fungible/common token this field
         * is REQUIRED and SHALL be the current amount of tokens offered.<br/>
         * If the pending airdrop is for a non-fungible/unique token,
         * this field SHALL NOT be set.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a PendingAirdropRecord.
         * @memberof proto
         * @interface IPendingAirdropRecord
         * @augments proto.PendingAirdropRecord.$Properties
         * @deprecated Use proto.PendingAirdropRecord.$Properties instead.
         */

        /**
         * Shape of a PendingAirdropRecord.
         * @typedef {{
         *   pendingAirdropId?: proto.PendingAirdropId.$Shape|null;
         *   pendingAirdropValue?: proto.PendingAirdropValue.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.PendingAirdropRecord.$Shape
         */

        /**
         * Constructs a new PendingAirdropRecord.
         * @memberof proto
         * @classdesc A record of a new pending airdrop.
         * @constructor
         * @param {proto.PendingAirdropRecord.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const PendingAirdropRecord = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A unique, composite, identifier for a pending airdrop.
         * <p>
         * This field is REQUIRED.
         * @member {proto.PendingAirdropId.$Properties|null|undefined} pendingAirdropId
         * @memberof proto.PendingAirdropRecord
         * @instance
         */
        PendingAirdropRecord.prototype.pendingAirdropId = null;

        /**
         * A single pending airdrop amount.
         * <p>
         * If the pending airdrop is for a fungible/common token this field
         * is REQUIRED and SHALL be the current amount of tokens offered.<br/>
         * If the pending airdrop is for a non-fungible/unique token,
         * this field SHALL NOT be set.
         * @member {proto.PendingAirdropValue.$Properties|null|undefined} pendingAirdropValue
         * @memberof proto.PendingAirdropRecord
         * @instance
         */
        PendingAirdropRecord.prototype.pendingAirdropValue = null;

        /**
         * Creates a new PendingAirdropRecord instance using the specified properties.
         * @function create
         * @memberof proto.PendingAirdropRecord
         * @static
         * @param {proto.PendingAirdropRecord.$Properties=} [properties] Properties to set
         * @returns {proto.PendingAirdropRecord} PendingAirdropRecord instance
         * @type {{
         *   (properties: proto.PendingAirdropRecord.$Shape): proto.PendingAirdropRecord & proto.PendingAirdropRecord.$Shape;
         *   (properties?: proto.PendingAirdropRecord.$Properties): proto.PendingAirdropRecord;
         * }}
         */
        PendingAirdropRecord.create = function(properties) {
            return new PendingAirdropRecord(properties);
        };

        /**
         * Encodes the specified PendingAirdropRecord message. Does not implicitly {@link proto.PendingAirdropRecord.verify|verify} messages.
         * @function encode
         * @memberof proto.PendingAirdropRecord
         * @static
         * @param {proto.PendingAirdropRecord.$Properties} message PendingAirdropRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingAirdropRecord.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pendingAirdropId != null && $Object.hasOwnProperty.call(message, "pendingAirdropId"))
                $root.proto.PendingAirdropId.encode(message.pendingAirdropId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.pendingAirdropValue != null && $Object.hasOwnProperty.call(message, "pendingAirdropValue"))
                $root.proto.PendingAirdropValue.encode(message.pendingAirdropValue, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PendingAirdropRecord message, length delimited. Does not implicitly {@link proto.PendingAirdropRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PendingAirdropRecord
         * @static
         * @param {proto.PendingAirdropRecord.$Properties} message PendingAirdropRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingAirdropRecord.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a PendingAirdropRecord message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PendingAirdropRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PendingAirdropRecord & proto.PendingAirdropRecord.$Shape} PendingAirdropRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingAirdropRecord.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.PendingAirdropRecord();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.pendingAirdropId = $root.proto.PendingAirdropId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pendingAirdropId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.pendingAirdropValue = $root.proto.PendingAirdropValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pendingAirdropValue);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a PendingAirdropRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PendingAirdropRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PendingAirdropRecord & proto.PendingAirdropRecord.$Shape} PendingAirdropRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingAirdropRecord.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PendingAirdropRecord message.
         * @function verify
         * @memberof proto.PendingAirdropRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PendingAirdropRecord.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pendingAirdropId != null && $Object.hasOwnProperty.call(message, "pendingAirdropId")) {
                let error = $root.proto.PendingAirdropId.verify(message.pendingAirdropId, _depth + 1);
                if (error)
                    return "pendingAirdropId." + error;
            }
            if (message.pendingAirdropValue != null && $Object.hasOwnProperty.call(message, "pendingAirdropValue")) {
                let error = $root.proto.PendingAirdropValue.verify(message.pendingAirdropValue, _depth + 1);
                if (error)
                    return "pendingAirdropValue." + error;
            }
            return null;
        };

        /**
         * Creates a PendingAirdropRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PendingAirdropRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PendingAirdropRecord} PendingAirdropRecord
         */
        PendingAirdropRecord.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.PendingAirdropRecord)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.PendingAirdropRecord: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.PendingAirdropRecord();
            if (object.pendingAirdropId != null) {
                if (!$util.isObject(object.pendingAirdropId))
                    throw $TypeError(".proto.PendingAirdropRecord.pendingAirdropId: object expected");
                message.pendingAirdropId = $root.proto.PendingAirdropId.fromObject(object.pendingAirdropId, _depth + 1);
            }
            if (object.pendingAirdropValue != null) {
                if (!$util.isObject(object.pendingAirdropValue))
                    throw $TypeError(".proto.PendingAirdropRecord.pendingAirdropValue: object expected");
                message.pendingAirdropValue = $root.proto.PendingAirdropValue.fromObject(object.pendingAirdropValue, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a PendingAirdropRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PendingAirdropRecord
         * @static
         * @param {proto.PendingAirdropRecord} message PendingAirdropRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PendingAirdropRecord.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.pendingAirdropId = null;
                object.pendingAirdropValue = null;
            }
            if (message.pendingAirdropId != null && $Object.hasOwnProperty.call(message, "pendingAirdropId"))
                object.pendingAirdropId = $root.proto.PendingAirdropId.toObject(message.pendingAirdropId, options, _depth + 1);
            if (message.pendingAirdropValue != null && $Object.hasOwnProperty.call(message, "pendingAirdropValue"))
                object.pendingAirdropValue = $root.proto.PendingAirdropValue.toObject(message.pendingAirdropValue, options, _depth + 1);
            return object;
        };

        /**
         * Converts this PendingAirdropRecord to JSON.
         * @function toJSON
         * @memberof proto.PendingAirdropRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PendingAirdropRecord.prototype.toJSON = function() {
            return PendingAirdropRecord.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for PendingAirdropRecord
         * @function getTypeUrl
         * @memberof proto.PendingAirdropRecord
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        PendingAirdropRecord.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PendingAirdropRecord";
        };

        return PendingAirdropRecord;
    })();

    proto.TransactionReceipt = (function() {

        /**
         * Properties of a TransactionReceipt.
         * @typedef {Object} proto.TransactionReceipt.$Properties
         * @property {proto.ResponseCodeEnum|null} [status] The consensus status of the transaction.
         * <p>
         * This SHALL be `UNKNOWN` if consensus has not been reached.<br/>
         * This SHALL be `UNKNOWN` if the associated transaction did not have
         * a valid payer signature.
         * @property {proto.AccountID.$Properties|null} [accountID] In the receipt of a `CryptoCreate`, the id of the newly created account.
         * @property {proto.FileID.$Properties|null} [fileID] In the receipt of a `FileCreate`, the id of the newly created file.
         * @property {proto.ContractID.$Properties|null} [contractID] In the receipt of a `ContractCreate`, the id of the newly created
         * contract.
         * @property {proto.ExchangeRateSet.$Properties|null} [exchangeRate] The exchange rates in effect when the transaction reached consensus.
         * @property {proto.TopicID.$Properties|null} [topicID] In the receipt of a `ConsensusCreateTopic`, the id of the newly
         * created topic.
         * @property {number|Long|null} [topicSequenceNumber] In the receipt of a `ConsensusSubmitMessage`, the new sequence
         * number for the topic that received the message.
         * @property {Uint8Array|null} [topicRunningHash] In the receipt of a `ConsensusSubmitMessage`, the new running hash of
         * the topic that received the message.<br/>
         * <p>
         * The inputs to the topic running hash have changed over time.<br/>
         * This 48-byte field is the output of a SHA-384 digest with input data
         * determined by the value of the `topicRunningHashVersion` field.<br/>
         * All new transactions SHALL use `topicRunningHashVersion` `3`.<br/>
         * The bytes of each uint64 or uint32 encoded for the hash input MUST be
         * in Big-Endian format.
         * <p>
         * <hr style="margin: 0.2em 5em 0.2em 5em; height: 0.5em; border-style: solid none solid none; border-width: 2px;"/>
         * <p>
         * The most recent version is denoted by `topicRunningHashVersion = 3`.
         * <p>
         * This version SHALL include, in order
         * <ol>
         * <li>The previous running hash of the topic (48 bytes)</li>
         * <li>The `topic_running_hash_version` field (8 bytes)</li>
         * <li>The payer account's shard (8 bytes)</li>
         * <li>The payer account's realm (8 bytes)</li>
         * <li>The payer account's number (8 bytes)</li>
         * <li>The topic's shard (8 bytes)</li>
         * <li>The topic's realm (8 bytes)</li>
         * <li>The topic's number (8 bytes)</li>
         * <li>The number of seconds since the epoch when the
         * `ConsensusSubmitMessage` reached consensus (8 bytes)</li>
         * <li>The number of nanoseconds within the second when the
         * `ConsensusSubmitMessage` reached consensus (4 bytes)</li>
         * <li>The `topic_sequence_number` field (8 bytes)</li>
         * <li>The output of a SHA-384 digest of the message bytes from the
         * `ConsensusSubmitMessage` (48 bytes)</li>
         * </ol>
         * <hr style="margin: 0.2em 5em 0.2em 5em; height: 0.5em; border-style: solid none solid none; border-width: 2px;"/>
         * <p>
         * The next older version is denoted by `topicRunningHashVersion = 2`.
         * <p>
         * This version SHALL include, in order
         * <ol>
         * <li>The previous running hash of the topic (48 bytes)</li>
         * <li>The `topic_running_hash_version` field (8 bytes)</li>
         * <li>The topic's shard (8 bytes)</li>
         * <li>The topic's realm (8 bytes)</li>
         * <li>The topic's number (8 bytes)</li>
         * <li>The number of seconds since the epoch when the
         * `ConsensusSubmitMessage` reached consensus (8 bytes)</li>
         * <li>The number of nanoseconds within the second when the
         * `ConsensusSubmitMessage` reached consensus (4 bytes)</li>
         * <li>The `topic_sequence_number` field (8 bytes)</li>
         * <li>The output of a SHA-384 digest of the message bytes from the
         * `ConsensusSubmitMessage` (48 bytes)</li>
         * </ol>
         * <hr style="margin: 0.2em 5em 0.2em 5em; height: 0.5em; border-style: solid none solid none; border-width: 2px;"/>
         * <p>
         * The original version, used at genesis, is denoted
         * by `topicRunningHashVersion = 1` or `topicRunningHashVersion = 0`.
         * <p>
         * This version SHALL include, in order
         * <ol>
         * <li>The previous running hash of the topic (48 bytes)</li>
         * <li>The topic's shard (8 bytes)</li>
         * <li>The topic's realm (8 bytes)</li>
         * <li>The topic's number (8 bytes)</li>
         * <li>The number of seconds since the epoch when the
         * `ConsensusSubmitMessage` reached consensus (8 bytes)</li>
         * <li>The number of nanoseconds within the second when the
         * `ConsensusSubmitMessage` reached consensus (4 bytes)</li>
         * <li>The `topic_sequence_number` field (8 bytes)</li>
         * <li>The message bytes from the `ConsensusSubmitMessage`
         * (variable)</li>
         * </ol>
         * @property {number|Long|null} [topicRunningHashVersion] In the receipt of a `ConsensusSubmitMessage`, the version of the
         * SHA-384 digest inputs used to update the running hash.
         * @property {proto.TokenID.$Properties|null} [tokenID] In the receipt of a `CreateToken`, the id of the newly created token
         * @property {number|Long|null} [newTotalSupply] In the receipt of `TokenMint`, `TokenWipe`, or `TokenBurn`.<br/>
         * For non-unique tokens, the current total supply of that token.<br/>
         * For unique tokens,the total number of NFTs issued for that token.
         * @property {proto.ScheduleID.$Properties|null} [scheduleID] In the receipt of a `ScheduleCreate`, the id of the newly created
         * Scheduled Entity
         * @property {proto.TransactionID.$Properties|null} [scheduledTransactionID] In the receipt of a `ScheduleCreate` or `ScheduleSign` that enables the
         * scheduled transaction to execute immediately, the `TransactionID` that
         * should be used to query for the receipt or record of the scheduled
         * transaction that was executed.
         * @property {Array.<number|Long>|null} [serialNumbers] In the receipt of a `TokenMint` for non-fungible/unique tokens,
         * the serial numbers of the newly created tokens.
         * @property {number|Long|null} [nodeId] An affected node identifier.<br/>
         * In the receipt of a NodeCreate, the id of the newly created node.
         * <p>
         * This value SHALL be set following a `createNode` transaction.<br/>
         * This value SHALL NOT be set following any other transaction.
         * @property {number|Long|null} [registeredNodeId] The identifier of a newly created RegisteredNode.
         * <p>
         * This value SHALL be set following a `createRegisteredNode`
         * transaction.<br/>
         * This value SHALL NOT be set following any other transaction.<br/>
         * This value SHALL be unique within a given network.
         * @property {google.protobuf.UInt64Value.$Properties|null} [blockNumber] The block number containing this transaction.
         * <p>
         * This value SHALL be unset or default if consensus has not been reached
         * or the block assignment is not yet known.<br/>
         * Once known, this value SHALL identify the block containing the
         * transaction represented by this receipt.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionReceipt.
         * @memberof proto
         * @interface ITransactionReceipt
         * @augments proto.TransactionReceipt.$Properties
         * @deprecated Use proto.TransactionReceipt.$Properties instead.
         */

        /**
         * Shape of a TransactionReceipt.
         * @typedef {{
         *   status?: proto.ResponseCodeEnum|null;
         *   accountID?: proto.AccountID.$Shape|null;
         *   fileID?: proto.FileID.$Shape|null;
         *   contractID?: proto.ContractID.$Shape|null;
         *   exchangeRate?: proto.ExchangeRateSet.$Shape|null;
         *   topicID?: proto.TopicID.$Shape|null;
         *   topicSequenceNumber?: number|Long|null;
         *   topicRunningHash?: Uint8Array|null;
         *   topicRunningHashVersion?: number|Long|null;
         *   tokenID?: proto.TokenID.$Shape|null;
         *   newTotalSupply?: number|Long|null;
         *   scheduleID?: proto.ScheduleID.$Shape|null;
         *   scheduledTransactionID?: proto.TransactionID.$Shape|null;
         *   serialNumbers?: Array.<number|Long>|null;
         *   nodeId?: number|Long|null;
         *   registeredNodeId?: number|Long|null;
         *   blockNumber?: google.protobuf.UInt64Value.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TransactionReceipt.$Shape
         */

        /**
         * Constructs a new TransactionReceipt.
         * @memberof proto
         * @classdesc The summary of a transaction's result so far.<br/>
         * If the transaction has not reached consensus, this result will
         * be necessarily incomplete.
         * 
         * Most items in this object are only set for specific transactions.
         * Those values SHALL be unset for all other transactions.
         * @constructor
         * @param {proto.TransactionReceipt.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionReceipt = function (properties) {
            this.serialNumbers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The consensus status of the transaction.
         * <p>
         * This SHALL be `UNKNOWN` if consensus has not been reached.<br/>
         * This SHALL be `UNKNOWN` if the associated transaction did not have
         * a valid payer signature.
         * @member {proto.ResponseCodeEnum} status
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.status = 0;

        /**
         * In the receipt of a `CryptoCreate`, the id of the newly created account.
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.accountID = null;

        /**
         * In the receipt of a `FileCreate`, the id of the newly created file.
         * @member {proto.FileID.$Properties|null|undefined} fileID
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.fileID = null;

        /**
         * In the receipt of a `ContractCreate`, the id of the newly created
         * contract.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.contractID = null;

        /**
         * The exchange rates in effect when the transaction reached consensus.
         * @member {proto.ExchangeRateSet.$Properties|null|undefined} exchangeRate
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.exchangeRate = null;

        /**
         * In the receipt of a `ConsensusCreateTopic`, the id of the newly
         * created topic.
         * @member {proto.TopicID.$Properties|null|undefined} topicID
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.topicID = null;

        /**
         * In the receipt of a `ConsensusSubmitMessage`, the new sequence
         * number for the topic that received the message.
         * @member {number|Long} topicSequenceNumber
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.topicSequenceNumber = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * In the receipt of a `ConsensusSubmitMessage`, the new running hash of
         * the topic that received the message.<br/>
         * <p>
         * The inputs to the topic running hash have changed over time.<br/>
         * This 48-byte field is the output of a SHA-384 digest with input data
         * determined by the value of the `topicRunningHashVersion` field.<br/>
         * All new transactions SHALL use `topicRunningHashVersion` `3`.<br/>
         * The bytes of each uint64 or uint32 encoded for the hash input MUST be
         * in Big-Endian format.
         * <p>
         * <hr style="margin: 0.2em 5em 0.2em 5em; height: 0.5em; border-style: solid none solid none; border-width: 2px;"/>
         * <p>
         * The most recent version is denoted by `topicRunningHashVersion = 3`.
         * <p>
         * This version SHALL include, in order
         * <ol>
         * <li>The previous running hash of the topic (48 bytes)</li>
         * <li>The `topic_running_hash_version` field (8 bytes)</li>
         * <li>The payer account's shard (8 bytes)</li>
         * <li>The payer account's realm (8 bytes)</li>
         * <li>The payer account's number (8 bytes)</li>
         * <li>The topic's shard (8 bytes)</li>
         * <li>The topic's realm (8 bytes)</li>
         * <li>The topic's number (8 bytes)</li>
         * <li>The number of seconds since the epoch when the
         * `ConsensusSubmitMessage` reached consensus (8 bytes)</li>
         * <li>The number of nanoseconds within the second when the
         * `ConsensusSubmitMessage` reached consensus (4 bytes)</li>
         * <li>The `topic_sequence_number` field (8 bytes)</li>
         * <li>The output of a SHA-384 digest of the message bytes from the
         * `ConsensusSubmitMessage` (48 bytes)</li>
         * </ol>
         * <hr style="margin: 0.2em 5em 0.2em 5em; height: 0.5em; border-style: solid none solid none; border-width: 2px;"/>
         * <p>
         * The next older version is denoted by `topicRunningHashVersion = 2`.
         * <p>
         * This version SHALL include, in order
         * <ol>
         * <li>The previous running hash of the topic (48 bytes)</li>
         * <li>The `topic_running_hash_version` field (8 bytes)</li>
         * <li>The topic's shard (8 bytes)</li>
         * <li>The topic's realm (8 bytes)</li>
         * <li>The topic's number (8 bytes)</li>
         * <li>The number of seconds since the epoch when the
         * `ConsensusSubmitMessage` reached consensus (8 bytes)</li>
         * <li>The number of nanoseconds within the second when the
         * `ConsensusSubmitMessage` reached consensus (4 bytes)</li>
         * <li>The `topic_sequence_number` field (8 bytes)</li>
         * <li>The output of a SHA-384 digest of the message bytes from the
         * `ConsensusSubmitMessage` (48 bytes)</li>
         * </ol>
         * <hr style="margin: 0.2em 5em 0.2em 5em; height: 0.5em; border-style: solid none solid none; border-width: 2px;"/>
         * <p>
         * The original version, used at genesis, is denoted
         * by `topicRunningHashVersion = 1` or `topicRunningHashVersion = 0`.
         * <p>
         * This version SHALL include, in order
         * <ol>
         * <li>The previous running hash of the topic (48 bytes)</li>
         * <li>The topic's shard (8 bytes)</li>
         * <li>The topic's realm (8 bytes)</li>
         * <li>The topic's number (8 bytes)</li>
         * <li>The number of seconds since the epoch when the
         * `ConsensusSubmitMessage` reached consensus (8 bytes)</li>
         * <li>The number of nanoseconds within the second when the
         * `ConsensusSubmitMessage` reached consensus (4 bytes)</li>
         * <li>The `topic_sequence_number` field (8 bytes)</li>
         * <li>The message bytes from the `ConsensusSubmitMessage`
         * (variable)</li>
         * </ol>
         * @member {Uint8Array} topicRunningHash
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.topicRunningHash = $util.newBuffer([]);

        /**
         * In the receipt of a `ConsensusSubmitMessage`, the version of the
         * SHA-384 digest inputs used to update the running hash.
         * @member {number|Long} topicRunningHashVersion
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.topicRunningHashVersion = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * In the receipt of a `CreateToken`, the id of the newly created token
         * @member {proto.TokenID.$Properties|null|undefined} tokenID
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.tokenID = null;

        /**
         * In the receipt of `TokenMint`, `TokenWipe`, or `TokenBurn`.<br/>
         * For non-unique tokens, the current total supply of that token.<br/>
         * For unique tokens,the total number of NFTs issued for that token.
         * @member {number|Long} newTotalSupply
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.newTotalSupply = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * In the receipt of a `ScheduleCreate`, the id of the newly created
         * Scheduled Entity
         * @member {proto.ScheduleID.$Properties|null|undefined} scheduleID
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.scheduleID = null;

        /**
         * In the receipt of a `ScheduleCreate` or `ScheduleSign` that enables the
         * scheduled transaction to execute immediately, the `TransactionID` that
         * should be used to query for the receipt or record of the scheduled
         * transaction that was executed.
         * @member {proto.TransactionID.$Properties|null|undefined} scheduledTransactionID
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.scheduledTransactionID = null;

        /**
         * In the receipt of a `TokenMint` for non-fungible/unique tokens,
         * the serial numbers of the newly created tokens.
         * @member {Array.<number|Long>} serialNumbers
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.serialNumbers = $util.emptyArray;

        /**
         * An affected node identifier.<br/>
         * In the receipt of a NodeCreate, the id of the newly created node.
         * <p>
         * This value SHALL be set following a `createNode` transaction.<br/>
         * This value SHALL NOT be set following any other transaction.
         * @member {number|Long} nodeId
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * The identifier of a newly created RegisteredNode.
         * <p>
         * This value SHALL be set following a `createRegisteredNode`
         * transaction.<br/>
         * This value SHALL NOT be set following any other transaction.<br/>
         * This value SHALL be unique within a given network.
         * @member {number|Long} registeredNodeId
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.registeredNodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * The block number containing this transaction.
         * <p>
         * This value SHALL be unset or default if consensus has not been reached
         * or the block assignment is not yet known.<br/>
         * Once known, this value SHALL identify the block containing the
         * transaction represented by this receipt.
         * @member {google.protobuf.UInt64Value.$Properties|null|undefined} blockNumber
         * @memberof proto.TransactionReceipt
         * @instance
         */
        TransactionReceipt.prototype.blockNumber = null;

        /**
         * Creates a new TransactionReceipt instance using the specified properties.
         * @function create
         * @memberof proto.TransactionReceipt
         * @static
         * @param {proto.TransactionReceipt.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionReceipt} TransactionReceipt instance
         * @type {{
         *   (properties: proto.TransactionReceipt.$Shape): proto.TransactionReceipt & proto.TransactionReceipt.$Shape;
         *   (properties?: proto.TransactionReceipt.$Properties): proto.TransactionReceipt;
         * }}
         */
        TransactionReceipt.create = function(properties) {
            return new TransactionReceipt(properties);
        };

        /**
         * Encodes the specified TransactionReceipt message. Does not implicitly {@link proto.TransactionReceipt.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionReceipt
         * @static
         * @param {proto.TransactionReceipt.$Properties} message TransactionReceipt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionReceipt.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.exchangeRate != null && $Object.hasOwnProperty.call(message, "exchangeRate"))
                $root.proto.ExchangeRateSet.encode(message.exchangeRate, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID"))
                $root.proto.TopicID.encode(message.topicID, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.topicSequenceNumber != null && $Object.hasOwnProperty.call(message, "topicSequenceNumber") && (typeof message.topicSequenceNumber === "object" ? message.topicSequenceNumber.low || message.topicSequenceNumber.high : message.topicSequenceNumber !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.topicSequenceNumber);
            if (message.topicRunningHash != null && $Object.hasOwnProperty.call(message, "topicRunningHash") && message.topicRunningHash.length)
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.topicRunningHash);
            if (message.topicRunningHashVersion != null && $Object.hasOwnProperty.call(message, "topicRunningHashVersion") && (typeof message.topicRunningHashVersion === "object" ? message.topicRunningHashVersion.low || message.topicRunningHashVersion.high : message.topicRunningHashVersion !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.topicRunningHashVersion);
            if (message.tokenID != null && $Object.hasOwnProperty.call(message, "tokenID"))
                $root.proto.TokenID.encode(message.tokenID, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.newTotalSupply != null && $Object.hasOwnProperty.call(message, "newTotalSupply") && (typeof message.newTotalSupply === "object" ? message.newTotalSupply.low || message.newTotalSupply.high : message.newTotalSupply !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.newTotalSupply);
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID"))
                $root.proto.ScheduleID.encode(message.scheduleID, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.scheduledTransactionID != null && $Object.hasOwnProperty.call(message, "scheduledTransactionID"))
                $root.proto.TransactionID.encode(message.scheduledTransactionID, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.serialNumbers != null && message.serialNumbers.length)
                writer.uint32(/* id 14, wireType 2 =*/114).int64s(message.serialNumbers);
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                writer.uint32(/* id 15, wireType 0 =*/120).uint64(message.nodeId);
            if (message.registeredNodeId != null && $Object.hasOwnProperty.call(message, "registeredNodeId") && (typeof message.registeredNodeId === "object" ? message.registeredNodeId.low || message.registeredNodeId.high : message.registeredNodeId !== 0))
                writer.uint32(/* id 16, wireType 0 =*/128).uint64(message.registeredNodeId);
            if (message.blockNumber != null && $Object.hasOwnProperty.call(message, "blockNumber"))
                $root.google.protobuf.UInt64Value.encode(message.blockNumber, writer.uint32(/* id 17, wireType 2 =*/138).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionReceipt message, length delimited. Does not implicitly {@link proto.TransactionReceipt.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionReceipt
         * @static
         * @param {proto.TransactionReceipt.$Properties} message TransactionReceipt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionReceipt.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionReceipt message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionReceipt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionReceipt & proto.TransactionReceipt.$Shape} TransactionReceipt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionReceipt.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionReceipt();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.exchangeRate = $root.proto.ExchangeRateSet.decode(reader, reader.uint32(), $undefined, _depth + 1, message.exchangeRate);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.topicID = $root.proto.TopicID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.topicID);
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.topicSequenceNumber = value;
                        else
                            delete message.topicSequenceNumber;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.topicRunningHash = value;
                        else
                            delete message.topicRunningHash;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.topicRunningHashVersion = value;
                        else
                            delete message.topicRunningHashVersion;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.tokenID = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenID);
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.newTotalSupply = value;
                        else
                            delete message.newTotalSupply;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.scheduleID = $root.proto.ScheduleID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleID);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.scheduledTransactionID = $root.proto.TransactionID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduledTransactionID);
                        continue;
                    }
                case 14: {
                        if (wireType === 2) {
                            if (!(message.serialNumbers && message.serialNumbers.length))
                                message.serialNumbers = [];
                            reader.int64s(message.serialNumbers);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.serialNumbers && message.serialNumbers.length))
                            message.serialNumbers = [];
                        message.serialNumbers.push(reader.int64());
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.nodeId = value;
                        else
                            delete message.nodeId;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.registeredNodeId = value;
                        else
                            delete message.registeredNodeId;
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        message.blockNumber = $root.google.protobuf.UInt64Value.decode(reader, reader.uint32(), $undefined, _depth + 1, message.blockNumber);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionReceipt message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionReceipt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionReceipt & proto.TransactionReceipt.$Shape} TransactionReceipt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionReceipt.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionReceipt message.
         * @function verify
         * @memberof proto.TransactionReceipt
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionReceipt.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                    return "status: enum value expected";
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                if (error)
                    return "fileID." + error;
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            if (message.exchangeRate != null && $Object.hasOwnProperty.call(message, "exchangeRate")) {
                let error = $root.proto.ExchangeRateSet.verify(message.exchangeRate, _depth + 1);
                if (error)
                    return "exchangeRate." + error;
            }
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID")) {
                let error = $root.proto.TopicID.verify(message.topicID, _depth + 1);
                if (error)
                    return "topicID." + error;
            }
            if (message.topicSequenceNumber != null && $Object.hasOwnProperty.call(message, "topicSequenceNumber"))
                if (!$util.isInteger(message.topicSequenceNumber) && !(message.topicSequenceNumber && $util.isInteger(message.topicSequenceNumber.low) && $util.isInteger(message.topicSequenceNumber.high)))
                    return "topicSequenceNumber: integer|Long expected";
            if (message.topicRunningHash != null && $Object.hasOwnProperty.call(message, "topicRunningHash"))
                if (!(message.topicRunningHash && typeof message.topicRunningHash.length === "number" || $util.isString(message.topicRunningHash)))
                    return "topicRunningHash: buffer expected";
            if (message.topicRunningHashVersion != null && $Object.hasOwnProperty.call(message, "topicRunningHashVersion"))
                if (!$util.isInteger(message.topicRunningHashVersion) && !(message.topicRunningHashVersion && $util.isInteger(message.topicRunningHashVersion.low) && $util.isInteger(message.topicRunningHashVersion.high)))
                    return "topicRunningHashVersion: integer|Long expected";
            if (message.tokenID != null && $Object.hasOwnProperty.call(message, "tokenID")) {
                let error = $root.proto.TokenID.verify(message.tokenID, _depth + 1);
                if (error)
                    return "tokenID." + error;
            }
            if (message.newTotalSupply != null && $Object.hasOwnProperty.call(message, "newTotalSupply"))
                if (!$util.isInteger(message.newTotalSupply) && !(message.newTotalSupply && $util.isInteger(message.newTotalSupply.low) && $util.isInteger(message.newTotalSupply.high)))
                    return "newTotalSupply: integer|Long expected";
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID")) {
                let error = $root.proto.ScheduleID.verify(message.scheduleID, _depth + 1);
                if (error)
                    return "scheduleID." + error;
            }
            if (message.scheduledTransactionID != null && $Object.hasOwnProperty.call(message, "scheduledTransactionID")) {
                let error = $root.proto.TransactionID.verify(message.scheduledTransactionID, _depth + 1);
                if (error)
                    return "scheduledTransactionID." + error;
            }
            if (message.serialNumbers != null && $Object.hasOwnProperty.call(message, "serialNumbers")) {
                if (!$Array.isArray(message.serialNumbers))
                    return "serialNumbers: array expected";
                for (let i = 0; i < message.serialNumbers.length; ++i)
                    if (!$util.isInteger(message.serialNumbers[i]) && !(message.serialNumbers[i] && $util.isInteger(message.serialNumbers[i].low) && $util.isInteger(message.serialNumbers[i].high)))
                        return "serialNumbers: integer|Long[] expected";
            }
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                    return "nodeId: integer|Long expected";
            if (message.registeredNodeId != null && $Object.hasOwnProperty.call(message, "registeredNodeId"))
                if (!$util.isInteger(message.registeredNodeId) && !(message.registeredNodeId && $util.isInteger(message.registeredNodeId.low) && $util.isInteger(message.registeredNodeId.high)))
                    return "registeredNodeId: integer|Long expected";
            if (message.blockNumber != null && $Object.hasOwnProperty.call(message, "blockNumber")) {
                let error = $root.google.protobuf.UInt64Value.verify(message.blockNumber, _depth + 1);
                if (error)
                    return "blockNumber." + error;
            }
            return null;
        };

        /**
         * Creates a TransactionReceipt message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionReceipt
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionReceipt} TransactionReceipt
         */
        TransactionReceipt.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionReceipt)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionReceipt: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionReceipt();
            if (object.status !== 0 && (typeof object.status !== "string" || $root.proto.ResponseCodeEnum[object.status] !== 0))
                switch (object.status) {
                case "OK":
                case 0:
                    message.status = 0;
                    break;
                case "INVALID_TRANSACTION":
                case 1:
                    message.status = 1;
                    break;
                case "PAYER_ACCOUNT_NOT_FOUND":
                case 2:
                    message.status = 2;
                    break;
                case "INVALID_NODE_ACCOUNT":
                case 3:
                    message.status = 3;
                    break;
                case "TRANSACTION_EXPIRED":
                case 4:
                    message.status = 4;
                    break;
                case "INVALID_TRANSACTION_START":
                case 5:
                    message.status = 5;
                    break;
                case "INVALID_TRANSACTION_DURATION":
                case 6:
                    message.status = 6;
                    break;
                case "INVALID_SIGNATURE":
                case 7:
                    message.status = 7;
                    break;
                case "MEMO_TOO_LONG":
                case 8:
                    message.status = 8;
                    break;
                case "INSUFFICIENT_TX_FEE":
                case 9:
                    message.status = 9;
                    break;
                case "INSUFFICIENT_PAYER_BALANCE":
                case 10:
                    message.status = 10;
                    break;
                case "DUPLICATE_TRANSACTION":
                case 11:
                    message.status = 11;
                    break;
                case "BUSY":
                case 12:
                    message.status = 12;
                    break;
                case "NOT_SUPPORTED":
                case 13:
                    message.status = 13;
                    break;
                case "INVALID_FILE_ID":
                case 14:
                    message.status = 14;
                    break;
                case "INVALID_ACCOUNT_ID":
                case 15:
                    message.status = 15;
                    break;
                case "INVALID_CONTRACT_ID":
                case 16:
                    message.status = 16;
                    break;
                case "INVALID_TRANSACTION_ID":
                case 17:
                    message.status = 17;
                    break;
                case "RECEIPT_NOT_FOUND":
                case 18:
                    message.status = 18;
                    break;
                case "RECORD_NOT_FOUND":
                case 19:
                    message.status = 19;
                    break;
                case "INVALID_SOLIDITY_ID":
                case 20:
                    message.status = 20;
                    break;
                case "UNKNOWN":
                case 21:
                    message.status = 21;
                    break;
                case "SUCCESS":
                case 22:
                    message.status = 22;
                    break;
                case "FAIL_INVALID":
                case 23:
                    message.status = 23;
                    break;
                case "FAIL_FEE":
                case 24:
                    message.status = 24;
                    break;
                case "FAIL_BALANCE":
                case 25:
                    message.status = 25;
                    break;
                case "KEY_REQUIRED":
                case 26:
                    message.status = 26;
                    break;
                case "BAD_ENCODING":
                case 27:
                    message.status = 27;
                    break;
                case "INSUFFICIENT_ACCOUNT_BALANCE":
                case 28:
                    message.status = 28;
                    break;
                case "INVALID_SOLIDITY_ADDRESS":
                case 29:
                    message.status = 29;
                    break;
                case "INSUFFICIENT_GAS":
                case 30:
                    message.status = 30;
                    break;
                case "CONTRACT_SIZE_LIMIT_EXCEEDED":
                case 31:
                    message.status = 31;
                    break;
                case "LOCAL_CALL_MODIFICATION_EXCEPTION":
                case 32:
                    message.status = 32;
                    break;
                case "CONTRACT_REVERT_EXECUTED":
                case 33:
                    message.status = 33;
                    break;
                case "CONTRACT_EXECUTION_EXCEPTION":
                case 34:
                    message.status = 34;
                    break;
                case "INVALID_RECEIVING_NODE_ACCOUNT":
                case 35:
                    message.status = 35;
                    break;
                case "MISSING_QUERY_HEADER":
                case 36:
                    message.status = 36;
                    break;
                case "ACCOUNT_UPDATE_FAILED":
                case 37:
                    message.status = 37;
                    break;
                case "INVALID_KEY_ENCODING":
                case 38:
                    message.status = 38;
                    break;
                case "NULL_SOLIDITY_ADDRESS":
                case 39:
                    message.status = 39;
                    break;
                case "CONTRACT_UPDATE_FAILED":
                case 40:
                    message.status = 40;
                    break;
                case "INVALID_QUERY_HEADER":
                case 41:
                    message.status = 41;
                    break;
                case "INVALID_FEE_SUBMITTED":
                case 42:
                    message.status = 42;
                    break;
                case "INVALID_PAYER_SIGNATURE":
                case 43:
                    message.status = 43;
                    break;
                case "KEY_NOT_PROVIDED":
                case 44:
                    message.status = 44;
                    break;
                case "INVALID_EXPIRATION_TIME":
                case 45:
                    message.status = 45;
                    break;
                case "NO_WACL_KEY":
                case 46:
                    message.status = 46;
                    break;
                case "FILE_CONTENT_EMPTY":
                case 47:
                    message.status = 47;
                    break;
                case "INVALID_ACCOUNT_AMOUNTS":
                case 48:
                    message.status = 48;
                    break;
                case "EMPTY_TRANSACTION_BODY":
                case 49:
                    message.status = 49;
                    break;
                case "INVALID_TRANSACTION_BODY":
                case 50:
                    message.status = 50;
                    break;
                case "INVALID_SIGNATURE_TYPE_MISMATCHING_KEY":
                case 51:
                    message.status = 51;
                    break;
                case "INVALID_SIGNATURE_COUNT_MISMATCHING_KEY":
                case 52:
                    message.status = 52;
                    break;
                case "EMPTY_LIVE_HASH_BODY":
                case 53:
                    message.status = 53;
                    break;
                case "EMPTY_LIVE_HASH":
                case 54:
                    message.status = 54;
                    break;
                case "EMPTY_LIVE_HASH_KEYS":
                case 55:
                    message.status = 55;
                    break;
                case "INVALID_LIVE_HASH_SIZE":
                case 56:
                    message.status = 56;
                    break;
                case "EMPTY_QUERY_BODY":
                case 57:
                    message.status = 57;
                    break;
                case "EMPTY_LIVE_HASH_QUERY":
                case 58:
                    message.status = 58;
                    break;
                case "LIVE_HASH_NOT_FOUND":
                case 59:
                    message.status = 59;
                    break;
                case "ACCOUNT_ID_DOES_NOT_EXIST":
                case 60:
                    message.status = 60;
                    break;
                case "LIVE_HASH_ALREADY_EXISTS":
                case 61:
                    message.status = 61;
                    break;
                case "INVALID_FILE_WACL":
                case 62:
                    message.status = 62;
                    break;
                case "SERIALIZATION_FAILED":
                case 63:
                    message.status = 63;
                    break;
                case "TRANSACTION_OVERSIZE":
                case 64:
                    message.status = 64;
                    break;
                case "TRANSACTION_TOO_MANY_LAYERS":
                case 65:
                    message.status = 65;
                    break;
                case "CONTRACT_DELETED":
                case 66:
                    message.status = 66;
                    break;
                case "PLATFORM_NOT_ACTIVE":
                case 67:
                    message.status = 67;
                    break;
                case "KEY_PREFIX_MISMATCH":
                case 68:
                    message.status = 68;
                    break;
                case "PLATFORM_TRANSACTION_NOT_CREATED":
                case 69:
                    message.status = 69;
                    break;
                case "INVALID_RENEWAL_PERIOD":
                case 70:
                    message.status = 70;
                    break;
                case "INVALID_PAYER_ACCOUNT_ID":
                case 71:
                    message.status = 71;
                    break;
                case "ACCOUNT_DELETED":
                case 72:
                    message.status = 72;
                    break;
                case "FILE_DELETED":
                case 73:
                    message.status = 73;
                    break;
                case "ACCOUNT_REPEATED_IN_ACCOUNT_AMOUNTS":
                case 74:
                    message.status = 74;
                    break;
                case "SETTING_NEGATIVE_ACCOUNT_BALANCE":
                case 75:
                    message.status = 75;
                    break;
                case "OBTAINER_REQUIRED":
                case 76:
                    message.status = 76;
                    break;
                case "OBTAINER_SAME_CONTRACT_ID":
                case 77:
                    message.status = 77;
                    break;
                case "OBTAINER_DOES_NOT_EXIST":
                case 78:
                    message.status = 78;
                    break;
                case "MODIFYING_IMMUTABLE_CONTRACT":
                case 79:
                    message.status = 79;
                    break;
                case "FILE_SYSTEM_EXCEPTION":
                case 80:
                    message.status = 80;
                    break;
                case "AUTORENEW_DURATION_NOT_IN_RANGE":
                case 81:
                    message.status = 81;
                    break;
                case "ERROR_DECODING_BYTESTRING":
                case 82:
                    message.status = 82;
                    break;
                case "CONTRACT_FILE_EMPTY":
                case 83:
                    message.status = 83;
                    break;
                case "CONTRACT_BYTECODE_EMPTY":
                case 84:
                    message.status = 84;
                    break;
                case "INVALID_INITIAL_BALANCE":
                case 85:
                    message.status = 85;
                    break;
                case "INVALID_RECEIVE_RECORD_THRESHOLD":
                case 86:
                    message.status = 86;
                    break;
                case "INVALID_SEND_RECORD_THRESHOLD":
                case 87:
                    message.status = 87;
                    break;
                case "ACCOUNT_IS_NOT_GENESIS_ACCOUNT":
                case 88:
                    message.status = 88;
                    break;
                case "PAYER_ACCOUNT_UNAUTHORIZED":
                case 89:
                    message.status = 89;
                    break;
                case "INVALID_FREEZE_TRANSACTION_BODY":
                case 90:
                    message.status = 90;
                    break;
                case "FREEZE_TRANSACTION_BODY_NOT_FOUND":
                case 91:
                    message.status = 91;
                    break;
                case "TRANSFER_LIST_SIZE_LIMIT_EXCEEDED":
                case 92:
                    message.status = 92;
                    break;
                case "RESULT_SIZE_LIMIT_EXCEEDED":
                case 93:
                    message.status = 93;
                    break;
                case "NOT_SPECIAL_ACCOUNT":
                case 94:
                    message.status = 94;
                    break;
                case "CONTRACT_NEGATIVE_GAS":
                case 95:
                    message.status = 95;
                    break;
                case "CONTRACT_NEGATIVE_VALUE":
                case 96:
                    message.status = 96;
                    break;
                case "INVALID_FEE_FILE":
                case 97:
                    message.status = 97;
                    break;
                case "INVALID_EXCHANGE_RATE_FILE":
                case 98:
                    message.status = 98;
                    break;
                case "INSUFFICIENT_LOCAL_CALL_GAS":
                case 99:
                    message.status = 99;
                    break;
                case "ENTITY_NOT_ALLOWED_TO_DELETE":
                case 100:
                    message.status = 100;
                    break;
                case "AUTHORIZATION_FAILED":
                case 101:
                    message.status = 101;
                    break;
                case "FILE_UPLOADED_PROTO_INVALID":
                case 102:
                    message.status = 102;
                    break;
                case "FILE_UPLOADED_PROTO_NOT_SAVED_TO_DISK":
                case 103:
                    message.status = 103;
                    break;
                case "FEE_SCHEDULE_FILE_PART_UPLOADED":
                case 104:
                    message.status = 104;
                    break;
                case "EXCHANGE_RATE_CHANGE_LIMIT_EXCEEDED":
                case 105:
                    message.status = 105;
                    break;
                case "MAX_CONTRACT_STORAGE_EXCEEDED":
                case 106:
                    message.status = 106;
                    break;
                case "TRANSFER_ACCOUNT_SAME_AS_DELETE_ACCOUNT":
                case 107:
                    message.status = 107;
                    break;
                case "TOTAL_LEDGER_BALANCE_INVALID":
                case 108:
                    message.status = 108;
                    break;
                case "EXPIRATION_REDUCTION_NOT_ALLOWED":
                case 110:
                    message.status = 110;
                    break;
                case "MAX_GAS_LIMIT_EXCEEDED":
                case 111:
                    message.status = 111;
                    break;
                case "MAX_FILE_SIZE_EXCEEDED":
                case 112:
                    message.status = 112;
                    break;
                case "RECEIVER_SIG_REQUIRED":
                case 113:
                    message.status = 113;
                    break;
                case "INVALID_TOPIC_ID":
                case 150:
                    message.status = 150;
                    break;
                case "INVALID_ADMIN_KEY":
                case 155:
                    message.status = 155;
                    break;
                case "INVALID_SUBMIT_KEY":
                case 156:
                    message.status = 156;
                    break;
                case "UNAUTHORIZED":
                case 157:
                    message.status = 157;
                    break;
                case "INVALID_TOPIC_MESSAGE":
                case 158:
                    message.status = 158;
                    break;
                case "INVALID_AUTORENEW_ACCOUNT":
                case 159:
                    message.status = 159;
                    break;
                case "AUTORENEW_ACCOUNT_NOT_ALLOWED":
                case 160:
                    message.status = 160;
                    break;
                case "TOPIC_EXPIRED":
                case 162:
                    message.status = 162;
                    break;
                case "INVALID_CHUNK_NUMBER":
                case 163:
                    message.status = 163;
                    break;
                case "INVALID_CHUNK_TRANSACTION_ID":
                case 164:
                    message.status = 164;
                    break;
                case "ACCOUNT_FROZEN_FOR_TOKEN":
                case 165:
                    message.status = 165;
                    break;
                case "TOKENS_PER_ACCOUNT_LIMIT_EXCEEDED":
                case 166:
                    message.status = 166;
                    break;
                case "INVALID_TOKEN_ID":
                case 167:
                    message.status = 167;
                    break;
                case "INVALID_TOKEN_DECIMALS":
                case 168:
                    message.status = 168;
                    break;
                case "INVALID_TOKEN_INITIAL_SUPPLY":
                case 169:
                    message.status = 169;
                    break;
                case "INVALID_TREASURY_ACCOUNT_FOR_TOKEN":
                case 170:
                    message.status = 170;
                    break;
                case "INVALID_TOKEN_SYMBOL":
                case 171:
                    message.status = 171;
                    break;
                case "TOKEN_HAS_NO_FREEZE_KEY":
                case 172:
                    message.status = 172;
                    break;
                case "TRANSFERS_NOT_ZERO_SUM_FOR_TOKEN":
                case 173:
                    message.status = 173;
                    break;
                case "MISSING_TOKEN_SYMBOL":
                case 174:
                    message.status = 174;
                    break;
                case "TOKEN_SYMBOL_TOO_LONG":
                case 175:
                    message.status = 175;
                    break;
                case "ACCOUNT_KYC_NOT_GRANTED_FOR_TOKEN":
                case 176:
                    message.status = 176;
                    break;
                case "TOKEN_HAS_NO_KYC_KEY":
                case 177:
                    message.status = 177;
                    break;
                case "INSUFFICIENT_TOKEN_BALANCE":
                case 178:
                    message.status = 178;
                    break;
                case "TOKEN_WAS_DELETED":
                case 179:
                    message.status = 179;
                    break;
                case "TOKEN_HAS_NO_SUPPLY_KEY":
                case 180:
                    message.status = 180;
                    break;
                case "TOKEN_HAS_NO_WIPE_KEY":
                case 181:
                    message.status = 181;
                    break;
                case "INVALID_TOKEN_MINT_AMOUNT":
                case 182:
                    message.status = 182;
                    break;
                case "INVALID_TOKEN_BURN_AMOUNT":
                case 183:
                    message.status = 183;
                    break;
                case "TOKEN_NOT_ASSOCIATED_TO_ACCOUNT":
                case 184:
                    message.status = 184;
                    break;
                case "CANNOT_WIPE_TOKEN_TREASURY_ACCOUNT":
                case 185:
                    message.status = 185;
                    break;
                case "INVALID_KYC_KEY":
                case 186:
                    message.status = 186;
                    break;
                case "INVALID_WIPE_KEY":
                case 187:
                    message.status = 187;
                    break;
                case "INVALID_FREEZE_KEY":
                case 188:
                    message.status = 188;
                    break;
                case "INVALID_SUPPLY_KEY":
                case 189:
                    message.status = 189;
                    break;
                case "MISSING_TOKEN_NAME":
                case 190:
                    message.status = 190;
                    break;
                case "TOKEN_NAME_TOO_LONG":
                case 191:
                    message.status = 191;
                    break;
                case "INVALID_WIPING_AMOUNT":
                case 192:
                    message.status = 192;
                    break;
                case "TOKEN_IS_IMMUTABLE":
                case 193:
                    message.status = 193;
                    break;
                case "TOKEN_ALREADY_ASSOCIATED_TO_ACCOUNT":
                case 194:
                    message.status = 194;
                    break;
                case "TRANSACTION_REQUIRES_ZERO_TOKEN_BALANCES":
                case 195:
                    message.status = 195;
                    break;
                case "ACCOUNT_IS_TREASURY":
                case 196:
                    message.status = 196;
                    break;
                case "TOKEN_ID_REPEATED_IN_TOKEN_LIST":
                case 197:
                    message.status = 197;
                    break;
                case "TOKEN_TRANSFER_LIST_SIZE_LIMIT_EXCEEDED":
                case 198:
                    message.status = 198;
                    break;
                case "EMPTY_TOKEN_TRANSFER_BODY":
                case 199:
                    message.status = 199;
                    break;
                case "EMPTY_TOKEN_TRANSFER_ACCOUNT_AMOUNTS":
                case 200:
                    message.status = 200;
                    break;
                case "INVALID_SCHEDULE_ID":
                case 201:
                    message.status = 201;
                    break;
                case "SCHEDULE_IS_IMMUTABLE":
                case 202:
                    message.status = 202;
                    break;
                case "INVALID_SCHEDULE_PAYER_ID":
                case 203:
                    message.status = 203;
                    break;
                case "INVALID_SCHEDULE_ACCOUNT_ID":
                case 204:
                    message.status = 204;
                    break;
                case "NO_NEW_VALID_SIGNATURES":
                case 205:
                    message.status = 205;
                    break;
                case "UNRESOLVABLE_REQUIRED_SIGNERS":
                case 206:
                    message.status = 206;
                    break;
                case "SCHEDULED_TRANSACTION_NOT_IN_WHITELIST":
                case 207:
                    message.status = 207;
                    break;
                case "SOME_SIGNATURES_WERE_INVALID":
                case 208:
                    message.status = 208;
                    break;
                case "TRANSACTION_ID_FIELD_NOT_ALLOWED":
                case 209:
                    message.status = 209;
                    break;
                case "IDENTICAL_SCHEDULE_ALREADY_CREATED":
                case 210:
                    message.status = 210;
                    break;
                case "INVALID_ZERO_BYTE_IN_STRING":
                case 211:
                    message.status = 211;
                    break;
                case "SCHEDULE_ALREADY_DELETED":
                case 212:
                    message.status = 212;
                    break;
                case "SCHEDULE_ALREADY_EXECUTED":
                case 213:
                    message.status = 213;
                    break;
                case "MESSAGE_SIZE_TOO_LARGE":
                case 214:
                    message.status = 214;
                    break;
                case "OPERATION_REPEATED_IN_BUCKET_GROUPS":
                case 215:
                    message.status = 215;
                    break;
                case "BUCKET_CAPACITY_OVERFLOW":
                case 216:
                    message.status = 216;
                    break;
                case "NODE_CAPACITY_NOT_SUFFICIENT_FOR_OPERATION":
                case 217:
                    message.status = 217;
                    break;
                case "BUCKET_HAS_NO_THROTTLE_GROUPS":
                case 218:
                    message.status = 218;
                    break;
                case "THROTTLE_GROUP_HAS_ZERO_OPS_PER_SEC":
                case 219:
                    message.status = 219;
                    break;
                case "SUCCESS_BUT_MISSING_EXPECTED_OPERATION":
                case 220:
                    message.status = 220;
                    break;
                case "UNPARSEABLE_THROTTLE_DEFINITIONS":
                case 221:
                    message.status = 221;
                    break;
                case "INVALID_THROTTLE_DEFINITIONS":
                case 222:
                    message.status = 222;
                    break;
                case "ACCOUNT_EXPIRED_AND_PENDING_REMOVAL":
                case 223:
                    message.status = 223;
                    break;
                case "INVALID_TOKEN_MAX_SUPPLY":
                case 224:
                    message.status = 224;
                    break;
                case "INVALID_TOKEN_NFT_SERIAL_NUMBER":
                case 225:
                    message.status = 225;
                    break;
                case "INVALID_NFT_ID":
                case 226:
                    message.status = 226;
                    break;
                case "METADATA_TOO_LONG":
                case 227:
                    message.status = 227;
                    break;
                case "BATCH_SIZE_LIMIT_EXCEEDED":
                case 228:
                    message.status = 228;
                    break;
                case "INVALID_QUERY_RANGE":
                case 229:
                    message.status = 229;
                    break;
                case "FRACTION_DIVIDES_BY_ZERO":
                case 230:
                    message.status = 230;
                    break;
                case "INSUFFICIENT_PAYER_BALANCE_FOR_CUSTOM_FEE":
                case 231:
                    message.status = 231;
                    break;
                case "CUSTOM_FEES_LIST_TOO_LONG":
                case 232:
                    message.status = 232;
                    break;
                case "INVALID_CUSTOM_FEE_COLLECTOR":
                case 233:
                    message.status = 233;
                    break;
                case "INVALID_TOKEN_ID_IN_CUSTOM_FEES":
                case 234:
                    message.status = 234;
                    break;
                case "TOKEN_NOT_ASSOCIATED_TO_FEE_COLLECTOR":
                case 235:
                    message.status = 235;
                    break;
                case "TOKEN_MAX_SUPPLY_REACHED":
                case 236:
                    message.status = 236;
                    break;
                case "SENDER_DOES_NOT_OWN_NFT_SERIAL_NO":
                case 237:
                    message.status = 237;
                    break;
                case "CUSTOM_FEE_NOT_FULLY_SPECIFIED":
                case 238:
                    message.status = 238;
                    break;
                case "CUSTOM_FEE_MUST_BE_POSITIVE":
                case 239:
                    message.status = 239;
                    break;
                case "TOKEN_HAS_NO_FEE_SCHEDULE_KEY":
                case 240:
                    message.status = 240;
                    break;
                case "CUSTOM_FEE_OUTSIDE_NUMERIC_RANGE":
                case 241:
                    message.status = 241;
                    break;
                case "ROYALTY_FRACTION_CANNOT_EXCEED_ONE":
                case 242:
                    message.status = 242;
                    break;
                case "FRACTIONAL_FEE_MAX_AMOUNT_LESS_THAN_MIN_AMOUNT":
                case 243:
                    message.status = 243;
                    break;
                case "CUSTOM_SCHEDULE_ALREADY_HAS_NO_FEES":
                case 244:
                    message.status = 244;
                    break;
                case "CUSTOM_FEE_DENOMINATION_MUST_BE_FUNGIBLE_COMMON":
                case 245:
                    message.status = 245;
                    break;
                case "CUSTOM_FRACTIONAL_FEE_ONLY_ALLOWED_FOR_FUNGIBLE_COMMON":
                case 246:
                    message.status = 246;
                    break;
                case "INVALID_CUSTOM_FEE_SCHEDULE_KEY":
                case 247:
                    message.status = 247;
                    break;
                case "INVALID_TOKEN_MINT_METADATA":
                case 248:
                    message.status = 248;
                    break;
                case "INVALID_TOKEN_BURN_METADATA":
                case 249:
                    message.status = 249;
                    break;
                case "CURRENT_TREASURY_STILL_OWNS_NFTS":
                case 250:
                    message.status = 250;
                    break;
                case "ACCOUNT_STILL_OWNS_NFTS":
                case 251:
                    message.status = 251;
                    break;
                case "TREASURY_MUST_OWN_BURNED_NFT":
                case 252:
                    message.status = 252;
                    break;
                case "ACCOUNT_DOES_NOT_OWN_WIPED_NFT":
                case 253:
                    message.status = 253;
                    break;
                case "ACCOUNT_AMOUNT_TRANSFERS_ONLY_ALLOWED_FOR_FUNGIBLE_COMMON":
                case 254:
                    message.status = 254;
                    break;
                case "MAX_NFTS_IN_PRICE_REGIME_HAVE_BEEN_MINTED":
                case 255:
                    message.status = 255;
                    break;
                case "PAYER_ACCOUNT_DELETED":
                case 256:
                    message.status = 256;
                    break;
                case "CUSTOM_FEE_CHARGING_EXCEEDED_MAX_RECURSION_DEPTH":
                case 257:
                    message.status = 257;
                    break;
                case "CUSTOM_FEE_CHARGING_EXCEEDED_MAX_ACCOUNT_AMOUNTS":
                case 258:
                    message.status = 258;
                    break;
                case "INSUFFICIENT_SENDER_ACCOUNT_BALANCE_FOR_CUSTOM_FEE":
                case 259:
                    message.status = 259;
                    break;
                case "SERIAL_NUMBER_LIMIT_REACHED":
                case 260:
                    message.status = 260;
                    break;
                case "CUSTOM_ROYALTY_FEE_ONLY_ALLOWED_FOR_NON_FUNGIBLE_UNIQUE":
                case 261:
                    message.status = 261;
                    break;
                case "NO_REMAINING_AUTOMATIC_ASSOCIATIONS":
                case 262:
                    message.status = 262;
                    break;
                case "EXISTING_AUTOMATIC_ASSOCIATIONS_EXCEED_GIVEN_LIMIT":
                case 263:
                    message.status = 263;
                    break;
                case "REQUESTED_NUM_AUTOMATIC_ASSOCIATIONS_EXCEEDS_ASSOCIATION_LIMIT":
                case 264:
                    message.status = 264;
                    break;
                case "TOKEN_IS_PAUSED":
                case 265:
                    message.status = 265;
                    break;
                case "TOKEN_HAS_NO_PAUSE_KEY":
                case 266:
                    message.status = 266;
                    break;
                case "INVALID_PAUSE_KEY":
                case 267:
                    message.status = 267;
                    break;
                case "FREEZE_UPDATE_FILE_DOES_NOT_EXIST":
                case 268:
                    message.status = 268;
                    break;
                case "FREEZE_UPDATE_FILE_HASH_DOES_NOT_MATCH":
                case 269:
                    message.status = 269;
                    break;
                case "NO_UPGRADE_HAS_BEEN_PREPARED":
                case 270:
                    message.status = 270;
                    break;
                case "NO_FREEZE_IS_SCHEDULED":
                case 271:
                    message.status = 271;
                    break;
                case "UPDATE_FILE_HASH_CHANGED_SINCE_PREPARE_UPGRADE":
                case 272:
                    message.status = 272;
                    break;
                case "FREEZE_START_TIME_MUST_BE_FUTURE":
                case 273:
                    message.status = 273;
                    break;
                case "PREPARED_UPDATE_FILE_IS_IMMUTABLE":
                case 274:
                    message.status = 274;
                    break;
                case "FREEZE_ALREADY_SCHEDULED":
                case 275:
                    message.status = 275;
                    break;
                case "FREEZE_UPGRADE_IN_PROGRESS":
                case 276:
                    message.status = 276;
                    break;
                case "UPDATE_FILE_ID_DOES_NOT_MATCH_PREPARED":
                case 277:
                    message.status = 277;
                    break;
                case "UPDATE_FILE_HASH_DOES_NOT_MATCH_PREPARED":
                case 278:
                    message.status = 278;
                    break;
                case "CONSENSUS_GAS_EXHAUSTED":
                case 279:
                    message.status = 279;
                    break;
                case "REVERTED_SUCCESS":
                case 280:
                    message.status = 280;
                    break;
                case "MAX_STORAGE_IN_PRICE_REGIME_HAS_BEEN_USED":
                case 281:
                    message.status = 281;
                    break;
                case "INVALID_ALIAS_KEY":
                case 282:
                    message.status = 282;
                    break;
                case "UNEXPECTED_TOKEN_DECIMALS":
                case 283:
                    message.status = 283;
                    break;
                case "INVALID_PROXY_ACCOUNT_ID":
                case 284:
                    message.status = 284;
                    break;
                case "INVALID_TRANSFER_ACCOUNT_ID":
                case 285:
                    message.status = 285;
                    break;
                case "INVALID_FEE_COLLECTOR_ACCOUNT_ID":
                case 286:
                    message.status = 286;
                    break;
                case "ALIAS_IS_IMMUTABLE":
                case 287:
                    message.status = 287;
                    break;
                case "SPENDER_ACCOUNT_SAME_AS_OWNER":
                case 288:
                    message.status = 288;
                    break;
                case "AMOUNT_EXCEEDS_TOKEN_MAX_SUPPLY":
                case 289:
                    message.status = 289;
                    break;
                case "NEGATIVE_ALLOWANCE_AMOUNT":
                case 290:
                    message.status = 290;
                    break;
                case "CANNOT_APPROVE_FOR_ALL_FUNGIBLE_COMMON":
                case 291:
                    message.status = 291;
                    break;
                case "SPENDER_DOES_NOT_HAVE_ALLOWANCE":
                case 292:
                    message.status = 292;
                    break;
                case "AMOUNT_EXCEEDS_ALLOWANCE":
                case 293:
                    message.status = 293;
                    break;
                case "MAX_ALLOWANCES_EXCEEDED":
                case 294:
                    message.status = 294;
                    break;
                case "EMPTY_ALLOWANCES":
                case 295:
                    message.status = 295;
                    break;
                case "SPENDER_ACCOUNT_REPEATED_IN_ALLOWANCES":
                case 296:
                    message.status = 296;
                    break;
                case "REPEATED_SERIAL_NUMS_IN_NFT_ALLOWANCES":
                case 297:
                    message.status = 297;
                    break;
                case "FUNGIBLE_TOKEN_IN_NFT_ALLOWANCES":
                case 298:
                    message.status = 298;
                    break;
                case "NFT_IN_FUNGIBLE_TOKEN_ALLOWANCES":
                case 299:
                    message.status = 299;
                    break;
                case "INVALID_ALLOWANCE_OWNER_ID":
                case 300:
                    message.status = 300;
                    break;
                case "INVALID_ALLOWANCE_SPENDER_ID":
                case 301:
                    message.status = 301;
                    break;
                case "REPEATED_ALLOWANCES_TO_DELETE":
                case 302:
                    message.status = 302;
                    break;
                case "INVALID_DELEGATING_SPENDER":
                case 303:
                    message.status = 303;
                    break;
                case "DELEGATING_SPENDER_CANNOT_GRANT_APPROVE_FOR_ALL":
                case 304:
                    message.status = 304;
                    break;
                case "DELEGATING_SPENDER_DOES_NOT_HAVE_APPROVE_FOR_ALL":
                case 305:
                    message.status = 305;
                    break;
                case "SCHEDULE_EXPIRATION_TIME_TOO_FAR_IN_FUTURE":
                case 306:
                    message.status = 306;
                    break;
                case "SCHEDULE_EXPIRATION_TIME_MUST_BE_HIGHER_THAN_CONSENSUS_TIME":
                case 307:
                    message.status = 307;
                    break;
                case "SCHEDULE_FUTURE_THROTTLE_EXCEEDED":
                case 308:
                    message.status = 308;
                    break;
                case "SCHEDULE_FUTURE_GAS_LIMIT_EXCEEDED":
                case 309:
                    message.status = 309;
                    break;
                case "INVALID_ETHEREUM_TRANSACTION":
                case 310:
                    message.status = 310;
                    break;
                case "WRONG_CHAIN_ID":
                case 311:
                    message.status = 311;
                    break;
                case "WRONG_NONCE":
                case 312:
                    message.status = 312;
                    break;
                case "ACCESS_LIST_UNSUPPORTED":
                case 313:
                    message.status = 313;
                    break;
                case "SCHEDULE_PENDING_EXPIRATION":
                case 314:
                    message.status = 314;
                    break;
                case "CONTRACT_IS_TOKEN_TREASURY":
                case 315:
                    message.status = 315;
                    break;
                case "CONTRACT_HAS_NON_ZERO_TOKEN_BALANCES":
                case 316:
                    message.status = 316;
                    break;
                case "CONTRACT_EXPIRED_AND_PENDING_REMOVAL":
                case 317:
                    message.status = 317;
                    break;
                case "CONTRACT_HAS_NO_AUTO_RENEW_ACCOUNT":
                case 318:
                    message.status = 318;
                    break;
                case "PERMANENT_REMOVAL_REQUIRES_SYSTEM_INITIATION":
                case 319:
                    message.status = 319;
                    break;
                case "PROXY_ACCOUNT_ID_FIELD_IS_DEPRECATED":
                case 320:
                    message.status = 320;
                    break;
                case "SELF_STAKING_IS_NOT_ALLOWED":
                case 321:
                    message.status = 321;
                    break;
                case "INVALID_STAKING_ID":
                case 322:
                    message.status = 322;
                    break;
                case "STAKING_NOT_ENABLED":
                case 323:
                    message.status = 323;
                    break;
                case "INVALID_PRNG_RANGE":
                case 324:
                    message.status = 324;
                    break;
                case "MAX_ENTITIES_IN_PRICE_REGIME_HAVE_BEEN_CREATED":
                case 325:
                    message.status = 325;
                    break;
                case "INVALID_FULL_PREFIX_SIGNATURE_FOR_PRECOMPILE":
                case 326:
                    message.status = 326;
                    break;
                case "INSUFFICIENT_BALANCES_FOR_STORAGE_RENT":
                case 327:
                    message.status = 327;
                    break;
                case "MAX_CHILD_RECORDS_EXCEEDED":
                case 328:
                    message.status = 328;
                    break;
                case "INSUFFICIENT_BALANCES_FOR_RENEWAL_FEES":
                case 329:
                    message.status = 329;
                    break;
                case "TRANSACTION_HAS_UNKNOWN_FIELDS":
                case 330:
                    message.status = 330;
                    break;
                case "ACCOUNT_IS_IMMUTABLE":
                case 331:
                    message.status = 331;
                    break;
                case "ALIAS_ALREADY_ASSIGNED":
                case 332:
                    message.status = 332;
                    break;
                case "INVALID_METADATA_KEY":
                case 333:
                    message.status = 333;
                    break;
                case "TOKEN_HAS_NO_METADATA_KEY":
                case 334:
                    message.status = 334;
                    break;
                case "MISSING_TOKEN_METADATA":
                case 335:
                    message.status = 335;
                    break;
                case "MISSING_SERIAL_NUMBERS":
                case 336:
                    message.status = 336;
                    break;
                case "TOKEN_HAS_NO_ADMIN_KEY":
                case 337:
                    message.status = 337;
                    break;
                case "NODE_DELETED":
                case 338:
                    message.status = 338;
                    break;
                case "INVALID_NODE_ID":
                case 339:
                    message.status = 339;
                    break;
                case "INVALID_GOSSIP_ENDPOINT":
                case 340:
                    message.status = 340;
                    break;
                case "INVALID_NODE_ACCOUNT_ID":
                case 341:
                    message.status = 341;
                    break;
                case "INVALID_NODE_DESCRIPTION":
                case 342:
                    message.status = 342;
                    break;
                case "INVALID_SERVICE_ENDPOINT":
                case 343:
                    message.status = 343;
                    break;
                case "INVALID_GOSSIP_CA_CERTIFICATE":
                case 344:
                    message.status = 344;
                    break;
                case "INVALID_GRPC_CERTIFICATE":
                case 345:
                    message.status = 345;
                    break;
                case "INVALID_MAX_AUTO_ASSOCIATIONS":
                case 346:
                    message.status = 346;
                    break;
                case "MAX_NODES_CREATED":
                case 347:
                    message.status = 347;
                    break;
                case "IP_FQDN_CANNOT_BE_SET_FOR_SAME_ENDPOINT":
                case 348:
                    message.status = 348;
                    break;
                case "GOSSIP_ENDPOINT_CANNOT_HAVE_FQDN":
                case 349:
                    message.status = 349;
                    break;
                case "FQDN_SIZE_TOO_LARGE":
                case 350:
                    message.status = 350;
                    break;
                case "INVALID_ENDPOINT":
                case 351:
                    message.status = 351;
                    break;
                case "GOSSIP_ENDPOINTS_EXCEEDED_LIMIT":
                case 352:
                    message.status = 352;
                    break;
                case "TOKEN_REFERENCE_REPEATED":
                case 353:
                    message.status = 353;
                    break;
                case "INVALID_OWNER_ID":
                case 354:
                    message.status = 354;
                    break;
                case "TOKEN_REFERENCE_LIST_SIZE_LIMIT_EXCEEDED":
                case 355:
                    message.status = 355;
                    break;
                case "SERVICE_ENDPOINTS_EXCEEDED_LIMIT":
                case 356:
                    message.status = 356;
                    break;
                case "INVALID_IPV4_ADDRESS":
                case 357:
                    message.status = 357;
                    break;
                case "EMPTY_TOKEN_REFERENCE_LIST":
                case 358:
                    message.status = 358;
                    break;
                case "UPDATE_NODE_ACCOUNT_NOT_ALLOWED":
                case 359:
                    message.status = 359;
                    break;
                case "TOKEN_HAS_NO_METADATA_OR_SUPPLY_KEY":
                case 360:
                    message.status = 360;
                    break;
                case "EMPTY_PENDING_AIRDROP_ID_LIST":
                case 361:
                    message.status = 361;
                    break;
                case "PENDING_AIRDROP_ID_REPEATED":
                case 362:
                    message.status = 362;
                    break;
                case "PENDING_AIRDROP_ID_LIST_TOO_LONG":
                case 363:
                    message.status = 363;
                    break;
                case "PENDING_NFT_AIRDROP_ALREADY_EXISTS":
                case 364:
                    message.status = 364;
                    break;
                case "ACCOUNT_HAS_PENDING_AIRDROPS":
                case 365:
                    message.status = 365;
                    break;
                case "THROTTLED_AT_CONSENSUS":
                case 366:
                    message.status = 366;
                    break;
                case "INVALID_PENDING_AIRDROP_ID":
                case 367:
                    message.status = 367;
                    break;
                case "TOKEN_AIRDROP_WITH_FALLBACK_ROYALTY":
                case 368:
                    message.status = 368;
                    break;
                case "INVALID_TOKEN_IN_PENDING_AIRDROP":
                case 369:
                    message.status = 369;
                    break;
                case "SCHEDULE_EXPIRY_IS_BUSY":
                case 370:
                    message.status = 370;
                    break;
                case "INVALID_GRPC_CERTIFICATE_HASH":
                case 371:
                    message.status = 371;
                    break;
                case "MISSING_EXPIRY_TIME":
                case 372:
                    message.status = 372;
                    break;
                case "NO_SCHEDULING_ALLOWED_AFTER_SCHEDULED_RECURSION":
                case 373:
                    message.status = 373;
                    break;
                case "RECURSIVE_SCHEDULING_LIMIT_REACHED":
                case 374:
                    message.status = 374;
                    break;
                case "WAITING_FOR_LEDGER_ID":
                case 375:
                    message.status = 375;
                    break;
                case "MAX_ENTRIES_FOR_FEE_EXEMPT_KEY_LIST_EXCEEDED":
                case 376:
                    message.status = 376;
                    break;
                case "FEE_EXEMPT_KEY_LIST_CONTAINS_DUPLICATED_KEYS":
                case 377:
                    message.status = 377;
                    break;
                case "INVALID_KEY_IN_FEE_EXEMPT_KEY_LIST":
                case 378:
                    message.status = 378;
                    break;
                case "INVALID_FEE_SCHEDULE_KEY":
                case 379:
                    message.status = 379;
                    break;
                case "FEE_SCHEDULE_KEY_CANNOT_BE_UPDATED":
                case 380:
                    message.status = 380;
                    break;
                case "FEE_SCHEDULE_KEY_NOT_SET":
                case 381:
                    message.status = 381;
                    break;
                case "MAX_CUSTOM_FEE_LIMIT_EXCEEDED":
                case 382:
                    message.status = 382;
                    break;
                case "NO_VALID_MAX_CUSTOM_FEE":
                case 383:
                    message.status = 383;
                    break;
                case "INVALID_MAX_CUSTOM_FEES":
                case 384:
                    message.status = 384;
                    break;
                case "DUPLICATE_DENOMINATION_IN_MAX_CUSTOM_FEE_LIST":
                case 385:
                    message.status = 385;
                    break;
                case "DUPLICATE_ACCOUNT_ID_IN_MAX_CUSTOM_FEE_LIST":
                case 386:
                    message.status = 386;
                    break;
                case "MAX_CUSTOM_FEES_IS_NOT_SUPPORTED":
                case 387:
                    message.status = 387;
                    break;
                case "BATCH_LIST_EMPTY":
                case 388:
                    message.status = 388;
                    break;
                case "BATCH_LIST_CONTAINS_DUPLICATES":
                case 389:
                    message.status = 389;
                    break;
                case "BATCH_TRANSACTION_IN_BLACKLIST":
                case 390:
                    message.status = 390;
                    break;
                case "INNER_TRANSACTION_FAILED":
                case 391:
                    message.status = 391;
                    break;
                case "MISSING_BATCH_KEY":
                case 392:
                    message.status = 392;
                    break;
                case "BATCH_KEY_SET_ON_NON_INNER_TRANSACTION":
                case 393:
                    message.status = 393;
                    break;
                case "INVALID_BATCH_KEY":
                case 394:
                    message.status = 394;
                    break;
                case "SCHEDULE_EXPIRY_NOT_CONFIGURABLE":
                case 395:
                    message.status = 395;
                    break;
                case "CREATING_SYSTEM_ENTITIES":
                case 396:
                    message.status = 396;
                    break;
                case "THROTTLE_GROUP_LCM_OVERFLOW":
                case 397:
                    message.status = 397;
                    break;
                case "AIRDROP_CONTAINS_MULTIPLE_SENDERS_FOR_A_TOKEN":
                case 398:
                    message.status = 398;
                    break;
                case "GRPC_WEB_PROXY_NOT_SUPPORTED":
                case 399:
                    message.status = 399;
                    break;
                case "NFT_TRANSFERS_ONLY_ALLOWED_FOR_NON_FUNGIBLE_UNIQUE":
                case 400:
                    message.status = 400;
                    break;
                case "INVALID_SERIALIZED_TX_MESSAGE_HASH_ALGORITHM":
                case 401:
                    message.status = 401;
                    break;
                case "WRONG_HOOK_ENTITY_TYPE":
                case 499:
                    message.status = 499;
                    break;
                case "EVM_HOOK_GAS_THROTTLED":
                case 500:
                    message.status = 500;
                    break;
                case "HOOK_ID_IN_USE":
                case 501:
                    message.status = 501;
                    break;
                case "BAD_HOOK_REQUEST":
                case 502:
                    message.status = 502;
                    break;
                case "REJECTED_BY_ACCOUNT_ALLOWANCE_HOOK":
                case 503:
                    message.status = 503;
                    break;
                case "HOOK_NOT_FOUND":
                case 504:
                    message.status = 504;
                    break;
                case "EVM_HOOK_STORAGE_UPDATE_BYTES_TOO_LONG":
                case 505:
                    message.status = 505;
                    break;
                case "EVM_HOOK_STORAGE_UPDATE_BYTES_MUST_USE_MINIMAL_REPRESENTATION":
                case 506:
                    message.status = 506;
                    break;
                case "INVALID_HOOK_ID":
                case 507:
                    message.status = 507;
                    break;
                case "EMPTY_EVM_HOOK_STORAGE_UPDATE":
                case 508:
                    message.status = 508;
                    break;
                case "HOOK_ID_REPEATED_IN_CREATION_DETAILS":
                case 509:
                    message.status = 509;
                    break;
                case "HOOKS_NOT_ENABLED":
                case 510:
                    message.status = 510;
                    break;
                case "HOOK_IS_NOT_AN_EVM_HOOK":
                case 511:
                    message.status = 511;
                    break;
                case "HOOK_DELETED":
                case 512:
                    message.status = 512;
                    break;
                case "TOO_MANY_EVM_HOOK_STORAGE_UPDATES":
                case 513:
                    message.status = 513;
                    break;
                case "HOOK_CREATION_BYTES_MUST_USE_MINIMAL_REPRESENTATION":
                case 514:
                    message.status = 514;
                    break;
                case "HOOK_CREATION_BYTES_TOO_LONG":
                case 515:
                    message.status = 515;
                    break;
                case "INVALID_HOOK_CREATION_SPEC":
                case 516:
                    message.status = 516;
                    break;
                case "HOOK_EXTENSION_EMPTY":
                case 517:
                    message.status = 517;
                    break;
                case "INVALID_HOOK_ADMIN_KEY":
                case 518:
                    message.status = 518;
                    break;
                case "HOOK_DELETION_REQUIRES_ZERO_STORAGE_SLOTS":
                case 519:
                    message.status = 519;
                    break;
                case "CANNOT_SET_HOOKS_AND_APPROVAL":
                case 520:
                    message.status = 520;
                    break;
                case "TRANSACTION_REQUIRES_ZERO_HOOKS":
                case 521:
                    message.status = 521;
                    break;
                case "INVALID_HOOK_CALL":
                case 522:
                    message.status = 522;
                    break;
                case "HOOKS_ARE_NOT_SUPPORTED_IN_AIRDROPS":
                case 523:
                    message.status = 523;
                    break;
                case "ACCOUNT_IS_LINKED_TO_A_NODE":
                case 524:
                    message.status = 524;
                    break;
                case "HOOKS_EXECUTIONS_REQUIRE_TOP_LEVEL_CRYPTO_TRANSFER":
                case 525:
                    message.status = 525;
                    break;
                case "NODE_ACCOUNT_HAS_ZERO_BALANCE":
                case 526:
                    message.status = 526;
                    break;
                case "TRANSFER_TO_FEE_COLLECTION_ACCOUNT_NOT_ALLOWED":
                case 527:
                    message.status = 527;
                    break;
                case "TOO_MANY_HOOK_INVOCATIONS":
                case 528:
                    message.status = 528;
                    break;
                case "INVALID_REGISTERED_NODE_ID":
                case 529:
                    message.status = 529;
                    break;
                case "INVALID_REGISTERED_ENDPOINT":
                case 530:
                    message.status = 530;
                    break;
                case "REGISTERED_ENDPOINTS_EXCEEDED_LIMIT":
                case 531:
                    message.status = 531;
                    break;
                case "INVALID_REGISTERED_ENDPOINT_ADDRESS":
                case 532:
                    message.status = 532;
                    break;
                case "INVALID_REGISTERED_ENDPOINT_TYPE":
                case 533:
                    message.status = 533;
                    break;
                case "REGISTERED_NODE_STILL_ASSOCIATED":
                case 534:
                    message.status = 534;
                    break;
                case "MAX_REGISTERED_NODES_EXCEEDED":
                case 535:
                    message.status = 535;
                    break;
                default:
                    if (typeof object.status === "number" && (object.status | 0) === object.status)
                        message.status = object.status;
                }
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.TransactionReceipt.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.fileID != null) {
                if (!$util.isObject(object.fileID))
                    throw $TypeError(".proto.TransactionReceipt.fileID: object expected");
                message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
            }
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.TransactionReceipt.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            if (object.exchangeRate != null) {
                if (!$util.isObject(object.exchangeRate))
                    throw $TypeError(".proto.TransactionReceipt.exchangeRate: object expected");
                message.exchangeRate = $root.proto.ExchangeRateSet.fromObject(object.exchangeRate, _depth + 1);
            }
            if (object.topicID != null) {
                if (!$util.isObject(object.topicID))
                    throw $TypeError(".proto.TransactionReceipt.topicID: object expected");
                message.topicID = $root.proto.TopicID.fromObject(object.topicID, _depth + 1);
            }
            if (object.topicSequenceNumber != null)
                if (typeof object.topicSequenceNumber === "object" ? object.topicSequenceNumber.low || object.topicSequenceNumber.high : $Number(object.topicSequenceNumber) !== 0)
                    if ($util.Long)
                        message.topicSequenceNumber = $util.Long.fromValue(object.topicSequenceNumber, true);
                    else if (typeof object.topicSequenceNumber === "string")
                        message.topicSequenceNumber = $parseInt(object.topicSequenceNumber, 10);
                    else if (typeof object.topicSequenceNumber === "number")
                        message.topicSequenceNumber = object.topicSequenceNumber;
                    else if (typeof object.topicSequenceNumber === "object")
                        message.topicSequenceNumber = new $util.LongBits(object.topicSequenceNumber.low >>> 0, object.topicSequenceNumber.high >>> 0).toNumber(true);
            if (object.topicRunningHash != null)
                if (object.topicRunningHash.length)
                    if (typeof object.topicRunningHash === "string")
                        $util.base64.decode(object.topicRunningHash, message.topicRunningHash = $util.newBuffer($util.base64.length(object.topicRunningHash)), 0);
                    else if (object.topicRunningHash.length >= 0)
                        message.topicRunningHash = object.topicRunningHash;
            if (object.topicRunningHashVersion != null)
                if (typeof object.topicRunningHashVersion === "object" ? object.topicRunningHashVersion.low || object.topicRunningHashVersion.high : $Number(object.topicRunningHashVersion) !== 0)
                    if ($util.Long)
                        message.topicRunningHashVersion = $util.Long.fromValue(object.topicRunningHashVersion, true);
                    else if (typeof object.topicRunningHashVersion === "string")
                        message.topicRunningHashVersion = $parseInt(object.topicRunningHashVersion, 10);
                    else if (typeof object.topicRunningHashVersion === "number")
                        message.topicRunningHashVersion = object.topicRunningHashVersion;
                    else if (typeof object.topicRunningHashVersion === "object")
                        message.topicRunningHashVersion = new $util.LongBits(object.topicRunningHashVersion.low >>> 0, object.topicRunningHashVersion.high >>> 0).toNumber(true);
            if (object.tokenID != null) {
                if (!$util.isObject(object.tokenID))
                    throw $TypeError(".proto.TransactionReceipt.tokenID: object expected");
                message.tokenID = $root.proto.TokenID.fromObject(object.tokenID, _depth + 1);
            }
            if (object.newTotalSupply != null)
                if (typeof object.newTotalSupply === "object" ? object.newTotalSupply.low || object.newTotalSupply.high : $Number(object.newTotalSupply) !== 0)
                    if ($util.Long)
                        message.newTotalSupply = $util.Long.fromValue(object.newTotalSupply, true);
                    else if (typeof object.newTotalSupply === "string")
                        message.newTotalSupply = $parseInt(object.newTotalSupply, 10);
                    else if (typeof object.newTotalSupply === "number")
                        message.newTotalSupply = object.newTotalSupply;
                    else if (typeof object.newTotalSupply === "object")
                        message.newTotalSupply = new $util.LongBits(object.newTotalSupply.low >>> 0, object.newTotalSupply.high >>> 0).toNumber(true);
            if (object.scheduleID != null) {
                if (!$util.isObject(object.scheduleID))
                    throw $TypeError(".proto.TransactionReceipt.scheduleID: object expected");
                message.scheduleID = $root.proto.ScheduleID.fromObject(object.scheduleID, _depth + 1);
            }
            if (object.scheduledTransactionID != null) {
                if (!$util.isObject(object.scheduledTransactionID))
                    throw $TypeError(".proto.TransactionReceipt.scheduledTransactionID: object expected");
                message.scheduledTransactionID = $root.proto.TransactionID.fromObject(object.scheduledTransactionID, _depth + 1);
            }
            if (object.serialNumbers) {
                if (!$Array.isArray(object.serialNumbers))
                    throw $TypeError(".proto.TransactionReceipt.serialNumbers: array expected");
                message.serialNumbers = $Array(object.serialNumbers.length);
                for (let i = 0; i < object.serialNumbers.length; ++i)
                    if ($util.Long)
                        message.serialNumbers[i] = $util.Long.fromValue(object.serialNumbers[i], false);
                    else if (typeof object.serialNumbers[i] === "string")
                        message.serialNumbers[i] = $parseInt(object.serialNumbers[i], 10);
                    else if (typeof object.serialNumbers[i] === "number")
                        message.serialNumbers[i] = object.serialNumbers[i];
                    else if (typeof object.serialNumbers[i] === "object")
                        message.serialNumbers[i] = new $util.LongBits(object.serialNumbers[i].low >>> 0, object.serialNumbers[i].high >>> 0).toNumber();
            }
            if (object.nodeId != null)
                if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                    if ($util.Long)
                        message.nodeId = $util.Long.fromValue(object.nodeId, true);
                    else if (typeof object.nodeId === "string")
                        message.nodeId = $parseInt(object.nodeId, 10);
                    else if (typeof object.nodeId === "number")
                        message.nodeId = object.nodeId;
                    else if (typeof object.nodeId === "object")
                        message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
            if (object.registeredNodeId != null)
                if (typeof object.registeredNodeId === "object" ? object.registeredNodeId.low || object.registeredNodeId.high : $Number(object.registeredNodeId) !== 0)
                    if ($util.Long)
                        message.registeredNodeId = $util.Long.fromValue(object.registeredNodeId, true);
                    else if (typeof object.registeredNodeId === "string")
                        message.registeredNodeId = $parseInt(object.registeredNodeId, 10);
                    else if (typeof object.registeredNodeId === "number")
                        message.registeredNodeId = object.registeredNodeId;
                    else if (typeof object.registeredNodeId === "object")
                        message.registeredNodeId = new $util.LongBits(object.registeredNodeId.low >>> 0, object.registeredNodeId.high >>> 0).toNumber(true);
            if (object.blockNumber != null) {
                if (!$util.isObject(object.blockNumber))
                    throw $TypeError(".proto.TransactionReceipt.blockNumber: object expected");
                message.blockNumber = $root.google.protobuf.UInt64Value.fromObject(object.blockNumber, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TransactionReceipt message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionReceipt
         * @static
         * @param {proto.TransactionReceipt} message TransactionReceipt
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionReceipt.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.serialNumbers = [];
            if (options.defaults) {
                object.status = options.enums === $String ? "OK" : 0;
                object.accountID = null;
                object.fileID = null;
                object.contractID = null;
                object.exchangeRate = null;
                object.topicID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.topicSequenceNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.topicSequenceNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.topicRunningHash = "";
                else {
                    object.topicRunningHash = [];
                    if (options.bytes !== $Array)
                        object.topicRunningHash = $util.newBuffer(object.topicRunningHash);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.topicRunningHashVersion = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.topicRunningHashVersion = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.tokenID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.newTotalSupply = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.newTotalSupply = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.scheduleID = null;
                object.scheduledTransactionID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.registeredNodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.registeredNodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.blockNumber = null;
            }
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === $String ? $root.proto.ResponseCodeEnum[message.status] === $undefined ? message.status : $root.proto.ResponseCodeEnum[message.status] : message.status;
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            if (message.exchangeRate != null && $Object.hasOwnProperty.call(message, "exchangeRate"))
                object.exchangeRate = $root.proto.ExchangeRateSet.toObject(message.exchangeRate, options, _depth + 1);
            if (message.topicID != null && $Object.hasOwnProperty.call(message, "topicID"))
                object.topicID = $root.proto.TopicID.toObject(message.topicID, options, _depth + 1);
            if (message.topicSequenceNumber != null && $Object.hasOwnProperty.call(message, "topicSequenceNumber"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.topicSequenceNumber = typeof message.topicSequenceNumber === "number" ? $BigInt(message.topicSequenceNumber) : $util.Long.fromBits(message.topicSequenceNumber.low >>> 0, message.topicSequenceNumber.high >>> 0, true).toBigInt();
                else if (typeof message.topicSequenceNumber === "number")
                    object.topicSequenceNumber = options.longs === $String ? $String(message.topicSequenceNumber) : message.topicSequenceNumber;
                else
                    object.topicSequenceNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.topicSequenceNumber) : options.longs === $Number ? new $util.LongBits(message.topicSequenceNumber.low >>> 0, message.topicSequenceNumber.high >>> 0).toNumber(true) : message.topicSequenceNumber;
            if (message.topicRunningHash != null && $Object.hasOwnProperty.call(message, "topicRunningHash"))
                object.topicRunningHash = options.bytes === $String ? $util.base64.encode(message.topicRunningHash, 0, message.topicRunningHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.topicRunningHash) : message.topicRunningHash;
            if (message.topicRunningHashVersion != null && $Object.hasOwnProperty.call(message, "topicRunningHashVersion"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.topicRunningHashVersion = typeof message.topicRunningHashVersion === "number" ? $BigInt(message.topicRunningHashVersion) : $util.Long.fromBits(message.topicRunningHashVersion.low >>> 0, message.topicRunningHashVersion.high >>> 0, true).toBigInt();
                else if (typeof message.topicRunningHashVersion === "number")
                    object.topicRunningHashVersion = options.longs === $String ? $String(message.topicRunningHashVersion) : message.topicRunningHashVersion;
                else
                    object.topicRunningHashVersion = options.longs === $String ? $util.Long.prototype.toString.call(message.topicRunningHashVersion) : options.longs === $Number ? new $util.LongBits(message.topicRunningHashVersion.low >>> 0, message.topicRunningHashVersion.high >>> 0).toNumber(true) : message.topicRunningHashVersion;
            if (message.tokenID != null && $Object.hasOwnProperty.call(message, "tokenID"))
                object.tokenID = $root.proto.TokenID.toObject(message.tokenID, options, _depth + 1);
            if (message.newTotalSupply != null && $Object.hasOwnProperty.call(message, "newTotalSupply"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.newTotalSupply = typeof message.newTotalSupply === "number" ? $BigInt(message.newTotalSupply) : $util.Long.fromBits(message.newTotalSupply.low >>> 0, message.newTotalSupply.high >>> 0, true).toBigInt();
                else if (typeof message.newTotalSupply === "number")
                    object.newTotalSupply = options.longs === $String ? $String(message.newTotalSupply) : message.newTotalSupply;
                else
                    object.newTotalSupply = options.longs === $String ? $util.Long.prototype.toString.call(message.newTotalSupply) : options.longs === $Number ? new $util.LongBits(message.newTotalSupply.low >>> 0, message.newTotalSupply.high >>> 0).toNumber(true) : message.newTotalSupply;
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID"))
                object.scheduleID = $root.proto.ScheduleID.toObject(message.scheduleID, options, _depth + 1);
            if (message.scheduledTransactionID != null && $Object.hasOwnProperty.call(message, "scheduledTransactionID"))
                object.scheduledTransactionID = $root.proto.TransactionID.toObject(message.scheduledTransactionID, options, _depth + 1);
            if (message.serialNumbers && message.serialNumbers.length) {
                object.serialNumbers = $Array(message.serialNumbers.length);
                for (let j = 0; j < message.serialNumbers.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.serialNumbers[j] = typeof message.serialNumbers[j] === "number" ? $BigInt(message.serialNumbers[j]) : $util.Long.fromBits(message.serialNumbers[j].low >>> 0, message.serialNumbers[j].high >>> 0, false).toBigInt();
                    else if (typeof message.serialNumbers[j] === "number")
                        object.serialNumbers[j] = options.longs === $String ? $String(message.serialNumbers[j]) : message.serialNumbers[j];
                    else
                        object.serialNumbers[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.serialNumbers[j]) : options.longs === $Number ? new $util.LongBits(message.serialNumbers[j].low >>> 0, message.serialNumbers[j].high >>> 0).toNumber() : message.serialNumbers[j];
            }
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                else if (typeof message.nodeId === "number")
                    object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                else
                    object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
            if (message.registeredNodeId != null && $Object.hasOwnProperty.call(message, "registeredNodeId"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.registeredNodeId = typeof message.registeredNodeId === "number" ? $BigInt(message.registeredNodeId) : $util.Long.fromBits(message.registeredNodeId.low >>> 0, message.registeredNodeId.high >>> 0, true).toBigInt();
                else if (typeof message.registeredNodeId === "number")
                    object.registeredNodeId = options.longs === $String ? $String(message.registeredNodeId) : message.registeredNodeId;
                else
                    object.registeredNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.registeredNodeId) : options.longs === $Number ? new $util.LongBits(message.registeredNodeId.low >>> 0, message.registeredNodeId.high >>> 0).toNumber(true) : message.registeredNodeId;
            if (message.blockNumber != null && $Object.hasOwnProperty.call(message, "blockNumber"))
                object.blockNumber = $root.google.protobuf.UInt64Value.toObject(message.blockNumber, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TransactionReceipt to JSON.
         * @function toJSON
         * @memberof proto.TransactionReceipt
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionReceipt.prototype.toJSON = function() {
            return TransactionReceipt.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionReceipt
         * @function getTypeUrl
         * @memberof proto.TransactionReceipt
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionReceipt.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionReceipt";
        };

        return TransactionReceipt;
    })();

    proto.ExchangeRate = (function() {

        /**
         * Properties of an ExchangeRate.
         * @typedef {Object} proto.ExchangeRate.$Properties
         * @property {number|null} [hbarEquiv] Denominator for a ratio of USD cents per HBAR.
         * @property {number|null} [centEquiv] Numerator for a ratio of USD cents per HBAR.
         * @property {proto.TimestampSeconds.$Properties|null} [expirationTime] Expiration time stamp for this exchange rate.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an ExchangeRate.
         * @memberof proto
         * @interface IExchangeRate
         * @augments proto.ExchangeRate.$Properties
         * @deprecated Use proto.ExchangeRate.$Properties instead.
         */

        /**
         * Shape of an ExchangeRate.
         * @typedef {proto.ExchangeRate.$Properties} proto.ExchangeRate.$Shape
         */

        /**
         * Constructs a new ExchangeRate.
         * @memberof proto
         * @classdesc An exchange rate as a ratio of USD cents per HBAR.
         * 
         * This ratio SHALL be used to convert tinycent (`10<sup>-8</sup>` USD cent)
         * to tinybar for fees and other purposes.<br/>
         * When applying an `ExchangeRate`, implementations SHOULD ensure input values
         * are `tinycent` and/or `tinybar` before applying the exchange ratio.<br/>
         * Exchange results MAY be converted to USD or HBAR via division if whole
         * unit values are required.
         * 
         * The ratio described here SHALL be assigned such that a value in `tinybar`
         * may be obtained with the following equation.
         * ```
         * amountInTinybar = (amountInTinycent * hbarEquiv) / centEquiv
         * ```
         * @constructor
         * @param {proto.ExchangeRate.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ExchangeRate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Denominator for a ratio of USD cents per HBAR.
         * @member {number} hbarEquiv
         * @memberof proto.ExchangeRate
         * @instance
         */
        ExchangeRate.prototype.hbarEquiv = 0;

        /**
         * Numerator for a ratio of USD cents per HBAR.
         * @member {number} centEquiv
         * @memberof proto.ExchangeRate
         * @instance
         */
        ExchangeRate.prototype.centEquiv = 0;

        /**
         * Expiration time stamp for this exchange rate.
         * @member {proto.TimestampSeconds.$Properties|null|undefined} expirationTime
         * @memberof proto.ExchangeRate
         * @instance
         */
        ExchangeRate.prototype.expirationTime = null;

        /**
         * Creates a new ExchangeRate instance using the specified properties.
         * @function create
         * @memberof proto.ExchangeRate
         * @static
         * @param {proto.ExchangeRate.$Properties=} [properties] Properties to set
         * @returns {proto.ExchangeRate} ExchangeRate instance
         * @type {{
         *   (properties: proto.ExchangeRate.$Shape): proto.ExchangeRate & proto.ExchangeRate.$Shape;
         *   (properties?: proto.ExchangeRate.$Properties): proto.ExchangeRate;
         * }}
         */
        ExchangeRate.create = function(properties) {
            return new ExchangeRate(properties);
        };

        /**
         * Encodes the specified ExchangeRate message. Does not implicitly {@link proto.ExchangeRate.verify|verify} messages.
         * @function encode
         * @memberof proto.ExchangeRate
         * @static
         * @param {proto.ExchangeRate.$Properties} message ExchangeRate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExchangeRate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hbarEquiv != null && $Object.hasOwnProperty.call(message, "hbarEquiv") && message.hbarEquiv !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.hbarEquiv);
            if (message.centEquiv != null && $Object.hasOwnProperty.call(message, "centEquiv") && message.centEquiv !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.centEquiv);
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                $root.proto.TimestampSeconds.encode(message.expirationTime, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ExchangeRate message, length delimited. Does not implicitly {@link proto.ExchangeRate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ExchangeRate
         * @static
         * @param {proto.ExchangeRate.$Properties} message ExchangeRate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExchangeRate.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an ExchangeRate message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ExchangeRate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ExchangeRate & proto.ExchangeRate.$Shape} ExchangeRate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExchangeRate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ExchangeRate();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.hbarEquiv = value;
                        else
                            delete message.hbarEquiv;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.centEquiv = value;
                        else
                            delete message.centEquiv;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.expirationTime = $root.proto.TimestampSeconds.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an ExchangeRate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ExchangeRate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ExchangeRate & proto.ExchangeRate.$Shape} ExchangeRate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExchangeRate.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExchangeRate message.
         * @function verify
         * @memberof proto.ExchangeRate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExchangeRate.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hbarEquiv != null && $Object.hasOwnProperty.call(message, "hbarEquiv"))
                if (!$util.isInteger(message.hbarEquiv))
                    return "hbarEquiv: integer expected";
            if (message.centEquiv != null && $Object.hasOwnProperty.call(message, "centEquiv"))
                if (!$util.isInteger(message.centEquiv))
                    return "centEquiv: integer expected";
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                let error = $root.proto.TimestampSeconds.verify(message.expirationTime, _depth + 1);
                if (error)
                    return "expirationTime." + error;
            }
            return null;
        };

        /**
         * Creates an ExchangeRate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ExchangeRate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ExchangeRate} ExchangeRate
         */
        ExchangeRate.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ExchangeRate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ExchangeRate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ExchangeRate();
            if (object.hbarEquiv != null)
                if ($Number(object.hbarEquiv) !== 0)
                    message.hbarEquiv = object.hbarEquiv | 0;
            if (object.centEquiv != null)
                if ($Number(object.centEquiv) !== 0)
                    message.centEquiv = object.centEquiv | 0;
            if (object.expirationTime != null) {
                if (!$util.isObject(object.expirationTime))
                    throw $TypeError(".proto.ExchangeRate.expirationTime: object expected");
                message.expirationTime = $root.proto.TimestampSeconds.fromObject(object.expirationTime, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an ExchangeRate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ExchangeRate
         * @static
         * @param {proto.ExchangeRate} message ExchangeRate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExchangeRate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.hbarEquiv = 0;
                object.centEquiv = 0;
                object.expirationTime = null;
            }
            if (message.hbarEquiv != null && $Object.hasOwnProperty.call(message, "hbarEquiv"))
                object.hbarEquiv = message.hbarEquiv;
            if (message.centEquiv != null && $Object.hasOwnProperty.call(message, "centEquiv"))
                object.centEquiv = message.centEquiv;
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                object.expirationTime = $root.proto.TimestampSeconds.toObject(message.expirationTime, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ExchangeRate to JSON.
         * @function toJSON
         * @memberof proto.ExchangeRate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExchangeRate.prototype.toJSON = function() {
            return ExchangeRate.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ExchangeRate
         * @function getTypeUrl
         * @memberof proto.ExchangeRate
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ExchangeRate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ExchangeRate";
        };

        return ExchangeRate;
    })();

    proto.ExchangeRateSet = (function() {

        /**
         * Properties of an ExchangeRateSet.
         * @typedef {Object} proto.ExchangeRateSet.$Properties
         * @property {proto.ExchangeRate.$Properties|null} [currentRate] A current exchange rate.
         * <p>
         * When present in a receipt, this SHALL be the exchange rate used to
         * compute the fees for that transaction.
         * @property {proto.ExchangeRate.$Properties|null} [nextRate] A future exchange rate.
         * <p>
         * This exchange rate SHALL be applied after the current exchange
         * rate expires.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an ExchangeRateSet.
         * @memberof proto
         * @interface IExchangeRateSet
         * @augments proto.ExchangeRateSet.$Properties
         * @deprecated Use proto.ExchangeRateSet.$Properties instead.
         */

        /**
         * Shape of an ExchangeRateSet.
         * @typedef {proto.ExchangeRateSet.$Properties} proto.ExchangeRateSet.$Shape
         */

        /**
         * Constructs a new ExchangeRateSet.
         * @memberof proto
         * @classdesc A set of two exchange rates.<br/>
         * The exchange rate for the network is stored and reported as a set of
         * two rates; current and next. This structure supports the network cleanly
         * switching between exchange rates when necessary. This also provides clear
         * notice to clients when the exchange rate will change and the exchange
         * rate that will be applied for the next time period.
         * 
         * The difference in rate between `currentRate` and `nextRate` MUST NOT exceed
         * the configured maximum percentage change. This limit SHALL be a
         * network configuration value.
         * @constructor
         * @param {proto.ExchangeRateSet.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ExchangeRateSet = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A current exchange rate.
         * <p>
         * When present in a receipt, this SHALL be the exchange rate used to
         * compute the fees for that transaction.
         * @member {proto.ExchangeRate.$Properties|null|undefined} currentRate
         * @memberof proto.ExchangeRateSet
         * @instance
         */
        ExchangeRateSet.prototype.currentRate = null;

        /**
         * A future exchange rate.
         * <p>
         * This exchange rate SHALL be applied after the current exchange
         * rate expires.
         * @member {proto.ExchangeRate.$Properties|null|undefined} nextRate
         * @memberof proto.ExchangeRateSet
         * @instance
         */
        ExchangeRateSet.prototype.nextRate = null;

        /**
         * Creates a new ExchangeRateSet instance using the specified properties.
         * @function create
         * @memberof proto.ExchangeRateSet
         * @static
         * @param {proto.ExchangeRateSet.$Properties=} [properties] Properties to set
         * @returns {proto.ExchangeRateSet} ExchangeRateSet instance
         * @type {{
         *   (properties: proto.ExchangeRateSet.$Shape): proto.ExchangeRateSet & proto.ExchangeRateSet.$Shape;
         *   (properties?: proto.ExchangeRateSet.$Properties): proto.ExchangeRateSet;
         * }}
         */
        ExchangeRateSet.create = function(properties) {
            return new ExchangeRateSet(properties);
        };

        /**
         * Encodes the specified ExchangeRateSet message. Does not implicitly {@link proto.ExchangeRateSet.verify|verify} messages.
         * @function encode
         * @memberof proto.ExchangeRateSet
         * @static
         * @param {proto.ExchangeRateSet.$Properties} message ExchangeRateSet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExchangeRateSet.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.currentRate != null && $Object.hasOwnProperty.call(message, "currentRate"))
                $root.proto.ExchangeRate.encode(message.currentRate, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.nextRate != null && $Object.hasOwnProperty.call(message, "nextRate"))
                $root.proto.ExchangeRate.encode(message.nextRate, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ExchangeRateSet message, length delimited. Does not implicitly {@link proto.ExchangeRateSet.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ExchangeRateSet
         * @static
         * @param {proto.ExchangeRateSet.$Properties} message ExchangeRateSet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExchangeRateSet.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an ExchangeRateSet message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ExchangeRateSet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ExchangeRateSet & proto.ExchangeRateSet.$Shape} ExchangeRateSet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExchangeRateSet.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ExchangeRateSet();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.currentRate = $root.proto.ExchangeRate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.currentRate);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.nextRate = $root.proto.ExchangeRate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nextRate);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an ExchangeRateSet message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ExchangeRateSet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ExchangeRateSet & proto.ExchangeRateSet.$Shape} ExchangeRateSet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExchangeRateSet.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExchangeRateSet message.
         * @function verify
         * @memberof proto.ExchangeRateSet
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExchangeRateSet.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.currentRate != null && $Object.hasOwnProperty.call(message, "currentRate")) {
                let error = $root.proto.ExchangeRate.verify(message.currentRate, _depth + 1);
                if (error)
                    return "currentRate." + error;
            }
            if (message.nextRate != null && $Object.hasOwnProperty.call(message, "nextRate")) {
                let error = $root.proto.ExchangeRate.verify(message.nextRate, _depth + 1);
                if (error)
                    return "nextRate." + error;
            }
            return null;
        };

        /**
         * Creates an ExchangeRateSet message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ExchangeRateSet
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ExchangeRateSet} ExchangeRateSet
         */
        ExchangeRateSet.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ExchangeRateSet)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ExchangeRateSet: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ExchangeRateSet();
            if (object.currentRate != null) {
                if (!$util.isObject(object.currentRate))
                    throw $TypeError(".proto.ExchangeRateSet.currentRate: object expected");
                message.currentRate = $root.proto.ExchangeRate.fromObject(object.currentRate, _depth + 1);
            }
            if (object.nextRate != null) {
                if (!$util.isObject(object.nextRate))
                    throw $TypeError(".proto.ExchangeRateSet.nextRate: object expected");
                message.nextRate = $root.proto.ExchangeRate.fromObject(object.nextRate, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an ExchangeRateSet message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ExchangeRateSet
         * @static
         * @param {proto.ExchangeRateSet} message ExchangeRateSet
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExchangeRateSet.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.currentRate = null;
                object.nextRate = null;
            }
            if (message.currentRate != null && $Object.hasOwnProperty.call(message, "currentRate"))
                object.currentRate = $root.proto.ExchangeRate.toObject(message.currentRate, options, _depth + 1);
            if (message.nextRate != null && $Object.hasOwnProperty.call(message, "nextRate"))
                object.nextRate = $root.proto.ExchangeRate.toObject(message.nextRate, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ExchangeRateSet to JSON.
         * @function toJSON
         * @memberof proto.ExchangeRateSet
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExchangeRateSet.prototype.toJSON = function() {
            return ExchangeRateSet.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ExchangeRateSet
         * @function getTypeUrl
         * @memberof proto.ExchangeRateSet
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ExchangeRateSet.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ExchangeRateSet";
        };

        return ExchangeRateSet;
    })();

    proto.CryptoGetAccountBalanceQuery = (function() {

        /**
         * Properties of a CryptoGetAccountBalanceQuery.
         * @typedef {Object} proto.CryptoGetAccountBalanceQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.AccountID.$Properties|null} [accountID] An account identifier.<br/>
         * This identifies an account for which the balance is requested.
         * <p>
         * Exactly one identifier MUST be provided.
         * @property {proto.ContractID.$Properties|null} [contractID] A smart contract identifier.<br/>
         * This identifies a smart contract for which the balance is requested.
         * <p>
         * Exactly one identifier MUST be provided.
         * @property {"accountID"|"contractID"} [balanceSource] CryptoGetAccountBalanceQuery balanceSource
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoGetAccountBalanceQuery.
         * @memberof proto
         * @interface ICryptoGetAccountBalanceQuery
         * @augments proto.CryptoGetAccountBalanceQuery.$Properties
         * @deprecated Use proto.CryptoGetAccountBalanceQuery.$Properties instead.
         */

        /**
         * Narrowed shape of a CryptoGetAccountBalanceQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   accountID?: proto.AccountID.$Shape|null;
         *   contractID?: proto.ContractID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ balanceSource?: undefined; accountID?: null; contractID?: null }|{ balanceSource?: "accountID"; accountID: proto.AccountID.$Shape; contractID?: null }|{ balanceSource?: "contractID"; accountID?: null; contractID: proto.ContractID.$Shape })
         * )} proto.CryptoGetAccountBalanceQuery.$Shape
         */

        /**
         * Constructs a new CryptoGetAccountBalanceQuery.
         * @memberof proto
         * @classdesc Query to read the HBAR balance of an account or contract.
         * 
         * This query SHALL return _only_ the HBAR balance for an account
         * or smart contract. Early releases of the network would return all
         * fungible/common token balances, but HIP-367 made it infeasible to
         * return all such balances. This query SHALL NOT return any information
         * beyond the current HBAR balance.
         * @constructor
         * @param {proto.CryptoGetAccountBalanceQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoGetAccountBalanceQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @instance
         */
        CryptoGetAccountBalanceQuery.prototype.header = null;

        /**
         * An account identifier.<br/>
         * This identifies an account for which the balance is requested.
         * <p>
         * Exactly one identifier MUST be provided.
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @instance
         */
        CryptoGetAccountBalanceQuery.prototype.accountID = null;

        /**
         * A smart contract identifier.<br/>
         * This identifies a smart contract for which the balance is requested.
         * <p>
         * Exactly one identifier MUST be provided.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @instance
         */
        CryptoGetAccountBalanceQuery.prototype.contractID = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * CryptoGetAccountBalanceQuery balanceSource.
         * @member {"accountID"|"contractID"|undefined} balanceSource
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @instance
         */
        $Object.defineProperty(CryptoGetAccountBalanceQuery.prototype, "balanceSource", {
            get: $util.oneOfGetter($oneOfFields = ["accountID", "contractID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new CryptoGetAccountBalanceQuery instance using the specified properties.
         * @function create
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @static
         * @param {proto.CryptoGetAccountBalanceQuery.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoGetAccountBalanceQuery} CryptoGetAccountBalanceQuery instance
         * @type {{
         *   (properties: proto.CryptoGetAccountBalanceQuery.$Shape): proto.CryptoGetAccountBalanceQuery & proto.CryptoGetAccountBalanceQuery.$Shape;
         *   (properties?: proto.CryptoGetAccountBalanceQuery.$Properties): proto.CryptoGetAccountBalanceQuery;
         * }}
         */
        CryptoGetAccountBalanceQuery.create = function(properties) {
            return new CryptoGetAccountBalanceQuery(properties);
        };

        /**
         * Encodes the specified CryptoGetAccountBalanceQuery message. Does not implicitly {@link proto.CryptoGetAccountBalanceQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @static
         * @param {proto.CryptoGetAccountBalanceQuery.$Properties} message CryptoGetAccountBalanceQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetAccountBalanceQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoGetAccountBalanceQuery message, length delimited. Does not implicitly {@link proto.CryptoGetAccountBalanceQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @static
         * @param {proto.CryptoGetAccountBalanceQuery.$Properties} message CryptoGetAccountBalanceQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetAccountBalanceQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoGetAccountBalanceQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoGetAccountBalanceQuery & proto.CryptoGetAccountBalanceQuery.$Shape} CryptoGetAccountBalanceQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetAccountBalanceQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoGetAccountBalanceQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        message.balanceSource = "accountID";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        message.balanceSource = "contractID";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoGetAccountBalanceQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoGetAccountBalanceQuery & proto.CryptoGetAccountBalanceQuery.$Shape} CryptoGetAccountBalanceQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetAccountBalanceQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoGetAccountBalanceQuery message.
         * @function verify
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoGetAccountBalanceQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                properties.balanceSource = 1;
                {
                    let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                    if (error)
                        return "accountID." + error;
                }
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                if (properties.balanceSource === 1)
                    return "balanceSource: multiple values";
                properties.balanceSource = 1;
                {
                    let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                    if (error)
                        return "contractID." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CryptoGetAccountBalanceQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoGetAccountBalanceQuery} CryptoGetAccountBalanceQuery
         */
        CryptoGetAccountBalanceQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoGetAccountBalanceQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoGetAccountBalanceQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoGetAccountBalanceQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.CryptoGetAccountBalanceQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.CryptoGetAccountBalanceQuery.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.CryptoGetAccountBalanceQuery.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoGetAccountBalanceQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @static
         * @param {proto.CryptoGetAccountBalanceQuery} message CryptoGetAccountBalanceQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoGetAccountBalanceQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.header = null;
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
                if (options.oneofs)
                    object.balanceSource = "accountID";
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
                if (options.oneofs)
                    object.balanceSource = "contractID";
            }
            return object;
        };

        /**
         * Converts this CryptoGetAccountBalanceQuery to JSON.
         * @function toJSON
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoGetAccountBalanceQuery.prototype.toJSON = function() {
            return CryptoGetAccountBalanceQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoGetAccountBalanceQuery
         * @function getTypeUrl
         * @memberof proto.CryptoGetAccountBalanceQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoGetAccountBalanceQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoGetAccountBalanceQuery";
        };

        return CryptoGetAccountBalanceQuery;
    })();

    proto.CryptoGetAccountBalanceResponse = (function() {

        /**
         * Properties of a CryptoGetAccountBalanceResponse.
         * @typedef {Object} proto.CryptoGetAccountBalanceResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.AccountID.$Properties|null} [accountID] An account identifier.<br/>
         * This is the account ID queried. <br/>
         * The inclusion of the account queried is useful with state proofs,
         * when needed to prove an account balance to a third party.
         * @property {number|Long|null} [balance] A current account balance.<br/>
         * This is the current HBAR balance denominated in tinybar
         * (10<sup>-8</sup> HBAR).
         * @property {Array.<proto.TokenBalance.$Properties>|null} [tokenBalances] This field became infeasible to support after HIP-367 removed limits on
         * the number of associated tokens.<br/>
         * A list of token balances for all tokens associated to the account.
         * <p>
         * This field was deprecated by
         * <a href="https://hips.hedera.com/hip/hip-367">HIP-367</a>, which
         * allowed an account to be associated to an unlimited number of tokens.
         * This scale makes it more efficient for users to consult mirror nodes
         * to review their token balances.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoGetAccountBalanceResponse.
         * @memberof proto
         * @interface ICryptoGetAccountBalanceResponse
         * @augments proto.CryptoGetAccountBalanceResponse.$Properties
         * @deprecated Use proto.CryptoGetAccountBalanceResponse.$Properties instead.
         */

        /**
         * Shape of a CryptoGetAccountBalanceResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   accountID?: proto.AccountID.$Shape|null;
         *   balance?: number|Long|null;
         *   tokenBalances?: Array.<proto.TokenBalance.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoGetAccountBalanceResponse.$Shape
         */

        /**
         * Constructs a new CryptoGetAccountBalanceResponse.
         * @memberof proto
         * @classdesc Response to a CryptoGetAccountBalanceQuery.<br/>
         * 
         * This response SHALL contain only the information needed to
         * identify the query request and the actual HBAR balance of the
         * identified account or contract.
         * @constructor
         * @param {proto.CryptoGetAccountBalanceResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoGetAccountBalanceResponse = function (properties) {
            this.tokenBalances = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @instance
         */
        CryptoGetAccountBalanceResponse.prototype.header = null;

        /**
         * An account identifier.<br/>
         * This is the account ID queried. <br/>
         * The inclusion of the account queried is useful with state proofs,
         * when needed to prove an account balance to a third party.
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @instance
         */
        CryptoGetAccountBalanceResponse.prototype.accountID = null;

        /**
         * A current account balance.<br/>
         * This is the current HBAR balance denominated in tinybar
         * (10<sup>-8</sup> HBAR).
         * @member {number|Long} balance
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @instance
         */
        CryptoGetAccountBalanceResponse.prototype.balance = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * This field became infeasible to support after HIP-367 removed limits on
         * the number of associated tokens.<br/>
         * A list of token balances for all tokens associated to the account.
         * <p>
         * This field was deprecated by
         * <a href="https://hips.hedera.com/hip/hip-367">HIP-367</a>, which
         * allowed an account to be associated to an unlimited number of tokens.
         * This scale makes it more efficient for users to consult mirror nodes
         * to review their token balances.
         * @member {Array.<proto.TokenBalance.$Properties>} tokenBalances
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @instance
         */
        CryptoGetAccountBalanceResponse.prototype.tokenBalances = $util.emptyArray;

        /**
         * Creates a new CryptoGetAccountBalanceResponse instance using the specified properties.
         * @function create
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @static
         * @param {proto.CryptoGetAccountBalanceResponse.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoGetAccountBalanceResponse} CryptoGetAccountBalanceResponse instance
         * @type {{
         *   (properties: proto.CryptoGetAccountBalanceResponse.$Shape): proto.CryptoGetAccountBalanceResponse & proto.CryptoGetAccountBalanceResponse.$Shape;
         *   (properties?: proto.CryptoGetAccountBalanceResponse.$Properties): proto.CryptoGetAccountBalanceResponse;
         * }}
         */
        CryptoGetAccountBalanceResponse.create = function(properties) {
            return new CryptoGetAccountBalanceResponse(properties);
        };

        /**
         * Encodes the specified CryptoGetAccountBalanceResponse message. Does not implicitly {@link proto.CryptoGetAccountBalanceResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @static
         * @param {proto.CryptoGetAccountBalanceResponse.$Properties} message CryptoGetAccountBalanceResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetAccountBalanceResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance") && (typeof message.balance === "object" ? message.balance.low || message.balance.high : message.balance !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.balance);
            if (message.tokenBalances != null && message.tokenBalances.length)
                for (let i = 0; i < message.tokenBalances.length; ++i)
                    $root.proto.TokenBalance.encode(message.tokenBalances[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoGetAccountBalanceResponse message, length delimited. Does not implicitly {@link proto.CryptoGetAccountBalanceResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @static
         * @param {proto.CryptoGetAccountBalanceResponse.$Properties} message CryptoGetAccountBalanceResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetAccountBalanceResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoGetAccountBalanceResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoGetAccountBalanceResponse & proto.CryptoGetAccountBalanceResponse.$Shape} CryptoGetAccountBalanceResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetAccountBalanceResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoGetAccountBalanceResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.balance = value;
                        else
                            delete message.balance;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tokenBalances && message.tokenBalances.length))
                            message.tokenBalances = [];
                        message.tokenBalances.push($root.proto.TokenBalance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoGetAccountBalanceResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoGetAccountBalanceResponse & proto.CryptoGetAccountBalanceResponse.$Shape} CryptoGetAccountBalanceResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetAccountBalanceResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoGetAccountBalanceResponse message.
         * @function verify
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoGetAccountBalanceResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (!$util.isInteger(message.balance) && !(message.balance && $util.isInteger(message.balance.low) && $util.isInteger(message.balance.high)))
                    return "balance: integer|Long expected";
            if (message.tokenBalances != null && $Object.hasOwnProperty.call(message, "tokenBalances")) {
                if (!$Array.isArray(message.tokenBalances))
                    return "tokenBalances: array expected";
                for (let i = 0; i < message.tokenBalances.length; ++i) {
                    let error = $root.proto.TokenBalance.verify(message.tokenBalances[i], _depth + 1);
                    if (error)
                        return "tokenBalances." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CryptoGetAccountBalanceResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoGetAccountBalanceResponse} CryptoGetAccountBalanceResponse
         */
        CryptoGetAccountBalanceResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoGetAccountBalanceResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoGetAccountBalanceResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoGetAccountBalanceResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.CryptoGetAccountBalanceResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.CryptoGetAccountBalanceResponse.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.balance != null)
                if (typeof object.balance === "object" ? object.balance.low || object.balance.high : $Number(object.balance) !== 0)
                    if ($util.Long)
                        message.balance = $util.Long.fromValue(object.balance, true);
                    else if (typeof object.balance === "string")
                        message.balance = $parseInt(object.balance, 10);
                    else if (typeof object.balance === "number")
                        message.balance = object.balance;
                    else if (typeof object.balance === "object")
                        message.balance = new $util.LongBits(object.balance.low >>> 0, object.balance.high >>> 0).toNumber(true);
            if (object.tokenBalances) {
                if (!$Array.isArray(object.tokenBalances))
                    throw $TypeError(".proto.CryptoGetAccountBalanceResponse.tokenBalances: array expected");
                message.tokenBalances = $Array(object.tokenBalances.length);
                for (let i = 0; i < object.tokenBalances.length; ++i) {
                    if (!$util.isObject(object.tokenBalances[i]))
                        throw $TypeError(".proto.CryptoGetAccountBalanceResponse.tokenBalances: object expected");
                    message.tokenBalances[i] = $root.proto.TokenBalance.fromObject(object.tokenBalances[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoGetAccountBalanceResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @static
         * @param {proto.CryptoGetAccountBalanceResponse} message CryptoGetAccountBalanceResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoGetAccountBalanceResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tokenBalances = [];
            if (options.defaults) {
                object.header = null;
                object.accountID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.balance = typeof message.balance === "number" ? $BigInt(message.balance) : $util.Long.fromBits(message.balance.low >>> 0, message.balance.high >>> 0, true).toBigInt();
                else if (typeof message.balance === "number")
                    object.balance = options.longs === $String ? $String(message.balance) : message.balance;
                else
                    object.balance = options.longs === $String ? $util.Long.prototype.toString.call(message.balance) : options.longs === $Number ? new $util.LongBits(message.balance.low >>> 0, message.balance.high >>> 0).toNumber(true) : message.balance;
            if (message.tokenBalances && message.tokenBalances.length) {
                object.tokenBalances = $Array(message.tokenBalances.length);
                for (let j = 0; j < message.tokenBalances.length; ++j)
                    object.tokenBalances[j] = $root.proto.TokenBalance.toObject(message.tokenBalances[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this CryptoGetAccountBalanceResponse to JSON.
         * @function toJSON
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoGetAccountBalanceResponse.prototype.toJSON = function() {
            return CryptoGetAccountBalanceResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoGetAccountBalanceResponse
         * @function getTypeUrl
         * @memberof proto.CryptoGetAccountBalanceResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoGetAccountBalanceResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoGetAccountBalanceResponse";
        };

        return CryptoGetAccountBalanceResponse;
    })();

    proto.CryptoGetAccountRecordsQuery = (function() {

        /**
         * Properties of a CryptoGetAccountRecordsQuery.
         * @typedef {Object} proto.CryptoGetAccountRecordsQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.AccountID.$Properties|null} [accountID] An account identifier.<br/>
         * This identifies the account to use when filtering the
         * transaction record lists.
         * <p>
         * This field is REQUIRED.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoGetAccountRecordsQuery.
         * @memberof proto
         * @interface ICryptoGetAccountRecordsQuery
         * @augments proto.CryptoGetAccountRecordsQuery.$Properties
         * @deprecated Use proto.CryptoGetAccountRecordsQuery.$Properties instead.
         */

        /**
         * Shape of a CryptoGetAccountRecordsQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   accountID?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoGetAccountRecordsQuery.$Shape
         */

        /**
         * Constructs a new CryptoGetAccountRecordsQuery.
         * @memberof proto
         * @classdesc Request records of all "recent" transactions for which the specified
         * account is the effective payer.
         * @constructor
         * @param {proto.CryptoGetAccountRecordsQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoGetAccountRecordsQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.CryptoGetAccountRecordsQuery
         * @instance
         */
        CryptoGetAccountRecordsQuery.prototype.header = null;

        /**
         * An account identifier.<br/>
         * This identifies the account to use when filtering the
         * transaction record lists.
         * <p>
         * This field is REQUIRED.
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.CryptoGetAccountRecordsQuery
         * @instance
         */
        CryptoGetAccountRecordsQuery.prototype.accountID = null;

        /**
         * Creates a new CryptoGetAccountRecordsQuery instance using the specified properties.
         * @function create
         * @memberof proto.CryptoGetAccountRecordsQuery
         * @static
         * @param {proto.CryptoGetAccountRecordsQuery.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoGetAccountRecordsQuery} CryptoGetAccountRecordsQuery instance
         * @type {{
         *   (properties: proto.CryptoGetAccountRecordsQuery.$Shape): proto.CryptoGetAccountRecordsQuery & proto.CryptoGetAccountRecordsQuery.$Shape;
         *   (properties?: proto.CryptoGetAccountRecordsQuery.$Properties): proto.CryptoGetAccountRecordsQuery;
         * }}
         */
        CryptoGetAccountRecordsQuery.create = function(properties) {
            return new CryptoGetAccountRecordsQuery(properties);
        };

        /**
         * Encodes the specified CryptoGetAccountRecordsQuery message. Does not implicitly {@link proto.CryptoGetAccountRecordsQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoGetAccountRecordsQuery
         * @static
         * @param {proto.CryptoGetAccountRecordsQuery.$Properties} message CryptoGetAccountRecordsQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetAccountRecordsQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoGetAccountRecordsQuery message, length delimited. Does not implicitly {@link proto.CryptoGetAccountRecordsQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoGetAccountRecordsQuery
         * @static
         * @param {proto.CryptoGetAccountRecordsQuery.$Properties} message CryptoGetAccountRecordsQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetAccountRecordsQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoGetAccountRecordsQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoGetAccountRecordsQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoGetAccountRecordsQuery & proto.CryptoGetAccountRecordsQuery.$Shape} CryptoGetAccountRecordsQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetAccountRecordsQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoGetAccountRecordsQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoGetAccountRecordsQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoGetAccountRecordsQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoGetAccountRecordsQuery & proto.CryptoGetAccountRecordsQuery.$Shape} CryptoGetAccountRecordsQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetAccountRecordsQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoGetAccountRecordsQuery message.
         * @function verify
         * @memberof proto.CryptoGetAccountRecordsQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoGetAccountRecordsQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            return null;
        };

        /**
         * Creates a CryptoGetAccountRecordsQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoGetAccountRecordsQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoGetAccountRecordsQuery} CryptoGetAccountRecordsQuery
         */
        CryptoGetAccountRecordsQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoGetAccountRecordsQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoGetAccountRecordsQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoGetAccountRecordsQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.CryptoGetAccountRecordsQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.CryptoGetAccountRecordsQuery.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoGetAccountRecordsQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoGetAccountRecordsQuery
         * @static
         * @param {proto.CryptoGetAccountRecordsQuery} message CryptoGetAccountRecordsQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoGetAccountRecordsQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.accountID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this CryptoGetAccountRecordsQuery to JSON.
         * @function toJSON
         * @memberof proto.CryptoGetAccountRecordsQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoGetAccountRecordsQuery.prototype.toJSON = function() {
            return CryptoGetAccountRecordsQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoGetAccountRecordsQuery
         * @function getTypeUrl
         * @memberof proto.CryptoGetAccountRecordsQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoGetAccountRecordsQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoGetAccountRecordsQuery";
        };

        return CryptoGetAccountRecordsQuery;
    })();

    proto.CryptoGetAccountRecordsResponse = (function() {

        /**
         * Properties of a CryptoGetAccountRecordsResponse.
         * @typedef {Object} proto.CryptoGetAccountRecordsResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.AccountID.$Properties|null} [accountID] An account identifier.<br/>
         * This identifies the account used when filtering the
         * transaction record lists.
         * <p>
         * This field SHALL match the requested account identifier.
         * @property {Array.<proto.TransactionRecord.$Properties>|null} [records] A list of records.
         * <p>
         * This list SHALL contain all available and "recent" records in which
         * the account identified in the `accountID` field acted as effective payer.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoGetAccountRecordsResponse.
         * @memberof proto
         * @interface ICryptoGetAccountRecordsResponse
         * @augments proto.CryptoGetAccountRecordsResponse.$Properties
         * @deprecated Use proto.CryptoGetAccountRecordsResponse.$Properties instead.
         */

        /**
         * Shape of a CryptoGetAccountRecordsResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   accountID?: proto.AccountID.$Shape|null;
         *   records?: Array.<proto.TransactionRecord.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoGetAccountRecordsResponse.$Shape
         */

        /**
         * Constructs a new CryptoGetAccountRecordsResponse.
         * @memberof proto
         * @classdesc Return records of all "recent" transactions for which the specified
         * account is the effective payer.
         * @constructor
         * @param {proto.CryptoGetAccountRecordsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoGetAccountRecordsResponse = function (properties) {
            this.records = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @instance
         */
        CryptoGetAccountRecordsResponse.prototype.header = null;

        /**
         * An account identifier.<br/>
         * This identifies the account used when filtering the
         * transaction record lists.
         * <p>
         * This field SHALL match the requested account identifier.
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @instance
         */
        CryptoGetAccountRecordsResponse.prototype.accountID = null;

        /**
         * A list of records.
         * <p>
         * This list SHALL contain all available and "recent" records in which
         * the account identified in the `accountID` field acted as effective payer.
         * @member {Array.<proto.TransactionRecord.$Properties>} records
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @instance
         */
        CryptoGetAccountRecordsResponse.prototype.records = $util.emptyArray;

        /**
         * Creates a new CryptoGetAccountRecordsResponse instance using the specified properties.
         * @function create
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @static
         * @param {proto.CryptoGetAccountRecordsResponse.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoGetAccountRecordsResponse} CryptoGetAccountRecordsResponse instance
         * @type {{
         *   (properties: proto.CryptoGetAccountRecordsResponse.$Shape): proto.CryptoGetAccountRecordsResponse & proto.CryptoGetAccountRecordsResponse.$Shape;
         *   (properties?: proto.CryptoGetAccountRecordsResponse.$Properties): proto.CryptoGetAccountRecordsResponse;
         * }}
         */
        CryptoGetAccountRecordsResponse.create = function(properties) {
            return new CryptoGetAccountRecordsResponse(properties);
        };

        /**
         * Encodes the specified CryptoGetAccountRecordsResponse message. Does not implicitly {@link proto.CryptoGetAccountRecordsResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @static
         * @param {proto.CryptoGetAccountRecordsResponse.$Properties} message CryptoGetAccountRecordsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetAccountRecordsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.records != null && message.records.length)
                for (let i = 0; i < message.records.length; ++i)
                    $root.proto.TransactionRecord.encode(message.records[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoGetAccountRecordsResponse message, length delimited. Does not implicitly {@link proto.CryptoGetAccountRecordsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @static
         * @param {proto.CryptoGetAccountRecordsResponse.$Properties} message CryptoGetAccountRecordsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetAccountRecordsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoGetAccountRecordsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoGetAccountRecordsResponse & proto.CryptoGetAccountRecordsResponse.$Shape} CryptoGetAccountRecordsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetAccountRecordsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoGetAccountRecordsResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.records && message.records.length))
                            message.records = [];
                        message.records.push($root.proto.TransactionRecord.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoGetAccountRecordsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoGetAccountRecordsResponse & proto.CryptoGetAccountRecordsResponse.$Shape} CryptoGetAccountRecordsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetAccountRecordsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoGetAccountRecordsResponse message.
         * @function verify
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoGetAccountRecordsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            if (message.records != null && $Object.hasOwnProperty.call(message, "records")) {
                if (!$Array.isArray(message.records))
                    return "records: array expected";
                for (let i = 0; i < message.records.length; ++i) {
                    let error = $root.proto.TransactionRecord.verify(message.records[i], _depth + 1);
                    if (error)
                        return "records." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CryptoGetAccountRecordsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoGetAccountRecordsResponse} CryptoGetAccountRecordsResponse
         */
        CryptoGetAccountRecordsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoGetAccountRecordsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoGetAccountRecordsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoGetAccountRecordsResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.CryptoGetAccountRecordsResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.CryptoGetAccountRecordsResponse.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.records) {
                if (!$Array.isArray(object.records))
                    throw $TypeError(".proto.CryptoGetAccountRecordsResponse.records: array expected");
                message.records = $Array(object.records.length);
                for (let i = 0; i < object.records.length; ++i) {
                    if (!$util.isObject(object.records[i]))
                        throw $TypeError(".proto.CryptoGetAccountRecordsResponse.records: object expected");
                    message.records[i] = $root.proto.TransactionRecord.fromObject(object.records[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoGetAccountRecordsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @static
         * @param {proto.CryptoGetAccountRecordsResponse} message CryptoGetAccountRecordsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoGetAccountRecordsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.records = [];
            if (options.defaults) {
                object.header = null;
                object.accountID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            if (message.records && message.records.length) {
                object.records = $Array(message.records.length);
                for (let j = 0; j < message.records.length; ++j)
                    object.records[j] = $root.proto.TransactionRecord.toObject(message.records[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this CryptoGetAccountRecordsResponse to JSON.
         * @function toJSON
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoGetAccountRecordsResponse.prototype.toJSON = function() {
            return CryptoGetAccountRecordsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoGetAccountRecordsResponse
         * @function getTypeUrl
         * @memberof proto.CryptoGetAccountRecordsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoGetAccountRecordsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoGetAccountRecordsResponse";
        };

        return CryptoGetAccountRecordsResponse;
    })();

    proto.CryptoGetInfoQuery = (function() {

        /**
         * Properties of a CryptoGetInfoQuery.
         * @typedef {Object} proto.CryptoGetInfoQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.AccountID.$Properties|null} [accountID] The account ID for which information is requested
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoGetInfoQuery.
         * @memberof proto
         * @interface ICryptoGetInfoQuery
         * @augments proto.CryptoGetInfoQuery.$Properties
         * @deprecated Use proto.CryptoGetInfoQuery.$Properties instead.
         */

        /**
         * Shape of a CryptoGetInfoQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   accountID?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoGetInfoQuery.$Shape
         */

        /**
         * Constructs a new CryptoGetInfoQuery.
         * @memberof proto
         * @classdesc A query to read information for an account.
         * 
         * The returned information SHALL include balance.<br/>
         * The returned information SHALL NOT include allowances.<br/>
         * The returned information SHALL NOT include token relationships.<br/>
         * The returned information SHALL NOT include account records.
         * @constructor
         * @param {proto.CryptoGetInfoQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoGetInfoQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.CryptoGetInfoQuery
         * @instance
         */
        CryptoGetInfoQuery.prototype.header = null;

        /**
         * The account ID for which information is requested
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.CryptoGetInfoQuery
         * @instance
         */
        CryptoGetInfoQuery.prototype.accountID = null;

        /**
         * Creates a new CryptoGetInfoQuery instance using the specified properties.
         * @function create
         * @memberof proto.CryptoGetInfoQuery
         * @static
         * @param {proto.CryptoGetInfoQuery.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoGetInfoQuery} CryptoGetInfoQuery instance
         * @type {{
         *   (properties: proto.CryptoGetInfoQuery.$Shape): proto.CryptoGetInfoQuery & proto.CryptoGetInfoQuery.$Shape;
         *   (properties?: proto.CryptoGetInfoQuery.$Properties): proto.CryptoGetInfoQuery;
         * }}
         */
        CryptoGetInfoQuery.create = function(properties) {
            return new CryptoGetInfoQuery(properties);
        };

        /**
         * Encodes the specified CryptoGetInfoQuery message. Does not implicitly {@link proto.CryptoGetInfoQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoGetInfoQuery
         * @static
         * @param {proto.CryptoGetInfoQuery.$Properties} message CryptoGetInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetInfoQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoGetInfoQuery message, length delimited. Does not implicitly {@link proto.CryptoGetInfoQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoGetInfoQuery
         * @static
         * @param {proto.CryptoGetInfoQuery.$Properties} message CryptoGetInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetInfoQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoGetInfoQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoGetInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoGetInfoQuery & proto.CryptoGetInfoQuery.$Shape} CryptoGetInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetInfoQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoGetInfoQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoGetInfoQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoGetInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoGetInfoQuery & proto.CryptoGetInfoQuery.$Shape} CryptoGetInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetInfoQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoGetInfoQuery message.
         * @function verify
         * @memberof proto.CryptoGetInfoQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoGetInfoQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            return null;
        };

        /**
         * Creates a CryptoGetInfoQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoGetInfoQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoGetInfoQuery} CryptoGetInfoQuery
         */
        CryptoGetInfoQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoGetInfoQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoGetInfoQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoGetInfoQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.CryptoGetInfoQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.CryptoGetInfoQuery.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoGetInfoQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoGetInfoQuery
         * @static
         * @param {proto.CryptoGetInfoQuery} message CryptoGetInfoQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoGetInfoQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.accountID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this CryptoGetInfoQuery to JSON.
         * @function toJSON
         * @memberof proto.CryptoGetInfoQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoGetInfoQuery.prototype.toJSON = function() {
            return CryptoGetInfoQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoGetInfoQuery
         * @function getTypeUrl
         * @memberof proto.CryptoGetInfoQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoGetInfoQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoGetInfoQuery";
        };

        return CryptoGetInfoQuery;
    })();

    proto.CryptoGetInfoResponse = (function() {

        /**
         * Properties of a CryptoGetInfoResponse.
         * @typedef {Object} proto.CryptoGetInfoResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.CryptoGetInfoResponse.AccountInfo.$Properties|null} [accountInfo] Details of the account.
         * <p>
         * A state proof MAY be generated for this field.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoGetInfoResponse.
         * @memberof proto
         * @interface ICryptoGetInfoResponse
         * @augments proto.CryptoGetInfoResponse.$Properties
         * @deprecated Use proto.CryptoGetInfoResponse.$Properties instead.
         */

        /**
         * Shape of a CryptoGetInfoResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   accountInfo?: proto.CryptoGetInfoResponse.AccountInfo.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoGetInfoResponse.$Shape
         */

        /**
         * Constructs a new CryptoGetInfoResponse.
         * @memberof proto
         * @classdesc Response when the client sends the node CryptoGetInfoQuery
         * @constructor
         * @param {proto.CryptoGetInfoResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoGetInfoResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.CryptoGetInfoResponse
         * @instance
         */
        CryptoGetInfoResponse.prototype.header = null;

        /**
         * Details of the account.
         * <p>
         * A state proof MAY be generated for this field.
         * @member {proto.CryptoGetInfoResponse.AccountInfo.$Properties|null|undefined} accountInfo
         * @memberof proto.CryptoGetInfoResponse
         * @instance
         */
        CryptoGetInfoResponse.prototype.accountInfo = null;

        /**
         * Creates a new CryptoGetInfoResponse instance using the specified properties.
         * @function create
         * @memberof proto.CryptoGetInfoResponse
         * @static
         * @param {proto.CryptoGetInfoResponse.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoGetInfoResponse} CryptoGetInfoResponse instance
         * @type {{
         *   (properties: proto.CryptoGetInfoResponse.$Shape): proto.CryptoGetInfoResponse & proto.CryptoGetInfoResponse.$Shape;
         *   (properties?: proto.CryptoGetInfoResponse.$Properties): proto.CryptoGetInfoResponse;
         * }}
         */
        CryptoGetInfoResponse.create = function(properties) {
            return new CryptoGetInfoResponse(properties);
        };

        /**
         * Encodes the specified CryptoGetInfoResponse message. Does not implicitly {@link proto.CryptoGetInfoResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoGetInfoResponse
         * @static
         * @param {proto.CryptoGetInfoResponse.$Properties} message CryptoGetInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetInfoResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountInfo != null && $Object.hasOwnProperty.call(message, "accountInfo"))
                $root.proto.CryptoGetInfoResponse.AccountInfo.encode(message.accountInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoGetInfoResponse message, length delimited. Does not implicitly {@link proto.CryptoGetInfoResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoGetInfoResponse
         * @static
         * @param {proto.CryptoGetInfoResponse.$Properties} message CryptoGetInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetInfoResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoGetInfoResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoGetInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoGetInfoResponse & proto.CryptoGetInfoResponse.$Shape} CryptoGetInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetInfoResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoGetInfoResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountInfo = $root.proto.CryptoGetInfoResponse.AccountInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountInfo);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoGetInfoResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoGetInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoGetInfoResponse & proto.CryptoGetInfoResponse.$Shape} CryptoGetInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetInfoResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoGetInfoResponse message.
         * @function verify
         * @memberof proto.CryptoGetInfoResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoGetInfoResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.accountInfo != null && $Object.hasOwnProperty.call(message, "accountInfo")) {
                let error = $root.proto.CryptoGetInfoResponse.AccountInfo.verify(message.accountInfo, _depth + 1);
                if (error)
                    return "accountInfo." + error;
            }
            return null;
        };

        /**
         * Creates a CryptoGetInfoResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoGetInfoResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoGetInfoResponse} CryptoGetInfoResponse
         */
        CryptoGetInfoResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoGetInfoResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoGetInfoResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoGetInfoResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.CryptoGetInfoResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.accountInfo != null) {
                if (!$util.isObject(object.accountInfo))
                    throw $TypeError(".proto.CryptoGetInfoResponse.accountInfo: object expected");
                message.accountInfo = $root.proto.CryptoGetInfoResponse.AccountInfo.fromObject(object.accountInfo, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoGetInfoResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoGetInfoResponse
         * @static
         * @param {proto.CryptoGetInfoResponse} message CryptoGetInfoResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoGetInfoResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.accountInfo = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.accountInfo != null && $Object.hasOwnProperty.call(message, "accountInfo"))
                object.accountInfo = $root.proto.CryptoGetInfoResponse.AccountInfo.toObject(message.accountInfo, options, _depth + 1);
            return object;
        };

        /**
         * Converts this CryptoGetInfoResponse to JSON.
         * @function toJSON
         * @memberof proto.CryptoGetInfoResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoGetInfoResponse.prototype.toJSON = function() {
            return CryptoGetInfoResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoGetInfoResponse
         * @function getTypeUrl
         * @memberof proto.CryptoGetInfoResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoGetInfoResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoGetInfoResponse";
        };

        CryptoGetInfoResponse.AccountInfo = (function() {

            /**
             * Properties of an AccountInfo.
             * @typedef {Object} proto.CryptoGetInfoResponse.AccountInfo.$Properties
             * @property {proto.AccountID.$Properties|null} [accountID] a unique identifier for this account.
             * <p>
             * An account identifier, when assigned to this field, SHALL be of
             * the form `shard.realm.number`.
             * @property {string|null} [contractAccountID] A Solidity ID.
             * <p>
             * This SHALL be populated if this account is a smart contract, and
             * SHALL NOT be populated otherwise.<br/>
             * This SHALL be formatted as a string according to Solidity ID
             * standards.
             * @property {boolean|null} [deleted] A boolean indicating that this account is deleted.
             * <p>
             * Any transaction involving a deleted account SHALL fail.
             * @property {proto.AccountID.$Properties|null} [proxyAccountID] Replaced by StakingInfo.<br/>
             * ID of the account to which this account is staking its balances. If
             * this account is not currently staking its balances, then this field,
             * if set, SHALL be the sentinel value of `0.0.0`.
             * @property {number|Long|null} [proxyReceived] Replaced by StakingInfo.<br/>
             * The total amount of tinybar proxy staked to this account.
             * @property {proto.Key.$Properties|null} [key] The key to be used to sign transactions from this account, if any.
             * <p>
             * This key SHALL NOT be set for hollow accounts until the account
             * is finalized.<br/>
             * This key SHALL be set on all other accounts, except for certain
             * immutable accounts (0.0.800 and 0.0.801) necessary for network
             * function and otherwise secured by the governing council.
             * @property {number|Long|null} [balance] The HBAR balance of this account, in tinybar (10<sup>-8</sup> HBAR).
             * <p>
             * This value SHALL always be a whole number.
             * @property {number|Long|null} [generateSendRecordThreshold] Obsolete and unused.<br/>
             * The threshold amount, in tinybars, at which a record was created for
             * any transaction that decreased the balance of this account.
             * @property {number|Long|null} [generateReceiveRecordThreshold] Obsolete and unused.<br/>
             * The threshold amount, in tinybars, at which a record was created for
             * any transaction that increased the balance of this account.
             * @property {boolean|null} [receiverSigRequired] A boolean indicating that the account requires a receiver signature
             * for inbound token transfer transactions.
             * <p>
             * If this value is `true` then a transaction to transfer tokens to this
             * account SHALL NOT succeed unless this account has signed the
             * transfer transaction.
             * @property {proto.Timestamp.$Properties|null} [expirationTime] The current expiration time for this account.
             * <p>
             * This account SHALL be due standard renewal fees when the network
             * consensus time exceeds this time.<br/>
             * If rent and expiration are enabled for the network, and automatic
             * renewal is enabled for this account, renewal fees SHALL be charged
             * after this time, and, if charged, the expiration time SHALL be
             * extended for another renewal period.<br/>
             * This account MAY be expired and removed from state at any point
             * after this time if not renewed.<br/>
             * An account holder MAY extend this time by submitting an account
             * update transaction to modify expiration time, subject to the current
             * maximum expiration time for the network.
             * @property {proto.Duration.$Properties|null} [autoRenewPeriod] A duration to extend this account's expiration.
             * <p>
             * The network SHALL extend the account's expiration by this
             * duration, if funds are available, upon automatic renewal.<br/>
             * This SHALL NOT apply if the account is already deleted
             * upon expiration.<br/>
             * If this is not provided in an allowed range on account creation, the
             * transaction SHALL fail with INVALID_AUTO_RENEWAL_PERIOD. The default
             * values for the minimum period and maximum period are currently
             * 30 days and 90 days, respectively.
             * @property {Array.<proto.LiveHash.$Properties>|null} [liveHashes] All of the livehashes attached to the account (each of which is a
             * hash along with the keys that authorized it and can delete it)
             * @property {Array.<proto.TokenRelationship.$Properties>|null} [tokenRelationships] As of `HIP-367`, which enabled unlimited token associations, the
             * potential scale for this value requires that users consult a mirror
             * node for this information.
             * @property {string|null} [memo] A short description of this account.
             * <p>
             * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
             * (default 100) bytes when encoded as UTF-8.
             * @property {number|Long|null} [ownedNfts] The total number of non-fungible/unique tokens owned by this account.
             * @property {number|null} [maxAutomaticTokenAssociations] The maximum number of tokens that can be auto-associated with the
             * account.
             * <p>
             * If this is less than or equal to `used_auto_associations` (or 0),
             * then this account MUST manually associate with a token before
             * transacting in that token.<br/>
             * Following HIP-904 This value may also be `-1` to indicate no
             * limit.<br/>
             * This value MUST NOT be less than `-1`.
             * @property {Uint8Array|null} [alias] An account alias.<br/>
             * This is a value used in some contexts to reference an account when
             * the tripartite account identifier is not available.
             * <p>
             * This field, when set to a non-default value, is immutable and
             * SHALL NOT be changed.
             * @property {Uint8Array|null} [ledgerId] The ledger ID of the network that generated this response.
             * <p>
             * This value SHALL identify the distributed ledger that responded to
             * this query.
             * @property {number|Long|null} [ethereumNonce] The ethereum transaction nonce associated with this account.
             * @property {proto.StakingInfo.$Properties|null} [stakingInfo] Staking information for this account.
             * @property {Uint8Array|null} [delegationAddress] The delegated contract address set for this account.
             * <p>
             * If this field is set on the account, anytime a call is made to the account's address
             * (either an EVM call within HSCS or a child call dispatched by CryptoTransfer)
             * the EVM code of the contract referenced by `delegation_address` will be
             * executed in the context of the account.
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an AccountInfo.
             * @memberof proto.CryptoGetInfoResponse
             * @interface IAccountInfo
             * @augments proto.CryptoGetInfoResponse.AccountInfo.$Properties
             * @deprecated Use proto.CryptoGetInfoResponse.AccountInfo.$Properties instead.
             */

            /**
             * Shape of an AccountInfo.
             * @typedef {{
             *   accountID?: proto.AccountID.$Shape|null;
             *   contractAccountID?: string|null;
             *   deleted?: boolean|null;
             *   proxyAccountID?: proto.AccountID.$Shape|null;
             *   proxyReceived?: number|Long|null;
             *   key?: proto.Key.$Shape|null;
             *   balance?: number|Long|null;
             *   generateSendRecordThreshold?: number|Long|null;
             *   generateReceiveRecordThreshold?: number|Long|null;
             *   receiverSigRequired?: boolean|null;
             *   expirationTime?: proto.Timestamp.$Shape|null;
             *   autoRenewPeriod?: proto.Duration.$Shape|null;
             *   liveHashes?: Array.<proto.LiveHash.$Shape>|null;
             *   tokenRelationships?: Array.<proto.TokenRelationship.$Shape>|null;
             *   memo?: string|null;
             *   ownedNfts?: number|Long|null;
             *   maxAutomaticTokenAssociations?: number|null;
             *   alias?: Uint8Array|null;
             *   ledgerId?: Uint8Array|null;
             *   ethereumNonce?: number|Long|null;
             *   stakingInfo?: proto.StakingInfo.$Shape|null;
             *   delegationAddress?: Uint8Array|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} proto.CryptoGetInfoResponse.AccountInfo.$Shape
             */

            /**
             * Constructs a new AccountInfo.
             * @memberof proto.CryptoGetInfoResponse
             * @classdesc Information describing A single Account in the Hedera distributed ledger.
             * 
             * #### Attributes
             * Each Account may have a unique three-part identifier, a Key, and one or
             * more token balances. Accounts also have an alias, which has multiple
             * forms, and may be set automatically. Several additional items are
             * associated with the Account to enable full functionality.
             * 
             * #### Expiration
             * Accounts, as most items in the network, have an expiration time, recorded
             * as a `Timestamp`, and must be "renewed" for a small fee at expiration.
             * This helps to reduce the amount of inactive accounts retained in state.
             * Another account may be designated to pay any renewal fees and
             * automatically renew the account for (by default) 30-90 days at a time as
             * a means to optionally ensure important accounts remain active.
             * 
             * ### Staking
             * Accounts may participate in securing the network by "staking" the account
             * balances to a particular network node, and receive a portion of network
             * fees as a reward. An account may optionally decline these rewards but
             * still stake its balances.
             * 
             * #### Transfer Restrictions
             * An account may optionally require that inbound transfer transactions be
             * signed by that account as receiver (in addition to any other signatures
             * required, including sender).
             * @constructor
             * @param {proto.CryptoGetInfoResponse.AccountInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const AccountInfo = function (properties) {
                this.liveHashes = [];
                this.tokenRelationships = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * a unique identifier for this account.
             * <p>
             * An account identifier, when assigned to this field, SHALL be of
             * the form `shard.realm.number`.
             * @member {proto.AccountID.$Properties|null|undefined} accountID
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.accountID = null;

            /**
             * A Solidity ID.
             * <p>
             * This SHALL be populated if this account is a smart contract, and
             * SHALL NOT be populated otherwise.<br/>
             * This SHALL be formatted as a string according to Solidity ID
             * standards.
             * @member {string} contractAccountID
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.contractAccountID = "";

            /**
             * A boolean indicating that this account is deleted.
             * <p>
             * Any transaction involving a deleted account SHALL fail.
             * @member {boolean} deleted
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.deleted = false;

            /**
             * Replaced by StakingInfo.<br/>
             * ID of the account to which this account is staking its balances. If
             * this account is not currently staking its balances, then this field,
             * if set, SHALL be the sentinel value of `0.0.0`.
             * @member {proto.AccountID.$Properties|null|undefined} proxyAccountID
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.proxyAccountID = null;

            /**
             * Replaced by StakingInfo.<br/>
             * The total amount of tinybar proxy staked to this account.
             * @member {number|Long} proxyReceived
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.proxyReceived = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * The key to be used to sign transactions from this account, if any.
             * <p>
             * This key SHALL NOT be set for hollow accounts until the account
             * is finalized.<br/>
             * This key SHALL be set on all other accounts, except for certain
             * immutable accounts (0.0.800 and 0.0.801) necessary for network
             * function and otherwise secured by the governing council.
             * @member {proto.Key.$Properties|null|undefined} key
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.key = null;

            /**
             * The HBAR balance of this account, in tinybar (10<sup>-8</sup> HBAR).
             * <p>
             * This value SHALL always be a whole number.
             * @member {number|Long} balance
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.balance = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * Obsolete and unused.<br/>
             * The threshold amount, in tinybars, at which a record was created for
             * any transaction that decreased the balance of this account.
             * @member {number|Long} generateSendRecordThreshold
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.generateSendRecordThreshold = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * Obsolete and unused.<br/>
             * The threshold amount, in tinybars, at which a record was created for
             * any transaction that increased the balance of this account.
             * @member {number|Long} generateReceiveRecordThreshold
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.generateReceiveRecordThreshold = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * A boolean indicating that the account requires a receiver signature
             * for inbound token transfer transactions.
             * <p>
             * If this value is `true` then a transaction to transfer tokens to this
             * account SHALL NOT succeed unless this account has signed the
             * transfer transaction.
             * @member {boolean} receiverSigRequired
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.receiverSigRequired = false;

            /**
             * The current expiration time for this account.
             * <p>
             * This account SHALL be due standard renewal fees when the network
             * consensus time exceeds this time.<br/>
             * If rent and expiration are enabled for the network, and automatic
             * renewal is enabled for this account, renewal fees SHALL be charged
             * after this time, and, if charged, the expiration time SHALL be
             * extended for another renewal period.<br/>
             * This account MAY be expired and removed from state at any point
             * after this time if not renewed.<br/>
             * An account holder MAY extend this time by submitting an account
             * update transaction to modify expiration time, subject to the current
             * maximum expiration time for the network.
             * @member {proto.Timestamp.$Properties|null|undefined} expirationTime
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.expirationTime = null;

            /**
             * A duration to extend this account's expiration.
             * <p>
             * The network SHALL extend the account's expiration by this
             * duration, if funds are available, upon automatic renewal.<br/>
             * This SHALL NOT apply if the account is already deleted
             * upon expiration.<br/>
             * If this is not provided in an allowed range on account creation, the
             * transaction SHALL fail with INVALID_AUTO_RENEWAL_PERIOD. The default
             * values for the minimum period and maximum period are currently
             * 30 days and 90 days, respectively.
             * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.autoRenewPeriod = null;

            /**
             * All of the livehashes attached to the account (each of which is a
             * hash along with the keys that authorized it and can delete it)
             * @member {Array.<proto.LiveHash.$Properties>} liveHashes
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.liveHashes = $util.emptyArray;

            /**
             * As of `HIP-367`, which enabled unlimited token associations, the
             * potential scale for this value requires that users consult a mirror
             * node for this information.
             * @member {Array.<proto.TokenRelationship.$Properties>} tokenRelationships
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.tokenRelationships = $util.emptyArray;

            /**
             * A short description of this account.
             * <p>
             * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
             * (default 100) bytes when encoded as UTF-8.
             * @member {string} memo
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.memo = "";

            /**
             * The total number of non-fungible/unique tokens owned by this account.
             * @member {number|Long} ownedNfts
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.ownedNfts = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * The maximum number of tokens that can be auto-associated with the
             * account.
             * <p>
             * If this is less than or equal to `used_auto_associations` (or 0),
             * then this account MUST manually associate with a token before
             * transacting in that token.<br/>
             * Following HIP-904 This value may also be `-1` to indicate no
             * limit.<br/>
             * This value MUST NOT be less than `-1`.
             * @member {number} maxAutomaticTokenAssociations
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.maxAutomaticTokenAssociations = 0;

            /**
             * An account alias.<br/>
             * This is a value used in some contexts to reference an account when
             * the tripartite account identifier is not available.
             * <p>
             * This field, when set to a non-default value, is immutable and
             * SHALL NOT be changed.
             * @member {Uint8Array} alias
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.alias = $util.newBuffer([]);

            /**
             * The ledger ID of the network that generated this response.
             * <p>
             * This value SHALL identify the distributed ledger that responded to
             * this query.
             * @member {Uint8Array} ledgerId
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.ledgerId = $util.newBuffer([]);

            /**
             * The ethereum transaction nonce associated with this account.
             * @member {number|Long} ethereumNonce
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.ethereumNonce = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Staking information for this account.
             * @member {proto.StakingInfo.$Properties|null|undefined} stakingInfo
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.stakingInfo = null;

            /**
             * The delegated contract address set for this account.
             * <p>
             * If this field is set on the account, anytime a call is made to the account's address
             * (either an EVM call within HSCS or a child call dispatched by CryptoTransfer)
             * the EVM code of the contract referenced by `delegation_address` will be
             * executed in the context of the account.
             * @member {Uint8Array} delegationAddress
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             */
            AccountInfo.prototype.delegationAddress = $util.newBuffer([]);

            /**
             * Creates a new AccountInfo instance using the specified properties.
             * @function create
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @static
             * @param {proto.CryptoGetInfoResponse.AccountInfo.$Properties=} [properties] Properties to set
             * @returns {proto.CryptoGetInfoResponse.AccountInfo} AccountInfo instance
             * @type {{
             *   (properties: proto.CryptoGetInfoResponse.AccountInfo.$Shape): proto.CryptoGetInfoResponse.AccountInfo & proto.CryptoGetInfoResponse.AccountInfo.$Shape;
             *   (properties?: proto.CryptoGetInfoResponse.AccountInfo.$Properties): proto.CryptoGetInfoResponse.AccountInfo;
             * }}
             */
            AccountInfo.create = function(properties) {
                return new AccountInfo(properties);
            };

            /**
             * Encodes the specified AccountInfo message. Does not implicitly {@link proto.CryptoGetInfoResponse.AccountInfo.verify|verify} messages.
             * @function encode
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @static
             * @param {proto.CryptoGetInfoResponse.AccountInfo.$Properties} message AccountInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AccountInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                    $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.contractAccountID != null && $Object.hasOwnProperty.call(message, "contractAccountID") && message.contractAccountID !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.contractAccountID);
                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.deleted);
                if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID"))
                    $root.proto.AccountID.encode(message.proxyAccountID, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.proxyReceived != null && $Object.hasOwnProperty.call(message, "proxyReceived") && (typeof message.proxyReceived === "object" ? message.proxyReceived.low || message.proxyReceived.high : message.proxyReceived !== 0))
                    writer.uint32(/* id 6, wireType 0 =*/48).int64(message.proxyReceived);
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    $root.proto.Key.encode(message.key, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
                if (message.balance != null && $Object.hasOwnProperty.call(message, "balance") && (typeof message.balance === "object" ? message.balance.low || message.balance.high : message.balance !== 0))
                    writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.balance);
                if (message.generateSendRecordThreshold != null && $Object.hasOwnProperty.call(message, "generateSendRecordThreshold") && (typeof message.generateSendRecordThreshold === "object" ? message.generateSendRecordThreshold.low || message.generateSendRecordThreshold.high : message.generateSendRecordThreshold !== 0))
                    writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.generateSendRecordThreshold);
                if (message.generateReceiveRecordThreshold != null && $Object.hasOwnProperty.call(message, "generateReceiveRecordThreshold") && (typeof message.generateReceiveRecordThreshold === "object" ? message.generateReceiveRecordThreshold.low || message.generateReceiveRecordThreshold.high : message.generateReceiveRecordThreshold !== 0))
                    writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.generateReceiveRecordThreshold);
                if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired") && message.receiverSigRequired !== false)
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.receiverSigRequired);
                if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                    $root.proto.Timestamp.encode(message.expirationTime, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
                if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                    $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
                if (message.liveHashes != null && message.liveHashes.length)
                    for (let i = 0; i < message.liveHashes.length; ++i)
                        $root.proto.LiveHash.encode(message.liveHashes[i], writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
                if (message.tokenRelationships != null && message.tokenRelationships.length)
                    for (let i = 0; i < message.tokenRelationships.length; ++i)
                        $root.proto.TokenRelationship.encode(message.tokenRelationships[i], writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                    writer.uint32(/* id 16, wireType 2 =*/130).string(message.memo);
                if (message.ownedNfts != null && $Object.hasOwnProperty.call(message, "ownedNfts") && (typeof message.ownedNfts === "object" ? message.ownedNfts.low || message.ownedNfts.high : message.ownedNfts !== 0))
                    writer.uint32(/* id 17, wireType 0 =*/136).int64(message.ownedNfts);
                if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations") && message.maxAutomaticTokenAssociations !== 0)
                    writer.uint32(/* id 18, wireType 0 =*/144).int32(message.maxAutomaticTokenAssociations);
                if (message.alias != null && $Object.hasOwnProperty.call(message, "alias") && message.alias.length)
                    writer.uint32(/* id 19, wireType 2 =*/154).bytes(message.alias);
                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId") && message.ledgerId.length)
                    writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.ledgerId);
                if (message.ethereumNonce != null && $Object.hasOwnProperty.call(message, "ethereumNonce") && (typeof message.ethereumNonce === "object" ? message.ethereumNonce.low || message.ethereumNonce.high : message.ethereumNonce !== 0))
                    writer.uint32(/* id 21, wireType 0 =*/168).int64(message.ethereumNonce);
                if (message.stakingInfo != null && $Object.hasOwnProperty.call(message, "stakingInfo"))
                    $root.proto.StakingInfo.encode(message.stakingInfo, writer.uint32(/* id 22, wireType 2 =*/178).fork(), _depth + 1).ldelim();
                if (message.delegationAddress != null && $Object.hasOwnProperty.call(message, "delegationAddress") && message.delegationAddress.length)
                    writer.uint32(/* id 23, wireType 2 =*/186).bytes(message.delegationAddress);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AccountInfo message, length delimited. Does not implicitly {@link proto.CryptoGetInfoResponse.AccountInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @static
             * @param {proto.CryptoGetInfoResponse.AccountInfo.$Properties} message AccountInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AccountInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an AccountInfo message from the specified reader or buffer.
             * @function decode
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.CryptoGetInfoResponse.AccountInfo & proto.CryptoGetInfoResponse.AccountInfo.$Shape} AccountInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AccountInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.proto.CryptoGetInfoResponse.AccountInfo();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.contractAccountID = value;
                            else
                                delete message.contractAccountID;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.deleted = value;
                            else
                                delete message.deleted;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.proxyAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.proxyAccountID);
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.proxyReceived = value;
                            else
                                delete message.proxyReceived;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.key = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key);
                            continue;
                        }
                    case 8: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.balance = value;
                            else
                                delete message.balance;
                            continue;
                        }
                    case 9: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.generateSendRecordThreshold = value;
                            else
                                delete message.generateSendRecordThreshold;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.generateReceiveRecordThreshold = value;
                            else
                                delete message.generateReceiveRecordThreshold;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.receiverSigRequired = value;
                            else
                                delete message.receiverSigRequired;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            message.expirationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                            continue;
                        }
                    case 14: {
                            if (wireType !== 2)
                                break;
                            if (!(message.liveHashes && message.liveHashes.length))
                                message.liveHashes = [];
                            message.liveHashes.push($root.proto.LiveHash.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 15: {
                            if (wireType !== 2)
                                break;
                            if (!(message.tokenRelationships && message.tokenRelationships.length))
                                message.tokenRelationships = [];
                            message.tokenRelationships.push($root.proto.TokenRelationship.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 16: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.memo = value;
                            else
                                delete message.memo;
                            continue;
                        }
                    case 17: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.ownedNfts = value;
                            else
                                delete message.ownedNfts;
                            continue;
                        }
                    case 18: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.maxAutomaticTokenAssociations = value;
                            else
                                delete message.maxAutomaticTokenAssociations;
                            continue;
                        }
                    case 19: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.bytes()).length)
                                message.alias = value;
                            else
                                delete message.alias;
                            continue;
                        }
                    case 20: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.bytes()).length)
                                message.ledgerId = value;
                            else
                                delete message.ledgerId;
                            continue;
                        }
                    case 21: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.ethereumNonce = value;
                            else
                                delete message.ethereumNonce;
                            continue;
                        }
                    case 22: {
                            if (wireType !== 2)
                                break;
                            message.stakingInfo = $root.proto.StakingInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stakingInfo);
                            continue;
                        }
                    case 23: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.bytes()).length)
                                message.delegationAddress = value;
                            else
                                delete message.delegationAddress;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an AccountInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.CryptoGetInfoResponse.AccountInfo & proto.CryptoGetInfoResponse.AccountInfo.$Shape} AccountInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AccountInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AccountInfo message.
             * @function verify
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AccountInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                    let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                    if (error)
                        return "accountID." + error;
                }
                if (message.contractAccountID != null && $Object.hasOwnProperty.call(message, "contractAccountID"))
                    if (!$util.isString(message.contractAccountID))
                        return "contractAccountID: string expected";
                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                    if (typeof message.deleted !== "boolean")
                        return "deleted: boolean expected";
                if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID")) {
                    let error = $root.proto.AccountID.verify(message.proxyAccountID, _depth + 1);
                    if (error)
                        return "proxyAccountID." + error;
                }
                if (message.proxyReceived != null && $Object.hasOwnProperty.call(message, "proxyReceived"))
                    if (!$util.isInteger(message.proxyReceived) && !(message.proxyReceived && $util.isInteger(message.proxyReceived.low) && $util.isInteger(message.proxyReceived.high)))
                        return "proxyReceived: integer|Long expected";
                if (message.key != null && $Object.hasOwnProperty.call(message, "key")) {
                    let error = $root.proto.Key.verify(message.key, _depth + 1);
                    if (error)
                        return "key." + error;
                }
                if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                    if (!$util.isInteger(message.balance) && !(message.balance && $util.isInteger(message.balance.low) && $util.isInteger(message.balance.high)))
                        return "balance: integer|Long expected";
                if (message.generateSendRecordThreshold != null && $Object.hasOwnProperty.call(message, "generateSendRecordThreshold"))
                    if (!$util.isInteger(message.generateSendRecordThreshold) && !(message.generateSendRecordThreshold && $util.isInteger(message.generateSendRecordThreshold.low) && $util.isInteger(message.generateSendRecordThreshold.high)))
                        return "generateSendRecordThreshold: integer|Long expected";
                if (message.generateReceiveRecordThreshold != null && $Object.hasOwnProperty.call(message, "generateReceiveRecordThreshold"))
                    if (!$util.isInteger(message.generateReceiveRecordThreshold) && !(message.generateReceiveRecordThreshold && $util.isInteger(message.generateReceiveRecordThreshold.low) && $util.isInteger(message.generateReceiveRecordThreshold.high)))
                        return "generateReceiveRecordThreshold: integer|Long expected";
                if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired"))
                    if (typeof message.receiverSigRequired !== "boolean")
                        return "receiverSigRequired: boolean expected";
                if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                    let error = $root.proto.Timestamp.verify(message.expirationTime, _depth + 1);
                    if (error)
                        return "expirationTime." + error;
                }
                if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                    let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                    if (error)
                        return "autoRenewPeriod." + error;
                }
                if (message.liveHashes != null && $Object.hasOwnProperty.call(message, "liveHashes")) {
                    if (!$Array.isArray(message.liveHashes))
                        return "liveHashes: array expected";
                    for (let i = 0; i < message.liveHashes.length; ++i) {
                        let error = $root.proto.LiveHash.verify(message.liveHashes[i], _depth + 1);
                        if (error)
                            return "liveHashes." + error;
                    }
                }
                if (message.tokenRelationships != null && $Object.hasOwnProperty.call(message, "tokenRelationships")) {
                    if (!$Array.isArray(message.tokenRelationships))
                        return "tokenRelationships: array expected";
                    for (let i = 0; i < message.tokenRelationships.length; ++i) {
                        let error = $root.proto.TokenRelationship.verify(message.tokenRelationships[i], _depth + 1);
                        if (error)
                            return "tokenRelationships." + error;
                    }
                }
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                    if (!$util.isString(message.memo))
                        return "memo: string expected";
                if (message.ownedNfts != null && $Object.hasOwnProperty.call(message, "ownedNfts"))
                    if (!$util.isInteger(message.ownedNfts) && !(message.ownedNfts && $util.isInteger(message.ownedNfts.low) && $util.isInteger(message.ownedNfts.high)))
                        return "ownedNfts: integer|Long expected";
                if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                    if (!$util.isInteger(message.maxAutomaticTokenAssociations))
                        return "maxAutomaticTokenAssociations: integer expected";
                if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                    if (!(message.alias && typeof message.alias.length === "number" || $util.isString(message.alias)))
                        return "alias: buffer expected";
                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                    if (!(message.ledgerId && typeof message.ledgerId.length === "number" || $util.isString(message.ledgerId)))
                        return "ledgerId: buffer expected";
                if (message.ethereumNonce != null && $Object.hasOwnProperty.call(message, "ethereumNonce"))
                    if (!$util.isInteger(message.ethereumNonce) && !(message.ethereumNonce && $util.isInteger(message.ethereumNonce.low) && $util.isInteger(message.ethereumNonce.high)))
                        return "ethereumNonce: integer|Long expected";
                if (message.stakingInfo != null && $Object.hasOwnProperty.call(message, "stakingInfo")) {
                    let error = $root.proto.StakingInfo.verify(message.stakingInfo, _depth + 1);
                    if (error)
                        return "stakingInfo." + error;
                }
                if (message.delegationAddress != null && $Object.hasOwnProperty.call(message, "delegationAddress"))
                    if (!(message.delegationAddress && typeof message.delegationAddress.length === "number" || $util.isString(message.delegationAddress)))
                        return "delegationAddress: buffer expected";
                return null;
            };

            /**
             * Creates an AccountInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.CryptoGetInfoResponse.AccountInfo} AccountInfo
             */
            AccountInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.proto.CryptoGetInfoResponse.AccountInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".proto.CryptoGetInfoResponse.AccountInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.proto.CryptoGetInfoResponse.AccountInfo();
                if (object.accountID != null) {
                    if (!$util.isObject(object.accountID))
                        throw $TypeError(".proto.CryptoGetInfoResponse.AccountInfo.accountID: object expected");
                    message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
                }
                if (object.contractAccountID != null)
                    if (typeof object.contractAccountID !== "string" || object.contractAccountID.length)
                        message.contractAccountID = $String(object.contractAccountID);
                if (object.deleted != null)
                    if (object.deleted)
                        message.deleted = $Boolean(object.deleted);
                if (object.proxyAccountID != null) {
                    if (!$util.isObject(object.proxyAccountID))
                        throw $TypeError(".proto.CryptoGetInfoResponse.AccountInfo.proxyAccountID: object expected");
                    message.proxyAccountID = $root.proto.AccountID.fromObject(object.proxyAccountID, _depth + 1);
                }
                if (object.proxyReceived != null)
                    if (typeof object.proxyReceived === "object" ? object.proxyReceived.low || object.proxyReceived.high : $Number(object.proxyReceived) !== 0)
                        if ($util.Long)
                            message.proxyReceived = $util.Long.fromValue(object.proxyReceived, false);
                        else if (typeof object.proxyReceived === "string")
                            message.proxyReceived = $parseInt(object.proxyReceived, 10);
                        else if (typeof object.proxyReceived === "number")
                            message.proxyReceived = object.proxyReceived;
                        else if (typeof object.proxyReceived === "object")
                            message.proxyReceived = new $util.LongBits(object.proxyReceived.low >>> 0, object.proxyReceived.high >>> 0).toNumber();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw $TypeError(".proto.CryptoGetInfoResponse.AccountInfo.key: object expected");
                    message.key = $root.proto.Key.fromObject(object.key, _depth + 1);
                }
                if (object.balance != null)
                    if (typeof object.balance === "object" ? object.balance.low || object.balance.high : $Number(object.balance) !== 0)
                        if ($util.Long)
                            message.balance = $util.Long.fromValue(object.balance, true);
                        else if (typeof object.balance === "string")
                            message.balance = $parseInt(object.balance, 10);
                        else if (typeof object.balance === "number")
                            message.balance = object.balance;
                        else if (typeof object.balance === "object")
                            message.balance = new $util.LongBits(object.balance.low >>> 0, object.balance.high >>> 0).toNumber(true);
                if (object.generateSendRecordThreshold != null)
                    if (typeof object.generateSendRecordThreshold === "object" ? object.generateSendRecordThreshold.low || object.generateSendRecordThreshold.high : $Number(object.generateSendRecordThreshold) !== 0)
                        if ($util.Long)
                            message.generateSendRecordThreshold = $util.Long.fromValue(object.generateSendRecordThreshold, true);
                        else if (typeof object.generateSendRecordThreshold === "string")
                            message.generateSendRecordThreshold = $parseInt(object.generateSendRecordThreshold, 10);
                        else if (typeof object.generateSendRecordThreshold === "number")
                            message.generateSendRecordThreshold = object.generateSendRecordThreshold;
                        else if (typeof object.generateSendRecordThreshold === "object")
                            message.generateSendRecordThreshold = new $util.LongBits(object.generateSendRecordThreshold.low >>> 0, object.generateSendRecordThreshold.high >>> 0).toNumber(true);
                if (object.generateReceiveRecordThreshold != null)
                    if (typeof object.generateReceiveRecordThreshold === "object" ? object.generateReceiveRecordThreshold.low || object.generateReceiveRecordThreshold.high : $Number(object.generateReceiveRecordThreshold) !== 0)
                        if ($util.Long)
                            message.generateReceiveRecordThreshold = $util.Long.fromValue(object.generateReceiveRecordThreshold, true);
                        else if (typeof object.generateReceiveRecordThreshold === "string")
                            message.generateReceiveRecordThreshold = $parseInt(object.generateReceiveRecordThreshold, 10);
                        else if (typeof object.generateReceiveRecordThreshold === "number")
                            message.generateReceiveRecordThreshold = object.generateReceiveRecordThreshold;
                        else if (typeof object.generateReceiveRecordThreshold === "object")
                            message.generateReceiveRecordThreshold = new $util.LongBits(object.generateReceiveRecordThreshold.low >>> 0, object.generateReceiveRecordThreshold.high >>> 0).toNumber(true);
                if (object.receiverSigRequired != null)
                    if (object.receiverSigRequired)
                        message.receiverSigRequired = $Boolean(object.receiverSigRequired);
                if (object.expirationTime != null) {
                    if (!$util.isObject(object.expirationTime))
                        throw $TypeError(".proto.CryptoGetInfoResponse.AccountInfo.expirationTime: object expected");
                    message.expirationTime = $root.proto.Timestamp.fromObject(object.expirationTime, _depth + 1);
                }
                if (object.autoRenewPeriod != null) {
                    if (!$util.isObject(object.autoRenewPeriod))
                        throw $TypeError(".proto.CryptoGetInfoResponse.AccountInfo.autoRenewPeriod: object expected");
                    message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
                }
                if (object.liveHashes) {
                    if (!$Array.isArray(object.liveHashes))
                        throw $TypeError(".proto.CryptoGetInfoResponse.AccountInfo.liveHashes: array expected");
                    message.liveHashes = $Array(object.liveHashes.length);
                    for (let i = 0; i < object.liveHashes.length; ++i) {
                        if (!$util.isObject(object.liveHashes[i]))
                            throw $TypeError(".proto.CryptoGetInfoResponse.AccountInfo.liveHashes: object expected");
                        message.liveHashes[i] = $root.proto.LiveHash.fromObject(object.liveHashes[i], _depth + 1);
                    }
                }
                if (object.tokenRelationships) {
                    if (!$Array.isArray(object.tokenRelationships))
                        throw $TypeError(".proto.CryptoGetInfoResponse.AccountInfo.tokenRelationships: array expected");
                    message.tokenRelationships = $Array(object.tokenRelationships.length);
                    for (let i = 0; i < object.tokenRelationships.length; ++i) {
                        if (!$util.isObject(object.tokenRelationships[i]))
                            throw $TypeError(".proto.CryptoGetInfoResponse.AccountInfo.tokenRelationships: object expected");
                        message.tokenRelationships[i] = $root.proto.TokenRelationship.fromObject(object.tokenRelationships[i], _depth + 1);
                    }
                }
                if (object.memo != null)
                    if (typeof object.memo !== "string" || object.memo.length)
                        message.memo = $String(object.memo);
                if (object.ownedNfts != null)
                    if (typeof object.ownedNfts === "object" ? object.ownedNfts.low || object.ownedNfts.high : $Number(object.ownedNfts) !== 0)
                        if ($util.Long)
                            message.ownedNfts = $util.Long.fromValue(object.ownedNfts, false);
                        else if (typeof object.ownedNfts === "string")
                            message.ownedNfts = $parseInt(object.ownedNfts, 10);
                        else if (typeof object.ownedNfts === "number")
                            message.ownedNfts = object.ownedNfts;
                        else if (typeof object.ownedNfts === "object")
                            message.ownedNfts = new $util.LongBits(object.ownedNfts.low >>> 0, object.ownedNfts.high >>> 0).toNumber();
                if (object.maxAutomaticTokenAssociations != null)
                    if ($Number(object.maxAutomaticTokenAssociations) !== 0)
                        message.maxAutomaticTokenAssociations = object.maxAutomaticTokenAssociations | 0;
                if (object.alias != null)
                    if (object.alias.length)
                        if (typeof object.alias === "string")
                            $util.base64.decode(object.alias, message.alias = $util.newBuffer($util.base64.length(object.alias)), 0);
                        else if (object.alias.length >= 0)
                            message.alias = object.alias;
                if (object.ledgerId != null)
                    if (object.ledgerId.length)
                        if (typeof object.ledgerId === "string")
                            $util.base64.decode(object.ledgerId, message.ledgerId = $util.newBuffer($util.base64.length(object.ledgerId)), 0);
                        else if (object.ledgerId.length >= 0)
                            message.ledgerId = object.ledgerId;
                if (object.ethereumNonce != null)
                    if (typeof object.ethereumNonce === "object" ? object.ethereumNonce.low || object.ethereumNonce.high : $Number(object.ethereumNonce) !== 0)
                        if ($util.Long)
                            message.ethereumNonce = $util.Long.fromValue(object.ethereumNonce, false);
                        else if (typeof object.ethereumNonce === "string")
                            message.ethereumNonce = $parseInt(object.ethereumNonce, 10);
                        else if (typeof object.ethereumNonce === "number")
                            message.ethereumNonce = object.ethereumNonce;
                        else if (typeof object.ethereumNonce === "object")
                            message.ethereumNonce = new $util.LongBits(object.ethereumNonce.low >>> 0, object.ethereumNonce.high >>> 0).toNumber();
                if (object.stakingInfo != null) {
                    if (!$util.isObject(object.stakingInfo))
                        throw $TypeError(".proto.CryptoGetInfoResponse.AccountInfo.stakingInfo: object expected");
                    message.stakingInfo = $root.proto.StakingInfo.fromObject(object.stakingInfo, _depth + 1);
                }
                if (object.delegationAddress != null)
                    if (object.delegationAddress.length)
                        if (typeof object.delegationAddress === "string")
                            $util.base64.decode(object.delegationAddress, message.delegationAddress = $util.newBuffer($util.base64.length(object.delegationAddress)), 0);
                        else if (object.delegationAddress.length >= 0)
                            message.delegationAddress = object.delegationAddress;
                return message;
            };

            /**
             * Creates a plain object from an AccountInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @static
             * @param {proto.CryptoGetInfoResponse.AccountInfo} message AccountInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AccountInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.liveHashes = [];
                    object.tokenRelationships = [];
                }
                if (options.defaults) {
                    object.accountID = null;
                    object.contractAccountID = "";
                    object.deleted = false;
                    object.proxyAccountID = null;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.proxyReceived = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.proxyReceived = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.key = null;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.generateSendRecordThreshold = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.generateSendRecordThreshold = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.generateReceiveRecordThreshold = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.generateReceiveRecordThreshold = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.receiverSigRequired = false;
                    object.expirationTime = null;
                    object.autoRenewPeriod = null;
                    object.memo = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.ownedNfts = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.ownedNfts = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.maxAutomaticTokenAssociations = 0;
                    if (options.bytes === $String)
                        object.alias = "";
                    else {
                        object.alias = [];
                        if (options.bytes !== $Array)
                            object.alias = $util.newBuffer(object.alias);
                    }
                    if (options.bytes === $String)
                        object.ledgerId = "";
                    else {
                        object.ledgerId = [];
                        if (options.bytes !== $Array)
                            object.ledgerId = $util.newBuffer(object.ledgerId);
                    }
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.ethereumNonce = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.ethereumNonce = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.stakingInfo = null;
                    if (options.bytes === $String)
                        object.delegationAddress = "";
                    else {
                        object.delegationAddress = [];
                        if (options.bytes !== $Array)
                            object.delegationAddress = $util.newBuffer(object.delegationAddress);
                    }
                }
                if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                    object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
                if (message.contractAccountID != null && $Object.hasOwnProperty.call(message, "contractAccountID"))
                    object.contractAccountID = message.contractAccountID;
                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                    object.deleted = message.deleted;
                if (message.proxyAccountID != null && $Object.hasOwnProperty.call(message, "proxyAccountID"))
                    object.proxyAccountID = $root.proto.AccountID.toObject(message.proxyAccountID, options, _depth + 1);
                if (message.proxyReceived != null && $Object.hasOwnProperty.call(message, "proxyReceived"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.proxyReceived = typeof message.proxyReceived === "number" ? $BigInt(message.proxyReceived) : $util.Long.fromBits(message.proxyReceived.low >>> 0, message.proxyReceived.high >>> 0, false).toBigInt();
                    else if (typeof message.proxyReceived === "number")
                        object.proxyReceived = options.longs === $String ? $String(message.proxyReceived) : message.proxyReceived;
                    else
                        object.proxyReceived = options.longs === $String ? $util.Long.prototype.toString.call(message.proxyReceived) : options.longs === $Number ? new $util.LongBits(message.proxyReceived.low >>> 0, message.proxyReceived.high >>> 0).toNumber() : message.proxyReceived;
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.Key.toObject(message.key, options, _depth + 1);
                if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.balance = typeof message.balance === "number" ? $BigInt(message.balance) : $util.Long.fromBits(message.balance.low >>> 0, message.balance.high >>> 0, true).toBigInt();
                    else if (typeof message.balance === "number")
                        object.balance = options.longs === $String ? $String(message.balance) : message.balance;
                    else
                        object.balance = options.longs === $String ? $util.Long.prototype.toString.call(message.balance) : options.longs === $Number ? new $util.LongBits(message.balance.low >>> 0, message.balance.high >>> 0).toNumber(true) : message.balance;
                if (message.generateSendRecordThreshold != null && $Object.hasOwnProperty.call(message, "generateSendRecordThreshold"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.generateSendRecordThreshold = typeof message.generateSendRecordThreshold === "number" ? $BigInt(message.generateSendRecordThreshold) : $util.Long.fromBits(message.generateSendRecordThreshold.low >>> 0, message.generateSendRecordThreshold.high >>> 0, true).toBigInt();
                    else if (typeof message.generateSendRecordThreshold === "number")
                        object.generateSendRecordThreshold = options.longs === $String ? $String(message.generateSendRecordThreshold) : message.generateSendRecordThreshold;
                    else
                        object.generateSendRecordThreshold = options.longs === $String ? $util.Long.prototype.toString.call(message.generateSendRecordThreshold) : options.longs === $Number ? new $util.LongBits(message.generateSendRecordThreshold.low >>> 0, message.generateSendRecordThreshold.high >>> 0).toNumber(true) : message.generateSendRecordThreshold;
                if (message.generateReceiveRecordThreshold != null && $Object.hasOwnProperty.call(message, "generateReceiveRecordThreshold"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.generateReceiveRecordThreshold = typeof message.generateReceiveRecordThreshold === "number" ? $BigInt(message.generateReceiveRecordThreshold) : $util.Long.fromBits(message.generateReceiveRecordThreshold.low >>> 0, message.generateReceiveRecordThreshold.high >>> 0, true).toBigInt();
                    else if (typeof message.generateReceiveRecordThreshold === "number")
                        object.generateReceiveRecordThreshold = options.longs === $String ? $String(message.generateReceiveRecordThreshold) : message.generateReceiveRecordThreshold;
                    else
                        object.generateReceiveRecordThreshold = options.longs === $String ? $util.Long.prototype.toString.call(message.generateReceiveRecordThreshold) : options.longs === $Number ? new $util.LongBits(message.generateReceiveRecordThreshold.low >>> 0, message.generateReceiveRecordThreshold.high >>> 0).toNumber(true) : message.generateReceiveRecordThreshold;
                if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired"))
                    object.receiverSigRequired = message.receiverSigRequired;
                if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                    object.expirationTime = $root.proto.Timestamp.toObject(message.expirationTime, options, _depth + 1);
                if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                    object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
                if (message.liveHashes && message.liveHashes.length) {
                    object.liveHashes = $Array(message.liveHashes.length);
                    for (let j = 0; j < message.liveHashes.length; ++j)
                        object.liveHashes[j] = $root.proto.LiveHash.toObject(message.liveHashes[j], options, _depth + 1);
                }
                if (message.tokenRelationships && message.tokenRelationships.length) {
                    object.tokenRelationships = $Array(message.tokenRelationships.length);
                    for (let j = 0; j < message.tokenRelationships.length; ++j)
                        object.tokenRelationships[j] = $root.proto.TokenRelationship.toObject(message.tokenRelationships[j], options, _depth + 1);
                }
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                    object.memo = message.memo;
                if (message.ownedNfts != null && $Object.hasOwnProperty.call(message, "ownedNfts"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.ownedNfts = typeof message.ownedNfts === "number" ? $BigInt(message.ownedNfts) : $util.Long.fromBits(message.ownedNfts.low >>> 0, message.ownedNfts.high >>> 0, false).toBigInt();
                    else if (typeof message.ownedNfts === "number")
                        object.ownedNfts = options.longs === $String ? $String(message.ownedNfts) : message.ownedNfts;
                    else
                        object.ownedNfts = options.longs === $String ? $util.Long.prototype.toString.call(message.ownedNfts) : options.longs === $Number ? new $util.LongBits(message.ownedNfts.low >>> 0, message.ownedNfts.high >>> 0).toNumber() : message.ownedNfts;
                if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                    object.maxAutomaticTokenAssociations = message.maxAutomaticTokenAssociations;
                if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                    object.alias = options.bytes === $String ? $util.base64.encode(message.alias, 0, message.alias.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.alias) : message.alias;
                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                    object.ledgerId = options.bytes === $String ? $util.base64.encode(message.ledgerId, 0, message.ledgerId.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ledgerId) : message.ledgerId;
                if (message.ethereumNonce != null && $Object.hasOwnProperty.call(message, "ethereumNonce"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.ethereumNonce = typeof message.ethereumNonce === "number" ? $BigInt(message.ethereumNonce) : $util.Long.fromBits(message.ethereumNonce.low >>> 0, message.ethereumNonce.high >>> 0, false).toBigInt();
                    else if (typeof message.ethereumNonce === "number")
                        object.ethereumNonce = options.longs === $String ? $String(message.ethereumNonce) : message.ethereumNonce;
                    else
                        object.ethereumNonce = options.longs === $String ? $util.Long.prototype.toString.call(message.ethereumNonce) : options.longs === $Number ? new $util.LongBits(message.ethereumNonce.low >>> 0, message.ethereumNonce.high >>> 0).toNumber() : message.ethereumNonce;
                if (message.stakingInfo != null && $Object.hasOwnProperty.call(message, "stakingInfo"))
                    object.stakingInfo = $root.proto.StakingInfo.toObject(message.stakingInfo, options, _depth + 1);
                if (message.delegationAddress != null && $Object.hasOwnProperty.call(message, "delegationAddress"))
                    object.delegationAddress = options.bytes === $String ? $util.base64.encode(message.delegationAddress, 0, message.delegationAddress.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.delegationAddress) : message.delegationAddress;
                return object;
            };

            /**
             * Converts this AccountInfo to JSON.
             * @function toJSON
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AccountInfo.prototype.toJSON = function() {
                return AccountInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for AccountInfo
             * @function getTypeUrl
             * @memberof proto.CryptoGetInfoResponse.AccountInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            AccountInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.CryptoGetInfoResponse.AccountInfo";
            };

            return AccountInfo;
        })();

        return CryptoGetInfoResponse;
    })();

    proto.CryptoGetLiveHashQuery = (function() {

        /**
         * Properties of a CryptoGetLiveHashQuery.
         * @typedef {Object} proto.CryptoGetLiveHashQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.AccountID.$Properties|null} [accountID] An account ID.<br/>
         * The network SHALL return live hash information for this account,
         * if successful.
         * @property {Uint8Array|null} [hash] The specific SHA-384 live hash to inspect
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoGetLiveHashQuery.
         * @memberof proto
         * @interface ICryptoGetLiveHashQuery
         * @augments proto.CryptoGetLiveHashQuery.$Properties
         * @deprecated Use proto.CryptoGetLiveHashQuery.$Properties instead.
         */

        /**
         * Shape of a CryptoGetLiveHashQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   accountID?: proto.AccountID.$Shape|null;
         *   hash?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoGetLiveHashQuery.$Shape
         */

        /**
         * Constructs a new CryptoGetLiveHashQuery.
         * @memberof proto
         * @classdesc Request detail for a specific live hash associated to a specific account.
         * @constructor
         * @param {proto.CryptoGetLiveHashQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoGetLiveHashQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.CryptoGetLiveHashQuery
         * @instance
         */
        CryptoGetLiveHashQuery.prototype.header = null;

        /**
         * An account ID.<br/>
         * The network SHALL return live hash information for this account,
         * if successful.
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.CryptoGetLiveHashQuery
         * @instance
         */
        CryptoGetLiveHashQuery.prototype.accountID = null;

        /**
         * The specific SHA-384 live hash to inspect
         * @member {Uint8Array} hash
         * @memberof proto.CryptoGetLiveHashQuery
         * @instance
         */
        CryptoGetLiveHashQuery.prototype.hash = $util.newBuffer([]);

        /**
         * Creates a new CryptoGetLiveHashQuery instance using the specified properties.
         * @function create
         * @memberof proto.CryptoGetLiveHashQuery
         * @static
         * @param {proto.CryptoGetLiveHashQuery.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoGetLiveHashQuery} CryptoGetLiveHashQuery instance
         * @type {{
         *   (properties: proto.CryptoGetLiveHashQuery.$Shape): proto.CryptoGetLiveHashQuery & proto.CryptoGetLiveHashQuery.$Shape;
         *   (properties?: proto.CryptoGetLiveHashQuery.$Properties): proto.CryptoGetLiveHashQuery;
         * }}
         */
        CryptoGetLiveHashQuery.create = function(properties) {
            return new CryptoGetLiveHashQuery(properties);
        };

        /**
         * Encodes the specified CryptoGetLiveHashQuery message. Does not implicitly {@link proto.CryptoGetLiveHashQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoGetLiveHashQuery
         * @static
         * @param {proto.CryptoGetLiveHashQuery.$Properties} message CryptoGetLiveHashQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetLiveHashQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.hash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoGetLiveHashQuery message, length delimited. Does not implicitly {@link proto.CryptoGetLiveHashQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoGetLiveHashQuery
         * @static
         * @param {proto.CryptoGetLiveHashQuery.$Properties} message CryptoGetLiveHashQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetLiveHashQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoGetLiveHashQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoGetLiveHashQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoGetLiveHashQuery & proto.CryptoGetLiveHashQuery.$Shape} CryptoGetLiveHashQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetLiveHashQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoGetLiveHashQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoGetLiveHashQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoGetLiveHashQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoGetLiveHashQuery & proto.CryptoGetLiveHashQuery.$Shape} CryptoGetLiveHashQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetLiveHashQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoGetLiveHashQuery message.
         * @function verify
         * @memberof proto.CryptoGetLiveHashQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoGetLiveHashQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                    return "hash: buffer expected";
            return null;
        };

        /**
         * Creates a CryptoGetLiveHashQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoGetLiveHashQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoGetLiveHashQuery} CryptoGetLiveHashQuery
         */
        CryptoGetLiveHashQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoGetLiveHashQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoGetLiveHashQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoGetLiveHashQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.CryptoGetLiveHashQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.CryptoGetLiveHashQuery.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.hash != null)
                if (object.hash.length)
                    if (typeof object.hash === "string")
                        $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                    else if (object.hash.length >= 0)
                        message.hash = object.hash;
            return message;
        };

        /**
         * Creates a plain object from a CryptoGetLiveHashQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoGetLiveHashQuery
         * @static
         * @param {proto.CryptoGetLiveHashQuery} message CryptoGetLiveHashQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoGetLiveHashQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.accountID = null;
                if (options.bytes === $String)
                    object.hash = "";
                else {
                    object.hash = [];
                    if (options.bytes !== $Array)
                        object.hash = $util.newBuffer(object.hash);
                }
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = options.bytes === $String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hash) : message.hash;
            return object;
        };

        /**
         * Converts this CryptoGetLiveHashQuery to JSON.
         * @function toJSON
         * @memberof proto.CryptoGetLiveHashQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoGetLiveHashQuery.prototype.toJSON = function() {
            return CryptoGetLiveHashQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoGetLiveHashQuery
         * @function getTypeUrl
         * @memberof proto.CryptoGetLiveHashQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoGetLiveHashQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoGetLiveHashQuery";
        };

        return CryptoGetLiveHashQuery;
    })();

    proto.CryptoGetLiveHashResponse = (function() {

        /**
         * Properties of a CryptoGetLiveHashResponse.
         * @typedef {Object} proto.CryptoGetLiveHashResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.LiveHash.$Properties|null} [liveHash] The requested live hash, if found.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoGetLiveHashResponse.
         * @memberof proto
         * @interface ICryptoGetLiveHashResponse
         * @augments proto.CryptoGetLiveHashResponse.$Properties
         * @deprecated Use proto.CryptoGetLiveHashResponse.$Properties instead.
         */

        /**
         * Shape of a CryptoGetLiveHashResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   liveHash?: proto.LiveHash.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoGetLiveHashResponse.$Shape
         */

        /**
         * Constructs a new CryptoGetLiveHashResponse.
         * @memberof proto
         * @classdesc Return the full live hash associated to an account, if it is present.
         * 
         * > Note that to generate a state proof of the _absence_ of a live hash from
         * > an account a transaction MUST retrieve a state proof of the `Account`
         * > with its list of live hashes.
         * @constructor
         * @param {proto.CryptoGetLiveHashResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoGetLiveHashResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.CryptoGetLiveHashResponse
         * @instance
         */
        CryptoGetLiveHashResponse.prototype.header = null;

        /**
         * The requested live hash, if found.
         * @member {proto.LiveHash.$Properties|null|undefined} liveHash
         * @memberof proto.CryptoGetLiveHashResponse
         * @instance
         */
        CryptoGetLiveHashResponse.prototype.liveHash = null;

        /**
         * Creates a new CryptoGetLiveHashResponse instance using the specified properties.
         * @function create
         * @memberof proto.CryptoGetLiveHashResponse
         * @static
         * @param {proto.CryptoGetLiveHashResponse.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoGetLiveHashResponse} CryptoGetLiveHashResponse instance
         * @type {{
         *   (properties: proto.CryptoGetLiveHashResponse.$Shape): proto.CryptoGetLiveHashResponse & proto.CryptoGetLiveHashResponse.$Shape;
         *   (properties?: proto.CryptoGetLiveHashResponse.$Properties): proto.CryptoGetLiveHashResponse;
         * }}
         */
        CryptoGetLiveHashResponse.create = function(properties) {
            return new CryptoGetLiveHashResponse(properties);
        };

        /**
         * Encodes the specified CryptoGetLiveHashResponse message. Does not implicitly {@link proto.CryptoGetLiveHashResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoGetLiveHashResponse
         * @static
         * @param {proto.CryptoGetLiveHashResponse.$Properties} message CryptoGetLiveHashResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetLiveHashResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.liveHash != null && $Object.hasOwnProperty.call(message, "liveHash"))
                $root.proto.LiveHash.encode(message.liveHash, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoGetLiveHashResponse message, length delimited. Does not implicitly {@link proto.CryptoGetLiveHashResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoGetLiveHashResponse
         * @static
         * @param {proto.CryptoGetLiveHashResponse.$Properties} message CryptoGetLiveHashResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetLiveHashResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoGetLiveHashResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoGetLiveHashResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoGetLiveHashResponse & proto.CryptoGetLiveHashResponse.$Shape} CryptoGetLiveHashResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetLiveHashResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoGetLiveHashResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.liveHash = $root.proto.LiveHash.decode(reader, reader.uint32(), $undefined, _depth + 1, message.liveHash);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoGetLiveHashResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoGetLiveHashResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoGetLiveHashResponse & proto.CryptoGetLiveHashResponse.$Shape} CryptoGetLiveHashResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetLiveHashResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoGetLiveHashResponse message.
         * @function verify
         * @memberof proto.CryptoGetLiveHashResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoGetLiveHashResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.liveHash != null && $Object.hasOwnProperty.call(message, "liveHash")) {
                let error = $root.proto.LiveHash.verify(message.liveHash, _depth + 1);
                if (error)
                    return "liveHash." + error;
            }
            return null;
        };

        /**
         * Creates a CryptoGetLiveHashResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoGetLiveHashResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoGetLiveHashResponse} CryptoGetLiveHashResponse
         */
        CryptoGetLiveHashResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoGetLiveHashResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoGetLiveHashResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoGetLiveHashResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.CryptoGetLiveHashResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.liveHash != null) {
                if (!$util.isObject(object.liveHash))
                    throw $TypeError(".proto.CryptoGetLiveHashResponse.liveHash: object expected");
                message.liveHash = $root.proto.LiveHash.fromObject(object.liveHash, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoGetLiveHashResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoGetLiveHashResponse
         * @static
         * @param {proto.CryptoGetLiveHashResponse} message CryptoGetLiveHashResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoGetLiveHashResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.liveHash = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.liveHash != null && $Object.hasOwnProperty.call(message, "liveHash"))
                object.liveHash = $root.proto.LiveHash.toObject(message.liveHash, options, _depth + 1);
            return object;
        };

        /**
         * Converts this CryptoGetLiveHashResponse to JSON.
         * @function toJSON
         * @memberof proto.CryptoGetLiveHashResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoGetLiveHashResponse.prototype.toJSON = function() {
            return CryptoGetLiveHashResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoGetLiveHashResponse
         * @function getTypeUrl
         * @memberof proto.CryptoGetLiveHashResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoGetLiveHashResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoGetLiveHashResponse";
        };

        return CryptoGetLiveHashResponse;
    })();

    proto.CryptoGetStakersQuery = (function() {

        /**
         * Properties of a CryptoGetStakersQuery.
         * @typedef {Object} proto.CryptoGetStakersQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.AccountID.$Properties|null} [accountID] The Account ID for which the records should be retrieved
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoGetStakersQuery.
         * @memberof proto
         * @interface ICryptoGetStakersQuery
         * @augments proto.CryptoGetStakersQuery.$Properties
         * @deprecated Use proto.CryptoGetStakersQuery.$Properties instead.
         */

        /**
         * Shape of a CryptoGetStakersQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   accountID?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoGetStakersQuery.$Shape
         */

        /**
         * Constructs a new CryptoGetStakersQuery.
         * @memberof proto
         * @classdesc Get all the accounts that are proxy staking to this account. For each of
         * them, give the amount currently staked. This was never implemented.
         * @constructor
         * @param {proto.CryptoGetStakersQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoGetStakersQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.CryptoGetStakersQuery
         * @instance
         */
        CryptoGetStakersQuery.prototype.header = null;

        /**
         * The Account ID for which the records should be retrieved
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.CryptoGetStakersQuery
         * @instance
         */
        CryptoGetStakersQuery.prototype.accountID = null;

        /**
         * Creates a new CryptoGetStakersQuery instance using the specified properties.
         * @function create
         * @memberof proto.CryptoGetStakersQuery
         * @static
         * @param {proto.CryptoGetStakersQuery.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoGetStakersQuery} CryptoGetStakersQuery instance
         * @type {{
         *   (properties: proto.CryptoGetStakersQuery.$Shape): proto.CryptoGetStakersQuery & proto.CryptoGetStakersQuery.$Shape;
         *   (properties?: proto.CryptoGetStakersQuery.$Properties): proto.CryptoGetStakersQuery;
         * }}
         */
        CryptoGetStakersQuery.create = function(properties) {
            return new CryptoGetStakersQuery(properties);
        };

        /**
         * Encodes the specified CryptoGetStakersQuery message. Does not implicitly {@link proto.CryptoGetStakersQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoGetStakersQuery
         * @static
         * @param {proto.CryptoGetStakersQuery.$Properties} message CryptoGetStakersQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetStakersQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoGetStakersQuery message, length delimited. Does not implicitly {@link proto.CryptoGetStakersQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoGetStakersQuery
         * @static
         * @param {proto.CryptoGetStakersQuery.$Properties} message CryptoGetStakersQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetStakersQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoGetStakersQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoGetStakersQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoGetStakersQuery & proto.CryptoGetStakersQuery.$Shape} CryptoGetStakersQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetStakersQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoGetStakersQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoGetStakersQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoGetStakersQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoGetStakersQuery & proto.CryptoGetStakersQuery.$Shape} CryptoGetStakersQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetStakersQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoGetStakersQuery message.
         * @function verify
         * @memberof proto.CryptoGetStakersQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoGetStakersQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            return null;
        };

        /**
         * Creates a CryptoGetStakersQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoGetStakersQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoGetStakersQuery} CryptoGetStakersQuery
         */
        CryptoGetStakersQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoGetStakersQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoGetStakersQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoGetStakersQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.CryptoGetStakersQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.CryptoGetStakersQuery.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoGetStakersQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoGetStakersQuery
         * @static
         * @param {proto.CryptoGetStakersQuery} message CryptoGetStakersQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoGetStakersQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.accountID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this CryptoGetStakersQuery to JSON.
         * @function toJSON
         * @memberof proto.CryptoGetStakersQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoGetStakersQuery.prototype.toJSON = function() {
            return CryptoGetStakersQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoGetStakersQuery
         * @function getTypeUrl
         * @memberof proto.CryptoGetStakersQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoGetStakersQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoGetStakersQuery";
        };

        return CryptoGetStakersQuery;
    })();

    proto.ProxyStaker = (function() {

        /**
         * Properties of a ProxyStaker.
         * @typedef {Object} proto.ProxyStaker.$Properties
         * @property {proto.AccountID.$Properties|null} [accountID] The Account ID that is proxy staking
         * @property {number|Long|null} [amount] The number of hbars that are currently proxy staked
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ProxyStaker.
         * @memberof proto
         * @interface IProxyStaker
         * @augments proto.ProxyStaker.$Properties
         * @deprecated Use proto.ProxyStaker.$Properties instead.
         */

        /**
         * Shape of a ProxyStaker.
         * @typedef {{
         *   accountID?: proto.AccountID.$Shape|null;
         *   amount?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ProxyStaker.$Shape
         */

        /**
         * Constructs a new ProxyStaker.
         * @memberof proto
         * @classdesc information about a single account that is proxy staking
         * @constructor
         * @param {proto.ProxyStaker.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ProxyStaker = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The Account ID that is proxy staking
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.ProxyStaker
         * @instance
         */
        ProxyStaker.prototype.accountID = null;

        /**
         * The number of hbars that are currently proxy staked
         * @member {number|Long} amount
         * @memberof proto.ProxyStaker
         * @instance
         */
        ProxyStaker.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ProxyStaker instance using the specified properties.
         * @function create
         * @memberof proto.ProxyStaker
         * @static
         * @param {proto.ProxyStaker.$Properties=} [properties] Properties to set
         * @returns {proto.ProxyStaker} ProxyStaker instance
         * @type {{
         *   (properties: proto.ProxyStaker.$Shape): proto.ProxyStaker & proto.ProxyStaker.$Shape;
         *   (properties?: proto.ProxyStaker.$Properties): proto.ProxyStaker;
         * }}
         */
        ProxyStaker.create = function(properties) {
            return new ProxyStaker(properties);
        };

        /**
         * Encodes the specified ProxyStaker message. Does not implicitly {@link proto.ProxyStaker.verify|verify} messages.
         * @function encode
         * @memberof proto.ProxyStaker
         * @static
         * @param {proto.ProxyStaker.$Properties} message ProxyStaker message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProxyStaker.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.amount);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ProxyStaker message, length delimited. Does not implicitly {@link proto.ProxyStaker.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ProxyStaker
         * @static
         * @param {proto.ProxyStaker.$Properties} message ProxyStaker message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProxyStaker.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ProxyStaker message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ProxyStaker
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ProxyStaker & proto.ProxyStaker.$Shape} ProxyStaker
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProxyStaker.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ProxyStaker();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ProxyStaker message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ProxyStaker
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ProxyStaker & proto.ProxyStaker.$Shape} ProxyStaker
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProxyStaker.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ProxyStaker message.
         * @function verify
         * @memberof proto.ProxyStaker
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProxyStaker.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            return null;
        };

        /**
         * Creates a ProxyStaker message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ProxyStaker
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ProxyStaker} ProxyStaker
         */
        ProxyStaker.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ProxyStaker)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ProxyStaker: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ProxyStaker();
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.ProxyStaker.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ProxyStaker message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ProxyStaker
         * @static
         * @param {proto.ProxyStaker} message ProxyStaker
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ProxyStaker.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.accountID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            return object;
        };

        /**
         * Converts this ProxyStaker to JSON.
         * @function toJSON
         * @memberof proto.ProxyStaker
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ProxyStaker.prototype.toJSON = function() {
            return ProxyStaker.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ProxyStaker
         * @function getTypeUrl
         * @memberof proto.ProxyStaker
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ProxyStaker.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ProxyStaker";
        };

        return ProxyStaker;
    })();

    proto.AllProxyStakers = (function() {

        /**
         * Properties of an AllProxyStakers.
         * @typedef {Object} proto.AllProxyStakers.$Properties
         * @property {proto.AccountID.$Properties|null} [accountID] The Account ID that is being proxy staked to
         * @property {Array.<proto.ProxyStaker.$Properties>|null} [proxyStaker] Each of the proxy staking accounts, and the amount they are proxy staking
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AllProxyStakers.
         * @memberof proto
         * @interface IAllProxyStakers
         * @augments proto.AllProxyStakers.$Properties
         * @deprecated Use proto.AllProxyStakers.$Properties instead.
         */

        /**
         * Shape of an AllProxyStakers.
         * @typedef {{
         *   accountID?: proto.AccountID.$Shape|null;
         *   proxyStaker?: Array.<proto.ProxyStaker.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.AllProxyStakers.$Shape
         */

        /**
         * Constructs a new AllProxyStakers.
         * @memberof proto
         * @classdesc All of the accounts proxy staking to a given account, and the amounts proxy
         * staked
         * @constructor
         * @param {proto.AllProxyStakers.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AllProxyStakers = function (properties) {
            this.proxyStaker = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The Account ID that is being proxy staked to
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.AllProxyStakers
         * @instance
         */
        AllProxyStakers.prototype.accountID = null;

        /**
         * Each of the proxy staking accounts, and the amount they are proxy staking
         * @member {Array.<proto.ProxyStaker.$Properties>} proxyStaker
         * @memberof proto.AllProxyStakers
         * @instance
         */
        AllProxyStakers.prototype.proxyStaker = $util.emptyArray;

        /**
         * Creates a new AllProxyStakers instance using the specified properties.
         * @function create
         * @memberof proto.AllProxyStakers
         * @static
         * @param {proto.AllProxyStakers.$Properties=} [properties] Properties to set
         * @returns {proto.AllProxyStakers} AllProxyStakers instance
         * @type {{
         *   (properties: proto.AllProxyStakers.$Shape): proto.AllProxyStakers & proto.AllProxyStakers.$Shape;
         *   (properties?: proto.AllProxyStakers.$Properties): proto.AllProxyStakers;
         * }}
         */
        AllProxyStakers.create = function(properties) {
            return new AllProxyStakers(properties);
        };

        /**
         * Encodes the specified AllProxyStakers message. Does not implicitly {@link proto.AllProxyStakers.verify|verify} messages.
         * @function encode
         * @memberof proto.AllProxyStakers
         * @static
         * @param {proto.AllProxyStakers.$Properties} message AllProxyStakers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllProxyStakers.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.proxyStaker != null && message.proxyStaker.length)
                for (let i = 0; i < message.proxyStaker.length; ++i)
                    $root.proto.ProxyStaker.encode(message.proxyStaker[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AllProxyStakers message, length delimited. Does not implicitly {@link proto.AllProxyStakers.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AllProxyStakers
         * @static
         * @param {proto.AllProxyStakers.$Properties} message AllProxyStakers message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllProxyStakers.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AllProxyStakers message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AllProxyStakers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AllProxyStakers & proto.AllProxyStakers.$Shape} AllProxyStakers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllProxyStakers.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.AllProxyStakers();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.proxyStaker && message.proxyStaker.length))
                            message.proxyStaker = [];
                        message.proxyStaker.push($root.proto.ProxyStaker.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AllProxyStakers message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AllProxyStakers
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AllProxyStakers & proto.AllProxyStakers.$Shape} AllProxyStakers
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllProxyStakers.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllProxyStakers message.
         * @function verify
         * @memberof proto.AllProxyStakers
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllProxyStakers.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            if (message.proxyStaker != null && $Object.hasOwnProperty.call(message, "proxyStaker")) {
                if (!$Array.isArray(message.proxyStaker))
                    return "proxyStaker: array expected";
                for (let i = 0; i < message.proxyStaker.length; ++i) {
                    let error = $root.proto.ProxyStaker.verify(message.proxyStaker[i], _depth + 1);
                    if (error)
                        return "proxyStaker." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllProxyStakers message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AllProxyStakers
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AllProxyStakers} AllProxyStakers
         */
        AllProxyStakers.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.AllProxyStakers)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.AllProxyStakers: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.AllProxyStakers();
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.AllProxyStakers.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.proxyStaker) {
                if (!$Array.isArray(object.proxyStaker))
                    throw $TypeError(".proto.AllProxyStakers.proxyStaker: array expected");
                message.proxyStaker = $Array(object.proxyStaker.length);
                for (let i = 0; i < object.proxyStaker.length; ++i) {
                    if (!$util.isObject(object.proxyStaker[i]))
                        throw $TypeError(".proto.AllProxyStakers.proxyStaker: object expected");
                    message.proxyStaker[i] = $root.proto.ProxyStaker.fromObject(object.proxyStaker[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllProxyStakers message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AllProxyStakers
         * @static
         * @param {proto.AllProxyStakers} message AllProxyStakers
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllProxyStakers.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.proxyStaker = [];
            if (options.defaults)
                object.accountID = null;
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            if (message.proxyStaker && message.proxyStaker.length) {
                object.proxyStaker = $Array(message.proxyStaker.length);
                for (let j = 0; j < message.proxyStaker.length; ++j)
                    object.proxyStaker[j] = $root.proto.ProxyStaker.toObject(message.proxyStaker[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this AllProxyStakers to JSON.
         * @function toJSON
         * @memberof proto.AllProxyStakers
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllProxyStakers.prototype.toJSON = function() {
            return AllProxyStakers.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AllProxyStakers
         * @function getTypeUrl
         * @memberof proto.AllProxyStakers
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AllProxyStakers.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AllProxyStakers";
        };

        return AllProxyStakers;
    })();

    proto.CryptoGetStakersResponse = (function() {

        /**
         * Properties of a CryptoGetStakersResponse.
         * @typedef {Object} proto.CryptoGetStakersResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.AllProxyStakers.$Properties|null} [stakers] List of accounts proxy staking to this account, and the amount each is
         * currently proxy staking
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CryptoGetStakersResponse.
         * @memberof proto
         * @interface ICryptoGetStakersResponse
         * @augments proto.CryptoGetStakersResponse.$Properties
         * @deprecated Use proto.CryptoGetStakersResponse.$Properties instead.
         */

        /**
         * Shape of a CryptoGetStakersResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   stakers?: proto.AllProxyStakers.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.CryptoGetStakersResponse.$Shape
         */

        /**
         * Constructs a new CryptoGetStakersResponse.
         * @memberof proto
         * @classdesc Response when the client sends the node CryptoGetStakersQuery
         * @constructor
         * @param {proto.CryptoGetStakersResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CryptoGetStakersResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.CryptoGetStakersResponse
         * @instance
         */
        CryptoGetStakersResponse.prototype.header = null;

        /**
         * List of accounts proxy staking to this account, and the amount each is
         * currently proxy staking
         * @member {proto.AllProxyStakers.$Properties|null|undefined} stakers
         * @memberof proto.CryptoGetStakersResponse
         * @instance
         */
        CryptoGetStakersResponse.prototype.stakers = null;

        /**
         * Creates a new CryptoGetStakersResponse instance using the specified properties.
         * @function create
         * @memberof proto.CryptoGetStakersResponse
         * @static
         * @param {proto.CryptoGetStakersResponse.$Properties=} [properties] Properties to set
         * @returns {proto.CryptoGetStakersResponse} CryptoGetStakersResponse instance
         * @type {{
         *   (properties: proto.CryptoGetStakersResponse.$Shape): proto.CryptoGetStakersResponse & proto.CryptoGetStakersResponse.$Shape;
         *   (properties?: proto.CryptoGetStakersResponse.$Properties): proto.CryptoGetStakersResponse;
         * }}
         */
        CryptoGetStakersResponse.create = function(properties) {
            return new CryptoGetStakersResponse(properties);
        };

        /**
         * Encodes the specified CryptoGetStakersResponse message. Does not implicitly {@link proto.CryptoGetStakersResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.CryptoGetStakersResponse
         * @static
         * @param {proto.CryptoGetStakersResponse.$Properties} message CryptoGetStakersResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetStakersResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.stakers != null && $Object.hasOwnProperty.call(message, "stakers"))
                $root.proto.AllProxyStakers.encode(message.stakers, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CryptoGetStakersResponse message, length delimited. Does not implicitly {@link proto.CryptoGetStakersResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CryptoGetStakersResponse
         * @static
         * @param {proto.CryptoGetStakersResponse.$Properties} message CryptoGetStakersResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CryptoGetStakersResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CryptoGetStakersResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CryptoGetStakersResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CryptoGetStakersResponse & proto.CryptoGetStakersResponse.$Shape} CryptoGetStakersResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetStakersResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CryptoGetStakersResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.stakers = $root.proto.AllProxyStakers.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stakers);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CryptoGetStakersResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CryptoGetStakersResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CryptoGetStakersResponse & proto.CryptoGetStakersResponse.$Shape} CryptoGetStakersResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CryptoGetStakersResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CryptoGetStakersResponse message.
         * @function verify
         * @memberof proto.CryptoGetStakersResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CryptoGetStakersResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.stakers != null && $Object.hasOwnProperty.call(message, "stakers")) {
                let error = $root.proto.AllProxyStakers.verify(message.stakers, _depth + 1);
                if (error)
                    return "stakers." + error;
            }
            return null;
        };

        /**
         * Creates a CryptoGetStakersResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CryptoGetStakersResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CryptoGetStakersResponse} CryptoGetStakersResponse
         */
        CryptoGetStakersResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CryptoGetStakersResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CryptoGetStakersResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CryptoGetStakersResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.CryptoGetStakersResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.stakers != null) {
                if (!$util.isObject(object.stakers))
                    throw $TypeError(".proto.CryptoGetStakersResponse.stakers: object expected");
                message.stakers = $root.proto.AllProxyStakers.fromObject(object.stakers, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CryptoGetStakersResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CryptoGetStakersResponse
         * @static
         * @param {proto.CryptoGetStakersResponse} message CryptoGetStakersResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CryptoGetStakersResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.stakers = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.stakers != null && $Object.hasOwnProperty.call(message, "stakers"))
                object.stakers = $root.proto.AllProxyStakers.toObject(message.stakers, options, _depth + 1);
            return object;
        };

        /**
         * Converts this CryptoGetStakersResponse to JSON.
         * @function toJSON
         * @memberof proto.CryptoGetStakersResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CryptoGetStakersResponse.prototype.toJSON = function() {
            return CryptoGetStakersResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CryptoGetStakersResponse
         * @function getTypeUrl
         * @memberof proto.CryptoGetStakersResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CryptoGetStakersResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CryptoGetStakersResponse";
        };

        return CryptoGetStakersResponse;
    })();

    proto.FileGetContentsQuery = (function() {

        /**
         * Properties of a FileGetContentsQuery.
         * @typedef {Object} proto.FileGetContentsQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.FileID.$Properties|null} [fileID] A file identifier.
         * <p>
         * This MUST be the identifier of a file that exists in HFS.<br/>
         * This value SHALL identify the file to be queried.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FileGetContentsQuery.
         * @memberof proto
         * @interface IFileGetContentsQuery
         * @augments proto.FileGetContentsQuery.$Properties
         * @deprecated Use proto.FileGetContentsQuery.$Properties instead.
         */

        /**
         * Shape of a FileGetContentsQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   fileID?: proto.FileID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.FileGetContentsQuery.$Shape
         */

        /**
         * Constructs a new FileGetContentsQuery.
         * @memberof proto
         * @classdesc A query request to the Hedera File Service (HFS) for file content.<br/>
         * This query requests the content of a file, but none of the information
         * _about_ a file. A client should submit a `fileGetInfo` query to view
         * information about a file.<br/>
         * File content may also be available from a block node or mirror node,
         * generally at lower cost.
         * @constructor
         * @param {proto.FileGetContentsQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FileGetContentsQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.FileGetContentsQuery
         * @instance
         */
        FileGetContentsQuery.prototype.header = null;

        /**
         * A file identifier.
         * <p>
         * This MUST be the identifier of a file that exists in HFS.<br/>
         * This value SHALL identify the file to be queried.
         * @member {proto.FileID.$Properties|null|undefined} fileID
         * @memberof proto.FileGetContentsQuery
         * @instance
         */
        FileGetContentsQuery.prototype.fileID = null;

        /**
         * Creates a new FileGetContentsQuery instance using the specified properties.
         * @function create
         * @memberof proto.FileGetContentsQuery
         * @static
         * @param {proto.FileGetContentsQuery.$Properties=} [properties] Properties to set
         * @returns {proto.FileGetContentsQuery} FileGetContentsQuery instance
         * @type {{
         *   (properties: proto.FileGetContentsQuery.$Shape): proto.FileGetContentsQuery & proto.FileGetContentsQuery.$Shape;
         *   (properties?: proto.FileGetContentsQuery.$Properties): proto.FileGetContentsQuery;
         * }}
         */
        FileGetContentsQuery.create = function(properties) {
            return new FileGetContentsQuery(properties);
        };

        /**
         * Encodes the specified FileGetContentsQuery message. Does not implicitly {@link proto.FileGetContentsQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.FileGetContentsQuery
         * @static
         * @param {proto.FileGetContentsQuery.$Properties} message FileGetContentsQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileGetContentsQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FileGetContentsQuery message, length delimited. Does not implicitly {@link proto.FileGetContentsQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FileGetContentsQuery
         * @static
         * @param {proto.FileGetContentsQuery.$Properties} message FileGetContentsQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileGetContentsQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FileGetContentsQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FileGetContentsQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FileGetContentsQuery & proto.FileGetContentsQuery.$Shape} FileGetContentsQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileGetContentsQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FileGetContentsQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FileGetContentsQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FileGetContentsQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FileGetContentsQuery & proto.FileGetContentsQuery.$Shape} FileGetContentsQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileGetContentsQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FileGetContentsQuery message.
         * @function verify
         * @memberof proto.FileGetContentsQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FileGetContentsQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                if (error)
                    return "fileID." + error;
            }
            return null;
        };

        /**
         * Creates a FileGetContentsQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FileGetContentsQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FileGetContentsQuery} FileGetContentsQuery
         */
        FileGetContentsQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FileGetContentsQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FileGetContentsQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FileGetContentsQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.FileGetContentsQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.fileID != null) {
                if (!$util.isObject(object.fileID))
                    throw $TypeError(".proto.FileGetContentsQuery.fileID: object expected");
                message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a FileGetContentsQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FileGetContentsQuery
         * @static
         * @param {proto.FileGetContentsQuery} message FileGetContentsQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FileGetContentsQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.fileID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this FileGetContentsQuery to JSON.
         * @function toJSON
         * @memberof proto.FileGetContentsQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FileGetContentsQuery.prototype.toJSON = function() {
            return FileGetContentsQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FileGetContentsQuery
         * @function getTypeUrl
         * @memberof proto.FileGetContentsQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FileGetContentsQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FileGetContentsQuery";
        };

        return FileGetContentsQuery;
    })();

    proto.FileGetContentsResponse = (function() {

        /**
         * Properties of a FileGetContentsResponse.
         * @typedef {Object} proto.FileGetContentsResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.FileGetContentsResponse.FileContents.$Properties|null} [fileContents] A combination of File identifier and content bytes.
         * <p>
         * This SHALL NOT be set if the file does not exist.<br/>
         * The network MAY generate a state proof for this field.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FileGetContentsResponse.
         * @memberof proto
         * @interface IFileGetContentsResponse
         * @augments proto.FileGetContentsResponse.$Properties
         * @deprecated Use proto.FileGetContentsResponse.$Properties instead.
         */

        /**
         * Shape of a FileGetContentsResponse.
         * @typedef {proto.FileGetContentsResponse.$Properties} proto.FileGetContentsResponse.$Shape
         */

        /**
         * Constructs a new FileGetContentsResponse.
         * @memberof proto
         * @classdesc A response to a query for the content of a file in the
         * Hedera File Service (HFS).
         * 
         * This message SHALL contain the full content of the requested file, but
         * SHALL NOT contain any metadata.
         * @constructor
         * @param {proto.FileGetContentsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FileGetContentsResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.FileGetContentsResponse
         * @instance
         */
        FileGetContentsResponse.prototype.header = null;

        /**
         * A combination of File identifier and content bytes.
         * <p>
         * This SHALL NOT be set if the file does not exist.<br/>
         * The network MAY generate a state proof for this field.
         * @member {proto.FileGetContentsResponse.FileContents.$Properties|null|undefined} fileContents
         * @memberof proto.FileGetContentsResponse
         * @instance
         */
        FileGetContentsResponse.prototype.fileContents = null;

        /**
         * Creates a new FileGetContentsResponse instance using the specified properties.
         * @function create
         * @memberof proto.FileGetContentsResponse
         * @static
         * @param {proto.FileGetContentsResponse.$Properties=} [properties] Properties to set
         * @returns {proto.FileGetContentsResponse} FileGetContentsResponse instance
         * @type {{
         *   (properties: proto.FileGetContentsResponse.$Shape): proto.FileGetContentsResponse & proto.FileGetContentsResponse.$Shape;
         *   (properties?: proto.FileGetContentsResponse.$Properties): proto.FileGetContentsResponse;
         * }}
         */
        FileGetContentsResponse.create = function(properties) {
            return new FileGetContentsResponse(properties);
        };

        /**
         * Encodes the specified FileGetContentsResponse message. Does not implicitly {@link proto.FileGetContentsResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.FileGetContentsResponse
         * @static
         * @param {proto.FileGetContentsResponse.$Properties} message FileGetContentsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileGetContentsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.fileContents != null && $Object.hasOwnProperty.call(message, "fileContents"))
                $root.proto.FileGetContentsResponse.FileContents.encode(message.fileContents, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FileGetContentsResponse message, length delimited. Does not implicitly {@link proto.FileGetContentsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FileGetContentsResponse
         * @static
         * @param {proto.FileGetContentsResponse.$Properties} message FileGetContentsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileGetContentsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FileGetContentsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FileGetContentsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FileGetContentsResponse & proto.FileGetContentsResponse.$Shape} FileGetContentsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileGetContentsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FileGetContentsResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.fileContents = $root.proto.FileGetContentsResponse.FileContents.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileContents);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FileGetContentsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FileGetContentsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FileGetContentsResponse & proto.FileGetContentsResponse.$Shape} FileGetContentsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileGetContentsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FileGetContentsResponse message.
         * @function verify
         * @memberof proto.FileGetContentsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FileGetContentsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.fileContents != null && $Object.hasOwnProperty.call(message, "fileContents")) {
                let error = $root.proto.FileGetContentsResponse.FileContents.verify(message.fileContents, _depth + 1);
                if (error)
                    return "fileContents." + error;
            }
            return null;
        };

        /**
         * Creates a FileGetContentsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FileGetContentsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FileGetContentsResponse} FileGetContentsResponse
         */
        FileGetContentsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FileGetContentsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FileGetContentsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FileGetContentsResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.FileGetContentsResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.fileContents != null) {
                if (!$util.isObject(object.fileContents))
                    throw $TypeError(".proto.FileGetContentsResponse.fileContents: object expected");
                message.fileContents = $root.proto.FileGetContentsResponse.FileContents.fromObject(object.fileContents, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a FileGetContentsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FileGetContentsResponse
         * @static
         * @param {proto.FileGetContentsResponse} message FileGetContentsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FileGetContentsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.fileContents = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.fileContents != null && $Object.hasOwnProperty.call(message, "fileContents"))
                object.fileContents = $root.proto.FileGetContentsResponse.FileContents.toObject(message.fileContents, options, _depth + 1);
            return object;
        };

        /**
         * Converts this FileGetContentsResponse to JSON.
         * @function toJSON
         * @memberof proto.FileGetContentsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FileGetContentsResponse.prototype.toJSON = function() {
            return FileGetContentsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FileGetContentsResponse
         * @function getTypeUrl
         * @memberof proto.FileGetContentsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FileGetContentsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FileGetContentsResponse";
        };

        FileGetContentsResponse.FileContents = (function() {

            /**
             * Properties of a FileContents.
             * @typedef {Object} proto.FileGetContentsResponse.FileContents.$Properties
             * @property {proto.FileID.$Properties|null} [fileID] A file identifier.
             * <p>
             * This SHALL be the identifier of a file that exists in HFS.<br/>
             * This value SHALL identify the file that was queried.
             * @property {Uint8Array|null} [contents] A byte array of file content.
             * <p>
             * This SHALL contain the full content of the requested file.<br/>
             * This SHALL be empty if, and only if, the file content is empty.
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FileContents.
             * @memberof proto.FileGetContentsResponse
             * @interface IFileContents
             * @augments proto.FileGetContentsResponse.FileContents.$Properties
             * @deprecated Use proto.FileGetContentsResponse.FileContents.$Properties instead.
             */

            /**
             * Shape of a FileContents.
             * @typedef {proto.FileGetContentsResponse.FileContents.$Properties} proto.FileGetContentsResponse.FileContents.$Shape
             */

            /**
             * Constructs a new FileContents.
             * @memberof proto.FileGetContentsResponse
             * @classdesc Represents a FileContents.
             * @constructor
             * @param {proto.FileGetContentsResponse.FileContents.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FileContents = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * A file identifier.
             * <p>
             * This SHALL be the identifier of a file that exists in HFS.<br/>
             * This value SHALL identify the file that was queried.
             * @member {proto.FileID.$Properties|null|undefined} fileID
             * @memberof proto.FileGetContentsResponse.FileContents
             * @instance
             */
            FileContents.prototype.fileID = null;

            /**
             * A byte array of file content.
             * <p>
             * This SHALL contain the full content of the requested file.<br/>
             * This SHALL be empty if, and only if, the file content is empty.
             * @member {Uint8Array} contents
             * @memberof proto.FileGetContentsResponse.FileContents
             * @instance
             */
            FileContents.prototype.contents = $util.newBuffer([]);

            /**
             * Creates a new FileContents instance using the specified properties.
             * @function create
             * @memberof proto.FileGetContentsResponse.FileContents
             * @static
             * @param {proto.FileGetContentsResponse.FileContents.$Properties=} [properties] Properties to set
             * @returns {proto.FileGetContentsResponse.FileContents} FileContents instance
             * @type {{
             *   (properties: proto.FileGetContentsResponse.FileContents.$Shape): proto.FileGetContentsResponse.FileContents & proto.FileGetContentsResponse.FileContents.$Shape;
             *   (properties?: proto.FileGetContentsResponse.FileContents.$Properties): proto.FileGetContentsResponse.FileContents;
             * }}
             */
            FileContents.create = function(properties) {
                return new FileContents(properties);
            };

            /**
             * Encodes the specified FileContents message. Does not implicitly {@link proto.FileGetContentsResponse.FileContents.verify|verify} messages.
             * @function encode
             * @memberof proto.FileGetContentsResponse.FileContents
             * @static
             * @param {proto.FileGetContentsResponse.FileContents.$Properties} message FileContents message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileContents.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                    $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.contents != null && $Object.hasOwnProperty.call(message, "contents") && message.contents.length)
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.contents);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FileContents message, length delimited. Does not implicitly {@link proto.FileGetContentsResponse.FileContents.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.FileGetContentsResponse.FileContents
             * @static
             * @param {proto.FileGetContentsResponse.FileContents.$Properties} message FileContents message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileContents.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a FileContents message from the specified reader or buffer.
             * @function decode
             * @memberof proto.FileGetContentsResponse.FileContents
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.FileGetContentsResponse.FileContents & proto.FileGetContentsResponse.FileContents.$Shape} FileContents
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileContents.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.proto.FileGetContentsResponse.FileContents();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.bytes()).length)
                                message.contents = value;
                            else
                                delete message.contents;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FileContents message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.FileGetContentsResponse.FileContents
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.FileGetContentsResponse.FileContents & proto.FileGetContentsResponse.FileContents.$Shape} FileContents
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileContents.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FileContents message.
             * @function verify
             * @memberof proto.FileGetContentsResponse.FileContents
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FileContents.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                    let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                    if (error)
                        return "fileID." + error;
                }
                if (message.contents != null && $Object.hasOwnProperty.call(message, "contents"))
                    if (!(message.contents && typeof message.contents.length === "number" || $util.isString(message.contents)))
                        return "contents: buffer expected";
                return null;
            };

            /**
             * Creates a FileContents message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.FileGetContentsResponse.FileContents
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.FileGetContentsResponse.FileContents} FileContents
             */
            FileContents.fromObject = function (object, _depth) {
                if (object instanceof $root.proto.FileGetContentsResponse.FileContents)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".proto.FileGetContentsResponse.FileContents: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.proto.FileGetContentsResponse.FileContents();
                if (object.fileID != null) {
                    if (!$util.isObject(object.fileID))
                        throw $TypeError(".proto.FileGetContentsResponse.FileContents.fileID: object expected");
                    message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
                }
                if (object.contents != null)
                    if (object.contents.length)
                        if (typeof object.contents === "string")
                            $util.base64.decode(object.contents, message.contents = $util.newBuffer($util.base64.length(object.contents)), 0);
                        else if (object.contents.length >= 0)
                            message.contents = object.contents;
                return message;
            };

            /**
             * Creates a plain object from a FileContents message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.FileGetContentsResponse.FileContents
             * @static
             * @param {proto.FileGetContentsResponse.FileContents} message FileContents
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FileContents.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.fileID = null;
                    if (options.bytes === $String)
                        object.contents = "";
                    else {
                        object.contents = [];
                        if (options.bytes !== $Array)
                            object.contents = $util.newBuffer(object.contents);
                    }
                }
                if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                    object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
                if (message.contents != null && $Object.hasOwnProperty.call(message, "contents"))
                    object.contents = options.bytes === $String ? $util.base64.encode(message.contents, 0, message.contents.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.contents) : message.contents;
                return object;
            };

            /**
             * Converts this FileContents to JSON.
             * @function toJSON
             * @memberof proto.FileGetContentsResponse.FileContents
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FileContents.prototype.toJSON = function() {
                return FileContents.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FileContents
             * @function getTypeUrl
             * @memberof proto.FileGetContentsResponse.FileContents
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FileContents.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.FileGetContentsResponse.FileContents";
            };

            return FileContents;
        })();

        return FileGetContentsResponse;
    })();

    proto.FileGetInfoQuery = (function() {

        /**
         * Properties of a FileGetInfoQuery.
         * @typedef {Object} proto.FileGetInfoQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.FileID.$Properties|null} [fileID] A file identifier.
         * <p>
         * This MUST be the identifier of a file that exists in HFS.<br/>
         * This value SHALL identify the file to be queried.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FileGetInfoQuery.
         * @memberof proto
         * @interface IFileGetInfoQuery
         * @augments proto.FileGetInfoQuery.$Properties
         * @deprecated Use proto.FileGetInfoQuery.$Properties instead.
         */

        /**
         * Shape of a FileGetInfoQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   fileID?: proto.FileID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.FileGetInfoQuery.$Shape
         */

        /**
         * Constructs a new FileGetInfoQuery.
         * @memberof proto
         * @classdesc Query to request file metadata from the Hedera File Service (HFS).<br/>
         * This query requests all of the information _about_ a file, but none of the
         * _content_ of a file. A client should submit a `fileGetContents` query to
         * view the content of a file. File content _may_ also be available from a
         * block node or mirror node, generally at lower cost.
         * 
         * File metadata SHALL be available for active files and deleted files.<br/>
         * The size of a deleted file SHALL be `0` and the content SHALL be empty.
         * @constructor
         * @param {proto.FileGetInfoQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FileGetInfoQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.FileGetInfoQuery
         * @instance
         */
        FileGetInfoQuery.prototype.header = null;

        /**
         * A file identifier.
         * <p>
         * This MUST be the identifier of a file that exists in HFS.<br/>
         * This value SHALL identify the file to be queried.
         * @member {proto.FileID.$Properties|null|undefined} fileID
         * @memberof proto.FileGetInfoQuery
         * @instance
         */
        FileGetInfoQuery.prototype.fileID = null;

        /**
         * Creates a new FileGetInfoQuery instance using the specified properties.
         * @function create
         * @memberof proto.FileGetInfoQuery
         * @static
         * @param {proto.FileGetInfoQuery.$Properties=} [properties] Properties to set
         * @returns {proto.FileGetInfoQuery} FileGetInfoQuery instance
         * @type {{
         *   (properties: proto.FileGetInfoQuery.$Shape): proto.FileGetInfoQuery & proto.FileGetInfoQuery.$Shape;
         *   (properties?: proto.FileGetInfoQuery.$Properties): proto.FileGetInfoQuery;
         * }}
         */
        FileGetInfoQuery.create = function(properties) {
            return new FileGetInfoQuery(properties);
        };

        /**
         * Encodes the specified FileGetInfoQuery message. Does not implicitly {@link proto.FileGetInfoQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.FileGetInfoQuery
         * @static
         * @param {proto.FileGetInfoQuery.$Properties} message FileGetInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileGetInfoQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FileGetInfoQuery message, length delimited. Does not implicitly {@link proto.FileGetInfoQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FileGetInfoQuery
         * @static
         * @param {proto.FileGetInfoQuery.$Properties} message FileGetInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileGetInfoQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FileGetInfoQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FileGetInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FileGetInfoQuery & proto.FileGetInfoQuery.$Shape} FileGetInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileGetInfoQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FileGetInfoQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FileGetInfoQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FileGetInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FileGetInfoQuery & proto.FileGetInfoQuery.$Shape} FileGetInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileGetInfoQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FileGetInfoQuery message.
         * @function verify
         * @memberof proto.FileGetInfoQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FileGetInfoQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                if (error)
                    return "fileID." + error;
            }
            return null;
        };

        /**
         * Creates a FileGetInfoQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FileGetInfoQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FileGetInfoQuery} FileGetInfoQuery
         */
        FileGetInfoQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FileGetInfoQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FileGetInfoQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FileGetInfoQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.FileGetInfoQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.fileID != null) {
                if (!$util.isObject(object.fileID))
                    throw $TypeError(".proto.FileGetInfoQuery.fileID: object expected");
                message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a FileGetInfoQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FileGetInfoQuery
         * @static
         * @param {proto.FileGetInfoQuery} message FileGetInfoQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FileGetInfoQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.fileID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this FileGetInfoQuery to JSON.
         * @function toJSON
         * @memberof proto.FileGetInfoQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FileGetInfoQuery.prototype.toJSON = function() {
            return FileGetInfoQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FileGetInfoQuery
         * @function getTypeUrl
         * @memberof proto.FileGetInfoQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FileGetInfoQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FileGetInfoQuery";
        };

        return FileGetInfoQuery;
    })();

    proto.FileGetInfoResponse = (function() {

        /**
         * Properties of a FileGetInfoResponse.
         * @typedef {Object} proto.FileGetInfoResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.FileGetInfoResponse.FileInfo.$Properties|null} [fileInfo] A combination of fields from the requested file metadata.
         * <p>
         * This SHALL NOT be set if the identified file does not exist
         * or has expired.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a FileGetInfoResponse.
         * @memberof proto
         * @interface IFileGetInfoResponse
         * @augments proto.FileGetInfoResponse.$Properties
         * @deprecated Use proto.FileGetInfoResponse.$Properties instead.
         */

        /**
         * Shape of a FileGetInfoResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   fileInfo?: proto.FileGetInfoResponse.FileInfo.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.FileGetInfoResponse.$Shape
         */

        /**
         * Constructs a new FileGetInfoResponse.
         * @memberof proto
         * @classdesc A response to a query for the metadata of a file in the HFS.
         * @constructor
         * @param {proto.FileGetInfoResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const FileGetInfoResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.FileGetInfoResponse
         * @instance
         */
        FileGetInfoResponse.prototype.header = null;

        /**
         * A combination of fields from the requested file metadata.
         * <p>
         * This SHALL NOT be set if the identified file does not exist
         * or has expired.
         * @member {proto.FileGetInfoResponse.FileInfo.$Properties|null|undefined} fileInfo
         * @memberof proto.FileGetInfoResponse
         * @instance
         */
        FileGetInfoResponse.prototype.fileInfo = null;

        /**
         * Creates a new FileGetInfoResponse instance using the specified properties.
         * @function create
         * @memberof proto.FileGetInfoResponse
         * @static
         * @param {proto.FileGetInfoResponse.$Properties=} [properties] Properties to set
         * @returns {proto.FileGetInfoResponse} FileGetInfoResponse instance
         * @type {{
         *   (properties: proto.FileGetInfoResponse.$Shape): proto.FileGetInfoResponse & proto.FileGetInfoResponse.$Shape;
         *   (properties?: proto.FileGetInfoResponse.$Properties): proto.FileGetInfoResponse;
         * }}
         */
        FileGetInfoResponse.create = function(properties) {
            return new FileGetInfoResponse(properties);
        };

        /**
         * Encodes the specified FileGetInfoResponse message. Does not implicitly {@link proto.FileGetInfoResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.FileGetInfoResponse
         * @static
         * @param {proto.FileGetInfoResponse.$Properties} message FileGetInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileGetInfoResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.fileInfo != null && $Object.hasOwnProperty.call(message, "fileInfo"))
                $root.proto.FileGetInfoResponse.FileInfo.encode(message.fileInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified FileGetInfoResponse message, length delimited. Does not implicitly {@link proto.FileGetInfoResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.FileGetInfoResponse
         * @static
         * @param {proto.FileGetInfoResponse.$Properties} message FileGetInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FileGetInfoResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a FileGetInfoResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.FileGetInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.FileGetInfoResponse & proto.FileGetInfoResponse.$Shape} FileGetInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileGetInfoResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.FileGetInfoResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.fileInfo = $root.proto.FileGetInfoResponse.FileInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileInfo);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a FileGetInfoResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.FileGetInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.FileGetInfoResponse & proto.FileGetInfoResponse.$Shape} FileGetInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FileGetInfoResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FileGetInfoResponse message.
         * @function verify
         * @memberof proto.FileGetInfoResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FileGetInfoResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.fileInfo != null && $Object.hasOwnProperty.call(message, "fileInfo")) {
                let error = $root.proto.FileGetInfoResponse.FileInfo.verify(message.fileInfo, _depth + 1);
                if (error)
                    return "fileInfo." + error;
            }
            return null;
        };

        /**
         * Creates a FileGetInfoResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.FileGetInfoResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.FileGetInfoResponse} FileGetInfoResponse
         */
        FileGetInfoResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.FileGetInfoResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.FileGetInfoResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.FileGetInfoResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.FileGetInfoResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.fileInfo != null) {
                if (!$util.isObject(object.fileInfo))
                    throw $TypeError(".proto.FileGetInfoResponse.fileInfo: object expected");
                message.fileInfo = $root.proto.FileGetInfoResponse.FileInfo.fromObject(object.fileInfo, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a FileGetInfoResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.FileGetInfoResponse
         * @static
         * @param {proto.FileGetInfoResponse} message FileGetInfoResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FileGetInfoResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.fileInfo = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.fileInfo != null && $Object.hasOwnProperty.call(message, "fileInfo"))
                object.fileInfo = $root.proto.FileGetInfoResponse.FileInfo.toObject(message.fileInfo, options, _depth + 1);
            return object;
        };

        /**
         * Converts this FileGetInfoResponse to JSON.
         * @function toJSON
         * @memberof proto.FileGetInfoResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FileGetInfoResponse.prototype.toJSON = function() {
            return FileGetInfoResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for FileGetInfoResponse
         * @function getTypeUrl
         * @memberof proto.FileGetInfoResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        FileGetInfoResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FileGetInfoResponse";
        };

        FileGetInfoResponse.FileInfo = (function() {

            /**
             * Properties of a FileInfo.
             * @typedef {Object} proto.FileGetInfoResponse.FileInfo.$Properties
             * @property {proto.FileID.$Properties|null} [fileID] A file identifier.
             * <p>
             * This SHALL be the identifier of a file that exists in HFS.<br/>
             * This value SHALL identify the file that was queried.
             * @property {number|Long|null} [size] A size, in bytes, for the file.
             * @property {proto.Timestamp.$Properties|null} [expirationTime] An expiration timestamp.
             * <p>
             * The file SHALL NOT expire before the network consensus time
             * exceeds this value.<br/>
             * The file SHALL expire after the network consensus time
             * exceeds this value.<br/>
             * @property {boolean|null} [deleted] A flag indicating this file is deleted.
             * <p>
             * A deleted file SHALL have a size `0` and empty content.
             * @property {proto.KeyList.$Properties|null} [keys] A KeyList listing all keys that "own" the file.
             * <p>
             * All keys in this list MUST sign a transaction to append to the
             * file content, or to modify file metadata.<br/>
             * At least _one_ key in this list MUST sign a transaction to delete
             * this file.<br/>
             * If this is an empty `KeyList`, the file is immutable, cannot be
             * modified or deleted, but MAY expire. A `fileUpdate` transaction MAY
             * extend the expiration time for an immutable file.
             * @property {string|null} [memo] A short description for this file.
             * <p>
             * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
             * (default 100) bytes when encoded as UTF-8.
             * @property {Uint8Array|null} [ledgerId] A ledger identifier for the responding network.
             * <p>
             * This value SHALL identify the distributed ledger that responded to
             * this query.
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FileInfo.
             * @memberof proto.FileGetInfoResponse
             * @interface IFileInfo
             * @augments proto.FileGetInfoResponse.FileInfo.$Properties
             * @deprecated Use proto.FileGetInfoResponse.FileInfo.$Properties instead.
             */

            /**
             * Shape of a FileInfo.
             * @typedef {{
             *   fileID?: proto.FileID.$Shape|null;
             *   size?: number|Long|null;
             *   expirationTime?: proto.Timestamp.$Shape|null;
             *   deleted?: boolean|null;
             *   keys?: proto.KeyList.$Shape|null;
             *   memo?: string|null;
             *   ledgerId?: Uint8Array|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} proto.FileGetInfoResponse.FileInfo.$Shape
             */

            /**
             * Constructs a new FileInfo.
             * @memberof proto.FileGetInfoResponse
             * @classdesc Represents a FileInfo.
             * @constructor
             * @param {proto.FileGetInfoResponse.FileInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FileInfo = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * A file identifier.
             * <p>
             * This SHALL be the identifier of a file that exists in HFS.<br/>
             * This value SHALL identify the file that was queried.
             * @member {proto.FileID.$Properties|null|undefined} fileID
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @instance
             */
            FileInfo.prototype.fileID = null;

            /**
             * A size, in bytes, for the file.
             * @member {number|Long} size
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @instance
             */
            FileInfo.prototype.size = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * An expiration timestamp.
             * <p>
             * The file SHALL NOT expire before the network consensus time
             * exceeds this value.<br/>
             * The file SHALL expire after the network consensus time
             * exceeds this value.<br/>
             * @member {proto.Timestamp.$Properties|null|undefined} expirationTime
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @instance
             */
            FileInfo.prototype.expirationTime = null;

            /**
             * A flag indicating this file is deleted.
             * <p>
             * A deleted file SHALL have a size `0` and empty content.
             * @member {boolean} deleted
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @instance
             */
            FileInfo.prototype.deleted = false;

            /**
             * A KeyList listing all keys that "own" the file.
             * <p>
             * All keys in this list MUST sign a transaction to append to the
             * file content, or to modify file metadata.<br/>
             * At least _one_ key in this list MUST sign a transaction to delete
             * this file.<br/>
             * If this is an empty `KeyList`, the file is immutable, cannot be
             * modified or deleted, but MAY expire. A `fileUpdate` transaction MAY
             * extend the expiration time for an immutable file.
             * @member {proto.KeyList.$Properties|null|undefined} keys
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @instance
             */
            FileInfo.prototype.keys = null;

            /**
             * A short description for this file.
             * <p>
             * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
             * (default 100) bytes when encoded as UTF-8.
             * @member {string} memo
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @instance
             */
            FileInfo.prototype.memo = "";

            /**
             * A ledger identifier for the responding network.
             * <p>
             * This value SHALL identify the distributed ledger that responded to
             * this query.
             * @member {Uint8Array} ledgerId
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @instance
             */
            FileInfo.prototype.ledgerId = $util.newBuffer([]);

            /**
             * Creates a new FileInfo instance using the specified properties.
             * @function create
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @static
             * @param {proto.FileGetInfoResponse.FileInfo.$Properties=} [properties] Properties to set
             * @returns {proto.FileGetInfoResponse.FileInfo} FileInfo instance
             * @type {{
             *   (properties: proto.FileGetInfoResponse.FileInfo.$Shape): proto.FileGetInfoResponse.FileInfo & proto.FileGetInfoResponse.FileInfo.$Shape;
             *   (properties?: proto.FileGetInfoResponse.FileInfo.$Properties): proto.FileGetInfoResponse.FileInfo;
             * }}
             */
            FileInfo.create = function(properties) {
                return new FileInfo(properties);
            };

            /**
             * Encodes the specified FileInfo message. Does not implicitly {@link proto.FileGetInfoResponse.FileInfo.verify|verify} messages.
             * @function encode
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @static
             * @param {proto.FileGetInfoResponse.FileInfo.$Properties} message FileInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                    $root.proto.FileID.encode(message.fileID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.size != null && $Object.hasOwnProperty.call(message, "size") && (typeof message.size === "object" ? message.size.low || message.size.high : message.size !== 0))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.size);
                if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                    $root.proto.Timestamp.encode(message.expirationTime, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.deleted);
                if (message.keys != null && $Object.hasOwnProperty.call(message, "keys"))
                    $root.proto.KeyList.encode(message.keys, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.memo);
                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId") && message.ledgerId.length)
                    writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.ledgerId);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FileInfo message, length delimited. Does not implicitly {@link proto.FileGetInfoResponse.FileInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @static
             * @param {proto.FileGetInfoResponse.FileInfo.$Properties} message FileInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a FileInfo message from the specified reader or buffer.
             * @function decode
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.FileGetInfoResponse.FileInfo & proto.FileGetInfoResponse.FileInfo.$Shape} FileInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.proto.FileGetInfoResponse.FileInfo();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.fileID = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileID);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.size = value;
                            else
                                delete message.size;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.expirationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.deleted = value;
                            else
                                delete message.deleted;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.keys = $root.proto.KeyList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.keys);
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.memo = value;
                            else
                                delete message.memo;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.bytes()).length)
                                message.ledgerId = value;
                            else
                                delete message.ledgerId;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FileInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.FileGetInfoResponse.FileInfo & proto.FileGetInfoResponse.FileInfo.$Shape} FileInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FileInfo message.
             * @function verify
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FileInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID")) {
                    let error = $root.proto.FileID.verify(message.fileID, _depth + 1);
                    if (error)
                        return "fileID." + error;
                }
                if (message.size != null && $Object.hasOwnProperty.call(message, "size"))
                    if (!$util.isInteger(message.size) && !(message.size && $util.isInteger(message.size.low) && $util.isInteger(message.size.high)))
                        return "size: integer|Long expected";
                if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                    let error = $root.proto.Timestamp.verify(message.expirationTime, _depth + 1);
                    if (error)
                        return "expirationTime." + error;
                }
                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                    if (typeof message.deleted !== "boolean")
                        return "deleted: boolean expected";
                if (message.keys != null && $Object.hasOwnProperty.call(message, "keys")) {
                    let error = $root.proto.KeyList.verify(message.keys, _depth + 1);
                    if (error)
                        return "keys." + error;
                }
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                    if (!$util.isString(message.memo))
                        return "memo: string expected";
                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                    if (!(message.ledgerId && typeof message.ledgerId.length === "number" || $util.isString(message.ledgerId)))
                        return "ledgerId: buffer expected";
                return null;
            };

            /**
             * Creates a FileInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.FileGetInfoResponse.FileInfo} FileInfo
             */
            FileInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.proto.FileGetInfoResponse.FileInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".proto.FileGetInfoResponse.FileInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.proto.FileGetInfoResponse.FileInfo();
                if (object.fileID != null) {
                    if (!$util.isObject(object.fileID))
                        throw $TypeError(".proto.FileGetInfoResponse.FileInfo.fileID: object expected");
                    message.fileID = $root.proto.FileID.fromObject(object.fileID, _depth + 1);
                }
                if (object.size != null)
                    if (typeof object.size === "object" ? object.size.low || object.size.high : $Number(object.size) !== 0)
                        if ($util.Long)
                            message.size = $util.Long.fromValue(object.size, false);
                        else if (typeof object.size === "string")
                            message.size = $parseInt(object.size, 10);
                        else if (typeof object.size === "number")
                            message.size = object.size;
                        else if (typeof object.size === "object")
                            message.size = new $util.LongBits(object.size.low >>> 0, object.size.high >>> 0).toNumber();
                if (object.expirationTime != null) {
                    if (!$util.isObject(object.expirationTime))
                        throw $TypeError(".proto.FileGetInfoResponse.FileInfo.expirationTime: object expected");
                    message.expirationTime = $root.proto.Timestamp.fromObject(object.expirationTime, _depth + 1);
                }
                if (object.deleted != null)
                    if (object.deleted)
                        message.deleted = $Boolean(object.deleted);
                if (object.keys != null) {
                    if (!$util.isObject(object.keys))
                        throw $TypeError(".proto.FileGetInfoResponse.FileInfo.keys: object expected");
                    message.keys = $root.proto.KeyList.fromObject(object.keys, _depth + 1);
                }
                if (object.memo != null)
                    if (typeof object.memo !== "string" || object.memo.length)
                        message.memo = $String(object.memo);
                if (object.ledgerId != null)
                    if (object.ledgerId.length)
                        if (typeof object.ledgerId === "string")
                            $util.base64.decode(object.ledgerId, message.ledgerId = $util.newBuffer($util.base64.length(object.ledgerId)), 0);
                        else if (object.ledgerId.length >= 0)
                            message.ledgerId = object.ledgerId;
                return message;
            };

            /**
             * Creates a plain object from a FileInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @static
             * @param {proto.FileGetInfoResponse.FileInfo} message FileInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FileInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.fileID = null;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.size = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.size = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.expirationTime = null;
                    object.deleted = false;
                    object.keys = null;
                    object.memo = "";
                    if (options.bytes === $String)
                        object.ledgerId = "";
                    else {
                        object.ledgerId = [];
                        if (options.bytes !== $Array)
                            object.ledgerId = $util.newBuffer(object.ledgerId);
                    }
                }
                if (message.fileID != null && $Object.hasOwnProperty.call(message, "fileID"))
                    object.fileID = $root.proto.FileID.toObject(message.fileID, options, _depth + 1);
                if (message.size != null && $Object.hasOwnProperty.call(message, "size"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.size = typeof message.size === "number" ? $BigInt(message.size) : $util.Long.fromBits(message.size.low >>> 0, message.size.high >>> 0, false).toBigInt();
                    else if (typeof message.size === "number")
                        object.size = options.longs === $String ? $String(message.size) : message.size;
                    else
                        object.size = options.longs === $String ? $util.Long.prototype.toString.call(message.size) : options.longs === $Number ? new $util.LongBits(message.size.low >>> 0, message.size.high >>> 0).toNumber() : message.size;
                if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                    object.expirationTime = $root.proto.Timestamp.toObject(message.expirationTime, options, _depth + 1);
                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                    object.deleted = message.deleted;
                if (message.keys != null && $Object.hasOwnProperty.call(message, "keys"))
                    object.keys = $root.proto.KeyList.toObject(message.keys, options, _depth + 1);
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                    object.memo = message.memo;
                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                    object.ledgerId = options.bytes === $String ? $util.base64.encode(message.ledgerId, 0, message.ledgerId.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ledgerId) : message.ledgerId;
                return object;
            };

            /**
             * Converts this FileInfo to JSON.
             * @function toJSON
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FileInfo.prototype.toJSON = function() {
                return FileInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FileInfo
             * @function getTypeUrl
             * @memberof proto.FileGetInfoResponse.FileInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FileInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.FileGetInfoResponse.FileInfo";
            };

            return FileInfo;
        })();

        return FileGetInfoResponse;
    })();

    proto.TransactionGetReceiptQuery = (function() {

        /**
         * Properties of a TransactionGetReceiptQuery.
         * @typedef {Object} proto.TransactionGetReceiptQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.TransactionID.$Properties|null} [transactionID] A transaction identifier.
         * <p>
         * This MUST contain the full identifier, as submitted, for the
         * transaction to query.
         * @property {boolean|null} [includeDuplicates] A flag to request duplicates.
         * <p>
         * If set, every transaction receipt within the receipt period that
         * matches the requested transaction identifier SHALL be returned.<br/>
         * If not set, duplicate transactions SHALL NOT be returned.<br/>
         * If not set, only the receipt for the first matching transaction to
         * reach consensus SHALL be returned.
         * @property {boolean|null} [includeChildReceipts] A flag to request "child" receipts.
         * <p>
         * If set, the response SHALL include receipts for each child transaction
         * executed as part of the requested parent transaction.<br/>
         * If not set, the response SHALL NOT include any receipts for child
         * transactions.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionGetReceiptQuery.
         * @memberof proto
         * @interface ITransactionGetReceiptQuery
         * @augments proto.TransactionGetReceiptQuery.$Properties
         * @deprecated Use proto.TransactionGetReceiptQuery.$Properties instead.
         */

        /**
         * Shape of a TransactionGetReceiptQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   transactionID?: proto.TransactionID.$Shape|null;
         *   includeDuplicates?: boolean|null;
         *   includeChildReceipts?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TransactionGetReceiptQuery.$Shape
         */

        /**
         * Constructs a new TransactionGetReceiptQuery.
         * @memberof proto
         * @classdesc A query to retrieve a transaction receipt.
         * This query retrieves the post-consensus (final) result of a transaction.
         * A transaction receipt may not be available if queried too early
         * (less than 5-10 seconds), or too late (more than 3 minutes). If a receipt
         * is available, it contains basic transaction results. A query to a mirror
         * node (or other archival system) is required to obtain full detail for a
         * transaction, or any result after the basic receipt time period.
         * 
         * This query is "free". The payment field in the header MUST be empty.<br/>
         * If a receipt is not available, the response SHALL be `UNKNOWN`.<br/>
         * A transaction receipt SHALL be available after the network reaches
         * consensus for a transaction.<br/>
         * A transaction receipt SHALL NOT be available after the end of the network
         * configured "receipt period", typically three(3) minutes.
         * 
         * <dl>
         * <dt>What is the "first" transaction?</dt>
         * <dd>The "first" transaction SHALL be the the transaction with
         * the earliest consensus time and a status that is neither
         * `INVALID_NODE_ACCOUNT` nor `INVALID_PAYER_SIGNATURE`.<br/>
         * If no transaction is found meeting this status criteria, the
         * "first" transaction SHALL be the transaction with the earliest
         * consensus time.</dd>
         * <dt>What is a "child" transaction?</dt>
         * <dd>A "child" transaction is any transaction created in the process of
         * completing another transaction. These are most common with a smart
         * contract call, where a call to a contract may initiate one or more
         * additional transactions to complete a complex process.</dd>
         * </dl>
         * @constructor
         * @param {proto.TransactionGetReceiptQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionGetReceiptQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.TransactionGetReceiptQuery
         * @instance
         */
        TransactionGetReceiptQuery.prototype.header = null;

        /**
         * A transaction identifier.
         * <p>
         * This MUST contain the full identifier, as submitted, for the
         * transaction to query.
         * @member {proto.TransactionID.$Properties|null|undefined} transactionID
         * @memberof proto.TransactionGetReceiptQuery
         * @instance
         */
        TransactionGetReceiptQuery.prototype.transactionID = null;

        /**
         * A flag to request duplicates.
         * <p>
         * If set, every transaction receipt within the receipt period that
         * matches the requested transaction identifier SHALL be returned.<br/>
         * If not set, duplicate transactions SHALL NOT be returned.<br/>
         * If not set, only the receipt for the first matching transaction to
         * reach consensus SHALL be returned.
         * @member {boolean} includeDuplicates
         * @memberof proto.TransactionGetReceiptQuery
         * @instance
         */
        TransactionGetReceiptQuery.prototype.includeDuplicates = false;

        /**
         * A flag to request "child" receipts.
         * <p>
         * If set, the response SHALL include receipts for each child transaction
         * executed as part of the requested parent transaction.<br/>
         * If not set, the response SHALL NOT include any receipts for child
         * transactions.
         * @member {boolean} includeChildReceipts
         * @memberof proto.TransactionGetReceiptQuery
         * @instance
         */
        TransactionGetReceiptQuery.prototype.includeChildReceipts = false;

        /**
         * Creates a new TransactionGetReceiptQuery instance using the specified properties.
         * @function create
         * @memberof proto.TransactionGetReceiptQuery
         * @static
         * @param {proto.TransactionGetReceiptQuery.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionGetReceiptQuery} TransactionGetReceiptQuery instance
         * @type {{
         *   (properties: proto.TransactionGetReceiptQuery.$Shape): proto.TransactionGetReceiptQuery & proto.TransactionGetReceiptQuery.$Shape;
         *   (properties?: proto.TransactionGetReceiptQuery.$Properties): proto.TransactionGetReceiptQuery;
         * }}
         */
        TransactionGetReceiptQuery.create = function(properties) {
            return new TransactionGetReceiptQuery(properties);
        };

        /**
         * Encodes the specified TransactionGetReceiptQuery message. Does not implicitly {@link proto.TransactionGetReceiptQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionGetReceiptQuery
         * @static
         * @param {proto.TransactionGetReceiptQuery.$Properties} message TransactionGetReceiptQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionGetReceiptQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID"))
                $root.proto.TransactionID.encode(message.transactionID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.includeDuplicates != null && $Object.hasOwnProperty.call(message, "includeDuplicates") && message.includeDuplicates !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.includeDuplicates);
            if (message.includeChildReceipts != null && $Object.hasOwnProperty.call(message, "includeChildReceipts") && message.includeChildReceipts !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.includeChildReceipts);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionGetReceiptQuery message, length delimited. Does not implicitly {@link proto.TransactionGetReceiptQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionGetReceiptQuery
         * @static
         * @param {proto.TransactionGetReceiptQuery.$Properties} message TransactionGetReceiptQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionGetReceiptQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionGetReceiptQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionGetReceiptQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionGetReceiptQuery & proto.TransactionGetReceiptQuery.$Shape} TransactionGetReceiptQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionGetReceiptQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionGetReceiptQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.transactionID = $root.proto.TransactionID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.includeDuplicates = value;
                        else
                            delete message.includeDuplicates;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.includeChildReceipts = value;
                        else
                            delete message.includeChildReceipts;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionGetReceiptQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionGetReceiptQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionGetReceiptQuery & proto.TransactionGetReceiptQuery.$Shape} TransactionGetReceiptQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionGetReceiptQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionGetReceiptQuery message.
         * @function verify
         * @memberof proto.TransactionGetReceiptQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionGetReceiptQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID")) {
                let error = $root.proto.TransactionID.verify(message.transactionID, _depth + 1);
                if (error)
                    return "transactionID." + error;
            }
            if (message.includeDuplicates != null && $Object.hasOwnProperty.call(message, "includeDuplicates"))
                if (typeof message.includeDuplicates !== "boolean")
                    return "includeDuplicates: boolean expected";
            if (message.includeChildReceipts != null && $Object.hasOwnProperty.call(message, "includeChildReceipts"))
                if (typeof message.includeChildReceipts !== "boolean")
                    return "includeChildReceipts: boolean expected";
            return null;
        };

        /**
         * Creates a TransactionGetReceiptQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionGetReceiptQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionGetReceiptQuery} TransactionGetReceiptQuery
         */
        TransactionGetReceiptQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionGetReceiptQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionGetReceiptQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionGetReceiptQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TransactionGetReceiptQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.transactionID != null) {
                if (!$util.isObject(object.transactionID))
                    throw $TypeError(".proto.TransactionGetReceiptQuery.transactionID: object expected");
                message.transactionID = $root.proto.TransactionID.fromObject(object.transactionID, _depth + 1);
            }
            if (object.includeDuplicates != null)
                if (object.includeDuplicates)
                    message.includeDuplicates = $Boolean(object.includeDuplicates);
            if (object.includeChildReceipts != null)
                if (object.includeChildReceipts)
                    message.includeChildReceipts = $Boolean(object.includeChildReceipts);
            return message;
        };

        /**
         * Creates a plain object from a TransactionGetReceiptQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionGetReceiptQuery
         * @static
         * @param {proto.TransactionGetReceiptQuery} message TransactionGetReceiptQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionGetReceiptQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.transactionID = null;
                object.includeDuplicates = false;
                object.includeChildReceipts = false;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID"))
                object.transactionID = $root.proto.TransactionID.toObject(message.transactionID, options, _depth + 1);
            if (message.includeDuplicates != null && $Object.hasOwnProperty.call(message, "includeDuplicates"))
                object.includeDuplicates = message.includeDuplicates;
            if (message.includeChildReceipts != null && $Object.hasOwnProperty.call(message, "includeChildReceipts"))
                object.includeChildReceipts = message.includeChildReceipts;
            return object;
        };

        /**
         * Converts this TransactionGetReceiptQuery to JSON.
         * @function toJSON
         * @memberof proto.TransactionGetReceiptQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionGetReceiptQuery.prototype.toJSON = function() {
            return TransactionGetReceiptQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionGetReceiptQuery
         * @function getTypeUrl
         * @memberof proto.TransactionGetReceiptQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionGetReceiptQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionGetReceiptQuery";
        };

        return TransactionGetReceiptQuery;
    })();

    proto.TransactionGetReceiptResponse = (function() {

        /**
         * Properties of a TransactionGetReceiptResponse.
         * @typedef {Object} proto.TransactionGetReceiptResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.TransactionReceipt.$Properties|null} [receipt] A transaction receipt.
         * <p>
         * This SHALL be the receipt for the "first" transaction that matches
         * the transaction identifier requested.<br/>
         * If the identified transaction has not reached consensus, this receipt
         * SHALL have a `status` of `UNKNOWN`.<br/>
         * If the identified transaction reached consensus prior to the current
         * configured receipt period (typically the last 180 seconds), this receipt
         * SHALL have a `status` of `UNKNOWN`.
         * @property {Array.<proto.TransactionReceipt.$Properties>|null} [duplicateTransactionReceipts] A list of duplicate transaction receipts.
         * <p>
         * If the request set the `includeDuplicates` flat, this list SHALL
         * include the receipts for each duplicate transaction associated to the
         * requested transaction identifier.
         * If the request did not set the `includeDuplicates` flag, this list
         * SHALL be empty.<br/>
         * If the `receipt` status is `UNKNOWN`, this list SHALL be empty.<br/>
         * This list SHALL be in order by consensus timestamp.
         * @property {Array.<proto.TransactionReceipt.$Properties>|null} [childTransactionReceipts] A list of receipts for all child transactions spawned by the requested
         * transaction.
         * <p>
         * If the request set the `include_child_receipts` flag, this list SHALL
         * include receipts for each child transaction executed as part of the
         * requested parent transaction.<br/>
         * If the request did not set the `include_child_receipts` flag, this
         * list SHALL be empty. <br/>
         * If the parent transaction did not initiate any child transactions
         * this list SHALL be empty.<br/>
         * If the `receipt` status is `UNKNOWN`, this list SHALL be empty.<br/>
         * This list SHALL be in order by consensus timestamp.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionGetReceiptResponse.
         * @memberof proto
         * @interface ITransactionGetReceiptResponse
         * @augments proto.TransactionGetReceiptResponse.$Properties
         * @deprecated Use proto.TransactionGetReceiptResponse.$Properties instead.
         */

        /**
         * Shape of a TransactionGetReceiptResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   receipt?: proto.TransactionReceipt.$Shape|null;
         *   duplicateTransactionReceipts?: Array.<proto.TransactionReceipt.$Shape>|null;
         *   childTransactionReceipts?: Array.<proto.TransactionReceipt.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TransactionGetReceiptResponse.$Shape
         */

        /**
         * Constructs a new TransactionGetReceiptResponse.
         * @memberof proto
         * @classdesc Response message for a `getTransactionReceipts` query.
         * 
         * The `receipt` field SHALL return the receipt for the "first" transaction
         * that matches the transaction identifier requested.<br/>
         * If receipts for duplicate transactions are requested, those duplicate
         * receipts SHALL be present in the `duplicateTransactionReceipts` list.<br/>
         * If receipts for child transactions are requested, those child receipts
         * SHALL be present in the `child_transaction_receipts` list.<br/>
         * A state proof SHALL NOT be provided for this response; transaction receipts
         * are not retained in network state.
         * 
         * <dl>
         * <dt>What is the "first" transaction?</dt>
         * <dd>The "first" transaction receipt SHALL be the receipt for the
         * first transaction with status that is neither
         * `INVALID_NODE_ACCOUNT` nor `INVALID_PAYER_SIGNATURE`.<br/>
         * If no transaction is found meeting the status criteria, the
         * "first" transaction SHALL be the first transaction by
         * consensus time.</dd>
         * <dt>What is a "child" transaction?</dt>
         * <dd>A "child" transaction is any transaction created in the process of
         * completing another transaction. These are most common with a smart
         * contract call, where a call to a contract may initiate one or more
         * additional transactions to complete a complex process.</dd>
         * </dl>
         * @constructor
         * @param {proto.TransactionGetReceiptResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionGetReceiptResponse = function (properties) {
            this.duplicateTransactionReceipts = [];
            this.childTransactionReceipts = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.TransactionGetReceiptResponse
         * @instance
         */
        TransactionGetReceiptResponse.prototype.header = null;

        /**
         * A transaction receipt.
         * <p>
         * This SHALL be the receipt for the "first" transaction that matches
         * the transaction identifier requested.<br/>
         * If the identified transaction has not reached consensus, this receipt
         * SHALL have a `status` of `UNKNOWN`.<br/>
         * If the identified transaction reached consensus prior to the current
         * configured receipt period (typically the last 180 seconds), this receipt
         * SHALL have a `status` of `UNKNOWN`.
         * @member {proto.TransactionReceipt.$Properties|null|undefined} receipt
         * @memberof proto.TransactionGetReceiptResponse
         * @instance
         */
        TransactionGetReceiptResponse.prototype.receipt = null;

        /**
         * A list of duplicate transaction receipts.
         * <p>
         * If the request set the `includeDuplicates` flat, this list SHALL
         * include the receipts for each duplicate transaction associated to the
         * requested transaction identifier.
         * If the request did not set the `includeDuplicates` flag, this list
         * SHALL be empty.<br/>
         * If the `receipt` status is `UNKNOWN`, this list SHALL be empty.<br/>
         * This list SHALL be in order by consensus timestamp.
         * @member {Array.<proto.TransactionReceipt.$Properties>} duplicateTransactionReceipts
         * @memberof proto.TransactionGetReceiptResponse
         * @instance
         */
        TransactionGetReceiptResponse.prototype.duplicateTransactionReceipts = $util.emptyArray;

        /**
         * A list of receipts for all child transactions spawned by the requested
         * transaction.
         * <p>
         * If the request set the `include_child_receipts` flag, this list SHALL
         * include receipts for each child transaction executed as part of the
         * requested parent transaction.<br/>
         * If the request did not set the `include_child_receipts` flag, this
         * list SHALL be empty. <br/>
         * If the parent transaction did not initiate any child transactions
         * this list SHALL be empty.<br/>
         * If the `receipt` status is `UNKNOWN`, this list SHALL be empty.<br/>
         * This list SHALL be in order by consensus timestamp.
         * @member {Array.<proto.TransactionReceipt.$Properties>} childTransactionReceipts
         * @memberof proto.TransactionGetReceiptResponse
         * @instance
         */
        TransactionGetReceiptResponse.prototype.childTransactionReceipts = $util.emptyArray;

        /**
         * Creates a new TransactionGetReceiptResponse instance using the specified properties.
         * @function create
         * @memberof proto.TransactionGetReceiptResponse
         * @static
         * @param {proto.TransactionGetReceiptResponse.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionGetReceiptResponse} TransactionGetReceiptResponse instance
         * @type {{
         *   (properties: proto.TransactionGetReceiptResponse.$Shape): proto.TransactionGetReceiptResponse & proto.TransactionGetReceiptResponse.$Shape;
         *   (properties?: proto.TransactionGetReceiptResponse.$Properties): proto.TransactionGetReceiptResponse;
         * }}
         */
        TransactionGetReceiptResponse.create = function(properties) {
            return new TransactionGetReceiptResponse(properties);
        };

        /**
         * Encodes the specified TransactionGetReceiptResponse message. Does not implicitly {@link proto.TransactionGetReceiptResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionGetReceiptResponse
         * @static
         * @param {proto.TransactionGetReceiptResponse.$Properties} message TransactionGetReceiptResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionGetReceiptResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt"))
                $root.proto.TransactionReceipt.encode(message.receipt, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.duplicateTransactionReceipts != null && message.duplicateTransactionReceipts.length)
                for (let i = 0; i < message.duplicateTransactionReceipts.length; ++i)
                    $root.proto.TransactionReceipt.encode(message.duplicateTransactionReceipts[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.childTransactionReceipts != null && message.childTransactionReceipts.length)
                for (let i = 0; i < message.childTransactionReceipts.length; ++i)
                    $root.proto.TransactionReceipt.encode(message.childTransactionReceipts[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionGetReceiptResponse message, length delimited. Does not implicitly {@link proto.TransactionGetReceiptResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionGetReceiptResponse
         * @static
         * @param {proto.TransactionGetReceiptResponse.$Properties} message TransactionGetReceiptResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionGetReceiptResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionGetReceiptResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionGetReceiptResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionGetReceiptResponse & proto.TransactionGetReceiptResponse.$Shape} TransactionGetReceiptResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionGetReceiptResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionGetReceiptResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.receipt = $root.proto.TransactionReceipt.decode(reader, reader.uint32(), $undefined, _depth + 1, message.receipt);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.duplicateTransactionReceipts && message.duplicateTransactionReceipts.length))
                            message.duplicateTransactionReceipts = [];
                        message.duplicateTransactionReceipts.push($root.proto.TransactionReceipt.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.childTransactionReceipts && message.childTransactionReceipts.length))
                            message.childTransactionReceipts = [];
                        message.childTransactionReceipts.push($root.proto.TransactionReceipt.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionGetReceiptResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionGetReceiptResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionGetReceiptResponse & proto.TransactionGetReceiptResponse.$Shape} TransactionGetReceiptResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionGetReceiptResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionGetReceiptResponse message.
         * @function verify
         * @memberof proto.TransactionGetReceiptResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionGetReceiptResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt")) {
                let error = $root.proto.TransactionReceipt.verify(message.receipt, _depth + 1);
                if (error)
                    return "receipt." + error;
            }
            if (message.duplicateTransactionReceipts != null && $Object.hasOwnProperty.call(message, "duplicateTransactionReceipts")) {
                if (!$Array.isArray(message.duplicateTransactionReceipts))
                    return "duplicateTransactionReceipts: array expected";
                for (let i = 0; i < message.duplicateTransactionReceipts.length; ++i) {
                    let error = $root.proto.TransactionReceipt.verify(message.duplicateTransactionReceipts[i], _depth + 1);
                    if (error)
                        return "duplicateTransactionReceipts." + error;
                }
            }
            if (message.childTransactionReceipts != null && $Object.hasOwnProperty.call(message, "childTransactionReceipts")) {
                if (!$Array.isArray(message.childTransactionReceipts))
                    return "childTransactionReceipts: array expected";
                for (let i = 0; i < message.childTransactionReceipts.length; ++i) {
                    let error = $root.proto.TransactionReceipt.verify(message.childTransactionReceipts[i], _depth + 1);
                    if (error)
                        return "childTransactionReceipts." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TransactionGetReceiptResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionGetReceiptResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionGetReceiptResponse} TransactionGetReceiptResponse
         */
        TransactionGetReceiptResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionGetReceiptResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionGetReceiptResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionGetReceiptResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TransactionGetReceiptResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.receipt != null) {
                if (!$util.isObject(object.receipt))
                    throw $TypeError(".proto.TransactionGetReceiptResponse.receipt: object expected");
                message.receipt = $root.proto.TransactionReceipt.fromObject(object.receipt, _depth + 1);
            }
            if (object.duplicateTransactionReceipts) {
                if (!$Array.isArray(object.duplicateTransactionReceipts))
                    throw $TypeError(".proto.TransactionGetReceiptResponse.duplicateTransactionReceipts: array expected");
                message.duplicateTransactionReceipts = $Array(object.duplicateTransactionReceipts.length);
                for (let i = 0; i < object.duplicateTransactionReceipts.length; ++i) {
                    if (!$util.isObject(object.duplicateTransactionReceipts[i]))
                        throw $TypeError(".proto.TransactionGetReceiptResponse.duplicateTransactionReceipts: object expected");
                    message.duplicateTransactionReceipts[i] = $root.proto.TransactionReceipt.fromObject(object.duplicateTransactionReceipts[i], _depth + 1);
                }
            }
            if (object.childTransactionReceipts) {
                if (!$Array.isArray(object.childTransactionReceipts))
                    throw $TypeError(".proto.TransactionGetReceiptResponse.childTransactionReceipts: array expected");
                message.childTransactionReceipts = $Array(object.childTransactionReceipts.length);
                for (let i = 0; i < object.childTransactionReceipts.length; ++i) {
                    if (!$util.isObject(object.childTransactionReceipts[i]))
                        throw $TypeError(".proto.TransactionGetReceiptResponse.childTransactionReceipts: object expected");
                    message.childTransactionReceipts[i] = $root.proto.TransactionReceipt.fromObject(object.childTransactionReceipts[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TransactionGetReceiptResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionGetReceiptResponse
         * @static
         * @param {proto.TransactionGetReceiptResponse} message TransactionGetReceiptResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionGetReceiptResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.duplicateTransactionReceipts = [];
                object.childTransactionReceipts = [];
            }
            if (options.defaults) {
                object.header = null;
                object.receipt = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.receipt != null && $Object.hasOwnProperty.call(message, "receipt"))
                object.receipt = $root.proto.TransactionReceipt.toObject(message.receipt, options, _depth + 1);
            if (message.duplicateTransactionReceipts && message.duplicateTransactionReceipts.length) {
                object.duplicateTransactionReceipts = $Array(message.duplicateTransactionReceipts.length);
                for (let j = 0; j < message.duplicateTransactionReceipts.length; ++j)
                    object.duplicateTransactionReceipts[j] = $root.proto.TransactionReceipt.toObject(message.duplicateTransactionReceipts[j], options, _depth + 1);
            }
            if (message.childTransactionReceipts && message.childTransactionReceipts.length) {
                object.childTransactionReceipts = $Array(message.childTransactionReceipts.length);
                for (let j = 0; j < message.childTransactionReceipts.length; ++j)
                    object.childTransactionReceipts[j] = $root.proto.TransactionReceipt.toObject(message.childTransactionReceipts[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TransactionGetReceiptResponse to JSON.
         * @function toJSON
         * @memberof proto.TransactionGetReceiptResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionGetReceiptResponse.prototype.toJSON = function() {
            return TransactionGetReceiptResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionGetReceiptResponse
         * @function getTypeUrl
         * @memberof proto.TransactionGetReceiptResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionGetReceiptResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionGetReceiptResponse";
        };

        return TransactionGetReceiptResponse;
    })();

    proto.TransactionGetRecordQuery = (function() {

        /**
         * Properties of a TransactionGetRecordQuery.
         * @typedef {Object} proto.TransactionGetRecordQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.TransactionID.$Properties|null} [transactionID] A transaction identifier.
         * <p>
         * This MUST contain the full identifier, as submitted, for the
         * transaction to query.
         * @property {boolean|null} [includeDuplicates] A flag to request duplicates.
         * <p>
         * If set, every transaction record within the record cache duration that
         * matches the requested transaction identifier SHALL be returned.<br/>
         * If not set, duplicate transactions SHALL NOT be returned.<br/>
         * If not set, only the record for the first matching transaction to
         * reach consensus SHALL be returned.
         * @property {boolean|null} [includeChildRecords] A flag to request "child" records.
         * <p>
         * If set, the response SHALL include records for each child transaction
         * executed as part of the requested parent transaction.<br/>
         * If not set, the response SHALL NOT include any records for child
         * transactions.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionGetRecordQuery.
         * @memberof proto
         * @interface ITransactionGetRecordQuery
         * @augments proto.TransactionGetRecordQuery.$Properties
         * @deprecated Use proto.TransactionGetRecordQuery.$Properties instead.
         */

        /**
         * Shape of a TransactionGetRecordQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   transactionID?: proto.TransactionID.$Shape|null;
         *   includeDuplicates?: boolean|null;
         *   includeChildRecords?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TransactionGetRecordQuery.$Shape
         */

        /**
         * Constructs a new TransactionGetRecordQuery.
         * @memberof proto
         * @classdesc Request for a `TransactionGetRecord` (a.k.a. `getTxRecordByTxID`) query.
         * <p>
         * A transaction record SHALL be available after the network reaches
         * consensus and completes execution for a transaction.<br/>
         * A transaction record SHALL NOT be available after the end of the network
         * configured "record cache duration".
         * 
         * <dl>
         * <dt>What is the "first" transaction?</dt>
         * <dd>The "first" transaction SHALL be the the transaction with
         * the earliest consensus time and a status that is neither
         * `INVALID_NODE_ACCOUNT` nor `INVALID_PAYER_SIGNATURE`.<br/>
         * If no transaction is found meeting this status criteria, the
         * "first" transaction SHALL be the transaction with the earliest
         * consensus time.</dd>
         * <dt>What is a "child" transaction?</dt>
         * <dd>A "child" transaction is any transaction created in the process of
         * completing another transaction. These are most common with a smart
         * contract call, where a call to a contract may initiate one or more
         * additional transactions to complete a complex process.</dd>
         * </dl>
         * @constructor
         * @param {proto.TransactionGetRecordQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionGetRecordQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.TransactionGetRecordQuery
         * @instance
         */
        TransactionGetRecordQuery.prototype.header = null;

        /**
         * A transaction identifier.
         * <p>
         * This MUST contain the full identifier, as submitted, for the
         * transaction to query.
         * @member {proto.TransactionID.$Properties|null|undefined} transactionID
         * @memberof proto.TransactionGetRecordQuery
         * @instance
         */
        TransactionGetRecordQuery.prototype.transactionID = null;

        /**
         * A flag to request duplicates.
         * <p>
         * If set, every transaction record within the record cache duration that
         * matches the requested transaction identifier SHALL be returned.<br/>
         * If not set, duplicate transactions SHALL NOT be returned.<br/>
         * If not set, only the record for the first matching transaction to
         * reach consensus SHALL be returned.
         * @member {boolean} includeDuplicates
         * @memberof proto.TransactionGetRecordQuery
         * @instance
         */
        TransactionGetRecordQuery.prototype.includeDuplicates = false;

        /**
         * A flag to request "child" records.
         * <p>
         * If set, the response SHALL include records for each child transaction
         * executed as part of the requested parent transaction.<br/>
         * If not set, the response SHALL NOT include any records for child
         * transactions.
         * @member {boolean} includeChildRecords
         * @memberof proto.TransactionGetRecordQuery
         * @instance
         */
        TransactionGetRecordQuery.prototype.includeChildRecords = false;

        /**
         * Creates a new TransactionGetRecordQuery instance using the specified properties.
         * @function create
         * @memberof proto.TransactionGetRecordQuery
         * @static
         * @param {proto.TransactionGetRecordQuery.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionGetRecordQuery} TransactionGetRecordQuery instance
         * @type {{
         *   (properties: proto.TransactionGetRecordQuery.$Shape): proto.TransactionGetRecordQuery & proto.TransactionGetRecordQuery.$Shape;
         *   (properties?: proto.TransactionGetRecordQuery.$Properties): proto.TransactionGetRecordQuery;
         * }}
         */
        TransactionGetRecordQuery.create = function(properties) {
            return new TransactionGetRecordQuery(properties);
        };

        /**
         * Encodes the specified TransactionGetRecordQuery message. Does not implicitly {@link proto.TransactionGetRecordQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionGetRecordQuery
         * @static
         * @param {proto.TransactionGetRecordQuery.$Properties} message TransactionGetRecordQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionGetRecordQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID"))
                $root.proto.TransactionID.encode(message.transactionID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.includeDuplicates != null && $Object.hasOwnProperty.call(message, "includeDuplicates") && message.includeDuplicates !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.includeDuplicates);
            if (message.includeChildRecords != null && $Object.hasOwnProperty.call(message, "includeChildRecords") && message.includeChildRecords !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.includeChildRecords);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionGetRecordQuery message, length delimited. Does not implicitly {@link proto.TransactionGetRecordQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionGetRecordQuery
         * @static
         * @param {proto.TransactionGetRecordQuery.$Properties} message TransactionGetRecordQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionGetRecordQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionGetRecordQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionGetRecordQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionGetRecordQuery & proto.TransactionGetRecordQuery.$Shape} TransactionGetRecordQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionGetRecordQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionGetRecordQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.transactionID = $root.proto.TransactionID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.includeDuplicates = value;
                        else
                            delete message.includeDuplicates;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.includeChildRecords = value;
                        else
                            delete message.includeChildRecords;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionGetRecordQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionGetRecordQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionGetRecordQuery & proto.TransactionGetRecordQuery.$Shape} TransactionGetRecordQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionGetRecordQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionGetRecordQuery message.
         * @function verify
         * @memberof proto.TransactionGetRecordQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionGetRecordQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID")) {
                let error = $root.proto.TransactionID.verify(message.transactionID, _depth + 1);
                if (error)
                    return "transactionID." + error;
            }
            if (message.includeDuplicates != null && $Object.hasOwnProperty.call(message, "includeDuplicates"))
                if (typeof message.includeDuplicates !== "boolean")
                    return "includeDuplicates: boolean expected";
            if (message.includeChildRecords != null && $Object.hasOwnProperty.call(message, "includeChildRecords"))
                if (typeof message.includeChildRecords !== "boolean")
                    return "includeChildRecords: boolean expected";
            return null;
        };

        /**
         * Creates a TransactionGetRecordQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionGetRecordQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionGetRecordQuery} TransactionGetRecordQuery
         */
        TransactionGetRecordQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionGetRecordQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionGetRecordQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionGetRecordQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TransactionGetRecordQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.transactionID != null) {
                if (!$util.isObject(object.transactionID))
                    throw $TypeError(".proto.TransactionGetRecordQuery.transactionID: object expected");
                message.transactionID = $root.proto.TransactionID.fromObject(object.transactionID, _depth + 1);
            }
            if (object.includeDuplicates != null)
                if (object.includeDuplicates)
                    message.includeDuplicates = $Boolean(object.includeDuplicates);
            if (object.includeChildRecords != null)
                if (object.includeChildRecords)
                    message.includeChildRecords = $Boolean(object.includeChildRecords);
            return message;
        };

        /**
         * Creates a plain object from a TransactionGetRecordQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionGetRecordQuery
         * @static
         * @param {proto.TransactionGetRecordQuery} message TransactionGetRecordQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionGetRecordQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.transactionID = null;
                object.includeDuplicates = false;
                object.includeChildRecords = false;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID"))
                object.transactionID = $root.proto.TransactionID.toObject(message.transactionID, options, _depth + 1);
            if (message.includeDuplicates != null && $Object.hasOwnProperty.call(message, "includeDuplicates"))
                object.includeDuplicates = message.includeDuplicates;
            if (message.includeChildRecords != null && $Object.hasOwnProperty.call(message, "includeChildRecords"))
                object.includeChildRecords = message.includeChildRecords;
            return object;
        };

        /**
         * Converts this TransactionGetRecordQuery to JSON.
         * @function toJSON
         * @memberof proto.TransactionGetRecordQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionGetRecordQuery.prototype.toJSON = function() {
            return TransactionGetRecordQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionGetRecordQuery
         * @function getTypeUrl
         * @memberof proto.TransactionGetRecordQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionGetRecordQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionGetRecordQuery";
        };

        return TransactionGetRecordQuery;
    })();

    proto.TransactionGetRecordResponse = (function() {

        /**
         * Properties of a TransactionGetRecordResponse.
         * @typedef {Object} proto.TransactionGetRecordResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.TransactionRecord.$Properties|null} [transactionRecord] A transaction record.
         * <p>
         * This SHALL be the record for the "first" transaction that matches
         * the transaction identifier requested.<br/>
         * If the identified transaction has not reached consensus, this
         * record SHALL have a `status` of `UNKNOWN`.<br/>
         * If the identified transaction reached consensus prior to the
         * current configured record cache duration, this record SHALL
         * have a `status` of `UNKNOWN`.
         * @property {Array.<proto.TransactionRecord.$Properties>|null} [duplicateTransactionRecords] A list of duplicate transaction records.
         * <p>
         * If the request set the `includeDuplicates` flat, this list SHALL
         * include the records for each duplicate transaction associated to the
         * requested transaction identifier.
         * If the request did not set the `includeDuplicates` flag, this list
         * SHALL be empty.<br/>
         * If the `transactionRecord` status is `UNKNOWN`, this list
         * SHALL be empty.<br/>
         * This list SHALL be in order by consensus timestamp.
         * @property {Array.<proto.TransactionRecord.$Properties>|null} [childTransactionRecords] A list of records for all child transactions spawned by the requested
         * transaction.
         * <p>
         * If the request set the `include_child_records` flag, this list SHALL
         * include records for each child transaction executed as part of the
         * requested parent transaction.<br/>
         * If the request did not set the `include_child_records` flag, this
         * list SHALL be empty. <br/>
         * If the parent transaction did not initiate any child transactions
         * this list SHALL be empty.<br/>
         * If the `transactionRecord` status is `UNKNOWN`, this list
         * SHALL be empty.<br/>
         * This list SHALL be in order by consensus timestamp.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionGetRecordResponse.
         * @memberof proto
         * @interface ITransactionGetRecordResponse
         * @augments proto.TransactionGetRecordResponse.$Properties
         * @deprecated Use proto.TransactionGetRecordResponse.$Properties instead.
         */

        /**
         * Shape of a TransactionGetRecordResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   transactionRecord?: proto.TransactionRecord.$Shape|null;
         *   duplicateTransactionRecords?: Array.<proto.TransactionRecord.$Shape>|null;
         *   childTransactionRecords?: Array.<proto.TransactionRecord.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TransactionGetRecordResponse.$Shape
         */

        /**
         * Constructs a new TransactionGetRecordResponse.
         * @memberof proto
         * @classdesc Response message for a `getTxRecordByTxID` query.
         * 
         * The `transactionRecord` field SHALL return the record for the "first"
         * transaction that matches the transaction identifier requested.<br/>
         * If records for duplicate transactions are requested, those duplicate
         * records SHALL be present in the `duplicateTransactionReceipts` list.<br/>
         * If records for child transactions are requested, those child records SHALL
         * be present in the `child_transaction_records` list.<br/>
         * A state proof MAY be provided for this response; provided the record is
         * still available from the consensus nodes.
         * 
         * <dl>
         * <dt>What is the "first" transaction?</dt>
         * <dd>The "first" transaction receipt SHALL be the receipt for the
         * first transaction with status that is neither
         * `INVALID_NODE_ACCOUNT` nor `INVALID_PAYER_SIGNATURE`.<br/>
         * If no transaction is found meeting the status criteria, the
         * "first" transaction SHALL be the first transaction by
         * consensus time.</dd>
         * <dt>What is a "child" transaction?</dt>
         * <dd>A "child" transaction is any transaction created in the process of
         * completing another transaction. These are most common with a smart
         * contract call, where a call to a contract may initiate one or more
         * additional transactions to complete a complex process.</dd>
         * </dl>
         * @constructor
         * @param {proto.TransactionGetRecordResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionGetRecordResponse = function (properties) {
            this.duplicateTransactionRecords = [];
            this.childTransactionRecords = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.TransactionGetRecordResponse
         * @instance
         */
        TransactionGetRecordResponse.prototype.header = null;

        /**
         * A transaction record.
         * <p>
         * This SHALL be the record for the "first" transaction that matches
         * the transaction identifier requested.<br/>
         * If the identified transaction has not reached consensus, this
         * record SHALL have a `status` of `UNKNOWN`.<br/>
         * If the identified transaction reached consensus prior to the
         * current configured record cache duration, this record SHALL
         * have a `status` of `UNKNOWN`.
         * @member {proto.TransactionRecord.$Properties|null|undefined} transactionRecord
         * @memberof proto.TransactionGetRecordResponse
         * @instance
         */
        TransactionGetRecordResponse.prototype.transactionRecord = null;

        /**
         * A list of duplicate transaction records.
         * <p>
         * If the request set the `includeDuplicates` flat, this list SHALL
         * include the records for each duplicate transaction associated to the
         * requested transaction identifier.
         * If the request did not set the `includeDuplicates` flag, this list
         * SHALL be empty.<br/>
         * If the `transactionRecord` status is `UNKNOWN`, this list
         * SHALL be empty.<br/>
         * This list SHALL be in order by consensus timestamp.
         * @member {Array.<proto.TransactionRecord.$Properties>} duplicateTransactionRecords
         * @memberof proto.TransactionGetRecordResponse
         * @instance
         */
        TransactionGetRecordResponse.prototype.duplicateTransactionRecords = $util.emptyArray;

        /**
         * A list of records for all child transactions spawned by the requested
         * transaction.
         * <p>
         * If the request set the `include_child_records` flag, this list SHALL
         * include records for each child transaction executed as part of the
         * requested parent transaction.<br/>
         * If the request did not set the `include_child_records` flag, this
         * list SHALL be empty. <br/>
         * If the parent transaction did not initiate any child transactions
         * this list SHALL be empty.<br/>
         * If the `transactionRecord` status is `UNKNOWN`, this list
         * SHALL be empty.<br/>
         * This list SHALL be in order by consensus timestamp.
         * @member {Array.<proto.TransactionRecord.$Properties>} childTransactionRecords
         * @memberof proto.TransactionGetRecordResponse
         * @instance
         */
        TransactionGetRecordResponse.prototype.childTransactionRecords = $util.emptyArray;

        /**
         * Creates a new TransactionGetRecordResponse instance using the specified properties.
         * @function create
         * @memberof proto.TransactionGetRecordResponse
         * @static
         * @param {proto.TransactionGetRecordResponse.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionGetRecordResponse} TransactionGetRecordResponse instance
         * @type {{
         *   (properties: proto.TransactionGetRecordResponse.$Shape): proto.TransactionGetRecordResponse & proto.TransactionGetRecordResponse.$Shape;
         *   (properties?: proto.TransactionGetRecordResponse.$Properties): proto.TransactionGetRecordResponse;
         * }}
         */
        TransactionGetRecordResponse.create = function(properties) {
            return new TransactionGetRecordResponse(properties);
        };

        /**
         * Encodes the specified TransactionGetRecordResponse message. Does not implicitly {@link proto.TransactionGetRecordResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionGetRecordResponse
         * @static
         * @param {proto.TransactionGetRecordResponse.$Properties} message TransactionGetRecordResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionGetRecordResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.transactionRecord != null && $Object.hasOwnProperty.call(message, "transactionRecord"))
                $root.proto.TransactionRecord.encode(message.transactionRecord, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.duplicateTransactionRecords != null && message.duplicateTransactionRecords.length)
                for (let i = 0; i < message.duplicateTransactionRecords.length; ++i)
                    $root.proto.TransactionRecord.encode(message.duplicateTransactionRecords[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.childTransactionRecords != null && message.childTransactionRecords.length)
                for (let i = 0; i < message.childTransactionRecords.length; ++i)
                    $root.proto.TransactionRecord.encode(message.childTransactionRecords[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionGetRecordResponse message, length delimited. Does not implicitly {@link proto.TransactionGetRecordResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionGetRecordResponse
         * @static
         * @param {proto.TransactionGetRecordResponse.$Properties} message TransactionGetRecordResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionGetRecordResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionGetRecordResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionGetRecordResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionGetRecordResponse & proto.TransactionGetRecordResponse.$Shape} TransactionGetRecordResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionGetRecordResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionGetRecordResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.transactionRecord = $root.proto.TransactionRecord.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionRecord);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.duplicateTransactionRecords && message.duplicateTransactionRecords.length))
                            message.duplicateTransactionRecords = [];
                        message.duplicateTransactionRecords.push($root.proto.TransactionRecord.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.childTransactionRecords && message.childTransactionRecords.length))
                            message.childTransactionRecords = [];
                        message.childTransactionRecords.push($root.proto.TransactionRecord.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionGetRecordResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionGetRecordResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionGetRecordResponse & proto.TransactionGetRecordResponse.$Shape} TransactionGetRecordResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionGetRecordResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionGetRecordResponse message.
         * @function verify
         * @memberof proto.TransactionGetRecordResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionGetRecordResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.transactionRecord != null && $Object.hasOwnProperty.call(message, "transactionRecord")) {
                let error = $root.proto.TransactionRecord.verify(message.transactionRecord, _depth + 1);
                if (error)
                    return "transactionRecord." + error;
            }
            if (message.duplicateTransactionRecords != null && $Object.hasOwnProperty.call(message, "duplicateTransactionRecords")) {
                if (!$Array.isArray(message.duplicateTransactionRecords))
                    return "duplicateTransactionRecords: array expected";
                for (let i = 0; i < message.duplicateTransactionRecords.length; ++i) {
                    let error = $root.proto.TransactionRecord.verify(message.duplicateTransactionRecords[i], _depth + 1);
                    if (error)
                        return "duplicateTransactionRecords." + error;
                }
            }
            if (message.childTransactionRecords != null && $Object.hasOwnProperty.call(message, "childTransactionRecords")) {
                if (!$Array.isArray(message.childTransactionRecords))
                    return "childTransactionRecords: array expected";
                for (let i = 0; i < message.childTransactionRecords.length; ++i) {
                    let error = $root.proto.TransactionRecord.verify(message.childTransactionRecords[i], _depth + 1);
                    if (error)
                        return "childTransactionRecords." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TransactionGetRecordResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionGetRecordResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionGetRecordResponse} TransactionGetRecordResponse
         */
        TransactionGetRecordResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionGetRecordResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionGetRecordResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionGetRecordResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TransactionGetRecordResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.transactionRecord != null) {
                if (!$util.isObject(object.transactionRecord))
                    throw $TypeError(".proto.TransactionGetRecordResponse.transactionRecord: object expected");
                message.transactionRecord = $root.proto.TransactionRecord.fromObject(object.transactionRecord, _depth + 1);
            }
            if (object.duplicateTransactionRecords) {
                if (!$Array.isArray(object.duplicateTransactionRecords))
                    throw $TypeError(".proto.TransactionGetRecordResponse.duplicateTransactionRecords: array expected");
                message.duplicateTransactionRecords = $Array(object.duplicateTransactionRecords.length);
                for (let i = 0; i < object.duplicateTransactionRecords.length; ++i) {
                    if (!$util.isObject(object.duplicateTransactionRecords[i]))
                        throw $TypeError(".proto.TransactionGetRecordResponse.duplicateTransactionRecords: object expected");
                    message.duplicateTransactionRecords[i] = $root.proto.TransactionRecord.fromObject(object.duplicateTransactionRecords[i], _depth + 1);
                }
            }
            if (object.childTransactionRecords) {
                if (!$Array.isArray(object.childTransactionRecords))
                    throw $TypeError(".proto.TransactionGetRecordResponse.childTransactionRecords: array expected");
                message.childTransactionRecords = $Array(object.childTransactionRecords.length);
                for (let i = 0; i < object.childTransactionRecords.length; ++i) {
                    if (!$util.isObject(object.childTransactionRecords[i]))
                        throw $TypeError(".proto.TransactionGetRecordResponse.childTransactionRecords: object expected");
                    message.childTransactionRecords[i] = $root.proto.TransactionRecord.fromObject(object.childTransactionRecords[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TransactionGetRecordResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionGetRecordResponse
         * @static
         * @param {proto.TransactionGetRecordResponse} message TransactionGetRecordResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionGetRecordResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.duplicateTransactionRecords = [];
                object.childTransactionRecords = [];
            }
            if (options.defaults) {
                object.header = null;
                object.transactionRecord = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.transactionRecord != null && $Object.hasOwnProperty.call(message, "transactionRecord"))
                object.transactionRecord = $root.proto.TransactionRecord.toObject(message.transactionRecord, options, _depth + 1);
            if (message.duplicateTransactionRecords && message.duplicateTransactionRecords.length) {
                object.duplicateTransactionRecords = $Array(message.duplicateTransactionRecords.length);
                for (let j = 0; j < message.duplicateTransactionRecords.length; ++j)
                    object.duplicateTransactionRecords[j] = $root.proto.TransactionRecord.toObject(message.duplicateTransactionRecords[j], options, _depth + 1);
            }
            if (message.childTransactionRecords && message.childTransactionRecords.length) {
                object.childTransactionRecords = $Array(message.childTransactionRecords.length);
                for (let j = 0; j < message.childTransactionRecords.length; ++j)
                    object.childTransactionRecords[j] = $root.proto.TransactionRecord.toObject(message.childTransactionRecords[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TransactionGetRecordResponse to JSON.
         * @function toJSON
         * @memberof proto.TransactionGetRecordResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionGetRecordResponse.prototype.toJSON = function() {
            return TransactionGetRecordResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionGetRecordResponse
         * @function getTypeUrl
         * @memberof proto.TransactionGetRecordResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionGetRecordResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionGetRecordResponse";
        };

        return TransactionGetRecordResponse;
    })();

    proto.TransactionGetFastRecordQuery = (function() {

        /**
         * Properties of a TransactionGetFastRecordQuery.
         * @typedef {Object} proto.TransactionGetFastRecordQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.TransactionID.$Properties|null} [transactionID] The ID of the transaction for which the record is requested.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionGetFastRecordQuery.
         * @memberof proto
         * @interface ITransactionGetFastRecordQuery
         * @augments proto.TransactionGetFastRecordQuery.$Properties
         * @deprecated Use proto.TransactionGetFastRecordQuery.$Properties instead.
         */

        /**
         * Shape of a TransactionGetFastRecordQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   transactionID?: proto.TransactionID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TransactionGetFastRecordQuery.$Shape
         */

        /**
         * Constructs a new TransactionGetFastRecordQuery.
         * @memberof proto
         * @classdesc Get the tx record of a transaction, given its transaction ID.
         * 
         * Once a transaction reaches consensus, then information about whether it
         * succeeded or failed will be available until the end of the receipt period.
         * Before and after the receipt period, and for a transaction that was never
         * submitted, the receipt is unknown.<br/>
         * This query is free (the payment field is left empty).
         * @constructor
         * @param {proto.TransactionGetFastRecordQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionGetFastRecordQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.TransactionGetFastRecordQuery
         * @instance
         */
        TransactionGetFastRecordQuery.prototype.header = null;

        /**
         * The ID of the transaction for which the record is requested.
         * @member {proto.TransactionID.$Properties|null|undefined} transactionID
         * @memberof proto.TransactionGetFastRecordQuery
         * @instance
         */
        TransactionGetFastRecordQuery.prototype.transactionID = null;

        /**
         * Creates a new TransactionGetFastRecordQuery instance using the specified properties.
         * @function create
         * @memberof proto.TransactionGetFastRecordQuery
         * @static
         * @param {proto.TransactionGetFastRecordQuery.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionGetFastRecordQuery} TransactionGetFastRecordQuery instance
         * @type {{
         *   (properties: proto.TransactionGetFastRecordQuery.$Shape): proto.TransactionGetFastRecordQuery & proto.TransactionGetFastRecordQuery.$Shape;
         *   (properties?: proto.TransactionGetFastRecordQuery.$Properties): proto.TransactionGetFastRecordQuery;
         * }}
         */
        TransactionGetFastRecordQuery.create = function(properties) {
            return new TransactionGetFastRecordQuery(properties);
        };

        /**
         * Encodes the specified TransactionGetFastRecordQuery message. Does not implicitly {@link proto.TransactionGetFastRecordQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionGetFastRecordQuery
         * @static
         * @param {proto.TransactionGetFastRecordQuery.$Properties} message TransactionGetFastRecordQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionGetFastRecordQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID"))
                $root.proto.TransactionID.encode(message.transactionID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionGetFastRecordQuery message, length delimited. Does not implicitly {@link proto.TransactionGetFastRecordQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionGetFastRecordQuery
         * @static
         * @param {proto.TransactionGetFastRecordQuery.$Properties} message TransactionGetFastRecordQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionGetFastRecordQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionGetFastRecordQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionGetFastRecordQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionGetFastRecordQuery & proto.TransactionGetFastRecordQuery.$Shape} TransactionGetFastRecordQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionGetFastRecordQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionGetFastRecordQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.transactionID = $root.proto.TransactionID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionGetFastRecordQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionGetFastRecordQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionGetFastRecordQuery & proto.TransactionGetFastRecordQuery.$Shape} TransactionGetFastRecordQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionGetFastRecordQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionGetFastRecordQuery message.
         * @function verify
         * @memberof proto.TransactionGetFastRecordQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionGetFastRecordQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID")) {
                let error = $root.proto.TransactionID.verify(message.transactionID, _depth + 1);
                if (error)
                    return "transactionID." + error;
            }
            return null;
        };

        /**
         * Creates a TransactionGetFastRecordQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionGetFastRecordQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionGetFastRecordQuery} TransactionGetFastRecordQuery
         */
        TransactionGetFastRecordQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionGetFastRecordQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionGetFastRecordQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionGetFastRecordQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TransactionGetFastRecordQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.transactionID != null) {
                if (!$util.isObject(object.transactionID))
                    throw $TypeError(".proto.TransactionGetFastRecordQuery.transactionID: object expected");
                message.transactionID = $root.proto.TransactionID.fromObject(object.transactionID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TransactionGetFastRecordQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionGetFastRecordQuery
         * @static
         * @param {proto.TransactionGetFastRecordQuery} message TransactionGetFastRecordQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionGetFastRecordQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.transactionID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.transactionID != null && $Object.hasOwnProperty.call(message, "transactionID"))
                object.transactionID = $root.proto.TransactionID.toObject(message.transactionID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TransactionGetFastRecordQuery to JSON.
         * @function toJSON
         * @memberof proto.TransactionGetFastRecordQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionGetFastRecordQuery.prototype.toJSON = function() {
            return TransactionGetFastRecordQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionGetFastRecordQuery
         * @function getTypeUrl
         * @memberof proto.TransactionGetFastRecordQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionGetFastRecordQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionGetFastRecordQuery";
        };

        return TransactionGetFastRecordQuery;
    })();

    proto.TransactionGetFastRecordResponse = (function() {

        /**
         * Properties of a TransactionGetFastRecordResponse.
         * @typedef {Object} proto.TransactionGetFastRecordResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.TransactionRecord.$Properties|null} [transactionRecord] The requested transaction records
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionGetFastRecordResponse.
         * @memberof proto
         * @interface ITransactionGetFastRecordResponse
         * @augments proto.TransactionGetFastRecordResponse.$Properties
         * @deprecated Use proto.TransactionGetFastRecordResponse.$Properties instead.
         */

        /**
         * Shape of a TransactionGetFastRecordResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   transactionRecord?: proto.TransactionRecord.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TransactionGetFastRecordResponse.$Shape
         */

        /**
         * Constructs a new TransactionGetFastRecordResponse.
         * @memberof proto
         * @classdesc Response when the client sends the node TransactionGetFastRecordQuery.
         * If it created a new entity (account, file, or smart contract instance) then
         * one of the three ID fields will be filled in with the ID of the new entity.
         * Sometimes a single transaction will create more than one new entity, such
         * as when a new contract instance is created, and this also creates the new
         * account that it owned by that instance.
         * @constructor
         * @param {proto.TransactionGetFastRecordResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionGetFastRecordResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.TransactionGetFastRecordResponse
         * @instance
         */
        TransactionGetFastRecordResponse.prototype.header = null;

        /**
         * The requested transaction records
         * @member {proto.TransactionRecord.$Properties|null|undefined} transactionRecord
         * @memberof proto.TransactionGetFastRecordResponse
         * @instance
         */
        TransactionGetFastRecordResponse.prototype.transactionRecord = null;

        /**
         * Creates a new TransactionGetFastRecordResponse instance using the specified properties.
         * @function create
         * @memberof proto.TransactionGetFastRecordResponse
         * @static
         * @param {proto.TransactionGetFastRecordResponse.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionGetFastRecordResponse} TransactionGetFastRecordResponse instance
         * @type {{
         *   (properties: proto.TransactionGetFastRecordResponse.$Shape): proto.TransactionGetFastRecordResponse & proto.TransactionGetFastRecordResponse.$Shape;
         *   (properties?: proto.TransactionGetFastRecordResponse.$Properties): proto.TransactionGetFastRecordResponse;
         * }}
         */
        TransactionGetFastRecordResponse.create = function(properties) {
            return new TransactionGetFastRecordResponse(properties);
        };

        /**
         * Encodes the specified TransactionGetFastRecordResponse message. Does not implicitly {@link proto.TransactionGetFastRecordResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionGetFastRecordResponse
         * @static
         * @param {proto.TransactionGetFastRecordResponse.$Properties} message TransactionGetFastRecordResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionGetFastRecordResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.transactionRecord != null && $Object.hasOwnProperty.call(message, "transactionRecord"))
                $root.proto.TransactionRecord.encode(message.transactionRecord, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionGetFastRecordResponse message, length delimited. Does not implicitly {@link proto.TransactionGetFastRecordResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionGetFastRecordResponse
         * @static
         * @param {proto.TransactionGetFastRecordResponse.$Properties} message TransactionGetFastRecordResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionGetFastRecordResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionGetFastRecordResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionGetFastRecordResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionGetFastRecordResponse & proto.TransactionGetFastRecordResponse.$Shape} TransactionGetFastRecordResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionGetFastRecordResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionGetFastRecordResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.transactionRecord = $root.proto.TransactionRecord.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionRecord);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionGetFastRecordResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionGetFastRecordResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionGetFastRecordResponse & proto.TransactionGetFastRecordResponse.$Shape} TransactionGetFastRecordResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionGetFastRecordResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionGetFastRecordResponse message.
         * @function verify
         * @memberof proto.TransactionGetFastRecordResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionGetFastRecordResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.transactionRecord != null && $Object.hasOwnProperty.call(message, "transactionRecord")) {
                let error = $root.proto.TransactionRecord.verify(message.transactionRecord, _depth + 1);
                if (error)
                    return "transactionRecord." + error;
            }
            return null;
        };

        /**
         * Creates a TransactionGetFastRecordResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionGetFastRecordResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionGetFastRecordResponse} TransactionGetFastRecordResponse
         */
        TransactionGetFastRecordResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionGetFastRecordResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionGetFastRecordResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionGetFastRecordResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TransactionGetFastRecordResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.transactionRecord != null) {
                if (!$util.isObject(object.transactionRecord))
                    throw $TypeError(".proto.TransactionGetFastRecordResponse.transactionRecord: object expected");
                message.transactionRecord = $root.proto.TransactionRecord.fromObject(object.transactionRecord, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TransactionGetFastRecordResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionGetFastRecordResponse
         * @static
         * @param {proto.TransactionGetFastRecordResponse} message TransactionGetFastRecordResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionGetFastRecordResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.transactionRecord = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.transactionRecord != null && $Object.hasOwnProperty.call(message, "transactionRecord"))
                object.transactionRecord = $root.proto.TransactionRecord.toObject(message.transactionRecord, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TransactionGetFastRecordResponse to JSON.
         * @function toJSON
         * @memberof proto.TransactionGetFastRecordResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionGetFastRecordResponse.prototype.toJSON = function() {
            return TransactionGetFastRecordResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionGetFastRecordResponse
         * @function getTypeUrl
         * @memberof proto.TransactionGetFastRecordResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionGetFastRecordResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionGetFastRecordResponse";
        };

        return TransactionGetFastRecordResponse;
    })();

    proto.NetworkGetVersionInfoQuery = (function() {

        /**
         * Properties of a NetworkGetVersionInfoQuery.
         * @typedef {Object} proto.NetworkGetVersionInfoQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NetworkGetVersionInfoQuery.
         * @memberof proto
         * @interface INetworkGetVersionInfoQuery
         * @augments proto.NetworkGetVersionInfoQuery.$Properties
         * @deprecated Use proto.NetworkGetVersionInfoQuery.$Properties instead.
         */

        /**
         * Shape of a NetworkGetVersionInfoQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.NetworkGetVersionInfoQuery.$Shape
         */

        /**
         * Constructs a new NetworkGetVersionInfoQuery.
         * @memberof proto
         * @classdesc Query the deployed versions of Hedera Services and the API definitions in
         * semantic version format
         * @constructor
         * @param {proto.NetworkGetVersionInfoQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NetworkGetVersionInfoQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.NetworkGetVersionInfoQuery
         * @instance
         */
        NetworkGetVersionInfoQuery.prototype.header = null;

        /**
         * Creates a new NetworkGetVersionInfoQuery instance using the specified properties.
         * @function create
         * @memberof proto.NetworkGetVersionInfoQuery
         * @static
         * @param {proto.NetworkGetVersionInfoQuery.$Properties=} [properties] Properties to set
         * @returns {proto.NetworkGetVersionInfoQuery} NetworkGetVersionInfoQuery instance
         * @type {{
         *   (properties: proto.NetworkGetVersionInfoQuery.$Shape): proto.NetworkGetVersionInfoQuery & proto.NetworkGetVersionInfoQuery.$Shape;
         *   (properties?: proto.NetworkGetVersionInfoQuery.$Properties): proto.NetworkGetVersionInfoQuery;
         * }}
         */
        NetworkGetVersionInfoQuery.create = function(properties) {
            return new NetworkGetVersionInfoQuery(properties);
        };

        /**
         * Encodes the specified NetworkGetVersionInfoQuery message. Does not implicitly {@link proto.NetworkGetVersionInfoQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.NetworkGetVersionInfoQuery
         * @static
         * @param {proto.NetworkGetVersionInfoQuery.$Properties} message NetworkGetVersionInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkGetVersionInfoQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NetworkGetVersionInfoQuery message, length delimited. Does not implicitly {@link proto.NetworkGetVersionInfoQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NetworkGetVersionInfoQuery
         * @static
         * @param {proto.NetworkGetVersionInfoQuery.$Properties} message NetworkGetVersionInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkGetVersionInfoQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NetworkGetVersionInfoQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NetworkGetVersionInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NetworkGetVersionInfoQuery & proto.NetworkGetVersionInfoQuery.$Shape} NetworkGetVersionInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkGetVersionInfoQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NetworkGetVersionInfoQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NetworkGetVersionInfoQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NetworkGetVersionInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NetworkGetVersionInfoQuery & proto.NetworkGetVersionInfoQuery.$Shape} NetworkGetVersionInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkGetVersionInfoQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NetworkGetVersionInfoQuery message.
         * @function verify
         * @memberof proto.NetworkGetVersionInfoQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NetworkGetVersionInfoQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            return null;
        };

        /**
         * Creates a NetworkGetVersionInfoQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NetworkGetVersionInfoQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NetworkGetVersionInfoQuery} NetworkGetVersionInfoQuery
         */
        NetworkGetVersionInfoQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NetworkGetVersionInfoQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NetworkGetVersionInfoQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NetworkGetVersionInfoQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.NetworkGetVersionInfoQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a NetworkGetVersionInfoQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NetworkGetVersionInfoQuery
         * @static
         * @param {proto.NetworkGetVersionInfoQuery} message NetworkGetVersionInfoQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NetworkGetVersionInfoQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.header = null;
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            return object;
        };

        /**
         * Converts this NetworkGetVersionInfoQuery to JSON.
         * @function toJSON
         * @memberof proto.NetworkGetVersionInfoQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NetworkGetVersionInfoQuery.prototype.toJSON = function() {
            return NetworkGetVersionInfoQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NetworkGetVersionInfoQuery
         * @function getTypeUrl
         * @memberof proto.NetworkGetVersionInfoQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NetworkGetVersionInfoQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NetworkGetVersionInfoQuery";
        };

        return NetworkGetVersionInfoQuery;
    })();

    proto.NetworkGetVersionInfoResponse = (function() {

        /**
         * Properties of a NetworkGetVersionInfoResponse.
         * @typedef {Object} proto.NetworkGetVersionInfoResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.SemanticVersion.$Properties|null} [hapiProtoVersion] An API version.
         * <p>
         * This SHALL be the current Hedera API (HAPI) protobuf message version
         * accepted by the network.
         * @property {proto.SemanticVersion.$Properties|null} [hederaServicesVersion] A Services version.
         * <p>
         * This SHALL be the current version of the Hedera Services software
         * operating the network.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NetworkGetVersionInfoResponse.
         * @memberof proto
         * @interface INetworkGetVersionInfoResponse
         * @augments proto.NetworkGetVersionInfoResponse.$Properties
         * @deprecated Use proto.NetworkGetVersionInfoResponse.$Properties instead.
         */

        /**
         * Shape of a NetworkGetVersionInfoResponse.
         * @typedef {proto.NetworkGetVersionInfoResponse.$Properties} proto.NetworkGetVersionInfoResponse.$Shape
         */

        /**
         * Constructs a new NetworkGetVersionInfoResponse.
         * @memberof proto
         * @classdesc A response to a `NetworkGetVersionInfoQuery`.
         * 
         * This SHALL return `SemanticVersion` information for both Hedera API (HAPI)
         * and Hedera Services.
         * @constructor
         * @param {proto.NetworkGetVersionInfoResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NetworkGetVersionInfoResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.NetworkGetVersionInfoResponse
         * @instance
         */
        NetworkGetVersionInfoResponse.prototype.header = null;

        /**
         * An API version.
         * <p>
         * This SHALL be the current Hedera API (HAPI) protobuf message version
         * accepted by the network.
         * @member {proto.SemanticVersion.$Properties|null|undefined} hapiProtoVersion
         * @memberof proto.NetworkGetVersionInfoResponse
         * @instance
         */
        NetworkGetVersionInfoResponse.prototype.hapiProtoVersion = null;

        /**
         * A Services version.
         * <p>
         * This SHALL be the current version of the Hedera Services software
         * operating the network.
         * @member {proto.SemanticVersion.$Properties|null|undefined} hederaServicesVersion
         * @memberof proto.NetworkGetVersionInfoResponse
         * @instance
         */
        NetworkGetVersionInfoResponse.prototype.hederaServicesVersion = null;

        /**
         * Creates a new NetworkGetVersionInfoResponse instance using the specified properties.
         * @function create
         * @memberof proto.NetworkGetVersionInfoResponse
         * @static
         * @param {proto.NetworkGetVersionInfoResponse.$Properties=} [properties] Properties to set
         * @returns {proto.NetworkGetVersionInfoResponse} NetworkGetVersionInfoResponse instance
         * @type {{
         *   (properties: proto.NetworkGetVersionInfoResponse.$Shape): proto.NetworkGetVersionInfoResponse & proto.NetworkGetVersionInfoResponse.$Shape;
         *   (properties?: proto.NetworkGetVersionInfoResponse.$Properties): proto.NetworkGetVersionInfoResponse;
         * }}
         */
        NetworkGetVersionInfoResponse.create = function(properties) {
            return new NetworkGetVersionInfoResponse(properties);
        };

        /**
         * Encodes the specified NetworkGetVersionInfoResponse message. Does not implicitly {@link proto.NetworkGetVersionInfoResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.NetworkGetVersionInfoResponse
         * @static
         * @param {proto.NetworkGetVersionInfoResponse.$Properties} message NetworkGetVersionInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkGetVersionInfoResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.hapiProtoVersion != null && $Object.hasOwnProperty.call(message, "hapiProtoVersion"))
                $root.proto.SemanticVersion.encode(message.hapiProtoVersion, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.hederaServicesVersion != null && $Object.hasOwnProperty.call(message, "hederaServicesVersion"))
                $root.proto.SemanticVersion.encode(message.hederaServicesVersion, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NetworkGetVersionInfoResponse message, length delimited. Does not implicitly {@link proto.NetworkGetVersionInfoResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NetworkGetVersionInfoResponse
         * @static
         * @param {proto.NetworkGetVersionInfoResponse.$Properties} message NetworkGetVersionInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkGetVersionInfoResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NetworkGetVersionInfoResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NetworkGetVersionInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NetworkGetVersionInfoResponse & proto.NetworkGetVersionInfoResponse.$Shape} NetworkGetVersionInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkGetVersionInfoResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NetworkGetVersionInfoResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.hapiProtoVersion = $root.proto.SemanticVersion.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hapiProtoVersion);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.hederaServicesVersion = $root.proto.SemanticVersion.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hederaServicesVersion);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NetworkGetVersionInfoResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NetworkGetVersionInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NetworkGetVersionInfoResponse & proto.NetworkGetVersionInfoResponse.$Shape} NetworkGetVersionInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkGetVersionInfoResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NetworkGetVersionInfoResponse message.
         * @function verify
         * @memberof proto.NetworkGetVersionInfoResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NetworkGetVersionInfoResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.hapiProtoVersion != null && $Object.hasOwnProperty.call(message, "hapiProtoVersion")) {
                let error = $root.proto.SemanticVersion.verify(message.hapiProtoVersion, _depth + 1);
                if (error)
                    return "hapiProtoVersion." + error;
            }
            if (message.hederaServicesVersion != null && $Object.hasOwnProperty.call(message, "hederaServicesVersion")) {
                let error = $root.proto.SemanticVersion.verify(message.hederaServicesVersion, _depth + 1);
                if (error)
                    return "hederaServicesVersion." + error;
            }
            return null;
        };

        /**
         * Creates a NetworkGetVersionInfoResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NetworkGetVersionInfoResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NetworkGetVersionInfoResponse} NetworkGetVersionInfoResponse
         */
        NetworkGetVersionInfoResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NetworkGetVersionInfoResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NetworkGetVersionInfoResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NetworkGetVersionInfoResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.NetworkGetVersionInfoResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.hapiProtoVersion != null) {
                if (!$util.isObject(object.hapiProtoVersion))
                    throw $TypeError(".proto.NetworkGetVersionInfoResponse.hapiProtoVersion: object expected");
                message.hapiProtoVersion = $root.proto.SemanticVersion.fromObject(object.hapiProtoVersion, _depth + 1);
            }
            if (object.hederaServicesVersion != null) {
                if (!$util.isObject(object.hederaServicesVersion))
                    throw $TypeError(".proto.NetworkGetVersionInfoResponse.hederaServicesVersion: object expected");
                message.hederaServicesVersion = $root.proto.SemanticVersion.fromObject(object.hederaServicesVersion, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a NetworkGetVersionInfoResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NetworkGetVersionInfoResponse
         * @static
         * @param {proto.NetworkGetVersionInfoResponse} message NetworkGetVersionInfoResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NetworkGetVersionInfoResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.hapiProtoVersion = null;
                object.hederaServicesVersion = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.hapiProtoVersion != null && $Object.hasOwnProperty.call(message, "hapiProtoVersion"))
                object.hapiProtoVersion = $root.proto.SemanticVersion.toObject(message.hapiProtoVersion, options, _depth + 1);
            if (message.hederaServicesVersion != null && $Object.hasOwnProperty.call(message, "hederaServicesVersion"))
                object.hederaServicesVersion = $root.proto.SemanticVersion.toObject(message.hederaServicesVersion, options, _depth + 1);
            return object;
        };

        /**
         * Converts this NetworkGetVersionInfoResponse to JSON.
         * @function toJSON
         * @memberof proto.NetworkGetVersionInfoResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NetworkGetVersionInfoResponse.prototype.toJSON = function() {
            return NetworkGetVersionInfoResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NetworkGetVersionInfoResponse
         * @function getTypeUrl
         * @memberof proto.NetworkGetVersionInfoResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NetworkGetVersionInfoResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NetworkGetVersionInfoResponse";
        };

        return NetworkGetVersionInfoResponse;
    })();

    proto.NetworkGetExecutionTimeQuery = (function() {

        /**
         * Properties of a NetworkGetExecutionTimeQuery.
         * @typedef {Object} proto.NetworkGetExecutionTimeQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {Array.<proto.TransactionID.$Properties>|null} [transactionIds] A list of transaction identifiers to query.
         * <p>
         * All of the queried transaction identifiers MUST have execution time
         * available. If any identifier does not have available execution time,
         * the query SHALL fail with an `INVALID_TRANSACTION_ID` response.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NetworkGetExecutionTimeQuery.
         * @memberof proto
         * @interface INetworkGetExecutionTimeQuery
         * @augments proto.NetworkGetExecutionTimeQuery.$Properties
         * @deprecated Use proto.NetworkGetExecutionTimeQuery.$Properties instead.
         */

        /**
         * Shape of a NetworkGetExecutionTimeQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   transactionIds?: Array.<proto.TransactionID.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.NetworkGetExecutionTimeQuery.$Shape
         */

        /**
         * Constructs a new NetworkGetExecutionTimeQuery.
         * @memberof proto
         * @classdesc Retrieve the time, in nanoseconds, spent in direct processing for one
         * or more recent transactions.
         * 
         * For each transaction identifier provided, if that transaction is
         * sufficiently recent (that is, it is within the range of the configuration
         * value `stats.executionTimesToTrack`), the node SHALL return the time, in
         * nanoseconds, spent to directly process that transaction.<br/>
         * This time will generally correspond to the time spent in a `handle` call
         * within the workflow.
         * 
         * Note that because each node processes every transaction for the Hedera
         * network, this query MAY be sent to any node, and results MAY be different
         * between different nodes.
         * @constructor
         * @param {proto.NetworkGetExecutionTimeQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NetworkGetExecutionTimeQuery = function (properties) {
            this.transactionIds = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.NetworkGetExecutionTimeQuery
         * @instance
         */
        NetworkGetExecutionTimeQuery.prototype.header = null;

        /**
         * A list of transaction identifiers to query.
         * <p>
         * All of the queried transaction identifiers MUST have execution time
         * available. If any identifier does not have available execution time,
         * the query SHALL fail with an `INVALID_TRANSACTION_ID` response.
         * @member {Array.<proto.TransactionID.$Properties>} transactionIds
         * @memberof proto.NetworkGetExecutionTimeQuery
         * @instance
         */
        NetworkGetExecutionTimeQuery.prototype.transactionIds = $util.emptyArray;

        /**
         * Creates a new NetworkGetExecutionTimeQuery instance using the specified properties.
         * @function create
         * @memberof proto.NetworkGetExecutionTimeQuery
         * @static
         * @param {proto.NetworkGetExecutionTimeQuery.$Properties=} [properties] Properties to set
         * @returns {proto.NetworkGetExecutionTimeQuery} NetworkGetExecutionTimeQuery instance
         * @type {{
         *   (properties: proto.NetworkGetExecutionTimeQuery.$Shape): proto.NetworkGetExecutionTimeQuery & proto.NetworkGetExecutionTimeQuery.$Shape;
         *   (properties?: proto.NetworkGetExecutionTimeQuery.$Properties): proto.NetworkGetExecutionTimeQuery;
         * }}
         */
        NetworkGetExecutionTimeQuery.create = function(properties) {
            return new NetworkGetExecutionTimeQuery(properties);
        };

        /**
         * Encodes the specified NetworkGetExecutionTimeQuery message. Does not implicitly {@link proto.NetworkGetExecutionTimeQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.NetworkGetExecutionTimeQuery
         * @static
         * @param {proto.NetworkGetExecutionTimeQuery.$Properties} message NetworkGetExecutionTimeQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkGetExecutionTimeQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.transactionIds != null && message.transactionIds.length)
                for (let i = 0; i < message.transactionIds.length; ++i)
                    $root.proto.TransactionID.encode(message.transactionIds[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NetworkGetExecutionTimeQuery message, length delimited. Does not implicitly {@link proto.NetworkGetExecutionTimeQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NetworkGetExecutionTimeQuery
         * @static
         * @param {proto.NetworkGetExecutionTimeQuery.$Properties} message NetworkGetExecutionTimeQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkGetExecutionTimeQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NetworkGetExecutionTimeQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NetworkGetExecutionTimeQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NetworkGetExecutionTimeQuery & proto.NetworkGetExecutionTimeQuery.$Shape} NetworkGetExecutionTimeQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkGetExecutionTimeQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NetworkGetExecutionTimeQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.transactionIds && message.transactionIds.length))
                            message.transactionIds = [];
                        message.transactionIds.push($root.proto.TransactionID.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NetworkGetExecutionTimeQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NetworkGetExecutionTimeQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NetworkGetExecutionTimeQuery & proto.NetworkGetExecutionTimeQuery.$Shape} NetworkGetExecutionTimeQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkGetExecutionTimeQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NetworkGetExecutionTimeQuery message.
         * @function verify
         * @memberof proto.NetworkGetExecutionTimeQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NetworkGetExecutionTimeQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.transactionIds != null && $Object.hasOwnProperty.call(message, "transactionIds")) {
                if (!$Array.isArray(message.transactionIds))
                    return "transactionIds: array expected";
                for (let i = 0; i < message.transactionIds.length; ++i) {
                    let error = $root.proto.TransactionID.verify(message.transactionIds[i], _depth + 1);
                    if (error)
                        return "transactionIds." + error;
                }
            }
            return null;
        };

        /**
         * Creates a NetworkGetExecutionTimeQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NetworkGetExecutionTimeQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NetworkGetExecutionTimeQuery} NetworkGetExecutionTimeQuery
         */
        NetworkGetExecutionTimeQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NetworkGetExecutionTimeQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NetworkGetExecutionTimeQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NetworkGetExecutionTimeQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.NetworkGetExecutionTimeQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.transactionIds) {
                if (!$Array.isArray(object.transactionIds))
                    throw $TypeError(".proto.NetworkGetExecutionTimeQuery.transactionIds: array expected");
                message.transactionIds = $Array(object.transactionIds.length);
                for (let i = 0; i < object.transactionIds.length; ++i) {
                    if (!$util.isObject(object.transactionIds[i]))
                        throw $TypeError(".proto.NetworkGetExecutionTimeQuery.transactionIds: object expected");
                    message.transactionIds[i] = $root.proto.TransactionID.fromObject(object.transactionIds[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a NetworkGetExecutionTimeQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NetworkGetExecutionTimeQuery
         * @static
         * @param {proto.NetworkGetExecutionTimeQuery} message NetworkGetExecutionTimeQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NetworkGetExecutionTimeQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.transactionIds = [];
            if (options.defaults)
                object.header = null;
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.transactionIds && message.transactionIds.length) {
                object.transactionIds = $Array(message.transactionIds.length);
                for (let j = 0; j < message.transactionIds.length; ++j)
                    object.transactionIds[j] = $root.proto.TransactionID.toObject(message.transactionIds[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this NetworkGetExecutionTimeQuery to JSON.
         * @function toJSON
         * @memberof proto.NetworkGetExecutionTimeQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NetworkGetExecutionTimeQuery.prototype.toJSON = function() {
            return NetworkGetExecutionTimeQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NetworkGetExecutionTimeQuery
         * @function getTypeUrl
         * @memberof proto.NetworkGetExecutionTimeQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NetworkGetExecutionTimeQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NetworkGetExecutionTimeQuery";
        };

        return NetworkGetExecutionTimeQuery;
    })();

    proto.NetworkGetExecutionTimeResponse = (function() {

        /**
         * Properties of a NetworkGetExecutionTimeResponse.
         * @typedef {Object} proto.NetworkGetExecutionTimeResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {Array.<number|Long>|null} [executionTimes] A list of execution times, in nanoseconds.
         * <p>
         * This list SHALL be in the same order as the transaction
         * identifiers were presented in the query.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NetworkGetExecutionTimeResponse.
         * @memberof proto
         * @interface INetworkGetExecutionTimeResponse
         * @augments proto.NetworkGetExecutionTimeResponse.$Properties
         * @deprecated Use proto.NetworkGetExecutionTimeResponse.$Properties instead.
         */

        /**
         * Shape of a NetworkGetExecutionTimeResponse.
         * @typedef {proto.NetworkGetExecutionTimeResponse.$Properties} proto.NetworkGetExecutionTimeResponse.$Shape
         */

        /**
         * Constructs a new NetworkGetExecutionTimeResponse.
         * @memberof proto
         * @classdesc A response to a `networkGetExecutionTime` query.
         * @constructor
         * @param {proto.NetworkGetExecutionTimeResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NetworkGetExecutionTimeResponse = function (properties) {
            this.executionTimes = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.NetworkGetExecutionTimeResponse
         * @instance
         */
        NetworkGetExecutionTimeResponse.prototype.header = null;

        /**
         * A list of execution times, in nanoseconds.
         * <p>
         * This list SHALL be in the same order as the transaction
         * identifiers were presented in the query.
         * @member {Array.<number|Long>} executionTimes
         * @memberof proto.NetworkGetExecutionTimeResponse
         * @instance
         */
        NetworkGetExecutionTimeResponse.prototype.executionTimes = $util.emptyArray;

        /**
         * Creates a new NetworkGetExecutionTimeResponse instance using the specified properties.
         * @function create
         * @memberof proto.NetworkGetExecutionTimeResponse
         * @static
         * @param {proto.NetworkGetExecutionTimeResponse.$Properties=} [properties] Properties to set
         * @returns {proto.NetworkGetExecutionTimeResponse} NetworkGetExecutionTimeResponse instance
         * @type {{
         *   (properties: proto.NetworkGetExecutionTimeResponse.$Shape): proto.NetworkGetExecutionTimeResponse & proto.NetworkGetExecutionTimeResponse.$Shape;
         *   (properties?: proto.NetworkGetExecutionTimeResponse.$Properties): proto.NetworkGetExecutionTimeResponse;
         * }}
         */
        NetworkGetExecutionTimeResponse.create = function(properties) {
            return new NetworkGetExecutionTimeResponse(properties);
        };

        /**
         * Encodes the specified NetworkGetExecutionTimeResponse message. Does not implicitly {@link proto.NetworkGetExecutionTimeResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.NetworkGetExecutionTimeResponse
         * @static
         * @param {proto.NetworkGetExecutionTimeResponse.$Properties} message NetworkGetExecutionTimeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkGetExecutionTimeResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.executionTimes != null && message.executionTimes.length)
                writer.uint32(/* id 2, wireType 2 =*/18).uint64s(message.executionTimes);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NetworkGetExecutionTimeResponse message, length delimited. Does not implicitly {@link proto.NetworkGetExecutionTimeResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NetworkGetExecutionTimeResponse
         * @static
         * @param {proto.NetworkGetExecutionTimeResponse.$Properties} message NetworkGetExecutionTimeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkGetExecutionTimeResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NetworkGetExecutionTimeResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NetworkGetExecutionTimeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NetworkGetExecutionTimeResponse & proto.NetworkGetExecutionTimeResponse.$Shape} NetworkGetExecutionTimeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkGetExecutionTimeResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NetworkGetExecutionTimeResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.executionTimes && message.executionTimes.length))
                                message.executionTimes = [];
                            reader.uint64s(message.executionTimes);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.executionTimes && message.executionTimes.length))
                            message.executionTimes = [];
                        message.executionTimes.push(reader.uint64());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NetworkGetExecutionTimeResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NetworkGetExecutionTimeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NetworkGetExecutionTimeResponse & proto.NetworkGetExecutionTimeResponse.$Shape} NetworkGetExecutionTimeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkGetExecutionTimeResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NetworkGetExecutionTimeResponse message.
         * @function verify
         * @memberof proto.NetworkGetExecutionTimeResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NetworkGetExecutionTimeResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.executionTimes != null && $Object.hasOwnProperty.call(message, "executionTimes")) {
                if (!$Array.isArray(message.executionTimes))
                    return "executionTimes: array expected";
                for (let i = 0; i < message.executionTimes.length; ++i)
                    if (!$util.isInteger(message.executionTimes[i]) && !(message.executionTimes[i] && $util.isInteger(message.executionTimes[i].low) && $util.isInteger(message.executionTimes[i].high)))
                        return "executionTimes: integer|Long[] expected";
            }
            return null;
        };

        /**
         * Creates a NetworkGetExecutionTimeResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NetworkGetExecutionTimeResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NetworkGetExecutionTimeResponse} NetworkGetExecutionTimeResponse
         */
        NetworkGetExecutionTimeResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NetworkGetExecutionTimeResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NetworkGetExecutionTimeResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NetworkGetExecutionTimeResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.NetworkGetExecutionTimeResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.executionTimes) {
                if (!$Array.isArray(object.executionTimes))
                    throw $TypeError(".proto.NetworkGetExecutionTimeResponse.executionTimes: array expected");
                message.executionTimes = $Array(object.executionTimes.length);
                for (let i = 0; i < object.executionTimes.length; ++i)
                    if ($util.Long)
                        message.executionTimes[i] = $util.Long.fromValue(object.executionTimes[i], true);
                    else if (typeof object.executionTimes[i] === "string")
                        message.executionTimes[i] = $parseInt(object.executionTimes[i], 10);
                    else if (typeof object.executionTimes[i] === "number")
                        message.executionTimes[i] = object.executionTimes[i];
                    else if (typeof object.executionTimes[i] === "object")
                        message.executionTimes[i] = new $util.LongBits(object.executionTimes[i].low >>> 0, object.executionTimes[i].high >>> 0).toNumber(true);
            }
            return message;
        };

        /**
         * Creates a plain object from a NetworkGetExecutionTimeResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NetworkGetExecutionTimeResponse
         * @static
         * @param {proto.NetworkGetExecutionTimeResponse} message NetworkGetExecutionTimeResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NetworkGetExecutionTimeResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.executionTimes = [];
            if (options.defaults)
                object.header = null;
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.executionTimes && message.executionTimes.length) {
                object.executionTimes = $Array(message.executionTimes.length);
                for (let j = 0; j < message.executionTimes.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.executionTimes[j] = typeof message.executionTimes[j] === "number" ? $BigInt(message.executionTimes[j]) : $util.Long.fromBits(message.executionTimes[j].low >>> 0, message.executionTimes[j].high >>> 0, true).toBigInt();
                    else if (typeof message.executionTimes[j] === "number")
                        object.executionTimes[j] = options.longs === $String ? $String(message.executionTimes[j]) : message.executionTimes[j];
                    else
                        object.executionTimes[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.executionTimes[j]) : options.longs === $Number ? new $util.LongBits(message.executionTimes[j].low >>> 0, message.executionTimes[j].high >>> 0).toNumber(true) : message.executionTimes[j];
            }
            return object;
        };

        /**
         * Converts this NetworkGetExecutionTimeResponse to JSON.
         * @function toJSON
         * @memberof proto.NetworkGetExecutionTimeResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NetworkGetExecutionTimeResponse.prototype.toJSON = function() {
            return NetworkGetExecutionTimeResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NetworkGetExecutionTimeResponse
         * @function getTypeUrl
         * @memberof proto.NetworkGetExecutionTimeResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NetworkGetExecutionTimeResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NetworkGetExecutionTimeResponse";
        };

        return NetworkGetExecutionTimeResponse;
    })();

    proto.TokenGetInfoQuery = (function() {

        /**
         * Properties of a TokenGetInfoQuery.
         * @typedef {Object} proto.TokenGetInfoQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.TokenID.$Properties|null} [token] A token identifier.
         * <p>
         * This SHALL identify the token to query.<br/>
         * The identified token MUST exist, and MUST NOT be deleted.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenGetInfoQuery.
         * @memberof proto
         * @interface ITokenGetInfoQuery
         * @augments proto.TokenGetInfoQuery.$Properties
         * @deprecated Use proto.TokenGetInfoQuery.$Properties instead.
         */

        /**
         * Shape of a TokenGetInfoQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   token?: proto.TokenID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenGetInfoQuery.$Shape
         */

        /**
         * Constructs a new TokenGetInfoQuery.
         * @memberof proto
         * @classdesc Request information for a token.
         * @constructor
         * @param {proto.TokenGetInfoQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenGetInfoQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.TokenGetInfoQuery
         * @instance
         */
        TokenGetInfoQuery.prototype.header = null;

        /**
         * A token identifier.
         * <p>
         * This SHALL identify the token to query.<br/>
         * The identified token MUST exist, and MUST NOT be deleted.
         * @member {proto.TokenID.$Properties|null|undefined} token
         * @memberof proto.TokenGetInfoQuery
         * @instance
         */
        TokenGetInfoQuery.prototype.token = null;

        /**
         * Creates a new TokenGetInfoQuery instance using the specified properties.
         * @function create
         * @memberof proto.TokenGetInfoQuery
         * @static
         * @param {proto.TokenGetInfoQuery.$Properties=} [properties] Properties to set
         * @returns {proto.TokenGetInfoQuery} TokenGetInfoQuery instance
         * @type {{
         *   (properties: proto.TokenGetInfoQuery.$Shape): proto.TokenGetInfoQuery & proto.TokenGetInfoQuery.$Shape;
         *   (properties?: proto.TokenGetInfoQuery.$Properties): proto.TokenGetInfoQuery;
         * }}
         */
        TokenGetInfoQuery.create = function(properties) {
            return new TokenGetInfoQuery(properties);
        };

        /**
         * Encodes the specified TokenGetInfoQuery message. Does not implicitly {@link proto.TokenGetInfoQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenGetInfoQuery
         * @static
         * @param {proto.TokenGetInfoQuery.$Properties} message TokenGetInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetInfoQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                $root.proto.TokenID.encode(message.token, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenGetInfoQuery message, length delimited. Does not implicitly {@link proto.TokenGetInfoQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenGetInfoQuery
         * @static
         * @param {proto.TokenGetInfoQuery.$Properties} message TokenGetInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetInfoQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenGetInfoQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenGetInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenGetInfoQuery & proto.TokenGetInfoQuery.$Shape} TokenGetInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetInfoQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenGetInfoQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.token = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.token);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenGetInfoQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenGetInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenGetInfoQuery & proto.TokenGetInfoQuery.$Shape} TokenGetInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetInfoQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenGetInfoQuery message.
         * @function verify
         * @memberof proto.TokenGetInfoQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenGetInfoQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
                let error = $root.proto.TokenID.verify(message.token, _depth + 1);
                if (error)
                    return "token." + error;
            }
            return null;
        };

        /**
         * Creates a TokenGetInfoQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenGetInfoQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenGetInfoQuery} TokenGetInfoQuery
         */
        TokenGetInfoQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenGetInfoQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenGetInfoQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenGetInfoQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TokenGetInfoQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.token != null) {
                if (!$util.isObject(object.token))
                    throw $TypeError(".proto.TokenGetInfoQuery.token: object expected");
                message.token = $root.proto.TokenID.fromObject(object.token, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenGetInfoQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenGetInfoQuery
         * @static
         * @param {proto.TokenGetInfoQuery} message TokenGetInfoQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenGetInfoQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.token = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = $root.proto.TokenID.toObject(message.token, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenGetInfoQuery to JSON.
         * @function toJSON
         * @memberof proto.TokenGetInfoQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenGetInfoQuery.prototype.toJSON = function() {
            return TokenGetInfoQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenGetInfoQuery
         * @function getTypeUrl
         * @memberof proto.TokenGetInfoQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenGetInfoQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenGetInfoQuery";
        };

        return TokenGetInfoQuery;
    })();

    proto.TokenInfo = (function() {

        /**
         * Properties of a TokenInfo.
         * @typedef {Object} proto.TokenInfo.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] A unique identifier for this token.
         * @property {string|null} [name] A human-readable name for this token.
         * <p>
         * This value MAY NOT be unique.<br/>
         * This value SHALL NOT exceed 100 bytes when encoded as UTF-8.
         * @property {string|null} [symbol] A human-readable symbol for the token.
         * <p>
         * This value SHALL NOT be unique.<br/>
         * This value SHALL NOT exceed 100 bytes when encoded as UTF-8.
         * @property {number|null} [decimals] A number of decimal places for this token.
         * <p>
         * If decimals are 8 or 11, then the number of whole tokens can be at most
         * billions or millions, respectively. More decimals allows for a more
         * finely-divided token, but also limits the maximum total supply.
         * <p>
         * Examples
         * <ul>
         * <li>Bitcoin satoshis (21 million whole tokens with 8 decimals).</li>
         * <li>Hedera tinybar (50 billion whole tokens with 8 decimals).</li>
         * <li>Bitcoin milli-satoshis (21 million whole tokens with 11
         * decimals).</li>
         * <li>Theoretical limit is roughly 92.2 billion with 8 decimals, or
         * 92.2 million with 11 decimals.</li>
         * </ul>
         * All token amounts in the network are stored as integer amounts, with
         * each unit representing 10<sup>-decimals</sup> whole tokens.
         * <p>
         * For tokens with `token_type` set to `NON_FUNGIBLE_UNIQUE` this MUST be 0.
         * @property {number|Long|null} [totalSupply] A _current_ total supply of this token, expressed in the smallest unit
         * of the token.
         * <p>
         * The number of _whole_ tokens this represents is (total_supply /
         * 10<sup>decimals</sup>). The value of total supply, MUST be within the
         * positive range of a twos-compliment signed 64-bit integer.
         * The `total_supply`, therefore MUST be between 1, and
         * 9,223,372,036,854,775,807, inclusive.
         * <p>
         * This value SHALL be reduced when a `token_burn` or `token_wipe_account`
         * operation is executed, and SHALL be increased when a `token_mint`
         * operation is executed.
         * @property {proto.AccountID.$Properties|null} [treasury] A treasury account identifier for this token.
         * <p>
         * When the token is created, the initial supply given in the token create
         * transaction SHALL be minted and deposited in the treasury account.<br/>
         * All token mint transactions for this token SHALL deposit the new minted
         * tokens in the treasury account.<br/>
         * All token burn transactions for this token SHALL remove the tokens to be
         * burned from the treasury account.
         * @property {proto.Key.$Properties|null} [adminKey] Access control for general modification of this token.
         * <p>
         * This key MUST sign any `token_update` transaction that
         * changes any attribute of the token other than expiration_time.
         * Other attributes of this token MAY be changed by transactions other than
         * `token_update`, and MUST be signed by one of the other purpose-specific
         * keys assigned to the token.<br/>
         * This value can be set during token creation, and SHALL NOT be
         * modified thereafter, unless the update transaction is signed by both
         * the existing `admin_key` and the new `admin_key`.<br/>
         * If the `admin_key` is not set for a token, that token SHALL be immutable.
         * @property {proto.Key.$Properties|null} [kycKey] Access control for KYC for this token.
         * <p>
         * Know Your Customer (KYC) status may be granted for an account by a token
         * grant kyc transaction signed by this key.<br/>
         * If this key is not set, then KYC status cannot be granted to an account
         * for this token, and any `TokenGrantKyc` transaction attempting to grant
         * kyc to an account for this token SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {proto.Key.$Properties|null} [freezeKey] Access control to freeze this token.
         * <p>
         * A token may be frozen for an account, preventing any transaction from
         * transferring that token for that specified account, by a token freeze
         * account transaction signed by this key.<br/>
         * If this key is not set, the token cannot be frozen, and any transaction
         * attempting to freeze the token for an account SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {proto.Key.$Properties|null} [wipeKey] Access control of account wipe for this token.
         * <p>
         * A token may be wiped, removing and burning tokens from a specific
         * account, by a token wipe transaction, which MUST be signed by this key.
         * The `treasury_account` cannot be subjected to a token wipe. A token burn
         * transaction, signed by the `supply_key`, serves to burn tokens held by
         * the `treasury_account` instead.<br/>
         * If this key is not set, the token cannot be wiped, and any transaction
         * attempting to wipe the token from an account SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {proto.Key.$Properties|null} [supplyKey] Access control of token mint/burn for this token.
         * <p>
         * A token mint transaction MUST be signed by this key, and any token mint
         * transaction not signed by the current `supply_key` for that token
         * SHALL NOT succeed.<br/>
         * A token burn transaction MUST be signed by this key, and any token burn
         * transaction not signed by the current `supply_key` for that token
         * SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {proto.TokenFreezeStatus|null} [defaultFreezeStatus] A flag indicating if accounts associated to this token are frozen by
         * default, not frozen, or freeze is not applicable.
         * <p>
         * Accounts frozen by default and newly associated with this token CANNOT
         * transact in the token until unfrozen.<br/>
         * This SHALL NOT prevent a `tokenReject` transaction to return the tokens
         * from an account to the treasury account.
         * @property {proto.TokenKycStatus|null} [defaultKycStatus] A flag indicating if accounts associated with this token are granted
         * KYC by default, revoked by default, or KYC is not applicable.
         * @property {boolean|null} [deleted] A flag indicating that this token is deleted.
         * <p>
         * A transaction involving a deleted token MUST NOT succeed.
         * @property {proto.AccountID.$Properties|null} [autoRenewAccount] An identifier for the account (if any) that the network will attempt
         * to charge for this token's auto-renewal upon expiration.
         * <p>
         * This field is OPTIONAL. If it is not set then renewal fees SHALL be
         * charged to the account identified by `treasury`.
         * @property {proto.Duration.$Properties|null} [autoRenewPeriod] A duration by which the network should automatically extend
         * this token's expiration.
         * <p>
         * If the token has a valid auto-renew account, and is not deleted upon
         * expiration, the network SHALL attempt to automatically renew this
         * token.<br/>
         * The default values for the minimum period and maximum period are 30 days
         * and 90 days, respectively.
         * @property {proto.Timestamp.$Properties|null} [expiry] An expiration time for this token, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the
         * UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.
         * @property {string|null} [memo] A short description of this token.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {proto.TokenType|null} [tokenType] A type for this token.
         * <p>
         * A token SHALL be either `FUNGIBLE_COMMON` or `NON_FUNGIBLE_UNIQUE`.<br/>
         * If this value was omitted during token creation, `FUNGIBLE_COMMON`
         * SHALL be used.<br/>
         * The value `FUNGIBLE_COMMON` SHALL represent a fungible/common token.
         * The value `NON_FUNGIBLE_UNIQUE` SHALL represent a
         * non-fungible/unique token.
         * @property {proto.TokenSupplyType|null} [supplyType] A supply type for this token.
         * <p>
         * A token SHALL have either `INFINITE` or `FINITE` supply type.<br/>
         * If this value was omitted during token creation, the value `INFINITE`
         * SHALL be used.
         * @property {number|Long|null} [maxSupply] A maximum supply of this token.<br/>
         * This is the maximum number of tokens of this type that may be issued.
         * <p>
         * This limit SHALL apply regardless of `token_type`.<br/>
         * If `supply_type` is `INFINITE` then this value MUST be 0.<br/>
         * If `supply_type` is `FINITE`, then this value MUST be greater than 0.
         * @property {proto.Key.$Properties|null} [feeScheduleKey] Access control of the `custom_fees` field for this token.
         * <p>
         * The token custom fee schedule may be changed, modifying the fees charged
         * for transferring that token, by a token update transaction, which MUST
         * be signed by this key.<br/>
         * If this key is not set, the token custom fee schedule cannot be changed,
         * and any transaction attempting to change the custom fee schedule for
         * this token SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {Array.<proto.CustomFee.$Properties>|null} [customFees] A custom fee schedule for this token.
         * @property {proto.Key.$Properties|null} [pauseKey] Access control of pause/unpause for this token.
         * <p>
         * A token may be paused, preventing any transaction from transferring that
         * token, by a token update transaction signed by this key.<br/>
         * If this key is not set, the token cannot be paused, and any transaction
         * attempting to pause the token SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {proto.TokenPauseStatus|null} [pauseStatus] A flag indicating that this token is paused.<br/>
         * A token may be paused, unpaused, or pause not applicable.
         * <p>
         * A transaction involving a paused token, other than token_unpause,
         * MUST NOT succeed.
         * @property {Uint8Array|null} [ledgerId] The ledger ID of the network that generated this response.
         * <p>
         * This value SHALL identify the distributed ledger that responded to
         * this query.
         * @property {Uint8Array|null} [metadata] A Token "Metadata".
         * <p>
         * This value, if set, SHALL NOT exceed 100 bytes.
         * @property {proto.Key.$Properties|null} [metadataKey] Access Control of metadata update for this token.
         * <p>
         * A transaction to update the `metadata` field of this token MUST be
         * signed by this key.<br/>
         * If this token is a non-fungible/unique token type, a transaction to
         * update the `metadata` field of any individual serialized unique token
         * of this type MUST be signed by this key.<br/>
         * If this key is not set, the token metadata SHALL NOT be changed after it
         * is created.<br/>
         * If this key is not set, the metadata for any individual serialized token
         * of this type SHALL NOT be changed after it is created.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenInfo.
         * @memberof proto
         * @interface ITokenInfo
         * @augments proto.TokenInfo.$Properties
         * @deprecated Use proto.TokenInfo.$Properties instead.
         */

        /**
         * Shape of a TokenInfo.
         * @typedef {{
         *   tokenId?: proto.TokenID.$Shape|null;
         *   name?: string|null;
         *   symbol?: string|null;
         *   decimals?: number|null;
         *   totalSupply?: number|Long|null;
         *   treasury?: proto.AccountID.$Shape|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   kycKey?: proto.Key.$Shape|null;
         *   freezeKey?: proto.Key.$Shape|null;
         *   wipeKey?: proto.Key.$Shape|null;
         *   supplyKey?: proto.Key.$Shape|null;
         *   defaultFreezeStatus?: proto.TokenFreezeStatus|null;
         *   defaultKycStatus?: proto.TokenKycStatus|null;
         *   deleted?: boolean|null;
         *   autoRenewAccount?: proto.AccountID.$Shape|null;
         *   autoRenewPeriod?: proto.Duration.$Shape|null;
         *   expiry?: proto.Timestamp.$Shape|null;
         *   memo?: string|null;
         *   tokenType?: proto.TokenType|null;
         *   supplyType?: proto.TokenSupplyType|null;
         *   maxSupply?: number|Long|null;
         *   feeScheduleKey?: proto.Key.$Shape|null;
         *   customFees?: Array.<proto.CustomFee.$Shape>|null;
         *   pauseKey?: proto.Key.$Shape|null;
         *   pauseStatus?: proto.TokenPauseStatus|null;
         *   ledgerId?: Uint8Array|null;
         *   metadata?: Uint8Array|null;
         *   metadataKey?: proto.Key.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenInfo.$Shape
         */

        /**
         * Constructs a new TokenInfo.
         * @memberof proto
         * @classdesc An Hedera Token Service(HTS) token.
         * 
         * A token SHALL represent a fungible or non-fungible unit of exchange.<br/>
         * The specified Treasury Account SHALL receive the initial supply of tokens and
         * SHALL determine distribution of all tokens once minted.
         * @constructor
         * @param {proto.TokenInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenInfo = function (properties) {
            this.customFees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A unique identifier for this token.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.tokenId = null;

        /**
         * A human-readable name for this token.
         * <p>
         * This value MAY NOT be unique.<br/>
         * This value SHALL NOT exceed 100 bytes when encoded as UTF-8.
         * @member {string} name
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.name = "";

        /**
         * A human-readable symbol for the token.
         * <p>
         * This value SHALL NOT be unique.<br/>
         * This value SHALL NOT exceed 100 bytes when encoded as UTF-8.
         * @member {string} symbol
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.symbol = "";

        /**
         * A number of decimal places for this token.
         * <p>
         * If decimals are 8 or 11, then the number of whole tokens can be at most
         * billions or millions, respectively. More decimals allows for a more
         * finely-divided token, but also limits the maximum total supply.
         * <p>
         * Examples
         * <ul>
         * <li>Bitcoin satoshis (21 million whole tokens with 8 decimals).</li>
         * <li>Hedera tinybar (50 billion whole tokens with 8 decimals).</li>
         * <li>Bitcoin milli-satoshis (21 million whole tokens with 11
         * decimals).</li>
         * <li>Theoretical limit is roughly 92.2 billion with 8 decimals, or
         * 92.2 million with 11 decimals.</li>
         * </ul>
         * All token amounts in the network are stored as integer amounts, with
         * each unit representing 10<sup>-decimals</sup> whole tokens.
         * <p>
         * For tokens with `token_type` set to `NON_FUNGIBLE_UNIQUE` this MUST be 0.
         * @member {number} decimals
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.decimals = 0;

        /**
         * A _current_ total supply of this token, expressed in the smallest unit
         * of the token.
         * <p>
         * The number of _whole_ tokens this represents is (total_supply /
         * 10<sup>decimals</sup>). The value of total supply, MUST be within the
         * positive range of a twos-compliment signed 64-bit integer.
         * The `total_supply`, therefore MUST be between 1, and
         * 9,223,372,036,854,775,807, inclusive.
         * <p>
         * This value SHALL be reduced when a `token_burn` or `token_wipe_account`
         * operation is executed, and SHALL be increased when a `token_mint`
         * operation is executed.
         * @member {number|Long} totalSupply
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.totalSupply = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A treasury account identifier for this token.
         * <p>
         * When the token is created, the initial supply given in the token create
         * transaction SHALL be minted and deposited in the treasury account.<br/>
         * All token mint transactions for this token SHALL deposit the new minted
         * tokens in the treasury account.<br/>
         * All token burn transactions for this token SHALL remove the tokens to be
         * burned from the treasury account.
         * @member {proto.AccountID.$Properties|null|undefined} treasury
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.treasury = null;

        /**
         * Access control for general modification of this token.
         * <p>
         * This key MUST sign any `token_update` transaction that
         * changes any attribute of the token other than expiration_time.
         * Other attributes of this token MAY be changed by transactions other than
         * `token_update`, and MUST be signed by one of the other purpose-specific
         * keys assigned to the token.<br/>
         * This value can be set during token creation, and SHALL NOT be
         * modified thereafter, unless the update transaction is signed by both
         * the existing `admin_key` and the new `admin_key`.<br/>
         * If the `admin_key` is not set for a token, that token SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.adminKey = null;

        /**
         * Access control for KYC for this token.
         * <p>
         * Know Your Customer (KYC) status may be granted for an account by a token
         * grant kyc transaction signed by this key.<br/>
         * If this key is not set, then KYC status cannot be granted to an account
         * for this token, and any `TokenGrantKyc` transaction attempting to grant
         * kyc to an account for this token SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} kycKey
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.kycKey = null;

        /**
         * Access control to freeze this token.
         * <p>
         * A token may be frozen for an account, preventing any transaction from
         * transferring that token for that specified account, by a token freeze
         * account transaction signed by this key.<br/>
         * If this key is not set, the token cannot be frozen, and any transaction
         * attempting to freeze the token for an account SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} freezeKey
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.freezeKey = null;

        /**
         * Access control of account wipe for this token.
         * <p>
         * A token may be wiped, removing and burning tokens from a specific
         * account, by a token wipe transaction, which MUST be signed by this key.
         * The `treasury_account` cannot be subjected to a token wipe. A token burn
         * transaction, signed by the `supply_key`, serves to burn tokens held by
         * the `treasury_account` instead.<br/>
         * If this key is not set, the token cannot be wiped, and any transaction
         * attempting to wipe the token from an account SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} wipeKey
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.wipeKey = null;

        /**
         * Access control of token mint/burn for this token.
         * <p>
         * A token mint transaction MUST be signed by this key, and any token mint
         * transaction not signed by the current `supply_key` for that token
         * SHALL NOT succeed.<br/>
         * A token burn transaction MUST be signed by this key, and any token burn
         * transaction not signed by the current `supply_key` for that token
         * SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} supplyKey
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.supplyKey = null;

        /**
         * A flag indicating if accounts associated to this token are frozen by
         * default, not frozen, or freeze is not applicable.
         * <p>
         * Accounts frozen by default and newly associated with this token CANNOT
         * transact in the token until unfrozen.<br/>
         * This SHALL NOT prevent a `tokenReject` transaction to return the tokens
         * from an account to the treasury account.
         * @member {proto.TokenFreezeStatus} defaultFreezeStatus
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.defaultFreezeStatus = 0;

        /**
         * A flag indicating if accounts associated with this token are granted
         * KYC by default, revoked by default, or KYC is not applicable.
         * @member {proto.TokenKycStatus} defaultKycStatus
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.defaultKycStatus = 0;

        /**
         * A flag indicating that this token is deleted.
         * <p>
         * A transaction involving a deleted token MUST NOT succeed.
         * @member {boolean} deleted
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.deleted = false;

        /**
         * An identifier for the account (if any) that the network will attempt
         * to charge for this token's auto-renewal upon expiration.
         * <p>
         * This field is OPTIONAL. If it is not set then renewal fees SHALL be
         * charged to the account identified by `treasury`.
         * @member {proto.AccountID.$Properties|null|undefined} autoRenewAccount
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.autoRenewAccount = null;

        /**
         * A duration by which the network should automatically extend
         * this token's expiration.
         * <p>
         * If the token has a valid auto-renew account, and is not deleted upon
         * expiration, the network SHALL attempt to automatically renew this
         * token.<br/>
         * The default values for the minimum period and maximum period are 30 days
         * and 90 days, respectively.
         * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.autoRenewPeriod = null;

        /**
         * An expiration time for this token, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the
         * UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.
         * @member {proto.Timestamp.$Properties|null|undefined} expiry
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.expiry = null;

        /**
         * A short description of this token.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.memo = "";

        /**
         * A type for this token.
         * <p>
         * A token SHALL be either `FUNGIBLE_COMMON` or `NON_FUNGIBLE_UNIQUE`.<br/>
         * If this value was omitted during token creation, `FUNGIBLE_COMMON`
         * SHALL be used.<br/>
         * The value `FUNGIBLE_COMMON` SHALL represent a fungible/common token.
         * The value `NON_FUNGIBLE_UNIQUE` SHALL represent a
         * non-fungible/unique token.
         * @member {proto.TokenType} tokenType
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.tokenType = 0;

        /**
         * A supply type for this token.
         * <p>
         * A token SHALL have either `INFINITE` or `FINITE` supply type.<br/>
         * If this value was omitted during token creation, the value `INFINITE`
         * SHALL be used.
         * @member {proto.TokenSupplyType} supplyType
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.supplyType = 0;

        /**
         * A maximum supply of this token.<br/>
         * This is the maximum number of tokens of this type that may be issued.
         * <p>
         * This limit SHALL apply regardless of `token_type`.<br/>
         * If `supply_type` is `INFINITE` then this value MUST be 0.<br/>
         * If `supply_type` is `FINITE`, then this value MUST be greater than 0.
         * @member {number|Long} maxSupply
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.maxSupply = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Access control of the `custom_fees` field for this token.
         * <p>
         * The token custom fee schedule may be changed, modifying the fees charged
         * for transferring that token, by a token update transaction, which MUST
         * be signed by this key.<br/>
         * If this key is not set, the token custom fee schedule cannot be changed,
         * and any transaction attempting to change the custom fee schedule for
         * this token SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} feeScheduleKey
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.feeScheduleKey = null;

        /**
         * A custom fee schedule for this token.
         * @member {Array.<proto.CustomFee.$Properties>} customFees
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.customFees = $util.emptyArray;

        /**
         * Access control of pause/unpause for this token.
         * <p>
         * A token may be paused, preventing any transaction from transferring that
         * token, by a token update transaction signed by this key.<br/>
         * If this key is not set, the token cannot be paused, and any transaction
         * attempting to pause the token SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} pauseKey
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.pauseKey = null;

        /**
         * A flag indicating that this token is paused.<br/>
         * A token may be paused, unpaused, or pause not applicable.
         * <p>
         * A transaction involving a paused token, other than token_unpause,
         * MUST NOT succeed.
         * @member {proto.TokenPauseStatus} pauseStatus
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.pauseStatus = 0;

        /**
         * The ledger ID of the network that generated this response.
         * <p>
         * This value SHALL identify the distributed ledger that responded to
         * this query.
         * @member {Uint8Array} ledgerId
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.ledgerId = $util.newBuffer([]);

        /**
         * A Token "Metadata".
         * <p>
         * This value, if set, SHALL NOT exceed 100 bytes.
         * @member {Uint8Array} metadata
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.metadata = $util.newBuffer([]);

        /**
         * Access Control of metadata update for this token.
         * <p>
         * A transaction to update the `metadata` field of this token MUST be
         * signed by this key.<br/>
         * If this token is a non-fungible/unique token type, a transaction to
         * update the `metadata` field of any individual serialized unique token
         * of this type MUST be signed by this key.<br/>
         * If this key is not set, the token metadata SHALL NOT be changed after it
         * is created.<br/>
         * If this key is not set, the metadata for any individual serialized token
         * of this type SHALL NOT be changed after it is created.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} metadataKey
         * @memberof proto.TokenInfo
         * @instance
         */
        TokenInfo.prototype.metadataKey = null;

        /**
         * Creates a new TokenInfo instance using the specified properties.
         * @function create
         * @memberof proto.TokenInfo
         * @static
         * @param {proto.TokenInfo.$Properties=} [properties] Properties to set
         * @returns {proto.TokenInfo} TokenInfo instance
         * @type {{
         *   (properties: proto.TokenInfo.$Shape): proto.TokenInfo & proto.TokenInfo.$Shape;
         *   (properties?: proto.TokenInfo.$Properties): proto.TokenInfo;
         * }}
         */
        TokenInfo.create = function(properties) {
            return new TokenInfo(properties);
        };

        /**
         * Encodes the specified TokenInfo message. Does not implicitly {@link proto.TokenInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenInfo
         * @static
         * @param {proto.TokenInfo.$Properties} message TokenInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol") && message.symbol !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.symbol);
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals") && message.decimals !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.decimals);
            if (message.totalSupply != null && $Object.hasOwnProperty.call(message, "totalSupply") && (typeof message.totalSupply === "object" ? message.totalSupply.low || message.totalSupply.high : message.totalSupply !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.totalSupply);
            if (message.treasury != null && $Object.hasOwnProperty.call(message, "treasury"))
                $root.proto.AccountID.encode(message.treasury, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.kycKey != null && $Object.hasOwnProperty.call(message, "kycKey"))
                $root.proto.Key.encode(message.kycKey, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.freezeKey != null && $Object.hasOwnProperty.call(message, "freezeKey"))
                $root.proto.Key.encode(message.freezeKey, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.wipeKey != null && $Object.hasOwnProperty.call(message, "wipeKey"))
                $root.proto.Key.encode(message.wipeKey, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.supplyKey != null && $Object.hasOwnProperty.call(message, "supplyKey"))
                $root.proto.Key.encode(message.supplyKey, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.defaultFreezeStatus != null && $Object.hasOwnProperty.call(message, "defaultFreezeStatus") && message.defaultFreezeStatus !== 0)
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.defaultFreezeStatus);
            if (message.defaultKycStatus != null && $Object.hasOwnProperty.call(message, "defaultKycStatus") && message.defaultKycStatus !== 0)
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.defaultKycStatus);
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.deleted);
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount"))
                $root.proto.AccountID.encode(message.autoRenewAccount, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                $root.proto.Timestamp.encode(message.expiry, writer.uint32(/* id 17, wireType 2 =*/138).fork(), _depth + 1).ldelim();
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 18, wireType 2 =*/146).string(message.memo);
            if (message.tokenType != null && $Object.hasOwnProperty.call(message, "tokenType") && message.tokenType !== 0)
                writer.uint32(/* id 19, wireType 0 =*/152).int32(message.tokenType);
            if (message.supplyType != null && $Object.hasOwnProperty.call(message, "supplyType") && message.supplyType !== 0)
                writer.uint32(/* id 20, wireType 0 =*/160).int32(message.supplyType);
            if (message.maxSupply != null && $Object.hasOwnProperty.call(message, "maxSupply") && (typeof message.maxSupply === "object" ? message.maxSupply.low || message.maxSupply.high : message.maxSupply !== 0))
                writer.uint32(/* id 21, wireType 0 =*/168).int64(message.maxSupply);
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                $root.proto.Key.encode(message.feeScheduleKey, writer.uint32(/* id 22, wireType 2 =*/178).fork(), _depth + 1).ldelim();
            if (message.customFees != null && message.customFees.length)
                for (let i = 0; i < message.customFees.length; ++i)
                    $root.proto.CustomFee.encode(message.customFees[i], writer.uint32(/* id 23, wireType 2 =*/186).fork(), _depth + 1).ldelim();
            if (message.pauseKey != null && $Object.hasOwnProperty.call(message, "pauseKey"))
                $root.proto.Key.encode(message.pauseKey, writer.uint32(/* id 24, wireType 2 =*/194).fork(), _depth + 1).ldelim();
            if (message.pauseStatus != null && $Object.hasOwnProperty.call(message, "pauseStatus") && message.pauseStatus !== 0)
                writer.uint32(/* id 25, wireType 0 =*/200).int32(message.pauseStatus);
            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId") && message.ledgerId.length)
                writer.uint32(/* id 26, wireType 2 =*/210).bytes(message.ledgerId);
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata") && message.metadata.length)
                writer.uint32(/* id 27, wireType 2 =*/218).bytes(message.metadata);
            if (message.metadataKey != null && $Object.hasOwnProperty.call(message, "metadataKey"))
                $root.proto.Key.encode(message.metadataKey, writer.uint32(/* id 28, wireType 2 =*/226).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenInfo message, length delimited. Does not implicitly {@link proto.TokenInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenInfo
         * @static
         * @param {proto.TokenInfo.$Properties} message TokenInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenInfo & proto.TokenInfo.$Shape} TokenInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenInfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.symbol = value;
                        else
                            delete message.symbol;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.decimals = value;
                        else
                            delete message.decimals;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.totalSupply = value;
                        else
                            delete message.totalSupply;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.treasury = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.treasury);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.kycKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.kycKey);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.freezeKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.freezeKey);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.wipeKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.wipeKey);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.supplyKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.supplyKey);
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.defaultFreezeStatus = value;
                        else
                            delete message.defaultFreezeStatus;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.defaultKycStatus = value;
                        else
                            delete message.defaultKycStatus;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.deleted = value;
                        else
                            delete message.deleted;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewAccount = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewAccount);
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        message.expiry = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expiry);
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.tokenType = value;
                        else
                            delete message.tokenType;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.supplyType = value;
                        else
                            delete message.supplyType;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maxSupply = value;
                        else
                            delete message.maxSupply;
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        message.feeScheduleKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeScheduleKey);
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        if (!(message.customFees && message.customFees.length))
                            message.customFees = [];
                        message.customFees.push($root.proto.CustomFee.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        message.pauseKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pauseKey);
                        continue;
                    }
                case 25: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.pauseStatus = value;
                        else
                            delete message.pauseStatus;
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.ledgerId = value;
                        else
                            delete message.ledgerId;
                        continue;
                    }
                case 27: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.metadata = value;
                        else
                            delete message.metadata;
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        message.metadataKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.metadataKey);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenInfo & proto.TokenInfo.$Shape} TokenInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenInfo message.
         * @function verify
         * @memberof proto.TokenInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                if (!$util.isString(message.symbol))
                    return "symbol: string expected";
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals"))
                if (!$util.isInteger(message.decimals))
                    return "decimals: integer expected";
            if (message.totalSupply != null && $Object.hasOwnProperty.call(message, "totalSupply"))
                if (!$util.isInteger(message.totalSupply) && !(message.totalSupply && $util.isInteger(message.totalSupply.low) && $util.isInteger(message.totalSupply.high)))
                    return "totalSupply: integer|Long expected";
            if (message.treasury != null && $Object.hasOwnProperty.call(message, "treasury")) {
                let error = $root.proto.AccountID.verify(message.treasury, _depth + 1);
                if (error)
                    return "treasury." + error;
            }
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.kycKey != null && $Object.hasOwnProperty.call(message, "kycKey")) {
                let error = $root.proto.Key.verify(message.kycKey, _depth + 1);
                if (error)
                    return "kycKey." + error;
            }
            if (message.freezeKey != null && $Object.hasOwnProperty.call(message, "freezeKey")) {
                let error = $root.proto.Key.verify(message.freezeKey, _depth + 1);
                if (error)
                    return "freezeKey." + error;
            }
            if (message.wipeKey != null && $Object.hasOwnProperty.call(message, "wipeKey")) {
                let error = $root.proto.Key.verify(message.wipeKey, _depth + 1);
                if (error)
                    return "wipeKey." + error;
            }
            if (message.supplyKey != null && $Object.hasOwnProperty.call(message, "supplyKey")) {
                let error = $root.proto.Key.verify(message.supplyKey, _depth + 1);
                if (error)
                    return "supplyKey." + error;
            }
            if (message.defaultFreezeStatus != null && $Object.hasOwnProperty.call(message, "defaultFreezeStatus"))
                if (typeof message.defaultFreezeStatus !== "number" || (message.defaultFreezeStatus | 0) !== message.defaultFreezeStatus)
                    return "defaultFreezeStatus: enum value expected";
            if (message.defaultKycStatus != null && $Object.hasOwnProperty.call(message, "defaultKycStatus"))
                if (typeof message.defaultKycStatus !== "number" || (message.defaultKycStatus | 0) !== message.defaultKycStatus)
                    return "defaultKycStatus: enum value expected";
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                if (typeof message.deleted !== "boolean")
                    return "deleted: boolean expected";
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount")) {
                let error = $root.proto.AccountID.verify(message.autoRenewAccount, _depth + 1);
                if (error)
                    return "autoRenewAccount." + error;
            }
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                if (error)
                    return "autoRenewPeriod." + error;
            }
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry")) {
                let error = $root.proto.Timestamp.verify(message.expiry, _depth + 1);
                if (error)
                    return "expiry." + error;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.tokenType != null && $Object.hasOwnProperty.call(message, "tokenType"))
                if (typeof message.tokenType !== "number" || (message.tokenType | 0) !== message.tokenType)
                    return "tokenType: enum value expected";
            if (message.supplyType != null && $Object.hasOwnProperty.call(message, "supplyType"))
                if (typeof message.supplyType !== "number" || (message.supplyType | 0) !== message.supplyType)
                    return "supplyType: enum value expected";
            if (message.maxSupply != null && $Object.hasOwnProperty.call(message, "maxSupply"))
                if (!$util.isInteger(message.maxSupply) && !(message.maxSupply && $util.isInteger(message.maxSupply.low) && $util.isInteger(message.maxSupply.high)))
                    return "maxSupply: integer|Long expected";
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey")) {
                let error = $root.proto.Key.verify(message.feeScheduleKey, _depth + 1);
                if (error)
                    return "feeScheduleKey." + error;
            }
            if (message.customFees != null && $Object.hasOwnProperty.call(message, "customFees")) {
                if (!$Array.isArray(message.customFees))
                    return "customFees: array expected";
                for (let i = 0; i < message.customFees.length; ++i) {
                    let error = $root.proto.CustomFee.verify(message.customFees[i], _depth + 1);
                    if (error)
                        return "customFees." + error;
                }
            }
            if (message.pauseKey != null && $Object.hasOwnProperty.call(message, "pauseKey")) {
                let error = $root.proto.Key.verify(message.pauseKey, _depth + 1);
                if (error)
                    return "pauseKey." + error;
            }
            if (message.pauseStatus != null && $Object.hasOwnProperty.call(message, "pauseStatus"))
                if (typeof message.pauseStatus !== "number" || (message.pauseStatus | 0) !== message.pauseStatus)
                    return "pauseStatus: enum value expected";
            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                if (!(message.ledgerId && typeof message.ledgerId.length === "number" || $util.isString(message.ledgerId)))
                    return "ledgerId: buffer expected";
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                if (!(message.metadata && typeof message.metadata.length === "number" || $util.isString(message.metadata)))
                    return "metadata: buffer expected";
            if (message.metadataKey != null && $Object.hasOwnProperty.call(message, "metadataKey")) {
                let error = $root.proto.Key.verify(message.metadataKey, _depth + 1);
                if (error)
                    return "metadataKey." + error;
            }
            return null;
        };

        /**
         * Creates a TokenInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenInfo} TokenInfo
         */
        TokenInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenInfo();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.TokenInfo.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.symbol != null)
                if (typeof object.symbol !== "string" || object.symbol.length)
                    message.symbol = $String(object.symbol);
            if (object.decimals != null)
                if ($Number(object.decimals) !== 0)
                    message.decimals = object.decimals >>> 0;
            if (object.totalSupply != null)
                if (typeof object.totalSupply === "object" ? object.totalSupply.low || object.totalSupply.high : $Number(object.totalSupply) !== 0)
                    if ($util.Long)
                        message.totalSupply = $util.Long.fromValue(object.totalSupply, true);
                    else if (typeof object.totalSupply === "string")
                        message.totalSupply = $parseInt(object.totalSupply, 10);
                    else if (typeof object.totalSupply === "number")
                        message.totalSupply = object.totalSupply;
                    else if (typeof object.totalSupply === "object")
                        message.totalSupply = new $util.LongBits(object.totalSupply.low >>> 0, object.totalSupply.high >>> 0).toNumber(true);
            if (object.treasury != null) {
                if (!$util.isObject(object.treasury))
                    throw $TypeError(".proto.TokenInfo.treasury: object expected");
                message.treasury = $root.proto.AccountID.fromObject(object.treasury, _depth + 1);
            }
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.TokenInfo.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.kycKey != null) {
                if (!$util.isObject(object.kycKey))
                    throw $TypeError(".proto.TokenInfo.kycKey: object expected");
                message.kycKey = $root.proto.Key.fromObject(object.kycKey, _depth + 1);
            }
            if (object.freezeKey != null) {
                if (!$util.isObject(object.freezeKey))
                    throw $TypeError(".proto.TokenInfo.freezeKey: object expected");
                message.freezeKey = $root.proto.Key.fromObject(object.freezeKey, _depth + 1);
            }
            if (object.wipeKey != null) {
                if (!$util.isObject(object.wipeKey))
                    throw $TypeError(".proto.TokenInfo.wipeKey: object expected");
                message.wipeKey = $root.proto.Key.fromObject(object.wipeKey, _depth + 1);
            }
            if (object.supplyKey != null) {
                if (!$util.isObject(object.supplyKey))
                    throw $TypeError(".proto.TokenInfo.supplyKey: object expected");
                message.supplyKey = $root.proto.Key.fromObject(object.supplyKey, _depth + 1);
            }
            if (object.defaultFreezeStatus !== 0 && (typeof object.defaultFreezeStatus !== "string" || $root.proto.TokenFreezeStatus[object.defaultFreezeStatus] !== 0))
                switch (object.defaultFreezeStatus) {
                case "FreezeNotApplicable":
                case 0:
                    message.defaultFreezeStatus = 0;
                    break;
                case "Frozen":
                case 1:
                    message.defaultFreezeStatus = 1;
                    break;
                case "Unfrozen":
                case 2:
                    message.defaultFreezeStatus = 2;
                    break;
                default:
                    if (typeof object.defaultFreezeStatus === "number" && (object.defaultFreezeStatus | 0) === object.defaultFreezeStatus)
                        message.defaultFreezeStatus = object.defaultFreezeStatus;
                }
            if (object.defaultKycStatus !== 0 && (typeof object.defaultKycStatus !== "string" || $root.proto.TokenKycStatus[object.defaultKycStatus] !== 0))
                switch (object.defaultKycStatus) {
                case "KycNotApplicable":
                case 0:
                    message.defaultKycStatus = 0;
                    break;
                case "Granted":
                case 1:
                    message.defaultKycStatus = 1;
                    break;
                case "Revoked":
                case 2:
                    message.defaultKycStatus = 2;
                    break;
                default:
                    if (typeof object.defaultKycStatus === "number" && (object.defaultKycStatus | 0) === object.defaultKycStatus)
                        message.defaultKycStatus = object.defaultKycStatus;
                }
            if (object.deleted != null)
                if (object.deleted)
                    message.deleted = $Boolean(object.deleted);
            if (object.autoRenewAccount != null) {
                if (!$util.isObject(object.autoRenewAccount))
                    throw $TypeError(".proto.TokenInfo.autoRenewAccount: object expected");
                message.autoRenewAccount = $root.proto.AccountID.fromObject(object.autoRenewAccount, _depth + 1);
            }
            if (object.autoRenewPeriod != null) {
                if (!$util.isObject(object.autoRenewPeriod))
                    throw $TypeError(".proto.TokenInfo.autoRenewPeriod: object expected");
                message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
            }
            if (object.expiry != null) {
                if (!$util.isObject(object.expiry))
                    throw $TypeError(".proto.TokenInfo.expiry: object expected");
                message.expiry = $root.proto.Timestamp.fromObject(object.expiry, _depth + 1);
            }
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.tokenType !== 0 && (typeof object.tokenType !== "string" || $root.proto.TokenType[object.tokenType] !== 0))
                switch (object.tokenType) {
                case "FUNGIBLE_COMMON":
                case 0:
                    message.tokenType = 0;
                    break;
                case "NON_FUNGIBLE_UNIQUE":
                case 1:
                    message.tokenType = 1;
                    break;
                default:
                    if (typeof object.tokenType === "number" && (object.tokenType | 0) === object.tokenType)
                        message.tokenType = object.tokenType;
                }
            if (object.supplyType !== 0 && (typeof object.supplyType !== "string" || $root.proto.TokenSupplyType[object.supplyType] !== 0))
                switch (object.supplyType) {
                case "INFINITE":
                case 0:
                    message.supplyType = 0;
                    break;
                case "FINITE":
                case 1:
                    message.supplyType = 1;
                    break;
                default:
                    if (typeof object.supplyType === "number" && (object.supplyType | 0) === object.supplyType)
                        message.supplyType = object.supplyType;
                }
            if (object.maxSupply != null)
                if (typeof object.maxSupply === "object" ? object.maxSupply.low || object.maxSupply.high : $Number(object.maxSupply) !== 0)
                    if ($util.Long)
                        message.maxSupply = $util.Long.fromValue(object.maxSupply, false);
                    else if (typeof object.maxSupply === "string")
                        message.maxSupply = $parseInt(object.maxSupply, 10);
                    else if (typeof object.maxSupply === "number")
                        message.maxSupply = object.maxSupply;
                    else if (typeof object.maxSupply === "object")
                        message.maxSupply = new $util.LongBits(object.maxSupply.low >>> 0, object.maxSupply.high >>> 0).toNumber();
            if (object.feeScheduleKey != null) {
                if (!$util.isObject(object.feeScheduleKey))
                    throw $TypeError(".proto.TokenInfo.feeScheduleKey: object expected");
                message.feeScheduleKey = $root.proto.Key.fromObject(object.feeScheduleKey, _depth + 1);
            }
            if (object.customFees) {
                if (!$Array.isArray(object.customFees))
                    throw $TypeError(".proto.TokenInfo.customFees: array expected");
                message.customFees = $Array(object.customFees.length);
                for (let i = 0; i < object.customFees.length; ++i) {
                    if (!$util.isObject(object.customFees[i]))
                        throw $TypeError(".proto.TokenInfo.customFees: object expected");
                    message.customFees[i] = $root.proto.CustomFee.fromObject(object.customFees[i], _depth + 1);
                }
            }
            if (object.pauseKey != null) {
                if (!$util.isObject(object.pauseKey))
                    throw $TypeError(".proto.TokenInfo.pauseKey: object expected");
                message.pauseKey = $root.proto.Key.fromObject(object.pauseKey, _depth + 1);
            }
            if (object.pauseStatus !== 0 && (typeof object.pauseStatus !== "string" || $root.proto.TokenPauseStatus[object.pauseStatus] !== 0))
                switch (object.pauseStatus) {
                case "PauseNotApplicable":
                case 0:
                    message.pauseStatus = 0;
                    break;
                case "Paused":
                case 1:
                    message.pauseStatus = 1;
                    break;
                case "Unpaused":
                case 2:
                    message.pauseStatus = 2;
                    break;
                default:
                    if (typeof object.pauseStatus === "number" && (object.pauseStatus | 0) === object.pauseStatus)
                        message.pauseStatus = object.pauseStatus;
                }
            if (object.ledgerId != null)
                if (object.ledgerId.length)
                    if (typeof object.ledgerId === "string")
                        $util.base64.decode(object.ledgerId, message.ledgerId = $util.newBuffer($util.base64.length(object.ledgerId)), 0);
                    else if (object.ledgerId.length >= 0)
                        message.ledgerId = object.ledgerId;
            if (object.metadata != null)
                if (object.metadata.length)
                    if (typeof object.metadata === "string")
                        $util.base64.decode(object.metadata, message.metadata = $util.newBuffer($util.base64.length(object.metadata)), 0);
                    else if (object.metadata.length >= 0)
                        message.metadata = object.metadata;
            if (object.metadataKey != null) {
                if (!$util.isObject(object.metadataKey))
                    throw $TypeError(".proto.TokenInfo.metadataKey: object expected");
                message.metadataKey = $root.proto.Key.fromObject(object.metadataKey, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenInfo
         * @static
         * @param {proto.TokenInfo} message TokenInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.customFees = [];
            if (options.defaults) {
                object.tokenId = null;
                object.name = "";
                object.symbol = "";
                object.decimals = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.totalSupply = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.totalSupply = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.treasury = null;
                object.adminKey = null;
                object.kycKey = null;
                object.freezeKey = null;
                object.wipeKey = null;
                object.supplyKey = null;
                object.defaultFreezeStatus = options.enums === $String ? "FreezeNotApplicable" : 0;
                object.defaultKycStatus = options.enums === $String ? "KycNotApplicable" : 0;
                object.deleted = false;
                object.autoRenewAccount = null;
                object.autoRenewPeriod = null;
                object.expiry = null;
                object.memo = "";
                object.tokenType = options.enums === $String ? "FUNGIBLE_COMMON" : 0;
                object.supplyType = options.enums === $String ? "INFINITE" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxSupply = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maxSupply = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.feeScheduleKey = null;
                object.pauseKey = null;
                object.pauseStatus = options.enums === $String ? "PauseNotApplicable" : 0;
                if (options.bytes === $String)
                    object.ledgerId = "";
                else {
                    object.ledgerId = [];
                    if (options.bytes !== $Array)
                        object.ledgerId = $util.newBuffer(object.ledgerId);
                }
                if (options.bytes === $String)
                    object.metadata = "";
                else {
                    object.metadata = [];
                    if (options.bytes !== $Array)
                        object.metadata = $util.newBuffer(object.metadata);
                }
                object.metadataKey = null;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                object.symbol = message.symbol;
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals"))
                object.decimals = message.decimals;
            if (message.totalSupply != null && $Object.hasOwnProperty.call(message, "totalSupply"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.totalSupply = typeof message.totalSupply === "number" ? $BigInt(message.totalSupply) : $util.Long.fromBits(message.totalSupply.low >>> 0, message.totalSupply.high >>> 0, true).toBigInt();
                else if (typeof message.totalSupply === "number")
                    object.totalSupply = options.longs === $String ? $String(message.totalSupply) : message.totalSupply;
                else
                    object.totalSupply = options.longs === $String ? $util.Long.prototype.toString.call(message.totalSupply) : options.longs === $Number ? new $util.LongBits(message.totalSupply.low >>> 0, message.totalSupply.high >>> 0).toNumber(true) : message.totalSupply;
            if (message.treasury != null && $Object.hasOwnProperty.call(message, "treasury"))
                object.treasury = $root.proto.AccountID.toObject(message.treasury, options, _depth + 1);
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.kycKey != null && $Object.hasOwnProperty.call(message, "kycKey"))
                object.kycKey = $root.proto.Key.toObject(message.kycKey, options, _depth + 1);
            if (message.freezeKey != null && $Object.hasOwnProperty.call(message, "freezeKey"))
                object.freezeKey = $root.proto.Key.toObject(message.freezeKey, options, _depth + 1);
            if (message.wipeKey != null && $Object.hasOwnProperty.call(message, "wipeKey"))
                object.wipeKey = $root.proto.Key.toObject(message.wipeKey, options, _depth + 1);
            if (message.supplyKey != null && $Object.hasOwnProperty.call(message, "supplyKey"))
                object.supplyKey = $root.proto.Key.toObject(message.supplyKey, options, _depth + 1);
            if (message.defaultFreezeStatus != null && $Object.hasOwnProperty.call(message, "defaultFreezeStatus"))
                object.defaultFreezeStatus = options.enums === $String ? $root.proto.TokenFreezeStatus[message.defaultFreezeStatus] === $undefined ? message.defaultFreezeStatus : $root.proto.TokenFreezeStatus[message.defaultFreezeStatus] : message.defaultFreezeStatus;
            if (message.defaultKycStatus != null && $Object.hasOwnProperty.call(message, "defaultKycStatus"))
                object.defaultKycStatus = options.enums === $String ? $root.proto.TokenKycStatus[message.defaultKycStatus] === $undefined ? message.defaultKycStatus : $root.proto.TokenKycStatus[message.defaultKycStatus] : message.defaultKycStatus;
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                object.deleted = message.deleted;
            if (message.autoRenewAccount != null && $Object.hasOwnProperty.call(message, "autoRenewAccount"))
                object.autoRenewAccount = $root.proto.AccountID.toObject(message.autoRenewAccount, options, _depth + 1);
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
            if (message.expiry != null && $Object.hasOwnProperty.call(message, "expiry"))
                object.expiry = $root.proto.Timestamp.toObject(message.expiry, options, _depth + 1);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.tokenType != null && $Object.hasOwnProperty.call(message, "tokenType"))
                object.tokenType = options.enums === $String ? $root.proto.TokenType[message.tokenType] === $undefined ? message.tokenType : $root.proto.TokenType[message.tokenType] : message.tokenType;
            if (message.supplyType != null && $Object.hasOwnProperty.call(message, "supplyType"))
                object.supplyType = options.enums === $String ? $root.proto.TokenSupplyType[message.supplyType] === $undefined ? message.supplyType : $root.proto.TokenSupplyType[message.supplyType] : message.supplyType;
            if (message.maxSupply != null && $Object.hasOwnProperty.call(message, "maxSupply"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maxSupply = typeof message.maxSupply === "number" ? $BigInt(message.maxSupply) : $util.Long.fromBits(message.maxSupply.low >>> 0, message.maxSupply.high >>> 0, false).toBigInt();
                else if (typeof message.maxSupply === "number")
                    object.maxSupply = options.longs === $String ? $String(message.maxSupply) : message.maxSupply;
                else
                    object.maxSupply = options.longs === $String ? $util.Long.prototype.toString.call(message.maxSupply) : options.longs === $Number ? new $util.LongBits(message.maxSupply.low >>> 0, message.maxSupply.high >>> 0).toNumber() : message.maxSupply;
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                object.feeScheduleKey = $root.proto.Key.toObject(message.feeScheduleKey, options, _depth + 1);
            if (message.customFees && message.customFees.length) {
                object.customFees = $Array(message.customFees.length);
                for (let j = 0; j < message.customFees.length; ++j)
                    object.customFees[j] = $root.proto.CustomFee.toObject(message.customFees[j], options, _depth + 1);
            }
            if (message.pauseKey != null && $Object.hasOwnProperty.call(message, "pauseKey"))
                object.pauseKey = $root.proto.Key.toObject(message.pauseKey, options, _depth + 1);
            if (message.pauseStatus != null && $Object.hasOwnProperty.call(message, "pauseStatus"))
                object.pauseStatus = options.enums === $String ? $root.proto.TokenPauseStatus[message.pauseStatus] === $undefined ? message.pauseStatus : $root.proto.TokenPauseStatus[message.pauseStatus] : message.pauseStatus;
            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                object.ledgerId = options.bytes === $String ? $util.base64.encode(message.ledgerId, 0, message.ledgerId.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ledgerId) : message.ledgerId;
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                object.metadata = options.bytes === $String ? $util.base64.encode(message.metadata, 0, message.metadata.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.metadata) : message.metadata;
            if (message.metadataKey != null && $Object.hasOwnProperty.call(message, "metadataKey"))
                object.metadataKey = $root.proto.Key.toObject(message.metadataKey, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenInfo to JSON.
         * @function toJSON
         * @memberof proto.TokenInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenInfo.prototype.toJSON = function() {
            return TokenInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenInfo
         * @function getTypeUrl
         * @memberof proto.TokenInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenInfo";
        };

        return TokenInfo;
    })();

    proto.TokenGetInfoResponse = (function() {

        /**
         * Properties of a TokenGetInfoResponse.
         * @typedef {Object} proto.TokenGetInfoResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.TokenInfo.$Properties|null} [tokenInfo] The information requested for the identified token.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenGetInfoResponse.
         * @memberof proto
         * @interface ITokenGetInfoResponse
         * @augments proto.TokenGetInfoResponse.$Properties
         * @deprecated Use proto.TokenGetInfoResponse.$Properties instead.
         */

        /**
         * Shape of a TokenGetInfoResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   tokenInfo?: proto.TokenInfo.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenGetInfoResponse.$Shape
         */

        /**
         * Constructs a new TokenGetInfoResponse.
         * @memberof proto
         * @classdesc A response message for the `getTokenInfo` query.
         * @constructor
         * @param {proto.TokenGetInfoResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenGetInfoResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.TokenGetInfoResponse
         * @instance
         */
        TokenGetInfoResponse.prototype.header = null;

        /**
         * The information requested for the identified token.
         * @member {proto.TokenInfo.$Properties|null|undefined} tokenInfo
         * @memberof proto.TokenGetInfoResponse
         * @instance
         */
        TokenGetInfoResponse.prototype.tokenInfo = null;

        /**
         * Creates a new TokenGetInfoResponse instance using the specified properties.
         * @function create
         * @memberof proto.TokenGetInfoResponse
         * @static
         * @param {proto.TokenGetInfoResponse.$Properties=} [properties] Properties to set
         * @returns {proto.TokenGetInfoResponse} TokenGetInfoResponse instance
         * @type {{
         *   (properties: proto.TokenGetInfoResponse.$Shape): proto.TokenGetInfoResponse & proto.TokenGetInfoResponse.$Shape;
         *   (properties?: proto.TokenGetInfoResponse.$Properties): proto.TokenGetInfoResponse;
         * }}
         */
        TokenGetInfoResponse.create = function(properties) {
            return new TokenGetInfoResponse(properties);
        };

        /**
         * Encodes the specified TokenGetInfoResponse message. Does not implicitly {@link proto.TokenGetInfoResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenGetInfoResponse
         * @static
         * @param {proto.TokenGetInfoResponse.$Properties} message TokenGetInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetInfoResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.tokenInfo != null && $Object.hasOwnProperty.call(message, "tokenInfo"))
                $root.proto.TokenInfo.encode(message.tokenInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenGetInfoResponse message, length delimited. Does not implicitly {@link proto.TokenGetInfoResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenGetInfoResponse
         * @static
         * @param {proto.TokenGetInfoResponse.$Properties} message TokenGetInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetInfoResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenGetInfoResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenGetInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenGetInfoResponse & proto.TokenGetInfoResponse.$Shape} TokenGetInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetInfoResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenGetInfoResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.tokenInfo = $root.proto.TokenInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenInfo);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenGetInfoResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenGetInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenGetInfoResponse & proto.TokenGetInfoResponse.$Shape} TokenGetInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetInfoResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenGetInfoResponse message.
         * @function verify
         * @memberof proto.TokenGetInfoResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenGetInfoResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.tokenInfo != null && $Object.hasOwnProperty.call(message, "tokenInfo")) {
                let error = $root.proto.TokenInfo.verify(message.tokenInfo, _depth + 1);
                if (error)
                    return "tokenInfo." + error;
            }
            return null;
        };

        /**
         * Creates a TokenGetInfoResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenGetInfoResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenGetInfoResponse} TokenGetInfoResponse
         */
        TokenGetInfoResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenGetInfoResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenGetInfoResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenGetInfoResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TokenGetInfoResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.tokenInfo != null) {
                if (!$util.isObject(object.tokenInfo))
                    throw $TypeError(".proto.TokenGetInfoResponse.tokenInfo: object expected");
                message.tokenInfo = $root.proto.TokenInfo.fromObject(object.tokenInfo, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenGetInfoResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenGetInfoResponse
         * @static
         * @param {proto.TokenGetInfoResponse} message TokenGetInfoResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenGetInfoResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.tokenInfo = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.tokenInfo != null && $Object.hasOwnProperty.call(message, "tokenInfo"))
                object.tokenInfo = $root.proto.TokenInfo.toObject(message.tokenInfo, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenGetInfoResponse to JSON.
         * @function toJSON
         * @memberof proto.TokenGetInfoResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenGetInfoResponse.prototype.toJSON = function() {
            return TokenGetInfoResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenGetInfoResponse
         * @function getTypeUrl
         * @memberof proto.TokenGetInfoResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenGetInfoResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenGetInfoResponse";
        };

        return TokenGetInfoResponse;
    })();

    proto.ScheduleGetInfoQuery = (function() {

        /**
         * Properties of a ScheduleGetInfoQuery.
         * @typedef {Object} proto.ScheduleGetInfoQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.ScheduleID.$Properties|null} [scheduleID] A schedule identifier.
         * <p>
         * This SHALL identify the schedule to retrieve.<br/>
         * This field is REQUIRED.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ScheduleGetInfoQuery.
         * @memberof proto
         * @interface IScheduleGetInfoQuery
         * @augments proto.ScheduleGetInfoQuery.$Properties
         * @deprecated Use proto.ScheduleGetInfoQuery.$Properties instead.
         */

        /**
         * Shape of a ScheduleGetInfoQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   scheduleID?: proto.ScheduleID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ScheduleGetInfoQuery.$Shape
         */

        /**
         * Constructs a new ScheduleGetInfoQuery.
         * @memberof proto
         * @classdesc Request for information about a scheduled transaction.
         * 
         * If the requested schedule does not exist, the network SHALL respond
         * with `INVALID_SCHEDULE_ID`.
         * @constructor
         * @param {proto.ScheduleGetInfoQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ScheduleGetInfoQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.ScheduleGetInfoQuery
         * @instance
         */
        ScheduleGetInfoQuery.prototype.header = null;

        /**
         * A schedule identifier.
         * <p>
         * This SHALL identify the schedule to retrieve.<br/>
         * This field is REQUIRED.
         * @member {proto.ScheduleID.$Properties|null|undefined} scheduleID
         * @memberof proto.ScheduleGetInfoQuery
         * @instance
         */
        ScheduleGetInfoQuery.prototype.scheduleID = null;

        /**
         * Creates a new ScheduleGetInfoQuery instance using the specified properties.
         * @function create
         * @memberof proto.ScheduleGetInfoQuery
         * @static
         * @param {proto.ScheduleGetInfoQuery.$Properties=} [properties] Properties to set
         * @returns {proto.ScheduleGetInfoQuery} ScheduleGetInfoQuery instance
         * @type {{
         *   (properties: proto.ScheduleGetInfoQuery.$Shape): proto.ScheduleGetInfoQuery & proto.ScheduleGetInfoQuery.$Shape;
         *   (properties?: proto.ScheduleGetInfoQuery.$Properties): proto.ScheduleGetInfoQuery;
         * }}
         */
        ScheduleGetInfoQuery.create = function(properties) {
            return new ScheduleGetInfoQuery(properties);
        };

        /**
         * Encodes the specified ScheduleGetInfoQuery message. Does not implicitly {@link proto.ScheduleGetInfoQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.ScheduleGetInfoQuery
         * @static
         * @param {proto.ScheduleGetInfoQuery.$Properties} message ScheduleGetInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleGetInfoQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID"))
                $root.proto.ScheduleID.encode(message.scheduleID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ScheduleGetInfoQuery message, length delimited. Does not implicitly {@link proto.ScheduleGetInfoQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ScheduleGetInfoQuery
         * @static
         * @param {proto.ScheduleGetInfoQuery.$Properties} message ScheduleGetInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleGetInfoQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ScheduleGetInfoQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ScheduleGetInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ScheduleGetInfoQuery & proto.ScheduleGetInfoQuery.$Shape} ScheduleGetInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleGetInfoQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ScheduleGetInfoQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.scheduleID = $root.proto.ScheduleID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ScheduleGetInfoQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ScheduleGetInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ScheduleGetInfoQuery & proto.ScheduleGetInfoQuery.$Shape} ScheduleGetInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleGetInfoQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScheduleGetInfoQuery message.
         * @function verify
         * @memberof proto.ScheduleGetInfoQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScheduleGetInfoQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID")) {
                let error = $root.proto.ScheduleID.verify(message.scheduleID, _depth + 1);
                if (error)
                    return "scheduleID." + error;
            }
            return null;
        };

        /**
         * Creates a ScheduleGetInfoQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ScheduleGetInfoQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ScheduleGetInfoQuery} ScheduleGetInfoQuery
         */
        ScheduleGetInfoQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ScheduleGetInfoQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ScheduleGetInfoQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ScheduleGetInfoQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.ScheduleGetInfoQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.scheduleID != null) {
                if (!$util.isObject(object.scheduleID))
                    throw $TypeError(".proto.ScheduleGetInfoQuery.scheduleID: object expected");
                message.scheduleID = $root.proto.ScheduleID.fromObject(object.scheduleID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ScheduleGetInfoQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ScheduleGetInfoQuery
         * @static
         * @param {proto.ScheduleGetInfoQuery} message ScheduleGetInfoQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScheduleGetInfoQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.scheduleID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID"))
                object.scheduleID = $root.proto.ScheduleID.toObject(message.scheduleID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ScheduleGetInfoQuery to JSON.
         * @function toJSON
         * @memberof proto.ScheduleGetInfoQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScheduleGetInfoQuery.prototype.toJSON = function() {
            return ScheduleGetInfoQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ScheduleGetInfoQuery
         * @function getTypeUrl
         * @memberof proto.ScheduleGetInfoQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ScheduleGetInfoQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ScheduleGetInfoQuery";
        };

        return ScheduleGetInfoQuery;
    })();

    proto.ScheduleInfo = (function() {

        /**
         * Properties of a ScheduleInfo.
         * @typedef {Object} proto.ScheduleInfo.$Properties
         * @property {proto.ScheduleID.$Properties|null} [scheduleID] A schedule identifier.
         * <p>
         * This SHALL identify the schedule retrieved.
         * @property {proto.Timestamp.$Properties|null} [deletionTime] A deletion timestamp.
         * <p>
         * If the schedule was deleted, this SHALL be set to the consensus
         * timestamp of the `deleteSchedule` transaction.<br/>
         * If the schedule is _not_ deleted, this field SHALL NOT be set.
         * @property {proto.Timestamp.$Properties|null} [executionTime] An execution timestamp.
         * <p>
         * If the schedule was completed, and the _scheduled_ transaction
         * executed, this SHALL be set to the consensus timestamp of the
         * transaction that initiated that execution.<br/>
         * If the schedule is _not_ complete, this field SHALL NOT be set.
         * @property {proto.Timestamp.$Properties|null} [expirationTime] An expiration timestamp.<br/>
         * This represents the time at which the schedule will expire. For a
         * long-term schedule (if enabled) this is when the schedule will be
         * executed, assuming it meets signature requirements at that time.
         * For a short-term schedule, this is the deadline to complete the
         * signature requirements for the scheduled transaction to execute.
         * Regardless of schedule type, the schedule will be removed from
         * state when it expires.
         * <p>
         * A schedule SHALL be removed from state when it expires.<br/>
         * A short-term schedule MUST meet signature requirements strictly
         * before expiration or it SHALL NOT be executed.<br/>
         * A long-term schedule SHALL be executed if, and only if, all signature
         * requirements for the scheduled transaction are met at expiration.<br/>
         * A long-term schedule SHALL NOT be executed if any signature requirement
         * for the scheduled transaction are not met at expiration.<br/>
         * @property {proto.SchedulableTransactionBody.$Properties|null} [scheduledTransactionBody] A scheduled transaction.
         * <p>
         * This SHALL be a transaction type enabled in the network property
         * `scheduling.whitelist`, and SHALL NOT be any other
         * transaction type.<br/>
         * This transaction SHALL be executed if the schedule meets all signature
         * and execution time requirements for this transaction.<br/>
         * The signature requirements for this transaction SHALL be evaluated
         * at schedule creation, SHALL be reevaluated with each `signSchedule`
         * transaction, and, for long-term schedules, SHALL be reevaluated when
         * the schedule expires.<br/>
         * @property {string|null} [memo] A short description for this schedule.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {proto.Key.$Properties|null} [adminKey] The key used to delete the schedule from state
         * @property {proto.KeyList.$Properties|null} [signers] A list of "valid" signatures for this schedule.<br/>
         * This list contains only "primitive" (i.e. cryptographic or contract)
         * signatures. The full signature requirements for the scheduled
         * transaction are evaluated as if this list of keys had signed the
         * scheduled transaction directly.
         * <p>
         * This list SHALL contain every "primitive" key that has signed the
         * original `createSchedule`, or any subsequent
         * `signSchedule` transaction.<br/>
         * This list MAY elide any signature not likely to be required by the
         * scheduled transaction. Such requirement SHOULD be evaluated when the
         * signature is presented (i.e. during evaluation of a `createSchedule` or
         * `signSchedule` transaction).
         * @property {proto.AccountID.$Properties|null} [creatorAccountID] An account identifier.
         * <p>
         * This SHALL identify the account that created this schedule.
         * @property {proto.AccountID.$Properties|null} [payerAccountID] An account identifier.
         * <p>
         * The identified account SHALL pay the full transaction fee for the
         * scheduled transaction _when it executes_.
         * @property {proto.TransactionID.$Properties|null} [scheduledTransactionID] A transaction identifier.
         * <p>
         * This SHALL be recorded as the transaction identifier for the
         * _scheduled_ transaction, if (and when) it is executed.
         * @property {Uint8Array|null} [ledgerId] The ledger ID of the network that generated this response.
         * <p>
         * This value SHALL identify the distributed ledger that responded to
         * this query.
         * @property {boolean|null} [waitForExpiry] A flag indicating this schedule will execute when it expires.
         * <p>
         * If this field is set
         * <ul>
         * <li>This schedule SHALL be considered a "long-term" schedule.</li>
         * <li>This schedule SHALL be evaluated when the network consensus time
         * reaches the `expirationTime`, and if the signature requirements
         * for the scheduled transaction are met at that time, the
         * scheduled transaction SHALL be executed.</li>
         * <li>This schedule SHALL NOT be executed before the network consensus
         * time reaches the `expirationTime`.</li>
         * </ul>
         * If this field is not set
         * <ul>
         * <li>This schedule SHALL be considered a "short-term" schedule.</li>
         * <li>This schedule SHALL be evaluated when created, and reevaluated
         * with each `signSchedule` transaction, and if the signature
         * requirements for the scheduled transaction are met at that time,
         * the scheduled transaction SHALL be executed immediately.</li>
         * <li>This schedule SHALL be executed as soon as the signature
         * requirements are met, and MUST be executed before the network
         * consensus time reaches the `expirationTime`, if at all.</li>
         * </ul>
         * @property {"deletionTime"|"executionTime"} [data] ScheduleInfo data
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ScheduleInfo.
         * @memberof proto
         * @interface IScheduleInfo
         * @augments proto.ScheduleInfo.$Properties
         * @deprecated Use proto.ScheduleInfo.$Properties instead.
         */

        /**
         * Narrowed shape of a ScheduleInfo.
         * @typedef {{
         *   scheduleID?: proto.ScheduleID.$Shape|null;
         *   deletionTime?: proto.Timestamp.$Shape|null;
         *   executionTime?: proto.Timestamp.$Shape|null;
         *   expirationTime?: proto.Timestamp.$Shape|null;
         *   scheduledTransactionBody?: proto.SchedulableTransactionBody.$Shape|null;
         *   memo?: string|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   signers?: proto.KeyList.$Shape|null;
         *   creatorAccountID?: proto.AccountID.$Shape|null;
         *   payerAccountID?: proto.AccountID.$Shape|null;
         *   scheduledTransactionID?: proto.TransactionID.$Shape|null;
         *   ledgerId?: Uint8Array|null;
         *   waitForExpiry?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ data?: undefined; deletionTime?: null; executionTime?: null }|{ data?: "deletionTime"; deletionTime: proto.Timestamp.$Shape; executionTime?: null }|{ data?: "executionTime"; deletionTime?: null; executionTime: proto.Timestamp.$Shape })
         * )} proto.ScheduleInfo.$Shape
         */

        /**
         * Constructs a new ScheduleInfo.
         * @memberof proto
         * @classdesc Information summarizing schedule state
         * @constructor
         * @param {proto.ScheduleInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ScheduleInfo = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A schedule identifier.
         * <p>
         * This SHALL identify the schedule retrieved.
         * @member {proto.ScheduleID.$Properties|null|undefined} scheduleID
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.scheduleID = null;

        /**
         * A deletion timestamp.
         * <p>
         * If the schedule was deleted, this SHALL be set to the consensus
         * timestamp of the `deleteSchedule` transaction.<br/>
         * If the schedule is _not_ deleted, this field SHALL NOT be set.
         * @member {proto.Timestamp.$Properties|null|undefined} deletionTime
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.deletionTime = null;

        /**
         * An execution timestamp.
         * <p>
         * If the schedule was completed, and the _scheduled_ transaction
         * executed, this SHALL be set to the consensus timestamp of the
         * transaction that initiated that execution.<br/>
         * If the schedule is _not_ complete, this field SHALL NOT be set.
         * @member {proto.Timestamp.$Properties|null|undefined} executionTime
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.executionTime = null;

        /**
         * An expiration timestamp.<br/>
         * This represents the time at which the schedule will expire. For a
         * long-term schedule (if enabled) this is when the schedule will be
         * executed, assuming it meets signature requirements at that time.
         * For a short-term schedule, this is the deadline to complete the
         * signature requirements for the scheduled transaction to execute.
         * Regardless of schedule type, the schedule will be removed from
         * state when it expires.
         * <p>
         * A schedule SHALL be removed from state when it expires.<br/>
         * A short-term schedule MUST meet signature requirements strictly
         * before expiration or it SHALL NOT be executed.<br/>
         * A long-term schedule SHALL be executed if, and only if, all signature
         * requirements for the scheduled transaction are met at expiration.<br/>
         * A long-term schedule SHALL NOT be executed if any signature requirement
         * for the scheduled transaction are not met at expiration.<br/>
         * @member {proto.Timestamp.$Properties|null|undefined} expirationTime
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.expirationTime = null;

        /**
         * A scheduled transaction.
         * <p>
         * This SHALL be a transaction type enabled in the network property
         * `scheduling.whitelist`, and SHALL NOT be any other
         * transaction type.<br/>
         * This transaction SHALL be executed if the schedule meets all signature
         * and execution time requirements for this transaction.<br/>
         * The signature requirements for this transaction SHALL be evaluated
         * at schedule creation, SHALL be reevaluated with each `signSchedule`
         * transaction, and, for long-term schedules, SHALL be reevaluated when
         * the schedule expires.<br/>
         * @member {proto.SchedulableTransactionBody.$Properties|null|undefined} scheduledTransactionBody
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.scheduledTransactionBody = null;

        /**
         * A short description for this schedule.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.memo = "";

        /**
         * The key used to delete the schedule from state
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.adminKey = null;

        /**
         * A list of "valid" signatures for this schedule.<br/>
         * This list contains only "primitive" (i.e. cryptographic or contract)
         * signatures. The full signature requirements for the scheduled
         * transaction are evaluated as if this list of keys had signed the
         * scheduled transaction directly.
         * <p>
         * This list SHALL contain every "primitive" key that has signed the
         * original `createSchedule`, or any subsequent
         * `signSchedule` transaction.<br/>
         * This list MAY elide any signature not likely to be required by the
         * scheduled transaction. Such requirement SHOULD be evaluated when the
         * signature is presented (i.e. during evaluation of a `createSchedule` or
         * `signSchedule` transaction).
         * @member {proto.KeyList.$Properties|null|undefined} signers
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.signers = null;

        /**
         * An account identifier.
         * <p>
         * This SHALL identify the account that created this schedule.
         * @member {proto.AccountID.$Properties|null|undefined} creatorAccountID
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.creatorAccountID = null;

        /**
         * An account identifier.
         * <p>
         * The identified account SHALL pay the full transaction fee for the
         * scheduled transaction _when it executes_.
         * @member {proto.AccountID.$Properties|null|undefined} payerAccountID
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.payerAccountID = null;

        /**
         * A transaction identifier.
         * <p>
         * This SHALL be recorded as the transaction identifier for the
         * _scheduled_ transaction, if (and when) it is executed.
         * @member {proto.TransactionID.$Properties|null|undefined} scheduledTransactionID
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.scheduledTransactionID = null;

        /**
         * The ledger ID of the network that generated this response.
         * <p>
         * This value SHALL identify the distributed ledger that responded to
         * this query.
         * @member {Uint8Array} ledgerId
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.ledgerId = $util.newBuffer([]);

        /**
         * A flag indicating this schedule will execute when it expires.
         * <p>
         * If this field is set
         * <ul>
         * <li>This schedule SHALL be considered a "long-term" schedule.</li>
         * <li>This schedule SHALL be evaluated when the network consensus time
         * reaches the `expirationTime`, and if the signature requirements
         * for the scheduled transaction are met at that time, the
         * scheduled transaction SHALL be executed.</li>
         * <li>This schedule SHALL NOT be executed before the network consensus
         * time reaches the `expirationTime`.</li>
         * </ul>
         * If this field is not set
         * <ul>
         * <li>This schedule SHALL be considered a "short-term" schedule.</li>
         * <li>This schedule SHALL be evaluated when created, and reevaluated
         * with each `signSchedule` transaction, and if the signature
         * requirements for the scheduled transaction are met at that time,
         * the scheduled transaction SHALL be executed immediately.</li>
         * <li>This schedule SHALL be executed as soon as the signature
         * requirements are met, and MUST be executed before the network
         * consensus time reaches the `expirationTime`, if at all.</li>
         * </ul>
         * @member {boolean} waitForExpiry
         * @memberof proto.ScheduleInfo
         * @instance
         */
        ScheduleInfo.prototype.waitForExpiry = false;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ScheduleInfo data.
         * @member {"deletionTime"|"executionTime"|undefined} data
         * @memberof proto.ScheduleInfo
         * @instance
         */
        $Object.defineProperty(ScheduleInfo.prototype, "data", {
            get: $util.oneOfGetter($oneOfFields = ["deletionTime", "executionTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ScheduleInfo instance using the specified properties.
         * @function create
         * @memberof proto.ScheduleInfo
         * @static
         * @param {proto.ScheduleInfo.$Properties=} [properties] Properties to set
         * @returns {proto.ScheduleInfo} ScheduleInfo instance
         * @type {{
         *   (properties: proto.ScheduleInfo.$Shape): proto.ScheduleInfo & proto.ScheduleInfo.$Shape;
         *   (properties?: proto.ScheduleInfo.$Properties): proto.ScheduleInfo;
         * }}
         */
        ScheduleInfo.create = function(properties) {
            return new ScheduleInfo(properties);
        };

        /**
         * Encodes the specified ScheduleInfo message. Does not implicitly {@link proto.ScheduleInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.ScheduleInfo
         * @static
         * @param {proto.ScheduleInfo.$Properties} message ScheduleInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID"))
                $root.proto.ScheduleID.encode(message.scheduleID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.deletionTime != null && $Object.hasOwnProperty.call(message, "deletionTime"))
                $root.proto.Timestamp.encode(message.deletionTime, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.executionTime != null && $Object.hasOwnProperty.call(message, "executionTime"))
                $root.proto.Timestamp.encode(message.executionTime, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                $root.proto.Timestamp.encode(message.expirationTime, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.scheduledTransactionBody != null && $Object.hasOwnProperty.call(message, "scheduledTransactionBody"))
                $root.proto.SchedulableTransactionBody.encode(message.scheduledTransactionBody, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.memo);
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.signers != null && $Object.hasOwnProperty.call(message, "signers"))
                $root.proto.KeyList.encode(message.signers, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.creatorAccountID != null && $Object.hasOwnProperty.call(message, "creatorAccountID"))
                $root.proto.AccountID.encode(message.creatorAccountID, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.payerAccountID != null && $Object.hasOwnProperty.call(message, "payerAccountID"))
                $root.proto.AccountID.encode(message.payerAccountID, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.scheduledTransactionID != null && $Object.hasOwnProperty.call(message, "scheduledTransactionID"))
                $root.proto.TransactionID.encode(message.scheduledTransactionID, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId") && message.ledgerId.length)
                writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.ledgerId);
            if (message.waitForExpiry != null && $Object.hasOwnProperty.call(message, "waitForExpiry") && message.waitForExpiry !== false)
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.waitForExpiry);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ScheduleInfo message, length delimited. Does not implicitly {@link proto.ScheduleInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ScheduleInfo
         * @static
         * @param {proto.ScheduleInfo.$Properties} message ScheduleInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ScheduleInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ScheduleInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ScheduleInfo & proto.ScheduleInfo.$Shape} ScheduleInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ScheduleInfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.scheduleID = $root.proto.ScheduleID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.deletionTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.deletionTime);
                        message.data = "deletionTime";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.executionTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.executionTime);
                        message.data = "executionTime";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.expirationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.scheduledTransactionBody = $root.proto.SchedulableTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduledTransactionBody);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.signers = $root.proto.KeyList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.signers);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.creatorAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.creatorAccountID);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.payerAccountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.payerAccountID);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.scheduledTransactionID = $root.proto.TransactionID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduledTransactionID);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.ledgerId = value;
                        else
                            delete message.ledgerId;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.waitForExpiry = value;
                        else
                            delete message.waitForExpiry;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ScheduleInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ScheduleInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ScheduleInfo & proto.ScheduleInfo.$Shape} ScheduleInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScheduleInfo message.
         * @function verify
         * @memberof proto.ScheduleInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScheduleInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID")) {
                let error = $root.proto.ScheduleID.verify(message.scheduleID, _depth + 1);
                if (error)
                    return "scheduleID." + error;
            }
            if (message.deletionTime != null && $Object.hasOwnProperty.call(message, "deletionTime")) {
                properties.data = 1;
                {
                    let error = $root.proto.Timestamp.verify(message.deletionTime, _depth + 1);
                    if (error)
                        return "deletionTime." + error;
                }
            }
            if (message.executionTime != null && $Object.hasOwnProperty.call(message, "executionTime")) {
                if (properties.data === 1)
                    return "data: multiple values";
                properties.data = 1;
                {
                    let error = $root.proto.Timestamp.verify(message.executionTime, _depth + 1);
                    if (error)
                        return "executionTime." + error;
                }
            }
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                let error = $root.proto.Timestamp.verify(message.expirationTime, _depth + 1);
                if (error)
                    return "expirationTime." + error;
            }
            if (message.scheduledTransactionBody != null && $Object.hasOwnProperty.call(message, "scheduledTransactionBody")) {
                let error = $root.proto.SchedulableTransactionBody.verify(message.scheduledTransactionBody, _depth + 1);
                if (error)
                    return "scheduledTransactionBody." + error;
            }
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.signers != null && $Object.hasOwnProperty.call(message, "signers")) {
                let error = $root.proto.KeyList.verify(message.signers, _depth + 1);
                if (error)
                    return "signers." + error;
            }
            if (message.creatorAccountID != null && $Object.hasOwnProperty.call(message, "creatorAccountID")) {
                let error = $root.proto.AccountID.verify(message.creatorAccountID, _depth + 1);
                if (error)
                    return "creatorAccountID." + error;
            }
            if (message.payerAccountID != null && $Object.hasOwnProperty.call(message, "payerAccountID")) {
                let error = $root.proto.AccountID.verify(message.payerAccountID, _depth + 1);
                if (error)
                    return "payerAccountID." + error;
            }
            if (message.scheduledTransactionID != null && $Object.hasOwnProperty.call(message, "scheduledTransactionID")) {
                let error = $root.proto.TransactionID.verify(message.scheduledTransactionID, _depth + 1);
                if (error)
                    return "scheduledTransactionID." + error;
            }
            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                if (!(message.ledgerId && typeof message.ledgerId.length === "number" || $util.isString(message.ledgerId)))
                    return "ledgerId: buffer expected";
            if (message.waitForExpiry != null && $Object.hasOwnProperty.call(message, "waitForExpiry"))
                if (typeof message.waitForExpiry !== "boolean")
                    return "waitForExpiry: boolean expected";
            return null;
        };

        /**
         * Creates a ScheduleInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ScheduleInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ScheduleInfo} ScheduleInfo
         */
        ScheduleInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ScheduleInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ScheduleInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ScheduleInfo();
            if (object.scheduleID != null) {
                if (!$util.isObject(object.scheduleID))
                    throw $TypeError(".proto.ScheduleInfo.scheduleID: object expected");
                message.scheduleID = $root.proto.ScheduleID.fromObject(object.scheduleID, _depth + 1);
            }
            if (object.deletionTime != null) {
                if (!$util.isObject(object.deletionTime))
                    throw $TypeError(".proto.ScheduleInfo.deletionTime: object expected");
                message.deletionTime = $root.proto.Timestamp.fromObject(object.deletionTime, _depth + 1);
            }
            if (object.executionTime != null) {
                if (!$util.isObject(object.executionTime))
                    throw $TypeError(".proto.ScheduleInfo.executionTime: object expected");
                message.executionTime = $root.proto.Timestamp.fromObject(object.executionTime, _depth + 1);
            }
            if (object.expirationTime != null) {
                if (!$util.isObject(object.expirationTime))
                    throw $TypeError(".proto.ScheduleInfo.expirationTime: object expected");
                message.expirationTime = $root.proto.Timestamp.fromObject(object.expirationTime, _depth + 1);
            }
            if (object.scheduledTransactionBody != null) {
                if (!$util.isObject(object.scheduledTransactionBody))
                    throw $TypeError(".proto.ScheduleInfo.scheduledTransactionBody: object expected");
                message.scheduledTransactionBody = $root.proto.SchedulableTransactionBody.fromObject(object.scheduledTransactionBody, _depth + 1);
            }
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.ScheduleInfo.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.signers != null) {
                if (!$util.isObject(object.signers))
                    throw $TypeError(".proto.ScheduleInfo.signers: object expected");
                message.signers = $root.proto.KeyList.fromObject(object.signers, _depth + 1);
            }
            if (object.creatorAccountID != null) {
                if (!$util.isObject(object.creatorAccountID))
                    throw $TypeError(".proto.ScheduleInfo.creatorAccountID: object expected");
                message.creatorAccountID = $root.proto.AccountID.fromObject(object.creatorAccountID, _depth + 1);
            }
            if (object.payerAccountID != null) {
                if (!$util.isObject(object.payerAccountID))
                    throw $TypeError(".proto.ScheduleInfo.payerAccountID: object expected");
                message.payerAccountID = $root.proto.AccountID.fromObject(object.payerAccountID, _depth + 1);
            }
            if (object.scheduledTransactionID != null) {
                if (!$util.isObject(object.scheduledTransactionID))
                    throw $TypeError(".proto.ScheduleInfo.scheduledTransactionID: object expected");
                message.scheduledTransactionID = $root.proto.TransactionID.fromObject(object.scheduledTransactionID, _depth + 1);
            }
            if (object.ledgerId != null)
                if (object.ledgerId.length)
                    if (typeof object.ledgerId === "string")
                        $util.base64.decode(object.ledgerId, message.ledgerId = $util.newBuffer($util.base64.length(object.ledgerId)), 0);
                    else if (object.ledgerId.length >= 0)
                        message.ledgerId = object.ledgerId;
            if (object.waitForExpiry != null)
                if (object.waitForExpiry)
                    message.waitForExpiry = $Boolean(object.waitForExpiry);
            return message;
        };

        /**
         * Creates a plain object from a ScheduleInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ScheduleInfo
         * @static
         * @param {proto.ScheduleInfo} message ScheduleInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScheduleInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.scheduleID = null;
                object.expirationTime = null;
                object.scheduledTransactionBody = null;
                object.memo = "";
                object.adminKey = null;
                object.signers = null;
                object.creatorAccountID = null;
                object.payerAccountID = null;
                object.scheduledTransactionID = null;
                if (options.bytes === $String)
                    object.ledgerId = "";
                else {
                    object.ledgerId = [];
                    if (options.bytes !== $Array)
                        object.ledgerId = $util.newBuffer(object.ledgerId);
                }
                object.waitForExpiry = false;
            }
            if (message.scheduleID != null && $Object.hasOwnProperty.call(message, "scheduleID"))
                object.scheduleID = $root.proto.ScheduleID.toObject(message.scheduleID, options, _depth + 1);
            if (message.deletionTime != null && $Object.hasOwnProperty.call(message, "deletionTime")) {
                object.deletionTime = $root.proto.Timestamp.toObject(message.deletionTime, options, _depth + 1);
                if (options.oneofs)
                    object.data = "deletionTime";
            }
            if (message.executionTime != null && $Object.hasOwnProperty.call(message, "executionTime")) {
                object.executionTime = $root.proto.Timestamp.toObject(message.executionTime, options, _depth + 1);
                if (options.oneofs)
                    object.data = "executionTime";
            }
            if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                object.expirationTime = $root.proto.Timestamp.toObject(message.expirationTime, options, _depth + 1);
            if (message.scheduledTransactionBody != null && $Object.hasOwnProperty.call(message, "scheduledTransactionBody"))
                object.scheduledTransactionBody = $root.proto.SchedulableTransactionBody.toObject(message.scheduledTransactionBody, options, _depth + 1);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.signers != null && $Object.hasOwnProperty.call(message, "signers"))
                object.signers = $root.proto.KeyList.toObject(message.signers, options, _depth + 1);
            if (message.creatorAccountID != null && $Object.hasOwnProperty.call(message, "creatorAccountID"))
                object.creatorAccountID = $root.proto.AccountID.toObject(message.creatorAccountID, options, _depth + 1);
            if (message.payerAccountID != null && $Object.hasOwnProperty.call(message, "payerAccountID"))
                object.payerAccountID = $root.proto.AccountID.toObject(message.payerAccountID, options, _depth + 1);
            if (message.scheduledTransactionID != null && $Object.hasOwnProperty.call(message, "scheduledTransactionID"))
                object.scheduledTransactionID = $root.proto.TransactionID.toObject(message.scheduledTransactionID, options, _depth + 1);
            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                object.ledgerId = options.bytes === $String ? $util.base64.encode(message.ledgerId, 0, message.ledgerId.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ledgerId) : message.ledgerId;
            if (message.waitForExpiry != null && $Object.hasOwnProperty.call(message, "waitForExpiry"))
                object.waitForExpiry = message.waitForExpiry;
            return object;
        };

        /**
         * Converts this ScheduleInfo to JSON.
         * @function toJSON
         * @memberof proto.ScheduleInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScheduleInfo.prototype.toJSON = function() {
            return ScheduleInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ScheduleInfo
         * @function getTypeUrl
         * @memberof proto.ScheduleInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ScheduleInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ScheduleInfo";
        };

        return ScheduleInfo;
    })();

    proto.ScheduleGetInfoResponse = (function() {

        /**
         * Properties of a ScheduleGetInfoResponse.
         * @typedef {Object} proto.ScheduleGetInfoResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.ScheduleInfo.$Properties|null} [scheduleInfo] Detail information for a schedule.
         * <p>
         * This field SHALL contain all available schedule detail.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ScheduleGetInfoResponse.
         * @memberof proto
         * @interface IScheduleGetInfoResponse
         * @augments proto.ScheduleGetInfoResponse.$Properties
         * @deprecated Use proto.ScheduleGetInfoResponse.$Properties instead.
         */

        /**
         * Shape of a ScheduleGetInfoResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   scheduleInfo?: proto.ScheduleInfo.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ScheduleGetInfoResponse.$Shape
         */

        /**
         * Constructs a new ScheduleGetInfoResponse.
         * @memberof proto
         * @classdesc A response message for a `getScheduleInfo` query.
         * @constructor
         * @param {proto.ScheduleGetInfoResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ScheduleGetInfoResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.ScheduleGetInfoResponse
         * @instance
         */
        ScheduleGetInfoResponse.prototype.header = null;

        /**
         * Detail information for a schedule.
         * <p>
         * This field SHALL contain all available schedule detail.
         * @member {proto.ScheduleInfo.$Properties|null|undefined} scheduleInfo
         * @memberof proto.ScheduleGetInfoResponse
         * @instance
         */
        ScheduleGetInfoResponse.prototype.scheduleInfo = null;

        /**
         * Creates a new ScheduleGetInfoResponse instance using the specified properties.
         * @function create
         * @memberof proto.ScheduleGetInfoResponse
         * @static
         * @param {proto.ScheduleGetInfoResponse.$Properties=} [properties] Properties to set
         * @returns {proto.ScheduleGetInfoResponse} ScheduleGetInfoResponse instance
         * @type {{
         *   (properties: proto.ScheduleGetInfoResponse.$Shape): proto.ScheduleGetInfoResponse & proto.ScheduleGetInfoResponse.$Shape;
         *   (properties?: proto.ScheduleGetInfoResponse.$Properties): proto.ScheduleGetInfoResponse;
         * }}
         */
        ScheduleGetInfoResponse.create = function(properties) {
            return new ScheduleGetInfoResponse(properties);
        };

        /**
         * Encodes the specified ScheduleGetInfoResponse message. Does not implicitly {@link proto.ScheduleGetInfoResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.ScheduleGetInfoResponse
         * @static
         * @param {proto.ScheduleGetInfoResponse.$Properties} message ScheduleGetInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleGetInfoResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.scheduleInfo != null && $Object.hasOwnProperty.call(message, "scheduleInfo"))
                $root.proto.ScheduleInfo.encode(message.scheduleInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ScheduleGetInfoResponse message, length delimited. Does not implicitly {@link proto.ScheduleGetInfoResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ScheduleGetInfoResponse
         * @static
         * @param {proto.ScheduleGetInfoResponse.$Properties} message ScheduleGetInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleGetInfoResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ScheduleGetInfoResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ScheduleGetInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ScheduleGetInfoResponse & proto.ScheduleGetInfoResponse.$Shape} ScheduleGetInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleGetInfoResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ScheduleGetInfoResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.scheduleInfo = $root.proto.ScheduleInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleInfo);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ScheduleGetInfoResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ScheduleGetInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ScheduleGetInfoResponse & proto.ScheduleGetInfoResponse.$Shape} ScheduleGetInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleGetInfoResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScheduleGetInfoResponse message.
         * @function verify
         * @memberof proto.ScheduleGetInfoResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScheduleGetInfoResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.scheduleInfo != null && $Object.hasOwnProperty.call(message, "scheduleInfo")) {
                let error = $root.proto.ScheduleInfo.verify(message.scheduleInfo, _depth + 1);
                if (error)
                    return "scheduleInfo." + error;
            }
            return null;
        };

        /**
         * Creates a ScheduleGetInfoResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ScheduleGetInfoResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ScheduleGetInfoResponse} ScheduleGetInfoResponse
         */
        ScheduleGetInfoResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ScheduleGetInfoResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ScheduleGetInfoResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ScheduleGetInfoResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.ScheduleGetInfoResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.scheduleInfo != null) {
                if (!$util.isObject(object.scheduleInfo))
                    throw $TypeError(".proto.ScheduleGetInfoResponse.scheduleInfo: object expected");
                message.scheduleInfo = $root.proto.ScheduleInfo.fromObject(object.scheduleInfo, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ScheduleGetInfoResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ScheduleGetInfoResponse
         * @static
         * @param {proto.ScheduleGetInfoResponse} message ScheduleGetInfoResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScheduleGetInfoResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.scheduleInfo = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.scheduleInfo != null && $Object.hasOwnProperty.call(message, "scheduleInfo"))
                object.scheduleInfo = $root.proto.ScheduleInfo.toObject(message.scheduleInfo, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ScheduleGetInfoResponse to JSON.
         * @function toJSON
         * @memberof proto.ScheduleGetInfoResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScheduleGetInfoResponse.prototype.toJSON = function() {
            return ScheduleGetInfoResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ScheduleGetInfoResponse
         * @function getTypeUrl
         * @memberof proto.ScheduleGetInfoResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ScheduleGetInfoResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ScheduleGetInfoResponse";
        };

        return ScheduleGetInfoResponse;
    })();

    proto.TokenGetAccountNftInfosQuery = (function() {

        /**
         * Properties of a TokenGetAccountNftInfosQuery.
         * @typedef {Object} proto.TokenGetAccountNftInfosQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.AccountID.$Properties|null} [accountID] The Account for which information is requested
         * @property {number|Long|null} [start] Specifies the start index (inclusive) of the range of NFTs to query for.
         * Value must be in the range [0; ownedNFTs-1]
         * @property {number|Long|null} [end] Specifies the end index (exclusive) of the range of NFTs to query for.
         * Value must be in the range (start; ownedNFTs]
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenGetAccountNftInfosQuery.
         * @memberof proto
         * @interface ITokenGetAccountNftInfosQuery
         * @augments proto.TokenGetAccountNftInfosQuery.$Properties
         * @deprecated Use proto.TokenGetAccountNftInfosQuery.$Properties instead.
         */

        /**
         * Shape of a TokenGetAccountNftInfosQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   accountID?: proto.AccountID.$Shape|null;
         *   start?: number|Long|null;
         *   end?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenGetAccountNftInfosQuery.$Shape
         */

        /**
         * Constructs a new TokenGetAccountNftInfosQuery.
         * @memberof proto
         * @classdesc Deleted and unsupported.
         * 
         * This query is not implemented and any query of this type submitted
         * SHALL return a `NOT_SUPPORTED` response code.
         * @constructor
         * @param {proto.TokenGetAccountNftInfosQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenGetAccountNftInfosQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @instance
         */
        TokenGetAccountNftInfosQuery.prototype.header = null;

        /**
         * The Account for which information is requested
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @instance
         */
        TokenGetAccountNftInfosQuery.prototype.accountID = null;

        /**
         * Specifies the start index (inclusive) of the range of NFTs to query for.
         * Value must be in the range [0; ownedNFTs-1]
         * @member {number|Long} start
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @instance
         */
        TokenGetAccountNftInfosQuery.prototype.start = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Specifies the end index (exclusive) of the range of NFTs to query for.
         * Value must be in the range (start; ownedNFTs]
         * @member {number|Long} end
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @instance
         */
        TokenGetAccountNftInfosQuery.prototype.end = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new TokenGetAccountNftInfosQuery instance using the specified properties.
         * @function create
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @static
         * @param {proto.TokenGetAccountNftInfosQuery.$Properties=} [properties] Properties to set
         * @returns {proto.TokenGetAccountNftInfosQuery} TokenGetAccountNftInfosQuery instance
         * @type {{
         *   (properties: proto.TokenGetAccountNftInfosQuery.$Shape): proto.TokenGetAccountNftInfosQuery & proto.TokenGetAccountNftInfosQuery.$Shape;
         *   (properties?: proto.TokenGetAccountNftInfosQuery.$Properties): proto.TokenGetAccountNftInfosQuery;
         * }}
         */
        TokenGetAccountNftInfosQuery.create = function(properties) {
            return new TokenGetAccountNftInfosQuery(properties);
        };

        /**
         * Encodes the specified TokenGetAccountNftInfosQuery message. Does not implicitly {@link proto.TokenGetAccountNftInfosQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @static
         * @param {proto.TokenGetAccountNftInfosQuery.$Properties} message TokenGetAccountNftInfosQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetAccountNftInfosQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.start != null && $Object.hasOwnProperty.call(message, "start") && (typeof message.start === "object" ? message.start.low || message.start.high : message.start !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.start);
            if (message.end != null && $Object.hasOwnProperty.call(message, "end") && (typeof message.end === "object" ? message.end.low || message.end.high : message.end !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.end);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenGetAccountNftInfosQuery message, length delimited. Does not implicitly {@link proto.TokenGetAccountNftInfosQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @static
         * @param {proto.TokenGetAccountNftInfosQuery.$Properties} message TokenGetAccountNftInfosQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetAccountNftInfosQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenGetAccountNftInfosQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenGetAccountNftInfosQuery & proto.TokenGetAccountNftInfosQuery.$Shape} TokenGetAccountNftInfosQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetAccountNftInfosQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenGetAccountNftInfosQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.start = value;
                        else
                            delete message.start;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.end = value;
                        else
                            delete message.end;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenGetAccountNftInfosQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenGetAccountNftInfosQuery & proto.TokenGetAccountNftInfosQuery.$Shape} TokenGetAccountNftInfosQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetAccountNftInfosQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenGetAccountNftInfosQuery message.
         * @function verify
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenGetAccountNftInfosQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                if (!$util.isInteger(message.start) && !(message.start && $util.isInteger(message.start.low) && $util.isInteger(message.start.high)))
                    return "start: integer|Long expected";
            if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                if (!$util.isInteger(message.end) && !(message.end && $util.isInteger(message.end.low) && $util.isInteger(message.end.high)))
                    return "end: integer|Long expected";
            return null;
        };

        /**
         * Creates a TokenGetAccountNftInfosQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenGetAccountNftInfosQuery} TokenGetAccountNftInfosQuery
         */
        TokenGetAccountNftInfosQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenGetAccountNftInfosQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenGetAccountNftInfosQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenGetAccountNftInfosQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TokenGetAccountNftInfosQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.TokenGetAccountNftInfosQuery.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.start != null)
                if (typeof object.start === "object" ? object.start.low || object.start.high : $Number(object.start) !== 0)
                    if ($util.Long)
                        message.start = $util.Long.fromValue(object.start, false);
                    else if (typeof object.start === "string")
                        message.start = $parseInt(object.start, 10);
                    else if (typeof object.start === "number")
                        message.start = object.start;
                    else if (typeof object.start === "object")
                        message.start = new $util.LongBits(object.start.low >>> 0, object.start.high >>> 0).toNumber();
            if (object.end != null)
                if (typeof object.end === "object" ? object.end.low || object.end.high : $Number(object.end) !== 0)
                    if ($util.Long)
                        message.end = $util.Long.fromValue(object.end, false);
                    else if (typeof object.end === "string")
                        message.end = $parseInt(object.end, 10);
                    else if (typeof object.end === "number")
                        message.end = object.end;
                    else if (typeof object.end === "object")
                        message.end = new $util.LongBits(object.end.low >>> 0, object.end.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a TokenGetAccountNftInfosQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @static
         * @param {proto.TokenGetAccountNftInfosQuery} message TokenGetAccountNftInfosQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenGetAccountNftInfosQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.accountID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.start = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.start = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.end = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.end = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.start = typeof message.start === "number" ? $BigInt(message.start) : $util.Long.fromBits(message.start.low >>> 0, message.start.high >>> 0, false).toBigInt();
                else if (typeof message.start === "number")
                    object.start = options.longs === $String ? $String(message.start) : message.start;
                else
                    object.start = options.longs === $String ? $util.Long.prototype.toString.call(message.start) : options.longs === $Number ? new $util.LongBits(message.start.low >>> 0, message.start.high >>> 0).toNumber() : message.start;
            if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.end = typeof message.end === "number" ? $BigInt(message.end) : $util.Long.fromBits(message.end.low >>> 0, message.end.high >>> 0, false).toBigInt();
                else if (typeof message.end === "number")
                    object.end = options.longs === $String ? $String(message.end) : message.end;
                else
                    object.end = options.longs === $String ? $util.Long.prototype.toString.call(message.end) : options.longs === $Number ? new $util.LongBits(message.end.low >>> 0, message.end.high >>> 0).toNumber() : message.end;
            return object;
        };

        /**
         * Converts this TokenGetAccountNftInfosQuery to JSON.
         * @function toJSON
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenGetAccountNftInfosQuery.prototype.toJSON = function() {
            return TokenGetAccountNftInfosQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenGetAccountNftInfosQuery
         * @function getTypeUrl
         * @memberof proto.TokenGetAccountNftInfosQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenGetAccountNftInfosQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenGetAccountNftInfosQuery";
        };

        return TokenGetAccountNftInfosQuery;
    })();

    proto.TokenGetAccountNftInfosResponse = (function() {

        /**
         * Properties of a TokenGetAccountNftInfosResponse.
         * @typedef {Object} proto.TokenGetAccountNftInfosResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {Array.<proto.TokenNftInfo.$Properties>|null} [nfts] List of NFTs associated to the account
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenGetAccountNftInfosResponse.
         * @memberof proto
         * @interface ITokenGetAccountNftInfosResponse
         * @augments proto.TokenGetAccountNftInfosResponse.$Properties
         * @deprecated Use proto.TokenGetAccountNftInfosResponse.$Properties instead.
         */

        /**
         * Shape of a TokenGetAccountNftInfosResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   nfts?: Array.<proto.TokenNftInfo.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenGetAccountNftInfosResponse.$Shape
         */

        /**
         * Constructs a new TokenGetAccountNftInfosResponse.
         * @memberof proto
         * @classdesc Deleted and unsupported.
         * @constructor
         * @param {proto.TokenGetAccountNftInfosResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenGetAccountNftInfosResponse = function (properties) {
            this.nfts = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.TokenGetAccountNftInfosResponse
         * @instance
         */
        TokenGetAccountNftInfosResponse.prototype.header = null;

        /**
         * List of NFTs associated to the account
         * @member {Array.<proto.TokenNftInfo.$Properties>} nfts
         * @memberof proto.TokenGetAccountNftInfosResponse
         * @instance
         */
        TokenGetAccountNftInfosResponse.prototype.nfts = $util.emptyArray;

        /**
         * Creates a new TokenGetAccountNftInfosResponse instance using the specified properties.
         * @function create
         * @memberof proto.TokenGetAccountNftInfosResponse
         * @static
         * @param {proto.TokenGetAccountNftInfosResponse.$Properties=} [properties] Properties to set
         * @returns {proto.TokenGetAccountNftInfosResponse} TokenGetAccountNftInfosResponse instance
         * @type {{
         *   (properties: proto.TokenGetAccountNftInfosResponse.$Shape): proto.TokenGetAccountNftInfosResponse & proto.TokenGetAccountNftInfosResponse.$Shape;
         *   (properties?: proto.TokenGetAccountNftInfosResponse.$Properties): proto.TokenGetAccountNftInfosResponse;
         * }}
         */
        TokenGetAccountNftInfosResponse.create = function(properties) {
            return new TokenGetAccountNftInfosResponse(properties);
        };

        /**
         * Encodes the specified TokenGetAccountNftInfosResponse message. Does not implicitly {@link proto.TokenGetAccountNftInfosResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenGetAccountNftInfosResponse
         * @static
         * @param {proto.TokenGetAccountNftInfosResponse.$Properties} message TokenGetAccountNftInfosResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetAccountNftInfosResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.nfts != null && message.nfts.length)
                for (let i = 0; i < message.nfts.length; ++i)
                    $root.proto.TokenNftInfo.encode(message.nfts[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenGetAccountNftInfosResponse message, length delimited. Does not implicitly {@link proto.TokenGetAccountNftInfosResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenGetAccountNftInfosResponse
         * @static
         * @param {proto.TokenGetAccountNftInfosResponse.$Properties} message TokenGetAccountNftInfosResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetAccountNftInfosResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenGetAccountNftInfosResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenGetAccountNftInfosResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenGetAccountNftInfosResponse & proto.TokenGetAccountNftInfosResponse.$Shape} TokenGetAccountNftInfosResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetAccountNftInfosResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenGetAccountNftInfosResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.nfts && message.nfts.length))
                            message.nfts = [];
                        message.nfts.push($root.proto.TokenNftInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenGetAccountNftInfosResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenGetAccountNftInfosResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenGetAccountNftInfosResponse & proto.TokenGetAccountNftInfosResponse.$Shape} TokenGetAccountNftInfosResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetAccountNftInfosResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenGetAccountNftInfosResponse message.
         * @function verify
         * @memberof proto.TokenGetAccountNftInfosResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenGetAccountNftInfosResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.nfts != null && $Object.hasOwnProperty.call(message, "nfts")) {
                if (!$Array.isArray(message.nfts))
                    return "nfts: array expected";
                for (let i = 0; i < message.nfts.length; ++i) {
                    let error = $root.proto.TokenNftInfo.verify(message.nfts[i], _depth + 1);
                    if (error)
                        return "nfts." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TokenGetAccountNftInfosResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenGetAccountNftInfosResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenGetAccountNftInfosResponse} TokenGetAccountNftInfosResponse
         */
        TokenGetAccountNftInfosResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenGetAccountNftInfosResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenGetAccountNftInfosResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenGetAccountNftInfosResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TokenGetAccountNftInfosResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.nfts) {
                if (!$Array.isArray(object.nfts))
                    throw $TypeError(".proto.TokenGetAccountNftInfosResponse.nfts: array expected");
                message.nfts = $Array(object.nfts.length);
                for (let i = 0; i < object.nfts.length; ++i) {
                    if (!$util.isObject(object.nfts[i]))
                        throw $TypeError(".proto.TokenGetAccountNftInfosResponse.nfts: object expected");
                    message.nfts[i] = $root.proto.TokenNftInfo.fromObject(object.nfts[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenGetAccountNftInfosResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenGetAccountNftInfosResponse
         * @static
         * @param {proto.TokenGetAccountNftInfosResponse} message TokenGetAccountNftInfosResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenGetAccountNftInfosResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.nfts = [];
            if (options.defaults)
                object.header = null;
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.nfts && message.nfts.length) {
                object.nfts = $Array(message.nfts.length);
                for (let j = 0; j < message.nfts.length; ++j)
                    object.nfts[j] = $root.proto.TokenNftInfo.toObject(message.nfts[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TokenGetAccountNftInfosResponse to JSON.
         * @function toJSON
         * @memberof proto.TokenGetAccountNftInfosResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenGetAccountNftInfosResponse.prototype.toJSON = function() {
            return TokenGetAccountNftInfosResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenGetAccountNftInfosResponse
         * @function getTypeUrl
         * @memberof proto.TokenGetAccountNftInfosResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenGetAccountNftInfosResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenGetAccountNftInfosResponse";
        };

        return TokenGetAccountNftInfosResponse;
    })();

    proto.TokenGetNftInfoQuery = (function() {

        /**
         * Properties of a TokenGetNftInfoQuery.
         * @typedef {Object} proto.TokenGetNftInfoQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.NftID.$Properties|null} [nftID] A non-fungible/unique token (NFT) identifier.
         * <p>
         * This SHALL identify the NFT to query.<br/>
         * The identified NFT MUST exist, and MUST NOT be deleted.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenGetNftInfoQuery.
         * @memberof proto
         * @interface ITokenGetNftInfoQuery
         * @augments proto.TokenGetNftInfoQuery.$Properties
         * @deprecated Use proto.TokenGetNftInfoQuery.$Properties instead.
         */

        /**
         * Shape of a TokenGetNftInfoQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   nftID?: proto.NftID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenGetNftInfoQuery.$Shape
         */

        /**
         * Constructs a new TokenGetNftInfoQuery.
         * @memberof proto
         * @classdesc Applicable only to tokens of type NON_FUNGIBLE_UNIQUE. Gets info on a NFT for a given TokenID (of
         * type NON_FUNGIBLE_UNIQUE) and serial number
         * @constructor
         * @param {proto.TokenGetNftInfoQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenGetNftInfoQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.TokenGetNftInfoQuery
         * @instance
         */
        TokenGetNftInfoQuery.prototype.header = null;

        /**
         * A non-fungible/unique token (NFT) identifier.
         * <p>
         * This SHALL identify the NFT to query.<br/>
         * The identified NFT MUST exist, and MUST NOT be deleted.
         * @member {proto.NftID.$Properties|null|undefined} nftID
         * @memberof proto.TokenGetNftInfoQuery
         * @instance
         */
        TokenGetNftInfoQuery.prototype.nftID = null;

        /**
         * Creates a new TokenGetNftInfoQuery instance using the specified properties.
         * @function create
         * @memberof proto.TokenGetNftInfoQuery
         * @static
         * @param {proto.TokenGetNftInfoQuery.$Properties=} [properties] Properties to set
         * @returns {proto.TokenGetNftInfoQuery} TokenGetNftInfoQuery instance
         * @type {{
         *   (properties: proto.TokenGetNftInfoQuery.$Shape): proto.TokenGetNftInfoQuery & proto.TokenGetNftInfoQuery.$Shape;
         *   (properties?: proto.TokenGetNftInfoQuery.$Properties): proto.TokenGetNftInfoQuery;
         * }}
         */
        TokenGetNftInfoQuery.create = function(properties) {
            return new TokenGetNftInfoQuery(properties);
        };

        /**
         * Encodes the specified TokenGetNftInfoQuery message. Does not implicitly {@link proto.TokenGetNftInfoQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenGetNftInfoQuery
         * @static
         * @param {proto.TokenGetNftInfoQuery.$Properties} message TokenGetNftInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetNftInfoQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.nftID != null && $Object.hasOwnProperty.call(message, "nftID"))
                $root.proto.NftID.encode(message.nftID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenGetNftInfoQuery message, length delimited. Does not implicitly {@link proto.TokenGetNftInfoQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenGetNftInfoQuery
         * @static
         * @param {proto.TokenGetNftInfoQuery.$Properties} message TokenGetNftInfoQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetNftInfoQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenGetNftInfoQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenGetNftInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenGetNftInfoQuery & proto.TokenGetNftInfoQuery.$Shape} TokenGetNftInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetNftInfoQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenGetNftInfoQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.nftID = $root.proto.NftID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nftID);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenGetNftInfoQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenGetNftInfoQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenGetNftInfoQuery & proto.TokenGetNftInfoQuery.$Shape} TokenGetNftInfoQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetNftInfoQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenGetNftInfoQuery message.
         * @function verify
         * @memberof proto.TokenGetNftInfoQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenGetNftInfoQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.nftID != null && $Object.hasOwnProperty.call(message, "nftID")) {
                let error = $root.proto.NftID.verify(message.nftID, _depth + 1);
                if (error)
                    return "nftID." + error;
            }
            return null;
        };

        /**
         * Creates a TokenGetNftInfoQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenGetNftInfoQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenGetNftInfoQuery} TokenGetNftInfoQuery
         */
        TokenGetNftInfoQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenGetNftInfoQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenGetNftInfoQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenGetNftInfoQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TokenGetNftInfoQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.nftID != null) {
                if (!$util.isObject(object.nftID))
                    throw $TypeError(".proto.TokenGetNftInfoQuery.nftID: object expected");
                message.nftID = $root.proto.NftID.fromObject(object.nftID, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenGetNftInfoQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenGetNftInfoQuery
         * @static
         * @param {proto.TokenGetNftInfoQuery} message TokenGetNftInfoQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenGetNftInfoQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.nftID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.nftID != null && $Object.hasOwnProperty.call(message, "nftID"))
                object.nftID = $root.proto.NftID.toObject(message.nftID, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenGetNftInfoQuery to JSON.
         * @function toJSON
         * @memberof proto.TokenGetNftInfoQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenGetNftInfoQuery.prototype.toJSON = function() {
            return TokenGetNftInfoQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenGetNftInfoQuery
         * @function getTypeUrl
         * @memberof proto.TokenGetNftInfoQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenGetNftInfoQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenGetNftInfoQuery";
        };

        return TokenGetNftInfoQuery;
    })();

    proto.TokenNftInfo = (function() {

        /**
         * Properties of a TokenNftInfo.
         * @typedef {Object} proto.TokenNftInfo.$Properties
         * @property {proto.NftID.$Properties|null} [nftID] A non-fungible/unique token (NFT) identifier.
         * <p>
         * This SHALL match the NFT requested.<br/>
         * @property {proto.AccountID.$Properties|null} [accountID] The current owner of the NFT
         * @property {proto.Timestamp.$Properties|null} [creationTime] The effective consensus timestamp at which the NFT was minted
         * @property {Uint8Array|null} [metadata] Represents the unique metadata of the NFT
         * @property {Uint8Array|null} [ledgerId] The ledger ID of the network that generated this response.
         * <p>
         * This value SHALL identify the distributed ledger that responded to
         * this query.
         * @property {proto.AccountID.$Properties|null} [spenderId] If an allowance is granted for the NFT, its corresponding spender account
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenNftInfo.
         * @memberof proto
         * @interface ITokenNftInfo
         * @augments proto.TokenNftInfo.$Properties
         * @deprecated Use proto.TokenNftInfo.$Properties instead.
         */

        /**
         * Shape of a TokenNftInfo.
         * @typedef {{
         *   nftID?: proto.NftID.$Shape|null;
         *   accountID?: proto.AccountID.$Shape|null;
         *   creationTime?: proto.Timestamp.$Shape|null;
         *   metadata?: Uint8Array|null;
         *   ledgerId?: Uint8Array|null;
         *   spenderId?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenNftInfo.$Shape
         */

        /**
         * Constructs a new TokenNftInfo.
         * @memberof proto
         * @classdesc Information for one non-fungible/unique token (NFT).
         * @constructor
         * @param {proto.TokenNftInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenNftInfo = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A non-fungible/unique token (NFT) identifier.
         * <p>
         * This SHALL match the NFT requested.<br/>
         * @member {proto.NftID.$Properties|null|undefined} nftID
         * @memberof proto.TokenNftInfo
         * @instance
         */
        TokenNftInfo.prototype.nftID = null;

        /**
         * The current owner of the NFT
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.TokenNftInfo
         * @instance
         */
        TokenNftInfo.prototype.accountID = null;

        /**
         * The effective consensus timestamp at which the NFT was minted
         * @member {proto.Timestamp.$Properties|null|undefined} creationTime
         * @memberof proto.TokenNftInfo
         * @instance
         */
        TokenNftInfo.prototype.creationTime = null;

        /**
         * Represents the unique metadata of the NFT
         * @member {Uint8Array} metadata
         * @memberof proto.TokenNftInfo
         * @instance
         */
        TokenNftInfo.prototype.metadata = $util.newBuffer([]);

        /**
         * The ledger ID of the network that generated this response.
         * <p>
         * This value SHALL identify the distributed ledger that responded to
         * this query.
         * @member {Uint8Array} ledgerId
         * @memberof proto.TokenNftInfo
         * @instance
         */
        TokenNftInfo.prototype.ledgerId = $util.newBuffer([]);

        /**
         * If an allowance is granted for the NFT, its corresponding spender account
         * @member {proto.AccountID.$Properties|null|undefined} spenderId
         * @memberof proto.TokenNftInfo
         * @instance
         */
        TokenNftInfo.prototype.spenderId = null;

        /**
         * Creates a new TokenNftInfo instance using the specified properties.
         * @function create
         * @memberof proto.TokenNftInfo
         * @static
         * @param {proto.TokenNftInfo.$Properties=} [properties] Properties to set
         * @returns {proto.TokenNftInfo} TokenNftInfo instance
         * @type {{
         *   (properties: proto.TokenNftInfo.$Shape): proto.TokenNftInfo & proto.TokenNftInfo.$Shape;
         *   (properties?: proto.TokenNftInfo.$Properties): proto.TokenNftInfo;
         * }}
         */
        TokenNftInfo.create = function(properties) {
            return new TokenNftInfo(properties);
        };

        /**
         * Encodes the specified TokenNftInfo message. Does not implicitly {@link proto.TokenNftInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenNftInfo
         * @static
         * @param {proto.TokenNftInfo.$Properties} message TokenNftInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenNftInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nftID != null && $Object.hasOwnProperty.call(message, "nftID"))
                $root.proto.NftID.encode(message.nftID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.creationTime != null && $Object.hasOwnProperty.call(message, "creationTime"))
                $root.proto.Timestamp.encode(message.creationTime, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata") && message.metadata.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.metadata);
            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId") && message.ledgerId.length)
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.ledgerId);
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId"))
                $root.proto.AccountID.encode(message.spenderId, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenNftInfo message, length delimited. Does not implicitly {@link proto.TokenNftInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenNftInfo
         * @static
         * @param {proto.TokenNftInfo.$Properties} message TokenNftInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenNftInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenNftInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenNftInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenNftInfo & proto.TokenNftInfo.$Shape} TokenNftInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenNftInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenNftInfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.nftID = $root.proto.NftID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nftID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.creationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.creationTime);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.metadata = value;
                        else
                            delete message.metadata;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.ledgerId = value;
                        else
                            delete message.ledgerId;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.spenderId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spenderId);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenNftInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenNftInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenNftInfo & proto.TokenNftInfo.$Shape} TokenNftInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenNftInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenNftInfo message.
         * @function verify
         * @memberof proto.TokenNftInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenNftInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nftID != null && $Object.hasOwnProperty.call(message, "nftID")) {
                let error = $root.proto.NftID.verify(message.nftID, _depth + 1);
                if (error)
                    return "nftID." + error;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            if (message.creationTime != null && $Object.hasOwnProperty.call(message, "creationTime")) {
                let error = $root.proto.Timestamp.verify(message.creationTime, _depth + 1);
                if (error)
                    return "creationTime." + error;
            }
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                if (!(message.metadata && typeof message.metadata.length === "number" || $util.isString(message.metadata)))
                    return "metadata: buffer expected";
            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                if (!(message.ledgerId && typeof message.ledgerId.length === "number" || $util.isString(message.ledgerId)))
                    return "ledgerId: buffer expected";
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId")) {
                let error = $root.proto.AccountID.verify(message.spenderId, _depth + 1);
                if (error)
                    return "spenderId." + error;
            }
            return null;
        };

        /**
         * Creates a TokenNftInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenNftInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenNftInfo} TokenNftInfo
         */
        TokenNftInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenNftInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenNftInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenNftInfo();
            if (object.nftID != null) {
                if (!$util.isObject(object.nftID))
                    throw $TypeError(".proto.TokenNftInfo.nftID: object expected");
                message.nftID = $root.proto.NftID.fromObject(object.nftID, _depth + 1);
            }
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.TokenNftInfo.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.creationTime != null) {
                if (!$util.isObject(object.creationTime))
                    throw $TypeError(".proto.TokenNftInfo.creationTime: object expected");
                message.creationTime = $root.proto.Timestamp.fromObject(object.creationTime, _depth + 1);
            }
            if (object.metadata != null)
                if (object.metadata.length)
                    if (typeof object.metadata === "string")
                        $util.base64.decode(object.metadata, message.metadata = $util.newBuffer($util.base64.length(object.metadata)), 0);
                    else if (object.metadata.length >= 0)
                        message.metadata = object.metadata;
            if (object.ledgerId != null)
                if (object.ledgerId.length)
                    if (typeof object.ledgerId === "string")
                        $util.base64.decode(object.ledgerId, message.ledgerId = $util.newBuffer($util.base64.length(object.ledgerId)), 0);
                    else if (object.ledgerId.length >= 0)
                        message.ledgerId = object.ledgerId;
            if (object.spenderId != null) {
                if (!$util.isObject(object.spenderId))
                    throw $TypeError(".proto.TokenNftInfo.spenderId: object expected");
                message.spenderId = $root.proto.AccountID.fromObject(object.spenderId, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenNftInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenNftInfo
         * @static
         * @param {proto.TokenNftInfo} message TokenNftInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenNftInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.nftID = null;
                object.accountID = null;
                object.creationTime = null;
                if (options.bytes === $String)
                    object.metadata = "";
                else {
                    object.metadata = [];
                    if (options.bytes !== $Array)
                        object.metadata = $util.newBuffer(object.metadata);
                }
                if (options.bytes === $String)
                    object.ledgerId = "";
                else {
                    object.ledgerId = [];
                    if (options.bytes !== $Array)
                        object.ledgerId = $util.newBuffer(object.ledgerId);
                }
                object.spenderId = null;
            }
            if (message.nftID != null && $Object.hasOwnProperty.call(message, "nftID"))
                object.nftID = $root.proto.NftID.toObject(message.nftID, options, _depth + 1);
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            if (message.creationTime != null && $Object.hasOwnProperty.call(message, "creationTime"))
                object.creationTime = $root.proto.Timestamp.toObject(message.creationTime, options, _depth + 1);
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                object.metadata = options.bytes === $String ? $util.base64.encode(message.metadata, 0, message.metadata.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.metadata) : message.metadata;
            if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                object.ledgerId = options.bytes === $String ? $util.base64.encode(message.ledgerId, 0, message.ledgerId.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ledgerId) : message.ledgerId;
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId"))
                object.spenderId = $root.proto.AccountID.toObject(message.spenderId, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenNftInfo to JSON.
         * @function toJSON
         * @memberof proto.TokenNftInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenNftInfo.prototype.toJSON = function() {
            return TokenNftInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenNftInfo
         * @function getTypeUrl
         * @memberof proto.TokenNftInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenNftInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenNftInfo";
        };

        return TokenNftInfo;
    })();

    proto.TokenGetNftInfoResponse = (function() {

        /**
         * Properties of a TokenGetNftInfoResponse.
         * @typedef {Object} proto.TokenGetNftInfoResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.TokenNftInfo.$Properties|null} [nft] The information about this NFT
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenGetNftInfoResponse.
         * @memberof proto
         * @interface ITokenGetNftInfoResponse
         * @augments proto.TokenGetNftInfoResponse.$Properties
         * @deprecated Use proto.TokenGetNftInfoResponse.$Properties instead.
         */

        /**
         * Shape of a TokenGetNftInfoResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   nft?: proto.TokenNftInfo.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenGetNftInfoResponse.$Shape
         */

        /**
         * Constructs a new TokenGetNftInfoResponse.
         * @memberof proto
         * @classdesc UNDOCUMENTED
         * @constructor
         * @param {proto.TokenGetNftInfoResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenGetNftInfoResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.TokenGetNftInfoResponse
         * @instance
         */
        TokenGetNftInfoResponse.prototype.header = null;

        /**
         * The information about this NFT
         * @member {proto.TokenNftInfo.$Properties|null|undefined} nft
         * @memberof proto.TokenGetNftInfoResponse
         * @instance
         */
        TokenGetNftInfoResponse.prototype.nft = null;

        /**
         * Creates a new TokenGetNftInfoResponse instance using the specified properties.
         * @function create
         * @memberof proto.TokenGetNftInfoResponse
         * @static
         * @param {proto.TokenGetNftInfoResponse.$Properties=} [properties] Properties to set
         * @returns {proto.TokenGetNftInfoResponse} TokenGetNftInfoResponse instance
         * @type {{
         *   (properties: proto.TokenGetNftInfoResponse.$Shape): proto.TokenGetNftInfoResponse & proto.TokenGetNftInfoResponse.$Shape;
         *   (properties?: proto.TokenGetNftInfoResponse.$Properties): proto.TokenGetNftInfoResponse;
         * }}
         */
        TokenGetNftInfoResponse.create = function(properties) {
            return new TokenGetNftInfoResponse(properties);
        };

        /**
         * Encodes the specified TokenGetNftInfoResponse message. Does not implicitly {@link proto.TokenGetNftInfoResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenGetNftInfoResponse
         * @static
         * @param {proto.TokenGetNftInfoResponse.$Properties} message TokenGetNftInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetNftInfoResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.nft != null && $Object.hasOwnProperty.call(message, "nft"))
                $root.proto.TokenNftInfo.encode(message.nft, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenGetNftInfoResponse message, length delimited. Does not implicitly {@link proto.TokenGetNftInfoResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenGetNftInfoResponse
         * @static
         * @param {proto.TokenGetNftInfoResponse.$Properties} message TokenGetNftInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetNftInfoResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenGetNftInfoResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenGetNftInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenGetNftInfoResponse & proto.TokenGetNftInfoResponse.$Shape} TokenGetNftInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetNftInfoResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenGetNftInfoResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.nft = $root.proto.TokenNftInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nft);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenGetNftInfoResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenGetNftInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenGetNftInfoResponse & proto.TokenGetNftInfoResponse.$Shape} TokenGetNftInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetNftInfoResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenGetNftInfoResponse message.
         * @function verify
         * @memberof proto.TokenGetNftInfoResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenGetNftInfoResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.nft != null && $Object.hasOwnProperty.call(message, "nft")) {
                let error = $root.proto.TokenNftInfo.verify(message.nft, _depth + 1);
                if (error)
                    return "nft." + error;
            }
            return null;
        };

        /**
         * Creates a TokenGetNftInfoResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenGetNftInfoResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenGetNftInfoResponse} TokenGetNftInfoResponse
         */
        TokenGetNftInfoResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenGetNftInfoResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenGetNftInfoResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenGetNftInfoResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TokenGetNftInfoResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.nft != null) {
                if (!$util.isObject(object.nft))
                    throw $TypeError(".proto.TokenGetNftInfoResponse.nft: object expected");
                message.nft = $root.proto.TokenNftInfo.fromObject(object.nft, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenGetNftInfoResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenGetNftInfoResponse
         * @static
         * @param {proto.TokenGetNftInfoResponse} message TokenGetNftInfoResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenGetNftInfoResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.nft = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.nft != null && $Object.hasOwnProperty.call(message, "nft"))
                object.nft = $root.proto.TokenNftInfo.toObject(message.nft, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenGetNftInfoResponse to JSON.
         * @function toJSON
         * @memberof proto.TokenGetNftInfoResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenGetNftInfoResponse.prototype.toJSON = function() {
            return TokenGetNftInfoResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenGetNftInfoResponse
         * @function getTypeUrl
         * @memberof proto.TokenGetNftInfoResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenGetNftInfoResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenGetNftInfoResponse";
        };

        return TokenGetNftInfoResponse;
    })();

    proto.TokenGetNftInfosQuery = (function() {

        /**
         * Properties of a TokenGetNftInfosQuery.
         * @typedef {Object} proto.TokenGetNftInfosQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.TokenID.$Properties|null} [tokenID] A token identifier.
         * <p>
         * This SHALL identify the token to query.<br/>
         * The identified token MUST exist, MUST NOT be deleted, and MUST be
         * a non-fungible/unique type.
         * @property {number|Long|null} [start] Specifies the start index (inclusive) of the range of NFTs to query for.
         * Value must be in the range [0; mintedNFTs-1]
         * @property {number|Long|null} [end] Specifies the end index (exclusive) of the range of NFTs to query for.
         * Value must be in the range (start; mintedNFTs]
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenGetNftInfosQuery.
         * @memberof proto
         * @interface ITokenGetNftInfosQuery
         * @augments proto.TokenGetNftInfosQuery.$Properties
         * @deprecated Use proto.TokenGetNftInfosQuery.$Properties instead.
         */

        /**
         * Shape of a TokenGetNftInfosQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   tokenID?: proto.TokenID.$Shape|null;
         *   start?: number|Long|null;
         *   end?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenGetNftInfosQuery.$Shape
         */

        /**
         * Constructs a new TokenGetNftInfosQuery.
         * @memberof proto
         * @classdesc Deleted and unsupported.
         * 
         * This query is not implemented and any query of this type submitted
         * SHALL return a `NOT_SUPPORTED` response code.
         * @constructor
         * @param {proto.TokenGetNftInfosQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenGetNftInfosQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.TokenGetNftInfosQuery
         * @instance
         */
        TokenGetNftInfosQuery.prototype.header = null;

        /**
         * A token identifier.
         * <p>
         * This SHALL identify the token to query.<br/>
         * The identified token MUST exist, MUST NOT be deleted, and MUST be
         * a non-fungible/unique type.
         * @member {proto.TokenID.$Properties|null|undefined} tokenID
         * @memberof proto.TokenGetNftInfosQuery
         * @instance
         */
        TokenGetNftInfosQuery.prototype.tokenID = null;

        /**
         * Specifies the start index (inclusive) of the range of NFTs to query for.
         * Value must be in the range [0; mintedNFTs-1]
         * @member {number|Long} start
         * @memberof proto.TokenGetNftInfosQuery
         * @instance
         */
        TokenGetNftInfosQuery.prototype.start = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Specifies the end index (exclusive) of the range of NFTs to query for.
         * Value must be in the range (start; mintedNFTs]
         * @member {number|Long} end
         * @memberof proto.TokenGetNftInfosQuery
         * @instance
         */
        TokenGetNftInfosQuery.prototype.end = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new TokenGetNftInfosQuery instance using the specified properties.
         * @function create
         * @memberof proto.TokenGetNftInfosQuery
         * @static
         * @param {proto.TokenGetNftInfosQuery.$Properties=} [properties] Properties to set
         * @returns {proto.TokenGetNftInfosQuery} TokenGetNftInfosQuery instance
         * @type {{
         *   (properties: proto.TokenGetNftInfosQuery.$Shape): proto.TokenGetNftInfosQuery & proto.TokenGetNftInfosQuery.$Shape;
         *   (properties?: proto.TokenGetNftInfosQuery.$Properties): proto.TokenGetNftInfosQuery;
         * }}
         */
        TokenGetNftInfosQuery.create = function(properties) {
            return new TokenGetNftInfosQuery(properties);
        };

        /**
         * Encodes the specified TokenGetNftInfosQuery message. Does not implicitly {@link proto.TokenGetNftInfosQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenGetNftInfosQuery
         * @static
         * @param {proto.TokenGetNftInfosQuery.$Properties} message TokenGetNftInfosQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetNftInfosQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.tokenID != null && $Object.hasOwnProperty.call(message, "tokenID"))
                $root.proto.TokenID.encode(message.tokenID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.start != null && $Object.hasOwnProperty.call(message, "start") && (typeof message.start === "object" ? message.start.low || message.start.high : message.start !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.start);
            if (message.end != null && $Object.hasOwnProperty.call(message, "end") && (typeof message.end === "object" ? message.end.low || message.end.high : message.end !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.end);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenGetNftInfosQuery message, length delimited. Does not implicitly {@link proto.TokenGetNftInfosQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenGetNftInfosQuery
         * @static
         * @param {proto.TokenGetNftInfosQuery.$Properties} message TokenGetNftInfosQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetNftInfosQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenGetNftInfosQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenGetNftInfosQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenGetNftInfosQuery & proto.TokenGetNftInfosQuery.$Shape} TokenGetNftInfosQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetNftInfosQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenGetNftInfosQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.tokenID = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.start = value;
                        else
                            delete message.start;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.end = value;
                        else
                            delete message.end;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenGetNftInfosQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenGetNftInfosQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenGetNftInfosQuery & proto.TokenGetNftInfosQuery.$Shape} TokenGetNftInfosQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetNftInfosQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenGetNftInfosQuery message.
         * @function verify
         * @memberof proto.TokenGetNftInfosQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenGetNftInfosQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.tokenID != null && $Object.hasOwnProperty.call(message, "tokenID")) {
                let error = $root.proto.TokenID.verify(message.tokenID, _depth + 1);
                if (error)
                    return "tokenID." + error;
            }
            if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                if (!$util.isInteger(message.start) && !(message.start && $util.isInteger(message.start.low) && $util.isInteger(message.start.high)))
                    return "start: integer|Long expected";
            if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                if (!$util.isInteger(message.end) && !(message.end && $util.isInteger(message.end.low) && $util.isInteger(message.end.high)))
                    return "end: integer|Long expected";
            return null;
        };

        /**
         * Creates a TokenGetNftInfosQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenGetNftInfosQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenGetNftInfosQuery} TokenGetNftInfosQuery
         */
        TokenGetNftInfosQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenGetNftInfosQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenGetNftInfosQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenGetNftInfosQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TokenGetNftInfosQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.tokenID != null) {
                if (!$util.isObject(object.tokenID))
                    throw $TypeError(".proto.TokenGetNftInfosQuery.tokenID: object expected");
                message.tokenID = $root.proto.TokenID.fromObject(object.tokenID, _depth + 1);
            }
            if (object.start != null)
                if (typeof object.start === "object" ? object.start.low || object.start.high : $Number(object.start) !== 0)
                    if ($util.Long)
                        message.start = $util.Long.fromValue(object.start, false);
                    else if (typeof object.start === "string")
                        message.start = $parseInt(object.start, 10);
                    else if (typeof object.start === "number")
                        message.start = object.start;
                    else if (typeof object.start === "object")
                        message.start = new $util.LongBits(object.start.low >>> 0, object.start.high >>> 0).toNumber();
            if (object.end != null)
                if (typeof object.end === "object" ? object.end.low || object.end.high : $Number(object.end) !== 0)
                    if ($util.Long)
                        message.end = $util.Long.fromValue(object.end, false);
                    else if (typeof object.end === "string")
                        message.end = $parseInt(object.end, 10);
                    else if (typeof object.end === "number")
                        message.end = object.end;
                    else if (typeof object.end === "object")
                        message.end = new $util.LongBits(object.end.low >>> 0, object.end.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a TokenGetNftInfosQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenGetNftInfosQuery
         * @static
         * @param {proto.TokenGetNftInfosQuery} message TokenGetNftInfosQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenGetNftInfosQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.tokenID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.start = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.start = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.end = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.end = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.tokenID != null && $Object.hasOwnProperty.call(message, "tokenID"))
                object.tokenID = $root.proto.TokenID.toObject(message.tokenID, options, _depth + 1);
            if (message.start != null && $Object.hasOwnProperty.call(message, "start"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.start = typeof message.start === "number" ? $BigInt(message.start) : $util.Long.fromBits(message.start.low >>> 0, message.start.high >>> 0, false).toBigInt();
                else if (typeof message.start === "number")
                    object.start = options.longs === $String ? $String(message.start) : message.start;
                else
                    object.start = options.longs === $String ? $util.Long.prototype.toString.call(message.start) : options.longs === $Number ? new $util.LongBits(message.start.low >>> 0, message.start.high >>> 0).toNumber() : message.start;
            if (message.end != null && $Object.hasOwnProperty.call(message, "end"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.end = typeof message.end === "number" ? $BigInt(message.end) : $util.Long.fromBits(message.end.low >>> 0, message.end.high >>> 0, false).toBigInt();
                else if (typeof message.end === "number")
                    object.end = options.longs === $String ? $String(message.end) : message.end;
                else
                    object.end = options.longs === $String ? $util.Long.prototype.toString.call(message.end) : options.longs === $Number ? new $util.LongBits(message.end.low >>> 0, message.end.high >>> 0).toNumber() : message.end;
            return object;
        };

        /**
         * Converts this TokenGetNftInfosQuery to JSON.
         * @function toJSON
         * @memberof proto.TokenGetNftInfosQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenGetNftInfosQuery.prototype.toJSON = function() {
            return TokenGetNftInfosQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenGetNftInfosQuery
         * @function getTypeUrl
         * @memberof proto.TokenGetNftInfosQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenGetNftInfosQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenGetNftInfosQuery";
        };

        return TokenGetNftInfosQuery;
    })();

    proto.TokenGetNftInfosResponse = (function() {

        /**
         * Properties of a TokenGetNftInfosResponse.
         * @typedef {Object} proto.TokenGetNftInfosResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.TokenID.$Properties|null} [tokenID] A token identifier.
         * <p>
         * This SHALL identify the token type to query.<br/>
         * The identified token MUST exist, and MUST NOT be deleted.
         * The identified token MUST be a non-fungible/unique type.
         * @property {Array.<proto.TokenNftInfo.$Properties>|null} [nfts] A list of messages, each of which describes one NFT.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenGetNftInfosResponse.
         * @memberof proto
         * @interface ITokenGetNftInfosResponse
         * @augments proto.TokenGetNftInfosResponse.$Properties
         * @deprecated Use proto.TokenGetNftInfosResponse.$Properties instead.
         */

        /**
         * Shape of a TokenGetNftInfosResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   tokenID?: proto.TokenID.$Shape|null;
         *   nfts?: Array.<proto.TokenNftInfo.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenGetNftInfosResponse.$Shape
         */

        /**
         * Constructs a new TokenGetNftInfosResponse.
         * @memberof proto
         * @classdesc Deleted and unsupported.
         * @constructor
         * @param {proto.TokenGetNftInfosResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenGetNftInfosResponse = function (properties) {
            this.nfts = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.TokenGetNftInfosResponse
         * @instance
         */
        TokenGetNftInfosResponse.prototype.header = null;

        /**
         * A token identifier.
         * <p>
         * This SHALL identify the token type to query.<br/>
         * The identified token MUST exist, and MUST NOT be deleted.
         * The identified token MUST be a non-fungible/unique type.
         * @member {proto.TokenID.$Properties|null|undefined} tokenID
         * @memberof proto.TokenGetNftInfosResponse
         * @instance
         */
        TokenGetNftInfosResponse.prototype.tokenID = null;

        /**
         * A list of messages, each of which describes one NFT.
         * @member {Array.<proto.TokenNftInfo.$Properties>} nfts
         * @memberof proto.TokenGetNftInfosResponse
         * @instance
         */
        TokenGetNftInfosResponse.prototype.nfts = $util.emptyArray;

        /**
         * Creates a new TokenGetNftInfosResponse instance using the specified properties.
         * @function create
         * @memberof proto.TokenGetNftInfosResponse
         * @static
         * @param {proto.TokenGetNftInfosResponse.$Properties=} [properties] Properties to set
         * @returns {proto.TokenGetNftInfosResponse} TokenGetNftInfosResponse instance
         * @type {{
         *   (properties: proto.TokenGetNftInfosResponse.$Shape): proto.TokenGetNftInfosResponse & proto.TokenGetNftInfosResponse.$Shape;
         *   (properties?: proto.TokenGetNftInfosResponse.$Properties): proto.TokenGetNftInfosResponse;
         * }}
         */
        TokenGetNftInfosResponse.create = function(properties) {
            return new TokenGetNftInfosResponse(properties);
        };

        /**
         * Encodes the specified TokenGetNftInfosResponse message. Does not implicitly {@link proto.TokenGetNftInfosResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenGetNftInfosResponse
         * @static
         * @param {proto.TokenGetNftInfosResponse.$Properties} message TokenGetNftInfosResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetNftInfosResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.tokenID != null && $Object.hasOwnProperty.call(message, "tokenID"))
                $root.proto.TokenID.encode(message.tokenID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.nfts != null && message.nfts.length)
                for (let i = 0; i < message.nfts.length; ++i)
                    $root.proto.TokenNftInfo.encode(message.nfts[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenGetNftInfosResponse message, length delimited. Does not implicitly {@link proto.TokenGetNftInfosResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenGetNftInfosResponse
         * @static
         * @param {proto.TokenGetNftInfosResponse.$Properties} message TokenGetNftInfosResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenGetNftInfosResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenGetNftInfosResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenGetNftInfosResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenGetNftInfosResponse & proto.TokenGetNftInfosResponse.$Shape} TokenGetNftInfosResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetNftInfosResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenGetNftInfosResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.tokenID = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenID);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.nfts && message.nfts.length))
                            message.nfts = [];
                        message.nfts.push($root.proto.TokenNftInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenGetNftInfosResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenGetNftInfosResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenGetNftInfosResponse & proto.TokenGetNftInfosResponse.$Shape} TokenGetNftInfosResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenGetNftInfosResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenGetNftInfosResponse message.
         * @function verify
         * @memberof proto.TokenGetNftInfosResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenGetNftInfosResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.tokenID != null && $Object.hasOwnProperty.call(message, "tokenID")) {
                let error = $root.proto.TokenID.verify(message.tokenID, _depth + 1);
                if (error)
                    return "tokenID." + error;
            }
            if (message.nfts != null && $Object.hasOwnProperty.call(message, "nfts")) {
                if (!$Array.isArray(message.nfts))
                    return "nfts: array expected";
                for (let i = 0; i < message.nfts.length; ++i) {
                    let error = $root.proto.TokenNftInfo.verify(message.nfts[i], _depth + 1);
                    if (error)
                        return "nfts." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TokenGetNftInfosResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenGetNftInfosResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenGetNftInfosResponse} TokenGetNftInfosResponse
         */
        TokenGetNftInfosResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenGetNftInfosResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenGetNftInfosResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenGetNftInfosResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.TokenGetNftInfosResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.tokenID != null) {
                if (!$util.isObject(object.tokenID))
                    throw $TypeError(".proto.TokenGetNftInfosResponse.tokenID: object expected");
                message.tokenID = $root.proto.TokenID.fromObject(object.tokenID, _depth + 1);
            }
            if (object.nfts) {
                if (!$Array.isArray(object.nfts))
                    throw $TypeError(".proto.TokenGetNftInfosResponse.nfts: array expected");
                message.nfts = $Array(object.nfts.length);
                for (let i = 0; i < object.nfts.length; ++i) {
                    if (!$util.isObject(object.nfts[i]))
                        throw $TypeError(".proto.TokenGetNftInfosResponse.nfts: object expected");
                    message.nfts[i] = $root.proto.TokenNftInfo.fromObject(object.nfts[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenGetNftInfosResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenGetNftInfosResponse
         * @static
         * @param {proto.TokenGetNftInfosResponse} message TokenGetNftInfosResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenGetNftInfosResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.nfts = [];
            if (options.defaults) {
                object.header = null;
                object.tokenID = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.tokenID != null && $Object.hasOwnProperty.call(message, "tokenID"))
                object.tokenID = $root.proto.TokenID.toObject(message.tokenID, options, _depth + 1);
            if (message.nfts && message.nfts.length) {
                object.nfts = $Array(message.nfts.length);
                for (let j = 0; j < message.nfts.length; ++j)
                    object.nfts[j] = $root.proto.TokenNftInfo.toObject(message.nfts[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TokenGetNftInfosResponse to JSON.
         * @function toJSON
         * @memberof proto.TokenGetNftInfosResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenGetNftInfosResponse.prototype.toJSON = function() {
            return TokenGetNftInfosResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenGetNftInfosResponse
         * @function getTypeUrl
         * @memberof proto.TokenGetNftInfosResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenGetNftInfosResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenGetNftInfosResponse";
        };

        return TokenGetNftInfosResponse;
    })();

    proto.GetAccountDetailsQuery = (function() {

        /**
         * Properties of a GetAccountDetailsQuery.
         * @typedef {Object} proto.GetAccountDetailsQuery.$Properties
         * @property {proto.QueryHeader.$Properties|null} [header] Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @property {proto.AccountID.$Properties|null} [accountId] An account ID for which information is requested
         * <p>
         * This value SHALL identify the account to be queried.<br/>
         * This value MUST identify a valid account.<br/>
         * This field is REQUIRED.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GetAccountDetailsQuery.
         * @memberof proto
         * @interface IGetAccountDetailsQuery
         * @augments proto.GetAccountDetailsQuery.$Properties
         * @deprecated Use proto.GetAccountDetailsQuery.$Properties instead.
         */

        /**
         * Shape of a GetAccountDetailsQuery.
         * @typedef {{
         *   header?: proto.QueryHeader.$Shape|null;
         *   accountId?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.GetAccountDetailsQuery.$Shape
         */

        /**
         * Constructs a new GetAccountDetailsQuery.
         * @memberof proto
         * @classdesc Request detail information about an account.
         * 
         * The returned information SHALL include balance and allowances.<br/>
         * The returned information SHALL NOT include a list of account records.
         * 
         * #### Important
         * This query is a _privileged_ query. Only "system" accounts SHALL be
         * permitted to submit this query.
         * @constructor
         * @param {proto.GetAccountDetailsQuery.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GetAccountDetailsQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Standard information sent with every query operation.<br/>
         * This includes the signed payment and what kind of response is requested
         * (cost, state proof, both, or neither).
         * @member {proto.QueryHeader.$Properties|null|undefined} header
         * @memberof proto.GetAccountDetailsQuery
         * @instance
         */
        GetAccountDetailsQuery.prototype.header = null;

        /**
         * An account ID for which information is requested
         * <p>
         * This value SHALL identify the account to be queried.<br/>
         * This value MUST identify a valid account.<br/>
         * This field is REQUIRED.
         * @member {proto.AccountID.$Properties|null|undefined} accountId
         * @memberof proto.GetAccountDetailsQuery
         * @instance
         */
        GetAccountDetailsQuery.prototype.accountId = null;

        /**
         * Creates a new GetAccountDetailsQuery instance using the specified properties.
         * @function create
         * @memberof proto.GetAccountDetailsQuery
         * @static
         * @param {proto.GetAccountDetailsQuery.$Properties=} [properties] Properties to set
         * @returns {proto.GetAccountDetailsQuery} GetAccountDetailsQuery instance
         * @type {{
         *   (properties: proto.GetAccountDetailsQuery.$Shape): proto.GetAccountDetailsQuery & proto.GetAccountDetailsQuery.$Shape;
         *   (properties?: proto.GetAccountDetailsQuery.$Properties): proto.GetAccountDetailsQuery;
         * }}
         */
        GetAccountDetailsQuery.create = function(properties) {
            return new GetAccountDetailsQuery(properties);
        };

        /**
         * Encodes the specified GetAccountDetailsQuery message. Does not implicitly {@link proto.GetAccountDetailsQuery.verify|verify} messages.
         * @function encode
         * @memberof proto.GetAccountDetailsQuery
         * @static
         * @param {proto.GetAccountDetailsQuery.$Properties} message GetAccountDetailsQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAccountDetailsQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.QueryHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetAccountDetailsQuery message, length delimited. Does not implicitly {@link proto.GetAccountDetailsQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GetAccountDetailsQuery
         * @static
         * @param {proto.GetAccountDetailsQuery.$Properties} message GetAccountDetailsQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAccountDetailsQuery.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GetAccountDetailsQuery message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GetAccountDetailsQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GetAccountDetailsQuery & proto.GetAccountDetailsQuery.$Shape} GetAccountDetailsQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAccountDetailsQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.GetAccountDetailsQuery();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.QueryHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetAccountDetailsQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GetAccountDetailsQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GetAccountDetailsQuery & proto.GetAccountDetailsQuery.$Shape} GetAccountDetailsQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAccountDetailsQuery.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetAccountDetailsQuery message.
         * @function verify
         * @memberof proto.GetAccountDetailsQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetAccountDetailsQuery.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.QueryHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                if (error)
                    return "accountId." + error;
            }
            return null;
        };

        /**
         * Creates a GetAccountDetailsQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GetAccountDetailsQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GetAccountDetailsQuery} GetAccountDetailsQuery
         */
        GetAccountDetailsQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.GetAccountDetailsQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.GetAccountDetailsQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.GetAccountDetailsQuery();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.GetAccountDetailsQuery.header: object expected");
                message.header = $root.proto.QueryHeader.fromObject(object.header, _depth + 1);
            }
            if (object.accountId != null) {
                if (!$util.isObject(object.accountId))
                    throw $TypeError(".proto.GetAccountDetailsQuery.accountId: object expected");
                message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a GetAccountDetailsQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GetAccountDetailsQuery
         * @static
         * @param {proto.GetAccountDetailsQuery} message GetAccountDetailsQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetAccountDetailsQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.accountId = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.QueryHeader.toObject(message.header, options, _depth + 1);
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
            return object;
        };

        /**
         * Converts this GetAccountDetailsQuery to JSON.
         * @function toJSON
         * @memberof proto.GetAccountDetailsQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetAccountDetailsQuery.prototype.toJSON = function() {
            return GetAccountDetailsQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetAccountDetailsQuery
         * @function getTypeUrl
         * @memberof proto.GetAccountDetailsQuery
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetAccountDetailsQuery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GetAccountDetailsQuery";
        };

        return GetAccountDetailsQuery;
    })();

    proto.GetAccountDetailsResponse = (function() {

        /**
         * Properties of a GetAccountDetailsResponse.
         * @typedef {Object} proto.GetAccountDetailsResponse.$Properties
         * @property {proto.ResponseHeader.$Properties|null} [header] The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @property {proto.GetAccountDetailsResponse.AccountDetails.$Properties|null} [accountDetails] Details of the account.
         * <p>
         * A state proof MAY be generated for this field.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GetAccountDetailsResponse.
         * @memberof proto
         * @interface IGetAccountDetailsResponse
         * @augments proto.GetAccountDetailsResponse.$Properties
         * @deprecated Use proto.GetAccountDetailsResponse.$Properties instead.
         */

        /**
         * Shape of a GetAccountDetailsResponse.
         * @typedef {{
         *   header?: proto.ResponseHeader.$Shape|null;
         *   accountDetails?: proto.GetAccountDetailsResponse.AccountDetails.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.GetAccountDetailsResponse.$Shape
         */

        /**
         * Constructs a new GetAccountDetailsResponse.
         * @memberof proto
         * @classdesc A response to a `GetAccountDetailsQuery`.
         * 
         * This SHALL contain the account details if requested and successful.
         * @constructor
         * @param {proto.GetAccountDetailsResponse.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GetAccountDetailsResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The standard response information for queries.<br/>
         * This includes the values requested in the `QueryHeader`
         * (cost, state proof, both, or neither).
         * @member {proto.ResponseHeader.$Properties|null|undefined} header
         * @memberof proto.GetAccountDetailsResponse
         * @instance
         */
        GetAccountDetailsResponse.prototype.header = null;

        /**
         * Details of the account.
         * <p>
         * A state proof MAY be generated for this field.
         * @member {proto.GetAccountDetailsResponse.AccountDetails.$Properties|null|undefined} accountDetails
         * @memberof proto.GetAccountDetailsResponse
         * @instance
         */
        GetAccountDetailsResponse.prototype.accountDetails = null;

        /**
         * Creates a new GetAccountDetailsResponse instance using the specified properties.
         * @function create
         * @memberof proto.GetAccountDetailsResponse
         * @static
         * @param {proto.GetAccountDetailsResponse.$Properties=} [properties] Properties to set
         * @returns {proto.GetAccountDetailsResponse} GetAccountDetailsResponse instance
         * @type {{
         *   (properties: proto.GetAccountDetailsResponse.$Shape): proto.GetAccountDetailsResponse & proto.GetAccountDetailsResponse.$Shape;
         *   (properties?: proto.GetAccountDetailsResponse.$Properties): proto.GetAccountDetailsResponse;
         * }}
         */
        GetAccountDetailsResponse.create = function(properties) {
            return new GetAccountDetailsResponse(properties);
        };

        /**
         * Encodes the specified GetAccountDetailsResponse message. Does not implicitly {@link proto.GetAccountDetailsResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.GetAccountDetailsResponse
         * @static
         * @param {proto.GetAccountDetailsResponse.$Properties} message GetAccountDetailsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAccountDetailsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                $root.proto.ResponseHeader.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountDetails != null && $Object.hasOwnProperty.call(message, "accountDetails"))
                $root.proto.GetAccountDetailsResponse.AccountDetails.encode(message.accountDetails, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GetAccountDetailsResponse message, length delimited. Does not implicitly {@link proto.GetAccountDetailsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GetAccountDetailsResponse
         * @static
         * @param {proto.GetAccountDetailsResponse.$Properties} message GetAccountDetailsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAccountDetailsResponse.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GetAccountDetailsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GetAccountDetailsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GetAccountDetailsResponse & proto.GetAccountDetailsResponse.$Shape} GetAccountDetailsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAccountDetailsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.GetAccountDetailsResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.header = $root.proto.ResponseHeader.decode(reader, reader.uint32(), $undefined, _depth + 1, message.header);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountDetails = $root.proto.GetAccountDetailsResponse.AccountDetails.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountDetails);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GetAccountDetailsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GetAccountDetailsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GetAccountDetailsResponse & proto.GetAccountDetailsResponse.$Shape} GetAccountDetailsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAccountDetailsResponse.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetAccountDetailsResponse message.
         * @function verify
         * @memberof proto.GetAccountDetailsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetAccountDetailsResponse.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.header != null && $Object.hasOwnProperty.call(message, "header")) {
                let error = $root.proto.ResponseHeader.verify(message.header, _depth + 1);
                if (error)
                    return "header." + error;
            }
            if (message.accountDetails != null && $Object.hasOwnProperty.call(message, "accountDetails")) {
                let error = $root.proto.GetAccountDetailsResponse.AccountDetails.verify(message.accountDetails, _depth + 1);
                if (error)
                    return "accountDetails." + error;
            }
            return null;
        };

        /**
         * Creates a GetAccountDetailsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GetAccountDetailsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GetAccountDetailsResponse} GetAccountDetailsResponse
         */
        GetAccountDetailsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.GetAccountDetailsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.GetAccountDetailsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.GetAccountDetailsResponse();
            if (object.header != null) {
                if (!$util.isObject(object.header))
                    throw $TypeError(".proto.GetAccountDetailsResponse.header: object expected");
                message.header = $root.proto.ResponseHeader.fromObject(object.header, _depth + 1);
            }
            if (object.accountDetails != null) {
                if (!$util.isObject(object.accountDetails))
                    throw $TypeError(".proto.GetAccountDetailsResponse.accountDetails: object expected");
                message.accountDetails = $root.proto.GetAccountDetailsResponse.AccountDetails.fromObject(object.accountDetails, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a GetAccountDetailsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GetAccountDetailsResponse
         * @static
         * @param {proto.GetAccountDetailsResponse} message GetAccountDetailsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetAccountDetailsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.header = null;
                object.accountDetails = null;
            }
            if (message.header != null && $Object.hasOwnProperty.call(message, "header"))
                object.header = $root.proto.ResponseHeader.toObject(message.header, options, _depth + 1);
            if (message.accountDetails != null && $Object.hasOwnProperty.call(message, "accountDetails"))
                object.accountDetails = $root.proto.GetAccountDetailsResponse.AccountDetails.toObject(message.accountDetails, options, _depth + 1);
            return object;
        };

        /**
         * Converts this GetAccountDetailsResponse to JSON.
         * @function toJSON
         * @memberof proto.GetAccountDetailsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetAccountDetailsResponse.prototype.toJSON = function() {
            return GetAccountDetailsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GetAccountDetailsResponse
         * @function getTypeUrl
         * @memberof proto.GetAccountDetailsResponse
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GetAccountDetailsResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GetAccountDetailsResponse";
        };

        GetAccountDetailsResponse.AccountDetails = (function() {

            /**
             * Properties of an AccountDetails.
             * @typedef {Object} proto.GetAccountDetailsResponse.AccountDetails.$Properties
             * @property {proto.AccountID.$Properties|null} [accountId] The unique ID of this account.
             * <p>
             * An account ID, when assigned to this field, SHALL be of
             * the form `shard.realm.number`.<br/>
             * Transactions MAY reference the account by alias, but the account
             * itself MUST always have a purely numeric identifier. This numeric
             * ID is the value used to reference the account in query responses,
             * transaction receipts, transaction records, and the block stream.
             * @property {string|null} [contractAccountId] A Solidity ID.<br/>
             * This identifies the contract instance, and the `Account` associated
             * with that contract instance.
             * <p>
             * This SHALL be populated if this account is a smart contract, and
             * SHALL NOT be populated otherwise.<br/>
             * This SHALL be formatted as a string according to Solidity ID
             * standards.
             * @property {boolean|null} [deleted] A boolean indicating that this account is deleted.
             * @property {proto.AccountID.$Properties|null} [proxyAccountId] Replaced by StakingInfo.<br/>
             * ID of the account to which this account is staking its balances. If
             * this account is not currently staking its balances, then this field,
             * if set, SHALL be the sentinel value of `0.0.0`.
             * @property {number|Long|null} [proxyReceived] The total amount of tinybar proxy staked to this account.
             * @property {proto.Key.$Properties|null} [key] The key to be used to sign transactions from this account, if any.
             * <p>
             * This key SHALL NOT be set for hollow accounts until the account
             * is finalized.<br/>
             * This key SHALL be set on all other accounts, except for certain
             * immutable accounts (0.0.800 and 0.0.801) necessary for network
             * function and otherwise secured by the governing council.
             * @property {number|Long|null} [balance] The HBAR balance of this account, in tinybar (10<sup>-8</sup> HBAR).
             * <p>
             * This value SHALL always be a whole number.
             * @property {boolean|null} [receiverSigRequired] A boolean indicating that the account requires a receiver signature
             * for inbound token transfer transactions.
             * <p>
             * If this value is `true` then a transaction to transfer tokens to this
             * account SHALL NOT succeed unless this account has signed the
             * transfer transaction.
             * @property {proto.Timestamp.$Properties|null} [expirationTime] The current expiration time for this account.
             * <p>
             * This account SHALL be due standard renewal fees when the network
             * consensus time exceeds this time.<br/>
             * If rent and expiration are enabled for the network, and automatic
             * renewal is enabled for this account, renewal fees SHALL be charged
             * after this time, and, if charged, the expiration time SHALL be
             * extended for another renewal period.<br/>
             * This account MAY be expired and removed from state at any point
             * after this time if not renewed.<br/>
             * An account holder MAY extend this time by submitting an account
             * update transaction to modify expiration time, subject to the current
             * maximum expiration time for the network.
             * @property {proto.Duration.$Properties|null} [autoRenewPeriod] A duration to extend this account's expiration.
             * <p>
             * The network SHALL extend the account's expiration by this
             * duration, if funds are available, upon automatic renewal.<br/>
             * This SHALL NOT apply if the account is already deleted
             * upon expiration.<br/>
             * If this is not provided in an allowed range on account creation, the
             * transaction SHALL fail with INVALID_AUTO_RENEWAL_PERIOD. The default
             * values for the minimum period and maximum period are currently
             * 30 days and 90 days, respectively.
             * @property {Array.<proto.TokenRelationship.$Properties>|null} [tokenRelationships] As of `HIP-367`, which enabled unlimited token associations, the
             * potential scale for this value requires that users consult a mirror
             * node for this information. Only the top `maxRelsPerInfoQuery`
             * (default 1000) relationships will be returned by this query.<br/>
             * A list of tokens to which this account is "associated", enabling the
             * transfer of that token type by this account.
             * @property {string|null} [memo] A short description of this account.
             * <p>
             * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
             * (default 100) bytes when encoded as UTF-8.
             * @property {number|Long|null} [ownedNfts] The total number of non-fungible/unique tokens owned by this account.
             * @property {number|null} [maxAutomaticTokenAssociations] The maximum number of tokens that can be auto-associated with the
             * account.
             * <p>
             * If this is less than or equal to `used_auto_associations` (or 0),
             * then this account MUST manually associate with a token before
             * transacting in that token.<br/>
             * Following HIP-904 This value may also be `-1` to indicate no
             * limit.<br/>
             * This value MUST NOT be less than `-1`.
             * @property {Uint8Array|null} [alias] An account EVM alias.<br/>
             * This is a value used in some contexts to reference an account when
             * the tripartite account identifier is not available.
             * <p>
             * This field, when set to a non-default value, is immutable and
             * SHALL NOT be changed.
             * @property {Uint8Array|null} [ledgerId] The ledger ID of the network that generated this response.
             * <p>
             * This value SHALL identify the distributed ledger that responded to
             * this query.
             * @property {Array.<proto.GrantedCryptoAllowance.$Properties>|null} [grantedCryptoAllowances] A list of crypto (HBAR) allowances approved by this account.
             * <p>
             * If this is not empty, each allowance SHALL permit a specified
             * "spender" account to spend this account's HBAR balance, up
             * to a designated limit.<br/>
             * This field SHALL permit spending only HBAR balance, not other
             * tokens the account may hold.<br/>
             * Allowances for other tokens SHALL be listed in the
             * `token_allowances` field or the
             * `approve_for_all_nft_allowances` field.
             * @property {Array.<proto.GrantedNftAllowance.$Properties>|null} [grantedNftAllowances] A list of non-fungible token (NFT) allowances approved by
             * this account.
             * <p>
             * If this is not empty, each allowance SHALL permit a specified
             * "spender" account to transfer _all_ of this account's
             * non-fungible/unique tokens from a particular collection.<br/>
             * Allowances for a specific serial number MUST be directly
             * associated with that specific non-fungible/unique token, rather
             * than the holding account.
             * @property {Array.<proto.GrantedTokenAllowance.$Properties>|null} [grantedTokenAllowances] A list of fungible token allowances approved by this account.
             * <p>
             * If this is not empty, each allowance SHALL permit a specified
             * "spender" to spend this account's fungible tokens, of the
             * designated type, up to a designated limit.
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an AccountDetails.
             * @memberof proto.GetAccountDetailsResponse
             * @interface IAccountDetails
             * @augments proto.GetAccountDetailsResponse.AccountDetails.$Properties
             * @deprecated Use proto.GetAccountDetailsResponse.AccountDetails.$Properties instead.
             */

            /**
             * Shape of an AccountDetails.
             * @typedef {{
             *   accountId?: proto.AccountID.$Shape|null;
             *   contractAccountId?: string|null;
             *   deleted?: boolean|null;
             *   proxyAccountId?: proto.AccountID.$Shape|null;
             *   proxyReceived?: number|Long|null;
             *   key?: proto.Key.$Shape|null;
             *   balance?: number|Long|null;
             *   receiverSigRequired?: boolean|null;
             *   expirationTime?: proto.Timestamp.$Shape|null;
             *   autoRenewPeriod?: proto.Duration.$Shape|null;
             *   tokenRelationships?: Array.<proto.TokenRelationship.$Shape>|null;
             *   memo?: string|null;
             *   ownedNfts?: number|Long|null;
             *   maxAutomaticTokenAssociations?: number|null;
             *   alias?: Uint8Array|null;
             *   ledgerId?: Uint8Array|null;
             *   grantedCryptoAllowances?: Array.<proto.GrantedCryptoAllowance.$Shape>|null;
             *   grantedNftAllowances?: Array.<proto.GrantedNftAllowance.$Shape>|null;
             *   grantedTokenAllowances?: Array.<proto.GrantedTokenAllowance.$Shape>|null;
             *   $unknowns?: Array.<Uint8Array>;
             * }} proto.GetAccountDetailsResponse.AccountDetails.$Shape
             */

            /**
             * Constructs a new AccountDetails.
             * @memberof proto.GetAccountDetailsResponse
             * @classdesc Information describing a single Account in the Hedera distributed ledger.
             * 
             * #### Attributes
             * Each Account may have a unique three-part identifier, a Key, and one or
             * more token balances. Accounts also have an alias, which has multiple
             * forms, and may be set automatically. Several additional items are
             * associated with the Account to enable full functionality.
             * 
             * #### Expiration
             * Accounts, as most items in the network, have an expiration time, recorded
             * as a `Timestamp`, and must be "renewed" for a small fee at expiration.
             * This helps to reduce the amount of inactive accounts retained in state.
             * Another account may be designated to pay any renewal fees and
             * automatically renew the account for (by default) 30-90 days at a time as
             * a means to optionally ensure important accounts remain active.
             * 
             * ### Staking
             * Accounts may participate in securing the network by "staking" the account
             * balances to a particular network node, and receive a portion of network
             * fees as a reward. An account may optionally decline these rewards but
             * still stake its balances.
             * 
             * #### Transfer Restrictions
             * An account may optionally require that inbound transfer transactions be
             * signed by that account as receiver (in addition to any other signatures
             * required, including sender).
             * @constructor
             * @param {proto.GetAccountDetailsResponse.AccountDetails.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const AccountDetails = function (properties) {
                this.tokenRelationships = [];
                this.grantedCryptoAllowances = [];
                this.grantedNftAllowances = [];
                this.grantedTokenAllowances = [];
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * The unique ID of this account.
             * <p>
             * An account ID, when assigned to this field, SHALL be of
             * the form `shard.realm.number`.<br/>
             * Transactions MAY reference the account by alias, but the account
             * itself MUST always have a purely numeric identifier. This numeric
             * ID is the value used to reference the account in query responses,
             * transaction receipts, transaction records, and the block stream.
             * @member {proto.AccountID.$Properties|null|undefined} accountId
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.accountId = null;

            /**
             * A Solidity ID.<br/>
             * This identifies the contract instance, and the `Account` associated
             * with that contract instance.
             * <p>
             * This SHALL be populated if this account is a smart contract, and
             * SHALL NOT be populated otherwise.<br/>
             * This SHALL be formatted as a string according to Solidity ID
             * standards.
             * @member {string} contractAccountId
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.contractAccountId = "";

            /**
             * A boolean indicating that this account is deleted.
             * @member {boolean} deleted
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.deleted = false;

            /**
             * Replaced by StakingInfo.<br/>
             * ID of the account to which this account is staking its balances. If
             * this account is not currently staking its balances, then this field,
             * if set, SHALL be the sentinel value of `0.0.0`.
             * @member {proto.AccountID.$Properties|null|undefined} proxyAccountId
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.proxyAccountId = null;

            /**
             * The total amount of tinybar proxy staked to this account.
             * @member {number|Long} proxyReceived
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.proxyReceived = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * The key to be used to sign transactions from this account, if any.
             * <p>
             * This key SHALL NOT be set for hollow accounts until the account
             * is finalized.<br/>
             * This key SHALL be set on all other accounts, except for certain
             * immutable accounts (0.0.800 and 0.0.801) necessary for network
             * function and otherwise secured by the governing council.
             * @member {proto.Key.$Properties|null|undefined} key
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.key = null;

            /**
             * The HBAR balance of this account, in tinybar (10<sup>-8</sup> HBAR).
             * <p>
             * This value SHALL always be a whole number.
             * @member {number|Long} balance
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.balance = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * A boolean indicating that the account requires a receiver signature
             * for inbound token transfer transactions.
             * <p>
             * If this value is `true` then a transaction to transfer tokens to this
             * account SHALL NOT succeed unless this account has signed the
             * transfer transaction.
             * @member {boolean} receiverSigRequired
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.receiverSigRequired = false;

            /**
             * The current expiration time for this account.
             * <p>
             * This account SHALL be due standard renewal fees when the network
             * consensus time exceeds this time.<br/>
             * If rent and expiration are enabled for the network, and automatic
             * renewal is enabled for this account, renewal fees SHALL be charged
             * after this time, and, if charged, the expiration time SHALL be
             * extended for another renewal period.<br/>
             * This account MAY be expired and removed from state at any point
             * after this time if not renewed.<br/>
             * An account holder MAY extend this time by submitting an account
             * update transaction to modify expiration time, subject to the current
             * maximum expiration time for the network.
             * @member {proto.Timestamp.$Properties|null|undefined} expirationTime
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.expirationTime = null;

            /**
             * A duration to extend this account's expiration.
             * <p>
             * The network SHALL extend the account's expiration by this
             * duration, if funds are available, upon automatic renewal.<br/>
             * This SHALL NOT apply if the account is already deleted
             * upon expiration.<br/>
             * If this is not provided in an allowed range on account creation, the
             * transaction SHALL fail with INVALID_AUTO_RENEWAL_PERIOD. The default
             * values for the minimum period and maximum period are currently
             * 30 days and 90 days, respectively.
             * @member {proto.Duration.$Properties|null|undefined} autoRenewPeriod
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.autoRenewPeriod = null;

            /**
             * As of `HIP-367`, which enabled unlimited token associations, the
             * potential scale for this value requires that users consult a mirror
             * node for this information. Only the top `maxRelsPerInfoQuery`
             * (default 1000) relationships will be returned by this query.<br/>
             * A list of tokens to which this account is "associated", enabling the
             * transfer of that token type by this account.
             * @member {Array.<proto.TokenRelationship.$Properties>} tokenRelationships
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.tokenRelationships = $util.emptyArray;

            /**
             * A short description of this account.
             * <p>
             * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
             * (default 100) bytes when encoded as UTF-8.
             * @member {string} memo
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.memo = "";

            /**
             * The total number of non-fungible/unique tokens owned by this account.
             * @member {number|Long} ownedNfts
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.ownedNfts = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * The maximum number of tokens that can be auto-associated with the
             * account.
             * <p>
             * If this is less than or equal to `used_auto_associations` (or 0),
             * then this account MUST manually associate with a token before
             * transacting in that token.<br/>
             * Following HIP-904 This value may also be `-1` to indicate no
             * limit.<br/>
             * This value MUST NOT be less than `-1`.
             * @member {number} maxAutomaticTokenAssociations
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.maxAutomaticTokenAssociations = 0;

            /**
             * An account EVM alias.<br/>
             * This is a value used in some contexts to reference an account when
             * the tripartite account identifier is not available.
             * <p>
             * This field, when set to a non-default value, is immutable and
             * SHALL NOT be changed.
             * @member {Uint8Array} alias
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.alias = $util.newBuffer([]);

            /**
             * The ledger ID of the network that generated this response.
             * <p>
             * This value SHALL identify the distributed ledger that responded to
             * this query.
             * @member {Uint8Array} ledgerId
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.ledgerId = $util.newBuffer([]);

            /**
             * A list of crypto (HBAR) allowances approved by this account.
             * <p>
             * If this is not empty, each allowance SHALL permit a specified
             * "spender" account to spend this account's HBAR balance, up
             * to a designated limit.<br/>
             * This field SHALL permit spending only HBAR balance, not other
             * tokens the account may hold.<br/>
             * Allowances for other tokens SHALL be listed in the
             * `token_allowances` field or the
             * `approve_for_all_nft_allowances` field.
             * @member {Array.<proto.GrantedCryptoAllowance.$Properties>} grantedCryptoAllowances
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.grantedCryptoAllowances = $util.emptyArray;

            /**
             * A list of non-fungible token (NFT) allowances approved by
             * this account.
             * <p>
             * If this is not empty, each allowance SHALL permit a specified
             * "spender" account to transfer _all_ of this account's
             * non-fungible/unique tokens from a particular collection.<br/>
             * Allowances for a specific serial number MUST be directly
             * associated with that specific non-fungible/unique token, rather
             * than the holding account.
             * @member {Array.<proto.GrantedNftAllowance.$Properties>} grantedNftAllowances
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.grantedNftAllowances = $util.emptyArray;

            /**
             * A list of fungible token allowances approved by this account.
             * <p>
             * If this is not empty, each allowance SHALL permit a specified
             * "spender" to spend this account's fungible tokens, of the
             * designated type, up to a designated limit.
             * @member {Array.<proto.GrantedTokenAllowance.$Properties>} grantedTokenAllowances
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             */
            AccountDetails.prototype.grantedTokenAllowances = $util.emptyArray;

            /**
             * Creates a new AccountDetails instance using the specified properties.
             * @function create
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @static
             * @param {proto.GetAccountDetailsResponse.AccountDetails.$Properties=} [properties] Properties to set
             * @returns {proto.GetAccountDetailsResponse.AccountDetails} AccountDetails instance
             * @type {{
             *   (properties: proto.GetAccountDetailsResponse.AccountDetails.$Shape): proto.GetAccountDetailsResponse.AccountDetails & proto.GetAccountDetailsResponse.AccountDetails.$Shape;
             *   (properties?: proto.GetAccountDetailsResponse.AccountDetails.$Properties): proto.GetAccountDetailsResponse.AccountDetails;
             * }}
             */
            AccountDetails.create = function(properties) {
                return new AccountDetails(properties);
            };

            /**
             * Encodes the specified AccountDetails message. Does not implicitly {@link proto.GetAccountDetailsResponse.AccountDetails.verify|verify} messages.
             * @function encode
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @static
             * @param {proto.GetAccountDetailsResponse.AccountDetails.$Properties} message AccountDetails message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AccountDetails.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                    $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.contractAccountId != null && $Object.hasOwnProperty.call(message, "contractAccountId") && message.contractAccountId !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.contractAccountId);
                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.deleted);
                if (message.proxyAccountId != null && $Object.hasOwnProperty.call(message, "proxyAccountId"))
                    $root.proto.AccountID.encode(message.proxyAccountId, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.proxyReceived != null && $Object.hasOwnProperty.call(message, "proxyReceived") && (typeof message.proxyReceived === "object" ? message.proxyReceived.low || message.proxyReceived.high : message.proxyReceived !== 0))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.proxyReceived);
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    $root.proto.Key.encode(message.key, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
                if (message.balance != null && $Object.hasOwnProperty.call(message, "balance") && (typeof message.balance === "object" ? message.balance.low || message.balance.high : message.balance !== 0))
                    writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.balance);
                if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired") && message.receiverSigRequired !== false)
                    writer.uint32(/* id 8, wireType 0 =*/64).bool(message.receiverSigRequired);
                if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                    $root.proto.Timestamp.encode(message.expirationTime, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
                if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                    $root.proto.Duration.encode(message.autoRenewPeriod, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
                if (message.tokenRelationships != null && message.tokenRelationships.length)
                    for (let i = 0; i < message.tokenRelationships.length; ++i)
                        $root.proto.TokenRelationship.encode(message.tokenRelationships[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.memo);
                if (message.ownedNfts != null && $Object.hasOwnProperty.call(message, "ownedNfts") && (typeof message.ownedNfts === "object" ? message.ownedNfts.low || message.ownedNfts.high : message.ownedNfts !== 0))
                    writer.uint32(/* id 13, wireType 0 =*/104).int64(message.ownedNfts);
                if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations") && message.maxAutomaticTokenAssociations !== 0)
                    writer.uint32(/* id 14, wireType 0 =*/112).int32(message.maxAutomaticTokenAssociations);
                if (message.alias != null && $Object.hasOwnProperty.call(message, "alias") && message.alias.length)
                    writer.uint32(/* id 15, wireType 2 =*/122).bytes(message.alias);
                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId") && message.ledgerId.length)
                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.ledgerId);
                if (message.grantedCryptoAllowances != null && message.grantedCryptoAllowances.length)
                    for (let i = 0; i < message.grantedCryptoAllowances.length; ++i)
                        $root.proto.GrantedCryptoAllowance.encode(message.grantedCryptoAllowances[i], writer.uint32(/* id 17, wireType 2 =*/138).fork(), _depth + 1).ldelim();
                if (message.grantedNftAllowances != null && message.grantedNftAllowances.length)
                    for (let i = 0; i < message.grantedNftAllowances.length; ++i)
                        $root.proto.GrantedNftAllowance.encode(message.grantedNftAllowances[i], writer.uint32(/* id 18, wireType 2 =*/146).fork(), _depth + 1).ldelim();
                if (message.grantedTokenAllowances != null && message.grantedTokenAllowances.length)
                    for (let i = 0; i < message.grantedTokenAllowances.length; ++i)
                        $root.proto.GrantedTokenAllowance.encode(message.grantedTokenAllowances[i], writer.uint32(/* id 19, wireType 2 =*/154).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AccountDetails message, length delimited. Does not implicitly {@link proto.GetAccountDetailsResponse.AccountDetails.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @static
             * @param {proto.GetAccountDetailsResponse.AccountDetails.$Properties} message AccountDetails message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AccountDetails.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an AccountDetails message from the specified reader or buffer.
             * @function decode
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.GetAccountDetailsResponse.AccountDetails & proto.GetAccountDetailsResponse.AccountDetails.$Shape} AccountDetails
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AccountDetails.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.proto.GetAccountDetailsResponse.AccountDetails();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.contractAccountId = value;
                            else
                                delete message.contractAccountId;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.deleted = value;
                            else
                                delete message.deleted;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.proxyAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.proxyAccountId);
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.proxyReceived = value;
                            else
                                delete message.proxyReceived;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.key = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key);
                            continue;
                        }
                    case 7: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.balance = value;
                            else
                                delete message.balance;
                            continue;
                        }
                    case 8: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.receiverSigRequired = value;
                            else
                                delete message.receiverSigRequired;
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            message.expirationTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.expirationTime);
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.autoRenewPeriod = $root.proto.Duration.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewPeriod);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if (!(message.tokenRelationships && message.tokenRelationships.length))
                                message.tokenRelationships = [];
                            message.tokenRelationships.push($root.proto.TokenRelationship.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.memo = value;
                            else
                                delete message.memo;
                            continue;
                        }
                    case 13: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.ownedNfts = value;
                            else
                                delete message.ownedNfts;
                            continue;
                        }
                    case 14: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.maxAutomaticTokenAssociations = value;
                            else
                                delete message.maxAutomaticTokenAssociations;
                            continue;
                        }
                    case 15: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.bytes()).length)
                                message.alias = value;
                            else
                                delete message.alias;
                            continue;
                        }
                    case 16: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.bytes()).length)
                                message.ledgerId = value;
                            else
                                delete message.ledgerId;
                            continue;
                        }
                    case 17: {
                            if (wireType !== 2)
                                break;
                            if (!(message.grantedCryptoAllowances && message.grantedCryptoAllowances.length))
                                message.grantedCryptoAllowances = [];
                            message.grantedCryptoAllowances.push($root.proto.GrantedCryptoAllowance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 18: {
                            if (wireType !== 2)
                                break;
                            if (!(message.grantedNftAllowances && message.grantedNftAllowances.length))
                                message.grantedNftAllowances = [];
                            message.grantedNftAllowances.push($root.proto.GrantedNftAllowance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 19: {
                            if (wireType !== 2)
                                break;
                            if (!(message.grantedTokenAllowances && message.grantedTokenAllowances.length))
                                message.grantedTokenAllowances = [];
                            message.grantedTokenAllowances.push($root.proto.GrantedTokenAllowance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an AccountDetails message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.GetAccountDetailsResponse.AccountDetails & proto.GetAccountDetailsResponse.AccountDetails.$Shape} AccountDetails
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AccountDetails.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AccountDetails message.
             * @function verify
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AccountDetails.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                    let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                    if (error)
                        return "accountId." + error;
                }
                if (message.contractAccountId != null && $Object.hasOwnProperty.call(message, "contractAccountId"))
                    if (!$util.isString(message.contractAccountId))
                        return "contractAccountId: string expected";
                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                    if (typeof message.deleted !== "boolean")
                        return "deleted: boolean expected";
                if (message.proxyAccountId != null && $Object.hasOwnProperty.call(message, "proxyAccountId")) {
                    let error = $root.proto.AccountID.verify(message.proxyAccountId, _depth + 1);
                    if (error)
                        return "proxyAccountId." + error;
                }
                if (message.proxyReceived != null && $Object.hasOwnProperty.call(message, "proxyReceived"))
                    if (!$util.isInteger(message.proxyReceived) && !(message.proxyReceived && $util.isInteger(message.proxyReceived.low) && $util.isInteger(message.proxyReceived.high)))
                        return "proxyReceived: integer|Long expected";
                if (message.key != null && $Object.hasOwnProperty.call(message, "key")) {
                    let error = $root.proto.Key.verify(message.key, _depth + 1);
                    if (error)
                        return "key." + error;
                }
                if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                    if (!$util.isInteger(message.balance) && !(message.balance && $util.isInteger(message.balance.low) && $util.isInteger(message.balance.high)))
                        return "balance: integer|Long expected";
                if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired"))
                    if (typeof message.receiverSigRequired !== "boolean")
                        return "receiverSigRequired: boolean expected";
                if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime")) {
                    let error = $root.proto.Timestamp.verify(message.expirationTime, _depth + 1);
                    if (error)
                        return "expirationTime." + error;
                }
                if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod")) {
                    let error = $root.proto.Duration.verify(message.autoRenewPeriod, _depth + 1);
                    if (error)
                        return "autoRenewPeriod." + error;
                }
                if (message.tokenRelationships != null && $Object.hasOwnProperty.call(message, "tokenRelationships")) {
                    if (!$Array.isArray(message.tokenRelationships))
                        return "tokenRelationships: array expected";
                    for (let i = 0; i < message.tokenRelationships.length; ++i) {
                        let error = $root.proto.TokenRelationship.verify(message.tokenRelationships[i], _depth + 1);
                        if (error)
                            return "tokenRelationships." + error;
                    }
                }
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                    if (!$util.isString(message.memo))
                        return "memo: string expected";
                if (message.ownedNfts != null && $Object.hasOwnProperty.call(message, "ownedNfts"))
                    if (!$util.isInteger(message.ownedNfts) && !(message.ownedNfts && $util.isInteger(message.ownedNfts.low) && $util.isInteger(message.ownedNfts.high)))
                        return "ownedNfts: integer|Long expected";
                if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                    if (!$util.isInteger(message.maxAutomaticTokenAssociations))
                        return "maxAutomaticTokenAssociations: integer expected";
                if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                    if (!(message.alias && typeof message.alias.length === "number" || $util.isString(message.alias)))
                        return "alias: buffer expected";
                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                    if (!(message.ledgerId && typeof message.ledgerId.length === "number" || $util.isString(message.ledgerId)))
                        return "ledgerId: buffer expected";
                if (message.grantedCryptoAllowances != null && $Object.hasOwnProperty.call(message, "grantedCryptoAllowances")) {
                    if (!$Array.isArray(message.grantedCryptoAllowances))
                        return "grantedCryptoAllowances: array expected";
                    for (let i = 0; i < message.grantedCryptoAllowances.length; ++i) {
                        let error = $root.proto.GrantedCryptoAllowance.verify(message.grantedCryptoAllowances[i], _depth + 1);
                        if (error)
                            return "grantedCryptoAllowances." + error;
                    }
                }
                if (message.grantedNftAllowances != null && $Object.hasOwnProperty.call(message, "grantedNftAllowances")) {
                    if (!$Array.isArray(message.grantedNftAllowances))
                        return "grantedNftAllowances: array expected";
                    for (let i = 0; i < message.grantedNftAllowances.length; ++i) {
                        let error = $root.proto.GrantedNftAllowance.verify(message.grantedNftAllowances[i], _depth + 1);
                        if (error)
                            return "grantedNftAllowances." + error;
                    }
                }
                if (message.grantedTokenAllowances != null && $Object.hasOwnProperty.call(message, "grantedTokenAllowances")) {
                    if (!$Array.isArray(message.grantedTokenAllowances))
                        return "grantedTokenAllowances: array expected";
                    for (let i = 0; i < message.grantedTokenAllowances.length; ++i) {
                        let error = $root.proto.GrantedTokenAllowance.verify(message.grantedTokenAllowances[i], _depth + 1);
                        if (error)
                            return "grantedTokenAllowances." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an AccountDetails message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.GetAccountDetailsResponse.AccountDetails} AccountDetails
             */
            AccountDetails.fromObject = function (object, _depth) {
                if (object instanceof $root.proto.GetAccountDetailsResponse.AccountDetails)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.proto.GetAccountDetailsResponse.AccountDetails();
                if (object.accountId != null) {
                    if (!$util.isObject(object.accountId))
                        throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.accountId: object expected");
                    message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
                }
                if (object.contractAccountId != null)
                    if (typeof object.contractAccountId !== "string" || object.contractAccountId.length)
                        message.contractAccountId = $String(object.contractAccountId);
                if (object.deleted != null)
                    if (object.deleted)
                        message.deleted = $Boolean(object.deleted);
                if (object.proxyAccountId != null) {
                    if (!$util.isObject(object.proxyAccountId))
                        throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.proxyAccountId: object expected");
                    message.proxyAccountId = $root.proto.AccountID.fromObject(object.proxyAccountId, _depth + 1);
                }
                if (object.proxyReceived != null)
                    if (typeof object.proxyReceived === "object" ? object.proxyReceived.low || object.proxyReceived.high : $Number(object.proxyReceived) !== 0)
                        if ($util.Long)
                            message.proxyReceived = $util.Long.fromValue(object.proxyReceived, false);
                        else if (typeof object.proxyReceived === "string")
                            message.proxyReceived = $parseInt(object.proxyReceived, 10);
                        else if (typeof object.proxyReceived === "number")
                            message.proxyReceived = object.proxyReceived;
                        else if (typeof object.proxyReceived === "object")
                            message.proxyReceived = new $util.LongBits(object.proxyReceived.low >>> 0, object.proxyReceived.high >>> 0).toNumber();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.key: object expected");
                    message.key = $root.proto.Key.fromObject(object.key, _depth + 1);
                }
                if (object.balance != null)
                    if (typeof object.balance === "object" ? object.balance.low || object.balance.high : $Number(object.balance) !== 0)
                        if ($util.Long)
                            message.balance = $util.Long.fromValue(object.balance, true);
                        else if (typeof object.balance === "string")
                            message.balance = $parseInt(object.balance, 10);
                        else if (typeof object.balance === "number")
                            message.balance = object.balance;
                        else if (typeof object.balance === "object")
                            message.balance = new $util.LongBits(object.balance.low >>> 0, object.balance.high >>> 0).toNumber(true);
                if (object.receiverSigRequired != null)
                    if (object.receiverSigRequired)
                        message.receiverSigRequired = $Boolean(object.receiverSigRequired);
                if (object.expirationTime != null) {
                    if (!$util.isObject(object.expirationTime))
                        throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.expirationTime: object expected");
                    message.expirationTime = $root.proto.Timestamp.fromObject(object.expirationTime, _depth + 1);
                }
                if (object.autoRenewPeriod != null) {
                    if (!$util.isObject(object.autoRenewPeriod))
                        throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.autoRenewPeriod: object expected");
                    message.autoRenewPeriod = $root.proto.Duration.fromObject(object.autoRenewPeriod, _depth + 1);
                }
                if (object.tokenRelationships) {
                    if (!$Array.isArray(object.tokenRelationships))
                        throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.tokenRelationships: array expected");
                    message.tokenRelationships = $Array(object.tokenRelationships.length);
                    for (let i = 0; i < object.tokenRelationships.length; ++i) {
                        if (!$util.isObject(object.tokenRelationships[i]))
                            throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.tokenRelationships: object expected");
                        message.tokenRelationships[i] = $root.proto.TokenRelationship.fromObject(object.tokenRelationships[i], _depth + 1);
                    }
                }
                if (object.memo != null)
                    if (typeof object.memo !== "string" || object.memo.length)
                        message.memo = $String(object.memo);
                if (object.ownedNfts != null)
                    if (typeof object.ownedNfts === "object" ? object.ownedNfts.low || object.ownedNfts.high : $Number(object.ownedNfts) !== 0)
                        if ($util.Long)
                            message.ownedNfts = $util.Long.fromValue(object.ownedNfts, false);
                        else if (typeof object.ownedNfts === "string")
                            message.ownedNfts = $parseInt(object.ownedNfts, 10);
                        else if (typeof object.ownedNfts === "number")
                            message.ownedNfts = object.ownedNfts;
                        else if (typeof object.ownedNfts === "object")
                            message.ownedNfts = new $util.LongBits(object.ownedNfts.low >>> 0, object.ownedNfts.high >>> 0).toNumber();
                if (object.maxAutomaticTokenAssociations != null)
                    if ($Number(object.maxAutomaticTokenAssociations) !== 0)
                        message.maxAutomaticTokenAssociations = object.maxAutomaticTokenAssociations | 0;
                if (object.alias != null)
                    if (object.alias.length)
                        if (typeof object.alias === "string")
                            $util.base64.decode(object.alias, message.alias = $util.newBuffer($util.base64.length(object.alias)), 0);
                        else if (object.alias.length >= 0)
                            message.alias = object.alias;
                if (object.ledgerId != null)
                    if (object.ledgerId.length)
                        if (typeof object.ledgerId === "string")
                            $util.base64.decode(object.ledgerId, message.ledgerId = $util.newBuffer($util.base64.length(object.ledgerId)), 0);
                        else if (object.ledgerId.length >= 0)
                            message.ledgerId = object.ledgerId;
                if (object.grantedCryptoAllowances) {
                    if (!$Array.isArray(object.grantedCryptoAllowances))
                        throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.grantedCryptoAllowances: array expected");
                    message.grantedCryptoAllowances = $Array(object.grantedCryptoAllowances.length);
                    for (let i = 0; i < object.grantedCryptoAllowances.length; ++i) {
                        if (!$util.isObject(object.grantedCryptoAllowances[i]))
                            throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.grantedCryptoAllowances: object expected");
                        message.grantedCryptoAllowances[i] = $root.proto.GrantedCryptoAllowance.fromObject(object.grantedCryptoAllowances[i], _depth + 1);
                    }
                }
                if (object.grantedNftAllowances) {
                    if (!$Array.isArray(object.grantedNftAllowances))
                        throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.grantedNftAllowances: array expected");
                    message.grantedNftAllowances = $Array(object.grantedNftAllowances.length);
                    for (let i = 0; i < object.grantedNftAllowances.length; ++i) {
                        if (!$util.isObject(object.grantedNftAllowances[i]))
                            throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.grantedNftAllowances: object expected");
                        message.grantedNftAllowances[i] = $root.proto.GrantedNftAllowance.fromObject(object.grantedNftAllowances[i], _depth + 1);
                    }
                }
                if (object.grantedTokenAllowances) {
                    if (!$Array.isArray(object.grantedTokenAllowances))
                        throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.grantedTokenAllowances: array expected");
                    message.grantedTokenAllowances = $Array(object.grantedTokenAllowances.length);
                    for (let i = 0; i < object.grantedTokenAllowances.length; ++i) {
                        if (!$util.isObject(object.grantedTokenAllowances[i]))
                            throw $TypeError(".proto.GetAccountDetailsResponse.AccountDetails.grantedTokenAllowances: object expected");
                        message.grantedTokenAllowances[i] = $root.proto.GrantedTokenAllowance.fromObject(object.grantedTokenAllowances[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an AccountDetails message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @static
             * @param {proto.GetAccountDetailsResponse.AccountDetails} message AccountDetails
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AccountDetails.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.tokenRelationships = [];
                    object.grantedCryptoAllowances = [];
                    object.grantedNftAllowances = [];
                    object.grantedTokenAllowances = [];
                }
                if (options.defaults) {
                    object.accountId = null;
                    object.contractAccountId = "";
                    object.deleted = false;
                    object.proxyAccountId = null;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.proxyReceived = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.proxyReceived = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.key = null;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.receiverSigRequired = false;
                    object.expirationTime = null;
                    object.autoRenewPeriod = null;
                    object.memo = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.ownedNfts = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.ownedNfts = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.maxAutomaticTokenAssociations = 0;
                    if (options.bytes === $String)
                        object.alias = "";
                    else {
                        object.alias = [];
                        if (options.bytes !== $Array)
                            object.alias = $util.newBuffer(object.alias);
                    }
                    if (options.bytes === $String)
                        object.ledgerId = "";
                    else {
                        object.ledgerId = [];
                        if (options.bytes !== $Array)
                            object.ledgerId = $util.newBuffer(object.ledgerId);
                    }
                }
                if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                    object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
                if (message.contractAccountId != null && $Object.hasOwnProperty.call(message, "contractAccountId"))
                    object.contractAccountId = message.contractAccountId;
                if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                    object.deleted = message.deleted;
                if (message.proxyAccountId != null && $Object.hasOwnProperty.call(message, "proxyAccountId"))
                    object.proxyAccountId = $root.proto.AccountID.toObject(message.proxyAccountId, options, _depth + 1);
                if (message.proxyReceived != null && $Object.hasOwnProperty.call(message, "proxyReceived"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.proxyReceived = typeof message.proxyReceived === "number" ? $BigInt(message.proxyReceived) : $util.Long.fromBits(message.proxyReceived.low >>> 0, message.proxyReceived.high >>> 0, false).toBigInt();
                    else if (typeof message.proxyReceived === "number")
                        object.proxyReceived = options.longs === $String ? $String(message.proxyReceived) : message.proxyReceived;
                    else
                        object.proxyReceived = options.longs === $String ? $util.Long.prototype.toString.call(message.proxyReceived) : options.longs === $Number ? new $util.LongBits(message.proxyReceived.low >>> 0, message.proxyReceived.high >>> 0).toNumber() : message.proxyReceived;
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.Key.toObject(message.key, options, _depth + 1);
                if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.balance = typeof message.balance === "number" ? $BigInt(message.balance) : $util.Long.fromBits(message.balance.low >>> 0, message.balance.high >>> 0, true).toBigInt();
                    else if (typeof message.balance === "number")
                        object.balance = options.longs === $String ? $String(message.balance) : message.balance;
                    else
                        object.balance = options.longs === $String ? $util.Long.prototype.toString.call(message.balance) : options.longs === $Number ? new $util.LongBits(message.balance.low >>> 0, message.balance.high >>> 0).toNumber(true) : message.balance;
                if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired"))
                    object.receiverSigRequired = message.receiverSigRequired;
                if (message.expirationTime != null && $Object.hasOwnProperty.call(message, "expirationTime"))
                    object.expirationTime = $root.proto.Timestamp.toObject(message.expirationTime, options, _depth + 1);
                if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                    object.autoRenewPeriod = $root.proto.Duration.toObject(message.autoRenewPeriod, options, _depth + 1);
                if (message.tokenRelationships && message.tokenRelationships.length) {
                    object.tokenRelationships = $Array(message.tokenRelationships.length);
                    for (let j = 0; j < message.tokenRelationships.length; ++j)
                        object.tokenRelationships[j] = $root.proto.TokenRelationship.toObject(message.tokenRelationships[j], options, _depth + 1);
                }
                if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                    object.memo = message.memo;
                if (message.ownedNfts != null && $Object.hasOwnProperty.call(message, "ownedNfts"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.ownedNfts = typeof message.ownedNfts === "number" ? $BigInt(message.ownedNfts) : $util.Long.fromBits(message.ownedNfts.low >>> 0, message.ownedNfts.high >>> 0, false).toBigInt();
                    else if (typeof message.ownedNfts === "number")
                        object.ownedNfts = options.longs === $String ? $String(message.ownedNfts) : message.ownedNfts;
                    else
                        object.ownedNfts = options.longs === $String ? $util.Long.prototype.toString.call(message.ownedNfts) : options.longs === $Number ? new $util.LongBits(message.ownedNfts.low >>> 0, message.ownedNfts.high >>> 0).toNumber() : message.ownedNfts;
                if (message.maxAutomaticTokenAssociations != null && $Object.hasOwnProperty.call(message, "maxAutomaticTokenAssociations"))
                    object.maxAutomaticTokenAssociations = message.maxAutomaticTokenAssociations;
                if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                    object.alias = options.bytes === $String ? $util.base64.encode(message.alias, 0, message.alias.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.alias) : message.alias;
                if (message.ledgerId != null && $Object.hasOwnProperty.call(message, "ledgerId"))
                    object.ledgerId = options.bytes === $String ? $util.base64.encode(message.ledgerId, 0, message.ledgerId.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.ledgerId) : message.ledgerId;
                if (message.grantedCryptoAllowances && message.grantedCryptoAllowances.length) {
                    object.grantedCryptoAllowances = $Array(message.grantedCryptoAllowances.length);
                    for (let j = 0; j < message.grantedCryptoAllowances.length; ++j)
                        object.grantedCryptoAllowances[j] = $root.proto.GrantedCryptoAllowance.toObject(message.grantedCryptoAllowances[j], options, _depth + 1);
                }
                if (message.grantedNftAllowances && message.grantedNftAllowances.length) {
                    object.grantedNftAllowances = $Array(message.grantedNftAllowances.length);
                    for (let j = 0; j < message.grantedNftAllowances.length; ++j)
                        object.grantedNftAllowances[j] = $root.proto.GrantedNftAllowance.toObject(message.grantedNftAllowances[j], options, _depth + 1);
                }
                if (message.grantedTokenAllowances && message.grantedTokenAllowances.length) {
                    object.grantedTokenAllowances = $Array(message.grantedTokenAllowances.length);
                    for (let j = 0; j < message.grantedTokenAllowances.length; ++j)
                        object.grantedTokenAllowances[j] = $root.proto.GrantedTokenAllowance.toObject(message.grantedTokenAllowances[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this AccountDetails to JSON.
             * @function toJSON
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AccountDetails.prototype.toJSON = function() {
                return AccountDetails.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for AccountDetails
             * @function getTypeUrl
             * @memberof proto.GetAccountDetailsResponse.AccountDetails
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            AccountDetails.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.GetAccountDetailsResponse.AccountDetails";
            };

            return AccountDetails;
        })();

        return GetAccountDetailsResponse;
    })();

    proto.GrantedCryptoAllowance = (function() {

        /**
         * Properties of a GrantedCryptoAllowance.
         * @typedef {Object} proto.GrantedCryptoAllowance.$Properties
         * @property {proto.AccountID.$Properties|null} [spender] The identifier for the spending account associated with this allowance.
         * <p>
         * This account SHALL be permitted to sign transactions to spend
         * HBAR from the funding/allowing account.<br/>
         * This permission SHALL be limited to no more than the specified `amount`.
         * @property {number|Long|null} [amount] The maximum amount that the spender account may transfer within
         * the scope of this allowance.
         * <p>
         * This allowance SHALL be consumed if any combination of transfers
         * authorized via this allowance meet this value in total.<br/>
         * This value MUST be specified in tinybar (i.e. 10<sup>-8</sup> HBAR).
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GrantedCryptoAllowance.
         * @memberof proto
         * @interface IGrantedCryptoAllowance
         * @augments proto.GrantedCryptoAllowance.$Properties
         * @deprecated Use proto.GrantedCryptoAllowance.$Properties instead.
         */

        /**
         * Shape of a GrantedCryptoAllowance.
         * @typedef {{
         *   spender?: proto.AccountID.$Shape|null;
         *   amount?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.GrantedCryptoAllowance.$Shape
         */

        /**
         * Constructs a new GrantedCryptoAllowance.
         * @memberof proto
         * @classdesc Permission granted by one account (the "funding" account) to another
         * account (the "spender" account) that allows the spender to spend a
         * specified amount of HBAR owned by the funding account.
         * 
         * An allowance SHALL NOT transfer any HBAR directly, it only permits
         * transactions signed only by the spender account to transfer HBAR, up
         * to the amount specified, from the funding account.
         * 
         * Once the specified amount is spent, the allowance SHALL be consumed
         * and a new allowance SHALL be required before that spending account
         * may spend additional HBAR from the funding account.
         * @constructor
         * @param {proto.GrantedCryptoAllowance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GrantedCryptoAllowance = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The identifier for the spending account associated with this allowance.
         * <p>
         * This account SHALL be permitted to sign transactions to spend
         * HBAR from the funding/allowing account.<br/>
         * This permission SHALL be limited to no more than the specified `amount`.
         * @member {proto.AccountID.$Properties|null|undefined} spender
         * @memberof proto.GrantedCryptoAllowance
         * @instance
         */
        GrantedCryptoAllowance.prototype.spender = null;

        /**
         * The maximum amount that the spender account may transfer within
         * the scope of this allowance.
         * <p>
         * This allowance SHALL be consumed if any combination of transfers
         * authorized via this allowance meet this value in total.<br/>
         * This value MUST be specified in tinybar (i.e. 10<sup>-8</sup> HBAR).
         * @member {number|Long} amount
         * @memberof proto.GrantedCryptoAllowance
         * @instance
         */
        GrantedCryptoAllowance.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new GrantedCryptoAllowance instance using the specified properties.
         * @function create
         * @memberof proto.GrantedCryptoAllowance
         * @static
         * @param {proto.GrantedCryptoAllowance.$Properties=} [properties] Properties to set
         * @returns {proto.GrantedCryptoAllowance} GrantedCryptoAllowance instance
         * @type {{
         *   (properties: proto.GrantedCryptoAllowance.$Shape): proto.GrantedCryptoAllowance & proto.GrantedCryptoAllowance.$Shape;
         *   (properties?: proto.GrantedCryptoAllowance.$Properties): proto.GrantedCryptoAllowance;
         * }}
         */
        GrantedCryptoAllowance.create = function(properties) {
            return new GrantedCryptoAllowance(properties);
        };

        /**
         * Encodes the specified GrantedCryptoAllowance message. Does not implicitly {@link proto.GrantedCryptoAllowance.verify|verify} messages.
         * @function encode
         * @memberof proto.GrantedCryptoAllowance
         * @static
         * @param {proto.GrantedCryptoAllowance.$Properties} message GrantedCryptoAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GrantedCryptoAllowance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender"))
                $root.proto.AccountID.encode(message.spender, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.amount);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GrantedCryptoAllowance message, length delimited. Does not implicitly {@link proto.GrantedCryptoAllowance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GrantedCryptoAllowance
         * @static
         * @param {proto.GrantedCryptoAllowance.$Properties} message GrantedCryptoAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GrantedCryptoAllowance.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GrantedCryptoAllowance message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GrantedCryptoAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GrantedCryptoAllowance & proto.GrantedCryptoAllowance.$Shape} GrantedCryptoAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GrantedCryptoAllowance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.GrantedCryptoAllowance();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.spender = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spender);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GrantedCryptoAllowance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GrantedCryptoAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GrantedCryptoAllowance & proto.GrantedCryptoAllowance.$Shape} GrantedCryptoAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GrantedCryptoAllowance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GrantedCryptoAllowance message.
         * @function verify
         * @memberof proto.GrantedCryptoAllowance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GrantedCryptoAllowance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender")) {
                let error = $root.proto.AccountID.verify(message.spender, _depth + 1);
                if (error)
                    return "spender." + error;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            return null;
        };

        /**
         * Creates a GrantedCryptoAllowance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GrantedCryptoAllowance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GrantedCryptoAllowance} GrantedCryptoAllowance
         */
        GrantedCryptoAllowance.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.GrantedCryptoAllowance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.GrantedCryptoAllowance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.GrantedCryptoAllowance();
            if (object.spender != null) {
                if (!$util.isObject(object.spender))
                    throw $TypeError(".proto.GrantedCryptoAllowance.spender: object expected");
                message.spender = $root.proto.AccountID.fromObject(object.spender, _depth + 1);
            }
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a GrantedCryptoAllowance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GrantedCryptoAllowance
         * @static
         * @param {proto.GrantedCryptoAllowance} message GrantedCryptoAllowance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GrantedCryptoAllowance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.spender = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender"))
                object.spender = $root.proto.AccountID.toObject(message.spender, options, _depth + 1);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            return object;
        };

        /**
         * Converts this GrantedCryptoAllowance to JSON.
         * @function toJSON
         * @memberof proto.GrantedCryptoAllowance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GrantedCryptoAllowance.prototype.toJSON = function() {
            return GrantedCryptoAllowance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GrantedCryptoAllowance
         * @function getTypeUrl
         * @memberof proto.GrantedCryptoAllowance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GrantedCryptoAllowance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GrantedCryptoAllowance";
        };

        return GrantedCryptoAllowance;
    })();

    proto.GrantedNftAllowance = (function() {

        /**
         * Properties of a GrantedNftAllowance.
         * @typedef {Object} proto.GrantedNftAllowance.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] The identifier for the token associated with this allowance.
         * <p>
         * This token MUST be a non-fungible/unique token.
         * @property {proto.AccountID.$Properties|null} [spender] The identifier for the spending account associated with this allowance.
         * <p>
         * This account SHALL be permitted to sign transactions to spend
         * tokens of the associated token type from the funding/allowing account.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GrantedNftAllowance.
         * @memberof proto
         * @interface IGrantedNftAllowance
         * @augments proto.GrantedNftAllowance.$Properties
         * @deprecated Use proto.GrantedNftAllowance.$Properties instead.
         */

        /**
         * Shape of a GrantedNftAllowance.
         * @typedef {{
         *   tokenId?: proto.TokenID.$Shape|null;
         *   spender?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.GrantedNftAllowance.$Shape
         */

        /**
         * Constructs a new GrantedNftAllowance.
         * @memberof proto
         * @classdesc Permission granted by one account (the "funding" account) to another
         * account (the "spender" account) that allows the spender to transfer
         * all serial numbers of a specific non-fungible/unique token (NFT)
         * collection owned by the funding account.<br/>
         * This is a broad permission, as it does not matter how many NFTs of the
         * specified collection the funding account owns, the spender MAY dispose
         * of any or all of them with this allowance.<br/>
         * Each token type (typically a collection of NFTs) SHALL require
         * a separate allowance.<br/>
         * Allowances for a specific serial number MUST be directly associated
         * with that specific non-fungible/unique token, rather than
         * the holding account.
         * 
         * An allowance SHALL NOT transfer any tokens directly, it only permits
         * transactions signed only by the spender account to transfer any
         * non-fungible/unique tokens of the specified type owned by
         * the funding account.
         * @constructor
         * @param {proto.GrantedNftAllowance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GrantedNftAllowance = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The identifier for the token associated with this allowance.
         * <p>
         * This token MUST be a non-fungible/unique token.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.GrantedNftAllowance
         * @instance
         */
        GrantedNftAllowance.prototype.tokenId = null;

        /**
         * The identifier for the spending account associated with this allowance.
         * <p>
         * This account SHALL be permitted to sign transactions to spend
         * tokens of the associated token type from the funding/allowing account.
         * @member {proto.AccountID.$Properties|null|undefined} spender
         * @memberof proto.GrantedNftAllowance
         * @instance
         */
        GrantedNftAllowance.prototype.spender = null;

        /**
         * Creates a new GrantedNftAllowance instance using the specified properties.
         * @function create
         * @memberof proto.GrantedNftAllowance
         * @static
         * @param {proto.GrantedNftAllowance.$Properties=} [properties] Properties to set
         * @returns {proto.GrantedNftAllowance} GrantedNftAllowance instance
         * @type {{
         *   (properties: proto.GrantedNftAllowance.$Shape): proto.GrantedNftAllowance & proto.GrantedNftAllowance.$Shape;
         *   (properties?: proto.GrantedNftAllowance.$Properties): proto.GrantedNftAllowance;
         * }}
         */
        GrantedNftAllowance.create = function(properties) {
            return new GrantedNftAllowance(properties);
        };

        /**
         * Encodes the specified GrantedNftAllowance message. Does not implicitly {@link proto.GrantedNftAllowance.verify|verify} messages.
         * @function encode
         * @memberof proto.GrantedNftAllowance
         * @static
         * @param {proto.GrantedNftAllowance.$Properties} message GrantedNftAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GrantedNftAllowance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender"))
                $root.proto.AccountID.encode(message.spender, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GrantedNftAllowance message, length delimited. Does not implicitly {@link proto.GrantedNftAllowance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GrantedNftAllowance
         * @static
         * @param {proto.GrantedNftAllowance.$Properties} message GrantedNftAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GrantedNftAllowance.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GrantedNftAllowance message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GrantedNftAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GrantedNftAllowance & proto.GrantedNftAllowance.$Shape} GrantedNftAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GrantedNftAllowance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.GrantedNftAllowance();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.spender = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spender);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GrantedNftAllowance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GrantedNftAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GrantedNftAllowance & proto.GrantedNftAllowance.$Shape} GrantedNftAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GrantedNftAllowance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GrantedNftAllowance message.
         * @function verify
         * @memberof proto.GrantedNftAllowance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GrantedNftAllowance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender")) {
                let error = $root.proto.AccountID.verify(message.spender, _depth + 1);
                if (error)
                    return "spender." + error;
            }
            return null;
        };

        /**
         * Creates a GrantedNftAllowance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GrantedNftAllowance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GrantedNftAllowance} GrantedNftAllowance
         */
        GrantedNftAllowance.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.GrantedNftAllowance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.GrantedNftAllowance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.GrantedNftAllowance();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.GrantedNftAllowance.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.spender != null) {
                if (!$util.isObject(object.spender))
                    throw $TypeError(".proto.GrantedNftAllowance.spender: object expected");
                message.spender = $root.proto.AccountID.fromObject(object.spender, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a GrantedNftAllowance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GrantedNftAllowance
         * @static
         * @param {proto.GrantedNftAllowance} message GrantedNftAllowance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GrantedNftAllowance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.tokenId = null;
                object.spender = null;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender"))
                object.spender = $root.proto.AccountID.toObject(message.spender, options, _depth + 1);
            return object;
        };

        /**
         * Converts this GrantedNftAllowance to JSON.
         * @function toJSON
         * @memberof proto.GrantedNftAllowance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GrantedNftAllowance.prototype.toJSON = function() {
            return GrantedNftAllowance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GrantedNftAllowance
         * @function getTypeUrl
         * @memberof proto.GrantedNftAllowance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GrantedNftAllowance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GrantedNftAllowance";
        };

        return GrantedNftAllowance;
    })();

    proto.GrantedTokenAllowance = (function() {

        /**
         * Properties of a GrantedTokenAllowance.
         * @typedef {Object} proto.GrantedTokenAllowance.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] The identifier for the token associated with this allowance.
         * <p>
         * This token MUST be a fungible/common token.
         * @property {proto.AccountID.$Properties|null} [spender] The identifier for the spending account associated with this allowance.
         * <p>
         * This account SHALL be permitted to sign transactions to spend tokens
         * of the associated token type from the funding/allowing account.<br/>
         * This permission SHALL be limited to no more than the specified `amount`.
         * @property {number|Long|null} [amount] GrantedTokenAllowance amount
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a GrantedTokenAllowance.
         * @memberof proto
         * @interface IGrantedTokenAllowance
         * @augments proto.GrantedTokenAllowance.$Properties
         * @deprecated Use proto.GrantedTokenAllowance.$Properties instead.
         */

        /**
         * Shape of a GrantedTokenAllowance.
         * @typedef {{
         *   tokenId?: proto.TokenID.$Shape|null;
         *   spender?: proto.AccountID.$Shape|null;
         *   amount?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.GrantedTokenAllowance.$Shape
         */

        /**
         * Constructs a new GrantedTokenAllowance.
         * @memberof proto
         * @classdesc Permission granted by one account (the "funding" account) to another
         * account (the "spender" account) that allows the spender to spend a
         * specified amount of a specific non-HBAR fungible token from the
         * balance owned by the funding account.
         * 
         * An allowance SHALL NOT transfer any tokens directly, it only permits
         * transactions signed only by the spender account to transfer tokens
         * of the specified type, up to the amount specified, from the funding account.
         * 
         * Once the specified amount is spent, the allowance SHALL be consumed
         * and a new allowance SHALL be required before that spending account
         * may spend additional tokens from the funding account.
         * @constructor
         * @param {proto.GrantedTokenAllowance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const GrantedTokenAllowance = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The identifier for the token associated with this allowance.
         * <p>
         * This token MUST be a fungible/common token.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.GrantedTokenAllowance
         * @instance
         */
        GrantedTokenAllowance.prototype.tokenId = null;

        /**
         * The identifier for the spending account associated with this allowance.
         * <p>
         * This account SHALL be permitted to sign transactions to spend tokens
         * of the associated token type from the funding/allowing account.<br/>
         * This permission SHALL be limited to no more than the specified `amount`.
         * @member {proto.AccountID.$Properties|null|undefined} spender
         * @memberof proto.GrantedTokenAllowance
         * @instance
         */
        GrantedTokenAllowance.prototype.spender = null;

        /**
         * GrantedTokenAllowance amount.
         * @member {number|Long} amount
         * @memberof proto.GrantedTokenAllowance
         * @instance
         */
        GrantedTokenAllowance.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new GrantedTokenAllowance instance using the specified properties.
         * @function create
         * @memberof proto.GrantedTokenAllowance
         * @static
         * @param {proto.GrantedTokenAllowance.$Properties=} [properties] Properties to set
         * @returns {proto.GrantedTokenAllowance} GrantedTokenAllowance instance
         * @type {{
         *   (properties: proto.GrantedTokenAllowance.$Shape): proto.GrantedTokenAllowance & proto.GrantedTokenAllowance.$Shape;
         *   (properties?: proto.GrantedTokenAllowance.$Properties): proto.GrantedTokenAllowance;
         * }}
         */
        GrantedTokenAllowance.create = function(properties) {
            return new GrantedTokenAllowance(properties);
        };

        /**
         * Encodes the specified GrantedTokenAllowance message. Does not implicitly {@link proto.GrantedTokenAllowance.verify|verify} messages.
         * @function encode
         * @memberof proto.GrantedTokenAllowance
         * @static
         * @param {proto.GrantedTokenAllowance.$Properties} message GrantedTokenAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GrantedTokenAllowance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender"))
                $root.proto.AccountID.encode(message.spender, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.amount);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GrantedTokenAllowance message, length delimited. Does not implicitly {@link proto.GrantedTokenAllowance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GrantedTokenAllowance
         * @static
         * @param {proto.GrantedTokenAllowance.$Properties} message GrantedTokenAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GrantedTokenAllowance.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a GrantedTokenAllowance message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GrantedTokenAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GrantedTokenAllowance & proto.GrantedTokenAllowance.$Shape} GrantedTokenAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GrantedTokenAllowance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.GrantedTokenAllowance();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.spender = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spender);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a GrantedTokenAllowance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GrantedTokenAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GrantedTokenAllowance & proto.GrantedTokenAllowance.$Shape} GrantedTokenAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GrantedTokenAllowance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GrantedTokenAllowance message.
         * @function verify
         * @memberof proto.GrantedTokenAllowance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GrantedTokenAllowance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender")) {
                let error = $root.proto.AccountID.verify(message.spender, _depth + 1);
                if (error)
                    return "spender." + error;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            return null;
        };

        /**
         * Creates a GrantedTokenAllowance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GrantedTokenAllowance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GrantedTokenAllowance} GrantedTokenAllowance
         */
        GrantedTokenAllowance.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.GrantedTokenAllowance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.GrantedTokenAllowance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.GrantedTokenAllowance();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.GrantedTokenAllowance.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.spender != null) {
                if (!$util.isObject(object.spender))
                    throw $TypeError(".proto.GrantedTokenAllowance.spender: object expected");
                message.spender = $root.proto.AccountID.fromObject(object.spender, _depth + 1);
            }
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a GrantedTokenAllowance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GrantedTokenAllowance
         * @static
         * @param {proto.GrantedTokenAllowance} message GrantedTokenAllowance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GrantedTokenAllowance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.tokenId = null;
                object.spender = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.spender != null && $Object.hasOwnProperty.call(message, "spender"))
                object.spender = $root.proto.AccountID.toObject(message.spender, options, _depth + 1);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            return object;
        };

        /**
         * Converts this GrantedTokenAllowance to JSON.
         * @function toJSON
         * @memberof proto.GrantedTokenAllowance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GrantedTokenAllowance.prototype.toJSON = function() {
            return GrantedTokenAllowance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for GrantedTokenAllowance
         * @function getTypeUrl
         * @memberof proto.GrantedTokenAllowance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        GrantedTokenAllowance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GrantedTokenAllowance";
        };

        return GrantedTokenAllowance;
    })();

    proto.Response = (function() {

        /**
         * Properties of a Response.
         * @typedef {Object} proto.Response.$Properties
         * @property {proto.GetByKeyResponse.$Properties|null} [getByKey] A response for a query requesting all accounts, claims, files, and
         * smart contract instances whose associated keys include the given Key.
         * <p>
         * > This query is no longer supported.
         * @property {proto.GetBySolidityIDResponse.$Properties|null} [getBySolidityID] A response for a query requesting Contract, Account, and File
         * identifiers for a smart contract, given a Solidity identifier.
         * @property {proto.ContractCallLocalResponse.$Properties|null} [contractCallLocal] A response for a _local_ call to a query function of a given smart
         * contract, providing function parameter inputs as needed.
         * <p>
         * > This call is only performed on the local node. It is _not_ a
         * > network consensus result.
         * @property {proto.ContractGetBytecodeResponse.$Properties|null} [contractGetBytecodeResponse] A response for a query requesting the current bytecode for
         * a smart contract.
         * @property {proto.ContractGetInfoResponse.$Properties|null} [contractGetInfo] A response for a query requesting detailed information about
         * a smart contract.
         * @property {proto.ContractGetRecordsResponse.$Properties|null} [contractGetRecordsResponse] A response for a query requesting records of all transactions
         * against the given contract in the last 25 hours.
         * <p>
         * > This query is no longer supported.
         * @property {proto.CryptoGetAccountBalanceResponse.$Properties|null} [cryptogetAccountBalance] A response for a query requesting the HBAR balance of an account
         * or contract.
         * @property {proto.CryptoGetAccountRecordsResponse.$Properties|null} [cryptoGetAccountRecords] A response for a query requesting records of all "recent"
         * transactions for which the specified account is the effective payer.
         * @property {proto.CryptoGetInfoResponse.$Properties|null} [cryptoGetInfo] A response for a query requesting information for an account.<br/>
         * This query includes balance, but not allowances or token
         * relationships.
         * @property {proto.CryptoGetLiveHashResponse.$Properties|null} [cryptoGetLiveHash] A response for a query requesting detail for a specific live hash
         * associated to a specific account.
         * <p>
         * > This query is no longer supported.
         * @property {proto.CryptoGetStakersResponse.$Properties|null} [cryptoGetProxyStakers] A response for a query requesting all the accounts that are proxy
         * staking to this account.
         * <p>
         * > This query is no longer supported.
         * @property {proto.FileGetContentsResponse.$Properties|null} [fileGetContents] A response for a query requesting the content of a file in the
         * Hedera File Service (HFS).
         * @property {proto.FileGetInfoResponse.$Properties|null} [fileGetInfo] A response for a query requesting file metadata from the
         * Hedera File Service (HFS).
         * @property {proto.TransactionGetReceiptResponse.$Properties|null} [transactionGetReceipt] A response for a query requesting the post-consensus (final)
         * result of a transaction.
         * @property {proto.TransactionGetRecordResponse.$Properties|null} [transactionGetRecord] A response for a query requesting a transaction record; the detail
         * changes completed in response to a transaction.
         * @property {proto.TransactionGetFastRecordResponse.$Properties|null} [transactionGetFastRecord] A response for a query requesting a very recent transaction record.
         * <p>
         * > This query is no longer supported.
         * @property {proto.ConsensusGetTopicInfoResponse.$Properties|null} [consensusGetTopicInfo] A response for a query requesting the current state of a topic for
         * the Hedera Consensus Service (HCS).
         * @property {proto.NetworkGetVersionInfoResponse.$Properties|null} [networkGetVersionInfo] A response for a query requesting the deployed versions of Hedera
         * Services and the API definitions in semantic version format
         * @property {proto.TokenGetInfoResponse.$Properties|null} [tokenGetInfo] A response for a query requesting metadata for a specific Token.
         * @property {proto.ScheduleGetInfoResponse.$Properties|null} [scheduleGetInfo] A response for a query requesting detail for a scheduled transaction.
         * @property {proto.TokenGetAccountNftInfosResponse.$Properties|null} [tokenGetAccountNftInfos] A response for a query requesting detail for a subset of individual
         * non-fungible/unique tokens owned by an account.<br/>
         * The requested tokens are selected by a list index, which is based
         * on the order in which the tokens were added to the account.
         * @property {proto.TokenGetNftInfoResponse.$Properties|null} [tokenGetNftInfo] A response for a query requesting detail for a specific
         * non-fungible/unique token selected by both token identifier and
         * serial number.
         * @property {proto.TokenGetNftInfosResponse.$Properties|null} [tokenGetNftInfos] A response for a query requesting detail for a subset of individual
         * non-fungible/unique tokens.<br/>
         * The requested tokens are selected by a list index, which is based
         * on the order in which the tokens were minted.
         * @property {proto.NetworkGetExecutionTimeResponse.$Properties|null} [networkGetExecutionTime] A response for a query requesting the time, in nanoseconds, spent
         * in direct processing for one or more recent transactions.
         * <p>
         * > This query is no longer supported.
         * @property {proto.GetAccountDetailsResponse.$Properties|null} [accountDetails] A response for a query requesting detail information
         * about an account.
         * <p>
         * This query is a privileged query and "system" account authorization
         * is REQUIRED for this query.
         * @property {"getByKey"|"getBySolidityID"|"contractCallLocal"|"contractGetBytecodeResponse"|"contractGetInfo"|"contractGetRecordsResponse"|"cryptogetAccountBalance"|"cryptoGetAccountRecords"|"cryptoGetInfo"|"cryptoGetLiveHash"|"cryptoGetProxyStakers"|"fileGetContents"|"fileGetInfo"|"transactionGetReceipt"|"transactionGetRecord"|"transactionGetFastRecord"|"consensusGetTopicInfo"|"networkGetVersionInfo"|"tokenGetInfo"|"scheduleGetInfo"|"tokenGetAccountNftInfos"|"tokenGetNftInfo"|"tokenGetNftInfos"|"networkGetExecutionTime"|"accountDetails"} [response] Response response
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Response.
         * @memberof proto
         * @interface IResponse
         * @augments proto.Response.$Properties
         * @deprecated Use proto.Response.$Properties instead.
         */

        /**
         * Narrowed shape of a Response.
         * @typedef {{
         *   getByKey?: proto.GetByKeyResponse.$Shape|null;
         *   getBySolidityID?: proto.GetBySolidityIDResponse.$Shape|null;
         *   contractCallLocal?: proto.ContractCallLocalResponse.$Shape|null;
         *   contractGetBytecodeResponse?: proto.ContractGetBytecodeResponse.$Shape|null;
         *   contractGetInfo?: proto.ContractGetInfoResponse.$Shape|null;
         *   contractGetRecordsResponse?: proto.ContractGetRecordsResponse.$Shape|null;
         *   cryptogetAccountBalance?: proto.CryptoGetAccountBalanceResponse.$Shape|null;
         *   cryptoGetAccountRecords?: proto.CryptoGetAccountRecordsResponse.$Shape|null;
         *   cryptoGetInfo?: proto.CryptoGetInfoResponse.$Shape|null;
         *   cryptoGetLiveHash?: proto.CryptoGetLiveHashResponse.$Shape|null;
         *   cryptoGetProxyStakers?: proto.CryptoGetStakersResponse.$Shape|null;
         *   fileGetContents?: proto.FileGetContentsResponse.$Shape|null;
         *   fileGetInfo?: proto.FileGetInfoResponse.$Shape|null;
         *   transactionGetReceipt?: proto.TransactionGetReceiptResponse.$Shape|null;
         *   transactionGetRecord?: proto.TransactionGetRecordResponse.$Shape|null;
         *   transactionGetFastRecord?: proto.TransactionGetFastRecordResponse.$Shape|null;
         *   consensusGetTopicInfo?: proto.ConsensusGetTopicInfoResponse.$Shape|null;
         *   networkGetVersionInfo?: proto.NetworkGetVersionInfoResponse.$Shape|null;
         *   tokenGetInfo?: proto.TokenGetInfoResponse.$Shape|null;
         *   scheduleGetInfo?: proto.ScheduleGetInfoResponse.$Shape|null;
         *   tokenGetAccountNftInfos?: proto.TokenGetAccountNftInfosResponse.$Shape|null;
         *   tokenGetNftInfo?: proto.TokenGetNftInfoResponse.$Shape|null;
         *   tokenGetNftInfos?: proto.TokenGetNftInfosResponse.$Shape|null;
         *   networkGetExecutionTime?: proto.NetworkGetExecutionTimeResponse.$Shape|null;
         *   accountDetails?: proto.GetAccountDetailsResponse.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ response?: undefined; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "getByKey"; getByKey: proto.GetByKeyResponse.$Shape; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "getBySolidityID"; getByKey?: null; getBySolidityID: proto.GetBySolidityIDResponse.$Shape; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "contractCallLocal"; getByKey?: null; getBySolidityID?: null; contractCallLocal: proto.ContractCallLocalResponse.$Shape; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "contractGetBytecodeResponse"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse: proto.ContractGetBytecodeResponse.$Shape; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "contractGetInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo: proto.ContractGetInfoResponse.$Shape; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "contractGetRecordsResponse"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse: proto.ContractGetRecordsResponse.$Shape; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "cryptogetAccountBalance"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance: proto.CryptoGetAccountBalanceResponse.$Shape; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "cryptoGetAccountRecords"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords: proto.CryptoGetAccountRecordsResponse.$Shape; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "cryptoGetInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo: proto.CryptoGetInfoResponse.$Shape; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "cryptoGetLiveHash"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash: proto.CryptoGetLiveHashResponse.$Shape; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "cryptoGetProxyStakers"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers: proto.CryptoGetStakersResponse.$Shape; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "fileGetContents"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents: proto.FileGetContentsResponse.$Shape; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "fileGetInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo: proto.FileGetInfoResponse.$Shape; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "transactionGetReceipt"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt: proto.TransactionGetReceiptResponse.$Shape; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "transactionGetRecord"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord: proto.TransactionGetRecordResponse.$Shape; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "transactionGetFastRecord"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord: proto.TransactionGetFastRecordResponse.$Shape; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "consensusGetTopicInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo: proto.ConsensusGetTopicInfoResponse.$Shape; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "networkGetVersionInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo: proto.NetworkGetVersionInfoResponse.$Shape; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "tokenGetInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo: proto.TokenGetInfoResponse.$Shape; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "scheduleGetInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo: proto.ScheduleGetInfoResponse.$Shape; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "tokenGetAccountNftInfos"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos: proto.TokenGetAccountNftInfosResponse.$Shape; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "tokenGetNftInfo"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo: proto.TokenGetNftInfoResponse.$Shape; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "tokenGetNftInfos"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos: proto.TokenGetNftInfosResponse.$Shape; networkGetExecutionTime?: null; accountDetails?: null }|{ response?: "networkGetExecutionTime"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime: proto.NetworkGetExecutionTimeResponse.$Shape; accountDetails?: null }|{ response?: "accountDetails"; getByKey?: null; getBySolidityID?: null; contractCallLocal?: null; contractGetBytecodeResponse?: null; contractGetInfo?: null; contractGetRecordsResponse?: null; cryptogetAccountBalance?: null; cryptoGetAccountRecords?: null; cryptoGetInfo?: null; cryptoGetLiveHash?: null; cryptoGetProxyStakers?: null; fileGetContents?: null; fileGetInfo?: null; transactionGetReceipt?: null; transactionGetRecord?: null; transactionGetFastRecord?: null; consensusGetTopicInfo?: null; networkGetVersionInfo?: null; tokenGetInfo?: null; scheduleGetInfo?: null; tokenGetAccountNftInfos?: null; tokenGetNftInfo?: null; tokenGetNftInfos?: null; networkGetExecutionTime?: null; accountDetails: proto.GetAccountDetailsResponse.$Shape })
         * )} proto.Response.$Shape
         */

        /**
         * Constructs a new Response.
         * @memberof proto
         * @classdesc A single query response.
         * 
         * Each query MUST define its specific response type.<br/>
         * Each query response MUST include both the information request and a
         * `ResponseHeader`.<br/>
         * All possible query response types MUST be listed here in a `oneof`.
         * @constructor
         * @param {proto.Response.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Response = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A response for a query requesting all accounts, claims, files, and
         * smart contract instances whose associated keys include the given Key.
         * <p>
         * > This query is no longer supported.
         * @member {proto.GetByKeyResponse.$Properties|null|undefined} getByKey
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.getByKey = null;

        /**
         * A response for a query requesting Contract, Account, and File
         * identifiers for a smart contract, given a Solidity identifier.
         * @member {proto.GetBySolidityIDResponse.$Properties|null|undefined} getBySolidityID
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.getBySolidityID = null;

        /**
         * A response for a _local_ call to a query function of a given smart
         * contract, providing function parameter inputs as needed.
         * <p>
         * > This call is only performed on the local node. It is _not_ a
         * > network consensus result.
         * @member {proto.ContractCallLocalResponse.$Properties|null|undefined} contractCallLocal
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.contractCallLocal = null;

        /**
         * A response for a query requesting the current bytecode for
         * a smart contract.
         * @member {proto.ContractGetBytecodeResponse.$Properties|null|undefined} contractGetBytecodeResponse
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.contractGetBytecodeResponse = null;

        /**
         * A response for a query requesting detailed information about
         * a smart contract.
         * @member {proto.ContractGetInfoResponse.$Properties|null|undefined} contractGetInfo
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.contractGetInfo = null;

        /**
         * A response for a query requesting records of all transactions
         * against the given contract in the last 25 hours.
         * <p>
         * > This query is no longer supported.
         * @member {proto.ContractGetRecordsResponse.$Properties|null|undefined} contractGetRecordsResponse
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.contractGetRecordsResponse = null;

        /**
         * A response for a query requesting the HBAR balance of an account
         * or contract.
         * @member {proto.CryptoGetAccountBalanceResponse.$Properties|null|undefined} cryptogetAccountBalance
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.cryptogetAccountBalance = null;

        /**
         * A response for a query requesting records of all "recent"
         * transactions for which the specified account is the effective payer.
         * @member {proto.CryptoGetAccountRecordsResponse.$Properties|null|undefined} cryptoGetAccountRecords
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.cryptoGetAccountRecords = null;

        /**
         * A response for a query requesting information for an account.<br/>
         * This query includes balance, but not allowances or token
         * relationships.
         * @member {proto.CryptoGetInfoResponse.$Properties|null|undefined} cryptoGetInfo
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.cryptoGetInfo = null;

        /**
         * A response for a query requesting detail for a specific live hash
         * associated to a specific account.
         * <p>
         * > This query is no longer supported.
         * @member {proto.CryptoGetLiveHashResponse.$Properties|null|undefined} cryptoGetLiveHash
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.cryptoGetLiveHash = null;

        /**
         * A response for a query requesting all the accounts that are proxy
         * staking to this account.
         * <p>
         * > This query is no longer supported.
         * @member {proto.CryptoGetStakersResponse.$Properties|null|undefined} cryptoGetProxyStakers
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.cryptoGetProxyStakers = null;

        /**
         * A response for a query requesting the content of a file in the
         * Hedera File Service (HFS).
         * @member {proto.FileGetContentsResponse.$Properties|null|undefined} fileGetContents
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.fileGetContents = null;

        /**
         * A response for a query requesting file metadata from the
         * Hedera File Service (HFS).
         * @member {proto.FileGetInfoResponse.$Properties|null|undefined} fileGetInfo
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.fileGetInfo = null;

        /**
         * A response for a query requesting the post-consensus (final)
         * result of a transaction.
         * @member {proto.TransactionGetReceiptResponse.$Properties|null|undefined} transactionGetReceipt
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.transactionGetReceipt = null;

        /**
         * A response for a query requesting a transaction record; the detail
         * changes completed in response to a transaction.
         * @member {proto.TransactionGetRecordResponse.$Properties|null|undefined} transactionGetRecord
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.transactionGetRecord = null;

        /**
         * A response for a query requesting a very recent transaction record.
         * <p>
         * > This query is no longer supported.
         * @member {proto.TransactionGetFastRecordResponse.$Properties|null|undefined} transactionGetFastRecord
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.transactionGetFastRecord = null;

        /**
         * A response for a query requesting the current state of a topic for
         * the Hedera Consensus Service (HCS).
         * @member {proto.ConsensusGetTopicInfoResponse.$Properties|null|undefined} consensusGetTopicInfo
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.consensusGetTopicInfo = null;

        /**
         * A response for a query requesting the deployed versions of Hedera
         * Services and the API definitions in semantic version format
         * @member {proto.NetworkGetVersionInfoResponse.$Properties|null|undefined} networkGetVersionInfo
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.networkGetVersionInfo = null;

        /**
         * A response for a query requesting metadata for a specific Token.
         * @member {proto.TokenGetInfoResponse.$Properties|null|undefined} tokenGetInfo
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.tokenGetInfo = null;

        /**
         * A response for a query requesting detail for a scheduled transaction.
         * @member {proto.ScheduleGetInfoResponse.$Properties|null|undefined} scheduleGetInfo
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.scheduleGetInfo = null;

        /**
         * A response for a query requesting detail for a subset of individual
         * non-fungible/unique tokens owned by an account.<br/>
         * The requested tokens are selected by a list index, which is based
         * on the order in which the tokens were added to the account.
         * @member {proto.TokenGetAccountNftInfosResponse.$Properties|null|undefined} tokenGetAccountNftInfos
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.tokenGetAccountNftInfos = null;

        /**
         * A response for a query requesting detail for a specific
         * non-fungible/unique token selected by both token identifier and
         * serial number.
         * @member {proto.TokenGetNftInfoResponse.$Properties|null|undefined} tokenGetNftInfo
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.tokenGetNftInfo = null;

        /**
         * A response for a query requesting detail for a subset of individual
         * non-fungible/unique tokens.<br/>
         * The requested tokens are selected by a list index, which is based
         * on the order in which the tokens were minted.
         * @member {proto.TokenGetNftInfosResponse.$Properties|null|undefined} tokenGetNftInfos
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.tokenGetNftInfos = null;

        /**
         * A response for a query requesting the time, in nanoseconds, spent
         * in direct processing for one or more recent transactions.
         * <p>
         * > This query is no longer supported.
         * @member {proto.NetworkGetExecutionTimeResponse.$Properties|null|undefined} networkGetExecutionTime
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.networkGetExecutionTime = null;

        /**
         * A response for a query requesting detail information
         * about an account.
         * <p>
         * This query is a privileged query and "system" account authorization
         * is REQUIRED for this query.
         * @member {proto.GetAccountDetailsResponse.$Properties|null|undefined} accountDetails
         * @memberof proto.Response
         * @instance
         */
        Response.prototype.accountDetails = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * Response response.
         * @member {"getByKey"|"getBySolidityID"|"contractCallLocal"|"contractGetBytecodeResponse"|"contractGetInfo"|"contractGetRecordsResponse"|"cryptogetAccountBalance"|"cryptoGetAccountRecords"|"cryptoGetInfo"|"cryptoGetLiveHash"|"cryptoGetProxyStakers"|"fileGetContents"|"fileGetInfo"|"transactionGetReceipt"|"transactionGetRecord"|"transactionGetFastRecord"|"consensusGetTopicInfo"|"networkGetVersionInfo"|"tokenGetInfo"|"scheduleGetInfo"|"tokenGetAccountNftInfos"|"tokenGetNftInfo"|"tokenGetNftInfos"|"networkGetExecutionTime"|"accountDetails"|undefined} response
         * @memberof proto.Response
         * @instance
         */
        $Object.defineProperty(Response.prototype, "response", {
            get: $util.oneOfGetter($oneOfFields = ["getByKey", "getBySolidityID", "contractCallLocal", "contractGetBytecodeResponse", "contractGetInfo", "contractGetRecordsResponse", "cryptogetAccountBalance", "cryptoGetAccountRecords", "cryptoGetInfo", "cryptoGetLiveHash", "cryptoGetProxyStakers", "fileGetContents", "fileGetInfo", "transactionGetReceipt", "transactionGetRecord", "transactionGetFastRecord", "consensusGetTopicInfo", "networkGetVersionInfo", "tokenGetInfo", "scheduleGetInfo", "tokenGetAccountNftInfos", "tokenGetNftInfo", "tokenGetNftInfos", "networkGetExecutionTime", "accountDetails"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Response instance using the specified properties.
         * @function create
         * @memberof proto.Response
         * @static
         * @param {proto.Response.$Properties=} [properties] Properties to set
         * @returns {proto.Response} Response instance
         * @type {{
         *   (properties: proto.Response.$Shape): proto.Response & proto.Response.$Shape;
         *   (properties?: proto.Response.$Properties): proto.Response;
         * }}
         */
        Response.create = function(properties) {
            return new Response(properties);
        };

        /**
         * Encodes the specified Response message. Does not implicitly {@link proto.Response.verify|verify} messages.
         * @function encode
         * @memberof proto.Response
         * @static
         * @param {proto.Response.$Properties} message Response message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Response.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.getByKey != null && $Object.hasOwnProperty.call(message, "getByKey"))
                $root.proto.GetByKeyResponse.encode(message.getByKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.getBySolidityID != null && $Object.hasOwnProperty.call(message, "getBySolidityID"))
                $root.proto.GetBySolidityIDResponse.encode(message.getBySolidityID, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.contractCallLocal != null && $Object.hasOwnProperty.call(message, "contractCallLocal"))
                $root.proto.ContractCallLocalResponse.encode(message.contractCallLocal, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.contractGetInfo != null && $Object.hasOwnProperty.call(message, "contractGetInfo"))
                $root.proto.ContractGetInfoResponse.encode(message.contractGetInfo, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.contractGetBytecodeResponse != null && $Object.hasOwnProperty.call(message, "contractGetBytecodeResponse"))
                $root.proto.ContractGetBytecodeResponse.encode(message.contractGetBytecodeResponse, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.contractGetRecordsResponse != null && $Object.hasOwnProperty.call(message, "contractGetRecordsResponse"))
                $root.proto.ContractGetRecordsResponse.encode(message.contractGetRecordsResponse, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.cryptogetAccountBalance != null && $Object.hasOwnProperty.call(message, "cryptogetAccountBalance"))
                $root.proto.CryptoGetAccountBalanceResponse.encode(message.cryptogetAccountBalance, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.cryptoGetAccountRecords != null && $Object.hasOwnProperty.call(message, "cryptoGetAccountRecords"))
                $root.proto.CryptoGetAccountRecordsResponse.encode(message.cryptoGetAccountRecords, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.cryptoGetInfo != null && $Object.hasOwnProperty.call(message, "cryptoGetInfo"))
                $root.proto.CryptoGetInfoResponse.encode(message.cryptoGetInfo, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.cryptoGetLiveHash != null && $Object.hasOwnProperty.call(message, "cryptoGetLiveHash"))
                $root.proto.CryptoGetLiveHashResponse.encode(message.cryptoGetLiveHash, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.cryptoGetProxyStakers != null && $Object.hasOwnProperty.call(message, "cryptoGetProxyStakers"))
                $root.proto.CryptoGetStakersResponse.encode(message.cryptoGetProxyStakers, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.fileGetContents != null && $Object.hasOwnProperty.call(message, "fileGetContents"))
                $root.proto.FileGetContentsResponse.encode(message.fileGetContents, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.fileGetInfo != null && $Object.hasOwnProperty.call(message, "fileGetInfo"))
                $root.proto.FileGetInfoResponse.encode(message.fileGetInfo, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.transactionGetReceipt != null && $Object.hasOwnProperty.call(message, "transactionGetReceipt"))
                $root.proto.TransactionGetReceiptResponse.encode(message.transactionGetReceipt, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.transactionGetRecord != null && $Object.hasOwnProperty.call(message, "transactionGetRecord"))
                $root.proto.TransactionGetRecordResponse.encode(message.transactionGetRecord, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.transactionGetFastRecord != null && $Object.hasOwnProperty.call(message, "transactionGetFastRecord"))
                $root.proto.TransactionGetFastRecordResponse.encode(message.transactionGetFastRecord, writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.consensusGetTopicInfo != null && $Object.hasOwnProperty.call(message, "consensusGetTopicInfo"))
                $root.proto.ConsensusGetTopicInfoResponse.encode(message.consensusGetTopicInfo, writer.uint32(/* id 150, wireType 2 =*/1202).fork(), _depth + 1).ldelim();
            if (message.networkGetVersionInfo != null && $Object.hasOwnProperty.call(message, "networkGetVersionInfo"))
                $root.proto.NetworkGetVersionInfoResponse.encode(message.networkGetVersionInfo, writer.uint32(/* id 151, wireType 2 =*/1210).fork(), _depth + 1).ldelim();
            if (message.tokenGetInfo != null && $Object.hasOwnProperty.call(message, "tokenGetInfo"))
                $root.proto.TokenGetInfoResponse.encode(message.tokenGetInfo, writer.uint32(/* id 152, wireType 2 =*/1218).fork(), _depth + 1).ldelim();
            if (message.scheduleGetInfo != null && $Object.hasOwnProperty.call(message, "scheduleGetInfo"))
                $root.proto.ScheduleGetInfoResponse.encode(message.scheduleGetInfo, writer.uint32(/* id 153, wireType 2 =*/1226).fork(), _depth + 1).ldelim();
            if (message.tokenGetAccountNftInfos != null && $Object.hasOwnProperty.call(message, "tokenGetAccountNftInfos"))
                $root.proto.TokenGetAccountNftInfosResponse.encode(message.tokenGetAccountNftInfos, writer.uint32(/* id 154, wireType 2 =*/1234).fork(), _depth + 1).ldelim();
            if (message.tokenGetNftInfo != null && $Object.hasOwnProperty.call(message, "tokenGetNftInfo"))
                $root.proto.TokenGetNftInfoResponse.encode(message.tokenGetNftInfo, writer.uint32(/* id 155, wireType 2 =*/1242).fork(), _depth + 1).ldelim();
            if (message.tokenGetNftInfos != null && $Object.hasOwnProperty.call(message, "tokenGetNftInfos"))
                $root.proto.TokenGetNftInfosResponse.encode(message.tokenGetNftInfos, writer.uint32(/* id 156, wireType 2 =*/1250).fork(), _depth + 1).ldelim();
            if (message.networkGetExecutionTime != null && $Object.hasOwnProperty.call(message, "networkGetExecutionTime"))
                $root.proto.NetworkGetExecutionTimeResponse.encode(message.networkGetExecutionTime, writer.uint32(/* id 157, wireType 2 =*/1258).fork(), _depth + 1).ldelim();
            if (message.accountDetails != null && $Object.hasOwnProperty.call(message, "accountDetails"))
                $root.proto.GetAccountDetailsResponse.encode(message.accountDetails, writer.uint32(/* id 158, wireType 2 =*/1266).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Response message, length delimited. Does not implicitly {@link proto.Response.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Response
         * @static
         * @param {proto.Response.$Properties} message Response message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Response.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Response message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Response
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Response & proto.Response.$Shape} Response
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Response.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Response();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.getByKey = $root.proto.GetByKeyResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.getByKey);
                        message.response = "getByKey";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.getBySolidityID = $root.proto.GetBySolidityIDResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.getBySolidityID);
                        message.response = "getBySolidityID";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.contractCallLocal = $root.proto.ContractCallLocalResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractCallLocal);
                        message.response = "contractCallLocal";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.contractGetBytecodeResponse = $root.proto.ContractGetBytecodeResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractGetBytecodeResponse);
                        message.response = "contractGetBytecodeResponse";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.contractGetInfo = $root.proto.ContractGetInfoResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractGetInfo);
                        message.response = "contractGetInfo";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.contractGetRecordsResponse = $root.proto.ContractGetRecordsResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractGetRecordsResponse);
                        message.response = "contractGetRecordsResponse";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.cryptogetAccountBalance = $root.proto.CryptoGetAccountBalanceResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptogetAccountBalance);
                        message.response = "cryptogetAccountBalance";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.cryptoGetAccountRecords = $root.proto.CryptoGetAccountRecordsResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoGetAccountRecords);
                        message.response = "cryptoGetAccountRecords";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.cryptoGetInfo = $root.proto.CryptoGetInfoResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoGetInfo);
                        message.response = "cryptoGetInfo";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.cryptoGetLiveHash = $root.proto.CryptoGetLiveHashResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoGetLiveHash);
                        message.response = "cryptoGetLiveHash";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.cryptoGetProxyStakers = $root.proto.CryptoGetStakersResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cryptoGetProxyStakers);
                        message.response = "cryptoGetProxyStakers";
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.fileGetContents = $root.proto.FileGetContentsResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileGetContents);
                        message.response = "fileGetContents";
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.fileGetInfo = $root.proto.FileGetInfoResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileGetInfo);
                        message.response = "fileGetInfo";
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.transactionGetReceipt = $root.proto.TransactionGetReceiptResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionGetReceipt);
                        message.response = "transactionGetReceipt";
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.transactionGetRecord = $root.proto.TransactionGetRecordResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionGetRecord);
                        message.response = "transactionGetRecord";
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.transactionGetFastRecord = $root.proto.TransactionGetFastRecordResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionGetFastRecord);
                        message.response = "transactionGetFastRecord";
                        continue;
                    }
                case 150: {
                        if (wireType !== 2)
                            break;
                        message.consensusGetTopicInfo = $root.proto.ConsensusGetTopicInfoResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusGetTopicInfo);
                        message.response = "consensusGetTopicInfo";
                        continue;
                    }
                case 151: {
                        if (wireType !== 2)
                            break;
                        message.networkGetVersionInfo = $root.proto.NetworkGetVersionInfoResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.networkGetVersionInfo);
                        message.response = "networkGetVersionInfo";
                        continue;
                    }
                case 152: {
                        if (wireType !== 2)
                            break;
                        message.tokenGetInfo = $root.proto.TokenGetInfoResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenGetInfo);
                        message.response = "tokenGetInfo";
                        continue;
                    }
                case 153: {
                        if (wireType !== 2)
                            break;
                        message.scheduleGetInfo = $root.proto.ScheduleGetInfoResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleGetInfo);
                        message.response = "scheduleGetInfo";
                        continue;
                    }
                case 154: {
                        if (wireType !== 2)
                            break;
                        message.tokenGetAccountNftInfos = $root.proto.TokenGetAccountNftInfosResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenGetAccountNftInfos);
                        message.response = "tokenGetAccountNftInfos";
                        continue;
                    }
                case 155: {
                        if (wireType !== 2)
                            break;
                        message.tokenGetNftInfo = $root.proto.TokenGetNftInfoResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenGetNftInfo);
                        message.response = "tokenGetNftInfo";
                        continue;
                    }
                case 156: {
                        if (wireType !== 2)
                            break;
                        message.tokenGetNftInfos = $root.proto.TokenGetNftInfosResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenGetNftInfos);
                        message.response = "tokenGetNftInfos";
                        continue;
                    }
                case 157: {
                        if (wireType !== 2)
                            break;
                        message.networkGetExecutionTime = $root.proto.NetworkGetExecutionTimeResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.networkGetExecutionTime);
                        message.response = "networkGetExecutionTime";
                        continue;
                    }
                case 158: {
                        if (wireType !== 2)
                            break;
                        message.accountDetails = $root.proto.GetAccountDetailsResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountDetails);
                        message.response = "accountDetails";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Response message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Response
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Response & proto.Response.$Shape} Response
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Response.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Response message.
         * @function verify
         * @memberof proto.Response
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Response.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.getByKey != null && $Object.hasOwnProperty.call(message, "getByKey")) {
                properties.response = 1;
                {
                    let error = $root.proto.GetByKeyResponse.verify(message.getByKey, _depth + 1);
                    if (error)
                        return "getByKey." + error;
                }
            }
            if (message.getBySolidityID != null && $Object.hasOwnProperty.call(message, "getBySolidityID")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.GetBySolidityIDResponse.verify(message.getBySolidityID, _depth + 1);
                    if (error)
                        return "getBySolidityID." + error;
                }
            }
            if (message.contractCallLocal != null && $Object.hasOwnProperty.call(message, "contractCallLocal")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.ContractCallLocalResponse.verify(message.contractCallLocal, _depth + 1);
                    if (error)
                        return "contractCallLocal." + error;
                }
            }
            if (message.contractGetBytecodeResponse != null && $Object.hasOwnProperty.call(message, "contractGetBytecodeResponse")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.ContractGetBytecodeResponse.verify(message.contractGetBytecodeResponse, _depth + 1);
                    if (error)
                        return "contractGetBytecodeResponse." + error;
                }
            }
            if (message.contractGetInfo != null && $Object.hasOwnProperty.call(message, "contractGetInfo")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.ContractGetInfoResponse.verify(message.contractGetInfo, _depth + 1);
                    if (error)
                        return "contractGetInfo." + error;
                }
            }
            if (message.contractGetRecordsResponse != null && $Object.hasOwnProperty.call(message, "contractGetRecordsResponse")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.ContractGetRecordsResponse.verify(message.contractGetRecordsResponse, _depth + 1);
                    if (error)
                        return "contractGetRecordsResponse." + error;
                }
            }
            if (message.cryptogetAccountBalance != null && $Object.hasOwnProperty.call(message, "cryptogetAccountBalance")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.CryptoGetAccountBalanceResponse.verify(message.cryptogetAccountBalance, _depth + 1);
                    if (error)
                        return "cryptogetAccountBalance." + error;
                }
            }
            if (message.cryptoGetAccountRecords != null && $Object.hasOwnProperty.call(message, "cryptoGetAccountRecords")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.CryptoGetAccountRecordsResponse.verify(message.cryptoGetAccountRecords, _depth + 1);
                    if (error)
                        return "cryptoGetAccountRecords." + error;
                }
            }
            if (message.cryptoGetInfo != null && $Object.hasOwnProperty.call(message, "cryptoGetInfo")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.CryptoGetInfoResponse.verify(message.cryptoGetInfo, _depth + 1);
                    if (error)
                        return "cryptoGetInfo." + error;
                }
            }
            if (message.cryptoGetLiveHash != null && $Object.hasOwnProperty.call(message, "cryptoGetLiveHash")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.CryptoGetLiveHashResponse.verify(message.cryptoGetLiveHash, _depth + 1);
                    if (error)
                        return "cryptoGetLiveHash." + error;
                }
            }
            if (message.cryptoGetProxyStakers != null && $Object.hasOwnProperty.call(message, "cryptoGetProxyStakers")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.CryptoGetStakersResponse.verify(message.cryptoGetProxyStakers, _depth + 1);
                    if (error)
                        return "cryptoGetProxyStakers." + error;
                }
            }
            if (message.fileGetContents != null && $Object.hasOwnProperty.call(message, "fileGetContents")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.FileGetContentsResponse.verify(message.fileGetContents, _depth + 1);
                    if (error)
                        return "fileGetContents." + error;
                }
            }
            if (message.fileGetInfo != null && $Object.hasOwnProperty.call(message, "fileGetInfo")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.FileGetInfoResponse.verify(message.fileGetInfo, _depth + 1);
                    if (error)
                        return "fileGetInfo." + error;
                }
            }
            if (message.transactionGetReceipt != null && $Object.hasOwnProperty.call(message, "transactionGetReceipt")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.TransactionGetReceiptResponse.verify(message.transactionGetReceipt, _depth + 1);
                    if (error)
                        return "transactionGetReceipt." + error;
                }
            }
            if (message.transactionGetRecord != null && $Object.hasOwnProperty.call(message, "transactionGetRecord")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.TransactionGetRecordResponse.verify(message.transactionGetRecord, _depth + 1);
                    if (error)
                        return "transactionGetRecord." + error;
                }
            }
            if (message.transactionGetFastRecord != null && $Object.hasOwnProperty.call(message, "transactionGetFastRecord")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.TransactionGetFastRecordResponse.verify(message.transactionGetFastRecord, _depth + 1);
                    if (error)
                        return "transactionGetFastRecord." + error;
                }
            }
            if (message.consensusGetTopicInfo != null && $Object.hasOwnProperty.call(message, "consensusGetTopicInfo")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.ConsensusGetTopicInfoResponse.verify(message.consensusGetTopicInfo, _depth + 1);
                    if (error)
                        return "consensusGetTopicInfo." + error;
                }
            }
            if (message.networkGetVersionInfo != null && $Object.hasOwnProperty.call(message, "networkGetVersionInfo")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.NetworkGetVersionInfoResponse.verify(message.networkGetVersionInfo, _depth + 1);
                    if (error)
                        return "networkGetVersionInfo." + error;
                }
            }
            if (message.tokenGetInfo != null && $Object.hasOwnProperty.call(message, "tokenGetInfo")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.TokenGetInfoResponse.verify(message.tokenGetInfo, _depth + 1);
                    if (error)
                        return "tokenGetInfo." + error;
                }
            }
            if (message.scheduleGetInfo != null && $Object.hasOwnProperty.call(message, "scheduleGetInfo")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.ScheduleGetInfoResponse.verify(message.scheduleGetInfo, _depth + 1);
                    if (error)
                        return "scheduleGetInfo." + error;
                }
            }
            if (message.tokenGetAccountNftInfos != null && $Object.hasOwnProperty.call(message, "tokenGetAccountNftInfos")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.TokenGetAccountNftInfosResponse.verify(message.tokenGetAccountNftInfos, _depth + 1);
                    if (error)
                        return "tokenGetAccountNftInfos." + error;
                }
            }
            if (message.tokenGetNftInfo != null && $Object.hasOwnProperty.call(message, "tokenGetNftInfo")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.TokenGetNftInfoResponse.verify(message.tokenGetNftInfo, _depth + 1);
                    if (error)
                        return "tokenGetNftInfo." + error;
                }
            }
            if (message.tokenGetNftInfos != null && $Object.hasOwnProperty.call(message, "tokenGetNftInfos")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.TokenGetNftInfosResponse.verify(message.tokenGetNftInfos, _depth + 1);
                    if (error)
                        return "tokenGetNftInfos." + error;
                }
            }
            if (message.networkGetExecutionTime != null && $Object.hasOwnProperty.call(message, "networkGetExecutionTime")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.NetworkGetExecutionTimeResponse.verify(message.networkGetExecutionTime, _depth + 1);
                    if (error)
                        return "networkGetExecutionTime." + error;
                }
            }
            if (message.accountDetails != null && $Object.hasOwnProperty.call(message, "accountDetails")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.proto.GetAccountDetailsResponse.verify(message.accountDetails, _depth + 1);
                    if (error)
                        return "accountDetails." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Response message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Response
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Response} Response
         */
        Response.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Response)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Response: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Response();
            if (object.getByKey != null) {
                if (!$util.isObject(object.getByKey))
                    throw $TypeError(".proto.Response.getByKey: object expected");
                message.getByKey = $root.proto.GetByKeyResponse.fromObject(object.getByKey, _depth + 1);
            }
            if (object.getBySolidityID != null) {
                if (!$util.isObject(object.getBySolidityID))
                    throw $TypeError(".proto.Response.getBySolidityID: object expected");
                message.getBySolidityID = $root.proto.GetBySolidityIDResponse.fromObject(object.getBySolidityID, _depth + 1);
            }
            if (object.contractCallLocal != null) {
                if (!$util.isObject(object.contractCallLocal))
                    throw $TypeError(".proto.Response.contractCallLocal: object expected");
                message.contractCallLocal = $root.proto.ContractCallLocalResponse.fromObject(object.contractCallLocal, _depth + 1);
            }
            if (object.contractGetBytecodeResponse != null) {
                if (!$util.isObject(object.contractGetBytecodeResponse))
                    throw $TypeError(".proto.Response.contractGetBytecodeResponse: object expected");
                message.contractGetBytecodeResponse = $root.proto.ContractGetBytecodeResponse.fromObject(object.contractGetBytecodeResponse, _depth + 1);
            }
            if (object.contractGetInfo != null) {
                if (!$util.isObject(object.contractGetInfo))
                    throw $TypeError(".proto.Response.contractGetInfo: object expected");
                message.contractGetInfo = $root.proto.ContractGetInfoResponse.fromObject(object.contractGetInfo, _depth + 1);
            }
            if (object.contractGetRecordsResponse != null) {
                if (!$util.isObject(object.contractGetRecordsResponse))
                    throw $TypeError(".proto.Response.contractGetRecordsResponse: object expected");
                message.contractGetRecordsResponse = $root.proto.ContractGetRecordsResponse.fromObject(object.contractGetRecordsResponse, _depth + 1);
            }
            if (object.cryptogetAccountBalance != null) {
                if (!$util.isObject(object.cryptogetAccountBalance))
                    throw $TypeError(".proto.Response.cryptogetAccountBalance: object expected");
                message.cryptogetAccountBalance = $root.proto.CryptoGetAccountBalanceResponse.fromObject(object.cryptogetAccountBalance, _depth + 1);
            }
            if (object.cryptoGetAccountRecords != null) {
                if (!$util.isObject(object.cryptoGetAccountRecords))
                    throw $TypeError(".proto.Response.cryptoGetAccountRecords: object expected");
                message.cryptoGetAccountRecords = $root.proto.CryptoGetAccountRecordsResponse.fromObject(object.cryptoGetAccountRecords, _depth + 1);
            }
            if (object.cryptoGetInfo != null) {
                if (!$util.isObject(object.cryptoGetInfo))
                    throw $TypeError(".proto.Response.cryptoGetInfo: object expected");
                message.cryptoGetInfo = $root.proto.CryptoGetInfoResponse.fromObject(object.cryptoGetInfo, _depth + 1);
            }
            if (object.cryptoGetLiveHash != null) {
                if (!$util.isObject(object.cryptoGetLiveHash))
                    throw $TypeError(".proto.Response.cryptoGetLiveHash: object expected");
                message.cryptoGetLiveHash = $root.proto.CryptoGetLiveHashResponse.fromObject(object.cryptoGetLiveHash, _depth + 1);
            }
            if (object.cryptoGetProxyStakers != null) {
                if (!$util.isObject(object.cryptoGetProxyStakers))
                    throw $TypeError(".proto.Response.cryptoGetProxyStakers: object expected");
                message.cryptoGetProxyStakers = $root.proto.CryptoGetStakersResponse.fromObject(object.cryptoGetProxyStakers, _depth + 1);
            }
            if (object.fileGetContents != null) {
                if (!$util.isObject(object.fileGetContents))
                    throw $TypeError(".proto.Response.fileGetContents: object expected");
                message.fileGetContents = $root.proto.FileGetContentsResponse.fromObject(object.fileGetContents, _depth + 1);
            }
            if (object.fileGetInfo != null) {
                if (!$util.isObject(object.fileGetInfo))
                    throw $TypeError(".proto.Response.fileGetInfo: object expected");
                message.fileGetInfo = $root.proto.FileGetInfoResponse.fromObject(object.fileGetInfo, _depth + 1);
            }
            if (object.transactionGetReceipt != null) {
                if (!$util.isObject(object.transactionGetReceipt))
                    throw $TypeError(".proto.Response.transactionGetReceipt: object expected");
                message.transactionGetReceipt = $root.proto.TransactionGetReceiptResponse.fromObject(object.transactionGetReceipt, _depth + 1);
            }
            if (object.transactionGetRecord != null) {
                if (!$util.isObject(object.transactionGetRecord))
                    throw $TypeError(".proto.Response.transactionGetRecord: object expected");
                message.transactionGetRecord = $root.proto.TransactionGetRecordResponse.fromObject(object.transactionGetRecord, _depth + 1);
            }
            if (object.transactionGetFastRecord != null) {
                if (!$util.isObject(object.transactionGetFastRecord))
                    throw $TypeError(".proto.Response.transactionGetFastRecord: object expected");
                message.transactionGetFastRecord = $root.proto.TransactionGetFastRecordResponse.fromObject(object.transactionGetFastRecord, _depth + 1);
            }
            if (object.consensusGetTopicInfo != null) {
                if (!$util.isObject(object.consensusGetTopicInfo))
                    throw $TypeError(".proto.Response.consensusGetTopicInfo: object expected");
                message.consensusGetTopicInfo = $root.proto.ConsensusGetTopicInfoResponse.fromObject(object.consensusGetTopicInfo, _depth + 1);
            }
            if (object.networkGetVersionInfo != null) {
                if (!$util.isObject(object.networkGetVersionInfo))
                    throw $TypeError(".proto.Response.networkGetVersionInfo: object expected");
                message.networkGetVersionInfo = $root.proto.NetworkGetVersionInfoResponse.fromObject(object.networkGetVersionInfo, _depth + 1);
            }
            if (object.tokenGetInfo != null) {
                if (!$util.isObject(object.tokenGetInfo))
                    throw $TypeError(".proto.Response.tokenGetInfo: object expected");
                message.tokenGetInfo = $root.proto.TokenGetInfoResponse.fromObject(object.tokenGetInfo, _depth + 1);
            }
            if (object.scheduleGetInfo != null) {
                if (!$util.isObject(object.scheduleGetInfo))
                    throw $TypeError(".proto.Response.scheduleGetInfo: object expected");
                message.scheduleGetInfo = $root.proto.ScheduleGetInfoResponse.fromObject(object.scheduleGetInfo, _depth + 1);
            }
            if (object.tokenGetAccountNftInfos != null) {
                if (!$util.isObject(object.tokenGetAccountNftInfos))
                    throw $TypeError(".proto.Response.tokenGetAccountNftInfos: object expected");
                message.tokenGetAccountNftInfos = $root.proto.TokenGetAccountNftInfosResponse.fromObject(object.tokenGetAccountNftInfos, _depth + 1);
            }
            if (object.tokenGetNftInfo != null) {
                if (!$util.isObject(object.tokenGetNftInfo))
                    throw $TypeError(".proto.Response.tokenGetNftInfo: object expected");
                message.tokenGetNftInfo = $root.proto.TokenGetNftInfoResponse.fromObject(object.tokenGetNftInfo, _depth + 1);
            }
            if (object.tokenGetNftInfos != null) {
                if (!$util.isObject(object.tokenGetNftInfos))
                    throw $TypeError(".proto.Response.tokenGetNftInfos: object expected");
                message.tokenGetNftInfos = $root.proto.TokenGetNftInfosResponse.fromObject(object.tokenGetNftInfos, _depth + 1);
            }
            if (object.networkGetExecutionTime != null) {
                if (!$util.isObject(object.networkGetExecutionTime))
                    throw $TypeError(".proto.Response.networkGetExecutionTime: object expected");
                message.networkGetExecutionTime = $root.proto.NetworkGetExecutionTimeResponse.fromObject(object.networkGetExecutionTime, _depth + 1);
            }
            if (object.accountDetails != null) {
                if (!$util.isObject(object.accountDetails))
                    throw $TypeError(".proto.Response.accountDetails: object expected");
                message.accountDetails = $root.proto.GetAccountDetailsResponse.fromObject(object.accountDetails, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Response message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Response
         * @static
         * @param {proto.Response} message Response
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Response.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.getByKey != null && $Object.hasOwnProperty.call(message, "getByKey")) {
                object.getByKey = $root.proto.GetByKeyResponse.toObject(message.getByKey, options, _depth + 1);
                if (options.oneofs)
                    object.response = "getByKey";
            }
            if (message.getBySolidityID != null && $Object.hasOwnProperty.call(message, "getBySolidityID")) {
                object.getBySolidityID = $root.proto.GetBySolidityIDResponse.toObject(message.getBySolidityID, options, _depth + 1);
                if (options.oneofs)
                    object.response = "getBySolidityID";
            }
            if (message.contractCallLocal != null && $Object.hasOwnProperty.call(message, "contractCallLocal")) {
                object.contractCallLocal = $root.proto.ContractCallLocalResponse.toObject(message.contractCallLocal, options, _depth + 1);
                if (options.oneofs)
                    object.response = "contractCallLocal";
            }
            if (message.contractGetInfo != null && $Object.hasOwnProperty.call(message, "contractGetInfo")) {
                object.contractGetInfo = $root.proto.ContractGetInfoResponse.toObject(message.contractGetInfo, options, _depth + 1);
                if (options.oneofs)
                    object.response = "contractGetInfo";
            }
            if (message.contractGetBytecodeResponse != null && $Object.hasOwnProperty.call(message, "contractGetBytecodeResponse")) {
                object.contractGetBytecodeResponse = $root.proto.ContractGetBytecodeResponse.toObject(message.contractGetBytecodeResponse, options, _depth + 1);
                if (options.oneofs)
                    object.response = "contractGetBytecodeResponse";
            }
            if (message.contractGetRecordsResponse != null && $Object.hasOwnProperty.call(message, "contractGetRecordsResponse")) {
                object.contractGetRecordsResponse = $root.proto.ContractGetRecordsResponse.toObject(message.contractGetRecordsResponse, options, _depth + 1);
                if (options.oneofs)
                    object.response = "contractGetRecordsResponse";
            }
            if (message.cryptogetAccountBalance != null && $Object.hasOwnProperty.call(message, "cryptogetAccountBalance")) {
                object.cryptogetAccountBalance = $root.proto.CryptoGetAccountBalanceResponse.toObject(message.cryptogetAccountBalance, options, _depth + 1);
                if (options.oneofs)
                    object.response = "cryptogetAccountBalance";
            }
            if (message.cryptoGetAccountRecords != null && $Object.hasOwnProperty.call(message, "cryptoGetAccountRecords")) {
                object.cryptoGetAccountRecords = $root.proto.CryptoGetAccountRecordsResponse.toObject(message.cryptoGetAccountRecords, options, _depth + 1);
                if (options.oneofs)
                    object.response = "cryptoGetAccountRecords";
            }
            if (message.cryptoGetInfo != null && $Object.hasOwnProperty.call(message, "cryptoGetInfo")) {
                object.cryptoGetInfo = $root.proto.CryptoGetInfoResponse.toObject(message.cryptoGetInfo, options, _depth + 1);
                if (options.oneofs)
                    object.response = "cryptoGetInfo";
            }
            if (message.cryptoGetLiveHash != null && $Object.hasOwnProperty.call(message, "cryptoGetLiveHash")) {
                object.cryptoGetLiveHash = $root.proto.CryptoGetLiveHashResponse.toObject(message.cryptoGetLiveHash, options, _depth + 1);
                if (options.oneofs)
                    object.response = "cryptoGetLiveHash";
            }
            if (message.cryptoGetProxyStakers != null && $Object.hasOwnProperty.call(message, "cryptoGetProxyStakers")) {
                object.cryptoGetProxyStakers = $root.proto.CryptoGetStakersResponse.toObject(message.cryptoGetProxyStakers, options, _depth + 1);
                if (options.oneofs)
                    object.response = "cryptoGetProxyStakers";
            }
            if (message.fileGetContents != null && $Object.hasOwnProperty.call(message, "fileGetContents")) {
                object.fileGetContents = $root.proto.FileGetContentsResponse.toObject(message.fileGetContents, options, _depth + 1);
                if (options.oneofs)
                    object.response = "fileGetContents";
            }
            if (message.fileGetInfo != null && $Object.hasOwnProperty.call(message, "fileGetInfo")) {
                object.fileGetInfo = $root.proto.FileGetInfoResponse.toObject(message.fileGetInfo, options, _depth + 1);
                if (options.oneofs)
                    object.response = "fileGetInfo";
            }
            if (message.transactionGetReceipt != null && $Object.hasOwnProperty.call(message, "transactionGetReceipt")) {
                object.transactionGetReceipt = $root.proto.TransactionGetReceiptResponse.toObject(message.transactionGetReceipt, options, _depth + 1);
                if (options.oneofs)
                    object.response = "transactionGetReceipt";
            }
            if (message.transactionGetRecord != null && $Object.hasOwnProperty.call(message, "transactionGetRecord")) {
                object.transactionGetRecord = $root.proto.TransactionGetRecordResponse.toObject(message.transactionGetRecord, options, _depth + 1);
                if (options.oneofs)
                    object.response = "transactionGetRecord";
            }
            if (message.transactionGetFastRecord != null && $Object.hasOwnProperty.call(message, "transactionGetFastRecord")) {
                object.transactionGetFastRecord = $root.proto.TransactionGetFastRecordResponse.toObject(message.transactionGetFastRecord, options, _depth + 1);
                if (options.oneofs)
                    object.response = "transactionGetFastRecord";
            }
            if (message.consensusGetTopicInfo != null && $Object.hasOwnProperty.call(message, "consensusGetTopicInfo")) {
                object.consensusGetTopicInfo = $root.proto.ConsensusGetTopicInfoResponse.toObject(message.consensusGetTopicInfo, options, _depth + 1);
                if (options.oneofs)
                    object.response = "consensusGetTopicInfo";
            }
            if (message.networkGetVersionInfo != null && $Object.hasOwnProperty.call(message, "networkGetVersionInfo")) {
                object.networkGetVersionInfo = $root.proto.NetworkGetVersionInfoResponse.toObject(message.networkGetVersionInfo, options, _depth + 1);
                if (options.oneofs)
                    object.response = "networkGetVersionInfo";
            }
            if (message.tokenGetInfo != null && $Object.hasOwnProperty.call(message, "tokenGetInfo")) {
                object.tokenGetInfo = $root.proto.TokenGetInfoResponse.toObject(message.tokenGetInfo, options, _depth + 1);
                if (options.oneofs)
                    object.response = "tokenGetInfo";
            }
            if (message.scheduleGetInfo != null && $Object.hasOwnProperty.call(message, "scheduleGetInfo")) {
                object.scheduleGetInfo = $root.proto.ScheduleGetInfoResponse.toObject(message.scheduleGetInfo, options, _depth + 1);
                if (options.oneofs)
                    object.response = "scheduleGetInfo";
            }
            if (message.tokenGetAccountNftInfos != null && $Object.hasOwnProperty.call(message, "tokenGetAccountNftInfos")) {
                object.tokenGetAccountNftInfos = $root.proto.TokenGetAccountNftInfosResponse.toObject(message.tokenGetAccountNftInfos, options, _depth + 1);
                if (options.oneofs)
                    object.response = "tokenGetAccountNftInfos";
            }
            if (message.tokenGetNftInfo != null && $Object.hasOwnProperty.call(message, "tokenGetNftInfo")) {
                object.tokenGetNftInfo = $root.proto.TokenGetNftInfoResponse.toObject(message.tokenGetNftInfo, options, _depth + 1);
                if (options.oneofs)
                    object.response = "tokenGetNftInfo";
            }
            if (message.tokenGetNftInfos != null && $Object.hasOwnProperty.call(message, "tokenGetNftInfos")) {
                object.tokenGetNftInfos = $root.proto.TokenGetNftInfosResponse.toObject(message.tokenGetNftInfos, options, _depth + 1);
                if (options.oneofs)
                    object.response = "tokenGetNftInfos";
            }
            if (message.networkGetExecutionTime != null && $Object.hasOwnProperty.call(message, "networkGetExecutionTime")) {
                object.networkGetExecutionTime = $root.proto.NetworkGetExecutionTimeResponse.toObject(message.networkGetExecutionTime, options, _depth + 1);
                if (options.oneofs)
                    object.response = "networkGetExecutionTime";
            }
            if (message.accountDetails != null && $Object.hasOwnProperty.call(message, "accountDetails")) {
                object.accountDetails = $root.proto.GetAccountDetailsResponse.toObject(message.accountDetails, options, _depth + 1);
                if (options.oneofs)
                    object.response = "accountDetails";
            }
            return object;
        };

        /**
         * Converts this Response to JSON.
         * @function toJSON
         * @memberof proto.Response
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Response.prototype.toJSON = function() {
            return Response.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Response
         * @function getTypeUrl
         * @memberof proto.Response
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Response.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Response";
        };

        return Response;
    })();

    proto.CryptoService = (function() {

        /**
         * Constructs a new CryptoService service.
         * @memberof proto
         * @classdesc Transactions and queries for the Hedera Crypto Service.
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const CryptoService = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        $Object.defineProperty(CryptoService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: CryptoService, writable: true, enumerable: false, configurable: true });

        /**
         * Creates new CryptoService service using the specified rpc implementation.
         * @function create
         * @memberof proto.CryptoService
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {CryptoService} RPC service. Useful where requests and/or responses are streamed.
         */
        CryptoService.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link proto.CryptoService#createAccount}.
         * @memberof proto.CryptoService
         * @typedef createAccountCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Create a new account by submitting the transaction
         * @memberof proto.CryptoService
         * @typedef createAccount
         * @type {{
         *   (request: proto.ITransaction, callback: proto.CryptoService.createAccountCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "createAccount";
         *   readonly path: "/proto.CryptoService/createAccount";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Create a new account by submitting the transaction
         * @name proto.CryptoService#createAccount
         * @type {proto.CryptoService.createAccount}
         */
        $Object.defineProperties(CryptoService.prototype.createAccount = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.createAccount, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "createAccount" },
            path: { value: "/proto.CryptoService/createAccount" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#updateAccount}.
         * @memberof proto.CryptoService
         * @typedef updateAccountCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Update an account by submitting the transaction
         * @memberof proto.CryptoService
         * @typedef updateAccount
         * @type {{
         *   (request: proto.ITransaction, callback: proto.CryptoService.updateAccountCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "updateAccount";
         *   readonly path: "/proto.CryptoService/updateAccount";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Update an account by submitting the transaction
         * @name proto.CryptoService#updateAccount
         * @type {proto.CryptoService.updateAccount}
         */
        $Object.defineProperties(CryptoService.prototype.updateAccount = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.updateAccount, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "updateAccount" },
            path: { value: "/proto.CryptoService/updateAccount" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#cryptoTransfer}.
         * @memberof proto.CryptoService
         * @typedef cryptoTransferCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Initiate a transfer by submitting the transaction
         * @memberof proto.CryptoService
         * @typedef cryptoTransfer
         * @type {{
         *   (request: proto.ITransaction, callback: proto.CryptoService.cryptoTransferCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "cryptoTransfer";
         *   readonly path: "/proto.CryptoService/cryptoTransfer";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Initiate a transfer by submitting the transaction
         * @name proto.CryptoService#cryptoTransfer
         * @type {proto.CryptoService.cryptoTransfer}
         */
        $Object.defineProperties(CryptoService.prototype.cryptoTransfer = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.cryptoTransfer, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "cryptoTransfer" },
            path: { value: "/proto.CryptoService/cryptoTransfer" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#cryptoDelete}.
         * @memberof proto.CryptoService
         * @typedef cryptoDeleteCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Delete an account by submitting the transaction
         * @memberof proto.CryptoService
         * @typedef cryptoDelete
         * @type {{
         *   (request: proto.ITransaction, callback: proto.CryptoService.cryptoDeleteCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "cryptoDelete";
         *   readonly path: "/proto.CryptoService/cryptoDelete";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Delete an account by submitting the transaction
         * @name proto.CryptoService#cryptoDelete
         * @type {proto.CryptoService.cryptoDelete}
         */
        $Object.defineProperties(CryptoService.prototype.cryptoDelete = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.cryptoDelete, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "cryptoDelete" },
            path: { value: "/proto.CryptoService/cryptoDelete" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#approveAllowances}.
         * @memberof proto.CryptoService
         * @typedef approveAllowancesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Add one or more approved allowances for spenders to transfer the paying
         * account's hbar or tokens.
         * @memberof proto.CryptoService
         * @typedef approveAllowances
         * @type {{
         *   (request: proto.ITransaction, callback: proto.CryptoService.approveAllowancesCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "approveAllowances";
         *   readonly path: "/proto.CryptoService/approveAllowances";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Add one or more approved allowances for spenders to transfer the paying
         * account's hbar or tokens.
         * @name proto.CryptoService#approveAllowances
         * @type {proto.CryptoService.approveAllowances}
         */
        $Object.defineProperties(CryptoService.prototype.approveAllowances = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.approveAllowances, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "approveAllowances" },
            path: { value: "/proto.CryptoService/approveAllowances" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#deleteAllowances}.
         * @memberof proto.CryptoService
         * @typedef deleteAllowancesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Delete one or more of the specific approved NFT serial numbers on an
         * owner account.
         * @memberof proto.CryptoService
         * @typedef deleteAllowances
         * @type {{
         *   (request: proto.ITransaction, callback: proto.CryptoService.deleteAllowancesCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "deleteAllowances";
         *   readonly path: "/proto.CryptoService/deleteAllowances";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Delete one or more of the specific approved NFT serial numbers on an
         * owner account.
         * @name proto.CryptoService#deleteAllowances
         * @type {proto.CryptoService.deleteAllowances}
         */
        $Object.defineProperties(CryptoService.prototype.deleteAllowances = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.deleteAllowances, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "deleteAllowances" },
            path: { value: "/proto.CryptoService/deleteAllowances" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#addLiveHash}.
         * @memberof proto.CryptoService
         * @typedef addLiveHashCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Add a livehash
         * <blockquote>Important<blockquote>
         * This transaction is obsolete, not supported, and SHALL fail with a
         * pre-check result of `NOT_SUPPORTED`.</blockquote></blockquote>
         * @memberof proto.CryptoService
         * @typedef addLiveHash
         * @type {{
         *   (request: proto.ITransaction, callback: proto.CryptoService.addLiveHashCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "addLiveHash";
         *   readonly path: "/proto.CryptoService/addLiveHash";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Add a livehash
         * <blockquote>Important<blockquote>
         * This transaction is obsolete, not supported, and SHALL fail with a
         * pre-check result of `NOT_SUPPORTED`.</blockquote></blockquote>
         * @name proto.CryptoService#addLiveHash
         * @type {proto.CryptoService.addLiveHash}
         */
        $Object.defineProperties(CryptoService.prototype.addLiveHash = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.addLiveHash, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "addLiveHash" },
            path: { value: "/proto.CryptoService/addLiveHash" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#deleteLiveHash}.
         * @memberof proto.CryptoService
         * @typedef deleteLiveHashCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Delete a livehash
         * <blockquote>Important<blockquote>
         * This transaction is obsolete, not supported, and SHALL fail with a
         * pre-check result of `NOT_SUPPORTED`.</blockquote></blockquote>
         * @memberof proto.CryptoService
         * @typedef deleteLiveHash
         * @type {{
         *   (request: proto.ITransaction, callback: proto.CryptoService.deleteLiveHashCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "deleteLiveHash";
         *   readonly path: "/proto.CryptoService/deleteLiveHash";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Delete a livehash
         * <blockquote>Important<blockquote>
         * This transaction is obsolete, not supported, and SHALL fail with a
         * pre-check result of `NOT_SUPPORTED`.</blockquote></blockquote>
         * @name proto.CryptoService#deleteLiveHash
         * @type {proto.CryptoService.deleteLiveHash}
         */
        $Object.defineProperties(CryptoService.prototype.deleteLiveHash = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.deleteLiveHash, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "deleteLiveHash" },
            path: { value: "/proto.CryptoService/deleteLiveHash" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#getLiveHash}.
         * @memberof proto.CryptoService
         * @typedef getLiveHashCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve a livehash for an account
         * <blockquote>Important<blockquote>
         * This query is obsolete, not supported, and SHALL fail with a pre-check
         * result of `NOT_SUPPORTED`.</blockquote></blockquote>
         * @memberof proto.CryptoService
         * @typedef getLiveHash
         * @type {{
         *   (request: proto.IQuery, callback: proto.CryptoService.getLiveHashCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getLiveHash";
         *   readonly path: "/proto.CryptoService/getLiveHash";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve a livehash for an account
         * <blockquote>Important<blockquote>
         * This query is obsolete, not supported, and SHALL fail with a pre-check
         * result of `NOT_SUPPORTED`.</blockquote></blockquote>
         * @name proto.CryptoService#getLiveHash
         * @type {proto.CryptoService.getLiveHash}
         */
        $Object.defineProperties(CryptoService.prototype.getLiveHash = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.getLiveHash, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getLiveHash" },
            path: { value: "/proto.CryptoService/getLiveHash" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#getAccountRecords}.
         * @memberof proto.CryptoService
         * @typedef getAccountRecordsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Return all transactions in the last 180s of consensus time for which
         * the given account was the effective payer **and** network property
         * `ledger.keepRecordsInState` was `true`.
         * @memberof proto.CryptoService
         * @typedef getAccountRecords
         * @type {{
         *   (request: proto.IQuery, callback: proto.CryptoService.getAccountRecordsCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getAccountRecords";
         *   readonly path: "/proto.CryptoService/getAccountRecords";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Return all transactions in the last 180s of consensus time for which
         * the given account was the effective payer **and** network property
         * `ledger.keepRecordsInState` was `true`.
         * @name proto.CryptoService#getAccountRecords
         * @type {proto.CryptoService.getAccountRecords}
         */
        $Object.defineProperties(CryptoService.prototype.getAccountRecords = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.getAccountRecords, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getAccountRecords" },
            path: { value: "/proto.CryptoService/getAccountRecords" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#cryptoGetBalance}.
         * @memberof proto.CryptoService
         * @typedef cryptoGetBalanceCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve the balance of an account
         * @memberof proto.CryptoService
         * @typedef cryptoGetBalance
         * @type {{
         *   (request: proto.IQuery, callback: proto.CryptoService.cryptoGetBalanceCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "cryptoGetBalance";
         *   readonly path: "/proto.CryptoService/cryptoGetBalance";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve the balance of an account
         * @name proto.CryptoService#cryptoGetBalance
         * @type {proto.CryptoService.cryptoGetBalance}
         */
        $Object.defineProperties(CryptoService.prototype.cryptoGetBalance = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.cryptoGetBalance, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "cryptoGetBalance" },
            path: { value: "/proto.CryptoService/cryptoGetBalance" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#getAccountInfo}.
         * @memberof proto.CryptoService
         * @typedef getAccountInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve the metadata of an account
         * @memberof proto.CryptoService
         * @typedef getAccountInfo
         * @type {{
         *   (request: proto.IQuery, callback: proto.CryptoService.getAccountInfoCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getAccountInfo";
         *   readonly path: "/proto.CryptoService/getAccountInfo";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve the metadata of an account
         * @name proto.CryptoService#getAccountInfo
         * @type {proto.CryptoService.getAccountInfo}
         */
        $Object.defineProperties(CryptoService.prototype.getAccountInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.getAccountInfo, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getAccountInfo" },
            path: { value: "/proto.CryptoService/getAccountInfo" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#getTransactionReceipts}.
         * @memberof proto.CryptoService
         * @typedef getTransactionReceiptsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve the latest receipt for a transaction that is either awaiting
         * consensus, or reached consensus in the last 180 seconds
         * @memberof proto.CryptoService
         * @typedef getTransactionReceipts
         * @type {{
         *   (request: proto.IQuery, callback: proto.CryptoService.getTransactionReceiptsCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getTransactionReceipts";
         *   readonly path: "/proto.CryptoService/getTransactionReceipts";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve the latest receipt for a transaction that is either awaiting
         * consensus, or reached consensus in the last 180 seconds
         * @name proto.CryptoService#getTransactionReceipts
         * @type {proto.CryptoService.getTransactionReceipts}
         */
        $Object.defineProperties(CryptoService.prototype.getTransactionReceipts = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.getTransactionReceipts, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getTransactionReceipts" },
            path: { value: "/proto.CryptoService/getTransactionReceipts" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.CryptoService#getTxRecordByTxID}.
         * @memberof proto.CryptoService
         * @typedef getTxRecordByTxIDCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve the record of a transaction that is either awaiting consensus,
         * or reached consensus in the last 180 seconds
         * @memberof proto.CryptoService
         * @typedef getTxRecordByTxID
         * @type {{
         *   (request: proto.IQuery, callback: proto.CryptoService.getTxRecordByTxIDCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getTxRecordByTxID";
         *   readonly path: "/proto.CryptoService/getTxRecordByTxID";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve the record of a transaction that is either awaiting consensus,
         * or reached consensus in the last 180 seconds
         * @name proto.CryptoService#getTxRecordByTxID
         * @type {proto.CryptoService.getTxRecordByTxID}
         */
        $Object.defineProperties(CryptoService.prototype.getTxRecordByTxID = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, CryptoService.prototype.getTxRecordByTxID, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getTxRecordByTxID" },
            path: { value: "/proto.CryptoService/getTxRecordByTxID" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return CryptoService;
    })();

    proto.FileService = (function() {

        /**
         * Constructs a new FileService service.
         * @memberof proto
         * @classdesc Service gRPC definitions for the Hedera File Service (HFS).
         * 
         * #### Signature Requirements
         * The HFS manages file authorization differently, depending on type of file
         * transaction, and this can be surprising.<br/>
         * The core element of file authorization is the `keys` field,
         * which is a `KeyList`; a list of individual `Key` messages, each of which
         * may represent a simple or complex key.<br/>
         * The file service transactions treat this list differently.<br/>
         * A `fileCreate`, `fileAppend`, or `fileUpdate` MUST have a valid signature
         * from _each_ key in the list.<br/>
         * A `fileDelete` MUST have a valid signature from _at least one_ key in
         * the list. This is different, and allows a file "owned" by many entities
         * to be deleted by any one of those entities. A deleted file cannot be
         * restored, so it is important to consider this when assigning keys for
         * a file.<br/>
         * If any of the keys in a `KeyList` are complex, the full requirements of
         * each complex key must be met to count as a "valid signature" for that key.
         * A complex key structure (i.e. a `ThresholdKey`, or `KeyList`, possibly
         * including additional `ThresholdKey` or `KeyList` descendants) may be
         * assigned as the sole entry in a file `keys` field to ensure all transactions
         * have the same signature requirements.
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const FileService = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        $Object.defineProperty(FileService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: FileService, writable: true, enumerable: false, configurable: true });

        /**
         * Creates new FileService service using the specified rpc implementation.
         * @function create
         * @memberof proto.FileService
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {FileService} RPC service. Useful where requests and/or responses are streamed.
         */
        FileService.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link proto.FileService#createFile}.
         * @memberof proto.FileService
         * @typedef createFileCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Create a file in HFS.
         * @memberof proto.FileService
         * @typedef createFile
         * @type {{
         *   (request: proto.ITransaction, callback: proto.FileService.createFileCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "createFile";
         *   readonly path: "/proto.FileService/createFile";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Create a file in HFS.
         * @name proto.FileService#createFile
         * @type {proto.FileService.createFile}
         */
        $Object.defineProperties(FileService.prototype.createFile = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.createFile, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "createFile" },
            path: { value: "/proto.FileService/createFile" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.FileService#updateFile}.
         * @memberof proto.FileService
         * @typedef updateFileCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Update a file in HFS.
         * @memberof proto.FileService
         * @typedef updateFile
         * @type {{
         *   (request: proto.ITransaction, callback: proto.FileService.updateFileCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "updateFile";
         *   readonly path: "/proto.FileService/updateFile";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Update a file in HFS.
         * @name proto.FileService#updateFile
         * @type {proto.FileService.updateFile}
         */
        $Object.defineProperties(FileService.prototype.updateFile = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.updateFile, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "updateFile" },
            path: { value: "/proto.FileService/updateFile" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.FileService#deleteFile}.
         * @memberof proto.FileService
         * @typedef deleteFileCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Delete a file in HFS.<br/>
         * The content of a file deleted in this manner is completely removed
         * from network state, but the file metadata remains.
         * @memberof proto.FileService
         * @typedef deleteFile
         * @type {{
         *   (request: proto.ITransaction, callback: proto.FileService.deleteFileCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "deleteFile";
         *   readonly path: "/proto.FileService/deleteFile";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Delete a file in HFS.<br/>
         * The content of a file deleted in this manner is completely removed
         * from network state, but the file metadata remains.
         * @name proto.FileService#deleteFile
         * @type {proto.FileService.deleteFile}
         */
        $Object.defineProperties(FileService.prototype.deleteFile = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.deleteFile, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "deleteFile" },
            path: { value: "/proto.FileService/deleteFile" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.FileService#appendContent}.
         * @memberof proto.FileService
         * @typedef appendContentCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Append content to a file in HFS.
         * @memberof proto.FileService
         * @typedef appendContent
         * @type {{
         *   (request: proto.ITransaction, callback: proto.FileService.appendContentCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "appendContent";
         *   readonly path: "/proto.FileService/appendContent";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Append content to a file in HFS.
         * @name proto.FileService#appendContent
         * @type {proto.FileService.appendContent}
         */
        $Object.defineProperties(FileService.prototype.appendContent = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.appendContent, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "appendContent" },
            path: { value: "/proto.FileService/appendContent" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.FileService#getFileContent}.
         * @memberof proto.FileService
         * @typedef getFileContentCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve the content of a file in HFS.<br/>
         * Note that this query retrieves _only_ the file content, not any of
         * the metadata for the file.
         * @memberof proto.FileService
         * @typedef getFileContent
         * @type {{
         *   (request: proto.IQuery, callback: proto.FileService.getFileContentCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getFileContent";
         *   readonly path: "/proto.FileService/getFileContent";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve the content of a file in HFS.<br/>
         * Note that this query retrieves _only_ the file content, not any of
         * the metadata for the file.
         * @name proto.FileService#getFileContent
         * @type {proto.FileService.getFileContent}
         */
        $Object.defineProperties(FileService.prototype.getFileContent = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.getFileContent, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getFileContent" },
            path: { value: "/proto.FileService/getFileContent" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.FileService#getFileInfo}.
         * @memberof proto.FileService
         * @typedef getFileInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve the metadata for a file in HFS.<br/>
         * Note that this query does not retrieve the file _content_.
         * @memberof proto.FileService
         * @typedef getFileInfo
         * @type {{
         *   (request: proto.IQuery, callback: proto.FileService.getFileInfoCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getFileInfo";
         *   readonly path: "/proto.FileService/getFileInfo";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve the metadata for a file in HFS.<br/>
         * Note that this query does not retrieve the file _content_.
         * @name proto.FileService#getFileInfo
         * @type {proto.FileService.getFileInfo}
         */
        $Object.defineProperties(FileService.prototype.getFileInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.getFileInfo, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getFileInfo" },
            path: { value: "/proto.FileService/getFileInfo" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.FileService#systemDelete}.
         * @memberof proto.FileService
         * @typedef systemDeleteCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Delete a "regular" file without "owner" authorization.<br/>
         * This transaction _does not_ require signatures for the keys in
         * the file `keys` list, but must be signed by a "privileged" account.
         * <p>
         * This transaction SHALL NOT accept a file identifier for
         * a "system" file.<br/>
         * This transaction SHALL NOT remove the _content_ of the file from state.
         * This permits use of the `systemUndelete` to reverse this action if
         * performed in error.
         * <p>
         * This is a privileged transaction, and only accounts 2-59 are permitted
         * to call this function, by default. The actual restriction is in the
         * `api-permission.properties` file in the consensus node configuration.
         * @memberof proto.FileService
         * @typedef systemDelete
         * @type {{
         *   (request: proto.ITransaction, callback: proto.FileService.systemDeleteCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "systemDelete";
         *   readonly path: "/proto.FileService/systemDelete";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Delete a "regular" file without "owner" authorization.<br/>
         * This transaction _does not_ require signatures for the keys in
         * the file `keys` list, but must be signed by a "privileged" account.
         * <p>
         * This transaction SHALL NOT accept a file identifier for
         * a "system" file.<br/>
         * This transaction SHALL NOT remove the _content_ of the file from state.
         * This permits use of the `systemUndelete` to reverse this action if
         * performed in error.
         * <p>
         * This is a privileged transaction, and only accounts 2-59 are permitted
         * to call this function, by default. The actual restriction is in the
         * `api-permission.properties` file in the consensus node configuration.
         * @name proto.FileService#systemDelete
         * @type {proto.FileService.systemDelete}
         */
        $Object.defineProperties(FileService.prototype.systemDelete = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.systemDelete, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "systemDelete" },
            path: { value: "/proto.FileService/systemDelete" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.FileService#systemUndelete}.
         * @memberof proto.FileService
         * @typedef systemUndeleteCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Undelete a "regular" file.
         * This transaction must be signed by a "privileged" account.<br/>
         * <p>
         * This transaction SHALL NOT accept a file identifier for
         * a "system" file.<br/>
         * The file identified SHOULD have been previously deleted.<br/>
         * This transaction SHALL NOT recover the _content_ of a file unless that
         * file was deleted with a `systemDelete` transaction. The _content_ of a
         * file deleted with a `fileDelete` transaction is not retained in state.
         * <p>
         * This is a privileged transaction, and only accounts 2-60 are permitted
         * to call this function, by default. The actual restriction is in the
         * `api-permission.properties` file in the consensus node configuration.
         * @memberof proto.FileService
         * @typedef systemUndelete
         * @type {{
         *   (request: proto.ITransaction, callback: proto.FileService.systemUndeleteCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "systemUndelete";
         *   readonly path: "/proto.FileService/systemUndelete";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Undelete a "regular" file.
         * This transaction must be signed by a "privileged" account.<br/>
         * <p>
         * This transaction SHALL NOT accept a file identifier for
         * a "system" file.<br/>
         * The file identified SHOULD have been previously deleted.<br/>
         * This transaction SHALL NOT recover the _content_ of a file unless that
         * file was deleted with a `systemDelete` transaction. The _content_ of a
         * file deleted with a `fileDelete` transaction is not retained in state.
         * <p>
         * This is a privileged transaction, and only accounts 2-60 are permitted
         * to call this function, by default. The actual restriction is in the
         * `api-permission.properties` file in the consensus node configuration.
         * @name proto.FileService#systemUndelete
         * @type {proto.FileService.systemUndelete}
         */
        $Object.defineProperties(FileService.prototype.systemUndelete = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.systemUndelete, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "systemUndelete" },
            path: { value: "/proto.FileService/systemUndelete" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return FileService;
    })();

    proto.FreezeService = (function() {

        /**
         * Constructs a new FreezeService service.
         * @memberof proto
         * @classdesc A service to manage network "freeze" events.
         * 
         * This service provides a facility to prepare for network upgrades, halt network processing,
         * perform network software upgrades, and automatically restart the network following an upgrade.
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const FreezeService = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        $Object.defineProperty(FreezeService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: FreezeService, writable: true, enumerable: false, configurable: true });

        /**
         * Creates new FreezeService service using the specified rpc implementation.
         * @function create
         * @memberof proto.FreezeService
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {FreezeService} RPC service. Useful where requests and/or responses are streamed.
         */
        FreezeService.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link proto.FreezeService#freeze}.
         * @memberof proto.FreezeService
         * @typedef freezeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Freeze, cancel, or prepare a freeze.
         * This single transaction performs all of the functions supported
         * by the network freeze service. These functions include actions to
         * prepare an upgrade, prepare a telemetry upgrade, freeze the network,
         * freeze the network for upgrade, or abort a scheduled freeze.
         * <p>
         * The actual freeze action SHALL be determined by the `freeze_type` field
         * of the `FreezeTransactionBody`.<br/>
         * The transaction body MUST be a `FreezeTransactionBody`.
         * @memberof proto.FreezeService
         * @typedef freeze
         * @type {{
         *   (request: proto.ITransaction, callback: proto.FreezeService.freezeCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "freeze";
         *   readonly path: "/proto.FreezeService/freeze";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Freeze, cancel, or prepare a freeze.
         * This single transaction performs all of the functions supported
         * by the network freeze service. These functions include actions to
         * prepare an upgrade, prepare a telemetry upgrade, freeze the network,
         * freeze the network for upgrade, or abort a scheduled freeze.
         * <p>
         * The actual freeze action SHALL be determined by the `freeze_type` field
         * of the `FreezeTransactionBody`.<br/>
         * The transaction body MUST be a `FreezeTransactionBody`.
         * @name proto.FreezeService#freeze
         * @type {proto.FreezeService.freeze}
         */
        $Object.defineProperties(FreezeService.prototype.freeze = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, FreezeService.prototype.freeze, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "freeze" },
            path: { value: "/proto.FreezeService/freeze" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return FreezeService;
    })();

    proto.NetworkService = (function() {

        /**
         * Constructs a new NetworkService service.
         * @memberof proto
         * @classdesc Basic "network information" queries.
         * 
         * This service supports queries for the active services and API versions,
         * and a query for account details.
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const NetworkService = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        $Object.defineProperty(NetworkService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: NetworkService, writable: true, enumerable: false, configurable: true });

        /**
         * Creates new NetworkService service using the specified rpc implementation.
         * @function create
         * @memberof proto.NetworkService
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {NetworkService} RPC service. Useful where requests and/or responses are streamed.
         */
        NetworkService.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link proto.NetworkService#getVersionInfo}.
         * @memberof proto.NetworkService
         * @typedef getVersionInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve the active versions of Hedera Services and API messages.
         * @memberof proto.NetworkService
         * @typedef getVersionInfo
         * @type {{
         *   (request: proto.IQuery, callback: proto.NetworkService.getVersionInfoCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getVersionInfo";
         *   readonly path: "/proto.NetworkService/getVersionInfo";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve the active versions of Hedera Services and API messages.
         * @name proto.NetworkService#getVersionInfo
         * @type {proto.NetworkService.getVersionInfo}
         */
        $Object.defineProperties(NetworkService.prototype.getVersionInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, NetworkService.prototype.getVersionInfo, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getVersionInfo" },
            path: { value: "/proto.NetworkService/getVersionInfo" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.NetworkService#getAccountDetails}.
         * @memberof proto.NetworkService
         * @typedef getAccountDetailsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Request detail information about an account.
         * <p>
         * The returned information SHALL include balance and allowances.<br/>
         * The returned information SHALL NOT include a list of account records.
         * @memberof proto.NetworkService
         * @typedef getAccountDetails
         * @type {{
         *   (request: proto.IQuery, callback: proto.NetworkService.getAccountDetailsCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getAccountDetails";
         *   readonly path: "/proto.NetworkService/getAccountDetails";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Request detail information about an account.
         * <p>
         * The returned information SHALL include balance and allowances.<br/>
         * The returned information SHALL NOT include a list of account records.
         * @name proto.NetworkService#getAccountDetails
         * @type {proto.NetworkService.getAccountDetails}
         */
        $Object.defineProperties(NetworkService.prototype.getAccountDetails = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, NetworkService.prototype.getAccountDetails, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getAccountDetails" },
            path: { value: "/proto.NetworkService/getAccountDetails" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.NetworkService#getExecutionTime}.
         * @memberof proto.NetworkService
         * @typedef getExecutionTimeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve the time, in nanoseconds, spent in direct processing for one or
         * more recent transactions.
         * <p>
         * For each transaction identifier provided, if that transaction is
         * sufficiently recent (that is, it is within the range of the
         * configuration value `stats.executionTimesToTrack`), the node SHALL
         * return the time, in nanoseconds, spent to directly process that
         * transaction (that is, excluding time to reach consensus).<br/>
         * Note that because each node processes every transaction for the Hedera
         * network, this query MAY be sent to any node.
         * <p>
         * <blockquote>Important<blockquote>
         * This query is obsolete, not supported, and SHALL fail with a pre-check
         * result of `NOT_SUPPORTED`.</blockquote></blockquote>
         * @memberof proto.NetworkService
         * @typedef getExecutionTime
         * @type {{
         *   (request: proto.IQuery, callback: proto.NetworkService.getExecutionTimeCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getExecutionTime";
         *   readonly path: "/proto.NetworkService/getExecutionTime";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve the time, in nanoseconds, spent in direct processing for one or
         * more recent transactions.
         * <p>
         * For each transaction identifier provided, if that transaction is
         * sufficiently recent (that is, it is within the range of the
         * configuration value `stats.executionTimesToTrack`), the node SHALL
         * return the time, in nanoseconds, spent to directly process that
         * transaction (that is, excluding time to reach consensus).<br/>
         * Note that because each node processes every transaction for the Hedera
         * network, this query MAY be sent to any node.
         * <p>
         * <blockquote>Important<blockquote>
         * This query is obsolete, not supported, and SHALL fail with a pre-check
         * result of `NOT_SUPPORTED`.</blockquote></blockquote>
         * @name proto.NetworkService#getExecutionTime
         * @type {proto.NetworkService.getExecutionTime}
         */
        $Object.defineProperties(NetworkService.prototype.getExecutionTime = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, NetworkService.prototype.getExecutionTime, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getExecutionTime" },
            path: { value: "/proto.NetworkService/getExecutionTime" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.NetworkService#uncheckedSubmit}.
         * @memberof proto.NetworkService
         * @typedef uncheckedSubmitCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Submit a transaction that wraps another transaction which will
         * skip most validation.
         * <p>
         * <blockquote>Important<blockquote>
         * This query is obsolete, not supported, and SHALL fail with a pre-check
         * result of `NOT_SUPPORTED`.
         * </blockquote></blockquote>
         * @memberof proto.NetworkService
         * @typedef uncheckedSubmit
         * @type {{
         *   (request: proto.ITransaction, callback: proto.NetworkService.uncheckedSubmitCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "uncheckedSubmit";
         *   readonly path: "/proto.NetworkService/uncheckedSubmit";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Submit a transaction that wraps another transaction which will
         * skip most validation.
         * <p>
         * <blockquote>Important<blockquote>
         * This query is obsolete, not supported, and SHALL fail with a pre-check
         * result of `NOT_SUPPORTED`.
         * </blockquote></blockquote>
         * @name proto.NetworkService#uncheckedSubmit
         * @type {proto.NetworkService.uncheckedSubmit}
         */
        $Object.defineProperties(NetworkService.prototype.uncheckedSubmit = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, NetworkService.prototype.uncheckedSubmit, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "uncheckedSubmit" },
            path: { value: "/proto.NetworkService/uncheckedSubmit" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return NetworkService;
    })();

    proto.ScheduleService = (function() {

        /**
         * Constructs a new ScheduleService service.
         * @memberof proto
         * @classdesc Transactions and queries for the Schedule Service.<br/>
         * The Schedule Service enables transactions to be submitted without all
         * required signatures and offers a `scheduleSign` transaction to provide
         * additional signatures independently after the schedule is created. The
         * scheduled transaction may be executed immediately when all required
         * signatures are present, or at expiration if "long term" schedules
         * are enabled in network configuration.
         * 
         * ### Execution
         * Scheduled transactions SHALL be executed under the following conditions.
         * 1. If "long term" schedules are enabled and `wait_for_expiry` is set for
         * that schedule then the transaction SHALL NOT be executed before the
         * network consensus time matches or exceeds the `expiration_time` field
         * for that schedule.
         * 1. If "long term" schedules are enabled and `wait_for_expiry` is _not_ set
         * for that schedule, then the transaction SHALL be executed when all
         * signatures required by the scheduled transaction are active for that
         * schedule. This MAY be immediately after the `scheduleCreate` or a
         * subsequent `scheduleSign` transaction, or MAY be at expiration if
         * the signature requirements are met at that time.
         * 1. If "long term" schedules are _disabled_, then the scheduled transaction
         * SHALL be executed immediately after all signature requirements for the
         * scheduled transaction are met during the `scheduleCreate` or a subsequent
         * `scheduleSign` transaction. The scheduled transaction SHALL NOT be
         * on expiration when "long term" schedules are disabled.
         * 
         * A schedule SHALL remain in state and MAY be queried with a `getScheduleInfo`
         * transaction after execution, until the schedule expires.<br/>
         * When network consensus time matches or exceeds the `expiration_time` for
         * a schedule, that schedule SHALL be removed from state, whether it has
         * executed or not.<br/>
         * If "long term" schedules are _disabled_, the maximum expiration time SHALL
         * be the consensus time of the `scheduleCreate` transaction extended by
         * the network configuration value `ledger.scheduleTxExpiryTimeSecs`.
         * 
         * ### Block Stream Effects
         * When a scheduled transaction is executed, the timestamp in the transaction
         * identifier for that transaction SHALL be 1 nanosecond after the consensus
         * timestamp for the transaction that resulted in its execution. If execution
         * occurred at expiration, that transaction may be almost any transaction,
         * including another scheduled transaction that executed at expiration.<br/>
         * The transaction identifier for a scheduled transaction that is executed
         * SHALL have the `scheduled` flag set and SHALL inherit the `accountID` and
         * `transactionValidStart` values from the `scheduleCreate` that created the
         * schedule.<br/>
         * The `scheduleRef` property of the record for a scheduled transaction SHALL
         * be populated with the schedule identifier of the schedule that executed.
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const ScheduleService = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        $Object.defineProperty(ScheduleService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: ScheduleService, writable: true, enumerable: false, configurable: true });

        /**
         * Creates new ScheduleService service using the specified rpc implementation.
         * @function create
         * @memberof proto.ScheduleService
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {ScheduleService} RPC service. Useful where requests and/or responses are streamed.
         */
        ScheduleService.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link proto.ScheduleService#createSchedule}.
         * @memberof proto.ScheduleService
         * @typedef createScheduleCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Create a new Schedule.
         * <p>
         * If all signature requirements are met with this transaction, the
         * scheduled transaction MAY execute immediately.
         * @memberof proto.ScheduleService
         * @typedef createSchedule
         * @type {{
         *   (request: proto.ITransaction, callback: proto.ScheduleService.createScheduleCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "createSchedule";
         *   readonly path: "/proto.ScheduleService/createSchedule";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Create a new Schedule.
         * <p>
         * If all signature requirements are met with this transaction, the
         * scheduled transaction MAY execute immediately.
         * @name proto.ScheduleService#createSchedule
         * @type {proto.ScheduleService.createSchedule}
         */
        $Object.defineProperties(ScheduleService.prototype.createSchedule = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, ScheduleService.prototype.createSchedule, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "createSchedule" },
            path: { value: "/proto.ScheduleService/createSchedule" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.ScheduleService#signSchedule}.
         * @memberof proto.ScheduleService
         * @typedef signScheduleCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Add signatures to an existing schedule.
         * <p>
         * Signatures on this transaction SHALL be added to the set of active
         * signatures on the schedule, and MAY result in execution of the
         * scheduled transaction if all signature requirements are met.
         * @memberof proto.ScheduleService
         * @typedef signSchedule
         * @type {{
         *   (request: proto.ITransaction, callback: proto.ScheduleService.signScheduleCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "signSchedule";
         *   readonly path: "/proto.ScheduleService/signSchedule";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Add signatures to an existing schedule.
         * <p>
         * Signatures on this transaction SHALL be added to the set of active
         * signatures on the schedule, and MAY result in execution of the
         * scheduled transaction if all signature requirements are met.
         * @name proto.ScheduleService#signSchedule
         * @type {proto.ScheduleService.signSchedule}
         */
        $Object.defineProperties(ScheduleService.prototype.signSchedule = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, ScheduleService.prototype.signSchedule, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "signSchedule" },
            path: { value: "/proto.ScheduleService/signSchedule" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.ScheduleService#deleteSchedule}.
         * @memberof proto.ScheduleService
         * @typedef deleteScheduleCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Mark an existing schedule deleted.
         * <p>
         * Once deleted a schedule SHALL NOT be executed and any subsequent
         * `scheduleSign` transaction SHALL fail.
         * @memberof proto.ScheduleService
         * @typedef deleteSchedule
         * @type {{
         *   (request: proto.ITransaction, callback: proto.ScheduleService.deleteScheduleCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "deleteSchedule";
         *   readonly path: "/proto.ScheduleService/deleteSchedule";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Mark an existing schedule deleted.
         * <p>
         * Once deleted a schedule SHALL NOT be executed and any subsequent
         * `scheduleSign` transaction SHALL fail.
         * @name proto.ScheduleService#deleteSchedule
         * @type {proto.ScheduleService.deleteSchedule}
         */
        $Object.defineProperties(ScheduleService.prototype.deleteSchedule = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, ScheduleService.prototype.deleteSchedule, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "deleteSchedule" },
            path: { value: "/proto.ScheduleService/deleteSchedule" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.ScheduleService#getScheduleInfo}.
         * @memberof proto.ScheduleService
         * @typedef getScheduleInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve the metadata for a schedule.
         * @memberof proto.ScheduleService
         * @typedef getScheduleInfo
         * @type {{
         *   (request: proto.IQuery, callback: proto.ScheduleService.getScheduleInfoCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getScheduleInfo";
         *   readonly path: "/proto.ScheduleService/getScheduleInfo";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve the metadata for a schedule.
         * @name proto.ScheduleService#getScheduleInfo
         * @type {proto.ScheduleService.getScheduleInfo}
         */
        $Object.defineProperties(ScheduleService.prototype.getScheduleInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, ScheduleService.prototype.getScheduleInfo, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getScheduleInfo" },
            path: { value: "/proto.ScheduleService/getScheduleInfo" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return ScheduleService;
    })();

    proto.SmartContractService = (function() {

        /**
         * Constructs a new SmartContractService service.
         * @memberof proto
         * @classdesc The Hedera Smart Contract Service (HSCS) provides an interface to an EVM
         * compatible environment to create, store, manage, and execute smart contract
         * calls. Smart Contracts implement useful and often highly complex
         * interactions between individuals, systems, and the distributed ledger.
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const SmartContractService = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        $Object.defineProperty(SmartContractService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: SmartContractService, writable: true, enumerable: false, configurable: true });

        /**
         * Creates new SmartContractService service using the specified rpc implementation.
         * @function create
         * @memberof proto.SmartContractService
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {SmartContractService} RPC service. Useful where requests and/or responses are streamed.
         */
        SmartContractService.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link proto.SmartContractService#createContract}.
         * @memberof proto.SmartContractService
         * @typedef createContractCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Create a new smart contract.
         * <p>
         * If this transaction succeeds, the `ContractID` for the new smart
         * contract SHALL be set in the transaction receipt.<br/>
         * The contract is defined by the initial bytecode (or `initcode`).
         * The `initcode` SHALL be provided either in a previously created file,
         * or in the transaction body itself for very small contracts.<br/>
         * As part of contract creation, the constructor defined for the new
         * smart contract SHALL run with the parameters provided in
         * the `constructorParameters` field.<br/>
         * The gas to "power" that constructor MUST be provided via the `gas`
         * field, and SHALL be charged to the payer for this transaction.<br/>
         * If the contract _constructor_ stores information, it is charged gas for
         * that storage. There is a separate fee in HBAR to maintain that storage
         * until the expiration, and that fee SHALL be added to this transaction
         * as part of the _transaction fee_, rather than gas.
         * @memberof proto.SmartContractService
         * @typedef createContract
         * @type {{
         *   (request: proto.ITransaction, callback: proto.SmartContractService.createContractCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "createContract";
         *   readonly path: "/proto.SmartContractService/createContract";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Create a new smart contract.
         * <p>
         * If this transaction succeeds, the `ContractID` for the new smart
         * contract SHALL be set in the transaction receipt.<br/>
         * The contract is defined by the initial bytecode (or `initcode`).
         * The `initcode` SHALL be provided either in a previously created file,
         * or in the transaction body itself for very small contracts.<br/>
         * As part of contract creation, the constructor defined for the new
         * smart contract SHALL run with the parameters provided in
         * the `constructorParameters` field.<br/>
         * The gas to "power" that constructor MUST be provided via the `gas`
         * field, and SHALL be charged to the payer for this transaction.<br/>
         * If the contract _constructor_ stores information, it is charged gas for
         * that storage. There is a separate fee in HBAR to maintain that storage
         * until the expiration, and that fee SHALL be added to this transaction
         * as part of the _transaction fee_, rather than gas.
         * @name proto.SmartContractService#createContract
         * @type {proto.SmartContractService.createContract}
         */
        $Object.defineProperties(SmartContractService.prototype.createContract = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.createContract, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "createContract" },
            path: { value: "/proto.SmartContractService/createContract" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.SmartContractService#updateContract}.
         * @memberof proto.SmartContractService
         * @typedef updateContractCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Modify a smart contract.<br/>
         * Any change other than updating the expiration time requires that the
         * contract be modifiable (has a valid `adminKey`) and that the
         * transaction be signed by the `adminKey`
         * <p>
         * Fields _not set_ on the request SHALL NOT be modified.
         * @memberof proto.SmartContractService
         * @typedef updateContract
         * @type {{
         *   (request: proto.ITransaction, callback: proto.SmartContractService.updateContractCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "updateContract";
         *   readonly path: "/proto.SmartContractService/updateContract";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Modify a smart contract.<br/>
         * Any change other than updating the expiration time requires that the
         * contract be modifiable (has a valid `adminKey`) and that the
         * transaction be signed by the `adminKey`
         * <p>
         * Fields _not set_ on the request SHALL NOT be modified.
         * @name proto.SmartContractService#updateContract
         * @type {proto.SmartContractService.updateContract}
         */
        $Object.defineProperties(SmartContractService.prototype.updateContract = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.updateContract, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "updateContract" },
            path: { value: "/proto.SmartContractService/updateContract" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.SmartContractService#contractCallMethod}.
         * @memberof proto.SmartContractService
         * @typedef contractCallMethodCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Call a function of a given smart contract, providing function parameter
         * inputs as needed.
         * <p>
         * Resource ("gas") charges SHALL include all relevant fees incurred by
         * the contract execution, including any storage required.<br/>
         * The total transaction fee SHALL incorporate all of the "gas" actually
         * consumed as well as the standard fees for transaction handling,
         * data transfers, signature verification, etc...
         * @memberof proto.SmartContractService
         * @typedef contractCallMethod
         * @type {{
         *   (request: proto.ITransaction, callback: proto.SmartContractService.contractCallMethodCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "contractCallMethod";
         *   readonly path: "/proto.SmartContractService/contractCallMethod";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Call a function of a given smart contract, providing function parameter
         * inputs as needed.
         * <p>
         * Resource ("gas") charges SHALL include all relevant fees incurred by
         * the contract execution, including any storage required.<br/>
         * The total transaction fee SHALL incorporate all of the "gas" actually
         * consumed as well as the standard fees for transaction handling,
         * data transfers, signature verification, etc...
         * @name proto.SmartContractService#contractCallMethod
         * @type {proto.SmartContractService.contractCallMethod}
         */
        $Object.defineProperties(SmartContractService.prototype.contractCallMethod = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.contractCallMethod, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "contractCallMethod" },
            path: { value: "/proto.SmartContractService/contractCallMethod" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.SmartContractService#contractCallLocalMethod}.
         * @memberof proto.SmartContractService
         * @typedef contractCallLocalMethodCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Call a query function of a given smart contract, providing
         * function parameter inputs as needed.<br/>
         * This is performed locally on the particular node that the client is
         * communicating with. Executing the call locally is faster and less
         * costly, but imposes certain restrictions.
         * <p>
         * The call MUST NOT change the state of the contract instance. This also
         * precludes any expenditure or transfer of HBAR or other tokens.<br/>
         * The call SHALL NOT have a separate consensus timestamp.<br/>
         * The call SHALL NOT generate a record nor a receipt.<br/>
         * The response SHALL contain the output returned by the function call.<br/>
         * <p>
         * This is generally useful for calling accessor functions which read
         * (query) state without changes or side effects. Any contract call that
         * would use the `STATICCALL` opcode MAY be called via contract call local
         * with performance and cost benefits.
         * <p>
         * Unlike a ContractCall transaction, the node SHALL always consume the
         * _entire_ amount of offered "gas" in determining the fee for this query,
         * so accurate gas estimation is important.
         * @memberof proto.SmartContractService
         * @typedef contractCallLocalMethod
         * @type {{
         *   (request: proto.IQuery, callback: proto.SmartContractService.contractCallLocalMethodCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "contractCallLocalMethod";
         *   readonly path: "/proto.SmartContractService/contractCallLocalMethod";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Call a query function of a given smart contract, providing
         * function parameter inputs as needed.<br/>
         * This is performed locally on the particular node that the client is
         * communicating with. Executing the call locally is faster and less
         * costly, but imposes certain restrictions.
         * <p>
         * The call MUST NOT change the state of the contract instance. This also
         * precludes any expenditure or transfer of HBAR or other tokens.<br/>
         * The call SHALL NOT have a separate consensus timestamp.<br/>
         * The call SHALL NOT generate a record nor a receipt.<br/>
         * The response SHALL contain the output returned by the function call.<br/>
         * <p>
         * This is generally useful for calling accessor functions which read
         * (query) state without changes or side effects. Any contract call that
         * would use the `STATICCALL` opcode MAY be called via contract call local
         * with performance and cost benefits.
         * <p>
         * Unlike a ContractCall transaction, the node SHALL always consume the
         * _entire_ amount of offered "gas" in determining the fee for this query,
         * so accurate gas estimation is important.
         * @name proto.SmartContractService#contractCallLocalMethod
         * @type {proto.SmartContractService.contractCallLocalMethod}
         */
        $Object.defineProperties(SmartContractService.prototype.contractCallLocalMethod = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.contractCallLocalMethod, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "contractCallLocalMethod" },
            path: { value: "/proto.SmartContractService/contractCallLocalMethod" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.SmartContractService#getContractInfo}.
         * @memberof proto.SmartContractService
         * @typedef getContractInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * A standard query to obtain detailed information for a smart contract.
         * @memberof proto.SmartContractService
         * @typedef getContractInfo
         * @type {{
         *   (request: proto.IQuery, callback: proto.SmartContractService.getContractInfoCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getContractInfo";
         *   readonly path: "/proto.SmartContractService/getContractInfo";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * A standard query to obtain detailed information for a smart contract.
         * @name proto.SmartContractService#getContractInfo
         * @type {proto.SmartContractService.getContractInfo}
         */
        $Object.defineProperties(SmartContractService.prototype.getContractInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.getContractInfo, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getContractInfo" },
            path: { value: "/proto.SmartContractService/getContractInfo" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.SmartContractService#contractGetBytecode}.
         * @memberof proto.SmartContractService
         * @typedef ContractGetBytecodeCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * A standard query to read the current bytecode for a smart contract.
         * @memberof proto.SmartContractService
         * @typedef ContractGetBytecode
         * @type {{
         *   (request: proto.IQuery, callback: proto.SmartContractService.ContractGetBytecodeCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "ContractGetBytecode";
         *   readonly path: "/proto.SmartContractService/ContractGetBytecode";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * A standard query to read the current bytecode for a smart contract.
         * @name proto.SmartContractService#contractGetBytecode
         * @type {proto.SmartContractService.ContractGetBytecode}
         */
        $Object.defineProperties(SmartContractService.prototype.contractGetBytecode = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.contractGetBytecode, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "ContractGetBytecode" },
            path: { value: "/proto.SmartContractService/ContractGetBytecode" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.SmartContractService#getBySolidityID}.
         * @memberof proto.SmartContractService
         * @typedef getBySolidityIDCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * A standard query to obtain account and contract identifiers for a smart
         * contract, given the Solidity identifier for that contract.
         * @memberof proto.SmartContractService
         * @typedef getBySolidityID
         * @type {{
         *   (request: proto.IQuery, callback: proto.SmartContractService.getBySolidityIDCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getBySolidityID";
         *   readonly path: "/proto.SmartContractService/getBySolidityID";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * A standard query to obtain account and contract identifiers for a smart
         * contract, given the Solidity identifier for that contract.
         * @name proto.SmartContractService#getBySolidityID
         * @type {proto.SmartContractService.getBySolidityID}
         */
        $Object.defineProperties(SmartContractService.prototype.getBySolidityID = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.getBySolidityID, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getBySolidityID" },
            path: { value: "/proto.SmartContractService/getBySolidityID" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.SmartContractService#getTxRecordByContractID}.
         * @memberof proto.SmartContractService
         * @typedef getTxRecordByContractIDCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * <blockquote>This query is no longer supported.</blockquote>
         * This query always returned an empty record list.
         * @memberof proto.SmartContractService
         * @typedef getTxRecordByContractID
         * @type {{
         *   (request: proto.IQuery, callback: proto.SmartContractService.getTxRecordByContractIDCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getTxRecordByContractID";
         *   readonly path: "/proto.SmartContractService/getTxRecordByContractID";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * <blockquote>This query is no longer supported.</blockquote>
         * This query always returned an empty record list.
         * @name proto.SmartContractService#getTxRecordByContractID
         * @type {proto.SmartContractService.getTxRecordByContractID}
         */
        $Object.defineProperties(SmartContractService.prototype.getTxRecordByContractID = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.getTxRecordByContractID, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getTxRecordByContractID" },
            path: { value: "/proto.SmartContractService/getTxRecordByContractID" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.SmartContractService#deleteContract}.
         * @memberof proto.SmartContractService
         * @typedef deleteContractCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Delete a smart contract, and transfer any remaining HBAR balance
         * to a designated account.
         * <p>
         * If this call succeeds then all subsequent calls to that smart
         * contract SHALL fail.<br/>
         * @memberof proto.SmartContractService
         * @typedef deleteContract
         * @type {{
         *   (request: proto.ITransaction, callback: proto.SmartContractService.deleteContractCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "deleteContract";
         *   readonly path: "/proto.SmartContractService/deleteContract";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Delete a smart contract, and transfer any remaining HBAR balance
         * to a designated account.
         * <p>
         * If this call succeeds then all subsequent calls to that smart
         * contract SHALL fail.<br/>
         * @name proto.SmartContractService#deleteContract
         * @type {proto.SmartContractService.deleteContract}
         */
        $Object.defineProperties(SmartContractService.prototype.deleteContract = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.deleteContract, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "deleteContract" },
            path: { value: "/proto.SmartContractService/deleteContract" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.SmartContractService#systemDelete}.
         * @memberof proto.SmartContractService
         * @typedef systemDeleteCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Delete a smart contract, as a system-initiated deletion, this
         * SHALL NOT transfer balances.
         * <blockquote>
         * This call is an administrative function of the Hedera network, and
         * SHALL require network administration authorization.<br/>
         * This transaction MUST be signed by one of the network administration
         * accounts (typically `0.0.2` through `0.0.59`, as defined in the
         * `api-permission.properties` file).
         * </blockquote>
         * If this call succeeds then all subsequent calls to that smart
         * contract SHALL fail.<br/>
         * @memberof proto.SmartContractService
         * @typedef systemDelete
         * @type {{
         *   (request: proto.ITransaction, callback: proto.SmartContractService.systemDeleteCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "systemDelete";
         *   readonly path: "/proto.SmartContractService/systemDelete";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Delete a smart contract, as a system-initiated deletion, this
         * SHALL NOT transfer balances.
         * <blockquote>
         * This call is an administrative function of the Hedera network, and
         * SHALL require network administration authorization.<br/>
         * This transaction MUST be signed by one of the network administration
         * accounts (typically `0.0.2` through `0.0.59`, as defined in the
         * `api-permission.properties` file).
         * </blockquote>
         * If this call succeeds then all subsequent calls to that smart
         * contract SHALL fail.<br/>
         * @name proto.SmartContractService#systemDelete
         * @type {proto.SmartContractService.systemDelete}
         */
        $Object.defineProperties(SmartContractService.prototype.systemDelete = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.systemDelete, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "systemDelete" },
            path: { value: "/proto.SmartContractService/systemDelete" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.SmartContractService#systemUndelete}.
         * @memberof proto.SmartContractService
         * @typedef systemUndeleteCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Un-Delete a smart contract, returning it (mostly) to its state
         * prior to deletion.
         * <p>
         * The contract to be restored MUST have been deleted via `systemDelete`.
         * If the contract was deleted via `deleteContract`, it
         * SHALL NOT be recoverable.
         * <blockquote>
         * This call is an administrative function of the Hedera network, and
         * SHALL require network administration authorization.<br/>
         * This transaction MUST be signed by one of the network administration
         * accounts (typically `0.0.2` through `0.0.59`, as defined in the
         * `api-permission.properties` file).
         * </blockquote>
         * If this call succeeds then subsequent calls to that smart
         * contract MAY succeed.<br/>
         * @memberof proto.SmartContractService
         * @typedef systemUndelete
         * @type {{
         *   (request: proto.ITransaction, callback: proto.SmartContractService.systemUndeleteCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "systemUndelete";
         *   readonly path: "/proto.SmartContractService/systemUndelete";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Un-Delete a smart contract, returning it (mostly) to its state
         * prior to deletion.
         * <p>
         * The contract to be restored MUST have been deleted via `systemDelete`.
         * If the contract was deleted via `deleteContract`, it
         * SHALL NOT be recoverable.
         * <blockquote>
         * This call is an administrative function of the Hedera network, and
         * SHALL require network administration authorization.<br/>
         * This transaction MUST be signed by one of the network administration
         * accounts (typically `0.0.2` through `0.0.59`, as defined in the
         * `api-permission.properties` file).
         * </blockquote>
         * If this call succeeds then subsequent calls to that smart
         * contract MAY succeed.<br/>
         * @name proto.SmartContractService#systemUndelete
         * @type {proto.SmartContractService.systemUndelete}
         */
        $Object.defineProperties(SmartContractService.prototype.systemUndelete = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.systemUndelete, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "systemUndelete" },
            path: { value: "/proto.SmartContractService/systemUndelete" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.SmartContractService#callEthereum}.
         * @memberof proto.SmartContractService
         * @typedef callEthereumCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Make an Ethereum transaction "call" with all data in Ethereum formats,
         * including the contract alias.
         * <p>
         * Call data MAY be in the transaction, or stored within a "File".<br/>
         * The caller MAY offer additional gas above what is offered in the call
         * data, but MAY be charged up to 80% of that value if the amount required
         * is less than this "floor" amount.
         * @memberof proto.SmartContractService
         * @typedef callEthereum
         * @type {{
         *   (request: proto.ITransaction, callback: proto.SmartContractService.callEthereumCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "callEthereum";
         *   readonly path: "/proto.SmartContractService/callEthereum";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Make an Ethereum transaction "call" with all data in Ethereum formats,
         * including the contract alias.
         * <p>
         * Call data MAY be in the transaction, or stored within a "File".<br/>
         * The caller MAY offer additional gas above what is offered in the call
         * data, but MAY be charged up to 80% of that value if the amount required
         * is less than this "floor" amount.
         * @name proto.SmartContractService#callEthereum
         * @type {proto.SmartContractService.callEthereum}
         */
        $Object.defineProperties(SmartContractService.prototype.callEthereum = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.callEthereum, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "callEthereum" },
            path: { value: "/proto.SmartContractService/callEthereum" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.SmartContractService#hookStore}.
         * @memberof proto.SmartContractService
         * @typedef hookStoreCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Update zero or more slots of an EVM hook's storage.
         * @memberof proto.SmartContractService
         * @typedef hookStore
         * @type {{
         *   (request: proto.ITransaction, callback: proto.SmartContractService.hookStoreCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "hookStore";
         *   readonly path: "/proto.SmartContractService/hookStore";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Update zero or more slots of an EVM hook's storage.
         * @name proto.SmartContractService#hookStore
         * @type {proto.SmartContractService.hookStore}
         */
        $Object.defineProperties(SmartContractService.prototype.hookStore = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, SmartContractService.prototype.hookStore, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "hookStore" },
            path: { value: "/proto.SmartContractService/hookStore" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return SmartContractService;
    })();

    proto.NodeMigrationRootHashVote = (function() {

        /**
         * Properties of a NodeMigrationRootHashVote.
         * @typedef {Object} proto.NodeMigrationRootHashVote.$Properties
         * @property {com.hedera.hapi.platform.state.NodeId.$Properties|null} [nodeId] The node submitting this vote.
         * @property {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties|null} [vote] The submitted vote body.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NodeMigrationRootHashVote.
         * @memberof proto
         * @interface INodeMigrationRootHashVote
         * @augments proto.NodeMigrationRootHashVote.$Properties
         * @deprecated Use proto.NodeMigrationRootHashVote.$Properties instead.
         */

        /**
         * Shape of a NodeMigrationRootHashVote.
         * @typedef {proto.NodeMigrationRootHashVote.$Properties} proto.NodeMigrationRootHashVote.$Shape
         */

        /**
         * Constructs a new NodeMigrationRootHashVote.
         * @memberof proto
         * @classdesc A node vote for wrapped-record migration root-hash agreement.
         * @constructor
         * @param {proto.NodeMigrationRootHashVote.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NodeMigrationRootHashVote = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The node submitting this vote.
         * @member {com.hedera.hapi.platform.state.NodeId.$Properties|null|undefined} nodeId
         * @memberof proto.NodeMigrationRootHashVote
         * @instance
         */
        NodeMigrationRootHashVote.prototype.nodeId = null;

        /**
         * The submitted vote body.
         * @member {com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.$Properties|null|undefined} vote
         * @memberof proto.NodeMigrationRootHashVote
         * @instance
         */
        NodeMigrationRootHashVote.prototype.vote = null;

        /**
         * Creates a new NodeMigrationRootHashVote instance using the specified properties.
         * @function create
         * @memberof proto.NodeMigrationRootHashVote
         * @static
         * @param {proto.NodeMigrationRootHashVote.$Properties=} [properties] Properties to set
         * @returns {proto.NodeMigrationRootHashVote} NodeMigrationRootHashVote instance
         * @type {{
         *   (properties: proto.NodeMigrationRootHashVote.$Shape): proto.NodeMigrationRootHashVote & proto.NodeMigrationRootHashVote.$Shape;
         *   (properties?: proto.NodeMigrationRootHashVote.$Properties): proto.NodeMigrationRootHashVote;
         * }}
         */
        NodeMigrationRootHashVote.create = function(properties) {
            return new NodeMigrationRootHashVote(properties);
        };

        /**
         * Encodes the specified NodeMigrationRootHashVote message. Does not implicitly {@link proto.NodeMigrationRootHashVote.verify|verify} messages.
         * @function encode
         * @memberof proto.NodeMigrationRootHashVote
         * @static
         * @param {proto.NodeMigrationRootHashVote.$Properties} message NodeMigrationRootHashVote message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeMigrationRootHashVote.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                $root.com.hedera.hapi.platform.state.NodeId.encode(message.nodeId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.vote != null && $Object.hasOwnProperty.call(message, "vote"))
                $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.encode(message.vote, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeMigrationRootHashVote message, length delimited. Does not implicitly {@link proto.NodeMigrationRootHashVote.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NodeMigrationRootHashVote
         * @static
         * @param {proto.NodeMigrationRootHashVote.$Properties} message NodeMigrationRootHashVote message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeMigrationRootHashVote.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NodeMigrationRootHashVote message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NodeMigrationRootHashVote
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NodeMigrationRootHashVote & proto.NodeMigrationRootHashVote.$Shape} NodeMigrationRootHashVote
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeMigrationRootHashVote.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NodeMigrationRootHashVote();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.nodeId = $root.com.hedera.hapi.platform.state.NodeId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nodeId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.vote = $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.vote);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeMigrationRootHashVote message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NodeMigrationRootHashVote
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NodeMigrationRootHashVote & proto.NodeMigrationRootHashVote.$Shape} NodeMigrationRootHashVote
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeMigrationRootHashVote.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeMigrationRootHashVote message.
         * @function verify
         * @memberof proto.NodeMigrationRootHashVote
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeMigrationRootHashVote.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId")) {
                let error = $root.com.hedera.hapi.platform.state.NodeId.verify(message.nodeId, _depth + 1);
                if (error)
                    return "nodeId." + error;
            }
            if (message.vote != null && $Object.hasOwnProperty.call(message, "vote")) {
                let error = $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.verify(message.vote, _depth + 1);
                if (error)
                    return "vote." + error;
            }
            return null;
        };

        /**
         * Creates a NodeMigrationRootHashVote message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NodeMigrationRootHashVote
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NodeMigrationRootHashVote} NodeMigrationRootHashVote
         */
        NodeMigrationRootHashVote.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NodeMigrationRootHashVote)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NodeMigrationRootHashVote: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NodeMigrationRootHashVote();
            if (object.nodeId != null) {
                if (!$util.isObject(object.nodeId))
                    throw $TypeError(".proto.NodeMigrationRootHashVote.nodeId: object expected");
                message.nodeId = $root.com.hedera.hapi.platform.state.NodeId.fromObject(object.nodeId, _depth + 1);
            }
            if (object.vote != null) {
                if (!$util.isObject(object.vote))
                    throw $TypeError(".proto.NodeMigrationRootHashVote.vote: object expected");
                message.vote = $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.fromObject(object.vote, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a NodeMigrationRootHashVote message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NodeMigrationRootHashVote
         * @static
         * @param {proto.NodeMigrationRootHashVote} message NodeMigrationRootHashVote
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeMigrationRootHashVote.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.nodeId = null;
                object.vote = null;
            }
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                object.nodeId = $root.com.hedera.hapi.platform.state.NodeId.toObject(message.nodeId, options, _depth + 1);
            if (message.vote != null && $Object.hasOwnProperty.call(message, "vote"))
                object.vote = $root.com.hedera.hapi.services.auxiliary.blockrecords.MigrationRootHashVoteTransactionBody.toObject(message.vote, options, _depth + 1);
            return object;
        };

        /**
         * Converts this NodeMigrationRootHashVote to JSON.
         * @function toJSON
         * @memberof proto.NodeMigrationRootHashVote
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeMigrationRootHashVote.prototype.toJSON = function() {
            return NodeMigrationRootHashVote.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeMigrationRootHashVote
         * @function getTypeUrl
         * @memberof proto.NodeMigrationRootHashVote
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeMigrationRootHashVote.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NodeMigrationRootHashVote";
        };

        return NodeMigrationRootHashVote;
    })();

    proto.BlockInfo = (function() {

        /**
         * Properties of a BlockInfo.
         * @typedef {Object} proto.BlockInfo.$Properties
         * @property {number|Long|null} [lastBlockNumber] A block number.
         * <p>
         * The block number of the last completed immutable block.
         * @property {proto.Timestamp.$Properties|null} [firstConsTimeOfLastBlock] A consensus timestamp.
         * <p>
         * The consensus time of the first transaction for the last completed immutable block.
         * @property {Uint8Array|null} [blockHashes] A list of the last 256 block hashes.<br/>
         * This is the SHA384 48 byte hashes of the previous 256 blocks,
         * collected in a single byte array.
         * <p>
         * The first 48 bytes SHALL be the oldest block in the list.<br/>
         * The last 48 bytes SHALL be the newest block, which is the last fully
         * completed immutable block.<br/>
         * This SHALL contain less than 256 block hashes if there are less than 256
         * preceding blocks; for instance, shortly after network genesis the network
         * will not have processed 256 blocks yet.
         * <p>
         * This MAY change significantly for Block Stream (HIP TBD).
         * @property {proto.Timestamp.$Properties|null} [consTimeOfLastHandledTxn] A consensus timestamp.
         * <p>
         * The consensus time of the last transaction that was handled by the node
         * within the current block. Only top-level transactions can trigger stake
         * period side effects, so we track them separately from the last-used
         * time of <i>all</i> transactions, which include children.<br/>
         * @property {boolean|null} [migrationRecordsStreamed] A flag indicating that migration records have been published.
         * <p>
         * This property SHALL be `false` immediately following a node upgrade<br/>
         * This SHALL be set 'true' as migration records, if any, are published.
         * Migration records are typically published "during" the first transaction
         * handled by the node following startup.
         * @property {proto.Timestamp.$Properties|null} [firstConsTimeOfCurrentBlock] A consensus timestamp.
         * <p>
         * The consensus time of the first transaction in the current block;
         * necessary for reconnecting nodes to detect when the current block
         * is finished.
         * @property {proto.Timestamp.$Properties|null} [lastUsedConsTime] The consensus time of the last transaction that was handled by the node.
         * <p>
         * This property is how we 'advance the consensus clock'. The node MUST
         * continually set this property to the consensus timestamp for the most
         * recent transaction completed by the node.
         * @property {proto.Timestamp.$Properties|null} [lastIntervalProcessTime] The time stamp at which the last interval process was done.
         * <p>
         * This field SHALL hold the consensus time for the last time
         * at which an interval of time-dependent events were processed.
         * @property {Uint8Array|null} [previousWrappedRecordBlockRootHash] The root hash of the previous wrapped record block.
         * @property {Array.<Uint8Array>|null} [wrappedIntermediatePreviousBlockRootHashes] The intermediate hashes, calculated for all historical wrapped
         * record blocks, needed for subroot 2 in the block merkle
         * tree structure. These hashes SHALL include the minimum required
         * wrapped record block root hashes needed to construct subroot 2's
         * final state at the end of the previous block.
         * @property {number|Long|null} [wrappedIntermediateBlockRootsLeafCount] The number of leaves in the intermediate wrapped record block
         * roots subtree.
         * @property {boolean|null} [votingComplete] Whether migration root-hash voting has finalized.
         * @property {number|Long|null} [votingCompletionDeadlineBlockNumber] The block number at or after which voting is no longer pending.
         * @property {Array.<proto.NodeMigrationRootHashVote.$Properties>|null} [migrationRootHashVotes] Collected migration root-hash votes from nodes.
         * @property {Array.<com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Properties>|null} [migrationWrappedHashes] Wrapped hashes captured while voting is pending.
         * @property {boolean|null} [previewStreamOverwritten] A flag indicating that the preview `BlockStreamInfo` object has been
         * overwritten with the real wrapped record block hashes data from `BlockInfo`,
         * i.e. as part of the block streams cutover release.
         * <p>
         * This SHALL be set to `true` only after the schema overwrite logic
         * has run, to prevent any block streams cutover logic from repeating.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a BlockInfo.
         * @memberof proto
         * @interface IBlockInfo
         * @augments proto.BlockInfo.$Properties
         * @deprecated Use proto.BlockInfo.$Properties instead.
         */

        /**
         * Shape of a BlockInfo.
         * @typedef {proto.BlockInfo.$Properties} proto.BlockInfo.$Shape
         */

        /**
         * Constructs a new BlockInfo.
         * @memberof proto
         * @classdesc Information for a transaction block.
         * 
         * This includes:
         * - last block number.
         * - consensus times for:
         * - previous block start.
         * - current block start.
         * - last handled transaction.
         * - hash data for a rolling window of 256 blocks.
         * - whether migration records were produced.
         * @constructor
         * @param {proto.BlockInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const BlockInfo = function (properties) {
            this.wrappedIntermediatePreviousBlockRootHashes = [];
            this.migrationRootHashVotes = [];
            this.migrationWrappedHashes = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A block number.
         * <p>
         * The block number of the last completed immutable block.
         * @member {number|Long} lastBlockNumber
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.lastBlockNumber = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A consensus timestamp.
         * <p>
         * The consensus time of the first transaction for the last completed immutable block.
         * @member {proto.Timestamp.$Properties|null|undefined} firstConsTimeOfLastBlock
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.firstConsTimeOfLastBlock = null;

        /**
         * A list of the last 256 block hashes.<br/>
         * This is the SHA384 48 byte hashes of the previous 256 blocks,
         * collected in a single byte array.
         * <p>
         * The first 48 bytes SHALL be the oldest block in the list.<br/>
         * The last 48 bytes SHALL be the newest block, which is the last fully
         * completed immutable block.<br/>
         * This SHALL contain less than 256 block hashes if there are less than 256
         * preceding blocks; for instance, shortly after network genesis the network
         * will not have processed 256 blocks yet.
         * <p>
         * This MAY change significantly for Block Stream (HIP TBD).
         * @member {Uint8Array} blockHashes
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.blockHashes = $util.newBuffer([]);

        /**
         * A consensus timestamp.
         * <p>
         * The consensus time of the last transaction that was handled by the node
         * within the current block. Only top-level transactions can trigger stake
         * period side effects, so we track them separately from the last-used
         * time of <i>all</i> transactions, which include children.<br/>
         * @member {proto.Timestamp.$Properties|null|undefined} consTimeOfLastHandledTxn
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.consTimeOfLastHandledTxn = null;

        /**
         * A flag indicating that migration records have been published.
         * <p>
         * This property SHALL be `false` immediately following a node upgrade<br/>
         * This SHALL be set 'true' as migration records, if any, are published.
         * Migration records are typically published "during" the first transaction
         * handled by the node following startup.
         * @member {boolean} migrationRecordsStreamed
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.migrationRecordsStreamed = false;

        /**
         * A consensus timestamp.
         * <p>
         * The consensus time of the first transaction in the current block;
         * necessary for reconnecting nodes to detect when the current block
         * is finished.
         * @member {proto.Timestamp.$Properties|null|undefined} firstConsTimeOfCurrentBlock
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.firstConsTimeOfCurrentBlock = null;

        /**
         * The consensus time of the last transaction that was handled by the node.
         * <p>
         * This property is how we 'advance the consensus clock'. The node MUST
         * continually set this property to the consensus timestamp for the most
         * recent transaction completed by the node.
         * @member {proto.Timestamp.$Properties|null|undefined} lastUsedConsTime
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.lastUsedConsTime = null;

        /**
         * The time stamp at which the last interval process was done.
         * <p>
         * This field SHALL hold the consensus time for the last time
         * at which an interval of time-dependent events were processed.
         * @member {proto.Timestamp.$Properties|null|undefined} lastIntervalProcessTime
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.lastIntervalProcessTime = null;

        /**
         * The root hash of the previous wrapped record block.
         * @member {Uint8Array} previousWrappedRecordBlockRootHash
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.previousWrappedRecordBlockRootHash = $util.newBuffer([]);

        /**
         * The intermediate hashes, calculated for all historical wrapped
         * record blocks, needed for subroot 2 in the block merkle
         * tree structure. These hashes SHALL include the minimum required
         * wrapped record block root hashes needed to construct subroot 2's
         * final state at the end of the previous block.
         * @member {Array.<Uint8Array>} wrappedIntermediatePreviousBlockRootHashes
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.wrappedIntermediatePreviousBlockRootHashes = $util.emptyArray;

        /**
         * The number of leaves in the intermediate wrapped record block
         * roots subtree.
         * @member {number|Long} wrappedIntermediateBlockRootsLeafCount
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.wrappedIntermediateBlockRootsLeafCount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Whether migration root-hash voting has finalized.
         * @member {boolean} votingComplete
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.votingComplete = false;

        /**
         * The block number at or after which voting is no longer pending.
         * @member {number|Long} votingCompletionDeadlineBlockNumber
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.votingCompletionDeadlineBlockNumber = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Collected migration root-hash votes from nodes.
         * @member {Array.<proto.NodeMigrationRootHashVote.$Properties>} migrationRootHashVotes
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.migrationRootHashVotes = $util.emptyArray;

        /**
         * Wrapped hashes captured while voting is pending.
         * @member {Array.<com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.$Properties>} migrationWrappedHashes
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.migrationWrappedHashes = $util.emptyArray;

        /**
         * A flag indicating that the preview `BlockStreamInfo` object has been
         * overwritten with the real wrapped record block hashes data from `BlockInfo`,
         * i.e. as part of the block streams cutover release.
         * <p>
         * This SHALL be set to `true` only after the schema overwrite logic
         * has run, to prevent any block streams cutover logic from repeating.
         * @member {boolean} previewStreamOverwritten
         * @memberof proto.BlockInfo
         * @instance
         */
        BlockInfo.prototype.previewStreamOverwritten = false;

        /**
         * Creates a new BlockInfo instance using the specified properties.
         * @function create
         * @memberof proto.BlockInfo
         * @static
         * @param {proto.BlockInfo.$Properties=} [properties] Properties to set
         * @returns {proto.BlockInfo} BlockInfo instance
         * @type {{
         *   (properties: proto.BlockInfo.$Shape): proto.BlockInfo & proto.BlockInfo.$Shape;
         *   (properties?: proto.BlockInfo.$Properties): proto.BlockInfo;
         * }}
         */
        BlockInfo.create = function(properties) {
            return new BlockInfo(properties);
        };

        /**
         * Encodes the specified BlockInfo message. Does not implicitly {@link proto.BlockInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.BlockInfo
         * @static
         * @param {proto.BlockInfo.$Properties} message BlockInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlockInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.lastBlockNumber != null && $Object.hasOwnProperty.call(message, "lastBlockNumber") && (typeof message.lastBlockNumber === "object" ? message.lastBlockNumber.low || message.lastBlockNumber.high : message.lastBlockNumber !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.lastBlockNumber);
            if (message.firstConsTimeOfLastBlock != null && $Object.hasOwnProperty.call(message, "firstConsTimeOfLastBlock"))
                $root.proto.Timestamp.encode(message.firstConsTimeOfLastBlock, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.blockHashes != null && $Object.hasOwnProperty.call(message, "blockHashes") && message.blockHashes.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.blockHashes);
            if (message.consTimeOfLastHandledTxn != null && $Object.hasOwnProperty.call(message, "consTimeOfLastHandledTxn"))
                $root.proto.Timestamp.encode(message.consTimeOfLastHandledTxn, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.migrationRecordsStreamed != null && $Object.hasOwnProperty.call(message, "migrationRecordsStreamed") && message.migrationRecordsStreamed !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.migrationRecordsStreamed);
            if (message.firstConsTimeOfCurrentBlock != null && $Object.hasOwnProperty.call(message, "firstConsTimeOfCurrentBlock"))
                $root.proto.Timestamp.encode(message.firstConsTimeOfCurrentBlock, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.lastUsedConsTime != null && $Object.hasOwnProperty.call(message, "lastUsedConsTime"))
                $root.proto.Timestamp.encode(message.lastUsedConsTime, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.lastIntervalProcessTime != null && $Object.hasOwnProperty.call(message, "lastIntervalProcessTime"))
                $root.proto.Timestamp.encode(message.lastIntervalProcessTime, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.previousWrappedRecordBlockRootHash != null && $Object.hasOwnProperty.call(message, "previousWrappedRecordBlockRootHash") && message.previousWrappedRecordBlockRootHash.length)
                writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.previousWrappedRecordBlockRootHash);
            if (message.wrappedIntermediatePreviousBlockRootHashes != null && message.wrappedIntermediatePreviousBlockRootHashes.length)
                for (let i = 0; i < message.wrappedIntermediatePreviousBlockRootHashes.length; ++i)
                    writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.wrappedIntermediatePreviousBlockRootHashes[i]);
            if (message.wrappedIntermediateBlockRootsLeafCount != null && $Object.hasOwnProperty.call(message, "wrappedIntermediateBlockRootsLeafCount") && (typeof message.wrappedIntermediateBlockRootsLeafCount === "object" ? message.wrappedIntermediateBlockRootsLeafCount.low || message.wrappedIntermediateBlockRootsLeafCount.high : message.wrappedIntermediateBlockRootsLeafCount !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).uint64(message.wrappedIntermediateBlockRootsLeafCount);
            if (message.votingComplete != null && $Object.hasOwnProperty.call(message, "votingComplete") && message.votingComplete !== false)
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.votingComplete);
            if (message.votingCompletionDeadlineBlockNumber != null && $Object.hasOwnProperty.call(message, "votingCompletionDeadlineBlockNumber") && (typeof message.votingCompletionDeadlineBlockNumber === "object" ? message.votingCompletionDeadlineBlockNumber.low || message.votingCompletionDeadlineBlockNumber.high : message.votingCompletionDeadlineBlockNumber !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).uint64(message.votingCompletionDeadlineBlockNumber);
            if (message.migrationRootHashVotes != null && message.migrationRootHashVotes.length)
                for (let i = 0; i < message.migrationRootHashVotes.length; ++i)
                    $root.proto.NodeMigrationRootHashVote.encode(message.migrationRootHashVotes[i], writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.migrationWrappedHashes != null && message.migrationWrappedHashes.length)
                for (let i = 0; i < message.migrationWrappedHashes.length; ++i)
                    $root.com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.encode(message.migrationWrappedHashes[i], writer.uint32(/* id 16, wireType 2 =*/130).fork(), _depth + 1).ldelim();
            if (message.previewStreamOverwritten != null && $Object.hasOwnProperty.call(message, "previewStreamOverwritten") && message.previewStreamOverwritten !== false)
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message.previewStreamOverwritten);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BlockInfo message, length delimited. Does not implicitly {@link proto.BlockInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BlockInfo
         * @static
         * @param {proto.BlockInfo.$Properties} message BlockInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlockInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a BlockInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BlockInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BlockInfo & proto.BlockInfo.$Shape} BlockInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlockInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.BlockInfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.lastBlockNumber = value;
                        else
                            delete message.lastBlockNumber;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.firstConsTimeOfLastBlock = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.firstConsTimeOfLastBlock);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.blockHashes = value;
                        else
                            delete message.blockHashes;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.consTimeOfLastHandledTxn = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consTimeOfLastHandledTxn);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.migrationRecordsStreamed = value;
                        else
                            delete message.migrationRecordsStreamed;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.firstConsTimeOfCurrentBlock = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.firstConsTimeOfCurrentBlock);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.lastUsedConsTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.lastUsedConsTime);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.lastIntervalProcessTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.lastIntervalProcessTime);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.previousWrappedRecordBlockRootHash = value;
                        else
                            delete message.previousWrappedRecordBlockRootHash;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.wrappedIntermediatePreviousBlockRootHashes && message.wrappedIntermediatePreviousBlockRootHashes.length))
                            message.wrappedIntermediatePreviousBlockRootHashes = [];
                        message.wrappedIntermediatePreviousBlockRootHashes.push(reader.bytes());
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.wrappedIntermediateBlockRootsLeafCount = value;
                        else
                            delete message.wrappedIntermediateBlockRootsLeafCount;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.votingComplete = value;
                        else
                            delete message.votingComplete;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.votingCompletionDeadlineBlockNumber = value;
                        else
                            delete message.votingCompletionDeadlineBlockNumber;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (!(message.migrationRootHashVotes && message.migrationRootHashVotes.length))
                            message.migrationRootHashVotes = [];
                        message.migrationRootHashVotes.push($root.proto.NodeMigrationRootHashVote.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if (!(message.migrationWrappedHashes && message.migrationWrappedHashes.length))
                            message.migrationWrappedHashes = [];
                        message.migrationWrappedHashes.push($root.com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.previewStreamOverwritten = value;
                        else
                            delete message.previewStreamOverwritten;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a BlockInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BlockInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BlockInfo & proto.BlockInfo.$Shape} BlockInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlockInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BlockInfo message.
         * @function verify
         * @memberof proto.BlockInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BlockInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.lastBlockNumber != null && $Object.hasOwnProperty.call(message, "lastBlockNumber"))
                if (!$util.isInteger(message.lastBlockNumber) && !(message.lastBlockNumber && $util.isInteger(message.lastBlockNumber.low) && $util.isInteger(message.lastBlockNumber.high)))
                    return "lastBlockNumber: integer|Long expected";
            if (message.firstConsTimeOfLastBlock != null && $Object.hasOwnProperty.call(message, "firstConsTimeOfLastBlock")) {
                let error = $root.proto.Timestamp.verify(message.firstConsTimeOfLastBlock, _depth + 1);
                if (error)
                    return "firstConsTimeOfLastBlock." + error;
            }
            if (message.blockHashes != null && $Object.hasOwnProperty.call(message, "blockHashes"))
                if (!(message.blockHashes && typeof message.blockHashes.length === "number" || $util.isString(message.blockHashes)))
                    return "blockHashes: buffer expected";
            if (message.consTimeOfLastHandledTxn != null && $Object.hasOwnProperty.call(message, "consTimeOfLastHandledTxn")) {
                let error = $root.proto.Timestamp.verify(message.consTimeOfLastHandledTxn, _depth + 1);
                if (error)
                    return "consTimeOfLastHandledTxn." + error;
            }
            if (message.migrationRecordsStreamed != null && $Object.hasOwnProperty.call(message, "migrationRecordsStreamed"))
                if (typeof message.migrationRecordsStreamed !== "boolean")
                    return "migrationRecordsStreamed: boolean expected";
            if (message.firstConsTimeOfCurrentBlock != null && $Object.hasOwnProperty.call(message, "firstConsTimeOfCurrentBlock")) {
                let error = $root.proto.Timestamp.verify(message.firstConsTimeOfCurrentBlock, _depth + 1);
                if (error)
                    return "firstConsTimeOfCurrentBlock." + error;
            }
            if (message.lastUsedConsTime != null && $Object.hasOwnProperty.call(message, "lastUsedConsTime")) {
                let error = $root.proto.Timestamp.verify(message.lastUsedConsTime, _depth + 1);
                if (error)
                    return "lastUsedConsTime." + error;
            }
            if (message.lastIntervalProcessTime != null && $Object.hasOwnProperty.call(message, "lastIntervalProcessTime")) {
                let error = $root.proto.Timestamp.verify(message.lastIntervalProcessTime, _depth + 1);
                if (error)
                    return "lastIntervalProcessTime." + error;
            }
            if (message.previousWrappedRecordBlockRootHash != null && $Object.hasOwnProperty.call(message, "previousWrappedRecordBlockRootHash"))
                if (!(message.previousWrappedRecordBlockRootHash && typeof message.previousWrappedRecordBlockRootHash.length === "number" || $util.isString(message.previousWrappedRecordBlockRootHash)))
                    return "previousWrappedRecordBlockRootHash: buffer expected";
            if (message.wrappedIntermediatePreviousBlockRootHashes != null && $Object.hasOwnProperty.call(message, "wrappedIntermediatePreviousBlockRootHashes")) {
                if (!$Array.isArray(message.wrappedIntermediatePreviousBlockRootHashes))
                    return "wrappedIntermediatePreviousBlockRootHashes: array expected";
                for (let i = 0; i < message.wrappedIntermediatePreviousBlockRootHashes.length; ++i)
                    if (!(message.wrappedIntermediatePreviousBlockRootHashes[i] && typeof message.wrappedIntermediatePreviousBlockRootHashes[i].length === "number" || $util.isString(message.wrappedIntermediatePreviousBlockRootHashes[i])))
                        return "wrappedIntermediatePreviousBlockRootHashes: buffer[] expected";
            }
            if (message.wrappedIntermediateBlockRootsLeafCount != null && $Object.hasOwnProperty.call(message, "wrappedIntermediateBlockRootsLeafCount"))
                if (!$util.isInteger(message.wrappedIntermediateBlockRootsLeafCount) && !(message.wrappedIntermediateBlockRootsLeafCount && $util.isInteger(message.wrappedIntermediateBlockRootsLeafCount.low) && $util.isInteger(message.wrappedIntermediateBlockRootsLeafCount.high)))
                    return "wrappedIntermediateBlockRootsLeafCount: integer|Long expected";
            if (message.votingComplete != null && $Object.hasOwnProperty.call(message, "votingComplete"))
                if (typeof message.votingComplete !== "boolean")
                    return "votingComplete: boolean expected";
            if (message.votingCompletionDeadlineBlockNumber != null && $Object.hasOwnProperty.call(message, "votingCompletionDeadlineBlockNumber"))
                if (!$util.isInteger(message.votingCompletionDeadlineBlockNumber) && !(message.votingCompletionDeadlineBlockNumber && $util.isInteger(message.votingCompletionDeadlineBlockNumber.low) && $util.isInteger(message.votingCompletionDeadlineBlockNumber.high)))
                    return "votingCompletionDeadlineBlockNumber: integer|Long expected";
            if (message.migrationRootHashVotes != null && $Object.hasOwnProperty.call(message, "migrationRootHashVotes")) {
                if (!$Array.isArray(message.migrationRootHashVotes))
                    return "migrationRootHashVotes: array expected";
                for (let i = 0; i < message.migrationRootHashVotes.length; ++i) {
                    let error = $root.proto.NodeMigrationRootHashVote.verify(message.migrationRootHashVotes[i], _depth + 1);
                    if (error)
                        return "migrationRootHashVotes." + error;
                }
            }
            if (message.migrationWrappedHashes != null && $Object.hasOwnProperty.call(message, "migrationWrappedHashes")) {
                if (!$Array.isArray(message.migrationWrappedHashes))
                    return "migrationWrappedHashes: array expected";
                for (let i = 0; i < message.migrationWrappedHashes.length; ++i) {
                    let error = $root.com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.verify(message.migrationWrappedHashes[i], _depth + 1);
                    if (error)
                        return "migrationWrappedHashes." + error;
                }
            }
            if (message.previewStreamOverwritten != null && $Object.hasOwnProperty.call(message, "previewStreamOverwritten"))
                if (typeof message.previewStreamOverwritten !== "boolean")
                    return "previewStreamOverwritten: boolean expected";
            return null;
        };

        /**
         * Creates a BlockInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BlockInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BlockInfo} BlockInfo
         */
        BlockInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.BlockInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.BlockInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.BlockInfo();
            if (object.lastBlockNumber != null)
                if (typeof object.lastBlockNumber === "object" ? object.lastBlockNumber.low || object.lastBlockNumber.high : $Number(object.lastBlockNumber) !== 0)
                    if ($util.Long)
                        message.lastBlockNumber = $util.Long.fromValue(object.lastBlockNumber, false);
                    else if (typeof object.lastBlockNumber === "string")
                        message.lastBlockNumber = $parseInt(object.lastBlockNumber, 10);
                    else if (typeof object.lastBlockNumber === "number")
                        message.lastBlockNumber = object.lastBlockNumber;
                    else if (typeof object.lastBlockNumber === "object")
                        message.lastBlockNumber = new $util.LongBits(object.lastBlockNumber.low >>> 0, object.lastBlockNumber.high >>> 0).toNumber();
            if (object.firstConsTimeOfLastBlock != null) {
                if (!$util.isObject(object.firstConsTimeOfLastBlock))
                    throw $TypeError(".proto.BlockInfo.firstConsTimeOfLastBlock: object expected");
                message.firstConsTimeOfLastBlock = $root.proto.Timestamp.fromObject(object.firstConsTimeOfLastBlock, _depth + 1);
            }
            if (object.blockHashes != null)
                if (object.blockHashes.length)
                    if (typeof object.blockHashes === "string")
                        $util.base64.decode(object.blockHashes, message.blockHashes = $util.newBuffer($util.base64.length(object.blockHashes)), 0);
                    else if (object.blockHashes.length >= 0)
                        message.blockHashes = object.blockHashes;
            if (object.consTimeOfLastHandledTxn != null) {
                if (!$util.isObject(object.consTimeOfLastHandledTxn))
                    throw $TypeError(".proto.BlockInfo.consTimeOfLastHandledTxn: object expected");
                message.consTimeOfLastHandledTxn = $root.proto.Timestamp.fromObject(object.consTimeOfLastHandledTxn, _depth + 1);
            }
            if (object.migrationRecordsStreamed != null)
                if (object.migrationRecordsStreamed)
                    message.migrationRecordsStreamed = $Boolean(object.migrationRecordsStreamed);
            if (object.firstConsTimeOfCurrentBlock != null) {
                if (!$util.isObject(object.firstConsTimeOfCurrentBlock))
                    throw $TypeError(".proto.BlockInfo.firstConsTimeOfCurrentBlock: object expected");
                message.firstConsTimeOfCurrentBlock = $root.proto.Timestamp.fromObject(object.firstConsTimeOfCurrentBlock, _depth + 1);
            }
            if (object.lastUsedConsTime != null) {
                if (!$util.isObject(object.lastUsedConsTime))
                    throw $TypeError(".proto.BlockInfo.lastUsedConsTime: object expected");
                message.lastUsedConsTime = $root.proto.Timestamp.fromObject(object.lastUsedConsTime, _depth + 1);
            }
            if (object.lastIntervalProcessTime != null) {
                if (!$util.isObject(object.lastIntervalProcessTime))
                    throw $TypeError(".proto.BlockInfo.lastIntervalProcessTime: object expected");
                message.lastIntervalProcessTime = $root.proto.Timestamp.fromObject(object.lastIntervalProcessTime, _depth + 1);
            }
            if (object.previousWrappedRecordBlockRootHash != null)
                if (object.previousWrappedRecordBlockRootHash.length)
                    if (typeof object.previousWrappedRecordBlockRootHash === "string")
                        $util.base64.decode(object.previousWrappedRecordBlockRootHash, message.previousWrappedRecordBlockRootHash = $util.newBuffer($util.base64.length(object.previousWrappedRecordBlockRootHash)), 0);
                    else if (object.previousWrappedRecordBlockRootHash.length >= 0)
                        message.previousWrappedRecordBlockRootHash = object.previousWrappedRecordBlockRootHash;
            if (object.wrappedIntermediatePreviousBlockRootHashes) {
                if (!$Array.isArray(object.wrappedIntermediatePreviousBlockRootHashes))
                    throw $TypeError(".proto.BlockInfo.wrappedIntermediatePreviousBlockRootHashes: array expected");
                message.wrappedIntermediatePreviousBlockRootHashes = $Array(object.wrappedIntermediatePreviousBlockRootHashes.length);
                for (let i = 0; i < object.wrappedIntermediatePreviousBlockRootHashes.length; ++i)
                    if (typeof object.wrappedIntermediatePreviousBlockRootHashes[i] === "string")
                        $util.base64.decode(object.wrappedIntermediatePreviousBlockRootHashes[i], message.wrappedIntermediatePreviousBlockRootHashes[i] = $util.newBuffer($util.base64.length(object.wrappedIntermediatePreviousBlockRootHashes[i])), 0);
                    else if (object.wrappedIntermediatePreviousBlockRootHashes[i].length >= 0)
                        message.wrappedIntermediatePreviousBlockRootHashes[i] = object.wrappedIntermediatePreviousBlockRootHashes[i];
            }
            if (object.wrappedIntermediateBlockRootsLeafCount != null)
                if (typeof object.wrappedIntermediateBlockRootsLeafCount === "object" ? object.wrappedIntermediateBlockRootsLeafCount.low || object.wrappedIntermediateBlockRootsLeafCount.high : $Number(object.wrappedIntermediateBlockRootsLeafCount) !== 0)
                    if ($util.Long)
                        message.wrappedIntermediateBlockRootsLeafCount = $util.Long.fromValue(object.wrappedIntermediateBlockRootsLeafCount, true);
                    else if (typeof object.wrappedIntermediateBlockRootsLeafCount === "string")
                        message.wrappedIntermediateBlockRootsLeafCount = $parseInt(object.wrappedIntermediateBlockRootsLeafCount, 10);
                    else if (typeof object.wrappedIntermediateBlockRootsLeafCount === "number")
                        message.wrappedIntermediateBlockRootsLeafCount = object.wrappedIntermediateBlockRootsLeafCount;
                    else if (typeof object.wrappedIntermediateBlockRootsLeafCount === "object")
                        message.wrappedIntermediateBlockRootsLeafCount = new $util.LongBits(object.wrappedIntermediateBlockRootsLeafCount.low >>> 0, object.wrappedIntermediateBlockRootsLeafCount.high >>> 0).toNumber(true);
            if (object.votingComplete != null)
                if (object.votingComplete)
                    message.votingComplete = $Boolean(object.votingComplete);
            if (object.votingCompletionDeadlineBlockNumber != null)
                if (typeof object.votingCompletionDeadlineBlockNumber === "object" ? object.votingCompletionDeadlineBlockNumber.low || object.votingCompletionDeadlineBlockNumber.high : $Number(object.votingCompletionDeadlineBlockNumber) !== 0)
                    if ($util.Long)
                        message.votingCompletionDeadlineBlockNumber = $util.Long.fromValue(object.votingCompletionDeadlineBlockNumber, true);
                    else if (typeof object.votingCompletionDeadlineBlockNumber === "string")
                        message.votingCompletionDeadlineBlockNumber = $parseInt(object.votingCompletionDeadlineBlockNumber, 10);
                    else if (typeof object.votingCompletionDeadlineBlockNumber === "number")
                        message.votingCompletionDeadlineBlockNumber = object.votingCompletionDeadlineBlockNumber;
                    else if (typeof object.votingCompletionDeadlineBlockNumber === "object")
                        message.votingCompletionDeadlineBlockNumber = new $util.LongBits(object.votingCompletionDeadlineBlockNumber.low >>> 0, object.votingCompletionDeadlineBlockNumber.high >>> 0).toNumber(true);
            if (object.migrationRootHashVotes) {
                if (!$Array.isArray(object.migrationRootHashVotes))
                    throw $TypeError(".proto.BlockInfo.migrationRootHashVotes: array expected");
                message.migrationRootHashVotes = $Array(object.migrationRootHashVotes.length);
                for (let i = 0; i < object.migrationRootHashVotes.length; ++i) {
                    if (!$util.isObject(object.migrationRootHashVotes[i]))
                        throw $TypeError(".proto.BlockInfo.migrationRootHashVotes: object expected");
                    message.migrationRootHashVotes[i] = $root.proto.NodeMigrationRootHashVote.fromObject(object.migrationRootHashVotes[i], _depth + 1);
                }
            }
            if (object.migrationWrappedHashes) {
                if (!$Array.isArray(object.migrationWrappedHashes))
                    throw $TypeError(".proto.BlockInfo.migrationWrappedHashes: array expected");
                message.migrationWrappedHashes = $Array(object.migrationWrappedHashes.length);
                for (let i = 0; i < object.migrationWrappedHashes.length; ++i) {
                    if (!$util.isObject(object.migrationWrappedHashes[i]))
                        throw $TypeError(".proto.BlockInfo.migrationWrappedHashes: object expected");
                    message.migrationWrappedHashes[i] = $root.com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.fromObject(object.migrationWrappedHashes[i], _depth + 1);
                }
            }
            if (object.previewStreamOverwritten != null)
                if (object.previewStreamOverwritten)
                    message.previewStreamOverwritten = $Boolean(object.previewStreamOverwritten);
            return message;
        };

        /**
         * Creates a plain object from a BlockInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BlockInfo
         * @static
         * @param {proto.BlockInfo} message BlockInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BlockInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.wrappedIntermediatePreviousBlockRootHashes = [];
                object.migrationRootHashVotes = [];
                object.migrationWrappedHashes = [];
            }
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastBlockNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.lastBlockNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.firstConsTimeOfLastBlock = null;
                if (options.bytes === $String)
                    object.blockHashes = "";
                else {
                    object.blockHashes = [];
                    if (options.bytes !== $Array)
                        object.blockHashes = $util.newBuffer(object.blockHashes);
                }
                object.consTimeOfLastHandledTxn = null;
                object.migrationRecordsStreamed = false;
                object.firstConsTimeOfCurrentBlock = null;
                object.lastUsedConsTime = null;
                object.lastIntervalProcessTime = null;
                if (options.bytes === $String)
                    object.previousWrappedRecordBlockRootHash = "";
                else {
                    object.previousWrappedRecordBlockRootHash = [];
                    if (options.bytes !== $Array)
                        object.previousWrappedRecordBlockRootHash = $util.newBuffer(object.previousWrappedRecordBlockRootHash);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.wrappedIntermediateBlockRootsLeafCount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.wrappedIntermediateBlockRootsLeafCount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.votingComplete = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.votingCompletionDeadlineBlockNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.votingCompletionDeadlineBlockNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.previewStreamOverwritten = false;
            }
            if (message.lastBlockNumber != null && $Object.hasOwnProperty.call(message, "lastBlockNumber"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.lastBlockNumber = typeof message.lastBlockNumber === "number" ? $BigInt(message.lastBlockNumber) : $util.Long.fromBits(message.lastBlockNumber.low >>> 0, message.lastBlockNumber.high >>> 0, false).toBigInt();
                else if (typeof message.lastBlockNumber === "number")
                    object.lastBlockNumber = options.longs === $String ? $String(message.lastBlockNumber) : message.lastBlockNumber;
                else
                    object.lastBlockNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.lastBlockNumber) : options.longs === $Number ? new $util.LongBits(message.lastBlockNumber.low >>> 0, message.lastBlockNumber.high >>> 0).toNumber() : message.lastBlockNumber;
            if (message.firstConsTimeOfLastBlock != null && $Object.hasOwnProperty.call(message, "firstConsTimeOfLastBlock"))
                object.firstConsTimeOfLastBlock = $root.proto.Timestamp.toObject(message.firstConsTimeOfLastBlock, options, _depth + 1);
            if (message.blockHashes != null && $Object.hasOwnProperty.call(message, "blockHashes"))
                object.blockHashes = options.bytes === $String ? $util.base64.encode(message.blockHashes, 0, message.blockHashes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.blockHashes) : message.blockHashes;
            if (message.consTimeOfLastHandledTxn != null && $Object.hasOwnProperty.call(message, "consTimeOfLastHandledTxn"))
                object.consTimeOfLastHandledTxn = $root.proto.Timestamp.toObject(message.consTimeOfLastHandledTxn, options, _depth + 1);
            if (message.migrationRecordsStreamed != null && $Object.hasOwnProperty.call(message, "migrationRecordsStreamed"))
                object.migrationRecordsStreamed = message.migrationRecordsStreamed;
            if (message.firstConsTimeOfCurrentBlock != null && $Object.hasOwnProperty.call(message, "firstConsTimeOfCurrentBlock"))
                object.firstConsTimeOfCurrentBlock = $root.proto.Timestamp.toObject(message.firstConsTimeOfCurrentBlock, options, _depth + 1);
            if (message.lastUsedConsTime != null && $Object.hasOwnProperty.call(message, "lastUsedConsTime"))
                object.lastUsedConsTime = $root.proto.Timestamp.toObject(message.lastUsedConsTime, options, _depth + 1);
            if (message.lastIntervalProcessTime != null && $Object.hasOwnProperty.call(message, "lastIntervalProcessTime"))
                object.lastIntervalProcessTime = $root.proto.Timestamp.toObject(message.lastIntervalProcessTime, options, _depth + 1);
            if (message.previousWrappedRecordBlockRootHash != null && $Object.hasOwnProperty.call(message, "previousWrappedRecordBlockRootHash"))
                object.previousWrappedRecordBlockRootHash = options.bytes === $String ? $util.base64.encode(message.previousWrappedRecordBlockRootHash, 0, message.previousWrappedRecordBlockRootHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.previousWrappedRecordBlockRootHash) : message.previousWrappedRecordBlockRootHash;
            if (message.wrappedIntermediatePreviousBlockRootHashes && message.wrappedIntermediatePreviousBlockRootHashes.length) {
                object.wrappedIntermediatePreviousBlockRootHashes = $Array(message.wrappedIntermediatePreviousBlockRootHashes.length);
                for (let j = 0; j < message.wrappedIntermediatePreviousBlockRootHashes.length; ++j)
                    object.wrappedIntermediatePreviousBlockRootHashes[j] = options.bytes === $String ? $util.base64.encode(message.wrappedIntermediatePreviousBlockRootHashes[j], 0, message.wrappedIntermediatePreviousBlockRootHashes[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.wrappedIntermediatePreviousBlockRootHashes[j]) : message.wrappedIntermediatePreviousBlockRootHashes[j];
            }
            if (message.wrappedIntermediateBlockRootsLeafCount != null && $Object.hasOwnProperty.call(message, "wrappedIntermediateBlockRootsLeafCount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.wrappedIntermediateBlockRootsLeafCount = typeof message.wrappedIntermediateBlockRootsLeafCount === "number" ? $BigInt(message.wrappedIntermediateBlockRootsLeafCount) : $util.Long.fromBits(message.wrappedIntermediateBlockRootsLeafCount.low >>> 0, message.wrappedIntermediateBlockRootsLeafCount.high >>> 0, true).toBigInt();
                else if (typeof message.wrappedIntermediateBlockRootsLeafCount === "number")
                    object.wrappedIntermediateBlockRootsLeafCount = options.longs === $String ? $String(message.wrappedIntermediateBlockRootsLeafCount) : message.wrappedIntermediateBlockRootsLeafCount;
                else
                    object.wrappedIntermediateBlockRootsLeafCount = options.longs === $String ? $util.Long.prototype.toString.call(message.wrappedIntermediateBlockRootsLeafCount) : options.longs === $Number ? new $util.LongBits(message.wrappedIntermediateBlockRootsLeafCount.low >>> 0, message.wrappedIntermediateBlockRootsLeafCount.high >>> 0).toNumber(true) : message.wrappedIntermediateBlockRootsLeafCount;
            if (message.votingComplete != null && $Object.hasOwnProperty.call(message, "votingComplete"))
                object.votingComplete = message.votingComplete;
            if (message.votingCompletionDeadlineBlockNumber != null && $Object.hasOwnProperty.call(message, "votingCompletionDeadlineBlockNumber"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.votingCompletionDeadlineBlockNumber = typeof message.votingCompletionDeadlineBlockNumber === "number" ? $BigInt(message.votingCompletionDeadlineBlockNumber) : $util.Long.fromBits(message.votingCompletionDeadlineBlockNumber.low >>> 0, message.votingCompletionDeadlineBlockNumber.high >>> 0, true).toBigInt();
                else if (typeof message.votingCompletionDeadlineBlockNumber === "number")
                    object.votingCompletionDeadlineBlockNumber = options.longs === $String ? $String(message.votingCompletionDeadlineBlockNumber) : message.votingCompletionDeadlineBlockNumber;
                else
                    object.votingCompletionDeadlineBlockNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.votingCompletionDeadlineBlockNumber) : options.longs === $Number ? new $util.LongBits(message.votingCompletionDeadlineBlockNumber.low >>> 0, message.votingCompletionDeadlineBlockNumber.high >>> 0).toNumber(true) : message.votingCompletionDeadlineBlockNumber;
            if (message.migrationRootHashVotes && message.migrationRootHashVotes.length) {
                object.migrationRootHashVotes = $Array(message.migrationRootHashVotes.length);
                for (let j = 0; j < message.migrationRootHashVotes.length; ++j)
                    object.migrationRootHashVotes[j] = $root.proto.NodeMigrationRootHashVote.toObject(message.migrationRootHashVotes[j], options, _depth + 1);
            }
            if (message.migrationWrappedHashes && message.migrationWrappedHashes.length) {
                object.migrationWrappedHashes = $Array(message.migrationWrappedHashes.length);
                for (let j = 0; j < message.migrationWrappedHashes.length; ++j)
                    object.migrationWrappedHashes[j] = $root.com.hedera.hapi.node.state.blockrecords.MigrationWrappedHashes.toObject(message.migrationWrappedHashes[j], options, _depth + 1);
            }
            if (message.previewStreamOverwritten != null && $Object.hasOwnProperty.call(message, "previewStreamOverwritten"))
                object.previewStreamOverwritten = message.previewStreamOverwritten;
            return object;
        };

        /**
         * Converts this BlockInfo to JSON.
         * @function toJSON
         * @memberof proto.BlockInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BlockInfo.prototype.toJSON = function() {
            return BlockInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for BlockInfo
         * @function getTypeUrl
         * @memberof proto.BlockInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        BlockInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BlockInfo";
        };

        return BlockInfo;
    })();

    proto.RunningHashes = (function() {

        /**
         * Properties of a RunningHashes.
         * @typedef {Object} proto.RunningHashes.$Properties
         * @property {Uint8Array|null} [runningHash] A running hash of all record stream items.
         * @property {Uint8Array|null} [nMinus_1RunningHash] The previous running hash of all record stream items.
         * @property {Uint8Array|null} [nMinus_2RunningHash] The previous, previous running hash of all record stream items.
         * @property {Uint8Array|null} [nMinus_3RunningHash] The previous, previous, previous running hash of all record stream items.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RunningHashes.
         * @memberof proto
         * @interface IRunningHashes
         * @augments proto.RunningHashes.$Properties
         * @deprecated Use proto.RunningHashes.$Properties instead.
         */

        /**
         * Shape of a RunningHashes.
         * @typedef {proto.RunningHashes.$Properties} proto.RunningHashes.$Shape
         */

        /**
         * Constructs a new RunningHashes.
         * @memberof proto
         * @classdesc The running hash of transaction records and the previous `3` running hashes.
         * 
         * All hashes are 48 byte SHA384 hash values. If the running hashes do not
         * exist yet (for example, at genesis) then each not-yet-available value SHALL
         * be empty (zero-length) bytes.
         * @constructor
         * @param {proto.RunningHashes.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RunningHashes = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A running hash of all record stream items.
         * @member {Uint8Array} runningHash
         * @memberof proto.RunningHashes
         * @instance
         */
        RunningHashes.prototype.runningHash = $util.newBuffer([]);

        /**
         * The previous running hash of all record stream items.
         * @member {Uint8Array} nMinus_1RunningHash
         * @memberof proto.RunningHashes
         * @instance
         */
        RunningHashes.prototype.nMinus_1RunningHash = $util.newBuffer([]);

        /**
         * The previous, previous running hash of all record stream items.
         * @member {Uint8Array} nMinus_2RunningHash
         * @memberof proto.RunningHashes
         * @instance
         */
        RunningHashes.prototype.nMinus_2RunningHash = $util.newBuffer([]);

        /**
         * The previous, previous, previous running hash of all record stream items.
         * @member {Uint8Array} nMinus_3RunningHash
         * @memberof proto.RunningHashes
         * @instance
         */
        RunningHashes.prototype.nMinus_3RunningHash = $util.newBuffer([]);

        /**
         * Creates a new RunningHashes instance using the specified properties.
         * @function create
         * @memberof proto.RunningHashes
         * @static
         * @param {proto.RunningHashes.$Properties=} [properties] Properties to set
         * @returns {proto.RunningHashes} RunningHashes instance
         * @type {{
         *   (properties: proto.RunningHashes.$Shape): proto.RunningHashes & proto.RunningHashes.$Shape;
         *   (properties?: proto.RunningHashes.$Properties): proto.RunningHashes;
         * }}
         */
        RunningHashes.create = function(properties) {
            return new RunningHashes(properties);
        };

        /**
         * Encodes the specified RunningHashes message. Does not implicitly {@link proto.RunningHashes.verify|verify} messages.
         * @function encode
         * @memberof proto.RunningHashes
         * @static
         * @param {proto.RunningHashes.$Properties} message RunningHashes message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RunningHashes.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.runningHash != null && $Object.hasOwnProperty.call(message, "runningHash") && message.runningHash.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.runningHash);
            if (message.nMinus_1RunningHash != null && $Object.hasOwnProperty.call(message, "nMinus_1RunningHash") && message.nMinus_1RunningHash.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.nMinus_1RunningHash);
            if (message.nMinus_2RunningHash != null && $Object.hasOwnProperty.call(message, "nMinus_2RunningHash") && message.nMinus_2RunningHash.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.nMinus_2RunningHash);
            if (message.nMinus_3RunningHash != null && $Object.hasOwnProperty.call(message, "nMinus_3RunningHash") && message.nMinus_3RunningHash.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.nMinus_3RunningHash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RunningHashes message, length delimited. Does not implicitly {@link proto.RunningHashes.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.RunningHashes
         * @static
         * @param {proto.RunningHashes.$Properties} message RunningHashes message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RunningHashes.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RunningHashes message from the specified reader or buffer.
         * @function decode
         * @memberof proto.RunningHashes
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.RunningHashes & proto.RunningHashes.$Shape} RunningHashes
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RunningHashes.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.RunningHashes();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.runningHash = value;
                        else
                            delete message.runningHash;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.nMinus_1RunningHash = value;
                        else
                            delete message.nMinus_1RunningHash;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.nMinus_2RunningHash = value;
                        else
                            delete message.nMinus_2RunningHash;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.nMinus_3RunningHash = value;
                        else
                            delete message.nMinus_3RunningHash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RunningHashes message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.RunningHashes
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.RunningHashes & proto.RunningHashes.$Shape} RunningHashes
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RunningHashes.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RunningHashes message.
         * @function verify
         * @memberof proto.RunningHashes
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RunningHashes.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.runningHash != null && $Object.hasOwnProperty.call(message, "runningHash"))
                if (!(message.runningHash && typeof message.runningHash.length === "number" || $util.isString(message.runningHash)))
                    return "runningHash: buffer expected";
            if (message.nMinus_1RunningHash != null && $Object.hasOwnProperty.call(message, "nMinus_1RunningHash"))
                if (!(message.nMinus_1RunningHash && typeof message.nMinus_1RunningHash.length === "number" || $util.isString(message.nMinus_1RunningHash)))
                    return "nMinus_1RunningHash: buffer expected";
            if (message.nMinus_2RunningHash != null && $Object.hasOwnProperty.call(message, "nMinus_2RunningHash"))
                if (!(message.nMinus_2RunningHash && typeof message.nMinus_2RunningHash.length === "number" || $util.isString(message.nMinus_2RunningHash)))
                    return "nMinus_2RunningHash: buffer expected";
            if (message.nMinus_3RunningHash != null && $Object.hasOwnProperty.call(message, "nMinus_3RunningHash"))
                if (!(message.nMinus_3RunningHash && typeof message.nMinus_3RunningHash.length === "number" || $util.isString(message.nMinus_3RunningHash)))
                    return "nMinus_3RunningHash: buffer expected";
            return null;
        };

        /**
         * Creates a RunningHashes message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.RunningHashes
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.RunningHashes} RunningHashes
         */
        RunningHashes.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.RunningHashes)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.RunningHashes: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.RunningHashes();
            if (object.runningHash != null)
                if (object.runningHash.length)
                    if (typeof object.runningHash === "string")
                        $util.base64.decode(object.runningHash, message.runningHash = $util.newBuffer($util.base64.length(object.runningHash)), 0);
                    else if (object.runningHash.length >= 0)
                        message.runningHash = object.runningHash;
            if (object.nMinus_1RunningHash != null)
                if (object.nMinus_1RunningHash.length)
                    if (typeof object.nMinus_1RunningHash === "string")
                        $util.base64.decode(object.nMinus_1RunningHash, message.nMinus_1RunningHash = $util.newBuffer($util.base64.length(object.nMinus_1RunningHash)), 0);
                    else if (object.nMinus_1RunningHash.length >= 0)
                        message.nMinus_1RunningHash = object.nMinus_1RunningHash;
            if (object.nMinus_2RunningHash != null)
                if (object.nMinus_2RunningHash.length)
                    if (typeof object.nMinus_2RunningHash === "string")
                        $util.base64.decode(object.nMinus_2RunningHash, message.nMinus_2RunningHash = $util.newBuffer($util.base64.length(object.nMinus_2RunningHash)), 0);
                    else if (object.nMinus_2RunningHash.length >= 0)
                        message.nMinus_2RunningHash = object.nMinus_2RunningHash;
            if (object.nMinus_3RunningHash != null)
                if (object.nMinus_3RunningHash.length)
                    if (typeof object.nMinus_3RunningHash === "string")
                        $util.base64.decode(object.nMinus_3RunningHash, message.nMinus_3RunningHash = $util.newBuffer($util.base64.length(object.nMinus_3RunningHash)), 0);
                    else if (object.nMinus_3RunningHash.length >= 0)
                        message.nMinus_3RunningHash = object.nMinus_3RunningHash;
            return message;
        };

        /**
         * Creates a plain object from a RunningHashes message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.RunningHashes
         * @static
         * @param {proto.RunningHashes} message RunningHashes
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RunningHashes.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.runningHash = "";
                else {
                    object.runningHash = [];
                    if (options.bytes !== $Array)
                        object.runningHash = $util.newBuffer(object.runningHash);
                }
                if (options.bytes === $String)
                    object.nMinus_1RunningHash = "";
                else {
                    object.nMinus_1RunningHash = [];
                    if (options.bytes !== $Array)
                        object.nMinus_1RunningHash = $util.newBuffer(object.nMinus_1RunningHash);
                }
                if (options.bytes === $String)
                    object.nMinus_2RunningHash = "";
                else {
                    object.nMinus_2RunningHash = [];
                    if (options.bytes !== $Array)
                        object.nMinus_2RunningHash = $util.newBuffer(object.nMinus_2RunningHash);
                }
                if (options.bytes === $String)
                    object.nMinus_3RunningHash = "";
                else {
                    object.nMinus_3RunningHash = [];
                    if (options.bytes !== $Array)
                        object.nMinus_3RunningHash = $util.newBuffer(object.nMinus_3RunningHash);
                }
            }
            if (message.runningHash != null && $Object.hasOwnProperty.call(message, "runningHash"))
                object.runningHash = options.bytes === $String ? $util.base64.encode(message.runningHash, 0, message.runningHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.runningHash) : message.runningHash;
            if (message.nMinus_1RunningHash != null && $Object.hasOwnProperty.call(message, "nMinus_1RunningHash"))
                object.nMinus_1RunningHash = options.bytes === $String ? $util.base64.encode(message.nMinus_1RunningHash, 0, message.nMinus_1RunningHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.nMinus_1RunningHash) : message.nMinus_1RunningHash;
            if (message.nMinus_2RunningHash != null && $Object.hasOwnProperty.call(message, "nMinus_2RunningHash"))
                object.nMinus_2RunningHash = options.bytes === $String ? $util.base64.encode(message.nMinus_2RunningHash, 0, message.nMinus_2RunningHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.nMinus_2RunningHash) : message.nMinus_2RunningHash;
            if (message.nMinus_3RunningHash != null && $Object.hasOwnProperty.call(message, "nMinus_3RunningHash"))
                object.nMinus_3RunningHash = options.bytes === $String ? $util.base64.encode(message.nMinus_3RunningHash, 0, message.nMinus_3RunningHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.nMinus_3RunningHash) : message.nMinus_3RunningHash;
            return object;
        };

        /**
         * Converts this RunningHashes to JSON.
         * @function toJSON
         * @memberof proto.RunningHashes
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RunningHashes.prototype.toJSON = function() {
            return RunningHashes.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RunningHashes
         * @function getTypeUrl
         * @memberof proto.RunningHashes
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RunningHashes.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.RunningHashes";
        };

        return RunningHashes;
    })();

    proto.EntityNumber = (function() {

        /**
         * Properties of an EntityNumber.
         * @typedef {Object} proto.EntityNumber.$Properties
         * @property {number|Long|null} [number] The entity number to store.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an EntityNumber.
         * @memberof proto
         * @interface IEntityNumber
         * @augments proto.EntityNumber.$Properties
         * @deprecated Use proto.EntityNumber.$Properties instead.
         */

        /**
         * Shape of an EntityNumber.
         * @typedef {proto.EntityNumber.$Properties} proto.EntityNumber.$Shape
         */

        /**
         * Constructs a new EntityNumber.
         * @memberof proto
         * @classdesc A single 64-bit number identifying a Hedera native entity.
         * 
         * Deprecated.<br/>
         * A primitive `int64` or `google.protobuf.Int64Value` wrapper is preferred.
         * @constructor
         * @param {proto.EntityNumber.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const EntityNumber = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The entity number to store.
         * @member {number|Long} number
         * @memberof proto.EntityNumber
         * @instance
         */
        EntityNumber.prototype.number = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new EntityNumber instance using the specified properties.
         * @function create
         * @memberof proto.EntityNumber
         * @static
         * @param {proto.EntityNumber.$Properties=} [properties] Properties to set
         * @returns {proto.EntityNumber} EntityNumber instance
         * @type {{
         *   (properties: proto.EntityNumber.$Shape): proto.EntityNumber & proto.EntityNumber.$Shape;
         *   (properties?: proto.EntityNumber.$Properties): proto.EntityNumber;
         * }}
         */
        EntityNumber.create = function(properties) {
            return new EntityNumber(properties);
        };

        /**
         * Encodes the specified EntityNumber message. Does not implicitly {@link proto.EntityNumber.verify|verify} messages.
         * @function encode
         * @memberof proto.EntityNumber
         * @static
         * @param {proto.EntityNumber.$Properties} message EntityNumber message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EntityNumber.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.number != null && $Object.hasOwnProperty.call(message, "number") && (typeof message.number === "object" ? message.number.low || message.number.high : message.number !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.number);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EntityNumber message, length delimited. Does not implicitly {@link proto.EntityNumber.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EntityNumber
         * @static
         * @param {proto.EntityNumber.$Properties} message EntityNumber message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EntityNumber.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an EntityNumber message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EntityNumber
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EntityNumber & proto.EntityNumber.$Shape} EntityNumber
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EntityNumber.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.EntityNumber();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.number = value;
                        else
                            delete message.number;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EntityNumber message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EntityNumber
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EntityNumber & proto.EntityNumber.$Shape} EntityNumber
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EntityNumber.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EntityNumber message.
         * @function verify
         * @memberof proto.EntityNumber
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EntityNumber.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.number != null && $Object.hasOwnProperty.call(message, "number"))
                if (!$util.isInteger(message.number) && !(message.number && $util.isInteger(message.number.low) && $util.isInteger(message.number.high)))
                    return "number: integer|Long expected";
            return null;
        };

        /**
         * Creates an EntityNumber message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EntityNumber
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EntityNumber} EntityNumber
         */
        EntityNumber.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.EntityNumber)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.EntityNumber: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.EntityNumber();
            if (object.number != null)
                if (typeof object.number === "object" ? object.number.low || object.number.high : $Number(object.number) !== 0)
                    if ($util.Long)
                        message.number = $util.Long.fromValue(object.number, false);
                    else if (typeof object.number === "string")
                        message.number = $parseInt(object.number, 10);
                    else if (typeof object.number === "number")
                        message.number = object.number;
                    else if (typeof object.number === "object")
                        message.number = new $util.LongBits(object.number.low >>> 0, object.number.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an EntityNumber message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EntityNumber
         * @static
         * @param {proto.EntityNumber} message EntityNumber
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EntityNumber.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.number = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.number = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.number != null && $Object.hasOwnProperty.call(message, "number"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.number = typeof message.number === "number" ? $BigInt(message.number) : $util.Long.fromBits(message.number.low >>> 0, message.number.high >>> 0, false).toBigInt();
                else if (typeof message.number === "number")
                    object.number = options.longs === $String ? $String(message.number) : message.number;
                else
                    object.number = options.longs === $String ? $util.Long.prototype.toString.call(message.number) : options.longs === $Number ? new $util.LongBits(message.number.low >>> 0, message.number.high >>> 0).toNumber() : message.number;
            return object;
        };

        /**
         * Converts this EntityNumber to JSON.
         * @function toJSON
         * @memberof proto.EntityNumber
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EntityNumber.prototype.toJSON = function() {
            return EntityNumber.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EntityNumber
         * @function getTypeUrl
         * @memberof proto.EntityNumber
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EntityNumber.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EntityNumber";
        };

        return EntityNumber;
    })();

    proto.EntityIDPair = (function() {

        /**
         * Properties of an EntityIDPair.
         * @typedef {Object} proto.EntityIDPair.$Properties
         * @property {proto.AccountID.$Properties|null} [accountId] An account identifier for the associated account.
         * @property {proto.TokenID.$Properties|null} [tokenId] A token identifier for the associated token.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an EntityIDPair.
         * @memberof proto
         * @interface IEntityIDPair
         * @augments proto.EntityIDPair.$Properties
         * @deprecated Use proto.EntityIDPair.$Properties instead.
         */

        /**
         * Shape of an EntityIDPair.
         * @typedef {{
         *   accountId?: proto.AccountID.$Shape|null;
         *   tokenId?: proto.TokenID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.EntityIDPair.$Shape
         */

        /**
         * Constructs a new EntityIDPair.
         * @memberof proto
         * @classdesc A Pair of AccountID and TokenID.<br/>
         * This is used as a key in certain cases.
         * 
         * Deprecated.<br/>
         * The TokenAssociation message should be used instead of this message.
         * @constructor
         * @param {proto.EntityIDPair.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const EntityIDPair = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An account identifier for the associated account.
         * @member {proto.AccountID.$Properties|null|undefined} accountId
         * @memberof proto.EntityIDPair
         * @instance
         */
        EntityIDPair.prototype.accountId = null;

        /**
         * A token identifier for the associated token.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.EntityIDPair
         * @instance
         */
        EntityIDPair.prototype.tokenId = null;

        /**
         * Creates a new EntityIDPair instance using the specified properties.
         * @function create
         * @memberof proto.EntityIDPair
         * @static
         * @param {proto.EntityIDPair.$Properties=} [properties] Properties to set
         * @returns {proto.EntityIDPair} EntityIDPair instance
         * @type {{
         *   (properties: proto.EntityIDPair.$Shape): proto.EntityIDPair & proto.EntityIDPair.$Shape;
         *   (properties?: proto.EntityIDPair.$Properties): proto.EntityIDPair;
         * }}
         */
        EntityIDPair.create = function(properties) {
            return new EntityIDPair(properties);
        };

        /**
         * Encodes the specified EntityIDPair message. Does not implicitly {@link proto.EntityIDPair.verify|verify} messages.
         * @function encode
         * @memberof proto.EntityIDPair
         * @static
         * @param {proto.EntityIDPair.$Properties} message EntityIDPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EntityIDPair.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EntityIDPair message, length delimited. Does not implicitly {@link proto.EntityIDPair.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EntityIDPair
         * @static
         * @param {proto.EntityIDPair.$Properties} message EntityIDPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EntityIDPair.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an EntityIDPair message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EntityIDPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EntityIDPair & proto.EntityIDPair.$Shape} EntityIDPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EntityIDPair.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.EntityIDPair();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an EntityIDPair message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EntityIDPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EntityIDPair & proto.EntityIDPair.$Shape} EntityIDPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EntityIDPair.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EntityIDPair message.
         * @function verify
         * @memberof proto.EntityIDPair
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EntityIDPair.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                if (error)
                    return "accountId." + error;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            return null;
        };

        /**
         * Creates an EntityIDPair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EntityIDPair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EntityIDPair} EntityIDPair
         */
        EntityIDPair.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.EntityIDPair)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.EntityIDPair: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.EntityIDPair();
            if (object.accountId != null) {
                if (!$util.isObject(object.accountId))
                    throw $TypeError(".proto.EntityIDPair.accountId: object expected");
                message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
            }
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.EntityIDPair.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an EntityIDPair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EntityIDPair
         * @static
         * @param {proto.EntityIDPair} message EntityIDPair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EntityIDPair.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.accountId = null;
                object.tokenId = null;
            }
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            return object;
        };

        /**
         * Converts this EntityIDPair to JSON.
         * @function toJSON
         * @memberof proto.EntityIDPair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EntityIDPair.prototype.toJSON = function() {
            return EntityIDPair.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for EntityIDPair
         * @function getTypeUrl
         * @memberof proto.EntityIDPair
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        EntityIDPair.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EntityIDPair";
        };

        return EntityIDPair;
    })();

    proto.CongestionLevelStarts = (function() {

        /**
         * Properties of a CongestionLevelStarts.
         * @typedef {Object} proto.CongestionLevelStarts.$Properties
         * @property {Array.<proto.Timestamp.$Properties>|null} [genericLevelStarts] Timestamps for each point where "entity utilization" congestion pricing
         * levels changed.
         * <p>
         * If congestion pricing has not occurred then this SHALL contain a single
         * timestamp of value 0.
         * @property {Array.<proto.Timestamp.$Properties>|null} [gasLevelStarts] Timestamps for each point where "gas utilization" congestion pricing
         * levels changed.
         * <p>
         * If congestion pricing has not occurred then this SHALL contain a single
         * timestamp of value 0.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CongestionLevelStarts.
         * @memberof proto
         * @interface ICongestionLevelStarts
         * @augments proto.CongestionLevelStarts.$Properties
         * @deprecated Use proto.CongestionLevelStarts.$Properties instead.
         */

        /**
         * Shape of a CongestionLevelStarts.
         * @typedef {proto.CongestionLevelStarts.$Properties} proto.CongestionLevelStarts.$Shape
         */

        /**
         * Constructs a new CongestionLevelStarts.
         * @memberof proto
         * @classdesc Two lists of congestion pricing level "start" times. Each list details the
         * start of each time period when the congestion pricing level changed
         * (increasing, or decreasing, the congestion fee multiplier).
         * @constructor
         * @param {proto.CongestionLevelStarts.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const CongestionLevelStarts = function (properties) {
            this.genericLevelStarts = [];
            this.gasLevelStarts = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Timestamps for each point where "entity utilization" congestion pricing
         * levels changed.
         * <p>
         * If congestion pricing has not occurred then this SHALL contain a single
         * timestamp of value 0.
         * @member {Array.<proto.Timestamp.$Properties>} genericLevelStarts
         * @memberof proto.CongestionLevelStarts
         * @instance
         */
        CongestionLevelStarts.prototype.genericLevelStarts = $util.emptyArray;

        /**
         * Timestamps for each point where "gas utilization" congestion pricing
         * levels changed.
         * <p>
         * If congestion pricing has not occurred then this SHALL contain a single
         * timestamp of value 0.
         * @member {Array.<proto.Timestamp.$Properties>} gasLevelStarts
         * @memberof proto.CongestionLevelStarts
         * @instance
         */
        CongestionLevelStarts.prototype.gasLevelStarts = $util.emptyArray;

        /**
         * Creates a new CongestionLevelStarts instance using the specified properties.
         * @function create
         * @memberof proto.CongestionLevelStarts
         * @static
         * @param {proto.CongestionLevelStarts.$Properties=} [properties] Properties to set
         * @returns {proto.CongestionLevelStarts} CongestionLevelStarts instance
         * @type {{
         *   (properties: proto.CongestionLevelStarts.$Shape): proto.CongestionLevelStarts & proto.CongestionLevelStarts.$Shape;
         *   (properties?: proto.CongestionLevelStarts.$Properties): proto.CongestionLevelStarts;
         * }}
         */
        CongestionLevelStarts.create = function(properties) {
            return new CongestionLevelStarts(properties);
        };

        /**
         * Encodes the specified CongestionLevelStarts message. Does not implicitly {@link proto.CongestionLevelStarts.verify|verify} messages.
         * @function encode
         * @memberof proto.CongestionLevelStarts
         * @static
         * @param {proto.CongestionLevelStarts.$Properties} message CongestionLevelStarts message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CongestionLevelStarts.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.genericLevelStarts != null && message.genericLevelStarts.length)
                for (let i = 0; i < message.genericLevelStarts.length; ++i)
                    $root.proto.Timestamp.encode(message.genericLevelStarts[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.gasLevelStarts != null && message.gasLevelStarts.length)
                for (let i = 0; i < message.gasLevelStarts.length; ++i)
                    $root.proto.Timestamp.encode(message.gasLevelStarts[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CongestionLevelStarts message, length delimited. Does not implicitly {@link proto.CongestionLevelStarts.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CongestionLevelStarts
         * @static
         * @param {proto.CongestionLevelStarts.$Properties} message CongestionLevelStarts message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CongestionLevelStarts.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CongestionLevelStarts message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CongestionLevelStarts
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CongestionLevelStarts & proto.CongestionLevelStarts.$Shape} CongestionLevelStarts
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CongestionLevelStarts.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.CongestionLevelStarts();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.genericLevelStarts && message.genericLevelStarts.length))
                            message.genericLevelStarts = [];
                        message.genericLevelStarts.push($root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.gasLevelStarts && message.gasLevelStarts.length))
                            message.gasLevelStarts = [];
                        message.gasLevelStarts.push($root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CongestionLevelStarts message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CongestionLevelStarts
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CongestionLevelStarts & proto.CongestionLevelStarts.$Shape} CongestionLevelStarts
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CongestionLevelStarts.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CongestionLevelStarts message.
         * @function verify
         * @memberof proto.CongestionLevelStarts
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CongestionLevelStarts.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.genericLevelStarts != null && $Object.hasOwnProperty.call(message, "genericLevelStarts")) {
                if (!$Array.isArray(message.genericLevelStarts))
                    return "genericLevelStarts: array expected";
                for (let i = 0; i < message.genericLevelStarts.length; ++i) {
                    let error = $root.proto.Timestamp.verify(message.genericLevelStarts[i], _depth + 1);
                    if (error)
                        return "genericLevelStarts." + error;
                }
            }
            if (message.gasLevelStarts != null && $Object.hasOwnProperty.call(message, "gasLevelStarts")) {
                if (!$Array.isArray(message.gasLevelStarts))
                    return "gasLevelStarts: array expected";
                for (let i = 0; i < message.gasLevelStarts.length; ++i) {
                    let error = $root.proto.Timestamp.verify(message.gasLevelStarts[i], _depth + 1);
                    if (error)
                        return "gasLevelStarts." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CongestionLevelStarts message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CongestionLevelStarts
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CongestionLevelStarts} CongestionLevelStarts
         */
        CongestionLevelStarts.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.CongestionLevelStarts)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.CongestionLevelStarts: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.CongestionLevelStarts();
            if (object.genericLevelStarts) {
                if (!$Array.isArray(object.genericLevelStarts))
                    throw $TypeError(".proto.CongestionLevelStarts.genericLevelStarts: array expected");
                message.genericLevelStarts = $Array(object.genericLevelStarts.length);
                for (let i = 0; i < object.genericLevelStarts.length; ++i) {
                    if (!$util.isObject(object.genericLevelStarts[i]))
                        throw $TypeError(".proto.CongestionLevelStarts.genericLevelStarts: object expected");
                    message.genericLevelStarts[i] = $root.proto.Timestamp.fromObject(object.genericLevelStarts[i], _depth + 1);
                }
            }
            if (object.gasLevelStarts) {
                if (!$Array.isArray(object.gasLevelStarts))
                    throw $TypeError(".proto.CongestionLevelStarts.gasLevelStarts: array expected");
                message.gasLevelStarts = $Array(object.gasLevelStarts.length);
                for (let i = 0; i < object.gasLevelStarts.length; ++i) {
                    if (!$util.isObject(object.gasLevelStarts[i]))
                        throw $TypeError(".proto.CongestionLevelStarts.gasLevelStarts: object expected");
                    message.gasLevelStarts[i] = $root.proto.Timestamp.fromObject(object.gasLevelStarts[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CongestionLevelStarts message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CongestionLevelStarts
         * @static
         * @param {proto.CongestionLevelStarts} message CongestionLevelStarts
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CongestionLevelStarts.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.genericLevelStarts = [];
                object.gasLevelStarts = [];
            }
            if (message.genericLevelStarts && message.genericLevelStarts.length) {
                object.genericLevelStarts = $Array(message.genericLevelStarts.length);
                for (let j = 0; j < message.genericLevelStarts.length; ++j)
                    object.genericLevelStarts[j] = $root.proto.Timestamp.toObject(message.genericLevelStarts[j], options, _depth + 1);
            }
            if (message.gasLevelStarts && message.gasLevelStarts.length) {
                object.gasLevelStarts = $Array(message.gasLevelStarts.length);
                for (let j = 0; j < message.gasLevelStarts.length; ++j)
                    object.gasLevelStarts[j] = $root.proto.Timestamp.toObject(message.gasLevelStarts[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this CongestionLevelStarts to JSON.
         * @function toJSON
         * @memberof proto.CongestionLevelStarts
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CongestionLevelStarts.prototype.toJSON = function() {
            return CongestionLevelStarts.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CongestionLevelStarts
         * @function getTypeUrl
         * @memberof proto.CongestionLevelStarts
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CongestionLevelStarts.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CongestionLevelStarts";
        };

        return CongestionLevelStarts;
    })();

    proto.Topic = (function() {

        /**
         * Properties of a Topic.
         * @typedef {Object} proto.Topic.$Properties
         * @property {proto.TopicID.$Properties|null} [topicId] The topic's ID.
         * <p>
         * This value SHALL be unique within the network.
         * @property {number|Long|null} [sequenceNumber] The number of messages sent to the topic.
         * @property {number|Long|null} [expirationSecond] The expiration time for this topic, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.
         * @property {number|Long|null} [autoRenewPeriod] The number of seconds for which the topic will be automatically renewed
         * upon expiring (if it has a valid auto-renew account).
         * @property {proto.AccountID.$Properties|null} [autoRenewAccountId] The id of the account (if any) that the network will attempt to charge
         * fees to complete auto-renewal of this topic, upon expiration.
         * @property {boolean|null} [deleted] A flag indicating that this topic is deleted.
         * @property {Uint8Array|null} [runningHash] The current running hash of this topic.
         * <p>
         * This 48-byte field is the output of a SHA-384 digest with input data
         * determined by the current version of the running hash algorithm used
         * by the network.<br/>
         * All topics in state SHALL use running hash algorithm version `3`.<br/>
         * The bytes of each uint64 or uint32 encoded for the hash input MUST be in
         * Big-Endian format.
         * <p>
         * <hr/>
         * If the algorithm version is '3', then the input data to the SHA-384
         * digest are, in order:
         * <ol>
         * <li>The previous running hash of the topic (48 bytes)</li>
         * <li>The `topicRunningHashVersion` (8 bytes)</li>
         * <li>The payer account's shard (8 bytes)</li>
         * <li>The payer account's realm (8 bytes)</li>
         * <li>The payer account's number (8 bytes)</li>
         * <li>The topic's shard (8 bytes)</li>
         * <li>The topic's realm (8 bytes)</li>
         * <li>The topic's number (8 bytes)</li>
         * <li>The number of seconds since the epoch when the
         * `ConsensusSubmitMessage` reached consensus (8 bytes)</li>
         * <li>The number of nanoseconds within the second when the
         * `ConsensusSubmitMessage` reached consensus (4 bytes)</li>
         * <li>The `topicSequenceNumber` (8 bytes)</li>
         * <li>The output of a SHA-384 digest of the message bytes from the
         * `ConsensusSubmitMessage` (48 bytes)</li>
         * </ol>
         * <blockquote>Note that older messages on a topic, which are available in
         * the block stream, MAY use older algorithm versions, and the block stream
         * record incorporates a running hash version field to ensure the correct
         * hash calculation for each such historical message.</blockquote>
         * @property {string|null} [memo] A short description of this topic.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {proto.Key.$Properties|null} [adminKey] Access control for modification of the topic.
         * <p>
         * If this field is set, that key MUST sign each message to update or delete
         * this topic.<br/>
         * A topic without an admin key SHALL be immutable, except for expiration
         * and renewal.<br/>
         * If this field is not set, the `auto_renew_account_id` MUST NOT be set.
         * @property {proto.Key.$Properties|null} [submitKey] Access control for message submission to the topic.
         * <p>
         * If this field is set, that key MUST sign each consensus submit message
         * for this topic.
         * @property {proto.Key.$Properties|null} [feeScheduleKey] Access control for update/delete of custom fees.
         * <p>
         * If this field is unset, the current custom fees CANNOT be changed.<br/>
         * If this field is set, that `Key` MUST sign any transaction to update
         * the custom fee schedule for this topic.
         * @property {Array.<proto.Key.$Properties>|null} [feeExemptKeyList] A set of "privileged payer" keys<br/>
         * Keys in this list are permitted to submit messages to this topic without
         * paying custom fees associated with this topic.
         * <p>
         * If a submit transaction is signed by _any_ key included in this set,
         * custom fees SHALL NOT be charged for that transaction.<br/>
         * A `fee_exempt_key_list` MUST NOT contain more than
         * `MAX_ENTRIES_FOR_FEE_EXEMPT_KEY_LIST` keys.<br/>
         * A `fee_exempt_key_list` MUST NOT contain any duplicate keys.<br/>
         * A `fee_exempt_key_list` MAY contain keys for accounts that are inactive,
         * deleted, or non-existent.
         * If not set, there SHALL NOT be any fee-exempt keys.  In particular, the
         * following keys SHALL NOT be implicitly or automatically added to this
         * list: `adminKey`, `submitKey`, `fee_schedule_key`.
         * @property {Array.<proto.FixedCustomFee.$Properties>|null} [customFees] A set of custom fee definitions.<br/>
         * These are fees to be assessed for each submit to this topic.
         * <p>
         * If this list is empty, the only fees charged for a submit to this
         * topic SHALL be the network and node fees.<br/>
         * If this list is not empty, each fee defined in this set SHALL
         * be evaluated for each message submitted to this topic, and the
         * resultant total assessed fees SHALL be charged.<br/>
         * If this list is not empty, custom fees defined here SHALL be
         * charged _in addition to_ the base network and node fees.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Topic.
         * @memberof proto
         * @interface ITopic
         * @augments proto.Topic.$Properties
         * @deprecated Use proto.Topic.$Properties instead.
         */

        /**
         * Shape of a Topic.
         * @typedef {{
         *   topicId?: proto.TopicID.$Shape|null;
         *   sequenceNumber?: number|Long|null;
         *   expirationSecond?: number|Long|null;
         *   autoRenewPeriod?: number|Long|null;
         *   autoRenewAccountId?: proto.AccountID.$Shape|null;
         *   deleted?: boolean|null;
         *   runningHash?: Uint8Array|null;
         *   memo?: string|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   submitKey?: proto.Key.$Shape|null;
         *   feeScheduleKey?: proto.Key.$Shape|null;
         *   feeExemptKeyList?: Array.<proto.Key.$Shape>|null;
         *   customFees?: Array.<proto.FixedCustomFee.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.Topic.$Shape
         */

        /**
         * Constructs a new Topic.
         * @memberof proto
         * @classdesc Representation of an Hedera Consensus Service(HCS) topic.
         * 
         * As with all network entities, a topic has a unique entity number, which is
         * usually given along with the network's shard and realm in the form of a
         * shard.realm.number id.<br/>
         * An HCS topic is an ordered logical stream of messages united and secured by
         * a running hash of those messages. The integrity of any message on a topic,
         * and the topic stream as a whole, can always be ascertained from block stream
         * data by recomputing the running hash across any subset of messages on the
         * topic.<br/>
         * The messages on a topic SHALL NOT be stored in network state, but are
         * available in the network block stream, and may be queried via the
         * Mirror Node system.
         * @constructor
         * @param {proto.Topic.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Topic = function (properties) {
            this.feeExemptKeyList = [];
            this.customFees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The topic's ID.
         * <p>
         * This value SHALL be unique within the network.
         * @member {proto.TopicID.$Properties|null|undefined} topicId
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.topicId = null;

        /**
         * The number of messages sent to the topic.
         * @member {number|Long} sequenceNumber
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.sequenceNumber = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The expiration time for this topic, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.
         * @member {number|Long} expirationSecond
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.expirationSecond = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The number of seconds for which the topic will be automatically renewed
         * upon expiring (if it has a valid auto-renew account).
         * @member {number|Long} autoRenewPeriod
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.autoRenewPeriod = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The id of the account (if any) that the network will attempt to charge
         * fees to complete auto-renewal of this topic, upon expiration.
         * @member {proto.AccountID.$Properties|null|undefined} autoRenewAccountId
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.autoRenewAccountId = null;

        /**
         * A flag indicating that this topic is deleted.
         * @member {boolean} deleted
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.deleted = false;

        /**
         * The current running hash of this topic.
         * <p>
         * This 48-byte field is the output of a SHA-384 digest with input data
         * determined by the current version of the running hash algorithm used
         * by the network.<br/>
         * All topics in state SHALL use running hash algorithm version `3`.<br/>
         * The bytes of each uint64 or uint32 encoded for the hash input MUST be in
         * Big-Endian format.
         * <p>
         * <hr/>
         * If the algorithm version is '3', then the input data to the SHA-384
         * digest are, in order:
         * <ol>
         * <li>The previous running hash of the topic (48 bytes)</li>
         * <li>The `topicRunningHashVersion` (8 bytes)</li>
         * <li>The payer account's shard (8 bytes)</li>
         * <li>The payer account's realm (8 bytes)</li>
         * <li>The payer account's number (8 bytes)</li>
         * <li>The topic's shard (8 bytes)</li>
         * <li>The topic's realm (8 bytes)</li>
         * <li>The topic's number (8 bytes)</li>
         * <li>The number of seconds since the epoch when the
         * `ConsensusSubmitMessage` reached consensus (8 bytes)</li>
         * <li>The number of nanoseconds within the second when the
         * `ConsensusSubmitMessage` reached consensus (4 bytes)</li>
         * <li>The `topicSequenceNumber` (8 bytes)</li>
         * <li>The output of a SHA-384 digest of the message bytes from the
         * `ConsensusSubmitMessage` (48 bytes)</li>
         * </ol>
         * <blockquote>Note that older messages on a topic, which are available in
         * the block stream, MAY use older algorithm versions, and the block stream
         * record incorporates a running hash version field to ensure the correct
         * hash calculation for each such historical message.</blockquote>
         * @member {Uint8Array} runningHash
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.runningHash = $util.newBuffer([]);

        /**
         * A short description of this topic.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.memo = "";

        /**
         * Access control for modification of the topic.
         * <p>
         * If this field is set, that key MUST sign each message to update or delete
         * this topic.<br/>
         * A topic without an admin key SHALL be immutable, except for expiration
         * and renewal.<br/>
         * If this field is not set, the `auto_renew_account_id` MUST NOT be set.
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.adminKey = null;

        /**
         * Access control for message submission to the topic.
         * <p>
         * If this field is set, that key MUST sign each consensus submit message
         * for this topic.
         * @member {proto.Key.$Properties|null|undefined} submitKey
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.submitKey = null;

        /**
         * Access control for update/delete of custom fees.
         * <p>
         * If this field is unset, the current custom fees CANNOT be changed.<br/>
         * If this field is set, that `Key` MUST sign any transaction to update
         * the custom fee schedule for this topic.
         * @member {proto.Key.$Properties|null|undefined} feeScheduleKey
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.feeScheduleKey = null;

        /**
         * A set of "privileged payer" keys<br/>
         * Keys in this list are permitted to submit messages to this topic without
         * paying custom fees associated with this topic.
         * <p>
         * If a submit transaction is signed by _any_ key included in this set,
         * custom fees SHALL NOT be charged for that transaction.<br/>
         * A `fee_exempt_key_list` MUST NOT contain more than
         * `MAX_ENTRIES_FOR_FEE_EXEMPT_KEY_LIST` keys.<br/>
         * A `fee_exempt_key_list` MUST NOT contain any duplicate keys.<br/>
         * A `fee_exempt_key_list` MAY contain keys for accounts that are inactive,
         * deleted, or non-existent.
         * If not set, there SHALL NOT be any fee-exempt keys.  In particular, the
         * following keys SHALL NOT be implicitly or automatically added to this
         * list: `adminKey`, `submitKey`, `fee_schedule_key`.
         * @member {Array.<proto.Key.$Properties>} feeExemptKeyList
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.feeExemptKeyList = $util.emptyArray;

        /**
         * A set of custom fee definitions.<br/>
         * These are fees to be assessed for each submit to this topic.
         * <p>
         * If this list is empty, the only fees charged for a submit to this
         * topic SHALL be the network and node fees.<br/>
         * If this list is not empty, each fee defined in this set SHALL
         * be evaluated for each message submitted to this topic, and the
         * resultant total assessed fees SHALL be charged.<br/>
         * If this list is not empty, custom fees defined here SHALL be
         * charged _in addition to_ the base network and node fees.
         * @member {Array.<proto.FixedCustomFee.$Properties>} customFees
         * @memberof proto.Topic
         * @instance
         */
        Topic.prototype.customFees = $util.emptyArray;

        /**
         * Creates a new Topic instance using the specified properties.
         * @function create
         * @memberof proto.Topic
         * @static
         * @param {proto.Topic.$Properties=} [properties] Properties to set
         * @returns {proto.Topic} Topic instance
         * @type {{
         *   (properties: proto.Topic.$Shape): proto.Topic & proto.Topic.$Shape;
         *   (properties?: proto.Topic.$Properties): proto.Topic;
         * }}
         */
        Topic.create = function(properties) {
            return new Topic(properties);
        };

        /**
         * Encodes the specified Topic message. Does not implicitly {@link proto.Topic.verify|verify} messages.
         * @function encode
         * @memberof proto.Topic
         * @static
         * @param {proto.Topic.$Properties} message Topic message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Topic.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.topicId != null && $Object.hasOwnProperty.call(message, "topicId"))
                $root.proto.TopicID.encode(message.topicId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.sequenceNumber != null && $Object.hasOwnProperty.call(message, "sequenceNumber") && (typeof message.sequenceNumber === "object" ? message.sequenceNumber.low || message.sequenceNumber.high : message.sequenceNumber !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.sequenceNumber);
            if (message.expirationSecond != null && $Object.hasOwnProperty.call(message, "expirationSecond") && (typeof message.expirationSecond === "object" ? message.expirationSecond.low || message.expirationSecond.high : message.expirationSecond !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.expirationSecond);
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod") && (typeof message.autoRenewPeriod === "object" ? message.autoRenewPeriod.low || message.autoRenewPeriod.high : message.autoRenewPeriod !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.autoRenewPeriod);
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId"))
                $root.proto.AccountID.encode(message.autoRenewAccountId, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.deleted);
            if (message.runningHash != null && $Object.hasOwnProperty.call(message, "runningHash") && message.runningHash.length)
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.runningHash);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.memo);
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.submitKey != null && $Object.hasOwnProperty.call(message, "submitKey"))
                $root.proto.Key.encode(message.submitKey, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                $root.proto.Key.encode(message.feeScheduleKey, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.feeExemptKeyList != null && message.feeExemptKeyList.length)
                for (let i = 0; i < message.feeExemptKeyList.length; ++i)
                    $root.proto.Key.encode(message.feeExemptKeyList[i], writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.customFees != null && message.customFees.length)
                for (let i = 0; i < message.customFees.length; ++i)
                    $root.proto.FixedCustomFee.encode(message.customFees[i], writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Topic message, length delimited. Does not implicitly {@link proto.Topic.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Topic
         * @static
         * @param {proto.Topic.$Properties} message Topic message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Topic.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Topic message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Topic
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Topic & proto.Topic.$Shape} Topic
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Topic.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Topic();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.topicId = $root.proto.TopicID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.topicId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.sequenceNumber = value;
                        else
                            delete message.sequenceNumber;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expirationSecond = value;
                        else
                            delete message.expirationSecond;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.autoRenewPeriod = value;
                        else
                            delete message.autoRenewPeriod;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewAccountId);
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.deleted = value;
                        else
                            delete message.deleted;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.runningHash = value;
                        else
                            delete message.runningHash;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.submitKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.submitKey);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.feeScheduleKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeScheduleKey);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.feeExemptKeyList && message.feeExemptKeyList.length))
                            message.feeExemptKeyList = [];
                        message.feeExemptKeyList.push($root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if (!(message.customFees && message.customFees.length))
                            message.customFees = [];
                        message.customFees.push($root.proto.FixedCustomFee.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Topic message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Topic
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Topic & proto.Topic.$Shape} Topic
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Topic.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Topic message.
         * @function verify
         * @memberof proto.Topic
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Topic.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.topicId != null && $Object.hasOwnProperty.call(message, "topicId")) {
                let error = $root.proto.TopicID.verify(message.topicId, _depth + 1);
                if (error)
                    return "topicId." + error;
            }
            if (message.sequenceNumber != null && $Object.hasOwnProperty.call(message, "sequenceNumber"))
                if (!$util.isInteger(message.sequenceNumber) && !(message.sequenceNumber && $util.isInteger(message.sequenceNumber.low) && $util.isInteger(message.sequenceNumber.high)))
                    return "sequenceNumber: integer|Long expected";
            if (message.expirationSecond != null && $Object.hasOwnProperty.call(message, "expirationSecond"))
                if (!$util.isInteger(message.expirationSecond) && !(message.expirationSecond && $util.isInteger(message.expirationSecond.low) && $util.isInteger(message.expirationSecond.high)))
                    return "expirationSecond: integer|Long expected";
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                if (!$util.isInteger(message.autoRenewPeriod) && !(message.autoRenewPeriod && $util.isInteger(message.autoRenewPeriod.low) && $util.isInteger(message.autoRenewPeriod.high)))
                    return "autoRenewPeriod: integer|Long expected";
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId")) {
                let error = $root.proto.AccountID.verify(message.autoRenewAccountId, _depth + 1);
                if (error)
                    return "autoRenewAccountId." + error;
            }
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                if (typeof message.deleted !== "boolean")
                    return "deleted: boolean expected";
            if (message.runningHash != null && $Object.hasOwnProperty.call(message, "runningHash"))
                if (!(message.runningHash && typeof message.runningHash.length === "number" || $util.isString(message.runningHash)))
                    return "runningHash: buffer expected";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.submitKey != null && $Object.hasOwnProperty.call(message, "submitKey")) {
                let error = $root.proto.Key.verify(message.submitKey, _depth + 1);
                if (error)
                    return "submitKey." + error;
            }
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey")) {
                let error = $root.proto.Key.verify(message.feeScheduleKey, _depth + 1);
                if (error)
                    return "feeScheduleKey." + error;
            }
            if (message.feeExemptKeyList != null && $Object.hasOwnProperty.call(message, "feeExemptKeyList")) {
                if (!$Array.isArray(message.feeExemptKeyList))
                    return "feeExemptKeyList: array expected";
                for (let i = 0; i < message.feeExemptKeyList.length; ++i) {
                    let error = $root.proto.Key.verify(message.feeExemptKeyList[i], _depth + 1);
                    if (error)
                        return "feeExemptKeyList." + error;
                }
            }
            if (message.customFees != null && $Object.hasOwnProperty.call(message, "customFees")) {
                if (!$Array.isArray(message.customFees))
                    return "customFees: array expected";
                for (let i = 0; i < message.customFees.length; ++i) {
                    let error = $root.proto.FixedCustomFee.verify(message.customFees[i], _depth + 1);
                    if (error)
                        return "customFees." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Topic message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Topic
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Topic} Topic
         */
        Topic.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Topic)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Topic: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Topic();
            if (object.topicId != null) {
                if (!$util.isObject(object.topicId))
                    throw $TypeError(".proto.Topic.topicId: object expected");
                message.topicId = $root.proto.TopicID.fromObject(object.topicId, _depth + 1);
            }
            if (object.sequenceNumber != null)
                if (typeof object.sequenceNumber === "object" ? object.sequenceNumber.low || object.sequenceNumber.high : $Number(object.sequenceNumber) !== 0)
                    if ($util.Long)
                        message.sequenceNumber = $util.Long.fromValue(object.sequenceNumber, false);
                    else if (typeof object.sequenceNumber === "string")
                        message.sequenceNumber = $parseInt(object.sequenceNumber, 10);
                    else if (typeof object.sequenceNumber === "number")
                        message.sequenceNumber = object.sequenceNumber;
                    else if (typeof object.sequenceNumber === "object")
                        message.sequenceNumber = new $util.LongBits(object.sequenceNumber.low >>> 0, object.sequenceNumber.high >>> 0).toNumber();
            if (object.expirationSecond != null)
                if (typeof object.expirationSecond === "object" ? object.expirationSecond.low || object.expirationSecond.high : $Number(object.expirationSecond) !== 0)
                    if ($util.Long)
                        message.expirationSecond = $util.Long.fromValue(object.expirationSecond, false);
                    else if (typeof object.expirationSecond === "string")
                        message.expirationSecond = $parseInt(object.expirationSecond, 10);
                    else if (typeof object.expirationSecond === "number")
                        message.expirationSecond = object.expirationSecond;
                    else if (typeof object.expirationSecond === "object")
                        message.expirationSecond = new $util.LongBits(object.expirationSecond.low >>> 0, object.expirationSecond.high >>> 0).toNumber();
            if (object.autoRenewPeriod != null)
                if (typeof object.autoRenewPeriod === "object" ? object.autoRenewPeriod.low || object.autoRenewPeriod.high : $Number(object.autoRenewPeriod) !== 0)
                    if ($util.Long)
                        message.autoRenewPeriod = $util.Long.fromValue(object.autoRenewPeriod, false);
                    else if (typeof object.autoRenewPeriod === "string")
                        message.autoRenewPeriod = $parseInt(object.autoRenewPeriod, 10);
                    else if (typeof object.autoRenewPeriod === "number")
                        message.autoRenewPeriod = object.autoRenewPeriod;
                    else if (typeof object.autoRenewPeriod === "object")
                        message.autoRenewPeriod = new $util.LongBits(object.autoRenewPeriod.low >>> 0, object.autoRenewPeriod.high >>> 0).toNumber();
            if (object.autoRenewAccountId != null) {
                if (!$util.isObject(object.autoRenewAccountId))
                    throw $TypeError(".proto.Topic.autoRenewAccountId: object expected");
                message.autoRenewAccountId = $root.proto.AccountID.fromObject(object.autoRenewAccountId, _depth + 1);
            }
            if (object.deleted != null)
                if (object.deleted)
                    message.deleted = $Boolean(object.deleted);
            if (object.runningHash != null)
                if (object.runningHash.length)
                    if (typeof object.runningHash === "string")
                        $util.base64.decode(object.runningHash, message.runningHash = $util.newBuffer($util.base64.length(object.runningHash)), 0);
                    else if (object.runningHash.length >= 0)
                        message.runningHash = object.runningHash;
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.Topic.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.submitKey != null) {
                if (!$util.isObject(object.submitKey))
                    throw $TypeError(".proto.Topic.submitKey: object expected");
                message.submitKey = $root.proto.Key.fromObject(object.submitKey, _depth + 1);
            }
            if (object.feeScheduleKey != null) {
                if (!$util.isObject(object.feeScheduleKey))
                    throw $TypeError(".proto.Topic.feeScheduleKey: object expected");
                message.feeScheduleKey = $root.proto.Key.fromObject(object.feeScheduleKey, _depth + 1);
            }
            if (object.feeExemptKeyList) {
                if (!$Array.isArray(object.feeExemptKeyList))
                    throw $TypeError(".proto.Topic.feeExemptKeyList: array expected");
                message.feeExemptKeyList = $Array(object.feeExemptKeyList.length);
                for (let i = 0; i < object.feeExemptKeyList.length; ++i) {
                    if (!$util.isObject(object.feeExemptKeyList[i]))
                        throw $TypeError(".proto.Topic.feeExemptKeyList: object expected");
                    message.feeExemptKeyList[i] = $root.proto.Key.fromObject(object.feeExemptKeyList[i], _depth + 1);
                }
            }
            if (object.customFees) {
                if (!$Array.isArray(object.customFees))
                    throw $TypeError(".proto.Topic.customFees: array expected");
                message.customFees = $Array(object.customFees.length);
                for (let i = 0; i < object.customFees.length; ++i) {
                    if (!$util.isObject(object.customFees[i]))
                        throw $TypeError(".proto.Topic.customFees: object expected");
                    message.customFees[i] = $root.proto.FixedCustomFee.fromObject(object.customFees[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Topic message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Topic
         * @static
         * @param {proto.Topic} message Topic
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Topic.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.feeExemptKeyList = [];
                object.customFees = [];
            }
            if (options.defaults) {
                object.topicId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.sequenceNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sequenceNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expirationSecond = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expirationSecond = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.autoRenewPeriod = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.autoRenewPeriod = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.autoRenewAccountId = null;
                object.deleted = false;
                if (options.bytes === $String)
                    object.runningHash = "";
                else {
                    object.runningHash = [];
                    if (options.bytes !== $Array)
                        object.runningHash = $util.newBuffer(object.runningHash);
                }
                object.memo = "";
                object.adminKey = null;
                object.submitKey = null;
                object.feeScheduleKey = null;
            }
            if (message.topicId != null && $Object.hasOwnProperty.call(message, "topicId"))
                object.topicId = $root.proto.TopicID.toObject(message.topicId, options, _depth + 1);
            if (message.sequenceNumber != null && $Object.hasOwnProperty.call(message, "sequenceNumber"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sequenceNumber = typeof message.sequenceNumber === "number" ? $BigInt(message.sequenceNumber) : $util.Long.fromBits(message.sequenceNumber.low >>> 0, message.sequenceNumber.high >>> 0, false).toBigInt();
                else if (typeof message.sequenceNumber === "number")
                    object.sequenceNumber = options.longs === $String ? $String(message.sequenceNumber) : message.sequenceNumber;
                else
                    object.sequenceNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.sequenceNumber) : options.longs === $Number ? new $util.LongBits(message.sequenceNumber.low >>> 0, message.sequenceNumber.high >>> 0).toNumber() : message.sequenceNumber;
            if (message.expirationSecond != null && $Object.hasOwnProperty.call(message, "expirationSecond"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expirationSecond = typeof message.expirationSecond === "number" ? $BigInt(message.expirationSecond) : $util.Long.fromBits(message.expirationSecond.low >>> 0, message.expirationSecond.high >>> 0, false).toBigInt();
                else if (typeof message.expirationSecond === "number")
                    object.expirationSecond = options.longs === $String ? $String(message.expirationSecond) : message.expirationSecond;
                else
                    object.expirationSecond = options.longs === $String ? $util.Long.prototype.toString.call(message.expirationSecond) : options.longs === $Number ? new $util.LongBits(message.expirationSecond.low >>> 0, message.expirationSecond.high >>> 0).toNumber() : message.expirationSecond;
            if (message.autoRenewPeriod != null && $Object.hasOwnProperty.call(message, "autoRenewPeriod"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.autoRenewPeriod = typeof message.autoRenewPeriod === "number" ? $BigInt(message.autoRenewPeriod) : $util.Long.fromBits(message.autoRenewPeriod.low >>> 0, message.autoRenewPeriod.high >>> 0, false).toBigInt();
                else if (typeof message.autoRenewPeriod === "number")
                    object.autoRenewPeriod = options.longs === $String ? $String(message.autoRenewPeriod) : message.autoRenewPeriod;
                else
                    object.autoRenewPeriod = options.longs === $String ? $util.Long.prototype.toString.call(message.autoRenewPeriod) : options.longs === $Number ? new $util.LongBits(message.autoRenewPeriod.low >>> 0, message.autoRenewPeriod.high >>> 0).toNumber() : message.autoRenewPeriod;
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId"))
                object.autoRenewAccountId = $root.proto.AccountID.toObject(message.autoRenewAccountId, options, _depth + 1);
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                object.deleted = message.deleted;
            if (message.runningHash != null && $Object.hasOwnProperty.call(message, "runningHash"))
                object.runningHash = options.bytes === $String ? $util.base64.encode(message.runningHash, 0, message.runningHash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.runningHash) : message.runningHash;
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.submitKey != null && $Object.hasOwnProperty.call(message, "submitKey"))
                object.submitKey = $root.proto.Key.toObject(message.submitKey, options, _depth + 1);
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                object.feeScheduleKey = $root.proto.Key.toObject(message.feeScheduleKey, options, _depth + 1);
            if (message.feeExemptKeyList && message.feeExemptKeyList.length) {
                object.feeExemptKeyList = $Array(message.feeExemptKeyList.length);
                for (let j = 0; j < message.feeExemptKeyList.length; ++j)
                    object.feeExemptKeyList[j] = $root.proto.Key.toObject(message.feeExemptKeyList[j], options, _depth + 1);
            }
            if (message.customFees && message.customFees.length) {
                object.customFees = $Array(message.customFees.length);
                for (let j = 0; j < message.customFees.length; ++j)
                    object.customFees[j] = $root.proto.FixedCustomFee.toObject(message.customFees[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this Topic to JSON.
         * @function toJSON
         * @memberof proto.Topic
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Topic.prototype.toJSON = function() {
            return Topic.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Topic
         * @function getTypeUrl
         * @memberof proto.Topic
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Topic.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Topic";
        };

        return Topic;
    })();

    proto.Bytecode = (function() {

        /**
         * Properties of a Bytecode.
         * @typedef {Object} proto.Bytecode.$Properties
         * @property {Uint8Array|null} [code] The raw bytes (not hex-encoded) of a contract's bytecode.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Bytecode.
         * @memberof proto
         * @interface IBytecode
         * @augments proto.Bytecode.$Properties
         * @deprecated Use proto.Bytecode.$Properties instead.
         */

        /**
         * Shape of a Bytecode.
         * @typedef {proto.Bytecode.$Properties} proto.Bytecode.$Shape
         */

        /**
         * Constructs a new Bytecode.
         * @memberof proto
         * @classdesc The bytecode for a contract account.
         * 
         * This is not referred to by any other protocol buffer, but is used internally
         * within the Hedera Node software.
         * @constructor
         * @param {proto.Bytecode.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Bytecode = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The raw bytes (not hex-encoded) of a contract's bytecode.
         * @member {Uint8Array} code
         * @memberof proto.Bytecode
         * @instance
         */
        Bytecode.prototype.code = $util.newBuffer([]);

        /**
         * Creates a new Bytecode instance using the specified properties.
         * @function create
         * @memberof proto.Bytecode
         * @static
         * @param {proto.Bytecode.$Properties=} [properties] Properties to set
         * @returns {proto.Bytecode} Bytecode instance
         * @type {{
         *   (properties: proto.Bytecode.$Shape): proto.Bytecode & proto.Bytecode.$Shape;
         *   (properties?: proto.Bytecode.$Properties): proto.Bytecode;
         * }}
         */
        Bytecode.create = function(properties) {
            return new Bytecode(properties);
        };

        /**
         * Encodes the specified Bytecode message. Does not implicitly {@link proto.Bytecode.verify|verify} messages.
         * @function encode
         * @memberof proto.Bytecode
         * @static
         * @param {proto.Bytecode.$Properties} message Bytecode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Bytecode.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.code != null && $Object.hasOwnProperty.call(message, "code") && message.code.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.code);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Bytecode message, length delimited. Does not implicitly {@link proto.Bytecode.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Bytecode
         * @static
         * @param {proto.Bytecode.$Properties} message Bytecode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Bytecode.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Bytecode message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Bytecode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Bytecode & proto.Bytecode.$Shape} Bytecode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Bytecode.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Bytecode();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Bytecode message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Bytecode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Bytecode & proto.Bytecode.$Shape} Bytecode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Bytecode.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Bytecode message.
         * @function verify
         * @memberof proto.Bytecode
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Bytecode.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                if (!(message.code && typeof message.code.length === "number" || $util.isString(message.code)))
                    return "code: buffer expected";
            return null;
        };

        /**
         * Creates a Bytecode message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Bytecode
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Bytecode} Bytecode
         */
        Bytecode.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Bytecode)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Bytecode: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Bytecode();
            if (object.code != null)
                if (object.code.length)
                    if (typeof object.code === "string")
                        $util.base64.decode(object.code, message.code = $util.newBuffer($util.base64.length(object.code)), 0);
                    else if (object.code.length >= 0)
                        message.code = object.code;
            return message;
        };

        /**
         * Creates a plain object from a Bytecode message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Bytecode
         * @static
         * @param {proto.Bytecode} message Bytecode
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Bytecode.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.code = "";
                else {
                    object.code = [];
                    if (options.bytes !== $Array)
                        object.code = $util.newBuffer(object.code);
                }
            if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                object.code = options.bytes === $String ? $util.base64.encode(message.code, 0, message.code.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.code) : message.code;
            return object;
        };

        /**
         * Converts this Bytecode to JSON.
         * @function toJSON
         * @memberof proto.Bytecode
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Bytecode.prototype.toJSON = function() {
            return Bytecode.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Bytecode
         * @function getTypeUrl
         * @memberof proto.Bytecode
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Bytecode.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Bytecode";
        };

        return Bytecode;
    })();

    proto.SlotKey = (function() {

        /**
         * Properties of a SlotKey.
         * @typedef {Object} proto.SlotKey.$Properties
         * @property {proto.ContractID.$Properties|null} [contractID] The Contract ID of the contract that owns (and pays for) this slot.
         * @property {Uint8Array|null} [key] The EVM key of this slot, left-padded with zeros to form a 256-bit word.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SlotKey.
         * @memberof proto
         * @interface ISlotKey
         * @augments proto.SlotKey.$Properties
         * @deprecated Use proto.SlotKey.$Properties instead.
         */

        /**
         * Shape of a SlotKey.
         * @typedef {{
         *   contractID?: proto.ContractID.$Shape|null;
         *   key?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.SlotKey.$Shape
         */

        /**
         * Constructs a new SlotKey.
         * @memberof proto
         * @classdesc The key of a storage slot. A slot is scoped to a specific contract ID.
         * 
         * For each contract, its EVM storage is a mapping of 256-bit keys (or "words")
         * to 256-bit values.
         * @constructor
         * @param {proto.SlotKey.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SlotKey = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The Contract ID of the contract that owns (and pays for) this slot.
         * @member {proto.ContractID.$Properties|null|undefined} contractID
         * @memberof proto.SlotKey
         * @instance
         */
        SlotKey.prototype.contractID = null;

        /**
         * The EVM key of this slot, left-padded with zeros to form a 256-bit word.
         * @member {Uint8Array} key
         * @memberof proto.SlotKey
         * @instance
         */
        SlotKey.prototype.key = $util.newBuffer([]);

        /**
         * Creates a new SlotKey instance using the specified properties.
         * @function create
         * @memberof proto.SlotKey
         * @static
         * @param {proto.SlotKey.$Properties=} [properties] Properties to set
         * @returns {proto.SlotKey} SlotKey instance
         * @type {{
         *   (properties: proto.SlotKey.$Shape): proto.SlotKey & proto.SlotKey.$Shape;
         *   (properties?: proto.SlotKey.$Properties): proto.SlotKey;
         * }}
         */
        SlotKey.create = function(properties) {
            return new SlotKey(properties);
        };

        /**
         * Encodes the specified SlotKey message. Does not implicitly {@link proto.SlotKey.verify|verify} messages.
         * @function encode
         * @memberof proto.SlotKey
         * @static
         * @param {proto.SlotKey.$Properties} message SlotKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SlotKey.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                $root.proto.ContractID.encode(message.contractID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.key);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SlotKey message, length delimited. Does not implicitly {@link proto.SlotKey.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SlotKey
         * @static
         * @param {proto.SlotKey.$Properties} message SlotKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SlotKey.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SlotKey message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SlotKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SlotKey & proto.SlotKey.$Shape} SlotKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SlotKey.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SlotKey();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.contractID = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SlotKey message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SlotKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SlotKey & proto.SlotKey.$Shape} SlotKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SlotKey.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SlotKey message.
         * @function verify
         * @memberof proto.SlotKey
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SlotKey.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID")) {
                let error = $root.proto.ContractID.verify(message.contractID, _depth + 1);
                if (error)
                    return "contractID." + error;
            }
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                if (!(message.key && typeof message.key.length === "number" || $util.isString(message.key)))
                    return "key: buffer expected";
            return null;
        };

        /**
         * Creates a SlotKey message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SlotKey
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SlotKey} SlotKey
         */
        SlotKey.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SlotKey)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SlotKey: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SlotKey();
            if (object.contractID != null) {
                if (!$util.isObject(object.contractID))
                    throw $TypeError(".proto.SlotKey.contractID: object expected");
                message.contractID = $root.proto.ContractID.fromObject(object.contractID, _depth + 1);
            }
            if (object.key != null)
                if (object.key.length)
                    if (typeof object.key === "string")
                        $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                    else if (object.key.length >= 0)
                        message.key = object.key;
            return message;
        };

        /**
         * Creates a plain object from a SlotKey message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SlotKey
         * @static
         * @param {proto.SlotKey} message SlotKey
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SlotKey.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.contractID = null;
                if (options.bytes === $String)
                    object.key = "";
                else {
                    object.key = [];
                    if (options.bytes !== $Array)
                        object.key = $util.newBuffer(object.key);
                }
            }
            if (message.contractID != null && $Object.hasOwnProperty.call(message, "contractID"))
                object.contractID = $root.proto.ContractID.toObject(message.contractID, options, _depth + 1);
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = options.bytes === $String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.key) : message.key;
            return object;
        };

        /**
         * Converts this SlotKey to JSON.
         * @function toJSON
         * @memberof proto.SlotKey
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SlotKey.prototype.toJSON = function() {
            return SlotKey.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SlotKey
         * @function getTypeUrl
         * @memberof proto.SlotKey
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SlotKey.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SlotKey";
        };

        return SlotKey;
    })();

    proto.SlotValue = (function() {

        /**
         * Properties of a SlotValue.
         * @typedef {Object} proto.SlotValue.$Properties
         * @property {Uint8Array|null} [value] The EVM value in this slot, left-padded with zeros to form a 256-bit word.
         * @property {Uint8Array|null} [previousKey] The word of the previous key in this contract's storage list (if any).
         * @property {Uint8Array|null} [nextKey] The word of the next key in this contract's storage list (if any).
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SlotValue.
         * @memberof proto
         * @interface ISlotValue
         * @augments proto.SlotValue.$Properties
         * @deprecated Use proto.SlotValue.$Properties instead.
         */

        /**
         * Shape of a SlotValue.
         * @typedef {proto.SlotValue.$Properties} proto.SlotValue.$Shape
         */

        /**
         * Constructs a new SlotValue.
         * @memberof proto
         * @classdesc The value of a contract storage slot. For the EVM, this is a single "word".
         * 
         * Because we iterate through all the storage slots for an expired contract
         * when purging it from state, our slot values also include the words of the
         * previous and next keys in this contract's storage "virtual linked list".
         * @constructor
         * @param {proto.SlotValue.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SlotValue = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The EVM value in this slot, left-padded with zeros to form a 256-bit word.
         * @member {Uint8Array} value
         * @memberof proto.SlotValue
         * @instance
         */
        SlotValue.prototype.value = $util.newBuffer([]);

        /**
         * The word of the previous key in this contract's storage list (if any).
         * @member {Uint8Array} previousKey
         * @memberof proto.SlotValue
         * @instance
         */
        SlotValue.prototype.previousKey = $util.newBuffer([]);

        /**
         * The word of the next key in this contract's storage list (if any).
         * @member {Uint8Array} nextKey
         * @memberof proto.SlotValue
         * @instance
         */
        SlotValue.prototype.nextKey = $util.newBuffer([]);

        /**
         * Creates a new SlotValue instance using the specified properties.
         * @function create
         * @memberof proto.SlotValue
         * @static
         * @param {proto.SlotValue.$Properties=} [properties] Properties to set
         * @returns {proto.SlotValue} SlotValue instance
         * @type {{
         *   (properties: proto.SlotValue.$Shape): proto.SlotValue & proto.SlotValue.$Shape;
         *   (properties?: proto.SlotValue.$Properties): proto.SlotValue;
         * }}
         */
        SlotValue.create = function(properties) {
            return new SlotValue(properties);
        };

        /**
         * Encodes the specified SlotValue message. Does not implicitly {@link proto.SlotValue.verify|verify} messages.
         * @function encode
         * @memberof proto.SlotValue
         * @static
         * @param {proto.SlotValue.$Properties} message SlotValue message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SlotValue.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.value);
            if (message.previousKey != null && $Object.hasOwnProperty.call(message, "previousKey") && message.previousKey.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.previousKey);
            if (message.nextKey != null && $Object.hasOwnProperty.call(message, "nextKey") && message.nextKey.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.nextKey);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SlotValue message, length delimited. Does not implicitly {@link proto.SlotValue.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SlotValue
         * @static
         * @param {proto.SlotValue.$Properties} message SlotValue message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SlotValue.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SlotValue message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SlotValue
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SlotValue & proto.SlotValue.$Shape} SlotValue
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SlotValue.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SlotValue();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.previousKey = value;
                        else
                            delete message.previousKey;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.nextKey = value;
                        else
                            delete message.nextKey;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SlotValue message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SlotValue
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SlotValue & proto.SlotValue.$Shape} SlotValue
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SlotValue.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SlotValue message.
         * @function verify
         * @memberof proto.SlotValue
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SlotValue.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!(message.value && typeof message.value.length === "number" || $util.isString(message.value)))
                    return "value: buffer expected";
            if (message.previousKey != null && $Object.hasOwnProperty.call(message, "previousKey"))
                if (!(message.previousKey && typeof message.previousKey.length === "number" || $util.isString(message.previousKey)))
                    return "previousKey: buffer expected";
            if (message.nextKey != null && $Object.hasOwnProperty.call(message, "nextKey"))
                if (!(message.nextKey && typeof message.nextKey.length === "number" || $util.isString(message.nextKey)))
                    return "nextKey: buffer expected";
            return null;
        };

        /**
         * Creates a SlotValue message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SlotValue
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SlotValue} SlotValue
         */
        SlotValue.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SlotValue)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SlotValue: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SlotValue();
            if (object.value != null)
                if (object.value.length)
                    if (typeof object.value === "string")
                        $util.base64.decode(object.value, message.value = $util.newBuffer($util.base64.length(object.value)), 0);
                    else if (object.value.length >= 0)
                        message.value = object.value;
            if (object.previousKey != null)
                if (object.previousKey.length)
                    if (typeof object.previousKey === "string")
                        $util.base64.decode(object.previousKey, message.previousKey = $util.newBuffer($util.base64.length(object.previousKey)), 0);
                    else if (object.previousKey.length >= 0)
                        message.previousKey = object.previousKey;
            if (object.nextKey != null)
                if (object.nextKey.length)
                    if (typeof object.nextKey === "string")
                        $util.base64.decode(object.nextKey, message.nextKey = $util.newBuffer($util.base64.length(object.nextKey)), 0);
                    else if (object.nextKey.length >= 0)
                        message.nextKey = object.nextKey;
            return message;
        };

        /**
         * Creates a plain object from a SlotValue message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SlotValue
         * @static
         * @param {proto.SlotValue} message SlotValue
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SlotValue.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.value = "";
                else {
                    object.value = [];
                    if (options.bytes !== $Array)
                        object.value = $util.newBuffer(object.value);
                }
                if (options.bytes === $String)
                    object.previousKey = "";
                else {
                    object.previousKey = [];
                    if (options.bytes !== $Array)
                        object.previousKey = $util.newBuffer(object.previousKey);
                }
                if (options.bytes === $String)
                    object.nextKey = "";
                else {
                    object.nextKey = [];
                    if (options.bytes !== $Array)
                        object.nextKey = $util.newBuffer(object.nextKey);
                }
            }
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = options.bytes === $String ? $util.base64.encode(message.value, 0, message.value.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.value) : message.value;
            if (message.previousKey != null && $Object.hasOwnProperty.call(message, "previousKey"))
                object.previousKey = options.bytes === $String ? $util.base64.encode(message.previousKey, 0, message.previousKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.previousKey) : message.previousKey;
            if (message.nextKey != null && $Object.hasOwnProperty.call(message, "nextKey"))
                object.nextKey = options.bytes === $String ? $util.base64.encode(message.nextKey, 0, message.nextKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.nextKey) : message.nextKey;
            return object;
        };

        /**
         * Converts this SlotValue to JSON.
         * @function toJSON
         * @memberof proto.SlotValue
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SlotValue.prototype.toJSON = function() {
            return SlotValue.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SlotValue
         * @function getTypeUrl
         * @memberof proto.SlotValue
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SlotValue.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SlotValue";
        };

        return SlotValue;
    })();

    proto.File = (function() {

        /**
         * Properties of a File.
         * @typedef {Object} proto.File.$Properties
         * @property {proto.FileID.$Properties|null} [fileId] This file's ID within the global network state.
         * <p>
         * This value SHALL be unique within the network.
         * @property {number|Long|null} [expirationSecond] The file's expiration time in seconds since the epoch.<br/>
         * This value should be compared against consensus time, which may not
         * exactly match clock time at the moment of expiration.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.
         * @property {proto.KeyList.$Properties|null} [keys] A list of keys that MUST sign any transaction to create
         * or update this file.
         * <p>
         * Only _one_ of these keys must sign a transaction to delete the file.<br/>
         * This field MAY be `null` or an empty list.<br/>
         * If this field is null or an empty `KeyList`, then the file SHALL be
         * immutable.<br/>
         * For an immutable file, the only transaction permitted to modify that
         * file SHALL be a `fileUpdate` transaction with _only_ the
         * `expirationTime` set.
         * @property {Uint8Array|null} [contents] The contents of the file.
         * <p>
         * This SHALL be limited to the current maximum file size; typically no
         * more than 1 Megabyte (1048576 bytes).
         * @property {string|null} [memo] A short description of the file.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {boolean|null} [deleted] A flag indicating that this file is deleted.
         * <p>
         * The `contents` of a deleted "regular" file SHALL be an empty
         * (zero length) bytes.
         * @property {number|Long|null} [preSystemDeleteExpirationSecond] The pre-system-delete expiration time of a deleted "system" file, in seconds.
         * <p>
         * This field SHALL contain the original expiration time of a "system" file
         * that is deleted. This SHOULD be used to restore that expiration time if
         * the file is subsequently "un-deleted" before it is purged from the
         * system.<br/>
         * A "regular" file cannot be "un-deleted", so this field SHALL NOT be set
         * for those files.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a File.
         * @memberof proto
         * @interface IFile
         * @augments proto.File.$Properties
         * @deprecated Use proto.File.$Properties instead.
         */

        /**
         * Shape of a File.
         * @typedef {{
         *   fileId?: proto.FileID.$Shape|null;
         *   expirationSecond?: number|Long|null;
         *   keys?: proto.KeyList.$Shape|null;
         *   contents?: Uint8Array|null;
         *   memo?: string|null;
         *   deleted?: boolean|null;
         *   preSystemDeleteExpirationSecond?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.File.$Shape
         */

        /**
         * Constructs a new File.
         * @memberof proto
         * @classdesc Representation of an Hedera File Service `file`.
         * 
         * Files offer a place to store additional data, much more than is available in
         * other entities, for use with smart contracts, non-fungible tokens, etc...
         * As with all network entities, a file has a unique entity number, which is
         * given along with the network's shard and realm in the form of a
         * shard.realm.number id.
         * @constructor
         * @param {proto.File.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const File = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * This file's ID within the global network state.
         * <p>
         * This value SHALL be unique within the network.
         * @member {proto.FileID.$Properties|null|undefined} fileId
         * @memberof proto.File
         * @instance
         */
        File.prototype.fileId = null;

        /**
         * The file's expiration time in seconds since the epoch.<br/>
         * This value should be compared against consensus time, which may not
         * exactly match clock time at the moment of expiration.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.
         * @member {number|Long} expirationSecond
         * @memberof proto.File
         * @instance
         */
        File.prototype.expirationSecond = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A list of keys that MUST sign any transaction to create
         * or update this file.
         * <p>
         * Only _one_ of these keys must sign a transaction to delete the file.<br/>
         * This field MAY be `null` or an empty list.<br/>
         * If this field is null or an empty `KeyList`, then the file SHALL be
         * immutable.<br/>
         * For an immutable file, the only transaction permitted to modify that
         * file SHALL be a `fileUpdate` transaction with _only_ the
         * `expirationTime` set.
         * @member {proto.KeyList.$Properties|null|undefined} keys
         * @memberof proto.File
         * @instance
         */
        File.prototype.keys = null;

        /**
         * The contents of the file.
         * <p>
         * This SHALL be limited to the current maximum file size; typically no
         * more than 1 Megabyte (1048576 bytes).
         * @member {Uint8Array} contents
         * @memberof proto.File
         * @instance
         */
        File.prototype.contents = $util.newBuffer([]);

        /**
         * A short description of the file.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.File
         * @instance
         */
        File.prototype.memo = "";

        /**
         * A flag indicating that this file is deleted.
         * <p>
         * The `contents` of a deleted "regular" file SHALL be an empty
         * (zero length) bytes.
         * @member {boolean} deleted
         * @memberof proto.File
         * @instance
         */
        File.prototype.deleted = false;

        /**
         * The pre-system-delete expiration time of a deleted "system" file, in seconds.
         * <p>
         * This field SHALL contain the original expiration time of a "system" file
         * that is deleted. This SHOULD be used to restore that expiration time if
         * the file is subsequently "un-deleted" before it is purged from the
         * system.<br/>
         * A "regular" file cannot be "un-deleted", so this field SHALL NOT be set
         * for those files.
         * @member {number|Long} preSystemDeleteExpirationSecond
         * @memberof proto.File
         * @instance
         */
        File.prototype.preSystemDeleteExpirationSecond = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new File instance using the specified properties.
         * @function create
         * @memberof proto.File
         * @static
         * @param {proto.File.$Properties=} [properties] Properties to set
         * @returns {proto.File} File instance
         * @type {{
         *   (properties: proto.File.$Shape): proto.File & proto.File.$Shape;
         *   (properties?: proto.File.$Properties): proto.File;
         * }}
         */
        File.create = function(properties) {
            return new File(properties);
        };

        /**
         * Encodes the specified File message. Does not implicitly {@link proto.File.verify|verify} messages.
         * @function encode
         * @memberof proto.File
         * @static
         * @param {proto.File.$Properties} message File message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        File.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fileId != null && $Object.hasOwnProperty.call(message, "fileId"))
                $root.proto.FileID.encode(message.fileId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.expirationSecond != null && $Object.hasOwnProperty.call(message, "expirationSecond") && (typeof message.expirationSecond === "object" ? message.expirationSecond.low || message.expirationSecond.high : message.expirationSecond !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.expirationSecond);
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys"))
                $root.proto.KeyList.encode(message.keys, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.contents != null && $Object.hasOwnProperty.call(message, "contents") && message.contents.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.contents);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.memo);
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.deleted);
            if (message.preSystemDeleteExpirationSecond != null && $Object.hasOwnProperty.call(message, "preSystemDeleteExpirationSecond") && (typeof message.preSystemDeleteExpirationSecond === "object" ? message.preSystemDeleteExpirationSecond.low || message.preSystemDeleteExpirationSecond.high : message.preSystemDeleteExpirationSecond !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.preSystemDeleteExpirationSecond);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified File message, length delimited. Does not implicitly {@link proto.File.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.File
         * @static
         * @param {proto.File.$Properties} message File message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        File.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a File message from the specified reader or buffer.
         * @function decode
         * @memberof proto.File
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.File & proto.File.$Shape} File
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        File.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.File();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.fileId = $root.proto.FileID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expirationSecond = value;
                        else
                            delete message.expirationSecond;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.keys = $root.proto.KeyList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.keys);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.contents = value;
                        else
                            delete message.contents;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.deleted = value;
                        else
                            delete message.deleted;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.preSystemDeleteExpirationSecond = value;
                        else
                            delete message.preSystemDeleteExpirationSecond;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a File message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.File
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.File & proto.File.$Shape} File
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        File.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a File message.
         * @function verify
         * @memberof proto.File
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        File.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fileId != null && $Object.hasOwnProperty.call(message, "fileId")) {
                let error = $root.proto.FileID.verify(message.fileId, _depth + 1);
                if (error)
                    return "fileId." + error;
            }
            if (message.expirationSecond != null && $Object.hasOwnProperty.call(message, "expirationSecond"))
                if (!$util.isInteger(message.expirationSecond) && !(message.expirationSecond && $util.isInteger(message.expirationSecond.low) && $util.isInteger(message.expirationSecond.high)))
                    return "expirationSecond: integer|Long expected";
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys")) {
                let error = $root.proto.KeyList.verify(message.keys, _depth + 1);
                if (error)
                    return "keys." + error;
            }
            if (message.contents != null && $Object.hasOwnProperty.call(message, "contents"))
                if (!(message.contents && typeof message.contents.length === "number" || $util.isString(message.contents)))
                    return "contents: buffer expected";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                if (typeof message.deleted !== "boolean")
                    return "deleted: boolean expected";
            if (message.preSystemDeleteExpirationSecond != null && $Object.hasOwnProperty.call(message, "preSystemDeleteExpirationSecond"))
                if (!$util.isInteger(message.preSystemDeleteExpirationSecond) && !(message.preSystemDeleteExpirationSecond && $util.isInteger(message.preSystemDeleteExpirationSecond.low) && $util.isInteger(message.preSystemDeleteExpirationSecond.high)))
                    return "preSystemDeleteExpirationSecond: integer|Long expected";
            return null;
        };

        /**
         * Creates a File message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.File
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.File} File
         */
        File.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.File)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.File: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.File();
            if (object.fileId != null) {
                if (!$util.isObject(object.fileId))
                    throw $TypeError(".proto.File.fileId: object expected");
                message.fileId = $root.proto.FileID.fromObject(object.fileId, _depth + 1);
            }
            if (object.expirationSecond != null)
                if (typeof object.expirationSecond === "object" ? object.expirationSecond.low || object.expirationSecond.high : $Number(object.expirationSecond) !== 0)
                    if ($util.Long)
                        message.expirationSecond = $util.Long.fromValue(object.expirationSecond, false);
                    else if (typeof object.expirationSecond === "string")
                        message.expirationSecond = $parseInt(object.expirationSecond, 10);
                    else if (typeof object.expirationSecond === "number")
                        message.expirationSecond = object.expirationSecond;
                    else if (typeof object.expirationSecond === "object")
                        message.expirationSecond = new $util.LongBits(object.expirationSecond.low >>> 0, object.expirationSecond.high >>> 0).toNumber();
            if (object.keys != null) {
                if (!$util.isObject(object.keys))
                    throw $TypeError(".proto.File.keys: object expected");
                message.keys = $root.proto.KeyList.fromObject(object.keys, _depth + 1);
            }
            if (object.contents != null)
                if (object.contents.length)
                    if (typeof object.contents === "string")
                        $util.base64.decode(object.contents, message.contents = $util.newBuffer($util.base64.length(object.contents)), 0);
                    else if (object.contents.length >= 0)
                        message.contents = object.contents;
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.deleted != null)
                if (object.deleted)
                    message.deleted = $Boolean(object.deleted);
            if (object.preSystemDeleteExpirationSecond != null)
                if (typeof object.preSystemDeleteExpirationSecond === "object" ? object.preSystemDeleteExpirationSecond.low || object.preSystemDeleteExpirationSecond.high : $Number(object.preSystemDeleteExpirationSecond) !== 0)
                    if ($util.Long)
                        message.preSystemDeleteExpirationSecond = $util.Long.fromValue(object.preSystemDeleteExpirationSecond, false);
                    else if (typeof object.preSystemDeleteExpirationSecond === "string")
                        message.preSystemDeleteExpirationSecond = $parseInt(object.preSystemDeleteExpirationSecond, 10);
                    else if (typeof object.preSystemDeleteExpirationSecond === "number")
                        message.preSystemDeleteExpirationSecond = object.preSystemDeleteExpirationSecond;
                    else if (typeof object.preSystemDeleteExpirationSecond === "object")
                        message.preSystemDeleteExpirationSecond = new $util.LongBits(object.preSystemDeleteExpirationSecond.low >>> 0, object.preSystemDeleteExpirationSecond.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a File message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.File
         * @static
         * @param {proto.File} message File
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        File.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.fileId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expirationSecond = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expirationSecond = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.keys = null;
                if (options.bytes === $String)
                    object.contents = "";
                else {
                    object.contents = [];
                    if (options.bytes !== $Array)
                        object.contents = $util.newBuffer(object.contents);
                }
                object.memo = "";
                object.deleted = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.preSystemDeleteExpirationSecond = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.preSystemDeleteExpirationSecond = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.fileId != null && $Object.hasOwnProperty.call(message, "fileId"))
                object.fileId = $root.proto.FileID.toObject(message.fileId, options, _depth + 1);
            if (message.expirationSecond != null && $Object.hasOwnProperty.call(message, "expirationSecond"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expirationSecond = typeof message.expirationSecond === "number" ? $BigInt(message.expirationSecond) : $util.Long.fromBits(message.expirationSecond.low >>> 0, message.expirationSecond.high >>> 0, false).toBigInt();
                else if (typeof message.expirationSecond === "number")
                    object.expirationSecond = options.longs === $String ? $String(message.expirationSecond) : message.expirationSecond;
                else
                    object.expirationSecond = options.longs === $String ? $util.Long.prototype.toString.call(message.expirationSecond) : options.longs === $Number ? new $util.LongBits(message.expirationSecond.low >>> 0, message.expirationSecond.high >>> 0).toNumber() : message.expirationSecond;
            if (message.keys != null && $Object.hasOwnProperty.call(message, "keys"))
                object.keys = $root.proto.KeyList.toObject(message.keys, options, _depth + 1);
            if (message.contents != null && $Object.hasOwnProperty.call(message, "contents"))
                object.contents = options.bytes === $String ? $util.base64.encode(message.contents, 0, message.contents.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.contents) : message.contents;
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                object.deleted = message.deleted;
            if (message.preSystemDeleteExpirationSecond != null && $Object.hasOwnProperty.call(message, "preSystemDeleteExpirationSecond"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.preSystemDeleteExpirationSecond = typeof message.preSystemDeleteExpirationSecond === "number" ? $BigInt(message.preSystemDeleteExpirationSecond) : $util.Long.fromBits(message.preSystemDeleteExpirationSecond.low >>> 0, message.preSystemDeleteExpirationSecond.high >>> 0, false).toBigInt();
                else if (typeof message.preSystemDeleteExpirationSecond === "number")
                    object.preSystemDeleteExpirationSecond = options.longs === $String ? $String(message.preSystemDeleteExpirationSecond) : message.preSystemDeleteExpirationSecond;
                else
                    object.preSystemDeleteExpirationSecond = options.longs === $String ? $util.Long.prototype.toString.call(message.preSystemDeleteExpirationSecond) : options.longs === $Number ? new $util.LongBits(message.preSystemDeleteExpirationSecond.low >>> 0, message.preSystemDeleteExpirationSecond.high >>> 0).toNumber() : message.preSystemDeleteExpirationSecond;
            return object;
        };

        /**
         * Converts this File to JSON.
         * @function toJSON
         * @memberof proto.File
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        File.prototype.toJSON = function() {
            return File.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for File
         * @function getTypeUrl
         * @memberof proto.File
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        File.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.File";
        };

        return File;
    })();

    proto.ProtoLong = (function() {

        /**
         * Properties of a ProtoLong.
         * @typedef {Object} proto.ProtoLong.$Properties
         * @property {number|Long|null} [value] ProtoLong value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ProtoLong.
         * @memberof proto
         * @interface IProtoLong
         * @augments proto.ProtoLong.$Properties
         * @deprecated Use proto.ProtoLong.$Properties instead.
         */

        /**
         * Shape of a ProtoLong.
         * @typedef {proto.ProtoLong.$Properties} proto.ProtoLong.$Shape
         */

        /**
         * Constructs a new ProtoLong.
         * @memberof proto
         * @classdesc A single 64-bit number with no particular meaning.
         * @constructor
         * @param {proto.ProtoLong.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ProtoLong = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ProtoLong value.
         * @member {number|Long} value
         * @memberof proto.ProtoLong
         * @instance
         */
        ProtoLong.prototype.value = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ProtoLong instance using the specified properties.
         * @function create
         * @memberof proto.ProtoLong
         * @static
         * @param {proto.ProtoLong.$Properties=} [properties] Properties to set
         * @returns {proto.ProtoLong} ProtoLong instance
         * @type {{
         *   (properties: proto.ProtoLong.$Shape): proto.ProtoLong & proto.ProtoLong.$Shape;
         *   (properties?: proto.ProtoLong.$Properties): proto.ProtoLong;
         * }}
         */
        ProtoLong.create = function(properties) {
            return new ProtoLong(properties);
        };

        /**
         * Encodes the specified ProtoLong message. Does not implicitly {@link proto.ProtoLong.verify|verify} messages.
         * @function encode
         * @memberof proto.ProtoLong
         * @static
         * @param {proto.ProtoLong.$Properties} message ProtoLong message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProtoLong.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && (typeof message.value === "object" ? message.value.low || message.value.high : message.value !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.value);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ProtoLong message, length delimited. Does not implicitly {@link proto.ProtoLong.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ProtoLong
         * @static
         * @param {proto.ProtoLong.$Properties} message ProtoLong message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProtoLong.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ProtoLong message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ProtoLong
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ProtoLong & proto.ProtoLong.$Shape} ProtoLong
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProtoLong.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ProtoLong();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ProtoLong message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ProtoLong
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ProtoLong & proto.ProtoLong.$Shape} ProtoLong
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProtoLong.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ProtoLong message.
         * @function verify
         * @memberof proto.ProtoLong
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProtoLong.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isInteger(message.value) && !(message.value && $util.isInteger(message.value.low) && $util.isInteger(message.value.high)))
                    return "value: integer|Long expected";
            return null;
        };

        /**
         * Creates a ProtoLong message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ProtoLong
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ProtoLong} ProtoLong
         */
        ProtoLong.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ProtoLong)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ProtoLong: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ProtoLong();
            if (object.value != null)
                if (typeof object.value === "object" ? object.value.low || object.value.high : $Number(object.value) !== 0)
                    if ($util.Long)
                        message.value = $util.Long.fromValue(object.value, false);
                    else if (typeof object.value === "string")
                        message.value = $parseInt(object.value, 10);
                    else if (typeof object.value === "number")
                        message.value = object.value;
                    else if (typeof object.value === "object")
                        message.value = new $util.LongBits(object.value.low >>> 0, object.value.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ProtoLong message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ProtoLong
         * @static
         * @param {proto.ProtoLong} message ProtoLong
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ProtoLong.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.value = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.value = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.value = typeof message.value === "number" ? $BigInt(message.value) : $util.Long.fromBits(message.value.low >>> 0, message.value.high >>> 0, false).toBigInt();
                else if (typeof message.value === "number")
                    object.value = options.longs === $String ? $String(message.value) : message.value;
                else
                    object.value = options.longs === $String ? $util.Long.prototype.toString.call(message.value) : options.longs === $Number ? new $util.LongBits(message.value.low >>> 0, message.value.high >>> 0).toNumber() : message.value;
            return object;
        };

        /**
         * Converts this ProtoLong to JSON.
         * @function toJSON
         * @memberof proto.ProtoLong
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ProtoLong.prototype.toJSON = function() {
            return ProtoLong.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ProtoLong
         * @function getTypeUrl
         * @memberof proto.ProtoLong
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ProtoLong.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ProtoLong";
        };

        return ProtoLong;
    })();

    proto.ProtoInteger = (function() {

        /**
         * Properties of a ProtoInteger.
         * @typedef {Object} proto.ProtoInteger.$Properties
         * @property {number|null} [value] ProtoInteger value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ProtoInteger.
         * @memberof proto
         * @interface IProtoInteger
         * @augments proto.ProtoInteger.$Properties
         * @deprecated Use proto.ProtoInteger.$Properties instead.
         */

        /**
         * Shape of a ProtoInteger.
         * @typedef {proto.ProtoInteger.$Properties} proto.ProtoInteger.$Shape
         */

        /**
         * Constructs a new ProtoInteger.
         * @memberof proto
         * @classdesc A single 32-bit number with no particular meaning.
         * @constructor
         * @param {proto.ProtoInteger.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ProtoInteger = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ProtoInteger value.
         * @member {number} value
         * @memberof proto.ProtoInteger
         * @instance
         */
        ProtoInteger.prototype.value = 0;

        /**
         * Creates a new ProtoInteger instance using the specified properties.
         * @function create
         * @memberof proto.ProtoInteger
         * @static
         * @param {proto.ProtoInteger.$Properties=} [properties] Properties to set
         * @returns {proto.ProtoInteger} ProtoInteger instance
         * @type {{
         *   (properties: proto.ProtoInteger.$Shape): proto.ProtoInteger & proto.ProtoInteger.$Shape;
         *   (properties?: proto.ProtoInteger.$Properties): proto.ProtoInteger;
         * }}
         */
        ProtoInteger.create = function(properties) {
            return new ProtoInteger(properties);
        };

        /**
         * Encodes the specified ProtoInteger message. Does not implicitly {@link proto.ProtoInteger.verify|verify} messages.
         * @function encode
         * @memberof proto.ProtoInteger
         * @static
         * @param {proto.ProtoInteger.$Properties} message ProtoInteger message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProtoInteger.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.value);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ProtoInteger message, length delimited. Does not implicitly {@link proto.ProtoInteger.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ProtoInteger
         * @static
         * @param {proto.ProtoInteger.$Properties} message ProtoInteger message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProtoInteger.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ProtoInteger message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ProtoInteger
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ProtoInteger & proto.ProtoInteger.$Shape} ProtoInteger
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProtoInteger.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ProtoInteger();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ProtoInteger message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ProtoInteger
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ProtoInteger & proto.ProtoInteger.$Shape} ProtoInteger
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProtoInteger.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ProtoInteger message.
         * @function verify
         * @memberof proto.ProtoInteger
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProtoInteger.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isInteger(message.value))
                    return "value: integer expected";
            return null;
        };

        /**
         * Creates a ProtoInteger message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ProtoInteger
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ProtoInteger} ProtoInteger
         */
        ProtoInteger.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ProtoInteger)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ProtoInteger: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ProtoInteger();
            if (object.value != null)
                if ($Number(object.value) !== 0)
                    message.value = object.value | 0;
            return message;
        };

        /**
         * Creates a plain object from a ProtoInteger message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ProtoInteger
         * @static
         * @param {proto.ProtoInteger} message ProtoInteger
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ProtoInteger.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.value = 0;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this ProtoInteger to JSON.
         * @function toJSON
         * @memberof proto.ProtoInteger
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ProtoInteger.prototype.toJSON = function() {
            return ProtoInteger.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ProtoInteger
         * @function getTypeUrl
         * @memberof proto.ProtoInteger
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ProtoInteger.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ProtoInteger";
        };

        return ProtoInteger;
    })();

    proto.ProtoBoolean = (function() {

        /**
         * Properties of a ProtoBoolean.
         * @typedef {Object} proto.ProtoBoolean.$Properties
         * @property {boolean|null} [value] ProtoBoolean value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ProtoBoolean.
         * @memberof proto
         * @interface IProtoBoolean
         * @augments proto.ProtoBoolean.$Properties
         * @deprecated Use proto.ProtoBoolean.$Properties instead.
         */

        /**
         * Shape of a ProtoBoolean.
         * @typedef {proto.ProtoBoolean.$Properties} proto.ProtoBoolean.$Shape
         */

        /**
         * Constructs a new ProtoBoolean.
         * @memberof proto
         * @classdesc A single boolean with no particular meaning.
         * @constructor
         * @param {proto.ProtoBoolean.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ProtoBoolean = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ProtoBoolean value.
         * @member {boolean} value
         * @memberof proto.ProtoBoolean
         * @instance
         */
        ProtoBoolean.prototype.value = false;

        /**
         * Creates a new ProtoBoolean instance using the specified properties.
         * @function create
         * @memberof proto.ProtoBoolean
         * @static
         * @param {proto.ProtoBoolean.$Properties=} [properties] Properties to set
         * @returns {proto.ProtoBoolean} ProtoBoolean instance
         * @type {{
         *   (properties: proto.ProtoBoolean.$Shape): proto.ProtoBoolean & proto.ProtoBoolean.$Shape;
         *   (properties?: proto.ProtoBoolean.$Properties): proto.ProtoBoolean;
         * }}
         */
        ProtoBoolean.create = function(properties) {
            return new ProtoBoolean(properties);
        };

        /**
         * Encodes the specified ProtoBoolean message. Does not implicitly {@link proto.ProtoBoolean.verify|verify} messages.
         * @function encode
         * @memberof proto.ProtoBoolean
         * @static
         * @param {proto.ProtoBoolean.$Properties} message ProtoBoolean message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProtoBoolean.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.value);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ProtoBoolean message, length delimited. Does not implicitly {@link proto.ProtoBoolean.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ProtoBoolean
         * @static
         * @param {proto.ProtoBoolean.$Properties} message ProtoBoolean message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProtoBoolean.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ProtoBoolean message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ProtoBoolean
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ProtoBoolean & proto.ProtoBoolean.$Shape} ProtoBoolean
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProtoBoolean.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ProtoBoolean();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ProtoBoolean message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ProtoBoolean
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ProtoBoolean & proto.ProtoBoolean.$Shape} ProtoBoolean
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProtoBoolean.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ProtoBoolean message.
         * @function verify
         * @memberof proto.ProtoBoolean
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProtoBoolean.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof message.value !== "boolean")
                    return "value: boolean expected";
            return null;
        };

        /**
         * Creates a ProtoBoolean message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ProtoBoolean
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ProtoBoolean} ProtoBoolean
         */
        ProtoBoolean.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ProtoBoolean)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ProtoBoolean: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ProtoBoolean();
            if (object.value != null)
                if (object.value)
                    message.value = $Boolean(object.value);
            return message;
        };

        /**
         * Creates a plain object from a ProtoBoolean message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ProtoBoolean
         * @static
         * @param {proto.ProtoBoolean} message ProtoBoolean
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ProtoBoolean.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.value = false;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this ProtoBoolean to JSON.
         * @function toJSON
         * @memberof proto.ProtoBoolean
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ProtoBoolean.prototype.toJSON = function() {
            return ProtoBoolean.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ProtoBoolean
         * @function getTypeUrl
         * @memberof proto.ProtoBoolean
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ProtoBoolean.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ProtoBoolean";
        };

        return ProtoBoolean;
    })();

    proto.ProtoString = (function() {

        /**
         * Properties of a ProtoString.
         * @typedef {Object} proto.ProtoString.$Properties
         * @property {string|null} [value] ProtoString value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ProtoString.
         * @memberof proto
         * @interface IProtoString
         * @augments proto.ProtoString.$Properties
         * @deprecated Use proto.ProtoString.$Properties instead.
         */

        /**
         * Shape of a ProtoString.
         * @typedef {proto.ProtoString.$Properties} proto.ProtoString.$Shape
         */

        /**
         * Constructs a new ProtoString.
         * @memberof proto
         * @classdesc A single string with no particular meaning.
         * @constructor
         * @param {proto.ProtoString.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ProtoString = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ProtoString value.
         * @member {string} value
         * @memberof proto.ProtoString
         * @instance
         */
        ProtoString.prototype.value = "";

        /**
         * Creates a new ProtoString instance using the specified properties.
         * @function create
         * @memberof proto.ProtoString
         * @static
         * @param {proto.ProtoString.$Properties=} [properties] Properties to set
         * @returns {proto.ProtoString} ProtoString instance
         * @type {{
         *   (properties: proto.ProtoString.$Shape): proto.ProtoString & proto.ProtoString.$Shape;
         *   (properties?: proto.ProtoString.$Properties): proto.ProtoString;
         * }}
         */
        ProtoString.create = function(properties) {
            return new ProtoString(properties);
        };

        /**
         * Encodes the specified ProtoString message. Does not implicitly {@link proto.ProtoString.verify|verify} messages.
         * @function encode
         * @memberof proto.ProtoString
         * @static
         * @param {proto.ProtoString.$Properties} message ProtoString message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProtoString.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.value);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ProtoString message, length delimited. Does not implicitly {@link proto.ProtoString.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ProtoString
         * @static
         * @param {proto.ProtoString.$Properties} message ProtoString message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProtoString.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ProtoString message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ProtoString
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ProtoString & proto.ProtoString.$Shape} ProtoString
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProtoString.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ProtoString();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ProtoString message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ProtoString
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ProtoString & proto.ProtoString.$Shape} ProtoString
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProtoString.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ProtoString message.
         * @function verify
         * @memberof proto.ProtoString
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProtoString.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isString(message.value))
                    return "value: string expected";
            return null;
        };

        /**
         * Creates a ProtoString message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ProtoString
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ProtoString} ProtoString
         */
        ProtoString.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ProtoString)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ProtoString: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ProtoString();
            if (object.value != null)
                if (typeof object.value !== "string" || object.value.length)
                    message.value = $String(object.value);
            return message;
        };

        /**
         * Creates a plain object from a ProtoString message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ProtoString
         * @static
         * @param {proto.ProtoString} message ProtoString
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ProtoString.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.value = "";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this ProtoString to JSON.
         * @function toJSON
         * @memberof proto.ProtoString
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ProtoString.prototype.toJSON = function() {
            return ProtoString.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ProtoString
         * @function getTypeUrl
         * @memberof proto.ProtoString
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ProtoString.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ProtoString";
        };

        return ProtoString;
    })();

    proto.ProtoBytes = (function() {

        /**
         * Properties of a ProtoBytes.
         * @typedef {Object} proto.ProtoBytes.$Properties
         * @property {Uint8Array|null} [value] ProtoBytes value
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ProtoBytes.
         * @memberof proto
         * @interface IProtoBytes
         * @augments proto.ProtoBytes.$Properties
         * @deprecated Use proto.ProtoBytes.$Properties instead.
         */

        /**
         * Shape of a ProtoBytes.
         * @typedef {proto.ProtoBytes.$Properties} proto.ProtoBytes.$Shape
         */

        /**
         * Constructs a new ProtoBytes.
         * @memberof proto
         * @classdesc A single byte array with no particular meaning.
         * @constructor
         * @param {proto.ProtoBytes.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ProtoBytes = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ProtoBytes value.
         * @member {Uint8Array} value
         * @memberof proto.ProtoBytes
         * @instance
         */
        ProtoBytes.prototype.value = $util.newBuffer([]);

        /**
         * Creates a new ProtoBytes instance using the specified properties.
         * @function create
         * @memberof proto.ProtoBytes
         * @static
         * @param {proto.ProtoBytes.$Properties=} [properties] Properties to set
         * @returns {proto.ProtoBytes} ProtoBytes instance
         * @type {{
         *   (properties: proto.ProtoBytes.$Shape): proto.ProtoBytes & proto.ProtoBytes.$Shape;
         *   (properties?: proto.ProtoBytes.$Properties): proto.ProtoBytes;
         * }}
         */
        ProtoBytes.create = function(properties) {
            return new ProtoBytes(properties);
        };

        /**
         * Encodes the specified ProtoBytes message. Does not implicitly {@link proto.ProtoBytes.verify|verify} messages.
         * @function encode
         * @memberof proto.ProtoBytes
         * @static
         * @param {proto.ProtoBytes.$Properties} message ProtoBytes message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProtoBytes.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.value);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ProtoBytes message, length delimited. Does not implicitly {@link proto.ProtoBytes.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ProtoBytes
         * @static
         * @param {proto.ProtoBytes.$Properties} message ProtoBytes message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProtoBytes.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ProtoBytes message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ProtoBytes
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ProtoBytes & proto.ProtoBytes.$Shape} ProtoBytes
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProtoBytes.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ProtoBytes();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ProtoBytes message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ProtoBytes
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ProtoBytes & proto.ProtoBytes.$Shape} ProtoBytes
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProtoBytes.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ProtoBytes message.
         * @function verify
         * @memberof proto.ProtoBytes
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProtoBytes.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!(message.value && typeof message.value.length === "number" || $util.isString(message.value)))
                    return "value: buffer expected";
            return null;
        };

        /**
         * Creates a ProtoBytes message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ProtoBytes
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ProtoBytes} ProtoBytes
         */
        ProtoBytes.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ProtoBytes)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ProtoBytes: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ProtoBytes();
            if (object.value != null)
                if (object.value.length)
                    if (typeof object.value === "string")
                        $util.base64.decode(object.value, message.value = $util.newBuffer($util.base64.length(object.value)), 0);
                    else if (object.value.length >= 0)
                        message.value = object.value;
            return message;
        };

        /**
         * Creates a plain object from a ProtoBytes message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ProtoBytes
         * @static
         * @param {proto.ProtoBytes} message ProtoBytes
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ProtoBytes.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.value = "";
                else {
                    object.value = [];
                    if (options.bytes !== $Array)
                        object.value = $util.newBuffer(object.value);
                }
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = options.bytes === $String ? $util.base64.encode(message.value, 0, message.value.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.value) : message.value;
            return object;
        };

        /**
         * Converts this ProtoBytes to JSON.
         * @function toJSON
         * @memberof proto.ProtoBytes
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ProtoBytes.prototype.toJSON = function() {
            return ProtoBytes.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ProtoBytes
         * @function getTypeUrl
         * @memberof proto.ProtoBytes
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ProtoBytes.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ProtoBytes";
        };

        return ProtoBytes;
    })();

    proto.TransactionRecordEntry = (function() {

        /**
         * Properties of a TransactionRecordEntry.
         * @typedef {Object} proto.TransactionRecordEntry.$Properties
         * @property {number|Long|null} [nodeId] A node identifier.<br/>
         * This identifier is the node, as known to the address book, that
         * submitted the transaction for consensus.
         * <p>
         * This SHALL be a whole number.
         * @property {proto.AccountID.$Properties|null} [payerAccountId] An Account identifier for the payer for the transaction.
         * <p>
         * This MAY be the same as the account ID within the Transaction ID of the
         * record, or it MAY be the account ID of the node that submitted the
         * transaction to consensus if the account ID in the Transaction ID was
         * not able to pay.
         * @property {proto.TransactionRecord.$Properties|null} [transactionRecord] A transaction record for the transaction.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionRecordEntry.
         * @memberof proto
         * @interface ITransactionRecordEntry
         * @augments proto.TransactionRecordEntry.$Properties
         * @deprecated Use proto.TransactionRecordEntry.$Properties instead.
         */

        /**
         * Shape of a TransactionRecordEntry.
         * @typedef {{
         *   nodeId?: number|Long|null;
         *   payerAccountId?: proto.AccountID.$Shape|null;
         *   transactionRecord?: proto.TransactionRecord.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TransactionRecordEntry.$Shape
         */

        /**
         * Constructs a new TransactionRecordEntry.
         * @memberof proto
         * @classdesc As transactions are handled and records and receipts are created, they are
         * stored in state for a configured time period (for example, 3 minutes).
         * During this time, any client can query the node and get the record or receipt
         * for the transaction. The `TransactionRecordEntry` is the object stored in
         * state with this information.
         * @constructor
         * @param {proto.TransactionRecordEntry.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionRecordEntry = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A node identifier.<br/>
         * This identifier is the node, as known to the address book, that
         * submitted the transaction for consensus.
         * <p>
         * This SHALL be a whole number.
         * @member {number|Long} nodeId
         * @memberof proto.TransactionRecordEntry
         * @instance
         */
        TransactionRecordEntry.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An Account identifier for the payer for the transaction.
         * <p>
         * This MAY be the same as the account ID within the Transaction ID of the
         * record, or it MAY be the account ID of the node that submitted the
         * transaction to consensus if the account ID in the Transaction ID was
         * not able to pay.
         * @member {proto.AccountID.$Properties|null|undefined} payerAccountId
         * @memberof proto.TransactionRecordEntry
         * @instance
         */
        TransactionRecordEntry.prototype.payerAccountId = null;

        /**
         * A transaction record for the transaction.
         * @member {proto.TransactionRecord.$Properties|null|undefined} transactionRecord
         * @memberof proto.TransactionRecordEntry
         * @instance
         */
        TransactionRecordEntry.prototype.transactionRecord = null;

        /**
         * Creates a new TransactionRecordEntry instance using the specified properties.
         * @function create
         * @memberof proto.TransactionRecordEntry
         * @static
         * @param {proto.TransactionRecordEntry.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionRecordEntry} TransactionRecordEntry instance
         * @type {{
         *   (properties: proto.TransactionRecordEntry.$Shape): proto.TransactionRecordEntry & proto.TransactionRecordEntry.$Shape;
         *   (properties?: proto.TransactionRecordEntry.$Properties): proto.TransactionRecordEntry;
         * }}
         */
        TransactionRecordEntry.create = function(properties) {
            return new TransactionRecordEntry(properties);
        };

        /**
         * Encodes the specified TransactionRecordEntry message. Does not implicitly {@link proto.TransactionRecordEntry.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionRecordEntry
         * @static
         * @param {proto.TransactionRecordEntry.$Properties} message TransactionRecordEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionRecordEntry.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.nodeId);
            if (message.payerAccountId != null && $Object.hasOwnProperty.call(message, "payerAccountId"))
                $root.proto.AccountID.encode(message.payerAccountId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.transactionRecord != null && $Object.hasOwnProperty.call(message, "transactionRecord"))
                $root.proto.TransactionRecord.encode(message.transactionRecord, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionRecordEntry message, length delimited. Does not implicitly {@link proto.TransactionRecordEntry.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionRecordEntry
         * @static
         * @param {proto.TransactionRecordEntry.$Properties} message TransactionRecordEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionRecordEntry.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionRecordEntry message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionRecordEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionRecordEntry & proto.TransactionRecordEntry.$Shape} TransactionRecordEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionRecordEntry.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionRecordEntry();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nodeId = value;
                        else
                            delete message.nodeId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.payerAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.payerAccountId);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.transactionRecord = $root.proto.TransactionRecord.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionRecord);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionRecordEntry message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionRecordEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionRecordEntry & proto.TransactionRecordEntry.$Shape} TransactionRecordEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionRecordEntry.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionRecordEntry message.
         * @function verify
         * @memberof proto.TransactionRecordEntry
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionRecordEntry.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                    return "nodeId: integer|Long expected";
            if (message.payerAccountId != null && $Object.hasOwnProperty.call(message, "payerAccountId")) {
                let error = $root.proto.AccountID.verify(message.payerAccountId, _depth + 1);
                if (error)
                    return "payerAccountId." + error;
            }
            if (message.transactionRecord != null && $Object.hasOwnProperty.call(message, "transactionRecord")) {
                let error = $root.proto.TransactionRecord.verify(message.transactionRecord, _depth + 1);
                if (error)
                    return "transactionRecord." + error;
            }
            return null;
        };

        /**
         * Creates a TransactionRecordEntry message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionRecordEntry
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionRecordEntry} TransactionRecordEntry
         */
        TransactionRecordEntry.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionRecordEntry)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionRecordEntry: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionRecordEntry();
            if (object.nodeId != null)
                if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                    if ($util.Long)
                        message.nodeId = $util.Long.fromValue(object.nodeId, false);
                    else if (typeof object.nodeId === "string")
                        message.nodeId = $parseInt(object.nodeId, 10);
                    else if (typeof object.nodeId === "number")
                        message.nodeId = object.nodeId;
                    else if (typeof object.nodeId === "object")
                        message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber();
            if (object.payerAccountId != null) {
                if (!$util.isObject(object.payerAccountId))
                    throw $TypeError(".proto.TransactionRecordEntry.payerAccountId: object expected");
                message.payerAccountId = $root.proto.AccountID.fromObject(object.payerAccountId, _depth + 1);
            }
            if (object.transactionRecord != null) {
                if (!$util.isObject(object.transactionRecord))
                    throw $TypeError(".proto.TransactionRecordEntry.transactionRecord: object expected");
                message.transactionRecord = $root.proto.TransactionRecord.fromObject(object.transactionRecord, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TransactionRecordEntry message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionRecordEntry
         * @static
         * @param {proto.TransactionRecordEntry} message TransactionRecordEntry
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionRecordEntry.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.payerAccountId = null;
                object.transactionRecord = null;
            }
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, false).toBigInt();
                else if (typeof message.nodeId === "number")
                    object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                else
                    object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber() : message.nodeId;
            if (message.payerAccountId != null && $Object.hasOwnProperty.call(message, "payerAccountId"))
                object.payerAccountId = $root.proto.AccountID.toObject(message.payerAccountId, options, _depth + 1);
            if (message.transactionRecord != null && $Object.hasOwnProperty.call(message, "transactionRecord"))
                object.transactionRecord = $root.proto.TransactionRecord.toObject(message.transactionRecord, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TransactionRecordEntry to JSON.
         * @function toJSON
         * @memberof proto.TransactionRecordEntry
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionRecordEntry.prototype.toJSON = function() {
            return TransactionRecordEntry.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionRecordEntry
         * @function getTypeUrl
         * @memberof proto.TransactionRecordEntry
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionRecordEntry.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionRecordEntry";
        };

        return TransactionRecordEntry;
    })();

    proto.TransactionReceiptEntry = (function() {

        /**
         * Properties of a TransactionReceiptEntry.
         * @typedef {Object} proto.TransactionReceiptEntry.$Properties
         * @property {number|Long|null} [nodeId] A node identifier.<br/>
         * This identifies the node that submitted the transaction to consensus.
         * The value is the identifier as known to the current address book.
         * <p>
         * Valid node identifiers SHALL be between 0 and <tt>2<sup>63-1</sup></tt>,
         * inclusive.
         * @property {proto.TransactionID.$Properties|null} [transactionId] A transaction identifier.<br/>
         * This identifies the submitted transaction for this receipt.
         * @property {proto.ResponseCodeEnum|null} [status] A status result.<br/>
         * This is the final status after handling the transaction.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionReceiptEntry.
         * @memberof proto
         * @interface ITransactionReceiptEntry
         * @augments proto.TransactionReceiptEntry.$Properties
         * @deprecated Use proto.TransactionReceiptEntry.$Properties instead.
         */

        /**
         * Shape of a TransactionReceiptEntry.
         * @typedef {{
         *   nodeId?: number|Long|null;
         *   transactionId?: proto.TransactionID.$Shape|null;
         *   status?: proto.ResponseCodeEnum|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TransactionReceiptEntry.$Shape
         */

        /**
         * Constructs a new TransactionReceiptEntry.
         * @memberof proto
         * @classdesc An entry in the record cache with the receipt for a transaction.
         * This is the entry stored in state that enables returning the receipt
         * information when queried by clients.
         * 
         * When a transaction is handled a receipt SHALL be created.<br/>
         * This receipt MUST be stored in state for a configured time limit
         * (e.g. 3 minutes).<br/>
         * While a receipt is stored, a client MAY query the node and retrieve
         * the receipt.
         * @constructor
         * @param {proto.TransactionReceiptEntry.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionReceiptEntry = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A node identifier.<br/>
         * This identifies the node that submitted the transaction to consensus.
         * The value is the identifier as known to the current address book.
         * <p>
         * Valid node identifiers SHALL be between 0 and <tt>2<sup>63-1</sup></tt>,
         * inclusive.
         * @member {number|Long} nodeId
         * @memberof proto.TransactionReceiptEntry
         * @instance
         */
        TransactionReceiptEntry.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A transaction identifier.<br/>
         * This identifies the submitted transaction for this receipt.
         * @member {proto.TransactionID.$Properties|null|undefined} transactionId
         * @memberof proto.TransactionReceiptEntry
         * @instance
         */
        TransactionReceiptEntry.prototype.transactionId = null;

        /**
         * A status result.<br/>
         * This is the final status after handling the transaction.
         * @member {proto.ResponseCodeEnum} status
         * @memberof proto.TransactionReceiptEntry
         * @instance
         */
        TransactionReceiptEntry.prototype.status = 0;

        /**
         * Creates a new TransactionReceiptEntry instance using the specified properties.
         * @function create
         * @memberof proto.TransactionReceiptEntry
         * @static
         * @param {proto.TransactionReceiptEntry.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionReceiptEntry} TransactionReceiptEntry instance
         * @type {{
         *   (properties: proto.TransactionReceiptEntry.$Shape): proto.TransactionReceiptEntry & proto.TransactionReceiptEntry.$Shape;
         *   (properties?: proto.TransactionReceiptEntry.$Properties): proto.TransactionReceiptEntry;
         * }}
         */
        TransactionReceiptEntry.create = function(properties) {
            return new TransactionReceiptEntry(properties);
        };

        /**
         * Encodes the specified TransactionReceiptEntry message. Does not implicitly {@link proto.TransactionReceiptEntry.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionReceiptEntry
         * @static
         * @param {proto.TransactionReceiptEntry.$Properties} message TransactionReceiptEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionReceiptEntry.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.nodeId);
            if (message.transactionId != null && $Object.hasOwnProperty.call(message, "transactionId"))
                $root.proto.TransactionID.encode(message.transactionId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.status);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionReceiptEntry message, length delimited. Does not implicitly {@link proto.TransactionReceiptEntry.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionReceiptEntry
         * @static
         * @param {proto.TransactionReceiptEntry.$Properties} message TransactionReceiptEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionReceiptEntry.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionReceiptEntry message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionReceiptEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionReceiptEntry & proto.TransactionReceiptEntry.$Shape} TransactionReceiptEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionReceiptEntry.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionReceiptEntry();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.nodeId = value;
                        else
                            delete message.nodeId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.transactionId = $root.proto.TransactionID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transactionId);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionReceiptEntry message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionReceiptEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionReceiptEntry & proto.TransactionReceiptEntry.$Shape} TransactionReceiptEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionReceiptEntry.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionReceiptEntry message.
         * @function verify
         * @memberof proto.TransactionReceiptEntry
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionReceiptEntry.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                    return "nodeId: integer|Long expected";
            if (message.transactionId != null && $Object.hasOwnProperty.call(message, "transactionId")) {
                let error = $root.proto.TransactionID.verify(message.transactionId, _depth + 1);
                if (error)
                    return "transactionId." + error;
            }
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                    return "status: enum value expected";
            return null;
        };

        /**
         * Creates a TransactionReceiptEntry message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionReceiptEntry
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionReceiptEntry} TransactionReceiptEntry
         */
        TransactionReceiptEntry.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionReceiptEntry)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionReceiptEntry: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionReceiptEntry();
            if (object.nodeId != null)
                if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                    if ($util.Long)
                        message.nodeId = $util.Long.fromValue(object.nodeId, true);
                    else if (typeof object.nodeId === "string")
                        message.nodeId = $parseInt(object.nodeId, 10);
                    else if (typeof object.nodeId === "number")
                        message.nodeId = object.nodeId;
                    else if (typeof object.nodeId === "object")
                        message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
            if (object.transactionId != null) {
                if (!$util.isObject(object.transactionId))
                    throw $TypeError(".proto.TransactionReceiptEntry.transactionId: object expected");
                message.transactionId = $root.proto.TransactionID.fromObject(object.transactionId, _depth + 1);
            }
            if (object.status !== 0 && (typeof object.status !== "string" || $root.proto.ResponseCodeEnum[object.status] !== 0))
                switch (object.status) {
                case "OK":
                case 0:
                    message.status = 0;
                    break;
                case "INVALID_TRANSACTION":
                case 1:
                    message.status = 1;
                    break;
                case "PAYER_ACCOUNT_NOT_FOUND":
                case 2:
                    message.status = 2;
                    break;
                case "INVALID_NODE_ACCOUNT":
                case 3:
                    message.status = 3;
                    break;
                case "TRANSACTION_EXPIRED":
                case 4:
                    message.status = 4;
                    break;
                case "INVALID_TRANSACTION_START":
                case 5:
                    message.status = 5;
                    break;
                case "INVALID_TRANSACTION_DURATION":
                case 6:
                    message.status = 6;
                    break;
                case "INVALID_SIGNATURE":
                case 7:
                    message.status = 7;
                    break;
                case "MEMO_TOO_LONG":
                case 8:
                    message.status = 8;
                    break;
                case "INSUFFICIENT_TX_FEE":
                case 9:
                    message.status = 9;
                    break;
                case "INSUFFICIENT_PAYER_BALANCE":
                case 10:
                    message.status = 10;
                    break;
                case "DUPLICATE_TRANSACTION":
                case 11:
                    message.status = 11;
                    break;
                case "BUSY":
                case 12:
                    message.status = 12;
                    break;
                case "NOT_SUPPORTED":
                case 13:
                    message.status = 13;
                    break;
                case "INVALID_FILE_ID":
                case 14:
                    message.status = 14;
                    break;
                case "INVALID_ACCOUNT_ID":
                case 15:
                    message.status = 15;
                    break;
                case "INVALID_CONTRACT_ID":
                case 16:
                    message.status = 16;
                    break;
                case "INVALID_TRANSACTION_ID":
                case 17:
                    message.status = 17;
                    break;
                case "RECEIPT_NOT_FOUND":
                case 18:
                    message.status = 18;
                    break;
                case "RECORD_NOT_FOUND":
                case 19:
                    message.status = 19;
                    break;
                case "INVALID_SOLIDITY_ID":
                case 20:
                    message.status = 20;
                    break;
                case "UNKNOWN":
                case 21:
                    message.status = 21;
                    break;
                case "SUCCESS":
                case 22:
                    message.status = 22;
                    break;
                case "FAIL_INVALID":
                case 23:
                    message.status = 23;
                    break;
                case "FAIL_FEE":
                case 24:
                    message.status = 24;
                    break;
                case "FAIL_BALANCE":
                case 25:
                    message.status = 25;
                    break;
                case "KEY_REQUIRED":
                case 26:
                    message.status = 26;
                    break;
                case "BAD_ENCODING":
                case 27:
                    message.status = 27;
                    break;
                case "INSUFFICIENT_ACCOUNT_BALANCE":
                case 28:
                    message.status = 28;
                    break;
                case "INVALID_SOLIDITY_ADDRESS":
                case 29:
                    message.status = 29;
                    break;
                case "INSUFFICIENT_GAS":
                case 30:
                    message.status = 30;
                    break;
                case "CONTRACT_SIZE_LIMIT_EXCEEDED":
                case 31:
                    message.status = 31;
                    break;
                case "LOCAL_CALL_MODIFICATION_EXCEPTION":
                case 32:
                    message.status = 32;
                    break;
                case "CONTRACT_REVERT_EXECUTED":
                case 33:
                    message.status = 33;
                    break;
                case "CONTRACT_EXECUTION_EXCEPTION":
                case 34:
                    message.status = 34;
                    break;
                case "INVALID_RECEIVING_NODE_ACCOUNT":
                case 35:
                    message.status = 35;
                    break;
                case "MISSING_QUERY_HEADER":
                case 36:
                    message.status = 36;
                    break;
                case "ACCOUNT_UPDATE_FAILED":
                case 37:
                    message.status = 37;
                    break;
                case "INVALID_KEY_ENCODING":
                case 38:
                    message.status = 38;
                    break;
                case "NULL_SOLIDITY_ADDRESS":
                case 39:
                    message.status = 39;
                    break;
                case "CONTRACT_UPDATE_FAILED":
                case 40:
                    message.status = 40;
                    break;
                case "INVALID_QUERY_HEADER":
                case 41:
                    message.status = 41;
                    break;
                case "INVALID_FEE_SUBMITTED":
                case 42:
                    message.status = 42;
                    break;
                case "INVALID_PAYER_SIGNATURE":
                case 43:
                    message.status = 43;
                    break;
                case "KEY_NOT_PROVIDED":
                case 44:
                    message.status = 44;
                    break;
                case "INVALID_EXPIRATION_TIME":
                case 45:
                    message.status = 45;
                    break;
                case "NO_WACL_KEY":
                case 46:
                    message.status = 46;
                    break;
                case "FILE_CONTENT_EMPTY":
                case 47:
                    message.status = 47;
                    break;
                case "INVALID_ACCOUNT_AMOUNTS":
                case 48:
                    message.status = 48;
                    break;
                case "EMPTY_TRANSACTION_BODY":
                case 49:
                    message.status = 49;
                    break;
                case "INVALID_TRANSACTION_BODY":
                case 50:
                    message.status = 50;
                    break;
                case "INVALID_SIGNATURE_TYPE_MISMATCHING_KEY":
                case 51:
                    message.status = 51;
                    break;
                case "INVALID_SIGNATURE_COUNT_MISMATCHING_KEY":
                case 52:
                    message.status = 52;
                    break;
                case "EMPTY_LIVE_HASH_BODY":
                case 53:
                    message.status = 53;
                    break;
                case "EMPTY_LIVE_HASH":
                case 54:
                    message.status = 54;
                    break;
                case "EMPTY_LIVE_HASH_KEYS":
                case 55:
                    message.status = 55;
                    break;
                case "INVALID_LIVE_HASH_SIZE":
                case 56:
                    message.status = 56;
                    break;
                case "EMPTY_QUERY_BODY":
                case 57:
                    message.status = 57;
                    break;
                case "EMPTY_LIVE_HASH_QUERY":
                case 58:
                    message.status = 58;
                    break;
                case "LIVE_HASH_NOT_FOUND":
                case 59:
                    message.status = 59;
                    break;
                case "ACCOUNT_ID_DOES_NOT_EXIST":
                case 60:
                    message.status = 60;
                    break;
                case "LIVE_HASH_ALREADY_EXISTS":
                case 61:
                    message.status = 61;
                    break;
                case "INVALID_FILE_WACL":
                case 62:
                    message.status = 62;
                    break;
                case "SERIALIZATION_FAILED":
                case 63:
                    message.status = 63;
                    break;
                case "TRANSACTION_OVERSIZE":
                case 64:
                    message.status = 64;
                    break;
                case "TRANSACTION_TOO_MANY_LAYERS":
                case 65:
                    message.status = 65;
                    break;
                case "CONTRACT_DELETED":
                case 66:
                    message.status = 66;
                    break;
                case "PLATFORM_NOT_ACTIVE":
                case 67:
                    message.status = 67;
                    break;
                case "KEY_PREFIX_MISMATCH":
                case 68:
                    message.status = 68;
                    break;
                case "PLATFORM_TRANSACTION_NOT_CREATED":
                case 69:
                    message.status = 69;
                    break;
                case "INVALID_RENEWAL_PERIOD":
                case 70:
                    message.status = 70;
                    break;
                case "INVALID_PAYER_ACCOUNT_ID":
                case 71:
                    message.status = 71;
                    break;
                case "ACCOUNT_DELETED":
                case 72:
                    message.status = 72;
                    break;
                case "FILE_DELETED":
                case 73:
                    message.status = 73;
                    break;
                case "ACCOUNT_REPEATED_IN_ACCOUNT_AMOUNTS":
                case 74:
                    message.status = 74;
                    break;
                case "SETTING_NEGATIVE_ACCOUNT_BALANCE":
                case 75:
                    message.status = 75;
                    break;
                case "OBTAINER_REQUIRED":
                case 76:
                    message.status = 76;
                    break;
                case "OBTAINER_SAME_CONTRACT_ID":
                case 77:
                    message.status = 77;
                    break;
                case "OBTAINER_DOES_NOT_EXIST":
                case 78:
                    message.status = 78;
                    break;
                case "MODIFYING_IMMUTABLE_CONTRACT":
                case 79:
                    message.status = 79;
                    break;
                case "FILE_SYSTEM_EXCEPTION":
                case 80:
                    message.status = 80;
                    break;
                case "AUTORENEW_DURATION_NOT_IN_RANGE":
                case 81:
                    message.status = 81;
                    break;
                case "ERROR_DECODING_BYTESTRING":
                case 82:
                    message.status = 82;
                    break;
                case "CONTRACT_FILE_EMPTY":
                case 83:
                    message.status = 83;
                    break;
                case "CONTRACT_BYTECODE_EMPTY":
                case 84:
                    message.status = 84;
                    break;
                case "INVALID_INITIAL_BALANCE":
                case 85:
                    message.status = 85;
                    break;
                case "INVALID_RECEIVE_RECORD_THRESHOLD":
                case 86:
                    message.status = 86;
                    break;
                case "INVALID_SEND_RECORD_THRESHOLD":
                case 87:
                    message.status = 87;
                    break;
                case "ACCOUNT_IS_NOT_GENESIS_ACCOUNT":
                case 88:
                    message.status = 88;
                    break;
                case "PAYER_ACCOUNT_UNAUTHORIZED":
                case 89:
                    message.status = 89;
                    break;
                case "INVALID_FREEZE_TRANSACTION_BODY":
                case 90:
                    message.status = 90;
                    break;
                case "FREEZE_TRANSACTION_BODY_NOT_FOUND":
                case 91:
                    message.status = 91;
                    break;
                case "TRANSFER_LIST_SIZE_LIMIT_EXCEEDED":
                case 92:
                    message.status = 92;
                    break;
                case "RESULT_SIZE_LIMIT_EXCEEDED":
                case 93:
                    message.status = 93;
                    break;
                case "NOT_SPECIAL_ACCOUNT":
                case 94:
                    message.status = 94;
                    break;
                case "CONTRACT_NEGATIVE_GAS":
                case 95:
                    message.status = 95;
                    break;
                case "CONTRACT_NEGATIVE_VALUE":
                case 96:
                    message.status = 96;
                    break;
                case "INVALID_FEE_FILE":
                case 97:
                    message.status = 97;
                    break;
                case "INVALID_EXCHANGE_RATE_FILE":
                case 98:
                    message.status = 98;
                    break;
                case "INSUFFICIENT_LOCAL_CALL_GAS":
                case 99:
                    message.status = 99;
                    break;
                case "ENTITY_NOT_ALLOWED_TO_DELETE":
                case 100:
                    message.status = 100;
                    break;
                case "AUTHORIZATION_FAILED":
                case 101:
                    message.status = 101;
                    break;
                case "FILE_UPLOADED_PROTO_INVALID":
                case 102:
                    message.status = 102;
                    break;
                case "FILE_UPLOADED_PROTO_NOT_SAVED_TO_DISK":
                case 103:
                    message.status = 103;
                    break;
                case "FEE_SCHEDULE_FILE_PART_UPLOADED":
                case 104:
                    message.status = 104;
                    break;
                case "EXCHANGE_RATE_CHANGE_LIMIT_EXCEEDED":
                case 105:
                    message.status = 105;
                    break;
                case "MAX_CONTRACT_STORAGE_EXCEEDED":
                case 106:
                    message.status = 106;
                    break;
                case "TRANSFER_ACCOUNT_SAME_AS_DELETE_ACCOUNT":
                case 107:
                    message.status = 107;
                    break;
                case "TOTAL_LEDGER_BALANCE_INVALID":
                case 108:
                    message.status = 108;
                    break;
                case "EXPIRATION_REDUCTION_NOT_ALLOWED":
                case 110:
                    message.status = 110;
                    break;
                case "MAX_GAS_LIMIT_EXCEEDED":
                case 111:
                    message.status = 111;
                    break;
                case "MAX_FILE_SIZE_EXCEEDED":
                case 112:
                    message.status = 112;
                    break;
                case "RECEIVER_SIG_REQUIRED":
                case 113:
                    message.status = 113;
                    break;
                case "INVALID_TOPIC_ID":
                case 150:
                    message.status = 150;
                    break;
                case "INVALID_ADMIN_KEY":
                case 155:
                    message.status = 155;
                    break;
                case "INVALID_SUBMIT_KEY":
                case 156:
                    message.status = 156;
                    break;
                case "UNAUTHORIZED":
                case 157:
                    message.status = 157;
                    break;
                case "INVALID_TOPIC_MESSAGE":
                case 158:
                    message.status = 158;
                    break;
                case "INVALID_AUTORENEW_ACCOUNT":
                case 159:
                    message.status = 159;
                    break;
                case "AUTORENEW_ACCOUNT_NOT_ALLOWED":
                case 160:
                    message.status = 160;
                    break;
                case "TOPIC_EXPIRED":
                case 162:
                    message.status = 162;
                    break;
                case "INVALID_CHUNK_NUMBER":
                case 163:
                    message.status = 163;
                    break;
                case "INVALID_CHUNK_TRANSACTION_ID":
                case 164:
                    message.status = 164;
                    break;
                case "ACCOUNT_FROZEN_FOR_TOKEN":
                case 165:
                    message.status = 165;
                    break;
                case "TOKENS_PER_ACCOUNT_LIMIT_EXCEEDED":
                case 166:
                    message.status = 166;
                    break;
                case "INVALID_TOKEN_ID":
                case 167:
                    message.status = 167;
                    break;
                case "INVALID_TOKEN_DECIMALS":
                case 168:
                    message.status = 168;
                    break;
                case "INVALID_TOKEN_INITIAL_SUPPLY":
                case 169:
                    message.status = 169;
                    break;
                case "INVALID_TREASURY_ACCOUNT_FOR_TOKEN":
                case 170:
                    message.status = 170;
                    break;
                case "INVALID_TOKEN_SYMBOL":
                case 171:
                    message.status = 171;
                    break;
                case "TOKEN_HAS_NO_FREEZE_KEY":
                case 172:
                    message.status = 172;
                    break;
                case "TRANSFERS_NOT_ZERO_SUM_FOR_TOKEN":
                case 173:
                    message.status = 173;
                    break;
                case "MISSING_TOKEN_SYMBOL":
                case 174:
                    message.status = 174;
                    break;
                case "TOKEN_SYMBOL_TOO_LONG":
                case 175:
                    message.status = 175;
                    break;
                case "ACCOUNT_KYC_NOT_GRANTED_FOR_TOKEN":
                case 176:
                    message.status = 176;
                    break;
                case "TOKEN_HAS_NO_KYC_KEY":
                case 177:
                    message.status = 177;
                    break;
                case "INSUFFICIENT_TOKEN_BALANCE":
                case 178:
                    message.status = 178;
                    break;
                case "TOKEN_WAS_DELETED":
                case 179:
                    message.status = 179;
                    break;
                case "TOKEN_HAS_NO_SUPPLY_KEY":
                case 180:
                    message.status = 180;
                    break;
                case "TOKEN_HAS_NO_WIPE_KEY":
                case 181:
                    message.status = 181;
                    break;
                case "INVALID_TOKEN_MINT_AMOUNT":
                case 182:
                    message.status = 182;
                    break;
                case "INVALID_TOKEN_BURN_AMOUNT":
                case 183:
                    message.status = 183;
                    break;
                case "TOKEN_NOT_ASSOCIATED_TO_ACCOUNT":
                case 184:
                    message.status = 184;
                    break;
                case "CANNOT_WIPE_TOKEN_TREASURY_ACCOUNT":
                case 185:
                    message.status = 185;
                    break;
                case "INVALID_KYC_KEY":
                case 186:
                    message.status = 186;
                    break;
                case "INVALID_WIPE_KEY":
                case 187:
                    message.status = 187;
                    break;
                case "INVALID_FREEZE_KEY":
                case 188:
                    message.status = 188;
                    break;
                case "INVALID_SUPPLY_KEY":
                case 189:
                    message.status = 189;
                    break;
                case "MISSING_TOKEN_NAME":
                case 190:
                    message.status = 190;
                    break;
                case "TOKEN_NAME_TOO_LONG":
                case 191:
                    message.status = 191;
                    break;
                case "INVALID_WIPING_AMOUNT":
                case 192:
                    message.status = 192;
                    break;
                case "TOKEN_IS_IMMUTABLE":
                case 193:
                    message.status = 193;
                    break;
                case "TOKEN_ALREADY_ASSOCIATED_TO_ACCOUNT":
                case 194:
                    message.status = 194;
                    break;
                case "TRANSACTION_REQUIRES_ZERO_TOKEN_BALANCES":
                case 195:
                    message.status = 195;
                    break;
                case "ACCOUNT_IS_TREASURY":
                case 196:
                    message.status = 196;
                    break;
                case "TOKEN_ID_REPEATED_IN_TOKEN_LIST":
                case 197:
                    message.status = 197;
                    break;
                case "TOKEN_TRANSFER_LIST_SIZE_LIMIT_EXCEEDED":
                case 198:
                    message.status = 198;
                    break;
                case "EMPTY_TOKEN_TRANSFER_BODY":
                case 199:
                    message.status = 199;
                    break;
                case "EMPTY_TOKEN_TRANSFER_ACCOUNT_AMOUNTS":
                case 200:
                    message.status = 200;
                    break;
                case "INVALID_SCHEDULE_ID":
                case 201:
                    message.status = 201;
                    break;
                case "SCHEDULE_IS_IMMUTABLE":
                case 202:
                    message.status = 202;
                    break;
                case "INVALID_SCHEDULE_PAYER_ID":
                case 203:
                    message.status = 203;
                    break;
                case "INVALID_SCHEDULE_ACCOUNT_ID":
                case 204:
                    message.status = 204;
                    break;
                case "NO_NEW_VALID_SIGNATURES":
                case 205:
                    message.status = 205;
                    break;
                case "UNRESOLVABLE_REQUIRED_SIGNERS":
                case 206:
                    message.status = 206;
                    break;
                case "SCHEDULED_TRANSACTION_NOT_IN_WHITELIST":
                case 207:
                    message.status = 207;
                    break;
                case "SOME_SIGNATURES_WERE_INVALID":
                case 208:
                    message.status = 208;
                    break;
                case "TRANSACTION_ID_FIELD_NOT_ALLOWED":
                case 209:
                    message.status = 209;
                    break;
                case "IDENTICAL_SCHEDULE_ALREADY_CREATED":
                case 210:
                    message.status = 210;
                    break;
                case "INVALID_ZERO_BYTE_IN_STRING":
                case 211:
                    message.status = 211;
                    break;
                case "SCHEDULE_ALREADY_DELETED":
                case 212:
                    message.status = 212;
                    break;
                case "SCHEDULE_ALREADY_EXECUTED":
                case 213:
                    message.status = 213;
                    break;
                case "MESSAGE_SIZE_TOO_LARGE":
                case 214:
                    message.status = 214;
                    break;
                case "OPERATION_REPEATED_IN_BUCKET_GROUPS":
                case 215:
                    message.status = 215;
                    break;
                case "BUCKET_CAPACITY_OVERFLOW":
                case 216:
                    message.status = 216;
                    break;
                case "NODE_CAPACITY_NOT_SUFFICIENT_FOR_OPERATION":
                case 217:
                    message.status = 217;
                    break;
                case "BUCKET_HAS_NO_THROTTLE_GROUPS":
                case 218:
                    message.status = 218;
                    break;
                case "THROTTLE_GROUP_HAS_ZERO_OPS_PER_SEC":
                case 219:
                    message.status = 219;
                    break;
                case "SUCCESS_BUT_MISSING_EXPECTED_OPERATION":
                case 220:
                    message.status = 220;
                    break;
                case "UNPARSEABLE_THROTTLE_DEFINITIONS":
                case 221:
                    message.status = 221;
                    break;
                case "INVALID_THROTTLE_DEFINITIONS":
                case 222:
                    message.status = 222;
                    break;
                case "ACCOUNT_EXPIRED_AND_PENDING_REMOVAL":
                case 223:
                    message.status = 223;
                    break;
                case "INVALID_TOKEN_MAX_SUPPLY":
                case 224:
                    message.status = 224;
                    break;
                case "INVALID_TOKEN_NFT_SERIAL_NUMBER":
                case 225:
                    message.status = 225;
                    break;
                case "INVALID_NFT_ID":
                case 226:
                    message.status = 226;
                    break;
                case "METADATA_TOO_LONG":
                case 227:
                    message.status = 227;
                    break;
                case "BATCH_SIZE_LIMIT_EXCEEDED":
                case 228:
                    message.status = 228;
                    break;
                case "INVALID_QUERY_RANGE":
                case 229:
                    message.status = 229;
                    break;
                case "FRACTION_DIVIDES_BY_ZERO":
                case 230:
                    message.status = 230;
                    break;
                case "INSUFFICIENT_PAYER_BALANCE_FOR_CUSTOM_FEE":
                case 231:
                    message.status = 231;
                    break;
                case "CUSTOM_FEES_LIST_TOO_LONG":
                case 232:
                    message.status = 232;
                    break;
                case "INVALID_CUSTOM_FEE_COLLECTOR":
                case 233:
                    message.status = 233;
                    break;
                case "INVALID_TOKEN_ID_IN_CUSTOM_FEES":
                case 234:
                    message.status = 234;
                    break;
                case "TOKEN_NOT_ASSOCIATED_TO_FEE_COLLECTOR":
                case 235:
                    message.status = 235;
                    break;
                case "TOKEN_MAX_SUPPLY_REACHED":
                case 236:
                    message.status = 236;
                    break;
                case "SENDER_DOES_NOT_OWN_NFT_SERIAL_NO":
                case 237:
                    message.status = 237;
                    break;
                case "CUSTOM_FEE_NOT_FULLY_SPECIFIED":
                case 238:
                    message.status = 238;
                    break;
                case "CUSTOM_FEE_MUST_BE_POSITIVE":
                case 239:
                    message.status = 239;
                    break;
                case "TOKEN_HAS_NO_FEE_SCHEDULE_KEY":
                case 240:
                    message.status = 240;
                    break;
                case "CUSTOM_FEE_OUTSIDE_NUMERIC_RANGE":
                case 241:
                    message.status = 241;
                    break;
                case "ROYALTY_FRACTION_CANNOT_EXCEED_ONE":
                case 242:
                    message.status = 242;
                    break;
                case "FRACTIONAL_FEE_MAX_AMOUNT_LESS_THAN_MIN_AMOUNT":
                case 243:
                    message.status = 243;
                    break;
                case "CUSTOM_SCHEDULE_ALREADY_HAS_NO_FEES":
                case 244:
                    message.status = 244;
                    break;
                case "CUSTOM_FEE_DENOMINATION_MUST_BE_FUNGIBLE_COMMON":
                case 245:
                    message.status = 245;
                    break;
                case "CUSTOM_FRACTIONAL_FEE_ONLY_ALLOWED_FOR_FUNGIBLE_COMMON":
                case 246:
                    message.status = 246;
                    break;
                case "INVALID_CUSTOM_FEE_SCHEDULE_KEY":
                case 247:
                    message.status = 247;
                    break;
                case "INVALID_TOKEN_MINT_METADATA":
                case 248:
                    message.status = 248;
                    break;
                case "INVALID_TOKEN_BURN_METADATA":
                case 249:
                    message.status = 249;
                    break;
                case "CURRENT_TREASURY_STILL_OWNS_NFTS":
                case 250:
                    message.status = 250;
                    break;
                case "ACCOUNT_STILL_OWNS_NFTS":
                case 251:
                    message.status = 251;
                    break;
                case "TREASURY_MUST_OWN_BURNED_NFT":
                case 252:
                    message.status = 252;
                    break;
                case "ACCOUNT_DOES_NOT_OWN_WIPED_NFT":
                case 253:
                    message.status = 253;
                    break;
                case "ACCOUNT_AMOUNT_TRANSFERS_ONLY_ALLOWED_FOR_FUNGIBLE_COMMON":
                case 254:
                    message.status = 254;
                    break;
                case "MAX_NFTS_IN_PRICE_REGIME_HAVE_BEEN_MINTED":
                case 255:
                    message.status = 255;
                    break;
                case "PAYER_ACCOUNT_DELETED":
                case 256:
                    message.status = 256;
                    break;
                case "CUSTOM_FEE_CHARGING_EXCEEDED_MAX_RECURSION_DEPTH":
                case 257:
                    message.status = 257;
                    break;
                case "CUSTOM_FEE_CHARGING_EXCEEDED_MAX_ACCOUNT_AMOUNTS":
                case 258:
                    message.status = 258;
                    break;
                case "INSUFFICIENT_SENDER_ACCOUNT_BALANCE_FOR_CUSTOM_FEE":
                case 259:
                    message.status = 259;
                    break;
                case "SERIAL_NUMBER_LIMIT_REACHED":
                case 260:
                    message.status = 260;
                    break;
                case "CUSTOM_ROYALTY_FEE_ONLY_ALLOWED_FOR_NON_FUNGIBLE_UNIQUE":
                case 261:
                    message.status = 261;
                    break;
                case "NO_REMAINING_AUTOMATIC_ASSOCIATIONS":
                case 262:
                    message.status = 262;
                    break;
                case "EXISTING_AUTOMATIC_ASSOCIATIONS_EXCEED_GIVEN_LIMIT":
                case 263:
                    message.status = 263;
                    break;
                case "REQUESTED_NUM_AUTOMATIC_ASSOCIATIONS_EXCEEDS_ASSOCIATION_LIMIT":
                case 264:
                    message.status = 264;
                    break;
                case "TOKEN_IS_PAUSED":
                case 265:
                    message.status = 265;
                    break;
                case "TOKEN_HAS_NO_PAUSE_KEY":
                case 266:
                    message.status = 266;
                    break;
                case "INVALID_PAUSE_KEY":
                case 267:
                    message.status = 267;
                    break;
                case "FREEZE_UPDATE_FILE_DOES_NOT_EXIST":
                case 268:
                    message.status = 268;
                    break;
                case "FREEZE_UPDATE_FILE_HASH_DOES_NOT_MATCH":
                case 269:
                    message.status = 269;
                    break;
                case "NO_UPGRADE_HAS_BEEN_PREPARED":
                case 270:
                    message.status = 270;
                    break;
                case "NO_FREEZE_IS_SCHEDULED":
                case 271:
                    message.status = 271;
                    break;
                case "UPDATE_FILE_HASH_CHANGED_SINCE_PREPARE_UPGRADE":
                case 272:
                    message.status = 272;
                    break;
                case "FREEZE_START_TIME_MUST_BE_FUTURE":
                case 273:
                    message.status = 273;
                    break;
                case "PREPARED_UPDATE_FILE_IS_IMMUTABLE":
                case 274:
                    message.status = 274;
                    break;
                case "FREEZE_ALREADY_SCHEDULED":
                case 275:
                    message.status = 275;
                    break;
                case "FREEZE_UPGRADE_IN_PROGRESS":
                case 276:
                    message.status = 276;
                    break;
                case "UPDATE_FILE_ID_DOES_NOT_MATCH_PREPARED":
                case 277:
                    message.status = 277;
                    break;
                case "UPDATE_FILE_HASH_DOES_NOT_MATCH_PREPARED":
                case 278:
                    message.status = 278;
                    break;
                case "CONSENSUS_GAS_EXHAUSTED":
                case 279:
                    message.status = 279;
                    break;
                case "REVERTED_SUCCESS":
                case 280:
                    message.status = 280;
                    break;
                case "MAX_STORAGE_IN_PRICE_REGIME_HAS_BEEN_USED":
                case 281:
                    message.status = 281;
                    break;
                case "INVALID_ALIAS_KEY":
                case 282:
                    message.status = 282;
                    break;
                case "UNEXPECTED_TOKEN_DECIMALS":
                case 283:
                    message.status = 283;
                    break;
                case "INVALID_PROXY_ACCOUNT_ID":
                case 284:
                    message.status = 284;
                    break;
                case "INVALID_TRANSFER_ACCOUNT_ID":
                case 285:
                    message.status = 285;
                    break;
                case "INVALID_FEE_COLLECTOR_ACCOUNT_ID":
                case 286:
                    message.status = 286;
                    break;
                case "ALIAS_IS_IMMUTABLE":
                case 287:
                    message.status = 287;
                    break;
                case "SPENDER_ACCOUNT_SAME_AS_OWNER":
                case 288:
                    message.status = 288;
                    break;
                case "AMOUNT_EXCEEDS_TOKEN_MAX_SUPPLY":
                case 289:
                    message.status = 289;
                    break;
                case "NEGATIVE_ALLOWANCE_AMOUNT":
                case 290:
                    message.status = 290;
                    break;
                case "CANNOT_APPROVE_FOR_ALL_FUNGIBLE_COMMON":
                case 291:
                    message.status = 291;
                    break;
                case "SPENDER_DOES_NOT_HAVE_ALLOWANCE":
                case 292:
                    message.status = 292;
                    break;
                case "AMOUNT_EXCEEDS_ALLOWANCE":
                case 293:
                    message.status = 293;
                    break;
                case "MAX_ALLOWANCES_EXCEEDED":
                case 294:
                    message.status = 294;
                    break;
                case "EMPTY_ALLOWANCES":
                case 295:
                    message.status = 295;
                    break;
                case "SPENDER_ACCOUNT_REPEATED_IN_ALLOWANCES":
                case 296:
                    message.status = 296;
                    break;
                case "REPEATED_SERIAL_NUMS_IN_NFT_ALLOWANCES":
                case 297:
                    message.status = 297;
                    break;
                case "FUNGIBLE_TOKEN_IN_NFT_ALLOWANCES":
                case 298:
                    message.status = 298;
                    break;
                case "NFT_IN_FUNGIBLE_TOKEN_ALLOWANCES":
                case 299:
                    message.status = 299;
                    break;
                case "INVALID_ALLOWANCE_OWNER_ID":
                case 300:
                    message.status = 300;
                    break;
                case "INVALID_ALLOWANCE_SPENDER_ID":
                case 301:
                    message.status = 301;
                    break;
                case "REPEATED_ALLOWANCES_TO_DELETE":
                case 302:
                    message.status = 302;
                    break;
                case "INVALID_DELEGATING_SPENDER":
                case 303:
                    message.status = 303;
                    break;
                case "DELEGATING_SPENDER_CANNOT_GRANT_APPROVE_FOR_ALL":
                case 304:
                    message.status = 304;
                    break;
                case "DELEGATING_SPENDER_DOES_NOT_HAVE_APPROVE_FOR_ALL":
                case 305:
                    message.status = 305;
                    break;
                case "SCHEDULE_EXPIRATION_TIME_TOO_FAR_IN_FUTURE":
                case 306:
                    message.status = 306;
                    break;
                case "SCHEDULE_EXPIRATION_TIME_MUST_BE_HIGHER_THAN_CONSENSUS_TIME":
                case 307:
                    message.status = 307;
                    break;
                case "SCHEDULE_FUTURE_THROTTLE_EXCEEDED":
                case 308:
                    message.status = 308;
                    break;
                case "SCHEDULE_FUTURE_GAS_LIMIT_EXCEEDED":
                case 309:
                    message.status = 309;
                    break;
                case "INVALID_ETHEREUM_TRANSACTION":
                case 310:
                    message.status = 310;
                    break;
                case "WRONG_CHAIN_ID":
                case 311:
                    message.status = 311;
                    break;
                case "WRONG_NONCE":
                case 312:
                    message.status = 312;
                    break;
                case "ACCESS_LIST_UNSUPPORTED":
                case 313:
                    message.status = 313;
                    break;
                case "SCHEDULE_PENDING_EXPIRATION":
                case 314:
                    message.status = 314;
                    break;
                case "CONTRACT_IS_TOKEN_TREASURY":
                case 315:
                    message.status = 315;
                    break;
                case "CONTRACT_HAS_NON_ZERO_TOKEN_BALANCES":
                case 316:
                    message.status = 316;
                    break;
                case "CONTRACT_EXPIRED_AND_PENDING_REMOVAL":
                case 317:
                    message.status = 317;
                    break;
                case "CONTRACT_HAS_NO_AUTO_RENEW_ACCOUNT":
                case 318:
                    message.status = 318;
                    break;
                case "PERMANENT_REMOVAL_REQUIRES_SYSTEM_INITIATION":
                case 319:
                    message.status = 319;
                    break;
                case "PROXY_ACCOUNT_ID_FIELD_IS_DEPRECATED":
                case 320:
                    message.status = 320;
                    break;
                case "SELF_STAKING_IS_NOT_ALLOWED":
                case 321:
                    message.status = 321;
                    break;
                case "INVALID_STAKING_ID":
                case 322:
                    message.status = 322;
                    break;
                case "STAKING_NOT_ENABLED":
                case 323:
                    message.status = 323;
                    break;
                case "INVALID_PRNG_RANGE":
                case 324:
                    message.status = 324;
                    break;
                case "MAX_ENTITIES_IN_PRICE_REGIME_HAVE_BEEN_CREATED":
                case 325:
                    message.status = 325;
                    break;
                case "INVALID_FULL_PREFIX_SIGNATURE_FOR_PRECOMPILE":
                case 326:
                    message.status = 326;
                    break;
                case "INSUFFICIENT_BALANCES_FOR_STORAGE_RENT":
                case 327:
                    message.status = 327;
                    break;
                case "MAX_CHILD_RECORDS_EXCEEDED":
                case 328:
                    message.status = 328;
                    break;
                case "INSUFFICIENT_BALANCES_FOR_RENEWAL_FEES":
                case 329:
                    message.status = 329;
                    break;
                case "TRANSACTION_HAS_UNKNOWN_FIELDS":
                case 330:
                    message.status = 330;
                    break;
                case "ACCOUNT_IS_IMMUTABLE":
                case 331:
                    message.status = 331;
                    break;
                case "ALIAS_ALREADY_ASSIGNED":
                case 332:
                    message.status = 332;
                    break;
                case "INVALID_METADATA_KEY":
                case 333:
                    message.status = 333;
                    break;
                case "TOKEN_HAS_NO_METADATA_KEY":
                case 334:
                    message.status = 334;
                    break;
                case "MISSING_TOKEN_METADATA":
                case 335:
                    message.status = 335;
                    break;
                case "MISSING_SERIAL_NUMBERS":
                case 336:
                    message.status = 336;
                    break;
                case "TOKEN_HAS_NO_ADMIN_KEY":
                case 337:
                    message.status = 337;
                    break;
                case "NODE_DELETED":
                case 338:
                    message.status = 338;
                    break;
                case "INVALID_NODE_ID":
                case 339:
                    message.status = 339;
                    break;
                case "INVALID_GOSSIP_ENDPOINT":
                case 340:
                    message.status = 340;
                    break;
                case "INVALID_NODE_ACCOUNT_ID":
                case 341:
                    message.status = 341;
                    break;
                case "INVALID_NODE_DESCRIPTION":
                case 342:
                    message.status = 342;
                    break;
                case "INVALID_SERVICE_ENDPOINT":
                case 343:
                    message.status = 343;
                    break;
                case "INVALID_GOSSIP_CA_CERTIFICATE":
                case 344:
                    message.status = 344;
                    break;
                case "INVALID_GRPC_CERTIFICATE":
                case 345:
                    message.status = 345;
                    break;
                case "INVALID_MAX_AUTO_ASSOCIATIONS":
                case 346:
                    message.status = 346;
                    break;
                case "MAX_NODES_CREATED":
                case 347:
                    message.status = 347;
                    break;
                case "IP_FQDN_CANNOT_BE_SET_FOR_SAME_ENDPOINT":
                case 348:
                    message.status = 348;
                    break;
                case "GOSSIP_ENDPOINT_CANNOT_HAVE_FQDN":
                case 349:
                    message.status = 349;
                    break;
                case "FQDN_SIZE_TOO_LARGE":
                case 350:
                    message.status = 350;
                    break;
                case "INVALID_ENDPOINT":
                case 351:
                    message.status = 351;
                    break;
                case "GOSSIP_ENDPOINTS_EXCEEDED_LIMIT":
                case 352:
                    message.status = 352;
                    break;
                case "TOKEN_REFERENCE_REPEATED":
                case 353:
                    message.status = 353;
                    break;
                case "INVALID_OWNER_ID":
                case 354:
                    message.status = 354;
                    break;
                case "TOKEN_REFERENCE_LIST_SIZE_LIMIT_EXCEEDED":
                case 355:
                    message.status = 355;
                    break;
                case "SERVICE_ENDPOINTS_EXCEEDED_LIMIT":
                case 356:
                    message.status = 356;
                    break;
                case "INVALID_IPV4_ADDRESS":
                case 357:
                    message.status = 357;
                    break;
                case "EMPTY_TOKEN_REFERENCE_LIST":
                case 358:
                    message.status = 358;
                    break;
                case "UPDATE_NODE_ACCOUNT_NOT_ALLOWED":
                case 359:
                    message.status = 359;
                    break;
                case "TOKEN_HAS_NO_METADATA_OR_SUPPLY_KEY":
                case 360:
                    message.status = 360;
                    break;
                case "EMPTY_PENDING_AIRDROP_ID_LIST":
                case 361:
                    message.status = 361;
                    break;
                case "PENDING_AIRDROP_ID_REPEATED":
                case 362:
                    message.status = 362;
                    break;
                case "PENDING_AIRDROP_ID_LIST_TOO_LONG":
                case 363:
                    message.status = 363;
                    break;
                case "PENDING_NFT_AIRDROP_ALREADY_EXISTS":
                case 364:
                    message.status = 364;
                    break;
                case "ACCOUNT_HAS_PENDING_AIRDROPS":
                case 365:
                    message.status = 365;
                    break;
                case "THROTTLED_AT_CONSENSUS":
                case 366:
                    message.status = 366;
                    break;
                case "INVALID_PENDING_AIRDROP_ID":
                case 367:
                    message.status = 367;
                    break;
                case "TOKEN_AIRDROP_WITH_FALLBACK_ROYALTY":
                case 368:
                    message.status = 368;
                    break;
                case "INVALID_TOKEN_IN_PENDING_AIRDROP":
                case 369:
                    message.status = 369;
                    break;
                case "SCHEDULE_EXPIRY_IS_BUSY":
                case 370:
                    message.status = 370;
                    break;
                case "INVALID_GRPC_CERTIFICATE_HASH":
                case 371:
                    message.status = 371;
                    break;
                case "MISSING_EXPIRY_TIME":
                case 372:
                    message.status = 372;
                    break;
                case "NO_SCHEDULING_ALLOWED_AFTER_SCHEDULED_RECURSION":
                case 373:
                    message.status = 373;
                    break;
                case "RECURSIVE_SCHEDULING_LIMIT_REACHED":
                case 374:
                    message.status = 374;
                    break;
                case "WAITING_FOR_LEDGER_ID":
                case 375:
                    message.status = 375;
                    break;
                case "MAX_ENTRIES_FOR_FEE_EXEMPT_KEY_LIST_EXCEEDED":
                case 376:
                    message.status = 376;
                    break;
                case "FEE_EXEMPT_KEY_LIST_CONTAINS_DUPLICATED_KEYS":
                case 377:
                    message.status = 377;
                    break;
                case "INVALID_KEY_IN_FEE_EXEMPT_KEY_LIST":
                case 378:
                    message.status = 378;
                    break;
                case "INVALID_FEE_SCHEDULE_KEY":
                case 379:
                    message.status = 379;
                    break;
                case "FEE_SCHEDULE_KEY_CANNOT_BE_UPDATED":
                case 380:
                    message.status = 380;
                    break;
                case "FEE_SCHEDULE_KEY_NOT_SET":
                case 381:
                    message.status = 381;
                    break;
                case "MAX_CUSTOM_FEE_LIMIT_EXCEEDED":
                case 382:
                    message.status = 382;
                    break;
                case "NO_VALID_MAX_CUSTOM_FEE":
                case 383:
                    message.status = 383;
                    break;
                case "INVALID_MAX_CUSTOM_FEES":
                case 384:
                    message.status = 384;
                    break;
                case "DUPLICATE_DENOMINATION_IN_MAX_CUSTOM_FEE_LIST":
                case 385:
                    message.status = 385;
                    break;
                case "DUPLICATE_ACCOUNT_ID_IN_MAX_CUSTOM_FEE_LIST":
                case 386:
                    message.status = 386;
                    break;
                case "MAX_CUSTOM_FEES_IS_NOT_SUPPORTED":
                case 387:
                    message.status = 387;
                    break;
                case "BATCH_LIST_EMPTY":
                case 388:
                    message.status = 388;
                    break;
                case "BATCH_LIST_CONTAINS_DUPLICATES":
                case 389:
                    message.status = 389;
                    break;
                case "BATCH_TRANSACTION_IN_BLACKLIST":
                case 390:
                    message.status = 390;
                    break;
                case "INNER_TRANSACTION_FAILED":
                case 391:
                    message.status = 391;
                    break;
                case "MISSING_BATCH_KEY":
                case 392:
                    message.status = 392;
                    break;
                case "BATCH_KEY_SET_ON_NON_INNER_TRANSACTION":
                case 393:
                    message.status = 393;
                    break;
                case "INVALID_BATCH_KEY":
                case 394:
                    message.status = 394;
                    break;
                case "SCHEDULE_EXPIRY_NOT_CONFIGURABLE":
                case 395:
                    message.status = 395;
                    break;
                case "CREATING_SYSTEM_ENTITIES":
                case 396:
                    message.status = 396;
                    break;
                case "THROTTLE_GROUP_LCM_OVERFLOW":
                case 397:
                    message.status = 397;
                    break;
                case "AIRDROP_CONTAINS_MULTIPLE_SENDERS_FOR_A_TOKEN":
                case 398:
                    message.status = 398;
                    break;
                case "GRPC_WEB_PROXY_NOT_SUPPORTED":
                case 399:
                    message.status = 399;
                    break;
                case "NFT_TRANSFERS_ONLY_ALLOWED_FOR_NON_FUNGIBLE_UNIQUE":
                case 400:
                    message.status = 400;
                    break;
                case "INVALID_SERIALIZED_TX_MESSAGE_HASH_ALGORITHM":
                case 401:
                    message.status = 401;
                    break;
                case "WRONG_HOOK_ENTITY_TYPE":
                case 499:
                    message.status = 499;
                    break;
                case "EVM_HOOK_GAS_THROTTLED":
                case 500:
                    message.status = 500;
                    break;
                case "HOOK_ID_IN_USE":
                case 501:
                    message.status = 501;
                    break;
                case "BAD_HOOK_REQUEST":
                case 502:
                    message.status = 502;
                    break;
                case "REJECTED_BY_ACCOUNT_ALLOWANCE_HOOK":
                case 503:
                    message.status = 503;
                    break;
                case "HOOK_NOT_FOUND":
                case 504:
                    message.status = 504;
                    break;
                case "EVM_HOOK_STORAGE_UPDATE_BYTES_TOO_LONG":
                case 505:
                    message.status = 505;
                    break;
                case "EVM_HOOK_STORAGE_UPDATE_BYTES_MUST_USE_MINIMAL_REPRESENTATION":
                case 506:
                    message.status = 506;
                    break;
                case "INVALID_HOOK_ID":
                case 507:
                    message.status = 507;
                    break;
                case "EMPTY_EVM_HOOK_STORAGE_UPDATE":
                case 508:
                    message.status = 508;
                    break;
                case "HOOK_ID_REPEATED_IN_CREATION_DETAILS":
                case 509:
                    message.status = 509;
                    break;
                case "HOOKS_NOT_ENABLED":
                case 510:
                    message.status = 510;
                    break;
                case "HOOK_IS_NOT_AN_EVM_HOOK":
                case 511:
                    message.status = 511;
                    break;
                case "HOOK_DELETED":
                case 512:
                    message.status = 512;
                    break;
                case "TOO_MANY_EVM_HOOK_STORAGE_UPDATES":
                case 513:
                    message.status = 513;
                    break;
                case "HOOK_CREATION_BYTES_MUST_USE_MINIMAL_REPRESENTATION":
                case 514:
                    message.status = 514;
                    break;
                case "HOOK_CREATION_BYTES_TOO_LONG":
                case 515:
                    message.status = 515;
                    break;
                case "INVALID_HOOK_CREATION_SPEC":
                case 516:
                    message.status = 516;
                    break;
                case "HOOK_EXTENSION_EMPTY":
                case 517:
                    message.status = 517;
                    break;
                case "INVALID_HOOK_ADMIN_KEY":
                case 518:
                    message.status = 518;
                    break;
                case "HOOK_DELETION_REQUIRES_ZERO_STORAGE_SLOTS":
                case 519:
                    message.status = 519;
                    break;
                case "CANNOT_SET_HOOKS_AND_APPROVAL":
                case 520:
                    message.status = 520;
                    break;
                case "TRANSACTION_REQUIRES_ZERO_HOOKS":
                case 521:
                    message.status = 521;
                    break;
                case "INVALID_HOOK_CALL":
                case 522:
                    message.status = 522;
                    break;
                case "HOOKS_ARE_NOT_SUPPORTED_IN_AIRDROPS":
                case 523:
                    message.status = 523;
                    break;
                case "ACCOUNT_IS_LINKED_TO_A_NODE":
                case 524:
                    message.status = 524;
                    break;
                case "HOOKS_EXECUTIONS_REQUIRE_TOP_LEVEL_CRYPTO_TRANSFER":
                case 525:
                    message.status = 525;
                    break;
                case "NODE_ACCOUNT_HAS_ZERO_BALANCE":
                case 526:
                    message.status = 526;
                    break;
                case "TRANSFER_TO_FEE_COLLECTION_ACCOUNT_NOT_ALLOWED":
                case 527:
                    message.status = 527;
                    break;
                case "TOO_MANY_HOOK_INVOCATIONS":
                case 528:
                    message.status = 528;
                    break;
                case "INVALID_REGISTERED_NODE_ID":
                case 529:
                    message.status = 529;
                    break;
                case "INVALID_REGISTERED_ENDPOINT":
                case 530:
                    message.status = 530;
                    break;
                case "REGISTERED_ENDPOINTS_EXCEEDED_LIMIT":
                case 531:
                    message.status = 531;
                    break;
                case "INVALID_REGISTERED_ENDPOINT_ADDRESS":
                case 532:
                    message.status = 532;
                    break;
                case "INVALID_REGISTERED_ENDPOINT_TYPE":
                case 533:
                    message.status = 533;
                    break;
                case "REGISTERED_NODE_STILL_ASSOCIATED":
                case 534:
                    message.status = 534;
                    break;
                case "MAX_REGISTERED_NODES_EXCEEDED":
                case 535:
                    message.status = 535;
                    break;
                default:
                    if (typeof object.status === "number" && (object.status | 0) === object.status)
                        message.status = object.status;
                }
            return message;
        };

        /**
         * Creates a plain object from a TransactionReceiptEntry message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionReceiptEntry
         * @static
         * @param {proto.TransactionReceiptEntry} message TransactionReceiptEntry
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionReceiptEntry.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.transactionId = null;
                object.status = options.enums === $String ? "OK" : 0;
            }
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                else if (typeof message.nodeId === "number")
                    object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                else
                    object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
            if (message.transactionId != null && $Object.hasOwnProperty.call(message, "transactionId"))
                object.transactionId = $root.proto.TransactionID.toObject(message.transactionId, options, _depth + 1);
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === $String ? $root.proto.ResponseCodeEnum[message.status] === $undefined ? message.status : $root.proto.ResponseCodeEnum[message.status] : message.status;
            return object;
        };

        /**
         * Converts this TransactionReceiptEntry to JSON.
         * @function toJSON
         * @memberof proto.TransactionReceiptEntry
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionReceiptEntry.prototype.toJSON = function() {
            return TransactionReceiptEntry.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionReceiptEntry
         * @function getTypeUrl
         * @memberof proto.TransactionReceiptEntry
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionReceiptEntry.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionReceiptEntry";
        };

        return TransactionReceiptEntry;
    })();

    proto.TransactionReceiptEntries = (function() {

        /**
         * Properties of a TransactionReceiptEntries.
         * @typedef {Object} proto.TransactionReceiptEntries.$Properties
         * @property {Array.<proto.TransactionReceiptEntry.$Properties>|null} [entries] TransactionReceiptEntries entries
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionReceiptEntries.
         * @memberof proto
         * @interface ITransactionReceiptEntries
         * @augments proto.TransactionReceiptEntries.$Properties
         * @deprecated Use proto.TransactionReceiptEntries.$Properties instead.
         */

        /**
         * Shape of a TransactionReceiptEntries.
         * @typedef {{
         *   entries?: Array.<proto.TransactionReceiptEntry.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TransactionReceiptEntries.$Shape
         */

        /**
         * Constructs a new TransactionReceiptEntries.
         * @memberof proto
         * @classdesc A cache of transaction receipts.<br/>
         * As transactions are handled and receipts are created, they are stored in
         * state for a configured time limit (perhaps, for example, 3 minutes).
         * During this time window, any client can query the node and get the receipt
         * for the transaction. The `TransactionReceiptEntries` is the object stored in
         * state with this information.
         * 
         * This message SHALL contain a list of `TransactionReceiptEntry` objects.
         * @constructor
         * @param {proto.TransactionReceiptEntries.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionReceiptEntries = function (properties) {
            this.entries = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * TransactionReceiptEntries entries.
         * @member {Array.<proto.TransactionReceiptEntry.$Properties>} entries
         * @memberof proto.TransactionReceiptEntries
         * @instance
         */
        TransactionReceiptEntries.prototype.entries = $util.emptyArray;

        /**
         * Creates a new TransactionReceiptEntries instance using the specified properties.
         * @function create
         * @memberof proto.TransactionReceiptEntries
         * @static
         * @param {proto.TransactionReceiptEntries.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionReceiptEntries} TransactionReceiptEntries instance
         * @type {{
         *   (properties: proto.TransactionReceiptEntries.$Shape): proto.TransactionReceiptEntries & proto.TransactionReceiptEntries.$Shape;
         *   (properties?: proto.TransactionReceiptEntries.$Properties): proto.TransactionReceiptEntries;
         * }}
         */
        TransactionReceiptEntries.create = function(properties) {
            return new TransactionReceiptEntries(properties);
        };

        /**
         * Encodes the specified TransactionReceiptEntries message. Does not implicitly {@link proto.TransactionReceiptEntries.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionReceiptEntries
         * @static
         * @param {proto.TransactionReceiptEntries.$Properties} message TransactionReceiptEntries message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionReceiptEntries.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.entries != null && message.entries.length)
                for (let i = 0; i < message.entries.length; ++i)
                    $root.proto.TransactionReceiptEntry.encode(message.entries[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionReceiptEntries message, length delimited. Does not implicitly {@link proto.TransactionReceiptEntries.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionReceiptEntries
         * @static
         * @param {proto.TransactionReceiptEntries.$Properties} message TransactionReceiptEntries message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionReceiptEntries.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionReceiptEntries message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionReceiptEntries
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionReceiptEntries & proto.TransactionReceiptEntries.$Shape} TransactionReceiptEntries
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionReceiptEntries.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionReceiptEntries();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.entries && message.entries.length))
                            message.entries = [];
                        message.entries.push($root.proto.TransactionReceiptEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionReceiptEntries message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionReceiptEntries
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionReceiptEntries & proto.TransactionReceiptEntries.$Shape} TransactionReceiptEntries
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionReceiptEntries.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionReceiptEntries message.
         * @function verify
         * @memberof proto.TransactionReceiptEntries
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionReceiptEntries.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.entries != null && $Object.hasOwnProperty.call(message, "entries")) {
                if (!$Array.isArray(message.entries))
                    return "entries: array expected";
                for (let i = 0; i < message.entries.length; ++i) {
                    let error = $root.proto.TransactionReceiptEntry.verify(message.entries[i], _depth + 1);
                    if (error)
                        return "entries." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TransactionReceiptEntries message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionReceiptEntries
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionReceiptEntries} TransactionReceiptEntries
         */
        TransactionReceiptEntries.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionReceiptEntries)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionReceiptEntries: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionReceiptEntries();
            if (object.entries) {
                if (!$Array.isArray(object.entries))
                    throw $TypeError(".proto.TransactionReceiptEntries.entries: array expected");
                message.entries = $Array(object.entries.length);
                for (let i = 0; i < object.entries.length; ++i) {
                    if (!$util.isObject(object.entries[i]))
                        throw $TypeError(".proto.TransactionReceiptEntries.entries: object expected");
                    message.entries[i] = $root.proto.TransactionReceiptEntry.fromObject(object.entries[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TransactionReceiptEntries message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionReceiptEntries
         * @static
         * @param {proto.TransactionReceiptEntries} message TransactionReceiptEntries
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionReceiptEntries.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.entries = [];
            if (message.entries && message.entries.length) {
                object.entries = $Array(message.entries.length);
                for (let j = 0; j < message.entries.length; ++j)
                    object.entries[j] = $root.proto.TransactionReceiptEntry.toObject(message.entries[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this TransactionReceiptEntries to JSON.
         * @function toJSON
         * @memberof proto.TransactionReceiptEntries
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionReceiptEntries.prototype.toJSON = function() {
            return TransactionReceiptEntries.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionReceiptEntries
         * @function getTypeUrl
         * @memberof proto.TransactionReceiptEntries
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionReceiptEntries.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionReceiptEntries";
        };

        return TransactionReceiptEntries;
    })();

    proto.Schedule = (function() {

        /**
         * Properties of a Schedule.
         * @typedef {Object} proto.Schedule.$Properties
         * @property {proto.ScheduleID.$Properties|null} [scheduleId] This schedule's ID within the global network state.
         * <p>
         * This value SHALL be unique within the network.
         * @property {boolean|null} [deleted] A flag indicating this schedule is deleted.
         * <p>
         * A schedule SHALL either be executed or deleted, but never both.
         * @property {boolean|null} [executed] A flag indicating this schedule has executed.
         * <p>
         * A schedule SHALL either be executed or deleted, but never both.
         * @property {boolean|null} [waitForExpiry] A schedule flag to wait for expiration before executing.
         * <p>
         * A schedule SHALL be executed immediately when all necessary signatures
         * are gathered, unless this flag is set.<br/>
         * If this flag is set, the schedule SHALL wait until the consensus time
         * reaches `expiration_time_provided`, when signatures MUST again be
         * verified. If all required signatures are present at that time, the
         * schedule SHALL be executed. Otherwise the schedule SHALL expire without
         * execution.
         * <p>
         * Note that a schedule is always removed from state after it expires,
         * regardless of whether it was executed or not.
         * @property {string|null} [memo] A short description for this schedule.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {proto.AccountID.$Properties|null} [schedulerAccountId] The scheduler account for this schedule.
         * <p>
         * This SHALL be the account that submitted the original
         * ScheduleCreate transaction.
         * @property {proto.AccountID.$Properties|null} [payerAccountId] The explicit payer account for the scheduled transaction.
         * <p>
         * If set, this account SHALL be added to the accounts that MUST sign the
         * schedule before it may execute.
         * @property {proto.Key.$Properties|null} [adminKey] The admin key for this schedule.
         * <p>
         * This key, if set, MUST sign any `schedule_delete` transaction.<br/>
         * If not set, then this schedule SHALL NOT be deleted, and any
         * `schedule_delete` transaction for this schedule SHALL fail.
         * @property {proto.Timestamp.$Properties|null} [scheduleValidStart] The transaction valid start value for this schedule.
         * <p>
         * This MUST be set, and SHALL be copied from the `TransactionID` of
         * the original `schedule_create` transaction.
         * @property {number|Long|null} [providedExpirationSecond] The requested expiration time of the schedule if provided by the user.
         * <p>
         * If not provided in the `schedule_create` transaction, this SHALL be set
         * to a default value equal to the current consensus time, forward offset by
         * the maximum schedule expiration time in the current dynamic network
         * configuration (typically 62 days).<br/>
         * The actual `calculated_expiration_second` MAY be "earlier" than this,
         * but MUST NOT be later.
         * @property {number|Long|null} [calculatedExpirationSecond] The calculated expiration time of the schedule.
         * <p>
         * This SHALL be calculated from the requested expiration time in the
         * `schedule_create` transaction, and limited by the maximum expiration time
         * in the current dynamic network configuration (typically 62 days).
         * <p>
         * The schedule SHALL be removed from global network state after the network
         * reaches a consensus time greater than or equal to this value.
         * @property {proto.Timestamp.$Properties|null} [resolutionTime] The consensus timestamp of the transaction that executed or deleted this schedule.
         * <p>
         * This value SHALL be set to the `current_consensus_time` when a
         * `schedule_delete` transaction is completed.<br/>
         * This value SHALL be set to the `current_consensus_time` when the
         * scheduled transaction is executed, either as a result of gathering the
         * final required signature, or, if long-term schedule execution is enabled,
         * at the requested execution time.
         * @property {proto.SchedulableTransactionBody.$Properties|null} [scheduledTransaction] The scheduled transaction to execute.
         * <p>
         * This MUST be one of the transaction types permitted in the current value
         * of the `schedule.whitelist` in the dynamic network configuration.
         * @property {proto.TransactionBody.$Properties|null} [originalCreateTransaction] The full transaction that created this schedule.
         * <p>
         * This is primarily used for duplicate schedule create detection. This is
         * also the source of the parent transaction ID, from which the child
         * transaction ID is derived when the `scheduled_transaction` is executed.
         * @property {Array.<proto.Key.$Properties>|null} [signatories] All of the "primitive" keys that have already signed this schedule.
         * <p>
         * The scheduled transaction SHALL NOT be executed before this list is
         * sufficient to "activate" the required keys for the scheduled transaction.<br/>
         * A Key SHALL NOT be stored in this list unless the corresponding private
         * key has signed either the original `schedule_create` transaction or a
         * subsequent `schedule_sign` transaction intended for, and referencing to,
         * this specific schedule.
         * <p>
         * The only keys stored are "primitive" keys (ED25519 or ECDSA_SECP256K1) in
         * order to ensure that any key list or threshold keys are correctly handled,
         * regardless of signing order, intervening changes, or other situations.
         * The `scheduled_transaction` SHALL execute only if, at the time of
         * execution, this list contains sufficient public keys to satisfy the
         * full requirements for signature on that transaction.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Schedule.
         * @memberof proto
         * @interface ISchedule
         * @augments proto.Schedule.$Properties
         * @deprecated Use proto.Schedule.$Properties instead.
         */

        /**
         * Shape of a Schedule.
         * @typedef {{
         *   scheduleId?: proto.ScheduleID.$Shape|null;
         *   deleted?: boolean|null;
         *   executed?: boolean|null;
         *   waitForExpiry?: boolean|null;
         *   memo?: string|null;
         *   schedulerAccountId?: proto.AccountID.$Shape|null;
         *   payerAccountId?: proto.AccountID.$Shape|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   scheduleValidStart?: proto.Timestamp.$Shape|null;
         *   providedExpirationSecond?: number|Long|null;
         *   calculatedExpirationSecond?: number|Long|null;
         *   resolutionTime?: proto.Timestamp.$Shape|null;
         *   scheduledTransaction?: proto.SchedulableTransactionBody.$Shape|null;
         *   originalCreateTransaction?: proto.TransactionBody.$Shape|null;
         *   signatories?: Array.<proto.Key.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.Schedule.$Shape
         */

        /**
         * Constructs a new Schedule.
         * @memberof proto
         * @classdesc Representation of a Hedera Schedule entry in the network Merkle tree.<br/>
         * A Schedule represents a request to run a transaction _at some future time_
         * either when the `Schedule` expires (if long term schedules are enabled and
         * `wait_for_expiry` is true) or as soon as the `Schedule` has gathered
         * enough signatures via any combination of the `scheduleCreate` and 0 or more
         * subsequent `scheduleSign` transactions.
         * @constructor
         * @param {proto.Schedule.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Schedule = function (properties) {
            this.signatories = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * This schedule's ID within the global network state.
         * <p>
         * This value SHALL be unique within the network.
         * @member {proto.ScheduleID.$Properties|null|undefined} scheduleId
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.scheduleId = null;

        /**
         * A flag indicating this schedule is deleted.
         * <p>
         * A schedule SHALL either be executed or deleted, but never both.
         * @member {boolean} deleted
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.deleted = false;

        /**
         * A flag indicating this schedule has executed.
         * <p>
         * A schedule SHALL either be executed or deleted, but never both.
         * @member {boolean} executed
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.executed = false;

        /**
         * A schedule flag to wait for expiration before executing.
         * <p>
         * A schedule SHALL be executed immediately when all necessary signatures
         * are gathered, unless this flag is set.<br/>
         * If this flag is set, the schedule SHALL wait until the consensus time
         * reaches `expiration_time_provided`, when signatures MUST again be
         * verified. If all required signatures are present at that time, the
         * schedule SHALL be executed. Otherwise the schedule SHALL expire without
         * execution.
         * <p>
         * Note that a schedule is always removed from state after it expires,
         * regardless of whether it was executed or not.
         * @member {boolean} waitForExpiry
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.waitForExpiry = false;

        /**
         * A short description for this schedule.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.memo = "";

        /**
         * The scheduler account for this schedule.
         * <p>
         * This SHALL be the account that submitted the original
         * ScheduleCreate transaction.
         * @member {proto.AccountID.$Properties|null|undefined} schedulerAccountId
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.schedulerAccountId = null;

        /**
         * The explicit payer account for the scheduled transaction.
         * <p>
         * If set, this account SHALL be added to the accounts that MUST sign the
         * schedule before it may execute.
         * @member {proto.AccountID.$Properties|null|undefined} payerAccountId
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.payerAccountId = null;

        /**
         * The admin key for this schedule.
         * <p>
         * This key, if set, MUST sign any `schedule_delete` transaction.<br/>
         * If not set, then this schedule SHALL NOT be deleted, and any
         * `schedule_delete` transaction for this schedule SHALL fail.
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.adminKey = null;

        /**
         * The transaction valid start value for this schedule.
         * <p>
         * This MUST be set, and SHALL be copied from the `TransactionID` of
         * the original `schedule_create` transaction.
         * @member {proto.Timestamp.$Properties|null|undefined} scheduleValidStart
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.scheduleValidStart = null;

        /**
         * The requested expiration time of the schedule if provided by the user.
         * <p>
         * If not provided in the `schedule_create` transaction, this SHALL be set
         * to a default value equal to the current consensus time, forward offset by
         * the maximum schedule expiration time in the current dynamic network
         * configuration (typically 62 days).<br/>
         * The actual `calculated_expiration_second` MAY be "earlier" than this,
         * but MUST NOT be later.
         * @member {number|Long} providedExpirationSecond
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.providedExpirationSecond = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The calculated expiration time of the schedule.
         * <p>
         * This SHALL be calculated from the requested expiration time in the
         * `schedule_create` transaction, and limited by the maximum expiration time
         * in the current dynamic network configuration (typically 62 days).
         * <p>
         * The schedule SHALL be removed from global network state after the network
         * reaches a consensus time greater than or equal to this value.
         * @member {number|Long} calculatedExpirationSecond
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.calculatedExpirationSecond = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The consensus timestamp of the transaction that executed or deleted this schedule.
         * <p>
         * This value SHALL be set to the `current_consensus_time` when a
         * `schedule_delete` transaction is completed.<br/>
         * This value SHALL be set to the `current_consensus_time` when the
         * scheduled transaction is executed, either as a result of gathering the
         * final required signature, or, if long-term schedule execution is enabled,
         * at the requested execution time.
         * @member {proto.Timestamp.$Properties|null|undefined} resolutionTime
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.resolutionTime = null;

        /**
         * The scheduled transaction to execute.
         * <p>
         * This MUST be one of the transaction types permitted in the current value
         * of the `schedule.whitelist` in the dynamic network configuration.
         * @member {proto.SchedulableTransactionBody.$Properties|null|undefined} scheduledTransaction
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.scheduledTransaction = null;

        /**
         * The full transaction that created this schedule.
         * <p>
         * This is primarily used for duplicate schedule create detection. This is
         * also the source of the parent transaction ID, from which the child
         * transaction ID is derived when the `scheduled_transaction` is executed.
         * @member {proto.TransactionBody.$Properties|null|undefined} originalCreateTransaction
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.originalCreateTransaction = null;

        /**
         * All of the "primitive" keys that have already signed this schedule.
         * <p>
         * The scheduled transaction SHALL NOT be executed before this list is
         * sufficient to "activate" the required keys for the scheduled transaction.<br/>
         * A Key SHALL NOT be stored in this list unless the corresponding private
         * key has signed either the original `schedule_create` transaction or a
         * subsequent `schedule_sign` transaction intended for, and referencing to,
         * this specific schedule.
         * <p>
         * The only keys stored are "primitive" keys (ED25519 or ECDSA_SECP256K1) in
         * order to ensure that any key list or threshold keys are correctly handled,
         * regardless of signing order, intervening changes, or other situations.
         * The `scheduled_transaction` SHALL execute only if, at the time of
         * execution, this list contains sufficient public keys to satisfy the
         * full requirements for signature on that transaction.
         * @member {Array.<proto.Key.$Properties>} signatories
         * @memberof proto.Schedule
         * @instance
         */
        Schedule.prototype.signatories = $util.emptyArray;

        /**
         * Creates a new Schedule instance using the specified properties.
         * @function create
         * @memberof proto.Schedule
         * @static
         * @param {proto.Schedule.$Properties=} [properties] Properties to set
         * @returns {proto.Schedule} Schedule instance
         * @type {{
         *   (properties: proto.Schedule.$Shape): proto.Schedule & proto.Schedule.$Shape;
         *   (properties?: proto.Schedule.$Properties): proto.Schedule;
         * }}
         */
        Schedule.create = function(properties) {
            return new Schedule(properties);
        };

        /**
         * Encodes the specified Schedule message. Does not implicitly {@link proto.Schedule.verify|verify} messages.
         * @function encode
         * @memberof proto.Schedule
         * @static
         * @param {proto.Schedule.$Properties} message Schedule message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Schedule.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.scheduleId != null && $Object.hasOwnProperty.call(message, "scheduleId"))
                $root.proto.ScheduleID.encode(message.scheduleId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.deleted);
            if (message.executed != null && $Object.hasOwnProperty.call(message, "executed") && message.executed !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.executed);
            if (message.waitForExpiry != null && $Object.hasOwnProperty.call(message, "waitForExpiry") && message.waitForExpiry !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.waitForExpiry);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.memo);
            if (message.schedulerAccountId != null && $Object.hasOwnProperty.call(message, "schedulerAccountId"))
                $root.proto.AccountID.encode(message.schedulerAccountId, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.payerAccountId != null && $Object.hasOwnProperty.call(message, "payerAccountId"))
                $root.proto.AccountID.encode(message.payerAccountId, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.scheduleValidStart != null && $Object.hasOwnProperty.call(message, "scheduleValidStart"))
                $root.proto.Timestamp.encode(message.scheduleValidStart, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.providedExpirationSecond != null && $Object.hasOwnProperty.call(message, "providedExpirationSecond") && (typeof message.providedExpirationSecond === "object" ? message.providedExpirationSecond.low || message.providedExpirationSecond.high : message.providedExpirationSecond !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.providedExpirationSecond);
            if (message.calculatedExpirationSecond != null && $Object.hasOwnProperty.call(message, "calculatedExpirationSecond") && (typeof message.calculatedExpirationSecond === "object" ? message.calculatedExpirationSecond.low || message.calculatedExpirationSecond.high : message.calculatedExpirationSecond !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.calculatedExpirationSecond);
            if (message.resolutionTime != null && $Object.hasOwnProperty.call(message, "resolutionTime"))
                $root.proto.Timestamp.encode(message.resolutionTime, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.scheduledTransaction != null && $Object.hasOwnProperty.call(message, "scheduledTransaction"))
                $root.proto.SchedulableTransactionBody.encode(message.scheduledTransaction, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.originalCreateTransaction != null && $Object.hasOwnProperty.call(message, "originalCreateTransaction"))
                $root.proto.TransactionBody.encode(message.originalCreateTransaction, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.signatories != null && message.signatories.length)
                for (let i = 0; i < message.signatories.length; ++i)
                    $root.proto.Key.encode(message.signatories[i], writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Schedule message, length delimited. Does not implicitly {@link proto.Schedule.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Schedule
         * @static
         * @param {proto.Schedule.$Properties} message Schedule message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Schedule.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Schedule message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Schedule
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Schedule & proto.Schedule.$Shape} Schedule
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Schedule.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Schedule();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.scheduleId = $root.proto.ScheduleID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.deleted = value;
                        else
                            delete message.deleted;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.executed = value;
                        else
                            delete message.executed;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.waitForExpiry = value;
                        else
                            delete message.waitForExpiry;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.schedulerAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.schedulerAccountId);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.payerAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.payerAccountId);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.scheduleValidStart = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduleValidStart);
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.providedExpirationSecond = value;
                        else
                            delete message.providedExpirationSecond;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.calculatedExpirationSecond = value;
                        else
                            delete message.calculatedExpirationSecond;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.resolutionTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.resolutionTime);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.scheduledTransaction = $root.proto.SchedulableTransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scheduledTransaction);
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.originalCreateTransaction = $root.proto.TransactionBody.decode(reader, reader.uint32(), $undefined, _depth + 1, message.originalCreateTransaction);
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (!(message.signatories && message.signatories.length))
                            message.signatories = [];
                        message.signatories.push($root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Schedule message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Schedule
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Schedule & proto.Schedule.$Shape} Schedule
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Schedule.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Schedule message.
         * @function verify
         * @memberof proto.Schedule
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Schedule.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.scheduleId != null && $Object.hasOwnProperty.call(message, "scheduleId")) {
                let error = $root.proto.ScheduleID.verify(message.scheduleId, _depth + 1);
                if (error)
                    return "scheduleId." + error;
            }
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                if (typeof message.deleted !== "boolean")
                    return "deleted: boolean expected";
            if (message.executed != null && $Object.hasOwnProperty.call(message, "executed"))
                if (typeof message.executed !== "boolean")
                    return "executed: boolean expected";
            if (message.waitForExpiry != null && $Object.hasOwnProperty.call(message, "waitForExpiry"))
                if (typeof message.waitForExpiry !== "boolean")
                    return "waitForExpiry: boolean expected";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.schedulerAccountId != null && $Object.hasOwnProperty.call(message, "schedulerAccountId")) {
                let error = $root.proto.AccountID.verify(message.schedulerAccountId, _depth + 1);
                if (error)
                    return "schedulerAccountId." + error;
            }
            if (message.payerAccountId != null && $Object.hasOwnProperty.call(message, "payerAccountId")) {
                let error = $root.proto.AccountID.verify(message.payerAccountId, _depth + 1);
                if (error)
                    return "payerAccountId." + error;
            }
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.scheduleValidStart != null && $Object.hasOwnProperty.call(message, "scheduleValidStart")) {
                let error = $root.proto.Timestamp.verify(message.scheduleValidStart, _depth + 1);
                if (error)
                    return "scheduleValidStart." + error;
            }
            if (message.providedExpirationSecond != null && $Object.hasOwnProperty.call(message, "providedExpirationSecond"))
                if (!$util.isInteger(message.providedExpirationSecond) && !(message.providedExpirationSecond && $util.isInteger(message.providedExpirationSecond.low) && $util.isInteger(message.providedExpirationSecond.high)))
                    return "providedExpirationSecond: integer|Long expected";
            if (message.calculatedExpirationSecond != null && $Object.hasOwnProperty.call(message, "calculatedExpirationSecond"))
                if (!$util.isInteger(message.calculatedExpirationSecond) && !(message.calculatedExpirationSecond && $util.isInteger(message.calculatedExpirationSecond.low) && $util.isInteger(message.calculatedExpirationSecond.high)))
                    return "calculatedExpirationSecond: integer|Long expected";
            if (message.resolutionTime != null && $Object.hasOwnProperty.call(message, "resolutionTime")) {
                let error = $root.proto.Timestamp.verify(message.resolutionTime, _depth + 1);
                if (error)
                    return "resolutionTime." + error;
            }
            if (message.scheduledTransaction != null && $Object.hasOwnProperty.call(message, "scheduledTransaction")) {
                let error = $root.proto.SchedulableTransactionBody.verify(message.scheduledTransaction, _depth + 1);
                if (error)
                    return "scheduledTransaction." + error;
            }
            if (message.originalCreateTransaction != null && $Object.hasOwnProperty.call(message, "originalCreateTransaction")) {
                let error = $root.proto.TransactionBody.verify(message.originalCreateTransaction, _depth + 1);
                if (error)
                    return "originalCreateTransaction." + error;
            }
            if (message.signatories != null && $Object.hasOwnProperty.call(message, "signatories")) {
                if (!$Array.isArray(message.signatories))
                    return "signatories: array expected";
                for (let i = 0; i < message.signatories.length; ++i) {
                    let error = $root.proto.Key.verify(message.signatories[i], _depth + 1);
                    if (error)
                        return "signatories." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Schedule message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Schedule
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Schedule} Schedule
         */
        Schedule.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Schedule)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Schedule: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Schedule();
            if (object.scheduleId != null) {
                if (!$util.isObject(object.scheduleId))
                    throw $TypeError(".proto.Schedule.scheduleId: object expected");
                message.scheduleId = $root.proto.ScheduleID.fromObject(object.scheduleId, _depth + 1);
            }
            if (object.deleted != null)
                if (object.deleted)
                    message.deleted = $Boolean(object.deleted);
            if (object.executed != null)
                if (object.executed)
                    message.executed = $Boolean(object.executed);
            if (object.waitForExpiry != null)
                if (object.waitForExpiry)
                    message.waitForExpiry = $Boolean(object.waitForExpiry);
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.schedulerAccountId != null) {
                if (!$util.isObject(object.schedulerAccountId))
                    throw $TypeError(".proto.Schedule.schedulerAccountId: object expected");
                message.schedulerAccountId = $root.proto.AccountID.fromObject(object.schedulerAccountId, _depth + 1);
            }
            if (object.payerAccountId != null) {
                if (!$util.isObject(object.payerAccountId))
                    throw $TypeError(".proto.Schedule.payerAccountId: object expected");
                message.payerAccountId = $root.proto.AccountID.fromObject(object.payerAccountId, _depth + 1);
            }
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.Schedule.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.scheduleValidStart != null) {
                if (!$util.isObject(object.scheduleValidStart))
                    throw $TypeError(".proto.Schedule.scheduleValidStart: object expected");
                message.scheduleValidStart = $root.proto.Timestamp.fromObject(object.scheduleValidStart, _depth + 1);
            }
            if (object.providedExpirationSecond != null)
                if (typeof object.providedExpirationSecond === "object" ? object.providedExpirationSecond.low || object.providedExpirationSecond.high : $Number(object.providedExpirationSecond) !== 0)
                    if ($util.Long)
                        message.providedExpirationSecond = $util.Long.fromValue(object.providedExpirationSecond, false);
                    else if (typeof object.providedExpirationSecond === "string")
                        message.providedExpirationSecond = $parseInt(object.providedExpirationSecond, 10);
                    else if (typeof object.providedExpirationSecond === "number")
                        message.providedExpirationSecond = object.providedExpirationSecond;
                    else if (typeof object.providedExpirationSecond === "object")
                        message.providedExpirationSecond = new $util.LongBits(object.providedExpirationSecond.low >>> 0, object.providedExpirationSecond.high >>> 0).toNumber();
            if (object.calculatedExpirationSecond != null)
                if (typeof object.calculatedExpirationSecond === "object" ? object.calculatedExpirationSecond.low || object.calculatedExpirationSecond.high : $Number(object.calculatedExpirationSecond) !== 0)
                    if ($util.Long)
                        message.calculatedExpirationSecond = $util.Long.fromValue(object.calculatedExpirationSecond, false);
                    else if (typeof object.calculatedExpirationSecond === "string")
                        message.calculatedExpirationSecond = $parseInt(object.calculatedExpirationSecond, 10);
                    else if (typeof object.calculatedExpirationSecond === "number")
                        message.calculatedExpirationSecond = object.calculatedExpirationSecond;
                    else if (typeof object.calculatedExpirationSecond === "object")
                        message.calculatedExpirationSecond = new $util.LongBits(object.calculatedExpirationSecond.low >>> 0, object.calculatedExpirationSecond.high >>> 0).toNumber();
            if (object.resolutionTime != null) {
                if (!$util.isObject(object.resolutionTime))
                    throw $TypeError(".proto.Schedule.resolutionTime: object expected");
                message.resolutionTime = $root.proto.Timestamp.fromObject(object.resolutionTime, _depth + 1);
            }
            if (object.scheduledTransaction != null) {
                if (!$util.isObject(object.scheduledTransaction))
                    throw $TypeError(".proto.Schedule.scheduledTransaction: object expected");
                message.scheduledTransaction = $root.proto.SchedulableTransactionBody.fromObject(object.scheduledTransaction, _depth + 1);
            }
            if (object.originalCreateTransaction != null) {
                if (!$util.isObject(object.originalCreateTransaction))
                    throw $TypeError(".proto.Schedule.originalCreateTransaction: object expected");
                message.originalCreateTransaction = $root.proto.TransactionBody.fromObject(object.originalCreateTransaction, _depth + 1);
            }
            if (object.signatories) {
                if (!$Array.isArray(object.signatories))
                    throw $TypeError(".proto.Schedule.signatories: array expected");
                message.signatories = $Array(object.signatories.length);
                for (let i = 0; i < object.signatories.length; ++i) {
                    if (!$util.isObject(object.signatories[i]))
                        throw $TypeError(".proto.Schedule.signatories: object expected");
                    message.signatories[i] = $root.proto.Key.fromObject(object.signatories[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Schedule message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Schedule
         * @static
         * @param {proto.Schedule} message Schedule
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Schedule.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.signatories = [];
            if (options.defaults) {
                object.scheduleId = null;
                object.deleted = false;
                object.executed = false;
                object.waitForExpiry = false;
                object.memo = "";
                object.schedulerAccountId = null;
                object.payerAccountId = null;
                object.adminKey = null;
                object.scheduleValidStart = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.providedExpirationSecond = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.providedExpirationSecond = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.calculatedExpirationSecond = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.calculatedExpirationSecond = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.resolutionTime = null;
                object.scheduledTransaction = null;
                object.originalCreateTransaction = null;
            }
            if (message.scheduleId != null && $Object.hasOwnProperty.call(message, "scheduleId"))
                object.scheduleId = $root.proto.ScheduleID.toObject(message.scheduleId, options, _depth + 1);
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                object.deleted = message.deleted;
            if (message.executed != null && $Object.hasOwnProperty.call(message, "executed"))
                object.executed = message.executed;
            if (message.waitForExpiry != null && $Object.hasOwnProperty.call(message, "waitForExpiry"))
                object.waitForExpiry = message.waitForExpiry;
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.schedulerAccountId != null && $Object.hasOwnProperty.call(message, "schedulerAccountId"))
                object.schedulerAccountId = $root.proto.AccountID.toObject(message.schedulerAccountId, options, _depth + 1);
            if (message.payerAccountId != null && $Object.hasOwnProperty.call(message, "payerAccountId"))
                object.payerAccountId = $root.proto.AccountID.toObject(message.payerAccountId, options, _depth + 1);
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.scheduleValidStart != null && $Object.hasOwnProperty.call(message, "scheduleValidStart"))
                object.scheduleValidStart = $root.proto.Timestamp.toObject(message.scheduleValidStart, options, _depth + 1);
            if (message.providedExpirationSecond != null && $Object.hasOwnProperty.call(message, "providedExpirationSecond"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.providedExpirationSecond = typeof message.providedExpirationSecond === "number" ? $BigInt(message.providedExpirationSecond) : $util.Long.fromBits(message.providedExpirationSecond.low >>> 0, message.providedExpirationSecond.high >>> 0, false).toBigInt();
                else if (typeof message.providedExpirationSecond === "number")
                    object.providedExpirationSecond = options.longs === $String ? $String(message.providedExpirationSecond) : message.providedExpirationSecond;
                else
                    object.providedExpirationSecond = options.longs === $String ? $util.Long.prototype.toString.call(message.providedExpirationSecond) : options.longs === $Number ? new $util.LongBits(message.providedExpirationSecond.low >>> 0, message.providedExpirationSecond.high >>> 0).toNumber() : message.providedExpirationSecond;
            if (message.calculatedExpirationSecond != null && $Object.hasOwnProperty.call(message, "calculatedExpirationSecond"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.calculatedExpirationSecond = typeof message.calculatedExpirationSecond === "number" ? $BigInt(message.calculatedExpirationSecond) : $util.Long.fromBits(message.calculatedExpirationSecond.low >>> 0, message.calculatedExpirationSecond.high >>> 0, false).toBigInt();
                else if (typeof message.calculatedExpirationSecond === "number")
                    object.calculatedExpirationSecond = options.longs === $String ? $String(message.calculatedExpirationSecond) : message.calculatedExpirationSecond;
                else
                    object.calculatedExpirationSecond = options.longs === $String ? $util.Long.prototype.toString.call(message.calculatedExpirationSecond) : options.longs === $Number ? new $util.LongBits(message.calculatedExpirationSecond.low >>> 0, message.calculatedExpirationSecond.high >>> 0).toNumber() : message.calculatedExpirationSecond;
            if (message.resolutionTime != null && $Object.hasOwnProperty.call(message, "resolutionTime"))
                object.resolutionTime = $root.proto.Timestamp.toObject(message.resolutionTime, options, _depth + 1);
            if (message.scheduledTransaction != null && $Object.hasOwnProperty.call(message, "scheduledTransaction"))
                object.scheduledTransaction = $root.proto.SchedulableTransactionBody.toObject(message.scheduledTransaction, options, _depth + 1);
            if (message.originalCreateTransaction != null && $Object.hasOwnProperty.call(message, "originalCreateTransaction"))
                object.originalCreateTransaction = $root.proto.TransactionBody.toObject(message.originalCreateTransaction, options, _depth + 1);
            if (message.signatories && message.signatories.length) {
                object.signatories = $Array(message.signatories.length);
                for (let j = 0; j < message.signatories.length; ++j)
                    object.signatories[j] = $root.proto.Key.toObject(message.signatories[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this Schedule to JSON.
         * @function toJSON
         * @memberof proto.Schedule
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Schedule.prototype.toJSON = function() {
            return Schedule.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Schedule
         * @function getTypeUrl
         * @memberof proto.Schedule
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Schedule.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Schedule";
        };

        return Schedule;
    })();

    proto.ScheduleList = (function() {

        /**
         * Properties of a ScheduleList.
         * @typedef {Object} proto.ScheduleList.$Properties
         * @property {Array.<proto.Schedule.$Properties>|null} [schedules] a list of schedules, in no particular order.
         * <p>
         * While the order is not _specified_, it MUST be deterministic.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ScheduleList.
         * @memberof proto
         * @interface IScheduleList
         * @augments proto.ScheduleList.$Properties
         * @deprecated Use proto.ScheduleList.$Properties instead.
         */

        /**
         * Shape of a ScheduleList.
         * @typedef {{
         *   schedules?: Array.<proto.Schedule.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ScheduleList.$Shape
         */

        /**
         * Constructs a new ScheduleList.
         * @memberof proto
         * @classdesc A message for storing a list of schedules in state.<br/>
         * This is used to store lists of `Schedule` values.
         * One example is all schedules that expire at a particular time.
         * @constructor
         * @param {proto.ScheduleList.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ScheduleList = function (properties) {
            this.schedules = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * a list of schedules, in no particular order.
         * <p>
         * While the order is not _specified_, it MUST be deterministic.
         * @member {Array.<proto.Schedule.$Properties>} schedules
         * @memberof proto.ScheduleList
         * @instance
         */
        ScheduleList.prototype.schedules = $util.emptyArray;

        /**
         * Creates a new ScheduleList instance using the specified properties.
         * @function create
         * @memberof proto.ScheduleList
         * @static
         * @param {proto.ScheduleList.$Properties=} [properties] Properties to set
         * @returns {proto.ScheduleList} ScheduleList instance
         * @type {{
         *   (properties: proto.ScheduleList.$Shape): proto.ScheduleList & proto.ScheduleList.$Shape;
         *   (properties?: proto.ScheduleList.$Properties): proto.ScheduleList;
         * }}
         */
        ScheduleList.create = function(properties) {
            return new ScheduleList(properties);
        };

        /**
         * Encodes the specified ScheduleList message. Does not implicitly {@link proto.ScheduleList.verify|verify} messages.
         * @function encode
         * @memberof proto.ScheduleList
         * @static
         * @param {proto.ScheduleList.$Properties} message ScheduleList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleList.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.schedules != null && message.schedules.length)
                for (let i = 0; i < message.schedules.length; ++i)
                    $root.proto.Schedule.encode(message.schedules[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ScheduleList message, length delimited. Does not implicitly {@link proto.ScheduleList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ScheduleList
         * @static
         * @param {proto.ScheduleList.$Properties} message ScheduleList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleList.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ScheduleList message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ScheduleList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ScheduleList & proto.ScheduleList.$Shape} ScheduleList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleList.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ScheduleList();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.schedules && message.schedules.length))
                            message.schedules = [];
                        message.schedules.push($root.proto.Schedule.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ScheduleList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ScheduleList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ScheduleList & proto.ScheduleList.$Shape} ScheduleList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleList.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScheduleList message.
         * @function verify
         * @memberof proto.ScheduleList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScheduleList.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.schedules != null && $Object.hasOwnProperty.call(message, "schedules")) {
                if (!$Array.isArray(message.schedules))
                    return "schedules: array expected";
                for (let i = 0; i < message.schedules.length; ++i) {
                    let error = $root.proto.Schedule.verify(message.schedules[i], _depth + 1);
                    if (error)
                        return "schedules." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ScheduleList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ScheduleList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ScheduleList} ScheduleList
         */
        ScheduleList.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ScheduleList)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ScheduleList: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ScheduleList();
            if (object.schedules) {
                if (!$Array.isArray(object.schedules))
                    throw $TypeError(".proto.ScheduleList.schedules: array expected");
                message.schedules = $Array(object.schedules.length);
                for (let i = 0; i < object.schedules.length; ++i) {
                    if (!$util.isObject(object.schedules[i]))
                        throw $TypeError(".proto.ScheduleList.schedules: object expected");
                    message.schedules[i] = $root.proto.Schedule.fromObject(object.schedules[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ScheduleList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ScheduleList
         * @static
         * @param {proto.ScheduleList} message ScheduleList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScheduleList.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.schedules = [];
            if (message.schedules && message.schedules.length) {
                object.schedules = $Array(message.schedules.length);
                for (let j = 0; j < message.schedules.length; ++j)
                    object.schedules[j] = $root.proto.Schedule.toObject(message.schedules[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ScheduleList to JSON.
         * @function toJSON
         * @memberof proto.ScheduleList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScheduleList.prototype.toJSON = function() {
            return ScheduleList.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ScheduleList
         * @function getTypeUrl
         * @memberof proto.ScheduleList
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ScheduleList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ScheduleList";
        };

        return ScheduleList;
    })();

    proto.ScheduleIdList = (function() {

        /**
         * Properties of a ScheduleIdList.
         * @typedef {Object} proto.ScheduleIdList.$Properties
         * @property {Array.<proto.ScheduleID.$Properties>|null} [scheduleIds] A list of schedule identifiers, in no particular order.
         * <p>
         * While the order is not _specified_, it MUST be deterministic.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ScheduleIdList.
         * @memberof proto
         * @interface IScheduleIdList
         * @augments proto.ScheduleIdList.$Properties
         * @deprecated Use proto.ScheduleIdList.$Properties instead.
         */

        /**
         * Shape of a ScheduleIdList.
         * @typedef {proto.ScheduleIdList.$Properties} proto.ScheduleIdList.$Shape
         */

        /**
         * Constructs a new ScheduleIdList.
         * @memberof proto
         * @classdesc A message for storing a list of schedule identifiers in state.<br/>
         * This is used to store lists of `ScheduleID` values.
         * One example is all schedules that expire at a particular time.
         * @constructor
         * @param {proto.ScheduleIdList.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ScheduleIdList = function (properties) {
            this.scheduleIds = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of schedule identifiers, in no particular order.
         * <p>
         * While the order is not _specified_, it MUST be deterministic.
         * @member {Array.<proto.ScheduleID.$Properties>} scheduleIds
         * @memberof proto.ScheduleIdList
         * @instance
         */
        ScheduleIdList.prototype.scheduleIds = $util.emptyArray;

        /**
         * Creates a new ScheduleIdList instance using the specified properties.
         * @function create
         * @memberof proto.ScheduleIdList
         * @static
         * @param {proto.ScheduleIdList.$Properties=} [properties] Properties to set
         * @returns {proto.ScheduleIdList} ScheduleIdList instance
         * @type {{
         *   (properties: proto.ScheduleIdList.$Shape): proto.ScheduleIdList & proto.ScheduleIdList.$Shape;
         *   (properties?: proto.ScheduleIdList.$Properties): proto.ScheduleIdList;
         * }}
         */
        ScheduleIdList.create = function(properties) {
            return new ScheduleIdList(properties);
        };

        /**
         * Encodes the specified ScheduleIdList message. Does not implicitly {@link proto.ScheduleIdList.verify|verify} messages.
         * @function encode
         * @memberof proto.ScheduleIdList
         * @static
         * @param {proto.ScheduleIdList.$Properties} message ScheduleIdList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleIdList.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.scheduleIds != null && message.scheduleIds.length)
                for (let i = 0; i < message.scheduleIds.length; ++i)
                    $root.proto.ScheduleID.encode(message.scheduleIds[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ScheduleIdList message, length delimited. Does not implicitly {@link proto.ScheduleIdList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ScheduleIdList
         * @static
         * @param {proto.ScheduleIdList.$Properties} message ScheduleIdList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduleIdList.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ScheduleIdList message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ScheduleIdList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ScheduleIdList & proto.ScheduleIdList.$Shape} ScheduleIdList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleIdList.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ScheduleIdList();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.scheduleIds && message.scheduleIds.length))
                            message.scheduleIds = [];
                        message.scheduleIds.push($root.proto.ScheduleID.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ScheduleIdList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ScheduleIdList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ScheduleIdList & proto.ScheduleIdList.$Shape} ScheduleIdList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduleIdList.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScheduleIdList message.
         * @function verify
         * @memberof proto.ScheduleIdList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScheduleIdList.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.scheduleIds != null && $Object.hasOwnProperty.call(message, "scheduleIds")) {
                if (!$Array.isArray(message.scheduleIds))
                    return "scheduleIds: array expected";
                for (let i = 0; i < message.scheduleIds.length; ++i) {
                    let error = $root.proto.ScheduleID.verify(message.scheduleIds[i], _depth + 1);
                    if (error)
                        return "scheduleIds." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ScheduleIdList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ScheduleIdList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ScheduleIdList} ScheduleIdList
         */
        ScheduleIdList.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ScheduleIdList)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ScheduleIdList: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ScheduleIdList();
            if (object.scheduleIds) {
                if (!$Array.isArray(object.scheduleIds))
                    throw $TypeError(".proto.ScheduleIdList.scheduleIds: array expected");
                message.scheduleIds = $Array(object.scheduleIds.length);
                for (let i = 0; i < object.scheduleIds.length; ++i) {
                    if (!$util.isObject(object.scheduleIds[i]))
                        throw $TypeError(".proto.ScheduleIdList.scheduleIds: object expected");
                    message.scheduleIds[i] = $root.proto.ScheduleID.fromObject(object.scheduleIds[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ScheduleIdList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ScheduleIdList
         * @static
         * @param {proto.ScheduleIdList} message ScheduleIdList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScheduleIdList.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.scheduleIds = [];
            if (message.scheduleIds && message.scheduleIds.length) {
                object.scheduleIds = $Array(message.scheduleIds.length);
                for (let j = 0; j < message.scheduleIds.length; ++j)
                    object.scheduleIds[j] = $root.proto.ScheduleID.toObject(message.scheduleIds[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ScheduleIdList to JSON.
         * @function toJSON
         * @memberof proto.ScheduleIdList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScheduleIdList.prototype.toJSON = function() {
            return ScheduleIdList.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ScheduleIdList
         * @function getTypeUrl
         * @memberof proto.ScheduleIdList
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ScheduleIdList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ScheduleIdList";
        };

        return ScheduleIdList;
    })();

    proto.ScheduledCounts = (function() {

        /**
         * Properties of a ScheduledCounts.
         * @typedef {Object} proto.ScheduledCounts.$Properties
         * @property {number|null} [numberScheduled] A number of transactions scheduled to expire at a consensus second.
         * @property {number|null} [numberProcessed] A number of scheduled transactions that have been processed at
         * a consensus second.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ScheduledCounts.
         * @memberof proto
         * @interface IScheduledCounts
         * @augments proto.ScheduledCounts.$Properties
         * @deprecated Use proto.ScheduledCounts.$Properties instead.
         */

        /**
         * Shape of a ScheduledCounts.
         * @typedef {proto.ScheduledCounts.$Properties} proto.ScheduledCounts.$Shape
         */

        /**
         * Constructs a new ScheduledCounts.
         * @memberof proto
         * @classdesc A count of schedules scheduled and processed.
         * This value summarizes the counts of scheduled and processed transactions
         * within a particular consensus second.
         * @constructor
         * @param {proto.ScheduledCounts.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ScheduledCounts = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A number of transactions scheduled to expire at a consensus second.
         * @member {number} numberScheduled
         * @memberof proto.ScheduledCounts
         * @instance
         */
        ScheduledCounts.prototype.numberScheduled = 0;

        /**
         * A number of scheduled transactions that have been processed at
         * a consensus second.
         * @member {number} numberProcessed
         * @memberof proto.ScheduledCounts
         * @instance
         */
        ScheduledCounts.prototype.numberProcessed = 0;

        /**
         * Creates a new ScheduledCounts instance using the specified properties.
         * @function create
         * @memberof proto.ScheduledCounts
         * @static
         * @param {proto.ScheduledCounts.$Properties=} [properties] Properties to set
         * @returns {proto.ScheduledCounts} ScheduledCounts instance
         * @type {{
         *   (properties: proto.ScheduledCounts.$Shape): proto.ScheduledCounts & proto.ScheduledCounts.$Shape;
         *   (properties?: proto.ScheduledCounts.$Properties): proto.ScheduledCounts;
         * }}
         */
        ScheduledCounts.create = function(properties) {
            return new ScheduledCounts(properties);
        };

        /**
         * Encodes the specified ScheduledCounts message. Does not implicitly {@link proto.ScheduledCounts.verify|verify} messages.
         * @function encode
         * @memberof proto.ScheduledCounts
         * @static
         * @param {proto.ScheduledCounts.$Properties} message ScheduledCounts message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduledCounts.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.numberScheduled != null && $Object.hasOwnProperty.call(message, "numberScheduled") && message.numberScheduled !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.numberScheduled);
            if (message.numberProcessed != null && $Object.hasOwnProperty.call(message, "numberProcessed") && message.numberProcessed !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.numberProcessed);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ScheduledCounts message, length delimited. Does not implicitly {@link proto.ScheduledCounts.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ScheduledCounts
         * @static
         * @param {proto.ScheduledCounts.$Properties} message ScheduledCounts message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduledCounts.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ScheduledCounts message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ScheduledCounts
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ScheduledCounts & proto.ScheduledCounts.$Shape} ScheduledCounts
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduledCounts.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ScheduledCounts();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.numberScheduled = value;
                        else
                            delete message.numberScheduled;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.numberProcessed = value;
                        else
                            delete message.numberProcessed;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ScheduledCounts message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ScheduledCounts
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ScheduledCounts & proto.ScheduledCounts.$Shape} ScheduledCounts
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduledCounts.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScheduledCounts message.
         * @function verify
         * @memberof proto.ScheduledCounts
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScheduledCounts.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.numberScheduled != null && $Object.hasOwnProperty.call(message, "numberScheduled"))
                if (!$util.isInteger(message.numberScheduled))
                    return "numberScheduled: integer expected";
            if (message.numberProcessed != null && $Object.hasOwnProperty.call(message, "numberProcessed"))
                if (!$util.isInteger(message.numberProcessed))
                    return "numberProcessed: integer expected";
            return null;
        };

        /**
         * Creates a ScheduledCounts message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ScheduledCounts
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ScheduledCounts} ScheduledCounts
         */
        ScheduledCounts.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ScheduledCounts)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ScheduledCounts: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ScheduledCounts();
            if (object.numberScheduled != null)
                if ($Number(object.numberScheduled) !== 0)
                    message.numberScheduled = object.numberScheduled >>> 0;
            if (object.numberProcessed != null)
                if ($Number(object.numberProcessed) !== 0)
                    message.numberProcessed = object.numberProcessed >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a ScheduledCounts message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ScheduledCounts
         * @static
         * @param {proto.ScheduledCounts} message ScheduledCounts
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScheduledCounts.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.numberScheduled = 0;
                object.numberProcessed = 0;
            }
            if (message.numberScheduled != null && $Object.hasOwnProperty.call(message, "numberScheduled"))
                object.numberScheduled = message.numberScheduled;
            if (message.numberProcessed != null && $Object.hasOwnProperty.call(message, "numberProcessed"))
                object.numberProcessed = message.numberProcessed;
            return object;
        };

        /**
         * Converts this ScheduledCounts to JSON.
         * @function toJSON
         * @memberof proto.ScheduledCounts
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScheduledCounts.prototype.toJSON = function() {
            return ScheduledCounts.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ScheduledCounts
         * @function getTypeUrl
         * @memberof proto.ScheduledCounts
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ScheduledCounts.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ScheduledCounts";
        };

        return ScheduledCounts;
    })();

    proto.ScheduledOrder = (function() {

        /**
         * Properties of a ScheduledOrder.
         * @typedef {Object} proto.ScheduledOrder.$Properties
         * @property {number|Long|null} [expirySecond] A consensus second in which the transaction is to be executed.
         * This is _also_ the consensus time when the transaction will expire
         * if it has not gathered enough signatures in time.
         * @property {number|null} [orderNumber] ScheduledOrder orderNumber
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ScheduledOrder.
         * @memberof proto
         * @interface IScheduledOrder
         * @augments proto.ScheduledOrder.$Properties
         * @deprecated Use proto.ScheduledOrder.$Properties instead.
         */

        /**
         * Shape of a ScheduledOrder.
         * @typedef {proto.ScheduledOrder.$Properties} proto.ScheduledOrder.$Shape
         */

        /**
         * Constructs a new ScheduledOrder.
         * @memberof proto
         * @classdesc An ordering for a scheduled transaction.<br/>
         * This establishes the order in which scheduled transactions intended to
         * execute at a particular consensus second will be executed.
         * 
         * Scheduled transactions that have the same `expiry_second` SHALL execute
         * in ascending order of `order_number`.
         * @constructor
         * @param {proto.ScheduledOrder.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ScheduledOrder = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A consensus second in which the transaction is to be executed.
         * This is _also_ the consensus time when the transaction will expire
         * if it has not gathered enough signatures in time.
         * @member {number|Long} expirySecond
         * @memberof proto.ScheduledOrder
         * @instance
         */
        ScheduledOrder.prototype.expirySecond = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ScheduledOrder orderNumber.
         * @member {number} orderNumber
         * @memberof proto.ScheduledOrder
         * @instance
         */
        ScheduledOrder.prototype.orderNumber = 0;

        /**
         * Creates a new ScheduledOrder instance using the specified properties.
         * @function create
         * @memberof proto.ScheduledOrder
         * @static
         * @param {proto.ScheduledOrder.$Properties=} [properties] Properties to set
         * @returns {proto.ScheduledOrder} ScheduledOrder instance
         * @type {{
         *   (properties: proto.ScheduledOrder.$Shape): proto.ScheduledOrder & proto.ScheduledOrder.$Shape;
         *   (properties?: proto.ScheduledOrder.$Properties): proto.ScheduledOrder;
         * }}
         */
        ScheduledOrder.create = function(properties) {
            return new ScheduledOrder(properties);
        };

        /**
         * Encodes the specified ScheduledOrder message. Does not implicitly {@link proto.ScheduledOrder.verify|verify} messages.
         * @function encode
         * @memberof proto.ScheduledOrder
         * @static
         * @param {proto.ScheduledOrder.$Properties} message ScheduledOrder message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduledOrder.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.expirySecond != null && $Object.hasOwnProperty.call(message, "expirySecond") && (typeof message.expirySecond === "object" ? message.expirySecond.low || message.expirySecond.high : message.expirySecond !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.expirySecond);
            if (message.orderNumber != null && $Object.hasOwnProperty.call(message, "orderNumber") && message.orderNumber !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.orderNumber);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ScheduledOrder message, length delimited. Does not implicitly {@link proto.ScheduledOrder.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ScheduledOrder
         * @static
         * @param {proto.ScheduledOrder.$Properties} message ScheduledOrder message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduledOrder.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ScheduledOrder message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ScheduledOrder
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ScheduledOrder & proto.ScheduledOrder.$Shape} ScheduledOrder
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduledOrder.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ScheduledOrder();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.expirySecond = value;
                        else
                            delete message.expirySecond;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.orderNumber = value;
                        else
                            delete message.orderNumber;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ScheduledOrder message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ScheduledOrder
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ScheduledOrder & proto.ScheduledOrder.$Shape} ScheduledOrder
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduledOrder.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScheduledOrder message.
         * @function verify
         * @memberof proto.ScheduledOrder
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScheduledOrder.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.expirySecond != null && $Object.hasOwnProperty.call(message, "expirySecond"))
                if (!$util.isInteger(message.expirySecond) && !(message.expirySecond && $util.isInteger(message.expirySecond.low) && $util.isInteger(message.expirySecond.high)))
                    return "expirySecond: integer|Long expected";
            if (message.orderNumber != null && $Object.hasOwnProperty.call(message, "orderNumber"))
                if (!$util.isInteger(message.orderNumber))
                    return "orderNumber: integer expected";
            return null;
        };

        /**
         * Creates a ScheduledOrder message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ScheduledOrder
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ScheduledOrder} ScheduledOrder
         */
        ScheduledOrder.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ScheduledOrder)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ScheduledOrder: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ScheduledOrder();
            if (object.expirySecond != null)
                if (typeof object.expirySecond === "object" ? object.expirySecond.low || object.expirySecond.high : $Number(object.expirySecond) !== 0)
                    if ($util.Long)
                        message.expirySecond = $util.Long.fromValue(object.expirySecond, true);
                    else if (typeof object.expirySecond === "string")
                        message.expirySecond = $parseInt(object.expirySecond, 10);
                    else if (typeof object.expirySecond === "number")
                        message.expirySecond = object.expirySecond;
                    else if (typeof object.expirySecond === "object")
                        message.expirySecond = new $util.LongBits(object.expirySecond.low >>> 0, object.expirySecond.high >>> 0).toNumber(true);
            if (object.orderNumber != null)
                if ($Number(object.orderNumber) !== 0)
                    message.orderNumber = object.orderNumber >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a ScheduledOrder message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ScheduledOrder
         * @static
         * @param {proto.ScheduledOrder} message ScheduledOrder
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScheduledOrder.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.expirySecond = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expirySecond = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.orderNumber = 0;
            }
            if (message.expirySecond != null && $Object.hasOwnProperty.call(message, "expirySecond"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expirySecond = typeof message.expirySecond === "number" ? $BigInt(message.expirySecond) : $util.Long.fromBits(message.expirySecond.low >>> 0, message.expirySecond.high >>> 0, true).toBigInt();
                else if (typeof message.expirySecond === "number")
                    object.expirySecond = options.longs === $String ? $String(message.expirySecond) : message.expirySecond;
                else
                    object.expirySecond = options.longs === $String ? $util.Long.prototype.toString.call(message.expirySecond) : options.longs === $Number ? new $util.LongBits(message.expirySecond.low >>> 0, message.expirySecond.high >>> 0).toNumber(true) : message.expirySecond;
            if (message.orderNumber != null && $Object.hasOwnProperty.call(message, "orderNumber"))
                object.orderNumber = message.orderNumber;
            return object;
        };

        /**
         * Converts this ScheduledOrder to JSON.
         * @function toJSON
         * @memberof proto.ScheduledOrder
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScheduledOrder.prototype.toJSON = function() {
            return ScheduledOrder.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ScheduledOrder
         * @function getTypeUrl
         * @memberof proto.ScheduledOrder
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ScheduledOrder.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ScheduledOrder";
        };

        return ScheduledOrder;
    })();

    proto.ThrottleUsageSnapshots = (function() {

        /**
         * Properties of a ThrottleUsageSnapshots.
         * @typedef {Object} proto.ThrottleUsageSnapshots.$Properties
         * @property {Array.<proto.ThrottleUsageSnapshot.$Properties>|null} [tpsThrottles] A list of snapshots for TPS throttles.
         * <p>
         * <blockquote>Question:<blockquote>What is the order?</blockquote></blockquote>
         * @property {proto.ThrottleUsageSnapshot.$Properties|null} [gasThrottle] A single snapshot for the gas throttle.
         * @property {proto.ThrottleUsageSnapshot.$Properties|null} [evmOpsDurationThrottle] A single snapshot for evm ops duration throttle.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ThrottleUsageSnapshots.
         * @memberof proto
         * @interface IThrottleUsageSnapshots
         * @augments proto.ThrottleUsageSnapshots.$Properties
         * @deprecated Use proto.ThrottleUsageSnapshots.$Properties instead.
         */

        /**
         * Shape of a ThrottleUsageSnapshots.
         * @typedef {proto.ThrottleUsageSnapshots.$Properties} proto.ThrottleUsageSnapshots.$Shape
         */

        /**
         * Constructs a new ThrottleUsageSnapshots.
         * @memberof proto
         * @classdesc All point-in-time snapshots of throttle usage for TPS, "gas" throttle
         * values for a given point in time and evm ops duration throttle.
         * 
         * > Question:
         * >> What point in time?  Should this store consensus timestamp here?
         * @constructor
         * @param {proto.ThrottleUsageSnapshots.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ThrottleUsageSnapshots = function (properties) {
            this.tpsThrottles = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of snapshots for TPS throttles.
         * <p>
         * <blockquote>Question:<blockquote>What is the order?</blockquote></blockquote>
         * @member {Array.<proto.ThrottleUsageSnapshot.$Properties>} tpsThrottles
         * @memberof proto.ThrottleUsageSnapshots
         * @instance
         */
        ThrottleUsageSnapshots.prototype.tpsThrottles = $util.emptyArray;

        /**
         * A single snapshot for the gas throttle.
         * @member {proto.ThrottleUsageSnapshot.$Properties|null|undefined} gasThrottle
         * @memberof proto.ThrottleUsageSnapshots
         * @instance
         */
        ThrottleUsageSnapshots.prototype.gasThrottle = null;

        /**
         * A single snapshot for evm ops duration throttle.
         * @member {proto.ThrottleUsageSnapshot.$Properties|null|undefined} evmOpsDurationThrottle
         * @memberof proto.ThrottleUsageSnapshots
         * @instance
         */
        ThrottleUsageSnapshots.prototype.evmOpsDurationThrottle = null;

        /**
         * Creates a new ThrottleUsageSnapshots instance using the specified properties.
         * @function create
         * @memberof proto.ThrottleUsageSnapshots
         * @static
         * @param {proto.ThrottleUsageSnapshots.$Properties=} [properties] Properties to set
         * @returns {proto.ThrottleUsageSnapshots} ThrottleUsageSnapshots instance
         * @type {{
         *   (properties: proto.ThrottleUsageSnapshots.$Shape): proto.ThrottleUsageSnapshots & proto.ThrottleUsageSnapshots.$Shape;
         *   (properties?: proto.ThrottleUsageSnapshots.$Properties): proto.ThrottleUsageSnapshots;
         * }}
         */
        ThrottleUsageSnapshots.create = function(properties) {
            return new ThrottleUsageSnapshots(properties);
        };

        /**
         * Encodes the specified ThrottleUsageSnapshots message. Does not implicitly {@link proto.ThrottleUsageSnapshots.verify|verify} messages.
         * @function encode
         * @memberof proto.ThrottleUsageSnapshots
         * @static
         * @param {proto.ThrottleUsageSnapshots.$Properties} message ThrottleUsageSnapshots message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottleUsageSnapshots.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tpsThrottles != null && message.tpsThrottles.length)
                for (let i = 0; i < message.tpsThrottles.length; ++i)
                    $root.proto.ThrottleUsageSnapshot.encode(message.tpsThrottles[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.gasThrottle != null && $Object.hasOwnProperty.call(message, "gasThrottle"))
                $root.proto.ThrottleUsageSnapshot.encode(message.gasThrottle, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.evmOpsDurationThrottle != null && $Object.hasOwnProperty.call(message, "evmOpsDurationThrottle"))
                $root.proto.ThrottleUsageSnapshot.encode(message.evmOpsDurationThrottle, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ThrottleUsageSnapshots message, length delimited. Does not implicitly {@link proto.ThrottleUsageSnapshots.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ThrottleUsageSnapshots
         * @static
         * @param {proto.ThrottleUsageSnapshots.$Properties} message ThrottleUsageSnapshots message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottleUsageSnapshots.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ThrottleUsageSnapshots message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ThrottleUsageSnapshots
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ThrottleUsageSnapshots & proto.ThrottleUsageSnapshots.$Shape} ThrottleUsageSnapshots
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottleUsageSnapshots.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ThrottleUsageSnapshots();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tpsThrottles && message.tpsThrottles.length))
                            message.tpsThrottles = [];
                        message.tpsThrottles.push($root.proto.ThrottleUsageSnapshot.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.gasThrottle = $root.proto.ThrottleUsageSnapshot.decode(reader, reader.uint32(), $undefined, _depth + 1, message.gasThrottle);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.evmOpsDurationThrottle = $root.proto.ThrottleUsageSnapshot.decode(reader, reader.uint32(), $undefined, _depth + 1, message.evmOpsDurationThrottle);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ThrottleUsageSnapshots message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ThrottleUsageSnapshots
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ThrottleUsageSnapshots & proto.ThrottleUsageSnapshots.$Shape} ThrottleUsageSnapshots
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottleUsageSnapshots.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ThrottleUsageSnapshots message.
         * @function verify
         * @memberof proto.ThrottleUsageSnapshots
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ThrottleUsageSnapshots.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tpsThrottles != null && $Object.hasOwnProperty.call(message, "tpsThrottles")) {
                if (!$Array.isArray(message.tpsThrottles))
                    return "tpsThrottles: array expected";
                for (let i = 0; i < message.tpsThrottles.length; ++i) {
                    let error = $root.proto.ThrottleUsageSnapshot.verify(message.tpsThrottles[i], _depth + 1);
                    if (error)
                        return "tpsThrottles." + error;
                }
            }
            if (message.gasThrottle != null && $Object.hasOwnProperty.call(message, "gasThrottle")) {
                let error = $root.proto.ThrottleUsageSnapshot.verify(message.gasThrottle, _depth + 1);
                if (error)
                    return "gasThrottle." + error;
            }
            if (message.evmOpsDurationThrottle != null && $Object.hasOwnProperty.call(message, "evmOpsDurationThrottle")) {
                let error = $root.proto.ThrottleUsageSnapshot.verify(message.evmOpsDurationThrottle, _depth + 1);
                if (error)
                    return "evmOpsDurationThrottle." + error;
            }
            return null;
        };

        /**
         * Creates a ThrottleUsageSnapshots message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ThrottleUsageSnapshots
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ThrottleUsageSnapshots} ThrottleUsageSnapshots
         */
        ThrottleUsageSnapshots.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ThrottleUsageSnapshots)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ThrottleUsageSnapshots: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ThrottleUsageSnapshots();
            if (object.tpsThrottles) {
                if (!$Array.isArray(object.tpsThrottles))
                    throw $TypeError(".proto.ThrottleUsageSnapshots.tpsThrottles: array expected");
                message.tpsThrottles = $Array(object.tpsThrottles.length);
                for (let i = 0; i < object.tpsThrottles.length; ++i) {
                    if (!$util.isObject(object.tpsThrottles[i]))
                        throw $TypeError(".proto.ThrottleUsageSnapshots.tpsThrottles: object expected");
                    message.tpsThrottles[i] = $root.proto.ThrottleUsageSnapshot.fromObject(object.tpsThrottles[i], _depth + 1);
                }
            }
            if (object.gasThrottle != null) {
                if (!$util.isObject(object.gasThrottle))
                    throw $TypeError(".proto.ThrottleUsageSnapshots.gasThrottle: object expected");
                message.gasThrottle = $root.proto.ThrottleUsageSnapshot.fromObject(object.gasThrottle, _depth + 1);
            }
            if (object.evmOpsDurationThrottle != null) {
                if (!$util.isObject(object.evmOpsDurationThrottle))
                    throw $TypeError(".proto.ThrottleUsageSnapshots.evmOpsDurationThrottle: object expected");
                message.evmOpsDurationThrottle = $root.proto.ThrottleUsageSnapshot.fromObject(object.evmOpsDurationThrottle, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ThrottleUsageSnapshots message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ThrottleUsageSnapshots
         * @static
         * @param {proto.ThrottleUsageSnapshots} message ThrottleUsageSnapshots
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThrottleUsageSnapshots.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tpsThrottles = [];
            if (options.defaults) {
                object.gasThrottle = null;
                object.evmOpsDurationThrottle = null;
            }
            if (message.tpsThrottles && message.tpsThrottles.length) {
                object.tpsThrottles = $Array(message.tpsThrottles.length);
                for (let j = 0; j < message.tpsThrottles.length; ++j)
                    object.tpsThrottles[j] = $root.proto.ThrottleUsageSnapshot.toObject(message.tpsThrottles[j], options, _depth + 1);
            }
            if (message.gasThrottle != null && $Object.hasOwnProperty.call(message, "gasThrottle"))
                object.gasThrottle = $root.proto.ThrottleUsageSnapshot.toObject(message.gasThrottle, options, _depth + 1);
            if (message.evmOpsDurationThrottle != null && $Object.hasOwnProperty.call(message, "evmOpsDurationThrottle"))
                object.evmOpsDurationThrottle = $root.proto.ThrottleUsageSnapshot.toObject(message.evmOpsDurationThrottle, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ThrottleUsageSnapshots to JSON.
         * @function toJSON
         * @memberof proto.ThrottleUsageSnapshots
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThrottleUsageSnapshots.prototype.toJSON = function() {
            return ThrottleUsageSnapshots.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ThrottleUsageSnapshots
         * @function getTypeUrl
         * @memberof proto.ThrottleUsageSnapshots
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ThrottleUsageSnapshots.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ThrottleUsageSnapshots";
        };

        return ThrottleUsageSnapshots;
    })();

    proto.ThrottleUsageSnapshot = (function() {

        /**
         * Properties of a ThrottleUsageSnapshot.
         * @typedef {Object} proto.ThrottleUsageSnapshot.$Properties
         * @property {number|Long|null} [used] Used throttle capacity.
         * @property {proto.Timestamp.$Properties|null} [lastDecisionTime] The time at which the this snapshot of capacity was calculated.<br/>
         * Stored as an offset from the `epoch`.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ThrottleUsageSnapshot.
         * @memberof proto
         * @interface IThrottleUsageSnapshot
         * @augments proto.ThrottleUsageSnapshot.$Properties
         * @deprecated Use proto.ThrottleUsageSnapshot.$Properties instead.
         */

        /**
         * Shape of a ThrottleUsageSnapshot.
         * @typedef {proto.ThrottleUsageSnapshot.$Properties} proto.ThrottleUsageSnapshot.$Shape
         */

        /**
         * Constructs a new ThrottleUsageSnapshot.
         * @memberof proto
         * @classdesc A single snapshot of the used throttle capacity for a throttle and point in
         * time.
         * 
         * > Question:
         * >> What throttle does this apply to? How is that determined?
         * @constructor
         * @param {proto.ThrottleUsageSnapshot.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ThrottleUsageSnapshot = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Used throttle capacity.
         * @member {number|Long} used
         * @memberof proto.ThrottleUsageSnapshot
         * @instance
         */
        ThrottleUsageSnapshot.prototype.used = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The time at which the this snapshot of capacity was calculated.<br/>
         * Stored as an offset from the `epoch`.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.
         * @member {proto.Timestamp.$Properties|null|undefined} lastDecisionTime
         * @memberof proto.ThrottleUsageSnapshot
         * @instance
         */
        ThrottleUsageSnapshot.prototype.lastDecisionTime = null;

        /**
         * Creates a new ThrottleUsageSnapshot instance using the specified properties.
         * @function create
         * @memberof proto.ThrottleUsageSnapshot
         * @static
         * @param {proto.ThrottleUsageSnapshot.$Properties=} [properties] Properties to set
         * @returns {proto.ThrottleUsageSnapshot} ThrottleUsageSnapshot instance
         * @type {{
         *   (properties: proto.ThrottleUsageSnapshot.$Shape): proto.ThrottleUsageSnapshot & proto.ThrottleUsageSnapshot.$Shape;
         *   (properties?: proto.ThrottleUsageSnapshot.$Properties): proto.ThrottleUsageSnapshot;
         * }}
         */
        ThrottleUsageSnapshot.create = function(properties) {
            return new ThrottleUsageSnapshot(properties);
        };

        /**
         * Encodes the specified ThrottleUsageSnapshot message. Does not implicitly {@link proto.ThrottleUsageSnapshot.verify|verify} messages.
         * @function encode
         * @memberof proto.ThrottleUsageSnapshot
         * @static
         * @param {proto.ThrottleUsageSnapshot.$Properties} message ThrottleUsageSnapshot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottleUsageSnapshot.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.used != null && $Object.hasOwnProperty.call(message, "used") && (typeof message.used === "object" ? message.used.low || message.used.high : message.used !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.used);
            if (message.lastDecisionTime != null && $Object.hasOwnProperty.call(message, "lastDecisionTime"))
                $root.proto.Timestamp.encode(message.lastDecisionTime, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ThrottleUsageSnapshot message, length delimited. Does not implicitly {@link proto.ThrottleUsageSnapshot.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ThrottleUsageSnapshot
         * @static
         * @param {proto.ThrottleUsageSnapshot.$Properties} message ThrottleUsageSnapshot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottleUsageSnapshot.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ThrottleUsageSnapshot message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ThrottleUsageSnapshot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ThrottleUsageSnapshot & proto.ThrottleUsageSnapshot.$Shape} ThrottleUsageSnapshot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottleUsageSnapshot.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ThrottleUsageSnapshot();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.used = value;
                        else
                            delete message.used;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.lastDecisionTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.lastDecisionTime);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ThrottleUsageSnapshot message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ThrottleUsageSnapshot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ThrottleUsageSnapshot & proto.ThrottleUsageSnapshot.$Shape} ThrottleUsageSnapshot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottleUsageSnapshot.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ThrottleUsageSnapshot message.
         * @function verify
         * @memberof proto.ThrottleUsageSnapshot
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ThrottleUsageSnapshot.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.used != null && $Object.hasOwnProperty.call(message, "used"))
                if (!$util.isInteger(message.used) && !(message.used && $util.isInteger(message.used.low) && $util.isInteger(message.used.high)))
                    return "used: integer|Long expected";
            if (message.lastDecisionTime != null && $Object.hasOwnProperty.call(message, "lastDecisionTime")) {
                let error = $root.proto.Timestamp.verify(message.lastDecisionTime, _depth + 1);
                if (error)
                    return "lastDecisionTime." + error;
            }
            return null;
        };

        /**
         * Creates a ThrottleUsageSnapshot message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ThrottleUsageSnapshot
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ThrottleUsageSnapshot} ThrottleUsageSnapshot
         */
        ThrottleUsageSnapshot.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ThrottleUsageSnapshot)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ThrottleUsageSnapshot: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ThrottleUsageSnapshot();
            if (object.used != null)
                if (typeof object.used === "object" ? object.used.low || object.used.high : $Number(object.used) !== 0)
                    if ($util.Long)
                        message.used = $util.Long.fromValue(object.used, false);
                    else if (typeof object.used === "string")
                        message.used = $parseInt(object.used, 10);
                    else if (typeof object.used === "number")
                        message.used = object.used;
                    else if (typeof object.used === "object")
                        message.used = new $util.LongBits(object.used.low >>> 0, object.used.high >>> 0).toNumber();
            if (object.lastDecisionTime != null) {
                if (!$util.isObject(object.lastDecisionTime))
                    throw $TypeError(".proto.ThrottleUsageSnapshot.lastDecisionTime: object expected");
                message.lastDecisionTime = $root.proto.Timestamp.fromObject(object.lastDecisionTime, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ThrottleUsageSnapshot message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ThrottleUsageSnapshot
         * @static
         * @param {proto.ThrottleUsageSnapshot} message ThrottleUsageSnapshot
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThrottleUsageSnapshot.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.used = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.used = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.lastDecisionTime = null;
            }
            if (message.used != null && $Object.hasOwnProperty.call(message, "used"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.used = typeof message.used === "number" ? $BigInt(message.used) : $util.Long.fromBits(message.used.low >>> 0, message.used.high >>> 0, false).toBigInt();
                else if (typeof message.used === "number")
                    object.used = options.longs === $String ? $String(message.used) : message.used;
                else
                    object.used = options.longs === $String ? $util.Long.prototype.toString.call(message.used) : options.longs === $Number ? new $util.LongBits(message.used.low >>> 0, message.used.high >>> 0).toNumber() : message.used;
            if (message.lastDecisionTime != null && $Object.hasOwnProperty.call(message, "lastDecisionTime"))
                object.lastDecisionTime = $root.proto.Timestamp.toObject(message.lastDecisionTime, options, _depth + 1);
            return object;
        };

        /**
         * Converts this ThrottleUsageSnapshot to JSON.
         * @function toJSON
         * @memberof proto.ThrottleUsageSnapshot
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThrottleUsageSnapshot.prototype.toJSON = function() {
            return ThrottleUsageSnapshot.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ThrottleUsageSnapshot
         * @function getTypeUrl
         * @memberof proto.ThrottleUsageSnapshot
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ThrottleUsageSnapshot.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ThrottleUsageSnapshot";
        };

        return ThrottleUsageSnapshot;
    })();

    proto.AccountPendingAirdrop = (function() {

        /**
         * Properties of an AccountPendingAirdrop.
         * @typedef {Object} proto.AccountPendingAirdrop.$Properties
         * @property {proto.PendingAirdropValue.$Properties|null} [pendingAirdropValue] An amount of fungible tokens to be sent for this pending airdrop.
         * <p>
         * This field SHALL NOT be set for non-fungible/unique tokens.
         * @property {proto.PendingAirdropId.$Properties|null} [previousAirdrop] A pending airdrop identifier.
         * <p>
         * This field SHALL identify the specific pending airdrop that
         * precedes this position within the doubly linked list of pending
         * airdrops "owed" by the sending account associated with this
         * account airdrop "list".<br/>
         * This SHALL match `pending_airdrop_id` if this is the only entry
         * in the "list".
         * @property {proto.PendingAirdropId.$Properties|null} [nextAirdrop] A pending airdrop identifier.<br/>
         * <p>
         * This field SHALL identify the specific pending airdrop that
         * follows this position within the doubly linked list of pending
         * airdrops "owed" by the sending account associated with this
         * account airdrop "list".<br/>
         * This SHALL match `pending_airdrop_id` if this is the only entry
         * in the "list".
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AccountPendingAirdrop.
         * @memberof proto
         * @interface IAccountPendingAirdrop
         * @augments proto.AccountPendingAirdrop.$Properties
         * @deprecated Use proto.AccountPendingAirdrop.$Properties instead.
         */

        /**
         * Shape of an AccountPendingAirdrop.
         * @typedef {{
         *   pendingAirdropValue?: proto.PendingAirdropValue.$Shape|null;
         *   previousAirdrop?: proto.PendingAirdropId.$Shape|null;
         *   nextAirdrop?: proto.PendingAirdropId.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.AccountPendingAirdrop.$Shape
         */

        /**
         * Constructs a new AccountPendingAirdrop.
         * @memberof proto
         * @classdesc A node within a doubly linked list of pending airdrop references.<br/>
         * This internal state message forms the entries in a doubly-linked list
         * of references to pending airdrop entries that are "owed" by a particular
         * account as "sender".
         * 
         * Each entry in this list MUST refer to an existing pending airdrop.<br/>
         * The pending airdrop MUST NOT be claimed.<br/>
         * The pending airdrop MUST NOT be canceled.<br/>
         * The pending airdrop `sender` account's `head_pending_airdrop_id` field
         * MUST match the `pending_airdrop_id` field in this message.
         * @constructor
         * @param {proto.AccountPendingAirdrop.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AccountPendingAirdrop = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An amount of fungible tokens to be sent for this pending airdrop.
         * <p>
         * This field SHALL NOT be set for non-fungible/unique tokens.
         * @member {proto.PendingAirdropValue.$Properties|null|undefined} pendingAirdropValue
         * @memberof proto.AccountPendingAirdrop
         * @instance
         */
        AccountPendingAirdrop.prototype.pendingAirdropValue = null;

        /**
         * A pending airdrop identifier.
         * <p>
         * This field SHALL identify the specific pending airdrop that
         * precedes this position within the doubly linked list of pending
         * airdrops "owed" by the sending account associated with this
         * account airdrop "list".<br/>
         * This SHALL match `pending_airdrop_id` if this is the only entry
         * in the "list".
         * @member {proto.PendingAirdropId.$Properties|null|undefined} previousAirdrop
         * @memberof proto.AccountPendingAirdrop
         * @instance
         */
        AccountPendingAirdrop.prototype.previousAirdrop = null;

        /**
         * A pending airdrop identifier.<br/>
         * <p>
         * This field SHALL identify the specific pending airdrop that
         * follows this position within the doubly linked list of pending
         * airdrops "owed" by the sending account associated with this
         * account airdrop "list".<br/>
         * This SHALL match `pending_airdrop_id` if this is the only entry
         * in the "list".
         * @member {proto.PendingAirdropId.$Properties|null|undefined} nextAirdrop
         * @memberof proto.AccountPendingAirdrop
         * @instance
         */
        AccountPendingAirdrop.prototype.nextAirdrop = null;

        /**
         * Creates a new AccountPendingAirdrop instance using the specified properties.
         * @function create
         * @memberof proto.AccountPendingAirdrop
         * @static
         * @param {proto.AccountPendingAirdrop.$Properties=} [properties] Properties to set
         * @returns {proto.AccountPendingAirdrop} AccountPendingAirdrop instance
         * @type {{
         *   (properties: proto.AccountPendingAirdrop.$Shape): proto.AccountPendingAirdrop & proto.AccountPendingAirdrop.$Shape;
         *   (properties?: proto.AccountPendingAirdrop.$Properties): proto.AccountPendingAirdrop;
         * }}
         */
        AccountPendingAirdrop.create = function(properties) {
            return new AccountPendingAirdrop(properties);
        };

        /**
         * Encodes the specified AccountPendingAirdrop message. Does not implicitly {@link proto.AccountPendingAirdrop.verify|verify} messages.
         * @function encode
         * @memberof proto.AccountPendingAirdrop
         * @static
         * @param {proto.AccountPendingAirdrop.$Properties} message AccountPendingAirdrop message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountPendingAirdrop.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.pendingAirdropValue != null && $Object.hasOwnProperty.call(message, "pendingAirdropValue"))
                $root.proto.PendingAirdropValue.encode(message.pendingAirdropValue, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.previousAirdrop != null && $Object.hasOwnProperty.call(message, "previousAirdrop"))
                $root.proto.PendingAirdropId.encode(message.previousAirdrop, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.nextAirdrop != null && $Object.hasOwnProperty.call(message, "nextAirdrop"))
                $root.proto.PendingAirdropId.encode(message.nextAirdrop, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AccountPendingAirdrop message, length delimited. Does not implicitly {@link proto.AccountPendingAirdrop.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AccountPendingAirdrop
         * @static
         * @param {proto.AccountPendingAirdrop.$Properties} message AccountPendingAirdrop message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountPendingAirdrop.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AccountPendingAirdrop message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AccountPendingAirdrop
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AccountPendingAirdrop & proto.AccountPendingAirdrop.$Shape} AccountPendingAirdrop
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountPendingAirdrop.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.AccountPendingAirdrop();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.pendingAirdropValue = $root.proto.PendingAirdropValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pendingAirdropValue);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.previousAirdrop = $root.proto.PendingAirdropId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.previousAirdrop);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.nextAirdrop = $root.proto.PendingAirdropId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nextAirdrop);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AccountPendingAirdrop message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AccountPendingAirdrop
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AccountPendingAirdrop & proto.AccountPendingAirdrop.$Shape} AccountPendingAirdrop
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountPendingAirdrop.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AccountPendingAirdrop message.
         * @function verify
         * @memberof proto.AccountPendingAirdrop
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AccountPendingAirdrop.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pendingAirdropValue != null && $Object.hasOwnProperty.call(message, "pendingAirdropValue")) {
                let error = $root.proto.PendingAirdropValue.verify(message.pendingAirdropValue, _depth + 1);
                if (error)
                    return "pendingAirdropValue." + error;
            }
            if (message.previousAirdrop != null && $Object.hasOwnProperty.call(message, "previousAirdrop")) {
                let error = $root.proto.PendingAirdropId.verify(message.previousAirdrop, _depth + 1);
                if (error)
                    return "previousAirdrop." + error;
            }
            if (message.nextAirdrop != null && $Object.hasOwnProperty.call(message, "nextAirdrop")) {
                let error = $root.proto.PendingAirdropId.verify(message.nextAirdrop, _depth + 1);
                if (error)
                    return "nextAirdrop." + error;
            }
            return null;
        };

        /**
         * Creates an AccountPendingAirdrop message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AccountPendingAirdrop
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AccountPendingAirdrop} AccountPendingAirdrop
         */
        AccountPendingAirdrop.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.AccountPendingAirdrop)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.AccountPendingAirdrop: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.AccountPendingAirdrop();
            if (object.pendingAirdropValue != null) {
                if (!$util.isObject(object.pendingAirdropValue))
                    throw $TypeError(".proto.AccountPendingAirdrop.pendingAirdropValue: object expected");
                message.pendingAirdropValue = $root.proto.PendingAirdropValue.fromObject(object.pendingAirdropValue, _depth + 1);
            }
            if (object.previousAirdrop != null) {
                if (!$util.isObject(object.previousAirdrop))
                    throw $TypeError(".proto.AccountPendingAirdrop.previousAirdrop: object expected");
                message.previousAirdrop = $root.proto.PendingAirdropId.fromObject(object.previousAirdrop, _depth + 1);
            }
            if (object.nextAirdrop != null) {
                if (!$util.isObject(object.nextAirdrop))
                    throw $TypeError(".proto.AccountPendingAirdrop.nextAirdrop: object expected");
                message.nextAirdrop = $root.proto.PendingAirdropId.fromObject(object.nextAirdrop, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an AccountPendingAirdrop message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AccountPendingAirdrop
         * @static
         * @param {proto.AccountPendingAirdrop} message AccountPendingAirdrop
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccountPendingAirdrop.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.pendingAirdropValue = null;
                object.previousAirdrop = null;
                object.nextAirdrop = null;
            }
            if (message.pendingAirdropValue != null && $Object.hasOwnProperty.call(message, "pendingAirdropValue"))
                object.pendingAirdropValue = $root.proto.PendingAirdropValue.toObject(message.pendingAirdropValue, options, _depth + 1);
            if (message.previousAirdrop != null && $Object.hasOwnProperty.call(message, "previousAirdrop"))
                object.previousAirdrop = $root.proto.PendingAirdropId.toObject(message.previousAirdrop, options, _depth + 1);
            if (message.nextAirdrop != null && $Object.hasOwnProperty.call(message, "nextAirdrop"))
                object.nextAirdrop = $root.proto.PendingAirdropId.toObject(message.nextAirdrop, options, _depth + 1);
            return object;
        };

        /**
         * Converts this AccountPendingAirdrop to JSON.
         * @function toJSON
         * @memberof proto.AccountPendingAirdrop
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccountPendingAirdrop.prototype.toJSON = function() {
            return AccountPendingAirdrop.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AccountPendingAirdrop
         * @function getTypeUrl
         * @memberof proto.AccountPendingAirdrop
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AccountPendingAirdrop.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AccountPendingAirdrop";
        };

        return AccountPendingAirdrop;
    })();

    proto.Account = (function() {

        /**
         * Properties of an Account.
         * @typedef {Object} proto.Account.$Properties
         * @property {proto.AccountID.$Properties|null} [accountId] The unique ID of this account.
         * <p>
         * An account ID, when assigned to this field, SHALL be of
         * the form `shard.realm.number`.<br/>
         * Transactions MAY reference the account by alias, but the account itself
         * MUST always have a purely numeric identifier. This numeric ID is the
         * value used to reference the account in query responses, transaction
         * receipts, transaction records, and the block stream.
         * @property {Uint8Array|null} [alias] An account EVM alias.
         * <p>
         * This is a value used in some contexts to reference an account when the
         * numeric account identifier is not available.<br/>
         * This field, when set to a non-default value, is immutable and
         * SHALL NOT be changed.
         * @property {proto.Key.$Properties|null} [key] The key to be used to sign transactions from this account, if any.
         * <p>
         * This key SHALL NOT be set for hollow accounts until the account
         * is finalized.<br/>
         * This key SHALL be set on all other accounts, except for certain
         * immutable accounts (0.0.800 and 0.0.801) necessary for network function
         * and otherwise secured by the governing council.
         * @property {number|Long|null} [expirationSecond] The current expiration time of this account, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch
         * with 0 at `1970-01-01T00:00:00.000Z`.<br/>
         * This account SHALL be due standard renewal fees when the network
         * consensus time exceeds this time.<br/>
         * If rent and expiration are enabled for the network, and automatic
         * renewal is enabled for this account, renewal fees SHALL be charged
         * after this time, and, if charged, the expiration time SHALL be
         * extended for another renewal period.<br/>
         * This account MAY be expired and removed from state at any point
         * after this time if not renewed.<br/>
         * An account holder MAY extend this time by submitting an account
         * update transaction to modify expiration time, subject to the current
         * maximum expiration time for the network.
         * @property {number|Long|null} [tinybarBalance] The HBAR balance of this account, in tinybar (10<sup>-8</sup> HBAR).
         * <p>
         * This value is a signed integer for efficiency, but MUST always
         * be a whole number.
         * @property {string|null} [memo] A short description of this account.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {boolean|null} [deleted] A boolean indicating that this account is deleted.
         * @property {number|Long|null} [stakedToMe] The amount of HBAR staked to this account by others.
         * @property {number|Long|null} [stakePeriodStart] If this account stakes to another account, this value SHALL be set to
         * the time when the current period for staking and reward
         * calculations began.
         * @property {proto.AccountID.$Properties|null} [stakedAccountId] An identifier for the account to which this account is
         * staking its balances as a proxy.
         * <p>
         * If this account is not currently staking its balances, then this
         * field, if set, SHALL be the sentinel value of `0.0.0`.
         * @property {number|Long|null} [stakedNodeId] An identifier for the node this account is staked to.
         * <p>
         * If this account is not currently staking its balances, then this
         * field, if set, SHALL be the sentinel value of `-1`.
         * Wallet software SHOULD surface staking issues to users and provide a
         * simple mechanism to update staking to a new node ID in the event the
         * prior staked node ID ceases to be valid.
         * <p>
         * <blockquote>Note: node IDs do fluctuate as node operators change.
         * The Account owner MUST submit a new transaction to change this value
         * if the current node ID changes or ceases to operate as a node. An
         * account with an invalid `staked_node_id` SHALL NOT participate in
         * staking until the `staked_node_id` is updated to a valid node ID.
         * </blockquote>
         * @property {boolean|null} [declineReward] A boolean indicating that this account has chosen to decline rewards for
         * staking its balances.
         * <p>
         * This account MAY still stake its balances, but SHALL NOT receive reward
         * payments for doing so.
         * @property {boolean|null} [receiverSigRequired] A boolean indicating that the account requires a receiver signature for
         * inbound token transfer transactions.
         * <p>
         * If this value is `true` then a transaction to transfer tokens to this
         * account SHALL NOT succeed unless this account has signed the transfer
         * transaction.
         * @property {proto.TokenID.$Properties|null} [headTokenId] A token ID at the head of the linked list for this account from the
         * token relations map.<br/>
         * The token relations are connected by including the "next" and "previous"
         * TokenID in each TokenRelation message. The "head" item in that list is
         * found by looking up the TokenRelation with this Account's account_id and
         * this head_token_id. Each subsequent item in the list is found via
         * similar lookup with both an AccountID and a TokenID.
         * @property {proto.NftID.$Properties|null} [headNftId] A NftID at the head of the linked list for this account from
         * the unique tokens map.<br/>
         * The unique token relations are connected by including the "next" and
         * "previous" NftID in each Nft message. The "head" item in that list is
         * found by looking up the Nft with ID matching this head_nft_id. Each
         * subsequent item in the list is found via similar lookup with the next
         * or previous NftID.
         * @property {number|Long|null} [headNftSerialNumber] A serial number in the NftID at the head of the linked list for this
         * account from unique tokens map.
         * <p>
         * This MUST match the `serial_number` field of `head_nft_id`.
         * @property {number|Long|null} [numberOwnedNfts] A number of non-fungible tokens (NTFs) owned by the account.
         * @property {number|null} [maxAutoAssociations] A maximum for the number of tokens that can be automatically
         * associated with this account.
         * <p>
         * If this is less than or equal to `used_auto_associations` (or 0), then
         * this account MUST manually associate with a token before transacting in
         * that token.<br/>
         * This value may also be `-1` to indicate no limit.<br/>
         * This value MUST NOT be less than `-1`.
         * @property {number|null} [usedAutoAssociations] A count of used auto-association slots.
         * <p>
         * If this is greater than, or equal to, the current value of
         * `max_auto_associations`, then this account MUST manually associate with
         * a new token before transacting in that token.
         * @property {number|null} [numberAssociations] A count of tokens associated with this account.
         * <p>
         * This value determines a portion of the renewal fee for this account.
         * @property {boolean|null} [smartContract] A boolean indicating that this account is owned by a smart contract.
         * @property {number|null} [numberPositiveBalances] A count of tokens with a positive balance associated with this account.
         * <p>
         * If the account has a positive balance in any token,
         * it SHALL NOT be deleted.
         * @property {number|Long|null} [ethereumNonce] A nonce of this account for Ethereum interoperability.
         * @property {number|Long|null} [stakeAtStartOfLastRewardedPeriod] An amount of HBAR staked by this account at the start of
         * the last reward period.
         * @property {proto.AccountID.$Properties|null} [autoRenewAccountId] An account identifier for automatic renewal.<br/>
         * This is the identifier of another account, in the same shard and
         * realm as this account, that has signed a transaction allowing the
         * network to use its balance, if needed, to automatically extend this
         * account's expiration time during automatic renewal processing.
         * <p>
         * If this is set, and this account lack sufficient HBAR balance to pay
         * renewal fees when due, then the network SHALL deduct the necessary fees
         * from the designated auto renew account, if that account has sufficient
         * balance.
         * @property {number|Long|null} [autoRenewSeconds] A count of the number of seconds to extend this account's expiration.
         * <p>
         * The network SHALL extend the account's expiration by this number of
         * seconds, if funds are available, upon automatic renewal.<br/>
         * This SHALL NOT apply if the account is already deleted
         * upon expiration.<br/>
         * If this is not provided in an allowed range on account creation, the
         * transaction SHALL fail with INVALID_AUTO_RENEWAL_PERIOD. The default
         * values for the minimum period and maximum period are currently 30 days
         * and 90 days, respectively.
         * @property {number|null} [contractKvPairsNumber] A count of smart contract key-value pairs.<br/>
         * If this account is a smart-contract, this is the number of key-value
         * pairs stored on the contract.
         * <p>
         * If this account is not a smart contract, this field
         * SHALL NOT be used.<br/>
         * This value SHALL determine a portion of the storage rental
         * fees for the contract.
         * @property {Array.<proto.AccountCryptoAllowance.$Properties>|null} [cryptoAllowances] A list of crypto (HBAR) allowances approved by this account.
         * <p>
         * If this is not empty, each allowance SHALL permit a specified "spender"
         * account to spend this account's HBAR balance, up to a designated
         * limit.<br/>
         * This field SHALL permit spending only HBAR balance, not other tokens the
         * account may hold. Allowances for other tokens SHALL be listed in the
         * `token_allowances` field or the `approve_for_all_nft_allowances` field.
         * @property {Array.<proto.AccountApprovalForAllAllowance.$Properties>|null} [approveForAllNftAllowances] A list of non-fungible token (NFT) allowances approved by this account.
         * <p>
         * If this is not empty, each allowance permits a specified "spender"
         * account to transfer _all_ of this account's non-fungible tokens from a
         * particular collection.<br/>
         * Allowances for a specific serial number MUST be directly associated with
         * that specific non-fungible token, rather than the holding account.
         * @property {Array.<proto.AccountFungibleTokenAllowance.$Properties>|null} [tokenAllowances] A list of fungible token allowances approved by this account.
         * <p>
         * If this is not empty, each allowance permits a specified "spender" to
         * spend this account's fungible tokens, of the designated type, up to a
         * designated limit.
         * @property {number|null} [numberTreasuryTitles] A count of tokens for which this account is the treasury account.
         * <p>
         * Each native token is initially created with all tokens held by its
         * treasury, and the owner of that account (which may be a smart contract)
         * determines how those tokens are distributed.
         * @property {boolean|null} [expiredAndPendingRemoval] A flag indicating that the account is expired and pending removal.
         * <p>
         * When the network checks for entity expiration, it SHALL set this flag if
         * the account expiration time has past and the account has no HBAR
         * sufficient to pay current renewal fees.<br/>
         * If the account has an auto-renew account set with an HBAR balance that
         * could pay for an auto-renewal, then this flag SHALL NOT be set. This
         * ensures the account is not encumbered during the time between expiration
         * and when the auto-renewal processing renews the account.
         * @property {Uint8Array|null} [firstContractStorageKey] A contract storage key.<br/>
         * This is the first key in the doubly-linked list of this
         * contract's storage mappings.
         * <p>
         * This value SHALL be empty if the account is not a contract or the
         * contract has no storage mappings.
         * @property {proto.PendingAirdropId.$Properties|null} [headPendingAirdropId] A pending airdrop ID.<br/>
         * This is the head of the linked list for this account from the
         * account airdrops map.<br/>
         * <p>
         * The account airdrops SHALL be connected by including the "next" and
         * "previous" `PendingAirdropID` in each `AccountAirdrop` message.<br/>
         * This value SHALL NOT be empty if this account is "sender" for any
         * pending airdrop, and SHALL be empty otherwise.
         * @property {number|Long|null} [numberPendingAirdrops] A number of pending airdrops.
         * <p>
         * This count SHALL be used to calculate rent _without_ walking the linked
         * list of pending airdrops associated to this account via the
         * `head_pending_airdrop_id` field.<br/>
         * This value MUST be updated for every airdrop, clam, or cancel transaction
         * that designates this account as a receiver.<br/>
         * This number MUST always match the count of entries in the "list"
         * identified by `head_pending_airdrop_id`.
         * @property {number|Long|null} [numberHooksInUse] The number of hooks currently in use on this account.
         * @property {number|Long|null} [firstHookId] If the account has more than zero hooks in use, the id of the first hook in its
         * doubly-linked list of hooks.
         * @property {number|Long|null} [numberEvmHookStorageSlots] The number of storage slots in use by this account's EVM hooks.
         * @property {Uint8Array|null} [delegationAddress] The delegated contract address set for this account.
         * <p>
         * If this field is set on the account, anytime a call is made to the account's address
         * (either an EVM call within HSCS or a child call dispatched by CryptoTransfer)
         * the EVM code of the contract referenced by `delegation_address` will be
         * executed in the context of the account.
         * @property {"stakedAccountId"|"stakedNodeId"} [stakedId] ID of the account or node to which this account is staking, if any.
         * <p>
         * if not set this field MAY be interpreted as staked_account_id
         * with value `0.0.0`.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an Account.
         * @memberof proto
         * @interface IAccount
         * @augments proto.Account.$Properties
         * @deprecated Use proto.Account.$Properties instead.
         */

        /**
         * Narrowed shape of an Account.
         * @typedef {{
         *   accountId?: proto.AccountID.$Shape|null;
         *   alias?: Uint8Array|null;
         *   key?: proto.Key.$Shape|null;
         *   expirationSecond?: number|Long|null;
         *   tinybarBalance?: number|Long|null;
         *   memo?: string|null;
         *   deleted?: boolean|null;
         *   stakedToMe?: number|Long|null;
         *   stakePeriodStart?: number|Long|null;
         *   stakedAccountId?: proto.AccountID.$Shape|null;
         *   stakedNodeId?: number|Long|null;
         *   declineReward?: boolean|null;
         *   receiverSigRequired?: boolean|null;
         *   headTokenId?: proto.TokenID.$Shape|null;
         *   headNftId?: proto.NftID.$Shape|null;
         *   headNftSerialNumber?: number|Long|null;
         *   numberOwnedNfts?: number|Long|null;
         *   maxAutoAssociations?: number|null;
         *   usedAutoAssociations?: number|null;
         *   numberAssociations?: number|null;
         *   smartContract?: boolean|null;
         *   numberPositiveBalances?: number|null;
         *   ethereumNonce?: number|Long|null;
         *   stakeAtStartOfLastRewardedPeriod?: number|Long|null;
         *   autoRenewAccountId?: proto.AccountID.$Shape|null;
         *   autoRenewSeconds?: number|Long|null;
         *   contractKvPairsNumber?: number|null;
         *   cryptoAllowances?: Array.<proto.AccountCryptoAllowance.$Shape>|null;
         *   approveForAllNftAllowances?: Array.<proto.AccountApprovalForAllAllowance.$Shape>|null;
         *   tokenAllowances?: Array.<proto.AccountFungibleTokenAllowance.$Shape>|null;
         *   numberTreasuryTitles?: number|null;
         *   expiredAndPendingRemoval?: boolean|null;
         *   firstContractStorageKey?: Uint8Array|null;
         *   headPendingAirdropId?: proto.PendingAirdropId.$Shape|null;
         *   numberPendingAirdrops?: number|Long|null;
         *   numberHooksInUse?: number|Long|null;
         *   firstHookId?: number|Long|null;
         *   numberEvmHookStorageSlots?: number|Long|null;
         *   delegationAddress?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ stakedId?: undefined; stakedAccountId?: null; stakedNodeId?: null }|{ stakedId?: "stakedAccountId"; stakedAccountId: proto.AccountID.$Shape; stakedNodeId?: null }|{ stakedId?: "stakedNodeId"; stakedAccountId?: null; stakedNodeId: number|Long })
         * )} proto.Account.$Shape
         */

        /**
         * Constructs a new Account.
         * @memberof proto
         * @classdesc A single Account in the Hedera distributed ledger.
         * 
         * Each Account SHALL have a unique three-part identifier, a Key, and one
         * or more token balances.<br/>
         * Each Account SHALL have an alias, which has multiple forms, and MAY be set automatically.<br/>
         * Several additional items SHALL be associated with the Account to enable
         * full functionality.<br/>
         * Assets SHALL be represented as linked-lists with only the "head" item
         * referenced directly in the Account, and the remaining items SHALL be
         * accessible via the token relation or unique tokens maps.<br/>
         * Accounts, as most items in the network, SHALL have an expiration time,
         * recorded as seconds since the epoch, and MUST be "renewed" for a small fee
         * at expiration. This helps to reduce the amount of inactive accounts retained
         * in state.<br/>
         * Another account MAY be designated to pay any renewal fees and automatically
         * renew an account for (by default) 30-90 days at a time as a means to
         * optionally ensure important accounts remain active.<br/>
         * Accounts MAY participate in securing the network by "staking" the account
         * balances to a particular network node, and receive a portion of network
         * fees as a reward. An account MAY optionally decline these rewards but still
         * stake its balances.<br/>
         * An account MAY optionally require that inbound transfer transactions be
         * signed by that account as receiver
         * (in addition to the sender's signature).<br/>
         * As with all network entities, Account ID SHALL be represented as
         * shard.realm.X.<br/>
         * Alias and contractId SHALL be additional identifiers used to connect accounts
         * to transactions before the account is fully enabled,
         * or in EVM contracts.<br/>
         * 
         * ---
         * 
         * #### Alias
         * There is considerable complexity with `alias` (aka `evm_address`) for
         * Accounts. Much of this comes from the existence of a "hidden" alias for
         * almost all accounts, and the reuse of the alias field for both EVM reference
         * and "automatic" account creation.
         * 
         * For the purposes of this specification, we will use the following terms for
         * clarity.
         * - `key_alias` is the account public key as a protobuf serialized message
         * and used for auto-creation and subsequent lookup. This is only valid if
         * the account key is a
         * single `primitive` key, either ED25519 or ECDSA_SECP256K1.
         * - `evm_address` exists for every account and is one of
         * - `contract_address`, which is the 20 byte EVM contract address per
         * EIP-1014
         * - `evm_key_address`, which is the keccak-256 hash of a ECDSA_SECP256K1
         * `primitive` key.
         * - This is for accounts lazy-created from EVM public keys, when the
         * corresponding ECDSA_SECP256K1 public key is presented in a
         * transaction signed by the private key for that public key, the
         * account is created that key assigned, and the protobuf-serialized
         * form is set as the account alias.
         * - `long_zero`, is a synthetic 20 byte address inferred for "normally"
         * created accounts. It is constructed from the "standard" AccountID as
         * follows.
         * - 4 byte big-endian shard number
         * - 8 byte big-endian realm number
         * - 8 byte big-endian entity number
         * 
         * The `alias` field in the `Account` message SHALL contain one of four values
         * for any given account.
         * - The `key_alias`, if the account was created by transferring HBAR to the
         * account referenced by `key_alias`.
         * - The `evm_key_address` if the account was created from an EVM public key
         * - The `contract_address` if the account belongs to an EVM contract
         * - Not-Set/null/Bytes.EMPTY (collectively `null`) if the account was
         * created normally
         * 
         * If the `alias` field of an `Account` is any form of `null`, then the account
         * MAY be referenced by `alias` in an `AccountID` by using the `long_zero`
         * address for the account. This "hidden default" alias SHALL NOT be stored,
         * but is synthesized by the node software as needed, and may be synthesized by
         * an EVM contract or client software as well.
         * 
         * An AccountID in a transaction MAY reference an `Account` with
         * `shard`.`realm`.`alias`.<br/>
         * If the account `alias` field is set for an Account, that value SHALL be the
         * account alias.<br/>
         * If the account `alias` field is not set for an Account, the `long_zero`
         * alias SHALL be the account alias.
         * @constructor
         * @param {proto.Account.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Account = function (properties) {
            this.cryptoAllowances = [];
            this.approveForAllNftAllowances = [];
            this.tokenAllowances = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The unique ID of this account.
         * <p>
         * An account ID, when assigned to this field, SHALL be of
         * the form `shard.realm.number`.<br/>
         * Transactions MAY reference the account by alias, but the account itself
         * MUST always have a purely numeric identifier. This numeric ID is the
         * value used to reference the account in query responses, transaction
         * receipts, transaction records, and the block stream.
         * @member {proto.AccountID.$Properties|null|undefined} accountId
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.accountId = null;

        /**
         * An account EVM alias.
         * <p>
         * This is a value used in some contexts to reference an account when the
         * numeric account identifier is not available.<br/>
         * This field, when set to a non-default value, is immutable and
         * SHALL NOT be changed.
         * @member {Uint8Array} alias
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.alias = $util.newBuffer([]);

        /**
         * The key to be used to sign transactions from this account, if any.
         * <p>
         * This key SHALL NOT be set for hollow accounts until the account
         * is finalized.<br/>
         * This key SHALL be set on all other accounts, except for certain
         * immutable accounts (0.0.800 and 0.0.801) necessary for network function
         * and otherwise secured by the governing council.
         * @member {proto.Key.$Properties|null|undefined} key
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.key = null;

        /**
         * The current expiration time of this account, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch
         * with 0 at `1970-01-01T00:00:00.000Z`.<br/>
         * This account SHALL be due standard renewal fees when the network
         * consensus time exceeds this time.<br/>
         * If rent and expiration are enabled for the network, and automatic
         * renewal is enabled for this account, renewal fees SHALL be charged
         * after this time, and, if charged, the expiration time SHALL be
         * extended for another renewal period.<br/>
         * This account MAY be expired and removed from state at any point
         * after this time if not renewed.<br/>
         * An account holder MAY extend this time by submitting an account
         * update transaction to modify expiration time, subject to the current
         * maximum expiration time for the network.
         * @member {number|Long} expirationSecond
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.expirationSecond = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The HBAR balance of this account, in tinybar (10<sup>-8</sup> HBAR).
         * <p>
         * This value is a signed integer for efficiency, but MUST always
         * be a whole number.
         * @member {number|Long} tinybarBalance
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.tinybarBalance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A short description of this account.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.memo = "";

        /**
         * A boolean indicating that this account is deleted.
         * @member {boolean} deleted
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.deleted = false;

        /**
         * The amount of HBAR staked to this account by others.
         * @member {number|Long} stakedToMe
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.stakedToMe = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * If this account stakes to another account, this value SHALL be set to
         * the time when the current period for staking and reward
         * calculations began.
         * @member {number|Long} stakePeriodStart
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.stakePeriodStart = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An identifier for the account to which this account is
         * staking its balances as a proxy.
         * <p>
         * If this account is not currently staking its balances, then this
         * field, if set, SHALL be the sentinel value of `0.0.0`.
         * @member {proto.AccountID.$Properties|null|undefined} stakedAccountId
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.stakedAccountId = null;

        /**
         * An identifier for the node this account is staked to.
         * <p>
         * If this account is not currently staking its balances, then this
         * field, if set, SHALL be the sentinel value of `-1`.
         * Wallet software SHOULD surface staking issues to users and provide a
         * simple mechanism to update staking to a new node ID in the event the
         * prior staked node ID ceases to be valid.
         * <p>
         * <blockquote>Note: node IDs do fluctuate as node operators change.
         * The Account owner MUST submit a new transaction to change this value
         * if the current node ID changes or ceases to operate as a node. An
         * account with an invalid `staked_node_id` SHALL NOT participate in
         * staking until the `staked_node_id` is updated to a valid node ID.
         * </blockquote>
         * @member {number|Long|null|undefined} stakedNodeId
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.stakedNodeId = null;

        /**
         * A boolean indicating that this account has chosen to decline rewards for
         * staking its balances.
         * <p>
         * This account MAY still stake its balances, but SHALL NOT receive reward
         * payments for doing so.
         * @member {boolean} declineReward
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.declineReward = false;

        /**
         * A boolean indicating that the account requires a receiver signature for
         * inbound token transfer transactions.
         * <p>
         * If this value is `true` then a transaction to transfer tokens to this
         * account SHALL NOT succeed unless this account has signed the transfer
         * transaction.
         * @member {boolean} receiverSigRequired
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.receiverSigRequired = false;

        /**
         * A token ID at the head of the linked list for this account from the
         * token relations map.<br/>
         * The token relations are connected by including the "next" and "previous"
         * TokenID in each TokenRelation message. The "head" item in that list is
         * found by looking up the TokenRelation with this Account's account_id and
         * this head_token_id. Each subsequent item in the list is found via
         * similar lookup with both an AccountID and a TokenID.
         * @member {proto.TokenID.$Properties|null|undefined} headTokenId
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.headTokenId = null;

        /**
         * A NftID at the head of the linked list for this account from
         * the unique tokens map.<br/>
         * The unique token relations are connected by including the "next" and
         * "previous" NftID in each Nft message. The "head" item in that list is
         * found by looking up the Nft with ID matching this head_nft_id. Each
         * subsequent item in the list is found via similar lookup with the next
         * or previous NftID.
         * @member {proto.NftID.$Properties|null|undefined} headNftId
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.headNftId = null;

        /**
         * A serial number in the NftID at the head of the linked list for this
         * account from unique tokens map.
         * <p>
         * This MUST match the `serial_number` field of `head_nft_id`.
         * @member {number|Long} headNftSerialNumber
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.headNftSerialNumber = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A number of non-fungible tokens (NTFs) owned by the account.
         * @member {number|Long} numberOwnedNfts
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.numberOwnedNfts = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A maximum for the number of tokens that can be automatically
         * associated with this account.
         * <p>
         * If this is less than or equal to `used_auto_associations` (or 0), then
         * this account MUST manually associate with a token before transacting in
         * that token.<br/>
         * This value may also be `-1` to indicate no limit.<br/>
         * This value MUST NOT be less than `-1`.
         * @member {number} maxAutoAssociations
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.maxAutoAssociations = 0;

        /**
         * A count of used auto-association slots.
         * <p>
         * If this is greater than, or equal to, the current value of
         * `max_auto_associations`, then this account MUST manually associate with
         * a new token before transacting in that token.
         * @member {number} usedAutoAssociations
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.usedAutoAssociations = 0;

        /**
         * A count of tokens associated with this account.
         * <p>
         * This value determines a portion of the renewal fee for this account.
         * @member {number} numberAssociations
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.numberAssociations = 0;

        /**
         * A boolean indicating that this account is owned by a smart contract.
         * @member {boolean} smartContract
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.smartContract = false;

        /**
         * A count of tokens with a positive balance associated with this account.
         * <p>
         * If the account has a positive balance in any token,
         * it SHALL NOT be deleted.
         * @member {number} numberPositiveBalances
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.numberPositiveBalances = 0;

        /**
         * A nonce of this account for Ethereum interoperability.
         * @member {number|Long} ethereumNonce
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.ethereumNonce = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An amount of HBAR staked by this account at the start of
         * the last reward period.
         * @member {number|Long} stakeAtStartOfLastRewardedPeriod
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.stakeAtStartOfLastRewardedPeriod = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An account identifier for automatic renewal.<br/>
         * This is the identifier of another account, in the same shard and
         * realm as this account, that has signed a transaction allowing the
         * network to use its balance, if needed, to automatically extend this
         * account's expiration time during automatic renewal processing.
         * <p>
         * If this is set, and this account lack sufficient HBAR balance to pay
         * renewal fees when due, then the network SHALL deduct the necessary fees
         * from the designated auto renew account, if that account has sufficient
         * balance.
         * @member {proto.AccountID.$Properties|null|undefined} autoRenewAccountId
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.autoRenewAccountId = null;

        /**
         * A count of the number of seconds to extend this account's expiration.
         * <p>
         * The network SHALL extend the account's expiration by this number of
         * seconds, if funds are available, upon automatic renewal.<br/>
         * This SHALL NOT apply if the account is already deleted
         * upon expiration.<br/>
         * If this is not provided in an allowed range on account creation, the
         * transaction SHALL fail with INVALID_AUTO_RENEWAL_PERIOD. The default
         * values for the minimum period and maximum period are currently 30 days
         * and 90 days, respectively.
         * @member {number|Long} autoRenewSeconds
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.autoRenewSeconds = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A count of smart contract key-value pairs.<br/>
         * If this account is a smart-contract, this is the number of key-value
         * pairs stored on the contract.
         * <p>
         * If this account is not a smart contract, this field
         * SHALL NOT be used.<br/>
         * This value SHALL determine a portion of the storage rental
         * fees for the contract.
         * @member {number} contractKvPairsNumber
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.contractKvPairsNumber = 0;

        /**
         * A list of crypto (HBAR) allowances approved by this account.
         * <p>
         * If this is not empty, each allowance SHALL permit a specified "spender"
         * account to spend this account's HBAR balance, up to a designated
         * limit.<br/>
         * This field SHALL permit spending only HBAR balance, not other tokens the
         * account may hold. Allowances for other tokens SHALL be listed in the
         * `token_allowances` field or the `approve_for_all_nft_allowances` field.
         * @member {Array.<proto.AccountCryptoAllowance.$Properties>} cryptoAllowances
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.cryptoAllowances = $util.emptyArray;

        /**
         * A list of non-fungible token (NFT) allowances approved by this account.
         * <p>
         * If this is not empty, each allowance permits a specified "spender"
         * account to transfer _all_ of this account's non-fungible tokens from a
         * particular collection.<br/>
         * Allowances for a specific serial number MUST be directly associated with
         * that specific non-fungible token, rather than the holding account.
         * @member {Array.<proto.AccountApprovalForAllAllowance.$Properties>} approveForAllNftAllowances
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.approveForAllNftAllowances = $util.emptyArray;

        /**
         * A list of fungible token allowances approved by this account.
         * <p>
         * If this is not empty, each allowance permits a specified "spender" to
         * spend this account's fungible tokens, of the designated type, up to a
         * designated limit.
         * @member {Array.<proto.AccountFungibleTokenAllowance.$Properties>} tokenAllowances
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.tokenAllowances = $util.emptyArray;

        /**
         * A count of tokens for which this account is the treasury account.
         * <p>
         * Each native token is initially created with all tokens held by its
         * treasury, and the owner of that account (which may be a smart contract)
         * determines how those tokens are distributed.
         * @member {number} numberTreasuryTitles
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.numberTreasuryTitles = 0;

        /**
         * A flag indicating that the account is expired and pending removal.
         * <p>
         * When the network checks for entity expiration, it SHALL set this flag if
         * the account expiration time has past and the account has no HBAR
         * sufficient to pay current renewal fees.<br/>
         * If the account has an auto-renew account set with an HBAR balance that
         * could pay for an auto-renewal, then this flag SHALL NOT be set. This
         * ensures the account is not encumbered during the time between expiration
         * and when the auto-renewal processing renews the account.
         * @member {boolean} expiredAndPendingRemoval
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.expiredAndPendingRemoval = false;

        /**
         * A contract storage key.<br/>
         * This is the first key in the doubly-linked list of this
         * contract's storage mappings.
         * <p>
         * This value SHALL be empty if the account is not a contract or the
         * contract has no storage mappings.
         * @member {Uint8Array} firstContractStorageKey
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.firstContractStorageKey = $util.newBuffer([]);

        /**
         * A pending airdrop ID.<br/>
         * This is the head of the linked list for this account from the
         * account airdrops map.<br/>
         * <p>
         * The account airdrops SHALL be connected by including the "next" and
         * "previous" `PendingAirdropID` in each `AccountAirdrop` message.<br/>
         * This value SHALL NOT be empty if this account is "sender" for any
         * pending airdrop, and SHALL be empty otherwise.
         * @member {proto.PendingAirdropId.$Properties|null|undefined} headPendingAirdropId
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.headPendingAirdropId = null;

        /**
         * A number of pending airdrops.
         * <p>
         * This count SHALL be used to calculate rent _without_ walking the linked
         * list of pending airdrops associated to this account via the
         * `head_pending_airdrop_id` field.<br/>
         * This value MUST be updated for every airdrop, clam, or cancel transaction
         * that designates this account as a receiver.<br/>
         * This number MUST always match the count of entries in the "list"
         * identified by `head_pending_airdrop_id`.
         * @member {number|Long} numberPendingAirdrops
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.numberPendingAirdrops = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * The number of hooks currently in use on this account.
         * @member {number|Long} numberHooksInUse
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.numberHooksInUse = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * If the account has more than zero hooks in use, the id of the first hook in its
         * doubly-linked list of hooks.
         * @member {number|Long} firstHookId
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.firstHookId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The number of storage slots in use by this account's EVM hooks.
         * @member {number|Long} numberEvmHookStorageSlots
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.numberEvmHookStorageSlots = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * The delegated contract address set for this account.
         * <p>
         * If this field is set on the account, anytime a call is made to the account's address
         * (either an EVM call within HSCS or a child call dispatched by CryptoTransfer)
         * the EVM code of the contract referenced by `delegation_address` will be
         * executed in the context of the account.
         * @member {Uint8Array} delegationAddress
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.delegationAddress = $util.newBuffer([]);

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ID of the account or node to which this account is staking, if any.
         * <p>
         * if not set this field MAY be interpreted as staked_account_id
         * with value `0.0.0`.
         * @member {"stakedAccountId"|"stakedNodeId"|undefined} stakedId
         * @memberof proto.Account
         * @instance
         */
        $Object.defineProperty(Account.prototype, "stakedId", {
            get: $util.oneOfGetter($oneOfFields = ["stakedAccountId", "stakedNodeId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Account instance using the specified properties.
         * @function create
         * @memberof proto.Account
         * @static
         * @param {proto.Account.$Properties=} [properties] Properties to set
         * @returns {proto.Account} Account instance
         * @type {{
         *   (properties: proto.Account.$Shape): proto.Account & proto.Account.$Shape;
         *   (properties?: proto.Account.$Properties): proto.Account;
         * }}
         */
        Account.create = function(properties) {
            return new Account(properties);
        };

        /**
         * Encodes the specified Account message. Does not implicitly {@link proto.Account.verify|verify} messages.
         * @function encode
         * @memberof proto.Account
         * @static
         * @param {proto.Account.$Properties} message Account message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Account.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias") && message.alias.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.alias);
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                $root.proto.Key.encode(message.key, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.expirationSecond != null && $Object.hasOwnProperty.call(message, "expirationSecond") && (typeof message.expirationSecond === "object" ? message.expirationSecond.low || message.expirationSecond.high : message.expirationSecond !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.expirationSecond);
            if (message.tinybarBalance != null && $Object.hasOwnProperty.call(message, "tinybarBalance") && (typeof message.tinybarBalance === "object" ? message.tinybarBalance.low || message.tinybarBalance.high : message.tinybarBalance !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.tinybarBalance);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.memo);
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.deleted);
            if (message.stakedToMe != null && $Object.hasOwnProperty.call(message, "stakedToMe") && (typeof message.stakedToMe === "object" ? message.stakedToMe.low || message.stakedToMe.high : message.stakedToMe !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.stakedToMe);
            if (message.stakePeriodStart != null && $Object.hasOwnProperty.call(message, "stakePeriodStart") && (typeof message.stakePeriodStart === "object" ? message.stakePeriodStart.low || message.stakePeriodStart.high : message.stakePeriodStart !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.stakePeriodStart);
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId"))
                $root.proto.AccountID.encode(message.stakedAccountId, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId"))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.stakedNodeId);
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward") && message.declineReward !== false)
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.declineReward);
            if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired") && message.receiverSigRequired !== false)
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.receiverSigRequired);
            if (message.headTokenId != null && $Object.hasOwnProperty.call(message, "headTokenId"))
                $root.proto.TokenID.encode(message.headTokenId, writer.uint32(/* id 14, wireType 2 =*/114).fork(), _depth + 1).ldelim();
            if (message.headNftId != null && $Object.hasOwnProperty.call(message, "headNftId"))
                $root.proto.NftID.encode(message.headNftId, writer.uint32(/* id 15, wireType 2 =*/122).fork(), _depth + 1).ldelim();
            if (message.headNftSerialNumber != null && $Object.hasOwnProperty.call(message, "headNftSerialNumber") && (typeof message.headNftSerialNumber === "object" ? message.headNftSerialNumber.low || message.headNftSerialNumber.high : message.headNftSerialNumber !== 0))
                writer.uint32(/* id 16, wireType 0 =*/128).int64(message.headNftSerialNumber);
            if (message.numberOwnedNfts != null && $Object.hasOwnProperty.call(message, "numberOwnedNfts") && (typeof message.numberOwnedNfts === "object" ? message.numberOwnedNfts.low || message.numberOwnedNfts.high : message.numberOwnedNfts !== 0))
                writer.uint32(/* id 17, wireType 0 =*/136).int64(message.numberOwnedNfts);
            if (message.maxAutoAssociations != null && $Object.hasOwnProperty.call(message, "maxAutoAssociations") && message.maxAutoAssociations !== 0)
                writer.uint32(/* id 18, wireType 0 =*/144).int32(message.maxAutoAssociations);
            if (message.usedAutoAssociations != null && $Object.hasOwnProperty.call(message, "usedAutoAssociations") && message.usedAutoAssociations !== 0)
                writer.uint32(/* id 19, wireType 0 =*/152).int32(message.usedAutoAssociations);
            if (message.numberAssociations != null && $Object.hasOwnProperty.call(message, "numberAssociations") && message.numberAssociations !== 0)
                writer.uint32(/* id 20, wireType 0 =*/160).int32(message.numberAssociations);
            if (message.smartContract != null && $Object.hasOwnProperty.call(message, "smartContract") && message.smartContract !== false)
                writer.uint32(/* id 21, wireType 0 =*/168).bool(message.smartContract);
            if (message.numberPositiveBalances != null && $Object.hasOwnProperty.call(message, "numberPositiveBalances") && message.numberPositiveBalances !== 0)
                writer.uint32(/* id 22, wireType 0 =*/176).int32(message.numberPositiveBalances);
            if (message.ethereumNonce != null && $Object.hasOwnProperty.call(message, "ethereumNonce") && (typeof message.ethereumNonce === "object" ? message.ethereumNonce.low || message.ethereumNonce.high : message.ethereumNonce !== 0))
                writer.uint32(/* id 23, wireType 0 =*/184).int64(message.ethereumNonce);
            if (message.stakeAtStartOfLastRewardedPeriod != null && $Object.hasOwnProperty.call(message, "stakeAtStartOfLastRewardedPeriod") && (typeof message.stakeAtStartOfLastRewardedPeriod === "object" ? message.stakeAtStartOfLastRewardedPeriod.low || message.stakeAtStartOfLastRewardedPeriod.high : message.stakeAtStartOfLastRewardedPeriod !== 0))
                writer.uint32(/* id 24, wireType 0 =*/192).int64(message.stakeAtStartOfLastRewardedPeriod);
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId"))
                $root.proto.AccountID.encode(message.autoRenewAccountId, writer.uint32(/* id 25, wireType 2 =*/202).fork(), _depth + 1).ldelim();
            if (message.autoRenewSeconds != null && $Object.hasOwnProperty.call(message, "autoRenewSeconds") && (typeof message.autoRenewSeconds === "object" ? message.autoRenewSeconds.low || message.autoRenewSeconds.high : message.autoRenewSeconds !== 0))
                writer.uint32(/* id 26, wireType 0 =*/208).int64(message.autoRenewSeconds);
            if (message.contractKvPairsNumber != null && $Object.hasOwnProperty.call(message, "contractKvPairsNumber") && message.contractKvPairsNumber !== 0)
                writer.uint32(/* id 27, wireType 0 =*/216).int32(message.contractKvPairsNumber);
            if (message.cryptoAllowances != null && message.cryptoAllowances.length)
                for (let i = 0; i < message.cryptoAllowances.length; ++i)
                    $root.proto.AccountCryptoAllowance.encode(message.cryptoAllowances[i], writer.uint32(/* id 28, wireType 2 =*/226).fork(), _depth + 1).ldelim();
            if (message.approveForAllNftAllowances != null && message.approveForAllNftAllowances.length)
                for (let i = 0; i < message.approveForAllNftAllowances.length; ++i)
                    $root.proto.AccountApprovalForAllAllowance.encode(message.approveForAllNftAllowances[i], writer.uint32(/* id 29, wireType 2 =*/234).fork(), _depth + 1).ldelim();
            if (message.tokenAllowances != null && message.tokenAllowances.length)
                for (let i = 0; i < message.tokenAllowances.length; ++i)
                    $root.proto.AccountFungibleTokenAllowance.encode(message.tokenAllowances[i], writer.uint32(/* id 30, wireType 2 =*/242).fork(), _depth + 1).ldelim();
            if (message.numberTreasuryTitles != null && $Object.hasOwnProperty.call(message, "numberTreasuryTitles") && message.numberTreasuryTitles !== 0)
                writer.uint32(/* id 31, wireType 0 =*/248).uint32(message.numberTreasuryTitles);
            if (message.expiredAndPendingRemoval != null && $Object.hasOwnProperty.call(message, "expiredAndPendingRemoval") && message.expiredAndPendingRemoval !== false)
                writer.uint32(/* id 32, wireType 0 =*/256).bool(message.expiredAndPendingRemoval);
            if (message.firstContractStorageKey != null && $Object.hasOwnProperty.call(message, "firstContractStorageKey") && message.firstContractStorageKey.length)
                writer.uint32(/* id 33, wireType 2 =*/266).bytes(message.firstContractStorageKey);
            if (message.headPendingAirdropId != null && $Object.hasOwnProperty.call(message, "headPendingAirdropId"))
                $root.proto.PendingAirdropId.encode(message.headPendingAirdropId, writer.uint32(/* id 34, wireType 2 =*/274).fork(), _depth + 1).ldelim();
            if (message.numberPendingAirdrops != null && $Object.hasOwnProperty.call(message, "numberPendingAirdrops") && (typeof message.numberPendingAirdrops === "object" ? message.numberPendingAirdrops.low || message.numberPendingAirdrops.high : message.numberPendingAirdrops !== 0))
                writer.uint32(/* id 35, wireType 0 =*/280).uint64(message.numberPendingAirdrops);
            if (message.numberHooksInUse != null && $Object.hasOwnProperty.call(message, "numberHooksInUse") && (typeof message.numberHooksInUse === "object" ? message.numberHooksInUse.low || message.numberHooksInUse.high : message.numberHooksInUse !== 0))
                writer.uint32(/* id 36, wireType 0 =*/288).uint64(message.numberHooksInUse);
            if (message.firstHookId != null && $Object.hasOwnProperty.call(message, "firstHookId") && (typeof message.firstHookId === "object" ? message.firstHookId.low || message.firstHookId.high : message.firstHookId !== 0))
                writer.uint32(/* id 37, wireType 0 =*/296).int64(message.firstHookId);
            if (message.numberEvmHookStorageSlots != null && $Object.hasOwnProperty.call(message, "numberEvmHookStorageSlots") && (typeof message.numberEvmHookStorageSlots === "object" ? message.numberEvmHookStorageSlots.low || message.numberEvmHookStorageSlots.high : message.numberEvmHookStorageSlots !== 0))
                writer.uint32(/* id 38, wireType 0 =*/304).uint64(message.numberEvmHookStorageSlots);
            if (message.delegationAddress != null && $Object.hasOwnProperty.call(message, "delegationAddress") && message.delegationAddress.length)
                writer.uint32(/* id 39, wireType 2 =*/314).bytes(message.delegationAddress);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Account message, length delimited. Does not implicitly {@link proto.Account.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Account
         * @static
         * @param {proto.Account.$Properties} message Account message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Account.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an Account message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Account
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Account & proto.Account.$Shape} Account
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Account.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Account();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.alias = value;
                        else
                            delete message.alias;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.key = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.key);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expirationSecond = value;
                        else
                            delete message.expirationSecond;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.tinybarBalance = value;
                        else
                            delete message.tinybarBalance;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.deleted = value;
                        else
                            delete message.deleted;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakedToMe = value;
                        else
                            delete message.stakedToMe;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakePeriodStart = value;
                        else
                            delete message.stakePeriodStart;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.stakedAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stakedAccountId);
                        message.stakedId = "stakedAccountId";
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        message.stakedNodeId = reader.int64();
                        message.stakedId = "stakedNodeId";
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.declineReward = value;
                        else
                            delete message.declineReward;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.receiverSigRequired = value;
                        else
                            delete message.receiverSigRequired;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.headTokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.headTokenId);
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.headNftId = $root.proto.NftID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.headNftId);
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.headNftSerialNumber = value;
                        else
                            delete message.headNftSerialNumber;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.numberOwnedNfts = value;
                        else
                            delete message.numberOwnedNfts;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.maxAutoAssociations = value;
                        else
                            delete message.maxAutoAssociations;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.usedAutoAssociations = value;
                        else
                            delete message.usedAutoAssociations;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.numberAssociations = value;
                        else
                            delete message.numberAssociations;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.smartContract = value;
                        else
                            delete message.smartContract;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.numberPositiveBalances = value;
                        else
                            delete message.numberPositiveBalances;
                        continue;
                    }
                case 23: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.ethereumNonce = value;
                        else
                            delete message.ethereumNonce;
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakeAtStartOfLastRewardedPeriod = value;
                        else
                            delete message.stakeAtStartOfLastRewardedPeriod;
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewAccountId);
                        continue;
                    }
                case 26: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.autoRenewSeconds = value;
                        else
                            delete message.autoRenewSeconds;
                        continue;
                    }
                case 27: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.contractKvPairsNumber = value;
                        else
                            delete message.contractKvPairsNumber;
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        if (!(message.cryptoAllowances && message.cryptoAllowances.length))
                            message.cryptoAllowances = [];
                        message.cryptoAllowances.push($root.proto.AccountCryptoAllowance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 29: {
                        if (wireType !== 2)
                            break;
                        if (!(message.approveForAllNftAllowances && message.approveForAllNftAllowances.length))
                            message.approveForAllNftAllowances = [];
                        message.approveForAllNftAllowances.push($root.proto.AccountApprovalForAllAllowance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 30: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tokenAllowances && message.tokenAllowances.length))
                            message.tokenAllowances = [];
                        message.tokenAllowances.push($root.proto.AccountFungibleTokenAllowance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 31: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.numberTreasuryTitles = value;
                        else
                            delete message.numberTreasuryTitles;
                        continue;
                    }
                case 32: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.expiredAndPendingRemoval = value;
                        else
                            delete message.expiredAndPendingRemoval;
                        continue;
                    }
                case 33: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.firstContractStorageKey = value;
                        else
                            delete message.firstContractStorageKey;
                        continue;
                    }
                case 34: {
                        if (wireType !== 2)
                            break;
                        message.headPendingAirdropId = $root.proto.PendingAirdropId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.headPendingAirdropId);
                        continue;
                    }
                case 35: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.numberPendingAirdrops = value;
                        else
                            delete message.numberPendingAirdrops;
                        continue;
                    }
                case 36: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.numberHooksInUse = value;
                        else
                            delete message.numberHooksInUse;
                        continue;
                    }
                case 37: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.firstHookId = value;
                        else
                            delete message.firstHookId;
                        continue;
                    }
                case 38: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.numberEvmHookStorageSlots = value;
                        else
                            delete message.numberEvmHookStorageSlots;
                        continue;
                    }
                case 39: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.delegationAddress = value;
                        else
                            delete message.delegationAddress;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an Account message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Account
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Account & proto.Account.$Shape} Account
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Account.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Account message.
         * @function verify
         * @memberof proto.Account
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Account.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                if (error)
                    return "accountId." + error;
            }
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                if (!(message.alias && typeof message.alias.length === "number" || $util.isString(message.alias)))
                    return "alias: buffer expected";
            if (message.key != null && $Object.hasOwnProperty.call(message, "key")) {
                let error = $root.proto.Key.verify(message.key, _depth + 1);
                if (error)
                    return "key." + error;
            }
            if (message.expirationSecond != null && $Object.hasOwnProperty.call(message, "expirationSecond"))
                if (!$util.isInteger(message.expirationSecond) && !(message.expirationSecond && $util.isInteger(message.expirationSecond.low) && $util.isInteger(message.expirationSecond.high)))
                    return "expirationSecond: integer|Long expected";
            if (message.tinybarBalance != null && $Object.hasOwnProperty.call(message, "tinybarBalance"))
                if (!$util.isInteger(message.tinybarBalance) && !(message.tinybarBalance && $util.isInteger(message.tinybarBalance.low) && $util.isInteger(message.tinybarBalance.high)))
                    return "tinybarBalance: integer|Long expected";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                if (typeof message.deleted !== "boolean")
                    return "deleted: boolean expected";
            if (message.stakedToMe != null && $Object.hasOwnProperty.call(message, "stakedToMe"))
                if (!$util.isInteger(message.stakedToMe) && !(message.stakedToMe && $util.isInteger(message.stakedToMe.low) && $util.isInteger(message.stakedToMe.high)))
                    return "stakedToMe: integer|Long expected";
            if (message.stakePeriodStart != null && $Object.hasOwnProperty.call(message, "stakePeriodStart"))
                if (!$util.isInteger(message.stakePeriodStart) && !(message.stakePeriodStart && $util.isInteger(message.stakePeriodStart.low) && $util.isInteger(message.stakePeriodStart.high)))
                    return "stakePeriodStart: integer|Long expected";
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId")) {
                properties.stakedId = 1;
                {
                    let error = $root.proto.AccountID.verify(message.stakedAccountId, _depth + 1);
                    if (error)
                        return "stakedAccountId." + error;
                }
            }
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId")) {
                if (properties.stakedId === 1)
                    return "stakedId: multiple values";
                properties.stakedId = 1;
                if (!$util.isInteger(message.stakedNodeId) && !(message.stakedNodeId && $util.isInteger(message.stakedNodeId.low) && $util.isInteger(message.stakedNodeId.high)))
                    return "stakedNodeId: integer|Long expected";
            }
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                if (typeof message.declineReward !== "boolean")
                    return "declineReward: boolean expected";
            if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired"))
                if (typeof message.receiverSigRequired !== "boolean")
                    return "receiverSigRequired: boolean expected";
            if (message.headTokenId != null && $Object.hasOwnProperty.call(message, "headTokenId")) {
                let error = $root.proto.TokenID.verify(message.headTokenId, _depth + 1);
                if (error)
                    return "headTokenId." + error;
            }
            if (message.headNftId != null && $Object.hasOwnProperty.call(message, "headNftId")) {
                let error = $root.proto.NftID.verify(message.headNftId, _depth + 1);
                if (error)
                    return "headNftId." + error;
            }
            if (message.headNftSerialNumber != null && $Object.hasOwnProperty.call(message, "headNftSerialNumber"))
                if (!$util.isInteger(message.headNftSerialNumber) && !(message.headNftSerialNumber && $util.isInteger(message.headNftSerialNumber.low) && $util.isInteger(message.headNftSerialNumber.high)))
                    return "headNftSerialNumber: integer|Long expected";
            if (message.numberOwnedNfts != null && $Object.hasOwnProperty.call(message, "numberOwnedNfts"))
                if (!$util.isInteger(message.numberOwnedNfts) && !(message.numberOwnedNfts && $util.isInteger(message.numberOwnedNfts.low) && $util.isInteger(message.numberOwnedNfts.high)))
                    return "numberOwnedNfts: integer|Long expected";
            if (message.maxAutoAssociations != null && $Object.hasOwnProperty.call(message, "maxAutoAssociations"))
                if (!$util.isInteger(message.maxAutoAssociations))
                    return "maxAutoAssociations: integer expected";
            if (message.usedAutoAssociations != null && $Object.hasOwnProperty.call(message, "usedAutoAssociations"))
                if (!$util.isInteger(message.usedAutoAssociations))
                    return "usedAutoAssociations: integer expected";
            if (message.numberAssociations != null && $Object.hasOwnProperty.call(message, "numberAssociations"))
                if (!$util.isInteger(message.numberAssociations))
                    return "numberAssociations: integer expected";
            if (message.smartContract != null && $Object.hasOwnProperty.call(message, "smartContract"))
                if (typeof message.smartContract !== "boolean")
                    return "smartContract: boolean expected";
            if (message.numberPositiveBalances != null && $Object.hasOwnProperty.call(message, "numberPositiveBalances"))
                if (!$util.isInteger(message.numberPositiveBalances))
                    return "numberPositiveBalances: integer expected";
            if (message.ethereumNonce != null && $Object.hasOwnProperty.call(message, "ethereumNonce"))
                if (!$util.isInteger(message.ethereumNonce) && !(message.ethereumNonce && $util.isInteger(message.ethereumNonce.low) && $util.isInteger(message.ethereumNonce.high)))
                    return "ethereumNonce: integer|Long expected";
            if (message.stakeAtStartOfLastRewardedPeriod != null && $Object.hasOwnProperty.call(message, "stakeAtStartOfLastRewardedPeriod"))
                if (!$util.isInteger(message.stakeAtStartOfLastRewardedPeriod) && !(message.stakeAtStartOfLastRewardedPeriod && $util.isInteger(message.stakeAtStartOfLastRewardedPeriod.low) && $util.isInteger(message.stakeAtStartOfLastRewardedPeriod.high)))
                    return "stakeAtStartOfLastRewardedPeriod: integer|Long expected";
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId")) {
                let error = $root.proto.AccountID.verify(message.autoRenewAccountId, _depth + 1);
                if (error)
                    return "autoRenewAccountId." + error;
            }
            if (message.autoRenewSeconds != null && $Object.hasOwnProperty.call(message, "autoRenewSeconds"))
                if (!$util.isInteger(message.autoRenewSeconds) && !(message.autoRenewSeconds && $util.isInteger(message.autoRenewSeconds.low) && $util.isInteger(message.autoRenewSeconds.high)))
                    return "autoRenewSeconds: integer|Long expected";
            if (message.contractKvPairsNumber != null && $Object.hasOwnProperty.call(message, "contractKvPairsNumber"))
                if (!$util.isInteger(message.contractKvPairsNumber))
                    return "contractKvPairsNumber: integer expected";
            if (message.cryptoAllowances != null && $Object.hasOwnProperty.call(message, "cryptoAllowances")) {
                if (!$Array.isArray(message.cryptoAllowances))
                    return "cryptoAllowances: array expected";
                for (let i = 0; i < message.cryptoAllowances.length; ++i) {
                    let error = $root.proto.AccountCryptoAllowance.verify(message.cryptoAllowances[i], _depth + 1);
                    if (error)
                        return "cryptoAllowances." + error;
                }
            }
            if (message.approveForAllNftAllowances != null && $Object.hasOwnProperty.call(message, "approveForAllNftAllowances")) {
                if (!$Array.isArray(message.approveForAllNftAllowances))
                    return "approveForAllNftAllowances: array expected";
                for (let i = 0; i < message.approveForAllNftAllowances.length; ++i) {
                    let error = $root.proto.AccountApprovalForAllAllowance.verify(message.approveForAllNftAllowances[i], _depth + 1);
                    if (error)
                        return "approveForAllNftAllowances." + error;
                }
            }
            if (message.tokenAllowances != null && $Object.hasOwnProperty.call(message, "tokenAllowances")) {
                if (!$Array.isArray(message.tokenAllowances))
                    return "tokenAllowances: array expected";
                for (let i = 0; i < message.tokenAllowances.length; ++i) {
                    let error = $root.proto.AccountFungibleTokenAllowance.verify(message.tokenAllowances[i], _depth + 1);
                    if (error)
                        return "tokenAllowances." + error;
                }
            }
            if (message.numberTreasuryTitles != null && $Object.hasOwnProperty.call(message, "numberTreasuryTitles"))
                if (!$util.isInteger(message.numberTreasuryTitles))
                    return "numberTreasuryTitles: integer expected";
            if (message.expiredAndPendingRemoval != null && $Object.hasOwnProperty.call(message, "expiredAndPendingRemoval"))
                if (typeof message.expiredAndPendingRemoval !== "boolean")
                    return "expiredAndPendingRemoval: boolean expected";
            if (message.firstContractStorageKey != null && $Object.hasOwnProperty.call(message, "firstContractStorageKey"))
                if (!(message.firstContractStorageKey && typeof message.firstContractStorageKey.length === "number" || $util.isString(message.firstContractStorageKey)))
                    return "firstContractStorageKey: buffer expected";
            if (message.headPendingAirdropId != null && $Object.hasOwnProperty.call(message, "headPendingAirdropId")) {
                let error = $root.proto.PendingAirdropId.verify(message.headPendingAirdropId, _depth + 1);
                if (error)
                    return "headPendingAirdropId." + error;
            }
            if (message.numberPendingAirdrops != null && $Object.hasOwnProperty.call(message, "numberPendingAirdrops"))
                if (!$util.isInteger(message.numberPendingAirdrops) && !(message.numberPendingAirdrops && $util.isInteger(message.numberPendingAirdrops.low) && $util.isInteger(message.numberPendingAirdrops.high)))
                    return "numberPendingAirdrops: integer|Long expected";
            if (message.numberHooksInUse != null && $Object.hasOwnProperty.call(message, "numberHooksInUse"))
                if (!$util.isInteger(message.numberHooksInUse) && !(message.numberHooksInUse && $util.isInteger(message.numberHooksInUse.low) && $util.isInteger(message.numberHooksInUse.high)))
                    return "numberHooksInUse: integer|Long expected";
            if (message.firstHookId != null && $Object.hasOwnProperty.call(message, "firstHookId"))
                if (!$util.isInteger(message.firstHookId) && !(message.firstHookId && $util.isInteger(message.firstHookId.low) && $util.isInteger(message.firstHookId.high)))
                    return "firstHookId: integer|Long expected";
            if (message.numberEvmHookStorageSlots != null && $Object.hasOwnProperty.call(message, "numberEvmHookStorageSlots"))
                if (!$util.isInteger(message.numberEvmHookStorageSlots) && !(message.numberEvmHookStorageSlots && $util.isInteger(message.numberEvmHookStorageSlots.low) && $util.isInteger(message.numberEvmHookStorageSlots.high)))
                    return "numberEvmHookStorageSlots: integer|Long expected";
            if (message.delegationAddress != null && $Object.hasOwnProperty.call(message, "delegationAddress"))
                if (!(message.delegationAddress && typeof message.delegationAddress.length === "number" || $util.isString(message.delegationAddress)))
                    return "delegationAddress: buffer expected";
            return null;
        };

        /**
         * Creates an Account message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Account
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Account} Account
         */
        Account.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Account)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Account: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Account();
            if (object.accountId != null) {
                if (!$util.isObject(object.accountId))
                    throw $TypeError(".proto.Account.accountId: object expected");
                message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
            }
            if (object.alias != null)
                if (object.alias.length)
                    if (typeof object.alias === "string")
                        $util.base64.decode(object.alias, message.alias = $util.newBuffer($util.base64.length(object.alias)), 0);
                    else if (object.alias.length >= 0)
                        message.alias = object.alias;
            if (object.key != null) {
                if (!$util.isObject(object.key))
                    throw $TypeError(".proto.Account.key: object expected");
                message.key = $root.proto.Key.fromObject(object.key, _depth + 1);
            }
            if (object.expirationSecond != null)
                if (typeof object.expirationSecond === "object" ? object.expirationSecond.low || object.expirationSecond.high : $Number(object.expirationSecond) !== 0)
                    if ($util.Long)
                        message.expirationSecond = $util.Long.fromValue(object.expirationSecond, false);
                    else if (typeof object.expirationSecond === "string")
                        message.expirationSecond = $parseInt(object.expirationSecond, 10);
                    else if (typeof object.expirationSecond === "number")
                        message.expirationSecond = object.expirationSecond;
                    else if (typeof object.expirationSecond === "object")
                        message.expirationSecond = new $util.LongBits(object.expirationSecond.low >>> 0, object.expirationSecond.high >>> 0).toNumber();
            if (object.tinybarBalance != null)
                if (typeof object.tinybarBalance === "object" ? object.tinybarBalance.low || object.tinybarBalance.high : $Number(object.tinybarBalance) !== 0)
                    if ($util.Long)
                        message.tinybarBalance = $util.Long.fromValue(object.tinybarBalance, false);
                    else if (typeof object.tinybarBalance === "string")
                        message.tinybarBalance = $parseInt(object.tinybarBalance, 10);
                    else if (typeof object.tinybarBalance === "number")
                        message.tinybarBalance = object.tinybarBalance;
                    else if (typeof object.tinybarBalance === "object")
                        message.tinybarBalance = new $util.LongBits(object.tinybarBalance.low >>> 0, object.tinybarBalance.high >>> 0).toNumber();
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.deleted != null)
                if (object.deleted)
                    message.deleted = $Boolean(object.deleted);
            if (object.stakedToMe != null)
                if (typeof object.stakedToMe === "object" ? object.stakedToMe.low || object.stakedToMe.high : $Number(object.stakedToMe) !== 0)
                    if ($util.Long)
                        message.stakedToMe = $util.Long.fromValue(object.stakedToMe, false);
                    else if (typeof object.stakedToMe === "string")
                        message.stakedToMe = $parseInt(object.stakedToMe, 10);
                    else if (typeof object.stakedToMe === "number")
                        message.stakedToMe = object.stakedToMe;
                    else if (typeof object.stakedToMe === "object")
                        message.stakedToMe = new $util.LongBits(object.stakedToMe.low >>> 0, object.stakedToMe.high >>> 0).toNumber();
            if (object.stakePeriodStart != null)
                if (typeof object.stakePeriodStart === "object" ? object.stakePeriodStart.low || object.stakePeriodStart.high : $Number(object.stakePeriodStart) !== 0)
                    if ($util.Long)
                        message.stakePeriodStart = $util.Long.fromValue(object.stakePeriodStart, false);
                    else if (typeof object.stakePeriodStart === "string")
                        message.stakePeriodStart = $parseInt(object.stakePeriodStart, 10);
                    else if (typeof object.stakePeriodStart === "number")
                        message.stakePeriodStart = object.stakePeriodStart;
                    else if (typeof object.stakePeriodStart === "object")
                        message.stakePeriodStart = new $util.LongBits(object.stakePeriodStart.low >>> 0, object.stakePeriodStart.high >>> 0).toNumber();
            if (object.stakedAccountId != null) {
                if (!$util.isObject(object.stakedAccountId))
                    throw $TypeError(".proto.Account.stakedAccountId: object expected");
                message.stakedAccountId = $root.proto.AccountID.fromObject(object.stakedAccountId, _depth + 1);
            }
            if (object.stakedNodeId != null)
                if ($util.Long)
                    message.stakedNodeId = $util.Long.fromValue(object.stakedNodeId, false);
                else if (typeof object.stakedNodeId === "string")
                    message.stakedNodeId = $parseInt(object.stakedNodeId, 10);
                else if (typeof object.stakedNodeId === "number")
                    message.stakedNodeId = object.stakedNodeId;
                else if (typeof object.stakedNodeId === "object")
                    message.stakedNodeId = new $util.LongBits(object.stakedNodeId.low >>> 0, object.stakedNodeId.high >>> 0).toNumber();
            if (object.declineReward != null)
                if (object.declineReward)
                    message.declineReward = $Boolean(object.declineReward);
            if (object.receiverSigRequired != null)
                if (object.receiverSigRequired)
                    message.receiverSigRequired = $Boolean(object.receiverSigRequired);
            if (object.headTokenId != null) {
                if (!$util.isObject(object.headTokenId))
                    throw $TypeError(".proto.Account.headTokenId: object expected");
                message.headTokenId = $root.proto.TokenID.fromObject(object.headTokenId, _depth + 1);
            }
            if (object.headNftId != null) {
                if (!$util.isObject(object.headNftId))
                    throw $TypeError(".proto.Account.headNftId: object expected");
                message.headNftId = $root.proto.NftID.fromObject(object.headNftId, _depth + 1);
            }
            if (object.headNftSerialNumber != null)
                if (typeof object.headNftSerialNumber === "object" ? object.headNftSerialNumber.low || object.headNftSerialNumber.high : $Number(object.headNftSerialNumber) !== 0)
                    if ($util.Long)
                        message.headNftSerialNumber = $util.Long.fromValue(object.headNftSerialNumber, false);
                    else if (typeof object.headNftSerialNumber === "string")
                        message.headNftSerialNumber = $parseInt(object.headNftSerialNumber, 10);
                    else if (typeof object.headNftSerialNumber === "number")
                        message.headNftSerialNumber = object.headNftSerialNumber;
                    else if (typeof object.headNftSerialNumber === "object")
                        message.headNftSerialNumber = new $util.LongBits(object.headNftSerialNumber.low >>> 0, object.headNftSerialNumber.high >>> 0).toNumber();
            if (object.numberOwnedNfts != null)
                if (typeof object.numberOwnedNfts === "object" ? object.numberOwnedNfts.low || object.numberOwnedNfts.high : $Number(object.numberOwnedNfts) !== 0)
                    if ($util.Long)
                        message.numberOwnedNfts = $util.Long.fromValue(object.numberOwnedNfts, false);
                    else if (typeof object.numberOwnedNfts === "string")
                        message.numberOwnedNfts = $parseInt(object.numberOwnedNfts, 10);
                    else if (typeof object.numberOwnedNfts === "number")
                        message.numberOwnedNfts = object.numberOwnedNfts;
                    else if (typeof object.numberOwnedNfts === "object")
                        message.numberOwnedNfts = new $util.LongBits(object.numberOwnedNfts.low >>> 0, object.numberOwnedNfts.high >>> 0).toNumber();
            if (object.maxAutoAssociations != null)
                if ($Number(object.maxAutoAssociations) !== 0)
                    message.maxAutoAssociations = object.maxAutoAssociations | 0;
            if (object.usedAutoAssociations != null)
                if ($Number(object.usedAutoAssociations) !== 0)
                    message.usedAutoAssociations = object.usedAutoAssociations | 0;
            if (object.numberAssociations != null)
                if ($Number(object.numberAssociations) !== 0)
                    message.numberAssociations = object.numberAssociations | 0;
            if (object.smartContract != null)
                if (object.smartContract)
                    message.smartContract = $Boolean(object.smartContract);
            if (object.numberPositiveBalances != null)
                if ($Number(object.numberPositiveBalances) !== 0)
                    message.numberPositiveBalances = object.numberPositiveBalances | 0;
            if (object.ethereumNonce != null)
                if (typeof object.ethereumNonce === "object" ? object.ethereumNonce.low || object.ethereumNonce.high : $Number(object.ethereumNonce) !== 0)
                    if ($util.Long)
                        message.ethereumNonce = $util.Long.fromValue(object.ethereumNonce, false);
                    else if (typeof object.ethereumNonce === "string")
                        message.ethereumNonce = $parseInt(object.ethereumNonce, 10);
                    else if (typeof object.ethereumNonce === "number")
                        message.ethereumNonce = object.ethereumNonce;
                    else if (typeof object.ethereumNonce === "object")
                        message.ethereumNonce = new $util.LongBits(object.ethereumNonce.low >>> 0, object.ethereumNonce.high >>> 0).toNumber();
            if (object.stakeAtStartOfLastRewardedPeriod != null)
                if (typeof object.stakeAtStartOfLastRewardedPeriod === "object" ? object.stakeAtStartOfLastRewardedPeriod.low || object.stakeAtStartOfLastRewardedPeriod.high : $Number(object.stakeAtStartOfLastRewardedPeriod) !== 0)
                    if ($util.Long)
                        message.stakeAtStartOfLastRewardedPeriod = $util.Long.fromValue(object.stakeAtStartOfLastRewardedPeriod, false);
                    else if (typeof object.stakeAtStartOfLastRewardedPeriod === "string")
                        message.stakeAtStartOfLastRewardedPeriod = $parseInt(object.stakeAtStartOfLastRewardedPeriod, 10);
                    else if (typeof object.stakeAtStartOfLastRewardedPeriod === "number")
                        message.stakeAtStartOfLastRewardedPeriod = object.stakeAtStartOfLastRewardedPeriod;
                    else if (typeof object.stakeAtStartOfLastRewardedPeriod === "object")
                        message.stakeAtStartOfLastRewardedPeriod = new $util.LongBits(object.stakeAtStartOfLastRewardedPeriod.low >>> 0, object.stakeAtStartOfLastRewardedPeriod.high >>> 0).toNumber();
            if (object.autoRenewAccountId != null) {
                if (!$util.isObject(object.autoRenewAccountId))
                    throw $TypeError(".proto.Account.autoRenewAccountId: object expected");
                message.autoRenewAccountId = $root.proto.AccountID.fromObject(object.autoRenewAccountId, _depth + 1);
            }
            if (object.autoRenewSeconds != null)
                if (typeof object.autoRenewSeconds === "object" ? object.autoRenewSeconds.low || object.autoRenewSeconds.high : $Number(object.autoRenewSeconds) !== 0)
                    if ($util.Long)
                        message.autoRenewSeconds = $util.Long.fromValue(object.autoRenewSeconds, false);
                    else if (typeof object.autoRenewSeconds === "string")
                        message.autoRenewSeconds = $parseInt(object.autoRenewSeconds, 10);
                    else if (typeof object.autoRenewSeconds === "number")
                        message.autoRenewSeconds = object.autoRenewSeconds;
                    else if (typeof object.autoRenewSeconds === "object")
                        message.autoRenewSeconds = new $util.LongBits(object.autoRenewSeconds.low >>> 0, object.autoRenewSeconds.high >>> 0).toNumber();
            if (object.contractKvPairsNumber != null)
                if ($Number(object.contractKvPairsNumber) !== 0)
                    message.contractKvPairsNumber = object.contractKvPairsNumber | 0;
            if (object.cryptoAllowances) {
                if (!$Array.isArray(object.cryptoAllowances))
                    throw $TypeError(".proto.Account.cryptoAllowances: array expected");
                message.cryptoAllowances = $Array(object.cryptoAllowances.length);
                for (let i = 0; i < object.cryptoAllowances.length; ++i) {
                    if (!$util.isObject(object.cryptoAllowances[i]))
                        throw $TypeError(".proto.Account.cryptoAllowances: object expected");
                    message.cryptoAllowances[i] = $root.proto.AccountCryptoAllowance.fromObject(object.cryptoAllowances[i], _depth + 1);
                }
            }
            if (object.approveForAllNftAllowances) {
                if (!$Array.isArray(object.approveForAllNftAllowances))
                    throw $TypeError(".proto.Account.approveForAllNftAllowances: array expected");
                message.approveForAllNftAllowances = $Array(object.approveForAllNftAllowances.length);
                for (let i = 0; i < object.approveForAllNftAllowances.length; ++i) {
                    if (!$util.isObject(object.approveForAllNftAllowances[i]))
                        throw $TypeError(".proto.Account.approveForAllNftAllowances: object expected");
                    message.approveForAllNftAllowances[i] = $root.proto.AccountApprovalForAllAllowance.fromObject(object.approveForAllNftAllowances[i], _depth + 1);
                }
            }
            if (object.tokenAllowances) {
                if (!$Array.isArray(object.tokenAllowances))
                    throw $TypeError(".proto.Account.tokenAllowances: array expected");
                message.tokenAllowances = $Array(object.tokenAllowances.length);
                for (let i = 0; i < object.tokenAllowances.length; ++i) {
                    if (!$util.isObject(object.tokenAllowances[i]))
                        throw $TypeError(".proto.Account.tokenAllowances: object expected");
                    message.tokenAllowances[i] = $root.proto.AccountFungibleTokenAllowance.fromObject(object.tokenAllowances[i], _depth + 1);
                }
            }
            if (object.numberTreasuryTitles != null)
                if ($Number(object.numberTreasuryTitles) !== 0)
                    message.numberTreasuryTitles = object.numberTreasuryTitles >>> 0;
            if (object.expiredAndPendingRemoval != null)
                if (object.expiredAndPendingRemoval)
                    message.expiredAndPendingRemoval = $Boolean(object.expiredAndPendingRemoval);
            if (object.firstContractStorageKey != null)
                if (object.firstContractStorageKey.length)
                    if (typeof object.firstContractStorageKey === "string")
                        $util.base64.decode(object.firstContractStorageKey, message.firstContractStorageKey = $util.newBuffer($util.base64.length(object.firstContractStorageKey)), 0);
                    else if (object.firstContractStorageKey.length >= 0)
                        message.firstContractStorageKey = object.firstContractStorageKey;
            if (object.headPendingAirdropId != null) {
                if (!$util.isObject(object.headPendingAirdropId))
                    throw $TypeError(".proto.Account.headPendingAirdropId: object expected");
                message.headPendingAirdropId = $root.proto.PendingAirdropId.fromObject(object.headPendingAirdropId, _depth + 1);
            }
            if (object.numberPendingAirdrops != null)
                if (typeof object.numberPendingAirdrops === "object" ? object.numberPendingAirdrops.low || object.numberPendingAirdrops.high : $Number(object.numberPendingAirdrops) !== 0)
                    if ($util.Long)
                        message.numberPendingAirdrops = $util.Long.fromValue(object.numberPendingAirdrops, true);
                    else if (typeof object.numberPendingAirdrops === "string")
                        message.numberPendingAirdrops = $parseInt(object.numberPendingAirdrops, 10);
                    else if (typeof object.numberPendingAirdrops === "number")
                        message.numberPendingAirdrops = object.numberPendingAirdrops;
                    else if (typeof object.numberPendingAirdrops === "object")
                        message.numberPendingAirdrops = new $util.LongBits(object.numberPendingAirdrops.low >>> 0, object.numberPendingAirdrops.high >>> 0).toNumber(true);
            if (object.numberHooksInUse != null)
                if (typeof object.numberHooksInUse === "object" ? object.numberHooksInUse.low || object.numberHooksInUse.high : $Number(object.numberHooksInUse) !== 0)
                    if ($util.Long)
                        message.numberHooksInUse = $util.Long.fromValue(object.numberHooksInUse, true);
                    else if (typeof object.numberHooksInUse === "string")
                        message.numberHooksInUse = $parseInt(object.numberHooksInUse, 10);
                    else if (typeof object.numberHooksInUse === "number")
                        message.numberHooksInUse = object.numberHooksInUse;
                    else if (typeof object.numberHooksInUse === "object")
                        message.numberHooksInUse = new $util.LongBits(object.numberHooksInUse.low >>> 0, object.numberHooksInUse.high >>> 0).toNumber(true);
            if (object.firstHookId != null)
                if (typeof object.firstHookId === "object" ? object.firstHookId.low || object.firstHookId.high : $Number(object.firstHookId) !== 0)
                    if ($util.Long)
                        message.firstHookId = $util.Long.fromValue(object.firstHookId, false);
                    else if (typeof object.firstHookId === "string")
                        message.firstHookId = $parseInt(object.firstHookId, 10);
                    else if (typeof object.firstHookId === "number")
                        message.firstHookId = object.firstHookId;
                    else if (typeof object.firstHookId === "object")
                        message.firstHookId = new $util.LongBits(object.firstHookId.low >>> 0, object.firstHookId.high >>> 0).toNumber();
            if (object.numberEvmHookStorageSlots != null)
                if (typeof object.numberEvmHookStorageSlots === "object" ? object.numberEvmHookStorageSlots.low || object.numberEvmHookStorageSlots.high : $Number(object.numberEvmHookStorageSlots) !== 0)
                    if ($util.Long)
                        message.numberEvmHookStorageSlots = $util.Long.fromValue(object.numberEvmHookStorageSlots, true);
                    else if (typeof object.numberEvmHookStorageSlots === "string")
                        message.numberEvmHookStorageSlots = $parseInt(object.numberEvmHookStorageSlots, 10);
                    else if (typeof object.numberEvmHookStorageSlots === "number")
                        message.numberEvmHookStorageSlots = object.numberEvmHookStorageSlots;
                    else if (typeof object.numberEvmHookStorageSlots === "object")
                        message.numberEvmHookStorageSlots = new $util.LongBits(object.numberEvmHookStorageSlots.low >>> 0, object.numberEvmHookStorageSlots.high >>> 0).toNumber(true);
            if (object.delegationAddress != null)
                if (object.delegationAddress.length)
                    if (typeof object.delegationAddress === "string")
                        $util.base64.decode(object.delegationAddress, message.delegationAddress = $util.newBuffer($util.base64.length(object.delegationAddress)), 0);
                    else if (object.delegationAddress.length >= 0)
                        message.delegationAddress = object.delegationAddress;
            return message;
        };

        /**
         * Creates a plain object from an Account message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Account
         * @static
         * @param {proto.Account} message Account
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Account.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.cryptoAllowances = [];
                object.approveForAllNftAllowances = [];
                object.tokenAllowances = [];
            }
            if (options.defaults) {
                object.accountId = null;
                if (options.bytes === $String)
                    object.alias = "";
                else {
                    object.alias = [];
                    if (options.bytes !== $Array)
                        object.alias = $util.newBuffer(object.alias);
                }
                object.key = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expirationSecond = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expirationSecond = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.tinybarBalance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.tinybarBalance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.memo = "";
                object.deleted = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakedToMe = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakedToMe = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakePeriodStart = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakePeriodStart = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.declineReward = false;
                object.receiverSigRequired = false;
                object.headTokenId = null;
                object.headNftId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.headNftSerialNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.headNftSerialNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.numberOwnedNfts = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.numberOwnedNfts = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.maxAutoAssociations = 0;
                object.usedAutoAssociations = 0;
                object.numberAssociations = 0;
                object.smartContract = false;
                object.numberPositiveBalances = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.ethereumNonce = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.ethereumNonce = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakeAtStartOfLastRewardedPeriod = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakeAtStartOfLastRewardedPeriod = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.autoRenewAccountId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.autoRenewSeconds = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.autoRenewSeconds = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.contractKvPairsNumber = 0;
                object.numberTreasuryTitles = 0;
                object.expiredAndPendingRemoval = false;
                if (options.bytes === $String)
                    object.firstContractStorageKey = "";
                else {
                    object.firstContractStorageKey = [];
                    if (options.bytes !== $Array)
                        object.firstContractStorageKey = $util.newBuffer(object.firstContractStorageKey);
                }
                object.headPendingAirdropId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.numberPendingAirdrops = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.numberPendingAirdrops = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.numberHooksInUse = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.numberHooksInUse = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.firstHookId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.firstHookId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.numberEvmHookStorageSlots = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.numberEvmHookStorageSlots = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.delegationAddress = "";
                else {
                    object.delegationAddress = [];
                    if (options.bytes !== $Array)
                        object.delegationAddress = $util.newBuffer(object.delegationAddress);
                }
            }
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
            if (message.alias != null && $Object.hasOwnProperty.call(message, "alias"))
                object.alias = options.bytes === $String ? $util.base64.encode(message.alias, 0, message.alias.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.alias) : message.alias;
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = $root.proto.Key.toObject(message.key, options, _depth + 1);
            if (message.expirationSecond != null && $Object.hasOwnProperty.call(message, "expirationSecond"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expirationSecond = typeof message.expirationSecond === "number" ? $BigInt(message.expirationSecond) : $util.Long.fromBits(message.expirationSecond.low >>> 0, message.expirationSecond.high >>> 0, false).toBigInt();
                else if (typeof message.expirationSecond === "number")
                    object.expirationSecond = options.longs === $String ? $String(message.expirationSecond) : message.expirationSecond;
                else
                    object.expirationSecond = options.longs === $String ? $util.Long.prototype.toString.call(message.expirationSecond) : options.longs === $Number ? new $util.LongBits(message.expirationSecond.low >>> 0, message.expirationSecond.high >>> 0).toNumber() : message.expirationSecond;
            if (message.tinybarBalance != null && $Object.hasOwnProperty.call(message, "tinybarBalance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.tinybarBalance = typeof message.tinybarBalance === "number" ? $BigInt(message.tinybarBalance) : $util.Long.fromBits(message.tinybarBalance.low >>> 0, message.tinybarBalance.high >>> 0, false).toBigInt();
                else if (typeof message.tinybarBalance === "number")
                    object.tinybarBalance = options.longs === $String ? $String(message.tinybarBalance) : message.tinybarBalance;
                else
                    object.tinybarBalance = options.longs === $String ? $util.Long.prototype.toString.call(message.tinybarBalance) : options.longs === $Number ? new $util.LongBits(message.tinybarBalance.low >>> 0, message.tinybarBalance.high >>> 0).toNumber() : message.tinybarBalance;
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                object.deleted = message.deleted;
            if (message.stakedToMe != null && $Object.hasOwnProperty.call(message, "stakedToMe"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakedToMe = typeof message.stakedToMe === "number" ? $BigInt(message.stakedToMe) : $util.Long.fromBits(message.stakedToMe.low >>> 0, message.stakedToMe.high >>> 0, false).toBigInt();
                else if (typeof message.stakedToMe === "number")
                    object.stakedToMe = options.longs === $String ? $String(message.stakedToMe) : message.stakedToMe;
                else
                    object.stakedToMe = options.longs === $String ? $util.Long.prototype.toString.call(message.stakedToMe) : options.longs === $Number ? new $util.LongBits(message.stakedToMe.low >>> 0, message.stakedToMe.high >>> 0).toNumber() : message.stakedToMe;
            if (message.stakePeriodStart != null && $Object.hasOwnProperty.call(message, "stakePeriodStart"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakePeriodStart = typeof message.stakePeriodStart === "number" ? $BigInt(message.stakePeriodStart) : $util.Long.fromBits(message.stakePeriodStart.low >>> 0, message.stakePeriodStart.high >>> 0, false).toBigInt();
                else if (typeof message.stakePeriodStart === "number")
                    object.stakePeriodStart = options.longs === $String ? $String(message.stakePeriodStart) : message.stakePeriodStart;
                else
                    object.stakePeriodStart = options.longs === $String ? $util.Long.prototype.toString.call(message.stakePeriodStart) : options.longs === $Number ? new $util.LongBits(message.stakePeriodStart.low >>> 0, message.stakePeriodStart.high >>> 0).toNumber() : message.stakePeriodStart;
            if (message.stakedAccountId != null && $Object.hasOwnProperty.call(message, "stakedAccountId")) {
                object.stakedAccountId = $root.proto.AccountID.toObject(message.stakedAccountId, options, _depth + 1);
                if (options.oneofs)
                    object.stakedId = "stakedAccountId";
            }
            if (message.stakedNodeId != null && $Object.hasOwnProperty.call(message, "stakedNodeId")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakedNodeId = typeof message.stakedNodeId === "number" ? $BigInt(message.stakedNodeId) : $util.Long.fromBits(message.stakedNodeId.low >>> 0, message.stakedNodeId.high >>> 0, false).toBigInt();
                else if (typeof message.stakedNodeId === "number")
                    object.stakedNodeId = options.longs === $String ? $String(message.stakedNodeId) : message.stakedNodeId;
                else
                    object.stakedNodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.stakedNodeId) : options.longs === $Number ? new $util.LongBits(message.stakedNodeId.low >>> 0, message.stakedNodeId.high >>> 0).toNumber() : message.stakedNodeId;
                if (options.oneofs)
                    object.stakedId = "stakedNodeId";
            }
            if (message.declineReward != null && $Object.hasOwnProperty.call(message, "declineReward"))
                object.declineReward = message.declineReward;
            if (message.receiverSigRequired != null && $Object.hasOwnProperty.call(message, "receiverSigRequired"))
                object.receiverSigRequired = message.receiverSigRequired;
            if (message.headTokenId != null && $Object.hasOwnProperty.call(message, "headTokenId"))
                object.headTokenId = $root.proto.TokenID.toObject(message.headTokenId, options, _depth + 1);
            if (message.headNftId != null && $Object.hasOwnProperty.call(message, "headNftId"))
                object.headNftId = $root.proto.NftID.toObject(message.headNftId, options, _depth + 1);
            if (message.headNftSerialNumber != null && $Object.hasOwnProperty.call(message, "headNftSerialNumber"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.headNftSerialNumber = typeof message.headNftSerialNumber === "number" ? $BigInt(message.headNftSerialNumber) : $util.Long.fromBits(message.headNftSerialNumber.low >>> 0, message.headNftSerialNumber.high >>> 0, false).toBigInt();
                else if (typeof message.headNftSerialNumber === "number")
                    object.headNftSerialNumber = options.longs === $String ? $String(message.headNftSerialNumber) : message.headNftSerialNumber;
                else
                    object.headNftSerialNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.headNftSerialNumber) : options.longs === $Number ? new $util.LongBits(message.headNftSerialNumber.low >>> 0, message.headNftSerialNumber.high >>> 0).toNumber() : message.headNftSerialNumber;
            if (message.numberOwnedNfts != null && $Object.hasOwnProperty.call(message, "numberOwnedNfts"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.numberOwnedNfts = typeof message.numberOwnedNfts === "number" ? $BigInt(message.numberOwnedNfts) : $util.Long.fromBits(message.numberOwnedNfts.low >>> 0, message.numberOwnedNfts.high >>> 0, false).toBigInt();
                else if (typeof message.numberOwnedNfts === "number")
                    object.numberOwnedNfts = options.longs === $String ? $String(message.numberOwnedNfts) : message.numberOwnedNfts;
                else
                    object.numberOwnedNfts = options.longs === $String ? $util.Long.prototype.toString.call(message.numberOwnedNfts) : options.longs === $Number ? new $util.LongBits(message.numberOwnedNfts.low >>> 0, message.numberOwnedNfts.high >>> 0).toNumber() : message.numberOwnedNfts;
            if (message.maxAutoAssociations != null && $Object.hasOwnProperty.call(message, "maxAutoAssociations"))
                object.maxAutoAssociations = message.maxAutoAssociations;
            if (message.usedAutoAssociations != null && $Object.hasOwnProperty.call(message, "usedAutoAssociations"))
                object.usedAutoAssociations = message.usedAutoAssociations;
            if (message.numberAssociations != null && $Object.hasOwnProperty.call(message, "numberAssociations"))
                object.numberAssociations = message.numberAssociations;
            if (message.smartContract != null && $Object.hasOwnProperty.call(message, "smartContract"))
                object.smartContract = message.smartContract;
            if (message.numberPositiveBalances != null && $Object.hasOwnProperty.call(message, "numberPositiveBalances"))
                object.numberPositiveBalances = message.numberPositiveBalances;
            if (message.ethereumNonce != null && $Object.hasOwnProperty.call(message, "ethereumNonce"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.ethereumNonce = typeof message.ethereumNonce === "number" ? $BigInt(message.ethereumNonce) : $util.Long.fromBits(message.ethereumNonce.low >>> 0, message.ethereumNonce.high >>> 0, false).toBigInt();
                else if (typeof message.ethereumNonce === "number")
                    object.ethereumNonce = options.longs === $String ? $String(message.ethereumNonce) : message.ethereumNonce;
                else
                    object.ethereumNonce = options.longs === $String ? $util.Long.prototype.toString.call(message.ethereumNonce) : options.longs === $Number ? new $util.LongBits(message.ethereumNonce.low >>> 0, message.ethereumNonce.high >>> 0).toNumber() : message.ethereumNonce;
            if (message.stakeAtStartOfLastRewardedPeriod != null && $Object.hasOwnProperty.call(message, "stakeAtStartOfLastRewardedPeriod"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakeAtStartOfLastRewardedPeriod = typeof message.stakeAtStartOfLastRewardedPeriod === "number" ? $BigInt(message.stakeAtStartOfLastRewardedPeriod) : $util.Long.fromBits(message.stakeAtStartOfLastRewardedPeriod.low >>> 0, message.stakeAtStartOfLastRewardedPeriod.high >>> 0, false).toBigInt();
                else if (typeof message.stakeAtStartOfLastRewardedPeriod === "number")
                    object.stakeAtStartOfLastRewardedPeriod = options.longs === $String ? $String(message.stakeAtStartOfLastRewardedPeriod) : message.stakeAtStartOfLastRewardedPeriod;
                else
                    object.stakeAtStartOfLastRewardedPeriod = options.longs === $String ? $util.Long.prototype.toString.call(message.stakeAtStartOfLastRewardedPeriod) : options.longs === $Number ? new $util.LongBits(message.stakeAtStartOfLastRewardedPeriod.low >>> 0, message.stakeAtStartOfLastRewardedPeriod.high >>> 0).toNumber() : message.stakeAtStartOfLastRewardedPeriod;
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId"))
                object.autoRenewAccountId = $root.proto.AccountID.toObject(message.autoRenewAccountId, options, _depth + 1);
            if (message.autoRenewSeconds != null && $Object.hasOwnProperty.call(message, "autoRenewSeconds"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.autoRenewSeconds = typeof message.autoRenewSeconds === "number" ? $BigInt(message.autoRenewSeconds) : $util.Long.fromBits(message.autoRenewSeconds.low >>> 0, message.autoRenewSeconds.high >>> 0, false).toBigInt();
                else if (typeof message.autoRenewSeconds === "number")
                    object.autoRenewSeconds = options.longs === $String ? $String(message.autoRenewSeconds) : message.autoRenewSeconds;
                else
                    object.autoRenewSeconds = options.longs === $String ? $util.Long.prototype.toString.call(message.autoRenewSeconds) : options.longs === $Number ? new $util.LongBits(message.autoRenewSeconds.low >>> 0, message.autoRenewSeconds.high >>> 0).toNumber() : message.autoRenewSeconds;
            if (message.contractKvPairsNumber != null && $Object.hasOwnProperty.call(message, "contractKvPairsNumber"))
                object.contractKvPairsNumber = message.contractKvPairsNumber;
            if (message.cryptoAllowances && message.cryptoAllowances.length) {
                object.cryptoAllowances = $Array(message.cryptoAllowances.length);
                for (let j = 0; j < message.cryptoAllowances.length; ++j)
                    object.cryptoAllowances[j] = $root.proto.AccountCryptoAllowance.toObject(message.cryptoAllowances[j], options, _depth + 1);
            }
            if (message.approveForAllNftAllowances && message.approveForAllNftAllowances.length) {
                object.approveForAllNftAllowances = $Array(message.approveForAllNftAllowances.length);
                for (let j = 0; j < message.approveForAllNftAllowances.length; ++j)
                    object.approveForAllNftAllowances[j] = $root.proto.AccountApprovalForAllAllowance.toObject(message.approveForAllNftAllowances[j], options, _depth + 1);
            }
            if (message.tokenAllowances && message.tokenAllowances.length) {
                object.tokenAllowances = $Array(message.tokenAllowances.length);
                for (let j = 0; j < message.tokenAllowances.length; ++j)
                    object.tokenAllowances[j] = $root.proto.AccountFungibleTokenAllowance.toObject(message.tokenAllowances[j], options, _depth + 1);
            }
            if (message.numberTreasuryTitles != null && $Object.hasOwnProperty.call(message, "numberTreasuryTitles"))
                object.numberTreasuryTitles = message.numberTreasuryTitles;
            if (message.expiredAndPendingRemoval != null && $Object.hasOwnProperty.call(message, "expiredAndPendingRemoval"))
                object.expiredAndPendingRemoval = message.expiredAndPendingRemoval;
            if (message.firstContractStorageKey != null && $Object.hasOwnProperty.call(message, "firstContractStorageKey"))
                object.firstContractStorageKey = options.bytes === $String ? $util.base64.encode(message.firstContractStorageKey, 0, message.firstContractStorageKey.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.firstContractStorageKey) : message.firstContractStorageKey;
            if (message.headPendingAirdropId != null && $Object.hasOwnProperty.call(message, "headPendingAirdropId"))
                object.headPendingAirdropId = $root.proto.PendingAirdropId.toObject(message.headPendingAirdropId, options, _depth + 1);
            if (message.numberPendingAirdrops != null && $Object.hasOwnProperty.call(message, "numberPendingAirdrops"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.numberPendingAirdrops = typeof message.numberPendingAirdrops === "number" ? $BigInt(message.numberPendingAirdrops) : $util.Long.fromBits(message.numberPendingAirdrops.low >>> 0, message.numberPendingAirdrops.high >>> 0, true).toBigInt();
                else if (typeof message.numberPendingAirdrops === "number")
                    object.numberPendingAirdrops = options.longs === $String ? $String(message.numberPendingAirdrops) : message.numberPendingAirdrops;
                else
                    object.numberPendingAirdrops = options.longs === $String ? $util.Long.prototype.toString.call(message.numberPendingAirdrops) : options.longs === $Number ? new $util.LongBits(message.numberPendingAirdrops.low >>> 0, message.numberPendingAirdrops.high >>> 0).toNumber(true) : message.numberPendingAirdrops;
            if (message.numberHooksInUse != null && $Object.hasOwnProperty.call(message, "numberHooksInUse"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.numberHooksInUse = typeof message.numberHooksInUse === "number" ? $BigInt(message.numberHooksInUse) : $util.Long.fromBits(message.numberHooksInUse.low >>> 0, message.numberHooksInUse.high >>> 0, true).toBigInt();
                else if (typeof message.numberHooksInUse === "number")
                    object.numberHooksInUse = options.longs === $String ? $String(message.numberHooksInUse) : message.numberHooksInUse;
                else
                    object.numberHooksInUse = options.longs === $String ? $util.Long.prototype.toString.call(message.numberHooksInUse) : options.longs === $Number ? new $util.LongBits(message.numberHooksInUse.low >>> 0, message.numberHooksInUse.high >>> 0).toNumber(true) : message.numberHooksInUse;
            if (message.firstHookId != null && $Object.hasOwnProperty.call(message, "firstHookId"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.firstHookId = typeof message.firstHookId === "number" ? $BigInt(message.firstHookId) : $util.Long.fromBits(message.firstHookId.low >>> 0, message.firstHookId.high >>> 0, false).toBigInt();
                else if (typeof message.firstHookId === "number")
                    object.firstHookId = options.longs === $String ? $String(message.firstHookId) : message.firstHookId;
                else
                    object.firstHookId = options.longs === $String ? $util.Long.prototype.toString.call(message.firstHookId) : options.longs === $Number ? new $util.LongBits(message.firstHookId.low >>> 0, message.firstHookId.high >>> 0).toNumber() : message.firstHookId;
            if (message.numberEvmHookStorageSlots != null && $Object.hasOwnProperty.call(message, "numberEvmHookStorageSlots"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.numberEvmHookStorageSlots = typeof message.numberEvmHookStorageSlots === "number" ? $BigInt(message.numberEvmHookStorageSlots) : $util.Long.fromBits(message.numberEvmHookStorageSlots.low >>> 0, message.numberEvmHookStorageSlots.high >>> 0, true).toBigInt();
                else if (typeof message.numberEvmHookStorageSlots === "number")
                    object.numberEvmHookStorageSlots = options.longs === $String ? $String(message.numberEvmHookStorageSlots) : message.numberEvmHookStorageSlots;
                else
                    object.numberEvmHookStorageSlots = options.longs === $String ? $util.Long.prototype.toString.call(message.numberEvmHookStorageSlots) : options.longs === $Number ? new $util.LongBits(message.numberEvmHookStorageSlots.low >>> 0, message.numberEvmHookStorageSlots.high >>> 0).toNumber(true) : message.numberEvmHookStorageSlots;
            if (message.delegationAddress != null && $Object.hasOwnProperty.call(message, "delegationAddress"))
                object.delegationAddress = options.bytes === $String ? $util.base64.encode(message.delegationAddress, 0, message.delegationAddress.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.delegationAddress) : message.delegationAddress;
            return object;
        };

        /**
         * Converts this Account to JSON.
         * @function toJSON
         * @memberof proto.Account
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Account.prototype.toJSON = function() {
            return Account.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Account
         * @function getTypeUrl
         * @memberof proto.Account
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Account.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Account";
        };

        return Account;
    })();

    proto.AccountApprovalForAllAllowance = (function() {

        /**
         * Properties of an AccountApprovalForAllAllowance.
         * @typedef {Object} proto.AccountApprovalForAllAllowance.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] The identifier for the token associated with this allowance.
         * <p>
         * This token MUST be a non-fungible/unique token.
         * @property {proto.AccountID.$Properties|null} [spenderId] The identifier for the spending account associated with this allowance.
         * <p>
         * This account SHALL be permitted to sign transactions to spend tokens of
         * the associated token type from the funding/allowing account.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AccountApprovalForAllAllowance.
         * @memberof proto
         * @interface IAccountApprovalForAllAllowance
         * @augments proto.AccountApprovalForAllAllowance.$Properties
         * @deprecated Use proto.AccountApprovalForAllAllowance.$Properties instead.
         */

        /**
         * Shape of an AccountApprovalForAllAllowance.
         * @typedef {{
         *   tokenId?: proto.TokenID.$Shape|null;
         *   spenderId?: proto.AccountID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.AccountApprovalForAllAllowance.$Shape
         */

        /**
         * Constructs a new AccountApprovalForAllAllowance.
         * @memberof proto
         * @classdesc Permission granted by one account (the "funding" account) to another account
         * (the "spender" account) that allows the spender to transfer all serial
         * numbers of a specific non-fungible token (NFT) collection owned by the
         * funding account.<br/>
         * This is a broad permission, as it does not matter how many NFTs of the
         * specified collection the funding account owns, the spender MAY dispose of
         * any or all of them with this allowance.<br/>
         * Each token type (typically a collection of NFTs) SHALL require a separate
         * allowance.<br/>
         * Allowances for a specific serial number MUST be directly associated with
         * that specific non-fungible token, rather than the holding account.
         * 
         * An allowance SHALL NOT transfer any tokens directly, it only permits
         * transactions signed only by the spender account to transfer any non-fungible
         * tokens of the specified type owned by the funding account.
         * @constructor
         * @param {proto.AccountApprovalForAllAllowance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AccountApprovalForAllAllowance = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The identifier for the token associated with this allowance.
         * <p>
         * This token MUST be a non-fungible/unique token.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.AccountApprovalForAllAllowance
         * @instance
         */
        AccountApprovalForAllAllowance.prototype.tokenId = null;

        /**
         * The identifier for the spending account associated with this allowance.
         * <p>
         * This account SHALL be permitted to sign transactions to spend tokens of
         * the associated token type from the funding/allowing account.
         * @member {proto.AccountID.$Properties|null|undefined} spenderId
         * @memberof proto.AccountApprovalForAllAllowance
         * @instance
         */
        AccountApprovalForAllAllowance.prototype.spenderId = null;

        /**
         * Creates a new AccountApprovalForAllAllowance instance using the specified properties.
         * @function create
         * @memberof proto.AccountApprovalForAllAllowance
         * @static
         * @param {proto.AccountApprovalForAllAllowance.$Properties=} [properties] Properties to set
         * @returns {proto.AccountApprovalForAllAllowance} AccountApprovalForAllAllowance instance
         * @type {{
         *   (properties: proto.AccountApprovalForAllAllowance.$Shape): proto.AccountApprovalForAllAllowance & proto.AccountApprovalForAllAllowance.$Shape;
         *   (properties?: proto.AccountApprovalForAllAllowance.$Properties): proto.AccountApprovalForAllAllowance;
         * }}
         */
        AccountApprovalForAllAllowance.create = function(properties) {
            return new AccountApprovalForAllAllowance(properties);
        };

        /**
         * Encodes the specified AccountApprovalForAllAllowance message. Does not implicitly {@link proto.AccountApprovalForAllAllowance.verify|verify} messages.
         * @function encode
         * @memberof proto.AccountApprovalForAllAllowance
         * @static
         * @param {proto.AccountApprovalForAllAllowance.$Properties} message AccountApprovalForAllAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountApprovalForAllAllowance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId"))
                $root.proto.AccountID.encode(message.spenderId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AccountApprovalForAllAllowance message, length delimited. Does not implicitly {@link proto.AccountApprovalForAllAllowance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AccountApprovalForAllAllowance
         * @static
         * @param {proto.AccountApprovalForAllAllowance.$Properties} message AccountApprovalForAllAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountApprovalForAllAllowance.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AccountApprovalForAllAllowance message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AccountApprovalForAllAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AccountApprovalForAllAllowance & proto.AccountApprovalForAllAllowance.$Shape} AccountApprovalForAllAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountApprovalForAllAllowance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.AccountApprovalForAllAllowance();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.spenderId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spenderId);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AccountApprovalForAllAllowance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AccountApprovalForAllAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AccountApprovalForAllAllowance & proto.AccountApprovalForAllAllowance.$Shape} AccountApprovalForAllAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountApprovalForAllAllowance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AccountApprovalForAllAllowance message.
         * @function verify
         * @memberof proto.AccountApprovalForAllAllowance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AccountApprovalForAllAllowance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId")) {
                let error = $root.proto.AccountID.verify(message.spenderId, _depth + 1);
                if (error)
                    return "spenderId." + error;
            }
            return null;
        };

        /**
         * Creates an AccountApprovalForAllAllowance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AccountApprovalForAllAllowance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AccountApprovalForAllAllowance} AccountApprovalForAllAllowance
         */
        AccountApprovalForAllAllowance.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.AccountApprovalForAllAllowance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.AccountApprovalForAllAllowance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.AccountApprovalForAllAllowance();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.AccountApprovalForAllAllowance.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.spenderId != null) {
                if (!$util.isObject(object.spenderId))
                    throw $TypeError(".proto.AccountApprovalForAllAllowance.spenderId: object expected");
                message.spenderId = $root.proto.AccountID.fromObject(object.spenderId, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an AccountApprovalForAllAllowance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AccountApprovalForAllAllowance
         * @static
         * @param {proto.AccountApprovalForAllAllowance} message AccountApprovalForAllAllowance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccountApprovalForAllAllowance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.tokenId = null;
                object.spenderId = null;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId"))
                object.spenderId = $root.proto.AccountID.toObject(message.spenderId, options, _depth + 1);
            return object;
        };

        /**
         * Converts this AccountApprovalForAllAllowance to JSON.
         * @function toJSON
         * @memberof proto.AccountApprovalForAllAllowance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccountApprovalForAllAllowance.prototype.toJSON = function() {
            return AccountApprovalForAllAllowance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AccountApprovalForAllAllowance
         * @function getTypeUrl
         * @memberof proto.AccountApprovalForAllAllowance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AccountApprovalForAllAllowance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AccountApprovalForAllAllowance";
        };

        return AccountApprovalForAllAllowance;
    })();

    proto.AccountFungibleTokenAllowance = (function() {

        /**
         * Properties of an AccountFungibleTokenAllowance.
         * @typedef {Object} proto.AccountFungibleTokenAllowance.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] The identifier for the token associated with this allowance.
         * <p>
         * This token MUST be a fungible/common token.
         * @property {proto.AccountID.$Properties|null} [spenderId] The identifier for the spending account associated with this allowance.
         * <p>
         * This account SHALL be permitted to sign transactions to spend tokens of
         * the associated token type from the funding/allowing account.<br/>
         * This permission SHALL be limited to no more than the specified `amount`.
         * @property {number|Long|null} [amount] AccountFungibleTokenAllowance amount
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AccountFungibleTokenAllowance.
         * @memberof proto
         * @interface IAccountFungibleTokenAllowance
         * @augments proto.AccountFungibleTokenAllowance.$Properties
         * @deprecated Use proto.AccountFungibleTokenAllowance.$Properties instead.
         */

        /**
         * Shape of an AccountFungibleTokenAllowance.
         * @typedef {{
         *   tokenId?: proto.TokenID.$Shape|null;
         *   spenderId?: proto.AccountID.$Shape|null;
         *   amount?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.AccountFungibleTokenAllowance.$Shape
         */

        /**
         * Constructs a new AccountFungibleTokenAllowance.
         * @memberof proto
         * @classdesc Permission granted by one account (the "funding" account) to another account
         * (the "spender" account) that allows the spender to spend a specified amount
         * of a specific non-HBAR fungible token from the balance owned by the funding
         * account.
         * 
         * An allowance SHALL NOT transfer any tokens directly, it only permits
         * transactions signed only by the spender account to transfer tokens of the
         * specified type, up to the amount specified, from the funding account.
         * 
         * Once the specified amount is spent, the allowance SHALL be consumed and a
         * new allowance SHALL be required before that spending account may spend
         * additional tokens from the funding account.
         * @constructor
         * @param {proto.AccountFungibleTokenAllowance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AccountFungibleTokenAllowance = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The identifier for the token associated with this allowance.
         * <p>
         * This token MUST be a fungible/common token.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.AccountFungibleTokenAllowance
         * @instance
         */
        AccountFungibleTokenAllowance.prototype.tokenId = null;

        /**
         * The identifier for the spending account associated with this allowance.
         * <p>
         * This account SHALL be permitted to sign transactions to spend tokens of
         * the associated token type from the funding/allowing account.<br/>
         * This permission SHALL be limited to no more than the specified `amount`.
         * @member {proto.AccountID.$Properties|null|undefined} spenderId
         * @memberof proto.AccountFungibleTokenAllowance
         * @instance
         */
        AccountFungibleTokenAllowance.prototype.spenderId = null;

        /**
         * AccountFungibleTokenAllowance amount.
         * @member {number|Long} amount
         * @memberof proto.AccountFungibleTokenAllowance
         * @instance
         */
        AccountFungibleTokenAllowance.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new AccountFungibleTokenAllowance instance using the specified properties.
         * @function create
         * @memberof proto.AccountFungibleTokenAllowance
         * @static
         * @param {proto.AccountFungibleTokenAllowance.$Properties=} [properties] Properties to set
         * @returns {proto.AccountFungibleTokenAllowance} AccountFungibleTokenAllowance instance
         * @type {{
         *   (properties: proto.AccountFungibleTokenAllowance.$Shape): proto.AccountFungibleTokenAllowance & proto.AccountFungibleTokenAllowance.$Shape;
         *   (properties?: proto.AccountFungibleTokenAllowance.$Properties): proto.AccountFungibleTokenAllowance;
         * }}
         */
        AccountFungibleTokenAllowance.create = function(properties) {
            return new AccountFungibleTokenAllowance(properties);
        };

        /**
         * Encodes the specified AccountFungibleTokenAllowance message. Does not implicitly {@link proto.AccountFungibleTokenAllowance.verify|verify} messages.
         * @function encode
         * @memberof proto.AccountFungibleTokenAllowance
         * @static
         * @param {proto.AccountFungibleTokenAllowance.$Properties} message AccountFungibleTokenAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountFungibleTokenAllowance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId"))
                $root.proto.AccountID.encode(message.spenderId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.amount);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AccountFungibleTokenAllowance message, length delimited. Does not implicitly {@link proto.AccountFungibleTokenAllowance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AccountFungibleTokenAllowance
         * @static
         * @param {proto.AccountFungibleTokenAllowance.$Properties} message AccountFungibleTokenAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountFungibleTokenAllowance.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AccountFungibleTokenAllowance message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AccountFungibleTokenAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AccountFungibleTokenAllowance & proto.AccountFungibleTokenAllowance.$Shape} AccountFungibleTokenAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountFungibleTokenAllowance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.AccountFungibleTokenAllowance();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.spenderId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spenderId);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AccountFungibleTokenAllowance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AccountFungibleTokenAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AccountFungibleTokenAllowance & proto.AccountFungibleTokenAllowance.$Shape} AccountFungibleTokenAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountFungibleTokenAllowance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AccountFungibleTokenAllowance message.
         * @function verify
         * @memberof proto.AccountFungibleTokenAllowance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AccountFungibleTokenAllowance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId")) {
                let error = $root.proto.AccountID.verify(message.spenderId, _depth + 1);
                if (error)
                    return "spenderId." + error;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            return null;
        };

        /**
         * Creates an AccountFungibleTokenAllowance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AccountFungibleTokenAllowance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AccountFungibleTokenAllowance} AccountFungibleTokenAllowance
         */
        AccountFungibleTokenAllowance.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.AccountFungibleTokenAllowance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.AccountFungibleTokenAllowance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.AccountFungibleTokenAllowance();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.AccountFungibleTokenAllowance.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.spenderId != null) {
                if (!$util.isObject(object.spenderId))
                    throw $TypeError(".proto.AccountFungibleTokenAllowance.spenderId: object expected");
                message.spenderId = $root.proto.AccountID.fromObject(object.spenderId, _depth + 1);
            }
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an AccountFungibleTokenAllowance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AccountFungibleTokenAllowance
         * @static
         * @param {proto.AccountFungibleTokenAllowance} message AccountFungibleTokenAllowance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccountFungibleTokenAllowance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.tokenId = null;
                object.spenderId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId"))
                object.spenderId = $root.proto.AccountID.toObject(message.spenderId, options, _depth + 1);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            return object;
        };

        /**
         * Converts this AccountFungibleTokenAllowance to JSON.
         * @function toJSON
         * @memberof proto.AccountFungibleTokenAllowance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccountFungibleTokenAllowance.prototype.toJSON = function() {
            return AccountFungibleTokenAllowance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AccountFungibleTokenAllowance
         * @function getTypeUrl
         * @memberof proto.AccountFungibleTokenAllowance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AccountFungibleTokenAllowance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AccountFungibleTokenAllowance";
        };

        return AccountFungibleTokenAllowance;
    })();

    proto.AccountCryptoAllowance = (function() {

        /**
         * Properties of an AccountCryptoAllowance.
         * @typedef {Object} proto.AccountCryptoAllowance.$Properties
         * @property {proto.AccountID.$Properties|null} [spenderId] The identifier for the spending account associated with this allowance.
         * <p>
         * This account SHALL be permitted to sign transactions to spend HBAR from
         * the funding/allowing account.<br/>
         * This permission SHALL be limited to no more than the specified `amount`.
         * @property {number|Long|null} [amount] AccountCryptoAllowance amount
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AccountCryptoAllowance.
         * @memberof proto
         * @interface IAccountCryptoAllowance
         * @augments proto.AccountCryptoAllowance.$Properties
         * @deprecated Use proto.AccountCryptoAllowance.$Properties instead.
         */

        /**
         * Shape of an AccountCryptoAllowance.
         * @typedef {{
         *   spenderId?: proto.AccountID.$Shape|null;
         *   amount?: number|Long|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.AccountCryptoAllowance.$Shape
         */

        /**
         * Constructs a new AccountCryptoAllowance.
         * @memberof proto
         * @classdesc Permission granted by one account (the "funding" account) to another account
         * (the "spender" account) that allows the spender to spend a specified amount
         * of HBAR owned by the funding account.
         * 
         * An allowance SHALL NOT transfer any HBAR directly, it only permits
         * transactions signed only by the spender account to transfer HBAR, up to the
         * amount specified, from the funding account.
         * 
         * Once the specified amount is spent, the allowance SHALL be consumed and a
         * new allowance SHALL be required before that spending account may spend
         * additional HBAR from the funding account.
         * @constructor
         * @param {proto.AccountCryptoAllowance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AccountCryptoAllowance = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The identifier for the spending account associated with this allowance.
         * <p>
         * This account SHALL be permitted to sign transactions to spend HBAR from
         * the funding/allowing account.<br/>
         * This permission SHALL be limited to no more than the specified `amount`.
         * @member {proto.AccountID.$Properties|null|undefined} spenderId
         * @memberof proto.AccountCryptoAllowance
         * @instance
         */
        AccountCryptoAllowance.prototype.spenderId = null;

        /**
         * AccountCryptoAllowance amount.
         * @member {number|Long} amount
         * @memberof proto.AccountCryptoAllowance
         * @instance
         */
        AccountCryptoAllowance.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new AccountCryptoAllowance instance using the specified properties.
         * @function create
         * @memberof proto.AccountCryptoAllowance
         * @static
         * @param {proto.AccountCryptoAllowance.$Properties=} [properties] Properties to set
         * @returns {proto.AccountCryptoAllowance} AccountCryptoAllowance instance
         * @type {{
         *   (properties: proto.AccountCryptoAllowance.$Shape): proto.AccountCryptoAllowance & proto.AccountCryptoAllowance.$Shape;
         *   (properties?: proto.AccountCryptoAllowance.$Properties): proto.AccountCryptoAllowance;
         * }}
         */
        AccountCryptoAllowance.create = function(properties) {
            return new AccountCryptoAllowance(properties);
        };

        /**
         * Encodes the specified AccountCryptoAllowance message. Does not implicitly {@link proto.AccountCryptoAllowance.verify|verify} messages.
         * @function encode
         * @memberof proto.AccountCryptoAllowance
         * @static
         * @param {proto.AccountCryptoAllowance.$Properties} message AccountCryptoAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountCryptoAllowance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId"))
                $root.proto.AccountID.encode(message.spenderId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount") && (typeof message.amount === "object" ? message.amount.low || message.amount.high : message.amount !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.amount);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AccountCryptoAllowance message, length delimited. Does not implicitly {@link proto.AccountCryptoAllowance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AccountCryptoAllowance
         * @static
         * @param {proto.AccountCryptoAllowance.$Properties} message AccountCryptoAllowance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountCryptoAllowance.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AccountCryptoAllowance message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AccountCryptoAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AccountCryptoAllowance & proto.AccountCryptoAllowance.$Shape} AccountCryptoAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountCryptoAllowance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.AccountCryptoAllowance();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.spenderId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spenderId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.amount = value;
                        else
                            delete message.amount;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AccountCryptoAllowance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AccountCryptoAllowance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AccountCryptoAllowance & proto.AccountCryptoAllowance.$Shape} AccountCryptoAllowance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountCryptoAllowance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AccountCryptoAllowance message.
         * @function verify
         * @memberof proto.AccountCryptoAllowance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AccountCryptoAllowance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId")) {
                let error = $root.proto.AccountID.verify(message.spenderId, _depth + 1);
                if (error)
                    return "spenderId." + error;
            }
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            return null;
        };

        /**
         * Creates an AccountCryptoAllowance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AccountCryptoAllowance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AccountCryptoAllowance} AccountCryptoAllowance
         */
        AccountCryptoAllowance.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.AccountCryptoAllowance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.AccountCryptoAllowance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.AccountCryptoAllowance();
            if (object.spenderId != null) {
                if (!$util.isObject(object.spenderId))
                    throw $TypeError(".proto.AccountCryptoAllowance.spenderId: object expected");
                message.spenderId = $root.proto.AccountID.fromObject(object.spenderId, _depth + 1);
            }
            if (object.amount != null)
                if (typeof object.amount === "object" ? object.amount.low || object.amount.high : $Number(object.amount) !== 0)
                    if ($util.Long)
                        message.amount = $util.Long.fromValue(object.amount, false);
                    else if (typeof object.amount === "string")
                        message.amount = $parseInt(object.amount, 10);
                    else if (typeof object.amount === "number")
                        message.amount = object.amount;
                    else if (typeof object.amount === "object")
                        message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an AccountCryptoAllowance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AccountCryptoAllowance
         * @static
         * @param {proto.AccountCryptoAllowance} message AccountCryptoAllowance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccountCryptoAllowance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.spenderId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.amount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.amount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId"))
                object.spenderId = $root.proto.AccountID.toObject(message.spenderId, options, _depth + 1);
            if (message.amount != null && $Object.hasOwnProperty.call(message, "amount"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.amount = typeof message.amount === "number" ? $BigInt(message.amount) : $util.Long.fromBits(message.amount.low >>> 0, message.amount.high >>> 0, false).toBigInt();
                else if (typeof message.amount === "number")
                    object.amount = options.longs === $String ? $String(message.amount) : message.amount;
                else
                    object.amount = options.longs === $String ? $util.Long.prototype.toString.call(message.amount) : options.longs === $Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber() : message.amount;
            return object;
        };

        /**
         * Converts this AccountCryptoAllowance to JSON.
         * @function toJSON
         * @memberof proto.AccountCryptoAllowance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccountCryptoAllowance.prototype.toJSON = function() {
            return AccountCryptoAllowance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AccountCryptoAllowance
         * @function getTypeUrl
         * @memberof proto.AccountCryptoAllowance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AccountCryptoAllowance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AccountCryptoAllowance";
        };

        return AccountCryptoAllowance;
    })();

    proto.NetworkStakingRewards = (function() {

        /**
         * Properties of a NetworkStakingRewards.
         * @typedef {Object} proto.NetworkStakingRewards.$Properties
         * @property {boolean|null} [stakingRewardsActivated] A flag indicating that staking rewards are activated on the network.
         * <p>
         * Among other criteria, this is set to true when the balance of 0.0.800
         * (the account that pays rewards) reaches a minimum required balance.
         * @property {number|Long|null} [totalStakedRewardStart] A global snapshot of the `stake_to_reward` value for all nodes at the
         * beginning of the current staking period.
         * <p>
         * The `stake_to_reward` value is the sum of balance and `staked_to_me` for
         * all accounts staked to a node that do not decline staking rewards.<br/>
         * This is needed for calculating rewards for current staking period without
         * considering changes to `stake_to_reward` within the current staking period.<br/>
         * This value SHALL be reset at the beginning of every staking period.
         * @property {number|Long|null} [totalStakedStart] A global snapshot of the `stake` value for all nodes at the beginning of
         * the current staking period.
         * <p>
         * The `stake` value is the sum of balance and `staked_to_me` for all
         * accounts staked to a node, and SHALL NOT consider whether the account
         * has accepted or declined rewards.<br/>
         * This value SHALL be reset at the beginning of every staking period.
         * @property {number|Long|null} [pendingRewards] The total staking rewards, in tinybars, that may be collected by all
         * accounts staking to all nodes after the end of this staking period.
         * <p>
         * This SHALL be calculated assuming that no account "renounces" its
         * rewards by setting `decline_reward` to true, or is ineligible for some
         * other reason.<br/>
         * If a node is removed, the `pending_rewards` value of that node SHALL be
         * subtracted from this value.
         * @property {proto.Timestamp.$Properties|null} [lastNodeRewardPaymentsTime] The last time a node reward payment was made. This will be set at the
         * end of a staking period.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NetworkStakingRewards.
         * @memberof proto
         * @interface INetworkStakingRewards
         * @augments proto.NetworkStakingRewards.$Properties
         * @deprecated Use proto.NetworkStakingRewards.$Properties instead.
         */

        /**
         * Shape of a NetworkStakingRewards.
         * @typedef {proto.NetworkStakingRewards.$Properties} proto.NetworkStakingRewards.$Shape
         */

        /**
         * Constructs a new NetworkStakingRewards.
         * @memberof proto
         * @classdesc An Hedera Token Service staking reward entity.
         * This stores values related to the aggregate staking rewards for all nodes in
         * the network. It is calculated at the beginning of each staking period.
         * @constructor
         * @param {proto.NetworkStakingRewards.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NetworkStakingRewards = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A flag indicating that staking rewards are activated on the network.
         * <p>
         * Among other criteria, this is set to true when the balance of 0.0.800
         * (the account that pays rewards) reaches a minimum required balance.
         * @member {boolean} stakingRewardsActivated
         * @memberof proto.NetworkStakingRewards
         * @instance
         */
        NetworkStakingRewards.prototype.stakingRewardsActivated = false;

        /**
         * A global snapshot of the `stake_to_reward` value for all nodes at the
         * beginning of the current staking period.
         * <p>
         * The `stake_to_reward` value is the sum of balance and `staked_to_me` for
         * all accounts staked to a node that do not decline staking rewards.<br/>
         * This is needed for calculating rewards for current staking period without
         * considering changes to `stake_to_reward` within the current staking period.<br/>
         * This value SHALL be reset at the beginning of every staking period.
         * @member {number|Long} totalStakedRewardStart
         * @memberof proto.NetworkStakingRewards
         * @instance
         */
        NetworkStakingRewards.prototype.totalStakedRewardStart = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A global snapshot of the `stake` value for all nodes at the beginning of
         * the current staking period.
         * <p>
         * The `stake` value is the sum of balance and `staked_to_me` for all
         * accounts staked to a node, and SHALL NOT consider whether the account
         * has accepted or declined rewards.<br/>
         * This value SHALL be reset at the beginning of every staking period.
         * @member {number|Long} totalStakedStart
         * @memberof proto.NetworkStakingRewards
         * @instance
         */
        NetworkStakingRewards.prototype.totalStakedStart = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The total staking rewards, in tinybars, that may be collected by all
         * accounts staking to all nodes after the end of this staking period.
         * <p>
         * This SHALL be calculated assuming that no account "renounces" its
         * rewards by setting `decline_reward` to true, or is ineligible for some
         * other reason.<br/>
         * If a node is removed, the `pending_rewards` value of that node SHALL be
         * subtracted from this value.
         * @member {number|Long} pendingRewards
         * @memberof proto.NetworkStakingRewards
         * @instance
         */
        NetworkStakingRewards.prototype.pendingRewards = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The last time a node reward payment was made. This will be set at the
         * end of a staking period.
         * @member {proto.Timestamp.$Properties|null|undefined} lastNodeRewardPaymentsTime
         * @memberof proto.NetworkStakingRewards
         * @instance
         */
        NetworkStakingRewards.prototype.lastNodeRewardPaymentsTime = null;

        /**
         * Creates a new NetworkStakingRewards instance using the specified properties.
         * @function create
         * @memberof proto.NetworkStakingRewards
         * @static
         * @param {proto.NetworkStakingRewards.$Properties=} [properties] Properties to set
         * @returns {proto.NetworkStakingRewards} NetworkStakingRewards instance
         * @type {{
         *   (properties: proto.NetworkStakingRewards.$Shape): proto.NetworkStakingRewards & proto.NetworkStakingRewards.$Shape;
         *   (properties?: proto.NetworkStakingRewards.$Properties): proto.NetworkStakingRewards;
         * }}
         */
        NetworkStakingRewards.create = function(properties) {
            return new NetworkStakingRewards(properties);
        };

        /**
         * Encodes the specified NetworkStakingRewards message. Does not implicitly {@link proto.NetworkStakingRewards.verify|verify} messages.
         * @function encode
         * @memberof proto.NetworkStakingRewards
         * @static
         * @param {proto.NetworkStakingRewards.$Properties} message NetworkStakingRewards message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkStakingRewards.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.stakingRewardsActivated != null && $Object.hasOwnProperty.call(message, "stakingRewardsActivated") && message.stakingRewardsActivated !== false)
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.stakingRewardsActivated);
            if (message.totalStakedRewardStart != null && $Object.hasOwnProperty.call(message, "totalStakedRewardStart") && (typeof message.totalStakedRewardStart === "object" ? message.totalStakedRewardStart.low || message.totalStakedRewardStart.high : message.totalStakedRewardStart !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.totalStakedRewardStart);
            if (message.totalStakedStart != null && $Object.hasOwnProperty.call(message, "totalStakedStart") && (typeof message.totalStakedStart === "object" ? message.totalStakedStart.low || message.totalStakedStart.high : message.totalStakedStart !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.totalStakedStart);
            if (message.pendingRewards != null && $Object.hasOwnProperty.call(message, "pendingRewards") && (typeof message.pendingRewards === "object" ? message.pendingRewards.low || message.pendingRewards.high : message.pendingRewards !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.pendingRewards);
            if (message.lastNodeRewardPaymentsTime != null && $Object.hasOwnProperty.call(message, "lastNodeRewardPaymentsTime"))
                $root.proto.Timestamp.encode(message.lastNodeRewardPaymentsTime, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NetworkStakingRewards message, length delimited. Does not implicitly {@link proto.NetworkStakingRewards.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NetworkStakingRewards
         * @static
         * @param {proto.NetworkStakingRewards.$Properties} message NetworkStakingRewards message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NetworkStakingRewards.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NetworkStakingRewards message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NetworkStakingRewards
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NetworkStakingRewards & proto.NetworkStakingRewards.$Shape} NetworkStakingRewards
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkStakingRewards.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NetworkStakingRewards();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.stakingRewardsActivated = value;
                        else
                            delete message.stakingRewardsActivated;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.totalStakedRewardStart = value;
                        else
                            delete message.totalStakedRewardStart;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.totalStakedStart = value;
                        else
                            delete message.totalStakedStart;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.pendingRewards = value;
                        else
                            delete message.pendingRewards;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.lastNodeRewardPaymentsTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.lastNodeRewardPaymentsTime);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NetworkStakingRewards message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NetworkStakingRewards
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NetworkStakingRewards & proto.NetworkStakingRewards.$Shape} NetworkStakingRewards
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NetworkStakingRewards.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NetworkStakingRewards message.
         * @function verify
         * @memberof proto.NetworkStakingRewards
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NetworkStakingRewards.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.stakingRewardsActivated != null && $Object.hasOwnProperty.call(message, "stakingRewardsActivated"))
                if (typeof message.stakingRewardsActivated !== "boolean")
                    return "stakingRewardsActivated: boolean expected";
            if (message.totalStakedRewardStart != null && $Object.hasOwnProperty.call(message, "totalStakedRewardStart"))
                if (!$util.isInteger(message.totalStakedRewardStart) && !(message.totalStakedRewardStart && $util.isInteger(message.totalStakedRewardStart.low) && $util.isInteger(message.totalStakedRewardStart.high)))
                    return "totalStakedRewardStart: integer|Long expected";
            if (message.totalStakedStart != null && $Object.hasOwnProperty.call(message, "totalStakedStart"))
                if (!$util.isInteger(message.totalStakedStart) && !(message.totalStakedStart && $util.isInteger(message.totalStakedStart.low) && $util.isInteger(message.totalStakedStart.high)))
                    return "totalStakedStart: integer|Long expected";
            if (message.pendingRewards != null && $Object.hasOwnProperty.call(message, "pendingRewards"))
                if (!$util.isInteger(message.pendingRewards) && !(message.pendingRewards && $util.isInteger(message.pendingRewards.low) && $util.isInteger(message.pendingRewards.high)))
                    return "pendingRewards: integer|Long expected";
            if (message.lastNodeRewardPaymentsTime != null && $Object.hasOwnProperty.call(message, "lastNodeRewardPaymentsTime")) {
                let error = $root.proto.Timestamp.verify(message.lastNodeRewardPaymentsTime, _depth + 1);
                if (error)
                    return "lastNodeRewardPaymentsTime." + error;
            }
            return null;
        };

        /**
         * Creates a NetworkStakingRewards message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NetworkStakingRewards
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NetworkStakingRewards} NetworkStakingRewards
         */
        NetworkStakingRewards.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NetworkStakingRewards)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NetworkStakingRewards: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NetworkStakingRewards();
            if (object.stakingRewardsActivated != null)
                if (object.stakingRewardsActivated)
                    message.stakingRewardsActivated = $Boolean(object.stakingRewardsActivated);
            if (object.totalStakedRewardStart != null)
                if (typeof object.totalStakedRewardStart === "object" ? object.totalStakedRewardStart.low || object.totalStakedRewardStart.high : $Number(object.totalStakedRewardStart) !== 0)
                    if ($util.Long)
                        message.totalStakedRewardStart = $util.Long.fromValue(object.totalStakedRewardStart, false);
                    else if (typeof object.totalStakedRewardStart === "string")
                        message.totalStakedRewardStart = $parseInt(object.totalStakedRewardStart, 10);
                    else if (typeof object.totalStakedRewardStart === "number")
                        message.totalStakedRewardStart = object.totalStakedRewardStart;
                    else if (typeof object.totalStakedRewardStart === "object")
                        message.totalStakedRewardStart = new $util.LongBits(object.totalStakedRewardStart.low >>> 0, object.totalStakedRewardStart.high >>> 0).toNumber();
            if (object.totalStakedStart != null)
                if (typeof object.totalStakedStart === "object" ? object.totalStakedStart.low || object.totalStakedStart.high : $Number(object.totalStakedStart) !== 0)
                    if ($util.Long)
                        message.totalStakedStart = $util.Long.fromValue(object.totalStakedStart, false);
                    else if (typeof object.totalStakedStart === "string")
                        message.totalStakedStart = $parseInt(object.totalStakedStart, 10);
                    else if (typeof object.totalStakedStart === "number")
                        message.totalStakedStart = object.totalStakedStart;
                    else if (typeof object.totalStakedStart === "object")
                        message.totalStakedStart = new $util.LongBits(object.totalStakedStart.low >>> 0, object.totalStakedStart.high >>> 0).toNumber();
            if (object.pendingRewards != null)
                if (typeof object.pendingRewards === "object" ? object.pendingRewards.low || object.pendingRewards.high : $Number(object.pendingRewards) !== 0)
                    if ($util.Long)
                        message.pendingRewards = $util.Long.fromValue(object.pendingRewards, false);
                    else if (typeof object.pendingRewards === "string")
                        message.pendingRewards = $parseInt(object.pendingRewards, 10);
                    else if (typeof object.pendingRewards === "number")
                        message.pendingRewards = object.pendingRewards;
                    else if (typeof object.pendingRewards === "object")
                        message.pendingRewards = new $util.LongBits(object.pendingRewards.low >>> 0, object.pendingRewards.high >>> 0).toNumber();
            if (object.lastNodeRewardPaymentsTime != null) {
                if (!$util.isObject(object.lastNodeRewardPaymentsTime))
                    throw $TypeError(".proto.NetworkStakingRewards.lastNodeRewardPaymentsTime: object expected");
                message.lastNodeRewardPaymentsTime = $root.proto.Timestamp.fromObject(object.lastNodeRewardPaymentsTime, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a NetworkStakingRewards message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NetworkStakingRewards
         * @static
         * @param {proto.NetworkStakingRewards} message NetworkStakingRewards
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NetworkStakingRewards.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.stakingRewardsActivated = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.totalStakedRewardStart = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.totalStakedRewardStart = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.totalStakedStart = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.totalStakedStart = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.pendingRewards = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.pendingRewards = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.lastNodeRewardPaymentsTime = null;
            }
            if (message.stakingRewardsActivated != null && $Object.hasOwnProperty.call(message, "stakingRewardsActivated"))
                object.stakingRewardsActivated = message.stakingRewardsActivated;
            if (message.totalStakedRewardStart != null && $Object.hasOwnProperty.call(message, "totalStakedRewardStart"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.totalStakedRewardStart = typeof message.totalStakedRewardStart === "number" ? $BigInt(message.totalStakedRewardStart) : $util.Long.fromBits(message.totalStakedRewardStart.low >>> 0, message.totalStakedRewardStart.high >>> 0, false).toBigInt();
                else if (typeof message.totalStakedRewardStart === "number")
                    object.totalStakedRewardStart = options.longs === $String ? $String(message.totalStakedRewardStart) : message.totalStakedRewardStart;
                else
                    object.totalStakedRewardStart = options.longs === $String ? $util.Long.prototype.toString.call(message.totalStakedRewardStart) : options.longs === $Number ? new $util.LongBits(message.totalStakedRewardStart.low >>> 0, message.totalStakedRewardStart.high >>> 0).toNumber() : message.totalStakedRewardStart;
            if (message.totalStakedStart != null && $Object.hasOwnProperty.call(message, "totalStakedStart"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.totalStakedStart = typeof message.totalStakedStart === "number" ? $BigInt(message.totalStakedStart) : $util.Long.fromBits(message.totalStakedStart.low >>> 0, message.totalStakedStart.high >>> 0, false).toBigInt();
                else if (typeof message.totalStakedStart === "number")
                    object.totalStakedStart = options.longs === $String ? $String(message.totalStakedStart) : message.totalStakedStart;
                else
                    object.totalStakedStart = options.longs === $String ? $util.Long.prototype.toString.call(message.totalStakedStart) : options.longs === $Number ? new $util.LongBits(message.totalStakedStart.low >>> 0, message.totalStakedStart.high >>> 0).toNumber() : message.totalStakedStart;
            if (message.pendingRewards != null && $Object.hasOwnProperty.call(message, "pendingRewards"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.pendingRewards = typeof message.pendingRewards === "number" ? $BigInt(message.pendingRewards) : $util.Long.fromBits(message.pendingRewards.low >>> 0, message.pendingRewards.high >>> 0, false).toBigInt();
                else if (typeof message.pendingRewards === "number")
                    object.pendingRewards = options.longs === $String ? $String(message.pendingRewards) : message.pendingRewards;
                else
                    object.pendingRewards = options.longs === $String ? $util.Long.prototype.toString.call(message.pendingRewards) : options.longs === $Number ? new $util.LongBits(message.pendingRewards.low >>> 0, message.pendingRewards.high >>> 0).toNumber() : message.pendingRewards;
            if (message.lastNodeRewardPaymentsTime != null && $Object.hasOwnProperty.call(message, "lastNodeRewardPaymentsTime"))
                object.lastNodeRewardPaymentsTime = $root.proto.Timestamp.toObject(message.lastNodeRewardPaymentsTime, options, _depth + 1);
            return object;
        };

        /**
         * Converts this NetworkStakingRewards to JSON.
         * @function toJSON
         * @memberof proto.NetworkStakingRewards
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NetworkStakingRewards.prototype.toJSON = function() {
            return NetworkStakingRewards.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NetworkStakingRewards
         * @function getTypeUrl
         * @memberof proto.NetworkStakingRewards
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NetworkStakingRewards.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NetworkStakingRewards";
        };

        return NetworkStakingRewards;
    })();

    proto.Nft = (function() {

        /**
         * Properties of a Nft.
         * @typedef {Object} proto.Nft.$Properties
         * @property {proto.NftID.$Properties|null} [nftId] The id of this NFT, consisting of a Token ID and serial number.
         * @property {proto.AccountID.$Properties|null} [ownerId] The account or contract id that owns this NFT.
         * <p>
         * If this NFT is owned by its token type's current treasury account,
         * this value SHALL be zero.
         * @property {proto.AccountID.$Properties|null} [spenderId] The account or contract id approved to spend this NFT.
         * <p>
         * If there is no approved spender, this value SHALL be null.
         * @property {proto.Timestamp.$Properties|null} [mintTime] The consensus time of the TokenMint that created this NFT as
         * offset from the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.
         * @property {Uint8Array|null} [metadata] The metadata bytes for this NFT. This is often a URI value.
         * <p>
         * This value, if set, SHALL NOT exceed 100 bytes.
         * @property {proto.NftID.$Properties|null} [ownerPreviousNftId] The NFT ID of the previous entry in the current owner's "virtual
         * double-linked list" of owned NFTs.
         * <p>
         * If the owner of this NFT is the token treasury, this SHALL be unset.
         * @property {proto.NftID.$Properties|null} [ownerNextNftId] The NFT ID of the next entry in the current owner's "virtual
         * double-linked list" of owned NFTs.
         * <p>
         * If the owner of this NFT is the token treasury, this SHALL be unset.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Nft.
         * @memberof proto
         * @interface INft
         * @augments proto.Nft.$Properties
         * @deprecated Use proto.Nft.$Properties instead.
         */

        /**
         * Shape of a Nft.
         * @typedef {{
         *   nftId?: proto.NftID.$Shape|null;
         *   ownerId?: proto.AccountID.$Shape|null;
         *   spenderId?: proto.AccountID.$Shape|null;
         *   mintTime?: proto.Timestamp.$Shape|null;
         *   metadata?: Uint8Array|null;
         *   ownerPreviousNftId?: proto.NftID.$Shape|null;
         *   ownerNextNftId?: proto.NftID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.Nft.$Shape
         */

        /**
         * Constructs a new Nft.
         * @memberof proto
         * @classdesc An Hedera Token Service non-fungible token (NFT).<br/>
         * Every NFT is a unique instance of a token with non-fungible type.
         * 
         * The NFT SHALL be identified by token ID and serial number.<br/>
         * The token treasury account SHALL own all minted NFTs of that token type
         * initially.<br/>
         * NFTs owned by the token treasury SHALL NOT be linked into that account's
         * virtual linked list of NFTs.<br/>
         * NFTs not owned by the token treasury SHALL be linked into the owner
         * account's virtual linked list of NFTs.
         * @constructor
         * @param {proto.Nft.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Nft = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The id of this NFT, consisting of a Token ID and serial number.
         * @member {proto.NftID.$Properties|null|undefined} nftId
         * @memberof proto.Nft
         * @instance
         */
        Nft.prototype.nftId = null;

        /**
         * The account or contract id that owns this NFT.
         * <p>
         * If this NFT is owned by its token type's current treasury account,
         * this value SHALL be zero.
         * @member {proto.AccountID.$Properties|null|undefined} ownerId
         * @memberof proto.Nft
         * @instance
         */
        Nft.prototype.ownerId = null;

        /**
         * The account or contract id approved to spend this NFT.
         * <p>
         * If there is no approved spender, this value SHALL be null.
         * @member {proto.AccountID.$Properties|null|undefined} spenderId
         * @memberof proto.Nft
         * @instance
         */
        Nft.prototype.spenderId = null;

        /**
         * The consensus time of the TokenMint that created this NFT as
         * offset from the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.
         * @member {proto.Timestamp.$Properties|null|undefined} mintTime
         * @memberof proto.Nft
         * @instance
         */
        Nft.prototype.mintTime = null;

        /**
         * The metadata bytes for this NFT. This is often a URI value.
         * <p>
         * This value, if set, SHALL NOT exceed 100 bytes.
         * @member {Uint8Array} metadata
         * @memberof proto.Nft
         * @instance
         */
        Nft.prototype.metadata = $util.newBuffer([]);

        /**
         * The NFT ID of the previous entry in the current owner's "virtual
         * double-linked list" of owned NFTs.
         * <p>
         * If the owner of this NFT is the token treasury, this SHALL be unset.
         * @member {proto.NftID.$Properties|null|undefined} ownerPreviousNftId
         * @memberof proto.Nft
         * @instance
         */
        Nft.prototype.ownerPreviousNftId = null;

        /**
         * The NFT ID of the next entry in the current owner's "virtual
         * double-linked list" of owned NFTs.
         * <p>
         * If the owner of this NFT is the token treasury, this SHALL be unset.
         * @member {proto.NftID.$Properties|null|undefined} ownerNextNftId
         * @memberof proto.Nft
         * @instance
         */
        Nft.prototype.ownerNextNftId = null;

        /**
         * Creates a new Nft instance using the specified properties.
         * @function create
         * @memberof proto.Nft
         * @static
         * @param {proto.Nft.$Properties=} [properties] Properties to set
         * @returns {proto.Nft} Nft instance
         * @type {{
         *   (properties: proto.Nft.$Shape): proto.Nft & proto.Nft.$Shape;
         *   (properties?: proto.Nft.$Properties): proto.Nft;
         * }}
         */
        Nft.create = function(properties) {
            return new Nft(properties);
        };

        /**
         * Encodes the specified Nft message. Does not implicitly {@link proto.Nft.verify|verify} messages.
         * @function encode
         * @memberof proto.Nft
         * @static
         * @param {proto.Nft.$Properties} message Nft message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Nft.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nftId != null && $Object.hasOwnProperty.call(message, "nftId"))
                $root.proto.NftID.encode(message.nftId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.ownerId != null && $Object.hasOwnProperty.call(message, "ownerId"))
                $root.proto.AccountID.encode(message.ownerId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId"))
                $root.proto.AccountID.encode(message.spenderId, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.mintTime != null && $Object.hasOwnProperty.call(message, "mintTime"))
                $root.proto.Timestamp.encode(message.mintTime, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata") && message.metadata.length)
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.metadata);
            if (message.ownerPreviousNftId != null && $Object.hasOwnProperty.call(message, "ownerPreviousNftId"))
                $root.proto.NftID.encode(message.ownerPreviousNftId, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.ownerNextNftId != null && $Object.hasOwnProperty.call(message, "ownerNextNftId"))
                $root.proto.NftID.encode(message.ownerNextNftId, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Nft message, length delimited. Does not implicitly {@link proto.Nft.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Nft
         * @static
         * @param {proto.Nft.$Properties} message Nft message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Nft.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Nft message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Nft
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Nft & proto.Nft.$Shape} Nft
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Nft.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Nft();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.nftId = $root.proto.NftID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nftId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.ownerId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ownerId);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.spenderId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.spenderId);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.mintTime = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.mintTime);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.metadata = value;
                        else
                            delete message.metadata;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.ownerPreviousNftId = $root.proto.NftID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ownerPreviousNftId);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.ownerNextNftId = $root.proto.NftID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ownerNextNftId);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Nft message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Nft
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Nft & proto.Nft.$Shape} Nft
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Nft.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Nft message.
         * @function verify
         * @memberof proto.Nft
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Nft.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nftId != null && $Object.hasOwnProperty.call(message, "nftId")) {
                let error = $root.proto.NftID.verify(message.nftId, _depth + 1);
                if (error)
                    return "nftId." + error;
            }
            if (message.ownerId != null && $Object.hasOwnProperty.call(message, "ownerId")) {
                let error = $root.proto.AccountID.verify(message.ownerId, _depth + 1);
                if (error)
                    return "ownerId." + error;
            }
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId")) {
                let error = $root.proto.AccountID.verify(message.spenderId, _depth + 1);
                if (error)
                    return "spenderId." + error;
            }
            if (message.mintTime != null && $Object.hasOwnProperty.call(message, "mintTime")) {
                let error = $root.proto.Timestamp.verify(message.mintTime, _depth + 1);
                if (error)
                    return "mintTime." + error;
            }
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                if (!(message.metadata && typeof message.metadata.length === "number" || $util.isString(message.metadata)))
                    return "metadata: buffer expected";
            if (message.ownerPreviousNftId != null && $Object.hasOwnProperty.call(message, "ownerPreviousNftId")) {
                let error = $root.proto.NftID.verify(message.ownerPreviousNftId, _depth + 1);
                if (error)
                    return "ownerPreviousNftId." + error;
            }
            if (message.ownerNextNftId != null && $Object.hasOwnProperty.call(message, "ownerNextNftId")) {
                let error = $root.proto.NftID.verify(message.ownerNextNftId, _depth + 1);
                if (error)
                    return "ownerNextNftId." + error;
            }
            return null;
        };

        /**
         * Creates a Nft message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Nft
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Nft} Nft
         */
        Nft.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Nft)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Nft: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Nft();
            if (object.nftId != null) {
                if (!$util.isObject(object.nftId))
                    throw $TypeError(".proto.Nft.nftId: object expected");
                message.nftId = $root.proto.NftID.fromObject(object.nftId, _depth + 1);
            }
            if (object.ownerId != null) {
                if (!$util.isObject(object.ownerId))
                    throw $TypeError(".proto.Nft.ownerId: object expected");
                message.ownerId = $root.proto.AccountID.fromObject(object.ownerId, _depth + 1);
            }
            if (object.spenderId != null) {
                if (!$util.isObject(object.spenderId))
                    throw $TypeError(".proto.Nft.spenderId: object expected");
                message.spenderId = $root.proto.AccountID.fromObject(object.spenderId, _depth + 1);
            }
            if (object.mintTime != null) {
                if (!$util.isObject(object.mintTime))
                    throw $TypeError(".proto.Nft.mintTime: object expected");
                message.mintTime = $root.proto.Timestamp.fromObject(object.mintTime, _depth + 1);
            }
            if (object.metadata != null)
                if (object.metadata.length)
                    if (typeof object.metadata === "string")
                        $util.base64.decode(object.metadata, message.metadata = $util.newBuffer($util.base64.length(object.metadata)), 0);
                    else if (object.metadata.length >= 0)
                        message.metadata = object.metadata;
            if (object.ownerPreviousNftId != null) {
                if (!$util.isObject(object.ownerPreviousNftId))
                    throw $TypeError(".proto.Nft.ownerPreviousNftId: object expected");
                message.ownerPreviousNftId = $root.proto.NftID.fromObject(object.ownerPreviousNftId, _depth + 1);
            }
            if (object.ownerNextNftId != null) {
                if (!$util.isObject(object.ownerNextNftId))
                    throw $TypeError(".proto.Nft.ownerNextNftId: object expected");
                message.ownerNextNftId = $root.proto.NftID.fromObject(object.ownerNextNftId, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Nft message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Nft
         * @static
         * @param {proto.Nft} message Nft
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Nft.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.nftId = null;
                object.ownerId = null;
                object.spenderId = null;
                object.mintTime = null;
                if (options.bytes === $String)
                    object.metadata = "";
                else {
                    object.metadata = [];
                    if (options.bytes !== $Array)
                        object.metadata = $util.newBuffer(object.metadata);
                }
                object.ownerPreviousNftId = null;
                object.ownerNextNftId = null;
            }
            if (message.nftId != null && $Object.hasOwnProperty.call(message, "nftId"))
                object.nftId = $root.proto.NftID.toObject(message.nftId, options, _depth + 1);
            if (message.ownerId != null && $Object.hasOwnProperty.call(message, "ownerId"))
                object.ownerId = $root.proto.AccountID.toObject(message.ownerId, options, _depth + 1);
            if (message.spenderId != null && $Object.hasOwnProperty.call(message, "spenderId"))
                object.spenderId = $root.proto.AccountID.toObject(message.spenderId, options, _depth + 1);
            if (message.mintTime != null && $Object.hasOwnProperty.call(message, "mintTime"))
                object.mintTime = $root.proto.Timestamp.toObject(message.mintTime, options, _depth + 1);
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                object.metadata = options.bytes === $String ? $util.base64.encode(message.metadata, 0, message.metadata.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.metadata) : message.metadata;
            if (message.ownerPreviousNftId != null && $Object.hasOwnProperty.call(message, "ownerPreviousNftId"))
                object.ownerPreviousNftId = $root.proto.NftID.toObject(message.ownerPreviousNftId, options, _depth + 1);
            if (message.ownerNextNftId != null && $Object.hasOwnProperty.call(message, "ownerNextNftId"))
                object.ownerNextNftId = $root.proto.NftID.toObject(message.ownerNextNftId, options, _depth + 1);
            return object;
        };

        /**
         * Converts this Nft to JSON.
         * @function toJSON
         * @memberof proto.Nft
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Nft.prototype.toJSON = function() {
            return Nft.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Nft
         * @function getTypeUrl
         * @memberof proto.Nft
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Nft.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Nft";
        };

        return Nft;
    })();

    proto.NodeRewards = (function() {

        /**
         * Properties of a NodeRewards.
         * @typedef {Object} proto.NodeRewards.$Properties
         * @property {number|Long|null} [numRoundsInStakingPeriod] A number of rounds so far, in this staking period.
         * @property {number|Long|null} [nodeFeesCollected] The fees collected by node accounts in this period.
         * @property {Array.<proto.NodeActivity.$Properties>|null} [nodeActivities] A list of node activities.<br/>
         * This records the number of rounds when each node created
         * judges for the consensus algorithm.
         * <p>
         * This list SHALL contain one entry for each node participating
         * in consensus during this staking period.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NodeRewards.
         * @memberof proto
         * @interface INodeRewards
         * @augments proto.NodeRewards.$Properties
         * @deprecated Use proto.NodeRewards.$Properties instead.
         */

        /**
         * Shape of a NodeRewards.
         * @typedef {proto.NodeRewards.$Properties} proto.NodeRewards.$Shape
         */

        /**
         * Constructs a new NodeRewards.
         * @memberof proto
         * @classdesc A record of node rewards status.<br/>
         * This is used to record the number of "active" nodes in a staking
         * period based on number of judges each node created in that period.
         * It also records the number of rounds so far in the staking period.
         * 
         * A Node SHALL be considered "active" if it produced "judges" according
         * to the consensus algorithm in a percentage of rounds, during the
         * staking period, greater than the network configuration value for
         * `nodes.activeRoundsPercent`.
         * @constructor
         * @param {proto.NodeRewards.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NodeRewards = function (properties) {
            this.nodeActivities = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A number of rounds so far, in this staking period.
         * @member {number|Long} numRoundsInStakingPeriod
         * @memberof proto.NodeRewards
         * @instance
         */
        NodeRewards.prototype.numRoundsInStakingPeriod = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * The fees collected by node accounts in this period.
         * @member {number|Long} nodeFeesCollected
         * @memberof proto.NodeRewards
         * @instance
         */
        NodeRewards.prototype.nodeFeesCollected = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A list of node activities.<br/>
         * This records the number of rounds when each node created
         * judges for the consensus algorithm.
         * <p>
         * This list SHALL contain one entry for each node participating
         * in consensus during this staking period.
         * @member {Array.<proto.NodeActivity.$Properties>} nodeActivities
         * @memberof proto.NodeRewards
         * @instance
         */
        NodeRewards.prototype.nodeActivities = $util.emptyArray;

        /**
         * Creates a new NodeRewards instance using the specified properties.
         * @function create
         * @memberof proto.NodeRewards
         * @static
         * @param {proto.NodeRewards.$Properties=} [properties] Properties to set
         * @returns {proto.NodeRewards} NodeRewards instance
         * @type {{
         *   (properties: proto.NodeRewards.$Shape): proto.NodeRewards & proto.NodeRewards.$Shape;
         *   (properties?: proto.NodeRewards.$Properties): proto.NodeRewards;
         * }}
         */
        NodeRewards.create = function(properties) {
            return new NodeRewards(properties);
        };

        /**
         * Encodes the specified NodeRewards message. Does not implicitly {@link proto.NodeRewards.verify|verify} messages.
         * @function encode
         * @memberof proto.NodeRewards
         * @static
         * @param {proto.NodeRewards.$Properties} message NodeRewards message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeRewards.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.numRoundsInStakingPeriod != null && $Object.hasOwnProperty.call(message, "numRoundsInStakingPeriod") && (typeof message.numRoundsInStakingPeriod === "object" ? message.numRoundsInStakingPeriod.low || message.numRoundsInStakingPeriod.high : message.numRoundsInStakingPeriod !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.numRoundsInStakingPeriod);
            if (message.nodeFeesCollected != null && $Object.hasOwnProperty.call(message, "nodeFeesCollected") && (typeof message.nodeFeesCollected === "object" ? message.nodeFeesCollected.low || message.nodeFeesCollected.high : message.nodeFeesCollected !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.nodeFeesCollected);
            if (message.nodeActivities != null && message.nodeActivities.length)
                for (let i = 0; i < message.nodeActivities.length; ++i)
                    $root.proto.NodeActivity.encode(message.nodeActivities[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeRewards message, length delimited. Does not implicitly {@link proto.NodeRewards.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NodeRewards
         * @static
         * @param {proto.NodeRewards.$Properties} message NodeRewards message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeRewards.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NodeRewards message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NodeRewards
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NodeRewards & proto.NodeRewards.$Shape} NodeRewards
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeRewards.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NodeRewards();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.numRoundsInStakingPeriod = value;
                        else
                            delete message.numRoundsInStakingPeriod;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.nodeFeesCollected = value;
                        else
                            delete message.nodeFeesCollected;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.nodeActivities && message.nodeActivities.length))
                            message.nodeActivities = [];
                        message.nodeActivities.push($root.proto.NodeActivity.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeRewards message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NodeRewards
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NodeRewards & proto.NodeRewards.$Shape} NodeRewards
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeRewards.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeRewards message.
         * @function verify
         * @memberof proto.NodeRewards
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeRewards.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.numRoundsInStakingPeriod != null && $Object.hasOwnProperty.call(message, "numRoundsInStakingPeriod"))
                if (!$util.isInteger(message.numRoundsInStakingPeriod) && !(message.numRoundsInStakingPeriod && $util.isInteger(message.numRoundsInStakingPeriod.low) && $util.isInteger(message.numRoundsInStakingPeriod.high)))
                    return "numRoundsInStakingPeriod: integer|Long expected";
            if (message.nodeFeesCollected != null && $Object.hasOwnProperty.call(message, "nodeFeesCollected"))
                if (!$util.isInteger(message.nodeFeesCollected) && !(message.nodeFeesCollected && $util.isInteger(message.nodeFeesCollected.low) && $util.isInteger(message.nodeFeesCollected.high)))
                    return "nodeFeesCollected: integer|Long expected";
            if (message.nodeActivities != null && $Object.hasOwnProperty.call(message, "nodeActivities")) {
                if (!$Array.isArray(message.nodeActivities))
                    return "nodeActivities: array expected";
                for (let i = 0; i < message.nodeActivities.length; ++i) {
                    let error = $root.proto.NodeActivity.verify(message.nodeActivities[i], _depth + 1);
                    if (error)
                        return "nodeActivities." + error;
                }
            }
            return null;
        };

        /**
         * Creates a NodeRewards message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NodeRewards
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NodeRewards} NodeRewards
         */
        NodeRewards.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NodeRewards)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NodeRewards: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NodeRewards();
            if (object.numRoundsInStakingPeriod != null)
                if (typeof object.numRoundsInStakingPeriod === "object" ? object.numRoundsInStakingPeriod.low || object.numRoundsInStakingPeriod.high : $Number(object.numRoundsInStakingPeriod) !== 0)
                    if ($util.Long)
                        message.numRoundsInStakingPeriod = $util.Long.fromValue(object.numRoundsInStakingPeriod, true);
                    else if (typeof object.numRoundsInStakingPeriod === "string")
                        message.numRoundsInStakingPeriod = $parseInt(object.numRoundsInStakingPeriod, 10);
                    else if (typeof object.numRoundsInStakingPeriod === "number")
                        message.numRoundsInStakingPeriod = object.numRoundsInStakingPeriod;
                    else if (typeof object.numRoundsInStakingPeriod === "object")
                        message.numRoundsInStakingPeriod = new $util.LongBits(object.numRoundsInStakingPeriod.low >>> 0, object.numRoundsInStakingPeriod.high >>> 0).toNumber(true);
            if (object.nodeFeesCollected != null)
                if (typeof object.nodeFeesCollected === "object" ? object.nodeFeesCollected.low || object.nodeFeesCollected.high : $Number(object.nodeFeesCollected) !== 0)
                    if ($util.Long)
                        message.nodeFeesCollected = $util.Long.fromValue(object.nodeFeesCollected, true);
                    else if (typeof object.nodeFeesCollected === "string")
                        message.nodeFeesCollected = $parseInt(object.nodeFeesCollected, 10);
                    else if (typeof object.nodeFeesCollected === "number")
                        message.nodeFeesCollected = object.nodeFeesCollected;
                    else if (typeof object.nodeFeesCollected === "object")
                        message.nodeFeesCollected = new $util.LongBits(object.nodeFeesCollected.low >>> 0, object.nodeFeesCollected.high >>> 0).toNumber(true);
            if (object.nodeActivities) {
                if (!$Array.isArray(object.nodeActivities))
                    throw $TypeError(".proto.NodeRewards.nodeActivities: array expected");
                message.nodeActivities = $Array(object.nodeActivities.length);
                for (let i = 0; i < object.nodeActivities.length; ++i) {
                    if (!$util.isObject(object.nodeActivities[i]))
                        throw $TypeError(".proto.NodeRewards.nodeActivities: object expected");
                    message.nodeActivities[i] = $root.proto.NodeActivity.fromObject(object.nodeActivities[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a NodeRewards message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NodeRewards
         * @static
         * @param {proto.NodeRewards} message NodeRewards
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeRewards.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.nodeActivities = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.numRoundsInStakingPeriod = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.numRoundsInStakingPeriod = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.nodeFeesCollected = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nodeFeesCollected = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.numRoundsInStakingPeriod != null && $Object.hasOwnProperty.call(message, "numRoundsInStakingPeriod"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.numRoundsInStakingPeriod = typeof message.numRoundsInStakingPeriod === "number" ? $BigInt(message.numRoundsInStakingPeriod) : $util.Long.fromBits(message.numRoundsInStakingPeriod.low >>> 0, message.numRoundsInStakingPeriod.high >>> 0, true).toBigInt();
                else if (typeof message.numRoundsInStakingPeriod === "number")
                    object.numRoundsInStakingPeriod = options.longs === $String ? $String(message.numRoundsInStakingPeriod) : message.numRoundsInStakingPeriod;
                else
                    object.numRoundsInStakingPeriod = options.longs === $String ? $util.Long.prototype.toString.call(message.numRoundsInStakingPeriod) : options.longs === $Number ? new $util.LongBits(message.numRoundsInStakingPeriod.low >>> 0, message.numRoundsInStakingPeriod.high >>> 0).toNumber(true) : message.numRoundsInStakingPeriod;
            if (message.nodeFeesCollected != null && $Object.hasOwnProperty.call(message, "nodeFeesCollected"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nodeFeesCollected = typeof message.nodeFeesCollected === "number" ? $BigInt(message.nodeFeesCollected) : $util.Long.fromBits(message.nodeFeesCollected.low >>> 0, message.nodeFeesCollected.high >>> 0, true).toBigInt();
                else if (typeof message.nodeFeesCollected === "number")
                    object.nodeFeesCollected = options.longs === $String ? $String(message.nodeFeesCollected) : message.nodeFeesCollected;
                else
                    object.nodeFeesCollected = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeFeesCollected) : options.longs === $Number ? new $util.LongBits(message.nodeFeesCollected.low >>> 0, message.nodeFeesCollected.high >>> 0).toNumber(true) : message.nodeFeesCollected;
            if (message.nodeActivities && message.nodeActivities.length) {
                object.nodeActivities = $Array(message.nodeActivities.length);
                for (let j = 0; j < message.nodeActivities.length; ++j)
                    object.nodeActivities[j] = $root.proto.NodeActivity.toObject(message.nodeActivities[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this NodeRewards to JSON.
         * @function toJSON
         * @memberof proto.NodeRewards
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeRewards.prototype.toJSON = function() {
            return NodeRewards.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeRewards
         * @function getTypeUrl
         * @memberof proto.NodeRewards
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeRewards.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NodeRewards";
        };

        return NodeRewards;
    })();

    proto.NodeActivity = (function() {

        /**
         * Properties of a NodeActivity.
         * @typedef {Object} proto.NodeActivity.$Properties
         * @property {number|Long|null} [nodeId] A node identifier.
         * @property {number|Long|null} [numMissedJudgeRounds] A count of rounds.<br/>
         * This is the count of rounds so far, in this staking period in which the node identified
         * by `node_id` did not create judges.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a NodeActivity.
         * @memberof proto
         * @interface INodeActivity
         * @augments proto.NodeActivity.$Properties
         * @deprecated Use proto.NodeActivity.$Properties instead.
         */

        /**
         * Shape of a NodeActivity.
         * @typedef {proto.NodeActivity.$Properties} proto.NodeActivity.$Shape
         */

        /**
         * Constructs a new NodeActivity.
         * @memberof proto
         * @classdesc A record of judge rounds missed by a single node.<br/>
         * This records, for a single node, the number of rounds so far, during this staking
         * period that missed creating judges. This is used to determine if the node is
         * "active" or not.
         * 
         * This message SHALL NOT record the total number of rounds in a staking
         * period.<br/>
         * This message SHALL record a count of rounds for a single node that missed creating judges.
         * @constructor
         * @param {proto.NodeActivity.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const NodeActivity = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A node identifier.
         * @member {number|Long} nodeId
         * @memberof proto.NodeActivity
         * @instance
         */
        NodeActivity.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A count of rounds.<br/>
         * This is the count of rounds so far, in this staking period in which the node identified
         * by `node_id` did not create judges.
         * @member {number|Long} numMissedJudgeRounds
         * @memberof proto.NodeActivity
         * @instance
         */
        NodeActivity.prototype.numMissedJudgeRounds = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new NodeActivity instance using the specified properties.
         * @function create
         * @memberof proto.NodeActivity
         * @static
         * @param {proto.NodeActivity.$Properties=} [properties] Properties to set
         * @returns {proto.NodeActivity} NodeActivity instance
         * @type {{
         *   (properties: proto.NodeActivity.$Shape): proto.NodeActivity & proto.NodeActivity.$Shape;
         *   (properties?: proto.NodeActivity.$Properties): proto.NodeActivity;
         * }}
         */
        NodeActivity.create = function(properties) {
            return new NodeActivity(properties);
        };

        /**
         * Encodes the specified NodeActivity message. Does not implicitly {@link proto.NodeActivity.verify|verify} messages.
         * @function encode
         * @memberof proto.NodeActivity
         * @static
         * @param {proto.NodeActivity.$Properties} message NodeActivity message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeActivity.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId") && (typeof message.nodeId === "object" ? message.nodeId.low || message.nodeId.high : message.nodeId !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.nodeId);
            if (message.numMissedJudgeRounds != null && $Object.hasOwnProperty.call(message, "numMissedJudgeRounds") && (typeof message.numMissedJudgeRounds === "object" ? message.numMissedJudgeRounds.low || message.numMissedJudgeRounds.high : message.numMissedJudgeRounds !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.numMissedJudgeRounds);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NodeActivity message, length delimited. Does not implicitly {@link proto.NodeActivity.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NodeActivity
         * @static
         * @param {proto.NodeActivity.$Properties} message NodeActivity message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeActivity.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a NodeActivity message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NodeActivity
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NodeActivity & proto.NodeActivity.$Shape} NodeActivity
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeActivity.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.NodeActivity();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.nodeId = value;
                        else
                            delete message.nodeId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.numMissedJudgeRounds = value;
                        else
                            delete message.numMissedJudgeRounds;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a NodeActivity message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NodeActivity
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NodeActivity & proto.NodeActivity.$Shape} NodeActivity
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeActivity.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeActivity message.
         * @function verify
         * @memberof proto.NodeActivity
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeActivity.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                    return "nodeId: integer|Long expected";
            if (message.numMissedJudgeRounds != null && $Object.hasOwnProperty.call(message, "numMissedJudgeRounds"))
                if (!$util.isInteger(message.numMissedJudgeRounds) && !(message.numMissedJudgeRounds && $util.isInteger(message.numMissedJudgeRounds.low) && $util.isInteger(message.numMissedJudgeRounds.high)))
                    return "numMissedJudgeRounds: integer|Long expected";
            return null;
        };

        /**
         * Creates a NodeActivity message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NodeActivity
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NodeActivity} NodeActivity
         */
        NodeActivity.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.NodeActivity)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.NodeActivity: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.NodeActivity();
            if (object.nodeId != null)
                if (typeof object.nodeId === "object" ? object.nodeId.low || object.nodeId.high : $Number(object.nodeId) !== 0)
                    if ($util.Long)
                        message.nodeId = $util.Long.fromValue(object.nodeId, true);
                    else if (typeof object.nodeId === "string")
                        message.nodeId = $parseInt(object.nodeId, 10);
                    else if (typeof object.nodeId === "number")
                        message.nodeId = object.nodeId;
                    else if (typeof object.nodeId === "object")
                        message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber(true);
            if (object.numMissedJudgeRounds != null)
                if (typeof object.numMissedJudgeRounds === "object" ? object.numMissedJudgeRounds.low || object.numMissedJudgeRounds.high : $Number(object.numMissedJudgeRounds) !== 0)
                    if ($util.Long)
                        message.numMissedJudgeRounds = $util.Long.fromValue(object.numMissedJudgeRounds, true);
                    else if (typeof object.numMissedJudgeRounds === "string")
                        message.numMissedJudgeRounds = $parseInt(object.numMissedJudgeRounds, 10);
                    else if (typeof object.numMissedJudgeRounds === "number")
                        message.numMissedJudgeRounds = object.numMissedJudgeRounds;
                    else if (typeof object.numMissedJudgeRounds === "object")
                        message.numMissedJudgeRounds = new $util.LongBits(object.numMissedJudgeRounds.low >>> 0, object.numMissedJudgeRounds.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a NodeActivity message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NodeActivity
         * @static
         * @param {proto.NodeActivity} message NodeActivity
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeActivity.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.nodeId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nodeId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.numMissedJudgeRounds = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.numMissedJudgeRounds = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.nodeId != null && $Object.hasOwnProperty.call(message, "nodeId"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nodeId = typeof message.nodeId === "number" ? $BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, true).toBigInt();
                else if (typeof message.nodeId === "number")
                    object.nodeId = options.longs === $String ? $String(message.nodeId) : message.nodeId;
                else
                    object.nodeId = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === $Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber(true) : message.nodeId;
            if (message.numMissedJudgeRounds != null && $Object.hasOwnProperty.call(message, "numMissedJudgeRounds"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.numMissedJudgeRounds = typeof message.numMissedJudgeRounds === "number" ? $BigInt(message.numMissedJudgeRounds) : $util.Long.fromBits(message.numMissedJudgeRounds.low >>> 0, message.numMissedJudgeRounds.high >>> 0, true).toBigInt();
                else if (typeof message.numMissedJudgeRounds === "number")
                    object.numMissedJudgeRounds = options.longs === $String ? $String(message.numMissedJudgeRounds) : message.numMissedJudgeRounds;
                else
                    object.numMissedJudgeRounds = options.longs === $String ? $util.Long.prototype.toString.call(message.numMissedJudgeRounds) : options.longs === $Number ? new $util.LongBits(message.numMissedJudgeRounds.low >>> 0, message.numMissedJudgeRounds.high >>> 0).toNumber(true) : message.numMissedJudgeRounds;
            return object;
        };

        /**
         * Converts this NodeActivity to JSON.
         * @function toJSON
         * @memberof proto.NodeActivity
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeActivity.prototype.toJSON = function() {
            return NodeActivity.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for NodeActivity
         * @function getTypeUrl
         * @memberof proto.NodeActivity
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        NodeActivity.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NodeActivity";
        };

        return NodeActivity;
    })();

    proto.StakingNodeInfo = (function() {

        /**
         * Properties of a StakingNodeInfo.
         * @typedef {Object} proto.StakingNodeInfo.$Properties
         * @property {number|Long|null} [nodeNumber] The entity number of this node.<br/>
         * The shard and realm numbers are implied, based on the network address
         * book entry for this node.
         * <p>
         * This value SHALL be unique within a given shard and realm.<br/>
         * This value MAY be repeated across shards and/or realms.
         * @property {number|Long|null} [minStake] The minimum stake, in tinybar, that is required for this node to have a
         * non-zero weight in the network consensus.
         * <p>
         * If the current value of `stake` is below this value, this node SHALL have
         * a zero weight in network consensus.
         * @property {number|Long|null} [maxStake] The maximum stake to this node that is considered to calculate its weight
         * in the network consensus.
         * <p>
         * If the current `stake` value is above this limit, the excess staked HBAR
         * SHALL NOT be considered when determining consensus weight.
         * @property {number|Long|null} [stakeToReward] The sum of balances of all accounts staked to this node, considering only
         * accounts that choose to receive rewards.
         * @property {number|Long|null} [stakeToNotReward] The sum of balances of all accounts staked to this node, considering only
         * accounts that decline to receive rewards.
         * @property {number|Long|null} [stakeRewardStart] The snapshot of stake_to_reward value at the beginning of the current
         * staking period.
         * <p>
         * This is necessary when calculating rewards for the current staking period
         * without considering changes to `stake_to_reward` _within_ the current
         * staking period.<br/>
         * This value SHALL be reset at the beginning of every staking period.
         * @property {number|Long|null} [unclaimedStakeRewardStart] The amount of staked HBAR from `stake_reward_start` that will have
         * unclaimed rewards due to accounts changing their staking metadata in a
         * way that disqualifies them for the current staking period.
         * <p>
         * This value SHALL be reset at the beginning of every staking period.
         * @property {number|Long|null} [stake] The total amount of HBAR staked to this node.
         * <p>
         * This is sum of stake_to_reward and stake_to_not_reward.<br/>
         * If the sum is greater than `max_stake`, then the _effective_ stake
         * SHALL be `max_stake`.<br/>
         * If the sum is less than `min_stake`, then the _effective_ stake
         * SHALL be `0`.
         * @property {Array.<number|Long>|null} [rewardSumHistory] A running list of reward amounts for the last 365+1 staking periods
         * (typically a year and a day).
         * <p>
         * The first element SHALL be the reward up to and including the last full
         * period prior to the present reward period.<br/>
         * The second element SHALL be the reward up to and including the period
         * before the last full period prior to the present period.<br/>
         * The list SHALL continue in reverse chronological order until the reward
         * history limit is reached.
         * @property {number|null} [weight] The consensus weight of this node in the network.
         * <p>
         * This is recomputed based on the `stake` of this node at midnight UTC of
         * each day. If the `stake` of this node at that time is less than
         * `min_stake`, then the weight SHALL be 0.<br/>
         * <p>
         * Given the following:
         * <ul>
         * <li>The `effective stake` of a single node SHALL be `0` if the node
         * `stake` is less than `min_stake`.</li>
         * <li>The `effective stake` SHALL be `max_stake` if the node `stake` is
         * greater than `max_stake`.</li>
         * <li>The `effective stake` SHALL be the actual value of `stake` if
         * `min_stake` < `stake` < `max_stake`.</li>
         * <li>The `effective network stake` SHALL be calculated as ∑(`effective
         * stake` of each node) for all nodes in the network address book.</li>
         * </ul>
         * <p>
         * This field is deprecated and SHALL NOT be used when RosterLifecycle
         * is enabled. The weight SHALL be same as the `effective_stake` described above.
         * @property {number|Long|null} [pendingRewards] The total staking rewards in tinybars that MAY be collected by all
         * accounts staking to the current node after the end of this staking
         * period.
         * <p>
         * This SHALL be calculated assuming that no account "renounces" its
         * rewards by setting `decline_reward` to true, or is ineligible for
         * some other reason.<br/>
         * When the current node is deleted, this amount SHALL be subtracted from
         * the total pending rewards of all accounts staking to all nodes in the
         * network in NetworkStakingRewards.
         * @property {boolean|null} [deleted] A flag indicating that this node has been removed from this network.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a StakingNodeInfo.
         * @memberof proto
         * @interface IStakingNodeInfo
         * @augments proto.StakingNodeInfo.$Properties
         * @deprecated Use proto.StakingNodeInfo.$Properties instead.
         */

        /**
         * Shape of a StakingNodeInfo.
         * @typedef {proto.StakingNodeInfo.$Properties} proto.StakingNodeInfo.$Shape
         */

        /**
         * Constructs a new StakingNodeInfo.
         * @memberof proto
         * @classdesc An Hedera Token Service staking info entity.
         * 
         * Staking info is per node. Shard and Realm are implied based on the network
         * address book entry for this node.
         * @constructor
         * @param {proto.StakingNodeInfo.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const StakingNodeInfo = function (properties) {
            this.rewardSumHistory = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The entity number of this node.<br/>
         * The shard and realm numbers are implied, based on the network address
         * book entry for this node.
         * <p>
         * This value SHALL be unique within a given shard and realm.<br/>
         * This value MAY be repeated across shards and/or realms.
         * @member {number|Long} nodeNumber
         * @memberof proto.StakingNodeInfo
         * @instance
         */
        StakingNodeInfo.prototype.nodeNumber = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The minimum stake, in tinybar, that is required for this node to have a
         * non-zero weight in the network consensus.
         * <p>
         * If the current value of `stake` is below this value, this node SHALL have
         * a zero weight in network consensus.
         * @member {number|Long} minStake
         * @memberof proto.StakingNodeInfo
         * @instance
         */
        StakingNodeInfo.prototype.minStake = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The maximum stake to this node that is considered to calculate its weight
         * in the network consensus.
         * <p>
         * If the current `stake` value is above this limit, the excess staked HBAR
         * SHALL NOT be considered when determining consensus weight.
         * @member {number|Long} maxStake
         * @memberof proto.StakingNodeInfo
         * @instance
         */
        StakingNodeInfo.prototype.maxStake = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The sum of balances of all accounts staked to this node, considering only
         * accounts that choose to receive rewards.
         * @member {number|Long} stakeToReward
         * @memberof proto.StakingNodeInfo
         * @instance
         */
        StakingNodeInfo.prototype.stakeToReward = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The sum of balances of all accounts staked to this node, considering only
         * accounts that decline to receive rewards.
         * @member {number|Long} stakeToNotReward
         * @memberof proto.StakingNodeInfo
         * @instance
         */
        StakingNodeInfo.prototype.stakeToNotReward = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The snapshot of stake_to_reward value at the beginning of the current
         * staking period.
         * <p>
         * This is necessary when calculating rewards for the current staking period
         * without considering changes to `stake_to_reward` _within_ the current
         * staking period.<br/>
         * This value SHALL be reset at the beginning of every staking period.
         * @member {number|Long} stakeRewardStart
         * @memberof proto.StakingNodeInfo
         * @instance
         */
        StakingNodeInfo.prototype.stakeRewardStart = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The amount of staked HBAR from `stake_reward_start` that will have
         * unclaimed rewards due to accounts changing their staking metadata in a
         * way that disqualifies them for the current staking period.
         * <p>
         * This value SHALL be reset at the beginning of every staking period.
         * @member {number|Long} unclaimedStakeRewardStart
         * @memberof proto.StakingNodeInfo
         * @instance
         */
        StakingNodeInfo.prototype.unclaimedStakeRewardStart = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The total amount of HBAR staked to this node.
         * <p>
         * This is sum of stake_to_reward and stake_to_not_reward.<br/>
         * If the sum is greater than `max_stake`, then the _effective_ stake
         * SHALL be `max_stake`.<br/>
         * If the sum is less than `min_stake`, then the _effective_ stake
         * SHALL be `0`.
         * @member {number|Long} stake
         * @memberof proto.StakingNodeInfo
         * @instance
         */
        StakingNodeInfo.prototype.stake = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A running list of reward amounts for the last 365+1 staking periods
         * (typically a year and a day).
         * <p>
         * The first element SHALL be the reward up to and including the last full
         * period prior to the present reward period.<br/>
         * The second element SHALL be the reward up to and including the period
         * before the last full period prior to the present period.<br/>
         * The list SHALL continue in reverse chronological order until the reward
         * history limit is reached.
         * @member {Array.<number|Long>} rewardSumHistory
         * @memberof proto.StakingNodeInfo
         * @instance
         */
        StakingNodeInfo.prototype.rewardSumHistory = $util.emptyArray;

        /**
         * The consensus weight of this node in the network.
         * <p>
         * This is recomputed based on the `stake` of this node at midnight UTC of
         * each day. If the `stake` of this node at that time is less than
         * `min_stake`, then the weight SHALL be 0.<br/>
         * <p>
         * Given the following:
         * <ul>
         * <li>The `effective stake` of a single node SHALL be `0` if the node
         * `stake` is less than `min_stake`.</li>
         * <li>The `effective stake` SHALL be `max_stake` if the node `stake` is
         * greater than `max_stake`.</li>
         * <li>The `effective stake` SHALL be the actual value of `stake` if
         * `min_stake` < `stake` < `max_stake`.</li>
         * <li>The `effective network stake` SHALL be calculated as ∑(`effective
         * stake` of each node) for all nodes in the network address book.</li>
         * </ul>
         * <p>
         * This field is deprecated and SHALL NOT be used when RosterLifecycle
         * is enabled. The weight SHALL be same as the `effective_stake` described above.
         * @member {number} weight
         * @memberof proto.StakingNodeInfo
         * @instance
         */
        StakingNodeInfo.prototype.weight = 0;

        /**
         * The total staking rewards in tinybars that MAY be collected by all
         * accounts staking to the current node after the end of this staking
         * period.
         * <p>
         * This SHALL be calculated assuming that no account "renounces" its
         * rewards by setting `decline_reward` to true, or is ineligible for
         * some other reason.<br/>
         * When the current node is deleted, this amount SHALL be subtracted from
         * the total pending rewards of all accounts staking to all nodes in the
         * network in NetworkStakingRewards.
         * @member {number|Long} pendingRewards
         * @memberof proto.StakingNodeInfo
         * @instance
         */
        StakingNodeInfo.prototype.pendingRewards = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A flag indicating that this node has been removed from this network.
         * @member {boolean} deleted
         * @memberof proto.StakingNodeInfo
         * @instance
         */
        StakingNodeInfo.prototype.deleted = false;

        /**
         * Creates a new StakingNodeInfo instance using the specified properties.
         * @function create
         * @memberof proto.StakingNodeInfo
         * @static
         * @param {proto.StakingNodeInfo.$Properties=} [properties] Properties to set
         * @returns {proto.StakingNodeInfo} StakingNodeInfo instance
         * @type {{
         *   (properties: proto.StakingNodeInfo.$Shape): proto.StakingNodeInfo & proto.StakingNodeInfo.$Shape;
         *   (properties?: proto.StakingNodeInfo.$Properties): proto.StakingNodeInfo;
         * }}
         */
        StakingNodeInfo.create = function(properties) {
            return new StakingNodeInfo(properties);
        };

        /**
         * Encodes the specified StakingNodeInfo message. Does not implicitly {@link proto.StakingNodeInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.StakingNodeInfo
         * @static
         * @param {proto.StakingNodeInfo.$Properties} message StakingNodeInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StakingNodeInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.nodeNumber != null && $Object.hasOwnProperty.call(message, "nodeNumber") && (typeof message.nodeNumber === "object" ? message.nodeNumber.low || message.nodeNumber.high : message.nodeNumber !== 0))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.nodeNumber);
            if (message.minStake != null && $Object.hasOwnProperty.call(message, "minStake") && (typeof message.minStake === "object" ? message.minStake.low || message.minStake.high : message.minStake !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.minStake);
            if (message.maxStake != null && $Object.hasOwnProperty.call(message, "maxStake") && (typeof message.maxStake === "object" ? message.maxStake.low || message.maxStake.high : message.maxStake !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.maxStake);
            if (message.stakeToReward != null && $Object.hasOwnProperty.call(message, "stakeToReward") && (typeof message.stakeToReward === "object" ? message.stakeToReward.low || message.stakeToReward.high : message.stakeToReward !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.stakeToReward);
            if (message.stakeToNotReward != null && $Object.hasOwnProperty.call(message, "stakeToNotReward") && (typeof message.stakeToNotReward === "object" ? message.stakeToNotReward.low || message.stakeToNotReward.high : message.stakeToNotReward !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.stakeToNotReward);
            if (message.stakeRewardStart != null && $Object.hasOwnProperty.call(message, "stakeRewardStart") && (typeof message.stakeRewardStart === "object" ? message.stakeRewardStart.low || message.stakeRewardStart.high : message.stakeRewardStart !== 0))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.stakeRewardStart);
            if (message.unclaimedStakeRewardStart != null && $Object.hasOwnProperty.call(message, "unclaimedStakeRewardStart") && (typeof message.unclaimedStakeRewardStart === "object" ? message.unclaimedStakeRewardStart.low || message.unclaimedStakeRewardStart.high : message.unclaimedStakeRewardStart !== 0))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.unclaimedStakeRewardStart);
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake") && (typeof message.stake === "object" ? message.stake.low || message.stake.high : message.stake !== 0))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.stake);
            if (message.rewardSumHistory != null && message.rewardSumHistory.length)
                writer.uint32(/* id 9, wireType 2 =*/74).int64s(message.rewardSumHistory);
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight") && message.weight !== 0)
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.weight);
            if (message.pendingRewards != null && $Object.hasOwnProperty.call(message, "pendingRewards") && (typeof message.pendingRewards === "object" ? message.pendingRewards.low || message.pendingRewards.high : message.pendingRewards !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.pendingRewards);
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.deleted);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StakingNodeInfo message, length delimited. Does not implicitly {@link proto.StakingNodeInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.StakingNodeInfo
         * @static
         * @param {proto.StakingNodeInfo.$Properties} message StakingNodeInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StakingNodeInfo.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a StakingNodeInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.StakingNodeInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.StakingNodeInfo & proto.StakingNodeInfo.$Shape} StakingNodeInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StakingNodeInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.StakingNodeInfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.nodeNumber = value;
                        else
                            delete message.nodeNumber;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.minStake = value;
                        else
                            delete message.minStake;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maxStake = value;
                        else
                            delete message.maxStake;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakeToReward = value;
                        else
                            delete message.stakeToReward;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakeToNotReward = value;
                        else
                            delete message.stakeToNotReward;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stakeRewardStart = value;
                        else
                            delete message.stakeRewardStart;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.unclaimedStakeRewardStart = value;
                        else
                            delete message.unclaimedStakeRewardStart;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.stake = value;
                        else
                            delete message.stake;
                        continue;
                    }
                case 9: {
                        if (wireType === 2) {
                            if (!(message.rewardSumHistory && message.rewardSumHistory.length))
                                message.rewardSumHistory = [];
                            reader.int64s(message.rewardSumHistory);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewardSumHistory && message.rewardSumHistory.length))
                            message.rewardSumHistory = [];
                        message.rewardSumHistory.push(reader.int64());
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.weight = value;
                        else
                            delete message.weight;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.pendingRewards = value;
                        else
                            delete message.pendingRewards;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.deleted = value;
                        else
                            delete message.deleted;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StakingNodeInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.StakingNodeInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.StakingNodeInfo & proto.StakingNodeInfo.$Shape} StakingNodeInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StakingNodeInfo.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StakingNodeInfo message.
         * @function verify
         * @memberof proto.StakingNodeInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StakingNodeInfo.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nodeNumber != null && $Object.hasOwnProperty.call(message, "nodeNumber"))
                if (!$util.isInteger(message.nodeNumber) && !(message.nodeNumber && $util.isInteger(message.nodeNumber.low) && $util.isInteger(message.nodeNumber.high)))
                    return "nodeNumber: integer|Long expected";
            if (message.minStake != null && $Object.hasOwnProperty.call(message, "minStake"))
                if (!$util.isInteger(message.minStake) && !(message.minStake && $util.isInteger(message.minStake.low) && $util.isInteger(message.minStake.high)))
                    return "minStake: integer|Long expected";
            if (message.maxStake != null && $Object.hasOwnProperty.call(message, "maxStake"))
                if (!$util.isInteger(message.maxStake) && !(message.maxStake && $util.isInteger(message.maxStake.low) && $util.isInteger(message.maxStake.high)))
                    return "maxStake: integer|Long expected";
            if (message.stakeToReward != null && $Object.hasOwnProperty.call(message, "stakeToReward"))
                if (!$util.isInteger(message.stakeToReward) && !(message.stakeToReward && $util.isInteger(message.stakeToReward.low) && $util.isInteger(message.stakeToReward.high)))
                    return "stakeToReward: integer|Long expected";
            if (message.stakeToNotReward != null && $Object.hasOwnProperty.call(message, "stakeToNotReward"))
                if (!$util.isInteger(message.stakeToNotReward) && !(message.stakeToNotReward && $util.isInteger(message.stakeToNotReward.low) && $util.isInteger(message.stakeToNotReward.high)))
                    return "stakeToNotReward: integer|Long expected";
            if (message.stakeRewardStart != null && $Object.hasOwnProperty.call(message, "stakeRewardStart"))
                if (!$util.isInteger(message.stakeRewardStart) && !(message.stakeRewardStart && $util.isInteger(message.stakeRewardStart.low) && $util.isInteger(message.stakeRewardStart.high)))
                    return "stakeRewardStart: integer|Long expected";
            if (message.unclaimedStakeRewardStart != null && $Object.hasOwnProperty.call(message, "unclaimedStakeRewardStart"))
                if (!$util.isInteger(message.unclaimedStakeRewardStart) && !(message.unclaimedStakeRewardStart && $util.isInteger(message.unclaimedStakeRewardStart.low) && $util.isInteger(message.unclaimedStakeRewardStart.high)))
                    return "unclaimedStakeRewardStart: integer|Long expected";
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                if (!$util.isInteger(message.stake) && !(message.stake && $util.isInteger(message.stake.low) && $util.isInteger(message.stake.high)))
                    return "stake: integer|Long expected";
            if (message.rewardSumHistory != null && $Object.hasOwnProperty.call(message, "rewardSumHistory")) {
                if (!$Array.isArray(message.rewardSumHistory))
                    return "rewardSumHistory: array expected";
                for (let i = 0; i < message.rewardSumHistory.length; ++i)
                    if (!$util.isInteger(message.rewardSumHistory[i]) && !(message.rewardSumHistory[i] && $util.isInteger(message.rewardSumHistory[i].low) && $util.isInteger(message.rewardSumHistory[i].high)))
                        return "rewardSumHistory: integer|Long[] expected";
            }
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                if (!$util.isInteger(message.weight))
                    return "weight: integer expected";
            if (message.pendingRewards != null && $Object.hasOwnProperty.call(message, "pendingRewards"))
                if (!$util.isInteger(message.pendingRewards) && !(message.pendingRewards && $util.isInteger(message.pendingRewards.low) && $util.isInteger(message.pendingRewards.high)))
                    return "pendingRewards: integer|Long expected";
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                if (typeof message.deleted !== "boolean")
                    return "deleted: boolean expected";
            return null;
        };

        /**
         * Creates a StakingNodeInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.StakingNodeInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.StakingNodeInfo} StakingNodeInfo
         */
        StakingNodeInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.StakingNodeInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.StakingNodeInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.StakingNodeInfo();
            if (object.nodeNumber != null)
                if (typeof object.nodeNumber === "object" ? object.nodeNumber.low || object.nodeNumber.high : $Number(object.nodeNumber) !== 0)
                    if ($util.Long)
                        message.nodeNumber = $util.Long.fromValue(object.nodeNumber, false);
                    else if (typeof object.nodeNumber === "string")
                        message.nodeNumber = $parseInt(object.nodeNumber, 10);
                    else if (typeof object.nodeNumber === "number")
                        message.nodeNumber = object.nodeNumber;
                    else if (typeof object.nodeNumber === "object")
                        message.nodeNumber = new $util.LongBits(object.nodeNumber.low >>> 0, object.nodeNumber.high >>> 0).toNumber();
            if (object.minStake != null)
                if (typeof object.minStake === "object" ? object.minStake.low || object.minStake.high : $Number(object.minStake) !== 0)
                    if ($util.Long)
                        message.minStake = $util.Long.fromValue(object.minStake, false);
                    else if (typeof object.minStake === "string")
                        message.minStake = $parseInt(object.minStake, 10);
                    else if (typeof object.minStake === "number")
                        message.minStake = object.minStake;
                    else if (typeof object.minStake === "object")
                        message.minStake = new $util.LongBits(object.minStake.low >>> 0, object.minStake.high >>> 0).toNumber();
            if (object.maxStake != null)
                if (typeof object.maxStake === "object" ? object.maxStake.low || object.maxStake.high : $Number(object.maxStake) !== 0)
                    if ($util.Long)
                        message.maxStake = $util.Long.fromValue(object.maxStake, false);
                    else if (typeof object.maxStake === "string")
                        message.maxStake = $parseInt(object.maxStake, 10);
                    else if (typeof object.maxStake === "number")
                        message.maxStake = object.maxStake;
                    else if (typeof object.maxStake === "object")
                        message.maxStake = new $util.LongBits(object.maxStake.low >>> 0, object.maxStake.high >>> 0).toNumber();
            if (object.stakeToReward != null)
                if (typeof object.stakeToReward === "object" ? object.stakeToReward.low || object.stakeToReward.high : $Number(object.stakeToReward) !== 0)
                    if ($util.Long)
                        message.stakeToReward = $util.Long.fromValue(object.stakeToReward, false);
                    else if (typeof object.stakeToReward === "string")
                        message.stakeToReward = $parseInt(object.stakeToReward, 10);
                    else if (typeof object.stakeToReward === "number")
                        message.stakeToReward = object.stakeToReward;
                    else if (typeof object.stakeToReward === "object")
                        message.stakeToReward = new $util.LongBits(object.stakeToReward.low >>> 0, object.stakeToReward.high >>> 0).toNumber();
            if (object.stakeToNotReward != null)
                if (typeof object.stakeToNotReward === "object" ? object.stakeToNotReward.low || object.stakeToNotReward.high : $Number(object.stakeToNotReward) !== 0)
                    if ($util.Long)
                        message.stakeToNotReward = $util.Long.fromValue(object.stakeToNotReward, false);
                    else if (typeof object.stakeToNotReward === "string")
                        message.stakeToNotReward = $parseInt(object.stakeToNotReward, 10);
                    else if (typeof object.stakeToNotReward === "number")
                        message.stakeToNotReward = object.stakeToNotReward;
                    else if (typeof object.stakeToNotReward === "object")
                        message.stakeToNotReward = new $util.LongBits(object.stakeToNotReward.low >>> 0, object.stakeToNotReward.high >>> 0).toNumber();
            if (object.stakeRewardStart != null)
                if (typeof object.stakeRewardStart === "object" ? object.stakeRewardStart.low || object.stakeRewardStart.high : $Number(object.stakeRewardStart) !== 0)
                    if ($util.Long)
                        message.stakeRewardStart = $util.Long.fromValue(object.stakeRewardStart, false);
                    else if (typeof object.stakeRewardStart === "string")
                        message.stakeRewardStart = $parseInt(object.stakeRewardStart, 10);
                    else if (typeof object.stakeRewardStart === "number")
                        message.stakeRewardStart = object.stakeRewardStart;
                    else if (typeof object.stakeRewardStart === "object")
                        message.stakeRewardStart = new $util.LongBits(object.stakeRewardStart.low >>> 0, object.stakeRewardStart.high >>> 0).toNumber();
            if (object.unclaimedStakeRewardStart != null)
                if (typeof object.unclaimedStakeRewardStart === "object" ? object.unclaimedStakeRewardStart.low || object.unclaimedStakeRewardStart.high : $Number(object.unclaimedStakeRewardStart) !== 0)
                    if ($util.Long)
                        message.unclaimedStakeRewardStart = $util.Long.fromValue(object.unclaimedStakeRewardStart, false);
                    else if (typeof object.unclaimedStakeRewardStart === "string")
                        message.unclaimedStakeRewardStart = $parseInt(object.unclaimedStakeRewardStart, 10);
                    else if (typeof object.unclaimedStakeRewardStart === "number")
                        message.unclaimedStakeRewardStart = object.unclaimedStakeRewardStart;
                    else if (typeof object.unclaimedStakeRewardStart === "object")
                        message.unclaimedStakeRewardStart = new $util.LongBits(object.unclaimedStakeRewardStart.low >>> 0, object.unclaimedStakeRewardStart.high >>> 0).toNumber();
            if (object.stake != null)
                if (typeof object.stake === "object" ? object.stake.low || object.stake.high : $Number(object.stake) !== 0)
                    if ($util.Long)
                        message.stake = $util.Long.fromValue(object.stake, false);
                    else if (typeof object.stake === "string")
                        message.stake = $parseInt(object.stake, 10);
                    else if (typeof object.stake === "number")
                        message.stake = object.stake;
                    else if (typeof object.stake === "object")
                        message.stake = new $util.LongBits(object.stake.low >>> 0, object.stake.high >>> 0).toNumber();
            if (object.rewardSumHistory) {
                if (!$Array.isArray(object.rewardSumHistory))
                    throw $TypeError(".proto.StakingNodeInfo.rewardSumHistory: array expected");
                message.rewardSumHistory = $Array(object.rewardSumHistory.length);
                for (let i = 0; i < object.rewardSumHistory.length; ++i)
                    if ($util.Long)
                        message.rewardSumHistory[i] = $util.Long.fromValue(object.rewardSumHistory[i], false);
                    else if (typeof object.rewardSumHistory[i] === "string")
                        message.rewardSumHistory[i] = $parseInt(object.rewardSumHistory[i], 10);
                    else if (typeof object.rewardSumHistory[i] === "number")
                        message.rewardSumHistory[i] = object.rewardSumHistory[i];
                    else if (typeof object.rewardSumHistory[i] === "object")
                        message.rewardSumHistory[i] = new $util.LongBits(object.rewardSumHistory[i].low >>> 0, object.rewardSumHistory[i].high >>> 0).toNumber();
            }
            if (object.weight != null)
                if ($Number(object.weight) !== 0)
                    message.weight = object.weight | 0;
            if (object.pendingRewards != null)
                if (typeof object.pendingRewards === "object" ? object.pendingRewards.low || object.pendingRewards.high : $Number(object.pendingRewards) !== 0)
                    if ($util.Long)
                        message.pendingRewards = $util.Long.fromValue(object.pendingRewards, false);
                    else if (typeof object.pendingRewards === "string")
                        message.pendingRewards = $parseInt(object.pendingRewards, 10);
                    else if (typeof object.pendingRewards === "number")
                        message.pendingRewards = object.pendingRewards;
                    else if (typeof object.pendingRewards === "object")
                        message.pendingRewards = new $util.LongBits(object.pendingRewards.low >>> 0, object.pendingRewards.high >>> 0).toNumber();
            if (object.deleted != null)
                if (object.deleted)
                    message.deleted = $Boolean(object.deleted);
            return message;
        };

        /**
         * Creates a plain object from a StakingNodeInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.StakingNodeInfo
         * @static
         * @param {proto.StakingNodeInfo} message StakingNodeInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StakingNodeInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.rewardSumHistory = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nodeNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.nodeNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.minStake = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.minStake = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxStake = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maxStake = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakeToReward = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakeToReward = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakeToNotReward = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakeToNotReward = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stakeRewardStart = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stakeRewardStart = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.unclaimedStakeRewardStart = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.unclaimedStakeRewardStart = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.stake = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.stake = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.weight = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.pendingRewards = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.pendingRewards = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.deleted = false;
            }
            if (message.nodeNumber != null && $Object.hasOwnProperty.call(message, "nodeNumber"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.nodeNumber = typeof message.nodeNumber === "number" ? $BigInt(message.nodeNumber) : $util.Long.fromBits(message.nodeNumber.low >>> 0, message.nodeNumber.high >>> 0, false).toBigInt();
                else if (typeof message.nodeNumber === "number")
                    object.nodeNumber = options.longs === $String ? $String(message.nodeNumber) : message.nodeNumber;
                else
                    object.nodeNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.nodeNumber) : options.longs === $Number ? new $util.LongBits(message.nodeNumber.low >>> 0, message.nodeNumber.high >>> 0).toNumber() : message.nodeNumber;
            if (message.minStake != null && $Object.hasOwnProperty.call(message, "minStake"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.minStake = typeof message.minStake === "number" ? $BigInt(message.minStake) : $util.Long.fromBits(message.minStake.low >>> 0, message.minStake.high >>> 0, false).toBigInt();
                else if (typeof message.minStake === "number")
                    object.minStake = options.longs === $String ? $String(message.minStake) : message.minStake;
                else
                    object.minStake = options.longs === $String ? $util.Long.prototype.toString.call(message.minStake) : options.longs === $Number ? new $util.LongBits(message.minStake.low >>> 0, message.minStake.high >>> 0).toNumber() : message.minStake;
            if (message.maxStake != null && $Object.hasOwnProperty.call(message, "maxStake"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maxStake = typeof message.maxStake === "number" ? $BigInt(message.maxStake) : $util.Long.fromBits(message.maxStake.low >>> 0, message.maxStake.high >>> 0, false).toBigInt();
                else if (typeof message.maxStake === "number")
                    object.maxStake = options.longs === $String ? $String(message.maxStake) : message.maxStake;
                else
                    object.maxStake = options.longs === $String ? $util.Long.prototype.toString.call(message.maxStake) : options.longs === $Number ? new $util.LongBits(message.maxStake.low >>> 0, message.maxStake.high >>> 0).toNumber() : message.maxStake;
            if (message.stakeToReward != null && $Object.hasOwnProperty.call(message, "stakeToReward"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakeToReward = typeof message.stakeToReward === "number" ? $BigInt(message.stakeToReward) : $util.Long.fromBits(message.stakeToReward.low >>> 0, message.stakeToReward.high >>> 0, false).toBigInt();
                else if (typeof message.stakeToReward === "number")
                    object.stakeToReward = options.longs === $String ? $String(message.stakeToReward) : message.stakeToReward;
                else
                    object.stakeToReward = options.longs === $String ? $util.Long.prototype.toString.call(message.stakeToReward) : options.longs === $Number ? new $util.LongBits(message.stakeToReward.low >>> 0, message.stakeToReward.high >>> 0).toNumber() : message.stakeToReward;
            if (message.stakeToNotReward != null && $Object.hasOwnProperty.call(message, "stakeToNotReward"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakeToNotReward = typeof message.stakeToNotReward === "number" ? $BigInt(message.stakeToNotReward) : $util.Long.fromBits(message.stakeToNotReward.low >>> 0, message.stakeToNotReward.high >>> 0, false).toBigInt();
                else if (typeof message.stakeToNotReward === "number")
                    object.stakeToNotReward = options.longs === $String ? $String(message.stakeToNotReward) : message.stakeToNotReward;
                else
                    object.stakeToNotReward = options.longs === $String ? $util.Long.prototype.toString.call(message.stakeToNotReward) : options.longs === $Number ? new $util.LongBits(message.stakeToNotReward.low >>> 0, message.stakeToNotReward.high >>> 0).toNumber() : message.stakeToNotReward;
            if (message.stakeRewardStart != null && $Object.hasOwnProperty.call(message, "stakeRewardStart"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stakeRewardStart = typeof message.stakeRewardStart === "number" ? $BigInt(message.stakeRewardStart) : $util.Long.fromBits(message.stakeRewardStart.low >>> 0, message.stakeRewardStart.high >>> 0, false).toBigInt();
                else if (typeof message.stakeRewardStart === "number")
                    object.stakeRewardStart = options.longs === $String ? $String(message.stakeRewardStart) : message.stakeRewardStart;
                else
                    object.stakeRewardStart = options.longs === $String ? $util.Long.prototype.toString.call(message.stakeRewardStart) : options.longs === $Number ? new $util.LongBits(message.stakeRewardStart.low >>> 0, message.stakeRewardStart.high >>> 0).toNumber() : message.stakeRewardStart;
            if (message.unclaimedStakeRewardStart != null && $Object.hasOwnProperty.call(message, "unclaimedStakeRewardStart"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.unclaimedStakeRewardStart = typeof message.unclaimedStakeRewardStart === "number" ? $BigInt(message.unclaimedStakeRewardStart) : $util.Long.fromBits(message.unclaimedStakeRewardStart.low >>> 0, message.unclaimedStakeRewardStart.high >>> 0, false).toBigInt();
                else if (typeof message.unclaimedStakeRewardStart === "number")
                    object.unclaimedStakeRewardStart = options.longs === $String ? $String(message.unclaimedStakeRewardStart) : message.unclaimedStakeRewardStart;
                else
                    object.unclaimedStakeRewardStart = options.longs === $String ? $util.Long.prototype.toString.call(message.unclaimedStakeRewardStart) : options.longs === $Number ? new $util.LongBits(message.unclaimedStakeRewardStart.low >>> 0, message.unclaimedStakeRewardStart.high >>> 0).toNumber() : message.unclaimedStakeRewardStart;
            if (message.stake != null && $Object.hasOwnProperty.call(message, "stake"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.stake = typeof message.stake === "number" ? $BigInt(message.stake) : $util.Long.fromBits(message.stake.low >>> 0, message.stake.high >>> 0, false).toBigInt();
                else if (typeof message.stake === "number")
                    object.stake = options.longs === $String ? $String(message.stake) : message.stake;
                else
                    object.stake = options.longs === $String ? $util.Long.prototype.toString.call(message.stake) : options.longs === $Number ? new $util.LongBits(message.stake.low >>> 0, message.stake.high >>> 0).toNumber() : message.stake;
            if (message.rewardSumHistory && message.rewardSumHistory.length) {
                object.rewardSumHistory = $Array(message.rewardSumHistory.length);
                for (let j = 0; j < message.rewardSumHistory.length; ++j)
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.rewardSumHistory[j] = typeof message.rewardSumHistory[j] === "number" ? $BigInt(message.rewardSumHistory[j]) : $util.Long.fromBits(message.rewardSumHistory[j].low >>> 0, message.rewardSumHistory[j].high >>> 0, false).toBigInt();
                    else if (typeof message.rewardSumHistory[j] === "number")
                        object.rewardSumHistory[j] = options.longs === $String ? $String(message.rewardSumHistory[j]) : message.rewardSumHistory[j];
                    else
                        object.rewardSumHistory[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.rewardSumHistory[j]) : options.longs === $Number ? new $util.LongBits(message.rewardSumHistory[j].low >>> 0, message.rewardSumHistory[j].high >>> 0).toNumber() : message.rewardSumHistory[j];
            }
            if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                object.weight = message.weight;
            if (message.pendingRewards != null && $Object.hasOwnProperty.call(message, "pendingRewards"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.pendingRewards = typeof message.pendingRewards === "number" ? $BigInt(message.pendingRewards) : $util.Long.fromBits(message.pendingRewards.low >>> 0, message.pendingRewards.high >>> 0, false).toBigInt();
                else if (typeof message.pendingRewards === "number")
                    object.pendingRewards = options.longs === $String ? $String(message.pendingRewards) : message.pendingRewards;
                else
                    object.pendingRewards = options.longs === $String ? $util.Long.prototype.toString.call(message.pendingRewards) : options.longs === $Number ? new $util.LongBits(message.pendingRewards.low >>> 0, message.pendingRewards.high >>> 0).toNumber() : message.pendingRewards;
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                object.deleted = message.deleted;
            return object;
        };

        /**
         * Converts this StakingNodeInfo to JSON.
         * @function toJSON
         * @memberof proto.StakingNodeInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StakingNodeInfo.prototype.toJSON = function() {
            return StakingNodeInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StakingNodeInfo
         * @function getTypeUrl
         * @memberof proto.StakingNodeInfo
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StakingNodeInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.StakingNodeInfo";
        };

        return StakingNodeInfo;
    })();

    proto.Token = (function() {

        /**
         * Properties of a Token.
         * @typedef {Object} proto.Token.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] A unique identifier for this token.
         * @property {string|null} [name] A human-readable name for this token.
         * <p>
         * This value MAY NOT be unique.<br/>
         * This value SHALL NOT exceed 100 bytes when encoded as UTF-8.
         * @property {string|null} [symbol] A human-readable symbol for the token.
         * <p>
         * This value SHALL NOT be unique.<br/>
         * This value SHALL NOT exceed 100 bytes when encoded as UTF-8.
         * @property {number|null} [decimals] A number of decimal places for this token.
         * <p>
         * If decimals are 8 or 11, then the number of whole tokens can be at most
         * billions or millions, respectively. More decimals allows for a more
         * finely-divided token, but also limits the maximum total supply.
         * <p>
         * Examples
         * <ul>
         * <li>Bitcoin satoshis (21 million whole tokens with 8 decimals).</li>
         * <li>Hedera tinybar (50 billion whole tokens with 8 decimals).</li>
         * <li>Bitcoin milli-satoshis (21 million whole tokens with 11 decimals).</li>
         * <li>Theoretical limit is roughly 92.2 billion with 8 decimals, or
         * 92.2 million with 11 decimals.</li>
         * </ul>
         * All token amounts in the network are stored as integer amounts, with each
         * unit representing 10<sup>-decimals</sup> whole tokens.
         * <p>
         * For tokens with `token_type` set to `NON_FUNGIBLE_UNIQUE` this MUST be 0.
         * @property {number|Long|null} [totalSupply] A _current_ total supply of this token, expressed in the smallest unit
         * of the token.
         * <p>
         * The number of _whole_ tokens this represents is (total_supply /
         * 10<sup>decimals</sup>). The value of total supply, MUST be within the
         * positive range of a twos-compliment signed 64-bit integer.
         * The `total_supply`, therefore MUST be between 1, and
         * 9,223,372,036,854,775,807, inclusive.
         * <p>
         * This value SHALL be reduced when a `token_burn` or `token_wipe_account`
         * operation is executed, and SHALL be increased when a `token_mint`
         * operation is executed.
         * @property {proto.AccountID.$Properties|null} [treasuryAccountId] A treasury account identifier for this token.
         * <p>
         * When the token is created, the initial supply given in the token create
         * transaction SHALL be minted and deposited in the treasury account.<br/>
         * All token mint transactions for this token SHALL deposit the new minted
         * tokens in the treasury account.<br/>
         * All token burn transactions for this token SHALL remove the tokens to be
         * burned from the treasury account.
         * @property {proto.Key.$Properties|null} [adminKey] Access control for general modification of this token.
         * <p>
         * This key MUST sign any `token_update` transaction that
         * changes any attribute of the token other than expiration_time.
         * Other attributes of this token MAY be changed by transactions other than
         * `token_update`, and MUST be signed by one of the other purpose-specific
         * keys assigned to the token.<br/>
         * This value can be set during token creation, and SHALL NOT be
         * modified thereafter, unless the update transaction is signed by both
         * the existing `admin_key` and the new `admin_key`.<br/>
         * If the `admin_key` is not set for a token, that token SHALL be immutable.
         * @property {proto.Key.$Properties|null} [kycKey] Access control for KYC for this token.
         * <p>
         * Know Your Customer (KYC) status may be granted for an account by a token
         * grant kyc transaction signed by this key.<br/>
         * If this key is not set, then KYC status cannot be granted to an account
         * for this token, and any `TokenGrantKyc` transaction attempting to grant
         * kyc to an account for this token SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {proto.Key.$Properties|null} [freezeKey] Access control to freeze this token.
         * <p>
         * A token may be frozen for an account, preventing any transaction from
         * transferring that token for that specified account, by a token freeze
         * account transaction signed by this key.<br/>
         * If this key is not set, the token cannot be frozen, and any transaction
         * attempting to freeze the token for an account SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {proto.Key.$Properties|null} [wipeKey] Access control of account wipe for this token.
         * <p>
         * A token may be wiped, removing and burning tokens from a specific
         * account, by a token wipe transaction, which MUST be signed by this key.
         * The `treasury_account` cannot be subjected to a token wipe. A token burn
         * transaction, signed by the `supply_key`, serves to burn tokens held by
         * the `treasury_account` instead.<br/>
         * If this key is not set, the token cannot be wiped, and any transaction
         * attempting to wipe the token from an account SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {proto.Key.$Properties|null} [supplyKey] Access control of token mint/burn for this token.
         * <p>
         * A token mint transaction MUST be signed by this key, and any token mint
         * transaction not signed by the current `supply_key` for that token
         * SHALL NOT succeed.<br/>
         * A token burn transaction MUST be signed by this key, and any token burn
         * transaction not signed by the current `supply_key` for that token
         * SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {proto.Key.$Properties|null} [feeScheduleKey] Access control of the `custom_fees` field for this token.
         * <p>
         * The token custom fee schedule may be changed, modifying the fees charged
         * for transferring that token, by a token update transaction, which MUST
         * be signed by this key.<br/>
         * If this key is not set, the token custom fee schedule cannot be changed,
         * and any transaction attempting to change the custom fee schedule for
         * this token SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {proto.Key.$Properties|null} [pauseKey] Access control of pause/unpause for this token.
         * <p>
         * A token may be paused, preventing any transaction from transferring that
         * token, by a token update transaction signed by this key.<br/>
         * If this key is not set, the token cannot be paused, and any transaction
         * attempting to pause the token SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {number|Long|null} [lastUsedSerialNumber] A last used serial number for this token.
         * <p>
         * This SHALL apply only to non-fungible tokens.<br/>
         * When a new NFT is minted, the serial number to apply SHALL be calculated
         * from this value.
         * @property {boolean|null} [deleted] A flag indicating that this token is deleted.
         * <p>
         * A transaction involving a deleted token MUST NOT succeed.
         * @property {proto.TokenType|null} [tokenType] A type for this token.
         * <p>
         * A token SHALL be either `FUNGIBLE_COMMON` or `NON_FUNGIBLE_UNIQUE`.<br/>
         * If this value was omitted during token creation, `FUNGIBLE_COMMON`
         * SHALL be used.
         * @property {proto.TokenSupplyType|null} [supplyType] A supply type for this token.
         * <p>
         * A token SHALL have either `INFINITE` or `FINITE` supply type.<br/>
         * If this value was omitted during token creation, the value `INFINITE`
         * SHALL be used.
         * @property {proto.AccountID.$Properties|null} [autoRenewAccountId] An identifier for the account (if any) that the network will attempt
         * to charge for this token's auto-renewal upon expiration.
         * <p>
         * This field is OPTIONAL. If it is not set then renewal fees SHALL be
         * charged to the account identified by `treasury_account_id`.
         * @property {number|Long|null} [autoRenewSeconds] A number of seconds by which the network should automatically extend
         * this token's expiration.
         * <p>
         * If the token has a valid auto-renew account, and is not deleted upon
         * expiration, the network SHALL attempt to automatically renew this
         * token.<br/>
         * If this is not provided in an allowed range on token creation, the
         * transaction SHALL fail with `INVALID_AUTO_RENEWAL_PERIOD`.<br/>
         * The default values for the minimum period and maximum period are 30 days
         * and 90 days, respectively.
         * @property {number|Long|null} [expirationSecond] An expiration time for this token, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the
         * UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.
         * @property {string|null} [memo] A short description of this token.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @property {number|Long|null} [maxSupply] A maximum supply of this token.<br/>
         * This is the maximum number of tokens of this type that may be issued.
         * <p>
         * This limit SHALL apply regardless of `token_type`.<br/>
         * If `supply_type` is `INFINITE` then this value MUST be 0.<br/>
         * If `supply_type` is `FINITE`, then this value MUST be greater than 0.
         * @property {boolean|null} [paused] A flag indicating that this token is paused.
         * <p>
         * A transaction involving a paused token, other than token_unpause,
         * MUST NOT succeed.
         * @property {boolean|null} [accountsFrozenByDefault] A flag indicating that accounts associated to this token are frozen by
         * default.
         * <p>
         * Accounts newly associated with this token CANNOT transact in the token
         * until unfrozen.<br/>
         * This SHALL NOT prevent a `tokenReject` transaction to return the tokens
         * from an account to the treasury account.
         * @property {boolean|null} [accountsKycGrantedByDefault] A flag indicating that accounts associated with this token are granted
         * KYC by default.
         * @property {Array.<proto.CustomFee.$Properties>|null} [customFees] A custom fee schedule for this token.
         * @property {Uint8Array|null} [metadata] A Token "Metadata".
         * <p>
         * This value, if set, SHALL NOT exceed 100 bytes.
         * @property {proto.Key.$Properties|null} [metadataKey] Access Control of metadata update for this token.
         * <p>
         * A transaction to update the `metadata` field of this token MUST be
         * signed by this key.<br/>
         * If this token is a non-fungible/unique token type, a transaction to
         * update the `metadata` field of any individual serialized unique token
         * of this type MUST be signed by this key.<br/>
         * If this key is not set, the token metadata SHALL NOT be changed after it
         * is created.<br/>
         * If this key is not set, the metadata for any individual serialized token
         * of this type SHALL NOT be changed after it is created.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a Token.
         * @memberof proto
         * @interface IToken
         * @augments proto.Token.$Properties
         * @deprecated Use proto.Token.$Properties instead.
         */

        /**
         * Shape of a Token.
         * @typedef {{
         *   tokenId?: proto.TokenID.$Shape|null;
         *   name?: string|null;
         *   symbol?: string|null;
         *   decimals?: number|null;
         *   totalSupply?: number|Long|null;
         *   treasuryAccountId?: proto.AccountID.$Shape|null;
         *   adminKey?: proto.Key.$Shape|null;
         *   kycKey?: proto.Key.$Shape|null;
         *   freezeKey?: proto.Key.$Shape|null;
         *   wipeKey?: proto.Key.$Shape|null;
         *   supplyKey?: proto.Key.$Shape|null;
         *   feeScheduleKey?: proto.Key.$Shape|null;
         *   pauseKey?: proto.Key.$Shape|null;
         *   lastUsedSerialNumber?: number|Long|null;
         *   deleted?: boolean|null;
         *   tokenType?: proto.TokenType|null;
         *   supplyType?: proto.TokenSupplyType|null;
         *   autoRenewAccountId?: proto.AccountID.$Shape|null;
         *   autoRenewSeconds?: number|Long|null;
         *   expirationSecond?: number|Long|null;
         *   memo?: string|null;
         *   maxSupply?: number|Long|null;
         *   paused?: boolean|null;
         *   accountsFrozenByDefault?: boolean|null;
         *   accountsKycGrantedByDefault?: boolean|null;
         *   customFees?: Array.<proto.CustomFee.$Shape>|null;
         *   metadata?: Uint8Array|null;
         *   metadataKey?: proto.Key.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.Token.$Shape
         */

        /**
         * Constructs a new Token.
         * @memberof proto
         * @classdesc An Hedera Token Service(HTS) token.
         * 
         * A token SHALL represent a fungible or non-fungible unit of exchange.<br/>
         * The specified Treasury Account SHALL receive the initial supply of tokens and
         * SHALL determine distribution of all tokens once minted.
         * @constructor
         * @param {proto.Token.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const Token = function (properties) {
            this.customFees = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A unique identifier for this token.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.tokenId = null;

        /**
         * A human-readable name for this token.
         * <p>
         * This value MAY NOT be unique.<br/>
         * This value SHALL NOT exceed 100 bytes when encoded as UTF-8.
         * @member {string} name
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.name = "";

        /**
         * A human-readable symbol for the token.
         * <p>
         * This value SHALL NOT be unique.<br/>
         * This value SHALL NOT exceed 100 bytes when encoded as UTF-8.
         * @member {string} symbol
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.symbol = "";

        /**
         * A number of decimal places for this token.
         * <p>
         * If decimals are 8 or 11, then the number of whole tokens can be at most
         * billions or millions, respectively. More decimals allows for a more
         * finely-divided token, but also limits the maximum total supply.
         * <p>
         * Examples
         * <ul>
         * <li>Bitcoin satoshis (21 million whole tokens with 8 decimals).</li>
         * <li>Hedera tinybar (50 billion whole tokens with 8 decimals).</li>
         * <li>Bitcoin milli-satoshis (21 million whole tokens with 11 decimals).</li>
         * <li>Theoretical limit is roughly 92.2 billion with 8 decimals, or
         * 92.2 million with 11 decimals.</li>
         * </ul>
         * All token amounts in the network are stored as integer amounts, with each
         * unit representing 10<sup>-decimals</sup> whole tokens.
         * <p>
         * For tokens with `token_type` set to `NON_FUNGIBLE_UNIQUE` this MUST be 0.
         * @member {number} decimals
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.decimals = 0;

        /**
         * A _current_ total supply of this token, expressed in the smallest unit
         * of the token.
         * <p>
         * The number of _whole_ tokens this represents is (total_supply /
         * 10<sup>decimals</sup>). The value of total supply, MUST be within the
         * positive range of a twos-compliment signed 64-bit integer.
         * The `total_supply`, therefore MUST be between 1, and
         * 9,223,372,036,854,775,807, inclusive.
         * <p>
         * This value SHALL be reduced when a `token_burn` or `token_wipe_account`
         * operation is executed, and SHALL be increased when a `token_mint`
         * operation is executed.
         * @member {number|Long} totalSupply
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.totalSupply = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A treasury account identifier for this token.
         * <p>
         * When the token is created, the initial supply given in the token create
         * transaction SHALL be minted and deposited in the treasury account.<br/>
         * All token mint transactions for this token SHALL deposit the new minted
         * tokens in the treasury account.<br/>
         * All token burn transactions for this token SHALL remove the tokens to be
         * burned from the treasury account.
         * @member {proto.AccountID.$Properties|null|undefined} treasuryAccountId
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.treasuryAccountId = null;

        /**
         * Access control for general modification of this token.
         * <p>
         * This key MUST sign any `token_update` transaction that
         * changes any attribute of the token other than expiration_time.
         * Other attributes of this token MAY be changed by transactions other than
         * `token_update`, and MUST be signed by one of the other purpose-specific
         * keys assigned to the token.<br/>
         * This value can be set during token creation, and SHALL NOT be
         * modified thereafter, unless the update transaction is signed by both
         * the existing `admin_key` and the new `admin_key`.<br/>
         * If the `admin_key` is not set for a token, that token SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} adminKey
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.adminKey = null;

        /**
         * Access control for KYC for this token.
         * <p>
         * Know Your Customer (KYC) status may be granted for an account by a token
         * grant kyc transaction signed by this key.<br/>
         * If this key is not set, then KYC status cannot be granted to an account
         * for this token, and any `TokenGrantKyc` transaction attempting to grant
         * kyc to an account for this token SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} kycKey
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.kycKey = null;

        /**
         * Access control to freeze this token.
         * <p>
         * A token may be frozen for an account, preventing any transaction from
         * transferring that token for that specified account, by a token freeze
         * account transaction signed by this key.<br/>
         * If this key is not set, the token cannot be frozen, and any transaction
         * attempting to freeze the token for an account SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} freezeKey
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.freezeKey = null;

        /**
         * Access control of account wipe for this token.
         * <p>
         * A token may be wiped, removing and burning tokens from a specific
         * account, by a token wipe transaction, which MUST be signed by this key.
         * The `treasury_account` cannot be subjected to a token wipe. A token burn
         * transaction, signed by the `supply_key`, serves to burn tokens held by
         * the `treasury_account` instead.<br/>
         * If this key is not set, the token cannot be wiped, and any transaction
         * attempting to wipe the token from an account SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} wipeKey
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.wipeKey = null;

        /**
         * Access control of token mint/burn for this token.
         * <p>
         * A token mint transaction MUST be signed by this key, and any token mint
         * transaction not signed by the current `supply_key` for that token
         * SHALL NOT succeed.<br/>
         * A token burn transaction MUST be signed by this key, and any token burn
         * transaction not signed by the current `supply_key` for that token
         * SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} supplyKey
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.supplyKey = null;

        /**
         * Access control of the `custom_fees` field for this token.
         * <p>
         * The token custom fee schedule may be changed, modifying the fees charged
         * for transferring that token, by a token update transaction, which MUST
         * be signed by this key.<br/>
         * If this key is not set, the token custom fee schedule cannot be changed,
         * and any transaction attempting to change the custom fee schedule for
         * this token SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} feeScheduleKey
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.feeScheduleKey = null;

        /**
         * Access control of pause/unpause for this token.
         * <p>
         * A token may be paused, preventing any transaction from transferring that
         * token, by a token update transaction signed by this key.<br/>
         * If this key is not set, the token cannot be paused, and any transaction
         * attempting to pause the token SHALL NOT succeed.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} pauseKey
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.pauseKey = null;

        /**
         * A last used serial number for this token.
         * <p>
         * This SHALL apply only to non-fungible tokens.<br/>
         * When a new NFT is minted, the serial number to apply SHALL be calculated
         * from this value.
         * @member {number|Long} lastUsedSerialNumber
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.lastUsedSerialNumber = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A flag indicating that this token is deleted.
         * <p>
         * A transaction involving a deleted token MUST NOT succeed.
         * @member {boolean} deleted
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.deleted = false;

        /**
         * A type for this token.
         * <p>
         * A token SHALL be either `FUNGIBLE_COMMON` or `NON_FUNGIBLE_UNIQUE`.<br/>
         * If this value was omitted during token creation, `FUNGIBLE_COMMON`
         * SHALL be used.
         * @member {proto.TokenType} tokenType
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.tokenType = 0;

        /**
         * A supply type for this token.
         * <p>
         * A token SHALL have either `INFINITE` or `FINITE` supply type.<br/>
         * If this value was omitted during token creation, the value `INFINITE`
         * SHALL be used.
         * @member {proto.TokenSupplyType} supplyType
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.supplyType = 0;

        /**
         * An identifier for the account (if any) that the network will attempt
         * to charge for this token's auto-renewal upon expiration.
         * <p>
         * This field is OPTIONAL. If it is not set then renewal fees SHALL be
         * charged to the account identified by `treasury_account_id`.
         * @member {proto.AccountID.$Properties|null|undefined} autoRenewAccountId
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.autoRenewAccountId = null;

        /**
         * A number of seconds by which the network should automatically extend
         * this token's expiration.
         * <p>
         * If the token has a valid auto-renew account, and is not deleted upon
         * expiration, the network SHALL attempt to automatically renew this
         * token.<br/>
         * If this is not provided in an allowed range on token creation, the
         * transaction SHALL fail with `INVALID_AUTO_RENEWAL_PERIOD`.<br/>
         * The default values for the minimum period and maximum period are 30 days
         * and 90 days, respectively.
         * @member {number|Long} autoRenewSeconds
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.autoRenewSeconds = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * An expiration time for this token, in seconds since the epoch.
         * <p>
         * For this purpose, `epoch` SHALL be the
         * UNIX epoch with 0 at `1970-01-01T00:00:00.000Z`.
         * @member {number|Long} expirationSecond
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.expirationSecond = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A short description of this token.
         * <p>
         * This value, if set, MUST NOT exceed `transaction.maxMemoUtf8Bytes`
         * (default 100) bytes when encoded as UTF-8.
         * @member {string} memo
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.memo = "";

        /**
         * A maximum supply of this token.<br/>
         * This is the maximum number of tokens of this type that may be issued.
         * <p>
         * This limit SHALL apply regardless of `token_type`.<br/>
         * If `supply_type` is `INFINITE` then this value MUST be 0.<br/>
         * If `supply_type` is `FINITE`, then this value MUST be greater than 0.
         * @member {number|Long} maxSupply
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.maxSupply = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A flag indicating that this token is paused.
         * <p>
         * A transaction involving a paused token, other than token_unpause,
         * MUST NOT succeed.
         * @member {boolean} paused
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.paused = false;

        /**
         * A flag indicating that accounts associated to this token are frozen by
         * default.
         * <p>
         * Accounts newly associated with this token CANNOT transact in the token
         * until unfrozen.<br/>
         * This SHALL NOT prevent a `tokenReject` transaction to return the tokens
         * from an account to the treasury account.
         * @member {boolean} accountsFrozenByDefault
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.accountsFrozenByDefault = false;

        /**
         * A flag indicating that accounts associated with this token are granted
         * KYC by default.
         * @member {boolean} accountsKycGrantedByDefault
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.accountsKycGrantedByDefault = false;

        /**
         * A custom fee schedule for this token.
         * @member {Array.<proto.CustomFee.$Properties>} customFees
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.customFees = $util.emptyArray;

        /**
         * A Token "Metadata".
         * <p>
         * This value, if set, SHALL NOT exceed 100 bytes.
         * @member {Uint8Array} metadata
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.metadata = $util.newBuffer([]);

        /**
         * Access Control of metadata update for this token.
         * <p>
         * A transaction to update the `metadata` field of this token MUST be
         * signed by this key.<br/>
         * If this token is a non-fungible/unique token type, a transaction to
         * update the `metadata` field of any individual serialized unique token
         * of this type MUST be signed by this key.<br/>
         * If this key is not set, the token metadata SHALL NOT be changed after it
         * is created.<br/>
         * If this key is not set, the metadata for any individual serialized token
         * of this type SHALL NOT be changed after it is created.<br/>
         * This key MAY be set when the token is created, and MAY be set or modified
         * via a token update transaction signed by the `admin_key`.<br/>
         * If `admin_key` is not set, this value, whether set or unset,
         * SHALL be immutable.
         * @member {proto.Key.$Properties|null|undefined} metadataKey
         * @memberof proto.Token
         * @instance
         */
        Token.prototype.metadataKey = null;

        /**
         * Creates a new Token instance using the specified properties.
         * @function create
         * @memberof proto.Token
         * @static
         * @param {proto.Token.$Properties=} [properties] Properties to set
         * @returns {proto.Token} Token instance
         * @type {{
         *   (properties: proto.Token.$Shape): proto.Token & proto.Token.$Shape;
         *   (properties?: proto.Token.$Properties): proto.Token;
         * }}
         */
        Token.create = function(properties) {
            return new Token(properties);
        };

        /**
         * Encodes the specified Token message. Does not implicitly {@link proto.Token.verify|verify} messages.
         * @function encode
         * @memberof proto.Token
         * @static
         * @param {proto.Token.$Properties} message Token message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Token.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol") && message.symbol !== "")
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.symbol);
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals") && message.decimals !== 0)
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.decimals);
            if (message.totalSupply != null && $Object.hasOwnProperty.call(message, "totalSupply") && (typeof message.totalSupply === "object" ? message.totalSupply.low || message.totalSupply.high : message.totalSupply !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.totalSupply);
            if (message.treasuryAccountId != null && $Object.hasOwnProperty.call(message, "treasuryAccountId"))
                $root.proto.AccountID.encode(message.treasuryAccountId, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                $root.proto.Key.encode(message.adminKey, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.kycKey != null && $Object.hasOwnProperty.call(message, "kycKey"))
                $root.proto.Key.encode(message.kycKey, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.freezeKey != null && $Object.hasOwnProperty.call(message, "freezeKey"))
                $root.proto.Key.encode(message.freezeKey, writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
            if (message.wipeKey != null && $Object.hasOwnProperty.call(message, "wipeKey"))
                $root.proto.Key.encode(message.wipeKey, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
            if (message.supplyKey != null && $Object.hasOwnProperty.call(message, "supplyKey"))
                $root.proto.Key.encode(message.supplyKey, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                $root.proto.Key.encode(message.feeScheduleKey, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
            if (message.pauseKey != null && $Object.hasOwnProperty.call(message, "pauseKey"))
                $root.proto.Key.encode(message.pauseKey, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
            if (message.lastUsedSerialNumber != null && $Object.hasOwnProperty.call(message, "lastUsedSerialNumber") && (typeof message.lastUsedSerialNumber === "object" ? message.lastUsedSerialNumber.low || message.lastUsedSerialNumber.high : message.lastUsedSerialNumber !== 0))
                writer.uint32(/* id 14, wireType 0 =*/112).int64(message.lastUsedSerialNumber);
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted") && message.deleted !== false)
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message.deleted);
            if (message.tokenType != null && $Object.hasOwnProperty.call(message, "tokenType") && message.tokenType !== 0)
                writer.uint32(/* id 16, wireType 0 =*/128).int32(message.tokenType);
            if (message.supplyType != null && $Object.hasOwnProperty.call(message, "supplyType") && message.supplyType !== 0)
                writer.uint32(/* id 17, wireType 0 =*/136).int32(message.supplyType);
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId"))
                $root.proto.AccountID.encode(message.autoRenewAccountId, writer.uint32(/* id 18, wireType 2 =*/146).fork(), _depth + 1).ldelim();
            if (message.autoRenewSeconds != null && $Object.hasOwnProperty.call(message, "autoRenewSeconds") && (typeof message.autoRenewSeconds === "object" ? message.autoRenewSeconds.low || message.autoRenewSeconds.high : message.autoRenewSeconds !== 0))
                writer.uint32(/* id 19, wireType 0 =*/152).int64(message.autoRenewSeconds);
            if (message.expirationSecond != null && $Object.hasOwnProperty.call(message, "expirationSecond") && (typeof message.expirationSecond === "object" ? message.expirationSecond.low || message.expirationSecond.high : message.expirationSecond !== 0))
                writer.uint32(/* id 20, wireType 0 =*/160).int64(message.expirationSecond);
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo") && message.memo !== "")
                writer.uint32(/* id 21, wireType 2 =*/170).string(message.memo);
            if (message.maxSupply != null && $Object.hasOwnProperty.call(message, "maxSupply") && (typeof message.maxSupply === "object" ? message.maxSupply.low || message.maxSupply.high : message.maxSupply !== 0))
                writer.uint32(/* id 22, wireType 0 =*/176).int64(message.maxSupply);
            if (message.paused != null && $Object.hasOwnProperty.call(message, "paused") && message.paused !== false)
                writer.uint32(/* id 23, wireType 0 =*/184).bool(message.paused);
            if (message.accountsFrozenByDefault != null && $Object.hasOwnProperty.call(message, "accountsFrozenByDefault") && message.accountsFrozenByDefault !== false)
                writer.uint32(/* id 24, wireType 0 =*/192).bool(message.accountsFrozenByDefault);
            if (message.accountsKycGrantedByDefault != null && $Object.hasOwnProperty.call(message, "accountsKycGrantedByDefault") && message.accountsKycGrantedByDefault !== false)
                writer.uint32(/* id 25, wireType 0 =*/200).bool(message.accountsKycGrantedByDefault);
            if (message.customFees != null && message.customFees.length)
                for (let i = 0; i < message.customFees.length; ++i)
                    $root.proto.CustomFee.encode(message.customFees[i], writer.uint32(/* id 26, wireType 2 =*/210).fork(), _depth + 1).ldelim();
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata") && message.metadata.length)
                writer.uint32(/* id 27, wireType 2 =*/218).bytes(message.metadata);
            if (message.metadataKey != null && $Object.hasOwnProperty.call(message, "metadataKey"))
                $root.proto.Key.encode(message.metadataKey, writer.uint32(/* id 28, wireType 2 =*/226).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Token message, length delimited. Does not implicitly {@link proto.Token.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Token
         * @static
         * @param {proto.Token.$Properties} message Token message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Token.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a Token message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Token
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Token & proto.Token.$Shape} Token
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Token.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.Token();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.symbol = value;
                        else
                            delete message.symbol;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.decimals = value;
                        else
                            delete message.decimals;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.totalSupply = value;
                        else
                            delete message.totalSupply;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.treasuryAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.treasuryAccountId);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.adminKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.adminKey);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.kycKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.kycKey);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.freezeKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.freezeKey);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.wipeKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.wipeKey);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.supplyKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.supplyKey);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.feeScheduleKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.feeScheduleKey);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.pauseKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pauseKey);
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.lastUsedSerialNumber = value;
                        else
                            delete message.lastUsedSerialNumber;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.deleted = value;
                        else
                            delete message.deleted;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.tokenType = value;
                        else
                            delete message.tokenType;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.supplyType = value;
                        else
                            delete message.supplyType;
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        message.autoRenewAccountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.autoRenewAccountId);
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.autoRenewSeconds = value;
                        else
                            delete message.autoRenewSeconds;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.expirationSecond = value;
                        else
                            delete message.expirationSecond;
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.memo = value;
                        else
                            delete message.memo;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.maxSupply = value;
                        else
                            delete message.maxSupply;
                        continue;
                    }
                case 23: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.paused = value;
                        else
                            delete message.paused;
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.accountsFrozenByDefault = value;
                        else
                            delete message.accountsFrozenByDefault;
                        continue;
                    }
                case 25: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.accountsKycGrantedByDefault = value;
                        else
                            delete message.accountsKycGrantedByDefault;
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        if (!(message.customFees && message.customFees.length))
                            message.customFees = [];
                        message.customFees.push($root.proto.CustomFee.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 27: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.metadata = value;
                        else
                            delete message.metadata;
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        message.metadataKey = $root.proto.Key.decode(reader, reader.uint32(), $undefined, _depth + 1, message.metadataKey);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a Token message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Token
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Token & proto.Token.$Shape} Token
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Token.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Token message.
         * @function verify
         * @memberof proto.Token
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Token.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                if (!$util.isString(message.symbol))
                    return "symbol: string expected";
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals"))
                if (!$util.isInteger(message.decimals))
                    return "decimals: integer expected";
            if (message.totalSupply != null && $Object.hasOwnProperty.call(message, "totalSupply"))
                if (!$util.isInteger(message.totalSupply) && !(message.totalSupply && $util.isInteger(message.totalSupply.low) && $util.isInteger(message.totalSupply.high)))
                    return "totalSupply: integer|Long expected";
            if (message.treasuryAccountId != null && $Object.hasOwnProperty.call(message, "treasuryAccountId")) {
                let error = $root.proto.AccountID.verify(message.treasuryAccountId, _depth + 1);
                if (error)
                    return "treasuryAccountId." + error;
            }
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey")) {
                let error = $root.proto.Key.verify(message.adminKey, _depth + 1);
                if (error)
                    return "adminKey." + error;
            }
            if (message.kycKey != null && $Object.hasOwnProperty.call(message, "kycKey")) {
                let error = $root.proto.Key.verify(message.kycKey, _depth + 1);
                if (error)
                    return "kycKey." + error;
            }
            if (message.freezeKey != null && $Object.hasOwnProperty.call(message, "freezeKey")) {
                let error = $root.proto.Key.verify(message.freezeKey, _depth + 1);
                if (error)
                    return "freezeKey." + error;
            }
            if (message.wipeKey != null && $Object.hasOwnProperty.call(message, "wipeKey")) {
                let error = $root.proto.Key.verify(message.wipeKey, _depth + 1);
                if (error)
                    return "wipeKey." + error;
            }
            if (message.supplyKey != null && $Object.hasOwnProperty.call(message, "supplyKey")) {
                let error = $root.proto.Key.verify(message.supplyKey, _depth + 1);
                if (error)
                    return "supplyKey." + error;
            }
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey")) {
                let error = $root.proto.Key.verify(message.feeScheduleKey, _depth + 1);
                if (error)
                    return "feeScheduleKey." + error;
            }
            if (message.pauseKey != null && $Object.hasOwnProperty.call(message, "pauseKey")) {
                let error = $root.proto.Key.verify(message.pauseKey, _depth + 1);
                if (error)
                    return "pauseKey." + error;
            }
            if (message.lastUsedSerialNumber != null && $Object.hasOwnProperty.call(message, "lastUsedSerialNumber"))
                if (!$util.isInteger(message.lastUsedSerialNumber) && !(message.lastUsedSerialNumber && $util.isInteger(message.lastUsedSerialNumber.low) && $util.isInteger(message.lastUsedSerialNumber.high)))
                    return "lastUsedSerialNumber: integer|Long expected";
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                if (typeof message.deleted !== "boolean")
                    return "deleted: boolean expected";
            if (message.tokenType != null && $Object.hasOwnProperty.call(message, "tokenType"))
                if (typeof message.tokenType !== "number" || (message.tokenType | 0) !== message.tokenType)
                    return "tokenType: enum value expected";
            if (message.supplyType != null && $Object.hasOwnProperty.call(message, "supplyType"))
                if (typeof message.supplyType !== "number" || (message.supplyType | 0) !== message.supplyType)
                    return "supplyType: enum value expected";
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId")) {
                let error = $root.proto.AccountID.verify(message.autoRenewAccountId, _depth + 1);
                if (error)
                    return "autoRenewAccountId." + error;
            }
            if (message.autoRenewSeconds != null && $Object.hasOwnProperty.call(message, "autoRenewSeconds"))
                if (!$util.isInteger(message.autoRenewSeconds) && !(message.autoRenewSeconds && $util.isInteger(message.autoRenewSeconds.low) && $util.isInteger(message.autoRenewSeconds.high)))
                    return "autoRenewSeconds: integer|Long expected";
            if (message.expirationSecond != null && $Object.hasOwnProperty.call(message, "expirationSecond"))
                if (!$util.isInteger(message.expirationSecond) && !(message.expirationSecond && $util.isInteger(message.expirationSecond.low) && $util.isInteger(message.expirationSecond.high)))
                    return "expirationSecond: integer|Long expected";
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.maxSupply != null && $Object.hasOwnProperty.call(message, "maxSupply"))
                if (!$util.isInteger(message.maxSupply) && !(message.maxSupply && $util.isInteger(message.maxSupply.low) && $util.isInteger(message.maxSupply.high)))
                    return "maxSupply: integer|Long expected";
            if (message.paused != null && $Object.hasOwnProperty.call(message, "paused"))
                if (typeof message.paused !== "boolean")
                    return "paused: boolean expected";
            if (message.accountsFrozenByDefault != null && $Object.hasOwnProperty.call(message, "accountsFrozenByDefault"))
                if (typeof message.accountsFrozenByDefault !== "boolean")
                    return "accountsFrozenByDefault: boolean expected";
            if (message.accountsKycGrantedByDefault != null && $Object.hasOwnProperty.call(message, "accountsKycGrantedByDefault"))
                if (typeof message.accountsKycGrantedByDefault !== "boolean")
                    return "accountsKycGrantedByDefault: boolean expected";
            if (message.customFees != null && $Object.hasOwnProperty.call(message, "customFees")) {
                if (!$Array.isArray(message.customFees))
                    return "customFees: array expected";
                for (let i = 0; i < message.customFees.length; ++i) {
                    let error = $root.proto.CustomFee.verify(message.customFees[i], _depth + 1);
                    if (error)
                        return "customFees." + error;
                }
            }
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                if (!(message.metadata && typeof message.metadata.length === "number" || $util.isString(message.metadata)))
                    return "metadata: buffer expected";
            if (message.metadataKey != null && $Object.hasOwnProperty.call(message, "metadataKey")) {
                let error = $root.proto.Key.verify(message.metadataKey, _depth + 1);
                if (error)
                    return "metadataKey." + error;
            }
            return null;
        };

        /**
         * Creates a Token message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Token
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Token} Token
         */
        Token.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.Token)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.Token: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.Token();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.Token.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.symbol != null)
                if (typeof object.symbol !== "string" || object.symbol.length)
                    message.symbol = $String(object.symbol);
            if (object.decimals != null)
                if ($Number(object.decimals) !== 0)
                    message.decimals = object.decimals | 0;
            if (object.totalSupply != null)
                if (typeof object.totalSupply === "object" ? object.totalSupply.low || object.totalSupply.high : $Number(object.totalSupply) !== 0)
                    if ($util.Long)
                        message.totalSupply = $util.Long.fromValue(object.totalSupply, false);
                    else if (typeof object.totalSupply === "string")
                        message.totalSupply = $parseInt(object.totalSupply, 10);
                    else if (typeof object.totalSupply === "number")
                        message.totalSupply = object.totalSupply;
                    else if (typeof object.totalSupply === "object")
                        message.totalSupply = new $util.LongBits(object.totalSupply.low >>> 0, object.totalSupply.high >>> 0).toNumber();
            if (object.treasuryAccountId != null) {
                if (!$util.isObject(object.treasuryAccountId))
                    throw $TypeError(".proto.Token.treasuryAccountId: object expected");
                message.treasuryAccountId = $root.proto.AccountID.fromObject(object.treasuryAccountId, _depth + 1);
            }
            if (object.adminKey != null) {
                if (!$util.isObject(object.adminKey))
                    throw $TypeError(".proto.Token.adminKey: object expected");
                message.adminKey = $root.proto.Key.fromObject(object.adminKey, _depth + 1);
            }
            if (object.kycKey != null) {
                if (!$util.isObject(object.kycKey))
                    throw $TypeError(".proto.Token.kycKey: object expected");
                message.kycKey = $root.proto.Key.fromObject(object.kycKey, _depth + 1);
            }
            if (object.freezeKey != null) {
                if (!$util.isObject(object.freezeKey))
                    throw $TypeError(".proto.Token.freezeKey: object expected");
                message.freezeKey = $root.proto.Key.fromObject(object.freezeKey, _depth + 1);
            }
            if (object.wipeKey != null) {
                if (!$util.isObject(object.wipeKey))
                    throw $TypeError(".proto.Token.wipeKey: object expected");
                message.wipeKey = $root.proto.Key.fromObject(object.wipeKey, _depth + 1);
            }
            if (object.supplyKey != null) {
                if (!$util.isObject(object.supplyKey))
                    throw $TypeError(".proto.Token.supplyKey: object expected");
                message.supplyKey = $root.proto.Key.fromObject(object.supplyKey, _depth + 1);
            }
            if (object.feeScheduleKey != null) {
                if (!$util.isObject(object.feeScheduleKey))
                    throw $TypeError(".proto.Token.feeScheduleKey: object expected");
                message.feeScheduleKey = $root.proto.Key.fromObject(object.feeScheduleKey, _depth + 1);
            }
            if (object.pauseKey != null) {
                if (!$util.isObject(object.pauseKey))
                    throw $TypeError(".proto.Token.pauseKey: object expected");
                message.pauseKey = $root.proto.Key.fromObject(object.pauseKey, _depth + 1);
            }
            if (object.lastUsedSerialNumber != null)
                if (typeof object.lastUsedSerialNumber === "object" ? object.lastUsedSerialNumber.low || object.lastUsedSerialNumber.high : $Number(object.lastUsedSerialNumber) !== 0)
                    if ($util.Long)
                        message.lastUsedSerialNumber = $util.Long.fromValue(object.lastUsedSerialNumber, false);
                    else if (typeof object.lastUsedSerialNumber === "string")
                        message.lastUsedSerialNumber = $parseInt(object.lastUsedSerialNumber, 10);
                    else if (typeof object.lastUsedSerialNumber === "number")
                        message.lastUsedSerialNumber = object.lastUsedSerialNumber;
                    else if (typeof object.lastUsedSerialNumber === "object")
                        message.lastUsedSerialNumber = new $util.LongBits(object.lastUsedSerialNumber.low >>> 0, object.lastUsedSerialNumber.high >>> 0).toNumber();
            if (object.deleted != null)
                if (object.deleted)
                    message.deleted = $Boolean(object.deleted);
            if (object.tokenType !== 0 && (typeof object.tokenType !== "string" || $root.proto.TokenType[object.tokenType] !== 0))
                switch (object.tokenType) {
                case "FUNGIBLE_COMMON":
                case 0:
                    message.tokenType = 0;
                    break;
                case "NON_FUNGIBLE_UNIQUE":
                case 1:
                    message.tokenType = 1;
                    break;
                default:
                    if (typeof object.tokenType === "number" && (object.tokenType | 0) === object.tokenType)
                        message.tokenType = object.tokenType;
                }
            if (object.supplyType !== 0 && (typeof object.supplyType !== "string" || $root.proto.TokenSupplyType[object.supplyType] !== 0))
                switch (object.supplyType) {
                case "INFINITE":
                case 0:
                    message.supplyType = 0;
                    break;
                case "FINITE":
                case 1:
                    message.supplyType = 1;
                    break;
                default:
                    if (typeof object.supplyType === "number" && (object.supplyType | 0) === object.supplyType)
                        message.supplyType = object.supplyType;
                }
            if (object.autoRenewAccountId != null) {
                if (!$util.isObject(object.autoRenewAccountId))
                    throw $TypeError(".proto.Token.autoRenewAccountId: object expected");
                message.autoRenewAccountId = $root.proto.AccountID.fromObject(object.autoRenewAccountId, _depth + 1);
            }
            if (object.autoRenewSeconds != null)
                if (typeof object.autoRenewSeconds === "object" ? object.autoRenewSeconds.low || object.autoRenewSeconds.high : $Number(object.autoRenewSeconds) !== 0)
                    if ($util.Long)
                        message.autoRenewSeconds = $util.Long.fromValue(object.autoRenewSeconds, false);
                    else if (typeof object.autoRenewSeconds === "string")
                        message.autoRenewSeconds = $parseInt(object.autoRenewSeconds, 10);
                    else if (typeof object.autoRenewSeconds === "number")
                        message.autoRenewSeconds = object.autoRenewSeconds;
                    else if (typeof object.autoRenewSeconds === "object")
                        message.autoRenewSeconds = new $util.LongBits(object.autoRenewSeconds.low >>> 0, object.autoRenewSeconds.high >>> 0).toNumber();
            if (object.expirationSecond != null)
                if (typeof object.expirationSecond === "object" ? object.expirationSecond.low || object.expirationSecond.high : $Number(object.expirationSecond) !== 0)
                    if ($util.Long)
                        message.expirationSecond = $util.Long.fromValue(object.expirationSecond, false);
                    else if (typeof object.expirationSecond === "string")
                        message.expirationSecond = $parseInt(object.expirationSecond, 10);
                    else if (typeof object.expirationSecond === "number")
                        message.expirationSecond = object.expirationSecond;
                    else if (typeof object.expirationSecond === "object")
                        message.expirationSecond = new $util.LongBits(object.expirationSecond.low >>> 0, object.expirationSecond.high >>> 0).toNumber();
            if (object.memo != null)
                if (typeof object.memo !== "string" || object.memo.length)
                    message.memo = $String(object.memo);
            if (object.maxSupply != null)
                if (typeof object.maxSupply === "object" ? object.maxSupply.low || object.maxSupply.high : $Number(object.maxSupply) !== 0)
                    if ($util.Long)
                        message.maxSupply = $util.Long.fromValue(object.maxSupply, false);
                    else if (typeof object.maxSupply === "string")
                        message.maxSupply = $parseInt(object.maxSupply, 10);
                    else if (typeof object.maxSupply === "number")
                        message.maxSupply = object.maxSupply;
                    else if (typeof object.maxSupply === "object")
                        message.maxSupply = new $util.LongBits(object.maxSupply.low >>> 0, object.maxSupply.high >>> 0).toNumber();
            if (object.paused != null)
                if (object.paused)
                    message.paused = $Boolean(object.paused);
            if (object.accountsFrozenByDefault != null)
                if (object.accountsFrozenByDefault)
                    message.accountsFrozenByDefault = $Boolean(object.accountsFrozenByDefault);
            if (object.accountsKycGrantedByDefault != null)
                if (object.accountsKycGrantedByDefault)
                    message.accountsKycGrantedByDefault = $Boolean(object.accountsKycGrantedByDefault);
            if (object.customFees) {
                if (!$Array.isArray(object.customFees))
                    throw $TypeError(".proto.Token.customFees: array expected");
                message.customFees = $Array(object.customFees.length);
                for (let i = 0; i < object.customFees.length; ++i) {
                    if (!$util.isObject(object.customFees[i]))
                        throw $TypeError(".proto.Token.customFees: object expected");
                    message.customFees[i] = $root.proto.CustomFee.fromObject(object.customFees[i], _depth + 1);
                }
            }
            if (object.metadata != null)
                if (object.metadata.length)
                    if (typeof object.metadata === "string")
                        $util.base64.decode(object.metadata, message.metadata = $util.newBuffer($util.base64.length(object.metadata)), 0);
                    else if (object.metadata.length >= 0)
                        message.metadata = object.metadata;
            if (object.metadataKey != null) {
                if (!$util.isObject(object.metadataKey))
                    throw $TypeError(".proto.Token.metadataKey: object expected");
                message.metadataKey = $root.proto.Key.fromObject(object.metadataKey, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Token message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Token
         * @static
         * @param {proto.Token} message Token
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Token.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.customFees = [];
            if (options.defaults) {
                object.tokenId = null;
                object.name = "";
                object.symbol = "";
                object.decimals = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.totalSupply = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.totalSupply = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.treasuryAccountId = null;
                object.adminKey = null;
                object.kycKey = null;
                object.freezeKey = null;
                object.wipeKey = null;
                object.supplyKey = null;
                object.feeScheduleKey = null;
                object.pauseKey = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastUsedSerialNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.lastUsedSerialNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.deleted = false;
                object.tokenType = options.enums === $String ? "FUNGIBLE_COMMON" : 0;
                object.supplyType = options.enums === $String ? "INFINITE" : 0;
                object.autoRenewAccountId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.autoRenewSeconds = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.autoRenewSeconds = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expirationSecond = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.expirationSecond = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.memo = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxSupply = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.maxSupply = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.paused = false;
                object.accountsFrozenByDefault = false;
                object.accountsKycGrantedByDefault = false;
                if (options.bytes === $String)
                    object.metadata = "";
                else {
                    object.metadata = [];
                    if (options.bytes !== $Array)
                        object.metadata = $util.newBuffer(object.metadata);
                }
                object.metadataKey = null;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.symbol != null && $Object.hasOwnProperty.call(message, "symbol"))
                object.symbol = message.symbol;
            if (message.decimals != null && $Object.hasOwnProperty.call(message, "decimals"))
                object.decimals = message.decimals;
            if (message.totalSupply != null && $Object.hasOwnProperty.call(message, "totalSupply"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.totalSupply = typeof message.totalSupply === "number" ? $BigInt(message.totalSupply) : $util.Long.fromBits(message.totalSupply.low >>> 0, message.totalSupply.high >>> 0, false).toBigInt();
                else if (typeof message.totalSupply === "number")
                    object.totalSupply = options.longs === $String ? $String(message.totalSupply) : message.totalSupply;
                else
                    object.totalSupply = options.longs === $String ? $util.Long.prototype.toString.call(message.totalSupply) : options.longs === $Number ? new $util.LongBits(message.totalSupply.low >>> 0, message.totalSupply.high >>> 0).toNumber() : message.totalSupply;
            if (message.treasuryAccountId != null && $Object.hasOwnProperty.call(message, "treasuryAccountId"))
                object.treasuryAccountId = $root.proto.AccountID.toObject(message.treasuryAccountId, options, _depth + 1);
            if (message.adminKey != null && $Object.hasOwnProperty.call(message, "adminKey"))
                object.adminKey = $root.proto.Key.toObject(message.adminKey, options, _depth + 1);
            if (message.kycKey != null && $Object.hasOwnProperty.call(message, "kycKey"))
                object.kycKey = $root.proto.Key.toObject(message.kycKey, options, _depth + 1);
            if (message.freezeKey != null && $Object.hasOwnProperty.call(message, "freezeKey"))
                object.freezeKey = $root.proto.Key.toObject(message.freezeKey, options, _depth + 1);
            if (message.wipeKey != null && $Object.hasOwnProperty.call(message, "wipeKey"))
                object.wipeKey = $root.proto.Key.toObject(message.wipeKey, options, _depth + 1);
            if (message.supplyKey != null && $Object.hasOwnProperty.call(message, "supplyKey"))
                object.supplyKey = $root.proto.Key.toObject(message.supplyKey, options, _depth + 1);
            if (message.feeScheduleKey != null && $Object.hasOwnProperty.call(message, "feeScheduleKey"))
                object.feeScheduleKey = $root.proto.Key.toObject(message.feeScheduleKey, options, _depth + 1);
            if (message.pauseKey != null && $Object.hasOwnProperty.call(message, "pauseKey"))
                object.pauseKey = $root.proto.Key.toObject(message.pauseKey, options, _depth + 1);
            if (message.lastUsedSerialNumber != null && $Object.hasOwnProperty.call(message, "lastUsedSerialNumber"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.lastUsedSerialNumber = typeof message.lastUsedSerialNumber === "number" ? $BigInt(message.lastUsedSerialNumber) : $util.Long.fromBits(message.lastUsedSerialNumber.low >>> 0, message.lastUsedSerialNumber.high >>> 0, false).toBigInt();
                else if (typeof message.lastUsedSerialNumber === "number")
                    object.lastUsedSerialNumber = options.longs === $String ? $String(message.lastUsedSerialNumber) : message.lastUsedSerialNumber;
                else
                    object.lastUsedSerialNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.lastUsedSerialNumber) : options.longs === $Number ? new $util.LongBits(message.lastUsedSerialNumber.low >>> 0, message.lastUsedSerialNumber.high >>> 0).toNumber() : message.lastUsedSerialNumber;
            if (message.deleted != null && $Object.hasOwnProperty.call(message, "deleted"))
                object.deleted = message.deleted;
            if (message.tokenType != null && $Object.hasOwnProperty.call(message, "tokenType"))
                object.tokenType = options.enums === $String ? $root.proto.TokenType[message.tokenType] === $undefined ? message.tokenType : $root.proto.TokenType[message.tokenType] : message.tokenType;
            if (message.supplyType != null && $Object.hasOwnProperty.call(message, "supplyType"))
                object.supplyType = options.enums === $String ? $root.proto.TokenSupplyType[message.supplyType] === $undefined ? message.supplyType : $root.proto.TokenSupplyType[message.supplyType] : message.supplyType;
            if (message.autoRenewAccountId != null && $Object.hasOwnProperty.call(message, "autoRenewAccountId"))
                object.autoRenewAccountId = $root.proto.AccountID.toObject(message.autoRenewAccountId, options, _depth + 1);
            if (message.autoRenewSeconds != null && $Object.hasOwnProperty.call(message, "autoRenewSeconds"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.autoRenewSeconds = typeof message.autoRenewSeconds === "number" ? $BigInt(message.autoRenewSeconds) : $util.Long.fromBits(message.autoRenewSeconds.low >>> 0, message.autoRenewSeconds.high >>> 0, false).toBigInt();
                else if (typeof message.autoRenewSeconds === "number")
                    object.autoRenewSeconds = options.longs === $String ? $String(message.autoRenewSeconds) : message.autoRenewSeconds;
                else
                    object.autoRenewSeconds = options.longs === $String ? $util.Long.prototype.toString.call(message.autoRenewSeconds) : options.longs === $Number ? new $util.LongBits(message.autoRenewSeconds.low >>> 0, message.autoRenewSeconds.high >>> 0).toNumber() : message.autoRenewSeconds;
            if (message.expirationSecond != null && $Object.hasOwnProperty.call(message, "expirationSecond"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.expirationSecond = typeof message.expirationSecond === "number" ? $BigInt(message.expirationSecond) : $util.Long.fromBits(message.expirationSecond.low >>> 0, message.expirationSecond.high >>> 0, false).toBigInt();
                else if (typeof message.expirationSecond === "number")
                    object.expirationSecond = options.longs === $String ? $String(message.expirationSecond) : message.expirationSecond;
                else
                    object.expirationSecond = options.longs === $String ? $util.Long.prototype.toString.call(message.expirationSecond) : options.longs === $Number ? new $util.LongBits(message.expirationSecond.low >>> 0, message.expirationSecond.high >>> 0).toNumber() : message.expirationSecond;
            if (message.memo != null && $Object.hasOwnProperty.call(message, "memo"))
                object.memo = message.memo;
            if (message.maxSupply != null && $Object.hasOwnProperty.call(message, "maxSupply"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.maxSupply = typeof message.maxSupply === "number" ? $BigInt(message.maxSupply) : $util.Long.fromBits(message.maxSupply.low >>> 0, message.maxSupply.high >>> 0, false).toBigInt();
                else if (typeof message.maxSupply === "number")
                    object.maxSupply = options.longs === $String ? $String(message.maxSupply) : message.maxSupply;
                else
                    object.maxSupply = options.longs === $String ? $util.Long.prototype.toString.call(message.maxSupply) : options.longs === $Number ? new $util.LongBits(message.maxSupply.low >>> 0, message.maxSupply.high >>> 0).toNumber() : message.maxSupply;
            if (message.paused != null && $Object.hasOwnProperty.call(message, "paused"))
                object.paused = message.paused;
            if (message.accountsFrozenByDefault != null && $Object.hasOwnProperty.call(message, "accountsFrozenByDefault"))
                object.accountsFrozenByDefault = message.accountsFrozenByDefault;
            if (message.accountsKycGrantedByDefault != null && $Object.hasOwnProperty.call(message, "accountsKycGrantedByDefault"))
                object.accountsKycGrantedByDefault = message.accountsKycGrantedByDefault;
            if (message.customFees && message.customFees.length) {
                object.customFees = $Array(message.customFees.length);
                for (let j = 0; j < message.customFees.length; ++j)
                    object.customFees[j] = $root.proto.CustomFee.toObject(message.customFees[j], options, _depth + 1);
            }
            if (message.metadata != null && $Object.hasOwnProperty.call(message, "metadata"))
                object.metadata = options.bytes === $String ? $util.base64.encode(message.metadata, 0, message.metadata.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.metadata) : message.metadata;
            if (message.metadataKey != null && $Object.hasOwnProperty.call(message, "metadataKey"))
                object.metadataKey = $root.proto.Key.toObject(message.metadataKey, options, _depth + 1);
            return object;
        };

        /**
         * Converts this Token to JSON.
         * @function toJSON
         * @memberof proto.Token
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Token.prototype.toJSON = function() {
            return Token.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for Token
         * @function getTypeUrl
         * @memberof proto.Token
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        Token.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Token";
        };

        return Token;
    })();

    proto.TokenRelation = (function() {

        /**
         * Properties of a TokenRelation.
         * @typedef {Object} proto.TokenRelation.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] A token identifier.
         * <p>
         * This SHALL identify the token involved in this association.
         * @property {proto.AccountID.$Properties|null} [accountId] An account identifier.
         * <p>
         * This SHALL identify the account involved in this association.
         * @property {number|Long|null} [balance] The fungible token balance of this token relationship.
         * <p>
         * This MUST be a whole number.
         * @property {boolean|null} [frozen] A flag indicating that this token relationship is frozen.
         * <p>
         * When a token relationship is frozen the associated account SHALL NOT be
         * permitted to transfer to or from the associated balance.
         * <p>
         * This flag is associated with the Token value `freeze_key`, and any
         * transaction to set this flag MUST be signed by that key. If the Token
         * does not have a `freeze_key` set, then this flag SHALL NOT be set true
         * for relationships between accounts and that token.
         * @property {boolean|null} [kycGranted] A flag indicating that this token relationship has been granted KYC status.
         * <p>
         * If the token flag `accounts_kyc_granted_by_default` is set true, then
         * this flag SHALL be set true for all accounts subsequently associated to
         * that token. Otherwise this flag SHALL NOT be set until a transaction
         * is submitted, and signed with the Token `kyc_key` to
         * set the flag true.<br/>
         * If the Token does not have a `kyc_key` set and the token flag
         * `accounts_kyc_granted_by_default` is not set true, then this value MUST
         * be false for all accounts subsequently associated to that token.
         * <p>
         * Typically a transaction to set this value to true is considered
         * equivalent to asserting that the "Know Your Customer" (KYC) requirements
         * have been met for this combination of account and token and the relevant
         * records are available as required.
         * @property {boolean|null} [automaticAssociation] A flag indicating that this token relationship was created using
         * automatic association.
         * <p>
         * If this is true then there MUST NOT exist a customer-signed transaction
         * associating this account and token combination and the account
         * `used_auto_associations` SHALL be incremented when this relationship
         * is created.
         * @property {proto.TokenID.$Properties|null} [previousToken] The Token ID of the previous entry in the associated Account's
         * "virtual double-linked list" of token relationships.
         * <p>
         * This must be combined with the value of `account_id` to identify the
         * actual `TokenRelation` referenced.
         * @property {proto.TokenID.$Properties|null} [nextToken] The Token ID of the next entry in the associated Account's "virtual
         * double-linked list" of token relationships.
         * <p>
         * This must be combined with the value of `account_id` to identify the
         * actual `TokenRelation` referenced.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenRelation.
         * @memberof proto
         * @interface ITokenRelation
         * @augments proto.TokenRelation.$Properties
         * @deprecated Use proto.TokenRelation.$Properties instead.
         */

        /**
         * Shape of a TokenRelation.
         * @typedef {{
         *   tokenId?: proto.TokenID.$Shape|null;
         *   accountId?: proto.AccountID.$Shape|null;
         *   balance?: number|Long|null;
         *   frozen?: boolean|null;
         *   kycGranted?: boolean|null;
         *   automaticAssociation?: boolean|null;
         *   previousToken?: proto.TokenID.$Shape|null;
         *   nextToken?: proto.TokenID.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.TokenRelation.$Shape
         */

        /**
         * Constructs a new TokenRelation.
         * @memberof proto
         * @classdesc An Hedera Token Service token relationship.
         * 
         * A token relationship connects an Account with a Token and is necessary for
         * that Account to transact in that Token. TokenRelationship defines a
         * connection between one account and one token type.
         * 
         * A TokenRelation SHALL be identified by the combination of token_id and
         * account_id.<br/>
         * A TokenRelation SHALL contain, for the referenced token,<br/>
         * The account's current balance, whether the account has KYC granted,
         * and whether the assets are frozen.
         * 
         * TokenRelation entries SHALL be connected via a "virtual linked list" with the
         * next TokenID and previous TokenID stored in the TokenRelation.
         * These TokenIDs MUST be combined with the AccountID to find the next or
         * previous relationship in the list.
         * @constructor
         * @param {proto.TokenRelation.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenRelation = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A token identifier.
         * <p>
         * This SHALL identify the token involved in this association.
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.TokenRelation
         * @instance
         */
        TokenRelation.prototype.tokenId = null;

        /**
         * An account identifier.
         * <p>
         * This SHALL identify the account involved in this association.
         * @member {proto.AccountID.$Properties|null|undefined} accountId
         * @memberof proto.TokenRelation
         * @instance
         */
        TokenRelation.prototype.accountId = null;

        /**
         * The fungible token balance of this token relationship.
         * <p>
         * This MUST be a whole number.
         * @member {number|Long} balance
         * @memberof proto.TokenRelation
         * @instance
         */
        TokenRelation.prototype.balance = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * A flag indicating that this token relationship is frozen.
         * <p>
         * When a token relationship is frozen the associated account SHALL NOT be
         * permitted to transfer to or from the associated balance.
         * <p>
         * This flag is associated with the Token value `freeze_key`, and any
         * transaction to set this flag MUST be signed by that key. If the Token
         * does not have a `freeze_key` set, then this flag SHALL NOT be set true
         * for relationships between accounts and that token.
         * @member {boolean} frozen
         * @memberof proto.TokenRelation
         * @instance
         */
        TokenRelation.prototype.frozen = false;

        /**
         * A flag indicating that this token relationship has been granted KYC status.
         * <p>
         * If the token flag `accounts_kyc_granted_by_default` is set true, then
         * this flag SHALL be set true for all accounts subsequently associated to
         * that token. Otherwise this flag SHALL NOT be set until a transaction
         * is submitted, and signed with the Token `kyc_key` to
         * set the flag true.<br/>
         * If the Token does not have a `kyc_key` set and the token flag
         * `accounts_kyc_granted_by_default` is not set true, then this value MUST
         * be false for all accounts subsequently associated to that token.
         * <p>
         * Typically a transaction to set this value to true is considered
         * equivalent to asserting that the "Know Your Customer" (KYC) requirements
         * have been met for this combination of account and token and the relevant
         * records are available as required.
         * @member {boolean} kycGranted
         * @memberof proto.TokenRelation
         * @instance
         */
        TokenRelation.prototype.kycGranted = false;

        /**
         * A flag indicating that this token relationship was created using
         * automatic association.
         * <p>
         * If this is true then there MUST NOT exist a customer-signed transaction
         * associating this account and token combination and the account
         * `used_auto_associations` SHALL be incremented when this relationship
         * is created.
         * @member {boolean} automaticAssociation
         * @memberof proto.TokenRelation
         * @instance
         */
        TokenRelation.prototype.automaticAssociation = false;

        /**
         * The Token ID of the previous entry in the associated Account's
         * "virtual double-linked list" of token relationships.
         * <p>
         * This must be combined with the value of `account_id` to identify the
         * actual `TokenRelation` referenced.
         * @member {proto.TokenID.$Properties|null|undefined} previousToken
         * @memberof proto.TokenRelation
         * @instance
         */
        TokenRelation.prototype.previousToken = null;

        /**
         * The Token ID of the next entry in the associated Account's "virtual
         * double-linked list" of token relationships.
         * <p>
         * This must be combined with the value of `account_id` to identify the
         * actual `TokenRelation` referenced.
         * @member {proto.TokenID.$Properties|null|undefined} nextToken
         * @memberof proto.TokenRelation
         * @instance
         */
        TokenRelation.prototype.nextToken = null;

        /**
         * Creates a new TokenRelation instance using the specified properties.
         * @function create
         * @memberof proto.TokenRelation
         * @static
         * @param {proto.TokenRelation.$Properties=} [properties] Properties to set
         * @returns {proto.TokenRelation} TokenRelation instance
         * @type {{
         *   (properties: proto.TokenRelation.$Shape): proto.TokenRelation & proto.TokenRelation.$Shape;
         *   (properties?: proto.TokenRelation.$Properties): proto.TokenRelation;
         * }}
         */
        TokenRelation.create = function(properties) {
            return new TokenRelation(properties);
        };

        /**
         * Encodes the specified TokenRelation message. Does not implicitly {@link proto.TokenRelation.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenRelation
         * @static
         * @param {proto.TokenRelation.$Properties} message TokenRelation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenRelation.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                $root.proto.AccountID.encode(message.accountId, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance") && (typeof message.balance === "object" ? message.balance.low || message.balance.high : message.balance !== 0))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.balance);
            if (message.frozen != null && $Object.hasOwnProperty.call(message, "frozen") && message.frozen !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.frozen);
            if (message.kycGranted != null && $Object.hasOwnProperty.call(message, "kycGranted") && message.kycGranted !== false)
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.kycGranted);
            if (message.automaticAssociation != null && $Object.hasOwnProperty.call(message, "automaticAssociation") && message.automaticAssociation !== false)
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.automaticAssociation);
            if (message.previousToken != null && $Object.hasOwnProperty.call(message, "previousToken"))
                $root.proto.TokenID.encode(message.previousToken, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.nextToken != null && $Object.hasOwnProperty.call(message, "nextToken"))
                $root.proto.TokenID.encode(message.nextToken, writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenRelation message, length delimited. Does not implicitly {@link proto.TokenRelation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenRelation
         * @static
         * @param {proto.TokenRelation.$Properties} message TokenRelation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenRelation.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenRelation message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenRelation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenRelation & proto.TokenRelation.$Shape} TokenRelation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenRelation.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenRelation();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.accountId = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountId);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.balance = value;
                        else
                            delete message.balance;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.frozen = value;
                        else
                            delete message.frozen;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.kycGranted = value;
                        else
                            delete message.kycGranted;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.automaticAssociation = value;
                        else
                            delete message.automaticAssociation;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.previousToken = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.previousToken);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.nextToken = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.nextToken);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenRelation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenRelation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenRelation & proto.TokenRelation.$Shape} TokenRelation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenRelation.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenRelation message.
         * @function verify
         * @memberof proto.TokenRelation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenRelation.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId")) {
                let error = $root.proto.AccountID.verify(message.accountId, _depth + 1);
                if (error)
                    return "accountId." + error;
            }
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (!$util.isInteger(message.balance) && !(message.balance && $util.isInteger(message.balance.low) && $util.isInteger(message.balance.high)))
                    return "balance: integer|Long expected";
            if (message.frozen != null && $Object.hasOwnProperty.call(message, "frozen"))
                if (typeof message.frozen !== "boolean")
                    return "frozen: boolean expected";
            if (message.kycGranted != null && $Object.hasOwnProperty.call(message, "kycGranted"))
                if (typeof message.kycGranted !== "boolean")
                    return "kycGranted: boolean expected";
            if (message.automaticAssociation != null && $Object.hasOwnProperty.call(message, "automaticAssociation"))
                if (typeof message.automaticAssociation !== "boolean")
                    return "automaticAssociation: boolean expected";
            if (message.previousToken != null && $Object.hasOwnProperty.call(message, "previousToken")) {
                let error = $root.proto.TokenID.verify(message.previousToken, _depth + 1);
                if (error)
                    return "previousToken." + error;
            }
            if (message.nextToken != null && $Object.hasOwnProperty.call(message, "nextToken")) {
                let error = $root.proto.TokenID.verify(message.nextToken, _depth + 1);
                if (error)
                    return "nextToken." + error;
            }
            return null;
        };

        /**
         * Creates a TokenRelation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenRelation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenRelation} TokenRelation
         */
        TokenRelation.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenRelation)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenRelation: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenRelation();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.TokenRelation.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.accountId != null) {
                if (!$util.isObject(object.accountId))
                    throw $TypeError(".proto.TokenRelation.accountId: object expected");
                message.accountId = $root.proto.AccountID.fromObject(object.accountId, _depth + 1);
            }
            if (object.balance != null)
                if (typeof object.balance === "object" ? object.balance.low || object.balance.high : $Number(object.balance) !== 0)
                    if ($util.Long)
                        message.balance = $util.Long.fromValue(object.balance, false);
                    else if (typeof object.balance === "string")
                        message.balance = $parseInt(object.balance, 10);
                    else if (typeof object.balance === "number")
                        message.balance = object.balance;
                    else if (typeof object.balance === "object")
                        message.balance = new $util.LongBits(object.balance.low >>> 0, object.balance.high >>> 0).toNumber();
            if (object.frozen != null)
                if (object.frozen)
                    message.frozen = $Boolean(object.frozen);
            if (object.kycGranted != null)
                if (object.kycGranted)
                    message.kycGranted = $Boolean(object.kycGranted);
            if (object.automaticAssociation != null)
                if (object.automaticAssociation)
                    message.automaticAssociation = $Boolean(object.automaticAssociation);
            if (object.previousToken != null) {
                if (!$util.isObject(object.previousToken))
                    throw $TypeError(".proto.TokenRelation.previousToken: object expected");
                message.previousToken = $root.proto.TokenID.fromObject(object.previousToken, _depth + 1);
            }
            if (object.nextToken != null) {
                if (!$util.isObject(object.nextToken))
                    throw $TypeError(".proto.TokenRelation.nextToken: object expected");
                message.nextToken = $root.proto.TokenID.fromObject(object.nextToken, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenRelation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenRelation
         * @static
         * @param {proto.TokenRelation} message TokenRelation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenRelation.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.tokenId = null;
                object.accountId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.frozen = false;
                object.kycGranted = false;
                object.automaticAssociation = false;
                object.previousToken = null;
                object.nextToken = null;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.accountId != null && $Object.hasOwnProperty.call(message, "accountId"))
                object.accountId = $root.proto.AccountID.toObject(message.accountId, options, _depth + 1);
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.balance = typeof message.balance === "number" ? $BigInt(message.balance) : $util.Long.fromBits(message.balance.low >>> 0, message.balance.high >>> 0, false).toBigInt();
                else if (typeof message.balance === "number")
                    object.balance = options.longs === $String ? $String(message.balance) : message.balance;
                else
                    object.balance = options.longs === $String ? $util.Long.prototype.toString.call(message.balance) : options.longs === $Number ? new $util.LongBits(message.balance.low >>> 0, message.balance.high >>> 0).toNumber() : message.balance;
            if (message.frozen != null && $Object.hasOwnProperty.call(message, "frozen"))
                object.frozen = message.frozen;
            if (message.kycGranted != null && $Object.hasOwnProperty.call(message, "kycGranted"))
                object.kycGranted = message.kycGranted;
            if (message.automaticAssociation != null && $Object.hasOwnProperty.call(message, "automaticAssociation"))
                object.automaticAssociation = message.automaticAssociation;
            if (message.previousToken != null && $Object.hasOwnProperty.call(message, "previousToken"))
                object.previousToken = $root.proto.TokenID.toObject(message.previousToken, options, _depth + 1);
            if (message.nextToken != null && $Object.hasOwnProperty.call(message, "nextToken"))
                object.nextToken = $root.proto.TokenID.toObject(message.nextToken, options, _depth + 1);
            return object;
        };

        /**
         * Converts this TokenRelation to JSON.
         * @function toJSON
         * @memberof proto.TokenRelation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenRelation.prototype.toJSON = function() {
            return TokenRelation.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenRelation
         * @function getTypeUrl
         * @memberof proto.TokenRelation
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenRelation.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenRelation";
        };

        return TokenRelation;
    })();

    proto.ThrottleGroup = (function() {

        /**
         * Properties of a ThrottleGroup.
         * @typedef {Object} proto.ThrottleGroup.$Properties
         * @property {Array.<proto.HederaFunctionality>|null} [operations] A list of operations to be throttled.
         * <p>
         * This list MUST contain at least one item.<br/>
         * This list SHOULD NOT contain any item included in any other
         * active `ThrottleGroup`.
         * @property {number|Long|null} [milliOpsPerSec] A throttle limit for this group.<br/>
         * This is a total number of operations, in thousandths, the network may
         * perform each second for this group. Every node executes every transaction,
         * so this limit effectively applies individually to each node as well.<br/>
         * <p>
         * This value MUST be greater than zero (`0`).<br/>
         * This value SHOULD be less than `9,223,372`.<br/>
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ThrottleGroup.
         * @memberof proto
         * @interface IThrottleGroup
         * @augments proto.ThrottleGroup.$Properties
         * @deprecated Use proto.ThrottleGroup.$Properties instead.
         */

        /**
         * Shape of a ThrottleGroup.
         * @typedef {proto.ThrottleGroup.$Properties} proto.ThrottleGroup.$Shape
         */

        /**
         * Constructs a new ThrottleGroup.
         * @memberof proto
         * @classdesc A single throttle limit applied to one or more operations.
         * 
         * The list of operations MUST contain at least one entry.<br/>
         * The throttle limit SHALL be specified in thousandths of an operation
         * per second; one operation per second for the network would be `1000`.<br/>
         * The throttle limit MUST be greater than zero (`0`).
         * @constructor
         * @param {proto.ThrottleGroup.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ThrottleGroup = function (properties) {
            this.operations = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of operations to be throttled.
         * <p>
         * This list MUST contain at least one item.<br/>
         * This list SHOULD NOT contain any item included in any other
         * active `ThrottleGroup`.
         * @member {Array.<proto.HederaFunctionality>} operations
         * @memberof proto.ThrottleGroup
         * @instance
         */
        ThrottleGroup.prototype.operations = $util.emptyArray;

        /**
         * A throttle limit for this group.<br/>
         * This is a total number of operations, in thousandths, the network may
         * perform each second for this group. Every node executes every transaction,
         * so this limit effectively applies individually to each node as well.<br/>
         * <p>
         * This value MUST be greater than zero (`0`).<br/>
         * This value SHOULD be less than `9,223,372`.<br/>
         * @member {number|Long} milliOpsPerSec
         * @memberof proto.ThrottleGroup
         * @instance
         */
        ThrottleGroup.prototype.milliOpsPerSec = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new ThrottleGroup instance using the specified properties.
         * @function create
         * @memberof proto.ThrottleGroup
         * @static
         * @param {proto.ThrottleGroup.$Properties=} [properties] Properties to set
         * @returns {proto.ThrottleGroup} ThrottleGroup instance
         * @type {{
         *   (properties: proto.ThrottleGroup.$Shape): proto.ThrottleGroup & proto.ThrottleGroup.$Shape;
         *   (properties?: proto.ThrottleGroup.$Properties): proto.ThrottleGroup;
         * }}
         */
        ThrottleGroup.create = function(properties) {
            return new ThrottleGroup(properties);
        };

        /**
         * Encodes the specified ThrottleGroup message. Does not implicitly {@link proto.ThrottleGroup.verify|verify} messages.
         * @function encode
         * @memberof proto.ThrottleGroup
         * @static
         * @param {proto.ThrottleGroup.$Properties} message ThrottleGroup message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottleGroup.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.operations != null && message.operations.length)
                writer.uint32(/* id 1, wireType 2 =*/10).int32s(message.operations);
            if (message.milliOpsPerSec != null && $Object.hasOwnProperty.call(message, "milliOpsPerSec") && (typeof message.milliOpsPerSec === "object" ? message.milliOpsPerSec.low || message.milliOpsPerSec.high : message.milliOpsPerSec !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.milliOpsPerSec);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ThrottleGroup message, length delimited. Does not implicitly {@link proto.ThrottleGroup.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ThrottleGroup
         * @static
         * @param {proto.ThrottleGroup.$Properties} message ThrottleGroup message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottleGroup.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ThrottleGroup message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ThrottleGroup
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ThrottleGroup & proto.ThrottleGroup.$Shape} ThrottleGroup
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottleGroup.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ThrottleGroup();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.operations && message.operations.length))
                                message.operations = [];
                            reader.int32s(message.operations);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.operations && message.operations.length))
                            message.operations = [];
                        message.operations.push(reader.int32());
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.milliOpsPerSec = value;
                        else
                            delete message.milliOpsPerSec;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ThrottleGroup message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ThrottleGroup
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ThrottleGroup & proto.ThrottleGroup.$Shape} ThrottleGroup
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottleGroup.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ThrottleGroup message.
         * @function verify
         * @memberof proto.ThrottleGroup
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ThrottleGroup.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.operations != null && $Object.hasOwnProperty.call(message, "operations")) {
                if (!$Array.isArray(message.operations))
                    return "operations: array expected";
                for (let i = 0; i < message.operations.length; ++i)
                    if (typeof message.operations[i] !== "number" || (message.operations[i] | 0) !== message.operations[i])
                        return "operations: enum value[] expected";
            }
            if (message.milliOpsPerSec != null && $Object.hasOwnProperty.call(message, "milliOpsPerSec"))
                if (!$util.isInteger(message.milliOpsPerSec) && !(message.milliOpsPerSec && $util.isInteger(message.milliOpsPerSec.low) && $util.isInteger(message.milliOpsPerSec.high)))
                    return "milliOpsPerSec: integer|Long expected";
            return null;
        };

        /**
         * Creates a ThrottleGroup message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ThrottleGroup
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ThrottleGroup} ThrottleGroup
         */
        ThrottleGroup.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ThrottleGroup)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ThrottleGroup: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ThrottleGroup();
            if (object.operations) {
                if (!$Array.isArray(object.operations))
                    throw $TypeError(".proto.ThrottleGroup.operations: array expected");
                message.operations = [];
                for (let i = 0; i < object.operations.length; ++i)
                    switch (object.operations[i]) {
                    case "NONE":
                    case 0:
                        message.operations[message.operations.length] = 0;
                        break;
                    case "CryptoTransfer":
                    case 1:
                        message.operations[message.operations.length] = 1;
                        break;
                    case "CryptoUpdate":
                    case 2:
                        message.operations[message.operations.length] = 2;
                        break;
                    case "CryptoDelete":
                    case 3:
                        message.operations[message.operations.length] = 3;
                        break;
                    case "CryptoAddLiveHash":
                    case 4:
                        message.operations[message.operations.length] = 4;
                        break;
                    case "CryptoDeleteLiveHash":
                    case 5:
                        message.operations[message.operations.length] = 5;
                        break;
                    case "ContractCall":
                    case 6:
                        message.operations[message.operations.length] = 6;
                        break;
                    case "ContractCreate":
                    case 7:
                        message.operations[message.operations.length] = 7;
                        break;
                    case "ContractUpdate":
                    case 8:
                        message.operations[message.operations.length] = 8;
                        break;
                    case "FileCreate":
                    case 9:
                        message.operations[message.operations.length] = 9;
                        break;
                    case "FileAppend":
                    case 10:
                        message.operations[message.operations.length] = 10;
                        break;
                    case "FileUpdate":
                    case 11:
                        message.operations[message.operations.length] = 11;
                        break;
                    case "FileDelete":
                    case 12:
                        message.operations[message.operations.length] = 12;
                        break;
                    case "CryptoGetAccountBalance":
                    case 13:
                        message.operations[message.operations.length] = 13;
                        break;
                    case "CryptoGetAccountRecords":
                    case 14:
                        message.operations[message.operations.length] = 14;
                        break;
                    case "CryptoGetInfo":
                    case 15:
                        message.operations[message.operations.length] = 15;
                        break;
                    case "ContractCallLocal":
                    case 16:
                        message.operations[message.operations.length] = 16;
                        break;
                    case "ContractGetInfo":
                    case 17:
                        message.operations[message.operations.length] = 17;
                        break;
                    case "ContractGetBytecode":
                    case 18:
                        message.operations[message.operations.length] = 18;
                        break;
                    case "GetBySolidityID":
                    case 19:
                        message.operations[message.operations.length] = 19;
                        break;
                    case "GetByKey":
                    case 20:
                        message.operations[message.operations.length] = 20;
                        break;
                    case "CryptoGetLiveHash":
                    case 21:
                        message.operations[message.operations.length] = 21;
                        break;
                    case "CryptoGetStakers":
                    case 22:
                        message.operations[message.operations.length] = 22;
                        break;
                    case "FileGetContents":
                    case 23:
                        message.operations[message.operations.length] = 23;
                        break;
                    case "FileGetInfo":
                    case 24:
                        message.operations[message.operations.length] = 24;
                        break;
                    case "TransactionGetRecord":
                    case 25:
                        message.operations[message.operations.length] = 25;
                        break;
                    case "ContractGetRecords":
                    case 26:
                        message.operations[message.operations.length] = 26;
                        break;
                    case "CryptoCreate":
                    case 27:
                        message.operations[message.operations.length] = 27;
                        break;
                    case "SystemDelete":
                    case 28:
                        message.operations[message.operations.length] = 28;
                        break;
                    case "SystemUndelete":
                    case 29:
                        message.operations[message.operations.length] = 29;
                        break;
                    case "ContractDelete":
                    case 30:
                        message.operations[message.operations.length] = 30;
                        break;
                    case "Freeze":
                    case 31:
                        message.operations[message.operations.length] = 31;
                        break;
                    case "CreateTransactionRecord":
                    case 32:
                        message.operations[message.operations.length] = 32;
                        break;
                    case "CryptoAccountAutoRenew":
                    case 33:
                        message.operations[message.operations.length] = 33;
                        break;
                    case "ContractAutoRenew":
                    case 34:
                        message.operations[message.operations.length] = 34;
                        break;
                    case "GetVersionInfo":
                    case 35:
                        message.operations[message.operations.length] = 35;
                        break;
                    case "TransactionGetReceipt":
                    case 36:
                        message.operations[message.operations.length] = 36;
                        break;
                    case "ConsensusCreateTopic":
                    case 50:
                        message.operations[message.operations.length] = 50;
                        break;
                    case "ConsensusUpdateTopic":
                    case 51:
                        message.operations[message.operations.length] = 51;
                        break;
                    case "ConsensusDeleteTopic":
                    case 52:
                        message.operations[message.operations.length] = 52;
                        break;
                    case "ConsensusGetTopicInfo":
                    case 53:
                        message.operations[message.operations.length] = 53;
                        break;
                    case "ConsensusSubmitMessage":
                    case 54:
                        message.operations[message.operations.length] = 54;
                        break;
                    case "UncheckedSubmit":
                    case 55:
                        message.operations[message.operations.length] = 55;
                        break;
                    case "TokenCreate":
                    case 56:
                        message.operations[message.operations.length] = 56;
                        break;
                    case "TokenGetInfo":
                    case 58:
                        message.operations[message.operations.length] = 58;
                        break;
                    case "TokenFreezeAccount":
                    case 59:
                        message.operations[message.operations.length] = 59;
                        break;
                    case "TokenUnfreezeAccount":
                    case 60:
                        message.operations[message.operations.length] = 60;
                        break;
                    case "TokenGrantKycToAccount":
                    case 61:
                        message.operations[message.operations.length] = 61;
                        break;
                    case "TokenRevokeKycFromAccount":
                    case 62:
                        message.operations[message.operations.length] = 62;
                        break;
                    case "TokenDelete":
                    case 63:
                        message.operations[message.operations.length] = 63;
                        break;
                    case "TokenUpdate":
                    case 64:
                        message.operations[message.operations.length] = 64;
                        break;
                    case "TokenMint":
                    case 65:
                        message.operations[message.operations.length] = 65;
                        break;
                    case "TokenBurn":
                    case 66:
                        message.operations[message.operations.length] = 66;
                        break;
                    case "TokenAccountWipe":
                    case 67:
                        message.operations[message.operations.length] = 67;
                        break;
                    case "TokenAssociateToAccount":
                    case 68:
                        message.operations[message.operations.length] = 68;
                        break;
                    case "TokenDissociateFromAccount":
                    case 69:
                        message.operations[message.operations.length] = 69;
                        break;
                    case "ScheduleCreate":
                    case 70:
                        message.operations[message.operations.length] = 70;
                        break;
                    case "ScheduleDelete":
                    case 71:
                        message.operations[message.operations.length] = 71;
                        break;
                    case "ScheduleSign":
                    case 72:
                        message.operations[message.operations.length] = 72;
                        break;
                    case "ScheduleGetInfo":
                    case 73:
                        message.operations[message.operations.length] = 73;
                        break;
                    case "TokenGetAccountNftInfos":
                    case 74:
                        message.operations[message.operations.length] = 74;
                        break;
                    case "TokenGetNftInfo":
                    case 75:
                        message.operations[message.operations.length] = 75;
                        break;
                    case "TokenGetNftInfos":
                    case 76:
                        message.operations[message.operations.length] = 76;
                        break;
                    case "TokenFeeScheduleUpdate":
                    case 77:
                        message.operations[message.operations.length] = 77;
                        break;
                    case "NetworkGetExecutionTime":
                    case 78:
                        message.operations[message.operations.length] = 78;
                        break;
                    case "TokenPause":
                    case 79:
                        message.operations[message.operations.length] = 79;
                        break;
                    case "TokenUnpause":
                    case 80:
                        message.operations[message.operations.length] = 80;
                        break;
                    case "CryptoApproveAllowance":
                    case 81:
                        message.operations[message.operations.length] = 81;
                        break;
                    case "CryptoDeleteAllowance":
                    case 82:
                        message.operations[message.operations.length] = 82;
                        break;
                    case "GetAccountDetails":
                    case 83:
                        message.operations[message.operations.length] = 83;
                        break;
                    case "EthereumTransaction":
                    case 84:
                        message.operations[message.operations.length] = 84;
                        break;
                    case "NodeStakeUpdate":
                    case 85:
                        message.operations[message.operations.length] = 85;
                        break;
                    case "UtilPrng":
                    case 86:
                        message.operations[message.operations.length] = 86;
                        break;
                    case "TransactionGetFastRecord":
                    case 87:
                        message.operations[message.operations.length] = 87;
                        break;
                    case "TokenUpdateNfts":
                    case 88:
                        message.operations[message.operations.length] = 88;
                        break;
                    case "NodeCreate":
                    case 89:
                        message.operations[message.operations.length] = 89;
                        break;
                    case "NodeUpdate":
                    case 90:
                        message.operations[message.operations.length] = 90;
                        break;
                    case "NodeDelete":
                    case 91:
                        message.operations[message.operations.length] = 91;
                        break;
                    case "TokenReject":
                    case 92:
                        message.operations[message.operations.length] = 92;
                        break;
                    case "TokenAirdrop":
                    case 93:
                        message.operations[message.operations.length] = 93;
                        break;
                    case "TokenCancelAirdrop":
                    case 94:
                        message.operations[message.operations.length] = 94;
                        break;
                    case "TokenClaimAirdrop":
                    case 95:
                        message.operations[message.operations.length] = 95;
                        break;
                    case "StateSignatureTransaction":
                    case 100:
                        message.operations[message.operations.length] = 100;
                        break;
                    case "HintsKeyPublication":
                    case 101:
                        message.operations[message.operations.length] = 101;
                        break;
                    case "HintsPreprocessingVote":
                    case 102:
                        message.operations[message.operations.length] = 102;
                        break;
                    case "HintsPartialSignature":
                    case 103:
                        message.operations[message.operations.length] = 103;
                        break;
                    case "HistoryAssemblySignature":
                    case 104:
                        message.operations[message.operations.length] = 104;
                        break;
                    case "HistoryProofKeyPublication":
                    case 105:
                        message.operations[message.operations.length] = 105;
                        break;
                    case "HistoryProofVote":
                    case 106:
                        message.operations[message.operations.length] = 106;
                        break;
                    case "CrsPublication":
                    case 107:
                        message.operations[message.operations.length] = 107;
                        break;
                    case "AtomicBatch":
                    case 108:
                        message.operations[message.operations.length] = 108;
                        break;
                    case "LambdaSStore":
                    case 109:
                        message.operations[message.operations.length] = 109;
                        break;
                    case "HookDispatch":
                    case 110:
                        message.operations[message.operations.length] = 110;
                        break;
                    case "HookStore":
                    case 111:
                        message.operations[message.operations.length] = 111;
                        break;
                    case "LedgerIdPublication":
                    case 112:
                        message.operations[message.operations.length] = 112;
                        break;
                    case "RegisteredNodeCreate":
                    case 113:
                        message.operations[message.operations.length] = 113;
                        break;
                    case "RegisteredNodeUpdate":
                    case 114:
                        message.operations[message.operations.length] = 114;
                        break;
                    case "RegisteredNodeDelete":
                    case 115:
                        message.operations[message.operations.length] = 115;
                        break;
                    case "MigrationRootHashVote":
                    case 116:
                        message.operations[message.operations.length] = 116;
                        break;
                    default:
                        if (typeof object.operations[i] === "number" && (object.operations[i] | 0) === object.operations[i])
                            message.operations[message.operations.length] = object.operations[i];
                    }
            }
            if (object.milliOpsPerSec != null)
                if (typeof object.milliOpsPerSec === "object" ? object.milliOpsPerSec.low || object.milliOpsPerSec.high : $Number(object.milliOpsPerSec) !== 0)
                    if ($util.Long)
                        message.milliOpsPerSec = $util.Long.fromValue(object.milliOpsPerSec, true);
                    else if (typeof object.milliOpsPerSec === "string")
                        message.milliOpsPerSec = $parseInt(object.milliOpsPerSec, 10);
                    else if (typeof object.milliOpsPerSec === "number")
                        message.milliOpsPerSec = object.milliOpsPerSec;
                    else if (typeof object.milliOpsPerSec === "object")
                        message.milliOpsPerSec = new $util.LongBits(object.milliOpsPerSec.low >>> 0, object.milliOpsPerSec.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a ThrottleGroup message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ThrottleGroup
         * @static
         * @param {proto.ThrottleGroup} message ThrottleGroup
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThrottleGroup.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.operations = [];
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.milliOpsPerSec = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.milliOpsPerSec = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.operations && message.operations.length) {
                object.operations = $Array(message.operations.length);
                for (let j = 0; j < message.operations.length; ++j)
                    object.operations[j] = options.enums === $String ? $root.proto.HederaFunctionality[message.operations[j]] === $undefined ? message.operations[j] : $root.proto.HederaFunctionality[message.operations[j]] : message.operations[j];
            }
            if (message.milliOpsPerSec != null && $Object.hasOwnProperty.call(message, "milliOpsPerSec"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.milliOpsPerSec = typeof message.milliOpsPerSec === "number" ? $BigInt(message.milliOpsPerSec) : $util.Long.fromBits(message.milliOpsPerSec.low >>> 0, message.milliOpsPerSec.high >>> 0, true).toBigInt();
                else if (typeof message.milliOpsPerSec === "number")
                    object.milliOpsPerSec = options.longs === $String ? $String(message.milliOpsPerSec) : message.milliOpsPerSec;
                else
                    object.milliOpsPerSec = options.longs === $String ? $util.Long.prototype.toString.call(message.milliOpsPerSec) : options.longs === $Number ? new $util.LongBits(message.milliOpsPerSec.low >>> 0, message.milliOpsPerSec.high >>> 0).toNumber(true) : message.milliOpsPerSec;
            return object;
        };

        /**
         * Converts this ThrottleGroup to JSON.
         * @function toJSON
         * @memberof proto.ThrottleGroup
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThrottleGroup.prototype.toJSON = function() {
            return ThrottleGroup.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ThrottleGroup
         * @function getTypeUrl
         * @memberof proto.ThrottleGroup
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ThrottleGroup.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ThrottleGroup";
        };

        return ThrottleGroup;
    })();

    proto.ThrottleBucket = (function() {

        /**
         * Properties of a ThrottleBucket.
         * @typedef {Object} proto.ThrottleBucket.$Properties
         * @property {string|null} [name] A name for this bucket.<br/>
         * This is used for log entries.
         * <p>
         * This value SHOULD NOT exceed 20 characters.
         * @property {number|Long|null} [burstPeriodMs] A burst duration limit, in milliseconds.<br/>
         * This value determines the total "capacity" of the bucket. The rate
         * at which the bucket "drains" is set by the throttles, and this duration
         * sets how long that rate must be sustained to empty a "full" bucket.
         * That combination (calculated as the product of this value and the least
         * common multiple of the `milliOpsPerSec` values for all throttle groups)
         * determines the maximum amount of operations this bucket can "hold".
         * <p>
         * The calculated capacity of this bucket MUST NOT exceed `9,223,372,036,854`.
         * @property {Array.<proto.ThrottleGroup.$Properties>|null} [throttleGroups] A list of throttle groups.<br/>
         * These throttle groups combined define the effective throttle
         * rate for the bucket.
         * <p>
         * This list MUST contain at least one entry.
         * @property {boolean|null} [highVolume] If set to true, this bucket is used for high-volume throttles.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ThrottleBucket.
         * @memberof proto
         * @interface IThrottleBucket
         * @augments proto.ThrottleBucket.$Properties
         * @deprecated Use proto.ThrottleBucket.$Properties instead.
         */

        /**
         * Shape of a ThrottleBucket.
         * @typedef {proto.ThrottleBucket.$Properties} proto.ThrottleBucket.$Shape
         */

        /**
         * Constructs a new ThrottleBucket.
         * @memberof proto
         * @classdesc A "bucket" of performance allocated across one or more throttle groups.<br/>
         * This entry combines one or more throttle groups into a single unit to
         * calculate limitations and congestion. Each "bucket" "fills" as operations
         * are completed, then "drains" over a period of time defined for each bucket.
         * This fill-and-drain characteristic enables the network to process sudden
         * bursts of heavy traffic while still observing throttle limits over longer
         * timeframes.
         * 
         * The value of `burstPeriodMs` is combined with the `milliOpsPerSec`
         * values for the individual throttle groups to determine the total
         * bucket "capacity". This combination MUST be less than the maximum
         * value of a signed long integer (`9223372036854775807`), when scaled to
         * a nanosecond measurement resolution.
         * 
         * > Note
         * >> There is some question regarding the mechanism of calculating the
         * >> combination of `burstPeriodMs` and `milliOpsPerSec`. The calculation
         * >> Is implemented in difficult-to-find code, and very likely does not
         * >> match the approach described here.
         * @constructor
         * @param {proto.ThrottleBucket.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ThrottleBucket = function (properties) {
            this.throttleGroups = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A name for this bucket.<br/>
         * This is used for log entries.
         * <p>
         * This value SHOULD NOT exceed 20 characters.
         * @member {string} name
         * @memberof proto.ThrottleBucket
         * @instance
         */
        ThrottleBucket.prototype.name = "";

        /**
         * A burst duration limit, in milliseconds.<br/>
         * This value determines the total "capacity" of the bucket. The rate
         * at which the bucket "drains" is set by the throttles, and this duration
         * sets how long that rate must be sustained to empty a "full" bucket.
         * That combination (calculated as the product of this value and the least
         * common multiple of the `milliOpsPerSec` values for all throttle groups)
         * determines the maximum amount of operations this bucket can "hold".
         * <p>
         * The calculated capacity of this bucket MUST NOT exceed `9,223,372,036,854`.
         * @member {number|Long} burstPeriodMs
         * @memberof proto.ThrottleBucket
         * @instance
         */
        ThrottleBucket.prototype.burstPeriodMs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * A list of throttle groups.<br/>
         * These throttle groups combined define the effective throttle
         * rate for the bucket.
         * <p>
         * This list MUST contain at least one entry.
         * @member {Array.<proto.ThrottleGroup.$Properties>} throttleGroups
         * @memberof proto.ThrottleBucket
         * @instance
         */
        ThrottleBucket.prototype.throttleGroups = $util.emptyArray;

        /**
         * If set to true, this bucket is used for high-volume throttles.
         * @member {boolean} highVolume
         * @memberof proto.ThrottleBucket
         * @instance
         */
        ThrottleBucket.prototype.highVolume = false;

        /**
         * Creates a new ThrottleBucket instance using the specified properties.
         * @function create
         * @memberof proto.ThrottleBucket
         * @static
         * @param {proto.ThrottleBucket.$Properties=} [properties] Properties to set
         * @returns {proto.ThrottleBucket} ThrottleBucket instance
         * @type {{
         *   (properties: proto.ThrottleBucket.$Shape): proto.ThrottleBucket & proto.ThrottleBucket.$Shape;
         *   (properties?: proto.ThrottleBucket.$Properties): proto.ThrottleBucket;
         * }}
         */
        ThrottleBucket.create = function(properties) {
            return new ThrottleBucket(properties);
        };

        /**
         * Encodes the specified ThrottleBucket message. Does not implicitly {@link proto.ThrottleBucket.verify|verify} messages.
         * @function encode
         * @memberof proto.ThrottleBucket
         * @static
         * @param {proto.ThrottleBucket.$Properties} message ThrottleBucket message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottleBucket.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.burstPeriodMs != null && $Object.hasOwnProperty.call(message, "burstPeriodMs") && (typeof message.burstPeriodMs === "object" ? message.burstPeriodMs.low || message.burstPeriodMs.high : message.burstPeriodMs !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.burstPeriodMs);
            if (message.throttleGroups != null && message.throttleGroups.length)
                for (let i = 0; i < message.throttleGroups.length; ++i)
                    $root.proto.ThrottleGroup.encode(message.throttleGroups[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.highVolume != null && $Object.hasOwnProperty.call(message, "highVolume") && message.highVolume !== false)
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.highVolume);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ThrottleBucket message, length delimited. Does not implicitly {@link proto.ThrottleBucket.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ThrottleBucket
         * @static
         * @param {proto.ThrottleBucket.$Properties} message ThrottleBucket message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottleBucket.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ThrottleBucket message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ThrottleBucket
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ThrottleBucket & proto.ThrottleBucket.$Shape} ThrottleBucket
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottleBucket.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ThrottleBucket();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.burstPeriodMs = value;
                        else
                            delete message.burstPeriodMs;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.throttleGroups && message.throttleGroups.length))
                            message.throttleGroups = [];
                        message.throttleGroups.push($root.proto.ThrottleGroup.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.highVolume = value;
                        else
                            delete message.highVolume;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ThrottleBucket message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ThrottleBucket
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ThrottleBucket & proto.ThrottleBucket.$Shape} ThrottleBucket
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottleBucket.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ThrottleBucket message.
         * @function verify
         * @memberof proto.ThrottleBucket
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ThrottleBucket.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.burstPeriodMs != null && $Object.hasOwnProperty.call(message, "burstPeriodMs"))
                if (!$util.isInteger(message.burstPeriodMs) && !(message.burstPeriodMs && $util.isInteger(message.burstPeriodMs.low) && $util.isInteger(message.burstPeriodMs.high)))
                    return "burstPeriodMs: integer|Long expected";
            if (message.throttleGroups != null && $Object.hasOwnProperty.call(message, "throttleGroups")) {
                if (!$Array.isArray(message.throttleGroups))
                    return "throttleGroups: array expected";
                for (let i = 0; i < message.throttleGroups.length; ++i) {
                    let error = $root.proto.ThrottleGroup.verify(message.throttleGroups[i], _depth + 1);
                    if (error)
                        return "throttleGroups." + error;
                }
            }
            if (message.highVolume != null && $Object.hasOwnProperty.call(message, "highVolume"))
                if (typeof message.highVolume !== "boolean")
                    return "highVolume: boolean expected";
            return null;
        };

        /**
         * Creates a ThrottleBucket message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ThrottleBucket
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ThrottleBucket} ThrottleBucket
         */
        ThrottleBucket.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ThrottleBucket)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ThrottleBucket: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ThrottleBucket();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.burstPeriodMs != null)
                if (typeof object.burstPeriodMs === "object" ? object.burstPeriodMs.low || object.burstPeriodMs.high : $Number(object.burstPeriodMs) !== 0)
                    if ($util.Long)
                        message.burstPeriodMs = $util.Long.fromValue(object.burstPeriodMs, true);
                    else if (typeof object.burstPeriodMs === "string")
                        message.burstPeriodMs = $parseInt(object.burstPeriodMs, 10);
                    else if (typeof object.burstPeriodMs === "number")
                        message.burstPeriodMs = object.burstPeriodMs;
                    else if (typeof object.burstPeriodMs === "object")
                        message.burstPeriodMs = new $util.LongBits(object.burstPeriodMs.low >>> 0, object.burstPeriodMs.high >>> 0).toNumber(true);
            if (object.throttleGroups) {
                if (!$Array.isArray(object.throttleGroups))
                    throw $TypeError(".proto.ThrottleBucket.throttleGroups: array expected");
                message.throttleGroups = $Array(object.throttleGroups.length);
                for (let i = 0; i < object.throttleGroups.length; ++i) {
                    if (!$util.isObject(object.throttleGroups[i]))
                        throw $TypeError(".proto.ThrottleBucket.throttleGroups: object expected");
                    message.throttleGroups[i] = $root.proto.ThrottleGroup.fromObject(object.throttleGroups[i], _depth + 1);
                }
            }
            if (object.highVolume != null)
                if (object.highVolume)
                    message.highVolume = $Boolean(object.highVolume);
            return message;
        };

        /**
         * Creates a plain object from a ThrottleBucket message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ThrottleBucket
         * @static
         * @param {proto.ThrottleBucket} message ThrottleBucket
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThrottleBucket.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.throttleGroups = [];
            if (options.defaults) {
                object.name = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.burstPeriodMs = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.burstPeriodMs = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.highVolume = false;
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.burstPeriodMs != null && $Object.hasOwnProperty.call(message, "burstPeriodMs"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.burstPeriodMs = typeof message.burstPeriodMs === "number" ? $BigInt(message.burstPeriodMs) : $util.Long.fromBits(message.burstPeriodMs.low >>> 0, message.burstPeriodMs.high >>> 0, true).toBigInt();
                else if (typeof message.burstPeriodMs === "number")
                    object.burstPeriodMs = options.longs === $String ? $String(message.burstPeriodMs) : message.burstPeriodMs;
                else
                    object.burstPeriodMs = options.longs === $String ? $util.Long.prototype.toString.call(message.burstPeriodMs) : options.longs === $Number ? new $util.LongBits(message.burstPeriodMs.low >>> 0, message.burstPeriodMs.high >>> 0).toNumber(true) : message.burstPeriodMs;
            if (message.throttleGroups && message.throttleGroups.length) {
                object.throttleGroups = $Array(message.throttleGroups.length);
                for (let j = 0; j < message.throttleGroups.length; ++j)
                    object.throttleGroups[j] = $root.proto.ThrottleGroup.toObject(message.throttleGroups[j], options, _depth + 1);
            }
            if (message.highVolume != null && $Object.hasOwnProperty.call(message, "highVolume"))
                object.highVolume = message.highVolume;
            return object;
        };

        /**
         * Converts this ThrottleBucket to JSON.
         * @function toJSON
         * @memberof proto.ThrottleBucket
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThrottleBucket.prototype.toJSON = function() {
            return ThrottleBucket.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ThrottleBucket
         * @function getTypeUrl
         * @memberof proto.ThrottleBucket
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ThrottleBucket.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ThrottleBucket";
        };

        return ThrottleBucket;
    })();

    proto.ThrottleDefinitions = (function() {

        /**
         * Properties of a ThrottleDefinitions.
         * @typedef {Object} proto.ThrottleDefinitions.$Properties
         * @property {Array.<proto.ThrottleBucket.$Properties>|null} [throttleBuckets] A list of throttle buckets.
         * <p>
         * This list MUST be set, and SHOULD NOT be empty.<br/>
         * An empty list SHALL have the effect of setting all operations to
         * a single group with throttle limit of `0` operations per second for the
         * entire network.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ThrottleDefinitions.
         * @memberof proto
         * @interface IThrottleDefinitions
         * @augments proto.ThrottleDefinitions.$Properties
         * @deprecated Use proto.ThrottleDefinitions.$Properties instead.
         */

        /**
         * Shape of a ThrottleDefinitions.
         * @typedef {proto.ThrottleDefinitions.$Properties} proto.ThrottleDefinitions.$Shape
         */

        /**
         * Constructs a new ThrottleDefinitions.
         * @memberof proto
         * @classdesc A list of throttle buckets.<br/>
         * This list, simultaneously enforced, defines a complete throttling policy.
         * 
         * 1. When an operation appears in more than one throttling bucket,
         * that operation SHALL be throttled unless all of the buckets where
         * the operation appears have "capacity" available.
         * 1. An operation assigned to no buckets is SHALL be throttled in every
         * instance.  The _effective_ throttle for this case is `0`.
         * @constructor
         * @param {proto.ThrottleDefinitions.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ThrottleDefinitions = function (properties) {
            this.throttleBuckets = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A list of throttle buckets.
         * <p>
         * This list MUST be set, and SHOULD NOT be empty.<br/>
         * An empty list SHALL have the effect of setting all operations to
         * a single group with throttle limit of `0` operations per second for the
         * entire network.
         * @member {Array.<proto.ThrottleBucket.$Properties>} throttleBuckets
         * @memberof proto.ThrottleDefinitions
         * @instance
         */
        ThrottleDefinitions.prototype.throttleBuckets = $util.emptyArray;

        /**
         * Creates a new ThrottleDefinitions instance using the specified properties.
         * @function create
         * @memberof proto.ThrottleDefinitions
         * @static
         * @param {proto.ThrottleDefinitions.$Properties=} [properties] Properties to set
         * @returns {proto.ThrottleDefinitions} ThrottleDefinitions instance
         * @type {{
         *   (properties: proto.ThrottleDefinitions.$Shape): proto.ThrottleDefinitions & proto.ThrottleDefinitions.$Shape;
         *   (properties?: proto.ThrottleDefinitions.$Properties): proto.ThrottleDefinitions;
         * }}
         */
        ThrottleDefinitions.create = function(properties) {
            return new ThrottleDefinitions(properties);
        };

        /**
         * Encodes the specified ThrottleDefinitions message. Does not implicitly {@link proto.ThrottleDefinitions.verify|verify} messages.
         * @function encode
         * @memberof proto.ThrottleDefinitions
         * @static
         * @param {proto.ThrottleDefinitions.$Properties} message ThrottleDefinitions message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottleDefinitions.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.throttleBuckets != null && message.throttleBuckets.length)
                for (let i = 0; i < message.throttleBuckets.length; ++i)
                    $root.proto.ThrottleBucket.encode(message.throttleBuckets[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ThrottleDefinitions message, length delimited. Does not implicitly {@link proto.ThrottleDefinitions.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ThrottleDefinitions
         * @static
         * @param {proto.ThrottleDefinitions.$Properties} message ThrottleDefinitions message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottleDefinitions.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ThrottleDefinitions message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ThrottleDefinitions
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ThrottleDefinitions & proto.ThrottleDefinitions.$Shape} ThrottleDefinitions
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottleDefinitions.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ThrottleDefinitions();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.throttleBuckets && message.throttleBuckets.length))
                            message.throttleBuckets = [];
                        message.throttleBuckets.push($root.proto.ThrottleBucket.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ThrottleDefinitions message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ThrottleDefinitions
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ThrottleDefinitions & proto.ThrottleDefinitions.$Shape} ThrottleDefinitions
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottleDefinitions.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ThrottleDefinitions message.
         * @function verify
         * @memberof proto.ThrottleDefinitions
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ThrottleDefinitions.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.throttleBuckets != null && $Object.hasOwnProperty.call(message, "throttleBuckets")) {
                if (!$Array.isArray(message.throttleBuckets))
                    return "throttleBuckets: array expected";
                for (let i = 0; i < message.throttleBuckets.length; ++i) {
                    let error = $root.proto.ThrottleBucket.verify(message.throttleBuckets[i], _depth + 1);
                    if (error)
                        return "throttleBuckets." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ThrottleDefinitions message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ThrottleDefinitions
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ThrottleDefinitions} ThrottleDefinitions
         */
        ThrottleDefinitions.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ThrottleDefinitions)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ThrottleDefinitions: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ThrottleDefinitions();
            if (object.throttleBuckets) {
                if (!$Array.isArray(object.throttleBuckets))
                    throw $TypeError(".proto.ThrottleDefinitions.throttleBuckets: array expected");
                message.throttleBuckets = $Array(object.throttleBuckets.length);
                for (let i = 0; i < object.throttleBuckets.length; ++i) {
                    if (!$util.isObject(object.throttleBuckets[i]))
                        throw $TypeError(".proto.ThrottleDefinitions.throttleBuckets: object expected");
                    message.throttleBuckets[i] = $root.proto.ThrottleBucket.fromObject(object.throttleBuckets[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ThrottleDefinitions message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ThrottleDefinitions
         * @static
         * @param {proto.ThrottleDefinitions} message ThrottleDefinitions
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThrottleDefinitions.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.throttleBuckets = [];
            if (message.throttleBuckets && message.throttleBuckets.length) {
                object.throttleBuckets = $Array(message.throttleBuckets.length);
                for (let j = 0; j < message.throttleBuckets.length; ++j)
                    object.throttleBuckets[j] = $root.proto.ThrottleBucket.toObject(message.throttleBuckets[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ThrottleDefinitions to JSON.
         * @function toJSON
         * @memberof proto.ThrottleDefinitions
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThrottleDefinitions.prototype.toJSON = function() {
            return ThrottleDefinitions.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ThrottleDefinitions
         * @function getTypeUrl
         * @memberof proto.ThrottleDefinitions
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ThrottleDefinitions.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ThrottleDefinitions";
        };

        return ThrottleDefinitions;
    })();

    proto.TokenService = (function() {

        /**
         * Constructs a new TokenService service.
         * @memberof proto
         * @classdesc Transactions and queries for the Token Service
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const TokenService = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        $Object.defineProperty(TokenService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: TokenService, writable: true, enumerable: false, configurable: true });

        /**
         * Creates new TokenService service using the specified rpc implementation.
         * @function create
         * @memberof proto.TokenService
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {TokenService} RPC service. Useful where requests and/or responses are streamed.
         */
        TokenService.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link proto.TokenService#createToken}.
         * @memberof proto.TokenService
         * @typedef createTokenCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Create a new token.
         * @memberof proto.TokenService
         * @typedef createToken
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.createTokenCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "createToken";
         *   readonly path: "/proto.TokenService/createToken";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Create a new token.
         * @name proto.TokenService#createToken
         * @type {proto.TokenService.createToken}
         */
        $Object.defineProperties(TokenService.prototype.createToken = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.createToken, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "createToken" },
            path: { value: "/proto.TokenService/createToken" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#updateToken}.
         * @memberof proto.TokenService
         * @typedef updateTokenCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Update a token.
         * @memberof proto.TokenService
         * @typedef updateToken
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.updateTokenCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "updateToken";
         *   readonly path: "/proto.TokenService/updateToken";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Update a token.
         * @name proto.TokenService#updateToken
         * @type {proto.TokenService.updateToken}
         */
        $Object.defineProperties(TokenService.prototype.updateToken = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.updateToken, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "updateToken" },
            path: { value: "/proto.TokenService/updateToken" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#mintToken}.
         * @memberof proto.TokenService
         * @typedef mintTokenCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Mint one or more tokens to the treasury account.
         * <p>
         * This MAY specify a quantity of fungible/common tokens or
         * a list of specific non-fungible/unique tokes, but
         * MUST NOT specify both.
         * @memberof proto.TokenService
         * @typedef mintToken
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.mintTokenCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "mintToken";
         *   readonly path: "/proto.TokenService/mintToken";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Mint one or more tokens to the treasury account.
         * <p>
         * This MAY specify a quantity of fungible/common tokens or
         * a list of specific non-fungible/unique tokes, but
         * MUST NOT specify both.
         * @name proto.TokenService#mintToken
         * @type {proto.TokenService.mintToken}
         */
        $Object.defineProperties(TokenService.prototype.mintToken = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.mintToken, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "mintToken" },
            path: { value: "/proto.TokenService/mintToken" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#burnToken}.
         * @memberof proto.TokenService
         * @typedef burnTokenCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Burn one or more tokens from the treasury account.
         * <p>
         * This MAY specify a quantity of fungible/common tokens or
         * a list of specific non-fungible/unique tokes, but
         * MUST NOT specify both.
         * @memberof proto.TokenService
         * @typedef burnToken
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.burnTokenCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "burnToken";
         *   readonly path: "/proto.TokenService/burnToken";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Burn one or more tokens from the treasury account.
         * <p>
         * This MAY specify a quantity of fungible/common tokens or
         * a list of specific non-fungible/unique tokes, but
         * MUST NOT specify both.
         * @name proto.TokenService#burnToken
         * @type {proto.TokenService.burnToken}
         */
        $Object.defineProperties(TokenService.prototype.burnToken = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.burnToken, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "burnToken" },
            path: { value: "/proto.TokenService/burnToken" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#deleteToken}.
         * @memberof proto.TokenService
         * @typedef deleteTokenCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Delete a token.
         * @memberof proto.TokenService
         * @typedef deleteToken
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.deleteTokenCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "deleteToken";
         *   readonly path: "/proto.TokenService/deleteToken";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Delete a token.
         * @name proto.TokenService#deleteToken
         * @type {proto.TokenService.deleteToken}
         */
        $Object.defineProperties(TokenService.prototype.deleteToken = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.deleteToken, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "deleteToken" },
            path: { value: "/proto.TokenService/deleteToken" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#wipeTokenAccount}.
         * @memberof proto.TokenService
         * @typedef wipeTokenAccountCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Wipe one or more tokens from an identified Account.
         * <p>
         * This MAY specify a quantity of fungible/common tokens or
         * a list of specific non-fungible/unique tokes, but
         * MUST NOT specify both.
         * @memberof proto.TokenService
         * @typedef wipeTokenAccount
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.wipeTokenAccountCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "wipeTokenAccount";
         *   readonly path: "/proto.TokenService/wipeTokenAccount";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Wipe one or more tokens from an identified Account.
         * <p>
         * This MAY specify a quantity of fungible/common tokens or
         * a list of specific non-fungible/unique tokes, but
         * MUST NOT specify both.
         * @name proto.TokenService#wipeTokenAccount
         * @type {proto.TokenService.wipeTokenAccount}
         */
        $Object.defineProperties(TokenService.prototype.wipeTokenAccount = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.wipeTokenAccount, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "wipeTokenAccount" },
            path: { value: "/proto.TokenService/wipeTokenAccount" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#freezeTokenAccount}.
         * @memberof proto.TokenService
         * @typedef freezeTokenAccountCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Freeze the transfer of tokens to or from an identified Account.
         * @memberof proto.TokenService
         * @typedef freezeTokenAccount
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.freezeTokenAccountCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "freezeTokenAccount";
         *   readonly path: "/proto.TokenService/freezeTokenAccount";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Freeze the transfer of tokens to or from an identified Account.
         * @name proto.TokenService#freezeTokenAccount
         * @type {proto.TokenService.freezeTokenAccount}
         */
        $Object.defineProperties(TokenService.prototype.freezeTokenAccount = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.freezeTokenAccount, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "freezeTokenAccount" },
            path: { value: "/proto.TokenService/freezeTokenAccount" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#unfreezeTokenAccount}.
         * @memberof proto.TokenService
         * @typedef unfreezeTokenAccountCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Unfreeze the transfer of tokens to or from an identified Account.
         * @memberof proto.TokenService
         * @typedef unfreezeTokenAccount
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.unfreezeTokenAccountCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "unfreezeTokenAccount";
         *   readonly path: "/proto.TokenService/unfreezeTokenAccount";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Unfreeze the transfer of tokens to or from an identified Account.
         * @name proto.TokenService#unfreezeTokenAccount
         * @type {proto.TokenService.unfreezeTokenAccount}
         */
        $Object.defineProperties(TokenService.prototype.unfreezeTokenAccount = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.unfreezeTokenAccount, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "unfreezeTokenAccount" },
            path: { value: "/proto.TokenService/unfreezeTokenAccount" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#grantKycToTokenAccount}.
         * @memberof proto.TokenService
         * @typedef grantKycToTokenAccountCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Assert that KYC requirements are met for a specific account with
         * respect to a specific token.
         * @memberof proto.TokenService
         * @typedef grantKycToTokenAccount
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.grantKycToTokenAccountCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "grantKycToTokenAccount";
         *   readonly path: "/proto.TokenService/grantKycToTokenAccount";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Assert that KYC requirements are met for a specific account with
         * respect to a specific token.
         * @name proto.TokenService#grantKycToTokenAccount
         * @type {proto.TokenService.grantKycToTokenAccount}
         */
        $Object.defineProperties(TokenService.prototype.grantKycToTokenAccount = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.grantKycToTokenAccount, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "grantKycToTokenAccount" },
            path: { value: "/proto.TokenService/grantKycToTokenAccount" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#revokeKycFromTokenAccount}.
         * @memberof proto.TokenService
         * @typedef revokeKycFromTokenAccountCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Assert that KYC requirements are _not_ met for a specific account with
         * respect to a specific token.
         * @memberof proto.TokenService
         * @typedef revokeKycFromTokenAccount
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.revokeKycFromTokenAccountCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "revokeKycFromTokenAccount";
         *   readonly path: "/proto.TokenService/revokeKycFromTokenAccount";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Assert that KYC requirements are _not_ met for a specific account with
         * respect to a specific token.
         * @name proto.TokenService#revokeKycFromTokenAccount
         * @type {proto.TokenService.revokeKycFromTokenAccount}
         */
        $Object.defineProperties(TokenService.prototype.revokeKycFromTokenAccount = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.revokeKycFromTokenAccount, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "revokeKycFromTokenAccount" },
            path: { value: "/proto.TokenService/revokeKycFromTokenAccount" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#associateTokens}.
         * @memberof proto.TokenService
         * @typedef associateTokensCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Associate one or more tokens to an account.
         * @memberof proto.TokenService
         * @typedef associateTokens
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.associateTokensCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "associateTokens";
         *   readonly path: "/proto.TokenService/associateTokens";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Associate one or more tokens to an account.
         * @name proto.TokenService#associateTokens
         * @type {proto.TokenService.associateTokens}
         */
        $Object.defineProperties(TokenService.prototype.associateTokens = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.associateTokens, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "associateTokens" },
            path: { value: "/proto.TokenService/associateTokens" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#dissociateTokens}.
         * @memberof proto.TokenService
         * @typedef dissociateTokensCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Dissociate one or more tokens from an account.
         * @memberof proto.TokenService
         * @typedef dissociateTokens
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.dissociateTokensCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "dissociateTokens";
         *   readonly path: "/proto.TokenService/dissociateTokens";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Dissociate one or more tokens from an account.
         * @name proto.TokenService#dissociateTokens
         * @type {proto.TokenService.dissociateTokens}
         */
        $Object.defineProperties(TokenService.prototype.dissociateTokens = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.dissociateTokens, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "dissociateTokens" },
            path: { value: "/proto.TokenService/dissociateTokens" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#updateTokenFeeSchedule}.
         * @memberof proto.TokenService
         * @typedef updateTokenFeeScheduleCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Update the custom fee schedule for a token.
         * @memberof proto.TokenService
         * @typedef updateTokenFeeSchedule
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.updateTokenFeeScheduleCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "updateTokenFeeSchedule";
         *   readonly path: "/proto.TokenService/updateTokenFeeSchedule";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Update the custom fee schedule for a token.
         * @name proto.TokenService#updateTokenFeeSchedule
         * @type {proto.TokenService.updateTokenFeeSchedule}
         */
        $Object.defineProperties(TokenService.prototype.updateTokenFeeSchedule = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.updateTokenFeeSchedule, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "updateTokenFeeSchedule" },
            path: { value: "/proto.TokenService/updateTokenFeeSchedule" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#getTokenInfo}.
         * @memberof proto.TokenService
         * @typedef getTokenInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve the detail characteristics for a token.
         * <p>
         * This query SHALL return information for the token type as a whole.<br/>
         * This query SHALL NOT return information for individual tokens.
         * @memberof proto.TokenService
         * @typedef getTokenInfo
         * @type {{
         *   (request: proto.IQuery, callback: proto.TokenService.getTokenInfoCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getTokenInfo";
         *   readonly path: "/proto.TokenService/getTokenInfo";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve the detail characteristics for a token.
         * <p>
         * This query SHALL return information for the token type as a whole.<br/>
         * This query SHALL NOT return information for individual tokens.
         * @name proto.TokenService#getTokenInfo
         * @type {proto.TokenService.getTokenInfo}
         */
        $Object.defineProperties(TokenService.prototype.getTokenInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.getTokenInfo, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getTokenInfo" },
            path: { value: "/proto.TokenService/getTokenInfo" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#getTokenNftInfo}.
         * @memberof proto.TokenService
         * @typedef getTokenNftInfoCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.Response} [response] Response
         */

        /**
         * Retrieve the metadata for a specific non-fungible/unique token.<br/>
         * The NFT to query is identified by token identifier and serial number.
         * <p>
         * This query SHALL return token metadata and, if an allowance is defined,
         * the designated "spender" account for the queried NFT.
         * @memberof proto.TokenService
         * @typedef getTokenNftInfo
         * @type {{
         *   (request: proto.IQuery, callback: proto.TokenService.getTokenNftInfoCallback): void;
         *   (request: proto.IQuery): Promise<proto.Response>;
         *   readonly name: "getTokenNftInfo";
         *   readonly path: "/proto.TokenService/getTokenNftInfo";
         *   readonly requestType: "Query";
         *   readonly responseType: "Response";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Retrieve the metadata for a specific non-fungible/unique token.<br/>
         * The NFT to query is identified by token identifier and serial number.
         * <p>
         * This query SHALL return token metadata and, if an allowance is defined,
         * the designated "spender" account for the queried NFT.
         * @name proto.TokenService#getTokenNftInfo
         * @type {proto.TokenService.getTokenNftInfo}
         */
        $Object.defineProperties(TokenService.prototype.getTokenNftInfo = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.getTokenNftInfo, $root.proto.Query, $root.proto.Response, request, callback);
        }, {
            name: { value: "getTokenNftInfo" },
            path: { value: "/proto.TokenService/getTokenNftInfo" },
            requestType: { value: "Query" },
            responseType: { value: "Response" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#pauseToken}.
         * @memberof proto.TokenService
         * @typedef pauseTokenCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Pause a token.
         * @memberof proto.TokenService
         * @typedef pauseToken
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.pauseTokenCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "pauseToken";
         *   readonly path: "/proto.TokenService/pauseToken";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Pause a token.
         * @name proto.TokenService#pauseToken
         * @type {proto.TokenService.pauseToken}
         */
        $Object.defineProperties(TokenService.prototype.pauseToken = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.pauseToken, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "pauseToken" },
            path: { value: "/proto.TokenService/pauseToken" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#unpauseToken}.
         * @memberof proto.TokenService
         * @typedef unpauseTokenCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Unpause (resume) a token.
         * @memberof proto.TokenService
         * @typedef unpauseToken
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.unpauseTokenCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "unpauseToken";
         *   readonly path: "/proto.TokenService/unpauseToken";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Unpause (resume) a token.
         * @name proto.TokenService#unpauseToken
         * @type {proto.TokenService.unpauseToken}
         */
        $Object.defineProperties(TokenService.prototype.unpauseToken = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.unpauseToken, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "unpauseToken" },
            path: { value: "/proto.TokenService/unpauseToken" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#updateNfts}.
         * @memberof proto.TokenService
         * @typedef updateNftsCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Update multiple non-fungible/unique tokens (NFTs) in a collection.<br/>
         * The NFTs are identified by token identifier and one or more
         * serial numbers.
         * <p>
         * This transaction SHALL update NFT metadata only.<br/>
         * This transaction MUST be signed by the token `metadata_key`.
         * @memberof proto.TokenService
         * @typedef updateNfts
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.updateNftsCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "updateNfts";
         *   readonly path: "/proto.TokenService/updateNfts";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Update multiple non-fungible/unique tokens (NFTs) in a collection.<br/>
         * The NFTs are identified by token identifier and one or more
         * serial numbers.
         * <p>
         * This transaction SHALL update NFT metadata only.<br/>
         * This transaction MUST be signed by the token `metadata_key`.
         * @name proto.TokenService#updateNfts
         * @type {proto.TokenService.updateNfts}
         */
        $Object.defineProperties(TokenService.prototype.updateNfts = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.updateNfts, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "updateNfts" },
            path: { value: "/proto.TokenService/updateNfts" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#rejectToken}.
         * @memberof proto.TokenService
         * @typedef rejectTokenCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Reject one or more tokens.
         * <p>
         * This transaction SHALL transfer the full balance of one or more tokens
         * from the requesting account to the treasury for each token.<br/>
         * This transfer SHALL NOT charge any custom fee or royalty defined for
         * the token(s) to be rejected.<br/>
         * ### Effects on success
         * <ul>
         * <li>If the rejected token is fungible/common, the requesting account
         * SHALL have a balance of 0 for the rejected token.<br/>
         * The treasury balance SHALL increase by the amount that the
         * requesting account decreased.</li>
         * <li>If the rejected token is non-fungible/unique the requesting
         * account SHALL NOT hold the specific serialized token that
         * is rejected.<br/>
         * The treasury account SHALL hold each specific serialized token
         * that was rejected.</li>
         * </li>
         * @memberof proto.TokenService
         * @typedef rejectToken
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.rejectTokenCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "rejectToken";
         *   readonly path: "/proto.TokenService/rejectToken";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Reject one or more tokens.
         * <p>
         * This transaction SHALL transfer the full balance of one or more tokens
         * from the requesting account to the treasury for each token.<br/>
         * This transfer SHALL NOT charge any custom fee or royalty defined for
         * the token(s) to be rejected.<br/>
         * ### Effects on success
         * <ul>
         * <li>If the rejected token is fungible/common, the requesting account
         * SHALL have a balance of 0 for the rejected token.<br/>
         * The treasury balance SHALL increase by the amount that the
         * requesting account decreased.</li>
         * <li>If the rejected token is non-fungible/unique the requesting
         * account SHALL NOT hold the specific serialized token that
         * is rejected.<br/>
         * The treasury account SHALL hold each specific serialized token
         * that was rejected.</li>
         * </li>
         * @name proto.TokenService#rejectToken
         * @type {proto.TokenService.rejectToken}
         */
        $Object.defineProperties(TokenService.prototype.rejectToken = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.rejectToken, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "rejectToken" },
            path: { value: "/proto.TokenService/rejectToken" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#airdropTokens}.
         * @memberof proto.TokenService
         * @typedef airdropTokensCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Airdrop one or more tokens to one or more accounts.
         * <p>
         * This transaction SHALL distribute tokens from the balance of one or
         * more sending account(s) to the balance of one or more
         * recipient accounts.<br/>
         * Accounts SHALL receive the tokens in one of four ways.
         * <ul>
         * <li>An account already associated to the token to be distributed
         * SHALL receive the airdropped tokens immediately to the
         * recipient account balance.</li>
         * <li>An account with available automatic association slots SHALL
         * be automatically associated to the token, and SHALL
         * immediately receive the airdropped tokens to the recipient
         * account balance.</li>
         * <li>An account with "receiver signature required" set SHALL have
         * a "Pending Airdrop" created and MUST claim that airdrop with
         * a `claimAirdrop` transaction.</li>
         * <li>An account with no available automatic association slots SHALL
         * have a "Pending Airdrop" created and MUST claim that airdrop
         * with a `claimAirdrop` transaction. </li>
         * </ul>
         * Any airdrop that completes immediately SHALL be irreversible.<br/>
         * Any airdrop that results in a "Pending Airdrop" MAY be canceled via
         * a `cancelAirdrop` transaction.<br/>
         * All transfer fees (including custom fees and royalties), as well as
         * the rent cost for the first auto-renewal period for any
         * automatic-association slot occupied by the airdropped tokens,
         * SHALL be charged to the account submitting this transaction.
         * @memberof proto.TokenService
         * @typedef airdropTokens
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.airdropTokensCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "airdropTokens";
         *   readonly path: "/proto.TokenService/airdropTokens";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Airdrop one or more tokens to one or more accounts.
         * <p>
         * This transaction SHALL distribute tokens from the balance of one or
         * more sending account(s) to the balance of one or more
         * recipient accounts.<br/>
         * Accounts SHALL receive the tokens in one of four ways.
         * <ul>
         * <li>An account already associated to the token to be distributed
         * SHALL receive the airdropped tokens immediately to the
         * recipient account balance.</li>
         * <li>An account with available automatic association slots SHALL
         * be automatically associated to the token, and SHALL
         * immediately receive the airdropped tokens to the recipient
         * account balance.</li>
         * <li>An account with "receiver signature required" set SHALL have
         * a "Pending Airdrop" created and MUST claim that airdrop with
         * a `claimAirdrop` transaction.</li>
         * <li>An account with no available automatic association slots SHALL
         * have a "Pending Airdrop" created and MUST claim that airdrop
         * with a `claimAirdrop` transaction. </li>
         * </ul>
         * Any airdrop that completes immediately SHALL be irreversible.<br/>
         * Any airdrop that results in a "Pending Airdrop" MAY be canceled via
         * a `cancelAirdrop` transaction.<br/>
         * All transfer fees (including custom fees and royalties), as well as
         * the rent cost for the first auto-renewal period for any
         * automatic-association slot occupied by the airdropped tokens,
         * SHALL be charged to the account submitting this transaction.
         * @name proto.TokenService#airdropTokens
         * @type {proto.TokenService.airdropTokens}
         */
        $Object.defineProperties(TokenService.prototype.airdropTokens = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.airdropTokens, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "airdropTokens" },
            path: { value: "/proto.TokenService/airdropTokens" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#cancelAirdrop}.
         * @memberof proto.TokenService
         * @typedef cancelAirdropCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Cancel one or more pending airdrops.
         * <p>
         * This transaction MUST be signed by _each_ account *sending* an
         * airdrop to be canceled.
         * @memberof proto.TokenService
         * @typedef cancelAirdrop
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.cancelAirdropCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "cancelAirdrop";
         *   readonly path: "/proto.TokenService/cancelAirdrop";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Cancel one or more pending airdrops.
         * <p>
         * This transaction MUST be signed by _each_ account *sending* an
         * airdrop to be canceled.
         * @name proto.TokenService#cancelAirdrop
         * @type {proto.TokenService.cancelAirdrop}
         */
        $Object.defineProperties(TokenService.prototype.cancelAirdrop = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.cancelAirdrop, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "cancelAirdrop" },
            path: { value: "/proto.TokenService/cancelAirdrop" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.TokenService#claimAirdrop}.
         * @memberof proto.TokenService
         * @typedef claimAirdropCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Claim one or more pending airdrops.
         * <p>
         * This transaction MUST be signed by _each_ account **receiving**
         * an airdrop to be claimed.<br>
         * If a "Sender" lacks sufficient balance to fulfill the airdrop at
         * the time the claim is made, that claim SHALL fail.
         * @memberof proto.TokenService
         * @typedef claimAirdrop
         * @type {{
         *   (request: proto.ITransaction, callback: proto.TokenService.claimAirdropCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "claimAirdrop";
         *   readonly path: "/proto.TokenService/claimAirdrop";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Claim one or more pending airdrops.
         * <p>
         * This transaction MUST be signed by _each_ account **receiving**
         * an airdrop to be claimed.<br>
         * If a "Sender" lacks sufficient balance to fulfill the airdrop at
         * the time the claim is made, that claim SHALL fail.
         * @name proto.TokenService#claimAirdrop
         * @type {proto.TokenService.claimAirdrop}
         */
        $Object.defineProperties(TokenService.prototype.claimAirdrop = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, TokenService.prototype.claimAirdrop, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "claimAirdrop" },
            path: { value: "/proto.TokenService/claimAirdrop" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return TokenService;
    })();

    proto.SignedTransaction = (function() {

        /**
         * Properties of a SignedTransaction.
         * @typedef {Object} proto.SignedTransaction.$Properties
         * @property {Uint8Array|null} [bodyBytes] A byte array containing a serialized `TransactionBody`.
         * <p>
         * This content is what the signatures in `sigMap` MUST sign.
         * @property {proto.SignatureMap.$Properties|null} [sigMap] A set of cryptographic signatures.
         * <p>
         * This set MUST contain all signatures required to authenticate
         * and authorize the transaction.<br/>
         * This set MAY contain additional signatures.
         * @property {boolean|null} [useSerializedTxMessageHashAlgorithm] If false then the hash of this transaction is the SHA-384 hash of the
         * serialization of this SignedTransaction message as it arrived on the wire.
         * <p>
         * If true then the hash of this transaction is the SHA-384 hash of the
         * ascending field order serialization of the Transaction whose `bodyBytes`
         * and sigMap fields are deserialized from the contents of this message.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SignedTransaction.
         * @memberof proto
         * @interface ISignedTransaction
         * @augments proto.SignedTransaction.$Properties
         * @deprecated Use proto.SignedTransaction.$Properties instead.
         */

        /**
         * Shape of a SignedTransaction.
         * @typedef {{
         *   bodyBytes?: Uint8Array|null;
         *   sigMap?: proto.SignatureMap.$Shape|null;
         *   useSerializedTxMessageHashAlgorithm?: boolean|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.SignedTransaction.$Shape
         */

        /**
         * Constructs a new SignedTransaction.
         * @memberof proto
         * @classdesc A combination transaction bytes and a map of signatures.<br/>
         * This message contains a serialized `TransactionBody` in a byte array
         * and a `SignatureMap` that contains all of the signatures offered to
         * authenticate the transaction.
         * 
         * ### Block Stream Effects
         * This content is recorded in the record stream exactly as received.
         * @constructor
         * @param {proto.SignedTransaction.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SignedTransaction = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A byte array containing a serialized `TransactionBody`.
         * <p>
         * This content is what the signatures in `sigMap` MUST sign.
         * @member {Uint8Array} bodyBytes
         * @memberof proto.SignedTransaction
         * @instance
         */
        SignedTransaction.prototype.bodyBytes = $util.newBuffer([]);

        /**
         * A set of cryptographic signatures.
         * <p>
         * This set MUST contain all signatures required to authenticate
         * and authorize the transaction.<br/>
         * This set MAY contain additional signatures.
         * @member {proto.SignatureMap.$Properties|null|undefined} sigMap
         * @memberof proto.SignedTransaction
         * @instance
         */
        SignedTransaction.prototype.sigMap = null;

        /**
         * If false then the hash of this transaction is the SHA-384 hash of the
         * serialization of this SignedTransaction message as it arrived on the wire.
         * <p>
         * If true then the hash of this transaction is the SHA-384 hash of the
         * ascending field order serialization of the Transaction whose `bodyBytes`
         * and sigMap fields are deserialized from the contents of this message.
         * @member {boolean} useSerializedTxMessageHashAlgorithm
         * @memberof proto.SignedTransaction
         * @instance
         */
        SignedTransaction.prototype.useSerializedTxMessageHashAlgorithm = false;

        /**
         * Creates a new SignedTransaction instance using the specified properties.
         * @function create
         * @memberof proto.SignedTransaction
         * @static
         * @param {proto.SignedTransaction.$Properties=} [properties] Properties to set
         * @returns {proto.SignedTransaction} SignedTransaction instance
         * @type {{
         *   (properties: proto.SignedTransaction.$Shape): proto.SignedTransaction & proto.SignedTransaction.$Shape;
         *   (properties?: proto.SignedTransaction.$Properties): proto.SignedTransaction;
         * }}
         */
        SignedTransaction.create = function(properties) {
            return new SignedTransaction(properties);
        };

        /**
         * Encodes the specified SignedTransaction message. Does not implicitly {@link proto.SignedTransaction.verify|verify} messages.
         * @function encode
         * @memberof proto.SignedTransaction
         * @static
         * @param {proto.SignedTransaction.$Properties} message SignedTransaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignedTransaction.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.bodyBytes != null && $Object.hasOwnProperty.call(message, "bodyBytes") && message.bodyBytes.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.bodyBytes);
            if (message.sigMap != null && $Object.hasOwnProperty.call(message, "sigMap"))
                $root.proto.SignatureMap.encode(message.sigMap, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.useSerializedTxMessageHashAlgorithm != null && $Object.hasOwnProperty.call(message, "useSerializedTxMessageHashAlgorithm") && message.useSerializedTxMessageHashAlgorithm !== false)
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.useSerializedTxMessageHashAlgorithm);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignedTransaction message, length delimited. Does not implicitly {@link proto.SignedTransaction.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SignedTransaction
         * @static
         * @param {proto.SignedTransaction.$Properties} message SignedTransaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignedTransaction.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SignedTransaction message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SignedTransaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SignedTransaction & proto.SignedTransaction.$Shape} SignedTransaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignedTransaction.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SignedTransaction();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.bodyBytes = value;
                        else
                            delete message.bodyBytes;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.sigMap = $root.proto.SignatureMap.decode(reader, reader.uint32(), $undefined, _depth + 1, message.sigMap);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.useSerializedTxMessageHashAlgorithm = value;
                        else
                            delete message.useSerializedTxMessageHashAlgorithm;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignedTransaction message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SignedTransaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SignedTransaction & proto.SignedTransaction.$Shape} SignedTransaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignedTransaction.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignedTransaction message.
         * @function verify
         * @memberof proto.SignedTransaction
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignedTransaction.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.bodyBytes != null && $Object.hasOwnProperty.call(message, "bodyBytes"))
                if (!(message.bodyBytes && typeof message.bodyBytes.length === "number" || $util.isString(message.bodyBytes)))
                    return "bodyBytes: buffer expected";
            if (message.sigMap != null && $Object.hasOwnProperty.call(message, "sigMap")) {
                let error = $root.proto.SignatureMap.verify(message.sigMap, _depth + 1);
                if (error)
                    return "sigMap." + error;
            }
            if (message.useSerializedTxMessageHashAlgorithm != null && $Object.hasOwnProperty.call(message, "useSerializedTxMessageHashAlgorithm"))
                if (typeof message.useSerializedTxMessageHashAlgorithm !== "boolean")
                    return "useSerializedTxMessageHashAlgorithm: boolean expected";
            return null;
        };

        /**
         * Creates a SignedTransaction message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SignedTransaction
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SignedTransaction} SignedTransaction
         */
        SignedTransaction.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SignedTransaction)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SignedTransaction: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SignedTransaction();
            if (object.bodyBytes != null)
                if (object.bodyBytes.length)
                    if (typeof object.bodyBytes === "string")
                        $util.base64.decode(object.bodyBytes, message.bodyBytes = $util.newBuffer($util.base64.length(object.bodyBytes)), 0);
                    else if (object.bodyBytes.length >= 0)
                        message.bodyBytes = object.bodyBytes;
            if (object.sigMap != null) {
                if (!$util.isObject(object.sigMap))
                    throw $TypeError(".proto.SignedTransaction.sigMap: object expected");
                message.sigMap = $root.proto.SignatureMap.fromObject(object.sigMap, _depth + 1);
            }
            if (object.useSerializedTxMessageHashAlgorithm != null)
                if (object.useSerializedTxMessageHashAlgorithm)
                    message.useSerializedTxMessageHashAlgorithm = $Boolean(object.useSerializedTxMessageHashAlgorithm);
            return message;
        };

        /**
         * Creates a plain object from a SignedTransaction message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SignedTransaction
         * @static
         * @param {proto.SignedTransaction} message SignedTransaction
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignedTransaction.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.bodyBytes = "";
                else {
                    object.bodyBytes = [];
                    if (options.bytes !== $Array)
                        object.bodyBytes = $util.newBuffer(object.bodyBytes);
                }
                object.sigMap = null;
                object.useSerializedTxMessageHashAlgorithm = false;
            }
            if (message.bodyBytes != null && $Object.hasOwnProperty.call(message, "bodyBytes"))
                object.bodyBytes = options.bytes === $String ? $util.base64.encode(message.bodyBytes, 0, message.bodyBytes.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.bodyBytes) : message.bodyBytes;
            if (message.sigMap != null && $Object.hasOwnProperty.call(message, "sigMap"))
                object.sigMap = $root.proto.SignatureMap.toObject(message.sigMap, options, _depth + 1);
            if (message.useSerializedTxMessageHashAlgorithm != null && $Object.hasOwnProperty.call(message, "useSerializedTxMessageHashAlgorithm"))
                object.useSerializedTxMessageHashAlgorithm = message.useSerializedTxMessageHashAlgorithm;
            return object;
        };

        /**
         * Converts this SignedTransaction to JSON.
         * @function toJSON
         * @memberof proto.SignedTransaction
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignedTransaction.prototype.toJSON = function() {
            return SignedTransaction.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignedTransaction
         * @function getTypeUrl
         * @memberof proto.SignedTransaction
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignedTransaction.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SignedTransaction";
        };

        return SignedTransaction;
    })();

    proto.UtilService = (function() {

        /**
         * Constructs a new UtilService service.
         * @memberof proto
         * @classdesc The Utility Service provides a pseudo-random number generator.
         * 
         * The single gRPC call defined for this service simply reports a single
         * pseudo-random number in the transaction record. That value may either
         * be a 32-bit integer within a requested range, or a 384-bit byte array.
         * 
         * ### Block Stream Effects
         * The requested value is reported exclusively in a `UtilPrngOutput` message.
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        const UtilService = function(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        };

        $Object.defineProperty(UtilService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: UtilService, writable: true, enumerable: false, configurable: true });

        /**
         * Creates new UtilService service using the specified rpc implementation.
         * @function create
         * @memberof proto.UtilService
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {UtilService} RPC service. Useful where requests and/or responses are streamed.
         */
        UtilService.create = function(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link proto.UtilService#prng}.
         * @memberof proto.UtilService
         * @typedef prngCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Generate a pseudo-random value.
         * <p>
         * The request body MUST be a
         * [UtilPrngTransactionBody](#proto.UtilPrngTransactionBody)
         * @memberof proto.UtilService
         * @typedef prng
         * @type {{
         *   (request: proto.ITransaction, callback: proto.UtilService.prngCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "prng";
         *   readonly path: "/proto.UtilService/prng";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Generate a pseudo-random value.
         * <p>
         * The request body MUST be a
         * [UtilPrngTransactionBody](#proto.UtilPrngTransactionBody)
         * @name proto.UtilService#prng
         * @type {proto.UtilService.prng}
         */
        $Object.defineProperties(UtilService.prototype.prng = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, UtilService.prototype.prng, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "prng" },
            path: { value: "/proto.UtilService/prng" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        /**
         * Callback as used by {@link proto.UtilService#atomicBatch}.
         * @memberof proto.UtilService
         * @typedef atomicBatchCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {proto.TransactionResponse} [response] TransactionResponse
         */

        /**
         * Execute a batch of transactions atomically.
         * <p>
         * All transactions in the batch will be executed in order, and if any
         * transaction fails, the entire batch will fail.
         * TODO: Add more details about the batch transaction
         * @memberof proto.UtilService
         * @typedef atomicBatch
         * @type {{
         *   (request: proto.ITransaction, callback: proto.UtilService.atomicBatchCallback): void;
         *   (request: proto.ITransaction): Promise<proto.TransactionResponse>;
         *   readonly name: "atomicBatch";
         *   readonly path: "/proto.UtilService/atomicBatch";
         *   readonly requestType: "Transaction";
         *   readonly responseType: "TransactionResponse";
         *   readonly requestStream: undefined;
         *   readonly responseStream: undefined;
         * }}
         */

        /**
         * Execute a batch of transactions atomically.
         * <p>
         * All transactions in the batch will be executed in order, and if any
         * transaction fails, the entire batch will fail.
         * TODO: Add more details about the batch transaction
         * @name proto.UtilService#atomicBatch
         * @type {proto.UtilService.atomicBatch}
         */
        $Object.defineProperties(UtilService.prototype.atomicBatch = function(request, callback) {
            return $protobuf.rpc.Service.prototype.rpcCall.call(this, UtilService.prototype.atomicBatch, $root.proto.Transaction, $root.proto.TransactionResponse, request, callback);
        }, {
            name: { value: "atomicBatch" },
            path: { value: "/proto.UtilService/atomicBatch" },
            requestType: { value: "Transaction" },
            responseType: { value: "TransactionResponse" },
            requestStream: { value: $undefined },
            responseStream: { value: $undefined }
        });

        return UtilService;
    })();

    proto.TokenUnitBalance = (function() {

        /**
         * Properties of a TokenUnitBalance.
         * @typedef {Object} proto.TokenUnitBalance.$Properties
         * @property {proto.TokenID.$Properties|null} [tokenId] A unique token id
         * @property {number|Long|null} [balance] Number of transferable units of the identified token. For token of type FUNGIBLE_COMMON -
         * balance in the smallest denomination. For token of type NON_FUNGIBLE_UNIQUE - the number of
         * NFTs held by the account
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TokenUnitBalance.
         * @memberof proto
         * @interface ITokenUnitBalance
         * @augments proto.TokenUnitBalance.$Properties
         * @deprecated Use proto.TokenUnitBalance.$Properties instead.
         */

        /**
         * Shape of a TokenUnitBalance.
         * @typedef {proto.TokenUnitBalance.$Properties} proto.TokenUnitBalance.$Shape
         */

        /**
         * Constructs a new TokenUnitBalance.
         * @memberof proto
         * @classdesc Represents a TokenUnitBalance.
         * @constructor
         * @param {proto.TokenUnitBalance.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TokenUnitBalance = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * A unique token id
         * @member {proto.TokenID.$Properties|null|undefined} tokenId
         * @memberof proto.TokenUnitBalance
         * @instance
         */
        TokenUnitBalance.prototype.tokenId = null;

        /**
         * Number of transferable units of the identified token. For token of type FUNGIBLE_COMMON -
         * balance in the smallest denomination. For token of type NON_FUNGIBLE_UNIQUE - the number of
         * NFTs held by the account
         * @member {number|Long} balance
         * @memberof proto.TokenUnitBalance
         * @instance
         */
        TokenUnitBalance.prototype.balance = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new TokenUnitBalance instance using the specified properties.
         * @function create
         * @memberof proto.TokenUnitBalance
         * @static
         * @param {proto.TokenUnitBalance.$Properties=} [properties] Properties to set
         * @returns {proto.TokenUnitBalance} TokenUnitBalance instance
         * @type {{
         *   (properties: proto.TokenUnitBalance.$Shape): proto.TokenUnitBalance & proto.TokenUnitBalance.$Shape;
         *   (properties?: proto.TokenUnitBalance.$Properties): proto.TokenUnitBalance;
         * }}
         */
        TokenUnitBalance.create = function(properties) {
            return new TokenUnitBalance(properties);
        };

        /**
         * Encodes the specified TokenUnitBalance message. Does not implicitly {@link proto.TokenUnitBalance.verify|verify} messages.
         * @function encode
         * @memberof proto.TokenUnitBalance
         * @static
         * @param {proto.TokenUnitBalance.$Properties} message TokenUnitBalance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenUnitBalance.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                $root.proto.TokenID.encode(message.tokenId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance") && (typeof message.balance === "object" ? message.balance.low || message.balance.high : message.balance !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.balance);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TokenUnitBalance message, length delimited. Does not implicitly {@link proto.TokenUnitBalance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TokenUnitBalance
         * @static
         * @param {proto.TokenUnitBalance.$Properties} message TokenUnitBalance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenUnitBalance.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TokenUnitBalance message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TokenUnitBalance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TokenUnitBalance & proto.TokenUnitBalance.$Shape} TokenUnitBalance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenUnitBalance.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TokenUnitBalance();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.tokenId = $root.proto.TokenID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tokenId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.balance = value;
                        else
                            delete message.balance;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TokenUnitBalance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TokenUnitBalance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TokenUnitBalance & proto.TokenUnitBalance.$Shape} TokenUnitBalance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenUnitBalance.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenUnitBalance message.
         * @function verify
         * @memberof proto.TokenUnitBalance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenUnitBalance.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId")) {
                let error = $root.proto.TokenID.verify(message.tokenId, _depth + 1);
                if (error)
                    return "tokenId." + error;
            }
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (!$util.isInteger(message.balance) && !(message.balance && $util.isInteger(message.balance.low) && $util.isInteger(message.balance.high)))
                    return "balance: integer|Long expected";
            return null;
        };

        /**
         * Creates a TokenUnitBalance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TokenUnitBalance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TokenUnitBalance} TokenUnitBalance
         */
        TokenUnitBalance.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TokenUnitBalance)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TokenUnitBalance: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TokenUnitBalance();
            if (object.tokenId != null) {
                if (!$util.isObject(object.tokenId))
                    throw $TypeError(".proto.TokenUnitBalance.tokenId: object expected");
                message.tokenId = $root.proto.TokenID.fromObject(object.tokenId, _depth + 1);
            }
            if (object.balance != null)
                if (typeof object.balance === "object" ? object.balance.low || object.balance.high : $Number(object.balance) !== 0)
                    if ($util.Long)
                        message.balance = $util.Long.fromValue(object.balance, true);
                    else if (typeof object.balance === "string")
                        message.balance = $parseInt(object.balance, 10);
                    else if (typeof object.balance === "number")
                        message.balance = object.balance;
                    else if (typeof object.balance === "object")
                        message.balance = new $util.LongBits(object.balance.low >>> 0, object.balance.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a TokenUnitBalance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TokenUnitBalance
         * @static
         * @param {proto.TokenUnitBalance} message TokenUnitBalance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenUnitBalance.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.tokenId = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.balance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.balance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.tokenId != null && $Object.hasOwnProperty.call(message, "tokenId"))
                object.tokenId = $root.proto.TokenID.toObject(message.tokenId, options, _depth + 1);
            if (message.balance != null && $Object.hasOwnProperty.call(message, "balance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.balance = typeof message.balance === "number" ? $BigInt(message.balance) : $util.Long.fromBits(message.balance.low >>> 0, message.balance.high >>> 0, true).toBigInt();
                else if (typeof message.balance === "number")
                    object.balance = options.longs === $String ? $String(message.balance) : message.balance;
                else
                    object.balance = options.longs === $String ? $util.Long.prototype.toString.call(message.balance) : options.longs === $Number ? new $util.LongBits(message.balance.low >>> 0, message.balance.high >>> 0).toNumber(true) : message.balance;
            return object;
        };

        /**
         * Converts this TokenUnitBalance to JSON.
         * @function toJSON
         * @memberof proto.TokenUnitBalance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenUnitBalance.prototype.toJSON = function() {
            return TokenUnitBalance.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TokenUnitBalance
         * @function getTypeUrl
         * @memberof proto.TokenUnitBalance
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TokenUnitBalance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TokenUnitBalance";
        };

        return TokenUnitBalance;
    })();

    proto.SingleAccountBalances = (function() {

        /**
         * Properties of a SingleAccountBalances.
         * @typedef {Object} proto.SingleAccountBalances.$Properties
         * @property {proto.AccountID.$Properties|null} [accountID] The account
         * @property {number|Long|null} [hbarBalance] The account's hbar balance
         * @property {Array.<proto.TokenUnitBalance.$Properties>|null} [tokenUnitBalances] The list of the account's token balances
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SingleAccountBalances.
         * @memberof proto
         * @interface ISingleAccountBalances
         * @augments proto.SingleAccountBalances.$Properties
         * @deprecated Use proto.SingleAccountBalances.$Properties instead.
         */

        /**
         * Shape of a SingleAccountBalances.
         * @typedef {{
         *   accountID?: proto.AccountID.$Shape|null;
         *   hbarBalance?: number|Long|null;
         *   tokenUnitBalances?: Array.<proto.TokenUnitBalance.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.SingleAccountBalances.$Shape
         */

        /**
         * Constructs a new SingleAccountBalances.
         * @memberof proto
         * @classdesc Includes all currency balances (both hbar and token) of a single account in the ledger.
         * @constructor
         * @param {proto.SingleAccountBalances.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SingleAccountBalances = function (properties) {
            this.tokenUnitBalances = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The account
         * @member {proto.AccountID.$Properties|null|undefined} accountID
         * @memberof proto.SingleAccountBalances
         * @instance
         */
        SingleAccountBalances.prototype.accountID = null;

        /**
         * The account's hbar balance
         * @member {number|Long} hbarBalance
         * @memberof proto.SingleAccountBalances
         * @instance
         */
        SingleAccountBalances.prototype.hbarBalance = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * The list of the account's token balances
         * @member {Array.<proto.TokenUnitBalance.$Properties>} tokenUnitBalances
         * @memberof proto.SingleAccountBalances
         * @instance
         */
        SingleAccountBalances.prototype.tokenUnitBalances = $util.emptyArray;

        /**
         * Creates a new SingleAccountBalances instance using the specified properties.
         * @function create
         * @memberof proto.SingleAccountBalances
         * @static
         * @param {proto.SingleAccountBalances.$Properties=} [properties] Properties to set
         * @returns {proto.SingleAccountBalances} SingleAccountBalances instance
         * @type {{
         *   (properties: proto.SingleAccountBalances.$Shape): proto.SingleAccountBalances & proto.SingleAccountBalances.$Shape;
         *   (properties?: proto.SingleAccountBalances.$Properties): proto.SingleAccountBalances;
         * }}
         */
        SingleAccountBalances.create = function(properties) {
            return new SingleAccountBalances(properties);
        };

        /**
         * Encodes the specified SingleAccountBalances message. Does not implicitly {@link proto.SingleAccountBalances.verify|verify} messages.
         * @function encode
         * @memberof proto.SingleAccountBalances
         * @static
         * @param {proto.SingleAccountBalances.$Properties} message SingleAccountBalances message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleAccountBalances.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                $root.proto.AccountID.encode(message.accountID, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.hbarBalance != null && $Object.hasOwnProperty.call(message, "hbarBalance") && (typeof message.hbarBalance === "object" ? message.hbarBalance.low || message.hbarBalance.high : message.hbarBalance !== 0))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.hbarBalance);
            if (message.tokenUnitBalances != null && message.tokenUnitBalances.length)
                for (let i = 0; i < message.tokenUnitBalances.length; ++i)
                    $root.proto.TokenUnitBalance.encode(message.tokenUnitBalances[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SingleAccountBalances message, length delimited. Does not implicitly {@link proto.SingleAccountBalances.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SingleAccountBalances
         * @static
         * @param {proto.SingleAccountBalances.$Properties} message SingleAccountBalances message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleAccountBalances.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SingleAccountBalances message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SingleAccountBalances
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SingleAccountBalances & proto.SingleAccountBalances.$Shape} SingleAccountBalances
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleAccountBalances.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SingleAccountBalances();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.accountID = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.accountID);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.hbarBalance = value;
                        else
                            delete message.hbarBalance;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tokenUnitBalances && message.tokenUnitBalances.length))
                            message.tokenUnitBalances = [];
                        message.tokenUnitBalances.push($root.proto.TokenUnitBalance.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SingleAccountBalances message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SingleAccountBalances
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SingleAccountBalances & proto.SingleAccountBalances.$Shape} SingleAccountBalances
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleAccountBalances.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SingleAccountBalances message.
         * @function verify
         * @memberof proto.SingleAccountBalances
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SingleAccountBalances.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID")) {
                let error = $root.proto.AccountID.verify(message.accountID, _depth + 1);
                if (error)
                    return "accountID." + error;
            }
            if (message.hbarBalance != null && $Object.hasOwnProperty.call(message, "hbarBalance"))
                if (!$util.isInteger(message.hbarBalance) && !(message.hbarBalance && $util.isInteger(message.hbarBalance.low) && $util.isInteger(message.hbarBalance.high)))
                    return "hbarBalance: integer|Long expected";
            if (message.tokenUnitBalances != null && $Object.hasOwnProperty.call(message, "tokenUnitBalances")) {
                if (!$Array.isArray(message.tokenUnitBalances))
                    return "tokenUnitBalances: array expected";
                for (let i = 0; i < message.tokenUnitBalances.length; ++i) {
                    let error = $root.proto.TokenUnitBalance.verify(message.tokenUnitBalances[i], _depth + 1);
                    if (error)
                        return "tokenUnitBalances." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SingleAccountBalances message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SingleAccountBalances
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SingleAccountBalances} SingleAccountBalances
         */
        SingleAccountBalances.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SingleAccountBalances)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SingleAccountBalances: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SingleAccountBalances();
            if (object.accountID != null) {
                if (!$util.isObject(object.accountID))
                    throw $TypeError(".proto.SingleAccountBalances.accountID: object expected");
                message.accountID = $root.proto.AccountID.fromObject(object.accountID, _depth + 1);
            }
            if (object.hbarBalance != null)
                if (typeof object.hbarBalance === "object" ? object.hbarBalance.low || object.hbarBalance.high : $Number(object.hbarBalance) !== 0)
                    if ($util.Long)
                        message.hbarBalance = $util.Long.fromValue(object.hbarBalance, true);
                    else if (typeof object.hbarBalance === "string")
                        message.hbarBalance = $parseInt(object.hbarBalance, 10);
                    else if (typeof object.hbarBalance === "number")
                        message.hbarBalance = object.hbarBalance;
                    else if (typeof object.hbarBalance === "object")
                        message.hbarBalance = new $util.LongBits(object.hbarBalance.low >>> 0, object.hbarBalance.high >>> 0).toNumber(true);
            if (object.tokenUnitBalances) {
                if (!$Array.isArray(object.tokenUnitBalances))
                    throw $TypeError(".proto.SingleAccountBalances.tokenUnitBalances: array expected");
                message.tokenUnitBalances = $Array(object.tokenUnitBalances.length);
                for (let i = 0; i < object.tokenUnitBalances.length; ++i) {
                    if (!$util.isObject(object.tokenUnitBalances[i]))
                        throw $TypeError(".proto.SingleAccountBalances.tokenUnitBalances: object expected");
                    message.tokenUnitBalances[i] = $root.proto.TokenUnitBalance.fromObject(object.tokenUnitBalances[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SingleAccountBalances message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SingleAccountBalances
         * @static
         * @param {proto.SingleAccountBalances} message SingleAccountBalances
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SingleAccountBalances.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tokenUnitBalances = [];
            if (options.defaults) {
                object.accountID = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.hbarBalance = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.hbarBalance = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.accountID != null && $Object.hasOwnProperty.call(message, "accountID"))
                object.accountID = $root.proto.AccountID.toObject(message.accountID, options, _depth + 1);
            if (message.hbarBalance != null && $Object.hasOwnProperty.call(message, "hbarBalance"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.hbarBalance = typeof message.hbarBalance === "number" ? $BigInt(message.hbarBalance) : $util.Long.fromBits(message.hbarBalance.low >>> 0, message.hbarBalance.high >>> 0, true).toBigInt();
                else if (typeof message.hbarBalance === "number")
                    object.hbarBalance = options.longs === $String ? $String(message.hbarBalance) : message.hbarBalance;
                else
                    object.hbarBalance = options.longs === $String ? $util.Long.prototype.toString.call(message.hbarBalance) : options.longs === $Number ? new $util.LongBits(message.hbarBalance.low >>> 0, message.hbarBalance.high >>> 0).toNumber(true) : message.hbarBalance;
            if (message.tokenUnitBalances && message.tokenUnitBalances.length) {
                object.tokenUnitBalances = $Array(message.tokenUnitBalances.length);
                for (let j = 0; j < message.tokenUnitBalances.length; ++j)
                    object.tokenUnitBalances[j] = $root.proto.TokenUnitBalance.toObject(message.tokenUnitBalances[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this SingleAccountBalances to JSON.
         * @function toJSON
         * @memberof proto.SingleAccountBalances
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SingleAccountBalances.prototype.toJSON = function() {
            return SingleAccountBalances.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SingleAccountBalances
         * @function getTypeUrl
         * @memberof proto.SingleAccountBalances
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SingleAccountBalances.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SingleAccountBalances";
        };

        return SingleAccountBalances;
    })();

    proto.AllAccountBalances = (function() {

        /**
         * Properties of an AllAccountBalances.
         * @typedef {Object} proto.AllAccountBalances.$Properties
         * @property {proto.Timestamp.$Properties|null} [consensusTimestamp] An instant in consensus time
         * @property {Array.<proto.SingleAccountBalances.$Properties>|null} [allAccounts] The list of account balances for all accounts, after handling all transactions with consensus
         * timestamp up to and including the above instant
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of an AllAccountBalances.
         * @memberof proto
         * @interface IAllAccountBalances
         * @augments proto.AllAccountBalances.$Properties
         * @deprecated Use proto.AllAccountBalances.$Properties instead.
         */

        /**
         * Shape of an AllAccountBalances.
         * @typedef {{
         *   consensusTimestamp?: proto.Timestamp.$Shape|null;
         *   allAccounts?: Array.<proto.SingleAccountBalances.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.AllAccountBalances.$Shape
         */

        /**
         * Constructs a new AllAccountBalances.
         * @memberof proto
         * @classdesc Includes all currency balances (both hbar and token) of all accounts in the ledger.
         * @constructor
         * @param {proto.AllAccountBalances.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const AllAccountBalances = function (properties) {
            this.allAccounts = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * An instant in consensus time
         * @member {proto.Timestamp.$Properties|null|undefined} consensusTimestamp
         * @memberof proto.AllAccountBalances
         * @instance
         */
        AllAccountBalances.prototype.consensusTimestamp = null;

        /**
         * The list of account balances for all accounts, after handling all transactions with consensus
         * timestamp up to and including the above instant
         * @member {Array.<proto.SingleAccountBalances.$Properties>} allAccounts
         * @memberof proto.AllAccountBalances
         * @instance
         */
        AllAccountBalances.prototype.allAccounts = $util.emptyArray;

        /**
         * Creates a new AllAccountBalances instance using the specified properties.
         * @function create
         * @memberof proto.AllAccountBalances
         * @static
         * @param {proto.AllAccountBalances.$Properties=} [properties] Properties to set
         * @returns {proto.AllAccountBalances} AllAccountBalances instance
         * @type {{
         *   (properties: proto.AllAccountBalances.$Shape): proto.AllAccountBalances & proto.AllAccountBalances.$Shape;
         *   (properties?: proto.AllAccountBalances.$Properties): proto.AllAccountBalances;
         * }}
         */
        AllAccountBalances.create = function(properties) {
            return new AllAccountBalances(properties);
        };

        /**
         * Encodes the specified AllAccountBalances message. Does not implicitly {@link proto.AllAccountBalances.verify|verify} messages.
         * @function encode
         * @memberof proto.AllAccountBalances
         * @static
         * @param {proto.AllAccountBalances.$Properties} message AllAccountBalances message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllAccountBalances.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp"))
                $root.proto.Timestamp.encode(message.consensusTimestamp, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.allAccounts != null && message.allAccounts.length)
                for (let i = 0; i < message.allAccounts.length; ++i)
                    $root.proto.SingleAccountBalances.encode(message.allAccounts[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AllAccountBalances message, length delimited. Does not implicitly {@link proto.AllAccountBalances.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AllAccountBalances
         * @static
         * @param {proto.AllAccountBalances.$Properties} message AllAccountBalances message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllAccountBalances.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes an AllAccountBalances message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AllAccountBalances
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AllAccountBalances & proto.AllAccountBalances.$Shape} AllAccountBalances
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllAccountBalances.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.AllAccountBalances();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.consensusTimestamp = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusTimestamp);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.allAccounts && message.allAccounts.length))
                            message.allAccounts = [];
                        message.allAccounts.push($root.proto.SingleAccountBalances.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes an AllAccountBalances message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AllAccountBalances
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AllAccountBalances & proto.AllAccountBalances.$Shape} AllAccountBalances
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllAccountBalances.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllAccountBalances message.
         * @function verify
         * @memberof proto.AllAccountBalances
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllAccountBalances.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp")) {
                let error = $root.proto.Timestamp.verify(message.consensusTimestamp, _depth + 1);
                if (error)
                    return "consensusTimestamp." + error;
            }
            if (message.allAccounts != null && $Object.hasOwnProperty.call(message, "allAccounts")) {
                if (!$Array.isArray(message.allAccounts))
                    return "allAccounts: array expected";
                for (let i = 0; i < message.allAccounts.length; ++i) {
                    let error = $root.proto.SingleAccountBalances.verify(message.allAccounts[i], _depth + 1);
                    if (error)
                        return "allAccounts." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllAccountBalances message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AllAccountBalances
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AllAccountBalances} AllAccountBalances
         */
        AllAccountBalances.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.AllAccountBalances)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.AllAccountBalances: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.AllAccountBalances();
            if (object.consensusTimestamp != null) {
                if (!$util.isObject(object.consensusTimestamp))
                    throw $TypeError(".proto.AllAccountBalances.consensusTimestamp: object expected");
                message.consensusTimestamp = $root.proto.Timestamp.fromObject(object.consensusTimestamp, _depth + 1);
            }
            if (object.allAccounts) {
                if (!$Array.isArray(object.allAccounts))
                    throw $TypeError(".proto.AllAccountBalances.allAccounts: array expected");
                message.allAccounts = $Array(object.allAccounts.length);
                for (let i = 0; i < object.allAccounts.length; ++i) {
                    if (!$util.isObject(object.allAccounts[i]))
                        throw $TypeError(".proto.AllAccountBalances.allAccounts: object expected");
                    message.allAccounts[i] = $root.proto.SingleAccountBalances.fromObject(object.allAccounts[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllAccountBalances message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AllAccountBalances
         * @static
         * @param {proto.AllAccountBalances} message AllAccountBalances
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllAccountBalances.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.allAccounts = [];
            if (options.defaults)
                object.consensusTimestamp = null;
            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp"))
                object.consensusTimestamp = $root.proto.Timestamp.toObject(message.consensusTimestamp, options, _depth + 1);
            if (message.allAccounts && message.allAccounts.length) {
                object.allAccounts = $Array(message.allAccounts.length);
                for (let j = 0; j < message.allAccounts.length; ++j)
                    object.allAccounts[j] = $root.proto.SingleAccountBalances.toObject(message.allAccounts[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this AllAccountBalances to JSON.
         * @function toJSON
         * @memberof proto.AllAccountBalances
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllAccountBalances.prototype.toJSON = function() {
            return AllAccountBalances.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for AllAccountBalances
         * @function getTypeUrl
         * @memberof proto.AllAccountBalances
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        AllAccountBalances.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AllAccountBalances";
        };

        return AllAccountBalances;
    })();

    proto.ContractActions = (function() {

        /**
         * Properties of a ContractActions.
         * @typedef {Object} proto.ContractActions.$Properties
         * @property {Array.<proto.ContractAction.$Properties>|null} [contractActions] ContractActions contractActions
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractActions.
         * @memberof proto
         * @interface IContractActions
         * @augments proto.ContractActions.$Properties
         * @deprecated Use proto.ContractActions.$Properties instead.
         */

        /**
         * Shape of a ContractActions.
         * @typedef {{
         *   contractActions?: Array.<proto.ContractAction.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractActions.$Shape
         */

        /**
         * Constructs a new ContractActions.
         * @memberof proto
         * @classdesc Represents a ContractActions.
         * @constructor
         * @param {proto.ContractActions.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractActions = function (properties) {
            this.contractActions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ContractActions contractActions.
         * @member {Array.<proto.ContractAction.$Properties>} contractActions
         * @memberof proto.ContractActions
         * @instance
         */
        ContractActions.prototype.contractActions = $util.emptyArray;

        /**
         * Creates a new ContractActions instance using the specified properties.
         * @function create
         * @memberof proto.ContractActions
         * @static
         * @param {proto.ContractActions.$Properties=} [properties] Properties to set
         * @returns {proto.ContractActions} ContractActions instance
         * @type {{
         *   (properties: proto.ContractActions.$Shape): proto.ContractActions & proto.ContractActions.$Shape;
         *   (properties?: proto.ContractActions.$Properties): proto.ContractActions;
         * }}
         */
        ContractActions.create = function(properties) {
            return new ContractActions(properties);
        };

        /**
         * Encodes the specified ContractActions message. Does not implicitly {@link proto.ContractActions.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractActions
         * @static
         * @param {proto.ContractActions.$Properties} message ContractActions message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractActions.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contractActions != null && message.contractActions.length)
                for (let i = 0; i < message.contractActions.length; ++i)
                    $root.proto.ContractAction.encode(message.contractActions[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractActions message, length delimited. Does not implicitly {@link proto.ContractActions.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractActions
         * @static
         * @param {proto.ContractActions.$Properties} message ContractActions message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractActions.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractActions message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractActions
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractActions & proto.ContractActions.$Shape} ContractActions
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractActions.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractActions();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.contractActions && message.contractActions.length))
                            message.contractActions = [];
                        message.contractActions.push($root.proto.ContractAction.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractActions message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractActions
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractActions & proto.ContractActions.$Shape} ContractActions
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractActions.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractActions message.
         * @function verify
         * @memberof proto.ContractActions
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractActions.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.contractActions != null && $Object.hasOwnProperty.call(message, "contractActions")) {
                if (!$Array.isArray(message.contractActions))
                    return "contractActions: array expected";
                for (let i = 0; i < message.contractActions.length; ++i) {
                    let error = $root.proto.ContractAction.verify(message.contractActions[i], _depth + 1);
                    if (error)
                        return "contractActions." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ContractActions message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractActions
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractActions} ContractActions
         */
        ContractActions.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractActions)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractActions: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractActions();
            if (object.contractActions) {
                if (!$Array.isArray(object.contractActions))
                    throw $TypeError(".proto.ContractActions.contractActions: array expected");
                message.contractActions = $Array(object.contractActions.length);
                for (let i = 0; i < object.contractActions.length; ++i) {
                    if (!$util.isObject(object.contractActions[i]))
                        throw $TypeError(".proto.ContractActions.contractActions: object expected");
                    message.contractActions[i] = $root.proto.ContractAction.fromObject(object.contractActions[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractActions message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractActions
         * @static
         * @param {proto.ContractActions} message ContractActions
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractActions.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.contractActions = [];
            if (message.contractActions && message.contractActions.length) {
                object.contractActions = $Array(message.contractActions.length);
                for (let j = 0; j < message.contractActions.length; ++j)
                    object.contractActions[j] = $root.proto.ContractAction.toObject(message.contractActions[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ContractActions to JSON.
         * @function toJSON
         * @memberof proto.ContractActions
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractActions.prototype.toJSON = function() {
            return ContractActions.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractActions
         * @function getTypeUrl
         * @memberof proto.ContractActions
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractActions.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractActions";
        };

        return ContractActions;
    })();

    /**
     * The type of action described by the action proto.
     * @name proto.ContractActionType
     * @enum {number}
     * @property {number} NO_ACTION=0 default non-value.
     * @property {number} CALL=1 Most CALL, CALLCODE, DELEGATECALL, and STATICCALL, and first action of ContractCall/ContractCallLocal to deployed
     * contracts. This does not include calls to system or precompiled contracts.
     * @property {number} CREATE=2 CREATE value
     * @property {number} PRECOMPILE=3 like Call, but to precompiled contracts (0x1 to 0x9 as of Berlin)
     * @property {number} SYSTEM=4 Call, but to system contract like HTS or ERC20 facades over Token accounts
     */
    proto.ContractActionType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "NO_ACTION"] = 0;
        values[valuesById[1] = "CALL"] = 1;
        values[valuesById[2] = "CREATE"] = 2;
        values[valuesById[3] = "PRECOMPILE"] = 3;
        values[valuesById[4] = "SYSTEM"] = 4;
        return values;
    })();

    /**
     * The specific operation type of a call. The OP prefix has been added to avoid name collisions for
     * the CALL and CREATE operation types since both ContractActionType and CallOperationType enums are
     * used in ContractAction
     * @name proto.CallOperationType
     * @enum {number}
     * @property {number} OP_UNKNOWN=0 default operation type is UNKNOWN
     * @property {number} OP_CALL=1 CALL operation type.
     * @property {number} OP_CALLCODE=2 CALLCODE operation type
     * @property {number} OP_DELEGATECALL=3 DELEGATECALL operation type
     * @property {number} OP_STATICCALL=4 STATICCALL operation type
     * @property {number} OP_CREATE=5 CREATE operation type
     * @property {number} OP_CREATE2=6 CREATE2 operation type
     */
    proto.CallOperationType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "OP_UNKNOWN"] = 0;
        values[valuesById[1] = "OP_CALL"] = 1;
        values[valuesById[2] = "OP_CALLCODE"] = 2;
        values[valuesById[3] = "OP_DELEGATECALL"] = 3;
        values[valuesById[4] = "OP_STATICCALL"] = 4;
        values[valuesById[5] = "OP_CREATE"] = 5;
        values[valuesById[6] = "OP_CREATE2"] = 6;
        return values;
    })();

    proto.ContractAction = (function() {

        /**
         * Properties of a ContractAction.
         * @typedef {Object} proto.ContractAction.$Properties
         * @property {proto.ContractActionType|null} [callType] The type of this action.
         * @property {proto.AccountID.$Properties|null} [callingAccount] If the caller was a regular account, the AccountID.
         * @property {proto.ContractID.$Properties|null} [callingContract] If the caller was a smart contract account, the ContractID.
         * @property {number|Long|null} [gas] The upper limit of gas this action can spend.
         * @property {Uint8Array|null} [input] Bytes passed in as input data to this action.
         * @property {proto.AccountID.$Properties|null} [recipientAccount] The AccountID of the recipient if the recipient is an account. Only HBars will be transferred, no other side
         * effects should be expected.
         * @property {proto.ContractID.$Properties|null} [recipientContract] The ContractID of the recipient if the recipient is a smart contract.
         * @property {Uint8Array|null} [targetedAddress] The bytes of the targeted by the action address.
         * Only set on failed executions. If set, denotes that the address did not
         * correspond to any account or contract at the time of finalization of
         * this action.
         * An example would be a failed lazy create as per HIP-583.
         * @property {number|Long|null} [value] The value (in tinybars) that is associated with this action.
         * @property {number|Long|null} [gasUsed] The actual gas spent by this action.
         * @property {Uint8Array|null} [output] If successful, the output bytes of the action.
         * @property {Uint8Array|null} [revertReason] The contract itself caused the transaction to fail via the `REVERT` operation
         * @property {Uint8Array|null} [error] The transaction itself failed without an explicit `REVERT`
         * @property {number|null} [callDepth] The nesting depth of this call. The original action is at depth=0.
         * @property {proto.CallOperationType|null} [callOperationType] The call operation type
         * @property {"callingAccount"|"callingContract"} [caller] Only the first action can come from an account, the rest will come from contracts.  Because of DELEGATECALL
         * and CALLCODE the caller of actions whose parent is an account may also be an account.
         * @property {"recipientAccount"|"recipientContract"|"targetedAddress"} [recipient] Who this action is directed to.
         * @property {"output"|"revertReason"|"error"} [resultData] The result data of the action.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractAction.
         * @memberof proto
         * @interface IContractAction
         * @augments proto.ContractAction.$Properties
         * @deprecated Use proto.ContractAction.$Properties instead.
         */

        /**
         * Narrowed shape of a ContractAction.
         * @typedef {{
         *   callType?: proto.ContractActionType|null;
         *   callingAccount?: proto.AccountID.$Shape|null;
         *   callingContract?: proto.ContractID.$Shape|null;
         *   gas?: number|Long|null;
         *   input?: Uint8Array|null;
         *   recipientAccount?: proto.AccountID.$Shape|null;
         *   recipientContract?: proto.ContractID.$Shape|null;
         *   targetedAddress?: Uint8Array|null;
         *   value?: number|Long|null;
         *   gasUsed?: number|Long|null;
         *   output?: Uint8Array|null;
         *   revertReason?: Uint8Array|null;
         *   error?: Uint8Array|null;
         *   callDepth?: number|null;
         *   callOperationType?: proto.CallOperationType|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ caller?: undefined; callingAccount?: null; callingContract?: null }|{ caller?: "callingAccount"; callingAccount: proto.AccountID.$Shape; callingContract?: null }|{ caller?: "callingContract"; callingAccount?: null; callingContract: proto.ContractID.$Shape })
         * ) & (
         *   ({ recipient?: undefined; recipientAccount?: null; recipientContract?: null; targetedAddress?: null }|{ recipient?: "recipientAccount"; recipientAccount: proto.AccountID.$Shape; recipientContract?: null; targetedAddress?: null }|{ recipient?: "recipientContract"; recipientAccount?: null; recipientContract: proto.ContractID.$Shape; targetedAddress?: null }|{ recipient?: "targetedAddress"; recipientAccount?: null; recipientContract?: null; targetedAddress: Uint8Array })
         * ) & (
         *   ({ resultData?: undefined; output?: null; revertReason?: null; error?: null }|{ resultData?: "output"; output: Uint8Array; revertReason?: null; error?: null }|{ resultData?: "revertReason"; output?: null; revertReason: Uint8Array; error?: null }|{ resultData?: "error"; output?: null; revertReason?: null; error: Uint8Array })
         * )} proto.ContractAction.$Shape
         */

        /**
         * Constructs a new ContractAction.
         * @memberof proto
         * @classdesc A finer grained action with a function result. Sometimes called "internal transactions." The function call itself
         * will be the first action in a list, followed by sub-action in the order they were executed.
         * @constructor
         * @param {proto.ContractAction.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractAction = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The type of this action.
         * @member {proto.ContractActionType} callType
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.callType = 0;

        /**
         * If the caller was a regular account, the AccountID.
         * @member {proto.AccountID.$Properties|null|undefined} callingAccount
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.callingAccount = null;

        /**
         * If the caller was a smart contract account, the ContractID.
         * @member {proto.ContractID.$Properties|null|undefined} callingContract
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.callingContract = null;

        /**
         * The upper limit of gas this action can spend.
         * @member {number|Long} gas
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.gas = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Bytes passed in as input data to this action.
         * @member {Uint8Array} input
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.input = $util.newBuffer([]);

        /**
         * The AccountID of the recipient if the recipient is an account. Only HBars will be transferred, no other side
         * effects should be expected.
         * @member {proto.AccountID.$Properties|null|undefined} recipientAccount
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.recipientAccount = null;

        /**
         * The ContractID of the recipient if the recipient is a smart contract.
         * @member {proto.ContractID.$Properties|null|undefined} recipientContract
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.recipientContract = null;

        /**
         * The bytes of the targeted by the action address.
         * Only set on failed executions. If set, denotes that the address did not
         * correspond to any account or contract at the time of finalization of
         * this action.
         * An example would be a failed lazy create as per HIP-583.
         * @member {Uint8Array|null|undefined} targetedAddress
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.targetedAddress = null;

        /**
         * The value (in tinybars) that is associated with this action.
         * @member {number|Long} value
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.value = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * The actual gas spent by this action.
         * @member {number|Long} gasUsed
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.gasUsed = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * If successful, the output bytes of the action.
         * @member {Uint8Array|null|undefined} output
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.output = null;

        /**
         * The contract itself caused the transaction to fail via the `REVERT` operation
         * @member {Uint8Array|null|undefined} revertReason
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.revertReason = null;

        /**
         * The transaction itself failed without an explicit `REVERT`
         * @member {Uint8Array|null|undefined} error
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.error = null;

        /**
         * The nesting depth of this call. The original action is at depth=0.
         * @member {number} callDepth
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.callDepth = 0;

        /**
         * The call operation type
         * @member {proto.CallOperationType} callOperationType
         * @memberof proto.ContractAction
         * @instance
         */
        ContractAction.prototype.callOperationType = 0;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * Only the first action can come from an account, the rest will come from contracts.  Because of DELEGATECALL
         * and CALLCODE the caller of actions whose parent is an account may also be an account.
         * @member {"callingAccount"|"callingContract"|undefined} caller
         * @memberof proto.ContractAction
         * @instance
         */
        $Object.defineProperty(ContractAction.prototype, "caller", {
            get: $util.oneOfGetter($oneOfFields = ["callingAccount", "callingContract"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Who this action is directed to.
         * @member {"recipientAccount"|"recipientContract"|"targetedAddress"|undefined} recipient
         * @memberof proto.ContractAction
         * @instance
         */
        $Object.defineProperty(ContractAction.prototype, "recipient", {
            get: $util.oneOfGetter($oneOfFields = ["recipientAccount", "recipientContract", "targetedAddress"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * The result data of the action.
         * @member {"output"|"revertReason"|"error"|undefined} resultData
         * @memberof proto.ContractAction
         * @instance
         */
        $Object.defineProperty(ContractAction.prototype, "resultData", {
            get: $util.oneOfGetter($oneOfFields = ["output", "revertReason", "error"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ContractAction instance using the specified properties.
         * @function create
         * @memberof proto.ContractAction
         * @static
         * @param {proto.ContractAction.$Properties=} [properties] Properties to set
         * @returns {proto.ContractAction} ContractAction instance
         * @type {{
         *   (properties: proto.ContractAction.$Shape): proto.ContractAction & proto.ContractAction.$Shape;
         *   (properties?: proto.ContractAction.$Properties): proto.ContractAction;
         * }}
         */
        ContractAction.create = function(properties) {
            return new ContractAction(properties);
        };

        /**
         * Encodes the specified ContractAction message. Does not implicitly {@link proto.ContractAction.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractAction
         * @static
         * @param {proto.ContractAction.$Properties} message ContractAction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractAction.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.callType != null && $Object.hasOwnProperty.call(message, "callType") && message.callType !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.callType);
            if (message.callingAccount != null && $Object.hasOwnProperty.call(message, "callingAccount"))
                $root.proto.AccountID.encode(message.callingAccount, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.callingContract != null && $Object.hasOwnProperty.call(message, "callingContract"))
                $root.proto.ContractID.encode(message.callingContract, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas") && (typeof message.gas === "object" ? message.gas.low || message.gas.high : message.gas !== 0))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.gas);
            if (message.input != null && $Object.hasOwnProperty.call(message, "input") && message.input.length)
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.input);
            if (message.recipientAccount != null && $Object.hasOwnProperty.call(message, "recipientAccount"))
                $root.proto.AccountID.encode(message.recipientAccount, writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.recipientContract != null && $Object.hasOwnProperty.call(message, "recipientContract"))
                $root.proto.ContractID.encode(message.recipientContract, writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
            if (message.targetedAddress != null && $Object.hasOwnProperty.call(message, "targetedAddress"))
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.targetedAddress);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && (typeof message.value === "object" ? message.value.low || message.value.high : message.value !== 0))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.value);
            if (message.gasUsed != null && $Object.hasOwnProperty.call(message, "gasUsed") && (typeof message.gasUsed === "object" ? message.gasUsed.low || message.gasUsed.high : message.gasUsed !== 0))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.gasUsed);
            if (message.output != null && $Object.hasOwnProperty.call(message, "output"))
                writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.output);
            if (message.revertReason != null && $Object.hasOwnProperty.call(message, "revertReason"))
                writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.revertReason);
            if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 13, wireType 2 =*/106).bytes(message.error);
            if (message.callDepth != null && $Object.hasOwnProperty.call(message, "callDepth") && message.callDepth !== 0)
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.callDepth);
            if (message.callOperationType != null && $Object.hasOwnProperty.call(message, "callOperationType") && message.callOperationType !== 0)
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.callOperationType);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractAction message, length delimited. Does not implicitly {@link proto.ContractAction.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractAction
         * @static
         * @param {proto.ContractAction.$Properties} message ContractAction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractAction.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractAction message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractAction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractAction & proto.ContractAction.$Shape} ContractAction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractAction.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractAction();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.callType = value;
                        else
                            delete message.callType;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.callingAccount = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.callingAccount);
                        message.caller = "callingAccount";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.callingContract = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.callingContract);
                        message.caller = "callingContract";
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.gas = value;
                        else
                            delete message.gas;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.input = value;
                        else
                            delete message.input;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.recipientAccount = $root.proto.AccountID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.recipientAccount);
                        message.recipient = "recipientAccount";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.recipientContract = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.recipientContract);
                        message.recipient = "recipientContract";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.targetedAddress = reader.bytes();
                        message.recipient = "targetedAddress";
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.gasUsed = value;
                        else
                            delete message.gasUsed;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.output = reader.bytes();
                        message.resultData = "output";
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.revertReason = reader.bytes();
                        message.resultData = "revertReason";
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.error = reader.bytes();
                        message.resultData = "error";
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.callDepth = value;
                        else
                            delete message.callDepth;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.callOperationType = value;
                        else
                            delete message.callOperationType;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractAction message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractAction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractAction & proto.ContractAction.$Shape} ContractAction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractAction.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractAction message.
         * @function verify
         * @memberof proto.ContractAction
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractAction.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.callType != null && $Object.hasOwnProperty.call(message, "callType"))
                if (typeof message.callType !== "number" || (message.callType | 0) !== message.callType)
                    return "callType: enum value expected";
            if (message.callingAccount != null && $Object.hasOwnProperty.call(message, "callingAccount")) {
                properties.caller = 1;
                {
                    let error = $root.proto.AccountID.verify(message.callingAccount, _depth + 1);
                    if (error)
                        return "callingAccount." + error;
                }
            }
            if (message.callingContract != null && $Object.hasOwnProperty.call(message, "callingContract")) {
                if (properties.caller === 1)
                    return "caller: multiple values";
                properties.caller = 1;
                {
                    let error = $root.proto.ContractID.verify(message.callingContract, _depth + 1);
                    if (error)
                        return "callingContract." + error;
                }
            }
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (!$util.isInteger(message.gas) && !(message.gas && $util.isInteger(message.gas.low) && $util.isInteger(message.gas.high)))
                    return "gas: integer|Long expected";
            if (message.input != null && $Object.hasOwnProperty.call(message, "input"))
                if (!(message.input && typeof message.input.length === "number" || $util.isString(message.input)))
                    return "input: buffer expected";
            if (message.recipientAccount != null && $Object.hasOwnProperty.call(message, "recipientAccount")) {
                properties.recipient = 1;
                {
                    let error = $root.proto.AccountID.verify(message.recipientAccount, _depth + 1);
                    if (error)
                        return "recipientAccount." + error;
                }
            }
            if (message.recipientContract != null && $Object.hasOwnProperty.call(message, "recipientContract")) {
                if (properties.recipient === 1)
                    return "recipient: multiple values";
                properties.recipient = 1;
                {
                    let error = $root.proto.ContractID.verify(message.recipientContract, _depth + 1);
                    if (error)
                        return "recipientContract." + error;
                }
            }
            if (message.targetedAddress != null && $Object.hasOwnProperty.call(message, "targetedAddress")) {
                if (properties.recipient === 1)
                    return "recipient: multiple values";
                properties.recipient = 1;
                if (!(message.targetedAddress && typeof message.targetedAddress.length === "number" || $util.isString(message.targetedAddress)))
                    return "targetedAddress: buffer expected";
            }
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (!$util.isInteger(message.value) && !(message.value && $util.isInteger(message.value.low) && $util.isInteger(message.value.high)))
                    return "value: integer|Long expected";
            if (message.gasUsed != null && $Object.hasOwnProperty.call(message, "gasUsed"))
                if (!$util.isInteger(message.gasUsed) && !(message.gasUsed && $util.isInteger(message.gasUsed.low) && $util.isInteger(message.gasUsed.high)))
                    return "gasUsed: integer|Long expected";
            if (message.output != null && $Object.hasOwnProperty.call(message, "output")) {
                properties.resultData = 1;
                if (!(message.output && typeof message.output.length === "number" || $util.isString(message.output)))
                    return "output: buffer expected";
            }
            if (message.revertReason != null && $Object.hasOwnProperty.call(message, "revertReason")) {
                if (properties.resultData === 1)
                    return "resultData: multiple values";
                properties.resultData = 1;
                if (!(message.revertReason && typeof message.revertReason.length === "number" || $util.isString(message.revertReason)))
                    return "revertReason: buffer expected";
            }
            if (message.error != null && $Object.hasOwnProperty.call(message, "error")) {
                if (properties.resultData === 1)
                    return "resultData: multiple values";
                properties.resultData = 1;
                if (!(message.error && typeof message.error.length === "number" || $util.isString(message.error)))
                    return "error: buffer expected";
            }
            if (message.callDepth != null && $Object.hasOwnProperty.call(message, "callDepth"))
                if (!$util.isInteger(message.callDepth))
                    return "callDepth: integer expected";
            if (message.callOperationType != null && $Object.hasOwnProperty.call(message, "callOperationType"))
                if (typeof message.callOperationType !== "number" || (message.callOperationType | 0) !== message.callOperationType)
                    return "callOperationType: enum value expected";
            return null;
        };

        /**
         * Creates a ContractAction message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractAction
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractAction} ContractAction
         */
        ContractAction.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractAction)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractAction: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractAction();
            if (object.callType !== 0 && (typeof object.callType !== "string" || $root.proto.ContractActionType[object.callType] !== 0))
                switch (object.callType) {
                case "NO_ACTION":
                case 0:
                    message.callType = 0;
                    break;
                case "CALL":
                case 1:
                    message.callType = 1;
                    break;
                case "CREATE":
                case 2:
                    message.callType = 2;
                    break;
                case "PRECOMPILE":
                case 3:
                    message.callType = 3;
                    break;
                case "SYSTEM":
                case 4:
                    message.callType = 4;
                    break;
                default:
                    if (typeof object.callType === "number" && (object.callType | 0) === object.callType)
                        message.callType = object.callType;
                }
            if (object.callingAccount != null) {
                if (!$util.isObject(object.callingAccount))
                    throw $TypeError(".proto.ContractAction.callingAccount: object expected");
                message.callingAccount = $root.proto.AccountID.fromObject(object.callingAccount, _depth + 1);
            }
            if (object.callingContract != null) {
                if (!$util.isObject(object.callingContract))
                    throw $TypeError(".proto.ContractAction.callingContract: object expected");
                message.callingContract = $root.proto.ContractID.fromObject(object.callingContract, _depth + 1);
            }
            if (object.gas != null)
                if (typeof object.gas === "object" ? object.gas.low || object.gas.high : $Number(object.gas) !== 0)
                    if ($util.Long)
                        message.gas = $util.Long.fromValue(object.gas, false);
                    else if (typeof object.gas === "string")
                        message.gas = $parseInt(object.gas, 10);
                    else if (typeof object.gas === "number")
                        message.gas = object.gas;
                    else if (typeof object.gas === "object")
                        message.gas = new $util.LongBits(object.gas.low >>> 0, object.gas.high >>> 0).toNumber();
            if (object.input != null)
                if (object.input.length)
                    if (typeof object.input === "string")
                        $util.base64.decode(object.input, message.input = $util.newBuffer($util.base64.length(object.input)), 0);
                    else if (object.input.length >= 0)
                        message.input = object.input;
            if (object.recipientAccount != null) {
                if (!$util.isObject(object.recipientAccount))
                    throw $TypeError(".proto.ContractAction.recipientAccount: object expected");
                message.recipientAccount = $root.proto.AccountID.fromObject(object.recipientAccount, _depth + 1);
            }
            if (object.recipientContract != null) {
                if (!$util.isObject(object.recipientContract))
                    throw $TypeError(".proto.ContractAction.recipientContract: object expected");
                message.recipientContract = $root.proto.ContractID.fromObject(object.recipientContract, _depth + 1);
            }
            if (object.targetedAddress != null)
                if (typeof object.targetedAddress === "string")
                    $util.base64.decode(object.targetedAddress, message.targetedAddress = $util.newBuffer($util.base64.length(object.targetedAddress)), 0);
                else if (object.targetedAddress.length >= 0)
                    message.targetedAddress = object.targetedAddress;
            if (object.value != null)
                if (typeof object.value === "object" ? object.value.low || object.value.high : $Number(object.value) !== 0)
                    if ($util.Long)
                        message.value = $util.Long.fromValue(object.value, false);
                    else if (typeof object.value === "string")
                        message.value = $parseInt(object.value, 10);
                    else if (typeof object.value === "number")
                        message.value = object.value;
                    else if (typeof object.value === "object")
                        message.value = new $util.LongBits(object.value.low >>> 0, object.value.high >>> 0).toNumber();
            if (object.gasUsed != null)
                if (typeof object.gasUsed === "object" ? object.gasUsed.low || object.gasUsed.high : $Number(object.gasUsed) !== 0)
                    if ($util.Long)
                        message.gasUsed = $util.Long.fromValue(object.gasUsed, false);
                    else if (typeof object.gasUsed === "string")
                        message.gasUsed = $parseInt(object.gasUsed, 10);
                    else if (typeof object.gasUsed === "number")
                        message.gasUsed = object.gasUsed;
                    else if (typeof object.gasUsed === "object")
                        message.gasUsed = new $util.LongBits(object.gasUsed.low >>> 0, object.gasUsed.high >>> 0).toNumber();
            if (object.output != null)
                if (typeof object.output === "string")
                    $util.base64.decode(object.output, message.output = $util.newBuffer($util.base64.length(object.output)), 0);
                else if (object.output.length >= 0)
                    message.output = object.output;
            if (object.revertReason != null)
                if (typeof object.revertReason === "string")
                    $util.base64.decode(object.revertReason, message.revertReason = $util.newBuffer($util.base64.length(object.revertReason)), 0);
                else if (object.revertReason.length >= 0)
                    message.revertReason = object.revertReason;
            if (object.error != null)
                if (typeof object.error === "string")
                    $util.base64.decode(object.error, message.error = $util.newBuffer($util.base64.length(object.error)), 0);
                else if (object.error.length >= 0)
                    message.error = object.error;
            if (object.callDepth != null)
                if ($Number(object.callDepth) !== 0)
                    message.callDepth = object.callDepth | 0;
            if (object.callOperationType !== 0 && (typeof object.callOperationType !== "string" || $root.proto.CallOperationType[object.callOperationType] !== 0))
                switch (object.callOperationType) {
                case "OP_UNKNOWN":
                case 0:
                    message.callOperationType = 0;
                    break;
                case "OP_CALL":
                case 1:
                    message.callOperationType = 1;
                    break;
                case "OP_CALLCODE":
                case 2:
                    message.callOperationType = 2;
                    break;
                case "OP_DELEGATECALL":
                case 3:
                    message.callOperationType = 3;
                    break;
                case "OP_STATICCALL":
                case 4:
                    message.callOperationType = 4;
                    break;
                case "OP_CREATE":
                case 5:
                    message.callOperationType = 5;
                    break;
                case "OP_CREATE2":
                case 6:
                    message.callOperationType = 6;
                    break;
                default:
                    if (typeof object.callOperationType === "number" && (object.callOperationType | 0) === object.callOperationType)
                        message.callOperationType = object.callOperationType;
                }
            return message;
        };

        /**
         * Creates a plain object from a ContractAction message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractAction
         * @static
         * @param {proto.ContractAction} message ContractAction
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractAction.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.callType = options.enums === $String ? "NO_ACTION" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.gas = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gas = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (options.bytes === $String)
                    object.input = "";
                else {
                    object.input = [];
                    if (options.bytes !== $Array)
                        object.input = $util.newBuffer(object.input);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.value = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.value = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.gasUsed = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.gasUsed = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.callDepth = 0;
                object.callOperationType = options.enums === $String ? "OP_UNKNOWN" : 0;
            }
            if (message.callType != null && $Object.hasOwnProperty.call(message, "callType"))
                object.callType = options.enums === $String ? $root.proto.ContractActionType[message.callType] === $undefined ? message.callType : $root.proto.ContractActionType[message.callType] : message.callType;
            if (message.callingAccount != null && $Object.hasOwnProperty.call(message, "callingAccount")) {
                object.callingAccount = $root.proto.AccountID.toObject(message.callingAccount, options, _depth + 1);
                if (options.oneofs)
                    object.caller = "callingAccount";
            }
            if (message.callingContract != null && $Object.hasOwnProperty.call(message, "callingContract")) {
                object.callingContract = $root.proto.ContractID.toObject(message.callingContract, options, _depth + 1);
                if (options.oneofs)
                    object.caller = "callingContract";
            }
            if (message.gas != null && $Object.hasOwnProperty.call(message, "gas"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gas = typeof message.gas === "number" ? $BigInt(message.gas) : $util.Long.fromBits(message.gas.low >>> 0, message.gas.high >>> 0, false).toBigInt();
                else if (typeof message.gas === "number")
                    object.gas = options.longs === $String ? $String(message.gas) : message.gas;
                else
                    object.gas = options.longs === $String ? $util.Long.prototype.toString.call(message.gas) : options.longs === $Number ? new $util.LongBits(message.gas.low >>> 0, message.gas.high >>> 0).toNumber() : message.gas;
            if (message.input != null && $Object.hasOwnProperty.call(message, "input"))
                object.input = options.bytes === $String ? $util.base64.encode(message.input, 0, message.input.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.input) : message.input;
            if (message.recipientAccount != null && $Object.hasOwnProperty.call(message, "recipientAccount")) {
                object.recipientAccount = $root.proto.AccountID.toObject(message.recipientAccount, options, _depth + 1);
                if (options.oneofs)
                    object.recipient = "recipientAccount";
            }
            if (message.recipientContract != null && $Object.hasOwnProperty.call(message, "recipientContract")) {
                object.recipientContract = $root.proto.ContractID.toObject(message.recipientContract, options, _depth + 1);
                if (options.oneofs)
                    object.recipient = "recipientContract";
            }
            if (message.targetedAddress != null && $Object.hasOwnProperty.call(message, "targetedAddress")) {
                object.targetedAddress = options.bytes === $String ? $util.base64.encode(message.targetedAddress, 0, message.targetedAddress.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.targetedAddress) : message.targetedAddress;
                if (options.oneofs)
                    object.recipient = "targetedAddress";
            }
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.value = typeof message.value === "number" ? $BigInt(message.value) : $util.Long.fromBits(message.value.low >>> 0, message.value.high >>> 0, false).toBigInt();
                else if (typeof message.value === "number")
                    object.value = options.longs === $String ? $String(message.value) : message.value;
                else
                    object.value = options.longs === $String ? $util.Long.prototype.toString.call(message.value) : options.longs === $Number ? new $util.LongBits(message.value.low >>> 0, message.value.high >>> 0).toNumber() : message.value;
            if (message.gasUsed != null && $Object.hasOwnProperty.call(message, "gasUsed"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.gasUsed = typeof message.gasUsed === "number" ? $BigInt(message.gasUsed) : $util.Long.fromBits(message.gasUsed.low >>> 0, message.gasUsed.high >>> 0, false).toBigInt();
                else if (typeof message.gasUsed === "number")
                    object.gasUsed = options.longs === $String ? $String(message.gasUsed) : message.gasUsed;
                else
                    object.gasUsed = options.longs === $String ? $util.Long.prototype.toString.call(message.gasUsed) : options.longs === $Number ? new $util.LongBits(message.gasUsed.low >>> 0, message.gasUsed.high >>> 0).toNumber() : message.gasUsed;
            if (message.output != null && $Object.hasOwnProperty.call(message, "output")) {
                object.output = options.bytes === $String ? $util.base64.encode(message.output, 0, message.output.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.output) : message.output;
                if (options.oneofs)
                    object.resultData = "output";
            }
            if (message.revertReason != null && $Object.hasOwnProperty.call(message, "revertReason")) {
                object.revertReason = options.bytes === $String ? $util.base64.encode(message.revertReason, 0, message.revertReason.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.revertReason) : message.revertReason;
                if (options.oneofs)
                    object.resultData = "revertReason";
            }
            if (message.error != null && $Object.hasOwnProperty.call(message, "error")) {
                object.error = options.bytes === $String ? $util.base64.encode(message.error, 0, message.error.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.error) : message.error;
                if (options.oneofs)
                    object.resultData = "error";
            }
            if (message.callDepth != null && $Object.hasOwnProperty.call(message, "callDepth"))
                object.callDepth = message.callDepth;
            if (message.callOperationType != null && $Object.hasOwnProperty.call(message, "callOperationType"))
                object.callOperationType = options.enums === $String ? $root.proto.CallOperationType[message.callOperationType] === $undefined ? message.callOperationType : $root.proto.CallOperationType[message.callOperationType] : message.callOperationType;
            return object;
        };

        /**
         * Converts this ContractAction to JSON.
         * @function toJSON
         * @memberof proto.ContractAction
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractAction.prototype.toJSON = function() {
            return ContractAction.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractAction
         * @function getTypeUrl
         * @memberof proto.ContractAction
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractAction.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractAction";
        };

        return ContractAction;
    })();

    proto.ContractBytecode = (function() {

        /**
         * Properties of a ContractBytecode.
         * @typedef {Object} proto.ContractBytecode.$Properties
         * @property {proto.ContractID.$Properties|null} [contractId] The contract to which the bytecodes apply to
         * @property {Uint8Array|null} [initcode] Contract bytecode during deployment
         * @property {Uint8Array|null} [runtimeBytecode] Contract bytecode after deployment
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractBytecode.
         * @memberof proto
         * @interface IContractBytecode
         * @augments proto.ContractBytecode.$Properties
         * @deprecated Use proto.ContractBytecode.$Properties instead.
         */

        /**
         * Shape of a ContractBytecode.
         * @typedef {{
         *   contractId?: proto.ContractID.$Shape|null;
         *   initcode?: Uint8Array|null;
         *   runtimeBytecode?: Uint8Array|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractBytecode.$Shape
         */

        /**
         * Constructs a new ContractBytecode.
         * @memberof proto
         * @classdesc Represents a ContractBytecode.
         * @constructor
         * @param {proto.ContractBytecode.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractBytecode = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The contract to which the bytecodes apply to
         * @member {proto.ContractID.$Properties|null|undefined} contractId
         * @memberof proto.ContractBytecode
         * @instance
         */
        ContractBytecode.prototype.contractId = null;

        /**
         * Contract bytecode during deployment
         * @member {Uint8Array} initcode
         * @memberof proto.ContractBytecode
         * @instance
         */
        ContractBytecode.prototype.initcode = $util.newBuffer([]);

        /**
         * Contract bytecode after deployment
         * @member {Uint8Array} runtimeBytecode
         * @memberof proto.ContractBytecode
         * @instance
         */
        ContractBytecode.prototype.runtimeBytecode = $util.newBuffer([]);

        /**
         * Creates a new ContractBytecode instance using the specified properties.
         * @function create
         * @memberof proto.ContractBytecode
         * @static
         * @param {proto.ContractBytecode.$Properties=} [properties] Properties to set
         * @returns {proto.ContractBytecode} ContractBytecode instance
         * @type {{
         *   (properties: proto.ContractBytecode.$Shape): proto.ContractBytecode & proto.ContractBytecode.$Shape;
         *   (properties?: proto.ContractBytecode.$Properties): proto.ContractBytecode;
         * }}
         */
        ContractBytecode.create = function(properties) {
            return new ContractBytecode(properties);
        };

        /**
         * Encodes the specified ContractBytecode message. Does not implicitly {@link proto.ContractBytecode.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractBytecode
         * @static
         * @param {proto.ContractBytecode.$Properties} message ContractBytecode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractBytecode.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId"))
                $root.proto.ContractID.encode(message.contractId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.initcode != null && $Object.hasOwnProperty.call(message, "initcode") && message.initcode.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.initcode);
            if (message.runtimeBytecode != null && $Object.hasOwnProperty.call(message, "runtimeBytecode") && message.runtimeBytecode.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.runtimeBytecode);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractBytecode message, length delimited. Does not implicitly {@link proto.ContractBytecode.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractBytecode
         * @static
         * @param {proto.ContractBytecode.$Properties} message ContractBytecode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractBytecode.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractBytecode message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractBytecode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractBytecode & proto.ContractBytecode.$Shape} ContractBytecode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractBytecode.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractBytecode();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.contractId = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.initcode = value;
                        else
                            delete message.initcode;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.runtimeBytecode = value;
                        else
                            delete message.runtimeBytecode;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractBytecode message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractBytecode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractBytecode & proto.ContractBytecode.$Shape} ContractBytecode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractBytecode.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractBytecode message.
         * @function verify
         * @memberof proto.ContractBytecode
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractBytecode.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId")) {
                let error = $root.proto.ContractID.verify(message.contractId, _depth + 1);
                if (error)
                    return "contractId." + error;
            }
            if (message.initcode != null && $Object.hasOwnProperty.call(message, "initcode"))
                if (!(message.initcode && typeof message.initcode.length === "number" || $util.isString(message.initcode)))
                    return "initcode: buffer expected";
            if (message.runtimeBytecode != null && $Object.hasOwnProperty.call(message, "runtimeBytecode"))
                if (!(message.runtimeBytecode && typeof message.runtimeBytecode.length === "number" || $util.isString(message.runtimeBytecode)))
                    return "runtimeBytecode: buffer expected";
            return null;
        };

        /**
         * Creates a ContractBytecode message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractBytecode
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractBytecode} ContractBytecode
         */
        ContractBytecode.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractBytecode)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractBytecode: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractBytecode();
            if (object.contractId != null) {
                if (!$util.isObject(object.contractId))
                    throw $TypeError(".proto.ContractBytecode.contractId: object expected");
                message.contractId = $root.proto.ContractID.fromObject(object.contractId, _depth + 1);
            }
            if (object.initcode != null)
                if (object.initcode.length)
                    if (typeof object.initcode === "string")
                        $util.base64.decode(object.initcode, message.initcode = $util.newBuffer($util.base64.length(object.initcode)), 0);
                    else if (object.initcode.length >= 0)
                        message.initcode = object.initcode;
            if (object.runtimeBytecode != null)
                if (object.runtimeBytecode.length)
                    if (typeof object.runtimeBytecode === "string")
                        $util.base64.decode(object.runtimeBytecode, message.runtimeBytecode = $util.newBuffer($util.base64.length(object.runtimeBytecode)), 0);
                    else if (object.runtimeBytecode.length >= 0)
                        message.runtimeBytecode = object.runtimeBytecode;
            return message;
        };

        /**
         * Creates a plain object from a ContractBytecode message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractBytecode
         * @static
         * @param {proto.ContractBytecode} message ContractBytecode
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractBytecode.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.contractId = null;
                if (options.bytes === $String)
                    object.initcode = "";
                else {
                    object.initcode = [];
                    if (options.bytes !== $Array)
                        object.initcode = $util.newBuffer(object.initcode);
                }
                if (options.bytes === $String)
                    object.runtimeBytecode = "";
                else {
                    object.runtimeBytecode = [];
                    if (options.bytes !== $Array)
                        object.runtimeBytecode = $util.newBuffer(object.runtimeBytecode);
                }
            }
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId"))
                object.contractId = $root.proto.ContractID.toObject(message.contractId, options, _depth + 1);
            if (message.initcode != null && $Object.hasOwnProperty.call(message, "initcode"))
                object.initcode = options.bytes === $String ? $util.base64.encode(message.initcode, 0, message.initcode.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.initcode) : message.initcode;
            if (message.runtimeBytecode != null && $Object.hasOwnProperty.call(message, "runtimeBytecode"))
                object.runtimeBytecode = options.bytes === $String ? $util.base64.encode(message.runtimeBytecode, 0, message.runtimeBytecode.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.runtimeBytecode) : message.runtimeBytecode;
            return object;
        };

        /**
         * Converts this ContractBytecode to JSON.
         * @function toJSON
         * @memberof proto.ContractBytecode
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractBytecode.prototype.toJSON = function() {
            return ContractBytecode.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractBytecode
         * @function getTypeUrl
         * @memberof proto.ContractBytecode
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractBytecode.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractBytecode";
        };

        return ContractBytecode;
    })();

    proto.ContractStateChanges = (function() {

        /**
         * Properties of a ContractStateChanges.
         * @typedef {Object} proto.ContractStateChanges.$Properties
         * @property {Array.<proto.ContractStateChange.$Properties>|null} [contractStateChanges] ContractStateChanges contractStateChanges
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractStateChanges.
         * @memberof proto
         * @interface IContractStateChanges
         * @augments proto.ContractStateChanges.$Properties
         * @deprecated Use proto.ContractStateChanges.$Properties instead.
         */

        /**
         * Shape of a ContractStateChanges.
         * @typedef {{
         *   contractStateChanges?: Array.<proto.ContractStateChange.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractStateChanges.$Shape
         */

        /**
         * Constructs a new ContractStateChanges.
         * @memberof proto
         * @classdesc Represents a ContractStateChanges.
         * @constructor
         * @param {proto.ContractStateChanges.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractStateChanges = function (properties) {
            this.contractStateChanges = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * ContractStateChanges contractStateChanges.
         * @member {Array.<proto.ContractStateChange.$Properties>} contractStateChanges
         * @memberof proto.ContractStateChanges
         * @instance
         */
        ContractStateChanges.prototype.contractStateChanges = $util.emptyArray;

        /**
         * Creates a new ContractStateChanges instance using the specified properties.
         * @function create
         * @memberof proto.ContractStateChanges
         * @static
         * @param {proto.ContractStateChanges.$Properties=} [properties] Properties to set
         * @returns {proto.ContractStateChanges} ContractStateChanges instance
         * @type {{
         *   (properties: proto.ContractStateChanges.$Shape): proto.ContractStateChanges & proto.ContractStateChanges.$Shape;
         *   (properties?: proto.ContractStateChanges.$Properties): proto.ContractStateChanges;
         * }}
         */
        ContractStateChanges.create = function(properties) {
            return new ContractStateChanges(properties);
        };

        /**
         * Encodes the specified ContractStateChanges message. Does not implicitly {@link proto.ContractStateChanges.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractStateChanges
         * @static
         * @param {proto.ContractStateChanges.$Properties} message ContractStateChanges message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractStateChanges.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contractStateChanges != null && message.contractStateChanges.length)
                for (let i = 0; i < message.contractStateChanges.length; ++i)
                    $root.proto.ContractStateChange.encode(message.contractStateChanges[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractStateChanges message, length delimited. Does not implicitly {@link proto.ContractStateChanges.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractStateChanges
         * @static
         * @param {proto.ContractStateChanges.$Properties} message ContractStateChanges message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractStateChanges.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractStateChanges message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractStateChanges
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractStateChanges & proto.ContractStateChanges.$Shape} ContractStateChanges
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractStateChanges.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractStateChanges();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.contractStateChanges && message.contractStateChanges.length))
                            message.contractStateChanges = [];
                        message.contractStateChanges.push($root.proto.ContractStateChange.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractStateChanges message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractStateChanges
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractStateChanges & proto.ContractStateChanges.$Shape} ContractStateChanges
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractStateChanges.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractStateChanges message.
         * @function verify
         * @memberof proto.ContractStateChanges
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractStateChanges.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.contractStateChanges != null && $Object.hasOwnProperty.call(message, "contractStateChanges")) {
                if (!$Array.isArray(message.contractStateChanges))
                    return "contractStateChanges: array expected";
                for (let i = 0; i < message.contractStateChanges.length; ++i) {
                    let error = $root.proto.ContractStateChange.verify(message.contractStateChanges[i], _depth + 1);
                    if (error)
                        return "contractStateChanges." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ContractStateChanges message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractStateChanges
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractStateChanges} ContractStateChanges
         */
        ContractStateChanges.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractStateChanges)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractStateChanges: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractStateChanges();
            if (object.contractStateChanges) {
                if (!$Array.isArray(object.contractStateChanges))
                    throw $TypeError(".proto.ContractStateChanges.contractStateChanges: array expected");
                message.contractStateChanges = $Array(object.contractStateChanges.length);
                for (let i = 0; i < object.contractStateChanges.length; ++i) {
                    if (!$util.isObject(object.contractStateChanges[i]))
                        throw $TypeError(".proto.ContractStateChanges.contractStateChanges: object expected");
                    message.contractStateChanges[i] = $root.proto.ContractStateChange.fromObject(object.contractStateChanges[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractStateChanges message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractStateChanges
         * @static
         * @param {proto.ContractStateChanges} message ContractStateChanges
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractStateChanges.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.contractStateChanges = [];
            if (message.contractStateChanges && message.contractStateChanges.length) {
                object.contractStateChanges = $Array(message.contractStateChanges.length);
                for (let j = 0; j < message.contractStateChanges.length; ++j)
                    object.contractStateChanges[j] = $root.proto.ContractStateChange.toObject(message.contractStateChanges[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ContractStateChanges to JSON.
         * @function toJSON
         * @memberof proto.ContractStateChanges
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractStateChanges.prototype.toJSON = function() {
            return ContractStateChanges.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractStateChanges
         * @function getTypeUrl
         * @memberof proto.ContractStateChanges
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractStateChanges.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractStateChanges";
        };

        return ContractStateChanges;
    })();

    proto.ContractStateChange = (function() {

        /**
         * Properties of a ContractStateChange.
         * @typedef {Object} proto.ContractStateChange.$Properties
         * @property {proto.ContractID.$Properties|null} [contractId] The contract to which the storage changes apply to
         * @property {Array.<proto.StorageChange.$Properties>|null} [storageChanges] The list of storage changes.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a ContractStateChange.
         * @memberof proto
         * @interface IContractStateChange
         * @augments proto.ContractStateChange.$Properties
         * @deprecated Use proto.ContractStateChange.$Properties instead.
         */

        /**
         * Shape of a ContractStateChange.
         * @typedef {{
         *   contractId?: proto.ContractID.$Shape|null;
         *   storageChanges?: Array.<proto.StorageChange.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.ContractStateChange.$Shape
         */

        /**
         * Constructs a new ContractStateChange.
         * @memberof proto
         * @classdesc The storage changes to a smart contract's storage as a side effect of the function call.
         * @constructor
         * @param {proto.ContractStateChange.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const ContractStateChange = function (properties) {
            this.storageChanges = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The contract to which the storage changes apply to
         * @member {proto.ContractID.$Properties|null|undefined} contractId
         * @memberof proto.ContractStateChange
         * @instance
         */
        ContractStateChange.prototype.contractId = null;

        /**
         * The list of storage changes.
         * @member {Array.<proto.StorageChange.$Properties>} storageChanges
         * @memberof proto.ContractStateChange
         * @instance
         */
        ContractStateChange.prototype.storageChanges = $util.emptyArray;

        /**
         * Creates a new ContractStateChange instance using the specified properties.
         * @function create
         * @memberof proto.ContractStateChange
         * @static
         * @param {proto.ContractStateChange.$Properties=} [properties] Properties to set
         * @returns {proto.ContractStateChange} ContractStateChange instance
         * @type {{
         *   (properties: proto.ContractStateChange.$Shape): proto.ContractStateChange & proto.ContractStateChange.$Shape;
         *   (properties?: proto.ContractStateChange.$Properties): proto.ContractStateChange;
         * }}
         */
        ContractStateChange.create = function(properties) {
            return new ContractStateChange(properties);
        };

        /**
         * Encodes the specified ContractStateChange message. Does not implicitly {@link proto.ContractStateChange.verify|verify} messages.
         * @function encode
         * @memberof proto.ContractStateChange
         * @static
         * @param {proto.ContractStateChange.$Properties} message ContractStateChange message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractStateChange.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId"))
                $root.proto.ContractID.encode(message.contractId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.storageChanges != null && message.storageChanges.length)
                for (let i = 0; i < message.storageChanges.length; ++i)
                    $root.proto.StorageChange.encode(message.storageChanges[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContractStateChange message, length delimited. Does not implicitly {@link proto.ContractStateChange.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContractStateChange
         * @static
         * @param {proto.ContractStateChange.$Properties} message ContractStateChange message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContractStateChange.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a ContractStateChange message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContractStateChange
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContractStateChange & proto.ContractStateChange.$Shape} ContractStateChange
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractStateChange.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.ContractStateChange();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.contractId = $root.proto.ContractID.decode(reader, reader.uint32(), $undefined, _depth + 1, message.contractId);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.storageChanges && message.storageChanges.length))
                            message.storageChanges = [];
                        message.storageChanges.push($root.proto.StorageChange.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContractStateChange message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContractStateChange
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContractStateChange & proto.ContractStateChange.$Shape} ContractStateChange
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContractStateChange.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContractStateChange message.
         * @function verify
         * @memberof proto.ContractStateChange
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContractStateChange.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId")) {
                let error = $root.proto.ContractID.verify(message.contractId, _depth + 1);
                if (error)
                    return "contractId." + error;
            }
            if (message.storageChanges != null && $Object.hasOwnProperty.call(message, "storageChanges")) {
                if (!$Array.isArray(message.storageChanges))
                    return "storageChanges: array expected";
                for (let i = 0; i < message.storageChanges.length; ++i) {
                    let error = $root.proto.StorageChange.verify(message.storageChanges[i], _depth + 1);
                    if (error)
                        return "storageChanges." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ContractStateChange message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContractStateChange
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContractStateChange} ContractStateChange
         */
        ContractStateChange.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.ContractStateChange)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.ContractStateChange: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.ContractStateChange();
            if (object.contractId != null) {
                if (!$util.isObject(object.contractId))
                    throw $TypeError(".proto.ContractStateChange.contractId: object expected");
                message.contractId = $root.proto.ContractID.fromObject(object.contractId, _depth + 1);
            }
            if (object.storageChanges) {
                if (!$Array.isArray(object.storageChanges))
                    throw $TypeError(".proto.ContractStateChange.storageChanges: array expected");
                message.storageChanges = $Array(object.storageChanges.length);
                for (let i = 0; i < object.storageChanges.length; ++i) {
                    if (!$util.isObject(object.storageChanges[i]))
                        throw $TypeError(".proto.ContractStateChange.storageChanges: object expected");
                    message.storageChanges[i] = $root.proto.StorageChange.fromObject(object.storageChanges[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ContractStateChange message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContractStateChange
         * @static
         * @param {proto.ContractStateChange} message ContractStateChange
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContractStateChange.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.storageChanges = [];
            if (options.defaults)
                object.contractId = null;
            if (message.contractId != null && $Object.hasOwnProperty.call(message, "contractId"))
                object.contractId = $root.proto.ContractID.toObject(message.contractId, options, _depth + 1);
            if (message.storageChanges && message.storageChanges.length) {
                object.storageChanges = $Array(message.storageChanges.length);
                for (let j = 0; j < message.storageChanges.length; ++j)
                    object.storageChanges[j] = $root.proto.StorageChange.toObject(message.storageChanges[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this ContractStateChange to JSON.
         * @function toJSON
         * @memberof proto.ContractStateChange
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContractStateChange.prototype.toJSON = function() {
            return ContractStateChange.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for ContractStateChange
         * @function getTypeUrl
         * @memberof proto.ContractStateChange
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        ContractStateChange.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContractStateChange";
        };

        return ContractStateChange;
    })();

    proto.StorageChange = (function() {

        /**
         * Properties of a StorageChange.
         * @typedef {Object} proto.StorageChange.$Properties
         * @property {Uint8Array|null} [slot] The storage slot changed.  Up to 32 bytes, big-endian, zero bytes left trimmed.
         * @property {Uint8Array|null} [valueRead] The value read from the storage slot.  Up to 32 bytes, big-endian, zero bytes left trimmed.
         * 
         * Because of the way SSTORE operations are charged the slot is always read before being written to.
         * @property {google.protobuf.BytesValue.$Properties|null} [valueWritten] The new value written to the slot.  Up to 32 bytes, big-endian, zero bytes left trimmed.
         * 
         * If a value of zero is written the valueWritten will be present but the inner value will be absent.
         * 
         * If a value was read and not written this value will not be present.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a StorageChange.
         * @memberof proto
         * @interface IStorageChange
         * @augments proto.StorageChange.$Properties
         * @deprecated Use proto.StorageChange.$Properties instead.
         */

        /**
         * Shape of a StorageChange.
         * @typedef {proto.StorageChange.$Properties} proto.StorageChange.$Shape
         */

        /**
         * Constructs a new StorageChange.
         * @memberof proto
         * @classdesc A storage slot change description.
         * @constructor
         * @param {proto.StorageChange.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const StorageChange = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The storage slot changed.  Up to 32 bytes, big-endian, zero bytes left trimmed.
         * @member {Uint8Array} slot
         * @memberof proto.StorageChange
         * @instance
         */
        StorageChange.prototype.slot = $util.newBuffer([]);

        /**
         * The value read from the storage slot.  Up to 32 bytes, big-endian, zero bytes left trimmed.
         * 
         * Because of the way SSTORE operations are charged the slot is always read before being written to.
         * @member {Uint8Array} valueRead
         * @memberof proto.StorageChange
         * @instance
         */
        StorageChange.prototype.valueRead = $util.newBuffer([]);

        /**
         * The new value written to the slot.  Up to 32 bytes, big-endian, zero bytes left trimmed.
         * 
         * If a value of zero is written the valueWritten will be present but the inner value will be absent.
         * 
         * If a value was read and not written this value will not be present.
         * @member {google.protobuf.BytesValue.$Properties|null|undefined} valueWritten
         * @memberof proto.StorageChange
         * @instance
         */
        StorageChange.prototype.valueWritten = null;

        /**
         * Creates a new StorageChange instance using the specified properties.
         * @function create
         * @memberof proto.StorageChange
         * @static
         * @param {proto.StorageChange.$Properties=} [properties] Properties to set
         * @returns {proto.StorageChange} StorageChange instance
         * @type {{
         *   (properties: proto.StorageChange.$Shape): proto.StorageChange & proto.StorageChange.$Shape;
         *   (properties?: proto.StorageChange.$Properties): proto.StorageChange;
         * }}
         */
        StorageChange.create = function(properties) {
            return new StorageChange(properties);
        };

        /**
         * Encodes the specified StorageChange message. Does not implicitly {@link proto.StorageChange.verify|verify} messages.
         * @function encode
         * @memberof proto.StorageChange
         * @static
         * @param {proto.StorageChange.$Properties} message StorageChange message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StorageChange.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.slot != null && $Object.hasOwnProperty.call(message, "slot") && message.slot.length)
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.slot);
            if (message.valueRead != null && $Object.hasOwnProperty.call(message, "valueRead") && message.valueRead.length)
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.valueRead);
            if (message.valueWritten != null && $Object.hasOwnProperty.call(message, "valueWritten"))
                $root.google.protobuf.BytesValue.encode(message.valueWritten, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StorageChange message, length delimited. Does not implicitly {@link proto.StorageChange.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.StorageChange
         * @static
         * @param {proto.StorageChange.$Properties} message StorageChange message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StorageChange.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a StorageChange message from the specified reader or buffer.
         * @function decode
         * @memberof proto.StorageChange
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.StorageChange & proto.StorageChange.$Shape} StorageChange
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StorageChange.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.StorageChange();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.slot = value;
                        else
                            delete message.slot;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.valueRead = value;
                        else
                            delete message.valueRead;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.valueWritten = $root.google.protobuf.BytesValue.decode(reader, reader.uint32(), $undefined, _depth + 1, message.valueWritten);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a StorageChange message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.StorageChange
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.StorageChange & proto.StorageChange.$Shape} StorageChange
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StorageChange.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StorageChange message.
         * @function verify
         * @memberof proto.StorageChange
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StorageChange.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.slot != null && $Object.hasOwnProperty.call(message, "slot"))
                if (!(message.slot && typeof message.slot.length === "number" || $util.isString(message.slot)))
                    return "slot: buffer expected";
            if (message.valueRead != null && $Object.hasOwnProperty.call(message, "valueRead"))
                if (!(message.valueRead && typeof message.valueRead.length === "number" || $util.isString(message.valueRead)))
                    return "valueRead: buffer expected";
            if (message.valueWritten != null && $Object.hasOwnProperty.call(message, "valueWritten")) {
                let error = $root.google.protobuf.BytesValue.verify(message.valueWritten, _depth + 1);
                if (error)
                    return "valueWritten." + error;
            }
            return null;
        };

        /**
         * Creates a StorageChange message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.StorageChange
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.StorageChange} StorageChange
         */
        StorageChange.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.StorageChange)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.StorageChange: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.StorageChange();
            if (object.slot != null)
                if (object.slot.length)
                    if (typeof object.slot === "string")
                        $util.base64.decode(object.slot, message.slot = $util.newBuffer($util.base64.length(object.slot)), 0);
                    else if (object.slot.length >= 0)
                        message.slot = object.slot;
            if (object.valueRead != null)
                if (object.valueRead.length)
                    if (typeof object.valueRead === "string")
                        $util.base64.decode(object.valueRead, message.valueRead = $util.newBuffer($util.base64.length(object.valueRead)), 0);
                    else if (object.valueRead.length >= 0)
                        message.valueRead = object.valueRead;
            if (object.valueWritten != null) {
                if (!$util.isObject(object.valueWritten))
                    throw $TypeError(".proto.StorageChange.valueWritten: object expected");
                message.valueWritten = $root.google.protobuf.BytesValue.fromObject(object.valueWritten, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a StorageChange message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.StorageChange
         * @static
         * @param {proto.StorageChange} message StorageChange
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StorageChange.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.slot = "";
                else {
                    object.slot = [];
                    if (options.bytes !== $Array)
                        object.slot = $util.newBuffer(object.slot);
                }
                if (options.bytes === $String)
                    object.valueRead = "";
                else {
                    object.valueRead = [];
                    if (options.bytes !== $Array)
                        object.valueRead = $util.newBuffer(object.valueRead);
                }
                object.valueWritten = null;
            }
            if (message.slot != null && $Object.hasOwnProperty.call(message, "slot"))
                object.slot = options.bytes === $String ? $util.base64.encode(message.slot, 0, message.slot.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.slot) : message.slot;
            if (message.valueRead != null && $Object.hasOwnProperty.call(message, "valueRead"))
                object.valueRead = options.bytes === $String ? $util.base64.encode(message.valueRead, 0, message.valueRead.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.valueRead) : message.valueRead;
            if (message.valueWritten != null && $Object.hasOwnProperty.call(message, "valueWritten"))
                object.valueWritten = $root.google.protobuf.BytesValue.toObject(message.valueWritten, options, _depth + 1);
            return object;
        };

        /**
         * Converts this StorageChange to JSON.
         * @function toJSON
         * @memberof proto.StorageChange
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StorageChange.prototype.toJSON = function() {
            return StorageChange.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for StorageChange
         * @function getTypeUrl
         * @memberof proto.StorageChange
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        StorageChange.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.StorageChange";
        };

        return StorageChange;
    })();

    /**
     * List of hash algorithms
     * @name proto.HashAlgorithm
     * @enum {number}
     * @property {number} HASH_ALGORITHM_UNKNOWN=0 HASH_ALGORITHM_UNKNOWN value
     * @property {number} SHA_384=1 SHA_384 value
     */
    proto.HashAlgorithm = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "HASH_ALGORITHM_UNKNOWN"] = 0;
        values[valuesById[1] = "SHA_384"] = 1;
        return values;
    })();

    proto.HashObject = (function() {

        /**
         * Properties of a HashObject.
         * @typedef {Object} proto.HashObject.$Properties
         * @property {proto.HashAlgorithm|null} [algorithm] Specifies the hashing algorithm
         * @property {number|null} [length] Hash length
         * @property {Uint8Array|null} [hash] Specifies the result of the hashing operation in bytes
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a HashObject.
         * @memberof proto
         * @interface IHashObject
         * @augments proto.HashObject.$Properties
         * @deprecated Use proto.HashObject.$Properties instead.
         */

        /**
         * Shape of a HashObject.
         * @typedef {proto.HashObject.$Properties} proto.HashObject.$Shape
         */

        /**
         * Constructs a new HashObject.
         * @memberof proto
         * @classdesc Encapsulates an object hash so that additional hash algorithms
         * can be added in the future without requiring a breaking change.
         * @constructor
         * @param {proto.HashObject.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const HashObject = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Specifies the hashing algorithm
         * @member {proto.HashAlgorithm} algorithm
         * @memberof proto.HashObject
         * @instance
         */
        HashObject.prototype.algorithm = 0;

        /**
         * Hash length
         * @member {number} length
         * @memberof proto.HashObject
         * @instance
         */
        HashObject.prototype.length = 0;

        /**
         * Specifies the result of the hashing operation in bytes
         * @member {Uint8Array} hash
         * @memberof proto.HashObject
         * @instance
         */
        HashObject.prototype.hash = $util.newBuffer([]);

        /**
         * Creates a new HashObject instance using the specified properties.
         * @function create
         * @memberof proto.HashObject
         * @static
         * @param {proto.HashObject.$Properties=} [properties] Properties to set
         * @returns {proto.HashObject} HashObject instance
         * @type {{
         *   (properties: proto.HashObject.$Shape): proto.HashObject & proto.HashObject.$Shape;
         *   (properties?: proto.HashObject.$Properties): proto.HashObject;
         * }}
         */
        HashObject.create = function(properties) {
            return new HashObject(properties);
        };

        /**
         * Encodes the specified HashObject message. Does not implicitly {@link proto.HashObject.verify|verify} messages.
         * @function encode
         * @memberof proto.HashObject
         * @static
         * @param {proto.HashObject.$Properties} message HashObject message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HashObject.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.algorithm != null && $Object.hasOwnProperty.call(message, "algorithm") && message.algorithm !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.algorithm);
            if (message.length != null && $Object.hasOwnProperty.call(message, "length") && message.length !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.length);
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash") && message.hash.length)
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.hash);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HashObject message, length delimited. Does not implicitly {@link proto.HashObject.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.HashObject
         * @static
         * @param {proto.HashObject.$Properties} message HashObject message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HashObject.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a HashObject message from the specified reader or buffer.
         * @function decode
         * @memberof proto.HashObject
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.HashObject & proto.HashObject.$Shape} HashObject
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HashObject.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.HashObject();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.algorithm = value;
                        else
                            delete message.algorithm;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.length = value;
                        else
                            delete message.length;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.hash = value;
                        else
                            delete message.hash;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a HashObject message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.HashObject
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.HashObject & proto.HashObject.$Shape} HashObject
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HashObject.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HashObject message.
         * @function verify
         * @memberof proto.HashObject
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HashObject.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.algorithm != null && $Object.hasOwnProperty.call(message, "algorithm"))
                if (typeof message.algorithm !== "number" || (message.algorithm | 0) !== message.algorithm)
                    return "algorithm: enum value expected";
            if (message.length != null && $Object.hasOwnProperty.call(message, "length"))
                if (!$util.isInteger(message.length))
                    return "length: integer expected";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                    return "hash: buffer expected";
            return null;
        };

        /**
         * Creates a HashObject message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.HashObject
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.HashObject} HashObject
         */
        HashObject.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.HashObject)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.HashObject: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.HashObject();
            if (object.algorithm !== 0 && (typeof object.algorithm !== "string" || $root.proto.HashAlgorithm[object.algorithm] !== 0))
                switch (object.algorithm) {
                case "HASH_ALGORITHM_UNKNOWN":
                case 0:
                    message.algorithm = 0;
                    break;
                case "SHA_384":
                case 1:
                    message.algorithm = 1;
                    break;
                default:
                    if (typeof object.algorithm === "number" && (object.algorithm | 0) === object.algorithm)
                        message.algorithm = object.algorithm;
                }
            if (object.length != null)
                if ($Number(object.length) !== 0)
                    message.length = object.length | 0;
            if (object.hash != null)
                if (object.hash.length)
                    if (typeof object.hash === "string")
                        $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                    else if (object.hash.length >= 0)
                        message.hash = object.hash;
            return message;
        };

        /**
         * Creates a plain object from a HashObject message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.HashObject
         * @static
         * @param {proto.HashObject} message HashObject
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HashObject.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.algorithm = options.enums === $String ? "HASH_ALGORITHM_UNKNOWN" : 0;
                object.length = 0;
                if (options.bytes === $String)
                    object.hash = "";
                else {
                    object.hash = [];
                    if (options.bytes !== $Array)
                        object.hash = $util.newBuffer(object.hash);
                }
            }
            if (message.algorithm != null && $Object.hasOwnProperty.call(message, "algorithm"))
                object.algorithm = options.enums === $String ? $root.proto.HashAlgorithm[message.algorithm] === $undefined ? message.algorithm : $root.proto.HashAlgorithm[message.algorithm] : message.algorithm;
            if (message.length != null && $Object.hasOwnProperty.call(message, "length"))
                object.length = message.length;
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = options.bytes === $String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hash) : message.hash;
            return object;
        };

        /**
         * Converts this HashObject to JSON.
         * @function toJSON
         * @memberof proto.HashObject
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HashObject.prototype.toJSON = function() {
            return HashObject.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for HashObject
         * @function getTypeUrl
         * @memberof proto.HashObject
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        HashObject.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.HashObject";
        };

        return HashObject;
    })();

    proto.RecordStreamFile = (function() {

        /**
         * Properties of a RecordStreamFile.
         * @typedef {Object} proto.RecordStreamFile.$Properties
         * @property {proto.SemanticVersion.$Properties|null} [hapiProtoVersion] Version of HAPI that was used to serialize the file.
         * @property {proto.HashObject.$Properties|null} [startObjectRunningHash] Running Hash of all RecordStreamItems before writing this file.
         * @property {Array.<proto.RecordStreamItem.$Properties>|null} [recordStreamItems] List of all the record stream items from that period.
         * @property {proto.HashObject.$Properties|null} [endObjectRunningHash] Running Hash of all RecordStreamItems before closing this file.
         * @property {number|Long|null} [blockNumber] The block number associated with this period.
         * @property {Array.<proto.SidecarMetadata.$Properties>|null} [sidecars] List of the hashes of all the sidecar record files created for the same period.
         * Allows multiple sidecar files to be linked to this RecordStreamFile.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RecordStreamFile.
         * @memberof proto
         * @interface IRecordStreamFile
         * @augments proto.RecordStreamFile.$Properties
         * @deprecated Use proto.RecordStreamFile.$Properties instead.
         */

        /**
         * Shape of a RecordStreamFile.
         * @typedef {{
         *   hapiProtoVersion?: proto.SemanticVersion.$Shape|null;
         *   startObjectRunningHash?: proto.HashObject.$Shape|null;
         *   recordStreamItems?: Array.<proto.RecordStreamItem.$Shape>|null;
         *   endObjectRunningHash?: proto.HashObject.$Shape|null;
         *   blockNumber?: number|Long|null;
         *   sidecars?: Array.<proto.SidecarMetadata.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.RecordStreamFile.$Shape
         */

        /**
         * Constructs a new RecordStreamFile.
         * @memberof proto
         * @classdesc RecordStreamFile is used to serialize all RecordStreamItems that are part of the
         * same period into record stream files.
         * This structure represents a block in Hedera (HIP-415).
         * @constructor
         * @param {proto.RecordStreamFile.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RecordStreamFile = function (properties) {
            this.recordStreamItems = [];
            this.sidecars = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Version of HAPI that was used to serialize the file.
         * @member {proto.SemanticVersion.$Properties|null|undefined} hapiProtoVersion
         * @memberof proto.RecordStreamFile
         * @instance
         */
        RecordStreamFile.prototype.hapiProtoVersion = null;

        /**
         * Running Hash of all RecordStreamItems before writing this file.
         * @member {proto.HashObject.$Properties|null|undefined} startObjectRunningHash
         * @memberof proto.RecordStreamFile
         * @instance
         */
        RecordStreamFile.prototype.startObjectRunningHash = null;

        /**
         * List of all the record stream items from that period.
         * @member {Array.<proto.RecordStreamItem.$Properties>} recordStreamItems
         * @memberof proto.RecordStreamFile
         * @instance
         */
        RecordStreamFile.prototype.recordStreamItems = $util.emptyArray;

        /**
         * Running Hash of all RecordStreamItems before closing this file.
         * @member {proto.HashObject.$Properties|null|undefined} endObjectRunningHash
         * @memberof proto.RecordStreamFile
         * @instance
         */
        RecordStreamFile.prototype.endObjectRunningHash = null;

        /**
         * The block number associated with this period.
         * @member {number|Long} blockNumber
         * @memberof proto.RecordStreamFile
         * @instance
         */
        RecordStreamFile.prototype.blockNumber = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * List of the hashes of all the sidecar record files created for the same period.
         * Allows multiple sidecar files to be linked to this RecordStreamFile.
         * @member {Array.<proto.SidecarMetadata.$Properties>} sidecars
         * @memberof proto.RecordStreamFile
         * @instance
         */
        RecordStreamFile.prototype.sidecars = $util.emptyArray;

        /**
         * Creates a new RecordStreamFile instance using the specified properties.
         * @function create
         * @memberof proto.RecordStreamFile
         * @static
         * @param {proto.RecordStreamFile.$Properties=} [properties] Properties to set
         * @returns {proto.RecordStreamFile} RecordStreamFile instance
         * @type {{
         *   (properties: proto.RecordStreamFile.$Shape): proto.RecordStreamFile & proto.RecordStreamFile.$Shape;
         *   (properties?: proto.RecordStreamFile.$Properties): proto.RecordStreamFile;
         * }}
         */
        RecordStreamFile.create = function(properties) {
            return new RecordStreamFile(properties);
        };

        /**
         * Encodes the specified RecordStreamFile message. Does not implicitly {@link proto.RecordStreamFile.verify|verify} messages.
         * @function encode
         * @memberof proto.RecordStreamFile
         * @static
         * @param {proto.RecordStreamFile.$Properties} message RecordStreamFile message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RecordStreamFile.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hapiProtoVersion != null && $Object.hasOwnProperty.call(message, "hapiProtoVersion"))
                $root.proto.SemanticVersion.encode(message.hapiProtoVersion, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.startObjectRunningHash != null && $Object.hasOwnProperty.call(message, "startObjectRunningHash"))
                $root.proto.HashObject.encode(message.startObjectRunningHash, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.recordStreamItems != null && message.recordStreamItems.length)
                for (let i = 0; i < message.recordStreamItems.length; ++i)
                    $root.proto.RecordStreamItem.encode(message.recordStreamItems[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.endObjectRunningHash != null && $Object.hasOwnProperty.call(message, "endObjectRunningHash"))
                $root.proto.HashObject.encode(message.endObjectRunningHash, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.blockNumber != null && $Object.hasOwnProperty.call(message, "blockNumber") && (typeof message.blockNumber === "object" ? message.blockNumber.low || message.blockNumber.high : message.blockNumber !== 0))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.blockNumber);
            if (message.sidecars != null && message.sidecars.length)
                for (let i = 0; i < message.sidecars.length; ++i)
                    $root.proto.SidecarMetadata.encode(message.sidecars[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RecordStreamFile message, length delimited. Does not implicitly {@link proto.RecordStreamFile.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.RecordStreamFile
         * @static
         * @param {proto.RecordStreamFile.$Properties} message RecordStreamFile message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RecordStreamFile.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RecordStreamFile message from the specified reader or buffer.
         * @function decode
         * @memberof proto.RecordStreamFile
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.RecordStreamFile & proto.RecordStreamFile.$Shape} RecordStreamFile
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RecordStreamFile.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.RecordStreamFile();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.hapiProtoVersion = $root.proto.SemanticVersion.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hapiProtoVersion);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.startObjectRunningHash = $root.proto.HashObject.decode(reader, reader.uint32(), $undefined, _depth + 1, message.startObjectRunningHash);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.recordStreamItems && message.recordStreamItems.length))
                            message.recordStreamItems = [];
                        message.recordStreamItems.push($root.proto.RecordStreamItem.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.endObjectRunningHash = $root.proto.HashObject.decode(reader, reader.uint32(), $undefined, _depth + 1, message.endObjectRunningHash);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.blockNumber = value;
                        else
                            delete message.blockNumber;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.sidecars && message.sidecars.length))
                            message.sidecars = [];
                        message.sidecars.push($root.proto.SidecarMetadata.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RecordStreamFile message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.RecordStreamFile
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.RecordStreamFile & proto.RecordStreamFile.$Shape} RecordStreamFile
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RecordStreamFile.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RecordStreamFile message.
         * @function verify
         * @memberof proto.RecordStreamFile
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RecordStreamFile.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hapiProtoVersion != null && $Object.hasOwnProperty.call(message, "hapiProtoVersion")) {
                let error = $root.proto.SemanticVersion.verify(message.hapiProtoVersion, _depth + 1);
                if (error)
                    return "hapiProtoVersion." + error;
            }
            if (message.startObjectRunningHash != null && $Object.hasOwnProperty.call(message, "startObjectRunningHash")) {
                let error = $root.proto.HashObject.verify(message.startObjectRunningHash, _depth + 1);
                if (error)
                    return "startObjectRunningHash." + error;
            }
            if (message.recordStreamItems != null && $Object.hasOwnProperty.call(message, "recordStreamItems")) {
                if (!$Array.isArray(message.recordStreamItems))
                    return "recordStreamItems: array expected";
                for (let i = 0; i < message.recordStreamItems.length; ++i) {
                    let error = $root.proto.RecordStreamItem.verify(message.recordStreamItems[i], _depth + 1);
                    if (error)
                        return "recordStreamItems." + error;
                }
            }
            if (message.endObjectRunningHash != null && $Object.hasOwnProperty.call(message, "endObjectRunningHash")) {
                let error = $root.proto.HashObject.verify(message.endObjectRunningHash, _depth + 1);
                if (error)
                    return "endObjectRunningHash." + error;
            }
            if (message.blockNumber != null && $Object.hasOwnProperty.call(message, "blockNumber"))
                if (!$util.isInteger(message.blockNumber) && !(message.blockNumber && $util.isInteger(message.blockNumber.low) && $util.isInteger(message.blockNumber.high)))
                    return "blockNumber: integer|Long expected";
            if (message.sidecars != null && $Object.hasOwnProperty.call(message, "sidecars")) {
                if (!$Array.isArray(message.sidecars))
                    return "sidecars: array expected";
                for (let i = 0; i < message.sidecars.length; ++i) {
                    let error = $root.proto.SidecarMetadata.verify(message.sidecars[i], _depth + 1);
                    if (error)
                        return "sidecars." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RecordStreamFile message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.RecordStreamFile
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.RecordStreamFile} RecordStreamFile
         */
        RecordStreamFile.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.RecordStreamFile)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.RecordStreamFile: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.RecordStreamFile();
            if (object.hapiProtoVersion != null) {
                if (!$util.isObject(object.hapiProtoVersion))
                    throw $TypeError(".proto.RecordStreamFile.hapiProtoVersion: object expected");
                message.hapiProtoVersion = $root.proto.SemanticVersion.fromObject(object.hapiProtoVersion, _depth + 1);
            }
            if (object.startObjectRunningHash != null) {
                if (!$util.isObject(object.startObjectRunningHash))
                    throw $TypeError(".proto.RecordStreamFile.startObjectRunningHash: object expected");
                message.startObjectRunningHash = $root.proto.HashObject.fromObject(object.startObjectRunningHash, _depth + 1);
            }
            if (object.recordStreamItems) {
                if (!$Array.isArray(object.recordStreamItems))
                    throw $TypeError(".proto.RecordStreamFile.recordStreamItems: array expected");
                message.recordStreamItems = $Array(object.recordStreamItems.length);
                for (let i = 0; i < object.recordStreamItems.length; ++i) {
                    if (!$util.isObject(object.recordStreamItems[i]))
                        throw $TypeError(".proto.RecordStreamFile.recordStreamItems: object expected");
                    message.recordStreamItems[i] = $root.proto.RecordStreamItem.fromObject(object.recordStreamItems[i], _depth + 1);
                }
            }
            if (object.endObjectRunningHash != null) {
                if (!$util.isObject(object.endObjectRunningHash))
                    throw $TypeError(".proto.RecordStreamFile.endObjectRunningHash: object expected");
                message.endObjectRunningHash = $root.proto.HashObject.fromObject(object.endObjectRunningHash, _depth + 1);
            }
            if (object.blockNumber != null)
                if (typeof object.blockNumber === "object" ? object.blockNumber.low || object.blockNumber.high : $Number(object.blockNumber) !== 0)
                    if ($util.Long)
                        message.blockNumber = $util.Long.fromValue(object.blockNumber, false);
                    else if (typeof object.blockNumber === "string")
                        message.blockNumber = $parseInt(object.blockNumber, 10);
                    else if (typeof object.blockNumber === "number")
                        message.blockNumber = object.blockNumber;
                    else if (typeof object.blockNumber === "object")
                        message.blockNumber = new $util.LongBits(object.blockNumber.low >>> 0, object.blockNumber.high >>> 0).toNumber();
            if (object.sidecars) {
                if (!$Array.isArray(object.sidecars))
                    throw $TypeError(".proto.RecordStreamFile.sidecars: array expected");
                message.sidecars = $Array(object.sidecars.length);
                for (let i = 0; i < object.sidecars.length; ++i) {
                    if (!$util.isObject(object.sidecars[i]))
                        throw $TypeError(".proto.RecordStreamFile.sidecars: object expected");
                    message.sidecars[i] = $root.proto.SidecarMetadata.fromObject(object.sidecars[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RecordStreamFile message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.RecordStreamFile
         * @static
         * @param {proto.RecordStreamFile} message RecordStreamFile
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RecordStreamFile.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.recordStreamItems = [];
                object.sidecars = [];
            }
            if (options.defaults) {
                object.hapiProtoVersion = null;
                object.startObjectRunningHash = null;
                object.endObjectRunningHash = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.blockNumber = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.blockNumber = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.hapiProtoVersion != null && $Object.hasOwnProperty.call(message, "hapiProtoVersion"))
                object.hapiProtoVersion = $root.proto.SemanticVersion.toObject(message.hapiProtoVersion, options, _depth + 1);
            if (message.startObjectRunningHash != null && $Object.hasOwnProperty.call(message, "startObjectRunningHash"))
                object.startObjectRunningHash = $root.proto.HashObject.toObject(message.startObjectRunningHash, options, _depth + 1);
            if (message.recordStreamItems && message.recordStreamItems.length) {
                object.recordStreamItems = $Array(message.recordStreamItems.length);
                for (let j = 0; j < message.recordStreamItems.length; ++j)
                    object.recordStreamItems[j] = $root.proto.RecordStreamItem.toObject(message.recordStreamItems[j], options, _depth + 1);
            }
            if (message.endObjectRunningHash != null && $Object.hasOwnProperty.call(message, "endObjectRunningHash"))
                object.endObjectRunningHash = $root.proto.HashObject.toObject(message.endObjectRunningHash, options, _depth + 1);
            if (message.blockNumber != null && $Object.hasOwnProperty.call(message, "blockNumber"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.blockNumber = typeof message.blockNumber === "number" ? $BigInt(message.blockNumber) : $util.Long.fromBits(message.blockNumber.low >>> 0, message.blockNumber.high >>> 0, false).toBigInt();
                else if (typeof message.blockNumber === "number")
                    object.blockNumber = options.longs === $String ? $String(message.blockNumber) : message.blockNumber;
                else
                    object.blockNumber = options.longs === $String ? $util.Long.prototype.toString.call(message.blockNumber) : options.longs === $Number ? new $util.LongBits(message.blockNumber.low >>> 0, message.blockNumber.high >>> 0).toNumber() : message.blockNumber;
            if (message.sidecars && message.sidecars.length) {
                object.sidecars = $Array(message.sidecars.length);
                for (let j = 0; j < message.sidecars.length; ++j)
                    object.sidecars[j] = $root.proto.SidecarMetadata.toObject(message.sidecars[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this RecordStreamFile to JSON.
         * @function toJSON
         * @memberof proto.RecordStreamFile
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RecordStreamFile.prototype.toJSON = function() {
            return RecordStreamFile.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RecordStreamFile
         * @function getTypeUrl
         * @memberof proto.RecordStreamFile
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RecordStreamFile.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.RecordStreamFile";
        };

        return RecordStreamFile;
    })();

    proto.RecordStreamItem = (function() {

        /**
         * Properties of a RecordStreamItem.
         * @typedef {Object} proto.RecordStreamItem.$Properties
         * @property {proto.Transaction.$Properties|null} [transaction] RecordStreamItem transaction
         * @property {proto.TransactionRecord.$Properties|null} [record] RecordStreamItem record
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a RecordStreamItem.
         * @memberof proto
         * @interface IRecordStreamItem
         * @augments proto.RecordStreamItem.$Properties
         * @deprecated Use proto.RecordStreamItem.$Properties instead.
         */

        /**
         * Shape of a RecordStreamItem.
         * @typedef {{
         *   transaction?: proto.Transaction.$Shape|null;
         *   record?: proto.TransactionRecord.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.RecordStreamItem.$Shape
         */

        /**
         * Constructs a new RecordStreamItem.
         * @memberof proto
         * @classdesc A RecordStreamItem consists of a Transaction and a TransactionRecord,
         * which are already defined protobuf messages.
         * @constructor
         * @param {proto.RecordStreamItem.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const RecordStreamItem = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * RecordStreamItem transaction.
         * @member {proto.Transaction.$Properties|null|undefined} transaction
         * @memberof proto.RecordStreamItem
         * @instance
         */
        RecordStreamItem.prototype.transaction = null;

        /**
         * RecordStreamItem record.
         * @member {proto.TransactionRecord.$Properties|null|undefined} record
         * @memberof proto.RecordStreamItem
         * @instance
         */
        RecordStreamItem.prototype.record = null;

        /**
         * Creates a new RecordStreamItem instance using the specified properties.
         * @function create
         * @memberof proto.RecordStreamItem
         * @static
         * @param {proto.RecordStreamItem.$Properties=} [properties] Properties to set
         * @returns {proto.RecordStreamItem} RecordStreamItem instance
         * @type {{
         *   (properties: proto.RecordStreamItem.$Shape): proto.RecordStreamItem & proto.RecordStreamItem.$Shape;
         *   (properties?: proto.RecordStreamItem.$Properties): proto.RecordStreamItem;
         * }}
         */
        RecordStreamItem.create = function(properties) {
            return new RecordStreamItem(properties);
        };

        /**
         * Encodes the specified RecordStreamItem message. Does not implicitly {@link proto.RecordStreamItem.verify|verify} messages.
         * @function encode
         * @memberof proto.RecordStreamItem
         * @static
         * @param {proto.RecordStreamItem.$Properties} message RecordStreamItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RecordStreamItem.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.transaction != null && $Object.hasOwnProperty.call(message, "transaction"))
                $root.proto.Transaction.encode(message.transaction, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.record != null && $Object.hasOwnProperty.call(message, "record"))
                $root.proto.TransactionRecord.encode(message.record, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RecordStreamItem message, length delimited. Does not implicitly {@link proto.RecordStreamItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.RecordStreamItem
         * @static
         * @param {proto.RecordStreamItem.$Properties} message RecordStreamItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RecordStreamItem.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a RecordStreamItem message from the specified reader or buffer.
         * @function decode
         * @memberof proto.RecordStreamItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.RecordStreamItem & proto.RecordStreamItem.$Shape} RecordStreamItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RecordStreamItem.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.RecordStreamItem();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.transaction = $root.proto.Transaction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.transaction);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.record = $root.proto.TransactionRecord.decode(reader, reader.uint32(), $undefined, _depth + 1, message.record);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a RecordStreamItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.RecordStreamItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.RecordStreamItem & proto.RecordStreamItem.$Shape} RecordStreamItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RecordStreamItem.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RecordStreamItem message.
         * @function verify
         * @memberof proto.RecordStreamItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RecordStreamItem.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.transaction != null && $Object.hasOwnProperty.call(message, "transaction")) {
                let error = $root.proto.Transaction.verify(message.transaction, _depth + 1);
                if (error)
                    return "transaction." + error;
            }
            if (message.record != null && $Object.hasOwnProperty.call(message, "record")) {
                let error = $root.proto.TransactionRecord.verify(message.record, _depth + 1);
                if (error)
                    return "record." + error;
            }
            return null;
        };

        /**
         * Creates a RecordStreamItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.RecordStreamItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.RecordStreamItem} RecordStreamItem
         */
        RecordStreamItem.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.RecordStreamItem)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.RecordStreamItem: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.RecordStreamItem();
            if (object.transaction != null) {
                if (!$util.isObject(object.transaction))
                    throw $TypeError(".proto.RecordStreamItem.transaction: object expected");
                message.transaction = $root.proto.Transaction.fromObject(object.transaction, _depth + 1);
            }
            if (object.record != null) {
                if (!$util.isObject(object.record))
                    throw $TypeError(".proto.RecordStreamItem.record: object expected");
                message.record = $root.proto.TransactionRecord.fromObject(object.record, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RecordStreamItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.RecordStreamItem
         * @static
         * @param {proto.RecordStreamItem} message RecordStreamItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RecordStreamItem.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.transaction = null;
                object.record = null;
            }
            if (message.transaction != null && $Object.hasOwnProperty.call(message, "transaction"))
                object.transaction = $root.proto.Transaction.toObject(message.transaction, options, _depth + 1);
            if (message.record != null && $Object.hasOwnProperty.call(message, "record"))
                object.record = $root.proto.TransactionRecord.toObject(message.record, options, _depth + 1);
            return object;
        };

        /**
         * Converts this RecordStreamItem to JSON.
         * @function toJSON
         * @memberof proto.RecordStreamItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RecordStreamItem.prototype.toJSON = function() {
            return RecordStreamItem.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for RecordStreamItem
         * @function getTypeUrl
         * @memberof proto.RecordStreamItem
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        RecordStreamItem.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.RecordStreamItem";
        };

        return RecordStreamItem;
    })();

    proto.SidecarMetadata = (function() {

        /**
         * Properties of a SidecarMetadata.
         * @typedef {Object} proto.SidecarMetadata.$Properties
         * @property {proto.HashObject.$Properties|null} [hash] The hash of the entire file.
         * @property {number|null} [id] The id of the sidecar record file
         * @property {Array.<proto.SidecarType>|null} [types] The types of sidecar records that will be included in the file.
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SidecarMetadata.
         * @memberof proto
         * @interface ISidecarMetadata
         * @augments proto.SidecarMetadata.$Properties
         * @deprecated Use proto.SidecarMetadata.$Properties instead.
         */

        /**
         * Shape of a SidecarMetadata.
         * @typedef {proto.SidecarMetadata.$Properties} proto.SidecarMetadata.$Shape
         */

        /**
         * Constructs a new SidecarMetadata.
         * @memberof proto
         * @classdesc Information about a single sidecar file.
         * @constructor
         * @param {proto.SidecarMetadata.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SidecarMetadata = function (properties) {
            this.types = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The hash of the entire file.
         * @member {proto.HashObject.$Properties|null|undefined} hash
         * @memberof proto.SidecarMetadata
         * @instance
         */
        SidecarMetadata.prototype.hash = null;

        /**
         * The id of the sidecar record file
         * @member {number} id
         * @memberof proto.SidecarMetadata
         * @instance
         */
        SidecarMetadata.prototype.id = 0;

        /**
         * The types of sidecar records that will be included in the file.
         * @member {Array.<proto.SidecarType>} types
         * @memberof proto.SidecarMetadata
         * @instance
         */
        SidecarMetadata.prototype.types = $util.emptyArray;

        /**
         * Creates a new SidecarMetadata instance using the specified properties.
         * @function create
         * @memberof proto.SidecarMetadata
         * @static
         * @param {proto.SidecarMetadata.$Properties=} [properties] Properties to set
         * @returns {proto.SidecarMetadata} SidecarMetadata instance
         * @type {{
         *   (properties: proto.SidecarMetadata.$Shape): proto.SidecarMetadata & proto.SidecarMetadata.$Shape;
         *   (properties?: proto.SidecarMetadata.$Properties): proto.SidecarMetadata;
         * }}
         */
        SidecarMetadata.create = function(properties) {
            return new SidecarMetadata(properties);
        };

        /**
         * Encodes the specified SidecarMetadata message. Does not implicitly {@link proto.SidecarMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.SidecarMetadata
         * @static
         * @param {proto.SidecarMetadata.$Properties} message SidecarMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SidecarMetadata.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                $root.proto.HashObject.encode(message.hash, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.id);
            if (message.types != null && message.types.length)
                writer.uint32(/* id 3, wireType 2 =*/26).int32s(message.types);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SidecarMetadata message, length delimited. Does not implicitly {@link proto.SidecarMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SidecarMetadata
         * @static
         * @param {proto.SidecarMetadata.$Properties} message SidecarMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SidecarMetadata.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SidecarMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SidecarMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SidecarMetadata & proto.SidecarMetadata.$Shape} SidecarMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SidecarMetadata.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SidecarMetadata();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.hash = $root.proto.HashObject.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hash);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.types && message.types.length))
                                message.types = [];
                            reader.int32s(message.types);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.types && message.types.length))
                            message.types = [];
                        message.types.push(reader.int32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SidecarMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SidecarMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SidecarMetadata & proto.SidecarMetadata.$Shape} SidecarMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SidecarMetadata.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SidecarMetadata message.
         * @function verify
         * @memberof proto.SidecarMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SidecarMetadata.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash")) {
                let error = $root.proto.HashObject.verify(message.hash, _depth + 1);
                if (error)
                    return "hash." + error;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.types != null && $Object.hasOwnProperty.call(message, "types")) {
                if (!$Array.isArray(message.types))
                    return "types: array expected";
                for (let i = 0; i < message.types.length; ++i)
                    if (typeof message.types[i] !== "number" || (message.types[i] | 0) !== message.types[i])
                        return "types: enum value[] expected";
            }
            return null;
        };

        /**
         * Creates a SidecarMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SidecarMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SidecarMetadata} SidecarMetadata
         */
        SidecarMetadata.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SidecarMetadata)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SidecarMetadata: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SidecarMetadata();
            if (object.hash != null) {
                if (!$util.isObject(object.hash))
                    throw $TypeError(".proto.SidecarMetadata.hash: object expected");
                message.hash = $root.proto.HashObject.fromObject(object.hash, _depth + 1);
            }
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.types) {
                if (!$Array.isArray(object.types))
                    throw $TypeError(".proto.SidecarMetadata.types: array expected");
                message.types = [];
                for (let i = 0; i < object.types.length; ++i)
                    switch (object.types[i]) {
                    case "SIDECAR_TYPE_UNKNOWN":
                    case 0:
                        message.types[message.types.length] = 0;
                        break;
                    case "CONTRACT_STATE_CHANGE":
                    case 1:
                        message.types[message.types.length] = 1;
                        break;
                    case "CONTRACT_ACTION":
                    case 2:
                        message.types[message.types.length] = 2;
                        break;
                    case "CONTRACT_BYTECODE":
                    case 3:
                        message.types[message.types.length] = 3;
                        break;
                    default:
                        if (typeof object.types[i] === "number" && (object.types[i] | 0) === object.types[i])
                            message.types[message.types.length] = object.types[i];
                    }
            }
            return message;
        };

        /**
         * Creates a plain object from a SidecarMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SidecarMetadata
         * @static
         * @param {proto.SidecarMetadata} message SidecarMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SidecarMetadata.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.types = [];
            if (options.defaults) {
                object.hash = null;
                object.id = 0;
            }
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                object.hash = $root.proto.HashObject.toObject(message.hash, options, _depth + 1);
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.types && message.types.length) {
                object.types = $Array(message.types.length);
                for (let j = 0; j < message.types.length; ++j)
                    object.types[j] = options.enums === $String ? $root.proto.SidecarType[message.types[j]] === $undefined ? message.types[j] : $root.proto.SidecarType[message.types[j]] : message.types[j];
            }
            return object;
        };

        /**
         * Converts this SidecarMetadata to JSON.
         * @function toJSON
         * @memberof proto.SidecarMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SidecarMetadata.prototype.toJSON = function() {
            return SidecarMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SidecarMetadata
         * @function getTypeUrl
         * @memberof proto.SidecarMetadata
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SidecarMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SidecarMetadata";
        };

        return SidecarMetadata;
    })();

    /**
     * The type of sidecar records contained in the sidecar record file
     * @name proto.SidecarType
     * @enum {number}
     * @property {number} SIDECAR_TYPE_UNKNOWN=0 SIDECAR_TYPE_UNKNOWN value
     * @property {number} CONTRACT_STATE_CHANGE=1 CONTRACT_STATE_CHANGE value
     * @property {number} CONTRACT_ACTION=2 CONTRACT_ACTION value
     * @property {number} CONTRACT_BYTECODE=3 CONTRACT_BYTECODE value
     */
    proto.SidecarType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "SIDECAR_TYPE_UNKNOWN"] = 0;
        values[valuesById[1] = "CONTRACT_STATE_CHANGE"] = 1;
        values[valuesById[2] = "CONTRACT_ACTION"] = 2;
        values[valuesById[3] = "CONTRACT_BYTECODE"] = 3;
        return values;
    })();

    proto.SidecarFile = (function() {

        /**
         * Properties of a SidecarFile.
         * @typedef {Object} proto.SidecarFile.$Properties
         * @property {Array.<proto.TransactionSidecarRecord.$Properties>|null} [sidecarRecords] List of sidecar records
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SidecarFile.
         * @memberof proto
         * @interface ISidecarFile
         * @augments proto.SidecarFile.$Properties
         * @deprecated Use proto.SidecarFile.$Properties instead.
         */

        /**
         * Shape of a SidecarFile.
         * @typedef {{
         *   sidecarRecords?: Array.<proto.TransactionSidecarRecord.$Shape>|null;
         *   $unknowns?: Array.<Uint8Array>;
         * }} proto.SidecarFile.$Shape
         */

        /**
         * Constructs a new SidecarFile.
         * @memberof proto
         * @classdesc A SidecarFile contains a list of TransactionSidecarRecords that are all created
         * in the same period and related to the same RecordStreamFile.
         * @constructor
         * @param {proto.SidecarFile.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SidecarFile = function (properties) {
            this.sidecarRecords = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * List of sidecar records
         * @member {Array.<proto.TransactionSidecarRecord.$Properties>} sidecarRecords
         * @memberof proto.SidecarFile
         * @instance
         */
        SidecarFile.prototype.sidecarRecords = $util.emptyArray;

        /**
         * Creates a new SidecarFile instance using the specified properties.
         * @function create
         * @memberof proto.SidecarFile
         * @static
         * @param {proto.SidecarFile.$Properties=} [properties] Properties to set
         * @returns {proto.SidecarFile} SidecarFile instance
         * @type {{
         *   (properties: proto.SidecarFile.$Shape): proto.SidecarFile & proto.SidecarFile.$Shape;
         *   (properties?: proto.SidecarFile.$Properties): proto.SidecarFile;
         * }}
         */
        SidecarFile.create = function(properties) {
            return new SidecarFile(properties);
        };

        /**
         * Encodes the specified SidecarFile message. Does not implicitly {@link proto.SidecarFile.verify|verify} messages.
         * @function encode
         * @memberof proto.SidecarFile
         * @static
         * @param {proto.SidecarFile.$Properties} message SidecarFile message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SidecarFile.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sidecarRecords != null && message.sidecarRecords.length)
                for (let i = 0; i < message.sidecarRecords.length; ++i)
                    $root.proto.TransactionSidecarRecord.encode(message.sidecarRecords[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SidecarFile message, length delimited. Does not implicitly {@link proto.SidecarFile.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SidecarFile
         * @static
         * @param {proto.SidecarFile.$Properties} message SidecarFile message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SidecarFile.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SidecarFile message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SidecarFile
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SidecarFile & proto.SidecarFile.$Shape} SidecarFile
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SidecarFile.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SidecarFile();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.sidecarRecords && message.sidecarRecords.length))
                            message.sidecarRecords = [];
                        message.sidecarRecords.push($root.proto.TransactionSidecarRecord.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SidecarFile message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SidecarFile
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SidecarFile & proto.SidecarFile.$Shape} SidecarFile
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SidecarFile.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SidecarFile message.
         * @function verify
         * @memberof proto.SidecarFile
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SidecarFile.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sidecarRecords != null && $Object.hasOwnProperty.call(message, "sidecarRecords")) {
                if (!$Array.isArray(message.sidecarRecords))
                    return "sidecarRecords: array expected";
                for (let i = 0; i < message.sidecarRecords.length; ++i) {
                    let error = $root.proto.TransactionSidecarRecord.verify(message.sidecarRecords[i], _depth + 1);
                    if (error)
                        return "sidecarRecords." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SidecarFile message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SidecarFile
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SidecarFile} SidecarFile
         */
        SidecarFile.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SidecarFile)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SidecarFile: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SidecarFile();
            if (object.sidecarRecords) {
                if (!$Array.isArray(object.sidecarRecords))
                    throw $TypeError(".proto.SidecarFile.sidecarRecords: array expected");
                message.sidecarRecords = $Array(object.sidecarRecords.length);
                for (let i = 0; i < object.sidecarRecords.length; ++i) {
                    if (!$util.isObject(object.sidecarRecords[i]))
                        throw $TypeError(".proto.SidecarFile.sidecarRecords: object expected");
                    message.sidecarRecords[i] = $root.proto.TransactionSidecarRecord.fromObject(object.sidecarRecords[i], _depth + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SidecarFile message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SidecarFile
         * @static
         * @param {proto.SidecarFile} message SidecarFile
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SidecarFile.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.sidecarRecords = [];
            if (message.sidecarRecords && message.sidecarRecords.length) {
                object.sidecarRecords = $Array(message.sidecarRecords.length);
                for (let j = 0; j < message.sidecarRecords.length; ++j)
                    object.sidecarRecords[j] = $root.proto.TransactionSidecarRecord.toObject(message.sidecarRecords[j], options, _depth + 1);
            }
            return object;
        };

        /**
         * Converts this SidecarFile to JSON.
         * @function toJSON
         * @memberof proto.SidecarFile
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SidecarFile.prototype.toJSON = function() {
            return SidecarFile.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SidecarFile
         * @function getTypeUrl
         * @memberof proto.SidecarFile
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SidecarFile.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SidecarFile";
        };

        return SidecarFile;
    })();

    proto.TransactionSidecarRecord = (function() {

        /**
         * Properties of a TransactionSidecarRecord.
         * @typedef {Object} proto.TransactionSidecarRecord.$Properties
         * @property {proto.Timestamp.$Properties|null} [consensusTimestamp] Consensus timestamp will be the same as the consensus timestamp of the
         * transaction the side car is related to. This offers a convenient
         * way to match record to sidecar.
         * @property {boolean|null} [migration] Whether sidecar is from migration.
         * @property {proto.ContractStateChanges.$Properties|null} [stateChanges] TransactionSidecarRecord stateChanges
         * @property {proto.ContractActions.$Properties|null} [actions] TransactionSidecarRecord actions
         * @property {proto.ContractBytecode.$Properties|null} [bytecode] TransactionSidecarRecord bytecode
         * @property {"stateChanges"|"actions"|"bytecode"} [sidecarRecords] TransactionSidecarRecord sidecarRecords
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a TransactionSidecarRecord.
         * @memberof proto
         * @interface ITransactionSidecarRecord
         * @augments proto.TransactionSidecarRecord.$Properties
         * @deprecated Use proto.TransactionSidecarRecord.$Properties instead.
         */

        /**
         * Narrowed shape of a TransactionSidecarRecord.
         * @typedef {{
         *   consensusTimestamp?: proto.Timestamp.$Shape|null;
         *   migration?: boolean|null;
         *   stateChanges?: proto.ContractStateChanges.$Shape|null;
         *   actions?: proto.ContractActions.$Shape|null;
         *   bytecode?: proto.ContractBytecode.$Shape|null;
         *   $unknowns?: Array.<Uint8Array>;
         * } & (
         *   ({ sidecarRecords?: undefined; stateChanges?: null; actions?: null; bytecode?: null }|{ sidecarRecords?: "stateChanges"; stateChanges: proto.ContractStateChanges.$Shape; actions?: null; bytecode?: null }|{ sidecarRecords?: "actions"; stateChanges?: null; actions: proto.ContractActions.$Shape; bytecode?: null }|{ sidecarRecords?: "bytecode"; stateChanges?: null; actions?: null; bytecode: proto.ContractBytecode.$Shape })
         * )} proto.TransactionSidecarRecord.$Shape
         */

        /**
         * Constructs a new TransactionSidecarRecord.
         * @memberof proto
         * @classdesc TransactionSidecarRecord is used to create sidecar records complementing
         * TransactionRecord and storing additional information about a transaction's execution.
         * @constructor
         * @param {proto.TransactionSidecarRecord.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const TransactionSidecarRecord = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Consensus timestamp will be the same as the consensus timestamp of the
         * transaction the side car is related to. This offers a convenient
         * way to match record to sidecar.
         * @member {proto.Timestamp.$Properties|null|undefined} consensusTimestamp
         * @memberof proto.TransactionSidecarRecord
         * @instance
         */
        TransactionSidecarRecord.prototype.consensusTimestamp = null;

        /**
         * Whether sidecar is from migration.
         * @member {boolean} migration
         * @memberof proto.TransactionSidecarRecord
         * @instance
         */
        TransactionSidecarRecord.prototype.migration = false;

        /**
         * TransactionSidecarRecord stateChanges.
         * @member {proto.ContractStateChanges.$Properties|null|undefined} stateChanges
         * @memberof proto.TransactionSidecarRecord
         * @instance
         */
        TransactionSidecarRecord.prototype.stateChanges = null;

        /**
         * TransactionSidecarRecord actions.
         * @member {proto.ContractActions.$Properties|null|undefined} actions
         * @memberof proto.TransactionSidecarRecord
         * @instance
         */
        TransactionSidecarRecord.prototype.actions = null;

        /**
         * TransactionSidecarRecord bytecode.
         * @member {proto.ContractBytecode.$Properties|null|undefined} bytecode
         * @memberof proto.TransactionSidecarRecord
         * @instance
         */
        TransactionSidecarRecord.prototype.bytecode = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * TransactionSidecarRecord sidecarRecords.
         * @member {"stateChanges"|"actions"|"bytecode"|undefined} sidecarRecords
         * @memberof proto.TransactionSidecarRecord
         * @instance
         */
        $Object.defineProperty(TransactionSidecarRecord.prototype, "sidecarRecords", {
            get: $util.oneOfGetter($oneOfFields = ["stateChanges", "actions", "bytecode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new TransactionSidecarRecord instance using the specified properties.
         * @function create
         * @memberof proto.TransactionSidecarRecord
         * @static
         * @param {proto.TransactionSidecarRecord.$Properties=} [properties] Properties to set
         * @returns {proto.TransactionSidecarRecord} TransactionSidecarRecord instance
         * @type {{
         *   (properties: proto.TransactionSidecarRecord.$Shape): proto.TransactionSidecarRecord & proto.TransactionSidecarRecord.$Shape;
         *   (properties?: proto.TransactionSidecarRecord.$Properties): proto.TransactionSidecarRecord;
         * }}
         */
        TransactionSidecarRecord.create = function(properties) {
            return new TransactionSidecarRecord(properties);
        };

        /**
         * Encodes the specified TransactionSidecarRecord message. Does not implicitly {@link proto.TransactionSidecarRecord.verify|verify} messages.
         * @function encode
         * @memberof proto.TransactionSidecarRecord
         * @static
         * @param {proto.TransactionSidecarRecord.$Properties} message TransactionSidecarRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionSidecarRecord.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp"))
                $root.proto.Timestamp.encode(message.consensusTimestamp, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.migration != null && $Object.hasOwnProperty.call(message, "migration") && message.migration !== false)
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.migration);
            if (message.stateChanges != null && $Object.hasOwnProperty.call(message, "stateChanges"))
                $root.proto.ContractStateChanges.encode(message.stateChanges, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
            if (message.actions != null && $Object.hasOwnProperty.call(message, "actions"))
                $root.proto.ContractActions.encode(message.actions, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
            if (message.bytecode != null && $Object.hasOwnProperty.call(message, "bytecode"))
                $root.proto.ContractBytecode.encode(message.bytecode, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TransactionSidecarRecord message, length delimited. Does not implicitly {@link proto.TransactionSidecarRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TransactionSidecarRecord
         * @static
         * @param {proto.TransactionSidecarRecord.$Properties} message TransactionSidecarRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TransactionSidecarRecord.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a TransactionSidecarRecord message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TransactionSidecarRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TransactionSidecarRecord & proto.TransactionSidecarRecord.$Shape} TransactionSidecarRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionSidecarRecord.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.TransactionSidecarRecord();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.consensusTimestamp = $root.proto.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.consensusTimestamp);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.migration = value;
                        else
                            delete message.migration;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.stateChanges = $root.proto.ContractStateChanges.decode(reader, reader.uint32(), $undefined, _depth + 1, message.stateChanges);
                        message.sidecarRecords = "stateChanges";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.actions = $root.proto.ContractActions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.actions);
                        message.sidecarRecords = "actions";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.bytecode = $root.proto.ContractBytecode.decode(reader, reader.uint32(), $undefined, _depth + 1, message.bytecode);
                        message.sidecarRecords = "bytecode";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a TransactionSidecarRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TransactionSidecarRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TransactionSidecarRecord & proto.TransactionSidecarRecord.$Shape} TransactionSidecarRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TransactionSidecarRecord.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TransactionSidecarRecord message.
         * @function verify
         * @memberof proto.TransactionSidecarRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TransactionSidecarRecord.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            let properties = {};
            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp")) {
                let error = $root.proto.Timestamp.verify(message.consensusTimestamp, _depth + 1);
                if (error)
                    return "consensusTimestamp." + error;
            }
            if (message.migration != null && $Object.hasOwnProperty.call(message, "migration"))
                if (typeof message.migration !== "boolean")
                    return "migration: boolean expected";
            if (message.stateChanges != null && $Object.hasOwnProperty.call(message, "stateChanges")) {
                properties.sidecarRecords = 1;
                {
                    let error = $root.proto.ContractStateChanges.verify(message.stateChanges, _depth + 1);
                    if (error)
                        return "stateChanges." + error;
                }
            }
            if (message.actions != null && $Object.hasOwnProperty.call(message, "actions")) {
                if (properties.sidecarRecords === 1)
                    return "sidecarRecords: multiple values";
                properties.sidecarRecords = 1;
                {
                    let error = $root.proto.ContractActions.verify(message.actions, _depth + 1);
                    if (error)
                        return "actions." + error;
                }
            }
            if (message.bytecode != null && $Object.hasOwnProperty.call(message, "bytecode")) {
                if (properties.sidecarRecords === 1)
                    return "sidecarRecords: multiple values";
                properties.sidecarRecords = 1;
                {
                    let error = $root.proto.ContractBytecode.verify(message.bytecode, _depth + 1);
                    if (error)
                        return "bytecode." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TransactionSidecarRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TransactionSidecarRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TransactionSidecarRecord} TransactionSidecarRecord
         */
        TransactionSidecarRecord.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.TransactionSidecarRecord)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.TransactionSidecarRecord: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.TransactionSidecarRecord();
            if (object.consensusTimestamp != null) {
                if (!$util.isObject(object.consensusTimestamp))
                    throw $TypeError(".proto.TransactionSidecarRecord.consensusTimestamp: object expected");
                message.consensusTimestamp = $root.proto.Timestamp.fromObject(object.consensusTimestamp, _depth + 1);
            }
            if (object.migration != null)
                if (object.migration)
                    message.migration = $Boolean(object.migration);
            if (object.stateChanges != null) {
                if (!$util.isObject(object.stateChanges))
                    throw $TypeError(".proto.TransactionSidecarRecord.stateChanges: object expected");
                message.stateChanges = $root.proto.ContractStateChanges.fromObject(object.stateChanges, _depth + 1);
            }
            if (object.actions != null) {
                if (!$util.isObject(object.actions))
                    throw $TypeError(".proto.TransactionSidecarRecord.actions: object expected");
                message.actions = $root.proto.ContractActions.fromObject(object.actions, _depth + 1);
            }
            if (object.bytecode != null) {
                if (!$util.isObject(object.bytecode))
                    throw $TypeError(".proto.TransactionSidecarRecord.bytecode: object expected");
                message.bytecode = $root.proto.ContractBytecode.fromObject(object.bytecode, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TransactionSidecarRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TransactionSidecarRecord
         * @static
         * @param {proto.TransactionSidecarRecord} message TransactionSidecarRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TransactionSidecarRecord.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.consensusTimestamp = null;
                object.migration = false;
            }
            if (message.consensusTimestamp != null && $Object.hasOwnProperty.call(message, "consensusTimestamp"))
                object.consensusTimestamp = $root.proto.Timestamp.toObject(message.consensusTimestamp, options, _depth + 1);
            if (message.migration != null && $Object.hasOwnProperty.call(message, "migration"))
                object.migration = message.migration;
            if (message.stateChanges != null && $Object.hasOwnProperty.call(message, "stateChanges")) {
                object.stateChanges = $root.proto.ContractStateChanges.toObject(message.stateChanges, options, _depth + 1);
                if (options.oneofs)
                    object.sidecarRecords = "stateChanges";
            }
            if (message.actions != null && $Object.hasOwnProperty.call(message, "actions")) {
                object.actions = $root.proto.ContractActions.toObject(message.actions, options, _depth + 1);
                if (options.oneofs)
                    object.sidecarRecords = "actions";
            }
            if (message.bytecode != null && $Object.hasOwnProperty.call(message, "bytecode")) {
                object.bytecode = $root.proto.ContractBytecode.toObject(message.bytecode, options, _depth + 1);
                if (options.oneofs)
                    object.sidecarRecords = "bytecode";
            }
            return object;
        };

        /**
         * Converts this TransactionSidecarRecord to JSON.
         * @function toJSON
         * @memberof proto.TransactionSidecarRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TransactionSidecarRecord.prototype.toJSON = function() {
            return TransactionSidecarRecord.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for TransactionSidecarRecord
         * @function getTypeUrl
         * @memberof proto.TransactionSidecarRecord
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        TransactionSidecarRecord.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TransactionSidecarRecord";
        };

        return TransactionSidecarRecord;
    })();

    proto.SignatureFile = (function() {

        /**
         * Properties of a SignatureFile.
         * @typedef {Object} proto.SignatureFile.$Properties
         * @property {proto.SignatureObject.$Properties|null} [fileSignature] Signature for the file
         * @property {proto.SignatureObject.$Properties|null} [metadataSignature] Metadata signature
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SignatureFile.
         * @memberof proto
         * @interface ISignatureFile
         * @augments proto.SignatureFile.$Properties
         * @deprecated Use proto.SignatureFile.$Properties instead.
         */

        /**
         * Shape of a SignatureFile.
         * @typedef {proto.SignatureFile.$Properties} proto.SignatureFile.$Shape
         */

        /**
         * Constructs a new SignatureFile.
         * @memberof proto
         * @classdesc The record signature file which is created for each record stream file
         * that signs the hash of the entire corresponding stream file.
         * @constructor
         * @param {proto.SignatureFile.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SignatureFile = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * Signature for the file
         * @member {proto.SignatureObject.$Properties|null|undefined} fileSignature
         * @memberof proto.SignatureFile
         * @instance
         */
        SignatureFile.prototype.fileSignature = null;

        /**
         * Metadata signature
         * @member {proto.SignatureObject.$Properties|null|undefined} metadataSignature
         * @memberof proto.SignatureFile
         * @instance
         */
        SignatureFile.prototype.metadataSignature = null;

        /**
         * Creates a new SignatureFile instance using the specified properties.
         * @function create
         * @memberof proto.SignatureFile
         * @static
         * @param {proto.SignatureFile.$Properties=} [properties] Properties to set
         * @returns {proto.SignatureFile} SignatureFile instance
         * @type {{
         *   (properties: proto.SignatureFile.$Shape): proto.SignatureFile & proto.SignatureFile.$Shape;
         *   (properties?: proto.SignatureFile.$Properties): proto.SignatureFile;
         * }}
         */
        SignatureFile.create = function(properties) {
            return new SignatureFile(properties);
        };

        /**
         * Encodes the specified SignatureFile message. Does not implicitly {@link proto.SignatureFile.verify|verify} messages.
         * @function encode
         * @memberof proto.SignatureFile
         * @static
         * @param {proto.SignatureFile.$Properties} message SignatureFile message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignatureFile.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.fileSignature != null && $Object.hasOwnProperty.call(message, "fileSignature"))
                $root.proto.SignatureObject.encode(message.fileSignature, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
            if (message.metadataSignature != null && $Object.hasOwnProperty.call(message, "metadataSignature"))
                $root.proto.SignatureObject.encode(message.metadataSignature, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignatureFile message, length delimited. Does not implicitly {@link proto.SignatureFile.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SignatureFile
         * @static
         * @param {proto.SignatureFile.$Properties} message SignatureFile message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignatureFile.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SignatureFile message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SignatureFile
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SignatureFile & proto.SignatureFile.$Shape} SignatureFile
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignatureFile.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SignatureFile();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.fileSignature = $root.proto.SignatureObject.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileSignature);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.metadataSignature = $root.proto.SignatureObject.decode(reader, reader.uint32(), $undefined, _depth + 1, message.metadataSignature);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignatureFile message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SignatureFile
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SignatureFile & proto.SignatureFile.$Shape} SignatureFile
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignatureFile.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignatureFile message.
         * @function verify
         * @memberof proto.SignatureFile
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignatureFile.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fileSignature != null && $Object.hasOwnProperty.call(message, "fileSignature")) {
                let error = $root.proto.SignatureObject.verify(message.fileSignature, _depth + 1);
                if (error)
                    return "fileSignature." + error;
            }
            if (message.metadataSignature != null && $Object.hasOwnProperty.call(message, "metadataSignature")) {
                let error = $root.proto.SignatureObject.verify(message.metadataSignature, _depth + 1);
                if (error)
                    return "metadataSignature." + error;
            }
            return null;
        };

        /**
         * Creates a SignatureFile message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SignatureFile
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SignatureFile} SignatureFile
         */
        SignatureFile.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SignatureFile)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SignatureFile: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SignatureFile();
            if (object.fileSignature != null) {
                if (!$util.isObject(object.fileSignature))
                    throw $TypeError(".proto.SignatureFile.fileSignature: object expected");
                message.fileSignature = $root.proto.SignatureObject.fromObject(object.fileSignature, _depth + 1);
            }
            if (object.metadataSignature != null) {
                if (!$util.isObject(object.metadataSignature))
                    throw $TypeError(".proto.SignatureFile.metadataSignature: object expected");
                message.metadataSignature = $root.proto.SignatureObject.fromObject(object.metadataSignature, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SignatureFile message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SignatureFile
         * @static
         * @param {proto.SignatureFile} message SignatureFile
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignatureFile.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.fileSignature = null;
                object.metadataSignature = null;
            }
            if (message.fileSignature != null && $Object.hasOwnProperty.call(message, "fileSignature"))
                object.fileSignature = $root.proto.SignatureObject.toObject(message.fileSignature, options, _depth + 1);
            if (message.metadataSignature != null && $Object.hasOwnProperty.call(message, "metadataSignature"))
                object.metadataSignature = $root.proto.SignatureObject.toObject(message.metadataSignature, options, _depth + 1);
            return object;
        };

        /**
         * Converts this SignatureFile to JSON.
         * @function toJSON
         * @memberof proto.SignatureFile
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignatureFile.prototype.toJSON = function() {
            return SignatureFile.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignatureFile
         * @function getTypeUrl
         * @memberof proto.SignatureFile
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignatureFile.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SignatureFile";
        };

        return SignatureFile;
    })();

    proto.SignatureObject = (function() {

        /**
         * Properties of a SignatureObject.
         * @typedef {Object} proto.SignatureObject.$Properties
         * @property {proto.SignatureType|null} [type] The signature type
         * @property {number|null} [length] Signature length
         * @property {number|null} [checksum] Signature checksum
         * @property {Uint8Array|null} [signature] Signature bytes
         * @property {proto.HashObject.$Properties|null} [hashObject] The hash that is signed by this signature
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SignatureObject.
         * @memberof proto
         * @interface ISignatureObject
         * @augments proto.SignatureObject.$Properties
         * @deprecated Use proto.SignatureObject.$Properties instead.
         */

        /**
         * Shape of a SignatureObject.
         * @typedef {proto.SignatureObject.$Properties} proto.SignatureObject.$Shape
         */

        /**
         * Constructs a new SignatureObject.
         * @memberof proto
         * @classdesc A Signature defined by its type, length, checksum and signature bytes and the hash that is signed
         * @constructor
         * @param {proto.SignatureObject.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        const SignatureObject = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * The signature type
         * @member {proto.SignatureType} type
         * @memberof proto.SignatureObject
         * @instance
         */
        SignatureObject.prototype.type = 0;

        /**
         * Signature length
         * @member {number} length
         * @memberof proto.SignatureObject
         * @instance
         */
        SignatureObject.prototype.length = 0;

        /**
         * Signature checksum
         * @member {number} checksum
         * @memberof proto.SignatureObject
         * @instance
         */
        SignatureObject.prototype.checksum = 0;

        /**
         * Signature bytes
         * @member {Uint8Array} signature
         * @memberof proto.SignatureObject
         * @instance
         */
        SignatureObject.prototype.signature = $util.newBuffer([]);

        /**
         * The hash that is signed by this signature
         * @member {proto.HashObject.$Properties|null|undefined} hashObject
         * @memberof proto.SignatureObject
         * @instance
         */
        SignatureObject.prototype.hashObject = null;

        /**
         * Creates a new SignatureObject instance using the specified properties.
         * @function create
         * @memberof proto.SignatureObject
         * @static
         * @param {proto.SignatureObject.$Properties=} [properties] Properties to set
         * @returns {proto.SignatureObject} SignatureObject instance
         * @type {{
         *   (properties: proto.SignatureObject.$Shape): proto.SignatureObject & proto.SignatureObject.$Shape;
         *   (properties?: proto.SignatureObject.$Properties): proto.SignatureObject;
         * }}
         */
        SignatureObject.create = function(properties) {
            return new SignatureObject(properties);
        };

        /**
         * Encodes the specified SignatureObject message. Does not implicitly {@link proto.SignatureObject.verify|verify} messages.
         * @function encode
         * @memberof proto.SignatureObject
         * @static
         * @param {proto.SignatureObject.$Properties} message SignatureObject message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignatureObject.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.type != null && $Object.hasOwnProperty.call(message, "type") && message.type !== 0)
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.length != null && $Object.hasOwnProperty.call(message, "length") && message.length !== 0)
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.length);
            if (message.checksum != null && $Object.hasOwnProperty.call(message, "checksum") && message.checksum !== 0)
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.checksum);
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature") && message.signature.length)
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.signature);
            if (message.hashObject != null && $Object.hasOwnProperty.call(message, "hashObject"))
                $root.proto.HashObject.encode(message.hashObject, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignatureObject message, length delimited. Does not implicitly {@link proto.SignatureObject.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SignatureObject
         * @static
         * @param {proto.SignatureObject.$Properties} message SignatureObject message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignatureObject.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SignatureObject message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SignatureObject
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SignatureObject & proto.SignatureObject.$Shape} SignatureObject
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignatureObject.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.proto.SignatureObject();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.length = value;
                        else
                            delete message.length;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.checksum = value;
                        else
                            delete message.checksum;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.signature = value;
                        else
                            delete message.signature;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.hashObject = $root.proto.HashObject.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hashObject);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignatureObject message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SignatureObject
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SignatureObject & proto.SignatureObject.$Shape} SignatureObject
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignatureObject.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignatureObject message.
         * @function verify
         * @memberof proto.SignatureObject
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignatureObject.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                    return "type: enum value expected";
            if (message.length != null && $Object.hasOwnProperty.call(message, "length"))
                if (!$util.isInteger(message.length))
                    return "length: integer expected";
            if (message.checksum != null && $Object.hasOwnProperty.call(message, "checksum"))
                if (!$util.isInteger(message.checksum))
                    return "checksum: integer expected";
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            if (message.hashObject != null && $Object.hasOwnProperty.call(message, "hashObject")) {
                let error = $root.proto.HashObject.verify(message.hashObject, _depth + 1);
                if (error)
                    return "hashObject." + error;
            }
            return null;
        };

        /**
         * Creates a SignatureObject message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SignatureObject
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SignatureObject} SignatureObject
         */
        SignatureObject.fromObject = function (object, _depth) {
            if (object instanceof $root.proto.SignatureObject)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".proto.SignatureObject: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.proto.SignatureObject();
            if (object.type !== 0 && (typeof object.type !== "string" || $root.proto.SignatureType[object.type] !== 0))
                switch (object.type) {
                case "SIGNATURE_TYPE_UNKNOWN":
                case 0:
                    message.type = 0;
                    break;
                case "SHA_384_WITH_RSA":
                case 1:
                    message.type = 1;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
            if (object.length != null)
                if ($Number(object.length) !== 0)
                    message.length = object.length | 0;
            if (object.checksum != null)
                if ($Number(object.checksum) !== 0)
                    message.checksum = object.checksum | 0;
            if (object.signature != null)
                if (object.signature.length)
                    if (typeof object.signature === "string")
                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                    else if (object.signature.length >= 0)
                        message.signature = object.signature;
            if (object.hashObject != null) {
                if (!$util.isObject(object.hashObject))
                    throw $TypeError(".proto.SignatureObject.hashObject: object expected");
                message.hashObject = $root.proto.HashObject.fromObject(object.hashObject, _depth + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SignatureObject message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SignatureObject
         * @static
         * @param {proto.SignatureObject} message SignatureObject
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignatureObject.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.type = options.enums === $String ? "SIGNATURE_TYPE_UNKNOWN" : 0;
                object.length = 0;
                object.checksum = 0;
                if (options.bytes === $String)
                    object.signature = "";
                else {
                    object.signature = [];
                    if (options.bytes !== $Array)
                        object.signature = $util.newBuffer(object.signature);
                }
                object.hashObject = null;
            }
            if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === $String ? $root.proto.SignatureType[message.type] === $undefined ? message.type : $root.proto.SignatureType[message.type] : message.type;
            if (message.length != null && $Object.hasOwnProperty.call(message, "length"))
                object.length = message.length;
            if (message.checksum != null && $Object.hasOwnProperty.call(message, "checksum"))
                object.checksum = message.checksum;
            if (message.signature != null && $Object.hasOwnProperty.call(message, "signature"))
                object.signature = options.bytes === $String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.signature) : message.signature;
            if (message.hashObject != null && $Object.hasOwnProperty.call(message, "hashObject"))
                object.hashObject = $root.proto.HashObject.toObject(message.hashObject, options, _depth + 1);
            return object;
        };

        /**
         * Converts this SignatureObject to JSON.
         * @function toJSON
         * @memberof proto.SignatureObject
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignatureObject.prototype.toJSON = function() {
            return SignatureObject.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SignatureObject
         * @function getTypeUrl
         * @memberof proto.SignatureObject
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SignatureObject.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SignatureObject";
        };

        return SignatureObject;
    })();

    /**
     * The signature type
     * @name proto.SignatureType
     * @enum {number}
     * @property {number} SIGNATURE_TYPE_UNKNOWN=0 SIGNATURE_TYPE_UNKNOWN value
     * @property {number} SHA_384_WITH_RSA=1 SHA_384_WITH_RSA value
     */
    proto.SignatureType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "SIGNATURE_TYPE_UNKNOWN"] = 0;
        values[valuesById[1] = "SHA_384_WITH_RSA"] = 1;
        return values;
    })();

    return proto;
})();

export const google = $root.google = (() => {

    /**
     * Namespace google.
     * @exports google
     * @namespace
     */
    const google = {};

    google.protobuf = (function() {

        /**
         * Namespace protobuf.
         * @memberof google
         * @namespace
         */
        const protobuf = {};

        protobuf.DoubleValue = (function() {

            /**
             * Properties of a DoubleValue.
             * @typedef {Object} google.protobuf.DoubleValue.$Properties
             * @property {number|null} [value] DoubleValue value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DoubleValue.
             * @memberof google.protobuf
             * @interface IDoubleValue
             * @augments google.protobuf.DoubleValue.$Properties
             * @deprecated Use google.protobuf.DoubleValue.$Properties instead.
             */

            /**
             * Shape of a DoubleValue.
             * @typedef {google.protobuf.DoubleValue.$Properties} google.protobuf.DoubleValue.$Shape
             */

            /**
             * Constructs a new DoubleValue.
             * @memberof google.protobuf
             * @classdesc Represents a DoubleValue.
             * @constructor
             * @param {google.protobuf.DoubleValue.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const DoubleValue = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DoubleValue value.
             * @member {number} value
             * @memberof google.protobuf.DoubleValue
             * @instance
             */
            DoubleValue.prototype.value = 0;

            /**
             * Creates a new DoubleValue instance using the specified properties.
             * @function create
             * @memberof google.protobuf.DoubleValue
             * @static
             * @param {google.protobuf.DoubleValue.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.DoubleValue} DoubleValue instance
             * @type {{
             *   (properties: google.protobuf.DoubleValue.$Shape): google.protobuf.DoubleValue & google.protobuf.DoubleValue.$Shape;
             *   (properties?: google.protobuf.DoubleValue.$Properties): google.protobuf.DoubleValue;
             * }}
             */
            DoubleValue.create = function(properties) {
                return new DoubleValue(properties);
            };

            /**
             * Encodes the specified DoubleValue message. Does not implicitly {@link google.protobuf.DoubleValue.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.DoubleValue
             * @static
             * @param {google.protobuf.DoubleValue.$Properties} message DoubleValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DoubleValue.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && !$Object.is(message.value, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DoubleValue message, length delimited. Does not implicitly {@link google.protobuf.DoubleValue.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.DoubleValue
             * @static
             * @param {google.protobuf.DoubleValue.$Properties} message DoubleValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DoubleValue.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a DoubleValue message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.DoubleValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.DoubleValue & google.protobuf.DoubleValue.$Shape} DoubleValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DoubleValue.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.DoubleValue();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a DoubleValue message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.DoubleValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.DoubleValue & google.protobuf.DoubleValue.$Shape} DoubleValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DoubleValue.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DoubleValue message.
             * @function verify
             * @memberof google.protobuf.DoubleValue
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DoubleValue.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof message.value !== "number")
                        return "value: number expected";
                return null;
            };

            /**
             * Creates a DoubleValue message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.DoubleValue
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.DoubleValue} DoubleValue
             */
            DoubleValue.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.DoubleValue)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.DoubleValue: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.DoubleValue();
                if (object.value != null)
                    if (!$Object.is($Number(object.value), 0))
                        message.value = $Number(object.value);
                return message;
            };

            /**
             * Creates a plain object from a DoubleValue message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.DoubleValue
             * @static
             * @param {google.protobuf.DoubleValue} message DoubleValue
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DoubleValue.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.value = 0;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = options.json && !$isFinite(message.value) ? $String(message.value) : message.value;
                return object;
            };

            /**
             * Converts this DoubleValue to JSON.
             * @function toJSON
             * @memberof google.protobuf.DoubleValue
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DoubleValue.prototype.toJSON = function() {
                return DoubleValue.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DoubleValue
             * @function getTypeUrl
             * @memberof google.protobuf.DoubleValue
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DoubleValue.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.DoubleValue";
            };

            return DoubleValue;
        })();

        protobuf.FloatValue = (function() {

            /**
             * Properties of a FloatValue.
             * @typedef {Object} google.protobuf.FloatValue.$Properties
             * @property {number|null} [value] FloatValue value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FloatValue.
             * @memberof google.protobuf
             * @interface IFloatValue
             * @augments google.protobuf.FloatValue.$Properties
             * @deprecated Use google.protobuf.FloatValue.$Properties instead.
             */

            /**
             * Shape of a FloatValue.
             * @typedef {google.protobuf.FloatValue.$Properties} google.protobuf.FloatValue.$Shape
             */

            /**
             * Constructs a new FloatValue.
             * @memberof google.protobuf
             * @classdesc Represents a FloatValue.
             * @constructor
             * @param {google.protobuf.FloatValue.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const FloatValue = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FloatValue value.
             * @member {number} value
             * @memberof google.protobuf.FloatValue
             * @instance
             */
            FloatValue.prototype.value = 0;

            /**
             * Creates a new FloatValue instance using the specified properties.
             * @function create
             * @memberof google.protobuf.FloatValue
             * @static
             * @param {google.protobuf.FloatValue.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.FloatValue} FloatValue instance
             * @type {{
             *   (properties: google.protobuf.FloatValue.$Shape): google.protobuf.FloatValue & google.protobuf.FloatValue.$Shape;
             *   (properties?: google.protobuf.FloatValue.$Properties): google.protobuf.FloatValue;
             * }}
             */
            FloatValue.create = function(properties) {
                return new FloatValue(properties);
            };

            /**
             * Encodes the specified FloatValue message. Does not implicitly {@link google.protobuf.FloatValue.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.FloatValue
             * @static
             * @param {google.protobuf.FloatValue.$Properties} message FloatValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FloatValue.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && !$Object.is(message.value, 0))
                    writer.uint32(/* id 1, wireType 5 =*/13).float(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FloatValue message, length delimited. Does not implicitly {@link google.protobuf.FloatValue.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.FloatValue
             * @static
             * @param {google.protobuf.FloatValue.$Properties} message FloatValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FloatValue.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a FloatValue message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.FloatValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.FloatValue & google.protobuf.FloatValue.$Shape} FloatValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FloatValue.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.FloatValue();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 5)
                                break;
                            if (!$Object.is(value = reader.float(), 0))
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FloatValue message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.FloatValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.FloatValue & google.protobuf.FloatValue.$Shape} FloatValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FloatValue.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FloatValue message.
             * @function verify
             * @memberof google.protobuf.FloatValue
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FloatValue.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof message.value !== "number")
                        return "value: number expected";
                return null;
            };

            /**
             * Creates a FloatValue message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.FloatValue
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.FloatValue} FloatValue
             */
            FloatValue.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.FloatValue)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.FloatValue: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.FloatValue();
                if (object.value != null)
                    if (!$Object.is($Number(object.value), 0))
                        message.value = $Number(object.value);
                return message;
            };

            /**
             * Creates a plain object from a FloatValue message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.FloatValue
             * @static
             * @param {google.protobuf.FloatValue} message FloatValue
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FloatValue.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.value = 0;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = options.json && !$isFinite(message.value) ? $String(message.value) : message.value;
                return object;
            };

            /**
             * Converts this FloatValue to JSON.
             * @function toJSON
             * @memberof google.protobuf.FloatValue
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FloatValue.prototype.toJSON = function() {
                return FloatValue.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FloatValue
             * @function getTypeUrl
             * @memberof google.protobuf.FloatValue
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FloatValue.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.FloatValue";
            };

            return FloatValue;
        })();

        protobuf.Int64Value = (function() {

            /**
             * Properties of an Int64Value.
             * @typedef {Object} google.protobuf.Int64Value.$Properties
             * @property {number|Long|null} [value] Int64Value value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an Int64Value.
             * @memberof google.protobuf
             * @interface IInt64Value
             * @augments google.protobuf.Int64Value.$Properties
             * @deprecated Use google.protobuf.Int64Value.$Properties instead.
             */

            /**
             * Shape of an Int64Value.
             * @typedef {google.protobuf.Int64Value.$Properties} google.protobuf.Int64Value.$Shape
             */

            /**
             * Constructs a new Int64Value.
             * @memberof google.protobuf
             * @classdesc Represents an Int64Value.
             * @constructor
             * @param {google.protobuf.Int64Value.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Int64Value = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Int64Value value.
             * @member {number|Long} value
             * @memberof google.protobuf.Int64Value
             * @instance
             */
            Int64Value.prototype.value = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Creates a new Int64Value instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Int64Value
             * @static
             * @param {google.protobuf.Int64Value.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.Int64Value} Int64Value instance
             * @type {{
             *   (properties: google.protobuf.Int64Value.$Shape): google.protobuf.Int64Value & google.protobuf.Int64Value.$Shape;
             *   (properties?: google.protobuf.Int64Value.$Properties): google.protobuf.Int64Value;
             * }}
             */
            Int64Value.create = function(properties) {
                return new Int64Value(properties);
            };

            /**
             * Encodes the specified Int64Value message. Does not implicitly {@link google.protobuf.Int64Value.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Int64Value
             * @static
             * @param {google.protobuf.Int64Value.$Properties} message Int64Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Int64Value.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && (typeof message.value === "object" ? message.value.low || message.value.high : message.value !== 0))
                    writer.uint32(/* id 1, wireType 0 =*/8).int64(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Int64Value message, length delimited. Does not implicitly {@link google.protobuf.Int64Value.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.Int64Value
             * @static
             * @param {google.protobuf.Int64Value.$Properties} message Int64Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Int64Value.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an Int64Value message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Int64Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Int64Value & google.protobuf.Int64Value.$Shape} Int64Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Int64Value.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.Int64Value();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an Int64Value message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.Int64Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.Int64Value & google.protobuf.Int64Value.$Shape} Int64Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Int64Value.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an Int64Value message.
             * @function verify
             * @memberof google.protobuf.Int64Value
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Int64Value.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isInteger(message.value) && !(message.value && $util.isInteger(message.value.low) && $util.isInteger(message.value.high)))
                        return "value: integer|Long expected";
                return null;
            };

            /**
             * Creates an Int64Value message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Int64Value
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Int64Value} Int64Value
             */
            Int64Value.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.Int64Value)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.Int64Value: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.Int64Value();
                if (object.value != null)
                    if (typeof object.value === "object" ? object.value.low || object.value.high : $Number(object.value) !== 0)
                        if ($util.Long)
                            message.value = $util.Long.fromValue(object.value, false);
                        else if (typeof object.value === "string")
                            message.value = $parseInt(object.value, 10);
                        else if (typeof object.value === "number")
                            message.value = object.value;
                        else if (typeof object.value === "object")
                            message.value = new $util.LongBits(object.value.low >>> 0, object.value.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from an Int64Value message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Int64Value
             * @static
             * @param {google.protobuf.Int64Value} message Int64Value
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Int64Value.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.value = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.value = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.value = typeof message.value === "number" ? $BigInt(message.value) : $util.Long.fromBits(message.value.low >>> 0, message.value.high >>> 0, false).toBigInt();
                    else if (typeof message.value === "number")
                        object.value = options.longs === $String ? $String(message.value) : message.value;
                    else
                        object.value = options.longs === $String ? $util.Long.prototype.toString.call(message.value) : options.longs === $Number ? new $util.LongBits(message.value.low >>> 0, message.value.high >>> 0).toNumber() : message.value;
                return object;
            };

            /**
             * Converts this Int64Value to JSON.
             * @function toJSON
             * @memberof google.protobuf.Int64Value
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Int64Value.prototype.toJSON = function() {
                return Int64Value.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Int64Value
             * @function getTypeUrl
             * @memberof google.protobuf.Int64Value
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Int64Value.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.Int64Value";
            };

            return Int64Value;
        })();

        protobuf.UInt64Value = (function() {

            /**
             * Properties of a UInt64Value.
             * @typedef {Object} google.protobuf.UInt64Value.$Properties
             * @property {number|Long|null} [value] UInt64Value value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a UInt64Value.
             * @memberof google.protobuf
             * @interface IUInt64Value
             * @augments google.protobuf.UInt64Value.$Properties
             * @deprecated Use google.protobuf.UInt64Value.$Properties instead.
             */

            /**
             * Shape of a UInt64Value.
             * @typedef {google.protobuf.UInt64Value.$Properties} google.protobuf.UInt64Value.$Shape
             */

            /**
             * Constructs a new UInt64Value.
             * @memberof google.protobuf
             * @classdesc Represents a UInt64Value.
             * @constructor
             * @param {google.protobuf.UInt64Value.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const UInt64Value = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * UInt64Value value.
             * @member {number|Long} value
             * @memberof google.protobuf.UInt64Value
             * @instance
             */
            UInt64Value.prototype.value = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * Creates a new UInt64Value instance using the specified properties.
             * @function create
             * @memberof google.protobuf.UInt64Value
             * @static
             * @param {google.protobuf.UInt64Value.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.UInt64Value} UInt64Value instance
             * @type {{
             *   (properties: google.protobuf.UInt64Value.$Shape): google.protobuf.UInt64Value & google.protobuf.UInt64Value.$Shape;
             *   (properties?: google.protobuf.UInt64Value.$Properties): google.protobuf.UInt64Value;
             * }}
             */
            UInt64Value.create = function(properties) {
                return new UInt64Value(properties);
            };

            /**
             * Encodes the specified UInt64Value message. Does not implicitly {@link google.protobuf.UInt64Value.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.UInt64Value
             * @static
             * @param {google.protobuf.UInt64Value.$Properties} message UInt64Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UInt64Value.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && (typeof message.value === "object" ? message.value.low || message.value.high : message.value !== 0))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UInt64Value message, length delimited. Does not implicitly {@link google.protobuf.UInt64Value.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.UInt64Value
             * @static
             * @param {google.protobuf.UInt64Value.$Properties} message UInt64Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UInt64Value.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a UInt64Value message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.UInt64Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.UInt64Value & google.protobuf.UInt64Value.$Shape} UInt64Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UInt64Value.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.UInt64Value();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a UInt64Value message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.UInt64Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.UInt64Value & google.protobuf.UInt64Value.$Shape} UInt64Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UInt64Value.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a UInt64Value message.
             * @function verify
             * @memberof google.protobuf.UInt64Value
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UInt64Value.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isInteger(message.value) && !(message.value && $util.isInteger(message.value.low) && $util.isInteger(message.value.high)))
                        return "value: integer|Long expected";
                return null;
            };

            /**
             * Creates a UInt64Value message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.UInt64Value
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.UInt64Value} UInt64Value
             */
            UInt64Value.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.UInt64Value)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.UInt64Value: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.UInt64Value();
                if (object.value != null)
                    if (typeof object.value === "object" ? object.value.low || object.value.high : $Number(object.value) !== 0)
                        if ($util.Long)
                            message.value = $util.Long.fromValue(object.value, true);
                        else if (typeof object.value === "string")
                            message.value = $parseInt(object.value, 10);
                        else if (typeof object.value === "number")
                            message.value = object.value;
                        else if (typeof object.value === "object")
                            message.value = new $util.LongBits(object.value.low >>> 0, object.value.high >>> 0).toNumber(true);
                return message;
            };

            /**
             * Creates a plain object from a UInt64Value message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.UInt64Value
             * @static
             * @param {google.protobuf.UInt64Value} message UInt64Value
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UInt64Value.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.value = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.value = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.value = typeof message.value === "number" ? $BigInt(message.value) : $util.Long.fromBits(message.value.low >>> 0, message.value.high >>> 0, true).toBigInt();
                    else if (typeof message.value === "number")
                        object.value = options.longs === $String ? $String(message.value) : message.value;
                    else
                        object.value = options.longs === $String ? $util.Long.prototype.toString.call(message.value) : options.longs === $Number ? new $util.LongBits(message.value.low >>> 0, message.value.high >>> 0).toNumber(true) : message.value;
                return object;
            };

            /**
             * Converts this UInt64Value to JSON.
             * @function toJSON
             * @memberof google.protobuf.UInt64Value
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UInt64Value.prototype.toJSON = function() {
                return UInt64Value.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for UInt64Value
             * @function getTypeUrl
             * @memberof google.protobuf.UInt64Value
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            UInt64Value.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.UInt64Value";
            };

            return UInt64Value;
        })();

        protobuf.Int32Value = (function() {

            /**
             * Properties of an Int32Value.
             * @typedef {Object} google.protobuf.Int32Value.$Properties
             * @property {number|null} [value] Int32Value value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an Int32Value.
             * @memberof google.protobuf
             * @interface IInt32Value
             * @augments google.protobuf.Int32Value.$Properties
             * @deprecated Use google.protobuf.Int32Value.$Properties instead.
             */

            /**
             * Shape of an Int32Value.
             * @typedef {google.protobuf.Int32Value.$Properties} google.protobuf.Int32Value.$Shape
             */

            /**
             * Constructs a new Int32Value.
             * @memberof google.protobuf
             * @classdesc Represents an Int32Value.
             * @constructor
             * @param {google.protobuf.Int32Value.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const Int32Value = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Int32Value value.
             * @member {number} value
             * @memberof google.protobuf.Int32Value
             * @instance
             */
            Int32Value.prototype.value = 0;

            /**
             * Creates a new Int32Value instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Int32Value
             * @static
             * @param {google.protobuf.Int32Value.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.Int32Value} Int32Value instance
             * @type {{
             *   (properties: google.protobuf.Int32Value.$Shape): google.protobuf.Int32Value & google.protobuf.Int32Value.$Shape;
             *   (properties?: google.protobuf.Int32Value.$Properties): google.protobuf.Int32Value;
             * }}
             */
            Int32Value.create = function(properties) {
                return new Int32Value(properties);
            };

            /**
             * Encodes the specified Int32Value message. Does not implicitly {@link google.protobuf.Int32Value.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Int32Value
             * @static
             * @param {google.protobuf.Int32Value.$Properties} message Int32Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Int32Value.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Int32Value message, length delimited. Does not implicitly {@link google.protobuf.Int32Value.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.Int32Value
             * @static
             * @param {google.protobuf.Int32Value.$Properties} message Int32Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Int32Value.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an Int32Value message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Int32Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Int32Value & google.protobuf.Int32Value.$Shape} Int32Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Int32Value.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.Int32Value();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an Int32Value message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.Int32Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.Int32Value & google.protobuf.Int32Value.$Shape} Int32Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Int32Value.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an Int32Value message.
             * @function verify
             * @memberof google.protobuf.Int32Value
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Int32Value.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                return null;
            };

            /**
             * Creates an Int32Value message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Int32Value
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Int32Value} Int32Value
             */
            Int32Value.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.Int32Value)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.Int32Value: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.Int32Value();
                if (object.value != null)
                    if ($Number(object.value) !== 0)
                        message.value = object.value | 0;
                return message;
            };

            /**
             * Creates a plain object from an Int32Value message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Int32Value
             * @static
             * @param {google.protobuf.Int32Value} message Int32Value
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Int32Value.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.value = 0;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this Int32Value to JSON.
             * @function toJSON
             * @memberof google.protobuf.Int32Value
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Int32Value.prototype.toJSON = function() {
                return Int32Value.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Int32Value
             * @function getTypeUrl
             * @memberof google.protobuf.Int32Value
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Int32Value.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.Int32Value";
            };

            return Int32Value;
        })();

        protobuf.UInt32Value = (function() {

            /**
             * Properties of a UInt32Value.
             * @typedef {Object} google.protobuf.UInt32Value.$Properties
             * @property {number|null} [value] UInt32Value value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a UInt32Value.
             * @memberof google.protobuf
             * @interface IUInt32Value
             * @augments google.protobuf.UInt32Value.$Properties
             * @deprecated Use google.protobuf.UInt32Value.$Properties instead.
             */

            /**
             * Shape of a UInt32Value.
             * @typedef {google.protobuf.UInt32Value.$Properties} google.protobuf.UInt32Value.$Shape
             */

            /**
             * Constructs a new UInt32Value.
             * @memberof google.protobuf
             * @classdesc Represents a UInt32Value.
             * @constructor
             * @param {google.protobuf.UInt32Value.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const UInt32Value = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * UInt32Value value.
             * @member {number} value
             * @memberof google.protobuf.UInt32Value
             * @instance
             */
            UInt32Value.prototype.value = 0;

            /**
             * Creates a new UInt32Value instance using the specified properties.
             * @function create
             * @memberof google.protobuf.UInt32Value
             * @static
             * @param {google.protobuf.UInt32Value.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.UInt32Value} UInt32Value instance
             * @type {{
             *   (properties: google.protobuf.UInt32Value.$Shape): google.protobuf.UInt32Value & google.protobuf.UInt32Value.$Shape;
             *   (properties?: google.protobuf.UInt32Value.$Properties): google.protobuf.UInt32Value;
             * }}
             */
            UInt32Value.create = function(properties) {
                return new UInt32Value(properties);
            };

            /**
             * Encodes the specified UInt32Value message. Does not implicitly {@link google.protobuf.UInt32Value.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.UInt32Value
             * @static
             * @param {google.protobuf.UInt32Value.$Properties} message UInt32Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UInt32Value.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UInt32Value message, length delimited. Does not implicitly {@link google.protobuf.UInt32Value.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.UInt32Value
             * @static
             * @param {google.protobuf.UInt32Value.$Properties} message UInt32Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UInt32Value.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a UInt32Value message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.UInt32Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.UInt32Value & google.protobuf.UInt32Value.$Shape} UInt32Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UInt32Value.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.UInt32Value();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a UInt32Value message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.UInt32Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.UInt32Value & google.protobuf.UInt32Value.$Shape} UInt32Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UInt32Value.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a UInt32Value message.
             * @function verify
             * @memberof google.protobuf.UInt32Value
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UInt32Value.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                return null;
            };

            /**
             * Creates a UInt32Value message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.UInt32Value
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.UInt32Value} UInt32Value
             */
            UInt32Value.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.UInt32Value)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.UInt32Value: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.UInt32Value();
                if (object.value != null)
                    if ($Number(object.value) !== 0)
                        message.value = object.value >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a UInt32Value message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.UInt32Value
             * @static
             * @param {google.protobuf.UInt32Value} message UInt32Value
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UInt32Value.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.value = 0;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this UInt32Value to JSON.
             * @function toJSON
             * @memberof google.protobuf.UInt32Value
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UInt32Value.prototype.toJSON = function() {
                return UInt32Value.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for UInt32Value
             * @function getTypeUrl
             * @memberof google.protobuf.UInt32Value
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            UInt32Value.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.UInt32Value";
            };

            return UInt32Value;
        })();

        protobuf.BoolValue = (function() {

            /**
             * Properties of a BoolValue.
             * @typedef {Object} google.protobuf.BoolValue.$Properties
             * @property {boolean|null} [value] BoolValue value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BoolValue.
             * @memberof google.protobuf
             * @interface IBoolValue
             * @augments google.protobuf.BoolValue.$Properties
             * @deprecated Use google.protobuf.BoolValue.$Properties instead.
             */

            /**
             * Shape of a BoolValue.
             * @typedef {google.protobuf.BoolValue.$Properties} google.protobuf.BoolValue.$Shape
             */

            /**
             * Constructs a new BoolValue.
             * @memberof google.protobuf
             * @classdesc Represents a BoolValue.
             * @constructor
             * @param {google.protobuf.BoolValue.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const BoolValue = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BoolValue value.
             * @member {boolean} value
             * @memberof google.protobuf.BoolValue
             * @instance
             */
            BoolValue.prototype.value = false;

            /**
             * Creates a new BoolValue instance using the specified properties.
             * @function create
             * @memberof google.protobuf.BoolValue
             * @static
             * @param {google.protobuf.BoolValue.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.BoolValue} BoolValue instance
             * @type {{
             *   (properties: google.protobuf.BoolValue.$Shape): google.protobuf.BoolValue & google.protobuf.BoolValue.$Shape;
             *   (properties?: google.protobuf.BoolValue.$Properties): google.protobuf.BoolValue;
             * }}
             */
            BoolValue.create = function(properties) {
                return new BoolValue(properties);
            };

            /**
             * Encodes the specified BoolValue message. Does not implicitly {@link google.protobuf.BoolValue.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.BoolValue
             * @static
             * @param {google.protobuf.BoolValue.$Properties} message BoolValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BoolValue.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== false)
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BoolValue message, length delimited. Does not implicitly {@link google.protobuf.BoolValue.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.BoolValue
             * @static
             * @param {google.protobuf.BoolValue.$Properties} message BoolValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BoolValue.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a BoolValue message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.BoolValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.BoolValue & google.protobuf.BoolValue.$Shape} BoolValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BoolValue.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.BoolValue();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a BoolValue message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.BoolValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.BoolValue & google.protobuf.BoolValue.$Shape} BoolValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BoolValue.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BoolValue message.
             * @function verify
             * @memberof google.protobuf.BoolValue
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BoolValue.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof message.value !== "boolean")
                        return "value: boolean expected";
                return null;
            };

            /**
             * Creates a BoolValue message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.BoolValue
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.BoolValue} BoolValue
             */
            BoolValue.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.BoolValue)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.BoolValue: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.BoolValue();
                if (object.value != null)
                    if (object.value)
                        message.value = $Boolean(object.value);
                return message;
            };

            /**
             * Creates a plain object from a BoolValue message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.BoolValue
             * @static
             * @param {google.protobuf.BoolValue} message BoolValue
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BoolValue.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.value = false;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this BoolValue to JSON.
             * @function toJSON
             * @memberof google.protobuf.BoolValue
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BoolValue.prototype.toJSON = function() {
                return BoolValue.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BoolValue
             * @function getTypeUrl
             * @memberof google.protobuf.BoolValue
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BoolValue.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.BoolValue";
            };

            return BoolValue;
        })();

        protobuf.StringValue = (function() {

            /**
             * Properties of a StringValue.
             * @typedef {Object} google.protobuf.StringValue.$Properties
             * @property {string|null} [value] StringValue value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StringValue.
             * @memberof google.protobuf
             * @interface IStringValue
             * @augments google.protobuf.StringValue.$Properties
             * @deprecated Use google.protobuf.StringValue.$Properties instead.
             */

            /**
             * Shape of a StringValue.
             * @typedef {google.protobuf.StringValue.$Properties} google.protobuf.StringValue.$Shape
             */

            /**
             * Constructs a new StringValue.
             * @memberof google.protobuf
             * @classdesc Represents a StringValue.
             * @constructor
             * @param {google.protobuf.StringValue.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const StringValue = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StringValue value.
             * @member {string} value
             * @memberof google.protobuf.StringValue
             * @instance
             */
            StringValue.prototype.value = "";

            /**
             * Creates a new StringValue instance using the specified properties.
             * @function create
             * @memberof google.protobuf.StringValue
             * @static
             * @param {google.protobuf.StringValue.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.StringValue} StringValue instance
             * @type {{
             *   (properties: google.protobuf.StringValue.$Shape): google.protobuf.StringValue & google.protobuf.StringValue.$Shape;
             *   (properties?: google.protobuf.StringValue.$Properties): google.protobuf.StringValue;
             * }}
             */
            StringValue.create = function(properties) {
                return new StringValue(properties);
            };

            /**
             * Encodes the specified StringValue message. Does not implicitly {@link google.protobuf.StringValue.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.StringValue
             * @static
             * @param {google.protobuf.StringValue.$Properties} message StringValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StringValue.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StringValue message, length delimited. Does not implicitly {@link google.protobuf.StringValue.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.StringValue
             * @static
             * @param {google.protobuf.StringValue.$Properties} message StringValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StringValue.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a StringValue message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.StringValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.StringValue & google.protobuf.StringValue.$Shape} StringValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StringValue.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.StringValue();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a StringValue message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.StringValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.StringValue & google.protobuf.StringValue.$Shape} StringValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StringValue.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StringValue message.
             * @function verify
             * @memberof google.protobuf.StringValue
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StringValue.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isString(message.value))
                        return "value: string expected";
                return null;
            };

            /**
             * Creates a StringValue message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.StringValue
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.StringValue} StringValue
             */
            StringValue.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.StringValue)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.StringValue: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.StringValue();
                if (object.value != null)
                    if (typeof object.value !== "string" || object.value.length)
                        message.value = $String(object.value);
                return message;
            };

            /**
             * Creates a plain object from a StringValue message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.StringValue
             * @static
             * @param {google.protobuf.StringValue} message StringValue
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StringValue.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.value = "";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this StringValue to JSON.
             * @function toJSON
             * @memberof google.protobuf.StringValue
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StringValue.prototype.toJSON = function() {
                return StringValue.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StringValue
             * @function getTypeUrl
             * @memberof google.protobuf.StringValue
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StringValue.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.StringValue";
            };

            return StringValue;
        })();

        protobuf.BytesValue = (function() {

            /**
             * Properties of a BytesValue.
             * @typedef {Object} google.protobuf.BytesValue.$Properties
             * @property {Uint8Array|null} [value] BytesValue value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BytesValue.
             * @memberof google.protobuf
             * @interface IBytesValue
             * @augments google.protobuf.BytesValue.$Properties
             * @deprecated Use google.protobuf.BytesValue.$Properties instead.
             */

            /**
             * Shape of a BytesValue.
             * @typedef {google.protobuf.BytesValue.$Properties} google.protobuf.BytesValue.$Shape
             */

            /**
             * Constructs a new BytesValue.
             * @memberof google.protobuf
             * @classdesc Represents a BytesValue.
             * @constructor
             * @param {google.protobuf.BytesValue.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            const BytesValue = function (properties) {
                if (properties)
                    for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BytesValue value.
             * @member {Uint8Array} value
             * @memberof google.protobuf.BytesValue
             * @instance
             */
            BytesValue.prototype.value = $util.newBuffer([]);

            /**
             * Creates a new BytesValue instance using the specified properties.
             * @function create
             * @memberof google.protobuf.BytesValue
             * @static
             * @param {google.protobuf.BytesValue.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.BytesValue} BytesValue instance
             * @type {{
             *   (properties: google.protobuf.BytesValue.$Shape): google.protobuf.BytesValue & google.protobuf.BytesValue.$Shape;
             *   (properties?: google.protobuf.BytesValue.$Properties): google.protobuf.BytesValue;
             * }}
             */
            BytesValue.create = function(properties) {
                return new BytesValue(properties);
            };

            /**
             * Encodes the specified BytesValue message. Does not implicitly {@link google.protobuf.BytesValue.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.BytesValue
             * @static
             * @param {google.protobuf.BytesValue.$Properties} message BytesValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BytesValue.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value.length)
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BytesValue message, length delimited. Does not implicitly {@link google.protobuf.BytesValue.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.BytesValue
             * @static
             * @param {google.protobuf.BytesValue.$Properties} message BytesValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BytesValue.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a BytesValue message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.BytesValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.BytesValue & google.protobuf.BytesValue.$Shape} BytesValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BytesValue.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                let end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.BytesValue();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.bytes()).length)
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a BytesValue message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.BytesValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.BytesValue & google.protobuf.BytesValue.$Shape} BytesValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BytesValue.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BytesValue message.
             * @function verify
             * @memberof google.protobuf.BytesValue
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BytesValue.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!(message.value && typeof message.value.length === "number" || $util.isString(message.value)))
                        return "value: buffer expected";
                return null;
            };

            /**
             * Creates a BytesValue message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.BytesValue
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.BytesValue} BytesValue
             */
            BytesValue.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.BytesValue)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.BytesValue: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let message = new $root.google.protobuf.BytesValue();
                if (object.value != null)
                    if (object.value.length)
                        if (typeof object.value === "string")
                            $util.base64.decode(object.value, message.value = $util.newBuffer($util.base64.length(object.value)), 0);
                        else if (object.value.length >= 0)
                            message.value = object.value;
                return message;
            };

            /**
             * Creates a plain object from a BytesValue message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.BytesValue
             * @static
             * @param {google.protobuf.BytesValue} message BytesValue
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BytesValue.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    if (options.bytes === $String)
                        object.value = "";
                    else {
                        object.value = [];
                        if (options.bytes !== $Array)
                            object.value = $util.newBuffer(object.value);
                    }
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = options.bytes === $String ? $util.base64.encode(message.value, 0, message.value.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.value) : message.value;
                return object;
            };

            /**
             * Converts this BytesValue to JSON.
             * @function toJSON
             * @memberof google.protobuf.BytesValue
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BytesValue.prototype.toJSON = function() {
                return BytesValue.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BytesValue
             * @function getTypeUrl
             * @memberof google.protobuf.BytesValue
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BytesValue.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.BytesValue";
            };

            return BytesValue;
        })();

        return protobuf;
    })();

    return google;
})();

export {
  $root as default
};
